# [REFINE_WORKFLOW]

[IMPORTANT]: We MUST maximize the entire planning mode time we have; ALL changes must be exhaustively reviewed, planned, and made with clear changes in ALL planning directories/files. We must have 4+ pages of planning content, during planning mode (we are in it right now) we can make local/tmp artifacts if needed with changes, additions, and ensure the plan we have has no gaps. When we leave the planning mode, we MUST have everything already identified to do, we do NOT want to do more research that we can't do now in planning, once we leave planning mode, everything needs to be identified to be done in exhaustive detail. The plan MUST be made with critique + red team passes for all the major phase work, and refined/pushed in quality, consideration, and detail, the expectaitons of our plan is 4x higher than what you think; do not waste the planning mode, we must use it for extreme detail, refinement/hardening of all content we will do this session. The plan must be created in a structured, detailed way, ALL work that we do should be done with workflows WHILE WE ARE IN PLANNING MODE, AND ONCE WE LEAVE IT, you are an orchestrator as much as possible, doing personal work only where surgical/needed. At the start of leaving the planning mode, you MUST re-read the entire plan + all artifacts we made, if any, create a proper task list. ALL agents/workflows/teams must re-read the plan for their phase work, fully, and they MUST read `docs/stacks/csharp` regardless of language target, understand these files, our `docs/stacks/csharp/README.md` for code doctrine, and other files in root of `docs/stacks/csharp` the code standard here is the FLOOR, ts/py code if we touch/consider it is far far weaker, and always needs refining/hardening to reduce surface, reduce object/shape/types with ultra-advanced/modern/bleeding-edge replacements to reduce LOC + surface + objects without losing functionality. This is a concern at all levels, we are NEVER making code files, we ALWAYS work on design docs, we can improve/refactor all tooling + configs at repo root like `pnpm-workspace.yaml`, `pyproject.toml`, `NuGet.config`, `global.json`, `Directory.Packages.props`, `Directory.Build.targets`, `Directory.Build.props`, and `.config`, etc... external packages/tooling is a big concern of ours, always refining/improving, adding most advanced/new tooling/configs when possible, and updating docs for them to be integrated (.api/ folders if needed, readme manifests, root repo docs if needed, etc)

THE PLANNING MODE CANNOT BE LEFT UNLESS IT IS EXHAUSTIVE, DETAILED, ADVANCED, COMPLEX/RICH, NO FLUFF/CHAFF, ALL CONSIDERATIONS/TASKS FULLY INVESTIGATED, RESOLVED, CRITIQUED/REFINED + RED-TEAMED

[IMPORTANT]: Server and native closure evidence comes from Assay `provision` reports. Planning prompts consume that evidence when a task needs live PG18, extension, DuckDB, or scientific-native facts. Read `tools/assay/README.md` for provison tool usage, ensure agents that need it are aware of it and how to use it.

[IMPORTANT]: For all workflows, it is 1-2 agents PER folder, the per folder pass, language-wide pass (not 1 agent for multiple folders), lib-wide pass (multiple agents per language) with synthesis and orchestration handled properly, never overload any agent to read more than 1 planning folder. Never burst a workflow, scope properly to have a phase slowly unroll agents (mainly a concern for lib-wide pass)

[PASS] REFINE over the `libs/` planning corpus. REFINE is the objective code-quality pass: its MAIN goal is the best possible refactoring of the architecture, the fenced code, and the prose in the design docs — unified rails and pipelines, collapsed surface into denser, richer, more-advanced owners, reduced LOC, closed gaps, capability never removed — to the `docs/stacks/csharp/` bar for every language. It adjusts ideas and tasks in parallel only as that refactoring reshapes them; it never executes a task, and it never runs IDEATE's systematic pool-construction or coverage audit; it emits a deferral only for a growth-potential structure it preserves. It is the deep code-and-doc hardening before the IMPLEMENT pass acts.

[METHOD] The campaign method, stated inline so the task is self-contained on entry; the routes below carry the full law and the complete doctrine.
- Surface: work ONLY on the `.planning/` design corpus — design pages and the transcription-complete code FENCES inside them — plus the four index docs per tier, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain. A FENCE is a markdown fenced block, the work product itself, never a `.cs`/`.py`/`.ts` source file.
- Bar: world-class library code — dense and polymorphic, one owner per axis, one rail family and one entrypoint per concern, total dispatch, no wrappers/shims/aliases, every admitted package mined to its full capability through the owning axis, capability conserved absolutely (a collapse proves every case, field, and method survived). Files are as large as the concern requires. `docs/stacks/<lang>/` is the floor and `docs/stacks/csharp/` the universal parity bar every branch meets whatever its language.
- Principles: one unified body of functionality — a new capability is a row, case, or column on an existing axis, never a parallel surface; full modality completeness from the root; everything parameterized; plug-in-play composition aligned only at the wire/companion seams, never coupled to a sibling's interior.
- Tiering + stages: run as fanned workflows across three altitudes — per-folder, then a full per-language pass, then the cross-`libs/` master, run BOTTOM-UP so each higher tier works a settled lower one. Each higher tier is a FULL pass at broader scope (the same research → produce → critique → red-team depth), never a thin reconcile: it re-grades and aligns the settled lower tier across folders, adds the cleanup the broader view surfaces, and applies the boundary-integrity check — a concern owned twice within a runtime, a folder mixing unrelated concerns, a concern scattered across folders, or a cross-folder/cross-language touchpoint coupled to a sibling's interior is a defect it fixes or records. Producer → critique → red-team, every stage applying its own fixes; a single un-adversaried author is never the final word.

[TARGETS]: READ ALL OF: `libs/.planning/planning-targets.md` 
- Contains all planning infrastructure folders that this session will target (lib scope, language scope, per folder).
- Read: `AGENTS.md` (repo root)
- Read: `README.md` (repo root)

[ROUTES]:
- `libs/.planning/campaign-method.md` — the full law behind `[METHOD]`: the bar, the universal principles, the REFINE pass, the workflow orchestration.
- `libs/.planning/architecture.md` — the strata, the `.planning/` lifecycle (single `<pkg>/.planning/<sub-domain>/`, the mature-folder exemption), and the per-language roles; the standard for folder design.
- `libs/.planning/README.md` — the four index-doc templates as the canonical shape every root file matches, the design-page grammar, the signature law (comment-free fences).
- `docs/standards/information-structure.md`, `formatting.md`, `style-guide.md` — the doc-craft law REFINE enforces: agent-first and implicit, no meta or provenance, md-tables only for true matrices, bracketed-heading idiom.
- `docs/stacks/<lang>/` for the tier's language is the density and code-shape doctrine every fence is held to; `docs/stacks/csharp/` is the parity bar.

[SCOPE] The PRIMARY, ultra-focused goal is the objective best refactoring of the architecture, the fenced code, and the prose in the design docs — pushed to the highest quality the doctrine admits. Ideas and tasks are adjusted in parallel only as that refactoring reshapes them, never executed. Implement every fix:
- Code quality (THE main concern): refactor every fence to the bar in `docs/stacks/csharp/` — read every file in it; the bar holds for ALL languages. Unify rails and pipelines; collapse parallel shapes, surface, and objects into fewer, denser, richer, more advanced and parameterized owners; reduce LOC where the collapse allows; unify features and capabilities; close every gap. Strictly improve quality and push code sophistication to its ceiling, conserving capability absolutely — a collapse proves every case and method survived, and NOTHING is removed.
- Architecture and folder design: split god pages into real sub-domain units; restructure or merge a mini folder/file pair with no growth potential; where a thin structure has genuine growth potential, preserve it and record one deferral task or idea to grow it. Sub-domains carry real professional domain names, never a rail/axis/lane scheme; the ARCHITECTURE map shows every sub-domain including planned ones.
- Doc-craft (prose): every root file matches the canonical index-doc template; strip meta, provenance, and drift; convert any md-table that is not a true matrix to a card, record, or tree; keep ideas, tasks, architecture, and README consistent.
- External packages: fix incorrect or missing package usage against the central language manifest and the README registry, and dissolve any stray per-folder manifest; fix `.api/` catalogue gaps and mistakes resolvable in place, recording a task for any it cannot close. Adopting a newer or stronger package or admitting a new one is IDEATE-recorded and IMPLEMENT-realized, not done here; REFINE records it as a deferral instead.
- Ideas and tasks (parallel adjustment, never execution): when the code refactoring reshapes an owner — a collapse, a unification, a closed gap — adjust the affected ideas and tasks to match (re-scope, merge, re-target, or drop a now-moot one), and sharpen or replace a low-value item the pass surfaces. This rides the refactoring across the tiers folder -> language -> cross-`libs/`; REFINE never executes a task or runs IDEATE's systematic creation.

[WORKFLOW] Construct the pass as fanned workflows, never one-off agents. Bottom-up tiering: folder, then a full language pass, then the cross-`libs/` master — each higher tier a FULL pass at broader scope (the same implement/critique/red-team depth), never a thin reconcile: it re-grades and aligns the settled lower tier across folders and adds the cleanup the broader view surfaces. Each workflow chains research -> implement -> critique -> red-team, every stage ULTRA-HARSH. Assign a capable model to authoring, red-team, and synthesis and a lighter model only to exploration and decompile reads. Keep concurrent heavy workflows at two or three. The orchestrating session is the sole ledger writer and never runs git from an agent. The same loop runs for every language against its own doctrine first and the C# bar second.

[BOUNDARY] REFINE implements fixes and never executes a task, and it does not run IDEATE's systematic top-down pool-construction or coverage-audit task-emission — its idea-and-task work is the PARALLEL adjustment that follows the code refactoring (re-scope, merge, re-target, drop a now-moot item, or sharpen a low-value one the pass surfaces), beyond the deferral note for a growth-potential structure it preserves. Every refactor conserves capability — a collapse proves every case and method survived. Density and rail-unification stay inside the fences and never bleed a rail/axis/lane term into an ARCHITECTURE sub-domain name. REFINE also removes any task that targets writing a source file (`.cs`/`.py`/`.ts` and the like) or transcribing or implementing code into source: a planning task never lands code. Source generation is a separate future project outside this campaign's passes; the session never enters it and defines no kickoff or pass for it, and every pass operates only on the `.planning/` design corpus (design pages and the transcription-complete code FENCES inside them), the four index docs, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain.

[BAR] A high-value REFINE turn leaves every fence at the highest code quality the doctrine admits — rails and pipelines unified, surface collapsed into denser richer owners, LOC reduced where the collapse allows, gaps closed, capability fully conserved — and every architecture map clean, every root file canonical, every god page split, every mini-folder either restructured or deferred with its growth note, every md-table that is not a true matrix converted, every fence free of comment litter, zero residual meta or provenance, and every idea or task the refactoring touched adjusted to match. The critique pass reads constructively across the full view; the red-team attacks every fence for a surface that could still collapse, a rail left un-unified, a gap left open, a capability silently dropped, plus residual meta, an md-table that should be a card, a non-canonical root file, a god page left un-split, a mini-folder left un-restructured without a growth deferral, comment litter, or an idea/task left stale by a code change — and fixes each in place.
