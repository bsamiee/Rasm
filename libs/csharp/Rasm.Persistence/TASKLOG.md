# [PERSISTENCE_TASKLOG]

The open and closed work for the durable-state spine, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup.

## [1]-[OPEN]

[T-TRACE-CHANGEFEED] [QUEUED]: Thread the W3C trace-context frame through the durable changefeed and the lineage CDC feed for the `TRACE_CONTEXT_CHANGEFEED` idea, realizing the `ONE_DISTRIBUTED_TRACE` concert seam on the op-log.
- Capability: add a `TraceContext` envelope slot (`traceparent`/`tracestate` bytes) to the `OpLogEntry` envelope at `sync/collaboration#OPLOG_CHANGEFEED` and to the `provenance#LINEAGE_CDC` `CdcEnvelope`, plus the extract-and-continue fold at `sync/collaboration#TRANSPORT_AXIS` drain and `stores-remote/object-store#ARTIFACT_SYNC_FEED` `Announce`/`Fetch` so a drain/announce continues the C#-minted parent context rather than minting an independent root.
- Packages: Npgsql.OpenTelemetry (driver-span instrumentation, settled), System.Diagnostics.Activity (BCL inbox); the propagation fold arrives settled from `AppHost/observability/diagnostics-and-telemetry#CORRELATION_SPINE` — never re-minted here.
- Integration: the `OpLogEntry` envelope and the CDC envelope carry the trace bytes internal to the folder; the cross-process extract-and-continue aligns to the `ONE_DISTRIBUTED_TRACE` cross-libs seam (C# owns the propagation fold, the Python `runtime/observability/receipts` and TS `platform/observability/metric-registry` legs extract-and-continue at the wire), never coupled to a peer interior; the attested-ledger entry and the CDC envelope inherit the originating trace for trace-correlated audit.
- Considerations: a second correlation owner is the deleted form — the trace context rides the existing envelope slot beside the HLC stamp and `CorrelationId`, never a parallel tracer; an op committed with no active span carries an empty context, never a synthesized root; the propagation fold and the `Propagators.DefaultTextMapPropagator` registration stay AppHost-owned.

[T-GRADUATION-IMPORT] [QUEUED]: Bind the content-keyed `ONE_GRADUATION_EVIDENCE` graduation handoff into the durable spine for the `GRADUATION_LEDGER_IMPORT` idea, the missing inward seam `ARCHITECTURE` names Persistence consuming.
- Capability: a `provenance#ATTESTED_LEDGER` cluster row admitting the `HandoffAxis` graduation evidence as one `AttestedEntry` keyed by its `ContentIdentity`, the `(checksum, OrtEpDevice)` provider fingerprint folded into the attestation tuple; a `GraduationAsset` kind row on `cache/indexes#ARTIFACT_BLOB_INDEX` `ArtifactIndexRow` content-addressing the `model-asset` ONNX artifact (post-`ModelAssetManifest` validation) identically to every other kind.
- Packages: System.IO.Hashing (content-address), System.Security.Cryptography (the attestation surface, settled), Microsoft.Extensions.Compliance.Redaction (settled); no new admission — the ONNX artifact is opaque bytes the blob index admits.
- Integration: the graduation evidence lands on the existing attested ledger and blob index internal to the folder; aligns to the `ONE_GRADUATION_EVIDENCE` cross-libs seam (Python `compute/graduation#GRADUATION` holds the rail singular, `AppHost/determinism/determinism-and-replay#EVENT_LOG` owns the determinism chain, this owner the durable residence) and the C# `SESSION_CAPSULE` key, never coupled to the Python or AppHost interior.
- Considerations: the surrogate crosses as a content-keyed ONNX artifact the C# side runs inference over, never an in-process training loop; the graduation asset rides the existing object-store residence and `redaction-retention#RETENTION_SWEEPS` `ClosureGc` reachability sweep so a stale surrogate is collected by the one GC; a re-import re-verifies the fingerprint so a provider-divergent fit is caught at admission, never at inference.

[T-REDIS-FABRIC] [QUEUED]: Deepen the `CacheResidence.Redis` tier from passive L2 blob store to live distributed-coordination fabric for the `REDIS_LIVE_FABRIC` idea, mining the unexploited `StackExchange.Redis` capability.
- Capability: at `cache/indexes#L2_CONTRIBUTION`, a RESP3 `ConnectionMultiplexer` server-assisted client-side-caching path invalidating L1 on the `__redis__:invalidate` channel, a `ISubscriber` keyspace-notification stream feeding the op-log HLC cursor as the cross-process tag-transition backplane, and an atomic `IServer`/`IDatabase` `ScriptEvaluate`/`FunctionLoad` Lua surface for the stampede single-flight and writer-lease fence — all gated so absent Redis the existing TTL-bounded path is the unchanged baseline.
- Packages: StackExchange.Redis, Microsoft.Extensions.Caching.StackExchangeRedis (both admitted); the RESP3 client-side-caching, keyspace-notification, and `FunctionLoad` member surface is the `.api/api-redis.md` decompile fold this task's API-pass leg verifies before the fence pins.
- Integration: deepens the one Persistence cache contribution row internal to the folder; AppHost owns the cache port, stampede policy, and tag vocabulary as settled (`AppHost/runtime#cache-pool-lanes`), never reversed; the keyspace-notification backplane refines the existing `cache/indexes#L2_CONTRIBUTION` peer-replay tag-transition path where Redis is present.
- Considerations: a second cache owner is the deleted form — every surface deepens the `CacheContribution` capsule and the `CacheResidence.Redis` row; the invalidation path is additive over the TTL-bounded baseline so a Redis-absent profile is unchanged; the Lua scripts and the keyspace-notification subscription are connection-instance state on the multiplexer, never persisted.

[T-PARITY-CORPUS] [QUEUED]: Mint the Persistence leg of the `ONE_WIRE_FIXTURE_CORPUS` content-identity golden corpus for the `CONTENT_PARITY_CORPUS` idea.
- Capability: a `versioning/version-control#CRDT_WIRE` cluster row emitting the frozen content-identity and HLC-stamp fixture bytes Persistence owns — the canonical `(SortedParents, SortedOpKeys, Hlc)` commit-key bytes, the sorted `ElementSet` receipt bytes, the `CrdtOpWire` op-set encoding, the `EmbeddingIdentity` seed, and the `Hlc.WriteTo` 16-byte cell — keyed by the one `XxHash128` seed.
- Packages: System.IO.Hashing (`XxHash128`), MessagePack (`CrdtOpWire` encoding), NodaTime (`Hlc` `Instant`); all admitted, no new surface.
- Integration: the fixture bytes are a content-addressed corpus row internal to the folder; aligns to the `ONE_CONTENT_IDENTITY`/`ONE_WIRE_FIXTURE_CORPUS` cross-libs seam (the corpus is keyed by the one `XxHash128` seed, two-64-bit-half order) consumed by the Python `runtime/identity/content-identity`, TS `interchange/artifacts/frame-reassembly`, and the C# shared test corpus, never coupled to a peer interior.
- Considerations: a per-harness re-derivation is the deleted form — the corpus is the one frozen-bytes owner every parity harness reads; the two-half HLC order and the seed-zero/two-64-bit-half content seed are the parity-critical invariants a corpus mismatch surfaces in one assertion; this is a fixture-byte fence on an existing wire page, never a new test source file.

[T-SCHEDULE-CPM] [QUEUED]: Deepen the schedule activity-network owner with the real CPM/baseline/resource-loading algebra for the `SCHEDULE_BASELINE_CASCADE` idea, retiring the single-longest-task critical-path stub.
- Capability: at `schedule/schedule-interchange#SCHEDULE_STORE`, a forward/backward float pass computing early/late start-finish and total/free float over the `TaskRelation` DAG (replacing the `CriticalPath` longest-duration stub with the float-zero activity set), a `ScheduleBaseline` content-addressed snapshot axis for multi-baseline variance, retained/progressed-logic scheduling against the data date over `ActualStart`/`ActualFinish`, and resource-loading columns on `ScheduleTask` for leveling and over-allocation folds.
- Packages: System.IO.Hashing (baseline content-address), NodaTime (`LocalDate`/`Duration` date math), LanguageExt.Core, Thinktecture.Runtime.Extensions; all admitted, no new surface.
- Integration: deepens the one activity-network owner internal to the folder; the CPM walk reuses the DAG-walk the commit-DAG and lineage-DAG use, the baseline rides the content-addressed snapshot identity, the 4D/5D fold at `#TASK_LINK_4D` consumes the float-correct activity dates, and the cost rollup (`profiles#COST_ROLLUP`) joins the resource-loaded set, never a per-metric calculator.
- Considerations: a per-format schedule library or a Primavera SDK is the deleted form — the XER rides the Sep tabular reader and the XML rides STJ as settled; a re-baseline is one content-keyed snapshot, never a per-baseline table; the CPM float pass is one DAG algebra serving CPM, lineage, and the commit-DAG.

## [2]-[CLOSED]

[T-SERVER-EXT-API] [COMPLETE]: Coverage-audit remediation — authored the four missing PG18 server-extension `.api/` catalogues (`pgvectorscale`/`pg-search`/`timescaledb`/`pg-server-bgworkers`) verifying the provisioning/search/document/audit fences that cited them, and added the `[SERVER_EXTENSIONS]` README group; live apply stays `[SERVER_PROVISIONING_PROBE]`.

[T-CRDT-PRESENCE] [COMPLETE]: Landed the sixth replicated type `CrdtField.EphemeralMap` + the `CrdtOp.Beat`/`Leave` delta arms on `versioning/version-control#CRDT_ALGEBRA` (per-origin LWW-by-HLC `Merge`, add-wins `Apply`, quiescence-horizon `Compact`); the `beat`(8)/`leave`(9) tags ride the one `CrdtOpWire` mint, the divergent `sync/collaboration` re-mint collapsed into it.

[T-DUCKLAKE] [COMPLETE]: Added `LakehouseCatalog`/`LakehouseKind`/`Lakehouse` to `data-lanes#ANALYTICAL_LANE` promoting the parquet export to a DuckLake v1.0 SQL-catalog lakehouse (per-backend `ATTACH`, `Snapshot`/`AsOfSql` time-travel via raw `DuckDBCommand` SQL, content-keyed object-store data files); catalog layout holds under `[DUCKLAKE_CATALOG]`.

[T-IDS-RULES] [COMPLETE]: Added the `IdsSpecification`/`IdsFacet`/`IdsImport` importer to `federation#RULE_PLAN` — `Parse` over `System.Xml.Linq`, `Scope` folding the six facets into one `SetExpr`, `Lower` projecting each requirement onto `RuleAst.Requires`/`Forbids` so a published IDS check runs the settled `RulePlan.Evaluate`; grammar holds under `[IDS_FACET_SCHEMA]`.

[T-TRANSPARENCY-PROOF] [COMPLETE]: Added the `TransparencyProof`/`InclusionProof`/`ConsistencyProof`/`HeadSeal` RFC 9162 Merkle owner to `provenance#ATTESTED_LEDGER` (O(log n) inclusion/consistency proofs over `XxHash128` nodes, `ECDsa` head seal, redaction-preserving `LeafDigest`); construction holds under `[MERKLE_PROOF_CONSTRUCTION]`.

[T-UNION-DISPATCH] [COMPLETE]: Converted the lossy-`_`-arm `[Union]` folds (`RuleAst`/`SetExpr`/`PlanNode`/`CrdtOp`) to the generated total `Switch` with `SwitchMethods` so a new case breaks the build; the tuple-`switch` merges and foreign `CrdtOpWire.Map` stay language `switch` per the product/foreign-type exemption.

[T-SPECKLE-BIND] [COMPLETE]: Full-bound the `SyncTransport.SpeckleLikeDiff` case to the `Speckle.Sdk` instance `IOperations.Send`/`Receive` on `sync/collaboration#TRANSPORT_AXIS` (`rootObjId` onto the one `ContentKey`, the inline `Closure` fork deleted, the inbound graph mapped to op-log entries at the seam); the heavy Speckle closure runs companion/outside-Rhino, faults lift once as `Error.New(8251)`, members verify against `.api/api-speckle.md`.

[T-ARROW-FLIGHT] [COMPLETE]: Bound the Arrow plane to an ADBC + Flight SQL zero-copy egress on `query-rail#ARROW_EGRESS` (the one carrier schema into a `RecordBatch`, `StoreFlightServer` `DoGet`/`DoExchange`, `AdbcEgress` over `IArrowArrayStream`); the server-side Flight/ADBC member surface is the `[ARROW_FLIGHT_SERVE]` decompile leg, live round-trip external-blocked.

[T-FASTCDC-DEDUP] [COMPLETE]: Replaced fixed-window framing with FastCDC content-defined chunking on `snapshots/codecs#CONTENT_CHUNKING` (gear-hash boundary, per-chunk `XxHash128` key, `Novel` projecting the chunks the blob index lacks so identical chunks dedup); the under-churn dedup ratio is the `[CDC_DEDUP_RATIO]` gate.

[T-LAYER-COMPOSE] [DROPPED]: USD/IFC5 layer opinion-composition dropped with its `[LAYERED_COMPOSITION]` idea — duplicated `versioning#COMMIT_DAG` branches + `CRDT_ALGEBRA` `MvRegister` + `TimeTravel.BranchFromPast`, a second composition surface breaking the closed-CRDT prohibition.

[T-DOC-MIGRATE] [COMPLETE]: Re-homed the design pages into the single `.planning/<sub-domain>/<page>.md` tree, rebuilt `ARCHITECTURE.md`/`README.md`, authored `IDEAS.md`/`TASKLOG.md`.
