extends: default

rules:
    line-length: disable
    indentation: disable
    document-start: disable
    truthy:
        allowed-values: ["true", "false", "off"]
        check-keys: false
    comments:
        min-spaces-from-content: 1
