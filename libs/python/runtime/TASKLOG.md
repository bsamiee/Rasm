# [PY_RUNTIME_TASKLOG]

Open and closed work for `runtime`, distilled from `IDEAS.md`. Each task card carries a status marker on its leader — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` when open; `[COMPLETE]` or `[DROPPED]` when closed — and three to four bullets: the design page or `.api/` catalogue to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks. A design-complete idea closes here; the downstream source-transcription mode is outside the planning task pool.

## [1]-[OPEN]

[QUEUED] [TRACE_INBOUND_EXTRACT]:
- Add the inbound extract-and-continue primitive to `observability/receipts#RECEIPT`: a `Signals.continue_inbound(carrier: dict[str, str]) -> Context` that runs `opentelemetry.propagate.extract(carrier)` (the global composite resolved through `propagate.get_global_textmap`) and a context-manager that attaches the extracted `Context` so the next measured span seeds from the C# parent rather than minting a root; the `trace_context` injecting processor stays, the extract is its inbound complement.
- Integrate `opentelemetry-api` (`propagate.extract`/`propagate.get_global_textmap`/`Context`/`TextMapPropagator`, branch `libs/python/.api/opentelemetry-api.md` — catalogue-backed, no admission); `serve#SERVE` reads the carrier off `grpc.aio.ServicerContext.invocation_metadata()` projected to a `dict[str, str]` and hands it to `continue_inbound` before the servicer body opens its span.
- Producer `csharp:Rasm.AppHost/observability/diagnostics-and-telemetry#CORRELATION_SPINE` registers the `TraceContextPropagator`/`BaggagePropagator` composite as `Propagators.DefaultTextMapPropagator`; the companion reads the same composite at the wire — a fresh root span in the peer is the named cross-language drift defect. Realizes the cross-`libs/` `ONE_DISTRIBUTED_TRACE` Python leg (`libs/.planning/TASKLOG.md`).
- Consideration: the carrier is a plain `dict[str, str]` per the `DefaultGetter` contract; the extract attaches a `Context`, the span open belongs to the measured operation, never to receipt emission; the propagation context is independent of the `xxhash` parity gate.

[QUEUED] [CAUSAL_TENANT_FRAME]:
- Add the causal/tenant inbound frame: a `CausalFrame` carrier on `context/admission#CONTEXT` holding the decoded `Hlc` two-half stamp and the `Tenant` partition, and a `serve#SERVE` decode that reads `hlc_physical`/`hlc_logical` and `tenant` off the inbound `ReceiptEnvelope` slot into the frame, threaded onto `RuntimeContext` so `lanes`, `receipts`, and `metrics` attribute to one host-minted frame; reuse the `serve#CRDT_DECODE` `Hlc` shape, mint no second stamp.
- Integrate `msgspec` (the slot decode `Struct`), `expression` (the context carry); the frame composes the existing `Hlc` from `server/serve#CRDT_DECODE` and the `RuntimeContext`/`Correlation` from `context/admission#CONTEXT`.
- Producer `csharp:Rasm.AppHost/ports/runtime-ports#PORT_RECORDS` mints the HLC two-half stamp on the `ReceiptEnvelope` and threads `TenantContext`; the companion decodes and propagates the same frame inbound, never re-minting. Realizes the cross-`libs/` `ONE_CAUSAL_TENANT_IDENTITY` Python leg (`libs/.planning/TASKLOG.md`).
- Consideration: the HLC two-64-bit-half order rides the same parity the content seed proves (a logical off-by-one-half folds a fresh op as stale); the tenant partition is a wire fact, not a per-runtime scheme; the `serve#PROTO_TRANSCODE` `FaultDetail` decode already reads the stamp fields — this task threads them into context rather than dropping them.

[QUEUED] [OTLP_EGRESS_INSTALL]:
- Author the OTLP install owner that collapses the egress wiring `observability/receipts#RECEIPT` and `observability/metrics#METRIC` each assume installed: one `Telemetry.install(profile, endpoint)` on `observability` minting the shared `Resource`, the `TracerProvider`/`MeterProvider`/`LoggerProvider` trio over the `OTLPSpanExporter`/`OTLPMetricExporter`/`OTLPLogExporter` family with `BatchSpanProcessor`/`PeriodicExportingMetricReader`/`BatchLogRecordProcessor`, installing the global propagator and gating on the `PROFILE_POLICY` `emit_otel` row; the `metrics#METRIC` `MeterProvider` construction and the `receipts#RECEIPT` `LogRecordProcessor` wiring fold into this one install.
- Integrate `opentelemetry-sdk` (`TracerProvider`/`MeterProvider`/`LoggerProvider`/`BatchSpanProcessor`/`BatchLogRecordProcessor`/`PeriodicExportingMetricReader`/`Resource.create`), `opentelemetry-exporter-otlp-proto-http` (`OTLPSpanExporter`/`OTLPMetricExporter`/`OTLPLogExporter`), `opentelemetry-api` (`set_tracer_provider`/`set_meter_provider`/`set_global_textmap`) — all branch-catalogued.
- Integration: reads the `context/admission#CONTEXT` `RuntimeProfile`/`emit_otel` policy row; the `serve#SERVE` interceptor and `TRACE_INBOUND_EXTRACT` read the installed `TracerProvider`; `metrics#METRIC` and `receipts#RECEIPT` lose their private provider/processor construction and read the installed trio. No second `Resource`, no per-signal exporter scatter — the C# `diagnostics` one-`UseOtlpExporter` parity.
- Consideration: a `PACKAGE`/`TEST` profile installs the API no-op provider (zero egress) and a `SIDECAR`/`TOOL` profile installs the batched OTLP trio; the install is the composition-root admission the three pages name but no owner carries.

[QUEUED] [STREAMING_ACQUISITION]:
- Deepen `resources/roots#RESOURCE`: `TransportResource.http` streams through `httpx.AsyncClient.stream`/`Response.aiter_bytes` under explicit `httpx.Timeout`/`httpx.Limits` instead of full-body `client.get`; `ResourceRoot.reader` adds a chunk `AsyncIterator[bytes]` modality beside the `bytes` whole-payload, recovered from the call shape (`MODAL_ARITY`, no `streaming=True` knob); the `obstore` arm reads the `GetResult.stream` chunk iterator; the chunk stream folds straight into `identity/content-identity#IDENTITY` `ContentIdentity.of` over the existing `Iterable[bytes]` modality.
- Integrate `httpx` (`AsyncClient.stream`/`Response.aiter_bytes`/`Timeout`/`Limits` — catalogued-but-unconsumed rows), `obstore` (streaming `GetResult` — catalogued); the streaming feeds the `lanes#LANE` offload and the `content-identity` streaming updater without a full-artifact buffer.
- Integration: the streaming key derivation composes the present `ContentIdentity.of` `Iterable[bytes]` arm; the retry stays the `resilience#RESILIENCE` `guard` row per transport class; faults cross the one `faults#FAULT` `boundary`/`async_boundary`.
- Consideration: large GLB/IFC/scan artifacts dominate companion pulls — memory-bounded streaming is the artifact-scale requirement the catalogues admit; the whole-payload `bytes` modality stays for small payloads, one polymorphic `reader`.

[QUEUED] [GRPC_CLIENT_AND_HOOKS]:
- Exploit the `serve#SERVE` instrumentation surface: wire `aio_client_interceptors(tracer_provider, filter_)` on the companion's outbound gRPC legs (the `CapabilityInvoke` dispatch, any host call-back channel) so the outbound call propagates the active span and continues the parent trace; admit `request_hook`/`response_hook` on the serve interceptor enriching the `SERVER` span with the decoded descriptor id, tenant, and fault case.
- Integrate `opentelemetry-instrumentation-grpc` (`aio_client_interceptors`/`client_interceptor`/`request_hook`/`response_hook` — folder-catalogued, currently only `aio_server_interceptor` consumed); composes the `TRACE_INBOUND_EXTRACT` global propagator so inbound and outbound legs share one trace.
- Integration: the outbound interceptor rides the same `Propagators.DefaultTextMapPropagator` the inbound extract reads; the span hooks read the `serve#CAPABILITY_INVOKE` descriptor id and the `CAUSAL_TENANT_FRAME` tenant; no second tracer.
- Consideration: the hooks run inside the active span scope and never block; the client leg is the outbound complement of the server interceptor the page already wires.

[QUEUED] [SECRET_BOUNDARY]:
- Resolve the orphaned `keyring`: EITHER fold a `context/admission#SETTINGS` secret-resolution boundary reading OS-keystore credentials for the OUTBOUND transport legs (`TransportResource.ssh` passphrase, `http` bearer) through `keyring.get_credential`/`get_password` with `NoKeyringError` falling back to the `pydantic-settings` `secrets_dir` source — OR remove `keyring` from the folder (manifest-row delta below) and delete its `.api/` catalogue.
- Integrate `keyring` (`get_credential`/`get_password`/`NoKeyringError`/`SimpleCredential` — folder-catalogued) IF kept; the boundary composes the existing `[3]-[SETTINGS]` `secrets_dir`/`file_secret_settings` source chain, the keystore the top-priority source and the secrets-file the headless fallback.
- Integration: the `resources/roots#RESOURCE` transport legs read the resolved credential from the settings boundary, never inline; the read is profile-gated and settings-admitted, never an eager keystore probe.
- Consideration: `keyring` is member-rich-catalogued with ZERO consumer after `[KEYRING_CATALOGUE]` dropped the serve leg — a README-vs-consumer mismatch the coverage audit forbids; the unattended-keystore-read hazard (a keychain prompt the session cannot answer) bounds any keep-resolution to a settings-admitted, profile-gated read. The orchestrator decides keep-vs-remove against the OUTBOUND credential law the transport catalogues already state.

[BLOCKED] [CRDT_OPLOG_LZ4]:
- Decompress the C# `MessagePackCompression.Lz4BlockArray` op-log envelope on the cp315 core at `server/serve#CRDT_DECODE`; the `msgspec.msgpack.Decoder(CrdtOpWire)` decode of the uncompressed delta is settled and cp315-clean, the LZ4 decompression is the single absent leg.
- Integrate `lz4` (`lz4.block`, companion `python_version<'3.15'` band); no cp315/abi3 `lz4` wheel synced.
- Producer is `csharp:Rasm.Persistence/versioning#CRDT_WIRE` `CrdtWire.Encode` under `Lz4BlockArray`; the `Lz4BlockArray` envelope is a MessagePack-csharp block-array framing (a msgpack `ext` wrapping independently-LZ4-block-compressed chunks), not a raw LZ4 frame read by `lz4.block`/`lz4.frame` or `msgspec` natively — the cross-agent `CRDT_OPLOG_WIRE_AMENDMENT` delta carries the producer-owned framing decision (a `MessagePackCompression.None` companion lane or the published envelope spec).
- [UPSTREAM-BLOCKED] on the cp315 `lz4` wheel AND the producer `Lz4BlockArray`-envelope framing publication; the companion `<3.15` band decodes the present build today.

## [2]-[CLOSED]

- [SERVE_HOST] [COMPLETE]: `server/serve#SERVE` `ServerHost` boots the `grpc.aio` server against the C# `ComputeService`/`DocumentService`/`ControlService`/`ArtifactSync` descriptors under the `anyio` runner, the `aio_server_interceptor` span emission wired once at construction with the health-RPC filter; the `Credential` axis is decoded against the C# `CredentialPolicy` five-row producer and resolved to `insecure_loopback` over the UDS leg with peer identity read by the kernel at accept (`#PEER_ADMISSION`), the serve credential `grpc.local_server_credentials(grpc.LocalConnectionType.UDS)`. `grpcio`/`protobuf`/`opentelemetry-instrumentation-grpc` ride the cp315 core via `specklepy`; `grpcio-tools` `protoc` rides the Forge companion lane (`python_version<'3.13'`), never at serve time. The runtime mints no wire vocabulary and awaits only the generated `_pb2` input.
- [CREDENTIAL_PEM] [COMPLETE]: decoded against `csharp:Rasm.Compute/remote/channels#CALL_POLICY` `CredentialPolicy`; the companion UDS serve leg admits `insecure_loopback` only (the `RemoteTransport.UnixDomainSocket.Credentials` column is `Seq(CredentialPolicy.InsecureLoopback)` exactly), so there is no serve-side PEM bundle — peer identity is the kernel `SO_PEERCRED`/`LOCAL_PEERCRED` read, and `tls`/`mtls`/`bearer`/`composed` are the outbound C# client legs the companion never serves. The `\n--SEP--\n` PEM placeholder and the `keyring` serve lookup are the deleted forms.
- [CRDT_OPLOG_DECODE] [COMPLETE] (msgpack leg): `server/serve#CRDT_DECODE` decodes the op-log CRDT-op union as MessagePack (`CrdtOpWire` `[MessagePack.Union]` tags 0-9, the C# `Rasm.Persistence/versioning#CRDT_WIRE` owner) through `msgspec.msgpack.Decoder` over a `msgspec.Struct` tagged-union with `tag_field`/`array_like=True`, LWW surviving only as the `set` arm; the prior protobuf `WireCodec (Struct, Message)` model was a decode DEFECT (the op-log is the durability codec, not the gRPC proto wire) and is corrected. The decode body is now real: the canonical `CrdtOp` union and `CrdtOpDecode.lift` total `match`/`assert_never` projection (mirroring the C# `CrdtWire.Map` switch, reconstructing `Hlc` from `(physical_ticks, logical)` and `ElementId` from `(origin, logical)`) replace the prior signature-only stub that stopped at the raw wire arm; `decode` runs `lift ∘ Decoder.decode`. The companion authors no op kind the wire does not carry. The LZ4 decompression of the compressed envelope is the open `[CRDT_OPLOG_LZ4]` gate.
- [CAPABILITY_SDK_CONSUME] [COMPLETE]: `server/serve#CAPABILITY_INVOKE` consumes the C# `Rasm.AppHost/capability/registry#SDK_CODEGEN` Python target — one polymorphic `CapabilityInvoke.run(descriptor_id, args)` invoke keyed by the `{surface}.{op}` descriptor id, the descriptor catalog decoded through `msgspec.json.Decoder(list[DiscoveryResult])` from the `DiscoveryResultWire[]` projection, the per-descriptor `input_schema` the C# `SuiteContracts.Schema` `JsonSchemaExporter` projection the three SDKs bind, the command receipt read through `ReceiptEnvelopeWire<CapabilityCommandReceiptWire>`, never a per-service hand client. The invoke body is now real: `run` does the catalog-id lookup (absent id → typed `wire` fault), `msgspec.json.decode` arg-schema validation, `async_boundary` stub dispatch, and `_receipt.decode` on the `RuntimeRail`, replacing the prior `discover`-only stub; `DiscoveryResult`/`CommandTxnKind` match the producer `DiscoveryResult`/`CommandTxn` field- and arm-for-arm. The companion reads the descriptor, re-authoring no capability shape; awaits only the live catalog emission at SDK-bootstrap.

- [OTEL_METRICS_PAGE] [COMPLETE]: `observability/metrics` `Metrics` mints one `MeterProvider`/`RUNTIME_RESOURCE`, the request-duration `Histogram`, the `outcome`-keyed `lane.drained` `ObservableCounter` folding the four `DrainReceipt` columns, and the RSS/CPU `ObservableGauge` instruments over the shared `OTLPMetricExporter`; callbacks read a shared `MetricState` without a lane lock.
- [INTERPRETER_OFFLOAD_LANE] [COMPLETE]: `concurrency/lanes` `LanePolicy.offload` routes a caller-supplied CPU kernel through reflection-confirmed `anyio.to_interpreter.run_sync` (PEP 734 per-subinterpreter GIL) under the one `CapacityLimiter`, the `BrokenWorkerInterpreter` crossing the one `async_boundary`.
- [CONTENT_ADDRESSED_LANE_CACHE] [COMPLETE]: `concurrency/lanes` `LanePolicy.cached` admits `(ContentKey, Work[T])` pairs against an immutable `expression` `Map[ContentKey, T]`, a present key short-circuiting and the `DrainReceipt` `hit` count distinguishing it; only an `Ok` caches, session-local, never durable.
- [STRUCTURAL_DRIFT_GUARD] [COMPLETE]: `evidence/evidence` `Structural.drift` emits a `DriftSpan` only where a canonical name binds across more than one language namespace, the multiplicity filter rejecting a distinct same-named concept; grammar pointers reflection-confirmed.
- [API_CATALOGUE_CAPTURE] [COMPLETE] (split): `obstore`/`apscheduler`(3.11.2)/`keyring` member spellings reflection-confirmed on the cp315 core and settled across `resources`/`concurrency`/`server`; `xxhash` stays [UPSTREAM-BLOCKED] at `[XXHASH_PARITY]` (no cp315/abi3 wheel synced), the `python_version<'3.15'` companion band carrying the present build.
- [WIRE_TRANSCODE] [COMPLETE]: `server/serve#TRANSCODE` `WireCodec` transcodes canonical `msgspec` `Struct` to protobuf `Message` and back via reflection-confirmed `to_builtins`/`convert` + `ParseDict`/`MessageToDict(preserving_proto_field_name=True)`/`proto.parse`/`SerializeToString`, the codec generic over `(Struct, Message)`; only concrete pairs await `_pb2`.
- [STREAMING_IDENTITY] [COMPLETE]: `identity/content-identity` folds whole-payload/streaming/Merkle-child modalities into one `ContentIdentity.of`; `[XXHASH_PARITY]` is the install-gated cp315 wheel.
- [OBSTORE_FAST_PATH] [COMPLETE]: `resources/roots` `ResourceRoot.reader` dispatches `OBJECT_STORE_SCHEMES` to the reflection-confirmed `obstore` async API and every other scheme to `UPath`.
- [APSCHEDULER_SOURCE] [COMPLETE]: `concurrency/lanes` carries the `apscheduler` `Trigger` union, `from_crontab` intake, and the `fired` feeder; `aiocron` purged, cron owned solely by `apscheduler`.
- [STRUCTLOG_CHAIN] [COMPLETE]: `observability/receipts` `Signals.configure` carries `merge_contextvars`, the `trace_context` processor, the timestamper, the JSON renderer, and the reflection-confirmed OTLP log egress.
- [XXHASH_ADMISSION] [COMPLETE]: `xxhash` admitted (`xxhash>=3.7.0`, abi3); the cp315 wheel install and the C# digest-endianness parity remain `[XXHASH_PARITY]`.
