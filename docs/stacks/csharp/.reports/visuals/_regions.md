# visuals — region rows

Final ledger material for `domain/visuals.md`: one page row at card altitude, snippet rows at fence altitude. Orchestrator consolidates into `_region-map.md`.

## [1]-[PAGE_ROW]

- domain/visuals.md: render and visuals law — one gated render capsule (record-once op budget, frozen render-policy record, target rows, pinned-projection hash receipts, evidence identity, paint/color-space law) with the page-protocol document fold and dual receipt; text shaping before raster (raw shape pipeline, run-spec determinism axes, typography role × script grid, fallback honesty, markdown AST projection); vector/icon/raster asset admission with variant matrices, two-key load receipts, and codec taxonomies; chart law as series-spec table plus headless evidence twin and chart-side stream-binding posture; the five-way frozen token algebra (variant × density totality, pure resolve fold, atomic generation swap, changed-key diff); motion rows with reduced-motion axis and perceptual color interpolation with ramp construction law.

## [2]-[SNIPPET_ROWS]

- VS-01 RENDER_POLICY_TARGET_ROWS: one frozen render-policy record (pinned `SKImageInfo`, surface props, op/byte ceilings) behind a record-once gate — `SKPictureRecorder.BeginRecording(useRTree)` with the op-budget refusal disposing the recording — dispatched over a closed target `[Union]` (evidence raster, derived `WithSize` thumbnail, unrendered cost probe) folding one `SKPicture` into a `FrameReceipt` whose hash is `Option`-carried, `PeekPixels` hash-without-copy, `BytesSize64` gate per raster. Distinct from B-02 (no `SafeHandle` capsule — capsule grammar composed) and A-11 (pixel-hash receipt, not numeric finite gate).
- VS-02 SHAPED_RUN_BRIDGE: the raw shape-before-raster pipeline — nested `Blob`→`Face`→`Font` admission capsule with reverse-order release and design-scale pin, buffer protocol with direction/script/language/cluster-level pinned from one `RunSpec` row, `Font.Shape` feature rows, fixed-512 scale projection with Y negation filling `SKTextBlobBuilder.AllocatePositionedRun` span buffers, cluster map carried out. Distinct from L-07 (no ref-struct kernel grammar — the span fill is a boundary kernel) and B-02 (lifetime nesting, not borrow brackets).
- VS-03 VECTOR_VARIANT_MATRIX: `SKSvg` admission with parse-null projected to the rail under dispose-on-failure ownership transfer, retained-source `ReLoad(SvgParameters)` restyle memoized per (asset, variant, generation) key, `CacheOriginalStream` as one type-initialization policy write, intrinsic bounds projected `Option<SKRect>` from `Picture.CullRect`. Distinct from R-07 (memo `Swap` mechanics are rails-owned supporting material — the spotlight is restyle-is-reload variant law).
- VS-04 SERIES_SPEC_EVIDENCE: closed series-kind vocabulary dispatching one spec record carrying a paint-token key resolved through one token-resolved paint map onto charting series rows — disable-motion sentinel and linear easing on every arm — and one headless cartesian chart `GetImage()` evidence fold with pinned axis rows (`Labeler`, `MinStep`, `UnitWidth`, limits). Distinct from S-04/SD-07 (state-threaded `Switch` is supporting material — the spotlight is spec-table-to-headless-evidence parity).
- VS-05 TOKEN_RESOLVE_SWAP: the generic token algebra — freeze proving (role × variant × density) totality via one fused `TraverseM` into a `FrozenDictionary` grid, one pure resolve fold, atomic generation swap whose prior state is tuple-captured inside the swap so the changed-key diff (computed on resolved payloads) cannot tear. Distinct from RT-03 (no options pipeline, no monitor, no reload token) and R-07 (`Atom` supporting material — diff-receipt publication is the spotlight).
- VS-06 PERCEPTUAL_TWEEN_ROW: cube-root LMS opponent-space algebra — forward transform, sub-epsilon chroma neutralization, one ramp builder whose pivot `Option` discriminates sequential from neutral-pinned diverging construction under a trend-signed monotone-lightness predicate failing to the rail — and the (motion token, ramp table) tween sampler index-clamped for declared-overshoot easings, with the disable sentinel and the static reduced-motion row. Distinct from A-04 (perceptual color algebra, not spectral operator drive).
- VS-07 PAGE_PROTOCOL_EXPORT: forward-only `SKDocument` fold over a format smart-enum row (`CreatePdf` with pinned `Creation`/`Modified`/`EncodingQuality` metadata beside `CreateXps` — format is the row, the metadata argument the only divergence), per-page `BeginPage`/`EndPage` emission whose dual receipt replays each page through the render capsule's evidence target, `Close()` tuple-sequenced before the byte-length receipt, `Try.lift` capture whose `MapFail` tuple-captures `Abort()` so dispose-only loss is unrepresentable. Composes VS-01 as settled law. Distinct from R-08 (fold receipts, no monoid) and R-01 (capture grammar supporting — Abort/Close polarity is the spotlight).
