# CAPABILITY LEDGER — lane vectors-b (Vectors even-rank)

Ranking law: `ls -la *.cs | sort -rn` (byte size desc). Even ranks = this lane.
Rank 1 Mesh.cs(a) · **2 Field.cs** · 3 Sample.cs(a) · **4 Cloud.cs** · 5 Matrix.cs(a) · **6 Spectral.cs** · 7 Flow.cs(a) · **8 Extraction.cs** · 9 Align.cs(a) · **10 Intent.cs** · 11 Atoms.cs(a) · **12 Modes.cs** · 13 Space.cs(a).

Lane scope (LOC / loc-complexity): Field 2599/613 · Cloud 1285/327 · Spectral 805/331 · Extraction 484/81 · Intent 359/2 · Modes 146/14. Total 5678 LOC. All read FULLY.

Target sub-domains (Geometry `.planning`): `Numerics · Spatial · Meshing · Parametric · Processing · Drawing`.

Substrate owners referenced (NOT this lane): `AtomProjection`+`ProjectionRow` = Atoms.cs(a); `MatrixKernel`/`Matrix`/`SymmetricMatrix`/`SparseMatrix`/`CholeskySparse`/`GaugePolicy`/`EigenSolveReceipt`/`SolveReceipt` = Matrix.cs(a); `MeshKernel`/`MeshSpace`/`IntrinsicMesh`/`IntrinsicEdge`/`LaplacianCache`/`SparseLaplacian`/`TopologyReceipt`/`SignpostTransportReceipt`/`HodgeDecompositionReceipt`/`TangentLogMap*`/`SdfMeshReceipt`/`SignedHeatReceipt`/`VolumeGridReceipt` = Mesh.cs(a); `SampleKind`/`SampleReceipt`/`ExtractionDomain`-sampling = Sample.cs(a); `FlowKernel`/`StreamlineTrace`/`FieldIntegrator`/`Termination` = Flow.cs(a); `Direction`/`VectorSpan`/`VectorFrame`/`VectorCone`/`SignedAxis`/`BoundarySense`/`AnglePivot`/`VectorRelation` = Atoms.cs(a); `SupportSpace`/`SurfaceSpace`/`SupportProjection` = Space.cs(a). **CROSS-STRATA (retiring Domain/):** `Op` (error rail), `Fin`/`guard`/`Optional` (LanguageExt), `Context` (tolerance), `ClosestHit`/`ComponentIndex`, `GeometryKernel.*` (NormalAt/FrameAt/SurfaceUv), `ScalarMetric.Gaussian/Mean`, `Lease<T>`, `IValidityEvidence`, `OpAcceptance` — ALL Domain-owned; heavily consumed by every unit below. Domain converts to design pages → these atoms need a ruled home in the new partition.

---

## Modes.cs (rank 12, 146 LOC) — curve/surface/cone/pose projection selectors

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| `CurveProjection` [SmartEnum<int>] | Modes.cs:4-42 | 9 cases (Tangent/Curvature/Frame/PerpendicularFrame/ArcLength/FrameNormal/FrameBinormal/PerpendicularNormal/PerpendicularBinormal); `Project<TOut>` validates domain, samples, routes to `AtomProjection.Raw`. | 8 | Clean. `CurveFrameProjection` factory folds 6 frame cases into 1 constructor — good collapse. | `Curve.TangentAt`,`.CurvatureAt`,`.GetLength(fractionalTolerance,subdomain)`,`.FrameAt`,`.PerpendicularFrameAt`,`.Domain.IncludesParameter`; `Interval`; `Vector3d.IsValid/IsTiny`; `RhinoMath.IsValidDouble` | Parametric |
| `SurfaceProjection` [SmartEnum<int>] | Modes.cs:44-119 | 13 cases (PrincipalCurvatures/Gaussian/Mean/Max+MinOsculatingCircle/Normal/ShapeOperator/Point/Frame/UvFrame/Jacobian/Metric/AreaScale); `WithCurvature`/`Derivatives` helpers; `ShapeOperatorOf` builds `SymmetricMatrix` from principal curvatures. | 8 | `ShapeOperator` (2nd-fundamental-form tensor) overlaps `TensorField.CurvatureCase` (Field.cs:1746) which calls THIS via `SurfaceProjection.ShapeOperator.Project` — correct single-owner but cross-file coupling. | `Surface.CurvatureAt`→`SurfaceCurvature`(`.MaximumPrincipalCurvature`/`.MinimumPrincipalCurvature`/`.OsculatingCircle`/`.Kappa`/`.Direction`); `Surface.PointAt`/`.Evaluate(numberDerivatives,derivatives)`; `Circle.IsValid`; `Vector3d.CrossProduct`; `GeometryKernel.NormalAt/FrameAt/SurfaceUv`(Domain); `ScalarMetric.Gaussian/Mean`(Domain) | Parametric (+Numerics for shape-operator tensor) |
| `ConeProjection` [SmartEnum<int>] | Modes.cs:121-130 | 4 cases (HalfAngle/SolidAngle/Axis/Apex) over `VectorCone`; thin `Project<TOut>` via `AtomProjection.Raw`. | 7 | Thin — 4 field getters wrapped as SmartEnum. Could collapse into `VectorCone` accessor family. | none direct (VectorCone from Atoms) | Parametric/Numerics |
| `MotionInterpolation` [SmartEnum<int>] | Modes.cs:132-152 | Linear/Slerp plane interpolation via quaternion; `InterpolatePlanes` origin-lerps + quaternion slerp/lerp. | 8 | Clean. Pose/motion primitive; parallels Intent `PoseCase`/`SlerpCase` (Intent.cs:27,23) quaternion logic — 2 slerp sites. | `Quaternion.Rotation/Slerp/Lerp/GetRotation`; `Plane.WorldXY`; `FieldNabla.Plane`(Field) | Parametric |

Modes note: cleanest file in lane — pure SmartEnum dispatch over Rhino curve/surface sampling + Atoms projection. Zero standing kernel. Shares `AtomProjection.Raw` output projection with Atoms (curve/surface/cone raw values).

---

## Intent.cs (rank 10, 359 LOC) — THE singular consumer rail

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| `VectorIntent` [Union] — cases | Intent.cs:4-38 | 32 case records spanning every capability band (). | 8 | 32 cases is the aggregate of ALL modules — appropriate for a dispatch root, but the widest single ADT in Vectors. | `Point3d`,`Vector3d`,`Point2d`,`Plane`,`Curve` in case fields | package entry root (cross-domain) |
| `VectorIntent` — 41 factory methods | Intent.cs:40-231 | `Axis`/`Direction`/`Axes`/`Angular`/`Support`/`Probe`/`Contour`/`IsoSurface`/`Glyph`/`SampleGrid`/`StreamBundle`/`Ray`/`Frame`/`Curve`/`Cloud`(×2)/`Winding`/`Cone`/`Components`/`Relation`/`Bounce`/`Streamline`/`Lerp`/`Slerp`/`ProjectOnto`/`Mirror`/`Surface`/`Pose`/`Flatten`/`Hull`(×2)/`Sample`/`Align`/`Remesh`/`Transport`/`Topology`/`Features`(×2)/`Descriptor`/`DiscreteCalculus`/`Segmentation`. Each admits inputs via `FieldNabla`/owner-`Admit` then constructs case. | 7 | **ENTRY-POINT WIDTH: 41 factories.** Overloads (Cloud/Hull/Features ×2) + per-case bespoke admission. This is the knob-spam surface the new partition must collapse behind a denser entry. | `SignedAxis.Cardinal`; `Transform`; `Point3d/Vector3d/Plane/Point2d/Curve` | package entry root |
| `Project<TOut>` + `Dispatch<TOut>` | Intent.cs:232-362 | Singular consumer rail: validates `Context`, 30-arm `Switch` dispatch each routing to owning module's kernel + `Project<TOut>`; inlines axes-cardinal/slerp-parallel/lerp/mirror/projectOnto math. | 8 | Dispatch inlines some domain math (slerp `IsParallelTo -1` branch, lerp interpolation, mirror/projectOnto transforms) that arguably belongs in Direction/owner. 30-arm switch is long but is the canonical dispatch table. | `Transform.Rotation/PlanarProjection/Mirror`; `Quaternion.Rotation/Slerp/Identity`; `Vector3d.IsParallelTo`; `Point3d.Origin` | package entry root |

Intent note: this IS `VectorIntent.Project<TOut>(Context, Op?)`, the sole invariant consumer rail (arch invariant L105). Under the new partition it becomes the package entry surface. loc-complexity=2 (pure delegation). The 41-factory width is the primary entry-point-rot signal.

---

## Extraction.cs (rank 8, 484 LOC) — extraction/sampling projection rail

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| `ExtractionRoute`/`ExtractionStatus`/`ToleranceSource` [SmartEnum<int>] | Extraction.cs:6-23 | Native/Local route, Complete/Approximate status, Context/RhinoDefault/NotApplicable tolerance provenance for receipts. | 7 | 3 two-value enums; candidates to fold into a single receipt-metadata enum family. | none | Processing |
| `ContourPolicy` [Union] | Extraction.cs:25-60 | Plane/Axis/SurfaceIso/MeshScalar section policies + `Admit` validation (finite points, span, iso-status, scalar levels). | 8 | Clean closed policy union. | `Plane`; `IsoStatus`; `RhinoMath.ZeroTolerance` | Processing (+Drawing sectioning) |
| `ExtractionDomain` [Union] + `Of(object?)` | Extraction.cs:62-89 | Support/Mesh/Cloud domain cases; polymorphic `Of(object?,Context)` ingests raw Mesh/VectorCloud/PointCloud/Brep/Surface → typed domain. | 8 | `Of` is the good polymorphic ingress; `Admit` re-validates. | `Mesh`,`PointCloud`,`Brep`,`Surface` type-switch; `MeshSpace.Of` | Processing |
| `ExtractionDomain.Contours` + native adapters | Extraction.cs:90-118,239-273 | Routes contour by geometry: Brep/Mesh/Surface/Cloud → native contour curves; `CurvesFromBrep/Mesh/Surface/Cloud`, `AcceptTrimAwareIso`. | 8 | Native-first; falls to local PL for mesh-scalar. | `Brep.CreateContourCurves(sectionPlane\|contourStart/End/interval)`; `Mesh.CreateContourCurves(sectionPlane\|contour…,tolerance)`; `Surface.IsoCurve`; `BrepFace.TrimAwareIsoCurve`; `PointCloud.CreateContourCurves/CreateSectionCurve`; `Curve` | Processing |
| **Scalar-isoline local PL kernel** | Extraction.cs:119-238 | `ScalarIsolinesDetailed`+`AddFaceIsolines`/`DeduplicateSegments`/`StitchSegments`/`Extend`/`KeyOf`: marching-triangles scalar contour on triangulated mesh (RhinoCommon has no scalar contour); dedup+stitch into polyline candidates with branch/plateau receipt stats. | 7 | Justified BoundaryAdapter (comment L119-120), but fully imperative mutable `List`/`HashSet`/`Dictionary` statement-kernel; quantized `ScalarIsolinePointKey` welding. Parallels Mesh.cs mesh-PL isoline work — cross-check on convergence. | `Mesh.DuplicateMesh/.Faces.QuadCount/.ConvertQuadsToTriangles/.Vertices`; `MeshFace.IsTriangle`; `Polyline.ToPolylineCurve`; `RhinoMath.SqrtEpsilon`; `CollectionsMarshal.GetValueRefOrAddDefault` | Processing/Meshing |
| `ExtractionProbe` [Union] | Extraction.cs:285-333 | Vector/Scalar/Tensor field point-probe → typed output; special-cases TangentLogMap/Hodge (→MeshKernel), Sdf/Reconstruction (→field detailed samplers) by `typeof(TOut)`. | 6 | **`typeof(TOut)` reflection-branching dispatch** (repeated across Vector/Scalar/Tensor) is the weakest pattern in lane — string-of-types switch instead of typed rows. Prime collapse target. | `Point3d`,`Vector3d` | Processing/Spatial |
| `Extraction` [Union, internal] + `Project<TOut>` | Extraction.cs:335-441 | Probe/Contour/IsoSurface/Glyph/SampleGrid/StreamBundle cases + factories + `Project<TOut>` + `ProjectCurves`/`ProjectSamples`/`SampledItems` (mutable accumulation loop). | 7 | `SampledItems` uses mutable `List`+`rejected++` (statement kernel). `typeof(TOut)` branching again in glyph/streamBundle projection. `Sampled<TField,TPolicy>` generic factory is a nice collapse. | `Mesh`,`BoundingBox`,`Line`,`Polyline`,`Curve`; `RhinoMath.IsValidDouble`; `Interlocked`(via field iso) | Processing |
| Receipt/policy models | Extraction.cs:443-506 | `CurveBatch`,`ScalarIsolinePointKey/Segment/Stats`,`ExtractionReceipt`,`GlyphPolicy`,`GridPolicy`,`ScalarIsolineReceipt/Result`,`StreamBundlePolicy`. | 8 | `GlyphPolicy`/`GridPolicy`/`StreamBundlePolicy` are thin `SampleKind`+scalar wrappers (3 near-identical policy structs) — collapse candidate. `ScalarIsolineStats` is a mutable `class` (only mutable-class in lane). | `StructLayout(LayoutKind.Auto)` | Processing |

Extraction note: tightly bound to Sample.cs(a) — arch invariant L106 makes `ExtractionDomain + SampleKind` the ONE sampling/extraction rail. Extraction owns projection+receipts, Sample owns candidate generation. **Co-locate in Processing.** Consumes FlowKernel(Flow), SampleKind(Sample), Field, MeshKernel(Mesh).

---

## Spectral.cs (rank 6, 805 LOC) — DEC substrate + spectral algebra (DDG core)

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| Spectral policy enums [SmartEnum<int>] | Spectral.cs:7-38 | `SpectralAssemblyKind`(Dec/EdgeConnection), `SpectralScaleNormalization`, `SpectralEnergyNormalization`, `SpectralZeroModePolicy`, `SpectralDistanceKind`. | 8 | Descriptor-normalization vocabulary; coherent. | `RhinoMath` (in consumers) | Numerics |
| `SpectralFilter` [Union] | Spectral.cs:40-83 | Heat/Wave/Biharmonic/Diffusion/CommuteTime/Identity; `Weight(eigenvalue)` spectral transfer fn; `Compose` **partial monoid** (Heat∘Heat, Diffusion∘Diffusion, Identity unit); `ApplyDetailed`. | 9 | Elegant closed algebra with `[MethodImpl(AggressiveInlining)]` weight. Best-designed unit in lane. Consumed by Mesh `MeshDescriptor` + Field `SpectralDistanceCase`. | `RhinoMath.SqrtEpsilon` | Numerics (+Meshing) |
| DEC/spectral receipt+carrier models | Spectral.cs:86-234 | `SpectralAssemblyReceipt`(topology/mass/nnz witnesses), `HarmonicOneFormReceipt`(residual-gated), `HarmonicOneFormBasis`, **`DiscreteCalculus`**(D0/D1/Star0/Star1/Star2 + transport + harmonic — the DEC operator bundle), `SpectralBasis`(eigenpairs +`Truncate`), `SpectralDescriptorPolicy`, `SpectralWaveReceipt`, `SpectralDescriptorReceipt`, `SpectralDescriptor`(+`Normalize`/`Rank`), `SpectralRankingPolicy`/`SpectralRank`/`SpectralRanking`. | 9 | Rich validity-gated receipts; `DiscreteCalculus.Project<TOut>` uses `AtomProjection.Rows`. `DiscreteCalculus` is the seam the `Rasm.Compute` DDG-adjoint surface consumes (arch L125-158). | `RhinoMath.IsValidDouble/SqrtEpsilon`; consumes `SparseMatrix`/`SymmetricMatrix`/`EigenSolveReceipt`(Matrix) | Numerics |
| `SpectralCore` — DEC assembly | Spectral.cs:237-479 | `IntrinsicTriangle` helper, `AssembleDecOperators`(d0 incidence/d1 curl/star0 mass/star1 cotan/star2 area), `ComputeIntrinsicStar1`, `CotAtOppositeCorner`, `BoundaryCompositionResidual`(∂∂=0 gate), `Build`(space,kind). | 9 | World-class intrinsic DEC. **DUP#1: `CotAtOppositeCorner`(L405) computes cotan-from-edge-lengths** — one of 3 cotangent paths in Vectors (see cross-cut). | `Mesh`; `RhinoMath.SqrtEpsilon/ZeroTolerance`; consumes `MeshKernel.IntrinsicMesh/IntrinsicEdge`(Mesh), `SparseMatrix.FromTriplets`(Matrix), `space.Laplacian`(Mesh mass) | Numerics+Meshing |
| `SpectralCore` — harmonic 1-forms + Hodge | Spectral.cs:481-656 | `BuildHarmonicOneForms`(genus-dim harmonic basis via symmetric-eigen of closed+coclosed normal operator, Star1-orthonormalized), `Star1OrthonormalForms`, residual folds (`MaxResidual`/`MaxCoClosedResidual`/`Star1OrthonormalResidual`), **`HodgeDecomposeDetailed`**(ω=dα+δβ+η via gauge-fixed Poisson + harmonic projection), `D0Apply`/`D0Transpose`/`HadamardEdge`. | 9 | Research-grade. `HodgeDecomposeDetailed` PRODUCES `HodgeDecompositionReceipt` but that TYPE is declared in Mesh.cs(a):442 — **receipt-vs-algorithm ownership split** (see cross-cut). | `RhinoMath.*`; consumes `SymmetricMatrix.DecomposeEigenDetailed`(Matrix), `stiffness.SingularSolveDetailed`+`GaugePolicy.PinConstant`(Matrix) | Numerics+Meshing |
| `SpectralCore` — Crouzeix-Raviart connection Laplacian | Spectral.cs:658-743 | `BuildCrouzeixRaviartHeatSystemDetailed`(edge-based CR connection heat operator, Hermitian-real block layout, symmetry-residual gate), `EmitCrouzeixRaviartPair`, `SampleCrouzeixRaviartFaceField`, `ComputeIntrinsicVertexDivergence`, `ScatterCotangentDivergence`. | 9 | Stein-Wardetzky-Jacobson-Grinspun 2020 SHM scaffold. `ScatterCotangentDivergence` = another extrinsic cotan-weighted op. | `Mesh.Vertices`; `Vector3d.CrossProduct/Unitize`; `RhinoMath.*`; consumes `MatrixKernel.AddHermitianRealBlockTriplets`(Matrix), `SparseMatrix.FromTriplets` | Meshing |
| `SpectralCore` — CDS holonomy 1-form | Spectral.cs:745-818 | `ComputeIntrinsicAngleDefects`, `DistributeHolonomy`(Crane-Desbrun-Schröder 2010 trivial connection), `ValidateGaussBonnet`(Σκ/2π=χ integer gate), `BuildConePrimalOneForm`, `IntrinsicCoexactRhs`, `IntrinsicEdgeGradient`. | 9 | Trivial-connection for cone/cross-field. Closed genus-0 only (arch L113). | `Mesh.GetNakedEdges`; `RhinoMath.*`; consumes `space.Cache.Cholesky`+`.Solve`(Mesh/Matrix), `MeshKernel.TopologyDetailed`(Mesh) | Meshing |
| `SpectralCore` — descriptor eval + extrinsic heat scaffold | Spectral.cs:258-371,820-857 | `EvaluateFilteredDetailed`(HKS/WKS-style filtered spectral signature, dense buffer), `WeightsOf`/`WaveWeightsOf`, `NormalizeValues`(L1/L2/z-score), `NormalizeDescriptor`/`RankDescriptors`/`DistanceOf`; **`ComputeTriangleGradients`/`ComputeVertexDivergence`/`BuildSourceDelta`** (EXTRINSIC face-gradient heat-geodesic scaffold). | 8 | **DUP#1 cont.: `ComputeVertexDivergence`(L844) `ScatterCotangentDivergence` computes cotan `-ab·ca/twoArea` from Mesh face geometry** — third cotangent path, extrinsic, parallels Mesh.cs `AssembleCotangent`. `EvaluateFilteredDetailed` is a dense imperative buffer loop (justified perf). | `Mesh.Faces/.Vertices`; `MeshFace.IsTriangle`; `Vector3d.CrossProduct`; `TensorPrimitives.SumOfMagnitudes/Norm`; `MathNet.Numerics.Distance.Manhattan/Cosine/Euclidean`; `RhinoMath.*` | Numerics+Meshing |

Spectral note: `Spectral.cs` is the shared DEC/spectral substrate for BOTH Mesh descriptors and Field scalar-spectral cases (arch L107). Splits cleanly: pure linear-algebra DEC carriers (`DiscreteCalculus`/`SpectralBasis`/`SpectralFilter`/descriptor algebra) → Numerics; mesh-bound assembly (CR/CDS/harmonic/extrinsic-heat) → Meshing.

---

## Cloud.cs (rank 4, 1285 LOC) — point-cloud construction, metrics, hull, transport

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| Cloud status enums [SmartEnum<int>] | Cloud.cs:9-59 | `CloudCurvatureRangeKind`,`CloudHullKind`(Convex3D/Footprint2D/ConcaveOutline/AlphaShape/FootprintWrapper),`CloudHullStatus`,`CloudNeighborhoodSearchBackend`,`SinkhornNumericStatus`,`SinkhornResidualKind`,`SinkhornStopKind`(+`Converged`). | 8 | `ConcaveOutline`/`AlphaShape` are declared-but-Unsupported (kernel returns `Unsupported`, L848) — dead cases carrying intent. | none | Spatial |
| `VectorCloud` [Union] | Cloud.cs:61-140 | Ring/Polyline/Cluster + `WeightedCluster`; `ClusterCase.Indexed` = lazy `PointCloud` via `ConditionalWeakTable`; `ClosestVertex`/`WithinRadius`; `AdmitPoints`/`AdmitCluster` dedup+mass admission. | 8 | Ring construction does self-intersection preflight — heavy but correct. `Indexed` cache is a clean memo. | `PointCloud.AddRange/.ClosestPoint/.PointAt`; `Polyline.IsClosedWithinTolerance/.SegmentCount/.ToPolylineCurve`; `RTree.PointCloudClosestPoints`; `Sphere`; `Intersection.CurveSelf`; `PolylineCurve`; `ConditionalWeakTable` | Spatial |
| `VectorCloudMetric` [SmartEnum<int>] | Cloud.cs:142-178 | **30 metrics** (); `Of`/`All`/`Ring`/`Cases`/`Poly`/`Cluster` factories admit by cloud-case; `Project<TOut>` type-checks output. | 9 | Excellent table-driven polymorphic metric surface. Widest metric SmartEnum in lane but genuinely one-row-per-capability. | (via CloudKernel) | Spatial |
| Cloud receipt/policy models | Cloud.cs:180-493 | `CloudAdmissionPolicy/Receipt`(mass-conservation gated), `CloudNeighborhoodPolicy/Receipt`, `CloudMetricPolicy`, `CloudNeighborhoodPca{Receipt,Result,Sample}`, `CloudCurvature{RangeReceipt,Receipt,Result,Sample}`, `CloudHullPolicy/Receipt/Result`, `CloudTransportPolicy`, `CloudCorrespondence/Set`, `SinkhornReceipt`, `VectorCloudShape`(18-Option omni-shape). | 8 | Heavy receipt proliferation but each carries evidence (`IValidityEvidence`). `VectorCloudShape` = 18 Option fields (God-shape smell, but arch mandates 5×-complexity). `CloudHullResult.Project` uses `AtomProjection.Rows`. | `AreaMassProperties`; `Mesh`; `CurveOrientation`; `StructLayout(LayoutKind.Auto)`; MathNet `SortedArrayStatistics.Quantile` | Spatial |
| `CloudKernel` — admission + PCA | Cloud.cs:496-567 | `AdmitCluster`(tolerance dedup + mass renormalize + `OriginalToUnique` map), `MassOf`, `CovarianceOf`(3 overloads via `SampleMoment`), `PrincipalStats`(mean+eigen), `AsVector3d`. | 9 | **Canonical covariance/PCA owner** — `SampleMoment` lives only here; consumed by Align.cs(a) GICP via `NeighborhoodPcaOf`. Good centralization. | consumes `SampleMoment.Of`(Domain?), `SymmetricMatrix.Of`/`.DecomposeEigen`(Matrix) | Spatial |
| `CloudKernel` — Bishop frames (RMF) | Cloud.cs:570-629 | `BishopFramesOf`/`BishopChainOf`/`InitialBishop`/`StepBishop`/`DoubleReflect`/`ReflectAcross`/`RedistributeClosureTwist`: rotation-minimizing frames via double-reflection, closed-loop twist redistribution. | 9 | Wang double-reflection RMF. Parallels Atoms `Direction.ParallelTransport(Seq<Plane>)` (arch L91) — 2 parallel-transport sites, cross-check. | `Vector3d.Unitize/IsTiny/IsValid`; `Plane.Rotate`; `Vector3d.VectorAngle`; `VectorFrame.Of/SeedPerpendicular`(Atoms) | Spatial |
| `CloudKernel` — winding + shape queries | Cloud.cs:632-809 | `PlanarWindingOf`(2D angle-sum ring winding)/`Winding<TOut>`; `CloudCases` dispatcher; `Centroid/BestFitPlane/PrincipalAxes/PrincipalFrame/Shape` via `AreaMassProperties`; ring metrics (`Compactness/EdgeAspect/Skewness/MomentAnisotropy/RingShape`); `WithRingCurve`/`WithMassProperties` `Lease`-scoped. | 8 | **DUP#2: `PlanarWindingOf`(L632) 2D winding vs Mesh.cs `SolidAngleWindingNumber`(L4177) 3D generalized winding** — same concept-name, different math/domain. Name-family for architect. | `AreaMassProperties.Compute`(`.Centroid/.Area/.AreaError/.CentroidError/.CentroidCoordinatesRadiiOfGyration/.CentroidCoordinatesPrincipalMomentsOfInertia`); `Plane.FitPlaneToPoints`; `Vector3d.VectorAngle/CrossProduct`; `PolylineCurve.TryGetPlane/.ClosedCurveOrientation`; `Polyline.GetSegments/.IsConvexLoop`; `Lease<T>`(Domain) | Spatial |
| `CloudKernel` — polyline metrics | Cloud.cs:812-841 | `TangentFlowOf`/`CumulativeArcLengthOf`/`EdgeCurvaturesOf`/`OpenLengthOf`/`SegmentLengthsOf`. | 8 | Clean folds. | `Vector3d.Unitize/VectorAngle`; `Point3d.DistanceTo` | Spatial |
| `CloudKernel` — hull | Cloud.cs:843-891 | `ComputeHullDetailed`→`ConvexHullOf`(3D)/`ConvexFootprintOf`(2D PCA-plane projection)/`ConvexHull2D`/`FootprintContainment`/`MeshFromFootprint`. | 8 | Concave/alpha unsupported (returns `Unsupported`). Native-routed. | `Mesh.CreateConvexHull3D(hullFacets,tolerance,angleTolerance)`; `PolylineCurve.CreateConvexHull2d`; `Point3d.ArePointsCoplanar`; `Plane.ClosestParameter/.PointAt`; `Mesh.CreateFromClosedPolyline`; `PointContainment` | Spatial |
| `CloudKernel` — neighborhoods (kNN) | Cloud.cs:892-1028 | `NeighborhoodGraphOf`(RTree kNN/radius + receipt), `NeighborhoodReceiptOf`, `RadiusNeighborhoodIdsOf`, `NeighborhoodOf`, `EstimateNormalGraph`, `NeighborhoodPcaOf`/`NeighborhoodPcaSampleOf`, `NeighborhoodCovarianceOf`, `SelfNeighborIncluded`. | 8 | **DUP#3: RTree kNN wrapper** — one of 3 independent `RTree.PointCloudKNeighbors` sites (Cloud L918, Align L173, Mesh L930). No shared neighborhood substrate. | `RTree.PointCloudKNeighbors/PointCloudClosestPoints`; `PointCloud.Count/.PointAt`; `RhinoMath.*` | Spatial |
| `CloudKernel` — normal estimation + orientation | Cloud.cs:1031-1091 | `OrientNormalsViaMst`(Hoppe-DeRose 1992 kNN-MST `1-\|nᵢ·nⱼ\|` weighted, Prim), `EstimateNormalsViaCovariance`, `EstimateNormalsFromPoints`, `EstimateNormalsOf`, `BatchedNeighborhoods`. | 9 | Hoppe MST normal orientation — imperative Prim (justified). | `Vector3d` dot/negate; consumes `Direction.Of`(Atoms), `SymmetricMatrix.DecomposeEigen`(Matrix) | Spatial |
| `CloudKernel` — principal curvature | Cloud.cs:1093-1214 | `PrincipalCurvaturesOf`(quadric fit in PCA tangent frame → shape-operator eigen), `QuadraticFit`(6-coef LSQ), `ShapeOperatorEigen`, `CurvednessOf`/`ShapeIndexOf`(Koenderink-van Doorn), `CurvatureRangeOf`(plane/sphere/saddle/mixed classify), `CurvatureAttempt` folds. | 9 | Umbilic-band sphere classification (`SphereLikenessBand=0.35`). | `Vector3d` ops; consumes `Matrix.Of`/`.LeastSquaresDetailed`, `SymmetricMatrix.Of`/`.DecomposeEigen`(Matrix), `Direction.Of`(Atoms) | Spatial |
| `CloudKernel` — Sinkhorn OT | Cloud.cs:1216-1341 | `Sinkhorn<TOut>`/`SinkhornCluster`/`SinkhornOt`(log-domain stabilized, balanced+unbalanced KL relaxation, debiased), `SinkhornPlan.Project`(distance/receipt/correspondence/matrix/barycentric-transport), `LogSumExp`/`ExpFloored`, `MarginalResiduals`/`ScalingResiduals`, `CouplingCorrespondences`. | 9 | World-class OT. `SinkhornPlan.Project` uses `AtomProjection.Rows`. `typeof(TOut)` avoided — uses ProjectionRow. `LogUnderflowFloor=-745` documented. | MathNet `DenseMatrix`/`DenseVector` (`OfRowArrays`/`PointwiseMultiply`/`RowSums`/`ColumnSums`/`Enumerate`); `RhinoMath.*`; consumes `MatrixKernel.FromMathNet`(Matrix) | Spatial |

Cloud note: `CloudKernel` is a heavy cross-cutting provider — Align.cs(a) depends on its `NeighborhoodPcaOf`/`CovarianceOf`; Field.cs `ClusterFieldCase` depends on `MassOf`; Extraction consumes cloud domains. Covariance/PCA correctly single-owned here.

---

## Field.cs (rank 2, 2599 LOC) — scalar/vector/tensor field algebra + implicit/reconstruction

| Unit | file:line | What it does | Q | Bloat/Dup | Rhino coupling (exact) | Home |
|---|---|---|---|---|---|---|
| Field value/policy vocab [Union/SmartEnum] | Field.cs:7-299 | `BlendKind`(Hard/Polynomial/Exponential/Root/Cubic/Chamfer/Groove/Round smin + erosion factors), `BouncePolicy`(Reflect/Refract), `CsgKind`(Union/Intersect/Difference), `Falloff`(Constant/Inverse/InverseSquare/Gaussian/Kernel/AnisotropicKernel), `FieldBlend`(Sum/Average), `KernelKind`(Wendland/Quintic/Cosine/Cubic/Linear/Epanechnikov +profiles), `NoiseKind`(Perlin/Simplex/SmoothSimplex/Worley), `RayPolicy`(Infinite/Segment), `ReconstructionMode`(7)/`Status`, `PoissonBoundary`(Free/Dirichlet/Neumann), `WeightKernelFamily`(5), `IsoSurfaceStatus`, `KernelProfileStatus`, `ProfileExtrusionFeature`. | 9 | Dense closed vocabularies with embedded math (smin/kernel profiles/erosion). 8 erosion-factor consts (L55-62) are hardcoded per-blend Lipschitz-erosion multipliers — parameterization-vs-const judgment call. | `RhinoMath.SqrtEpsilon/ZeroTolerance`; `Math.*` | Spatial(implicit)/Numerics(kernels) |
| `ScalarField` [Union] — cases + operators | Field.cs:301-436 | ~35 cases () + `+`/`-`/`*` operators + `LipschitzBound`. | 9 | Largest ADT in lane (35 cases). `+` operator flattens Blend chains — good. Mesh-aware cases (Geodesic/MCF/SpectralDistance/Stripe/SignedDistanceFromMesh) delegate to MeshKernel (arch L94). | `BoundingBox`; `ImmutableDictionary`; `RhinoMath.*` | Spatial+Meshing |
| `ScalarField.SampleScalar` (35-arm Switch) | Field.cs:759-867 | Continuous evaluation: CSG smin, potential/density/noise/morse/mollifier/worley, twist/bend/elongate/displace domain warps, finite-diff divergence/laplacian/strain, primitive/profile SDF, reconstruction (rbf/mls/levin/apss/poisson/tet). | 9 | Coherent 35-arm dispatch. `SampleMapped` helper collapses scaled/power/clamp/onion/round. Mesh cases → `MeshKernel.HeatGeodesicAt/MeanCurvatureMagnitudeAt/SpectralDistanceAt/StripeAt/SignedDistanceFromMeshDetailed`. | `Math.*`; `Transform.Rotation`; `Point3d/Vector3d` | Spatial+Meshing |
| `ScalarField` — SDF/iso/admit | Field.cs:437-758 | `SampleSdfDetailed`(status-tagged), `IsoSurfaceDetailed`/`IsoSurfaceAttemptDetailed`(native marching-cubes + evaluator-failure receipt), `Admit`/`AdmitScalarPayload`(35-case recursive validation), `AdmitVectorField`/`AdmitTensorField`/`AdmitFalloff`/`AdmitCharges`/`AdmitReconstructionReceipt` + per-mode admit. | 8 | `AdmitScalarPayload`+`AdmitVectorField` are large recursive validation switches (~180 LOC) — necessary but the densest validation surface; mirrors the case unions (two parallel 35/25-arm structures to keep in sync). | `Mesh.CreateFromIsosurface(scalarFieldEvaluator,box,resolution,RootFindingMaxSteps)`; `Interlocked.Increment`; `BoundingBox.Diagonal` | Spatial+Meshing |
| Reconstruction kernel — RBF/MLS | Field.cs:892-943,1285-1357 | `ReconstructCore`(mode dispatch), `BuildRbf`(interp/approx kernel-matrix solve), `EvaluateRbf`/`KernelWeights`, `EvaluateMls`(4-eqn/pt normal-constrained LSQ + SVD rank/condition), `CollectNeighborhood`, `TangentFrame`. | 9 | Research-grade. `typeof` avoided; uses SolveReceipt. | `MathNet` implicit via Matrix; consumes `Matrix.Of/.LeastSquaresDetailed/.SolveDetailed/.DecomposeSvd`(Matrix); `RhinoMath.*` | Meshing/Spatial |
| Reconstruction kernel — Levin MLS | Field.cs:1358-1527 | `EvaluateLevinMls`(2-step: planar-fit via covariance eigen + energy-min offset/normal iteration via `Brent` root-find, then polynomial height fit), `LevinAlternate`/`LevinHeightFit`/`LevinPolyValue`. | 9 | Densest single algorithm in lane; nested iteration + ridge-regularized poly fit. Fully imperative (justified numeric kernel). | `MathNet.Numerics.RootFinding.Brent.TryFindRoot`; `Vector3d/Point3d`; consumes `SymmetricMatrix.DecomposeEigenDetailed`, `Matrix.LeastSquaresDetailed/.DecomposeSvd`(Matrix) | Meshing/Spatial |
| Reconstruction kernel — APSS | Field.cs:1528-1602 | `EvaluateApss`(algebraic point-set surface: weighted sphere/plane fit, Pratt-normalized), `ApssDescribe`(sphere center/radius/curvature + iterative projection). | 9 | Guennebaud-Gross 2007 APSS. | `Vector3d.SquareLength/CrossProduct`; `RhinoMath.*` | Meshing/Spatial |
| Reconstruction kernel — Poisson | Field.cs:944-1167 | `BuildPoisson`/`SplatPoissonField`(trilinear normal splat to lattice), `AssemblePoissonSystem`(7-pt Laplacian + Dirichlet/screening), `ImposeDirichlet`, `AccumulateScreening`, `SummarizePoisson`, `GradientEnergyOf`/`ScreeningEnergyOf`, `PoissonLattice`/`PoissonField`/`PoissonSystem`. | 9 | Kazhdan screened-Poisson on a dense regular lattice (not octree). Depth-capped `PoissonMaxDepth=6`/`PoissonNodeCeiling=262144`. Heavy imperative FEM. | consumes `SparseMatrix.FromTriplets`, `CholeskySparse.Of/.SolveDetailed`, `.SingularSolveDetailed`+`GaugePolicy`(Matrix); `RhinoMath.*` | Meshing |
| Reconstruction kernel — Tet signed-heat FEM | Field.cs:1168-1284 | `SolveTetSignedHeat`(P1 tet FEM: mass/stiffness/heat assembly → heat solve → divergence → Poisson solve → calibrate sign), `AssembleTetFem`/`TetDivergenceOf`/`CalibrateTetSignedHeat`/`InterpolateTet`, tet record structs. | 9 | **DUP#4: this is a signed-heat-method impl for TET domains that parallels Mesh.cs boundary/closed-grid SHM** (`SignedHeatReceipt`/`VolumeGridReceipt`). Two SHM implementations for different domains, no shared solver spine. | consumes `SparseMatrix.FromTriplets`, `CholeskySparse.Of/.SolveDetailed`, `.SingularSolveDetailed`+`GaugePolicy`(Matrix); `Vector3d.CrossProduct/Unitize`; `RhinoMath.*` | Meshing |
| `SdfKind` [SmartEnum<int>] | Field.cs:1636-1714 | 12 exact analytic SDF primitives (Sphere/Box/Capsule/Cylinder/Cone/HalfSpace/CappedCone/Torus/HexPrism/Octahedron/Ellipsoid/Slab) with `Lipschitz`/`RequiredKeys`/`Validate`/`Compute`; `SignedDistance`(pose remap), `SdfCappedConeCentered`/`SdfExactOctahedron`. | 9 | Inigo-Quilez exact SDFs; `ImmutableDictionary<string,double>` param bag is stringy (`"r"`,`"x"` keys) — the one **stringy-parameterization** smell (contra hardcoded-value ban). | `Point3d/Vector2d`; `Plane.RemapToPlaneSpace`; `ImmutableDictionary`; `Math.*` | Spatial(implicit) |
| SDF-mesh policy vocab [SmartEnum] | Field.cs:1716-1740,2255-2326 | `SdfMeshMethod`(GeneralizedWinding/BoundarySignedHeat/ClosedSurfaceSignedHeat), `SdfSignConvention`, `SdfStatus`, `SignedHeatTime`, `VolumeGridPolicy`, `VolumeSolverPolicy`, `VolumeBoundaryCondition`, `VolumeInterpolation`, `SdfMeshPolicy`, `TetGaugePolicy`, `TetInterpolation`, `TetSignedHeatPolicy`. | 8 | **STRATA SPLIT: SDF-mesh POLICY vocab defined HERE but the executing KERNEL (`MeshKernel.SignedDistanceFromMeshDetailed`/`GeneralizedWindingDistance`) + RECEIPTS (`SdfMeshReceipt`/`SignedHeatReceipt`/`VolumeGridReceipt`/`SdfMeshSample`) live in Mesh.cs(a).** Policy⊥kernel ownership fracture. | `RhinoMath` | Meshing |
| `TensorField` [Union] | Field.cs:1742-1798 | Constant/Curvature/Lift/Warp/Scaled/Blend; `SampleTensor`, `PrincipalDirections`, `BlendTensors`, `TransformTensor`(R·M·Rᵀ). | 8 | `CurvatureCase` delegates to `SurfaceProjection.ShapeOperator`(Modes) — correct reuse. `LiftCase` carries a raw `Func<Point3d,SymmetricMatrix>` (opaque closure — non-inspectable). | `Surface.ClosestPoint`; `Transform.TryGetInverse`; consumes `SymmetricMatrix.Of/.ToDense/.DecomposeEigen`, `Matrix.Of/.Multiply/.Transpose`(Matrix), `Direction.Of`(Atoms) | Numerics+Spatial |
| `VectorField` [Union] — cases + operators | Field.cs:1806-1881 | ~25 cases (Constant/Blend/Scaled/Influence/HitField/Vortex/Ring/Helical/Coulomb/ClusterField/Dipole/Harmonic/Projected/Warp/ClampMagnitude/Gradient/Curl/CurlNoise/CrossProduct/BiotSavart/Saddle/CrossField/Hodge/VectorHeat/GeodesicTangent/TangentLogMap) + operators + 20 factories. | 9 | Mesh-aware cases (CrossField/Hodge/VectorHeat/GeodesicTangent/TangentLogMap) delegate to MeshKernel (arch L94). Rich analytic field library. | `Vector3d/Point3d/Plane/Transform` | Spatial+Meshing |
| `VectorField.SampleVector` (25-arm Switch) + kernels | Field.cs:1882-1995 | Analytic sampling: influence/hit (closest-directed), vortex/ring/helical (`RotationalField`), coulomb/cluster (`SampleRadialContribution`), dipole/harmonic/saddle/biot-savart (`BiotSavartContribution`), gradient/curl/curlnoise/divergence (finite-diff), warp/projected/clamp; mesh cases → MeshKernel. | 9 | `ClosestDirected`/`RotationalField`/`SampleRadialContribution` collapse shared field structure well. | `Vector3d.CrossProduct/Decompose`; `Transform.PlanarProjection`; consumes `SupportSpace.Closest`(Space), `CloudKernel` cluster(Cloud), `MeshKernel.CrossFieldAt/HodgeProjectedAt/VectorHeatAt/GeodesicTangentAt/TangentLogMapAt`(Mesh) | Spatial+Meshing |
| Reconstruction/volume models | Field.cs:1997-2333 | `IsoSurfaceGrid/Receipt/Result`, `KernelProfile`, `MlsSample`, `Reconstruction{Receipt,Result,Sample,SampleReceipt}`, `LevinMlsPolicy/SampleReceipt`, `ApssPolicy/SampleReceipt`, `PoissonPolicy/Grid/Receipt`, `SdfReceipt/Sample`, `TetCell/FemReceipt/InterpolationReceipt/MeshDomain/CellMetric/SignedHeat{Policy,Receipt,Sample}`. | 8 | `PoissonPolicy` = 17 fields, several unused by the dense-lattice impl (octree-era knobs: FullDepth/CgDepth/KernelDepth/Confidence/LinearFit/PrimalGrid) — **vestigial-knob bloat**. `TetMeshDomain.Of` does full topology derivation (boundary-face extraction) — heavy value-object constructor. | `BoundingBox`; `Mesh`(implicit); `StructLayout(LayoutKind.Auto)` | Meshing+Spatial |
| `FieldNabla` — validation gate | Field.cs:2335-2588 | ~40 guard helpers: `NotNull`/`AllFinite`/`AllFiniteSpan`/`FinitePositive`/`Finite`(point/vector/double)/`Positive`/`Dimension`/`WithPositive(Pair)`/`WithDivisor`/`WithSourceEpsilon`/`Plane`/`PlaneSequence`/`Direction`/`Cone`/`Period`/`KernelInput`/`FalloffInput`/`NoiseInput`/`MlsInput`/`LevinPolicyInput`/`ApssPolicyInput`/`PoissonPolicyInput`/`ProfileExtrusionInput`/`IsoGrid`/`MeshNative`/`MeshOf`/`MeshVertices`/`MeshSources`/`ScalarMeshPayload`. | 7 | **Grab-bag utility (254 LOC).** Mixed concern: pure validation + factory sugar + input-admit. Many single-caller guards. `Plane`/`Direction`/`MeshOf` validations parallel Domain/Atoms/Mesh admission — potential centralization into Domain boundary owner. Named `FieldNabla` but serves ALL Vectors modules (Cloud/Spectral/Extraction/Intent all call it). | `RhinoMath.*`; `TensorPrimitives.IsFiniteAll/Min`; `Vector3d.AreOrthonormal/AreRighthanded`; `Plane`; `Curve.CreateBooleanRegions/DuplicateCurve/TryGetPlane`; `Intersection.CurveSelf`; `CurveBooleanRegions`; `BoundingBox` | Numerics (validation)/Domain-boundary |
| `FieldNabla` — differential operators | Field.cs:2591-2612 | `SampleAxes`(6-axis central-difference stencil), `GradientAt`/`CurlAt`/`CurlNoiseAt`/`DivergenceAt`/`LaplacianAt`/`StrainMagnitudeAt` — finite-difference vector calculus over any field. | 8 | Clean central-difference calculus. Distinct concern from the validation half of FieldNabla — **should split** from the guard grab-bag. `CurlOffset2/3` are hardcoded decorrelation offsets (L2337). | `Vector3d.XAxis/YAxis/ZAxis`; `RhinoMath.ZeroTolerance` | Numerics |
| `FieldNoise` | Field.cs:2615-2662 | `PerlinAt`/`WorleyAt`/`SkewedSimplexAt`/`SimplexAt` + `PermTable`(256)/`Perm`/`Fade`/`Grad`/`HashCell`/`SimplexCorner`. | 8 | Standard gradient/cellular noise. `PermTable` is the canonical Perlin permutation (hardcoded-by-spec, acceptable). | `Point3d`; `Math.Floor` | Spatial/Numerics |

---

## CROSS-CUTTING FINDINGS

### Entry-point rot
- **Field.cs: 58 public field factories** (`ScalarField.*`/`VectorField.*`/`TensorField.*`) + **Intent.cs: 41 `VectorIntent` factories**. Combined ~100 top-level static factory methods form the public surface. `VectorIntent.Project<TOut>(Context,Op?)` is the single *consumer* rail, but *construction* is knob-spam: per-case bespoke factories with overloads (Cloud/Hull/Features/Contour ×2). The new partition needs a denser construction entry that hides per-case admission (arch [WORKSPACE_LAW] "one arity owns all modalities").
- `FieldNabla` (Field.cs:2335) is a 254-LOC cross-module utility named for one module but consumed by all six — a mis-homed shared boundary/validation owner, not a Field concern.

### Parallel rails / fragmentation
1. **Three cotangent computations**: Spectral intrinsic `CotAtOppositeCorner`+`ComputeIntrinsicStar1` (edge-length, L395-415) · Spectral extrinsic `ScatterCotangentDivergence` (face-geometry, L739/854) · Mesh.cs `AssembleCotangent` (L1109). One cotangent-weight primitive should own all three; the extrinsic-vs-intrinsic split is real but the arithmetic is duplicated.
2. **Two signed-heat-method implementations**: Field.cs `SolveTetSignedHeat` (P1 tet FEM, L1173) vs Mesh.cs boundary/closed-grid SHM (`SignedHeatReceipt`/`VolumeGridReceipt`). Both do heat-solve→divergence→Poisson-solve→sign-calibrate on different discretizations with no shared solver spine.
3. **Three independent RTree kNN wrappers**: Cloud `NeighborhoodGraphOf` (L912) · Align `FindCorrespondences` (L173) · Mesh power-diagram seed (L930). No shared neighborhood substrate; each re-derives receipt/policy.
4. **Two parallel-transport / RMF sites**: Cloud `BishopChainOf` double-reflection (L578) vs Atoms `Direction.ParallelTransport(Seq<Plane>)` (arch L91). Same rotation-minimizing-frame concept.
5. **Two quaternion-slerp sites**: Modes `MotionInterpolation.InterpolatePlanes` (L138) vs Intent `Dispatch` slerp/pose arms (L296-328).
6. **`typeof(TOut)` reflection dispatch** in Extraction `ExtractionProbe.Project`/`Extraction.Project` (L294-411) — a stringy type-switch where the rest of the corpus uses typed `ProjectionRow` rows (`AtomProjection.Rows`). Weakest dispatch pattern in the lane; contradicts the ban on fragile stringy logic.

### Receipt-vs-algorithm / policy-vs-kernel ownership splits
- **Hodge**: algorithm `HodgeDecomposeDetailed` (Spectral.cs:596) produces `HodgeDecompositionReceipt` declared in **Mesh.cs:442**.
- **HarmonicOneForm**: `HarmonicOneFormReceipt/Basis` + `BuildHarmonicOneForms` in Spectral; `SignpostTransportReceipt`/`TangentLogMap*`/`CommonSubdivision` in Mesh — the DDG receipt family is split across Spectral(mine) and Mesh(a).
- **SDF-from-mesh**: policy vocab (`SdfMeshPolicy`/`SignedHeatTime`/`VolumeSolverPolicy`/`VolumeGridPolicy`/`SdfSignConvention`/`SdfMeshMethod`/`VolumeBoundaryCondition`/`VolumeInterpolation`) in **Field.cs**; kernel (`MeshKernel.SignedDistanceFromMeshDetailed`) + receipts (`SdfMeshReceipt`/`SignedHeatReceipt`/`VolumeGridReceipt`/`SdfMeshSample`) in **Mesh.cs**.
- **Spectral descriptor**: `SpectralDescriptor`/`SpectralFilter`/`EvaluateFilteredDetailed`/ranking in Spectral(mine); `SpectralBasisBundle`/`DescriptorReceipt`/`DescriptorResult`/`MeshSamplingSpectrumReceipt`/`MeshKernel.DescribeShape` + basis caching in Mesh(a). Documented substrate split (arch L107) but the receipt fracture is real.

### Hardcoding / stringy logic
- `SdfKind` uses `ImmutableDictionary<string,double>` param bags with string keys (`"r"`,`"x"`,`"y"`,`"z"`,`"h"`,`"angle"`,`"R"`,`"r1"`,`"r2"`,`"s"`) — Field.cs:1638-1685. Only stringy-parameterization in lane; contradicts full-parameterization mandate. Candidate for typed per-primitive parameter records.
- `BlendKind` 8 erosion-factor consts (Field.cs:55-62), `CurlOffset2/3` decorrelation vectors (L2337), `SphereLikenessBand=0.35` (Cloud L1096), `LogUnderflowFloor=-745` (Cloud L1217), `WaveBandwidthFloor=1e-9`/`HarmonicEpsRankDefault=1e-10` (Spectral L238-239), `PoissonMaxDepth=6`/`PoissonNodeCeiling`/`GaussianBandwidthSquared` (Field). Most are documented numeric-analysis constants (acceptable); the erosion factors are the least-justified magic-number table.
- `PoissonPolicy` carries ~6 vestigial octree-era knobs unused by the dense-lattice implementation (FullDepth/CgDepth/KernelDepth/Confidence/ConfidenceBias/LinearFit/PrimalGrid) — dead parameterization.

### Strata violations (against KERNEL→AEC-DOMAIN→… law + retiring Domain/)
- Every lane unit depends UPWARD-INTO Domain/ for the `Op` error rail, `Fin`/`guard`/`Optional` (LanguageExt), `Context` tolerance, `ClosestHit`/`ComponentIndex`, `GeometryKernel.*`, `ScalarMetric`, `Lease<T>`, `IValidityEvidence`, `OpAcceptance`. Domain converts to design pages this campaign → these atoms MUST be re-homed as the KERNEL substrate the whole geometry stack sits on; no Vectors page compiles without them.
- Cross-module kernel reach: Align(a)→`CloudKernel.NeighborhoodPcaOf` (Cloud internal), Field→`CloudKernel.MassOf`, Spectral→`space.Cache.Cholesky`/`MeshKernel.*` (Mesh internal), Extraction→`FlowKernel`/`SampleKind`. Internal-kernel dependencies cross module boundaries pervasively — the new sub-domain partition must decide which are legitimate seams vs leaks.

### The 5-10 strongest structural rulings the architect must make
1. **Re-home the Domain kernel atoms** (`Op`/`Fin`-rail/`Context`/`ClosestHit`/`GeometryKernel`/`ScalarMetric`/`Lease`/`IValidityEvidence`) as the foundational KERNEL page-set every geometry sub-domain imports. Nothing in this lane stands without it. This is the gating decision for the whole conversion.
2. **Collapse the ~100-factory construction surface** (Field 58 + Intent 41) into a denser typed construction entry per sub-domain, keeping `VectorIntent.Project<TOut>` as the sole consumer rail. Do not preserve per-case overload spam.
3. **Unify signed-heat-method** across Field-tet + Mesh-surface/grid into one SHM spine (heat→divergence→Poisson→sign-calibrate) parameterized by discretization (tet/surface/regular-grid), and place the SDF-mesh POLICY vocab beside its KERNEL and RECEIPTS (currently fractured Field↔Mesh).
4. **Own cotangent-weight once**: one intrinsic/extrinsic cotangent primitive consumed by Spectral DEC assembly, Spectral heat-scaffold, and Mesh cotangent Laplacian — end the three-path duplication.
5. **Extract a shared neighborhood/kNN substrate** (RTree kNN + radius + receipt) consumed by Cloud metrics, Align correspondence, and Mesh power-diagram — end the three parallel RTree wrappers.
6. **Split `FieldNabla`**: the differential-calculus half (SampleAxes/Gradient/Curl/Div/Laplacian/Strain) → a Numerics finite-difference owner; the validation/admission half → the re-homed Domain boundary owner. It is not a Field concern.
7. **Kill `typeof(TOut)` dispatch** in Extraction (ExtractionProbe/Extraction.Project) — convert to typed `ProjectionRow` rows like the rest of the corpus already does (Cloud/Spectral/Field `.Project` all use `AtomProjection.Rows`).
8. **Resolve the DDG receipt fracture**: `DiscreteCalculus`/`HarmonicOneForm*` (Spectral) vs `HodgeDecompositionReceipt`/`SignpostTransportReceipt`/`TangentLogMap*` (Mesh) — one DDG receipt family, one owner, with `DiscreteCalculus` as the Numerics seam the `Rasm.Compute` adjoint surface already binds.
9. **Type the SDF parameter bag**: replace `SdfKind`'s `ImmutableDictionary<string,double>` string-keyed params with typed per-primitive parameter records (full-parameterization mandate).
10. **Sub-domain placement**: Numerics = DEC linear-algebra (`DiscreteCalculus`/`SpectralBasis`/`SpectralFilter`/descriptor algebra), tensor transforms, finite-difference calculus, noise, matrix-adjacent; Meshing = DEC assembly (CR/CDS/harmonic/heat-scaffold), reconstruction (Poisson/tet/MLS/APSS/Levin), mesh-aware field cases, scalar-isoline PL, SDF-mesh; Spatial = clouds/metrics/hull/transport/normals/curvature, analytic scalar/vector/tensor fields, SDF primitives, support-field cases; Parametric = curve/surface/cone/pose projections (Modes); Processing = extraction/contour/glyph/streamline/sample rail (Extraction + Sample); package entry root = `VectorIntent`.
