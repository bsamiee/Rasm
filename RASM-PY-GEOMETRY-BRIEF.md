# [RASM_PY_GEOMETRY_BRIEF]

Authoritative campaign input for the ground-up restructure and rebuild of `libs/python/geometry/.planning/`. The page-set delta is fully ruled here: seven NEW pages — the four-page `energy/` sub-domain, `mesh/serve.md`, `graduation.md`, `graph/analytic.md` — one vocabulary re-homing, zero page deletions. `.archive/RASM-PY-RUNTIME-BRIEF.md` and `RASM-PY-DATA-BRIEF.md` (landed; archives at close) are finalized upstream law this brief consumes at the seam, their rulings LANDED on disk — the identity rename, the lanes offload axis, the reproduction corpus, and the data `MeshPayload`/Arrow-bridge/codec-shed seams are upstream rulings geometry rewires against, never re-plans. Standing upstream exports geometry consumes, every one disk-verified LANDED: the `offload` `retry: RetryClass` keyword, the THREAD-modality band, and the `OCCT`/`RPC`/`OCC_NATIVE` POLICY rows (`reliability/resilience.md:78-80,282-288`, `execution/lanes.md:69,98,164`, geometry named); the reproduction fixture — landed as the `name="glb-by-key"` design-pin with `payload=Nothing` (`evidence/reproduction.md:128-130`; this campaign's daemon graduates it) — and the `IdentityPolicy`-as-generic-seed LAW, landed as prose (`evidence/identity.md:13`) while the fence still carries the mesher knobs (`:92-95`), the `[V3]` strip being this campaign's authorized 1-hop runtime edit; the recipe rail with geometry's energy plane the named `python:` consumer (`execution/recipe.md:5`); the C#-minted tessellation wire rows — LANDED as `TessellationRequest`/`TessellationReceipt` with their registry rows (`transport/shapes.md:194-206,268-269`, geometry `mesh/serve` named at `:19`); and `ServerHost`/`dispatch` sibling-composable servicer registration (`transport/serve.md:133,144`). `networkx` stays branch-tier with geometry the analysis consumer of record and geometry's `arrow_las` boundary decode ALREADY sits on `data/.api/pyarrow.md:12`'s owner line — both dual-consumer motions are landed; `[V11]` verifies them. `compute` is DOWNSTREAM — `RASM-PY-COMPUTE-BRIEF.md` consumes this brief as finalized upstream law: this brief pins the graduation contract compute inherits (`[V2]`) and geometry stops importing it. `docs/stacks/python/` governs.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level excellent — dense, table-driven, doctrine-fluent, `.api`-grounded — and seam-level and governance-level broken. The energy plane, formerly the folder's largest unrealized claim, stands realized under `[V1]`: `energy/{climate,model,district,simulate}.md` on disk, README router rows `[19]-[22]`, the `ARCHITECTURE.md` `energy/` codemap nodes and seam rows, and the ten energy catalogs' consuming charter lines. Three standing proofs, each independently fatal to a world-class claim:

1. **The load-bearing wire owner cannot deliver its product.** `mesh/daemon` computes per-element GLB and strands it: `tessellate` returns `self` (`daemon.md:181`), `contribute` yields receipts carrying only `content_key.hex` and counts (`:78-84,219`), the GLB lives solely in `self._cache` (`:171,175`), and the method roster exposes no accessor. `ComputeService`/`ArtifactSync`/`ServerHost` exist as Boundary prose only (`daemon.md:3,20,222`; `cad.md:22`); the five wire/transport seam rows (`ARCH:44-49,63` post-energy-shift) hang on zero fences. The C# side can never retrieve the folder's flagship output.
2. **Every graduation and identity prelude binds a soon-deleted or unbuilt module.** Twelve pages import `rasm.runtime.content_identity` (`algebra.md:49`, `features.md:44`, `nonmanifold.md:42`, `analysis.md:37`, `costing.md:35`, `structural.md:42`, `brep.md:58`, `cad.md:51`, `daemon.md:36`, `deviation.md:33`, `reconstruction.md:37`, `registration.md:31`) — the exact spelling runtime `[V4]` deletes; `daemon.md:228` anchors `evidence/identity#SEED_REPRODUCTION`, which runtime `[V7]` re-homes. Eleven pages import `rasm.compute.graduation.handoff` (`algebra.md:48`, `features.md:43`, `nonmanifold.md:41`, `analysis.md:35`, `costing.md:33`, `structural.md:40`, `brep.md:57`, `repair.md:35`, `deviation.md:37`, `reconstruction.md:41`, `registration.md:35`) — compute is the LAST campaign folder, so the producer imports its unbuilt consumer, codes against unbuilt page anchors (`structural.md:528`), and is unimportable until the folder AFTER it lands. The folder breaks the moment its upstream lands and cannot import until its downstream exists.
3. **The scan plane's concurrency is illusory and the product carries hard corruption.** All four scan owners declare `lane: LanePolicy | None = None` (`ingestion.md:145`, `registration.md:170`, `deviation.md:248`, `reconstruction.md:243`) and compose it in ZERO fences — the entries are sync `def` (`ingestion.md:147`, `registration.md:172`, `deviation.md:250`, `reconstruction.md:245`) running pdal SMRF, ICP, RANSAC, and Poisson on the event loop against their own Entry prose. Beside it, hard defects in the product: `quality.md:346` is dedented to column 0 inside an 8-space method body (`IndentationError`), `quality.md:365` is a stray fence delimiter, `nonmanifold.md:459-460` leaks literal `</content>`/`</invoke>` tool XML into the page, and three limiter comments truncate mid-sentence (`algebra.md:98-99`, `features.md:139-140`, `nonmanifold.md:98`).

What IS sound: the table-driven dispatch discipline corpus-wide (`MOMENT_KERNELS`/`PROFILE_SAMPLERS`/`FEATURE_OPS`/`ANALYTICS`/`READERS`/`_CONSTRUCT`/`_STAGE`/`IfcApiVerb` — the next case is one row everywhere), the runtime FIT (`boundary`/`@receipted`/`guarded`/`railed`/`Receipt.of(owner, evidence)` consumed by symbol, nothing re-minted), every C# seam decode-only with the seed-zero GLB key a reproduce-under-parity (`daemon.md:228`), the internal import graph ACYCLIC (four edges, all leaf-sink — the one structural win over runtime/data), and two reference pages (`ifc/selector`, `ifc/authoring`). The rot is an unwired wire, inverted seams, and duplicated cross-cutting mechanisms — page-local fixes cannot reach it; seams first.

### [TELOS]

`geometry` is the host-free geometry/IFC/BIM evaluation companion of the Python branch — the independent peer producer that meets C# only at the wire (`libs/.planning/architecture.md` `[04]-[GEOMETRY_FLOW]`): the IfcOpenShell GLB tessellation daemon the C# Bim/Compute rail and the TS viewer consume, scan verification (ingest/register/deviate/reconstruct), IFC analysis/authoring/costing/structural evaluation, computational-geometry and graph analytics, and — ruled here — the building-physics energy/climate/comfort band. World-class means the COMPOSED companion: source bytes arrive over the wire, tessellate content-keyed with cache elision, the GLB streams BACK across `ArtifactSync`, scan clouds verify against it, IFC models analyze/author/cost/enrich against it, evidence graduates to compute under differentiated subjects, and energy models simulate through the runtime recipe rail — every step railed, receipted, and off the event loop. Assume 5x the consumer demand on every surface: C# `ArtifactSync` consumes the GLB stream, the TS viewer renders it, compute admits the graduation evidence, data decodes the scan Arrow records, construction-verification and energy evidence feed artifacts reports (the result frames crossing the data seam self-describing per `[V1]`), and the artifacts scene plane's typed mesh ingress decodes this folder's mesh interchange (`RASM-PY-ARTIFACTS-BRIEF.md` `[V9]`/`[V15]` — the massing suite's named upstream) — zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The campaign owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — the four-page `energy/` sub-domain (realized), NEW `mesh/serve.md`, NEW `graduation.md`, NEW `graph/analytic.md`, the `GeometrySubject` re-homing — are the floor, not the ceiling. Every structural change updates `ARCHITECTURE.md` `[01]`+`[02]`+`[03]`, the `README.md` router and package groups, and BOTH `.api` tiers in the same motion. No page survives on incumbency, and no catalog survives on authorship: a catalog without consuming fences is pressure to realize, never evidence the owner exists.

### [INTERNAL_DAG_LAW]

The realized package is `rasm.geometry.<sub>.<leaf>`; the internal import graph stays acyclic under one topological order — a fence import binds only a strictly-earlier module, braced peers are mutually import-free, and an edge into a later tier is a defect resolved by re-homing or re-tiering, never dodged:

```text
graduation < {graph/analytic, ifc/selector, mesh/cad, mesh/repair}
    < mesh/quality
    < {scan/{ingestion,registration,deviation,reconstruction}, mesh/{spatial,brep},
       graph/{algebra,features,nonmanifold}, ifc/{analysis,authoring,costing,structural}}
    < mesh/daemon < mesh/serve
graduation < energy/climate < energy/model < energy/district < energy/simulate
```

The four standing intra-folder edges stay legal (`daemon.md:35` -> cad; `analysis.md:36`/`costing.md:34`/`structural.md:41` -> selector); the new edges the verdicts mint point strictly downward (producers -> graduation/analytic, quality -> repair, spatial -> repair, reconstruction/deviation -> quality per `[V4]`/`[V7]`, brep -> cad for the `[V3]` `TessellationPolicy` mint, serve -> daemon). The compute forward-import DIES (`[V2]`), so the folder imports only runtime, data, and itself. Every rebuilt page proves its prelude against this order; no fence imports a later-tier module.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it. The live violation class is duplicate MECHANISM, not enumerated instances: three independent `CapacityLimiter(4)` mints whose comments name each other and truncate mid-sentence (`algebra.md:100`, `features.md:141`, `nonmanifold.md:99`); `AnalyticValue` authored verbatim twice (`features.md:166` ≡ `nonmanifold.md:112`) with `_leaders`/`_ranked`/census-peak folds triplicated; the `Mesh`/`Mesh64`-by-uint32-ceiling `Manifold` build spelled three times (`repair.md:198`, `spatial.md:186`, `quality.md:206`) against repair's own canonical-owner charter; the six-element evidence span weave hand-authored three times (`analysis.md:126`, `costing.md:168`, `structural.md:78`) beside seven inline `_TRACER` mints; `ReceiptContributor` consumed three incompatible ways. The test: the next analytics op, manifold consumer, graduating owner, or scan kernel is one row or one composition — never a re-spelling.

### [SEAM_AND_WIRE_LAW]

- ONE wire discipline: geometry meets C# only at the wire — GLB bytes plus content identity over the `ComputeService`/`ArtifactSync` contract. The daemon decodes and reproduces, never re-mints: the seed-zero (`Some(0)`) `XxHash128` GLB wire key equals the C# `RepresentationContentHash` entry byte-for-byte (reproduce-under-parity, proven against the runtime reproduction corpus's landed `name="glb-by-key"` design-pin — `evidence/reproduction.md:128-130`, `payload=Nothing`; this campaign's daemon graduates it into a graded sample), the policy-folded cache key stays distinct, and the `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` count-prefix law is consumed transitively through the runtime writer — geometry keys GLB blobs, never PropertySet bags, and `PY_WIRE_ALIGNMENT` is its own campaign with geometry downstream.
- ONE graduation direction: the handoff vocabulary is geometry-MINTED (`[V2]`); the crossing to compute is content-keyed receipt data, never an import; the frozen contract is pinned in the `graduation.md` fence for the compute campaign to inherit.
- ONE identity mint: every content key rides `rasm.runtime.identity` `ContentIdentity.of` over canonical bytes (12-site rename, `[V12]`); tessellation knobs ride a geometry-owned `TessellationPolicy` folded into the seed, never `IdentityPolicy` fields (`[V3]`).
- The `ARCHITECTURE.md` seam ledger is enforceable law: every wired edge appears as a ledger row and every row has a composing fence. The ledger is currently blind to all four intra-folder edges and seven of eleven graduation seams, mistypes two declared graduation rows `⇄` (`ARCH:59-60` post-energy-shift — geometry only imports), and carries five wire rows with no fence (`ARCH:44-49,63`). `[V11]` closes all of it.
- Upstream consumption is pinned exports, not hope: `guarded`/`RetryClass.OCCT`/`RetryClass.RPC`/`RetryClass.OCC_NATIVE`, `offload(retry=)`, the THREAD band, the `"glb-by-key"` reproduction pin, the tessellation `shapes` registry rows, and the `ServerHost`/`dispatch` sibling registration are all LANDED runtime exports with geometry named — `daemon.md:200`'s `retry=` keyword stands on that ruled upstream surface, and the wire VOCABULARY stays runtime-owned: geometry authors no wire shape — the tessellation rows are LANDED (`transport/shapes.md:194-206` `TessellationRequest`/`TessellationReceipt`, registry rows `:268-269`, geometry `mesh/serve` named the consumer at `:19`), and serve consumes the existing rows by symbol.

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as minted data and is only decoded here — the C#-minted tessellation request over the runtime `shapes` rows (source modality, SPF/STEP source bytes, the `TessellationPolicy` echo), the deviation reference GLB and its element identity fetched BY CONTENT KEY (`ARCH:47` `Rasm.Bim/Model` — scan never re-tessellates), the `BimWire` IFC ingest vocabulary (`ARCH:45`), scan sensor bytes (E57/LAS/COPC through the data codec seams, `ARCH:60`) and the Arrow point-record bridge (`ARCH:56`), data `MeshPayload` cell blocks (`ARCH:57`), and the energy plane's document bytes — HBJSON models, the IFC SPF source bytes the model plane's BIM-to-BEM modality derives rooms from, EPW/STAT/DDY weather, IDS/BCF documents, standards vocabularies resolved by identifier. What is COMPUTED here under a cross-language parity obligation, never a re-mint — the seed-zero (`Some(0)`) GLB wire key equal to the C# `RepresentationContentHash` byte-for-byte, the policy-folded re-tessellation cache key, and the GLB bytes themselves. Deviation verdicts, section integrals, IDS/clash/BCF evidence, energy result frames, graduation carriers, and all receipts are companion-local products, minted freely.

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. Carried obligations: the ten `[GEOMETRY]` energy rows (`pyproject.toml:134-143`) STAND — the realized `[V1]` plane consumes every one; the recipe trio's `[RUNTIME]` tags (`:144-146`) stand — runtime owns execution, geometry binds schemas only. The codec retags are HALF-LANDED: `laspy` (`:59`) already reads `[DATA]` and `meshio` (`:60`) already reads `[DATA_COMPUTE]` (the terminal data+compute tag), and their geometry `.api` catalogs are already struck; `rhino3dm` (`:85`) is the ONE remaining codec retag — still `[GEOMETRY]`-tagged with ZERO geometry imports, geometry's own codec-shed seams ceding it to data — retag `[DATA]` (its geometry catalog is already struck). The same misattribution class covers the geospatial quartet: `geopandas` (`:53`), `pyogrio` (`:81`), `pyproj` (`:82`), and `shapely` (`:119`) are `[GEOMETRY]`-tagged with ZERO geometry fences and ZERO geometry-tier catalogs — data owns all four (data-tier catalogs stand at `data/.api/{geopandas,pyogrio,pyproj,shapely}.md`; the data brief rules them the pushdown-ingress/geospatial owners), and geometry's only touchpoint is the dragonfly GeoJSON exchange composing at the data seam (`dragonfly-core.md:82`/`dragonfly-energy.md:81` already mark them cross-folder) — retag all four `[DATA]` in the same `[V11]` governance motion. Admission-owner truth is manifest law: the ten companion-band packages (`ifcopenshell`, `open3d`, `small-gicp`, `topologicpy`, and the utility ring `ifctester`/`ifcclash`/`ifc5d`/`ifc4d`/`ifcpatch`/`ifcdiff`) carry NO root-manifest row — the former `geometry-companion` dependency group is struck from `pyproject.toml`, and the band provisions through the companion-lane owner (`uv run python -m tools.assay provision` over the Forge cp312 scientific environment) — and the README `[02]` preamble already states each group's admission owner on disk (`README.md:32` — root-manifest row, `<3.15`-markered worker row, or companion-lane provisioning; a package with neither is a phantom), the registry truth `[V11]` verifies. The markered worker rows that DO sit in the manifest (`pye57`/`cadquery-ocp`/`kiss-matcher`) stay manifest-owned. The TWO ADDITIONS are LANDED in the manifest: `rtree` (`:131`, no marker — `py3-none` platform wheels, live-imports on 3.15) and `python-fcl` (`:132`, `python_version<'3.15'` — cp314 wheel ceiling), both `[GEOMETRY]` worker-lane rows with folder-tier `.api` stubs authored and README rows registered; the named concerns stand fence-bound (`spatial.md:3,39-43,126-130`) and `[V9]` composes the admitted packages; `embreex` stays an unadmitted transparent trimesh accel (no fence binds its members). The `python_version<'3.15'` pins on `pye57`/`cadquery-ocp`/`kiss-matcher` (`:79,129,130`) stand on real cp315 wheel gaps (pye57 0.4.19 and cadquery-ocp 7.9.3.1.1 cap at cp314 — cadquery-ocp's own `requires_python` is `<3.15`; kiss-matcher 1.0.2 caps at cp312). The categorical-best supersession sweep returns ZERO strikes beyond the ruled retags — the apparent overlaps are complementary by boundary: `pye57` owns the typed `ScanHeader` pose/metadata surface `pdal`'s flat `Reader.e57` row drops, `small-gicp` owns the VGICP fine-refinement speed path beside open3d's tensor multiscale/colored arms, and `kiss-matcher`-primary/FGR-fallback is realized selection law, never duplication. `gmsh` stays deferred with no catalog until a consuming fence exists; its sdist-build exclusion stands as a standing admission blocker. `topologicpy` stays the AGPL opt-in lane; the permissive replacement stays a deferred growth direction on the `[V6]` owner. The categorical-best sweep over the energy/scan/mesh planes returns zero further admissions beyond the two ruled additions: deferred capability directions (the daylight modality via `honeybee-radiance`, urban-microclimate EPW morphing) stay admission-gated growth on `[V1]`, never pre-admitted dead rows. `.api` tier motions are LANDED and `[V11]` verifies them: the geometry-tier `queenbee`/`lbt-recipes`/`pollination-handlers` catalogs struck (runtime-tier canonical), `geometry/.api/{rhino3dm,meshio,laspy}.md` struck, `networkx` branch-tier dual-consumer, `data/.api/pyarrow.md:12` carrying geometry's `arrow_las` boundary decode on its owner line. Paid/gated/proprietary stays rejected.

## [01]-[VERDICTS]

Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; the build may exceed a floor, never dilute it.

### [V1_ENERGY_PLANE]

Ruled REALIZE and realized on disk — the campaign's defining coverage ruling, standing as the folder's energy contract. Four-page plane: `energy/climate.md` — the ladybug climate/comfort owner (EPW/Wea/Location intake, analysis periods, `Sunpath` solar geometry — the energy plane's solar owner only: artifacts' diagram sun-path furniture stays its own closed-form kernel per `RASM-PY-ARTIFACTS-BRIEF.md` `[V15]`, so `Sunpath` gains no diagram consumer — `DataCollection` algebra, and the comfort tier — PMV/UTCI/adaptive/SolarCal point models and maps over `ladybug-comfort` — as an owner block); `energy/model.md` — the HBJSON building-model owner (honeybee-core object graph + the `check_all` validation gate as the model admission rail — ONE polymorphic admission discriminating by input shape: HBJSON bytes ARRIVE and decode (`Model.from_dict`/`from_hbjson`), and the BIM-to-BEM derivation is COMPUTED here, the modality only this folder can own — `ifcopenshell` bound directly (`by_type("IfcSpace")` + `geom.create_shape` space solids; a package consume, never an ifc-plane page import) lifts through `Polyface3D.from_faces` into `Room.from_polyface3d`, `Room.solve_adjacency(rooms, tolerance)` closes the zone graph, and the `Face3D` aperture generators (`sub_faces_by_ratio*`) mint fenestration where the source model carries none, as ratio policy rows — both modalities fronted by the same `check_all` gate, never a second entry; honeybee-energy constructions/loads/schedules/HVAC properties, the two standards backends resolved by identifier — honeybee-standards defaults and honeybee-energy-standards ASHRAE 90.1/DOE vintage sets as seed rows, never hand-copied data); `energy/district.md` — the dragonfly district owner (2.5-D massing object graph, the `to_honeybee` explosion seam onto `energy/model`, dragonfly-energy URBANopt/DES/OpenDSS translation rows); `energy/simulate.md` — the simulation egress owner (honeybee-openstudio in-process OSM/IDF translation OFFLOADED on the worker lane, the recipe BINDING — geometry constructs queenbee `Job`/`RecipeInterface` and coerces inputs through pollination-handlers, then hands execution to runtime `execution/recipe` per the README's own "runtime owns execution" concession — and SQLite result decode back through ladybug-core into typed result frames crossing the data seam — the crossing fence-pinned SELF-DESCRIBING per the data assessment-wire discipline: output name, unit, analysis period, room/zone identifier, and content key ride the frame columns, the physical crossing content-keyed Arrow bytes composed at the consumer edge, never comment-flow). Every page is a producer: receipts through the `[V5]` weave, graduation through `[V2]`'s `building-energy`/`thermal-comfort` subjects, AGPL isolation at the process boundary per the standing companion-lane charter. The catalogs dispose complete onto their integration homes, no energy admission left ownerless: `energy/climate` consumes `ladybug-core`+`ladybug-comfort`; `energy/model` consumes `ladybug-geometry`+`honeybee-core`+`honeybee-energy`+`honeybee-standards`+`honeybee-energy-standards`; `energy/district` consumes `dragonfly-core`+`dragonfly-energy`; `energy/simulate` consumes `honeybee-openstudio` plus `ladybug-core`'s SQLite decode and binds `queenbee`/`lbt-recipes`/`pollination-handlers` schema-only over the runtime-tier catalogs. Growth stays admission-gated, never pre-admitted: the daylight modality (`honeybee-radiance` model properties plus the lbt-recipes daylight recipes) and urban-microclimate EPW morphing extend `energy/model`/`energy/district` only through a future package-admission motion carrying its own catalogs and consuming fences. The plane's seam rows enter the ledger with the pages: `energy/simulate -> python:runtime/recipe`, `energy -> python:data/tabular` (result frames), `energy -> graduation`.
The realized plane is the standing contract: `energy/climate.md` (`Climate` — shape-discriminated EPW admission, `SeriesSpec`/`Reduce` derived-name collection algebra, `Derived`/`SolarQuery` families, the `COMFORT` PMV/UTCI/PET rows + `MAPS` spatial rows, `OutdoorSolarCal` MRT), `energy/model.md` (`BuildingModel` — the one `ModelSource` hbjson/bim admission under one `check_all(detailed=True)` gate, `BemPolicy` orientation-binned WWR rows, `EnergySpec`+`RESOLVERS` standards assignment, `wire()` content-keyed canonical HBJSON bytes), `energy/district.md` (`District` — dfjson/GeoJSON/massing admission, ordered `intersect_adjacency`->`solve_adjacency`, `explode` crossing every emitted model through `BuildingModel.of`, the `DistrictTarget` urbanopt/des_param/opendss/reopt/geojson rows), `energy/simulate.md` (`Simulation` — the `WRITERS` translate rows behind the `find_spec("openstudio")` probe with the OSW/CLI fall-through, the runtime `RecipeExecution` binding for `annual-energy-use`, `ResultQuery` SQL decode into the seven-column `output`/`unit`/`period`/`zone`/`step`/`value`/`content_key` frames, `crossing` over the data `arrow_bytes` fold). README router rows [19]-[22], the ARCH `energy/` codemap nodes, the energy seam rows, the AGPL companion-lane paragraph, and the ten catalogs' `- consumer:` charter lines all stand; the geometry-tier `queenbee`/`lbt-recipes`/`pollination-handlers` catalogs are struck per `[V11]` (runtime tier canonical). The pages consume the graduation spine under the pinned spellings `graduation.md` exports: `evidence_run(EvidenceScope.ENERGY_CLIMATE|ENERGY_MODEL|ENERGY_DISTRICT|ENERGY_SIMULATE, <operation>, dispatch) -> RuntimeRail[T]`, `GeometryHandoff.of(subject, key, measured, ceilings)`, and `GeometrySubject.BUILDING_ENERGY`/`THERMAL_COMFORT`; identity rides `rasm.runtime.identity` with the raising `ContentIdentity.key` inside every fence (the `evidence_run` boundary converts).

### [V2_GRADUATION_INVERSION]

NEW `graduation.md` at the `.planning/` root (module `rasm.geometry.graduation`) — the folder's tier-0 evidence spine; root-siting is deliberate (the one cross-sub-domain leaf; a one-page sub-folder is structure spam). Three rulings. (a) The subject vocabulary re-homes: `GeometrySubject` is minted HERE — the producer owns the vocabulary of what it produces, matching the build order — and all eleven `from rasm.compute.graduation.handoff` imports die; recited compute-page anchors (`structural.md:513`'s union recital, `deviation.md:337`, `registration.md:380`, and every sibling research-tail recital — `[V10]` purges the tier) rewire to the local owner. (b) The subject union DIFFERENTIATES: `"numerical-primitive"` today carries four semantically distinct evidence classes (`algebra.md:336` compas numerics, `analysis.md:121` BIM compliance/clash/BCF, `costing.md:157` 5D/4D lifecycle, `structural.md:74` section integrals); recommended floor — `bim-compliance`, `bim-lifecycle`, and `section-property` join the union, algebra keeps `numerical-primitive`, and `[V1]` adds `building-energy`/`thermal-comfort`; each graduating owner binds its own member. (c) The crossing becomes data: `graduation.md` mints the typed `GeometryHandoff` carrier (subject, evidence key, measured facts, ceilings) every producer's `graduates()` returns, and the fence PINS the frozen boundary contract the compute campaign inherits with geometry the named demanding consumer — the eight standing literals plus the new members, the `graduates(owner, HandoffAxis(geometry=subject), key, measured, ceiling)` admission shape, and the residual-over-ceiling `_admit` direction; compute's `HandoffAxis` hub and `GraduationReceipt` admission fold DECODE this carrier at the seam and re-shape nothing without a geometry ripple. The pin is contract DATA — the frozen union, admission shape, and `_admit` direction stated once in the fence's contract block — never a geometry-typed callable over compute symbols: `HandoffAxis`/`GraduationReceipt` appear in no geometry prelude. Deleted forms: any `from rasm.compute` import, any recited compute interior anchor, any second subject vocabulary.

### [V3_DAEMON_WIRE_EGRESS]

The flagship output becomes reachable and served. Ruled: `tessellate` returns the results — `RuntimeRail[Block[TessellationResult]]` replaces the fluent `self` (`daemon.md:181`), receipts stay on `contribute` — and NEW `mesh/serve.md` binds the daemon behind the C# wire: the geometry servicer registered in the runtime `ServerHost` lifecycle, streaming per-element GLB plus the semantic header over `ComputeService`/`ArtifactSync` with the 64 KiB `FrameEdge` framing, per-frame Crc32, and whole-artifact XxHash128 the daemon's own research already proves (`daemon.md:222`); the five wire/transport ledger rows (`ARCH:44-49,63` post-shift) re-file onto serve as composed fences or die. Registration direction is boundary law: serve owns the geometry-side composition root — it registers the tessellation servicer as `ServerHost.dispatch` descriptor-id + codec-pair + railed-handler rows and composes the runtime Entrypoint install -> admit -> serve -> drain fold from the geometry side (both runtime `[V6]` pinned exports); runtime never imports geometry. The request/response wire shapes import from `rasm.runtime.shapes` — the LANDED `TessellationRequest`/`TessellationReceipt` registry rows beside `artifact_frame` (`transport/shapes.md:194-206,268-269`; `Rasm.Bim/Exchange/tessellation` + `Rasm.Compute/Runtime/codecs` are the C# mints) — never a geometry-authored shape. The decoded FIELD FLOOR is pinned consumer law riding those rows (runtime `[V2]` carries it): the request delivers source modality, source bytes, and the `TessellationPolicy` echo; the response/receipt delivers the content key, the element/triangle tally, the `SemanticHeader` schema/project, the whole-artifact XxHash128, and the replay provenance (the daemon's `admitted`-vs-`emitted` phase) so the C# artifact index distinguishes by-reference replay from fresh iteration — serve decodes every floor field by symbol; a response the consumer cannot dedupe or attribute is a chain break, never a doc gap. The daemon/serve boundary: daemon tessellates, caches, and keys, never opening a channel or framing bytes; serve registers, frames, and streams, never tessellating, re-keying, or reaching past the daemon's returned results. In the same motion the policy coupling dies: the mesher knobs (`deflection`/`angle_tolerance`/`spec`) leave runtime `IdentityPolicy` for a geometry-owned `TessellationPolicy` — MINTED on `mesh/cad` beside `BridgeFormat` (`StepBridge.tessellate` consumes it in-signature at tier 1; a daemon-sited mint forces a cad -> daemon back-edge), daemon and brep importing it downward — folded into the canonical seed bytes — `daemon.md:15,92-95`, `cad.md:51,221,233,246-254`, and `brep.md:58,228,247` rewire and `ARCH:45` (post-shift) rewords. The runtime half is an AUTHORIZED 1-hop edit landing with this motion: the runtime fence still carries the knobs (`evidence/identity.md:92-95` plus the `spec` fold `:98-99`) against its own generic-seed prose (`:13`) — the strip removes `deflection`/`angle_tolerance` and their `spec` components from the `IdentityPolicy` owner block, `CANONICAL_POLICY` (`:102`) and the `ContentIdentity.of` policy parameter unchanged, resolving the runtime page's prose/fence split-brain to the prose's own law. The two-key discipline is preserved law: the policy-folded re-tessellation cache key and the seed-zero GLB wire key stay distinct, the wire key proven against the runtime reproduction corpus's landed `name="glb-by-key"` design-pin (`evidence/reproduction.md:128-130` — the daemon graduates the pinned row into a graded sample; the anchor migrates from `evidence/identity` to `rasm.runtime.reproduction`, both landed). `daemon.md:200`'s `offload(..., retry=RetryClass.OCCT)` stands on the pinned runtime surface.

### [V4_SCAN_LANE_REALITY]

The scan family adopts the mesh worker shape: `async def` entries composing `await lane.offload(_dispatch, ...)` at all four owners (`ingestion.md:147`, `registration.md:172`, `deviation.md:250`, `reconstruction.md:245` — the kernels are already module-level and picklable, and the pdal stage graph additionally crosses by its own JSON pickle state, `.api/pdal.md:79,115` `__getstate__`/`__setstate__` round-tripping the pipeline JSON, so ingestion's `_execute` survives even the process-modality fallback), the dead `lane: LanePolicy | None = None` fields become the composed lane, and the "lane accepted yet never composed" form is deleted corpus-wide. The seven inline `_TRACER` span mints (`ingestion.md:74`, `registration.md:68`, `deviation.md:78`, `reconstruction.md:72`, plus the ifc trio `[V5]` collapses) route through the `[V5]` weave — no inline `start_as_current_span` beside the owned aspects. Hardcode folds onto policy: `ingestion.md:94`'s `"Z[0:30]"` crop default becomes a policy row; `deviation.md:69-75` and `registration.md:63-64` module-Final graduation/verticality ceilings fold onto their `Policy` carriers. `registration.md:3`'s stale `mesh-utility` sub-domain name corrects. The verification chain's ingress and precondition harden: `evaluate`'s reference GLB arrives BY CONTENT KEY over the `ARCH:55` (post-shift) `Rasm.Bim/Model` row — the daemon's welded artifact fetched keyed, never re-tessellated in scan — and the watertight precondition stops raising bare `ValueError` (`deviation.md:303`, exception-in-domain against the folder's own rails): it composes `mesh/quality`'s exact-closure fold (quality tiers below the scan producers per `[00]`) and rails a typed `BoundaryFault`. The offload obligation lands over the scan+mesh set and extends to the graph owners (`[V6]`).

### [V5_ONE_EVIDENCE_WEAVE]

`graduation.md` additionally owns the shared evidence-run weave — geometry's `faults`-analogue tier. One `evidence_run(scope, subject, dispatch)` fold composes runtime `boundary` + the span + `@receipted`; the triplicated six-element weave (`_TRACER`/`run`/`_ok`/`_emit`/`_REDACTION` at `analysis.md:126`, `costing.md:168`, `structural.md:78`) collapses onto it and the scan inline spans (`[V4]`) ride the same owner. The per-page tracer-scope literals become one seed table on the spine (the runtime `[V8]` scope-vocabulary law at geometry scale). The `ReceiptContributor` law resolves its three-way split — ruled default STRUCTURAL-ONLY: the Protocol is runtime-checkable, the scan trio already conforms without importing it, and a subclass adds nothing — the three capsule subclass sites (`daemon.md:166`, `quality.md:229`, `spatial.md:262`) drop the base, and the fourteen import sites keep only consumed names (`cad.md:53`-class annotation-only imports die).

### [V6_GRAPH_ANALYTICS_SUBSTRATE]

NEW `graph/analytic.md` — the tier-0 graph-analytics leaf: `AnalyticValue` (`@tagged_union` scalar/leaderboard/groups with `as_scalar`/`peak`, today verbatim at `features.md:166` AND `nonmanifold.md:112`), the `_leaders`/`_ranked` ranking folds, and the census-peak projection — one owner all three graph producers compose. The triple limiter dies: `algebra.md:100`, `features.md:141`, `nonmanifold.md:99` collapse onto `lane.offload` THREAD modality under the runtime-owned band (the runtime `[V5]` export) — zero geometry-minted `CapacityLimiter`s, and the three truncated comments die with them. `networkx` ownership is settled and LANDED: `graph/features` is the branch's analysis consumer of record, the catalog sits branch-tier with both consumers named (`libs/python/.api/networkx.md:12`), and `features.md:555`'s `[NETWORKX_ANALYTICS]` tier citation is already corrected on disk (the data campaign's ripple applied it) — `[V6]` verifies, never re-does. `nonmanifold` hardens: the leaked `</content>`/`</invoke>` XML (`:459-460`) strips, the empty `[04]` dies, and the module-top `topologicpy`/`ifcopenshell` imports go function-local per the page's own AGPL gated-import discipline.

### [V7_MANIFOLD_KERNEL_AND_QUALITY]

One manifold conversion, one closure fold, one name. `repair`'s `_to_manifold` promotes to the repair-owned PUBLIC kernel (repair is the chartered canonical `manifold3d` owner) and `spatial.md:186`/`quality.md:206` compose it — one uint32-ceiling `Mesh`/`Mesh64` law; `repair` sits tier-1 in the `[00]` order so both consumers point downward. The `MeshQuality` collision resolves by composition: `reconstruction.md:103`'s leaf value object and its `fold` (re-computing the watertight/euler/volume/area closure `quality.md:74-93` owns, with a second component-count strategy) DIE — `scan/reconstruction` composes `mesh/quality`'s exact-closure fold (quality tiers below the scan producers), `scan/deviation`'s `[V4]` watertight gate is the second consumer of the same fold, and the component-count strategy is the quality owner's backend row (manifold3d `decompose` when the package resolves, trimesh `body_count` the CORE fall-through), never a per-consumer choice. The two fence defects fix with the page: `quality.md:346` re-indents, `quality.md:365`'s stray fence deletes.

### [V8_IFC_TRUTH_AND_DEPTH]

The ifc plane's provider shapes are settled law: the clash-set surface reads `clash_set.get("clash_groups", {})` keyed by `cluster` index (the `[CLASH_SET_INTERNAL]` row, `analysis.md:371,390`), BCF topic titles read `TopicHandler.topic.title` (`analysis.md:342`), and the ifc4d schedule egress populates the `.file`/`.xml`/`.work_plan` slots (`costing.md:413`) — the fences bind exactly these slots, and a typed field outside them is a phantom. Depth: `ifctester`'s `Json(ids).report()` Results graph (`percent_checks_pass`/`total_applicable_fail`) lands as the sharper IDS verdict source beside the `Specification.passed_entities`/`failed_entities` per-spec ratio the page already folds (`analysis.md:392`). `structural.md:485`'s mermaid diagram — the only one corpus-wide — drops; the flow is table-stated and the corpus grammar carries no diagram tier. Subject differentiation rides `[V2]`; the weave rides `[V5]`.

### [V9_SPATIAL_BACKEND_ADMISSION]

The native band is ADMITTED and LANDED: `rtree` and `python-fcl` sit in `pyproject.toml:131-132` (`[GEOMETRY]` worker-lane rows; `python-fcl` on the `<3.15` band), their folder-tier `.api` stubs authored (103/96 LOC) and README rows registered (`README.md:63-64`) — the Bounds and CORE-clearance arms (`spatial.md:39-43,126-130`) stand on admitted packages and `[V9]`'s remaining work is COMPOSING them; `embreex` stays a probe-only transparent accel with no admission and no catalog; the probe band enters `ARCH:[03]-[COMPANION_LANES]` so the `find_spec` set is governed, not implicit. The plane's phantom substrate spelling dies in the same motion: `spatial.md:7,201-202` recite a `trimesh`/`numpy`/`scipy` spine while no geometry fence imports a scipy member and the only scipy catalog is compute-tier (`pyproject.toml:116` `[COMPUTE]`; `compute/.api/scipy.md`) — the prose corrects to the realized trimesh/numpy members, a genuine scipy consume entering only through a tier-lawful catalog motion naming both consumers. Both admissions stand feed-verified live, maintained, and license-clean: `rtree` 1.4.1 MIT with gap-free `py3-none` platform wheels including macOS arm64 (live-imports on 3.15), `python-fcl` 0.7.0.11 BSD-3-Clause with cp314 wheels on the `<3.15` band; the manifold3d `min_gap` clearance path stays the fall-through on the gapped band.

### [V10_PAGE_GRAMMAR]

The 18 `[03]-[RESEARCH]` tails are zero-provenance defects and delete, and geometry's second appendix layer deletes with them: eleven `[04]-[UPSTREAM]` tails, one `[04]-[CROSS_FOLDER]` label drift (`deviation.md:335`), five of them empty or dead (`brep.md:418,421` both empty, `spatial.md:330,333` both empty, `nonmanifold.md:457`, `costing.md:422`, `quality.md:363`) — cross-folder facts belong in the seam ledger, never per-page prose. The finished corpus simply carries no appendix tier, and NO page or governance surface states anything about that tier in either polarity — a "research appendices are banned/purged" sentence is the same meta-prose defect as the tail itself. The ruling is purge-and-fold: the genuinely load-bearing member confirmations (`selector.md:316+` grammar-parity proof, `analysis.md:388+`, `structural.md:503+`, `algebra.md:424+` compas arity corrections, `nonmanifold.md:454+` topologic handle resolutions, `daemon.md:222+` KEYED_ADMISSION/REPRESENTATION_KEY law) fold into the owning `Packages` block or the folder `.api` catalog; "confirmed against the live distribution" framing, version narration, and every empty header delete. The corruption fixes land here where a verdict has not already claimed them; the ~40-line run-on Boundary walls break into scannable deleted-form clauses, each invariant stated once. This is the folder's largest clean prose reduction (~700-900 lines) and lands with each owning page's rebuild.

### [V11_API_TIER_AND_GOVERNANCE_TRUTH]

Both `.api` tiers and all governance surfaces become exact. LANDED tier motions ( `[V11]` verifies, never re-does): the geometry-tier `queenbee`/`lbt-recipes`/`pollination-handlers` catalogs are struck (runtime-tier canonical), the codec-trio strikes are DONE (`geometry/.api/{rhino3dm,meshio,laspy}.md` absent from the 34-catalog tier; the data tier carries all three), the two `[V9]` stubs are authored, `data/.api/pyarrow.md:12`'s owner line already carries geometry's `arrow_las` boundary decode, and the ten energy catalogs carry consuming fences with named `- consumer:` charter lines (`[V1]`). Standing tier motion: `ifc4d.md:62`'s stacking recital of the compute-owned `"numerical-primitive"` subject rewires to the `[V2]` geometry-minted union (`bim-lifecycle`) with the charter rewrites. Governance — the README half is LANDED: the router routes all 22 pages (`[01]-[22]`; energy at `[19]-[22]`), the repair row already defers mesh-file codecs to the data `MeshPayload` seam (`README.md:18`), and the `[02]` preamble already states per-group admission owners (`README.md:32`) — the router's only growth is the three NEW pages (`mesh/serve`, `graduation`, `graph/analytic` -> 25 rows). The ARCH half is STANDING: the codemap gains `mesh/serve`, `graduation`, and `graph/analytic` beside the standing `energy/` nodes; `ARCH:80`'s companion-lane paragraph (post-shift) drops `rhino3dm`/`laspy`, records the `[V9]` probe band, and states the companion-band provisioning owner; the seam ledger declares all intra-folder edges and all eleven graduation edges as `->` rows (the `⇄` on `ARCH:59-60` dies — no compute->geometry import exists), kills the wildcard `ARCH:43` csharp-graduation row (the graduation crossing is `python:compute` and the `[V2]` carrier; C# consumption rides the compute hub downstream, never a second geometry-owned graduation wire), and re-files the wire rows onto serve beside the standing energy rows. The C# mirror of the wildcard kill is an AUTHORIZED 1-hop ledger repair landing with the same motion: the landed `Rasm.Compute/ARCHITECTURE.md:81` direct `Runtime ← python:geometry # [GRADUATION]` row merges into the `:143` `python:compute/graduation` hub row (the geometry-case annotation preserved on the hub row), so both ends carry one graduation wire. Realized-capability truth: the warping/plastic/shear tier (27 fence hits) and the `OPEN3D_FGR` fallback arm (12 hits) stand authored on disk — the governance surfaces record them, never re-plan them; the mesh codec shed (`ARCH:58`), the `MeshPayload` cell-block seam (`ARCH:57`), and the COPC boundary (`ARCH:60`) verify with their consuming pages' rebuilds; the topologicpy AGPL dual-gate on an in-memory IFC-bytes constructor and the gmsh sdist-build exclusion stand as named admission blockers on their roster owners, untouched by this campaign.

### [V12_UPSTREAM_REBIND]

The mechanical corpus-wide sweeps — standing target-state law, every site live on disk until its owning page rebuilds: (a) `rasm.runtime.content_identity` -> `rasm.runtime.identity` at all 12 sites — the runtime `[V4]` ruling this folder makes load-bearing; (b) the reproduction anchor migration — `daemon.md:228`'s `evidence/identity#SEED_REPRODUCTION` row retargets the landed `rasm.runtime.reproduction` `name="glb-by-key"` design-pin (`evidence/reproduction.md:128-130` — the on-disk spelling is the string literal, never an uppercase constant); (c) the drain-vocabulary citation truth — daemon's lanes-attributed `DrainReceipt` prose (`daemon.md:13-14`) re-cites `observability/receipts` per runtime `[V1]` (imports are return-typed, prose only); (d) the compute-import kill and graduation rewire (`[V2]`) at all 11 sites. The finished corpus carries zero `content_identity`, `rasm.compute`, or `evidence/identity#SEED_REPRODUCTION` spellings under `libs/python/geometry/.planning/`.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every row re-verifies against disk before it is consumed. Anchors are `libs/python/geometry/.planning/` relative; `ARCH:` anchors `libs/python/geometry/ARCHITECTURE.md`; `README:` the folder README; `pyproject.toml:` the repo root manifest.

| # | Class | Evidence |
|---|---|---|
| E1 | Energy plane (resolved) | Realized per `[V1]`: `energy/{climate,model,district,simulate}.md` on disk; README router rows `[19]-[22]`; ARCH `energy/` codemap nodes and seam rows; ten catalogs' consuming charter lines; residual tier/registry motions ride `[V11]` |
| E2 | Daemon strand | `tessellate` returns `self` `daemon.md:181`; GLB only in `self._cache` `:171,175`; `contribute`/`fact` carry `content_key.hex`+counts `:78-84,219`; no accessor in the method roster; `ComputeService`/`ArtifactSync` prose-only `daemon.md:3,20,222`, `cad.md:22`; wire rows `ARCH:44-49,63` (post-shift) fence-less |
| E3 | Compute forward-dep | 11 imports: `algebra.md:48`, `features.md:43`, `nonmanifold.md:41`, `analysis.md:35`, `costing.md:33`, `structural.md:40`, `brep.md:57`, `repair.md:35`, `deviation.md:37`, `reconstruction.md:41`, `registration.md:35`; unbuilt-page anchors `structural.md:513`, `deviation.md:337`, `registration.md:380`; compute is downstream and unbuilt |
| E4 | Identity drift | 12 `rasm.runtime.content_identity` imports (`algebra.md:49`, `features.md:44`, `nonmanifold.md:42`, `analysis.md:37`, `costing.md:35`, `structural.md:42`, `brep.md:58`, `cad.md:51`, `daemon.md:36`, `deviation.md:33`, `reconstruction.md:37`, `registration.md:31`); reproduction anchor `daemon.md:228`; stale drain citation `daemon.md:13-14` |
| E5 | Scan dead lane | sync entries `ingestion.md:147`, `registration.md:172`, `deviation.md:250`, `reconstruction.md:245`; dead `lane` fields `:145/:170/:248/:243`; zero `.offload(` fence calls under `scan/`; inline `_TRACER` mints `ingestion.md:74`, `registration.md:68`, `deviation.md:78`, `reconstruction.md:72` |
| E6 | Product corruption | `quality.md:346` column-0 dedent inside `_metrics` (`IndentationError`); `quality.md:365` stray fence; `nonmanifold.md:459-460` literal `</content>`/`</invoke>`; truncated comments `algebra.md:98-99`, `features.md:139-140`, `nonmanifold.md:98` |
| E7 | Duplicate mechanism | `CapacityLimiter(4)` x3 `algebra.md:100`/`features.md:141`/`nonmanifold.md:99`; `AnalyticValue` verbatim x2 `features.md:166`/`nonmanifold.md:112`; manifold build x3 `repair.md:198`/`spatial.md:186`/`quality.md:206`; span weave x3 `analysis.md:126`/`costing.md:168`/`structural.md:78`; closure fold x2 `reconstruction.md:103-123` vs `quality.md:74-93` |
| E8 | Subject overload | `"numerical-primitive"` carries four evidence classes: `algebra.md:336`, `analysis.md:121`, `costing.md:157`, `structural.md:74` |
| E9 | ReceiptContributor split | 14 import sites; subclass-as-base `daemon.md:166`, `quality.md:229`, `spatial.md:262`; structural-only (no import) `deviation`/`reconstruction`/`registration`; annotation-only imports `cad.md:53`, `repair.md:38`, `brep.md:61` |
| E10 | Ledger blindness | zero rows for the 4 intra-folder edges (`daemon.md:35`, `analysis.md:36`, `costing.md:34`, `structural.md:41`); 7 of 11 graduation seams undeclared (nonmanifold/costing/brep/repair/deviation/reconstruction/registration); `ARCH:59-60` (post-shift) mistyped `⇄`; `ARCH:80` claims `rhino3dm`/`laspy` on the runtime lane against zero imports |
| E11 | Policy coupling | `IdentityPolicy` carries mesher knobs: `daemon.md:15,92-95`, `cad.md:51,221,233,246-254`, `brep.md:58,228,247`; `ARCH:45` (post-shift) bakes "deflection/tolerance policy-seed" into the seam row; the runtime fence still carries the knobs (`evidence/identity.md:92-99`) against its own generic prose (`:13`) |
| E12 | `.api` tier state (resolved) | The codec-trio strikes are DONE (`rhino3dm.md`/`meshio.md`/`laspy.md` absent from `geometry/.api/`; data-tier catalogs stand on all three); `features.md:555`'s tier citation is corrected on disk; the recipe-trio duplicates are struck; the energy catalogs carry consumers; the `[V9]` stubs are authored — `[V11]` verifies the tier, zero strikes remain |
| E13 | Governance state (split) | README half RESOLVED on disk: the router routes all 22 pages (`[01]-[22]`, energy `[19]-[22]`), the repair row defers codecs to the data seam (`README:18`), the `[02]` preamble states per-group admission owners (`:32`). STANDING: realized-but-unrecorded capability — the warping/plastic/shear tier (27 hits, `ifc/structural.md`) and the `OPEN3D_FGR` fallback arm (12 hits, `scan/registration.md`) are authored while no governance surface records them; the offload obligation omits the graph owners; `registration.md:3` stale `mesh-utility` name; the ARCH ledger/codemap motions per `[V11]` |
| E14 | Hardcode + probe band | `ingestion.md:94` `"Z[0:30]"`; `deviation.md:69-75`/`registration.md:63-64` module-Final ceilings; per-page tracer literals (7 sites); `rtree`/`python-fcl` rows+stubs LANDED (`pyproject.toml:131-132`) with the ARCH `[COMPANION_LANES]` probe band still to declare; `embreex` probe-only |
| E15 | Manifest-owner drift | ten companion-band packages (`ifcopenshell`/`open3d`/`small-gicp`/`topologicpy` + `ifctester`/`ifcclash`/`ifc5d`/`ifc4d`/`ifcpatch`/`ifcdiff`) README-registered and `.api`-cataloged with NO `pyproject.toml` row — the `geometry-companion` group is struck from the manifest, provisioning re-homed to the companion lane — and the once-false README `[02]` one-manifest claim is CORRECTED on disk (the preamble now states the per-group admission owner: manifest worker rows, companion-lane band, `[RUNTIME]` recipe trio); `geopandas`/`pyogrio`/`pyproj`/`shapely` `[GEOMETRY]`-tagged (`pyproject.toml:53,81,82,119`) with zero geometry fences and data-tier catalogs on all four |

Sound surfaces (preserve; only the deltas a verdict names change them): `ifc/selector.md` (9 — grammar-parity demolition; the reference leaf), `ifc/authoring.md` (9 — the verb-table/AOP exemplar; correctly graduation-free), `ifc/structural.md` core (9 — Green's-theorem kernels, tier gating, `ClosedRing` refinement), `scan/deviation.md` domain content (9 — signed-band fold, RANSAC peel), `ifc/costing.md` core (9 — `_token[E: StrEnum]` rail, population-divided drift ledger), `mesh/cad.md` (8.5 — XCAF hop, `READERS`/`_APPLY`), `mesh/spatial.md` routing core (8.5 — capability-aware `_route`, lexsort reduction), the corpus-wide table discipline, the acyclic internal graph, the C# FIT (seed-zero reproduce-under-parity, decode-only seams), and the three data seams (Arrow point bridge, `MeshPayload` cell-block, codec shed).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| graduation spine | — | 9.5 | NEW: geometry-minted subject union (differentiated); `GeometryHandoff` frozen contract; evidence weave + scope table; ReceiptContributor law |
| mesh/daemon | 6 | 9.5 | results exposed; `TessellationPolicy` owned; anchors rebound; two-key law proven against the `"glb-by-key"` pin |
| mesh/serve | — | 9.5 | NEW: servicer in the runtime `ServerHost` lifecycle; `FrameEdge` framing; wire rows composed; consumer field floor decoded by symbol |
| mesh worker (repair/spatial/quality/brep) | 8 | 9.5 | public manifold kernel; closure fold composed; fence fixes; `rtree`/`python-fcl` admitted; structural-only receipts |
| mesh/cad | 8.5 | 9.5 | policy rewire; grammar purge; cold otherwise |
| scan | 6.5 | 9.5 | async offload composed; weave-routed spans; policy-folded ceilings; keyed reference ingress; railed watertight gate; grammar |
| graph | 7.5 | 9.5 | analytic substrate owner; THREAD-band offload; corruption stripped; gated AGPL imports; networkx cite corrected |
| ifc | 8.5 | 9.5 | weave composed; differentiated subjects; provider shapes settled; `Json(ids).report()` depth; mermaid dropped |
| energy | realized | 9.5 | landed at the ruled floor — polymorphic model admission (HBJSON decode + BIM-to-BEM derivation), recipe binding onto the runtime rail, validation-gated models, self-describing result frames; hold under the `[V1]` standing contract |
| governance (ARCH/README/`.api`) | drifted | exact | full router; truthful codemap+lanes; complete `->` ledger; admission-owner registry truth; tier strikes and stubs |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate, the folder tier for domain packages):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Option`/`Block`/`Map` as the one ADT and dispatch spine (already corpus-wide; regressions are defects), `anyio` structured concurrency ONLY through the runtime lane owner (zero geometry-minted limiters per `[V6]`), retry ONLY through `guarded` over the exported `OCCT`/`RPC`/`OCC_NATIVE` rows (never bare `stamina` — `analysis.md:133`'s `_CLASH_RETRY` re-routes through the runtime-pinned `OCC_NATIVE` in-process transient row; `structural.md:92`'s `_FE_RETRY` dies, the deterministic sectionproperties solve owns no transiency and a retry over it is a deleted form), `msgspec.Struct` admission at ingress, `beartype.vale` refinements on geometric preconditions (`ClosedRing`/`SignedField` are the exemplars), one span per fold through the `[V5]` weave — the span handles mint from the `[V5]` scope table over the branch-tier `opentelemetry-api` catalog (`libs/python/.api/opentelemetry-api.md`; today seven page-local `from opentelemetry import trace` preludes, collapsed onto the graduation owner). The README `[WIRE_CODEGEN]` substrate rows (`grpcio`/`grpcio-tools`/`protobuf`) are consumed ONLY through the runtime transport owners — `mesh/serve` registers descriptor rows on `ServerHost.dispatch` and geometry authors no proto, stub, or codegen surface.
- `ladybug-core` — EPW/Wea/Location intake, `AnalysisPeriod`, `Sunpath` solar geometry, `DataCollection` algebra, SQLite result reads: the `energy/climate` + `energy/simulate` substrate (`.api/ladybug-core.md`).
- `ladybug-comfort` — PMV/UTCI/adaptive/SolarCal models and comfort maps as `energy/climate` owner-block rows; `ladybug-geometry` — the planar/solid value objects the model construction composes, never a second geometry kernel; `Polyface3D.from_faces`/`from_offset_face` and the `Face3D` aperture/fin generators (`sub_faces_by_ratio*`, `contour_fins_*`) are the `[V1]` BIM-to-BEM derivation substrate (`.api/ladybug-comfort.md`, `.api/ladybug-geometry.md`).
- `honeybee-core`/`honeybee-energy` — the HBJSON object graph with `check_all` as the model ADMISSION gate; constructions/loads/schedules/HVAC as typed property rows; the catalog's own rail law (`honeybee-energy.md:126-135`) becomes the `energy/model`/`energy/simulate` fence shape it was authored for; `Room.from_polyface3d`/`solve_adjacency`/`stories_by_floor_height` back the `[V1]` derivation modality.
- `honeybee-standards`/`honeybee-energy-standards` — identifier-resolved seed data (defaults, ASHRAE 90.1/DOE vintage sets) through `honeybee-energy.lib`, never hand-copied constants.
- `honeybee-openstudio` — the in-process OSM/IDF translation, offloaded on the worker lane; `dragonfly-core`/`dragonfly-energy` — 2.5-D massing, `to_honeybee` explosion, URBANopt/DES/OpenDSS rows (`energy/district`).
- `queenbee`/`lbt-recipes`/`pollination-handlers` — BINDING ONLY: `Job`/`RecipeInterface` construction and handler input coercion in `energy/simulate`; execution is runtime `execution/recipe`'s (`[V1]`; the geometry-tier catalogs strike per `[V11]`).
- `ifcopenshell` — the spine holds at depth (seven consuming pages: `analysis`/`authoring`/`costing`/`selector`/`structural`/`daemon` plus `nonmanifold`'s gated arm); the daemon's in-memory ingress is decided — SPF source bytes through `file.from_string`, the modality the wire delivers per `[INGRESS_LEDGER]`; the sqlite/zipped variants DECLINE; the `[V1]` BIM-to-BEM modality adds the eighth consumer — `IfcSpace` solids through `by_type` + `geom.create_shape`.
- `ifctester` — `Json(ids).report()` `Results` graph (`.api/ifctester.md:62,79`) into the analysis verdict fold (`[V8]`); `ifcclash`/`ifc5d`/`ifc4d`/`ifcpatch`/`ifcdiff`/`bcf-client` — held at the landed bar (the `Clasher`/`ClashSet`/`smart_group_clashes` weave stacked with bcf-client issue authoring, `analysis.md:342-371,390-391`; the qto/diff/patch/schedule phases, `costing.md:13-16`); MINE: `Clasher.get_viewpoint_snapshot` (`.api/ifcclash.md:55`) lands as the BCF topic viewpoint source in the analysis clash weave; the `export_json`/`export_bcfxml` same-string round-trips stay deleted forms (the page no longer spells them — the prohibition stands regardless).
- `cadquery-ocp` — MINE the unmined band into `mesh/brep`: `Geom_*` NURBS curves/surfaces, `BRepBuilderAPI_Sewing`/`NurbsConvert`, `BOPAlgo` `SetFuzzyValue`/`History`/`SectionEdges`, and `BRepAlgoAPI_Splitter` (`.api/cadquery-ocp.md:61-71,84-86,227-228`) join `BrepOp` as dispatch rows, never a parallel entry.
- `trimesh` — the most-consumed owner holds; the unmined-by-design declines record once (`load`/`export` -> data, `Scene` -> artifacts, `creation` superseded by `BrepOp`, `registration`/`voxel`/`section` unused).
- `manifold3d` — `batch_boolean`/`Mesh64`/`CrossSection`/`min_gap` held; the SDF/Minkowski/hull surfaces DECLINE — no fence demands them and the clearance path rides `min_gap`.
- `open3d` — VERIFIED AT DEPTH across the scan plane: the tensor band (`multi_scale_icp`/Tukey `RobustKernel`/colored estimator, `.api/open3d.md:105-118`), the multiway `PoseGraph`/`global_optimization` fold re-scored through `evaluate_registration` (`:89-103`; `registration.md:12-14`), RANSAC `segment_plane` peel, DBSCAN cluster-split reconstruction, and the Poisson/ball-pivot/alpha `_CONSTRUCT` rows (`:63-86`; `reconstruction.md:16-18`) all consume by symbol; recorded DECLINES — `io.*` (`:134+`, codecs are data-owned), the legacy pairwise `registration_icp`/`registration_generalized_icp` dispatch (`:152`, superseded in-corpus by the tensor arms plus VGICP), and `compute_point_cloud_distance` (deviation rides trimesh `ProximityQuery`); `[V4]` moves execution off the loop, never re-shapes the kernels.
- `small-gicp`/`kiss-matcher` — held at the landed bar: `align(registration_type='VGICP')` with the `T_target_source`/`num_inliers`/`error`/`iterations` readback and the full `KISSMatcherConfig` gain constructor with the staged `match`/`prune_and_solve` decomposition (`registration.md:14,16`; `.api/small-gicp.md:44+`, `.api/kiss-matcher.md:58+`); the FGR fallback is realized law.
- `pdal` — the injected driver factories and `execute()` are consumed (`ingestion.md:16,233-238`); MINE: the streaming arms (`execute_streaming`/`iterator`, `.api/pdal.md:75-76,113`) own block-scale clouds, and the `metadata`/`schema`/`srswkt2` output rows (`:81+`) land as ingest receipt facts; the JSON-pickle offload law (`:79,115`) is the `[V4]` process-modality proof.
- `pye57` — the conditioned `read_scan(transform=True)` intake is realized (`ingestion.md:198-206`); MINE: `ScanHeader` pose/acquisition metadata and `scan_position` station origin (`.api/pye57.md:41-58,71+`) land as scan-provenance receipt facts; the write path (`:60-68`) DECLINES — scan egress is the data seam's; the COPC boundary stays the data codec seam (`ingestion.md:17`).
- `compas`/`compas-dr`/`compas-tna` — held (342-entry `__all__` verified phantom-free); the RPC bring-up rides the exported `RetryClass.RPC` row.
- `topologicpy` — the AGPL opt-in lane holds; imports go function-local (`[V6]`); the permissive replacement stays a deferred growth direction, admission-gated.
- `sectionproperties` — the warping/plastic/shear tier stands realized on disk; `[V11]` records it as governance truth.
- `networkx` — the branch catalog serves both consumers per the applied data-brief edit; features' eleven-family `ANALYTICS` table (`features.md:404-469`) is the geometry consume of record; `lark` — held (selector is the reference leaf).
- `rtree` + `python-fcl` — ADDITIONS per `[00]`/`[V9]`: named concerns fence-bound today; stubs authored with the admission; verified live, maintained, license-clean per `[00]`; `embreex` stays probe-only.
- `pyarrow` — ingress decode ONLY: the `arrow_las` arm binds `pyarrow.Table` at boundary scope over the `ARCH:56` point-record bridge (`ingestion.md:12,60-61`); the catalog stays data-tier (`data/.api/pyarrow.md`) and gains geometry on its owner line as this campaign's `[V11]` motion (the data brief rules the dual-consumer discipline via its networkx precedent, not the row); geometry never keys or egresses through it.
- `numpy` — the one direct substrate consume (structural kernels, quality folds, spatial reductions; twelve module-top sites) — held at the branch-tier bar (`libs/python/.api/numpy.md`).
- `geopandas`/`pyogrio`/`pyproj`/`shapely` — ZERO geometry consumes; data-owned with data-tier catalogs standing on all four; retag `[DATA]` per `[00]` — the dragonfly GeoJSON exchange composes at the data seam, never through a geometry import.
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): the daemon's keyed-drain cache short-circuit and warm-pool discipline, selector's grammar round-trip, authoring's `to_kwargs` railed slot-resolve, structural's centroid-relative `_extreme_fibers` and FE void carving, quality's half-edge incidence fold, spatial's `_nearest_hits` lexsort, deviation's `DeviationBand.fold`, features' `mode_guard` data-gating.
- Standing seam-verification obligations: the mesh codec shed (`ARCH:58`), the `MeshPayload` cell-block topology seam (`ARCH:57`), and the COPC boundary (`ARCH:60`) verify with their consuming pages' rebuilds; the offload law spans the scan+mesh+graph owners (`[V4]`/`[V6]`); the warping tier and the FGR fallback arm stand realized (`[V11]` records them); the topologicpy AGPL dual-gate on an in-memory IFC-bytes constructor and the gmsh sdist-build exclusion stand as named admission blockers, untouched by this campaign.

## [05]-[BUILD_ORDER]

The build order is the `[INTERNAL_DAG_LAW]` topology — foundations before consumers, every page importing only pages of the same or an earlier tier, and each tier's listed sequence the canonical landing order.

1. **SPINE** — NEW `graduation.md` FIRST (`[V2]` vocabulary + contract pin + `[V5]` weave — everything downstream composes it), NEW `graph/analytic.md`, `mesh/repair.md` (public manifold kernel; tier-1), `mesh/quality.md` (fence fixes; closure fold public), `mesh/cad.md` (`TessellationPolicy` MINT + rewire per `[V3]`), `ifc/selector.md` (cold; grammar purge). The `[V12]` spellings — the identity rename, the compute-import kill and graduation rewire, the reproduction/drain anchor migrations — land with each owning page's rebuild, spine pages first so every consumer inherits clean spellings.
2. **PRODUCERS** — `scan/{ingestion,registration,deviation,reconstruction}.md` (`[V4]`), `mesh/spatial.md` (`[V9]` — the landed `rtree`/`python-fcl` rows and stubs compose with this page), `mesh/brep.md` (`[V7]` compose; cadquery-ocp rows), `graph/{algebra,features,nonmanifold}.md` (`[V6]`), `ifc/{analysis,costing,structural,authoring}.md` (`[V5]` weave, `[V8]` settlements).
3. **WIRE** — `mesh/daemon.md` (`[V3]` results + policy + anchors), NEW `mesh/serve.md` LAST (the servicer composing the daemon; the `ARCH` wire rows re-file here).
4. **ENERGY + GOVERNANCE** — the `energy/climate.md` -> `energy/model.md` -> `energy/district.md` -> `energy/simulate.md` plane stands realized (`[V1]`); the governance closeout — `ARCHITECTURE.md` codemap+ledger+lanes (including the authorized C# graduation-ledger merge), `README.md` router growth to 25, the `[V11]` tier verifications, and the remaining `pyproject.toml` motions (the `rhino3dm` retag and the `geopandas`/`pyogrio`/`pyproj`/`shapely` retags — `laspy`/`meshio` are landed) — closes with the pages whose facts it records.

Co-motion law: a page rebuild repairs the cross-file ripples it exposes in the same motion; `README.md`/`ARCHITECTURE.md` close with the change that alters their facts; the `[V10]` grammar purge applies to every rebuilt page; `pyproject.toml` and `.api` stub motions land with the admission, removal, or retag they serve.

Campaign acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.geometry.*` import with zero back-edges, and no `from rasm.compute` import survives under the folder; (b) the deleted forms are extinct corpus-wide — `rasm.runtime.content_identity`, `evidence/identity#SEED_REPRODUCTION`, the `[03]-[RESEARCH]`/`[04]-[UPSTREAM]`/`[04]-[CROSS_FOLDER]` appendix tiers, geometry-minted `CapacityLimiter(`, bare `stamina` imports, and leaked tool XML; (c) the wire proof — `mesh/serve`'s fence streams `TessellationResult.glb` through a named symbol pair whose types agree, its servicer registration composes `ServerHost.dispatch` rows with request/response shapes imported from `rasm.runtime.shapes` (zero geometry-minted wire vocabulary), the seed-zero wire key is proven against the runtime `"glb-by-key"` reproduction pin (graduated from design-pin to graded sample), and the response floor fields (content key, tally, semantic header, artifact hash, replay phase) decode by symbol; (d) the scan proof — all four entries are `async def` composing `lane.offload`, with zero inline span opens corpus-wide, the deviation reference a keyed fetch, and the watertight precondition railed typed; (e) the energy proof holds on the realized plane — every `[ENERGY]` manifest row has a consuming fence, the recipe binding composes runtime `execution/recipe` by symbol, the `check_all` gate fronts every simulation, the model admission discriminates HBJSON bytes and IFC-derived rooms through one entry, and the result-frame columns are fence-pinned; (f) ledger, codemap, router, manifest, and registry agree on one 25-page set, one seam ownership, and one admission owner per package, every graduation edge a `->` row; (g) the graduation proof — `graduation.md`'s fence pins the frozen union and admission shape, every producer's `graduates()` returns the local carrier, and no page recites a compute interior anchor.

## [06]-[OUT_OF_SCOPE]

- The C# seam owners: `Rasm.Bim` (IFC semantic authority), `Rasm.Element/Graph` (`RepresentationContentHash` mint), `Rasm.Compute/Runtime` (`ComputeService`/`ArtifactSync` contract) — geometry decodes, reproduces under parity, and serves; it never re-mints. The ONE authorized C# write is the `[V11]` graduation-ledger merge (`Rasm.Compute/ARCHITECTURE.md:81` into the `:143` hub row). The `PY_WIRE_ALIGNMENT` canonical-writer campaign is upstream-owned; the count-prefix law is consumed transitively only.
- The runtime interior: the identity rename, the lanes `retry=`/THREAD-band implementation, the recipe owner, and the `"glb-by-key"` reproduction pin are LANDED — geometry consumes the landed surface. The ONE authorized 1-hop runtime edit is the `[V3]` `IdentityPolicy` mesher-knob strip (`evidence/identity.md:92-99`), bounded to the owner block and its `spec` fold, resolving that page's own prose/fence split-brain.
- The data interior: `MeshPayload` deepening and the laspy COPC rebind are data's landed campaign; the `networkx` owner-line motion is landed; geometry consumes the seams unchanged.
- The compute campaign: the multi-domain `HandoffAxis` hub and `GraduationReceipt` admission fold stay compute's to build — it inherits `graduation.md`'s frozen contract with geometry the named demanding consumer; `RASM-PY-COMPUTE-BRIEF.md` `[V1]` now rules that decode — the frozen 13-literal union inherited as wire data, zero `rasm.geometry` imports, an out-of-union subject railing typed — so a geometry union change ships a compute ripple, never a silent admit.
- The two standing admission blockers — the topologicpy AGPL dual-gate on an in-memory IFC-bytes constructor and the gmsh sdist-build exclusion — package-admission-gated, not this campaign.
- Deferred expansion directions — IFC authoring pipeline orchestration, point-cloud instance labelling, parametric layout, clash/QTO receipt productization, the permissive topologicpy replacement, the `[V1]`-gated daylight/microclimate modalities (the landed Compute CBDM companion pin, `Rasm.Compute/.planning/Analysis/daylight.md:174`, is the demanding consumer awaiting the `honeybee-radiance` admission), and the SPZ/SOG gaussian-splat scan DECODE the landed Compute wire pins on this folder (`Rasm.Compute/.planning/Runtime/wire.md:114,339` — the `GaussianSplatScan` scan-buffer frame is minted C#-side; the decode lands with a future `realitycapture` admission motion, its `xxhash` cp315 wheel riding the Forge scientific lane) — genuinely deferred; the graduation spine and serve owner this campaign lands are their prerequisites, not their execution.
