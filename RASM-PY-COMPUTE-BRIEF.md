# [RASM_PY_COMPUTE_BRIEF]

Authoritative campaign input for the ground-up restructure and rebuild of `libs/python/compute/.planning/`. `compute` is the FOURTH and TERMINAL folder in the Python campaign sequence (runtime -> data -> geometry -> compute): the upstream campaigns are LANDED — the runtime, data, and geometry `.planning` corpora carry the identity rename, the offload axis, the reproduction corpus, the frozen `GeometryHandoff` contract, and the `xarray`/`dask`/`sparse` tier motions as finalized on-disk law compute rewires against, never re-plans (the consumed upstream briefs live under `.archive/`). The landed upstream exports this brief consumes at the seam: the offload isolation-modality axis with compute bound to the PROCESS half of the worker-death band (`runtime/.planning/reliability/resilience.md:78,:280-282` — `RetryClass.OCCT` targeting `anyio.BrokenWorkerInterpreter`/`anyio.BrokenWorkerProcess`, artifacts and compute the process half, geometry the interpreter half); `CANONICAL_POLICY` as the exported identity default with the key-equality law and the buffer/stream `IdentitySource.lift` modalities (`runtime/.planning/evidence/identity.md:103,:114,:209,:220`); BOTH compute fixtures admitted to the reproduction corpus beside `material-layer-golden`/`glb-by-key` as design-pins — the `array-layout` cross-backend bit-identity parity and the C#-minted `evidence-bundle` golden backing codegen's decode+emit drift gate (`runtime/.planning/evidence/reproduction.md:146,:149`); and the `universal-pathlib` compute `UPath` consumer named on the branch stub owner line (`libs/python/.api/universal-pathlib.md:12`; `experiments/model.md:36` the consuming page). The campaign executes only against that frozen upstream corpus. The page-set delta is fully ruled in `[01]`: zero new pages, zero page deletions, one vocabulary re-homing, one hub rebuild in place. The campaign closes on the `[05]` acceptance proofs. `docs/stacks/python/` governs.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level exceptional — 26 pages at 8.5-9.5 craft, the strongest table/ADT discipline of the four py folders — and hub-level inverted, seam-level illusory, and governance-level blind. Four proofs, each independently fatal to a world-class claim:

1. **The graduation hub still authors a vocabulary geometry owns.** Geometry owns the mint on disk — `geometry/.planning/graduation.md:43-58` mints the frozen 13-member `GeometrySubject` StrEnum (the 8 standing + `bim-compliance`/`bim-lifecycle`/`section-property`/`building-energy`/`thermal-comfort`), exported as the `SUBJECTS` tuple (`:87`) — and the geometry campaign's ripple repair already extended compute's copy to the 13 literals byte-identical and re-framed the `handoff.md:21`/`:206` prose decode-side. The REMAINING defect is structural: `handoff.md:63` still declares `type GeometrySubject = Literal[...]` — a compute-authored second source of truth where the inherited contract block belongs — and the hub still admits only a pre-destructured tuple (no carrier-decode fence exists), so a geometry union change still requires a hand-synchronized compute edit instead of arriving as wire data. Geometry PRODUCES `GeometryHandoff` carriers (`graduation.md:93`); compute DECODES them, re-shaping nothing without a geometry ripple. The seam ledger already carries the ONE decode row (`ARCH:51` `graduation ← python:geometry`) with the old interior-plane rows and the reverse projection gone — the hub page catches up to the ledger and the row gains its `python:geometry/graduation` counterpart.
2. **The graduation rail is dead on five of eight axes and wired three incompatible ways.** Only `inference`/`model`/`quantity` import handoff and define `graduates()` (verified: `rg 'def graduates\('` + import census); `convex.md:19-20` lists `graduation/handoff.md#GRADUATION` in its Packages block and claims the `convex_program` crossing while its prelude imports nothing from handoff; `spatial.md:14` claims a geometry-case graduation with no import; `array_layout` has no producing fence anywhere in the corpus; `solver` has no producing fence though `handoff.md:193-199` recites receipt/design/program/convex/symbolic/array/spatial as feeders. A tagged-union axis whose cases have no producers is dead vocabulary wearing a rail.
3. **The folder is unimportable the moment its upstream lands, and its identity-policy rail self-contradicts.** All 20 pages spell `rasm.runtime.content_identity` (21 sites — the module spelling the landed runtime retired; disk is `rasm.runtime.identity`, `identity.md:3`, with a second spelling banned at `:24`), including `codegen.md:188`'s `_BARE` frozenset DATA row that ships the stale spelling inside every generated C# stub preamble. Two camps call `ContentIdentity.of` incompatibly: ten pages rest on the `CANONICAL_POLICY` default and name a fresh `IdentityPolicy()` "rejected by the runtime content owner" (`study.md:496`, `statistics.md:264`, `array.md:23`, `quantity.md:482`), while six pass the explicit allocation (`symbolic.md:766`, `history.md:289`, `design.md:507`, `program.md:257`, `field.md:343`, `mesh.md:281`). The runtime owner settles the split: `CANONICAL_POLICY` is the exported default (`identity.md:103`, defaulted at `:209,:220`) and `IdentityPolicy` is a frozen single-field value (`:92-96`), so `IdentityPolicy() == CANONICAL_POLICY` by value equality — `history.md:289` and `study.md:497` provably key identically and the resume cache hits by construction, while `study.md:496`'s "rejected by the runtime content owner" claim is false prose against the owner. The `[V7]` rewire is the one-spelling convention law plus that correction.
4. **The concurrency story is illusory and one solver cycle hides under a guard.** Eight pages claim "worker lane" execution; `offload(` occurs ZERO times corpus-wide — every JAX x64 solve, scipy factorization, PyMC sample, cvxpy conic solve, and ONNX session runs synchronously on the event loop, and the JAX carriers pin `jax.config.update("jax_enable_x64", True)`, a process-global flag concurrent in-process solves corrupt (the dead-lane defect the geometry campaign killed, at compute scale). Beside it, `solvers/quadrature.md:56-57` imports mesh's `AssembledSystem` under `TYPE_CHECKING` while `mesh.md:48` imports quadrature's `ElementKind`/`FemForm` at runtime — a guarded cycle-dodge (the anti-pattern the data campaign killed) rooted in one FEM element vocabulary fractured across two files.

What IS sound: the table-driven dispatch discipline corpus-wide (`_STATUS`/`_QUAD`/`_INTERP`/`_SOLVER`/`_LEVY`/`_JIT_ROUTES`/`_STAT_ROUTES`/`_CONE_KKT`/`SALIB_ROUTES`/`FOURIER_ROUTES`/`Umath` — the next case is one row everywhere), the runtime FIT (`boundary`/`railed`/`traversed`/`Receipt.of`/`@receipted` consumed by symbol, every content key through `ContentIdentity.of` over canonical contiguous buffers, no re-mint, no canonical-writer mirror — the count-prefix law consumed transitively), the internal import graph acyclic modulo the one dodge, exemplary collapses (sensitivity's `DiffSpec`, quadrature's single-row catalogs, convex's per-cone KKT, statistics' two-level routes, codegen's three-interpreter catamorphism), and two deliberate boundaries held clean (statistics graduation-free by charter, sensitivity/study DGSM disjoint). The rot is hub direction, dead axes, unstated graph, and governance blindness — no per-page fix reaches it. Hub and graph first.

### [TELOS]

`compute` is the terminal evidence plane of the Python branch: array admission, one route-discriminated solver surface, differentiation, validated numerics, unit-bearing uncertainty, signal/symbolic/spatial analysis, design-of-experiments, Bayesian inference, model assets, and the ONE graduation hub through which every Python-branch result — compute-own and geometry-minted alike — crosses to the C# `Rasm.Compute` managed owner. World-class here means the COMPOSED hub: evidence is produced under receipts, admitted against ceilings, keyed over canonical bytes through one policy convention, decoded from the geometry carrier without re-shaping, and emitted on the unified wire with stub codegen closing the C# loop — every axis case backed by a live producer, every heavy solve off the event loop, every claimed seam a fence. Assume 5x the consumer demand on every surface: the C# graduation gate reads `SolverReceipt` verdicts, `Rasm.Compute` decodes `EvidenceBundle` stubs, geometry ships thirteen subject literals the hub must admit, data frames arrive as study inputs, artifacts renders study/posterior evidence — zero current consumers never lowers the bar. The output bar is the composed graduation chain closed in BOTH directions: outward, a study runs offloaded and content-keyed, resumes from history under provably-equal keys, graduates through the hub beside decoded geometry evidence, and crosses as the fence-pinned self-describing receipt floor the C# graduation gate decodes; inward, the C#-minted `EvidenceBundle` decodes through codegen into importable Python stubs downstream compute types against — every step railed, traced, receipted, no hand-off resting on unstated glue.

### [STRUCTURAL_AUTHORITY]

The campaign owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — the `handoff.md` hub rebuild in place, the FEM element-axis re-homing onto `solvers/mesh.md`, the evidence-weave and scope-table owner sited on `graduation/handoff.md` — are the floor, not the ceiling; a NEW `solvers/element.md` leaf carries the burden of proving mesh's ownership breaks, and no other new page is demanded. Every structural change updates `ARCHITECTURE.md` `[01]`+`[02]`, the `README.md` router and package groups, and BOTH `.api` tiers in the same motion. No page survives on incumbency, and no catalog survives on authorship: three sampler catalogs for string-only backends are pressure to reduce, never evidence of capability.

### [IMPORT_DAG_LAW]

The realized package is `rasm.compute.<sub>.<leaf>`; the corrected codemap names the 26-module set and the import graph honors one topological order — a fence import binds only a strictly-earlier module, braced peers are mutually import-free, and an edge into a later tier is a defect resolved by re-homing or re-tiering, never dodged with `TYPE_CHECKING` guards or function-local imports whose purpose is cycle evasion:

```text
graduation/handoff < {solvers/receipt, graduation/codegen} < numerics/array
    < {solvers/linear, solvers/mesh, numerics/{interval, jit, quantity, statistics}, analysis/transform}
    < {solvers/{quadrature, nonlinear, differential, sensitivity}, analysis/{signal, spatial, symbolic},
       optimization/{convex, design}, experiments/{study, inference, model}}
    < {solvers/field, optimization/program, experiments/history}
```

The chain is the canonical topological ORDER, not an edge list. `handoff` is tier-0 — the hub plus the `[V6]` evidence weave every producer composes, importing only runtime; `receipt` imports handoff (the `solver`-axis graduation projection, `[V2]`); `codegen` imports handoff ONLY (the offline-wire decode plus the `[V6]` weave) and braces with receipt — its former tier-4 siting licensed imports its fences never bind and its tier-0 brace forbids; `array` imports handoff (the `array_layout` crossing). One re-homing makes the solver plane hold: the FEM element vocabulary (`ElementKind`/`FemForm`, today `quadrature`-declared) moves to `solvers/mesh` beside `_CTOR`/`AssembledSystem` — mesh constructs elements and owns assembly, so `quadrature` imports mesh downward for its FEM condense-solve arm, `mesh.md:48`'s back-import dies, `quadrature.md:56-57`'s `TYPE_CHECKING` dodge dies, and `field` reads the whole element axis from mesh. The standing legal edges stay (`history -> study`, `program -> design`, `signal -> transform`, `field -> {mesh, receipt}`, `quadrature -> linear` for `_sparse_receipt` post-`[V8]` promotion) — `field -> quadrature` DIES with the `[V3]` rewire: `ElementKind` (`field.md:40`) is its only typed cargo and the solved DOF vector arrives as a value. The `[V9]` bridge edges point strictly downward: `symbolic -> jit`, `study -> jit`, `quadrature -> jit` — the lowered-spec vocabulary is jit-minted, so a symbolic-derived spec crosses into a consumer as a value, never a symbolic import. Every rebuilt page proves its prelude against this order; the import graph re-derived from every fence carries zero back-edges, and a straggler back-edge is a defect resolved at the offending page.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it. The live violation class is duplicate MECHANISM and per-page literals, not enumerated instances: the `RESULTS._name_to_item` enum-verdict inversion + batched `jnp.max` code-reduce is hand-spelled four times (`linear.md:312`, `nonlinear.md:221`, `differential.md:259`, `design.md:308` — 21 occurrences, all over `equinox.Enumeration` subclasses, the pages' own prose cross-acknowledging the sameness); eight pages mint a page-local `_TRACER = trace.get_tracer("compute.<x>")` with inconsistent spelling (`interval.md:402` says `compute.rigor`, not `compute.interval`) and no scope vocabulary exists; `_REDACTION: Final[Redaction] = Redaction(classified=Map.empty())` is declared verbatim on eight pages; the `_SLOTS`-driven receipt fold (`zip(_SLOTS[tag], payload, strict=True)` projecting `.facts`/`.status`) replicates near-verbatim across `SolverReceipt`/`MeshReceipt`/`FieldReceipt`. The test: the next solver route, evidence owner, or scope is one row or one composition — never a re-spelling.

### [SEAM_AND_HUB_LAW]

- ONE graduation direction: geometry MINTS `GeometrySubject` and the `GeometryHandoff` carrier (`geometry/.planning/graduation.md:43-58,:93` — frozen, compute the named demanding consumer at `:16`); compute's `HandoffAxis.geometry` case DECODES the carrier at the seam — the frozen geometry-owned `GeometrySubject` StrEnum whose values are the wire literals, the `graduates(owner, HandoffAxis(geometry=subject), key, measured, ceiling)` admission shape, the residual-over-ceiling `_admit` direction — as wire DATA with zero `rasm.geometry` imports and zero re-declared vocabulary. Compute-own evidence (solver/symbolic/model_asset/array_layout/unit_law/uncertainty_law/convex_program) EGRESSES outward to C# through the same hub; every live axis case has exactly one self-wired producer pattern (`[V2]`), and a case with no producer dies rather than standing as prose.
- ONE identity convention: every content key rides `rasm.runtime.identity` `ContentIdentity.of` over canonical contiguous bytes under the `CANONICAL_POLICY` default (the runtime identity owner's exported default, `identity.md:103`) — an explicit `IdentityPolicy()` allocation is a deleted form; buffer/stream modalities ride `IdentitySource.lift`; the count-prefix canonical-bytes law (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` item 1) is consumed transitively through the runtime writer — compute authors no canonical-writer mirror, and `PY_WIRE_ALIGNMENT` is its own campaign with compute downstream.
- ONE offline wire ruling: `EvidenceBundle`/`OwnerDescriptor`/`FieldNode` (`codegen` decode, `ARCH:49`) is the C#-minted OFFLINE graduation-evidence wire — msgspec json/msgpack bytes at rest, never the UDS gRPC leg — so it stays compute-owned and enters NO runtime `transport/shapes` registry row; the drift gate is the carried `schema_version` plus a C#-minted golden bundle admitted to the runtime reproduction corpus as a `CorpusFixture` (the applied `[V7]` waterfall) — the fixture pairs the bundle bytes with the expected `GeneratedModule` projection so the gate proves decode AND emit round-trip byte-stability under the deterministic encoder, and a `schema_version` the decoder does not carry rails as a typed fault band, never a best-effort decode. The registry arm is closed law: the shape enters a runtime `shapes` row exactly when the crossing moves onto the gRPC channel, and carries no row until then.
- The `ARCHITECTURE.md` seam ledger is enforceable law: every intra-compute import appears as a ledger edge (the ledger is currently blind to all ~24), every cross-`libs/` row names the real owner and direction — the wildcard `ARCH:48` `* ⇄ csharp:Rasm.Compute` narrows to `→`, `ARCH:52` re-homes `ContentIdentity` off `runtime/transport` to `← python:runtime/evidence`, and the standing geometry decode row (`ARCH:51`) gains its `python:geometry/graduation` counterpart — and every declared row has a composing fence (the two declared data-seam rows stay declared-unwired until `[V10]` lands them).

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as minted data and is only decoded here — the geometry-minted `GeometryHandoff` carrier (subject, evidence key, measured facts, ceilings — the frozen geometry contract, `graduation.md:93,:109`, crossing as content-keyed receipt data, never an import), the C#-minted `EvidenceBundle` offline wire (`codegen` decode under the schema-version + golden-fixture gate), data-gated DOE frames and labelled arrays on the study/array admission arms (`[V10]`), the `xarray.DataTree` posterior carrier (`inference.md:45`), and the prior `StudyReceipt` cohort + response cache `history` resumes over (`history.md:9` — a `Map[ContentKey, np.ndarray]` in-memory rail): compute owns NO durable run store — cross-session durability is the C# Persistence reuse ledger's — so the resume proof is key equality over caller-supplied evidence, never storage. What is COMPUTED here under a cross-language parity obligation, never a re-mint — every `ContentKey` over canonical bytes through runtime `ContentIdentity` under `CANONICAL_POLICY`, the `ARRAY_LAYOUT` cross-backend bit-identity proven against the runtime reproduction corpus, and the emitted stub preambles whose rendered names and imports derive from one `_BARE` data row. Solve receipts, study designs, posterior summaries, KKT certificates, and graduation receipts are companion-local evidence, minted freely.

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. The roster is reconciled — the manifest states this truth and a divergence is a defect: `sparse` and `jaxtyping` carry `[COMPUTE]`, both under the `python_version<'3.15'` marker of the JAX-stack band; `sympy`, `pint`, and `uncertainties` carry `[COMPUTE]`; `meshio` names data and compute only — geometry never enters the tag; `netcdf4` is `[DATA]` with zero compute claim; `rustworkx` is `[DATA_ARTIFACTS]` (zero compute fences, no compute-tier stub; the data `graph.md` and artifacts `drawing/detail.md:53-56` owners are the live consumers), re-opened for compute only by a live spatial/mesh graph-route fence, never by preference; `dask` is a branch-tier dual-consumer catalog — `libs/python/.api/dask.md` with data and compute on the owner line, no compute-tier file — and the manifest row mirrors the dual-consumer set; `xarray` carries `[DATA_COMPUTE]` on its manifest row mirroring its dual-consumer branch owner line; `universal-pathlib` and `narwhals` name compute in their consumer sets (`experiments/model.md:36` `UPath`; the `[V10]` DOE-frame lens landing compute's first `narwhals` import). `jaxtyping` is a genuine explicit wheel (MIT, pure-Python, actively released): shape/dtype-annotated array contracts (`Float[Array, "..."]`, `PyTree`) on the JAX-gated carriers and process-lane spec payloads, checked through the existing `beartype` fence (`jaxtyped(typechecker=beartype)`), the `[V4]` integration; its compute-tier catalog lands with its first fence (`[V14]`). The `numpyro`/`blackjax`/`nutpie` admissions STAND — PyMC's `nuts_sampler` dispatch resolves them at run time, so installed-never-imported is the admission's nature, recorded on each contract stub (`[V14]`); `blackjax` survives only as that string backend — the kernel/SMC/SGMCMC/VI surfaces stay closed, never re-litigated. `findiff` STANDS narrowed — `coefficients` arbitrary-accuracy stencils are capability `numpy.gradient` lacks, and the catalog carries exactly that floor. The categorical-best supersession sweep returns no strikes and no holes: second-order error propagation (`mcerp`/`soerp`) is a sealed decline — the capability is already owned by the `uncertainties` first-order owner plus the study-sampler Monte-Carlo bridge, and a second propagation surface is roster spam; the sparse-eigen demand is rows on the EXISTING `scipy` catalog, never a package; the graph-algorithm axis has no compute consumer to serve. `scipy-stubs` rides the `scipy` row as its typing companion — no separate catalog, no tag motion. Paid/gated/proprietary stays rejected.

## [01]-[PHASE_REBUILD]

Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; a floor may be exceeded, never diluted.

### [V1_GRADUATION_HUB_INVERSION]

`graduation/handoff.md` flips from vocabulary-owner to the multi-domain HUB — the campaign's defining ruling and every other verdict subordinates to it. Ruled: (a) the compute-authored `GeometrySubject` mint (`handoff.md:63` — already 13-literal byte-identical after the geometry ripple repair) DIES as a declaration form — the `geometry` case carries the geometry-minted subject as decoded wire data, typed against the frozen geometry-owned `GeometrySubject` StrEnum `rasm.geometry.graduation` pins (`graduation.md:43-58` — the 13 subjects whose values are the wire literals, exported as `SUBJECTS` `:87`), stated in the fence as the inherited contract block, never a compute-authored `Literal` source of truth and never a `rasm.geometry` import; a geometry union change is a geometry ripple, not a compute edit. (b) The producer framing is already decode-side on disk (the `handoff.md:21`/`:206` prose corrected by the geometry ripple repair) — the motion here is the RAIL: geometry owners PRODUCE `GeometryHandoff` carriers, the hub admits them through the same `boundary(_admit).bind(_clear).bind(boundary(_emit))` rail its compute-own axes ride, and re-emits on the unified C# wire. (c) The ledger finishes what disk already started: the geometry crossing is ALREADY one decode row (`ARCH:51` `graduation ← python:geometry`) with the old interior-plane rows and the reverse projection gone — the campaign refines the counterpart to `python:geometry/graduation` (the carrier-mint owner, so a geometry union change reads as that owner's ripple); the wildcard `ARCH:48` `* ⇄ csharp:Rasm.Compute` narrows to `→` (compute graduates outward; C# never imports back); the `←` offline-wire decode and `→` verdict projection rows stand. (d) `analysis/spatial`'s geometry-case emit DIES with the inversion: `spatial.md:14`'s "graduates outward on the geometry case aligned to the scan companion" claim is struck (the ledger's reverse-projection row is already gone) — geometry's own reconstruction plane mints `reconstructed-mesh` and rules the direction closed (`graduation.md:20` — a second graduation direction does not exist), so a compute-emitted geometry subject would re-mint the vocabulary this verdict just decoded; the alpha-shape `Boundary` evidence stays a compute-native receipt product (`SpatialEvidence.facts()` unchanged), and re-graduation requires a named consumer plus a compute-owned axis case, never the geometry case. (e) Both crossing halves are FENCES, not framing: the geometry case gains the ONE carrier-decode ingress (the standing entry admits only a pre-destructured tuple, `handoff.md:104-107` — no carrier-decode fence exists) — a fenced msgspec one-shot admission of the `GeometryHandoff` payload (bytes or builtins — channel-agnostic by construction, the landed data one-fence admission precedent: in-process composition and a future wire leg admit through the one fence, no carrier-transport ruling owed) against the inherited contract block, an out-of-union subject railing as `Error(BoundaryFault)` on the `("graduation.geometry", "unknown-subject")` band (never an unfenced `ValidationError`, never a silent admit — the drift signal a geometry union extension trips until the compute ripple lands), the decoded `(subject, key, measured, ceiling)` feeding the same two-stage rail; and the outward crossing pins its contract — the `planned` receipt's fact floor (`axis` tag, resolved subject, `evidence_key` hex render, the cleared residual ledger, phase) is fence-pinned SELF-DESCRIBING as the C# graduation gate's decode vocabulary, the data assessment-wire discipline at graduation scale: a free-form facts map the gate can neither attribute nor dedupe is a chain break, never a doc gap. The two-stage admission rail, finiteness refinements, and residual-over-ceiling `_clear` fold are the preserved core — this is a re-siting of AUTHORITY, not of machinery.

### [V2_GRADUATION_WIRING_UNIFICATION]

One producer pattern: every live `HandoffAxis` case has a self-wired `graduates()` producer importing the hub downward — the pattern `inference`/`model`/`quantity` already hold — and a case with no producer dies. Ruled per case: `solver` — `solvers/receipt` hosts the one solver-axis graduation projection (a `graduates`-composing fold beside the `_STATUS`/`_SLOTS` tables it already owns), and the solve owners (`linear`/`nonlinear`/`differential`/`quadrature` via receipt; `design`/`program` via their `OutcomeReceipt`; `interval` via its `Certificate`) feed it rather than each inlining an admission — feeding is the owner CALLING the projection with the `(ledger, ceiling, key)` triple projected from its own receipt, importing receipt downward: the projection is parameterized over that triple and receipt imports neither `OutcomeReceipt` nor `Certificate` nor any downstream type; `convex_program` — `convex` self-wires the `graduates()` its Packages block already claims (`convex.md:19-20` names the hub with no prelude import — the prose-vs-fence split closes fence-side); `symbolic` — `symbolic` self-wires under its own stability law (a derivation graduates only when `Precision.CERTIFIED` or reproducibly `HEURISTIC` — the page's stated bar becomes the admission ceiling); `array_layout` — `array` self-wires over the cross-backend bit-identity proof riding the runtime reproduction `ParityReceipt` (`[V12]`; the standing array-layout graduation obligation realizes here); `geometry` — decode-only per `[V1]`; `model_asset`/`unit_law`/`uncertainty_law` hold as landed. `statistics` stays deliberately graduation-free (its own `:16` boundary) and `sensitivity` stays disjoint from study DGSM — both are preserved law, never gaps. Ceiling law rides the pattern: every graduating family's DEFAULT ceiling is a governed policy row on that family's own policy carrier beside its route table (the `[V4]` modality-row siting; the hub's caller-supplied tighter row stays the override), so the admission bar is seed data with one owner per family — an ad-hoc ceiling literal at a `graduates()` call site is a deleted form. The axis roster extends by sibling campaign, never by silent admit — and the artifacts-origin `artifact` case is LANDED on the hub: the artifacts campaign shipped the case + `_subject` arm and its self-wired `core/receipt` producer in one joint motion (the geometry-ripple discipline at axis scale), so the roster closes at the landed nine-case set; its producer is artifacts-side (`artifacts/.planning/core/receipt.md` `graduates`), never a compute-side producer obligation, and a producerless case stays the deleted form.

### [V3_ELEMENT_AXIS_AND_SOLVER_GRAPH]

The FEM element vocabulary unifies on `solvers/mesh` per `[IMPORT_DAG_LAW]`: `ElementKind`/`FemForm` move from `quadrature` to mesh beside `_CTOR`/`AssembledSystem` (one element axis, one owner — mesh constructs elements, assembles systems, and owns the meshio interchange); `quadrature`'s FEM condense-solve arm imports mesh downward at module top and its `TYPE_CHECKING` guard (`quadrature.md:56-57`) dies; `mesh.md:48`'s quadrature import dies; `field` reads `ElementKind` from mesh (`field.md:39-41` rewires). Deleted forms: any `TYPE_CHECKING` import of a runtime-constructed symbol, any function-local import whose purpose is cycle evasion, any second element vocabulary. Mesh owns the axis outright: the rebuilt page carries it with room to spare (377 LOC today; the axis adds ~20 declaration lines and the dodge deletion returns more), so a standalone `solvers/element.md` leaf is a forbidden split.

### [V4_WORKER_LANE_REALITY]

The eight "worker lane" prose claims become composed fences or die — ruled COMPOSE: the heavy entries (`linear`/`nonlinear`/`differential`/`quadrature` solves, `design`/`program`/`convex` optimizations, `study` evaluation sweeps, `inference` sampling, `model` sessions, the scan-scale `spatial` queries) become `async def` composing `lane.offload(...)` on the runtime-exported isolation-modality axis (the landed `Modality` lane owner, `runtime/.planning/execution/lanes.md:66-69,:163`): JAX-gated carriers pin the PROCESS modality — `jax.config.update("jax_enable_x64", True)` is process-global, so concurrent in-process solves corrupt the flag and the isolation IS the correctness fix, not a tuning knob; the interpreter modality is STRUCK for the numeric band — the x64 flag is process-global native state no same-process interpreter isolation can fence (`differential.md:237`/`field.md:209` are the standing gates), so a JAX family row naming `interpreter` is a deleted form — while scipy factorizations, PyMC sampling, cvxpy conic solves, and ONNX sessions ride the THREAD/process band under the runtime-owned bounds; compute mints zero `CapacityLimiter`s. Modality is a policy ROW per route family, homed on that family's own policy carrier beside its route table — never a per-page literal and never a compute-central lane registry: the runtime owns the axis and its bounds, compute owns only the per-family modality choice. The crossing law rides the modality: a process-lane payload is spec DATA plus a module-level kernel — the declarative intent/policy `Struct`s and array operands cross, the worker resolves the kernel by import, and the module-level x64 gate applies at worker import (`differential.md:237`, `field.md:209` are the standing gates); the crossing operands and the gated carriers' array contracts go shape-typed in the same motion — `jaxtyping` `Float[Array, "..."]`/`PyTree` annotations checked through the existing `beartype(conf=FAULT_CONF)` fence (`jaxtyped(typechecker=beartype)`, the `[00]` admission), so a rank/dtype breach rails at the boundary beside the finiteness refinement rather than surfacing as a mid-solve XLA shape error, and a bare `object`/untyped-`Array` operand annotation on a JAX-gated public entry is a deleted form — and a closure-bearing carrier offloads on the process lane only when its callables are module-qualified importables: `study.md:62`'s `Objective` wraps caller callables, so the study entry resolves its lane by payload shape (module-level objective -> process, closure -> THREAD band) and a lambda objective on the process lane is a deleted form, never a runtime pickle crash. Retry law rides the same motion: compute's solves are deterministic, so a `guarded`/`stamina` wrap over them is a deleted form (the geometry `_FE_RETRY` precedent) and compute adds NO `POLICY` row; the only admissible retry surface is a genuinely flaky external oracle, and none currently exists in the folder. Worker DEATH is not a solve fault: a process-lane broker crash rides `offload(retry=RetryClass.OCCT)` — the runtime row's landed target spans the worker-death band (`anyio.BrokenWorkerProcess`, `resilience.md:282`), and the retry wraps the isolation leg, never the deterministic kernel, so the no-solve-retry law holds.

### [V5_DISPATCH_RAIL_UNIFICATION]

`expression.Map` (`Map.of_seq`) is THE dispatch-table rail — the doctrine's own immutable-lookup owner (`docs/stacks/python/rails-and-effects.md:17`) and the rail the landed runtime and data corpora already ride. The three-way bifurcation dies: every `beartype.FrozenDict` table (117 occurrences, 53 of them type-position declarations, across 15 solver/numerics/optimization pages — `receipt.md:28,64-71`, convex's tables, field's) re-types `Final[Map[...]]`, codegen's plain-`dict` `_SCALAR`/`_DECODER` tables join the same rail, and the analysis/experiments `Map` camp becomes the folder convention it already models. `FrozenDict` is real but off-label (a beartype `hint_overrides` helper, not a table substrate), and every page already imports `Map` through `Redaction` — the split is gratuitous. Acceptance: `rg 'FrozenDict' libs/python/compute/.planning` returns zero table declarations; deleted-form prose names the anti-pattern by the `Map`-vs-frozendict contrast.

### [V6_EVIDENCE_WEAVE_AND_SCOPE]

`graduation/handoff.md` additionally owns the shared evidence weave — compute's analogue of geometry's landed `evidence_run` weave (`graduation.md:150-169`). One `evidence_run(scope, subject, dispatch)` fold composes runtime `boundary` + the span + `@receipted`; the eight page-local `_TRACER` mints (`history.md:215`, `inference.md:311`, `model.md:66`, `study.md:366`, `codegen.md:167`, `handoff.md:93`, `interval.md:402`, `statistics.md:213`) and their inline `start_as_current_span` opens collapse onto it; the scope literals become one seed table on the hub (the runtime scope-vocabulary law at compute scale — `compute.rigor`-vs-`compute.interval` inconsistency dies with the literals), and the seed rows derive from the module-leaf vocabulary — `compute.<leaf>` — so a scope spelling disagreeing with its owning module name is a deleted form, never a per-page naming choice; the eight verbatim `_REDACTION` declarations collapse to one hub-exported constant. The span-bearing-vs-span-free emission split (some producers weave spans, others ride bare `@receipted`) unifies on the weave — one owner, one instrumentation shape, a new evidence owner is one scope row. Composing the weave is an observability import, never a graduation admission: statistics' graduation-free charter bans `graduates()` calls, not the hub import, so the graduation-free owners ride the same weave without breaching their preserved boundary.

### [V7_IDENTITY_POLICY_CONVENTION]

`CANONICAL_POLICY` is ruled canonical — the runtime identity owner exports it as the default (`identity.md:103`, defaulted `:209,:220`) with the bytes-law key equality. The six explicit-allocation sites rewire to the default (`symbolic.md:766`, `history.md:289`, `design.md:507`, `program.md:257`, `field.md:343`, `mesh.md:281` — the exact deleted form four sibling pages' prose already names); `history.md:286`'s equality assertion is provable law (`IdentityPolicy` is a frozen single-field value, so the default and a fresh allocation key identically) and the design-key resume cache (`Map[ContentKey, np.ndarray]`) hits by construction — `study.md:496`'s contrary "rejected by the runtime content owner" prose corrects to the owner's truth in the same motion; `study`'s receipt already carries the reuse key — `StudyReceipt.content_key` (`study.md:409`) spreads `content_key.hex` into its facts (`:435`) — the rebuilt page HOLDS that row, and the resume-equality proof is what makes it load-bearing downstream. Acceptance: `rg 'IdentityPolicy\(\)' libs/python/compute/.planning` returns zero call-site allocations.

### [V8_VERDICT_FOLD_AND_PUBLIC_CONTRACTS]

The four-way enum-verdict mechanism collapses: ONE shared fold parameterized by the `equinox.Enumeration` `RESULTS` class — the `_name_to_item` inversion + batched `jnp.max` code-reduce — hosted on `solvers/receipt` beside the `_STATUS` table its output feeds; the fold takes the gated module handle AND the `RESULTS` class as parameters, so receipt imports neither `jax` nor `equinox` at module top — every solver page, JAX-gated or not, imports receipt, and the x64-gated carrier stays the folder's sole JAX-importing owner; `LinearEngine.verdict` (`linear.md:314`), `NonlinearEngine.verdict` (`nonlinear.md:223`), `SolveEngine.verdict` (`differential.md:254`), and `_result_names` (`design.md:307`) become one-row compositions. The private-but-shared contracts go honest in the same motion: `_status` (cross-imported at `mesh.md:49`/`field.md:41`/`design.md:41`), `_sparse_receipt` (quadrature <- linear, `quadrature.md:52`), and the element-axis names promote to public surface on their owners or the cross-module reuse folds into the owning entry — a public contract masquerading as private is a deleted form. `linear.md:181`'s reflective `getattr(self, self.tag)` structure read — the exact form receipt's own doctrine names deleted — becomes a total `match`.

### [V9_JIT_AND_LOWERING_BRIDGE]

`numerics/jit.md` (298 LOC, ZERO importing fences — prose references only, `nonlinear.md:5`'s boundary disclaimer, `inference.md:21`, `quantity.md:482`) is ruled WIRED, not deleted: `study`'s `MeasurementMode` batch lane composes `JitBackend.compile` for loop-kernel objectives (the numba njit row) — the "solve/study spine harvests the `Jitted` stream" its receipt prose already describes becomes a fence — and `symbolic`'s discarded `_lower` callable (`symbolic.md:431` materializes then drops it; the "cross-file lowering seam" is currently unwired) gains its consumer: the lowering bridge emits the lowered-callable spec `jit` compiles and the study/quadrature consumers harvest, realizing the symbolic->jit->consumer lowering chain — DAG-lawful by construction: the spec vocabulary is jit-MINTED (a `Cfunc` row beside the standing `Njit`/`Vectorize`/`JaxJit` rows), `symbolic` imports jit downward to type its emission, and `study`/`quadrature` import jit ONLY, a symbolic-derived spec reaching them as a value — `symbolic`/`study`/`quadrature` are braced peers in the `[00]` order, so a consumer importing symbolic is a back-edge, the deleted form; `jit` stays in the first braced tier. WIRED is the ruling — the Python-level loop kernels exist on disk (`jit.md:56`'s numba `Kernel` alias, the `Njit` route `:158,:164`, `JitBackend.compile` `:179`, `study.md:91`'s SPEEDUP batch lane, symbolic's discarded `_lower`), so the bridge lands whole.

### [V10_DATA_SEAM_REALIZATION]

Both declared data seams become fences — pre-agreed by the data campaign (`xarray` held branch-tier BECAUSE compute co-consumes). `experiments/study` gains the DOE-frame admission arm: one polymorphic entry discriminating by input shape — the existing `Objective`+axes modality AND a contract-gated data frame (narwhals-typed, admitted through the data contract surfaces `FrameAdmission.admit`/`enforce` + `FrameInterop.schema_of`, decoded to the design matrix + `AxisDist` rows at the boundary) — never a second entry; `numerics/array` gains the labelled-array arm: `ArraySource` admits `xarray.Dataset`/`DataArray`, extracting `.data` + coords into `NamedAxis` rows (the declared labelled-array row's own claim) — a package-level co-consumption with no data-side seam row owed (the DOE frame is the data-crossing route; the labelled-array arm admits `xarray` objects from any producer), the branch `xarray.md` compute sub-page set gaining `numerics/array` with the arm's first fence. Both arms decode at the boundary and import no data interior beyond the published surfaces; the `ARCH` seam rows re-align to the post-data-rebuild page set in the same motion. The render seam is the third fence: the study/posterior evidence artifacts renders (the named demanding consumer, its crossing pinned on the landed artifacts corpus) crosses as a self-describing tabular frame — one frame projection on the experiments receipts (study cohort, posterior summary) whose columns carry source, unit, identifier, and content key, the physical crossing content-keyed Arrow bytes through the data `columnar` public fold (`arrow_bytes`, `columnar.md:465`; content-keying composes over it — the landed geometry crossing precedent), never an artifacts-side re-derivation.

### [V11_PAGE_GRAMMAR]

The 25 `[RESEARCH]` tails are zero-provenance defects and delete (every page but `model.md`, the standing grammar exemplar; `quadrature.md:572` and `codegen.md:390` are EMPTY headers); the finished corpus carries no appendix tier, and NO page or governance surface states anything about that tier in either polarity. The ruling is purge-and-fold: load-bearing member/arity confirmations fold into the owning `Packages` block or the `.api` catalog; "confirmed against the live distribution" framing, version narration, and empty headers delete; UNSETTLED items convert to in-body gated obligations. Invariant-once executes with it: transform's linear-amplitude contract (restated 4x — `transform.md:3,80-87,279,342`), the `FiniteGate` exclusion recital, and the ~6x hand-restated span-then-fence-then-receipt weave prose each state once in their owning block; index/body numbering aligns (`handoff.md:15` `[03]` vs `:201` `[04]`, `codegen.md:7` vs body). The purge applies to every page as it rebuilds, never deferred — the folder's largest clean prose reduction.

### [V12_UPSTREAM_REBIND]

The mechanical corpus-wide sweeps — each rename lands on its owning page as that page rebuilds, foundation pages first so later pages inherit clean spellings: (a) `rasm.runtime.content_identity` -> `rasm.runtime.identity` at all 21 sites on 20 pages INCLUDING the `codegen.md:188` `_BARE` frozenset row — a data row that ships the stale spelling in every generated stub preamble, the rename's one non-import site; (b) the reproduction rebind — `array`'s cross-backend bit-identity proof rides runtime `evidence/reproduction` `ParityReceipt`/`CorpusFixture` (the applied `[V7]` pin), and the C#-minted `EvidenceBundle` golden bundle joins the corpus as codegen's drift fixture per `[SEAM_AND_HUB_LAW]`; (c) the identity seam truth — `numerics/array ⇄ python:runtime/transport` (`ARCH:52`) re-homes to `← python:runtime/evidence` (identity is evidence, not transport; array decodes, never re-mints). Acceptance: `rg 'rasm.runtime.content_identity' libs/python/compute/.planning` returns zero.

### [V13_GOVERNANCE_TRUTH]

All three governance surfaces — codemap, seam ledger, README router/rosters — become exact. The codemap and router are already exact on disk (26 nodes with `field.py` and `jit.py` aboard, `ARCH:19`/`ARCH:31`; 26 router rows including `[08]-[FIELD]`/`[17]-[JIT]`; `pydantic`/`anyio` already struck — concurrency arrives through runtime lanes, never direct) — hold them, a roster regression is a defect. The seam ledger completes: every intra-compute edge as `->` rows (today ZERO of ~24 — the quadrature⇄mesh cycle is invisible to it), the cross-`libs/` retypes per `[V1]`/`[V12]`, and the undeclared `experiments/model -> python:runtime/roots` row (`model.md:42` `ResourceRef`; the upstream owner `runtime/transport/roots.md:127`). `[02]-[DOMAIN_PACKAGES]` gains the admitted `jaxtyping` (`[00]`) beside its first fence; `findiff`/`quadax`/`interpax`/`xarray`/`universal-pathlib` roster rows stand. The carried growth obligations land with their owning verdicts, never as a parallel ledger: the `array_layout` graduation via `[V2]`/`[V12]`, the data admission arms via `[V10]`, the seed-provenance vocabulary as policy rows on the owning numerics pages (numpy `Generator` and jax `PRNGKey` algebras reconciled as data, no new page), the symbolic->jit lowering bridge via `[V9]`, and the sparse-eigen route as one `LinearIntent` row on `solvers/linear.md` composing the catalogued `lobpcg`/shift-invert/`svds` surface.

### [V14_API_TIER_RECONCILIATION]

The compute `.api` tier is lean and truthful — settled state, and a regression is a defect: the three sampler catalogs (`numpyro.md`/`blackjax.md`/`nutpie.md`, packages consumed ONLY as `Literal["numpyro","blackjax","nutpie"]` strings — `inference.md:22,50`) are string-backend CONTRACT stubs carrying the `nuts_sampler` name, the per-engine `nuts_sampler_kwargs` keys (`nutpie` `backend='jax'/'numba'`, `numpyro` `chain_method`), and the stated, sealed DECLINE of the kernel/SMC/SGMCMC/VI surfaces — never re-opened by catalog authorship; `findiff.md` carries exactly the `coefficients(deriv, acc)` floor `sensitivity.md:7,487` mines; the dask catalog is branch-tier (`libs/python/.api/dask.md`, data+compute on the owner line) with no compute-tier file; `scipy.md` carries the sparse-eigen rows (`sparse.linalg.lobpcg`, shift-invert `OPinv`, `ArpackNoConvergence`, the full `svds` surface) the `[V13]` `LinearIntent` route composes; `array-api-compat.md`/`array-api-extra.md` carry `- owner: compute`, `xarray.md`'s branch owner line names both consumers (its compute sub-page set gains `numerics/array` beside `experiments/inference` with the `[V10]` arm), and `universal-pathlib.md`'s runtime-owned branch stub names compute on the consumer line (`model.md:36` `UPath`). Remaining motions, each landing with its owning fence: the admitted `jaxtyping` lands its compute-tier catalog with its first fence — the `Float`/`Int`/`Bool`/`Shaped` array annotations, `Array`/`PyTree`, and the `jaxtyped(typechecker=beartype)` weave, members verified against the live distribution, the charter line naming the `[V4]` gated-carrier consumers; `narwhals`'s data-tier stub gains compute on its owner line when the `[V10]` DOE-frame arm lands. No `rustworkx` compute-tier stub exists or lands — the data- and artifacts-tier catalogs are the Python owners. Every surviving stub's charter line names its live consuming fence.

## [02]-[PRESERVED_SURFACES]

Held as landed — each surface gains only the deltas a verdict names, and a regression against this list is a defect: `numerics/array.md` (9.5 — the wire-seam masterpiece; gains only the `[V10]` arm + `[V2]` graduation + rename), `analysis/symbolic.md` (9.5 core — FLINT ground-domain routing; gains `[V9]` bridge + `[V2]`), `optimization/convex.md` (9.5 — per-cone KKT engineering; gains its import), `solvers/sensitivity.md` (`DiffSpec` collapse; DGSM boundary held), `solvers/quadrature.md`/`differential.md`/`nonlinear.md` route catalogs, `graduation/codegen.md`'s catamorphism + `_ordered` gray-set guard, `experiments/inference.md` arviz-1.x fidelity + honest never-imported prose, `numerics/quantity.md` fenced consistency verdict, statistics' no-graduation boundary, the corpus-wide `_SLOTS`+`assert_never` receipt discipline, the raise-inside-fence idiom (every raise FENCED), the JAX x64 discipline, and the runtime FIT (single-mint, decode-only, no canonical-writer mirror).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| graduation/handoff | 9 craft / 5 campaign | 10 | decode-hub inversion; carrier-decode fence + unknown-subject fault; pinned self-describing fact floor; frozen-union contract block; one producer pattern; evidence weave + scope table + shared redaction; grammar |
| graduation/codegen | 8 | 9.5 | `_BARE` rename-safe; schema-version fault band + decode/emit round-trip golden fixture; Map rail; empty tail dies |
| solvers (receipt..field) | 8.5-9 | 9.5 | element axis on mesh; dodge dead; shared verdict fold on receipt; public contracts; solver-axis graduation + family ceiling rows; offload composed (PROCESS-pinned JAX, spec-crossing payload); rail |
| optimization | 9 | 9.5 | convex `graduates()` real; design/program feed the solver projection; `_result_names` folds; policy-default keys; offload; rail |
| numerics/array | 9.5 | 10 | xarray admission arm; `array_layout` graduation over `ParityReceipt`; `ARCH:51` truth |
| numerics (interval/jit/quantity/statistics) | 8.5-9.5 | 9.5 | jit wired or narrowed; seed-provenance rows; scope table composed; rail; grammar |
| analysis | 9 | 9.5 | symbolic lowering bridge; spatial seam disposition; invariant-once; rail |
| experiments | 8.5-9.5 | 9.5 | study DOE-frame arm (`content_key` fact HELD, already on disk); history resume-key equality proven over honest in-memory cohort ingress; offloaded sweeps with lane-by-payload-shape study entry; weave-routed spans |
| governance (ARCH/README/`.api`) | drifted | exact | 26-node codemap; full `->` ledger; 26-row router; roster and tier truth |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate, the folder tier for domain packages):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Block`/`Map` as the one ADT and dispatch spine (`[V5]` completes it), concurrency ONLY through the runtime lane owner (zero compute limiters, zero direct `anyio` — `[V4]`), retry NEVER over deterministic solves and only through runtime `guarded` at a genuinely flaky oracle (worker-death on the isolation leg rides `offload(retry=RetryClass.OCCT)` per `[V4]`, never a solve retry), `msgspec.Struct` one-shot admission at ingress with the landed `gc=False` scalar-leaf discipline, `beartype(conf=FAULT_CONF)` on public entries with `vale.Is` refinements, one span per fold through the `[V6]` weave — never an inline `start_as_current_span` beside the owned aspects. The corpus already models most of this weave; regressions are defects, not style.
- `numpy` (branch) — the numeric substrate mined to true depth: `ndarray` carriers on every page, `asarray`/`ascontiguousarray` C-order admission feeding every `tobytes()` canonical-bytes key (the `ContentIdentity` buffer law), `linalg` floors, `isfinite` gates, `einsum`/`gradient`/`interp` kernels; the `random.Generator`/`SeedSequence` algebra lands as the seed-provenance policy rows (`[V13]`); the branch stub (`libs/python/.api/numpy.md`) already names compute the rail — a hand-rolled loop over a vectorized numpy owner is a defect, never style.
- `scipy` — the deepest multi-page consume holds (8 importing pages across 9 submodules — optimize, spatial, stats, fft, signal, sparse, integrate, interpolate, linalg — 331 corpus references); the catalog carries the sparse-eigen rows (`lobpcg`, the full `svds` surface, shift-invert `OPinv`, `ArpackNoConvergence`) per `[V14]`; the `LinearIntent` route lands on `solvers/linear.md` per `[V13]`; `scipy-stubs` rides this row as the typing companion, no separate catalog.
- `sympy`/`python-flint`/`mpmath` — the exemplary FLINT ground-domain tier holds (`_FLINT_MATRIX_ROUTES` subset gating, `flint.good` certified evaluate, arb `real_roots` isolation); `[V9]` wires the lambdify/codegen lowering the `_lower` fold today discards.
- `jax`/`equinox`/`optimistix`/`lineax`/`diffrax`/`optax` — the gated-carrier discipline holds (`LinearEngine`/`NonlinearEngine`/`SolveEngine`/`DiffEngine` x64 contract); the `RESULTS` verdict fold collapses per `[V8]`; the x64 process-global flag is `[V4]`'s isolation proof, never a per-solve toggle.
- `jaxtyping` — the `[00]` admission: `Float`/`Int`/`Bool`/`Shaped` shape/dtype annotations over `Array`/`np.ndarray` operands and `PyTree` structure types on the gated carriers and process-lane spec payloads, runtime-checked through `jaxtyped(typechecker=beartype)` composing the standing `FAULT_CONF` fence (`[V4]`); an explicit `[COMPUTE]` roster row under the `python_version<'3.15'` marker of the JAX-stack band it annotates, its compute-tier catalog landing with its first fence (`[V14]`).
- `numba` — `jit.md`'s njit/vectorize/guvectorize + `CPUDispatcher` evidence surface gains its consumer per `[V9]` or the charter narrows honestly.
- `cvxpy`/`clarabel` — the DPP `Parameter` warm-re-solve sweep and per-cone dual-certificate recovery hold; the `convex_program` graduation goes live per `[V2]`.
- `pint`/`uncertainties` — held at depth (`Measurement` threading, `correlated_values`, `unumpy`/`ulinalg`, the one `_UREG` application registry); the Monte-Carlo bridge stays routed to the study sampler per the page's own `[MONTE_CARLO_BRIDGE]` law; `mcerp`/`soerp` stay a sealed decline (`[00]`).
- `SALib` — the eight-analyzer `SALIB_ROUTES` collapse holds; study's offload (`[V4]`) and DOE-frame arm (`[V10]`) compose around it, never through it.
- `pymc`/`arviz` — held at depth (arviz-1.x field fidelity, `psense_summary`, `ELPDData`); sampling offloads per `[V4]`; the three NUTS backends stay string-dispatch per `[V14]`.
- `numpyro`/`blackjax`/`nutpie` — contract stubs only: the `nuts_sampler` string + per-engine `nuts_sampler_kwargs` keys; kernel/SMC/SGMCMC/VI surfaces are stated, sealed DECLINES (`[00]`).
- `onnx`/`onnxruntime`/`skl2onnx`/`scikit-learn` — the export/validate/probe weave holds (`PROBE_RANK`, io-name set-equality, dtype-keyed feeds); sessions offload per `[V4]`.
- `scikit-fem`/`meshio` — the assemble/condense/solve and physical-group round-trip surfaces hold; the element axis unifies on mesh per `[V3]`.
- `pywavelets` — the wavedec/swt/cwt/packet multiresolution consume holds on the one `Signal` owner.
- `interpax`/`quadax`/`findiff` — README roster rows stand (`[V13]`); `findiff` is narrowed to the `coefficients` floor (`[V14]`); interpax x64 resample and quadax adaptive/sampled families hold.
- `sparse` — `[COMPUTE]`-tagged (`array.md:33` the consumer); the folder stub's owner line correct.
- `array-api-compat`/`array-api-extra` — the resolver + extension stack is the folder spine; both stubs carry `- owner: compute`.
- `xarray` (branch) — `DataTree` co-consume (`inference.md:45`) + the `[V10]` Dataset admission arm; the branch owner line names both consumers.
- `dask` — passive `array_namespace` backend only (`array.md:48,53`); the catalog is branch-tier with both consumers named and no compute-tier file (`[V14]`).
- `universal-pathlib` — `model.md:36`'s `UPath` consume stands in README `[03]`; the runtime-owned branch stub names compute on the consumer line (`[00]`).
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): array's `FiniteGate` lazy/eager fork + `_host_buffer` residence fold, interval's `_FLOOR_LADDER` + tailrec `_bisect`, statistics' `_STAT_ROUTES`/`_SIGNIFICANCE_CALLS`, history's `CrossStat`-owned kernels, spatial's `NEIGHBOUR_FLOOR` + Chebyshev-centre interior point, codegen's divergence-free `ast.unparse` identity, convex's `_CONE_KKT` closures, quantity's `Umath` arity enum.
- Carried growth obligations, each homed on its owning verdict: the `array_layout` graduation (`[V2]`+`[V12]`), the DOE-frame/labelled-array admission arms (`[V10]`), the seed-provenance policy rows on the owning numerics pages (`[V13]`), the symbolic->jit lowering bridge (`[V9]`), the sparse-eigen `LinearIntent` route (`[V13]`); the deferred growth themes stay deferred (`[06]`).

## [05]-[BUILD_ORDER_AND_ACCEPTANCE]

The landing order is the `[IMPORT_DAG_LAW]` topological order — foundations before consumers, every page landing only after the pages it imports:

1. **HUB + SUBSTRATE** — `graduation/handoff.md` FIRST (`[V1]` inversion + `[V2]` pattern law + `[V6]` weave/scope/redaction owners), `solvers/receipt.md` (`[V8]` verdict fold + public contracts + the solver-axis graduation projection), `numerics/array.md` (`[V10]` xarray arm + `[V2]` `array_layout` + `[V12]` parity rebind), `graduation/codegen.md` (`[V12]` `_BARE` + drift gate; Map rail).
2. **SOLVERS + NUMERICS** — `solvers/mesh.md` FIRST (`[V3]` element axis lands), `solvers/linear.md` (`[V8]` compose; `match` structure read), `numerics/jit.md` (`[V9]` page shape — the lowered-spec mint lands before its consumers), `solvers/quadrature.md` (dodge dies; imports mesh and jit downward), `solvers/nonlinear.md`, `solvers/differential.md`, `solvers/sensitivity.md`, `numerics/interval.md`, `numerics/quantity.md`, `numerics/statistics.md`, `solvers/field.md` LAST (reads the unified axis); the seed-provenance policy rows land with the numerics pages.
3. **ANALYSIS + OPTIMIZATION + EXPERIMENTS** — `analysis/transform.md`, `analysis/signal.md`, `analysis/spatial.md` (`[V1]`(d) disposition), `analysis/symbolic.md` (`[V9]` bridge; `[V2]` graduation), `optimization/convex.md` (`[V2]`), `optimization/design.md`, `optimization/program.md`, `experiments/study.md` (`[V10]` arm + `[V9]` compose + `content_key` fact), `experiments/inference.md`, `experiments/model.md`, `experiments/history.md` LAST (`[V7]` resume proof).

The mechanical corpus-wide sweeps — the identity rename (`[V12]`), the `CANONICAL_POLICY` rewires (`[V7]`), the dispatch-rail kill (`[V5]`) — land on each owning page as it rebuilds, foundation pages first so later pages inherit clean spellings. Closure obligations ride the change that alters their facts, never a parallel ledger: `README.md`/`ARCHITECTURE.md` close with the pages that change them — the codemap+ledger exactness and 26-row router (`[V13]`) close with the last page they describe; the `jaxtyping` README row and compute-tier catalog land with its first fence (`[V14]`); any residual manifest or `.api` stub motion lands with the change that admits, removes, or relocates the package; the `[V11]` grammar purge applies to every rebuilt page, never deferred.

Campaign acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.compute.*` import with zero back-edges and zero `TYPE_CHECKING` cycle-dodges, re-derived from the fences; (b) grep proofs return zero: `rasm.runtime.content_identity`, `[RESEARCH]` headers, `FrozenDict` tables, `IdentityPolicy()` call-site allocations, `type GeometrySubject` under `graduation/`; (c) the hub proof — every `HandoffAxis` case has a live self-wired producer or is gone, the `geometry` case decodes the frozen geometry-owned `GeometrySubject` union as data with zero `rasm.geometry` imports through the fenced carrier-decode arm (out-of-union subject -> typed fault), the `planned` fact floor is fence-pinned, and the C# crossing is `→` only; (d) the lane proof — every heavy entry is `async def` composing `lane.offload` with a modality row, JAX routes pinned PROCESS with spec-shaped payloads (no closure crosses the process lane), zero compute-minted limiters, zero inline span opens; (e) the seam proof — the study DOE-frame and array labelled-array arms are fences, `model -> runtime/roots` is ledgered, and the ledger's wildcard glyph, geometry decode counterpart, and identity-seam home carry the corrected owners and directions; (f) the identity proof — every content key rides the `CANONICAL_POLICY` default, `history`'s resume key provably equals `study`'s design key, and the `ARRAY_LAYOUT` and `EvidenceBundle` decode+emit round-trip fixtures sit in the runtime reproduction corpus; (g) ledger, codemap, router, and manifest agree on one 26-module set and one seam ownership.

## [06]-[OUT_OF_SCOPE]

- The C# seam owners: `Rasm.Compute` (the managed graduation gate, `EvidenceBundle` mint, `SolverReceipt` verdict reader) — compute graduates outward and decodes the offline wire, never re-mints; the `PY_WIRE_ALIGNMENT` canonical-writer campaign is upstream-owned and the count-prefix law is consumed transitively only.
- The runtime interior: the identity rename execution, `CANONICAL_POLICY` export, offload modality implementation, reproduction corpus landing — runtime's own campaign; the landed exports (the worker-death `RetryClass.OCCT` row `resilience.md:282`, `CANONICAL_POLICY`/`IdentitySource.lift` `identity.md:103,:114`, the two compute `CorpusFixture` design-pins `reproduction.md:146,:149`, the `universal-pathlib` compute consumer line on the branch stub) are the consumed contract.
- The geometry interior: `graduation.md`'s union mint, the `GeometryHandoff` fence, the energy subjects — geometry's own campaign; compute inherits the frozen contract and ships no ripple beyond the `[V1]` decode.
- The data interior: the `tabular/contract`/`interop` admission surfaces, the `xarray`/`dask`/`sparse`/`netcdf4` tier motions — data's own campaign; compute executes only its own side (the dask compute-tier strike, the retag verifications, the admission arms).
- Sibling folder `artifacts` — its pages never change here; cross-folder consequences land as surgical ripple edits on the owning sibling campaign brief, never as compute-side pre-builds.
- Deferred growth themes — the certified interval pipeline, the surrogate-gradient loop, probabilistic graduation, the differentiable-everything rail — genuinely deferred; the hub inversion and wiring law this campaign lands are their prerequisites, not their execution. No blocked work stands in the folder; the artifacts-origin axis case enters by the `[V2]` sibling-motion law.
