# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: no-never-arm
language: tsx
severity: error
files:
    - '**/.claude/plugins/function-hooks/hooks/**'
utils:
    never-parameter:
        has:
            field: parameters
            has:
                kind: required_parameter
                has: {field: type, has: {kind: predefined_type, regex: ^never$}}
                not: {has: {field: value, pattern: $_}}
rule:
    any:
        - kind: pair
          all:
            - {has: {field: key, pattern: $KEY}}
            - has:
                field: value
                kind: arrow_function, function_expression, generator_function
                matches: never-parameter
        - kind: method_definition
          inside: {kind: object}
          all:
            - {has: {field: name, pattern: $KEY}}
            - {matches: never-parameter}
    pattern: $MATCH
fix: '$KEY: absurd'
message: The arm over `never` is written inline
note: 'Name `absurd` from composition/decision.ts as the arm a type rules out, `{ deny: absurd, answer: absurd }`'
labels: {MATCH: {style: primary}}
