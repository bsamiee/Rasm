# Every case is a function, the match sits under a function body
id: no-result-match-for-map
valid:
    # The corrected map form
    - |
      def owner(names):
          return names.map(lambda text: text.strip())
    # Bind by statement, the ok arm passes and the remaining statements read the value
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  pass
          lines = text.splitlines()
          return Ok(lines)
    # Ok arm with a statement before the return
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  stripped = text.strip()
                  return Ok(stripped)
    # Error arm returning the bound name through another callee
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Failed(failure)
              case Result(ok=text):
                  return Ok(text)
    # Error arm mapping the error to another variant
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(Unsupported(failure))
              case Result(ok=text):
                  return Ok(text)
    # Error arm returning a name the pattern did not bind
    - |
      def owner(names, other):
          match names:
              case Result(tag="error", error=failure):
                  return Error(other)
              case Result(ok=text):
                  return Ok(text)
    # Error arm returning a member of the bound name
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure.inner)
              case Result(ok=text):
                  return Ok(text)
    # Error arm with a statement before the return
    - |
      def owner(names, log):
          match names:
              case Result(tag="error", error=failure):
                  log(failure)
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
    # Error pattern with a keyword beside the tag and the error
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure, ok=_):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
    # Guard on the error arm
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure) if failure.fatal:
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
    # Guard on the ok arm
    - |
      def owner(names, rid):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=manifest) if rid in manifest.runtimes:
                  return Ok(manifest.runtimes[rid])
    # Third arm
    - |
      def owner(names, rid):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=manifest) if rid not in manifest.runtimes:
                  return Ok(Unsupported(rid))
              case Result(ok=manifest):
                  return Ok(manifest.runtimes[rid])
    # Ok arm return holding an await, a lambda has no await
    - |
      async def owner(names, fetch):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(await fetch(text))
    # Ok value destructured as a tuple
    - |
      def owner(pair):
          match pair:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=(repo, ref)):
                  return Ok(f"{repo}@{ref}")
    # Ok value compared with a dotted constant
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=Mode.Fast):
                  return Ok(True)
    # Ok pattern with a keyword beside the tag and the ok
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text, error=_):
                  return Ok(text)
    # Match followed by a statement
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
          return Ok("")
    # Class other than Result
    - |
      def owner(names):
          match names:
              case Outcome(tag="error", error=failure):
                  return Error(failure)
              case Outcome(ok=text):
                  return Ok(text)
    # Comment inside the ok arm, the explanation needs an explicit review
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  # The strip drops the trailing newline
                  return Ok(text.strip())
    # Ok value compared with None
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=None):
                  return Ok("")
    # Ok value narrowed through a class pattern
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=str(text)):
                  return Ok(text.strip())
    # Match as the last statement of a loop body, the ok path continues with the next iteration
    - |
      def owner(names, parse):
          for name in names:
              match name:
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      pass
          return Ok(parse(text))
    # Match as the last statement of a with block, the return runs after the context closes
    - |
      def owner(names, parse, lock):
          with lock:
              match names:
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      pass
          return Ok(parse(text))
    # Different qualified Result classes are different dispatch domains
    - |
      def owner(value):
          match value:
              case first.Result(error=failure):
                  return Error(failure)
              case second.Result(ok=text):
                  return Ok(text.strip())
    # Error construction has another argument, not identity propagation
    - |
      def owner(value):
          match value:
              case Result(error=failure):
                  return Error(failure, context)
              case Result(ok=text):
                  return Ok(text.strip())
    # The tag literal is an exact value, not a prefix or alternative
    - |
      def owner(value):
          match value:
              case Result(tag="error" | "cancelled", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text.strip())
    # A catch-all arm before the error arm consumes errors itself
    - |
      def owner(value):
          match value:
              case Result():
                  return Ok("default")
              case Result(error=failure):
                  return Error(failure)
    # An unknown call return does not establish a Result bind
    - |
      def owner(names, parse):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return parse(text)
    # Success outside the branch must not gain a duplicated or invented fallback
    - |
      async def owner(space, tool, install):
          if not tool.exists():
              overlay = space.cache / "overlay"
              match await run([*install, "--no-print-usage"], space.root):
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result():
                      pass
          return Ok({**space.environment, "PKG_CONFIG": str(tool)})
    # A raw success return has a different boundary contract from map.
    - |
      def boundary(value):
          match value:
              case Result(error=error):
                  return Error(error)
              case Result(ok=text):
                  return text.strip()
    # Assignment expressions retain their enclosing scope.
    - |
      def owner(value):
          match value:
              case Result(error=error):
                  return Error(error)
              case Result(ok=text):
                  return Ok(saved := text.strip())
    # Pattern locals remain visible to cleanup outside the callback
    - |
      def owner(value):
          try:
              match value:
                  case Result(error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      return Ok(text.strip())
          finally:
              observe(locals())
    # Multiple arguments do not have the unary map contract.
    - |
      def owner(value):
          match value:
              case Result(error=error):
                  return Error(error)
              case Result(ok=text):
                  return Ok(text, context)
invalid:
    # The instance, an awaited subject
    - |
      async def owner(path, cwd):
          match await run(["otool", "-L", str(path)], cwd, capture=True):
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok([line.split()[0] for line in text.splitlines()[1:] if line.strip()])
    # Arms in the other order
    - |
      def owner(names):
          match names:
              case Result(ok=text):
                  return Ok(text.strip())
              case Result(tag="error", error=failure):
                  return Error(failure)
    # Ok arm spelled with the tag
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(tag="ok", ok=text):
                  return Ok(text.strip())
    # Ok pattern with no capture, callback parameter naming requires review
    - |
      async def owner(space, tool):
          env = {**space.environment, "PKG_CONFIG": str(tool)}
          match await run(["pkgconf"], space.root):
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result():
                  return Ok(env)
    # Ok pattern with the wildcard
    - |
      def owner(names, env):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=_):
                  return Ok(env)
    # Ok pattern spelled with the tag alone
    - |
      def owner(names, env):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(tag="ok"):
                  return Ok(env)
    # Static method spellings with the error keywords in the other order
    - |
      def owner(names):
          match names:
              case Result(error=failure, tag="error"):
                  return Result.Error(failure)
              case Result(ok=text):
                  return Result.Ok(text.strip())
    # Error pattern without the tag
    - |
      def owner(names):
          match names:
              case Result(error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text.strip())
    # Comment after the match, the match stays the last statement
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
          # Every arm returns
    # Match as the last statement of an if block, both arms return for the function
    - |
      def owner(names, flag):
          if flag:
              match names:
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      return Ok(text)
          return Ok("")
    # Match under a with block under an if block
    - |
      def owner(names, flag, lock):
          if flag:
              with lock:
                  match names:
                      case Result(tag="error", error=failure):
                          return Error(failure)
                      case Result(ok=text):
                          return Ok(text)
          return Ok("")
    # Class pattern spelled through the module
    - |
      def owner(names):
          match names:
              case result.Result(tag="error", error=failure):
                  return Error(failure)
              case result.Result(ok=text):
                  return Ok(text.strip())
    # Match as the last statement of a nested function, the return returns for the nested function
    - |
      def owner(names):
          def inner(value):
              match value:
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      return Ok(text.strip())
          return names.bind(inner)
    # Wildcards do not bind underscores; a generated lambda parameter would shadow this one
    - |
      def owner(value, _):
          match value:
              case Result(error=failure):
                  return Error(failure)
              case Result(ok=_):
                  return Ok(_)
