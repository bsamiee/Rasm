# [RASM_CS_GEOMETRY_SYNTHESIS] — blueprint of record

BASE: `draft-floor-maximalist.md` (consensus winner — law-gates + consumer-reach lenses; partition-elegance crowned folder-architecture-first, whose partition discipline is grafted in whole below). 28 page rows: 16 REBUILD, 2 MOVE, 10 NEW, plus 4 surgical `improve` pre-motions. Both probe rulings carried verbatim from the base (identical across all three passing drafts). All 14 verdicts, 16 evidence rows, 17 `[03]` deltas, 2 probe rulings, both consumer gates, and the full `[04]` roster disposed below. Every graft two judges named is applied; every single-judge graft passed its disk check (`pack.md:33/:18` + `solver.md:13` phantom sites, `repair.md:76-102`/`:57-62`/`:288-292` donor blocks, the on-disk `faults.md` stride map 2400/2404/2408/2412/2416/2420/2424 matching the adopted cluster numbering, zero clearance surface in today's `offset.md`). The graft ledger and recorded choices are `[13]`.

## [00]-[PROBE_RULINGS] — both hinges ruled from probe evidence, baked into page rows, never re-opened mid-leg

### R1 — GShark hinge: VENDOR-AND-OWN NOW (whole algorithm set, one motion; the pin dies)

Probe verdict FEASIBLE_WITH_GAPS with two ACCESS-MODIFIER WALLS (G1 surface ctor `internal` with no subclass path — `NurbsSurface.cs:29`; G2 surface derivatives `internal` + metric-blind public API — `Evaluate/Surface.cs:25`, open #234) that have NO in-envelope closure; the no-re-mint clause (`api-gshark.md:171`) forbids owned re-implementation; the one-engine law forbids a package+vendored-surface split. The only branch that closes G1+G2 AND keeps host-neutral surface evaluation for the Generation KERNEL GATE is vendoring the WHOLE composed algorithm set (MIT-clean): the verified Piegl-Tiller Gauss-Legendre arc-length engine (1e-6), Wang-2008 double-reflection RMF, NURBS-Book evaluation/refinement, the public `KnotVector` algebra, and the `Fitting.Curve` lane. The vendored owner is the NEW `Parametric/nurbs.md`; in the same motion it: publicizes curve+surface arbitrary-knot construction (G1 closed), publicizes raw `RationalDerivatives` with the #234 order convention fixed (G2 closed), mints fundamental-form/curvature projections over those derivatives (G3 closed — kernel math atop vendored evaluation, not a re-mint), lands the Piegl-Tiller surface interpolation/approximation lane so normal-offset surfaces are REAL NURBS (G5 closed on the strong branch, not the tessellation re-scope), adds the RMF closure-correction policy row for #373 + the coincident-sample NaN guard (G6 partial), parameterizes the `ClosestParameter` knobs (G7 closed), and seals clamp/`Normalize()`-at-ingress + hash-the-normalized-form as adapter law (G6/G10 — one curve, one content key). G8 curve offset: vendored `Offset` is the seed; the kernel deviation-refinement + self-intersection trim through the `[V4]` crossing lattice is a `curve.md` op body. G4: the surface-intersection TRIPLE (SSI #127, surface-plane #128, curve-surface #129) stays host-deferred — the disposition row widens to the full triple per probe. G9: `Fin`/`GeometryFault` boundary kernels at every vendored throw site. Manifest consequence, two-stage by design (closing the three-leg incoherence window): wave 1 applies the six probe catalog corrections so `api-gshark.md` stays truthful while settled pages still cite it; the `GShark 2.3.1` pin + `Rasm.csproj:18` reference + kernel `api-gshark.md` DELETE only in the leg that lands `nurbs.md` (wave 4).

### R2 — implicit-point hinge: the V4 EXACT PATH LANDS (no honesty re-scope), with the probe's companion set mandatory in the same legs

Probe verdict FEASIBLE_WITH_GAPS; the mechanism is proven (Attene indirect predicates; Cherchi mesh arrangements; Diazzi-Attene CDT) and each gap is a case-row widening on an existing owner. Ruled companions, all wave-1/wave-2 rows on the affected pages: (1) hybrid vertex table — `Tessellation`'s vertex payload widens from `Point3d[]` to a closed two-kind row store `Explicit(Point3d) | Crossing(defining-entity ids)` (SSI-2D: 4 ids, LPI-3D: 5, TPI-3D: 9); `SimplexStore` untouched; depth-1 law sealed (implicit rows reference INPUT vertices only; recovery-time constraint×constraint crossings re-express as TPI-3D over original entities; sub-segment splits re-anchor on the `Constraint` carrier). (2) Construction re-founding in `predicates.md` — `Lpi` re-founded as SSI-2D (four points); NEW LPI-3D (five points, three numerators + λ); `Tpi` re-founded over NINE input points with X,Y,Z numerators (derived `Plane` inputs die); leaf differences through error-free transforms; the rounded `Denominator`/`d1.Estimate()` inside the exact branch dies; the orient pair gains the negative-λ verdict flip mirroring `RationalOracle.InCircum`. (3) Family widening ~21 members on the ONE `Predicate` owner — `Orient2D` implicit combos {IEE, IIE, III} × axis-projected {xy, yz, zx}; `LessThanOn{X,Y,Z}` {IE, II} (the honest `OrderKey`, replacing exact-over-rounded); a directed-rounding INTERVAL-FILTER tier for every implicit member (kills the false no-filter rationale at `predicates.md:15,572` and the BigInteger-per-cavity-test throughput gap — this is also the `README:86` PeterO row landing). (4) In-circum ruled on the consumer: `TessellationPolicy` gains a CONSTRAINED-ONLY mode (no Delaunay restoration) for implicit-bearing builds — the reference pipeline's exact shape, zero multi-implicit in-circum burden; the `InCircle` {IIEE..IIII} combos and the 3D `orient3d`/`inSphere` multi-implicit family are RECORDED GROWTH gated on a CDTet consumer, never this campaign's landing. (5) Carrier plumbing — `Crossing`/`Constraint` carry defining-entity ids (the `bool IsImplicit` + rounded-`Point3d`-at-birth carriage dies); interning and chain-merge key on defining entities; `.Round()` is the ONE emission-seam materialization; both pages state signs-exact / coordinates-rounded-at-emission. V10b rides the cheapest sub-family (LPI-3D + projected Orient2D + LessThan; no in-circum, no substrate). V10c is DECOUPLED — the medial CDT input is a simple polygon ring (all-explicit, zero Steiner), so the Voronoi-dual accessor lands regardless: exact DT adjacency + circumcenter coordinates at the emission seam; an exact circumcenter side-of test is a recorded growth row. `README:42`/`ARCH:42` implicit-point rows STAND — the fences become true.

## [01]-[RATIFIED_PARTITION] — folders, TIER-2 namespaces, fault clusters (V1 + V12 discharged)

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`): `Solving/` MINTED (solver + fit — the one-LM family lifts out of Processing; 2-page floor satisfied, real growth axis: constrained optimization); `Drawing/` KEEPS BOTH PAGES AND ITS NAME under the ratified charter "downstream emission = projection + residency encoding" — a RECORDED, JUDGE-SCORED DEPARTURE from the V1 ruled default's "renamed to match" clause: the charter lands in `ARCHITECTURE.md`/`README.md`, the growth-axis test passes, and the rename is pure path churn (two extra move-lowered pages, two extra deletePages, counterpart-anchor sweeps across `FAB:33/46/47` + `ARCH:91-93` + AppUi rows) buying zero capability or collapse — the law-gates judge scored exactly that churn a defect in the draft that executed the rename; `Parametric/` grows to 7 robust-core pages beside landed `projections.md`/`locate.md`; `Meshing/` grows to 7 robust-core pages beside landed `mesh.md`/`dec.md`/`reconstruct.md`; `Processing/` holds 5 robust-core pages beside its 7 landed — `remesh.md` sites in `Processing/` literally beside the landed `segment.md` host-capture tier it coexists with, under the widened Simplification charter (the bijective-partition consequence; V11's own text sites the substrate beside the capture tier). Every folder ≥2 pages. The `ARCHITECTURE.md:110` `Rasm.Geometry.*` placeholder row is REPLACED by the enumerated partition below; codemap, README router, and `faults.md` cluster taxonomy update in the same wave-1 motion; the `[LANDED_KERNEL_LAW]` (b) inbound-reference sweep runs with it.

Namespace partition — 16 TIER-2 namespaces, 13 fault-routing namespaces mapped BIJECTIVELY onto 13 clusters exactly covering 2400-2449 (12×4 + 2 — the century closes arithmetically); 3 recorded exemptions (root `Rasm.Geometry` is the union owner; `Rasm.Geometry.Numerics` is total-pure; `Rasm.Geometry.Meshing` is the total arena — arena failures surface at freeze/re-admission through the landed `MeshSpace.Of` substrate rail, the two-family seam in action, so no `ArenaFault` mints). No namespace spans two folders; no namespace borrows; no band splits across namespaces. The phantom `Rasm.Geometry.Topology` dies at its exactly-three live sites (`pack.md:33` import, `pack.md:18` packages row, `solver.md:13` owner prose — all disk-confirmed) — all re-anchor to `Rasm.Geometry.Naming`; the `topology` cluster 2404-2407 renames to `naming` in the same motion. The four namespace-less pages gain declarations: predicates (W1), naming (W1), reconciliation (W1), receipts (W3).

| # | Namespace | Pages (folder) | Fault cluster |
|---|---|---|---|
| 1 | `Rasm.Geometry` (root) | faults (Numerics) | band owner 2400-2449 — union vocabulary, exempt |
| 2 | `Rasm.Geometry.Numerics` | predicates (Numerics) | exempt (total, no rail) |
| 3 | `Rasm.Geometry.Meshing` | edit (Meshing) | exempt (total arena; re-admission rides landed `MeshSpace.Of`) |
| 4 | `Rasm.Geometry.Spatial` | index (Spatial) | 2400-2403 `spatial`: `DegenerateInput` 2400 (cross-cutting admission case any namespace routes — recorded exception), `IndexMismatch` 2401, `KindMismatch` 2402 (NEW — the V8 Fin-routed query-kind × index-kind mismatch) |
| 5 | `Rasm.Geometry.Naming` | naming, reconciliation (Spatial) | 2404-2407 `naming` (renamed from `topology`): `NameCollision` 2404, `HashMismatch` 2405 |
| 6 | `Rasm.Geometry.Healing` | repair, receipts (Processing) | 2408-2411 `healing`: `UnrepairableMesh` 2408 re-typed (`NativeAssetMissing` re-codes OUT to 2423) |
| 7 | `Rasm.Geometry.Constraints` | solver (Solving) | 2412-2415 `constraints`: `OverConstrained` 2412, `SingularSystem` 2413 (rich, stay) |
| 8 | `Rasm.Geometry.Offsetting` | offset, skeleton (Meshing) | 2416-2419 `offsetting`: `DegenerateOffset` 2416 re-typed, `SkeletonStalled` 2417 (rich, stays), `CollapseStalled` 2418 (NEW — skeleton MCF stall) |
| 9 | `Rasm.Geometry.Arrangement` | delaunay, arrangement (Meshing) | 2420-2423 `arrangement`: `DegenerateArrangement` 2420 re-typed, `ConstraintUnrecoverable` 2421 (NEW — delaunay's home), `DegenerateTessellation` 2422 (NEW), `NativeAssetMissing` 2423 RE-CODED from 2409 (the tier-3 gate lives with the boolean owner — the census gap and the V5 re-home close together) |
| 10 | `Rasm.Geometry.Intersection` | intersect, slice (Meshing) | 2424-2427 `intersection`: `IntersectionFault` 2424 re-typed, `SectionFault` 2425 (NEW — slice's home) |
| 11 | `Rasm.Geometry.Fitting` | fit (Solving) | 2428-2431 `fitting`: `FitFault` 2428 (rich, stays) |
| 12 | `Rasm.Geometry.Parameterization` | flatten (Processing) | 2432-2435 `parameterization`: `ParameterizationFault` 2432 re-typed; curve's borrow ENDS |
| 13 | `Rasm.Geometry.Projection` | view (Drawing) | 2436-2439 `projection`: `ProjectionFault` 2436 re-typed |
| 14 | `Rasm.Geometry.Simplification` | decimate, remesh (Processing) | 2440-2443 `simplification` (charter widens: mesh rewrite under budget — QEM reduction + isotropic/quad re-tessellation): `DecimationFault` 2440 (rich, stays), `RemeshStalled` 2441 (NEW) |
| 15 | `Rasm.Geometry.Encoding` | pack (Drawing) | 2444-2447 `encoding`: `EncodingFault` 2444 re-typed |
| 16 | `Rasm.Geometry.Parametric` | nurbs, curve, surface, subdivide, develop, panelize, patternmap (Parametric) | 2448-2449 `parametric` — the DELIBERATE headroom spend forming the 13th (two-code) cluster per `faults.md:19`'s own growth law: `ParametricFault` 2448, `DevelopmentFault` 2449 |

Census choices RECORDED (ruled here, never re-litigated): (a) `Rasm.Geometry.Tessellation` DIES by unification into `Rasm.Geometry.Arrangement` — delaunay + arrangement are one exact-subdivision concept (the Cherchi reference pipeline is literally CDT-per-face); the `Tessellation` `[Union]` TYPE keeps its name inside the Arrangement namespace; delaunay's generic `DegenerateInput`/`UnrepairableMesh` borrowing dies for 2421/2422. The sub-band-share alternative (a `Tessellation` namespace owning 2421-2422 inside the arrangement stride) was equally legal under the V1 sub-band reading — the collapse is CHOSEN for the bijective 13↔13 discipline and minimum namespace count. (b) The V12-recommended widened-parameterization-stride share for subdivide/develop/panelize is SUPERSEDED WITH CAUSE: it would force Parametric-folder pages into the Processing-resident `Parameterization` namespace across a folder boundary, breaking the two-tier law — exceeded, not diluted; the `parametric` 2448-2449 cluster carries the tier instead. (c) Slice stays in the `intersection` stride and skeleton in the `offsetting` stride per the concern-adjacency rulings. No federation re-plan needed: the ruled map fits 2400-2449 with zero silent squeezes.

Fault-payload signature set — SIGNATURE-LOCKED for the `faults.md` rebuild (the 10 stringly re-types + 8 new mints; the 7 rich cases stay verbatim):

- Re-typed: `DegenerateInput(GeometryKind kind, int index, string witness)` 2400 · `IndexMismatch(EntityKind kind, int expected, int actual)` 2401 · `UnrepairableMesh(HealStage stage, int remaining)` 2408 · `DegenerateOffset(int wavefrontVertex, double time)` 2416 · `DegenerateArrangement(int cellCount, string manifoldWitness)` 2420 · `NativeAssetMissing(string engine, string rid, long ceiling)` 2423 (re-coded) · `IntersectionFault(PrimitiveKind a, PrimitiveKind b)` 2424 · `ParameterizationFault(ChartId chart, double distortion)` 2432 · `ProjectionFault(EdgeKind kind, int segment)` 2436 · `EncodingFault(ChannelKind channel, ChannelDtype dtype, string detail)` 2444.
- New mints: `KindMismatch(SpatialKind index, QueryKind query)` 2402 · `ConstraintUnrecoverable(int constraint, int budget)` 2421 · `DegenerateTessellation(int simplex, string witness)` 2422 · `SectionFault(int layer, double elevation, int openChains)` 2425 · `CollapseStalled(int iteration, double residual)` 2418 · `RemeshStalled(double targetLength, double achieved, int iterations)` 2441 · `ParametricFault(ParametricStage stage, string carrier, string witness)` 2448 (stage ∈ {Construction, Evaluation, Station, Offset, Encode} — per-concern typing through the bounded discriminant, discharging V12's per-concern bar inside the two-code cluster) · `DevelopmentFault(DevelopmentStage stage, int unit, double witness)` 2449 (stage ∈ {Subdivision, Strip, Panel, Pattern}; witness = the per-concern measure: refinement level · isometry error · panel defect · instance defect).
- Rich cases kept: `NameCollision` 2404, `HashMismatch` 2405, `OverConstrained` 2412, `SingularSystem` 2413, `SkeletonStalled` 2417, `FitFault` 2428, `DecimationFault` 2440.

Numbering rule for codes not anchored above: next free code within the named cluster stride, emitted as the complete numeric map by the `faults.md` rebuild (cluster taxonomy = this partition).

## [02]-[PAGE_SET] — 28 rows (paths `libs/csharp/Rasm/.planning/`-relative)

Row schema: path | action → engine lowering | charter (TIER-1 folder · TIER-2 namespace · fault cluster) | entry (`Apply` op-union, or NONE for vocabulary/substrate pages) | seams (out = composes, in = consumed by; one anchor per seam, both ledger directions) | wave.

### Wave 1 — FLOOR + CONTRACTS

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 1 | `Numerics/predicates.md` | REBUILD → rebuild | Numerics · `Rasm.Geometry.Numerics` · exempt (total). The adaptive exact-predicate + implicit-construction owner: four-tier direct ladder PRESERVED VERBATIM (double filter → `ddouble` Refine → `Expansion` → four-way oracle); implicit half RE-FOUNDED per R2 items 2-3 (SSI-2D/LPI-3D/TPI-9pt constructions, ~21 family members, negative-λ flip, exact leaf EFTs, directed-rounding interval-filter tier via `ERounding.Floor`/`Ceiling`); strict-IEEE-754/RID invariant stated on `NumericsPolicy`; FMA-free Dekker-split `TwoProduct` fallback row gated on RID FMA capability (`Splitter` consumed); honest precursor rationale ("no maintained, NuGet-published, indirect-complete predicate library exists") | NONE (pure `Predicate` family: `Orient2D/3D`, `InCircle/InSphere`, implicit combos, `LessThanOn{X,Y,Z}`; `Sign`-returning, no rail — public = the package-boundary-consumable export) | out: `TYoshimura.DoubleDouble`/`ExtendedNumerics`/`PeterO` (`api-*` anchors); in: every robust-core owner; cross-package: `Predicates ← Fabrication/Posting` (`ARCH:89`), NEW `Predicates ← Rasm.Compute` CDTet row (`RASM-CS-COMPUTE [V7]` — public `Orient3D`/`InSphere` already satisfy by shape) | 1 |
| 2 | `Numerics/faults.md` | REBUILD → rebuild | Numerics · `Rasm.Geometry` (root) · union owner. ONE `GeometryFault` union: 10 stringly cases re-typed + 8 new mints per the `[01]` signature-locked payload set; the two-family seam statement lands (`GeometryFault` band-2400 ⟂ `Rasm.Domain` `Expected`/`Fault`, neither absorbs — mirroring `rails.md`'s side, discharging `ARCH:112`); cluster taxonomy = the `[01]` ratified partition (topology→naming rename, delaunay homed in the arrangement cluster, `NativeAssetMissing` 2409→2423 re-code, the `parametric` 2448-2449 headroom spend); growth law restated: a new failure is a case row in its sibling's sub-band | NONE (fault vocabulary: `GeometryFault.<Case>(...).ToError()`) | out: none; in: every fault-routing owner; `ARCH:112` seam row both sides | 1 |
| 3 | `Spatial/index.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Spatial` · 2400-2403. Primitive broad-phase owner (SAH-BVH · Morton octree · agglomerative PLOC + degradation-refit, preserved verbatim): `PointCloud` arm EXCISED in one motion (case `:118`, kind `:44`, `BuildPointCloud` `:151-155`, `KdNearest/KdRange` `:372-381`, guards `:130,354-359,542,558,573` — point k-NN/radius routes landed `neighbors.md`); zero throws (total over {Bvh, LinearOctree}); kind mismatch routes `Fin` + typed `KindMismatch` 2402 (the silent empty dies); bounds narrowing rounds OUTWARD (min toward −∞, max toward +∞ incl. the `:590-591` wire casts); the `Wire` case returns `Fin<(float[] Bounds, long[] Nodes)>` over `NodeLinkProjection` (the `using Rasm.Compute.Solver` dies); GWN single-owner: index owns the BVH-accelerated winding query, `reconstruct.md` GWN + `fields.md` `SignedDistanceFromMesh` compose it — the ONE distance-field lane; counts resync (2 cases · 3 kinds); `CLASH_GOLDEN` marked harness-gated | `Fin<SpatialAnswer> Spatial.Apply(SpatialOp, Op? key = null)` — ONE entry over `Build(SpatialKind, BuildPolicy)` · `Refit` · `Query(SpatialQuery)` · `Wire` (the `Build`/`Query`/`ToAcceleration` multi-static surface collapses) | out: predicates (winding signs), faults (`KindMismatch`), `neighbors.md` (point-query routing row); in: intersect/arrangement (broad-phase), view (occlusion), decimate (Hausdorff); cross-package: index⇄Compute node-array wire (`ARCH:86` composed; `COMPUTE:121` decodes; `COMPUTE:120` spurious reverse row DELETED); `Index ↔ Fabrication` broad-phase rows (`ARCH:84,85`) made bidirectional | 1 |
| 4 | `Spatial/naming.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Naming` · 2404-2407. Persistent-name algebra: `RebuiltEntity` gains explicit `int Self`; `With`/`Track` key `VertexNames` by `Self` (the `IncidentVertices[0]` star-minimum mis-key dies); `Subsumes` subset direction corrected for splits (child ⊂ parent) AND the migration predicate re-founded on shared-name overlap fraction (policy-thresholded — the fragile vertex-set-subset model hardens); Survive×Migrate injectivity resolved: a claimed survivor still seeds sibling `Parent` provenance (lineage preserved; injectivity via fresh `born.Value` stands); namespace declared | `Fin<NameTable> Naming.Apply(NamingOp, Op? key = null)` — `Track(prior, rebuilt)` · `Resolve(boundary)` (ONE entry) | out: `identity.md` `ContentHash.Of` (landed), faults; in: reconciliation (entity bridge), solver (`EntityKind` re-anchor), pack (`CanonicalTopology.OfMesh`) | 1 |
| 5 | `Spatial/reconciliation.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Naming` · 2404-2407 (shared with naming). Naming↔hash bridge: `BuildEntities` emits `Self=v` (`star` kept for the histogram only); namespace + `using Rasm.Domain` declared (the unqualified `ContentHash.Of` compiles); `Encode` widens to ONE op-union over {Mesh, Cloud, Parametric} — the Parametric case carries RAW ARRAYS (degree · knots · weights · control net, canonicalized; NO nurbs.md type appears in wave 1 — the W4 carriers project INTO this raw shape, killing the forward type edge by construction), hashed in NORMALIZED form per R1, an `EncodeOp` case row on the ONE seam family, a COMPONENT of the Element `ToCanonicalBytes` fold, never a sibling SpineRef key; `CANONICAL_BYTE_IDENTITY` digest flagged harness-confirmed-once; Persistence anchor unified to ONE row | `Fin<ReconcileAnswer> Reconciliation.Apply(ReconcileOp, Op? key = null)` — `Encode(EncodeForm)` · `Reconcile(prior, rebuilt)` · `BuildEntities(MeshSpace)` (ONE entry; the `Encode`/`Reconcile` twin statics collapse) | out: naming (Track), `identity.md` (`ContentHash.Of` — ledger row `ARCH:79`); in: pack (mesh/cloud keys), curve/surface (parametric bytes, W4 backward); cross-package: → Persistence content-key, ONE anchor = `Rasm.Persistence/ARCHITECTURE.md:55` spelling adopted at `reconciliation.md:3`/`:129` and `ARCH:79`; parametric component feeds `Element/codec.md:151-152` | 1 |
| 6 | `Meshing/edit.md` | NEW → new | Meshing · `Rasm.Geometry.Meshing` · exempt (total arena; re-admission rides landed `MeshSpace.Of`). The `[V6]` MUTABLE-ARENA tier beside landed `mesh.md`: `MeshEdit` extracted from `repair.md:76-102` (the W1 `improve` pre-motion on repair excises the donor block — charter-carried, never absorb-encoded) and RE-FOUNDED as a true SoA arena (the immutable-copy-per-op O(V+F) shape dies); the two-carrier seam stated both pages (`MeshSpace` immutable admission snapshot / `MeshEdit` predicate-gated build arena — no third carrier); the soup adapter ×3 collapses here (`intersect.md:326`, `view.md:386`, `arrangement.md:352-365` → ONE `Soup(MeshSpace)`/`OfMesh` owner, native duplication deduped); `Kernels.WeldDuplicates` + the `RepairPolicy.WeldTolerance` knob SITE HERE (dedup-on-arena is an arena op — the arrangement→repair wave-2→3 edge dies, completing the V5 cycle break); the store-mutability + arena-concurrency contract lands corpus-wide in this page's law block (arenas single-writer, publish-by-freeze, `LaplacianCache` the exemplar; frozen/partition-disjoint spans only for parallel folds); `CommunityToolkit.HighPerformance` ADMITTED on these arena shapes (`Span2D`/`MemoryOwner<T>`/`ParallelHelper`, `api-highperformance.md:26,35,54`) | NONE (arena substrate: `MeshEdit.Of(MeshSpace | soup)` carrier lifecycle + `ToSpace(Context, Op?) → Fin<MeshSpace>` freeze + `Kernels` weld/soup family — total ops) | out: `mesh.md` (two-carrier seam row, 1-hop ripple re-anchoring `mesh.md:647`), predicates (weld gates); in: intersect, arrangement, view, decimate, receipts, repair, remesh, slice, skeleton, subdivide | 1 |

Wave-1 `improve` pre-motions (engine-lowered rows on later-wave pages, folded into their main motions — the V1 phantom-kill obligation of brief `[05]` leg 1 executed IN leg 1): `Processing/repair.md` improve — `MeshEdit` donor block `:76-102` excised to `edit.md`, imports re-anchor, page otherwise untouched until W3; `Drawing/pack.md` improve — phantom `:33` import + `:18` packages row re-anchor to `Rasm.Geometry.Naming`; `Processing/solver.md` improve — phantom `:13` owner prose re-anchors to `Rasm.Geometry.Naming`. Wave-1 roster motion rides beside rows 1-6 (see `[09]`).

### Wave 2 — MESHING LATTICE (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 7 | `Meshing/delaunay.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Arrangement` (namespace unification — the `Tessellation` `[Union]` TYPE keeps its name) · 2421-2422 (`ConstraintUnrecoverable`, `DegenerateTessellation`; the generic borrowing dies). Exact CDT/CDTet: the seven `[STORE_OPS]` stub bodies (`:192-198`) TRANSCRIBED (the hardest transcription in the tree — `Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces`); vertex payload widens to the R2 two-kind row store (`Explicit | Crossing(ids)`), `Retriangulate` appends implicit rows without materializing; `TessellationPolicy` gains the CONSTRAINED-ONLY mode for implicit-bearing builds; the walk/cavity route the widened predicate family (projected `Orient2D` combos; `InCircleLPI` stays for the explicit-corner Steiner case); Voronoi-dual accessor lands as a `Tessellation` projection (exact DT adjacency + circumcenters materialized at the emission seam — V10c decoupled, all-explicit medial input; `README:99` discharged); lifted-paraboloid LOWER-HULL projection lands as the second projection row (the `[V11]` exact tier: Fabrication stock/clearing envelopes, Generation containment at robust grade); `SimplexStore` re-labeled honest mutable arena under the `[V6]` contract; rejection rationale rewritten (Triangle.NET is MIT; the load-bearing bar is float-epsilon + no exact CDT/CDTet; TetGen stays AGPL-rejected); Morton insertion order preserved verbatim | `Fin<Tessellation> Tessellation.Build(TessellationOp, Op? key = null)`; projections `VoronoiDual`/`LowerHull`/`ToMesh` on the result | out: predicates (walk/cavity/implicit), edit (arena law + soup), faults; in: arrangement (`Tessellation.Build`), offset (dual), repair (CDT re-mesh, W3); cross-package: Fabrication stock-envelope hull via the `[V11]` ruling; namespace-unification sweep: inbound imports in offset/repair/arrangement + `ARCH:[03]` swept in their own waves (no landed page imports the dying namespace) | 2 |
| 8 | `Meshing/intersect.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Intersection` · 2424. The exact-crossing lattice, single owner: `Chain` RE-FOUNDED — crossing endpoints keyed by DEFINING ENTITIES (R2 item 5; the cross-face merge key that does not exist today), linked ACROSS segments, walked by true connectivity; closed loops close ORIENTED (outer CCW / holes CW), open curves run end to end as typed OPEN-chain rows (`IntersectResult` widens beyond loops-only); the dead `Next` column (`:75`) wired as the successor walk or deleted; `Crossing` carries defining-entity ids + LPI-3D payload (the `Denominator`/`Lambda` dual-field resolves with the predicates re-found; `.Round()` = emission only); `SegmentSegment`/`LineLine` typed as a 2D restriction case; the three `QueryResult` hard casts (`:222,233,241`) route `Fin`; the E7 collapse LANDS BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply` — no fourth kernel); host-delegation cites re-anchor `Analysis/Intersect.cs` → `relations.md` at BOTH sites (`:5`, `:388`); `contract` label restored only when the chain is real | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Op? key = null)` | out: predicates (Guigue-Devillers signs + LPI-3D + LessThan ordering), index (BVH broad-phase), edit (soup), faults; in: arrangement, offset, repair (V4), slice (V10b), view (Section), curve (offset trim); cross-package: altitude boundary ⇄ `Analysis/relations.md` (both sides, one anchor) | 2 |
| 9 | `Meshing/arrangement.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Arrangement` · 2420 + 2423. Exact arrangement/boolean owner: `BooleanOp` RE-HOMES here from `repair.md:57` (the W2 `improve` pre-motion on repair excises the enum `:57-62` + `BooleanArrangement` `:288-292` — charter-carried donor motion; `ARCH:42` codemap already sites it); `BooleanReceipt` is ONE TYPE owned HERE — receipts' heal-session boolean case COMPOSES it as payload beside `ManifoldStatus`, no renamed `HealBooleanReceipt` sibling; `Constraint` gains the implicit-point case — operand crossings ride defining-entity carriage into `Tessellation.Build` (R2; `PlaneCrossPoint`/`EdgeCrossings`/`InTriangle` inline copies DIE into `Intersection.Apply`; the dead `ImplicitCrossing.IsImplicit`/`Explicit` carrier dies); interning keys on defining entities (bit-equality `Dictionary<Point3d,int>` dies); Manifold NAMED as the tier-3 scale companion behind `ArrangementPolicy.ScaleCeiling` = 1_000_000 combined operand faces (finite, revisable policy row; `int.MaxValue` dead code dies) routing the in-house `manifoldc` P/Invoke when the RID asset resolves, else `Fin` fail `NativeAssetMissing` 2423; golden-boolean fixture gates activation; managed exact arrangement stays the ONE correctness rail ("retires the tier-3 native CSG gate" re-scopes to exactly this meaning); weld edge → `edit.md` (`Kernels.WeldDuplicates` sited wave 1 — the repair edge is GONE); soup + triple-`DuplicateNative` dedup via edit; store honest mutable arena | `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp, Op? key = null)` (MeshBoolean · PlanarOverlay · CellComplex) + the `BooleanOp` smart enum | out: delaunay (`Tessellation.Build` + implicit `Constraint`), intersect (`Apply`/`Crossing`), index (`Winding`/`Overlap` — the correct V8 consumer), edit (weld/soup), predicates, faults; in: repair (`HealOp.Boolean` delegation, W3), receipts (`BooleanReceipt` payload, W3), offset (`PlanarOverlayResolve`, W2 backward), view + curve `ToMesh` (`PlanarOverlay` fill per `README:99`, W4); cross-package: `FAB:44` re-anchors `csharp:Rasm/Processing/repair` → `Meshing/arrangement` | 2 |
| 10 | `Meshing/offset.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Offsetting` · 2416-2419. Exact wavefront skeleton/offset/medial: medial made REAL against the delaunay `VoronoiDual` projection (the discarded build at `:325-326` dies; parabolic reflex arcs reconcile against the dual as `:16,478` claim); the shared 2D/3D CLEARANCE VOCABULARY MINTS HERE — ONE clearance result family: per-point clearance RADIUS as a first-class result field + `Clearance(Point3d probe)` arbitrary-probe query row (the `[V10]` mandate — a radius-less medial is a kernel defect; `skeleton.md` COMPOSES this same family for 3D, one result family across both dimensionalities, never a sibling shape); `JoinType`(Miter/Round/Bevel/Square)×`EndType` corner-strategy rows over the ONE offset assembly (`[GENERATOR_LAW]` — the next join is a row; CavalierContours kerf/arc stays the Fabrication stratum); `MinkowskiConvolution` COMPLETED (convex-vertex arc-fans + BOTH convolution directions); `SegmentsCross` (`:440-444`) dies into `Intersection.Apply`; `PlanarOverlayResolve` feeds the arrangement a valid soup (self-intersecting loops route the arrangement directly, no `CreateFromClosedPolyline` null-skip); `WavefrontStore` honest mutable arena with amortized-doubling capacity (the 2n under-allocation at `:195` dies); immutable prose dies | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Op? key = null)` (Skeleton · Offset(join×end rows) · Medial · Minkowski · Weighted · Clearance(probe)) | out: delaunay (`VoronoiDual`), intersect (`Apply`), arrangement (`PlanarOverlayResolve` — backward intra-wave, the corrected ledger row), predicates, edit (arena law), faults; in: skeleton (clearance vocabulary, W4), Fabrication via seam; cross-package: `FAB:22` `Toolpath/Skeleton.cs` DIES for `Offsetting.Apply` — the clearance-radius payload + arbitrary-probe query IS the seam payload (`RASM-CS-FABRICATION [V5]` weighted/variable-speed rows ride the existing `Weighted` modality) | 2 |

Wave-2 `improve` pre-motion: `Processing/repair.md` improve — `BooleanOp` enum `:57-62` + `BooleanArrangement` `:288-292` excised to `arrangement.md`; `HealOp.Boolean` remains as a thin delegation stub until the W3 rebuild.

### Wave 3 — PROCESSING + SOLVING (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 11 | `Solving/solver.md` | MOVE + REBUILD → new at `Solving/solver.md`; deletePages: [`Processing/solver.md`]; absorb {into: `Solving/solver.md`, from: `Processing/solver.md`} | Solving (MINTED) · `Rasm.Geometry.Constraints` · 2412-2415. The ONE damped-Gauss-Newton owner: `Iterate`/`Step` GENERALIZED to a residual+Jacobian functor (`Lm.Minimize(ILmModel, SolvePolicy, Op?) → Fin<LmResult>`) over the existing `SolvePolicy` λ-ladder (`LambdaUp`/`LambdaDown`/`StepFloor` — policy already exists, only the residual source abstracts); `ConstraintSolver.Solve` becomes the `Constraint` instantiation; the `ddouble` Σd² objective accumulation lands (`README:86`); the phantom at `:13` is already dead (W1 pre-motion) — the move carries the corrected prose; dense-LA law = landed `matrix.md` owners (the `register.md` phrasing adopted); preserve verbatim: `LambdaCeiling` singular guard, `SolveReceipt` all-finite gate, witness-rank DOF, bounded reject chain, the deliberate `ConstraintSolveReceipt` name distinction | `Fin<Solution> ConstraintSolver.Solve(ConstraintSystem, SolvePolicy, Op? key = null)`; functor row `Lm.Minimize(ILmModel, SolvePolicy, Op?) → Fin<LmResult>` | out: `matrix.md` (landed solve rail), naming (`EntityKind`), predicates, faults; in: fit (functor instantiation), decimate (pattern exemplar — already composes `CholeskyResult.SolveDetailed`); cross-package: `COMPUTE:120` spurious "Geometry solver consumes Compute LM" row DELETED (disproven by `solver.md:303-346`) | 3 |
| 12 | `Solving/fit.md` | MOVE + REBUILD → new at `Solving/fit.md`; deletePages: [`Processing/fit.md`]; absorb {into: `Solving/fit.md`, from: `Processing/fit.md`} | Solving · `Rasm.Geometry.Fitting` · 2428-2431. MLESAC/PROSAC primitive fitting: `Refine` INSTANTIATES `Lm.Minimize` (the `:560-627` re-implementation dies — hardcoded 1e-3/×10/1e12 die; λ threads through `RefineState` on the policy ladder, carry-down on accept); the dead `Consensus` BVH (`:331-336`, built-never-queried, wrong owner) dies — seeded local draw + range prefilter ride the landed `neighbors.md` kd-tree lane (`README:93` lands); `FitStore` (`:246-255`) DELETED (`Candidate` is the live carrier; density-bar prose corrected); `Line` chart de-degenerated (foot-of-perpendicular anchor as `PackPlane` does; `MinimalSamples` 3→2); cloud-PCA vocabulary composes landed `cloud.md` `CloudKernel`/`stats.md` `SampleMoment` (consumer, never a second covariance owner); the `:18` broken-backtick Packages line repaired; `ddouble` Σd² via the functor; preserve verbatim: truncated-cost sampler, adaptive budget, six closed-form minimal solvers, analytic `Distance`/`Gradient` arms | `Fin<FitResult> Fit.Apply(FitOp, Op? key = null)` | out: solver (`Lm.Minimize`), `neighbors.md` (kd-tree lane), `cloud.md`/`stats.md` (landed), faults; in: `Rasm.Bim` reconstruction (seam), reality-capture dry-run | 3 |
| 13 | `Processing/receipts.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Healing` · 2408-2411. Typed heal receipts: namespace MINTED (the fourth namespace-less page); `Converged` for a boolean tests topological SUCCESS (`!b.AssetGated` is not the fix — the gate flag leaves the convergence fold entirely; convergence = the boolean's own success witness); the duplicate `BooleanReceipt` shape (`:44`) DIES — the boolean case CARRIES arrangement's `BooleanReceipt` as payload beside `ManifoldStatus` (ONE type corpus-wide, no rename, no sibling record); receipts REGISTER into the landed `IValidityEvidence` validity fold (standalone `Converged`/`Improved` hand-rolls die); `ManifoldStatus.Improved` boundary heuristic fixed (edge-split INCREASES boundary components — `Improved` per-op semantics re-derived); before/after status arrives from the Genus-tolerant total topology seam (row 14); preserve: the 7-case `RebuildReceipt` union, `Of` mint discrimination, `Affected` seed, `ToLog` fold | NONE (receipt vocabulary registering into the validity fold) | out: arrangement (`BooleanReceipt` payload composition, W2 backward), `intent.md:214` `Topology` projection (landed, kept), `mesh.md` `TopologyReceipt.Project` + the NEW Genus-tolerant `ProjectionRow` (landed Growth extension), `validation.md` (landed fold), faults; in: repair (mints receipts) | 3 |
| 14 | `Processing/repair.md` | REBUILD → rebuild (main motion; the W1 arena-donor and W2 boolean-donor `improve` pre-motions already excised — two-touch page, explicit) | Processing · `Rasm.Geometry.Healing` · 2408-2411. Predicate-gated heal fold: the SEVERE partiality dies — ONE `ProjectionRow` Growth extension on the landed `mesh.md` `TopologyReceipt.Project` exposes the un-gated `(Euler, BoundaryComponents, IsManifold, IsOriented, Option<int> Genus)` so the heal rail is TOTAL over non-manifold/boundaried/odd-Euler inputs (its own input class; `mesh.md:196-206` Growth law, never a second topology rail); `SelfIntersectResolve` becomes a REAL CDT re-mesh through the substrate — segment-carrying tri-tri crossings (not one point), BOTH faces re-meshed, multi-crossings per face (the `patched.ContainsKey` drop dies) via `Intersection.Apply` + `Tessellation.Build`; topology threads forward (`before[n] = after[n-1]` — the 2N recompute dies); `GapClose` pairs on ORIENTED boundary half-edges (direction recovered from face winding, not index-sorted keys); the `((QueryResult.Hits)hits)` cast routes `Fin`; the four crossing-kernel copies (`:346-372`) die into `Intersection.Apply`; `HealOp.Boolean` stays as a case DELEGATING `Arrangement.Apply(MeshBoolean, …)` (type + implementation live in arrangement since W2); preserve: `HealOp`/`Standard` order, `DegenerateCollapse`/`DominantAxis` sliver logic, `WeldDuplicates` idempotence law (now composed from edit) | `Fin<HealSession> Heal.Repair(HealPlan, Op? key = null)` | out: edit (arena + weld), receipts (mints — backward in listed order), intersect (crossings), delaunay (CDT re-mesh), arrangement (boolean delegation), `mesh.md` (Genus-tolerant row), `api-rhino` `RebuildNormals`-only boundary, faults; in: reality-capture dry-run, Fabrication heal seam | 3 |
| 15 | `Processing/decimate.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Simplification` · 2440-2443. QEM decimation: the VACUOUS flip guard dies — `CollapseValid` compares the exact orientation of each incident face against its ORIGINAL supporting plane (the `Orient3D(face, face+normal) ≡ Positive` identity can never fire and is removed); boundary link condition admits `shared == 1` (open meshes decimate; spurious `DecimationFault` dies); the maintained vertex-vertex `Adjacency` EXTENDS to a vertex→incident-face incidence index feeding `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` (the O(F²) full-array scans die; O(F log F) drain); the PHANTOM `field.IsoSurfaceDetailed` dies — `Voxelize` composes the REAL static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key)` (`reconstruct.md:609`, prose at `:3,5,16,18,20,553,560` corrected); the `(QueryResult.Nearest)` cast (`:459`) routes `Fin`; `MeshEdit` from edit.md; `VoxelRemesh` stays a distinct row beside W4 remesh (boundary stated); preserve verbatim: `ddouble` quadric fold (`README:86` row), lazy version-skip queue, `matrix.md`-routed `OptimalPosition`, BVH Hausdorff bound; meshoptimizer/Compute split recorded (Alimer binding is Compute's) | `Fin<DecimationResult> Simplify.Apply(SimplifyOp, Op? key = null)` | out: edit (arena), `matrix.md` (landed solve), `reconstruct.md` (`IsoSurface.Detailed`), index (nearest via `Fin` rail), predicates, faults; in: reality-capture dry-run | 3 |
| 16 | `Processing/flatten.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Parameterization` · 2432-2435. Chart-atlas parameterization over the landed DEC handle: Dirichlet pin CORRECTED (large penalty P on pinned diagonals ONLY with rhs = P·value — the 1e-9-on-every-diagonal + value/1e-9 inversion dies); LSCM assembled SPARSE as the conformal-energy least-squares smallest-eigenpair through the `matrix.md` sparse owners (the dense `(2n)²` + `DecomposeSvd` dies); ARAP global assembly O(F) (in-scope face index `f` passed; `IndexOf` O(F)-scan + return-0-on-miss dies); `FactorNonZeros` reports the ACTUAL `CholeskySparse` factor fill (the `D1.NonZeros` wrong-operator dies; CSparse `Refactorize`/`Update` amortization noted for the factor cache); `ChartStore.Dead`/`FreeList`/`Kill` DELETED (BFS labelling is the island former); `OrderLoop` boundary walk keyed on oriented half-edges (the min-vertex `next` overwrite dies); `DiscreteCalculus` sparse shape re-verified against landed `spectral.md:191`; the `:18` wrapper-discipline framing adopts the ONE `matrix.md`-owner law phrasing; DEVELOP SEAM declared (ChartAtlas → `develop.md`); preserve: `MeshAdjointSnapshot.Of` composition, per-face Jacobian-SVD distortion, exact `Orient2D` flip guard, seam-cut island walk | `Fin<ChartAtlas> Flatten.Apply(ParamOp, Op? key = null)` | out: `dec.md`/`matrix.md`/`spectral.md` (landed), predicates, faults; in: develop (W4), segment.md capture tier (coexists); cross-package: flatten→Fabrication/AppUi rows (`ARCH` spine) stand | 3 |

### Wave 4 — NEW MESH OWNERS + PARAMETRIC TIER + DRAWING (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 17 | `Meshing/slice.md` | NEW → new | Meshing · `Rasm.Geometry.Intersection` · `SectionFault` 2425. The slice-stack owner: ONE section fold composing `IntersectOp.PlaneMesh` over a parallel-plane family; layer-height POLICY ROWS (uniform · adaptive · variable-by-slope · support-interface — ALL FOUR as seed rows, the family OPEN per `[GENERATOR_LAW]`; the named Fabrication `[V6]` demand lands NOW, the next policy is a row); per-layer contours ORIENTED (outer CCW / holes CW — riding intersect's re-founded oriented chains) with typed OPEN-chain rows for non-watertight sections (never silent closure); contour-NESTING TREES emitted as a kernel-owned SoA forest wire (parent/child index arrays — QuikGraph serves in-computation only, per the landed lane law); V10b exactness rides the cheapest implicit sub-family (LPI-3D + projected Orient2D + LessThan ordering; contour orientation/nesting are exact signs; coordinates round at `Polyline` emission) | `Fin<SliceStack> Slicing.Apply(SliceOp, Op? key = null)` | out: intersect (`PlaneMesh` fold + oriented chains), edit (soup), predicates (nesting signs), faults; in: Fabrication (`FAB:23` `Slicing.cs` DIES; `FAB:48` re-route REALIZED), Compute circulation (`RASM-CS-COMPUTE [V12]`a story-elevation contours through the wire) | 4 |
| 18 | `Meshing/skeleton.md` | NEW → new | Meshing · `Rasm.Geometry.Offsetting` · `CollapseStalled` 2418 (+ `SkeletonStalled` 2417 shared stride). 3D MCF curve-skeleton: author-kernel mean-curvature-flow skeletonization COMPOSING the landed machinery (`geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow` case — no permissive engine exists; CGAL GPL rejected stands); COMPOSES offset.md's clearance result family — per-point clearance RADIUS + `Clearance(Point3d probe)` arbitrary-probe query as the SAME first-class result family across 2D/3D (one clearance vocabulary, never a sibling shape; the `[V10]`c+d payload mandate); skeleton topology emitted as a kernel-owned SoA wire (node/edge/radius arrays) | `Fin<CurveSkeleton> Skeletonize.Apply(SkeletonOp, Op? key = null)` | out: `geodesics.md`/`fields.md` (landed MCF), offset (clearance vocabulary — W2 backward), edit (arena), `mesh.md` (substrate), faults; in: Fabrication (`FAB:22` payload — the weighted/variable-speed + arbitrary-probe demands of `RASM-CS-FABRICATION [V5]` land here and in offset.md) | 4 |
| 19 | `Processing/remesh.md` | NEW → new | Processing · `Rasm.Geometry.Simplification` · `RemeshStalled` 2441 (the widened mesh-rewrite-under-budget charter, recorded on both simplification pages). The remesh substrate, BOTH tiers landed (quad remesh is NOT deferred — panelize composes it; the V11 deferral criterion is moot): Botsch-Kobbelt isotropic remesh (split/collapse/flip/tangential-relax, genus-preserving) + cross-field-guided QUAD remesh composing landed `segment.md` Knöppel cross-fields/stripes — the AUTHOR-KERNEL robust tier BESIDE segment.md's landed HOST-capture tier (QuadRemesh/`QuadTarget`/`RemeshKind`), coexisting per the capture law with one anchor each, sited in the SAME folder; `decimate.md`'s `VoxelRemesh` stays a distinct row (recorded); Compute volumetric discretization names isotropic as its boundary-conditioning pre-step (decoded through the wire, never a Compute-side remesher) | `Fin<RemeshResult> Remeshing.Apply(RemeshOp, Op? key = null)` (Isotropic · QuadField rows) | out: edit (arena), `segment.md` (landed cross-fields, one anchor), predicates (flip gates), `mesh.md`, faults; in: panelize (W4 backward), Fabrication mesh-prep, `RASM-CS-COMPUTE [V7]` recorded growth | 4 |
| 20 | `Parametric/nurbs.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2448 (shared). THE VENDORED NURBS ENGINE (probe ruling R1): the whole MIT algorithm set owned in-kernel — NURBS-Book evaluation (curve+surface, arbitrary order), the `KnotVector` algebra (public `Refine` both carriers, `Normalize`, periodic/clamped vocabulary), Piegl-Tiller Bezier-decomposed Gauss-Legendre arc-length (1e-6) with `ParameterAtLength`/length inversion, Wang-2008 double-reflection RMF with the closure-correction policy row (#373) + coincident-sample guard, `Fitting` interpolation/approximation for curves AND the Piegl-Tiller SURFACE interpolation/approximation lane (G5 — normal-offset refit substrate), PUBLIC arbitrary-knot construction for both carriers (G1), PUBLIC raw `RationalDerivatives` with the #234 order convention FIXED (G2), fundamental forms I/II + principal/Gaussian/mean curvature as kernel projections over those derivatives (G3), `ClosestParameter` with PARAMETERIZED iteration/tolerance knobs (G7); ingress law: knots NORMALIZED + clamped at construction, canonical bytes hash the normalized form (G6/G10 — one curve, one content key; the raw-array projection into reconciliation's W1 `EncodeOp.Parametric` case); every construction/fitting throw site wrapped to `Fin` (G9); SoA span/caching wins recorded as the perf law (#391 class); vendor provenance + fixed-defect roster recorded (`GSharker/G-Shark@57bfbfb`, MIT, #234/#337/#373/#391) | `Fin<NurbsForm> Nurbs.Of(NurbsWire)` — ONE polymorphic admission discriminating curve/surface wire (the twin `NurbsCurveKernel`/`NurbsSurfaceKernel` admission statics collapse); evaluation members live on the carriers; op rails live in curve/surface | out: predicates (exact gates where signs matter), `Rasm.Vectors` (wire types), faults; in: curve, surface, develop (strip evaluation); host wire ingress (Rhino control points + knots + weights, meeting at the wire per `[PLACEMENT_LAW]`); manifest: GShark pin + `Rasm.csproj:18` + kernel `api-gshark.md` DELETE with this landing | 4 |
| 21 | `Parametric/curve.md` | REBUILD → rebuild | Parametric · `Rasm.Geometry.Parametric` · 2448. Host-neutral curve algebra FULL-BODY (the six stubs at `:116-140` die): the STATION/FRAME family lands as first-class cases — arc-length stations via `Divide(maxSegmentLength, equalSegmentLengths)`/`ParameterAtLength`/`PointAtLength` sweeping BATCH `PerpendicularFrames` RMF over the SpineRef `[T0,T1]` window (`SubCurve` + normalized domain), emitting `(station, frame)` PAIRS — the `PathRow`/`Placement` producer (`RASM-GENERATION-SPEC.md:138,164,173`); `Offset` PROMOTED to a first-class `ParametricOp` case: vendored seed + kernel deviation-refinement loop + self-intersection trim through the `[V4]` crossing lattice (G8); SSI disposition row states the HOST-DEFERRED TRIPLE (surface-surface, surface-plane, curve-surface — probe-widened); re-anchor SPLIT two-target per the dossier: evaluation-peer cites (`:3,20,190`) → `projections.md`, intersection cite (`:170`) → `relations.md`; the anomalous `using Rhino.Geometry` (`:33`) dies (host-neutral thesis holds; `Ray3d` resolves from `Rasm.Vectors`); parametric canonical-bytes emitted through reconciliation `EncodeOp.Parametric` (row 5); borrowed `ParameterizationFault` ends → `ParametricFault` 2448; `ToMesh` trimmed-fill → arrangement `PlanarOverlay` (kept) | `Fin<ParametricResult> Parametric.Apply(ParametricOp, Op? key = null)` (Evaluate · Measure · Divide · Stations · Split · Reconstruct · Offset · Intersect2D) | out: nurbs (engine — backward), reconciliation (`EncodeOp.Parametric`), intersect (trim lattice), arrangement (`PlanarOverlay`), faults; in: Generation SpineRef (stations/frames rows enter `ARCH:[02]`), patternmap/surface siblings; boundary: ⇄ `projections.md` (evaluation, reciprocal), ⇄ `relations.md` (intersection), ⇄ `locate.md` (never a second location algebra) | 4 |
| 22 | `Parametric/surface.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2448 (shared). Host-neutral surface ops: UV-PROVENANCED tessellation sampler (kernel-owned UV grid over vendored `PointAt` — per-vertex `(u,v)` beside the `MeshSpace`; the tier-wide UV-pullback obligation lands HERE by construction, `ClosestParameter` the parameterized foreign-point fallback); isoline FAMILIES via `KnotVector.Refine` + `IsoCurve` rows; geodesic POLYLINES composing landed `fields.md` `Geodesic` + `geodesics.md` heat/MMP through the UV pullback (results return to the parametric domain); NORMAL-OFFSET surfaces REAL: Greville-abscissae normal offset + vendored Piegl-Tiller surface refit (R1/G5 strong branch); curvature-domain sampling over nurbs fundamental forms (G3); surface canonical-bytes component through `EncodeOp.Parametric`; host-authored surfaces INGRESS through the public arbitrary-knot construction (G1 closed — SpineRef surface resolution lives) | `Fin<SurfaceResult> Surfaces.Apply(SurfaceOp, Op? key = null)` (Tessellate · Isolines · Geodesics · NormalOffset · CurvatureSample · Pullback) | out: nurbs (evaluation/derivatives/fitting — backward), `fields.md`/`geodesics.md` (landed), reconciliation (bytes), `mesh.md` (tessellation admission), faults; in: develop, panelize, patternmap (UV-provenanced input law: a world-space polyline with no surface binding cannot feed them), Generation gate | 4 |
| 23 | `Parametric/subdivide.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · `DevelopmentFault` 2449 (Subdivision stage). Subdivision surfaces: Catmull-Clark/Loop as STENCIL ROWS over ONE refinement fold (`[GENERATOR_LAW]` — the next scheme is a data row, never a sibling subdivider); limit-surface evaluation author-kernel (Stam eigenbasis rows); crease/boundary rules as row data; OpenSubdiv stays rejected (no managed binding; admission = P/Invoke burden gated on proven limit-eval-at-scale demand — recorded) | `Fin<SubdivisionResult> Subdivision.Apply(SubdivideOp, Op? key = null)` | out: edit (arena), `mesh.md` (substrate), faults; in: Generation gate (region subdivision item) | 4 |
| 24 | `Parametric/develop.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2449 (Strip stage). Guaranteed-ISOMETRIC developable-strip unroll — distinct from flatten's low-distortion conformal maps AND segment.md's host LSCM capture (tier boundary stated, one anchor each): geodesic-strip decomposition + ruling-line extraction + exact unroll with a per-strip ISOMETRY WITNESS receipt (the Fabrication sheet/plywood/fabric acceptance reads the witness) | `Fin<DevelopmentResult> Development.Apply(DevelopOp, Op? key = null)` | out: surface (UV-provenanced strips — backward), flatten (ChartAtlas seam, W3), `geodesics.md` (landed strips), nurbs (strip evaluation), faults; in: Fabrication unroll dry-run (`[05]`b), Generation gate (developable item) | 4 |
| 25 | `Parametric/panelize.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2449 (Panel stage). Cross-field-guided panelization: ONE mapper composing landed `fields.md` `CrossField` + `segment.md` Knöppel globally-optimal fields/stripes + the remesh substrate (row 19); `int symmetry` owns the quad/diagrid/tri family AS DATA; `sample.md` CCVT/blue-noise seeds panel distributions; panel families are policy rows | `Fin<PanelResult> Panelization.Apply(PanelOp, Op? key = null)` | out: remesh (W4 backward), surface (UV — backward), `fields.md`/`segment.md`/`sample.md` (landed), faults; in: Generation gate (panelization item) | 4 |
| 26 | `Parametric/patternmap.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2449 (Pattern stage). Pattern-to-surface instancing: landed `geodesics.md` tangent LOG/EXP maps + 2D symmetry-GROUP rows (wallpaper groups as data) → surface-mapped INSTANCE STREAM; every instance carries a frame parallel-transported by the landed vector-heat transport (position without orientation is half an instance); the Generation PATTERN/TILING plane's exact input | `Fin<InstanceStream> Patterning.Apply(PatternOp, Op? key = null)` | out: surface (UV pullback — backward), `geodesics.md`/transport lanes (landed), faults; in: Generation gate (pattern-to-surface item; per-instance frames in the `[05]`a dry-run) | 4 |
| 27 | `Drawing/view.md` | REBUILD (ground-up) → rebuild | Drawing · `Rasm.Geometry.Projection` · 2436-2439. The visibility engine made REAL — ruled choice: EXACT ANALYTIC APPEL QUANTITATIVE-INVISIBILITY (the exact-floor-native branch): invisibility counts updated ONLY at silhouette crossings computed through the `[V4]` crossing lattice — no sampling, no `SampleStep`, zero missed occluder edges (the E1 failure class dies by construction); the dead `BspNode`/`Partition`/`Split`/`Kill`/`FreeList` apparatus DIES with the choice (float BSP splitting mints rounded constructed vertices — the wrong shape beside an exact lattice); the phantom `PaintBackToFront` prose dies; `ToPolylines` chains by successor-linked connectivity (never kind-key concat, never visible+hidden merged); dead `ViewKind.EmitsHidden`/`NeedsBsp` columns consumed by the op table or deleted; `OccludedAt`/`Clip`/`OcclusionBias` die with sampling; soup round-trip collapses onto edit.md; SALVAGE verbatim: exact `FacesOppose` silhouette locus, `Cut` Section delegation to `IntersectOp.PlaneMesh`; select boundary stated on view's side (reciprocal of `select.md:593`); polygon fill routes arrangement `PlanarOverlay` (`README:99`); `README:65` RE-SCOPES in the same motion: "one `ViewOp` over an exact analytic quantitative-invisibility kernel" (the one index-doc re-scope this blueprint takes; `ARCH:63` already accurate) | `Fin<DrawingProjection> View.Apply(ViewOp, Op? key = null)` (HiddenLine · Silhouette · Section · Creases) | out: intersect (Section + silhouette crossings), index (ray/overlap acceleration for QI seeding), predicates (`Orient3D` signs), edit (soup), arrangement (`PlanarOverlay` fill), faults; in: Fabrication (`FAB:33` Posting BSP HLR DIES; `FAB:46` "beside" clause dies; `FAB:47` `HiddenLineResult` = thin projection), AppUi (`ARCH:91`); documentation dry-run (`[05]`c) | 4 |
| 28 | `Drawing/pack.md` | REBUILD → rebuild (the W1 phantom-kill `improve` pre-motion already applied — two-touch page, explicit) | Drawing · `Rasm.Geometry.Encoding` · 2444-2447. Residency encoding made REAL: `EncodedStore.Payload` becomes dtype-STRIDED `byte[]` with per-channel byte descriptors; the `System.Numerics.Tensors` seam survives as descriptor-dispatched typed views (`ReadOnlyTensorSpan<float>`/`ReadOnlyTensorSpan<Half>` selected on the descriptor `Dtype` row — element-type-generic span verified at `api-tensors.md:14,29,175-176`; `Half` HALVES residency for real; the widen-back `Pack` at `:53` dies); the phantom sites are already dead (W1) — the rebuild carries the re-anchored `Rasm.Geometry.Naming` truth; `PackKind` gains `field` · `toolpath` case rows with active-channel sets (Compute decodes as the residency case axis `Rasm.Compute/ARCHITECTURE.md:126`; AppHost signature-locks `EncodingKind.Field`/`.Toolpath` on them — two DATA rows, never a downstream encoding owner); the `:3` `OrientedNormals` provenance corrects to cloud→neighbors; PRESERVED VERBATIM: the `SampleDetailed` composition (`:346,463` onto `fields.md:367`) and the `VectorCloudMetric.OrientedNormals` composition (`:463`) | `Fin<EncodedGeometry> Encode.Apply(PackOp, Op? key = null)` (`PackKind` rows incl. `field`/`toolpath`) | out: reconciliation (`Encode` keys), naming (`CanonicalTopology`), `fields.md` (`SampleDetailed`, landed), `identity.md` (cloud keys), faults; in: AppHost (`ARCH:92`), Compute residency (`ARCH:93`) — both rows composed in-fence today, preserved | 4 |

## [03]-[ENGINE_EXECUTION_MANIFEST]

- deletePages: `Processing/solver.md`, `Processing/fit.md` (absorbed by the two `Solving/` rows — the only delete-with-absorb pairs).
- Kind census: rebuild ×16 (rows 1-5, 7-10, 13-16, 21, 27, 28) · new ×12 (rows 6, 17-20, 22-26 + the two MOVE destinations 11-12) · surgical `improve` pre-motions ×4 (repair W1 + W2; pack + solver W1 phantom kills — folded into their pages' later main motions; two-touch pages explicit: repair {W1, W2, W3}, pack {W1, W4}, solver {W1, W3}).
- Donor-block excisions WITHOUT source deletion are charter-carried on both rows, never absorb-encoded: repair→edit (`MeshEdit` block `:76-102` + weld primitive, W1), repair→arrangement (`BooleanOp` enum `:57-62` + `BooleanArrangement` `:288-292`, W2), intersect/view/arrangement→edit (soup adapter ×3, in their own waves).
- `.api` deletions ride the wave-1 roster motion, not deletePages: kernel `api-csparse.md` (dedupe), kernel `api-mathnet-providers.md` (OpenBLAS drop); kernel `api-gshark.md` deletes with the WAVE-4 `nurbs.md` landing (the R1 two-stage staging), never in wave 1.

## [04]-[SEAM_LEDGER] — corrected, both directions, one anchor per seam

In-kernel rows (arrow = composes; every edge equal-or-earlier wave; intra-wave edges point backward in listed order — verified acyclic in `[11]`): predicates ← ALL; faults ← every fault-routing owner; edit ← intersect·arrangement·view·decimate·receipts·repair·remesh·slice·skeleton·subdivide; index ← intersect·arrangement·view·decimate; naming ← reconciliation·solver·pack; reconciliation ← pack·curve·surface; delaunay ← arrangement·offset·repair; intersect ← arrangement·offset·repair·slice·view·curve; arrangement ← offset(`PlanarOverlayResolve` — the corrected row the `[02]` register omitted)·repair(`HealOp.Boolean` delegation)·receipts(`BooleanReceipt` payload)·view(`PlanarOverlay`)·curve(`ToMesh` fill); offset ← skeleton (the ONE clearance vocabulary); solver ← fit; flatten ← develop; remesh ← panelize; nurbs ← curve·surface·develop; surface ← develop·panelize·patternmap; mesh.md(landed) ← edit two-carrier row + repair/receipts Genus-tolerant `ProjectionRow` (Growth); neighbors.md(landed) ← index routing row + fit MLESAC lane; reconstruct.md/fields.md(landed) ← index GWN one-distance-field lane; geodesics.md/segment.md/sample.md(landed) ← skeleton·panelize·patternmap·develop·surface.

Counterpart-package ledger edits (1-hop ripples, landing with the leg that changes the seam):

| Counterpart anchor | Edit | Leg |
|---|---|---|
| `Rasm.Fabrication/ARCHITECTURE.md:22` | `Toolpath/Skeleton.cs` dies → consumes `Offsetting.Apply` + `Skeletonize.Apply` clearance-radius/arbitrary-probe payload (the ONE clearance family, 2D+3D) | 2 (offset), 4 (skeleton) |
| `FAB:23` + `FAB:48` | `Slicing.cs` planar section dies → `Slicing.Apply` slice-stack seam ("when realized" promise REALIZED) | 4 |
| `FAB:33` + `FAB:46` | `Posting/Projection.cs` BSP HLR dies; "beside the in-folder BSP solver" clause dies → consumes kernel `DrawingProjection` | 4 |
| `FAB:44` | BooleanOp anchor `csharp:Rasm/Processing/repair` → `Meshing/arrangement` | 2 |
| `FAB:47` | `HiddenLineResult` = thin projection of kernel `DrawingProjection` | 4 |
| `Rasm.Compute/ARCHITECTURE.md:120` | spurious "Geometry solver consumes Compute LM" row DELETED | 3 |
| `COMPUTE:121` | index⇄Compute direction settled: kernel emits `Fin<(float[],long[])>` node-array wire, Compute decodes | 1 |
| `COMPUTE` (new row) | `Predicates ← Rasm.Compute` CDTet Orient3D/InSphere consumer seam (`RASM-CS-COMPUTE [V7]`) | 1 |
| `COMPUTE` (new row) | slice → Compute circulation story-elevation contours (`RASM-CS-COMPUTE [V12]`a) | 4 |
| `Rasm.Persistence/ARCHITECTURE.md:55` | ONE-anchor unification (reconciliation.md:3/:129 + ARCH:79 adopt this spelling); parametric canonical-bytes component feeds `Element/codec.md:151-152` | 1 (anchor), 4 (parametric row) |
| `Rasm.AppHost/.planning/Sandbox/solver.md:54-55` | `EncodingKind.Field`/`.Toolpath` spellings signature-locked on pack's landed rows (verify) | 4 |
| Kernel `ARCH:[02]/[03]` + `README` | placeholder row → `[01]` partition; the ratified Drawing charter lands (name kept, recorded departure); Parametric consumer fan enters `[02]-[SEAMS]` as rows (curve.md:~192 prose dies); `README:65` re-scope (analytic QI); all other E16 rows LAND (see `[05]` V14) | 1, 4 |

## [05]-[VERDICT_DISPOSITIONS] V1-V14

| V | Disposition |
|---|---|
| V1 | RATIFIED at `[01]`: 16 single-folder TIER-2 namespaces, 13 fault-routing ↔ 13 clusters BIJECTIVE over exactly 2400-2449, 3 recorded exemptions; placeholder row replaced; phantom `Rasm.Geometry.Topology` dead at all 3 sites IN WAVE 1 (surgical improve pre-motions) → `Rasm.Geometry.Naming`; topology→naming cluster rename; `Solving/` minted; the Tessellation-namespace unification into Arrangement RULED with the sub-band alternative recorded; Drawing charter ratified "downstream emission = projection + residency encoding" with the NAME KEPT as a recorded judge-scored departure from the "renamed to match" clause (rationale in `[01]`; the decide gate ratifies); 4 namespace-less pages minted (predicates/naming/reconciliation W1, receipts W3); `FAB:44` follows V5. Inbound-reference sweep runs wave 1 and with every later folder delta. |
| V2 | Tier landed at rows 20-26 under probe ruling R1 (vendor-and-own NOW): nurbs with the ONE polymorphic `Nurbs.Of(NurbsWire)` admission; curve full-body + stations/frames + Offset promoted + SSI triple host-deferred; surface with UV-provenanced tessellation + isoline families + geodesic pullback + REAL NURBS normal-offset; subdivide stencil rows; develop isometric with witness; panelize symmetry-as-data; patternmap with transported frames. Both tier-wide obligations: (a) parametric content identity = `EncodeOp.Parametric` RAW-ARRAY row on the ONE reconciliation seam (row 5, W1 — no forward type edge), component of `ToCanonicalBytes`, hashing the NORMALIZED form; (b) UV pullback by construction (surface sampler emits per-vertex `(u,v)`; `ClosestParameter` the parameterized fallback). `Parametric/` name kept (beside landed `projections.md`/`locate.md`). `ParameterizationFault` borrow ends → 2448. ONE namespace over the whole tier (`[01]` recorded choice). |
| V3 | View rebuilt ground-up on the ANALYTIC APPEL QI branch (exact-lattice-native; BSP apparatus dies; `README:65` re-scopes in the same motion); connectivity chaining; select.md boundary reciprocal; `PlanarOverlay` fill composed; `FAB:33/46/47` counterpart edits. Salvage: `FacesOppose`, Section delegation. |
| V4 | Crossing collapse EXECUTED BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply` — rows 8-10, 14); implicit-point `Constraint` case LANDS per probe R2 with mandatory companions (hybrid vertex table, construction re-founding, ~21-member family, constrained-only mode, defining-entity carriage); typed 2D restriction on `SegmentSegment`; host-delegation re-anchored to `relations.md` at both cite sites. No honesty re-scope needed — the fences become true. |
| V5 | `BooleanOp` re-homed to arrangement (row 9; the W2 improve pre-motion excises the donor blocks so no two-owner transient survives on disk); `HealOp.Boolean` stays a delegating case; `BooleanReceipt` collision → ONE TYPE owned by arrangement, receipts' boolean case CARRIES it as payload (no rename, no sibling); Manifold NAMED tier-3 scale companion behind finite `ScaleCeiling` = 1_000_000 combined operand faces, in-house `manifoldc` P/Invoke (NO NuGet pin — the `Manifold` NuGet ID is a named-and-rejected HOMONYM, a CLI/MCP source-generator framework; `ManifoldNET` stays rejected-stalled), golden-boolean fixture gates activation; `NativeAssetMissing` re-coded 2423 into the boolean owner's cluster; `[T-BOOLEAN-NATIVE-ASSET]` card disposed (see `[12]`); cycle break COMPLETED by siting `Kernels.WeldDuplicates` + weld knob in wave-1 edit.md. |
| V6 | edit.md (row 6): arena extraction + re-found as true SoA arena, two-carrier seam, soup ×3 collapse + native-dup dedup, weld primitive sited, store-mutability + arena-concurrency contract corpus-wide (all four Meshing SoA stores re-labeled honest; offset 2n sizing fixed), HighPerformance ADMITTED on the arena shapes; arena namespace TOTAL (no `ArenaFault` — re-admission rides landed `MeshSpace.Of`, the two-family seam in action). Donor excision is the W1 improve pre-motion on repair — engine-executable, no two-owner window. |
| V7 | One LM: solver generalizes to `Lm.Minimize` functor over `SolvePolicy` (row 11); fit instantiates with λ threaded (row 12); decimate already exemplary; dense-LA law = landed `matrix.md` owners corpus-wide (flatten's two-vocabulary framing dies); `ddouble` Σd² + QEM accumulation land (`README:86`). |
| V8 | index contract (row 3): PointCloud lifted onto `neighbors.md` in ONE excision (kills E9 kd-dup + E10 throws/silent-empty + E11 counts — one structural cause); kind mismatch routes the typed `KindMismatch` 2402 mint; outward-rounded bounds; kernel-owned node-array wire composed via the ONE `Spatial.Apply` entry's `Wire` case; GWN single-owner ruling = index owns the accelerated query, reconstruct/fields compose (the ONE distance-field lane = Fabrication ingress); naming/reconciliation fixes (Self, Subsumes, injectivity, Encode composition verified, fixtures harness-gated); ONE Persistence anchor. |
| V9 | All Processing repairs landed as rebuilds (rows 13-16): flatten (Dirichlet, sparse LSCM, O(F) ARAP, honest fill, dead carriers, OrderLoop), decimate (REAL flip guard; boundary collapse; incidence index; phantom `IsoSurfaceDetailed` → real `IsoSurface.Detailed`; second cast), repair (Genus-tolerant TOTAL topology via ONE landed `ProjectionRow` Growth extension; real CDT self-intersect re-mesh; threading; oriented GapClose), receipts (Converged = success witness; validity fold; `Improved` semantics; BooleanReceipt composed), fit (FitStore deleted; neighbors lane; Line chart; functor). |
| V10 | (a) corner rows `JoinType`×`EndType` author-kernel on the exact wavefront (row 10; CavalierContours = Fabrication lane, pin re-grouped); (b) slice.md (row 17) with ALL FOUR layer policies as OPEN seed rows, oriented contours, typed open chains, nesting forest wire; (c) medial REAL against the delaunay `VoronoiDual` projection (probe: decoupled, all-explicit — lands unconditionally); (d) skeleton.md (row 18) MCF over landed machinery; the clearance-radius + arbitrary-probe query is ONE shared result family minted in offset.md and composed by skeleton.md across (c)+(d); (e) `Toolpath/Skeleton.cs` dies unconditionally. |
| V11 | Hull two-tier ruling: landed `cloud.md` rail STANDS (standing law) + the exact lifted-paraboloid LOWER-HULL lands as a `Tessellation` projection (row 7) — tier boundary stated on both pages (cloud = host/complex kinds; delaunay = predicate-gated exact fold for robust-grade envelopes). Remesh substrate landed BOTH tiers (row 19, sited `Processing/` beside the landed capture tier — isotropic mandatory, quad NOT deferred because panelize composes it; deferral criterion moot). |
| V12 | Fault plan at `[01]`: 10 stringly cases re-typed + 8 new mints, ALL signature-locked; two-family seam statement lands in faults.md (`ARCH:112` discharged); band plan complete — bijective 13↔13 over exactly 2400-2449, delaunay homed 2421-2422 in the arrangement cluster, `NativeAssetMissing` re-coded 2423, topology→naming rename, the `parametric` 2448-2449 headroom spend as the deliberate 13th two-code cluster (per-concern typing preserved through the stage discriminants); the widened-parameterization-stride recommendation SUPERSEDED WITH CAUSE (two-tier law — recorded in `[01]`); NO federation re-plan needed. |
| V13 | pack (row 28): dtype-strided byte store + descriptor-dispatched `ReadOnlyTensorSpan` views (verified viable — no honesty re-scope); phantom import dead IN WAVE 1 (pre-motion); `field`/`toolpath` `PackKind` rows landed for the Compute/AppHost consumers; two landed compositions preserved verbatim; `:3` provenance corrected. |
| V14 | Ledger fence-conformance: index wire, reconciliation identity entry, pack rows all COMPOSED in-fence; Parametric fan enters `[02]-[SEAMS]` as rows; `COMPUTE:120` dies; anchor drift unified (reconciliation triple → ONE; intersect→Fabrication split resolved with V10 rows); the offset→arrangement omission corrected in `[04]`; false rejection rationales corrected (delaunay:3 honest split; predicates:3 precise claim); strict-IEEE/RID invariant + FMA-free row + PeterO directed-rounding interval filter LAND in predicates (the filter tier doubles as the implicit-family throughput fix per R2); package-boundary `Orient3D`/`InSphere` export declared as the Compute seam row (already public by shape). Index-doc ledger: every E16 row LANDS except `README:65` which RE-SCOPES (BSP → analytic QI). |

## [06]-[EVIDENCE_DISPOSITIONS] E1-E16

| E | Disposition |
|---|---|
| E1 | Row 27: sampling engine + dead BSP + phantom `PaintBackToFront` + kind-key concat all die; analytic QI + connectivity chaining land. Documentation dry-run (`[05]`c of the brief) is the acceptance. |
| E2 | Rows 20-26 close every gate item (tables in `[08]`); curve stubs die; Offset promoted; enabling landed primitives composed one level up exactly as the register lists them. |
| E3 | delaunay:3 rationale rewritten (row 7); curve cites re-anchored at FOUR sites `{3,20,170,190}` with the dossier's two-target split (row 21); intersect cites at BOTH sites `{5,388}` (row 8); `index.md:27` upward using dies (row 3); the phantom namespace's three sites die IN WAVE 1 (the improve pre-motions — the E3 wave defect the judges flagged is closed); 12-reference CSPROJ + float-lane law stand. |
| E4 | flatten Dirichlet + dense LSCM (row 16); naming/reconciliation mis-key via `Self` (rows 4-5); receipts `Converged` (row 13); decimate boundary freeze + O(F²) (row 15). All five confirmed bugs die in rebuilds, not patches. |
| E5 | Namespace ratification `[01]`; dual paradigm dies corpus-wide: every robust-core entry adopts `Op? key = null` threading, `IValidityEvidence` registration, `validation.md` admission vocabulary, with `GeometryFault` staying the fault family (two-family seam stated both sides). |
| E6 | Row 3: upward using + Compute-type return die; node-array wire composed per `ARCH:86`; `COMPUTE:120` deleted. Zero upward edges remain corpus-wide. |
| E7 | Collapse landed by consumers (rows 8-10, 14); medial real (rows 7, 10); fit λ-ladder composition (row 12); arrangement implicit case REAL (row 9 — exact path, not re-scope). |
| E8 | Fabrication duplicates die by counterpart edits: `FAB:33` (view), `FAB:22` (offset/skeleton), `FAB:23/48` (slice). Kernel owner + downstream consumption through declared seams. |
| E9 | LM ×2 → one functor (rows 11-12); GWN ×2 → index owns accelerated query, reconstruct/fields compose (row 3); `BooleanReceipt` ×2 → ONE composed type (rows 9, 13); soup ×3 → edit.md (row 6); dense-LA two rules → one `matrix.md` law (rows 11-16); kd-tree ×2 (dossier beyond-register) → PointCloud excision (row 3). |
| E10 | index throws + silent empty + bounds narrowing → `Fin` + typed `KindMismatch` (row 3); repair cast (row 14); decimate `(QueryResult.Nearest)` cast — dossier beyond-register (row 15); intersect's three casts — dossier beyond-register (row 8). All route `Fin` + typed fault. |
| E11 | BspNode apparatus dies (row 27); `FitStore` deleted + dead Consensus BVH dies (row 12); `ChartStore` carriers deleted + `FactorNonZeros` fixed (row 16); pack false-Half → real strided store (row 28); index counts resync (row 3); offset store prose + 2n sizing (row 10); dead `ImplicitCrossing` carrier dies (row 9); dead `ViewKind` columns + BSP free-list (row 27); dead `IntersectStore.Next` wired-or-deleted (row 8). |
| E12 | Ledger conformance per `[04]`: index wire composed, reconciliation ONE anchor, pack rows preserved, `COMPUTE:120/121` settled, Parametric fan rows enter `[02]-[SEAMS]` (curve.md:~192 prose becomes rows), intersect→Fabrication anchor split resolved with V10, the offset→arrangement row added. |
| E13 | api-hashing:82 rewritten to `XxHash128.HashToUInt128` seed-zero federation truth (wave 1); csparse deduped → shared (kernel copy deleted); tensors RECLASSIFIED overlay-pair KEEP (dossier refuted the dupe claim — the kernel superset is load-bearing); mathnet overlays verified proper subsets, re-verified after the 5.0.0 downgrade; LanguageExt catalog VERIFIED LANDED (dossier: already authored, shared tier = 31 — verify, never re-author). |
| E14 | All 13 version facts feed-verified HOLD; the sweep WIDENS to the 4th beta pin `MathNet.Numerics.FSharp` at `PROPS:467` (dossier drift row); GShark dormancy is the R1 vendor trigger; ManifoldNET stall + `Manifold` homonym recorded; WATCH re-key per `[09]`. |
| E15 | faults 10 stringly cases re-typed + 8 mints, the COMPLETE signature-locked payload set at `[01]`; delaunay seven stub bodies transcribed (row 7 — the `[03]` transcription target). |
| E16 | Index-doc ledger: `README:42`/`ARCH:42` implicit-point + BooleanOp cell welds LAND (rows 7-9), native-gate claim re-scoped to the V5 meaning; `README:99` Voronoi dual LANDS (row 7) + `PlanarOverlay` fill LANDS (rows 21, 27); `README:65` RE-SCOPES to analytic QI (row 27; `ARCH:63` accurate — E16 pairing drift recorded); `README:86` ddouble rows LAND (rows 11, 15) + PeterO interval filter LANDS (row 1); `README:93` kd-tree feeds fit LANDS (row 12); `ARCH:112` faults seam statement LANDS (row 2). |

## [07]-[DELTA_DISPOSITIONS] — `[03]-[CAPABILITY_ESCALATION]`, all 17 rows

| Plane (now→target) | Homed at |
|---|---|
| Numerics/predicates (9→9.5) | Row 1 — RID/strict-IEEE invariant, precise rationale, FMA-free row, PeterO interval filter, Compute-consumable export seam; PLUS the R2 re-founding (exceeds the floor). |
| Numerics/faults (7.5→9.5) | Row 2 — 10 re-types + 8 mints signature-locked, two-family seam, band plan, taxonomy = `[01]` partition. |
| Parametric tier (3→9.5) | Rows 5, 20-26 — every delta item incl. canonical-bytes identity, SSI disposition, Offset promotion; vendored engine exceeds the floor (fundamental forms, surface fitting, real normal-offset NURBS); ONE namespace, ONE polymorphic nurbs admission. |
| Spatial/index (7→9.5) | Row 3 — PointCloud lift, zero throws, typed `KindMismatch`, outward bounds, wire composed via ONE `Apply`, GWN single-owner, counts resynced. |
| Spatial/naming+reconciliation (6.5→9.5) | Rows 4-5 — Self, Subsumes, namespaces, injectivity, fixtures harness-gated, `Encode`+identity, parametric bytes on the one seam; ONE-`Apply` entries. |
| Meshing/delaunay (6→9.5) | Row 7 — seven stubs fenced, hull ruling, Voronoi accessor, honest rationale; PLUS hybrid vertex table + constrained mode; Arrangement-namespace home. |
| Meshing/intersect (8.5→9.5) | Row 8 — collapse by consumers, 2D restriction, slice producer seam, re-anchor; PLUS the chain re-found (the dossier's revise-down defect closed). |
| Meshing/arrangement (7.5→9.5) | Row 9 — implicit `Constraint` (landed, not re-scoped), BooleanOp home (W2 donor excision), ONE BooleanReceipt, Manifold named + finite ceiling 1_000_000, `NativeAssetMissing` 2423, cycle broken (weld sited wave 1). |
| Meshing/offset+toolpath (5→9.5) | Row 10 — medial real, the MINTED shared clearance family, corner rows, store sizing, prose resync; PLUS Minkowski completed + `PlanarOverlayResolve` fixed + the arrangement seam row declared. |
| Meshing substrate NEW (0→9.5) | Row 6 — arena page, two-carrier seam, soup ×3 collapsed, mutability+concurrency contract, weld primitive sited, total-arena namespace. |
| Processing/solver+fit (8→9.5) | Rows 11-12 — one generalized LM, fit instantiates, λ threaded, FitStore resolved, Line chart fixed, `matrix.md` law, ddouble Σd², MLESAC via neighbors. |
| Processing/repair+receipts (7→9.5) | Rows 13-14 — real CDT re-mesh, Converged fixed, MeshEdit extracted W1, threading, ONE BooleanReceipt composed, triple projection kept + ONE Genus-tolerant `ProjectionRow`. |
| Processing/decimate (7→9.5) | Row 15 — incidence index O(F log F), boundary collapse, ddouble fold, meshoptimizer split recorded; PLUS real flip guard + phantom kill (dossier beyond-register). |
| Processing/flatten (6→9.5) | Row 16 — correct Dirichlet, sparse LSCM via `matrix.md`, O(F) ARAP, true fill, dead carriers, develop seam, `spectral.md:191` verified. |
| Drawing/view (4→9.5) | Row 27 — real analytic engine, connectivity chaining, select boundary, Fabrication consumes. |
| Drawing/pack (7→9.5) | Row 28 — real dtype-strided residency, phantom dead W1, compositions preserved, field/toolpath rows. |
| Slice/skeleton/hull/remesh NEW (0→9.5) | Rows 17-19 + row 7 hull projection — oriented contours + typed open sections + all four layer policies, MCF skeleton over landed machinery composing the ONE clearance family, two-tier hull ruling, isotropic + quad remesh beside the host capture tier. |

## [08]-[CONSUMER_GATE_HOMING] — both gates fully homed (per-item tables, spec-anchored)

### Generation KERNEL GATE (`RASM-GENERATION-SPEC.md:179,226,164,138,173`)

| Gate item | Owning page (row) | Seam |
|---|---|---|
| SpineRef content-key resolution (`:164`) | reconciliation `EncodeOp.Parametric` (row 5) fed by nurbs/curve/surface canonical bytes | the ONE identity chain: raw-array projection → landed `identity.md` `ContentHash.Of` (`ARCH:76-81`); a COMPONENT of the Element `ToCanonicalBytes` fold (`Rasm.Persistence/Element/codec.md:151-152`), never a sibling key; Persistence mints the key |
| arc-length stations + RMF frames over `[T0,T1]` (`:138,:173`) | `curve.md` `Stations` case (row 21) | `(station, frame)` pairs → `PathRow`/`Placement`; NEW Parametric fan row in `ARCH:[02]`, wave 4 |
| surface isolines | `surface.md` `Isolines` (row 22) | isoline families over vendored knot refinement + `IsoCurve` rows |
| surface geodesics | `surface.md` `Geodesics` (row 22) | landed `fields.md` `Geodesic` + `geodesics.md` heat/MMP through the UV pullback (results return to the parametric domain) |
| curve offset / surface normal-offset | `curve.md` `Offset` (row 21) / `surface.md` `NormalOffset` (row 22) | vendored seed + `[V4]`-lattice trim; Greville offset + Piegl-Tiller refit (G5 strong branch) |
| region tessellation + subdivision | `delaunay.md` (row 7) + `subdivide.md` (row 23) + `surface.md` `Tessellate` | exact CDT + stencil-row schemes; Generation fan rows |
| developable | `develop.md` (row 24) | isometry witness receipt; Fabrication unroll shares the owner |
| panelization | `panelize.md` (row 25) | `int symmetry` data-arg over landed cross-fields + the remesh substrate |
| pattern-to-surface with per-instance frames | `patternmap.md` (row 26) | instance stream with vector-heat-transported frames — the `[05]`a dry-run reads them |
| SSI disposition | `curve.md`/`surface.md` charter row (rows 21-22) | HOST-DEFERRED TRIPLE (SSI + surface-plane + curve-surface, probe-widened); boundary stated beside landed `relations.md` |
| G1 ingress hinge (key → live surface) | `nurbs.md` `Nurbs.Of(NurbsWire)` (row 20) | public arbitrary-knot construction — SpineRef surface resolution lives |

### Fabrication toolpath gate (`Rasm.Fabrication/ARCHITECTURE.md` + `RASM-CS-FABRICATION-BRIEF [V5]/[V6]`)

| Demand | Owning page (row) | Seam |
|---|---|---|
| corner-strategy offsets (JoinType×EndType) | `offset.md` rows (row 10) | kernel exact skeleton + corner rows through the seam; CavalierContours kerf stays Fabrication-stratum (`README:99`) |
| slice stacks (oriented contours + typed open chains + nesting forest + open layer-policy family) | `slice.md` (row 17) | `FAB:48` re-route resolves; `FAB:23` `Slicing.cs` dies; all four layer policies seeded; Compute circulation the second consumer |
| 2D medial with per-point clearance radius | `offset.md` `Medial` + `Clearance(probe)` (row 10) | `FAB:22` payload mandate — the ONE clearance result family |
| 3D MCF curve-skeleton with the same payload | `skeleton.md` (row 18) | composes the SAME clearance family; `FAB:22` `Toolpath/Skeleton.cs` dies unconditionally |
| arbitrary-probe clearance query | both (rows 10, 18) | one vocabulary, 2D/3D; `RASM-CS-FABRICATION [V5]` weighted/variable-speed rows ride `Weighted` |
| manufacturing-tolerance booleans | `arrangement.md` (row 9) | managed exact + Manifold tier-3 behind `ScaleCeiling` 1_000_000; `FAB:44` re-anchor |
| developable/unroll with isometry witness | `develop.md` (row 24) | witness receipt; dry-run (b) |
| stock-envelope hull (V11 two-tier) | `delaunay.md` `LowerHull` (row 7) + landed `cloud.md` rail | tier boundary declared on both pages, one anchor each |
| distance-field read (V8 one lane) | landed `fields.md` `SignedDistanceFromMesh` → landed `reconstruct.md` GWN → `index.md` BVH winding (row 3) | the ONE distance-field lane; the kernel mints no second SDF owner (PicoGK stays Fabrication's voxel lane) |
| zero-sampling plan/section HLR | `view.md` `DrawingProjection` (row 27) + `intersect.md` Section (row 8) | `FAB:33` dies; `FAB:46/47` re-anchor; documentation dry-run (c) zero sampling misses by construction |

All four `[05]` acceptance dry-runs compose from these owners plus landed fences; none reaches for a missing owner.

## [09]-[ROSTER_AND_API] — deltas with `.api` obligations

| Motion | Detail | Leg |
|---|---|---|
| MathNet downgrade | `MathNet.Numerics` + `.Providers.OpenBLAS` + `.FSharp` (+ `.MKL` for co-consumers) `PROPS:62-64,467` → `5.0.0` stable (feed-proven coherent — providers have same-day 5.0.0); beta survives only on a recorded 6.0-only fence member found by the WIDENED survival sweep (landed kernel fences + robust-core rails + Compute/Fabrication co-consumers); both mathnet overlay catalogs re-verify against the downgraded assembly | 1 |
| OpenBLAS DROP | Evidence-backed (no arm64 asset exists — nuget cache lib/-only, catalog self-admits): kernel reference `Rasm.csproj:11` dies; pin `PROPS:64` dies unless a sibling csproj references it; kernel `api-mathnet-providers.md` retires; `matrix.md` provider prose re-scopes managed-only (landed 1-hop ripple) | 1 |
| Fabrication-lane re-group | `CavalierContours:70`/`Clipper2:71`/`SharpVoronoiLib:75` out of the mislabeled "Kernel Geometry" group into a `Fabrication` label; `geometry3Sharp:72` disposition routes to consuming campaigns; `LibTessDotNet` stays retired outright (no pin, no catalog); post-motion the kernel group holds GShark(until wave 4)/MIConvexHull/Supercluster; the standing `Rasm.Fabrication/.api` copies serve as the folder-tier catalogs | 1 |
| Manifold ADD | In-house `manifoldc` P/Invoke (elalish/manifold, Apache-2.0): NO NuGet pin (native asset RIDs recorded at admission); `api-manifold.md` authored kernel-tier WITH the admission, spelling `manifoldc` and NAME-AND-REJECTING the `Manifold` NuGet ID homonym (CLI/MCP source-gen framework) + `ManifoldNET` (stalled 1.0.7-alpha) | 1 (catalog), 2 (gate composes) |
| HighPerformance ADMIT | `CommunityToolkit.HighPerformance` kernel reference added with edit.md's arena composition (`Span2D`/`MemoryOwner<T>`/`ParallelHelper` on frozen/partition-disjoint spans only); shared catalog suffices (multi-stratum); the W1 leg deepens the stub on first composition | 1 |
| GShark VENDOR | Wave 1: the six probe catalog corrections applied (dead repo pointer :31 → `GSharker/G-Shark`; `AdaptiveSample`/`AdaptiveSampleRange` public; `KnotVector.Refine` public both carriers; surface `Weights`/`ControlPointLocations` recorded; `DivideByChordLength` returns `List<double>`; wire law :175 READ-only noted) — the catalog stays truthful while settled pages cite it. Wave 4: pin + `Rasm.csproj:18` + kernel `api-gshark.md` DELETE with `nurbs.md` landing (one-engine law — no package beside the vendor) | 1, 4 |
| Catalog hygiene | api-hashing:82 rewritten (`XxHash128.HashToUInt128` seed-zero → `UInt128`; `XxHash3` at most a fast-root example, never the ContentHash seed); api-csparse deduped → shared, kernel copy deleted; api-tensors KEPT both tiers (overlay pair — kernel superset with the `IsFiniteAll` validity rows is load-bearing); api-rhino:134 VERIFIED corrected (refuted on disk); api-languageext VERIFIED landed (never re-author); shared tier census 31 recorded | 1 |
| WATCH re-key | geometry4Sharp trigger re-keyed to a post-2022 release signal (1.0.0 on NuGet since 2022-11 — dead trigger); CSparse.Extensions unpublished, TVGL 2019, Kemsekov 3.1.2 2024-02 recorded | 1 |
| Judged shared-tier stubs | RECORDED no-mine (so silence is never mistaken for oversight): `Generator.Equals` — Thinktecture owns kernel value identity; the structured-diff surface is the seam's. `Riok.Mapperly` — kernel wires are columnar span transcription, never object mapping. `UnitsNet` — kernel angles stay raw radian `double`; `Angle` is the AppUi↔kernel boundary bridge. The transport/app set (grpc ×7, protobuf, nodatime ×3, messagepack + thinktecture adapters, hybrid-cache, jsonpatch, redaction, system-configuration, extensions-ai, unicolour) carries no geometry-plane member — zero mine obligation | — |
| Stratum-split records | RECORDED (never mistaken for kernel candidates or dead weight): `Alimer.Bindings.MeshOptimizer` = Compute's; PicoGK (Apache-2.0, active) = Fabrication's voxel/implicit lane — never the kernel's second SDF owner (the V8 one-lane ruling) and never its slicer; `OcctNet.Wrapper` = Fabrication's own pin (the OCCT rejection is KERNEL-scoped) | — |
| Reserves + rejections | DoubleDouble special-function surface + BigRational `Mediant` stay recorded no-consumer reserves; TensorPrimitives non-exact paths only, span-feed law stands (`api-tensors.md:248`); QuikGraph bounded-lane law generalizes (kernel graph RESULTS emit SoA wires: skeleton topology, slice forests, naming migration); named-and-rejected list stands verbatim (SISL, TetGen, CGAL, SolveSpace, CuraEngine/Slic3r, Instant Meshes/NeurCross, MCUT, OCCT/LNLib, MeshLib, rhino3dm-as-algorithm-owner, SkeletonNet/CGAL_StraightSkeleton_Dotnet, OpenSubdiv-until-proven, V-HACD/CoACD-deferred) + the `Manifold` NuGet ID homonym added | — |

## [10]-[LEG_PARTITION] — acyclic, engine-invocation rows

Every edge lands equal-or-earlier wave; intra-wave edges point backward in listed order (the page-set tables ARE dependency order). Proof: wave-1 pages compose only landed floors + each other backward (index→faults ✓, reconciliation→naming ✓, edit→predicates ✓); wave-2 order delaunay→intersect→arrangement→offset has arrangement→{delaunay,intersect} backward and offset→{delaunay,intersect,arrangement} backward (the grafted offset→arrangement row is backward by this order); wave-3 order solver→fit→receipts→repair→decimate→flatten has fit→solver backward, repair→receipts backward, receipts→arrangement into W2, all other edges into waves 1-2 or landed; wave-4 order slice→skeleton→remesh→nurbs→curve→surface→subdivide→develop→panelize→patternmap→view→pack has curve→nurbs, surface→nurbs, develop→{surface, flatten(w3)}, panelize→{remesh, surface}, patternmap→surface all backward, skeleton→offset into W2 (the clearance family), view/pack reaching only waves 1-2. Zero upward strata edges (the `index.md:27` inversion dies in wave 1); zero page cycles (repair⇄arrangement broken by the V5 re-home + W1 weld siting in edit.md + the W2 donor excision; the repair/receipts pair is ONE direction — repair composes the receipts vocabulary, receipts listed first); the delaunay←repair edge is w3→w2 backward.

| Leg | Engine invocation | Targets (dependency order) + riders |
|---|---|---|
| 1 | `Workflow(rebuild.js, {targets: [Numerics/predicates.md, Numerics/faults.md, Spatial/index.md, Spatial/naming.md, Spatial/reconciliation.md, Meshing/edit.md, Processing/repair.md, Drawing/pack.md, Processing/solver.md], brief: DECISION})` — kinds: rebuild ×5, new ×1 (edit), improve ×3 (repair = MeshEdit donor excision `:76-102` ONLY; pack = phantom kill `:33`/`:18` ONLY; solver = phantom kill `:13` ONLY) | + the `[09]` wave-1 roster motion; V1 ratification sweep (ARCH `[01]/[02]/[03]` partition replaces the placeholder; README router; faults taxonomy; ratified Drawing charter recorded; inbound-reference sweep); GWN seam declaration 1-hop ripples on `reconstruct.md`/`fields.md`; `COMPUTE:121` + Persistence:55 + Compute-CDTet counterpart edits; fixtures (`0x9462…`, `CLASH_GOLDEN`) harness-gated |
| 2 | `Workflow(rebuild.js, {targets: [Meshing/delaunay.md, Meshing/intersect.md, Meshing/arrangement.md, Meshing/offset.md, Processing/repair.md], brief: DECISION})` — rebuild ×4, improve ×1 (repair = `BooleanOp` `:57-62` + `BooleanArrangement` `:288-292` excision ONLY) | + `FAB:44` ripple; Manifold gate composes; `README:42`/`ARCH:42`/`README:99` rows become fence-true; cloud.md hull-tier boundary anchor |
| 3 | `Workflow(rebuild.js, {targets: [Solving/solver.md, Solving/fit.md, Processing/receipts.md, Processing/repair.md, Processing/decimate.md, Processing/flatten.md], brief: DECISION})` — new ×2 with absorb {into: Solving/solver.md, from: Processing/solver.md}, {into: Solving/fit.md, from: Processing/fit.md} + deletePages [Processing/solver.md, Processing/fit.md]; rebuild ×4 | + `Solving/` mint + inbound sweep; `COMPUTE:120` ripple; `mesh.md` Genus-tolerant `ProjectionRow` Growth extension; `README:86/93` rows become fence-true |
| 4 | `Workflow(rebuild.js, {targets: [Meshing/slice.md, Meshing/skeleton.md, Processing/remesh.md, Parametric/nurbs.md, Parametric/curve.md, Parametric/surface.md, Parametric/subdivide.md, Parametric/develop.md, Parametric/panelize.md, Parametric/patternmap.md, Drawing/view.md, Drawing/pack.md], brief: DECISION})` — new ×8, rebuild ×4 (curve, view, pack + none other) | + GShark pin/reference/catalog deletion; `FAB:22/23/33/46/47/48` ripples; AppHost signature-lock verify; Parametric fan + slice/skeleton seam rows enter `ARCH:[02]`; `README:65` re-scope; the four `[05]` acceptance dry-runs compose |

Residuals resolve per leg before the next launches; docs close in the leg that changes their facts; residuals naming later-leg pages transfer forward explicitly.

## [11]-[CARD_AND_FIXTURE_DISPOSITIONS]

- `[T-BOOLEAN-NATIVE-ASSET]-[BLOCKED]` — DISPOSED into V5: `HealOp.Boolean` delegates `Arrangement.Apply(MeshBoolean, …)`; `NativeAssetMissing` 2423 reserved for the tier-3 scale route; Manifold gate = finite `ScaleCeiling` 1_000_000 + golden-boolean fixture + RID-asset resolution; card closes with leg 2.
- `[HOST_BOUNDARY_REENTRY]-[BLOCKED]` — realization-phase law, OUT of scope, NOT disposed here.
- `[WATCH_PACKAGES]` — re-keyed per `[09]`, leg 1.
- Frozen fixtures — `reconciliation.md:147` digest and `index.md:696` `CLASH_GOLDEN` marked harness-confirmed-once (`XxHash128`/`NodeLinkProjection` reproof) before any page cites them REAL; leg-1 gate.
- Growth rows seeded for the idea pool: `InCircle` implicit combos {IIEE..IIII} + 3D `orient3d`/`inSphere` multi-implicit family (CDTet-gated); exact circumcenter side-of row (medial trim); OpenSubdiv P/Invoke (limit-eval-at-scale-gated); V-HACD/CoACD convex decomposition (consumer-gated); weighted/variable-speed wavefront rows (`RASM-CS-FABRICATION [V5]` riders); quad-remesh growth absorbed (landed row 19).

## [12]-[GRAFT_LEDGER_AND_RECORDED_CHOICES]

Grafts applied onto the floor-maximalist base (source draft · judges naming it):

1. W1/W2 surgical `improve` pre-motions — pack.md/solver.md phantom kills W1, repair donor excisions W1 (arena) + W2 (boolean) — roster-mining · law-gates + consumer-reach. Closes the E3 wave defect and the two-owner donor transient; brief `[05]` leg-1 "phantom-import kill" now executes in leg 1.
2. Typed `KindMismatch` 2402 mint for the V8 Fin-routed kind mismatch — folder-architecture-first · law-gates + consumer-reach.
3. Drawing-rename clause resolved EXPLICITLY: name KEPT, charter ratified, departure recorded with the judge-scored rationale — all three judges' disjunctive graft; the rename-churn branch was itself scored a defect by the law-gates judge.
4. offset→arrangement seam row added (`PlanarOverlayResolve`; backward intra-wave) — law-gates + consumer-reach (self-fix).
5. Engine-lowering law verbatim (donor excision charter-carried, never absorb-encoded; `.api` deletions ride the roster motion, not deletePages) — folder-architecture-first · law-gates (+ consumer-reach by mechanism).
6. Consumer-gate homing as per-item TABLES with spec anchors — salvaged FORMAT from the disqualified consumer-gate-first · law-gates + consumer-reach.
7. Bijective partition collapse: 16 namespaces, 13 fault-routing ↔ 13 clusters exactly covering 2400-2449; delaunay unified into `Rasm.Geometry.Arrangement`; ONE `Rasm.Geometry.Parametric` namespace over the 7-page tier (Patterning/Development namespaces die); edit total under `Rasm.Geometry.Meshing` (ArenaFault dies); remesh re-sited `Processing/remesh.md` under the widened `Rasm.Geometry.Simplification`; `NativeAssetMissing` 2423 in the boolean owner's stride — folder-architecture-first · consumer-reach (mandated) + partition-elegance (its winning discipline); law-gates permits with the choice RECORDED (done in `[01]` — the sub-band alternative was legal, not re-litigated).
8. Roster-record tail: judged shared-tier no-mine rows, stratum-split records, transport/app zero-obligation — roster-mining `[06]` · law-gates; disk-checked against brief `[04]:212-214`.
9. Shared 2D/3D clearance vocabulary: ONE result family minted in offset.md, composed by skeleton.md — roster-mining rows 17/19 · law-gates; disk-checked (today's offset.md carries zero clearance surface — genuinely new, single owner).
10. `Nurbs.Of(NurbsWire)` single polymorphic admission as nurbs.md's entry — folder-architecture-first · consumer-reach; the ONE-entry law generalized to the substrate admission.
11. ONE-`Apply` entry collapse on the Spatial trio (`Spatial.Apply`/`Naming.Apply`/`Reconciliation.Apply`) — roster-mining rows 3-5 · partition-elegance; disk-checked; kills the base's `Build`/`Query`/`ToAcceleration` + `Encode`/`Reconcile` multi-static surfaces.
12. One-shape `BooleanReceipt`: receipts' boolean case CARRIES arrangement's type as payload; the base's `HealBooleanReceipt` rename dies — roster-mining/folder-architecture-first · partition-elegance; the denser collapse under the no-proliferation law; receipts→arrangement backward edge declared.
13. Fault-payload signature set COMPLETED (the base's 10 re-types + the 8 new-mint signatures, incl. the stage-discriminant parametric pair) — roster-mining `[02]` · partition-elegance; the faults.md rebuild is signature-locked, not deferred.

Verified already in the base (no action): GShark two-stage manifest staging (W1 corrections / W4 deletion); concrete `ScaleCeiling` 1_000_000; leg-3 receipts-before-repair order with the one-direction seam mirror; `NativeAssetMissing` re-code into the arrangement stride.

Named-and-rejected structural alternative (recorded so the overturn auditor sees it weighed): flatten→`Parametric/` move (consumer-gate-first salvage candidate) — REJECTED: it inflates blast radius onto the landed flatten→Fabrication/AppUi seam rows for zero capability gain and breaks the develop→flatten W3 wave logic; two judges rejected or gated it on a full inbound-sweep costing nobody performed.

## [13]-[SELF_REPORT]

Page rows: 28 (16 REBUILD, 2 MOVE with delete-with-absorb, 10 NEW; deletePages ×2; improve pre-motions ×4). Verdicts disposed: 14/14. Evidence rows disposed: 16/16. `[03]` deltas disposed: 17/17. Probe rulings: 2/2 (GShark vendor-and-own NOW; implicit-point exact path LANDS). Consumer gates: both fully homed as tables (`[08]` — Generation 11/11 items, Fabrication 10/10 demands). Fault partition: 13 clusters ↔ 13 fault-routing namespaces, bijective, exactly 2400-2449; 16 namespaces total, none spanning folders; complete payload signature set locked. Strata: zero upward edges; zero page cycles; leg partition proven acyclic at `[10]` with every intra-wave edge backward in listed order. Roster: every brief `[04]` row disposed incl. the no-mine and stratum-split record tail. Recorded departures: one (the Drawing folder-name keep, judge-scored, `[01]`/`[12]`).
