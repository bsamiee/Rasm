# REVIEW F05: 05-sequences-and-linq.md

Verified with no finding: every named member exists with the stated signature and semantics
(`Seq`/`Iterable`, `AsIterable`/`ToSeq`, indexed `Map`, `At(Index) : Option<A>`, `Order()` with `toSeq`,
`Zip` with `Tail`, `Exists`/`ForAll`, `parseInt`, `Traverse` to `Option<Seq<Story>>`,
`Map<K, V>.AddOrUpdate(K, Func<V, V>, V)`, `Map<K, V>.ToSeq()` in key order). Probe run this session:
`Seq(1, 2, 3).Fold(Seq<int>(), (acc, x) => acc.Add(x))` is `[1, 2, 3]`, `FoldBack` of the same is `[3, 2, 1]`,
`Seq("a","b","c").Fold("", (acc, x) => acc + x)` is `abc`, `Map<int,int>().AddOrUpdate(1, v => v + 100, 5)`
is `[(1, 5)]` and a second call is `[(1, 105)]` — the `Fold`/`FoldBack` diagram and the grouping fold are right.
Every `csharp` block matches `F05/Domain.cs`, `F05/Sequences.cs` verbatim (checked block by block; the
`Story` record comes from `Domain.cs`). `.scratch/lab-f05` rebuilt: 0 warnings, 0 errors, run exit 0.
No contradiction with `24-languageext-collections-and-shared-state.md` on `Fold`, `Scan`, `At`, or `Iterable`.

1. Location: line 166.
   Claim: "The seed defines the empty-input result and may have a different type from the elements."
   Evidence: BRIEF prose rule bans the hedges "may", "might", "could", "should" in added or adjusted text.
   This sentence has no counterpart in the book chapter, so it is added prose. It is the only hedge in the file.
   Correction: replace the clause with —
   `The seed defines the empty-input result, and its type is independent of the element type.`

2. Location: line 174, heading `## Non-mutating updates to lazy sequences`.
   Claim: "Non-mutating updates to lazy sequences"
   Evidence: the section body operates on `Seq<A>` only, and line 14 of the same file states "`Seq<A>` is the
   strict sequence type". File 24 states "`Seq<A>` is strict". The heading survives from the pre-LanguageExt
   text, where the sequence type was `IEnumerable<T>`, and is now false.
   Correction: `## Non-mutating updates`

3. Location: line 186.
   Claim: "Private setters and read-only interfaces do not make the resulting object graph deeply immutable."
   Evidence: the book chapter (`painter-functional-csharp/chpater-2.md`, line 159) carries this material as its
   own section, `## Immutability is deeper than private setters`. In the doc the sentence sits at the end of the
   `ReplaceAt` section, which is about a replacement over `Seq<A>`; the paragraph before it does not lead to it.
   BRIEF forbids merging sections. Nothing about LanguageExt made the heading false.
   Correction: promote the sentence back to its own section, placed where it now stands —
   ```
   ## Immutability is deeper than private setters

   Private setters and read-only interfaces do not make the resulting object graph deeply immutable.
   ```

4. Location: line 170, heading `## Recursion` and the single sentence under it.
   Claim: "## Recursion" / "A recursive iteration can model an early stop without mutable locals, but C# does not
   guarantee tail-call optimization, so recursion carries a real stack cost."
   Evidence: the book heading is `## Recursion is expressive but stack-sensitive` (chapter line 138). Every other
   heading in the doc is the book heading word for word, so this one was shortened by the edit. The book also
   states the base condition and the recursive step, and states that a complete implementation needs an exhaustion
   case. LanguageExt supplies no reason to drop either fact: `Trampoline<A>` and `Monad.recur` replace the deep
   recursion, not the definition of recursion. The book's `FirstPositionAtZero` block reads a `deltas` field that
   the fragment never declares, so removing the block is right; the prose around it is not.
   Correction: restore the heading and the two dropped facts —
   ```
   ## Recursion is expressive but stack-sensitive

   A recursive iteration has a base condition that returns the answer and a recursive step that calls the function
   with updated state. It models an early stop without mutable locals. A complete implementation also needs an
   exhaustion case, because an unreachable base condition recurses without end. C# does not guarantee tail-call
   optimization, so recursion carries a real stack cost.
   ```

5. Location: line 214 and the `Stories` block.
   Claim: "One failed parse makes the whole input `None`."
   Evidence: `Stories` splits on `Environment.NewLine` and traverses. `"".Split(',')` is one empty field, and
   `parseInt("")` is `None`, so a text that ends with a newline makes `Report` return `None`. A caller that reads
   a file and passes the text straight in hits this on the first try. The statement is true but the reader has no
   way to see which input triggers it, and the notes name only a real CSV reader.
   Correction: append one sentence after the quoted one —
   ```
   An empty line parses to `None`, so the text carries no trailing newline.
   ```

Blockers: none.
