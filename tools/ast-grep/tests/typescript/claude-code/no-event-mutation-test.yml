id: no-event-mutation
valid:
    - 'on("tool.call", ($, event, next) => { const seen = []; seen.push(event); return next(event); });'
    - "on('tool.call', ($, e, next) => next({ ...e, command: restore(e.command) }));"
    - |
      on('prompt.submit', ($, e, next) => {
          const copy = { ...e, text: redact(e.text) };
          copy.text = trim(copy.text);
          return next(copy);
      });
    - |
      on('tool.call', ($, e, next) => {
          const row = { command: e.command };
          row.command = restore(row.command);
          return next({ ...e, ...row });
      });
    - "on('tool.call', ($, e, next) => next({ ...e, context: [e.tool] }));"
    - "on('tool.call', ($, e, next) => next(Object.assign({}, e, { command: 'ls' })));"
    - |
      on('tool.call', ($, e, next) => {
          const copy = { ...e };
          delete copy.tool_use_id;
          return next(copy);
      });
    - |
      const touch = (e) => {
          e.command = 'ls';
          return e;
      };
    - |
      on('tool.call', ($, e, next) => {
          const rows = list.map((e) => {
              e.count += 1;
              return e;
          });
          return next({ ...e, context: rows });
      });
    - |
      on('tool.call', ($, e, next) => {
          const rows = list.map(function (e) {
              e.count += 1;
              return e;
          });
          return next({ ...e, context: rows });
      });
    - |
      on('tool.call', ($, e, next) => {
          function bump(e) {
              e.count += 1;
              return e;
          }
          return next({ ...e, context: [bump(row)] });
      });
    - |
      on('tool.call', ($, e, next) => {
          const actions = {
              bump(e) {
                  e.count += 1;
              },
          };
          return next({ ...e, context: [actions] });
      });
    - |
      on('tool.call', ($, e, next) => {
          const rows = list.map(function* (e) {
              e.count += 1;
              yield e;
          });
          return next({ ...e, context: rows });
      });
    - "on('tool.call', ($, e, next) => next(Object.assign({}, e)));"
    - |
      on('tool.call', ($, e, next) => {
          Object.freeze(e);
          return next(e);
      });
    - |
      register('tool.call', ($, e, next) => {
          e.command = 'ls';
          return next(e);
      });
    - "on('tool.call', ($, e, next) => next({ ...e, context: e.context.concat(['line']) }));"
    - "on('tool.call', ($, e, next) => next({ ...e, context: [String(!e.tool)] }));"
    - "on('tool.call', ($, e, next) => next({ ...e, context: e.context.map(trim) }));"
    - |
      on('tool.call', async ($, e, next) => {
          try {
              return await next(e);
          } catch (e) {
              e.handled = true;
              throw e;
          }
      });
    - |
      on('tool.call', ($, e, next) => {
          [command = e.command] = parts;
          return next({ ...e, command });
      });
    - |
      on('tool.call', ($, e, next) => {
          [command = trim(e.command)] = parts;
          return next({ ...e, command });
      });
    - |
      on('tool.call', ($, e, next) => {
          for (const e of rows) {
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          for (let e = first; e !== null; e = e.next) {
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          if (ready) {
              const e = copy(row);
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => next(Object.assign({ ...e }, patch)));
    - |
      on('tool.call', ($, e, next) => {
          rows.map(({ e }) => {
              e.count += 1;
              return e;
          });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e = { ...e, command: 'ls' };
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => next({ ...e, context: [Reflect.get(e, 'tool')] }));
    - |
      on('tool.call', ($, e, next) => {
          const bump = (e = row) => {
              e.count += 1;
          };
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          for (const [e] of pairs) {
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          if (ready) {
              var e = copy(row);
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.defineProperty(row, 'event', { value: e });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          for (const { e } of rows) {
              e.count += 1;
          }
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          for (const { row: e } of rows) {
              e.count += 1;
          }
          return next(e);
      });
    - 'on("tool.call", ($, e, next) => { rows.map(e => { e.count++; return e; }); return next(e); });'
    - 'on("tool.call", ($, e, next) => { { const { e } = row; e.count++; } return next(e); });'
    - 'function first() { const hook = ($,e,next)=>{e.count++; return next(e)}; return hook; } function second(on: On) { const hook = ($,e,next)=>next(e); on("tool.call",hook); }'
    - 'on("tool.call", ($, e, next) => { e = {...e}; e.count++; return next(e); });'
    - 'on("tool.call", ($, e, next) => { e &&= {...e}; e.count++; return next(e); });'
    - 'on("tool.call", ($, e, next) => { e = {...e}; if (ready) { e.count++; } return next(e); });'
    - 'const hook = ($, e, next) => next(e); { const hook = ($, e, next) => { e.count++; return next(e); }; } on("tool.call", hook);'
    - 'on("tool.call", ($, e, next) => { function* bump(e) { e.count++; yield e; } return next(e); });'
    - 'on("tool.call", ($, e, next) => { const apply = (...e) => { e.count++; }; return next(e); });'
    - 'on("tool.call", ($, e, next) => { const apply = ({row: e = value}) => { e.count++; }; return next(e); });'
    - 'on("tool.call", ($, e, next) => { const apply = ({e = value}) => { e.count++; }; return next(e); });'
    - 'on("tool.call", ($, e, next) => { for (let e = row; ready;) { e.count++; } return next(e); });'
    - 'on("tool.call", ($, e, next) => { e = {...e}; for (e.row of rows) { work(); } return next(e); });'
    - 'on("tool.call", ($, e, next) => { e = {...e}; log(e); e.count++; return next(e); });'
invalid:
    - |
      on('tool.call', ($, e, next) => {
          e.command = restore(e.command);
          return next(e);
      });
    - |
      on('prompt.submit', ($, e, next) => {
          e.origin.text += ' redacted';
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e['command'] = 'ls';
          return next(e);
      });
    - |
      on('turn.complete', ($, e, next) => {
          e.count++;
          return next(e);
      });
    - |
      on('turn.complete', ($, e, next) => {
          --e.usage.turns;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          delete e.input.command;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.assign(e, { command: 'ls' });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.assign(e.input, { command: 'ls' });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.defineProperty(e, 'command', { value: 'ls' });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.defineProperties(e.input, { command: { value: 'ls' } });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Object.setPrototypeOf(e, base);
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Reflect.set(e, 'command', 'ls');
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Reflect.deleteProperty(e.input, 'command');
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Reflect.defineProperty(e, 'command', { value: 'ls' });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          Reflect.setPrototypeOf(e, base);
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          if (ready) {
              const e = copy(row);
              log(e);
          }
          e.count += 1;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          const actions = {
              apply(command) {
                  e.command = command;
              },
          };
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e.context.push('line');
          return next(e);
      });
    - |
      on('tool.call', ($, input, next) => {
          input.command = 'ls';
          return next(input);
      });
    - |
      on('tool.call', { tool: 'Bash' }, async ($, e, next) => {
          e.command = 'ls';
          return next(e);
      });
    - |
      on('*', function ($, e, next) {
          e.text = '';
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          const apply = (command) => {
              e.command = command;
          };
          apply('ls');
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          list.forEach(function (row) {
              e.context.push(row);
          });
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          [e.command] = parts;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          ({ command: e.command } = row);
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          [[e.command]] = pairs;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e.context[0] = 'line';
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e.context.length = 0;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          const apply = (command = e.command) => {
              e.command = command;
          };
          apply();
          return next(e);
      });
    - |
      const hook = ($, e, next) => {
          e.command = 'ls';
          return next(e);
      };
      on('tool.call', hook);
    - |
      on('tool.call', ($, e, next) => {
          e.command ??= 'ls';
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          e.input.args.splice(0, 1);
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          [...e.context] = parts;
          return next(e);
      });
    - |
      on('tool.call', ($, e, next) => {
          for (e.command of commands) {
              log(e);
          }
          return next(e);
      });
    - |
      const hook = ($, e, next) => {
          e.command = 'ls';
          return next(e);
      };
      on('tool.call', { tool: 'Bash' }, hook);
    - |
      const arm = (register: On): void => {
          register('tool.call', ($, e, next) => {
              e.command = 'ls';
              return next(e);
          });
      };
    - 'on("tool.call", ($, e, next) => { for (const { other: row = e } of rows) { e.count++; } return next(e); });'
    - 'on("tool.call", ($, e, next) => { const apply = ({ row = e }) => { e.count++; }; return next(e); });'
    - 'function hook($, e, next) { e.count++; return next(e); } on("tool.call", hook);'
    - 'function register(on: On) { const hook = ($, e, next) => { e.count++; return next(e); }; on("tool.call", hook); }'
    - 'on("tool.call", ($, e, next) => { for (e.row of rows) { work(); } return next(e); });'
    - 'on("tool.call", ($, e, next) => { e.count++; e = {...e}; return next(e); });'
    - 'on("tool.call", ($, e, next) => { e ??= {...e}; e.count++; return next(e); });'
    - 'on("tool.call", ($, e, next) => { e ||= {...e}; e.count++; return next(e); });'
    - 'on("tool.call", ($, e, next) => { if (ready) { e = {...e}; } e.count++; return next(e); });'
