# [DOSSIER] Rasm/Geometry — API-Tiers Lane

Read-only survey. Both `.api` catalog tiers COMPLETE + `Directory.Packages.props` + `libs/csharp/Rasm/Rasm.csproj` + all 18 `Geometry/.planning/` pages cross-referenced. Geometry is a **planning-only sub-domain of the `Rasm` kernel package** (no own `.csproj`; `Rasm.csproj` at `libs/csharp/Rasm/` is the owner — Geometry ships under the `Rasm.Geometry.*` namespace tree).

Central axis, stated once: the greenfield robust-core (`ARCHITECTURE.md:3,14` — "admits NO external geometry library", "`Rasm.Geometry.*` exact-predicate kernel") **AUTHORS from first principles over exact predicates** and REJECTS the admitted float geometry packages. The per-package `.api` catalogs were authored to the OPPOSITE intent — every geometry catalog names specific Geometry pages as its consumer ("Meshing/offset routes to CavalierContours", "Meshing/arrangement uses Clipper2", etc.). The pages reject those exact claims. This catalog-vs-page contradiction is systemic and campaign-defining.

## [01]-[PER-CATALOG-STUB VERDICTS]

Catalog quality is uniformly exceptional (400-700-line integration charters with `LOCAL_ADMISSION`/`STACKING_LAW`/`RAIL_LAW`). Verdict scores grade CATALOG FITNESS FOR THE ROBUST-CORE SCOPE, not authoring craft. `M`=mined, `U`=unmined, `DEAD`=catalog names a Geometry-page consumer the page rejects.

### Per-package tier — `libs/csharp/Rasm/.api/` (18 catalogs)

| catalog | v | mined→pages | unmined / dead-in-kernel |
| :------ | :-: | :---------- | :----------------------- |
| `api-bigrational` (Fraction/BigRational) | 10 | M → `predicates` (Fraction exact-rational oracle, top of ladder), `arrangement`/`intersect` (rational crossing-order keys `RationalProjection`/`RationalKey`) | `Mediant` Stern-Brocot flagged "no current consumer" (catalog:148) — genuinely unmined member, correctly documented |
| `api-doubledouble` (`ddouble`) | 10 | M → `predicates` (106-bit middle tier via `AdjustScale`/`Sign`), `fit`/`decimate` (106-bit LM/QEM accumulation) | The **entire special-function library** (gamma/beta/erf/Bessel/elliptic/Jacobi/zeta/orthogonal-polys, ~200 members) is UNMINED — no Geometry page evaluates a special function at 106-bit. Catalog:263 reserves it for "curvature/geodesic/fitting-residuals/quadrature"; those live in Vectors ScalarField (double), not here. Vast unmined richness, correctly reserved not wrong-stratum |
| `api-peteronumbers` (EFloat/ERational/EInteger/EDecimal) | 9 | M → `predicates` (`RationalOracle.BinaryOf` EFloat second-source + ERational third-source differential; `predicates.md:352-365`) | `EDecimal`, directed-rounding `Floor`/`Ceiling` interval filter, `EContext.Flag*` interval-bracket — UNMINED (catalog:153 flags "no current consumer composes it"). CC0-1.0 license (not MIT) correctly noted |
| `api-clipper2` (integer-exact 2D boolean/offset) | 3 | **NONE** | **DEAD**: catalog:91-92 claims `Meshing/arrangement#PLANAR_OVERLAY` "uses Clipper64+PolyTree64" and `Meshing/offset#OFFSET`/`#ASSEMBLE` "route through Clipper2". `arrangement.md:14,470` explicitly "retires the float Clipper2"; `offset.md:3` "Clipper offsetting is float-epsilon and rejected for the robust core". Real consumer: Rasm.Fabrication (catalog:26 owners line). Wrong-stratum in `Rasm.csproj:26` |
| `api-cavaliercontours` (arc/bulge 2D offset+boolean, adaptive-clearing) | 4 | **NONE** in kernel | **DEAD** kernel claim: catalog:95 "`Meshing/offset#OFFSET` routes to `ParallelOffset`". `offset.md` authors wavefront-only, no arc offset. Primary consumer IS Fabrication (catalog:25,94 — kerf/lead-arc/adaptive-clearing). `Shape.ParallelOffset` multi-pocket clearing = the Fabrication-scope adaptive-clearing owner, but wired to the kernel csproj not Fabrication's. Wrong-stratum in `Rasm.csproj:25` |
| `api-geometry3sharp` (g3 DMesh3 + full processing stack) | 3 | **NONE** | **DEAD** across the board: `decimate.md:5` authors own QEM (not g3 `Reducer`); `intersect.md` authors own tri-tri (not g3 `MeshMeshCut`); `repair.md` authors own weld/manifold (not g3); `flatten.md` uses Vectors DEC (not g3 `LaplacianMeshDeformer`); SDF is Vectors `ScalarField` (not g3 `MeshSignedDistanceGrid`). No page uses `DMesh3` (pages use Rhino `Mesh`+`MeshEdit`). Real consumers: Bim (import), Fabrication (`BiArcFit2`). Wrong-stratum in `Rasm.csproj:27`. Biggest unmined surface in the roster |
| `api-gshark` (host-neutral NURBS) | 9 | M → `curve.md` (full `NurbsBase`/`NurbsSurface` algebra + `SurfaceFactory` 7 cases + `Fitting.Curve` + `Intersection.Intersect`) | `curve.md` is the SMALLEST page (156 LOC) and body-STUBBED (`CurveFrom`/`SurfaceFrom`/`CurveApply`/`ToMesh` signature-only). GShark has no geodesic/surface-offset/pattern-map — Generation-gate surface ops are a genuine gap (below) |
| `api-triangle` (Triangle.NET CDT, MIT) | 3 | **NONE** in kernel | **DEAD + WRONG REASON**: `delaunay.md:3` "Triangle/TetGen are GPL and rejected" — but catalog:25 confirms Triangle.NET is **MIT/permissive** (only the original Shewchuk C is research-licensed). Referenced `Rasm.csproj:30` with `Aliases="TriangleNet"`. Catalog envisions the `IPredicates` injection seam (drive Triangle with Rasm exact predicates) — UNUSED. Real consumer: Materials `InteractionDiagram`. Wrong-stratum + factually-wrong rejection |
| `api-kdtree` (Supercluster.KDTree, generic k-NN) | 9 | M → `index.md` (`SpatialIndex.PointCloud` case wraps `KDTree<double,double,int>`; `index.md:118,151-155`) | Namespace-discipline (reach `SuperClusterKDTree` not `Supercluster.KDTree`) correctly captured; `ddouble`-coordinate binding unmined but reserved |
| `api-libtess` (winding-rule polygon fill) | 3 | **NONE** | **DEAD**: catalog:84 "backs the `Drawing/view`+`Drawing/pack` FILL leg". `view.md` produces line-SEGMENTS (no fill); `pack.md` is a feature-CHANNEL encoder (no fill). No Geometry page tessellates a filled region. Wrong-stratum in `Rasm.csproj` |
| `api-csparse` (direct sparse Cholesky/LDL/LU/QR, LGPL-2.1) | 8 | M → `flatten.md` (sparse Cholesky via **Vectors `CholeskySparse` wrapper**, `flatten.md:18` "never a raw `SparseCholesky.Create`") | Real kernel consumer is Rasm.**Vectors** (the mature sibling wraps CSparse); Geometry composes the Vectors wrapper. `Refactorize`/`Update`/`Downdate` amortization unmined by Geometry (Vectors may use). LGPL-2.1-only license (weak-copyleft) worth a license-gate note |
| `api-doubledouble` — see above | | | |
| `api-mathnet-numerics` (distributions/integration/rootfinding/interp/specfun) | 6 | M → `solver`/`fit`/`decimate` (LinearAlgebra `DenseMatrix`/`Cholesky`/`Svd` DIRECT) | Distributions (18+8), integration, root-finding, interpolation, special-functions surfaces UNMINED by Geometry (Compute concerns; `fit` MLESAC could use distributions but authors truncated-cost). This catalog + `api-mathnet-providers` are the non-LA half — mostly unmined by kernel |
| `api-mathnet-providers` (dense LA + MKL/OpenBLAS provider) | 6 | M → `solver`/`fit`/`decimate`/`flatten` (`Matrix<double>`/`Cholesky`/`Svd`/`PseudoInverse`) | **MKL/OpenBLAS have NO osx-arm64 native asset** (catalog:13,176 "osx-arm64 falls back to UseManaged"). `Directory.Packages.props:63-64` pins BOTH providers AND `Rasm.csproj:11-12` references them — DEAD-ON-PRIMARY-PLATFORM (dev machine is darwin/arm64). Useful only for x64 CI; questionable in the kernel csproj |
| `api-miconvexhull` (typed-result hull/Delaunay/Voronoi) | 4 | **NONE** in kernel | **DEAD** kernel claim: catalog:119 "kernel `[COMPUTATIONAL_GEOMETRY]` owner". `fit.md` authors own minimal solvers (no hull); `delaunay.md` authors own Bowyer-Watson (no MIConvexHull). Real consumer: Materials `InteractionDiagram`. Wrong-stratum |
| `api-rhino` (RhinoCommon host geometry) | 10 | M → every page (`Point3d`/`Vector3d`/`Mesh`/`Plane`/`BoundingBox`/`Polyline` value vocabulary via Vectors) | Rhino `Intersection.MeshMesh`/`CreateBooleanUnion`/`RTree`/`Mesh.Weld`/`FillHoles`/`HealNakedEdges` all UNMINED — catalog:134 claims "the repair surface `Processing/repair` composes" but `repair.md` authors its own weld/manifold/gap kernels. Correctly reserved: Rhino parametric = Analysis-layer, Rhino booleans = the retired native gate |
| `api-sharpvoronoilib` (2D Fortune Voronoi) | 3 | **NONE** | **DEAD**: catalog:138 "backs the Geometry `Drawing`/`Meshing` Voronoi leg". `offset.md` medial-axis uses Tessellation Voronoi-dual (in-house `delaunay`); no Drawing page uses point-site Voronoi. Wrong-stratum |
| `api-tensors` (System.Numerics.Tensors SIMD) | 8 | M → `pack.md` (`ReadOnlyTensorSpan<float>` residency seam to Compute; `pack.md:465`) | `TensorPrimitives` SIMD elementwise/reduction surface UNMINED by Geometry interior (correctly — catalog:251 "NEVER the exact-predicate path"). Duplicated identically across both tiers (below) |

### Shared substrate tier — `libs/csharp/.api/` (31 catalogs)

| catalog | v | relevance to Geometry |
| :------ | :-: | :-------------------- |
| `api-thinktecture-runtime-extensions` | 10 | M → **every** page (`[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]`/`[ComplexValueObject]` + `ComparerAccessors.StringOrdinal`). The single most-mined substrate — the ADT-collapse backbone |
| `api-tensors` | 8 | M → `pack` (identical file to per-package tier — DUPLICATE) |
| `api-generator-equals` | 5 | U by Geometry — catalog owners = Element/Bim/Persistence. Geometry pages carry no `[Equatable]` (they use Thinktecture union equality); `reconciliation` uses `XxHash128` content-identity, not structural diff |
| `api-quikgraph` | 4 | U by Geometry — catalog `RAIL_LAW:160` explicitly "the geometric spatial index... own broad-phase" (rejects geometry). Owners = Element/Persistence/Bim. `Spatial/naming` lineage/`arrangement` cell-complex could compose it but do not |
| `api-highperformance` (CommunityToolkit) | 4 | U by Geometry AND not referenced: pinned `Directory.Packages.props:38` under "Shared Domain Primitives" but `Rasm.csproj` does NOT reference it. `ParallelHelper`/`Span2D`/`MemoryOwner`/pooled buffers unavailable to the SAH-BVH build, mesh SoA stores, MLESAC parallel draws. `BitHelper` is bit-range not Morton-interleave (`index.md` correctly uses own `Expand10`/notes `Bmi2.ParallelBitDeposit`) |
| `api-unitsnet` | 3 | Boundary-only — `Angle` bridges kernel radians (catalog:184), but interior is raw `double`. Pinned but `Rasm.csproj` does not reference it. Correctly not kernel-interior |
| `api-mapperly` | 3 | U by Geometry — boundary transcription (Element↔DTO). Not a kernel concern |
| `api-csparse` / `api-mathnet-numerics` / `api-mathnet-providers` | — | DUPLICATES of per-package tier (csparse + mathnet-providers **byte-identical**; mathnet-numerics **divergent** size). See roster-health |
| `api-hashing` (shared) | 4 | **STALE/WRONG** — see verdict-candidate [V4] |
| grpc×7, `api-protobuf`, `api-messagepack`, `api-nodatime`×3, `api-extensions-ai`, `api-hybrid-cache`, `api-jsonpatch`, `api-redaction`, `api-system-configuration`, `api-unicolour`, `api-thinktecture-json`, `api-thinktecture-messagepack` | n/a | Other-stratum (wire/serialization/AI/time/color/config). NOT demanded by the Geometry kernel. Serve AppHost/Persistence/Compute/Materials/Bim. No geometry hook in any header |

## [02]-[CROSS-CUTTING]

### Unmined capability (catalog anchors)

- **Seven float geometry packages fully unmined by the robust-core** (`Clipper2`, `CavalierContours`, `geometry3Sharp`, `Triangle`, `MIConvexHull`, `SharpVoronoiLib`, `LibTessDotNet`). Every one names a Geometry-page consumer in its catalog `LOCAL_ADMISSION`/`STACKING_LAW`; every one is rejected or ignored by that page. The pages author exact equivalents from first principles (`predicates` ladder, `delaunay` Bowyer-Watson, `arrangement` Cherchi-Attene, `offset` Aichholzer-Aurenhammer wavefront, `intersect` Guigue-Devillers, `index` SAH-BVH+octree+GWN, `decimate` QEM, `flatten` DEC-LSCM/ARAP/BFF, `fit` MLESAC+LM, `view` Newell-BSP+Appel).
- **g3 `MeshSignedDistanceGrid`+`MarchingCubes` SDF** unmined — the SDF/distance-field concern the SCOPE MANDATE names is owned by the mature sibling `Rasm.Vectors ScalarField.SignedDistanceFromMesh`/`IsoSurfaceDetailed` (consumed by `decimate` VoxelRemesh + `pack` occupancy channel), NOT g3. g3's SDF is redundant.
- **`ddouble` special-function library** (~200 members) unmined — reserved but no consumer.
- **MathNet distributions/integration/rootfinding/interpolation** unmined by Geometry (Compute concern).
- **`TensorPrimitives` SIMD** unmined by Geometry interior (correctly — exact predicates forbid float SIMD).

### Domain gaps (SCOPE MANDATE concerns no admitted package + no page owns; named candidates, NOT research-verified)

- **Offset with corner strategies (Miter/Round/Bevel/Square joins)** — Fabrication toolpath gate. `offset.md` owns straight-skeleton/medial/Minkowski/wavefront-snapshot but NO join-strategy offset. Clipper2 `JoinType`×`EndType` (catalog:80) owns this but is rejected. GAP unless the kernel composes Clipper2 for the float toolpath tier OR the concern moves to Fabrication.
- **Adaptive clearing (2D pocket morphed-spiral/trochoidal)** — Fabrication CAM gate. CavalierContours `Shape.ParallelOffset` multi-pocket (catalog:82) owns it, wired to kernel csproj but consumed by nobody. GAP — belongs in Fabrication.
- **Slice/section STACK (layered parallel-plane contour stack for 3D print)** — `intersect.md` `PlaneMesh` owns ONE plane; no owner for a layer-stack with per-layer contour nesting + infill. Candidate: loop `PlaneMesh` + a new `SliceStack` owner. GAP.
- **Surface geodesic CURVES / surface offset / pattern-to-surface UV mapping** — Generation KERNEL GATE. `curve.md` (GShark) has `IsoCurve` + `PerpendicularFrames` (stations/frames = OK) but NO geodesic-path, NO normal-offset-surface, NO pattern-map op. `ScalarField.Geodesic` (Vectors) gives the heat-geodesic scalar FIELD (consumed by `pack` geodesic channel) but not geodesic POLYLINES on a NURBS surface. GAP — candidates: MMP/heat-method geodesic-path authoring, or a surface-op expansion of `curve.md`.
- **Subdivision / panelization** — Generation gate (tessellation/subdivision/panelization). No owner. `curve.md` `ToMesh(uvResolution)` grid-tessellates a surface but no Catmull-Clark/Loop subdivision, no quad-panel/diagrid panelization. GAP — candidate: OpenSubdiv-family C# binding or authored subdivision, panelization authored on the arrangement/parametric substrate.
- **Medial axis / distance fields / mesh booleans / developable-unroll** — ALL OWNED: `offset` (medial), Vectors `ScalarField` (SDF), `arrangement` (mesh boolean), `flatten` LSCM/ARAP/BFF (developable-unroll). NOT gaps.

### Roster health

- **Wrong-stratum kernel deps**: `Rasm.csproj` "Geometry"/"Computational Geometry" ItemGroups (lines 18-31) reference GShark, LibTessDotNet, Supercluster.KDTree.Net, CavalierContours, Clipper2, geometry3Sharp, MIConvexHull, SharpVoronoiLib, Triangle. Only **GShark + KDTree** are mined by Geometry pages. The other **7 are consumed only by downstream Fabrication/Materials/Bim** or the mature Analysis/Vectors siblings (unverifiable here — mature, no `.planning`). If the mature siblings do not use them, they are dead kernel refs that belong in the downstream `.csproj` files.
- **MKL/OpenBLAS dead-on-platform**: `MathNet.Numerics.Providers.MKL`/`OpenBLAS` pinned + referenced but no osx-arm64 native asset (arm64 dev machine → managed fallback always). x64-CI-only value.
- **Centrally-pinned-but-unreferenced substrate the kernel wants**: `CommunityToolkit.HighPerformance` (`ParallelHelper` for parallel BVH build, `MemoryOwner`/`Span2D` for the SoA `NodeStore`/`QuadricStore`/`WavefrontStore`/`SimplexStore`/`FitStore`/`IntersectStore`/`BspNode` scratch arenas) is pinned (`Directory.Packages.props:38`) but NOT referenced by `Rasm.csproj`. The kernel hand-rolls flat `double[]`/`int[]` SoA everywhere; `MemoryOwner<T>`/`ArrayPool` pooling and `ParallelHelper.For` are unmined substrate the hot kernels want.
- **Catalog duplication across tiers**: `api-csparse.md` (16132 B both) and `api-mathnet-providers.md` (21901 B both) and `api-tensors.md` (23931 B both) are byte-identical in `libs/csharp/.api/` AND `libs/csharp/Rasm/.api/`; `api-hashing.md` (9001 vs 9292) and `api-mathnet-numerics.md` (27688 vs 18605) DIVERGE. Two-tier design intends shared-substrate vs per-package; these five sit in both — duplication drift, and the divergent pair actively disagrees.

### Concern-mixing / mining inconsistency / duplicate mechanisms

- **MathNet dense-LA owner is inconsistent**: `fit.md`/`decimate.md`/`solver.md` call MathNet DIRECTLY (`using MathNet.Numerics.LinearAlgebra`, `DenseMatrix.OfArray`, `spd.Cholesky().Solve`); `flatten.md:18` composes the **Vectors wrappers** (`CholeskySparse.Of`, `Matrix.DecomposeSvd`) and forbids "a raw provider call". Two rules for one concern (dense linear algebra).
- **Intra-folder crossing-test duplication (self-acknowledged)**: `repair.md` (`TriangleCrossPoint`/`EdgesCrossTriangle`), `offset.md` (`SegmentsCross`), `arrangement.md` (face-subdivision straddle) each spell an inline exact-`Orient` crossing test; `intersect.md:3,388` names itself the owner they should collapse onto (`Intersection.Apply`/`Crossing`) but the collapse is pending — three near-identical crossing kernels live.

### Wrong-stratum seam (unwired/mis-directed)

- **`index.md` references UP**: `index.md:27` `using Rasm.Compute.Solver` and `ToAcceleration()` returns `Rasm.Compute.Solver.AccelerationStructure` (`index.md:572-578`). The KERNEL (base stratum) references app-platform `Rasm.Compute` — a dependency-direction violation (`architecture.md:35` "Rasm references no sibling"). The acceleration-wire type is named as Compute's; the kernel is coupled upward. Clean fix: kernel owns the neutral wire (`float[]`/`long[]` node-link) and Compute wraps it, OR the `AccelerationStructure` union lives in the kernel.

### Folder-architecture verdicts

- **`.api` folder topology**: per-package tier (18 files, flat, all geometry/numerics) is well-scoped. Shared tier (31 files, flat) mixes true cross-package substrate (thinktecture/tensors/hashing/generator-equals/mapperly/quikgraph/highperformance/unitsnet) with pure other-stratum wire (grpc×7/protobuf/nodatime×3/etc.) AND the 5 duplicated numerics catalogs. No sub-foldering; acceptable at 31 but the duplication is the real defect.
- **Namespace law drift**: `ARCHITECTURE.md:64` (NAMESPACE_LAW) declares 5 sub-domain namespaces (`Rasm.Geometry.Numerics`/`Spatial`/`Meshing`/`Processing`/`Drawing`). The realized page fences use **~14 leaf namespaces**: `Rasm.Geometry.Numerics`, `.Spatial`, `.Tessellation`, `.Arrangement`, `.Intersection`, `.Offsetting`, `.Simplification`, `.Parameterization`, `.Fitting`, `.Constraints`, `.Projection`, `.Encoding`, `.Healing`, `.Parametric` (+`.Topology` referenced by `pack`). The `Meshing/` FOLDER holds `Tessellation`/`Arrangement`/`Intersection`/`Offsetting` NAMESPACES (folder≠namespace). The codemap (`ARCHITECTURE.md:14-37`) names `Delaunay.cs`/`Arrangement.cs`/`Intersect.cs`/`Offset.cs` but pages namespace them `Tessellation`/`Arrangement`/`Intersection`/`Offsetting`. ARCHITECTURE and the realized fences disagree on both namespace granularity and file↔namespace mapping.
- **Parametric is the thin folder**: 1 page (`curve.md`, 156 LOC, stub-heavy) against a Generation gate demanding surface geodesics/offset/subdivision/panelization/pattern-map. The folder is under-built for its consumer pressure — a coverage-axis defect. Every other folder (Meshing 4, Processing 6, Spatial 3, Drawing 2, Numerics 2) is transcription-complete and dense.

## [03]-[VERDICT CANDIDATES] (strongest first)

**[V1] RESOLVE THE CATALOG-VS-PAGE CONTRADICTION: robust-core authors exact; float packages move downstream.** Seven admitted geometry packages (Clipper2/CavalierContours/g3/Triangle/MIConvexHull/SharpVoronoiLib/LibTess) name Geometry-page consumers in their `.api` catalogs; every named page rejects them and authors an exact equivalent. Evidence: `offset.md:3` (rejects Clipper2 "float-epsilon"), `arrangement.md:14,470` (retires Clipper2), `delaunay.md:3` (rejects Triangle), `decimate.md:5` (own QEM not g3), `fit.md` (own solvers not MIConvexHull), `view.md`/`pack.md` (no LibTess fill), `offset.md` medial (Tessellation dual not SharpVoronoiLib). RULING: kernel owns the EXACT robust tier (author from scratch — the world-class bar); the float packages relocate to their real consumers (Fabrication toolpath, Materials InteractionDiagram, Bim import) and drop from `Rasm.csproj`; the catalogs' kernel-consumer claims get rewritten to name the real downstream consumers. This satisfies FIT-NEVER-COUPLED (each package to its stratum) and the robust-core doctrine simultaneously.

**[V2] FABRICATION-SCOPE FLOAT TOOLPATH GEOMETRY IS UNOWNED.** The SCOPE MANDATE's Fabrication gate demands corner-strategy offset (Miter/Round/Bevel), adaptive clearing (trochoidal/morphed-spiral pockets), and slice-stacks. The kernel `offset.md` owns ONLY exact straight-skeleton/medial/Minkowski (no join strategy, no clearing, no arc offset); `intersect.md` owns ONLY single-plane section (no layer stack). CavalierContours (`Shape.ParallelOffset` clearing) + Clipper2 (`JoinType` offset) own exactly these but are rejected by the robust core. RULING: these are FLOAT PRODUCTION concerns, not exact-robustness concerns — they belong in Rasm.Fabrication composing CavalierContours/Clipper2 directly, NOT the kernel. The kernel provides the exact substrate (medial axis, section curves, arrangement); Fabrication builds toolpaths on it + the float packages. A new `SliceStack` owner (looping `PlaneMesh` + contour-nesting) is the one genuine kernel addition.

**[V3] PARAMETRIC FOLDER IS UNDER-BUILT AGAINST THE GENERATION GATE.** `curve.md` is 156 LOC (smallest page), body-stubbed, and covers curve+surface EVALUATION only. The Generation KERNEL GATE (RASM-GENERATION-SPEC SpineRef "forms-live-in-kernel-geometry") demands stations/frames-along-curves (OK — `PerpendicularFrames`), isolines (partial — `IsoCurve`), and geodesics/surface-offset/tessellation/subdivision/panelization/pattern-to-surface-mapping (NONE owned). GShark provides none of the missing ops. RULING: the Parametric folder needs a surface-operations expansion (geodesic-path via heat-method/MMP, normal-offset surface, subdivision, panelization, pattern-to-surface UV map) — the single largest COVERAGE gap in the corpus, and the one the Generation campaign directly gates on.

**[V4] DIVERGENT-DUPLICATED `api-hashing.md` ACTIVELY CONTRADICTS THE SEAM LAW.** `Rasm/.api/api-hashing.md:82`: content-identity is `XxHash128.HashToUInt128` seed-zero → `UInt128` (matches `ARCHITECTURE.md:44-47` "XxHash128 seed-zero" + every page's `ContentHash.Of` usage + `pack.md` `NamingHashOps.Encode`). `libs/csharp/.api/api-hashing.md:82`: content-identity is `XxHash3.HashToUInt64` → `ulong`. The shared-tier catalog names the WRONG algorithm AND width, contradicting the cross-runtime content-address seam the whole `libs/` federation reproduces bit-identically. RULING: the shared-tier hashing catalog is stale/wrong; reconcile to XxHash128/UInt128 seed-zero, and resolve the 5-catalog cross-tier duplication (csparse/mathnet-providers/tensors identical; hashing/mathnet-numerics divergent) — one owning tier per catalog.

**[V5] WRONG-STRATUM UPWARD REFERENCE IN `Spatial/index`.** `index.md:27,572-578` — the kernel references `Rasm.Compute.Solver.AccelerationStructure` and returns it from `ToAcceleration()`, an UP reference violating `architecture.md:35` (kernel references no sibling). RULING: the acceleration-structure wire type must be kernel-owned (or a neutral `float[]`/`long[]` node-link payload the kernel emits and Compute wraps). This is the one concrete strata violation in the corpus and a FIT defect at the Compute seam.

**[V6] MKL/OPENBLAS PROVIDERS ARE DEAD ON THE PRIMARY PLATFORM.** `Directory.Packages.props:63-64` + `Rasm.csproj:11-12` pin/reference `MathNet.Numerics.Providers.MKL`/`OpenBLAS`; `api-mathnet-providers.md:13,176` confirm no osx-arm64 native asset (arm64 dev → managed always). RULING: drop the provider references from the kernel csproj (keep the central pins only if x64 CI/deploy needs them via a conditional ItemGroup); the kernel's MathNet use is dense Cholesky/SVD which runs managed. Removes two dead native gates from the geometry kernel.

**[V7] UNIFY THE DENSE-LINEAR-ALGEBRA OWNER.** `fit`/`decimate`/`solver` call MathNet directly; `flatten` composes Vectors `Matrix`/`CholeskySparse` wrappers and forbids raw provider calls. Two rules for one concern. RULING: pick one — either the kernel composes the Vectors linear-algebra wrappers uniformly (matching `flatten`, treating Vectors as the numeric-rail owner), or MathNet direct is sanctioned uniformly. Given `flatten`'s wrapper discipline is the denser/more-owned form and CSparse is ALREADY reached only through Vectors, the Vectors-wrapper rule should win corpus-wide.

**[V8] WIRE `CommunityToolkit.HighPerformance` INTO THE KERNEL.** Pinned (`Directory.Packages.props:38`) but `Rasm.csproj` does not reference it. Every hot kernel hand-rolls flat SoA arenas (`NodeStore`/`QuadricStore`/`WavefrontStore`/`SimplexStore`/`FitStore`/`IntersectStore`/`BspNode`) and serial builds. `ParallelHelper.For` (parallel BVH/QEM/MLESAC), `MemoryOwner<T>`/`ArrayPool` (pooled scratch on the reject-loop hot paths), and `Span2D` are unmined substrate the pages want. RULING: reference HighPerformance in `Rasm.csproj` and mine `ParallelHelper`/`MemoryOwner` into the SoA-heavy kernels — a density/perf lift with zero new shape.

**[V9] COLLAPSE THE THREE INLINE EXACT-CROSSING TESTS ONTO `intersect`.** `repair` (`TriangleCrossPoint`), `offset` (`SegmentsCross`), `arrangement` (face straddle) each spell an inline exact-`Orient` crossing test; `intersect.md:388` names itself the owner and the collapse as pending. RULING: land the collapse — the three consumers compose `Intersection.Apply`/`Crossing`, no fourth crossing kernel. Duplicate-mechanism removal the corpus already flags against itself.

**[V10] RECONCILE THE NAMESPACE LAW WITH THE REALIZED FENCES.** `ARCHITECTURE.md:64` declares 5 sub-domain namespaces; pages use ~14 leaf namespaces and the `Meshing/` folder holds four non-`Meshing` namespaces. RULING: either update `ARCHITECTURE.md` NAMESPACE_LAW + codemap to the realized per-leaf namespace scheme (the pages are transcription-complete and self-consistent, so the law is stale), or re-scope the page namespaces to the 5-folder law. The pages are the source of truth; the ARCHITECTURE law should follow them.
