id: no-unshielded-cleanup-await
valid:
    - |
      import anyio

      async def run(root):
          try:
              outcome = await act(root)
          finally:
              with anyio.CancelScope(shield=True):
                  cleaned = await clean(root)
          return outcome
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with anyio.move_on_after(5, shield=True):
                  await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with anyio.fail_after(5, shield=True) as scope:
                  await conn.wait_closed()
    - |
      from anyio import CancelScope

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with CancelScope(shield=True):
                  await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          with anyio.CancelScope(shield=True):
              try:
                  await conn.run("echo")
              finally:
                  await conn.wait_closed()
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              conn.close()
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          except ValueError:
              await conn.reset()
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          except Exception:
              await conn.reset()
              raise
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              async def later():
                  await conn.wait_closed()
              register(later)
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          except BaseException:
              conn.close()
              raise
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          except CancelledRequest:
              await conn.reset()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with anyio.CancelScope(shield=False):
                  conn.close()
    - |
      import anyio

      async def run(conn):
          scope = anyio.CancelScope(shield=True)
          try:
              await conn.run("echo")
          finally:
              with scope:
                  await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              if conn.open:
                  with anyio.CancelScope(shield=True):
                      await conn.wait_closed()
    - |
      import anyio

      async def run(conn, lock):
          try:
              await conn.run("echo")
          finally:
              with anyio.CancelScope(shield=True), lock:
                  await conn.wait_closed()
    - |
      async def run(conn):
          try:
              await conn.run("echo")
          except BaseExceptionGroup:
              await conn.reset()
              raise
    - |
      async def run(conns):
          try:
              await conns[0].run("echo")
          finally:
              for conn in conns:
                  conn.close()
invalid:
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with anyio.CancelScope(shield=False):
                  await conn.wait_closed()
    - |
      import tempfile

      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with tempfile.TemporaryDirectory(ignore_cleanup_errors=True) as report:
                  await conn.save(report)
    - |
      import anyio

      async def run(root):
          try:
              outcome = await act(root)
          finally:
              cleaned = await clean(root)
          return outcome.bind(lambda _: cleaned)
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              conn.close()
              await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              if conn.open:
                  await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              with anyio.move_on_after(5):
                  await conn.wait_closed()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              async with conn.lock:
                  conn.close()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              async for chunk in conn.drain():
                  discard(chunk)
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except BaseException:
              await conn.reset()
              raise
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except:
              await conn.reset()
              raise
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except anyio.get_cancelled_exc_class():
              await conn.reset()
              raise
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except (ValueError, BaseException) as error:
              await conn.reset(error)
              raise
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except* BaseException:
              await conn.reset()
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          except:
              # The reset
              await conn.reset()
              raise
    - |
      import anyio

      async def run(conns):
          try:
              await conns[0].run("echo")
          finally:
              [await conn.close() for conn in conns]
    - |
      import anyio

      async def run(conn):
          try:
              await conn.run("echo")
          finally:
              try:
                  await conn.wait_closed()
              except OSError:
                  pass
    - |
      import asyncio

      async def run(conn):
          try:
              await conn.run("echo")
          except asyncio.CancelledError as cancelled:
              await conn.reset()
              raise cancelled
    - |
      import anyio

      async def run(conn):
          try:
              with anyio.CancelScope(shield=True):
                  await conn.run("echo")
          finally:
              await conn.wait_closed()
