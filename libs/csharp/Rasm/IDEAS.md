# [RASM_IDEAS]

Forward pool of higher-order kernel concepts grounded in the robust-geometry domain and the monorepo geometry-flow. `[1]-[OPEN]` carries live ideas; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[CONTENT_HASH_LANE_COLLAPSE]-[COMPLETE]: the BigEndian fork adjudicated a DEFECT-free boundary projection, not a second lane convention — the blobstore nonce's `>> 64` extraction is the standard high lane (bit-identical to `ContentHash.Half(value, 1)`) and its big-endian byte WRITE is the sealed-frame format's own frozen layout, exactly the byte-order-stays-at-the-consuming-seam clause `Domain/identity.md` `[02]` Boundary already owns, so composing the owner re-cut zero stored bytes and the Deterministic ruling's [NOT] carve held; six sites re-cut onto `Half` (blobstore nonce, `Symbolic/lowering` compiled-key frame, `Tensor/blas` dedup preimage, `Runtime/codecs` HLC compose, plus the sweep's `Collab/sync` peer-tint unit and `Posting/optimization` XOR lane fold), the card's `Tensor/factor.md:742` anchor was stale (that page's digest rides `SolveDedupKey`/`XxHash128` whole with no inline split), and the collapse ruling landed at `libs/csharp/.planning/RULINGS.md` `[03]-[COLLAPSE]`.
[MATRIX_TRANSFORM_BAND]-[COMPLETE]: landed as `Numerics/matrix.md` `[06]-[TRANSFORM_BAND]` — `SpectralArena` `[Union]` over four caller-owned buffer layouts (interleaved-over-`CellLattice`, split, packed half-spectrum, Hartley) with one `MatrixKernel.Transform(sense, scaling)` entry, `SpectralScaling` the declared convention row, the 15-row `WindowTaper` roster with FFT framing as a column, the 12-row `InterpolationRoute` + two off-roster owners and the three-case `Interpolant` capability union; 2D IS the row-column fold (managed multidim throws — refused at selection per CAPABILITY_PROBE_FLOOR); the STRATA_TWIN premise held (kernel seats the fold; the Materials `Fourier2` re-seat and the false Fabrication PicoGK-morphology Unlocks clause are recorded — the latter DELETED as a verified negative, morphology composes no managed transform).
[CLOUD_VORONOI_FIELD]-[COMPLETE]: landed across three owners — `Spatial/cloud.md` `[05]-[VORONOI_COMPLEX]` (3D cells/adjacency/skeleton over `VoronoiMesh.Create<CloudVertex, CloudCell, CloudVoronoiEdge>`, Option-carried measures so an unbounded cell publishes no volume, `NaturalNeighborWeights` stolen-volume fold, `VectorIntent.Voronoi` entry), `Meshing/reconstruct.md` (`SibsonCase` admission-only mint, `NaturalNeighbor` mode row, `EvaluateSibson`), and `Spatial/fields.md` (`ScalarField.SibsonCase` fitted case); the 2D border-clipped Voronoi stays `Meshing/delaunay` `Tessellation.VoronoiDual` per the stated split.
[FLOW_TOPOLOGY_ATLAS]-[COMPLETE]: landed as `Processing/flow.md` `[04]-[TOPOLOGY]` — `MorseAtlas.Of` over one `ToAdjacencyGraph` container feeding SCC + condensation, the public carrier the frozen-column SoA `MorseGraph` per the QuikGraph collapse ruling, fixed-point classification by the GENERAL (complex-spectrum) eigen decomposition (a flow Jacobian is not symmetric — the symmetric-owner anchor was corrected), separatrices as multi-seed `FlowKernel.Trace` runs with the `CriticalCaptureCase` termination arm; the singularity vocabulary is MINTED here — the card's `Processing/segment.md` anchor was a verified negative; panelize/patternmap adoption waits on genuine consumer pressure.
[SENSITIVITY_RAIL]-[COMPLETE]: narrowed and landed at `Solving/solver.md` — `Dual<T>` forward-mode scalar over generic math, `IDualResidual` + `DualModel : ILmModel` accumulating packed-upper `JᵀJ`/`Jᵀr` through `Lm.PackedIndex` with no dense `J`, FD demoted to test oracle; NO new page (extend-the-owner law) and NO adjoint band — `Rasm.Compute`'s landed `Sensitivity` family riding the `MeshAdjointSnapshot` seam is the canonical adjoint owner per the second-owner ruling.
[PERCEPTUAL_COLOUR_DEPTH]-[COMPLETE]: landed at `Numerics/atoms.md` `[02]` — the viewing condition rides case payloads per the axis-split precedent (`BlendPath.Appearance` and the `DeltaMetric` `[Union]` carry `CamConfiguration`; `RgbProfile.Condition` mints an authored condition off a rostered illuminant, `RgbProfile.Viewed` is the ONE cam-bearing `Configuration` mint, memoized per condition), so an unconditioned appearance blend or CAM delta-E is unspellable — the card's Tension landed structurally, no default surround is fabricated, and WCAG `Contrast` stays condition-free; `AppearanceReading` publishes the measured condition onto the receipt.
[NATIVE_LANE_GOVERNANCE]-[COMPLETE]: landed at `Meshing/arrangement.md` — the managed rail reads `policy.Cancel` at the subdivision/classify/weld stage heads through the `ArrangeStage` governance seam and publishes stage-fraction progress, the native lane attaches the execution context to the RESULT before the terminal `manifold_status` force (deferred ops ignore operand-attached contexts — the header-dictated correction), and both routes lower abandonment as `GeometryFault.RunAbandoned` 2403.
[OPTIONAL_KEY_LEVEL_FAMILY]-[COMPLETE]: landed at `Domain/telemetry.md` `[04]` — the scalar and family cell stores collapsed into ONE `Atom<HashMap<(string Name, Option<string> Key), double>>`, `InstrumentSet.Level(name, value, Option<string> key = default)` is the one write, and the keyed reader projects `Some` tagged / `None` untagged on the params-span ctor, mirroring the settled `TenantContext.Partitions` arm; the full tag-set structural-equality widening stays deliberately unlanded — a strictly larger axis this realization does not need.
[UNIT_CARRIED_MEASURES]-[COMPLETE]: delivered across two owners by explicit boundary decision — the dynamic-conversion seam landed at `Domain/context.md` `[UNIT_BRIDGE]` (`ModelUnit.Convert` over guarded `TryConvert`, the cached typed-rail `Converter<TQuantity>`), and the dimensioned carrier is `Rasm.Bim` `MeasureValue.OfSi` per `Analysis/measure.md` `[02]` Boundary; unit identity federates at the `UnitsNet` `BaseDimensions` 7-vector, never the carrier — `libs/csharp/.planning/RULINGS.md` `[02]-[SHAPE]` carries the row.
[UV_ISLAND_PACKING_OWNER]-[DROPPED]: false premise on all three legs — `Rasm.Fabrication` `Nesting/nfp.md:35` already composes the kernel Minkowski walk (nothing unreachable), `Processing/flatten.md:119` rules "packing stays the downstream owner's concern" (the kernel deliberately ships the boundary projection and stops), and `Rasm.Materials` has no atlas-packing owner (its `wang` atlas is a fixed grid, not irregular packing); the STRATA_TWIN the card invoked has no second twin, and every kernel owner nfp composes (`Minkowski`, `PlanarOverlay`, `LowerHull`) is already landed.
[GEOMETRY_BATCH_PRODUCER]-[DROPPED]: wrong owner — S0 references no sibling, so the kernel reaches neither `LakeGeneration` nor `Apache.Arrow` without pushing a columnar dependency onto every package above it, and `Encode.Apply` yields ONE `EncodedGeometry` per op where a lake generation needs a corpus; landed as `Rasm.Compute` `[GEOMETRY_LAKE_EGRESS]` keying its generation on this page's `PackSchema.SchemaId`.
[KERNEL_SIGNAL_FABRIC]-[COMPLETE]: the arming row is `libs/csharp/.api/api-languageext.md` `[07]` `Eff.runtime<RT>() -> Eff<RT, RT>`, and the charge already rides settled fences — `Env.EnvAsks`/`Asks`/`Taps` and the two-exit `Operation.Apply` cost charge at `Analysis/query.md` `[03]-[OPERATION_RUNTIME]` — so the signal plane carries no research remnant.
[BENCH_CLAIM_LEDGER]-[COMPLETE]: landed as `Domain/telemetry.md` `[08]-[BENCH_LEDGER]` (`BenchClaim`/`BenchLedger`) with claim rows at `Processing/decimate.md` `Simplify.HausdorffClaim`, `Parametric/curve.md` `Parametric.FrameDefectClaim`, `Parametric/surface.md` `Surfaces.CurvatureSummaryClaim`, and `Processing/flatten.md` `Flatten.DistortionClaim`.
[COLUMNAR_WIRE_SCHEMA]-[COMPLETE]: landed as `Drawing/pack.md` `[03]-[SCHEMA_AND_EVIDENCE]` — `PackWireContext` supplies the resolver, `EvidenceWire.Json` seals through `JsonSerializerOptions.MakeReadOnly()` at type init with `DDoubleJsonConverter` registered options-level, and the context folds into `Rasm.AppHost/Runtime/ports#WIRE_LAW` `SuiteContracts.Wire` as one argument.
[CAUSAL_FRAME_CAPSULE]-[COMPLETE]: landed as `Domain/telemetry.md` `[03]-[CAUSAL_FRAME]` — `TelemetrySource`, `CorrelationId`, `TenantId`/`TenantContext`, `ReceiptEnvelope`, and `ReceiptSinkPort`; tenancy stamps the kernel `AsyncLocal` slot and the BCL `Activity` store, and the OTel baggage store registers as one composition `TenantMirror` row.
[INSTRUMENT_SPEC_CAPSULE]-[COMPLETE]: landed as `Domain/telemetry.md` `[04]-[INSTRUMENT_MECHANISM]` — `InstrumentSpec` absorbed `InstrumentRow` whole, `InstrumentKind` x `MeasureForm` derives every bind from one generic body, and `InstrumentSet.Write` folds the three write verbs onto one typed rail.
[BURN_RATE_CAPSULE]-[COMPLETE]: landed as `Domain/telemetry.md` `[06]-[SLO_ALGEBRA]` — `Sli`, `Objective`, the four-row `BurnRow` table, `AlertSeverity` over page and ticket, `AlertSpec`, and the eight-row `PanelKind`.
[TRIANGULAR_ADDRESS_OWNER]-[COMPLETE]: `SymmetricMatrix.FlatIndex` promoted `internal` is the one triangular-addressing mint; `Domain/stats.md` `SampleMoment` and `Solving/solver.md` `Lm.PackedIndex` delegate, the solver keeping its checked `long` widening at the call boundary.
[DRAWING_HATCH_PLANE]-[COMPLETE]: landed as `Drawing/hatch.md` — `HatchPattern` rows (`Parallel`/`Crosshatch`/`Staggered`/`Motif`) carry rhythm as row data, courses clip by exact winding parity over `DrawingProjection.Fill` loops, and the motif arm composes `Patterning.Apply`'s wallpaper vocabulary; realized by `TASKLOG` `[HATCH_SYNTHESIS]`.
[POLYGON_NORMAL_OWNER]-[COMPLETE]: `Numerics/atoms.md` `VectorFrame.NewellNormal` is the one inexact polygon-normal fold; `Spatial/neighbors.md` and `Parametric/panelize.md` compose it, `Numerics/predicates.md` keeps the exact-carrier bar, and `Spatial/cloud.md`'s fitted-plane ring normal stays the deliberate divergent method.
