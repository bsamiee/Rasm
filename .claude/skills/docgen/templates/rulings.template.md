# [<owner-token>_RULINGS]

<rulings-charter-lead: the folder's decision registry in owning voice — the settled rulings agents re-litigate for lack of a home; a row is one decision with the one why that guards its re-litigation: a package admission or rejection, an owner-choice discriminant, a collapse refusal naming the guarantee the denser form loses, a structure retirement, or a folder-learned working law. Append-mostly; a row earns its seat while its why stays homeless, and dies only when fact and why both live at one durable surface.>

<!-- source-only: ruling row template (every section shares it):
- <subject as a code span — the package, owner, surface, or decision> <DECISION-VERB in owning voice — KEEP / rejected / homes at / stays / do-not-re-reject> — <the one load-bearing why that guards the re-litigation>.

Row anatomy — a row stays under 150 characters; the why is mandatory, naming the discriminant or lost guarantee precisely enough that two readers reach one verdict, and every later field earns its seat: the recurring wrong move only where not the why's plain negation, the reopen predicate only where evidence-bound and non-derivable from the why, the correct route only where the capability lands somewhere non-obvious. One decision per row — a row carrying two splits into siblings; a clause every row of a section repeats hoists into one prose line opening the section, the sole non-list line a section admits, legislating never narrating.

Lifespan and admission: this registry is the folder's PERMANENT surface while design pages and fences are ephemeral — each converts to a code file and its prose dies with the conversion — so permanence decides homing; a row exists ONLY while its why is a standing re-litigation guard with no permanent home (a RULINGS tier, README/ARCHITECTURE registry, or manifest carrying both fact and why), and a design page or fence stating the why does not home it — it marks the migration. Migration is a MOVE, never a copy: the row lands here and the page-side guard prose — the why, the wrong-move litany, the reopen clause — deletes from the page in the same pass, the page keeping mechanism and at most one boundary pointer (what homes where, one consumed symbol); two surfaces stating one guard fork truth on first edit. Every row is TRUE NOW: each named surface verifies against live disk before landing, a stale claim corrects to disk truth or dies, and an un-landed prescription is a card, never a row. DROPPED ideas graduate only by explicit promotion — the rejection re-stated as a standing guard in registry voice; the drop record stays in IDEAS.md [02]-[CLOSED], never auto-graduating into a ruling.

Placement: a new ruling appends at its section's tail — adjacency is arrival order, never taxonomy; a later insight sharpens the owning row in place, never re-sorting the section or minting a near-duplicate beside it.

Scope: RULINGS.md stands at every registry-carrying tier — package folder, branch, cross-libs, `tests/` root, each `tests/<language>/` tree — durable and maintained like README/ARCHITECTURE/IDEAS/TASKLOG; an unpopulated instance carries the charter lead and all five sections marked `(none)` so a future session appends its first ruling instead of skipping the folder, and a populated instance omits unused sections and renumbers survivors. Rulings home at the NARROWEST tier whose scope contains them — a package decision at the package, branch-spanning at the branch, estate-spanning at cross-libs, a test-tree decision at its `tests/<language>/` tier, cross-tree at the `tests/` root — appearing at exactly one tier; a wider tier references nothing downward, and a cross-tier duplicate resolves to the narrowest legitimate home.

Sections are a closed vocabulary — `[PACKAGES]` admissions and rejections, `[SHAPE]` owner-choice discriminants, `[COLLAPSE]` density refusals, `[STRUCTURE]` layout and retirement rulings, `[PROCESS]` folder-learned working law. Subjects and members are code spans in verified spelling; any slug reference is UPPERCASE_SNAKE — a numeric or hyphenated slug is a defect — and every path is repo-relative.
-->

## [01]-[PACKAGES]

- <package-name> KEEP — <the objection named and refuted, e.g. "unmaintained" is chronology, not capability>; <the non-derivable reopen, when one exists>.
- <admission-discriminant> — <the rule deciding what this folder admits versus routes to a sibling, e.g. managed purity on a live consumer>.

## [02]-[SHAPE]

- <owner> homes at <surface>, not <the rejected alternative> — discriminant: <the fact that decides it>.

## [03]-[COLLAPSE]

- <plural surface> stays plural — <the central owner that IS the collapse>; folding <the rosters> into one row erases <the guarantee lost>.

## [04]-[STRUCTURE]

- <retired or relocated surface> does NOT re-enter — <where its capability now lives>; a "missing" gap fills from <the durable owner>.

## [05]-[PROCESS]

- <working law> — <the folder-learned rule and the failure it fences>; <the recurring mistake that re-litigates without this row>.
