# [APPUI_TASKLOG]

Open and closed work for the product UI engine, distilled from the ideas in `IDEAS.md`. Each open card carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — plus the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. Live-host probes and tool-gated arms are tracked as blocked tasks against the substrate that unblocks them.

## [1]-[OPEN]

[T-SHADING-PAGE] [QUEUED] — author `shading/shader-pipeline.md`, the content-keyed shader-asset owner
- Author the `shading/` design page the `ARCHITECTURE.md` codemap and the `README.md` router already name but no file backs: `ShaderModule` keying each WGSL/SKSL source by its `XxHash128` content hash, the `PipelineState` cache memoizing the `RenderPipeline`/`ComputePipeline`/`SKRuntimeEffect` per `(ShaderModule, GpuBackend, vertex-layout)` key, the `ShaderCompileReceipt` sealing the compile diagnostic through `ReceiptSinkPort`, and the dev-loop shader-watch under HotAvalonia hot-reload.
- Integrate Silk.NET.WebGPU (`DeviceCreateShaderModule`/`DeviceCreateRenderPipeline`/`DeviceCreateComputePipeline`), Silk.NET.WebGPU.Extensions.WGPU (`SetLogCallback` compile-diagnostic routing), SkiaSharp (`SKRuntimeEffect`), System.IO.Hashing (content key), Thinktecture.Runtime.Extensions, LanguageExt.Core.
- Wires the cache below the `viewport-pipeline.md#RENDER_GRAPH` `RenderTargetFactory` column so every `RenderPass` binds a compiled pipeline by key; the content key folds through the same `interchange#CONTENT_ADDRESSING` `XxHash128` law the residency already keys on; the dev-watch rides the `evidence/diagnostics-evidence.md` dev-loop HUD.
- Internal: the `.api/api-silk-webgpu.md`, `.api/api-silk-webgpu-wgpu.md`, and `.api/api-skiasharp.md` catalogues carry the shader-compile and pipeline-create member sets; the per-backend pipeline-state object spelling and the WGSL-versus-SKSL source divergence resolve at the `GpuFamily` axis the cache keys on, fence-complete now and SPIKE-gated on the live host-shared GPU context exactly as the render-graph GPU passes are.

[T-IMMERSIVE-PAGE] [QUEUED] — author `immersive/xr-review.md`, the OpenXR stereo design-review session
- Author the `immersive/` design page the codemap/router name but no file backs: `XrSession` over the `Silk.NET.OpenXR` graphics-binding chain on the shared `Wgpu` `Device`/`Queue`, the `StereoSwapchain` per-eye image array, the `XrFrameLoop` `WaitFrame`/`BeginFrame`/`EndFrame` on the predicted-display-time clock, the controller-navigated `XrViewpoint` projecting the BCF `Viewpoint`/measurement/section into the headset, and the `EnvironmentBlend` passthrough case folding to the flat viewport when the OpenXR loader is absent.
- Integrate Silk.NET.OpenXR, Silk.NET.OpenXR.Extensions.KHR (`KHR_vulkan_enable2`/`KHR_composition_layer_depth`), Silk.NET.OpenXR.Extensions.EXT (`EXT_hand_tracking`), and the NEW Silk.NET.OpenXR.Extensions.FB admission (`FB_passthrough` env-blend), reusing the `Silk.NET.WebGPU` `Device` so one GPU lifetime spans flat and immersive.
- Wires the stereo frame as the one `viewport-pipeline.md#RENDER_GRAPH` projected through a per-eye `RenderTargetFactory`; the `XrViewpoint` round-trips the `viewport-pipeline.md#VIEWPOINT_CODEC` portable BCF view-state the `coordination/issue-board.md` board mints; the LiDAR measurement rides the `realitycapture/reality-capture.md#MEASURE_OVERLAY` anchor.
- Internal: the `.api/api-silk-openxr.md` catalogue carries the session/swapchain/frame-loop surface; the env-blend passthrough is BLOCKED until the manifest admits `Silk.NET.OpenXR.Extensions.FB` (the manifest-row delta this pass returns), and the live in-headset frame is SPIKE-gated on the host OpenXR loader exactly as the render-graph GPU passes are host-gated.

[T-PRESENTATION-PAGE] [QUEUED] — author `presentation/review-tour.md`, the narrated saved-viewpoint review tour
- Author the `presentation/` design page the codemap/router name but no file backs: the `ReviewTour` ordered `TourStop` sequence each binding a saved `Viewpoint`, per-stop dwell/transition off the motion tokens, and a narration-annotation track; the `TourPlayhead` camera interpolation on the animation `Track`/`Scrub` clock; the offline walkthrough render through the visuals export; and the `TopicTour` BCF-topic-set-to-stop-sequence projection.
- Integrate Thinktecture.Runtime.Extensions, LanguageExt.Core, NodaTime, SkiaSharp (walkthrough frame raster), reusing the settled owners rather than admitting a package.
- Wires the tour onto the `animation/animation-timeline.md` re-entrant-no-drift `Track`/`Scrub` playhead, the `motion/motion-tokens.md` reduced-motion easing, the `typography/typography-shaping.md` narration shaping, the `viewport-pipeline.md#VIEWPOINT_CODEC` saved view-state, and the `visuals/visuals-offscreen.md#DOCUMENT_EXPORT` offline render; the BCF topic source composes `Rasm.Bim` at the boundary exactly as `coordination/issue-board.md` does.
- Internal: no package admission — the tour composes settled owners; the offline walkthrough render is fence-complete now and the camera-interpolation easing traces to the motion-token vocabulary, never a tour-local clock.

[T-GPU-PROFILER] [QUEUED] — hardware-measured per-pass GPU-timing and pipeline-statistics timeline on the evidence page
- Add the `GpuTimingPass` timestamp-query and `PipelineStatistics` query cluster to `evidence/diagnostics-evidence.md` so the `FrameReceipt` per-pass GPU figure becomes resolved GPU nanoseconds rather than the encoder-projected accumulated cost the budget gate sums, and the `GpuTimeline` correlates the measured GPU seq against the projected CPU seq on the diagnostics evidence timeline keyed by the frame ordinal so a projection-versus-measurement divergence is itself attributable.
- Integrate Silk.NET.WebGPU (`DeviceCreateQuerySet`/`CommandEncoderWriteTimestamp`/`CommandEncoderResolveQuerySet`, `QueryType.Timestamp`/`QueryType.PipelineStatistics`), Silk.NET.WebGPU.Extensions.WGPU (non-blocking `DevicePoll` so the resolve completes without a blocking fence), Thinktecture.Runtime.Extensions, LanguageExt.Core, NodaTime.
- Wires the measured per-pass GPU times into the existing `evidence/diagnostics-evidence.md#PERF_BUDGET` `QualityVerdict` so the governor degrades the genuinely-overrunning pass on measured cost, and onto the `viewport-pipeline.md#RENDER_GRAPH` `FrameReceipt` per-pass fold as a GPU-side companion seq; render-hash lanes read the correlated timeline.
- Internal: the `.api/api-silk-webgpu.md` catalogue carries the query surface (`DeviceCreateQuerySet`/`CommandEncoderWriteTimestamp`/`CommandEncoderResolveQuerySet`/`QuerySetDescriptor`); the GPU timeline is one evidence-receipt projection, never a second telemetry surface, and the live resolve is SPIKE-gated on the host-shared GPU context.

[T-RESOLVE-LADDER] [QUEUED] — temporal-AA and super-resolution resolve ladder on the render graph
- Add the `ResolvePass` `[SmartEnum]` policy family (`Msaa`/`Taa`/`Fsr`/`Smaa`) to `viewport-pipeline.md` as a `RenderPass.Composite` policy column — the `Taa` camera-jitter-plus-motion-vector-reprojection with neighborhood-clamp history rejection, the `Fsr` sub-resolution spatial upscale under the VRAM budget, and the `ResolvePolicy` binding to the `QualityTier` so the perf governor drops resolve quality on the same hysteresis band.
- Integrate Silk.NET.WebGPU (compute-pass resolve, motion-vector buffer), SkiaSharp (`SKRuntimeEffect` SKSL resolve below the Ganesh factory), Thinktecture.Runtime.Extensions, LanguageExt.Core.
- Wires the resolve ladder onto the `viewport-pipeline.md#RENDER_GRAPH` `Composite` pass below the `RenderTargetFactory` column, the `evidence/diagnostics-evidence.md#PERF_BUDGET` `QualityTier` selecting the active row, and the `RESIDENCY_BUDGET` VRAM bound the `Fsr` sub-resolution render reads.
- Internal: the resolve is one `RenderPass.Composite` policy column and the motion-vector buffer is one `BindlessTable` slot, never a parallel post-process engine; fence-complete now and SPIKE-gated on the live host-shared GPU context.

[T-GPU-MANIFEST-COVERAGE] [QUEUED] — close the README-vs-manifest GPU-backend mismatch
- The `README.md` `[GPU_BACKEND]` registry claims `Silk.NET.WebGPU.Extensions.WGPU`, `Silk.NET.OpenXR`, `Silk.NET.OpenXR.Extensions.KHR`, and `Silk.NET.OpenXR.Extensions.EXT` and the `.api/api-silk-webgpu-wgpu.md`/`.api/api-silk-openxr.md` catalogues carry their member sets, but `Directory.Packages.props` carries only `Silk.NET.WebGPU`/`Silk.NET.WebGPU.Native.WGPU` — the four claimed-and-catalogued rows are absent from the manifest.
- Integrate the four already-catalogued Silk.NET packages plus the NEW `Silk.NET.OpenXR.Extensions.FB` (the immersive passthrough need) at the `2.23.0` line the core families already carry, into the one C# manifest.
- Wires: the manifest is single-owner and the orchestrator lands the rows from this pass's returned delta; this card authors the `.api/api-silk-openxr-fb.md` catalogue for the new FB admission and confirms the README registry lists all six Silk.NET GPU/XR packages plus the FB row.
- Internal: a manifest-claim-without-row is the coverage-audit defect; every Silk.NET row admits at one version because Silk.NET publishes its whole core-plus-extension set from one monorepo release, so the FB row carries no pin and rides the `2.23.0` line beside the core families.

[T-EMBED-PROBE] [HOST-PROBE-DEFERRED] — live host-shared embedding and GPU lease
- Resolve the `hosts/surface-hosts.md` and `viewport/viewport-pipeline.md` RESEARCH rows under a running integrated host: Avalonia-in-Rhino NSView embedding (pump coexistence, resize sync, render-backend contention), the seam `OnUiThread` access-assertion under the Rhino-owned AppKit run-loop, and the host-shared `GRContext` lease through `ISkiaSharpApiLease.TryLeasePlatformGraphicsApi` for meshlet/path-trace emit.
- Integrate the SkiaSharp GPU backend-context (`GRMtlBackendContext`/`GRVkBackendContext`) and `SKRuntimeEffect` over the leased context bound through one `SurfaceSeam` delegate column.
- Wires bind only at the app root that composes a live host; AppUi references the abstract seam columns, never a host API.
- HOST-PROBE-DEFERRED on the live RhinoWIP host surface and the host-owned GPU pipeline (external: an integrated running host does not exist); the CPU/2D-Skia fallback ships today, de-risked standalone against a windowed `GRContext`, and the `GpuBackend` `RenderTargetFactory` column the `T-BACKEND-PORT` realization landed gives the lease a backend-bound target seam to confirm in-host.

[T-INPUT-FABRIC-SDKS] [UPSTREAM-BLOCKED] — alternative-input device SDK spellings
- Re-ground the `input/input-interaction.md` `InputDevice`/`DeviceOutput` per-device member spellings — SpaceMouse HID, gamepad API, gaze SDK, switch-access scan, speech recognition, MIDI, and CNC/robot/haptic transport — and the `localization/#RTL_MIRRORING` `LiveCaption.Translated` recognizer/translator spellings.
- Integrate each device SDK and the speech-and-translation package as `SurfaceSeam` delegate columns bound at composition.
- Wires fold every device sample onto the one `CommandIntent` table and symmetric output over normalized `DeviceAxis`; SDKs cross only as composition delegates.
- UPSTREAM-BLOCKED on the host-bound device/recognizer SDKs (external: SpaceMouse HID, gamepad, gaze, switch-access, speech, MIDI, CNC/robot/haptic transports are not-yet-admitted bindings crossing only as `SurfaceSeam` composition delegates); the fabric vocabulary and the one `CommandIntent`/`DeviceAxis` fold are fence-complete now.

[T-EXPORT-WRITERS] [QUEUED] — Office and DWG/DXF emit writer sets
- Re-ground the `visuals/#DOCUMENT_EXPORT` `OfficeExport` OOXML part-graph member set and the `drafting/#DRAFT_EMIT` DWG/DXF model-space entity-writer set against the catalogued `.api/api-drafting-export.md` writer surfaces; the PDF/SVG arms are settled on the `SKDocument`/`SKSvgCanvas` rail.
- Integrate DocumentFormat.OpenXml (XLSX/PPTX/DOCX), and ACadSharp or netDxf for the DWG/DXF arm, reusing the `FlowFold` break algebra for deterministic pagination.
- Wires deliver through the one `VisualDestination`; the writer pins land at admission, never a per-format export surface.
- Internal: DocumentFormat.OpenXml, ACadSharp, and netDxf are already admitted in the one C# manifest (`Directory.Packages.props`) and referenced by `Rasm.AppUi.csproj`, and their writer member sets are catalogued in `.api/api-drafting-export.md` — there is no external manifest-admission blocker; the `OfficeExport`/`VisualExport` flow-fold and break algebra are fence-complete and the writer-body transcription is internal authoring work to land now.

[T-HEADLESS-PROOF] [QUEUED] — headless render-hash and journal-replay lanes
- Build the `evidence/#HEADLESS_DERIVATION` proof matrix, render-hash lanes, and command-journal replay as specs on the existing `Rasm.AppUi.Tests` node so they run under virtual time.
- Integrate Avalonia.Headless, Avalonia.Headless.XUnit, and Verify.XunitV3 for deterministic frame capture and `FrameHash` equality.
- Wires the headless `SurfaceHost.Headless` row through the deterministic `ForceRenderTimerTick` frames and the virtual `TimeProvider` from the test composition.
- Internal: the `tests/csharp/libs/Rasm.AppUi/Rasm.AppUi.Tests.csproj` node already exists on the test rail referencing AppUi and admitting `Avalonia.Headless.XUnit`/`Avalonia.Skia`/`Verify.XunitV3` (a `PackageAdmissionLaws` spec passes), and the Verify catalogue is already admitted in the one C# manifest — there is no external admission blocker; the `ProofEngine` derivation, the capture lanes, and the synthetic-input proof verbs are fence-complete and the proof/render-hash specs are internal authoring work to land now.

## [2]-[CLOSED]

[T-BACKEND-PORT] [COMPLETE] — `GpuBackend` SmartEnum gains the `GpuFamily` column and the `Wgpu`/`WebGpu` rows beside the `Metal`/`Vulkan`/`OpenGl`/`Software` Ganesh rows, the `RenderTargetFactory` owner binds a backend-provided target factory the `Lease` and every `RenderPass`/`CapturePass`/`CustomVisual` emit body consumes, and `visuals-offscreen.md#DRAW_CAPSULE` mirrors the factory; the `Wgpu` row binds the stable .NET Foundation `Silk.NET.WebGPU` wgpu/Dawn surface and the `Software` Skia Ganesh raster row is the shippable floor, Graphite re-admitting as one `SkiaGraphite` row the moment SkiaSharp ships its Recorder/Context surface; the archived VelloSharp/VelloSharp.Avalonia.Vello rows retired root-up with no shim, no Vello identity surviving in the manifest, README, or `.api/` (`viewport-pipeline.md#RENDER_GRAPH`).

[T-REALITY-CAPTURE] [COMPLETE] — `realitycapture/reality-capture.md` authored: `SplatSource`/`PointCloudSource` decoded off the Compute splat/point payloads, the `CapturePass` `Splat`/`Point` cases over the backend target factory, the LiDAR-anchored `MeasureOverlay` bound to the `Viewpoint`, and the `CaptureClip` on the animation playhead; the LAZ/SOG scan decode stays the Python companion's external producer crossing as a Compute payload ([CAPTURE_DECODE] upstream).

[T-WEB-VIEWPORT-WIRE] [COMPLETE] — `viewport-pipeline.md#TS_PROJECTION` deepened into the `ResidencyManifest` single-mint producer of the `WEB_GEOMETRY_RESIDENCY_WIRE` (geometry-residency/meshlet/splat tile wire shapes) the TS web leg consumes, each tile content-keyed by `XxHash128` off the same Compute `GeometryPayload` residency; single-mint invariant graded at the cross-libs master.

[T-COORDINATION-BOARD] [COMPLETE] — `coordination/issue-board.md` authored: `Issue` composing the AppUi `Viewpoint` and the `Rasm.Bim` `BcfTopic`/`BcfComment`/`BcfViewpoint` at the boundary, the `CommentThread` CRDT over the notebook replicated-document law, the `IssueTile` dashboard projection, and `IssueBoard` owning the issue-to-viewpoint binding and the BCF round-trip; BCF schema stays `Rasm.Bim`-owned ([BCF_TOPIC_SEAM] composed at the package edge).

[T-PERF-GOVERNOR] [COMPLETE] — `evidence/diagnostics-evidence.md#PERF_BUDGET` cluster adds the `QualityTier` smart enum, the `PerfSample` telemetry fold, and the `PerfBudget` governor folding the `FrameReceipt`/`ResidencyBudget`/HUD telemetry into one `QualityVerdict` degrading render passes, the residency watermark, and the motion tokens together under a hysteresis band so render-hash lanes stay attributable.

[T-NOTEBOOK-DETERMINISM] [COMPLETE] — the `CapabilityPin` composes the AppHost `DeterminismContext`/`EnvFingerprint` and `DeterminismKernel.Reproduces`, the `ReplayManifest`/`NotebookReplay.Verify` composes the `ContentHash` chain and `ReplayVerify` per-step proof through `ProofEngine.Replay`, and the `RecomputePlan` cell DAG aligns to the AppHost `RecomputeNode` content-address node identity ([NOTEBOOK_DETERMINISM] composed at the package edge).
