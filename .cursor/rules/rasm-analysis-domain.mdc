---
description: Rasm Analysis and Domain: shared kernel semantics, analysis rails, validation, stats, and geometry identity
globs: "libs/csharp/Rasm/Analysis/*.cs,libs/csharp/Rasm/Domain/*.cs"
alwaysApply: false
---

# Rasm Analysis And Domain

Read `libs/csharp/Rasm/AGENTS.md` before edits.

## Ownership

| [INDEX] | [FOLDER]    |
| :-----: | ----------- |
|   [1]   | `Domain/`   |
|   [2]   | `Analysis/` |

[OWNS]
- [1] `Context`, tolerances, geometry kind, coercion, ownership, validity, requirements, faults, stats, residuals, distributions, acceptance, and projection carriers
- [2] `Analyze`, `Operation<TGeometry,TOut>`, `IAspect`, and higher-order geometry analysis built on `Domain`

## Rules

- Extend existing `Domain` owners before adding local validation, stats, geometry-kind, or coercion logic.
- Keep `Domain` independent of `Analysis`, `Vectors`, and future concern folders.
- Route analysis through the unified `Analyze`/operation surface instead of per-file consumer APIs.
- Model new intent as typed records, smart enums, unions, or projection carriers when it reduces primitive knobs.
- Keep native interop converted at the boundary into typed rails and receipts.

## Validation

Run `uv run python -m tools.quality static check` for cleanup. Add `uv run python -m tools.quality static build` for compile/analyzer proof and `uv run python -m tools.quality test run` when behavior or test oracles change.
