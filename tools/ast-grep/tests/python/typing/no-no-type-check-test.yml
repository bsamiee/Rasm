id: no-no-type-check
valid:
    - |
      def decode(raw: bytes) -> Item:
          return msgspec.json.decode(raw, type=Item)
    - |
      from typing import TYPE_CHECKING
    - |
      skip = policy.no_type_check
    - |
      import typing

      @typing.no_type_check_decorator
      def load(raw):
          return raw
invalid:
    - |
      from typing import no_type_check
    - |
      from typing_extensions import no_type_check
    - |
      import typing

      @typing.no_type_check
      def load(raw):
          return raw
    - |
      import typing as t

      @t.no_type_check
      def load(raw):
          return raw
    - |
      from typing import no_type_check as skip
