# [REGIONS] — compute (final fragment)

Final region rows for `domain/compute.md`; the orchestrator consolidates this fragment into the shared region map.

[PAGE_REGION]:
- domain/compute.md: compute lane law — the tensor view algebra with three memory gates (owner, span, marshal), layout probes, broadcast and content-equality law; the `TensorPrimitives` kernel-row vocabulary with dtype acceleration classes, NaN and fused-triad columns, tolerance-class closure, and power-sum batching; the closed five-class staging axis with reset columns, consuming transfers, plane projections, and budget-floored parallel rows; the one-process stream pool with frozen options, tag-lane conservation folds, and discard taxonomy; the inference lane — `OrtValue` residency lattice, fingerprinted session admission with provider veto rows and warmup equivalence, `OrtIoBinding` zero-allocation loop posture, result-cache key identity; measured dispatch — typed intent family, substrate-row fold over veto/cost/cap/fallback columns, benchmark-gated claims under environment-fingerprint epochs with margin hysteresis, typed route receipts with dual evidence; the units boundary — canonical-unit admission, operator-derived cross-family algebra, frozen family tables, and the perceptual-to-physical tuning row.

[SNIPPET_REGIONS]:
- CM-01 TENSOR_GATE_AND_DENSE_ROUTE: three-gate memory admission into one tensor view vocabulary — rank-N coverage gate via `TensorPrimitives.Product` over the shape span feeding pinned uninitialized owner creation under a full-coverage write plan, the `TensorMarshal.CreateReadOnlyTensorSpan` marshal gate projecting pooled planes into the algebra without copy, stride-sharing range slice with destination-overload composition, and the `TryGetSpan` dense probe routing flat payloads to span kernels with the `FlattenTo` scratch bridge on pitched views. Distinct from A-11 (no finite-gate demonstration — composed as settled law) and L-07 (no ref-struct kernel grammar; library views only).
- CM-02 KERNEL_ROW_VOCABULARY: the primitives dispatch table as frozen kernel rows — method-group kernel identity under `nameof`-stable symbols, the NaN column as two declared rows (`Min` propagating beside `MinNumber` ignoring), the closed five-row tolerance-class vocabulary (exact, ULP-banded, accumulation-scaled, platform-variant, cross-platform-variant) whose accumulation-scaled bound derives from length and magnitude mass, the cancellation-ratio envelope, and power-sum batching deriving mean and variance from two passes. Distinct from S-04 (record rows with arrow fields, no `[UseDelegateFromConstructor]` column) and SD-06 (kernel-row semantics, not signature collapse).
- CM-03 STAGING_CLASS_AXIS: the closed allocation-class fold — stack-versus-scoped selection under one declared cap inside one rent expression with a lane-isolated `ArrayPool<T>` argument, `AllocationMode` as the trust-seam reset column, the consuming `MemoryOwner.Slice` clip-to-written transfer that kills the source owner, and the writer-then-owner handoff as the one legal two-stage lifetime. Distinct from B-02 (pooled transfer polarity, not borrowed/owned `SafeHandle` capsule) and R-05 (bracket grammar is supporting material).
- CM-04 STREAM_POOL_LEDGER: the one-process stream pool — frozen manager options record with bounded free-byte caps and the copy-out throw row, closed tag vocabulary naming lanes, lifecycle events folded into one pool fact stream, the per-tag conservation identity created = disposed + live + finalized proven by one single-pass fold, and discard-reason taxonomy steering opposite tuning verdicts. Distinct from CC-06 (pool-buffer conservation over manager events, not lane drain) and DG-02 (pool economy facts, not sink loss rail).
- CM-05 SESSION_ADMISSION_CHOREOGRAPHY: receipted model-session admission — provider rows vetoed against the probed capability set with degrade-to-next receipts, the session fingerprint minted from model hash plus every behavior-bearing option column, pinned free dimensions, and the warmup golden run whose equivalence defect gates publication; a fast wrong session is refused. Distinct from RT-05 (admission choreography, not rank fold) and RS-03 (veto/degrade rows, not registry claim).
- CM-06 ZERO_COPY_BINDING_LOOP: the steady-state inference loop — long-lived memory-backed `OrtValue` inputs refreshed by span writes over a pooled plane, pre-allocated outputs, `RunWithBinding` bracketed by unconditional device fences, in-place egress post-processing on the mutable output view under same-start aliasing, and disposal ordering where the value's dispose is the owner's release point. Distinct from B-02 (value-over-owner lifetime inversion, not handle borrow kernel).
- CM-07 SUBSTRATE_FOLD_AND_CLAIMS: the mass-engine capstone — one closed intent family whose dtype symbol and vector-width-derived length classes are recoverable from the payload, the budget-lane derivation record minting both per-class caps and the environment stamp (ISA, budget version, vector width) so one budget edit re-caps routing and stales claims at once, one total substrate-row fold over veto/cost/cap/fallback columns with the scalar reference as the unvetoable terminal row, the benchmark-claim gate keyed on (kernel symbol, dtype, substrate, length class) under fingerprint staleness and a margin floor, and the typed route receipt carrying taken row plus every vetoed row with its reason. Distinct from A-01 (column-table fold with claim gating, not operand-shape union dispatch), CC-02 (consumes the settled budget by derivation, no self-proving record), and RS-01 (route admission, not strategy ordering).
- CM-08 UNITS_CANONICAL_SEAM: the units boundary — non-throwing quantity admission converting once to the family canonical unit, dual evidence carrying the original (value, unit) pair beside the canonical scalar, `UnitMath` collection folds pinning the result unit, operator-derived cross-family algebra, and the perceptual-to-physical tuning row storing two knobs and deriving angular frequency, stiffness, and a quantity-typed damping rate as computed members. No adjacent region exists.
