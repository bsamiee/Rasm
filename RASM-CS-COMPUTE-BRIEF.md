# [RASM_CS_COMPUTE_BRIEF]

Campaign: cs track 3/6 — run after geometry + persistence. Single-phase: `Workflow(rebuild.js, {targets: "libs/csharp/Rasm.Compute", brief: "RASM-CS-COMPUTE-BRIEF.md"})`, legs per `[05]` (manifest motions stage by proof gate inside the legs); hard residuals between legs via `resolve-residuals.js`.

Authoritative campaign input for the restructure-and-harden of `libs/csharp/Rasm.Compute/.planning/` — the APP-PLATFORM measured-execution engine (41 pages, 7 folders: Analysis · Model · Runtime · Solver · Stats · Symbolic · Tensor). Upstream law honored throughout: `RASM-CS-GEOMETRY-BRIEF.md` over the CONVERTED kernel — the whole `Rasm` corpus lives under one `libs/csharp/Rasm/.planning/` root (34 landed pages standing law + the 18 geometry-campaign robust-core pages; fence namespaces frozen per the kernel `ARCHITECTURE.md` [03] matrix), and the kernel ledger declares exactly five kernel->Compute seams composed as settled law (`Rasm/ARCHITECTURE.md:78,82-83,86,93` — the `ContentHash.Of` identity entry, the `DiscreteCalculus` adjoint bundle, the `MeshAdjointSnapshot` handle, `SpatialIndex.ToAcceleration` node arrays, `EncodedGeometry`-as-`EncodedTensor`; Compute decodes the kernel acceleration/encoding wires, never re-implements kernel geometry), `RASM-CS-PERSISTENCE-BRIEF.md` (Compute reads the surviving cache/retrieval owners by reference; the deleted-owner seam rows re-point here), and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (Compute consumes `graph.SectionOf` -> `ComputedSection`/seam `SectionProperties` with ZERO structural ripple, `:72`). `docs/stacks/csharp/` governs unchanged and is never restated here.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is the inverse of its siblings: world-class INTERIORS under a stale, blind PERIMETER — and the world-class claim still dies on six proofs, each independently fatal:

1. **The assessment rail is blind to its own seam's lifecycle.** `Rasm.Element` ships a complete 8-row `AssessmentOutcome` lattice with `Dispatchable`/`Coherent`/`Next` columns, `Diagnostic.Of(phase, kind, message, key)`, `FailureKind.Transient`, and `AssessmentPayload.Failed` (`Rasm.Element/.planning/Assessment/assessment.md:70,104,122,158`), whose comments name "the Rasm.Compute sweep" and "a Diagnostic.Kind.Transient policy in Compute" as the consumer (`:59-60`); the Element TASKLOG carries `COMPUTE_ASSESSMENT_RUNNERS` QUEUED against this package (`Rasm.Element/TASKLOG.md:25`). `Analysis/assessment.md` consumes only `Computed`/`Pending`: CacheFirst blanket-short-circuits ANY `graph.Find<Node.Assessment>` hit to the noop regardless of the cached `Outcome` (`assessment.md:245-247` — a cached Failed/Stale/in-flight node reads as satisfied), the failure path writes NO `Failed` node (`:248-250` — `Run(...).Bind(WriteBack)` aborts, so a deterministic failure re-runs on every call and nothing caches), no `Outcome.Dispatchable` re-solve sweep exists, no `Transient` retry gate exists, and the `AssessmentWire` receipt carries no failure column (`Runtime/receipts.md:416`). The largest unwired seam in the corpus, and it is load-bearing.
2. **All four runners fail stringly, and the wire cannot carry their faults.** `StructuralAnalysis`/`BuildingPhysics`/`EnergySimulation`/`LifecycleAssessment` share the `Fin<AssessmentResult>` contract and all abort onto opaque interpolated `ComputeFault.AnalysisRunFailed` (`structural.md:285` `<bfe-solve-break:...>`, `:389` `<fealite-singular:...>`; `energy.md:279,299,318`) — no typed `SolvePhase`/`FailureKind` exists Compute-side, so the seam `Diagnostic` has nothing to compose. Meanwhile `Runtime/channels.md:9` projects a "seventeen-code taxonomy" over a fifteen-row rail while the `ComputeFault` band runs 2200-2220 — the analysis lane (2217-2219) and scheduling lane (2220) CANNOT cross the one `FaultDetail` family, violating the package's own `ARCHITECTURE.md` [05]#3 law.
3. **The seam ledger points at dead siblings.** `ARCHITECTURE.md:111` and `:119` target `Rasm.Persistence/Sync` (deleted by the Persistence campaign), `:115` targets `Query/pipeline` (deleted phantom), `:116` targets `Store/quality` (deleted; `clash.md:18` disclaims any producer), `:99` targets `Query/lane` (its `VECTOR_CODEBOOK` extracts to `Query/retrieval`), and `:120` asserts "Processing/solver consumes the Compute LM/constraint Solver rail" — disproven (the kernel LM is `Processing/solver.md`'s own policy-laddered engine over the landed `Numerics/matrix.md` owners; `RASM-CS-GEOMETRY-BRIEF.md` `[V14]` kills the reverse edge). The page bodies are FRESHER than the ledger (`channels.md:92` already cites the surviving `Version/ledger` `CHANGEFEED`) — governance decoupled from the realized corpus.
4. **Two confirmed interior defects ship inside 9-grade solver fences.** (a) The linear-elastic path hardcodes Poisson nu=1/3: `Elasticity(scale)` sets `lambda = scale, mu = 0.5 * scale` (`Solver/contract.md:65-66`) and `SolveProblem` carries one `materialScale` scalar with NO Poisson slot and NO `MechanicalOf` read (`:297-304`) — the declared elastic-D seam (`ARCHITECTURE.md:123`) is honored ONLY on the nonlinear `ElasticPrimal` path (`contract.md:1024`, correct 2-parameter form), so every fea-static/modal/buckling/transient solve runs a wrong constitutive tensor while the seam sits unwired. (b) `SolveProblem.ContentKey` hashes a `CultureInfo.InvariantCulture`-formatted interpolated string through a per-call-site `XxHash128` (`:304`) — the exact form the Model/identity seam forbids (`ARCHITECTURE.md:100`) and the corpus's own canonical-bytes keys (`factor.md` `ShardPlan.Digest`, `expression.md` `ContentKey`) prove wrong.
5. **The volumetric mesher re-implements kernel geometry with float predicates.** `DelaunayCore` hand-rolls Bowyer-Watson over non-robust float `Orient`/`InSphere`/`Det4` (`Solver/discretization.md:585,617,913-921,962`) — the exact adaptive-exact-predicate concern the kernel crowns and `libs/.planning/architecture.md:54` forbids consumers to re-own; near-coplanar/cocircular configurations (the COMMON case in building geometry) flip signs and emit non-Delaunay/inverted cells the FE solve then trusts. No discretization->Geometry predicate seam is declared. The page also mints a local `record MeshSpace` (`:373`) name-colliding with the kernel Vectors `MeshSpace` its own prose composes (`:3,18`; the frozen-name owner is the landed `Rasm/.planning/Meshing/mesh.md`).
6. **The roster and the catalogs describe different systems, and the domain roster is a thin slice.** `FParsec` is a DIRECT admission (`Rasm.Compute.csproj:28`) the design itself disowns ("consumed transitively ... never referenced directly", `Symbolic/expression.md:5,22,101`; `api-mathnet-symbolics.md:18,52`); `Microsoft.ML.OnnxRuntime.DirectML`/`.Gpu` (`csproj:47,49`) ship ZERO realized `ExecutionProvider` rows (`providers.md:12` — Cpu+CoreMl only; `:173` a growth note) and ZERO osx-arm64 payload (`api-onnxruntime.md:122-131`); `SharpGLTF.Ext.3DTiles` (`csproj:71`) is admitted directly while its catalog documents none of its surface and names the stacking Core-only (`api-sharpgltf.md:491`); `LanguageExt.Core` — the ROP rail of all 41 pages — has NO catalog in either tier; 9 cross-tier catalogs are md5-identical copies (1,971 LOC of drift-guaranteed duplication — the figure is exact) and EIGHT more have already diverged in the base body (csparse, hashing, hybrid-cache, mathnet-providers, nodatime, tensors, thinktecture-json, unitsnet — md5-confirmed forks, not overlay deltas). And on COVERAGE: the Element seam declares FIFTEEN `Discipline` rows (`Rasm.Element/.planning/Classification/classification.md:170-185`) — Compute runs SEVEN; `Daylight` is a seam row with no runner, circulation/egress (life-safety, code-mandated) has no row anywhere, `daylight-radiosity` is one FEM row not a discipline (`contract.md:35`), zero solar-position computation exists corpus-wide, the only eigensolver is dense EVD (self-flagged: "never a sparse LOBPCG/Lanczos it lacks", `contract.md:733,104`) capping modal/seismic/buckling at toy DOF, and the corpus admits Jacobians everywhere while hand-rolling finite differences ("the autodiff-tape-absent fall", `Stats/estimator.md:574,582`).

What IS sound (preserve; harden ONLY per the verdict that names it — treating these as greenfield teardown would destroy award-grade interiors): the 25-case `ComputeReceipt` union and fold-projection family, the `ComputeFault` 2200-band custody, the `JobGraph` incremental engine (`scheduling.md:183-198` — `JobState` Faulted/Spilled/Resumable, content-key `Reconcile`, `CheckpointPort` spill/resume: exactly the substrate the V1 sweep composes), the 11-kernel `Optimizer` with genuinely distinct CMA-ES/PSO/SA/Bayesian-GP, the full Wiener-Askey UQ with HLRF/Breitung/Au-Beck, the Joe-Kuo `LowDiscrepancy` owner, the 196-row `TensorOpFamily` and dual-mode AD `Sensitivity`, the OrtValue-only Model plane, the EN/ISO closed-form physics kernel, the EnergyPlus subprocess discipline, the EC3 basis-tagged `Normalize`, the AISI-aware capacity table's design-code delegates, `GeoArrowBuffer.ToImportedGeometry` (mandate item 6 RECORDED — the GLB wire layout the python:data GeoArrow buffers share, `Runtime/codecs.md:59`, `TASKLOG.md:56`, `libs/python/data/ARCHITECTURE.md:54` — never re-mint), and the zero-stale-paradigm state (mandate item 4 CLEAN — grep `ComponentSection` is empty; every Analysis page consumes the post-DECISION `graph.SectionOf` twenty-column seam).

### [TELOS]

`Rasm.Compute` is the federation's measured-execution engine: the discipline-assessment authority over the concrete `ElementGraph` (every seam-declared discipline answerable, every verdict cached by content key, every failure a typed cached fact with a retry policy — a farm of solvers whose failure semantics are as engineered as their success semantics), the numeric substrate (dense/sparse/factored/spectral/quadrature/sampling at building scale — 10^4-10^6 DOF modal and buckling, exact-predicate meshing, exact AD gradients), the optimization/UQ/DOE spine over one `evaluate` oracle, the symbolic lane with real CAS depth, the ONNX model plane, and the admit-to-receipt runtime boundary whose wire carries every fault family. Every owner grades against the 5x consumer bar: the assessment rail feeds generated-design screening loops that re-solve thousands of variants (failure caching and `Dispatchable` sweeps are throughput law, not hygiene), the numeric spine feeds Generation form-finding and Fabrication verification, the wire feeds remote farms. A discipline the seam declares and Compute cannot answer is a coverage defect; a failure that re-runs deterministically is a correctness defect; zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The legs own the page-set within the verdict envelope: split any page named by `[V13]`, mint the `[V12]` discipline pages, extract owner blocks, re-home concerns across the seven folders — every structural change updates `ARCHITECTURE.md` `[01]`-codemap/`[02]`-seams, the `README.md` `[01]-[ROUTER]` and package groups, and the TASKLOG/IDEAS cards whose anchors it moves, in the same motion. The folder-architecture law binds and the seven-folder partition is RATIFIED — Analysis(6)/Model(7)/Runtime(7)/Solver(6)/Stats(2)/Symbolic(4)/Tensor(9) are all >=2-page growth axes with zero one-file folders and zero flat sprawl; NO new top-level folder is warranted by any verdict (a new discipline is one `Analysis/<name>.md` page + one seam `Discipline` row + one `AssessmentRoute`; a numeric enabler is one row on an existing Tensor/Solver owner). The one graded structural defect is page-granularity asymmetry inside Runtime (`[V13]`). The post-campaign page-set is sealed so no leg guesses a name: Analysis 6->8 (`circulation.md`, `daylight.md`), Solver 6->7 (`satisfy.md` — the `[V12]` Z3 owner; 8 only if the `[V13]` criterion extracts `constitutive.md` from `contract.md`), Runtime 7->8 (`channels.md` dies into `wire.md` + `transport.md`; 9 only if the codecs criterion yields `interchange.md`), Model 7 (`extension.md` KEEP is the `[V13]` ruled default; 6 only if the leg-4 cold pass overturns it), Stats 2, Symbolic 4, Tensor 9 — 45 pages ruled, 47 at the criterion ceiling, the same seven folders, every folder >=2 pages. No page survives on incumbency, and no page falls to it either: a 9.5-grade fence is rebuilt only where a verdict names its defect.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md:35,54`): Compute is APP-PLATFORM, depending UP on `Rasm` (kernel) and `Rasm.Element` (the seam) only, PORT-peering with `Rasm.Persistence`/`Rasm.AppHost`/`Rasm.AppUi`, and NEVER referencing an AEC peer (`Rasm.Materials`/`Rasm.Bim`/`Rasm.Fabrication`) except at the companion-rpc wire. Consequences sealed here: the mandate's `SectionProfile`->capacity-overload dispatch CANNOT live in the Compute structural resolver (`[V4]`); Compute consumes kernel geometry (predicates, acceleration wires, encoded payloads) and never re-owns it (`[V7]`); the seam vocabularies (`Discipline`, `AssessmentOutcome`, `SolvePhase`, `FailureKind`, `Diagnostic`, `AssessmentPayload`) are Element-owned and composed, never re-declared ("a Compute-local discipline enum is the deleted form", `Analysis/assessment.md:18`). The existing `ARCHITECTURE.md` [04]-[SEAM_PROHIBITIONS] and [05]-[PROHIBITIONS] blocks carry forward unamended.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — every roster is `[SmartEnum]`/`[Union]` + FrozenDictionary seed DATA with a generated total Switch — so the law binds the NEW planes: runner failure classification is `(SolvePhase, FailureKind)` rows lowered onto the seam `Diagnostic`, never per-runner string formats; the EC2 shear pairing is one capacity-table row + one context extension, never a parallel check family; new disciplines are one seam row + one route + one runner page over the existing spine (the `ComputeReceipt.Assessment` partial already absorbs them); execution providers are rows with realized consumers or the packages leave; sparse-eigen and native-direct are rows on the existing `FactorKind`/substrate axes mirroring blas.md's managed<->native pattern; AD is a leg on the ONE `Sensitivity`/`Chain` family (geometry tape + symbolic tape + hyperdual — never a fourth parallel gradient mechanism beside the finite-difference fall it deletes). The test: the next discipline, provider, eigensolver, join, or failure kind is one data row, never a new arm, a second rail, or a stringly format.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — the corpus norm (`Analysis.Assess`, `Solve`, `Optimize`, `Propagate`, `Transform.Apply`), sealed for every new owner; the `[V1]` `Sweep` is a second Analysis-rail ENTRY on the one spine (admission.md confirms discipline lanes are self-contained entries, not `ComputeIntent` cases), never a parallel dispatcher.
- Every fault is a typed `ComputeFault` case in the 2200-band custody (`admission.md` owns the map; next-free 2221); a stringly interpolated detail where a typed case belongs is the deleted form (`[V2]` kills the four live breaches); every band case crosses the wire through the one `FaultDetail` family (`[V3]` restores the law).
- Lifecycle is seam law: the persisted `Node.Assessment` carries the seam `AssessmentOutcome`; Compute reads `Usable`/`Terminal`/`Dispatchable`/`Coherent` columns and flips through `Next()` adjacency — a Compute-side lifecycle enum or a blanket cache-hit that ignores `Outcome` is the deleted form; and Compute OWNS the flips the seam assigns it — stale-marking (`IsStaleFor` -> `Advance(Stale)`), supersede close-out, orphan-cancel (`Element assessment.md:229-236`) — a sweep that only dispatches is half the contract.
- Content identity composes the kernel seed-zero `XxHash128` rail (`ContentHash.Of` — the landed `Rasm/.planning/Domain/identity.md` `ReadOnlySpan<byte> -> UInt128` federation entry / the seam `CanonicalWriter`) over canonical bytes — never a per-call-site hash over a formatted string (`[V6]`b kills the sole breach; the kernel ledger pins the law on Compute by name, `Rasm/ARCHITECTURE.md:78` mirroring `ARCHITECTURE.md:100`); the policy-seeded `InterchangeIdentity` stays DISTINCT and never conflates.
- Cross-package member claims verify or die, each against the instrument that owns it: planning-scoped sibling members READ THE LANDED FENCES — the Element seam rows (`AssessmentPayload.Failed`, `Diagnostic.Of`, `AssessmentOutcome.Dispatchable`) against `Element assessment.md`, every kernel member against the landed `Rasm/.planning/` pages plus the `.api` catalogues (the converted kernel is source-less; `assay api` has no kernel or Element assembly to decompile and is never this campaign's verification spine for them); restored external packages verify through `uv run python -m tools.assay api` (`HyperJet` `DDScalar` members, `Microsoft.Z3`, the OrTools `Google.OrTools.Graph` flow members); vendored sources verify against the vendored tree with the catalog authored at admission (csparse-interop ARPACK bindings, cslsqp). A member no instrument verifies is a phantom.
- Frozen fixtures are harness-confirmed, never asserted: the `CLASH_GOLDEN` 160-byte two-sided fixture (`clash.md:387`) re-proves against actual kernel `NodeLinkProjection` output (the landed emit site: `ToAcceleration` at `Rasm/.planning/Spatial/index.md:572-575`) when the Geometry campaign lands its `[V8]` index rebuild, before any page cites it as REAL.
- Recorded consumer pressure (`RASM-CS-APPUI-BRIEF.md` `[V5]`, AppUi the demanding consumer): `Runtime/payload.md`'s `meshlet-cluster` row extends to a cluster-LOD chain — per-cluster object-space error + parent-error columns on the `ResidencyMeshlet` descriptor, minted by the meshopt simplify ladder the `SimplifyTarget` policy already drives, under a MONOTONIC cut guarantee (parent error >= child error) so a screen-space error cut is crack-free and double-draw-free by construction; AppUi reads the encoded columns for its hysteresis LOD cut and never re-clusters.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped; never `dotnet add`) + `Rasm.Compute.csproj`; per-package catalogs under `libs/csharp/Rasm.Compute/.api/` (37), shared substrate under `libs/csharp/.api/` (30). Manifest motion is staged by gate, never one blanket leg: the UNCONDITIONAL motion lands in leg 1 (FParsec/DirectML/Gpu removals; HyperJet + Microsoft.Z3 admissions with stubs); the CRITERION-GATED motions execute in leg 2 where their probes run (`[V11]` CAS swap — AngouriMath in, MathNet.Symbolics + FSharp-weight out — only after the determinism probe holds; `[V8]` BFE/FEALiTE retirement only after the parity fixtures prove); vendored sources (`csparse-interop`, `cslsqp`, contingent `RobustGeometry.NET`) land with their leg-2 first-compose; the leg-4 folder admissions (QuikGraph/NTS/Clipper2 csproj rows) land with the runner that first composes them, central pins unchanged. Carried obligations: REMOVE `FParsec` as a direct reference (transitive under MathNet.Symbolics; dies entirely with `[V11]`); REMOVE `Microsoft.ML.OnnxRuntime.DirectML` + `.Gpu` with catalogs (zero realized rows, zero osx-arm64 payload; re-admission requires the deployment that demands the `ExecutionProvider` rows in the same motion); KEEP `MathNet.Numerics.Providers.MKL` as `blas.md`'s RID-claim-gated x64 `LinearProvider` row — the kernel conversion dropped its own MKL reference (x86-64-only, cannot load on osx-arm64) and flagged Compute's as sibling-roster debt; the disposition is that the claim gate IS the design (osx-arm64 resolves managed/OpenBLAS, an x64 deployment claims MKL), recorded here so the conversion flag closes; RESOLVE `SharpGLTF.Ext.3DTiles` ownership (`[V15]` — ruled default: it IS Compute's, `codecs.md` TILE_PARTITION is the live tileset emitter; catalog the `Tiles3D` surface Compute-side and correct `api-sharpgltf.md`'s scope); REPLACE `MathNet.Symbolics`+`MathNet.Numerics.FSharp`+`FSharp.Core`-weight with `AngouriMath` per `[V11]`; ADD `HyperJet` (ISC, net10, NuGet-published), `Microsoft.Z3` (MIT; the NuGet stable 4.12.2 ships win-x64/osx-x64 natives ONLY — nupkg-verified, NO osx-arm64 — so the netstandard2.0 managed wrapper admits with a Forge-provisioned arm64 `libz3` from the upstream 4.15.x arm64-osx release, the same activation-gate law as csparse-interop), `csparse-interop` (BSD-3, source-vendored from the live `wo80/csparse-interop` — same author as the admitted CSparse), `cslsqp` (ISC — vendored source until its NuGet release lands); RETIRE the four frame-backend rows `BriefFiniteElement.Net`/`BriefFiniteElementNet.CustomElements`/`FEALiTE2D`/`FEALiTE2D.Plotting` (`csproj:33-36` — the exact manifest ids; `BFE.CustomElements` is not a real package id) from the runtime graph per `[V8]`'s parity gate, the retirement taking its central transitive-floor pins in the same motion (`BriefFiniteElementNet.Common` `PROPS:470` under the BFE pair; `netDxf.netstandard` `PROPS:472` under `FEALiTE2D.Plotting` — lock-verified sole parents; the Fabrication brief's residue check depends on this sweep); AUTHOR the absent `api-languageext.md` at the substrate tier (LanguageExt.Core `5.0.0-beta-77` is the rail every fence composes and no catalog pins its moving v5 surface) — a track-shared authoring: the first campaign to run lands the ONE substrate file, later campaigns verify, never re-author, and the shared-tier dedupe/reconcile motions below ride the same track law over whatever the earlier Geometry/Persistence legs already landed; DEDUPE the 9 md5-identical cross-tier catalog copies to substrate-canonical + one-line pointers and RECONCILE all EIGHT divergent pairs (`api-tensors` — substrate richer than the domain overlay, backwards; `api-unitsnet` — 310 diff-lines; `api-csparse`/`api-hashing`/`api-hybrid-cache`/`api-mathnet-providers`/`api-nodatime`/`api-thinktecture-json` — domain copies forking the base body) to one canonical base + a Compute-delta overlay each; UNIFY the MathNet tiering (core catalog in substrate, providers/symbolics satellites in the domain tier — one tier for the stack) — and the central `MathNet.Numerics` pin rides the Geometry `[ROSTER_RECONCILIATION]` 5.0.0 downgrade ruling: the leg-2 numeric-spine fences verify against the downgraded surface, a fence-composed 6.0-only member is the recorded bump blocker reported upstream, never a silent local re-pin; GATE the `api-microsoftaspnetcoretesthost.md` precedent explicitly (the single sanctioned production-seam-shaped-for-test overlay for the `RemoteTransport.InProcess` slot — test packages never proliferate into domain tiers). License gate: OSS and free-for-OSS-commercial admissible; GPL/AGPL/pay-tiered/seat-licensed REJECTED — recorded rejections never re-proposed: TetGen (AGPL), Gmsh (GPL), CGAL (GPL), Epanet.Net (GPL — the Todini global-gradient algebra is freely implementable), MSolve/MGroup (NO license file — monitored only), Numerics.NET/NMath/ALGLIB-commercial/Extreme (commercial), Accord.NET (archived 2020), ML.NET (AutoML-oriented, weaker than the TorchSharp substrate), CMAES.NET (redundant — `optimizer.md` already runs a faithful CMA-ES over the lane's own Evd), in-process ANN/HNSW (the retrieval-index concern is Persistence's by contract — `embedding.md`'s two-stage rail is honest and stays).

## [01]-[CAMPAIGN_VERDICTS]

The legs must land every verdict below. Recommended shapes follow each; a leg may exceed them, never dilute them.

### [V1_FAILED_LIFECYCLE_SPINE]

The campaign-defining realization: `Analysis/assessment.md` rebuilds to consume the FULL seam lifecycle. Four legs on the one spine, composing — never re-minting — the seam vocabulary: (a) lifecycle-aware CacheFirst — the `graph.Find<Node.Assessment>` hit inspects the cached payload's `Outcome` through the seam columns before short-circuiting: `Computed`/readable-`Stale`-when-policy-allows noop, `Failed` returns the cached failure receipt (deterministic failures cache), in-flight `Queued`/`Running` returns a typed in-flight verdict, `Dispatchable` rows recompute; (b) the Failed write-back leg — a runner failure lowers through `Diagnostic.Of(phase, kind, message, key)` into `AssessmentPayload.Failed(discipline, route, inputKey, diagnostic, provenance)` persisting a Failed `Node.Assessment` under the SAME content-keyed id, while admission/infrastructure faults stay rail-only and never cache; (c) the retry gate — a cached Failed whose `Diagnostic.Kind.Transient` is true re-dispatches under the policy row; a non-transient Failed re-runs only through an explicit Pending re-request (the seam's own law, `Element assessment.md:59`); the gate is BOUNDED, never an unbounded re-dispatch loop: backoff derives from the cached failure's `Provenance.At` age, and because the seam payload carries NO attempt ordinal (`Provenance` at `Element assessment.md:146` is Author/Tool/Version/At/Elapsed/Window/Correlation only), a capped-attempts policy is unenforceable on the seam as it stands — the attempt ordinal on the Failed write-back is a NAMED Element counterpart obligation (one audit-axis column the content key never folds, exactly the `Provenance` shape), recorded with Compute as the demanding consumer; until it lands the gate runs At-age backoff and states the cap gap inline; (d) the `Sweep` entry — a second Analysis-rail entry dispatching every `Outcome.Dispatchable` row (Pending/Stale) over the target set and structurally skipping Queued/Running, COMPOSING the `Runtime/scheduling` `JobGraph` (content-key `Reconcile`, `JobState` lifecycle, `CheckpointPort`, fair-share) as its execution substrate — a parallel re-solve engine is the deleted form. The Sweep is a RECONCILER, not a dispatcher alone — three legs the seam's own adjacency assigns Compute ("the next Bake surfaces a Stale assessment the Compute sweep re-dispatches", `Element assessment.md:229-236`): (1) STALE-MARKING — before dispatch, the sweep folds baked `Node.Assessment` rows against the CURRENT graph, recomputes each row's input key through the one content-key fold, and flips `IsStaleFor`-true Computed rows to Stale through `Advance` (the legal `Computed -> Stale` edge, `:232,:239`) — without this fold the sweep finds zero Stale rows forever and the re-solve flagship dies at its first link; (2) SUPERSEDE CLOSE-OUT — a re-solved row writes the fresh node under the fresh key and flips the drifted predecessor Superseded in the SAME delta, so exactly ONE usable node survives per `(discipline, route)` — the seam's own one-usable-node law; (3) ORPHAN RECOVERY — a Queued/Running row whose content key resolves to NO live `JobGraph` job (the `Reconcile` content-key check) is a dead worker's wreckage: the sweep flips it Cancelled with the abort `Diagnostic` (`(Solve, Resource)` — transient) and re-dispatches through the Pending edge, so one worker death never wedges an assessment behind the in-flight skip forever. `JobState` (job lifecycle) and `AssessmentOutcome` (node lifecycle) stay orthogonal, mapped at the sweep boundary only. The `ResultBlob` artifact sink threads in the SAME signature motion (`assessment.md:107,114` and `energy.md`'s eplusout.sql both self-flag the pending injected sink) — one `Assess`/`Run`/runner signature change, not two — and the sink's LANDING is named, never left as an injected abstraction: the content-keyed blob rides the Persistence blob lane and registers through `ArtifactIndexRow.Admit` under an assessment `ArtifactKind` row (one data row per the Persistence growth law, `cache.md:20`) so every heavy artifact is retention-governed and reusable, never an orphan blob no index owns — the in-corpus proof shapes are the `Model/sessions` EpContext row (`ARCHITECTURE.md:101`) and the scheduling SPILL row (`:106`); the `ArtifactKind` row rides as consumer pressure on the Persistence brief's `cache.md` sound-surface row with Compute the demanding consumer, re-recorded in the leg return. `ComputeReceipt.Assessment` gains the failure columns (phase/kind/transient beside verdict) with the `AssessmentWire` projection (`receipts.md:416`) widening in the same motion. Element's `COMPUTE_ASSESSMENT_RUNNERS` card closes against this verdict.

### [V2_RUNNER_TYPED_FAILURE]

The four runners surface typed failure UNIFORMLY — one refinement across the shared `Fin<AssessmentResult>` contract, not four bespoke rebuilds. The stringly `ComputeFault.AnalysisRunFailed($"<...>")` arms (`structural.md:285,389`; `energy.md:279,299,318`; the physics fire-route and lifecycle EC3 arms) die for one typed case carrying the SEAM's vocabularies:

```csharp
// ComputeFault analysis lane (2217-2219 custody), declared in assessment.md's existing partial-extension block
// (assessment.md:219-223) — a nested case reaches the private (detail, code) base ctor; a top-level record beside
// the [Union] is the deleted form. Inherits AnalysisRunFailed's 2219 slot (2217/2218 stay AssessmentInputMissing/
// ToolchainUnresolved); the admission.md:173 custody row renames in the same motion. SolvePhase/FailureKind are
// the Rasm.Element seam rows — a Compute-local phase/kind enum is the deleted form; Code carries the foreign
// exit/status code into the seam Diagnostic's own Option<int> slot, never smuggled through the message.
public abstract partial record ComputeFault {
    public sealed record AnalysisFailed(SolvePhase Phase, FailureKind Kind, string Detail, Option<int> Code = default) : ComputeFault(Detail, 2219);
}
```

Classification is row data per runner over the VERIFIED seam rosters — `SolvePhase` is exactly `Admission|Solve|Extraction|Publication` (`Element assessment.md:92-99`) and `FailureKind` exactly `Input|Numeric|Resource|Timeout|Aborted|Foreign` with `Transient` a derived COLUMN true only on `Resource`/`Timeout` (`:104-115`); a mapping onto any other token is a phantom seam member. The corrected rows: structural maps assemble/factor/recover breaks onto `(Solve, Numeric)` and missing section/support inputs onto `(Admission, Input)`; energy maps toolchain discovery onto `(Admission, Resource)` (the seam's own missing-binary transient), subprocess exit onto `(Solve, Foreign)` with the exit code riding `Diagnostic.Code` (`Option<int>`, `:122-141`), and eplusout.sql absence onto `(Extraction, Foreign)`; physics maps admission gaps onto `(Admission, Input)`; lifecycle maps EC3 transport onto `(Admission, Timeout)` with the HTTP status in `Code` — the exact rows are the leg's to fix against those rosters; the law is that every arm names its phase and kind, `Resource`/`Timeout` are chosen ONLY where re-dispatch can clear the cause (network, worker death, toolchain race — a singular matrix is `(Solve, Numeric)`, deterministic and cached), and the interpolated detail survives as the message payload, never the discriminant.

### [V3_FAULT_WIRE_PARITY]

`Runtime/channels.md`'s `FaultDetail` projection re-derives from the band: the "seventeen-code taxonomy" prose and fifteen-row `StatusRail` (`channels.md:9`) become a band-complete rail covering 2200-2220 with the `[V2]` case in its 2219 slot — the analysis lane (2217-2219) and `GraphCyclic` (2220) cross the wire, and the `StatusCode`->`WireFault` fold stays total by construction (a band case without a wire row fails the build, mirroring the receipts registry discipline). Growth law: a new `ComputeFault` case is one band row + one wire row in the same declaration motion — the count-vs-band drift class this verdict kills cannot recur. The Remote `WireFault` 4520-4532 wire sub-band (`channels.md:205,218` — distinct from the 2200 band and from AppHost `HopFault` 4500) is RECORDED beside the `admission.md` custody map as Compute's second custody and is PINNED as a mirror row in the AppHost/AppUi/Persistence registries (`RASM-CS-APPHOST-BRIEF.md` `[V1]` re-bands `CoordinationFault` to 4540 around it) so cross-package disjointness is checkable from both ends; the custody map additionally carries the reciprocal PINNED foreign-neighborhood rows (AppHost 1xxx/4100-4810 with the 4540 `CoordinationFault` re-band, AppUi 6xxx, Persistence 5xxx/771x/82xx-83xx, the AEC 23xx-27xx registry) exactly as those registries pin Compute's two custodies — the mirror discipline is symmetric, a foreign band change is a row edit on both ends, never prose; the `[V13]` split carries the custody note into `wire.md`.

### [V4_SECTION_DISPATCH_STRATA_RULING]

Mandate item 2 is reframed as strata law — the stated shape ("Catalogued -> rolled `SteelDesign.Capacity`, ColdFormedC -> `ColdFormedSections.Of` -> the cold-formed Capacity overload" IN the Compute structural resolver) is architecturally FORBIDDEN: `SectionProfile` is a `Rasm.Materials`-authoring union (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:234`), Compute reads the arm-erased resolved `graph.SectionOf` -> `ComputedSection`/seam `SectionProperties` (DECISION `:72` rules Compute ZERO structural ripple), and an APP-PLATFORM -> AEC-DOMAIN reference violates the sealed prohibitions. RULED: the `Catalogued`/`ColdFormedC` overload dispatch is Materials-interior; the cold-formed EFFECTIVE section bakes onto the twenty-column seam Materials-side, and Compute's `aisi-s100` cells (`structural.md:621-623` — today the gross-section bound, effective-width explicitly deferred at `:466`) read the seam-baked effective properties FLAT, gross-vs-effective invisible to Compute. Compute-side action this campaign: the AISI cell boundary prose corrects to name the seam-baked effective-section read, and the Materials counterpart (bake `ColdFormedDetail` effective properties into `ComputedSection`) is recorded as a counterpart obligation with Compute named as the demanding consumer — the forbidden downward edge is never authored, by any leg, ever.

### [V5_EC2_MEMBER_SHEAR]

Mandate item 3 lands as one capacity-table refinement: Materials' RC screen carries `RcElastic.ShearLinkAreaMm2` (`Rasm.Materials/.planning/Component/capacity.md:144,380`) and returns only the section-decidable web-crushing ceiling `V_Rd,max`, explicitly deferring the member term ("the member V_Rd,s = (Asw/s)·z·f_ywd·cotθ needs the stirrup SPACING the RcSection does not carry, so a linked pass DEFERS detailing to the forward Compute member", `:253-254,262`). The Compute `en1992` shear cells (`structural.md:636` — plain `ConcreteVc` today) gain the truss-model pairing: for a linked section the governing resistance is `min(V_Rd,s, V_Rd,max)` with `V_Rd,s = (Asw/s) · z · f_ywd · cot(theta)`, reading `Asw` off the seam-baked RC properties, the member stirrup spacing `s` off a `CapacityContext`/`StructuralPolicy` extension (member-scope input, exactly why Materials defers), `z = 0.9d`, and `cot(theta)` as a policy row defaulting 2.5 (matching the Materials ceiling's assumption so the pair is consistent by construction); the linkless arm keeps `V_Rd,c`. One table row family + one context extension + one policy row — no new owner, no parallel check rail.

### [V6_CONTRACT_ELASTICITY_AND_KEY]

The two confirmed `Solver/contract.md` defects land as repairs with the page's harden pass: (a) `SolveProblem` reads `ElementGraph.MechanicalOf(NodeId)` at admission (or carries `(E, nu)` explicitly), `PhysicsKind.Material` takes both parameters, the `Elasticity(scale)` nu=1/3 collapse (`:65-66`) and the lone `materialScale`/`materialShift` erasure (`:301`) die, the ONE isotropic elasticity-D is the correct 2-parameter form `lambda = E·nu/((1+nu)(1-2nu)), mu = E/(2(1+nu))` the nonlinear `ElasticPrimal` already proves (`:1024`), and `GeometricStiffness` inherits the corrected D — the declared-unwired `ARCHITECTURE.md:123` seam wires on the linear path; (b) `SolveProblem.ContentKey` (`:304`) composes the kernel `ContentHash.Of` (the landed `Rasm/.planning/Domain/identity.md` seed-zero entry; the kernel ledger names Compute on exactly this law, `Rasm/ARCHITECTURE.md:78`) over canonical bytes (physics key, element key, condition count-prefixed digests, material parameters as raw doubles), killing the culture-formatted interpolated string — `factor.md`'s `ShardPlan.Digest` is the in-corpus proof shape.

### [V7_EXACT_PREDICATE_FLOOR]

`Solver/discretization.md`'s predicates compose an exact floor; the hand-rolled Bowyer-Watson TOPOLOGY stays owned (the 3D-mesher ecosystem is copyleft — TetGen AGPL, Gmsh GPL, CGAL GPL — which justifies the mesher, never the float predicates). RULED default: the in-sphere/orientation tests route through the kernel's exact predicate surface via a NEW declared `Solver/discretization -> Rasm Numerics/predicates` seam row (the landed `Rasm/.planning/Numerics/predicates.md` `Predicate` family — `Orient2D`/`Orient3D`/`InCircle`/`InSphere` plus the `OrientLPI`/`OrientTPI`/`InCircleLPI`/`InSphereTPI` implicit-point tier over the `PrecisionTier` `Adaptive.Resolve` ladder; the kernel ledger already carries the cross-package predicate-wire proof shape at `Rasm/ARCHITECTURE.md:89`, Fabrication <- `Predicate.Orient2D/Orient3D`) — Compute already depends up on the kernel, the Geometry campaign hardens `predicates.md` as its reference floor, and `libs/.planning/architecture.md:54` makes the kernel the only legitimate owner; deciding criterion: the export is consumable ONLY as an entry that puts no host geometry type on a Compute lane signature (Compute's own `ARCHITECTURE.md` [04]#2 — the settled fence's `Point3d`-typed entries fail that test, so the geometry `[V14]` export is the coordinate-level adaptive-filtered family, fenced as public surface and verified by READING the rebuilt `predicates.md` fence when its leg lands, realization proving it compiled); if the landed export still fails the test, vendor `RobustGeometry.NET` (MIT; Shewchuk adaptive-precision orient3d/insphere, predicate core public-domain, single-file — the algorithm is a stable spec) as the interim substrate with the seam row recorded as the convergence target. Either way the float `Orient`/`InSphere`/`Det4` (`:913-921,962`) die on the sign-decision path, and the local `record MeshSpace` (`:373`) renames off the kernel Vectors `MeshSpace` it flattens from (`Rasm/.planning/Meshing/mesh.md` — a frozen kernel name) — a re-declared soup carrier under a kernel type's name is the deleted form. The consumable-surface demand is WATERFALLED: `RASM-CS-GEOMETRY-BRIEF.md` `[V14]` now names Compute the demanding consumer of a package-boundary-consumable exact `Orient3D`/`InSphere` member family, so the kernel-seam default is the expected path and the vendored fallback the exception. Recorded growth on the same kernel seam: the isotropic-remesh substrate (`RASM-CS-GEOMETRY-BRIEF.md` `[V11]`, Compute the third named consumer) is the boundary-conditioning pre-step for defective building shells before tetrahedralization — consumed through the decoded wire when it lands, an IDEAS row, never a Compute-side remesher.

### [V8_FRAME_BACKEND_CONSOLIDATION]

The corpus's one duplicate mechanism collapses: `Analysis/structural.md`'s `FrameBackend` outsources FE assemble-solve to `BriefFiniteElement.Net` (linear-static, .NET4-era, last release 2022, maintainer GitHub-banned — supply-chain fragility) + `FEALiTE2D` (2D hobby), while `Solver/contract.md` owns a genuine continuum-FE engine assembling `B^T·D·B` over the SAME CSparse `SparseLU`. RULED default: Euler-Bernoulli/Timoshenko beam/frame rows join the owned `ElementClass` axis (standard 12-DOF member stiffness with releases and rigid-end offsets as row columns), `FrameModel` idealization and the `(DesignCode, LimitState)` capacity table stay UNTOUCHED (the genuinely excellent layer), `FrameBackend` re-routes member response through the owned spine, and BFE+FEALiTE retire from the runtime graph. The retirement executes behind a PARITY GATE: frozen frame-solve fixtures (member end-forces, envelope extrema, modal frequencies on reference frames) prove the owned beam rows against the BFE/FEALiTE oracle BEFORE the packages leave the manifest; deciding criterion — if the owned rows cannot reproduce a frame feature the runner genuinely consumes (member releases, semi-rigid connections), the dual backend SURVIVES with the supply-chain risk recorded as a standing TASKLOG card and the pin-conflict `RasmSolverFactory` discipline preserved. MSolve/MGroup stays GATE-BLOCKED (no license file) — monitored, never admitted.

### [V9_SPARSE_EIGEN_NATIVE_TIER]

The self-flagged eigensolver ceiling lifts: `contract.md:104,733` admits dense EVD is the only eigensolver the admitted stack provides, while building-scale `fea-modal`/`fea-buckling` (and `[V12]`'s seismic response-spectrum route) are 10^4-10^6 DOF where dense O(n^3) is infeasible. ADMIT `csparse-interop` (BSD-3, same author as the admitted CSparse — the canonical extension, not a parallel rail; source-vendored, not on public NuGet) binding ARPACK shift-invert Lanczos (the industry-standard sparse generalized `K·phi = lambda·M·phi` eigensolver) plus native CHOLMOD/SuperLU direct factors: one sparse-eigen row on the `Solver/contract` `SolveMethod` axis + native-direct rows on `Tensor/factor`'s `FactorKind`, mirroring blas.md's managed<->native substrate pattern (managed CSparse stays the cold-start terminal; the native row is claim-gated by RID exactly as `LinearProvider` is). osx-arm64 runs the SuiteSparse/ARPACK leg (MKL is x64-only). The landed kernel `Numerics/matrix.md` LOBPCG is KERNEL-INTERIOR spectral machinery — the kernel ledger declares no matrix-plane seam toward Compute (`Rasm/ARCHITECTURE.md:73-99` carries only the identity, `DiscreteCalculus`, `MeshAdjointSnapshot`, acceleration, and encoding rows) and the conversion ruled Compute consumes wire arrays, so the sparse-eigen row composes the admitted ARPACK substrate over Compute's own CSparse-assembled `K`/`M`, never a reach into the kernel matrix plane. This verdict DISPOSES the `[NATIVE-BLAS]` BLOCKED card's design half — the native-asset provisioning itself is a Parametric_Forge toolchain lane, recorded as the activation gate, never this campaign's closeout blocker.

### [V10_AD_OWNER_AND_SLSQP]

Exact gradients replace the finite-difference fall: ADMIT `HyperJet` (ISC, net10, NuGet-published; source-generated `DDScalar1..15`/`DDScalar` hyper-dual numbers, `GetGradient()`/`GetHessian()` -> MathNet export) as the scalar-AD leg of the ONE `Sensitivity` family — `Tensor/dispatch` already owns the dual-mode geometry tape — its ingress is the DECLARED kernel adjoint seam: `Sensitivity.Operator` routes `OperatorRow.Apply`/`Adjoint` over the recorded kernel `MeshAdjointSnapshot` carrying the frozen `DiscreteCalculus` bundle (`ARCHITECTURE.md:124` mirrored by `Rasm/ARCHITECTURE.md:82-83`; the landed fences are `Rasm/.planning/Numerics/spectral.md` and `Meshing/mesh.md`, and Compute names only `MeshAdjointSnapshot`/`OperatorRow`, never a re-assembled DEC matrix) — and `Symbolic/lowering` the symbolic tape, all under the one `Chain` contract; hyperdual joins as the third leg for general smooth scalar objectives, and a fourth parallel gradient mechanism is the deleted form. Consumers rewire: `Stats/estimator`'s ARMA-MLE `FiniteJacobian` (`estimator.md:582,604,621` — "the autodiff-tape-absent fall") takes the exact Jacobian, `Solver/uncertainty`'s FORM/SORM limit-state gradient gains the exact-AD row beside the finite-difference fallback (caller-supplied black-box oracles keep FD honestly), and `Tensor/blas`'s `LevenbergMarquardt` documents HyperJet as its canonical Jacobian provider. A smooth constrained-NLP row joins `OptimizerKind`: `slsqp` via `cslsqp` (ISC, pure-C# net10, span-based — the matched Oberbichler pair with HyperJet) for sizing/calibration problems that are neither topology-adjoint nor MILP — vendored source until its NuGet release lands (feed-verified absent today); the row's admission gate is that vendoring law, recorded on the page.

### [V11_SYMBOLIC_CAS_REBASE]

The Symbolic folder re-bases on the categorical-best CAS: `MathNet.Symbolics` is self-described "basic" (no equation solving, no integration, no limits) and costs four dependencies (Symbolics + Numerics.FSharp + FSharp.Core + the contradictory direct FParsec); `AngouriMath` (MIT, active, net-current) natively owns parse/simplify/solve/integrate/differentiate/limit/LaTeX/`Compile<>` and collapses the FParsec parser and the `compileExpression` machinery into one owner. RULED default REPLACE: `SymbolicExpr`'s identity re-bases on AngouriMath's canonical form (the `[ComplexValueObject]` content-key law survives — identity stays the canonical-NF `XxHash128`, the engine's `Entity` replaces the F# `Expression` as the stored-but-excluded member), `dimensional.md`'s Q^7 fold re-bases its expression walk, `lowering.md`'s `CompiledExpr` cache re-bases on `Compile<>` (the L1-over-`CacheLane.ModelResult` discipline and the `Rasm.Persistence/Query/cache` seam-98 crossing are UNCHANGED — the content key is the page's own, not the engine's), and `units.md` is untouched except its E14 typed-`CorrelationId` repair. Deciding criterion: the re-base holds only if AngouriMath's canonical normal form is deterministic across process runs and pin-stable (content-key drift across versions is the disqualifier — probe with frozen expression fixtures in the leg); on failure the corpus KEEPS MathNet.Symbolics, the FParsec direct admission still dies (transitive-only either way), and the solve/integrate gap records as a bounded charter statement, never a second CAS beside the first. New capability either way: the cost/QTO formula lane's `SymbolicOp` axis gains solve/integrate rows the AEC formula consumers (cut-parameter inversion, quantity integrals) can reach.

### [V12_DISCIPLINE_COVERAGE]

The Analysis frontier: the seam declares fifteen `Discipline` rows, Compute answers seven, and the spine is BUILT for growth (one row + one route + one runner page + the existing receipt partial). Three disciplines land this campaign: (a) NEW `Analysis/circulation.md` — `Discipline.Circulation` egress/life-safety runner (IBC Ch.10 / EN route rows): travel-distance shortest-path (Dijkstra/A*) and exit-capacity max-flow/min-cut over the space-adjacency subgraph of the concrete `ElementGraph`, occupant-load-weighted distribution, dead-end/common-path checks — the exit-capacity flow composing the ADMITTED `Google.OrTools.Graph` `MaxFlow`/`MinCostFlow` (bound-assembly-verified members the `api-ortools.md:11` namespace roster omits; zero new pins for the flow concern), `QuikGraph` (admitted substrate consumed by Element/Persistence/Bim and NEVER by Compute today) for the path/topology algebra (Dijkstra/A*, SCC, betweenness), and the centrally-pinned `NetTopologySuite`/`Clipper2` for the planar side (isovist polygons, corridor medial-axis, occupant areas); ingress is HONEST — targets, space nodes, and adjacency arrive off the concrete `ElementGraph`, planar boundaries resolve through the SAME `GeometrySource` content-key port every runner threads (`Analysis/assessment.md:278-283`), never a second decode path, a space whose boundary cannot resolve is a typed `(Admission, Input)` failure, never a silently-skipped node, and the kernel slice-stack owner (`RASM-CS-GEOMETRY-BRIEF.md` `[V10]`b — oriented outer-CCW/holes-CW contours with typed OPEN-chain rows, that verdict naming this runner its SECOND consumer) is the NAMED floor-plate upgrade consumed through the decoded wire when it lands; ZERO new central pins; the one 1-hop counterpart is the `circulation` seam `Discipline` row per the seam's own growth law ("a new analysis discipline is one Discipline row", `classification.md:161`), recorded in the leg return — and until that Element row lands, `Discipline.Circulation` is a NAMED phantom (the roster ends at `Cost`, `classification.md:171-185`; `Circularity` is a different row): the runner page signature-locks its dispatch on the recorded counterpart row, stating the gate inline, and never asserts a green dry-run against an absent seam member. (b) NEW `Analysis/daylight.md` — the seam `Daylight` row EXISTS with no runner (`classification.md:178`): an owned NREL-SPA-grade solar-position kernel (public-domain stable spec, closed-form azimuth/altitude — zero solar computation exists corpus-wide), Perez all-weather sky rows with their ingress NAMED — site latitude/longitude/timezone and the hourly direct-normal/diffuse-horizontal irradiance the Perez rows require arrive through the energy lane's OWN weather surface, a `WeatherRef`-shaped request input (`energy.md:30`) read through the admitted OpenStudio `EpwFile` reader (`api-openstudio.md:85`; the header/data member set `assay api`-gated at the leg), the solar kernel computing position while the EPW supplies sky state, a weather-less request degrading to geometry-only shadow facts stated inline, never a silently-defaulted sky — and shadow/obstruction rays REUSING `clash.md`'s decoded BVH + Moller-Trumbore (the ray engine is already built over the kernel `ToAcceleration` wire per `ARCHITECTURE.md:121`); climate-based CBDM/glare (Radiance-class) stays the Python companion decoded over the existing graduation-evidence seam — C# owns geometry-derived daylight facts, Python owns the annual simulation, the same law energy already follows; and the solar-position kernel exports package-boundary-consumable (fenced as public surface per the `[SEAM_AND_RAIL_LAW]` verification tiers; realization proves it compiled) — `Rasm.AppUi`'s viewport `LightSource.Sun` row (RASM-CS-APPUI-BRIEF.md `[V6]`) is its second named consumer, composing azimuth/altitude from site + instant through the declared seam, because a Compute-internal-only kernel forces the AppUi-side duplicate AppUi's own one-kernel clause forbids. (c) `Solver/` gains the rule-satisfaction owner — ADMIT `Microsoft.Z3` (MIT; the arm64 native is Forge-gated per `[ROSTER_RECONCILIATION]`): a typed rule set lowers to Z3 assertions (naturally from `SymbolicExpr` — the CAS is the lowering source) returning SAT/UNSAT + model/unsat-core, surfaced as an `AssessmentResult` whose unsat-core names the exact violated rules; CP-SAT OPTIMIZES, Z3 VERIFIES-and-EXPLAINS — orthogonal concerns, ONE new page `Solver/satisfy.md` sited beside `optimizer.md`; it earns a seam `Discipline.Compliance` row ONLY IF the verdict must persist as its own content-keyed `Node.Assessment` the `[V1]` Sweep dispatches — a rule verdict that merely enriches an existing discipline's `AssessmentResult` rides that discipline's route, the seam growth law the mechanism either way. Seismic response-spectrum unlocks as a structural ROUTE row over `[V9]`'s sparse modal (the seam `Seismic` row exists; `StructuralCase.Seismic` exists) — a route whose chain is fully named, not a new runner: the sparse modal supplies `(phi, lambda)` and modal masses off the owned lumped-mass field (`contract.md:520-538`); the design spectrum is policy DATA — code-spectrum rows (EN 1998 Type 1/2, ASCE 7 — the codes the seam row itself names, `classification.md:206`) parameterized by site class, behavior factor, and damping, never a hardcoded curve; the combination is a row axis (`Srss`/`Cqc`, CQC the closely-spaced-mode default); the 90% modal-mass-participation floor is a TYPED `(Solve, Numeric)` shortfall naming the achieved fraction, never a silent truncation; participation and the combination key land as receipt columns on the `[V1]`-widened `AssessmentWire`. Recorded as seeded growth (IDEAS rows, not built): MEP network-flow (Todini gradient over the admitted sparse solver + QuikGraph), geometric room-acoustics (image-source over the clash BVH), hygrothermal EN 15026 transient (physics.md's own flagged growth), Infer.NET Bayesian calibration (the inverse-UQ/twin-updating owner — admitted only when its consumer page is chartered; its metered-data ingress is the AppHost livewire BACnet row decoded at the seam, RASM-CS-APPHOST-BRIEF.md `[04]`, never a Compute-side building-protocol client), and the Fabrication NFP nest-policy scorer (the injected-delegate producer closing that package's `NFP_DRL_POLICY` BLOCKED card — RASM-CS-FABRICATION-BRIEF.md the demanding consumer). One cross-package ingress row rides this frontier as consumer pressure: the `Rasm.Fabrication` `Nesting/stock -> Compute` seam (`Rasm.Fabrication/ARCHITECTURE.md:50`) lands its Compute-side named row — `NestYield.WasteAreaMm2` decoded into the `AggregateEnvironmental`/`AggregateCost` rollups through the seam Material node's Environmental/Cost cases, `RASM-CS-FABRICATION-BRIEF.md` the demanding consumer — a decode-side ingress row on the existing lifecycle/cost lanes, never a new discipline.

### [V13_PAGE_GRANULARITY]

Runtime's multi-owner mega-pages normalize against the corpus one-owner-per-page norm: `channels.md` (778 LOC, SEVEN index owners) SPLITS into `Runtime/wire.md` (proto vocabulary + contract evolution + fault projection + TS projection — the cross-process CONTRACT surface; the `[V3]` widening lands here in the same motion) and `Runtime/transport.md` (transport axis + call policy + artifact frames — channel mechanics and the frame law); the seam re-anchor map is the split's partition proof: `ARCH:67-68` (typescript:wire vocabulary/contract-drift) -> `wire.md`, `ARCH:69-70` (typescript:wire/frame) -> `transport.md`, and the TS projection cites the frame format by prose anchor, never a cross-split fence import; AppHost consumer pressure (RASM-CS-APPHOST-BRIEF.md): `Rasm.AppHost` composes `channels#TRANSPORT_AXIS` `GrpcChannelPolicy` by reference (`Rasm.AppHost/.planning/Wire/outbound.md:153,161`) and `channels#PROTO_VOCABULARY` (`Wire/companion.md:23,168`), so the re-anchor map carries both AppHost re-points (`TRANSPORT_AXIS`->`transport.md`, `PROTO_VOCABULARY`->`wire.md`) as counterpart obligations recorded in the split leg's return. `codecs.md` (832 LOC, FIVE owners) SPLITS ruled default into `Runtime/codecs.md` (tessellation bridge + field/result codec + geometry delta) and `Runtime/interchange.md` (tile partition + content addressing) — deciding criterion: the split survives only if the seam anchors partition cleanly (the GeoArrow/IDS/tile consumers re-anchor without a cross-split import; the known tangle to test is CONTENT_ADDRESSING's `InterchangeIdentity`, consumed by the tessellation/delta owners — if it crosses back as a fence dependency rather than a cited anchor, the page stays whole with its index as the navigation law). `Solver/contract.md` (1220 LOC, SOLVE_CONTRACT + CONSTITUTIVE) extracts the constitutive owner to `Solver/constitutive.md` only if the `[V6]` repair leg finds the section independently coherent — single-owner mega-pages (`optimizer.md`, `discretization.md`) STAY; they are one fold each. `Model/extension.md` (66 LOC) — RULED default KEEP: it is the corpus norm this verdict enforces (ONE owner, `CustomOps`, with its own `Register(SessionOptions, SessionPolicy)` entry, its own non-tensor `Egress` concern, a direct package admission, and its own receipt lane); folding it into `sessions.md` would mint the exact multi-owner defect this verdict kills in Runtime — the fold executes only if the leg-4 cold pass proves `CustomOps` has no independent entry, which disk already disproves. Every split updates the `ARCHITECTURE.md` codemap, README router, and seam-ledger anchors in the same motion; no new top-level folder.

### [V14_SEAM_LEDGER_TRUTH]

The ledger re-points to the post-campaign siblings, ledger-only (verified: every fence composes Persistence generically — zero fence edits): `ARCHITECTURE.md:111` (`Sync` FastCDC) -> the Persistence snapshot-frame FastCDC owner (`Version/timetravel` frames + the blobstore multipart window); `:115` (`Query/pipeline` PORT) -> `Query/columnar` (the surviving parse-to-canonical-bytes owner); `:116` (`Store/quality` SHAPE) -> verify-or-drop — the row is near-vestigial (`clash.md:18` disclaims any quality-plan producer); ruled default DROP unless the leg finds a real Compute-side anomaly-rule producer, in which case it re-points to the columnar/provisioning verification surfaces; `:119` (`Sync` WIRE, Confluent Protobuf Kafka) -> `Version/egress` — Compute owns the Protobuf frame; the counterpart is the CloudEvents/NATS pump, and the Kafka-serdes clause survives with the upstream sink-row default (the Persistence `[V3]` law lands broker rows by default) and dies only if that DECISION's per-package redundancy proof removes the Kafka row; `:99` (`Query/lane` VECTOR_CODEBOOK) -> `Query/retrieval` (the extraction; Compute's side is already honest — `embedding.md` consumes the codebook by reference, fits nothing, stores nothing); `:120` — the reverse clause "Processing/solver consumes the Compute LM/constraint Solver rail" DIES (disproven — the kernel LM rides its own λ-ladder over the landed `Numerics/matrix.md` owners, and the Geometry campaign's `COMPUTE:120` counterpart ripple may land the kill first, so the leg VERIFIES dead-or-kills, never re-authors; the forward `:121` clash WIRE row stays, with the `CLASH_GOLDEN` re-prove obligation per `[SEAM_AND_RAIL_LAW]`). Confirmed-stable rows a leg must NOT churn: `:98,:101,:102,:103,:108,:109` (`Query/cache` survives the Persistence campaign — `MODEL_RESULT_INDEX` and the artifact index are the surviving owners), `:123` (wires via `[V6]`), the Bim companion-rpc rows, and the AppUi/AppHost rows. WATERFALL RIPPLE: NONE REQUIRED BY THIS VERDICT — the Persistence brief already records `COMPUTE:111,115,116,119` and the VECTOR_CODEBOOK re-anchor as counterpart obligations (its E6/V12), and the Geometry brief already records the `:120`/`:121` correction (its V14); both verified in place. The campaign's two live waterfall edits sit elsewhere: `[V7]`'s predicate-export clause in the Geometry brief's V14 and `[V1]`'s assessment-`ArtifactKind` consumer-pressure clause on the Persistence brief's `cache.md` sound-surface row — both landed and verified.

### [V15_API_TIER_RECONCILIATION]

The `[ROSTER_RECONCILIATION]` catalog motion executes as a verdict, not a chore: the 9 md5-identical cross-tier duplicates (~1,971 LOC: extensions-ai, grpc x4, grpc-tools, highperformance, nodatime-protobuf, protobuf) collapse to substrate-canonical with one-line domain-tier pointers; all EIGHT divergent pairs reconcile per `[ROSTER_RECONCILIATION]` (`api-tensors` — the RICHER substrate copy becomes canonical, the domain overlay narrows to the Compute delta; the other seven — one canonical base, the Compute delta re-derived); `api-languageext.md` is AUTHORED at the substrate tier (the highest-value gap in either tier); the MathNet stack unifies to one tier (core beside its providers/symbolics satellites); `api-ortools.md` gains the `Google.OrTools.Graph` flow surface its `:11` namespace roster omits (`MaxFlow`/`MinCostFlow` — bound-assembly-verified, the `[V12]` exit-capacity solver; the deepen lands with the leg-4 first compose per the closeout law); `SharpGLTF.Ext.3DTiles` gains its `Tiles3D` surface documentation Compute-side with `api-sharpgltf.md`'s scope corrected (the ~30 lines documenting the Bim-owned `SharpGLTF.Runtime` decode surface demote to a one-line pointer — a catalog documenting a deleted-form surface at full depth is drift bait); the TestHost precedent is ruled explicitly per `[ROSTER_RECONCILIATION]`. Every removed package's catalog leaves with it; every admission lands its stub in the same leg; the `api-extensions-ai.md:251` embedding-production stacking claim is verified against the real wire (the `IEmbeddingGenerator` production lane is the AppHost-governed generative port; `embedding.md` is retrieval over pre-computed vectors — confirm the split and record it in both, never rewire).

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every leg re-verifies its rows against disk before editing. Anchors are `libs/csharp/Rasm.Compute/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md`; `CSPROJ:` the package csproj; `ELEMENT:`/`MATERIALS:` those packages' planning trees; `DECISION:` `.archive/RASM-COMPONENT-PARADIGM-DECISION.md`; `API:`/`SUBAPI:` the domain/substrate catalog tiers.

| # | Class | Evidence |
|---|---|---|
| E1 | Unwired lifecycle seam | `ELEMENT:Assessment/assessment.md:59-60` names the Compute sweep + Transient policy; `:70` 8-row `AssessmentOutcome` (Failed/Stale/Superseded + Dispatchable/Coherent/Next), `:104` `FailureKind.Transient`, `:122` `Diagnostic.Of(phase,kind,message,key)`, `:158` `AssessmentPayload`, `:196` its `.Failed` factory; `ELEMENT:TASKLOG.md:25` `COMPUTE_ASSESSMENT_RUNNERS` QUEUED; Compute `Analysis/assessment.md:245-247` blanket `Find` noop, `:248-250` failure never cached, `:107,114` `ResultBlob` sink pending (`energy.md` twin flag); `Runtime/receipts.md:416` `AssessmentWire` no failure slot. Seam rosters verified: `SolvePhase` = Admission/Solve/Extraction/Publication (`ELEMENT:Assessment/assessment.md:92-99`), `FailureKind` = Input/Numeric/Resource/Timeout/Aborted/Foreign with `Transient` true only on Resource/Timeout (`:104-115`), `Diagnostic` carries the `Option<int>` foreign-code slot (`:122-141`); staleness is assigned to the Compute sweep (`:229-236` `IsStaleFor`, `:239` `Advance`); `Provenance` (`:146`) carries NO attempt ordinal — the bounded-retry custody gap |
| E2 | Stringly runner faults | `Analysis/structural.md:285` `<bfe-solve-break>`, `:389` `<fealite-singular>`; `Analysis/energy.md:279,299,318`; all four runners share `Fin<AssessmentResult>`; `Runtime/channels.md:9` seventeen-code prose / fifteen-row rail vs the 2200-2220 band (`admission.md` custody map) |
| E3 | Stale seam ledger | `ARCH:111,115,116,119` target Persistence pages the Persistence campaign deletes; `ARCH:99` `Query/lane` -> `Query/retrieval` extraction; `ARCH:120` spurious reverse kernel-solver clause; `channels.md:92` already cites the surviving `Version/ledger` CHANGEFEED — bodies fresher than ledger; `RASM-CS-PERSISTENCE-BRIEF.md` E6/V12 records the counterpart obligations; stable rows `ARCH:98,101,102,103,108,109,123` |
| E4 | Strata mis-attribution | Mandate item 2 vs `DECISION:72` (Compute ZERO structural ripple; reads `graph.SectionOf`), `DECISION:234` (`SectionProfile` Materials-authoring union); `structural.md:461` `aisi-s100` route row, `:466` effective-width deferred to Materials, `:621-623` gross-section AISI cells |
| E5 | EC2 member-shear defer | `MATERIALS:Component/capacity.md:144` `ShearLinkAreaMm2`, `:250-254` V_Rd,s defer prose (stirrup spacing is member-scope), `:262` ceiling-only return, `:380` Asw mapping; `structural.md:636` plain `ConcreteVc` shear cell |
| E6 | Contract defects | `Solver/contract.md:65-66` `lambda = scale, mu = 0.5 * scale` (nu=1/3), `:297-304` no Poisson slot + culture-string `XxHash128` ContentKey (`:304`), `:301` lone `materialScale`; `ARCH:123` declared `MechanicalOf` elastic-D seam; `:1024` correct 2-parameter `ElasticPrimal` (nonlinear path only); canonical-byte contrast `Tensor/factor.md` `ShardPlan.Digest` |
| E7 | Float predicates | `Solver/discretization.md:585,617` Orient/InSphere consumption, `:913-921` float `Orient`/`InSphere`, `:962` `Det4`; `:373` local `record MeshSpace` vs the kernel Vectors `MeshSpace` (`:3,18`; frozen owner `Rasm/.planning/Meshing/mesh.md`); `libs/.planning/architecture.md:54` consumers never re-own kernel geometry; `RASM-CS-GEOMETRY-BRIEF.md` crowns `predicates.md` (`Rasm/.planning/Numerics/predicates.md` — `Predicate.Orient2D/Orient3D/InCircle/InSphere` + the LPI/TPI implicit-point family; its `[V14]` designs the Compute-consumable export) |
| E8 | Eigensolver ceiling + FE duplicate | `contract.md:104` `DenseEvd` row, `:733` "never a sparse LOBPCG/Lanczos it lacks"; `TASKLOG.md:20` `[NATIVE-BLAS]-[BLOCKED]`; `structural.md:197-201` BFE/FEALiTE `FrameBackend` beside contract's owned `B^T·D·B` spine over the same CSparse LU; BFE 2022/maintainer-banned, FEALiTE2D 2D-hobby (feed-verified) |
| E9 | AD gap | `Stats/estimator.md:574` "autodiff-tape-absent fall", `:582,604,621` `FiniteJacobian` calls; uncertainty FORM/SORM finite-difference limit-state gradient; `optimizer.md` derivative rows geometry-DDG-adjoint only — no smooth constrained-NLP row |
| E10 | CAS depth + FParsec | `Symbolic/expression.md:5,22,101` FParsec transitive-only law vs `CSPROJ:28` direct admission (lock `type:Direct`); `API:api-mathnet-symbolics.md:18,52` owns the transitive fact; MathNet.Symbolics upstream self-describes "basic" — no solve/integrate/limits |
| E11 | Coverage holes | `ELEMENT:Classification/classification.md:170-185` fifteen `Discipline` rows (`Daylight` `:178`, `Seismic`, `Hygrothermal`, `Water`...) vs seven Compute runners; `Analysis/assessment.md:18` seam-vocabulary law; `contract.md:35` `daylight-radiosity` one FEM row; zero solar-position hits corpus-wide; `SUBAPI:api-quikgraph.md` admitted substrate with zero Compute consumers; NTS/Clipper2 centrally pinned, Compute-unconsumed; no circulation/egress owner anywhere |
| E12 | Roster-vs-catalog | `CSPROJ:47,49` DirectML/Gpu vs `Model/providers.md:12` Cpu+CoreMl rows only, `:173` growth-note-only members, `API:api-onnxruntime.md:122-131` no osx-arm64 payload; `CSPROJ:71` 3DTiles vs `API:api-sharpgltf.md:491` Core-only stacking; LanguageExt.Core uncatalogued both tiers; MathNet core-in-substrate/satellites-in-domain split; 9 md5-identical cross-tier copies (1,971 LOC exact) + 8 divergent pairs (tensors/unitsnet/csparse/hashing/hybrid-cache/mathnet-providers/nodatime/thinktecture-json); `API:api-ortools.md:11` omits the assembly-real `Google.OrTools.Graph` namespace |
| E13 | Page granularity | `channels.md` 778 LOC / 7 index owners; `codecs.md` 832 / 5; `contract.md` 1220 / 2 (SOLVE_CONTRACT + CONSTITUTIVE); corpus norm one owner per page (Model/Analysis 66-698); `Model/extension.md` 66 LOC single-owner (`CustomOps` with its own entry/receipt lane — the `[V13]` KEEP default) |
| E14 | Small typed-carrier defects | `Symbolic/units.md:81-96,129,209-211` bare `Guid` correlation vs the corpus-wide typed `CorrelationId` (sole page, grep-confirmed); `clash.md:387` `CLASH_GOLDEN` asserted, not yet two-sided-harness-proven; `clash.md:255-259` `VertexDepth` plane-distance heuristic documented as candidate-grade, honest |
| E15 | Mandate items 4+6 clean | Item 4: grep `ComponentSection` = zero hits; Analysis pages consume `graph.SectionOf`/`SectionProperties`/`ProfileSet` per `DECISION:72`. Item 6: `Runtime/codecs.md:59` `GeoArrowBuffer.ToImportedGeometry` records the GLB wire layout (interior rings faulted to the kernel, never silently filled); `TASKLOG.md:56` COMPLETE; `libs/python/data/ARCHITECTURE.md:54` counterpart seam honest — RECORDED, never re-minted |

Sound surfaces (preserve; touch only where a verdict names them): `Analysis/physics.md` (10), `aggregator.md` (9.5), `lifecycle.md` (9.5), `energy.md` (9.5 minus E2), all seven `Model/` pages (9-10; `extension.md` KEEP per `[V13]`), `Runtime/scheduling.md` (10 — the V1 substrate), `admission.md` (10), `progress.md` (10), `payload.md` (10 — its SPZ/SOG wire stays UPSTREAM-BLOCKED; forward consumer pressure: `Rasm.AppUi` (RASM-CS-APPUI-BRIEF.md `[V5]`) consumes the `meshlet-cluster` row as its SOLE cluster source — its viewport LOD selection demands cluster-LOD chain columns (simplification levels + per-cluster error bounds beside the cone set, the error bounds monotonic non-decreasing up the parent chain so a consumer's threshold LOD cut is well-defined and crack-free) as encode-arm data per `[GENERATOR_LAW]`, so AppUi reads encoded error metadata and never re-clusters; AppUi the demanding consumer), `receipts.md` (9.5 minus E1's column), `Solver/optimizer.md` (10 minus the V10 row), `sweep.md` (10), `uncertainty.md` (10 minus the V10 gradient row), `clash.md` (9.5 minus E14), `Stats/signal.md` (9.5), `estimator.md` (9 minus E9), all nine `Tensor/` pages (9-9.5; factor/blas/dispatch gain V9/V10 rows; `residency.md#GEOMETRY_ENCODING` carries forward consumer pressure: `Rasm.AppHost` (RASM-CS-APPHOST-BRIEF.md) demands the `Field` and `Toolpath` encoding case rows its `Sandbox/solver.md:54-55,197` working spellings signature-lock on — two rows per `[GENERATOR_LAW]` landing with the Tensor leg as kernel `PackKind` rows + a residency wire row (`ARCHITECTURE.md:126`; the kernel counterpart is landed law — `Rasm/.planning/Drawing/pack.md:19`'s Growth law already names the field-grid/toolpath modalities as one-`PackKind`-row growth and `RASM-CS-GEOMETRY-BRIEF.md` `[V13]` lands the two rows against this exact residency row; never a residency-side `GeometryEncoding` owner, the ledger's own deleted-form name)), `Symbolic/dimensional.md` (9.5), and the `ARCHITECTURE.md` [04]/[05] prohibition blocks — the strongest governance prose in the C# tree.

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; the verdicts carry the shapes that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Analysis/assessment spine | 6 | 9.5 | lifecycle-aware CacheFirst; Failed write-back; Dispatchable Sweep over JobGraph with stale-marking/supersede/orphan-recovery; bounded Transient retry; ResultBlob sink -> ArtifactIndex landing; receipt failure columns |
| Analysis/structural | 8 | 9.5 | typed SolvePhase/FailureKind; EC2 V_Rd,s pairing; seam-baked effective-section read; owned frame rows behind the parity gate; seismic response-spectrum route over sparse modal (code-spectrum policy rows, SRSS/CQC axis, typed participation floor) |
| Analysis/physics+energy+lifecycle+aggregator | 9.5 | 9.5 | typed-failure classification rows only; interiors untouched |
| Coverage frontier (Analysis circulation/daylight + Solver satisfy) | 0 | 9 | egress graph+planar runner (OrTools-Graph flow + QuikGraph paths + NTS/Clipper2 planar, GeometrySource-fed); owned solar kernel + EPW-fed Perez sky + clash-BVH shadow rays + Python CBDM companion; Z3 SAT/unsat-core rule owner (arm64 native Forge-gated) |
| Solver/contract | 8 | 9.5 | MechanicalOf wired on the linear path; one 2-parameter elasticity-D; canonical-bytes ContentKey; beam/frame ElementClass rows; ARPACK sparse-eigen row |
| Solver/discretization | 7.5 | 9.5 | exact predicate floor (kernel seam or vendored Shewchuk); MeshSpace rename; owned topology preserved |
| Solver/optimizer+sweep+uncertainty | 9.5 | 9.5+ | slsqp row (cslsqp); exact-AD FORM/SORM gradient row; interiors untouched |
| Stats | 9 | 9.5 | HyperJet exact Jacobians (FiniteJacobian fall dies); NWaves rows roadmap-gated (Hilbert/cepstrum/Goertzel) |
| Symbolic | 9 | 9.5 | AngouriMath re-base (solve/integrate/limits reachable) or the bounded-charter fallback; FParsec admission dies; typed CorrelationId through units |
| Tensor | 9.5 | 9.5 | native-direct + ARPACK rows on factor (managed terminal stays); hyperdual leg on the one Sensitivity family |
| Model | 9.5 | 9.5 | cold; DirectML/Gpu admissions resolved; embedding-production split recorded |
| Runtime wire/codecs | 8.5 | 9.5 | band-complete FaultDetail; channels split (wire/transport); codecs split per criterion; seam anchors re-pointed |
| Governance perimeter (ledger/catalogs/manifest/cards) | 5 | 9.5 | six seam rows re-pointed; zero orphan admissions; zero contradiction rows; both tiers deduped + LanguageExt authored; IDEAS reseeded with the growth rows |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Eff` rails at every entry (the `[V15]` catalog pins its beta-77 surface), `System.IO.Hashing` `XxHash128` seed-zero as the ONE content hasher, NodaTime under `ClockPolicy`, `CommunityToolkit.HighPerformance` pooled spans/`ParallelHelper` on hot folds, `System.Numerics.Tensors` `TensorPrimitives` on float paths — a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package.
- `QuikGraph` (CONSUME — admitted substrate, zero Compute consumers today): Dijkstra/A*/Bellman-Ford shortest paths, SCC/betweenness for wayfinding — the `[V12]` circulation runner's path/topology algebra; the space-adjacency view is built from the concrete `ElementGraph`, never a second graph store; the exit-capacity flow is NOT QuikGraph's — it rides the OrTools row below.
- `Google.OrTools` (DEEPEN — admitted, the flow module uncatalogued): `Google.OrTools.Graph` `MaxFlow`/`MinCostFlow` push-relabel members verified in the bound `lib/net8.0` assembly while the `api-ortools.md:11` namespace roster omits the module — the `[V12]` exit-capacity solver, outranking a managed Edmonds-Karp consumer minted for the same concern; the catalog gains the Graph rows with the circulation leg; the CP-SAT/SCIP lanes stay the `optimizer.md` exemplar (`API:api-ortools.md:3`).
- `NetTopologySuite` + `Clipper2` (CONSUME — centrally pinned, Compute-unconsumed): isovist/visibility polygons, corridor medial-axis, occupant-area/offset algebra for the circulation planar side; the kernel stays the exact-geometry owner — these are float production-plane tools at the discipline boundary, never a second exact rail. Folder admission is leg-4 work the central pins don't cover: csproj reference rows plus catalogs — `api-quikgraph.md` (substrate) deepens on first compose; NTS/Clipper2 today carry exactly ONE sibling folder-tier catalog each (`Rasm.Bim/.api/api-nettopologysuite.md`; `Rasm.Fabrication/.api/api-clipper2.md` — the kernel-tier float-lane catalogs are conversion-deleted per the kernel float-lane law, `Rasm/README.md:99`), so Compute authors the substrate-canonical `api-nettopologysuite.md`/`api-clipper2.md` per the `[V15]` dedupe law and records the sibling demote-to-pointer as counterpart obligations — never a second folder-tier copy.
- `Riok.Mapperly` + `Generator.Equals` (CONSUME — centrally pinned doctrine substrate, zero Compute consumers today): Mapperly owns the per-case proto<->domain field transcription at the `[V13]` `wire.md` boundary — the `oneof` envelope law: the union's generated total `Switch` dispatches, Mapperly transcribes the per-case fields the protobuf runtime does not (`SUBAPI:api-mapperly.md:3`) — and `[Equatable]` owns structural equality wherever a class-root shape surrenders Thinktecture's record-root equality (`SUBAPI:api-generator-equals.md:3`; `docs/stacks/csharp/shapes.md:312`); the folder admission rides the wire split, and hand-written transcription or `Equals`/`GetHashCode` where the generator reaches is the deleted form.
- `Microsoft.Data.Sqlite` (CONSUME — centrally pinned 10.0.9, folder admission only): the eplusout.sql tabular reader the energy page names as its own growth axis (`Analysis/energy.md:354`) — a read-only connection over the bracketed scratch artifact landing with the leg-3 energy touch; zero new central pins, absence/malformation stays `(Extraction, Foreign)`-railed per `[V2]`.
- `Microsoft.ML.OnnxRuntime` — the EP-context/config-key family stays reference-mined; the CUDA/DML append members remain catalog-recorded with zero rows until a deployment admits them (`[ROSTER_RECONCILIATION]` removes the packages).
- `TorchSharp`+`libtorch-cpu` — the `[CLASSICAL_ML_BLAS]` dual-leg stays the exemplar; no change.
- `csparse-interop` (ADD, source-vendored, BSD-3 — `wo80/csparse-interop`, repo live and active): ARPACK `dsaupd`-family shift-invert Lanczos for `K·phi = lambda·M·phi`, CHOLMOD/SuperLU native direct — rows per `[V9]`; osx-arm64 = SuiteSparse leg; catalog authored with the admission; Forge provisioning gates activation, not design.
- `HyperJet` (ADD, ISC — feed-verified 0.2.0: net10.0-only TFM, zero-dep, pre-1.0 surface; the nuspec repo pointer is dead, the live owner is `oberbichler/HyperJet`): `DDScalar1..15` source-generated hyperduals, dynamic `DDScalar`, `GetGradient()`/`GetHessian()` MathNet export — the `[V10]` scalar-AD leg; the pre-1.0 pin is a recorded watch item; catalog with admission.
- `cslsqp` (ADD-GATED, ISC, vendored from the live `oberbichler/cslsqp` until NuGet — feed absence re-confirmed): `SlsqpSolver` span-based SQP — the `OptimizerKind.slsqp` row; the vendoring law records on the page.
- `Microsoft.Z3` (ADD, MIT): `Context`/`Solver` NRA/NIA assertions, `unsat_core`, model extraction — the `[V12]` rule-satisfaction owner; the feed stable is 4.12.2 (2023, netstandard2.0, win-x64/osx-x64 natives ONLY — nupkg-verified) so the osx-arm64 `libz3` provisions through Forge from the upstream 4.15.x arm64-osx release, the activation gate recorded beside csparse-interop's; catalog with admission.
- `AngouriMath` (REPLACE per `[V11]`, MIT — feed-verified 1.4.0 stable, MIT license expression, the net7.0 asset binds on net10): `Entity` algebra, `MathS.Solve`/`Integrate`/`Differentiate`/`Limit`, `Compile<>`, `Latexise` — nuspec deps exactly Antlr4.Runtime.Standard/GenericTensor/HonkSharp/PeterO.Numbers, all permissive; FParsec/FSharp.Core/MathNet.Numerics.FSharp leave with the swap; determinism probe per the verdict's criterion.
- `RobustGeometry.NET` (ADD-CONTINGENT per `[V7]`, MIT/public-domain core): vendored single-file Shewchuk adaptive predicates — only if the kernel predicate seam proves non-consumable.
- `NWaves` (GATE, MIT, 2022-stale): Hilbert envelope / cepstrum / Goertzel as new `SpectralTransform` rows ONLY when a structural-health-monitoring/vibration route names them; a wholesale DSP swap is rejected — it would delete the `TensorRun` receipt/determinism integration.
- `Microsoft.ML.Probabilistic` (RECORDED-FUTURE, MIT, active — feed-verified 2025 release cadence): the Bayesian calibration/inverse-UQ owner (energy-model-to-metered-data, twin updating beyond scalar Kalman) — admitted only when its consumer page is chartered; never an `EstimatorKind` row.
- `PsychroLib` (RECORDED-FUTURE, MIT — feed-verified 2.5.0, repo MIT and unarchived): the ASHRAE moist-air property substrate the hygrothermal EN 15026 IDEAS row will demand — admitted only when its consumer page is chartered, the same law as Infer.NET; `physics.md`'s realized EN/ISO closed-form kernel stays hand-owned and untouched.
- MINED-CONFIRMED (uncited-stub audit — every remaining domain-tier catalog verified against its realized consumer, zero action): `GeneticSharp` — the nsga2/RobustMinimax proposal kernel only, CMA-ES/PSO/SA stay owned folds (`API:api-geneticsharp.md:3`; `optimizer.md:1161`); `Silk.NET.WebGPU`+`Extensions.WGPU` — the ONE_WGPU_DEVICE WGSL compute-pipeline registry with `Wgpu` device-tick readback (`API:api-silk-webgpu.md:3`; `dispatch.md:792-799`); `Alimer.Bindings.MeshOptimizer` — index/vertex codec, meshlet build, and `SimplifyTarget` LOD all realized (`API:api-alimer-meshoptimizer.md:3`; `payload.md:87,152`); `Microsoft.IO.RecyclableMemoryStream` (`API:api-recyclable-stream.md:3`; four consumers), `Microsoft.Extensions.Caching.Hybrid` (`API:api-hybrid-cache.md:3`; three consumers), `Microsoft.ML.OnnxRuntimeGenAI` (`API:api-onnxruntimegenai.md:3`; `Model/generative.md`), `Microsoft.ML.OnnxRuntime.Extensions` (`API:api-onnx-extensions.md:3`; `Model/extension.md` — stays with the `[V13]` KEEP default), the `EC3` REST catalog (`API:api-ec3.md:3`; the lifecycle basis-tagged Normalize). Substrate-tier catalogs with no Compute lane (messagepack + thinktecture-messagepack, jsonpatch, redaction, unicolour, system-configuration, nodatime-stj, grpc-core-api) are out-of-lane single-tier files — no Compute row, no dedupe motion.
- REMOVE per `[ROSTER_RECONCILIATION]`: `FParsec` (direct), `Microsoft.ML.OnnxRuntime.DirectML`, `Microsoft.ML.OnnxRuntime.Gpu`, and (with `[V11]`) `MathNet.Symbolics`+`MathNet.Numerics.FSharp`; (post-`[V8]`-parity) `BriefFiniteElement.Net`+`BriefFiniteElementNet.CustomElements`+`FEALiTE2D`+`FEALiTE2D.Plotting` — each with its catalog and README group row, plus their central transitive-floor pins (`BriefFiniteElementNet.Common` `PROPS:470`, `netDxf.netstandard` `PROPS:472`) per `[ROSTER_RECONCILIATION]`.
- REJECTED (adversarial floor, never re-proposed): CMAES.NET, Accord.NET, ML.NET, MSolve/MGroup (license fail), TetGen/Gmsh/CGAL (copyleft), Epanet.Net (GPL — algebra hand-rollable over the owned sparse solver), Triangle.NET (no clean NuGet; 2D-boundary-only value — record, revisit only on a named 2D quality-mesh consumer), commercial numerics suites, in-process ANN (Persistence owns retrieval indexes by contract), ILGPU (a second GPU compute rail beside the ONE_WGPU_DEVICE WGSL registry — the parallel-rail deleted form), ComputeSharp (DirectX12/HLSL — the osx-arm64 primary RID has no payload), CoordinateSharp (AGPL/commercial dual license — gate fail; the `[V12]` owned NREL-SPA solar kernel stands).
- TASKLOG/IDEAS absorption: `[NATIVE-BLAS]` disposes into `[V9]` (design lands; Forge activation gate recorded); `[DATA_GEOARROW_GLB]` stays COMPLETE (E15); the IDEAS OPEN pool ((none) today) reseeds with the `[V12]` growth rows (MEP network-flow, room-acoustics, hygrothermal, Infer.NET calibration, seismic-route completion) so the forward frontier is never implicit.

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with THIS brief as the campaign parameter. Within a leg the listed order IS dependency order. The sequencing law:

1. **GOVERNANCE + ROSTER FLOOR** — `[V14]` seam-ledger re-points (ledger-only; the stable-row do-not-churn list travels with the leg) and `[V15]`+`[ROSTER_RECONCILIATION]` in one motion: the UNCONDITIONAL manifest motion per the staging law (criterion-gated and first-compose motions stay with legs 2/4), catalog dedupe/drift-reconcile/authoring (`api-languageext`, `Tiles3D`, MathNet tier, TestHost ruling), README/ARCHITECTURE codemap re-emit, E15 dispositions recorded. This leg gates every later page's imports and catalog claims.
2. **NUMERIC SPINE** — `Tensor/factor`+`blas` (native-direct + ARPACK rows, `[V9]`); `Tensor/dispatch` (hyperdual leg on `Sensitivity`, `[V10]`); `Solver/contract` (`[V6]` elasticity + key, beam/frame rows `[V8]`, sparse-eigen row `[V9]`, CONSTITUTIVE extraction per `[V13]` criterion); `Solver/discretization` (`[V7]`); `Solver/optimizer`+`uncertainty` (slsqp row + exact-AD gradient row, `[V10]`); `Stats/estimator`+`signal` (FiniteJacobian dies; NWaves rows only if route-named); `Symbolic/` quartet (`[V11]` re-base or fallback; typed `CorrelationId` through units per E14). The `[V8]` parity fixtures freeze in this leg; the BFE/FEALiTE manifest removal executes only after they prove.
3. **ASSESSMENT SPINE + RUNNERS + WIRE** — `[V2]` typed-failure classification across the four runners with the `admission.md:173` custody-row rename in the same motion (structural additionally lands `[V5]` shear, the `[V4]` seam-read correction, the `FrameBackend` re-route onto the leg-2 beam rows, and the `[V12]` seismic response-spectrum route over the leg-2 sparse modal — ONE touch of the page; energy additionally lands the `Microsoft.Data.Sqlite` eplusout.sql reader per `[04]`); `[V1]` spine rebuild on `Analysis/assessment.md` (lifecycle CacheFirst, Failed write-back, Sweep-over-JobGraph with the stale-marking/supersede/orphan-recovery legs, bounded Transient retry, ResultBlob sink threading through all four runners plus its ArtifactIndex landing); `Runtime/receipts` failure columns; the `[V13]` channels split landing WITH the `[V3]` band-complete widening (one touch) and the Mapperly/Generator.Equals folder admission riding the wire split per `[04]`, the codecs split per its criterion. Element's QUEUED card closes; the circulation-row counterpart obligation is recorded for `[V12]`.
4. **COVERAGE + CLOSEOUT** — `Analysis/circulation.md` + `Analysis/daylight.md` + `Solver/satisfy.md` (`[V12]`), with the QuikGraph/NTS/Clipper2 folder admission (csproj rows + substrate-canonical catalogs per `[04]`) landing beside the runner that first composes them; `Model/` cold pass (`extension.md` KEEP default per `[V13]`); IDEAS reseed; final ARCHITECTURE/README/TASKLOG truth pass.

Per-leg closeout: hard residuals resolve before the next leg launches; `ARCHITECTURE.md`/`README.md`/`TASKLOG.md`/`IDEAS.md` close in the leg that changes their facts; `.api` stubs and manifest edits land with the leg that admits, removes, or first composes the package, and the leg that first composes an admitted package deepens its stub; cross-package counterpart obligations (the Materials effective-section bake `[V4]`, the Element circulation `Discipline` row `[V12]`, the Element retry-attempt `Provenance` ordinal `[V1]`c, the Persistence assessment `ArtifactKind` row `[V1]`, the `CLASH_GOLDEN` two-sided re-prove at the Geometry landing) are recorded in the leg return with Compute named as the demanding consumer — never edited into sibling interiors. Campaign acceptance closes on composed contracts, not the ledger: (a) a failure-lifecycle dry-run — a deterministic structural failure persists ONE Failed `Node.Assessment` carrying `Diagnostic(phase, kind)`; re-`Assess` under CacheFirst returns the cached failure WITHOUT re-running; a `Transient`-kinded failure re-dispatches through the retry gate; the `Sweep` dispatches Pending/Stale and skips Queued/Running; a graph edit flips the drifted Computed row Stale through the marking fold and its re-solve supersedes it (exactly ONE usable row survives); a killed worker's Running row reconciles to Cancelled and re-dispatches; the fault crosses the wire as a `FaultDetail` row — composes entirely from rebuilt fences; (b) a numeric dry-run — a two-material fea-static reads per-node `(E, nu)` through `MechanicalOf` (two distinct D tensors provably assembled); a >=10^4-DOF fea-modal solves through the ARPACK row; a near-coplanar boundary set meshes clean through the exact predicate floor; the frame parity fixtures hold on the owned beam rows; (c) a design-check dry-run — a linked RC member governs at `min(V_Rd,s, V_Rd,max)` consistent with the Materials seam values; a cold-formed member resolves through the seam-baked effective section with zero Materials references in the fence; (d) a coverage dry-run — an egress assessment (travel distance + max-flow exit capacity) and a daylight assessment (solar position + clash-BVH shadow rays) write `Node.Assessment` rows through the SAME spine as the settled runners (the egress leg is counterpart-gated per `[V12]`: it composes the recorded `circulation` row and records the gate, never asserting green before the Element row lands), and a rule-set verdict returns an unsat-core naming the violated rules. A dry-run reaching for a missing owner reopens `[V1]`/`[V9]`/`[V12]` as residuals.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Materials` interiors: the `SectionProfile` overload dispatch and the cold-formed effective-section seam-bake are that campaign's edits — `[V4]` records Compute as the demanding consumer; the Materials capacity/steel pages are read-only seam counterparts here.
- `Rasm.Element` interiors: the assessment lifecycle vocabulary is LANDED LAW consumed as-is; the single 1-hop counterpart is the `circulation` `Discipline` row per the seam's own one-row growth law — recorded, never authored from this campaign.
- `Rasm.Persistence` interiors and the `Rasm` kernel corpus (the one `libs/csharp/Rasm/.planning/` root — 34 landed pages standing law, 18 robust-core pages geometry-campaign-governed) and both briefs: consumed as finalized law; every counterpart obligation this target needs is recorded upstream — the pre-existing rows (Persistence E6/V12; Geometry V14) plus the two surgical waterfall clauses this campaign added as consumer pressure (Geometry V14's `Orient3D`/`InSphere` package-boundary export per `[V7]`; the Persistence `cache.md` assessment-`ArtifactKind` row per `[V1]`) — all verified in place; no further upstream edit is open.
- `Rasm.Bim` IFC semantics (the `IdsVerdict`/`IdsAudit.Reconcile` stay Bim-owned; codecs relays the wire only), AppUi `ResidencyManifest.Mint`, AppHost M.E.AI governance and drain conductor — settled port peers.
- Python companion lanes: model training, PCE offline fit, Radiance-class CBDM/glare — the C#-inference/Python-training law stands; the `[V12]` daylight runner owns geometry-derived facts only.
- `Runtime/payload.md`'s gaussian-splat SPZ/SOG companion wire — honestly `[UPSTREAM-BLOCKED]`, untouched.
- The `[NATIVE-BLAS]`/`csparse-interop` native-asset provisioning — a `Parametric_Forge` toolchain lane; the campaign lands the design and records the activation gate.
- The federation-wide fault-band renumber beyond the 2200-band custody — escalation path only if the `[V2]`/`[V3]` rows cannot fit 2217-2299.
