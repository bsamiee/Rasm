# [PY_ARTIFACTS_API_USD_CORE]

`usd-core` (dist `usd-core`, import `pxr`) supplies Pixar's Universal Scene Description stage-authoring + USDZ-packaging surface for the artifacts scene rail beside the `vtk` USD exporter: a `pxr.Usd.Stage` authoring root (`CreateNew`/`CreateInMemory`/`Open`/`DefinePrim`/`Save`/`Export`), the `pxr.UsdGeom` typed-prim schema family (`Xform`/`Mesh`/`Points`/`Camera`/`Scope` + the `SetStageUpAxis`/`SetStageMetersPerUnit` stage axes), the `pxr.UsdShade` material/shader graph, the `pxr.Sdf` layer/asset-path/value-type substrate, the `pxr.Gf`/`pxr.Vt` math-value and typed-array vocabulary, and the `pxr.UsdUtils` asset-packaging family whose `CreateNewUsdzPackage(assetPath, usdzFilePath)` zips an authored `.usdc`/`.usda` layer (with all its dependency assets) into the `.usdz` AR/Apple deliverable. The package owner composes `pxr.UsdUtils.CreateNewUsdzPackage` into the `figures/scene#SCENE` `Scene3d` owner's `Export`-`USDZ` arm — the `vtk` `vtkUSDExporter` writes the rendered scene to a `.usdc` layer on the worker, then `CreateNewUsdzPackage(Sdf.AssetPath(usdc), usdz)` packages it — and never re-implements the USD crate file format (`.usdc` binary / `.usda` ASCII), the composition-arc layer stack, the Hydra render-graph, or the USDZ zip+alignment record layout the C++ USD runtime already owns. It is authoring/packaging only on the worker lane — the runtime render path stays at `pyvista`/`vtk`, and mesh-file interchange stays at `geometry`.

## [01]-[PACKAGE_SURFACE]

[PACKAGE_SURFACE]: `usd-core`
- package: `usd-core`
- import: `pxr` (dist name `usd-core`, import name `pxr`); the public names are the `pxr.{Usd,UsdGeom,UsdShade,UsdLux,UsdSkel,Sdf,Ar,Gf,Vt,Tf,Kind,Pcp,Plug,Trace,UsdUtils,...}` Boost.Python extension submodules, each `pxr/<Module>/__init__.py` re-exporting from its native `_<module>.so` — `import pxr` then `from pxr import Usd, UsdGeom, UsdUtils, Sdf, Gf, Vt`
- owner: `artifacts`
- rail: scene
- version: `26.5`
- license: `LicenseRef-TOST-1.0` — the Tomorrow Open Source Technology (modified Apache 2.0) license OpenUSD ships under; recorded at admission and on the consuming scene page, an OSS license admitting full commercial use
- capability: in-memory or on-disk USD stage authoring (`Usd.Stage.CreateNew`/`CreateInMemory`); typed-prim definition over the `UsdGeom` schema family (`Xform`/`Mesh`/`Points`/`Camera`/`Scope`/`PointInstancer`); per-prim attribute authoring (`CreatePointsAttr`/`CreateFaceVertexCountsAttr`/`CreateFaceVertexIndicesAttr`/`CreateNormalsAttr`/`CreateWidthsAttr`/`CreateDisplayColorAttr`) over the `Vt`/`Gf` value vocabulary populated zero-copy from numpy buffers through `Vt.<Type>Array.FromNumpy(ndarray)`; stage metadata (`SetStageUpAxis`/`SetStageMetersPerUnit`/`SetDefaultPrim`); material/shader graph authoring (`UsdShade.Material`/`UsdShade.Shader`); layer save/export to `.usdc` (crate binary), `.usda` (ASCII), or `.usd` (`Stage.Save`/`Stage.Export`/`Sdf.Layer.Export`); USDZ packaging of an authored layer plus all its dependency assets (`UsdUtils.CreateNewUsdzPackage`, the ARKit-constrained variant `CreateNewARKitUsdzPackage`); USDZ extraction (`UsdUtils.ExtractUsdzPackage`); and asset-dependency discovery/rewrite (`UsdUtils.ComputeAllDependencies`/`ModifyAssetPaths`)

## [02]-[PUBLIC_TYPES]

[PUBLIC_TYPE_SCOPE]: stage, prim, and layer roots
- rail: scene — `pxr.Usd`, `pxr.Sdf`

`Usd.Stage` is the single authoring root; the source kind (a new on-disk layer, an in-memory anonymous layer, or an opened existing layer) is a static-method choice on one type, never a parallel stage class — `CreateNew` takes a file path, `CreateInMemory` allocates an anonymous root layer, and `Open` resolves an existing layer. `Usd.Prim` is the one scene-graph node every typed schema (`UsdGeom.Mesh`, `UsdGeom.Xform`, ...) wraps via `Define`/`Get`; `Sdf.Layer` is the persisted substrate (`.usdc`/`.usda`) a stage composes and saves through. There is no per-format stage subtype — the layer format is the file extension `Stage.Export`/`Sdf.Layer.Export` writes (`.usdc` crate-binary, `.usda` ASCII), discriminated by suffix, never a parallel writer class.

| [INDEX] | [SYMBOL]            | [TYPE_FAMILY]    | [CAPABILITY]                                                                                                                                                  |
| :-----: | :------------------ | :--------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------- |
|  [01]   | `Usd.Stage`         | authoring root   | the composed scene root; statics `CreateNew(path)`/`CreateInMemory()`/`Open(path)`; `DefinePrim`/`OverridePrim`/`GetPrimAtPath`/`GetPseudoRoot`/`Traverse`/`TraverseAll`; `SetDefaultPrim`/`SetMetadata`/`GetRootLayer`/`Flatten`; `Save`/`Export(path)`/`Load`/`Unload` |
|  [02]   | `Usd.Prim`          | scene-graph node | one prim; `GetAttribute`/`CreateAttribute`/`SetTypeName`/`GetPath`/`GetReferences`/`GetVariantSets`/`IsValid`; the node every `UsdGeom`/`UsdShade` typed schema is applied to |
|  [03]   | `Usd.Attribute`     | typed attribute  | a typed prim property; `Set(value, time=TimeCode.Default())`/`Get`/`GetTypeName` — the `Vt`/`Gf` value carrier                                                |
|  [04]   | `Usd.TimeCode`      | sample time      | `Default()` / a numeric frame; the time axis on `Attribute.Set` for animated samples (a static scene authors at `Default()`)                                  |
|  [05]   | `Usd.ModelAPI`      | model schema     | model-kind / asset-info applied API over a prim (`Kind.Tokens.component`/`assembly`) for the scene-as-asset close                                              |
|  [06]   | `Sdf.Layer`         | layer substrate  | the persisted `.usdc`/`.usda` file; statics `CreateNew`/`CreateAnonymous`/`FindOrOpen`; `Save`/`Export(path)` — the unit `CreateNewUsdzPackage` packages       |
|  [07]   | `Sdf.AssetPath`     | asset reference  | a resolvable asset-path value (`Sdf.AssetPath(str)`); the REQUIRED first argument of `UsdUtils.CreateNewUsdzPackage` (NOT a bare `str`)                         |
|  [08]   | `Sdf.Path`          | namespace path   | a scene-graph path (`/Root/Mesh`); the prim-path key `DefinePrim`/`GetPrimAtPath` resolve                                                                     |
|  [09]   | `Sdf.ValueTypeNames`| value-type table | the SdfValueTypeName registry (`Point3fArray`/`Float3`/`Token`/...) keying `CreateAttribute` type declarations                                                |

[PUBLIC_TYPE_SCOPE]: typed geometry prims and value vocabulary
- rail: scene — `pxr.UsdGeom`, `pxr.UsdShade`, `pxr.Gf`, `pxr.Vt`

`UsdGeom.*` are the typed prim schemas applied to a `Usd.Prim` through one `Define(stage, path)` static per schema — a `Mesh`, an `Xform`, a `Camera` are three schemas over the one `Usd.Prim` node, never three parallel prim classes. `Gf.*` are the fixed-size math-value scalars and `Vt.*` are the typed homogeneous arrays the geometry attributes carry — `Vt.Vec3fArray` for points, `Vt.IntArray` for face indices — so a mesh's point buffer is one typed-array `Set`, never a per-vertex object list. `UsdShade.Material`/`Shader` are the material-graph nodes a scene binds for PBR surfaces.

| [INDEX] | [SYMBOL]                | [TYPE_FAMILY]   | [CAPABILITY]                                                                                                              |
| :-----: | :---------------------- | :-------------- | :---------------------------------------------------------------------------------------------------------------------- |
|  [01]   | `UsdGeom.Xform`         | transform prim  | `Define(stage, path)`; an `Xformable` carrying `AddTranslateOp`/`AddRotateXYZOp`/`AddScaleOp` transform-op stack — the scene-graph grouping/placement node |
|  [02]   | `UsdGeom.Mesh`          | polygon mesh    | `Define`; `CreatePointsAttr`/`CreateFaceVertexCountsAttr`/`CreateFaceVertexIndicesAttr`/`CreateNormalsAttr`/`CreateDisplayColorAttr` — the authored surface |
|  [03]   | `UsdGeom.Points`        | point cloud     | `Define`; `CreatePointsAttr`/`CreateWidthsAttr` — the point-primitive schema                                              |
|  [04]   | `UsdGeom.Camera`        | camera prim     | `Define`; the `GfCamera`-backed projection/focal/clipping schema for the scene viewpoint                                  |
|  [05]   | `UsdGeom.Scope`         | grouping prim   | `Define`; a pure namespace-grouping prim with no transform (asset-organization scaffolding)                              |
|  [06]   | `UsdGeom.PointInstancer`| instancer prim  | `Define`; prototype-relationship + per-instance index/position/orientation arrays for large repeated-geometry scenes      |
|  [07]   | `UsdGeom.Tokens`        | token table     | the schema token vocabulary (`y`/`z` up-axis, `rightHanded`/`leftHanded` orientation, `vertex`/`faceVarying` interpolation) feeding `SetStageUpAxis` and attr metadata |
|  [08]   | `UsdShade.Material`     | material node   | `Define`; `CreateSurfaceOutput`/`CreateInput` — the bound material a `Gprim` references for PBR                           |
|  [09]   | `UsdShade.Shader`       | shader node     | `Define`; `CreateIdAttr`/`CreateInput`/`CreateOutput`/`ConnectableAPI` — the UsdPreviewSurface/texture node graph         |
|  [10]   | `Gf.Vec3f` / `Gf.Vec3d` | math vector     | fixed 3-vector value (`Vec3f` float, `Vec3d` double) — the point/normal/translate scalar                                  |
|  [11]   | `Gf.Matrix4d` / `Gf.Quatf` | math transform | 4x4 matrix and quaternion transform values for the xform-op stack                                                       |
|  [12]   | `Vt.Vec3fArray` / `Vt.IntArray` | typed array | the homogeneous point/index/normal buffers (`Vec3fArray`/`Vec3dArray`/`IntArray`/`FloatArray`/`TokenArray`) a geometry attribute `Set` carries, populated zero-copy from a numpy buffer through `Vt.<Type>Array.FromNumpy(ndarray)` (the bare `Vt.Vec3fArray(ndarray)` ctor has no registered converter and raises `TypeError`) |

[PUBLIC_TYPE_SCOPE]: error reporting
- rail: scene — `pxr.Tf`

USD reports authoring/packaging faults through `pxr.Tf.ErrorException` (the Boost.Python-mapped `Tf` error), and a Boost.Python argument-type mismatch surfaces as `Boost.Python.ArgumentError`; map both at the `to_process.run_sync` worker boundary into the `runtime` `RuntimeRail` fault, never let a C++ coding-error or a wrong-arg type cross the interpreter seam bare.

| [INDEX] | [SYMBOL]                     | [TYPE_FAMILY]   | [CAPABILITY]                                                                                  |
| :-----: | :--------------------------- | :-------------- | :------------------------------------------------------------------------------------------- |
|  [01]   | `Tf.ErrorException`          | runtime fault   | the USD runtime error (missing asset, malformed layer, write failure) raised across the binding |
|  [02]   | `Boost.Python.ArgumentError` | binding fault   | a wrong Python-argument type for a `pxr` C++ overload (e.g. a bare `str` where `Sdf.AssetPath` is required) |

## [03]-[ENTRYPOINTS]

[ENTRYPOINT_SCOPE]: stage authoring and layer export
- rail: scene — `pxr.Usd`, `pxr.UsdGeom`, `pxr.Sdf`

`Usd.Stage.CreateNew(path)` opens a new on-disk root layer; `CreateInMemory()` allocates an anonymous layer for an author-then-export-then-package flow with no intermediate file. `Define(stage, primPath)` (per `UsdGeom` schema) defines the typed prim, then `Create<Attr>Attr(value)` authors each attribute over a `Vt` typed array. `SetStageUpAxis(stage, UsdGeom.Tokens.y)` and `SetStageMetersPerUnit(stage, 1.0)` set the stage axes; `SetDefaultPrim(prim)` names the root. `Stage.GetRootLayer().Save()` (or `Stage.Export(path)`) persists to `.usdc`/`.usda` by suffix. The whole stage authors lazily — prims compose into the layer stack and write on `Save`/`Export`.

| [INDEX] | [SURFACE]                          | [CALL_SHAPE]                                                                                              | [CAPABILITY]                                                |
| :-----: | :--------------------------------- | :------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------- |
|  [01]   | `Usd.Stage.CreateNew`              | `CreateNew(identifier: str, load: Usd.StageLoadRules = LoadAll) -> Usd.Stage` (static)                   | open a new on-disk root-layer stage (suffix selects format) |
|  [02]   | `Usd.Stage.CreateInMemory`         | `CreateInMemory(identifier: str = '') -> Usd.Stage` (static)                                             | allocate an anonymous in-memory stage (no file)            |
|  [03]   | `Usd.Stage.Open`                   | `Open(filePath: str, load = LoadAll) -> Usd.Stage` (static)                                              | open an existing `.usd`/`.usdc`/`.usda`/`.usdz` layer       |
|  [04]   | `Usd.Stage.DefinePrim`             | `DefinePrim(path: Sdf.Path, typeName: str = '') -> Usd.Prim`                                             | define (or fetch) a prim at a namespace path               |
|  [05]   | `UsdGeom.<Schema>.Define`          | `Mesh.Define(stage: Usd.Stage, path: Sdf.Path) -> UsdGeom.Mesh` (static, per schema)                    | define a typed geometry prim (`Xform`/`Mesh`/`Points`/`Camera`/`Scope`) |
|  [06]   | `UsdGeom.Mesh.CreatePointsAttr`    | `CreatePointsAttr(defaultValue: Vt.Vec3fArray = None, writeSparsely: bool = False) -> Usd.Attribute`     | author the mesh point buffer (and `FaceVertexCounts`/`FaceVertexIndices`/`Normals`/`DisplayColor` siblings) |
|  [07]   | `UsdGeom.SetStageUpAxis`           | `SetStageUpAxis(stage: Usd.Stage, upAxis: str) -> bool`                                                  | set the stage up-axis (`UsdGeom.Tokens.y`/`UsdGeom.Tokens.z`) |
|  [08]   | `UsdGeom.SetStageMetersPerUnit`    | `SetStageMetersPerUnit(stage: Usd.Stage, metersPerUnit: float) -> bool`                                  | set the stage linear scale (the AR/real-world unit anchor) |
|  [09]   | `Usd.Stage.SetDefaultPrim`         | `SetDefaultPrim(prim: Usd.Prim) -> None`                                                                 | name the stage's default root prim (required for clean USDZ reference) |
|  [10]   | `Usd.Stage.GetRootLayer` / `Save`  | `GetRootLayer() -> Sdf.Layer`; `Sdf.Layer.Save() -> bool`                                                | fetch the root layer and persist edits to disk             |
|  [11]   | `Usd.Stage.Export`                 | `Export(filename: str, addSourceFileComment: bool = True) -> bool`                                       | flatten + export the composed stage to a new `.usdc`/`.usda` file |
|  [12]   | `Sdf.Layer.Export`                 | `Export(filename: str) -> bool`                                                                          | export one layer to a path (format by suffix)              |
|  [13]   | `Vt.<Type>Array.FromNumpy`         | `Vt.Vec3fArray.FromNumpy(array: numpy.ndarray) -> Vt.Vec3fArray` (static, per array type)                | zero-copy typed-array construction feeding `Create<Attr>Attr` (`Vec3fArray`/`Vec3dArray`/`IntArray`/`FloatArray`; no `TokenArray.FromNumpy`); accepts `(N,3)`/flat float and int buffers, widening dtype as needed, where the bare `Vt.<Type>Array(ndarray)` ctor raises `TypeError` |

[ENTRYPOINT_SCOPE]: USDZ packaging and asset dependencies
- rail: scene — `pxr.UsdUtils`

`UsdUtils.CreateNewUsdzPackage(assetPath, usdzFilePath)` is the USDZ builder the scene `Export`-`USDZ` arm calls: it takes the root layer as an `Sdf.AssetPath` (NOT a bare `str`), gathers every dependency asset the layer references, and writes the aligned-zip `.usdz` deliverable, returning `bool` success. `CreateNewARKitUsdzPackage` is the same with the ARKit/QuickLook compliance constraints (single root layer, restricted file types) applied. `ExtractUsdzPackage` is the inverse, unpacking a `.usdz` to a directory. `ComputeAllDependencies` discovers the layer/reference/asset closure; `ModifyAssetPaths` rewrites asset paths (relocation). The packaging functions are Boost.Python overload stubs (no Python `__doc__`); the verified C++ signature is reflected below.

| [INDEX] | [SURFACE]                              | [CALL_SHAPE]                                                                                                                       | [CAPABILITY]                                                |
| :-----: | :------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------- |
|  [01]   | `UsdUtils.CreateNewUsdzPackage`        | `CreateNewUsdzPackage(assetPath: Sdf.AssetPath, usdzFilePath: str, firstLayerName: str = '', editLayersInPlace: bool = False) -> bool` | package an authored layer + all dependency assets into `.usdz` |
|  [02]   | `UsdUtils.CreateNewARKitUsdzPackage`   | `CreateNewARKitUsdzPackage(assetPath: Sdf.AssetPath, usdzFilePath: str, firstLayerName: str = '', editLayersInPlace: bool = False) -> bool` | the ARKit/QuickLook-compliant USDZ build (single root, restricted types) |
|  [03]   | `UsdUtils.ExtractUsdzPackage`          | `ExtractUsdzPackage(usdzFile: str, extractDir: str, recurse: bool = False) -> bool`                                                | unpack a `.usdz` archive to a directory (recursing nested packages) |
|  [04]   | `UsdUtils.ComputeAllDependencies`      | `ComputeAllDependencies(layerPath: Sdf.AssetPath) -> tuple[list[Sdf.Layer], list[str], list[str]]`                                 | the layer/reference/asset dependency closure of a root layer |
|  [05]   | `UsdUtils.ModifyAssetPaths`            | `ModifyAssetPaths(layer: Sdf.Layer, modifyFn) -> None`                                                                             | rewrite every asset path in a layer through a callback (relocation) |

## [04]-[IMPLEMENTATION_LAW]

[SCENE_USDZ_PACKAGING]:
- packaging axis: `CreateNewUsdzPackage(Sdf.AssetPath(usdc_path), usdz_path)` is the single USDZ builder the scene `Export`-`USDZ` arm calls — it gathers the root layer plus every dependency asset and writes the aligned-zip `.usdz`, returning `bool`; the `vtk` `vtkUSDExporter` writes the `.usdc` layer first (over `Plotter.render_window`), then this function packages it. The FIRST argument is an `Sdf.AssetPath`, NOT a bare `str` — a raw-string call raises `Boost.Python.ArgumentError`. `CreateNewARKitUsdzPackage` is the ARKit-constrained variant for the Apple QuickLook deliverable; `ExtractUsdzPackage` is the inverse — one packaging family, never a per-target zip function.
- authoring axis: a USD scene authored from scratch (rather than exported from VTK) is `Usd.Stage.CreateNew(path)` (or `CreateInMemory()`), then `UsdGeom.<Schema>.Define(stage, primPath)` per typed prim, then `Create<Attr>Attr(Vt.<Type>Array(...))` per attribute over the `Gf`/`Vt` value vocabulary, then `SetStageUpAxis`/`SetStageMetersPerUnit`/`SetDefaultPrim` stage metadata, then `GetRootLayer().Save()` or `Export(path)` — one stage root, typed prims defined by schema, never a parallel per-geometry-kind writer. The layer format (`.usdc` crate-binary / `.usda` ASCII) is the file suffix, discriminated by extension at export.
- value axis: a geometry attribute carries a `Vt` typed array directly (`Vt.Vec3fArray` for points/normals, `Vt.IntArray` for face counts/indices, `Vt.TokenArray` for interpolation tokens), and a transform-op a `Gf` value (`Gf.Vec3f`/`Gf.Matrix4d`/`Gf.Quatf`) — there is no per-vertex or per-element wrapper object, so a mesh's whole point buffer is one `CreatePointsAttr(Vt.Vec3fArray.FromNumpy(points))` `Set` over the zero-copy numpy bridge, never a `.tolist()` Python-list copy nor a vertex-by-vertex append (the bare `Vt.Vec3fArray(ndarray)` ctor has no registered converter and raises `TypeError`, so `FromNumpy` is the buffer path).
- dependency axis: `ComputeAllDependencies(Sdf.AssetPath(layer))` returns the `(layers, references, assets)` closure a USDZ package will embed, and `ModifyAssetPaths(layer, fn)` rewrites asset paths for relocation before packaging — so a scene with external texture/reference assets packages a complete self-contained `.usdz` rather than a layer with dangling references.
- fault axis: the USD runtime raises `Tf.ErrorException` (missing asset, malformed layer, write failure) and the Boost.Python binding raises `Boost.Python.ArgumentError` for a wrong-typed argument (the `Sdf.AssetPath`-vs-`str` mismatch); both are mapped at the `_render_export` worker boundary into the `runtime` `RuntimeRail` fault, never surfaced bare across the interpreter seam, and a `bool` `False` return from `CreateNewUsdzPackage` is a packaging-failure rail, not a silent miss.
- evidence: each USD/USDZ export captures the scene file target (`usdc`/`usdz`), the authored prim count, the up-axis and meters-per-unit stage metadata, the dependency-asset count embedded, and the recovered-package content key as the `figures/scene#SCENE` `ArtifactReceipt.Scene(key, target)` fact (`target` the `SceneTarget` value), so the scene owner records the packaging without minting a separate receipt.

[STACKING]:
- `usd-core` is the USDZ-packaging back half of the scene `Export`-`USD`/`USDZ` arm whose front half is `vtk` `vtkUSDExporter`: the VTK exporter writes the rendered scene to a `.usdc` layer over `Plotter.render_window`, then `UsdUtils.CreateNewUsdzPackage(Sdf.AssetPath(usdc), usdz)` packages that layer into the `.usdz` AR/Apple deliverable — so the two-package arm produces a USDZ with zero hand-rolled zip alignment, the `vtk` row owning the render-to-layer write and the `usd-core` row owning the layer-to-package zip.
- `Usd.Stage.CreateInMemory()` + `UsdGeom.<Schema>.Define` + `Export` lets the scene owner author a USD layer directly from a `numpy` point/index buffer (bypassing VTK) over `Vt.Vec3fArray.FromNumpy(points)`/`Vt.IntArray.FromNumpy(indices)`, so a pure-geometry USD export does not require a full render pass — one stage-authoring path the same `Export` arm reaches when the target is `USD` and no render is needed.
- `CreateNewARKitUsdzPackage` hardens the packaging policy to the Apple QuickLook constraint set (single root layer, restricted asset types) as a data choice on the same arm — an AR-deliverable scene selects the ARKit packager, a general USDZ selects `CreateNewUsdzPackage`, never a branching per-platform export owner.
- `ComputeAllDependencies` + `ModifyAssetPaths` close the self-contained-package invariant: before `CreateNewUsdzPackage`, the dependency closure is computed and any external asset path rewritten relative, so a scene authored with referenced textures packages a complete `.usdz` rather than one with broken asset references.

[RAIL_LAW]:
- Package: `usd-core`
- Owns: USD stage authoring (`Usd.Stage` create/open/define/save/export), typed geometry prims (`UsdGeom.Xform`/`Mesh`/`Points`/`Camera`/`Scope`/`PointInstancer` + `SetStageUpAxis`/`SetStageMetersPerUnit`), per-attribute authoring over the `Gf`/`Vt` math-value and typed-array vocabulary, material/shader graphs (`UsdShade.Material`/`Shader`), layer serialization to `.usdc`/`.usda` (`Sdf.Layer`), USDZ packaging/extraction and asset-dependency discovery/rewrite (`UsdUtils.CreateNewUsdzPackage`/`CreateNewARKitUsdzPackage`/`ExtractUsdzPackage`/`ComputeAllDependencies`/`ModifyAssetPaths`)
- Accept: the `figures/scene#SCENE` `Scene3d` `Export`-`USDZ` arm packaging the `vtk`-written `.usdc` rendered scene into `.usdz`; the gated `to_process.run_sync` sub-3.13 worker import; the `Sdf.AssetPath(usdc_path)` first-argument USDZ call; direct in-memory stage authoring from `numpy` buffers over `Vt` arrays; the ARKit-constrained packager for the Apple QuickLook deliverable; the dependency-closure compute + asset-path rewrite before packaging
