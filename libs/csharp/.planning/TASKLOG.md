# [CSHARP_BRANCH_TASKLOG]

The cross-package C# work no single folder owns, plus the mature-folder open work that carries no `.planning/` scaffold. Per-folder work lives in each package `TASKLOG.md`; this node carries the cross-folder seam arbitrations, the mature `Rasm` cleanup, and the host-boundary seams the future app root composes. Cross-language amendments (the CRDT wire-vocabulary change, the SDK-codegen consumption) live in `libs/.planning/TASKLOG.md` and are referenced as wire seams, never restated. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed.

## [1]-[OPEN]

[QUEUED] Capture the two-sided clash-seam golden fixture between the kernel spatial index and the Compute clash compute.
- The node-link acceleration layout is the AGREED frozen contract: `Rasm.Geometry/Spatial/index#CLASH_SEAM` `SpatialIndex.ToAcceleration`/`NodeLinkProjection` emits the per-node interleaved AABB plus the `(FirstChild << 21) | ChildCount` descriptor with a leaf primitive-id tail, and `Compute/Solver/clash#CLASH_AND_TWIN` `ClashScale.BvhPairs` decodes it by a proper BVH descent over the contiguous `[FirstChild, FirstChild+ChildCount)` child range — the prior flat per-primitive O(N²) decode is the deleted form on both pages.
- No new package; the seam is the `Rasm.Compute.Solver` `AccelerationStructure` union the projection returns; integrate the testkit under `testing-cs`.
- The contract is settled across the kernel/app-platform boundary and the `[CLASH_GOLDEN]` pin is now COMPLETE in the kernel `Spatial/index#CLASH_GOLDEN` design — the 8 `BoundingBox(min,max)` coordinates, `NodeCount == 3`, the 160-byte LE `Bounds`/`Nodes` golden stream (descriptors `2097154`/`-5`/`-8388613`, identity `Order` tail), and the 4-pair clash set `{(0,1),(2,3),(4,5),(6,7)}` are byte-derivable on both sides. The residual is the two-sided byte-equality fixture frozen once into the shared test corpus, the Rasm `Spatial/index#CLASH_GOLDEN` spec asserting `NodeLinkProjection` emits those bytes and the Compute `Solver/clash#CLASH_GOLDEN` spec asserting `BvhPairs` decodes them into the agreed clash-pair set.
- The fixture rides the shared corpus node (`BRANCH_TEST_NODE_PROVISIONING`), the degradation-keyed refit and agglomerative builder riding the same projection transparently with no layout change; the only remaining artifact is the two-sided spec, a `testing-cs` obligation, not a design gap.

[BLOCKED] Capture the canonical-adjacency byte-identity golden fixture between the kernel topology hash and the Persistence structural diff.
- The canonical int32-LE adjacency layout is the AGREED frozen contract owned by `Rasm.Geometry/Spatial/reconciliation#CANONICAL_BYTE_IDENTITY` `NamingHashOps.Encode`; `Persistence/Version/diff#STRUCTURAL_DIFF` `GeometryHash`/`StructuralMerge` reads the IDENTICAL layout VERBATIM before `XxHash128.HashToUInt128` and never re-derives a second encoding.
- Integrate System.IO.Hashing (`XxHash128`); CsCheck and the testkit under `testing-cs`.
- The byte order is frozen and the digest pinned; the residual is the shared golden-bytes fixture — the single-triangle reference (`VertexCount=3`; edges `(0,1),(0,2),(1,2)`; face cycle `[0,1,2]`) whose 52-byte stream and `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest both packages assert, with the morph (equal hash) and topology-break (distinct hash) discriminating laws. The live-host `Mesh.TopologyVertices.IndicesFromFace` boundary-cycle winding is host-derived `[0,1,2]` CCW (Rhino 9 WIP slot probe), and the 52-byte stream plus the digest are host-derived-validated against the pin (`System.IO.Hashing.XxHash128.HashToUInt128`, exact match).
- Blocked only on that frozen golden-bytes fixture existing in both packages' test scope; the `IndicesFromFace` winding and the digest are host-derived-confirmed, so the rotation law is settled and the bytes are freeze-ready.

[QUEUED] Reconcile the mature `Domain/Geometry.cs` owner with the greenfield `Geometry/` robust-core sub-domain.
- The mature `Rasm/Domain/Geometry.cs` geometry-normalization owner and the greenfield `Rasm/Geometry/` robust-core share the name `Geometry`; rename or re-scope so the `Rasm.Geometry.*` robust-core namespaces (`Numerics`/`Spatial`/`Topology`/`Healing`/`Constraints`/`Tessellation`/`Faults`) do not collide with the `Domain` owner.
- No external package; a co-located source rename or namespace re-scope internal to the kernel.
- Internal to `Rasm` — aligns the mature `Domain` source with the greenfield `Geometry` sub-domain tree; no cross-package consumer reads the `Domain` geometry owner by the colliding name.
- Settle the rename before the kernel transcription task creates the `Geometry/` source tree so no two owners claim the name.

[QUEUED] Mature-folder densification of the kernel `Vectors`/`Analysis`/`Domain` source.
- Carry the open split/densify/re-architecture work for the three mature kernel sub-domains as task cards here rather than in a `.planning/` scaffold those folders no longer hold: the named concept-density pressure is `Vectors/Mesh.cs` (4445 LOC: `LaplacianCache`, topology, remesh, descriptors, DEC-aware fields — a multi-concept owner the REFINE pass grades for in-place polymorphic collapse versus a `Mesh.Topology`/`Mesh.Descriptors` owner-block split) and `Vectors/Field.cs` (2662 LOC: CSG, kernels, noise, gradients, streamlines), each against the `Vectors/_ARCHITECTURE.md` co-located source note and the singular `VectorIntent.Project` rail; the `Analysis` measure/query/intersect/topology algebra and the `Domain` Rhino-normalization/context/stats/validation owners stay leaner and carry only the doctrine-form sweep.
- No new external package; the mature lanes compose the admitted CSparse, System.Numerics.Tensors, MathNet.Numerics, and (for the spectral substrate) the DEC/heat-method author-kernel already registered.
- Internal to `Rasm` — the mature siblings are settled source consumed by every stratum above, so a densification preserves the public operator vocabulary and the `Project<TOut>(Context, Op?)` rail; the greenfield `Geometry/` robust-core composes `Vectors` value-objects (`Align`/cloud-ICP registration, `Spectral` DEC operators) as settled, never re-minting a primitive, and the `REALITY_CAPTURE_TO_BIM` reconstruction arm reads the `Align`/cloud algebra by reference.
- The mature folders carry no `.planning/`; their design surface is the co-located source architecture note and this card is the only open-work home — a REFINE-pass density grade, not a new sub-domain, and never a file extraction to cut LOC.

[QUEUED] Compose the host-boundary seams at the future app root.
- The out-of-scope-durable `Rasm.Rhino` and `Rasm.Grasshopper` source own the live-host RhinoCommon/GH2 surfaces (capture, events, commands, exchange, drafting, components); the future app root composes them with the app-platform — the `AppUi/Shell/hosts` NSView embed and host-shared `GRContext` lease, the `Rasm.Rhino` host-attach fact stream feeding the surface-host axis, and the document-transaction commit at the grid edge.
- No new package admission in the host-boundary source; the app root admits the host packages and pins the app-root-only surfaces (OTLP exporter, the MCP HTTP transport, the WASM and industrial-protocol runtimes, Kestrel/gRPC, the Serilog host bridge).
- The host boundaries reference only `Rasm` and enter at the app root, never as an interior dependency of a host-neutral package; `Rasm.AppUi` references the abstract surface seam columns, never a host API, and Rhino owns Make2D/sheet-layout/native file I/O while `Rasm.Bim` independently owns the universal IFC semantics — the two coexist.
- The composition is the final implementation-start gate: the in-host Generic Host boot and cooperative-then-forced drain across all four app packages, the cross-process correlation/tenant threading over the UDS/HTTP2 hop, and the live host-shared GPU lease are the live-host probes the app root resolves, each named on its own folder `TASKLOG.md` as a blocked live-host probe.
- The HLC two-half causal stamp and tenant context the `AppHost/Runtime/ports` fan-in mints are exported once on the wire for the peers to read; the cross-runtime reproduction of that causal stamp and tenant frame is the `libs/.planning` causal-tenant-identity seam, never re-minted branch-side.

[QUEUED] Land the capability control plane across the runtime spine, the execution lane, and the UI.
- Build the one self-describing op catalog spanning packages: project each `CapabilityDescriptor` to an MCP tool and an SDK at `AppHost/Agent/capability` and `AppHost/Agent/mcp`, route the command algebra onto the `Compute/Runtime/admission` rail, and gate exposure through the `AppUi/Shell/commands` algebra.
- Integrate ModelContextProtocol and Microsoft.Extensions.AI.Abstractions (AppHost), composing the Compute proto vocabulary and the AppUi intent table.
- The descriptor source is owned once in AppHost; Compute executes the invocation and AppUi projects availability, each consuming the descriptor as settled vocabulary, never minting a second op-metadata owner. The signed grant attestation chains into the determinism event log over the Persistence op-log at the wire.
- The cross-language SDK-codegen and MCP-agent consumption by the sibling branches is the `libs/.planning` capability-catalog seam, referenced there and never restated; this card lands only the C# concert.

[QUEUED] Land the federated coordination cockpit across BIM semantics, durable annotation, and the UI.
- Compose the openBIM coordination surface: the `Rasm.Bim/coordination` BCF 3.0 topic/component model and GlobalId-stable diff, the `Persistence/annotation` anchor algebra and CDE sync, the `Persistence/federation` entity graph and rule engine, and the `AppUi/coordination` board projection over the viewpoint codec and a CRDT comment thread.
- No new branch-level package; each folder admits its own BCF/IDS/bSDD surface, and the board composes admitted AppUi owners.
- The BCF semantic model is owned once in `Rasm.Bim` and consumed at the boundary; AppUi owns the board projection and never re-mints a BCF schema or writes BCF-XML in the app-platform leaf. The diff joins by GlobalId plus the one content-key, and the round-trip persists through the op-log changefeed.
- The IDS audit and the rule engine ride the one federated graph; the C# Bim owner authors and parses the IDS spec while the Python ifctester companion is the deterministic cross-tool oracle at the `libs/.planning` companion seam.

[QUEUED] Land the deterministic replay observatory across the spine, the op-log, and the notebook.
- Build the reproducibility kernel spanning packages: the `AppHost/Runtime/determinism` pinned RNG/float-mode/fingerprint and the hash-chained event log over the `Persistence/Sync/collaboration` op-log, replay-verify with per-step content-hash proof, and the `AppUi/Editing/notebook` capability pin and replay bundle.
- Integrate System.IO.Hashing (the content-address seed) and the BCL cryptography inbox (the detached signature); no new central admission.
- The event log is the single hash-chained command log riding the durable op-log at the Persistence wire, never coupled to its interior; the notebook cell-edit op projects onto the op-log CRDT delta, and the determinism kernel owns the seed and float mode that the cross-machine replay-verify consumes from the Compute provider-determinism fingerprint.
- Cross-machine replay-verify is unsound without the Compute provider-determinism fingerprint; the cross-runtime reproduction of the seed is the `libs/.planning` content-address seam, and the inward re-import of the Python `HandoffAxis` graduation evidence by the one content key is the `libs/.planning` graduation-evidence seam — the event log references the offline result by key, never re-running the Python training loop.

[QUEUED] Branch registry-truth sweep — reconcile stale README registry and design-card prose against the one C# manifest.
- The per-folder README registries and a handful of design-card status markers drifted from manifest reality: AppUi cards on `GPU_BACKEND_PORT`/`T-EXPORT-WRITERS` claim a "new manifest admission" for VelloSharp/ACadSharp/netDxf/OpenXml already admitted (re-ground to "row/factory work remains"); Fabrication `CLIPPER2_API_CATALOGUE [QUEUED]` and `KINEMATICS_SPLIT [QUEUED]` are stale where the catalogue and the serial-chain page already exist (mark `[COMPLETE]`); the Clipper2 `Triangulate` catalogue-vs-prose contradiction re-grounds on the author's "buggy, I regret releasing it" caveat rather than a false "internal demo type"; Persistence README omits the used `Npgsql.NetTopologySuite` entry and AppUi README omits bare `VelloSharp` under `[GPU_BACKEND]`.
- No package change; a README/card prose reconciliation internal to each folder against the one C# manifest, never a per-folder manifest.
- Each folder owns its README and design cards; the sweep aligns prose to the manifest truth at the folder boundary and lands as folder-local edits, never a branch-level registry.
- Prose-only remediation that proposes no capability; reclassified from an idea because it reconciles stale prose rather than opening a surface.

[QUEUED] Branch README marker sweep — trim the universal kernels from the folder README registries.
- The Bim/Materials/Compute/AppUi folder READMEs redundantly list `LanguageExt.Core` and `Thinktecture.Runtime.Extensions` core in their flat package lists; those universal kernels are branch-registered (`README#[2]-[FUNCTIONAL_CORE]`, "Every C# folder") and excluded from catalogues entirely, so they are trimmed from the folder READMEs and each folder lists only its own additions.
- No package change; a folder-README trim internal to each folder.
- The branch README §2 owns the cross-cutting universal-kernel registration; a C# folder README carries no `[CROSS_CUTTING]` section (C# holds no branch `.api/`) and never re-lists the universal kernels.
- Prose-only remediation, no capability proposed; aligns the folder READMEs to the form standard.

[QUEUED] Provision the shared C# test corpus node and freeze the two cross-package golden fixtures — from `BRANCH_TEST_NODE_PROVISIONING`.
- Stand up the one shared test-corpus node every consuming spec reads, holding both cross-package golden fixtures as serialized frozen bytes: the `CANONICAL_BYTE_IDENTITY` single-triangle reference (`VertexCount=3`; edges `(0,1),(0,2),(1,2)`; face cycle `[0,1,2]`) with its 52-byte stream and `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest, and the `CLASH_GOLDEN` 8-primitive `BoundingBox[]` set built with `BuildPolicy.Canonical` plus its expected `NodeCount` and clash-pair set.
- Integrate `System.IO.Hashing` (`XxHash128`) for the adjacency digest; the corpus is the resource the kernel `Spatial/reconciliation#CANONICAL_BYTE_IDENTITY` and `Spatial/index#CLASH_GOLDEN` specs and the Persistence `Version/diff#STRUCTURAL_DIFF` and Compute `Solver/clash#CLASH_GOLDEN` specs all read.
- The corpus node is the single owner of both frozen byte sets; the two existing two-sided fixture cards (the clash-seam and the canonical-adjacency byte-identity captures) consume it rather than each minting its own bytes, and the `libs/.planning` `ONE_WIRE_FIXTURE_CORPUS` seam names this shared C# corpus as one of its consumers.
- The `CANONICAL_BYTE_IDENTITY` bytes are host-derived-confirmed and freeze-ready; the `CLASH_GOLDEN` bytes are blocked on pinning the 8 `BoundingBox(min,max)` coordinates in the `Spatial/index#CLASH_GOLDEN` design (the SAH split, `Order` permutation, and node count are not deterministically derivable until the real coordinates land), so this node freezes the adjacency fixture now and the clash fixture once the design pin lands.

[BLOCKED] Admit the `Rasm.AppUi.Tests` node so the headless render-hash and journal-replay lanes run under virtual time — from `BRANCH_TEST_NODE_PROVISIONING`.
- Admit the `Rasm.AppUi.Tests` project node on the test rail so the AppUi `evidence/#HEADLESS_DERIVATION` proof matrix, the render-hash lanes, and the command-journal replay run deterministically under virtual time, with `FrameHash` equality the render contract.
- Integrate Avalonia.Headless, Avalonia.Headless.XUnit, and Verify.XunitV3 (the Verify catalogue) for deterministic frame capture and the snapshot artifact diff; the journal-replay route composes the AppHost `ReplayVerify`/`EventLog`/`ContentHash` determinism vocabulary as settled.
- The node lands the AppUi `T-HEADLESS-PROOF` test-project node it is blocked on; the render-hash lane is the snapshot rail under `testing-cs`, kept distinct from the static-managed law rail and the kernel oracle harnesses.
- Blocked on the test-project node and the Verify catalogue landing; admitting it unblocks every AppUi spec and render-hash lane (`T-HEADLESS-PROOF`).

[QUEUED] Provision the `Rasm` kernel testkit node — the `BigInteger` oracle and the five law-matrix harnesses — from `BRANCH_TEST_NODE_PROVISIONING`.
- Stand up the kernel testkit node carrying the `System.Numerics.BigInteger` exact oracle and the five CsCheck law-matrix harnesses: predicate sign-exactness (`Numerics/predicates#NUMERIC_DETERMINISM`), Delaunay correctness (`Meshing/delaunay#BOWYER_WATSON_DELAUNAY`/`#CONSTRAINT_RECOVERY`), re-anchor injectivity (`Spatial/naming#REANCHOR_INJECTIVITY`), LM convergence (`Processing/solver#LM_CONVERGENCE`), and the implicit-point exact-sign extension on the same oracle.
- Integrate CsCheck and the active repo testkit under `testing-cs`; `System.Numerics.BigInteger` as the exact rational-determinant and in-circum oracle, with the `Rasm.Geometry.Numerics` `Predicate`/`Expansion`/`ErrorBound` floor under test.
- Internal to the kernel test scope; the harnesses are the law authority over the total-by-construction kernel, never a structure gate, and a new predicate extends the law-matrix as one exact-sign row rather than a parallel oracle. The byte-equality harness for `CANONICAL_BYTE_IDENTITY` reads the shared corpus node, never its own fixture.
- The five harnesses are already carried as per-page kernel `TASKLOG` cards; this node is the shared `BigInteger`-oracle test-project home those cards land in, so the oracle is provisioned once for the whole kernel rather than per harness.

[QUEUED] Confirm the BenchmarkDotNet and SharpFuzz test-project membership on the manifest — from `BRANCH_TEST_NODE_PROVISIONING`.
- Confirm the BenchmarkDotNet benchmark project and the SharpFuzz fuzz project are members of the test rail on the central C# manifest so the durable speed/allocation budget gate and the parser/decoder crash-resilience harness resolve and run.
- Integrate BenchmarkDotNet (the release-mode budget gate) and SharpFuzz (the fuzz harness); both stay centralized in `Directory.Packages.props`, never pinned per project.
- The benchmark and fuzz rails are the specialized rails under `testing-cs`, kept orthogonal to the static-managed law rail, the evidence rails, and the architecture rail; a benchmark proves no correctness and a fuzz harness proves no managed-law behavior.
- Membership confirmation only — no new capability; the rail classes already exist in doctrine, and this card closes the manifest-membership honesty gap before any benchmark or fuzz spec lands.

[QUEUED] Stand up the `Rasm.Compute/Symbolic/` sub-domain and register the orphaned CAS manifest rows — from `SYMBOLIC_PARAMETRIC_ALGEBRA`.
- Open the new `Rasm.Compute/Symbolic/expression` design page as the closed `SymbolicExpr` owner over `MathNet.Symbolics` (`Expression`/`Calculus.differentiate`/`Compile.compileExpression`/`Infix`/`Rational`/`Algebraic` simplify) and `FParsec` (infix-string parse), with the `SymbolicExpr` value content-addressed by the `XxHash128` canonical-normal-form key so an identical formula reuses its compiled `Func<double,double>`; the symbolic Jacobian feeds `Tensor/quadrature#OWNED_BUILDS` and `Solver/optimizer`, the symbolic dimension feeds the `Symbolic/units` boundary, and the result graduates inward as the cross-libs `symbolic` `HandoffAxis` arm.
- `MathNet.Symbolics`, `MathNet.Numerics.FSharp`, and `FParsec` are present in the one C# manifest, already README-registered under `Rasm.Compute/README.md#[SYMBOLIC]` and catalogued at `Rasm.Compute/.api/api-mathnet-symbolics.md` (the twelve verified F# modules, the `Expression` DU cases, the `SymbolicExpression` fluent wrapper); the open work is the missing `Symbolic/expression` design-page owner, not the registration or catalogue. The owner page closes the catalogue's two `[4]-[RESEARCH]` flags first — the exact `Compile.compileExpression{,1,2,3}`/`compileComplexExpression` arity and `Func<>` return shape, and the `SymbolicExpression` operator/conversion/`.Variables` surface — before any compile or case-matching fence transcribes a signature.
- The owner lands inside `Rasm.Compute`, composing the kernel value-objects and the AppHost clock as settled vocabulary; the `Rasm.Persistence/Store/profiles#COST_CATALOG` formula rows and `Query/federation#RULE_PLAN` QTO formulas consume the parsed `SymbolicExpr` at the wire instead of a string `eval`, and `Rasm.Materials/physical-properties` admits a parametric constitutive law as one expression — each a consumer of the one CAS owner, never a second expression model.
- Three F#-interop packages enter the C# branch for the first time (`FSharp.Core` is the transitive floor): the F# `Expression` discriminated union crosses the boundary as settled vocabulary the C# owner wraps in `SymbolicExpr`, with the simplify/differentiate/compile calls the only F# surface the C# side touches — a hand-rolled C# expression-tree parser beside the admitted CAS is the named reimplementation defect.

[QUEUED] Realize the symbolic-formula consumers across Persistence and Materials — from `SYMBOLIC_PARAMETRIC_ALGEBRA`.
- Thread the `SymbolicExpr` owner into the `Rasm.Persistence/Store/profiles#COST_CATALOG` formula-evaluated 5D rollup (parse the cost formula once into a typed `SymbolicExpr`, not a runtime string eval), the `Rasm.Persistence/Query/federation#RULE_PLAN` QTO derived-quantity formulas, and the `Rasm.Materials/physical-properties#PROPERTY_SETS` parametric temperature/strain constitutive curves.
- No new package admission — the consumers compose the `Rasm.Compute/Symbolic/expression#SYMBOLIC_EXPR` owner as settled vocabulary; the formula parse and dimensional check happen once at admission, the compiled delegate caches by content key.
- The symbolic owner is `Rasm.Compute`-resident and the consumers are AEC-domain/app-platform; they meet at the parsed-expression contract, never an interior `MathNet.Symbolics` call in a Persistence or Materials fence — a second formula parser in any consumer is the drift defect this card forecloses.
- Each consumer page gains a single fence cluster reading `SymbolicExpr.Parse`/`Compile`/`Differentiate`; the cost-catalog string `eval`, the QTO hand-bound lambda, and the Materials frozen constitutive constant are the deleted forms.

[QUEUED] Open the `Rasm.Bim/reconstruction/` sub-domain for scan-to-BIM primitive fitting — from `REALITY_CAPTURE_TO_BIM`.
- Author the new `Rasm.Bim/reconstruction` design page: a `ReconstructionPrimitive` closed-union (plane/cylinder/torus/freeform) folding the kernel-registered segmented cloud into `BimElement` rows with `ElementPredicate`-classified `IfcClass` and a confidence band carried on the `properties#PROPERTY_SETS` Pset, reading the kernel `Rasm/Vectors` `Align`/cloud-ICP and `Rasm/Geometry/spatial` segmentation as settled vocabulary and the `Rasm.Compute/Runtime/codecs` splat/point payload as the content-keyed input.
- No new branch package; the segmentation/registration is the kernel's, the splat/point payload is Compute's, and the offline RANSAC/learned-segmentation fit crosses as the `libs/.planning` graduation `reconstructed-mesh`/`topology-graph` `geometry` axis (the C# side re-imports, never an in-process learned segmenter).
- The reconstruction arm composes the one `BimModel` and the `Model/query#ELEMENT_SET` algebra, never a parallel model surface; the `Rasm.Persistence/Query/federation` graph joins the reconstructed element to its source-cloud lineage by `(GeometryHash, content-key)` and `Rasm.AppUi/Render/reality` plays the capture back over the reconstructed model — each a settled consumer, the reconstruction owned once in Bim.
- This is the single new sub-domain that closes the cloud-to-semantics gap between the realized `AppUi/realitycapture` capture viewport and the `Rasm.Bim` semantic graph; the primitive fit is the only new owner, the registration and tessellation are reused.

[QUEUED] Land the drafting-and-document production rail across the kernel/Fabrication projection, the AppUi sheet engine, and the OOXML/DWG export — from `DRAFTING_DOCUMENT_PRODUCTION`.
- Compose the construction-documentation rail: the `Rasm.Fabrication/Posting/projection#PROJECTION_HIDDEN_LINE` BSP visibility kernel and the kernel `Rasm/Geometry` arrangement produce the 2D drawing geometry sharing the viewport camera basis, the `Rasm.AppUi/Render/drafting` ISO/ANSI/JIS sheet-set + ASME Y14.5 dimensioning composes the sheet, the `Rasm.Persistence/Query/federation#ELEMENT_SET` query and `Rasm.Persistence/Sync/schedule` activity network feed the schedule/quantity tables, and the `Rasm.AppUi/Render/capture` ACadSharp/netDxf/DocumentFormat.OpenXml writer emits the deliverable.
- No new package admission — `ACadSharp`, `netDxf`, and `DocumentFormat.OpenXml` are admitted and catalogued at `Rasm.AppUi/.api/api-drafting-export.md`; the rail exploits the lightly-used export surface to its full capability (DWG/DXF entity round-trip, OOXML schedule/report tables).
- The drawing is a view of the federated model, never a re-modeled document; the sheet is owned once in AppUi, the projection once in Fabrication/kernel, the schedule once in Persistence — a new sheet standard is one title-block row, a new export format one writer row, and a second drafting or document model is the drift defect.
- The rail binds the AppUi `drafting`/`visuals` owners, the Fabrication `projection`, and the Persistence `federation`/`schedule` owners that already exist; this card lands the cross-folder composition, each folder owning its leg and meeting at the projection geometry and the `ElementSet` query.

[QUEUED] Branch coverage-audit sweep — close the symbolic-CAS owner gap (catalogue + README already landed).
- The coverage audit against the one C# manifest finds the three symbolic-CAS rows — `MathNet.Symbolics` (0.25.0), `MathNet.Numerics.FSharp` (6.0.0-beta2), `FParsec` (2.0.0-beta2) — already README-registered (`Rasm.Compute/README.md#[SYMBOLIC]`) and catalogued (`Rasm.Compute/.api/api-mathnet-symbolics.md`); the sole residual gap is the `Rasm.Compute/Symbolic/expression` design-page owner no fence reads yet ("symbolic fill" in `Tensor/blas` is the sparse-factorization fill count, not symbolic algebra). The remaining manifest rows (drafting-export, Bridge stack, OnnxRuntime EP variants, transitive floors) all carry an owner or a documented app-root/tooling placement.
- The owner lands as the `SYMBOLIC_PARAMETRIC_ALGEBRA` design-page task above, which closes the catalogue's `[4]-[RESEARCH]` arity flags; this card is the audit finding of record so the gap is never re-surfaced as missing registration or catalogue.
- No package change — every row already lives in `Directory.Packages.props`; the gap is the design-page owner only, not admission, README registration, or catalogue. The `Bridge` stack (`StreamJsonRpc`/`Nerdbank.MessagePack`/`PolyType`/`Nerdbank.Streams`) is correctly out of branch scope — owned by `tools/rhino-bridge`, not a `libs/` library.
- Coverage honesty closure: the manifest-to-owner reconciliation is complete once the symbolic design-page owner lands; no other admitted-but-unowned C# package remains.

## [2]-[CLOSED]

(none)
