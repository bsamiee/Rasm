# [RASM_TASKLOG]

The kernel's open and closed work, distilled from the ideas and the design-page RESEARCH residuals. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]` open, `[UPSTREAM-BLOCKED]`/`[HOST-PROBE-DEFERRED]` blocked on an external dependency, `[COMPLETE]`/`[DROPPED]` closed — plus the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations. `[1]-[OPEN]` carries live work; `[2]-[CLOSED]` records settled or dropped tasks.

## [1]-[OPEN]

[UPSTREAM-BLOCKED] Boolean tier-3 native arrangement asset gate, SCALE PATH ONLY (`healing/repair#BOOLEAN_NATIVE_ASSET`).
- The design-resolvable half is LANDED: `healing/repair.md` `Kernels.BooleanArrangement` now composes the managed `arrangement/arrangement#MESH_BOOLEAN` companion through `Arrangement.Apply` for the common cases (the `EXACT_ARRANGEMENT_BOOLEAN` owner, Cherchi-Attene indirect predicates, no native asset, no epsilon snap), `BeyondManagedScope` routing `NativeAssetMissing` only when the operand face-product exceeds `BooleanManagedScopeFaceProduct`.
- EXTERNAL DEPENDENCY (the remaining blocked half): a robust exact-arithmetic mesh-arrangement native asset for the future performance/scale path the managed exact arrangement does not cover — a charter amendment with its RID burden assessed. Stays UPSTREAM-BLOCKED on that scale path alone; the common-case correctness is owned by the managed companion, so this is non-blocking for correctness.
- Internal to the healing sub-domain; the managed companion landed the `Apply` re-route this pass, the native-asset admission reserved for scale.

## [2]-[CLOSED]

[COMPLETE] EXACT_ARRANGEMENT_BOOLEAN — `arrangement/arrangement.md` owns the `Arrangement` `[Union]` (MeshBoolean/PlanarOverlay/CellComplex) over `ArrangementStore`, GWN cell classification, `BooleanOp`-selected weld, and the `BooleanReceipt` typed evidence (sibling-authored page; this folder's `healing/repair#BOOLEAN_NATIVE_ASSET` re-route and `offsetting/offset#MINKOWSKI_CONVOLUTION` `Assemble` seam compose it).
[COMPLETE] FAULT_BAND_EXTENSION — `faults/faults#FAULT_BAND` owns the seven new `GeometryFault` sub-band cases `DegenerateArrangement(2450)`/`IntersectionFault(2460)`/`FitFault(2470)`/`ParameterizationFault(2480)`/`ProjectionFault(2490)`/`DecimationFault(2500)`/`EncodingFault(2510)` on the `Code`/`Message` total Switch arms, `[CASES]` 10→17, the Growth "2450+" prediction realized.
[COMPLETE] PRIMITIVE_FIT_FRONTIER — `fitting/fit.md` owns the `FitOp` `[Union]` (plane/sphere/cylinder/cone/torus/line) over the MLESAC consensus + LM refine, returning the typed `FitReceipt` (sibling-authored page).
[COMPLETE] SURFACE_PARAMETERIZATION_OWNER — `parameterization/flatten.md` owns the `ParamOp` `[Union]` (harmonic/LSCM/ARAP/BFF) over the `Vectors` DEC operator substrate, returning the typed `ChartAtlas` (sibling-authored page).
[COMPLETE] OFFSETTING_WAVEFRONT_BODY_TRANSCRIPTION — `offsetting/offset#STRAIGHT_SKELETON`/`#MEDIAL_AXIS`/`#MINKOWSKI_CONVOLUTION` owns the bodied `[WAVEFRONT]`/`[PROJECTIONS]` clusters (`Seed`/`EnqueueAll`/`EdgeCollapseTime`/`SplitTime`/`Collapse`/`Divide`/`MedialFrom`/`OffsetSnapshot`), the `Assemble` re-route through `arrangement/arrangement#PLANAR_OVERLAY`, and the `weighted` `OffsetKind`/`OffsetOp` growth row over `OffsetPolicy.EdgeSpeed`.
[COMPLETE] INCIRCUM_IMPLICIT_PREDICATE_GROWTH — `numerics/predicates#ROBUST_PREDICATES`/`#INDIRECT_PREDICATES` owns the `Predicate.InCircleLPI`/`InSphereTPI` implicit-point in-circum members (exact-only homogeneous lift over `Lpi`/`Tpi` with the `lambda^2` denominator clear and sign-flip on negative `lambda`), `[CASES]` 6→8, the router parenthetical naming the implicit family.
[COMPLETE] PREDICATE_LAW_MATRIX — `numerics/predicates#NUMERIC_DETERMINISM`/`#INDIRECT_PREDICATES` owns the `PredicateLaws` obligation extended over the eight members (four direct + four implicit-point) vs the `BigInteger` rational oracle; design-settled, the harness is the `testing-cs` obligation.
[COMPLETE] REANCHOR_INJECTIVITY_HARNESS — `topology/naming#REANCHOR_INJECTIVITY` owns the `ReanchorInjectivityLaws` obligation (naming fences transcription-complete, the per-`EntityKind` `claimed` set guards both cross-cases); design-settled test obligation.
[COMPLETE] LM_CONVERGENCE_HARNESS — `constraints/solver#LM_CONVERGENCE`/`#WITNESS_DOF` owns the `ConstraintLaws` obligation (LM iterate + landed `WitnessAnalyze` transcription-complete); design-settled test obligation, the one sanctioned MathNet Cholesky try/catch the sole boundary.
[COMPLETE] DELAUNAY_LAW_MATRIX — `tessellation/delaunay#BOWYER_WATSON_DELAUNAY`/`#CONSTRAINT_RECOVERY` owns the `DelaunayLaws` obligation (fences transcription-complete, the Steiner flip composes the landed `OrientLPI`/`OrientTPI`); independent of INCIRCUM, design-settled test obligation.
[COMPLETE] OFFSETTING_LAW_MATRIX — `offsetting/offset#STRAIGHT_SKELETON`/`#MEDIAL_AXIS`/`#MINKOWSKI_CONVOLUTION` owns the `SkeletonLaws` obligation (valid planar skeleton, simple offset at `t`, medial deformation-retract, simple Minkowski boundary, weighted-speed deterministic ordering); design-settled test obligation over the bodied clusters + the arrangement `Assemble` seam.
[COMPLETE] NEW_OWNER_LAW_MATRICES — the seven owner pages (`arrangement`/`intersection`/`fitting`/`parameterization`/`projection`/`simplification`/`encoding`) carry their `ArrangementLaws`/`IntersectionLaws`/`FittingLaws`/`ParameterizationLaws`/`ProjectionLaws`/`DecimationLaws`/`EncodingLaws` obligations named against the now-landed fences; each is a `testing-cs`/`BRANCH_TEST_NODE_PROVISIONING` harness obligation, no open design fence.
[COMPLETE] HIDDEN_LINE_PROJECTION — `projection/view.md` owns the `ViewOp` `[Union]` (silhouette/hidden-line/section/outline) over the Newell-Newell-Sancha BSP visibility kernel, the section cut one `IntersectOp.Apply`, returning the typed `DrawingProjection` (sibling-authored page).
[COMPLETE] ROBUST_MESH_DECIMATION — `simplification/decimate.md` owns the `SimplifyOp` `[Union]` (quadric-collapse/progressive-mesh/voxel-remesh/feature-preserve) over the QEM edge-collapse queue gated by the exact `Orient3D` sign, returning the typed `DecimationReceipt` (sibling-authored page).
[COMPLETE] GEOMETRY_ENCODING_PACK — `encoding/pack.md` owns the `PackOp` `[Union]` (point-cloud/mesh-patch/voxel-grid/brep-patch) over the eight-row `EncodingChannel` lattice composing the live `Vectors` readers, returning the typed `EncodedGeometry` with the `CANONICAL_BYTE_IDENTITY`-keyed round-trip witness (sibling-authored page).
[COMPLETE] MATURE_FOLDER_DE_HARDCODE_AUDIT — read-only audit of `Domain/Geometry.cs`/`Analysis/Topology.cs`/`Analysis/Intersect.cs`: the `Domain/Kind`/`Topology` coercion table stays `FrozenDictionary`/`FrozenSet` table-driven, `Analysis/TopologyScalar` + `Analysis/IntersectionKind` are composed vocabulary the new owners ALIGN to at the seam (never re-minted), and the one genuine hardcode `RayQuery.MaxReflections` (constrained `>=1 and <=1000` at `Analysis/Intersect.cs`) is a `BuildPolicy`-style column candidate only where a real downstream varies it; no mature owner thinned, the audit confirms the seam.
[COMPLETE] API_CATALOGUE_SURFACE_CONFIRMATION — coverage-confirmed the six `.api` catalogues: MathNet `Integrate`/`FindRoots`/`Interpolate`/`SpecialFunctions` present in `api-mathnet-numerics.md`; the factorization surface `Cholesky`/`Svd`/`Evd` (the Parameterization dense eigen-solve + constraint solver compose) confirmed present in `api-mathnet-providers.md` (NOT the numerics catalogue); Tensors `ReadOnlyTensorSpan`/`TensorPrimitives`, CSparse `Cholesky`/`LU`/`QR`/`SparseMatrix`, and `Rhino.Geometry` value-type rows present — no owner composes an absent row.

[COMPLETE] Robust intersection lattice owner design page (`intersection/intersect#INTERSECTION` owns the predicate-exact `IntersectOp` `[Union]`). Realized: the `intersection/intersect.md` page authoring the `IntersectOp` `[Union]` (`SegmentSegment`/`SegmentTriangle`/`TriangleTriangle`/`RayMesh`/`MeshMesh`/`PlaneMesh`), the `IntersectKind` `[SmartEnum<string>]` discriminant binding `GeometryKeyPolicy`, the `Crossing` carrier (materialized `Point3d` `Site` decided by the exact `Orient3D`/`Orient2D` straddle signs + exact `Expansion` `OrderKey`, the exactness living in the sign decisions and the order key), the flat `IntersectStore` SoA crossing memory with its one-cell `Live` write cursor, and the one `Apply` fold — the Guigue-Devillers exact triangle-triangle interval, the `spatial/index` BVH-broad-phased mesh-mesh/plane-mesh ordered crossing chain, the four-`Orient2D`-sign segment straddle, all over the `numerics/predicates` exact-sign floor. The `healing/repair`/`offsetting/offset`/`arrangement/arrangement` inline crossing tests ALIGN inward to `Intersection.Apply` (the source-pass collapse lands on the consuming pages); the host `Analysis/Intersect.cs` parametric surface is NOT thinned. The `IntersectionLaws` law-matrix is the remaining `testing-cs` obligation (W5 `NEW_OWNER_LAW_MATRICES`, gated until the harness lands), not an open design fence.

[COMPLETE] Plane-mesh intersection growth row + section consumer alignment (`intersection/intersect#INTERSECTION`, `#ORDERED_CROSSING_CHAIN`). Realized: the `IntersectOp.PlaneMesh` case + `IntersectKind.PlaneMesh` row riding the SAME exact-sign crossing fold and `Crossing` carrier — `CrossPlaneMesh` plane-slab BVH-broad-phases the straddling triangles, `Section` runs the exact plane-side `Orient3D` straddle per triangle emitting a materialized `PlaneCrossPoint` segment, and `Chain` assembles the ordered section `Polyline` by the exact `OrderKey`. The `projection/view#HIDDEN_LINE_PROJECTION` `ViewOp.Section` and the AEC-domain section/elevation ALIGN to `IntersectOp.Apply`/`Crossing` as the one section owner, never a host Make2D round-trip — one case on the existing union, not a sibling section type.

[COMPLETE] Clash-seam node-link golden fixture (`spatial/index#CLASH_SEAM`, `#CLASH_GOLDEN`). The node-link wire is settled and `[CLASH_GOLDEN]` is fully PINNED — the 8 `BoundingBox(min,max)` coordinates, `NodeCount == 3`, the 160-byte `Bounds`/`Nodes` LE golden stream (descriptors `2097154`/`-5`/`-8388613`, identity `Order` tail), and the 4-pair clash set `{(0,1),(2,3),(4,5),(6,7)}` are byte-derivable on both sides; the corpus row in `topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` is REAL. The two-sided byte-equality spec is the only remaining test artifact (a `testing-cs` obligation, not a design gap).

[COMPLETE] Canonical-adjacency byte-identity golden fixture (`topology/reconciliation#CANONICAL_BYTE_IDENTITY`). The geometry-side fixture is REAL and frozen (the 52-byte single-triangle stream + `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest, morph and topology-break discriminators). The remaining cross-package byte-equality assertion against the Persistence `GeometryHash` is the synthesis tier's two-package test scope — a `csharp:Persistence/...#STRUCTURAL_DIFF` alignment, outside this folder's write-scope.

[COMPLETE] Degradation-keyed refit and agglomerative builder (idea `DEGRADATION_REFIT`, `spatial/index#DEGRADATION_REFIT`, `#AGGLOMERATIVE_BUILD`). Realized: `BuildPolicy.RefitDegradationLimit` column, the `SpatialKind.Agglomerative` `Builders` row writing the shared `NodeStore` (`BuildAgglomerative` Morton-presorted nearest-neighbour clustering), `Bvh.BuildCost`/`Builder` columns, and the `SahCost`-vs-baseline degradation-keyed `Refit` rebuild trigger — the Compute clash seam rides the same `ToAcceleration` projection unchanged.

[COMPLETE] Indirect-predicate family extension (idea `INDIRECT_PREDICATES`, `numerics/predicates#INDIRECT_PREDICATES`). Realized: `OrientLPI`/`OrientTPI` members over `Lpi`/`Tpi` exact homogeneous constructions evaluating the orientation sign over a constructed line-line / three-plane intersection point without rounding, with `OrientLPIBound`/`OrientTPIBound` filter rows. Unblocks the healing self-intersect split, the Delaunay Steiner-point recovery, and the new exact-arrangement boolean and intersection-lattice owners. The `InCircleLPI`/`InSphereTPI` in-circum companions are the OPEN growth task above.

[COMPLETE] Generalized-winding-number query (idea `GENERALIZED_WINDING`, `spatial/index#GENERALIZED_WINDING`). Realized: `SpatialQuery.Winding` case + `QueryResult.Scalar` + the `Winding`/`NodeMoment`/`SolidAngle` Barill/Jacobson tree-based GWN best-first descent over the existing BVH — a query case, not a new structure; the inside/outside classifier the new exact-arrangement boolean cell-keep predicate composes.

[COMPLETE] Witness-configuration DOF analysis (idea `WITNESS_DOF`, `constraints/solver#WITNESS_DOF`). Realized: `DofAnalysis.RedundantConsistent` row + `ConstraintSolver.WitnessAnalyze` (numeric Jacobian rank at the seed witness, `Svd`-tail residual-consistency test distinguishing redundant-consistent from over-constrained-inconsistent) wired into the `Solve` over-constraint gate so a redundant-but-consistent system solves.

[COMPLETE] Mature-folder `Geometry` naming reconciliation (`ARCHITECTURE#NAMESPACE_LAW`). Settled: `Domain/Geometry.cs` (namespace `Rasm.Domain`, types `Topology`/`Kind`/`CurveForm`) and the `Rasm.Geometry.*` robust-core tree are DISJOINT namespace roots; the bare `Geometry` token is a filesystem-path coincidence the disjoint roots already resolve. No rename, no namespace re-scope, no source move — authored as settled kernel ARCHITECTURE vocabulary, the "before the source tree" trigger dropped (source-tree creation is out of scope).
