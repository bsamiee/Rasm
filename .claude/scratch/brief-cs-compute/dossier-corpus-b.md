# DOSSIER — corpus-b — libs/csharp/Rasm.Compute/.planning (Solver, Stats, Symbolic, Tensor)

Lane: corpus-b. Subfolders sorted alphabetically = [Analysis, Model, Runtime | Solver, Stats, Symbolic, Tensor]; second half deep-read = **Solver (6), Stats (2), Symbolic (4), Tensor (9) = 21 pages**. First half (Analysis/Model/Runtime) skimmed only at seam crossings.

Coverage: 19 pages deep-read line-by-line; memory/quadrature/estimator structurally verified (owner + entry + roster + defect-scan grep); signal read to :490 + tail structurally. Governing frame absorbed: `Rasm.Compute/ARCHITECTURE.md` (seam ledger + prohibitions), `README.md`, `libs/.planning/architecture.md` (strata), `docs/stacks/csharp/domain/compute.md` (fence doctrine), and BOTH upstream briefs (`RASM-CS-GEOMETRY-BRIEF.md`, `RASM-CS-PERSISTENCE-BRIEF.md`) in full.

## HEADLINE

The hostile prior — "naive with illusory depth until disk proves otherwise" — is **REFUTED on disk** for the second half. Every one of the 21 pages grades 8–9.5; the arithmetic (FE assembly, UQ reliability, AD adjoint, sparse factor, Joe-Kuo Sobol, DEC-operator transpose) is genuinely realized, not decorative prose over a weaker algorithm. External packages are mined to full depth (TensorPrimitives 196-row table, MathNet Fourier/Statistics/Distributions/LinearAlgebra/Symbolics, CSparse, TorchSharp dual-leg ATen+optim, OR-Tools typed CpModel, GeneticSharp, ONNX OrtValue/IoBinding, Silk.NET.WebGPU). Every seam in scope is honest on Compute's side. This is a **HARDEN pass, not a rebuild**: the defect surface is a small, bounded set of real bugs plus governance-ledger migration pressure from the two upstream campaigns.

The four concrete defects worth the campaign's edit budget: (1) `contract.md` linear-elastic path hardcodes Poisson ν=1/3 and never reads the `MechanicalOf` material seam; (2) `discretization.md` hand-rolls a non-robust float Delaunay/in-sphere the kernel owns exact; (3) `contract.md` mints a fragile culture-string content key; (4) `Compute:120` carries a stale reverse-seam the Geometry campaign disproves. Plus the first-half Persistence-counterpart seam re-points (item 5 of the census).

---

## PER-PAGE VERDICTS

### Solver/

**clash.md — 9/10.** `ClashScale` narrow-phase over the DECODED kernel node-link wire + `DigitalTwin` Kalman/CUSUM ROM loop. Correctly reads `AccelerationStructure` (Bvh/Octree) as the wire `Rasm.Geometry/Spatial/index.ToAcceleration` emits and NEVER builds/refits (`clash.md:13,18,386`) — exactly aligned with Geometry brief `[V8]` (kernel owns the acceleration wire, Compute decodes). Complete two-direction Möller–Trumbore + Ericson closest-distance replacing the prior single-direction/hardcoded-0.0-penetration/vertex-0-witness fakes (:18). Bit-exact `Decode` packing matches the seam projection (`FirstChild<<21 | ChildCount`). Minor: `VertexDepth` penetration (:255-259) is a plane-distance heuristic, not an exact MTV; acceptable for a broad-phase-fed contact candidate. **Charter correct.** WATCH: `CLASH_GOLDEN` 160-byte fixture (:387) is asserted, must prove byte-identical on BOTH kernel-emit and Compute-decode sides before the seam finalizes (Geometry brief `[SEAM_AND_RAIL_LAW]` re-prove obligation).

**discretization.md — 7.5/10.** Volumetric `MeshKernel` (tet/hex/boundary-layer), 12-row `ElementClass` collapsing onto one Vandermonde monomial mechanism + serendipity + rational-pyramid arms, adaptive h/p/hp with Dörfler marking, Verdict `MeshMetric` quality. Genuinely deep. **DEFECT 1 (medium-strong):** `DelaunayCore.Triangulate` (`:607-628`) hand-rolls a float Bowyer-Watson tet Delaunay and `Topology.InSphere`/`Orient`/`Det4` (`:913-922,:962-973`) hand-roll a NON-ROBUST float in-sphere/orientation predicate — precisely the exact-predicate/CDTet concern the kernel crowns (Geometry brief calls `predicates.md` the "corpus crown jewel", `[TELOS]` names "exact CDT/CDTet"). `architecture.md:54`: "Rasm.Compute … CONSUME Rasm geometry; they never own or re-implement it." There is NO declared discretization→Geometry seam in the ledger. A sliver tet mis-classified by float round-off yields a non-Delaunay/inverted mesh — the exact failure the kernel's adaptive-exact predicate prevents. **DEFECT 2 (low-medium):** local `MeshSpace` record (`:373`, with `Encloses` ray-cast) name-collides with the kernel Vectors `MeshSpace` the page claims to compose/flatten from (`:1020` `Rasm/Vectors MeshSpace.Of(Mesh, Context)`). A re-declared soup carrier under a kernel type's name. **Charter as-is:** volumetric FE mesher is a legitimate Compute concern (Gmsh-class, distinct from exact CDT) — but the in-sphere PREDICATE must compose the kernel exact `Orient3D`/`InSphere` OR the page must state honest non-exact scope + add a Geometry-predicate seam row; and the local carrier renames off `MeshSpace`.

**contract.md — 8/10.** One `PhysicsKind × BoundaryCondition × ElementClass` solve axis (11 physics rows), real Newmark/central-difference marching, modified Newton-Raphson on the genuine nonlinear residual `f_ext − f_int(u)`, buckling geometric-stiffness `K_g`, mass-normalized modal congruence, Aitken Δ² coupling, `RecoveryAction` ladder, `[03]` constitutive AD tapes. World-class solver interior. **DEFECT 1 (STRONG):** the LINEAR-elastic path `PhysicsKind.Material`→`Elasticity(scale)` (`:58-68`, `lambda = scale, mu = 0.5*scale`) **HARDCODES Poisson's ratio to ν=1/3** (λ=scale, μ=0.5·scale ⟹ ν=λ/(2(λ+μ))=1/3) and collapses both (E,ν) onto a single `MaterialScale` double (`SolveProblem` :289-304, `materialScale`/`materialShift`). It never reads `ElementGraph.MechanicalOf(NodeId)`. The seam contract `ARCHITECTURE.md:123` mandates the elastic-D read `YoungsModulus/PoissonsRatio` via `MechanicalOf` — honored ONLY on the nonlinear `[03]` path (`ElasticPrimal` `:1037-1041` uses the correct 2-parameter `lambda=E·nu/((1+nu)(1-2nu)), mu=E/(2(1+nu))`). So TWO elasticity-D formulas coexist: the lossy `PhysicsKind.Material` drives every fea-static/modal/buckling/transient solve (the most common case) at ν=1/3, while the correct `ElasticPrimal` drives only materially-nonlinear runs. `GeometricStiffness` (`:778`) inherits the lossy D. **DEFECT 2 (medium):** `SolveProblem.ContentKey` (`:304`) = inline `XxHash128.HashToUInt128(MemoryMarshal.AsBytes(string.Create(CultureInfo.InvariantCulture, $"{physics.Key}|…")))` — a per-call-site `XxHash128` over a culture-FORMATTED interpolated string. The Model/identity:100 seam warns "never a per-call-site System.IO.Hashing.XxHash128 invocation"; factor.md's `ShardPlan.Digest` (`factor.md:501-505`) and lowering's `CompiledKey` correctly hash CANONICAL bytes. Fragile stringy identity. **Charter:** `SolveProblem` must carry `(YoungModulus, PoissonRatio)` or read `MechanicalOf` at admission; `PhysicsKind.Material` takes both; `Elasticity(scale)` dies; ContentKey composes the kernel `ContentHash.Of` over canonical bytes.

**optimizer.md — 9.5/10.** One `Optimize` entry, 11 `OptimizerKind` rows, FIVE genuinely distinct metaheuristic kernels (NSGA over GeneticSharp, faithful CMA-ES with `Matrix<double>.Evd` covariance update, Clerc-constriction PSO, Metropolis SA, Bayesian-GP that FITS the GP each iteration) — the anti-masquerade prose (`:18`, "operator-swap masquerade … is the deleted form") is backed by real distinct state. OR-Tools `cp-sat`/`milp` lower a typed `LinearModel` to a genuine `CpModel`/`Solver` (honors prohibition [04], no string-eval). Gradient-adjoint reads `SensitivityLaw.Chain` (the verified 2-arg overload). Honest surrogate-hit `Atom` meter. **Exceptional. Zero defects found.**

**sweep.md — 9.5/10.** Joint DOE design-matrix (`DoeDesign` Cartesian vs space-filling JOINT vs response-surface), `SensitivityTornado` quantile-binned and SHARED with uncertainty (`:18` `SensitivityTornado.Of`), frame-budget `IO.Fork` refinement, rolled-up `ProgressCell.Aggregate`. The "per-axis 1-D Sobol Cartesian-producted is the deleted form" law (`:17`) is correct QMC. Contract-uniform `evaluate` oracle. **Excellent.**

**uncertainty.md — 9.5/10.** 8-method forward-UQ/reliability (`UqStrategy` 4-driver total Switch): MC/LHS via owned `LowDiscrepancy`, PCE via one orthonormal `RecurrenceCoefficients` (correct Wiener-Askey: Gamma/Exp→Laguerre, Beta→Jacobi, non-Askey→discretized-Stieltjes — explicitly rejects the "Weibull→Laguerre" error), FORM/SORM via HLRF+Breitung over finite-difference limit-state, subset via Au-Beck Modified Metropolis, Saltelli-A/B/AB + Jansen total-effect. Gaussian-copula `Transform` via `Cholesky`. `Component` reads `LimitStateObjective` (never silently first). C# producer of `ONE_GRADUATION_EVIDENCE`; offline PCE-fit stays Python (honors seam [06]). **Exceptional.**

### Stats/

**estimator.md — 9/10 (structural-verify).** One `Estimator` `[Union]` (Regression/Reduction/Cluster/Classify) over 14 `EstimatorKind` rows (ols/ridge/lasso/glm-{logistic,poisson,gamma}/pca/kernel-pca/nmf/kmeans/gmm/dbscan/hierarchical/knn/svm/naive-bayes), uniform `Fit(X,y)→FittedModel`/`Predict` contract, row-data columns (`factorized`/`supervised`/`torchLoss`/`driver`/`link`). Factorized rows (OLS/ridge/PCA/SVM) route the `Tensor/blas#DENSE_ALGEBRA` `DenseRoute`; iterative rows (lasso/GLM) route TorchSharp `torch.optim.LBFGS`/`Adam` + `torch.autograd` inside one `DisposeScope` (`:44-45,:198-207`) — the `[CLASSICAL_ML_BLAS]` dual-leg the ARCHITECTURE promises, matching blas.md's discipline. `LinkFunction` GLM rows carry inverse-link + deviance. Contract-uniform (`:19`). Not line-by-line read; owner/roster/dual-leg confirmed clean by grep. **Charter correct.**

**signal.md — 9.5/10.** `SpectralTransform` (7 rows: fft/ifft/rfft/stft/spectrogram/welch/dwt) + `FilterDesign` (6 rows) as row-DELEGATE axes folding to one `Transform.Apply`/`Design`. Per-bin over MathNet `Fourier.Forward/Inverse/ForwardReal` split real/imag, magnitude via vectorized `TensorPrimitives.Hypot`/`Atan2` (never per-element loop), bin spacing `Fourier.FrequencyScale` (never 1/N), 15-row `WindowKind` = full `MathNet.Numerics.Window` catalog, IIR via ONE band-aware `Bilinear` + per-family analog `Prototype` (Butterworth/Cheby1/Cheby2/elliptic zpk), FIR via windowed-sinc or equiripple Lawson-WLS over `DenseRoute` thin-QR, overlap-add via `ComplexZip(Multiply)`, wavelet QMF via stride-2 `Conv1D`. Correct `FourierOptions` scaling discipline (`:18`), direct-form-II-transposed IIR, `filtfilt` odd-reflect. Composes dispatch/factor/blas — no re-mint of FFT/GEMM/Complex. **Excellent.**

### Symbolic/

**expression.md — 9.5/10.** `SymbolicExpr` `[ComplexValueObject]` identity = canonical-NF (`Rational.Reduce`→`Infix.Format`) `XxHash128` ALONE (`[MemberEqualityComparer]` on ContentKey; the `[<StructuralEquality;NoComparison>]` F# DU excluded). One `Build` union entry, `SymbolicOp` `[Union]` over the verified 12-module MathNet.Symbolics surface, `SymbolicFault` extends `ComputeFault` 2213-2215. Every F# carrier (`Expression`/`FSharpOption`/`FloatingPoint`) projected once at `ExpressionCapsule`/`FloatBox`. **Excellent.**

**dimensional.md — 9.5/10.** `DimensionMonomial` `[ValueObject<Seq<BigRational>>]` ℚ⁷ implementing the generic-math group (`operator *`/`/`/`Pow`/`MultiplicativeIdentity`) — not a parallel `DimensionAlgebra`. `Validation<Error,DimensionMonomial>` accumulating rail, exhaustive F# `Expression`-DU fold. RESEARCH `[SEAM_DIMENSION_ALIGNMENT]` (`:243`) proves the seam-97 alignment HONEST: ℚ⁷ proof ↔ seam `Dimension` ℤ⁷ measure both project from UnitsNet `BaseDimensions`, align at the 7-vector, NO reference either way, symbolic side alone resolves onward to `QuantityFamily`. **Excellent.**

**lowering.md — 9.5/10.** `CompiledExpr` + `CompileArity` (6 delegate-backed rows owning BOTH compile-form and invoke-down-cast) + `LoweringCache` L1-only read-through over the ONE model-lane `HybridCache` (`CacheLane.ModelResult`, never a second instance — honors prohibition [05]). `CompiledKey` L1-local partition over `SymbolicExpr.ContentKey`. `SymbolicJacobian`/`SymbolicTape`/`SymbolicAdjoint.Chain` answer the SAME 2-arg `Chain(tape, seed)` contract as the geometry `SensitivityLaw.Chain` (design point baked into the tape mirroring `MeshAdjointSnapshot`). Seam-98 honest: `SymbolicExpr.ContentKey` crosses to `Rasm.Persistence/Query/cache#MODEL_RESULT_INDEX` by its OWN content identity, "NEVER a fabricated ModelResultKey" — and MODEL_RESULT_INDEX/cache.md SURVIVES the Persistence campaign (retrieval split moved VECTOR_CODEBOOK, not the result cache), so **seam-98 is stable, no migration pressure.** **Excellent.**

**units.md — 9/10.** `QuantityFamily` `[SmartEnum]` 21-row SI-canonicalization, dual `UnitEvidence`, `UnitAlgebra` compound-consistency + `Reciprocals` + `Dimensionless` sweep, `CutParameterIngress` Compute-internal. Correctly asserts the AEC-in-folder law (`:7`, Fabrication/Materials admit UnitsNet in-folder, never reaching DOWN to Compute — honors seam prohibition [04]). **DEFECT (low):** `Admit(..., Guid correlation)` and `UnitEvidence.CorrelationId : Guid` (`:81,:91,:96,:129,:203`) use a BARE `Guid` where every other Compute surface threads the typed `CorrelationId` (settled per ARCHITECTURE). The one weak-typed correlation at a boundary. (Also the one `StringOrdinalIgnoreCase` comparer vs the corpus's `StringOrdinal` — justified: unit keys are legitimately case-insensitive.) **Charter:** thread the typed `CorrelationId` through the units boundary.

### Tensor/

**vocabulary.md — 9.5/10.** `TensorDtype` map (15 rows, ONNX byte-width + quantization columns, `complex64`/FP8/Int4 honestly absent-by-construction), `QuantizationPolicy` union (PerTensor/PerAxis/Blocked), and the **196-row `TensorOpFamily` table** across 13 `TensorOpKind` rows, each carrying its `ToleranceClass` error-envelope (`Bound(N,mass)` under a cancellation-ratio gate, never a stored relative scalar). Roster-as-seed-DATA (GENERATOR_LAW clean). **Excellent.**

**layout.md — 9.5/10.** `LayoutForm` axis-label permutation DERIVATION (nchw↔nhwc derives, hand-keyed table is deleted), `Contiguity` classification, `AxisPermutation.Admit` STRICTLY STRONGER than the host `PermuteDimensions`, `ReshapeOp<T>` generic union (14 verbs) under one total `Switch`. `System.Numerics.Tensors` mined correctly (share-vs-materialize per verb). **Excellent.**

**dispatch.md — 9.5/10.** Constraint-faithful kernel tables (IFloatingPointIeee754/IBinaryNumber/IBinaryInteger split so `IsPow2` binds), activations as author-folds (no phantom `TensorPrimitives.Relu`), claim-gated `ParallelHelper` partition. `[02]-[EQUIVALENCE_INTEROP]` is a **full dual-mode AD engine**: one `Sensitivity` owner carrying BOTH VJP+JVP (no copy-paste dual), `DifferentiableOp` rows, `SensitivityLaw.Chain`/`Pushforward`/`GaussNewton` (`JᵀJ·v` matrix-free) with an **HONEST** disclaimer (`:366,:646-649`) that it is NOT the exact Hessian (the FEM consistent-tangent second-order leaf is documented, not faked). `JacobianColoring` scatters into factor.md CSR. **Seam-124 VERIFIED clean:** `GeometryAdjoint.Rows` maps 6 geometry `TensorOpFamily` rows → kernel `OperatorRow.{Gradient..Spectral}`, `Sensitivity.Operator` routes `row.Adjoint`/`Apply` over `MeshAdjointSnapshot`, "never a re-assembled DEC matrix" (Compute names only `MeshAdjointSnapshot`/`OperatorRow`). `[03]-[DEVICE_KERNELS]` = real WGSL compute pipelines; **seam-112 VERIFIED clean:** `WgpuDevice` composes the AppUi `ONE_WGPU_DEVICE`, "NEVER acquires a second Device/Queue", AppUi owns Device/Queue, Compute owns compute-only resources (honors prohibition [01]). **Excellent.**

**residency.md — 9.5/10.** `OrtResidency` 5-gate lattice (uniquely keyed by `(Wraps,Device,ProjectsInPlace,Foreign)`), `TensorBridge` the sole `OrtValue` C-data factory, `BoundFlow` the ONE steady-state `OrtIoBinding` capsule, `EncodedTensor` wraps the kernel `EncodedGeometry` float payload as residency (`:190` "never a re-pack", host-neutral) — aligned with Geometry `[V13]`/seam-125. `DeviceResident` is the SINGLE device-residency row shared with `Substrate.DeviceWgpu` (no parallel lattice, prohibition [01]). Witness rides the kernel `CANONICAL_BYTE_IDENTITY`, no second content key. **Excellent.**

**memory.md — 9/10 (structural-verify).** `AllocationClass` `[SmartEnum]` 6-row staging axis (span-stack/pooled-memory/recyclable-stream/native-ort/edge-copy/device-wgpu — the compute-doctrine five-class extended by device/edge), `StagingEventKind` 13-row event stream folding to a conservation ledger. `Grant(CorrelationId correlation, …)` uses the TYPED CorrelationId. Owner/roster confirmed clean by grep. **Charter correct.**

**blas.md — 9.5/10.** Dense LA over the MathNet provider stack + native TorchSharp ATen substrate. `LinearProvider` RID-keyed (managed/openblas/mkl), `DenseSubstrate` (managed/native-aten), `FactorRoute` shape-spine `[Union]`, `Admission` re-imposing every gate MathNet refuses (`Try.lift` around throwing `Cholesky()`, `DeterminantLn` finiteness, all-finite via `TensorPrimitives.IsFiniteAll`). Honest witness against the ORIGINAL operator (no factor reconstruction). ATen leg route-discriminates `cholesky_ex`/`ldl_factor_ex`/`solve_ex`/`lstsq` inside `torch.inference_mode`, info-gated (no native-exception control flow). `DeterminismTag`/`SolveDedupKey` fold provider+substrate identity (exactly what dispatch's device-GEMM references). `LevenbergMarquardt` Compute-internal; seam prohibition [05] honored (Materials BRDF stays in-folder). **Excellent.**

**factor.md — 9.5/10.** `SparseFormat`/`FactorKind`/`IterativeMethod` axes over MathNet CSR + CSparse CSC direct factors, `FactoredOp` capability owner (transpose-solve/rank-1-edit/inertia/reentrancy recovered from the row), honest true-relative-residual witness (criterion-stack, no `Iterator` count), `Edit` `[Union]` structural dialect (Pin/Prune/Bump/Revalue all realized). `SparseTensorOpFamily` + `EinsumPlan` greedy-pairwise + `KernelLowering` GEMM/im2col/pool with `ProveGemm`. **Seam-103 VERIFIED clean/stable:** `ShardPlan.Blocked` fan-out (`:419,:480-497`) dials `ModelResultIndex.Lookup` resolving RESIDENCE ("the index never holds the payload"), object-store port yields bytes — matches `Compute:103` and the surviving Persistence `cache.md#MODEL_RESULT_INDEX` exactly; content-addresses CANONICAL proto bytes via `XxHash128` (contrast contract's culture-string). **Excellent.**

**quadrature.md — 9/10 (structural-verify).** `QuadratureRoute` accuracy axis (double-exponential/gauss-legendre/gauss-kronrod) + `Integrate` re-imposing the gates MathNet `Integrate` refuses; plus an embedded owned ODE initial-value integrator (`StepTableau` embedded-pair Butcher tableau, Scalar/Complex/Vector states). Owner/roster confirmed clean by grep. **Charter correct.**

**sampling.md — 9.5/10.** Owned QMC lane (MathNet has no Sobol/Halton): genuine Joe-Kuo direction-number recurrence over the embedded 21,201-dim `new-joe-kuo-6.21201` primitive-polynomial resource, prime-base Halton radical inverse, `Scramble` (none/digital-shift/Owen nested-uniform), `LatinHypercube` rank-stratified JOINT design, `Replicates` RQMC with Warnock L2 star-discrepancy + worst-2D-projection net-quality, `Scatter` RBF-plus-polynomial-tail into the blas held SVD. The `LowDiscrepancy` owner is EXACTLY what `sweep.md`/`uncertainty.md` compose (`LowDiscrepancy.Sobol/Halton/LatinHypercube` — call sites verified consistent). Prose names the real broken forms it avoids (all-zero direction table, unscaled-m recurrence, shared Halton base). **Excellent.**

---

## CROSS-CUTTING

### Duplication
- **discretization ↔ kernel Geometry (real):** discretization hand-rolls a non-robust float Bowyer-Watson Delaunay + `InSphere`/`Orient` predicate (`discretization.md:607-628,:913-922`) that duplicates the kernel's exact-CDTet/adaptive-exact-predicate floor. No declared seam. See VC-3.
- **contract elasticity-D ×2 (real):** the lossy `PhysicsKind.Material.Elasticity(scale)` (`contract.md:66`) and the correct 2-parameter `ConstitutiveModel.ElasticPrimal` (`:1037`) are two isotropic-elasticity tensors for one concept. See VC-2.
- Otherwise **no duplicate mechanisms**: `SensitivityTornado` is shared (sweep↔uncertainty), the `evaluate` oracle is one contract-uniform surface (optimizer/sweep/uncertainty), `LowDiscrepancy` is the one QMC owner, `Sensitivity`/`SensitivityLaw` is the one AD owner (geometry tape + symbolic tape both fold under one `Chain`), the sparse CSR storage is one owner (csc/coo/dok are ingestion conversions).

### Concern mixing
None material in scope. discretization holds mesh generation + isoparametric evaluation + quality metrics, but they are one coherent discretize-owner concern the solve consumes. blas holds dense-LA + spectral + provider-claims + online-moments, all one dense-numeric owner. Clean separation of Symbolic (expression=CAS / dimensional=proof / lowering=compile-cache / units=boundary) — "three distinct concerns over one shared value" (`expression.md:5`).

### Hardcoding vs generator
Near-universal generator discipline: every roster is `[SmartEnum]`/`[Union]` + `FrozenDictionary` seed-DATA where a new capability is one row (documented per-page Growth blocks; verified real). The ONE physical-constant hardcode: **contract Poisson ν=1/3** (`contract.md:66-67`) — the single genuine hardcoding-where-a-generator/seam-read-belongs defect (VC-2).

### Dead / weak typed carriers
None found in scope. Every union case, row column, and field is load-bearing (the pages explicitly delete "dead field" forms — e.g. `Periodic.Master` read, `Contact.Master` reaction, `TwinFilter` CUSUM, `DifferentiableOp.Jvp` total-not-Option). units' bare `Guid` (VC-7) is weak-TYPED, not dead.

### Unwired seams / stale seam-rows
- **Compute:120 stale reverse-half (VC-4):** `ARCHITECTURE.md:120` claims "Geometry/Processing/solver consumes the Compute LM/constraint Solver rail" — the Geometry brief `[VERDICT]` proof 5 + `[V8]` explicitly DISPROVE it (`solver.md:295-347` composes MathNet directly). The forward half (Solver↔`SpatialIndex.ToAcceleration` BVH wire) is honest and consumed by clash.md. Governance edit at the Geometry-campaign landing.
- **contract linear-path Element seam UNWIRED (part of VC-2):** `ARCHITECTURE.md:123` declares the elastic-D read via `MechanicalOf` — the LINEAR assembly never reads it (only the `[03]` constitutive path does). The seam is declared-but-unwired for the common solve.
- **Persistence-counterpart re-points (item 5 census, mostly FIRST-half/Runtime):** `Compute:111` (→Persistence/Sync, folder DELETED), `:115` (→Query/pipeline, DELETED), `:116` (→Store/quality, DELETED), `:119` (→Sync + Confluent.SchemaRegistry, DELETED+PRUNED) are all confirmed stale by the Persistence brief E6/V12 counterpart-obligation list — they must re-point to post-Persistence owners. These are Runtime seams (corpus-a). In MY scope: `factor:103`+`lowering:98` (→Query/cache MODEL_RESULT_INDEX) are STABLE. The lane VECTOR_CODEBOOK↔Compute/Model/embedding seam (`:99`) is Model (first-half); stays honest Compute-side.

### Unmined capability
Materially none — the package-pressure is largely satisfied in scope. Full-depth mining verified: `TensorPrimitives` (196-row table + constraint-faithful dispatch + AD adjoint), `System.Numerics.Tensors` (Tensor<T>/TensorSpan/TensorMarshal/layout), MathNet (Fourier/Window/Statistics/Distributions/LinearAlgebra Evd-Svd-QR-Cholesky-LU/Symbolics 12-module/Interpolate), CSparse (SparseCholesky/LDL/LU/QR + AMD ordering), TorchSharp (dual-leg `torch.linalg` ATen + `torch.optim` + autograd), OR-Tools (typed CpModel/Solver), GeneticSharp (GA engine), ONNX (OrtValue/OrtIoBinding/OrtEpDevice/shared-allocator), Silk.NET.WebGPU (compute pipelines + two-phase readback), UnitsNet (BaseDimensions/QuantityInfo), StudentT/SpecialFunctions. `CommunityToolkit.HighPerformance` (SpanOwner/MemoryOwner/ParallelHelper/Span2D) woven throughout.

### Folder architecture
**Sound. No rot in the second half.** Solver (6), Tensor (9), Symbolic (4), Stats (2) — all ≥2-page growth axes, zero one-file folders, coherent partitions. Tensor at 9 is the largest but a coherent numeric-substrate concept (vocabulary→layout→dispatch→residency→memory→blas→factor→quadrature→sampling), each a distinct owner not sprawl. Stats at 2 is the minimum but coherent (estimator=statistical-learning / signal=DSP). discretization is correctly homed in Solver as the discretize→solve spine head (contract consumes `DiscreteMesh`). **No split/merge/move pressure in scope.**

---

## VERDICT CANDIDATES (campaign-defining, evidence-ranked)

**VC-1 — The second-half corpus is world-class, NOT naive; the campaign is a HARDEN, not a rebuild.** 21/21 pages 8–9.5; every seam honest Compute-side (103/112/124/96/97/98/120-forward verified); generator discipline near-universal; external packages mined to full depth; the arithmetic (FE assembly, HLRF/Breitung reliability, dual-mode AD with honest-not-Hessian `JᵀJ`, Joe-Kuo Sobol, DEC-transpose adjoint) is genuinely realized. The hostile prior is refuted by disk. Structural authority should be spent on the bounded defect set below + governance ledger, not page rebuilds. *Evidence: the per-page verdicts; `optimizer.md:18`, `dispatch.md:366,646`, `sampling.md:23`, `factor.md:480`.*

**VC-2 — contract.md's linear-elastic path hardcodes Poisson ν=1/3 and leaves the `MechanicalOf` material seam unwired; two elasticity-D formulas coexist.** The single strongest correctness+coverage defect. `Elasticity(scale)` (`contract.md:66-67`, `lambda=scale, mu=0.5*scale`) fixes ν=1/3 and derives E from one `MaterialScale` scalar for EVERY fea-static/modal/buckling/transient solve; `SolveProblem` (`:289-304`) carries no Poisson slot and never reads `MechanicalOf`; the seam `ARCHITECTURE.md:123` (elastic-D = YoungsModulus/PoissonsRatio via `MechanicalOf`) is honored only on the nonlinear `[03]` `ElasticPrimal` (`:1037`, correct 2-parameter). FIX: `SolveProblem` reads `MechanicalOf(NodeId)` at admission (or carries `(E,ν)`); `PhysicsKind.Material` takes both; `Elasticity(scale)` and the single `MaterialScale`/`MaterialShift` collapse die; the seam wires on the linear path.

**VC-3 — discretization.md hand-rolls a non-robust float Delaunay/in-sphere the kernel owns exact, with no declared seam.** `DelaunayCore.Triangulate` (`:607-628`) + `Topology.InSphere`/`Orient`/`Det4` (`:913-922,:962-973`) are a float Bowyer-Watson tet mesher + non-exact predicate — the exact-CDTet/adaptive-exact-predicate concern the Geometry brief crowns (`predicates.md` "crown jewel", `[TELOS]` "exact CDT/CDTet") — violating "consumers never re-implement kernel geometry" (`architecture.md:54`). FIX: compose the kernel exact `Orient3D`/`InSphere` via a NEW discretization→`Rasm/Geometry` predicate seam, OR state honest non-exact-FE-mesh scope on the page. Same page: the local `MeshSpace` record (`:373`) renames off the kernel Vectors `MeshSpace` (`:1020`) it flattens from — a naming collision.

**VC-4 — Compute:120 carries a stale reverse-seam the Geometry campaign disproves.** `ARCHITECTURE.md:120` asserts "Geometry/Processing/solver consumes the Compute LM/constraint Solver rail"; Geometry brief `[VERDICT]` proof 5 + `[V8]` disprove it (`solver.md:295-347` composes MathNet directly). The forward half (Solver consumes `SpatialIndex.ToAcceleration` BVH wire) is honest and live in clash.md. FIX: at the Geometry-campaign landing, delete the reverse-consumption clause from `Compute:120`, keep the forward `[SHAPE]` seam.

**VC-5 — contract.md mints a fragile culture-string content key; the corpus's canonical-byte pattern is the fix.** `SolveProblem.ContentKey` (`:304`) hashes a `CultureInfo.InvariantCulture`-formatted interpolated string via a per-call-site `XxHash128` — the exact "per-call-site System.IO.Hashing.XxHash128 invocation" the Model/identity:100 seam forbids. Contrast the correct canonical-byte content keys: `factor.md ShardPlan.Digest` (`:501-505`), `lowering.md CompiledKey` (`:136-142`), `expression.md SymbolicExpr.ContentKey` (`:38-39`). FIX: compose the kernel `ContentHash.Of` (the ONE federation hasher) over canonical bytes, not a formatted string. (Sole occurrence corpus-wide — grep-confirmed.)

**VC-6 — Persistence-counterpart seam ledger: re-point the deleted-owner rows, confirm the survivors.** The Persistence campaign deleted `Sync/`, `Query/pipeline`, `Store/quality` and pruned the Confluent cluster, staling `Compute:111/115/116/119` (Persistence brief E6/V12 counterpart-obligation list). Mostly Runtime (corpus-a). IN SCOPE: `factor:103` (`ShardPlan.Blocked`→`MODEL_RESULT_INDEX`) and `lowering:98` (compiled-formula→`MODEL_RESULT_INDEX`) are STABLE — `cache.md#MODEL_RESULT_INDEX` survives the campaign (retrieval split moved VECTOR_CODEBOOK, not the result cache). Governance edit is a 1-hop ripple, not a page rebuild.

**VC-7 — units.md threads a bare `Guid` correlation where all of Compute threads the typed `CorrelationId`.** `QuantityFamily.Admit(..., Guid correlation)` / `UnitEvidence.CorrelationId : Guid` (`units.md:81,:91,:96,:129,:203`) — the one weak-typed correlation at a boundary (sole occurrence beyond units, grep-confirmed) against the settled `CorrelationId` value type every receipt/clash/solve/memory surface uses. FIX: thread the typed `CorrelationId`.

**VC-8 — CLASH_GOLDEN 160-byte fixture is asserted, not yet harness-proven both-sided.** `clash.md:387` cites the frozen fixture as byte-identical across kernel-emit → Compute-decode; Geometry brief `[SEAM_AND_RAIL_LAW]` requires re-proving it against actual `XxHash128`/`NodeLinkProjection` output before any page cites it as REAL. Verification obligation at the Geometry-seam finalization, not a defect in the page's logic.
