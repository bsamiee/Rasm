# [RASM_CS_GEOMETRY_BRIEF]

Campaign: cs track 1/6 — run FIRST (every cs campaign consumes the kernel planes). Phase 1: `Workflow(design-cs-geometry.js)` emits `RASM-CS-GEOMETRY-DECISION.md` (before or after the kernel conversion, never mid); Phase 2: `Workflow(rebuild.js, {targets: <per-leg pages>, brief: "RASM-CS-GEOMETRY-DECISION.md"})` legs per the DECISION partition.

Authoritative campaign input for the ground-up restructure and rebuild of `libs/csharp/Rasm/Geometry/.planning/` — the greenfield robust-core sub-domain of the `Rasm` KERNEL package (no own `.csproj`; `libs/csharp/Rasm/Rasm.csproj` owns the manifest, `libs/csharp/Rasm/ARCHITECTURE.md` owns the codemap and seams). The campaign is two-phase: `[01]-[PHASE_ARCHITECTURE]` runs ONE ephemeral design workflow that emits `RASM-CS-GEOMETRY-DECISION.md` — the binding restructure blueprint (final page-set, folder partition, namespace ratification, seam ledger, roster deltas, leg partition); `[05]-[BUILD_LEGS]` then runs the durable rebuild engine against that DECISION, floor-first. `docs/stacks/csharp/` governs unchanged and is never restated here. Two downstream campaigns are GATED on this one: `Rasm.Generation` stand-up (`RASM-GENERATION-SPEC.md:179` KERNEL GATE) and the `Rasm.Fabrication` scope expansion — every verdict grades against both.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is world-class in isolation and naive as a system: the exact-predicate floor is genuinely award-grade, and the structure above it ships dead engines, phantom namespaces, a fictional roster, and an absent consumer-gated plane. Six proofs, each independently fatal to a world-class claim:

1. **The advertised HLR engine does not exist.** `Drawing/view.md`'s thesis is a Newell-Newell-Sancha BSP painter with Appel quantitative-invisibility (`view.md:3,16`); the fence builds the full BSP apparatus (`Partition`/`Split`/`BspNode`, `view.md:263-300`) and never consumes it — `Paint` takes `bsp` as a parameter (`view.md:302`) and its body never references it. Visibility is uniform `SampleStep` ray-sampling (`Clip`/`OccludedAt`, `view.md:320-347`): an occluder edge between two samples is missed, which is precisely the sampling non-robustness the page claims the exact floor eliminates. `ToPolylines` concatenates all segments of a kind by `Edge.Key` regardless of connectivity (`view.md:130-135`) — garbage polylines. The page's central claim is decorative prose over a different, weaker algorithm.
2. **The plane both gated consumers demand is absent while the corpus over-invests in what nobody gates on.** The KERNEL GATE (`RASM-GENERATION-SPEC.md:179`) demands surface geodesics/offsets, region subdivision, developable/panelization, and pattern-to-surface mapping; grep of Geometry AND Vectors returns ZERO subdivision/develop/panelize/unroll owners — `Rasm.Vectors` owns every enabling primitive (`ScalarField.Geodesic`/`MeanCurvatureFlow`/`SignedDistanceFromMesh` at `Vectors/Field.cs:386-394`, `CrossField`/`VectorHeat`/`GeodesicTangent`/`TangentLogMap` at `Field.cs:1864-1872`, isoline extraction + `Streamline` in `Extraction.cs`/`Flow.cs`) and no owner one level up composes them. `Parametric/` is a one-file folder (`curve.md`, 156 LOC, the smallest page) whose `Apply`/`CurveApply`/`SurfaceApply`/`ToPolyline`/`ToMesh`/`CurveFrom`/`SurfaceFrom` are signature-only stubs (`curve.md:117-138`), and curve `Offset` — a gate item — is demoted to Growth (`curve.md:19`). Fabrication's toolpath gate is equally unowned: `offset.md` is miter-only straight-skeleton (no `JoinType`/`EndType` corner strategies), `intersect.md` `PlaneMesh` is a single cut (no slice stack), so Fabrication declares its OWN skeleton, HLR, and planar section (`Rasm.Fabrication/ARCHITECTURE.md:22,33,47`) — the kernel's charter leaks downstream by absence.
3. **The roster is three-way fiction.** `ARCHITECTURE.md:3` declares the robust-core "admits no external geometry library" while `README.md:44` documents a nine-package `[GEOMETRY_KERNEL]`/`[COMPUTATIONAL_GEOMETRY]` roster and `curve.md:3` composes GShark. Seven of nine admitted kernel-geometry packages (`Directory.Packages.props:70-78`, `Rasm.csproj:18-31`) are composed by ZERO pages — Clipper2, CavalierContours, geometry3Sharp, Triangle, MIConvexHull, SharpVoronoiLib, LibTessDotNet — while each package's `.api` catalog names a Geometry-page consumer the page explicitly rejects (`api-clipper2.md:91-92` vs `arrangement.md:470`; `api-sharpvoronoilib.md:138` vs offset's in-house dual; `api-libtess.md:84` "backs the Drawing/view + Drawing/pack fill leg" — neither page references it, and `README.md:44` repeats the claim). `delaunay.md:3` rejects Triangle as "GPL" — Triangle.NET is MIT; the stated reason is factually false. The manifests, the catalogs, and the pages describe three different systems.
4. **Five confirmed math/logic bugs ship inside transcription-complete fences.** (a) `flatten.md`'s Dirichlet pin is inverted: `Policy.MassShift` (1e-9) is added to EVERY diagonal (`flatten.md:359`) and the pinned rhs is `value / MassShift` = value·1e9 (`flatten.md:364`) with no large penalty on pinned rows — the interior harmonic/BFF solve is corrupted before `Scatter` overwrites the boundary (`flatten.md:369`); (b) LSCM claims a sparse operator (`flatten.md:4`) and allocates dense `(2n)²` + full `DecomposeSvd` (`flatten.md:377,173`) — O(n³), non-scaling; (c) `naming.md:113` keys `VertexNames` by `entity.IncidentVertices[0]` — the star minimum, not the vertex itself, because `reconciliation.md:55-62` builds `star[v]` as self+neighbours sorted with no self-index field — every vertex not smallest in its star registers under a neighbour, corrupting `ResolveBoundary` and every downstream `TopoSignature`; (d) `receipts.md` mints boolean receipts `AssetGated: false` (`receipts.md:65`) while `Converged` requires `b.AssetGated` true (`receipts.md:99`) — every heal session containing a successful managed boolean reports non-converged; (e) `decimate.md` hardcodes the interior link condition `shared == 2` (`decimate.md:312`) so boundary edges (`shared == 1`) never collapse — `TargetFraction` unreachable on open meshes, spurious `DecimationFault` — while `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` linear-scan the full face array per collapse (`decimate.md:318-322,342-360`) against the page's own "O(1) incident-face fans" prose (`decimate.md:16`): O(F²).
5. **The namespace/folder/fault-cluster tri-way schism, a phantom namespace, and a strata violation.** `ARCHITECTURE.md:64` law: five sub-domain namespaces, and "the robust-core mints no `Topology` namespace." The fences declare FOURTEEN (`Tessellation`/`Arrangement`/`Intersection`/`Offsetting` inside `Meshing/`; `Healing`/`Constraints`/`Fitting`/`Parameterization`/`Simplification` inside `Processing/`; `Projection`/`Encoding` inside `Drawing/`; `Parametric` undeclared in the codemap; `Spatial`; root `Rasm.Geometry` for faults), four pages declare none (`predicates.md`, `receipts.md`, `naming.md`, `reconciliation.md`), `pack.md:33` imports the forbidden `Rasm.Geometry.Topology`, `solver.md:13` cites it as "the sibling namespace," and Fabrication's ledger consumes `Rasm/Geometry/Healing` cross-package (`Rasm.Fabrication/ARCHITECTURE.md:44`). Separately the KERNEL references app-platform UPWARD: `index.md:27` `using Rasm.Compute.Solver` with `ToAcceleration` returning Compute's `AccelerationStructure` (`index.md:572-579`) — the exact violation `libs/.planning/architecture.md:35` forbids — while Compute's ledger claims the spurious reverse seam "Geometry/Processing/solver consumes the Compute LM" (`Rasm.Compute/ARCHITECTURE.md:120`) that `solver.md:295-347` disproves by composing MathNet directly.
6. **Declared collapses are unrealized — the corpus states its own law and ships the violation.** `intersect.md:388` names itself the single exact-crossing owner and lists the three inline copies to collapse; all three still live (`arrangement.md:242-262` `EdgeCrossings`/`InTriangle`/`PlaneCrossPoint`, `offset.md:440-444` `SegmentsCross`, `repair.md:346-372` `TriangleCrossPoint`/`EdgesCrossTriangle`). `offset.md:16,478` claims the medial axis "reconciles against the constrained-Delaunay Voronoi dual" while `MedialFrom` builds the tessellation and DISCARDS it (`.Map(_ => new MedialAxis(...))`, `offset.md:325-326`) — and `delaunay.md` exposes no Voronoi-dual surface at all, so the seam is phantom. `fit.md:16` claims "the SAME LM the sketch solver composes" and names a second refine loop "the double-owner defect"; its fence re-implements the entire ladder (`fit.md:568-593` vs `solver.md:295-347`) with hardcoded `1e-3`/`×10`/`1e12` constants and no λ carry-down. `arrangement.md` claims implicit-point exactness (`arrangement.md:467`) while `PlaneCrossPoint` rounds the operand crossing to a `double` (`arrangement.md:258-262`) and feeds it as an explicit `Constraint.Segment` — the LPI/TPI path covers only tessellation-internal Steiner points; the headline boolean is exact-sign, rounded-vertex.

What IS sound (preserve; harden, never re-derive): the `predicates.md` four-tier ladder with the four-way independent oracle (the corpus crown jewel — every composed external member `.api`-verified), the `faults.md` single-union band shape, `solver.md`'s analytic-Jacobian LM with witness DOF, `index.md`'s BVH/octree/PLOC/GWN breadth, `flatten.md`'s DEC composition shape over the settled `MeshAdjointSnapshot` seam, `arrangement.md`'s composition depth, `reconciliation.md`'s frozen fixture spine, and the corpus-wide external-member discipline. The rot is structural — dead engines beside live claims, a dead roster beside hand-rolls, missing consumer-gated planes, duplicate owners across strata, a namespace law nobody follows — and no per-page cold pass fixes it. Architecture first.

### [TELOS]

`Rasm/Geometry` is the federation's exact-arithmetic computational-geometry robust-core AND the operational geometry plane two gated campaigns compose — one body. The robust tier (adaptive exact predicates, exact CDT/CDTet, exact arrangement/boolean, exact crossing lattice, robust skeleton/medial, predicate-gated heal/decimate/flatten/fit/solve) is necessary but not sufficient; the consumer tier (Generation's SpineRef forms: stations/frames, isolines/geodesics/offsets, subdivision, panelization, pattern-to-surface mapping; Fabrication's toolpath grade: corner-strategy offsets, slice stacks, 3D medial/skeleton, booleans at manufacturing tolerance, developable/unroll, hull) sits ON TOP and composes it plus the Vectors field substrate. Every owner grades against both tiers at once. The hand-roll law is calibrated, not absolute: author from first principles where no admissible package carries the robustness guarantee (verified TRUE for predicates, CDT, arrangement, crossing, skeleton, constraint-LM); compose the categorical-best engine where the ecosystem owns the concern (GShark NURBS, kd-tree leaf, Manifold at scale, CavalierContours arc-offset); NEVER both for one concern. Assume 5x consumer demand: the parametric tier feeds every generated form, the toolpath plane feeds every CNC/AM program, the encoding plane feeds every GPU residency lane, the naming plane feeds every persistence diff — zero current consumers never lowers the bar. One geometry owner per runtime; the kernel meets hosts at the wire.

### [STRUCTURAL_AUTHORITY]

Phase 1 owns the page-set absolutely: split any page into a folder (minimum 2 pages per new folder), merge pages, move concerns across sub-domain boundaries, delete pages after absorption, mint new sub-domains, rename folders. Calibration examples, not the ceiling: `Parametric/` growing from a one-file stub into the surface-development tier; `MeshEdit` extracting from `repair.md` into a substrate owner; `BooleanOp` re-homing from Processing to Meshing. The folder-architecture law binds: NO one-file folders (`Parametric/` is the standing violation); the two-tier partition law rules — folders are ≥2-page domain groups, namespaces are concepts mapping 1:1 to fault sub-bands (the realized ~14-namespace structure is the truth candidate; the 5-folder codemap is the stale bucket — `[V1]` rules); every folder a growth axis where the next concern lands as a page, never a cram or a mismatched home. Every structural change updates `ARCHITECTURE.md` `[01]-[DOMAIN_MAP]`+`[02]-[SEAMS]`+`[03]-[NAMESPACE_LAW]`, `README.md` roster paragraphs, and the `faults.md` cluster taxonomy in the same motion. No page survives on incumbency: a page that exists is not evidence it belongs.

### [PLACEMENT_LAW]

The strata law is absolute: `Rasm` references no sibling (`libs/.planning/architecture.md:35`). Geometry composes in-package siblings (Vectors/Domain/Analysis) and admitted external packages, NEVER `Rasm.Compute`/`Rasm.Fabrication`/any upward stratum — every wire type the kernel emits is kernel-owned and downstream-decoded (the `index.md:27` `AccelerationStructure` inversion dies). Float/production geometry engines live at their consuming stratum: Clipper2 belongs to Fabrication's manifest, CavalierContours to the toolpath-offset owner's stratum per `[V10]`. Concerns owned twice across strata resolve to the kernel owner with the downstream consuming through the seam: Fabrication's `Posting/Projection.cs` BSP HLR (`Rasm.Fabrication/ARCHITECTURE.md:33`) dies for `DrawingProjection`, its `Toolpath/Skeleton.cs` (`:22`) dies for `Offsetting.Apply`, its in-folder planar section (`:47` already promises "re-routes to a kernel mesh-section primitive if one lands") dies for the slice-stack owner. Host parametric stays host-side: `Analysis/Intersect.cs` Rhino NURBS/Brep is the Rhino-runtime owner, GShark the host-neutral owner, meeting at the wire (control points + knots + weights) — the runtime-split law `curve.md:191` states correctly.

### [FOUNDATIONS_FIRST]

Build order is a dependency law: the Numerics floor before every consumer; Spatial + the mesh substrate before Meshing; the Meshing lattice before Processing; Processing before the Parametric tier and Drawing. A consumer page may not hand-roll what its floor owns (the crossing-collapse law), and a floor too thin to serve its consumers is the floor's defect, never license to reinvent (the Voronoi-dual accessor, the Vectors `SampleDetailed` exposure). A seam declared in `ARCHITECTURE.md` and not composed in a fence is a defect on BOTH pages. The leg partition is a topological order over the corrected seam ledger; an edge into a later wave is a partition defect resolved by re-homing or re-waving, never tolerated — the live inversions (`BooleanOp` upward, `MeshEdit` hub in Healing, `index`→Compute) are the census, not the ceiling.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — every owner is a `[Union]`/`[SmartEnum]` + FrozenDictionary data-table dispatch where rosters are seed DATA (predicate kinds, heal ops, fit primitives, pack channels) — so the law binds the NEW planes: subdivision schemes are stencil rows over ONE refinement fold (Catmull-Clark/Loop as data, never sibling subdividers); corner strategies are `JoinType`×`EndType` rows over ONE offset assembly; slice stacks are policy rows (uniform/adaptive height) over ONE section fold composing `PlaneMesh`; panelization families are policy rows over ONE cross-field-guided mapper; fault growth is one case row in the owning cluster's sub-band. The test: the next scheme, join, layer policy, or panel family is one data row, never a new arm, a second lowering, or a 15th namespace.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner: `public static Fin<TResult> Apply(TOp op, ...)` discriminating on the closed `[Union]` — already the corpus norm; sealed as law for every new owner (no `Build*`/`Get*` sibling families).
- Exception escapes are forbidden on the rail: `index.md`'s `throw new InvalidOperationException` (`index.md:130,574,579`) and the Ray/Overlap/Winding-over-PointCloud silent empty (`index.md:358` vs its own ":351 never-a-silent-empty" prose) are the breach class; kind mismatch routes `Fin` fail with a typed `GeometryFault`; `ToAcceleration` returns `Fin<>` over a kernel-owned wire.
- The exactness contract is honest per owner: a page states where SIGNS are exact and where COORDINATES are rounded; an overstated claim (`arrangement.md:467` implicit-point framing over `:258-262` rounded crossings) is repaired by a richer implicit-point-capable `Constraint` case or an honest re-scope — never left as prose.
- The store-mutability contract settles once, corpus-wide: SoA stores (`SimplexStore`/`WavefrontStore`/`IntersectStore`/`ArrangementStore`/`QuadricStore`/`NodeStore`) are MUTABLE build arenas; "hash-friendly immutable record" prose dies where the fence mutates (`delaunay.md:5,66-74`, `arrangement.md:87-91,488`); content-addressing binds the frozen emitted result through `reconciliation.md`'s `Encode`, never a mid-build store; `WavefrontStore`'s `2n` allocation vs unbounded `Spawn` (`offset.md:195`) is the sizing defect the contract fixes.
- Cross-package member claims verify or die: `assay api` over the restored `Rasm.Vectors` assembly is truth for `ScalarField.SampleDetailed` (`pack.md:459` admits it is unexposed), the `VectorIntent.Topology` projection row (`receipts.md:441` — `IsManifold`/`NonManifoldEdges` non-projectable), and the `DiscreteCalculus` CSR shape `flatten.md:349` assumes; a member you cannot verify is a phantom.
- Frozen fixtures are harness-confirmed, never asserted: the `0x9462A71A…` digest (`reconciliation.md:147`) and the `CLASH_GOLDEN` 160-byte stream re-prove against actual `XxHash128`/`NodeLinkProjection` output before any page cites them as REAL.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.csproj`; per-package catalogs under `libs/csharp/Rasm/.api/` (18), shared substrate under `libs/csharp/.api/` (30). Carried obligations — the full manifest/catalog motion lands in wave 1 per `[05]` (safe: every purged package is zero-page-consumer), a later leg carries only the composition-time deepening of the stubs its pages compose: PURGE the zero-consumer kernel packages — LibTessDotNet, geometry3Sharp (abandoned upstream; Boost-licensed geometry4Sharp is the successor ONLY if a managed mesh toolkit is ever wanted), SharpVoronoiLib, Triangle (`0.0.6-Beta3`, beta AND dead), MIConvexHull (the `[V11]` hull-as-`Tessellation`-projection fold replaces it) — with their `.api` catalogs; RE-SCOPE Clipper2 to the Fabrication manifest group (its own catalog `api-clipper2.md:26` and `arrangement.md:470` name Fabrication the real consumer); PROMOTE CavalierContours from orphan to the `[V10]` toolpath-offset owner at whichever stratum that verdict sites; ADMIT Manifold (Apache-2.0) as the named tier-3 boolean engine per `[V5]`; RESOLVE the `MathNet.Numerics 6.0.0-beta2` floor (`Directory.Packages.props:62-64` — no stable 6.0 exists; ruled default: downgrade to `5.0.0` stable, the beta pin surviving only on a fence-composed 6.0-only member recorded with a bump blocker — and the survival sweep covers EVERY central-pin consumer's fences, never the kernel corpus alone: Compute's solve/SVD/statistics rails and Fabrication's `[V8]` capability statistics verify against the `5.0.0` surface before the downgrade lands, `RASM-CS-COMPUTE-BRIEF.md`/`RASM-CS-FABRICATION-BRIEF.md` the co-consumers); DROP the MKL/OpenBLAS provider references from `Rasm.csproj:11-12` (no osx-arm64 native asset — managed fallback always; keep central pins only if x64 CI proves need); REFERENCE the pinned-but-unreferenced `CommunityToolkit.HighPerformance` (`Directory.Packages.props:38`) and mine it per `[04]`; AUTHOR the absent LanguageExt catalog — `LanguageExt.Core 5.0.0-beta-77` (`Directory.Packages.props:29`) is the rail every fence composes and NO `.api` file exists in either tier (a track-shared obligation with the Persistence/Compute/AppHost campaigns: the first campaign to run lands the ONE substrate-tier authoring, later campaigns verify, never re-author — and every shared-tier catalog motion in this section rides the same track law: the first campaign lands the substrate-canonical edit, later campaigns verify and layer only their folder-tier delta); CORRECT the divergent shared-tier `api-hashing.md` (`libs/csharp/.api/api-hashing.md:82` says `XxHash3.HashToUInt64`/`ulong`; the truth is `XxHash128.HashToUInt128` seed-zero/`UInt128` per `Rasm/.api/api-hashing.md:82`, `ARCHITECTURE.md:44`, and every fence); DEDUPE the five cross-tier catalog copies (csparse/mathnet-providers/tensors byte-identical, hashing/mathnet-numerics divergent) to one owning tier each. License gate: OSS and free-for-OSS-commercial admissible; GPL/AGPL/pay-tiered/seat-licensed REJECTED — recorded rejections that are never re-proposed: SISL (AGPL), TetGen (AGPL), CGAL straight-skeleton/skeletonization (GPL), SolveSpace (GPL), CuraEngine/Slic3r cores (AGPL), Instant Meshes (repo license NOASSERTION, dormant since 2022), MeshLib (free-non-commercial/paid-commercial), MCUT (LGPL/commercial, dominated by Manifold).

## [01]-[PHASE_ARCHITECTURE]

ONE ephemeral design workflow (authored via `workflow-creator`, deleted after landing) runs survey -> draft -> judge -> decide -> salvage and emits `RASM-CS-GEOMETRY-DECISION.md`. Shape:

- **Survey**: 4-6 read-only agents partitioned by plane (Numerics+Spatial / Meshing / Processing / Parametric+Drawing+gates / both `.api` tiers+manifests) re-verify `[02]-[EVIDENCE_REGISTER]` against disk (the register is initial pointer, never ceiling), run `assay api` over the restored `Rasm.Vectors` for every unverified seam member, and sweep every `using Rasm.*` and cross-package ledger edge into the raw seam census.
- **Draft**: 4 independent COMPLETE page-set proposals — one row per page carrying action (KEEP/REBUILD/SPLIT/MERGE/MOVE/DELETE/NEW), owner charter with namespace + fault cluster, entry signature (`Apply` op-union or NONE for vocabulary pages), in/out seam edges, and wave — drafted under four lenses (floor-maximalist, consumer-gate-first working backward from the Generation SpineRef and a Fabrication toolpath program, roster-mining-first, folder-architecture-first). A draft leaving an evidence row undisposed, a verdict unanswered, or a `[03]` delta homeless is incomplete and loses at judge.
- **Judge + Decide**: adversarial scoring against `[00]` law + `[03]` targets — verdict disposition, strata acyclicity (zero upward edges, zero page cycles), and both consumer gates fully homed are disqualifying; capability reach then surface collapse rank survivors; the decision is one blueprint with grafts.
- **Salvage**: per-draft miners + overturn auditor + ONE writing integrator; a mined finding is accepted only with a disk or `.api` file:line anchor the auditor re-verifies.
- **Emit**: `RASM-CS-GEOMETRY-DECISION.md` at the repo root carrying (a) the final page-set table with per-page skeletons, (b) the ratified folder/namespace/fault-cluster partition, (c) the corrected seam ledger (owner + counterpart ledgers, one anchor per seam), (d) roster deltas with `.api` obligations, (e) the leg partition proven acyclic, (f) the `[T-BOOLEAN-NATIVE-ASSET]` card disposition.

The DECISION must resolve every verdict below. Recommended shapes follow each; the design pass may exceed them, never dilute them.

### [V1_STRUCTURE_RATIFICATION]

Folders = namespaces = fault clusters = the concept partition, ratified ONCE. The realized per-concept namespace scheme (14 fence declarations, self-consistent `using`s, against the 12 fault clusters at `faults.md:14`) WINS over the stale 5-namespace law; `ARCHITECTURE.md:64` and the `[01]` codemap are rewritten to the ratified partition, `Parametric` enters the codemap (it is absent today — an undeclared sixth sub-domain), and the four namespace-less pages (`predicates.md`, `receipts.md`, `naming.md`, `reconciliation.md`) gain declarations. The ratification is two-tier because one flat folders=namespaces equation cannot hold — the post-campaign corpus is ~27 pages over 13+ concept namespaces, so folder=namespace 1:1 forces one-file folders and the folder-of-5 buries concepts: TIER-1 folders are domain groups (every folder ≥2 pages, a real growth axis); TIER-2 namespaces are concepts mapping 1:1 to fault sub-bands, a namespace never spanning two folders, each folder charter enumerating its namespaces in `ARCHITECTURE.md:[03]`. The census is not 1:1 either — 13 concept namespaces vs 12 clusters: `Tessellation` owns no cluster (rule delaunay's fault home) and `Parametric` borrows (`[V12]` mints) — the ratified partition disposes both. Recommended folder floor (exceed, never dilute): `Numerics/` (predicates, faults) · `Spatial/` (index, naming, reconciliation) · `Meshing/` (substrate `[V6]`, delaunay, intersect, slice `[V10]`b, arrangement, offset, skeleton `[V10]`d) · `Solving/` (solver, fit — the `[V7]` one-LM family lifted out of the Processing grab-bag) · `Processing/` (repair, receipts, decimate, remesh `[V11]`, flatten — mesh-in/mesh-out transforms) · `Parametric/` (the `[V2]` six) · `Drawing/` (view, pack — a pairing sharing no concern today; ruled default: the folder charter becomes downstream emission = projection + residency encoding, renamed to match, and pack re-homes only if that charter fails the growth-axis test). The phantom `Rasm.Geometry.Topology` dies everywhere: `pack.md:33` re-imports the ratified naming/reconciliation namespace, `solver.md:13` corrects its prose, Fabrication's `Rasm/Geometry/Healing` ledger anchor (`Rasm.Fabrication/ARCHITECTURE.md:44`) follows the `[V5]` re-home. The naming/reconciliation namespace honors `ARCHITECTURE.md:64`'s no-`Topology` clause (collision guard against `Rasm.Domain.Topology`); ruled default: `Rasm.Geometry.Naming` with the `topology` fault cluster (2404-2407) renamed in the same motion — folding it into index's `.Spatial` breaks the concept-1:1 law (two clusters, one namespace); sanctioning `.Topology` instead requires proving the collision fear void.

### [V2_PARAMETRIC_TIER]

The campaign-defining coverage verdict: `Parametric/` grows into the surface-development tier that closes the Generation KERNEL GATE (`RASM-GENERATION-SPEC.md:179,226` — "a Vectors/Geometry rebuild campaign PRECEDES Generation stand-up"). The recommended-shape floor, all owners composing Vectors primitives and the flatten `ChartAtlas`, never re-deriving them: `curve.md` REBUILT full-body (the stubs at `curve.md:117-138` die; curve `Offset` promoted from Growth `curve.md:19` to a first-class `ParametricOp` case; the station/frame family lands as first-class cases — arc-length stations via `Divide(maxSegmentLength, equalSegmentLengths)`/`ParameterAtLength`/`PointAtLength` sweeping rotation-minimizing `PerpendicularFrames`, all `.api`-verified (`api-gshark.md:81-88`), evaluated over the `SpineRef` `[T0,T1]` window on GShark's normalized-domain convention (`api-gshark.md:165`) — the `PathRow` placement input `RASM-GENERATION-SPEC.md:138,164` consumes; SSI disposition explicit — GShark has no surface-surface intersection; ruled default: the host-deferred charter row — no gate item at `RASM-GENERATION-SPEC.md:179` names trimmed surfaces, and an owned discrete-lattice op requires a named gated consumer first); `surface.md` NEW (NURBS surface ops the gate names: isoline families beyond the single `IsoCurve`, geodesic POLYLINES on parametric surfaces composing Vectors `ScalarField.Geodesic`+trace, normal-offset surfaces, curvature-domain sampling); `subdivide.md` NEW (Catmull-Clark/Loop as stencil rows over ONE refinement fold per `[GENERATOR_LAW]`; limit-surface evaluation; the OpenSubdiv admission carries the burden of proving limit-eval-at-scale demand); `develop.md` NEW (guaranteed-isometric developable-strip unroll — distinct from flatten's low-distortion conformal maps — feeding Fabrication sheet/plywood/fabric); `panelize.md` NEW (cross-field-guided quad/diagrid panelization composing Vectors `CrossField` — one `int symmetry` argument owns the quad/diagrid/tri family as data, `Field.cs:1864` — + the remesh substrate `[V11]`); `patternmap.md` NEW (Vectors `TangentLogMap` + 2D symmetry-group tiling -> surface-mapped instance stream — the Generation PATTERN/TILING plane's exact input — with each instance carrying a frame parallel-transported by `VectorHeat` (`Field.cs:1868`); position without orientation is half an instance). Two tier-wide chain obligations bind every owner above: (a) PARAMETRIC CONTENT IDENTITY — `SpineRef` resolves geometry by content key (`RASM-GENERATION-SPEC.md:164`) while `Encode` is topology-adjacency-only and the domain computes no hash beyond that seam (`reconciliation.md:17,21`); the tier owns a canonical-bytes projection for parametric forms (degree · knots · weights · control net, canonicalized) riding the SAME reconciliation seam family — kernel emits the digest, Persistence mints the key — or the Generation dry-run's FIRST link, resolving the spine, has no owner; and the projection is a COMPONENT of the parametric node's `ToCanonicalBytes(tolerance)` contribution the Element seam `ContentAddress.Of(Node)`/`OfGraph` folds (`Rasm.Persistence/.planning/Element/codec.md:151-152`), never a sibling SpineRef-resolution key beside it — demanded by `Rasm.Persistence` (the consumer): its recovery content-identity proof and federation reproducibility assert an `OfGraph` digest that must fold parametric forms bit-identically cross-runtime, so a parametric edit that leaves `OfGraph` unchanged (digest blind to the parametric body) or a parametric key the graph address never folds (two unreconciled identities) both break the downstream content-identity assertion at its source; (b) UV PULLBACK — every "composes Vectors" claim crosses the parametric↔discrete boundary twice, so `surface.md`'s sampler emits UV-provenanced tessellation (per-vertex `(u,v)` beside the `MeshSpace`) and field results pull back to the parametric domain by construction, `NurbsSurface.ClosestParameter` (`api-gshark.md:111`) the foreign-point fallback — a world-space polyline with no surface binding cannot feed `panelize.md` or `patternmap.md`. Fault home per `[V12]`; `curve.md`'s borrowed `ParameterizationFault` (`curve.md:191`) ends. The folder name ruled default is `Parametric/` (a rename needs a judge-scored win); the one-file folder ends either way.

### [V3_VIEW_ENGINE]

`Drawing/view.md`'s visibility engine is rebuilt ground-up — the page cannot be polished because the advertised engine does not exist (`[VERDICT]` proof 1). The exact silhouette locus (`FacesOppose` exact `Orient3D`, `view.md:235-241`) and the Section delegation to `IntersectOp.PlaneMesh` (`view.md:349-353`) are the salvage. The rebuild is ONE of: a REAL BSP painter `Paint` actually consumes (back-to-front traversal driving the visibility solve), or exact analytic Appel quantitative-invisibility (invisibility count updated ONLY at silhouette crossings, computed through the crossing lattice — no sampling). The dead `BspNode`/`Partition`/`Split` apparatus becomes load-bearing or dies with the choice. `ToPolylines` chains by connectivity (successor-linked walk), never by kind-key concatenation. Downstream in the same ruling: Fabrication's `Posting/Projection.cs` BSP HLR dies — its own ledger already routes `HiddenLineResult` to AppUi as "the BSP visibility solver superseding the AppUi painter sort" (`Rasm.Fabrication/ARCHITECTURE.md:46`); the kernel `DrawingProjection` supersedes BOTH, Fabrication consumes.

### [V4_CROSSING_COLLAPSE_LANDED]

The `intersect.md:388` collapse is executed, not re-declared: `arrangement.md` (`EdgeCrossings`/`SegmentOf`/`PlaneCrossPoint`/`InTriangle`, `:242-262`), `offset.md` (`SegmentsCross`, `:440-444`), and `repair.md` (`TriangleCrossPoint`/`EdgesCrossTriangle`, `:346-372`) re-route through `Intersection.Apply`/`Crossing` — no fourth crossing kernel. The arrangement rounding gap resolves in the same motion — ruled default: `Constraint` gains the implicit-point case carrying the `Lpi` crossing into `Tessellation.Build` so operand crossings ride the same exact path as internal Steiner points; the exact-sign/rounded-vertex honesty re-scope on both pages (`arrangement.md:467` and `delaunay.md`'s recovery prose) survives only if drafting proves `Tessellation` cannot host implicit-coordinate vertices through constraint recovery. `SegmentSegment`/`LineLine`'s silent Z=0 projection (`intersect.md:293`) becomes a typed 2D restriction on the op case.

### [V5_BOOLEAN_OWNERSHIP]

`BooleanOp` re-homes from `repair.md:57` (Processing/Healing) to the boolean owner `arrangement.md` or the ratified shared-vocabulary floor — the current shape has a lower stratum consuming an upper stratum's type plus a repair⇄arrangement page cycle (`arrangement.md:18` composes Healing; `repair.md:288-292` composes Arrangement). The duplicate `BooleanReceipt` name collision (`arrangement.md:115` vs `receipts.md:44` — same name, different shape and namespace) resolves to one type or two names. The tier-3 native gate is NAMED: Manifold (`elalish/manifold`, Apache-2.0, guaranteed-manifold boolean, C FFI `manifoldc`) is the categorical-best engine for the reserved scale path (`arrangement.md:469` `[BOOLEAN_NATIVE_ASSET]`; `ArrangementPolicy.ScaleCeiling: int.MaxValue` at `arrangement.md:61` makes the gate dead code today) — admitted behind a finite `ScaleCeiling`, bound via a thin in-house P/Invoke over `manifoldc` (the `ManifoldNET` binding is alpha/stale — pinning it requires recorded risk), gated behind the page's own golden-boolean fixture law. This ruling disposes the `[T-BOOLEAN-NATIVE-ASSET]` BLOCKED card. The managed exact arrangement stays the correctness owner; Manifold is the scale companion, never a second correctness rail.

### [V6_MESH_SUBSTRATE]

`MeshEdit` — the de-facto shared mesh working set five non-healing pages consume (`repair.md:76-102`; intersect/arrangement/view/decimate via `using Rasm.Geometry.Healing`, receipts unqualified in `Of`'s signature — pack is NOT in this fan; its phantom import is `[V1]`'s concern) — extracts to the `Meshing/` floor page under its own ratified concept namespace: the folder charter becomes substrate + exact construction lattice, and Spatial stays identity/query, never a mutable mesh arena. The wave-1 extraction is additive plus a surgical excise of repair's donor block only — repair's full `[V9]` rebuild stays wave 3. The triangle-soup adapter collapses from three near-identical `Soup(MeshSpace)` copies (`intersect.md:327`, `view.md:386`, `arrangement.md:352`) onto the same owner. The `[SEAM_AND_RAIL_LAW]` store-mutability contract lands here for every SoA store in one motion.

### [V7_ONE_LM_ENGINE]

One damped-Gauss-Newton owner: `solver.md`'s `Iterate`/`Step`/`SolveCholesky` (`solver.md:295-347`) generalizes to a residual+Jacobian functor + `SolvePolicy` (it is today private and `Constraint`-coupled through `Linearize`); `fit.md`'s `Refine` re-implementation (`fit.md:568-593`) dies — its `Distance`/`Gradient` per-primitive rows ARE the functor — with λ threaded through the refine state on the policy ladder (the hardcoded `1e-3`/`×10.0`/`1e12` and the no-carry-down accept branch die); `decimate.md`'s local Cholesky wrapper (`decimate.md:374-379`) composes the one solve rail. The dense-LA rule unifies: the shared `SolveCholesky` rail is the ONE provider boundary (MathNet dense for small normal-equation systems, Vectors `CholeskySparse` for sparse — `flatten.md:18`'s wrapper discipline generalized), so `fit`/`decimate`/`solver`'s direct `MathNet.Numerics.LinearAlgebra` scatter and `flatten`'s wrapper rule stop being two laws for one concern.

### [V8_SPATIAL_CONTRACT]

`Spatial/index.md`: `PointCloud` lifts out of the `NodeStore`-centric union (a sibling owner or a distinct union whose shape owns the kd-tree) so the throws (`index.md:130,574,579`) and the silent-empty breach (`index.md:358`) die on the `Fin` rail; bounds narrowing rounds OUTWARD (`Single` round-to-nearest at `index.md:79` shrinks internal-node bounds below the child union — the descent prune at `:453,474,518` can produce range/ray/overlap FALSE NEGATIVES; broad-phase min rounds down, max up); `ToAcceleration` returns `Fin<>` over a KERNEL-owned acceleration wire (the `using Rasm.Compute.Solver` at `index.md:27` dies; Compute decodes the node-link wire per its own `:125` residency row); the GWN double-owner reconciles (index's Barnes-Hut tree-code vs Vectors' exact per-triangle winding — one composes the other, ruled default: index owns the BVH-accelerated query, Vectors' SDF composes it) — and that ruling IS the Fabrication distance-field ingress: `SignedDistanceFromMesh` (`Field.cs:394`) through the index-accelerated winding is the ONE distance-field lane, the kernel mints no second SDF owner; stale Cases counts (`index.md:14,707`) resync. `Spatial/naming.md`+`reconciliation.md`: `RebuiltEntity` gains an explicit `Self` index (fixing the `[VERDICT]` proof-4c mis-key), `Subsumes` fixes its subset direction for splits (`naming.md:62-66` requires the child to hold MORE of the parent's boundary; a split child holds fewer), the Survive×Migrate injectivity residual (`naming.md:161`) resolves, and both frozen fixtures re-prove per `[SEAM_AND_RAIL_LAW]`. The reconciliation→Persistence seam anchor unifies (three ledgers, three anchors today: `reconciliation.md:2` vs `ARCHITECTURE.md:45` vs `Rasm.Persistence/ARCHITECTURE.md:55`).

### [V9_PROCESSING_REPAIRS]

The confirmed-defect set lands as rebuilds, not patches. `flatten.md`: correct Dirichlet (large penalty on pinned rows only with rhs = penalty·value, or the eliminate-boundary-rows formulation); LSCM assembled SPARSE as the conformal-energy least-squares smallest-eigenpair (the dense `(2n)²`+SVD dies); ARAP global assembly O(F) (pass the in-scope face index `f`; `IndexOf`'s O(F) scan with `return 0` on miss at `flatten.md:482-485` dies); `FactorNonZeros` reports the actual Cholesky factor fill, not `D1.NonZeros` (`flatten.md:330`); `ChartStore.Dead`/`FreeList`/`Kill` wired or deleted. `decimate.md`: real vertex->incident-face adjacency seeded in `QuadricStore` and maintained across collapses (O(F log F) drain); boundary link condition (`shared == 1`) so open meshes decimate. `repair.md`: `SelfIntersectResolve` becomes a real CDT re-mesh through the substrate (the one-point fan patching only face `g` at `repair.md:244-250` is the naive form its own delaunay dependency exists to replace); topology status threads forward (`before[n] = after[n-1]`, killing the 2N recompute at `repair.md:382-385`); the `((QueryResult.Hits)hits)` hard cast joins the typed rail. `receipts.md`: `Converged` for a boolean tests success, not the gate flag. `fit.md`: `FitStore` wired or deleted (documented as the consensus SoA at `fit.md:3,13,248`, never constructed); the `Line` chart de-degenerates (Z-fixed anchor biases near-vertical lines; `MinimalSamples=3` vs 2-point `MinimalLine` at `fit.md:481` resyncs).

### [V10_TOOLPATH_PLANE]

The Fabrication gate closes kernel-side where the concern is kernel-shaped, with Fabrication consuming: (a) **corner-strategy offset** — `JoinType`(Miter/Round/Bevel/Square)×`EndType` rows over one offset assembly; ruled: the arc-aware toolpath modality COMPOSES CavalierContours (ISC, arc-bulge true arcs, already pinned and orphaned) beside the exact skeleton core, never a hand-rolled arc offset; sited as an `OffsetOp` arm when it composes the exact medial for clearance, as a Fabrication owner when it is pure kerf compensation — the DECISION rules on that criterion, and the ruling yields ONE consuming stratum: if both modalities survive, the kernel owns the offset assembly and Fabrication consumes the seam, never CavalierContours in two manifests; offset ownership stays domain-partitioned three ways — region/polygon (this owner), parametric curve (`[V2]` `ParametricOp` case), surface normal-offset (`[V2]` surface.md) — three domains on one exact floor, never one fuzzy owner; (b) **slice stack** — a NEW owner folding `IntersectOp.PlaneMesh` over a parallel-plane family into per-layer contour-nesting trees with uniform/adaptive layer-height policy rows (author-kernel; AGPL slicer cores rejected — and the policy family is OPEN as seed rows: Fabrication's production-additive lane demands variable-by-slope adaptive and support-interface layer rows on the same family, `RASM-CS-FABRICATION-BRIEF.md` `[V6]` the named consumer); layer contours land ORIENTED (outer CCW / holes CW — the convention the downstream arc-offset `Shape` engine demands, `api-cavaliercontours.md:41,43`) and non-watertight sections emit typed OPEN-chain rows, never silent closure (`IntersectResult.Chains` is `Seq<Polyline>` loops-only today — the fold widens the result, or real scan/repair meshes silently fail the gate); Fabrication's `Toolpath/slicing` re-routes per its own ledger promise (`Rasm.Fabrication/ARCHITECTURE.md:47`), and the Compute circulation runner is the SECOND named consumer — story-elevation floor-plate contours decoded through the wire (`RASM-CS-COMPUTE-BRIEF.md` `[V12]`a), never a Compute-side section fold; (c) **medial made real** — ruled default: `Tessellation` exposes the Voronoi-dual accessor the medial trim reconciles against (wave-2 delaunay already carries the accessor per `[05]`); the `offset.md:16,478` claim re-scopes to trimmed-skeleton honesty only if that leg proves the dual reconciliation vacuous; the discarded build (`offset.md:325-326`) dies either way; the stale 4-of-5 owner sentence (`offset.md:13` omits `weighted`) resyncs; (d) **3D medial/curve-skeleton** — author-kernel mean-curvature-flow skeletonization composing Vectors `MeanCurvatureFlow` (no permissive engine exists; CGAL GPL rejected), sited by the DECISION as the growth page of the offset/skeleton concern; across (c)+(d) the seam payload carries the per-point CLEARANCE-RADIUS function — Fabrication's consuming demand is exactly the "wavefront clearance-radius field" (`Rasm.Fabrication/ARCHITECTURE.md:22`) — sharpened by the production campaign to a clearance query at ARBITRARY probe point plus weighted/variable-speed wavefront rows for variable-kerf compensation (`RASM-CS-FABRICATION-BRIEF.md` `[V5]` rest-machining and engagement control the demanding consumers) — and its `Toolpath/Skeleton.cs` dies ONLY if the kernel medial carries radius; a radius-less medial keeps the duplicate alive on honest need; (e) Fabrication's duplicate `Toolpath/Skeleton.cs` dies for `Offsetting.Apply` per `[PLACEMENT_LAW]`.

### [V11_HULL_AND_REMESH]

Convex hull gains an owner: the lifted-paraboloid lower hull is a `Tessellation` projection (a Bowyer-Watson byproduct on the same exact floor — no new package), and MIConvexHull leaves the manifest; both consumers touch hull (Fabrication stock/clearing envelopes, Generation bounding/containment). Remeshing gains its substrate: Botsch-Kobbelt isotropic remesh (split/collapse/flip/tangential-relax, genus-preserving) + cross-field-guided quad remesh composing Vectors `CrossField` — the panelization floor `[V2]` composes; `decimate.md`'s `VoxelRemesh` (topology-changing SDF resample) stays a distinct row, never conflated. Deciding criterion for deferral: quad remesh may defer to a growth card ONLY if `panelize.md` proves its grid family lands on the isotropic substrate alone; the isotropic owner itself is not deferrable — panelization and Fabrication mesh-prep both gate on it, and Compute's volumetric discretization names it the boundary-conditioning pre-step for defective building shells (`RASM-CS-COMPUTE-BRIEF.md` `[V7]` recorded growth, the third named consumer — decoded through the wire, never a Compute-side remesher).

### [V12_FAULT_TYPING_AND_BAND]

`faults.md` re-types its stringly majority — 10 of 17 cases carry only `string Detail` against the page's own "typed to its failure with its real payload" law (`faults.md:20`); the five rich cases (`OverConstrained`/`SingularSystem`/`FitFault`/`DecimationFault`/`SkeletonStalled`) prove the shape. The band plan lands with the new planes: the geometry century 2400-2449 is hard-bounded (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:144` pins the 2400 mirror; Material owns 2450), 12 clusters occupy 2400-2447, headroom is 2448-2449 (`faults.md:19`), and `[V2]`/`[V10]`/`[V11]` mint new concerns needing homes (parametric today borrows `ParameterizationFault`). The DECISION emits the plan: concern-adjacent new owners share existing clusters (subdivision/develop/panelize under a widened `parameterization`-family stride, slicing under `intersection`-family), the last stride 2448-2449 is spent deliberately, and a federation re-plan escalates ONLY if the ruled cluster map cannot fit — never a silent 13th-cluster squeeze.

### [V13_PACK_RESIDENCY]

`Drawing/pack.md`: the phantom `Topology` import dies (`[V1]`); the dtype residency claim becomes real or dies — `EncodedStore.Payload` is always `float[]` (`pack.md:126-129`) and `ChannelDtype.Pack` quantizes-then-widens back to `float`, so "a `Half` halves residency" (`pack.md:13,463`) is false today; ruled default: the store becomes dtype-strided byte storage with per-channel descriptors (the `System.Numerics.Tensors` seam at `pack.md:465` reads spans over it; the `[TELOS]` residency lane binds), and the Dtype column re-scopes to round-trip-error-only honesty only if the strided store defeats that span seam; the `ScalarField.SampleDetailed` seam verifies via `assay api` or re-routes to the landed Vectors member; the `OrientedNormals` mis-attribution (`pack.md:3` credits Processing/fit for a Vectors capability) corrects. Consumer pressure lands the two-row modality growth the page's own Growth law anticipates (`pack.md:19` — "a field grid, a toolpath polyline"): `PackKind` gains `field` · `toolpath` case rows with their active-channel sets — `Rasm.Compute` decodes them as the residency wire's case axis (`Rasm.Compute/ARCHITECTURE.md:126` already rules "Field/Toolpath land as kernel PackKind rows + a residency Wire row, never a residency-side GeometryEncoding owner") and `Rasm.AppHost` signature-locks its `EncodingKind.Field`/`.Toolpath` working spellings on them (`Rasm.AppHost/.planning/Sandbox/solver.md:54-55,197`; RASM-CS-COMPUTE-BRIEF.md/RASM-CS-APPHOST-BRIEF.md the demanding consumers) — two data rows per `[GENERATOR_LAW]`, never a downstream encoding owner.

### [V14_LEDGER_AND_DOCS]

The seam ledger becomes enforceable law, both directions: `ARCHITECTURE.md:[02]` gains the wired-undeclared edges (pack->Compute/Tensor/residency per `Rasm.Compute/ARCHITECTURE.md:125`, pack->AppHost/Sandbox `:126`, the entire Parametric consumer fan `curve.md:193`, the Vectors DEC/ScalarField/CloudMetric intra-kernel seams); the mis-stated edges die (Compute `:120`'s spurious "Geometry solver consumes Compute LM"; the index⇄Compute bidirectional overstatement; view->Fabrication honored by Fabrication CONSUMING per `[V3]`); anchor drift fixes to one anchor per seam everywhere (`[V8]`'s reconciliation triple; intersect->Fabrication's `Posting` vs `Toolpath/slicing` split resolves with `[V10]`). `README.md`'s `[GEOMETRY_KERNEL]`/`[COMPUTATIONAL_GEOMETRY]` paragraphs rewrite to the surviving roster (the LibTess fill-leg fiction at `README.md:44` dies); the two false rejection rationales correct (`delaunay.md:3` "Triangle GPL" -> float-epsilon + no exact CDT/CDTet; `predicates.md:3` "no library carries a robustness guarantee" -> no maintained, NuGet-published, indirect-complete predicate library exists); `predicates.md` gains the strict-IEEE-754/RID invariant on `NumericsPolicy` that makes `TwoSum` safe (x87 extended precision defeats Shewchuk EFTs; the target RIDs are strict) plus the FMA-free `TwoProduct` fallback row — Dekker-split EFT product where the target lacks hardware FMA. Consumer pressure on the same hardening: `Rasm.Compute` (`RASM-CS-COMPUTE-BRIEF.md` `[V7]` — the `Solver/discretization` CDTet in-sphere/orientation sign-decision floor) composes the exact predicate family cross-package through a declared seam row, so the hardened surface exports a package-boundary-consumable `Orient3D`/`InSphere` member family (adaptive-filtered entry, `assay api`-verifiable from the restored `Rasm` assembly) — a kernel-internal-only predicate floor forces that consumer onto a vendored Shewchuk duplicate, the exact re-own `libs/.planning/architecture.md:54` forbids.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — Phase 1 re-verifies every row against disk. Anchors are `libs/csharp/Rasm/Geometry/.planning/`-relative; `ARCH:` anchors `libs/csharp/Rasm/ARCHITECTURE.md`; `FAB:`/`COMPUTE:` anchor those packages' `ARCHITECTURE.md`; `PROPS:`/`CSPROJ:` anchor the manifests.

| # | Class | Evidence |
|---|---|---|
| E1 | Dead HLR engine | `view.md:302` `Paint` never reads `bsp`; `:263-300` Partition/Split built-discarded; `:320-347` uniform `SampleStep` sampling not Appel QI; `:3,16` the claimed engine; `:130-135` kind-key polyline concat |
| E2 | Gate-plane absence | `RASM-GENERATION-SPEC.md:179,226` gate roster; zero subdivision/develop/panelize/unroll owners in Geometry+Vectors (grep); `curve.md:117-138` stub bodies, `:19` Offset demoted, 156 LOC one-file folder; Vectors primitives real (`Vectors/Field.cs:386-394,1864-1872`); `intersect.md` single-plane only; `offset.md` miter-only |
| E3 | Roster fiction | `ARCH:3` "no external geometry library" vs `README.md:44` roster vs `curve.md:3` GShark; `PROPS:70-78`+`CSPROJ:18-31` seven zero-consumer packages; `delaunay.md:3` false "Triangle GPL"; `api-clipper2.md:91-92`/`api-cavaliercontours.md:95`/`api-libtess.md:84`/`api-sharpvoronoilib.md:138`/`api-geometry3sharp.md`/`api-miconvexhull.md:119`/`api-triangle.md` catalogs naming page consumers the pages reject |
| E4 | Confirmed math bugs | `flatten.md:359,364` inverted Dirichlet; `:377,173` dense LSCM vs `:4` sparse prose; `naming.md:113`+`reconciliation.md:55-62` vertex mis-key; `receipts.md:65,99` Converged inversion; `decimate.md:312` boundary freeze, `:318-322,342-360` O(F²) scans vs `:16` O(1) prose |
| E5 | Namespace schism | `ARCH:64` five-namespace law + no-`Topology` clause; 14 fence declarations (rg census), 4 pages declare none; `pack.md:33` + `solver.md:13` phantom `Rasm.Geometry.Topology`; `FAB:44` cross-package `Rasm/Geometry/Healing` anchor; Parametric absent from `ARCH:[01]` codemap |
| E6 | Strata violations | `index.md:27` `using Rasm.Compute.Solver`, `:572-579` returns Compute's type — vs `libs/.planning/architecture.md:35`; `COMPUTE:120` spurious reverse seam disproven by `solver.md:295-347` |
| E7 | Unrealized collapses | `intersect.md:388` owner-declared vs live copies `arrangement.md:242-262`, `offset.md:440-444`, `repair.md:346-372`; `offset.md:16,478` medial dual claim vs `:325-326` discarded build; `fit.md:16` "SAME LM" vs `:568-593` re-implementation vs `solver.md:295-347`; `arrangement.md:467` implicit framing vs `:258-262` rounded `PlaneCrossPoint` |
| E8 | Duplicate owners cross-strata | `FAB:33` Posting BSP HLR vs `view.md`; `FAB:22` Toolpath skeleton vs `offset.md`; `FAB:47` in-folder planar section vs `intersect.md` `PlaneMesh` (its own row promises re-route) |
| E9 | Duplicate mechanisms in-kernel | LM ×2 + Cholesky wrapper ×3 (`solver.md:295-347`/`fit.md:568-593`/`decimate.md:374-379`); GWN ×2 (`index.md:383-444` Barnes-Hut vs Vectors exact per-triangle); `BooleanReceipt` name ×2 (`arrangement.md:115` vs `receipts.md:44`); soup adapter ×3 (`intersect.md:327`/`view.md:386`/`arrangement.md:352`); dense-LA two rules (`flatten.md:18` wrappers vs direct MathNet elsewhere) |
| E10 | Rail breaches | `index.md:130,574,579` throws; `:358` silent empty vs `:351` prose; `:79` round-to-nearest bounds narrowing -> false-negative prune at `:453,474,518`; `repair.md:243` hard cast |
| E11 | Dead/misleading carriers | `BspNode` apparatus (E1); `fit.md:3,13,248` `FitStore` never constructed; `flatten.md:81` `ChartStore.Dead/FreeList/Kill` unused, `:330` wrong-operator `FactorNonZeros`; `pack.md:13,463` false Half-residency over `:126-129` `float[]`; `index.md:14,707` stale Cases counts; `offset.md:13` stale 4-of-5 owner roster; `offset.md:195` `WavefrontStore` 2n under-allocation |
| E12 | Seam-ledger drift | reconciliation->Persistence three anchors (`reconciliation.md:2` / `ARCH:45` / `Rasm.Persistence/ARCHITECTURE.md:55`); `ARCH:58` ⇄ vs `COMPUTE:121` one-way; wired-undeclared pack->Compute (`COMPUTE:125,126`), Parametric fan (`curve.md:193`); `ARCH:16` codemap missing kd-tree/GWN growth |
| E13 | Catalog divergence | shared `libs/csharp/.api/api-hashing.md:82` `XxHash3`/`ulong` vs per-package `XxHash128`/`UInt128` truth (`Rasm/.api/api-hashing.md:82`, `ARCH:44`); five catalogs duplicated across tiers (csparse/mathnet-providers/tensors identical; hashing/mathnet-numerics divergent) |
| E14 | Version/platform risk | `PROPS:62-64` MathNet `6.0.0-beta2` floor (feed-verified: `5.0.0` is the newest stable, `beta2` the newest release); `CSPROJ:11-12` MKL/OpenBLAS dead on osx-arm64; `PROPS:78` Triangle `0.0.6-Beta3` beta+dead (latest release 2016-05); `PROPS:38` HighPerformance pinned, unreferenced; `PROPS:29` LanguageExt.Core beta pin with NO `.api` catalog in either tier; GShark upstream dormant (last release+push 2023-08, MIT — the `[04]` vendoring fallback); `Alimer.Bindings.MeshOptimizer` (`PROPS:91`) is Compute's and PicoGK/OcctNet.Wrapper (`PROPS:84-85`) are Fabrication's — record the splits so they are never mistaken for kernel dead weight |
| E15 | Stringly faults + stub fences | `faults.md:20` typed-payload law vs 10 of 17 cases carrying bare `string Detail` — the `[V12]` re-type set; `delaunay.md:192-198` seven signature-only `[STORE_OPS]` bodies (`Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces`) — the `[03]` delaunay transcription target |

Sound surfaces (preserve; harden only per the verdict that names them): `predicates.md` (9 — the reference floor; only the `[V14]` rationale/RID rows touch it), `faults.md`'s union shape (the `[V12]` re-type is additive), `solver.md`'s constraint algebra and witness DOF, `index.md`'s builder/query breadth, `arrangement.md`'s composition spine, `flatten.md`'s DEC seam shape, `reconciliation.md`'s fixture-corpus design, `intersect.md`'s exact lattice, and the corpus-wide external-member verification discipline (every composed GShark/kdtree/ddouble/Fraction/EFloat member resolves against its catalog).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the Phase-2 acceptance bar; the DECISION carries the per-page skeletons that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Numerics/predicates | 9 | 9.5 | RID/strict-IEEE invariant; precise precursor rationale; FMA-free fallback row; PeterO directed-rounding interval filter as a ladder enhancement candidate; package-boundary-consumable `Orient3D`/`InSphere` export for the Compute discretization consumer |
| Numerics/faults | 7.5 | 9.5 | ten stringly cases re-typed; band plan with new-plane homes; cluster taxonomy = ratified partition |
| Parametric tier | 3 | 9.5 | full-body curve; arc-length station/RMF-frame family; surface ops (isolines/geodesic paths/offset); subdivision; developable/unroll; panelization; pattern-to-surface with `VectorHeat` instance frames; parametric canonical-bytes identity; UV-provenanced pullback; SSI disposition; offset promoted |
| Spatial/index | 7 | 9.5 | PointCloud lifted; zero throws; outward-rounded bounds; kernel-owned acceleration wire; GWN single-owner; counts resynced |
| Spatial/naming+reconciliation | 6.5 | 9.5 | Self index; corrected `Subsumes`; namespaces declared; injectivity resolved; fixtures harness-proven; one Persistence anchor; parametric canonical-bytes projection on the one `Encode` seam |
| Meshing/delaunay | 6 | 9.5 | the seven stub bodies fenced (the hardest transcription in the tree); hull projection; Voronoi-dual accessor; honest rejection rationale |
| Meshing/intersect | 8.5 | 9.5 | collapse landed by consumers; typed 2D restriction; slice-stack producer seam |
| Meshing/arrangement | 7.5 | 9.5 | implicit-point `Constraint` or honest re-scope; `BooleanOp` homed; Manifold tier-3 named + finite ceiling; repair cycle broken |
| Meshing/offset+toolpath | 5 | 9.5 | medial real or re-scoped; clearance-radius seam payload; JoinType×EndType corner rows (CavalierContours composed); 3D MCF skeleton; store sizing; prose resync |
| Processing/solver+fit | 8 | 9.5 | one generalized LM (functor+policy); fit instantiates; λ ladder threaded; `FitStore` resolved; Line chart fixed; one dense-LA rule |
| Processing/repair+receipts | 7 | 9.5 | real CDT self-intersect re-mesh; `Converged` fixed; MeshEdit extracted; topology threaded forward; `BooleanReceipt` collision resolved |
| Processing/decimate | 7 | 9.5 | maintained face incidence (O(F log F)); boundary collapse; meshoptimizer/Compute split recorded |
| Processing/flatten | 6 | 9.5 | correct Dirichlet; sparse LSCM eigenpair; O(F) ARAP; true factor fill; dead carriers resolved; developable seam to the parametric tier |
| Drawing/view | 4 | 9.5 | a real visibility engine (consumed BSP or analytic QI); connectivity chaining; Fabrication consumes `DrawingProjection` |
| Drawing/pack | 7 | 9.5 | real dtype-strided residency or honest re-scope; phantom import dead; `SampleDetailed` verified; attribution fixed; `field`/`toolpath` `PackKind` rows landed for the Compute residency + AppHost solver consumers |
| Slice/skeleton/hull/remesh (new) | 0 | 9 | slice-stack owner with oriented contours + typed open sections; MCF curve-skeleton; hull-as-Tessellation-projection; isotropic (+quad) remesh substrate |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every kernel fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` as the ADT backbone (already the most-mined substrate; stays law), LanguageExt `Fin`/`Error` rails at every entry, `System.IO.Hashing` `XxHash128` seed-zero as the ONE content hasher, `CommunityToolkit.HighPerformance` `Span2D`/`MemoryOwner<T>`/`ParallelHelper` (`api-highperformance.md:26,35,54`) into the SoA build arenas and parallel BVH/QEM/MLESAC folds once referenced (`PROPS:38` pinned, `Rasm.csproj` blind today), `System.Numerics.Tensors` `TensorPrimitives` for elementwise reduces on the non-exact float/double paths only (MLESAC consensus, Hausdorff sampling — never the exact-predicate path); the span-feed law is already written at `api-tensors.md:248` — the kernel's SoA coordinate buffers feed operators DIRECTLY (`TensorMarshal.CreateTensorSpan` wraps pinned buffers copy-free), a tensor-shaped re-pack is the rejected double-layout.
- `GShark` — mined to the instance surface (`NurbsBase`/`NurbsSurface`/`Fitting.Curve`/`Intersection.Intersect`, all `.api`-verified); the `[V2]` tier widens consumption (the station/frame spine `Divide`/`ParameterAtLength`/`PointAtLength`/`PerpendicularFrames` at `api-gshark.md:81-88`, `NurbsSurface.ClosestParameter` for UV pullback, `DivideByChordLength`, `DecomposeIntoBeziers`, `Offset`, the `From*` factory family). Upstream is DORMANT, not merely slow — last release AND last repo push 2023-08 (feed+repo verified) — and `[V2]` makes it the load-bearing engine of the campaign-defining tier, so the watch gains a ruled fallback: MIT permits vendor-and-own of the composed algorithm set in-kernel if dormancy ever blocks a `[V2]` owner; never a second NURBS engine beside it (the managed field is bare — `NurbsSharp 0.3.0` is a toy, feed-verified).
- `Supercluster.KDTree.Net` — namespace `SuperClusterKDTree`, squared-L2 `DistanceMetrics` (correctly handled at `index.md:377-380`); survives the `[V8]` PointCloud lift as the point-leaf engine.
- `TYoshimura.DoubleDouble` — the 106-bit tier + LM/QEM accumulation stays; the ~200-member special-function library remains reserved (no kernel consumer; recorded so it is never mistaken for dead).
- `ExtendedNumerics.BigRational` — `Fraction` oracle + rational keys stay; `Mediant` Stern-Brocot remains a recorded no-consumer reserve.
- `PeterO.Numbers` — the differential-oracle sources stay; the directed-rounding `Floor`/`Ceiling` interval filter (`api-peteronumbers.md:153`) is the one genuinely unmined ladder enhancement — `[V14]`'s predicates hardening evaluates it.
- `CSparse` — reached through Vectors `CholeskySparse` only (the `[V7]` rule); `Refactorize`/`Update`/`Downdate` amortization noted for the flatten factor cache; LGPL-2.1 weak-copyleft recorded at the license gate.
- `MathNet.Numerics` — the `[ROSTER_RECONCILIATION]` beta ruling; consumption narrows to the `[V7]` solve rail + `DecomposeSvd`; providers leave the kernel csproj.
- Manifold (ADD) — Apache-2.0 (upstream active, pushed this week), the `[V5]` tier-3 engine; thin P/Invoke over `manifoldc` (RID assets recorded at admission; `ManifoldNET` feed-verified stalled at `1.0.7-alpha`, 2024-08); `.api` catalog authored with the admission; golden-boolean fixture gates activation.
- CavalierContours (PROMOTE) — ISC, arc-bulge true-arc offset+boolean, `Shape.ParallelOffset` multi-pocket (`api-cavaliercontours.md:41`); the port is actively published (1.0.0, 2026-06 — feed-verified, the PROMOTE carries no staleness risk); the `[V10]` corner-strategy engine at the ruled stratum; its catalog's kernel-consumer claim (`:95`) rewritten to the real owner — and the catalog PAIR (kernel-tier + `Rasm.Fabrication/.api/api-cavaliercontours.md`, both live on disk) resolves to the `[V10]`-ruled stratum's tier in the same motion, the other copy deleting: never two folder-tier catalogs for one pin.
- Clipper2 (RE-SCOPE) — Boost (BSL-1.0, upstream active); leaves the kernel manifest group for Fabrication (NFP/silhouette lane per `arrangement.md:470`; Compute's circulation planar side is the second downstream consumer, `RASM-CS-COMPUTE-BRIEF.md` `[V12]`a); its kernel-tier catalog RE-HOMES with the pin — and the re-home is a RECONCILE, never a blind move: `libs/csharp/Rasm.Fabrication/.api/api-clipper2.md` already exists on disk, so the kernel copy's real rows fold into that standing catalog and the kernel file deletes (one tier, one file) — a kernel-tier catalog for a package the kernel manifest no longer references is an orphan by this section's own hygiene law, and the substrate-canonical authoring is the Compute `[V15]` motion (later campaigns verify, never re-author); its `JoinType`×`EndType` vocabulary seeds the `[V10]` row shape.
- REMOVE with catalogs: LibTessDotNet (redundancy proof named, never a bare zero-consumer strike — the exact CDT owner `delaunay.md` subsumes the polygon-fill concern its catalog claimed), geometry3Sharp (abandoned upstream; the Boost successor geometry4Sharp is itself a single 2022 release — the conditional stands on that record), SharpVoronoiLib, Triangle (latest release IS the 2016 beta — feed-verified, dead ten years), MIConvexHull (post-`[V11]` hull fold); the seven orphaned `.api` kernel-consumer claims die with them or rewrite to real downstream owners (Materials `InteractionDiagram` for hull/CDT if that package ever admits them itself); the REMOVE is kernel-scoped for the two with a standing downstream consumer — `SharpVoronoiLib`'s and `geometry3Sharp`'s central pins re-group to the Fabrication label rather than die (Fabrication's Voronoi CAM lane and posting biarc rail are the live consumers; final disposition is `RASM-CS-FABRICATION-BRIEF.md` `[04]`'s) — and "with catalogs" is kernel-tier-scoped for those two as well: the standing `Rasm.Fabrication/.api` copies (`api-sharpvoronoilib.md`, `api-geometry3sharp.md`; a Bim-tier geometry3Sharp copy also lives, that campaign's concern) survive as the pin's folder-tier catalogs, only the kernel-tier files delete.
- Catalog hygiene: the `[ROSTER_RECONCILIATION]` api-hashing correction + five-catalog dedupe (verified: csparse/mathnet-providers/tensors byte-identical, hashing/mathnet-numerics divergent) + LanguageExt authoring; `api-rhino.md:134`'s claim that repair composes Rhino heal members corrects (repair author-kernels); `api-mathnet-providers` gains the osx-arm64 managed-fallback fact as its headline.
- Judged shared-tier stubs, NOT mined kernel-side (recorded so silence is never mistaken for oversight): `QuikGraph` — kernel graph results (skeleton/medial topology `[V10]`d, slice nesting forests `[V10]`b, naming migration structure) emit as kernel-owned SoA wires; QuikGraph is the DOWNSTREAM view substrate (`api-quikgraph.md:141` Persistence topology lane; `:160` rejects the geometric broad-phase concern outright) and the kernel composes none of it. `Generator.Equals` — Thinktecture owns kernel value identity; the structured-diff surface is the seam's (`api-generator-equals.md:3`). `Riok.Mapperly` — kernel wires are columnar span transcription, never object mapping; Mapperly owns the Element seam↔wire rail (`api-mapperly.md:3`). `UnitsNet` — kernel angles stay raw radian `double`; `Angle` is the AppUi↔kernel boundary bridge (`api-unitsnet.md:184`), never a kernel interior type. The transport/app set (grpc ×7, protobuf, nodatime ×3, messagepack + thinktecture-json/messagepack adapters, hybrid-cache, jsonpatch, redaction, system-configuration, extensions-ai, unicolour) carries no geometry-plane member — zero mine obligation.
- Stratum-split records (like the Alimer/Compute split — never mistaken for kernel candidates or dead weight): PicoGK (`PROPS:85`, Apache-2.0, active) is Fabrication's voxel/implicit lane — never the kernel's second SDF owner (the `[V8]` one-distance-field-lane ruling) and never its slicer; OcctNet.Wrapper (`PROPS:84`) is Fabrication's own pin — the OCCT rejection below is KERNEL-scoped, the manifest does not contradict it.
- Named-and-rejected (never re-proposed): SISL, TetGen, CGAL, SolveSpace, CuraEngine/Slic3r, Instant Meshes (repo license NOASSERTION AND dormant since 2022 — both facts repo-verified)/NeurCross, MCUT, OCCT/LNLib (LGPL, host-overlap — the kernel meets Rhino at the wire); MeshLib (the most capable maintained managed mesh-processing binding, feed-fresh 2026-06 — REJECTED on the license gate: custom free-non-commercial/paid-commercial); rhino3dm (MIT, active — representation/IO only, the algorithm surface lives host-side or in Rhino.Compute; GShark stays the host-neutral ALGORITHM owner); SkeletonNet/CGAL_StraightSkeleton_Dotnet (unmaintained float port / GPL wrapper — dominated by the author-kernel exact wavefront); OpenSubdiv admissible ONLY on proven limit-surface-at-scale demand, and no managed binding exists — admission means an in-house P/Invoke, the Manifold class of burden; V-HACD/CoACD (BSD/MIT convex decomposition) recorded as an admissible future arrangement-adjacent owner, deferred until a consumer names it.
- TASKLOG absorption: `[T-BOOLEAN-NATIVE-ASSET]` resolves into `[V5]` (the DECISION disposes the card); `IDEAS.md` carries no open geometry cards — the DECISION's growth rows seed it fresh.

## [05]-[BUILD_LEGS]

Phase 2 runs the durable rebuild engine per leg with `RASM-CS-GEOMETRY-DECISION.md` as brief. The DECISION owns the exact partition, proven against the post-campaign seam graph (arrow = composes; every edge lands in an equal-or-earlier wave, and an intra-wave edge points backward in the leg's listed page order — the lists below ARE dependency order, never prose order): predicates+faults ← every owner; substrate `[V6]` ← intersect/arrangement/view/decimate/receipts/repair/remesh/slice/skeleton; index ← arrangement (broad-phase); naming+reconciliation ← pack (`Encode`); delaunay ← arrangement (`Tessellation.Build`), offset (dual, `[V10]`c), repair (CDT re-mesh, `[V9]`); intersect ← arrangement/offset/repair (`[V4]`), slice (`[V10]`b), view (Section, `[V3]`); arrangement ← repair (booleans — never the reverse post-`[V5]`/`[V6]`); solver ← fit/decimate (`[V7]`); flatten ← develop (`[V2]`); remesh ← panelize (`[V2]`/`[V11]`). The sequencing law it must honor:

1. **FLOOR + CONTRACTS** — Numerics (predicates hardening, faults re-type + band plan), the `[V1]` structure ratification landing corpus-wide (namespaces, codemap, cluster taxonomy), Spatial (`[V8]` contract fixes, kernel acceleration wire, naming/reconciliation bug fixes + fixture re-proof), the `[V6]` mesh substrate extraction. The FULL roster reconciliation (purge/re-scope/admit/manifest/`.api`) lands HERE — it gates every later page's imports and catalog claims.
2. **MESHING LATTICE** — delaunay (stub bodies, hull projection, dual accessor), intersect (consumer collapse landed), arrangement (`BooleanOp` home, implicit constraint ruling, Manifold gate, cycle break), offset (`[V10]` a+c — corner strategies, medial — plus the `[SEAM_AND_RAIL_LAW]` store sizing; slice is `[V10]`b, wave 4).
3. **PROCESSING** — solver+fit (`[V7]` one LM), repair+receipts (`[V9]` fixes over the wave-2 lattice), decimate, flatten.
4. **NEW MESH OWNERS + PARAMETRIC TIER + DRAWING** — first the `[V10]`-b/d + `[V11]` slice/skeleton/remesh owners (over wave-2 intersect/offset and the wave-1 substrate), then the `[V2]` surface-development pages (panelize composes remesh; develop composes wave-3 flatten; all compose the Vectors substrate), with view (`[V3]` rebuild over wave-2 intersect + wave-1 index) and pack (`[V13]` over wave-1 reconciliation) independent of the parametric tier and parallelizable beside it.

Per-leg closeout: hard residuals resolve before the next leg launches; `ARCHITECTURE.md`/`README.md`/`TASKLOG.md` close in the leg that changes their facts; `.api` stubs and manifest edits land in wave 1 with the roster reconciliation, and the leg that first composes an admitted package deepens its stub; cross-package counterpart ledger edits (`FAB:22,33,44,46,47`, `COMPUTE:120,121`, `Rasm.Persistence/ARCHITECTURE.md:55`) land as 1-hop ripples with the leg that changes the seam (waves 1, 2, and 4). Residuals naming a later leg's pages transfer forward explicitly in the leg return, never silently. Campaign acceptance closes on the consumers, not the ledger: after wave 4, (a) a Generation SpineRef dry-run — the spine RESOLVED by content key through the `[V2]` parametric-identity seam, arc-length stations carrying rotation-minimizing frames over the `[T0,T1]` window, geodesic + isoline families on a surface pulled back to UV, one subdivision pass, one panelized region, one pattern-mapped instance stream with per-instance frames — composes entirely from rebuilt fences; (b) a Fabrication toolpath dry-run — corner-strategy offset family, a slice stack with oriented contours + nesting tree, a medial-axis clearance-RADIUS read, a distance-field read through the `[V8]` lane — composes from the kernel seams with zero Fabrication-side geometry re-implementation; and (c) a documentation dry-run — plan + section HLR over a healed model emitting connectivity-chained visible/hidden/silhouette/section polylines with zero sampling misses (the E1 failure class) — composes view+intersect+index, feeding the `FAB:46` `HiddenLineResult` route. A dry-run that reaches for a missing owner reopens `[V2]`/`[V3]`/`[V10]` as residuals.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Vectors` interiors (mature, no `.planning`): the `SampleDetailed`/`TopologyDetailed`-projection exposures are named seam obligations verified by `assay api`, never a Vectors rebuild.
- Fabrication interiors: kinematics, G-code posting, nesting/cutting-stock, CAM material-removal simulation, adaptive-clearing STRATEGY (the kernel supplies medial/offset/slice substrate; pocketing strategy is Fabrication's); its duplicate-owner deletions are 1-hop ledger ripples only.
- Rhino host parametric: `Analysis/Intersect.cs` NURBS/Brep/Make2D stays host-side per the runtime-split law; no thinning, no SSI migration into the host lane.
- Compute interiors: meshlet/vertex-cache/encode (meshoptimizer is Compute's), GPU clash traversal, the LM/tensor rails; Compute consumes the kernel wire.
- IFC semantics (`Rasm.Bim`), Element/Persistence interiors — seam counterparts only, one anchor each.
- `Rasm.Generation` surfaces: the spec composes the kernel; nothing here reaches up; form-finding/dynamic-relaxation solvers are flagged for the Generation campaign, not built here.
- The federation-wide fault-band renumber beyond the geometry century — escalation path only if `[V12]`'s ruled cluster map cannot fit 2400-2449.
