# [RASM_ELEMENT_RULINGS]

`Rasm.Element` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- Domain packages do not land here — provider codecs live in the AEC peers that project onto this graph, so the contract stays provider-free.

## [02]-[SHAPE]

- `AppearanceSummary` freezes its seven-value preimage — a richer fact rides the `DetailSchema.Appearance` bag; an eighth column re-keys every node.
- Float-bearing members refuse `[PrecisionEquality]` — the generator drops them from `GetHashCode`, and every double is quantized or key preimage.
- Strings `CanonicalWriter` writes verbatim take `[StringEquality(StringComparison.Ordinal)]` — a comparer edit cannot fork equality from identity.
- Imported `Pset` rows land in a neutral `PROPERTY_BAG` node under `EvidenceGrade.Import` — an Option-widened FULL-VECTOR column reads as measured.
- `ValueBag` rows carry `QuantityType` identity wherever every producer names it truthfully — `OfSi(Dimension, double)` mints the unspellable alone.
- Shared `Vector3` owns domain math and BCL `Matrix4x4`/`Vector3` stay LAYOUT carriers — a BCL carrier in domain position escapes the tolerance reads.
- `Object.Placement` is EXCLUDED from the canonical-bytes preimage — folding it re-keys every node on a rigid move and kills the diff's `Moved` arm.
- Provider sentinels never become shared column types — past admission a sentinel reads as measured, so `Option<T>` takes it at the projector read.
- `Federate` takes ONE coordination `Header` and refuses divergence — measures mint at the SOURCE tolerance and no union re-quantizes them.
- Rooted occurrence ids colliding across sources FAULT, never rename — content- and type-derived ids merge on equal payloads and fault on unequal.
- Admission slots return the concrete `Validation<Error, _>` — C# bars interface targets from user-defined conversions, so the lift compiles nowhere.
- `SectionProperties` is the ONE cross-section algebra — a mm-basis carrier admits through `OfMillimetres`, so no column crosses unconverted.
- `PropertyEvidence` + `EvidenceGrade` are the ONE evidence carrier and rank — the int key IS the precedence `ValueBag.Merge` reads.
- `UnitScheme` carries the dimension-axis regime and display override — a peer-minted scheme forks the display read, so none survives.
- `Coerce`'s declared tail is the ONE per-value unit override, ingress-only — a boundary applying `(native + Offset) × Factor` by hand is deleted.
- `Predicate<TLeaf>` is the ONE boolean selection closure — a stored `Func<…, bool>` query filter is the deleted form wherever this shared reaches.
- `Classification` hierarchy is bSDD-resolved at `Rasm.Bim`; the contract carries identity, never ancestry — a crosswalk owner here is the deleted form.
- `MeasureStat` pairs kernel `Stat<Scalar>` with a `QuantitySignature` — a moments fold over `MeasureValue` drops the signature the pair carries.
- `ResolveRepresentation` is the ONE content-key→geometry resolver the contract publishes; a same-named narrower twin composes or renames.
- `[ValidationError]`-stamped owners admit through `FactoryBridge.Accept<TVO>` — a generic-token wrapper is deleted.
- By-column roster resolves compose kernel `FactoryBridge.Row` with the comparer stated on site and preserve its kernel read refusal.
- `TableDeclaration` is the ONE dataset self-description and `TableBatch` keys on it — a foreign producer instantiates the carrier, never a row.
- Contract value-space compares read a kernel `Tolerance` minted on the `Relative` lane — a bare tolerance double beside the contract is the deleted form.
- Snapshot memos are cache mechanics, never domain facts — observability lives at the consumer's cache, a latency series as a caller `GaugedSpan`.
- Kernel `AdmissionSlots` owns reusable accumulation and scalar bands; Element supplies typed semantic refusals through its deferred gate.
- `Gate(holds, concern, detail, refuse)` is the ONE refusal door, constructed on the failing arm — a pre-built fault or per-plane wrapper is deleted.
- `GraphMembers.Advance` returns `Refold` only for a valid grid change — malformed deltas and absent members remain failures on its `Fin` result.
- Every 16-byte key column composes kernel `ContentHash.Wire`/`Admit` — a contract-local `BinaryPrimitives` spelling forks the byte order.

## [03]-[COLLAPSE]

- (none)

## [04]-[STRUCTURE]

- `Rasm.Element.Classification.Classification` keeps type-equals-namespace-segment; a consumer-side `using` alias resolves it in one hop.
- Wire-unfreeze events do NOT re-enter — the family reshapes in place with every decoder regenerated; a new arm lands with its shared feature.
- `WireCodec` is ONE `[Mapper]` partial across the `Graph/` wire pages — the attribute, parity fold, key codecs, and shared gates ride `Graph/wire`.
- `BoundaryConverters`-shaped peer reach resolves through `Graph/wire`'s `[UserMapping]` set — a copied converter forks the crossing's one spelling.
- Wire-plane pages carry the `wire` prefix their fence carries — a bare `payload` page name hides the codec family and forks the router pairing.

## [05]-[PROCESS]

- Generated support-family union changes land their corpus arm, `Graph/wire` census, and transcription in one edit; the corpus proto numbers scalars.
- Node encode and decode share one `Fin` result; a product-only fact the generated payload closure cannot carry refuses before lowering.
- `GraphReflection` roots one Celly evaluator for `NodeWire`; local gates add only domain key-space narrowing and never restate corpus rules.
