id: no-filter-over-lifted-value
snapshots:
  import * as E from "effect/Effect"; const value = E.succeed(input).pipe(E.filterOrFail(predicate, failure), E.map(render));:
    fixed: import * as E from "effect/Effect"; const value = E.liftPredicate(input, predicate, failure).pipe(E.map(render));
    labels:
    - source: E.succeed(input).pipe(E.filterOrFail(predicate, failure), E.map(render))
      style: primary
      start: 50
      end: 122
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = Effect.succeed(report).pipe(Effect.filterOrFail((held) => held.ok, () => error));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Effect.liftPredicate(report, (held) => held.ok, () => error);
    labels:
    - source: Effect.succeed(report).pipe(Effect.filterOrFail((held) => held.ok, () => error))
      style: primary
      start: 71
      end: 151
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = Effect.succeed(report).pipe(Effect.filterOrFail((held) => held.ok, () => error), Effect.map(summarize));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Effect.liftPredicate(report, (held) => held.ok, () => error).pipe(Effect.map(summarize));
    labels:
    - source: Effect.succeed(report).pipe(Effect.filterOrFail((held) => held.ok, () => error), Effect.map(summarize))
      style: primary
      start: 71
      end: 174
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = Either.filterOrLeft(Either.right(report), (held) => held.ok, () => left);
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Either.liftPredicate(report, (held) => held.ok, () => left);
    labels:
    - source: Either.filterOrLeft(Either.right(report), (held) => held.ok, () => left)
      style: primary
      start: 71
      end: 143
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = Either.right(report).pipe(Either.filterOrLeft((held) => held.ok, () => left));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Either.liftPredicate(report, (held) => held.ok, () => left);
    labels:
    - source: Either.right(report).pipe(Either.filterOrLeft((held) => held.ok, () => left))
      style: primary
      start: 71
      end: 148
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = Either.right(report).pipe(Either.filterOrLeft((held) => held.ok, () => left), Either.map(summarize));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Either.liftPredicate(report, (held) => held.ok, () => left).pipe(Either.map(summarize));
    labels:
    - source: Either.right(report).pipe(Either.filterOrLeft((held) => held.ok, () => left), Either.map(summarize))
      style: primary
      start: 71
      end: 171
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(Effect.succeed(report), Effect.filterOrFail((held) => held.ok, () => error));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Effect.liftPredicate(report, (held) => held.ok, () => error);
    labels:
    - source: pipe(Effect.succeed(report), Effect.filterOrFail((held) => held.ok, () => error))
      style: primary
      start: 71
      end: 152
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(Effect.succeed(report), Effect.filterOrFail((held) => held.ok, () => error), Effect.map(summarize));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(report, Effect.liftPredicate((held) => held.ok, () => error), Effect.map(summarize));
    labels:
    - source: pipe(Effect.succeed(report), Effect.filterOrFail((held) => held.ok, () => error), Effect.map(summarize))
      style: primary
      start: 71
      end: 175
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(Either.right(report), Either.filterOrLeft((held) => held.ok, () => left));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = Either.liftPredicate(report, (held) => held.ok, () => left);
    labels:
    - source: pipe(Either.right(report), Either.filterOrLeft((held) => held.ok, () => left))
      style: primary
      start: 71
      end: 149
  ? import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(Either.right(report), Either.filterOrLeft((held) => held.ok, () => left), Either.map(summarize));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const checked = pipe(report, Either.liftPredicate((held) => held.ok, () => left), Either.map(summarize));
    labels:
    - source: pipe(Either.right(report), Either.filterOrLeft((held) => held.ok, () => left), Either.map(summarize))
      style: primary
      start: 71
      end: 172
  import { Effect, Either, Option, pipe } from "effect"; const named = Option.filter(Option.some(name), String.isNonEmpty);:
    fixed: import { Effect, Either, Option, pipe } from "effect"; const named = Option.liftPredicate(name, String.isNonEmpty);
    labels:
    - source: Option.filter(Option.some(name), String.isNonEmpty)
      style: primary
      start: 69
      end: 120
  import { Effect, Either, Option, pipe } from "effect"; const named = Option.some(name).pipe(Option.filter(String.isNonEmpty));:
    fixed: import { Effect, Either, Option, pipe } from "effect"; const named = Option.liftPredicate(name, String.isNonEmpty);
    labels:
    - source: Option.some(name).pipe(Option.filter(String.isNonEmpty))
      style: primary
      start: 69
      end: 125
  ? import { Effect, Either, Option, pipe } from "effect"; const named = Option.some(name).pipe(Option.filter(String.isNonEmpty), Option.map(String.trim));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const named = Option.liftPredicate(name, String.isNonEmpty).pipe(Option.map(String.trim));
    labels:
    - source: Option.some(name).pipe(Option.filter(String.isNonEmpty), Option.map(String.trim))
      style: primary
      start: 69
      end: 150
  import { Effect, Either, Option, pipe } from "effect"; const named = pipe(Option.some(name), Option.filter(String.isNonEmpty));:
    fixed: import { Effect, Either, Option, pipe } from "effect"; const named = Option.liftPredicate(name, String.isNonEmpty);
    labels:
    - source: pipe(Option.some(name), Option.filter(String.isNonEmpty))
      style: primary
      start: 69
      end: 126
  ? import { Effect, Either, Option, pipe } from "effect"; const named = pipe(Option.some(name), Option.filter(String.isNonEmpty), Option.map(String.trim));
  : fixed: import { Effect, Either, Option, pipe } from "effect"; const named = pipe(name, Option.liftPredicate(String.isNonEmpty), Option.map(String.trim));
    labels:
    - source: pipe(Option.some(name), Option.filter(String.isNonEmpty), Option.map(String.trim))
      style: primary
      start: 69
      end: 151
  ? import { Effect, Either, Option, pipe } from 'effect'; const checked = Effect.filterOrFail(Effect.succeed(report), (held) => held.verdict !== 'regression', () => error);
  : fixed: import { Effect, Either, Option, pipe } from 'effect'; const checked = Effect.liftPredicate(report, (held) => held.verdict !== 'regression', () => error);
    labels:
    - source: Effect.filterOrFail(Effect.succeed(report), (held) => held.verdict !== 'regression', () => error)
      style: primary
      start: 71
      end: 168
  ? import { Either as E, pipe as flow } from "effect/index"; const value = flow(E.right(input), E.filterOrLeft(predicate, failure));
  : fixed: import { Either as E, pipe as flow } from "effect/index"; const value = E.liftPredicate(input, predicate, failure);
    labels:
    - source: flow(E.right(input), E.filterOrLeft(predicate, failure))
      style: primary
      start: 72
      end: 128
  ? import { Either, pipe } from "effect"; const value = pipe(Either.right(input), Either.filterOrLeft(predicate, failure), factory());
  : fixed: import { Either, pipe } from "effect"; const value = pipe(input, Either.liftPredicate(predicate, failure), factory());
    labels:
    - source: pipe(Either.right(input), Either.filterOrLeft(predicate, failure), factory())
      style: primary
      start: 53
      end: 130
  import { Option as O } from "effect"; const value = O.filter(O.some(input), predicate);:
    fixed: import { Option as O } from "effect"; const value = O.liftPredicate(input, predicate);
    labels:
    - source: O.filter(O.some(input), predicate)
      style: primary
      start: 52
      end: 86
  ? import { Option } from "effect"; const named = Option.some(name).pipe(Option.filter(String.isNonEmpty), Option.map(String.trim), Option.getOrElse(() => ""));
  : fixed: import { Option } from "effect"; const named = Option.liftPredicate(name, String.isNonEmpty).pipe(Option.map(String.trim), Option.getOrElse(() => ""));
    labels:
    - source: Option.some(name).pipe(Option.filter(String.isNonEmpty), Option.map(String.trim), Option.getOrElse(() => ""))
      style: primary
      start: 47
      end: 156
  import { Option } from "effect"; const value = Option.some(input).pipe(Option.filter(predicate), factory());:
    fixed: import { Option } from "effect"; const value = Option.liftPredicate(input, predicate).pipe(factory());
    labels:
    - source: Option.some(input).pipe(Option.filter(predicate), factory())
      style: primary
      start: 47
      end: 107
  ? import { Option } from "effect"; import { pipe as flow } from "effect/Function"; const value = flow(Option.some(input), Option.filter(predicate), factory());
  : fixed: import { Option } from "effect"; import { pipe as flow } from "effect/Function"; const value = flow(input, Option.liftPredicate(predicate), factory());
    labels:
    - source: flow(Option.some(input), Option.filter(predicate), factory())
      style: primary
      start: 95
      end: 156
  import { Option, pipe } from "effect"; const value = pipe(Option.some(input), Option.filter(predicate), factory());:
    fixed: import { Option, pipe } from "effect"; const value = pipe(input, Option.liftPredicate(predicate), factory());
    labels:
    - source: pipe(Option.some(input), Option.filter(predicate), factory())
      style: primary
      start: 53
      end: 114
