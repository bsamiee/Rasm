# [RASM_RULINGS]

Per-folder decision registry for the geometry kernel — the settled rulings agents re-litigate for lack of a home. A row is one decision with the one why that guards its re-litigation: a package admission or rejection, an owner-choice discriminant, a collapse refusal naming the guarantee the denser form loses, a structure retirement, or a folder-learned working law. Append-mostly; a row earns its seat while its why stays homeless, and dies only when fact and why both live at one durable surface.

## [01]-[PACKAGES]

- QuikGraph KEEP — do NOT re-reject as "unmaintained": MS-PL, the dominant graph library with no maintained successor, algorithmically complete for topo-sort/BFS/DAG; "unmaintained" is chronology-only, not a capability gap. Its core package alone suffices — no companions; reopens only on a maintained successor owning the same algorithm set under the license gate.
- Kernel admits only pure-managed, no-native-asset leaves — a package carrying a native/RID runtime asset or a host-engine binding homes at its consuming sibling, never `Rasm.csproj`; the discriminant is managed purity, not geometry domain, so a managed leaf still enters only on a live kernel consumer.
- geometry3Sharp and the MathNet MKL/OpenBLAS providers are NOT kernel references — those pins carry sibling debt for the L2/L3 consumers (geometry3Sharp co-consumed by Bim/Fabrication, the native providers by the compute lane), so a kernel author reaching for geometry3Sharp's DDG surface is re-adding sibling debt.
- Triangle.NET and TetGen REJECTED — capability decides: Triangle.NET is float-epsilon with no exact in-circum, no constrained recovery over constructed points, and no defining-entity carriage, re-opening the rounded-coordinate class `Tessellation` closes; TetGen enters only as a native binding, failing the managed-purity admission. "Admit Triangle.NET because it is MIT" is the recurring move. Reopens on a managed engine exposing exact CDT with `Implicit` vertices under the license gate.
- `Predicate` authors its exact-sign floor in-repo and admits no external predicate package — no published library carries BOTH the adaptive-precision exact-sign guarantee and the constructed-point family, and the float-epsilon geometry packages guarantee no exact sign at all; re-searching for a predicate package to skip the adaptive ladder is the recurring move, the route staying the in-repo ladder. Reopens only on a package shipping the exact-sign guarantee with the implicit-point family together under the license gate.
- Vendored G-Shark is the kernel's ONE NURBS engine — upstream walls surface construction and surface derivatives behind internal access, admitting no in-envelope closure, so owning the whole algorithm set is the only route to public arbitrary-knot construction and raw rational surface derivatives; a NURBS package re-added beside the source, or a parallel basis/De Boor kernel on a sibling page, re-litigates it, and new evaluation lands as a `NurbsForm` member. Reopens on a package exposing both.
- OpenSubdiv is REJECTED — no managed binding exists, so admission buys P/Invoke native-asset debt against the managed-purity gate, and the `SubdivisionScheme` Stam eigen lane over the complex-general EVD already evaluates the limit surface at arbitrary `(u, v)`; a limit-evaluation scale sweep re-proposes admission, the route extending the Stam lane. Reopens on a managed binding under the license gate, or on measured throughput the Stam lane cannot meet.

## [02]-[SHAPE]

- One synchronous hook capsule, not two shapes — discriminant: the kernel `Op` is a value object with a synchronous catch funnel, not an effect monad, so a second effect-threaded capsule type (the AppHost `IO`-shaped fire) is the rejected form; an effect-rail caller lifts `Fire` at its own composition seam instead.
- `SampleMoment.UpperCovariance` and `SymmetricMatrix.Upper` share one packed-upper layout contract — identical index arithmetic lets `CloudKernel.CovarianceOf` hand the moment triangle straight into `SymmetricMatrix.Of` with no repack; nothing types an `Arr<double>` layout, so a single-owner "simplification" or a defensive repack at the handoff is the move that corrupts every spectral consumer type-invisibly, and the layout changes only at both owners in one pass.
- Packed-upper triangular addressing has ONE arithmetic mint — `SymmetricMatrix.FlatIndex` promotes to `internal` and every packed-upper producer (`SampleMoment`'s indexer, `Lm.PackedIndex`) delegates to it; a hand-copied index formula is the drift the mirror-law prose names without enforcing, and the `Lm` copy retains only its checked `long` widening at the call boundary.
- `Analysis/relations` homes host-native intersection and `Meshing/intersect` the predicate-exact robust counterpart — discriminant: the consumer's tolerance source, a host consumer needing the model's own tolerance semantics against a robust consumer needing no `RhinoDoc` calibration; a robust re-mint inside `Analysis/relations` or a host-native mint inside `Meshing/intersect` is the cross-altitude leak, and a consumer routes to the owning altitude instead.
- `Deterministic` is the kernel's ONE splitmix64 owner — its finalizer is unreachable outside the owner, so the matrix eigensolver's basis streams and the sampler's coordinate keys derive from its members; a consumer minting a local splitmix64, an ad-hoc coordinate mix, or a `System.Random` draw is the reproducibility drift the collapse already cured, and a new draw shape lands as one member here composing `Advance`/`OrderKey`.
- `SampleMoment` homes at `Domain/stats` as the kernel's one weighted covariance and moment owner, not a per-consumer accumulation — discriminant: single-owner covariance, with `Spatial/cloud`, `Analysis/select`, and `Solving/fit` composing it upstream of their own boundaries; a covariance re-mint beside a consumer — the move a locality or dedup sweep re-proposes — forks the moment algebra into two drifting owners.
- Every mutable geometry store homes as a single-writer publish-by-freeze arena, never an immutable-record store — discriminant: build state mutates pooled columns in place and becomes composable truth only at the freeze seam, so a copy-per-op `with` record re-pays a full-store copy per operation and forks hash-eligibility; a store page labeling itself immutable while mutating in place is the recurring move, and every store composes the arena contract `Meshing/edit` owns.
- `IntersectOp` is the kernel's ONE crossing kernel — every crossing consumer routes `Intersection.Apply`, because a page-local straddle test re-derives the exact-predicate floor and forks the `CrossKey` cross-face merge every chain keys on; a "local crossing helper" re-proposed under a folding or convenience demand lands as one `IntersectOp` case folded by `Apply`. Reopens only where a crossing modality cannot express as an `IntersectOp` case.
- `ClearanceNode` is the one clearance carrier across the 2D medial and the 3D curve skeleton, not a per-consumer clearance record — `Meshing/offset` mints the family (position, boundary-distance radius, nearest-feature witness) and `Meshing/skeleton` composes it verbatim; a radius-less medial node or a sibling 3D clearance shape forks the payload so toolpath and offset consumers read divergent clearance vocabularies across dimensionalities, and a new clearance fact lands as one column on the family.
- `MatrixKernel` homes the kernel's ONLY MathNet and CSparse access path — every numeric consumer composes the `Matrix`/`SymmetricMatrix`/`SparseMatrix` owners rather than reaching a provider factorization directly, because a raw factorization bypasses the typed-receipt evidence and the recomputed-residual witness and returns an unvalidated result; a direct reach re-homes on the owning model member. Reopens only where a sibling needs a MathNet surface no `Matrix` owner exposes, which extends the owner rather than bypassing it.
- Flattening homes at three tiers by distortion grade — discriminant: fabrication material tolerance; `Processing/flatten` owns the low-distortion variational parameterization, `Processing/segment` the host-capture LSCM tier with its edge-length witness, `Parametric/develop` the exact-isometry strips proven by the `ddouble` witness; a distortion-minimizing solve re-derived at the isometry tier ships a non-isometric strip as isometric, and a low-distortion need routes to `Processing/flatten`.
- `Parametric/curve` and `Parametric/locate` split by RUNTIME, never by capability — division, closest-point, and arc-length live in both by decision, meeting only at the encoded wire; discriminant: the target runtime, the host-neutral vendored engine against the live RhinoCommon host, so a dedup sweep folding the two into one owner erases the split every cross-runtime consumer needs, and a shared concept re-homes as one owner per runtime.
- `SimplifyKind.VoxelRemesh` stays a decimate modality, never a `Processing/remesh` arm — discriminant: the voxel lane is budgeted volumetric REDUCTION whose output genus follows the extracted level set, while `RemeshOp` rewrites an already-valid mesh toward an edge-length or cross-field sampling target at preserved topology; a dedup sweep reading both as "remeshing" folds them into one rewriter and drops the face-budget contract on one side and the target-sampling contract on the other, so a new resample-first modality lands as a `SimplifyKind` row and a new sampling target as a `RemeshOp` case.
- `RemeshOp` homes beside the host-capture tier, never merged into it — discriminant: a predicate-gated pipeline and a non-Rhino runtime demand the first-principles rewrite `Processing/remesh` owns, while capturing RhinoCommon parameters into echo receipts is `Processing/segment`'s `RemeshKind`/`QuadTarget` concern; a dedup sweep re-proposes folding the two pages or reaching native `QuadRemesh`/`Reduce` inside the author kernel, and a host-parameter need lands as one `RemeshKind` row at the capture tier. Reopens only when RhinoCommon exposes the structural predicates the author kernel gates on.
- Every `ILmModel` Jacobian arm stays closed-form — finite differences halve the precision the 106-bit `ddouble` objective buys and cost one residual evaluation per parameter per row, so `FitPrimitive.Gradient` and `Constraint.Residual` carry analytic partials and the FD form lives only as test oracle; FD offered as the fallback for a new residual arm is the recurring move, the route being one closed-form arm on the owning `[Union]` fold. Reopens for a residual with no closed-form derivative.
- `Lm.Minimize` is the kernel's ONE damped Gauss-Newton iterate — every `ILmModel` composes the same accept/reject λ-ladder, the `SolveReceipt` all-finite step gate, the `ddouble` accept test, and one attempt budget, so a page re-rolling a private iterate/step/λ loop forks the singular guard, the accept ordering, and the budget accounting; a fitting, registration, or parameterization page routes its refine through an `ILmModel` conformance rather than its own iterate. Reopens only on a descent regime the `SolvePolicy` λ-ladder and step rows cannot express.
- Hull capability homes at two owners by robustness tier, never by hull shape — `Spatial/cloud`'s rail owns the native-first host and concave kinds it composes from admitted natives while `Meshing/delaunay`'s `LowerHull` owns the predicate-gated exact envelope; a dedup sweep folding them into one owner, or a hand-rolled incremental complex on the cloud page, is the recurring drift, and a robust-grade exact envelope routes to `LowerHull` instead.
- `SpatialIndex` owns primitive-AABB broad phase and `NeighborIndex` owns point-set neighborhoods — discriminant: the query subject, primitive bounding extents against a bare point set; a "dedup the two spatial owners" sweep wraps, seeds, or re-mints either owner inside the other and manufactures two owners for one tier, so primitive overlap and ray broad phase route `SpatialIndex` while point kNN and radius route `NeighborIndex`. Reopens only where the primitive owner must serve bare points carrying no bounding box.

## [03]-[COLLAPSE]

- QuikGraph results leave as frozen SoA columns, never a graph field — `SliceStack` and `CurveSkeleton` columns are the decode contract every consumer binds and `Chain`/`SkeletonArc`/`SkeletonGraph` views project from them on read, while a graph-typed result member re-couples consumers to mutable topology and forfeits decode determinism; a dedup sweep folding the columns back into their graph is the deleted move, the route runs the algorithm on a transient graph and projects columns at extraction.

## [04]-[STRUCTURE]

- Retired source directories do NOT re-enter — the pre-conversion source trees are deleted and their capability lives in the design pages; the archive grounds an old anchor's referent only, never a restore, so a "missing source" gap is filled from the pages, never by re-adding a tree.
- Host-bound factory and route spellings rename only as a paired edit — the `Rasm.Grasshopper` component surface and the Rhino overlay bind the `Measure`/`Bounds` factories, the `AnalysisQuery` routes, and the `IntersectionHit`/`RayQuery` hit spellings by exact name, so a density or clarity pass renaming a factory to a "better" spelling in isolation breaks the live host binding; a rename lands at the kernel owner and the host component surface in one pass.
