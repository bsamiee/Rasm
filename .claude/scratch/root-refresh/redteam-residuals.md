# Geometry-close redteam residuals — Phase B writer input (cross-package obligations, verified)

1. `Rasm.Compute` `Tensor/residency.md:190` + Compute `ARCHITECTURE.md:178` name the kernel entry `Encoding.Apply`; the landed owner is `Encode.Apply` (`Drawing/pack.md`) — compute-brief refresh captures the correction.
2. `Rasm.AppHost` has no `GeometryPacking` capsule page though `pack.md` and Compute cite it — unrealized obligation for the apphost brief.
3. `Rasm.Persistence/Version/merge.md` geometry-axis no longer anchors the reconciliation byte-identity harness — persistence DECISION refresh arbitrates.
4. `.api` gaps: `api-rhino.md` misses the Analysis-consumed surface; `api-languageext.md` misses `WrappedErrorExpectedException` — a focused rebuild-api note, not a root-doc row.
5. `Rasm.Domain.Expected` shadows `LanguageExt.Common.Expected` cross-package under the global using — one disambiguation note wherever a doc designs cross-package `Expected` handling.

# Hygiene-lane structural finding (fabrication, fence-level — Phase B routes to DECISION rows)

6. THREE Fabrication pages author the in-folder solvers their corrected ARCHITECTURE declares dead — prose matches their own fences, so only a fence rebuild resolves it: `Toolpath/skeleton.md` (authors `StraightSkeleton`/`Wavefront`/`SkeletonEvent` medial kernel — must become kernel clearance-family delegation), `Toolpath/slicing.md` (authors `Slicing.Section` planar-section kernel — must become kernel `SliceStack` delegation), `Posting/projection.md` (authors `Hlr.Solve` BSP visibility — must become kernel `DrawingProjection` delegation). Echo: `Fabrication/README.md:11/13/14` router entries + `AppUi/.planning/Render/drafting.md:418` describe the dead mechanisms and converge only with the fence rebuilds. The fabrication DECISION's page rows must command these three rebuilds EXPLICITLY (verify its rows already do; sharpen if they merely imply).

# Lane-2 hygiene structural findings (Phase B routes where a root doc owns the obligation)

7. `Rasm.Persistence/Element/graph.md:222` + `identity.md:280` cite `Rasm.AppHost/Agent/identity#PRINCIPAL` — no such anchor exists on the AppHost page: the apphost brief should mint the sub-anchor obligation, or the persistence DECISION drops the cites to page level.
8. `Rasm.Compute/ARCHITECTURE.md:76` `ProgressStore` is a corpus-wide phantom, possibly a duplicate of the `:73` `ProgressMarkWire` seam — the compute brief rules it.
9. `JsonSchema.Net` is design-cited (`api-pg-server-bgworkers.md:118-122` onto `Store/provisioning#SERVER_EXTENSIONS`) but absent from `Directory.Packages.props` — a real admission gap for the persistence DECISION's roster table (integration-first: admit + catalog, or correct the cite).
10. rebuild-api pass notes (not root-doc rows): duplicate `[INDEX]` rows in `api-grpc-common.md`/`api-microsoftaspnetcoretesthost.md`/`api-onnxruntime.md`; `api-pgvectorscale.md:43` unbalanced backtick; `api-silk-webgpu.md:14` roster incomplete; `api-nts-io.md` `EnforceRfc9746`-vs-"RFC 7946" needs assay decompile; `Runtime/channels.md:484` fence-comment cites `#BCF_API` (real: `BCF_ARCHIVE`); `Model/generative.md:5,7` duplicate section labels.

# Lane-1 findings — future focused passes (NOT Phase B root-doc work; recorded so nothing is lost)

11. Bim delocalization pass (dedicated, post-closeout): 22 `grounds against ELEMENT-REBUILD-PLAN.md §N` citations across 12 Rasm.Bim pages reference a doc retired to gitignored `.archive/` — per-sentence delocalization required, disambiguating plan-`§` from legitimate ACI/ASTM/Eurocode/OGC `§` clauses. Same family: the Materials Component seed pages' `the brief`/`[CONSUMER_RIPPLE_RULES]` lane-law coupling idiom.
12. OPERATOR CONVENTION CALL: the `REALIZED`/`NEXT-CAMPAIGN`/`RESEARCH` terminal-anchor status tags (96 instances, confined to Bim/Element/Materials, zero elsewhere) — keep as a done-vs-deferred convention or strip corpus-wide; a convention ruling, not surgery.
