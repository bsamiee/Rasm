id: result-match-to-map
valid:
    - |
      def owner(names):
          return names.map(lambda text: text.strip())
    - |
      def owner(names, env):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=_):
                  return Ok(env)
    - |
      def owner(names, env):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(tag="ok"):
                  return Ok(env)
    - |
      def owner(names, env):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result():
                  return Ok(env)
    - |
      def owner(first, second):
          match first or second:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text.strip())
    - |
      def owner(names, rid):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=manifest) if rid in manifest.runtimes:
                  return Ok(manifest.runtimes[rid])
    - |
      async def owner(names, fetch):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(await fetch(text))
    - |
      def owner(pair):
          match pair:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=(repo, ref)):
                  return Ok(f"{repo}@{ref}")
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
          return Ok("")
    - |
      def owner(value):
          match value:
              case first.Result(error=failure):
                  return Error(failure)
              case second.Result(ok=text):
                  return Ok(text.strip())
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=Mode.Fast):
                  return Ok(True)
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text, error=_):
                  return Ok(text)
    - |
      match names:
          case Result(tag="error", error=failure):
              return Error(failure)
          case Result(ok=text):
              return Ok(text.strip())
    - |
      def owner(value):
          try:
              match value:
                  case Result(error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      return Ok(text.strip())
          finally:
              observe(locals())
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  # The strip drops the trailing newline
                  return Ok(text.strip())
    - |
      def owner(names, rid):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=manifest) if rid not in manifest.runtimes:
                  return Ok(Unsupported(rid))
              case Result(ok=manifest):
                  return Ok(manifest.runtimes[rid])
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  stripped = text.strip()
                  return Ok(stripped)
    - |
      def owner(names, path):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(load(path))
    - |
      def owner(names, log):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  log(text)
                  return Ok(text.strip())
invalid:
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
          # Every arm returns
    - |
      def owner(names):
          try:
              def inner(value):
                  match value:
                      case Result(tag="error", error=failure):
                          return Error(failure)
                      case Result(ok=text):
                          return Ok(text.strip())
              return names.bind(inner)
          finally:
              observe(locals())
    - |
      async def owner(path, cwd):
          match await run(["otool", "-L", str(path)], cwd, capture=True):
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok([line.split()[0] for line in text.splitlines()[1:] if line.strip()])
    - |
      def owner(names):
          match names:
              case Result(ok=text):
                  return Ok(text.strip())
              case Result(tag="error", error=failure):
                  return Error(failure)
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(tag="ok", ok=text):
                  return Ok(text.strip())
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(parse(text))
    - |
      def owner(self, names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(self.parse(text))
    - |
      def owner(names, path):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(parse(text, path))
    - |
      def owner(names):
          match names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
    - |
      async def owner(path):
          match await run(path):
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text)
    - |
      def owner(self):
          match self.names:
              case Result(error=failure):
                  return Result.Error(failure)
              case Result(ok=text):
                  return Result.Ok(text)
    - |
      def owner(names):
          match names:
              case Result(error=failure, tag="error"):
                  return Result.Error(failure)
              case Result(ok=text):
                  return Result.Ok(text.strip())
    - |
      def owner(names):
          match names:
              case result.Result(tag="error", error=failure):
                  return Error(failure)
              case result.Result(ok=text):
                  return Ok(text.strip())
    - |
      def owner(names, flag):
          if flag:
              match names:
                  case Result(tag="error", error=failure):
                      return Error(failure)
                  case Result(ok=text):
                      return Ok(text)
          return Ok("")
    - |
      def owner(self):
          match self.names:
              case Result(tag="error", error=failure):
                  return Error(failure)
              case Result(ok=text):
                  return Ok(text.strip())
