# F10 review: `10-discriminated-unions.md`

Fabrication check clean. Every member named exists with the stated signature: `OptionT.lift<M, A>(K<M, Option<A>>)` (`OptionT.txt:71`), `OptionT<M, A>.Match<B>(Func<A, B>, Func<B>) : K<M, B>` (`OptionT_2.txt:29`), `Prelude.Optional<A>(A?) : Option<A>` (`Prelude.txt:7333`), `IO.lift(Action) : IO<Unit>` (`IO.txt:86`), the instance `IO<A>.Catch(Func<Error, bool>, Func<Error, K<IO, A>>) : IO<A>` (`IO_1.txt:736`), `Prelude.parseInt(string?) : Option<int>` (`Prelude.txt:12136`), `Option<A>.Match<B>(Func<A, B>, Func<B>)` (`Option_1.txt:907`). The closedness claim also holds: `Option<A>` is a `readonly struct` and `Fin<A>` is an abstract class whose only constructor is `private Fin()` with sealed nested `Succ`/`Fail`, so no caller adds a case. All eight `csharp` blocks appear verbatim in `.scratch/lab-functional-csharp/F10/` modulo the `namespace` line (mechanical diff, zero misses). No contradiction with `21-languageext-result-types.md`: both name `OptionT.lift<IO, Person>(IO.lift(...))` and `Optional` as the null boundary. `RunSafe` appears in prose only and is placed at the host. No banned token, no cross-file reference, no nested `IO<Option<`/`IO<Fin<`, 178 lines.

1. **Location** `.scratch/functional-csharp/10-discriminated-unions.md:129`
   **Claim** "A delegate is the default shape for a one-operation dependency; an interface remains appropriate as a common contract for genuinely distinct implementations."
   **Evidence** Added prose. It offers an alternative ("an interface remains appropriate"), which the brief forbids in a file that shows one path per concern; it carries the `-ly` adverb "genuinely" and a semicolon, both barred in added text. `NOTES/F10.md` records this fact as "one clause" in the numbered list; it stands as two independent clauses.
   **Correction** Replace the second sentence with one clause on the shape the file uses: `1. Keep console access behind a narrow dependency, so the uncontrolled console can be replaced by a controlled implementation. A delegate carries a one-operation dependency.`

2. **Location** `10-discriminated-unions.md:33`
   **Claim** "This approximates a discriminated union in C#, although the abstract base and variant declarations require more ceremony than a native union definition."
   **Evidence** Both halves already stand in the file. The opening section states C# "can approximate them with: an abstract record representing the union; one sealed record per case". The design rules state "Expect a C# tradeoff: declaring the union costs boilerplate". The brief bars restating one fact twice in a file.
   **Correction** Delete the sentence. The paragraph ends: `Consumers can pattern match on descriptive cases, and each case carries only the data it needs.`

3. **Location** `10-discriminated-unions.md:166`
   **Claim** "A hand-written hierarchy has two limits: an open hierarchy cannot stop a caller from adding a case, and a hand `Success<T>` does not reject `null`."
   **Evidence** Two defects. "two limits" is a count, which the prose rules bar inside the doc. `Success<T>` is a hand-rolled type from the book code that this file no longer defines, so the name has no referent for the reader; the brief calls that out as the case for adjusting a sentence.
   **Correction** `The shipped `Option` and `Fin` are closed: no caller adds a case. `Some` does not check for `null`, so `Optional` is the null boundary. A hand-written hierarchy cannot stop a caller from adding a case, and its success case does not reject `null`.`

4. **Location** `10-discriminated-unions.md:164-166` (book source: `.scratch/painter-functional-csharp/chpater-6.md`, "Three-State Maybe" subsection)
   **Claim** The generic-unions section keeps the null-payload limit and the open-hierarchy limit and drops the book's collection point: "`Something<IEnumerable<T>>` means one or more results were returned, `Nothing<IEnumerable<T>>` means none were found ... The producer checks for both a missing collection and an empty collection before choosing `Nothing`."
   **Evidence** Book claim dropped without cause. It is not a hand-rolled-type artifact: the distinction between an empty collection and absence survives under `Option<Seq<A>>` versus an empty `Seq<A>`, and no other sentence in the file carries it. The design rule "Keep absence and failure separate" covers failure, not emptiness.
   **Correction** Append one sentence to the paragraph at line 164: `An empty `Seq<A>` is a result, not absence, so a producer wraps a collection in `Option` only where the receiver responds differently to no collection and to an empty one.`

5. **Location** `10-discriminated-unions.md:129-160` (the `Input` section)
   **Claim** `Read` returns `IO<UserInput>` where `ConsoleError(Error)` is a `UserInput` case, and "The `Catch` overload with a predicate maps the captured error to the `ConsoleError` case at the boundary."
   **Evidence** The `Catch` placement is correct and the recovery belongs to the boundary that owns the error, so no code change is warranted. The file states no reason for moving the failure off the `IO` error channel into a value case, while every other section of the file keeps failure on the channel. A reader who applies the file's own design rules to a new console reads the two sections as conflicting.
   **Correction** Add one sentence after the first sentence of line 160: `The prompt reads again after any case, so the console failure is a case the prompt matches.`

6. **What a project built on this file still needs**
   - The prompt loop the prose names ("can recursively prompt again for the other cases") has no member. `IO<A>.RepeatUntil(Func<A, bool>)` is the compiled form (`LAB-NOTES.md`, `IO<A>` row): `Input.Read(console).RepeatUntil(static input => input is IntegerInput)`.
   - Absence as a failure has no boundary here. `Greeting.Describe` turns `None` into a string. A host that needs a typed error converts one layer at a time: `lookup.Run().As().Bind(static o => IO.lift(o.ToFin(new NotFound())))` with `NotFound` a `sealed record NotFound() : Expected(...)` in the package that raises it.
   - `Read` recovers on `error.IsExceptional`, so a cancellation surfaced as an exception from the console delegate becomes a `ConsoleError` value. A host that cancels needs the predicate narrowed to the console failure it owns.

Blockers: none
