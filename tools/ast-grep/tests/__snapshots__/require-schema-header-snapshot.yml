id: require-schema-header
snapshots:
  ? |
    #  yaml-language-server:  $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
    id: no-item
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      id: no-item
    labels:
    - source: |
        #  yaml-language-server:  $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
        id: no-item
      style: primary
      start: 0
      end: 121
  ? |
    # No fix, a probe over `Item` shows the correction deletes a sibling pair
    # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
    id: no-item
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      # No fix, a probe over `Item` shows the correction deletes a sibling pair
      id: no-item
    labels:
    - source: |
        # No fix, a probe over `Item` shows the correction deletes a sibling pair
        # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
        id: no-item
      style: primary
      start: 0
      end: 193
  ? |
    # yaml-language-server: $schema="https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json"
    id: no-item
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      id: no-item
    labels:
    - source: |
        # yaml-language-server: $schema="https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json"
        id: no-item
      style: primary
      start: 0
      end: 121
  ? |
    # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/yaml_rule.json
    id: no-item
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      id: no-item
    labels:
    - source: |
        # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/yaml_rule.json
        id: no-item
      style: primary
      start: 0
      end: 124
  ? |
    ---
    # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
    id: no-item
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      ---
      id: no-item
    labels:
    - source: |
        ---
        # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
        id: no-item
      style: primary
      start: 0
      end: 123
  ? |
    id: no-item
    language: yaml
  : fixed: |
      # yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
      id: no-item
      language: yaml
    labels:
    - source: |
        id: no-item
        language: yaml
      style: primary
      start: 0
      end: 27
