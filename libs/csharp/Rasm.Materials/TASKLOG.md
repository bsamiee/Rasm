# [MATERIALS_TASKLOG]

The open and closed work for the host-neutral materials owner, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed — and names the exact sub-domain and file it lands in. One idea spawns one or more tasks; a task is scoped guidance, not a full spec.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

(none)


## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[COMPONENT_COLLAPSE_AUTHOR]-[COMPLETE]: the `Component/` sub-domain authored as the `Profiles` + `Connection` merge — `Component/component.md` (the polymorphic `Component` owner, the `ComponentClass` {Primary,Minor} axis, the nine-case `ComponentFamily`, the `ComponentId` `family.designation` key, the `ComponentFault` band 2300, the `ComponentSection` `[Union]` cross-section FIELD, the relocated `Coring`/`CoringClass`, the seventeen-field `ComputedSection`, the `ParametricSection` solver, the `ComponentCatalogue`, the `[M7]` `ComponentResolution`), the nine family pages (masonry/steel/cmu/timber/glazing/reinforcement/fastener/connector/joint, the `hanger`→`connector` rename), `Component/capacity.md` (the `SectionCapacity` `[Union]`), and `Projection/component.md` (the `ComponentProjector` merge of `MaterialProjector`+`ConnectionProjector`); the old `Profiles/`+`Connection/` pages and `Projection/material.md` retired. README router + ARCHITECTURE codemap/seams + IDEAS/TASKLOG re-derived to the Component topology. Ripple: Rasm.Element [UNIFIED_MATERIAL_COMPONENT_ELEMENT_PARADIGM].
[PANEL_PAGE_AUTHOR]-[COMPLETE]: `Component/panel.md` authored — the TENTH `ComponentFamily.Panel` sheet-goods case grown IN PLACE onto the `[COMPONENT_COLLAPSE_AUTHOR]` owner: `component#COMPONENT_OWNER` widened to the ten-case `ComponentFamily` axis, the new `ComponentClass.Panel` third row of the now-`{Primary,Panel,Minor}` discriminant (an `IfcBuiltElement` one-board-type-many-pieces regime), and the new `ComponentSection.Panel(PanelSection Board)` `[Union]` arm with `panel:` fold arms threaded through every `ComponentSection` projection (`Family`/`Class`/`CrossNominalMm`/`GrossRectangleMm`/`IfcEntity`/`PredefinedToken`), so the prior nine-case/two-row `[COMPONENT_COLLAPSE_AUTHOR]` record stays truthful for that step and the tenth family is this landing. The `panel.md` owner realized the `PanelKind` board-type `[SmartEnum<string>]` (gypsum-board/gypsum-sheathing/plywood-sheathing/osb-sheathing/cement-board/steel-deck-roof/steel-deck-floor/rigid-board-eps·xps·poly, each carrying its assay-verified `IfcCovering`/`IfcPlate`/`IfcSlab` entity + `IfcCoveringTypeEnum`/`IfcPlateTypeEnum`/`IfcSlabTypeEnum` token + `structural` flag + `SubstanceId`), the `EdgeProfile`/`PanelLayerRole`/`PanelOrientation`/`DeckForm`/`DeckProfile`/`FastenerType` axes, the `CoreType`/`SpanRating`/`BondClass`/`FoamType`/`Facer` board-property axes, the `PanelLayer`/`FastenPattern`/`DeckRib` value-objects, the `PanelSection` generative board payload with its `Layup`/`ToUnit`/`CrossNominalMm`/`GrossRectangleMm`/`RValuePerInch`/`RValue` projections, the steel-deck rib `ComputedSection` re-derived through the shared `component#COMPONENT_OWNER` `ParametricSection` Green's-theorem integral (`panel#DECK_SECTION`), and the hand-rolled `ComponentCatalogue.BuildPanelRows`/`PanelSections` (ASTM C1396 / EN 520 / APA PRP-108 / ASTM C1325 / SDI / ASTM C578-C1289) folded into `component#COMPONENT_OWNER` `ComponentCatalogue.Build`/`Sections`. `Projection/component.md` gained the `panel:` kind-determined-leaf egress fold (`p.Board.Kind.IfcEntity`/`IfcPredefinedType`), and `Construction/layout.md` gained the `LayoutStage.Sheathing` row (`run.Component.Family == ComponentFamily.Panel`, `NeedsMortar` false) with the `SheetCoursing` board-grid fold reading `EdgeProfile`/`FastenPattern`/`PanelOrientation` over the SAME `Resolve`. README router + ARCHITECTURE codemap/seams updated to the ten-family Component topology. Ripple: Rasm.Element [UNIFIED_MATERIAL_COMPONENT_ELEMENT_PARADIGM] + Rasm.Bim [PANEL_COVERING_EGRESS].
[JOINT_PAGE_AUTHOR]-[COMPLETE]: Component/joint.md authored — the `joint` ComponentFamily case realized over JointKind (weld/adhesive/stud), the WeldType/ElectrodeClass/AdhesiveClass/StudClass axes, the JointSection union arm with NominalMm throat/bond-line/diameter dispatch and DesignShearKn (AWS D1.1 0.6·FEXX·throat + lap-shear·area + the AISC 360 Eq I8-1 steel-side StudClass.SteelShearKn cap, the composite Qn = min(steel, concrete) the steel#STEEL_FAMILY CompositeMn develops), and BuildJointRows concatenated into ComponentCatalogue.Build. Ripple: Rasm.Bim [JOINT_CONNECTION_FAMILY].
[SUSTAINABILITY_PAGE_AUTHOR]-[COMPLETE]: Properties/sustainability.md authored — Environmental (EN 15978 banded StageGwp ReadOnlyMemory<double> over LifecycleStage + UnitsNet Mass GWP + EPD/recycled/end-of-life), Cost (supply/install/lifecycle over Currency/MeasurementBasis) MaterialPropertySet cases plus the Classification value-object threaded onto the bound element's Object node; the AggregateEnvironmental/AggregateCost per-ply folds RELOCATED to Rasm.Compute. Ripple: Rasm.Bim [PSET_ENVIRONMENTAL_PROJECTION].
[STEEL_COMPOSITE_AND_COLDFORMED]-[COMPLETE]: steel#STEEL_FAMILY grew the SteelClass.Composite (AISC 360 Ch I plastic composite Mn over slab effective-width + StudClass shear interface) and SteelClass.ColdFormed (AISI S100 effective-section Seff·Fy) arms over one SteelSection with Option<CompositeDetail>/<ColdFormedDetail>, mapped to IfcArbitraryClosedProfileDef/IfcUShapeProfileDef and seeded through CompositeColdFormedRows. Ripple: Component/joint [JOINT_FAMILY].
[GENERATED_BOND_INTERPRETER_REALIZE]-[COMPLETE]: masonry#MASONRY_FAMILY realized the BondGeometry parametric descriptor + BondPattern vocabulary + GeneratedBond.Interpret fold so flemish/herringbone/basketweave/pinwheel/diaper/stack bonds compute their CourseTemplate (orientation + offset + PerUnitRotationDegrees into Placement.PathAngleDegrees) rather than railing ComponentFault.Bond.
[MORTAR_JOINT_PROFILE]-[COMPLETE]: masonry#MASONRY_FAMILY added the MortarProfile (ASTM C270 concave/V/weathered/struck/raked/flush/beaded with DepthFactor/ShadowLine), MortarType (M/S/N/O/K compressive class), and MortarJoint (head/bed width + profile + strength) the layout#ASSEMBLY_FOLD JointPolicy.Resolve reads, replacing the scalar StandardJointThicknessMm; the StationStep pitch reads HeadWidthMm unchanged.
[ARCH_DOME_VAULT_EXPANSION]-[COMPLETE]: layout#ASSEMBLY_FOLD grew the ArchProfile axis (segmental/semicircular/three-centre/ogee/equilateral/lancet over RiseRatio→SweepDegrees) on Voussoirs and the RunPath.Dome case + DomeRings fold (ring courses springing→crown, R·cos(latitude) unit counts, meridian tilt, crown keystone) lifting Voussoirs to a surface of revolution through the one Resolve fold.
[BSDF_PAGE_SPLIT]-[COMPLETE]: bsdf.md split by concern into the kernel half (ShadingFrame/MaterialFault/SpectralBand, Microfacet, BsdfLobe/MultiScatter, LayeredBsdf) and the new surface.md lowering half (SpectralUpsample/ToneMap/ConductorIor/SlabStack OpenPBR construction), the two sharing the MaterialFault band 2450; README router + ARCHITECTURE codemap gained the Surface.cs node.
[WEATHERING_SLAB_DEEPEN]-[COMPLETE]: weathering#WEATHERING lifted WeatheringTrajectory to a SlabColumnDelta (CoatRoughnessTo/FuzzWeightTo/ConductorSwap) Weathering.ApplySlab folds onto the surface#OPENPBR_SLAB SlabStack coat/fuzz/base columns before ToLayered, plus the texture#TEXTURE_UV cavity-AO sample scaling the age per shade point through CavityScaled; the flat MaterialParameters Apply survives as the row-level path.
[THINKTECTURE_WIRE_CODEC]-[COMPLETE]: interchange#MATERIAL_WIRE serializes the MaterialWire/OpenPbrGroupsWire/WireProvenance/WireColor shapes through WireCodec — the one JsonSerializerOptions carrying ThinktectureJsonConverterFactory and MessagePackSerializerOptions carrying ThinktectureMessageFormatterResolver — so every SmartEnum/Union/ValueObject wire key serializes by its generated codec, replacing the hand-rolled CultureInfo string projection; csproj + README admit the two serializer pins.
[MATERIALX_NODE_CATEGORY_MAPPING]-[COMPLETE]: texture#TEXTURE_UV TextureSource.MtlxCategory + NoiseBasis.MtlxNode + AddressMode.MtlxAddress + FilterMode.MtlxFilter map each closed case to its MaterialX 1.39 standard node, interchange#MATERIALX_DOCUMENT NodeCategory carries the full verified noise/ramp/image set resolved by Mtlx.CategoryOf(TextureSource); the three-basis NoiseBasis confirmed complete against the standard library.
[BRDF_FIT_COMPUTE_SEAM]-[COMPLETE]: blocker resolved — MathNet.Numerics admitted as a DIRECT Materials NuGet pin (not a Rasm.Compute edge, which the acyclic strata forbids); acquisition#ACQUISITION FitBrdf runs the overdetermined GGX/Smith thin-QR Gauss-Newton solve (Matrix<double>.QR(Thin).Solve over the log-residual Jacobian, Svd(true) rank-deficient fallback, Control.UseManaged) writing the witnessed FitResidual the white-furnace harness gates. Ripple: Rasm.Compute/algorithms [ROUTE_SPINE] (DOCTRINE citation only, no Compute edge).
[STEEL_LRFD_CAPACITY_AND_SHAPES]-[COMPLETE]: steel#STEEL_FAMILY grown to all six SteelClass cases (W/M/S/HP·C/MC·L·HSS-rect·HSS-round·WT) with the CompactnessClass 3-state verdict replacing IsCompact and the DesignCapacity LRFD projection (φMn/φPn/φVn over AISC 360 F/E/G) derived from the stored Zx/Sx/Ix/Area columns; 15-row SteelRows table carrying the SteelClass discriminant, admitted through SteelShapeOf.
[WEATHERING_COLOURMAP_RAMP]-[COMPLETE]: weathering#WEATHERING trajectory Terminal is now a Wacton.Unicolour.Datasets Colourmap ramp (Flare/Rocket/Mako/Vlag) the Age fold samples by the eased fraction via Terminal.Map(f), the four frozen Unicolour endpoints deleted; verified against api-unicolour-datasets.md Colourmaps registry + Map(double).
[MATERIALS_THERMAL_SCALAR_PEER_CONTRACT]-[COMPLETE]: properties#MATERIAL_PROPERTY_CATALOGUE states the THERMAL_SCALAR_PEER_EXPORT contract — Thermal.ConductivityWMK/SpecificHeatJKgK + Mechanical.DensityKgM3 as raw SI doubles, decoupled from the UnitsNet proxy, the stable Fabrication peer-read surface; Ripple: Rasm.Fabrication [TOOL_CUTTING_DATA_TABLE].
