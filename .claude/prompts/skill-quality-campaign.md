Read `/Users/bardiasamiee/.claude/HANDOFF-SKILL-QUALITY-CAMPAIGN.md` in full before anything else — it is the decision-complete grounding for this session and its rulings are settled. Do not re-litigate the architecture or the standard it fixes.

This session raises three skills under `/Users/bardiasamiee/Documents/99.Github/Rasm/.claude/skills/` to one bar: `references/` and `SKILL.md` must POWER AN AGENT to produce the world-class behavior of the skill's concern — create-doctrine an author executes, not narration, not table-stakes, not stored templates. The no-op law, the docgen register, the no-cross-file-reference rule, the density budget, and the three-pass refinement method are defined in the handoff's SKILL_STANDARD and REFINEMENT_METHOD sections.

Ground first, then work:

1. Read the handoff. Load the `docgen` skill (the prose standard and its gate). Read each of the three skills IN DETAIL — `html-studio`, `interviewing`, `mermaid-diagramming` — every `SKILL.md` and every file under `references/` — to grasp what each skill is FOR and where each stands against the standard. Read `docs/stacks/python/README.md` and its core doctrine pages once as the register model the references must meet.

2. `html-studio` is the exemplar and has two open items (the handoff's HTML_STUDIO section): the redteam was killed mid-run — re-run it over the whole skill and implement what survives; and the `templates/`/`examples/` roster is unstarted and co-owned with the user (they hold many template and element ideas), so enumerate it WITH them, then author each template and example to the standard, carrying the token-stamp rule. Never invent the roster alone.

3. `interviewing` and `mermaid-diagramming` are competent but under the bar (their handoff sections). Take each — one at a time — through the three-pass refinement (initial or rebuild, adversarial critique, terminal redteam), each pass ultra-adversarial yet grounded in the skill's real intent, corrections implemented between, every touched file gated and verified before seal. Hunt cross-file `TWIN_TRUTH` and no-op narration hardest; confirm each reference gives create-grade doctrine; hold each `SKILL.md` to router discipline.

4. Close the campaign with the ASSAY_UNIFICATION goal (its handoff section): make `uv run python -m tools.assay docs` the single quality rail — add the docgen prose gate and the html-studio html gate beside the already-integrated mermaid engine, then repoint every skill and doc touchpoint to route through `assay docs`. Sequence this last, after the three skills' engines are final.

Open by setting the sequence with the user and confirming the html-studio roster is theirs to drive. Delegate authoring to `fable` agents and adversarial passes to `opus` agents per the handoff method; verify (gate, LOC, no cross-references, tool-runs) and commit per skill with a scoped, honest message. When adding or changing any validation tool or engine, provision CLIs through `Parametric_Forge` (never per-repo) and reconcile the handoff's TOOLCHAIN and TOUCHPOINTS sections so no routing drifts. Treat archived and prior skill versions as material to mine, never blueprints to copy.
