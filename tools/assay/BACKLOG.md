# Assay — Known Issues & Backlog

Open issues only. **Every item is verified real** (reproduced, or confirmed against installed-library source) — nothing speculative. Closed-wave history lives in git (this file at 2026-06-10 held the pre-closeout backlog; the 2026-06-11 closeout session landed or explicitly rejected everything except the items below). Rows tagged `[DONE 2026-06-14]` / `[FIXED 2026-06-14]` were closed by the finalization session and are kept inline as recent-closure context — do NOT re-do them; rows tagged `[NOT DONE]` / `[OPEN ...]` are the live work. Companion: `.cache/testing-infra-research-ledger.md` holds the research delta and earlier rejected-with-rationale rows (frozendict, schemathesis, CrossHair-as-dep, atheris, codspeed, ghostwriter — declined on portability/theater grounds; do not re-propose).


## OPEN WORK — 2026-06-14 finalization plan (ordered; supersedes the 2026-06-12 TOP CONCERN ordering)

Phase order runs smallest/safest -> live/heaviest. Per-row mechanics live in the issue/smell rows below and in
`.cache/closeout-2026-06-12/` (closeout SPEC.md, host-bound verdicts, static-opt research, testkit DECISION.md, VPS runbook).

1. **[DONE 2026-06-14] Bound build-scope retention** — `ArtifactStore.retain_builds(keep)` + `build_scope_retention=24`, called from `registry._persist`; the stable `build/<closure>/<config>` dirs now prune oldest-first like history/scopes.
2. **[DONE 2026-06-14] Stale litter + balloon reclaim** — `.artifacts/rhino-bridge-next`, `.cache/mut-venv`, `.cache/mutation-requirements.txt` deleted; the 13 GB `build/` balloon reclaimed to the new bound; frozen-tree litter sweep clean.
3. **[DONE 2026-06-14] Collapsed the 3x supervisor poll loops** onto one named-kernel `Poll.Until<T>(probe, deadline, cadence, ct)` in HostControl.cs (`LaunchAndPoll`, `QuitLadder.WaitForExit`, and `HostWatch`'s degraded arm via `volatile bool`->`CancellationTokenSource`). The v5 `Schedule` type is a Duration-stream combinator built for effect repeat/retry — verified it is the WRONG primitive for a synchronous native bool-probe (it would force an Eff allocation per tick); the named-kernel with the native probe as the single platform-forced seam is the doctrine-correct collapse, and the deadline value (incl. `Timeout.InfiniteTimeSpan`) discriminates bounded session-poll from unbounded watch.
4. **[DONE 2026-06-14] `Stub.cs` endpoint duplication — root-up fix** — folded `fault` into `EndpointRecord` as a 7th value member so `ShellHost.WritePoisoned` collapses to one typed record through the shared converter (Shell-side hand-rolled poison writer GONE; converter read-after-write asymmetry closed). The dependency-zero stub keeps its lone inline writer as the necessary isolation-seam exemption (shell-load-FAILED branch — no shell DLL to delegate to); its false `LanguageExt` comment corrected to the real StreamJsonRpc closure. Removing the stub verified UNSAFE (load-bearing ALC isolation + immutable plugin GUID / yak-slug lifecycle); source-link rejected (same-FQN type-identity split in default ALC). See rejected-with-rationale ledger.
5. **[DONE 2026-06-14] Projected supervisor `Host`/`Capabilities` into assay** — new `BridgeLifecycle` Detail (tag `bridge`) in `core/model.py` + `AnyDetail`; `bridge.py` adds `_CapabilityEntry` + `host`/`capabilities` to `_SessionEnvelope` and `_lifecycle` folds the detail, so doctor/quit/launch/check/clean carry structured host fingerprint + capability rows (incl. the `mcp.*` unsupported facts) without opening the artifact. Live `bridge doctor` proof of the `kind:bridge` detail is a Phase-6 (live-Rhino) item.
6. **[DONE 2026-06-14] Wired `Evidence.GcDump` into the cargo-unload-leak path** (Session.cs `VerifyAsync`, `!unload.Confirmed && !unload.DebuggerAttached` branch) — emits `cargo.gcdump=<path>` + feeds the path into `cargo.recycle.after-leak`. Code + Supervisor build green; the live U8 soak that exercises the dump is a Phase-6 item.
7. **[DONE 2026-06-14] Assay micro-smell polish** — `mutation_gate._cluster` O(n^2) `reduce`+dict-merge collapsed to O(n log n) `groupby`+`itemgetter`. The `_yak_*_tail` collapse was SKIPPED (hypothesis-gated): three one-line factories with distinct signatures/inputs (build needs version, install/push need the package file, push has a conditional source) — a discriminated entrypoint adds a union for negative LOC; the originals are denser. Do not re-propose.
8. **[DONE 2026-06-14] Collapsed static `check`+`build`+`fix` into ONE polymorphic `static` verb** — value-driven targets (`--all`/`--project`/`--folder`/`--file`/changed-default), fix-always lane `auto-fix -> diagnose -> restore -> build` per language; registered as a ROOT LEAF (`assay static`, no sub-verb) via `_register_claim` branching on `len(rows)==1`. `static.py` rewritten to one `run` entrypoint + `_MODES`; registry/usage/`_VERB_SLOTS` collapsed; README + tests (`test_rail_static.py` rewritten, `test_main.py` bare-bridge + cli calls) updated. self-test census + CLI tree green.
9. **[NOT DONE] SSH offload finalization incl. live VPS** — execution-side artifact placement + uniform path translation; S3/GCS store laws vs `Bucket`; loopback + live `root@31.97.131.41` proofs. NOTE: the HARD INVARIANT (bridge claim + package stage/deploy/publish NEVER offload) is satisfied by DESIGN today (both run host-local), but there is no EXPLICIT typed reject — when `exec_target` is non-empty, bridge/package should fault `UNSUPPORTED` rather than silently ignore it (`_materialize` already rejects staged tools under `exec_target`; extend that to the bridge/package leases). -> Phase 4; remote rail + governance rows below.
10. **[NOT DONE] Static-rail perf + flag posture + PATH-WALKING proof matrix** — the matrix MUST target the collapsed single `static` verb, NOT `static check/build/fix` (retired 2026-06-14). VALIDATED 2026-06-14 on a clean C# project: the verb walks `[fix] dotnet format --severity error` -> `[diagnostic] dotnet format --verify-no-changes` -> `[restore] dotnet restore --locked-mode` -> `[build] dotnet build --no-restore ... -p:CspSarifDir=` with correct argv/order. REMAINING: the rest of the matrix (`--all`, moved roots, TS file, trigger-file rejection, host-bound partition) + the flag-posture A/B (static-rail performance row below). FIX-ALWAYS CONSEQUENCE (new): the collapse removed the non-mutating `check`, so every `static` run mutates — there is no read-only dry-run; `detail.planned` still carries argv for inspection on already-clean targets, but a CI preflight wanting "what would this do without writing" now needs an explicit `--plan`/dry flag. -> Phase 5.
11. **[NOT DONE — live Rhino] Live bridge proof matrix** — full 28-scenario matrix, U6 dirty-quit, U8 soak, MCP/GH2 capability facts, `vectors.SpectralDec` doc scope + open-quad DEC production fix (bridge issue rows below). NOTE: the 2026-06-14 `SessionFold` divergence fix (`>=`->`<=`, tooling-issues row) means `evidence.divergence` now FIRES correctly when the durable spool out-counts the relay — directly relevant to the U8 crash-harvest + evidence-reconciliation rows, which previously could not have observed a real divergence. -> Phase 6.
12. **[NOT DONE] Terminal verification** — staged mutation gate vs the 0.80 floor (baseline 0.7345), `.archive/quality` A/B + delete, full gates, final truth-sync. BINDING LESSON (new): the full gates MUST RUN the owning VSTest/pytest suites, not just `static`/`bridge build` — the build COMPILES but never executes tests, so a green build masked a real runtime bug this session (the inverted divergence test sat committed unseen). Terminal sign-off = green builds AND green test suites across the changed surface. -> Phase 7.

### Lifecycle/cutover mechanics (Phase 6-7 detail; ordering above is binding)

The dual rebuild has the canonical bridge tree in place and the supervisor kernel present; cutover is not signed off
until the live supervisor proofs, quit hygiene, and Assay path-walking proof all pass on the renamed tree.

1. **Supervisor session kernel present; full-matrix proof incomplete** — `SessionKernel` now owns launch -> endpoint
   poll -> pipe connect -> Hello -> LoadCargo -> Run -> SessionFold envelope -> PrepareQuit -> QuitLadder, and doctor
   no longer fakes a cargo-loading state. Blocks proof is complete (`bridge verify blocks.CoreRail` and
   `bridge verify blocks.CommandHistoryRail`, 2026-06-14); remaining sign-off is the full typed scenario matrix plus
   the lifecycle edge cases below.
2. **Dirty-GH2 quit row still unproven** — `PrepareQuitAsync` now uses the bundle-verified
   `Grasshopper2.Doc.Document.AllDocuments` + `Unmodify()` surface instead of a guessed `Modified` setter, but the U6
   dirty-Rhino-doc + dirty-GH2-doc row has not been run after the fix. Keep this as a proof blocker until unattended
   quit shows zero save prompts and zero crash markers on next launch.
3. **MCP remains unsupported until the listener/platform is configured** — launch intentionally suppresses Rhino MCP
   autostart, and the current host has no admitted MCP platform assembly. The fix is configuration/admission with
   explicit `mcp.listener` and `mcp.platform.version` facts in the bridge envelope; do not silence the facts in Assay
   or treat unsupported MCP as success beyond the documented coexistence contract.
4. **Cargo unload leak persists; recycle policy needs soak proof** — live proof still reports `cargo.hotswap=unsupported`
   and `cargo.unload.leaked` because Rhino pins the collectible Cargo ALC. The supervisor now observes leakage and
   emits a recycle/quit path (`cargo.recycle.after-leak=quit-ladder` proven 2026-06-14), but the 10-swap soak below must
   prove the before-next-load semantics under repeated verify and crash-harvest conditions.
5. **U8 cargo soak never ran live** (10-swap unload + kill-mid-scenario spool harvest) — cutover sign-off gate. Include
   the host-recycle behavior above so the soak proves repeated verify semantics, not just manual quit discipline.
6. **Assay cutover mostly proved; final matrix remains** — bridge path constants target
   `tools/rhino-bridge/{Contract,Cargo,Shell,Stub,Supervisor}` and Assay consumes supervisor envelopes. Proved
   2026-06-14: `bridge build/doctor/verify/quit`, the COLLAPSED single `static` verb (C# project path-walk:
   fix->diagnose->restore->build argv verified), and `package plan/stage/deploy rasm-bridge`. (`static check/build/fix`
   are retired — the path-walk re-proof on the new verb is Phase 5.) Remaining proof: full typed scenario matrix and
   the lifecycle rows below.
7. **Structural closeout landed; runtime closeout pending** — old bridge archived, the temporary bridge tree collapsed to
   `tools/rhino-bridge`, the legacy tool-test root removed, probe/parity scratch directories removed, testkit scenario sources flattened to
   `tests/csharp/_testkit/Scenarios`, and bridge/test project folders now use concise owner names. Package closeout
   is done (`rasm-bridge-next` uninstalled; only `rasm-bridge 1.0.2` is installed; `~/.rasm` has only the live endpoint
   and quit journal). Remaining closeout: A/B `.archive/quality` parity, frozen-tree litter sweep at final cutover, and
   terminal full gates.

## Rebuild-owned (the work that remains)

### Tri-tool charter (assay + rhino-bridge + cs-analyzer) — binding for the rebuild session
The three tools form one quality surface and are held to one standard, enshrined here so the next session starts from it (each tool's corpus carries its own specialization):
- **Agent-first/only.** Every consumer is an AI agent, never a human. Concretely: one structured envelope per invocation; diagnostics that carry the FIX, not just the finding (cs-analyzer emits the doctrine-compliant rewrite an agent applies; bridge failures carry the evidence an agent triages from — no log spelunking); fail fast with typed faults over hangs; `--help`/README text written as agent operating contracts.
- **Monorepo-first, never coupled.** No hardcoded project paths, target names, or single-package assumptions (cs-analyzer rules describe semantic shapes per CLAUDE.md, never `Rasm.*` namespaces; assay's testkit is SUT-registered per package; the bridge serves any host-bound verification need, not one plugin). New packages integrate by declaration, not by editing tool internals.
- **Doctrine-bound.** Rebuild code per `docs/stacks/csharp/` (code doctrine + every root file) for the two C# tools, `docs/stacks/python/` for assay; all tool docs to page-craft standards. Minimal surface/dispatch shapes; external packages first (add to the plan where they own a concern); no boilerplate rule/verb authoring — adding a rule or verb is one descriptor row + one body + one test.
- **Hardened, non-fragile, performance-honest.** Leases serialize contended hosts (busy = clean exit-5 envelope, never a hang under N concurrent agents); resource governors cap fan at every tier; no orphaned processes; benchmarks are real and regression-gated; mutation runs are staged, root-clean, and feed harden-or-delete decisions; per-file coverage floors and law MANIFEST gates stay binding.
- **Inter-relations + rebuild order.** cs-analyzer is consumed by assay's static rail (diagnostic contract + ID stability owned by the analyzer corpus); the bridge is consumed by assay's bridge/package rails (the rebuild deletes assay-side choreography — `rails/bridge.py` becomes a thin typed consumer). Order: each corpus's distill doc fixes the bridge-vs-assay update sequencing; cs-analyzer rebuild is independent of the bridge and may land first.

### Execution roadmap — do-once ordering (binding)
Each phase covers all code born in the phases before it; mutation runs LAST so its verdict reflects the end state and cannot be invalidated by later churn. No item is touched twice.

| Phase | Work | Why this slot |
|---|---|---|
| **R1** | **DONE 2026-06-12 (foundation form)**: cs-analyzer rebuilt — kernel/contracts/generator-in-analyzer, harness + meta-invariants, SARIF lane, assay decoder. ZERO rules by decision (old rules judged not worth porting); rule authoring is future work: one catalog row + one body + one docs/rules/<ID>.md + one spec, nothing else | Landed first as planned |
| **R2** | **IN FLIGHT**: probes done (0c PASS->MDNC; 0a/0b blocked by 26160 host churn, re-probe through new stack), Contract FROZEN, Stub dual-run proven, Shell onward running; old-tool execute lane re-fixed for parity oracle (three-gate 26160 churn: deferred WaitStatusComplete, wildcard spec rejection, cwd-reset reference loss); assay cutover map line-anchored and ready |
| **R3** | Assay consolidation batch, one pass, internal order: (1) mutation-verdict survivor triage FILTERED to stable core — skip anything R2 rewrote/deleted (those survivors are moot) and anything R3.3 will rewrite; (2) agent-feedback deep scan (mutmut row timeout, progress-probe doc, CHANGED-lane-as-loop doc, ext-lib mining); (3) remote/offload rail (testkit env-mock layer → live SSH proof → target-side artifacts + offload opt-in); (4) `py_analyzer` law suite | Single pass over a surface that is finally stable; the env-mock layer lands once and serves every later package |
| **R4** | Terminal verification: ONE full staged mutation run over the final tree + gate verdict + last (small) survivor triage; full gates; README/BACKLOG truth-sync | The light at the end: nothing changes after R4, so no further run can expose new findings |
| anytime | Upstream filings (mutmut trampoline, cyclopts Enum-help) | Zero dependencies |
| event-blocked | CrossHair cp315 wheel, xdist↔benchmark plugin conflict, `register_random`/`de_eq` triggers | External releases / named-consumer triggers, not schedulable |

### rhino-bridge ground-up rebuild
Research corpus archived at `.archive/rhino-bridge-v1/.research/` (review 01–11 grade A−; design 01–06 done; **07–11 authored + finalized + SEALED 2026-06-11/12** (17 registered amendments; zero dangling deferred items across both corpora — the 35-row closeout register is 10 §7): code blueprints with the agent-first diagnostic surface and the MCP-server seam, file-precise assay-impact plan, object-model red-team, one-page distill charter — plus a re-evaluated MCP complement matrix against current official Rhino MCP capability and an agent-first doc-reframing appendix; stale-against-tree corpus claims amended with both-sides citations); four Phase-0 live probes gate any build code; original tool preserved side-by-side until cutover. The macOS limitation is the tool's reason for existing (no COM automation story; clean-quit/crash-marker constraints are design inputs, not afterthoughts). ASSAY IMPACT: the rebuild deletes assay-side hand-rolls — `rails/bridge.py` shrinks to a ~150 LOC typed consumer (design doc 02/06: `SessionEnvelope` lands field-for-field in `VerifySummary`), lifecycle choreography/evidence scraping leaves python entirely, and capture/image handling may ride the bridge or the official Rhino MCP surface (complement verdict) instead of assay — so do NOT deep-polish `bridge.py`/`package.py` bridge-coupled code; those regions are rebuild-owned. THE REBUILD ALSO OWNS (folded 2026-06-11): bridge/package quality-parity vs `.archive/quality` plus the live rhino-bridge launch/check/verify/quit cycle on both, the `rhinocode` doctor probe (archive GAP — bridge-owned diagnostic), and `.archive/quality` deletion after that parity closes.

### cs-analyzer ground-up rebuild
Tabula rasa — the same posture as assay-vs-old-quality-tool. The current tool is treated as a demolition survey input, not a base. Research/planning corpus at `tools/cs-analyzer/.planning/` (authored + finalized + SEALED 2026-06-11/12: 01 source audit of all low-quality code, 02 binding `docs/stacks/csharp/` doctrine constraints, 03 cited modern-Roslyn landscape (incremental APIs, perf doctrine, config surface, Microsoft.CodeAnalysis.Testing, packaging, major-analyzer architectures), 04 post-red-team target architecture, 05 per-rule CARRY/COLLAPSE/DROP parity map + the new-rule authoring recipe + the limited new-rule set, 06 testing harness (positive+negative per rule), 07 monorepo + assay integration map (props wiring, static-rail contract, CSP ID-stability policy, agent-consumption spec), 08 red-team ledger, README charter). End state: a 100%-done tool where ALL future work is adding rules — one descriptor row + one rule body + one test file, nothing else touched. No god files; kernel/rules/tests split; hardened (deterministic, culture-invariant, concurrency-enabled, generated-code policy); agent-first diagnostics (an agent fixes a finding without reading rule source). Semantic-shape rules only per CLAUDE.md — never namespace/path/one-off-symbol coupling. Next session codes from the corpus. SEAL NOTES (binding): bridge implementers must NOT build `Handshake.RpcVersion` as drawn in immutable design 06 §2.4 — amendment 16 supersedes it (the `rpc.streamjsonrpc` capability row is the carrier; 07 §2 pin 2 is operative); immutable-doc line cites carry a registered ±2 drift profile — do not re-litigate offsets.

### Full mutation run — VERDICT RECORDED 2026-06-12 (triage is R3.1, NOT immediate)
Completed 2026-06-12 via the staged rail posture, 48.7 min wall-clock under the 2-worker governor, repo root clean throughout. **Score 0.7345 (killed 8809 / survived 3185 / selected 12003) — gate FAILED vs the 0.80 floor, but +0.078 over the 0.656 baseline.** Survivor clusters + top-N diffs ride the gate's structured projection (read the staged metas at `.artifacts/python/mutmut/work/mutants/**/*.meta`; terminal-verdict filtering mandatory — `exit_code_by_key` is pre-seeded). R3.1 OPENING GUIDANCE: the largest survivor mass sits in registry/engine telemetry-assembly paths (configure chains, log/span kwargs — `_logging` processors=None is the visible sample) — open with an ACCEPT-class sweep there BEFORE writing any law; the kill-ratio gain per law is materially higher in the `code.py`/`routing.py` clusters, so law-writing budget goes there first. ALSO IN R3: delete `.cache/mut-venv` after one more verified staged run — mutmut now rides the main venv via the `mutation` dependency group; the mut-venv is a stale ~500 MB artifact of the libcst no-cp315-wheel era (pyproject comment updated 2026-06-12; `.cache/mutation-requirements.txt` retires with it). The verdict + survivor clusters get RECORDED here, then triaged in roadmap phase R3.1 under the stable-core filter — hardening survivors in regions R2 deletes (bridge.py, package bridge arms) or R3.3 rewrites (remote arms) would be work thrown away; R4's terminal run is the binding verdict. Gate read-back: `cd .artifacts/python/mutmut/work && uv run python -m tools.assay.rails.mutation_gate`. Survivor clusters + per-file durations ride the gate's structured stderr line (survivor projection landed 2026-06-11). Baseline to beat: score 0.656 (2026-06-10 first live run). Reading caveats: remote-body mutants (engine SSH arm) sit outside the covered-line population under `-m 'not network'` — the dispatch seam is law-killed but body kill-ratio needs a one-off network-inclusive coverage feed if it matters; the 2026-06-11 triage's "snapshot-contradicting survivors" were path-coverage gaps, NOT inert assertions (verified live under the exact mutmut posture). The 2026-06-11 scoped run + 59 targeted triage laws raised kill-rate on the six most-edited modules; docs module verified loop closure (19 survivors → 2, both proven-equivalent).

## Assay forward work (queued 2026-06-12)

### Agent-feedback deep scan — automatic info, never command-gated
Remaining (mutmut `timeout=3600` LANDED; fsspec find/glob LANDED; help-truth LANDED via registry `App.usage`): document the CHANGED lane as the standard agent loop (README); `mutation.progress` structured stderr event ONLY if long runs persist after the R4 terminal run; finish the ext-lib sweep (psutil beyond stall/census, OTel span attributes agents could correlate on); backpressure/queueing facts must reach the ENVELOPE, not just telemetry (see resource governance (4)). Candidate (named value, post-cutover): compare bridge capability facts (`rhinoVersion`, `capability.mcp.platform.version`, `rpc.streamjsonrpc`) across session history in `delta` so host drift is an automatic cross-session signal — the bridge owns in-session drift (HostDrift floor); assay history owns across-session.

### Cross-invocation resource governance — machine-altitude admission (verified live 2026-06-12, loadavg 48)
Every existing protection operates at the wrong altitude for multi-agent contention: the flock lease serializes IDENTICAL closure keys only (two different closures fan freely in parallel); `governed_concurrency` caps each invocation's OWN fan — memory backpressure included — but is blind to system load and to sibling invocations; and `--disable-build-servers` (the deliberate isolation posture) makes every concurrent build maximally expensive — full cold process trees, no shared compiler. Observed: one 8-project static-build fan overlapping two agents' dotnet/Rhino brackets drove 1-min loadavg to 48 with zero protection triggering. Fix directions, in order of invariant strength: (1) **machine-wide dotnet admission slots** — one flock-counted slot pool (e.g. `locks/machine.dotnet`) acquired per spawned dotnet process across ALL invocations; bounds the total exactly; busy = queue with a visible envelope note, never a silent pile-on. Slot unit DECIDED: dotnet-family process census (= MSBuild node count in practice), NOT per-invocation; land together with (3) since re-enabled servers make node accounting smaller and more stable. (2) **reactive census backpressure — LANDED 2026-06-12**: `governed_concurrency` halves DOTNET batches when the foreign dotnet/MSBuild/VBCSCompiler process census (own tree excluded) reaches `cpu_count` (`_foreign_dotnet_count`, law-pinned incl. the non-DOTNET-batch exclusion and telemetry fields). This is the census substitute for loadavg, which stays rejected (macOS load1 counts uninterruptible waits — the in-code rejection comment stands). (3) **shared-compilation re-enable** (research-verified: VBCSCompiler pipe identity is per-user+per-SDK, stateless, isolation-safe with per-closure `--artifacts-path`) — reduces per-build amplitude so contention costs less when it happens; ride the existing A/B criteria; (4) pressure facts surface AUTOMATICALLY in the envelope per the agent-feedback law (an orchestrating agent must see "queued 40s behind 2 invocations / census 14" without running a probe — the backpressure event exists in telemetry; the envelope note does not yet). Acceptance: N concurrent assay invocations + M direct dotnet builds hold machine pressure below a declared bound, with queuing/backpressure visible in every affected envelope.

### Remote/cloud/SSH reality + mockable environment testing (monorepo-shared first)
Two halves, ordered:
1. **Mock-anything testing infrastructure** — LANDED 2026-06-12: `tests/python/_testkit/env.py` owns the declarative env-double layer (`EnvSpec = SshHost | Bucket | RemoteFS`, structural `provision` -> `Provisioned(url, client_factory, teardown)`); `SshHost` runs asyncssh over an AF_UNIX socketpair (no TCP) so the engine's remote body (`_run_remote`/`_ssh_connection` pooling/streaming) now rides default AND mutation lanes via the `ssh_env` fixture (`ssh_loopback` deleted); `Bucket` = `moto[server]` dev-dep (loopback TCP, import-smoke-skipped where moto fails to import); `RemoteFS` = fsspec `DirFileSystem` over per-test memory roots with an AbstractFileSystem law battery. Remaining gap: assay's S3/GCS `ArtifactBackend` paths are still never driven against `Bucket` (store-protocol laws stop at `memory://`).
2. **Verify-then-build the offload rail** — VPS READY (root@31.97.131.41, uv 0.11.21, repo synced, `uv sync` green; BatchMode SSH skips rc files so remote commands need `export PATH=$HOME/.local/bin:$PATH` inline) — the intent is a REAL, fully-made remote tooling story, not plumbing. Two orthogonal axes, named once and never conflated: **execution target** (`exec_target` — where the tool process runs) and **storage target** (`ArtifactBackend` — where artifacts/history live). A cloud-drive path is storage-remote with LOCAL execution (artifacts "remote" only in the fsspec sense); a VPS offload is execution-remote, and there artifacts default to the TARGET (co-located with execution — that is the optimization; a flag overrides, but no flag forest). Invariants regardless of axis values: the calling agent ALWAYS receives the envelope/diagnostics/status locally and automatically (the agent-feedback law above), and path semantics are uniform root-down so commands AND automations behave identically across local/ssh/cloud. Sequence: (a) prove `exec_target=ssh://` end-to-end live (loopback + real host); (b) land the execution-side artifact placement + path translation; (c) decide defaults deliberately — LOCAL execution stays the default for agent ergonomics; offload is explicit opt-in (one flag or a verb form, e.g. heavy lanes only: mutation FULL, static full, dotnet build closures). Strategic end-state: multi-agent resource management — an agent sees local pressure via the automatic resource feedback that already exists (`_sys_pressure` load/mem/swap sampling, stall monitor) and routes its heavy lane to a remote executor; a future automation can do the same routing on pressure thresholds. Understand and document every side-effect (retention pruning on remote stores, history reads in `delta`, capture round-trips, lease semantics across hosts) before flipping any default. HARD INVARIANT: the bridge claim NEVER offloads — it is host-bound by definition (live Rhino on this machine); offload eligibility is the heavy managed lanes only (mutation FULL, static full, dotnet closures).

### Static-rail performance + flag posture (measured program; research verdicts recorded 2026-06-12; corrected 2026-06-13)
Folder/file-driven build now routes by language. C# folder/file targets derive owner projects from source files; TypeScript folder/file targets promote to the project-owned `tsc` row without adding file tails to `-p`; languages with no build-capable rows report the resolved route/skips rather than inventing work. The 2026-06-13 build-storm regression remains the guardrail: path targets must not expand into unrelated reverse-dependent project fans. Measured A/B window owns (all UNVALIDATED still — flag-posture work, NOT done): drop redundant `MSBUILDDISABLENODEREUSE` (covered by `--disable-build-servers`); confirm `-tl:off` present on the build phase; `--no-restore` where the format/diagnostic phase precedes the closure restore (the closure restore covers IDE0005); VBCSCompiler shared-compilation re-enable (pipe identity per-user+per-SDK, stateless — also shrinks cross-agent storm amplitude); `RunMSBuildChecks=false` after live property-name verify; machine-slots admission pool (unit = dotnet-family process census). Interpretation caveat: SDK 10.0.300 carries a 2x `.slnx` restore regression (dotnet/sdk#54344). PATH-WALKING PROOF MATRIX (target the COLLAPSED single `static` verb — `check/build/fix` retired): the C# `--project` walk is VALIDATED (fix `dotnet format` -> diagnostic `--verify-no-changes` -> restore -> build, argv correct, 2026-06-14); REMAINING to prove are `--all`, `--folder/--file`, changed-default, moved roots (`tests/csharp/tools/`, `tests/ast-grep/`), trigger-file rejection, host-bound partition, and post-rename bridge paths — inspecting the ARGV per phase (not just exit codes) and the route note per envelope; any mis-walked path is a defect, not a re-tune. The matrix MUST include the format lane over the full rebuild diff when `--all` is explicit: IDE0001/IDE0002-class simplifier diagnostics never fire in build (EnforceCodeStyleInBuild enforces only the build-capable rule subset), so `dotnet build`+TWAE is structurally blind to them and only `dotnet format` surfaces them — a file no format lane ever visited can carry error-severity style violations through green builds (observed: Harness.cs IDE0001/0002, 2026-06-12). Also queued: mypy/ty dual type-check on the python report lane (drop mypy after parity proof); `rich`+registry import deferral (~165ms of 477ms startup, every agent invocation pays it); zstd artifact history compression; `ArtifactScope` cold-scope `DOTNET_CLI_HOME` pre-seed.

### Closeout (after bridge finalization; the only non-bridge work left)
Structural cleanup landed: old bridge is in `.archive/rhino-bridge-v1`; the rebuilt bridge is canonical `tools/rhino-bridge`; bridge folders are `Cargo`, `Contract`, `Shell`, `Stub`, `Supervisor`, and `Gate`; tests live under `tests/csharp/tools/rhino-bridge/{Contract,Scenarios,Supervisor}`; testkit scenario SDK sources live under `tests/csharp/_testkit/Scenarios`; temporary bridge, legacy tool-test, probe, and parity scratch roots are gone from the active tree. Package closeout landed 2026-06-14 (`rasm-bridge 1.0.2`, no `rasm-bridge-next`, current-only `~/.rasm`). gcdump is now WIRED (2026-06-14: `Evidence.GcDump` -> `cargo.gcdump` fact in the unload-leak path); dotnet-counters earned via probe 0c. Remaining closeout: full-tree gates on the frozen tree (RUN the test suites, not just builds — see terminal lesson); A/B baseline capture from `.archive/quality` BEFORE its deletion; the live U8 soak that exercises the gcdump path.

### Tooling issues observed this session (verify or fix at the frozen-tree gate)
- **[FIXED 2026-06-14] `SessionFold` divergence condition was inverted** — `Session.cs` carried `spoolTail.Count >= relayed ? evidence : evidence + Divergence(...)`, which SUPPRESSED the `evidence.divergence` reconciliation fact in the exact host-died-mid-relay case it exists for (spool has MORE facts than were relayed). Corrected to `<=`. Pre-existing (committed `>=`), surfaced ONLY by running the Supervisor spec suite (`FoldLaws.SpoolDivergenceEmitsTheReconciliationFact`, 1/33). The `static`/`bridge build` gate compiles but never RUNS tests, so a green build masks runtime test failures — when an API/contract changes, run the owning VSTest/pytest suite, not just the build.
- **[OPEN smell] Test-file lint debt is invisible to the `static` lane** — the lane routes production/changed files, so `tests/python/**` accumulates ruff debt the gate never sees (e.g. `tests/python/tools/assay/composition/test_registry.py:_strict_params` carries `DOC201`/`DOC501`, surfaced only by an explicit `static --file <test>`). Decide: route test files through the lane on a cadence, or accept the gap explicitly.
- **Static streaming gap (observed live 2026-06-13):** a long `static` run emits stall-monitor telemetry on stderr, but phase start/end, process receipts, diagnostics, SARIF folds, and `StaticRun` details remain buffered until the final envelope. The engine already owns psutil census, process monitors, slots, receipts, and artifacts; the missing integration is a structured event stream for command start/end, phase boundaries, slot waits, and discovered diagnostics while preserving the final one-envelope stdout contract.
- **Supervisor full-matrix proof gap (updated 2026-06-14):** the Blocks delivery path is proven through the supervisor delivery vehicle, including launch/reuse, endpoint poll, pipe connect, Hello, LoadCargo, Run, envelope fold, command-history facts, PrepareQuit, and quit ladder. Required before cutover: run the full typed scenario matrix and keep the same proof strictness for every scenario family. Folds in: U6 dirty-Rhino + dirty-GH2 quit proof after the bundle-verified `Grasshopper2.Doc.Document.AllDocuments` + `Unmodify()` scrub, GH2 capability admission below, MCP capability admission below, cargo recycle behavior, and evidence reconciliation.
- **GH2 dataflow/render unsupported on RhinoWIP 9.0.26160:** live probe reports `gh2.dataflow=unsupported` and `gh2.render=unsupported` (`Grasshopper2.UI.Editor` type load failure). Root fix lives in bridge/Cargo capability admission: bind the actual supported GH2 headless/render APIs for the active RhinoWIP build, or keep scenarios requiring GH2 gated by typed capability facts. Do not hide the unsupported facts in Assay or downgrade them to generic probe noise.
- **MCP listener/platform unsupported unless configured:** current launch suppresses MCP autostart and no MCP platform assembly is admitted, so `mcp.listener`/`mcp.platform.version` stay explicit unsupported facts. Assay remains envelope passthrough only; no new Python schema, fallback listener, or silence filter. CODE FOLLOW-UPS (per the `additive_external` MCP verdict in Deferred): harden `McpListener()` from unconditional-Unsupported to a real suppression GATE, and pin `McpPlatform()`'s `'mcp'` assembly scan to the McNeel identity (it currently can't distinguish McNeel's `rhinomcp` from the community one).
- **Cargo discovery/evidence reconciliation need full-matrix pinning:** declared `scenarioAssemblies` discovery is proven for Blocks and the noisy type-load failures are gone there; ordinary Blocks receipts also no longer emit unexplained `evidence.divergence`. Keep this open only as a matrix requirement: the full typed scenario run must prove the same behavior across every scenario family and U8 crash-harvest receipts.
- **Cargo unload leak requires soak-level recycle proof:** `cargo.unload.leaked` remains an evidence fact. Supervisor now records the recycle/quit path, but the lease must prove automatic before-next-load behavior under repeated verify, not just single-scenario cleanup.
- **Command history is proven; command-window capture buffer remains empty:** `blocks.CommandHistoryRail` proves non-empty Rhino command history (`Command: _SelNone`) inside a disposable document. `RhinoApp.CapturedCommandWindowStrings()` still returned an empty capture buffer in the same run. Decide whether the bridge contract requires command-window capture specifically, or rename the evidence contract to command history and keep capture as best-effort.
- **PRODUCTION defect (parity matched-red, 2026-06-12)**: `VectorIntent.DiscreteCalculus` over an open 1x1 quad-plane MeshSpace returns `InvalidResult` on RhinoWIP 9.0.26160 (identical on old and new rails; first live run since the execute-lane restoration). Owner: `libs/csharp/Rasm/Vectors/Spectral.cs` assembly guard / quad handling. Library internalizes degeneracy — fix production, not the scenario.
- **Vector scenarios still lack document ownership on the bridge path:** `bridge verify vectors.SpectralDec` now reaches supervisor/cargo but fails before the production DEC assertion with `scenario.doc.absent`. Owner is the vectors scenario/testkit contract: scenarios requiring a document must create a disposable `DocumentScope` explicitly, not rely on Cargo global doc creation.
- **Quit ladder blocks on unsaved-changes modals (observed live 2026-06-12)**: a scenario that dirtied the Rhino doc AND a GH2 definition produced TWO save prompts at quit; Apple-Event terminate waited on them until a human clicked Discard. The current shell scrub clears open Rhino docs and calls `Unmodify()` on every `Grasshopper2.Doc.Document.AllDocuments` entry, but the dirty-Rhino + dirty-GH2 U6 row has not been rerun. Acceptance: unattended quit, zero prompts, zero crash markers on next launch. Scenario teardown discipline is the second layer (DocumentScope-equivalent must cover GH2 docs), but the quit scrub must not depend on well-behaved scenarios.
- U8 cargo live soak BLOCKED during the rebuild (cargo compiled green; the 10-swap unload soak + kill-mid-scenario spool harvest never ran live) — MUST pass before cutover sign-off (SEQ-2 gate, not optional).
- **Root litter / TypeScript verifier artifact leak (reported 2026-06-12)**: a root `.ts-verify` artifact was observed during agent work; 2026-06-14 sweep found none. Keep only the frozen-tree litter sweep so root dotfiles and known verifier/temp names cannot silently recur. Preferred fix if reproduced: no durable file at all, or route through `.artifacts/assay` / owner-declared `.cache`.
- **C# SARIF evidence is incremental-build sensitive:** a cold `static` run emitted source/generated analyzer SARIF rows, while the immediate warm `static --all` emitted zero SARIF files and reported clean. Static must either force the analyzer-producing compile path for evidence runs or make the absence of expected SARIF an explicit typed fact; a warm incremental skip must not masquerade as "no analyzer diagnostics."
- **Generated Thinktecture `CS0436` evidence remains noisy:** `static --all` now folds source diagnostics cleanly, but generated `obj/**/Thinktecture.Annotations.g.cs` rows still group into 40 `CS0436` evidence rows against `Rasm.TestKit`. They are evidence-only today; decide whether the owner is package/reference layout, generated-code suppression, or static evidence projection before treating the full static report as noise-free.
- Pre-existing NETSDK1206 warnings (Microsoft.ML.OnnxRuntime.Extensions RID assets) ride every full build — not ours, but the frozen-tree gate should confirm they are the ONLY warnings.

## Upstream issues worth filing

### [UPSTREAM] mutmut stats-phase crash — fixed locally (`max_stack_depth = -1`); issue still worth filing
The previously documented rootdir/cwd theory is **falsified** (the captured stats argv is well-formed and pytest loads the parent pyproject config — the prepended addopts prove it). Root cause, established empirically with `debug=true`: with `max_stack_depth != -1`, `record_trampoline_hit` (`mutmut/__main__.py:130`) walks caller frames calling `Path(co_filename).resolve(strict=True)`; module-level calls into mutated code during conftest import put `<frozen importlib._bootstrap>` pseudo-filenames on that stack, the strict resolve raises `FileNotFoundError`, the conftest import dies, pytest exits 4, and mutmut raises `BadTestExecutionCommandsException`. The local fix is the upstream-default `max_stack_depth = -1` (repo had 10), which disables the strict-resolve stack walk entirely. Anyone with `max_stack_depth != -1` plus import-time calls into mutated code still hits this — file upstream as-is:

> **TITLE**: record_trampoline_hit crashes with FileNotFoundError on '<frozen importlib._bootstrap>' frames when max_stack_depth != -1 and mutated code runs at import time.
> **BODY**: With max_stack_depth set (e.g. the README's max_stack_depth=8), record_trampoline_hit (mutmut/__main__.py, the `while c and f:` loop) calls Path(f.f_code.co_filename).resolve(strict=True) on every caller frame. When a mutated function is invoked at module import time (module-level constant computed by a mutated function, imported transitively from a conftest), the caller stack contains importlib machinery frames whose co_filename is the pseudo-filename '<frozen importlib._bootstrap>' (also '<frozen importlib._bootstrap_external>', '<string>'). strict=True raises FileNotFoundError('<cwd>/<frozen importlib._bootstrap>'), the conftest import aborts, pytest exits 4, and mutmut raises BadTestExecutionCommandsException — misleadingly suggesting the pytest command itself is malformed.
> **REPRO (minimal)**: src/m.py with `def cap() -> int: return 1`; src/pkg using `X = cap()` at module level; a conftest.py importing pkg; [tool.mutmut] source_paths=['src'], max_stack_depth=8; `mutmut run` -> stats phase exit 4 with the traceback above; with max_stack_depth=-1 the run proceeds.
> **OBSERVED ON**: mutmut 3.6.0, pytest 9.0.3, CPython 3.15.0b1, macOS.
> **FIX CANDIDATES**: (a) skip frames whose co_filename starts with '<' before resolving; (b) resolve with strict=False; (c) wrap the resolve in try/except FileNotFoundError and continue; (d) break on importlib frames the same way 'pytest'/'unittest' filenames break the walk. Additionally, the exit-4 path would be far easier to debug if BadTestExecutionCommandsException included the captured pytest output even without debug=true.

When filing, also mention the forked-children consequence: mutant workers fork from the post-clean parent, so any set-once global poisoned across in-process `pytest.main()` calls silently converts test-infrastructure failures into false "killed" verdicts — an argument for subprocess-isolated stats/clean phases in mutmut core.

### [UPSTREAM] cyclopts help renderer crashes on Enum-hinted params with None defaults
cyclopts 4.16.1 `help/help.py` (the `if argument.show_default:` block) renders Enum-hinted defaults via `default_val.name` BEFORE consulting any `show_default` callable, so `language: Language | None = None` crashes `--help` with `AttributeError: 'NoneType' object has no attribute 'name'` regardless of the callable form (`show_default=lambda d: "" if d is None else d.name` was tried and is structurally unreachable for Enum hints). Local posture: `Parameter(show_default=False)` on the field with a source-cited comment; the law `test_main_enum_param_help_renders` pins the regression. Revisit on cyclopts bump.

## Code smells / weak sections (flagged, deliberately not fixed)

### `tools/assay`
- **`Diagnostic.failing_step` carries `Step` members in-process but plain `str` after a wire round-trip** — `==`/`!=` comparisons are safe (StrEnum), identity (`is Step.X`) on decoded envelopes silently fails. The one existing comparison site uses `!=`; flag if identity comparison is ever introduced.
- **[DONE 2026-06-14] Bridge envelope fact projection was verify-heavy** — doctor/quit/launch/check/clean now fold a `BridgeLifecycle` Detail projecting the bridge-owned `Host`/`Capabilities` facts uniformly (no lifecycle logic recreated in Python). Live `bridge doctor` proof of the `kind:bridge` detail is Phase-6.

### `tools/rhino-bridge`
- **[DONE 2026-06-14] Manual poll/quit loops collapsed** — the 3 hand-rolled `while/Thread.Sleep` loops now route through one named-kernel `Poll.Until` (HostControl.cs), policy-cadence-driven. v5 `Schedule` verified as the wrong primitive for a sync native probe (see OPEN WORK item 3).
- **[DONE 2026-06-14] Endpoint path ownership one-hop + poison-record dedup** — `SessionRun.ReadLiveEndpoint()` resolves through `EndpointRecord.EndpointPath` (Session.cs:333). The endpoint poison shape now folds into `EndpointRecord` (7th `Fault` member): `ShellHost.WritePoisoned` serializes one typed record through the shared converter, eliminating the Shell-side hand-rolled writer and closing the converter's read-after-write asymmetry. The `Stub.cs` writer stays as the lone dependency-zero isolation-seam exemption (it is the only writer reachable when the shell DLL fails to load).
- **[DONE 2026-06-14] `SessionFold.Divergence()` typed JSON** — folds through the `EvidenceDivergence` record + STJ source-gen (Session.cs:562-579), not interpolated-then-reparsed text.

### `tests/python` (carried intent flags)
- **`SeamProbe.projected[K]` unused** in the assay specialization (`commands`/`checks` read `captured` directly). Retained as a real engine affordance (post-hoc `calls`-log view distinct from the per-call `project` stream) — flag so a reviewer does not strip it as dead.
- **MemoryViewType identity asymmetry** — `st.binary().map(memoryview)` round-trips by equality (msgspec decodes to `bytes`); no consumer compares by identity and no memoryview wire field exists — inert, one-line note marks the arm.

## Rejected-with-rationale (2026-06-11 closeout ledger)

Static rail closeout landed 2026-06-12; the `check`/`build`/`fix` verbs were COLLAPSED 2026-06-14 into ONE polymorphic root-leaf `static`
verb (auto-fix -> diagnose -> restore -> build per language, value-driven targets). `plan`, `report`, `full`, positional paths, `--language`,
implicit project paths, solution paths, root-trigger escalation, and the per-verb `check`/`build`/`fix` split are all retired. `StaticRun` remains
the route/check/resource detail owner; one `run` entrypoint resolves targets purely by value (`--all`/`--project`/`--folder`/`--file`/changed-default)
and runs every language's fix-then-diagnose-then-build lane in one envelope.

| Item | Rationale |
|---|---|
| `code rewrite` verb (DROPPED, not deferred) | zero scoped usage since the run-id scheme; preview ≈5 LOC over raw ast-grep; apply's lease value has no named consumer. Revival: one `Bind` row + 2 catalog WRITE rows + one splice, ONLY with a named lease-guarded-codemod consumer |
| code-rail tooling replacement | none needed — ast-grep 0.42.1 / ripgrep 15.1.0 / tree-sitter 0.25.2 all current; deprecated `timeout_micros` already absent |
| tree-sitter parse hang guard via `progress_callback` | tree-sitter 0.25.2: bytestring sources warn "progress_callback is ignored" (raises under `filterwarnings=error`); the callable-source variant SIGSEGVs (exit 139, reproduced 2×, 2026-06-11) even with a benign callback. No false hang-protection; `match_limit` saturation handling kept |
| help-truth `show_default` callable | cyclopts 4.16.1 Enum branch crashes before consulting the callable (see upstream issue above) |
| structured self-test detail | new wire `Detail` type not warranted without a named consumer; `yak_ready=` token landed in the summary note instead |
| `api doctor` structured `sources` tuple (archive GAP-3) | 298 per-source structs is wire bloat; Match rows carry the data — stabilize their text contract instead (smell above); rhinocode probe is bridge-rebuild-owned |
| `_recv_*` / `_write_*` trios collapse | legitimately distinct algebra (stream sources; POSIX short-write vs truncate+encode vs owner build) |
| cyclopts `resolve_returncode` | hand-rolled `_returncode` carries the int() arm the helper lacks |
| cyclopts `config.Env(prefix='ASSAY_')` | splits config ownership with pydantic-settings; one resolver owns `ASSAY_*` |
| pydantic-settings PyprojectTomlConfigSettingsSource | config sprawl; pyproject already read directly where needed |
| `msgspec.json.schema(Envelope)` verb | new surface, marginal agent value; `core/model.py` is the authoritative contract |
| beartype `claw_skip_package_names` swap | equivalence with the load-order workaround unproven; working code stays |
| psutil getloadavg / memory_percent swaps | ty-ignore is load-bearing cross-platform; hand-rolled ratio avoids nothing real |
| `_FAN_LIMITER` live governor resampler | speculative, no named consumer |
| process_iter create_time guard in hygiene scan | best-effort scan; marginal hardening vs budget |
| expression `traverse()` ×7 swap | zero behavioral gain, 7-site churn risk |
| `beartype.door.is_bearable` in `_spec` | pattern-match already discriminates; adds ceremony |
| single-caller inlines (`_checked_report`, `_identity_hom`) | named for layer-table legibility |
| SUT-profile registry in testkit | re-rejected (W1): speculative scaffolding |
| asyncssh connection-pool refactor | no public pool API in asyncssh 2.23; the engine's dict+lock cache IS the pattern |
| loadavg backpressure (re-rejected) | macOS load1 counts uninterruptible waits; foreign dotnet-process census landed instead (engine `_foreign_dotnet_count`) |
| free-threading / PEP 734 interpreters / 3.15 asyncio subprocess | GIL build; fan is IO-bound; INPROC thunks unshareable; no surface via anyio (static-opt research 2026-06-12) |
| analyzer vocabulary as AdditionalFiles .txt | public-analyzer pattern (BannedApiAnalyzers) wrong for a repo-local ProjectReference'd tool; vocabulary lives as typed constants (`Kernel/Vocabulary.cs`), syntax-aware hygiene scan exempts string literals |
| `typing.cast` at untyped client boundaries | TID251; `@runtime_checkable` Protocol + match narrowing validates at runtime (env.py boto3 admission) |
| `_steal` won/lost law-pair collapse | distinct lock topologies (uncontended vs nested-exclusive-contended) and distinct assertions share no clean projection; at 2 rows the originals read denser |
| `retain_history`/`retain_scopes`/`sorted_history_ids` one-hop collapse | three public verbs carry real vocabulary; a single erased `retain(root, keep)` weakens the surface for two delegation lines |
| Stub `ProjectReference` to Contract, OR source-linking a shared `EndpointPaths.cs` into both (2026-06-14) | the stub is dependency-zero by charter (`UseWorkspaceLibraries=false`, "Dependency-zero RhinoWIP stub"). A Contract reference would pin Contract + its StreamJsonRpc closure (MessagePack/Nerdbank/PolyType/Thinktecture — NOT LanguageExt, verified absent) into the default ALC (co-resident-plugin identity unification) and split `EndpointRecord`'s identity from the collectible shell ALC. Source-linking the same file into Contract.dll + Stub.dll manufactures two same-FQN `EndpointPaths` types in the default ALC — the very identity split the boundary prevents — plus an invisible build edge. RESOLUTION: the duplication was killed on the Shell side instead (folded `fault` into `EndpointRecord`; `WritePoisoned` routes through the typed converter); the stub keeps ONE inline writer as the necessary isolation-seam exemption (the only writer reachable on shell-load failure), with a corrected "why" comment |

## Deferred (real, intentionally held — not yet warranted)

- **MCP integration — VERDICT `additive_external` (investigated 2026-06-14, high confidence).** Rasm owns ZERO MCP server code. The official "Rhino MCP" is McNeel's **`Rhino-MCP-Platform`** (`mcneel/rhinomcp`, MIT, first release 2026-05-27, plugin v0.1.5) — a C# in-Rhino plugin + bundled stdio router that coding agents connect to. The bridge ALREADY honors the complement verdict in shipping code: suppression `RHINO_MCP_AUTOSTART_PORT=0` (HostControl.cs Launch) + two doctor facts `mcp.listener`/`mcp.platform.version` (ShellHost.cs). The two lanes are a CATEGORY boundary, not build-vs-buy: Platform = interactive/conversational modeling; bridge = deterministic typed verification. INSTALL is an operator/per-machine step (Rhino `PackageManager` -> `Rhino-MCP-Platform`, run `MCPConnect`, register the stdio entry with Claude Code) — do NOT commit a project-scoped `.mcp.json` (implies a repo build-dependency). TWO code follow-ups worth doing: (1) harden suppression from FACT to GATE (`McpListener()` returns Unsupported unconditionally today); (2) disambiguate `McpPlatform()` (ShellHost.cs ~312-330) — its `'mcp'` substring assembly scan cannot tell McNeel's `rhinomcp` from the community `jingcheng-chen/rhinomcp` (both ship under that name), so pin the McNeel assembly identity before that fact is gated on. The stdio FACADE (bridge-speakable-as-MCP) stays DEFERRED — revive with `ModelContextProtocol` 1.4.0 ONLY for a NAMED shell-less consumer.
- **`tools.py_analyzer` law suite** — the pilot SUT is registered (two-line conftest) with a 15-symbol exempt debt ledger; authoring its laws is its own future work, gated per-package by the now-package-scoped coverage gate.
- **`de_eq` fuzzy-eq adapter — deleted 2026-06-09 (unconsumed)**: revival is one line — return the `IsApprox` instance directly handed to oracle `eq` kwargs as right-operand comparator. Revive only with a named consumer.
- **`register_random`** — still blocked, re-verified 2026-06-09: no private `random.Random` seam exists in the engines; the `tests/python/_testkit/runtime.py` comment names the trigger + the hypothesis-weakref gotcha. Call `register_random(rng)` (strong ref) the moment such a seam is introduced.
- **Coverage per-test contexts** — `--cov-context=test` is documented opt-in (pyproject) but not in default addopts (coverage's dynamic context hard-errors under `--cov` and blocks `-n auto`). Wire it if per-test line attribution becomes worth the constraint.
- **CrossHair symbolic lane** — still blocked, re-verified 2026-06-09: crosshair-tool 0.0.106 publishes cp38–cp314 wheels only, no cp315 wheel. `rasm-symbolic` profile stays documented in the runtime plugin; enable when a cp315 wheel ships.
- **xdist parallel lane (`-n 2` / `-n auto`)** — still broken, re-verified 2026-06-09: both exit 3 with 32 `INTERNALERROR` lines; `pytest_benchmark.logger.PytestBenchmarkWarning` is escalated to an xdist-worker INTERNALERROR by `filterwarnings = ["error"]`. Benchmark and xdist sessions stay separated.
