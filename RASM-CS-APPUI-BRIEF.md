# [RASM_CS_APPUI_BRIEF]

[TRACK_ORDER]: Geometry (1) -> Persistence (2) -> Compute (3) -> AppHost (4) -> **AppUi (5)** -> Fabrication (6) — run the cs campaigns in this order; this campaign is 5/6, after the AppHost campaign lands.

Authoritative campaign input for the restructure-and-harden of `libs/csharp/Rasm.AppUi/.planning/` — the APP-PLATFORM Avalonia product UI engine (37 pages, 5 folders: Shell 10 · Render 9 · Charts 2 · Editing 10 · Theme 6). Upstream law honored throughout: `RASM-CS-APPHOST-BRIEF.md` (the composition-root exports, seven-port vocabulary, and `FaultBand` registry AppUi composes as settled — never re-mints), `RASM-CS-PERSISTENCE-BRIEF.md` (the durable spine: `OpLogEntry`/`SyncOpKind` op-log, `CrdtOpWire` bit-parity law, blob lane, `Version/ledger` changefeed — AppUi projects and decodes, never persists a second CRDT), `RASM-CS-COMPUTE-BRIEF.md` (the `ResidencyPayload` meshopt cluster wire and `ONE_WGPU_DEVICE` — AppUi consumes clusters, never re-partitions), the landed kernel corpus (`libs/csharp/Rasm/.planning/` — one root, nine folders; standing law composed as LANDED FENCES through AppUi's material kernel reference `CSPROJ:8`: the `Domain/identity.md` `ContentHash.Of` federation hasher every AppUi content-identity mint composes under the `Rasm/ARCHITECTURE.md:87-92` one-hasher law, and the kernel `DrawingProjection` rebuild Fabrication consumes — AppUi stays on the Fabrication receipt; kernel members verify against the landed fences, never a restored assembly), and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149` (the `FaultBand` `[SmartEnum<int>]` shape; AppUi occupies its own neighborhood, never the AEC 23xx-27xx or APP-PLATFORM 4xxx registries). Seven upstream consumer-pressure clauses are landed and verified in place: the AppHost `[V1]` AppUi 6xxx pinned-mirror row, the AppHost `[V10]` `RecomputeGraph` granularity-neutral clause (per-cell composition kept caller-keyed, AppUi `[V11]` named), the AppHost `[V12]` secrets-lease signing-consumer clause (the `Document/export.md` `IDigitalSigner` material ingress, AppUi named), the AppHost `[V17]` topics/bus live-delta broadcast row (session-ephemeral Loro wire as opaque payload rows, AppUi `[V2]`b named, both sides declared), the Persistence `[V12]` ledger op-log + blob-lane consumer-pressure rows (AppUi edit-intent projection + replay-window read; the snapshot accelerator content-keyed and derivable), the Compute `[SEAM_AND_RAIL_LAW]` cluster-LOD consumer-pressure clause (the `payload.md` `meshlet-cluster` error columns + monotonic-error cut guarantee the AppUi LOD cut requires; the Compute campaign lands it on the page), and the Compute `[V12]`b solar-position package-boundary export (the `LightSource.Sun` azimuth/altitude seam, AppUi `[V6]` named). `docs/stacks/csharp/` governs.

## [EXECUTION]

Settled campaign, cs track 5/6, no DECISION pass — launched after the AppHost campaign lands (and the Persistence DECISION emits, for the collab-recharter signature-locks): the durable rebuild engine runs directly against this brief per the four-leg `[05]` partition — the `[05]` leg rows ARE the page-set source; one-run form `{brief, leg: [1, 2, 3, 4]}` lands all four behind per-wave barriers; each leg self-contained, its pass agents repairing the cross-file ripples their own work exposes in-pass and its serialized per-wave tail closing index docs and central manifests before it returns — and legs re-run across cold sessions against the same brief until a pass finds nothing.

| [STEP] | [LAUNCH] | [PRODUCES] |
| :----: | :------- | :--------- |
| 1 | `Workflow(rebuild, {brief: "RASM-CS-APPUI-BRIEF.md", leg: 1})` — PERIMETER + STRUCTURE FLOOR | `[V3]` structure motion to the 8-folder/43-page map + `[V1]` 6xxx registry + `[V9]` ledger corrections + `[V12]` catalog/manifest motion |
| 2 | `Workflow(rebuild, {brief: "RASM-CS-APPUI-BRIEF.md", leg: 2})` — VOCABULARY + SHELL SPINE | Theme vocabulary + Shell spine landed |
| 3 | `Workflow(rebuild, {brief: "RASM-CS-APPUI-BRIEF.md", leg: 3})` — RENDER PLANE | render plane landed |
| 4 | `Workflow(rebuild, {brief: "RASM-CS-APPUI-BRIEF.md", leg: 4})` — CONSUMER PLANES + CLOSEOUT | Collab/Document/Editing/Charts/Diagnostics planes + final `[V9]` governance truth pass + the `[05]` acceptance dry-runs — campaign landed |

## [00]-[SHARED_LAW]

### [VERDICT]

The 37 fence interiors are genuinely world-class — 18-case `[Union]` control families, seed-DATA rosters everywhere, `Fin`/`Validation`/`IO` rails, single-owner spines consistently consumed (`AppUiTelemetry.Contribute`, `ReceiptSinkPort`, `SurfaceSeam`, `VirtualWindow`, `CommandIntent`, `Viewpoint`, `TokenRow`), zero host-plumbing re-mints — and the PERIMETER around them is fiction. Six proofs, each independently fatal to a world-class claim:

1. **The fault-code space is collided, foreign-squatting, and dual-radix.** Three confirmed internal integer collisions: `TourFault` 4300-4302 (`Editing/tour.md:30-32`) ≡ `VirtualFault` 4300-4303 (`Shell/virtualization.md:48-51`); `FormFault` 4900-4903 (`Editing/forms.md:29-32`) ≡ `CaptureFault` 4900-4904 (`Render/reality.md:30-34`); `CollabFault` 0x4B00-0x4B05 (`Editing/collab.md:43-48`) ≡ `ContentFault` 0x4B00-0x4B03 (`Editing/media.md:28-31`). Five AppHost-registry squats: `SurfaceFault` 4100 (`Shell/hosts.md:43`) + `AssetFault` 4120 (`Theme/assets.md:29-32`) + `DialogFault` 4130 (`Shell/dialogs.md:39`) + `InputDriverFault` 4150 (`Shell/input.md:289-292`) all inside AppHost `ConfigError` 4100-4199; `ViewportFault` 4500-4504 (`Render/pipeline.md:32-36`) ≡ AppHost `HopFault` 4500; `DraftFault` 4600-4603 (`Render/drafting.md:271-274`) ≡ AppHost `CommandFault` 4600; `EditFault` claims the WHOLE 4700-4799 decade (`Editing/inspector.md:138,152-174`) — swallowing TEN AppHost bands (`FeatureFault`/`SolverFault`/`WireFault`/`BusFault`/`OutboxFault`/`LaneFault`/`ReplayFault`/`OrchestrationFault`/`SecretFault`/`PemFault`); `NotebookFault` 4800-4803 (`Editing/notebook.md:96-99`) ≡ AppHost `FederationFault` 4800; `IssueFault` 5000-5003 (`Editing/issues.md:29-32`) inside Persistence 5xxx. Plus a decimal/hex FORK (`HistoryFault` 0x4A00, `CollabFault` 0x4B00, `ShaderFault` 0x4C00, `ImmersiveFault` 0x4D00 — 0x4A00 = 18944, not decimal-adjacent to 4900). Every code is a `base(detail, NNNN)` literal — the exact form the federation GENERATOR_LAW bans — and NO registry exists. Every AppUi fault also crosses the shared `ReceiptEnvelope`/`EvidenceTimeline`, so disjointness is load-bearing telemetry identity, not hygiene.
2. **The durable collaboration spine is the exact form the Persistence law forecloses.** `Editing/collab.md:105` designs `SyncRail` durability as "the local delta projects onto the Persistence `OpLogEntry` ... and Persistence replays the opaque delta blob without re-modeling the CRDT"; `:117-120` content-addresses the raw Loro `Export(Snapshot)` bytes as THE durable document state; `api-loro.md:171-172` seals the same stacking. That mints a SECOND durable CRDT representation — the "Rust-binding CRDT whose wire is its own" `RASM-CS-PERSISTENCE-BRIEF.md:49` records as rejected and `:204` re-seals ("LoroCs is rejected for this package (wire bit-parity law)"). `Editing/history.md` proves the correct discipline on the SAME seam (typed `RevertibleOp` deltas -> `SyncOpKind`); collab, notebook (`notebook.md:178`), and issues (`issues.md:123`) all ride the rejected form.
3. **The merge authority is an orphan its own corpus contradicts.** `collab.md` (212 LOC, the merge authority for notebook/issues/tables/livedata) is ABSENT from the `ARCHITECTURE.md` codemap (36 nodes, `ARCH:10-52` — no collab), the seam ledger, and the `README.md` router (36 rows). And `Editing/issues.md:118-146` ships a full bespoke HLC last-writer-wins CRDT (`CommentOp` `[Union]` + `CommentThread` `Apply`/`Merge`/`Log`) that `collab.md:20` explicitly DROPS root-up ("the `CommentThread`/`CommentOp` register are DROPPED root-up"; `:17` "an issue comment thread is a `map` container keyed by comment GUID") — `issues.md:123` still cites the dead `notebook.md#CRDT_COEDIT` law. The collab refactor reconciled notebook and never issues.
4. **Settled Render fences are decorative.** `Render/meshlets.md:71-81` `Partition` assigns EVERY meshlet the whole-mesh `Bounds`, a never-cull `NormalCone(0,0,1,-1)`, and `ScreenSpaceError = mesh.Bounds.Radius` — cull is non-functional while `:14` claims per-meshlet bounds/cones and an edge-collapse LOD tree, and while `pipeline.md:463-470,542-556` consumes Compute's meshopt-built clusters whose wire carries REAL cone apex/axis/cutoff. `Render/pathtrace.md:33-36` `Bvh.Split` never recurses (the root claims children never emitted) and `:64-67` `Accumulate` traces nothing under a "correctness oracle" claim (`:3`). `Shell/virtualization.md:127-131` `Reaccumulate` is an O(n) prefix rebuild under an O(log n) Fenwick claim (`:78`). `Shell/controls.md:181-184` `Rebind` type-checks and resets nothing (the recycling stale-state core). `Render/reality.md:46-49` re-hashes splat components through a local `XxHash128` against `pipeline.md:404,496`'s single-mint law, and its `SplatSort` enum (`:54-58`) is never read.
5. **Three admitted packages have zero page consumers while their concerns are hand-rolled or unwired.** `PDFsharp`+`PDFsharp-MigraDoc` (`csproj:98-99`, `props:362-363`): zero consumers while `Render/capture.md:274-340` hand-rolls the `FlowBlock`/`FlowFold` pagination engine MigraDoc's flow DOM owns and `:478` emits PDF via `SKDocument` with none of PDFsharp's Security/Signatures/AcroForms/UA surface — `api-pdfsharp.md:155,161` names consumers that do not consume. `Mapsui.Avalonia12` (`csproj:117`): zero consumers while `ARCH:67` declares the Bim NTS basemap seam no page realizes. `NodeEditorAvalonia` (`csproj:118`): zero consumers while README promises the parametric/dependency-graph canvas no page owns. Each is an integration MANDATE — absence of consumers is never a REMOVE reason.
6. **The seam ledger is stale in both directions.** Phantom source nodes: `Render/glb` (`ARCH:63`) and `Render/query` (`ARCH:66`) name pages that do not exist. Dead vocabulary: `ARCH:70` cites `NotebookOp` — dropped by `collab.md:20`. Three rows target `Rasm.Persistence/Sync` (`ARCH:69,70,80`) — a folder the Persistence campaign DELETES (owner: `Version/ledger`). Declared-unwired: `ARCH:72` Geometry `DrawingProjection` (drafting composes the FABRICATION seam, `drafting.md:308-310`; the landed kernel `Drawing/view.md` confirms Fabrication is the kernel engine's consumer), `ARCH:73` `ChartAtlas`, `ARCH:67` basemap. Wired-undeclared: collab -> Persistence ledger, collab -> AppHost transport, and the entire AppHost port spine (`TelemetryContributorPort`/`ReceiptSinkPort`/`UiSchedulerPort`/`ClockPolicy`/`DataClassification` — composed on ~30 pages, ledgered nowhere beyond `ARCH:65`'s one notebook row).

What IS sound (preserve; harden ONLY per the verdict that names it): `Shell/hosts.md` (10 — the host-neutral mount exemplar; mandate item 3's composition law disposed), `commands.md` (9.5 — the one command rail), `screens.md`/`controls.md`/`solver.md`/`virtualization.md`/`input.md`/`navigation.md`/`dialogs.md`/`accessibility.md` (8.5-9), `Render/pipeline.md` (9 — pass-DAG + backend factory + resolve ladder + BCF `Viewpoint` codec + the ONE `ResidencyManifest.Mint`), `capture.md` (9 — float-end-to-end color management), `drafting.md` (9 — painter sort dropped root-up, `HiddenLineSeam` composing Fabrication's `#PROJECTION_HIDDEN_LINE`, zero painter-sort remnant corpus-wide: mandate item 2 DISPOSED), `evidence.md` content (9 — mis-filed, not weak), `Editing/inspector.md` (9.5 minus the diff), `tables.md`/`livedata.md`/`forms.md`/`history.md` (9 — history is the correct-durable-discipline exemplar), `notebook.md` (8.5), `Charts/dashboards.md`/`custom.md` (9 — the `DimensionIndex`/`CrossFilter` bitmap engine and 14-case layout-fold algebra are award-grade), `Theme/tokens.md`/`typography.md` (9.5 — the Unicolour OKLab delegate law and one-shaping-rail are exemplary), `motion.md`/`assets.md`/`locale.md` (9), `animation.md` content (9 — mis-placed), and a 57-catalog `.api` tier whose craft is 8-9 (dense, TFM-honest, verified-real) with defects that are RELATIONAL, not intrinsic. Rosters are seed DATA corpus-wide — zero enumerated-instance defects found on the generator axis. The rot is the PERIMETER — bands, the durable seam, folder placement, ledger truth, three unconsumed admissions — and no per-page cold pass fixes the perimeter. Perimeter law first.

### [TELOS]

`Rasm.AppUi` is the federation's product surface: the host-neutral Avalonia shell (mount axis, one command rail, one control vocabulary, constraint layout, one virtualization fabric, full a11y), the GPU viewport engine (pass-DAG over one wgpu device, cluster-LOD residency, path-traced appearance from Materials BSDF, reality capture, OpenXR), the 2D documentation surface (sheets, GD&T, hidden-line consumption, DWG/DXF/PDF emit), the chart/dashboard plane (LiveCharts rail + bespoke Skia algebra + cross-filter engine + geo basemap), the typed-edit plane (inspector, tables, forms, revert algebra, reactive data spine, node-graph canvas), the document plane (reproducible notebook, media, paginated Office/PDF export), the live-collaboration plane (Loro live merge + presence + time travel over a Persistence-owned durable spine), the diagnostics plane (one evidence union, correlation, headless proof, dev loop, quality governor), and the theme vocabulary every literal traces to — one body. Every owner grades against the 5x consumer bar: every screen windows through the one `VirtualWindow`, every fault crosses the shared envelope under one registry, every collaborative surface converges through one merge authority whose durable truth replays in ANY runtime, every visual export is color-managed and print-honest, every viewport tile streams from Compute's content-keyed payloads. World-class additionally means the fences are HONEST: a cull that never culls, a BVH that never recurses, a Fenwick claim over a linear scan, and a durable claim over a process-local blob are each fiction — the fence is the product, and a fence that lies about being settled is a defect, not a deferral. Zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The legs own the page-set within the verdict envelope. The post-campaign map is SEALED so no leg guesses a name — 8 folders, 43 pages, zero one-file folders, every folder a growth axis:

- `Shell/` 10 — unchanged (navigation, screens, hosts, commands, controls, solver, virtualization, dialogs, input, accessibility).
- `Render/` 9 — the pure GPU-viewport + temporal tier: pipeline, meshlets, pathtrace, shading, immersive, reality, capture (post-`[V3]` extraction: raster capsule + codec/color only), drafting, animation (IN from Theme — the timeline/playhead/walkthrough runtime engine; Theme keeps only vocabulary).
- `Charts/` 3 — dashboards, custom, + NEW `basemap.md` (`[V8]`).
- `Editing/` 6 — the typed-edit tier: inspector, tables, forms, history, livedata, + NEW `graph.md` (`[V8]`).
- `Document/` 3 (NEW folder) — notebook (in from Editing), media (in from Editing), + NEW `export.md` (`[V3]`/`[V8]` — the Office/PDF flow-export owner extracted from capture).
- `Collab/` 3 (NEW folder) — `sync.md` (in from `Editing/collab.md`, re-chartered per `[V2]`), issues (in from Editing), tour (in from Editing).
- `Diagnostics/` 4 (NEW folder) — `evidence.md` (receipt union + correlation join), `proof.md` (capture lanes + headless derivation + `ProofLaw`), `devloop.md`, `governor.md` — the `Render/evidence.md` 456-LOC six-concern split (`[V3]`).
- `Theme/` 5 — tokens, typography, motion, assets, locale.

A move is a rebuild opportunity, never a bare `git mv`: the moving page re-fences against its new folder charter in its owning leg. Cross-leg absorbs run two ruled mechanics, both absorber-owns-first: a MOVE-absorb (capture -> export) transfers the donor sections verbatim into the absorber's skeleton at the leg-1 structure motion — condemned-but-intact THERE — so the donor rebuilds genuinely post-extraction in its own leg and the absorber's leg rebuilds the transferred content ground-up; a COLLAPSE-absorb (tour -> animation) transfers nothing — the absorber already owns the target fold (`animation.md:194-238` `WalkthroughSpec`/`Walkthrough.Render`), its Render leg hardens it, and the donor's later leg deletes the clones and re-fences as projection. Every structural change updates `ARCHITECTURE.md` `[01]`-codemap/`[02]`-seams and the `README.md` router and package groups in the same motion. No page survives on incumbency, and no page falls to it either: a 9.5-grade fence is rebuilt only where a verdict names its defect.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md`): AppUi is APP-PLATFORM, depending UP on `Rasm` (kernel) only — the strata law fixes AppUi's reference set as kernel+AppHost+Compute+Persistence, and the `Rasm.Element` seam, though legal app-platform substrate, has ZERO AppUi page consumers on disk: an `ElementGraph` crossing admits it wire-shaped when a page names one, never speculatively — PORT-peering with `Rasm.AppHost`/`Rasm.Persistence`/`Rasm.Compute`, consuming AEC-domain contracts (`Rasm.Bim` BCF/geospatial/planning, `Rasm.Materials` appearance, `Rasm.Fabrication` posting receipts) as boundary wire only, and it is NOT the app composition root — host binding (Rhino/GH2), exporter pins, and process attachment belong to the APP stratum. Consequences sealed here: (a) AppHost exports are settled vocabulary — `TelemetryContributorPort`, `ReceiptSinkPort`, `UiSchedulerPort`, `ClockPolicy`, `CorrelationId`, `TenantContext`, `DeterminismContext`/`RecomputeGraph`, `DataClassification`, `FaultBand` — composed, never re-minted, with every crossing a declared `[V9]` ledger row; (b) Persistence results are decode-only (the AppHost `[V2]` law binds AppUi symmetrically): the op-log rows, replay windows, blob receipts, and conflict receipts are Persistence-owned types — no AppUi interface or type crosses down; (c) the in-Rhino ALC firebreak stays law — `loro.dylib` and every companion-only native never enters a plugin ALC (`collab.md:20`); (d) `ONE_WGPU_DEVICE` is shared with Compute at the declared seam (`ARCH:81`), never a second device; (e) the kernel is the one below-port sibling composed as DIRECT compile surface (`CSPROJ:8`), bounded by the RhinoCommon compile firebreak — AppUi is deliberately NOT RhinoCommon-aware (`Directory.Build.props:46` scopes host-reference inheritance to the kernel and the Rhino/GH roots), so AppUi composes exactly the kernel members whose signatures are Rhino-type-free (`Rasm.Domain` `ContentHash.Of(ReadOnlySpan<byte>) -> UInt128` is the canonical composable entry) while a Rhino-typed kernel member (`SpatialIndex.Build` over `BoundingBox`, `VectorIntent.Pose` over `Plane`) is boundary-unreachable and its concern lands as an AppUi-local measured kernel or arrives as wire, never by widening AppUi's host-reference surface.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — `ControlIntent`(18), `ChartSeriesSpec`(15), `CustomVisual`(14), `EditorFactory`(11), `TypographyRole`(10), `SheetSize`, `GpuBackend`, `TokenRow`, `LocaleRow` are all `[Union]`/`[SmartEnum]` + FrozenDictionary seed DATA — so the law binds the NEW planes: fault bands are rows in ONE type-enforced registry where a duplicate integer fails at type initialization (`[V1]`), never per-page literals; collaborative containers are `CollabContainer` rows, never new CRDTs; residency tiles are Compute payload rows, never local re-clustering (`[V5]`); export destinations are rows on one export owner (`[V8]`); basemap layers are rows over one `MapControl` surface; gamut/transfer is ONE color-policy row family (`[V10]`); track interpolation is a per-`Track`-case policy row — the Camera row the ONE pose-interpolation site AppUi-wide, the Color row the tokens OKLab mix (`[V6]`). The test: the next fault, container, tile kind, export target, chart series, device driver, or interpolation law is one data row, never a new arm, a loose integer, or a second engine.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — the corpus norm (`CommandDeck.Freeze`, `ControlFactory`, `SurfaceHost` mount, `VirtualWindow`, `Assess`-shaped folds), sealed for every new owner.
- Every fault is a typed `[Union] : Expected` case whose `Code` derives through the `[V1]` registry row; bare `Error.New` on a rail is the deleted form — the live breaches are `navigation.md:46,81`, `locale.md:172-173`, `dashboards.md:296,304`, `custom.md:168-231`, `animation.md:62`.
- Durable collaboration is decode/replay at the boundary: the edit-intent op stream is Persistence-owned rows (`[V2]`); a Loro-native byte persisted as system-of-record is the deleted form; the snapshot blob survives only as a derivable content-keyed accelerator.
- Content identity is single-mint and ONE-HASHER: residency and splat tiles key by the Compute payload's own `ContentKey` — decode-only, a local `XxHash128` over raw component floats (`reality.md:46-49`) is the deleted form (`pipeline.md:404,496` is the law) — and every AppUi-SIDE content-identity mint composes the kernel `Rasm.Domain` `ContentHash.Of` seed-zero entry (`Rasm/.planning/Domain/identity.md`, the `Rasm/ARCHITECTURE.md:87-92` federation one-hasher law): the capture runtime `ContentHash` delegate binding (`capture.md:84,162`) and with it the walkthrough per-frame proof, the command payload digest (`commands.md:197`), the notebook replay-input hashes (`notebook.md:243`), and the `[V2]` snapshot-accelerator key — a page-local `XxHash128.Hash` call site is the deleted form, hex/wire encodings stay boundary projections at the consuming seam, and the TS `RenderReceiptWire` frame-hash proof (`ARCH:60`) thereby reproduces the one federation seed exactly as the kernel's typescript/python seed rows demand (`Rasm/ARCHITECTURE.md:91-92`).
- Consumer contracts are RECORDED where the owner is upstream, never re-homed: meshlet clusters and their LOD/error columns are Compute `payload.md` rows (waterfalled, AppUi named); the edit-intent op-log ingest and replay-window read are Persistence `Version/ledger` rows (waterfalled); the AppUi 6xxx band neighborhood is an AppHost registry mirror row (waterfalled); the signing-credential lease is an AppHost `Runtime/secrets.md` export row (waterfalled, AppUi named); `HiddenLineResult` stays the Fabrication Posting receipt, insulated from the Geometry `[V3]` engine supersession beneath it.
- Cross-package member claims verify or die: `uv run python -m tools.assay api` over restored assemblies is truth for every composed external-package member (LoroCs `SubscribeLocalUpdate`/`ExportMode`/`EphemeralStore`/`EventTriggerKind`, LiveCharts `Xaml*Series`, MigraDoc `PdfDocumentRenderer`/`FormattedDocument`, PDFsharp `DigitalSignatureHandler`/`IDigitalSigner`, Mapsui `MapControl`/`GeometryFeature`/`SphericalMercator`, NodeEditor `IDrawingNode`/`DrawingNodeEditor`, ACadSharp `DwgWriter`/`DxfWriter` entity set); KERNEL members verify against the LANDED KERNEL FENCES — the planning-scoped kernel restores no assembly, so the landed `libs/csharp/Rasm/.planning/` fences are the kernel verification spine (`ContentHash.Of` at `Domain/identity.md`; the `Spatial/index.md:15,157,536` `SpatialIndex` and `Processing/intent.md:165` `VectorIntent.Pose` surfaces verify as Rhino-typed and therefore firebreak-unreachable per `[PLACEMENT_LAW]`(e)); a member you cannot verify is a phantom.
- Prose-fence parity is law: a Boundary paragraph describing behavior the fence does not encode (the 40-60-line run-ons at `dashboards.md:20`, `custom.md:19`, `collab.md:20`, `capture.md:165`, `evidence.md:278`) either lands in the fence or converts to an explicit research gate.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped `[App UI]` block at `:320-386`, ~66 pins) + `Rasm.AppUi.csproj` (14 label groups); per-package catalogs under `libs/csharp/Rasm.AppUi/.api/` (57), shared substrate under `libs/csharp/.api/`. The roster is COMPLETE and otherwise fully consumed — the consumed-census claim breaks exactly three times, and each break disposes by the integration-first law, never as a REMOVE: `PDFsharp`+`PDFsharp-MigraDoc` INTEGRATE into `Document/export.md` (`[V8]` — the bespoke `FlowBlock`/`FlowFold` pagination dies for the MigraDoc flow DOM; PDF gains Security/AES-256/Signatures/AcroForms/tagged-UA; `SKDocument.CreatePdf` narrows to the pure-visual vector-print arm); `Mapsui.Avalonia12` INTEGRATE into `Charts/basemap.md` (`[V8]` — wires `ARCH:67`); `NodeEditorAvalonia` INTEGRATE into `Editing/graph.md` (`[V8]`). `ACadSharp` re-scopes to WRITE-ONLY consumption of the shared central pin (`props:122`, `[BIM]` group — Bim is the READ authority): the csproj reference (`csproj:95`) STAYS and now owns BOTH write rows — `netDxf` (`csproj:97`) REMOVES on feed-verified upstream abandonment (haplokuon/netDxf archived upstream, a `net6.0` asset under the net10 consumer; `api-drafting-export.md:15`'s "maintained fork" claim is contradicted by the archived upstream), the DXF concern landing on the stronger admitted owner's `DxfWriter` (`api-drafting-export.md:79,164,166` — decompile-verified static + instance emit) — the mandate's "over netDxf" letter yields to its load-bearing half: the WRITE leg stays AppUi-owned, only the package binding moves, and the DXF round-trip fidelity probe (dimensions/hatches/leaders/blocks against the drafting entity set) confirms the leg-1 removal at the `[V7]` leg — a failed entity class re-pins netDxf as a recorded exception — so capability never deletes; `api-drafting-export.md` re-scopes to ONE writer family (`DwgWriter`/`DxfWriter`/`SvgWriter` + typed entities over one `CadDocument`), its `DwgReader`/`DxfReader` rows (`:76-79,159-164,217,230`) move to the Bim catalog and its netDxf surface (`:14-18,86-101,169-181,218,223,231`) deletes. REMOVE — `netDxf` (the one feed-verified abandonment; every other concern already lands on its categorical-best owner). ADD — `ProDiagnostics` (MIT, feed-verified current on the Avalonia-12 family, wieslawsoltes — the roster's proven NodeEditor/Dock/PanAndZoom/Svg.Skia author): the dev-loop visual-tree/property/event/layout inspector `Diagnostics/devloop.md` charters, Debug-gated `PrivateAssets="all"` beside HotAvalonia (`csproj:127-128`) — both first-party alternates fail the gate (`Avalonia.Diagnostics` feed-dead at 11.3.x with no Avalonia-12 asset; the Accelerate DevTools pay-tiered, license-gate REJECTED). Catalog hygiene is a first-class obligation (`[V12]`): the byte-identical `api-unicolour.md` target copy DELETES (shared tier canonical), `api-thinktecture-json.md` DELETES (duplicate + zero AppUi overlay), `api-hashing`/`api-nodatime`/`api-unitsnet` trim to AppUi-delta overlays or delete, `api-mapsui.md:131`'s phantom "PDFsharp page" cross-reference repairs, `api-loro.md:171-172`'s durable stacking re-points to the `[V2]` charter, and every kept catalog re-anchors to the 43-page set. Recorded REJECT, never re-proposed: a `meshoptimizer` AppUi admission (Compute owns clustering — `[V5]` consumes its payload rows); a second color-science kernel beside Unicolour (`[V10]`); speech/MT engines for `LiveCaption` (no viable net10 owner — the rejection stands as recorded corpus law; `locale.md:212-226` re-scopes to an explicit gate); LoroCs REMOVAL (integration-first: four live consumers; `[V2]` re-charters its durable half only); a `TreeDataGrid` second grid engine (the `tables.md:120` seal upheld from architecture — hierarchy renders as `TreeRow` rows on the ONE virtualized `DataGrid` rail). Recorded domain gap: ICC/device-CMYK print fidelity — a LittleCMS-class binding admits only when `Document/export.md`'s print arm names a device-CMYK deliverable, feed-verified OSS + net10 at admission, never speculative. Second recorded gap: in-process video encode — animation's `[04]-[WALKTHROUGH]` terminates honestly at the frame-sequence + manifest over capture's encode rows (no mux/encoder exists in the roster; `LibMpv` is playback-only), and a video-encoder admission gates on a named flythrough-video deliverable, never speculative — the walkthrough page states the frame-sequence boundary as its charter. License gate: OSS and free-for-OSS-commercial admissible; pay-tiered/seat-licensed/proprietary-gated REJECTED.

## [01]-[CAMPAIGN_VERDICTS]

The legs must land every verdict below. Recommended shapes follow each; a leg may exceed them, never dilute them.

### [V1_FAULT_BAND_REGISTRY]

Decidable NOW: mint the AppUi band registry mirroring the federation `FaultBand` (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149`, the AppHost `[V1]` precedent) — a `[SmartEnum<int>]` whose generated key lookup makes a duplicate band fail at type initialization; every fault union's `Code` derives through the registry row, never a `base(detail, NNNN)` literal. Ruled siting: a `[FAULT_TABLES]` owner block on `Diagnostics/evidence.md` — the receipt-union spine every page already folds through (`AppUiTelemetry.Contribute`). The re-partition is TOTAL — no AppUi union keeps its integer, because the whole occupied space is foreign (AppHost 41xx-48xx, Persistence 5xxx) or internally collided, and the hex block dies with it. Ruled neighborhood: **6000-6999**, folder-strided, single-radix decimal, one decade per union: Shell 60xx (`SurfaceFault` 6000, `ControlFault` 6010, `LayoutFault` 6020, `VirtualFault` 6030, `DialogFault` 6040, `InputDriverFault` 6050, NEW `NavFault` 6060 — the navigation bare-`Error.New` rail lands typed); Render 61xx (`ViewportFault` 6100 — pipeline+pathtrace shared, `ShaderFault` 6110, `ImmersiveFault` 6120, `CaptureFault` 6130 (reality.md's union — the reality-capture rail; `capture.md` owns no fault union), `DraftFault` 6140, NEW `AnimationFault` 6150 — `animation.md:62` lands typed); Charts 62xx (NEW `ChartFault` 6200 — the dashboards/custom bare rails land typed); Editing 63xx (`EditFault` 6300 — ONE decade, the 4700-4799 whole-decade claim dies, `Aggregate` carries child codes in the payload rather than a reserved 6399; `FormFault` 6310, `HistoryFault` 6320, NEW `CanvasFault` 6330 for `graph.md`); Document 64xx (`NotebookFault` 6400, `ContentFault` 6410, NEW `ExportFault` 6420); Collab 65xx (`CollabFault` 6500, `IssueFault` 6510, `TourFault` 6520); Theme 66xx (`AssetFault` 6600, NEW `LocaleFault` 6610); Diagnostics 67xx (NEW `ProofFault` 6700 as the proof/devloop/governor rails demand typed cases). The registry carries PINNED MIRROR rows for every foreign neighborhood (AppHost 1xxx/4100-4810, Compute 2200-2220 + Remote 4520-4532, Persistence 5xxx/771x/82xx-83xx, the AEC 23xx-27xx registry) exactly as the AppHost registry pins its foreigners — and the AppHost registry gains the reciprocal AppUi 6xxx pin (the waterfall edit, landed). Every page's band prose becomes a registry reference; each union's re-derivation lands with its owning page's leg; the three internal collisions and eleven foreign squats become unrepresentable in leg 1.

### [V2_COLLAB_DURABLE_RECHARTER]

Mandate item 1 disposed honestly. LoroCs KEEPS as the LIVE-session merge authority — `CollabDoc` over one `LoroDoc`, the 6-kind `CollabContainer` axis, `Presence`/`EphemeralStore`, `TimeTravel`/`UndoManager`, live peer broadcast via `SubscribeLocalUpdate` delta -> `Import` — four live consumers make removal a capability deletion, and the live half violates no law. The DURABLE seam re-charters root-up in `Collab/sync.md`: (a) the durable stream is the TYPED EDIT-INTENT op stream — AppUi's own domain ops (cell insert/edit/move, comment add/edit/resolve, table row commit, annotation ops), the `history.md` `RevertibleOp` -> `SyncOpKind` discipline generalized — projected onto Persistence-owned `OpLogEntry`/`SyncOpKind` rows through the `Version/ledger` changefeed (the waterfalled consumer-pressure row; Persistence owns the row types, AppUi projects and decodes) — and the intent vocabulary has ONE owner: `Collab/sync.md` mints the single edit-intent `[Union]` whose rows the domain planes contribute per `[GENERATOR_LAW]`, `history.md`'s `RevertibleOp` staying the local revert algebra that projects onto the same family (`ARCH:80`'s target re-points onto this rail), never parallel per-page op unions; (b) cold-load = the per-document replay window decoded from the ledger and replayed into a FRESH `LoroDoc` in log order — deterministic hydration, no Loro byte read from durable truth — under the SESSION-EPOCH law that makes it honest: a rehydrated `LoroDoc`'s version vector is unrelated to any live session's, so a live peer's `Export(Updates(vv))` delta cannot import over it (`ImportUpdatesThatDependsOnOutdatedVersion`/`DecodeVersionVectorException`, `api-loro.md:158-159`, are the verified failure surface); replay-window rehydration is therefore the cold-START path that SEEDS a session epoch, and a peer joining an ACTIVE session syncs Loro-native session state from a live peer over the AppHost transport (in-session wire, ephemeral, never persisted — the bit-parity law binds durable truth only), never by replaying the log beside a live epoch; (c) the Loro `Export(Snapshot)` blob survives ONLY as a content-keyed cold-start ACCELERATOR in the Persistence blob lane — its content key composing the kernel `ContentHash.Of` one-hasher entry the blob lane's own content addressing already rides (`Rasm/ARCHITECTURE.md:77`; the page-local `XxHash128.Hash` at `collab.md:119` dies with the durable claim) — derivable, deletable, verified reconstructible from the op-log alone, never system-of-record; `collab.md:105`'s opaque-replay clause and `CollabSnapshot`-as-durable (`:117-120`) are the deleted form, and `api-loro.md:171-172` re-points in the same motion; (d) `notebook.md:171-210` and `issues.md` re-anchor their durable prose onto this charter. Deciding criterion for the one hard container: if character-granular text co-editing provably loses convergence information at intent granularity (concurrent intra-cell edits), the text container's durable rows are character-run ops at `CrdtOpWire` granularity — stable position identifiers, Persistence-owned wire — still never Loro binary; the criterion is probed in the leg with a two-replica concurrent-edit fixture before the text arm fences — and the fired branch lands on EXISTING wire: `CrdtField.RgaSequence` (`Rasm.Persistence/.planning/Version/commits.md:156`) is the stable-position sequence case the character-run rows ride, zero new Persistence row demanded.

### [V3_FOLDER_REPARTITION]

The `[STRUCTURAL_AUTHORITY]` map executes as a verdict: (a) `Diagnostics/` mints from the `Render/evidence.md` six-concern god-page (sections `[02]`-`[07]`, 456 LOC — receipt union, correlation join, capture lanes, headless derivation, dev loop, perf governor; none a viewport projection) as four real owners — `evidence.md` (union + `EvidenceJoin`/`SkewBand` + the `[V1]` `[FAULT_TABLES]` block), `proof.md` (capture lanes + `ProofEngine`/`ProofLaw` headless matrix), `devloop.md` (hot-reload gating + HUD + flamegraph + solve-scrub + replay-verify + REPL), `governor.md` (`PerfBudget` quality governor + `GpuTimeline`); the donor page's `[08]`-`[TS_PROJECTION]` `EvidenceFeed` rides the new `evidence.md` beside the union it projects (the `ARCH:61` source re-anchors) and its `[09]` research items redistribute to their owning new pages; (b) `Collab/` mints from Editing's coordination cluster — `sync.md` (ex-collab, `[V2]`), `issues.md`, `tour.md` — registering the orphan into codemap/router in the same motion; tour's Collab placement is law-grounded, not assumed: `TourSource.TopicTour` folds the issues-owned BCF topic set into stops (`tour.md:3,10`) and the named growth arm is presenter-follow live review riding sync's `Presence`/`EphemeralStore` — the page composes zero collab machinery today, so the moving rebuild states the arm as its charter; (c) `Document/` mints — `notebook.md`, `media.md`, and NEW `export.md` extracting capture's `[06]`-`[DOCUMENT_EXPORT]` + `OfficeExport` OOXML part-graph writers (`capture.md:274-340,367-462,478`) and realizing the `[V8]` PDFsharp/MigraDoc mandate; `capture.md` remains the pure raster capsule + `VisualCodec` color owner; (d) `Theme/animation.md` MOVES to `Render/animation.md` — the timeline/playhead/walkthrough runtime engine is temporal viewport machinery (pipeline `SimVisual` playback, reality `CaptureClip`, capture clip render), not vocabulary; Theme stays the pure vocabulary tier; (e) `Editing/tour.md`'s duplicated sampler collapses onto animation in the tour leg: `tour.md:74` already admits "a tour is, structurally, one camera `Track`" while `:106-129` re-mints `Bracket`/`Walk` and `:231-261` re-mints `WalkthroughTour.Render` — the tour becomes a projection (stops -> camera `Track` keyframes; `WalkthroughTour` -> `animation.Walkthrough`), deleting both clones. Charts stays (the 2-page thinness resolves by `basemap.md`, not by fold).

### [V4_ISSUES_CRDT_COLLAPSE]

`Editing/issues.md` (-> `Collab/issues.md`) rebuilds its `[03]-[COMMENT_THREAD]`: the bespoke `CommentOp` `[Union]` + `CommentThread` `Apply`/`Merge`/`Log` + `HlcStamp` register (`issues.md:118-146`) DIES — `collab.md:20` dropped it root-up and `collab.md:17` already charters the replacement ("an issue comment thread is a `map` container keyed by comment GUID"). The comment thread becomes a `CollabDoc` `map` container attach; `BcfComment` projects from the map at the Bim boundary (the lossless `ToTopic` round-trip and carried-source-row preservation at `issues.md:54-95` survive untouched — they are the page's excellence); the stale `notebook.md#CRDT_COEDIT` citation (`issues.md:123`) re-points to `Collab/sync.md`; the durable half rides `[V2]`'s edit-intent stream. The board stays a pure PROJECTION over the Bim-owned BCF contract — `IssueBoard`, `ViewpointCodec.FromBcf`, and the `IssueStatus` lifecycle are correct as-is.

### [V5_RESIDENCY_CONSUMPTION]

The meshlet/splat plane consumes Compute, never re-builds. (a) `Render/meshlets.md` drops `MeshletCluster.Build`/`Partition` (`meshlets.md:57-81` — degenerate whole-mesh bounds, never-cull cones, no LOD) and consumes `ResidencyPayload.Clusters` — the meshopt-built, cone-carrying `ResidencyMeshlet` descriptors Compute's `payload.md` `meshlet-cluster` row already mints and `pipeline.md:542-556` already projects to the TS wire; the page KEEPS its genuinely strong half — `ResidencyBudget` LRU/velocity-prefetch (`:107-125`), `BindlessTable`, `InstanceBuffer`, `ClusterCull` — now culling on REAL per-cluster wire cones; the cluster-LOD selection reads the encoded error columns the waterfalled Compute clause lands (AppUi named the demanding consumer), and until that lands the LOD arm signature-locks on the recorded row with the gate stated inline — the SELECTION ALGEBRA is AppUi's own and lands NOW: the per-cluster error bound projects to screen space under the camera row and a `LodPolicy` threshold picks the cut, a hysteresis band on the same policy row kills LOD flicker across dolly moves, and cut well-formedness (crack-free, no double-draw) rides the encode arm's monotonic-error guarantee (the extended Compute clause). The cull ladder is RAISED past cone parity to the page's own infinite-viewport charter (`meshlets.md:3`): frustum -> wire-cone backface -> LOD cut -> prior-frame depth-pyramid (HZB) two-phase occlusion, each a pass-DAG row over the shared device — the pyramid build is one compute pass (`ComputePassEncoder`/`BeginComputePass`/`DispatchWorkgroups`, `api-silk-webgpu.md:52,118-120`; `QueryType.Occlusion`, `:80,142`, the fallback probe row) — frustum+cone alone is the parity form, not the bar. (b) `Render/reality.md` reconciles its divergent carriers: `SplatPayload`/`PointPayload` (`reality.md:52,112`) collapse onto the one Compute `ResidencyPayload` boundary record `pipeline.md` already consumes — one AppUi-side projection of one Compute concept; `SplatEllipsoid.ContentKey` (`:46-49`) DIES — tiles key by the payload's own `ContentKey` per the single-mint law, doubly foreclosed by the kernel one-hasher law (no AppUi-side content-key fold exists beside `ContentHash.Of`); `SplatSort` (`:54-58`) is consumed by a real radix sort or the enum dies with the `OrderByDescending` stopgap stated honestly; the `MemoryMarshal.Cast` layout assumption stays gated on `CAPTURE_PAYLOAD`. The SPZ/SOG companion decode stays `[UPSTREAM-BLOCKED]` (Compute brief `[06]` confirms).

### [V6_FENCE_REALISM_SET]

The confirmed decorative-fence set lands as repairs with the owning page's rebuild, never patches beside it — each either implements to the prose or downgrades the prose to an explicit gate: `pathtrace.md:33-36` `Bvh.Split` becomes a real recursive SAH build (children emitted, leaf criterion), `:38-40` `Enclose` a real bounding sphere, the `:30-31` leaf-only `Refit` a real bottom-up re-bound whose refit-vs-rebuild policy adopts the kernel `[DEGRADATION_REFIT]` shape (topology-stable in-place re-bounding + a deterministic SahCost rebuild trigger, `Rasm/.planning/Spatial/index.md:699` — the law the page's `:17` refit prose claims), the oracle BVH staying a PAGE-LOCAL measured kernel over wire-decoded meshlet bounds and staying PRIVATE — the kernel `SpatialIndex` is the federation broad-phase owner behind the `[PLACEMENT_LAW]`(e) firebreak and its `ToAcceleration` node-array wire (`Rasm/ARCHITECTURE.md:86`) stays the ONE cross-package acceleration currency, so an AppUi acceleration wire or a second exported BVH is unrepresentable; `:64-67` `Accumulate` a real integrate-or-gate — and the integrate arm has nothing to integrate until the plane owns an emitter: the `Reservoir` prose samples "light samples" (`pathtrace.md:14`) while the WHOLE Render plane carries zero light vocabulary (no environment/sun/emissive row on any page), so the "correctness oracle" renders black by construction; the repair mints ONE closed `LightSource` row family (Environment | Sun | Emissive — seed DATA per `[GENERATOR_LAW]`) shared by the raster shading path and the oracle — one light rig, two integrators, comparability by construction — with the site-anchored Sun row deriving from the Bim `GeoReference` seam + the NodaTime instant under `ClockPolicy`, its azimuth/altitude COMPOSING the Compute daylight solar-position member (RASM-CS-COMPUTE-BRIEF.md `[V12]`b exports it package-boundary-consumable on AppUi's named demand; the crossing lands as a declared `[V9]` ledger row), never a second geodesy or solar-position kernel; `virtualization.md:127-131` `Reaccumulate` becomes the Fenwick/prefix tree the `:78` prose claims (O(log n) update/query) or the prose states the linear rebuild honestly — ruled: Fenwick, the million-row tree is the page's charter; `controls.md:181-184` `Rebind` actually resets bindings to the new row's `ValuePath` and re-applies the token role (the recycling core), and `Apply` (`:136-141`) covers the token brush/metric set its prose claims; `animation.md:107,121` `PingPong` gains a direction field and actually reverses, `Scrub.To`'s unused `scheduler` param (`:176`) marshals or dies, and the caller-threaded interpolation-delegate tail (`lerpD`/`lerpCam`/`lerpColor` re-spelled at every call site — `SampleAt` `:141-145`, `Scrub.To` `:176-185`, `Walkthrough.Render` `:210-217`, `tour.md:73`) collapses into track-owned interpolation policy rows per `[GENERATOR_LAW]`: the Camera row is a POSE interpolation (`System.Numerics.Quaternion.Slerp` over the camera orientation with the eased positional arc — the stratum peer of the kernel `MotionInterpolation` one-slerp law, `Rasm/.planning/Parametric/projections.md`, under the same one-site discipline: animation's Camera row is the ONE pose-interpolation site AppUi-wide and `Collab/tour.md`'s transition interpolation rides it at the `[V3]`e collapse), the Color row is the tokens OKLab mix the page's own `:18` prose already legislates against its delegate-taking fence (prose-fence parity), and a component-wise eye/target/up lerp or a second pose-interpolation site is the deleted form; `tables.md:234-237` `Delimited` gains RFC-4180 quoting/escaping; `inspector.md:360-372` `ThreeWay.Diff` becomes a real LCS/diff3 alignment (anchor-based hunk detection) — the positional zip contradicts the page's own `:299` law; `pipeline.md` `Empty` fault path stops sinking ordinal `0L` (`:194` — thread the interlocked ordinal) and the `:16` prose signature resyncs with the `:155` fence; `drafting.md:310` `ProjectionBasis.Top` un-pins — `SheetRegion` (`:77`) carries its own basis/view spec per region and `meshOf(region.Key)` stops conflating region key with mesh lookup; `solver.md:182` `Suggest` registers edit variables in `Load` (the implicit contract enforced) and `SolvedRect` (`:190`) keys by a required child identity, never a nullable `Name`; `dialogs.md:68` `ToastPipe` returns `IO<Unit>`; `media.md:100-101` `Mount` wires seam/`Source`/`AutoPlay`/`Loop` over the verified `MpvView.Renderer`/`MpvContext` binding — ruled: wire (`api-libmpv` members verified); `MPV_SURFACE_MOUNT` narrows to the libmpv-native provisioning fact; `navigation.md:39` parses the `rasm:` scheme robustly; `typography.md:172-178` FrontMatter/Footnotes populate or the fields carry their `FRONT_MATTER_AST` gate inline; `shading.md:106-109` `BindShade` stays spike-gated with the stub named. The five run-on Boundary paragraphs refactor to declarative clauses under the `[SEAM_AND_RAIL_LAW]` prose-fence parity law.

### [V7_DRAFTING_WRITE_LEG]

Mandate item 2 is DISPOSED as satisfied on disk — painter sort dropped root-up (`drafting.md:96,418`), `HiddenLineSeam` composes Fabrication `#PROJECTION_HIDDEN_LINE`, the seam declared BOTH sides (`ARCH:75-76` ⇄ `Rasm.Fabrication/ARCHITECTURE.md:47`), zero painter-sort remnant corpus-wide — and this verdict seals its perimeter. (a) The WRITE leg is TWO rows on one `DraftEmit` axis, BOTH binding write-scoped ACadSharp over ONE `CadDocument` entity fold: DXF via `DxfWriter` (binary/text), DWG via `DwgWriter` (`drafting.md:342-360`) — `netDxf` REMOVES per `[ROSTER_RECONCILIATION]` (archived upstream; one document model with two writer rows is the `[GENERATOR_LAW]` form the two-model split violated), the DXF round-trip fidelity probe (dimensions/hatches/leaders/blocks) landing in this leg with a failed entity class re-pinning netDxf as a recorded exception, never silently; Bim keeps the READ authority (`Fabrication/ARCHITECTURE.md:52`'s "netDxf in AppUi owns the distinct WRITE leg" refines to the ACadSharp two-format write leg as that campaign's counterpart obligation, recorded); the hardcoded `AutoCad2018` version literal becomes a policy row. (b) Geometry migration insulation: the kernel visibility engine is LANDED (`Drawing/view.md` `DrawingProjection`) and Fabrication's BSP dies for it — AppUi's producer STAYS the Fabrication `HiddenLineResult` receipt, so the supersession is invisible at this seam (the `ARCH:75` label still reads "BSP visibility solver" — the wording re-points to the kernel `DrawingProjection` route in the same ledger motion); the stale declared-unwired `ARCH:72` Geometry `DrawingProjection` row RETIRES (Fabrication is the kernel engine's consumer, never AppUi), and `ARCH:73` `ChartAtlas` retires AppUi-SIDE only (ruled: retire — no AppUi page demands a UV atlas today; the capability stands on the `[V9]` growth register; the kernel `Processing/flatten` ChartAtlas owner does NOT retire — its live consumer is Fabrication `Nesting/nfp`, Fab `ARCH:55`); both retirements are TWO-SIDED: the landed kernel ledger declares the mirror rows (`Rasm/ARCHITECTURE.md:108` view→AppUi, `:112` flatten→AppUi), so the retirement is RECORDED as a kernel-side counterpart ledger edit — the `:112` flatten row re-scopes toward the Fabrication route with the growth recording carried — never left one-sided. (c) The silhouette edge-style consumption (`EdgeStyle.Silhouette`) stays forward-ready with its producer chain now NAMED: the landed kernel view engine carries connectivity-chained visible/hidden polylines with the `EdgeKind` classes (`DrawingProjection.ToPolylines`, successor-linked per set), Fabrication's `HiddenLineResult` receipt carries the silhouette class through the settled route (`FAB:46-47`), and the AppUi row consumes the receipt class — no new seam.

### [V8_INTEGRATION_MANDATES]

The three zero-consumer admissions realize as named pages (integration-first law): (a) `Document/export.md` — PDFsharp+MigraDoc become the paginated-document owner: the MigraDoc flow DOM (`Document`/`Section`/`PdfDocumentRenderer`/`FormattedDocument`) replaces the hand-rolled `FlowBlock`/`FlowFold` engine (`capture.md:274-340`); PDFsharp `Security` (AES-256), `Signatures`, `AcroForms`, and `UniversalAccessibility` tagged-PDF land as export policy rows — the signature arm's key ingress is honest: `DigitalSignatureHandler.ForDocument(PdfDocument, IDigitalSigner, DigitalSignatureOptions)` (`api-pdfsharp.md:137`) takes a signer COMPOSED at the boundary whose credential material rides the AppHost `Runtime/secrets.md` lease lifecycle (acquire/renew/zeroize — the waterfalled ABRIEF `[V12]` clause, AppUi named), never AppUi-held key bytes, with the crossing a declared `[V9]` ledger row; `OfficeExport` XLSX/DOCX/PPTX (`capture.md:367-462`) moves in as the OOXML arm; `SKDocument.CreatePdf/CreateXps` (`capture.md:478`) narrows to the pure-visual vector-print arm capture keeps; drafting's sheet-PDF and the diagnostics report-PDF compose THIS owner. (b) `Charts/basemap.md` — Mapsui `MapControl`/`Map`/`GeometryFeature` realize the declared `ARCH:67` seam: Bim-owned NetTopologySuite geometries draw as tiled map overlays beside the Wgpu viewport, with the CRS chain honest: the features arrive carrying the Bim-owned `GeoReference` (the `GeoReferenceProjector` IfcMapConversion/IfcProjectedCRS lowering) and Bim's `GeoFeature.Reproject` owns geodesy (`Rasm.Bim/ARCHITECTURE.md:25,80,82` — the seam already declared both sides), so AppUi reprojects only WGS-84 input through `SphericalMercator.FromLonLat`/`ProjectionDefaults.Projection` (`api-mapsui.md:78-79`) and a local geodesy kernel is the forbidden form; LiveCharts `GeoMap`/`DrawnMap` (dashboards) stays the CHART-projection row, Mapsui the TILED-basemap owner — disjoint charters stated on both pages; the dashboards geo research items (`dashboards.md:473-474`) resolve against the split. (c) `Editing/graph.md` — NodeEditorAvalonia `IDrawingNode`/`DrawingNodeEditor` realize the promised parametric/dependency-graph canvas on ReactiveUI: node/pin/connector editing over a typed graph model, `LoroTree` as the co-edit data seam (`api-loro.md:176`) under ONE bidirectional projection with echo suppression — subscriber diffs discriminate `EventTriggerKind.Local/Import/Checkout` (`api-loro.md:78`) so a local UI mutation commits as tree ops without re-applying its own echo and a remote `Import` diff applies to the ReactiveUI graph model without re-emitting (the feedback loop is the named deleted form) — notebook `DependencyGraph` visualization as a read projection; `api-nodeeditor.md:149`'s pan/zoom disambiguation stands RULED: the canvas rides the package's own `NodeZoomBorder` (a distinct asset, NOT the admitted `PanAndZoom`, which keeps its five page consumers) — no dup exists. Each page lands its `.api` re-anchor in the same motion.

### [V9_SEAM_LEDGER_AND_GOVERNANCE]

The perimeter becomes machine-checkably true in one motion. Ledger, stale rows: `ARCH:63` `Render/glb` phantom node DIES (the residency wire lives at `pipeline#TS_PROJECTION` — the row re-anchors there); `ARCH:66` `Render/query` ← Bim `ElementSet` retires or gains its owning page (ruled: retire — no query page is chartered; the Bim query algebra stands on the growth register, re-opening when a consumer names it); `ARCH:69,70,80` re-point off deleted `Rasm.Persistence/Sync` onto `Version/ledger` (the op-log/changefeed owner; `NotebookOp` vocabulary dead per `collab.md:20` — the row re-writes to the `[V2]` edit-intent stream); `ARCH:72,73` per `[V7]`; `ARCH:68` Bim/Planning `ScheduleNetwork` RETIRES (ruled: zero consuming fence corpus-wide — the same class as `ARCH:66`/`ARCH:73`; it stands on the growth register). Ledger, wired-undeclared rows land: `Collab/sync` -> Persistence `Version/ledger` (op-log projection + replay-window read, `[V2]`) and -> Persistence blob lane (snapshot accelerator); `Collab/sync` -> AppHost transport (the live-delta broadcast topic riding the AppHost bus/topics law — both sides named); the AppHost PORT spine row family (`TelemetryContributorPort`/`ReceiptSinkPort`/`UiSchedulerPort`/`ClockPolicy`/`DataClassification` — one declared row family naming every composing folder, mirroring the AppHost `[V7]` wire-vocabulary tier); `Charts/basemap` ← Bim geospatial (realized per `[V8]`, the `GeoReference` CRS ingress named); `Render/meshlets` ← Compute `payload.md` clusters (`[V5]`); `Document/export` -> AppHost `Runtime/secrets.md` (the signing-credential lease ingress, `[V8]`a); the content-identity mint row family -> kernel `Domain/identity` (`ContentHash.Of` — the capture/commands/notebook-replay/snapshot mints, ONE declared row family naming every minting folder, mirroring the AppHost port-spine row form; the kernel-side CONTENT_KEY fan gains the reciprocal AppUi row as a recorded counterpart, `Rasm/ARCHITECTURE.md:87-92`). Named blocker, standing: the live-Rhino embed probe — `SurfaceHost.RhinoPanel`/`EmbedCapsule`/`RenderGraph.Lease` GPU-lease validation against an integrated RhinoWIP host surface that does not yet exist — stays host-gated and open; no leg closes it. Growth register, the standing deferred-capability record (each row re-opens only when a consumer names it, never as a speculative seam): `ChartAtlas` texture channel, Bim `ElementSet` query surface, `ScheduleNetwork` 4D/CPM dashboards projection, ICC/CMYK print lane, LiveCaption gate, kernel `Analysis/query.md` `Analyze` measured-query receipt projection — measure/inspect receipts as inspector and dashboard rows when a host-side producer ships them over the receipt spine. README router and the `ARCHITECTURE.md` codemap re-emit the final 8-folder/43-page set; every seam row whose SOURCE page moves re-anchors to its post-`[V3]` path in the same motion (`ARCH:61` evidence -> Diagnostics, `ARCH:65,70` notebook -> Document, `ARCH:78,79` issues -> Collab); the `[COLLABORATION_LOCALE]` package group (`README.md:148`) gains its router rows.

### [V10_COLOR_VOCAB_SINGLETONS]

Two vocabulary splits collapse to one owner each. (a) Gamut: `custom.md:452` `ColorSpaceAxis` (4 rows, claimed "single suite-wide gamut vocabulary") vs `capture.md:190-196` `ColorPolicy` (6 rows: +WideGamut/+HdrPq) — ruled: `ColorPolicy` on the capture codec is the ONE gamut/transfer row family; `ColorSpaceAxis` dies or becomes a keyed projection of `ColorPolicy` rows (never a parallel enum with divergent membership). (b) Color science: `accessibility.md:142-143` computes WCAG relative luminance via Avalonia `ColorHelper` while `tokens.md:20` seals Unicolour as the one suite colour owner — ruled: the `ContrastGate` luminance routes through the Unicolour kernel (it owns the sRGB->luminance transform beside OKLab mix and colormap sampling), one color kernel suite-wide; the ColorHelper call is the deleted form.

### [V11_RECOMPUTE_CONSUMPTION]

`Document/notebook.md` resolves its one-owner contradiction: `DependencyGraph` (`notebook.md:115-168`) implements a full local topo/dirty/recompute engine while `:210` claims alignment to the AppHost `RecomputeGraph` and names "a second incremental-recompute owner" the deleted form. Ruled default: COMPOSE — the cell-dependency identity and dirty-propagation algebra ride the AppHost `RecomputeGraph` vocabulary through the declared port (decode-only), with the notebook keeping only the document-local projection (cell-id <-> node-id map, UI dirty overlay); the local engine dies. Deciding criterion: if the AppHost `RecomputeGraph` surface proves document-granularity-hostile at the leg (per-cell recompute demands a member the vocabulary lacks), the local engine SURVIVES with the one-owner claim retracted and the divergence chartered — never a silent duplicate. The `CapabilityPin`/`DeterminismContext`/`EnvFingerprint` composition (`:206-210`) is already correct and stays. The replay-bundle input hashes (`notebook.md:243`) re-route through the kernel `ContentHash.Of` entry per the `[SEAM_AND_RAIL_LAW]` one-hasher clause.

### [V12_API_TIER_HYGIENE]

The catalog motion executes as a verdict, not a chore: the target-tier `api-unicolour.md` (byte-identical, 480 loc) and `api-thinktecture-json.md` (duplicate, zero AppUi overlay) DELETE — shared tier canonical per the two-tier law; `api-hashing`/`api-nodatime`/`api-unitsnet` trim to the AppUi delta over the shared base or delete; `api-drafting-export.md` re-scopes to the `[V7]` write leg with the READ rows moving to the Bim catalog; `api-loro.md` stacking bullets 1-2 (`:171-172`) re-point to the `[V2]` durable charter; `api-pdfsharp.md`/`api-mapsui.md`/`api-nodeeditor.md` re-anchor to their `[V8]` owning pages and the phantom cross-references (`api-mapsui.md:131`) repair; `api-avalonia-fluent.md` confirms its retained-floor-beneath-Semi charter is live (one composition-level wiring) or flags dead; `api-avalonia-color.md` gains the `[V10]` caveat (`ColorHelper.GetRelativeLuminance` = deleted form, `ToDisplayName` stays); the substrate `api-languageext.md` VERIFIES under the track-shared authoring law (the first upstream campaign lands the ONE substrate file; this campaign verifies, never re-authors); every kept catalog's page anchor re-verifies against the 43-page set. The manifest motion is zero-VERSION (no pin bumps; the three integrations bind existing pins) but not zero-row: leg 1 carries the `netDxf` REMOVE (props row + `csproj:97` + the `api-drafting-export.md` netDxf surface) and the `ProDiagnostics` ADD (App UI props row + a Dev Loop `PrivateAssets` csproj line + its stub catalog), beside the csproj group-label and catalog moves.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every leg re-verifies its rows against disk before editing. Anchors are `libs/csharp/Rasm.AppUi/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md`; `CSPROJ:`/`PROPS:` the manifests; `API:` the target catalog tier; `PBRIEF:`/`ABRIEF:`/`CBRIEF:` the Persistence/AppHost/Compute briefs (kernel anchors are `libs/csharp/Rasm/.planning/` pages); `FAB:` `Rasm.Fabrication/ARCHITECTURE.md`; `DECISION:` `.archive/RASM-COMPONENT-PARADIGM-DECISION.md`.

| # | Class | Evidence |
|---|---|---|
| E1 | Band collisions + squats | Internal: `tour.md:30-32`(4300)≡`virtualization.md:48-51`(4300); `forms.md:29-32`(4900)≡`reality.md:30-34`(4900); `collab.md:43-48`(0x4B00)≡`media.md:28-31`(0x4B00). AppHost squats: `hosts.md:43`(4100)+`assets.md:29-32`(4120)+`dialogs.md:39`(4130)+`input.md:289-292`(4150) ⊂ `ConfigError` 4100-4199; `pipeline.md:32-36`(4500)≡`HopFault`; `drafting.md:271-274`(4600)≡`CommandFault`; `inspector.md:138,152-174`(4700-4799) ⊃ ten ABRIEF V1 bands; `notebook.md:96-99`(4800)≡`FederationFault`. Persistence squat: `issues.md:29-32`(5000). Hex fork: `history.md:37-40`(0x4A00), `shading.md:28-31`(0x4C00), `immersive.md:29-33`(0x4D00). Also `controls.md:29-33`(4200), `solver.md:58-65`(4400) — foreign-space residents. Zero registry; every code a `base(detail, NNNN)` literal (ABRIEF V1 banned form) |
| E2 | Durable CRDT inversion | `collab.md:101,105` opaque-delta replay + `:117-120` `CollabSnapshot` content-addressed Loro blob as durable state; `API:api-loro.md:171-172` same stacking; `notebook.md:178` co-edit over "the Persistence op-log changefeed" inheriting the form; `issues.md:123` `CommentOp` "projects onto the Persistence `OpLogEntry`"; PBRIEF:49 (`[ROSTER_RECONCILIATION]` LoroCs rejection) + PBRIEF:204; `history.md` the correct-discipline exemplar (typed deltas -> `SyncOpKind`) |
| E3 | Orphan + contradiction | `collab.md` absent from ARCH codemap (`ARCH:10-52`, 36 nodes), ARCH seams, README router (36 rows); `issues.md:118-146` `CommentOp`/`CommentThread`/`HlcStamp` vs `collab.md:20` ("DROPPED root-up") + `collab.md:17` (map container); `issues.md:123` cites dead `notebook.md#CRDT_COEDIT` law |
| E4 | Decorative fences | `meshlets.md:71-81` degenerate Partition (whole-mesh `Bounds`, `NormalCone(0,0,1,-1)`, radius-as-error) vs `:14` prose + `pipeline.md:463-470` cone-carrying `MeshletWire` + `:542-556` Compute-built consumption; `pathtrace.md:33-36` non-recursive `Bvh.Split`, `:38-40` center-sum `Enclose`, `:64-67` traceless `Accumulate` vs `:3` oracle claim; `pathtrace.md:14` `Reservoir` "light samples" prose with ZERO light/emitter vocabulary on any Render page (no environment/sun/emissive row — the oracle has no light to transport); `virtualization.md:127-131` O(n) vs `:78` Fenwick claim; `controls.md:181-184` no-op `Rebind` vs `:158`; `reality.md:46-49` local `XxHash128` vs `pipeline.md:404,496` single-mint; `reality.md:54-58` dead `SplatSort` (`:72-78` `OrderByDescending`); `animation.md:121` PingPong≡Once, `:176` unused scheduler; `inspector.md:360-372` positional zip vs `:299`; `tables.md:234-237` unquoted `string.Join`; `media.md:100-101` Mount stub; `drafting.md:310` pinned `ProjectionBasis.Top`; `solver.md:182,190` Suggest/SolvedRect contracts; `pipeline.md:194` `Empty` ordinal 0L, `:16` vs `:155` prose drift; `typography.md:172-178` always-empty FrontMatter/Footnotes |
| E5 | Zero-consumer admissions | `CSPROJ:95-99,117-118,122`; `PROPS:354-363`; PDFsharp: `API:api-pdfsharp.md:155,161` names non-consuming consumers, `capture.md:274-340` bespoke `FlowBlock`/`FlowFold`, `:367-462` OfficeExport, `:478` `SKDocument.CreatePdf`; Mapsui: `ARCH:67` declared-unwired, `API:api-mapsui.md:131` phantom page cite; NodeEditor: README promise, no page, `API:api-nodeeditor.md:149` PanAndZoom note |
| E6 | Seam-ledger rot | Phantoms: `ARCH:63` `Render/glb`, `ARCH:66` `Render/query`; dead targets: `ARCH:69,70,80` `Persistence/Sync` (deleted; owner `Version/ledger` per PBRIEF), `ARCH:70` dead `NotebookOp`; declared-unwired: `ARCH:72` Geometry `DrawingProjection` (the landed kernel `Drawing/view.md`: Fabrication consumes the kernel engine; `drafting.md:308-310` composes the FAB seam; kernel mirror row `Rasm/ARCHITECTURE.md:91`), `ARCH:73` `ChartAtlas` (kernel mirror row `Rasm/ARCHITECTURE.md:95`), `ARCH:67` basemap; wired-undeclared: collab->ledger/blob/transport, the AppHost port spine (~30 composing pages, one `ARCH:65` row), meshlets<-Compute payload; two-sided-correct: `ARCH:75-76` ⇄ `FAB:47` HLR receipt |
| E7 | Write-leg scope | `drafting.md:342-360` ACadSharp `CadDocument`/`DwgWriter` + `AutoCad2018` literal (`:363`); `FAB:52` "netDxf in AppUi owns the distinct WRITE leg" (netDxf is DXF-only — imprecise); `API:api-drafting-export.md:76-79,159-164,217,230` READ+WRITE over-claim vs `drafting.md:3` "mints no second CAD writer"; `PROPS:122` ACadSharp in `[BIM]` group; `CSPROJ:95` direct reference; feed truth: haplokuon/netDxf ARCHIVED 2023-10 (latest `2023.11.10`), DomCR/ACadSharp active — `[V7]` re-binds both write rows |
| E8 | Vocabulary splits | `custom.md:452` `ColorSpaceAxis` (4 rows) vs `capture.md:190-196` `ColorPolicy` (6 rows), claimed projection at `capture.md:165`; `accessibility.md:142-143` `ColorHelper.GetRelativeLuminance` vs `tokens.md:20` Unicolour one-owner law; `notebook.md:115-168` local recompute engine vs `:210` one-owner claim (AppHost `RecomputeGraph`) |
| E9 | Bare `Error.New` rails | `navigation.md:46,81`; `locale.md:172-173`; `dashboards.md:296,304`; `custom.md:168,184,203,220,231`; `animation.md:62` — federation SEAM_AND_RAIL_LAW breaches pairing with E1 |
| E10 | Governance state | the corpus presents a settled surface while the E1-E9 perimeter defects stand — the sealed-corpus illusion; the live-Rhino embed probe (host-gated GPU-lease validation, no integrated RhinoWIP host surface exists) stands open per the `[V9]` blocker; `README.md:148` `[COLLABORATION_LOCALE]` group with zero collab router row; run-on Boundary paragraphs `dashboards.md:20`/`custom.md:19`/`collab.md:20`/`capture.md:165`/`evidence.md:278`; `locale.md:212-226` unbacked LiveCaption capability |
| E11 | Catalog tier | `API:api-unicolour.md` byte-identical to shared (480 loc); `API:api-thinktecture-json.md` duplicate + zero consumers; hashing/nodatime/unitsnet near-duplicates; `API:api-loro.md` 9/10 craft with the E2 seam; 57 catalogs / 9314 loc, craft 8-9, defects relational |
| E12 | Upstream-clean confirmations | AppHost ports composed as settled vocabulary corpus-wide (hosts/commands/navigation/notebook/evidence — no host-plumbing re-mint; mandate item 3 sound except E1 bands + E8 recompute); `drafting.md:96,418` painter sort dropped (mandate item 2 disposed; zero painter-sort tokens corpus-wide); `pipeline.md:496` single-mint law + CBRIEF:216 AppUi `ResidencyManifest.Mint` settled peer; `reality.md` CAPTURE_DECODE honestly `[UPSTREAM-BLOCKED]` (CBRIEF:218); Compute `payload.md:11-12` `meshlet-cluster` row with per-cluster `ResidencyMeshlet` descriptors — the [V5] producer verified real |
| E13 | Kernel composition surface | `CSPROJ:8` kernel project reference material; firebreak: `Directory.Build.props:46` `IsRhinoCommonAwareProject` scopes host-reference inheritance to `libs/csharp/Rasm/` + the Rhino/GH roots — AppUi is NOT RhinoCommon-aware, so Rhino-typed kernel ingress (`Rasm/.planning/Spatial/index.md:15,157` `Build(SpatialKind, ReadOnlySpan<BoundingBox>, BuildPolicy)`, `Processing/intent.md:165` `Pose(Plane, Plane, ...)`) is boundary-unreachable while `Domain/identity.md` `ContentHash.Of(ReadOnlySpan<byte>) -> UInt128` composes clean; one-hasher law `Rasm/ARCHITECTURE.md:87-92` (Element/Persistence/Compute/python/ts rows); AppUi raw-hash census the law re-routes: `capture.md:84,162` runtime `ContentHash` delegate, `commands.md:197`, `notebook.md:243`, `collab.md:117-119`, `reality.md:46-49` (the `[V5]` kill); kernel mirror seam rows `Rasm/ARCHITECTURE.md:91,95`; caller-threaded lerp tails `animation.md:141-145,176-185,210-217` + `tour.md:73`; kernel `[DEGRADATION_REFIT]` law `Rasm/.planning/Spatial/index.md:699` vs the `pathtrace.md:30-31` leaf-only refit |

Sound surfaces (preserve; touch only where a verdict names them): `Shell/hosts.md` (10), `commands.md` (9.5), `screens.md` (9), `Theme/tokens.md`/`typography.md` (9.5), `Editing/inspector.md` (9.5 minus E4 diff), `livedata.md`/`tables.md`/`forms.md`/`history.md` (9 minus named repairs), `Charts/dashboards.md`/`custom.md` (9 minus E8/E9), `Render/pipeline.md` (9 minus E4 edges), `capture.md` (9 minus the `[V3]` extraction + E8), `drafting.md` (9 minus `[V7]` perimeter), `evidence.md` content (9 — structure moves per `[V3]`), `animation.md` content (9 — placement moves), and the corpus-wide receipt/telemetry spine (`AppUiTelemetry.Contribute` + `ReceiptSinkPort` envelope discipline) — the strongest single-spine discipline in the C# tree.

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; the verdicts carry the shapes that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Fault space (registry) | 1 | 9.5 | type-enforced 6xxx `FaultBand` registry; 3 collisions + 11 foreign squats unrepresentable; hex fork dead; zero bare `Error.New`; AppHost mirror row both directions |
| Collab durable spine | 3 | 9.5 | edit-intent ops on Persistence-owned rows; replay-window cold-load into fresh `LoroDoc`; session-epoch join law (active-session join = in-session Loro wire, cold start = replay seed); snapshot demoted to derivable accelerator; issues on the map container; live half untouched |
| Render residency (meshlets/reality) | 5 | 9.5 | Compute cluster consumption (local Partition dead); real wire-cone cull; hysteresis LOD cut off monotonic error columns; HZB two-phase occlusion pass; one payload carrier; payload-key identity (re-hash dead) |
| Render integrators + temporal (pathtrace/shading/animation) | 6.5 | 9.5 | recursive SAH BVH + real enclose + kernel-shaped degradation refit, page-local and private (the kernel `ToAcceleration` wire stays the one acceleration currency) + honest accumulate; ONE `LightSource` row family shared by both integrators (oracle comparability by construction); shading spike-gates named; one BSDF two evaluators preserved; the lerp-delegate tail dead — track-owned interpolation policy rows, the Camera row the one pose-slerp site AppUi-wide |
| Drafting + CAD emit | 8.5 | 9.5 | per-region projection basis; two-row write leg on ONE `CadDocument` fold (ACadSharp `DxfWriter`+`DwgWriter`; netDxf removed on archived upstream, fidelity-probed); version-policy row; Geometry supersession insulated at the Fabrication receipt |
| Document plane (notebook/media/export) | 6.5 | 9.5 | MigraDoc flow DOM + PDF security/signatures/AcroForms/UA; signing-key ingress via the AppHost secrets lease; OOXML arm re-homed; `RecomputeGraph` composed; media mount wired; bespoke pagination dead |
| Charts + geo | 7 | 9.5 | typed `ChartFault`; Mapsui basemap page wiring the Bim NTS seam with the `GeoReference` CRS ingress named; GeoMap/basemap charters disjoint; cross-filter engine preserved |
| Editing typed-edit | 8.5 | 9.5 | real diff3; CSV quoting; NodeEditor graph canvas page with echo-suppressed LoroTree co-edit |
| Shell spine | 8.5 | 9.5 | Fenwick ledger real; Rebind recycling real; solver contracts enforced; typed `NavFault`; Unicolour luminance |
| Theme vocabulary | 8.5 | 9.5 | animation re-homed (pure vocabulary tier); typed `LocaleFault`; LiveCaption honest gate; front-matter fields live or gated |
| Diagnostics | struct 4 / content 9 | 9.5 | four real owners (evidence/proof/devloop/governor); `[FAULT_TABLES]` sited; receipt union + correlation preserved verbatim; devloop gains the ProDiagnostics inspection row (`[ROSTER_RECONCILIATION]` ADD) |
| Governance perimeter | 4 | 9.5 | ledger complete both directions; 43-page codemap/router; collab registered; catalogs deduped + re-anchored |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` + `.Json` as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Atom` rails at every entry, content identity through the kernel `Rasm.Domain` `ContentHash.Of` seed-zero entry (the federation one-hasher — an AppUi-local `System.IO.Hashing` `XxHash128` call site is the deleted form; Compute-minted payload/splat keys stay decode-only per `[V5]`), NodaTime under `ClockPolicy`, UnitsNet on measure-bearing rows, Unicolour as the ONE color kernel (`[V10]`), Verify+Headless as the proof substrate — a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package. The weave extends past the base seven where a page's concern names it: `QuikGraph` (`props:41`, shared `api-quikgraph.md`) owns `graph.md`'s connection-admission cycle gate and graph algebra (never a hand-rolled adjacency, never a second recompute owner beside the AppHost `RecomputeGraph`); `CommunityToolkit.HighPerformance` (`props:38`, shared `api-highperformance.md`) owns the Charts `DimensionIndex`/`CrossFilter` bitmap engine's and residency/virtualization hot paths' pooled buffers (`Span2D`/`MemoryOwner`); `Riok.Mapperly` (`props:30`, shared `api-mapperly.md`) owns the boundary wire projections (BCF `ToTopic`, TS wires, payload decode) per the mapping-aspect law; the typography `FRONT_MATTER_AST` gate's YAML decode binds the existing `YamlDotNet` transitive-floor pin (`props:466`) promoted to a direct reference only when the arm goes live — no new pin.
- `Rasm` (kernel, project reference `csproj:8`) — the below-port compile surface, composed as landed fences under the `[PLACEMENT_LAW]`(e) firebreak: `Domain/identity.md` `ContentHash.Of` is THE composed entry (every AppUi content-identity mint — capture runtime delegate, walkthrough frame proof, command payload digest, notebook replay inputs, the `[V2]` snapshot key — with hex/wire encodings staying boundary projections at the consuming seam); `Drawing/view.md` `DrawingProjection` reaches AppUi ONLY through the Fabrication `HiddenLineResult` receipt (`[V7]`); the `Analysis/query.md` `Analyze` receipt projection stays a `[V9]` growth-register row, never a speculative seam; Rhino-typed kernel members are firebreak-unreachable and never named in AppUi fences.
- `LoroCs` — the tier's best-mined catalog stays the reference depth (container forest, `SubscribeLocalUpdate`, `ExportMode.Snapshot/Updates/ShallowSnapshot`, `EphemeralStore`/`Awareness`, `UndoManager`, `ForkAt`/`RevertTo`); the `[V2]` recharter re-points ONLY the durable stacking (`api-loro.md:171-172`); `LoroTree` gains its `[V8]` graph-canvas consumer.
- `PDFsharp`+`PDFsharp-MigraDoc` — the `[V8]` export owner mines: MigraDoc `Document`/`Section` flow DOM + `PdfDocumentRenderer`/`FormattedDocument` auto-pagination (kills `FlowBlock`/`FlowFold`), PDFsharp `PdfDocument` security (AES-256), `DigitalSignatureHandler.ForDocument`/`IDigitalSigner` signatures (credential material via the AppHost secrets lease, never AppUi-held bytes), `AcroForm` fields, `UniversalAccessibility` tagged output; `XGraphics` interop with the capture capsule for placed visuals.
- `Mapsui.Avalonia12` — `MapControl`/`Map`/`Navigator`, `GeometryFeature` over Bim NTS, tiling/rendering-Skia layers as basemap rows, `SphericalMercator`/`ProjectionDefaults.Projection` as the ONLY AppUi-side reprojection (geodesy stays Bim's `GeoFeature.Reproject`); the transitive Mapsui/Tiling/Nts/Rendering.Skia set stays transitive.
- `NodeEditorAvalonia`(+`.Model`, the transitive-floor pin `props:511`) — `IDrawingNode`/`DrawingNodeEditor`/pin-connector model on ReactiveUI; the `LoroTree` co-edit bridge discriminates `EventTriggerKind` for echo suppression; export surface for canvas snapshots through the capture capsule; the PanAndZoom transitive-vs-admitted split is disjoint per `[V8]`c (`NodeZoomBorder` is the package's own asset).
- `SkiaSharp`(+harfbuzz/svg/skia-native) — `SKDocument` narrows to the pure-visual vector-print arm; the color-management mine (`SKColorSpace` ICC, float surfaces, `ToneMap` LUTs) stays capture's reference depth; `svg-skia` retained-scene pipeline stays assets'.
- `Silk.NET.WebGPU`(+`Extensions.WGPU`+wgpu native, `csproj:47-49`) — pipeline/shading/meshlets share `ONE_WGPU_DEVICE`; the `[V5]` occlusion raise mines `ComputePassEncoder`/`DispatchWorkgroups` for the depth-pyramid build and `QueryType.Occlusion` as the probe fallback; the timestamp-query mine (`GpuTimeline`) moves with `Diagnostics/governor.md`; `Silk.NET.OpenXR`(+EXT/FB/KHR ext, `csproj:43-46`) stays immersive's certified stack with the macOS loader-absent degrade honest.
- `LiveCharts`(XamlSeries), `DynamicData` (`Virtualise`/`TransformToTree`/one-`ObserveOn` capsule), `Kiwi` (`Try*` family + edit variables per `[V6]`), `PropertyGrid`/PropertyModels (ranked `EditorFactory` metadata lookup), `AvaloniaEdit`+`TextMateSharp` (CodePane capsule), `LibMpv` (mount wiring per `[V6]`), `AsyncImageLoader`, `Markdig` (front-matter/footnote arms live or gated), `HidSharp`/`DryWetMidi`/`Silk.NET.Input`/`Silk.NET.SDL` (the 4-SDK `DeviceDriver` capsules), `Dock.Avalonia`(+`Dock.Model.ReactiveUI`+serializer — `BoardState` + layout blobs over one serializer), `Semi`/`Ursa` over the retained Fluent floor, `Behaviors` (one `BehaviorRail.Intent` bridge), `PanAndZoom`, `HotAvalonia` (devloop), `MessageFormat` (ICU CLDR plural law) — mined-confirmed, zero action beyond registry-band touches and the named `[V6]` repairs.
- Avalonia platform tier — mined-confirmed, previously uncited (stub anchors now law): `api-avalonia.md` (280 loc — the composition/`ICustomDrawOperation`/`GetObservable` core capture/pipeline/controls compose), `api-avalonia-desktop.md`/`api-avalonia-skia.md`/`api-avalonia-fonts.md` (host lifetime + Skia platform + the Inter floor under tokens/typography), `api-avalonia-grid.md` (the `DataGridCollectionView` projection engine — `tables.md`'s ONE tabular boundary; the `tables.md:120` TreeDataGrid rejection stays sealed), `api-avalonia-color.md` (inspector `ColorView` editor row; its `ColorHelper.GetRelativeLuminance` row becomes the `[V10]` deleted form — the catalog gains the Unicolour-routing caveat at `[V12]`, `ToDisplayName` stays), `api-dialoghost.md` (the `DialogIntent` -> static `Show`/`DialogSession` stack `dialogs.md` composes; the `[V6]` `ToastPipe` repair rides it), `api-fluenticons.md` (the 2789-member `Symbol` icon identity — `assets.md`'s canonical vocabulary).
- `api-avalonia-gpu-interop.md` — UNMINED depth, the one catalog whose consumer fence lags its own law: `:3` rules `ICompositionGpuInterop.ImportImage`/`ImportSemaphore` + `CompositionDrawingSurface.UpdateWith{KeyedMutex,Semaphores,TimelineSemaphores}Async` as the composited-presentation family superseding the bare `ISkiaSharpApiLease` path when an external backend owns the render, while `pipeline.md:594` stops at `DeviceLuid`/`DeviceUuid` adapter matching and keeps a `SurfaceConfigure`/`SurfaceGetCurrentTexture` swapchain; the leg-3 pipeline rebuild discriminates the Wgpu presentation arm — the in-tree composited viewport imports the rendered texture through the interop family (a second swapchain in composited mode is the deleted form), the surface swapchain surviving only as the exclusive-fullscreen/headless arm.
- Reactive stack quartet — mined-confirmed, previously uncited: `api-reactive.md` (scheduler/disposable algebra — the one-`ObserveOn` law's substrate), `api-reactiveui.md`/`api-reactiveui-avalonia.md` (`WhenActivated` lifecycle + `ReactiveCommand` under `commands.md`'s one rail), `api-reactiveui-validation.md` (`ValidationContext`/`ValidationRule` lifted into the suite `Validation<Error,T>` applicative — `forms.md`'s stated law); `api-avalonia-xaml-loader.md` re-anchors to `Diagnostics/devloop.md` (zero direct page composition — it is HotAvalonia's runtime-inflation substrate, `PrivateAssets` dev-loop scoped `csproj:127`, the `evidence.md:368-375` knob rows moving with the `[V3]` split).
- `ACadSharp` (write-scoped per `[V7]`) — `DwgWriter`+`DxfWriter` over ONE `CadDocument` typed entity fold; version/unit derivation as policy rows; READ members re-home to the Bim catalog; `netDxf` REMOVED per `[ROSTER_RECONCILIATION]` (archived upstream, feed-latest 2023.11.10) — its `api-drafting-export.md` surface deletes and the DXF fidelity probe lands with `[V7]`.
- `DocumentFormat.OpenXml` — the OOXML part-graph writers move to `Document/export.md` intact.
- `Avalonia.Headless`+`Verify` — the `Diagnostics/proof.md` matrix substrate (CsCheck property lanes + Verify snapshot goldens); the headless capture lanes are the rewire's parity oracle for every `[V6]` fence repair.
- Catalog motion per `[V12]`: DELETE api-unicolour/api-thinktecture-json target copies; trim hashing/nodatime/unitsnet overlays; re-scope api-drafting-export (netDxf surface deleted); re-point api-loro; re-anchor api-pdfsharp/api-mapsui/api-nodeeditor; caveat api-avalonia-color (`[V10]`); stub api-prodiagnostics with the devloop leg deepening it.
- ROSTER rows: ADD — `ProDiagnostics` (devloop inspection; MIT; Debug-gated `PrivateAssets` binding); INTEGRATE — `PDFsharp`+`PDFsharp-MigraDoc`, `Mapsui.Avalonia12`, `NodeEditorAvalonia` (`[V8]`); REMOVE — `netDxf` (feed-verified archived upstream; DXF lands on ACadSharp `DxfWriter`, fidelity-probed at `[V7]`); REJECT recorded — `meshoptimizer` (Compute owns clustering), a second color kernel, speech/MT LiveCaption engines, LoroCs removal, `TreeDataGrid` (`tables.md:120` seal upheld), `Avalonia.Diagnostics` (feed-dead at 11.3.x) and the pay-tiered Accelerate DevTools; GAP recorded — LittleCMS-class ICC/CMYK on a named print demand.

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with THIS brief as the campaign parameter. Within a leg the listed order IS dependency order. The sequencing law:

1. **PERIMETER + STRUCTURE FLOOR** — the `[V3]` structure motion FIRST (Diagnostics/Document/Collab folder mints with skeleton pages, the animation and Editing moves, the MOVE-absorb transfer of capture's `[06]` export sections into the `export.md` skeleton — condemned-but-intact there per `[STRUCTURAL_AUTHORITY]`); then the `[V1]` registry landing COMPLETE on the freshly minted `Diagnostics/evidence.md` (every 6xxx row assigned including the NEW union bands; a duplicate integer unrepresentable from the first leg; each union's re-derivation lands with its owning page's leg); the `[V9]` ledger corrections + codemap/router re-emit to the 43-page map; the `[V12]` catalog + manifest motion (the `netDxf` REMOVE and `ProDiagnostics` ADD land here); the four upstream consumer-pressure clauses verified in place (never re-applied).
2. **VOCABULARY + SHELL SPINE** — `Theme/` (tokens/motion cold with bands; typography front-matter arms per `[V6]`; assets band; locale typed `LocaleFault` + LiveCaption gate); `Shell/` (controls `Rebind`/`Apply` real, virtualization Fenwick, solver contracts, navigation `NavFault` + scheme parse, dialogs `IO<Unit>`, input band, accessibility Unicolour luminance per `[V10]`, hosts/commands/screens cold with bands, the `commands.md:197` payload digest re-routed through the kernel `ContentHash.Of` per the one-hasher law).
3. **RENDER PLANE** — `pipeline.md` (ordinal thread, prose resync, band); `meshlets.md` (`[V5]` consumption — Compute clusters, wire cones, hysteresis LOD cut, HZB occlusion pass, LOD-column gate); `pathtrace.md` (`[V6]` real BVH/enclose/degradation-refit — page-local, private, kernel-wire-respecting — + honest accumulate + the `LightSource` rig shared with shading); `shading.md`/`immersive.md` (bands, honest gates); `reality.md` (`[V5]` carriers + payload-key identity + SplatSort disposition); `animation.md` (post-move rebuild: PingPong direction, scrub marshal, typed `AnimationFault`, the lerp-delegate tail collapsed to track-owned interpolation policy rows — the Camera row the one pose-slerp site, the Color row the tokens OKLab mix — the `Walkthrough` fold hardened as the target tour's leg-4 projection collapses onto); `capture.md` (post-extraction raster/codec owner + the `[V10]` gamut singleton + the runtime `ContentHash` delegate bound to the kernel `ContentHash.Of` entry per the one-hasher law); `drafting.md` (`[V7]` — region basis, the one-`CadDocument` two-row ACadSharp write leg with the DXF fidelity probe, version policy row, retired Geometry rows; the sheet-PDF egress signature-locks on the leg-1-moved export destination vocabulary, its MigraDoc deepening verifying at leg 4's output dry-run).
4. **CONSUMER PLANES + CLOSEOUT** — `Collab/` (`sync.md` `[V2]` recharter with the text-container criterion probed; `issues.md` `[V4]` map-container collapse; `tour.md` `[V3]`e collapse onto animation — its offline arm rides animation's leg-3 `Walkthrough` fold and the leg-1-moved `VisualDestination`, zero forward dependency on export's leg-4 deepening); `Document/` (`notebook.md` `[V11]` + `[V2]` re-anchor; `media.md` mount wiring; `export.md` NEW per `[V8]`a absorbing capture's flow/Office arms); `Editing/` (inspector diff3, tables quoting, forms/history/livedata cold with bands, `graph.md` NEW per `[V8]`c); `Charts/` (dashboards typed `ChartFault` + geo-split re-anchor, custom `ColorSpaceAxis` disposition, `basemap.md` NEW per `[V8]`b); `Diagnostics/` (evidence/proof/devloop/governor full rebuilds off the split skeletons — the leg-1 `[FAULT_TABLES]` block is SEALED, preserved verbatim with rows append-only); the final `[V9]` governance truth pass; acceptance dry-runs.

Per-leg closeout: residuals resolve in-run — every pass agent repairs the cross-file ripples its own work exposes in-pass, and the leg's serialized per-wave tail closes index docs and central manifests before it returns; `ARCHITECTURE.md`/`README.md` close in the leg that changes their facts; `.api` stubs, re-anchors, and catalog edits land with the leg that moves or first composes the package, and the leg that first composes an integrated package deepens its catalog; cross-package counterpart obligations (the Compute cluster-LOD columns with the monotonic-error guarantee `[V5]`, the Compute solar-position member export `[V6]`, the Persistence op-log ingest + replay-window read `[V2]`, the AppHost 6xxx mirror row `[V1]`, the AppHost secrets-lease signing ingress `[V8]`, the Fabrication `FAB:52` write-leg wording refine `[V7]`, the kernel `Rasm/ARCHITECTURE.md:91,95` view→AppUi / flatten→AppUi mirror-row re-scope plus the CONTENT_KEY fan's reciprocal AppUi row `[V7]`/`[V9]` — the geometry campaign's `[V14]` ledger duty, the Bim catalog READ-row adoption `[V12]`) are RECORDED in leg returns with AppUi named as the demanding consumer — never edited into sibling interiors; every `[V2]`/`[V5]` signature-lock against a Persistence/Compute-owned spelling resolves against the emitted upstream DECISION when it exists and otherwise locks on the upstream brief's ruled defaults with the re-verify recorded as a leg residual. Campaign acceptance closes on composed contracts, not the ledger: (a) a collaboration dry-run — two live replicas converge through one `LoroDoc`; a committed edit lands as a typed Persistence-owned op row; a FRESH process rehydrates the document from the replay window ALONE (zero Loro bytes read from durable truth) and matches the live frontier at content parity; a NEW peer joins the rehydrated epoch via live-peer state sync and a live delta round-trips both directions (zero Loro bytes crossing durable truth on either path); the snapshot blob deletes and cold-load still succeeds; an issue comment lands as a map-container op with `CommentOp` unrepresentable; (b) a fault-space audit — `rg 'base\(detail, [0-9]'` and `rg 'base\(detail, 0x'` return zero under `.planning/`, every `Code` resolves through exactly one 6xxx registry row, the three collisions unrepresentable, the AppHost mirror row landed both directions; (c) a residency dry-run — a viewport tile renders from a Compute `meshlet-cluster` payload with ZERO AppUi-side clustering, cull decisions read per-cluster wire cones, a cluster fully occluded by the prior-frame depth pyramid draws nothing, the LOD cut holds stable under the hysteresis band across a dolly move, a splat tile keys by the payload's own `ContentKey`; (d) an output dry-run — a paginated flow report with running headers/footers renders through the MigraDoc owner, a signed tagged-PDF (its `IDigitalSigner` composing AppHost-leased credential material) and an XLSX cross the same export owner, drafting emits DWG+DXF through one `CadDocument` fold (the DXF fidelity probe green) + sheet-PDF with per-region projection bases, a basemap tile draws a Bim NTS overlay, a node-graph canvas round-trips over `LoroTree`; (e) the perimeter audit — every ledger row composed in a fence and every fence crossing ledgered, every catalog anchoring a live page, the router and codemap carrying exactly 43 pages in 8 folders, and the one-hasher audit green — every AppUi content-identity mint resolves through the kernel `ContentHash.Of` entry and `rg 'XxHash128\.Hash'` returns zero under `.planning/` outside decode-only payload-key reads. A dry-run reaching for a missing owner reopens `[V2]`/`[V5]`/`[V8]` as residuals.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Persistence` interiors: the `OpLogEntry`/`SyncOpKind` row shapes, the ledger changefeed, the replay-window read, the blob lane, and the `CrdtOpWire` vocabulary are PBRIEF `[V2]`/`[V3]`/`[V12]` work — AppUi projects and decodes; the waterfalled ledger consumer-pressure row is verified in place; no further upstream edit is open.
- `Rasm.AppHost` interiors: the registry mechanics, transport/topics, `RecomputeGraph`, and the port implementations are ABRIEF work — AppUi composes settled exports; the waterfalled 6xxx mirror row and the `[V12]` secrets-lease signing-consumer clause are verified in place.
- `Rasm.Compute` interiors: the `meshlet-cluster` encode arm, cluster-LOD columns, splat payload decode, and `ONE_WGPU_DEVICE` custody are CBRIEF work — the waterfalled cluster-LOD clause (with the monotonic-error cut guarantee) is verified in place; the SPZ/SOG companion decode stays `[UPSTREAM-BLOCKED]` (CBRIEF `[06]`).
- `Rasm` kernel and `Rasm.Fabrication` interiors: the landed kernel corpus is standing law consumed as landed fences — the `DrawingProjection` engine (`Drawing/view.md`), the spatial index (`Spatial/index.md`), and Fabrication's consumption of the kernel engine are those campaigns' work; AppUi stays on the Fabrication `HiddenLineResult` receipt, composes `ContentHash.Of` as a landed public surface without editing kernel interiors, and records consumer pressure only (the `Rasm/ARCHITECTURE.md:108,112` mirror-row re-scope and the CONTENT_KEY-fan AppUi row are recorded kernel-side counterpart obligations); the `FAB:52` write-leg wording refine is the Fabrication campaign's recorded counterpart obligation.
- `Rasm.Bim` interiors: BCF topic semantics, the ACadSharp READ surface, NTS geospatial ownership, and the `ScheduleNetwork` domain — consumed at declared seams only; the READ-row catalog adoption is that campaign's recorded obligation.
- `Rasm.Materials` interiors: `LayeredBsdf`/`SlabStack`/`SurfaceShade` are consumed at the PATH_TRACE seam — no Render-side lobe math, the standing law.
- App-root composition: Rhino/GH2 host binding, exporter pins, process attachment — the APP stratum's; AppUi never grows a composition root, and the in-Rhino ALC firebreak (`loro.dylib` companion-only) stays law.
- The live-Rhino embed probe (`SurfaceHost.RhinoPanel`/`EmbedCapsule`/`RenderGraph.Lease` GPU-lease validation) — host-gated per the `[V9]` standing blocker, untouched.
- The typescript viewer/state/wire counterparts of the declared TS projections — consumed at the boundary wires (`GeometryResidencyWire`, `CommandPayloadWire`, `LayoutConstraintWire`, `EvidenceFeed`), never re-planned here.
- The federation-wide fault-band renumber beyond the AppUi 6xxx registry — escalation path only if the `[V1]` neighborhood cannot fit, which its registry headroom forecloses.
