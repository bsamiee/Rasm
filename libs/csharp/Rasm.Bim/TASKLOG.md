# [RASM_BIM_TASKLOG]

Open and closed work distilled from `IDEAS.md`. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open task card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
- Atomic: <present only on a minor-scope task; names the small unit so a later session sizes its turn>.
Capability, Shape, Unlocks, and Anchors are required on every open card, Atomic included; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Tasks state landing-grain work decomposing an idea.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[USD_SCOPE_GROWTH]-[COMPLETE]: `Exchange/import#IMPORT_RAIL` landed `UsdScope` (`WholeStage`/`Populated` with the railed path admission over `SdfPath.IsValidPathString` + the absolute-and-prim predicates) as the optional entry argument on `ImportGeometry`, the USD arm opening through `UsdStage.OpenMasked` over a `UsdStagePopulationMask` built once from the admitted paths — open-time population, never a post-open prim filter — with the subset partition, prototype exclusion, and `Declared` lane evidence unchanged below the open; `Model/faults` gained the `Detail.UsdScopePath` row and `api-usd.md`'s anchors now name the landed consumer, every member assay-verified.
[ENERGY_RESULTS_ADMISSION_FOLD]-[COMPLETE]: `Energy/results#RESULTS_ADMISSION` landed — `EnergyResults.Admit` folds the Compute-receipt-shaped `EnergyResult` rows onto content-minted `PropertySource.Derived` `Pset_EnergyResults` bags on the reconstruct-bag precedent, the standing egress re-emitting them as IFC Psets with zero new code; `Eui` mints through the sanctioned open `QuantityType.Create` because UnitsNet's `Irradiation` shares `M·T⁻²` under a radiometric name, and template QA deliberately never audits derived analysis evidence.
[PROGRESS_COMPARISON_FOLD]-[COMPLETE]: the comparison fold and typed receipt landed at `Planning/progress.md` — one cluster, the join through the federation `ExternalId` idiom, faults composing `ScheduleNetwork.BindAssignments` whole so the detail token stays the schedule's own, and the capture key minted as a plain `ContentHash` fold rather than a re-minted `CaptureLineage` (the one-type-two-key-spaces defect reconstruct closed stays closed).
[EARNED_VALUE_ACTUALS_JOIN]-[COMPLETE]: `Planning/cost#EARNED_VALUE` gained the three-tier actual election with the precedence law stated — observed `ProgressEvidence` outranks the authored `IfcTaskTime.Completion` (dispute-grade evidence beats authored claims), the interval fraction last — and the `ConstructionResource.Spent` ACWP middle tier (recorded accounting > resource `Spent` join > duration proxy) with the now-public `Fraction` composing one interval-fraction law across both pages.
[CONNECTION_KEY_LOWERING]-[COMPLETE]: both ends pinned — `PreserveInterface` over the existing `IIfcProfileStore` fragment lane stamps the `Connect.Interface` slot (element arm) and the `InterfaceKey` attribute row (2nd-level space boundary, the closed-`ConnectKind` discriminant stated), and the egress `ReauthorRelationships` re-materializes both through `InterfaceOf` on the now-railed `Fin<Unit>` fold, an unanswerable key faulting `DanglingReference`; `Compute` reads connection-interface geometry one-hop by content key and re-exported analysis models keep their boundary surfaces.
[QUANTITY_GROUP_AXIS_ENDS]-[COMPLETE]: the flatten stamps `GroupIdentity` rows per complex prefix and `RaiseQuantities` rebuilds the full nesting tree by longest-prefix ownership — the seam column (`Rasm.Element` `[QUANTITY_GROUP_COLUMN]`) landed first in the same pass, so the tension resolved in order and the prefix-only reconstruction is the named deleted lossy form.
[APPEARANCE_EMIT_BINDING]-[DROPPED]: the `Author`/`Bind` egress pair exists WHOLE and armed at `Semantics/appearance#APPEARANCE_PROJECTION`, and the `Projection/egress` Boundary carries the arming law as standing page text — a standing card re-carrying it duplicates boundary law while blocking on a capability the charter deliberately excludes (geometry egress rides glTF/3dm); the pair fires the day an IFC-body-emit capability is chartered, which is that charter's own card.
[TYPE_CANDIDATE_EXPORT]-[COMPLETE]: `Exchange/import#REIMPORT` gained `TypeCandidate` and `BimIo.ExportTypeCandidates` — candidacy IS the reconciliation verdict (`TypeSignatureSet` bag presence, no second trust column), the durable leg stays `ProvKind.Import` derived lineage with zero store column, and the Materials `AdmitImported` counterpart landed in the same pass with the co-signed `ComponentOrigin` provenance column.
[BCF_RESPONSE_CARRIER]-[COMPLETE]: `Review/issues#TS_PROJECTION` gained the response half — `BcfOutcome` the closed typed family, `BcfApi.Open` the ONE status-gated total Switch over the same `BcfResource` union re-admitting every body through the snake_case `BcfApiContext` register and the `BcfWireMapper.ToDomain` inverses, and `BcfQuery` the OData `$filter`/`$orderby`/`$top`/`$skip` axis whose `Skip` cursor is the one paging mint; execution stays on the Compute transport.
[SCHEDULE_CHARTS_SEAM]-[COMPLETE]: the blocker was stale on its own rail — `Rasm.AppUi` `Charts/custom` `PlanFeed.Of(ScheduleNetwork, …)` and `Render/animation` `SchedulePlayback.FromSchedule` consume the planning receipts today, so the `Planning -> Rasm.AppUi [RECEIPT]: ScheduleNetwork` seam row landed at both `ARCHITECTURE.md` maps beside the standing `CostSchedule`/`ConstructionState` receipt edges; the phantom AppUi `[V9]` growth-register anchor is retired with it.
[ELEMENT_SET_VIEWPORT_SEAM]-[DROPPED]: the branch `[05]-[PROCESS]` ruling is the standing law — a projection seam row enters the `ARCHITECTURE.md` seams map only on a live consumer fence and deferred pressure never rides the ledger — so a standing card re-carrying that pressure duplicates the ruling while anchoring to a phantom AppUi `[V9]` register; the AppUi prose mis-attributing the Persistence `Query/lane#ELEMENT_SET_ALGEBRA` receipt struct to Bim is repaired scope-qualified at both AppUi sites, and the row re-enters the moment an AppUi page lands a consuming fence.
[MOISTURE_DIFFUSIVITY_BASE]-[COMPLETE]: the IFC4.3 measure table declares moisture diffusivity in `m3/s`, so the `MeasureDimensions` row re-signed `L³T⁻¹` (`Dimension.Create(3, 0, -1, …)`) beside the `T¹` vapor-permeability row the same schema clause fixes; the hygric rows now carry the schema-declared-base comment and the research row is closed.
[READER_ROWS_RECONCILE]-[COMPLETE]: every row the connection and structural ingests stamp resolves to a `Rasm.Element` `StructuralRows` static or a `PropertyCategory.Seam.Row` mint and no call-site `PropertyName.Create` survives either reader; the reconcile collapsed the restraint twin — the six DOF rows carry restraint-versus-spring on the `PropertyValue` case — and folded the prefix-built frame name family onto the one declared `StructuralRows.Frame` list row. `Rasm.Compute`'s mirror `[STRUCTURAL_ROW_STATICS]` reads the same statics and landed the collapsed DOF and frame shapes at its end.
[BRICK_SYSTEMS_PROJECTOR]-[COMPLETE]: `Model/systems#CONNECTIVITY` `DistributionNetwork.BrickProjection` lowers the settled `DistributionSystem` view onto the Brick graph, returning the `BrickGraph` the `[WIRE]: BrickGraph` seam carries; the blocker was REFUTED on its own rail — `Rasm.AppHost` `Wire/livewire` is the named live-binding owner whose transport axis carries the BACnet/Modbus point rows — and class election with interval-rollup analytics stays that seat's through the injected `BrickBinding`, since `AddEntity<T>` binds its Brick class at compile time.
[SEAM_REGISTRY_RECONCILE]-[COMPLETE]: seam registry re-anchored to page-owned spellings — `GeoFeatureWire` holds as the seam wire at every endpoint against the interior `GeoWire` projector, `BcfTopicWire` verified page-owned at its typescript core consumer and carried bilaterally, `ModelDiff` verified page-owned as the cross-runtime wire; the `typescript:core` `[BIM_CENSUS_RECONCILE]` census now reads current mints.
[HOOK_RAIL_ROSTER]-[COMPLETE]: `Model/observability#HOOK_RAIL` owns the point roster and the per-composition `BimHooks` registry record over the kernel point capsule; modality rows and subscriber-fault isolation arrive settled from the kernel, faults parking as `IsolatedFault` rows on the composition evidence cell.
[PROGRESS_POINT_WIRING]-[COMPLETE]: progress points wired — `Exchange/import#IMPORT_RAIL` `AcadReader.Read` fires `rasm.bim.exchange.progress` off `ICadReader.OnProgress`, `Energy/derive#TRANSLATE_MATRIX` `TranslateProgress` fires `rasm.bim.energy.progress` off `onPercentageUpdated`.
[INSTRUMENT_ROSTER_MOUNT]-[COMPLETE]: `Model/observability#TELEMETRY_TAP` owns the instrument roster as kernel `InstrumentSpec` declarations carrying kind, measurement form, `Buckets` advice bounds, and dotted `rasm.bim.<dimension>` slots, materialized by an app fan through the contributor port or bound directly by a root through `InstrumentSet.Of`; every projected write rides the kernel `InstrumentSet.Write` rail out through the capsule's rail-shaped `Observe`, which parks the refusal point-attributed.
[SPAN_ATTRIBUTION_LAW]-[COMPLETE]: span and attribution law rows landed on `Model/observability#TELEMETRY_TAP` — the kernel `SpanBand` over `BimPoint`-derived planes taken on a nullable receiver, `Op`-derived span names, the kernel `TenantContext` partition on every metric write, and `rasm.bim.model` stamped on the span alone from `Traced`'s own required argument.
[EVENT_ENVELOPE_PROJECTION]-[COMPLETE]: `Exchange/events#EVENTS` owns `BimEvent`, the `BimEnvelope` projection over `JsonEventFormatter` with the traceparent/tracestate extension rows, and the `rasm.bim.<domain>.<fact>` type law.
[EVENT_MINT_ROWS]-[COMPLETE]: mint rows pinned — versioning `CommitLanded`, issues `IssueMutated`, validation `VerdictIssued`, export `ArtifactMinted`, energy exchange `EnergyMinted`.
[BENCH_RECEIPT_ROSTER]-[COMPLETE]: `Model/observability#BENCH_RECEIPTS` owns the `BimBenchClaims` kernel `BenchClaim` roster and `BimBenchReceipt` record under the AppHost corpus-gate admission row.
[SURFACE_TEXTURE_INGEST]-[COMPLETE]: `Semantics/appearance#APPEARANCE_PROJECTION` carries IFC texture styles both ways — the `TextureMode` mode-to-canonical-channel roster with its gloss/transparency polarity column, the `SurfaceTexture` `[Union]` over the URL, blob, and pixel-extent payloads with the `UvTransform` frame, and `StyledAppearance` pairing the roster with the content-keyed node so the frozen seven-value summary preimage stays sealed; egress lands every element select through ONE five-slot `IfcSurfaceStyle` constructor whose null slots carry the absent axes.
[TEXTURE_COORDINATE_BINDING]-[COMPLETE]: `Semantics/appearance#APPEARANCE_PROJECTION` `SurfaceTexture` carries `CoordinateSet` universally with `At` the re-stamp and the egress writing it back through `IfcSurfaceTexture.Parameter`; `Exchange/tessellation#EXPLICIT_TESSELLATION` resolves the ordinal off `IfcTessellatedFaceSet.HasTextures`, so `Exchange/export#EXPORT_RAIL` binds a measured set onto a mesh that now carries `TEXCOORD_0`.
[APPEARANCE_ARITY_ALIGN]-[COMPLETE]: the Bim lowering composes the seam `AppearanceSummary.Of(…, Op key) -> Fin<AppearanceSummary>` arity — `SummaryOf` returns `Fin` under the projector's own `Op`, the phantom `tolerance: 0.0` named argument is gone, and the raw-IEEE-bit writer stays inside the seam factory where no caller can fork the shared dedup key.
[GLTF_TEXTURE_CHANNEL]-[COMPLETE]: `Exchange/export#EXPORT_RAIL` binds texture maps onto the pooled glTF material through the `GltfChannel` roster — `ChannelImage.Of` admits a CANONICAL channel name and refuses one glTF has no slot for, its row naming the `KnownChannel` targets one `ImageBuilder` threads and the `KhrExtension` the channel obliges — and `MaterialFinish` keys the material pool on the seam `AppearanceKey` and every bound map so a textured element never inherits an untextured neighbour's material; registration unions the payload's own obliged rows with the policy roster.
[PLATE_FLOOR_HOLES_LOWER]-[COMPLETE]: `FootprintPolygon` carries `Holes` with the seam `Area` Newell fold, `Dragonfly` fills `Room2D(floorHoles:)` per interior ring through the same `Open` normalization, and the honeybee `Face3D` lower passes hole loops — a courtyard, atrium, or lightwell subtracts from conditioned floor area at both arms; the `Rasm.Element` `[FOOTPRINT_INTERIOR_RINGS]` counterpart closed with it.
[IFC_VERTEX_COLOUR_LANE]-[COMPLETE]: the blocker was REFUTED by live probe — `IfcColourRgbList.mColourList` and `IfcIndexedColourMap.mColourIndex` bind through the `Semantics/appearance#APPEARANCE_PROJECTION` `IfcInternals` `[UnsafeAccessor]` capsule, so `Exchange/tessellation#EXPLICIT_TESSELLATION` `Decode` fills `EncodingChannel.ColorRgba` from `HasColours`, the read landing as the shared `IndexedColour` value; the arm carried a shape correction — per-FACE colour runs and per-triangle UV index runs both address CORNERS, so either present switches the walk to a per-corner unwelded gather.
[IFC_TEXTURE_MAP_BINDING]-[COMPLETE]: `Semantics/appearance#APPEARANCE_PROJECTION` `Bind` closes the UV round trip the tessellation ingest opened — `IfcIndexedTriangleTextureMap` mints through the same accessor capsule, `Maps`/`TexCoords`/`MappedTo` and the per-triangle index triples land, and the `Binders` table drives every seam attribute lane onto its representation item, so the `ColorRgba` egress rides the same entry on public surface alone; both rows guard arity first, because a lane whose corner run walks off its own end authors a file that faults in the receiving application.
[GLTF_MATERIAL_FACTORS]-[COMPLETE]: `Exchange/export#EXPORT_RAIL` `MaterialFinish` carries the seam `AppearanceSummary` whole and `Author` is the ONE material mint under the `RULINGS.md` factor law; the display `0xRRGGBBAA` the dotbim column reads derives through the appearance projector's own sRGB OETF, and the `InterchangePolicy` presets drop every extension row no finish column can fill.
