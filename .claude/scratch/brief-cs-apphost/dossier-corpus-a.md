# DOSSIER-CORPUS-A — Rasm.AppHost .planning survey (lane corpus-a)

Coverage: `.planning/` subfolders alphabetical = {Agent, Observability, Runtime, Sandbox, Wire}; corpus-a = FIRST HALF = {Agent, Observability, Runtime} = 20 pages, ALL deep-read. Plus package `ARCHITECTURE.md`+`README.md`+`TASKLOG.md`+`IDEAS.md` full, `libs/.planning/architecture.md`, and upstream `RASM-CS-{GEOMETRY,PERSISTENCE,COMPUTE}-BRIEF.md` + `RASM-COMPONENT-PARADIGM-DECISION.md` (FaultBand registry law). corpus-b owns {Sandbox, Wire} — referenced-only here where seams cross.

TELOS-LEVEL VERDICT (mirrors the Persistence/Compute sibling pattern exactly): the fence INTERIORS are genuinely world-class (dense ADT collapse, `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` + FrozenDictionary dispatch, LanguageExt `Fin`/`Validation`/`IO` rails, `ClockPolicy`-owned time, `XxHash128` content-addressing, one-owner-per-axis, honest phantom-flagging). The rot is the PERIMETER: no fault-band registry (a CONFIRMED collision), four Persistence PORT seams that INVERT the finalized Persistence-V2 ownership law (AppHost types pushed downward), a stale/deleted Persistence-2PC seam, a Health driver roster the Persistence prune obsoletes, and an over-claimed "seven ports only" invariant contradicted by pervasive sibling-type composition. Grades below are interior grades; the campaign is a perimeter+seam rebuild, not a fence teardown.

---

## PER-PAGE VERDICTS

### Agent/ (6 pages)

**Agent/capability.md — 9/10** (567 LOC, the biggest page; DESCRIPTOR_AXIS/DISCOVERY_FOLD/COMMAND_ALGEBRA/GRANT_BROKER/SDK_CODEGEN). World-class self-describing op catalog: `CapabilityDescriptor` one op-metadata owner, `DiscoveryQuery` `[Union]` polymorphic discovery (no Get/GetBy proliferation), commit-or-rollback `CommandAlgebra` over the Compute rail, fenced `GrantBroker`, tri-language codegen off one source, air-gapped `TiktokenTokenizer` pricing.
- DEFECT (FIT, top): `DistributedBudget.Debit(TenantId, FencingToken, CostVector, CostVector)` + `Token → Fin<FencingToken>` (`:420-423`) push AppHost's `FencingToken` (owned time.md) AND `CostVector`/`CostUnit` (owned here) DOWN into the Persistence store — violates the mandate's "AppHost PORT adapters DECODE Persistence-owned types and never push an AppHost interface or type downward."
- DEFECT (FIT, waterfall): the debit is a multi-unit `CostVector` (5 `CostUnit` rows, each ceiling-capped independently, `:62-67,468-472`); Persistence-V2 specs a SCALAR "balance >= debit" CAS — a capability gap in the upstream owner.
- DEFECT (band): `CommandFault` 4600-4605 (`:258-263`), `GrantFault` 4620-4625 (`:403-408`) hardcode base integers, no registry.
- Owner charter AS-SHOULD: unchanged owner set; `DistributedBudget` re-shaped to decode a Persistence-owned coordination result over a projected primitive request (opaque token + per-unit debit/ceiling scalars); fault codes derive `Band+n` from an AppHost fault-band registry.

**Agent/federation.md — 9/10** (361 LOC). Excellent inversion of the mcp projection: folds external MCP servers inward as brokered `CapabilityDescriptor` rows under `federated.{server}.{tool}`, reuses the one `CommandAIFunction`, `TrustScope` permission floor, `FederatedDispatch` over `OutboundHop`, resource/prompt/subscription fold into the livewire `SubscriptionLane`. `TransportKind` `[SmartEnum]` closed / `FederatedServer` `[ValueObject]` open-identity — the correct discriminant split.
- DEFECT (band, revealing): `FederationFault` 4800-4805 (`:102-107`) "fresh of every occupied AppHost band"; the boundary prose at `:22` hand-lists the AppHost band map ("4640 McpFault, 4720-4725 live-wire WireFault, 4760-4763 ReplayFault, 4780-4783 SecretFault, 4790-4799 PemFault") and **OMITS `IdentityFault` 4640 entirely** — proof the manual per-page band scheme is already inaccurate and collision-blind.
- DEFECT (seam-direction): composes `ComputeIntent.Federated` (Compute type) — the AppHost→Compute reference pattern (see VC5).
- Charter AS-SHOULD: unchanged; fault codes from the registry; the hand-kept band-census prose dies for one registry pointer.

**Agent/identity.md — 9.5/10** (308 LOC). Award-grade OIDC boundary: per-issuer `ConfigurationManager<OpenIdConnectConfiguration>` rotating JWKS, `JsonWebTokenHandler.ValidateTokenAsync` → one `Principal`, `OpenIddictClientService` flow-discriminated acquisition, standalone `AddAuthorizationCore` ABAC gate; identity-store read is a clean Persistence `TenantId`-RLS seam (`:308`, matches ARCH:75). Correct three-ordered-seams law (authn `Principal` → policy gate → grant broker).
- DEFECT (band): `IdentityFault` 4640-4648 (`:119-127`) **COLLIDES with `McpFault` 4640-4644** (mcp.md:125). `Expected.Code == 4640` is ambiguous between two fault families — the exact Persistence-E4 defect. Namespaces disambiguate the type symbol, not the telemetry code.
- DEFECT (anchor): references `Runtime/config#SECRET_MATERIAL` (`:3,171,307`) — config.md has NO `SECRET_MATERIAL` section (its sections are `SECRET_LEASE` [05] and `CREDENTIAL_PEM` [07]). Stale intra-package anchor.
- Charter AS-SHOULD: unchanged; re-band via registry; re-point `#SECRET_MATERIAL` → `#SECRET_LEASE`/`#CREDENTIAL_PEM` (or the extracted `Runtime/secrets.md`, VC7).

**Agent/mcp.md — 9.5/10** (361 LOC). Excellent SDK projection: registry→`AIFunction`, `McpServerTool.Create` adoption fenced at one `ToolProjection.Adopt` site, `ApprovalRequiredAIFunction` human-in-the-loop on irreversible effects, brokered dry-run preview, SSE-resumable `StreamProgress` with HLC-cursored replay, task primitives. Honest `.api` verification (RESEARCH catalogues every SDK member).
- DEFECT (band): `McpFault` 4640-4644 (`:125-129`) collides with `IdentityFault`; `:111` deliberately picks 4640 "the JSON-RPC error-code band the MCP transport maps" — a legitimate JSON-RPC intent but the base band must be registry-assigned and disjoint.
- Charter AS-SHOULD: unchanged; the JSON-RPC-to-band mapping stays but rides a registry-assigned McpFault band; the WireFault namespace-collision note (`:22`) is real and correctly handled by alias.

**Agent/reasoning.md — 9.5/10** (534 LOC). World-class model governance: one `ChatClientBuilder` fold (`UseOpenTelemetry`/`UseDistributedCache`/`GoverningChatClient`/`UseFunctionInvocation`), `ModelRoute` `[SmartEnum]` feature-verdict routing, `GoverningChatClient : DelegatingChatClient` collapsing model-select + content-filter into ONE public subclass (decompile-justified vs the internal `AnonymousDelegatingChatClient`), `SemanticDiscovery` embedding-rank adding one `DiscoveryQuery.ByIntent` case, replayable content-addressed transcript, gated `[Experimental]` modal input. Two front doors (in-process loop + MCP server-sampling) share one pipeline.
- DEFECT (anchor, cross-file): ARCHITECTURE.md:86 anchors this M.E.AI seam at phantom folder `Model/agent → csharp:Rasm.Compute/Model` — AppHost has no `Model/` folder; owner is `Agent/reasoning#MODEL_GOVERNANCE` (+ `Agent/capability#ModelProjection` for pricing).
- DEFECT (seam-direction): composes `ComputeIntent` (see VC5); no own fault band (rides `CommandFault`) — acceptable.
- Charter AS-SHOULD: unchanged; re-anchor ARCH:86 to `Agent/reasoning`; the AppHost-owned generative M.E.AI port vs Compute retrieval split (Compute-E15) is correctly honored.

**Agent/runtime.md — 9/10** (103 LOC, smallest page). Clean declaration-only restructure: `CommandDispatch.Run(CommandIntent) → CommandReceipt` consolidates the capability/mcp/reasoning dispatch the spine referenced but never declared, resolving the dangling reference; three callers (operator/agent/plugin) + orchestration-step + bus-subscription → one transaction; chains each receipt into the one `EventLog`. No second dispatcher.
- No material defects. Charter AS-SHOULD: keep as the single `CommandDispatch` front-door owner over `CommandAlgebra`; `Project` fold shared with mcp `McpDispatch.Project`.

### Observability/ (3 pages)

**Observability/health.md — 9/10** (544 LOC). World-class: `HealthContributorRow`/`DriverProbe` one-adapter-binds-any-Xabaril-check, `ResourceMonitoring` observable-instrument `UtilizationCell`, coherent `Atom<DegradationReading>` (READ here / WRITE at `Wire/companion#DEGRADATION_CASCADE` — race-free split), five-level `DegradationLevel` retained-capability sets, `AlertEngine` (threshold/anomaly/forecast with hysteresis + backtest + versioning). `ShedVerdict`/laneguard reads the coherent cell.
- DEFECT (FIT, the mandate's :85 obligation): `DriverProbe` roster (`:100-110`) `Cache=redis` over singleton `IConnectionMultiplexer` (`:76-77,102`) + `Broker=kafka` over topics `ProducerConfig` (`:103`) assume a raw-driver roster the finalized Persistence campaign PRUNES/RE-HOMES — Persistence-V7 prunes `StackExchange.Redis` direct ref (L2 rides `Caching.StackExchangeRedis` transit), Persistence-V3 makes Kafka an egress SINK row with NATS the spine anchor. The roster must re-write integration-first.
- Charter AS-SHOULD: `DriverProbe` axis stays; ADD `DriverProbe.Nats` (admit `AspNetCore.HealthChecks.Nats`), re-point `Cache` to probe the L2 caching-transit `IDistributedCache` (not a raw multiplexer), keep Kafka as a conditional broker sink row; README `[OBSERVABILITY]` health-check package group re-aligns to the Persistence backing-service roster.

**Observability/telemetry.md — 9.5/10** (405 LOC). World-class: `TelemetrySource`/minted `IMeterFactory` pairs, W3C trace-context `TraceContext.Inject/Extract/Continue` over gRPC metadata, Serilog-vs-OTel per-profile `LogPipeline`, `SignalGovernance` (samplers/buffers/`AddView` cardinality caps incl. tenant-tag `*` cap 256), nine-row `DataClassification` redaction bound at every exporter seam, GenAI conventions, latency-context checkpoints (no `Stopwatch`).
- DEFECT (count drift): page-header (`:2`) says "the seven-row `DataClassification` taxonomy" but the `[06]` fence + header (`:350,368-378`) carry NINE rows (None/Operational/Internal/HostIdentity/UserContent/Personal/Confidential/Credential/Secret).
- DEFECT (seam, minor): `DataClassification → Persistence ClassificationGuard` (`:355`, "consumed by Persistence ClassificationGuard through GetRedactor") is composed in prose but not obviously in ARCHITECTURE.md [02]-[SEAMS] — undeclared shared-vocabulary seam.
- Charter AS-SHOULD: unchanged; fix 7→9 count; declare the `DataClassification` shared-vocabulary export as a seam row.

**Observability/bundles.md — 9/10** (326 LOC). Excellent bounded diagnostic capture: `SupportTrigger` `[Union]` six causes, window-freeze → ordered contributor fan → redact-before-write → cap-with-truncation-receipts → one zip, retention sweep on the schedule port, `Active`-cell coalesce gate. `FaultTransition` reuses the collapsed `FaultRecord` fact (COLLAPSE_FAULT_SOURCE_SUPPORT_TRIGGER).
- DEFECT (typed-rail): `ProcessDump.Produce` uses bare `Error.New(9300, "dump-admission-gated")` (`:90`) — the ONLY corpus-a owner with no typed fault union, and 9300 is an orphan band (AppHost bands are 1xxx/4xxx). The exact "a bare `Error.New` is the deleted form" doctrine breach.
- Charter AS-SHOULD: unchanged owner; the dump-gate failure becomes a typed `SupportFault` case in the registry band, killing the bare `Error.New(9300)`.

### Runtime/ (11 pages)

**Runtime/ports.md — 9.5/10** (223 LOC). The cross-package seam owner: seven inward port records, `PortCardinality` invariant (eighth port = named defect), `ReceiptEnvelope` HLC two-half stamp + `TenantContext` value-object, one `Strict` wire context + `SuiteContracts.Wire`/`Schema` merge, TS tooling map. The HLC `Advance` logic is correct (physical-dominant, logical reset).
- DEFECT (over-claim, VC5 root): `:3` "exactly seven typed port records as its **only** cross-package seam; siblings adapt to them and the dependency never reverses" — contradicted by pages that compose sibling-interior types (ComputeIntent, OpLogEntry, driver instances) and push AppHost types down.
- DEFECT (verification): `JsonSchemaExporter.GetJsonSchemaAsNode`/`JsonSchemaExporterOptions.TreatNullObliviousAsNonNullable` flagged "not in folder .api/ catalogue, open verification" (`:114`) — catalog debt.
- Charter AS-SHOULD: keep the seven-port owner; the "only seam" prose restates honestly (seven inward DELEGATE contracts + a named outward shared-wire-vocabulary tier; see VC5).

**Runtime/resources.md — 9.5/10** (281 LOC). `CacheLane` `[SmartEnum]` (ModelResult/Projection/ArtifactBlob) HybridCache read-through with keyed L2 topology, delegate-row object pools, `DrainKind`/`DrainQueue`/`DrainSurface` (Pipe/Network fan-out/join/coalesce). The L2 `IDistributedCache`+serializer-factory "single Persistence contribution" IS the CACHE_PORT #69 seam, declared here.
- DEFECT (anchor, minor): the CACHE_PORT counterpart anchor drifts — ports.md:19 points at `Persistence/indexes#L2_CONTRIBUTION`, Persistence owns it at `cache.md#L2_CONTRIBUTION` (Persistence-E6); ARCH:69 points at `Query/cache`. One anchor per seam needed.
- Note: `DrainKind` has 5 rows, `DrainQueue` union 2 cases (Pipe + Network absorbing FanOut/CorrelatedJoin/DualCoalesce) — intentional topology-vs-materialization split, sound. `DrainSurface` is the owner `Wire/topics` (corpus-b) BUILDS on, and the `ledger#PRESENCE ← DrainSurface` seam (mandate) is declared corpus-b-side.
- Charter AS-SHOULD: unchanged; reconcile the CACHE_PORT anchor to `cache.md#L2_CONTRIBUTION` both sides.

**Runtime/orchestration.md — 9/10** (342 LOC). Crash-durable workflow: five-case `StepKind` `[Union]`, `Orchestrator.Drive` folding each step through the one `CommandDispatch.Run`, saga compensation via `CommandAlgebra.Batch` reverse-fold, `StepStateSeam` fenced-CAS Persistence ripple, `CrashResume` off the collapsed `SupportTrigger.FaultTransition`, persistent-job cadence on `SchedulePort`.
- DEFECT (FIT, top): `StepStateSeam.Persist(WorkflowInstance, WorkflowStep, FencingToken)` (`:268-278`) + `WorkflowInstance.Begin(... FencingToken ...)` (`:90-93`) push THREE AppHost types down into the Persistence store. Charter says "the seam writes only wire-stable keys" (`:259`) but the delegate SIGNATURE passes the whole AppHost `WorkflowInstance`/`WorkflowStep` records — Persistence must reference AppHost interiors. Violates the mandate.
- DEFECT (band): `OrchestrationFault` 4770-4774 (`:67-71`) hardcoded; `SpineLog` "1000-1999 band" (`:20`).
- Charter AS-SHOULD: `StepStateSeam` re-shaped so `Persist` accepts a PROJECTED durable row (primitives: instance/workflow id, status key, cursor, serialized `StepKind` payload, opaque token) never the AppHost record; the token is a Persistence-issued/validated value AppHost carries; Compute-V1 lifecycle outcomes (`Dispatchable`/`Transient`) consumed as settled vocab, no parallel job-state store (mandate — already honored: the executor is `CommandDispatch`, not a re-minted store).

**Runtime/time.md — 9.5/10** (333 LOC). One temporal law: `ClockPolicy` (TimeProvider + IClock), nine-row `DeadlineClass` (every duration literal traces here), one `SchedulePort` (Cronos + period + annual, fleet-spread `XxHash3` seed), `FencingToken`.
- DEFECT (FIT, top — VC2 root): `#FENCING_TOKEN` (`:298-333`) declares AppHost OWNS `FencingToken` `[ValueObject<ulong>]` with `Admits` (Kleppmann reject-lower) + `LeaseElection.Acquire` (local strictly-increasing mint via `Atom<FencingToken>.Swap(Next)`). Persistence-V2 (finalized) RAISES the store to token-VALIDATING and makes fencing tokens Persistence-owned types AppHost decodes — AppHost's local `LeaseElection.Acquire` is the in-process re-mint the token-validating CAS store replaces. Direct ownership conflict.
- Charter AS-SHOULD: `FencingToken` becomes a value AppHost CARRIES (decoded from the Persistence-issued CAS token); `LeaseElection.Acquire`'s local-mint is reconciled with the store-issued token (the store increments the lease generation, AppHost receives it); `Admits` may stay a client-side pre-check but the AUTHORITATIVE fence is the Persistence CAS predicate (per Persistence-V2 "a fence a resource never checks is decorative").

**Runtime/config.md — 9/10** (535 LOC, SIX sections). `ConfigSource` 8-source ranked chain, source-gen `PolicyBinding` on `Validation<ConfigError,T>`, validate-once frozen `OptionsAdmission` with RFC-6902 JSON-patch reload, `KillSwitch` `OperatorOverride`, `SecretLease` acquire/renew/zeroize, `CredentialPem` RFC-7468 bundle wire. Very deep; the KMS-unwrap seam (config KMS_UNWRAP_PORT → Persistence identity#KEY_ENVELOPE, ARCH:72) is well-declared and correctly directioned.
- DEFECT (band): `ConfigError` 4100-4199 (`:123-146`), `SecretFault` 4780-4783 (`:329-332`), `PemFault` 4790-4793 (`:460-463`) hardcoded.
- DEFECT (verification, cluster): `PosixSignalRegistration`/`PosixSignal`, `JsonPatchError.Operation/.ErrorMessage`, `CryptographicOperations.ZeroMemory`, `PemEncoding.WriteString/TryFind/PemFields`, `X509Certificate2.RawData/CreateFromPem` all flagged "not in folder .api/ catalogue, open verification" (`:529-534`). `.api` authoring debt (System.Security.Cryptography, System.Runtime.InteropServices).
- SPLIT PRESSURE (VC7): `SECRET_LEASE` + `CREDENTIAL_PEM` (`:304-535`, ~230 LOC, credential material lifecycle + PEM wire) are a distinct concern from configuration admission (`SOURCE_AXIS`/`TYPED_BINDING`/`POLICY_VALUES`/`KILL_SWITCH`) and are what identity.md's `#SECRET_MATERIAL` anchor reaches for — extract to `Runtime/secrets.md` (2-concern owner).
- Charter AS-SHOULD: config.md = pure config-admission owner; a new `Runtime/secrets.md` owns the credential-lease + PEM wire; fault codes from registry; `.api` stubs authored for the flagged BCL members.

**Runtime/determinism.md — 9.5/10** (407 LOC). Reproducibility kernel: `DeterminismContext` (seed/float-mode/env-fingerprint), hash-chained content-addressed `EventLog`, `ReplayVerify`, `MacroEngine`, `RecomputeGraph` (content-address memoization), `AdversarialProbe` (chaos-replay/log-time-bisect/counterfactual — all composing the kernel's own owners). `ReplayFault` 4760-4763.
- DEFECT (seam-direction, VC5): `EventLog.ToOpLog` (`:118-126`) constructs a Persistence `OpLogEntry` with `SyncOpKind.Upsert`/`SnapshotCodec.JsonStj`/`OriginStoreId` directly — AppHost composing Persistence-interior types (RESEARCH OPLOG_PROJECTION `:406` "composes the finalized Rasm.Persistence/Version/ledger#CHANGEFEED OpLogEntry record constructor directly"). Either a shared changefeed wire-vocabulary (name it) or a reversed reference.
- Charter AS-SHOULD: unchanged interior; the `ToOpLog` projection targets a named shared-wire changefeed vocabulary (or AppHost projects to a neutral row the Persistence port maps to `OpLogEntry`); `HostFingerprint` composition against Compute benchmarks confirmed.

**Runtime/lifecycle.md — 9/10** (394 LOC). Total lifecycle: eight-phase `RuntimePhase` CAS transition law, `FaultSpine` (`FaultSource`→`FaultRecord` flatten → one `SupportTrigger.FaultTransition`), rank-band `DrainConductor`, `CancelScope` spine. `LifecycleFault` 1200-1201, `DrainBand` `[SmartEnum<int>]` 100-400.
- DEFECT (FIT, STALE/DELETED seam — the mandate's :71 obligation): DRAIN_CONDUCTOR's Stores-band 2PC in-doubt row (`:288` + RESEARCH TWO_PHASE_DRAIN `:394`) reads `Rasm.Persistence/Query/transaction TwoPhase.InDoubt(db)` / `#TWO_PHASE_COMMIT` — a Persistence page DELETED by the element rebuild (Persistence-E1/V12; folded into `graph#STORE_RAIL`; ARCH:71 flagged stale). The finalized Persistence single-`IDocumentSession` + same-tx outbox model carries NO distributed 2PC/prepared-transactions anywhere — the 2PC-in-doubt drain concern is likely architecturally OBSOLETE.
- DEFECT (band, overlap): `LifecycleFault` 1200 + `ProfileFault` 1100 live INSIDE the 1000-1999 `SpineLog` event band (no registry enforcing event-vs-fault disjointness).
- Charter AS-SHOULD: the 2PC in-doubt drain row RE-POINTS to the Persistence store-rail owner if in-doubt reconciliation survives, else RETIRES (single-session model eliminates 2PC); ARCH:71→`graph#STORE_RAIL`, ARCH:73→V3 `Version/egress`.

**Runtime/profiles.md — 9/10** (335 LOC). Host-variance axis: eight-row `HostProfile` `[SmartEnum]` (all modality diff as columns + delegates), `ResolvedProfile`, `ProfileBoot` lifetime adapters (systemd sd_notify mirror), `ProfileIdentity` OTel resource identity, `PowerCell`/`FidelityScale` energy-aware fidelity scaling (IOKit/SMC). `RecoveryObjective(Rpo,Rto)` DR column → Persistence recovery (ARCH:70, clean seam). `ProfileFault` 1100-1102. `ShipVehicle.Yak` = the Rhino package manager (legit, not a placeholder).
- DEFECT (seam, minor): `FidelityScale → Compute scheduler` (`:256` "the Compute scheduler reads to bound its CpuBudget and lane parallelism") composed in prose but not obviously in ARCHITECTURE [02]-[SEAMS] (distinct from the laneguard `ShedVerdict` seam). Undeclared.
- DEFECT (verification): `PowerProbe` IOKit `IOPSCopyPowerSourcesInfo` P/Invoke + `NSProcessInfo.thermalState` tier-3 live-host residual (honestly flagged `:333`).
- Charter AS-SHOULD: unchanged; declare the `FidelityScale → Compute` seam row.

**Runtime/laneguard.md — 9.5/10** (153 LOC). In-process resilience governor: one keyed Polly `ResiliencePipeline` per `WorkLane` mirroring `Wire/outbound#KEYED_PIPELINES` verbatim (bulkhead/breaker/hedge/Simmy-chaos-on-test-host), `AdaptiveConcurrency` reading `ResourceMonitoring`, `LoadShed` reading the coherent `DegradationReading`, `ShedVerdict` minted once → `Rasm.Compute/Runtime/admission` (ONE_DEGRADATION_SHED_VERDICT). `LaneFault` 4750-4753. This seam is CORRECTLY directioned (AppHost-owned contract Compute decodes) and Compute-brief-confirmed (settled port peer).
- No FIT defect. Charter AS-SHOULD: keep; fault codes from registry.

**Runtime/modules.md — 9.5/10** (192 LOC). Composition root: frozen `ModuleContribution` table, `ServiceDescriptor.Describe`/`DescribeKeyed` ONLY (no `AddSingleton`/`AddKeyed*` spellings — matches PROHIBITIONS), Scrutor `FromAssemblies` scan + `Decorate`/`TryDecorate`, `RegistrationStrategy.Throw`, `MakeReadOnly` freeze, `BoundaryActivation` (cached `ObjectFactory` plans, `IServiceProviderIsService` probe, `AsyncServiceScope`), FluentValidation discovery. Exemplary DI hygiene.
- No fault band (composition owner). No material defects. Charter AS-SHOULD: keep as the one composition-root owner.

**Runtime/features.md — 9.5/10** (247 LOC). Feature-flag/rollout/experimentation: `FlagDefinition`/`TargetingRule` `[Union]` compiled to one config-backed `OpenFeature` `InMemoryProvider`, deterministic `XxHash3` sticky bucketing (matches SchedulePort.Spread seed), one `FlagVerdict` seam → reasoning `ModelRoute.From` + provisioning `RollStrategy` (both one-direction), kill-switch collapsed to one `TargetingRule.ForcedOff` row. `FeatureFault` 4700-4704. Realizes TARGETED_DELIVERY_EXPERIMENTATION; `FlagVerdict → typescript:host` ONE_FEATURE_FLAG_PROJECTION (ARCH:83).
- No FIT defect. Charter AS-SHOULD: keep; fault codes from registry.

---

## CROSS-CUTTING

**FAULT-BAND CENSUS (corpus-a, complete) — no type-enforced registry, one CONFIRMED collision:**
- 1000-1999: `SpineLog` events (informational) — OVERLAPS `ProfileFault` 1100-1102 (profiles.md:45-47) + `LifecycleFault` 1200-1201 (lifecycle.md:68-69). Event-band and fault-band share the 1xxx space with nothing enforcing disjointness.
- 4100-4199 `ConfigError` · 4600-4605 `CommandFault` · 4620-4625 `GrantFault` · **4640-4648 `IdentityFault` ≡ 4640-4644 `McpFault` [COLLISION]** · 4700-4704 `FeatureFault` · 4750-4753 `LaneFault` · 4760-4763 `ReplayFault` · 4770-4774 `OrchestrationFault` · 4780-4783 `SecretFault` · 4790-4793 `PemFault` · 4800-4805 `FederationFault`.
- 9300: `Error.New(9300)` bare/untyped orphan (bundles.md:90).
- corpus-b referenced: `LiveWire` `WireFault` 4720-4725 (Wire/livewire), Compute `WireFault` 4520-4532 (Compute type, namespace-qualified).
- Every fault union hardcodes `base(detail, NNNN)` integers per-page; federation.md:22's own hand-kept band map is already WRONG (omits IdentityFault). This is Persistence-E4/V4 and the paradigm `FaultBand` registry (`RASM-COMPONENT-PARADIGM-DECISION.md:141-149`) verbatim — AppHost is APP-PLATFORM (its own band neighborhood, NOT the AEC 23xx-27xx registry) and needs its OWN `[SmartEnum<int>]` registry.

**CONCERN MIXING / SEAM-DIRECTION (the central architectural tension):** ARCHITECTURE.md:3,18 + ports.md:3 claim the seven inward ports are "the only cross-package seam" and "the dependency never reverses," yet pages compose sibling-INTERIOR types directly AND push AppHost types downward: (a) Compute `ComputeIntent`/`IntentAdmission`/`SelectionReceipt`/`AdmittedIntent`/`SubstrateSelection` (capability, runtime, reasoning, federation, mcp); (b) Persistence `OpLogEntry`/`SyncOpKind`/`SnapshotCodec` (determinism.md:118-126) + `TwoPhase.InDoubt` (lifecycle.md:288); (c) AppHost `FencingToken`/`CostVector`/`WorkflowInstance`/`WorkflowStep` PUSHED into the Persistence coordination store (capability.md:422, orchestration.md:269). The [02]-[SEAMS] table DOES list these edges, so the "only seam" invariant refers to the seven inward DELEGATE contracts while outward composition is un-named — the prose over-claims and must be reconciled.

**HARDCODING-vs-GENERATOR:** clean on the primary axis (rosters are seed DATA — `HostProfile`/`CacheLane`/`DeadlineClass`/`DriverProbe`/`ModelRoute`/`TargetingRule`/`DrainKind` all `[SmartEnum]`/`[Union]` + FrozenDictionary dispatch). The ONE hardcoding defect is the fault-code integer space (no registry). No bare duration literals (all trace to `DeadlineClass`). No `DateTime.UtcNow`/`Stopwatch` call sites (all `ClockPolicy`).

**DEAD/UNTYPED CARRIERS:** minimal — bundles.md:90 bare `Error.New(9300)` (should be typed `SupportFault`); otherwise every carrier is wired. `RosterReceipt`/`FleetRollReceipt` (ports.md wire context) are corpus-b (Sandbox/provisioning, Wire/coordination) receipts, out of scope.

**UNWIRED / STALE / UNDECLARED SEAMS:** (1) STALE-DELETED: lifecycle.md 2PC in-doubt → `Persistence/Query/transaction` (deleted page). (2) STALE-ANCHOR: ARCH:71 (`Query/transaction`), ARCH:73 (`Sync/egress`), ARCH:86 (phantom `Model/agent` folder → `Agent/reasoning`). (3) DRIFTED-ANCHOR: CACHE_PORT (ports.md:19 `indexes#L2_CONTRIBUTION` vs Persistence `cache.md#L2_CONTRIBUTION` vs ARCH:69 `Query/cache`). (4) MANDATE-REWRITE: Health :85 driver roster vs Persistence V7/V3 prune. (5) UNDECLARED: `DataClassification → Persistence ClassificationGuard` (telemetry.md:355), `FidelityScale → Compute scheduler` (profiles.md:256). (6) intra-package: identity.md `#SECRET_MATERIAL` → nonexistent config section.

**UNMINED CAPABILITY / CATALOG DEBT (`.api` obligation):** honestly phantom-flagged BCL/SDK members needing catalog authoring: System.Security.Cryptography (`PemEncoding`, `X509Certificate2.RawData/CreateFromPem`, `CryptographicOperations.ZeroMemory`), System.Runtime.InteropServices (`PosixSignalRegistration`/`PosixSignal`), `JsonSchemaExporter`, `JsonPatchError` shape. All under `libs/csharp/Rasm.AppHost/.api/`. Packages already deeply mined (M.E.AI middleware, ModelContextProtocol SDK, OpenFeature, Polly.Simmy, ResourceMonitoring, IdentityModel, Marten-adjacent) — verification depth is genuinely high.

**FOLDER-ARCHITECTURE VERDICT (positive, with one split-pressure):** the 5-folder structure (Agent 6 / Observability 3 / Runtime 11 / Sandbox 3 / Wire 6) is a SOUND growth structure — every folder ≥2 pages, real growth axes, zero one-file folders, zero flat sprawl. NO new top-level folder warranted. Two graded observations: (a) `Runtime` at 11 pages is the largest folder — coherent today but the sprawl ceiling; (b) `config.md` (535 LOC, 6 sections) carries a distinct credential-material concern (`SECRET_LEASE`+`CREDENTIAL_PEM`, ~230 LOC) worth extracting to `Runtime/secrets.md` (also fixes identity's `#SECRET_MATERIAL` anchor). This is the ONE structural split-pressure in corpus-a.

---

## VERDICT CANDIDATES (campaign-defining, evidence-anchored)

**VC1 — MINT AN APPHOST FAULT-BAND REGISTRY (highest-value perimeter ruling).** ~14 fault unions hardcode per-page 4xxx/1xxx base integers with no type-enforced disjointness; CONFIRMED collision `IdentityFault` 4640-4648 (identity.md:119) ≡ `McpFault` 4640-4644 (mcp.md:125); 1xxx event-band overlaps `ProfileFault` 1100 + `LifecycleFault` 1200; bundles.md:90 bare `Error.New(9300)` orphan; federation.md:22's own band census omits IdentityFault (the scheme is already self-inconsistent). RULING: one AppHost `[SmartEnum<int>]` fault-band registry (mirror `RASM-COMPONENT-PARADIGM-DECISION.md:141-149` + Persistence-V4) where a duplicate integer fails at type-init; every `Expected` union's `Code => Band + n`; per-page band prose and federation's hand-census die for one registry pointer; `SupportFault` types the dump gate. AppHost occupies its own APP-PLATFORM neighborhood (1xxx/4xxx), disjoint from the AEC 23xx-27xx registry.

**VC2 — RE-SHAPE THE FOUR PERSISTENCE PORT SEAMS TO DECODE, NOT PUSH DOWN (the accumulated-census core).** capability.md:420-423 (`DistributedBudget.Debit`/`Token` pass `FencingToken`+`CostVector`), orchestration.md:268-278 (`StepStateSeam.Persist` passes `WorkflowInstance`/`WorkflowStep`/`FencingToken`), time.md:298-333 (AppHost OWNS `FencingToken` `[ValueObject<ulong>]` + `LeaseElection.Acquire` local mint) all INVERT the finalized Persistence-V2 law ("fencing tokens/op-union/receipts are Persistence-owned types AppHost's PORT adapters decode — no AppHost interface or type crosses down"; the store is token-VALIDATING). RULING: the four PORT adapters (fenced Budget debit, workflow step-state CAS, transactional outbox consumer, CAS+lease+membership) project the durable row to PRIMITIVES (opaque Persistence-issued token + wire-stable keys + serialized payload) and DECODE Persistence-owned results; `time#FENCING_TOKEN`'s local `Acquire`/`Admits` is reconciled to CARRY the store-issued/validated token (client pre-check at most, authoritative fence Persistence-side).

**VC3 — WATERFALL: PERSISTENCE-V2 BUDGET DEBIT MUST BE MULTI-UNIT (surgical upstream owner-extension).** AppHost `GrantBroker` meters a `CostVector` across five independent `CostUnit` ceilings (capability.md:62-67,468-472); Persistence-V2 specs a scalar "balance >= debit" CAS. RULING (record in return; AppHost = demanding consumer): edit `RASM-CS-PERSISTENCE-BRIEF.md` V2 coordination owner — the per-tenant Budget debit gains a per-`CostUnit` VECTOR fenced compare-and-decrement (N independent balances, one atomic CAS predicate, `WHERE token >= held AND balance_i >= debit_i ∀i`). Framed as a capability delta, not a re-plan.

**VC4 — RE-WRITE THE HEALTH DRIVER ROSTER INTEGRATION-FIRST (the mandate's :85 obligation, confirmed).** health.md:100-110 `DriverProbe.Cache=redis` (over `IConnectionMultiplexer` :76-77) + `Broker=kafka` (over `ProducerConfig`) assume raw drivers Persistence prunes/re-homes: V7 prunes `StackExchange.Redis` direct ref (L2 rides `Caching.StackExchangeRedis` transit); V3 makes Kafka an egress sink row with NATS the spine anchor. RULING: ADD `DriverProbe.Nats` (admit `AspNetCore.HealthChecks.Nats`), re-point `Cache` to the caching-transit L2 `IDistributedCache`, keep Kafka a conditional broker sink row; README `[OBSERVABILITY]` health-check package group re-aligns to the finalized Persistence backing-service roster (integration-first: brokers land as sink rows before any removal). ARCH:85 re-writes both sides.

**VC5 — RECONCILE THE "SEVEN PORTS ONLY" INVARIANT WITH OUTWARD SIBLING-TYPE COMPOSITION.** ports.md:3 / ARCHITECTURE.md:18 claim the seven inward ports are the ONLY cross-package seam and "the dependency never reverses," but AppHost composes Compute `ComputeIntent`/`IntentAdmission`/`SelectionReceipt` (5 pages), Persistence `OpLogEntry`/`SyncOpKind`/`SnapshotCodec` (determinism.md:118), driver instances (health), and pushes AppHost types down (VC2). RULING: the campaign formally NAMES a shared APP-PLATFORM wire-vocabulary tier (`ComputeIntent` family, the changefeed `OpLogEntry`, the coordination request/result shapes, `ShedVerdict`, `DataClassification`) co-composed at the boundary — NOT a port, NOT a reversed interior dependency — and states it in ARCHITECTURE/BOUNDARIES; the "seven inward DELEGATE contracts are the only INWARD seam" prose replaces the over-broad "only cross-package seam."

**VC6 — RETIRE-OR-REPOINT THE 2PC IN-DOUBT DRAIN (stale/deleted-owner seam).** lifecycle.md:288 + RESEARCH:394 read `Rasm.Persistence/Query/transaction TwoPhase.InDoubt(db)` — a Persistence page DELETED by the element rebuild (Persistence-E1/V12, folded into `graph#STORE_RAIL`; ARCH:71 flagged stale). The finalized Persistence single-`IDocumentSession` + same-tx outbox model carries NO distributed 2PC/prepared-transactions — the drain concern is likely architecturally obsolete. RULING: the Stores-band 2PC in-doubt drain row re-points to the Persistence store-rail owner IF in-doubt reconciliation survives the finalized design, else RETIRES entirely; ARCH:71/:73 re-point per the Persistence counterpart table.

**VC7 — EXTRACT `Runtime/secrets.md` FROM config.md (the one folder-architecture split-pressure).** config.md (535 LOC, 6 sections) fuses configuration admission (`SOURCE_AXIS`/`TYPED_BINDING`/`POLICY_VALUES`/`KILL_SWITCH`) with a distinct credential-material concern (`SECRET_LEASE`+`CREDENTIAL_PEM`, :304-535, ~230 LOC — acquire/renew/zeroize lifecycle + RFC-7468 PEM wire + KMS-unwrap custody), which is exactly what identity.md's stale `#SECRET_MATERIAL` anchor reaches for. RULING: extract `Runtime/secrets.md` (2-concern credential owner: `SecretLease` + `CredentialPem`), leaving config.md the pure config-admission owner; re-point identity.md `#SECRET_MATERIAL` → the new page; Runtime holds at 12 pages, all coherent. (Folder structure otherwise ratified: zero one-file folders, real growth axes.)

**VC8 — CORRECT THE SEAM-LEDGER + COUNT DRIFTS (governance truth pass).** ARCH:86 phantom `Model/agent` folder → `Agent/reasoning#MODEL_GOVERNANCE`; ARCH:69/ports.md:19 CACHE_PORT anchor drift → `Persistence/cache.md#L2_CONTRIBUTION` (one anchor per seam); telemetry.md:2 "seven-row DataClassification" → nine rows; identity.md `#SECRET_MATERIAL` → real config/secrets section; author the flagged `.api` catalogs (System.Security.Cryptography, PosixSignal, JsonSchemaExporter); declare the `DataClassification → Persistence ClassificationGuard` and `FidelityScale → Compute scheduler` seam rows. RULING: one governance-reconcile motion makes ARCHITECTURE.md [02]-[SEAMS], the page headers, and the `.api` tier machine-checkably true (mirror Persistence-V12).
