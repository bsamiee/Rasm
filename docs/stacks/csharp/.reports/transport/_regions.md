# [TRANSPORT_REGIONS] — final ledger fragment

Final region rows for `domain/transport.md`; the orchestrator consolidates these into `_region-map.md`, replacing the provisional transport rows.

## [1]-[PAGE_REGION]

- domain/transport.md: wire transport law — the typed port record over a closed transport-row vocabulary (in-process, UDS companion, remote TLS, browser-translated) with channel lifecycle, resolution, keep-alive, and trust as row law; the per-call seam (deadline minting, status-taxonomy fold, one stamping interceptor, binary metadata, retry-as-data with the per-row owner column, call credentials, per-call compression); protobuf message law and contract evolution by descriptor diff with one verdict gating peer attach, store open, and replay decode; the dual-codec temporal bridge; one server exposure record fold with structured `-bin` faults, health rows, and grpc-web consent; UDS/IPC topology — manifest publish-last lifecycle, peer kernel credentials, epoch redial, and the framed corridor under the four-gate invariant with frame-kind cap rows; suite JSON resolver merge with frozen options and schema-hash export under the same evolution gate; JsonPatch as recorded intent applied once at the boundary.

## [2]-[SNIPPET_REGIONS]

[TRANSPORT — final]:
- TP-01 TRANSPORT_ROW_AXIS: closed transport-row vocabulary — in-process, UDS companion, remote TLS, browser-translated — as smart-enum rows whose channel column carries the `ConnectCallback` dial, keep-alive/multiplex knobs, the `ServiceConfig` retry row, and `GrpcWebHandler`, plus a `ChannelRetry` owner column stamped into the port; one `Materialize` projecting two call arrows (unary `AsyncUnaryCall`, server-stream `AsyncServerStreamingCall` behind a disposal-held `ReadAllAsync` drain iterator) into one provider-free `Port` record with an in-process local-port fallback; distinct from RT-01 (host boot fold), CC-01 (channel-lane rows), and S-04 (delegate-column mechanics are supporting material).
- TP-02 CALL_SEAM_FOLD: per-call seam — built-in metadata stamping via `CallInvoker.Intercept(Func<Metadata,Metadata>)`, deadline minted from the hop budget into the `CallOptions` triple under an injected `TimeProvider`, and the `RpcException.StatusCode` taxonomy folded once to `Fin` with `-bin` trailer fault decode through `GetValueBytes`; distinct from RS-02 (`Outcome<T>` pipeline fold, token discrimination) and DG-05 (envelope propagator mechanics).
- TP-03 DESCRIPTOR_DIFF_GATE: contract-evolution classifier — canonical checksum over `Fields.InDeclarationOrder()` rows carrying number, type, label, packed-ness, oneof membership, and `JsonName`, plus reserved ranges and service methods, via `XxHash3`; additive-vs-breaking verdict union with reserved-range ceremony deciding removal class and per-field retype detection spanning the full diff-relevant column set; never hashes raw `SerializedData`.
- TP-04 EXPOSURE_RECORD_FOLD: one server exposure record folded into `AddGrpc`/`AddServiceOptions`/`MapGrpcService`/`MapGrpcHealthChecksService`/`UseGrpcWeb`+`EnableGrpcWeb`, the null-assignment cap LIFT on a per-service row, tag-predicate health mapping rows, and structured fault projection `RpcException(Status, trailers)` under a `-bin` detail key in one expression; distinct from RT-02 (descriptor fold) and VL-03 (seam matrix).
- TP-05 MANIFEST_ATTACH: discovery-manifest lifecycle — owner-only directory mode at creation, same-directory staged write plus atomic `File.Move` publish, two-probe staleness ladder (pid/start-stamp advisory, socket-connect authoritative) folded to stale-listener rejection on the `Fin` rail; distinct from B-06 (no token-gated cell) and RT-04 (no phase machine).
- TP-06 FRAMED_CORRIDOR: raw-lane framing invariant — `CalculateSize` producer pre-check against a `FrameKind` smart-enum cap column consuming the manifest cap pair, fixed little-endian header with version and kind bytes, version/kind drift gate then length gate before any allocation, length-seeded `XxHash3` checksum-then-parse over a rented buffer, disjoint oversize/truncated/corrupt/drifted fault taxonomy; distinct from B-05 (byte identity, not framing) and SA-02 (UTF-8 format-parse).
- TP-07 SUITE_RESOLVER_MERGE: federated JSON contract merge — per-package `IJsonTypeInfoResolver` disjointness probe accumulating contract-fork conflicts on the validation carrier, `JsonTypeInfoResolver.Combine` plus `WithAddedModifier` suite posture, `MakeReadOnly` freeze, schema-node export hash via `GetJsonSchemaAsNode`; distinct from B-08 (converter mechanics) and the system-apis serializer-surface chooser.
