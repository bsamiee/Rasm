# [REGIONS] — persistence

Final ledger material for `domain/persistence.md`; replaces the provisional persistence rows when the orchestrator consolidates `_region-map.md`.

## [1]-[PAGE_REGION]

- domain/persistence.md: data persistence law — the store-profile axis (engine x placement x codec rows with option-typed capability slots, the relational-base knob set, the model-cache trilemma, naming policy, generated-converter admission) over a pooled stamped unit-of-work lifecycle with the three-route model-acquisition fold; document-shape law (complex-type table-split versus `ToJson`, JSON-only complex collections, converter-then-document composition, the `ConfigureConventions` admission seam, parameterized-collection translation modes); the three-altitude interceptor spine with typed suppression gates, tracker-disposition policy, the identity-resolution row, and `ConfigureWarnings` escalation; migration algebra (migration-as-data, rollback as a generation parameter, lock reclamation and seeding, vehicle-by-placement, the boot verdict fold over identifier sets, the generation-time wave gate with physical classification per `ActiveProvider`); the closed identity-policy axis (admission mint, order-preserving transcription, ordering and collision columns, one key selector deriving every secondary surface, the dual-key identity migration); one store-op rail (closed op family, strategy-composed pooled bracket, declared transaction posture, value projection, `TagWith` provenance, compiled-delegate graduation, the raw seam, cache-port tag law, keyset paging on `Option<Cursor>`); the bulk write lane (set-based, copy, merge intensities) self-emitting facts and derived tag-cuts inside the ambient transaction, with render-without-execute as the dry-run law.

## [2]-[SNIPPET_REGIONS]

- PS-01 STORE_PROFILE_AXIS: engine x placement x codec as one store-profile row — provider-admission delegate columns with option-typed capability slots, the placement authority vocabulary, one codec composition fold (`UseSnakeCaseNamingConvention` + `UseThinktectureValueConverters(Configuration.Default)` + tracking row + interceptor intake), and stamped pooled acquisition — a wrapping `IDbContextFactory<TContext>` over `PooledDbContextFactory<TContext>` stamping the placement discriminant per acquisition; distinct from RT-01/RT-02 (options-builder fold over engine rows, not host boot or descriptor fold).
- PS-02 DOCUMENT_SHAPE: one aggregate declaration choosing table-split versus `ToJson` document mapping — aliasing-legal complex value in two slots, the `ToJson`-chained `HasJsonPropertyName` one-site composition, JSON-only `ComplexCollection`, generated-type document interior under the converter seam, primitive collection, generated key via `HasKey`.
- PS-03 SUPPRESSION_GATE: unit-of-work interceptor as a typed gate — `InterceptionResult<int>.SuppressWithResult` with tracker disposition declared as policy rows consumed by one shared verdict kernel, mandatory sync/async modality twins; the smart-enum delegate column is S-04 supporting material, never the spotlight.
- PS-04 MIGRATION_VERDICT_FOLD: boot schema verdict over assembly/applied identifier sets — schema-newer typed rejection carrying unknown identifiers, placement-routed pending/fresh/apply arms, `HasPendingModelChanges` drift gate; distinct from TP-03 (EF migration set algebra, not protobuf descriptor diff) and VL-04 (set difference over store state, not validator metadata bijection).
- PS-05 IDENTITY_ROW_VOCABULARY: identity policy rows carrying the axis columns — `Guid.CreateVersion7` admission mint beside the deterministic-backfill overload, the ordering-semantics column, `ToByteArray(bigEndian: true)` order-preserving transcription, collision-posture column, composed-digest factory row.
- PS-06 RAIL_BRACKET_ARITY: one closed store-op family under one strategy-composed pooled bracket — state-threaded `IExecutionStrategy.ExecuteAsync` with static lambdas, input-shape arity (point/batch/keyset page as `Option<Cursor>` lexicographic predicate), value projection plus `TagWith` provenance, the `EF.CompileAsyncQuery` compiled-delegate row, boundary fault conversion sparing cancellation; distinct from R-01 (bracket conversion, not `Try.lift` capture grammar).
- PS-07 WRITE_MASS_OUTPUT: bulk-lane self-emission across all three intensities — statement-bodied `ExecuteUpdateAsync` setters with the zero-affected concurrency fold, `BulkCopyAsync` under `KeepIdentity` with the rows-versus-source reconciliation receipt, bridge merge enlisted in the ambient transaction with declaration-ordered when-clauses and `MergeWithOutputAsync` changefeed projection, and one fact record deriving its tag-cut mechanically from lane plus owner keys, cut before commit; distinct from R-08 (self-emitted write facts with derived invalidation, not a receipt monoid fold).
