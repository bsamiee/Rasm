# [RASM_IDEAS]

The forward pool of higher-order kernel concepts, grounded in the robust-geometry domain and the monorepo geometry-flow. Each is a card: a bracketed slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on. `[1]-[OPEN]` carries live ideas; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

## [2]-[CLOSED]

[EXACT_ARRANGEMENT_BOOLEAN]: realized — `arrangement/arrangement#MESH_BOOLEAN`/`#PLANAR_OVERLAY`/`#CELL_COMPLEX` owns the fully-managed exact-arithmetic `Arrangement` `[Union]` over `ArrangementStore`, the Cherchi-Attene indirect-predicate cell complex classified by the `spatial/index#GENERALIZED_WINDING` GWN scalar with `BooleanOp`-selected cells welded through `DuplicateWeld` and the `BooleanReceipt` typed evidence; retires the tier-3 native CSG gate for the common cases, the `healing/repair#BOOLEAN_NATIVE_ASSET` `Apply` re-routed to `Arrangement.Apply`.
[PRIMITIVE_FIT_FRONTIER]: realized — `fitting/fit#PRIMITIVE_FIT` owns the `FitOp` `[Union]` (plane/sphere/cylinder/cone/torus/line) over the MLESAC robust-consensus sampler plus the geometric-orthogonal-distance LM refine reusing the `constraints/solver` iterate, returning the typed `FitReceipt`; the scan-to-BIM segmentation primitive the `REALITY_CAPTURE_TO_BIM` consumer reads.
[SURFACE_PARAMETERIZATION_OWNER]: realized — `parameterization/flatten#PARAMETERIZATION` owns the `ParamOp` `[Union]` (harmonic/LSCM/ARAP/BFF) over the `Vectors` DEC operator substrate reached at the public `MeshAdjointSnapshot.Of` boundary handle (never the internal `LaplacianCache`), reusing one cached `CholeskySparse` factor across every modality and ARAP global step plus `Matrix.DecomposeSvd` for the LSCM conformal minimizer, returning the typed `ChartAtlas`; the kernel-quality flattening the fabrication unroll/nesting and texture pipelines compose, never a thinning of the host unwrap.
[OFFSETTING_FRONTIER]: realized — `offsetting/offset#STRAIGHT_SKELETON`/`#MEDIAL_AXIS`/`#MINKOWSKI_CONVOLUTION` owns the bodied `[WAVEFRONT]`/`[PROJECTIONS]` clusters (`Seed`/`EnqueueAll`/`Collapse`/`Divide`/`MedialFrom`/`OffsetSnapshot`/`Assemble`), the `Assemble` loop assembly re-routed through `arrangement/arrangement#PLANAR_OVERLAY`, and the `weighted` `OffsetKind`/`OffsetOp` growth row (Aichholzer-Aurenhammer weighted skeleton over the per-edge `OffsetPolicy.EdgeSpeed` column).
[HIDDEN_LINE_PROJECTION]: realized — `projection/view#HIDDEN_LINE_PROJECTION` owns the `ViewOp` `[Union]` (silhouette/hidden-line/section/outline) over the Newell-Newell-Sancha BSP visibility kernel, the silhouette locus decided by the exact `Orient3D` view-dot sign, the section cut one `IntersectOp.Apply`, returning the typed `DrawingProjection`; the predicate-exact 2D-drawing producer the `DRAFTING_DOCUMENT_PRODUCTION` rail composes.
[ROBUST_MESH_DECIMATION]: realized — `simplification/decimate#ROBUST_MESH_DECIMATION` owns the `SimplifyOp` `[Union]` (quadric-collapse/progressive-mesh/voxel-remesh/feature-preserve) over the Garland-Heckbert QEM edge-collapse queue gated by the exact `Orient3D` collapse-validity sign, returning the typed `DecimationReceipt` (per-LOD budget, one-sided Hausdorff bound, reversible vsplit stream); the LOD producer the tile-pyramid/coarse-solver-seed/meshlet-residency consumers compose.
[GEOMETRY_ENCODING_PACK]: realized — `encoding/pack#GEOMETRY_ENCODING_PACK` owns the `PackOp` `[Union]` (point-cloud/mesh-patch/voxel-grid/brep-patch) over the eight-row `EncodingChannel` feature lattice composing the live `Vectors` mesh/cloud/SDF/curvature/geodesic readers, returning the typed `EncodedGeometry` with the lossless-round-trip witness keyed by the `topology/reconciliation#CANONICAL_BYTE_IDENTITY` content hash; the kernel-side producer the plugin `GeometryPacking` capsule and the Compute `EncodedTensor` lane read.

[CONSTRAINED_DELAUNAY]: REALIZED — `tessellation/delaunay#BOWYER_WATSON_DELAUNAY`/`#CONSTRAINT_RECOVERY` the author-kernel constrained Delaunay triangulation/tetrahedralization `[Union]` over a flat `SimplexStore`, the exact `InCircle`/`InSphere`-driven Bowyer-Watson cavity insertion, and the predicate-guarded segment/facet recovery now composing the landed `OrientLPI`/`OrientTPI` Steiner-point family. The `[STORE_OPS]` cluster bodies and the `DelaunayLaws` law-matrix are the remaining transcription/test obligations, not open design fences; the constrained-Delaunay arrangement substrate the offsetting/arrangement consumers compose is authored.
[INDIRECT_PREDICATES]: REALIZED — `numerics/predicates#INDIRECT_PREDICATES` `OrientLPI`/`OrientTPI` over `Lpi`/`Tpi` exact homogeneous constructions; the constructed line-line / three-plane intersection point's orientation sign is exact without rounding. Unblocks the healing self-intersect split, the Delaunay Steiner recovery, the managed boolean companion path, and the new exact-intersection lattice.
[GENERALIZED_WINDING]: REALIZED — `spatial/index#GENERALIZED_WINDING` `SpatialQuery.Winding` + `QueryResult.Scalar` Barill/Jacobson tree-based GWN best-first descent over the existing BVH (per-node dipole/centroid moments, far-field β approximation, exact per-triangle solid angle at leaves). A query case, not a new structure; the inside/outside classifier the exact-arrangement boolean cell-keep predicate composes.
[WITNESS_DOF]: REALIZED — `constraints/solver#WITNESS_DOF` `DofAnalysis.RedundantConsistent` + `ConstraintSolver.WitnessAnalyze` (numeric Jacobian rank + `Svd`-tail residual-consistency at the seed witness) wired into `Solve` so a redundant-but-consistent system solves rather than faulting.
[DEGRADATION_REFIT]: REALIZED — `spatial/index#DEGRADATION_REFIT`/`#AGGLOMERATIVE_BUILD` `BuildPolicy.RefitDegradationLimit`, the `SpatialKind.Agglomerative` `Builders` row over the shared `NodeStore`, `Bvh.BuildCost`/`Builder` columns, and the `SahCost`-keyed `Refit` rebuild trigger — the Compute clash seam unchanged.
[ROBUST_INTERSECTION_LATTICE]: realized — `intersection/intersect#INTERSECTION` owns the predicate-exact `IntersectOp` `[Union]` (`SegmentSegment`/`SegmentTriangle`/`TriangleTriangle`/`RayMesh`/`MeshMesh`/`PlaneMesh`) over one flat `IntersectStore`, the exact `Orient3D` straddle narrow-phase, the Guigue-Devillers exact triangle-triangle interval, the `spatial/index` BVH-broad-phased mesh-mesh/plane-mesh ordered crossing chain, and the `Crossing` carrier (materialized `Point3d` `Site` decided by the exact `Orient3D`/`Orient2D` straddle signs + exact `Expansion` `OrderKey`, the exactness living in the sign decisions and the order key, the `Site` re-emitted at the `Vectors` `Polyline`/point boundary) — the single owner collapsing the three inline crossing tests in `healing/repair`/`offsetting/offset`/`arrangement/arrangement`, the `PlaneMesh` section arm the `projection/view#HIDDEN_LINE_PROJECTION` `ViewOp.Section` reads. The `IntersectionLaws` law-matrix is the remaining test obligation, not an open design fence.
