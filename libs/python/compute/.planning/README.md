# [PY_COMPUTE_PLANNING]

`compute` owns offline scientific computation planning, study evidence, model asset preparation, and C# graduation receipts. It is not the production compute runtime.

## [1]-[PAGE_INDEX]

| [INDEX] | [PAGE] | [OWNS] |
| :-----: | :----- | :----- |
|   [1]   | [array solver](array-solver.md) | array admission, solver routes, symbolic derivation |
|   [2]   | [units study models](units-study-models.md) | units, uncertainty, studies, model asset evidence |
|   [3]   | [graduation](graduation.md) | C# handoff records and boundary law |

## [2]-[OWNER_CLUSTERS]

[ARRAY]:
- Owner symbols: `ArrayPayload`, `ArrayAdmission`, `NamedAxis`.
- API routes: `.api/api-numpy.md`, `.api/api-xarray.md`, `.api/api-dask.md`.
- Boundary: no production tensor runtime.

[SOLVER]:
- Owner symbols: `NumericIntent`, `SolverPlan`, `SymbolicDerivation`.
- API routes: `.api/api-scipy.md`, `.api/api-sympy.md`.
- Boundary: no production substrate selection or benchmark authority.

[STUDY]:
- Owner symbols: `StudyPlan`, `ComputeStudyReceipt`.
- API routes: `.api/api-dask.md`, `.api/api-xarray.md`, `.api/api-numpy.md`.
- Boundary: no job framework, farm scheduler, or C# `ComputeReceipt` minting.

[MODEL]:
- Owner symbols: `ModelAsset`, `ModelAssetManifest`.
- API routes: pending `.api/api-onnx.md`, `.api/api-onnxruntime.md`, `.api/api-scikit-learn.md`.
- Boundary: no production inference session runtime.

## [3]-[TRANSCRIPTION_LAW]

- Future source lands directly under `libs/python/compute`.
- Compute consumes runtime rails and receipts only after runtime source exists.
- Compute consumes data and artifact bundle shapes only as study inputs or graduation evidence.
- Successful Python experiments graduate as C# owner rows, not permanent product execution surfaces.
