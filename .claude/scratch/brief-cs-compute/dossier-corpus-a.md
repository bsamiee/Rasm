# [DOSSIER_CS_COMPUTE_CORPUS_A]

Read-only survey, lane = corpus-a. Partition: 7 subfolders alphabetical [Analysis, Model, Runtime, Solver, Stats, Symbolic, Tensor]; corpus-a = FIRST HALF (ceil for odd count) = **Analysis, Model, Runtime, Solver** (26 pages) + the four package-root docs. Gap-free with corpus-b = {Stats, Symbolic, Tensor}. Every page deep-read; optimizer.md/uncertainty.md read at charter+full-boundary depth (their single-owner [_LANE] sections; the boundary prose is the densest defect surface). ARCHITECTURE.md, README.md, TASKLOG.md, IDEAS.md, Rasm.Compute.csproj, packages.lock listing, libs/.planning/architecture.md, RASM-COMPONENT-PARADIGM-DECISION.md (targeted), RASM-CS-PERSISTENCE-BRIEF.md (targeted) all consulted for FIT.

## [00]-[HEADLINE]

Compute is a **mature, cold-pass corpus**: every IDEAS card and every TASKLOG card is CLOSED except one BLOCKED (`NATIVE-BLAS`, correctly host-RID-gated). The interior is genuinely **world-class** — the naivety hunt on APPROACH and COVERAGE comes up near-empty inside the fences: real EN 1993-1-2 steel-fire marching, ISO 13788 Glaser lower-hull condensation, EN ISO 10077-1 whole-window Uw, EnergyPlus subprocess with full end-use fold, CMA-ES/PSO/Bayesian-GP as five distinct kernels (not one GA masquerade), HLRF FORM/SORM + Au-Beck subset simulation, Bowyer-Watson Delaunay + Vandermonde isoparametric elements, two-direction Möller-Trumbore + Kalman/CUSUM digital twin, meshopt residency codecs, FastCDC geometry delta. Pages carry explicit "deleted form" / "named defect" self-audit prose at a density I have not seen exceeded.

The real findings are therefore NOT interior naivety. They are: (1) **the mandate's forward capabilities genuinely absent** — the assessment-failure lifecycle (item 1) is the single largest gap and it is architecturally load-bearing, not a card; (2) **seam-ledger staleness** — Compute's `ARCHITECTURE.md` [02]-[SEAMS] rows point at Persistence sub-folders the Persistence campaign DELETED (`Sync/`, `Query/pipeline`) and likely-renamed (`Query/lane`→`Query/rail`, `Query/cache`); (3) **a genuine cross-strata tension** in mandate item 2 (cold-formed capacity dispatch cannot live in Compute as stated); (4) **page-granularity asymmetry** — Runtime `channels.md` (779) / `codecs.md` (833) and Solver `contract.md` (1093) / `optimizer.md` (1128) / `discretization.md` (896) are multi-owner mega-pages against a corpus norm of one-owner-per-page.

LOC: Analysis 2532, Model 1333, Runtime 2797, Solver 4470 (my half = 11132 of 17101). Root: ARCHITECTURE ~199 lines, README ~137, TASKLOG ~57, IDEAS ~33.

## [01]-[ROOT_DOCS]

- **ARCHITECTURE.md** — verdict **8/10**. Exceptional domain map + the two prohibition blocks ([04]-[SEAM_PROHIBITIONS] 9 rows, [05]-[PROHIBITIONS] 7 rows) are load-bearing spine law with real code-band custody (ComputeFault 2200 band, next-free 2221). The `[03]-[SPINE]` mermaid + prose is faithful. DEFECTS: (a) **[02]-[SEAMS] ledger rows are STALE against the Persistence campaign** — :111 `Runtime ← Rasm.Persistence/Sync [PROJECTION] FastCDC` and :119 `Runtime/channels → Rasm.Persistence/Sync [WIRE]` both target `Sync/` which the Persistence brief VERDICT (RASM-CS-PERSISTENCE-BRIEF.md:14) states the element-rebuild collapse DELETED; :115 `Runtime/codecs ⇄ Rasm.Persistence/Query/pipeline [PORT]` targets `Query/pipeline`, also deleted; the heavy `Query/cache` refs (:101,:102,:103,:108,:109) and `Query/lane` (:99) predate the Persistence roster reconcile (E3 line renames `api-arrow`→`Query/rail`+`Sync/egress`) — the whole Persistence-side seam column needs re-pointing (mandate item 5, CONFIRMED with disk evidence). (b) :116 `Compute → Rasm.Persistence/Store/quality [SHAPE]: geometry-derived anomaly rule source` is a Compute→Persistence WRITE edge that no read-half page in my scope realizes — a dangling declared seam (unwired). (c) The lane VECTOR_CODEBOOK↔embedding seam (:99) is honest on Compute's side (embedding.md never re-mints — verified) but the Persistence owner token `Query/lane#VECTOR_CODEBOOK` may have drifted to `Query/rail`. Owner charter as it SHOULD be: the seam ledger is the ONE place cross-package drift is caught; a Persistence-campaign-landing MUST trigger a mechanical re-point pass here, and the four dead rows are the proof it hasn't happened.
- **README.md** — verdict **8/10**. `[01]-[ROUTER]` 41 entries match the on-disk page set exactly. `[02]-[DOMAIN_PACKAGES]` + `[03]-[SUBSTRATE_PACKAGES]` group law is clean and the GeneticSharp/TorchSharp/libtorch-cpu dual-leg charter is precise. Minor: the prose is long but every clause is load-bearing (package charter). No stale-paradigm terms.
- **TASKLOG.md** — verdict **7/10**. Honest closed-card ledger; `NATIVE-BLAS` BLOCKED is correctly reasoned (managed fallback is the cold-start until a host RID resolves the native asset). GAP vs mandate: the mandate's **COMPUTE_ASSESSMENT_RUNNERS** card (failure-write-back lifecycle) is NOT present here — the mandate names it as "Rasm.Element TASKLOG queued card + Element-leg residual," so it is inbound forward work Compute's own TASKLOG has not yet absorbed. DATA_GEOARROW_GLB is COMPLETE (verified in codecs.md — GeoArrowBuffer.ToImportedGeometry fully realized, item 6 satisfied).
- **IDEAS.md** — verdict **7/10**. OPEN pool is `(none)`; everything CLOSED. Correct for a cold-pass corpus but it means the mandate's three forward concerns (failure lifecycle, cold-formed dispatch, EC2 shear) have no idea-pool home yet — a `brief`/`ideate` obligation.

## [02]-[PER_PAGE_VERDICTS]

### Analysis (6 pages) — the mandate-critical folder

- **assessment.md** (335) — **9/10**. The discipline-assessment spine: `AssessmentRequest` [Union] 7 cases, uniform `AssessmentResult` fact-stream collapse (the doctrinal 7-parallel-result-record→one `(PropertyName,PropertyValue)` carrier), content-keyed `Node.Assessment` write-back over the seam CanonicalWriter+ContentAddress (one kernel XxHash128 seed-zero rail), RerunPolicy CacheFirst/Force. **THE MANDATE-ITEM-1 GAP LIVES HERE.** DEFECTS: (a) `Run` (line 283-291) returns `Fin<AssessmentResult>`; every runner aborts a deterministic failure onto opaque `ComputeFault.AnalysisRunFailed` — there is NO `AssessmentPayload.Failed` write-back, so a deterministic failure re-runs every call and NOTHING caches (mandate item 1 core). (b) `Assess` CacheFirst (line 245-250) **blanket-short-circuits ANY `Node.Assessment` hit to the 412-noop regardless of the cached payload's Outcome** — a cached Failed/Stale/in-flight node wrongly returns Satisfied-shaped noop (the mandate's "lifecycle-aware CacheFirst" defect, verbatim). (c) The `ComputeReceipt.Assessment` case (line 207) `Assessment(Discipline,Route,Key,Verdict,GoverningRatio,Admitted)` carries NO diagnostic/FailureKind/SolvePhase column — a typed failure cannot ride the receipt (confirmed against receipts.md:416 AssessmentWire — no failure slot). (d) `ResultBlob` egress sink is a documented pending capability (3-arg `Run` threads `None`, self-flagged line 76,79) — genuine but self-owned. Charter as it SHOULD be: the spine must own an `AssessmentPayload.Failed(discipline,route,inputKey,diagnostic,provenance)` write-back leg composing `Diagnostic.Of(phase,kind,message,key)`, a `RerunPolicy`/`Outcome`-aware CacheFirst that reads the cached node's lifecycle before short-circuiting, and a retry gate on `Diagnostic.Kind.Transient` — this is a **spine rebuild, not a card**, and it should compose the EXISTING `Runtime/scheduling` `JobGraph` reconcile/checkpoint substrate (see cross-cutting).
- **structural.md** (698) — **9/10**. The `Discipline.Structural` runner: `FrameModel` idealization, polymorphic `FrameBackend` (BFE 3D / FEALiTE 2D) both factoring through the ONE shared CSparse `SparseLU` owner (`RasmSolverFactory` injecting `Solve(ISolverFactory)` so BFE's binary-incompatible `DenseLU` never loads — a genuinely deep pin-conflict resolution), `(DesignCode,LimitState)` capacity table of REAL slenderness-aware delegates (AISC E3 Fcr, EN 1993 χ/χLT, NDS Ylinen Cp/Cl, EN 1995 kc/kcrit over E0,05 fractile, ...) with BOTH US+EN routes per family, real LTB/buckling/deflection off the FE envelope. **MANDATE ITEMS 2+3 LAND HERE.** DEFECTS/PRESSURES: (a) item 2 — the AISI cold-formed cells (line 621-627) are computed as the GROSS-section bound directly in-table; the boundary (466,780) EXPLICITLY defers effective-width to Materials `Profiles/steel ColdFormedDetail`. The mandate wants the resolver to dispatch `ColdFormedC → ColdFormedSections.Of → cold-formed Capacity overload`, but **this collides with the strata prohibition** (see verdict candidate V3): Compute reads the flat resolved `graph.SectionOf` `ComputedSection`, NOT the `SectionProfile` discriminant (which is Materials-authoring per RASM-COMPONENT-PARADIGM-DECISION.md:234-258) — Compute structurally cannot see `Catalogued` vs `ColdFormedC`. (b) item 3 — the capacity table has `shear-major`/`shear-minor` as web-crushing/gross-area checks; there is NO member-level EC2 6.2.3 `V_Rd,s=(Asw/s)·z·f_ywd·cot(theta)` stirrup pairing (correctly a forward DESIGN_CHECK concern — stirrup spacing is member-scope, and the Materials section screen already returns the web-crushing ceiling per the mandate). (c) item 1 — `Solve` failures (line 285 `AnalysisRunFailed($"<bfe-solve-break:{error.Message}>")`, line 389 `<fealite-singular:dof=...>`) lower onto opaque `ComputeFault`, NOT a typed `SolvePhase`/`FailureKind` — the runner-interior rebuild the mandate names. Charter: the runner should surface a typed `SolvePhase`(Project/Assemble/Factor/Recover/Check)×`FailureKind`(Singular/NonConvergent/InputMissing/Transient) discriminant the spine's `Diagnostic` composes.
- **physics.md** (483) — **10/10**. The single strongest page in my half. Three closed-form disciplines (Thermal/Acoustic/Fire) collapsed onto ONE `BuildingPhysics` kernel. Real EN ISO 13788 Glaser via genuine lower-convex-hull (Andrew's monotone chain) condensation-MASS-rate construction; EN ISO 10077-1 whole-window Uw via `AssemblyAggregator.AggregateWindow` (the side-by-side glazing-in-frame composition the through-thickness series structurally cannot reach — with the spacer Ψg correctly read off the window Pset not MaterialPropertySet.Thermal); EN 1993-1-2 steel-fire genuine 5s incremental march with temperature-dependent specific heat (735°C latent-heat spike) and shadow factor; full EN 1992-1-2 concrete tabulated method (both min-dimension AND axis-distance cover, worse-governed). Same-runner item 1 gap: failures lower onto `AssessmentInputMissing`/`AnalysisRunFailed`, no typed SolvePhase. Zero interior defects found.
- **energy.md** (481) — **9/10**. EnergyPlus subprocess runner: in-process OpenStudio model build (real annual RunPeriod + ideal-air dual-setpoint conditioning + policy internal loads, so demand is envelope-driven not free-floating-zero), parameterized `EnergyToolchain` discovery (env→configured→bundled, version-locked 3.11.0→25.2.0 with a `--version` self-report VersionGuard), full per-end-use `SqlFile.endUses()` fold (fuel-agnostic, Water excluded), hours-simulated annual-completeness validity fact, exhaustive SWIG `Optional<T>`/`IDisposable`/`Path` boundary discipline. DEFECTS: (a) item 1 — same opaque-fault gap (line 279 `<energy-native-fault>`, 299 `<energyplus-exit>`, 318 `<...no-total-site-energy>`). (b) self-flagged: `ResultBlob` egress (the eplusout.sql heavy artifact) is ephemeral because the 3-arg `Run` threads no object-store sink (480) — pairs with assessment.md's pending-sink gap. Charter as-is is correct; the sink is a real cross-stratum capability the failure-lifecycle rebuild should thread simultaneously.
- **lifecycle.md** (378) — **9/10**. EN 15978 embodied-carbon + EC3/openEPD REST + cost rollup. `Ec3Service` hand-thin typed-HttpClient (GET-only surface so a READ token can't write), one polymorphic `Cached<T>` fold, success-only HybridCache keyed by XxHash128 (transient fault NEVER cached — throw-at-boundary discipline). The `Normalize` basis-tagging (per-m³/m²/kg/item via the strongest-dimension Option `|` choice, killing the migration's ~1000× per-declared-unit-as-per-m³ error) is a genuinely subtle correctness win. Same item-1 opaque-fault gap. GWP-as-domain-scalar (dimensionless MeasureValue + kgCO2e label, never UnitsNet.Mass) is doctrinally exact.
- **aggregator.md** (306) — **9/10**. The relocated multi-ply `AssemblyAggregator` (from Materials — the [RELOCATION_SEAM] is clean, Materials retires its aggregator half). ISO 6946 series-U + ISO 13786 heat-capacity + EN 13788 vapour-Sd + mass-law STC through the SEAM `RatingContour.Stc.Fit` (one contour owner, never a second algorithm) + rule-of-mixtures + EN ISO 10077-1 window fold, all with per-discipline NaN decoupling (a thermal U survives a missing mechanical density). Typed `[FOLD_STATE]` accumulators. No defects.

### Model (7 pages) — mandate item 5 (embedding seam)

- **embedding.md** (231) — **9/10**. `VectorEncoding`×`VectorScore` retrieval half over `TensorPrimitives`. Content-key over ENCODED bytes+dimension+codebookId (so quantization posture is in the address; the non-invertible binary-hamming length gets the dimension folded in to avoid aliasing). **Item 5 embedding sub-clause CONFIRMED HONEST**: consumes `ProductCodebook` from `Rasm.Persistence` `Query/lane#VECTOR_CODEBOOK` by reference, does NO codebook fit, owns NO vector store — the two-stage retrieval is honest (binary sign-only gate returns keys, Persistence resolves fine forms, rerank over those). Only risk: the Persistence owner token `Query/lane` may have drifted to `Query/rail` (seam-freshness, not a Compute-side defect).
- **inference.md** (234) — **10/10**. OrtValue-only run-mode fold: `RunInput` [Union] polymorphic ingress over `TensorBridge.Ingress` (the sole OrtValue C-data factory), BoundFlow shared-arena device-resident hot path, `CachePolicy` 5-row behaviour-column axis with echo-validated read-through + precision-TTL negative cache + single-flight stampede collapse. The `ScoreFloat`/dispatch ref-struct exhaustiveness forfeit + loud runtime fault is a correctly-reasoned exemption. No defects.
- **generative.md** (354) — **10/10**. ORT-GenAI token-streaming: one `GenerationEvent` stream, per-sequence `StopOracle`, 4 RunMode arms (text/multimodal/streaming-audio/batched) all composing one Drain, real resolver-backed tool-call arm (Detect→Resolve→AppendTokens/RewindTo), process-global OgaHandle held ONCE (the per-Stream `new OgaHandle()` shutdown-race is a named defect it avoids), LIFO disposal. The receipt's constrained/tool slots are structurally caller-uninfluenceable. No defects.
- **identity.md** (156) — **9/10**. `ModelSource` [Union] 5-case acquisition fold to one byte admission, `ModelIdentity.Snapshot` pure-value checksum via the kernel `ContentHash.Of` seed-zero rail (never a per-call-site XxHash128 — the named federation-hasher discipline), `Accepts`/`Initializer` admission gates. Correctly reasons the opset-version omission (managed ModelMetadata doesn't expose it; a Google.Protobuf field-scan is rejected). No defects.
- **sessions.md** (198) — **10/10**. One shared InferenceSession per checksum, `Gate`-serialized OrtEnv boot, LRU-evicting resident map, shared-device-allocator lease, EP-context warm-start GENERALIZED into a device-keyed fleet-shared compiled context (`Compile` publishing a `(checksum,OrtEpDevice fingerprint)`-keyed ArtifactIndexRow so a cold farm node warms from the blob). The `OrtCompiledModelCompatibility` two-step verdict consumption + the `verdict.ToString().Contains("Incompatible")` named-defect avoidance is precise. No defects.
- **providers.md** (174) — **9/10**. EP axis (Cpu/CoreMl live, Cuda/DirectMl as design-record RESEARCH rows behind GPU RIDs), autoEP `OrtEpDevice` discovery with `HardwareAffinity`-ranked selection, `ModelPrecision` folded into BOTH the CoreML flag AND the EP-agnostic qdq accuracy knob (two sinks, correctly). No defects.
- **extension.md** (66) — **9/10**. Custom-op registration (ORT-managed lifetime, the out-handle V2 leak-form rejected) + the non-tensor `Egress` reader (string-tensor/ZipMap map/sequence). Tight, complete. No defects.

### Runtime (7 pages) — mandate item 5 (channels/scheduling seams)

- **admission.md** (288) — **10/10**. 6-case `ComputeIntent` + one shared `Spec`, 5-row `Substrate` axis (device-wgpu/onnx/genai/remote as column data), one derived `Veto`, per-intent eligible-chain-IS-degrade-order, `ComputeFault` 2200 band custody (the fault-family HOME — 2200..2212 core here, 2213..2220 lanes, next-free 2221). The `ShedVerdict` seam consumption + the `if(shed)` ladder avoidance is exact. No defects. NOTE: this owns the band any typed assessment `FailureKind` would extend at 2221+.
- **receipts.md** (434) — **9/10**. The 25-case `ComputeReceipt` union (single fact vocabulary), `[JsonDerivedType]` single-sited registry, fold-projection family, benchmark-claim fingerprint gating, NodaTime-protobuf wire bridges, full TS_PROJECTION. DEFECT (item 1 counterpart): the `Assessment` case (line 101 region / wire 416) carries no failure column — a typed assessment failure needs either a column here or a new receipt case; this is the receipts-side ripple of the failure-lifecycle rebuild. Otherwise flawless.
- **codecs.md** (833) — **9/10** content, **6/10** page-architecture. FIVE distinct owners in one page: TWO_HOP_TESSELLATION (+ GeoArrow item-6 consumer, + ifctester IDS relay), FIELD_RESULT_CODEC (chunked error-bounded + residual-predictor), GEOMETRY_DELTA (real FastCDC with SplitMix64 Gear + strict/loose dual-mask), TILE_PARTITION (3D-Tiles octree + EXT_structural_metadata), CONTENT_ADDRESSING (policy-seeded XxHash128 vs kernel-seed-zero GeometryHash — the distinct-keys discipline is meticulous). Content is 9/10 (item 6 DATA_GEOARROW_GLB fully realized + interior-ring fault-to-kernel is correct). But 833 LOC / 5 owners is a **split candidate** (see V5). Seam note: :115 `Query/pipeline` PORT is stale.
- **channels.md** (779) — **9/10** content, **5/10** page-architecture. SEVEN owners: PROTO_VOCABULARY (5 services + parity seam), CONTRACT_EVOLUTION (descriptor-diff drift), FAULT_PROJECTION (WireFault 17-code total fold), TRANSPORT_AXIS, CALL_POLICY, ARTIFACT_FRAMES, TS_PROJECTION. Content is superb (the `WireDocument` parity seam, the `Surface(...)` canonical checksum-then-diff two-gate, the `StatusRail` FrozenDictionary-by-numeric-value). But it is the **most overloaded page in the corpus** — 7 index entries, 779 LOC. The GraphDiff rpc correctly cites the surviving `Version/ledger#CHANGEFEED` (line 92) — so the BODY is fresher than ARCHITECTURE.md:119's dead `Sync` row. Strongest split candidate (V5): a `wire.md`(proto+contract+fault) / `transport.md`(transport+call+frames) partition.
- **scheduling.md** (394) — **10/10**. Five bounded `WorkLane` channel rows + `LaneRuntime` solve-guard (dispatch structurally returns a `LaneHandle`, never executes) + `CpuBudget` (the ONE cap all three concurrency axes read) + the `JobGraph` batch-wave dependency scheduler. **The JobGraph is a full incremental compute engine**: content-key reactive `Reconcile` (recompute only the moved subgraph, clean nodes replay the RESULT_CACHE hit), speculative run-ahead + `Poison` cone, fair-share preemption, cooperative memory-spill via `CheckpointPort` (the `Runtime/scheduling → Persistence [SPILL]` seam), `JobState` 8-row lifecycle with Faulted/Spilled/Preempted/Resumable + `ProgressCell.Aggregate` roll-up. **This is the substrate the assessment-failure lifecycle should compose** — it ALREADY owns JobState.Faulted, content-key reconcile, and checkpoint/resume, exactly the machinery mandate item 1's "Outcome.Dispatchable re-solve sweep" and "retry gate" need. `GraphCyclic` fault at 2220. No defects.
- **payload.md** (302) — **10/10**. The GPU-ready residency PAYLOAD codec (meshlet-cluster/quantized-vertex/point-splat/gaussian-splat) over the SAFE `Meshopt` span surface — mints only the content-keyed blob + `StreamSpan` layout, NEVER the manifest (AppUi `ResidencyManifest.Mint` owns that 1:1 projection — the second-mint-drift discipline). The four explicit-pointer boundary kernels (BuildMeshlets/ComputeMeshletBounds/ComputeSphereBounds/SimplifyPoints) are correctly reasoned as the count-arg exemption. Gaussian-splat arm fully implemented, honestly [UPSTREAM-BLOCKED] on the companion SPZ/SOG wire. No defects.
- **progress.md** (218) — **10/10**. One monotonic `ProgressPhase` (9 rows, rank+terminal+Dominance columns), Atom-backed `ProgressCell` with CAS rank guard + cadence-gated delivery + `Aggregate` roll-up, observation seams (AppUi marshal / wire / receipt). The `Resolve` bottleneck fold by Dominance column (Faulted>Cancelled, Completed requires unanimity) is elegant. No defects.

### Solver (6 pages)

- **contract.md** (1093) — **10/10** content, **7/10** page-architecture. One `PhysicsKind`×`BoundaryCondition`×`ElementClass` uniform solve axis, `SolveLane` assembling the genuine `Bᵀ·D·B` operator (per-MaterialForm strain-displacement B, full constitutive D, Gauss-weighted by real detJ — the "single trilinear stencil reused across every topology" is a named illusory defect it deletes), 7 SolveMethod rows (the 4 iterative mirroring the verified numeric-lane IterativeMethod 1:1, no phantom LOBPCG/ILU), real Newmark-β + central-difference + modified Newton-Raphson (genuine `f_ext−f_int(u)` nonlinear residual, Armijo on that residual not the inner Krylov — a named deleted fake), Aitken Δ²-relaxed CoupledLane, RecoveryAction ladder. Two sections (SOLVE_CONTRACT + CONSTITUTIVE) at 1093 LOC — CONSTITUTIVE is a splittable owner. The `Periodic`/`Contact` BC "read Master, real tie" fixes are meticulous. No content defects.
- **discretization.md** (896) — **10/10** content, **7/10** page-architecture. Volumetric `MeshKernel` (Bowyer-Watson Delaunay + octree + sweep + inflation — the "bounding-box voxelization masquerading as six meshers" is a named deleted form), `ElementClass` 12 topologies over 3 ShapeFamily arms collapsed onto ONE Vandermonde monomial `Sample` + serendipity + rational-pyramid, owned-build Gauss tables (conical pyramid rule avoiding the (1−ζ) singularity), Verdict `MeshMetric`, Dörfler-marked h/p/hp adaptive refinement. One giant [DISCRETIZATION_MESH] section at 896 LOC — coherent but dense. No content defects.
- **optimizer.md** (1128) — **10/10** content, **7/10** page-architecture. One `Optimizer` axis, 11 OptimizerKind rows each a GENUINE kernel (the "operator-swap masquerade routing cma-es/pso/sa through one GeneticAlgorithm" is the explicitly-deleted fake) — real CMA-ES (Evd-factored covariance, CSA step-size), Clerc PSO, Metropolis SA, Bayesian-GP that FITS the GpModel each iteration, gradient-adjoint reading the DDG `SensitivityLaw.Chain` reverse tape, topology-SIMP off the real compliance sensitivity, CP-SAT/MILP via the typed model-builder, robust-minimax sharing the UQ RandomVariable inputs. Single 1128-LOC [OPTIMIZER_LANE] section — the largest page; it IS one Optimize fold so arguably one owner, but it stretches the single-page norm hard. No content defects.
- **uncertainty.md** (696) — **10/10** content. 8 UncertaintyMethod rows over 4 UqStrategy drivers, `RandomVariable` [Union] 10 cases with correct Wiener-Askey correspondence (the "Weibull→Laguerre" is a named mathematically-wrong assignment it avoids — Weibull/Gumbel/Triangular/Empirical→discretized-Stieltjes arbitrary-PCE), HLRF FORM + Breitung SORM (Evd principal curvatures of the FD Hessian at the MPP), Au-Beck subset simulation, Saltelli-2010/Jansen Sobol, Gaussian-copula correlation via Cholesky. Composes the sweep `SensitivityTornado` (one variance fold, no parallel Sobol loop). No defects.
- **sweep.md** (313) — **10/10**. DOE `SweepGrid`: `SweepAxis`×`DoeDesign` orthogonal (the "per-axis-1-D-then-Cartesian space-filling mis-model" is a named deleted form — space-filling designs draw JOINT d-dim nets), 8 DoeDesign rows (factorial/fractional-2^(k-p)/Plackett-Burman-Hadamard/LHS/Sobol/Halton/CCD-rotatable/Box-Behnken), quantile-binned `SensitivityTornado`, `FrameBudget` early-stop forking refinement onto a background lane, rolled-up per-point ProgressCell. Contract-uniform `evaluate` oracle with optimizer/uncertainty. No defects.
- **clash.md** (344) — **10/10**. `ClashScale` narrow-phase over the geometry-owned DECODED node-link wire (`SpatialIndex.ToAcceleration` — Compute NEVER builds/refits, the double-owner form is named), one `NodeLinkPairs` fold for both BVH+octree (the parallel BvhPairs/OctreePairs + the Morton-cell mis-read are named deleted forms), COMPLETE two-direction Möller-Trumbore + Ericson closest-distance (the single-direction test + hardcoded-0.0 penetration + vertex-0 witness are named fakes), `DigitalTwin` Kalman+CUSUM ROM loop over the optimizer `Surrogate` baseline, receipted write-back. The CLASH_GOLDEN 160-byte two-sided fixture is a real cross-package parity anchor. No defects.

## [03]-[CROSS_CUTTING]

- **DUPLICATION**: essentially none. The corpus is aggressive about single-owner collapse. The `ComputeReceipt` union is the ONE fact vocabulary (25 cases, no parallel receipt types — every "would-be" MeshReceipt/OptimizerReceipt/SolveReceipt is a named rejected form). The `ComputeFault` 2200 band is the ONE fault family (partial-extended across pages, never a parallel union). The `evaluate` oracle (`Func<DesignPoint,Fin<Seq<double>>>`) is the ONE coupling point shared by optimizer/sweep/uncertainty/clash-twin. The `LowDiscrepancy` sampler, the `RatingContour` contour kernel, the `SensitivityTornado` variance fold, and the CSparse `SparseLU` are each single-owned and composed cross-page. `AssemblyAggregator` is composed by BOTH physics.md and lifecycle.md with no re-derivation.
- **CONCERN MIXING**: minimal. The seam prohibitions are enforced page-by-page (Compute never re-declares Bim's IdsVerdict, never re-mints the AppUi ResidencyManifest, never fits the Persistence ProductCodebook, never re-authors the geometry SpatialIndex). The ONE structural tension is item-2's cold-formed dispatch (V3).
- **HARDCODING-vs-GENERATOR**: rosters are correctly seed-DATA. DesignCode×LimitState is a frozen delegate table (not imperative arms). ConcreteFireTable is a per-member-type frozen table. Substrate/WorkLane/ProgressPhase/OptimizerKind/UncertaintyMethod are all row-data smart-enums with generated total Switches. No enumerated-instance-where-a-generator-belongs found. The one honest constant-heavy spot (physics fire ISO curves) is correctly per-row delegates on `FireExposure`.
- **DEAD CARRIERS**: none found in read pages. Named-and-avoided dead fields are pervasive (Periodic.Master, Contact.Master, AccelerationStructure's provenance int, StructuralPolicy fields all folded into CanonicalBytes). The mandate-flagged `ComputeReceipt.Assessment` lacking a failure column is the closest to a carrier-shape gap, but it is an ABSENCE not a dead field.
- **UNWIRED SEAMS**: (a) ARCHITECTURE.md:116 `Compute → Persistence/Store/quality [SHAPE] geometry-derived anomaly rule source` — declared, no read-half realizer. (b) The four stale Persistence seam rows (:111,:115,:119 + the Query/cache/lane token drift) are wired to DEAD owners. (c) The GaussianSplatScan/SplatScan wire ([UPSTREAM-BLOCKED]) is honestly declared, not a defect.
- **UNMINED CAPABILITY (catalog anchors)**: the `.api/` set (37 Compute-local + 30 shared substrate) is deep and the pages mine it hard (api-onnxruntime 80KB → inference/sessions/providers fully; api-sharpgltf → codecs tile emit; api-openstudio → energy; api-torchsharp/libtorch-cpu → the CLASSICAL_ML_BLAS dual-leg is DECLARED in README but I did not see a Tensor-folder consumer in my half — the estimator/blas consumers are corpus-b's Stats/Tensor). `GeneticSharp`/`Google.OrTools`/`MathNet` mined genuinely in optimizer. No obvious unmined admitted package in my half; the roster question (native BLAS activation) is the one BLOCKED card.
- **FOLDER ARCHITECTURE**: the folder SET is healthy — Analysis(6)/Model(7)/Runtime(7)/Solver(6) are all real higher-order domains, no one-file-one-folder combos, no flat sprawl, growth-conducive. The DEFECT is **page granularity asymmetry**: Model/Analysis run one-owner-per-page (66–698 LOC, one [INDEX] concern) while Runtime channels.md (779, 7 owners) / codecs.md (833, 5 owners) and Solver contract.md (1093, 2 owners) / optimizer.md (1128, 1 owner) / discretization.md (896, 1 owner) are multi-owner or single-mega-owner behemoths. This is a graded folder-architecture inconsistency (V5).

## [04]-[VERDICT_CANDIDATES]

**V1 — The assessment-failure lifecycle is an architecturally load-bearing ABSENCE, not a card, and it should compose the existing JobGraph substrate.** Evidence: assessment.md `Run` returns `Fin<AssessmentResult>` (283) → deterministic failures abort onto opaque `ComputeFault.AnalysisRunFailed` (structural.md:285/389, physics fire-route, energy.md:279/299/318) and NOTHING caches; CacheFirst blanket-short-circuits any node hit to the 412-noop ignoring the cached Outcome (assessment.md:245-250); `ComputeReceipt.Assessment` (receipts.md:101/416) has no failure column. Meanwhile `Runtime/scheduling` JobGraph ALREADY owns `JobState.Faulted`/`Spilled`/`Resumable`, content-key `Reconcile` (recompute-only-moved), and `CheckpointPort` spill/resume (scheduling.md:183-421) — exactly the "Outcome.Dispatchable re-solve sweep" + "retry gate reading Diagnostic.Kind.Transient" machinery the mandate demands. RULING: the failure-write-back leg (`AssessmentPayload.Failed` + `Diagnostic.Of(phase,kind,message,key)` caching a Failed `Node.Assessment`) + lifecycle-aware CacheFirst + typed `SolvePhase`/`FailureKind` in the four runners is a spine+runner-interior rebuild that MUST reuse the JobGraph reconcile/checkpoint substrate rather than mint a parallel re-solve engine.

**V2 — Compute's ARCHITECTURE.md [02]-[SEAMS] Persistence column is stale against the landed Persistence campaign and needs a mechanical re-point.** Evidence: RASM-CS-PERSISTENCE-BRIEF.md:14 VERDICT states the element-rebuild collapse DELETED `Sync/` and `Query/pipeline`; E3 renames `api-arrow`→`Query/rail`+`Sync/egress`. Compute rows :111 (`Sync` FastCDC), :115 (`Query/pipeline` PORT), :119 (`Sync` WIRE) target deleted owners; the heavy `Query/cache` refs (:101,:102,:103,:108,:109) + `Query/lane` (:99) predate the reconcile. Yet channels.md:92 already cites the SURVIVING `Version/ledger#CHANGEFEED` — proving the page bodies are fresher than the seam ledger. RULING: a per-row re-point (`Sync`→`Sync/egress`, `Query/pipeline`→the surviving canonical-bytes owner, `Query/lane`/`Query/cache`→`Query/rail` where renamed) with each Compute-side consumer re-verified; the embedding VECTOR_CODEBOOK seam stays honest but its owner token needs the same check.

**V3 — Mandate item 2 (cold-formed capacity dispatch) as stated collides with the AEC→app-platform strata prohibition and must be reframed.** Evidence: the mandate wants Compute's structural resolver to dispatch `Catalogued→SteelDesign.Capacity` / `ColdFormedC→ColdFormedSections.Of→cold-formed Capacity`, but `SectionProfile` (the Catalogued/ColdFormedC discriminant) is a `Rasm.Materials`-authoring union (RASM-COMPONENT-PARADIGM-DECISION.md:234-258, `Solve(SectionProfile)→Fin<ComputedSection>`), and Compute reads only the FLAT resolved `graph.SectionOf`→`ComputedSection`/seam `SectionProperties` (structural.md boundary 466,780 explicitly defers effective-width to Materials; SEAM_PROHIBITIONS [05] forbids a Compute→Materials reference). RULING: either Materials bakes the cold-formed EFFECTIVE `ComputedSection` onto the seam (Compute reads it flat, gross vs effective invisible to Compute) OR the overload dispatch stays Materials-side and Compute's AISI cells consume the pre-reduced section — the dispatch cannot live "in the Compute structural resolver" without the forbidden downward edge. The brief must pick the seam-bake resolution.

**V4 — Mandate item 3 (EC2 6.2.3 stirrup shear V_Rd,s) is a genuine forward DESIGN_CHECK the structural capacity table lacks, and it is correctly member-scope.** Evidence: structural.md capacity table (line 603-666) has `shear-major`/`shear-minor` as web-crushing/gross-area stress checks; there is no `V_Rd,s=(Asw/s)·z·f_ywd·cot(theta)` truss-model stirrup term, and no `Asw`/`s`/`theta` inputs on `CapacityContext`. The mandate confirms the Materials section screen already carries `RcElastic.ShearLinkAreaMm2` and returns the web-crushing ceiling (Rasm.Materials capacity.md:242-244), so the member-level stirrup pairing is Compute's forward concern. RULING: a new `en1992` `shear-links` LimitState row reading a seam-baked `Asw/s`/`theta`/`z` off the RC design payload, paired against the existing web-crushing ceiling — one table row + one CapacityContext extension, not a new owner.

**V5 — Page-granularity asymmetry: Runtime channels.md (779/7-owner) + codecs.md (833/5-owner) and the Solver mega-pages are split/normalize candidates against the corpus one-owner-per-page norm.** Evidence: Model/Analysis pages carry one [INDEX] concern (66-698 LOC); channels.md carries SEVEN ([01]PROTO..[07]TS), codecs.md FIVE, contract.md TWO (SOLVE_CONTRACT+CONSTITUTIVE at 1093). A logical, growth-conducive folder architecture is a graded axis. RULING: channels.md is the strongest split (a `wire.md`{proto+contract+fault} / `transport.md`{transport+call+frames} partition — three wire-vocabulary owners vs three transport/policy owners); codecs.md could shed TILE_PARTITION+CONTENT_ADDRESSING; contract.md could extract CONSTITUTIVE. optimizer.md (1128, one Optimize fold) and discretization.md (896, one mesh kernel) are defensibly single-owner and may stay. NOT a correctness issue — a maintainability/navigability ruling the brief should weigh.

**V6 — The `ResultBlob` object-store egress sink is a pending cross-stratum capability declared identically in assessment.md and energy.md; it should land WITH the failure-lifecycle rebuild, not separately.** Evidence: assessment.md [03] boundary (79,183) and energy.md [05] (480) both self-flag that the 3-arg `Run(graph,request,clocks)` threads no artifact sink, so a subprocess route's heavy artifact (EnergyPlus SQLite, FEA result set) stays ephemeral and `AssessmentResult.ResultBlob` threads `None`. The failure-lifecycle rebuild (V1) is the natural moment to thread the injected sink through `Assess`/`Run` + the four runners. RULING: fold the sink-threading into the V1 spine rebuild as one signature change, not two.

**V7 — The dangling `Compute → Persistence/Store/quality` write-seam (ARCHITECTURE.md:116) is declared but unrealized in the read half.** Evidence: the "geometry-derived anomaly rule source" [SHAPE] edge has no producer among the 26 read pages (the closest is clash.md's typed ClashPair/TwinVerdict egress, which flows to receipts + AppHost write-back, not a Persistence quality store). RULING: either the seam is a genuine unwired obligation (a Compute-side anomaly-rule projection Persistence's Store/quality consumes) or it is a stale row from a superseded design — the brief must resolve which; if Store/quality also drifted in the Persistence campaign it compounds V2.

**V8 — Item 6 (DATA_GEOARROW_GLB) and the embedding VECTOR_CODEBOOK seam are CLOSED/HONEST — record as satisfied, do not re-open.** Evidence: codecs.md `GeoArrowBuffer.ToImportedGeometry` (line 59-114) fully decodes the column-major coordinate/offset buffers into the ONE ImportedGeometry vertex/index spans on the existing companion channel, with interior-ring geometries correctly faulted to the kernel's robust triangulation (never a silent hole-fill) — Compute records the consumer contract and re-mints no layout (mandate item 6 satisfied, TASKLOG confirms COMPLETE). embedding.md consumes the Persistence codebook by reference with zero Compute-side fit/store (item 5 embedding sub-clause honest). RULING: the brief should mark these disposed, focusing forward work on V1-V4.
