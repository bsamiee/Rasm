# [<owner-token>_RULINGS]

<rulings-charter-lead: the folder's decision registry in owning voice — the settled rulings agents re-litigate for lack of a home. A row is one decision with the one why that guards its re-litigation: a package admission or rejection, an owner-choice discriminant, a collapse refusal naming the guarantee the denser form loses, a structure retirement, or a folder-learned working law. Append-mostly; a row earns its seat while its why stays homeless, and dies only when fact and why both live at one durable surface.>

<!-- source-only: ruling row template (every section shares it):
- <subject as a code span — the package, owner, surface, or decision> <DECISION-VERB in owning voice — KEEP / rejected / homes at / stays / do-not-re-reject> — <the one load-bearing why that guards the re-litigation>.

Row anatomy — a guard-grade row pairs both framings: the why names the discriminant or lost guarantee precisely enough that two readers reach one verdict; the recurring wrong move is named as the move a review or sweep re-proposes (a dedup sweep, a folding demand, a bump-to-newest, a gate-every-native re-add); the correct route states where the capability legitimately lands instead; and an evidence-bound decision carries its reopen predicate — the one observable that legitimately re-litigates it — so the row guards without freezing. A decision-only row naming no attacker and no route is unfinished.

Admission bar: a row exists ONLY when its rationale is a standing re-litigation guard with no durable home; a row dies only when its fact AND its why both live at one durable surface — a page, manifest, registry, or fence stating the what while the why (discriminant, lost guarantee, refuted objection) stays homeless ADMITS the row. A DROPPED idea graduates only by explicit promotion — its rejection re-stated as a standing guard in registry voice; the drop record itself stays in IDEAS.md [02]-[CLOSED], never auto-graduating into a ruling.

Placement: a new ruling appends at its section's tail — adjacency is arrival order, never taxonomy; a later insight sharpens the owning row in place rather than re-sorting the section or minting a near-duplicate beside it.

Scope: RULINGS.md is a per-folder citizen minted ON DEMAND at a territory's first homeless ruling — never pre-seeded, an empty instance is litter. Any `.planning`-carrying tier is eligible: package folder, branch tier, cross-libs tier. A ruling homes at the NARROWEST tier whose scope contains it — a package-scoped decision at the package, a branch-spanning decision at the branch, an estate-spanning decision at the cross-libs tier — and appears at exactly one tier; a wider tier references nothing downward, and a duplicate across tiers is a defect resolved by keeping the narrowest legitimate home.

Sections are a closed vocabulary — `[PACKAGES]` admissions and rejections, `[SHAPE]` owner-choice discriminants, `[COLLAPSE]` density refusals, `[STRUCTURE]` layout and retirement rulings, `[PROCESS]` folder-learned working law; omit an unused section and renumber survivors. One decision per row; a row carrying two decisions splits into siblings. Subjects and members are code spans in verified spelling; any slug reference is UPPERCASE_SNAKE — a numeric or hyphenated slug is a defect — and every path is repo-relative.
-->

## [01]-[PACKAGES]

- <package-name> KEEP — <the reason the admission holds against its recurring re-rejection, e.g. newest stable, dominant with no successor, algorithmically complete; the objection named and refuted>.
- <admission-discriminant> — <the rule that decides what this folder admits versus routes to a sibling, e.g. managed purity, native-asset placement, license gate>.

## [02]-[SHAPE]

- <owner> homes at <stratum or surface>, not <the rejected alternative> — discriminant: <the fact that decides it, e.g. package dependency, runtime floor>; <the drift the wrong choice manufactures>.

## [03]-[COLLAPSE]

- <plural surface> stays plural — <the central owner that IS the collapse> is the collapse; folding <the closed rosters> into one parameterized row erases <the compile-time guarantee lost> and mints <the anti-pattern>, denser on paper and weaker in every guarantee.

## [04]-[STRUCTURE]

- <retired or relocated surface> does NOT re-enter — <the reason it was retired and where its capability now lives>; a "missing" gap is filled from <the durable owner>, never by re-adding <the retired form>.

## [05]-[PROCESS]

- <working law> — <the folder-learned rule and the failure it fences, e.g. verification route, admission sequencing, gate ordering>; <the recurring mistake that re-litigates without this row>.
