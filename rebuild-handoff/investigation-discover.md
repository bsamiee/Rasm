# [WF1_INVESTIGATION_DISCOVER]

The read-only DISCOVER findings of WF `wf_35e9d759-619` (the `element-component` rebuild: collapse of Materials `Profiles/*`+`Connection/*` into one `Component` owner, the `Rasm.Element` seam touchpoints, and the `Appearance` anchors).

## [APPEARANCE-ANCHORS]

The 5 outbound `page#CLUSTER` references FROM 2 Appearance pages INTO the collapsing `Profiles/*`+`Connection/*` pages, plus the 4 inbound Appearance cluster targets the collapsing pages reference back, plus the FROZEN `AppearanceSummary.Of` / `AppearanceKey` seam invariant.

### Pages
- `libs/csharp/Rasm.Materials/.planning/Appearance/bsdf.md` — SOURCE: holds 1 of the 5 outbound anchors (line 3 -> `Profiles/profile#PROFILE_OWNER`, a `ProfileFault`-2300 band-disjointness citation); ALSO owns the inbound target cluster `#SHADING_FRAME` (`Profiles/capacity` references it)
- `libs/csharp/Rasm.Materials/.planning/Appearance/graph.md` — SOURCE: holds 4 of the 5 outbound anchors (line 250 -> `Connection/connection#CONNECTION_OWNER` + `Connection/joint#JOINT_FAMILY`; line 254 -> `Profiles/glazing#GLAZING_FAMILY` + `Profiles/masonry#PROFILE_FAMILY`, all in `MaterialLibrary` render-row notes); ALSO owns the inbound target `#MATERIAL_LIBRARY` (every `Profile`/`ConnectionItem.AppearanceId` resolves here)
- `libs/csharp/Rasm.Materials/.planning/Appearance/interchange.md` — FROZEN owner: `AppearanceSummary.Of` / `AppearanceKey` / `MaterialWire.Summary` seam lowering (tolerance 0.0); inbound target `#MATERIAL_WIRE` (`Profiles/glazing` references it). No outbound anchors
- `libs/csharp/Rasm.Materials/.planning/Appearance/photometric.md` — inbound target `#PHOTOMETRIC` (`Profiles/steel`:528 references it). No outbound anchors
- `libs/csharp/Rasm.Materials/.planning/Appearance/{acquisition,finish,surface,texture,weathering}.md` — the remaining 5 Appearance pages; NO anchor role into/from the collapsing pages (intra-Appearance refs only)
- `libs/csharp/Rasm.Materials/.planning/Profiles/profile.md` — COLLAPSING target of anchor #1 (`#PROFILE_OWNER`); also carries the inbound `Profile.AppearanceId:MaterialId` field (line 174) -> `graph#MATERIAL_LIBRARY`
- `libs/csharp/Rasm.Materials/.planning/Connection/connection.md` — COLLAPSING target of anchor #2 (`#CONNECTION_OWNER`); carries `ConnectionItem.AppearanceId:MaterialId` (line 157) + the two-slot `AppearanceId`/`CapacityKey` independence (line 17)
- `libs/csharp/Rasm.Materials/.planning/Connection/joint.md` — COLLAPSING target of anchor #3 (`#JOINT_FAMILY`); `AppearanceId` Switch projection (lines 147-148)
- `libs/csharp/Rasm.Materials/.planning/Profiles/glazing.md` — COLLAPSING target of anchor #4 (`#GLAZING_FAMILY`); also inbound-references `graph#MATERIAL_LIBRARY` (16,58) + `interchange#MATERIAL_WIRE` (61)
- `libs/csharp/Rasm.Materials/.planning/Profiles/masonry.md` — COLLAPSING target of anchor #5 (`#PROFILE_FAMILY`); also inbound-references `graph#MATERIAL_LIBRARY` (3,311)
- `libs/csharp/Rasm.Materials/.planning/Connection/{fastener,hanger,reinforcement}.md` — COLLAPSING pages carrying inbound `AppearanceId:MaterialId` -> `graph#MATERIAL_LIBRARY` (fastener:199, hanger:192, reinforcement:183)
- `libs/csharp/Rasm.Materials/.planning/Profiles/capacity.md` — COLLAPSING page; inbound-references `Appearance/bsdf#SHADING_FRAME` (16,384)
- `libs/csharp/Rasm.Materials/.planning/Profiles/steel.md` — COLLAPSING page; inbound-references `Appearance/photometric#PHOTOMETRIC` (528)
- `libs/csharp/Rasm.Materials/ARCHITECTURE.md` — `[02]-[SEAMS]` is the WF-1 seam-mirror endpoint; line 64 (`Appearance/interchange` -> `Rasm.Element/Graph` `CONTENT_KEY`) states the FROZEN `AppearanceKey` dedup invariant; codemap [01] + lines 64-71 list the Appearance seam rows
- `libs/csharp/Rasm.Materials/README.md` — `[01]-[ROUTER]` entries [13]-[21] are the 9 Appearance pages (stable; only the [01]-[12] Profiles+Connection entries collapse to `Component/`)

### Gaps / Defects
- DANGLING-ON-COLLAPSE (the sole work item): all 5 outbound anchors target `Profiles/*` + `Connection/*` `page#cluster` paths that collapse into `Component/` per scope [5]; post-collapse they dangle. They are ANCHOR-ONLY fixups — only the reference-token path retargets to the new `Component/` `page#cluster` (WF-1 DECIDE's ~428-anchor old->new map owns the exact new targets); NO signature, `MaterialParameters` render-row, band-disjointness fact, two-slot-independence note, or IOR-1.0 cavity semantic may change. The 5: `bsdf.md:3->Profiles/profile#PROFILE_OWNER`; `graph.md:250->Connection/connection#CONNECTION_OWNER`; `graph.md:250->Connection/joint#JOINT_FAMILY`; `graph.md:254->Profiles/glazing#GLAZING_FAMILY`; `graph.md:254->Profiles/masonry#PROFILE_FAMILY`
- INBOUND-CLUSTER PRESERVATION: 4 Appearance clusters are anchor TARGETS the collapsing pages reference back — `graph#MATERIAL_LIBRARY` (`Profile`/`ConnectionItem.AppearanceId` resolution, ~10 inbound hits), `bsdf#SHADING_FRAME` (capacity), `interchange#MATERIAL_WIRE` (glazing), `photometric#PHOTOMETRIC` (steel). The Appearance side must NOT rename these clusters; the collapsing-page rebuild owns updating the reference TEXT on its own (moved) side, but the cluster names on the Appearance side are frozen anchor points
- NO signature-rebuild gaps in Appearance: scope [5] freezes Appearance signatures, so the scope-[2] defect categories (naive owners, scalar smears, hardcodes, phantom members) do NOT apply here — they apply to the COLLAPSING `Profiles`/`Connection` generative-data pages (CMU/brick/IGU/fastener/rebar/stud/weld), not to the appearance-anchors area. Appearance pages read as already-realized (REALIZED markers, content-key + single-mint law enforced)
- NO present dual-paradigm coupling: `interchange.md` already names the anti-patterns explicitly (a local `CanonicalWriter` beside the seam factory = byte-order divergence defect; a peer re-mint of the OpenPBR algebra = `CROSS_LANGUAGE_WIRE` drift defect). The only RISK is a careless anchor retarget that perturbs the seam-mirror or the dedup factory composition; not a defect to fix, an invariant to guard
- BASELINE CLEAN: all 5 outbound target clusters and all 4 inbound target clusters resolve in the current tree (`PROFILE_OWNER`, `CONNECTION_OWNER`, `JOINT_FAMILY`, `GLAZING_FAMILY`, `PROFILE_FAMILY`; `MATERIAL_LIBRARY`, `SHADING_FRAME`, `MATERIAL_WIRE`, `PHOTOMETRIC` all exist as `## [02]` headings) — so there is NO pre-existing dangle; the gap is purely the prospective collapse. No appearance-anchor residual is logged in `IDEAS.md`/`TASKLOG.md` (the IDEAS `[SPECTRAL_REFLECTANCE_GROUNDING]` item is an unrelated forward Appearance-internal growth idea)

### Anchors to Preserve
- FROZEN `AppearanceSummary.Of(double r, double g, double b, double metallic, double roughness, double opacity, bool transmissive, double tolerance)` called with `tolerance: 0.0` — SEAM-OWNED (`Rasm.Element/Graph/element#NODE_MODEL`); mints `AppearanceKey` as kernel seed-zero `XxHash128` over canonical PBR bytes at full IEEE precision (tolerance 0.0 = no Header-quantization). `interchange.md:119`, `ARCHITECTURE.md:64`. NEVER perturb the signature, the tolerance, or the hasher
- FROZEN `AppearanceSummary` seam record shape: `(UInt128 AppearanceKey, double BaseColorR, double BaseColorG, double BaseColorB, double Metallic, double Roughness, double Opacity, bool Transmissive)` — `Transmissive` distinct from `Opacity`/alpha. `interchange.md:480`. Owned by the seam; Materials composes, never re-declares
- FROZEN `MaterialWire.Summary(MaterialParameters parameters) -> AppearanceSummary` — the Materials lowering that composes the seam factory; the CONTRACTED `projection#MATERIAL_SUBGRAPH` entry `MaterialSubgraph.Capture` composes via `MaterialLibrary.Lookup(id, key).Map(MaterialWire.Summary)`. `interchange.md:111`
- FROZEN dedup invariant: the SAME `AppearanceSummary.Of` factory is composed by `Rasm.Bim` `Semantics/appearance#APPEARANCE_PROJECTION.Project`, so a Materials OpenPBR row and a BIM `IfcSurfaceStyleRendering` style describing one surface mint ONE `AppearanceKey` and dedup to one `Node.Appearance` id. This is the WF-1 seam-mirror row at `ARCHITECTURE.md:64` (must mirror the Element `[2]-[SEAMS]` endpoint verbatim)
- The 5 cross-page anchors are ANCHOR-ONLY: retarget the reference token to the new `Component/` path, but the surrounding text is frozen — `bsdf.md:3` `ProfileFault-2300` band-disjointness citation; `graph.md:250` `metal.steel` + `polymer.adhesive` render-row resolution notes; `graph.md:254` `gas.cavity` IOR-1.0 glazing-cavity-fill note + `Assign` masonry-`Profile`->row generalization note
- Stable inbound carriers (scope [5]: `Profile`/`ConnectionItem` carry a stable `MaterialId` `AppearanceId`; reconcile is at the owner-agnostic `AppearanceKey`): `Profile.AppearanceId:MaterialId` (`profile.md:174`) and `ConnectionItem.AppearanceId:MaterialId` (`connection.md:157`) plus per-family projections (fastener:199, hanger:192, joint:147-148, reinforcement:183) — the field name, `MaterialId` type, and `graph#MATERIAL_LIBRARY` resolution target survive the `Component/` collapse unchanged
- The 4 inbound Appearance cluster names: `graph#MATERIAL_LIBRARY`, `bsdf#SHADING_FRAME`, `interchange#MATERIAL_WIRE`, `photometric#PHOTOMETRIC` — preserved on the Appearance side so collapsing-page references stay valid
- `ConnectionItem` two-slot independence: `AppearanceId` (`graph#MATERIAL_LIBRARY` render row) and `CapacityKey` (`properties#MATERIAL_PROPERTY_CATALOGUE` Mechanical row) are INDEPENDENT `MaterialId` slots, neither derived from the other (`connection.md:17`) — the rationale several anchors cite (coated rebar: capacity `metal.steel`, appearance epoxy-coat row); must survive the collapse

## [PROFILES-CONNECTION]

Materials `Profiles/*` + `Connection/*` under `libs/csharp/Rasm.Materials/.planning` (the collapse SOURCE that `RASM-REBUILD-SCOPE.md` [1].3/[5] fuses into one `Component/` sub-domain; cross-section becomes a `Component` FIELD, the two projectors collapse into one `Component` projection). Index docs + 19-catalog `.api/` tier live at the package root `Rasm.Materials/`, not in `.planning`.

### Pages
- `libs/csharp/Rasm.Materials/.planning/Profiles/profile.md` (423L) — `Profile` polymorphic owner, `ProfileFamily {masonry/cmu/steel/timber/glazing}` `[SmartEnum]`, `ProfileFault` band-2300 `[Union]` (7 slots Dimension/Coring/Family/Bond/Mortar/Section/Capacity), canonical 17-field `ComputedSection`, shared `ParametricSection` solver, `ProfileAuthority` `[SmartEnum]`, `ProfileCatalogue.Build/Sections`, [03] M7 `ProfileResolution` one-hop (`ProfileRef->ResolvedProfile` cache)
- `libs/csharp/Rasm.Materials/.planning/Profiles/capacity.md` (384L) — `SectionCapacity` `[Union]` closed at 5 (RcInteraction/RcElastic/SteelLrfd/TimberEc5/MasonryCompression), Demand/Utilisation/GoverningAction, DiagramResolution, `SectionCapacityResolver` eager-solve boundary, `CapacityRay` Moller-Trumbore hull ray-cast; composes VividOrange `InteractionDiagram`/`IForceMomentMesh` and imports `Connection.Reinforcement` `RcSection` + Steel/Timber/Cmu receipts
- `libs/csharp/Rasm.Materials/.planning/Profiles/cmu.md` (300L) — CmuGrade/CmuStrength/CmuDensity/CmuAggregate `[SmartEnum]`, `CmuSection` cell/face-shell/grout record, NetSection/GroutedSection via `ParametricSection.Hollow`, `CmuStrength.Resolve` TMS 602, `ProfileCatalogue.BuildCmuRows` + CmuSections
- `libs/csharp/Rasm.Materials/.planning/Profiles/glazing.md` (492L) — GlassType/Coating/CavityGas/SpacerType/GlazingBuild `[SmartEnum]`, Pane/Cavity/GlazingSection, `GlazingThermal` EN 673 Ug kernel + EN 410 solar-optical + mass-law Acoustic, GlazingPerformance, ToProperties/ToLayerSet/ToUnit, GlazingGwp
- `libs/csharp/Rasm.Materials/.planning/Profiles/masonry.md` (346L) — Coring/CoringClass/BondKind/BondGeometry(delegate-bearing)/Orientation/Cut/ClosureRule/SpecialShape/MortarProfile/MortarType `[SmartEnum]`, CourseTemplate/MortarJoint, `BondName` generative course algebra, `ProfileCatalogue.BuildMasonryRows`; OWNS the cross-family `Coring` vocabulary
- `libs/csharp/Rasm.Materials/.planning/Profiles/steel.md` (533L) — SteelTopology/SteelClass/CompactnessClass/SteelGrade `[SmartEnum]`, `SteelStiffness` closed-form Zx/Zy/J/Iw/Avy/Avz, `SectionReader.Read` VividOrange admission, `SteelDesign` Classify/Capacity LRFD (LTB/composite/cold-formed), `CompositeDetail` reads `Joint.StudClass`; structurally richest, mostly REALIZED
- `libs/csharp/Rasm.Materials/.planning/Profiles/timber.md` (444L) — TimberForm/TimberGrade/ServiceClass/LoadDuration `[SmartEnum]`, PlyLayup, `TimberSection` (OrthotropicLaw/EffectiveStiffness gamma-method/ToLayerSet/ToProperties), [03] `TimberCapacity` EC5 + `ResidualSection` charring; mostly REALIZED
- `libs/csharp/Rasm.Materials/.planning/Connection/connection.md` (342L) — `ConnectionItem` `[Union]` owner over `ConnectionFamily {reinforcement/fastener/hanger/joint}` `[SmartEnum]`, `ConnectionId` `[ValueObject]`, `ConnectionFault` band-2360 `[Union]`, `ConnectionSection` `[Union]` (IfcEntity/PredefinedToken/NominalMm), `ConnectionCatalogue.Build`; [03] `ConnectionProjector` `IElementProjection` authoring `Rasm_ConnectionRealization` detail bag (authors NO Object node)
- `libs/csharp/Rasm.Materials/.planning/Connection/reinforcement.md` (365L) — RebarStandard/RebarUsage/RebarSurface/RebarGrade/BarSize/RebarHook `[SmartEnum]`, RebarBend/RebarSection, [03] `RcSection` host-neutral RC assembler over `VividOrange.Sections` ConcreteSection, `RebarLayout` `[Union]` (4 layout engines), `EnGrade` admission boundary, `RcSectionBuilder.Of` (family-agnostic, admits grouted CMU)
- `libs/csharp/Rasm.Materials/.planning/Connection/fastener.md` (331L) — FastenerKind(anchor folded in)/FastenerStandard/FastenerGrade/BoltCategory/FayingSurface/ThreadSeries/ThreadSize `[SmartEnum]`, `FastenerSection` shear/bearing/proof capacity, [03] `FastenerAssembly` preload/slip-resistance (Fp,C=0.7fub.As)
- `libs/csharp/Rasm.Materials/.planning/Connection/hanger.md` (306L) — HangerType/SteelGauge/HangerInstall/LoadDuration `[SmartEnum]`, `LoadResistance` `[ComplexValueObject]`, HangerSection/HangerPlate/HangerCapacity, GovernedCapacity/DemandCapacityRatio; the page scope [5] renames hanger->connector
- `libs/csharp/Rasm.Materials/.planning/Connection/joint.md` (237L) — JointKind/WeldType/ElectrodeClass/AdhesiveClass/StudClass `[SmartEnum]`, `JointSection` `[Union]` (Weld/Adhesive/Stud), DesignShearKn AWS D1.1, `ConnectionCatalogue.BuildJointRows`; `StudClass` is `steel#Composite`'s shear-stud source; the `JointSection`/NominalMm disambiguation site

### Gaps / Defects
- CORE COLLAPSE (scope [1].3/[5]): two parallel owners model ONE concept — `Profile` over `ProfileFamily {masonry/cmu/steel/timber/glazing}` (`profile.md`) + `ConnectionItem` over `ConnectionFamily {reinforcement/fastener/hanger/joint}` (`connection.md`), with two fault bands (`ProfileFault` 2300, `ConnectionFault` 2360), two catalogues (`ProfileCatalogue`, `ConnectionCatalogue`), two `Of` entries, two `ConnectionSection`/`ProfileUnit` shapes. Must collapse into ONE `Component` owner over a primary/minor discriminant (primary space-bounding `IfcBuiltElement` beam/wall/plate vs minor standardized `IfcElementComponent` bolt/rebar/stud/brick/CMU/IGU). Cross-section is a `Component` FIELD, never a peer; the `ParametricSection` machinery + the `ComputedSection` capacity rail become `Component`-internal.
- CORE COLLAPSE (scope [1].2/.3): no ONE `Component` projection. Profiles has NO projector (relies on `Construction/Projection`); `connection.md` [03] `ConnectionProjector` authors ONLY the occurrence detail bag and explicitly 'authors NO Object node, mints NO element identity'. The unified `Component` projection must MINT the deterministic-rooted Type Object, stamp Classification/PredefinedType (neutral token), lower the complete parametric data, AND bind occurrences via `Assign.TypeDefinition` — one `Project` fold discriminating primary/minor + type/occurrence. The `MaterialProjector` + `ConnectionProjector` collapse into this.
- CORE COLLAPSE (scope [1].1/.4): no Type/Occurrence model. `Profile`/`ConnectionItem` are catalogue ROWS, not Type Objects with `ObjectKind` in {Type,Occurrence}; no deterministic-rooted Type `NodeId` derived from `Component` canonical content (`Object.ToCanonicalBytes` excluding volatile Representations). No NAMED `Bake` type->occurrence inheritance with per-field precedence (single=occurrence-overrides-type, Seq=union+dedup-by-key) and no `TypeId` reference on the baked Element. `connection.md` uses `InheritanceMode.OccurrenceWins` on the PropertyBag only — scope [1].4 notes that is PropertyBag-only, NOT the named inheritance.
- RENAME (scope [5]): hanger->connector. Touches the whole `hanger.md` page (HangerType/HangerSection/HangerInstall/HangerPlate/HangerCapacity/HangerRow/BuildHangerRows + `hanger#HANGER_FAMILY`/`#IFC_DISCRETE_ACCESSORY_WIRE` anchors), `connection.md` (`ConnectionFamily.Hanger` line 58, `ConnectionSection.Hanger` line 101, the `hanger:` switch arms in Family/NominalMm/IfcEntity/PredefinedToken lines 107/114/129/148 + the projector Detail hanger arm lines 303-308), and the ARCHITECTURE/README codemap (`Hanger.cs`, hanger family).
- RENAME (scope [5]): `JointSection`/NominalMm disambiguation. `joint.md` `JointSection` `[Union]` (line 128) and `connection.md` `ConnectionSection.NominalMm` polymorphic dispatch (line 111, throat/bond-line/stud-diameter for joint vs diameter/thread/carried-width for the others) collide under the `Component` section-field naming; the exact target is WF-1's DECIDE ~428-anchor map.
- RENAME + DATA (scope [5]/[2]): cmu `WebThicknessMm`->`CrossWebMm`/`EndWebMm` split. `cmu.md` `CmuSection.WebThicknessMm` (line 122) is a single uniform web thickness; ASTM C90 distinguishes end-web from cross-web. The CellVoids/CellAreaMm2 math (`cmu.md` 139-164) assumes the uniform single web — both the field and the generative geometry must split.
- RENAME + DATA (scope [5]/[2]): cmu `GroutFraction`->`GroutedCellFraction`. `cmu.md` `CmuSection.GroutFraction` (UnitInterval, line 125) is a single homogeneous scalar smear; scope [2] wants per-cell grout boolean + rebar-cell position. The CellVoids homogeneous void-shrink by sqrt(1-f) (line 161) is the area-honest approximation the per-cell boolean replaces.
- COUPLING (scope [5]): `Coring` cross-family leak. Coring/CoringClass are DEFINED in `masonry.md` (`Rasm.Materials.Profiles.Masonry`, lines 52-70) but consumed by EVERY family — `profile.md` imports it for the `Profile` record field (line 34), `cmu.md` `ToCoring()` maps to masonry rows incl. the CMU-specific Hollow2Cell/Hollow3Cell (`cmu.md` 170-175, `masonry.md` 60-61), and steel/timber/glazing all pass `Coring.None`. masonry OWNS a concept the whole axis needs; the `Component` collapse needs a clean owner.
- GENERATIVE DATA (scope [2] CMU): incomplete. Missing per-cell positions/shapes (CellVoids infers uniform equal cells, `cmu.md` 150-164), cell taper/draft + face-shell flare, special units (bond-beam/knockout/channel/lintel/sash/control-joint/open-end — CmuGrade only has hollow/solid x load-bearing), finish geometry (split-depth/score-count/rib — splitface is just the row label cmu.8in-splitface), 3D head joint. FIX: `FireRatingHours()` (`cmu.md` 202) is LINEAR (`Math.Clamp(EquivalentThicknessMm*RatingFactor,0,4)`) — scope wants power-law; grout density hardcoded 2240.0 kg/m3 in `SelfWeightKnPerM2()` (`cmu.md` 211).
- GENERATIVE DATA (scope [2] Brick/masonry): incomplete. Missing frog geometry depth/taper/single-vs-double/pocket (`Coring.Frog` is just voidFraction:0.10), core/perforation hole count/positions/diameter (Coring is scalar void-fraction bands — 'replace scalar void-fraction bands'), per-unit packing transforms (BondGeometry emits PerUnitRotationDegrees but not full basketweave/pinwheel/diaper transforms), cut-plane position/orientation (Cut is lengthFraction+bevelDegrees scalars), voussoir taper (`SpecialShape.Voussoir` has all-zero modifier columns, `masonry.md` 201), actual-vs-work size + tolerance, mortar 3D recess profile (`MortarProfile.DepthFactor`+RecessDepthMm is scalar depth).
- GENERATIVE DATA (scope [2] Glazing): incomplete. FIX the hardcoded 1200mm placeholder (`glazing.md` FrameRebateModuleMm=1200.0 line 241, used for height/length/course in ToUnit); missing spacer geometry (SpacerType is just PsiWmK scalar — no width/depth/edge-seal/desiccant/corner-keys), interlayer TYPE PVB/SGP/EVA + multi-ply (`Pane.InterlayerMm` is just a double), gas fill %/mixture (Cavity is single CavityGas+width), per-pane spectral reflectance/absorptance (SolarOptical is series-transmittance product only, `glazing.md` 348), frame/muntin/grid, VIG. FIX hardcodes: specificHeat 840.0 (line 215), GlassGwpPerKg 1.20 (line 399), `FireResistance.Ei(30)` (line 224), recycledContent 0.25/endOfLifeRecovery 0.90 (line 220), GlassDensityKgM3 2500.0 (line 363). FIX NaN-sentinel emissivity -> Option: `Coating.None` correctedEmissivity:double.NaN (line 78) guarded by EmissivityOf but still a NaN sentinel.
- GENERATIVE DATA (scope [2] Fastener): incomplete. Missing thread form alpha=60 deg, P/d2/d3/D1, tolerance class 6g/6H (ThreadSize has major/pitch/minor/across-flats + derived PitchDiameterMm/TensileStressAreaMm2 only); head e/k-height/b/u thread-length+run-out (only AcrossFlatsMm s — no head height k); nut s/m (`FastenerKind.Nut` is a bare kind, no m height); washer d1/d2/t + HV (absent — 'washer a project property', `fastener.md` 17); shank-vs-thread length (ShankLengthMm is single). `FastenerAssembly` has grip-plies/shear-planes/withWasher bool but not the full bolt+nut+washer geometry assembly.
- GENERATIVE DATA (scope [2] Rebar): incomplete. Missing rib geometry — rib height h, spacing c, longitudinal-rib height, flank angle, relative-rib-area fR (the bond parameter): RebarSurface is just Textured/Plain token (`reinforcement.md` 81-87), NO rib geometry. The bend/hook schedule is partial — RebarBend + RebarHook cover 90/135/180 and shape codes 13/14/15/51 (`reinforcement.md` 155-205), but the full stirrup/tie bend schedule and the wider BS 8666 shape-code set are absent.
- GENERATIVE DATA (scope [2] Headed stud): incomplete. `joint.md` `StudClass` (lines 104-122) has DiameterMm/StandardHeightMm/UltimateMpa only — missing head dia d5 + thickness h3, underhead neck h4, length-before-weld l1 + weld burn-off -> as-welded length (both exposed). ISO 13918 Type SD geometry absent.
- GENERATIVE DATA (scope [2] Weld/joint): incomplete. `WeldType.EffectiveThroat` (`joint.md` 65-72) is a scalar throat — missing weld bead profile (face concave/convex/flat, reinforcement, toe/root), groove prep (root opening/face, bevel V/U/J angle, backing bar — Groove/Butt are just throat=depth), plug-weld hole geometry (LegMm as hole diameter only), adhesive fillet/bite (AdhesiveClass is lap-shear/peel/service scalars).
- DUAL-PARADIGM COUPLING: kernel value-object namespace contradiction. Profiles pages import `PositiveMagnitude`/`Dimension` from `Rasm.Domain` (`profile.md` 26, `cmu.md` 21, `capacity.md` 21, `steel.md` 25, `timber.md` 22), while ALL Connection pages import them from `Rasm.Vectors` with an explicit 'the kernel value-object atoms live in `Rasm.Vectors`, NOT `Rasm.Domain`' comment (`connection.md` 27, `joint.md` 24, `fastener.md` 22, `reinforcement.md` 22, `hanger.md` 24). One is a phantom import; the `Component` collapse MUST resolve to one namespace (verify via `assay api` — verified-local wins).
- COLLISION (Component-merge): `LoadDuration` name collision. `Profiles/timber.md` defines `LoadDuration` (EC5 k_mod, sc1/sc2/sc3 columns, `Rasm.Materials.Profiles.Timber`, `timber.md` 132-142) AND `Connection/hanger.md` defines `LoadDuration` (NDS Cd, permanent/ten-year/etc., `Rasm.Materials.Connection.Hanger`, `hanger.md` 99-109). `hanger.md` 33-34 notes they are 'distinct ... separate namespaces'; merging Profiles+Connection into one `Component/` sub-domain collides them — a rename in the ~428-anchor map.
- DANGLING ANCHORS (seam-mirror endpoint, WF-1 lands the Materials side only): consumer pages reference `Profiles/profile#PROFILE_RESOLUTION` / `#PROFILE_OWNER` — `Construction/assembly.md` (lines 7/130/134/166), `Construction/layout.md` (3/15), `Projection/material.md` (3/14/17/98/223/224/233/290/331), `Properties/properties.md` references `connection#CONNECTION_OWNER` (105/206). Under the `Component` rename these become dangling; the SEAM rows at the Materials `ARCHITECTURE.md` `[2]-[SEAMS]` endpoint (`Profiles/profile`, `Profiles/capacity`, `Connection`, `Connection/reinforcement` rows, lines 61-81) are what WF-1 must update. The consumer BODY re-binds are WF-2/WF-3.
- POSSIBLE COVERAGE GAP (verify vs `api-vividorange-profiles-catalogue.md`): `steel.md` `SteelClass.OfShape(EuropeanShape)` (lines 93-100) maps ONLY the H/I families -> IShape and channel families -> UShape, with `_ => ProfileFault.Family`. The American fold (lines 80-89) covers L/DoubleL/HSS/Pipe/WT/MT/ST, but the European fold has NO angle/hollow/tee/double-angle mapping — a European angle or RHS/CHS section would rail Family. Confirm the `EuropeanShape` enum's full member set against the `.api` catalog before the collapse.
- SCOPED `.api` CITATION: per scope [7] each page cites ONLY the catalogs its concept composes, never noise. The folder tier has 19 catalogs at `Rasm.Materials/.api/`. `api-vividorange-uncertainties.md` is admitted (README) but composed by Properties, not by any Profiles/Connection page; the `Component` page set must cite only the VividOrange family it composes (Profiles.Catalogue/Sections/SectionProperties/Materials/Standards/InteractionDiagram/IForceMomentInteraction/ForceMomentInteraction/Serialization) + MathNet (capacity/acquisition), MessagePack/UnitsNet/Unicolour substrate as relevant — plus the shared `libs/csharp/.api/` substrate tier.

### Anchors to Preserve
- FROZEN (scope TASK + [5]): `AppearanceKey` / `AppearanceSummary.Of` with tolerance 0.0 (full IEEE precision, the no-quantization dedup invariant) — a FROZEN invariant the rebuild must never perturb; anchor-only, no signature touch.
- FROZEN (scope [5]): the 5 Appearance cross-page anchors INTO the collapsing pages get ANCHOR-ONLY fixups (the `page#ANCHOR` target changes when `Profiles`/`Connection`->`Component`): `Appearance/bsdf.md:3` -> `Profiles/profile`; `Appearance/graph.md:250` -> `Connection/connection`; `Appearance/graph.md:250` -> `Connection/joint`; `Appearance/graph.md:254` -> `Profiles/glazing`; `Appearance/graph.md:254` -> `Profiles/masonry`.
- FROZEN (scope [5]): `Profile.AppearanceId` / `ConnectionItem.AppearanceId` — the stable `MaterialId` column (`profile.md` 174, `connection.md` 157); Appearance is OUT OF SCOPE for signatures, reconcile at the owner-agnostic `AppearanceKey`.
- SEAM-CANONICAL (scope [5], rename STOPS at the Materials boundary): `ProfileRef` / `ProfileSet` / `ComputedSection` stay seam-canonical — the `MaterialComposition.ProfileSet(ProfileRef)` case + the M7 resolution spanning Bim/Compute/python/ts. `Component` composes them UNCHANGED. The 17-field `ComputedSection` receipt (`profile.md` 201-218, Area/Ix/Iy/Sx/Sy/Rx/Ry/Zx/Zy/J/Iw/Avy/Avz/Depth/Width/HeatedPerimeter/AxisDistance with Iw 5th at the seam) every family fills identically.
- SEAM-CANONICAL: the [M7] `ProfileResolution` one-hop (`profile.md` [03], Build/Resolve frozen cache, `ProfileRef->ResolvedProfile(Profile, Option<ComputedSection>)`) so a structural consumer never re-runs the section integral per call; the per-family `ProfileId->ComputedSection` section maps (SteelSections/CmuSections/TimberSections) the join reads.
- PRESERVE (mostly-REALIZED, do not regress): `SectionCapacity` `[Union]` 5-case + Demand/Utilisation/GoverningAction unified rail (`capacity.md`), the VividOrange `InteractionDiagram`/`IForceMomentMesh` eager-solve composition + Moller-Trumbore `CapacityRay`; the `SteelStiffness` Zx/Zy/J/Iw/Avy/Avz closed forms (`steel.md`, the scope [2] 'steel sections complete' item already done); the timber EC5 `TimberCapacity` + gamma-method `EffectiveStiffness` + `ResidualSection` charring (`timber.md`, scope [2] timber done).
- PRESERVE: `RcSection` / `IConcreteSection` host-neutral RC assembler (`reinforcement.md` [03]) — `RcSectionBuilder.Of` is FAMILY-AGNOSTIC (admits any `Profile` incl. a grouted-CMU outline for reinforced masonry); `RebarLayout` `[Union]` collapsing the 4 VividOrange layout engines; `EnGrade` boundary trapping VividOrange throws onto the typed fault rail.
- PRESERVE: the `Rasm_ConnectionRealization` detail-bag round-trip contract with `Rasm.Bim` `Semantics/connection#CONNECTION_DETAIL` (`connection.md` [03] — set name, row vocabulary JointType/FastenerType/AccessoryType/BarType/NominalDiameter/etc., OccurrenceWins, JointType allowed-set, dimension-only `MeasureValue.OfSi`, `NodeId.Content` content-key dedup). The projector authoring-NO-Object-node behavior CHANGES under the `Component` Type/Occurrence collapse, but the Bim detail-bag round-trip shape is preserved.
- PRESERVE: `StudClass.SteelShearKn` (`joint.md` 121) — the ONE shear-stud vocabulary `steel#STEEL_FAMILY` Composite reads for the ΣQn cap; never a parallel stud owner.
- PRESERVE: the verified IFC enum tokens — `IfcReinforcingBarTypeEnum` {MAIN/LIGATURE/SPACEBAR/PUNCHING/EDGE/RING/ANCHORING/SHEAR/STUD/...}, `IfcMechanicalFastenerTypeEnum` {ANCHORBOLT/BOLT/COUPLER/DOWEL/NAIL/NAILPLATE/RIVET/SCREW/SHEARCONNECTOR/STAPLE/STUDSHEARCONNECTOR/...} (no NUT member), `IfcFastenerTypeEnum` {GLUE/MORTAR/WELD/...}, `IfcDiscreteAccessoryTypeEnum` {SHOE/BRACKET/ANCHORPLATE}, `IfcReinforcingBarSurfaceEnum` {PLAIN/TEXTURED} — assay/GeometryGym-verified members the egress tokens carry; never introduce an unverified member.
- PRESERVE: the fault-band disjointness law (`ProfileFault` 2300, `ConnectionFault` 2360, both disjoint from kernel `GeometryFault` 2400, `ConstructionFault` 2350, `MaterialFault` 2450, `ProjectionFault` 2470). The two bands MERGE under the one `Component` owner, but the Expected-derived-bare-lift discipline + disjoint-from-kernel-2400 invariant preserves.
- PRESERVE: the VividOrange typed-designation catalogue grounding (steel `SectionReader.Read` over `CatalogueFactory.CreateAmerican/European`; RC `ConcreteSection`; capacity `InteractionDiagram`; EnSteelFactory/EnConcreteFactory/EnRebarFactory grade DATA) — never hand-keyed section-property or grade literals (scope [4] `CATALOG_LAW`).

## [ELEMENT-SEAM]

The `Rasm.Element` seam touchpoints for the unified Material/Component/Element rebuild (WF-1, the atomic Element+Materials concern). The five neutral-seam pages the unified paradigm ([1]) and the rename+boundary law ([5]) re-bind, plus the Element-endpoint `ARCHITECTURE.md` `[2]-[SEAMS]` that WF-1 must land the seam-mirror rows into.

### Pages
- `libs/csharp/Rasm.Element/.planning/Graph/element.md` (578L) — `NodeId` identity owner, `Node` `[Union]` 7-case property-graph vocabulary, `ToCanonicalBytes` canonical projection, Header, frozen `ElementGraph` + incidence index + QuikGraph view, memoized `Bake` fold, the `MaterialBinding` record + SectionOf/CompositionOf/MaterialsOf/PropertiesOf read accessors, `ObjectKind{Occurrence,Type}`, `AppearanceSummary`+`Of` (FROZEN)
- `libs/csharp/Rasm.Element/.planning/Relations/relation.md` (291L) — `Relationship` `[Union]` neutral 5-kind edge algebra + Generic passthrough, RelationshipKind/ComposeKind/AssignKind/ConnectKind/VoidKind/CardinalPoint sub-kind vocabularies, `MaterialUsage` occurrence payload on the Associate edge, the uniform accessor surface (Kind/Endpoints/Members/DirectedPairs/IsContainment/Touches/CanonicalBytes/Remap)
- `libs/csharp/Rasm.Element/.planning/Properties/property.md` (259L) — `PropertyValue` `[Union]` 10-case typed IFC-value family, `PropertyName` `[ValueObject<string>]` key, `ValueBag<V>` with PropertyBag/QuantityBag global-using aliases, `InheritanceMode` `[SmartEnum]` owning the generic `Resolve<V>` type->occurrence precedence fold the bag Merge delegates to
- `libs/csharp/Rasm.Element/.planning/Composition/material.md` (713L) — `MaterialId`, `MaterialComposition` `[Union]` (Single/LayerSet/ProfileSet/ConstituentSet) + `ProfileRef` + the M7-baked neutral `SectionProperties` receipt (WithSection), `MaterialPropertySet` `[Union]` (Mechanical/Orthotropic/Thermal/Acoustic/Fire/Environmental/Cost) keyed to Discipline, the EN15804+A2 impact matrix, `MaterialPropertyAccess` generic `Property<T>`
- `libs/csharp/Rasm.Element/.planning/Projection/projection.md` (147L) — `IElementProjection` projector floor (one `Project->Fin<GraphDelta>`), `IGraphConstraint` legality floor (one `Validate->Validation`), `ProjectionContext` (ElementIds + Header + neutral primitives + Owns vouch predicate), the `ProjectionAssembly.Assemble` composition capability (applicative capture, monoid Header floor, AdmitOnto, Constrain)
- ENDPOINT (seam-mirror landing target, in-scope per the cross-workflow contract): `libs/csharp/Rasm.Element/ARCHITECTURE.md` `[2]-[SEAMS]` (lines 37-62) — currently carries ZERO unified-paradigm rows

### Gaps / Defects
- [1].1 DETERMINISTIC TYPE `NodeId` ABSENT (`element.md`). `ObjectKind{Occurrence,Type}` statics exist (lines 76-80) but the deterministic Type-Object rooted-id derivation does not: `NodeId.Rooted()` mints Guid-v7 for ALL rooted nodes (line 39) and the prose asserts 'the rooted Object mints its id once at authoring (Guid v7)' (line 15) — the pre-unification regime. There is NO Type-seed mint entry and NO `ToCanonicalBytes` variant that EXCLUDES the volatile Representations for the Type seed (the Object arm at line 292 hashes Representations unconditionally), so identical Components cannot dedup to one Type and a later geometry attach would re-key the type. WF-1 must add the principled Type-deterministic `NodeId` derivation to the one identity owner.
- [1].4 NAMED `Bake` TYPE->OCCURRENCE INHERITANCE ABSENT (`element.md`, major). `BakeObject` (lines 511-536) folds ONLY the occurrence's own edges and `TypeBagsOf` (lines 539-548) inherits ONLY property/quantity bags via `PropertyBag.Merge`. Type-level materials, assessments, and classifications are NOT inherited onto the occurrence; the `Element` record (lines 382-393) carries NO `TypeId` field (so the generator cannot recover which `Component` a piece inherits); and SectionOf/CompositionOf/MaterialsOf/PropertiesOf (lines 475-493) resolve ONLY occurrence-direct Associate edges (Subject==member), so an occurrence whose material/section rides its `Component` Type returns empty. WF-1 must add the NAMED inheritance dimension (single fields = occurrence-overrides-type; Seq fields materials/assessments/classifications = union+dedup-by-key) — NOT an `InheritanceMode` extension — surface `TypeId` on Element, and type-resolve the accessors WITHOUT breaking Compute's Op-free `graph.SectionOf(member)`.
- [1].6 `MaterialBinding` COLLISION UNRESOLVED (`element.md`). `element.md` declares `MaterialBinding` (record struct, line 379), uses it as `Element.Materials` `Seq<MaterialBinding>` (line 385) and in the Bake Associate-material arm (line 524) and [03] Owner prose (line 352). Per [1].6 the Element-side baked accessor must rename to `BakedMaterial` (`MaterialBinding` reserved for the Materials projection), and a NEW `TypeBinding` (the type->occurrence bind) must be added. Not yet done.
- [1].5 NEUTRAL DETAIL SCHEMA + CANONICAL `PropertyName` VOCABULARY ABSENT (`property.md`, spanning `relation.md` + `projection.md`). `PropertyName` is a bare `[ValueObject<string>]` (lines 39-43) with NO closed canonical neutral-name vocabulary, and there is NO single seam-declared neutral detail schema unifying the realizing-detail bag and the material-property->Pset column shape over PropertyBag. The Materials endpoint still spells the IFC name `Rasm_ConnectionRealization` (Materials `ARCHITECTURE.md` lines 20, 73) — Element must own ONE neutral schema carrying no `Pset_*`/`Rasm_ConnectionRealization` IFC names, both the projector (author) and Bim (egress/reader) targeting it.
- [1].2 OWNER-MINTS-ITS-IDENTITY PROJECTION LAW PARTIAL (`projection.md`). The seam contract supports only `NodeId.Rooted()` Guid-v7 minting by a PRIMARY projector (lines 14, 18, 38-39); there is no contract for the `Component` projection minting the DETERMINISTIC-rooted Type Object (the [1].2 owner-mints law replacing the 'minter stamps the aspect projector's egress' convention). Coupled to the [1].1 `element.md` gap — the deterministic Type mint entry it would compose does not yet exist on `NodeId`.
- SEAM-MIRROR ROWS NOT LANDED AT THE ELEMENT ENDPOINT (`ARCHITECTURE.md` `[2]-[SEAMS]`, lines 37-62). The seams block carries NO unified-paradigm rows: no Component-projection-mints-deterministic-Type-Object, no owner-mints-its-identity law, no TypeBinding/BakedMaterial, no named type->occurrence `Bake` inheritance, no neutral detail schema. The cross-workflow contract requires WF-1 to land the seam-mirror rows at BOTH endpoints (Element + Materials `ARCHITECTURE.md` `[2]-[SEAMS]`); the Materials endpoint likewise lacks them.
- PHANTOM/COHERENCE RISK: `material.md` `SectionProperties` mints `QuantityType` discriminators OUTSIDE the UnitsNet registry (lines 64-66: `QuantityType.Create("SectionModulus")`/"SecondMomentOfArea"/"TorsionConstant"/"WarpingConstant"), contradicting `quantity.md`'s contract that 'the discriminator IS the UnitsNet QuantityInfo.Name' (`quantity.md` line 3) — UnitsNet has `AreaMomentOfInertia` but no SectionModulus/WarpingConstant/TorsionConstant quantity. Either `quantity.md` must explicitly sanction free-form non-UnitsNet `QuantityType.Create` tokens for engineering quantities UnitsNet lacks, or `material.md` cites phantom discriminators. Also unconfirmed: `Dimension.Create(int x7)` (`material.md` lines 65-66) — `quantity.md` exposes `QuantityType.OfDimension` (line 96) and the `*Dim` statics but no 7-int `Dimension.Create` was located; verify both via `assay api` against the restored assembly before the build relies on them.
- OUT-OF-SCOPE-FOR-THESE-PAGES, NOTED: [1].3 (Profiles+Connection collapse into ONE `Component` owner/projection, MaterialProjector+ConnectionProjector -> one `Component` projection) and the [5] symbol renames (hanger->connector, cmu `WebThicknessMm`->`CrossWebMm`/`EndWebMm`, `GroutFraction`->`GroutedCellFraction`, `Coring` leak) are MATERIALS-side bodies (WF-2/WF-3): the Materials `.planning` STILL has separate `Profiles/` and `Connection/` folders with `hanger.md` present. The [2] generative-data scalar-smears (CMU/brick/IGU/fastener/rebar/stud/weld) live in Materials Profiles/Connection, NOT in these five neutral-seam pages — the seam pages carry no parametric generative data. Element's `.api/` folder tier is empty BY DESIGN (README [02]: admits no domain packages; composes only the substrate `libs/csharp/.api/` tier).
- CAPTURED WATCH RESIDUALS (cross-PACKAGE prose anchors that WF-2/WF-3 will rename, resolve ONLY if WF-1 touches their site): `material.md` references `Rasm.Materials` `timber#TIMBER_FAMILY` (the Orthotropic consumer); `projection.md` references `Rasm.Materials/Projection/material#MATERIAL_PROJECTOR` and `Rasm.Bim/Projection/semantic#GRAPH_LEGALITY`. Under [5] the Materials `Profiles/*` collapses to `Component/` and Bim `Projection/semantic.md` splits into semantic/relations/egress, so these anchors will dangle after the sibling bodies re-bind — WF-2/WF-3 work, not WF-1.

### Anchors to Preserve
- FROZEN INVARIANT (never perturb signature): `AppearanceSummary` record + `AppearanceSummary.Of(r,g,b,metallic,roughness,opacity,transmissive,tolerance)` at `element.md` [02] lines 201-214 — tolerance 0.0, full IEEE precision (hashes raw IEEE bits, not Header-quantized), the ONE seam-owned appearance content-key factory both the Materials `MaterialWire.Summary` and the Bim `AppearanceProjection` compose. The `AppearanceKey` field and Opacity+Transmissive are load-bearing in the key.
- The 5 Appearance cross-page anchors in the in-scope pages get ANCHOR-ONLY fixups (Appearance is 9-page out-of-scope for signatures): `Node.Appearance` case (`element.md` line 278), `Element.Appearance` `Option<AppearanceSummary>` field (line 389), the Bake Associate->Appearance arm (line 525), the appearance `ToCanonicalBytes` arm `w.U128(a.Summary.AppearanceKey)` (line 297), and the `AppearanceSummary` content-key factory itself.
- SEAM-CANONICAL (semantic-rename STOPS at the Materials folder boundary): `ProfileRef` (`material.md` lines 45-52), `MaterialComposition.ProfileSet` case (lines 135-143), and the neutral `SectionProperties` receipt + the M7 one-hop `ProfileRef->SectionProperties` WithSection bake (lines 84-103, 181-182). The Materials `ComputedSection` capacity rail resolves ONTO this seam `SectionProperties`; both names coexist (seam owns ProfileRef/ProfileSet/SectionProperties, Materials owns ComputedSection).
- The Op-FREE `graph.SectionOf(member)` (`element.md` line 492, no Bake, no Op key) — `Rasm.Compute` reads it directly off the seam; the [1].4 accessor type-resolution rework must NOT break this signature or its Op-free contract.
- `ObjectKind{Occurrence,Type}` `[SmartEnum<string>]` statics (`element.md` lines 76-80) — REUSE, never re-create a second identity scheme; the [1].1 deterministic Type derivation composes THIS token.
- The ONE `ToCanonicalBytes` canonical projection (id-excluded, tolerance-quantized, IEEE-754-LE, explicit attribute order) + the kernel `XxHash128` seed-zero content identity shared by `NodeId.Content`, `ContentAddress.Of`/`OfGraph`, and the cross-runtime `MATERIAL_LAYER_GOLDEN` parity corpus — the unified rebuild must keep node/edge content keys byte-stable across C#/Python/TypeScript (the Type-seed Representations exclusion is the ONLY sanctioned change to the Object arm).
- The clean cross-page cluster-anchor set: every `page#CLUSTER` reference the five in-scope pages make resolves today to a real `## [..]` header on its owner page (GRAPH_DELTA, CANONICAL_WRITER, CONTENT_ADDRESS, FAULT_BAND, MEASURE_VALUE, DISCIPLINE_AXIS, CLASSIFICATION_AXIS, ACOUSTIC_FOLDS, ASSESSMENT_NODE, COVERAGE_NODE, GEO_REFERENCE, NODE_MODEL, ELEMENT_GRAPH, EDGE_ALGEBRA, PROPERTY_VALUE, PROPERTY_BAG, MATERIAL_COMPOSITION, MATERIAL_PROPERTY, PROJECTION_CONTRACT, GRAPH_CONSTRAINT). These must stay resolving through the rebuild. Verified-real cross-page members: `MeasureValue.OfSi(QuantityType,Dimension,double)`+`OfSi(Dimension,double)`+Sum+Zero+Si+CanonicalUnit+Type, `ContentAddress.Of`/`OfGraph`/`.Value`, all 6 `ElementFault` cases, `GraphDelta.Empty`/Merge/Reheader/AdmitOnto/ReplayOnto/Put/Link/LegalLink/AddedEdges, `Discipline` statics, the Acoustic carrier reads, `AssessmentPayload`, `CoverageGrid.CanonicalBytes`, `GeoReference.Identity`/CanonicalBytes/Epsg, and `Classification.Edition` (a string identity member, distinct from `EditionDate:Option<LocalDate>`) — so `element.md`'s `w.String(o.Classification.Edition)` is type-correct, not a defect.
- The NEUTRAL edge algebra boundary (`relation.md`): `Relationship` closed at 5 neutral kinds (Compose/Assign/Associate/Connect/Void) + Generic passthrough, NO `IfcRel*` roster on the seam; the Associate edge carries the `MaterialUsage` occurrence payload and the LegalLink Material/Appearance/Coverage closure. The unified paradigm must not re-open the IFC strata the Classification collapse closed.
- The two-interface count (`projection.md`): net-new Rasm interfaces = 2 (`IElementProjection` + `IGraphConstraint`), no instance default-interface-member, no per-provider seam method; the Assemble shape is applicative capture (independent projectors accumulate faults) then monadic admit-then-constrain tail; `ProjectionContext.Header` is the load-bearing monoid floor and `ctx.Owns` is a PREDICATE (skip-vs-rail policy is the projector's, never a silent-drop seam helper).

## [PROPERTIES-PROJECTION-CONSTRUCTION]

Materials `Properties/*` + `Projection/material.md` + `Construction/*` under `libs/csharp/Rasm.Materials/.planning`.

### Pages
- `libs/csharp/Rasm.Materials/.planning/Properties/properties.md`
- `libs/csharp/Rasm.Materials/.planning/Properties/sustainability.md`
- `libs/csharp/Rasm.Materials/.planning/Projection/material.md`
- `libs/csharp/Rasm.Materials/.planning/Construction/assembly.md`
- `libs/csharp/Rasm.Materials/.planning/Construction/layout.md`
- `libs/csharp/Rasm.Materials/.planning/Construction/nesting.md`

### Gaps / Defects
- [STRUCTURAL, [1].3] `Projection/material.md` is a standalone `MaterialProjector:IElementProjection` — the dual-projector paradigm [1] cures. WF-1 collapses `MaterialProjector` + the Connection `ConnectionProjector` into ONE `Component` projection whose single `Project` fold discriminates primary/minor (`IfcBuiltElement` vs `IfcElementComponent`) + type/occurrence. `material.md` is one half of the dual coupling and is rebuilt as the unified `Component` projection page.
- [STRUCTURAL, [1].1/[1].2] `material.md`'s core stance (line 3, [02], [H12]) 'authors NO Object node, mints NO element identity' INVERTS: the `Component` projection must mint the deterministic-rooted Type Object (`ObjectKind.Type`, `NodeId` from `Component` canonical content with `Object.ToCanonicalBytes` excluding Representations), stamp Classification/PredefinedType (neutral token; validity stays Bim's AdmitPredefined), lower complete parametric data, and bind occurrences via `Assign.TypeDefinition`. The seam is READY — `ObjectKind` exists at `Rasm.Element/Graph/element.md` (lines 7,13,77-79,254,258,383) and `Assign.TypeDefinition` at `Graph/element.md:354` + `Relations/relation.md:14` — so this is body work in `material.md`, not a seam-member gap.
- [STRUCTURAL/NEW, [1].6] `TypeBinding` (the type->occurrence bind) does NOT exist anywhere in the Element seam (grep over `Rasm.Element/.planning` = zero hits) — WF-1 must mint it. The `MaterialBinding` name-collision ([1].6) resolves to `BakedMaterial` (Element baked accessor) / `MaterialBinding` (Materials projection, the name `material.md` keeps) / `TypeBinding` (new). `material.md`'s `MaterialBinding` record (line 80) stays but the unified projection must add the `TypeBinding`/`Assign.TypeDefinition` pairing.
- [DANGLING ANCHORS, [5]] ~30 cross-area anchors into the collapsing Profiles/Connection pages must re-home to `Component`: `assembly.md` (`Profiles/profile#PROFILE_OWNER`, `#PROFILE_RESOLUTION`, `masonry#PROFILE_FAMILY`, `using Rasm.Materials.Profiles[.Masonry]`); `layout.md` (`profile#PROFILE_OWNER`, `masonry#PROFILE_FAMILY` x7, `using Rasm.Materials.Profiles` + `.Masonry`, Profile/ProfileFamily/Coring); `material.md` (`Profiles/profile#PROFILE_RESOLUTION` x7, `Rasm.Materials.Profiles[.Timber/.Glazing]`, CaptureTimber/CaptureGlazing/TimberSection/GlazingSection/MaterialShapeKind); `properties.md` (`Connection/connection#CONNECTION_OWNER` `ConnectionItem.CapacityKey`, the `metal.steel` alias at lines 105,206). `ProfileRef`/`ProfileSet`/`ComputedSection` STAY seam-canonical — the rename STOPS at the Materials folder boundary for those three.
- [OWNERSHIP SHIFT, [1].2] `sustainability.md`'s classification egress (`SustainabilityCatalogue.Classification` at lines 165-170, threaded onto `MaterialBinding` -> the bound element's Object node) conflicts with [1].2: 'the `Component` projection stamps its Classification'. Classification is a `Component`/Type concern; the egress must move from the per-occurrence `MaterialBinding` to the `Component`-Type projection. `material.md`'s `MATERIAL_CLASSIFICATION_EGRESS` ([04]) and the Capture/CaptureTimber/CaptureGlazing threading inherit this shift.
- [FROZEN-ANCHOR TENSION] `material.md` mints `Node.Appearance` via `NodeId.Content` over `ToCanonicalBytes(ctx.Header.Tolerance)` (lines 131, 175 — Mint uses the run tolerance). The FROZEN `AppearanceKey` dedup invariant + Materials ARCHITECTURE seam row (`Appearance/interchange`, tolerance 0.0, full IEEE precision, the no-quantization the Bim `AppearanceProjection` passes) require the Appearance content-key at tolerance 0.0. Minting the Appearance node at `Header.Tolerance` breaks cross-projector appearance dedup when two runs differ in tolerance. Anchor-only fixup at the Appearance touch-point.
- [NEUTRAL SCHEMA, [1].5] The Connection realizing-detail bag (`Rasm_ConnectionRealization`) + the material-property->Pset column shape become ONE neutral schema declared in Element over PropertyBag + the canonical `PropertyName` vocabulary (Element carries no `Pset_*`/`Rasm_ConnectionRealization` IFC names). `properties.md`'s `IFC_MATERIAL_PROPERTIES` ([04], `Pset_MaterialMechanical`/Thermal mapping) and `sustainability.md`'s `IFC_PSET_ENVIRONMENTAL` must target that single neutral schema; IFC names + egress mapping stay in Bim's `SemanticProjector`.
- [SCALAR SMEAR] `properties.md` `MaterialPropertyRow` carries a per-material `UValueWM2K` column (steel 5.88, concrete 11.5, 'insulation U via lambda/0.2m' at lines 47,103-172) lowered through the seam `OfThermal(conductivity, specificHeat, uValue, vapour)`. U-value is an ASSEMBLY property (the series-resistance ISO 6946 fold is relocated to Compute); the material physics is lambda (`ConductivityWMK`). A per-material U at an arbitrary 200mm reference is a scalar smear — the `OfThermal` uValue column should be reconsidered at the seam rebuild.
- [RENAME CONSUME-SIDE, [5]] `layout.md` `LayoutRun.Profile` + the heavy `run.Profile.Unit`/`.Standard`/`.Coring`/`.Family` reads (lines 167-181, 207-212, 250-251, 328) bind the `Profile` cross-section owner that becomes a `Component` FIELD under [1].3; the `Coring` cross-family leak ([5] names it explicitly) consumed in the arch/dome guard (line 207, `run.Profile.Coring == Coring.None`) must re-bind to WF-1's `Coring` resolution. `layout.md` is a consumer of the masonry vocabulary that collapses into `Component`.
- [.api DEFECT] `libs/csharp/Rasm.Materials/.api/api-rectanglebinpack-csharp.md` mis-attributes the consuming design owner as `Construction/layout` (lines 3, 18, 137-138) when the actual owner is `Construction/nesting` (`StockNest.Pack`). External members otherwise verify SOLID (all five packers, Rect X/Y/Width/Height/Area, the two distinct `FreeRectChoiceHeuristic` enums, GuillotineSplitHeuristic, LevelChoiceHeuristic, ShelfChoiceHeuristic, Insert overloads, FreeRectangles, MergeFreeRectangles, `SingleBinPack.Insert(w,h,qty)`, assembly id RectangleBinPacking) — no phantom in `nesting.md`'s `RectangleBinPack` usage.
- [CROSS-WORKFLOW, WF-3 — do not edit here] `nesting.md`'s Fabrication WIRE (`NestPlan` -> `Rasm.Fabrication` CuttingPlan/PlannedPlacement via app-stratum mediator, Nest.Honor, FabricationInput.Plan, PartTransform) and the Materials ARCHITECTURE seam rows `Construction/nesting`->`Fabrication/Nesting` and `Properties`->`Fabrication/Process` are WF-3 body re-binds per [5]. The Materials-side seam rows stay; the Fabrication bodies are out of WF-1 scope.
- [VERIFY, low-confidence] `sustainability.md` `SustainabilityRow.StageGwp` is a 6-element `double[]` in every row, while the model comment says 'seam `LifecycleStage.Count` entries' and `CarbonMatrix(row.StageGwp.AsMemory())` embeds it. The seam owns the (ImpactCategory x LifecycleStage) matrix (`Rasm.Element/Composition/material.md` lines 498-530); confirm `LifecycleStage.Count == 6` ({A1A3,A4,A5,B,C,D}) so the carbon-only-row arity matches — a seam that expands B1-B7/C1-C4 would break the embed.

### Anchors to Preserve
- FROZEN: `AppearanceSummary.Of` / `AppearanceKey` at tolerance 0.0 (full IEEE precision) — never perturb the signature; the dedup invariant the Bim `AppearanceProjection` shares. In this area the touch-point is `material.md`'s `MaterialWire.Summary` -> content-keyed `Node.Appearance` path. The 5 Appearance cross-page anchors that get anchor-only fixups live in the COLLAPSING Profiles/Connection pages (`Profile`/`ConnectionItem` carrying `MaterialId` `AppearanceId`), not in this area.
- `ProfileRef` / `ProfileSet` / `ComputedSection` STAY seam-canonical ([5]) — the `MaterialComposition.ProfileSet(ProfileRef)` case + the M7 one-hop `ProfileRef->ComputedSection` resolution (`graph.SectionOf`, no re-resolve per call). The semantic rename STOPS at the Materials folder boundary for these three. `assembly.md` `ProfileSet`->`ProfileRef.Of` and `material.md` BakeSection/SeamSection compose them unchanged.
- Disjoint fault bands: `ProfileFault` 2300 / `ConstructionFault` 2350 / `ConnectionFault` 2360 / kernel `GeometryFault` 2400 / `MaterialFault` 2450 / `ProjectionFault` 2470 / seam `ElementFault.ValueRejected` 2500. Properties+sustainability admission rail the seam `ElementFault` 2500 (NOT `MaterialFault` 2450 — the explicit cross-concern non-leak); Construction rails 2350 (path/joint/course/opening/nest only — composition admission is the seam's); Projection rails 2470. Each band is Expected-derived, `[SkipUnionOps]`, with the `…Case`-suffixed factory pattern.
- Seam-owned acoustic folds (Nrc/Saa/StcWeighted/Rw over the shared `RatingContour.Fit` contour kernel, `Rasm.Element/Composition/acoustic`) — READ off the Acoustic case, never re-authored in Materials; the assembly STC and material STC share one contour owner.
- Relocated-to-Compute aggregation: `AssemblyAggregator` (series-resistance U / mass-law layered-STC / rule-of-mixtures / worst-ply fire / GWP / cost) + AggregateEnvironmental/AggregateCost are `Rasm.Compute`'s; the seam carries the per-material `MaterialPropertySet` INPUT only. Materials authors NO Assessment node and NO assessment-input marshaller — Compute reads Material plies directly and writes `Assessment.Result` content-keyed on (input key, route).
- The ONE content-hash law: `NodeId.Content` over `ToCanonicalBytes` via the kernel seed-zero `XxHash128` (the single hasher; no second hasher in this folder); Material/Appearance are non-rooted content-addressed identities, idempotent under re-projection (no collision rail). The kernel/Python/TypeScript parity corpus pins byte-for-byte agreement.
- `IElementProjection` contract: one `Fin<GraphDelta> Project(ProjectionContext)` op, `TraverseM`->`GraphDelta.Merge` monoid fold (the exact Assemble shape, not a hand-threaded accumulator); the seam owns Assemble; a projector is one registration row. Plus the §4C source-capture inversion (catalogues are private projector fields) and [H12] vouched-element Associate edge — short-circuit on `spec.Bindings.IsEmpty`, NEVER `ctx.ElementIds.IsEmpty`; an unvouched binding rails `ProjectionFault.Unvouched`, never an invented Object node.
- REQUIRED-vs-OPTIONAL Lookup asymmetry: `MaterialPropertyCatalogue.Lookup` faults `ElementFault.ValueRejected` on an unregistered material (engineering properties REQUIRED); `SustainabilityCatalogue.Lookup` returns `Fin.Succ(empty)` for a material with no declared EPD (lifecycle OPTIONAL); `SustainabilityCatalogue.Classification` returns None when row/material absent.
- CO2e domain-basis invariant: `StageGwp` modules, RecycledContent/EndOfLifeRecovery, and Cost columns are raw domain scalars the seam guards (AllFinite / [0,1] / non-negative), NEVER MeasureValue/UnitsNet/MassUnit coerced (CO2e is a domain basis, not an SI dimension). Basis-native EPD: each row keeps its own `declared_unit` (PerKg/PerM2/PerM3/PerItem) — no forced PerM3 normalization at ingress; a negative A1-A3 is valid biogenic-sequestration carbon, never rejected.
- Seam owns `MaterialComposition` admission: `CompositionAuthor` is coerce-and-delegate, owning NO invariant the seam owns — empty-set/non-positive-thickness/fraction-range/fraction-normalization admission is the seam's `ElementFault.ValueRejected`, never a `ConstructionFault`. The layer thickness coerces once through the seam `MeasureValue.Of`; a duplicated pre-guard or a `ConstructionFault` for a composition miss is the named defect.
- Strata-acyclic disjoint nesting: `RectangleBinPack.CSharp` (Materials AABB rectangular cutting-stock YIELD — the in-folder pin) vs `Rasm.Fabrication` RectpackSharp/Clipper2 (true-shape irregular NEST); neither re-packs the other; the seam to Fabrication is a mediated [WIRE], never a direct Materials->Fabrication reference. AEC peers depend only UPWARD to {`Rasm`, `Rasm.Element`}.
- Single-engineering-object law + closed composition set: a material carries all disciplines as one `Seq<MaterialPropertySet>` over one `MaterialId` (never StructuralMaterial/ThermalMaterial/AcousticMaterial/EcoMaterial/CostMaterial/ClassifiedMaterial); the `MaterialComposition` vocabulary is the SEAM's closed set (Single/LayerSet/ProfileSet(ProfileRef)/ConstituentSet; `IfcMaterialList` deprecated, never admitted); the C7 `MaterialUsage` occurrence payload stays disjoint from the type-level composition.

## [ANCHORS_TO_PRESERVE_CONSOLIDATED]

The FROZEN invariant — `AppearanceSummary.Of(double r, double g, double b, double metallic, double roughness, double opacity, bool transmissive, double tolerance)` called with `tolerance: 0.0` — recurs across all four areas and is the single non-negotiable anchor.

### Appearance / AppearanceKey (FROZEN, all areas)
- FROZEN `AppearanceSummary.Of(...tolerance:0.0)` — SEAM-OWNED (`Rasm.Element/Graph/element#NODE_MODEL`, `element.md` [02] lines 201-214); mints `AppearanceKey` as kernel seed-zero `XxHash128` over canonical PBR bytes at full IEEE precision (no Header-quantization, hashes raw IEEE bits). `interchange.md:119`, `ARCHITECTURE.md:64`. NEVER perturb the signature, the tolerance, or the hasher.
- FROZEN `AppearanceSummary` record shape `(UInt128 AppearanceKey, double BaseColorR, double BaseColorG, double BaseColorB, double Metallic, double Roughness, double Opacity, bool Transmissive)` — `Transmissive` distinct from `Opacity`/alpha; `AppearanceKey`+Opacity+Transmissive load-bearing in the key. `interchange.md:480`. Materials composes, never re-declares.
- FROZEN `MaterialWire.Summary(MaterialParameters parameters) -> AppearanceSummary` lowering composing the seam factory; the CONTRACTED `projection#MATERIAL_SUBGRAPH` `MaterialSubgraph.Capture` composes via `MaterialLibrary.Lookup(id, key).Map(MaterialWire.Summary)`. `interchange.md:111`.
- FROZEN dedup invariant: the SAME `AppearanceSummary.Of` factory is composed by `Rasm.Bim` `Semantics/appearance#APPEARANCE_PROJECTION.Project`, so a Materials OpenPBR row and a BIM `IfcSurfaceStyleRendering` style mint ONE `AppearanceKey` and dedup to one `Node.Appearance` id — the WF-1 seam-mirror row at `ARCHITECTURE.md:64` (mirror the Element `[2]-[SEAMS]` endpoint verbatim).
- The 5 Appearance cross-page anchors are ANCHOR-ONLY (retarget the reference token to the new `Component/` path; surrounding text frozen): `bsdf.md:3` -> `Profiles/profile#PROFILE_OWNER` (`ProfileFault-2300` band-disjointness citation); `graph.md:250` -> `Connection/connection#CONNECTION_OWNER` (`metal.steel`/`polymer.adhesive` render-row notes); `graph.md:250` -> `Connection/joint#JOINT_FAMILY`; `graph.md:254` -> `Profiles/glazing#GLAZING_FAMILY` (`gas.cavity` IOR-1.0 glazing-cavity-fill note); `graph.md:254` -> `Profiles/masonry#PROFILE_FAMILY` (`Assign` masonry-`Profile`->row generalization note).
- The 4 inbound Appearance cluster names preserved on the Appearance side so collapsing-page references stay valid: `graph#MATERIAL_LIBRARY`, `bsdf#SHADING_FRAME`, `interchange#MATERIAL_WIRE`, `photometric#PHOTOMETRIC`.
- Stable inbound carriers: `Profile.AppearanceId:MaterialId` (`profile.md:174`) and `ConnectionItem.AppearanceId:MaterialId` (`connection.md:157`) plus per-family projections (fastener:199, hanger:192, joint:147-148, reinforcement:183) — field name, `MaterialId` type, and `graph#MATERIAL_LIBRARY` resolution target survive the `Component/` collapse unchanged; reconcile at the owner-agnostic `AppearanceKey`.
- `ConnectionItem` two-slot independence: `AppearanceId` (`graph#MATERIAL_LIBRARY` render row) and `CapacityKey` (`properties#MATERIAL_PROPERTY_CATALOGUE` Mechanical row) are INDEPENDENT `MaterialId` slots, neither derived from the other (`connection.md:17`); must survive the collapse.
- Element-side Appearance touch-points get ANCHOR-ONLY fixups: `Node.Appearance` case (`element.md:278`), `Element.Appearance` `Option<AppearanceSummary>` (line 389), the Bake Associate->Appearance arm (line 525), the `ToCanonicalBytes` arm `w.U128(a.Summary.AppearanceKey)` (line 297), the factory itself.
- FROZEN-ANCHOR TENSION (defect-and-anchor): `material.md` mints `Node.Appearance` at `ctx.Header.Tolerance` (lines 131, 175) — must mint at tolerance 0.0 to preserve cross-projector dedup; anchor-only fixup at the Appearance touch-point.

### Seam-canonical Profile chain (rename STOPS at the Materials folder boundary)
- `ProfileRef` / `ProfileSet` / `ComputedSection` stay seam-canonical — the `MaterialComposition.ProfileSet(ProfileRef)` case + the M7 resolution spanning Bim/Compute/python/ts; `Component` composes them UNCHANGED. The 17-field `ComputedSection` receipt (Area/Ix/Iy/Sx/Sy/Rx/Ry/Zx/Zy/J/Iw/Avy/Avz/Depth/Width/HeatedPerimeter/AxisDistance, Iw 5th at the seam) every family fills identically (`profile.md:201-218`).
- The [M7] `ProfileResolution` one-hop (`profile.md` [03], Build/Resolve frozen cache, `ProfileRef->ResolvedProfile(Profile, Option<ComputedSection>)`); the per-family `ProfileId->ComputedSection` maps (SteelSections/CmuSections/TimberSections) the join reads — never re-run the section integral per call.
- The neutral `SectionProperties` receipt + the M7 one-hop `ProfileRef->SectionProperties` WithSection bake (`material.md` lines 84-103, 181-182); the Materials `ComputedSection` capacity rail resolves ONTO this seam `SectionProperties` (both names coexist: seam owns ProfileRef/ProfileSet/SectionProperties, Materials owns ComputedSection).
- The Op-FREE `graph.SectionOf(member)` (`element.md:492`, no Bake, no Op key) — `Rasm.Compute` reads it directly; the [1].4 accessor type-resolution rework must NOT break this signature or its Op-free contract.

### Element seam identity & projection law
- `ObjectKind{Occurrence,Type}` `[SmartEnum<string>]` statics (`element.md:76-80`) — REUSE, never re-create a second identity scheme; the [1].1 deterministic Type derivation composes THIS token.
- The ONE `ToCanonicalBytes` canonical projection (id-excluded, tolerance-quantized, IEEE-754-LE, explicit attribute order) + the kernel `XxHash128` seed-zero content identity shared by `NodeId.Content`, `ContentAddress.Of`/`OfGraph`, and the cross-runtime `MATERIAL_LAYER_GOLDEN` parity corpus — node/edge content keys byte-stable across C#/Python/TypeScript (the Type-seed Representations exclusion is the ONLY sanctioned change to the Object arm).
- The clean cross-page cluster-anchor set: every `page#CLUSTER` reference the five in-scope Element pages make resolves today to a real `## [..]` header (GRAPH_DELTA, CANONICAL_WRITER, CONTENT_ADDRESS, FAULT_BAND, MEASURE_VALUE, DISCIPLINE_AXIS, CLASSIFICATION_AXIS, ACOUSTIC_FOLDS, ASSESSMENT_NODE, COVERAGE_NODE, GEO_REFERENCE, NODE_MODEL, ELEMENT_GRAPH, EDGE_ALGEBRA, PROPERTY_VALUE, PROPERTY_BAG, MATERIAL_COMPOSITION, MATERIAL_PROPERTY, PROJECTION_CONTRACT, GRAPH_CONSTRAINT) — must stay resolving. Verified-real cross-page members: `MeasureValue.OfSi(QuantityType,Dimension,double)`+`OfSi(Dimension,double)`+Sum+Zero+Si+CanonicalUnit+Type, `ContentAddress.Of`/`OfGraph`/`.Value`, all 6 `ElementFault` cases, `GraphDelta.Empty`/Merge/Reheader/AdmitOnto/ReplayOnto/Put/Link/LegalLink/AddedEdges, `Discipline` statics, the Acoustic carrier reads, `AssessmentPayload`, `CoverageGrid.CanonicalBytes`, `GeoReference.Identity`/CanonicalBytes/Epsg, `Classification.Edition` (string identity member, distinct from `EditionDate:Option<LocalDate>`).
- The NEUTRAL edge algebra boundary (`relation.md`): `Relationship` closed at 5 neutral kinds (Compose/Assign/Associate/Connect/Void) + Generic passthrough, NO `IfcRel*` roster on the seam; the Associate edge carries the `MaterialUsage` occurrence payload and the LegalLink Material/Appearance/Coverage closure. Do not re-open the IFC strata the Classification collapse closed.
- The two-interface count (`projection.md`): net-new Rasm interfaces = 2 (`IElementProjection` + `IGraphConstraint`), no instance default-interface-member, no per-provider seam method; Assemble = applicative capture then monadic admit-then-constrain tail; `ProjectionContext.Header` the load-bearing monoid floor; `ctx.Owns` a PREDICATE (never a silent-drop seam helper).
- `IElementProjection` contract: one `Fin<GraphDelta> Project(ProjectionContext)` op, `TraverseM`->`GraphDelta.Merge` monoid fold (seam owns Assemble; a projector is one registration row), the §4C source-capture inversion (catalogues are private projector fields), the [H12] vouched-element Associate edge — short-circuit on `spec.Bindings.IsEmpty` NEVER `ctx.ElementIds.IsEmpty`; an unvouched binding rails `ProjectionFault.Unvouched`, never an invented Object node.

### Materials structural & catalogue anchors (preserve, do not regress)
- `SectionCapacity` `[Union]` 5-case + Demand/Utilisation/GoverningAction unified rail (`capacity.md`), the VividOrange `InteractionDiagram`/`IForceMomentMesh` eager-solve composition + Moller-Trumbore `CapacityRay`; the `SteelStiffness` Zx/Zy/J/Iw/Avy/Avz closed forms (`steel.md`); the timber EC5 `TimberCapacity` + gamma-method `EffectiveStiffness` + `ResidualSection` charring (`timber.md`). steel.md and timber.md are already REALIZED — preserve only.
- `RcSection` / `IConcreteSection` host-neutral RC assembler (`reinforcement.md` [03]) — `RcSectionBuilder.Of` FAMILY-AGNOSTIC (admits any `Profile` incl. grouted-CMU outline); `RebarLayout` `[Union]` collapsing the 4 VividOrange layout engines; `EnGrade` boundary trapping VividOrange throws onto the typed fault rail.
- The `Rasm_ConnectionRealization` detail-bag round-trip contract with `Rasm.Bim` `Semantics/connection#CONNECTION_DETAIL` (set name, row vocabulary JointType/FastenerType/AccessoryType/BarType/NominalDiameter/etc., OccurrenceWins, JointType allowed-set, dimension-only `MeasureValue.OfSi`, `NodeId.Content` content-key dedup). Projector authoring-NO-Object-node behavior CHANGES under the `Component` Type/Occurrence collapse; the Bim detail-bag round-trip shape is preserved.
- `StudClass.SteelShearKn` (`joint.md:121`) — the ONE shear-stud vocabulary `steel#STEEL_FAMILY` Composite reads for the ΣQn cap; never a parallel stud owner.
- The verified IFC enum tokens (assay/GeometryGym-verified): `IfcReinforcingBarTypeEnum` {MAIN/LIGATURE/SPACEBAR/PUNCHING/EDGE/RING/ANCHORING/SHEAR/STUD/...}, `IfcMechanicalFastenerTypeEnum` {ANCHORBOLT/BOLT/COUPLER/DOWEL/NAIL/NAILPLATE/RIVET/SCREW/SHEARCONNECTOR/STAPLE/STUDSHEARCONNECTOR/...} (no NUT member), `IfcFastenerTypeEnum` {GLUE/MORTAR/WELD/...}, `IfcDiscreteAccessoryTypeEnum` {SHOE/BRACKET/ANCHORPLATE}, `IfcReinforcingBarSurfaceEnum` {PLAIN/TEXTURED} — never introduce an unverified member.
- The VividOrange typed-designation catalogue grounding (steel `SectionReader.Read` over `CatalogueFactory.CreateAmerican/European`; RC `ConcreteSection`; capacity `InteractionDiagram`; EnSteelFactory/EnConcreteFactory/EnRebarFactory grade DATA) — never hand-keyed section-property or grade literals (scope [4] `CATALOG_LAW`).

### Fault bands, aggregation, content-hash, composition admission, nesting (Materials seam law)
- Disjoint fault bands: `ProfileFault` 2300 / `ConstructionFault` 2350 / `ConnectionFault` 2360 / kernel `GeometryFault` 2400 / `MaterialFault` 2450 / `ProjectionFault` 2470 / seam `ElementFault.ValueRejected` 2500. The 2300+2360 bands MERGE under the one `Component` owner but the Expected-derived-bare-lift discipline + disjoint-from-kernel-2400 invariant preserves. Properties+sustainability rail `ElementFault` 2500 (NOT `MaterialFault` 2450); Construction rails 2350 (path/joint/course/opening/nest only); Projection rails 2470. Each band Expected-derived, `[SkipUnionOps]`, `…Case`-suffixed factory.
- Seam-owned acoustic folds (Nrc/Saa/StcWeighted/Rw over the shared `RatingContour.Fit` kernel, `Rasm.Element/Composition/acoustic`) — READ off the Acoustic case, never re-authored in Materials; assembly STC and material STC share one contour owner.
- Relocated-to-Compute aggregation: `AssemblyAggregator` (series-resistance U / mass-law layered-STC / rule-of-mixtures / worst-ply fire / GWP / cost) + AggregateEnvironmental/AggregateCost are `Rasm.Compute`'s; the seam carries the per-material `MaterialPropertySet` INPUT only. Materials authors NO Assessment node and NO assessment-input marshaller.
- The ONE content-hash law: `NodeId.Content` over `ToCanonicalBytes` via the kernel seed-zero `XxHash128` (single hasher, no second hasher in the folder); Material/Appearance non-rooted content-addressed identities, idempotent under re-projection (no collision rail); kernel/Python/TypeScript parity corpus pins byte-for-byte agreement.
- REQUIRED-vs-OPTIONAL Lookup asymmetry: `MaterialPropertyCatalogue.Lookup` faults `ElementFault.ValueRejected` on an unregistered material (engineering properties REQUIRED); `SustainabilityCatalogue.Lookup` returns `Fin.Succ(empty)` for no-EPD (lifecycle OPTIONAL); `SustainabilityCatalogue.Classification` returns None when row/material absent.
- CO2e domain-basis invariant: `StageGwp` modules, RecycledContent/EndOfLifeRecovery, Cost columns are raw domain scalars the seam guards (AllFinite / [0,1] / non-negative), NEVER MeasureValue/UnitsNet/MassUnit coerced; basis-native EPD keeps its own `declared_unit` (PerKg/PerM2/PerM3/PerItem), no forced PerM3 normalization, a negative A1-A3 is valid biogenic-sequestration carbon.
- Seam owns `MaterialComposition` admission: `CompositionAuthor` is coerce-and-delegate, owning NO invariant the seam owns — empty-set/non-positive-thickness/fraction-range/fraction-normalization admission is the seam's `ElementFault.ValueRejected`, never a `ConstructionFault`; layer thickness coerces once through the seam `MeasureValue.Of`.
- Strata-acyclic disjoint nesting: `RectangleBinPack.CSharp` (Materials AABB rectangular cutting-stock YIELD, in-folder pin) vs `Rasm.Fabrication` RectpackSharp/Clipper2 (true-shape irregular NEST); neither re-packs the other; the seam to Fabrication is a mediated [WIRE], never a direct Materials->Fabrication reference; AEC peers depend only UPWARD to {`Rasm`, `Rasm.Element`}.
- Single-engineering-object law + closed composition set: a material carries all disciplines as one `Seq<MaterialPropertySet>` over one `MaterialId` (never StructuralMaterial/ThermalMaterial/AcousticMaterial/EcoMaterial/CostMaterial/ClassifiedMaterial); the `MaterialComposition` vocabulary is the SEAM's closed set (Single/LayerSet/ProfileSet(ProfileRef)/ConstituentSet; `IfcMaterialList` deprecated, never admitted); the C7 `MaterialUsage` occurrence payload stays disjoint from type-level composition.
