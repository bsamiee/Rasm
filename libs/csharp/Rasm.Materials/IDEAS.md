# [MATERIALS_IDEAS]

The forward pool of higher-order concepts for the host-neutral materials owner, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[SPECTRAL_REFLECTANCE_GROUNDING]-[QUEUED]: Promote the three-band RGB appearance edge to a measured-spectral reflectance grounding that mines the full Wacton.Unicolour Spd/SpectralCoefficients/Observer/Illuminant surface so every Materials base color, conductor, and pigment grounds through one spectral upsampling and one measured-illuminant reduction, and the ConductorIor three-band table reconstructs a full per-wavelength eta/k curve from its RGB anchors through a spectral-reconstruction inference run. The validated `RgbSpectrum` `[ComplexValueObject]` band carrier and the MacAdam spectral-locus admission gate are realized; the in-folder Unicolour-based spectral-upsampling/illuminant-reduction leg is actionable now, while the ONNX conductor-curve leg is upstream-blocked on a host-free-peer / host-edge spectral-reconstruction run (Materials cannot reference the app-platform Rasm.Compute ONNX owner — the strata graph forbids the AEC→app-platform edge — so the inference arrives as a decoded curve over the wire, never a Compute project reference).
- Capability: One spectral grounding owner over the realized validated carrier: the bsdf#SPECTRAL_UPSAMPLE RGB->SPD kernel widens to consume the Unicolour Spd/SpectralCoefficients spectral construction and a measured Illuminant/Observer reduction so a MaterialLibrary base color carries a witnessed reflectance SPD that the realized graph#MATERIAL_LIBRARY SpectralAdmit MacAdam spectral-locus gate (IsImaginary then IsInMacAdamLimits, MapToSpectral the recoverable nearest) admits as physically reachable, AND the bsdf#CONDUCTOR_IOR ConductorMetal table replaces the hand-transcribed Johnson-Christy RGB triples with a spectral-inference projection (the RGB anchor reconstructed to a per-wavelength complex-IOR curve through a spectral-reconstruction inference run decoded over the host-free-peer / host-edge wire — never a Rasm.Compute project reference, which the acyclic strata forbids the AEC-domain folder — the SpectralUpsample ToSpd consuming the decoded curve per band). Every channel rides the realized RgbSpectrum/ComplexIor `[ComplexValueObject]` carriers — non-finite/negative bands unrepresentable at Create — so the spectral curve reduces to the validated band carrier the ToSpd/SceneLinear kernel, the ConductorIor table, and every lobe already produce and read, never a raw triple. Also consolidates the inline `new Unicolour(Configuration, Spd)` constructions scattered across acquisition#GroundSpectral and finish#FinishMix.Reflectance onto the one declared SPECTRAL_UPSAMPLE owner — one spectral surface the lobe kernel, acquisition, finish, and library row read, never N parallel inline spectral-construction sites.
- Shape: A grounding capability on bsdf.md#SPECTRAL_UPSAMPLE and #CONDUCTOR_IOR (or surface.md post-split): the SpectralUpsample kernel composing Unicolour Spd/SpectralCoefficients/Observer (Degree2/Degree10) / Illuminant (the 10 A/C/D50-D75/E/F-series statics) over the realized Smits seven-basis ToSpd/Resample5nm fence, the ConductorIor spectral-inference projection over the host-free-peer / host-edge ONNX run (decoded over the wire, never a Rasm.Compute project reference) replacing the realized eight-row measured `(η, k)` table, and the measured-illuminant reduction every base color, conductor, and FinishMix pigment grounds through — the SpectralBand 3-band SmartEnum stays the fast-path band-centre vocabulary the spectral curve reduces to and the realized SpectralAdmit gate the spectral-reachability admission, the acquisition/finish inline Spd sites collapsed onto the one owner.
- Unlocks: Every metal grounds in a measured complex-IOR curve rather than a guessed RGB triple, a base color round-trips through a witnessed reflectance SPD under a named illuminant and clears the realized MacAdam spectral-locus bound, metameric-pair and custom-illuminant (D50/D65/F-series swap) rendering admits, and the FinishMix Kubelka-Munk path and the conductor lobe read one spectral grounding — the appearance engine reaches measured-spectral fidelity professional renderers require, the EPFL-RGL upstream-blocked leg awaiting only the host-free-peer / host-edge ONNX spectral-reconstruction run decoded over the wire.
- Anchors: bsdf#SPECTRAL_UPSAMPLE realized RGB->SPD Smits kernel (ToSpd/Resample5nm) + Unicolour Spd->XYZ composition + the validated RgbSpectrum carrier SceneLinear produces; bsdf#CONDUCTOR_IOR realized eight-row ConductorMetal/ConductorIor measured `(η, k)` table over ComplexIor + the spectral-curve upstream-blocked note; graph#MATERIAL_LIBRARY realized SpectralAdmit/MapToSpectral MacAdam spectral-locus gate (IsImaginary/IsInMacAdamLimits) the third domain gamut gate; acquisition#GroundSpectral + finish#FinishMix.Reflectance inline new Unicolour(Configuration, Spd) sites the owner absorbs; Wacton.Unicolour Spd/SpectralCoefficients/Observer/Illuminant + MacAdam-limit surface (api-unicolour [02]); the host-free-peer / host-edge ONNX spectral-reconstruction conductor curve decoded over the wire (NOT a Rasm.Compute/Symbolic [PORT] — the acyclic strata forbids the AEC-domain Materials a reference to the app-platform Compute); the EPFL-RGL measured-BRDF spectral set, the Johnson-Christy/Rakic optical constants.
- Tension: The validated band carrier (RgbSpectrum/ComplexIor) and the MacAdam spectral-locus admission are realized, so the grounding reuses the landed carrier and reachability gate rather than minting either; the remaining open work is the full per-wavelength conductor curve, which is upstream-blocked on a host-free-peer / host-edge spectral-reconstruction run delivering the decoded curve over the wire — until that lands the table grounds three measured RGB-band anchors, the spectral inference the upstream-gated leg over a realized three-band fast path. Materials references no ML package and no Rasm.Compute project (the strata graph forbids the AEC→app-platform edge); the decoded curve enters as wire data, not a Compute reference.


## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[JOINT_CONNECTION_FAMILY]-[COMPLETE]: ConnectionFamily widened to closed-at-four with the deliberate fourth `joint` case (Connection/joint.md) — the JointKind weld/adhesive/stud discriminant, the WeldType/ElectrodeClass/AdhesiveClass/StudClass axes, the JointSection union arm + DesignShearKn spec-nominal capacity projection (the stud arm returning the steel-side StudClass.SteelShearKn cap, the composite Qn = min(steel, concrete) the steel#STEEL_FAMILY CompositeMn fold develops against the slab f'c), and the BuildJointRows AWS D1.1 / structural-adhesive / shear-stud table feeding ConnectionItem.Of and ConnectionCatalogue.Build; the StudClass vocabulary the steel#STEEL_FAMILY Composite arm reads. Ripple: Rasm.Bim [JOINT_CONNECTION_FAMILY].
[SUSTAINABILITY_PROPERTY_DISCIPLINE]-[COMPLETE]: the lifecycle-and-procurement discipline landed (Properties/sustainability.md) — Environmental/Cost/Classification MaterialProperty cases over the EN 15978 banded StageGwp carrier (mirroring Acoustic) + UnitsNet Mass GWP + Currency/MeasurementBasis/ClassificationSystem bands, plus AssemblyAggregator.AggregateEnvironmental/AggregateCost per-ply folds; a material is a full lifecycle object over one MaterialId. Ripple: Rasm.Bim [PSET_ENVIRONMENTAL_PROJECTION] + cs:AEC_SIMULATION_BRIDGE [EMBODIED_CARBON_QTO] (deferred cross-package leg).
[GENERATED_BOND_INTERPRETER]-[COMPLETE]: the Bond axis is a generative algebra (masonry#PROFILE_FAMILY) — the BondGeometry descriptor + BondPattern vocabulary + GeneratedBond.Interpret fold compute stack/flemish/herringbone/basketweave/pinwheel/diaper course placement and per-unit rotation (into Placement.PathAngleDegrees) from data rather than railing ProfileFault.Bond; a new decorative bond is one BondGeometry row.
[STEEL_DESIGN_OBJECT]-[COMPLETE]: the steel family is a true AISC design object (steel#STEEL_FAMILY) — all six rolled SteelClass cases + the Composite (AISC 360 Ch I) and ColdFormed (AISI S100) arms over one SteelSection, the CompactnessClass 3-state verdict replacing IsCompact, and the DesignCapacity LRFD projection (φMn/φPn/φVn) derived from the stored Zx/Sx/Ix/Area columns; the composite arm reads the Connection/joint StudClass shear interface.
