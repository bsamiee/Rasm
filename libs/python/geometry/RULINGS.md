# [PY_GEOMETRY_RULINGS]

`python/geometry` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- (none)

## [02]-[SHAPE]

- `charter_record` reads its `GeometrySubject` and the weave close-out reads its `EvidenceScope`, so a charter measure records under a discriminant the fold already holds and neither entry takes a `kind` argument — an engine page owning no scope member otherwise invents a string no vocabulary owns, and a rename strands it on a live series. Recurring wrong move: a producing fold widening its signature to carry the discriminant it was handed.
- A graduating receipt mints its own evidence key from its own `spec` projection through the spine's `evidence_key`, and its ceiling roster derives PER MEASURE — a measure the arm never took drops its bar rather than grading a fabricated zero, since the spine distinguishes `unmeasured:` from `over:` and a zero clears every ceiling. Its frame projection keys the same evidence through the same fold, so `graduates()` and `frame()` never disagree on one result's identity. Recurring wrong move: a `graduates(evidence_key, …)` signature taking the key from a caller, so two identical results key differently and no owner can be found for the mint.
- Producing folds return `(value, receipt)` and no owner publishes a `receipt(...)` method — a caller handing in a count, a coordinate, or a percentage the fold already holds is the `[ASSERTED_VALUE]` shape, and the weave's conditional harvest then emits nothing because no returned value conforms to the contributor port. Recurring wrong move: a public receipt builder added beside a fold so a caller can attach facts.
- A receipt carries the CENSUS and its frame carries the ROWS at the finest grain the producing fold computed — the per-segment band, the cell distribution, the per-element reading — so a receipt never grows with the row count and the columnar tier never receives an extremum standing in for a distribution. A grade published as its own tail lets no consumer separate one bad row from a wholly bad result. Recurring wrong move: a `frame()` projecting the same fact map the receipt already emits, which leaves the row-grain evidence with no carrier at all.
- A graduation measure a fold never computed is OMITTED from `measured`, never defaulted — the spine's `_breached` already distinguishes `unmeasured:<name>` from `over:<name>`, so a `default_value(0.0)` converts a missing reading into one that clears every ceiling. Recurring wrong move: an `Option` receipt slot flattened at the handoff instead of at the key.

## [03]-[COLLAPSE]

- The AGPL band's late binding is ONE `LateBound{module, member, leads}` value at `energy/climate` — `COMFORT`, `MAPS`, `INDEX`, `RESOLVERS`, and `WRITERS` are five tables over one grammar, `leads` naming the owner attributes a kernel takes so a row binds them by keyword at any position. Recurring wrong move: a second `import_module` + `getattr` fold minted beside a new table, or a boolean prefix column standing in for the named roster.
- Measurement and grading stay at the runtime bench tier and this folder authors a ROSTER and bound kernels only — `CORPUS` rows name which seams are graded and on what bar, `graded` hands them to `Bench.graded`, and no window, sample fold, quantile, bar comparison, or verdict counter is minted here. One `bench_subject` derivation spells every subject id, so a roster row and its producing `bench_seam` call cannot drift into two ids for one seam. Recurring wrong move: a page-side subject f-string beside a transcribed roster literal, which reports an uncovered subject for the pair that drifted rather than for one anyone forgot to bind.
- A coarse pose reaches a fine solver through that solver's OWN initial-transform argument, never a pre-transformed copy of the source carrier — every admitted registration provider publishes one, and the copy pays a whole-cloud allocation plus a post-solve composition the provider performs inside its iteration for free. Recurring wrong move: a uniform carrier-level pre-pose adopted because one arm was assumed to publish no slot.

## [04]-[STRUCTURE]

- IFC and energy verbs run caller-floor by default and cross the process band only for a genuinely long native phase — source or document bytes in, pure-Python graph or successor bytes out; a short pure decode or attribute walk earns no serialization round-trip, so offloading every verb is the rejected form.
- A marked provider proves presence at import and refuses BY NAME — one `find_spec` correspondence resolved once into an absence map, gating the selected arm onto `Error(BoundaryFault(import_=module))` ahead of any offload, and a projection that must return a provider handle raising its owner's typed `unprovisioned` fault into the enclosing fence. An ungated arm dies as a bare `ModuleNotFoundError` inside a worker and reaches the caller as a worker-crash rail carrying a private module path; the probe selects a capability tier, never an offload route, because a process-pool worker shares the one venv. Recurring wrong move: a two-row probe treating its second, unprobed row as an always-available floor.

## [05]-[PROCESS]

- Domain refusal raises a typed structured fault into the converting fence — `BrepFault`-class kwargs lift into the boundary fault whole, and the lane's worker seam re-wraps a bare exception to its string; a bare `ValueError` f-string flattens the refusal either way and forks the vocabulary across peer subsystems. Fence coverage is the precondition — a factory or projection reachable outside `evidence_run` returns the rail, since a raise no enclosing fence converts escapes to its caller.
- A durable object-store tier is an OPTIMIZATION and never a correctness dependency: a read-through failure folds to absence and the kernel runs, a write failure leaves a live artifact, and both land as receipt evidence rather than as a rail — the cost of a store outage is work nobody skipped, where a rail there is work nobody GOT. The lane arrives BUILT as a composition parameter, so no domain page mints a handle, a scheme roster, a credential, or a retry row. Recurring wrong move: a spill whose refusal rails the producing crossing, or a folder-local `from_url` standing in for the composition input.
- Capsule fencing is `FAULT_CONF` on the one innermost untrusted seam — `_dispatch` for a rail-returning capsule, the numeric leaf for an effect-result capsule — and the public `run` head carries no decorator, because a head fence double-guards the seam and forks the fence idiom across peer capsules.
