# [DRAFT_FLOOR_MAXIMALIST] — RASM-CS-GEOMETRY structural blueprint

Lens: every recommended-shape floor pushed to the strongest form the evidence justifies — the deepest exact lattice (implicit-point path landed with its full companion set, not the honesty re-scope), the deepest parametric tier (vendor-and-own NOW, closing every access wall in one motion), the deepest toolpath owners (clearance-radius medial, oriented slice forests, corner-row offsets, analytic hidden-line). Zero hinges deferred mid-leg; both probe rulings baked into page rows. 28 page rows: 16 REBUILD, 2 MOVE, 10 NEW, 0 KEEP/DELETE-standalone. All 14 verdicts, all 16 evidence rows, all 17 `[03]` deltas disposed below.

## [00]-[PROBE_RULINGS] — both hinges ruled from probe evidence

### R1 — GShark hinge: VENDOR-AND-OWN NOW (whole algorithm set, one motion; the pin dies)

Probe verdict FEASIBLE_WITH_GAPS with two ACCESS-MODIFIER WALLS (G1 surface ctor `internal` with no subclass path — `NurbsSurface.cs:29`; G2 surface derivatives `internal` + metric-blind public API — `Evaluate/Surface.cs:25`, open #234) that have NO in-envelope closure; the no-re-mint clause (`api-gshark.md:171`) forbids owned re-implementation; the one-engine law forbids a package+vendored-surface split. The only branch that closes G1+G2 AND keeps host-neutral surface evaluation for the Generation KERNEL GATE is vendoring the WHOLE composed algorithm set (MIT-clean): the verified Piegl-Tiller Gauss-Legendre arc-length engine (1e-6), Wang-2008 double-reflection RMF, NURBS-Book evaluation/refinement, the public `KnotVector` algebra, and the `Fitting.Curve` lane. The vendored owner is the NEW `Parametric/nurbs.md`; in the same motion it: publicizes curve+surface arbitrary-knot construction (G1 closed), publicizes raw `RationalDerivatives` with the #234 order convention fixed (G2 closed), mints fundamental-form/curvature projections over those derivatives (G3 closed — kernel math atop vendored evaluation, not a re-mint), lands the Piegl-Tiller surface interpolation/approximation lane so normal-offset surfaces are REAL NURBS (G5 closed on the strong branch, not the tessellation re-scope), adds the RMF closure-correction policy row for #373 + the coincident-sample NaN guard (G6 partial), parameterizes the `ClosestParameter` knobs (G7 closed), and seals clamp/`Normalize()`-at-ingress + hash-the-normalized-form as adapter law (G6/G10 — one curve, one content key). G8 curve offset: vendored `Offset` is the seed; the kernel deviation-refinement + self-intersection trim through the `[V4]` crossing lattice is a `curve.md` op body. G4: the surface-intersection TRIPLE (SSI #127, surface-plane #128, curve-surface #129) stays host-deferred — the disposition row widens to the full triple per probe. G9: `Fin`/`GeometryFault` boundary kernels at every vendored throw site. Manifest consequence: the `GShark 2.3.1` pin + `Rasm.csproj:18` reference + kernel `api-gshark.md` DELETE in the leg that lands `nurbs.md` (wave 4); waves 1-3 keep the catalog truthful under the six probe corrections (`[08]`).

### R2 — implicit-point hinge: the V4 EXACT PATH LANDS (no honesty re-scope), with the probe's companion set mandatory in the same leg

Probe verdict FEASIBLE_WITH_GAPS; the mechanism is proven (Attene indirect predicates; Cherchi mesh arrangements; Diazzi-Attene CDT) and each gap is a case-row widening on an existing owner. Ruled companions, all wave-1/wave-2 rows on the affected pages: (1) hybrid vertex table — `Tessellation`'s vertex payload widens from `Point3d[]` to a closed two-kind row store `Explicit(Point3d) | Crossing(defining-entity ids)` (SSI-2D: 4 ids, LPI-3D: 5, TPI-3D: 9); `SimplexStore` untouched; depth-1 law sealed (implicit rows reference INPUT vertices only; recovery-time constraint×constraint crossings re-express as TPI-3D over original entities; sub-segment splits re-anchor on the `Constraint` carrier). (2) Construction re-founding in `predicates.md` — `Lpi` re-founded as SSI-2D (four points); NEW LPI-3D (five points, three numerators + λ); `Tpi` re-founded over NINE input points with X,Y,Z numerators (derived `Plane` inputs die); leaf differences through error-free transforms; the rounded `Denominator`/`d1.Estimate()` inside the exact branch dies; the orient pair gains the negative-λ verdict flip mirroring `RationalOracle.InCircum`. (3) Family widening ~21 members on the ONE `Predicate` owner — `Orient2D` implicit combos {IEE, IIE, III} × axis-projected {xy, yz, zx}; `LessThanOn{X,Y,Z}` {IE, II} (the honest `OrderKey`, replacing exact-over-rounded); a directed-rounding INTERVAL-FILTER tier for every implicit member (kills the false no-filter rationale at `predicates.md:15,572` and the BigInteger-per-cavity-test throughput gap — this is also the `README:86` PeterO row landing). (4) In-circum ruled on the consumer: `TessellationPolicy` gains a CONSTRAINED-ONLY mode (no Delaunay restoration) for implicit-bearing builds — the reference pipeline's exact shape, zero multi-implicit in-circum burden; the `InCircle` {IIEE..IIII} combos and the 3D `orient3d`/`inSphere` multi-implicit family are RECORDED GROWTH gated on a CDTet consumer, never this campaign's landing. (5) Carrier plumbing — `Crossing`/`Constraint` carry defining-entity ids (the `bool IsImplicit` + rounded-`Point3d`-at-birth carriage dies); interning and chain-merge key on defining entities; `.Round()` is the ONE emission-seam materialization; both pages state signs-exact / coordinates-rounded-at-emission. V10b rides the cheapest sub-family (LPI-3D + projected Orient2D + LessThan; no in-circum, no substrate). V10c is DECOUPLED — the medial CDT input is a simple polygon ring (all-explicit, zero Steiner), so the Voronoi-dual accessor lands regardless: exact DT adjacency + circumcenter coordinates at the emission seam; an exact circumcenter side-of test is a recorded growth row. `README:42`/`ARCH:42` implicit-point rows STAND — the fences become true.

## [01]-[RATIFIED_PARTITION] — folders, TIER-2 namespaces, fault clusters (V1 + V12 discharged)

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`): `Solving/` MINTED (solver + fit — the one-LM family lifts out of Processing; 2-page floor satisfied, real growth axis: constrained optimization); `Drawing/` KEEPS both pages under the ratified charter "downstream emission = projection + residency encoding" (growth-axis test passes; no rename — a rename buys nothing and costs an inbound sweep); `Parametric/` grows to 7 robust-core pages beside landed `projections.md`/`locate.md`; `Meshing/` grows to 8 robust-core pages beside landed `mesh.md`/`dec.md`/`reconstruct.md`. Every folder ≥2 pages. The `ARCHITECTURE.md:110` `Rasm.Geometry.*` placeholder row is REPLACED by the enumerated partition below; codemap, README router, and `faults.md` cluster taxonomy update in the same wave-1 motion; the `[LANDED_KERNEL_LAW]` (b) inbound-reference sweep runs with it.

Namespace partition — 20 TIER-2 concept namespaces, each single-folder, each mapped 1:1 to a fault sub-band (two structural exemptions stated: `Rasm.Geometry.Numerics` is total-pure, routes no faults; root `Rasm.Geometry` is the union owner itself). The phantom `Rasm.Geometry.Topology` dies at its three sites (`pack.md:33` import, `pack.md:18` packages row, `solver.md:13` owner prose) — all re-anchor to `Rasm.Geometry.Naming`; the `topology` cluster 2404-2407 renames to `naming` in the same motion.

| # | Namespace | Pages (folder) | Fault sub-band |
|---|---|---|---|
| 1 | `Rasm.Geometry` | faults (Numerics) | union owner — root vocabulary |
| 2 | `Rasm.Geometry.Numerics` | predicates (Numerics) | exempt (total, no rail) |
| 3 | `Rasm.Geometry.Spatial` | index (Spatial) | `spatial` stride (`IndexMismatch` re-typed home) |
| 4 | `Rasm.Geometry.Naming` | naming, reconciliation (Spatial) | `naming` 2404-2407 (`NameCollision` 2404, `HashMismatch` 2405) — renamed from `topology` |
| 5 | `Rasm.Geometry.Tessellation` | delaunay (Meshing) | sub-band 2421-2422 in the arrangement stride: `TessellationFault` 2421, `ConstraintRecoveryFault` 2422 — the "Tessellation owns no cluster" census gap closes |
| 6 | `Rasm.Geometry.Intersection` | intersect, slice (Meshing) | `intersection` 2424-2427: `IntersectionFault` 2424 re-typed, `SectionFault` 2425 (slice) |
| 7 | `Rasm.Geometry.Arrangement` | arrangement (Meshing) | sub-band 2420 + 2423: `DegenerateArrangement` 2420 re-typed, `NativeAssetMissing` 2423 re-homed + re-typed (tier-3 gate lives with the boolean owner) |
| 8 | `Rasm.Geometry.Offsetting` | offset, skeleton (Meshing) | `offsetting` stride: `DegenerateOffset` re-typed, `SkeletonStalled` (rich, stays) |
| 9 | `Rasm.Geometry.Editing` | edit (Meshing) | sub-band in the healing stride: `ArenaFault` (new, typed: stage · counts · tolerance) |
| 10 | `Rasm.Geometry.Remeshing` | remesh (Meshing) | sub-band 2441-2442 in the simplification stride: `RemeshFault` 2441 (typed) |
| 11 | `Rasm.Geometry.Healing` | repair, receipts (Processing) | `healing` stride: `UnrepairableMesh` re-typed |
| 12 | `Rasm.Geometry.Simplification` | decimate (Processing) | 2440 `DecimationFault` (rich, stays) |
| 13 | `Rasm.Geometry.Parameterization` | flatten (Processing) | 2432 `ParameterizationFault` re-typed; curve's borrow ENDS |
| 14 | `Rasm.Geometry.Constraints` | solver (Solving) | `constraints` stride: `OverConstrained`, `SingularSystem` (rich, stay) |
| 15 | `Rasm.Geometry.Fitting` | fit (Solving) | 2428 `FitFault` (rich, stays) |
| 16 | `Rasm.Geometry.Parametric` | nurbs, curve, surface (Parametric) | 2448 `ParametricFault` (typed: carrier · stage · witness) — first headroom code spent deliberately |
| 17 | `Rasm.Geometry.Development` | subdivide, develop, panelize (Parametric) | sub-band 2433-2435 in the widened parameterization stride: `SubdivisionFault` 2433, `DevelopabilityFault` 2434, `PanelizationFault` 2435 |
| 18 | `Rasm.Geometry.Patterning` | patternmap (Parametric) | 2449 `PatternFault` (typed) — second headroom code spent deliberately |
| 19 | `Rasm.Geometry.Projection` | view (Drawing) | `projection` stride: `ProjectionFault` re-typed |
| 20 | `Rasm.Geometry.Encoding` | pack (Drawing) | `encoding` stride: `EncodingFault` re-typed (`ChannelKind` · dtype · detail) |

Band law: the geometry century 2400-2449 holds — 12 existing clusters + the sub-band shares above + both headroom codes (2448, 2449) spent; NO 13th-cluster squeeze, NO federation re-plan needed. `DegenerateInput` (2400 stride) stays the cross-cutting admission case any namespace routes. Numbering rule for codes not anchored above: next free code within the named cluster stride, emitted as the complete numeric map by the `faults.md` rebuild (cluster taxonomy = this partition). E15 re-type set (all 10 stringly cases gain real payloads): `DegenerateInput(GeometryKind kind, int index, string witness)`, `IndexMismatch(int expected, int actual, EntityKind kind)`, `UnrepairableMesh(HealStage stage, int remaining)`, `NativeAssetMissing(string engine, string rid, long ceiling)`, `DegenerateOffset(int wavefrontVertex, double time)`, `DegenerateArrangement(int cellCount, string manifoldWitness)`, `IntersectionFault(PrimitiveKind a, PrimitiveKind b)`, `ParameterizationFault(ChartId chart, double distortion)`, `ProjectionFault(EdgeKind kind, int segment)`, `EncodingFault(ChannelKind channel, ChannelDtype dtype, string detail)`.

## [02]-[PAGE_SET] — 28 rows (paths `libs/csharp/Rasm/.planning/`-relative)

Row schema: path | action → engine lowering | charter (folder · namespace · fault home) | entry | seams (out = composes, in = consumed by; one anchor per seam) | wave.

### Wave 1 — FLOOR + CONTRACTS

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 1 | `Numerics/predicates.md` | REBUILD → rebuild | Numerics · `Rasm.Geometry.Numerics` · exempt (total). The adaptive exact-predicate + implicit-construction owner: four-tier direct ladder PRESERVED VERBATIM (double filter → `ddouble` Refine → `Expansion` → four-way oracle); implicit half RE-FOUNDED per R2 items 2-3 (SSI-2D/LPI-3D/TPI-9pt constructions, ~21 family members, negative-λ flip, exact leaf EFTs, directed-rounding interval-filter tier via `ERounding.Floor`/`Ceiling`); strict-IEEE-754/RID invariant stated on `NumericsPolicy`; FMA-free Dekker-split `TwoProduct` fallback row gated on RID FMA capability (`Splitter` consumed); honest precursor rationale ("no maintained, NuGet-published, indirect-complete predicate library exists") | NONE (pure `Predicate` family: `Orient2D/3D`, `InCircle/InSphere`, implicit combos, `LessThanOn{X,Y,Z}`; `Sign`-returning, no rail) | out: `TYoshimura.DoubleDouble`/`ExtendedNumerics`/`PeterO` (`api-*` anchors); in: every robust-core owner; cross-package: `Predicates ← Fabrication/Posting` (`ARCH:89`), NEW `Predicates ← Rasm.Compute` CDTet row (`RASM-CS-COMPUTE [V7]` — public `Orient3D`/`InSphere` already satisfy by shape) | 1 |
| 2 | `Numerics/faults.md` | REBUILD → rebuild | Numerics · `Rasm.Geometry` (root) · union owner. ONE `GeometryFault` union: 10 stringly cases re-typed (`[01]` payload set); the two-family seam statement lands (`GeometryFault` band-2400 ⟂ `Rasm.Domain` `Expected`/`Fault`, neither absorbs — mirroring `rails.md`'s side, discharging `ARCH:112`); cluster taxonomy = the `[01]` ratified partition (topology→naming rename, Tessellation home, both headroom spends, new-plane sub-bands) | NONE (fault vocabulary) | out: none; in: every owner routes; `ARCH:112` seam row both sides | 1 |
| 3 | `Spatial/index.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Spatial` · spatial stride. Primitive broad-phase owner (SAH-BVH · Morton octree · agglomerative PLOC + degradation-refit, preserved verbatim): `PointCloud` arm EXCISED in one motion (case `:118`, kind `:44`, `BuildPointCloud` `:151-155`, `KdNearest/KdRange` `:372-381`, guards `:130,354-359,542,558,573` — point k-NN/radius routes landed `neighbors.md`); zero throws (`Store`/`ToAcceleration` total over {Bvh, LinearOctree}); kind-mismatch routes `Fin` + typed fault (silent empty dies); bounds narrowing rounds OUTWARD (min toward −∞, max toward +∞ incl. the `:590-591` wire casts); `ToAcceleration → Fin<(float[] Bounds, long[] Nodes)>` over `NodeLinkProjection` (the `using Rasm.Compute.Solver` dies); GWN single-owner: index owns the BVH-accelerated winding query, `reconstruct.md` GWN + `fields.md` `SignedDistanceFromMesh` compose it — the ONE distance-field lane; counts resync (2 cases · 3 kinds); `CLASH_GOLDEN` marked harness-gated | `SpatialIndex.Build(SpatialKind, …) → Fin<SpatialIndex>`; `Query(SpatialQuery) → Fin<QueryResult>`; `Refit`; `ToAcceleration() → Fin<(float[],long[])>` | out: predicates (winding signs), `neighbors.md` (point-query routing row); in: intersect/arrangement (broad-phase), view (occlusion), decimate (Hausdorff); cross-package: index⇄Compute node-array wire (`ARCH:86` composed; `COMPUTE:121` decodes; `COMPUTE:120` spurious reverse row DELETED); `Index ↔ Fabrication` broad-phase rows (`ARCH:84,85`) made bidirectional | 1 |
| 4 | `Spatial/naming.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Naming` · 2404-2407. Persistent-name algebra: `RebuiltEntity` gains explicit `int Self`; `With`/`Track` key `VertexNames` by `Self` (the `IncidentVertices[0]` star-minimum mis-key dies); `Subsumes` subset direction corrected for splits (child ⊂ parent) AND the migration predicate re-founded on shared-name overlap fraction (the fragile vertex-set-subset model hardens); Survive×Migrate injectivity resolved: a claimed survivor still seeds sibling `Parent` provenance (lineage preserved; injectivity via fresh `born.Value` stands); namespace declared | `Track(NamingTable prior, RebuiltEntity[] rebuilt, Op? key = null) → Fin<NamingTable>` | out: `identity.md` `ContentHash.Of` (landed); in: reconciliation (entity bridge), pack (`CanonicalTopology.OfMesh`) | 1 |
| 5 | `Spatial/reconciliation.md` | REBUILD → rebuild | Spatial · `Rasm.Geometry.Naming` · 2404-2407 (shared with naming). Naming↔hash bridge: `BuildEntities` emits `Self=v` (`star` kept for the histogram only); namespace + `using Rasm.Domain` declared (the unqualified `ContentHash.Of` compiles); `Encode` widens to ONE op-union over {Mesh, Cloud, Parametric} — the `[V2]` parametric canonical-bytes projection (degree · knots · weights · control net, NORMALIZED form per R1) is an `EncodeOp` case row on the ONE seam family, a COMPONENT of the Element `ToCanonicalBytes` fold, never a sibling SpineRef key; `CANONICAL_BYTE_IDENTITY` digest flagged harness-confirmed-once; Persistence anchor unified to ONE row | `NamingHashOps.Encode(EncodeOp) → Fin<UInt128>`; `Reconcile(...) → Fin<ReconcileResult>` | out: naming (Track), `identity.md` (`ContentHash.Of` — ledger row `ARCH:79`); in: pack (mesh/cloud keys), curve/surface (parametric bytes); cross-package: → Persistence content-key, ONE anchor = `Rasm.Persistence/ARCHITECTURE.md:55` spelling adopted at `reconciliation.md:3`/`:129` and `ARCH:79`; parametric component feeds `Element/codec.md:151-152` | 1 |
| 6 | `Meshing/edit.md` | NEW → new | Meshing · `Rasm.Geometry.Editing` · ArenaFault (healing stride). The `[V6]` MUTABLE-ARENA tier beside landed `mesh.md`: `MeshEdit` extracted from `repair.md:76-102` and RE-FOUNDED as a true SoA arena (the immutable-copy-per-op O(V+F) shape dies); the two-carrier seam stated both pages (`MeshSpace` immutable admission snapshot / `MeshEdit` predicate-gated build arena — no third carrier); the soup adapter ×3 collapses here (`intersect.md:326`, `view.md:386`, `arrangement.md:352-365` → ONE `Soup(MeshSpace)`/`OfMesh` owner, native duplication deduped); `Kernels.WeldDuplicates` + the `RepairPolicy.WeldTolerance` knob SITE HERE (dedup-on-arena is an arena op — the arrangement→repair wave-2→3 edge dies, completing the V5 cycle break); the store-mutability + arena-concurrency contract lands corpus-wide in this page's law block (arenas single-writer, publish-by-freeze, `LaplacianCache` the exemplar; frozen/partition-disjoint spans only for parallel folds); `CommunityToolkit.HighPerformance` ADMITTED on these arena shapes (`Span2D`/`MemoryOwner<T>`/`ParallelHelper`, `api-highperformance.md:26,35,54`) | NONE (arena substrate: `MeshEdit` carrier + `Kernels` weld/soup family) | out: `mesh.md` (two-carrier seam row, 1-hop ripple re-anchoring `mesh.md:647`), predicates (weld gates); in: intersect, arrangement, view, decimate, receipts, repair, remesh, slice, skeleton | 1 |

Wave-1 roster motion rides beside rows 1-6 (see `[08]`): MathNet 5.0.0 downgrade + widened survival sweep, OpenBLAS drop, pin re-group, Manifold admission + catalog, HighPerformance reference, hashing correction, csparse dedupe, WATCH re-key, api-gshark corrections.

### Wave 2 — MESHING LATTICE (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 7 | `Meshing/delaunay.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Tessellation` · 2421-2422. Exact CDT/CDTet: the seven `[STORE_OPS]` stub bodies (`:192-198`) TRANSCRIBED (the hardest transcription in the tree — `Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces`); vertex payload widens to the R2 two-kind row store (`Explicit | Crossing(ids)`), `Retriangulate` appends implicit rows without materializing; `TessellationPolicy` gains the CONSTRAINED-ONLY mode for implicit-bearing builds; the walk/cavity route the widened predicate family (projected `Orient2D` combos; `InCircleLPI` stays for the explicit-corner Steiner case); Voronoi-dual accessor lands as a `Tessellation` projection (exact DT adjacency + circumcenters materialized at the emission seam — V10c decoupled, all-explicit medial input; `README:99` discharged); lifted-paraboloid LOWER-HULL projection lands as the second projection row (the `[V11]` exact tier: Fabrication stock/clearing envelopes, Generation containment at robust grade); `SimplexStore` re-labeled honest mutable arena under the `[V6]` contract; rejection rationale rewritten (Triangle.NET is MIT; the load-bearing bar is float-epsilon + no exact CDT/CDTet; TetGen stays AGPL-rejected) | `Tessellation.Build(TessellationOp, Op? key = null) → Fin<Tessellation>`; projections `VoronoiDual`/`LowerHull`/`ToMesh` on the result | out: predicates (walk/cavity/implicit), edit (arena law); in: arrangement (`Tessellation.Build`), offset (dual), repair (CDT re-mesh) | 2 |
| 8 | `Meshing/intersect.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Intersection` · 2424. The exact-crossing lattice, single owner: `Chain` RE-FOUNDED — crossing endpoints keyed by DEFINING ENTITIES (R2 item 5; the cross-face merge key that does not exist today), linked ACROSS segments, walked by true connectivity; closed loops close ORIENTED (outer CCW / holes CW), open curves run end to end as typed OPEN-chain rows (`IntersectResult` widens beyond loops-only); the dead `Next` column (`:75`) wired as the successor walk or deleted; `Crossing` carries defining-entity ids + LPI-3D payload (the `Denominator`/`Lambda` dual-field resolves with the predicates re-found; `.Round()` = emission only); `SegmentSegment`/`LineLine` typed as a 2D restriction case; the three `QueryResult` hard casts (`:222,233,241`) route `Fin`; the E7 collapse LANDS BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply` — no fourth kernel); host-delegation cites re-anchor `Analysis/Intersect.cs` → `relations.md` at BOTH sites (`:5`, `:388`); `contract` label restored only when the chain is real | `Intersection.Apply(IntersectOp, Op? key = null) → Fin<IntersectResult>` | out: predicates (Guigue-Devillers signs + LPI-3D + LessThan ordering), index (BVH broad-phase), edit (soup); in: arrangement, offset, repair (V4), slice (V10b), view (Section); cross-package: altitude boundary ⇄ `Analysis/relations.md` (both sides, one anchor) | 2 |
| 9 | `Meshing/arrangement.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Arrangement` · 2420 + 2423. Exact arrangement/boolean owner: `BooleanOp` RE-HOMES here from `repair.md:57` (with the regularized keep-predicate; `ARCH:42` codemap already sites it); the `BooleanReceipt` collision resolves to THIS page's type (receipts' renames, row 22); `Constraint` gains the implicit-point case — operand crossings ride defining-entity carriage into `Tessellation.Build` (R2; `PlaneCrossPoint`/`EdgeCrossings`/`InTriangle` inline copies DIE into `Intersection.Apply`; the dead `ImplicitCrossing.IsImplicit`/`Explicit` carrier dies); interning keys on defining entities (bit-equality `Dictionary<Point3d,int>` dies); Manifold NAMED as the tier-3 scale companion behind `ArrangementPolicy.ScaleCeiling` = 1_000_000 combined operand faces (finite, revisable policy row; `int.MaxValue` dead code dies) routing the in-house `manifoldc` P/Invoke when the RID asset resolves, else `Fin` fail `NativeAssetMissing` 2423; golden-boolean fixture gates activation; managed exact arrangement stays the ONE correctness rail ("retires the tier-3 native CSG gate" re-scopes to exactly this meaning); weld edge → `edit.md` (`Kernels.WeldDuplicates` sited wave 1 — the repair edge is GONE); soup + triple-`DuplicateNative` dedup via edit; store honest mutable arena | `Arrangement.Apply(ArrangementOp, Op? key = null) → Fin<ArrangementResult>` (MeshBoolean · PlanarOverlay · CellComplex) | out: delaunay (`Tessellation.Build` + implicit `Constraint`), intersect (`Apply`/`Crossing`), index (`Winding`/`Overlap` — the correct V8 consumer), edit (weld/soup), predicates; in: repair (`HealOp.Boolean` delegation), view + curve `ToMesh` (`PlanarOverlay` fill per `README:99`); cross-package: `FAB:44` re-anchors `csharp:Rasm/Processing/repair` → `Meshing/arrangement` | 2 |
| 10 | `Meshing/offset.md` | REBUILD → rebuild | Meshing · `Rasm.Geometry.Offsetting` · offsetting stride. Exact wavefront skeleton/offset/medial: medial made REAL against the delaunay `VoronoiDual` projection (the discarded build at `:325-326` dies; parabolic reflex arcs reconcile against the dual as `:16,478` claim); per-point CLEARANCE-RADIUS carried as a first-class result field + `Clearance(Point3d probe)` arbitrary-probe query (the `[V10]` mandate — a radius-less medial is a kernel defect); `JoinType`(Miter/Round/Bevel/Square)×`EndType` corner-strategy rows over the ONE offset assembly (`[GENERATOR_LAW]` — the next join is a row; CavalierContours kerf/arc stays the Fabrication stratum); `MinkowskiConvolution` COMPLETED (convex-vertex arc-fans + BOTH convolution directions — edges-of-A×verts-of-B and verts-of-A×edges-of-B); `SegmentsCross` (`:440-444`) dies into `Intersection.Apply`; `PlanarOverlayResolve` feeds the arrangement a valid soup (the `CreateFromClosedPolyline`-on-self-intersecting-input hole closes — loops route the arrangement directly, no null-skip); `WavefrontStore` honest mutable arena with amortized-doubling capacity (the 2n under-allocation at `:195` dies); immutable prose dies | `Offsetting.Apply(OffsetOp, Op? key = null) → Fin<OffsetResult>` (Skeleton · Offset(join×end rows) · Medial · Minkowski · Weighted) | out: delaunay (`VoronoiDual`), intersect (`Apply`), predicates, edit (arena law); in: skeleton (growth page), Fabrication via seam; cross-package: `FAB:22` `Toolpath/Skeleton.cs` DIES for `Offsetting.Apply` — the clearance-radius payload + arbitrary-probe query IS the seam payload (`RASM-CS-FABRICATION [V5]` weighted/variable-speed rows ride the existing `Weighted` modality) | 2 |

### Wave 3 — PROCESSING + SOLVING (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 11 | `Solving/solver.md` | MOVE + REBUILD → new at `Solving/solver.md`; deletePages: [`Processing/solver.md`]; absorb {into: `Solving/solver.md`, from: `Processing/solver.md`} | Solving (MINTED) · `Rasm.Geometry.Constraints` · constraints stride. The ONE damped-Gauss-Newton owner: `Iterate`/`Step` GENERALIZED to a residual+Jacobian functor (`Lm.Minimize(ILmModel, SolvePolicy, Op?) → Fin<LmResult>`) over the existing `SolvePolicy` λ-ladder (`LambdaUp`/`LambdaDown`/`StepFloor` — policy already exists, only the residual source abstracts); `ConstraintSolver.Solve` becomes the `Constraint` instantiation; the `ddouble` Σd² objective accumulation lands (`README:86`); phantom `Rasm.Geometry.Topology` at `:13` dies → `Rasm.Geometry.Naming`; dense-LA law = landed `matrix.md` owners (the `register.md` phrasing adopted); preserve verbatim: `LambdaCeiling` singular guard, `SolveReceipt` all-finite gate, witness-rank DOF, bounded reject chain | `ConstraintSolver.Solve(ConstraintSystem, SolvePolicy, Op? key = null) → Fin<Solution>`; functor row `Lm.Minimize(ILmModel, SolvePolicy, Op?) → Fin<LmResult>` | out: `matrix.md` (landed solve rail), predicates; in: fit (functor instantiation), decimate (pattern exemplar — already composes `CholeskyResult.SolveDetailed`); cross-package: `COMPUTE:120` spurious "Geometry solver consumes Compute LM" row DELETED (disproven by `solver.md:303-346`) | 3 |
| 12 | `Solving/fit.md` | MOVE + REBUILD → new at `Solving/fit.md`; deletePages: [`Processing/fit.md`]; absorb {into: `Solving/fit.md`, from: `Processing/fit.md`} | Solving · `Rasm.Geometry.Fitting` · 2428. MLESAC/PROSAC primitive fitting: `Refine` INSTANTIATES `Lm.Minimize` (the `:560-627` re-implementation dies — hardcoded 1e-3/×10/1e12 die; λ threads through `RefineState` on the policy ladder, carry-down on accept); the dead `Consensus` BVH (`:331-336`, built-never-queried, wrong owner) dies — seeded local draw + range prefilter ride the landed `neighbors.md` kd-tree lane (`README:93` lands); `FitStore` (`:246-255`) DELETED (`Candidate` is the live carrier; density-bar prose corrected); `Line` chart de-degenerated (foot-of-perpendicular anchor as `PackPlane` does; `MinimalSamples` 3→2); cloud-PCA vocabulary composes landed `cloud.md` `CloudKernel`/`stats.md` `SampleMoment` (consumer, never a second covariance owner); the `:18` broken-backtick Packages line repaired; `ddouble` Σd² via the functor; preserve verbatim: truncated-cost sampler, adaptive budget, six closed-form minimal solvers, analytic `Distance`/`Gradient` arms | `Fit.Apply(FitOp, Op? key = null) → Fin<FitResult>` | out: solver (`Lm.Minimize`), `neighbors.md` (kd-tree lane), `cloud.md`/`stats.md` (landed); in: `Rasm.Bim` reconstruction (seam), reality-capture dry-run | 3 |
| 13 | `Processing/receipts.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Healing` · healing stride. Typed heal receipts: namespace MINTED; `Converged` for a boolean tests topological SUCCESS (`!b.AssetGated` is not the fix — the gate flag leaves the convergence fold entirely; convergence = the boolean's own success witness); `BooleanReceipt` renames `HealBooleanReceipt` (arrangement keeps the name — one type per name); receipts REGISTER into the landed `IValidityEvidence` validity fold (standalone `Converged`/`Improved` hand-rolls die); `ManifoldStatus.Improved` boundary heuristic fixed (edge-split INCREASES boundary components — `Improved` per-op semantics re-derived); before/after status arrives from the Genus-tolerant total topology seam (row 14); preserve: the 7-case `RebuildReceipt` union, `Of` mint discrimination, `Affected` seed, `ToLog` fold | NONE (receipt vocabulary registering into the validity fold) | out: `intent.md:214` `Topology` projection (landed, kept), `mesh.md` `TopologyReceipt.Project` + the NEW Genus-tolerant `ProjectionRow` (landed Growth extension); in: repair (mints receipts) | 3 |
| 14 | `Processing/repair.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Healing` · healing stride. Predicate-gated heal fold: the SEVERE partiality dies — ONE `ProjectionRow` Growth extension on the landed `mesh.md` `TopologyReceipt.Project` exposes the un-gated `(Euler, BoundaryComponents, IsManifold, IsOriented, Option<int> Genus)` so the heal rail is TOTAL over non-manifold/boundaried/odd-Euler inputs (its own input class; `mesh.md:196-206` Growth law, never a second topology rail); `SelfIntersectResolve` becomes a REAL CDT re-mesh through the substrate — segment-carrying tri-tri crossings (not one point), BOTH faces re-meshed, multi-crossings per face (the `patched.ContainsKey` drop dies) via `Intersection.Apply` + `Tessellation.Build`; topology threads forward (`before[n] = after[n-1]` — the 2N recompute dies); `GapClose` pairs on ORIENTED boundary half-edges (direction recovered from face winding, not index-sorted keys); the `((QueryResult.Hits)hits)` cast routes `Fin`; the four crossing-kernel copies (`:346-372`) die into `Intersection.Apply`; `MeshEdit` donor block EXCISED (arena arrives from `edit.md`); `HealOp.Boolean` stays as a case DELEGATING `Arrangement.Apply(MeshBoolean, …)` (the `BooleanOp` type + implementation live in arrangement); preserve: `HealOp`/`Standard` order, `DegenerateCollapse`/`DominantAxis` sliver logic, `WeldDuplicates` idempotence law (now composed from edit) | `Heal.Repair(HealPlan, Op? key = null) → Fin<HealSession>` | out: edit (arena + weld), intersect (crossings), delaunay (CDT re-mesh), arrangement (boolean delegation), receipts (mints), `mesh.md` (Genus-tolerant row), `api-rhino` `RebuildNormals`-only boundary; in: reality-capture dry-run, Fabrication heal seam | 3 |
| 15 | `Processing/decimate.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Simplification` · 2440. QEM decimation: the VACUOUS flip guard dies — `CollapseValid` compares the exact orientation of each incident face against its ORIGINAL supporting plane (`Orient3D(a,b,c, apex-moved)` against the pre-collapse plane; the `Orient3D(face, face+normal) ≡ Positive` identity can never fire and is removed); boundary link condition admits `shared == 1` (open meshes decimate; spurious `DecimationFault` dies); the maintained vertex-vertex `Adjacency` EXTENDS to a vertex→incident-face incidence index feeding `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` (the O(F²) full-array scans die; O(F log F) drain); the PHANTOM `field.IsoSurfaceDetailed` dies — `Voxelize` composes the REAL static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key)` (`reconstruct.md:609`, prose at `:3,5,16,18,20,553,560` corrected); the `(QueryResult.Nearest)` cast (`:459`) routes `Fin`; `MeshEdit` from edit.md; preserve verbatim: `ddouble` quadric fold (`README:86` row), lazy version-skip queue, `matrix.md`-routed `OptimalPosition`, BVH Hausdorff bound; meshoptimizer/Compute split recorded (Alimer binding is Compute's) | `Simplify.Apply(SimplifyOp, Op? key = null) → Fin<DecimationResult>` | out: edit (arena), `matrix.md` (landed solve), `reconstruct.md` (`IsoSurface.Detailed`), index (nearest via `Fin` rail), predicates; in: reality-capture dry-run | 3 |
| 16 | `Processing/flatten.md` | REBUILD → rebuild | Processing · `Rasm.Geometry.Parameterization` · 2432. Chart-atlas parameterization over the landed DEC handle: Dirichlet pin CORRECTED (large penalty P on pinned diagonals ONLY with rhs = P·value — the 1e-9-on-every-diagonal + value/1e-9 inversion dies); LSCM assembled SPARSE as the conformal-energy least-squares smallest-eigenpair through the `matrix.md` sparse owners (the dense `(2n)²` + `DecomposeSvd` dies); ARAP global assembly O(F) (in-scope face index `f` passed; `IndexOf` O(F)-scan + return-0-on-miss dies); `FactorNonZeros` reports the ACTUAL `CholeskySparse` factor fill (the `D1.NonZeros` wrong-operator dies); `ChartStore.Dead`/`FreeList`/`Kill` DELETED (BFS labelling is the island former); `OrderLoop` boundary walk keyed on oriented half-edges (the min-vertex `next` overwrite dies); `DiscreteCalculus` sparse shape re-verified against landed `spectral.md:191`; the `:18` wrapper-discipline framing adopts the ONE `matrix.md`-owner law phrasing; DEVELOP SEAM declared (ChartAtlas → `develop.md`); preserve: `MeshAdjointSnapshot.Of` composition, per-face Jacobian-SVD distortion, exact `Orient2D` flip guard, seam-cut island walk | `Flatten.Apply(ParamOp, Op? key = null) → Fin<ChartAtlas>` | out: `dec.md`/`matrix.md`/`spectral.md` (landed); in: develop (W4), segment.md capture tier (coexists); cross-package: flatten→Fabrication/AppUi rows (`ARCH` spine) stand | 3 |

### Wave 4 — NEW MESH OWNERS + PARAMETRIC TIER + DRAWING (listed order = dependency order)

| # | Path | Action → lowering | Charter | Entry | Seams | W |
|---|---|---|---|---|---|---|
| 17 | `Meshing/slice.md` | NEW → new | Meshing · `Rasm.Geometry.Intersection` · `SectionFault` 2425. The slice-stack owner: ONE section fold composing `IntersectOp.PlaneMesh` over a parallel-plane family; layer-height POLICY ROWS (uniform · adaptive · variable-by-slope · support-interface — seed rows OPEN per `[GENERATOR_LAW]`; the Fabrication additive lane's next policy is a row); per-layer contours ORIENTED (outer CCW / holes CW — riding intersect's re-founded oriented chains) with typed OPEN-chain rows for non-watertight sections (never silent closure); contour-NESTING TREES emitted as a kernel-owned SoA forest wire (parent/child index arrays — QuikGraph serves in-computation only, per the landed lane law); V10b exactness rides the cheapest implicit sub-family (LPI-3D + projected Orient2D + LessThan ordering; contour orientation/nesting are exact signs; coordinates round at `Polyline` emission) | `Slicing.Apply(SliceOp, Op? key = null) → Fin<SliceStack>` | out: intersect (`PlaneMesh` fold + oriented chains), edit (soup), predicates (nesting signs); in: Fabrication (`FAB:23` `Slicing.cs` DIES; `FAB:48` re-route REALIZED), Compute circulation (`RASM-CS-COMPUTE [V12]`a story-elevation contours through the wire) | 4 |
| 18 | `Meshing/skeleton.md` | NEW → new | Meshing · `Rasm.Geometry.Offsetting` · `SkeletonStalled` (shared stride). 3D MCF curve-skeleton: author-kernel mean-curvature-flow skeletonization COMPOSING the landed machinery (`geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow` case — no permissive engine exists; CGAL GPL rejected stands); per-point CLEARANCE RADIUS + `Clearance(Point3d probe)` arbitrary-probe query as first-class result fields (the `[V10]`d mandate, symmetric with offset.md's 2D medial payload); skeleton topology emitted as a kernel-owned SoA wire (node/edge/radius arrays) | `Skeletonize.Apply(SkeletonOp, Op? key = null) → Fin<CurveSkeleton>` | out: `geodesics.md`/`fields.md` (landed MCF), edit (arena), `mesh.md` (substrate); in: Fabrication (`FAB:22` payload — the weighted/variable-speed + arbitrary-probe demands of `RASM-CS-FABRICATION [V5]` land here and in offset.md) | 4 |
| 19 | `Meshing/remesh.md` | NEW → new | Meshing · `Rasm.Geometry.Remeshing` · `RemeshFault` 2441. The remesh substrate, BOTH tiers landed (floor-maximalist: quad remesh is NOT deferred — panelize composes it): Botsch-Kobbelt isotropic remesh (split/collapse/flip/tangential-relax, genus-preserving) + cross-field-guided QUAD remesh composing landed `segment.md` Knöppel cross-fields/stripes — the AUTHOR-KERNEL robust tier BESIDE segment.md's landed HOST-capture tier (QuadRemesh/`QuadTarget`/`RemeshKind`), coexisting per the capture law with one anchor each; `decimate.md`'s `VoxelRemesh` stays a distinct row (recorded); Compute volumetric discretization names isotropic as its boundary-conditioning pre-step (decoded through the wire, never a Compute-side remesher) | `Remeshing.Apply(RemeshOp, Op? key = null) → Fin<RemeshResult>` (Isotropic · QuadField rows) | out: edit (arena), `segment.md` (landed cross-fields, one anchor), predicates (flip gates), `mesh.md`; in: panelize (W4 backward), Fabrication mesh-prep, `RASM-CS-COMPUTE [V7]` recorded growth | 4 |
| 20 | `Parametric/nurbs.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2448 (shared). THE VENDORED NURBS ENGINE (probe ruling R1): the whole MIT algorithm set owned in-kernel — NURBS-Book evaluation (curve+surface, arbitrary order), the `KnotVector` algebra (public `Refine` both carriers, `Normalize`, periodic/clamped vocabulary), Piegl-Tiller Bezier-decomposed Gauss-Legendre arc-length (1e-6) with `ParameterAtLength`/length inversion, Wang-2008 double-reflection RMF with the closure-correction policy row (#373) + coincident-sample guard, `Fitting` interpolation/approximation for curves AND the Piegl-Tiller SURFACE interpolation/approximation lane (G5 — normal-offset refit substrate), PUBLIC arbitrary-knot construction for both carriers (G1), PUBLIC raw `RationalDerivatives` with the #234 order convention FIXED (G2), fundamental forms I/II + principal/Gaussian/mean curvature as kernel projections over those derivatives (G3), `ClosestParameter` with PARAMETERIZED iteration/tolerance knobs (G7); ingress law: knots NORMALIZED + clamped at construction, canonical bytes hash the normalized form (G6/G10); every construction/fitting throw site wrapped to `Fin` (G9); SoA span/caching wins recorded as the perf law (#391 class) | NONE (evaluation substrate: `NurbsCurveKernel`/`NurbsSurfaceKernel` carriers + knot algebra + evaluators + fitting lane) | out: predicates (exact gates where signs matter), `Rasm.Vectors` (wire types); in: curve, surface, develop (strip evaluation); manifest: GShark pin + `Rasm.csproj:18` + kernel `api-gshark.md` DELETE with this landing | 4 |
| 21 | `Parametric/curve.md` | REBUILD → rebuild | Parametric · `Rasm.Geometry.Parametric` · 2448. Host-neutral curve algebra FULL-BODY (the six stubs at `:116-140` die): the STATION/FRAME family lands as first-class cases — arc-length stations via `Divide(maxSegmentLength, equalSegmentLengths)`/`ParameterAtLength`/`PointAtLength` sweeping BATCH `PerpendicularFrames` RMF over the SpineRef `[T0,T1]` window (`SubCurve` + normalized domain), emitting `(station, frame)` PAIRS — the `PathRow`/`Placement` producer (`RASM-GENERATION-SPEC.md:138,164,173`); `Offset` PROMOTED to a first-class `ParametricOp` case: vendored seed + kernel deviation-refinement loop + self-intersection trim through the `[V4]` crossing lattice (G8); SSI disposition row states the HOST-DEFERRED TRIPLE (surface-surface, surface-plane, curve-surface — probe-widened); re-anchor SPLIT per the dossier: evaluation-peer cites (`:3,20,190`) → `projections.md`, intersection cite (`:170`) → `relations.md`; the anomalous `using Rhino.Geometry` (`:33`) dies (host-neutral thesis holds; `Ray3d` resolves from `Rasm.Vectors`); parametric canonical-bytes emitted through reconciliation `EncodeOp.Parametric` (row 5); borrowed `ParameterizationFault` ends → `ParametricFault` 2448; `ToMesh` trimmed-fill → arrangement `PlanarOverlay` (kept) | `Parametric.Apply(ParametricOp, Op? key = null) → Fin<ParametricResult>` (Evaluate · Measure · Divide · Stations · Split · Reconstruct · Offset · Intersect2D) | out: nurbs (engine), reconciliation (`EncodeOp.Parametric`), intersect (trim lattice), arrangement (`PlanarOverlay`); in: Generation SpineRef (stations/frames rows enter `ARCH:[02]`), patternmap/surface siblings; boundary: ⇄ `projections.md` (evaluation, reciprocal), ⇄ `relations.md` (intersection), ⇄ `locate.md` (never a second location algebra) | 4 |
| 22 | `Parametric/surface.md` | NEW → new | Parametric · `Rasm.Geometry.Parametric` · 2448 (shared). Host-neutral surface ops: UV-PROVENANCED tessellation sampler (kernel-owned UV grid over vendored `PointAt` — per-vertex `(u,v)` beside the `MeshSpace`; the tier-wide UV-pullback obligation lands HERE by construction, `ClosestParameter` the parameterized foreign-point fallback); isoline FAMILIES via `KnotVector.Refine` + `IsoCurve` rows; geodesic POLYLINES composing landed `fields.md` `Geodesic` + `geodesics.md` heat/MMP through the UV pullback (results return to the parametric domain); NORMAL-OFFSET surfaces REAL: Greville-abscissae normal offset + vendored Piegl-Tiller surface refit (R1/G5 strong branch); curvature-domain sampling over nurbs fundamental forms (G3); surface canonical-bytes component through `EncodeOp.Parametric`; host-authored surfaces INGRESS through the public arbitrary-knot construction (G1 closed — SpineRef surface resolution lives) | `Surfaces.Apply(SurfaceOp, Op? key = null) → Fin<SurfaceResult>` (Tessellate · Isolines · Geodesics · NormalOffset · CurvatureSample · Pullback) | out: nurbs (evaluation/derivatives/fitting), `fields.md`/`geodesics.md` (landed), reconciliation (bytes), `mesh.md` (tessellation admission); in: develop, panelize, patternmap (UV-provenanced input law: a world-space polyline with no surface binding cannot feed them), Generation gate | 4 |
| 23 | `Parametric/subdivide.md` | NEW → new | Parametric · `Rasm.Geometry.Development` · `SubdivisionFault` 2433. Subdivision surfaces: Catmull-Clark/Loop as STENCIL ROWS over ONE refinement fold (`[GENERATOR_LAW]` — the next scheme is a data row, never a sibling subdivider); limit-surface evaluation author-kernel (Stam eigenbasis rows); OpenSubdiv stays rejected (no managed binding; admission = P/Invoke burden gated on proven limit-eval-at-scale demand — recorded) | `Subdivision.Apply(SubdivideOp, Op? key = null) → Fin<SubdivisionResult>` | out: edit (arena), `mesh.md` (substrate); in: Generation gate (region subdivision item) | 4 |
| 24 | `Parametric/develop.md` | NEW → new | Parametric · `Rasm.Geometry.Development` · `DevelopabilityFault` 2434. Guaranteed-ISOMETRIC developable-strip unroll — distinct from flatten's low-distortion conformal maps AND segment.md's host LSCM capture (tier boundary stated, one anchor each): geodesic-strip decomposition + ruling-line extraction + exact unroll with a per-strip ISOMETRY WITNESS receipt (the Fabrication sheet/plywood/fabric acceptance reads the witness) | `Development.Apply(DevelopOp, Op? key = null) → Fin<DevelopmentResult>` | out: surface (UV-provenanced strips), flatten (ChartAtlas seam, W3), `geodesics.md` (landed strips); in: Fabrication unroll dry-run (`[05]`b) | 4 |
| 25 | `Parametric/panelize.md` | NEW → new | Parametric · `Rasm.Geometry.Development` · `PanelizationFault` 2435. Cross-field-guided panelization: ONE mapper composing landed `fields.md` `CrossField` + `segment.md` Knöppel globally-optimal fields/stripes + the remesh substrate (row 19); `int symmetry` owns the quad/diagrid/tri family AS DATA; `sample.md` CCVT/blue-noise seeds panel distributions; panel families are policy rows | `Panelization.Apply(PanelOp, Op? key = null) → Fin<PanelResult>` | out: remesh (W4 backward), surface (UV), `fields.md`/`segment.md`/`sample.md` (landed); in: Generation gate (panelization item) | 4 |
| 26 | `Parametric/patternmap.md` | NEW → new | Parametric · `Rasm.Geometry.Patterning` · `PatternFault` 2449. Pattern-to-surface instancing: landed `geodesics.md` tangent LOG/EXP maps + 2D symmetry-GROUP rows (wallpaper groups as data) → surface-mapped INSTANCE STREAM; every instance carries a frame parallel-transported by the landed vector-heat transport (position without orientation is half an instance); the Generation PATTERN/TILING plane's exact input | `Patterning.Apply(PatternOp, Op? key = null) → Fin<InstanceStream>` | out: surface (UV pullback), `geodesics.md`/`transport` lanes (landed); in: Generation gate (pattern-to-surface item; per-instance frames in the `[05]`a dry-run) | 4 |
| 27 | `Drawing/view.md` | REBUILD (ground-up) → rebuild | Drawing · `Rasm.Geometry.Projection` · projection stride. The visibility engine made REAL — ruled choice: EXACT ANALYTIC APPEL QUANTITATIVE-INVISIBILITY (the exact-floor-native branch): invisibility counts updated ONLY at silhouette crossings computed through the `[V4]` crossing lattice — no sampling, no `SampleStep`, zero missed occluder edges (the E1 failure class dies by construction); the dead `BspNode`/`Partition`/`Split`/`Kill`/`FreeList` apparatus DIES with the choice (float BSP splitting mints rounded constructed vertices — the wrong shape beside an exact lattice); the phantom `PaintBackToFront` prose dies; `ToPolylines` chains by successor-linked connectivity (never kind-key concat, never visible+hidden merged); dead `ViewKind.EmitsHidden`/`NeedsBsp` columns consumed by the op table or deleted; `OccludedAt`/`Clip`/`OcclusionBias` die with sampling; soup round-trip collapses onto edit.md; SALVAGE verbatim: exact `FacesOppose` silhouette locus, `Cut` Section delegation to `IntersectOp.PlaneMesh`; select boundary stated on view's side (reciprocal of `select.md:593`); polygon fill routes arrangement `PlanarOverlay` (`README:99`); `README:65` RE-SCOPES in the same motion: "one `ViewOp` over an exact analytic quantitative-invisibility kernel" (the one index-doc re-scope this blueprint takes; `ARCH:63` already accurate) | `View.Apply(ViewOp, Op? key = null) → Fin<DrawingProjection>` (HiddenLine · Silhouette · Section · Creases) | out: intersect (Section + silhouette crossings), index (ray/overlap acceleration for QI seeding), predicates (`Orient3D` signs), edit (soup); in: Fabrication (`FAB:33` Posting BSP HLR DIES; `FAB:46` "beside" clause dies; `FAB:47` `HiddenLineResult` = thin projection), AppUi (`ARCH:91`); documentation dry-run (`[05]`c) | 4 |
| 28 | `Drawing/pack.md` | REBUILD → rebuild | Drawing · `Rasm.Geometry.Encoding` · encoding stride. Residency encoding made REAL: `EncodedStore.Payload` becomes dtype-STRIDED `byte[]` with per-channel byte descriptors; the `System.Numerics.Tensors` seam survives as descriptor-dispatched typed views (`ReadOnlyTensorSpan<float>`/`ReadOnlyTensorSpan<Half>` selected on the descriptor `Dtype` row — element-type-generic span verified at `api-tensors.md:14,29,175-176`; `Half` HALVES residency for real; the widen-back `Pack` at `:53` dies); phantom `using Rasm.Geometry.Topology` (`:33`) + `:18` packages row re-anchor to `Rasm.Geometry.Naming`; `PackKind` gains `field` · `toolpath` case rows with active-channel sets (Compute decodes as the residency case axis `Rasm.Compute/ARCHITECTURE.md:126`; AppHost signature-locks `EncodingKind.Field`/`.Toolpath` on them — two DATA rows, never a downstream encoding owner); the `:3` `OrientedNormals` provenance corrects to cloud→neighbors; PRESERVED VERBATIM: the `SampleDetailed` composition (`:346,463` onto `fields.md:367`) and the `VectorCloudMetric.OrientedNormals` composition (`:463`) | `Encode.Apply(PackOp, Op? key = null) → Fin<EncodedGeometry>` | out: reconciliation (`Encode` keys), naming (`CanonicalTopology`), `fields.md` (`SampleDetailed`, landed), `identity.md` (cloud keys); in: AppHost (`ARCH:92`), Compute residency (`ARCH:93`) — both rows composed in-fence today, preserved | 4 |

## [03]-[SEAM_LEDGER] — corrected, both directions, one anchor per seam

In-kernel rows (arrow = composes; every edge equal-or-earlier wave; intra-wave edges point backward in listed order — verified acyclic in `[09]`): predicates ← ALL; faults ← ALL; edit ← intersect·arrangement·view·decimate·receipts·repair·remesh·slice·skeleton; index ← intersect·arrangement·view·decimate; naming ← reconciliation·pack; reconciliation ← pack·curve·surface; delaunay ← arrangement·offset·repair; intersect ← arrangement·offset·repair·slice·view; arrangement ← repair(`HealOp.Boolean` delegation)·view(`PlanarOverlay`)·curve(`ToMesh` fill); solver ← fit; flatten ← develop; remesh ← panelize; nurbs ← curve·surface·develop; surface ← develop·panelize·patternmap; mesh.md(landed) ← edit two-carrier row + repair/receipts Genus-tolerant `ProjectionRow` (Growth); neighbors.md(landed) ← index routing row + fit MLESAC lane; reconstruct.md/fields.md(landed) ← index GWN one-distance-field lane; geodesics.md/segment.md/sample.md(landed) ← skeleton·panelize·patternmap·develop.

Counterpart-package ledger edits (1-hop ripples, landing with the leg that changes the seam):

| Counterpart anchor | Edit | Leg |
|---|---|---|
| `Rasm.Fabrication/ARCHITECTURE.md:22` | `Toolpath/Skeleton.cs` dies → consumes `Offsetting.Apply` + `Skeletonize.Apply` clearance-radius/arbitrary-probe payload | 2 (offset), 4 (skeleton) |
| `FAB:23` + `FAB:48` | `Slicing.cs` planar section dies → `Slicing.Apply` slice-stack seam ("when realized" promise REALIZED) | 4 |
| `FAB:33` + `FAB:46` | `Posting/Projection.cs` BSP HLR dies; "beside the in-folder BSP solver" clause dies → consumes kernel `DrawingProjection` | 4 |
| `FAB:44` | BooleanOp anchor `csharp:Rasm/Processing/repair` → `Meshing/arrangement` | 2 |
| `FAB:47` | `HiddenLineResult` = thin projection of kernel `DrawingProjection` | 4 |
| `Rasm.Compute/ARCHITECTURE.md:120` | spurious "Geometry solver consumes Compute LM" row DELETED | 3 |
| `COMPUTE:121` | index⇄Compute direction settled: kernel emits `Fin<(float[],long[])>` node-array wire, Compute decodes | 1 |
| `COMPUTE` (new row) | `Predicates ← Rasm.Compute` CDTet Orient3D/InSphere consumer seam (`RASM-CS-COMPUTE [V7]`) | 1 |
| `COMPUTE` (new row) | slice → Compute circulation story-elevation contours (`RASM-CS-COMPUTE [V12]`a) | 4 |
| `Rasm.Persistence/ARCHITECTURE.md:55` | ONE-anchor unification (reconciliation.md:3/:129 + ARCH:79 adopt this spelling); parametric canonical-bytes component feeds `Element/codec.md:151-152` | 1 (anchor), 4 (parametric row) |
| Kernel `ARCH:[02]/[03]` + `README` | placeholder row → `[01]` partition; Parametric consumer fan enters `[02]-[SEAMS]` as rows; `README:65` re-scope (analytic QI); all other E16 rows LAND (see `[05]` V14) | 1, 4 |

## [04]-[VERDICT_DISPOSITIONS] V1-V14

| V | Disposition |
|---|---|
| V1 | RATIFIED at `[01]`: 20 single-folder TIER-2 namespaces, 1:1 fault sub-bands (2 stated exemptions), placeholder row replaced, phantom `Rasm.Geometry.Topology` dead at all 3 sites → `Rasm.Geometry.Naming`, topology→naming cluster rename, `Solving/` minted, Drawing charter ratified "downstream emission" (no rename), 4 namespace-less pages minted, `FAB:44` follows V5. Inbound-reference sweep runs wave 1. |
| V2 | Tier landed at rows 20-26 under probe ruling R1 (vendor-and-own NOW): curve full-body + stations/frames + Offset promoted + SSI triple host-deferred; surface with UV-provenanced tessellation + isoline families + geodesic pullback + REAL NURBS normal-offset; subdivide stencil rows; develop isometric with witness; panelize symmetry-as-data; patternmap with transported frames. Both tier-wide obligations: (a) parametric content identity = `EncodeOp.Parametric` row on the ONE reconciliation seam (row 5), component of `ToCanonicalBytes`, hashing the NORMALIZED form; (b) UV pullback by construction (surface sampler emits per-vertex `(u,v)`; `ClosestParameter` the parameterized fallback). `Parametric/` name kept. `ParameterizationFault` borrow ends → 2448. |
| V3 | View rebuilt ground-up on the ANALYTIC APPEL QI branch (exact-lattice-native; BSP apparatus dies; `README:65` re-scopes in the same motion); connectivity chaining; select.md boundary reciprocal; `PlanarOverlay` fill composed; `FAB:33/46/47` counterpart edits. Salvage: `FacesOppose`, Section delegation. |
| V4 | Crossing collapse EXECUTED BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply` — rows 8-10, 14); implicit-point `Constraint` case LANDS per probe R2 with mandatory companions (hybrid vertex table, construction re-founding, ~21-member family, constrained-only mode, defining-entity carriage); typed 2D restriction on `SegmentSegment`; host-delegation re-anchored to `relations.md` at both cite sites. No honesty re-scope needed — the fences become true. |
| V5 | `BooleanOp` re-homed to arrangement (row 9); `HealOp.Boolean` stays a delegating case; `BooleanReceipt` collision → arrangement keeps the name, receipts mints `HealBooleanReceipt`; Manifold NAMED tier-3 scale companion behind finite `ScaleCeiling` = 1_000_000 combined operand faces, in-house `manifoldc` P/Invoke (NO NuGet pin — the `Manifold` NuGet ID is a name-and-rejected HOMONYM, a CLI/MCP source-generator framework; `ManifoldNET` stays rejected-stalled), golden-boolean fixture gates activation; `[T-BOOLEAN-NATIVE-ASSET]` card disposed (see `[08]`); cycle break COMPLETED by siting `Kernels.WeldDuplicates` + weld knob in wave-1 edit.md (the dossier's partition defect closed). |
| V6 | edit.md (row 6): arena extraction + re-found as true SoA arena, two-carrier seam, soup ×3 collapse + native-dup dedup, weld primitive sited, store-mutability + arena-concurrency contract corpus-wide (all four Meshing SoA stores re-labeled honest; offset 2n sizing fixed), HighPerformance ADMITTED on the arena shapes. |
| V7 | One LM: solver generalizes to `Lm.Minimize` functor over `SolvePolicy` (row 11); fit instantiates with λ threaded (row 12); decimate already exemplary; dense-LA law = landed `matrix.md` owners corpus-wide (flatten's two-vocabulary framing dies); `ddouble` Σd² + QEM accumulation land (`README:86`). |
| V8 | index contract (row 3): PointCloud lifted onto `neighbors.md` in ONE excision (kills E9 kd-dup + E10 throws/silent-empty + E11 counts — one structural cause); outward-rounded bounds; kernel-owned node-array wire composed; GWN single-owner ruling = index owns the accelerated query, reconstruct/fields compose (the ONE distance-field lane = Fabrication ingress); naming/reconciliation fixes (Self, Subsumes, injectivity, Encode composition verified, fixtures harness-gated); ONE Persistence anchor. |
| V9 | All Processing repairs landed as rebuilds (rows 13-16): flatten (Dirichlet, sparse LSCM, O(F) ARAP, honest fill, dead carriers, OrderLoop), decimate (REAL flip guard — the vacuous `Orient3D(face,face+normal)` no-op replaced by original-supporting-plane comparison; boundary collapse; incidence index; phantom `IsoSurfaceDetailed` → real `IsoSurface.Detailed`; second cast), repair (Genus-tolerant TOTAL topology via ONE landed `ProjectionRow` Growth extension; real CDT self-intersect re-mesh; threading; oriented GapClose), receipts (Converged = success witness; validity fold; `Improved` semantics), fit (FitStore deleted; neighbors lane; Line chart; functor). |
| V10 | (a) corner rows `JoinType`×`EndType` author-kernel on the exact wavefront (row 10; CavalierContours = Fabrication lane, pin re-grouped); (b) slice.md (row 17) with OPEN policy rows, oriented contours, typed open chains, nesting forest wire; (c) medial REAL against the delaunay `VoronoiDual` projection (probe: decoupled, all-explicit — lands unconditionally); (d) skeleton.md (row 18) MCF over landed machinery; clearance-radius + arbitrary-probe query first-class across (c)+(d); (e) `Toolpath/Skeleton.cs` dies unconditionally. |
| V11 | Hull two-tier ruling: landed `cloud.md` rail STANDS (standing law) + the exact lifted-paraboloid LOWER-HULL lands as a `Tessellation` projection (row 7) — tier boundary stated on both pages (cloud = host/complex kinds; delaunay = predicate-gated exact fold for robust-grade envelopes). Remesh substrate landed BOTH tiers (row 19 — isotropic mandatory, quad NOT deferred because panelize composes it; deferral criterion moot). |
| V12 | Fault plan at `[01]`: 10 stringly cases re-typed with payload shapes; two-family seam statement lands in faults.md (`ARCH:112` discharged); band plan complete — sub-band shares for every new plane, Tessellation homed 2421-2422, topology→naming rename, BOTH headroom codes spent deliberately (2448 Parametric, 2449 Patterning); NO federation re-plan needed. |
| V13 | pack (row 28): dtype-strided byte store + descriptor-dispatched `ReadOnlyTensorSpan` views (verified viable — no honesty re-scope); phantom import dead; `field`/`toolpath` `PackKind` rows landed for the Compute/AppHost consumers; two landed compositions preserved verbatim; `:3` provenance corrected. |
| V14 | Ledger fence-conformance: index wire, reconciliation identity entry, pack rows all COMPOSED in-fence; Parametric fan enters `[02]-[SEAMS]` as rows; `COMPUTE:120` dies; anchor drift unified (reconciliation triple → ONE; intersect→Fabrication split resolved with V10 rows); false rejection rationales corrected (delaunay:3 honest split; predicates:3 precise claim); strict-IEEE/RID invariant + FMA-free row + PeterO directed-rounding interval filter LAND in predicates (the filter tier doubles as the implicit-family throughput fix per R2); package-boundary `Orient3D`/`InSphere` export declared as the Compute seam row (already public by shape). Index-doc ledger: every E16 row LANDS except `README:65` which RE-SCOPES (BSP → analytic QI). |

## [05]-[EVIDENCE_DISPOSITIONS] E1-E16

| E | Disposition |
|---|---|
| E1 | Row 27: sampling engine + dead BSP + phantom `PaintBackToFront` + kind-key concat all die; analytic QI + connectivity chaining land. Documentation dry-run (`[05]`c of the brief) is the acceptance. |
| E2 | Rows 20-26 close every gate item (table in `[07]`); curve stubs die; Offset promoted; enabling landed primitives composed one level up exactly as the register lists them. |
| E3 | delaunay:3 rationale rewritten (row 7); curve cites re-anchored at FOUR sites `{3,20,170,190}` with the dossier's two-target split (row 21); intersect cites at BOTH sites `{5,388}` (row 8); `index.md:27` upward using dies (row 3); phantom namespace's three sites die (rows 11, 28); 12-reference CSPROJ + float-lane law stand. |
| E4 | flatten Dirichlet + dense LSCM (row 16); naming/reconciliation mis-key via `Self` (rows 4-5); receipts `Converged` (row 13); decimate boundary freeze + O(F²) (row 15). All five confirmed bugs die in rebuilds, not patches. |
| E5 | Namespace ratification `[01]`; dual paradigm dies corpus-wide: every robust-core entry adopts `Op? key = null` threading, `IValidityEvidence` registration, `validation.md` admission vocabulary, with `GeometryFault` staying the fault family (two-family seam stated both sides). |
| E6 | Row 3: upward using + Compute-type return die; node-array wire composed per `ARCH:86`; `COMPUTE:120` deleted. Zero upward edges remain corpus-wide. |
| E7 | Collapse landed by consumers (rows 8-10, 14); medial real (rows 7, 10); fit λ-ladder composition (row 12); arrangement implicit case REAL (row 9 — exact path, not re-scope). |
| E8 | Fabrication duplicates die by counterpart edits: `FAB:33` (view), `FAB:22` (offset/skeleton), `FAB:23/48` (slice). Kernel owner + downstream consumption through declared seams. |
| E9 | LM ×2 → one functor (rows 11-12); GWN ×2 → index owns accelerated query, reconstruct/fields compose (row 3); `BooleanReceipt` ×2 → rename (rows 9, 13); soup ×3 → edit.md (row 6); dense-LA two rules → one `matrix.md` law (rows 11-16); kd-tree ×2 (dossier beyond-register) → PointCloud excision (row 3). |
| E10 | index throws + silent empty + bounds narrowing (row 3); repair cast (row 14); decimate `(QueryResult.Nearest)` cast — dossier beyond-register (row 15); intersect's three casts — dossier beyond-register (row 8). All route `Fin` + typed fault. |
| E11 | BspNode apparatus dies (row 27); `FitStore` deleted + dead Consensus BVH dies (row 12); `ChartStore` carriers deleted + `FactorNonZeros` fixed (row 16); pack false-Half → real strided store (row 28); index counts resync (row 3); offset store prose + 2n sizing (row 10); dead `ImplicitCrossing` carrier dies (row 9); dead `ViewKind` columns + BSP free-list (row 27); dead `IntersectStore.Next` wired-or-deleted (row 8). |
| E12 | Ledger conformance per `[03]`: index wire composed, reconciliation ONE anchor, pack rows preserved, `COMPUTE:120/121` settled, Parametric fan rows enter `[02]-[SEAMS]` (curve.md:192 prose becomes rows), intersect→Fabrication anchor split resolved with V10. |
| E13 | api-hashing:82 rewritten to `XxHash128.HashToUInt128` seed-zero federation truth (wave 1); csparse deduped → shared (kernel copy deleted); tensors RECLASSIFIED overlay-pair KEEP (dossier refuted the dupe claim — the kernel superset is load-bearing); mathnet overlays verified proper subsets, re-verified after the 5.0.0 downgrade; LanguageExt catalog VERIFIED LANDED (dossier: already authored, shared tier = 31 — verify, never re-author). |
| E14 | All 13 version facts feed-verified HOLD; the sweep WIDENS to the 4th beta pin `MathNet.Numerics.FSharp` at `PROPS:467` (dossier drift row); GShark dormancy is the R1 vendor trigger; ManifoldNET stall + `Manifold` homonym recorded; WATCH re-key per `[08]`. |
| E15 | faults 10 stringly cases re-typed (`[01]` payload set); delaunay seven stub bodies transcribed (row 7 — the `[03]` transcription target). |
| E16 | Index-doc ledger: `README:42`/`ARCH:42` implicit-point + BooleanOp cell welds LAND (rows 7-9), native-gate claim re-scoped to the V5 meaning; `README:99` Voronoi dual LANDS (row 7) + `PlanarOverlay` fill LANDS (rows 21, 27); `README:65` RE-SCOPES to analytic QI (row 27; `ARCH:63` accurate — E16 pairing drift recorded); `README:86` ddouble rows LAND (rows 11, 15) + PeterO interval filter LANDS (row 1); `README:93` kd-tree feeds fit LANDS (row 12); `ARCH:112` faults seam statement LANDS (row 2). |

## [06]-[CONSUMER_GATES] — both gates fully homed

Generation KERNEL GATE (`RASM-GENERATION-SPEC.md:179,226`): arc-length stations + RMF frames over `[T0,T1]` → `curve.md` `Stations` case (row 21); surface isolines/geodesics/offsets → `surface.md` (row 22); region tessellation + subdivision → `subdivide.md` (row 23) + `surface.md` Tessellate; developable → `develop.md` (row 24); panelization → `panelize.md` (row 25); pattern-to-surface with per-instance frames → `patternmap.md` (row 26); SpineRef content-key resolution → reconciliation `EncodeOp.Parametric` (row 5) composing landed `ContentHash.Of`, a component of the Element `ToCanonicalBytes` fold; SSI → host-deferred TRIPLE row on `curve.md`/`surface.md` (probe-widened).

Fabrication toolpath gate: corner-strategy offsets → `offset.md` join×end rows (row 10); slice stacks with oriented contours + nesting trees + typed open sections → `slice.md` (row 17); medial-with-clearance-radius + arbitrary-probe query → `offset.md` + `skeleton.md` (rows 10, 18); manufacturing-tolerance booleans → `arrangement.md` managed exact + Manifold scale gate (row 9); developable/unroll with isometry witness → `develop.md` (row 24); hull/stock envelopes → delaunay `LowerHull` projection + landed `cloud.md` rail two-tier ruling (row 7); distance-field ingress → the V8 one-lane ruling (`fields.SignedDistanceFromMesh` → `reconstruct` GWN → index BVH, row 3); HLR → `view.md` `DrawingProjection` (row 27).

## [07]-[DELTA_DISPOSITIONS] — `[03]-[CAPABILITY_ESCALATION]`, all 17 rows

| Plane (now→target) | Homed at |
|---|---|
| Numerics/predicates (9→9.5) | Row 1 — RID/strict-IEEE invariant, precise rationale, FMA-free row, PeterO interval filter, Compute-consumable export seam; PLUS the R2 re-founding (exceeds the floor). |
| Numerics/faults (7.5→9.5) | Row 2 — 10 re-types, two-family seam, band plan, taxonomy = `[01]` partition. |
| Parametric tier (3→9.5) | Rows 5, 20-26 — every delta item incl. canonical-bytes identity, SSI disposition, Offset promotion; vendored engine exceeds the floor (fundamental forms, surface fitting, real normal-offset NURBS). |
| Spatial/index (7→9.5) | Row 3 — PointCloud lift, zero throws, outward bounds, wire composed, GWN single-owner, counts resynced. |
| Spatial/naming+reconciliation (6.5→9.5) | Rows 4-5 — Self, Subsumes, namespaces, injectivity, fixtures harness-gated, `Encode`+identity, parametric bytes on the one seam. |
| Meshing/delaunay (6→9.5) | Row 7 — seven stubs fenced, hull ruling, Voronoi accessor, honest rationale; PLUS hybrid vertex table + constrained mode. |
| Meshing/intersect (8.5→9.5) | Row 8 — collapse by consumers, 2D restriction, slice producer seam, re-anchor; PLUS the chain re-found (the dossier's revise-down defect closed). |
| Meshing/arrangement (7.5→9.5) | Row 9 — implicit `Constraint` (landed, not re-scoped), BooleanOp home, Manifold named + finite ceiling, cycle broken (weld sited wave 1). |
| Meshing/offset+toolpath (5→9.5) | Row 10 — medial real, clearance seam payload, corner rows, store sizing, prose resync; PLUS Minkowski completed + `PlanarOverlayResolve` fixed (dossier beyond-register). |
| Meshing substrate NEW (0→9.5) | Row 6 — arena page, two-carrier seam, soup ×3 collapsed, mutability+concurrency contract, weld primitive sited. |
| Processing/solver+fit (8→9.5) | Rows 11-12 — one generalized LM, fit instantiates, λ threaded, FitStore resolved, Line chart fixed, `matrix.md` law, ddouble Σd², MLESAC via neighbors. |
| Processing/repair+receipts (7→9.5) | Rows 13-14 — real CDT re-mesh, Converged fixed, MeshEdit extracted, threading, collision resolved, triple projection kept + ONE Genus-tolerant `ProjectionRow`. |
| Processing/decimate (7→9.5) | Row 15 — incidence index O(F log F), boundary collapse, ddouble fold, meshoptimizer split recorded; PLUS real flip guard + phantom kill (dossier beyond-register). |
| Processing/flatten (6→9.5) | Row 16 — correct Dirichlet, sparse LSCM via `matrix.md`, O(F) ARAP, true fill, dead carriers, develop seam, `spectral.md:191` verified. |
| Drawing/view (4→9.5) | Row 27 — real analytic engine, connectivity chaining, select boundary, Fabrication consumes. |
| Drawing/pack (7→9.5) | Row 28 — real dtype-strided residency, phantom dead, compositions preserved, field/toolpath rows. |
| Slice/skeleton/hull/remesh NEW (0→9.5) | Rows 17-19 + row 7 hull projection — oriented contours + typed open sections, MCF skeleton over landed machinery, two-tier hull ruling, isotropic + quad remesh beside the host capture tier. |

## [08]-[ROSTER_AND_API] — deltas with `.api` obligations

| Motion | Detail | Leg |
|---|---|---|
| MathNet downgrade | `MathNet.Numerics` + `.Providers.OpenBLAS` + `.FSharp` (+ `.MKL` for co-consumers) `PROPS:62-64,467` → `5.0.0` stable (feed-proven coherent — providers have same-day 5.0.0); beta survives only on a recorded 6.0-only fence member found by the WIDENED survival sweep (landed kernel fences + robust-core rails + Compute/Fabrication co-consumers); both mathnet overlay catalogs re-verify against the downgraded assembly | 1 |
| OpenBLAS DROP | Evidence-backed (no arm64 asset exists — nuget cache lib/-only, catalog self-admits): kernel reference `Rasm.csproj:11` dies; pin `PROPS:64` dies unless a sibling csproj references it; kernel `api-mathnet-providers.md` retires; `matrix.md` provider prose re-scopes managed-only (landed 1-hop ripple) | 1 |
| Fabrication-lane re-group | `CavalierContours:70`/`Clipper2:71`/`SharpVoronoiLib:75` out of the mislabeled "Kernel Geometry" group into a `Fabrication` label; `geometry3Sharp:72` disposition routes to consuming campaigns; post-motion the kernel group holds GShark(until wave 4)/MIConvexHull/Supercluster | 1 |
| Manifold ADD | In-house `manifoldc` P/Invoke (elalish/manifold, Apache-2.0): NO NuGet pin (native asset RIDs recorded at admission); `api-manifold.md` authored kernel-tier WITH the admission, spelling `manifoldc` and NAME-AND-REJECTING the `Manifold` NuGet ID homonym (CLI/MCP source-gen framework) + `ManifoldNET` (stalled 1.0.7-alpha) | 1 (catalog), 2 (gate composes) |
| HighPerformance ADMIT | `CommunityToolkit.HighPerformance` kernel reference added with edit.md's arena composition (`Span2D`/`MemoryOwner<T>`/`ParallelHelper` on frozen/partition-disjoint spans only); shared catalog suffices (multi-stratum) | 1 |
| GShark VENDOR | Wave 1: the six probe catalog corrections applied (dead repo pointer :31 → `GSharker/G-Shark`; `AdaptiveSample`/`AdaptiveSampleRange` public; `KnotVector.Refine` public both carriers; surface `Weights`/`ControlPointLocations` recorded; `DivideByChordLength` returns `List<double>`; wire law :175 READ-only noted). Wave 4: pin + `Rasm.csproj:18` + kernel `api-gshark.md` DELETE with `nurbs.md` landing (one-engine law — no package beside the vendor) | 1, 4 |
| Catalog hygiene | api-hashing:82 rewritten (XxHash128/HashToUInt128/seed-zero/UInt128); api-csparse deduped → shared, kernel copy deleted; api-tensors KEPT both tiers (overlay pair — kernel superset load-bearing); api-rhino:134 VERIFIED corrected (refuted on disk, 2h mtime); api-languageext VERIFIED landed (never re-author); shared tier census 31 recorded | 1 |
| WATCH re-key | geometry4Sharp trigger re-keyed to a post-2022 release signal (1.0.0 on NuGet since 2022-11 — dead trigger); CSparse.Extensions unpublished, TVGL 2019, Kemsekov 3.1.2 2024-02 recorded | 1 |
| Reserves + rejections | DoubleDouble special-function surface + BigRational `Mediant` stay recorded no-consumer reserves; TensorPrimitives non-exact paths only, span-feed law stands (`api-tensors.md:248`); QuikGraph bounded-lane law generalizes (kernel graph RESULTS emit SoA wires: skeleton topology, slice forests, naming migration); named-and-rejected list stands verbatim + `Manifold` NuGet ID added | — |

## [09]-[LEG_PARTITION] — acyclic, engine-invocation rows

Every edge lands equal-or-earlier wave; intra-wave edges point backward in listed order (the page-set tables ARE dependency order). Proof sketch: wave-1 pages compose only landed floors + each other backward (faults←predicates: none; reconciliation→naming ✓; edit→predicates ✓); wave-2 order delaunay→intersect→arrangement→offset has arrangement→{delaunay,intersect} backward, offset→{delaunay,intersect} backward; wave-3 order solver→fit→receipts→repair→decimate→flatten has fit→solver backward, repair→receipts backward, all other edges into waves 1-2 or landed; wave-4 order slice→skeleton→remesh→nurbs→curve→surface→subdivide→develop→panelize→patternmap→view→pack has curve→nurbs, surface→nurbs, develop→{surface,flatten(w3)}, panelize→{remesh,surface}, patternmap→surface all backward; view/pack reach only waves 1-2. Zero upward strata edges (index inversion dead); zero page cycles (repair⇄arrangement broken by V5+V6-weld; delaunay-repair edge is w3→w2 backward).

| Leg | Engine invocation | Targets (dependency order) |
|---|---|---|
| 1 | `Workflow(rebuild.js, {targets: [Numerics/predicates.md, Numerics/faults.md, Spatial/index.md, Spatial/naming.md, Spatial/reconciliation.md, Meshing/edit.md], brief: DECISION})` — kinds: rebuild ×5, new ×1 (edit) | + the `[08]` wave-1 roster motion; V1 ratification sweep (ARCH/README/faults taxonomy + inbound references); fixtures (`0x9462…`, `CLASH_GOLDEN`) harness-gated |
| 2 | `Workflow(rebuild.js, {targets: [Meshing/delaunay.md, Meshing/intersect.md, Meshing/arrangement.md, Meshing/offset.md], brief: DECISION})` — rebuild ×4 | + `FAB:44` ripple; Manifold gate composes |
| 3 | `Workflow(rebuild.js, {targets: [Solving/solver.md, Solving/fit.md, Processing/receipts.md, Processing/repair.md, Processing/decimate.md, Processing/flatten.md], brief: DECISION})` — new ×2 with absorb {into: Solving/solver.md, from: Processing/solver.md}, {into: Solving/fit.md, from: Processing/fit.md} + deletePages [Processing/solver.md, Processing/fit.md]; rebuild ×4 | + `COMPUTE:120` ripple; `mesh.md` Genus-tolerant `ProjectionRow` Growth extension |
| 4 | `Workflow(rebuild.js, {targets: [Meshing/slice.md, Meshing/skeleton.md, Meshing/remesh.md, Parametric/nurbs.md, Parametric/curve.md, Parametric/surface.md, Parametric/subdivide.md, Parametric/develop.md, Parametric/panelize.md, Parametric/patternmap.md, Drawing/view.md, Drawing/pack.md], brief: DECISION})` — new ×8, rebuild ×4 (curve, view, pack + none other) | + GShark pin/catalog deletion; `FAB:22/23/33/46/47/48` ripples; Parametric fan seam rows; the four `[05]` acceptance dry-runs compose |

Residuals resolve per leg before the next launches; docs close in the leg that changes their facts; residuals naming later-leg pages transfer forward explicitly.

## [10]-[CARD_AND_FIXTURE_DISPOSITIONS]

- `[T-BOOLEAN-NATIVE-ASSET]-[BLOCKED]` — DISPOSED into V5: `HealOp.Boolean` delegates `Arrangement.Apply(MeshBoolean, …)`; `NativeAssetMissing` 2423 reserved for the tier-3 scale route; Manifold gate = finite `ScaleCeiling` 1_000_000 + golden-boolean fixture + RID-asset resolution; card closes with leg 2.
- `[HOST_BOUNDARY_REENTRY]-[BLOCKED]` — realization-phase law, OUT of scope, NOT disposed here.
- `[WATCH_PACKAGES]` — re-keyed per `[08]`, leg 1.
- Frozen fixtures — `reconciliation.md:147` digest and `index.md:696` `CLASH_GOLDEN` marked harness-confirmed-once (`XxHash128`/`NodeLinkProjection` reproof) before any page cites them REAL; leg-1 gate.
- Growth rows seeded for the idea pool: `InCircle` implicit combos {IIEE..IIII} + 3D `orient3d`/`inSphere` multi-implicit family (CDTet-gated); exact circumcenter side-of row (medial trim); OpenSubdiv P/Invoke (limit-eval-at-scale-gated); V-HACD/CoACD convex decomposition (consumer-gated); quad-remesh growth absorbed (landed row 19).


