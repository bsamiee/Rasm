---
title: Rasm Strata Leverage
input: full_diff
effort: high
conclusion: neutral
include:
  - "libs/**"
---

# [STRATA_LEVERAGE]

Downward composition is this pass's one question: whether a changed fence composes the owners its stratum designates instead of re-deriving them. Read the touched folder's stratum row in the owning package `ARCHITECTURE.md` strata section before any finding; direction breaches, seam coupling, and placement belong to the architecture-integrity pass and are not re-litigated here — this pass owns leverage alone, and a touched folder is presumed under-leveraged until its composition set is checked against its row.

Hunt these classes, each finding naming the strata row, the designated owner, and the uncomposed or duplicated member:
- Composition gap: a fence re-deriving capability a designated lower-stratum owner carries — a host sub-domain re-spelling the shared commit envelope the document spine owns, a boundary fence hand-rolling a kernel-neutral computation the kernel floor ships — demand composition of the named owner.
- Higher-stratum duplication: an owner minted at a higher stratum for a concern a lower-stratum owner already carries — a second hasher beside the one content-key federation owner is the named drift class — demand the lower owner absorb the concern or the mint delete.
- Wrong-high homing: a shared mechanism homed above the lowest stratum every consumer references — the causal engine of per-folder twins, since consumers that legally cannot reference the high home re-declare the concept under folder-local names; a type or vocabulary re-declaring a lower-stratum owner's concept is that twin, its demanded fix composing the substrate owner as an instance. Per-folder fact unions typing a shared mechanism are the one legitimate per-folder plurality, never the machinery around them, and the homing split is dependency cleanliness — a piece stays high only while it needs a dependency the lower stratum cannot carry, while a consumer blocked on an unlanded higher-stratum surface stays blocked, since seating the awaited composition below its stratum to clear the block is the rejected shortcut.
- Sibling-consumer drift: sibling folders composing one stratum owner through divergent spellings, partial surfaces, or local wrappings, and a layout, grammar, or vocabulary two folders keep identical by hand with no typed owner — demand one composed form, the divergence named at both sites.
- Row-fence misalignment: a fence composing an owner its stratum row does not designate signals the row or the fence is wrong — the finding names both ends and demands the alignment landing in the same change, never a silent divergence.

Settled strata outrank intuition: a composition the owning strata row or a `RULINGS.md` homing row rules is never a finding, and a downward-homing demand pulling a dependency below its lawful stratum names its own refutation. A pass that finds nothing after walking every touched folder's stratum row states that verdict plainly.
