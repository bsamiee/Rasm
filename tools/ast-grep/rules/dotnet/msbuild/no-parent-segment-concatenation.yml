# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: no-parent-segment-concatenation
language: xml
severity: error
rule:
    matches: msbuild-value
    regex: '\$\([^)]+\)[\\/]?\.\.[\\/]'
    # The file-above functions normalize their own result
    not: {regex: 'FileAbove'}
message: The value appends a parent segment to a directory property as text, the path keeps the .. and the hand-written separator
note: Compose the path with $([MSBuild]::NormalizeDirectory('$(Base)', '..', 'segment')) or NormalizePath
