# [CSHARP_BRANCH_RULINGS]

`libs/csharp` rulings settle branch-spanning decisions.

## [01]-[PACKAGES]

- `Silk.NET.WebGPU` and its `Native.WGPU` train are branch substrate — the three-consumer census (`Rasm.AppUi` display rendering, `Rasm.Compute` tensor dispatch, `Rasm.Materials` bake backend) seats the catalogs at `libs/csharp/.api` under the `[04]-[STRUCTURE]` tier law, and each consumer composes the surface behind one narrow folder seam, `Raster/gpu.md` the sole Materials composer; the maintenance-mode train's successor-major swap therefore lands as one row edit per seam owner, never a consumer-wide sweep.

## [02]-[SHAPE]

- `Rasm` is RhinoCommon-aware by charter and the whole branch inherits that host dependence through it — every package the strata table seats above the kernel links RhinoCommon transitively, so the branch builds only against a host bundle; a host-free kernel twin, a `Rasm3dm` substitution, or a claim that a host-neutral package is adoptable off the bundle each contradict `[03]-[UNIVERSAL_VS_CAPTURE]`, which licenses exactly this capture. Host-neutral names the absence of a host-aware flag and a host namespace in a package's own manifest, never freedom from the kernel's bundle. Reopens only on a kernel split proving a host-free numeric floor no consumer re-couples.
- Composition-root surfaces — host boot shells, cache and endpoint registrations, host-side preset tables — home at the app-shell stratum; a host-boundary or parity-source package blocked on the not-yet-landed shell stays blocked, and pulling the surface down into the boundary package to clear the block is the rejected shortcut that seats composition below its stratum.
- Hook-point rosters name `<Package>Point` and never `HookPoint` — the kernel `HookPoint<TFact>` capsule is in scope on every declaring fence, so arity-only disambiguation between a vocabulary row and the mechanism it seats reads as a per-folder twin of the owner; a roster carries its host-truth modality column alone and mints every id through the kernel `HookId` grammar. Host-boundary packages hold no carve — they were the two rosters that wore the owner's name.
- Folder hook registries beside the kernel `HookPoint` are earned solely by plugin-identity grant custody — first-mount-wins seat arbitration the kernel's composition-frozen point mount does not own; a registry that re-keys `(point, scope, token)` is the deleted form, and a grant-custody registry is never deleted as redundant on the strength of a sibling's drop.
- Benchmark-grading AEC peers compile-reference `Rasm.AppHost` legally — admission is by-name string scope, never a reverse type reference, so the up-reference is cycle-safe and exists only for the gate-stamped receipt; an observability-only peer reaches the signal plane through the kernel capsule with no AppHost reference.
- Columnar analytics storage has one custodian — `Rasm.Persistence` alone owns the lake's writers, residence, slots, and serving plane; a producer package joins by handing its typed record-batch schema across the seam as one seam-ledger row, and folder-local columnar storage code in a producer is the rejected form that forks lake custody.
- Cross-stratum causal-frame primitives — `CorrelationId`, `TenantId`/`TenantContext`, `ReceiptEnvelope`, and the `ReceiptSinkPort` record — home at the kernel signal capsule beside `TelemetryContributorPort`: the S2 domain and every app-platform peer emit receipts through them, so an `Rasm.AppHost` home forces an S2-to-spine reference the S1 law forbids or a neutral-`Guid` twin at every emitting package, the per-folder-twin defect the contributor-port move already settled; OTel, HLC, and baggage lacing stays `Rasm.AppHost` `SignalGovernance`, so tenancy stamps an `AsyncLocal` slot and the BCL `Activity` store at S0 while the OTel `Baggage.Current` store registers as one composition `TenantMirror` row — a `Baggage` or `RuntimeContext` type at S0 re-imports the SDK the OTel-free charter excludes, and a process-wide named slot registry faults on a second composition.
- Neutral seam property-bag row-name vocabulary is single-owned by its `Rasm.Element` declarer — writer and reader compose owner-declared `PropertyName` statics, and a projector's ingest-only reader row is either promoted to an owner static the moment any consumer keys on it or carried under an owner-blessed reader-local category; a call-site `PropertyName.Create` spelling in a writer or reader forks the bag's key space between non-referencing packages. Custody has one mechanism: `Rasm.Element` declares `StructuralRows` for every name two packages key on and `PropertyCategory.<scope>.Row(name)` mints each producer's own vocabulary under a prefix that owner blesses, so a roster lands in its own domain without reaching a peer's key space; `PropertyCategory.Seam` carries the EMPTY prefix, keeping every round-tripped IFC row name bare.
- `Rasm.AppHost` IS the branch telemetry composition owner and legitimately holds Serilog, OTLP exporter, SDK, and durable-egress types — the app-neutral library law binds every other package in the branch, where a tier emits `ILogger` and its minted `Meter` alone and no rank carves an exception, since the spine sits at S1 beside the seam the law still binds whole; discriminant: the branch has exactly one seat that can see the resolved consumption profile, the redaction taxonomy, the contributor ports, and the deploy-plane endpoint together, and pushing the lacing above it to an app shell forces every product root to re-derive governance the estate already settled, which is the per-product-twin defect the contributor port exists to delete. Reopens only on a second S1 package earning the same four inputs.
- Per-signal `AddOtlpExporter` is the branch's one OTLP registration form and the cross-signal `UseOtlpExporter` claim verb is declined — discriminant: the claim verb routes reader temporality, reader cadence, log-record processor shape, and `HttpClientFactory` through `OtlpExporterBuilder`, `OtlpExporterBuilderOptions`, and `IOtlpExporterOptions`, all `internal` at the admitted pin, so every per-signal policy value the governance table pins is unsettable behind it and the two registrations throw when composed together; a root taking the claim verb silently accepts package defaults for the whole wire. Reopens only on the builder options turning public.
- Host-boundary log egress is classification-tagged at the boundary — each host-plane package attaches the app-root `DataClassification` values at its `LogProperties` seam, since only the producer knows a payload embeds user content or a host path, while the fail-closed redactor executes at the app root alone; an unclassified boundary log line reaches the redactor invisible and crosses unredacted.
- Spring-motion parity has one source — the kernel `SpringShape` mint (`SpringShape.OfResponse`: response and damping fraction onto the proven admission gate) is the sole spring algebra, `Rasm.AppUi`'s `SpringValue` is its token-facing authoring projection, and a host-side app-root preset table copies the projected members verbatim as values; a host-local re-derivation of the constants is the recurring move that forks motion feel silently on any tuning change, so the host stays a value consumer and the preset table seats under the composition-root homing row. The same source owns every derived physical read — the settling-duration projection at declared epsilon (`SpringShape.Settle`) and the inertial release algebra (`DecayShape`: rate, projected rest, position under way, settling) — so a shell or host names retention and epsilon as VALUES and never carries an envelope inversion or a velocity-decay expression of its own.
- Package self-identity — `TelemetrySource`'s minted package rows — homes at the kernel signal capsule beside the causal-frame band, not `Rasm.AppHost`: the `ReceiptSinkPort` scope seam is string-typed, so an emitter outside AppHost's reach (the S2 fabrication fence composes `TelemetrySource.Fabrication.Key` under a `{Rasm, Rasm.Element}` manifest) hand-spells the id and forks the identity vocabulary; re-seating the roster at AppHost because app identity reads as platform vocabulary is the recurring wrong move; foreign-source rows and OTel resource lacing stay `Rasm.AppHost` `SignalGovernance`.
- Host beat evidence composes the kernel `MonotonicBeat` — the Grasshopper `ClockBeat` projection is the canonical form and the Rhino `PulseBeat` now composes the same kernel beat; a flat host-local beat record re-spelling ordinal and elapsed beside its cadence columns re-mints temporal identity so drift semantics fork between hosts on any timeline change — the foreclosed form both host instances once took; cadence-only columns — interval, drift, missed — extend the composing receipt, never replace the composed evidence.
- Kernel measures leave as bare `double` and `Rasm.Bim` `MeasureValue` is the branch's dimensioned carrier — `MeasureValue.OfSi` wraps downstream, `Rasm.Element` `Properties/quantity` owns the UnitsNet registry end, and unit identity federates at the one `UnitsNet` `BaseDimensions` 7-vector, never the carrier; a kernel quantity band, a second dimensioned carrier, or a UnitsNet type on a cross-runtime wire each fork a decision the `QuantityFamily` wire's SI-scalar payload law already settles.
- Material occurrence-usage rides the seam `Associate` edge's typed `MaterialUsage` payload as input data on each occurrence binding [OCCURRENCE_USAGE_RULING] — a projector minting a parallel usage node, or deriving occurrence usage from the type-level composition, double-owns the material-usage fact across the Bim/Materials seam.
- Tenancy crosses every branch surface as the kernel `TenantId`/`TenantContext` pair and its ONE text — `TenantId.Wire` (`x32`) fixes a fixed-width invariant render `Text` produces and `Of` inverts, so the ambient-store entry, the `rasm.tenant` meter tag, the `set_config` GUC value, an RLS predicate, a durable partition column, an object-name prefix, and an AAD digest input all compare byte-identically and a peer joins a metric series to a store partition without converting a base; a frame slot carrying the raw `Guid`/`UInt128`, a page re-spelling the format, a decimal render, or a `::uuid` provider cast each fork one identity into two alphabets and strand the absent-tenant arm on a zero sentinel `TenantContext.Partitions` already decides.
- Dimension keys are owner-declared consts in the emitting package's dotted namespace — `CorrelationId.Slot` and `TenantContext.TenantSlot` spell the causal frame's halves, each package declares its `rasm.<pkg>.<dimension>` block once, and declaring row and writing fan arm read that one vocabulary so governance derives view tag-keys off the mounted row; `nameof(<Type>)` spells PascalCase against that grammar, and a bare noun at a write site forks on the second package tagging the concept.
- Success shares bind ONE mounted counter partitioned on the outcome dimension its own projection arm stamps, so the good half is a tag value the write that counts the occurrence already carries — a good-half twin counter doubles the series a roster mounts, strands its own denominator on the next arm edit, and re-mints per value the dimension already keys. Good VALUE SETS derive rather than enumerate: a vocabulary owning the verdict publishes an `Items`-derived key set as a column beside its rows, and a population owning its own binary verdict publishes both values as consts the arm and the objective read, so a ninth vocabulary row moves every share with no edit at a consumer. `Ratio` survives for genuinely independent counters alone, `Slo.Admit` proves the partition key against the declared row's own dimensions, and `Breaching <= Total` holds at the minting seam because the sampler filters one population by that key; payload-field matchers are the deleted stand-in that survives a rename sampling nothing.
- Package receipt unions carry one kind vocabulary — the `[JsonDerivedType]` roster projected once at type init, read by the envelope kind a sink stamps, the fan arm-table key, the wire union, and the boot bijection probe; a case-to-literal dispatch beside the annotations is the twin two rosters manufacture, and a per-access re-read lets three readings of one registry drift.
- Typed rails a consumer seam cannot carry outward park on the composing app's evidence cell, never `ignore`d: a kernel delegate returning `void` or `Unit` constrains the seam shape and never licenses discarding the refusal that rail raises, and two compositions hold two cells so a mount defect stays attributable. Seams whose every consumer must park expose the delegate's own defect — `InstrumentArm` returns `Fin<Unit>` and `ReceiptFan.Project` folds it outward, so the capsule's rail-shaped `Observe` parks a refused write beside every other tap fault and no folder mints a refusal cell of its own. Measurement values PROJECTING off a frozen owner relocate to that owner's freeze rather than widening the seam that cannot carry the rail — the capability roster counts the registry's surface index at `CapabilityRegistry.Mount`, so descriptor registration stays rail-free and no mid-composition push publishes a partial census.
- Measurement polarity enforces at one gate, never at a raw cell: `InstrumentSet.Write` is the pushed entry while `InstrumentSet.Level` and `InstrumentSet.Bind` are the pulled pair — a call-site push and an owner-lifetime registration — each resolving the mounted row and refusing on `InstrumentKind.Pulled`; the cell key is `(name, Option<string>)` on ONE entry, so an untagged `Level` call answers a scalar row OR a `Levels` family's unpartitioned entry, a keyed call answers a family key, only a KEY handed to a scalar pulled row refuses, and `Bind`'s untagged probe answers the same two shapes. `LevelCells`'s own writes and registrations stay assembly-internal, so no consuming package reaches an ungated cell. Levels written for a name no observable row projects refuse, never accumulating where no reader samples, and `Bind` hands back the scope that retires exactly its own registration.
- Span custody is the kernel `SpanBand`'s and a library tier owns none — an emitting package declares `TraceScope` rows off its hook-point ids and the composing root admits them through `SpanBand.Of`, so the listener gate, close, fail status, and source lifetime exist once; a folder-local `ActivitySource` or `Traced` wrapper re-mints the app's lacing per package and leaks a scope when a root keeps the set and drops the source. Trace and meter scopes are distinct grammars neither derives from.
- Host-boundary packages resolve host names bare — the boundary's own namespaces re-resolve a partial qualification, an unshadowed host type the prelude cannot reach spells `global::` in full, and a host simple-name collision resolves through one csproj `<Using Alias>` row; a per-fence alias is the forked form two host boundaries re-decide independently.
- `TelemetryContributorPort` carries its contributor's `BoardPack` beside the rows that pack names, and the pack admits against the port's OWN `Declared` roster — `PanelSpec.Admit`, `Slo.Admit`, and `BoardPack.Admit` each take a declaration keyset, `Roster` freezes it and refuses a name carried on both columns, and the argument-free `Admit` runs at `InstrumentFan.Mount` and `AppUiTelemetry.Mount` BEFORE the first meter mints; admitting against a root's mounted set instead proves nothing for a contributor whose per-load-context meter owns its handles, which is a pack provable by nobody, and a composition root reaching a package-specific pack field by name re-mints that lacing at every root and silently skips whichever pack it forgets.
- Analytics datasets declare a temporal CATEGORY and their spine follows it — an EVENT-TIME dataset, whose truth is when the world produced the fact, declares its own observation clock as a spine column the producer stamps; a LANDING-TIME dataset, whose truth is when the custodian admitted it, declares none and reads the custodian's landing clock. Category follows the dataset's own semantics, never a producer's convenience, so a producer mixing both on one dataset or omitting the column on an event-time one silently re-dates every fact to admission and a board joining two datasets on time compares two clocks under one axis. `time` staying optional at the seam is what makes the drift invisible, so the declaration carries the category and the seam refuses a dataset whose category and columns disagree.
- Hook censuses fold into ONE frozen registry — the AppHost `HookRail.Mount` takes each contributing package's `Points` census beside its own declared points, so a composition freezes exactly one audit table and a duplicate id across two rosters dies at the frozen merge; a peer census reaching its own `HookRegistry.Mount` call splits the audit into partial tables where a fired id resolves in neither, and a contributor declaring its point on the AppHost roster inverts the downward contribution the port shape exists to carry.
- An artifact TAXONOMY two strata peers both key on seats at the lowest stratum either reaches — the asset-class axis lives at the retention owner and the read index composes it downward, because a store catalog reaching up for a query-tier roster is the forbidden upward edge the strata law names; the recurring move seats the taxonomy beside its first consumer's index, and re-litigation opens only if the object-plane catalog stops deriving retention from the asset class.
- Every declared hook row lands its FIRE SITE in the same change, reached through a seam the owning rail publishes — a named member per row where the roster is a record of fields, one polymorphic raise over the declared row vocabulary where the roster is a table — so the rail decides which point, which modality, and which rail shape a call takes: a veto row hands its verdict to the caller's own transaction and an observe row carries none. Decoration serves wherever the producer already publishes, keeping that fold free of a hook call. Prose naming a fire site declares vocabulary rather than a plane, and a veto point nothing fires advertises an admission gate admitting everything.
- `Rasm` `Deterministic` is the branch's ONE splitmix64 owner — a package-local mixer, a `System.Random` mint, or a gamma-constant transcription forks replay across runtimes; a package API whose signature demands `System.Random` crosses through `Deterministic.Source` alone. [NOT] a frozen wire constant whose VALUES define a format — a content-defined-chunking gear table keys its own artifacts and re-deriving it re-cuts every stored payload.
- An upstream payload present-but-sealed binds through ONE `[UnsafeAccessor]` capsule at the owning boundary page, pinned to the manifest version — loud on drift, reflection-free, AOT-safe — never a re-parse, a vendored fork, or a second accessor copy; the Bim `[SEALED_PAYLOAD_RULING]` is the landed instance and the class generalizes to every sealed host payload.
- A domain carrier's concrete propagation getter/setter bodies seat beside the CONSUMING egress leg, never inside the propagation owner — the `Rasm.AppHost` `TraceContext` generic `Inject`/`Extract`/`Continue` spine is the one mechanism, its landed adapters (gRPC `Metadata`, MQTT) are transport legs it owns, and every domain carrier (the Persistence NATS/CloudEvents legs, the AppUi `CollabWireContext` carrier) binds its own delegate pair at its consumer; a carrier adapter row pushed into the propagation owner couples the S1 spine to every domain shape above it and re-opens per-carrier edits at a page that owns none of them.
- A bounded wire column's ceiling is admitted at BOTH ends against the same constant, so neither end accepts what the other refuses — the AppUi `SearchLimit` and the Persistence `LimitCeiling` admit one shared bound, and a ceiling spelled once at one end silently forks the first time either edits; the shared constant is the seam's own contract row, never a per-end literal.
- A `[Union]` crossing or persisting JSON carries its own hand-declared `[JsonPolymorphic]`/`[JsonDerivedType]` roster with locked kind literals on the union declaration — the Thinktecture generator emits NO union JSON support (no converter attribute, no derived-type roster; the factory's `CanConvert` declines unions), so a union on a wire without the roster serializes as its abstract base (`{}` per case) with no decode refusal, the silent total-data-loss shape; `[SmartEnum]`/`[ValueObject]` owners need no registration — the generator stamps `[JsonConverter]` on the declaration and an options-level factory is a no-op for them.
- A package's registered member roster is a claim its fences must honor — a member named in a `- Packages:` attribution row with zero call sites is a decorative registration, and the repair is either the composing consumer or a recorded negative stating the structural reason the member stays unreachable, never silent removal of the row.
- App-platform dispatch vocabulary homes at the spine and `Rasm.Compute` composes it downward — `WorkLane` (identity and `Rank`), the `SubscriptionPolicy` cadence thresholds, the capability intent `Spec`, and `HostFingerprint` declare at `Rasm.AppHost` and cross through Compute's legal S3 reference, never the reverse; discriminant: an AppHost `Packages: Rasm.Compute (project …)` row closes the S1-to-S3 cycle the acyclicity law forbids, three surfaces refute it (the AppHost port-only charter, the `AssemblyBoundaries` spec pinning AppHost to the kernel alone, the declared `[PORT]: WorkLane` seam edge), and `tests/contracts/MANIFEST.md` `[02.15]` names the AppHost determinism kernel the `HostFingerprint` minter with Compute's receipts the consumer. Compute keeps every column only its own domain decides: the per-lane `LaneBound` channel table and reader budgets key on the AppHost roster at `Runtime/scheduling`, the cadence `Due` delivery predicate lands as a Compute-side extension over `ProgressMark`, and `ComputeIntent` never crosses downward — Compute admission adopts the AppHost-declared `Spec` whole. Reopens only on a lane, cadence, or fingerprint column no spine concern reads.
- LanguageExt carriers cross System.Text.Json at every stratum through the ONE kernel carrier-space owner — `Rasm/Domain/rails#CARRIER_CODEC` declares `LanguageExtJsonConverterFactory` and its closed converter rows, and each wire MINT registers it: `SuiteContracts.Wire` for the merged suite, the S2 private mints (`Rasm.Persistence` `ElementJson`, `Rasm.Fabrication` `FabricationWireContext`) for their own graphs; discriminant: LanguageExt ships no STJ support and its carriers are structurally unpopulatable, so a bare carrier member is a silent decode failure — the write leg succeeds while every read throws — and an S1 home for the factory strands the S2 graphs whose reference set is `{Rasm, Rasm.Element}`; a per-member `[JsonConverter]` attribute, a mint-local converter, and a second factory declaration are the deleted forms. Emission posture stays the registering resolver's contract — the suite merge omits absence through `OmitAbsent`, the S2 mints write explicit null — and the one `OptionJsonConverter` serves both without a knob.
- A merged wire has ONE producer surface — the composition-bound OPTIONS handle the mint seats (`SuiteContracts.Host`, AppUi `EvidenceOps.Wire`) — and both context-shaped alternatives are probe-refuted: a `JsonTypeInfo` off `.Default` carries the context's OWN options, so the merged resolver's modifiers and registered factories never engage (an omitted-posture `Option<T>` serializes its own struct members there), and a context INSTANCE constructed over the merge rebinds the options' resolver to the context itself, silently dropping every resolver modifier; `.Default` survives only as a merge argument and for type-init metadata roster reads. The predicate is the MERGE — a private context whose converters are attribute-declared (`ComputeWireContext`, `FabricationWireContext`) legitimately serializes through its own context, because its `.Default` options already carry its registrations and it rides no resolver modifier.
- A domain carrier's concrete getter and setter bodies seat beside the CONSUMING EGRESS LEG, never inside the propagation owner — the propagation spine declares the carrier shape and the leg that writes a transport's headers owns the header spellings; instantiated at `Rasm.AppHost` NATS/CloudEvents telemetry and `Rasm.AppUi` `Collab/sync` `CollabCarrier`, binding every future carrier pair.
- `ONE_WGPU_DEVICE` — the branch loads ONE wgpu runtime per process: one `WebGPU.GetApi()` function-table core carrying its one `Wgpu` extension view, every consumer reaching the native surface through that single loaded binary. Device arity is BY ROLE, exactly two sanctioned mints: the PRESENTED device (`Rasm.AppUi` display — adapter matched to the compositor at composition, one device, one queue) that every dispatch-only lane (`Rasm.Compute` tensor dispatch) BINDS rather than re-requesting, and the SURFACELESS BAKE device (`Rasm.Materials` `Raster/gpu#PRESS_DEVICE`, null `CompatibleSurface`) whose throughput-only products never content-address and never share the presented queue's frame pacing. A second runtime load, a second presented adapter/device/queue, a dispatch lane minting its own device, or a bake reaching the presented device are the rejected forms at every consumer.

## [03]-[COLLAPSE]

- Host-boundary machinery twins stay plural — the near-identical Eto marshal, transfer, display, and presence surfaces in `Rasm.Rhino` and `Rasm.Grasshopper` are the strata-forced cost of host-boundary independence: a shared host-facing owner forces the cross-reference the S1 host law forbids or an Eto binding below the host plane, and either package must load and marshal without the other present.
- Instrument-shape bind factories collapse to one kernel capsule owner — `InstrumentSpec` carries the kind roster over the full instrument space (count, delta, advised-or-exponential distribution, reading, total, balance, level, keyed-levels) beside the `MeasureForm` axis that closes each create's measurement type; every per-sink descriptor composes it, the AppUi and Compute per-folder spec records with divergent kind vocabularies are the twins the missing owner manufactured, and a folder re-spelling the counter/gauge/histogram bind bodies re-mints them. Kind rides the row crossing `TelemetryContributorPort`, so a second kind-less row shape strands the governance leg's view derivation.
- Multiwindow burn-rate SLO algebra has one kernel carrier — the four-row window table, the `Sli` family, the objective with its derived budget, and the burn fold mint once at the kernel signal capsule, and per-sink descriptors (AppUi tiles, Compute IaC rows, AppHost health rules, Materials and Fabrication rosters) compose it; hand-typed window constants in two non-referencing packages fork alerting silently on any factor change. One `AlertSeverity` carries `page` and `ticket` alone — the vocabulary the deploy plane's contact rows key on — with rank, hold, tone, urgency, and the escalation walk as its columns, so a rank-ordered incident ladder never re-mints a second severity type; one `PanelKind` carries the eight closed board rows, and an instrument-kind vocabulary wearing the panel name is the collision that ruling deletes. Descriptor CARRIERS collapse with the algebra — `PanelSpec` and `BoardPack` are kernel rows every sink composes as data, so a per-folder panel record with its own admission fold is the twin two non-referencing S2 packages mint independently, its widget re-decided per row against the `PanelKind.For` projection that already derives one, and its name colliding with whatever the folder's own domain calls a panel.
- Instrument families partition by UCUM unit, never by domain case — a closed evidence family whose cases share a unit mounts ONE row per unit and carries the case key as a declared dimension, so ten passport sustainability cases resolve five `rasm.<pkg>.<family>.<quantity>` streams and a case in a landed unit needs no roster edit; the per-case instrument mints one stream per case, strands every case no roster row happens to name, and forks the unit vocabulary across roster and arm. Units that genuinely differ stay separate rows, so the unit is the whole discriminant.
- Streaming-quantile machinery stays three-formed — Compute `StreamMonitor.Quantile` is the P² sketch over the live `ComputeReceipt` stream, the bench statistics (AppUi's tiles and AppHost's `BenchMeasurement` median/p95/IQR alike) are exact nearest-rank over a bounded materialized sample, and the kernel `Distribution.Of` policy row is the anticipated geometry-sample sketch; folding the three on a shared p95/P² reading lossifies the exact bench figure or ships an estimator to a bucket-edge lane, an estimator never replaces an exact bench figure at either bench owner, and a new operational latency-quantile consumer taps `MonitorChannel.RemoteSeconds`, never a new sketch.
- Mounted sets fold over any number of meters — `InstrumentSet.Of(LevelCells, params (Meter, Seq<InstrumentSpec>))` binds every contributor's rows against its own meter into one frozen `ByName`/`Rows`/`Cells` product, so a one-meter package is the one-element call and a composition root mounting one meter per contributor port composes the same entry. Roots re-folding pre-bound `(row, handle)` pairs into a positional `InstrumentSet` construction re-mint the derivation the capsule owns and drifts the moment the record grows a field.
- Dimension-key materialization homes at the write entry — `InstrumentSet.Tags` projects `(slot, value)` pairs into the BCL measurement shape once, so a folder re-spelling that fold mints a per-package copy of the one materialization every arm needs when a tag set spans two writes.
- Raster-encode owners stay plural per stratum — `Rasm.Rhino` `RasterPolicy` owns viewport capture at the host boundary, `Rasm.AppUi` `VisualCodec` owns UI capture at the leaf, and `Rasm.Materials` `RasterCodec` owns content-keyed texture-grade asset encoding at the domain rank; a shared codec owner forces a cross-stratum reference or seats an app concern below its consumers, so a new encoder is earned only by a product class no seated stratum reaches.
- Solar ephemeris has ONE home — the kernel `Rasm/Numerics/calculus#SOLAR_EPHEMERIS` `SolarPosition`/`SolarSite`/`SunPosition` almanac at the higher truncation order (nutation, quadratic mean longitude, pressure-corrected refraction); `Rasm.Compute` daylight, `Rasm.AppUi` light rig, and the `Rasm.Materials` environment frame adapter all compose it, each projecting the ANGLES into its own world frame at its own edge, and the S2-cannot-reference-S3 ban never forced the twin because both prior owners already referenced the kernel — a package-local NOAA/Meeus fold is the deleted form two truncation orders once forked.
- Coherent-noise lattices stay two-formed — the kernel `Numerics/calculus#NOISE_LATTICES` `FieldNoise` hashes the canonical published Perlin permutation feeding `NoiseKind.ContinuouslyDifferentiable` (the `CurlNoise` admission gate and the Lipschitz fold), while the Materials `Appearance/texture#TEXTURE_UV` `ProceduralNoise` family holds FastNoiseLite byte-exactness for MaterialX 1.39 category parity and the WGSL `f32` wrap law with 2D arms, periodic-by-construction cell-index lattices, and the cellular return set; the discriminant is differentiability-gating versus byte-parity, never package availability — collapsing either end breaks the other's gating, and a third lattice family is earned only by a gating axis neither end carries.
- Interchange decoded-geometry has ONE carrier at the seam — `Rasm.Element/Projection/projection#INTERCHANGE_CARRIER` `ImportedGeometry` (with `MeshBlock`/`MeshInstance` and the one `Bake` flatten) is the shape the Bim import rail produces and the Compute tile/residency lane reads, seated per the `STRATA_TWIN` law at the lowest stratum both non-referencing consumers reach; its format column carries the `format#FORMAT_AXIS` row KEY because the S2 format vocabulary cannot seat below its stratum and only the Bim end re-hydrates the row, and its `Uv` and `ColorRgba` LANES fill at the ONE Bim decode so TEXCOORD_0 and per-vertex colour reach the residency arm with no re-import edge; an absent lane is a missing descriptor, never an empty buffer, and a per-channel column beside the arena — like a package-local same-named carrier twin — is the deleted form.
- Colour-space and gamut vocabulary has ONE branch owner and the `Wacton.Unicolour` `Configuration` INSTANCE is the discriminant that forces it: `ConvertToConfiguration` short-circuits on reference equality and the class overrides no value equality, so two instances of one working space adapt through XYZ on every crossing and hold two lazy-conversion caches — the kernel `Numerics/atoms#SCALAR_FLOOR` `RgbProfile` is therefore the branch's sole mint, one row per space carrying its EXPLICIT `DynamicRange` (the package default is `High`, so an inherited range encodes an SDR row at the 203-nit HDR white) and publishing the preset's own chromaticity geometry; a peer-minted `Configuration`, a transcribed primaries or whitepoint table, and a per-folder gamut predicate/projection rename each fork the identity, so `Rasm.Materials` names rows (`PortValue.SceneLinear`, `DisplayEncoding`, `PlanePrimaries`, the AP1 luminance derivation) and keeps only the columns its own containers and observers add. `GamutPolicy` pairs each reproducibility DOMAIN with both its containment predicate and its nearest-in-domain projection, so the RGB strategies and the Pointer and MacAdam volumes read as one vocabulary and a call site spelling `MapToRgbGamut`, `IsInPointerGamut`, or `MapToMacAdamLimits` is the deleted form. Interpolation carries THREE axes on one owner and never their cross-product: the kernel BlendPath row IS the space, the hue traversal rides the polar case's payload, and the viewing condition rides the appearance case's payload — `DeltaMetric` applies the same split to the difference axis — so a rectangular space has no traversal to spell, an unconditioned appearance blend or CAM delta-E is unspellable, and the pairings the package's own mix ignores are unrepresentable rather than dead columns; each row names the RgbProfile row whose Configuration it interpolates under, because the absolute-luminance spaces encode through the PQ inverse EOTF at that row's DynamicRange white and would otherwise inherit whichever operand was the receiver. A row-per-space-and-traversal roster, a traversal argument threaded beside a space, and a kernel rename of the package's four-row HueSpan axis are the three deleted forms. The viewing-condition axis is landed at the kernel — `CamConfiguration` rides the appearance-case payload with the cam slot a parameter of the one `Configuration` mint, never a roster column every row carries — so reopen survives only on a second OBSERVER axis, which seats beside its one consumer rather than widening the roster.
- Per-folder benchmark claim rosters stay plural — the central `BenchmarkGate`/`BenchmarkReceipt` and its field map are the sanctioned collapse, while each folder's closed claim roster (`BimBenchClaim`, kernel `BenchClaim`, `FabricationBench`, the Materials `BenchKernel`/`BenchWorkload`/`BenchInput` roster) stays its own compile-time closed enum; folding them into one parameterized row erases each folder's closed-roster guard and mints a god-enum, so a new benchmark claim lands as a case on its owning folder roster and the central gate reads it through the field map. Compute's `BenchmarkClaim` family — the corpus `BENCHMARK_CLAIM` wire's minting form — admits identically through one field-map row, so the rail's totality survives the wire family with no sibling verdict grammar beside it.

## [04]-[STRUCTURE]

- One package carries one `.api` catalogue at one tier, and the branch `README.md` carve decides that tier ahead of consumer count — every `OpenTelemetry.*` train package keeps a branch catalogue without a registry row, so a single-consumer instrumentation package never moves down on census strength. Every other package resolves on the consuming-`.csproj` census: two folders make it substrate with a registry row, one keeps it folder-local, and a vendor bridge follows its seam partner down.
- Dual-homing one package is the defect the tier law forecloses — the losing file's rows merge into the survivor whole, that file is deleted, and every citing anchor repoints in the same pass; a redirect stub left in its place re-mints the second home under another name. Multi-package catalogues dual-home per surface block: the branch-owned block's rows merge up and the block goes, while the file survives on the packages it alone carries.
- Host SDK assemblies partition by namespace-and-subsystem surface, so the tier law binds the catalogued PARTITION and never the assembly — the branch tier holds the surface every consuming folder crosses and each folder tier holds the subsystem only that boundary reaches, every partition declaring its edge against the substrate it registers. Two folder partitions over one host surface are the folder-local seam `libs/.planning/README.md` `[07]` sanctions; a folder partition re-tabling the substrate carriers at member depth is the dual-home defect, and it repairs to a registration naming the substrate owner beside the carriers the partition alone adds.
- Package tier and README section state one fact — a package the branch registry names appears in every consuming folder's `## [03]-[SUBSTRATE_PACKAGES]` section under the registry's own concern label, and a folder-local package appears in `## [02]-[DOMAIN_PACKAGES]`; carding a substrate package as a folder domain package claims ownership the branch already holds and hides the shared catalogue from the folder's reader.

## [05]-[PROCESS]

- `[Union]`/`Switch` case-collapse projects near-zero net LOC — union declarations and exhaustive arm padding cancel the case savings, so a LOC ceiling falls only by unifying repeated helpers, never by case discrimination or file splitting.
- Projection seam rows enter an `ARCHITECTURE.md` seams map only on a live consumer fence — deferred cross-folder pressure never rides the ledger, and the row re-enters the moment a consuming page names it, never before.
