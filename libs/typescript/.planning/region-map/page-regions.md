# [TS_REGION_MAP_PAGE_REGIONS]

TS region map — row shapes and protocol live in the [suite standard](../../../csharp/.planning/README.md) ledger-protocol section.

[TYPESCRIPT]:

- wire-contracts.md [FINAL]: the byte-to-typed boundary; one shared transport, five decode rails read by codec key, the embedded geometry rail, and the quarantine fold that turns every malformed frame or unknown discriminant into a typed staleness marker.
- state-stores.md [FINAL]: the key-discriminated folds collapsing server-streams and receipt sequences into immutable keyed maps with wire-declared ordering, the envelope carrier binding every structured-text payload, and the staleness-forward and availability-gate posture.
- view-surfaces.md [FINAL]: leaf rendering through the one sanctioned atom binding and the query-string deep-link binding, and the read-only observation routes for evidence, benchmark, geo-series, and collector panels.
- control-edge.md [FINAL]: the browser write edge; the single command gateway over three control verbs and the intent registry keyed for deep-link survival, gated on the availability fold.
- runtime-host.md [FINAL]: the co-hosted SPA root; one Layer graph and runtime providing the five closed app-services, the browser platform layer, the self-telemetry export edge, the build and PWA pipeline, and the browser decode-worker pool.
- node-tier.md [PARTIAL]: the two node-only execution surfaces under one exports root; the resource components and automation lifecycle and policy, the observability-stack provisioning, and the durable cluster-workflow tier with the internal RPC surface, the engine wiring surface on a stage-[2.C] research row.
