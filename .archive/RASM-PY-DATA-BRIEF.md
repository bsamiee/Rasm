# [RASM_PY_DATA_BRIEF]

Authoritative ruling for the ground-up restructure and rebuild of `libs/python/data/.planning/`. `data` is the SECOND folder in the Python dependency sequence (runtime -> data -> geometry -> compute): `.archive/RASM-PY-RUNTIME-BRIEF.md` is finalized upstream law this brief consumes at the seam, and its rulings are LANDED on disk under `libs/python/runtime/` — the identity module (`rasm.runtime.identity`, `evidence/identity.md`), the resilience envelope contract (`reliability/resilience.md`), the `[V5]`/`[V7]` branch-consumer exports (`reliability/resilience.md:232-264`, `evidence/reproduction.md:62`), and the wire/shapes anchors (`transport/shapes.md:211`) are runtime rulings data rewires against, never re-plans. `docs/stacks/python/` governs. The `[05]` acceptance proofs are the closure condition.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level excellent and graph-level broken. Three proofs, each independently fatal to a world-class claim:

1. **The composed module graph cannot import.** Three page-level cycles, two of them eager: `{columnar, lakehouse, query}` form one SCC because the MATERIALIZE composing-concern is mis-homed in the base — `columnar`'s `[04]-[MATERIALIZE]` prelude imports `Lakehouse` and `QueryEngine` module-top (`tabular/columnar.md:442-443`) while `lakehouse.md:35` and `query.md:42` import columnar's `DatasetRef`/`QueryReceipt` back; `contract ⇄ interop` is closed eagerly by contract's COLLECTION prelude (`contract.md:264`) while the SAME page's ADMISSION section `TYPE_CHECKING`-guards the identical edge "to avoid closing the cycle" (`contract.md:183-184`, plus a function-local dodge `:227`) — one module, one guarded edge plus one unguarded, import-order-fragile by its own admission; `catalog ⇄ geospatial` survives only through a lazy `# noqa: PLC0415` dodge (`catalog.md:503` eager, `geospatial.md:591` lazy). The root causes are ownership, not imports: `DerivedSnapshot` composes engines above its module; `FieldShape` is minted by interop (`interop.md:163`) yet declared in contract; STAC-table encode and raster-claim construction are cross-composed.
2. **The dispatch substrate is fiction on ten of sixteen pages.** `from builtins import frozendict` (13 import sites: `columnar.md:21,90`, `query.md:19`, `lakehouse.md:21`, `catalog.md:25`, `geospatial.md:636,769`, `mesh.md:21`, `store.md:19,482`, `virtual.md:23`, `ragged.md:19`, `field.md:141`) raises `ImportError` — `frozendict` is not a CPython builtin (PEP 416 rejected) and no substrate injects it. The corpus canonizes BOTH rails in opposite directions: `ragged.md:16,406` cites `Final[frozendict[...]]` beside the sibling `gridded/store#STORE` tables while `virtual.md:23` imports it with no prose defense at all, and `graph.md:14,18` brands the same form a deleted one and runs `expression.Map.of_seq` (graph 9, profile 12, contract 8, geospatial/mesh MIX both in one page). No owner ruled the folder-level rail; both camps cite "the sibling."
3. **Every identity import breaks the moment upstream lands, and the folder's own module paths drift.** The 14 identity-drifted pages (21 sites; `query.md` rides it transitively; `impact.md` already spells the target) spell `rasm.runtime.content_identity` — the exact drift the landed runtime `[V4]` ruling deletes for `rasm.runtime.identity` (`.archive/RASM-PY-RUNTIME-BRIEF.md`; the rename is executed runtime-side); two sites spell `rasm.data.catalog` against the codemap's `rasm.data.spatial.catalog` (`catalog.md:500` self-import, `geospatial.md:591`); `mesh.md:380` self-imports its own module (`from rasm.data.spatial.mesh import _column` inside mesh); and `catalog.md:624-629` constructs `RasterGeoClaim(..., transform=...)` against an owner that declares no `transform` field (`geospatial.md:372-376`) — a seam that cannot type-check.

The formerly fourth fatal proof — the hollow impact plane — no longer stands: `impact/impact.md` is realized on disk with zero `...` stubs, all four normalization arms real folds, the eight-column self-describing assessment wire, and `TransportResource` a real import. `[V4]` records the realized register as the regression bar.

The corpus also carries: fifteen forbidden `[RESEARCH]` provenance tails (every page but the realized `impact.md` — the `[V11]` ban applies folder-wide); an 896-LOC `geospatial.md` realizing THREE owner planes (`[02]-[GEO]` `:11`, `[03]-[SPATIAL]` `:622`, `[04]-[GRID]` `:759`) against a one-plane codemap charter (`ARCHITECTURE.md:20`) and a two-plane README row (`README.md:14`); a virtual-cube concern split across two pages with the codemap chartering BOTH as the virtualizarr owner (`ARCHITECTURE.md:25` vs `:27`; `virtual.md:28` imports `FieldVirtual` module-top while its own prose claims function-local, `virtual.md:14,16,295`); a `repr(dict)` content-key byte source (`field.md:605`) the corpus's own law forbids (`graph.md:18`) beside the canonical sibling (`virtual.md:205`); a seam ledger blind to all ~15 intra-data import edges and all three cycles; and governance that undercounts the realized surface (the ~40-arm `graph.md` kernel with the `IG_COMMUNITY` split, and the end-to-end lineage projection — `SqlGate.edges` threaded through every `_provenance` arm, `query.md:548-576`, onto the declared `columnar.md:263` field — both realized, neither recorded at figure by ledger, router, or codemap). What IS sound: the tabular plane's density bar (`egress`/`query`/`lakehouse`/`profile` at 9-9.5 — the `_ROUTE`/`_BACKEND`/`_FOLD`/`_PORTABLE`/`_SCHEME`/`_SURFACE` table discipline is corpus-wide), the runtime FIT (a maximal, clean consumer — `ContentIdentity`/`guarded`/`guarded_sync`/`ReceiptContributor`/`TransportResource` composed everywhere, nothing re-minted), and all seven C# seams (content-keyed wire hand-offs, decoded at the boundary, no interior coupling — the folder's strongest dimension). The rot is structural — graph, rail bifurcation, governance blindness — and no per-page cold pass fixes it. Graph first.

### [TELOS]

`data` is the host-free data-interchange plane of the Python branch: typed dataset refs, columnar scan/egress, the transactional lakehouse, cross-engine relational query, the data-contract gate, frame interop with a pyarrow-free Arrow carrier, vector/raster/STAC/mesh spatial, chunked-and-virtual tensor stores, graph payloads, and EPD/LCA impact normalization. Every AEC dataset the platform touches — BIM quantity tables, scan point clouds, simulation tensors, geo context, carbon declarations — crosses through it. World-class here means the folder's OWN tabular bar held corpus-wide: a provable import DAG with a stated topological order; one dispatch rail; every admitted package mined to fence depth (a catalog with no consuming fence is an illusion the roster sheds); every content key over canonical bytes through runtime `ContentIdentity`, never re-minted; every claimed provider arm a realized fold. Assume 5x the consumer demand on every surface: `compute` schedules studies over its frames, C# `Rasm.Persistence` federates its content keys and Substrait plans, `Rasm.Materials` decodes its assessment wire, `artifacts` renders its profiles and palettes, `geometry` exchanges its meshes and point clouds — zero current consumers never lowers the bar. The output bar is the composed interchange: a dataset arrives by ref, scans lazily under one session rail, gates through contract, materializes through lakehouse/query, egresses content-keyed to the object store, and lands on the C# wire — every step railed, receipted, and reproducible.

### [STRUCTURAL_AUTHORITY]

This brief owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — NEW `tabular/materialize.md`, NEW `spatial/query.md` + `spatial/grid.md` (the geospatial triptych), the `field.md#VIRTUAL` sub-domain absorbing into `gridded/virtual.md`, `FieldShape` re-homing into `interop`, `StacGeoClaim`/`StacIngest` re-homing into `spatial/catalog` — are the floor, not the ceiling. Every structural change updates `ARCHITECTURE.md` `[01]-[DOMAIN_MAP]`+`[02]-[SEAMS]`, the `README.md` router and package groups, and BOTH `.api` tiers in the same motion. No page survives on incumbency, and no catalog survives on authorship: a 159-LOC catalog with zero consuming fences is pressure to realize or expel, never evidence of capability.

### [IMPORT_DAG_LAW]

The realized package is `rasm.data.<sub>.<leaf>`; the corrected codemap names the nineteen-module set and the import graph honors one topological order — an edge into a later tier is a defect resolved by re-homing the vocabulary or re-tiering the consumer, never tolerated, and never dodged with `TYPE_CHECKING` guards or function-local imports whose purpose is cycle evasion:

```text
tabular/interop < tabular/columnar
    < {tabular/contract, tabular/egress, graph/graph, gridded/store, gridded/field}
    < {tabular/lakehouse, tabular/query, tabular/profile, gridded/ragged, gridded/virtual, spatial/geospatial, spatial/mesh}
    < {tabular/materialize, spatial/catalog, spatial/query, impact/impact}
    < spatial/grid
```

The chain is the canonical topological ORDER, not an edge list: a fence import binds only a strictly-earlier module; braced peers are mutually import-free and unordered among themselves. Three re-homings and one extraction make it hold: `FieldShape` moves to `interop` (its minter — `interop.md:162`; contract and profile rewire downward, the ADMISSION guard and function-local dodge die); `DerivedSnapshot`/`PartitionBundle` extract to NEW `tabular/materialize.md` (the composing concern above the engines it composes; columnar becomes a pure base importing nothing intra-data); `StacGeoClaim`/`StacIngest` move to `spatial/catalog` (the STAC-table owner geospatial reaches only through the lazy dodge — post-move, `catalog -> geospatial` is the one legal downward claims edge and `geospatial.md:591` dies); `gridded/virtual` absorbs `field#VIRTUAL` per `[V6]`, dissolving the `virtual -> field` edge by ownership. `impact` tiers with the composers, never the bases: its two declared intra-data seams (`ARCH:58` contract gate, `ARCH:59` profile frame) are fence-realizable only with `contract`/`profile` strictly earlier, and its zero intra-data imports today (`impact.md:28-30`) make the placement free — the tier-3 alternative turns `[V4]`'s realized gate hand-off into an intra-brace back-edge. The standing cross-plane edges `spatial/catalog -> gridded/virtual` (`catalog.md:502`) and `spatial/catalog -> tabular/egress` (`catalog.md:504`) are chain-legal and stay. Every page proves its prelude against this order; acceptance is mechanical — the graph re-derives from every fence and the `[00]` order holds over every `from rasm.data.*` import with zero back-edges.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it and unify its substrate. Rosters are seed DATA over a generative shape; the test: adding the next instance is one row, never a second declaration site, a new arm body, or a hand-mirrored literal. Live violations: the dispatch-table SUBSTRATE is bifurcated (`[V2]` — one rail, `expression.Map`, everywhere); graph's weighted algorithms hardcode `weight_fn=float`/`lambda _: 1.0`/`lambda _: 0.0` at eight kernel sites (`graph.md:379-433`) while the prose claims carried path-policy and weight selectors the case payloads do not declare (`graph.md:12` vs `:128-156`); and the DuckDB connect-install-load lifecycle is hand-rolled at three sites where one session rail with extension policy rows belongs (`columnar.md:331,337,345`, `query.md:336`, `geospatial.md:750`). Impact's provider fold is the settled counter-exemplar: one `_normalize` fold, four realized arms, unit and stage correspondences as row data.

### [SEAM_AND_RAIL_LAW]

- ONE receipt owner: `QueryReceipt` (minted in `columnar`, shared by query/geospatial/mesh/catalog/profile) stays the folder receipt; per-plane typed receipts (`EgressReceipt`/`VirtualReceipt`/`RaggedReceipt`/`FieldReceipt`/`ProfileReceipt`/`ImpactReceipt`) carry algorithm evidence and contribute through runtime `ReceiptContributor` — never a second receipt RAIL, never a re-spelled `predicate_count`.
- ONE identity mint: every content key rides runtime `ContentIdentity.of` over CANONICAL bytes — `snapshot.encode()`/sorted-refs joins/Arrow buffers (`virtual.md:205` is the exemplar); a `repr()`/`str()` byte source is a deleted form (`field.md:605` dies). Table-payload keys ride `columnar`'s public canonical Arrow-bytes fold (`combine_chunks().to_batches()[0].serialize()`, `columnar.md:411-415`); interop's `ArrowCStream` is interchange-only and never a key byte source — a consume-once capsule is not a deterministic byte stream. Keys bind the runtime `CANONICAL_POLICY` default (the runtime `[V7]` export; key equality is bytes-law) — a fresh default-equivalent `IdentityPolicy()` allocation is a deleted form (the compute `[V7]` precedent) — while the C#-pinned seed contracts (`ARCH:49,53`) stay the ruled explicit-policy exceptions. The count-prefixed canonical-bytes law (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` item 1) is consumed transitively through runtime's writer: data feeds canonical bytes and never authors a canonical-writer mirror — the C#-side canonical-writer alignment in `Rasm.Element` is its own upstream campaign and this folder is its downstream, not its author.
- ONE retry surface: `guarded`/`guarded_sync` over the runtime `POLICY` rows (`OBJECT_STORE`/`HTTP`/`WIRE`/`RPC`/`LAKE_COMMIT`/`REMOTE_DB`/`STREAMING` are the data-consumed set) — never a hand-opened `stamina` block, never a second envelope; engine-native retry (`obstore` `RetryConfig`, `tensorstore` `Context`) stays the inner layer the stamina row wraps. The upstream contract is pinned in `.archive/RASM-PY-RUNTIME-BRIEF.md` `[V5]` and LANDED at `reliability/resilience.md:232-264`.
- The `ARCHITECTURE.md` seam ledger is enforceable law: every eager intra-data import appears as a ledger edge (the ledger is currently blind to ~15 edges and all three cycles); every cross-`libs/` row has a composing fence (`ARCH:43` — `tabular ← python:artifacts/figures` palette — is declared-unwired: strike or wire); ledger, codemap, router, and fences agree on one module set.
- The `<3.15` scientific band NARROWS on live wheel evidence: the impact cluster (bw2calc 2.5.0/bw2data 4.7/bw2io 0.9.17/bw2analyzer 0.11.8 — pure-python), flox 0.11.2 (pure-python core), and icechunk 2.1.0 (cp312-abi3 stable-ABI) run on 3.15 — icechunk and flox-core lift to module-top imports as their owning pages rebuild, while the impact cluster's function-local posture RE-RATIONALIZES as optional-provider lazy loading (3.15-clean, no version gate; zero fence churn on the realized `[V4]` register). The band keeps exactly: `pyiceberg` (cp313-max native wheels — already the catalog-write fallback under the `[V8]` iceberg-extension promote), `tensorstore` (marker row `pyproject.toml:162`), `premise` (marker row `:174`, unlifted pending resolver evidence), and the numba-accelerated `ReductionEngine` rows (engine-row facts — flox core runs, the accel engines activate on numba cp315). `epdx` is 3.15-clean (cp310-abi3); its macOS/arm64 build rides the Forge scientific lane.

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as minted data and is only decoded here — the C#-owned `XxHash128` seed and count-prefix parity contract (`ARCH:49,53` — the algorithm is shared law, the seed C#-pinned), the `Discipline.Environmental`/`MaterialPropertySet.Environmental` assessment vocabulary (`ARCH:60` — C# mints it; data encodes against it), the inbound Substrait plan bytes on the federation leg (`ARCH:50` — Persistence-authored, executed here, never re-planned), the GLB wire layout the GeoArrow buffers share (`ARCH:54` — `Rasm.Compute` owns the layout law), runtime-admitted `TransportResource` DSNs and credentials (`ARCH:40,44,56`), artifacts `to_corpus_row` records (`ARCH:42`), and geometry scan Arrow point records (`ARCH:46`). What is COMPUTED here under the cross-language parity obligation, never a re-mint — every `ContentKey` over canonical bytes through runtime `ContentIdentity`, the icechunk as-of snapshot identity reproduced from the seed (`ARCH:53`), the normalized EN 15804 cell stream, and all receipts, profiles, and lineage edges (companion-local evidence, minted freely).

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. The roster reconciliation is settled manifest fact: `csvkit` is struck (row and stub — its own charter admitted redundancy with the mined `polars`/`duckdb`/`pyarrow`/`fastexcel` CSV paths; the one non-redundant capability, DBF/fixed-width foreign decode, closes over admitted surfaces per `[04]`); `zlib-ng` carries no data admission — its manifest row is artifacts-owned (`[ARTIFACTS]`, `artifacts/.api/zlib-ng.md`) and data holds zero obligation against it; `brightway2` is struck (row, stub, README roster, prose — a zero-code star-import shell over the four directly-admitted owners; the `bw2setup`/ecoinvent bootstrap is `bw2io` code and `premise`'s bootstrap recital rides `bw2io.bw2setup`); `bw2analyzer` is admitted (row + `data/.api/bw2analyzer.md`, members source-verified against `0.11.8`) carrying per-stage/contribution/uncertainty depth on the Brightway leg; `h5netcdf` is admitted (row + folder-tier stub — the h5netcdf org's h5py-native engine, BSD-3-Clause, the one pure-h5py netCDF-4 implementation), closing the fence-bound-unadmitted inversion behind `FieldEngine.H5NETCDF` (`field.md:69,88,100`); the tag truths hold — `netcdf4` `[DATA]`, `sparse` `[COMPUTE]` (compute's `numerics/array` consumer; no data obligation), `laspy` `[DATA]` (the mesh point-cloud rows `mesh.md:362` with `data/.api/laspy.md`; the geometry brief concurs — its codec-shed seams cede the codec to data). `datafusion` (`pyproject.toml:156`) is the one admitted-unconsumed row and is ruled REALIZED, never expelled: `[V9]` lands `QuerySpec.Federated` (the remaining leg of the Substrait-portability scope; the C# Persistence federation seam is standing design pressure). The `.api` tier motions are landed: `daft`/`numcodecs`/`arro3-core`/`adbc-driver-manager` live at `libs/python/data/.api/` (the branch tier's own law — domain-owned catalogues live in the consuming folder, `libs/python/.planning/README.md:20`); `data/.api/fsspec.md` is authored for the runtime-delegated read-slice; `networkx` and `xarray` stay branch-tier (each carries a second-folder consumer — geometry `graph/features` analysis, compute `DataTree` at `libs/python/compute/.planning/experiments/inference.md:45`) with dual-consumer owner lines. Live obligations: the remaining owner-less folder stubs gain their `owner: data` lines per `[V12]`; the `meshio`/`narwhals` lines gain the compute consumer beside data when the terminal campaign lands its half (`RASM-PY-COMPUTE-BRIEF.md` `[00]`/`[V10]`). The runtime-struck `s3fs`/`gcsfs` rows have their data-side disposition sealed: NO data-tier re-admission — `obstore` owns object transport and `fsspec`/`UPath` the resolution slice; the strike is final track-wide. Paid/gated/proprietary stays rejected.

## [01]-[VERDICTS]

Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; the realized surface may exceed a floor, never dilute it; a hedge names its deciding criteria and resolves by disk evidence, never by preference.

### [V1_IMPORT_DAG_RESTORATION]

The three cycles die via the `[IMPORT_DAG_LAW]` motions — this is the campaign's primary ruling and every other verdict subordinates to it. Ruled: NEW `tabular/materialize.md` owns `DerivedSnapshot`/`PartitionBundle` (the CDC-materialization and partition-bundle concern composing `Lakehouse`+`QueryEngine`+`ScanPlan` downward; columnar's `[04]-[MATERIALIZE]` section and its two engine imports `columnar.md:442-443` move out whole); the canonical Arrow-bytes serialization is `columnar`'s PUBLIC `arrow_bytes` fold (landed at `columnar.md:410`; impact's `wire()` already composes it) that materialize composes — additionally a branch-consumer export: geometry `energy/simulate` keys its energy result frames through it at the consumer edge (`RASM-PY-GEOMETRY-BRIEF.md` `[V1]`, the crossing's named upstream) and compute's experiments render-frame projection rides the same fold (`RASM-PY-COMPUTE-BRIEF.md` `[V10]`, the artifacts render seam), so the public fold never narrows to intra-data callers. `interop` owns `FieldShape` (it constructs it, `interop.md:158-162`); `contract`/`profile` import it downward; contract's `TYPE_CHECKING` guard (`contract.md:183-184`), function-local dodge (`:227`), and eager COLLECTION import (`:264`) all resolve to one legal module-top prelude. `spatial/catalog` owns `StacGeoClaim`/`StacIngest` (the STAC-table encode it already owns at `[03]-[TABLE]`); `geospatial` narrows to vector/raster claims + egress formats and its lazy `catalog` import (`geospatial.md:591`) dies; `RasterGeoClaim` GAINS the `transform: tuple[float, ...]` affine field the AssetFold construction proves demand for (`catalog.md:629` vs `geospatial.md:372-376`) — the claim owner extends, the constructor stops passing a phantom. Deleted forms: any function-scope import whose purpose is cycle evasion, any `TYPE_CHECKING` import of a runtime-constructed symbol, any second drain of the same vocabulary. Acceptance is mechanical: the `[00]` order holds over every `from rasm.data.*` import with zero back-edges.

### [V2_DISPATCH_RAIL_UNIFICATION]

`expression.Map` (`Map.of_seq`) is THE dispatch-table rail; every `from builtins import frozendict` dies — all 13 sites, all 10 pages — and every `Final[frozendict[...]]` table re-types as the corpus's own `Final[Map[...]]` convention (`graph.md`'s `RX_CENTRALITY`/`IG_COMMUNITY`, `egress.md`'s `_ROUTE`, `profile.md`'s tables are the standing exemplars). Every prose carrier of the literal rewrites in the same motion: `ragged.md:16,406` canonize the rail beside the sibling `gridded/store#STORE` tables and die with it, and the five prose sites that spell the importable statement while branding it a deleted form (`graph.md:14,18`, `contract.md:18`, `interop.md:17`, `geospatial.md:16`) re-phrase to the contrast; `geospatial`/`mesh` unify their mixed usage. Deleted forms: any `from builtins import frozendict` import, any `Final[frozendict[...]]` or module-level mutable `dict` table, any third rail. Acceptance: `rg 'from builtins import frozendict' libs/python/data/.planning` returns zero — all 13 imports dead and every prose carrier (`ragged.md:16,406`, `graph.md:14,18`, `contract.md:18`, `interop.md:17`, `geospatial.md:16`) rewritten; deleted-form prose names the anti-pattern by the `Map`-vs-frozendict contrast, never the importable statement.

### [V3_IDENTITY_AND_CANONICAL_BYTES]

Three mechanical truths land corpus-wide — (a) and (b) on every owning page as it rebuilds, stragglers fixed in place wherever found, (c) with the `[V6]` absorb. (a) The identity rename: `rasm.runtime.content_identity` -> `rasm.runtime.identity` at the 21 standing sites across 14 pages (`impact.md` already spells the target) — the runtime `[V4]` ruling this folder makes load-bearing at scale. (b) The module-path drift: `rasm.data.catalog` -> `rasm.data.spatial.catalog` (`catalog.md:500`, `geospatial.md:591` — the second site dies entirely with `[V1]`'s re-home); `mesh.md:380`'s same-module self-import dies (the POINTCLOUD prelude binds its own module's names directly). (c) Canonical bytes lands with the absorb: `field.md:605`'s `repr(var.data.manifest.dict()).encode()` content-key source — physically inside the `field.md#VIRTUAL` block `[V6]` moves whole into `virtual.md` — is replaced by a canonical manifest wire (the per-variable chunk-reference rows serialized deterministically: sorted path/offset/length encoding, the `virtual.md:205` `snapshot.encode()`/joined-refs discipline) in the absorbed manifest owner as that absorb lands; the `graph.md:18` prohibition becomes folder law: no `repr()`/`str()`/hash-of-object byte source anywhere.

### [V4_IMPACT_REALIZATION]

The plane is realized on disk — one page under the ~450 signal, the ruled single-page outcome; the realized register is the regression bar and any dilution of it is a defect. `_from_openepd` folds all THREE `ScopesetByNameBase` containers — `Impacts` (`RootModel[dict[LCIAMethod, ImpactSet]]` -> `get_impact_set(method).get_scopeset_by_name(indicator)` per-stage ScopeSet cells) plus the direct `decl.resource_uses`/`decl.output_flows` halves — through the `OPENEPD_FIELD`/`OPENEPD_STAGE` correspondences with `Measurement.rsd -> Spread` per cell, keyed `(decl.id, decl.version)` (`.api/openepd.md` [03]-[05]; an `Impacts`-only fold leaves the pere/penre/fw and hwd/nhwd/eee families unreachable while the enum claims them). `_from_epdx` threads `INDICATOR_UNIT` per cell through `EPDX_FIELD` (the one spelling drift: `MFR -> "mrf"`) — openepd's per-indicator `ScopeSet` subclass pins its own unit, epdx's `ImpactCategory` carries NONE, so the `Indicator` vocabulary carries the canonical EN 15804 unit as row data every arm reads, one table, never a per-arm literal, the 28-member row set covering the EN 15804+A2 core, resource-use, and output-flow families end to end (the GWP fossil/biogenic/luluc split included); a method reporting an indicator the table lacks surfaces as a vocabulary gap at admission, never a silent empty unit. `_from_score` solves `prepare_lca_inputs` -> staged `lci`/`lcia`/`score` with the method unit off `bd.Method(...).metadata`, Monte Carlo one draw per `next(lca)` into `Spread(kind="std")` (`use_distributions=True` — the confirmed `bw2calc` substrate), and `bw2analyzer` `annotated_top_processes` into typed `ContributionRow`s — per-stage contribution and uncertainty mined, never a single aggregate `a1a3` cell — with `premise_background` the same arm keyed by scenario tuple over a premise-shifted database (`.api/bw2calc.md`, `.api/premise.md`). `_from_olca` runs the full `calculate -> wait_until_ready -> get_total_impacts -> dispose(finally)` lifecycle with the `categories` correspondence faulting on a vocabulary gap (`.api/olca-ipc.md`). `ImpactCell` carries `Option[Spread]` — the mined spread has its typed carrier slot. `TransportResource` is a real fence import (`impact.md:37`) with `fetched` the EC3/openLCA transport leg (the `impact ← python:runtime [PORT]` seam, `ARCH:56`, composed not claimed). The `Rasm.Materials` assessment wire is the fence-pinned SELF-DESCRIBING eight-column floor — `source`/`method`/`indicator`/`stage`/`amount`/`unit`/`declared_unit`/`content_key`, column names and dtypes fixed in the fence — with `wire()` composing the `columnar` public `arrow_bytes` fold: content-keyed canonical Arrow bytes, the `Discipline.Environmental`/`MaterialPropertySet.Environmental` DECODE contract (`ARCH:60`), composed at the consumer edge, never a new intra-data egress import. epdx is settled from the live `0.3.0` distribution: `convert_ilcd(json)` is the sole Rust-core export (no `as_type`); parse failure is `pyo3_runtime.PanicException` (BaseException-rooted — the fence guards by qualname; no `ParsingException` exists in this line); the model field spelling is `mrf`. Standing obligation: the `ARCH:58` contract-gate and `ARCH:59` profile-frame hand-offs compose downward as fence imports once `contract`/`profile` land in the `[00]` chain — impact tiers above both precisely so the declared gate and quality seams are realizable, never comment-flow.

### [V5_GEOSPATIAL_TRIPTYCH]

`spatial/geospatial.md` (896 LOC — three owner planes against a one-plane charter, `ARCHITECTURE.md:20`) splits into the triptych, tabular-precedent parity: `spatial/geospatial.md` keeps the claims plane (`VectorGeoClaim`/`RasterGeoClaim`+`transform`/`EgressFormat`, the `_CONSTRUCT` table, coverage transforms) PLUS the deferred `[COVERAGE]` cluster — `rioxarray` bridging bare-ndarray `CoverageResult` to CF `DataArray` and closing the raster-WRITE gap (`.rio.to_raster(driver="COG")` — `EgressFormat` today has no COG output); NEW `spatial/query.md` owns `SpatialQuery`/`SpatialEngine` (the DuckDB-spatial join/index/H3-SQL engine, `[03]-[SPATIAL]` `:622-758`) over the `[V8]` session rail; NEW `spatial/grid.md` owns `GridSystem` (the h3ronpy/S2 DGG plane, `[04]-[GRID]` `:759-1089`) with `engine_bin` (`:966`) re-homed to grid binding spatial/query downward — the two-H3-substrate boundary stated as law (in-DB binning = spatial/query; in-frame vectorized = grid). The ingress-deepening scope lands with the claims page: pushdown vector ingress (`pyogrio` `read_dataframe`/`open_arrow` with OGR-side where/bbox/mask/sql), geodesic `pyproj` ops, `shapely` `polygonize`/linear-referencing, and the native GeoArrow buffer path via `geoarrow-rust-compute` replacing the `to_parquet` byte-roundtrip for `EgressFormat.GEOARROW` (the `Rasm.Compute` GLB wire share, `ARCH:54`). `StacGeoClaim`/`StacIngest` leave for catalog per `[V1]`. Codemap and README declare all three planes in the same motion. The plane's frames are additionally downstream-consumed law: the artifacts diagram plan-geometry ingress reads room/zone/parcel/footprint polygons and circulation paths as typed coordinate columns on the tabular adjacency frame (`.archive/RASM-PY-ARTIFACTS-BRIEF.md` `[V15]`), and artifacts `visualization/table` reads QTO/schedule rows over the same interchange — the typed-column vocabulary and the native GeoArrow path are consumer exports, never internal conveniences.

### [V6_VIRTUAL_MANIFEST_OWNERSHIP]

The virtual-reference cube concern unifies on `gridded/virtual.md` — the codemap ALREADY charters virtual.py as "virtualizarr manifest parsers + icechunk set_virtual_ref" (`ARCHITECTURE.md:25`) while chartering field.py's "virtualizarr/h5py virtual leg" in the same breath (`:27`): the double charter resolves by the absorb. `field.md` `[04]-[VIRTUAL]` (`:374-651` — `FieldVirtual`, the 8-row `VirtualParser`, `CFDtype`, `ManifestExport`, the h5py `VirtualLayout` native path) moves into `virtual.md` whole; `gridded/field.md` narrows to the CF owner (read/select/reduce/egress — `FieldDataset`/`FieldEncoding`/`FieldSelection`/`ReductionPolicy`/flox); the `virtual -> field` module edge dissolves; `catalog` rewires its `FieldVirtual` import to virtual (`catalog.md:501`). The near-identical manifest vocabulary collapses: `ManifestExport` (field) and `ManifestWrite` (virtual) become one export/registration axis on the unified owner. The `field.md:605` canonical-manifest key lands here per `[V3]`. `gridded/store.md`'s five standing `[04]-[RESEARCH]` entries (`store.md:663-669`) are SETTLED and fold per `[V11]` with the store rebuild: [ZARR_PIPELINE] confirmed at zarr 3.2.1 — `create_array` keyword-only slots in `filters -> compressors -> serializer` order, the eight cited `zarr.codecs` members real, the `ArrayArrayCodec`/`ArrayBytesCodec`/`BytesBytesCodec` split real, `Array.nbytes_stored` a SYNC method (the engine-symmetric `data.nbytes` choice stands); [ZARR_FILTERS] narrowed — `zc.ScaleOffset` is `(*, offset=0, scale=1)` (the `dtype`/`astype` slots belong to `numcodecs.FixedScaleOffset`) and the numcodecs filter family's live home is `zarr.codecs.numcodecs.*` (absorption landed; `numcodecs.zarr3` is the deprecated spelling — fences bind the absorbed home); [CUBED_LINALG] confirmed at cubed 0.28.0 — `matmul`/`svd`/`qr`/`svdvals`/`tensordot`/`outer`/`vecdot`/`matrix_transpose`, `Callback`/`TaskEndEvent` with `peak_measured_mem_end`; the two tensorstore entries are source-verified (tensorstore is manifest-gated off 3.15, reflection impossible by construction) — `Transaction(atomic=False, repeatable_read=False)` with `commit_async()`/`commit_sync()` and the `.atomic` accessor, kvstore root drivers exactly `file`/`gcs`/`http` (read-only)/`memory`/`s3`/`tsgrpc_kvstore` with adapters `kvstack`/`neuroglancer_uint64_sharded`/`ocdbt`/`zarr3_sharding_indexed`/`zip`, and NO `azure` driver (an azure fence path narrows to the confirmed surface). `virtual.md`'s `[PERSISTENCE_VERSION_WIRE]` cross-runtime snapshot-seed reproduction (`:293-301` tail) composes the LANDED runtime `evidence/reproduction` `ParityReceipt` rail (`libs/python/runtime/.planning/evidence/reproduction.md:62`) as its consumer seam — recorded against the real rail, never hand-proven here.

### [V7_GRAPH_HARDENING]

`graph/graph.md` is the dispatch exemplar with three defects. (a) Weight parameterization: the weighted case payloads (`shortest_path`/`bellman_ford`/`astar`/`k_shortest`/`floyd_warshall`/`min_cut`/`spanning_tree`/`steiner_tree`/`max_weight_matching`) gain a `WeightSelector` policy value (default: float-payload identity) replacing the eight hardcoded `weight_fn=float`/unit-lambda sites (`graph.md:379-433`) — the prose-claimed "path policy"/"weight selector" (`:12`) becomes carried data against payloads that declare none (`:128-156`), and a non-float edge payload stops being unweightable. (b) GPL boundary: the unconditional module-top `import igraph` (`:27`) contradicts the page's own "GPL-confined to the community split" charter (`:3,18,526`) — ruled: igraph binds function-local inside `_run_ig` under the page's existing gated-import discipline, making the confinement structural; the whole-module-GPL alternative dies. (c) Depth truth: the realized ~40-arm kernel and the `IG_COMMUNITY` split are corpus fact the governance surfaces record at full figure per `[V12]`; the genuinely-unmined rustworkx residue (isomorphism/bisimulation, `generators`, DOT/Matrix-Market IO, group centrality, edge coloring — `.api/rustworkx.md`) either lands as rows in this leg or the page's Growth row names exactly that residue as the deferred set — never an unstated gap, never a stale "~11 of 40" figure anywhere.

### [V8_DUCKDB_SESSION_AND_LAKEHOUSE_ROWS]

ONE request-scoped DuckDB session owner lands on `tabular/columnar` (the folder's scan base — already the DuckDB-heaviest page): connect + extension policy rows (`httpfs`/`spatial`/`h3`/`substrait`/`iceberg`/`ducklake` as seed data, install-vs-core-load as a row property) + `register_filesystem` threading, composed downward by `query`, `materialize`, `spatial/query`, and `lakehouse` — the three hand-rolled connect-install-load sites (`columnar.md:331,337,345`, `query.md:336`, `geospatial.md:750`) collapse onto it; request-scoping stays law (no module global, `columnar.md:76`). The scan/query boundary states itself in the same motion: `ScanPlan` sources refs/globs/wire rows, `QuerySpec` binds frames — `ScanPlan.DuckDb(sql, projection)` (`columnar.md:180`) narrows to source-scoped SQL-over-ref or re-homes to query — deciding criterion: the standing arm binds NO admitted source (`con.sql(sql)` free-form, `columnar.md:331-333`, the one arm never touching `source`/`dataset.ref`); an arm rebuilt to bind only the admitted ref's relation stays scan, and SQL naming its own sources is `QuerySpec.Sql`'s concern. Two lakehouse capabilities ride the session rail: `TableFormat.DUCKLAKE` (one row + one `_apply_ducklake` arm — `ATTACH 'ducklake:<dsn>'`, versioned reads, `table_changes`, maintenance CALLs; the DSN through runtime `TransportResource`) and the DuckDB-iceberg promote (`_apply_iceberg` primary path through the core-loadable iceberg extension; `pyiceberg` demotes to the `<3.15` catalog-write fallback). The `pyiceberg` write arm additionally stops DISCARDING `_partition_by` — partition-spec/transform authoring (`PartitionSpec`/`BucketTransform`/`YearTransform`, `.api/pyiceberg.md`) is mined or the discard is stated as a format capability row, never silent.

### [V9_QUERY_FEDERATION_AND_PROVENANCE]

`tabular/query.md` completes its one admitted-but-absent capability and truth-closes its landed one. `QuerySpec.Federated(sql, sources)` lands over `datafusion` `SessionContext` + `register_object_store` + async streaming to Arrow, and the Substrait interchange leg is BIDIRECTIONAL because the `ARCH:50` seam is (`⇄`): outbound, `substrait.Serde.serialize_bytes`/`Plan.encode` mint the portable plan bytes Persistence consumes; inbound, a Persistence-authored plan arrives as BYTES and executes here through `Serde.deserialize_bytes` -> `Consumer.from_substrait_plan` -> the session's Arrow stream — data executes foreign plans, never re-plans them (all four codec members catalog-confirmed — the `substrait` codec table rows, `data/.api/datafusion.md:151-157`). The received or minted plan bytes are additionally the federated query's identity: `ContentIdentity.of` over the plan bytes stamps the `QueryReceipt`, giving the `ARCH:49` reuse ledger a recompute-dedupe key — a federated run with no plan-keyed receipt is invisible to the ledger (the remaining half of the Substrait-portability scope; the DuckDB-substrait half is landed, `data/.api/duckdb-substrait.md`; the 159-LOC catalog stops being dead weight or the package leaves the manifest — realization is the ruled default). `QueryReceipt.lineage_edges` is REALIZED on disk — `SqlGate.edges` (`query.md:135-176`) threads lineage through all four `_provenance` arms (`:548-565`) onto the receipt fold (`:575-576`) and the declared `columnar.md:263` field — settled corpus truth the build preserves, extending the projection only to the new `Federated` arm; `sqlglot` `diff`/`Edit` records as the query-version-change adjacency row in the catalog, mined only when a consumer names it. The plan-bytes channel is RULED by the landed Persistence corpus: content-keyed and AT-REST — Persistence retains the Substrait protobuf bytes as the wire (`Rasm.Persistence/.planning/Query/federation.md:15`, retention never re-serialization), keys them through the kernel `ContentHash.Of` (`federation.md:3`), and content-addresses results into the `ArtifactKind` reuse row (`Query/cache.md:22`); no live servicer or serve plane exists in the landed corpus, and the seam ledger names `python:data` the plan PRODUCER (`Rasm.Persistence/ARCHITECTURE.md:71` — `Query/federation ← python:data`, the gate this verdict's producer leg discharges). data's fence MINTS outbound plan bytes as the primary leg and decodes inbound bytes channel-agnostically at rest; a serve-plane servicer is not a ruled channel and is never authored here.

### [V10_INTEROP_STREAM_DEPTH]

`tabular/interop.md` holds its landed producer promotion and completes the depth beyond it. The consumer-ahead-of-producer inversion is CLOSED on disk — `ArrowCStream.of` is the public construction classmethod (`interop.md:170-171`, composed at `:130`) and `gridded/ragged` composes it cleanly (`ragged.md:28,351`); a regression re-privatizing the symbol is a defect. The remaining depth lands here: streaming consumption via `nanoarrow.c_array_stream` (chunked, no full materialization); schema-only negotiation (`__arrow_c_schema__` against the now-local `FieldShape`); the `Backend` axis lifts from the five eager rows (`interop.md:40-44`) to the full narwhals lazy set (DUCKDB/IBIS/DASK lazy rows with `_LOWER` branching eager-vs-lazy on the Implementation level); the C Device Data Interface row LANDS — the member set is confirmed at nanoarrow 0.8.0: `nanoarrow.device.CDeviceArray` (carrying `__arrow_c_device_array__`) and `nanoarrow.device.c_device_array(obj, schema=None)` plus `Device`/`DeviceType`/`DEVICE_CPU`/`cpu`/`resolve`, all under the `nanoarrow.device` submodule (no top-level spellings; no device-array-STREAM constructor exists, so the device row is array-level and the stream row stays the non-device `c_array_stream`).

### [V11_RESEARCH_PURGE_AND_PAGE_GRAMMAR]

The fifteen standing `[RESEARCH]` tails are zero-provenance defects and die (`graph.md:546`, `field.md:730`, `ragged.md:398`, `store.md:663`, `virtual.md:293`, `catalog.md:635`, `geospatial.md:1090`, `mesh.md:562`, `columnar.md:528`, `contract.md:471`, `egress.md:418`, `interop.md:196`, `lakehouse.md:417`, `profile.md:390`, `query.md:579`; `impact.md`'s tail is already purged); the finished corpus carries no appendix tier, and no page or governance surface states anything about that tier in either polarity. These tails are NON-empty load-bearing member/arity confirmations, so the ruling is purge-and-fold, never delete-blind: genuinely load-bearing member confirmations fold into the owning page's `Packages` block or the package's `.api` catalog (the catalog is where member truth lives); "SETTLED — confirmed against the live X.Y.Z distribution" framing, version pins, and freshness narration delete; the once-unsettled items (`store.md`'s five, `virtual.md`'s `[PERSISTENCE_VERSION_WIRE]`) are SETTLED per `[V6]` and fold as settled member truth in their owning blocks, never provenance prose. The run-on Boundary walls (`virtual.md:18` is one ~40-line sentence) break into scannable deleted-form clauses; each invariant states once in its owning block. This is the folder's largest clean prose reduction (~800-1200 lines), applied to every page as it is touched.

### [V12_API_TIER_AND_GOVERNANCE_TRUTH]

Both `.api` tiers and all three governance surfaces (`ARCHITECTURE.md`, `README.md`, the root manifest) become exact. The tier motions are landed fact per `[00]`: `daft`/`numcodecs`/`arro3-core`/`adbc-driver-manager` live at the folder tier (the branch law `libs/python/.planning/README.md:20`; the codec pair reunited with `zarr`; runtime's `resilience` module-top adbc imports die under runtime `[V5]`'s `_named` hook); `networkx` and `xarray` stay branch-tier with dual-consumer owner lines (geometry `graph/features` the networkx analysis lane, `libs/python/geometry/.planning/graph/features.md:404-469`; compute the xarray `DataTree` co-consumer); `pyarrow`'s owner line names geometry `scan/ingestion`'s boundary-scope `arrow_las` `pyarrow.Table` decode as the seam consumer of record (`RASM-PY-GEOMETRY-BRIEF.md` `[04]` the demanding consumer; decode-only, never a key or egress path, so the catalog stays folder-tier); `data/.api/fsspec.md` is authored for the data read-slice (`UPath.fs` + `register_filesystem`, `columnar.md:340`); `dask.md` lives at the branch tier with both consumers on its owner line (flox's dask tree strategies, odc-stac's lazy cubes, and `[V10]`'s DASK lazy row for data; four compute pages co-consume) and the superseded `compute/.api/dask.md` is removed. Live tier motions: the seven remaining owner-less folder stubs (`ibis-framework`/`meshio`/`nanoarrow`/`narwhals`/`pandas`/`pandera`/`polars`) gain `owner: data`; `data/.api/fsspec.md`'s runtime-delegated `transaction`/`cat_ranges`/block-cache/`FSMap`/`rsync` surfaces resolve as mine-or-decline rows — each surface either gains a consuming fence arm or the stub records the decline; `egress.md`'s cross-tier anchor (`:420` citing `runtime/.api/obstore.md` for the exceptions taxonomy `data/.api/obstore.md:145-157` carries) retargets to the folder tier. Governance: the codemap redraws to the nineteen-module set (materialize, spatial/query, spatial/grid in; field/virtual charters un-doubled); the seam ledger gains the intra-data import edges as declared rows (the SCC-blindness closes); `ARCH:43` (artifacts/figures palette inbound) STRIKES — no consuming fence exists, and the edge re-enters only with a data-side consumer arm; the README router grows to nineteen rows; governance truth lands at full figure — the graph row records the realized ~40-arm kernel and the community split, the query row the lineage projection, the interop row the public carrier construction, the impact row the realized five-source plane, and every landed capability is recorded by the surface that routes to it in the same motion, stale figures corrected wherever they survive.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — rows are re-verified against disk before use, never taken as the current state. Anchors are `libs/python/data/.planning/` relative; `ARCH:` anchors `libs/python/data/ARCHITECTURE.md`; `pyproject.toml:` anchors the repo root manifest.

| # | Class | Evidence |
|---|---|---|
| E1 | Tabular SCC | MATERIALIZE prelude imports engines module-top `tabular/columnar.md:442-443`; back-edges `lakehouse.md:35` (`DatasetKind`/`DatasetRef`), `query.md:42` (`QueryReceipt`/`predicate_count`); root = composing concern in the base module |
| E2 | contract⇄interop | eager `contract.md:264` vs `TYPE_CHECKING` guard `:183-184` + function-local dodge `:227` in ONE module; `interop.md:31` imports the `FieldShape` it mints at `:162` |
| E3 | catalog⇄geospatial + arity | eager `catalog.md:503`; lazy dodge `geospatial.md:591`; `catalog.md:624-629` passes `transform=` to a `RasterGeoClaim` declaring none (`geospatial.md:372-376`) |
| E4 | Rail bifurcation | 13 `from builtins import frozendict` sites (`columnar.md:21,90`, `query.md:19`, `lakehouse.md:21`, `catalog.md:25`, `geospatial.md:636,769`, `mesh.md:21`, `store.md:19,482`, `virtual.md:23`, `ragged.md:19`, `field.md:141`); prose carriers of the literal: `ragged.md:16,406` (canonizing), `graph.md:14,18` (branding it deleted), `contract.md:18`, `interop.md:17`, `geospatial.md:16`; `Map.of_seq` camp graph(9)/profile(12)/contract(8)/egress/interop/geospatial/mesh |
| E5 | Identity + path drift | 21 `rasm.runtime.content_identity` sites on 14 pages (`query.md` rides transitively; `impact.md` already spells `rasm.runtime.identity`); `rasm.data.catalog` at `catalog.md:500`, `geospatial.md:591` vs codemap `ARCH:21`; same-module self-import `mesh.md:380` |
| E6 | Impact realized | `impact/impact.md` — zero `...` stubs; four real arms over all three openepd containers; 28-member `Indicator` + `INDICATOR_UNIT` table; `ImpactCell` carries `Option[Spread]`; the eight-column self-describing wire with `wire()` over the public `arrow_bytes` fold; `TransportResource` imported (`:37`) with `fetched` (`:252`); identity import already `rasm.runtime.identity` — the `[V4]` register is the regression bar; residue: the `ARCH:58`/`ARCH:59` downward composition lands with `contract`/`profile` |
| E7 | Geospatial over-scope | 896 LOC; three planes `geospatial.md:11,622,759`; codemap charters one (`ARCH:20`); README names two (`README.md:14`) |
| E8 | Virtual split concern | `field.md` `[04]-[VIRTUAL]` `:374-651`; `virtual.md:28` module-top import vs own function-local prose claim `virtual.md:14,16,295`; double codemap charter `ARCH:25` vs `ARCH:27`; `ManifestExport`/`ManifestWrite` twin vocabulary; `repr(dict)` key `field.md:605` vs canonical `virtual.md:205` |
| E9 | Graph defects | module-top `import igraph` `graph.md:27` vs GPL-confined charter `:3,18,526`; hardcoded weights `:379,382,385,387,393,417,433` vs prose-claimed selectors `:12` against payloads `:128-156` |
| E10 | RESEARCH tails | 15/16 pages (`impact.md` purged): `graph.md:546`, `field.md:730`, `ragged.md:398`, `store.md:663`, `virtual.md:293`, `catalog.md:635`, `geospatial.md:1090`, `mesh.md:562`, `columnar.md:528`, `contract.md:471`, `egress.md:418`, `interop.md:196`, `lakehouse.md:417`, `profile.md:390`, `query.md:579` |
| E11 | Tier residue | landed: `daft`/`numcodecs`/`arro3-core`/`adbc-driver-manager` folder-tier, `data/.api/fsspec.md` authored, `dask.md` branch-tier, `networkx`/`xarray`/`pyarrow` owner lines corrected; live: 7 owner-less folder stubs; cross-tier anchor `egress.md:420` |
| E12 | Roster reconciliation | landed: `csvkit`/`brightway2` struck, `zlib-ng` artifacts-owned (`[ARTIFACTS]` row + `artifacts/.api/zlib-ng.md`), `bw2analyzer`/`h5netcdf` admitted with stubs, `netcdf4`/`laspy` `[DATA]` + `sparse` `[COMPUTE]` retags, `rustworkx` `[DATA_ARTIFACTS]` (`pyproject.toml:85`); live: `datafusion` `:156` admitted-unconsumed, CLOSED by `[V9]`'s `QuerySpec.Federated` (the design-page `datafusion` hits are deltalake's embedded engine) |
| E13 | Governance under-records | ledger blind to ~15 intra-data edges and all three cycles (sole intra-data rows `ARCH:41,58,59`); `ARCH:43` declared-unwired; realized surfaces recorded nowhere at figure — the ~40-arm graph kernel + `IG_COMMUNITY`, the lineage projection (`query.md:548-576`, `columnar.md:263`), the five-row interop `Backend` axis (`interop.md:40-44`), the public `ArrowCStream.of` construction (`interop.md:170-171`) |
| E14 | Settled fence assumptions | `store.md:663-669` five entries SETTLED per `[V6]` — zarr 3.2.1 `create_array`/codecs confirmed, `zc.ScaleOffset(*, offset, scale)` corrected, `zarr.codecs.numcodecs.*` the live filter home, cubed 0.28.0 linalg + `peak_measured_mem_end` confirmed, tensorstore `Transaction(atomic, repeatable_read)`/`commit_async()` + root drivers file/gcs/http/memory/s3/tsgrpc_kvstore source-verified (no azure driver); `virtual.md` `[PERSISTENCE_VERSION_WIRE]` (`:293-301`) composes the landed runtime `ParityReceipt` rail |
| E15 | Session triplication | `duckdb.connect()`+extension lifecycle at `columnar.md:331,337,345`, `query.md:336`, `geospatial.md:750`; boundary blur `ScanPlan.DuckDb` `columnar.md:180` vs `QuerySpec.Sql` |
| E16 | Producer promotion landed | `ArrowCStream.of` public on the producer (`interop.md:130,170-171`) and composed by the consumer (`ragged.md:28,351`) — the inversion is closed; `[V10]` holds it and completes the streaming/negotiation depth above it |

Sound surfaces (preserve; only the deltas a verdict names change): the realized `impact/impact.md` plane (E6), `tabular/egress.md` (9.5 — the `_ROUTE` reference page; rail + anchor retarget only), `tabular/profile.md` (9.5 — rail only), `tabular/lakehouse.md` (9.5 core — V8 rows additive), `tabular/query.md` (9 core — V9 additive), `gridded/ragged.md` (9 — rail + V10 resolution), `gridded/store.md` (9 — rail + E14 settlement), `spatial/mesh.md` (9.5 — rail + self-import), `spatial/catalog.md` (9.5 content — V1 motions), `graph/graph.md`'s `_run_rx`/`_as_rx` kernel and `GraphResult.frame` seam, `contract.md`'s `CheckKind`/`FrameCovenant` depth, the corpus-wide table-driven dispatch discipline, the runtime FIT (maximal clean consumer — nothing re-minted), and all seven C# seams (`ARCH:37,49,50,53,54,60,61` — content-keyed, decode-only, no interior coupling).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| tabular/columnar | 7 | 9.5 | MATERIALIZE out; session rail + extension rows; public canonical Arrow-bytes fold; scan/query boundary stated; rail |
| tabular/materialize | — | 9.5 | NEW: DerivedSnapshot/PartitionBundle over lakehouse+query+columnar downward; CDC discipline carried intact |
| tabular/interop | 8 | 9.5 | FieldShape owned; `ArrowCStream.of` public; lazy backend rows; c_array_stream streaming; schema negotiation; device row gated |
| tabular/contract | 7 | 9.5 | one-way imports (guards die); covenant/quality depth preserved; rail |
| tabular/query | 8 | 9.5 | `Federated` real over both Substrait codec directions with plan-keyed receipts; lineage held and extended to `Federated`; rail |
| tabular/lakehouse | 8 | 9.5 | DUCKLAKE row; iceberg-extension promote with pyiceberg `<3.15` fallback; partition-spec authoring or stated capability row; rail |
| tabular/egress + profile | 9.5 | 9.5 | cold; anchor retarget; rail confirm |
| spatial/geospatial | 9 content / 6 structure | 9.5 | triptych split; `transform` field; COVERAGE cluster (rioxarray, COG write); pushdown ingress; native GeoArrow path; StacIngest out |
| spatial/query + grid | — | 9 | NEW owners from the split; session rail composed; engine_bin boundary law; DGG plane governance-visible |
| spatial/catalog | 7 | 9.5 | StacGeoClaim/StacIngest absorbed; path drift dead; cycle dead; AssetFold arity legal |
| spatial/mesh | 8 | 9.5 | rail; self-import dies; cold otherwise |
| gridded/store | 8 | 9.5 | rail; five RESEARCH entries settled or narrowed |
| gridded/virtual | 9 | 9.5 | absorbs the manifest sub-domain; one manifest vocabulary; canonical manifest key; parity obligation recorded |
| gridded/field | 7 | 9.5 | narrowed CF owner (~450 LOC); flox/select depth preserved; rail |
| gridded/ragged | 9 | 9.5 | rail; producer promotion consumed |
| graph | 8 | 9.5 | WeightSelector payloads; structural GPL confinement; residue mined or named in the Growth row |
| impact | 9.5 | 9.5 | landed — four realized arms over all three openepd containers, unit row-data law, uncertainty-slotted carrier, TransportResource wired, self-describing wire fence-pinned, bw2analyzer depth; residue: the `ARCH:58`/`ARCH:59` fence composition with `contract`/`profile` |
| governance (ARCH/README/`.api`) | drifted | exact | 19-module codemap; ledger sees the import graph; router complete; tier law enforced; realized figures recorded |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate, the folder tier for domain packages — a misplaced catalog is a defect per `[V12]`):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Block`/`Map` as the one ADT and dispatch spine (`[V2]` completes it), `anyio` task-group/memory-stream at every concurrent seam (the `columnar` CDC discipline is the exemplar), retry ONLY through runtime `guarded`/`guarded_sync` over the `POLICY` rows (never bare `stamina`; engine-native retry stays the inner layer), `msgspec.Struct` one-shot admission at ingress, `beartype(conf=FAULT_CONF)` on public entries, one span per fold through the owned aspects. The corpus already models this weave; regressions are defects, not style.
- `openepd` — `Impacts` RootModel -> `available_methods`/`get_impact_set`/`get_scopeset_by_name` per-stage cells PLUS the `ResourceUseSet`/`OutputFlowSet` containers (the resource-use and output-flow indicator halves) and the per-indicator `ScopeSet` subclass units; `OpenEpdApiClientSync`+`DefaultBundleReader` under the wired `TransportResource` leg (`data/.api/openepd.md`; `[V4]`).
- `bw2calc`/`bw2data`/`bw-processing`/`bw2io` — staged `lci`/`lcia`/`score`, `MultiLCA` demand×method spread, the COO datapackage substrate, the Monte Carlo iteration protocol (`use_distributions=True`, one sample per `next()`) feeding the `[V4]` uncertainty slot; mined past the single-aggregate-cell floor (`data/.api/bw2calc.md`).
- `bw2analyzer` — admitted per `[00]` (row + stub): contribution/per-stage analysis realized on the brightway arm — `annotated_top_processes` into typed `ContributionRow`s; hold the depth.
- `olca-ipc` — `Result.get_total_impacts()` rows; the `setup -> calculate -> wait_until_ready -> query -> dispose` lifecycle as the fence shape (`data/.api/olca-ipc.md`).
- `premise` — IAM-scenario background shift feeding `_from_score` as data, never a fourth solver arm (`data/.api/premise.md`).
- `epdx` — the realized ingest arm's substrate held explicit: `convert_ilcd` (the one Rust-core entry, ILCD+EPD JSON -> EPDx JSON) + `epdx.pydantic` `EPD`/`ImpactCategory` stage-module model; `ImpactCategory` carries NO unit — the `[V4]` Indicator unit-row law exists because of this asymmetry; parse failure is `pyo3_runtime.PanicException` (BaseException-rooted, guarded by qualname); the `<1.0` pin stands on the 1.x wheel break (`data/.api/epdx.md`).
- `datafusion` — `SessionContext`/`register_object_store`/async Arrow streaming plus BOTH Substrait codec directions (`Serde.serialize_bytes`/`deserialize_bytes`, `Producer.to_substrait_plan`/`Consumer.from_substrait_plan`, `Plan.encode`) into `QuerySpec.Federated` (`data/.api/datafusion.md` — 159 LOC ready; `[V9]`).
- `duckdb` — the session rail + an `[EXTENSIONS]` catalog section (httpfs/spatial/h3/substrait/iceberg/ducklake SQL surfaces — the iceberg-promote leg's own obligation) (`data/.api/duckdb.md`; `[V8]`); two extension catalogs ALREADY stand and the section anchors them, never re-catalogs: `data/.api/ducklake.md` (`ATTACH 'ducklake:'`/`table_changes`/the `ducklake_*` maintenance procedures) and `data/.api/duckdb-substrait.md` (the `get_substrait`/`from_substrait` connection twins — the landed DuckDB half of the Substrait-portability scope).
- `pyiceberg` — partition-spec/transform authoring (`PartitionSpec`/`BucketTransform`/`YearTransform`), `ManageSnapshots` branch/tag; demotes to `<3.15` catalog-write fallback under the iceberg-extension promote (`data/.api/pyiceberg.md`).
- `deltalake` — the unmined governance tail (`add_constraint`/`TableFeatures`/`create_checkpoint`/deletion vectors) records as mine-or-decline rows with the lakehouse leg (`data/.api/deltalake.md`).
- `pylance` — the `TableFormat.LANCE` arm holds (`import lance` + `LanceMode`, `lakehouse.md:27,47,388`); the version settlement is LANDED (`lakehouse.md:420-421` `[LANCE_VERSION]: SETTLED` / `[LANCE_INDEX]: SETTLED`, reflection-verified; the catalog carries the full surface): the multimodal surfaces MINE with the lakehouse leg — version tags/branches (`LanceDataset.versions`/`tags`/`branches`/`checkout_version`, `data/.api/pylance.md:78-80`), `merge_insert(on) -> MergeInsertBuilder` (`:64,:95`), and the vector/FTS index rows (`create_index` IVF_PQ/IVF_HNSW_*, `create_scalar_index` INVERTED/BTREE/NGRAM, `:75-76,94`) — with the write format pinned as a `data_storage_version` policy row, never an ambient default.
- `daft` — the streaming read-scan frontend holds (`_DAFT_READ` lakehouse-format rows + `_daft_scan`, `query.md:488-498`, re-typed per `[V2]`); the Ray-runner and multimodal `DataType` surfaces stay DECLINED under the companion single-node charter; the catalog lives at `data/.api/daft.md` per `[V12]`.
- `rioxarray` — `open_rasterio`/`.rio.reproject`/`.rio.to_raster(driver="COG")` into the `[COVERAGE]` cluster (`data/.api/rioxarray.md`; `[V5]`).
- `pyogrio`/`shapely`/`pyproj`/`geopandas` — pushdown ingress (`read_dataframe`/`open_arrow` where/bbox/mask/sql), `polygonize`/linear-referencing, geodesic ops, `to_arrow` (`[V5]`).
- `geoarrow-rust-compute` — the native buffer path for `EgressFormat.GEOARROW` sharing the C# GLB wire layout, replacing the `to_parquet` roundtrip (`data/.api/geoarrow-rust-compute.md`).
- `rasterio` — the raster-claim decode and `RasterioError` transient taxonomy hold (`geospatial.md:41,372-379`); `WarpedVRT`/windowed-boundless reads/`MemoryFile` record as mine-or-decline rows with the triptych claims page (`data/.api/rasterio.md`; `[V5]`).
- `polars-st` — the `st` `GeoExprNameSpace` (predicate/measure/overlay/transform rows over WKB geometry columns) plus the `from_wkb`/`from_shapely`/`from_geopandas` lifts land with NEW `spatial/grid.md` — the frame-native geometry vocabulary beside h3ronpy's vectorized cell ops (`geospatial.md:9,759`); the LGPL-2.1 dynamic-linkage flag stays recorded (`data/.api/polars-st.md`; `[V5]`).
- `xarray-spatial` — the terrain/hydrology/focal/zonal surface rides the one standing hold: a hard `numba` core dependency with numba capped at cp314 (numba cp315 the named activation); the stub is the standing capability record satisfying acceptance (e) (`data/.api/xarray-spatial.md`).
- `nanoarrow` — `c_array_stream` chunked streaming into `[V10]`; the consume-once capsule discipline holds (`data/.api/nanoarrow.md`).
- `pyarrow` — the canonical-bytes substrate (`combine_chunks().to_batches()[0].serialize()`, `columnar.md:411-415`), parquet/dataset egress, and the PyCapsule `__arrow_c_*` trio hold; `pyarrow.substrait` stays DECLINED — `datafusion` owns the federation codec, one Substrait executor never two (`data/.api/pyarrow.md`).
- `arro3-core` — the CDC drain (`load_cdf` -> `arro3.core.RecordBatchReader.read_all()`, `columnar.md:422,477`) and the `list_with_delimiter(return_arrow=True)` egress Table (`egress.md:165,171`) hold; the catalog lives at the folder tier per `[V12]`.
- `ibis-framework` — the `parse_sql`-to-bound-`IbisTable` leg on `QuerySpec` holds (`query.md:26,33,166-167`); the 25-backend `ibis.connect` surface and the UDF families stay DECLINED — query owns connections through the `[V8]` session rail and ADBC (`data/.api/ibis-framework.md`).
- `pandas` — boundary lowering ONLY: `Table.from_pandas`/`to_dataframe` on the field Arrow egress (`field.md:715`) and the `read_fwf` fixed-width foreign decode the roster gap-closure names; never a mined frame owner — `narwhals` owns backend-agnosticism; gains `owner: data` per `[V12]` (`data/.api/pandas.md`).
- `rustworkx` — the residue families: VF2 isomorphism/bisimulation, `generators`, DOT/Matrix-Market IO, group centrality, edge coloring — rows in the `[V7]` motion or the Growth-row residue naming (`data/.api/rustworkx.md`); the manifest tag is `[DATA_ARTIFACTS]` (`pyproject.toml:85`) — compute shed its co-tag with zero fences and no compute-tier stub, while artifacts is a live second consumer (`drawing/detail`'s rustworkx prelude, `libs/python/artifacts/.planning/drawing/detail.md:53-55`, with its own folder-tier catalog `artifacts/.api/rustworkx.md` and the artifacts-brief `[04]` deepening onto register/transmittal/classify), so data and artifacts are the tag owners.
- `networkx` — the codec/egress lane holds and CLOSES there (the `GraphFormat` `NODE_LINK`/`GRAPHML`/`EDGE_LIST` codec rows, `graph.md:59-62,261-279`); the analysis surface is DECLINED for data's own graph plane — `rustworkx` owns data analysis (`[V7]`) — while geometry `graph/features` is the branch's networkx-analysis consumer of record, so the catalog keeps its analysis families with geometry named on the owner line; branch-tier residency and the dual-consumer owner line are landed per `[V12]`.
- `igraph` — the GPL-confined community-detection split holds and CLOSES there: `_run_ig` binds the catalogued `igraph.Graph` community family function-local per `[V7]`'s structural confinement, `GraphPayload.of` keeps the igraph admission hop (`graph.md:13`), and the GPL-3 posture stays recorded in `data/.api/igraph.md` — never a second analysis kernel beside `_run_rx`, never a module-top import.
- `obstore` — the write/conditional/`PutMode` surface runtime `[V13]` delegated is HELD at depth by `egress.md` (verified); the exceptions-taxonomy anchor retargets to `data/.api/obstore.md:145-157`.
- `fsspec` — NEW `data/.api/fsspec.md`: the `.fs` accessor + `register_filesystem` slice (live at `columnar.md:340`) plus the delegated `transaction`/`cat_ranges`/block-cache/`FSMap`/`rsync` surfaces as mine-or-decline rows (`[V12]`).
- `zarr`/`numcodecs`/`tensorstore`/`cubed` — the five `store.md` `[RESEARCH]` entries are SETTLED per `[V6]`; the numcodecs filter family binds `zarr.codecs.numcodecs.*` (absorption landed at zarr >= 3.1.3; `numcodecs.zarr3` is the deprecated spelling), recorded in the relocated `data/.api/numcodecs.md`.
- `flox` — `Aggregation`/`Scan`/`ReindexStrategy` and the `flox`/`numpy`/`numba`/`numbagg` `ReductionEngine` kernels mined into the `FieldSelection` grouped/binned/resampled arms (`field.md:131-137`) under the `<3.15` band; `xarray_reduce` stays the one xarray-aware entry (`data/.api/flox.md`).
- `netcdf4` + `h5netcdf` — the `FieldEngine` engine axis: the netCDF-C-only quantization encoding keys stay ROW properties the h5netcdf backend rejects (`field.md:44,88-100`); the landed `h5netcdf` admission closes the fence-bound-unadmitted inversion; low-level CF time/group access reaches `netCDF4` direct (`data/.api/netcdf4.md`, `data/.api/h5netcdf.md`).
- `icechunk`/`virtualizarr`/`h5py` — the absorbed manifest owner composes the settled accessor surfaces; the snapshot-seed parity proof rides runtime `evidence/reproduction` when it lands (`[V6]`).
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): `pointblank` (profile's near-total mine), `dataframely` (contract COLLECTION), `pandera`, `narwhals` (eager/lazy correctness invariant), `polars`/`duckdb` scan surfaces, `awkward` (`_FOLD` closure table), `laspy`/`lazrs`/`laszip` COPC (the dual-backend admission is DELIBERATE — the `LazBackend` availability ladder `mesh.md:533-534` is the stated selection law), `meshio`/`trimesh`/`rhino3dm` `_BACKEND`, `pystac`/`pystac-client`/`stac-geoparquet`/`planetary-computer`/`odc-stac` (catalog), `h3ronpy`, `sqlglot` (transpile/lineage/`predicate_count`), `connectorx`/`adbc-driver-flightsql`/`adbc-driver-manager` (query transport), `fastexcel`, `obspec-utils`.
- The `[00]` csvkit named gap CLOSES over admitted surfaces: fixed-width decodes through `pandas.read_fwf` (`data/.api/pandas.md:59`), bare-`.dbf` attribute tables through the OGR ESRI Shapefile driver `pyogrio.read_dataframe` already binds (driver discovery, `data/.api/pyogrio.md:36`); a foreign decode outside both re-opens the roster, never a silent narrow.
- Standing holds ride their owning rows: the xarray-spatial terrain band (numba cp315 the named activation) is the one standing hold; the windowed/watermark stream owner stays deferred scope per `[06]`.

## [05]-[BUILD_ORDER_AND_ACCEPTANCE]

The landing order is the `[IMPORT_DAG_LAW]` topological order — foundations before consumers: a page lands after every page it imports, and the ordered list below is the canonical sequence. The three corpus-wide mechanical sweeps — the identity rename, the frozendict kill (`[V2]`), and the module-path drift fix (`[V3]`) — land on every owning page as it rebuilds, earliest pages first so later pages inherit clean spellings, stragglers fixed in place wherever found. `README.md`/`ARCHITECTURE.md` close in the same motion that changes their facts; the `[V11]` grammar purge applies to every touched page; pyproject and `.api` stub motions land with the change that admits, removes, or relocates a package; cross-file ripples repair in the same motion that exposes them.

1. **TABULAR BASE** — `tabular/interop.md` (FieldShape lands; `[V10]` depth), `tabular/columnar.md` (MATERIALIZE out; session rail; the public `arrow_bytes` fold is landed), `tabular/contract.md` (one-way imports), `tabular/egress.md` (cold; anchor retarget), `tabular/profile.md` (cold; rail).
2. **ENGINES + GRIDDED** — `tabular/lakehouse.md` (`[V8]` rows), `tabular/query.md` (`[V9]`), NEW `tabular/materialize.md` (after lakehouse/query — it composes both), `graph/graph.md` (`[V7]`), `gridded/store.md` (E14 settlement), `gridded/field.md` (narrowed FIRST), `gridded/virtual.md` (absorb AFTER the narrow — the absorb pair executes together, `field.md` condemned sections intact until the absorb lands), `gridded/ragged.md` (rail; `ArrowCStream.of` real).
3. **SPATIAL + GOVERNANCE** — `spatial/geospatial.md` (split source; claims + COVERAGE + ingress), NEW `spatial/query.md`, NEW `spatial/grid.md`, `spatial/catalog.md` (StacIngest absorbed; `transform` arity legal), `spatial/mesh.md`; the governance closure — `ARCHITECTURE.md` codemap+ledger, `README.md` router+groups, the live `[V12]` motions (owner lines, fsspec mine-or-decline rows, anchor retarget) — lands with the pages that change its facts. `impact/impact.md` (`[V4]`) is realized and carries no open motion beyond the `ARCH:58`/`ARCH:59` composition its residue names.

Acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.data.*` import in every fence — zero back-edges, zero cycle-dodging deferred imports, mechanically re-derived; (b) grep proofs return zero: `from builtins import frozendict`, `rasm.runtime.content_identity`, bare `rasm.data.catalog`, `[RESEARCH]` headers, `...` fence-stub bodies; (c) the impact proof holds on disk — all five `ImpactSource` cases reach realized arms producing real-unit cells over all three openepd containers, the assessment-wire columns are fence-pinned and self-describing (`source`/`declared_unit`/content-key ride the frame), the carrier declares the uncertainty slot the Brightway mining fills, and `TransportResource` is an import, not a comment; (d) ledger, codemap, router, and manifest agree on one nineteen-module set and one seam ownership; (e) every admitted package has a consuming fence or a named blocker condition recorded on its roster row and `.api` stub — `datafusion` consumed, the struck rows stay gone (`csvkit`/`brightway2`; `zlib-ng` artifacts-owned with zero data consumers), `bw2analyzer`/`h5netcdf` consumed, the pylance multimodal rows consumed, `xarray-spatial` blocker-named; (f) every `ContentIdentity.of` byte source in the folder is canonical — no `repr()`/`str()` sources — and the snapshot-seed parity obligation is recorded against the runtime reproduction rail; (g) the flagship-chain proof — every hand-off in the `[TELOS]` composed interchange (ref -> scan -> contract gate -> materialize/query -> content-keyed egress -> the three C# wire legs: Substrait plan bytes ⇄ Persistence, assessment frame -> Materials, GeoArrow buffers -> Compute) is a named fence symbol pair whose types agree — a hand-off requiring unstated glue is a chain break, never a doc gap.

## [06]-[OUT_OF_SCOPE]

- The C# seam owners: `Rasm.Persistence` (durable reuse ledger, version-control engine, Substrait federation counterpart), `Rasm.Materials` (assessment vocabulary mint), `Rasm.Compute` (GLB wire, assessment runner) — data decodes and content-keys at the wire, never re-mints; the C#-side canonical-writer alignment campaign is upstream-owned — this campaign records the count-prefix law as consumed law only.
- The runtime interior: the identity rename, `transport/shapes.md`, and the resilience rebuild are LANDED on disk (`rasm.runtime.identity` at `evidence/identity.md`, `guarded`/`guarded_sync` + the POLICY rows at `reliability/resilience.md:232-264`, the shapes registry at `transport/shapes.md:211`); data consumes the landed surface, with the `.archive/RASM-PY-RUNTIME-BRIEF.md` `[V5]` consumer-contract clause finalized upstream law.
- Sibling folders (`artifacts`/`geometry`/`compute`) — their pages never change here; cross-folder consequences travel as named seam obligations into the owning campaign surfaces (the Substrait federation counterpart is LANDED C#-side at `Rasm.Persistence/.planning/Query/federation.md`, gated on this campaign's producer leg; the GeoArrow GLB share -> `RASM-PY-COMPUTE-BRIEF.md`; the TransportResource DSN and lineage-receipt rows are landed runtime law).
- The one standing hold: the geospatial terrain band — `xarray-spatial` carries a hard `numba` core dependency and numba caps at cp314 (numba cp315 the named activation); the surface stub is the standing capability record.
- The windowed/watermark stream owner (`stream/incremental.md`) — genuinely deferred next-campaign scope; its interop streaming dependency lands here with `[V10]`.
- The `<3.15` scientific-band posture and the companion/offline execution charter — landed law, not re-opened.
