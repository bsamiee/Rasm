id: no-registration-outside-register
snapshots:
  ? |
    const arm = (register: On): void => {
        register('session.start', ($, e, next) => {
            register('tool.call', ($, e, next) => next(e));
            return next(e);
        });
    };
  : labels:
    - source: register('tool.call', ($, e, next) => next(e))
      style: primary
      start: 94
      end: 140
  ? |
    const arm = (register: On): void => {
        register('tool.call', ($, e, next) => next(e));
        register('tool.call', ($, e, next) => next(e));
    };
  : labels:
    - source: register('tool.call', ($, e, next) => next(e));
      style: primary
      start: 94
      end: 141
  ? |
    const hook = ($, e, next) => {
        on('prompt.submit', ($, e, next) => next(e));
        return next(e);
    };
    on('tool.call', { tool: 'Bash' }, hook);
  : labels:
    - source: on('prompt.submit', ($, e, next) => next(e))
      style: primary
      start: 35
      end: 79
  ? |
    const hook = ($, e, next) => {
        on('tool.call', ($, e, next) => next(e));
        return next(e);
    };
    on('session.start', hook);
  : labels:
    - source: on('tool.call', ($, e, next) => next(e))
      style: primary
      start: 35
      end: 75
  ? |
    on("tool.call", ($, e, next) => next(e));
    on("tool.call", ($, e, next) => next(e));
  : labels:
    - source: on("tool.call", ($, e, next) => next(e));
      style: primary
      start: 42
      end: 83
  on("tool.call", first); on('tool.call', second);:
    labels:
    - source: on('tool.call', second);
      style: primary
      start: 24
      end: 48
  ? |
    on('*', ($, e, next) => next(e));
    on('*', ($, e, next) => next(e));
  : labels:
    - source: on('*', ($, e, next) => next(e));
      style: primary
      start: 34
      end: 67
  ? |
    on('session.start', ($, e, next) => {
        const arm = (): void => {
            on('tool.call', ($, e, next) => next(e));
        };
        arm();
        return next(e);
    });
  : labels:
    - source: on('tool.call', ($, e, next) => next(e))
      style: primary
      start: 76
      end: 116
  ? |
    on('session.start', ($, e, next) => {
        on('tool.call', ($, e, next) => next(e));
        return next(e);
    });
  : labels:
    - source: on('tool.call', ($, e, next) => next(e))
      style: primary
      start: 42
      end: 82
  ? |
    on('session.start', hook);
    function hook($, e, next) {
        on('tool.call', ($, e, next) => next(e));
        return next(e);
    }
  : labels:
    - source: on('tool.call', ($, e, next) => next(e))
      style: primary
      start: 59
      end: 99
  ? |
    on('tool.call', ($, e, next) => next(e));
    on('session.start', ($, e, next) => next(e));
    on('tool.call', ($, e, next) => next(e));
  : labels:
    - source: on('tool.call', ($, e, next) => next(e));
      style: primary
      start: 88
      end: 129
  ? |
    on('tool.call', ($, e, next) => next(e));
    on('tool.call', ($, e, next) => next(e));
  : labels:
    - source: on('tool.call', ($, e, next) => next(e));
      style: primary
      start: 42
      end: 83
  ? |
    on('tool.call', ($, e, next) => next(e));
    on('tool.call', { tool: 'Bash' }, ($, e, next) => next(e));
    on('tool.call', ($, e, next) => next(e));
  : labels:
    - source: on('tool.call', ($, e, next) => next(e));
      style: primary
      start: 102
      end: 143
  ? |
    on('tool.call', first);
    on('tool.call', second);
  : labels:
    - source: on('tool.call', second);
      style: primary
      start: 24
      end: 48
  on('tool.call', first); on(`tool.call`, second);:
    labels:
    - source: on(`tool.call`, second);
      style: primary
      start: 24
      end: 48
  ? |
    on('tool.call', { tool: 'Bash' }, ($, e, next) => {
        on('prompt.submit', ($, e, next) => next(e));
        return next(e);
    });
  : labels:
    - source: on('prompt.submit', ($, e, next) => next(e))
      style: primary
      start: 56
      end: 100
  on(`tool.call`, first); on(`tool.call`, second);:
    labels:
    - source: on(`tool.call`, second);
      style: primary
      start: 24
      end: 48
