# [DOSSIER_CROSS_LANGUAGE]

Decision-complete integration blueprint for the three exemplar capability families — energy/environmental simulation, Speckle-class interchange/viewing, scale-out any-info store backends — across the three independent branches. The governing law is `libs/.planning/architecture.md`: each branch is lib-grade in isolation; branches couple ONLY at the wire (`[07]-[CROSS_LANGUAGE_WIRE]` — one `XxHash128` seed-zero content-identity mint, C#-owned wire vocabulary, content-keyed artifacts); an admitted package's disposition is full implementation at its per-language home, never removal, and never a 1-1-1 name-mirror across branches. Every claim below is disk- or feed-anchored; feed checks run 2026-07: `Speckle.Sdk 2026.6.0` (nuget latest, published 2026-06-24), `PollinationSDK 1.10.0` (nuget latest, 2026-02-04), `HoneybeeSchema 2.102.0` (nuget latest, 2026-06-26) — all three central pins are current.

## [01]-[PER_LANGUAGE_POSTURE]

### [F1]-[ENERGY_ENVIRONMENTAL]

**Python — the full ladybug/pollination band as first-class in-isolation owners.** The manifest band is exactly: `pyproject.toml:130-139` `[GEOMETRY]`-tagged — `honeybee-core`, `honeybee-energy`, `honeybee-openstudio`, `honeybee-standards`, `honeybee-energy-standards`, `dragonfly-core`, `dragonfly-energy`, `ladybug-core`, `ladybug-geometry`, `ladybug-comfort`; `pyproject.toml:140-142` `[RUNTIME]`-tagged — `queenbee`, `lbt-recipes`, `pollination-handlers`; plus the sibling LCA/environmental band `pyproject.toml:162-170` `[DATA]` — `openepd`, `epdx<1.0`, `brightway2`, `bw2data`, `bw2calc`, `bw2io`, `bw-processing`, `olca-ipc`, `premise` (`<3.15`-markered). Lib-grade in isolation means the COMPOSED companion the extended briefs now rule: `RASM-PY-GEOMETRY-BRIEF.md` `[V1_ENERGY_PLANE]` mints the four-page `energy/` sub-domain — `energy/climate.md` (ladybug EPW/Wea/`Sunpath`/`DataCollection` + the full `ladybug-comfort` PMV/UTCI/adaptive/SolarCal tier), `energy/model.md` (honeybee HBJSON object graph, ONE polymorphic `check_all`-gated admission discriminating HBJSON-bytes decode vs the IFC BIM-to-BEM derivation via `ifcopenshell` `IfcSpace` solids → `Polyface3D.from_faces` → `Room.from_polyface3d`/`solve_adjacency`, standards resolved by identifier), `energy/district.md` (dragonfly 2.5-D massing, `to_honeybee` explosion, URBANopt/DES/OpenDSS rows), `energy/simulate.md` (honeybee-openstudio OSM/IDF translation offloaded on the worker lane, queenbee `Job`/`RecipeInterface` binding, SQLite result decode into self-describing typed frames). Execution is runtime-owned: `RASM-PY-RUNTIME-BRIEF.md` `[V3_RECIPE_OWNER]` authors `execution/recipe.md` over the recipe trio (geometry binds schemas ONLY). Evidence graduates as `building-energy`/`thermal-comfort` subjects on the geometry-minted union (`[V2]`), inherited by `RASM-PY-COMPUTE-BRIEF.md` as frozen wire data. The LCA band realizes at `data/impact/impact.md` (`RASM-PY-DATA-BRIEF.md` `[V4_IMPACT_REALIZATION]` — the four normalization arms over openepd containers, staged `bw2calc` `lci`/`lcia`/`MultiLCA`, olca-ipc interchange). AGPL isolation holds at the process boundary per the standing companion-lane charter.

**C# — SDK integration at its own proper homes, two disjoint rails.** (a) SIMULATION: `Rasm.Compute/.planning/Analysis/energy.md` — the `Discipline.Energy` runner: builds an `OpenStudio.Model` in-process from the concrete `Rasm.Element` `ElementGraph`, forward-translates to an EnergyPlus IDF `Workspace` via the parameterized `EnergyToolchain` subprocess (env-var → configured-path → bundled-fallback), reads the post-run `SqlFile`; results content-keyed `(input subgraph, route, policy)` into Persistence (`Rasm.Compute/ARCHITECTURE.md:84-88`). The compute brief grades it 9.5/sound — only typed-failure classification rows land (`RASM-CS-COMPUTE-BRIEF.md` `[V2]`). (b) SEMANTIC EXCHANGE: `Rasm.Bim` owns the `[ENERGY_MODEL_EXCHANGE]` group (`Rasm.Bim/README.md:103-106`) — `HoneybeeSchema 2.102.0` (HBJSON authoring leg, 296 model classes over `OpenAPIGenBaseModel`, `LBT.Newtonsoft.Json` codec), `DragonflySchema 2.201.0`, `NREL.OpenStudio.macOS-arm64 3.11.0` (IFC↔OSM/gbXML translator matrix) — three authored catalogs (`Rasm.Bim/.api/api-honeybee-schema.md`, `api-dragonfly-schema.md`, `api-openstudio.md`) and the seam row `Exchange ⇄ csharp:Rasm.Compute/Analysis` (`Rasm.Bim/ARCHITECTURE.md:106` — "OpenStudio energy SIMULATION (Compute) distinct from the IFC↔OSM semantic exchange (Bim), aligned by the seam graph never coupled"). The exchange leg is currently a design-page phantom — see `[03]` G1. (c) CLOUD TRANSPORT: `PollinationSDK 1.10.0` — cataloged at `libs/csharp/Rasm.Persistence/.api/api-pollination-sdk.md` as the Sync cloud-job leg, PRUNED by `RASM-CS-PERSISTENCE-DECISION.md` `[05]` — see `[03]` G2.

**TypeScript — no energy owner, BY DESIGN.** Energy evidence reaches TS as typed wire facts only: assessment/progress evidence decodes at `wire` into `state/evidence` (`libs/typescript/.planning/ARCHITECTURE.md:33`), receipts stay the C# typed-receipt family never erased (`dataflow-system.md` `[06]`), and rendering is app-composed `ui` charts over `@effect-atom` bindings. A TS honeybee/ladybug re-mint is the named cross-language drift defect. This family is the proof the alignment is never a 1-1-1 mirror: py owns building-physics evaluation, C# owns graph-native simulation + semantic exchange, TS owns presentation of the receipts.

**Alignment seam.** One content-identity regime (seed-zero `XxHash128`, `kernel/identity` TS / `rasm.runtime.identity` py / kernel `ContentHash.Of` C#); energy result frames cross the py data seam self-describing (output name, unit, analysis period, zone identifier, content key ride the columns — geometry `[V1]`); C# EnergyPlus results and py simulation artifacts land content-keyed in their own stores and are reusable by key; the two OpenStudio integrations (py `honeybee-openstudio`, C# `NREL.OpenStudio`) and the two schema stacks (py `honeybee-core`, C# `HoneybeeSchema`) align through HBJSON document bytes and the seam graph, never a shared client or a cross-language import. The artifacts-branch solar owner (`pvlib`, admitted `RASM-PY-ARTIFACTS-BRIEF.md` `[04]`) deliberately stays OUT of the ladybug path (`[V15]` — AGPL process-boundary law), so diagram furniture never couples to the companion lane.

### [F2]-[SPECKLE_INTERCHANGE_VIEWING]

**C# — the SDK home, in-isolation complete.** `Speckle.Sdk`/`Speckle.Objects 2026.6.0` (`Directory.Packages.props:147-148`) bind the Persistence `SyncTransport.SpeckleLikeDiff` case owned at `Version/ledger#SYNC_TRANSPORTS`, dispatched through `SyncPump.Offer` — DI-resolved instance `IOperations.Send/Receive` (no static surface exists), the `Send` tuple's `rootObjId` content hash projecting to the `UInt128 ContentKey` under the `SyncFault.SpeckleMarshal` drift gate (`libs/csharp/Rasm.Persistence/.api/api-speckle.md` `[03]`/`[05]`). The 2026.6 model replaces `BuiltElements` with the `Speckle.Objects.Data` `DataObject` host-object family (catalog `[02]` records it). `Rasm.Bim/Exchange/import` consumes the transport for Speckle `Base` → seam `ElementGraph` import (`Rasm.Bim/ARCHITECTURE.md:104`). Sidecar law: the SDK runs OUTSIDE-RHINO with the `Speckle.Sdk.Dependencies` repacked closure; the in-Rhino assembly composes only the canonical Sync case. Codec-lane separation is sealed: Speckle's own `Base`-graph serializer never routes through the Thinktecture/MessagePack snapshot codecs — the rails meet only at the `SyncTransport` case algebra.

**Python — deliberately absent.** `specklepy` is not a branch dependency (`libs/python/.planning/ARCHITECTURE.md` `[04]`, stated as law). Python interchange rides the wire and content keys; a second Speckle client would be a second mint of the object-graph sync concern C# owns.

**TypeScript — the enablement, not "Speckle in TS".** Zero Speckle spellings exist under `libs/typescript/` (verified). The composable capability a future custom Speckle-class app needs is already the branch's chartered shape: `wire` decodes the C#-minted version/CRDT vocabulary (`codec/oplog`, `codec/crdt` CrdtOpWire MessagePack union, `codec/snapshot` canonical-CBOR, `codec/version` commit/branch/version-vector/Merkle → `state/causal`); `store/journal` is the append-only record with OCC, outbox, upcasting; `state/crdt` converges wire-decoded and app-authored op instances under one fold algebra; `edge/live` mounts the `store` EventLog sync server (port row 08, `composition-system.md` `[03]`); `ui/viewer` renders the GLB rail with BCF/GlobalId marks. The `Geospatial/BIM viewer` and `Realtime dashboard` archetypes (`composition-system.md` `[04]`) compose that app with zero lib edits — the store/backends/viewer capability is parameterized for any info of the type, which is the ruled disposition.

**Alignment seam.** Speckle's MD5/SHA object hashing stays its own lane; the ONE crossing is the `rootObjId → ContentKey` projection at the ledger. TS never sees Speckle wire — it consumes only the C#-minted `CrdtOpWire`/`SnapshotHeader`/JsonPatch shapes (`libs/typescript/wire/ARCHITECTURE.md:59-62`), so an imported Speckle graph reaches the web as ordinary content-keyed `ElementGraph`/op-log traffic. No branch re-mints another's sync vocabulary.

### [F3]-[SCALE_OUT_ANY_INFO_STORE]

**C# — the axis map as ruled law.** `RASM-CS-PERSISTENCE-DECISION.md` `[05]` `[V13]` STORE_AXIS_MAP: every store-class concern is one axis with provider rows as seed DATA — object store (`s3`/`azure-blob`/`gcs`/`minio` on the `ObjectStore` `[SmartEnum]`), egress sinks (`webhook`/`nats`/`kafka`/`rabbitmq`/`pulsar`/`wire-native` on the `EgressSink` `[Union]`, `[V3]`), columnar (`duckdb`/`pg_duckdb`/`clickhouse-scaleout`), lakehouse (`delta` + forward `ducklake`), vector (`pgvector`/`pgvectorscale`/PQ-ADC/`qdrant-scaleout`), embedded/KV (`sqlite`/`rocksdb`/`lmdb`), the sealed singular postgres-18 SoR spine. Each kept scale-out row carries a proven ceiling the in-PG owner cannot reach; `ScyllaDBCSharpDriver` is the single scale-out prune with a recorded forward re-admission condition. Federation is the ruled `Query/federation.md` owner (Substrait plan → `SetExpr`/columnar lowering, `SourceKind` capability axis).

**Python — the host-free interchange peer.** `data` owns the portable planes over the manifest band: object (`obstore`/`fsspec`/`s3fs`/`gcsfs` + `universal-pathlib`), lakehouse/versioned-tensor (`deltalake`, `pyiceberg`, `icechunk`, `pylance`, `virtualizarr`, `zarr`), engines (`duckdb`, `datafusion` with Substrait interchange, `daft`, `ibis-framework`), Arrow transport (`pyarrow`, `adbc-driver-flightsql`, `nanoarrow`) — all `RASM-PY-DATA-BRIEF.md`-governed; `runtime` mints `ResourceRoot`/`TransportResource` so siblings compose transport, never re-own it.

**TypeScript — the parameterized store for any info type.** `store` is the W3 no-migration event-sourced plane (`libs/typescript/store/ARCHITECTURE.md`): one append surface keyed `(appKey, tenantId, aggregate)`, read-time upcasting, inline/async projection lanes, the typed PG 18.4 capability matrix (`pgvector`/VectorChord, `pg_search`, `timescaledb`, `pg_partman`, `pgmq`, `pg_cron`, `pg_ivm`, `pg_duckdb`, `postgis`, `h3`, …) as fail-closed probe rows realized by the `iac` CNPG image, sqlite node/bun/OPFS-wasm lanes under the same contracts with an explicit degradation table, the hybrid retrieval roster (FTS|trigram|phonetic|fuzzy|semantic RRF), and the content-addressed object plane (`ObjectKey` = kernel `ContentKey`; presign + `sharp` fan-out; If-None-Match 412 = idempotent noop). Tenancy is a scope value (`StoreHandle` Layer family + RLS GUC), never a fork. Any info type is app data: events are app-authored `Schema.TaggedClass` families; a new lane/extension/tenancy shape is one row (`dataflow-system.md` `[07]`).

**Alignment seam.** One content-address notion across all three stores (C# blobstore write-once 412-noop seal ↔ TS conditional-put 412-noop ↔ py content-keyed artifact store); an artifact hashed by any runtime is reusable by every other; cross-runtime reuse/federation is GATED on the `python:data` producer and the `SourceKind.SignedArtifact` binding on the `python:artifacts` producer — named blockers on the C# federation owner, never an eager coupling. No branch shares an engine, a driver, or a schema with another.

## [02]-[ROUTING_TABLE]

| # | Touchpoint | Owning doc / campaign | Status |
|---|---|---|---|
| R1 | py energy plane (10 `[GEOMETRY]` rows + 13 catalogs) | `RASM-PY-GEOMETRY-BRIEF.md` `[V1]` (four `energy/` pages), `[V11]` (catalog/tier motions), `[04]` (per-package mining rows), leg 4 | CARRIED by the extended brief |
| R2 | py recipe trio (`queenbee`/`lbt-recipes`/`pollination-handlers`, `[RUNTIME]` rows) | `RASM-PY-RUNTIME-BRIEF.md` `[V3]` NEW `execution/recipe.md`; geometry `energy/simulate` binds schemas only | CARRIED |
| R3 | py energy graduation subjects (`building-energy`/`thermal-comfort`) | geometry `[V2]` mints; `RASM-PY-COMPUTE-BRIEF.md` `[V1]` decodes the frozen union as wire data | CARRIED |
| R4 | py LCA/impact band (`openepd`/`epdx`/`bw2*`/`olca-ipc`/`premise`) | `RASM-PY-DATA-BRIEF.md` `[V4_IMPACT_REALIZATION]` → `data/impact/impact.md` | CARRIED |
| R5 | py solar ephemeris (`pvlib` admission; ladybug `Sunpath` excluded from diagrams) | `RASM-PY-ARTIFACTS-BRIEF.md` `[V15]` + `[04]` admission row | CARRIED |
| R6 | py deferred energy growth (`honeybee-radiance` daylight, EPW morphing) | geometry `[V1]` — admission-gated growth, never pre-admitted rows | GATED by design, not a gap |
| R7 | C# energy SIMULATION runner | `Rasm.Compute/.planning/Analysis/energy.md`; `RASM-CS-COMPUTE-BRIEF.md` `[V2]` typed-failure rows only (page sound at 9.5) | CARRIED |
| R8 | C# energy EXCHANGE leg (`HoneybeeSchema`/`DragonflySchema`/`NREL.OpenStudio`) | future `Rasm.Bim` campaign (no `RASM-CS-BIM-BRIEF.md` exists at root); catalogs + README group + `ARCHITECTURE.md:106` seam row stand | MISSING page — `[03]` G1 |
| R9 | `PollinationSDK` cloud-job transport | `RASM-CS-PERSISTENCE-DECISION.md` `[05]` PRUNE row ("re-admission belongs to whichever package names a consumer"); catalog `Rasm.Persistence/.api/api-pollination-sdk.md` leaves with the leg-1 manifest motion | HOMELESS — `[03]` G2 |
| R10 | Speckle sync transport | `RASM-CS-PERSISTENCE-DECISION.md` `[02]` row 5 `Version/ledger.md` (`← BIM:104` Speckle Base import seam) + `[04]` sibling-row table "BIM:104 → `Version/ledger` (Speckle SyncTransport)"; catalog `Rasm.Persistence/.api/api-speckle.md` | CARRIED (leg 2) |
| R11 | Speckle Base → ElementGraph import | `Rasm.Bim/.planning/Exchange/import.md` + `Rasm.Bim/ARCHITECTURE.md:104`; interior re-point rides the future Bim campaign per the DECISION `[04]` counterpart table | CARRIED, Bim-campaign counterpart pending |
| R12 | C# store axes / scale-out / egress / federation | `RASM-CS-PERSISTENCE-DECISION.md` `[05]` `[V13]` axis map, `[V3]` sink rows, `[V1]` federation rows, `[02]` rows 12/19/22/24, legs 1-4 | CARRIED |
| R13 | TS branch law (no root TS brief exists) | `libs/typescript/RASM-TS-PLATFORM-DECISION.md` + `libs/typescript/BLUEPRINT.md` — the branch campaign surfaces; `libs/typescript/.planning/{composition-system,dataflow-system}.md` the standing law | CARRIED at branch altitude |
| R14 | TS store obligations | `libs/typescript/store/` — README router (18 pages) + `ARCHITECTURE.md` seams; `.planning/{journal,project,capability,scope,lane,retrieve,object}/` | CARRIED at folder altitude |
| R15 | TS viewer/GLB/BCF tier | `libs/typescript/ui/` — viewer rows 11-21 (`.planning/viewer/{scene,geo,mark,probe,panel}/`), `scope:viewer` Nx project, `GlbViewport` port to `browser` | CARRIED at folder altitude |
| R16 | TS wire decode for both families | `libs/typescript/wire/` — `.planning/codec/{oplog,crdt,snapshot,version,bim,bcf,geo,appearance}.md`, `frame/{artifact,geometry,residency}.md` | CARRIED at folder altitude |
| R17 | Cross-language concert cards | `libs/.planning/IDEAS.md` + `TASKLOG.md` per `architecture.md` `[07]` | EMPTY — `[03]` G3 |

## [03]-[GAP_LIST]

**G1 — C# energy-exchange phantom (the Bim twin of the py geometry E1 defect).** `HoneybeeSchema 2.102.0`, `DragonflySchema 2.201.0`, `NREL.OpenStudio.macOS-arm64 3.11.0` are manifest-admitted (`Directory.Packages.props:131,127,92`), fully cataloged (`Rasm.Bim/.api/api-{honeybee-schema,dragonfly-schema,openstudio}.md`), README-grouped, and seam-rowed (`Rasm.Bim/ARCHITECTURE.md:106`) — and ZERO pages under `Rasm.Bim/.planning/` consume any of them (grep-verified). Owning home: NEW `Rasm.Bim/.planning/Exchange/energy.md` — the HBJSON/OSM/gbXML semantic energy-model exchange owner: `ElementGraph` ↔ `HoneybeeSchema.Model` projection through the `IElementProjection` seam contract, the OpenStudio translator matrix for IFC↔OSM/gbXML, DragonflySchema urban-massing generation onto the honeybee leg, content-keyed exchange artifacts through the one kernel `ContentHash` fold. The future Bim campaign brief carries this as a realize-not-expel verdict (the exact `RASM-PY-GEOMETRY-BRIEF.md` `[V1]` pattern); alignment with `Rasm.Compute/Analysis/energy` stays the existing seam row, never a project reference.

**G2 — `PollinationSDK` homeless post-prune.** The DECISION strikes it from Persistence on charter grounds (a domain cloud SDK with no store-class concern) with re-admission owned by "whichever package names a consumer." The consumer is the C# energy family: the cloud-run leg — submit a recipe job, watch the `Run`, pull `RunAsset`s. Proposed home: a cloud-route row on the `Rasm.Compute/Analysis/energy` runner (the runner already owns local simulation; cloud submission is one route row on the same rail, matching the single-entry law), with the Bim `Exchange/energy` owner (G1) supplying the HBJSON payload. Integration shape preserved from the dying catalog: sidecar isolation (vendored `LBT.RestSharp`/`LBT.Newtonsoft.Json` closure never in-Rhino), token as app-root connection input, artifact bytes riding the Persistence object plane (presigned `S3UploadRequest` = the same S3 rail `Store/blobstore` owns), downloaded assets landing content-keyed. Re-admission is one manifest row + one `.api` stub + the consuming fence in the naming campaign — the DECISION's reversible-by-construction motion.

**G3 — empty cross-libs concert ledger.** `libs/.planning/IDEAS.md`/`TASKLOG.md` carry `(none)` while `architecture.md` `[07]` assigns them the cross-language concert. Three cards are due, one per family: (a) energy — py result-frame column discipline ↔ C# assessment receipts ↔ TS evidence decode, plus the G1/G2 C# counterpart obligations; (b) interchange — the `rootObjId → ContentKey` projection law and the TS store/viewer enablement archetype; (c) store — the cross-runtime content-address parity and the two GATED federation blockers (`python:data` producer, `SourceKind.SignedArtifact`). Each card names producer/consumer touchpoints in `lang:pkg/page#CLUSTER` notation per the ledger template.

**G4 — Bim-campaign counterpart backlog for the DECISION's sibling rows.** The Persistence DECISION `[04]` corrects `BIM:101` (annotation/CDE → `Version/ledger#CHANGEFEED`), `BIM:102` (P6/4D → `Ingest/schedule.md`), `BIM:104` (Speckle → `Version/ledger`), `BIM:81/91/94/95` — all re-point in the Bim campaign. No Bim brief exists; these counterparts plus G1 define that brief's minimum seam scope for the two families here.

No py-side gap stands for the three families: the four extended briefs route every admitted energy/LCA/solar row to a named owner, and the deliberate absences (`specklepy`, TS energy, py Speckle) are ruled law, not omissions. The TS viewer/store admissions (`three 0.185.1`, `@google/model-viewer 4.3.1`, deck.gl 9.3.5 band, `maplibre-gl 5.24.0` — `pnpm-workspace.yaml:122-133`) are all routed to `ui/viewer` rows; none is orphaned.
