# [RASM_FABRICATION_RULINGS]

`Rasm.Fabrication` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `lib3mf` and `OpenCAMLib` enter as vendored native builds — do NOT re-shop NuGet for a managed replacement: no free managed package owns full-extension 3MF write or cutter-location computation (managed 3MF entries stop at core-spec read, the commercial one fails the license gate), so a challenger enters only by owning the full concern under the license gate, never by matching the name.
- `RectangleBinPack.CSharp` stays admitted — no kernel algebra subsumes it: `OffsetOp.Minkowski` and `ArrangementOp.PlanarOverlay` build a configuration SPACE while the search over that space is this package's own concern, and `SearchOp.Rectangular` isolates the provider behind the one `StockNest.Pack` entry so the integer-frame lane never competes with the configuration-space walk; a challenger enters only by owning bounded rectangular multi-stock assignment whole.

## [02]-[SHAPE]

- Simulation reads its motion source off `SimulatePolicy` as one closed `MotionSource`, never a second entry parameter: a posted `CutProgram` folds controller semantics and a `RobotCell` folds the posed-station census `Kinematics/cell` resolves, so a new motion modality is one case and one arm rather than a sibling `Execute`.
- Provider kinematics stay behind `Kinematics/cell` whole — `Verify/simulate` consumes a provider-free `CellPosedStation` census and names no `Robots` type, so the `Rhino3dm` alias boundary has exactly one crossing in the package and posed display meshes leave as a kernel `BoundingBox` and a count.
- Cell cycle time reads `Program.Duration` off the look-ahead planner, never a re-derivation from `MotionDynamics`, which has no answer for a serial chain; per-station elapsed and travel measure against the PRIOR posed station, so the ledger sums to the planner's own clock.
- A swept solid proves only what a planar sweep can answer: an axis-free `Move` sweeps EXACTLY, and a `Move` carrying `MoveOrientation` takes the guard's typed refusal rather than an approximation, because a clean verdict over an approximated oriented cut reports a proof nobody ran; widening the sweep to oriented motion is a new envelope owner, never a tolerance on this one.
- Abandonment is `FabricationFault.RunAbandoned(FabConcern, Done, Witness)` — the offset-56 case `Process/faults` mints — raised on the abandoning folder's own `FabConcern`, never a rethrown `OperationCanceledException`, never the `PolicyInadmissible` policy/parameter-admission arm wearing a cancel, and never a kernel-band `GeometryFault.RunAbandoned` borrow (its `Kind` slot names geometry a fabrication run has no value for); a `Fin`-declaring entry that throws on cancel is partial and escapes the run fold. `Process/owner` stage boundaries and `Nesting/nfp`'s pair-memo leg are the landed producers, and an interior leg with no run-grain fraction states its structural zero at the site.
- A kernel fold whose rejected arm returns a SUCCESS carrying an empty result is read through its whole result carrier, never its collection row — `Toolpath/partition` projects `CloudVoronoiResult` and gates `Receipt.Status`, because the cells row alone made a degenerate dual byte-identical to a caller that asked for no depth. Discriminant: the identity fallthrough on `AtomProjection.Rows` yields the whole carrier from the SAME fold, so the gate costs no second run. Reopens only where a rail's rejected arm faults instead of succeeding empty.
- A generated owner's `Validate` populates its `out` slot exactly when it answers a null refusal, so ONE read decides both arms and the null-forgiving projection is the contract rather than a guard; the slot's type follows the owner kind — a reference owner hands a null-annotated slot, a `readonly partial struct` owner hands the value itself and never `Nullable<TOwner>`. One refusal extension binds both kinds, while the keyed arities stay two names because a generic constraint is not part of the signature overload resolution reads; a struct owner spelling `is { } refusal ? Fail : Succ(slot!.Value)` is both the deleted form and uncompilable.
- A hold point's satisfaction law lives with the plan that DECLARES it and publishes the open ROSTER, not a verdict: `Joining/procedure` `InspectionTestPlan.Unreleased` answers which blocking holds carry no attested release and `Released` is that roster's verdict, so `Documentation/traveler` gates its document and reports its open-hold witness column off ONE read — a documentation-plane predicate re-spelling party, attendance, and key correspondence is a second law over one fact, and a traveler that prints a hold point as an instruction and advances anyway is the rendered-text reading the gate exists to refuse.
- A distortion field carries every load family that MOVES the part or it is not a field: `Joining/sequence` loads thermal shrinkage, clamp preload, and stage release into one stiffness and one solve, so a clamped-but-unwelded member enters the member index and a schedule cannot report the spring-back of a part nobody let go of. Dominance compares the LOAD each row placed on the operator in the operator's own units — a per-case magnitude ranks newtons against millimetres — and a member moving only through its restraints answers `None` rather than a zero-magnitude release naming a stage that never ran.
- A specialized cut lane rides `Toolpath/motion` `EngagementPolicy` as one `Option<TPolicy>` column and its PRESENCE is the routing discriminant: the `Toolpath/wire` law makes an erosion boundary pass a wire cut and the `Toolpath/bevel` law makes a thermal or abrasive one a prepared edge, so the lane owner's admitted law travels on the engagement rather than being re-transcribed at the strategy that routes into it. Absence answers the lane's own default — the ordinary contour where a square edge is the cut, a typed refusal where no default cut exists — and a per-run demand flag beside the law is the deleted form, because two facts then answer one question. A lane's Cartesian egress rides `Move` while its non-Cartesian state stays whole on the `SpecializedToolpathEnvelope`, which is what keeps every lowered lane move axis-free under the swept-solid ruling above.
- `Toolpath/surface` holds NO layout algorithm and NO layout-topology evidence — the key space is caller-minted and the generator injected, so a kernel field tracer composes inside that generator at the composition root and a topology column on `SurfacePathReceipt` is refused twice: the generator returns drives alone, so the column has no producer, and a slot shaped for one of the fifteen layouts the key space spans is dead under the other fourteen.

## [03]-[COLLAPSE]

- The tolerance stackup is ONE algebra over ONE term roster. `Spec/tolerance` `ToleranceChain` declares the terms and owns their analytic combination — `StackMethod` seats on the chain that declares what it combines, and `Evaluate(StackMethod)` reads those same terms under any method — so `ChainReceipt` is the one receipt `Spec/capability`, `Spec/manufacturability`, and `Documentation/report` all read. `Spec/capability` adds the correlated simulation over THOSE terms under a `StackContributor` bijective onto them by key, carrying only what the term cannot state — systematic offset, shared-factor loadings, and an optional measured fit overriding the term's declared family — and `StackupReceipt` publishes both readings. A contributor re-declaring a term's spread or sensitivity is the deleted form, because a simulation run on a roster the chain never saw makes two receipts describe different stacks under one seed.

## [04]-[STRUCTURE]

- Analytic arc geometry rides the kernel `PackOp.Toolpath` carrier alone — the kernel toolpath lane preserves line and circular spans with arc centre and sense, so geometry custody stays kernel-owned; a carrier shortfall surfaces as a typed refusal at the emission arm, never a folder-local arc channel routed around the kernel.
- `IngressSource`, `AdmittedGeometry`, and `Ingress.Admit` seat on `Ingress/profile`, not a page of their own — a sub-domain fold whose every arm calls a sibling's published reader carries no owner state and mints no vocabulary, so an `Ingress.cs` node holds three declarations and four one-line arms; the fold seats beside the first-admitted source and earns its own page the moment an arm gains routing, policy, or receipt logic no sibling reader owns.
- A canonical preimage rides the `CanonicalWriter` BINARY under one declared quantization grid, and `FabricationCanon` is the package's one framing family for rows, discriminants, coordinates, and optional slots — so a plan keyed at any page addresses identically and a second facade cannot fork the byte order. JSON never seeds a signed or content-addressed identity: serializer output attests nothing a verifier reproduces once member order, culture, or a unit SPELLING can move it, and a hex-text render inside a preimage lets a formatting choice decide byte order. A provider enum ordinal never enters a preimage — key on the owned vocabulary row instead, so a provider reordering its own enum cannot fork every key already minted.
- A `CanonicalWriter`-digested scalar column carries its unit basis as prose at the declaration and stays a bare double — lifting it to a typed quantity moves the content preimage and forks every key already minted, so unit conversion belongs at the derivation site that needs it, never the carrier; `Nesting/nfp` `NestEvidence`/`Digest` and `Nesting/stock` `Digest` state it in place.

## [05]-[PROCESS]

- (none)
