# Plan: `Rasm.Element` seam + Materials/Bim/Persistence rebuild + package admission

## [RESUME ANCHOR — READ FIRST AFTER COMPACTION]
MODE: PLAN MODE (read-only except THIS plan file). Do NOT implement until ExitPlanMode.
TASK: stand up a NEW lowest-AEC seam package `Rasm.Element` + rebuild `Rasm.Materials`/`Rasm.Bim`/`Rasm.Persistence` `.planning` design corpora to world-class, collapsing two parallel unaligned element owners into ONE property-graph IFC-native element; admit the settled external packages fully (central pin + folder README + csproj + FULL `.api`). These are DESIGN-PAGE specs (`.planning/**/*.md`), not code.
STATUS: package research DONE; APPROACH design workflow DONE (wf_5f8dc0c2-111 — converged + 19 critique findings applied); RED-TEAM DONE (wf_0d61d54b-348 — 24 ACCEPT/WATCH findings folded into §4-RT, C4 refuted by live nuget, L2 rejected). New pins version-locked (nuget MCP 2026-06-28). §4-RT is AUTHORITATIVE over §4A-H on conflict. ALL operator decisions RESOLVED (§8); Forge EnergyPlus probe folded (§4E, dev/CI-only); strata amendment recorded (§4A); LanguageExt hedge removed. NOTHING in flight.
NEXT ACTION: ExitPlanMode. ON EXIT: write the durable plan doc (`ELEMENT-REBUILD-PLAN.md`) to repo root, then author ALL workflows back-to-back via workflow-creator (§7), then run them in sequence with the manual package-admission + nuget pass first.

## 1. CONTEXT & MANDATE
Hardening `libs/` into a world-class foundation for hundreds of future Rhino9-WIP/GH2 apps. Two intertwined needs:
1. **Package admission** — admit every modern, macOS-arm64, newest-stable, non-duplicative, commercial-safe external package that lets the C# AEC folders avoid hand-rolling across thousands of materials/elements. Each lands FULLY: central `Directory.Packages.props` pin + folder `README.md` roster + folder `.csproj` + a FULL `.api/` catalog.
2. **Fundamental rebuild** — `Materials`+`Bim` are naive in DECOMPOSITION (not collapse): a "material"/"element" must be ONE rich aligned aggregate that roundtrips FULLY at the app wire (geometry + IFC semantics + typed structural/thermal/energy/env/acoustic/fire + cost + appearance), ALIGNED not coupled, so a consumer "has it all" and downstream code is trivial. Don't hand-roll steel/wood/brick. General IFC + EQUAL geospatial attention. Cost/scheduling/appearance: admit packages but BRACKET their planning rebuild; rebuild focus = materials, IFC-for-materials, IFC-general, geospatial.

## 1A. LIBS DESIGN LAW (standing goal — every decision serves this)
- LIB-GRADE for countless future consumers, NOT a single app. Every `libs/csharp` package is fully usable in ISOLATION (only the kernel + the shared element seam below it) yet ALIGNED-not-coupled — peers NEVER reference each other; alignment travels through shared lower-stratum contracts + content-keyed wire seams, never sibling coupling.
- Parameterization / polymorphism / interfaces are the alignment mechanism: one polymorphic owner per concept; ingress AND egress parameterized so one owner serves many consumers without interior edits.
- Python is a COMPANION (wire-aligned), never foundation. Parametric_Forge is a LOCAL dev/probe toolchain, NEVER a shipped-app dependency.

## 2. NAIVETY FINDING (justification — authoritative over any "already advanced" verdict)
- TWO parallel unaligned element owners: `Materials.Element(Profile,Placement,MaterialAssignment)` vs `Bim.BimElement(GlobalId,IfcClass,…,Materials,…)`. No unified rich element.
- `BimElement.Properties=PropertyBinding(string,string,string)`, `.Quantities=QuantityBinding(string,string,double,string)` — STRINGLY-TYPED IFC Psets/Qtos. The rich typed `Materials.MaterialProperty` unions (Mechanical/Thermal/Acoustic/Fire/Environmental/Cost) live SEPARATELY keyed by MaterialId, NEVER joined to the element. "Everything baked in" is absent.
- Decomposition is by ASPECT (Materials) and by OPERATION (Bim), never by the rich THING an app consumes — scattered across ~10 owners / 2 packages, ID-referenced not aligned.
- VERDICT: this is a DECOMPOSITION + ALIGNMENT rebuild (a unified rich element/material spine), NOT a per-page collapse pass. Per-page doctrine conformance is already fine; the decomposition is naive.

## 3. RESOLVED DECISIONS (operator)
1. Element model = property-graph IFC-native (entity + objectified relationships + typed property/quantity, mirroring IFC's graph).
2. Ownership = a NEW SHARED element seam both Materials+Bim project onto (neither owns alone).
3. Persistence = FULL rebuild.
4. Analysis = C#-FIRST (py is a side companion, NOT foundation): typed analysis nodes on the element graph; run via VividOrange/BriefFiniteElement/FEALiTE2D (structural) + NREL.OpenStudio C# SDK in-process (build) + subprocess EnergyPlus + hand-rolled closed-form C# (ISO 6946 U / EN 13788 Glaser / ISO 12354 acoustic / EN 1992-1993-1-2 fire / EN 15978 LCA) + EC3 REST.

# ==================== 4. FINAL ARCHITECTURE (CONVERGED) ====================
Source: approach workflow wf_5f8dc0c2-111 (Doctrine+Map → 4-lens Design → Synthesize → Critique → Reconcile); all 19 critique findings applied. Four lenses converged UNANIMOUSLY on seam = new lowest-AEC package `Rasm.Element`. RED-TEAM (wf_0d61d54b-348) in-flight; fold ACCEPT findings into the matching subsection (`[RED-TEAM WATCH]` markers flag the likely sites).

## 4A. SEAM DECISION
- NEW lowest-AEC-DOMAIN package **`Rasm.Element`**, inserted between KERNEL(`Rasm`) and `{Materials, Bim, Fabrication}`. Strata: KERNEL(Rasm) → Rasm.Element → {Materials,Bim,Fabrication} → APP-PLATFORM(AppHost,Compute,Persistence,AppUi) → HOST-BOUNDARY(Rasm.Rhino, Rasm.Grasshopper) → APP. Depends UP on `Rasm` kernel ONLY; references NO GeometryGym.
- Acyclic-verified: every AEC peer references only `../Rasm` today, so one kernel-only insertion + one upward ProjectReference per peer preserves the DAG.
- GeometryGym stays SOLE IFC owner in Bim; **Bim implements the seam interface** to provide the IFC projection (inversion of control) → no GeometryGym edge points down into the seam, no 2nd IFC stack.
- Name distinct from `Rasm.Fabrication` (manufacturing/CAM) — separate bounded concepts; the new README/ARCHITECTURE state the distinction.
- STRATA AMENDMENT (sanctioned, not a violation): adding `Rasm.Element` as a new lowest-AEC sub-stratum AMENDS the charter — AEC peers now depend up on `{Rasm, Rasm.Element}`. This is the SAME shape as depending on the kernel: a shared LOWER stratum aligns the peers WITHOUT coupling them to each other (peers never reference each other; alignment is via the `IElementProjection` + `IGraphConstraint` contracts). Each package stays fully usable in ISOLATION — Materials alone produces a material subgraph (authoring its own `Associate` edges, §4-RT H12), Bim alone the IFC projection, Persistence persists any `ElementGraph`. This REPLACES the prior "AEC peers reference only Rasm + each other minimally" clause; the build WF amends `libs/.planning/architecture.md` + `libs/csharp/.planning/ARCHITECTURE.md` + `planning-targets.md`. Operator-chosen; red-team affirmed acyclicity. REJECTS the "local contracts + per-package wire seams" alternative — that is the operator-rejected option and would fragment the canonical element into N per-package definitions, defeating lib-grade reuse.

## 4B. CANONICAL SHAPE (ElementGraph) + VOCABULARY
- Authoritative thing = **`ElementGraph`** = `Header`(ReleaseVersion+ModelView+GeoReference(CRS)+Tolerance+Instant) + `Nodes:FrozenDictionary<NodeId,Node>` + `Edges:Seq<Relationship>`. The consumer-facing **`Element` is a derived fold `Bake(objectNode)`** over the reachable subgraph — never a 2nd stored record (cures "typed data stranded off the element"). "Has it all" = one flat Option-typed field read at the wire.
- `NodeId` = `[ValueObject<string>]` via the `IObjectFactory<TSelf,TKey,TError>` floor: GlobalId for rooted `IfcRoot`, kernel content-hash (XxHash128) for non-rooted (IfcMaterial/IfcProfileDef/IfcRepresentationMap/IfcMaterialLayer). ONE node-id owner; `MaterialId` is a node attribute, never a parallel key.
- `Node` = `[Union]`: `Object` (IfcObjectDefinition mirror; `Occurrence|Type` two cases; carries generic `Classification(system,code)` value-object NOT IfcClass; `GeometryRef(GeometryHash,IfcRepHash)` content-hash only; `Option<GeoReference>`) | `Material` | `PropertySet` | `QuantitySet` | `Assessment` | `Appearance` | `Coverage`. Zero inlined string Psets.
- `Relationship` = `[Union]`, one case per real IfcRel* (17: Aggregates, Nests, AssociatesMaterial, DefinesByProperties, DefinesByType, ContainedInSpatialStructure, ReferencedInSpatialStructure, ConnectsElements, ConnectsWithRealizingElements, ConnectsPorts, VoidsElement, FillsElement, AssociatesClassification, AssignsToGroup, Sequence, ServicesBuildings, AssignsToAssessment) — typed RelatingRef/RelatedRef endpoints + per-case static IFC wire-name (no parallel RelationshipKind). Graph mutation = ONE polymorphic request `[Union]` → `Fin<ElementGraph>` (a `GraphDelta` applied immutably). Traversal reuses **QuikGraph** (pinned 2.5). Type→occurrence Pset/Qto inheritance = a fold over `DefinesByType` in `Bake`, never stored twice. [RED-TEAM WATCH: confirm GeometryGym 25.7.30 covers every chosen IfcRel* case + audit IfcRel* cases NOT in the 17 before freezing the union.]
- Typed VALUE family (seam-owned, library-neutral): `PropertyValue=[Union]` Text|Measure|Boolean|Enumerated|Reference|Bounded|List|Table (IfcValue mirror); `MeasureValue(QuantityKind,double Si,string SiUnit)` UnitsNet SI-coerced once at admission; `QuantityKind` (Length/Area/Volume/Weight/Count/Time — Weight only); `PropertyName`; `PropertySet`/`QuantitySet` bag NODES via DefinesByProperties. IFC Pset/Qto roster + bSDD resolution + QTO override + base-qty derivation STAY in Bim.
- `Material` node: MaterialId attribute; `MaterialComposition=[Union]` Single|LayerSet|ProfileSet(ProfileRef)|ConstituentSet; `MaterialPropertySet=[Union]` Mechanical|Thermal|Acoustic|Fire|Environmental|Cost keyed to the ONE `Discipline`; intrinsic single-material PURE acoustic folds (Nrc/Saa/StcWeighted/StcContourFit) on the seam Acoustic case.
- `Assessment` node = generic opaque typed payload keyed by `Discipline`+content-key+provenance, attached via AssignsToAssessment; discipline-specific solver I/O + the multi-ply AssemblyAggregator live in Compute.
- ONE `Discipline` `[SmartEnum]`: Structural/Thermal/Energy/Acoustic/Fire/Environmental/Cost.
- Geospatial: `Coverage` node (raster/field by-ref+bands+CRS) + generic `GeoReference/ProjectedCrs` value-object on Header/Object/Coverage; vector features ride `Object` (no parallel Feature family); NTS/ProjNET/GDAL projector in Bim.

## 4C. PROJECTION CONTRACT (the ONE new interface)
- ONE instance interface **`IElementProjection`** with one polymorphic op `Fin<GraphDelta> Project(ProjectionContext ctx)`. Each concrete projector captures its foreign source internally (Bim `SemanticProjector` over GeometryGym; Materials `MaterialProjector`; Fabrication later). `ProjectionContext` carries the element NodeId set so an aspect projector never invents element identities. Materials projects ONLY the material subgraph (no element→material edges); `AssociatesMaterial` authored by whoever knows both ids (Bim at IFC ingress, or the app at the wire). Wire holds `Seq<IElementProjection>`, folds GraphDeltas via generic `Assemble`; adding a projector = one registration row. IFC egress (`Emit`) = Bim-INTERNAL op, NOT a seam member. NET NEW Rasm interfaces for the whole rebuild = **1**; `IObjectFactory` reused; no instance default-interface-members. [RED-TEAM WATCH: who mints element NodeIds when AUTHORING a brand-new element from scratch, not ingesting IFC?]

## 4D. PER-PACKAGE FOLDER/FILE STRUCTURE (.planning/<Folder>/<page>.md)
```
libs/csharp/Rasm.Element/   (NEW; refs ../Rasm only)
  Graph/         element.md (ElementGraph, Node [Union], NodeId via IObjectFactory, the Bake fold)
                 delta.md   (GraphDelta — the ONE merge type; graph-mutation request [Union] + total Switch)
  Relations/     relation.md (Relationship [Union] objectified IfcRel*, typed endpoints, per-case wire-name)
  Classification/ classification.md (generic Classification value-object system+code; the ONE Discipline [SmartEnum])
  Properties/    property.md (PropertyValue [Union], PropertyName, PropertySet/QuantitySet bag nodes)
                 quantity.md (MeasureValue + QuantityKind, UnitsNet SI coercion)
  Composition/   material.md (Material node, MaterialComposition [Union] w/ ProfileRef, MaterialPropertySet [Union])
                 acoustic.md (intrinsic pure folds Nrc/Saa/StcWeighted + the one StcContourFit kernel)
  Assessment/    assessment.md (generic Assessment node keyed by Discipline+content-key+provenance; AssignsToAssessment)
  Geospatial/    coverage.md (Coverage node raster/field by-ref+bands+CRS)
                 reference.md (generic GeoReference/ProjectedCrs value-object)
  Projection/    projection.md (IElementProjection instance + one Project; ProjectionContext; Assemble fold)
                 address.md (ContentAddress [ValueObject<UInt128>] over kernel hash + non-geometry canonical ordering)
                 fault.md (ElementFault [Union] banded)
  + governing: README.md, ARCHITECTURE.md, IDEAS.md, TASKLOG.md, .api/, Rasm.Element.csproj
libs/csharp/Rasm.Bim/   (REBUILT as projector; SOLE GeometryGym owner)
  Model/elements.md(RETIRE BimElement/BimModel; KEEP IfcClass/IfcDomain/PredefinedType — GeometryGym-grounded) +structure/query/systems/zones/structural
  Semantics/properties.md(KEEP PropertyKey roster+bSDD+QTO-override+base-qty; RETIRE value half now seam-owned) +composition/classification/appearance/connection/georeference/geospatial
  Exchange/ Review/ Planning/  (codecs/IDS/COBie/BCF/cost-schedule over the seam graph)
  Projection/semantic.md (SemanticProjector:IElementProjection — Project lowers GeometryGym→seam; Emit Bim-internal)
libs/csharp/Rasm.Materials/   (REBUILT as projector)
  Construction/assembly.md(RETIRE Materials.Element/MaterialAssignment) +layout/nesting
  Properties/properties.md(MaterialProperty unions = SOURCE lowered to Material/MaterialPropertySet + Assessment inputs) +sustainability
  Profiles/ (VividOrange — resolved by ProfileRef)   Appearance/ (Wacton.Unicolour + 7-lobe BSDF + .mtlx → Appearance node)
  Projection/material.md (MaterialProjector:IElementProjection — material subgraph only; no element→material edges)
libs/csharp/Rasm.Persistence/  (FULL rebuild — see §4F)
  Element/ (graph store-load roundtrip)  Version/ (op-log/CRDT/time-travel/StructuralMerge/causal-DAG engine; Marten = append substrate beneath — §4-RT H11)  Query/ (in-process QuikGraph topology + DuckDB/Ara3D columnar; AGE optional self-hosted — §4-RT H5)  Ingest/tabular.md (MiniExcel)  Store/ (object-store geometry blobs)
```

## 4E. INTEGRATION MAP (one owner per concern)
- Seam(`Rasm.Element`): graph algebra + typed value vocabulary + generic Classification/Discipline + the ONE projector interface.
- Substrate: Thinktecture.Runtime.Extensions (`[Union]`/`[SmartEnum]`/`[ValueObject]`/`[ComplexValueObject]` + `IObjectFactory` floor) + LanguageExt.Core (Fin/Validation/Option/Eff + K<F,A> traits). Measure: UnitsNet. Graph traversal: QuikGraph (reused). Graph↔DTO/proto: Mapperly. Node/edge equality+content key: Generator.Equals. Content-identity seed (XxHash128 seed-zero, GeometryHash, frozen byte layout): Rasm kernel (seam composes, no 2nd hasher).
- IFC semantics/geometry/IfcRel*/IfcClass roster/Pset-Qto-bSDD: GeometryGymIFC_Core 25.7.30 + Bim (sole). Section props/capacity/interaction: VividOrange (Materials). Nesting: RectangleBinPack. Appearance: Wacton.Unicolour + hand-rolled 7-lobe BSDF + .mtlx.
- Analysis (C#-first, in Compute, reads the concrete ElementGraph directly — above the seam, no interface): VividOrange + BriefFiniteElement/FEALiTE2D structural; NREL.OpenStudio 3.11 in-process model + EnergyPlus subprocess; hand-rolled closed-form (ISO 6946 U / EN 13788 Glaser / ISO 12354 STC / EN 199x-1-2 fire / EN 15978 LCA) + the relocated multi-ply AssemblyAggregator; EC3 REST embodied carbon; hand-rolled design-code checks. Writes `Assessment.Result` nodes back content-keyed on (input key, route).
- ENERGYPLUS PROVISIONING (Forge probe — folded; dev/CI-only): `NREL.OpenStudio.macOS-arm64` 3.11.0 (SWIG) BUILDS the model + `EnergyPlusForwardTranslator`→IDF + reads `SqlFile`; it does NOT run or bundle EnergyPlus. The EnergyPlus subprocess binary is a PARAMETERIZED boundary — discovery env-var → configured-path → bundled-fallback, never hardcoded. Version-control: EnergyPlus MUST track the OpenStudio SWIG version — OpenStudio 3.11.0 expects EnergyPlus **25.2.0**. DEV/CI (us): point the subprocess at the OpenStudio-BUNDLED 25.2.0 via `OPENSTUDIO_ENERGYPLUSDIR` (version-matched); do NOT use the Forge STANDALONE EnergyPlus 26.1.0 (mismatched). Parametric_Forge is dev/probe-only — NEVER a shipped dependency; SHIPPED apps own their EnergyPlus provisioning (bundle a 25.2.0 osx-arm64 binary, or env/config). NOTE: energy simulation is currently a Python (lbt-recipes) rail; this rebuild ADDS the C#-first energy rail in Compute (model exchange already exists in Bim).
- IDS/COBie/QTO/BCF: ids-lib + Xbim.InformationSpecifications / Xbim.CobieExpress / Xbim.Properties / Smino.Bcf (Bim). Pset/Qto templates: bSDD REST (Bim). Geospatial CRS/vector/raster: NetTopologySuite + ProjNET + MaxRev.Gdal (Bim). GLB encode (authored): SharpGLTF (+3DTiles, MeshOptimizer, Draco) at the kernel wire.

## 4F. PERSISTENCE MULTI-STORE ROUNDTRIP (full rebuild)
- Write SoR = **Marten** event stream on PostgreSQL (one stream per NodeId; events ElementCreated|Revised|Retired; payload carries the WHOLE ElementGraph → zero-loss rehydrate; SingleStreamProjection aggregate; `AggregateStreamAsync(version|timestamp)` AS-OF). `Version/` = `ledger.md` (Marten event SoR + time-travel/snapshot/provenance/recovery/retention as policies) + `merge.md` (content-hash structural 3-way graph diff/merge). CRDT/HLC/op-log relocated to `Sync/coordination` (gated on Open Q1).
- Element↔bytes = ONE `SnapshotCodec` `[SmartEnum<string>]` (messagepack|cbor|json-stj|file-raw); canonical CBOR for content-stable hashing; `ContentAddress` composes the kernel seed-zero XxHash128 (no 2nd hasher).
- Geometry bytes SoR = content-keyed object store: GLB by GeometryHash, lossless IFC/BREP by IfcRepHash. Write protocol: content-address blob, **write blob first**, reference immutable hash from the event, orphan-GC sweep; NOT same-PG-txn with the event.
- Relational identity tier (Npgsql/EF: ElementIdentity PK/TenantId/GlobalId/H3/pgvector + ACL + classification catalog) committed in the SAME PG txn as the Marten event.
- Read projections (async, rebuildable from the one stream via the Marten daemon): **Apache AGE** openCypher topology + **DuckDB/Parquet/Ara3D.BimOpenSchema** columnar. Strong-consistency reads go through the ledger, not the projection. [RED-TEAM WATCH: per-element-stream vs per-model grain; whole-graph-per-event bloat; async-projection staleness for clash/live-QTO.]
- DuckDB is fully-featured via SQL `INSTALL`/`LOAD` over ADO.NET (spatial/fts/vss/parquet/httpfs/iceberg/delta/postgres_scanner) — a documented pattern in `api-duckdb.md`, not a missing package. PG server extensions to provision: pg_duckdb, timescaledb-toolkit, postgis_raster, postgis_sfcgal.
- Ingest under ONE `TabularSource` owner (`Persistence/Ingest/tabular.md`); tabular→element map at the app composition root.

## 4G. INTERFACE DOCTRINE (the operator's facet — outcome)
- The interface FORM is already doctrinal (`docs/stacks/csharp/README.md` INTERFACE_SEAM). NEW = the cross-stratum alignment pattern (a lowest-stratum seam package hosts ONE instance interface floor + closed vocabulary siblings depend up on and implement, aligning by contract without sibling references). Net new Rasm interfaces = 1.
- MINIMAL doctrine edits (applied LATER, separate work, NOT in this rebuild): (1) append a one-line cross-stratum-seam clause to the README INTERFACE_SEAM card; (2) add one `docs/stacks/csharp/domain/README.md` router row (cross-package element alignment → surfaces-and-dispatch INTERFACE_SEAM). NO new card (doc spam). The "NO interface package" stance HOLDS (`Rasm.Element` is a domain floor, not interface-only); static-abstract self-constrained stays reserved for compile-time admission (`IObjectFactory`).

## 4H. IFC TESSELLATION (recommendation; Open Q2 confirms the imported-IFC default)
- TWO provenances, each its own owner: (1) AUTHORED geometry (kernel-produced) → kernel Brep/Mesh → SharpGLTF GLB (kernel owns; instanced via IfcRepresentationMap/IfcMappedItem content-key). (2) IMPORTED IFC geometry → IfcConvert/IfcOpenShell companion → GLB content-keyed (does NOT pass through the kernel; consistent with architecture.md [07] + CLAUDE.md [06]). Both meet at the content-keyed GLB wire (one XxHash128 seed) shared with Python + TS.
- Imported-IFC engine = swappable adapter, NOT a seam interface. PERMANENT default = the IfcConvert/ifcopenshell subprocess companion (content-keyed GLB; the Python wire seam) — OPERATOR-CONFIRMED Q2. The web-ifc in-process .NET arm64 binding is structurally blocked (red-team M5 — the only .NET binding is Windows C++/CLI) and is DROPPED from the plan; a future P/Invoke-over-native-static-lib is research-only. REJECT a standalone REST geometry service for the interactive path.

# ==================== 4-RT. RED-TEAM REVISIONS (AUTHORITATIVE — OVERRIDE §4A–H ON CONFLICT) ====================
Source: red-team workflow wf_0d61d54b-348 (3 perspective-diverse attackers + synthesis). 24 ACCEPT/WATCH findings folded; C4 REFUTED by live nuget; L2 REJECTED. These resolutions OVERRIDE the matching §4 statements.

CRITICAL
- [C1 event grain] Stream grain = ONE stream PER MODEL (or per spatial partition), NOT per-NodeId. Event body = the `GraphDelta` (NOT a whole-graph snapshot). Fold `GraphDelta→ElementGraph` via Marten INLINE `SingleStreamProjection` (cross-model aggregates use `MultiStreamProjection`) + periodic `AggregateSnapshot` to bound replay. (Overrides §4F "one stream per NodeId; payload carries the WHOLE ElementGraph".)
- [C2 read-your-writes] Authoritative topology/containment stays SYNCHRONOUS / co-transactional (Marten INLINE projection in the write txn, or ltree/closure). AGE/DuckDB are ANALYTICAL lanes only, with an explicit staleness watermark; interactive-correctness queries (clash, void-resolution, live QTO) block on `WaitForNonStaleProjectionDataAsync` and NEVER route to an async projection.
- [C4 OpenStudio arm64] REFUTED by live nuget: `NREL.OpenStudio.macOS-arm64` latest IS 3.11.0 (published 2026-01-19); the current pin restores. KEEP in-process OpenStudio 3.11.0. WATCH only: smoke-check the `OpenStudioCSharp` dylib P/Invoke-loads on net10/arm64 at admission.
- [C5 relationship roster] The seam does NOT carry 17 typed IfcRel* cases (that re-opens the IFC-schema strata leak the critique fixed for Classification). Seam `Relationship` = a NEUTRAL edge algebra `[Union]` (Compose | Assign | Associate | Connect | Void) with generic `NodeId` endpoints + a typed-attribute payload + a `Generic(wireName, relating, related, attrs)` passthrough so NO relationship is dropped. ALL IfcRel* names/directionality/inverse-semantics + the 8 previously-stranded families (ConnectsStructuralMember/Activity, ConnectsPortToElement, SpaceBoundary 1st/2nd, Covers*, ConnectsPathElements wall-join priorities, Declares, AssignsToControl/Process/Product/Actor, InterferesElements) live in BIM's projector and ride the neutral payload. (Overrides §4B "17 cases".)
- [C6 PredefinedType] Carry `PredefinedType` as a first-class typed value on the `Object` node; validity is a Bim-owned EGRESS gate (`Emit` resolves the `IfcClass` row from `code` + runs `AdmitPredefined` against the frozen valid set → `BimFault.UnmappedClass`); fold the predefined token into the content hash.
- [C7 material occurrence-usage] The `Associate` (material) edge carries a typed usage payload: LayerSetUsage (LayerSetDirection / DirectionSense / `OffsetFromReferenceLine`) and ProfileSetUsage (`CardinalPoint` / `ReferenceExtent`). The type-level `MaterialComposition` Set stays; usage is the occurrence binding on the edge.

HIGH
- [H1] Stamp each PropertySet/QuantitySet node with a structural `InheritanceMode` (`OccurrenceWins` | `TypeDrivenOverride` | `TypeDrivenOnly`) at Bim ingress so `Bake` applies correct type→occurrence precedence wholly within the seam.
- [H2] Replace the 6-value `QuantityKind` with a `Dimension` value-object over the 7 SI base dimensions + a UnitsNet `QuantityType` discriminator: `MeasureValue(quantityType, double Si, canonicalUnit)`; the 6 named kinds become convenience accessors on the QTO fold only. (Pset_* measures — ThermalTransmittance, Pressure, Force, MassDensity, SoundPressureLevel, PlaneAngle, Temperature — now admit with units/SI.) (Overrides §4B QuantityKind.)
- [H3] `ElementGraph` carries a `FrozenDictionary<NodeId, ImmutableArray<Relationship>>` incidence index built once at snapshot freeze (`Bake` → O(degree)); memoize `Bake` keyed by `(objectNodeId, graphContentHash)`, invalidate on `GraphDelta`; build the QuikGraph view once per snapshot.
- [H4] Split graph by phase: `ImmutableDictionary`/HAMT for the live authoring/delta path (O(log n) structural sharing); `ToFrozenDictionary()` only at the read-snapshot/persistence/projection boundary. Mutable working graph ≠ frozen read snapshot.
- [H5] AGE demoted to an OPTIONAL self-hosted-only read projection; DEFAULT topology owner = the in-process QuikGraph view (Bake needs it anyway) + DuckDB columnar. Never assume one PG instance hosts both AGE and Marten-9/Npgsql-10. (Overrides §4F AGE-as-default.)
- [H6] Rooted `NodeId` = a NEUTRAL kernel-minted stable id (ULID/Guid); the compressed IFC `GlobalId` is a Bim-stored projection attribute on the Object node (1:1, emitted at `Emit`). The kernel owns the whole `IObjectFactory` floor; IFC GUID encoding leaves both the kernel and the from-scratch authoring path. (Overrides §4B "GlobalId for rooted".)
- [H7] ONE seam-owned canonical value codec: fixed IEEE-754 LE bits (`-0.0→0.0`, `NaN→canonical`), measure quantization to `Header.Tolerance` before hashing, explicit attribute-order canon — exposed as an INSTANCE member `ToCanonicalBytes()` on the `Node` union (mint stays static-abstract; verification/re-hash go through the union). The diff `ContentBytes` and the NodeId hash SHARE this projection; add a float-bearing golden vector (`IfcMaterialLayer`) to the cross-runtime parity corpus.
- [H8] Per-node `IntroducedIn`/`RemovedIn` schema-span stamped by Bim at ingress; validate `code`+`predefined` against `Header.ReleaseVersion` at `Emit`; sniff `FILE_SCHEMA`/JSON `schema_identifier` before constructing `DatabaseIfc` (never hardcode 4x3).
- [H9] Add an `OwnerHistory` value-object (owning user/app, created/modified, ChangeAction, State) carried optionally on ROOTED nodes from `IfcRoot.OwnerHistory`, re-emitted with a diff-derived ChangeAction; add a `StepHeader` record (FILE_DESCRIPTION/FILE_NAME/FILE_SCHEMA) to the Header. Marten provenance is a SEPARATE additive axis, NOT a substitute for IFC owner-history.
- [H10] Geometry object-store gets the same content-lineage + retention catalog row the snapshot spine has; GC reachability runs over the FULL event history (every AS-OF cut), not head — or geometry GC is forbidden (dedup + cold-tiering). Real `List`-vs-catalog sweep with a pending-write ledger committed before the blob Put. Pick ONE transaction owner for identity+event (identity as a Marten doc in the same `IDocumentSession`, OR the event through EF) — no free two-ORM atomicity. Write-blob-first + 412-noop survives.
- [H11] Version/ does NOT collapse to ledger+merge-only. Treat Marten (if adopted) as the APPEND SUBSTRATE BENEATH the existing op-log / CRDT / time-travel / StructuralMerge / CausalDag engine — the engine PROJECTS from Marten events instead of the bespoke `OpLogEntry` store; TimeTravel (AS-OF/blame/bisect/scrub/checkpoint), 3-way `StructuralMerge`, and the attested causal DAG are PRESERVED as the read/merge layer (re-keyed to `NodeId`/`Relationship`). CRDT stays first-class (collab + IFC-merge substrate) — OPERATOR-CONFIRMED Q1: KEEP (offline multi-writer + IFC 3-way merge). (Overrides §4F "Version/ collapses to ledger.md + merge.md".)
- [H12] The projector owning BOTH endpoints authors the `Associate` (material) edge: when Materials is given a non-empty element-NodeId set it authors element→material edges; a pure-Materials+app composition thus has a named edge owner — "the app authors it at the wire" is removed. (Overrides §4C for the both-ids case.)

MEDIUM
- [M1] Extend GeoReference to `(Eastings, Northings, OrthogonalHeight, XAxisAbscissa, XAxisOrdinate, ScaleX, ScaleY, ScaleZ, GeodeticDatum, VerticalDatum, ProjectedCrsName, Epsg)`; parse EPSG from URN/authority/`EPSG:`; FAULT (not skip) when a CRS name is present but unresolvable. Keep GeoReference on Header/Coverage ONLY — DROP it from the Object node. (Overrides §4B "Option<GeoReference> on Object".)
- [M2] `GeometryRef` becomes a keyed map `RepresentationIdentifier → contentHash` (axis/body/box/footprint), renamed neutral `RepresentationContentHash` (no IFC-name leak); Bim owns the IFC representation mapping + IfcRepresentationMap/IfcMappedItem instancing per representation. (Overrides §4B singular `GeometryRef`.)
- [M3] Net-new Rasm interfaces = **2**, not 1: add `IGraphConstraint.Validate(GraphDelta, ElementGraph) → Validation<Error,Unit>`, Bim-implemented with IFC-semantic legality (containment-relating-must-be-spatial, VoidsElement element→opening, Type-may-not-aggregate-Occurrence); the seam's total Switch enforces ONLY structural invariants. (Overrides §4C/§4G "net new interfaces = 1".)
- [M4 WATCH] ElementGraph→Ara3D.BimOpenSchema egress: if BimOpenSchema is EAV-generic, Persistence owns a structural map; if BIM-typed, it is a Bim-implemented seam projection. Surface it as a Marten FLAT-TABLE projection (co-transactional), not daemon-lagged.
- [M5] Imported-IFC tessellation default = IfcConvert/ifcopenshell PERMANENTLY (the only .NET web-ifc binding is Windows C++/CLI; no arm64 in-process exists). If ever pursued, web-ifc is a P/Invoke binding over a natively-built static lib (research, not plan). (Refines §4H + Open Q2.)
- [M6 WATCH] No schedule/task/sequence node for 4D — add explicit schedule/task nodes only if 4D is a real target (schedule/cost already in Bim via MPXJ).
- [M7 WATCH] Settle `ProfileRef`→section-property (VividOrange) as a ONE-HOP resolution so structural consumers don't re-resolve per call.

LOW
- [L2 REJECT] DuckDB spatial/vss "unused" is not a defect — spatial→PG GiST, ANN→pgvector is valid; DuckDB as a columnar aggregator is fine.
(L1 dropped: Thinktecture + LanguageExt are the established CORE rails of this codebase — no version-hedging.)

SURVIVED (confidence): GeometryGym IoC inversion + acyclicity; write-blob-first content-keyed object store + 412-noop ordering; seed-zero single-algorithm XxHash128 discipline; map-conversion translation+rotation fidelity; material layer/profile/constituent SET structure; `IElementProjection` instance interface + mint-at-construction; GDAL + DuckDB osx-arm64 native viability.

# ==================== 5. PACKAGE ADMISSION REGISTER ====================
Each NEW package lands FULLY: central `Directory.Packages.props` pin + folder `README.md` roster row + folder `.csproj` `PackageReference` + FULL `.api/` catalog. `.api` tier rule: cross-cutting SUBSTRATE → shared `libs/csharp/.api/`; folder-specific → that folder's `.api/`. nuget-MCP newest-version + net10.0 TFM + commercial-safe license confirmed at admission (versions below confirmed 2026-06-28; re-confirm).

| Package | Version | Pin | `.api` location | Folder csproj + README | Status |
|---|---|---|---|---|---|
| Marten | 9.11.0 | central | `Rasm.Persistence/.api/api-marten.md` | Persistence | NEW |
| Riok.Mapperly | 4.3.1 | central | `libs/csharp/.api/api-mapperly.md` (substrate) | Element + consumers | NEW |
| Generator.Equals | 4.0.0 | central | `libs/csharp/.api/api-generator-equals.md` (substrate) | Element + consumers | NEW |
| bSDD REST client | n/a | none (hand-thin over HttpClient) | `Rasm.Bim/.api/api-bsdd.md` (doc) | Bim | NEW thin |
| EC3 REST client | n/a | none (hand-thin over HttpClient) | `Rasm.Compute/.api/api-ec3.md` (doc) | Compute | NEW thin |
| DuckDB INSTALL/LOAD pattern | (DuckDB.NET pinned) | — | `Rasm.Persistence/.api/api-duckdb.md` (document the SQL load pattern) | Persistence | DOC |
| PG server extensions (pg_duckdb, timescaledb-toolkit, postgis_raster, postgis_sfcgal) | server-side | provision (not nuget) | Persistence provisioning note | Persistence | NEW server-ext |
| MiniExcel | 1.45.0 | central | `Rasm.Persistence/.api/api-miniexcel.md` | Persistence | ADMITTED (retired Sylvan.Data.Excel) |
| MagmaWorks.SectionProperties | — | — | — | — | DEFERRED (not on nuget.org; GitHub-only — needs a GitHub Packages source + auth; VividOrange already owns section properties) |
| Ara3D.SDK | — | — | — | — | NOT ADMITTED (operator; GeometryGym covers IFC) |
| QuestPDF / SixLabors.ImageSharp | — | — | — | — | NOT ADMITTED (operator; revenue-gated license declined) |

ALREADY PINNED & COMPOSED (no new pin; the rebuild composes them, deepening `.api` where the rebuild reveals gaps): QuikGraph 2.5, Thinktecture 10.4, LanguageExt 5.0, UnitsNet 5.75, GeometryGymIFC_Core 25.7.30, VividOrange.*, SharpGLTF(+3DTiles/MeshOptimizer/Draco), NetTopologySuite/ProjNET/MaxRev.Gdal, DuckDB.NET, DeltaLake.Net, Ara3D.BimOpenSchema, ids-lib/Xbim.InformationSpecifications/Xbim.CobieExpress/Xbim.Properties/Smino.Bcf, BriefFiniteElement.Net/FEALiTE2D, NREL.OpenStudio 3.11, RectangleBinPack, Wacton.Unicolour.

REJECTED (carried — do NOT re-propose): Xbim.Ifc as a 2nd IFC core; AGPL (Xbim.IDS.Validator.Core, CoordinateSharp, pgmq, EventStore); BSL (SurrealDB); non-commercial (EPPlus); .NET-Framework/Windows-only (Wosad); dup/stale (Vogen, CSharpFunctionalExtensions, FSharpx.Collections, ImTools, LiteDB); the AutoInterface/proxy/AutoRegisterInject/PolySharp family; instance default-interface-members. REFERENCE-only (study, don't admit): BHoM (LGPL alpha), Hypar.Elements/IFC4.
CAVEATS: HoneybeeSchema/DragonflySchema/PollinationSDK = AGPL schema-only (already admitted) — SBOM note. Design codes (AISC360/NDS/ACI318/TMS402/AISI-S100/EN 199x) = HAND-ROLL (~250 rules; no .NET package). OpenStudio C# SWIG 3.11.0 builds the model in-process on macOS-arm64 (nuget-verified: `NREL.OpenStudio.macOS-arm64` 3.11.0 published 2026-01-19; smoke-check the `OpenStudioCSharp` dylib P/Invoke on net10/arm64 at admission); EnergyPlus runs as a subprocess; results parsed from SQLite.

# ==================== 6. RIPPLE/SEAM ADJUSTMENTS (all csharp + py; surgical, no drift) ====================
- `Rasm` (kernel): no domain change; EXPOSE a generic seed-zero XxHash128 content-hash entry the seam composes; confirm geometry referenceable BY content-hash only.
- `Rasm.Bim`: REBUILD as projector (ProjectReference Rasm.Element; SemanticProjector:IElementProjection; Project lowers GeometryGym, Emit Bim-internal). KEEP IfcClass/PredefinedType + PropertyKey/bSDD/QTO/base-qty/IfcRelDefinesByProperties; emit generic Classification onto Object nodes; RETIRE BimElement/BimModel + stringly bindings + the seam-owned value half. Geospatial projector → Object/Coverage nodes; georeference.md maps IfcMapConversion→seam GeoReference.
- `Rasm.Materials`: REBUILD as projector (MaterialProjector:IElementProjection, Project-only). RETIRE Materials.Element/MaterialAssignment; project ONLY the material subgraph with material-scoped NodeIds; route MaterialProperty unions into Material/MaterialPropertySet + Assessment inputs; ProfileSet→ProfileRef; move intrinsic acoustic folds to the seam; hand the AssemblyAggregator to Compute; Appearance node content-keyed.
- `Rasm.Fabrication`: ProjectReference Rasm.Element; later a 3rd IElementProjection (one registration row).
- `Rasm.Compute`: ProjectReference Rasm.Element; own discipline-specific Assessment input/result shapes + runners (reads the concrete ElementGraph directly) + the relocated AssemblyAggregator; write Assessment.Result nodes back content-keyed. Closed-form physics live HERE, never in the seam.
- `Rasm.Persistence`: FULL rebuild (§4F).
- `Rasm.AppHost`: STAYS reference-light (referenced BY Compute/AppUi/Persistence; must NOT gain AEC-domain refs); contributes only ProjectionContext primitive ingredients (ClockPolicy/CorrelationId/TenantContext); enforce ArchUnitNET no-GeometryGym-below-the-seam fitness rule.
- `Rasm.AppUi`: STAYS pure-UI (works in concert, never an endpoint); does NOT host wire composition — NO AEC-domain refs added for assembly (operator Q4).
- WIRE COMPOSITION OWNER (operator Q4): the live element assembly (register `Seq<IElementProjection>`, run `Assemble` against a live source, select the tessellation adapter, wire tabular→element map + DI) is an APP / HOST-BOUNDARY composition-root concern, owned per-app — `Rasm.Rhino` / `Rasm.Grasshopper` today, future standalone/web/sidecar apps later. The SEAM (`Rasm.Element/Projection`) owns the composition CAPABILITY (the `Assemble` fold + the projector-registration contract); apps own the wiring. No APP-PLATFORM package (AppHost or AppUi) hosts it.
- `Rasm.Rhino` / `Rasm.Grasshopper` (HOST-BOUNDARY): confirm they consume the seam `Element`/`ElementGraph` at the wire, not the retired BimElement/Materials.Element; surgical seam rename only.
- Architecture docs (3 levels): `libs/.planning/architecture.md` (add `Rasm.Element` to the strata + package roster) + `libs/.planning/planning-targets.md` (register the new package) + `libs/csharp/.planning/ARCHITECTURE.md` (C# stack map + seams) + each rebuilt package's `ARCHITECTURE.md` (+ new `Rasm.Element/ARCHITECTURE.md`).
- `Directory.Packages.props`: add the §5 pins.
- `docs/stacks/csharp`: the interface-doctrine edits (§4G) — LATER, separate work, not in this rebuild.
- `libs/python`: wire-alignment ONLY (no Materials/Bim/Persistence reference; coupling through the wire per architecture.md [07]). Align the IFC→IfcOpenShell→GLB companion + interchange decoders to the seam GeometryRef content-key (one XxHash128 seed) + the typed Material/Property/Assessment/Classification wire vocabulary — decode, never re-mint.

# ==================== 7. POST-PLAN EXECUTION (on ExitPlanMode) ====================
Order: re-read this plan line-by-line → make it durable → manual package admission + nuget pass → author ALL workflows back-to-back → run the workflows in sequence. These are DESIGN-PAGE rebuilds; there is no runtime test/validation gate — the quality gate is doctrinal conformance + the adversarial passes inside the harden workflow. The roundtrip property is a DESIGN invariant enforced by the build workflow's align pass and the harden workflow's red-team, not a runtime harness.

## 7.0 DURABLE PLAN DOC (first action on exit)
Copy this ENTIRE plan to the repo as `ELEMENT-REBUILD-PLAN.md` at repo root (temporary campaign doc; remove after the campaign lands) so every workflow agent reads the full plan via a repo-relative path. All three workflows below take its path as a constant and instruct EVERY agent to read it in full FIRST.

## 7.1 MANUAL — central package admission + nuget pass (operator/parent, not a workflow)
Hand-edit `Directory.Packages.props` to add the §5 pins; run nuget-MCP `get_latest_package_version` / `get_package_context` for each to lock the newest stable + license; `dotnet restore` + `dotnet nuget why` to confirm the graph (a restored assembly is REQUIRED so the `.api` workflow's `assay api` can decompile it). Resolve Open Q3 (MiniExcel vs Sylvan) and the eval/license Open Qs first so the pin set is final.

## 7.2 WORKFLOW SET (author ALL up front via workflow-creator, then run in sequence)
Common contract for every workflow: each agent reads (1) `ELEMENT-REBUILD-PLAN.md` in FULL, (2) the route-owned doctrine `docs/stacks/csharp/**` (+ the relevant `domain/` shard), (3) the SHARED capability tier `libs/csharp/.api/**`, (4) the target folder's own `.api/**` and current `.planning/**`. Workflows are logical and barrier-minimal (pipeline by default; a barrier only where a stage genuinely needs all prior results). No 1-file × 3-pass tedium in the BUILD stage — that shape belongs only to the granular HARDEN stage.
DESIGN-DOC OUTPUT LAW: the `.planning` pages we author are REAL design specs — full transcription-complete code fences, ZERO placeholder/stub/TODO logic, NO page/length cap; densify in place. They follow the conventions of sibling design pages + the canonical package-README structure BY EXAMPLE. "Page craft" is a doctrine-file concept (`docs/stacks/csharp`) ONLY and does NOT govern our design docs.

**WF-1 `element-api-authoring` (parallel fan-out; runs after 7.1).** One agent per NEW package (Marten, Mapperly, Generator.Equals, MiniExcel, bSDD-doc, EC3-doc, DuckDB-INSTALL/LOAD-doc; MagmaWorks DEFERRED — not on nuget.org). Each: resolve the LIVE surface via Context7 + nuget `get_package_context` + `assay api` (decompile the restored assembly — no phantoms), author the FULL `.api` catalog matching the existing `.api` catalog convention in the correct tier (§5), and update that folder's `README.md` roster + `.csproj` `PackageReference`. Final RECONCILE agent: verify every `.api` is non-phantom + every README/csproj updated + the shared-vs-folder tier placement is correct. Output: the new `.api` set ready to be mined by WF-2. (Central pins are already in place from 7.1 — this WF does NOT touch `Directory.Packages.props`.)

**WF-2 `element-architect-build` (the BUILD workflow — per-folder, NOT per-file).** Stands up the new structure and rebuilds the design corpora coherently.
- Phase BUILD (1 agent per FOLDER: `Rasm.Element` [new], `Rasm.Bim`, `Rasm.Materials`, `Rasm.Persistence`; pooled): each agent reads the common contract sources + §4D structure for its folder + the current `.planning` (migration source), then AUTHORS the whole folder's new design pages — create folders/files, grow owners per §4, retire the named owners, compose the §5 packages and the shared/folder `.api`, ground/root-up as if always-present, ZERO spam. Cross-folder needs logged as residuals. (Rasm.Element may split its sub-domains across a few agents if size warrants, but the unit is the folder/sub-domain, never one-file-at-a-time-3x.)
- Phase ARCHITECTURE (simple, 1 agent): update the 3-level architecture docs + planning-targets registration (§6) so strata/seams/roster are truthful for the new `Rasm.Element` + the rebuilt folders.
- Phase RECONCILE/ALIGN (cross-folder + internal smoothing): align every seam the rebuild created — `IElementProjection` (Rasm.Element) ↔ `SemanticProjector`/`MaterialProjector` (Bim/Materials); the GeometryRef/content-key seam ↔ Persistence ↔ kernel; the py wire-alignment doc — and smooth internal drift (one-semantic-name, no duplicate owners) across all rebuilt + rippled folders (incl. Compute/Fabrication/AppUi/AppHost/Rasm.Rhino/Rasm.Grasshopper seam renames). One align → one adversarial verify; no drift. This is the workflow's OWN reconcile/align/smoothing — both internal and cross-folder.

**WF-3 `element-harden` (the HARDEN workflow — granular adversarial refactor of the NEW content).** The cs-rebuild-single/many adversarial meaning (hostile rebuild → critique(xhigh) → redteam(max), then folder-wide reconcile + whole-stack final-align), pointed at the NEWLY-built `{Rasm.Element, Rasm.Bim, Rasm.Materials, Rasm.Persistence, Rasm.Compute}` content — CORRECTED vs the legacy WFs: it MUST stack BOTH the shared `libs/csharp/.api/` tier AND each folder's `.api/` (the legacy `cs-rebuild-*` doctrine wrongly says "C# has NO central .api tier"), and it MUST account for the new `Rasm.Element` seam + the `IElementProjection` cross-stratum interface pattern + the new value/graph vocabulary. Granular (per-page within the new folders) adversarial passes maximize collapse + capability + `.api` depth + doctrine adherence; `.api` files are deepened in place where the rebuild reveals gaps. Whole-stack final-align (align→critique→redteam) closes any cross-folder seam/strata drift the per-folder hardening could not see.

Sequence: 7.0 → 7.1 → WF-1 → WF-2 → WF-3. WF-1's `.api` outputs are the input WF-2 mines; WF-2's built pages are the input WF-3 hardens. Author all three (and confirm/patch the legacy `cs-rebuild-*` `.api` gap if reused) BEFORE launching any.

# ==================== 8. OPERATOR DECISIONS (RESOLVED) ====================
1. Version/collab engine = KEEP the bespoke op-log/CRDT/time-travel/StructuralMerge/causal-DAG engine first-class (Marten = append substrate beneath; offline multi-writer + IFC 3-way merge). (§4-RT H11.)
2. Imported-IFC tessellation = IfcConvert/ifcopenshell PERMANENT default; web-ifc spike DROPPED. (§4-RT M5; §4H.)
3. Packages = ADMITTED MiniExcel 1.45.0 (retired Sylvan.Data.Excel) + Marten 9.11.0 + Riok.Mapperly 4.3.1 + Generator.Equals 4.0.0. DEFERRED: MagmaWorks.SectionProperties (not on nuget.org; GitHub-only). NOT admitted: Ara3D.SDK (GeometryGym covers IFC), QuestPDF + SixLabors.ImageSharp (revenue-gated license declined).
4. Wire-composition home = NOT AppUi (stays pure-UI, never an endpoint). The live element assembly is an APP / HOST-BOUNDARY composition-root concern, owned per-app (Rasm.Rhino / Rasm.Grasshopper today; future standalone/web/sidecar apps); the seam owns the `Assemble` CAPABILITY, apps own the wiring. (§6.)
C4 OpenStudio: refuted by live nuget; in-process 3.11.0 kept. Forge OpenStudio/EnergyPlus probe folded (dev/CI-only) — see §4E ENERGYPLUS PROVISIONING.
