{
    "extends": "../../../tsconfig.base.json",
    "compilerOptions": {
        // Source-exporting spec-plane package: nothing emits declarations, so the stated-annotation gate does not bind.
        "declaration": false,
        "isolatedDeclarations": false,
        "rootDir": "."
    },
    "include": ["src/**/*.ts"]
}
