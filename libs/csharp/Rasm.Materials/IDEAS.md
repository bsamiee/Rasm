# [MATERIALS_IDEAS]

The forward pool of higher-order concepts for the host-neutral materials owner, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[JOINT_CONNECTION_FAMILY]-[QUEUED]: Widen the closed-at-three ConnectionFamily axis by ONE deliberate fourth case `joint` carrying welds, adhesive bonds, and shear studs as one continuous-connection vocabulary, the metallurgical/adhesive complement to the discrete reinforcement/fastener/hanger items the axis already owns.
- Capability: A new ConnectionFamily case `Joint` over one `ConnectionSection.Joint` arm parameterizing a weld (AWS D1.1 fillet/groove/plug/butt over a `JointKind` discriminant carrying leg/throat/groove-angle/root-opening columns and an electrode-class strength receipt), an adhesive bond (bond-line thickness, substrate pair, lap-shear/peel allowable per the structural-adhesive class), and a shear stud (the AWS D1.1 stud-welded connector over diameter/height/spacing the composite-section seam reads) as ROWS over one `ConnectionItem` — never a `Weld`/`AdhesiveBond`/`ShearStud` type, never a fifth ConnectionFamily sibling. The continuous weld/bond/stud has no thread/bar-diameter section so it cannot fold into an existing family arm the way `anchor` folds into fastener; `ConnectionSection.NominalMm` dispatches the joint arm to throat/bond-line/stud-diameter.
- Shape: A new sibling page Connection/joint.md realizing the `JointKind` (weld/adhesive/stud) discriminant, the `WeldType`/`AdhesiveClass`/`StudClass` axes, the `JointSection` ConnectionSection arm, and the `ConnectionCatalogue.BuildJointRows` AWS D1.1 / structural-adhesive row table the one `ConnectionCatalogue.Build` fold concatenates; the connection.md ConnectionFamily SmartEnum widens by one case (closed-at-four) and the ConnectionSection Union gains one arm, the ConnectionItem.Of admission and the band-2360 ConnectionFault unchanged.
- Unlocks: A composite steel-concrete shear interface, a moment-connection weld schedule, and a bonded curtain-wall joint all key and serialize the way a bolt does, so the connection owner spans the full structural-connection discipline (mechanical AND metallurgical AND adhesive) over one polymorphic surface the layout fold places and the IFC IfcMechanicalFastener/IfcRelConnectsWithRealizingElements wire serializes.
- Anchors: connection#CONNECTION_OWNER ConnectionFamily/ConnectionSection/ConnectionItem.Of (connection.md lines 30-101, closed-at-three at line 12); the anchor-as-FastenerKind-arm precedent (connection.md line 12); ConnectionCatalogue.Build fold (lines 92-101); properties#MATERIAL_PROPERTY Mechanical electrode/adhesive capacity receipt read by CapacityKey MaterialId; AWS D1.1 weld-symbol/electrode-class data, the structural-adhesive lap-shear allowable, the AWS D1.1 stud-welded shear-connector tables.
- Tension: connection.md prose and ARCHITECTURE [03] state the axis is CLOSED at three with anchor folded as a FastenerKind arm; admitting `joint` is the single deliberate axis-widening, justified because a continuous weld/bond/stud is structurally distinct from a discrete placed item (no thread/bar-diameter NominalMm) and the STEEL_COMPOSITE_AND_COLDFORMED composite leg depends on the StudClass vocabulary landing here.

[SUSTAINABILITY_PROPERTY_DISCIPLINE]-[QUEUED]: Establish a sustainability-and-procurement property discipline beside the mechanical/thermal/acoustic/fire engineering family so a material carries its embodied-carbon, cost, and BIM-classification alongside its structural and physical properties, completing the material as a full lifecycle object rather than only a physics-and-shade carrier.
- Capability: New MaterialProperty cases `Environmental` (the EN 15978 / ISO 14040 cradle-to-gate GWP per declared unit over a UnitsNet Mass/MassConcentration quantity, the EPD declaration number and validity, the per-module A1-A3/A4/A5/C lifecycle-stage vector as a fixed-length ReadOnlyMemory<double> over a `LifecycleStage` band exactly as the realized Acoustic case carries its 6-band AbsorptionSpectrum, the recycled-content and end-of-life fractions), `Cost` (supply/install/lifecycle columns over a `Currency` band and a measurement basis), and `Classification` (the Uniclass2015/OmniClass/IFC Pset assignment for federation), plus `AssemblyAggregator.AggregateEnvironmental`/`AggregateCost` lifecycle folds that sum the per-ply EN 15978 GWP and cost over a LayerSet/ConstituentSet the SAME way the realized U-value/STC folds aggregate — one discipline by case and column growth, never a parallel Eco/Cost owner.
- Shape: A new sibling page Properties/sustainability.md realizing the three MaterialProperty cases, the LifecycleStage/Currency/ClassificationSystem closed bands, the EPD/embodied-carbon receipt, and the two lifecycle aggregation folds; properties.md MaterialProperty Union widens by three cases and AssemblyProperty gains the GWP/cost columns, the band-2450 MaterialFault and the MaterialPropertySet bundle unchanged.
- Unlocks: A whole-building embodied-carbon takeoff and a cost-of-construction rollup compute from the same MaterialAssignment plies the thermal/acoustic envelope already folds, so the construction model feeds the forward cs:AEC_SIMULATION_BRIDGE LCA/QTO consumer by MaterialId without a second property surface, and a material federates to IFC Pset_EnvironmentalImpactValues / Uniclass at the Rasm.Bim boundary.
- Anchors: properties#MATERIAL_PROPERTY MaterialProperty Union + MaterialPropertySet + the banded Acoustic 6-band ReadOnlyMemory<double> carrier (properties.md lines 70-119); properties#ASSEMBLY_PROPERTY AssemblyAggregator fold-over-plies (lines 220-279); assembly#MATERIAL_ASSIGNMENT LayerSet/ConstituentSet; UnitsNet Mass/MassConcentration for the GWP kgCO2e quantity; EN 15978 lifecycle modules, ISO 14040/14044 stages, the EPD declared-unit basis, Uniclass2015/OmniClass tables, IFC Pset_EnvironmentalImpactValues.
- Tension: The embodied-carbon GWP rides a UnitsNet Mass/MassConcentration quantity per declared functional unit and the per-module vector is a fixed-length ReadOnlyMemory<double> over the LifecycleStage band, so the discipline reuses the realized banded-vector carrier shape rather than minting a new collection primitive; the procurement lead-time row is dropped below the density bar.

[SPECTRAL_REFLECTANCE_GROUNDING]-[QUEUED]: Promote the three-band RGB appearance edge to a measured-spectral reflectance grounding that mines the full Wacton.Unicolour Spd/SpectralCoefficients/Observer/Illuminant surface so every Materials base color, conductor, and pigment grounds through one spectral upsampling and one measured-illuminant reduction, and the ConductorIor three-band table reconstructs a full per-wavelength eta/k curve from its RGB anchors through the Rasm.Compute transitive ONNX Runtime path. The validated `RgbSpectrum` `[ComplexValueObject]` band carrier and the MacAdam spectral-locus admission gate are realized; the open legs are the spectral upsampling/illuminant reduction and the ONNX conductor curve.
- Capability: One spectral grounding owner over the realized validated carrier: the bsdf#SPECTRAL_UPSAMPLE RGB->SPD kernel widens to consume the Unicolour Spd/SpectralCoefficients spectral construction and a measured Illuminant/Observer reduction so a MaterialLibrary base color carries a witnessed reflectance SPD that the realized graph#MATERIAL_LIBRARY SpectralAdmit MacAdam spectral-locus gate (IsImaginary then IsInMacAdamLimits, MapToSpectral the recoverable nearest) admits as physically reachable, AND the bsdf#CONDUCTOR_IOR ConductorMetal table replaces the hand-transcribed Johnson-Christy RGB triples with a spectral-inference projection (the RGB anchor reconstructed to a per-wavelength complex-IOR curve through the Compute ONNX spectral-reconstruction model the SpectralUpsample ToSpd consumes per band). Every channel rides the realized RgbSpectrum/ComplexIor `[ComplexValueObject]` carriers — non-finite/negative bands unrepresentable at Create — so the spectral curve reduces to the validated band carrier the ToSpd/SceneLinear kernel, the ConductorIor table, and every lobe already produce and read, never a raw triple. Also consolidates the inline `new Unicolour(Configuration, Spd)` constructions scattered across acquisition#GroundSpectral and finish#FinishMix.Reflectance onto the one declared SPECTRAL_UPSAMPLE owner — one spectral surface the lobe kernel, acquisition, finish, and library row read, never N parallel inline spectral-construction sites.
- Shape: A grounding capability on bsdf.md#SPECTRAL_UPSAMPLE and #CONDUCTOR_IOR (or surface.md post-split): the SpectralUpsample kernel composing Unicolour Spd/SpectralCoefficients/Observer (Degree2/Degree10) / Illuminant (the 10 A/C/D50-D75/E/F-series statics) over the realized Smits seven-basis ToSpd/Resample5nm fence, the ConductorIor spectral-inference projection over the Compute ONNX path replacing the realized eight-row measured `(η, k)` table, and the measured-illuminant reduction every base color, conductor, and FinishMix pigment grounds through — the SpectralBand 3-band SmartEnum stays the fast-path band-centre vocabulary the spectral curve reduces to and the realized SpectralAdmit gate the spectral-reachability admission, the acquisition/finish inline Spd sites collapsed onto the one owner.
- Unlocks: Every metal grounds in a measured complex-IOR curve rather than a guessed RGB triple, a base color round-trips through a witnessed reflectance SPD under a named illuminant and clears the realized MacAdam spectral-locus bound, metameric-pair and custom-illuminant (D50/D65/F-series swap) rendering admits, and the FinishMix Kubelka-Munk path and the conductor lobe read one spectral grounding — the appearance engine reaches measured-spectral fidelity professional renderers require, the EPFL-RGL upstream-blocked leg awaiting only the Compute ONNX path.
- Anchors: bsdf#SPECTRAL_UPSAMPLE realized RGB->SPD Smits kernel (ToSpd/Resample5nm) + Unicolour Spd->XYZ composition + the validated RgbSpectrum carrier SceneLinear produces; bsdf#CONDUCTOR_IOR realized eight-row ConductorMetal/ConductorIor measured `(η, k)` table over ComplexIor + the spectral-curve upstream-blocked note; graph#MATERIAL_LIBRARY realized SpectralAdmit/MapToSpectral MacAdam spectral-locus gate (IsImaginary/IsInMacAdamLimits) the third domain gamut gate; acquisition#GroundSpectral + finish#FinishMix.Reflectance inline new Unicolour(Configuration, Spd) sites the owner absorbs; Wacton.Unicolour Spd/SpectralCoefficients/Observer/Illuminant + MacAdam-limit surface (api-unicolour [02]); ARCHITECTURE [02]-[SEAMS] Appearance/bsdf <- Rasm.Compute/Symbolic [PORT] ONNX spectral-reconstruction conductor curve; the EPFL-RGL measured-BRDF spectral set, the Johnson-Christy/Rakic optical constants.
- Tension: The validated band carrier (RgbSpectrum/ComplexIor) and the MacAdam spectral-locus admission are realized, so the grounding reuses the landed carrier and reachability gate rather than minting either; the remaining open work is the full per-wavelength conductor curve, which depends on the Rasm.Compute transitive ONNX Runtime spectral-reconstruction path — until that lands the table grounds three measured RGB-band anchors, the spectral inference the upstream-gated leg over a realized three-band fast path, and the ONNX must ride the Compute Symbolic seam (Materials references no ML package directly, ARCHITECTURE [03] composition-over-re-mint law).

[GENERATED_BOND_INTERPRETER]-[QUEUED]: Promote the masonry Bond axis from a closed template-course lookup to a parametric generative bond algebra so a flemish/herringbone/basketweave/pinwheel/diaper/quetta bond DERIVES its course placement from a parametric bond geometry rather than a hand-keyed template course set, turning BondName from a data table into a generative surface the layout fold consumes the same way it reads a template bond.
- Capability: A `GeneratedBond.Interpret` interpreter algebra on BondName producing a CourseTemplate for a `BondKind.Generated` row from a parametric `BondGeometry` descriptor (the unit-cell repeat, the per-course orientation pattern, the diagonal/herringbone rotation, the flemish alternating stretcher-header sequence) so `BondName.Course(index, key)` resolves a generated bond by COMPUTING its course rather than railing ProfileFault.Bond — the interpreter one fold over a bounded bond-geometry vocabulary, never a per-bond method and never a parallel generated-layout owner. The higher-order move: the Bond axis becomes a generative algebra (template-OR-computed) rather than a fixed catalogue, so a new decorative bond is a BondGeometry row not a transcribed course set.
- Shape: Realized capability on masonry#PROFILE_FAMILY: the `BondGeometry` parametric descriptor (repeat/offset-pattern/rotation columns), the flemish/herringbone/basketweave/pinwheel/diaper/stack generated rows replacing the stubs that rail today, and the `GeneratedBond.Interpret` course-deriving fold filling the Course arm the layout#ASSEMBLY_FOLD StepCourse already consumes; the BondName SmartEnum gains the generated rows, the CourseTemplate shape and the Construction layout fold unchanged.
- Unlocks: Every decorative and structural bond (running/english/flemish/stack/herringbone/basketweave/pinwheel/diaper/quetta) places through the one Construction layout fold as a parametric Bond axis, so the construction model spans the full professional masonry-bond vocabulary by data and a new bond is a BondGeometry row, removing masonry's last open probe and elevating the Bond axis to a generative surface.
- Anchors: masonry#PROFILE_FAMILY BondName/BondKind/CourseTemplate/Orientation (masonry.md lines 11-16), the named GENERATED_BOND_INTERPRETER probe and the BondKind.Generated rows that today rail ProfileFault.Bond (masonry.md lines 3, 13); layout#ASSEMBLY_FOLD StepCourse template consumption + Placement.PathAngleDegrees for per-unit rotation; the flemish/herringbone/basketweave/pinwheel coursing geometry, the diaper/quetta diagonal-pattern repeat rules.
- Tension: A herringbone/diaper bond rotates units off the horizontal course axis, so the interpreter emits per-unit rotation the Placement.PathAngleDegrees already carries; it derives orientation+rotation into the existing CourseTemplate/Placement columns rather than widening the Element model, keeping the host-neutral scalar-Placement discipline layout.md fixes.

[STEEL_DESIGN_OBJECT]-[QUEUED]: Grow the steel family from the five-W-shape I-shape seed to the full AISC v16.0 shape roster with LRFD design-capacity columns AND the composite/cold-formed SteelClass arms, replacing the single IsCompact boolean with the professional steel-design receipt so a steel section is a true AISC design object spanning rolled, composite, and cold-formed members rather than a dimensional row.
- Capability: The declared U-shape/L-shape/HSS-rect/HSS-round/Tee SteelClass cases (already declared, unseeded) get their row tables (C/MC channels, L angles, HSS, WT/MT/ST tees) keyed by EDI designation; IsCompact widens to a `CompactnessClass` (compact/noncompact/slender by flange AND web width-to-thickness against AISC Table B4.1) and a `DesignCapacity` projection emits the LRFD flexural phi-Mn (yielding/LTB/FLB), compression phi-Pn (flexural buckling over a slenderness input), and shear phi-Vn from the Zx/Sx/Ix/area columns already stored; two further SteelClass arms `Composite` (AISC 360 Chapter I steel-concrete over the slab effective-width/depth and the shear-stud reference into the JOINT_CONNECTION_FAMILY stud vocabulary) and `ColdFormed` (AISI S100 light-gauge over gross/effective-width, corner radius, design thickness, and the effective-section-modulus local-buckling reduction) each map to their IfcProfileDef subtype — every capacity a derived projection over the PositiveMagnitude columns, every section a SteelRow, never a re-minted dimension or a parallel composite owner.
- Shape: A capability addition on steel.md#STEEL_FAMILY: the CompactnessClass SmartEnum replacing the bool, the DesignCapacity record + SteelSection.Capacity(double yieldMpa, double unbracedLengthMm, double effectiveLengthMm) BoundaryAdapter projection, the channel/angle/HSS/tee SteelRow tables seeding through the IShapeOf-pattern admission into BuildSteelRows, and the SteelClass.Composite/ColdFormed cases with their IfcProfileDef subtype mapping and effective-section projections.
- Unlocks: A beam/column/brace reports its LRFD design strength and slenderness classification the structural seam reads directly, a composite floor beam and a cold-formed stud carry the same Profile shape and place through the same Resolve fold as a rolled W-shape, so the steel family covers the full structural-steel product range (rolled + composite + cold-formed) by SteelClass case and row data.
- Anchors: steel#STEEL_FAMILY SteelClass (i/u/l/hss-rect/hss-round/tee declared at line 12, only IShapeRows seeded lines 67-72), SteelSection FlangeSlenderness/WebSlenderness/RadiusOfGyrationMm + IsCompact bool (lines 45-56), BuildSteelRows/IShapeOf admission (lines 64-92); steel#COMPOSITE_AND_BUILTUP the named SteelClass growth note (line 99); the JOINT_CONNECTION_FAMILY shear-stud StudClass for the composite interface; AISC Shapes Database v16.0 rows, AISC 360 Chapters B4.1/E/F/G/I, AISI S100 effective-width method, the IfcProfileDef subtype mapping.
- Tension: The composite arm references the JOINT_CONNECTION_FAMILY shear-stud StudClass for its shear interface, so the composite leg sequences after that idea lands; the rolled-shape roster, capacity columns, and cold-formed arm are independent. The IsCompact bool->CompactnessClass widening is a true polymorphic collapse (a 3-state vocabulary replacing a 2-state flag carrying design-class semantics).

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
