# [APPUI_FEATURES]

Feature atlas for the product UI rail. Every concept rides SurfaceHost, DataSource, ThemeVariant×Density, TypographyRole, IconSource, ChartSeriesSpec, EditorFactory, DialogIntent, CommandIntent, MotionToken, TableProjection, DragPayload, LocaleRow, and EvidenceReceipt rows — a new concept is a row, never a surface. Mechanics live in the `.planning/` pages; each row below cites the owning page clusters.

## [1]-[CONCEPTS]

| [INDEX] | [CONCEPT] | [MODALITIES] | [PAGES] |
| :-----: | --------- | ------------ | ------- |
| [1] | One shell mounted on any host through one mount transaction; foreign-view embedding with retained NSView handoff; one UI scheduler boundary; per-RID native asset identity receipts | all UI | surface-hosts#HOST_AXIS, #EMBED_CAPSULE, #SCHEDULER_BOUNDARY, #NATIVE_ASSETS, #SCALE_FOCUS |
| [2] | Routing spine with deep-link grammar; dock layouts with checkpoint cadence, drain flush, crash restore; chrome derivation from intent keys; adaptive breakpoints | all UI | shell-navigation#ROUTING_SPINE, #DOCK_LAYOUTS, #SHELL_CHROME, #ADAPTIVE_LAYOUT |
| [3] | One screen catalog row drives dockables, titles, automation names, and proof lanes; activation scopes with suspend/resume; paced derived state; typed-rail validation lift; per-surface state snapshots with restore-on-activate merge | all UI | screens-activation#SCREEN_CATALOG, #ACTIVATION_SCOPES, #DERIVED_STATE, #VALIDATION_UX, #SCREEN_STATE |
| [4] | Command palette + deep links + remote invocation + journal replay over one intent table; availability algebra over degradation, validation, selection, busy; total execution receipts; fuzzy palette search; ControlService verbs as table rows | all UI | commands-availability#INTENT_TABLE, #AVAILABILITY_ALGEBRA, #EXECUTION_RECEIPTS, #PALETTE_AND_REMOTE |
| [5] | Live-data spine (host fact to projection to tag transition to delta fetch to IChangeSet); fourteen operator rows; one UI-thread binding capsule; stat folds and change audit | all UI | live-data#DATA_SOURCES, #CHANGE_PIPELINES, #BINDING_CAPSULE, #AGGREGATION_SPINE |
| [6] | Virtualized tables with classified-column masking; serializable view state in one DeferRefresh; tree-flatten fold on the free DataGrid; grid-edit commit to StoreOp.Upsert and DocumentTransaction; delimited exports | all UI | tables-hierarchy#GRID_SUBSTRATE, #VIEW_STATE, #TREE_FLATTEN, #GRID_COMMIT |
| [7] | Property inspector with eleven ranked editor rows incl. value-object and quantity editors; preview-versus-commit edit rail; options inspector to user-settings write and ReloadReceipt banner; conflict-resolution pane over Persistence conflict receipts; grammar-scoped code panes with completion projection | all UI | inspector-editing#INSPECTOR_SURFACE, #EDITOR_FACTORIES, #COMMIT_VALIDATION, #OPTIONS_INSPECTOR, #CONFLICT_RESOLUTION, #CODE_EDITING |
| [8] | Fifteen chart series rows incl. geo-map; five axis scales; one interaction policy with ZoomBorder dashboard canvas; windowed + downsampled stream binding; benchmark and activity-timeline dashboards as named layout rows | all UI | charts-dashboards#SERIES_TABLE, #AXES_SECTIONS, #CHART_INTERACTION, #STREAM_BINDING, #DASHBOARD_TILES |
| [9] | Borrowed/owned Skia draw capsule; host-agnostic thumbnails with blob-backed cache; receipt-keyed zoomable previews; content-hashed encode identity; PDF/XPS document export through SKDocument — zero new render packages | all | visuals-offscreen#DRAW_CAPSULE, #THUMBNAIL_PIPELINE, #PREVIEW_SURFACES, #ENCODE_IDENTITY, #DOCUMENT_EXPORT |
| [10] | One frozen token catalogue feeding five consumers; orthogonal variant and density families composed at resolve; host-matched appearance probing; atomic theme swap with token-diff receipts | all UI | theme-tokens#TOKEN_CATALOG, #VARIANT_AXIS, #DENSITY_AXIS, #CONTROL_THEMES |
| [11] | Ten typography roles; deterministic embedded-Inter admission with ranked fallback chains; HarfBuzz shaping before every Skia glyph; Markdig AST to role-keyed rows (AvaloniaEdit owns code fences); baseline-grid metrics with tabular-numeral proof | all UI | typography-shaping#ROLE_AXIS, #FONT_ADMISSION, #SHAPING_RAIL, #MARKDOWN_PROJECTION, #TEXT_METRICS |
| [12] | Five icon sourcing cases with rank-column fallback walk; retained SVG pipeline with animation invalidation and hit testing; async raster rows with DPI variants; nameof-derived asset catalog with preload receipts and geo asset routing | all UI | icons-assets#ICON_AXIS, #SVG_PIPELINE, #RASTER_ASSETS, #ASSET_CATALOG |
| [13] | Six dialog intents with typed Fin results and dismissal as a value; per-surface session topology rows; toast suppression fold over phase and degradation with queued/dropped receipts; host-agnostic pickers with format-derived filters | all UI | dialogs-notifications#DIALOG_INTENTS, #SESSION_ALGEBRA, #NOTIFICATIONS, #PICKERS_HOST_MODALITY |
| [14] | Hotkey derivation from the command table with platform chord transform; admitted behavior trigger/action rows; pan-zoom canvas family; typed drag payloads and clipboard codec rows on the validation rail | all UI | input-interaction#HOTKEY_DERIVATION, #BEHAVIOR_RAIL, #POINTER_GESTURES, #DRAG_CLIPBOARD |
| [15] | Six motion tokens with reduced pairs and spring algebra; plan rows binding transitions, charts, zoom; ProgressPhase-to-MotionToken frozen mapping; one global reduced-motion degrade switch; OKLab perceptual color interpolation | all UI | motion-tokens#MOTION_AXIS, #MOTION_APPLICATION, #PHASE_MAPPING, #REDUCED_MOTION |
| [16] | Catalog-sourced automation identity and live-region announcements; tab-order, trap, and refocus law; the suite's single WCAG luminance gate with floor rows; per-row compliance audit across the variant-density grid | all + test-host | accessibility#AUTOMATION_PEERS, #KEYBOARD_NAV, #CONTRAST_GATE, #COMPLIANCE_PROOF |
| [17] | Locale rows on inbox resx/ICU/NodaTime with a pseudo-locale conformance row; nameof-derived string tables with PropertyGrid bridge; atomic culture composition under the options reload class; RTL mirroring with icon exemption | all UI | localization-culture#LOCALE_AXIS, #STRING_TABLES, #CULTURE_COMPOSITION, #RTL_MIRRORING |
| [18] | Seven-case evidence union sealed through the HLC sink envelope; correlation join with typed skew bands and model-result provenance projection; render-hash capture lanes; catalog-derived headless proof matrix with command-journal replay under virtual time; Debug hot-reload loop with dispatcher-lag probe | all + test-host | diagnostics-evidence#RECEIPT_UNION, #CORRELATION_JOIN, #CAPTURE_LANES, #HEADLESS_DERIVATION, #DEV_LOOP |

## [2]-[CAPABILITY_ROWS]

- Hosts: avalonia-desktop, rhino-panel, rhino-modal, gh2-companion, sidecar-shell, headless; web-browser stays designed-only — TS owns web UI while CommandIntent keys and EvidenceReceipt cases serialize for it today (commands-availability#TS_PROJECTION, diagnostics-evidence#TS_PROJECTION).
- Live data: DynamicData change-set engine — set algebra, grouping, virtualization, paging, expiry, TransformToTree; SourceCache keys ride the Persistence IdentityPolicy vocabulary; classified-exclusion subtracts the DataClassification deny projection (live-data#CHANGE_PIPELINES).
- Controls: free DataGrid + tree-flatten fold (TreeDataGrid rejected), PropertyGrid with Thinktecture value-object editor wiring, Dock layouts with checkpoint cadence + crash restore, PanAndZoom canvases, AvaloniaEdit + TextMate fences, Markdig role folds, FluentIcons + SVG rows with fallback ranks, AsyncImageLoader rows.
- Motion: MotionToken rows with reduced pairs; ProgressPhase-to-MotionToken frozen mapping with a conformance sweep that fails on Compute phase drift; spring parity with the host SpringPreset tuning math; OKLab interpolation in parity with the host kernel (motion-tokens#MOTION_AXIS, #PHASE_MAPPING).
- Evidence: EvidenceReceipt cases serialize for dashboards; the correlation join owns the three-stream fold keyed correlation + HLC; headless + Skia render-hash lanes prove visual states in CI; replayed journals, deep links, and interactive execution seal the same receipt family (diagnostics-evidence#CORRELATION_JOIN, #HEADLESS_DERIVATION).
- Wire: command intent, availability, invocation, payload, outcome, and receipt shapes plus the six evidence wire shapes transcribe the camelCase Strict emission; receipts bind as envelope payload type parameters (commands-availability#TS_PROJECTION, diagnostics-evidence#TS_PROJECTION).
