---
title: Rasm API Stacking Census
input: full_diff
effort: high
conclusion: neutral
include:
  - "libs/**"
---

# [API_STACKING]

Bidirectional capability closure is this pass's one question: whether every touched fence — the diff and the touched file around it — stacks admitted packages and host SDKs to their deepest contract-complete form and consumes every declared capability. Run the census against the folder `.api/`, language `.api/`, and central manifest before any finding. Approved leverage proves the exact invocation, return shape, complete provider roster, owner input contract, and preserved semantics; a partial catalogue fragment or an operator lacking the owner's deterministic walk, geometry, precedence, or custody contract grounds no demand.

Hunt these classes, each finding naming file, anchor, the shallow or hand-rolled form, and the exact catalogued member demanded:
- Handroll: a fence body re-deriving capability an admitted catalog member owns at either tier — folder overlay or language substrate — proven leverage includes `Try.lift(...).Run()` for provider exception custody; `TensorPrimitives` fused arithmetic, finite-span predicates, and reductions; QuikGraph roots, paths, acyclicity, components, and sinks; and `ArrayPoolBufferWriter<byte>` for framed canonical emission; the demand names the member, canonical owner, and smallest replacing range.
- Under-stacking: a shallow composition leaving a deeper verified member idle — proven leverage includes NodaTime interval, clock, and mapping operations; quantity algebra; endpoint-inclusive numeric lattices; provider-native segment geometry; scaled heterogeneous inverse minimization; and a broker client's batch-grain delivery family — batch fold, per-record retry, manual offset resolution, commit precedence — over a flat per-message callback. Acceptance, fallback, deterministic walk, precedence, and custody must already fit the owner; the extension lands as a row, case, or member, never a parallel surface.
- Unconsumed capability: a rail-verified host or package member that strengthens a touched owner and sits unconsumed — the extension lands as a row, case, or member on that owner, never a parallel surface.
- Decorative declaration: a capability noun with no anchoring fence member, a receipt nothing mints, or a policy row nothing consumes — demand the consuming member or the deletion; a policy column consumed only by a cache key, receipt, or log annotates rather than governs, and an arm that cannot honor a policy field refuses the combination loudly before provider work.
- Parallel-owner admission: an overlapping second package composed as a peer owner rather than a bounded fast-path under a named split predicate — the predicate decides which surface serves a call, a per-page arbitrary choice between two overlapping surfaces is the defect, and a fast-path member never owns the concern it accelerates; an admitted package no touched page composes is an orphaned admission demanding the composing fence or the re-card; a transport or overlay package re-exporting its core dependency's types makes the re-exported spelling the one import — a duplicate core import beside it is the same defect at import grain.
- Charter-gap ideation: a catalogued capability that unlocks what the folder charter implies yet no touched page carries — the gap names the member and existing owner, while an unresolved invocation, incomplete vocabulary, missing precedence law, or unproven consumer contract returns no finding. Catalog admission alone earns no demand: a capability whose landing mints a parallel entry point, a new policy or persistence surface, or law the touched owner's concern does not carry is card material, never a finding.

Settled corpus law outranks intuition: a composition the owning page or `.api/` catalogue rules is never a finding, and a manifest-admitted package under a temporary distribution gap stays design-real. A pass that finds nothing after a genuine census states that verdict plainly.
