{
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "name": "eng",
    "tags": ["language:python"],
    "targets": {
        "provision": {
            "command": "uv run --only-group eng python -m eng.scripts.provision",
            "cache": false,
            "parallelism": false
        },
        "lint": { "inputs": ["default", "python", "{workspaceRoot}/eng/native/**/*"] },
        "format": {},
        "typecheck": {},
        "check": {}
    }
}
