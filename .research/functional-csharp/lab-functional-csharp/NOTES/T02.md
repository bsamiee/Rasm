# T02 notes: Value Objects

Lab: `.scratch/functional-csharp/lab-functional-csharp/T02`, namespace `Lab.T02`. Build: zero diagnostics. Run: exit 0, every check in `Samples.Run()` passes. Doc code and lab code are the same text: one lab file per doc section, generated from the fences by a sync script, plus `Samples.cs` as scaffolding.


## (a) Verified identifiers

- `ValueObjectAttribute<TKey>` with `where TKey : notnull` — decomp:core
- `ValueObjectAttribute<TKey>.KeyMemberAccessModifier` default `Private` — decomp:core
- `ValueObjectAttribute<TKey>.KeyMemberKind` default `Field` — decomp:core
- `ValueObjectAttribute<TKey>.KeyMemberName` default `_value` — decomp:core, probe (generated `_value` used by `PostalCode.Length`, `DayMonth`)
- `ValueObjectAttribute<TKey>.SkipKeyMember` — decomp:core, probe (`OpenEndDate`)
- `ValueObjectAttribute<TKey>.NullInFactoryMethodsYieldsNull` — decomp:core, probe P6
- `ValueObjectAttribute<TKey>.EmptyStringInFactoryMethodsYieldsNull` (implies the null setting) — decomp:core, probe P6, wiki:Value-Objects-Customization
- `ValueObjectAttribute<TKey>.SkipIComparable`, `SkipIParsable`, `SkipISpanParsable`, `SkipIFormattable` — decomp:core, probe (`SkipIFormattable` removes `Formattable.g.cs`, MA0185 in lab confirms no `IFormattable`)
- `ValueObjectAttribute<TKey>.AdditionOperators`, `SubtractionOperators`, `MultiplyOperators`, `DivisionOperators` — decomp:core, probe P7
- `ValueObjectAttribute<TKey>.ComparisonOperators`, `EqualityComparisonOperators` — decomp:core, probe P7, probe TTRESG105
- `ValueObjectAttribute<TKey>.ConversionToKeyMemberType` default `Implicit` — decomp:core, probe (generated `implicit operator string?(ProductName?)`, `implicit operator decimal(Amount)`)
- `ValueObjectAttribute<TKey>.ConversionFromKeyMemberType` default `Explicit` — decomp:core, probe (generated `explicit operator ProductName?(string?)`, `(ProductName)"ab"` throws `ValidationException`)
- `ValueObjectAttribute<TKey>.UnsafeConversionToKeyMemberType` default `Explicit`, throws `NullReferenceException` on `null` — decomp:core, decomp:sourcegenerator
- `ValueObjectAttributeBase.SkipFactoryMethods`, `ConstructorAccessModifier` (default `Private`), `CreateFactoryMethodName` (default `Create`), `TryCreateFactoryMethodName` (default `TryCreate`), `SkipToString`, `AllowDefaultStructs`, `DefaultInstancePropertyName` (default `Empty`), `SkipEqualityComparison` — decomp:core
- `CreateFactoryMethodName = "FromRange"` yields `FromRange`, `TryFromRange`, `FromRangeCore`, `ValidateCore` — probe (Span)
- `ComplexValueObjectAttribute.DefaultStringComparison` default `OrdinalIgnoreCase` — decomp:core, probe TTRESG049 without it
- `KeyMemberEqualityComparerAttribute<TAccessor, TKey>`, `KeyMemberComparerAttribute<TAccessor, TKey>` — decomp:core, probe
- `MemberEqualityComparerAttribute<TAccessor, TMember>` — decomp:core, probe P8 (`Item` equality by `Identifier` alone, ordinal)
- `IgnoreMemberAttribute` targets property and field — decomp:core, probe (exempts `Tagged._tag` from TTRESG001)
- `ValidationErrorAttribute<TValidationError>` with `class, IValidationError<TValidationError>` — decomp:core, probe P12
- `IValidationError<out T>.Create(string message)` static abstract — decomp:core
- `ValidationError` sealed with `Message` — decomp:core
- `ComparerAccessors.StringOrdinal`, `StringOrdinalIgnoreCase`, `CurrentCulture`, `CurrentCultureIgnoreCase`, `InvariantCulture`, `InvariantCultureIgnoreCase`, `Default<T>` — decomp:core
- `IEqualityComparerAccessor<T>.EqualityComparer`, `IComparerAccessor<T>.Comparer` static abstract — decomp:core, wiki:Value-Objects-Customization
- `OperatorsGeneration { None, Default, DefaultWithKeyTypeOverloads }`, `ConversionOperatorsGeneration { None, Implicit, Explicit }`, `AccessModifier`, `MemberKind` — decomp:core
- `TypeParamRef1` through `TypeParamRef5` — decomp:core, probe (`Measure<T>`)
- `IDisallowDefaultValue` added to struct value objects — decomp:sourcegenerator
- `Create` throws `ValidationException(validationError.ToString() ?? "Validation failed.")` — decomp:sourcegenerator, probe P1 and generated text
- `TryCreate(value, out obj)` and `TryCreate(value, out obj, out validationError)` — probe P2, generated text
- `[NotNullWhen(true)]` on `obj` present by default, absent with `NullInFactoryMethodsYieldsNull` — probe generated text (`ProductName` versus `ProductNameOrNull`)
- `Validate(TKey value, IFormatProvider? provider, out T? obj)` — probe P3, generated text
- Complex `Validate(members..., out T? obj)` without provider — probe (`Boundary.TryCreate(2m, 1m, out _)`, sample `Boundary.Validate(lower, upper, out ...)`)
- Null key error `The argument 'value' must not be null.` — probe P1/P3
- Null member error `The member "Currency" of type "CurrencyAmount" must not be null.` — probe X
- `ValidateFactoryArguments(ref TValidationError? validationError, ref TKey value[, extras])` — probe generated text
- Extra parameter emitted with `= null` on the generated declaration, `Validate` passes `default` to `ValidateCore` — probe P4 generated text
- `private static TValidationError? ValidateCore(...)`, `private static T CreateCore(...)` — probe P4 generated text
- Non-void hook: `private static partial string ValidateFactoryArguments(ref ValidationError? validationError, ref int value)` and `partial void FactoryPostInit(string factoryArgumentsValidationError)` called on the constructed instance in `Validate` — probe P10 generated text and runtime (`Tagged(5).Tag == "odd"`)
- `ValidateConstructorArguments(ref TKey value)` — wiki:Value-Objects, decomp:sourcegenerator (`ValidateConstructorArguments()` call in generated smart enum constructor confirms the family)
- Keyed `ToString()` returns key `ToString()`, complex `ToString()` returns `{ Lower = 1.23, Upper = 2.57 }` — probe P8
- Generated `IFormattable.ToString(format, provider)` forwards to the key even with `SkipToString = true` — probe generated `OpenEndDate.Formattable.g.cs`
- `Parse(string s, IFormatProvider? provider)`, `Parse(ReadOnlySpan<char> s, IFormatProvider? provider)`, `TryParse(...)` — probe P13 generated text
- `Parse` throws `FormatException(validationError.ToString() ?? ...)` — probe P13, generated text
- `Parse` with `EmptyStringInFactoryMethodsYieldsNull` throws `FormatException("Unable to parse \"T\". Empty or whitespace input is not allowed because it would yield null.")` — probe P6
- `[TypeConverter(typeof(ThinktectureTypeConverter<T, TKey, TValidationError>))]` — probe P11 generated text and `TypeDescriptor.GetConverter`
- `public static readonly T Zero = default` / `Empty = default` field — probe P5 generated text
- `operator checked +` and siblings for `decimal` and `int` keys — probe P7 generated text
- Key-type overloads in both operand orders — probe P7
- Arithmetic result through `Create` (`Amount.Create(1m) - 5m` throws) — probe P7
- `Amount.Zero == default(Amount)` — probe P5
- Generated equality for a non-string key calls `this._value.Equals(other._value)` — probe generated text
- `TTRESG001` field must be read-only (Error) — decomp:analyzers, probe
- `TTRESG003` property must be read-only (Error) — decomp:analyzers, probe
- `TTRESG043`, `TTRESG044`, `TTRESG045`, `TTRESG047`, `TTRESG057`, `TTRESG058`, `TTRESG074`, `TTRESG076`, `TTRESG080` (Error) — decomp:analyzers
- `TTRESG048`, `TTRESG049`, `TTRESG102`, `TTRESG103`, `TTRESG104`, `TTRESG105`, `TTRESG109`, `TTRESG110` (Warning) — decomp:analyzers
- `TTRESG048` fires on `[ValueObject<string>]` without comparer, `TTRESG049` fires on a complex type with a string member and no `DefaultStringComparison`, `TTRESG104` fires on `init` and `set` properties of a type that disallows default (not on get-only members or record parameters), `TTRESG105` fires when `ComparisonOperators` is set alone — probe Diagnostics.cs
- `ThinktectureJsonConverterFactory` (non-generic) — decomp:json
- `ThinktectureNewtonsoftJsonConverterFactory` — decomp:newtonsoft.json
- `ThinktectureMessageFormatterResolver.Instance` — decomp:messagepack
- `ThinktectureModelBinderProvider` — decomp:aspnetcore
- `AddThinktectureOpenApiFilters(this IServiceCollection, Action<ThinktectureSchemaFilterOptions>?)` — decomp:swashbuckle
- `ThinktectureSchemaFilterOptions.RequiredMemberEvaluator` default `RequiredMemberEvaluator.FromDependencyInjection`, registration `TryAddSingleton<IRequiredMemberEvaluator, DefaultRequiredMemberEvaluator>` — decomp:swashbuckle
- `RequiredMemberEvaluator.Default`, `All`, `None`, `FromDependencyInjection` — decomp:swashbuckle
- `UseThinktectureValueConverters(this DbContextOptionsBuilder)` and overload with `Configuration` — decomp:entityframeworkcore10
- `AddThinktectureValueConverters` on `ModelBuilder`, `EntityTypeBuilder`, `OwnedNavigationBuilder`, `ComplexPropertyBuilder` — decomp:entityframeworkcore10
- `Configuration.UseConstructorForRead` default `true`, `Configuration.KeyedValueObjects` default `KeyedValueObjectConfiguration.NoMaxLength` — decomp:entityframeworkcore10
- `FixedKeyedValueObjectMaxLengthStrategy`, `CustomKeyedValueObjectMaxLengthStrategy(Func<Type, Type, MaxLengthChange>, bool)`, `NoOpKeyedValueObjectMaxLengthStrategy`, `MaxLengthChange` — decomp:entityframeworkcore10
- `ObjectFactoryAttribute<T>.UseForSerialization`, `UseForModelBinding`, `UseWithEntityFramework`, `HasCorrespondingConstructor` — wiki:Object-Factories, probe (`FileUrn` compiles with `HasCorrespondingConstructor = true` and a `private FileUrn(string)` constructor)
- `IObjectFactory<T, string, ValidationError>` requires `Validate(string?, IFormatProvider?, out T?)` — probe (generated error CS0535 when the method was missing)
- `IConvertible<string>` requires `ToValue()` — probe generated `FileUrn` interfaces
- `SerializationFrameworks.All` — decomp:core, probe
- JSON: `Amount` as `10.5`, `Boundary` as `{"Lower":1,"Upper":2}`, `FileUrn` as `"blob:a/b.pdf"`, invalid read throws `JsonException` with the validation text — probe P9, lab checks
- `Destructure.UsingThinktectureRuntimeExtensions(TypesToRenderAsString renderAsString = None)` — decomp:serilog
- Serilog: simple value object logs as key (`99.95`, `Widget`), complex value object falls to default destructuring (`{"Lower": 1, "Upper": 2, "$type": "Boundary"}`) — probe
- `[Union]` `Jurisdiction` with nested value object cases and `Switch` — probe X
- `[SmartEnum]` keyless with `[UseDelegateFromConstructor]`, constructor parameter order members then delegate — probe generated text
- `OpenEndDate`: `default == Infinite`, `Infinite == DateOnly.MaxValue`, `MoveToEndOfMonth` — probe X
- `DayMonth`: `Create(2, 29)` valid, `Create(4, 31)` and `Create(13, 1)` throw `ValidationException`, implicit conversion from `DateOnly` — probe X, lab
- `Measure<T> where T : INumber<T>`: `+`, `Parse`, `<`, negative rejected — probe X, lab

## (b) Statements of the existing artifacts dropped or corrected

- value-objects-1: `Amount.Parse("invalid")` with one argument. Generated `Parse(string s, IFormatProvider? provider)` has two parameters (probe P13). Dropped.
- value-objects-1: "Introduced with .NET 5 in 2020" and other source-generator history. Not a library fact, dates banned. Dropped.
- value-objects-1: `value.Trim().ToUpper()`. Culture-sensitive, fails the repo analyzers. Replaced by `ToUpperInvariant()` and listed as a design rule.
- value-objects-1: `[ValueObject<string>]` types without comparer attributes. `TTRESG048` warns (probe Diagnostics.cs), warnings as errors. Every string-keyed fence carries both attributes.
- value-objects-1: `_value.StartsWith("INV-")` without `StringComparison`. Fails CA1310 under the repo analyzers. Dropped with the `ReferenceCode` fence.
- value-objects-2: "Normalization, such as rounding, could also occur here" comment. Empty statement, banned modal. Dropped.
- value-objects-2 and wiki: `Math.Round(lower, 2)`. Fails MA0193 under the repo analyzers. Spelled `Math.Round(lower, 2, MidpointRounding.ToEven)`.
- value-objects-3: `Money` hook checks `currency is null`. The generated `Validate` rejects a null non-nullable member before the hook runs (probe X: `The member "Currency" of type "CurrencyAmount" must not be null.`). Dropped and listed as a design rule.
- value-objects-3: `Order` aggregate with mutable `List<T>` and `private set`. Not library content, exceeds budget. Dropped.
- value-objects-4: `OpenEndDate` without `EqualityComparisonOperators` and `ComparisonOperators = DefaultWithKeyTypeOverloads`. The sample includes both and `TTRESG105` fires when one is set alone (probe Diagnostics.cs). Restored.
- value-objects-4: `public readonly struct DayMonth` without `partial`. `TTRESG006` error. Corrected.
- value-objects-4: `DayMonth.Create` with `catch (ArgumentOutOfRangeException)`. Brief bans `catch`. Replaced by a range check through `DateTime.DaysInMonth` and a conditional expression (IDE0046 in the lab).
- value-objects-4: `FileUrn.Validate` returns an error for whitespace input. Kept, because the sample's `null` return makes `TryParse("")` return `true` with a `null` item (probe X), which the file does not want to teach.
- value-objects-4: `Jurisdiction` cases with `KeyMemberName = "_isoCode"` and no comparers. Wiki form uses `IsoCode` with both comparer attributes. Used the wiki form, in prose.
- value-objects-5: `modelBuilder.UseThinktectureValueConverters()`. The `ModelBuilder` method is `AddThinktectureValueConverters`, the `DbContextOptionsBuilder` method is `UseThinktectureValueConverters` (decomp:entityframeworkcore10). Corrected.
- value-objects-5: "Complex value objects are normally mapped with EF Core complex properties or owned entity types; the library does not change those mappings". `AddThinktectureValueConverters` exists for `ComplexPropertyBuilder` and `OwnedNavigationBuilder` (decomp:entityframeworkcore10). Refined.
- value-objects-5: "EF Core 7 and later" and "EntityFrameworkCore8" package guidance. The installed package is `EntityFrameworkCore10`, version talk banned. Dropped.
- value-objects-5: Swashbuckle schema detail "`additionalProperties` is `false`". Contradicted: `decomp:swashbuckle` `CopyPropertiesFromKeyTypeSchema` sets `AdditionalPropertiesAllowed = true`. Dropped as false.
- value-objects-5: `async` endpoint filter without `await` and `MaybeBound` code. Host-only, kept as prose.
- value-objects-5: `ValueObjectModelBinderProvider`, `UseValueObjectValueConverter`. Removed in v10 (wiki:Migration-from-v9-to-v10). Not used.
- All artifacts: "should", "could", "may", "normally", "seamlessly", "comprehensive", "robust". Removed per the voice rules.
- wiki:Value-Objects-Framework-Integration: "`RequiredMemberEvaluator` ... `Default`" as the default. The option defaults to `FromDependencyInjection` and the registration adds `DefaultRequiredMemberEvaluator` (decomp:swashbuckle). Stated as such.

## (c) Host-only blocks

The file contains no host-only fence. Every registration identifier appears in prose or in the integration table:

- `options.Converters.Add(new ThinktectureJsonConverterFactory())` — needs `JsonSerializerOptions` from a host, verified decomp:json
- `ThinktectureNewtonsoftJsonConverterFactory` — needs Newtonsoft.Json, verified decomp:newtonsoft.json
- `ThinktectureMessageFormatterResolver.Instance` in a `CompositeResolver` — needs MessagePack, verified decomp:messagepack
- `options.ModelBinderProviders.Insert(0, new ThinktectureModelBinderProvider())` — needs ASP.NET Core MVC, verified decomp:aspnetcore
- `services.AddThinktectureOpenApiFilters()` — needs Swashbuckle, verified decomp:swashbuckle
- `optionsBuilder.UseThinktectureValueConverters()`, `modelBuilder.AddThinktectureValueConverters()`, `Configuration`, max-length strategies — need EF Core, verified decomp:entityframeworkcore10
- `MaybeBound<T, TKey, TValidationError>` — application code from the wiki, needs a web host, kept as prose
- `Destructure.UsingThinktectureRuntimeExtensions()` — compiles in the probe, kept as prose because the file has no Serilog fence

## (d) Open questions

- None of the ledger's probe questions stays open: Q1 checked operators are generated for `int` and `decimal`, Q2 the default instance is a `static readonly` field for keyed and complex structs, Q3 the Swashbuckle option defaults to `FromDependencyInjection` with `DefaultRequiredMemberEvaluator` registered, Q4 the synthesized default is `default` in the generated `Validate` and `= null` on the hook declaration, Q5 `TTRESG049` fires without `DefaultStringComparison`, Q6 the non-void hook compiles and `FactoryPostInit` receives the value.
- `FactoryPostInit` runs on the constructed instance, and the analyzer rejects every mutable field or property (`TTRESG001`, `TTRESG003`) unless it carries `[IgnoreMember]`. The file states that, but the intended use of the return value beyond an ignored field is not documented anywhere.

## Lab rulings

Lab at `.scratch/lab-t02` (gitignored): `dotnet build LabT02.csproj` exit 0, 0 warnings, 0 errors; `dotnet run --no-build` exit 0 with every check passing.

- MA0193: `Math.Round(x, 2)` and `decimal.Round(d, 2)` need a `MidpointRounding` argument. `Boundary` and `MoneyRoundingStrategy.Default` spell `MidpointRounding.ToEven`.
- EPC20: the generated `Switch`/`Map` of a keyless smart enum formats `{this}` in `InvalidOperationException` text, and the default `ToString()` fails the rule. `MoneyRoundingStrategy` gained `public string Name { get; }` (first constructor argument) and `public override string ToString() => Name`. One sentence added to the doc.
- IDE0046: an `if (...) throw/return ...; return ...;` pair becomes a conditional expression. `DayMonth.Create(int, int)` and the second guard of `FileUrn.Validate(string?, ...)` are conditional expressions.
- MA0003: a `null` argument must be named. `BoundaryValidationError.Create` spells `new(message, Lower: null, Upper: null)`.
- MA0011 then MA0185 (scaffolding): `OpenEndDate.ToString()` and `DayMonth.ToString()` needed a provider while the generated `IFormattable` existed, and the generated `IFormattable` formats the key instead of the hand-written override. Both types now set `SkipIFormattable = true`, and the doc gained the sentence about `SkipToString` leaving `IFormattable` in place.
- TTRESG001/TTRESG003 (probe): a field or property that `FactoryPostInit` assigns must carry `[IgnoreMember]`. Stated in the doc.
- Scaffolding-only rulings (not in the doc): MA0003 named `provider: null` on `Validate`, `Parse`, `TryParse` calls, IDE0100/MA0073 `== false`, IDE0034 `default(T)` to `default`, RCS1098 constant on the right, MA0191 no null-forgiving operator (the null-component check moved to the probe).
- Naming: the doc's `Interval` is the probe's `Bounded`; the lab compiles the doc text.

## Merge

Review B items 1-20 applied in full (8 AGREE, 7 AMEND, 5 new, no REJECT). The `MoneyRoundingStrategy` Smart Enum left the file, `Money` takes `MidpointRounding`. The `DayMonth` fence, the null/empty matrix, the generic-constraint table, the `OpenEndDate` convenience factory and hook, and the `FileUrn` hook are cut. `FileUrn` now sets `UseWithEntityFramework = true`.
The reserve cut applied: `Measure<T>` is a bare declaration.

## Shared lab rulings

- `EPC20` (ErrorProne.NET, a repo analyzer) forced a `ToString` override on the keyless Smart Enum that the review then removed from the file. The `Money` extra parameter is `MidpointRounding`.
- `FileUrn.Parse("blob:x:y").Urn` is `x:y`: the constructor splits on the first separator only. The lab checks it.

## Repo form

- One design rule added: `Create` or `Parse` at a boundary admits user input and throws, and the correct form is `[ValidationError<X>]` with an `X : Expected, IValidationError<X>` record plus a hand-written `From` that maps `Validate` to `Fin<T>`.