# [PY_DATA_RULINGS]

`python/data` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `arro3-*` is the slim carrier owning `wire_bytes` — acero, dataset, and flight stay `pyarrow`, and the CDF split needs `sort_by` it lacks.
- `geoarrow-rust-io` splits from GDAL on FORMAT, `pyogrio` keeping the OGR tail and shapefile — in-frame kernels split on what GEOS vectorizes.

## [02]-[SHAPE]

- `graph/graph#TOPOLOGY` indexes the two organization key spaces apart — one merged map lets a foreign key shadow a 32-hex entity address.
- Wire structs mint at `runtime/transport/shapes#VOCABULARY` alone — a decode-side struct here is the second owner that page's law forecloses.
- Corpus raw containers own a READ-ONLY leg on `gridded/field`, never a `FieldEngine` row — the C# producer emits and CF folding forces `phony_dims`.
- `arrow_bytes` at `tabular/interop` is the one whole-table serialization every keyer imports — a second spelling forks one frame into two keys.
- Floor-unreachable manifest rows stay ADMITTED — import-time `find_spec` refuses onto `BoundaryFault(import_=)` and admission records intent alone.
- Floor gates bind every surface naming the gated coordinate, never the dispatch arm — a roster, seam, or constant dies on the import it forecloses.
- Residence names the engine PREFERENCE and reach decides where no caller named one — a named provider below the floor still refuses by module.
- `store_handle` belongs to the residence, never the engine — hardcoding one leaves the sibling sole arm, so a floor marker takes the class down.
- Caller-selected providers ride VERBATIM — reach refuses the cells it cannot serve, so no arm swaps an accelerator in behind the selection.
- Providers owning a store client take one pre-built config VALUE — `store_handle` reaches `obstore` alone, and unnamed axes keep provider defaults.
- Transport-band transforms never reach the identity fold — a codec is a transport parameter, and the gate hands original bytes onward whole.
- Versioned store owners emit the snapshot-identity `ContentKey` alone — branch-merge, retention, and table-protocol state stay `Rasm.Persistence`'s.
- Outbound store legs open `SpanKind.CLIENT`, local legs stay `INTERNAL`, one span per leg — a folded span drops the boundary a trace joins on.
- Capability bounds answer as reach-matrix rows read before every call, hook, and retry — a lifted provider exception reads as transport failure.
- Per-arm measures ride a `(quantity, unit)` pair, file and byte evidence keeping typed slots — one slot spanning rows and files strands consumers.
- Contributors spell `domain`, `kind`, and `key` into facts and `Metrics.record` alike — an omission writes a nameless partition no predicate prunes.
- Non-transactional planes enter as a FORMAT row whose unreached cells refuse — a second writer drifts from the gate, veto, retry, span, and receipt.
- Evidence-plane writes take the receipt-free half answering landed files — a scan-plane receipt there enters the stream as a foreign-domain row.
- Commit receipts report the operation's OWN churn off the commit log — a current-file listing counts the whole table, so one append prices it.
- Every partition and cluster column is TOTAL, filled from producer identity — an empty path segment writes a directory no predicate prunes.
- Clustering and retention ride the MAINTAIN phase on the deploy cadence — an ingest-folded pass takes no partition scope and rewrites the plane.
- Residence commits prove the SUBJECT they write — `kind` rides the opened handle, so a stream admitted on format alone lands in a foreign table.
- Durable-plane rebuild folds COLUMN-WISE at the provider — a row-wise decode allocates an object per row, and one projection serves both readers.
- Residence arming leads the ingest plan as a matrix CELL — an if-not-exists create costs one probe, and a table-less format derives no arming.
- Layout crosses as ONE transform vocabulary each arm projects onto its provider — a transform a format cannot spell refuses by name, never vanishes.
- Profile harvests carry each PROVIDER's raw payload into one decoder — an engine measuring no cpu, blocked, or IO fact reports proven zeros.
- `EngineProfile` widens only where every engine answers the new fact or proves zero — a per-engine field forks the band its decoder exists to hold.
- Consumers compose the owner and read its receipt payload — re-opening the provider beneath it forks a reader and strands every format it arms.
- `ResidenceRow` extends the estate consumption descriptor with SUBJECT columns — `kind`, `table_format`, and `domain` name what the residence holds.
- Residence LAYOUT columns answer where bytes land — `partition_by`, `zorder`, `tuning`, and `layout` reading `None` where a format plants no table.
- Residence POLICY columns answer duration and audience — `tenancy` and `lifetime` name window and ending owner, `lifetime_hours` data, `cap` false.
- `degrade` and `ops` DERIVE off the reach matrix — `degrade` reads refusal cells and `ops` projects onto `LakePhase`, so a format edit costs no row.
- `Ledger` relations declare their spine role at open — `residence=None` discriminates nothing, so a commit owner reads that plane before it records.
- Durable evidence and the receipt fan fold one operation twice — a manifest commit meters referenced BYTES durably and the COUNT on its series.
- Dropping a read-time `validation=` hands the decision to the composing owner — its algebra grades every restore, so no argument drops a grade.

## [03]-[COLLAPSE]

- Overlapping second libraries enter as a bounded fast path under a NAMED split predicate — a fast-path member never owns the concern it accelerates.

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- (none)
