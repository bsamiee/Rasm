# [PY_ARTIFACTS_TASKLOG]

The folder's open and closed work, distilled from `IDEAS.md`. Open tasks are cards in `[1]-[OPEN]` with a `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` leader; closed tasks move to `[2]-[CLOSED]` with `[COMPLETE]`/`[DROPPED]`. Each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[BLOCKED] Widen the imaging code arm to the full encoded-mark family on `imaging/preview.md` — from `ENCODED_MARK_OWNER`.
- BLOCKED on the `python-barcode` catalogue resolving on uv-sync. The segno QR/Micro-QR half is REALIZED: `imaging/preview#PREVIEW` collapses `PreviewOp.QR` into one `PreviewOp.MARK` row carrying a `Symbology` `StrEnum` sub-axis (QR/Micro-QR settled fence code over segno `make`/`make_micro`/`save(kind="svg")`, the `is_qr` discriminant routing `_qr` vs `_barcode`). The linear (1D) `python-barcode` rows (`get_barcode_class`-resolved Code128/Code39/EAN13/EAN8/UPCA/ITF/Codabar/ISBN13/ISSN/PZN/GS1-128 over `SVGWriter`) stay a marked RESEARCH seam in `[3]-[RESEARCH]`. DataMatrix/PDF417 DROPPED (python-barcode is strictly linear). Close-condition: uv-sync resolves `python-barcode` and `ApiPackage.reflect` confirms `get_barcode_class`/`SVGWriter`/`PROVIDED_BARCODES`, settling the `_barcode` body and the linear `Symbology` rows.
- The arm lands on the existing `Preview` owner as one symbology-row axis beside the raster ops, contributing the existing `ArtifactReceipt.Preview` case; the encoded-mark concern is one axis, never a parallel code class. The `SVGWriter` is the ONLY admitted writer (the `ImageWriter` PNG path needs Pillow and re-introduces the leak segno removed).

[BLOCKED] Author the `python-barcode` `.api` catalogue on capture — coverage move from `ENCODED_MARK_OWNER`.
- BLOCKED on the package resolving in the environment: `python-barcode` is present in the lockfile but absent from the active venv (catalogue header `RESEARCH-capture-pending-on-uv-sync`), so the `.api/python-barcode.md` catalogue is AUTHORED-PENDING-VERIFICATION (linear-only, canonical-source-authored) until `assay api` reflection confirms `get_barcode_class`/`get`/`PROVIDED_BARCODES`/`SVGWriter`/`ImageWriter`/the per-symbology classes on uv-sync. README already lists python-barcode (no README edit). Close-condition: uv-sync resolves the package and reflection confirms the canonical-authored surface.
- CENTRAL-MANIFEST: the repo-root `pyproject.toml` python-barcode comment is stale (`linear/2D barcode symbologies` contradicts the verified linear-only catalogue) — a stale-comment correction is owed to the central manifest, recorded for the orchestrator. No admission row to add (the row already exists).

[BLOCKED] Contribute the artifacts measured signals into the one runtime metric stream — trickle-down from branch `ONE_MEASURED_SIGNAL_STREAM`.
- Route every artifacts production duration/byte-volume signal (document emit, chart render, scene render, table build, bundle compression ratio) into the runtime `observability/metrics` `MeterProvider` rather than a local log field, so render-duration histograms and output-byte gauges become first-class observable metrics on the one branch stream.
- Integrate the runtime `observability/metrics` instrument set (consumed, never re-minted) through the `receipt/artifact-receipt#RECEIPT` `contribute` fold; no new artifacts package.
- The signal contribution lands on the `ArtifactReceipt.contribute` path — the receipt is the natural per-production fact carrier — feeding the runtime instruments at composition; this is the artifacts-side consumer of the branch metric stream, never a parallel artifacts metric owner.
- Blocked-aligned on the runtime `observability/metrics` task landing the instrument set; the artifacts contribution is the receipt-fold edge that consumes it. Close-condition: the runtime `MeterProvider` instrument set lands, then `receipt/artifact-receipt#SIGNALS` `contribute` records against it (the consumer edge is already authored).

[BLOCKED] Key the artifacts document output into the content-addressed reuse fabric — trickle-down from branch `CONTENT_ADDRESSED_REUSE_FABRIC`.
- Confirm every artifacts producer admits its `(ContentKey, Work)` pair into the runtime lane-admission elision so an identical document/chart/table/bundle at an identical spec short-circuits to the cached content key, and the `ArtifactReceipt` carries the hit/miss distinction — the artifacts owners already key every output by `ContentIdentity.of`, so the elision is inherited, not re-minted.
- Integrate the runtime `concurrency/lanes` `(ContentKey, Work[T])` admission and `frozendict` session cache (consumed); no new artifacts package.
- The change is verification-only on the artifacts side: every `_emit` already returns `ContentIdentity.of(...)`, so the producers thread the same key into the lane admission the runtime owns; the most expensive artifacts output (a signed archival PDF, a rendered notebook) is the natural elision target.
- Aligned on the runtime lane-admission task; the artifacts producers are pre-keyed consumers, never a second cache owner. Close-condition: the runtime `concurrency/lanes` `(ContentKey, Work)` admission lands, then the producers thread the existing `ContentIdentity.of` key into it (the consumer edge is authored at `receipt/artifact-receipt#SIGNALS`).

[BLOCKED] Hold the outward figure handoff to the one HandoffAxis and clear the drift guard — trickle-down from branch `ONE_GRADUATION_RAIL_OUTWARD` + master `CROSS_PACKAGE_DRIFT_GUARD`.
- Confirm every artifacts figure/table/chart/scene that crosses outward to a sibling package travels only as the `compute/graduation` `HandoffAxis` model-asset case keyed by `ContentIdentity`, never a parallel per-artifact handoff; and confirm the artifacts sources re-mint no canonical concept (content-identity seed, receipt rail, retry owner, wire-projection name) so the runtime `evidence/evidence` `Structural.drift` cross-language query finds zero re-mints in any artifacts module.
- Integrate the `compute/graduation` `HandoffAxis` model-asset case and the runtime `evidence` `Structural.drift` query (both consumed, never re-minted); no new artifacts package.
- The change is alignment-and-verification on the artifacts side: every `_emit` already returns `ContentIdentity.of(...)` and every producer contributes one `ArtifactReceipt` case consuming the runtime ports, so the outward edge is the single `HandoffAxis` model-asset case and the source surface is drift-clean by construction.
- Aligned on the `compute/graduation` HandoffAxis task and the runtime `evidence` drift-detector task; the artifacts contribution is the pre-keyed outward consumer and the drift-clean source, never a second owner. Close-condition: the upstream graduation `HandoffAxis` task and the runtime `Structural.drift` detector land, then the artifacts sources confirm against the live drift query (the `ARCHITECTURE.md` charter lines 25-26 already assert both invariants).

[BLOCKED] Widen the test-tool source scope to the artifacts package source — trickle-down from branch `PYTHON_TEST_INFRA_FLOOR`.
- When the artifacts source transcribes, widen the central `pyproject.toml` test-tool scope by one row: `[tool.coverage.run].source`, `[tool.mutmut].source_paths`, the pytest `testpaths`, and the mypy advisory `files` each gain the artifacts source root, so the package ships under the same 95% coverage gate, mutation kill-ratio, and advisory type pass as the tooling.
- Integrate no new package — the kit (`coverage`/`pytest-cov`, `mutmut`, `pytest` + plugins, `hypothesis`, `inline-snapshot`, `mypy`, `ty`) is already admitted in the branch groups; the manifest-row widening is returned to the orchestrator as a central-`pyproject.toml` edit, never a per-package manifest.
- The widening lands in the one central manifest at the artifacts transcription, never before its source exists; source-scope-only, no capability proposed.
- Aligned on the branch `PYTHON_TEST_INFRA_FLOOR` task; the artifacts row is one entry in the per-package widening sequence. Close-condition: the artifacts `.py` source transcribes (this campaign is design-corpus only — no source exists), then the central `pyproject.toml` `coverage.run.source`/`mutmut.source_paths`/`pytest.testpaths`/`mypy.files` gain the artifacts source root. CENTRAL-MANIFEST edit recorded for the orchestrator.

## [2]-[CLOSED]

[COMPLETE] Host-free chart-export floor on `charts/chart-spec.md` — `_export_host_free` folds vega (`vegafusion` subprocess pre-pass then host-free `vl-convert`), plotly (`kaleido` host-Chrome-gated), and matplotlib (gated-band Agg `savefig`); keys `ArtifactReceipt.Chart`, all spellings `.api`-verified.

[COMPLETE] Offscreen software-GL floor on `scene3d/scene.md` — `_render_scene` builds `pv.Plotter(off_screen=True)`/`add_mesh` and folds the PNG `screenshot` / `export_gltf`/`export_vrml` target on the gated `python_version<'3.13'` floor across `to_process.run_sync`; keys `ArtifactReceipt.Scene`, `.api`-verified.

[COMPLETE] Reproducible notebook report on `reporting/report-plan.md` — `ReportPlan` `NOTEBOOK` runs `papermill.execute_notebook` on the nbclient engine and serializes the executed `NotebookNode` via `nbformat.write(NO_CONVERT)` as the content-keyed audit artifact; keys `ArtifactReceipt.Report`, `.api`-verified.

[COMPLETE] Publication-table owner on `tables/table-plan.md` — `TablePlan` folds `tab_spanner`/`FORMATTERS`/`data_color` over `GT`, emits HTML/LaTeX host-free and PDF host-Chrome-gated; keys `ArtifactReceipt.Table`, `.api`-verified. Re-opened as `[PUBLICATION_TABLE_DEPTH]` for the unexploited surface.

[COMPLETE] PDF conformance and signing on `typography/conformance.md` — `_apply` folds fonttools `Subsetter`/`instancer` and pyhanko `SimpleSigner.load`/`PdfSignatureMetadata(use_pades_lta)`/`sign_pdf` with `HTTPTimeStamper`; keys `ArtifactReceipt.Pdf`, `.api`-verified. Re-opened as `[ARCHIVAL_CONFORMANCE_VERIFICATION]` for the unexploited validation surface.

[COMPLETE] Color-managed visual pipeline on `color-management/colorimetry.md` — `Colorimetry._compute` folds CONVERT/SPECTRAL/APPEARANCE (CIECAM02/CAM16)/PALETTE (Oklab) over the colour-science gateway; carries no receipt case, `.api`-verified. Re-opened as `[ICC_COLOR_MANAGED_OUTPUT]` for the unbuilt ICC/LUT layer.

[COMPLETE] Imaging scientific-transform deepening + QR re-base on `imaging/preview.md` — `_gated_raster` folds pillow + scikit-image on the gated band, `_qr` re-based onto segno removing the qrcode Pillow leak (qrcode dropped from manifest); keys `ArtifactReceipt.Preview`, `.api`-verified. Widened by `[ENCODED_MARK_OWNER]` to the full symbology family.

[COMPLETE] RECEIPT-FAMILY-AMEND — `receipt/artifact-receipt#RECEIPT` owns the three new `ArtifactReceipt` cases landed once on the 9-case union: `Introspection` (recovered-tree-shape facts), `Egress` (post-finish byte/page + encryption-R/outline-depth/overlay facts), and `Verdict` (the `ConformanceVerdict` value), each with one static constructor + one `_facts` match arm, so the three downstream pages contribute settled cases rather than three conflicting same-file edits.

[COMPLETE] DOCNODE-SEAM — `documents/model#NODE` owns the `DocumentNode` msgspec tagged-union tree + `DocumentDelta` diff/merge defined once, `documents/inspection/lens#LENS` owns the `LensOp` recover-TO half, and `documents/document-plan#DOCUMENT` is rebased so every `BACKENDS` arm is a lowering fold FROM the tree (Emitter signature `Callable[[DocumentPlan, DocumentNode], bytes]`, the opaque `dict[str,object]` payload deleted); keys `ArtifactReceipt.Introspection`, `.api`-verified.

[COMPLETE] EGRESS-SUBDOMAIN — `documents/egress/finish#FINISH` owns the `DocumentEgress` + `EgressStep` (ENCRYPT/OUTLINE/WATERMARK/ATTACH/IMPOSE) policy-table over pikepdf/pypdf/weasyprint with the `Permissions` row-policy ValueObject, finishing an emitted PDF (re-signing routes to `typography/conformance#CONFORM`); keys the new `ArtifactReceipt.Egress` case, `.api`-verified.

[COMPLETE] SHAPE-ARM — `typography/conformance#CONFORM` `SHAPE` `ConformStep` row owns the uharfbuzz `Blob`->`Face`->`Font`->`Buffer`->`shape` pipeline returning the `PositionedGlyphRun` ValueObject, with `set_variations` axis-pinning and the `draw_glyph_with_pen`->`SVGPathPen` outline-bridge sub-rows; the uharfbuzz `SubsetInput`/`subset` duplicate is rejected (fonttools owns subsetting); `.api`-verified on cp315-core uharfbuzz.

[COMPLETE] TYPST-INTROSPECTION — `documents/document-plan#DOCUMENT` `TYPST_QUERY`/`TYPST_DATA` `DocumentMode` rows own the held `typst.Compiler` font-cached world, `Compiler.query(selector, field, one)` structured extraction (the introspection inverse), and `sys_inputs` data injection amortized across a batch; `.api`-verified.

[COMPLETE] CONFORM-AUDIT — `typography/conformance#CONFORM` `AUDIT` `ConformStep` row owns the pyhanko `validate_pdf_signature`/`evaluate_signature_coverage` + pikepdf `open_metadata` fold into the `ConformanceVerdict` ValueObject on the synchronous `boundary` (sync `validate_pdf_signature` keeps the owner off the async path); keys the new `ArtifactReceipt.Verdict` case, `.api`-verified.

[COMPLETE] ICC-MANAGED-OUTPUT — `color-management/colorimetry#COLOR` `MANAGED` `ColorOp` row owns the colour-science `read_LUT`/`LUTSequence`/`cctf_encoding`/`cctf_decoding`/`delta_E` tone-curve chain (settled, core) + the `RenderingIntent` `IntEnum` and pillow `ImageCms` raster apply on the gated subprocess seam (forcing the async owner); `buildTransform`/`applyTransform`/`INTENT_*` held `[3]-[RESEARCH]` until pillow `ImageCms` reflection. colour-science spellings `.api`-verified.

[COMPLETE] FIGURE-SUBDOMAIN — `imaging/figure/compose#COMPOSE` owns the `Figure` + `FigureOp` (SCALE_FIT/TILE/CROP over svgelements on the core, ANNOTATE/METADATA over pillow on the gated band) composition algebra; keys the existing `ArtifactReceipt.Preview` case, `.api`-verified against the real svgelements 1.9.6 capture + the pillow catalogue.

[COMPLETE] SVGELEMENTS-API-CATALOGUE — `artifacts/.api/svgelements.md` is the verified real reflection (`1.9.6` on cp315-core, pure-Python `py3-none-any`); `SVG.parse`/`elements`, `Path`/`d`/`bbox`, `Matrix.scale`/`translate`/`rotate`, `Length`/`Color`/`Angle`/`Point`, and the shape primitives confirmed present; svgelements registered in the manifest (pyproject line 170) and README `[2]-[PACKAGES]` (line 33) — pure verify, no admission to land.

[COMPLETE] TABLE-PUB-SURFACE — `tables/table-plan#TABLE` folds the great-tables publication surface (`STRUCTURE` header/footnote/source-note/stub, summary-row algebra, `COLUMNS` control family, `STYLERS` `loc`-targeted cell styling, `fmt_image`/`fmt_nanoplot`) as data-driven spec-row catalogues into the one `reduce`-over-spec build; keys `ArtifactReceipt.Table`, `.api`-verified.

[COMPLETE] TABLE-THEME-MERGE — `tables/table-plan#TABLE` adds the `tab_options(**theme)` global-theme map (the publication identity), `cols_merge_range`, and `data_color(autocolor_text=True)` into the same `reduce`-over-spec pipeline; `.api`-verified.
