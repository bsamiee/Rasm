id: no-succeed-in-flat-map
snapshots:
  import * as O from "effect/Option"; O.flatMap(work, x => O.some(x));:
    fixed: import * as O from "effect/Option"; O.map(work, x => x);
    labels:
    - source: O.flatMap(work, x => O.some(x))
      style: primary
      start: 36
      end: 67
  import { Channel } from "effect"; const next = Channel.flatMap(channel, (value) => Channel.succeed(value + 1));:
    fixed: import { Channel } from "effect"; const next = Channel.map(channel, (value) => value + 1);
    labels:
    - source: Channel.flatMap(channel, (value) => Channel.succeed(value + 1))
      style: primary
      start: 47
      end: 110
  import { Effect as E } from "effect"; E.flatMap(work, x => E.succeed(x));:
    fixed: import { Effect as E } from "effect"; E.map(work, x => x);
    labels:
    - source: E.flatMap(work, x => E.succeed(x))
      style: primary
      start: 38
      end: 72
  import { Effect } from "effect"; Effect.flatMap(work, x => { /* explain */ return Effect.succeed(x); });:
    fixed: import { Effect } from "effect"; Effect.map(work, x => { /* explain */ return x; });
    labels:
    - source: Effect.flatMap(work, x => { /* explain */ return Effect.succeed(x); })
      style: primary
      start: 33
      end: 103
  import { Effect } from "effect"; const next = Effect.andThen(work, () => { return Effect.void; });:
    fixed: import { Effect } from "effect"; const next = Effect.asVoid(work);
    labels:
    - source: Effect.andThen(work, () => { return Effect.void; })
      style: primary
      start: 46
      end: 97
  import { Effect } from "effect"; const next = Effect.andThen(work, (value) => Effect.succeed(value + 1));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value + 1);
    labels:
    - source: Effect.andThen(work, (value) => Effect.succeed(value + 1))
      style: primary
      start: 46
      end: 104
  import { Effect } from "effect"; const next = Effect.andThen(work, Effect.succeed(1));:
    fixed: import { Effect } from "effect"; const next = Effect.as(work, 1);
    labels:
    - source: Effect.andThen(work, Effect.succeed(1))
      style: primary
      start: 46
      end: 85
  import { Effect } from "effect"; const next = Effect.andThen(work, Effect.succeed({ value }));:
    fixed: import { Effect } from "effect"; const next = Effect.as(work, ({ value }));
    labels:
    - source: Effect.andThen(work, Effect.succeed({ value }))
      style: primary
      start: 46
      end: 93
  import { Effect } from "effect"; const next = Effect.andThen<number>(work, Effect.succeed(1));:
    fixed: import { Effect } from "effect"; const next = Effect.as(work, 1);
    labels:
    - source: Effect.andThen<number>(work, Effect.succeed(1))
      style: primary
      start: 46
      end: 93
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.succeed(1));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => 1);
    labels:
    - source: Effect.flatMap(work, () => Effect.succeed(1))
      style: primary
      start: 46
      end: 91
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.succeed(Effect.succeed(1)));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => Effect.succeed(1));
    labels:
    - source: Effect.flatMap(work, () => Effect.succeed(Effect.succeed(1)))
      style: primary
      start: 46
      end: 107
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.succeed(build()));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => build());
    labels:
    - source: Effect.flatMap(work, () => Effect.succeed(build()))
      style: primary
      start: 46
      end: 97
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.succeed(state.value));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => state.value);
    labels:
    - source: Effect.flatMap(work, () => Effect.succeed(state.value))
      style: primary
      start: 46
      end: 101
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.succeed({}));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => ({}));
    labels:
    - source: Effect.flatMap(work, () => Effect.succeed({}))
      style: primary
      start: 46
      end: 92
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => Effect.void);:
    fixed: import { Effect } from "effect"; const next = Effect.asVoid(work);
    labels:
    - source: Effect.flatMap(work, () => Effect.void)
      style: primary
      start: 46
      end: 85
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => { /* keep */ return Effect.void; });:
    fixed: import { Effect } from "effect"; const next = Effect.flatMap(work, () => { /* keep */ return Effect.void; });
    labels:
    - source: Effect.flatMap(work, () => { /* keep */ return Effect.void; })
      style: primary
      start: 46
      end: 108
  import { Effect } from "effect"; const next = Effect.flatMap(work, () => { return Effect.succeed(1); });:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, () => { return 1; });
    labels:
    - source: Effect.flatMap(work, () => { return Effect.succeed(1); })
      style: primary
      start: 46
      end: 103
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => (Effect.succeed(value + 1)));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => (value + 1));
    labels:
    - source: Effect.flatMap(work, (value) => (Effect.succeed(value + 1)))
      style: primary
      start: 46
      end: 106
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.succeed(value + 1));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value + 1);
    labels:
    - source: Effect.flatMap(work, (value) => Effect.succeed(value + 1))
      style: primary
      start: 46
      end: 104
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.succeed(value.map((x) => x + 1)));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value.map((x) => x + 1));
    labels:
    - source: Effect.flatMap(work, (value) => Effect.succeed(value.map((x) => x + 1)))
      style: primary
      start: 46
      end: 118
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.succeed({ value }));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => ({ value }));
    labels:
    - source: Effect.flatMap(work, (value) => Effect.succeed({ value }))
      style: primary
      start: 46
      end: 104
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.succeed<Item>({ value }));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => ({ value }));
    labels:
    - source: Effect.flatMap(work, (value) => Effect.succeed<Item>({ value }))
      style: primary
      start: 46
      end: 110
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.succeed<number>(value + 1));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value + 1);
    labels:
    - source: Effect.flatMap(work, (value) => Effect.succeed<number>(value + 1))
      style: primary
      start: 46
      end: 112
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => Effect.void);:
    fixed: import { Effect } from "effect"; const next = Effect.asVoid(work);
    labels:
    - source: Effect.flatMap(work, (value) => Effect.void)
      style: primary
      start: 46
      end: 90
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => { return Effect.succeed(value + 1); });:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => { return value + 1; });
    labels:
    - source: Effect.flatMap(work, (value) => { return Effect.succeed(value + 1); })
      style: primary
      start: 46
      end: 116
  import { Effect } from "effect"; const next = Effect.flatMap(work, (value) => { return Effect.succeed<number>(value); });:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => { return value; });
    labels:
    - source: Effect.flatMap(work, (value) => { return Effect.succeed<number>(value); })
      style: primary
      start: 46
      end: 120
  'import { Effect } from "effect"; const next = Effect.flatMap(work, (value): Effect.Effect<number> => Effect.succeed(value + 1));':
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value + 1);
    labels:
    - source: 'Effect.flatMap(work, (value): Effect.Effect<number> => Effect.succeed(value + 1))'
      style: primary
      start: 46
      end: 127
  'import { Effect } from "effect"; const next = Effect.flatMap(work, (value: number) => Effect.succeed(value + 1));':
    fixed: 'import { Effect } from "effect"; const next = Effect.map(work, (value: number) => value + 1);'
    labels:
    - source: 'Effect.flatMap(work, (value: number) => Effect.succeed(value + 1))'
      style: primary
      start: 46
      end: 112
  import { Effect } from "effect"; const next = Effect.flatMap(work, function () { return Effect.succeed(1); });:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, function () { return 1; });
    labels:
    - source: Effect.flatMap(work, function () { return Effect.succeed(1); })
      style: primary
      start: 46
      end: 109
  import { Effect } from "effect"; const next = Effect.flatMap(work, function (value) { return Effect.succeed(value + 1); });:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, function (value) { return value + 1; });
    labels:
    - source: Effect.flatMap(work, function (value) { return Effect.succeed(value + 1); })
      style: primary
      start: 46
      end: 122
  'import { Effect } from "effect"; const next = Effect.flatMap(work, function (value): Result { return Effect.succeed(value); });':
    fixed: import { Effect } from "effect"; const next = Effect.map(work, function (value) { return value; });
    labels:
    - source: 'Effect.flatMap(work, function (value): Result { return Effect.succeed(value); })'
      style: primary
      start: 46
      end: 126
  import { Effect } from "effect"; const next = Effect.flatMap(work, value => Effect.succeed(value));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, value => value);
    labels:
    - source: Effect.flatMap(work, value => Effect.succeed(value))
      style: primary
      start: 46
      end: 98
  import { Effect } from "effect"; const next = Effect.flatMap<number>(work, () => Effect.void);:
    fixed: import { Effect } from "effect"; const next = Effect.asVoid(work);
    labels:
    - source: Effect.flatMap<number>(work, () => Effect.void)
      style: primary
      start: 46
      end: 93
  import { Effect } from "effect"; const next = Effect.flatMap<number>(work, (value) => Effect.succeed(value + 1));:
    fixed: import { Effect } from "effect"; const next = Effect.map(work, (value) => value + 1);
    labels:
    - source: Effect.flatMap<number>(work, (value) => Effect.succeed(value + 1))
      style: primary
      start: 46
      end: 112
  import { Effect } from "effect"; const next = pipe(work, Effect.flatMap((value) => Effect.succeed(value + 1)));:
    fixed: import { Effect } from "effect"; const next = pipe(work, Effect.map((value) => value + 1));
    labels:
    - source: Effect.flatMap((value) => Effect.succeed(value + 1))
      style: primary
      start: 57
      end: 109
  import { Effect } from "effect"; const next = work.pipe(Effect.andThen(Effect.succeed(current)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.as(current));
    labels:
    - source: Effect.andThen(Effect.succeed(current))
      style: primary
      start: 56
      end: 95
  import { Effect } from "effect"; const next = work.pipe(Effect.andThen<number>(Effect.succeed(1)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.as(1));
    labels:
    - source: Effect.andThen<number>(Effect.succeed(1))
      style: primary
      start: 56
      end: 97
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => Effect.succeed(1)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map(() => 1));
    labels:
    - source: Effect.flatMap(() => Effect.succeed(1))
      style: primary
      start: 56
      end: 95
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => Effect.succeed(current)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map(() => current));
    labels:
    - source: Effect.flatMap(() => Effect.succeed(current))
      style: primary
      start: 56
      end: 101
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => Effect.void));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.asVoid);
    labels:
    - source: Effect.flatMap(() => Effect.void)
      style: primary
      start: 56
      end: 89
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => { /* keep */ return Effect.void; }));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => { /* keep */ return Effect.void; }));
    labels:
    - source: Effect.flatMap(() => { /* keep */ return Effect.void; })
      style: primary
      start: 56
      end: 112
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => { return Effect.succeed(1); }));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map(() => { return 1; }));
    labels:
    - source: Effect.flatMap(() => { return Effect.succeed(1); })
      style: primary
      start: 56
      end: 107
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(() => { return Effect.void; }));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.asVoid);
    labels:
    - source: Effect.flatMap(() => { return Effect.void; })
      style: primary
      start: 56
      end: 101
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap((value) => (Effect.succeed({ value }))));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map((value) => (({ value }))));
    labels:
    - source: Effect.flatMap((value) => (Effect.succeed({ value })))
      style: primary
      start: 56
      end: 110
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap((value) => Effect.succeed(value + 1)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map((value) => value + 1));
    labels:
    - source: Effect.flatMap((value) => Effect.succeed(value + 1))
      style: primary
      start: 56
      end: 108
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap(function () { return Effect.succeed(1); }));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map(function () { return 1; }));
    labels:
    - source: Effect.flatMap(function () { return Effect.succeed(1); })
      style: primary
      start: 56
      end: 113
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap<number>(() => Effect.void));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.asVoid);
    labels:
    - source: Effect.flatMap<number>(() => Effect.void)
      style: primary
      start: 56
      end: 97
  import { Effect } from "effect"; const next = work.pipe(Effect.flatMap<number>((value) => Effect.succeed(value + 1)));:
    fixed: import { Effect } from "effect"; const next = work.pipe(Effect.map((value) => value + 1));
    labels:
    - source: Effect.flatMap<number>((value) => Effect.succeed(value + 1))
      style: primary
      start: 56
      end: 116
  import { Either } from "effect"; const next = Either.flatMap(decoded, () => Either.right(1));:
    fixed: import { Either } from "effect"; const next = Either.map(decoded, () => 1);
    labels:
    - source: Either.flatMap(decoded, () => Either.right(1))
      style: primary
      start: 46
      end: 92
  import { Either } from "effect"; const next = decoded.pipe(Either.flatMap(() => Either.right(1)));:
    fixed: import { Either } from "effect"; const next = decoded.pipe(Either.map(() => 1));
    labels:
    - source: Either.flatMap(() => Either.right(1))
      style: primary
      start: 59
      end: 96
  import { Exit } from "effect"; const next = Exit.flatMap(exit, (value) => Exit.succeed(value + 1));:
    fixed: import { Exit } from "effect"; const next = Exit.map(exit, (value) => value + 1);
    labels:
    - source: Exit.flatMap(exit, (value) => Exit.succeed(value + 1))
      style: primary
      start: 44
      end: 98
  import { Exit } from "effect"; const next = exit.pipe(Exit.flatMap((value) => Exit.succeed(value + 1)));:
    fixed: import { Exit } from "effect"; const next = exit.pipe(Exit.map((value) => value + 1));
    labels:
    - source: Exit.flatMap((value) => Exit.succeed(value + 1))
      style: primary
      start: 54
      end: 102
  import { Micro } from "effect"; const next = Micro.flatMap(work, (value) => Micro.succeed(value + 1));:
    fixed: import { Micro } from "effect"; const next = Micro.map(work, (value) => value + 1);
    labels:
    - source: Micro.flatMap(work, (value) => Micro.succeed(value + 1))
      style: primary
      start: 45
      end: 101
  import { Option } from "effect"; const next = Option.andThen(found, () => Option.some(1));:
    fixed: import { Option } from "effect"; const next = Option.map(found, () => 1);
    labels:
    - source: Option.andThen(found, () => Option.some(1))
      style: primary
      start: 46
      end: 89
  import { Option } from "effect"; const next = Option.andThen(found, Option.some(1));:
    fixed: import { Option } from "effect"; const next = Option.as(found, 1);
    labels:
    - source: Option.andThen(found, Option.some(1))
      style: primary
      start: 46
      end: 83
  import { Option } from "effect"; const next = Option.flatMap(found, () => Option.void);:
    fixed: import { Option } from "effect"; const next = Option.asVoid(found);
    labels:
    - source: Option.flatMap(found, () => Option.void)
      style: primary
      start: 46
      end: 86
  import { Option } from "effect"; const next = Option.flatMap(found, (value) => Option.some(value + 1));:
    fixed: import { Option } from "effect"; const next = Option.map(found, (value) => value + 1);
    labels:
    - source: Option.flatMap(found, (value) => Option.some(value + 1))
      style: primary
      start: 46
      end: 102
  import { Option } from "effect"; const next = Option.flatMap(found, (value) => Option.some<number>(value + 1));:
    fixed: import { Option } from "effect"; const next = Option.map(found, (value) => value + 1);
    labels:
    - source: Option.flatMap(found, (value) => Option.some<number>(value + 1))
      style: primary
      start: 46
      end: 110
  import { STM } from "effect"; const next = STM.flatMap(transaction, (_value) => STM.void);:
    fixed: import { STM } from "effect"; const next = STM.asVoid(transaction);
    labels:
    - source: STM.flatMap(transaction, (_value) => STM.void)
      style: primary
      start: 43
      end: 89
  import { STM } from "effect"; const next = STM.flatMap(transaction, (value) => STM.succeed(value + 1));:
    fixed: import { STM } from "effect"; const next = STM.map(transaction, (value) => value + 1);
    labels:
    - source: STM.flatMap(transaction, (value) => STM.succeed(value + 1))
      style: primary
      start: 43
      end: 102
  import { STM } from "effect"; const next = STM.flatMap(transaction, (value) => { return STM.succeed(value + 1); });:
    fixed: import { STM } from "effect"; const next = STM.map(transaction, (value) => { return value + 1; });
    labels:
    - source: STM.flatMap(transaction, (value) => { return STM.succeed(value + 1); })
      style: primary
      start: 43
      end: 114
  import { Sink } from "effect"; const next = Sink.flatMap(sink, (value) => Sink.succeed(value + 1));:
    fixed: import { Sink } from "effect"; const next = Sink.map(sink, (value) => value + 1);
    labels:
    - source: Sink.flatMap(sink, (value) => Sink.succeed(value + 1))
      style: primary
      start: 44
      end: 98
  import { Stream } from "effect"; const next = Stream.flatMap(stream, (value) => Stream.succeed(value + 1));:
    fixed: import { Stream } from "effect"; const next = Stream.map(stream, (value) => value + 1);
    labels:
    - source: Stream.flatMap(stream, (value) => Stream.succeed(value + 1))
      style: primary
      start: 46
      end: 106
