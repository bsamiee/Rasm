---
title: Rasm Strongest-Form Adversary
input: full_diff
effort: high
conclusion: neutral
include:
  - "libs/**"
---

# [STRONGEST_FORM]

One maximum-depth adversarial lens the broad correctness sweep cannot afford: every changed code fence is naive, shallow, or illusory until it survives attack, and the burden of proof is on the fence. Where the correctness review matches lines against a defect checklist, this pass runs the `libs/.planning/campaign-method.md` red-team counterfactual as a standing gate — it derives and names the stronger shape, never merely flags a conformance gap. Judge every fence against the owning `docs/stacks/<language>/` doctrine as the floor, then push past it; a fence that merely conforms is unfinished wherever a stronger form exists.

Hold each changed owner as its own target and hunt these classes, every finding naming file, anchor, the naive shape, and the concrete stronger form — the owner kind, the collapse, or the admitted-package operator — precise enough to transcribe:
- Counterfactual collapse: replace flat models, arm ladders, parallel types, and per-instance bodies with the generated owner, table, discriminant, or generator that spans them. One survivor must consume the same state and rail while preserving behavior, precedence, evidence, and contract mirrors; distinct stages, domain walks absent from package traversal, ABI spellings, and payloads with different effects remain separate.
- Coverage naivety at tenfold demand: name missing axes as rows, cases, or members on the existing owner. Pressure grounded bounds, integrality, allocation capacity, calendar resolution, numerical scaling, identity framing, and case-owned projection data; zero consumers never lowers the bar, while a claim with no charter or domain proof emits nothing.
- Approach naivety on both parameterization directions: a fixed roster of styles, patterns, or variants hardcoded where it is seed data feeding one algebraic owner over named parameters; a literal that encodes a decision or recurs across sites where a policy value or vocabulary row owns it, and a stringly key, path, or per-site spelling of one correspondence that the owning symbol derives; inversely, a knob set whose combinations the body re-derives, collapsing into policy rows, table dispatch, or a denser polymorphic owner with defaults derived once.
- Consumer overhead and entry-surface quality: orchestration, policy resolution, routing, retry, telemetry, or lifecycle pushed onto callers instead of internalized behind the entrypoint; judge each touched module's public roster as a designed artifact — verb siblings, arity twins, mode flags, and exports grown per capability are findings, a helper with exactly one caller, no plausible second consumer, and no meaning in isolation inlines into that caller, and the roster narrows by absorption, never omission.
- Diff-of-the-next-feature: an owner not shaped for the family it absorbs next — the next case, dimension, or modality landing as a new surface rather than one declaration inside the owner with every consumer untouched or loudly broken.

Settled corpus law outranks intuition: a shape the owning page or `.api/` catalogue rules is never a finding. A pass that finds nothing after genuine attack states that verdict plainly and never manufactures a finding to fill the space.
