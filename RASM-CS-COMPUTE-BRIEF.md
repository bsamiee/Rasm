# [RASM_CS_COMPUTE_BRIEF]

[DEPENDENCY_POSTURE]: Compute depends up on the LANDED kernel (the one `libs/csharp/Rasm/.planning/` root) and the `Rasm.Element` seam, reads the surviving `Rasm.Persistence` owners by reference, and is read the same way by AppHost, AppUi, and Fabrication — foundations before consumers, always.

The authoritative ruling for the restructure-and-harden of `libs/csharp/Rasm.Compute/.planning/` — the APP-PLATFORM measured-execution engine (41 pages, 7 folders: Analysis · Model · Runtime · Solver · Stats · Symbolic · Tensor). Upstream law honored throughout: the LANDED kernel — the whole `Rasm` corpus lives under one `libs/csharp/Rasm/.planning/` root (fence namespaces frozen per the kernel `ARCHITECTURE.md` [03] matrix), and the kernel ledger declares the kernel->Compute seams as settled law (`Rasm/ARCHITECTURE.md:89,93-94,97,100,103,106,110` — the `ContentHash.Of` identity entry, the `DiscreteCalculus` adjoint bundle + `MeshAdjointSnapshot` handle, the `Spatial.Apply` Wire emit, the `AtElevations` `SliceStack` story-contour wire, the isotropic-remesh pre-step, the discretization predicate export, `EncodedGeometry`-as-`EncodedTensor`; Compute decodes the kernel acceleration/encoding wires, never re-implements kernel geometry), `.archive/RASM-CS-PERSISTENCE-BRIEF.md` (Compute reads the surviving cache/retrieval owners by reference; the deleted-owner seam rows re-point here), and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (Compute consumes `graph.SectionOf` -> `ComputedSection`/seam `SectionProperties` with ZERO structural ripple, `:72`). `docs/stacks/csharp/` governs.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is the inverse of its siblings: world-class INTERIORS under a stale, blind PERIMETER — and the world-class claim still dies on six proofs, each independently fatal:

1. The assessment rail is blind to its own seam's lifecycle. `Rasm.Element` ships a complete 8-row `AssessmentOutcome` lattice with `Dispatchable`/`Coherent`/`Next` columns, `Diagnostic.Of(phase, kind, message, key)`, `FailureKind.Transient`, and `AssessmentPayload.Failed` (`Rasm.Element/.planning/Assessment/assessment.md:70,104,122,158`), whose comments name "the Rasm.Compute sweep" and "a Diagnostic.Kind.Transient policy in Compute" as the consumer (`:59-60`). `Analysis/assessment.md` consumes only `Computed`/`Pending`: CacheFirst short-circuits a `nodeId`-gated `graph.Find<Node.Assessment>` hit to the noop regardless of the cached `Outcome` (`assessment.md:254-258` — a cached Failed/Stale/in-flight node reads as satisfied), the failure path writes NO `Failed` node (`:260-261` — `Run(...).Bind(WriteBack)` aborts, so a deterministic failure re-runs on every call and nothing caches), no `Outcome.Dispatchable` re-solve sweep exists, no `Transient` retry gate exists, and the `AssessmentWire` receipt carries no failure column (`Runtime/receipts.md:416`). The largest unwired seam in the corpus, and it is load-bearing.
2. All four runners fail stringly, and the wire cannot carry their faults. `StructuralAnalysis`/`BuildingPhysics`/`EnergySimulation`/`LifecycleAssessment` share the `Fin<AssessmentResult>` contract and all abort onto opaque interpolated `ComputeFault.AnalysisRunFailed` (`structural.md:302` `<bfe-solve-break:...>`, `:406` `<fealite-singular:...>`; `energy.md` SIX stringly arms — subprocess `:336,:362,:388` plus the cloud route `:613,:630,:638`) — no typed `SolvePhase`/`FailureKind` exists Compute-side, so the seam `Diagnostic` has nothing to compose. Meanwhile `Runtime/channels.md:9` projects a "seventeen-code taxonomy" over a fifteen-row rail while the `ComputeFault` band runs 2200-2220 — the analysis lane (2217-2219) and scheduling lane (2220) CANNOT cross the one `FaultDetail` family, violating the package's own `ARCHITECTURE.md` [05]#3 law.
3. The seam ledger points at dead siblings. `ARCHITECTURE.md:113` and `:121` target `Rasm.Persistence/Sync` (deleted by the Persistence campaign), `:117` targets `Query/pipeline` (deleted phantom), `:118` targets `Store/quality` (deleted; `clash.md:18` disclaims any producer — DROP), `:101` targets `Query/lane` (its `VECTOR_CODEBOOK` extracts to `Query/retrieval`), and the once-declared reverse clause "Processing/solver consumes the Compute LM/constraint Solver rail" is ALREADY ABSENT from the kernel ledger — verified dead (the kernel LM is `Solving/solver.md`'s own λ-ladder over the landed `Numerics/matrix.md` owners). The page bodies are FRESHER than the ledger (`channels.md:92` already cites the surviving `Version/ledger` `CHANGEFEED`) — governance decoupled from the realized corpus.
4. Two confirmed interior defects ship inside 9-grade solver fences. (a) The linear-elastic path hardcodes Poisson nu=1/3: `Elasticity(scale)` sets `lambda = scale, mu = 0.5 * scale` (`Solver/contract.md:65-66`) and `SolveProblem` carries one `materialScale` scalar with NO seam elasticity read (`:297-304`; the real seam member is `graph.PropertiesOf(member).Mechanical` — the ledger's `MechanicalOf` spelling is a phantom member the `[V6]` repair corrects alongside `ARCHITECTURE.md:126`) — the declared elastic-D seam is honored ONLY on the nonlinear `ElasticPrimal` path (`contract.md:1038`, correct 2-parameter form), so every fea-static/modal/buckling/transient solve runs a wrong constitutive tensor while the seam sits unwired. (b) `SolveProblem.ContentKey` hashes a `CultureInfo.InvariantCulture`-formatted interpolated string through a per-call-site `XxHash128` (`:304`) — the exact form the Model/identity seam forbids (`ARCHITECTURE.md:102`) and the corpus's own canonical-bytes keys (`factor.md` `ShardPlan.Digest`, `expression.md` `ContentKey`) prove wrong.
5. The volumetric mesher re-implements kernel geometry with float predicates. `DelaunayCore` hand-rolls Bowyer-Watson over non-robust float `Orient`/`InSphere`/`Det4` (`Solver/discretization.md:585,617,913-921,962`) — the exact adaptive-exact-predicate concern the kernel crowns and `libs/.planning/architecture.md` forbids consumers to re-own; near-coplanar/cocircular configurations (the COMMON case in building geometry) flip signs and emit non-Delaunay/inverted cells the FE solve then trusts. No discretization->Geometry predicate seam is declared. The page also mints a local `record MeshSpace` (`:373`) name-colliding with the kernel Vectors `MeshSpace` its own prose composes (`:3,18`; the frozen-name owner is the landed `Rasm/.planning/Meshing/mesh.md`).
6. The roster and the catalogs described different systems, and the domain roster is a thin slice. The perimeter half is partly settled on disk: `FParsec`'s direct admission is REMOVED (the central pin `PROPS:109` survives only as the `MathNet.Symbolics` transitive floor and dies with `[V11]`); `Microsoft.ML.OnnxRuntime.DirectML`/`.Gpu` are out of the csproj with their catalogs, though their orphan central pins (`PROPS:117,119`) still leave (re-admission requires the deployment that demands the `ExecutionProvider` rows — `providers.md:12` runs Cpu+CoreMl only, and the base package ships ZERO osx-arm64 accelerated payload, `api-onnxruntime.md:122-131`); `SharpGLTF.Ext.3DTiles` (`csproj:74`) is ruled Compute's with its `Tiles3D` surface catalogued at depth (`api-sharpgltf.md:47`); `LanguageExt.Core` — the ROP rail of all 41 pages — carries its substrate catalog (`libs/csharp/.api/api-languageext.md`; this ruling VERIFIES, never re-authors); the nine once-md5-identical cross-tier copies are COLLAPSED to substrate-canonical bodies with three-line domain pointers. Still open: EIGHT domain-tier catalogs fork their substrate base body (csparse, hashing, hybrid-cache, mathnet-providers, nodatime, tensors, thinktecture-json, unitsnet — md5-confirmed forks, not overlay deltas). And on COVERAGE: the Element seam declares FIFTEEN `Discipline` rows (`Rasm.Element/.planning/Classification/classification.md:170-185`) — Compute runs SEVEN; `Daylight` is a seam row with no runner, circulation/egress (life-safety, code-mandated) has no row anywhere, `daylight-radiosity` is one FEM row not a discipline (`contract.md:35`), zero solar-position computation exists corpus-wide, the only eigensolver is dense EVD (self-flagged: "never a sparse LOBPCG/Lanczos it lacks", `contract.md:733,104`) capping modal/seismic/buckling at toy DOF, and the corpus admits Jacobians everywhere while hand-rolling finite differences ("the autodiff-tape-absent fall", `Stats/estimator.md:574,582`).

What IS sound (preserve; harden ONLY per the verdict that names it — treating these as greenfield teardown would destroy award-grade interiors): the 25-case `ComputeReceipt` union and fold-projection family, the `ComputeFault` 2200-band custody, the `JobGraph` incremental engine (`scheduling.md:183-198` — `JobState` Faulted/Spilled/Resumable, content-key `Reconcile`, `CheckpointPort` spill/resume: exactly the substrate the V1 sweep composes), the 11-kernel `Optimizer` with genuinely distinct CMA-ES/PSO/SA/Bayesian-GP, the full Wiener-Askey UQ with HLRF/Breitung/Au-Beck, the Joe-Kuo `LowDiscrepancy` owner, the 196-row `TensorOpFamily` and dual-mode AD `Sensitivity`, the OrtValue-only Model plane, the EN/ISO closed-form physics kernel, the EnergyPlus subprocess discipline, the EC3 basis-tagged `Normalize`, the AISI-aware capacity table's design-code delegates, `GeoArrowBuffer.ToImportedGeometry` (the GLB wire layout the python:data GeoArrow buffers share — `Runtime/codecs.md:59`, `libs/python/data/ARCHITECTURE.md:54` — RECORDED, never re-mint), and the zero-stale-paradigm state (grep `ComponentSection` is empty; every Analysis page consumes the post-DECISION `graph.SectionOf` twenty-column seam).

### [TELOS]

`Rasm.Compute` is the federation's measured-execution engine: the discipline-assessment authority over the concrete `ElementGraph` (every seam-declared discipline answerable, every verdict cached by content key, every failure a typed cached fact with a retry policy — a farm of solvers whose failure semantics are as engineered as their success semantics), the numeric substrate (dense/sparse/factored/spectral/quadrature/sampling at building scale — 10^4-10^6 DOF modal and buckling, exact-predicate meshing, exact AD gradients), the optimization/UQ/DOE spine over one `evaluate` oracle, the symbolic lane with real CAS depth, the ONNX model plane, and the admit-to-receipt runtime boundary whose wire carries every fault family. Every owner grades against the 5x consumer bar: the assessment rail feeds generated-design screening loops that re-solve thousands of variants (failure caching and `Dispatchable` sweeps are throughput law, not hygiene), the numeric spine feeds Generation form-finding and Fabrication verification, the wire feeds remote farms. A discipline the seam declares and Compute cannot answer is a coverage defect; a failure that re-runs deterministically is a correctness defect; zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The rebuild owns the page-set within the verdict envelope: split any page named by `[V13]`, mint the `[V12]` discipline pages, extract owner blocks, re-home concerns across the seven folders — every structural change updates `ARCHITECTURE.md` `[01]`-codemap/`[02]`-seams, the `README.md` `[01]-[ROUTER]` and package groups in the same motion. The folder-architecture law binds and the seven-folder partition is RATIFIED — Analysis(6)/Model(7)/Runtime(7)/Solver(6)/Stats(2)/Symbolic(4)/Tensor(9) are all >=2-page growth axes with zero one-file folders and zero flat sprawl; NO new top-level folder is warranted by any verdict (a new discipline is one `Analysis/<name>.md` page + one seam `Discipline` row + one `AssessmentRoute`; a numeric enabler is one row on an existing Tensor/Solver owner). The one graded structural defect is page-granularity asymmetry inside Runtime (`[V13]`). The post-campaign page-set is sealed so no rebuild guesses a name: Analysis 6->8 (`circulation.md`, `daylight.md`), Solver 6->8 (`satisfy.md` — the `[V12]` Z3 owner; `constitutive.md` — the `[V13]` extraction), Runtime 7->8 (`channels.md` dies into `wire.md` + `transport.md`; `codecs.md` stays whole), Model 7 (`extension.md` KEEP sealed), Stats 2, Symbolic 4, Tensor 9 — 46 pages sealed, the same seven folders, every folder >=2 pages. No page survives on incumbency, and no page falls to it either: a 9.5-grade fence is rebuilt only where a verdict names its defect.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md:35,54`): Compute is APP-PLATFORM, depending UP on `Rasm` (kernel) and `Rasm.Element` (the seam) only, PORT-peering with `Rasm.Persistence`/`Rasm.AppHost`/`Rasm.AppUi`, and NEVER referencing an AEC peer (`Rasm.Materials`/`Rasm.Bim`/`Rasm.Fabrication`) except at the companion-rpc wire. Consequences sealed here: the mandate's `SectionProfile`->capacity-overload dispatch CANNOT live in the Compute structural resolver (`[V4]`); Compute consumes kernel geometry (predicates, acceleration wires, encoded payloads) and never re-owns it (`[V7]`); the seam vocabularies (`Discipline`, `AssessmentOutcome`, `SolvePhase`, `FailureKind`, `Diagnostic`, `AssessmentPayload`) are Element-owned and composed, never re-declared ("a Compute-local discipline enum is the deleted form", `Analysis/assessment.md:18`). The existing `ARCHITECTURE.md` [04]-[SEAM_PROHIBITIONS] and [05]-[PROHIBITIONS] blocks carry forward unamended.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — every roster is `[SmartEnum]`/`[Union]` + FrozenDictionary seed DATA with a generated total Switch — so the law binds the NEW planes: runner failure classification is `(SolvePhase, FailureKind)` rows lowered onto the seam `Diagnostic`, never per-runner string formats; the EC2 shear pairing is one capacity-table row + one context extension, never a parallel check family; new disciplines are one seam row + one route + one runner page over the existing spine (the `ComputeReceipt.Assessment` partial already absorbs them); execution providers are rows with realized consumers or the packages leave; sparse-eigen and native-direct are rows on the existing `FactorKind`/substrate axes mirroring blas.md's managed<->native pattern; AD is a leg on the ONE `Sensitivity`/`Chain` family (geometry tape + symbolic tape + hyperdual — never a fourth parallel gradient mechanism beside the finite-difference fall it deletes). The test: the next discipline, provider, eigensolver, join, or failure kind is one data row, never a new arm, a second rail, or a stringly format.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — the corpus norm (`Analysis.Assess`, `Solve`, `Optimize`, `Propagate`, `Transform.Apply`), sealed for every new owner; the `[V1]` `Sweep` is a second Analysis-rail ENTRY on the one spine (admission.md confirms discipline lanes are self-contained entries, not `ComputeIntent` cases), never a parallel dispatcher.
- Every fault is a typed `ComputeFault` case in the 2200-band custody (`admission.md` owns the map; next-free 2221); a stringly interpolated detail where a typed case belongs is the deleted form (`[V2]` kills the four live breaches); every band case crosses the wire through the one `FaultDetail` family (`[V3]` restores the law).
- Lifecycle is seam law: the persisted `Node.Assessment` carries the seam `AssessmentOutcome`; Compute reads `Usable`/`Terminal`/`Dispatchable`/`Coherent` columns and flips through `Next()` adjacency — a Compute-side lifecycle enum or a blanket cache-hit that ignores `Outcome` is the deleted form; and Compute OWNS the flips the seam assigns it — stale-marking (`IsStaleFor` -> `Advance(Stale)`), supersede close-out, orphan-cancel (`Element assessment.md:229-236`) — a sweep that only dispatches is half the contract.
- Content identity composes the kernel seed-zero `XxHash128` rail (`ContentHash.Of` — the landed `Rasm/.planning/Domain/identity.md` `ReadOnlySpan<byte> -> UInt128` federation entry / the seam `CanonicalWriter`) over canonical bytes — never a per-call-site hash over a formatted string (`[V6]`b kills the sole breach; the kernel ledger pins the law on Compute by name, `Rasm/ARCHITECTURE.md:89` mirroring `ARCHITECTURE.md:102`); the policy-seeded `InterchangeIdentity` stays DISTINCT and never conflates.
- Cross-package member claims verify or die, each against the instrument that owns it: planning-scoped sibling members READ THE LANDED FENCES — the Element seam rows (`AssessmentPayload.Failed`, `Diagnostic.Of`, `AssessmentOutcome.Dispatchable`) against `Element assessment.md`, every kernel member against the landed `Rasm/.planning/` pages plus the `.api` catalogues (the converted kernel is source-less; `assay api` has no kernel or Element assembly to decompile and is never this campaign's verification spine for them); restored external packages verify through `uv run python -m tools.assay api` (`HyperJet` `DDScalar` members, `Microsoft.Z3`, the OrTools `Google.OrTools.Graph` flow members); vendored sources verify against the vendored tree with the catalog authored at admission (csparse-interop ARPACK bindings, cslsqp). A member no instrument verifies is a phantom.
- Frozen fixtures are derivation-proven, never asserted: the `CLASH_GOLDEN` 160-byte two-sided fixture (`clash.md:387`) re-proves by byte-layout derivation from the two transcription-complete fences — the kernel `[CLASH_SEAM]`/`[CLASH_GOLDEN]` wire law (`Rasm/.planning/Spatial/index.md:778-779`: 72 bounds bytes + 88 node bytes = 160, every value pinned to one-ULP rounding) against the Compute assertion — performed and recorded on the citing page, with the kernel page's harness-gate prose (`:779,793`) re-framing to the derivation law in the same motion, a bounded 1-hop ripple. No executable harness and no test file exists in this campaign.
- Recorded consumer pressure (`RASM-CS-APPUI-BRIEF.md` `[V5]`, AppUi the demanding consumer): `Runtime/payload.md`'s `meshlet-cluster` row extends to a cluster-LOD chain — per-cluster object-space error + parent-error columns on the `ResidencyMeshlet` descriptor, minted by the meshopt simplify ladder the `SimplifyTarget` policy already drives, under a MONOTONIC cut guarantee (parent error >= child error) so a screen-space error cut is crack-free and double-draw-free by construction; AppUi reads the encoded columns for its hysteresis LOD cut and never re-clusters.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped; never `dotnet add`) + `Rasm.Compute.csproj`; per-package catalogs under `libs/csharp/Rasm.Compute/.api/` (40), shared substrate under `libs/csharp/.api/` (32). Manifest motion is staged by dependency, never one blanket sweep: the FParsec/DirectML/Gpu direct references are out and HyperJet (`csproj:19`, `PROPS:112`) + Microsoft.Z3 (`csproj:33`, `PROPS:121`) are admitted with their catalogs; the `[V11]` CAS swap executes with the Symbolic stage (the AngouriMath pin `PROPS:104` and catalog are staged; MathNet.Symbolics + FSharp-weight leave with the swap — determinism source-settled at the pin); the `[V8]` BFE/FEALiTE retirement executes with the stage-2 beam rows; vendored sources (`csparse-interop`, `cslsqp`) land with their first compose; the QuikGraph/NTS/Clipper2 folder admissions (csproj rows) land with the runner that first composes them, central pins unchanged. Carried obligations, one motion per line:
- REMOVE the orphan `Microsoft.ML.OnnxRuntime.DirectML` + `.Gpu` central pins (`PROPS:117,119` — references and catalogs already out; re-admission requires the deployment that demands the `ExecutionProvider` rows in the same motion). The `FParsec` central pin (`PROPS:109`) survives only as the `MathNet.Symbolics` transitive floor and dies entirely with `[V11]`.
- KEEP `MathNet.Numerics.Providers.MKL` as `blas.md`'s RID-claim-gated x64 `LinearProvider` row — the kernel dropped its own MKL reference (x86-64-only, cannot load on osx-arm64) and flagged Compute's as sibling-roster debt; the claim gate IS the design (osx-arm64 resolves managed/OpenBLAS, an x64 deployment claims MKL), recorded here so the flag closes.
- KEEP `PollinationSDK` (`csproj:45`, `README:80`, `PROPS:319` 1.10.0) — the `EnergyRoute.Cloud` run-dispatch transport the `Analysis/energy` cloud route composes; the DURABLE half (artifact bytes, lineage, cloud-run result index) is Persistence-owned with the canonical full-surface catalog at `Rasm.Persistence/.api/api-pollination-sdk.md`, and the Compute-tier `api-pollination-sdk.md` carries the dispatch delta only — the census row that closes the roster reconciliation.
- SETTLED: `SharpGLTF.Ext.3DTiles` IS Compute's (`codecs.md` TILE_PARTITION is the live tileset emitter); `api-sharpgltf.md` documents the `Tiles3D` surface at depth (`:47`) and scopes the `SharpGLTF.Runtime` decode surface to the Bim glTF-import owner.
- REPLACE `MathNet.Symbolics`+`MathNet.Numerics.FSharp`+`FSharp.Core`-weight with `AngouriMath` per `[V11]` — the pin (`PROPS:104` 1.4.0) and the domain catalog (`api-angourimath.md`) are staged; the csproj swap executes with the Symbolic stage.
- ADD — LANDED with catalogs: `HyperJet` (ISC, net10, NuGet-published; `api-hyperjet.md`); `Microsoft.Z3` (MIT; the NuGet stable ships win-x64/osx-x64 natives only — the managed wrapper is admitted with the arm64 `libz3` provisioned through the Forge nix lane, nixpkgs `z3`; a `Context` operation without the provisioned native faults at init; `api-microsoft-z3.md`). ADD — catalog staged, source vendoring at first compose: `csparse-interop` (BSD-3, vendored from the live `wo80/csparse-interop` — same author as the admitted CSparse; `api-csparse-interop.md`); `cslsqp` (ISC — vendored source until its NuGet release lands).
- RETIRE the four frame-backend rows `BriefFiniteElement.Net`/`BriefFiniteElementNet.CustomElements`/`FEALiTE2D`/`FEALiTE2D.Plotting` (`csproj:37-40` — the exact manifest ids; `BFE.CustomElements` is not a real package id) from the runtime graph per `[V8]`, the retirement taking its central transitive-floor pins in the same motion (`BriefFiniteElementNet.Common` `PROPS:480` under the BFE pair; `netDxf.netstandard` `PROPS:482` under `FEALiTE2D.Plotting` — lock-verified sole parents; the Fabrication brief's residue check depends on this sweep).
- VERIFY the substrate `api-languageext.md` (the ONE substrate file is landed — verify, never re-author); the shared-tier reconcile motions below ride the same one-substrate-file law.
- SETTLED: the nine once-md5-identical cross-tier copies (extensions-ai, grpc-aspnetcore, grpc-client, grpc-client-web, grpc-common, grpc-tools, highperformance, nodatime-protobuf, protobuf) are collapsed to substrate-canonical bodies with three-line domain-tier pointers. RECONCILE the EIGHT still-forked pairs (`api-tensors` — substrate richer than the domain overlay, backwards; `api-unitsnet`; `api-csparse`/`api-hashing`/`api-hybrid-cache`/`api-mathnet-providers`/`api-nodatime`/`api-thinktecture-json` — domain copies forking the base body) to one canonical base + a Compute-delta overlay each.
- UNIFY the MathNet tiering — the substrate core catalog (`libs/csharp/.api/api-mathnet-numerics.md`) is landed with the providers/symbolics satellites in the domain tier; the open half is the `api-mathnet-providers` fork reconcile above. The central `MathNet.Numerics` pin HOLDS at `6.0.0-beta2` (the once-planned 5.0.0 downgrade is SUPERSEDED — `Integrate.OnCuboid` is 6.0-only; the `PROPS:62-63` comment carries the ruling), so the numeric-spine fences verify against the held surface.
- GATE the `api-microsoftaspnetcoretesthost.md` precedent explicitly (the single sanctioned production-seam-shaped-for-test overlay for the `RemoteTransport.InProcess` slot — test packages never proliferate into domain tiers). License gate: OSS and free-for-OSS-commercial admissible; GPL/AGPL/pay-tiered/seat-licensed REJECTED — recorded rejections never re-proposed: TetGen (AGPL), Gmsh (GPL), CGAL (GPL), Epanet.Net (GPL — the Todini global-gradient algebra is freely implementable), MSolve/MGroup (NO license file on the core stack — verified), Numerics.NET/NMath/ALGLIB-commercial/Extreme (commercial), Accord.NET (archived 2020), ML.NET (AutoML-oriented, weaker than the TorchSharp substrate), CMAES.NET (redundant — `optimizer.md` already runs a faithful CMA-ES over the lane's own Evd), in-process ANN/HNSW (the retrieval-index concern is Persistence's by contract — `embedding.md`'s two-stage rail is honest and stays).

## [01]-[CAMPAIGN_VERDICTS]

Every verdict below lands. Recommended shapes follow each; the realization may exceed them, never dilute them.

### [V1_FAILED_LIFECYCLE_SPINE]

The campaign-defining realization: `Analysis/assessment.md` rebuilds to consume the FULL seam lifecycle. Four motions on the one spine, composing — never re-minting — the seam vocabulary: (a) lifecycle-aware CacheFirst — the `graph.Find<Node.Assessment>` hit inspects the cached payload's `Outcome` through the seam columns before short-circuiting: `Computed`/readable-`Stale`-when-policy-allows noop, `Failed` returns the cached failure receipt (deterministic failures cache), in-flight `Queued`/`Running` returns a typed in-flight verdict, `Dispatchable` rows recompute; (b) the Failed write-back motion — a runner failure lowers through `Diagnostic.Of(phase, kind, message, key)` into `AssessmentPayload.Failed(discipline, route, inputKey, diagnostic, provenance)` persisting a Failed `Node.Assessment` under the SAME content-keyed id, while admission/infrastructure faults stay rail-only and never cache; (c) the retry gate — a cached Failed whose `Diagnostic.Kind.Transient` is true re-dispatches under the policy row; a non-transient Failed re-runs only through an explicit Pending re-request (the seam's own law, `Element assessment.md:59`); the gate is BOUNDED, never an unbounded re-dispatch loop: capped attempts plus `Provenance.At`-age backoff, both enforceable on the seam — the attempt ordinal lands in this campaign as an authorized 1-hop Element edit: `Provenance` (`Element assessment.md:146-148`) gains one additive audit column `int Attempt = default`, content-key-inert by construction because the `CanonicalBytes` projection (`:225-226`) folds only the `(Discipline, Route, InputKey)` triple and excludes the whole `Provenance` record; (d) the `Sweep` entry — a second Analysis-rail entry dispatching every `Outcome.Dispatchable` row (Pending/Stale) over the target set and structurally skipping Queued/Running, COMPOSING the `Runtime/scheduling` `JobGraph` (content-key `Reconcile`, `JobState` lifecycle, `CheckpointPort`, fair-share) as its execution substrate — a parallel re-solve engine is the deleted form. The Sweep is a RECONCILER, not a dispatcher alone — three duties the seam's own adjacency assigns Compute ("the next Bake surfaces a Stale assessment the Compute sweep re-dispatches", `Element assessment.md:229-236`): (1) STALE-MARKING — before dispatch, the sweep folds baked `Node.Assessment` rows against the CURRENT graph, recomputes each row's input key through the one content-key fold, and flips `IsStaleFor`-true Computed rows to Stale through `Advance` (the legal `Computed -> Stale` edge, `:232,:239`) — without this fold the sweep finds zero Stale rows forever and the re-solve flagship dies at its first link; (2) SUPERSEDE CLOSE-OUT — a re-solved row writes the fresh node under the fresh key and flips the drifted predecessor Superseded in the SAME delta, so exactly ONE usable node survives per `(discipline, route)` — the seam's own one-usable-node law; (3) ORPHAN RECOVERY — a Queued/Running row whose content key resolves to NO live `JobGraph` job (the `Reconcile` content-key check) is a dead worker's wreckage: the sweep flips it Cancelled with the abort `Diagnostic` (`(Solve, Resource)` — transient) and re-dispatches through the Pending edge, so one worker death never wedges an assessment behind the in-flight skip forever. `JobState` (job lifecycle) and `AssessmentOutcome` (node lifecycle) stay orthogonal, mapped at the sweep boundary only. The `ResultBlob` artifact sink threads in the SAME signature motion (`assessment.md:107,114` and `energy.md`'s eplusout.sql both self-flag the pending injected sink) — one `Assess`/`Run`/runner signature change, not two — and the sink's LANDING is named, never left as an injected abstraction: the content-keyed blob rides the Persistence blob lane and registers through `ArtifactIndexRow.Admit` under an assessment `ArtifactKind` row (one data row per the Persistence growth law, `cache.md:20`) so every heavy artifact is retention-governed and reusable, never an orphan blob no index owns — the in-corpus proof shapes are the `Model/sessions` EpContext row (`ARCHITECTURE.md:101`) and the scheduling SPILL row (`:106`); the assessment `ArtifactKind` row lands in this campaign as an authorized 1-hop Persistence edit per that page's own growth law (`cache.md:21`): `public static readonly ArtifactKind Assessment = new("assessment", RetentionClass.Cache, CacheLane.ModelResult);` in the roster (`cache.md:30-41`), the Cases enumeration (`:16`) and the blob-vs-cache prose (`:22`) advancing in the same motion — `Admit` is already polymorphic on kind. `ComputeReceipt.Assessment` gains the failure columns (phase/kind/transient beside verdict) with the `AssessmentWire` projection (`receipts.md:416`) widening in the same motion. This verdict discharges the Element seam's named Compute-consumer obligation (`Element assessment.md:59-60`).

### [V2_RUNNER_TYPED_FAILURE]

The four runners surface typed failure UNIFORMLY — one refinement across the shared `Fin<AssessmentResult>` contract, not four bespoke rebuilds. The stringly `ComputeFault.AnalysisRunFailed($"<...>")` arms (`structural.md:285,389`; `energy.md:279,299,318`; the physics fire-route and lifecycle EC3 arms) die for one typed case carrying the SEAM's vocabularies:

```csharp
// ComputeFault analysis lane (2217-2219 custody), declared in assessment.md's existing partial-extension block
// (assessment.md:219-223) — a nested case reaches the private (detail, code) base ctor; a top-level record beside
// the [Union] is the deleted form. Inherits AnalysisRunFailed's 2219 slot (2217/2218 stay AssessmentInputMissing/
// ToolchainUnresolved); the admission.md:173 custody row renames in the same motion. SolvePhase/FailureKind are
// the Rasm.Element seam rows — a Compute-local phase/kind enum is the deleted form; Code carries the foreign
// exit/status code into the seam Diagnostic's own Option<int> slot, never smuggled through the message.
public abstract partial record ComputeFault {
    public sealed record AnalysisFailed(SolvePhase Phase, FailureKind Kind, string Detail, Option<int> Code = default) : ComputeFault(Detail, 2219);
}
```

Classification is row data per runner over the VERIFIED seam rosters — `SolvePhase` is exactly `Admission|Solve|Extraction|Publication` (`Element assessment.md:92-99`) and `FailureKind` exactly `Input|Numeric|Resource|Timeout|Aborted|Foreign` with `Transient` a derived COLUMN true only on `Resource`/`Timeout` (`:104-115`); a mapping onto any other token is a phantom seam member. The corrected rows: structural maps assemble/factor/recover breaks onto `(Solve, Numeric)` and missing section/support inputs onto `(Admission, Input)`; energy maps toolchain discovery onto `(Admission, Resource)` (the seam's own missing-binary transient), subprocess exit onto `(Solve, Foreign)` with the exit code riding `Diagnostic.Code` (`Option<int>`, `:122-141`), and eplusout.sql absence onto `(Extraction, Foreign)`; physics maps admission gaps onto `(Admission, Input)`; lifecycle maps EC3 transport onto `(Admission, Timeout)` with the HTTP status in `Code` — the exact rows fix against those rosters; the law is that every arm names its phase and kind, `Resource`/`Timeout` are chosen ONLY where re-dispatch can clear the cause (network, worker death, toolchain race — a singular matrix is `(Solve, Numeric)`, deterministic and cached), and the interpolated detail survives as the message payload, never the discriminant.

### [V3_FAULT_WIRE_PARITY]

`Runtime/channels.md`'s `FaultDetail` projection re-derives from the band: the "seventeen-code taxonomy" prose and fifteen-row `StatusRail` (`channels.md:9`) become a band-complete rail covering 2200-2220 with the `[V2]` case in its 2219 slot — the analysis lane (2217-2219) and `GraphCyclic` (2220) cross the wire, and the `StatusCode`->`WireFault` fold stays total by construction (a band case without a wire row fails the build, mirroring the receipts registry discipline). Growth law: a new `ComputeFault` case is one band row + one wire row in the same declaration motion — the count-vs-band drift class this verdict kills cannot recur. The Remote `WireFault` 4520-4532 wire sub-band (`channels.md:205,218` — distinct from the 2200 band and from AppHost `HopFault` 4500) is RECORDED beside the `admission.md` custody map as Compute's second custody and is PINNED as a mirror row in the AppHost/AppUi/Persistence registries (`RASM-CS-APPHOST-BRIEF.md` `[V1]` re-bands `CoordinationFault` to 4540 around it) so cross-package disjointness is checkable from both ends; the custody map additionally carries the reciprocal PINNED foreign-neighborhood rows (AppHost 1xxx/4100-4810 with the 4540 `CoordinationFault` re-band, AppUi 6xxx, Persistence 5xxx/771x/82xx-83xx, the AEC 23xx-27xx registry) exactly as those registries pin Compute's two custodies — the mirror discipline is symmetric, a foreign band change is a row edit on both ends, never prose; the `[V13]` split carries the custody note into `wire.md`.

### [V4_SECTION_DISPATCH_STRATA_RULING]

Mandate item 2 is reframed as strata law — the stated shape ("Catalogued -> rolled `SteelDesign.Capacity`, ColdFormedC -> `ColdFormedSections.Of` -> the cold-formed Capacity overload" IN the Compute structural resolver) is architecturally FORBIDDEN: `SectionProfile` is a `Rasm.Materials`-authoring union (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:234`), Compute reads the arm-erased resolved `graph.SectionOf` -> `ComputedSection`/seam `SectionProperties` (DECISION `:72` rules Compute ZERO structural ripple), and an APP-PLATFORM -> AEC-DOMAIN reference violates the sealed prohibitions. RULED: the `Catalogued`/`ColdFormedC` overload dispatch is Materials-interior, and the effective-section concern is ALREADY REALIZED Materials-side at its correct stress-aware owner — `steel#STEEL_FAMILY` `DesignCapacity` derives `Seff` on the cold-formed arm (`Rasm.Materials/.planning/Component/capacity.md:159,504`, read directly by the capacity lift). A `ComputedSection` bake is the rejected form: it would break the receipt's one-polygon-integral invariant (`component.md:797`) and collapse the strength-vs-deflection stress dependence that makes effective width a capacity-owner concern. Compute-side action this campaign: the `aisi-s100` cells (`structural.md:638-644` — today the gross-section bound, effective-width explicitly deferred at `:619`) and their boundary prose correct to consume the Materials `Seff`-scaled capacity read, gross-vs-effective invisible to Compute; no Materials edit exists, and the forbidden downward edge is never authored, ever.

### [V5_EC2_MEMBER_SHEAR]

Mandate item 3 lands as one capacity-table refinement: Materials' RC screen carries `RcElastic.ShearLinkAreaMm2` (`Rasm.Materials/.planning/Component/capacity.md:144,380`) and returns only the section-decidable web-crushing ceiling `V_Rd,max`, explicitly deferring the member term ("the member V_Rd,s = (Asw/s)·z·f_ywd·cotθ needs the stirrup SPACING the RcSection does not carry, so a linked pass DEFERS detailing to the forward Compute member", `:253-254,262`). The Compute `en1992` shear cells (`structural.md:648-654` — plain `ConcreteVc` today) gain the truss-model pairing: for a linked section the governing resistance is `min(V_Rd,s, V_Rd,max)` with `V_Rd,s = (Asw/s) · z · f_ywd · cot(theta)`, reading `Asw` off the seam-baked RC properties, the member stirrup spacing `s` off a `CapacityContext`/`StructuralPolicy` extension (member-scope input, exactly why Materials defers), `z = 0.9d`, and `cot(theta)` as a policy row defaulting 2.5 (matching the Materials ceiling's assumption so the pair is consistent by construction); the linkless arm keeps `V_Rd,c`. One table row family + one context extension + one policy row — no new owner, no parallel check rail.

### [V6_CONTRACT_ELASTICITY_AND_KEY]

The two confirmed `Solver/contract.md` defects land as repairs with the page's rebuild: (a) `SolveProblem` reads the seam elasticity at admission — `graph.PropertiesOf(member).Mechanical` (the REAL seam member, `Rasm.Element/.planning/Graph/element.md:510`; the ledger's `MechanicalOf` spelling is a phantom and re-spells in the same motion across `ARCHITECTURE.md:126`, `contract.md`, and `structural.md`) or carries `(E, nu)` explicitly — `PhysicsKind.Material` takes both parameters, the `Elasticity(scale)` nu=1/3 collapse (`:65-66`) and the lone `materialScale`/`materialShift` erasure (`:301`) die, the ONE isotropic elasticity-D is the correct 2-parameter form `lambda = E·nu/((1+nu)(1-2nu)), mu = E/(2(1+nu))` the nonlinear `ElasticPrimal` already proves (`:1038`), and `GeometricStiffness` inherits the corrected D — the declared-unwired `ARCHITECTURE.md:126` seam wires on the linear path; (b) `SolveProblem.ContentKey` (`:304`) composes the kernel `ContentHash.Of` (the landed `Rasm/.planning/Domain/identity.md` seed-zero entry; the kernel ledger names Compute on exactly this law, `Rasm/ARCHITECTURE.md:89`) over canonical bytes (physics key, element key, condition count-prefixed digests, material parameters as raw doubles), killing the culture-formatted interpolated string — `factor.md`'s `ShardPlan.Digest` is the in-corpus proof shape.

### [V7_EXACT_PREDICATE_FLOOR]

`Solver/discretization.md`'s predicates compose an exact floor; the hand-rolled Bowyer-Watson TOPOLOGY stays owned (the 3D-mesher ecosystem is copyleft — TetGen AGPL, Gmsh GPL, CGAL GPL — which justifies the mesher, never the float predicates). RULED: the in-sphere/orientation tests route through the kernel's exact predicate surface via the declared `Solver/discretization -> Rasm Numerics/predicates` seam row — Compute already depends up on the kernel, the landed `predicates.md` is the reference floor, `libs/.planning/architecture.md` makes the kernel the only legitimate owner, and the kernel ledger already carries the cross-package predicate-wire proof shape at `Rasm/ARCHITECTURE.md:105` (Fabrication <- `Predicate.Orient2D/Orient3D`) and declares the Compute export at `:106`. DISK TRUTH: every public `Predicate` entry as landed is `Point3d`-typed (`Rasm/.planning/Numerics/predicates.md:171-288`; the `Implicit` `[Union]` carrier holds `Point3d` fields; no `Adaptive.Resolve` symbol exists — the adaptive ladder is the inline `ErrorBound ?? Refine ?? Exact` chain inside each member), which fails Compute's own no-host-type lane law (`ARCHITECTURE.md` [04]#2). The gap closes in this campaign as an authorized 1-hop kernel edit under libs-wide ripple authority: `predicates.md` gains coordinate-level core entries — `Orient2D`/`Orient3D`/`InCircle`/`InSphere` over raw `double` coordinate triples routing the same `ErrorBound ?? Refine ?? Exact` ladder — with the `Point3d` entries becoming thin seam adapters over them and the `Rasm/ARCHITECTURE.md:106` seam-row comment updating in the same motion; `Solver/discretization` consumes the coordinate-level entries. The float `Orient`/`InSphere`/`Det4` (`:913-921,962`) die on the sign-decision path, and the local `record MeshSpace` (`:373`) renames off the kernel Vectors `MeshSpace` it flattens from (`Rasm/.planning/Meshing/mesh.md` — a frozen kernel name) — a re-declared soup carrier under a kernel type's name is the deleted form. Recorded growth on the same kernel seam: the isotropic-remesh substrate (LANDED — `Processing/remesh.md` `Remeshing.Apply(Isotropic)`; the kernel ledger row `Rasm/ARCHITECTURE.md:103` names Compute the volumetric boundary-conditioning consumer) is the boundary-conditioning pre-step for defective building shells before tetrahedralization — consumed through the decoded wire when it lands, recorded growth only, never a Compute-side remesher.

### [V8_FRAME_BACKEND_CONSOLIDATION]

The corpus's one duplicate mechanism collapses: `Analysis/structural.md`'s `FrameBackend` outsources FE assemble-solve to `BriefFiniteElement.Net` (linear-static, .NET4-era, last release 2022, maintainer GitHub-banned — supply-chain fragility) + `FEALiTE2D` (2D hobby), while `Solver/contract.md` owns a genuine continuum-FE engine assembling `B^T·D·B` over the SAME CSparse `SparseLU`. RULED default: Euler-Bernoulli/Timoshenko beam/frame rows join the owned `ElementClass` axis (standard 12-DOF member stiffness with releases and rigid-end offsets as row columns), `FrameModel` idealization and the `(DesignCode, LimitState)` capacity table stay UNTOUCHED (the genuinely excellent layer), `FrameBackend` re-routes member response through the owned spine, and BFE+FEALiTE retire from the runtime graph. The retirement is UNCONDITIONAL — the consumed feature set is disk-verified plain linear-frame capability: assemble-solve, rigid 6-DOF supports, point/uniform/trapezoidal member loads, and per-station internal-force and displacement recovery, with envelopes a Compute-side fold over station queries (`structural.md:265-286,298-353,373-379,449-468`); member end releases, semi-rigid connections, and rigid-end offsets are consumed NOWHERE (`:285` runs `BarElementBehaviours.FullFrame`; `:66-70` lowers spring columns conservatively rigid). The owned beam rows land carrying releases (static condensation), rigid-end offsets (transformation), and semi-rigid stiffness modification as row columns — closed forms exceeding the consumed set — and BFE+FEALiTE leave the manifest with their catalogs, README group rows, and transitive-floor pins in the stage-2 motion. MSolve/MGroup stays REJECTED (the core stack carries no license file — verified).

### [V9_SPARSE_EIGEN_NATIVE_TIER]

The self-flagged eigensolver ceiling lifts: `contract.md:104,733` admits dense EVD is the only eigensolver the admitted stack provides, while building-scale `fea-modal`/`fea-buckling` (and `[V12]`'s seismic response-spectrum route) are 10^4-10^6 DOF where dense O(n^3) is infeasible. ADMIT `csparse-interop` (BSD-3, same author as the admitted CSparse — the canonical extension, not a parallel rail; source-vendored, not on public NuGet) binding ARPACK shift-invert Lanczos (the industry-standard sparse generalized `K·phi = lambda·M·phi` eigensolver) plus native CHOLMOD/SuperLU direct factors: one sparse-eigen row on the `Solver/contract` `SolveMethod` axis + native-direct rows on `Tensor/factor`'s `FactorKind`, mirroring blas.md's managed<->native substrate pattern (managed CSparse stays the cold-start terminal; the native row is claim-gated by RID exactly as `LinearProvider` is). osx-arm64 runs the SuiteSparse/ARPACK leg (MKL is x64-only). The landed kernel `Numerics/matrix.md` LOBPCG is KERNEL-INTERIOR spectral machinery — the kernel ledger declares no matrix-plane seam toward Compute (`Rasm/ARCHITECTURE.md:73-99` carries only the identity, `DiscreteCalculus`, `MeshAdjointSnapshot`, acceleration, and encoding rows) and the conversion ruled Compute consumes wire arrays, so the sparse-eigen row composes the admitted ARPACK substrate over Compute's own CSparse-assembled `K`/`M`, never a reach into the kernel matrix plane. This verdict lands the native-BLAS design half; the native assets provision through the `Parametric_Forge` nix lane — nixpkgs carries `z3`, `suitesparse`, and `arpack` with osx-arm64 derivations — and a native row without its provisioned asset faults at init, never silently degrades.

### [V10_AD_OWNER_AND_SLSQP]

Exact gradients replace the finite-difference fall: ADMIT `HyperJet` (ISC, net10, NuGet-published; source-generated `DDScalar1..15`/`DDScalar` hyper-dual numbers, `GetGradient()`/`GetHessian()` -> MathNet export) as the scalar-AD leg of the ONE `Sensitivity` family — `Tensor/dispatch` already owns the dual-mode geometry tape — its ingress is the DECLARED kernel adjoint seam: `Sensitivity.Operator` routes `OperatorRow.Apply`/`Adjoint` over the recorded kernel `MeshAdjointSnapshot` carrying the frozen `DiscreteCalculus` bundle (`ARCHITECTURE.md:124` mirrored by `Rasm/ARCHITECTURE.md:82-83`; the landed fences are `Rasm/.planning/Numerics/spectral.md` and `Meshing/mesh.md`, and Compute names only `MeshAdjointSnapshot`/`OperatorRow`, never a re-assembled DEC matrix) — and `Symbolic/lowering` the symbolic tape, all under the one `Chain` contract; hyperdual joins as the third leg for general smooth scalar objectives, and a fourth parallel gradient mechanism is the deleted form. Consumers rewire: `Stats/estimator`'s three `FiniteJacobian` sites (`estimator.md:582` ARMA-MLE, `:604` Holt, `:621` the recurrence arm — "the autodiff-tape-absent fall") take the exact Jacobian, `Solver/uncertainty`'s FORM/SORM limit-state gradient gains the exact-AD row beside the finite-difference fallback (caller-supplied black-box oracles keep FD honestly), and `Tensor/blas`'s `LevenbergMarquardt` documents HyperJet as its canonical Jacobian provider. A smooth constrained-NLP row joins `OptimizerKind`: `slsqp` via `cslsqp` (ISC, pure-C# net10, span-based — the matched Oberbichler pair with HyperJet) for sizing/calibration problems that are neither topology-adjoint nor MILP — vendored from the live `oberbichler/cslsqp` source (feed-verified absent from NuGet), vendored source the ruled admission form, recorded on the page.

### [V11_SYMBOLIC_CAS_REBASE]

The Symbolic folder re-bases on the categorical-best CAS: `MathNet.Symbolics` is self-described "basic" (no equation solving, no integration, no limits) and costs four dependencies (Symbolics + Numerics.FSharp + FSharp.Core + the contradictory direct FParsec); `AngouriMath` (MIT, active, net-current) natively owns parse/simplify/solve/integrate/differentiate/limit/LaTeX/`Compile<>` and collapses the FParsec parser and the `compileExpression` machinery into one owner. RULED default REPLACE: `SymbolicExpr`'s identity re-bases on AngouriMath's canonical form (the `[ComplexValueObject]` content-key law survives — identity stays the canonical-NF `XxHash128`, the engine's `Entity` replaces the F# `Expression` as the stored-but-excluded member), `dimensional.md`'s Q^7 fold re-bases its expression walk, `lowering.md`'s `CompiledExpr` cache re-bases on `Compile<>` (the L1-over-`CacheLane.ModelResult` discipline and the `Rasm.Persistence/Query/cache` seam-98 crossing are UNCHANGED — the content key is the page's own, not the engine's), and `units.md` is untouched except its E14 typed-`CorrelationId` repair. Determinism is SETTLED at source (tag v1.4.0): commutative operand ordering is structural-lexicographic — `SortAndGroup` orders children by pure-recursive `SortHash` strings through an explicit `OrderBy`, with no `GetHashCode`, no `Random`, and no dictionary-enumeration dependence on the canonical path — so the canonical form is byte-stable across process runs at the pin and the swap executes unconditionally. Two identity laws ride the re-base: the canonical-form fold binds ordinal string comparison (culture-independent by construction), and a key never derives from `Entity.GetHashCode()` (per-process randomized) — the content key is the canonical-NF bytes through the one `XxHash128` rail, identity binds the admitted expression's canonical form, and algebraic equivalence between differently-spelled inputs is not identity, exactly the standing law. New capability either way: the cost/QTO formula lane's `SymbolicOp` axis gains solve/integrate rows the AEC formula consumers (cut-parameter inversion, quantity integrals) can reach.

### [V12_DISCIPLINE_COVERAGE]

The Analysis frontier: the seam declares fifteen `Discipline` rows, Compute answers seven, and the spine is BUILT for growth (one row + one route + one runner page + the existing receipt partial). Three disciplines land this campaign: (a) NEW `Analysis/circulation.md` — `Discipline.Circulation` egress/life-safety runner (IBC Ch.10 / EN route rows): travel-distance shortest-path (Dijkstra/A*) and exit-capacity max-flow/min-cut over the space-adjacency subgraph of the concrete `ElementGraph`, occupant-load-weighted distribution, dead-end/common-path checks — the exit-capacity flow composing the ADMITTED `Google.OrTools.Graph` `MaxFlow`/`MinCostFlow` (bound-assembly-verified members the catalog now carries at `api-ortools.md:74-80`, `MaxFlow.Status` `OPTIMAL`/`POSSIBLE_OVERFLOW`/`BAD_INPUT`/`BAD_RESULT` the typed solve verdict; zero new pins for the flow concern), `QuikGraph` (admitted substrate consumed by Element/Persistence/Bim and NEVER by Compute today) for the path/topology algebra (Dijkstra/A*, SCC, betweenness), and the centrally-pinned `NetTopologySuite`/`Clipper2` for the planar side (isovist polygons, corridor medial-axis, occupant areas); ingress is HONEST — targets, space nodes, and adjacency arrive off the concrete `ElementGraph`, planar boundaries resolve through the SAME `GeometrySource` content-key port every runner threads (`Analysis/assessment.md:278-283`), never a second decode path, a space whose boundary cannot resolve is a typed `(Admission, Input)` failure, never a silently-skipped node, and the kernel slice-stack owner is LANDED (`Meshing/slice.md#Slicing.Apply` — story contours through `LayerPlan.AtElevations` + `LayerAt`, oriented outer-CCW/holes-CW with typed OPEN-chain rows; the kernel ledger row `Rasm/ARCHITECTURE.md:100` names this runner the consumer) — the floor-plate upgrade consumes the decoded wire; ZERO new central pins; the one 1-hop counterpart — the `circulation` seam `Discipline` row per the seam's own growth law ("a new analysis discipline is one Discipline row", `classification.md:161`) — lands in this campaign as an authorized Element edit: `public static readonly Discipline Circulation = new("circulation", "Circulation", physical: true);` inserted in the roster (`classification.md:171-185`, which ends at `Cost`; `Circularity` `:183` is a distinct lifecycle row), the page's fifteen-row literals (`:3,157,158,162,206`) advancing to sixteen in the same motion, the IBC/EN code references living in the coverage prose per the roster's own convention; the runner dispatches on the real row. (b) NEW `Analysis/daylight.md` — the seam `Daylight` row EXISTS with no runner (`classification.md:178`): an owned NREL-SPA-grade solar-position kernel (public-domain stable spec, closed-form azimuth/altitude — zero solar computation exists corpus-wide), Perez all-weather sky rows with their ingress NAMED — site latitude/longitude/timezone and the hourly direct-normal/diffuse-horizontal irradiance the Perez rows require arrive through the energy lane's OWN weather surface, a `WeatherRef`-shaped request input (`energy.md:30`) read through the admitted OpenStudio `EpwFile` reader (`api-openstudio.md:85`; the header/data member set `assay api`-gated before compose), the solar kernel computing position while the EPW supplies sky state, a weather-less request degrading to geometry-only shadow facts stated inline, never a silently-defaulted sky — and shadow/obstruction rays REUSING `clash.md`'s decoded BVH + Moller-Trumbore (the ray engine is already built over the kernel `ToAcceleration` wire per `ARCHITECTURE.md:121`); climate-based CBDM/glare (Radiance-class) stays the Python companion decoded over the existing graduation-evidence seam — C# owns geometry-derived daylight facts, Python owns the annual simulation, the same law energy already follows; and the solar-position kernel exports package-boundary-consumable (fenced as public surface per the `[SEAM_AND_RAIL_LAW]` verification tiers; realization proves it compiled) — the AppUi viewport sun-light consumer (the `RASM-CS-APPUI-BRIEF.md:196` `[V6]` solar-position export pin) is its second named consumer, composing azimuth/altitude from site + instant through the declared seam, because a Compute-internal-only kernel forces the AppUi-side duplicate AppUi's own one-kernel clause forbids. (c) `Solver/` gains the rule-satisfaction owner — ADMIT `Microsoft.Z3` (MIT; the arm64 native is Forge-gated per `[ROSTER_RECONCILIATION]`): a typed rule set lowers to Z3 assertions (naturally from `SymbolicExpr` — the CAS is the lowering source) returning SAT/UNSAT + model/unsat-core, surfaced as an `AssessmentResult` whose unsat-core names the exact violated rules; CP-SAT OPTIMIZES, Z3 VERIFIES-and-EXPLAINS — orthogonal concerns, ONE new page `Solver/satisfy.md` sited beside `optimizer.md`; it earns a seam `Discipline.Compliance` row ONLY IF the verdict must persist as its own content-keyed `Node.Assessment` the `[V1]` Sweep dispatches — a rule verdict that merely enriches an existing discipline's `AssessmentResult` rides that discipline's route, the seam growth law the mechanism either way. Seismic response-spectrum unlocks as a structural ROUTE row over `[V9]`'s sparse modal (the seam `Seismic` row exists; `StructuralCase.Seismic` exists) — a route whose chain is fully named, not a new runner: the sparse modal supplies `(phi, lambda)` and modal masses off the owned lumped-mass field (`contract.md:520-538`); the design spectrum is policy DATA — code-spectrum rows (EN 1998 Type 1/2, ASCE 7 — the codes the seam row itself names, `classification.md:206`) parameterized by site class, behavior factor, and damping, never a hardcoded curve; the combination is a row axis (`Srss`/`Cqc`, CQC the closely-spaced-mode default); the 90% modal-mass-participation floor is a TYPED `(Solve, Numeric)` shortfall naming the achieved fraction, never a silent truncation; participation and the combination key land as receipt columns on the `[V1]`-widened `AssessmentWire`. Recorded as seeded growth (named growth rows on this verdict, not built): MEP network-flow (Todini gradient over the admitted sparse solver + QuikGraph), geometric room-acoustics (image-source over the clash BVH), hygrothermal EN 15026 transient (physics.md's own flagged growth), Infer.NET Bayesian calibration (the inverse-UQ/twin-updating owner — admitted only when its consumer page is chartered; its metered-data ingress is the AppHost livewire BACnet row decoded at the seam, RASM-CS-APPHOST-BRIEF.md `[04]`, never a Compute-side building-protocol client), and the Fabrication NFP nest-policy scorer (the injected-delegate producer for that package's recorded nest-policy demand — RASM-CS-FABRICATION-DECISION.md the demanding consumer). One cross-package ingress row rides this frontier as consumer pressure: the `Rasm.Fabrication` `Nesting/stock -> Compute` seam (`Rasm.Fabrication/ARCHITECTURE.md:51`) lands its Compute-side named row — `NestYield.WasteAreaMm2` decoded into the `AggregateEnvironmental`/`AggregateCost` rollups through the seam Material node's Environmental/Cost cases, `RASM-CS-FABRICATION-DECISION.md` the demanding consumer — a decode-side ingress row on the existing lifecycle/cost lanes, never a new discipline.

### [V13_PAGE_GRANULARITY]

Runtime's multi-owner mega-pages normalize against the corpus one-owner-per-page norm: `channels.md` (SEVEN index owners) SPLITS into `Runtime/wire.md` (proto vocabulary + contract evolution + fault projection + TS projection — the cross-process CONTRACT surface; the `[V3]` widening lands here in the same motion, and `wire.md` additionally grounds the `GaussianSplatScan` scan-buffer frame: the SPZ v4 binary spec and the SOG v2 spec are stable versioned MIT-published law, C# owns the wire vocabulary, so the frame layout — position/scale/rotation/spherical-harmonic buffer order, harmonic-degree band, per-splat counts, the member set the landed `SplatScan` carrier `payload.md:114-121` already fixes — mints here and `payload.md:350`'s [UPSTREAM-BLOCKED] marker deletes; the Python companion decode and the `xxhash` cp315 wheel stay named sibling-branch facts) and `Runtime/transport.md` (transport axis + call policy + artifact frames — channel mechanics and the frame law); the seam re-anchor map is the split's partition proof: `ARCH:67-68` (typescript:core/interchange codec/contract vocabulary/contract-drift) -> `wire.md`, `ARCH:69-70` (typescript:core/interchange/frame) -> `transport.md`, and the TS projection cites the frame format by prose anchor, never a cross-split fence import; AppHost consumer pressure (RASM-CS-APPHOST-BRIEF.md): `Rasm.AppHost` composes `channels#TRANSPORT_AXIS` `GrpcChannelPolicy` by reference (`Rasm.AppHost/.planning/Wire/outbound.md:153,161`) and `channels#PROTO_VOCABULARY` (`Wire/companion.md:23,168`), so the re-anchor map carries both AppHost re-points (`TRANSPORT_AXIS`->`transport.md`, `PROTO_VOCABULARY`->`wire.md`) as counterpart obligations recorded with the split. `codecs.md` STAYS WHOLE — the partition fails on disk: `InterchangeIdentity` is a fence-body dependency of the tessellation-bridge and field/result-codec owners (`codecs.md:43,147,257` call `InterchangeIdentity.Key` inside fences that would stay), and `TessellationPolicy` (`:601`, consumed at `:40,143,257`) plus `ComputeArtifact` (`:778`, consumed at `:251`) cross the same boundary; the page index is the navigation law. `Solver/contract.md` (SOLVE_CONTRACT + CONSTITUTIVE) extracts the constitutive owner to `Solver/constitutive.md` UNCONDITIONALLY — the `[03]-[CONSTITUTIVE]` block (`contract.md:971-1214`) declares its own complete type set and dispatch (the `ConstitutiveModel`/`ContactConstraint` unions, `StressUpdate`/`ContactEnforcement`) with zero contract-interior fence dependencies, and SOLVE_CONTRACT imports FROM it (`:296,:691,:707`), the normal downward direction — single-owner mega-pages (`optimizer.md`, `discretization.md`) STAY; they are one fold each. `Model/extension.md` — RULED default KEEP: it is the corpus norm this verdict enforces (ONE owner, `CustomOps`, with its own `Register(SessionOptions, SessionPolicy)` entry, its own non-tensor `Egress` concern, a direct package admission, and its own receipt lane); folding it into `sessions.md` would mint the exact multi-owner defect this verdict kills in Runtime — KEEP is sealed; disk disproves the fold condition. Every split updates the `ARCHITECTURE.md` codemap, README router, and seam-ledger anchors in the same motion; no new top-level folder.

### [V14_SEAM_LEDGER_TRUTH]

The ledger re-points to the post-campaign siblings, ledger-only (verified: every fence composes Persistence generically — zero fence edits): `ARCHITECTURE.md:113` (`Sync` FastCDC) -> the Persistence snapshot-frame FastCDC owner (`Version/timetravel` frames + the blobstore multipart window); `:117` (`Query/pipeline` PORT) -> `Query/columnar` (the surviving parse-to-canonical-bytes owner); `:118` (`Store/quality` SHAPE) -> DROP — the row is vestigial (`clash.md:18` disclaims any quality-plan producer); `:121` (`Sync` WIRE, Confluent Protobuf Kafka) -> `Version/egress` — Compute owns the Protobuf frame; the counterpart is the CloudEvents/NATS pump, and the Kafka-serdes clause resolves against the landed Persistence broker rows on disk; `:101` (`Query/lane` VECTOR_CODEBOOK) -> `Query/retrieval` (the extraction; Compute's side is already honest — `embedding.md` consumes the codebook by reference, fits nothing, stores nothing); the once-declared reverse clause "Processing/solver consumes the Compute LM/constraint Solver rail" is ALREADY ABSENT from the kernel ledger — verified dead, nothing to kill (the forward `:122` clash WIRE row stays, with the `CLASH_GOLDEN` re-prove obligation per `[SEAM_AND_RAIL_LAW]`); `:76` `ProgressStore` — a corpus-wide PHANTOM (zero fence hits; the real progress wire is the `:73` `ProgressMarkWire` row) — RULED: collapse the phantom row into the one progress wire, the governance-stage ledger motion deletes it. Confirmed-stable rows the rebuild must NOT churn: `:100,:103,:104,:105,:110-111` (`Query/cache` survives the Persistence campaign — `MODEL_RESULT_INDEX` and the artifact index are the surviving owners), `:126` (wires via `[V6]` with the `MechanicalOf`->`graph.PropertiesOf(id).Mechanical` re-spell), the Bim companion-rpc rows, and the AppUi/AppHost rows. WATERFALL RIPPLE: NONE REQUIRED BY THIS VERDICT — the Persistence brief already records the Compute ledger rows and the VECTOR_CODEBOOK re-anchor as counterpart obligations (its E6/V12), and the kernel side is landed disk truth. The campaign's two once-waterfalled edits are both DISCHARGED: the predicate export is declared both ends (`Rasm/ARCHITECTURE.md:106`, `[V7]`) and the assessment-`ArtifactKind` consumer-pressure clause sits on the Persistence brief's `cache.md` sound-surface row — verified in place.

### [V15_API_TIER_RECONCILIATION]

The `[ROSTER_RECONCILIATION]` catalog motion executes as a verdict, not a chore. SETTLED on disk: the nine once-md5-identical cross-tier duplicates (extensions-ai, grpc-aspnetcore, grpc-client, grpc-client-web, grpc-common, grpc-tools, highperformance, nodatime-protobuf, protobuf) are collapsed to substrate-canonical bodies with three-line domain-tier pointers; `api-ortools.md` carries the `Google.OrTools.Graph` flow surface at `:74-80` (`MaxFlow`/`MinCostFlow` — bound-assembly-verified, the `[V12]` exit-capacity solver); `api-sharpgltf.md` documents the `Tiles3D` surface Compute-side (`:47`) with the Bim-owned `SharpGLTF.Runtime` decode surface scoped to a transitive-only note. OPEN: the EIGHT still-forked pairs reconcile per `[ROSTER_RECONCILIATION]` (`api-tensors` — the RICHER substrate copy becomes canonical, the domain overlay narrows to the Compute delta; the other seven — one canonical base, the Compute delta re-derived); `api-languageext.md` is VERIFIED at the substrate tier (landed — verify, never re-author); the `api-mathnet-providers` fork closes the MathNet tiering (the substrate `api-mathnet-numerics.md` core is landed beside the domain-tier providers/symbolics satellites); the TestHost precedent is ruled explicitly per `[ROSTER_RECONCILIATION]`. Every removed package's catalog leaves with it; every admission lands its stub in the same motion; the substrate `libs/csharp/.api/api-extensions-ai.md:251` embedding-production stacking claim is verified against the real wire (the `IEmbeddingGenerator` production lane is the AppHost-governed generative port; `embedding.md` is retrieval over pre-computed vectors — confirm the split and record it in both, never rewire).

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every edit re-verifies its rows against disk first. Anchors are `libs/csharp/Rasm.Compute/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md`; `CSPROJ:` the package csproj; `ELEMENT:`/`MATERIALS:` those packages' planning trees; `DECISION:` `.archive/RASM-COMPONENT-PARADIGM-DECISION.md`; `API:`/`SUBAPI:` the domain/substrate catalog tiers.

| # | Class | Evidence |
|---|---|---|
| E1 | Unwired lifecycle seam | `ELEMENT:Assessment/assessment.md:59-60` names the Compute sweep + Transient policy; `:70` 8-row `AssessmentOutcome` (Failed/Stale/Superseded + Dispatchable/Coherent/Next), `:104` `FailureKind.Transient`, `:122` `Diagnostic.Of(phase,kind,message,key)`, `:158` `AssessmentPayload`, `:196` its `.Failed` factory; Compute `Analysis/assessment.md:254-258` `nodeId`-gated `Find` noop, `:260-261` failure never cached, `:107,114` `ResultBlob` sink pending (`energy.md` twin flag); `Runtime/receipts.md:416` `AssessmentWire` no failure slot. Seam rosters verified: `SolvePhase` = Admission/Solve/Extraction/Publication (`ELEMENT:Assessment/assessment.md:92-99`), `FailureKind` = Input/Numeric/Resource/Timeout/Aborted/Foreign with `Transient` true only on Resource/Timeout (`:104-115`), `Diagnostic` carries the `Option<int>` foreign-code slot (`:122-141`); staleness is assigned to the Compute sweep (`:229-236` `IsStaleFor`, `:239` `Advance`); `Provenance` (`:146`) carries NO attempt ordinal — the bounded-retry custody gap |
| E2 | Stringly runner faults | `Analysis/structural.md:302` `<bfe-solve-break>`, `:406` `<fealite-singular>`; `Analysis/energy.md` six arms — `:336,:362,:388` subprocess + `:613,:630,:638` cloud route; all four runners share `Fin<AssessmentResult>`; `Runtime/channels.md:9` seventeen-code prose / fifteen-row rail vs the 2200-2220 band (`admission.md` custody map) |
| E3 | Stale seam ledger | `ARCH:113,117,118,121` target Persistence pages the Persistence campaign deletes; `ARCH:101` `Query/lane` -> `Query/retrieval` extraction; the reverse kernel-solver clause ALREADY ABSENT (verified dead); `ARCH:76` `ProgressStore` phantom beside the `:73` `ProgressMarkWire` row; `channels.md:92` already cites the surviving `Version/ledger` CHANGEFEED — bodies fresher than ledger; `.archive/RASM-CS-PERSISTENCE-BRIEF.md` E6/V12 records the counterpart obligations; stable rows `ARCH:100,103,104,105,110-111,126` |
| E4 | Strata mis-attribution | Mandate item 2 vs `DECISION:72` (Compute ZERO structural ripple; reads `graph.SectionOf`), `DECISION:234` (`SectionProfile` Materials-authoring union); `structural.md:534` `aisi-s100` route row, `:619` effective-width deferred to Materials, `:638-644` gross-section AISI cells |
| E5 | EC2 member-shear defer | `MATERIALS:Component/capacity.md:144` `ShearLinkAreaMm2`, `:250-254` V_Rd,s defer prose (stirrup spacing is member-scope), `:262` ceiling-only return, `:380` Asw mapping; `structural.md:648-654` plain `ConcreteVc` shear cell |
| E6 | Contract defects | `Solver/contract.md:65-66` `lambda = scale, mu = 0.5 * scale` (nu=1/3), `:297-304` no Poisson slot + culture-string `XxHash128` ContentKey (`:304`), `:301` lone `materialScale`; `ARCH:126` declared elastic-D seam (its `MechanicalOf` spelling is a phantom — real member `graph.PropertiesOf(id).Mechanical`, `Rasm.Element/.planning/Graph/element.md:510`); `:1038` correct 2-parameter `ElasticPrimal` (nonlinear path only); canonical-byte contrast `Tensor/factor.md` `ShardPlan.Digest` |
| E7 | Float predicates | `Solver/discretization.md:585,617` Orient/InSphere consumption, `:913-921` float `Orient`/`InSphere`, `:962` `Det4`; `:373` local `record MeshSpace` vs the kernel Vectors `MeshSpace` (`:3,18`; frozen owner `Rasm/.planning/Meshing/mesh.md`); `libs/.planning/architecture.md` consumers never re-own kernel geometry; the landed `Rasm/.planning/Numerics/predicates.md` crowns the exact floor (`Predicate.Orient2D/Orient3D/InCircle/InSphere` + the LPI/TPI implicit-point family — every public entry `Point3d`-typed as landed, `:171-288`); the Compute-consumable export is DECLARED at `Rasm/ARCHITECTURE.md:106` and the coordinate-level entries land per `[V7]` |
| E8 | Eigensolver ceiling + FE duplicate | `contract.md:104` `DenseEvd` row, `:733` "never a sparse LOBPCG/Lanczos it lacks"; the native assets provision through the Forge nix lane per `[V9]`; `structural.md:213-218` BFE/FEALiTE `FrameBackend` beside contract's owned `B^T·D·B` spine over the same CSparse LU; BFE 2022/maintainer-banned, FEALiTE2D 2D-hobby (feed-verified) |
| E9 | AD gap | `Stats/estimator.md:574` "autodiff-tape-absent fall", `:582,604,621` `FiniteJacobian` calls; uncertainty FORM/SORM finite-difference limit-state gradient; `optimizer.md` derivative rows geometry-DDG-adjoint only — no smooth constrained-NLP row |
| E10 | CAS depth + FParsec | `Symbolic/expression.md:5,22,101` FParsec transitive-only law — the once-contradicting direct admission is REMOVED from the csproj (the pin `PROPS:109` survives as the transitive floor, dies with `[V11]`); `API:api-mathnet-symbolics.md:18,52` owns the transitive fact; MathNet.Symbolics upstream self-describes "basic" — no solve/integrate/limits |
| E11 | Coverage holes | `ELEMENT:Classification/classification.md:170-185` fifteen `Discipline` rows (`Daylight` `:178`, `Seismic`, `Hygrothermal`, `Water`...) vs seven Compute runners; `Analysis/assessment.md:18` seam-vocabulary law; `contract.md:35` `daylight-radiosity` one FEM row; zero solar-position hits corpus-wide; `SUBAPI:api-quikgraph.md` admitted substrate with zero Compute consumers; NTS/Clipper2 centrally pinned, Compute-unconsumed; no circulation/egress owner anywhere |
| E12 | Roster-vs-catalog | DirectML/Gpu references + catalogs REMOVED, orphan pins `PROPS:117,119` remain (`Model/providers.md:12` Cpu+CoreMl rows only, `:173` growth-note-only members, `API:api-onnxruntime.md:122-131` no osx-arm64 payload); 3DTiles SETTLED (`CSPROJ:74`; `API:api-sharpgltf.md:47` `Tiles3D` at depth); LanguageExt.Core substrate catalog landed (verify stands); MathNet core-in-substrate landed, `api-mathnet-providers` fork open; 9 cross-tier copies collapsed to pointer stubs (SETTLED) + 8 still-forked pairs (tensors/unitsnet/csparse/hashing/hybrid-cache/mathnet-providers/nodatime/thinktecture-json); `API:api-ortools.md:74-80` carries the `Google.OrTools.Graph` flow rows (SETTLED) |
| E13 | Page granularity | `channels.md` 7 index owners; `codecs.md` 5; `contract.md` 2 (SOLVE_CONTRACT + CONSTITUTIVE); corpus norm one owner per page; `Model/extension.md` single-owner (`CustomOps` with its own entry/receipt lane — the `[V13]` KEEP default) |
| E14 | Small typed-carrier defects | `Symbolic/units.md:81-96,129,209-211` bare `Guid` correlation vs the corpus-wide typed `CorrelationId` (sole page, grep-confirmed); `clash.md:387` `CLASH_GOLDEN` derivation-provable from the two transcription-complete fences, the derivation recording with this campaign; `clash.md:255-259` `VertexDepth` plane-distance heuristic documented as candidate-grade, honest |
| E15 | Mandate items 4+6 clean | Item 4: grep `ComponentSection` = zero hits; Analysis pages consume `graph.SectionOf`/`SectionProperties`/`ProfileSet` per `DECISION:72`. Item 6: `Runtime/codecs.md:59` `GeoArrowBuffer.ToImportedGeometry` records the GLB wire layout (interior rings faulted to the kernel, never silently filled); `libs/python/data/ARCHITECTURE.md:54` counterpart seam honest — RECORDED, never re-minted |

Sound surfaces (preserve; touch only where a verdict names them): `Analysis/physics.md` (10), `aggregator.md` (9.5), `lifecycle.md` (9.5), `energy.md` (9.5 minus E2), all seven `Model/` pages (9-10; `extension.md` KEEP per `[V13]`), `Runtime/scheduling.md` (10 — the V1 substrate), `admission.md` (10), `progress.md` (10), `payload.md` (10 — the `GaussianSplatScan` scan-buffer frame grounds on the `[V13]` wire page this campaign; forward consumer pressure: `Rasm.AppUi` (RASM-CS-APPUI-BRIEF.md `[V5]`) consumes the `meshlet-cluster` row as its SOLE cluster source — its viewport LOD selection demands cluster-LOD chain columns (simplification levels + per-cluster error bounds beside the cone set, the error bounds monotonic non-decreasing up the parent chain so a consumer's threshold LOD cut is well-defined and crack-free) as encode-arm data per `[GENERATOR_LAW]`, so AppUi reads encoded error metadata and never re-clusters; AppUi the demanding consumer), `receipts.md` (9.5 minus E1's column), `Solver/optimizer.md` (10 minus the V10 row), `sweep.md` (10), `uncertainty.md` (10 minus the V10 gradient row), `clash.md` (9.5 minus E14), `Stats/signal.md` (9.5), `estimator.md` (9 minus E9), all nine `Tensor/` pages (9-9.5; factor/blas/dispatch gain V9/V10 rows; `residency.md#GEOMETRY_ENCODING` carries forward consumer pressure: `Rasm.AppHost` (RASM-CS-APPHOST-BRIEF.md) demands the `Field` and `Toolpath` encoding case rows its `Sandbox/solver.md:54-55,197` working spellings signature-lock on — two rows per `[GENERATOR_LAW]` landing with the Tensor rebuild as kernel `PackKind` rows + a residency wire row (`ARCHITECTURE.md:128-129`; the kernel counterpart is LANDED — `Rasm/.planning/Drawing/pack.md` carries the `field` and `toolpath` `PackKind` rows on the settled 6-row axis; never a residency-side `GeometryEncoding` owner, the ledger's own deleted-form name)), `Symbolic/dimensional.md` (9.5), and the `ARCHITECTURE.md` [04]/[05] prohibition blocks — the strongest governance prose in the C# tree.

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; the verdicts carry the shapes that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Analysis/assessment spine | 6 | 9.5 | lifecycle-aware CacheFirst; Failed write-back; Dispatchable Sweep over JobGraph with stale-marking/supersede/orphan-recovery; bounded Transient retry; ResultBlob sink -> ArtifactIndex landing; receipt failure columns |
| Analysis/structural | 8 | 9.5 | typed SolvePhase/FailureKind; EC2 V_Rd,s pairing; Seff-scaled capacity read; owned frame rows (BFE/FEALiTE retired); seismic response-spectrum route over sparse modal (code-spectrum policy rows, SRSS/CQC axis, typed participation floor) |
| Analysis/physics+energy+lifecycle+aggregator | 9.5 | 9.5 | typed-failure classification rows only; interiors untouched |
| Coverage frontier (Analysis circulation/daylight + Solver satisfy) | 0 | 9 | egress graph+planar runner (OrTools-Graph flow + QuikGraph paths + NTS/Clipper2 planar, GeometrySource-fed); owned solar kernel + EPW-fed Perez sky + clash-BVH shadow rays + Python CBDM companion; Z3 SAT/unsat-core rule owner (arm64 native Forge-provisioned) |
| Solver/contract | 8 | 9.5 | the seam elasticity read (`graph.PropertiesOf(id).Mechanical`) wired on the linear path; one 2-parameter elasticity-D; canonical-bytes ContentKey; beam/frame ElementClass rows; ARPACK sparse-eigen row |
| Solver/discretization | 7.5 | 9.5 | exact predicate floor (kernel seam or vendored Shewchuk); MeshSpace rename; owned topology preserved |
| Solver/optimizer+sweep+uncertainty | 9.5 | 9.5+ | slsqp row (cslsqp); exact-AD FORM/SORM gradient row; interiors untouched |
| Stats | 9 | 9.5 | HyperJet exact Jacobians (FiniteJacobian fall dies); NWaves rows roadmap-gated (Hilbert/cepstrum/Goertzel) |
| Symbolic | 9 | 9.5 | AngouriMath re-base (solve/integrate/limits reachable); FParsec admission dies; typed CorrelationId through units |
| Tensor | 9.5 | 9.5 | native-direct + ARPACK rows on factor (managed terminal stays); hyperdual leg on the one Sensitivity family |
| Model | 9.5 | 9.5 | cold; DirectML/Gpu admissions resolved; embedding-production split recorded |
| Runtime wire/codecs | 8.5 | 9.5 | band-complete FaultDetail; channels split (wire/transport); codecs whole (sealed); GaussianSplatScan frame minted; seam anchors re-pointed |
| Governance perimeter (ledger/catalogs/manifest) | 5 | 9.5 | six seam rows re-pointed; zero orphan admissions; zero contradiction rows; the nine-copy dedupe held + the eight forked pairs reconciled + LanguageExt verified; the `[V12]` growth frontier recorded on its owning verdicts |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Eff` rails at every entry (the `[V15]` catalog pins its beta-77 surface), `System.IO.Hashing` `XxHash128` seed-zero as the ONE content hasher, NodaTime under `ClockPolicy`, `CommunityToolkit.HighPerformance` pooled spans/`ParallelHelper` on hot folds, `System.Numerics.Tensors` `TensorPrimitives` on float paths — a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package.
- `QuikGraph` (CONSUME — admitted substrate, zero Compute consumers today): Dijkstra/A*/Bellman-Ford shortest paths, SCC/betweenness for wayfinding — the `[V12]` circulation runner's path/topology algebra; the space-adjacency view is built from the concrete `ElementGraph`, never a second graph store; the exit-capacity flow is NOT QuikGraph's — it rides the OrTools row below.
- `Google.OrTools` (DEEPEN LANDED — the catalog carries the Graph rows at `API:api-ortools.md:74-80`): `Google.OrTools.Graph` `MaxFlow` (push-relabel, `MaxFlow.Status` `OPTIMAL`/`POSSIBLE_OVERFLOW`/`BAD_INPUT`/`BAD_RESULT` the typed solve verdict) + `MinCostFlow` (`: MinCostFlowBase`), each `IDisposable` over a native graph taking `int` node/arc indices with `long` capacities/costs, verified in the bound `lib/net8.0` assembly — the `[V12]` exit-capacity solver, outranking a managed Edmonds-Karp consumer minted for the same concern; the CP-SAT/SCIP lanes stay the `optimizer.md` exemplar (`API:api-ortools.md:3`).
- `NetTopologySuite` + `Clipper2` (CONSUME — centrally pinned, Compute-unconsumed): isovist/visibility polygons, corridor medial-axis, occupant-area/offset algebra for the circulation planar side; the kernel stays the exact-geometry owner — these are float production-plane tools at the discipline boundary, never a second exact rail. Folder admission rides the first compose, central pins already held: the csproj reference rows land beside the circulation runner; the substrate-canonical `api-nettopologysuite.md` (146 LOC) and `api-clipper2.md` (215 LOC) are LANDED at `libs/csharp/.api/` per the `[V15]` dedupe law, and `api-quikgraph.md` (substrate, 143 LOC) deepens on first compose; the sibling folder-tier copies (`Rasm.Bim/.api/api-nettopologysuite.md` 162 LOC, `Rasm.Fabrication/.api/api-clipper2.md` 214 LOC — still full bodies) demote to substrate pointers in this campaign as authorized 1-hop edits — never a second folder-tier copy.
- `Riok.Mapperly` + `Generator.Equals` (CONSUME — centrally pinned doctrine substrate, zero Compute consumers today): Mapperly owns the per-case proto<->domain field transcription at the `[V13]` `wire.md` boundary — the `oneof` envelope law: the union's generated total `Switch` dispatches, Mapperly transcribes the per-case fields the protobuf runtime does not (`SUBAPI:api-mapperly.md:3`) — and `[Equatable]` owns structural equality wherever a class-root shape surrenders Thinktecture's record-root equality (`SUBAPI:api-generator-equals.md:3`; `docs/stacks/csharp/shapes.md:312`); the folder admission rides the wire split, and hand-written transcription or `Equals`/`GetHashCode` where the generator reaches is the deleted form.
- `Microsoft.Data.Sqlite` (CONSUME — centrally pinned 10.0.9, folder admission only): the eplusout.sql tabular reader the energy page names as its own growth axis (`Analysis/energy.md:354`) — a read-only connection over the bracketed scratch artifact landing with the energy-runner rebuild; zero new central pins, absence/malformation stays `(Extraction, Foreign)`-railed per `[V2]`.
- `Microsoft.ML.OnnxRuntime` — the EP-context/config-key family stays reference-mined; the CUDA/DML append members remain catalog-recorded with zero rows until a deployment admits them (`[ROSTER_RECONCILIATION]` removes the packages).
- `TorchSharp`+`libtorch-cpu` — the `[CLASSICAL_ML_BLAS]` dual-leg stays the exemplar; no change.
- `csparse-interop` (ADD — catalog LANDED at `API:api-csparse-interop.md`, source vendoring at first compose; BSD-3 — `wo80/csparse-interop`, repo live and active): the `CSparse.Interop.ARPACK` `Arpack` driver (standard `A·x = lambda·x` and generalized `K·phi = lambda·M·phi` shift-invert Lanczos/Arnoldi over the CSparse `SparseMatrix`), `Cholmod` (supernodal Cholesky for SPD stiffness), `SuperLU` (unsymmetric LU), `Umfpack` — each `Factorize()`/`Solve(b)` `IDisposable` native factor, the native counterparts of the managed `SparseCholesky`/`SparseLU` terminals — rows per `[V9]`; osx-arm64 = SuiteSparse leg, provisioned through the Forge nix lane (nixpkgs `suitesparse`/`arpack` carry arm64 derivations); a native row without its asset faults at init.
- `HyperJet` (ADD — LANDED: `csproj:19`, `PROPS:112`, `API:api-hyperjet.md`; ISC, feed-verified 0.2.0: net10.0-only TFM, zero-dep, pre-1.0 surface; the nuspec repo pointer is dead, the live owner is `oberbichler/HyperJet`): `DDScalar1..15` source-generated stack-allocated hyperduals over `IFloatingPoint<T>` (`Value`, `G(int)`, `H(int,int)`), the `ref struct` `DDScalarSpan` for runtime-dynamic variable counts over a `stackalloc` buffer sized by `Kernel.GetDataLength(size, order)`, the dynamic heap `DDScalar`, `HyperJetMath` transcendentals, `GetGradient()`/`GetHessian()` MathNet export — the `[V10]` scalar-AD leg; the pre-1.0 pin is a recorded watch item.
- `cslsqp` (ADD-GATED, ISC, vendored from the live `oberbichler/cslsqp` until NuGet — feed absence re-confirmed): `SlsqpSolver` span-based SQP — the `OptimizerKind.slsqp` row; the vendoring law records on the page.
- `Microsoft.Z3` (ADD — LANDED: `csproj:33`, `PROPS:121` 4.12.2, `API:api-microsoft-z3.md`): ONE `Context` as AST factory and owning arena (`IDisposable`; every `Expr`/`Sort`/`Solver` minted by it), `Solver.Assert` over `BoolExpr` NRA/NIA assertions, `UnsatCore` naming the exact conflicting constraints, `Model` extraction — the `[V12]` rule-satisfaction owner; the feed stable ships win-x64/osx-x64 natives ONLY (nupkg-verified) so the osx-arm64 `libz3` provisions through the Forge nix lane (nixpkgs `z3` carries the arm64 derivation) — a `Context` operation without the provisioned native faults at init, never silently degrading.
- `AngouriMath` (REPLACE per `[V11]` — pin `PROPS:104` 1.4.0 and catalog `API:api-angourimath.md` staged; MIT, the net7.0 asset binds on net10): `Entity` algebra with implicit `Entity e = "..."` parse through `MathS.FromString` (the ANTLR front-end replacing the FParsec parser), `MathS.Variable`/typed builders, `MathS.Equations` multi-equation solve, `Solve`/`Integrate`/`Differentiate`/`Limit`, `Compile<>`, `Latexise` — nuspec deps exactly Antlr4.Runtime.Standard/GenericTensor/HonkSharp/PeterO.Numbers, all permissive; FParsec/FSharp.Core/MathNet.Numerics.FSharp leave with the swap; determinism source-settled at the pin per `[V11]`.
- `NWaves` (GATE, MIT, 2022-stale): Hilbert envelope / cepstrum / Goertzel as new `SpectralTransform` rows ONLY when a structural-health-monitoring/vibration route names them; a wholesale DSP swap is rejected — it would delete the `TensorRun` receipt/determinism integration.
- `Microsoft.ML.Probabilistic` (RECORDED-FUTURE, MIT, active — feed-verified 2025 release cadence): the Bayesian calibration/inverse-UQ owner (energy-model-to-metered-data, twin updating beyond scalar Kalman) — admitted only when its consumer page is chartered; never an `EstimatorKind` row.
- `PsychroLib` (RECORDED-FUTURE, MIT — feed-verified 2.5.0, repo MIT and unarchived): the ASHRAE moist-air property substrate the hygrothermal EN 15026 growth row (`[V12]`) will demand — admitted only when its consumer page is chartered, the same law as Infer.NET; `physics.md`'s realized EN/ISO closed-form kernel stays hand-owned and untouched.
- MINED-CONFIRMED (uncited-stub audit — every remaining domain-tier catalog verified against its realized consumer, zero action): `GeneticSharp` — the nsga2/RobustMinimax proposal kernel only, CMA-ES/PSO/SA stay owned folds (`API:api-geneticsharp.md:3`; `optimizer.md:1161`); `Silk.NET.WebGPU`+`Extensions.WGPU` — the ONE_WGPU_DEVICE WGSL compute-pipeline registry with `Wgpu` device-tick readback (`API:api-silk-webgpu.md:3`; `dispatch.md:792-799`); `Alimer.Bindings.MeshOptimizer` — index/vertex codec, meshlet build, and `SimplifyTarget` LOD all realized (`API:api-alimer-meshoptimizer.md:3`; `payload.md:87,152`); `Microsoft.IO.RecyclableMemoryStream` (`API:api-recyclable-stream.md:3`; four consumers), `Microsoft.Extensions.Caching.Hybrid` (`API:api-hybrid-cache.md:3`; three consumers), `Microsoft.ML.OnnxRuntimeGenAI` (`API:api-onnxruntimegenai.md:3`; `Model/generative.md`), `Microsoft.ML.OnnxRuntime.Extensions` (`API:api-onnx-extensions.md:3`; `Model/extension.md` — stays with the `[V13]` KEEP default), the `EC3` REST catalog (`API:api-ec3.md:3`; the lifecycle basis-tagged Normalize); `PollinationSDK` — LIVE (`csproj:45`, `README:80`, `PROPS:319` 1.10.0): the `EnergyRoute.Cloud` run-dispatch slice only (`API:api-pollination-sdk.md:3` — recipe job to a Pollination project, watched to completion, result handed to the Persistence durable landing), the DURABLE half (artifact bytes, lineage, cloud-run result index, `Wrapper` orchestration, `S3UploadRequest`) Persistence-owned at `Rasm.Persistence/.api/api-pollination-sdk.md`. Substrate-tier catalogs with no Compute lane (messagepack + thinktecture-messagepack, jsonpatch, redaction, unicolour, system-configuration, nodatime-stj, grpc-core-api) are out-of-lane single-tier files — no Compute row, no dedupe motion.
- REMOVE per `[ROSTER_RECONCILIATION]` — LANDED: the `FParsec`/`Microsoft.ML.OnnxRuntime.DirectML`/`.Gpu` direct references and the DirectML/Gpu catalogs are out. REMAINING: the orphan DirectML/Gpu central pins (`PROPS:117,119`); (with `[V11]`) `MathNet.Symbolics`+`MathNet.Numerics.FSharp` and the `FParsec` transitive-floor pin (`PROPS:109`); (with `[V8]`) `BriefFiniteElement.Net`+`BriefFiniteElementNet.CustomElements`+`FEALiTE2D`+`FEALiTE2D.Plotting` (`csproj:37-40`) — each with its catalog and README group row, plus their central transitive-floor pins (`BriefFiniteElementNet.Common` `PROPS:480`, `netDxf.netstandard` `PROPS:482`) per `[ROSTER_RECONCILIATION]`.
- REJECTED (adversarial floor, never re-proposed): CMAES.NET, Accord.NET, ML.NET, MSolve/MGroup (license fail), TetGen/Gmsh/CGAL (copyleft), Epanet.Net (GPL — algebra hand-rollable over the owned sparse solver), Triangle.NET (no clean NuGet; 2D-boundary-only value — record, revisit only on a named 2D quality-mesh consumer), commercial numerics suites, in-process ANN (Persistence owns retrieval indexes by contract), ILGPU (a second GPU compute rail beside the ONE_WGPU_DEVICE WGSL registry — the parallel-rail deleted form), ComputeSharp (DirectX12/HLSL — the osx-arm64 primary RID has no payload), CoordinateSharp (AGPL/commercial dual license — gate fail; the `[V12]` owned NREL-SPA solar kernel stands).
- Growth frontier: the native-BLAS design lands per `[V9]` with the Forge nix-lane provisioning recorded; the GeoArrow GLB wire stays RECORDED law, never re-minted (E15); the `[V12]` growth rows (MEP network-flow, room-acoustics, hygrothermal, Infer.NET calibration, seismic-route completion) stand on that verdict so the forward frontier is never implicit.

## [05]-[BUILD_ORDER]

Dependency order is content law — foundations before consumers, and within a stage the listed order IS dependency order. The four stages:

1. **GOVERNANCE + ROSTER FLOOR** — `[V14]` seam-ledger re-points (ledger-only; the stable-row do-not-churn list binds; the `ProgressStore` phantom row deletes) and `[V15]`+`[ROSTER_RECONCILIATION]` in one motion: the remaining manifest motions per the staging law (the unconditional motions are landed; criterion-gated and first-compose motions stay with their proving stages), the eight-pair catalog reconcile (verify `api-languageext`, the MathNet tier, the TestHost ruling), README/ARCHITECTURE codemap re-emit, E15 dispositions recorded. This stage gates every later page's imports and catalog claims.
2. **NUMERIC SPINE** — `Tensor/factor`+`blas` (native-direct + ARPACK rows, `[V9]`); `Tensor/dispatch` (hyperdual leg on `Sensitivity`, `[V10]`); `Solver/contract` (`[V6]` elasticity + key, beam/frame rows `[V8]`, sparse-eigen row `[V9]`, the `[V13]` CONSTITUTIVE extraction); `Solver/discretization` (`[V7]`); `Solver/optimizer`+`uncertainty` (slsqp row + exact-AD gradient row, `[V10]`); `Stats/estimator`+`signal` (FiniteJacobian dies; NWaves rows only if route-named); `Symbolic/` quartet (`[V11]` re-base; typed `CorrelationId` through units per E14). The `[V8]` beam rows and the BFE/FEALiTE manifest removal execute in this stage as one motion.
3. **ASSESSMENT SPINE + RUNNERS + WIRE** — `[V2]` typed-failure classification across the four runners with the `admission.md:173` custody-row rename in the same motion (structural additionally lands `[V5]` shear, the `[V4]` seam-read correction, the `FrameBackend` re-route onto the stage-2 beam rows, and the `[V12]` seismic response-spectrum route over the stage-2 sparse modal; energy additionally lands the `Microsoft.Data.Sqlite` eplusout.sql reader per `[04]`); `[V1]` spine rebuild on `Analysis/assessment.md` (lifecycle CacheFirst, Failed write-back, Sweep-over-JobGraph with the stale-marking/supersede/orphan-recovery duties, bounded Transient retry, ResultBlob sink threading through all four runners plus its ArtifactIndex landing); `Runtime/receipts` failure columns; the `[V13]` channels split landing WITH the `[V3]` band-complete widening and the `GaussianSplatScan` frame mint in the same motion, the Mapperly/Generator.Equals folder admission riding the wire split per `[04]`; `codecs.md` stays whole per `[V13]`. The `[V1]` rebuild discharges the Element seam's named Compute-consumer obligation; the `[V12]` circulation Element row is an authorized 1-hop edit landing with the stage-4 runner.
4. **COVERAGE + CLOSEOUT** — `Analysis/circulation.md` + `Analysis/daylight.md` + `Solver/satisfy.md` (`[V12]`), with the QuikGraph/NTS/Clipper2 folder admission (csproj rows per `[04]`; the substrate catalogs are landed) beside the runner that first composes them; `Model/` cold verification (`extension.md` KEEP default per `[V13]`); final ARCHITECTURE/README truth re-emit.

Closure law: `ARCHITECTURE.md`/`README.md` close with the stage that changes their facts; `.api` stubs and manifest edits land with the motion that admits, removes, or first composes the package, and the first compose deepens the stub; cross-package 1-hop counterpart edits are AUTHORIZED campaign work under libs-wide ripple authority, each bounded to its sealed shape — the Element `Circulation` row (`[V12]`), the Element `Provenance` attempt ordinal (`[V1]`c), the Persistence assessment `ArtifactKind` row (`[V1]`), the kernel coordinate-level predicate entries (`[V7]`), the kernel `CLASH_GOLDEN` derivation-law prose re-frame, and the Bim/Fabrication `.api` demotions to substrate pointers (`[04]`) — wider sibling interiors stay unedited; the Materials effective-section obligation is DISSOLVED (`[V4]` — already realized at the capacity owner). Campaign acceptance closes on composed contracts, not the ledger: (a) a failure-lifecycle dry-run — a deterministic structural failure persists ONE Failed `Node.Assessment` carrying `Diagnostic(phase, kind)`; re-`Assess` under CacheFirst returns the cached failure WITHOUT re-running; a `Transient`-kinded failure re-dispatches through the retry gate; the `Sweep` dispatches Pending/Stale and skips Queued/Running; a graph edit flips the drifted Computed row Stale through the marking fold and its re-solve supersedes it (exactly ONE usable row survives); a killed worker's Running row reconciles to Cancelled and re-dispatches; the fault crosses the wire as a `FaultDetail` row — composes entirely from rebuilt fences; (b) a numeric dry-run — a two-material fea-static reads per-node `(E, nu)` through `graph.PropertiesOf(id).Mechanical` (two distinct D tensors provably assembled); a >=10^4-DOF fea-modal solves through the ARPACK row; a near-coplanar boundary set meshes clean through the exact predicate floor; the frame parity fixtures hold on the owned beam rows; (c) a design-check dry-run — a linked RC member governs at `min(V_Rd,s, V_Rd,max)` consistent with the Materials seam values; a cold-formed member resolves through the seam-baked effective section with zero Materials references in the fence; (d) a coverage dry-run — an egress assessment (travel distance + max-flow exit capacity) and a daylight assessment (solar position + clash-BVH shadow rays) write `Node.Assessment` rows through the SAME spine as the settled runners (the egress dry-run composes the landed `circulation` row per `[V12]`), and a rule-set verdict returns an unsat-core naming the violated rules. A dry-run reaching for a missing owner reopens `[V1]`/`[V9]`/`[V12]`.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Materials` interiors: the `SectionProfile` overload dispatch is Materials-interior law, and the effective-section concern is already realized at the Materials capacity owner (`[V4]`); the Materials capacity/steel pages are read-only seam counterparts here.
- `Rasm.Element` interiors: the assessment lifecycle vocabulary is LANDED LAW consumed as-is; the two authorized 1-hop edits — the `Circulation` `Discipline` row (`[V12]`) and the `Provenance` attempt ordinal (`[V1]`c) — are the ONLY Element writes, each bounded to its sealed shape.
- `Rasm.Persistence` interiors and the LANDED `Rasm` kernel corpus (the one `libs/csharp/Rasm/.planning/` root): consumed as finalized law; the authorized 1-hop edits — the Persistence assessment `ArtifactKind` row (`[V1]`), the kernel coordinate-level predicate entries (`[V7]`), and the kernel `CLASH_GOLDEN` derivation-law prose re-frame — are the ONLY writes, each bounded to its sealed shape; everything else stays read-only.
- `Rasm.Bim` IFC semantics (the `IdsVerdict`/`IdsAudit.Reconcile` stay Bim-owned; codecs relays the wire only), AppUi `ResidencyManifest.Mint`, AppHost M.E.AI governance and drain conductor — settled port peers.
- Python companion lanes: model training, PCE offline fit, Radiance-class CBDM/glare — the C#-inference/Python-training law stands; the `[V12]` daylight runner owns geometry-derived facts only.
- The Python companion's SPZ/SOG scan DECODE and the `xxhash` cp315 wheel — sibling-branch facts (the geometry campaign and the Forge scientific lane); this campaign mints the `GaussianSplatScan` scan-buffer frame on the wire page (`[V13]`) and touches nothing companion-side.
- The native-BLAS/`csparse-interop`/`libz3` native assets — provisioned through the `Parametric_Forge` nix lane (nixpkgs arm64 derivations); the campaign lands the design and the fault-at-init law.
