# [RASM_MATERIALS_RULINGS]

`Rasm.Materials` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `SixLabors.ImageSharp` pins at the last unconditional Apache-2.0 major — the successor gates builds on a licence key; forgone EXR is the cost.
- `TinyEXR.NET` owns OpenEXR WHOLE — flat, tiled, deep, and multi-part alike, so one engine per CONTAINER keeps `RasterFormat` the only caller axis.
- `TextureCompressor` pins EXACT behind one `KtxGate` arm over the `ktx` CLI floor — a single-maintainer prerelease retires by deleting one arm.
- `Magick.NET-Q16-HDRI-AnyCPU` admits INGEST-ONLY — `RasterEngine.Breadth` carries no encoder column; a managed AVIF/HEIF/JXL/DPX set re-closes it.

## [02]-[SHAPE]

- Family pages carry VOCABULARY and ROWS alone — `ComponentSeed` owns the seed traverse and its gates, so a per-family seed body re-mints the spine.
- Grade identity is ONE `MaterialGrade` row over closed `GradeProperties` — a per-family owner forks the authority axis and re-opens the name clash.
- Code-REGISTERED characteristics cross `Published.Exact` — exactness is the zero-width band; an AUTHORED transcription carries a relative band.
- `SectionCapacity` cases cut by structural FAMILY and MODALITY — a code-named case forks the closed `GoverningAction`/`Utilisation` vocabulary.
- `DesignBasis` MEMBER keys share one KEY SET with `Rasm.Compute/Analysis/structural#DESIGN_CHECK` — a key minted alone forks the set.
- γM2 has ONE authority — the `DesignBasis` partial-factor column; a family-page copy is the deleted form, and a φ-format basis REFUSES the read.
- Glazing, connection, anchorage, and fatigue keys are the DECLARED section-and-load-path carve — no member-check counterpart exists or crosses.
- `Demand` MODALITY columns bind their OWN case — unit shear to `LateralPanel`, range/count to `Fatigue`; member arms neither resist nor read them.
- φ-format jurisdictions land WHOLE — proven ACI anchorage coefficients wait on the unproven anchor φ roster; a partial landing mis-prices verdicts.
- `ModelCard.LicenseClass` is EVIDENCE off the weight card — a repo licence resolves independently, so a silent card enters `Blocked` with no grant.
- `WeightPolicy` states redistribution and carries NO address — the app-root import boundary resolves bytes, so the registry stays vocabulary.
- Generative super-resolution admits as `base_color` ALONE via `ModelCard.PhysicalChannelForbidden` — invented detail elsewhere shades as measured.
- Text-to-material generation is an EXTERNAL-SERVICE seam minting no `PbrStage` — a local generator clearing `tile#TILE_SYNTH` coherence reopens it.
- `CieSkyType` rows the ISO 15469 gradation × indicatrix ALGEBRA — one five-parameter formula, so a national-annex sky is a row over the groups.
- Published constant tables with NO defining sequence stay content-keyed data assets — a table whose sequence exists transcribes as literals.
- `PbrStage` rows declare CONSUMES and EMITS alone — the plan derives every edge, so a predecessor list contradicting the graph is unrepresentable.
- `PriorField` rows carry each stage product no `TextureSet` holds — `delight` and `depth` drop at `Acquisition.Import`, never seating as measured.
- `IblProducts` are STORED-FRAME and `EnvironmentLight` applies rotation and intensity at read — baking either makes a re-exposure a re-bake.
- Tileability is EVIDENCE, never a flag — `TextureSet.Tiled` fills from the one `TileProof.Grade` mint as kernel `Evidence<TileProof>`.
- `set#SET_BIND` `Average` MEASURES every summary row — only a box fold's `1×1` tail is the mean, so a pyramid read fabricates the seam key.
- Every GPU preview covers its subject's FULL algebra or refuses at admission — a substituted basis shows a field the bake will not produce.
- WGSL bodies whose CPU twin exists TRANSCRIBE that twin's own members and constants — a second derivation is the deleted form even where both agree.
- WGSL constants DERIVE by interpolation off the CPU owner's declared consts — a re-typed literal is the drift the goldens cannot see.
- Codec `catch` arms inside an ordered format PROBE return `Option` absence — the ONE typed refusal lifts total decline onto the band rail.
- GPU uniforms cross as WORDS through one `KernelUniform` writer and each golden pins the kernel's OWN sum — `4f` in a `u32` word reads as a billion.
- GPU planes are never content-keyed — the veto is STRUCTURAL, since `f32` cannot reproduce the CPU `f64` lattice the content key takes as preimage.
- Tiling periodicity is a SOURCE property — `NoisePeriod` wraps the lattice and `tile#TILE_SYNTH` heals only what cannot; `Noise.Of` proves it whole.
- `KtxPayload` decides WIRE LEGALITY — no corpus transcoder reads `rawBcn`, so both `Of` mints refuse it and a desktop reader fetches by address.
- Spatial evidence enters as its own `TextureSource` field — `PressSubject.MeshSpace` carries chart texels as DATA, none landing at compile time.
- Press plan keys exclude subject-NAMING columns and admit subject-SHAPING ones — the discriminant is what a column does to the produced bytes.
- Synthesized sky planes carry the DIFFUSE field alone — the disc rides `EnvironmentSample.Sun`, so folding it into the dome double-counts the sun.
- Blend modes MaterialX cannot express cross as DECLARED lossy substitutions — authoring stays total, and the row names what the far end renders.
- Spectral absorption rides `LobeWeight.Throughput`, never the normalized weight — the cascade renormalizes, so a folded hue whitens the coat.
- Anisotropy DIRECTION is a scalar rotation column end to end — a Box mip over opposed tangent vectors cancels to zero, a rotation scalar averages.
- `EnvironmentMap` admits `linear`/`pq`/`hlg` alone and lifts one sampler PER LAYER — `srgb` carries no sun, and `AsImage` carries one layer.
- Substance catalogues never author an ASSEMBLY- or PRODUCT-level quantity — U-value is an EN ISO 6946 fold, so the honest form is stated absence.
- Cross-catalogue roster parity DERIVES from a type-init census over the owning rosters — an unenforced parity claim in prose is the defect.
- Weighted composition is DECLARED BY RECIPE at the capture root — `recipeOf` supplies a `ConstituentRecipe`, never a roster column inferring one.
- `AsceRcSystem`/`EnConcreteDuctility` are SYSTEM-level coefficient rows — Compute's spectrum divisor takes q or R as a SCALAR at composition.
- `Bake` is a name this folder does NOT take — `Rasm.Rhino` `TextureBake` and the seam fold hold it; generation is `TexturePress`/`Press` throughout.
- `SolarFrame` names the FRAME the sun adapter projects into, never the ephemeris — a `SolarPosition` twin forces qualified calls at every site.
- `gpu#PRESS_DEVICE` is the SOLE `Silk.NET.WebGPU` composer here — `press#TEXTURE_PRESS` names a `PressBackend` row, so a binding swap is one page.
- Inbound corpus contracts admit ONCE through a generated reader mirroring the required keys — tokens ride as TEXT and cross rosters at one admission.

## [03]-[COLLAPSE]

- `graph#MATERIAL_GRAPH` carries ONE evaluation rail — a per-point read re-enters `CompiledGraph.ShadeSpan` over a one-element window; a twin forks.
- `TexturePlane`'s extent spine is the kernel `CellLattice` at `Layers = 1`, the LAYER band its own — `Coarsen` halves six cube faces to three.
- Colour primaries are a plane axis SEPARATE from transfer, `unknown` the honest default — `ToPrimaries` alone converts, refusing what is undeclared.
- Hosek-Wilkie rides TWO content-keyed assets on one lattice algebra — sky and solar sets differ in RANK, so a shared digest re-bakes the whole dome.
- Emitted radiance travels BESIDE the `LayeredBsdf` as an emission field — a lobe rides the estimator's pdf, so an emission lobe double-weights.
- Geometric composition never re-cuts for a mass decomposition — a `Layered` row stays `LayerSet`, product structure and mixture being two truths.

## [04]-[STRUCTURE]

- Host geometry enters ONLY as a confined `[KERNEL_EDGE]` block where no host-free mint exists — confinement is a retirement queue, never a licence.

## [05]-[PROCESS]

- (none)
