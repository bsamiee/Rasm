# [PLATFORM_TASKLOG]

The folder's open and closed work, distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open; `[COMPLETE]`/`[DROPPED]` closed — plus three to four bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks; each names the exact sub-domain/file it lands in.

## [1]-[OPEN]

[QUEUED] Browser-capability owner at `capabilities/browser-capability.md` (from BROWSER_CAPABILITY_POLICY) — author the new `capabilities/` sub-domain and its `BrowserCapability` `Effect.Service`: one closed `CapabilityKind` `Schema.Literal` axis (`notifications`/`clipboard-read`/`clipboard-write`/`geolocation`/`persistent-storage`), one `PermissionState` `Data.TaggedEnum` (`Granted`/`Denied`/`Prompt`) per kind in a `SubscriptionRef`, the `query`/`request` total-dispatch entrypoint, and the `persist`/`estimate` persistent-storage grant.
- Integrate the admitted-but-uncomposed `@effect/platform-browser` `Permissions` (`query`), `Clipboard`, and `Geolocation` service capsules behind the one axis; compose `BrowserPlatform` for the platform binding; lift each `PermissionStatus.change` event through `runtime-composition/scoped-event-stream.md` `scopedEventStream` (a `PermissionStatus` is an `EventTarget` absent from `WindowEventMap`).
- Wire: the `persistent-storage` grant feeds `runtime-composition/capability-rank.md` as a fifth health input (a denied persist grant degrades toward `OfflineOnly` since the IndexedDB store risks eviction); `ui` reads the grant cell through the `AtomBinding` to grey a denied affordance, never importing `platform`; `local-persistence` reads the persist grant before relying on durable IndexedDB quota.
- Author the folder `.api/effect-platform-browser-capabilities.md` capturing the `Permissions`/`Clipboard`/`Geolocation` capsule surfaces (or extend the existing `effect-platform-browser.md` region), and the native `Permissions.query`/`PermissionStatus.change`/`StorageManager.persist`/`estimate` shapes; a native `navigator.notification`/`navigator.clipboard`/`navigator.storage` call outside this owner is the named ungated-native-call defect.

[QUEUED] Socket-transport modality at `realtime/socket-transport.md` (from SOCKET_TRANSPORT_MODALITY) — author the new `realtime/` sub-domain and its `SocketTransport` `Effect.Service`: one duplex `Channel` over the admitted `BrowserSocket.layerWebSocket`, the inbound frame `Stream` folded through `Schema.decodeUnknown`, the outbound write riding the one scoped resource, and the reconnect on the reused `projection` `StreamPolicy` shape.
- Integrate `@effect/platform-browser` `BrowserSocket.layerWebSocket`/`layerWebSocketConstructor` (the admitted-but-uncomposed duplex `Socket`); compose `RuntimeConfig` for the socket endpoint and `Effect.acquireRelease`/`Effect.forkScoped` for the connection lifetime; reuse the `projection` `StreamPolicy` reconnect rather than a bespoke retry.
- Wire: the `WebTransport`/socket modality `feature-flags/flag-stream.md` and `worker/decode-pool.md` each NAME as a growth leg lands as one transport case on this owner; a future presence/heartbeat cell and the cross-`libs/` CRDT op-log push-back compose this duplex channel as a modality; the decoded inbound frame obeys the same `Schema.decodeUnknown` law the SSE/wire ingresses do, never a parallel socket.
- Author the folder `.api/` region for `BrowserSocket` (extend `effect-platform-browser.md` `[BrowserSocket]`); a second ad-hoc `WebSocket` constructed outside this owner is the named parallel-socket defect.

[QUEUED] OIDC redirect-continuity leg in `identity-session/auth-session.md` (from OIDC_REDIRECT_CONTINUITY) — extend `AuthSession` with the PKCE authorization-redirect round-trip: mint `state`/`codeVerifier` through `arctic` `generateState`/`generateCodeVerifier`, persist the `{ state, codeVerifier, returnTo, nonce }` pending-flow record, complete the callback (`state` CSRF match, `OAuth2Client.validateAuthorizationCode`, `decodeIdToken` claims into the `SessionStatus.Authenticated` subject), and add `revokeToken` to `logout`.
- Integrate the admitted `arctic` `OAuth2Client` (`createAuthorizationURLWithPKCE`/`validateAuthorizationCode`/`refreshAccessToken`/`revokeToken`), `generateState`/`generateCodeVerifier`, `decodeIdToken`, and `OAuth2RequestError` (the `.code` RFC-6749 reason folding to the typed `AuthFault` `reason` arm) — the surface PKCE acquisition + refresh alone leaves unexploited.
- Wire: persist the pending-flow record through the `local-persistence` `auth-flow` `StoreDomain` named store (single-entry, cleared on completion); the `returnTo` drives the post-login `routing` `AppRouter` push; the callback ingress is a `routing` `RouteKey` (`login` callback arm) the router resolves, never a second navigation surface; the `OAuth2RequestError` folds to the existing `AuthFault` rail, never a console error.
- Key consideration: the `codeVerifier` is `Redacted` in the persisted record and never logged; the cross-origin Bearer/DPoP growth rows on the page stay designed-only; the `@simplewebauthn/browser` second-factor leg is unchanged.

[QUEUED] Local-store topology in `local-persistence/local-persistence.md` (from LOCAL_STORE_TOPOLOGY) — extend `LocalPersistence` with one `StoreDomain` `Schema.Literal` axis (`snapshot`/`offline-queue`/`auth-flow`/`viewpoint-cache`) each mapped to its own named backing store, and convert the offline-queue drain to one atomic `entries`-then-`clear` transaction.
- Integrate the admitted-but-uncomposed `idb-keyval` `createStore` (per-domain `dbName`+`storeName` isolation), `setMany`/`getMany` (atomic bulk), `entries`/`keys` (cursor drain), and `update` (read-modify-write) under the `@effect/platform-browser` `KeyValueStore`/`BrowserKeyValueStore` abstraction — never a hand-rolled `idb-keyval` call, the abstraction composing the named stores.
- Wire: the `auth-flow` store backs the `OIDC_REDIRECT_CONTINUITY` pending-flow record; the `viewpoint-cache` store backs the `worker/decode-pool.md` residency-manifest last-good; `offline-cache/background-sync-replay.md` consumes the atomic `drainOnRedial` over the `offline-queue` store verbatim, so a mid-drain crash cannot half-apply.
- Update the folder `.api/idb-keyval.md` `[LOCAL_ADMISSION]` to record the named-store + atomic-bulk usage law (the catalogue already carries the surface); a key-prefix convention in one flat store is the named flat-store defect.

[QUEUED] W3C trace-context continuation in `observability/metric-registry.md` (from TRACE_CONTINUATION_EDGE, realizing cross-`libs/` ONE_DISTRIBUTED_TRACE) — extend `MetricRegistry` to register the `W3CTraceContextPropagator`/`W3CBaggagePropagator` composite as the `Propagators.DefaultTextMapPropagator` on the `WebSdk` binding, replacing the independent-root-only sampler with parent-context continuation.
- Integrate `@opentelemetry/core` `W3CTraceContextPropagator`/`CompositePropagator`/`propagation` (verify the `@effect/opentelemetry` `WebSdk` propagator-registration seam against the `.api`); the `ParentBasedSampler` row stays as the root-sampling decision under the now-propagated parent.
- Wire: the `interchange` `WireTransport` interceptor injects the active span's `traceparent`/`tracestate` onto every outbound grpc-web/HTTP request header (the inject leg lives at the transport-owning folder, this owner registers the propagator); an inbound C#-minted parent seeds the browser span rather than a fresh root, so the `crash.report`/`web.vital.breach`/`session.replay` spans hang under the one cross-runtime trace id; the C# `csharp:Rasm.AppHost/observability/diagnostics-and-telemetry#CORRELATION_SPINE` owns the propagation fold, the browser extracts-and-continues at the wire, and a second tracer/fresh root in this peer is the named cross-language drift defect.
- Author or extend the folder `.api/` for the `@opentelemetry/core` propagator surface and the `@effect/opentelemetry` `WebSdk` propagator field; the manifest-row delta for `@opentelemetry/core` returns to the orchestrator.

[QUEUED] SSE parser hardening in `feature-flags/flag-stream.md` (from STREAMING_FLAG_DELIVERY re-open) — replace the hand-rolled native `EventSource` `message`-listener `JSON.parse` with the admitted `@effect/experimental` `Sse` `makeParser`/`makeChannel` SSE-frame decode and the `Retry.lastEventId` reattach cursor.
- Integrate `@effect/experimental` `Sse` (`makeParser(onParse)` over the raw `MessageEvent` data, the `Event`/`Retry` `AnyEvent` union, `Retry.lastEventId` for the reconnect cursor) — a branch cross-cutting admission now that `platform` is its second consumer beside the `services` `agent/mcp-transport#MCP_TRANSPORT` leg; the `FlagDelta` `Schema.decodeUnknown` fold and the in-place `RemoteConfig.flags` patch are unchanged.
- Wire: the `Retry.lastEventId` cursor reattaches the stream after a reconnect so a delta missed during an outage backfills by event id rather than only the demoted poll; the reconnect rides the reused `projection` `StreamPolicy` shape; the `scopedEventStream` ingress is retained for the raw `MessageEvent` source the parser consumes.
- Key consideration: the parser is a transport-decode mechanic, not a second flag source — the `services`-owned bucket/variant vocabulary and the deterministic local bucket stay untouched; the `Sse` surface is verified against the catalogue `[13]-[SSE]` section, which promotes from the `services` folder `.api/` to the branch `libs/typescript/.api/` once this second consumer lands (orchestrator move — see residuals).

## [2]-[CLOSED]

[COMPLETE] Page-lifecycle spine in `runtime-composition/app-lifecycle.md` — the closed `Phase` `Data.TaggedEnum` over the merged `visibilitychange`/`pagehide`(`persisted`)/`beforeunload` `Stream.merge` fold in one `SubscriptionRef`, the `transitions` edge projection, the `LifecycleSignal` arm fold; the three private `visibilitychange` ingresses retired.

[COMPLETE] Capability-rank cell in `runtime-composition/capability-rank.md` — the ordered `Rank` `as const satisfies Record` vocabulary, the four-input `Order.max` worst-of candidate fold, the escalate-fast/recover-slow `advance` hysteresis, and the `retains` capability query; PROJECTS capability, holds no behavior.

[COMPLETE] Generic scoped-event-stream bridge in `runtime-composition/scoped-event-stream.md` — one `scopedEventStream` `Stream.asyncScoped` over the `Effect.acquireRelease` listener pair, the single non-`WindowEventMap` ingress the navigate/`Workbox`/`PerformanceObserver`/`EventSource`/`MutationObserver` sources compose.

[COMPLETE] Connectivity owner in `connectivity/connectivity.md` — the one `online` `SubscriptionRef` merged fold, the `redials` rising-edge projection, and the native `SyncManager` `rasm-offline-drain` wake; four concerns read the one cell, no per-component `navigator.onLine` probe.

[COMPLETE] Decode pool relocated to `worker/decode-pool.md` — the transferable `Worker.makePoolSerialized` `DecodeRequest` family, the `DecodeResidency` case, and the worker-interior `hash-wasm` `createXXHash128` single content-key mint; `build-pipeline.md` is build-time-only, the spawner repointed to `../worker/decode.worker.ts`. Residency-wire shape stays RESEARCH-gated on `ONE_CONTENT_IDENTITY` + C# `SPLAT_REALITY_CAPTURE`.

[COMPLETE] Boot entry + host-owner wiring in `runtime-composition/composition-root.md` — `BrowserRuntime.runMain` replaced `runtime.runFork`; `AppLifecycle`/`CapabilityRank`/`Connectivity`/`DecodeWorkerPool`/`SessionRecorder` merged as platform-bound HOST owner bands; the closed five-app-service budget holds. The boot arms `connectivity.registerNativeSync` and the flag-gated session recorder.

[COMPLETE] HTTP-claim repair in `runtime-composition/browser-platform.md` — `FetchHttpClient.layer` serves `RemoteConfig` config fetch and OTLP exporters, never `interchange` (connect-web transport); spawner repointed.

[COMPLETE] OTLP exporters + trace Sampler in `observability/metric-registry.md` — `WebSdk.layer` over the required `resource`, `BatchSpanProcessor`/`OTLPTraceExporter`, `PeriodicExportingMetricReader`/`OTLPMetricExporter`, and the one `tracerConfig.sampler` `ParentBasedSampler`(`TraceIdRatioBasedSampler`) row reading the `RuntimeConfig` collector endpoint. The `session.replay` span added to the closed `spanVocabulary`. RE-OPENED for trace-context propagation — see `[1]-[OPEN]` W3C trace-context continuation.

[COMPLETE] Flag-stream promoted to REAL in `feature-flags/flag-stream.md` — the native `EventSource` SSE channel through `scopedEventStream` into the `FlagDelta` `Schema.decodeUnknown` fold patching the one `RemoteConfig` `FlagSet` cell in place; the `remote-config.md` poll demoted to the backfill fallback under one `Schedule`. RE-OPENED for the `@effect/experimental` `Sse` parser — see `[1]-[OPEN]` SSE parser hardening.

[COMPLETE] Router re-founded on the Navigation API in `routing/app-router.md` — the scoped `navigate`-event interception over `window.navigation`, `event.intercept` `scroll: "after-transition"`, and the `NavigateEvent` `signal` pending/abort state replaced the `popstate` cell + manual history mutation + scroll-offset map; `AppRouter` Tag + `AppRouterLive` Layer exported; ZERO router package.

[COMPLETE] View-transition fold in `routing/app-router.md#VIEW_TRANSITION` — the `viewTransition` `Effect.acquireRelease` over `document.startViewTransition` (commit in the update callback, `ready`/`updateCallbackDone`/`finished` via `Effect.tryPromise`, `skipTransition()` on scope exit) gated by one `matchMedia` reduced-motion row; wired into the intercept handler; ZERO animation dependency.

[COMPLETE] Soft-nav web-vitals reset + flush in `web-vitals/performance-budget.md` — the CLS/INP accumulators and `(vital, route)` dedupe hoisted to service-level `Ref`s, reset on the `AppRouter` `location.changes` commit, flushed terminal on the `AppLifecycle` `transitions` `Hidden`/`Draining` edge with a final breach check; the breach span carries the `SessionRecorder` `replayWindow` correlation.

[COMPLETE] Session-replay recorder authored at `session-replay/session-recorder.md` — the flag-gated (`RemoteConfig` `session-replay`) sampled (`RuntimeConfig.replaySampleRate`) `MutationObserver`/interaction recorder over `scopedEventStream`, the redaction-at-capture `redactEvent` fold, the bounded `Ref<Chunk<RecordedEvent>>` ring, the `session.replay` flush span, and the `windowId` cell `crash.report`/`web.vital.breach` annotate; `FlagEvaluation` exported, `replaySampleRate` added to `RuntimeConfig`, `session-replay` flag added to `FlagKey`. No vendor SDK, no third telemetry path.

[COMPLETE] Live acceptance signals carried on `offline-cache`, `fault-capture`, and `web-vitals` — the SW install/activate/`skipWaiting` + redial-drain convergence on `offline-cache`, the uncaught-`throw`/rejected-`Promise` round-trip into the one `crash.report` span (now `replayWindow`-correlated, recorder-flushed) on `fault-capture`, and the native `PerformanceObserver` LCP/INP/CLS/TTFB/FCP attribution with the 40ms INP floor and soft-nav reset on `web-vitals`.
