# [FABRICATION_TASKLOG]

The open and closed work for `Rasm.Fabrication`, distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed — plus `Capability`, `Shape`, `Unlocks`, `Anchors`, and optional `Tension` fields.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[NFP_DRL_POLICY]-[BLOCKED]: learned nesting remains a policy score column, not a second packer.
- Capability: learned placement ranking for `Nesting/nfp#NESTING` over the existing no-fit-polygon placement primitive.
- Shape: `NestPolicy.Score` carries the injected `Func<NoFitPolygon, PartTransform, double>` delegate; the app-platform consumer runs `Rasm.Compute/Model/inference#INFERENCE_MODES` `RunOps.Infer` and passes the scalar score while Fabrication keeps the bottom-left/genetic folds, `NoFitPolygon.Of`, and the `Stock`/`Remnant` feasibility set.
- Unlocks: higher utilization on irregular sheets and remnants after a trained ranking model exists, with phase 1 still shipping the Geometry2D-routed NFP and deterministic/genetic heuristics.
- Anchors: `Nesting/nfp#NESTING`, `NestPolicy`, `NoFitPolygon`, `PartTransform`, `Polygon/clipper#POLYGON_ALGEBRA`, `Rasm.Compute/Model/inference#INFERENCE_MODES`, and `RunOps.Infer`.
- Tension: the trained placement-ranking model asset and consumer-side inference wiring are outside this folder's scope, and the AEC-domain strata boundary forbids a Fabrication reference to `Rasm.Compute`.
- Ripple: realizes the `[DRL_NEST_POLICY]` idea in this folder's `IDEAS.md`.

[CSG_WATERTIGHT_SILHOUETTE]-[BLOCKED]: watertight-solid silhouettes compose the kernel arrangement owner, not in-folder CSG.
- Capability: exact boolean-solid outline production for `Posting/projection#PROJECTION_HIDDEN_LINE`.
- Shape: the silhouette arm reads `Rasm.Geometry/Meshing/arrangement#ARRANGEMENT` through the realized `Arrangement.Apply` / `ToMesh` seam over `MeshBoolean`/`PlanarOverlay`/`CellComplex`, with `Meshing/delaunay#TESSELLATION` and `Meshing/offset#STRAIGHT_SKELETON` supplying the managed exact arrangement substrate.
- Unlocks: drafted outlines for boolean-combined watertight solids while the existing per-facet HLR kernel remains the pure-managed default for ordinary projection.
- Anchors: `Posting/projection#PROJECTION_HIDDEN_LINE`, `Rasm.Geometry/Meshing/arrangement#ARRANGEMENT`, `Arrangement.Apply`, `ToMesh`, `Meshing/delaunay#TESSELLATION`, `Meshing/offset#STRAIGHT_SKELETON`, and `ROBUST_ARRANGEMENT_SUBSTRATE`.
- Tension: the realized C# arrangement owner must land before this arm opens; Fabrication never authors a CSG kernel, admits a native CSG asset, or couples to `ArrangementStore` / `SimplexStore` internals.
- Ripple: realizes the `[CSG_SILHOUETTE]` idea in this folder's `IDEAS.md`.

[TOOLPATH_STRATEGY_MODALITY_FACTOR]-[QUEUED]: Factor the flat 11-row ToolpathKind SmartEnum into a CutStrategy x RemovalModality cross-product so a toolpath strategy is process-agnostic and the modality-bound rows (turn-rough/finish/face/groove/thread, thermal-contour, slice-layer) stop masquerading as strategy siblings, collapsing the strategy/modality conflation the page bakes into one axis.
- Capability: ToolpathKind (motion.md lines 40-52) flattens orthogonal concerns: contour/pocket/drill/trochoidal are genuine STRATEGY rows but turn-rough/turn-finish/face/groove/thread encode turning MODALITY, thermal-contour thermal modality, slice-layer additive modality. The factoring splits ToolpathKind into a CutStrategy SmartEnum (boundary-pass/pocket-clear/peck/adaptive/radial-sweep/plunge-dwell/helical/layer-walk) and reads RemovalModality from the Process row already carried on FabricationInput, so Cam.Solve dispatches the (RemovalModality, CutStrategy) pair through one generated total Switch where a strategy lands once and the modality selects its envelope, carrying an Admits relation routing an inadmissible pair to FabricationFault.
- Shape: An in-place collapse of motion.md CAM_MOTION: ToolpathKind's 11 rows fold to a CutStrategy axis plus the (RemovalModality, CutStrategy) Generate dispatch reading Process.RemovalModality off FabricationInput, FabricationPolicy.Cam carrying CutStrategy in place of ToolpathKind; the milling four stay strategy rows, turn-rough/finish/face/groove/thread collapse onto radial-sweep/plunge-dwell/helical modality-selected, thermal-contour collapses into boundary-pass on a thermal modality, slice-layer into layer-walk on an additive modality.
- Unlocks: A new process reusing an existing engagement pattern adds zero ToolpathKind rows (a routing contour and a thermal contour are one boundary-pass strategy the modality envelopes differ on), Cam.Generate shrinks from 11 modality-tangled arms to a strategy x modality table, and a new-modality strategy enters as one CutStrategy row read across every modality.
- Anchors: Toolpath/motion#CAM_MOTION ToolpathKind/Cam.Generate (11-row SmartEnum lines 40-52, Generate Switch lines 75-88); Process/family#PROCESS_FAMILY Process.RemovalModality the dispatch already carries; Process/physics#CUT_PARAMETER RemovalBudget per modality; Process/owner#FABRICATION_OWNER FabricationPolicy.Cam
- Tension: The (RemovalModality, CutStrategy) cross-product is partial — turning's radial-sweep is meaningless on a cartesian-gantry laser; the Generate dispatch routes an inadmissible pair to FabricationFault.InadmissiblePair rather than a silent empty move set, so the factoring carries an Admits relation the way Machine.Admits gates the process set.

[POST_DIALECT_OVERRIDE_SEAM]-[QUEUED]: Author the independent PostDialect override seam so a job posts to any controller dialect decoupled from the Process row's constructor-bound dialect default, settling the override carrier the family page names but never shapes and the posting page reads as PostPolicy.Dialect with no admission path.
- Capability: Process.Dialect (family.md PostDialect rows 64-75, Process column line 91) is the constructor-bound default but has no settled override CARRIER: PostPolicy.Dialect is a bare field fed from PostPolicy.Canonical (program.md line 86) with no admission boundary and FabricationInput carries no dialect override, so a mill row is welded to linuxcnc and a fanuc shop reposting the same toolpath has no seam. The override seam adds an Option<PostDialect> dialect override on FabricationInput resolved against Process.Dialect at posting, plus a span-keyed PostDialect.Admit boundary routing GeometryFault.DegenerateInput on an unknown dialect key the way ProcessFamily.Admit gates the process axis.
- Shape: Edits to family.md (Process.Dialect re-stated as the FALLBACK the override resolves against), program.md (PostPolicy.Dialect fed from a FabricationInput Option<PostDialect> override resolved at Posting.Post, plus the PostDialect.Admit span-keyed boundary), and the FabricationInput shape on owner.md gaining the dialect-override field beside the settled Process/Machine/Stock state — one resolution fold, never a second dialect enum.
- Unlocks: One toolpath posts to grbl, fanuc, or linuxcnc by an input override without re-selecting the Process, a shop's controller fleet is policy data not a Process constructor weld, and the RS-274-vs-Marlin family column gates the override admission.
- Anchors: Process/family#PROCESS_FAMILY Process.Dialect (PostDialect rows 64-75, Process column line 91); Posting/program#CUT_PROGRAM PostPolicy.Dialect (line 86)/Posting.Post (line 115); Process/owner#FABRICATION_OWNER FabricationInput (lines 65-73); ProcessFamily.Admit span-keyed precedent (family.md lines 116-124)
- Tension: An override must validate modality compatibility — a thermal Process overridden onto a Marlin (additive RS-274=false) dialect is incoherent; the resolution fold gates the override against the Process.RemovalModality the dialect family column admits. The FabricationInput growth co-lands with the GUARD keep/remove split and the NEST_MULTISHEET Seq<Stock> change — one input-shape edit on owner.md.

[FABRICATION_FAULT_BAND_DEEPENING]-[QUEUED]: Deepen the thin four-arm FabricationFault band with the modality-specific and safety failures the widened process axis and new sub-domains produce — an inadmissible process-strategy pair, a swept-tool Gouge, a holder Collision, a non-convergent slice contour, a stock-envelope overflow — so every kernel routes a typed fabrication fault instead of degrading an out-of-band concern onto GeometryFault.DegenerateInput or overloading KerfCollision.
- Capability: FabricationFault is a four-arm band (NoFit 2501/Unreachable 2502/KerfCollision 2503/OpenLoop 2504, faults.md lines 35-45); the strategy x modality factoring, GUARD, and the additive slicer produce fabrication-specific failures with no typed arm. The deepening adds InadmissiblePair 2505, Gouge 2506, Collision 2507, NonManifoldSlice 2508, StockOverflow 2509 carrying their next-ordinal band-2500 codes, the kernel GeometryFault still owning only the shared degenerate/non-finite primitive set.
- Shape: An in-place widening of faults.md FAULT_BAND: new FabricationFault arms each carrying its Detail and next-ordinal Code, the Code/Message Switch extended to the new ordinals, the Cases list growing past four, the kernel DegenerateInput composition unchanged — no new band, the fabrication failures stay typed per concern, and the Auto section names TOOLPATH_STRATEGY_MODALITY_FACTOR / GUARD / SLICING / NESTING as the producers.
- Unlocks: The strategy x modality factoring routes an inadmissible pair to InadmissiblePair rather than an empty move set, GUARD routes Gouge and Collision through Fin<T> instead of cutting through a clamp, the slicer routes NonManifoldSlice the additive emitter reads, and the nesting kernel distinguishes a true NoFit from a StockOverflow.
- Anchors: Process/faults#FAULT_BAND FabricationFault [Union]/Code/ToError (4 arms lines 35-45); Toolpath/motion#CAM_MOTION the strategy x modality dispatch; Toolpath/guard#GUARD the Gouge/Collision producer; Toolpath/slicing#SLICING the section contract; Nesting/nfp#NESTING the Stock envelope; Thinktecture [Union]; LanguageExt Error/Fin; realizes the `GUARD` idea.
- Tension: Each new arm must be a genuinely fabrication-specific concern, not a re-cased geometry failure — a non-finite slice vertex stays GeometryFault.DegenerateInput; the deepening adds only the contract-level fabrication rejections the kernel union never declares, and Gouge/Collision stay distinct from KerfCollision (the kerf-offset collapse).

[TOOL_CUTTING_DATA_TABLE]-[QUEUED]: Process/physics#CUT_PARAMETER grows the Tool axis from the bare (diameter, flutes) pair into a full tooling payload — coating, corner-radius, helix-angle, projected-stickout, runout — and replaces the single hardcoded SurfaceSpeed fallback with a frozen (Material, Tool, Operation)-keyed cutting-data table projecting SFM/RPM and chip-load per cell, so SubtractiveBudget reads measured cutting data instead of the one 90.0 m/min constant.
- Capability: Realizes the SFM-RPM/chip-load lookup by (material, tool, operation) the prompt names absent: each Tool row (physics.md lines 36-49) gains Coating [SmartEnum] (uncoated/TiN/TiAlN/diamond), CornerRadius, HelixAngle, Stickout, Runout columns; a frozen FrozenDictionary<(string,string,string),(double Sfm,double ChipLoad)> CuttingData keyed by the triple supplies the surface speed and chip load SubtractiveBudget reads, defeating the inline `material.Physics is Subtractive sub ? sub.SurfaceSpeed : 90.0` fallback (physics.md line 114) with a real per-cell row consulted before the formula. The CuttingData FrozenDictionary rides BESIDE the existing empty Overrides FrozenDictionary (physics.md lines 99-100), never a parallel ToolMaterialTable.
- Shape: An expansion of Process/physics CUT_PARAMETER: the Tool [SmartEnum<string>] gains Coating/CornerRadius/HelixAngle/Stickout/Runout behavior columns; a static FrozenDictionary CuttingData SubtractiveBudget reads keyed by (Material, Tool, Operation); the Boundary law gains a 'cutting data is the (M,T,O) table cell, never a per-Tool scalar fallback' row; the cross-product table widens one column not one new owner, the Stickout column feeding MAGAZINE holder geometry and POST_THERMAL_DEFLECTION_COMP bending stiffness.
- Unlocks: Removes the single-surface-speed working form from SubtractiveBudget so a TiAlN-coated endmill in stainless reads its own SFM/chip-load cell, and gives the trochoidal HEM engagement limiting a real per-tool chip-load to hold constant chip thickness against.
- Anchors: Process/physics#CUT_PARAMETER Tool/Operation/Material/RemovalParameter.SubtractiveBudget (Tool 9-row SmartEnum lines 36-49, fallback line 114, Overrides table lines 99-100); Thinktecture [SmartEnum<string>] payload columns + System.Collections.Frozen; Toolpath/motion#CAM_MOTION trochoidal chip-load read; MAGAZINE Stickout consumer; POST_THERMAL_DEFLECTION_COMP stiffness consumer; realizes the `MAGAZINE` idea.
- Tension: A per-(material,tool,operation) cutting-data table is a second cross-product inside the existing (process,material,tool,operation) Overrides table — it must ride the frozen CuttingData table or the Tool payload column, never a parallel ToolMaterialTable sibling the density law forbids.

[TROCHOIDAL_ENGAGEMENT_LIMIT]-[QUEUED]: Toolpath/motion#CAM_MOTION grows the trochoidal arm from a fixed-step medial-axis march into a true HEM engagement controller — the radial step and trochoidal arc radius vary along the skeleton to hold constant radial engagement angle and constant chip thickness, the axial depth capped by the tool stickout — so the adaptive-clearing toolpath holds the constant-MRR contract the page claims but the fixed len/stepOver Engage walk does not deliver.
- Capability: Closes the axial/radial engagement-limiting gap for trochoidal HEM: the Engage fold (motion.md Engage lines 129-136, currently a uniform len/stepOver linear subdivision ignoring channel width) reads the local medial-axis clearance radius (from SKELETON_CLEARANCE_FIELD) and the SubtractiveBudget MRR ceiling to compute the radial step holding constant engagement angle, caps the axial depth at the tool stickout-derived limit, and emits the variable-radius circular arcs (G2/G3) the constant-chip HEM strategy needs.
- Shape: An expansion of Toolpath/motion CAM_MOTION Trochoidal/Engage: the Engage arm reads the StraightSkeleton clearance radius per axis segment and the RemovalBudget.Subtractive MRR to size each trochoidal loop holding constant engagement angle; an EngagementPolicy column on the Cam policy carrying the target engagement angle and the max axial depth; the emitted moves become variable-radius arc moves the biarc emission renders as G2/G3; the Boundary law gains a 'radial step holds constant engagement, never a uniform stepOver' row.
- Unlocks: Turns the trochoidal generator into the constant-MRR HEM the motion page advertises, and feeds GUARD a real per-loop swept radius instead of a point march, so the engagement-limited path is both productive and safe.
- Anchors: Toolpath/motion#CAM_MOTION Trochoidal/Engage (lines 124-136); Toolpath/skeleton#STRAIGHT_SKELETON MedialAxis clearance radius; Process/physics#CUT_PARAMETER SubtractiveBudget.MaterialRemovalRate + chip-load; BIARC_ARC_EMISSION arc moves; TOOL_CUTTING_DATA_TABLE chip-load; SKELETON_CLEARANCE_FIELD
- Tension: Holding constant engagement needs the exact local channel half-width per point, which the wavefront propagation already encodes but the skeleton page discards — this task depends on SKELETON_CLEARANCE_FIELD exposing the clearance radius rather than re-deriving a distance transform.

[NEST_MULTISHEET_SCHEDULE]-[QUEUED]: Nesting/nfp#NESTING grows Nest.Solve from a single-stock placement into a multi-sheet/multi-stock scheduler — parts that do not fit the first stock spill onto the next stock in a supplied stock list, the remnant of each consumed sheet re-enters the feasibility set, and the receipt carries the per-sheet placement partition — so the nest packs a part batch across a heterogeneous stock inventory the way professional sheet-metal nesting schedules a cut list.
- Capability: Closes the multi-sheet/multi-stock scheduling gap: Nest.Solve (nfp.md Solve takes one input.Stock, line 128) reads a Seq<Stock> inventory instead of one Stock, folds the unplaced parts from sheet k onto sheet k+1, mints each consumed sheet's remnant back into the inventory tail, and returns a Placement partition keyed by sheet index; the genetic decode scores total utilization across all consumed sheets. The RECTPACK_FASTPATH rectangle arm and the NEST_REMNANT_LINEAGE remnant both ride this same multi-sheet fold.
- Shape: An expansion of Nesting/nfp NESTING Nest.Solve: the FabricationInput Stock field (owner.md line 71) widens to a Seq<Stock> inventory; the placement fold becomes a sheet-by-sheet greedy spilling unplaced parts forward, each FabricationResult.Placement gaining a SheetIndex per PartTransform; the genetic Utilization scores the cross-sheet sum; the Boundary law gains a 'multi-sheet is one fold over the stock inventory, never a per-sheet Solve call' row.
- Unlocks: Turns the nest from a one-sheet packer into a cut-list scheduler consuming a real stock inventory and tracking which part lands on which sheet, the partition Posting needs to emit one program per sheet.
- Anchors: Nesting/nfp#NESTING Nest.Solve/Stock union/Utilization (Solve lines 117-134, Stock union lines 58-97); Process/owner#FABRICATION_OWNER FabricationInput.Stock (line 71) + FabricationResult.Placement (line 90); RECTPACK_FASTPATH rectangle arm; HASHING_NFP_MEMO NFP reuse; NEST_REMNANT_LINEAGE remnant producer
- Tension: FabricationInput today carries one Stock (owner.md line 71); widening to Seq<Stock> ripples the owner.md FabricationInput shape, so the multi-sheet task, the dialect-override task, and the GUARD keep/remove split all touch the same FabricationInput record and must land the input-shape change once.

[NEST_REMNANT_LINEAGE]-[QUEUED]: Nesting/nfp#NESTING grows a remnant-tracking arm subtracting the placed-part outlines (kerf-inflated) from the consumed stock through the Polygon/clipper Boolean Difference and minting the leftover region as a new content-keyed Remnant, so a nest run produces the next run's remnant inventory with a lineage hash linking child remnant to parent stock — the remnant-tracking depth beyond the existing FromRemnant case.
- Capability: Closes the remnant-tracking gap: after placement, Nest.Solve folds the kerf-inflated placed outlines against the stock boundary through the Clipper2 Difference (ClipOp.Difference, the Clip operation clipper.md exposes), takes each resulting region as a candidate Remnant, content-addresses it through the existing XxHash128 mint (Remnant.Of nfp.md lines 42-52), and carries the parent-stock identity as a lineage column — so a partially-consumed sheet yields its real leftover polygon (not a virgin rectangle) and the remnant inventory grows automatically across runs.
- Shape: An expansion of Nesting/nfp NESTING: a Remnant.From(stock, placedOutlines, kerf) projection over PolygonAlgebra Difference minting the leftover region(s); the Remnant record (lines 41-56) gains a Parent UInt128 lineage column beside its Identity; FabricationResult.Placement gains the produced Remnant set; the Boundary law gains a 'remnant is the kerf-inflated Boolean difference, never an axis-aligned leftover rectangle' row.
- Unlocks: Closes the loop from a finished nest to the next nest's stock inventory — the leftover of cutting sheet A becomes a real Remnant the NEST_MULTISHEET_SCHEDULE inventory carries forward, with a lineage hash auditing which stock a remnant descends from.
- Anchors: Nesting/nfp#NESTING Remnant.Of/Stock.FromRemnant/Utilization (Remnant lines 41-56, FromRemnant line 68); Polygon/clipper#POLYGON_ALGEBRA Clip Difference (ClipOp.Difference) + Offset for kerf inflation; System.IO.Hashing XxHash128 (the existing mint, the lineage column); NEST_MULTISHEET_SCHEDULE inventory consumer
- Tension: The Boolean difference yields multiple disjoint leftover regions; each must mint its own content-keyed Remnant carrying the same parent lineage, so the producer is a fold over the difference result regions, not a single remnant.

[RECTPACK_FASTPATH_ADMISSION]-[QUEUED]: RectpackSharp admits as the rectangle bin-packing fast-path arm on the Nesting Stock placement fold, owning the guillotine/maxrects heuristics the hand-rolled bottom-left/genetic author-kernel cannot match for axis-aligned planar stock, while the NFP true-shape kernel stays the irregular-part owner and never displaces the BarStock/TubeStock/Remnant true-shape cases.
- Capability: Nesting/nfp#NESTING gains a maxrects/guillotine rectangle-packing placement mode for the planar Stock cases (Sheet/Plate/Billet, nfp.md union lines 62-66) keyed off the NestPolicy placement discriminant, integrated as one Nest placement arm over the existing PartTransform receipt rather than a parallel packer class; the single-bin Pack signature drives the multi-sheet loop over residual rects that NEST_MULTISHEET_SCHEDULE owns, plus the GrainDirection/Guillotine constraint columns on NestPolicy so a panel-saw job nests under the straight-cut constraint while a laser job keeps the unconstrained NFP.
- Shape: A package admission task: Directory.Packages.props Fabrication ItemGroup + Rasm.Fabrication.csproj PackageReference + README [02]-[DOMAIN_PACKAGES] registry + .api/api-rectpacksharp.md via uv run python -m tools.assay api; nfp.md NestPolicy (lines 99-103) gains a placement-mode discriminant (nfp-true-shape / rect-fastpath) plus Guillotine/GrainDirection constraint columns, the rectangle arm reading the Bound() box and routing RectpackSharp for Sheet/Plate/Billet only.
- Unlocks: Tighter, faster utilization on rectangular sheet-metal/plate nests (the dominant laser/plasma/waterjet flatbed case) without evolving a GA population, plus guillotine/grain-constrained panel nesting — closing the constraint-satisfaction dimension free-form NFP placement leaves open.
- Anchors: Nesting/nfp#NESTING Nest.Solve fold/NestPolicy/Stock planar cases (Sheet/Plate/Billet lines 62-66, NestPolicy lines 99-103); Process/family#PROCESS_FAMILY Machine.HoldingClass flatbed machines; FabricationResult.Placement receipt; NEST_MULTISHEET_SCHEDULE residual-rect loop; CLIPPER_MEASURE_FOLD GetBounds box
- Tension: Docs settle bottom-left + genetic as author-kernels and NFP as the canonical placement primitive; RectpackSharp 1.2.0 (MIT, pure-managed AnyCPU, zero native deps, RID-safe) admits ONLY as the axis-aligned-rectangle fast-path placement arm coexisting with NFP, never touching BarStock/TubeStock/Remnant true-shape cases — and supersedes the dropped BinPack.NET/MaxRect candidates, which own the same AABB concern with no maintained edge.

[BIARC_ARC_EMISSION_ADMISSION]-[QUEUED]: geometry4Sharp BiArcFit2 admits at Posting to fit the linear Move/contour chord stream into G1-continuous circular biarcs so the dialect-neutral CutProgram emits real G2/G3 ArcCw/ArcCcw words and arc lead-in/out instead of dense linearized Feed blocks, the admission firewalled to the biarc/curve-fit surface only.
- Capability: Posting/program#CUT_PROGRAM wires the dormant ArcCw/ArcCcw GCommand rows (program.md lines 42-43) and the LeadArc generator (lines 172-180) to a biarc fit over each conditioned contour run, collapsing collinear-ish Feed micro-segments into curvature-faithful arc blocks the controller interpolates natively, and the lead-in/out arc becomes a fitted biarc rather than a single hand-built I/J word; this is the arc-faithfulness resolution that defeats the competing second-offset-engine proposal — the one Clipper2 owner stays chord-based and the biarc refit recovers arc identity at emission, no parallel arc-aware offset call site.
- Shape: A package admission task: Directory.Packages.props + csproj + README [02]-[DOMAIN_PACKAGES] registry + .api/api-geometry4sharp.md via uv run python -m tools.assay api SCOPED to the BiArcFit2/curve surface only; program.md gains a Biarc fold (Move/contour chord run -> G2/G3 GWord with I/J center) feeding the existing ArcCw/ArcCcw rows, the Emit fold rendering the arc blocks.
- Unlocks: Smaller, smoother, controller-native G-code (arc moves the look-ahead planner accelerates through), faithful arc reproduction for imported DXF bulge/arc spans that currently degrade to polyline chords, a real consumer for the ArcCw/ArcCcw/I/J AST rows the page already declares, and the variable-radius arc emission TROCHOIDAL_ENGAGEMENT_LIMIT needs.
- Anchors: Posting/program#CUT_PROGRAM GCommand ArcCw/ArcCcw rows (lines 42-43), GWord I/J columns (line 95), CutProgram.Emit fold, LeadArc/TabbedRing generators (lines 172-191); Polygon/import bulge/arc tessellation; Toolpath/motion Move stream; TROCHOIDAL_ENGAGEMENT_LIMIT arc consumer
- Tension: geometry4Sharp (NewWheelTech fork of geometry3Sharp, BSL-1.0, pure-managed) is a HEAVY mesh/2D/3D library; the admission scopes the .api capture to the BiArcFit2/curve-fit rail and firewalls its mesh-boolean/DMesh3 surface from the kernel Rasm.Geometry strata owner — this displaces the dropped CavalierContours arc-offset engine, which would have added a second offset call site the one-Clipper2 law forbids.

[CLIPPER_VARIABLE_KERF]-[QUEUED]: PolygonAlgebra grows a variable-delta offset arm driving the Clipper2 ClipperOffset.DeltaCallback64 per-vertex delta so torch-taper kerf and weighted-skeleton offsets ride the one polygon owner instead of a uniform InflatePaths, the page's own Growth row (clipper.md line 19) already naming this as the realization seam.
- Capability: Per-vertex variable-delta polygon/open-path offset over Clipper2 ClipperOffset + DeltaCallback64 delegate — the offset whose delta changes along the contour (thermal torch taper, weighted straight-skeleton clearing, variable-width perimeter shells in slicing) — as one Offset arm on PolygonAlgebra driving a stateful ClipperOffset engine where the per-vertex signed delta is a Func<vertex,double> column, the uniform InflatePaths (clipper.md Offset lines 65-68) staying the constant-delta arm; the catalogued-but-unused api-clipper2 ClipperOffset rows 06 (Execute(DeltaCallback64,Paths64)) and 10 (DeltaCallback property) are the wiring seam.
- Shape: One Offset arm on PolygonAlgebra at Polygon/clipper#POLYGON_ALGEBRA driving a stateful ClipperOffset (AddPaths with OffsetEnds.Join/End, Execute(DeltaCallback64, Paths64)), the per-vertex signed delta a Func<vertex,double> column; the variable-delta arm runs the int64 Paths64 engine while the uniform arm runs the PathD facade, the boundary map carrying both precision rails on the one owner; the POST_THERMAL_DEFLECTION_COMP per-vertex deflection offset rides this same arm.
- Unlocks: Thermal-cut torch-taper kerf, the weighted/variable straight-skeleton offset the clipper.md growth row names, variable-width perimeter shells in slicing, and the per-vertex deflection-compensation offset — all without a second offset engine.
- Anchors: Polygon/clipper#POLYGON_ALGEBRA Offset owner (lines 65-68, Growth line 19); Clipper2 ClipperOffset.AddPaths/Execute(DeltaCallback64,Paths64)/DeltaCallback (api-clipper2 ClipperOffset rows 06+10, catalogued UNUSED); Toolpath/skeleton#STRAIGHT_SKELETON weighted-skeleton consumer (skeleton.md Growth line 19) and Posting/program#CUT_PROGRAM consumers; POST_THERMAL_DEFLECTION_COMP consumer
- Tension: DeltaCallback64 receives path/normals/current+previous index and returns int64 delta (api-clipper2 NOTES line 230) — the variable-delta arm runs the int64 Paths64 engine while the uniform arm runs the PathD facade; the boundary map must carry both precision rails on the one owner.

[CLIPPER_INNER_FIT]-[QUEUED]: Nesting gains an inner-fit-polygon containment primitive from the catalogued-but-unused Clipper2 Minkowski.Diff so a part's feasible-inside-container locus is exact polygon algebra, collapsing the hand-rolled axis-aligned Stock.InRect bounds for non-rectangular stock.
- Capability: Inner-fit polygon (IFP) — the Minkowski difference of the container boundary and the reflected part — giving the exact set of reference positions where a part lies fully inside an irregular container, the dual of the NFP the nesting kernel already builds; NoFitPolygon grows an InnerFit factory routing PolygonAlgebra over Minkowski.Diff(container, reflected-part, isClosed, digits), and Stock.Contains (nfp.md lines 84-97) discriminates the IFP-inside test for a non-rectangular Remnant/Plate envelope while the rectangle cases keep the cheap InRect arm.
- Shape: An expansion of Nesting/nfp NESTING: a NoFitPolygon.InnerFit factory over a PolygonAlgebra Minkowski.Diff arm (api-clipper2 Minkowski facade Diff(PathD,PathD,isClosed,decimalPlaces) row 04, catalogued UNUSED); Stock.Contains gains an IFP-inside arm for the non-rectangular cases discriminated through the existing Switch, the planar-rectangle cases keeping InRect; the IFP locus reads the same exact Orient2D containment. PolygonAlgebra grows a MinkowskiDiff sibling to MinkowskiSum (clipper.md lines 75-76), never a second engine.
- Unlocks: True-shape feasibility against irregular remnants and non-rectangular plate cutouts (a part nesting inside an L-shaped offcut), not just axis-aligned bounds, over the one polygon owner — the exact inside-container locus the NEST_REMNANT_LINEAGE leftover polygon demands.
- Anchors: Nesting/nfp#NESTING NoFitPolygon/Stock owner (Contains lines 84-97); Polygon/clipper#POLYGON_ALGEBRA MinkowskiSum sibling arm (lines 75-76); Clipper2 Minkowski.Diff (api-clipper2 Minkowski facade row 04); Process/owner#FABRICATION_OWNER Loop.Covers exact-Orient2D containment; NEST_REMNANT_LINEAGE leftover-polygon consumer
- Tension: The IFP is exact only for a fixed rotation; rotated instances need a per-rotation IFP, so the InnerFit factory keys the rotation step the same way the NFP precompute does. The Minkowski.Diff PathD overload carries decimalPlaces (api row 04) so it scales at Precision.Digits, never the Clipper.MinkowskiDiff shorthand that fixes precision at 2.

[ACADSHARP_SPLINE_INSERT]-[QUEUED]: ProfileImport.Admit grows the Spline tessellation arm and the Insert nested-block flattening arm over the ratified ACadSharp surface so NURBS profiles and block-referenced part instances admit through the one DXF/DWG boundary at the existing ChordTolerance knob, after the .api catalogue confirms the Spline-sampler and Insert member spellings.
- Capability: Spline-to-Loop sampling (control polygon at chord.Segments) and Insert-block flattening (read BlockRecords[name].Entities, compose the Insert InsertPoint/scale/rotation transform) as two new Admit total-switch arms over the same ACadSharp curve sampler; the dropped-entity None arm for Spline/Insert (import.md Admit switch lines 84-92) today becomes a real Admit arm, neither a new entrypoint nor a sibling reader, the one Read fold unchanged.
- Shape: Two arms on the ProfileImport.Admit switch at Polygon/import#PROFILE_IMPORT — a Spline arm sampling at ChordTolerance.Segments, an Insert arm reading BlockRecord.Entities and composing the placement transform before recursing Admit; import.md Cases/Growth move the Spline and Insert-flatten arms from deferred to real Admit arms; lands AFTER the ACADSHARP_CATALOGUE_GROWTH .api task confirms the Spline-sampler-vs-control-polygon spelling and the Insert member set.
- Unlocks: DXF/DWG part libraries authored with splines and block instances (the common sheet-metal/CAM source shape) admit into the Loop vocabulary the nesting and posting kernels consume.
- Anchors: Polygon/import#PROFILE_IMPORT Admit owner (lines 84-92, Spline/Insert deferred line 14); ACADSHARP_CATALOGUE_GROWTH ratified spellings; ACadSharp Spline (api row 07)/Insert/BlockRecord.Entities; api-acadsharp.md [BLOCK_TRAVERSAL] forward arm; import.md Growth rows (line 19)
- Tension: Insert carries its own placement transform that must compose explicitly (api [BLOCK_TRAVERSAL] warns Entities does NOT auto-flatten) — the Insert arm recurses Admit through the composed transform, the one place a non-identity part placement enters the boundary; and if the .api confirms no native ACadSharp Spline tessellator, the Spline arm samples the control polygon at chord.Segments in-folder, never a native NURBS dependency the host-neutral boundary forbids.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
