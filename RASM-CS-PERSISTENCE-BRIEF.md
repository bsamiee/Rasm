# [RASM_CS_PERSISTENCE_BRIEF]

Campaign: cs track 2/6 — run after the geometry campaign (its DECISION also unblocks the AppHost/AppUi signature-lock re-verification). Phase 1: `Workflow(design-cs-persistence.js)` emits `RASM-CS-PERSISTENCE-DECISION.md`; Phase 2: `Workflow(rebuild.js, {targets: <per-leg pages>, brief: "RASM-CS-PERSISTENCE-DECISION.md"})` legs per the DECISION, spine first.

Authoritative campaign input for the ground-up restructure and rebuild of `libs/csharp/Rasm.Persistence/.planning/` — the APP-PLATFORM durable-state spine persisting the `Rasm.Element` `ElementGraph` as its system of record. The campaign is two-phase: `[01]-[PHASE_ARCHITECTURE]` runs ONE ephemeral design workflow that emits `RASM-CS-PERSISTENCE-DECISION.md` — the binding restructure blueprint (final page-set, new owners, splits, seam ledger, band registry, roster deltas, leg partition); `[05]-[BUILD_LEGS]` then runs the durable rebuild engine against that DECISION. This brief binds both phases. Upstream law honored throughout: the conversion-landed kernel corpus (`libs/csharp/Rasm/.planning/` — one root, eight sub-domain folders; `Domain/identity.md` `ContentHash.Of`, `Domain/rails.md` `Expected`, and `Spatial/reconciliation.md` `Encode`/`Reconcile` are the landed surfaces this package composes) together with `RASM-CS-GEOMETRY-BRIEF.md` (the kernel campaign — Persistence writes against the UNION of the landed pages and that brief's mandates: its key-minting consumer, never its re-implementer) and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (the frozen-wire rule, the `[AMENDMENTS]` count-prefix law, the 25xx->27xx re-band gate, the `FaultBand` registry shape). `docs/stacks/csharp/` governs unchanged and is never restated here.

## [00]-[SHARED_LAW]

### [VERDICT]

The 18 fence interiors are genuinely world-class — dense ADT collapse, seam-composed content addressing, one-materializer discipline, honest rails — and the PACKAGE around them is fiction. Five proofs, each independently fatal to a world-class claim:

1. **The card pool asserts realized phantoms.** Three OPEN cards claim a federation owner that does not exist: `IDEAS.md:38` "the source-agnostic `FederatedEntity`/`EntityGraph` ... is realized", `IDEAS.md:45` "the Persistence-side lowering target (`ElementSet`/`SetExpr` algebra + `FederatedPlan`) is realized", `TASKLOG.md:24` "`Query/federation#ENTITY_GRAPH` `SourceKind.SignedArtifact` ... is realized" — grep of `FederatedEntity|FederatedPlan|PlanNode|SourceKind|Query/federation` over all 18 pages returns ZERO structural hits. Six MORE `-[COMPLETE]` cards claim landed pages/`.cs` nodes the tree does not contain: `TASKLOG.md:37` (`Query/transaction.md`), `:41` (`Store/quality.md`), `:42` (`Query/pipeline.md`), `:43` (`Sync/egress.md`), `:44` (`Schema/ddl#EXTENSION_DDL` — directly contradicted by `:45` "there is no `SchemaDdl.Sql` owner"), `:48` (`Version/snapshots#SNAPSHOT_PROTOCOL AsOfKey`). A `-[COMPLETE]` card citing a nonexistent node is a verify-remove failure by construction; governance is decoupled from the realized tree.
2. **A third of the manifest is orphaned.** The element-rebuild collapse (~30 pages -> 18) deleted `Sync/`, `Schema/`, `Query/{federation,pipeline,transaction}`, `Store/{quality,tenancy,server,profiles}`, `Version/snapshots` — and the roster was never pruned: ~30 of ~90 admitted packages have ZERO live-page consumer, including the entire streaming/messaging cluster (14 packages: `Confluent.Kafka`+`SchemaRegistry`×4, `Chr.Avro`×3, `CloudNative.CloudEvents`×3, `NATS.Net`, `RabbitMQ.Client`, `DotPulsar` — `Directory.Packages.props:264-277,297,309`), the scale-out backend tier the sealed two-engine sweep FORBIDS (`ClickHouse.Driver`/`ScyllaDBCSharpDriver`/`Qdrant.Client`/`DeltaLake.Net`/`rocksdb`/`LightningDB` — `provisioning.md:378` rejects a third engine, `ARCHITECTURE.md:115` "NEVER admit a new relational engine row"), `FlowtideDotNet.Substrait` (`:281`, backing the phantom federation), `MPXJ.Net`, `PollinationSDK`, and an 80%-unmined EF sub-stack. Nearly all 77 per-package catalogs mis-anchor to the deleted pages (`api-duckdb`→`Store/profiles`, `api-arrow`→`Query/rail`+`Sync/egress`, `api-cbor`→`Sync/egress`, `api-zstd`/`api-lz4`→`Version/snapshots`); 16 carry no anchor at all; the shared-tier `api-messagepack.md` is a byte-identical misfiled duplicate tagged `RASM_PERSISTENCE` in `libs/csharp/.api/`.
3. **The fault-code space is triple-collided.** Verified exact collisions: `ColumnarFault` 8350-8352 (`columnar.md:144-146`) ≡ `ServerFault` 8350-8352 (`provisioning.md:220-222`); cypher `GraphFault` 8360-8363 (`cypher.md:68-71`) ≡ lane codebook 8360-8363 (`lane.md:432-441` — BARE `Error.New`, the only un-banded owner, violating the corpus's own "a bare `Error.New` is the deleted form"); `TopologyFault` 8370-8371 (`topology.md:179-181`) ≡ `TabularFault` 8370-8373 (`tabular.md:104-108`) ≡ provisioning's loose receipt codes 8371-8375/8379/8380 (`provisioning.md:72-74,293-294,321,326`). Plus a simple-name collision: TWO `GraphFault : Expected` unions in one package (`graph.md:155` ns `Rasm.Persistence` 830x vs `cypher.md:60` ns `Rasm.Persistence.Query` 836x). `Expected.Code` is the telemetry key; the band space is ambiguous by construction, partitioned only by mutually inconsistent per-page prose (`topology.md:166` claims 837x beside columnar 835x, `columnar.md:128` claims 835x beside cypher 836x, neither aware of its own collision).
4. **The AppHost contract law dangles ownerless.** `Rasm.AppHost/ARCHITECTURE.md:76-79` declares Persistence the backing store for four PORT seams — fenced per-tenant Budget debit, workflow step-state CAS, transactional outbox same-tx, and CAS+fenced-lease+membership (`ONE_FENCED_LEASE_STORE`/`ONE_OUTBOX_EGRESS_SPINE`) — and NO page owns a fenced lease, a CAS primitive, a membership row, or a NAMED outbox. Two more AppHost rows point at deleted pages (`:71` `Query/transaction`, `:73` `Sync/egress`); the own ledger carries three declared-unwired rows (`ARCHITECTURE.md:50` icechunk `AsOfKey` — landed, per `TASKLOG.md:48`, on a page that no longer exists; `:55` `Rasm/Spatial/reconciliation` `GeometryHash` — the upstream owner is LANDED (`NamingHashOps.Encode`/`Reconcile` project `TopoName`→`GeometryHash` through the kernel hasher, and kernel `ARCHITECTURE.md:79` declares the counterpart row) while `topology.md` contains zero reconciliation references — a one-sided live seam; `:57` the Substrait half) and omits two wired seams (lane `VECTOR_CODEBOOK`⇄`Compute/Model/embedding`, declared only by Compute at `Rasm.Compute/ARCHITECTURE.md:99`; `cache#L2_CONTRIBUTION`⇄AppHost `CACHE_PORT`, declared only by AppHost at `:69`).
5. **Verified logic defects ship inside 9-grade fences.** `recovery.md:181` spells a COUNT of missing content keys as MINUTES (`Duration.FromMinutes(absent.Count)`) on the load-bearing DR gauge feeding `objective.MeetsRpo` (`:151`), against the page's own freshest-blob-lag law (`:15`; the `:175-177` comment restates the defect); `recovery.md:172` reuses the 16-MiB WAL SEGMENT SIZE as a per-second THROUGHPUT rate; `commits.md:108-113` `MergeBase` re-runs `Rank` per common candidate — O(V²) on criss-cross histories under a "true merge-base set" claim; `identity.md:120` hand-writes a `NodeId` converter while `:82` relies on the Thinktecture auto-converter for `H3Cell` — one of the two violates the package's own prohibition (`ARCHITECTURE.md:113`) whichever way the registration falls; `merge.md:283` `ExternalKey` aligns ONLY `ExternalId`-bearing Objects, so any derived-Type re-key reads as delete-all+insert-all — the exact defect `Reconcile` exists to prevent.

What IS sound (preserve; harden, never re-derive): `codec.md`'s seam-composed `ContentAddress` + 8-rank `RejectTier` ladder + single-pass atomic seal + CBOR trust boundary; `graph.md`'s one-materializer event sourcing with co-transactional identity and one-boundary fault conversion; the `Version/` engine's changefeed-projection law, content-addressed commit DAG, six-type CRDT algebra with the `CrdtOpWire` flagship, W3C-PROV exact typing with independent-`digestOf` attested verify, fail-closed retention with full-history reachability, and the real `(Timeline, Lsn)` recovery coordinate; `lane.md`'s sequence-gap `StalenessWatermark` and length-framed `ElementSet` receipt; `topology.md`'s seam-frozen incidence; `cypher.md`'s injection-safe server-side `format('%L')` composition; `blobstore.md`'s nine-delegate `ObjectLeg` and write-once 412-noop seal; `provisioning.md`'s verification-first fold with `FailureRank.Absorb` as the absence policy. The rot is the PERIMETER — governance, roster, bands, ledgers, plus a bounded set of confirmed in-fence defects — and no per-page cold pass fixes the perimeter. Architecture first.

### [TELOS]

`Rasm.Persistence` is the federation's durable truth: the content-addressed system of record for the `ElementGraph`, the version-control engine over it (commit-DAG, convergent CRDT, AS-OF time travel, three-way structural merge that survives re-ingest AND re-key, PROV-O provenance under a tamper-evident attested ledger, classification-driven retention with full-history GC, provably-restorable recovery), the consistency-split read lanes, the content-keyed geometry object store, AND the coordination substrate the runtime law already assigns it — one body. Every owner grades against the durable-spine bar and the 5x consumer bar at once: every AppHost workflow leans on the fenced-lease store; every cross-runtime reuse row resolves against the one kernel `ContentHash.Of` `XxHash128` entry bit-identically (`Rasm/.planning/Domain/identity.md` — its landed fence names the Persistence snapshot spine and artifact index as composing consumers); a Type-seed re-key must read as a RENAME through the merge engine, never churn; a million-event model must scrub at O(delta), never O(N·graph); a posted fabrication program, a Materials library row, and a Python-stamped reuse key all land on this spine when their producers pin. Zero current consumers never lowers the bar. World-class additionally means the PERIMETER is machine-checkably true: one type-enforced fault-band registry, a seam ledger where every row is composed in a fence and every fence crossing has a row, catalogs anchored to live pages, a manifest with zero orphan admissions, and a card pool whose every claim resolves against disk.

### [STRUCTURAL_AUTHORITY]

Phase 1 owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain folders, delete pages after absorption, mint new owners and folders (minimum 2 pages per new folder). Calibration examples, not the ceiling: `Element/identity.md` (653 LOC, four concerns) extracting its object-ACL authz vocabulary to `Element/authority.md`; `Query/lane.md` (521 LOC, four concerns) extracting fusion+codebook to `Query/retrieval.md`; `Ingest/` growing from the standing one-file-folder violation to a ≥2-page codec-ingress axis; `Store/` growing the coordination owner; the federation and egress owners minting fresh. The folder-architecture law binds: NO one-file folders; every folder a growth axis where the next concern lands as a page (`Store/` absorbs the next durable-home concern, `Ingest/` the next file codec, `Query/` the next read lane); no flat sprawl — `Version/` stays flat at the post-campaign 8 (`[V3]`'s egress joins the 7), its core/read/governance sub-partition recorded as the split axis only if it grows past ~9. The ruled-default post-campaign map drafts calibrate against (exceed, never dilute; the `[V1]` retire and `[V11]` fold-up alternatives re-derive it): `Element/` 4 (+authority) · `Version/` 8 (+egress) · `Query/` 7 (+retrieval, +federation) · `Ingest/` 2 (+schedule) · `Store/` 3 (+coordination) — 24 pages, zero one-file folders. Every structural change updates `ARCHITECTURE.md` `[01]-[DOMAIN_MAP]`+`[02]-[SEAMS]`, `README.md` `[01]-[ROUTER]` and package groups, and the TASKLOG/IDEAS cards whose anchors it moves, in the same motion. No page survives on incumbency; no CARD survives on incumbency either — a card whose anchor no longer exists re-points or dies.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md:35-40`): Persistence is APP-PLATFORM, depending UP on `Rasm` (the landed `Domain/identity.md` `ContentHash.Of(ReadOnlySpan<byte>)` seed-zero entry — the [CONTENT_KEY] family kernel `ARCHITECTURE.md:76-81` declares; own `ARCH:42` is the wired seam-reference row) and `Rasm.Element` (the `ElementGraph`/`GraphDelta`/`ContentAddress` seam) ONLY; it never references a sibling AEC peer (`Rasm.Materials`/`Rasm.Bim`/`Rasm.Fabrication`) or a host SDK; alignment travels through the seam contracts and the content-keyed wire. AppHost is a PORT peer — Persistence contributes rows (health, cache L2, identity, recovery objectives, coordination) and never reverses the dependency. Seam-owned machinery is composed, never re-homed: the canonical-bytes writer, `ContentAddress.Of`/`OfGraph`, and the delta monoid are `Rasm.Element`'s (`codec.md:157-158` — "a Persistence-local NodeHash/GraphHash forwarding owner ... is the deleted form" stays law); Marten owns the durable append (never a bespoke op-log store beneath it); the relational sweep stays sealed at PostgreSQL 18 + embedded SQLite (`ARCHITECTURE.md:115`); entity types never cross the package boundary; provider variance is row data on closed axes. Every `ARCHITECTURE.md:102-117` prohibition carries forward unless a verdict below explicitly amends it.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — `RetentionClass`/`ServerExtension`/`ObjectStore`/`ColumnarExtension`/`CrdtField`/`ProvRelation`/`GraphQuery`/`SnapshotCodec` are all `[SmartEnum]`/`[Union]` + FrozenDictionary dispatch where rosters are seed DATA — so the law binds the NEW planes: fault bands are rows in ONE type-enforced registry (a duplicate integer fails at type initialization, mirroring `.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149`), never per-page prose neighborhoods; egress sinks are delivery rows over ONE pump fold and ONE envelope; lease/CAS/membership verbs are cases on ONE coordination op-union; federation sources are rows over ONE plan-lowering fold; ingest codecs are format rows over ONE record rail. The test: the next band, sink, source, or codec is one data row, never a new arm, a loose integer, or a second pump.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — already the corpus norm (`GraphStoreOp`, `TabularSpec`, `GraphQuery`, `TopologyQuery`); sealed as law for every new owner.
- Every fault is a typed `[Union] : Rasm.Domain.Expected` case (the landed `Rasm/.planning/Domain/rails.md#[04]-[FAULT_BAND]` base — `IsExpected` pinned, `Category` projected, `Error`-subtyped so `Code` stays the telemetry key) in a registry-assigned band; bare `Error.New` on a rail is the deleted form (`lane.md:432-441` and `provisioning.md:72-74,293-294,321,326` are the two live breaches); receipts stay typed per `ARCHITECTURE.md:114` and every receipt-carried code resolves through the registry.
- The frozen-wire rule binds: `CrdtOpWire`'s `[MessagePack.Union]` `[Key]` sequence IS the wire schema; canonical bytes are seam-owned and the `[AMENDMENTS]` count-prefix fork is absorbed BY COMPOSITION (`codec.md:151-158`, `merge.md:160,173`, `timetravel.md:187-188` compose the seam writer verbatim; no golden node-canonical vector is pinned corpus-side; the amendment IS the migration — planning-phase store, nothing persisted). Content-key literals stamp on the host-validation pass, never as asserted values. The landed kernel fixture corpus (`Rasm/.planning/Spatial/reconciliation.md#ONE_WIRE_FIXTURE_CORPUS`) carries `CRDT_OP_SET` as a DESIGN-PIN row naming `Version/commits#CRDT_ALGEBRA` the producer: the commits leg PINS the `MvRegister`/`opMerge` op-set whose divergent-delivery folds converge byte-identically, turning the corpus row REAL under the one kernel seed — a fabricated byte set never stands in.
- ONE hasher entry: every Persistence digest mint composes the landed kernel `ContentHash.Of` seed-zero entry (`Rasm/.planning/Domain/identity.md` — THE federation content key; kernel `ARCHITECTURE.md:77` "no second hasher at the codec", the Compute ledger's "never a per-call-site `System.IO.Hashing.XxHash128` invocation" the precedent row) — directly for the opaque preimages codec today raw-hashes (`SnapshotHeader.Seal` sealed bytes, the `ContentChunker` per-chunk and whole-payload digests, the `[V1]` plan digest, the cache artifact identity fold), transitively through the seam `ContentAddress` for node/graph keys; digests are value-unchanged (the entry IS `XxHash128.HashToUInt128` seed-zero), so the re-anchor is call-path collapse, never an identity migration. Persistence additionally names itself the demanding consumer of the identity.md Growth row's streaming-identity member (`XxHash128` `Append` + `GetCurrentHashAsUInt128`, seed zero) for the blobstore multipart and chunk folds whose payloads outgrow a one-shot span — composed on landing, the `Of(UInt128)` wrap of a precomputed digest the documented interim.
- Consumer contracts are RECORDED where the owner is upstream, never re-homed: the delta-composable incremental `ContentAddress.OfGraph` is the `Rasm.Element/Projection/address` owner's to provide (`Element/Graph/delta.md:55` DELTA_MONOID address law is the hook); Persistence composes it when it lands and documents the interim full recompute.
- Cross-package member claims verify or die — external members against restored assemblies (`assay api` is truth there), kernel and `Rasm.Element` seam members against the LANDED FENCES (the kernel corpus is planning-scoped with no assembly to decompile: `ContentHash.Of`, `Expected`, `NamingHashOps.Encode`/`Reconcile` read off `Rasm/.planning/` pages): `MemberPathSegment.Value is NodeId` typing under Generator.Equals `[UnorderedEquality]` (`merge.md:246-249,402-403`, `timetravel.md:283-285` — if string keys emit, member-granular patch silently degrades to whole-node replace); the `AWSSDK.S3` `ChecksumAlgorithm.XXHASH128`/`ChecksumType.FULL_OBJECT` members blobstore's integrity spine composes; the seam `ContentAddress.Of(Node, Tolerance)` overload `timetravel.md:187` spells against merge's span form (`merge.md:160`) — verify the overload set or unify the spelling, and flag the `OfGraph`-vs-`Of(ElementGraph)` naming split to the Element campaign.
- One `IDocumentSession` transaction owner; one materializer (`GraphDelta.ReplayOnto`); one recency horizon; one retention executor; one changefeed — the standing singletons stay law, and the new owners (outbox, egress pump, lease store) join them rather than minting parallels.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped `Persistence` block at `:251`) + `Rasm.Persistence.csproj`; per-package catalogs under `libs/csharp/Rasm.Persistence/.api/` (77), shared substrate under `libs/csharp/.api/` (30). The manifest motion lands in leg 1 (safe: every ruled prune is zero-page-consumer). Carried obligations: the scale-out backend tier (`ClickHouse.Driver`, `ScyllaDBCSharpDriver`, `Qdrant.Client`, `DeltaLake.Net`, `rocksdb`, `LightningDB`) re-disposes INTEGRATION-FIRST under `[V13]` — each lands as a provider row on its owning perimeter axis or leaves only on the per-package redundancy proof against the in-PG/in-process owner of the same concern (TimescaleDB OLAP, pgvector/pgvectorscale ANN, DuckDB columnar), argued in the DECISION, never defaulted on zero consumers; PRUNE `PollinationSDK` (a domain cloud SDK with no store-class concern — charter misplacement, not consumer absence; re-admission belongs to whichever package names a consumer); PRUNE the NTS persistence bridges (`Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite`, `Npgsql.NetTopologySuite`, `NetTopologySuite.IO.GeoJSON4STJ`, `NetTopologySuite.IO.GeoPackage` — no persisted NTS geometry column exists; H3 cell derivation rides `pocketken.H3`) and `Microsoft.EntityFrameworkCore.Sqlite` (the embedded floor is raw ADO by design, `provisioning.md:369-434`); PRUNE `StackExchange.Redis` as a direct reference (the L2 swap row keeps `Microsoft.Extensions.Caching.StackExchangeRedis`, which transits the driver); the streaming/messaging cluster rides `[V3]` INTEGRATION-FIRST (`NATS.Net` + `CloudNative.CloudEvents`(+`.SystemTextJson`) anchor the spine; `Confluent.Kafka`+`SchemaRegistry`×4 with `Chr.Avro`×3 as the row's codec column, `CloudNative.CloudEvents.Kafka`, `RabbitMQ.Client`, `DotPulsar` land as sink rows by default, each leaving only on the `[V3]` redundancy proof); `FlowtideDotNet.Substrait` + `Apache.Arrow.Adbc.Drivers.{Apache,BigQuery}` + `Apache.Arrow.Flight` ride `[V1]` — kept only when the federation owner's fences compose them, removed with the retire alternative; COMMIT `EFCore.NamingConventions`, `Microsoft.EntityFrameworkCore.Design`, `Thinktecture.Runtime.Extensions.EntityFrameworkCore10` per `[V6]`; `MPXJ.Net` re-homes to the `[V11]` schedule-ingress owner or leaves with it. Two admissions the census left undisposed dispose here: `Ara3D.BimOpenSchema`(+`.IO`) KEEPS on its live co-transactional columnar consumer (`columnar.md:405-490`) with the DEBUG-IL upstream fix demanded (E2; 1.0.1 is the latest release, repo active); `Npgsql.OpenTelemetry` (ZERO page consumers) ruled default KEEP: it earns the named provisioning observability row — tracing+metrics builder subscriptions composed at the AppHost root exactly as the health rows are — and leaves with its catalog only if the provisioning draft rejects the row. Catalog hygiene is a first-class obligation: every kept catalog re-anchors to the live page set, the 16 anchor-less catalogs gain anchors, the misfiled shared `api-messagepack.md` deletes (or re-scopes to a neutral `RASM_API` substrate catalog), `api-h3.md:3`'s claim of composing the pruned NTS IO bridges corrects to the transitive core-NTS crossing (`pocketken.H3` survives the `[V7]` prune intact), the 9 double-cataloged overlays reconcile against their shared-tier owners, and a uniform provenance tag + STACKING section lands across the kept tier; the absent substrate-tier `api-languageext.md` (the rail every fence composes, uncatalogued in either tier) is a track-shared authoring obligation — the first campaign to run lands the ONE substrate file, later campaigns verify, never re-author (RASM-CS-APPHOST-BRIEF.md `[ROSTER_RECONCILIATION]` the coordination law), and every shared-tier catalog motion rides the same track law. Admissions carry `.api` stubs in the same motion. License gate: OSS and free-for-OSS-commercial admissible; pay-tiered/seat-licensed/proprietary-gated REJECTED. Recorded rejection, never re-proposed: `LoroCs` as the Persistence CRDT owner — the cross-runtime bit-parity wire law (`CrdtOpWire` `[Key]` sequence reproduced in Python/TS) forecloses a Rust-binding CRDT whose wire is its own; LoroCs stays App-UI's live-collaboration concern.

## [01]-[PHASE_ARCHITECTURE]

ONE ephemeral design workflow (authored via `workflow-creator`, deleted after landing) runs survey -> draft -> judge -> decide -> salvage and emits `RASM-CS-PERSISTENCE-DECISION.md`. Shape:

- **Survey**: 4-6 read-only agents partitioned by plane (Element+Store / Version / Query+Ingest / both `.api` tiers+manifests / sibling ledgers) re-verify `[02]-[EVIDENCE_REGISTER]` against disk (the register is initial pointer, never ceiling), resolve every `[SEAM_AND_RAIL_LAW]` verify-or-die member (`assay api` for restored external assemblies; the landed kernel/Element fences for seam members), and sweep every cross-page and cross-package reference into the raw seam census.
- **Draft**: 4 independent COMPLETE page-set proposals — one row per page carrying action (KEEP/REBUILD/SPLIT/MERGE/MOVE/DELETE/NEW), owner charter with its registry band, entry signature (op-union or NONE for vocabulary pages), in/out seam edges, and leg — drafted under four lenses (coordination-contract-first working backward from the AppHost PORT law, federation/egress-first working backward from the cross-runtime wire seams, perimeter-first (registry/ledger/catalog/manifest coherence), and folder-architecture-first). A draft leaving an evidence row undisposed, a verdict unanswered, or an orphan package undisposed is incomplete and loses at judge.
- **Judge + Decide**: adversarial scoring against `[00]` law + `[03]` targets — verdict disposition, strata acyclicity (zero upward edges; the four on-disk intra-package inversions disposed per `[V5]`c/d), band-registry uniqueness, and all four AppHost PORT seams homed are disqualifying gates; capability reach then surface collapse rank survivors; the decision is one blueprint with grafts.
- **Salvage**: per-draft miners + overturn auditor + ONE writing integrator; a mined finding is accepted only with a disk or `.api` file:line anchor the auditor re-verifies.
- **Emit**: `RASM-CS-PERSISTENCE-DECISION.md` carrying (a) the final page-set table with per-page skeletons, (b) the band registry with the re-partitioned 83xx map, (c) the corrected seam ledger (own rows corrected; the twelve stale sibling rows listed as counterpart obligations for the Compute/Bim/AppHost campaigns), (d) roster deltas with `.api` obligations, (e) the leg partition proven acyclic, (f) the full TASKLOG/IDEAS card disposition table.

The DECISION must resolve every verdict below. Recommended shapes follow each; the design pass may exceed them, never dilute them.

### [V1_FEDERATION_DISPOSITION]

The campaign-defining structural ruling: retire or reintroduce the federation owner the element rebuild collapsed. The realized substrate (`lane#ELEMENT_SET_ALGEBRA` `ElementSet`/`SetExpr`, `cache` artifact/result indexes, `commits` `ContentParityCorpus.Contribute`) carries the reuse currency; the declared wire seam (`ARCHITECTURE.md:57` "ElementSet receipt currency + Substrait portable plan") and three BLOCKED cards demand the plan half. Ruled default: REINTRODUCE — ONE `Query/federation.md` owner: the Substrait plan decode (`FlowtideDotNet.Substrait`, real members verified) lowering portable plans onto the existing `SetExpr` algebra and the columnar/ADBC execution lane (source rows over one lowering fold — `SourceKind` as seed DATA covering durable-store/signed-artifact/ADBC-warehouse sources), signature-locked at the wire so the queued `python:data` producer lands against it; the three cards re-anchor to it with honest status (lowering target realized as fences; BLOCKED on the Python producer). Two chain links the reintroduced owner closes honestly at ingress, or the verdict is vapor at its first link: (a) WIRE FORM — the Substrait protobuf round-trip is INTERNAL to `FlowtideDotNet.Substrait` (`api-flowtide-substrait.md:151` — no public `Serialize()`/`Deserialize()`), so "plan decode" has no public ingress member today; the DECISION names the wire's form — ruled default: the plan arrives as Substrait protobuf bytes and a Persistence-owned boundary adapter transcribes them onto the managed `Plan` through the `RelationVisitor`/`ExpressionVisitor` fold, riding the shared-tier `Google.Protobuf`+`Grpc.Tools` substrait codegen (zero new admission); `SqlPlanBuilder.Sql`/`GetPlan()` (public, verified) stays the self-hosted SQL front-end, never the cross-runtime wire; a transcription heavier than the lowering itself feeds the retire criterion. (b) PINNED EXECUTION — every lowered plan executes against ONE `TimeCut` (default: the lane watermark head) and returns the `ElementSet` receipt (`lane.md:129`) plus the Arrow batch stream; the receipt records the (plan digest = the kernel `ContentHash.Of` entry over the received wire bytes — never a local `XxHash128` call site, cut, watermark) triple so a federated result is content-addressed and reproducible, and replay lands as one `ArtifactKind` reuse row per `cache.md:20`'s growth law — never a second cache, never an unpinned read. Deciding criterion for the retire alternative: if drafting proves the Substrait relational surface cannot lower onto `SetExpr` + the columnar lane without minting a second query engine (breaching `ARCHITECTURE.md`'s one-engine boundaries), the concern formally RETIRES — cards re-scoped to the ElementSet+cache substrate, `FlowtideDotNet.Substrait` + the ADBC BigQuery/Apache drivers + `Apache.Arrow.Flight` pruned, and `ARCHITECTURE.md:57` re-scoped to the ElementSet-currency half only. Either way the phantom-realization prose dies everywhere.

### [V2_COORDINATION_AND_OUTBOX]

The largest true capability gap: NEW `Store/coordination.md` owning the four AppHost PORT contracts (`Rasm.AppHost/ARCHITECTURE.md:76-79`) — ONE fenced-lease store (monotonic fencing tokens over PG row-CAS/advisory locks; per-tenant Budget debit and workflow step-state CAS as op cases on one coordination op-union). The fencing is RAISED from a lease-issuing store to a token-VALIDATING one, because a fence a resource never checks is decorative (Kleppmann fencing): every guarded op case (Budget debit, step-state CAS) carries the holder's monotonic token, and the guarded write REJECTS a token older than the row's current lease generation via the same PG row-CAS predicate that debits — a stale holder that resumed after a lease handoff is a typed `LeaseFenced` fault, never a lost-update. The Budget debit is exactly a fenced compare-and-decrement (the same-row CAS carries both the token check and the non-negative-balance guard in one predicated `UPDATE ... WHERE token >= held AND balance >= debit RETURNING`), so per-tenant accounting cannot double-spend across a fenced handoff. The debit predicate is PER-UNIT VECTOR, not scalar — AppHost's `GrantBroker` meters a multi-unit `CostVector` with independently-capped `CostUnit` ceilings (`Rasm.AppHost/.planning/Agent/capability.md:62-67,420-423`; AppHost the demanding consumer, RASM-CS-APPHOST-BRIEF.md), so the Budget row carries N unit balances and the one predicated `UPDATE` checks `token >= held AND balance_i >= debit_i` for every requested unit atomically; a scalar-only debit forces a client-side multi-statement span the fence cannot cover. The op-union carries READ cases beside the guarded writes — the per-tenant in-flight step-state enumeration and the expired-lease scan (`Rasm.AppHost`'s `CrashResume`/orphan-reclaim ingress, `Rasm.AppHost/.planning/Runtime/orchestration.md:271,284` already composing `StepStateSeam.InFlight`; AppHost the demanding consumer, RASM-CS-APPHOST-BRIEF.md) — a write-only union leaves the crash-resume flagship without ingress and forces the AppHost-side table scan the PORT law forbids. Also membership rows with lease-expiry semantics, and the NAMED `ONE_OUTBOX_EGRESS_SPINE`: the Marten event stream IS the outbox (events commit in the same `IDocumentSession` as state — the same-tx guarantee already holds); the owner NAMES it, mints the durable drain cursor + at-least-once advance law, and `[V3]`'s pump consumes it. Composes Marten/Npgsql primitives (`FetchForWriting`, `QueueSqlCommand`, advisory locks, LISTEN/NOTIFY wake) — never a second event store, never a distributed-lock sidecar. The op-union, fencing tokens, membership rows, and receipts are Persistence-owned types AppHost's PORT adapters decode — no AppHost interface or type crosses down. `Store/` grows to a real 3-page growth axis. The stale AppHost rows (`:71` transaction — folded into `graph#STORE_RAIL` per `TASKLOG.md:37`'s fold record; `:73` egress) are that campaign's 1-hop ripple; this DECISION lists the corrected targets.

### [V3_CHANGEFEED_EGRESS]

CDC egress is in-scope — the changefeed exists (`ledger#CHANGEFEED`), the AppHost egress port demands a drain target, and `TASKLOG.md:43`/`IDEAS.md:61` claim a landed owner that is deleted. NEW egress owner (ruled default site: `Version/egress.md`, beside the changefeed it drains; a `Sync/` folder revival requires a second sync-shaped sibling to clear the folder law): ONE pump fold draining the `[V2]` outbox cursor past `OpLogEntry` rows into `EgressSink` delivery rows under ONE CloudEvents envelope (`CloudNative.CloudEvents`+`.SystemTextJson`), with at-least-once advance, typed dead-letter, and replay — never a second changefeed. The parity floor RAISES to exactly-once EFFECT, because at-least-once + replay without an idempotency contract is a duplicate-delivery generator, not a world-class egress: the envelope is content-addressed once — the CloudEvent `id` IS the `OpLogEntry.ContentKey` (`ledger.md:107` — the delta content key already exists, never a minted UUID), the `Sequence` extension (`api-cloudevents.md:62`, String-typed) carries the outbox `long Sequence` for per-sink total ordering, and `Partitioning.partitionkey` carries the `EntityKey` for ordered partitioned delivery. That envelope law makes each sink's dedup a composition, not a promise: `Nats` maps the CloudEvent `id` to the `Nats-Msg-Id` header (`api-nats.md:44`) so JetStream's server-side dedup window collapses a redelivered op to a no-op, and the Settle ceremony's publish-ack (`api-nats.md:94`) is what advances the cursor. The `Webhook`/`pg_net` sink states its delivery honesty as owner law rather than inheriting the `Nats` guarantee it cannot hold: `net.http_post` returns only a request-id `bigint` and is fire-and-forget (`api-pg-net.md:35,44`), so the pump NEVER advances the cursor on post — it advances only after reconciling `net._http_response`/`net.request_status` (`api-pg-net.md:50-53`) to `SUCCESS`, with `PENDING` holding the cursor and `ERROR`/timeout routing the typed dead-letter (`net._http_response` is UNLOGGED — a server crash drops pending rows, the held cursor re-posts, and the idempotency-key header absorbs the duplicate), and the receiver-side idempotency-key header carries the same `id` for at-most-once effect on a non-transactional endpoint. Sink roster as seed DATA, categorical-best floor: `Webhook` (server-side via the admitted `pg_net` extension row, response-reconciled advance), `Nats` (JetStream durable streams own the Settle ceremony, `Nats-Msg-Id` dedup; its KV/ObjectStore surface stays unconsumed until a named consumer), and the wire-native gRPC/Arrow hop where AppHost's `OutboundHop` law already carries it — that row's delivery honesty READS the AppHost side: `HopPolicy` gains a per-hop delivery-honesty axis (RASM-CS-APPHOST-BRIEF.md `[V9]`, AppHost the demanding consumer of stated-honesty-per-sink), so the pump's cursor-advance law for the wire-native row composes the hop's declared guarantee, never an assumed one. `Kafka` (+SchemaRegistry serdes, with `Chr.Avro`×3 riding the row as its codec column), `RabbitMQ.Client`, and `DotPulsar` land as sink ROWS by default — the generator law prices each at one delivery row over the one pump fold, and zero current consumers never lowers the bar (enterprise-interop egress is standing 5x pressure); a broker row leaves only on proven redundancy against an admitted sink of the same delivery semantics or feed-verified abandonment, argued per-package in the DECISION per `[ROSTER_RECONCILIATION]`. Presence/awareness stays `ledger#PRESENCE` — the egress owner drains durable ops, never the lossy lane. The siting adds the `Version/egress` -> `Store/coordination` cursor edge to the seam graph; the DECISION's acyclicity proof carries it (no reverse edge — coordination never reads the pump).

### [V4_FAULT_BAND_REGISTRY]

Decidable NOW: mint the Persistence band registry mirroring the federation `FaultBand` (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149`) — a `[SmartEnum<int>]` whose generated key lookup makes a duplicate band fail at type initialization; every fault union's `Code` derives `Band + n`; every loose receipt code becomes a typed case or a registered sub-band row. Ruled default siting: a `[FAULT_TABLES]` owner block on `Element/graph.md` (the store-rail root every rail composes). The re-partition it must emit: existing disjoint bands register as-is (RemoteStore 540x, Embedded 771x, Sync 825x, Commit 826x, Retention 828x, Recovery 829x, Graph 830x, Codec 831x-833x as a legal multi-decade stride, Identity 834x); the three collisions re-partition to unique decades (ruled: Columnar keeps 835x, provisioning `ServerFault`+receipt codes re-band to a fresh decade; cypher keeps 836x, the lane codebook gains a typed `RetrievalFault` band on `[V5]`'s new page; Topology keeps 837x, Tabular re-bands). The `GraphFault` simple-name collision resolves in the same motion: cypher's renames to `CypherFault` (the store fault keeps `GraphFault`). The registry additionally carries PINNED MIRROR rows for every foreign neighborhood (AppHost 1xxx/4100-4810, Compute 2200-2299 plus the Remote `WireFault` 4520-4532 wire band, AppUi 6xxx, the AEC 23xx-27xx registry — whose 2400 row is the pinned mirror of the landed kernel `GeometryFault` century 2400-2449, `Rasm/.planning/Numerics/faults.md` — and the kernel-substrate `Fault.UnsupportedCode` 9104, the only coded case on the landed `Rasm/.planning/Domain/rails.md` `Fault` union) exactly as the AppHost and AppUi registries pin theirs reciprocally (`RASM-CS-APPHOST-BRIEF.md` `[V1]` the precedent) — cross-package disjointness is a row, never prose. Per-page band-neighborhood prose dies for one registry pointer.

### [V5_PAGE_SPLITS_AND_CYCLES]

Two overloaded pages split; the four on-disk intra-package inversions settle. (a) `Element/identity.md` extracts `Element/authority.md` — the object-ACL authz vocabulary (`Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit`+`Effective`+`LapsedFor`, `identity.md:287-341,464-484` — a pure set-algebra with zero `KmsProvider`); identity keeps the relational tier + `IdentityPolicy` + KMS custody + `SchemaVerdict`. The signing+envelope co-location on ONE `KmsProvider` axis is SETTLED (`TASKLOG.md:39` folded `Store/encryption` in and forbade a separate envelope page; `identity.md:279-280` codifies it) — the split is authz-vs-crypto, never signing-vs-envelope; the extraction re-points the seam rows it moves in the same leg — the own `ARCH:53` AppHost PORT row splits, `ObjectAcl` store to `authority`, Tenant-RLS + KMS keyrings staying `identity` (`APPHOST:72`'s KMS-unwrap target unchanged). (b) `Query/lane.md` extracts `Query/retrieval.md` — `FUSION_AND_CACHE` + `VECTOR_CODEBOOK` (`lane.md:267-521`, one coupled ANN subsystem: the fusion vector branch reads the codebook's ADC rows at `:388`) with the `[V4]` typed band; lane drops to routing + set-algebra. The codebook's Persistence residency is settled (Compute imports it downward, `Rasm.Compute/ARCHITECTURE.md:99`); only the page placement moves. (c) The `retention ⇄ blobstore` cycle (`blobstore.md:528` imports the retention GC surface; `retention.md:100,121-124` imports blobstore's `StorageTier`) settles: ruled default — `StorageTier` stays blobstore-owned and legs treat it as a FROZEN vocabulary contract the earlier Version leg consumes; the `Store/tier` neutral-vocabulary extraction wins only if a draft proves a leg-4 owner must amend a frozen vocabulary mid-campaign — preference alone never overturns the documented pivot. The census carries two more edges of the SAME Store-vocabulary class: `recovery.md:175,179,204` composes blobstore's `ObjectStore`/`ObjectStore.Head` for the object-replica RPO probe, and `cypher.md:3` installs through provisioning's `ServerExtension.CreateSql` on the identity migration rail — each an earlier-leg page consuming a leg-4-owned vocabulary. (d) `graph.md:229-232` consumes timetravel's `TimeCut` — same frozen-contract treatment or the AS-OF cut vocabulary re-homes to the store rail. The DECISION rules ONCE for all four, and the frozen-contract ruling binds leg 4 symmetrically: a leg-4 rebuild may not alter a vocabulary an earlier leg consumed frozen (`StorageTier` rows, the `ObjectLeg` `Head` delegate shape, the `ServerExtension` row shape, `TimeCut` cases); a change there reopens the consuming leg as a hard residual.

### [V6_EF_COMMIT_AND_EMBEDDED_BOUNDARY]

The EF read-projection commits properly or dies; ruled default COMMIT: register `UseValueObjectValueConverter()` (Thinktecture.EF) + `UseSnakeCaseNamingConvention()` (EFCore.NamingConventions) on the identity `DbContext`; the hand `NodeId` converter (`identity.md:120`) and the ~40 hand `HasColumnName` calls die; the LanguageExt-type converters (`Option<Vector>`/`Seq<NodeId>`, `identity.md:87-105`) stay — Thinktecture.EF does not own those forms. The migration owner lands: `identity#SCHEMA_VERDICT` already gates on "EF-migration state" (`identity.md:615-643`) with NO authored DDL owner anywhere (`MigrationBuilder` = zero hits; the `Schema/ddl` home is deleted) — the DECISION sites the `element_identity`/`node_cell` DDL authoring on identity's schema section (ruled default) or a dedicated migration page, and `Microsoft.EntityFrameworkCore.Design` earns its admission there. Extension DDL stays data on that one rail: `ServerExtension.CreateSql` rows commit through the identity migration owner (`cypher.md:3` already records the coupling) — never a second DDL executor. The embedded-profile boundary states honestly: Marten is PG-only, so `StoreProfile.Embedded` (SQLite) carries NO ElementGraph event-sourcing path — the DECISION scopes the embedded floor's real charter (relational identity floor + `EngineOps` checkpoint/snapshot/backup lanes, a read-only/offline tier, never the SoR) as page law instead of the current silence. The pgvector EF lane resolves the same way, ruled default MINE: `Pgvector.EntityFrameworkCore` maps the column (`identity.md:87`) while lane's vector search is in-process PQ/ADC — the `<->`/`<=>` LINQ distance path lands as the server-side ANN row on `[V5]`b's retrieval owner (corpus-scale ANN belongs to the pgvector/pgvectorscale indexes, the in-process PQ/ADC row keeps the hot-set lane, residency is row data on the one retrieval axis); the column-mapping-only demotion executes only if the retrieval draft proves the server-side row redundant beside the PQ lane.

### [V7_SCALE_OUT_AND_ORPHAN_PRUNE]

The roster motion executes as a verdict, not a chore — INTEGRATION-FIRST: the six scale-out backends re-dispose under `[V13]` (a provider row on the owning axis, or the per-package redundancy proof — never a zero-consumer default); `PollinationSDK` (charter misplacement), the NTS bridges (no persisted NTS geometry column exists; H3 derivation rides `pocketken.H3`), EF-Sqlite (the embedded floor is raw ADO by design, `provisioning.md:369-434`), and the raw Redis driver (the L2 swap row keeps `Microsoft.Extensions.Caching.StackExchangeRedis`, which transits it) leave manifest + csproj + README groups + `.api` in one motion on their named redundancy/charter proofs, README `[STORE_BACKENDS]`/`[EMBEDDED_KV]` sections re-emitting to the `[V13]` axis map. The SoR-spine seal stands per `[V13]`; a perimeter-axis provider row is a DECISION-level admission through `[V13]`, never a build-leg drift. The one named forward candidate stays recorded: a DuckLake catalog row (a DuckDB/ServerExtension roster row, not a NuGet admission) if the self-hosted lakehouse catalog the `api-deltalake.md` charter referenced is ever chartered.

### [V8_TYPE_REKEY_CORRELATION]

The Materials-leg residual closes store-side, both halves. (a) The consumer contract is RECORDED for the next Materials/Element campaign: the preferred Type-seed amendment EXCLUDES `Classifications` from `NodeId.RootedType`'s seed (as `Representations` already are, `Element/Graph/element.md:40-48`) so derived Type NodeIds never re-key — Persistence names itself the demanding consumer. (b) Regardless, `merge.md` `Reconcile` gains the Type-correlation path NOW: `ExternalKey` (`merge.md:283`) aligns only `ExternalId`-bearing Objects, so ANY re-key event (Type-seed amendment or foreign re-ingest of type definitions) reads as delete-all+insert-all; the fix is a second correlation key row for `ObjectKind.Type` nodes — a stable classification-independent Type natural key (the representation-excluded content seed identity, mirroring the `ExternalKey` shape) — so a re-keyed Type diffs as a RENAME. The executing mechanism for a landed re-key wave stays `identity#IDENTITY_POLICY`'s expand-wave/derivation-flip/contract-wave (`identity.md:189` — "the only identity migration preserving foreign references and AS-OF cursor validity at once"), named as the migration case; occurrence->Type reference re-pointing rides the same wave.

### [V9_INCREMENTAL_OFGRAPH_CONTRACT]

The Element-leg residual is a consumer contract, never a re-home. Hot-path census: `timetravel.md:236` `Scrub` computes `ContentAddress.OfGraph` TWICE per frame (O(N·|graph|) per window); `:258-259` `Bisect` per probe over full reconstructions; `topology.md:76` `TopologyView.Of` per snapshot. `codec.md#CONTENT_ADDRESS` records the contract: a delta-composable `OfGraph(prior, delta)` is the `Rasm.Element/Projection/address` owner's to provide — `Element/Graph/delta.md:55`'s DELTA_MONOID address law is the seam hook — and Persistence composes it on landing; the Scrub reel re-shapes to fold the incremental form (frames already carry deltas — O(N·|delta|)); the whole-graph recompute is documented interim, never the permanent shape. The kernel hasher is never re-homed (`codec.md:157-158` stays law). The geometry-brief `[V2]` parametric content identity rides the SAME seam with a SHARPENED contract this pass demands upstream: the parametric-form digest is a COMPONENT of the parametric node's `ToCanonicalBytes(tolerance)` the seam `ContentAddress.Of(Node)`/`OfGraph` folds (`codec.md:151-152`), NOT merely a standalone key wrapped through `ContentAddress.Of(UInt128)` — otherwise `OfGraph` over a parametric-bearing model is blind to a parametric-body edit and the recovery/federation content-identity proofs (`[V12]` `AsOfKey`, this verdict's `OfGraph`) assert on an incomplete digest (a false green). The `Of(UInt128)` wrap stays the carrier for the separate SpineRef-resolution key only. This is the demand waterfalled into `RASM-CS-GEOMETRY-BRIEF.md:[V2]` with Persistence named as the demanding consumer; Persistence composes the folded digest on landing, zero current Persistence-side edit.

### [V10_VERIFIED_DEFECT_SET]

The confirmed-defect set lands as repairs with the owning page's rebuild, never patches beside it. `recovery.md`: `ObjectReplica` RPO computes the real lag (freshest-replicated-blob age vs newest event instant), killing `Duration.FromMinutes(absent.Count)` (`:181`); the `PgPitr` WAL throughput assumption (`:172`) lifts into a policy row. `commits.md`: `MergeBase` re-shapes to one reverse-reachability generation-mark pass + dominance filter (near-linear), killing the per-candidate `Rank` re-run (`:108-113`); `VectorOrder`/`Order` (`:38,100`) gains its consumer or dies. `ledger.md`: `SyncOpKind.Truncate` (`:31`, zero minters, zero `WholeRelation` readers) wires or dies; `OpLogEntry.Codec` collapses to a `Family`-derived accessor unless a bi-codec lane is named (`:149-153`); `ProcessEventsAsync` batches the projected range into one fold (`:119`). `merge.md`: the `Append` insert arm threads the union-aware STJ options the page's own provenance sibling threads (`:390` vs `provenance.md:266`); the `MemberPath` typing verifies per `[SEAM_AND_RAIL_LAW]`. `blobstore.md`: `MultipartTransfer.Upload`+`BlobTransferReceipt` (`:362,:323`) becomes THE composed receipt path (the `store.Put`/`Placement.Put`/`WriteBlobFirst` callers route through it) or both die; the `ObjectChecksum` column states its S3-only wire honesty or applies per-provider (`:45-49,467` vs the `:16` every-wire claim); `BlobResidence.Correlation` (`:160-161`) populates or dies. `retention.md`: `StorageLane.Durable` (`:36`, unread) consumes or dies. `provenance.md`: `ProvJson` reads the authority's agent class off the `Principal` instead of stamping `Person` unconditionally (`:257`); the mutation-boundary annotation lands (`:243-284`). `columnar.md`: the raw-interpolated `destination`/`stamp`/`store`/`alias`/`root` arguments (`:256,356-361,391`) gain a typed trust gate (validated identifier/path value objects) beside the `DuckDBParameter`-bound values; the four repeated `"80%"`/`"90%"` posture literals collapse to one package constant (`:80-83`). `codec.md`: `HashPolicy`'s three dead rows + `Compute`/`ByDomainId` dispatch (`:215-227`) either become load-bearing (Seal/Verify route through the policy, `:319-321,354-357`) or the roster shrinks to the two live rows with the header byte's forward-compat law stated. `timetravel.md`: `AggregateStreamToLastKnownAsync` (`:16`) composes or leaves the package list.

### [V11_INGEST_GROWTH]

`Ingest/` ends the one-file-folder violation as a real codec-ingress axis: `tabular.md` KEEPS (one rectangular-data owner — MiniExcel spreadsheet + the `Sep` delimited lane it already charters at `:3,5,18,298`, made explicit as owner law rather than sibling prose); NEW `Ingest/schedule.md` — the `MPXJ.Net` schedule-file codec (.mpp/XER/PMXML -> the record rail) plus the durable schedule rows, the Persistence half of the relocated Bim schedule domain (`TASKLOG.md:47` moved the CPM/4D DOMAIN to `Rasm.Bim`; the file codec + durable store is ingest-shaped, the app composition root owns the schedule->element map exactly as tabular law states). Deciding criterion: if drafting finds no durable-schedule consumer within the Bim seam, `MPXJ.Net` leaves the roster with the page unminted and `Ingest/tabular` folds up to a single top-level page — the folder never stays at one file.

### [V12_GOVERNANCE_RECONCILE]

The perimeter becomes machine-checkably true in one motion. Cards: every phantom-realization clause corrects (`IDEAS.md:38,45`, `TASKLOG.md:24` re-anchor per `[V1]`); the six stale `-[COMPLETE]` cards re-point to their real fold owners (transaction->`graph#STORE_RAIL`, quality->the columnar/provisioning verification surfaces, pipeline->`columnar`, egress->`[V3]`'s owner, schemaddl->`[V6]`'s migration owner, icechunk->`timetravel`); `[FABRICATION_PROGRAM_DURABLE_ROWS]` (`IDEAS.md:26-32`) re-anchors off the dead `Schema/ddl#IDENTITY` onto the real owners (blobstore content rows + cache artifact index + the `[V2]`/`Store` growth axis) and carries the forward constraint that its durable receipts decode the re-banded 2701-2710 Fabrication codes, never 25xx (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:679` — the persisted-boundary probe passes today: every persisted receipt carries Persistence-owned bands only), and the row family is KIND-AGNOSTIC: programs, `.cli` layer stacks, 3MF payloads, NC1 text, stock-state snapshots, and travelers land as `ArtifactKind` rows on the one artifact index under one kernel-entry `XxHash128` identity fold (`ContentHash.Of` — the landed fence names the artifact index a composing consumer; RASM-CS-FABRICATION-BRIEF.md `[SEAM_AND_ENTRY_LAW]` the demanding consumer), never per-kind parallel indexes. Ledger: the own `ARCHITECTURE.md#SEAMS` adds the wired-undeclared rows (lane `VECTOR_CODEBOOK`⇄`Compute/Model/embedding`; `cache#L2_CONTRIBUTION`⇄AppHost `CACHE_PORT`; ledger `PRESENCE`←AppHost companion `PeerRoster` beats riding the `DrainSurface` lane, `ledger.md:468` — the beat PRODUCER is `Rasm.AppHost/Wire/companion`'s roster, `DrainSurface` only the in-process transport, driver corrected per RASM-CS-APPHOST-BRIEF.md, and the lane shape crosses as a Persistence-owned wire row, never an AppHost type referenced downward; ledger `CHANGEFEED` op-log ← `Rasm.AppUi` Collab/Editing edit-intent projection — AppUi's collaborative surfaces project typed edit-intent deltas onto `OpLogEntry`/`SyncOpKind` rows and rehydrate on cold load through a per-document replay-window READ, so the op-log owner carries that read case beside the changefeed; the durable form is these Persistence-owned rows, never an opaque Loro-native blob — the `[ROSTER_RECONCILIATION]` LoroCs rejection's other half; AppUi the demanding consumer, RASM-CS-APPUI-BRIEF.md `[V2]`, whose text-container contingency (character-run durable rows where intent granularity provably loses convergence) rides the EXISTING `CrdtOpWire` `RgaSequence` case — zero new wire row; ledger `CHANGEFEED` ← AppHost `Runtime/determinism` neutral log projection plus its origin/sequence-window read-back (RASM-CS-APPHOST-BRIEF.md `[V10]`, AppHost the demanding consumer) — ONE windowed-read case parameterized by origin/entity/window serves the AppUi replay window AND the determinism rehydrate, never two parallel read rails; blob lane ← AppUi `Collab/sync` snapshot-accelerator rows — content-keyed, derivable-class retention, never system-of-record, the same `[V2]` charter) and corrects the declared-unwired three (`:50` icechunk — ruled default: `timetravel.md` gains the `AsOfKey` cross-language as-of content-key arm on its checkpoint chain, re-realizing the `TASKLOG.md:48` claim on a live page, and the arm is RAISED from a governance re-anchor to a load-bearing capability: the checkpoint's content digest (`Checkpoint.Hash`/`Address`, `timetravel.md:84`) IS the `AsOfKey` — ONE digest serving both the icechunk cross-runtime seam (`python:data/gridded/virtual` reads the identical as-of snapshot identity over the one kernel `ContentHash.Of` seed-zero entry per `[SEAM_AND_RAIL_LAW]`) and the recovery flagship's content-identity oracle, never two parallel keys, so `RecoveryPoint.AsCut()` (`recovery.md:65`) resolves the restored coordinate to the checkpoint `AsOfKey` and `RecoveryFault.VerifyFailed(Route, Expected, Found)` (`recovery.md:88`) fires on the reconstructed-vs-checkpoint mismatch — this is what makes `recovery.md:3`'s "re-establishes CONTENT IDENTITY" an assertable proof rather than best-effort completion; the incremental `OfGraph` contract (`[V9]`) is the seam that keeps the AS-OF reconstruction underneath it O(delta), not O(N·graph), across a million-event restore; `:55` — the upstream owner is LANDED (`Rasm/.planning/Spatial/reconciliation.md`: `NamingHashOps.Encode` emits the frozen canonical-adjacency bytes through the kernel hasher, `Reconcile` binds `TopoName`→`GeometryHash` `NameAddress` rows, and the golden single-triangle 52-byte stream + `0x9462A71A…` digest is the cross-package byte-identity fixture both sides assert), so the row re-anchors per the geometry campaign's `[V8]` one-row unification: the adjacency-signature seam feeds `Version/merge`'s diff — the per-node `GraphNode.GeometryHash` the landed page itself names `StructuralMerge` the consumer of — topology's memo stays the Element-seam `OfGraph`, and the kernel ledger row `Rasm/ARCHITECTURE.md:79` (today targeting `Rasm.Persistence/Query`) re-points onto the ruled home as that campaign's counterpart obligation in the DECISION table; `:57` — per `[V1]`). The twelve stale sibling->Persistence seams (Compute `:111,115,116,119`; Bim `:91,95,101,102,104` — the federation, quality, and three Sync-era rows; AppHost `:71,73`, plus `:85` — the `Observability/Health.cs` HEALTH_PROBE row folds over an `Npgsql/Redis/Kafka` driver roster the `[V7]`/`[V3]` prune re-writes) are listed in the DECISION as counterpart obligations with corrected targets — sibling interiors stay out of edit scope. The DECISION additionally states the prepared-transaction disposition explicitly: the single-`IDocumentSession` spine mints no `pg_prepared_xacts` in-doubt set, so the AppHost 2PC drain-row criterion resolves RETIRE (RASM-CS-APPHOST-BRIEF.md `[V11]` the waiting consumer) unless a draft proves a prepared-tx owner on `graph#STORE_RAIL`. Catalogs + manifest close per `[ROSTER_RECONCILIATION]`; README router/groups and `ARCHITECTURE.md` codemap re-emit the final page-set.

### [V13_STORE_AXIS_PARAMETERIZATION]

The store perimeter is PARAMETERIZED BY DESIGN: provider variance is row data on closed axes, and the corpus's own exemplars prove the shape (`ObjectStore` rows s3/azure-blob/gcs/minio, the `StoreProfile` engine axis, the cache L2 swap row, `[V3]`'s sink rows). The DECISION drafts the full AXIS MAP as first-class content — every store-class concern names its owning axis, its provider rows, and its selection policy (rows are deployment/policy DATA, so any app composes any admitted provider through the one axis surface with zero central edits): object stores (rows landed), egress sinks (`[V3]`), read-lane/analytic engines (the DuckDB columnar lane row; a `ClickHouse.Driver` row admissible where in-PG TimescaleDB provably cannot own the scale), lakehouse interchange (`DeltaLake.Net` as a format row where the columnar plane demands the Delta wire), vector search (pgvector/pgvectorscale in-PG rows beside the in-process PQ/ADC lane; a `Qdrant.Client` row only over a proven in-PG ceiling), embedded/KV floors (`rocksdb`/`LightningDB` as `EngineOps`-tier rows only over a proven SQLite-floor ceiling), wide-column (`ScyllaDBCSharpDriver` only with a named owning axis — absent one, the redundancy proof rules). TWO boundaries hold: the SoR spine stays SINGULAR — the Marten/PG event stream + the one `GraphDelta` materializer; a provider row NEVER mints a second event store, second materializer, second identity, or second changefeed (those one-owner prohibitions are load-bearing law, not challengeable seals) — and `ARCHITECTURE.md:115`'s closed relational sweep RE-SCOPES to exactly that spine boundary: a perimeter-axis engine row carrying capability the in-PG/in-process owner cannot reach is a legal DECISION-level admission, never "a new relational engine row" by definition. Every axis row carries its provisioning/health/recovery posture through the existing verification-first fold so a provider row is operable, not merely linkable.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — Phase 1 re-verifies every row against disk. Anchors are `libs/csharp/Rasm.Persistence/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md`; `TASKLOG:`/`IDEAS:`/`README:` the package index docs; `PROPS:` the central manifest; `APPHOST:`/`COMPUTE:`/`BIM:`/`ELEMENT:` those packages' governance docs; `DECISION:` `.archive/RASM-COMPONENT-PARADIGM-DECISION.md`.

| # | Class | Evidence |
|---|---|---|
| E1 | Phantom realization | `IDEAS:38` `FederatedEntity`/`EntityGraph` "is realized", `IDEAS:45` `FederatedPlan` "is realized", `TASKLOG:24` `Query/federation#ENTITY_GRAPH` "is realized" — grep `FederatedEntity\|FederatedPlan\|PlanNode\|SourceKind\|Query/federation` = zero structural hits; stale `-[COMPLETE]` cards `TASKLOG:37,41,42,43,48` cite deleted pages; `TASKLOG:44` claims `SchemaDdl.Sql` landed while `TASKLOG:45` states "there is no `SchemaDdl.Sql` owner" |
| E2 | Orphaned roster | ~30 of ~90 packages pages=0: streaming cluster `PROPS:264-277,297,309`; scale-out tier `PROPS:267,276,285,308,310,311` vs sealed sweep `provisioning.md:378` + `ARCH:115`; `FlowtideDotNet.Substrait` `PROPS:281`; `PollinationSDK` `PROPS:307`; raw Redis `PROPS:314` vs the swap-row-only charter `cache.md:185`; `Ara3D.BimOpenSchema` ships DEBUG-built IL `columnar.md:411` |
| E3 | Catalog drift | `api-duckdb`→`Store/profiles`, `api-arrow`→`Query/rail`+`Sync/egress`, `api-cbor`→`Sync/egress`, `api-zstd`/`api-lz4`/`api-nodatime`/`api-thinktecture-serialization`/`api-messagepack-analyzer`→`Version/snapshots`, `api-highperformance`→`Store/remote`, `api-h3`/`api-sep`/`api-pgvectorscale`/`api-timescaledb`→`Query/lanes` (plural), `api-pg-search`→`Store/server`, `api-apache-age`/`api-pgrouting`/`api-pg-graphql`→`Query/federation` — all dead anchors; 16 catalogs anchor-less; shared `libs/csharp/.api/api-messagepack.md` byte-identical to the package copy, both tagged `RASM_PERSISTENCE` |
| E4 | Band collisions | 8350-8352 `columnar.md:144-146` ≡ `provisioning.md:220-222`; 8360-8363 `cypher.md:68-71` ≡ `lane.md:432-441` (bare `Error.New`); 8370-8373 `tabular.md:104-108` ≡ 8370-8371 `topology.md:179-181` ≡ loose 8371-8375/8379/8380 `provisioning.md:72-74,293-294,321,326`; `GraphFault` simple name ×2 `graph.md:155`/`cypher.md:60`; inconsistent prose neighborhoods `topology.md:166`/`columnar.md:128`/`cypher.md:49` |
| E5 | Coordination gap | `APPHOST:76-79` four PORT rows (fenced Budget debit, step-state CAS, transactional outbox, CAS+lease+membership) with zero Persistence owners; stale `APPHOST:71` `Query/transaction`, `APPHOST:73` `Sync/egress` |
| E6 | Seam-ledger drift | Declared-unwired: `ARCH:50` icechunk `AsOfKey` (landed page deleted, `TASKLOG:48`), `ARCH:55` `Rasm/Spatial/reconciliation` adjacency-derived `GeometryHash` (`topology.md` zero reconciliation refs; upstream LANDED at `Rasm/.planning/Spatial/reconciliation.md` with the counterpart kernel row `Rasm/ARCHITECTURE.md:79`; two-sided with `RASM-CS-GEOMETRY-BRIEF.md` E12), `ARCH:57` Substrait half. Wired-undeclared: lane codebook⇄Compute (`lane.md:388`, `COMPUTE:99`), cache L2⇄AppHost (`cache.md:191-232`, `APPHOST:69`), ledger presence←AppHost drain (`ledger.md:468`). Sibling-stale (counterpart obligations): `COMPUTE:111,115,116,119`; `BIM:91,95,101,102,104`; `APPHOST:71,73`; `APPHOST:85` health-probe driver roster vs the `[V7]`/`[V3]` prune |
| E7 | EF stack | `identity.md:82` converter-less `H3Cell` vs `:120` hand `NodeId` converter (violates `ARCH:113` either way); `EFCore.NamingConventions`/`EntityFrameworkCore.Design`/`EF.Sqlite`/NTS bridges = zero mines; `MigrationBuilder` = zero hits (no DDL owner); embedded profile has no ElementGraph path (Marten PG-only); pgvector EF distance operators unmined beside the in-process PQ scan |
| E8 | Verified logic bugs | `recovery.md:181` count-as-minutes RPO feeding `:151` `MeetsRpo`, `:172` segment-size-as-throughput; `commits.md:108-113` O(common·rank) `MergeBase`; `ledger.md:119` per-event sequential `RunAsync`; `merge.md:390` unthreaded STJ options vs `provenance.md:266`; `merge.md:246-249,402-403`+`timetravel.md:283-285` unverified `MemberPath` NodeId-key typing |
| E9 | Dead carriers | `blobstore.md:362,323` orphaned `Upload`+`BlobTransferReceipt` (no wired caller; `:256,277,548` bypass), `:160-161` `Correlation` never populated; `ledger.md:31` `Truncate` minter-less, `:149-153` `Codec` Family-derivable; `retention.md:36` `StorageLane.Durable` unread; `timetravel.md:16` `AggregateStreamToLastKnownAsync` composed nowhere; `codec.md:215-227` `HashPolicy` Frame/Wide/FrameWide + `Compute`/`ByDomainId` bypassed by static `:319-321,354-357` |
| E10 | Type-rekey gap | `merge.md:283` `ExternalKey` Object-`ExternalId`-only; `ELEMENT` `Graph/element.md:40-48` `RootedType` representation-excluded seed with `Classifications` in the Object shape (`:225`); `identity.md:189` the expand/flip/contract migration mechanism, scenario unnamed |
| E11 | OfGraph hot paths | `timetravel.md:236` Scrub 2×/frame, `:258-259` Bisect per probe; `topology.md:76` per-snapshot memo; `codec.md:157-158` no-second-hasher law; `ELEMENT` `Graph/delta.md:55,253` DELTA_MONOID address-law hook; overload split `timetravel.md:187` `Of(Node, Tolerance)` vs `merge.md:160` span form |
| E12 | Mandate 1+2 clean | Count-prefix: `DECISION:63` amendment + `DECISION:73` "ZERO structural ... the amendment IS the migration"; `codec.md:151-158`/`merge.md:160,173`/`timetravel.md:187-188` compose the seam writer; no node-canonical golden pinned (`commits.md:365-368` parity slots are hlc/commit/crdt/elementset). Re-band: `DECISION:679` gate; no 25xx crosses a persisted boundary (all persisted receipts carry own bands per E4 census); forward constraint on `IDEAS:26-32` (stale `Schema/ddl#IDENTITY` anchor, future rows decode 2701-2710) |
| E13 | Folder + page overload | `Ingest/` one-file folder; `Sep` chartered only as prose sibling `tabular.md:3,5,18,298`; `identity.md` 653 LOC / 4 concerns (`:273-523` AUTHORITY fuses authz+signing+envelope); `lane.md` 521 LOC / 4 concerns (`:267-521`); `retention ⇄ blobstore` cycle `blobstore.md:528` / `retention.md:100,121-124`; `recovery.md:175,179,204` ← blobstore `ObjectStore.Head`; `cypher.md:3` ← provisioning `ServerExtension.CreateSql`; `graph.md:229-232` ← timetravel `TimeCut` |
| E14 | Parameterization unevenness | `columnar.md:256,356-361,391` raw-interpolated identifiers/paths beside `:225-227,271` bound values, no trust gate; `:80-83` four repeated posture literals; `provenance.md:257` unconditional `AgentClass.Person`; `provisioning.md:133` `H3Postgis` fence row vs `README:75` parenthetical (superset holds; the "MUST agree" clause `README:60` untested for the bridge row) |

Sound surfaces (preserve; harden only per the verdict that names them): `graph.md` (10 — the store rail, one materializer, co-txn identity), `codec.md` (10 — minus the E9 HashPolicy ruling), `provisioning.md` (9.5 — minus E4 loose codes), `retention.md` (9.5), `provenance.md` (9.5 — the independent-`digestOf` verify is the federation-wide template recovery already reuses), `commits.md` (9.5 — minus E8 MergeBase), `blobstore.md` (9 — minus E9), `timetravel.md` (9 — minus E11), `ledger.md` (9 — minus E9), `topology.md` (9), `cache.md` (9 — the sync-over-async L2 bridges are the accepted `IDistributedCache` boundary shape; a batching note, never a rebuild; forward consumer pressure: `Rasm.Compute` (`RASM-CS-COMPUTE-BRIEF.md` `[V1]`) lands its assessment `ResultBlob` artifacts as ONE assessment `ArtifactKind` row + retention class on the artifact index — the `cache.md:20` one-row growth law is load-bearing for that consumer and survives any harden), `cypher.md` (8.5 — rename only), `tabular.md` (8 — folder move only), and corpus-wide: the `Rasm.Domain.Expected` fault base, the external-member `.api` discipline, strata one-directionality.

## [03]-[CAPABILITY_ESCALATION]

Target grades are the Phase-2 acceptance bar; the DECISION carries the per-page skeletons that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Element (graph/codec/identity+authority) | 9 | 9.5 | authority extraction; EF commit + snake-case + generated converters; DDL/migration owner; band registry sited; incremental-OfGraph + parametric-digest + streaming-identity consumer contracts recorded; raw-hash mints re-anchored onto the kernel `ContentHash.Of` entry; HashPolicy ruling |
| Version core (ledger/commits/timetravel) | 9 | 9.5 | near-linear MergeBase; dead vocabulary wired-or-dead; batched changefeed fold; `AsOfKey`=`Checkpoint.Hash` as the ONE cross-runtime + recovery content-identity digest; `CRDT_OP_SET` parity fixture pinned by commits; Scrub re-shaped to the incremental contract |
| Version merge | 8.5 | 9.5 | Type-correlation key row; MemberPath typing verified; STJ options threaded; re-key migration case named onto `IdentityPolicy` |
| Version governance (provenance/retention/recovery) | 9 | 9.5 | real RPO lag; WAL-throughput policy row; Principal-derived agent class; `StorageLane.Durable` disposed |
| Query read lanes (lane/topology/columnar/cypher/cache) | 8.5 | 9.5 | retrieval extracted with typed band; `CypherFault` rename; columnar trust gates + posture constant; pgvector LINQ ruling; watermark/receipt law preserved |
| Query retrieval (split) | — | 9.5 | fusion + PQ codebook + `RetrievalFault` band as one ANN owner; Compute codebook seam declared in the ledger |
| Query federation (new) | 0 | 9 | Substrait->`SetExpr` lowering fold; protobuf-wire ingress adapter over the visitor fold (no phantom `Deserialize`); source rows; ADBC execution lane; cut-pinned content-addressed federated result (plan-digest·cut·watermark receipt); signature-locked python:data wire; honest card status |
| Ingest (tabular+schedule) | 6.5 | 9 | folder law satisfied; Sep charter explicit; MPXJ schedule codec + durable rows or the fold-up alternative |
| Store (blobstore/provisioning) | 9 | 9.5 | receipt path composed; checksum honesty; loose codes typed; roster-vs-README bridge row reconciled |
| Store coordination (new) | 0 | 9.5 | token-VALIDATING fenced-lease store (stale-holder write rejected, not just leased); Budget debit = fenced predicated compare-and-decrement (no double-spend across handoff); CAS/membership op-union; named outbox spine + durable cursor; all four AppHost PORT rows homed |
| Changefeed egress (new) | 0 | 9 | one pump over the outbox cursor; exactly-once-effect envelope (`id`=`ContentKey`, `Sequence`=cursor, `partitionkey`=`EntityKey`); sink rows — NATS `Nats-Msg-Id` dedup + Settle-ack advance, pg_net response-reconciled advance (never fire-and-forget); typed dead-letter + replay |
| Governance perimeter (cards/seams/catalogs/manifest) | 3 | 9.5 | zero phantom claims; ledger complete both directions; 77 catalogs live-anchored; zero orphan admissions; band registry type-enforced |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` + the `.Json`/`.MessagePack` adapters as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Eff` rails at every entry, `System.IO.Hashing` `XxHash128` seed-zero as the ONE content hasher reached ONLY through the landed kernel `ContentHash.Of` entry or the seam `ContentAddress` composing it (`[SEAM_AND_RAIL_LAW]`; a Persistence-local raw call site is the deleted form), NodaTime `Instant`/`Duration` under `ClockPolicy`, `Generator.Equals` for value carriers (its `Inequalities`/`MemberPath` diff surface is load-bearing — the `[SEAM_AND_RAIL_LAW]` verify gates it), `Riok.Mapperly` for seam↔wire transcription, `CommunityToolkit.HighPerformance` pooled writers on the blob/codec hot paths, `System.Numerics.Tensors` on the PQ float paths, `QuikGraph` as the ONE in-process graph-view substrate (`api-quikgraph.md:3-8` — topology's `BidirectionalGraph` view, graph's memoized incidence, lane's synchronous case, and cypher's demote-beneath law all ride it; the `[V10]` `MergeBase` generation-mark fold stays hand-owned), and `Microsoft.Extensions.Compliance.Redaction` binding the `DataClassification` taxonomy at every exporter seam (`api-redaction.md:4-11`; seven live pages compose it).
- `Marten` — deepen for `[V2]`: `FetchForWriting` optimistic/exclusive stream locking, `QueueSqlCommand` same-session SQL, the async-daemon coordination surface, `Snapshot<T>(SnapshotLifecycle.Inline)` (`api-marten.md:30,70,83-85,103` carry the rows); the graph/identity/cache/columnar mines stay the reference depth.
- `Npgsql` — `[V2]`/`[V3]`: advisory-lock family (`pg_advisory_xact_lock`) for the fenced lease, `LISTEN`/`NOTIFY` pump wake, the batch-fold pattern provisioning already proves. CATALOG GAP: `api-npgsql.md` documents ZERO advisory-lock/`NOTIFY` members — the leg-1 catalog motion adds those rows with the mine; the already-catalogued `Npgsql.Replication` pgoutput surface (`api-npgsql.md:86-95`) stays recorded-unconsumed — the changefeed is Marten's daemon, logical replication the named escalation if a raw-WAL CDC consumer ever lands.
- `EFCore.NamingConventions` + `Thinktecture.Runtime.Extensions.EntityFrameworkCore10` — `[V6]`: `UseSnakeCaseNamingConvention()`, `UseValueObjectValueConverter()`; the hand-mapping die-off is the mine's proof.
- `Pgvector.EntityFrameworkCore` — `[V6]` ruled default: the `<->`/`<=>` LINQ distance path as the server-side ANN row on the retrieval owner; column-mapping-only is the demotion the retrieval draft must prove.
- `FlowtideDotNet.Substrait` — `[V1]`: the plan IR decode surface (`api-flowtide-substrait.md` re-anchored to the federation owner); paired with `Apache.Arrow.Adbc.Drivers.{Apache,BigQuery}` as federation source rows and `Apache.Arrow.Flight` as the bulk-result hop — all three live or die with the verdict.
- `NATS.Net` — `[V3]`: JetStream durable streams/consumers/acks for the Settle ceremony; the KV/ObjectStore surface stays recorded-unconsumed until a named consumer.
- `CloudNative.CloudEvents`(+`.SystemTextJson`) — `[V3]`: the ONE egress envelope; attribute extensions carry tenant/causal identity from the HLC frame.
- `pg_net` (extension row) — `[V3]`: server-side webhook egress (`net.http_post`) as the zero-client sink row.
- `MPXJ.Net` — `[V11]`: the schedule-file read surface into the record rail; leaves the roster if the page does not mint.
- `AWSSDK.S3` — the `ChecksumAlgorithm.XXHASH128`/`ChecksumType.FULL_OBJECT`/`CompleteMultipartUploadRequest.ChecksumXXHASH128` members are `assay api`-confirmed against the pinned assembly before the blobstore leg closes (`api-objectstore.md:58-59` carries the catalog rows; member existence is the open confirm).
- `AWSSDK.S3`+`Azure.Storage.Blobs`+`Google.Cloud.Storage.V1`+`Minio` — the four-provider `ObjectClient` union to FULL depth (`api-objectstore.md:4`, `api-minio.md:4`): multipart/staged-block/resumable transfer legs, the conditional-write `IfMatch`/`IfNoneMatch` seal, the SSE-KMS/SSE-C request stance, and the WORM object-lock retention members retention classes can pin; `Minio` is the S3-compatible self-hosted fourth leg (MinIO/R2/B2/Wasabi/Ceph) on the SAME placement rows, never a second code path.
- KMS trio `AWSSDK.KeyManagementService`+`Azure.Security.KeyVault.Keys`+`Google.Cloud.Kms.V1` — TWO disjoint arms per provider on the ONE `KmsProvider` axis (`api-aws-kms.md:4-9`, `api-azure-keyvault.md:6`, `api-google-kms.md:4-9`): envelope DEK custody and signing custody; the catalogued asymmetry is law (Azure natively `WrapKey`/`UnwrapKey`, AWS/GCP wrap through the `GenerateDataKey`/`Decrypt` shim; GCP requests carry CRC32C integrity fields); the `[V5]`a split moves authz only — KMS custody stays `identity`.
- `System.Formats.Cbor` — `CborConformanceMode.Canonical` deterministic map-key ordering IS the content-addressing conformance and the bounded `PeekState` loop IS the trust boundary codec already fences (`api-cbor.md:4`); the `CborTag` vocabulary and `Canonical`-vs-`Ctap2Canonical` distinction are the unmined depth.
- `FastCDC.Net` — the gear-hash cut-descriptor chunker for snapshot frames and the multipart window (`api-fastcdc.md:4-11`): `Chunk.Hash` is a 32-bit boundary marker NEVER a dedup identity (`XxHash128` downstream stays the key). Roster honesty recorded: two releases, dormant since 2022-12 (feed + repo verified), MIT/Apache-2.0, no categorical .NET alternative (sub-3k-download field swept); in-corpus absorption of the gear loop is the named escalation if the pin ever blocks.
- `K4os.Compression.LZ4` + `ZstdSharp.Port` — the `CompressionPolicy` axis rows to depth (`api-lz4.md:4-9`, `api-zstd.md:4-14`): LZ4 `Pickler` `IBufferWriter<byte>` sink + chained `ILZ4Encoder` streaming for >1-MiB frames; Zstd trained dictionaries (`DictBuilder.TrainFromBuffer`) for small-blob corpora, the `OperationStatus` incremental engine, `SetPledgedSrcSize` single-frame embedding; NEITHER double-frames MessagePack's in-codec `Lz4BlockArray` — the stub law carries into the fences.
- `Microsoft.AspNetCore.JsonPatch.SystemTextJson` — the RFC 6902 STJ patch document as the `HttpDelta` `OutboundHop` fallback and the member-granular patch wire (`api-jsonpatch.md:4-9`; ledger + merge are the two live consumers); RFC 7386 merge-patch stays the rejected form.
- `ParquetSharp` — three layered surfaces over one native core (`api-parquetsharp.md:4`): the typed `LogicalColumnWriter<T>.WriteBatch` chunk API, the `RowOriented` tuple mapper tabular ingest rides, the `ParquetSharp.Arrow` `FileReader`/`FileWriter` C-Data bridge; `WriterPropertiesBuilder` per-column tuning plus Parquet Modular Encryption (`CryptoFactory`/`IKmsClient`) ULTRA-STACKS onto the admitted KMS trio — encrypted-Parquet egress is a composition, not a gap — distinct from the DuckDB `COPY` lane by design.
- `Speckle.Sdk`+`Speckle.Objects` — DI-resolved `IOperations.Send`/`Receive` (never the nonexistent static form) on `SyncTransport.SpeckleLikeDiff`, the `Send` tuple's `rootObjId` mapping onto the existing `UInt128 ContentKey` (`api-speckle.md:4`; ledger is the one live consumer).
- `Microsoft.Data.Sqlite`+`SQLitePCLRaw.bundle_e_sqlite3` — the `[V6]` embedded-floor charter's substance (`api-sqlite.md:4-9`, `api-sqlitepcl.md:4-9`): `SqliteConnection.Handle` bridges to `sqlite3_snapshot_*`/`sqlite3_wal_checkpoint_v2`/paged `sqlite3_backup_*` for the `EngineOps` checkpoint/snapshot/backup lanes over the bundled 3.50.4 engine.
- PG server-extension tier — every extension is a `ServerExtension` seed-DATA row catalogued serverside-only (`api-h3-pg.md:4-8`; `api-pg-server-bgworkers.md:4-8` pg_cron/pg_partman/pg_squeeze/pg_jsonschema/pgaudit; `api-apache-age.md:4-8`): the re-anchor motion points them at `provisioning#SERVER_EXTENSIONS` plus their LIVE read-lane consumers (`cypher`, never the dead `Query/federation`/`Query/lanes` anchors); `pocketken.H3` keeps the in-process/in-PG cell-parity charter (`api-h3.md:3`) on the corrected core-NTS transitive crossing.
- `MiniExcel` + `Sep` — the `[V11]` rectangular-data owner's two legs with stub anchors (`api-miniexcel.md:4-8` streaming `IDataReader` + shared-strings disk cache; `api-sep.md:3-4` SIMD span-typed columns); `Ara3D.BimOpenSchema`(+`.IO`) is the columnar BIM-analytics egress with its LIVE co-transactional consumer (`api-ara3d-bimopenschema.md:4`, `columnar.md:405-490`) — the `.IO` transitive `Parquet.Net`/`ClosedXML` writers are file-format interop only, never a second managed Parquet lane, and the DEBUG-IL upstream fix (E2) is demanded before the columnar leg closes.
- `Microsoft.Extensions.Caching.Hybrid` — the `IBufferDistributedCache`/`IHybridCacheSerializerFactory` mine (`cache.md:191-232`) stays the reference; the Redis swap row keeps `Microsoft.Extensions.Caching.StackExchangeRedis` only.
- `DuckDB.NET.Data.Full` — the extension-profile + ADBC + `COPY` mines stay; the DuckLake catalog row is the recorded forward candidate; the interpolation trust gate is `[V10]`'s.
- ROSTER rows (ADD/REMOVE/REPLACE under `[ROSTER_RECONCILIATION]`): ADD — none demanded, a VERIFIED verdict: the domain-gap sweep (live feed + two corroborating sources per candidate, license gate enforced) found every scoped concern owned by an admitted package, and the near-misses are recorded so they are never re-litigated blind — `WolverineFx` (MIT, active 6.16.0) REJECTED: its envelope-table durable outbox is a parallel store beside the stream-IS-outbox law and its handler runtime is AppHost's process model; `DistributedLock.Postgres` (MIT) REJECTED: no fencing tokens — `[V2]`'s token-VALIDATING CAS is strictly stronger than any lease library; `MemoryPack` REJECTED: C#-only wire against the frozen msgpack bit-parity law; `Apache.Arrow.Flight.Sql` (Apache-2.0, the admitted Arrow 23.0.0 train) CONDITIONAL-FORWARD: admitted only if the `[V1]` owner ever charters a standards Flight-SQL endpoint for external BI consumers — the ADBC+Flight pair otherwise owns the concern. REMOVE (ruled defaults — each carrying its `[ROSTER_RECONCILIATION]`/`[V13]` redundancy or charter proof argued per-package in the DECISION, never a bare zero-consumer default): `ClickHouse.Driver`, `ScyllaDBCSharpDriver`, `Qdrant.Client`, `DeltaLake.Net`, `rocksdb`, `LightningDB`, `PollinationSDK`, `StackExchange.Redis` (direct ref), `Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite`, `Npgsql.NetTopologySuite`, `NetTopologySuite.IO.GeoJSON4STJ`, `NetTopologySuite.IO.GeoPackage`, `Microsoft.EntityFrameworkCore.Sqlite` — each with its `.api` catalog and README group row. SINK-ROW DEFAULTS (the `[V3]`/`[ROSTER_RECONCILIATION]` streaming law governs this set — each lands as one delivery row over the one pump fold and leaves ONLY on the `[V3]` per-package redundancy proof, a leaving row taking its serdes/codec satellites, `.api` catalog, README group row, and AppHost probe row (RASM-CS-APPHOST-BRIEF.md `[V6]`) with it): `Confluent.Kafka` with `Confluent.SchemaRegistry`(+`.Serdes.Avro`/`.Json`/`.Protobuf`) and `Chr.Avro`(+`.Binary`+`.Confluent`) as the row's codec column, `CloudNative.CloudEvents.Kafka`, `RabbitMQ.Client`, `DotPulsar`. CONDITIONAL: `FlowtideDotNet.Substrait`/`Apache.Arrow.Adbc.Drivers.*`/`Apache.Arrow.Flight` (ride `[V1]`); `MPXJ.Net` (rides `[V11]`); `linq2db.EntityFrameworkCore` (the tabular BulkCopy prose ref becomes a composed fence or it leaves); `Npgsql.OpenTelemetry` (ruled default KEEP per `[ROSTER_RECONCILIATION]` — the provisioning observability row names its tracing+metrics builder subscriptions at the composition root; REMOVE only on draft rejection). KEEP-AND-COMMIT: `EFCore.NamingConventions`, `Microsoft.EntityFrameworkCore.Design`, `Thinktecture.Runtime.Extensions.EntityFrameworkCore10`, `NATS.Net`, `CloudNative.CloudEvents`(+`.SystemTextJson`), `Ara3D.BimOpenSchema`(+`.IO`) (live columnar consumer; DEBUG-IL pressure recorded).
- TASKLOG/IDEAS absorption: the `[V12]` card table disposes every card; the three python-gated BLOCKED cards and the two producer-gated QUEUED cards stay open with corrected anchors and honest status.

## [05]-[BUILD_LEGS]

Phase 2 runs the durable rebuild engine per leg with `RASM-CS-PERSISTENCE-DECISION.md` as brief. The DECISION owns the exact partition, proven acyclic against the corrected seam graph; the four on-disk intra-package inversions (`retention`←blobstore `StorageTier`; `recovery`←blobstore `ObjectStore.Head`; `cypher`←provisioning `ServerExtension`; `graph`←timetravel `TimeCut`) resolve per `[V5]` — frozen vocabulary contracts consumed across legs, or re-homed. Within a leg the listed order IS dependency order, never prose order: leg 3 rebuilds retrieval and columnar before the federation owner that lowers onto them; leg 4 lands `Store/coordination.md` before the egress pump that drains its cursor. The sequencing law:

1. **SPINE + PERIMETER** — the `[V4]` band registry landing COMPLETE (every row of the re-partitioned 83xx map assigned in leg 1 — the `RetrievalFault` band, the new-owner bands (coordination, egress, federation, schedule), and the re-banded provisioning decades included — so a duplicate integer is unrepresentable from the first leg); each union's re-derivation through it lands with its owning page's leg, the `CypherFault` rename executing in leg 3 with cypher's pass; `Element/` (authority extraction `[V5]`a, EF commit + migration owner `[V6]`, codec consumer-contract records `[V9]` + the kernel-entry hash re-anchor per `[SEAM_AND_RAIL_LAW]` + HashPolicy ruling `[V10]`); the FULL roster reconciliation + catalog re-anchor/dedup/stub motion (`[V7]`, `[ROSTER_RECONCILIATION]`) — it gates every later page's imports and catalog claims; the own-ledger seam corrections (`[V12]` ledger half).
2. **VERSION ENGINE** — ledger/commits/timetravel/merge/provenance/retention/recovery `[V10]` repairs; the `AsOfKey` arm; the `CRDT_OP_SET` parity-fixture pin on commits (`[SEAM_AND_RAIL_LAW]`); the `[V8]` Type-correlation path + named migration case; the `[V9]` Scrub/Bisect re-shape to the incremental composition shape (interim documented).
3. **QUERY + INGEST** — `Query/retrieval.md` extraction with its typed band (`[V5]`b); columnar trust gates + posture constant; the `[V1]` federation owner (or the ruled retire executing across cards/ledger/manifest); cache/cypher/topology cold passes; `Ingest/` growth (`[V11]`).
4. **STORE + COORDINATION + EGRESS** — blobstore `[V10]` repairs; provisioning loose-code typing onto the registry; `Store/coordination.md` (`[V2]`); the egress owner (`[V3]`) over the leg-2 changefeed and the leg-4 outbox spine.

Per-leg closeout: hard residuals resolve before the next leg launches; `README.md` router/groups, `ARCHITECTURE.md` codemap/seams, and TASKLOG/IDEAS cards close in the leg that changes their facts; `.api` stubs, catalog re-anchors, and manifest edits land with the leg that admits, removes, or first composes the package; residuals naming a later leg's pages transfer forward explicitly in the leg return, never silently. Campaign acceptance closes on composed contracts, not the ledger: after leg 4, (a) a version-engine dry-run — commit -> three-way merge across a simulated Type re-key reading as RENAME -> AS-OF scrub shaped for the incremental contract -> attested provenance verify -> retention sweep -> verified restore whose terminal proof is a content-identity assertion (the restored coordinate's reconstructed `OfGraph` equals the checkpoint `AsOfKey`, `RecoveryFault.VerifyFailed` unreachable) beside a real RPO lag — composes entirely from rebuilt fences; (b) the AppHost coordination dry-run — a fenced Budget debit whose stale-token replay is REJECTED (a `LeaseFenced` fault, proving the fence is validated not merely issued), a step-state CAS, and an outbox drain through the egress pump to a CloudEvents sink where a redelivered event dedups by `id`=`ContentKey` — composes `[V2]`+`[V3]` fences against the four PORT rows; (c) a federation dry-run — a Substrait plan lowered to `SetExpr` executing on the columnar lane (or the retire alternative proven closed everywhere); (d) the perimeter audit — every fault code resolves to exactly one registry row, every digest mint resolves through the kernel `ContentHash.Of` entry or the seam `ContentAddress` composing it, every catalog anchor resolves to a live page, every card anchor resolves on disk, `dotnet restore` clean after the manifest motion. A dry-run reaching for a missing owner reopens `[V1]`/`[V2]`/`[V3]` as residuals.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Element` interiors: the count-prefix amendment execution, the incremental `OfGraph(prior, delta)` implementation, the `OfGraph`-vs-`Of` naming unification, and the Type-seed Classification exclusion — this brief records Persistence as the demanding consumer (`[V8]`/`[V9]`); the Element/Materials campaigns own the edits. The stale-open `ELEMENT` `TASKLOG.md:20` `[PERSISTENCE_GRAPH_REBUILD]` verify-close is that campaign's action — the realized 18-page corpus IS the rebuild.
- Kernel interiors (`libs/csharp/Rasm/.planning/` + `RASM-CS-GEOMETRY-BRIEF.md`): the `[V2]` parametric canonical-bytes projection and the `[V8]` reconciliation-anchor unification land kernel-side — including the `Rasm/ARCHITECTURE.md:79` ledger-row target correction onto the `[V12]`-ruled home; Persistence consumes the folded digest through the seam `ContentAddress.Of(Node)`/`OfGraph` and re-anchors its own `ARCH:55` row. The `Domain/identity.md` streaming-identity member (`XxHash128` `Append` lifecycle) lands kernel-side on that page's own Growth row — this brief records Persistence as the demanding consumer (`[SEAM_AND_RAIL_LAW]`), never authors it. ONE capability demand stays open on the geometry brief, waterfalled surgically (`[V9]`): the geometry `[V2]` parametric digest must compose INTO the parametric node's `ToCanonicalBytes` the graph address folds, not sit beside it as a standalone key — recorded there with Persistence named as the demanding consumer; the projection implementation stays kernel-side.
- Sibling ledger interiors: the twelve stale Compute/Bim/AppHost seam rows re-point in their own campaigns from the DECISION's counterpart table; AppHost's drain/retry/classification/scheduling laws are consumed as settled vocabulary.
- The CRDT engine stays owned: `LoroCs` is rejected for this package (wire bit-parity law); Speckle transport interiors, `Rasm.Bim` IFC semantics, and Compute's embedding/encode lanes are consumed at their seams only.
- Blocked/queued cards held open by design: `[ARTIFACT_CONTENT_KEY_FEDERATION]`, `[REUSE_WIRE]`, `[SUBSTRAIT_FEDERATION_SEAM]` (python-producer-gated; their realization claims correct per `[V12]`), `[PERSISTENCE_LIBRARY_TABLES]` (Materials content-key pins), `[FABRICATION_PROGRAM_DURABLE_ROWS]` (Fabrication wire pins; 2701-2710 decode constraint recorded).
- The federation-wide fault-band renumber beyond the Persistence registry — escalation path only if the `[V4]` re-partition cannot fit the 5xxx/7xxx/8xxx space it already occupies.
