# [MATERIALS_TASKLOG]

The open and closed work for the host-neutral materials owner, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed — and names the exact sub-domain and file it lands in. One idea spawns one or more tasks; a task is scoped guidance, not a full spec.

## [1]-[OPEN]

[QUEUED] `connection/` sub-domain: the reinforcement/hanger/fastener/anchor catalogue.
- Author `connection/connection` (the `ConnectionItem` `[Union]` over the closed `ConnectionFamily` axis, the `ConnectionId` key, the `ConnectionCatalogue.Build` registered-row fold mirroring `profile#PROFILE_OWNER` `ProfileCatalogue.Build`, the `ConnectionFault` band — band 2360, disjoint from `ProfileFault` 2300 / `ConstructionFault` 2350 / `MaterialFault` 2450), `connection/reinforcement` (the `RebarGrade`/`BarSize` axis, the `RebarSection` ASTM A615/A706 bar-diameter/area record, the ACI 318 standard-bend/hook geometry, the `BuildRebarRows` catalogue), `connection/fastener` (the `FastenerKind` bolt/nut/screw/anchor discriminant, the `FastenerGrade` ISO 898 / SAE J429 proof/tensile axis, the `FastenerSection` thread/diameter/length record, the `BuildFastenerRows` catalogue), and `connection/hanger` (the `HangerType` framing-connector discriminant, the `HangerSection` carried-member/capacity record, the `BuildHangerRows` catalogue).
- No new external package; composes `profile#PROFILE_OWNER` for the catalogue/key/`ProfileStandard` shape, the `Rasm` kernel `PositiveMagnitude`/`Dimension` for every length column, `graph#MATERIAL_LIBRARY` `MaterialId` for the appearance assignment, and `physical-properties#MATERIAL_PROPERTY` `Mechanical` for the capacity receipt; the `connection/layout` placement reads `construction/layout#ASSEMBLY_FOLD` `StationStep` for the station-stepped rebar schedule / fastener pattern.
- Boundary: a new sub-domain sibling of `profiles/`/`construction/`; each item serializes to the IFC 4.3 `IfcReinforcingBar`/`IfcReinforcingMesh`/`IfcMechanicalFastener`/`IfcFastener` element at the `Rasm.Bim` boundary (portable scalar data here, never an `IfcOpenShell` evaluation), and the structural-connection-design seam crosses to the future analysis consumer by `MaterialId`, never re-deriving capacity here.
- Considerations: each item is data, not a type — a new bar size / bolt grade / hanger is one catalogue row; the rebar bend geometry is a scalar bend-angle/radius/hook tuple the host materializes (never a host curve); a closed-set growth is one `ConnectionFamily` case or one row, never a per-item surface. New ARCHITECTURE codemap entries: `connection/` and its four pages.

[QUEUED] `appearance/finish` sub-domain: the Kubelka-Munk pigment/coat-stack finish engine.
- Author `appearance/finish` (one `FinishMix` over the `Pigment[]`/weight Kubelka-Munk construction, the `FinishLayer` primer/base/topcoat coat-stack, the `FinishKind` paint/coating/stain/plaster discriminant, the `Finish.Resolve` fold producing a scene-linear `Unicolour` reflectance plus an OpenPBR coat-stack lowering).
- Integrate Wacton.Unicolour `Pigment`/`new Unicolour(Pigment[], double[])` (the admitted-but-unexploited Kubelka-Munk reflectance ctor) and Wacton.Unicolour.Datasets `ArtistPaint` (the 19-pigment measured reflectance set + its D50 `Configuration`) — the named external-capability gap; compose `bsdf#SPECTRAL_UPSAMPLE` for the SPD round-trip, `graph#MATERIAL_LIBRARY` `NearestChecker` for the ΔE validation, and `bsdf#OPENPBR_SLAB` for the coat-stack→coat-slab lowering.
- Boundary: internal to `appearance/`, a sibling of `texture`/`weathering`; the resolved reflectance seeds a `MaterialLibrary` row `BaseColor` with measured pigment `Provenance`, and the coat stack lowers to the OpenPBR `Coat` slab — never a second color register, never a hand-keyed finish triple.
- Considerations: the pigment mix is a `Pigment[]` weight vector resolving through the one Unicolour ctor (Kubelka-Munk owned by Unicolour, never re-derived); a translucent stain rides the base-substrate transmission, an opaque plaster a high-scattering single-constant pigment; the `.api/api-unicolour-datasets.md` `ArtistPaint` row and the `.api/api-unicolour.md` `Pigment` ctor are already catalogued, so the fences transcribe verified members. New ARCHITECTURE codemap entry: `appearance/finish`.

[QUEUED] Engineering-property aggregation folds in `physical-properties/properties`.
- Land the `AssemblyProperty` aggregation owner: the series-resistance U-value fold (`1/U = Σ t_i/λ_i`) over a `LayerSet`, the constituent-`Fraction`-weighted rule-of-mixtures density/conductivity fold over a `ConstituentSet`, and the mass-law/coincidence layered-STC fold over the wall buildup — each a fold over `construction/assembly#MATERIAL_ASSIGNMENT` reading the per-material `MaterialPropertySet`, producing one `AssemblyProperty` receipt.
- No new external package; composes the realized `assembly#MATERIAL_ASSIGNMENT` `LayerSet.TotalThickness`/`Materials`/`ConstituentSet`, the `properties#MATERIAL_PROPERTY` `Thermal`/`Mechanical`/`Acoustic` cases, and the `UnitsNet` coercion already on the page.
- Boundary: internal to `physical-properties/properties` (a new `[3]` cluster), feeding the IFC `Pset_` aggregate at the `Rasm.Bim` boundary and the `cs:AEC_SIMULATION_BRIDGE` analysis seam by `MaterialId`; never a parallel composite-material owner.
- Considerations: the aggregation is an immutable `Fold` over the assignment's plies/constituents, never a mutable accumulation; an absent ply property rails `MaterialFault.Parameter` rather than defaulting; the `CONSTITUENT_FRACTION` and `LAYER_BUILDUP_GEOMETRY` research probes close here, flipped from probe to realized fold.

[QUEUED] Per-octave-band acoustic spectrum in `physical-properties/properties`.
- Re-shape the `MaterialProperty.Acoustic` case from `(int SoundTransmissionClass, UnitInterval AbsorptionCoefficient)` to the six-band absorption spectrum (`ReadOnlyMemory<double>` over the 125/250/500/1k/2k/4k Hz ISO-354 band centres) plus the per-band sound-reduction-index vector, with `Nrc`/`Saa`/`StcWeighted` as derived projections of the bands.
- No new external package; the band vocabulary mirrors the `bsdf#SPECTRAL_UPSAMPLE` fixed-interval spectral carrier (a fixed-length coefficient buffer), the derived projections expression-bodied folds over the band vector.
- Boundary: internal to `physical-properties/properties`, feeding the `ENGINEERING_PROPERTY_AGGREGATION` layered-STC fold; the band spectrum serializes to the IFC acoustic Pset, host-neutral here.
- Considerations: the band carrier is the one acoustic spectral register, never a scalar collapse; `Nrc` is the four-band average a projection computes, never a stored column; an out-of-`[0,1]` band rails `MaterialFault.Parameter`. This is a breaking re-shape of the realized `Acoustic` case (greenfield, no shim), so the `MaterialPropertyLibrary` `wood.oak` acoustic row migrates to the band vector.

[QUEUED] Exploit the dormant Unicolour gamut/CVD/Pointer surface across `appearance`.
- Wire the admitted-but-unexploited `Unicolour.MapToPointerGamut()`/`IsInPointerGamut` (real-surface gamut, the correct gate for an architectural-pigment reflectance that must be physically reproducible on a painted surface, distinct from the display `IsInRgbGamut`) into the `finish`/`graph#MATERIAL_LIBRARY` admission so a finish reflectance gates against the Pointer real-surface gamut, and expose `Unicolour.Simulate(Cvd, severity)` as a `graph#MATERIAL_LIBRARY` accessibility-preview projection for the color-specification seam.
- Integrate Wacton.Unicolour `MapToPointerGamut`/`IsInPointerGamut`/`MapToMacAdamLimits`/`Simulate` (all catalogued in `.api/api-unicolour.md`, all currently uncalled); no new package.
- Boundary: internal to `appearance/graph` and the new `appearance/finish`; the Pointer gate is the real-surface admission for pigment-mixed materials, the RGB gate stays the display-preview gate — two gamut gates by domain, never one collapse.
- Considerations: a pigment reflectance outside the Pointer gamut is physically unreproducible and rails `MaterialFault.Gamut`, distinct from a display-out-of-gamut preview that chroma-reduces; the CVD simulation is a read-only preview projection, never a stored row.

[ACTIVE] Layout condition-stage detailing depth in `construction/layout`.
- The `StationStep`/`StepCourse` course fold and the four condition stages — opening subtraction (`Opening.Interrupts`), corner closure (`Corner.At`/`ClosureRule.Closer`), arch placement (`Voussoirs` over `RunPath.Arc`), pier solving (`PierClosure`) — plus the `LayerSet` cumulative-thickness per-ply offset (`LayerOffset`/`StackLayers`) are REALIZED at `layout#LAYOUT_STAGE_DEPTH_FILL`; the open work is the residual calibration the page names — the per-region edge-cut-request detailing depth on `Opening` and the multi-leaf corner-bond reconciliation on `Corner` — column growth on the existing condition records, not a re-architecture of the one `ConstructionLayout.Resolve` fold.
- No new external package; composes `masonry#PROFILE_FAMILY` `BondName.Course`/`CourseTemplate` for the course geometry, `assembly#ELEMENT_MODEL` `RunPathAlgebra` for path length/angle, and `assembly#MATERIAL_ASSIGNMENT` `LayerSet`/`ProfileSet` for the resolved buildup; the `Profile.Unit` columns drive the footprint run/rise.
- Boundary: internal to `construction/layout`, a sibling of `assembly`; the resolved `Layout` is portable scalar data the host boundary materializes at the future app root (never a `Rhino.Geometry` type here) and the appearance engine shades through `appearance/graph#MATERIAL_LIBRARY` by `MaterialId`.
- Considerations: each edge-cut/corner-bond column threads the same immutable `StepCursor` and re-folds the projected `Seq<Element>`, never a mutable accumulation; an unsupported voussoir or unreconciled multi-leaf corner rails `ConstructionFault.Course` rather than emitting a degenerate course; a `BondKind.Generated` bond rails `ProfileFault.Bond` through `BondName.Course`, gated on the `masonry#GENERATED_BOND_INTERPRETER` probe.

[QUEUED] cmu, timber, and glazing `ProfileFamily` vocabularies.
- Author `profiles/cmu` (cell/face-shell columns), `profiles/timber` (sawn/glulam/CLT lamella/grade columns), and `profiles/glazing` (IGU pane/spacer/frame columns), each one `ProfileFamily` case with its own `ProfileUnit` projection.
- Integrate the standard cmu cell/face-shell tables, the standard timber/CLT property tables, and the IGU pane/spacer/frame tables as row data; compose `profile#PROFILE_OWNER` and the `Rasm` kernel `Dimension`.
- Boundary: internal to `profiles/`, three sibling pages of `masonry`/`steel`; each member resolves through the same `construction/layout#ASSEMBLY_FOLD`.
- Considerations: each family is data, not a type — a new family is one `ProfileFamily` case carrying its unit vocabulary; the `IfcProfileDef` alignment per family is the wire shape.

[QUEUED] Reconcile the `Rasm.Compute` project reference into `Rasm.Materials.csproj`.
- The README `[PACKAGES]` `[PROJECTS]` manifest lists `Rasm.Compute` (`QuantityFamily`/`UnitAlgebra`/`UnitPolicy`/`UnitEvidence`) and three design pages compose it — `physical-properties/properties#MATERIAL_PROPERTY` `Admit` routes every measured property through `UnitAlgebra.Numeric(value, unit, CanonicalUnit)`, `appearance/photometric#PHOTOMETRIC` coerces the luminous family through the `QuantityFamily` seam, and `appearance/graph` the emission luminance — yet `Rasm.Materials.csproj` declares only the `../Rasm/Rasm.csproj` `ProjectReference`, so the registered-and-composed `Rasm.Compute` dependency is absent from the project manifest.
- Add the `<ProjectReference Include="../Rasm.Compute/Rasm.Compute.csproj" />` to the csproj `ItemGroup` beside the `Rasm` reference; no central-package edit (a `ProjectReference`, not a `PackageReference`), no README change (the manifest already registers it correctly). The dependency direction holds — `Rasm.Compute` sits below `Rasm.Materials` on the AEC-DOMAIN stratum and the kernel-composing unit seam is the documented owner the Materials author-kernel coerces through, never a re-minted unit owner.
- Boundary: csproj packaging integration only — the design pages and the README manifest are already correct; this closes the registry-vs-project-manifest mismatch so the `UnitAlgebra` admission the property/photometric fences transcribe resolves at build.
- Considerations: the `UnitsNet` `PackageReference` stays (the quantity/unit enum owner `Rasm.Compute` itself composes); the missing reference is the `Rasm.Compute` unit-algebra seam, not the raw `UnitsNet` package.

## [2]-[CLOSED]

[COMPLETE] OpenPBR slab-tree reshape of `appearance/bsdf` — realized at `bsdf#OPENPBR_SLAB`: the `SlabKind` axis, the closed `Slab` `[Union]` (fuzz/coat/emission/base), the `SlabStack` outermost-to-base albedo-scaling layering algebra, `SlabStack.Lower` (the one OpenPBR Surface 1.1 construction reading a `MaterialParameters` row and grounding the base substrate through `bsdf#CONDUCTOR_IOR`), and `ToLayered` (the energy-preserving collapse into the `bsdf#LAYERED_COMPOSITION` `LayeredBsdf` weighted-lobe fold the integrator shades), with the `MaterialParameters` columns aligned to the OpenPBR parameter groups and the fuzz slab realized as the `Sheen` lobe at the fuzz position. The `LayeredBsdf` fold is the lowering target by design; an over-unit or non-finite slab weight rails `MaterialFault.Parameter` at `Lower`.

[COMPLETE] MaterialX node-graph interchange for `appearance/graph` and `appearance/texture` — realized at `interchange#MATERIALX_DOCUMENT`: the `MtlxDocument`/`MtlxNode`/`MtlxInput` MaterialX 1.39 shape, the `NodeCategory` `[SmartEnum<string>]` axis mapping the six `AppearanceNode` cases (`constant`/`image`/`multiply`/`mix`/`normalmap`/`open_pbr_surface`) and the `PortValue` polarities onto the MaterialX typed ports, and `Mtlx.FromGraph`/`ToOpenPbr` bidirectional `.mtlx` projection over `System.Xml.Linq` at the host boundary; the `BsdfOutput`→`open_pbr_surface` inputs read the `bsdf#OPENPBR_SLAB` `OpenPbrSurface` columns. Residue: the per-input port-name calibration and host-boundary schema validation are the `[4]-[RESEARCH]` `[MATERIALX_GRAPH_INTERCHANGE]`/`[MTLX_SCHEMA_VALIDATION]` probes, and the `texture#MATERIALX_NODE_PARITY` Worley/color-ramp node-category mapping is the matching `texture` probe.

[COMPLETE] Conductor complex-IOR grounding of `appearance/graph` library rows — realized internal at `bsdf#CONDUCTOR_IOR`: the `ConductorIor` measured `(η, k)` per-RGB-band table (gold/copper/aluminum/silver/iron/chromium/titanium/brass, Johnson-Christy / `refractiveindex.info`) grounds every `metal.*` row's `bsdf#LOBE_FAMILY` `Conductor` lobe from the measured Fresnel via `ConductorIor.Resolve(family, name)`, round-tripping in-gamut through the `bsdf#WHITE_FURNACE_HARNESS` and the `graph#MATERIAL_LIBRARY` `NearestChecker` ΔE seam. Residual [UPSTREAM-BLOCKED]: the EPFL RGL 195-wavelength `.bsdf` measured-BRDF binary admitted through `bsdf#SPECTRAL_UPSAMPLE` `ToSpd` stays gated on a vendored managed `.bsdf` reader at `acquisition#EPFL_RGL_BRDF_LOADER`.

[COMPLETE] Steel `ProfileFamily` vocabulary in `profiles/steel` — the `ProfileFamily.Steel` case with the `SteelClass` `IfcProfileDef`-subtype axis, the `SteelSection` AISC v16.0 section-property record (depth/flange/web/fillet/`Ix`/`Sx`/`Zx`), the `SteelSection.ToUnit` bridge to the canonical `ProfileUnit`, and the `BuildSteelRows` W-shape catalogue folded into `profile#PROFILE_OWNER` `ProfileCatalogue.Build`; the `PositiveMagnitude.Of` factory spelling is the one flagged RESEARCH gap.

[COMPLETE] `StationStep` placement projection in `construction/layout` — the realized station-stepped course fold (the `StepCursor` cursor, per-unit pitch, offset-fraction shift, `FootprintRun`/`FootprintRise` per `Orientation`, closing-cut bat) replacing the empty-`Seq` stub; the four condition stages tracked under the [ACTIVE] layout task.

[COMPLETE] Photometric UnitsNet unit-enum catalogue in `.api/api-unitsnet.md` — the `IlluminanceUnit`/`LuminanceUnit`/`LuminousFluxUnit`/`LuminousIntensityUnit`/`IrradianceUnit` enums and the matching quantity structs are catalogued with their SI-base canonical members, ratifying the `appearance/photometric` fences.

[COMPLETE] Wacton.Unicolour.Datasets admission — the package is admitted in the manifest and `.api/api-unicolour-datasets.md` carries the narrowed surface; exploited by `graph#MATERIAL_LIBRARY` `NearestChecker` (ColorChecker `Macbeth.All` ΔE validation) and `Named` (`Css`/`Xkcd`/`Nord` named-colour resolution), closing the admitted-but-unused under-utilization.

[COMPLETE] Physical-property model in `physical-properties/properties` — the `MaterialProperty` `[Union]` (mechanical/thermal/acoustic/fire) keyed by `MaterialId`, the `MaterialPropertySet` bundle, the `FireRating`/`MaterialPropertyKind` bands, the `UnitsNet`-composing `Admit` coercion, and the `MaterialPropertyLibrary` rows; the dedicated `ThermalConductivityUnit` enums are a flagged `.api` catalogue gap riding proxy units.

[COMPLETE] Weathering-aging operator in `appearance/weathering` — the `Weathering.Apply` fold over the closed `WeatheringEffect` `[Union]` driving a `MaterialParameters` row along an `AgeParameter` through Unicolour `Mix`, re-admitting through `MaterialParameters.Of`.

[COMPLETE] Material-acquisition import in `appearance/acquisition` — the `Acquisition.Import` fold over the closed `CaptureSource` `[Union]` producing a `MaterialParameters` row with measured `Provenance`; the angular-sample fit and field-average arms are internal, the EPFL `.bsdf` binary decode and neural-SVBRDF inference [UPSTREAM-BLOCKED].
