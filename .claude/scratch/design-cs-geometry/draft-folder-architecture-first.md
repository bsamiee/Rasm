# [DRAFT-D4] — FOLDER-ARCHITECTURE-FIRST — RASM-CS-GEOMETRY structural blueprint

Lens: derive the structure from the `[V1]` two-tier ratification FIRST — TIER-1 folders as growth-conducive domain groups, TIER-2 concept namespaces 1:1 with fault sub-bands — then fit every capability, verdict, evidence row, probe ruling, and consumer-gate demand into the ratified structure. The partition is the product; the pages are its rows.

THESIS: seven robust-core-bearing folders (the six landed + a minted `Solving/`), sixteen TIER-2 namespaces of which thirteen fault-routing namespaces map 1:1 onto thirteen clusters (twelve landed strides + the `parametric` 2448-2449 deliberate spend), 28 page rows (17 rebuilds, 2 moves, 9 new pages), both probes ruled AT DECISION TIME (GShark: vendor-and-own NOW, whole set, pin dies, `Parametric/nurbs.md` the owner; implicit-point: the `[V4]` exact path LANDS with the probe's five companion gaps in the same legs), zero upward strata edges, both consumer gates fully homed, every E-row and `[03]` delta disposed.

---

## [01]-[RATIFIED_PARTITION] — the [V1] two-tier structure

### [01a]-[TIER1_FOLDERS]

Folders are ≥2-page domain groups and real growth axes. The landed eight folders keep their landed pages' homes; one folder mints. `ARCHITECTURE.md:[01]` codemap, `README.md` router, and `faults.md` taxonomy update in the wave that changes each fact.

| [INDEX] | [FOLDER] | [CHARTER] | [ROBUST-CORE PAGES] | [LANDED PAGES] | [GROWTH_AXIS] |
|---|---|---|---|---|---|
| [01] | `Numerics/` | Exact-arithmetic floor + the band-2400 fault vocabulary | predicates, faults | atoms, matrix, integrate, spectral, calculus | new exact tiers/filters are ladder rows; new failures are case rows |
| [02] | `Spatial/` | Acceleration structures + topological identity/naming | index, naming, reconciliation | support, cloud, neighbors, transport, fields | new index kinds are `SpatialKind` rows; new entity kinds are `EntityKind` rows |
| [03] | `Meshing/` | The exact discrete lattice: arena substrate, tessellation, crossing, arrangement, offset/skeleton, slicing | edit (NEW), delaunay, intersect, arrangement, offset, skeleton (NEW), slice (NEW) | mesh, dec, reconstruct | new lattice ops are op-union case rows; every store rides the one arena contract |
| [04] | `Processing/` | Predicate-gated mesh processing pipelines | repair, receipts, decimate, flatten, remesh (NEW) | intent, sample, extract, flow, register, geodesics, segment | new heal kernels are `HealOp` rows; new rewrite policies are policy rows |
| [05] | `Solving/` (MINT) | The one damped-Gauss-Newton numeric-solving family | solver (MOVE), fit (MOVE) | — | new residual sources instantiate the one LM functor; new charts are `FitKind` rows |
| [06] | `Parametric/` | The host-neutral parametric/surface-development tier | nurbs (NEW), curve, surface (NEW), subdivide (NEW), develop (NEW), panelize (NEW), patternmap (NEW) | projections, locate | new schemes/tilings/panel families/join styles are data rows, never sibling owners |
| [07] | `Drawing/` | Downstream emission: projection + residency encoding ([V1] ruled charter — passes the growth-axis test; pack stays) | view, pack | — | new view ops are `ViewOp` rows; new residency modalities are `PackKind` rows |

`Analysis/` and `Domain/` stay landed-only. `Processing/remesh.md` sits beside the landed `segment.md` host-capture tier exactly as `[V11]` sites it; `Meshing/skeleton.md` is the growth page of the offset/skeleton concern per `[V10]`d; `Meshing/slice.md` is the intersection concern's growth page per `[V12]`'s slicing-under-intersection ruling.

### [01b]-[TIER2_NAMESPACES] — the 1:1 map

The 1:1 law binds fault-routing namespaces: each owns exactly one sub-band, no namespace borrows, no band splits across namespaces, no namespace spans two folders. Total/pure namespaces route no band and are recorded explicitly — never silently.

| [INDEX] | [NAMESPACE] | [PAGES] | [FOLDER] | [SUB-BAND] | [CASES AFTER REBUILD] |
|---|---|---|---|---|---|
| [01] | `Rasm.Geometry` (root) | faults | Numerics | — (band owner, vocabulary) | owns all 17→re-typed cases + the two-family seam statement |
| [02] | `Rasm.Geometry.Numerics` | predicates | Numerics | — (total/pure, no rail) | — |
| [03] | `Rasm.Geometry.Spatial` | index | Spatial | 2400-2403 `spatial` | `DegenerateInput` 2400 (re-typed: count+witness), `IndexMismatch` 2401, `KindMismatch` 2402 (NEW: query kind × index kind — the [V8] Fin-routed mismatch) |
| [04] | `Rasm.Geometry.Naming` | naming, reconciliation | Spatial | 2404-2407 `naming` (RENAMED from `topology` — the [V1] phantom kill rides this) | `NameCollision` 2404, `HashMismatch` 2405 |
| [05] | `Rasm.Geometry.Meshing` | edit | Meshing | — (total arena; re-admission rides the landed `MeshSpace.Of` substrate rail — the two-family seam in action) | — |
| [06] | `Rasm.Geometry.Arrangement` | delaunay, arrangement | Meshing | 2420-2423 `arrangement` | `DegenerateArrangement` 2420 (re-typed: cellCount+manifoldWitness), `ConstraintUnrecoverable` 2421 (NEW: constraint id+budget — delaunay's home), `DegenerateTessellation` 2422 (NEW: witness simplex) |
| [07] | `Rasm.Geometry.Intersection` | intersect, slice | Meshing | 2424-2427 `intersection` | `IntersectionFault` 2424 (re-typed: PrimitiveKind pair), `NestingAmbiguous` 2425 (NEW: layer index+witness — slice's home) |
| [08] | `Rasm.Geometry.Offsetting` | offset, skeleton | Meshing | 2416-2419 `offsetting` | `DegenerateOffset` 2416 (re-typed), `SkeletonStalled` 2417, `CollapseStalled` 2418 (NEW: MCF iteration+residual — skeleton's home) |
| [09] | `Rasm.Geometry.Healing` | repair, receipts | Processing | 2408-2411 `healing` | `UnrepairableMesh` 2408 (re-typed: kernel+budget+witness), `NativeAssetMissing` 2409 (re-typed: RID+asset) |
| [10] | `Rasm.Geometry.Simplification` | decimate, remesh | Processing | 2440-2443 `simplification` (charter widens: mesh rewrite under budget — QEM reduction + isotropic/quad re-tessellation) | `DecimationFault` 2440, `RemeshStalled` 2441 (NEW: target length+achieved+iterations — remesh's home) |
| [11] | `Rasm.Geometry.Parameterization` | flatten | Processing | 2432-2435 `parameterization` | `ParameterizationFault` 2432 (re-typed: chart id+divergence witness) |
| [12] | `Rasm.Geometry.Constraints` | solver | Solving | 2412-2415 `constraints` | `OverConstrained` 2412, `SingularSystem` 2413 |
| [13] | `Rasm.Geometry.Fitting` | fit | Solving | 2428-2431 `fitting` | `FitFault` 2428 |
| [14] | `Rasm.Geometry.Parametric` | nurbs, curve, surface, subdivide, develop, panelize, patternmap | Parametric | 2448-2449 `parametric` (the DELIBERATE last-stride spend, 13th cluster per `faults.md:19`'s own growth law) | `ParametricFault` 2448 (NEW: op discriminant+detail — construction/evaluation/station/offset), `DevelopmentFault` 2449 (NEW: stage+witness — subdivide/develop/panelize/patternmap) |
| [15] | `Rasm.Geometry.Projection` | view | Drawing | 2436-2439 `projection` | `ProjectionFault` 2436 (re-typed: silhouette/QI witness) |
| [16] | `Rasm.Geometry.Encoding` | pack | Drawing | 2444-2447 `encoding` | `EncodingFault` 2444 |

Census disposal ([V1]): 13 fault-routing namespaces ↔ 13 clusters, bijective. `Tessellation` owns no cluster — RULED: the `Rasm.Geometry.Tessellation` namespace DIES by unification into `Rasm.Geometry.Arrangement` (delaunay + arrangement are one exact-subdivision concept: the CDT substrate and its classification algebra — the Cherchi reference pipeline is literally CDT-per-face; the `Tessellation` [Union] TYPE keeps its name inside the Arrangement namespace); delaunay's fault home is the arrangement cluster (`ConstraintUnrecoverable`/`DegenerateTessellation`), and its generic `DegenerateInput`/`UnrepairableMesh` borrowing DIES. `Parametric` borrows `ParameterizationFault` — RULED: the borrowing ends; the 13th cluster mints at 2448-2449. `[V12]`'s widened-parameterization-stride recommendation is SUPERSEDED with cause: it would force the V2 pages into the Processing-resident `Parameterization` namespace across a folder boundary, breaking the two-tier law — exceeded, not diluted. No federation re-plan needed: the ruled map fits 2400-2449.

Phantom kill ([V1], wave 1, surgical): `Rasm.Geometry.Topology` dies at its exactly-three live sites — `pack.md:33` import, `pack.md:18` packages row, `solver.md:13` owner prose — all re-anchor to `Rasm.Geometry.Naming`; the `topology` cluster renames to `naming` in the same motion; `Rasm.Fabrication/ARCHITECTURE.md:44`'s `BooleanOp` anchor follows the `[V5]` re-home (wave 2 ripple). Namespace declarations mint on the four namespace-less pages in their own waves: predicates (w1), naming (w1), reconciliation (w1), receipts (w3, `Rasm.Geometry.Healing`).

Namespace-unification sweep (delaunay → Arrangement namespace): inbound references in offset.md, repair.md, arrangement.md imports + `ARCHITECTURE.md:[03]` — swept in the pages' own waves; no landed page imports the dying namespace.

---

## [02]-[PROBE_RULINGS] — both hinges ruled from probe evidence

### [02a]-[GSHARK_HINGE] — VENDOR-AND-OWN NOW, whole set, pin dies

Probe verdict FEASIBLE_WITH_GAPS with two ACCESS-MODIFIER WALLS: (G1) `NurbsSurface`'s sole ctor is `internal` with no accessible base ctor — the control-points+knots+weights wire is READ-ONLY for surfaces, killing `surface.md` ingress and SpineRef surface resolution; (G2) surface derivatives are `internal` carrying open order-flip bug #234, and the public API unitizes everything — metric-blind, no fundamental forms. Neither closes in-envelope; the no-re-mint clause (`api-gshark.md:171`) forbids owned re-implementation beside the package; the one-engine law forbids a package+vendored-surface split. RULING: vendor-and-own the WHOLE composed algorithm set in one motion into `Parametric/nurbs.md` (upstream `GSharker/G-Shark@57bfbfb`, MIT, dormancy two-source confirmed 35 months — every open defect frozen permanent). The GShark pin (`PROPS:73`) and reference (`Rasm.csproj:18`) DIE in the wave-1 roster motion; the kernel `api-gshark.md` DELETES (vendor provenance and the fixed-defect roster record in `nurbs.md`; the probe's six catalog corrections are moot-by-deletion, recorded here so silence is never oversight). The vendored motion fixes in one pass: #234 order-flip, #373 RMF closure (policy row), #337 closed/periodic checklist (clamp-at-ingress adapter law), RMF NaN on coincident samples, G7 hardcoded `ClosestParameter` knobs (parameterized policy record), G9 exception boundary (→ `Fin`), G10 raw-domain evaluation (normalize-then-clamp at ingress; canonical bytes hash the NORMALIZED clamped form — one curve, one content key across the Rhino seam); it publicizes ctors (G1) and derivatives (G2), mints fundamental forms + principal/Gaussian/mean curvature as kernel projections over the derivatives (G3), and lands the Piegl-Tiller surface interpolation/approximation lane (G5) that normal-offset refit demands. G4 (the surface-intersection TRIPLE — SSI #127, surface-plane #128, curve-surface #129) stays OUT: host-deferred charter row on `surface.md`, widened from SSI-alone to the full triple per the probe. G8 (curve offset quality) closes by composition: vendored offset as seed + kernel deviation-refinement + trim through the `[V4]` crossing lattice, a `curve.md` op case.

### [02b]-[IMPLICIT_POINT_HINGE] — the [V4] exact path LANDS; no honesty re-scope

Probe verdict FEASIBLE_WITH_GAPS: the mechanism is proven (Attene 2020 indirect predicates; Cherchi 2020 arrangements; Diazzi-Attene 2023 CDT) and the landed lattice shape is correct, but the brief's "the floor already exists" premise is only shape-true. RULING: the exact path lands with the probe's five companion gaps IN THE SAME LEGS, each a case-row/carrier widening on an existing owner: (1) delaunay's vertex payload widens to the closed two-kind row store `Explicit(Point3d) | Crossing(defining input-entity ids)` (SSI-2D: 4, LPI-3D: 5, TPI-3D: 9) under the depth-1 law — implicit rows reference INPUT vertices only, recovery-time constraint×constraint crossings re-express as TPI-3D over the original nine vertices, sub-segment splits re-anchor on the `Constraint` carrier; (2) predicates re-founds its constructions — `Lpi` becomes SSI-2D (four points), a NEW LPI-3D lands (five points, three numerators + λ), `Tpi` re-founds over nine input points with X, Y, AND Z numerators, leaf differences ride error-free transforms, the rounded `Denominator` field DIES, the orient pair gains the negative-λ verdict flip mirroring the in-circum oracle flip; (3) the predicate family widens ~21 members on the ONE `Predicate` owner — `Orient2D` implicit combos {IEE, IIE, III} × axis-projected xy/yz/zx, `LessThanOn{X,Y,Z}` {IE, II} for exact identity/ordering, and an interval-filter tier for EVERY implicit member via the PeterO `ERounding.Floor`/`Ceiling` directed-rounding bracket (killing the fence's false no-filter rationale AND landing the `README:86` interval-filter row in one mechanism); (4) the in-circum question rules MINIMAL: `TessellationPolicy` gains a constrained-only mode (no Delaunay restoration) for implicit-bearing builds — the reference pipeline's exact shape, zero in-circum burden, sufficient for the arrangement's per-face re-mesh; the `InCircle` implicit combos {IEEE..IIII} are a recorded growth row gated on a Delaunay-restoration-over-implicit consumer; (5) `Crossing`/`ImplicitCrossing` carry defining-entity ids (the `bool IsImplicit` dies), interning and chain-merge key on defining entities, `.Round()` becomes the ONE emission-seam materialization, and both pages state the signs-exact/coordinates-rounded-at-emission contract. (6) The 3D CDTet implicit family (orient3d/inSphere multi-implicit) is a recorded growth burden gated on the Compute CDTet consumer. `[V10]`b rides the CHEAPEST sub-family (LPI-3D + projected Orient2D + LessThan, no in-circum, no substrate); `[V10]`c is DECOUPLED — the medial CDT input is a PSLG ring with zero Steiner points, the dual accessor lands as exact DT adjacency + circumcenters materialized at the emission seam, so the `README:99` dual obligation LANDS regardless of implicit machinery and `offset.md:325-326`'s discarded build repairs against it directly.

---

## [03]-[PAGE_SET] — 28 rows

Row schema: PATH | SEMANTIC ACTION → ENGINE LOWERING | CHARTER (TIER-1 folder · TIER-2 namespace · fault cluster) | ENTRY | SEAMS (IN ← / OUT →, one anchor per seam) | WAVE. Landed kernel planes (`matrix`/`neighbors`/`mesh`/`fields`/`geodesics`/`segment`/`identity`/`stats`/`cloud`/`dec`/`reconstruct`/`intent`/`sample`) are ambient floors composed freely in any wave. Engine-lowering vocabulary: kind `new|rebuild|improve`; `deletePages`; `absorb {into, from}` (reserved for delete-with-absorb; donor-block excision without source deletion is charter-carried on both rows).

### [NUMERICS]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Numerics/predicates.md` | REBUILD → {kind: rebuild} | Numerics · `Rasm.Geometry.Numerics` · no band (total/pure). PRESERVE VERBATIM: the four-tier ladder (double filter → `ddouble` 106-bit `Refine` → `Expansion` sign-exact → `Fraction`/`EFloat`/`ERational` four-way oracle), Shewchuk fast-expansion-sum, the FMA `TwoProduct`. LAND: probe re-scope (2)+(3) — construction re-founding (SSI-2D `Lpi`, NEW LPI-3D, nine-point XYZ `Tpi`, EFT leaves, exact-λ orient flip, `Denominator` dies) + ~21-member implicit widening + the PeterO directed-rounding interval tier (README:86); strict-IEEE/RID invariant on `NumericsPolicy`; FMA-free Dekker `TwoProduct` row gated on RID FMA capability (`Splitter` consumed); honest precursor rationale (no maintained NuGet indirect-complete predicate library); constrained-only in-circum ruling recorded; CDTet 3D implicit family a recorded growth row | NONE (vocabulary: `Predicate.*` total static family + `Lpi`/`Lpi3`/`Tpi`/`Expansion` constructions) | IN: ← every robust-core owner; ← `Rasm.Fabrication/Posting` (ARCH:89); ← `Rasm.Compute` CDTet floor (NEW ledger row, [V14]). OUT: → none (floor) | 1 |
| `Numerics/faults.md` | REBUILD → {kind: rebuild} | Numerics · `Rasm.Geometry` root · band owner. Re-type the 10 stringly cases to real payloads (the 7 rich cases the template); land the two-family seam statement (`GeometryFault` band-2400 ⟂ `Rasm.Domain` `Expected`/`Fault`, neither absorbs — ARCH:112 discharged); the ratified [01b] cluster taxonomy IS the page's band plan (naming rename, `KindMismatch` 2402, `ConstraintUnrecoverable` 2421, `DegenerateTessellation` 2422, `NestingAmbiguous` 2425, `CollapseStalled` 2418, `RemeshStalled` 2441, `parametric` 2448-2449); growth law restated: a new failure is a case row in its sibling's sub-band | NONE (vocabulary: `GeometryFault.<Case>(...).ToError()`) | IN: ← every fault-routing owner. OUT: → `Domain/rails.md` two-family seam (counterpart already states its side) | 1 |

### [SPATIAL]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Spatial/index.md` | REBUILD → {kind: rebuild} | Spatial · `Rasm.Geometry.Spatial` · 2400-2403. The `PointCloud` arm EXCISED in one motion (case :118, kind :44, `BuildPointCloud` :151-155, `KdNearest`/`KdRange` :372-381, guards :130,354-359,542,558,573) — point kNN/radius routes the landed `neighbors.md` lane (README:93); zero throws (`Store`/`ToAcceleration` total over {Bvh, LinearOctree}); kind mismatch routes `Fin` + `KindMismatch` 2402, never a silent empty; bounds narrowing rounds OUTWARD (min → −∞, max → +∞, incl. `NodeLinkProjection` :590-591); `ToAcceleration` returns `Fin<(float[] Bounds, long[] Nodes)>` — the kernel-owned wire ARCH:86 declares (`using Rasm.Compute.Solver` DIES); GWN single-owner: index keeps the BVH-accelerated `Winding`; `reconstruct.md` GWN + `fields.md` `SignedDistanceFromMesh` compose it through the declared seam — the ONE distance-field lane; counts resync (:14,:707 → 2 cases, 3 kinds); `CLASH_GOLDEN` kept, flagged harness-confirmed-once; Op-threading + validity fold adopted; preserve verbatim the SAH-BVH/Morton-octree/PLOC build kernels, four query bodies, degradation-refit | `Fin<SpatialIndex> SpatialIndex.Build(BuildPolicy, …)` + `Fin<QueryResult> Query(SpatialQuery)` + `Fin<(float[],long[])> ToAcceleration()` | IN: ← intersect/arrangement broad-phase; ← view occlusion; ← slice; ← `Rasm.Fabrication/Toolpath/guard` (ARCH:84) + `Posting/projection` (ARCH:85). OUT: → ARCH:86 index⇄Compute node-array wire (Compute decodes per its own :121); → landed `reconstruct.md`/`fields.md` GWN-composition seam (declared, landed-page 1-hop) | 1 |
| `Spatial/naming.md` | REBUILD → {kind: rebuild} | Spatial · `Rasm.Geometry.Naming` · 2404-2407. `RebuiltEntity` gains explicit `int Self`; `With`/`Track` key `VertexNames` by `Self` (the E4 star-minimum mis-key dies); migration predicate corrected — containment direction fixed for splits (child ⊆ parent) with a shared-name overlap-fraction fallback row (policy threshold, one predicate); Survive×Migrate ruled: a claimed survivor still seeds sibling `Parent` provenance (lineage preserved; name injectivity holds by fresh-name construction); namespace declared; `topology`→`naming` cluster rename rides [V1] | `Fin<NameTable> Naming.Track(NameTable prior, RebuiltEntity[] rebuilt, Op? key = null)` | IN: ← reconciliation (entities); ← pack (`CanonicalTopology`/`NamingHashOps` re-anchor). OUT: → landed `identity.md` `ContentHash.Of` (qualified) | 1 |
| `Spatial/reconciliation.md` | REBUILD → {kind: rebuild} | Spatial · `Rasm.Geometry.Naming` · 2404-2407. `BuildEntities` emits `Self = v` (star kept for the histogram only); namespace + `using Rasm.Domain` declared (the unqualified `ContentHash.Of` compiles); the Persistence seam unifies to ONE anchor — the ledger row ARCH:79 spelling, page cites it, counterpart `Rasm.Persistence/ARCHITECTURE.md:55` verified in the same wave-1 ripple; `Encode` composition of `ContentHash.Of` PRESERVED (verified landed); `Encode` gains the `ParametricForm` canonical-bytes row (degree · knots NORMALIZED-CLAMPED · weights · control net — the [V2]a identity component `nurbs.md` feeds, a COMPONENT of the Element `ToCanonicalBytes` fold, never a sibling SpineRef key); the `0x9462A71A…` digest flagged harness-confirmed-once with the reproof gate stated | `Fin<ReconcileReceipt> Reconcile(...)` + the `Encode` canonical-bytes projection family | IN: ← naming; ← pack (mesh content key); ← nurbs (`ParametricForm` bytes, w4). OUT: → landed `identity.md` seed-zero entry (ARCH:76-81 family); → `Rasm.Persistence/Query` (ARCH:79, ONE anchor) | 1 |

### [MESHING]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Meshing/edit.md` | NEW → {kind: new} | Meshing · `Rasm.Geometry.Meshing` · no band (total ops; re-admission rides the landed `MeshSpace.Of` substrate rail). The robust-core MUTABLE-ARENA tier beside landed `mesh.md`: absorbs the `MeshEdit` donor block (`repair.md:76-102`) re-founded as a true SoA arena (the immutable-copy-per-op `Arr<T>` form dies — O(V+F)-per-op rebuild cost was the V6 motive); absorbs the dedup weld primitive (`Kernels.WeldDuplicates` + the `WeldTolerance` policy knob) — THE cycle-breaking siting: arrangement's weld dependency lands wave 1, no arrangement→repair edge survives (meshing-lane partition defect closed); absorbs the triangle-soup adapter ×3 (`intersect.md:326`, `view.md:386`, `arrangement.md:352-365`) as ONE owner; STATES the corpus-wide store-mutability + arena-concurrency contract ([SEAM_AND_RAIL_LAW]): SoA stores are mutable single-writer build arenas publishing by freeze; only frozen surfaces feed parallel folds (landed `LaplacianCache` the exemplar); content-addressing binds frozen projections through `reconciliation.Encode`, never a mid-build store; the two-carrier seam stated both sides (`MeshSpace` immutable admission snapshot / `MeshEdit` predicate-gated arena — no third carrier); `CommunityToolkit.HighPerformance` ADMITTED on this composition (`Span2D`/`MemoryOwner<T>` arena backing, `ParallelHelper` on frozen/partition-disjoint spans only) | `MeshEdit.OfMesh(MeshSpace)` / `edit.ToSpace(Context, Op?) → Fin<MeshSpace>` (carrier lifecycle; kernels total) | IN: ← repair/decimate/receipts (working set); ← intersect/arrangement/view (soup); ← remesh (split/collapse/flip arena); ← subdivide. OUT: → landed `mesh.md` (`MeshSpace.Of` re-admission; two-carrier seam counterpart) | 1 |
| `Meshing/delaunay.md` | REBUILD → {kind: rebuild} | Meshing · `Rasm.Geometry.Arrangement` (namespace unification — the `Tessellation` [Union] TYPE keeps its name) · 2420-2423 (`ConstraintUnrecoverable` 2421, `DegenerateTessellation` 2422 — the generic `DegenerateInput`/`UnrepairableMesh` borrowing dies). The seven `[STORE_OPS]` stub bodies transcribed (`Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces` — the hardest transcription in the tree); the hybrid vertex table lands (probe gap 1: `Explicit | Crossing(entity ids)` closed two-kind row store, depth-1 law, `SimplexStore` untouched); `TessellationPolicy` constrained-only mode for implicit-bearing builds (probe gap 4 minimal); the Voronoi-DUAL accessor lands as a `Tessellation` projection (exact DT adjacency + circumcenters at the emission seam — README:99 discharged; [V10]c decoupled per probe); the HULL projection lands ([V11] ruling: lifted-paraboloid lower-hull fold, a Bowyer-Watson byproduct at robust grade for Fabrication stock/clearing envelopes + Generation bounding — tier boundary declared on BOTH pages: this exact tier beside the landed `cloud.md` hull rail, which is STANDING LAW, one anchor each); `SimplexStore` an explicit mutable arena (immutable prose dies); honest rejection rationale (Triangle.NET is MIT; TetGen AGPL stays license-rejected; the load-bearing bar is float-epsilon + no exact CDT/CDTet); Morton insertion order preserved verbatim | `Fin<Tessellation> Tessellation.Build(input, TessellationPolicy, Context, Op?)` + projections `Dual()` / `Hull()` / `ToMesh()` | IN: ← arrangement (`Tessellation.Build`); ← offset (dual, [V10]c); ← repair (CDT re-mesh, [V9]). OUT: → predicates (walk/cavity + implicit family); → edit (soup) | 2 |
| `Meshing/intersect.md` | REBUILD → {kind: rebuild} | Meshing · `Rasm.Geometry.Intersection` · 2424-2427. `Chain` REBUILT on real connectivity (the lane's primary beyond-register defect — today it links only within-segment store slots, emitting disconnected 2-point "Loops"): crossing endpoints key by DEFINING ENTITIES (probe gap 5 — the exact `OrderKey`/`RationalKey` + entity ids already carried), links ACROSS segments sharing a vertex, walks true connectivity; closed loops close ORIENTED (outer CCW / holes CW), non-watertight sections emit typed OPEN-chain rows (`IntersectResult` widens — the [V10]b producer contract); the dead `Next` successor column becomes the walk's live output; `Crossing` carries defining-entity ids, `bool IsImplicit` dies, `.Round()` is the ONE emission materialization, the exactness contract stated; the E7 consumer collapse LANDS BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply`/`Crossing` — no fourth crossing kernel); `SegmentSegment`/`LineLine` typed 2D restriction (the silent Z=0 dies); the three unguarded `QueryResult` casts (:222,:233,:241) route the `Fin` rail; host-delegation re-anchors at BOTH sites {:5, :388} to landed `Analysis/relations.md`; `contract` label earned back by the real chain | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Context, Op?)` | IN: ← arrangement/offset/repair ([V4] collapse); ← slice ([V10]b `PlaneMesh` fold); ← view (Section, [V3]); ← `Rasm.Fabrication/Posting` (ARCH:87 wire). OUT: → predicates (exact tri-tri + LPI-3D constructions); → index (broad-phase); → edit (soup) | 2 |
| `Meshing/arrangement.md` | REBUILD → {kind: rebuild} | Meshing · `Rasm.Geometry.Arrangement` · 2420-2423. `BooleanOp` HOMED here (the smart enum re-homes from `repair.md:57`; ARCH:42 already sites "BooleanOp cell welds" here); `BooleanReceipt` collision resolved: ONE type owned here — receipts' heal-session case COMPOSES it beside `ManifoldStatus`, no second shape; the implicit-point `Constraint` case LANDS (probe ruling: carries the LPI-3D crossing into `Tessellation.Build`; `PlaneCrossPoint` rounding DIES; interning keys on defining entities; README:42 becomes fence-true); Manifold NAMED as the tier-3 scale companion behind a finite `ScaleCeiling` (the `int.MaxValue` dead code dies): in-house P/Invoke over `manifoldc` (elalish/manifold, Apache-2.0; `ManifoldNET` 1.0.7-alpha stalled — not pinned; the NuGet-ID homonym `Manifold` 1.0.0 NAME-AND-REJECTED — a CLI/MCP source-gen framework), golden-boolean fixture gates activation, managed exact arrangement stays the ONE correctness owner ("retires the tier-3 native CSG gate" re-scopes to exactly this meaning); the repair⇄arrangement cycle is DEAD both ways (boolean re-homed here; weld primitive in wave-1 edit.md); store an explicit arena (:488 prose dies); soup + weld route edit.md; the triple `DuplicateNative` materialization collapses with the soup owner; `SegmentOf` keeps interior crossings for coplanar pairs; `AssetGated` semantics feed the receipts fix | `Fin<ArrangementResult> Arrangement.Apply(Arrangement op, ArrangementPolicy, Context, Op?)` + `BooleanOp` smart enum | IN: ← repair (`HealOp.Boolean` delegation — wave 3, backward); ← offset (`PlanarOverlayResolve`); ← view + curve `ToMesh` (PlanarOverlay fill, README:99); ← `Rasm.Fabrication/Posting/projection` (ARCH:88 wire; FAB:44 re-anchor ripple). OUT: → delaunay (`Tessellation.Build` + implicit `Constraint`); → intersect (`Crossing` lattice); → index (`Winding`/`Overlap`); → edit (weld + soup); → faults | 2 |
| `Meshing/offset.md` | REBUILD → {kind: rebuild} | Meshing · `Rasm.Geometry.Offsetting` · 2416-2419. The medial made REAL: `MedialFrom`'s discarded build dies — the medial reconciles against delaunay's `Dual()` accessor (exact adjacency + circumcenters), parabolic arcs at reflex vertices land per the page's own prose; the per-point CLEARANCE-RADIUS function + the arbitrary-probe clearance query land as FIRST-CLASS result fields (the [V10]c/d mandate — a radius-less medial is a [V10] defect; `FAB:22`'s "wavefront clearance-radius field" is the consuming demand); corner-strategy rows: `JoinType`(Miter/Round/Bevel/Square)×`EndType` over the ONE exact wavefront assembly ([V10]a; CavalierContours kerf/arc stays the Fabrication stratum per README:99); `MinkowskiConvolution` COMPLETED (vertex arc-fans + both convolution directions — capability extended, never re-scoped); `WavefrontStore` arena honesty (:5 immutable prose dies) + growth-safe sizing (the 2n under-allocation dies — split-heavy skeletons spawn past 2n); `SegmentsCross` dies for `Intersection.Apply` ([V4]); `PlanarOverlayResolve` feeds the arrangement segment constraints directly (the `Mesh.CreateFromClosedPolyline` self-defeat on self-intersecting loops dies); `Weighted` per-edge speed row preserved | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Context, Op?)` (JoinType×EndType as data rows) | IN: ← skeleton (concern-adjacent growth page); ← Fabrication kerf lane (composes the exact skeleton + corner rows + clearance radius through the seam). OUT: → delaunay (`Dual()`); → intersect (crossing straddles); → arrangement (`PlanarOverlay`); → predicates (reflex/collapse gates) | 2 |
| `Meshing/skeleton.md` | NEW → {kind: new} | Meshing · `Rasm.Geometry.Offsetting` · 2416-2419 (`CollapseStalled` 2418). Author-kernel 3D mean-curvature-flow curve-skeletonization composing the LANDED machinery (`geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow` case — no permissive engine exists; CGAL GPL rejected); carries the per-point clearance RADIUS and the arbitrary-probe clearance query as first-class result fields (the shared [V10]c+d payload mandate, sharpened to `RASM-CS-FABRICATION-BRIEF [V5]`'s arbitrary-probe + weighted/variable-speed rows — seeded as policy rows here); skeleton topology emits as a kernel-owned SoA wire (QuikGraph serves in-kernel combinatorial computation only, per the landed `neighbors.md` lane law); `Rasm.Fabrication/Toolpath/Skeleton.cs` dies unconditionally (FAB:22 ripple) | `Fin<SkeletonResult> Skeleton.Apply(SkeletonOp, Context, Op?)` | IN: ← `Rasm.Fabrication/Toolpath` (clearance-radius + probe query seam — NEW ledger row; FAB:22 counterpart edit). OUT: → landed `geodesics.md` MCF + `fields.md` case (declared seam); → edit (arena); → index (proximity) | 4 |
| `Meshing/slice.md` | NEW → {kind: new} | Meshing · `Rasm.Geometry.Intersection` · 2424-2427 (`NestingAmbiguous` 2425). The slice-stack owner: ONE section fold of `IntersectOp.PlaneMesh` over a parallel-plane family → per-layer contour-nesting FORESTS; layer-height policy rows (uniform + adaptive as seed DATA — the family OPEN: Fabrication's variable-by-slope + support-interface land as rows per `RASM-CS-FABRICATION-BRIEF [V6]`, never new arms); contours ORIENTED (outer CCW / holes CW — the downstream arc-offset convention) and non-watertight sections emit the typed OPEN-chain rows intersect's widened result carries — never silent closure; orientation/nesting decided EXACTLY on the cheapest implicit sub-family (probe [V10]b: LPI-3D + projected `Orient2D` + `LessThan` ordering — no in-circum, no substrate); nesting forest emits as a kernel-owned SoA wire; consumers: Fabrication `Toolpath/slicing` re-route (FAB:48 promise resolves; `Slicing.cs` dies FAB:23) and the Compute circulation runner (story-elevation floor plates, `RASM-CS-COMPUTE-BRIEF [V12]`a — decoded through the wire) | `Fin<SliceStack> Slicing.Apply(SliceOp, LayerPolicy, Context, Op?)` | IN: ← `Rasm.Fabrication/Toolpath/slicing` (FAB:48/23 ripples); ← `Rasm.Compute` circulation (NEW ledger row). OUT: → intersect (`PlaneMesh` fold + open-chain rows); → predicates (implicit ordering sub-family); → index (plane broad-phase) | 4 |

### [PROCESSING]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Processing/repair.md` | REBUILD → {kind: rebuild} | Processing · `Rasm.Geometry.Healing` · 2408-2411. Donor excisions stated: `MeshEdit` block (:76-102) → wave-1 `edit.md`; `BooleanOp` smart enum (:57-62) → wave-2 arrangement (`HealOp.Boolean` stays as a thin DELEGATION case composing `Arrangement.Apply(MeshBoolean, …)` — the T-BOOLEAN card's own framing); the four crossing kernels (:346-372) die for `Intersection.Apply` ([V4]); `SelfIntersectResolve` becomes a REAL substrate CDT re-mesh (segment-not-point, both faces patched, multi-crossings kept — the `patched.ContainsKey` drop dies); the TOTAL topology status lands: the heal rail composes a Genus-TOLERANT projection — ONE `TopologyReceipt.Project` `ProjectionRow` extension on landed `mesh.md` exposing `(Euler, BoundaryComponents, IsManifold, IsOriented, NonManifoldEdges)` without the genus gate (declared landed-page Growth extension, [LANDED_KERNEL_LAW] (c)) — killing the severest lane defect (the heal rail failing on exactly the defective-mesh classes it exists to repair); topology threads forward (`before[n] = after[n-1]`, the 2N recompute dies); the `((QueryResult.Hits))` hard cast routes the rail; `GapClose` pairs on DIRECTED boundary edges (orientation-reliable bridging); `api-rhino` truth: repair composes `RebuildNormals` only; preserve verbatim the `HealOp`/`Standard` order, the `DegenerateCollapse`/`DominantAxis` sliver logic, the weld idempotence law (now composed from edit.md) | `Fin<HealSession> Heal.Repair(MeshSpace, RepairPolicy, Context, Op?)` | IN: ← receipts (session vocabulary); ← `Rasm.Bim` heal consumers (dormant). OUT: → edit (arena + weld); → arrangement (boolean delegation); → intersect (crossings); → delaunay (CDT re-mesh); → landed `mesh.md` (topology `ProjectionRow` — declared extension); → landed `intent.md:214` (`Topology` projection, preserved) | 3 |
| `Processing/receipts.md` | REBUILD → {kind: rebuild} | Processing · `Rasm.Geometry.Healing` · 2408-2411. Namespace MINTED (`Rasm.Geometry.Healing` — the fourth namespace-less page); `Converged` fixed: a boolean's convergence is its topological SUCCESS — the tier-3 gate flag leaves the convergence fold entirely (the `:99 b.AssetGated` inversion and the category error both die); the boolean case COMPOSES arrangement's `BooleanReceipt` + `ManifoldStatus` pair (the name collision resolves to one type, [V5]); receipts REGISTER into the landed `IValidityEvidence` fold — the hand-rolled `Converged`/`Improved` guards die (the dual paradigm ends here first); `manifoldRepair.Improved` semantics corrected (edge-split legitimately increases boundary components); the status pair rides the wave-3 Genus-tolerant projection; preserve the `RebuildReceipt` [Union] (7 cases), `Of` mint discrimination, `Affected` re-anchor seed, `ToLog` fold | NONE (vocabulary: `RebuildReceipt.Of` mint + `HealSession`/`RebuildLog` folds registering `IValidityEvidence`) | IN: ← repair (mint per op); ← naming (`Affected` re-anchor seed). OUT: → arrangement (`BooleanReceipt` composition); → landed `validation.md` validity fold; → landed `intent.md:214` (preserved) | 3 |
| `Processing/decimate.md` | REBUILD → {kind: rebuild} | Processing · `Rasm.Geometry.Simplification` · 2440-2443. The collapse-flip guard made REAL (the `Orient3D(face, face+normal)` identity — always `Positive`, the headline claim inert — dies for a true orientation test: exact `Orient3D` of the moved apex against each incident face's ORIGINAL supporting plane); boundary collapse admitted (`shared == 1` at the link gate — open meshes reach `TargetFraction`); the maintained vertex-vertex `Adjacency` extends to a vertex→incident-face incidence index feeding `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` (the O(F²) drain becomes O(F log F)); the PHANTOM `field.IsoSurfaceDetailed` dies for the real static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key)` (landed `reconstruct.md:609`; prose sweep :3-:560); the second unguarded `(QueryResult.Nearest)` cast (:459) routes the rail; `ddouble` QEM quadric accumulation preserved and named as the README:86 row; `MeshEdit` from the wave-1 arena; `VoxelRemesh` stays a distinct row beside wave-4 remesh (boundary stated); meshoptimizer/Compute stratum split recorded; preserve verbatim the lazy version-skip queue, `matrix.md`-routed `OptimalPosition` (the [V7] exemplar), the BVH Hausdorff bound | `Fin<SimplifyResult> Simplification.Apply(SimplifyOp, Context, Op?)` | IN: ← reality-capture pipeline (dry-run d). OUT: → edit (arena); → landed `matrix.md` (`CholeskyResult.SolveDetailed`); → landed `reconstruct.md` (`IsoSurface.Detailed` seam); → index (Hausdorff BVH); → predicates (flip guard) | 3 |
| `Processing/flatten.md` | REBUILD → {kind: rebuild} | Processing · `Rasm.Geometry.Parameterization` · 2432-2435. Dirichlet pin CORRECTED (large penalty P on pinned diagonals ONLY with `rhs = P·value` — the inverted 1e-9-everywhere + value·1e9 form dies; interior harmonic/BFF solve valid before `Scatter`); LSCM assembled SPARSE as the conformal-energy least-squares smallest-eigenpair through the landed `matrix.md` sparse owners (the dense `(2n)²` + `DecomposeSvd` O(n³) dies; the :3 sparse prose becomes true); ARAP global assembly O(F) (the in-scope face index `f` passes through; `IndexOf`'s O(F) scan + return-0-on-miss dies); `FactorNonZeros` reports the actual Cholesky FACTOR fill via the solve rail (the wrong-operator `D1.NonZeros` dies); `ChartStore` `Dead`/`FreeList`/`Kill` DELETED (BFS island labelling is the mechanism); `OrderLoop` walks DIRECTED boundary edges (the min-keyed `next` overwrite dies); dense-LA framing unified to the landed `matrix.md` owner law (one law, one vocabulary); the `DiscreteCalculus` sparse-shape assumption re-verified against landed `spectral.md:191`; the developable seam to wave-4 `develop.md` declared; preserve verbatim the `MeshAdjointSnapshot.Of`/DEC composition, per-face Jacobian-SVD distortion scoring, exact `Orient2D` flip guard, seam-cut island walk | `Fin<ChartAtlas> Parameterization.Apply(ParamOp, Context, Op?)` | IN: ← develop ([V2], w4); ← `Rasm.Fabrication/Nesting/nfp` (ARCH:94) + `Rasm.AppUi/Render` (ARCH:95). OUT: → landed `matrix.md` (sparse eigenpair + factor); → landed `mesh.md`/`dec.md` (adjoint snapshot); → landed `spectral.md` (shape verify) | 3 |
| `Processing/remesh.md` | NEW → {kind: new} | Processing · `Rasm.Geometry.Simplification` · 2440-2443 (`RemeshStalled` 2441). The author-kernel remeshing substrate BESIDE the landed `segment.md` host-capture tier (coexisting per the capture law, one anchor each side): Botsch-Kobbelt isotropic remesh (split/collapse/flip/tangential-relax, genus-preserving) over the wave-1 arena; cross-field-guided QUAD remesh rows LAND composing the landed `segment.md` Knöppel cross-fields (the [V11] deferral valve is NOT taken — panelize and Fabrication mesh-prep both gate on the substrate, Compute names it the boundary-conditioning pre-step; hardening law: land, never defer); `decimate.md` `VoxelRemesh` boundary stated (distinct row, its page); the namespace's widened charter (mesh rewrite under budget) recorded on both simplification pages | `Fin<RemeshResult> Remeshing.Apply(RemeshOp, RemeshPolicy, Context, Op?)` (isotropic + quad as rows) | IN: ← panelize ([V2], w4 backward); ← Fabrication mesh-prep (growth); ← Compute volumetric pre-step (recorded growth, decoded through the wire — `RASM-CS-COMPUTE-BRIEF [V7]`). OUT: → edit (arena rewrite); → landed `segment.md` (cross-field guidance, declared seam); → landed `mesh.md` (quality metrics) | 4 |

### [SOLVING] (minted folder)

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Solving/solver.md` | MOVE (rebuild-grade) → {kind: new, absorb: {into: `Solving/solver.md`, from: `Processing/solver.md`}, deletePages: [`Processing/solver.md`]} | Solving · `Rasm.Geometry.Constraints` · 2412-2415. The ONE damped-Gauss-Newton owner: `Iterate`/`Step` generalizes to a residual+Jacobian FUNCTOR over the existing `SolvePolicy` λ-ladder (`LambdaUp`/`LambdaDown`/`StepFloor`/`InitialLambda` — the policy record already exists; only the residual source abstracts); `Constraint` linearization becomes the first functor instantiation; the `ddouble` Σd² objective accumulation lands (README:86); the phantom `Rasm.Geometry.Topology` owner prose (:13) dies → `Rasm.Geometry.Naming` (`EntityKind` home); Op-threading + validity fold adopted; preserve verbatim the `LambdaCeiling` singular guard, `SolveReceipt` all-finite gate, `WitnessAnalyze` DOF refinement, bounded reject chain, the deliberate `ConstraintSolveReceipt` name distinction | `Fin<Solution> ConstraintSolver.Solve(ConstraintSystem, SolvePolicy, Context, Op?)` + the generalized LM functor (one engine, internal) | IN: ← fit (functor instantiation, [V7]); ← `Rasm.Generation` form-finding (flagged growth, not built here). OUT: → landed `matrix.md` (the ONE dense/sparse boundary — the `COMPUTE:120` spurious reverse seam dies in wave 1) | 3 |
| `Solving/fit.md` | MOVE (rebuild-grade) → {kind: new, absorb: {into: `Solving/fit.md`, from: `Processing/fit.md`}, deletePages: [`Processing/fit.md`]} | Solving · `Rasm.Geometry.Fitting` · 2428-2431. `Refine` INSTANTIATES the solver functor (the full LM re-implementation `:560-627` dies — the `:740` reuse claim becomes true); λ THREADS through `RefineState` on the policy ladder (the hardcoded 1e-3 seed/×10 climb/1e12 ceiling and the reset-every-outer-iteration die); `FitStore` DELETED (`Candidate` is the live consensus carrier; prose + density bar resync); MLESAC seeded draw + range prefilter ride the landed `neighbors.md` kd-tree lane (README:93; the built-never-queried O(n log n) BVH dies; landed `register.md` the exemplar); the `Line` chart de-degenerates (foot-of-perpendicular anchor as `PackPlane` does; `MinimalSamples` 3→2 resyncing `MinimalLine`); cloud-PCA vocabulary composes the landed `cloud.md` `CloudKernel`/`stats.md` `SampleMoment` owners (consumer, never a second covariance); the `ddouble` Σd² objective; the broken `:18` Packages line repaired; preserve verbatim the MLESAC/PROSAC truncated-cost sampler, adaptive budget, six closed-form minimal solvers, analytic per-kind `Distance`/`Gradient` arms | `Fin<FitResult> Fitting.Apply(FitOp, FitPolicy, Context, Op?)` | IN: ← `Rasm.Bim` reconstruction (`FitReceipt` consumer, dormant); ← reality-capture dry-run (d). OUT: → solver (functor); → landed `neighbors.md` (kd-tree lane); → landed `cloud.md`/`stats.md` (PCA vocabulary) | 3 |

### [PARAMETRIC]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Parametric/nurbs.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. The vendored-and-owned NURBS engine ([02a] ruling — ONE engine in-kernel, the GShark pin dies): NURBS-Book evaluation both carriers; `RationalDerivatives` PUBLIC with #234 order-flip FIXED; fundamental forms + principal/Gaussian/mean curvature as kernel projections over the derivatives (G3); arbitrary-knot construction PUBLIC for curve AND surface (G1 — the wire ingress: control points + knots + weights); knot algebra (`Refine`/`Normalize`/multiplicity/clamp) with the INGRESS LAW: normalize-then-clamp before construction, canonical bytes hash the normalized-clamped form (G6/G10 — one curve, one content key); the Bezier-decomposed Gauss-Legendre arc-length engine at 1e-6 (the station family's substrate); Wang-2008 double-reflection RMF with the closure-correction policy row (#373) and the coincident-sample guard; `ClosestParameter` knobs PARAMETERIZED as a projection policy record (G7 — foreign-point fallback grade stated); Piegl-Tiller surface interpolation/approximation lane (G5); `Fitting.Curve` Interpolated/Approximate; exception boundary → `Fin` + `GeometryFault` (G9); vendor provenance + fixed-defect roster recorded (GSharker/G-Shark@57bfbfb, MIT, #234/#337/#373/#391) | `Fin<NurbsForm> Nurbs.Of(NurbsWire)` (one polymorphic admission over curve/surface wire; evaluation members on the carriers) | IN: ← curve, surface (the op rails); ← host wire (Rhino control points + knots + weights, meeting at the wire per [PLACEMENT_LAW]). OUT: → reconciliation (`ParametricForm` canonical bytes → landed `identity.md` — the [V2]a identity chain) | 4 |
| `Parametric/curve.md` | REBUILD → {kind: rebuild} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449 (the borrowed `ParameterizationFault` ends). Full-body rebuild composing `nurbs.md` (the six stubs die); the STATION/FRAME family lands as first-class `ParametricOp` cases: arc-length stations (`Divide` equal-length/equal-count, `ParameterAtLength`, `PointAtLength`) SWEEPING rotation-minimizing `PerpendicularFrames` over the SpineRef `[T0,T1]` window (`SubCurve` + normalized domain) — emitting `(station, RMF frame)` PAIRS, the direct `PathRow`/`Placement` producer (`RASM-GENERATION-SPEC:138,164,173`); curve `Offset` PROMOTED to a first-class case (vendored offset seed + kernel deviation-refinement + self-intersection trim through the [V4] crossing lattice — G8); the retired-source citations re-anchor TWO-TARGET at all four sites {:3,:20,:170,:190}: parametric-evaluation peer → landed `projections.md`, intersection → landed `relations.md` (the blanket relations-only re-anchor would mis-home the evaluation edge); the anomalous `using Rhino.Geometry` dies (host-neutral thesis holds); `ToMesh` trimmed-surface fill keeps routing arrangement `PlanarOverlay`; canonical-bytes identity emitted through `reconciliation.Encode` `ParametricForm` | `Fin<...> Parametric.Apply(ParametricOp, Context, Op?)` (Evaluate/Measure/Divide/Stations/Split/Offset/Reconstruct/Intersect cases) | IN: ← `Rasm.Generation` SpineRef stations (NEW ledger fan row); ← landed `projections.md`/`locate.md` (boundary counterparts, reciprocal statements land). OUT: → nurbs (evaluation); → intersect (offset trim); → reconciliation (identity); → arrangement (fill) | 4 |
| `Parametric/surface.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. Host-neutral surface ops over `nurbs.md`: the UV-GRID SAMPLER emitting UV-PROVENANCED tessellation (per-vertex `(u,v)` beside the `MeshSpace` — the kernel-owned sampler the probe confirms is a kernel duty, and the [V2]b pullback law: every field/geodesic result pulls back to the parametric domain BY CONSTRUCTION; `ClosestParameter` is the rare foreign-point fallback only); isoline FAMILIES (fold over `IsoCurve` + public `KnotVector.Refine`); geodesic POLYLINES on parametric surfaces (landed `fields.md` `Geodesic` case + `geodesics.md` heat/MMP through the UV pullback); NORMAL-OFFSET surfaces (Greville-abscissae normal offset + the vendored refit lane — G5 closed, capability landed live, not re-scoped to tessellation); curvature-domain sampling (fundamental forms from nurbs derivatives — G3); the SSI disposition row: the surface-intersection TRIPLE (SSI + surface-plane + curve-surface) is HOST-DEFERRED (probe-widened) — slice rides mesh `PlaneMesh`, isolines ride knot refinement, neither needs the triple; SpineRef surface resolution rides the G1 arbitrary-knot ingress | `Fin<...> Surfacing.Apply(SurfaceOp, Context, Op?)` (SampleGrid/Isolines/Geodesic/OffsetNormal/CurvatureSample cases) | IN: ← panelize + patternmap (UV-provenanced input binding — a world polyline with no surface binding is rejected); ← `Rasm.Generation` isoline/geodesic/offset gate items (fan row). OUT: → nurbs (evaluation + derivatives + refit); → landed `fields.md`/`geodesics.md` (declared seams); → landed `mesh.md` (tessellation carrier) | 4 |
| `Parametric/subdivide.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. Catmull-Clark + Loop as STENCIL ROWS over ONE refinement fold ([GENERATOR_LAW]: the next scheme is a data row, never a sibling subdivider); limit-surface evaluation; operates on the wave-1 arena/`MeshSpace`; the OpenSubdiv admission stays REJECTED-until-proven (no managed binding; admission = in-house P/Invoke, the Manifold class of burden — recorded so silence is never oversight) | `Fin<SubdivisionResult> Subdivision.Apply(SubdivideOp, Context, Op?)` (scheme stencil rows) | IN: ← `Rasm.Generation` region-subdivision gate item (fan row). OUT: → edit (arena refinement); → landed `mesh.md` (carrier) | 4 |
| `Parametric/develop.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. GUARANTEED-ISOMETRIC developable-strip unroll with the isometry WITNESS receipt (the acceptance dry-run names it) — distinct from flatten's low-distortion conformal maps AND segment.md's host LSCM capture (tier boundaries stated, one anchor each); composes wave-3 `flatten` `ChartAtlas` + landed `geodesics.md`; feeds Fabrication sheet/plywood/fabric | `Fin<DevelopResult> Development.Apply(DevelopOp, Context, Op?)` (isometry witness first-class) | IN: ← `Rasm.Generation` developable gate item (fan row); ← Fabrication unroll consumer (dry-run b). OUT: → flatten (`ChartAtlas`, w3); → landed `geodesics.md` (strip guides) | 4 |
| `Parametric/panelize.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. Cross-field-guided panelization: ONE `int symmetry` argument owns the quad/diagrid/tri family AS DATA ([GENERATOR_LAW]); composes the landed `fields.md` `CrossField` case + `segment.md` Knöppel globally-optimal cross-fields + stripe patterns + the wave-4 `remesh.md` substrate + landed `sample.md` CCVT/blue-noise panel-distribution seeds; input binding is UV-provenanced tessellation from `surface.md` (the pullback law) | `Fin<PanelLayout> Panelization.Apply(PanelOp, Context, Op?)` (`int symmetry` data-arg) | IN: ← `Rasm.Generation` panelization gate item (fan row). OUT: → remesh (substrate, w4 backward); → surface (UV binding); → landed `segment.md`/`fields.md`/`sample.md` (declared seams) | 4 |
| `Parametric/patternmap.md` | NEW → {kind: new} | Parametric · `Rasm.Geometry.Parametric` · 2448-2449. Pattern-to-surface mapping: 2D symmetry-GROUP rows over ONE tangent-log-map instancing fold (landed `geodesics.md` log/exp maps) → the surface-mapped INSTANCE STREAM — each instance carrying a frame parallel-transported by the landed vector-heat transport (position without orientation is half an instance); the Generation PATTERN/TILING plane's exact input | `Fin<InstanceStream> PatternMapping.Apply(PatternOp, Context, Op?)` (symmetry-group rows) | IN: ← `Rasm.Generation` pattern-to-surface gate item (fan row). OUT: → landed `geodesics.md` (log maps + vector-heat frames, declared seam); → surface (UV binding) | 4 |

### [DRAWING]

| PATH | ACTION → ENGINE | CHARTER | ENTRY | SEAMS | WAVE |
|---|---|---|---|---|---|
| `Drawing/view.md` | REBUILD ground-up → {kind: rebuild} | Drawing · `Rasm.Geometry.Projection` · 2436-2439. ENGINE RULED: exact analytic Appel QUANTITATIVE-INVISIBILITY through the crossing lattice — the invisibility count updates ONLY at silhouette crossings computed exactly (no sampling, the E1 class dies by construction); the dead `BspNode`/`Partition`/`Split` apparatus (+ dead `Kill`/`FreeList`, the phantom `PaintBackToFront` prose) DIES WITH THE CHOICE — the exactness thesis composes intersect's lattice, not a float ordering tree; `README:65` RE-SCOPES in the same motion ("one `ViewOp` over the exact Appel QI kernel" — [INDEX_DOC_OBLIGATIONS]; `ARCH:63` already accurate); SALVAGE preserved: the exact silhouette locus (`FacesOppose` via `Predicate.Orient3D`) becomes the QI-crossing driver; `Section` keeps delegating `IntersectOp.PlaneMesh`; `ToPolylines` chains by CONNECTIVITY (successor-linked walk; never kind-key concat; never merging visible with hidden); dead `ViewKind` columns (`EmitsHidden`/`NeedsBsp`) die or become consumed by `Apply`; the `MeshSpace`→soup→native→`MeshSpace` round-trip collapses onto edit.md; the select boundary stated view-side (`select.md:593` already states its capture-tier side); polygon fill routes arrangement `PlanarOverlay` (README:99) | `Fin<DrawingProjection> Projection.Apply(ViewOp, ViewPolicy, Context, Op?)` | IN: ← `Rasm.Fabrication/Posting` (ARCH:90; FAB:33 `Posting/Projection.cs` DIES, FAB:46 "beside" clause dies, FAB:47 `HiddenLineResult` becomes a thin projection of the kernel result — three counterpart edits); ← `Rasm.AppUi/Render` (ARCH:91); ← documentation dry-run (c). OUT: → intersect (Section + QI crossings); → index (occlusion candidates); → predicates (silhouette signs); → edit (soup); → arrangement (fill) | 4 |
| `Drawing/pack.md` | REBUILD → {kind: rebuild} | Drawing · `Rasm.Geometry.Encoding` · 2444-2447. Dtype-strided residency made REAL (ruled default CONFIRMED viable — `ReadOnlyTensorSpan<T>` is element-type-generic with `ConvertToHalf`/`ConvertToSingle` verified at `api-tensors.md:14,:175-176`; the honesty re-scope escape is NOT taken): `EncodedStore.Payload` becomes dtype-strided `byte[]` with per-channel descriptors; the tensor seam survives as descriptor-DISPATCHED typed views (`ReadOnlyTensorSpan<float>`/`<Half>` on the descriptor's `Dtype` row); `Half` genuinely halves residency (the widen-back `Pack` dies); the PHANTOM `using Rasm.Geometry.Topology` dies → `Rasm.Geometry.Naming` (+ the `:18` packages row re-credits); the `field` · `toolpath` `PackKind` rows LAND with their active-channel sets — `Rasm.Compute` decodes them as the residency wire's case axis (`Rasm.Compute/ARCHITECTURE.md:126`) and `Rasm.AppHost` signature-locks `EncodingKind.Field`/`.Toolpath` on them (`Sandbox/solver.md:54-55`) — two DATA rows per [GENERATOR_LAW]; PRESERVED VERBATIM: the `ScalarField.SampleDetailed` composition (`:346,:463` onto landed `fields.md:367`) and the `OrientedNormals` attribution through `VectorIntent.Cloud` (landed cloud→neighbors path); the `:3` provenance sentence fixes (cloud/neighbors own oriented normals; fit consumes); `EncodedStore` stays a clean single-writer pack arena under the [V6] contract | `Fin<EncodedGeometry> Encoding.Apply(PackOp, Context, Op?)` (`PackKind` rows incl. `field`/`toolpath`) | IN: ← `Rasm.AppHost/Runtime` (ARCH:92); ← `Rasm.Compute/Tensor/residency` (ARCH:93 — residency view, never a re-pack). OUT: → reconciliation + naming (content keys through the one hasher); → landed `fields.md` (`SampleDetailed`); → landed `intent.md` (`VectorIntent.Cloud`) | 4 |

### [ENGINE_EXECUTION_MANIFEST]

- deletePages: `Processing/solver.md`, `Processing/fit.md` (absorbed by the two `Solving/` rows — the only delete-with-absorb pairs).
- kind census: new ×11 (edit, skeleton, slice, remesh, nurbs, surface, subdivide, develop, panelize, patternmap + the 2 MOVE destinations counted in their rows), rebuild ×17, improve ×0 — no page survives on incumbency; every settled page carries confirmed defects.
- Donor-block excisions WITHOUT source deletion (charter-carried on both rows, never absorb-encoded): repair→edit (`MeshEdit` block + weld primitive), repair→arrangement (`BooleanOp` enum), intersect/view/arrangement→edit (soup adapter ×3).
- `.api` deletions ride the wave-1 roster motion, not deletePages: kernel `api-gshark.md` (vendor ruling), kernel `api-csparse.md` (dedupe), kernel `api-mathnet-providers.md` (OpenBLAS drop).

---

## [04]-[CONSUMER_GATE_HOMING] — both gates fully homed

### [04a]-[GENERATION_KERNEL_GATE] (`RASM-GENERATION-SPEC.md:179,226,164,138,173`)

| GATE ITEM | OWNING PAGE | SEAM |
|---|---|---|
| SpineRef content-key resolution (`:164`) | `nurbs.md` canonical bytes → `reconciliation.md` `Encode(ParametricForm)` → landed `identity.md` `ContentHash.Of` | the ONE identity chain (ARCH:76-81 family); a COMPONENT of the Element `ToCanonicalBytes` fold (`Rasm.Persistence/Element/codec.md:151-152`), never a sibling key |
| arc-length stations + RMF frames over `[T0,T1]` (`:138,:173`) | `curve.md` station/frame cases | `(station, frame)` pairs → `PathRow`/`Placement` (NEW Parametric fan row in ARCH:[02], wave 4) |
| surface isolines / geodesics / offsets | `surface.md` | isoline families over nurbs knot refinement; geodesics via landed `fields.md`/`geodesics.md` UV pullback; normal offset via the vendored refit lane |
| region tessellation + subdivision | `delaunay.md` (exact tessellation) + `subdivide.md` (scheme rows) | Generation fan rows |
| developable | `develop.md` (isometry witness) | fan row; Fabrication unroll shares the owner |
| panelization | `panelize.md` (symmetry data-arg over cross-fields + remesh substrate) | fan row |
| pattern-to-surface mapping | `patternmap.md` (instance stream with transported frames) | fan row |
| SSI | `surface.md` charter row: HOST-DEFERRED TRIPLE (SSI + surface-plane + curve-surface, probe-widened) | boundary stated beside landed `relations.md` |

### [04b]-[FABRICATION_TOOLPATH_GATE] (`Rasm.Fabrication/ARCHITECTURE.md`)

| DEMAND | OWNING PAGE | SEAM |
|---|---|---|
| corner-strategy offsets (JoinType×EndType) | `offset.md` rows | kernel exact skeleton + corner rows + clearance radius through the seam; CavalierContours kerf stays Fabrication-stratum (README:99) |
| slice stacks (oriented contours + nesting) | `slice.md` | FAB:48 re-route resolves; FAB:23 `Slicing.cs` dies; Compute circulation second consumer |
| medial with clearance radius | `offset.md` (2D medial over delaunay `Dual()`) | FAB:22 payload mandate — per-point radius + arbitrary-probe query first-class |
| 3D medial/curve-skeleton | `skeleton.md` (MCF over landed machinery) | FAB:22 `Toolpath/Skeleton.cs` dies unconditionally |
| manufacturing-tolerance booleans | `arrangement.md` (+ Manifold tier-3 behind finite ceiling) | FAB:44 re-anchor; ARCH:88 wire |
| developable/unroll | `develop.md` | isometry witness; dry-run (b) |
| hull / stock envelope | `delaunay.md` `Hull()` exact projection + landed `cloud.md` rail (STANDING) | [V11] two-tier boundary declared on both pages, one anchor each |
| distance-field lane | landed `fields.md` `SignedDistanceFromMesh` → landed `reconstruct.md` GWN → `index.md` BVH winding | the [V8] ONE distance-field lane; the kernel mints no second SDF owner (PicoGK stays Fabrication's voxel lane) |
| plan/section HLR | `view.md` (`DrawingProjection`) + `intersect.md` (Section) | FAB:33 dies; FAB:46/47 re-anchor; documentation dry-run (c) zero sampling misses |

---

## [05]-[SEAM_LEDGER] — corrected ledger

### [05a]-[KERNEL_LEDGER_EDITS] (`ARCHITECTURE.md:[02]-[SEAMS]`)

- ARCH:86 index⇄Compute: fence CONFORMS in wave 1 (`ToAcceleration` returns the declared node arrays as `Fin<(float[], long[])>`; the upward import dies). Row text unchanged; the fence follows the ledger.
- ARCH:79 reconciliation→Persistence: ONE anchor ruled — the ledger row's spelling; `reconciliation.md`'s three drifted spellings unify to it (wave 1); counterpart `Rasm.Persistence/ARCHITECTURE.md:55` verified same motion.
- ARCH:89 predicates←Fabrication/Posting: stands. NEW row: `Numerics/Predicates.cs ← csharp:Rasm.Compute` — the CDTet in-sphere/orientation sign-decision floor (`RASM-CS-COMPUTE-BRIEF [V7]`); the public adaptive-filtered `Orient3D`/`InSphere` family is the consumable surface (wave 1).
- NEW rows (wave 4): `Meshing/Slice.cs → csharp:Rasm.Fabrication/Toolpath/slicing` [WIRE: SliceStack oriented contours + nesting forest]; `Meshing/Slice.cs → csharp:Rasm.Compute` [WIRE: circulation floor-plate contours]; `Meshing/Skeleton.cs → csharp:Rasm.Fabrication/Toolpath` [WIRE: skeleton SoA + clearance-radius + probe query]; the Parametric consumer fan — `Parametric/{Curve,Surface,Subdivide,Develop,Panelize,Patternmap}.cs → csharp:Rasm.Generation` one row each (curve.md's `:192` fan prose becomes rows).
- In-corpus declared seams (landed-page 1-hop extensions, [LANDED_KERNEL_LAW] (b)/(c)): `mesh.md` `TopologyReceipt.Project` gains ONE Genus-tolerant `ProjectionRow` (repair/receipts consumers, wave 3); `reconstruct.md` GWN + `fields.md` `SignedDistanceFromMesh` compose `index.md`'s accelerated `Winding` (wave 1 declaration); `neighbors.md` is the point-query + MLESAC + ICP lane (README:93 — index excision + fit composition); `cloud.md` hull rail ↔ delaunay `Hull()` tier boundary (wave 2); `segment.md` ↔ remesh capture-law boundary (wave 4); `matrix.md` OpenBLAS mention sweeps with the roster drop (wave 1).

### [05b]-[COUNTERPART_LEDGER_EDITS] (1-hop ripples, the leg that changes the seam)

| COUNTERPART | EDIT | WAVE |
|---|---|---|
| `Rasm.Compute/ARCHITECTURE.md:120` | spurious "Geometry solver consumes Compute LM" row DIES (disproven by the one-LM owner) | 1 |
| `Rasm.Compute/ARCHITECTURE.md:121` | index⇄Compute direction resolves: Compute decodes the kernel node-array wire | 1 |
| `Rasm.Persistence/ARCHITECTURE.md:55` | reconciliation content-key anchor verified against the unified ARCH:79 spelling | 1 |
| `Rasm.Fabrication/ARCHITECTURE.md:44` | `BooleanOp` anchor `csharp:Rasm/Processing/repair` → `csharp:Rasm/Meshing/arrangement` | 2 |
| `Rasm.Fabrication/ARCHITECTURE.md:22` | `Toolpath/Skeleton.cs` dies → kernel `Offsetting.Apply` + `Skeleton.Apply` clearance seams | 4 |
| `Rasm.Fabrication/ARCHITECTURE.md:23,48` | `Slicing.cs` dies; the FAB:48 re-route promise resolves to `slice.md` | 4 |
| `Rasm.Fabrication/ARCHITECTURE.md:33,46,47` | `Posting/Projection.cs` BSP HLR dies; the ":46 beside" clause dies; `HiddenLineResult` becomes a thin projection of kernel `DrawingProjection` | 4 |
| `Rasm.AppHost/.planning/Sandbox/solver.md:54-55` | `EncodingKind.Field`/`.Toolpath` spellings signature-locked on pack's landed rows (verify) | 4 |

---

## [06]-[ROSTER_MOTION] (wave 1, atomic)

1. MathNet stack DOWNGRADE to `5.0.0` stable — core `PROPS:62` + `Providers.MKL:63` (kept for other campaigns' consumers) + `Providers.OpenBLAS:64` (superseded by 2) + the register-missed fourth beta `MathNet.Numerics.FSharp` `PROPS:467`; provider 5.0.0 releases feed-verified same-day coherent. The WIDENED survival sweep covers every consumer fence: landed `matrix`/`integrate`/`spectral`/`dec`/`reconstruct`, the robust-core solve rails, Compute + Fabrication co-consumer briefs; the beta survives only on a recorded 6.0-only fence member (bump blocker) — none known.
2. OpenBLAS DROP — evidence-backed (feed cache ships `lib/` only, zero arm64 native asset; the catalog self-admits x64-only): `PROPS:64` pin + `Rasm.csproj:11` reference + kernel `api-mathnet-providers.md` DELETE; kernel LA is managed MathNet + CSparse through the landed `matrix.md` owners; `matrix.md`'s opt-in-accel mention sweeps (1-hop). The decorative-capability class (manifest-tier dead engine) closes.
3. GShark pin `PROPS:73` + reference `Rasm.csproj:18` + kernel `api-gshark.md` DIE (the [02a] vendor ruling); vendor provenance records in `nurbs.md` wave 4.
4. Manifold ADD: in-house `manifoldc` P/Invoke admission (elalish/manifold, Apache-2.0, RID assets recorded); `api-manifold.md` AUTHORED with the admission (kernel tier), spelling the engine `manifoldc` and NAME-REJECTING the `Manifold` NuGet-ID homonym (a CLI/MCP source-generator framework) and the stalled `ManifoldNET 1.0.7-alpha`; golden-boolean fixture gates activation (wave-2 arrangement composes).
5. Pin re-group: `CavalierContours:70`/`Clipper2:71`/`SharpVoronoiLib:75` move OUT of the mislabeled "Kernel Geometry" group (`PROPS:69-77` — no float-lane group exists on disk) into a `Fabrication` label; post-motion Kernel Geometry holds MIConvexHull + Supercluster; `geometry3Sharp:72` disposition routes to its consuming campaigns; `LibTessDotNet` stays retired outright.
6. `CommunityToolkit.HighPerformance` ADMITTED kernel-side on the [V6] arena composition (`Rasm.csproj` reference added; `Span2D`/`MemoryOwner<T>`/`ParallelHelper` per `api-highperformance.md:26,35,54`; `ParallelHelper` bound to frozen/partition-disjoint spans per the arena-concurrency contract).
7. Catalog hygiene: shared `api-hashing.md:82` falsehood corrected (`XxHash128.HashToUInt128` seed-zero → `UInt128`, deferring to the landed `identity.md` federation entry; `XxHash3` may remain a fast-root example, never the ContentHash seed); `api-csparse.md` deduped to shared (kernel copy deletes; CSparse is kernel+Compute multi-stratum); `api-tensors.md` RECLASSIFIED overlay pair — kernel additive superset KEPT (the `IsFiniteAll` validity-gate enrichment is load-bearing); mathnet overlays stay verified subsets, RE-VERIFIED against the downgraded 5.0.0 assembly; `api-languageext.md` VERIFIED LANDED (shared tier, the +1 that makes 31 — the track-shared authoring obligation is discharged; verify, never re-author); `api-rhino.md:134` overclaim VERIFIED already corrected on disk.
8. `IDEAS.md` `[WATCH_PACKAGES]` re-key: geometry4Sharp's "on first NuGet release" trigger is dead (1.0.0 published 2022-11) → post-2022 release signal; `CSparse.Extensions` unpublished, TVGL 2019-stale, `Kemsekov.GraphSharp` 3.1.2 recorded.

---

## [07]-[LEG_PARTITION] — four legs, proven acyclic

Arrow = composes; every edge lands in an equal-or-earlier wave; every intra-wave edge points backward in listed order. Landed planes ambient in all waves. Zero upward strata edges (the one violation dies in wave 1).

| LEG | TARGETS (dependency order) | COMPANION MOTIONS | ENGINE INVOCATION |
|---|---|---|---|
| 1 — FLOOR + CONTRACTS | `Numerics/predicates.md`, `Numerics/faults.md`, `Meshing/edit.md`, `Spatial/index.md`, `Spatial/naming.md`, `Spatial/reconciliation.md` | the [V1] ratification sweep (ARCH [01]/[02]/[03] namespace matrix replaces the placeholder row with [01b]; README router; the three phantom-site surgical edits `pack.md:33,:18` + `solver.md:13`; cluster taxonomy rides faults.md); the [06] roster motion; COMPUTE:120/121 + Persistence:55 ripples; the index→reconstruct/fields GWN seam declaration | `Workflow(rebuild.js, {targets: [leg-1 six], brief: DECISION})` |
| 2 — MESHING LATTICE | `Meshing/delaunay.md`, `Meshing/intersect.md`, `Meshing/arrangement.md`, `Meshing/offset.md` | README:42 + ARCH:42 rows become fence-true; README:99 dual + PlanarOverlay rows land; FAB:44 ripple; cloud.md hull-tier boundary anchor; Manifold golden-boolean gate composes | `Workflow(rebuild.js, {targets: [leg-2 four], brief: DECISION})` |
| 3 — PROCESSING + SOLVING MINT | `Solving/solver.md`, `Solving/fit.md`, `Processing/repair.md`, `Processing/receipts.md`, `Processing/decimate.md`, `Processing/flatten.md` | the Solving/ folder mint executes (2 moves + 2 deletes); the mesh.md Genus-tolerant `ProjectionRow` extension; README:86 ddouble rows + README:93 kd-tree row become fence-true | `Workflow(rebuild.js, {targets: [leg-3 six], brief: DECISION})` |
| 4 — NEW MESH OWNERS + PARAMETRIC TIER + DRAWING | `Meshing/slice.md`, `Meshing/skeleton.md`, `Processing/remesh.md`, `Parametric/nurbs.md`, `Parametric/curve.md`, `Parametric/surface.md`, `Parametric/subdivide.md`, `Parametric/develop.md`, `Parametric/panelize.md`, `Parametric/patternmap.md`, `Drawing/view.md`, `Drawing/pack.md` | README:65 re-scope (Appel QI); FAB:22/23/33/46/47/48 ripples; AppHost signature-lock verify; the Parametric fan + slice/skeleton rows enter ARCH:[02]; the four [05] acceptance dry-runs compose | `Workflow(rebuild.js, {targets: [leg-4 twelve], brief: DECISION})` |

Acyclicity spot-proof of every cross-wave edge: predicates←all (w1 floor); edit←intersect/arrangement/view/decimate/receipts/repair/remesh/slice/subdivide (w1←w2/w3/w4); index←arrangement/slice/view (w1←w2/w4); naming+reconciliation←pack (w1←w4); delaunay←arrangement/offset/repair (w2 intra-backward + w3); intersect←arrangement/offset/repair/slice/view (w2 intra-backward + w3/w4); arrangement←offset(w2 backward)/repair(w3)/view+curve fill(w4) — and arrangement's OWN dependencies (weld→edit w1, tessellation→delaunay w2 backward) never touch w3 (the dossier's dangling-weld partition defect is closed by construction); solver←fit (w3 backward); flatten←develop (w3←w4); remesh←panelize, nurbs←curve/surface, surface←panelize/patternmap (w4 intra-backward). Dry-run→verdict reopen mapping stands as brief law.

---

## [08]-[VERDICT_DISPOSITIONS] — V1-V14

| V | DISPOSITION |
|---|---|
| V1 | RATIFIED at [01]: 7 robust-core-bearing folders (Solving/ minted; Drawing charter = projection + residency encoding, pack stays); 16 namespaces, 13 fault-routing 1:1 with 13 clusters; census disposed (Tessellation namespace-unifies into Arrangement — delaunay's fault home ruled; Parametric mints 2448-2449); phantom killed at its exactly-three sites (w1); `topology`→`naming` rename; FAB:44 follows [V5]; every folder delta runs the inbound sweep |
| V2 | The 7-page Parametric tier lands (nurbs NEW + curve REBUILD + surface/subdivide/develop/panelize/patternmap NEW), all composing landed planes; chain obligation (a) identity: `Encode(ParametricForm)` component row through landed `identity.md` — never a sibling SpineRef key; (b) UV pullback: surface.md's UV-provenanced tessellation binds panelize/patternmap inputs; SSI = host-deferred TRIPLE; Offset promoted; stations/frames land in curve.md beside landed `locate.md` (never a second location algebra); GShark hinge RULED [02a] |
| V3 | view REBUILT ground-up on RULED exact analytic Appel QI (BSP apparatus dies with the choice); connectivity chaining; select boundary stated both sides; PlanarOverlay fill; README:65 re-scoped same motion; FAB:33 dies, FAB:46/47 re-anchor; Fabrication consumes `DrawingProjection` |
| V4 | Crossing collapse EXECUTED by consumers (arrangement/offset/repair route `Intersection.Apply` — no fourth kernel); the implicit-point path LANDS per [02b] (Constraint implicit case + the five probe companions in the same legs); typed 2D restriction; host-delegation re-anchored to `relations.md` at all sites |
| V5 | `BooleanOp` homed to arrangement; `BooleanReceipt` one type composed; Manifold NAMED tier-3 behind finite `ScaleCeiling` via in-house `manifoldc` P/Invoke (homonym NuGet ID name-rejected); managed arrangement stays the ONE correctness owner; `[T-BOOLEAN-NATIVE-ASSET]` disposed; the cycle break COMPLETED (weld primitive sited in wave-1 edit.md — the dossier's dangling-edge defect closed) |
| V6 | `edit.md` NEW: the MeshEdit arena beside landed mesh.md (two-carrier seam, no third); soup ×3 collapsed; weld primitive absorbed; the store-mutability + arena-concurrency contract stated once, adopted per-store corpus-wide; HighPerformance ADMITTED on this composition |
| V7 | One LM: solver generalizes to the residual+Jacobian functor over `SolvePolicy`; fit instantiates with λ threading; decimate the standing exemplar; dense-LA law = landed `matrix.md` owners corpus-wide (flatten's second vocabulary dies); ddouble QEM + Σd² rows land (README:86); the family lifts into Solving/ |
| V8 | PointCloud lifted onto landed `neighbors.md`; zero throws; outward bounds; the ARCH:86 wire composed; GWN single-owner (index accelerates; reconstruct/fields compose) = the ONE distance-field lane; counts resync; naming Self/Subsumes/lineage rulings; `Encode`∘`ContentHash.Of` preserved; both fixtures harness-reproof-gated; ONE Persistence anchor |
| V9 | The full confirmed-defect set lands as rebuilds (Dirichlet, sparse LSCM, O(F) ARAP, factor fill, boundary collapse, incidence index, CDT self-intersect re-mesh, threaded topology, Converged, casts) PLUS the four beyond-register severities (vacuous flip guard, Genus-partiality via the mesh.md ProjectionRow, phantom `IsoSurfaceDetailed`, second cast) and the secondary set (OrderLoop, GapClose orientation, FitStore, dead BVH, Line chart, λ reset) |
| V10 | (a) JoinType×EndType rows author-kernel, CavalierContours = Fabrication lane; (b) slice.md NEW — oriented contours, typed open chains, nesting forest, cheapest implicit sub-family; (c) medial REAL over delaunay `Dual()` (probe: decoupled, zero Steiner) with the clearance-radius payload; (d) skeleton.md NEW — MCF over landed machinery, radius + arbitrary-probe query first-class; (e) `Toolpath/Skeleton.cs` dies. The radius mandate is satisfied kernel-side on both (c) and (d) |
| V11 | Hull RULED two-tier: delaunay `Hull()` exact projection LANDS (Fabrication stock + Generation bounding demand it at robust grade) beside the STANDING cloud.md rail, boundary on both pages; remesh.md NEW in Processing/ beside segment.md — isotropic core + quad rows BOTH land (deferral valve not taken); `VoxelRemesh` distinct row |
| V12 | faults re-typed (10 cases); the two-family seam statement lands (ARCH:112 discharged); the band plan IS [01b] — naming rename, concern-adjacent homes (slice→intersection, skeleton→offsetting, remesh→simplification, delaunay→arrangement), `parametric` 2448-2449 the deliberate spend; no silent squeeze; no federation re-plan needed |
| V13 | pack: dtype-strided store REAL (typed-view dispatch verified viable — re-scope not taken); phantom import dies; `field`/`toolpath` rows land for the named Compute/AppHost consumers; `SampleDetailed` + `OrientedNormals` compositions preserved verbatim; ARCH:92/93 stay composed |
| V14 | Ledger fence-conformance (index wire, reconciliation anchor, pack rows) + residual edits ([05a]/[05b]: fan rows, COMPUTE:120/121, FAB set, one-anchor fixes); README/ARCH rows land-or-rescope per row ([09] E16); both false rationales corrected (delaunay:3 honest split; predicates:3 precise precursor); predicates gains RID/strict-IEEE invariant + FMA-free row + PeterO interval filter + the Compute CDTet seam row (public `Orient3D`/`InSphere` already consumable by shape) |

---

## [09]-[EVIDENCE_DISPOSITIONS] — E1-E16

| E | DISPOSITION (pages · wave) |
|---|---|
| E1 | view.md rebuild on Appel QI + connectivity `ToPolylines` (w4); README:65 re-scope; the same failure class killed in intersect `Chain` (w2) |
| E2 | The V2 tier (w4: nurbs/curve/surface/subdivide/develop/panelize/patternmap) + offset corner rows (w2) + slice.md (w4); Offset promoted; stubs die; both gates homed at [04] |
| E3 | curve re-anchors all FOUR sites {3,20,170,190} two-target (evaluation→projections.md, intersection→relations.md — the dossier's refinement); intersect both sites {5,388}→relations.md (w2); delaunay:3 honest rationale (w2); index:27 dies (w1); the three phantom sites die (w1); manifest truth already landed (12 refs, float-lane law) |
| E4 | flatten Dirichlet + dense-LSCM (w3); naming/reconciliation `Self` mis-key (w1); receipts `Converged` (w3); decimate boundary freeze + O(F²) incidence (w3) |
| E5 | The [01b] ratification (w1) + the ONE-paradigm adoption on every rebuilt page (Op-threading, `IValidityEvidence` registration, `validation.md` admission vocabulary; `GeometryFault` stays the robust-core family per the two-family seam) — stated as corpus law, landed per wave |
| E6 | index upward using + Compute-typed return die for the ARCH:86 wire (w1); COMPUTE:120 spurious row dies (w1) |
| E7 | The [V4] collapse executed (arrangement :242-262, offset :440-444 w2; repair :346-372 w3); medial discard dies over `Dual()` (w2); fit λ-ladder claim becomes true by functor instantiation (w3); arrangement implicit framing becomes fence-true by the [02b] landing (w2) |
| E8 | FAB:33 (BSP HLR), FAB:22 (skeleton), FAB:23 (slicing) duplicates die — w4 counterpart ripples with view/skeleton/slice |
| E9 | LM ×2 → one functor (w3); GWN ×2 → single accelerated owner + composing seams (w1); `BooleanReceipt` ×2 → one composed type (w2/w3); soup ×3 → edit.md (w1); dense-LA two-rules → the matrix.md owner law (w3) |
| E10 | index throws + silent empty + inward bounds die (w1); intersect casts (w2); repair :243 + decimate :459 casts (w3) — every union result on the `Fin` rail with typed `KindMismatch` |
| E11 | BspNode apparatus + ViewKind columns + `PaintBackToFront` prose die (w4); FitStore deleted (w3); ChartStore carriers deleted + FactorNonZeros fixed (w3); pack false-Half becomes real dtype-strided residency (w4); index counts resync (w1); WavefrontStore prose + 2n sizing (w2); intersect `Next` column becomes the live chain walk (w2) |
| E12 | Declared rows composed (index wire w1, reconciliation identity verified w1, pack rows preserved w4); reconciliation triple-anchor → ONE (w1); COMPUTE:120/121 (w1); Parametric fan prose → rows (w4); FAB anchor set (w2/w4) — the full [05] ledger |
| E13 | Roster motion (w1): api-hashing:82 corrected; csparse deduped; tensors RECLASSIFIED overlay-keep (the register's dupe claim was refuted on disk — the kernel superset is load-bearing); mathnet overlays verified + re-verified post-downgrade; LanguageExt verified LANDED (discharged) |
| E14 | Roster motion (w1): MathNet 5.0.0 downgrade incl. the fourth beta pin `PROPS:467`; OpenBLAS DROP (no-arm64-asset evidence); GShark pin dies (vendor ruling); Manifold ADD + homonym rejection; WATCH re-key; all other pins newest-verified |
| E15 | faults 10-case re-type (w1); delaunay's seven stub bodies transcribed (w2 — the hardest transcription, first in its leg) |
| E16 | Per-row: README:42+ARCH:42 LAND (w2, incl. the native-gate re-scope to the [V5] meaning); README:99 LAND both claims (w2); README:65 RE-SCOPE to Appel QI (w4); README:86 LAND (PeterO filter w1; ddouble rows w3); README:93 LAND (fit→neighbors w3); ARCH:112 LAND (faults seam statement w1) — no index doc keeps a claim its fence falsifies |

---

## [10]-[DELTA_DISPOSITIONS] — the [03] escalation table (17 rows)

| PLANE (now→target) | CLOSED BY |
|---|---|
| Numerics/predicates (9→9.5) | w1 rebuild: RID invariant, precursor rationale, FMA-free row, PeterO interval tier (dual-duty: explicit ladder + implicit `_t` filters), Compute seam row + the [02b] construction re-founding |
| Numerics/faults (7.5→9.5) | w1 rebuild: 10 re-types, two-family seam, the [01b] band plan = cluster taxonomy |
| Parametric tier (3→9.5) | w4: nurbs (vendored engine) + full-body curve (stations/RMF over [T0,T1], Offset case, two-target re-anchor) + surface (UV pullback law) + subdivide + develop + panelize + patternmap + `Encode(ParametricForm)` identity + SSI triple disposition |
| Spatial/index (7→9.5) | w1: PointCloud lift, zero throws, outward bounds, wire composed, GWN single-owner, counts |
| Spatial/naming+reconciliation (6.5→9.5) | w1: `Self` index, corrected containment + overlap fallback, namespaces, lineage ruling, harness-gated fixtures, one anchor, `Encode` preserved + ParametricForm row |
| Meshing/delaunay (6→9.5) | w2: seven stubs, hull + dual projections, honest rationale, hybrid vertex table, fault home (arrangement cluster) |
| Meshing/intersect (8.5→9.5) | w2: REAL chain (the lane's revise-down defect closed), consumer collapse, 2D restriction, slice producer seam, re-anchors, entity-keyed carrier |
| Meshing/arrangement (7.5→9.5) | w2: implicit Constraint LANDS, BooleanOp home, Manifold gate named, cycle broken (weld w1), arena honesty |
| Meshing/offset+toolpath (5→9.5) | w2: medial real + clearance radius, corner rows, Minkowski completed, sizing, prose resync, PlanarOverlayResolve fix |
| Meshing substrate NEW (0→9.5) | w1: edit.md — arena + weld + soup ×3 + the corpus store/concurrency contract + HighPerformance admission |
| Processing/solver+fit (8→9.5) | w3: one functor + instantiation, λ threading, FitStore deleted, Line chart, matrix.md law, ddouble Σd², neighbors MLESAC, Solving/ mint |
| Processing/repair+receipts (7→9.5) | w3: real CDT re-mesh, Converged fix, MeshEdit excised, threaded topology, BooleanReceipt composed, Genus-tolerant ProjectionRow on landed mesh.md (ONE row, its Growth law) |
| Processing/decimate (7→9.5) | w3: incidence index O(F log F), boundary collapse, REAL flip guard, ddouble fold, phantom iso fix, Compute split recorded |
| Processing/flatten (6→9.5) | w3: correct Dirichlet, sparse LSCM eigenpair, O(F) ARAP, true factor fill, dead carriers deleted, develop seam, spectral shape verified |
| Drawing/view (4→9.5) | w4: Appel QI engine (consumed, not decorative), connectivity chaining, select boundary, Fabrication consumes |
| Drawing/pack (7→9.5) | w4: real dtype-strided residency, phantom dead, preserved compositions verbatim, field/toolpath rows |
| Slice/skeleton/hull/remesh NEW (0→9.5) | w4 slice (oriented + open chains + nesting) + w4 skeleton (MCF + radius) + w2 hull ruling (exact projection + cloud tier) + w4 remesh (isotropic + quad beside the capture tier) |

---

## [11]-[CARD_DISPOSITIONS]

- `[T-BOOLEAN-NATIVE-ASSET]-[BLOCKED]` → DISPOSED into [V5]: `HealOp.Boolean` composes `Arrangement.Apply(MeshBoolean, …)`; `NativeAssetMissing` 2409 reserved for the tier-3 scale route; the card closes with the wave-2 arrangement landing.
- `[WATCH_PACKAGES]` → re-keyed in the wave-1 roster motion ([06].8).
- `[HOST_BOUNDARY_REENTRY]-[BLOCKED]` → NOT disposed here — realization-phase law, out of this campaign's scope by brief mandate.
- The DECISION's growth rows seed the geometry idea pool fresh: InCircle implicit combos {IEEE..IIII}, the 3D CDTet implicit family (Compute-gated), OpenSubdiv limit-eval-at-scale, V-HACD/CoACD convex decomposition (consumer-gated), the Fabrication variable-by-slope + support-interface layer rows, the weighted/variable-speed wavefront rows, `Mediant` Stern-Brocot + the ddouble special-function reserves.

---

## [12]-[SELF_REPORT]

Page rows: 28 (17 REBUILD, 2 MOVE with delete-with-absorb, 9 NEW; deletePages ×2). Verdicts disposed: 14/14. Evidence rows disposed: 16/16. `[03]` deltas disposed: 17/17. Probe rulings: 2/2 (GShark vendor-and-own NOW; implicit-point exact path LANDS). Consumer gates: both fully homed ([04]). Fault partition: 13 clusters ↔ 13 fault-routing namespaces, bijective, inside 2400-2449. Strata: zero upward edges; leg partition proven acyclic at [07].
