# [RASM_FEATURES]

The realized robust-geometry capability list. Every feature is a row or case on a budgeted owner, never a new surface; mechanics live at the `Geometry/.planning/` page#cluster anchor named on each row, and the owner's realization state is read from `ARCHITECTURE.md` `[OWNER_REGISTRY]`. The settled `Vectors`/`Domain`/`Analysis` layers carry their own capability in `Vectors/_ARCHITECTURE.md`; this list is the `Geometry/` robust-geometry domain. The portable fabrication frontier is realized in the `Rasm.Fabrication` package's `FEATURES.md`.

## [1]-[ROBUST_PREDICATE_FLOOR]

The exact-predicate substance and the interior robust numerics every higher geometry owner rides.

| [INDEX] | [FEATURE]                                                                       | [PAGE#CLUSTER]                    |
| :-----: | :------------------------------------------------------------------------------ | :------------------------------- |
|   [1]   | Filter-then-exact orientation/incircle/insphere returning an exact `Sign`        | geometry-kernel#ROBUST_PREDICATES |
|   [2]   | Conservative static-permanence error-bound filter gating the exact branch        | geometry-kernel#INTERIOR_NUMERICS |
|   [3]   | Sign-exact nonoverlapping expansion arithmetic via FMA two-product/two-sum        | geometry-kernel#INTERIOR_NUMERICS |
|   [4]   | One sanctioned interior-double scope under `NumericsPolicy`, no quantity at the seam | geometry-kernel#INTERIOR_NUMERICS |

## [2]-[ACCELERATION_AND_TOPOLOGY]

The polymorphic spatial index and the persistent topological naming reconciled to one content identity.

| [INDEX] | [FEATURE]                                                                       | [PAGE#CLUSTER]              |
| :-----: | :------------------------------------------------------------------------------ | :------------------------- |
|   [5]   | One SAH-BVH + Morton linear-octree index over one struct-of-arrays node store    | spatial-index#SPATIAL_INDEX |
|   [6]   | Polymorphic query union — nearest/range/ray/overlap folded by one `Query`         | spatial-index#SPATIAL_INDEX |
|   [7]   | Topology-stable refit and direct Compute `ClashScale` acceleration projection     | spatial-index#SPATIAL_INDEX |
|   [8]   | Persistent topological naming — one lineage algebra over vertex/edge/face          | topology#TOPO_NAMING        |
|   [9]   | Signature re-anchor surviving rebuilds with survive/migrate/born lineage outcomes  | topology#TOPO_NAMING        |
|  [10]   | Canonical-adjacency byte encoder the Persistence `GeometryHash` content-addresses  | topology#NAMING_HASH        |
|  [11]   | Naming↔content-hash reconciliation — orthogonal reference and content axes, one bridge | topology#NAMING_HASH    |

## [3]-[HEALING_AND_CONSTRAINTS]

The repair/rebuild rail with typed receipts and the author-kernel geometric constraint solver.

| [INDEX] | [FEATURE]                                                                       | [PAGE#CLUSTER]                |
| :-----: | :------------------------------------------------------------------------------ | :---------------------------- |
|  [12]   | Closed heal algebra — weld/degenerate/gap/manifold/orient/self-intersect over one fold | healing#HEALING          |
|  [13]   | Predicate-grounded repair — exact collinearity and self-intersection, never an epsilon guess | healing#HEALING        |
|  [14]   | Typed per-heal-kind `RebuildReceipt` chain feeding the naming re-anchor            | healing#REBUILD_RECEIPTS      |
|  [15]   | Nine-constraint analytic residual/Jacobian algebra over one entity parameter vector | constraints#CONSTRAINT_SOLVER |
|  [16]   | Author-kernel damped Levenberg-Marquardt solve over the admitted MathNet dense lane | constraints#CONSTRAINT_SOLVER |
|  [17]   | Structural DOF verdict gating the rail — well/under/over with typed `SolveReceipt`  | constraints#CONSTRAINT_SOLVER |
