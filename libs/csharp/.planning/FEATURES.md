# [SUITE_FEATURES]

Higher-order feature atlas for the four app-grade packages. Each concept is a product the suite owns in concert across processes; mechanics live in the named planning pages, and a concept never adds a surface — it rides axis rows. Package-local atlases (`Rasm.<Pkg>/FEATURES.md`) carry the isolation concepts; this file carries the cross-package and multi-process topologies, and every concept names its owning page clusters across the four packages. State is never marked here: each concept routes to its owning page clusters, and the owner state lives in exactly one surface — the charter DENSITY_BAR `[STATE]` column. A concept inherits the weakest riding row read at the owner, never a duplicated marker.

## [1]-[TOPOLOGY_VOCABULARY]

| [INDEX] | [TOPOLOGY] | [SHAPE]                                                                          |
| :-----: | :--------- | :------------------------------------------------------------------------------- |
|   [1]   | in-process | one host, all four packages composed in one ALC                                  |
|   [2]   | paired     | standalone discovers a running Rhino host over UDS; single-writer store lease    |
|   [3]   | companion  | host dials out to N spawned/attached service processes over UDS/HTTP2            |
|   [4]   | sidecar    | memory-scratch process; durable access remote-only through its owner             |
|   [5]   | hub        | N peers sync through one durable hub (op-log changefeed + delta transports)      |
|   [6]   | service    | headless/web roots on PostgresServer with migration bundles at deploy            |
|   [7]   | web-fed    | web-service co-hosts the TS SPA; browser consumes grpc-web unary + server-stream |

The dual mandate binds every concept below: each is a universal standalone capability AND a first-class rhino/gh2 capability, distinguished by the host-modality row (`AppHost/host-profiles#PROFILE_AXIS`, `Persistence/store-profiles#PLACEMENT_MATRIX`, `Persistence/sync-collaboration#TRANSPORT_AXIS`, `AppUi/surface-hosts#HOST_AXIS`), never by a fork.

## [2]-[CONCERT_CONCEPTS]

Each concept states its spine, then the owning page clusters and the dual-mandate split (standalone face · rhino-gh2 face); state is read at the owning charter DENSITY_BAR, never marked here. Concepts riding one topology group under it; mixed-topology concepts name their mix inline.

[IN_PROCESS]:
- Live-data spine — host watch fact → projection write → tag transition → delta fetch → `IChangeSet`. Standalone: desktop one-ALC feed. Rhino/gh2: host-document `DataSource` case feeds the same projection. AppUi/live-data#DATA_SOURCES · Persistence/query-rail#INTERCEPTOR_SPINE · AppHost/resource-lanes#CACHE_PORT.
- Deterministic test-host concert — test-host profile + SqliteMemory placement + `InProcess` transport + headless surface + fake clock pair. Standalone: desktop one-ALC boot. Rhino/gh2: plugin ALC boot. AppHost/host-profiles#PROFILE_AXIS · AppHost/time-and-deadlines#CLOCK_SPLIT · Persistence/store-profiles#PLACEMENT_MATRIX · Compute/remote-lane#TRANSPORT_AXIS · AppUi/surface-hosts#HOST_AXIS.
[PAIRED]:
- Standalone ⇄ Rhino paired editing — discovery manifest + `DocumentService` + storeEpoch lease + `LocalOnly` degradation fold. Standalone: discovers a running host over UDS. Rhino/gh2: is the host the standalone pairs to. AppHost/outbound-resilience#DISCOVERY_ATTACH · Compute/remote-lane#PROTO_VOCABULARY · Persistence/store-profiles#CROSS_PROCESS_LAW · AppHost/health-and-degradation#DEGRADATION_RAIL.
[COMPANION]:
- Companion compute farm — `ComputeService` + `ProgressPhase` stream + content-addressed model-result reuse + warm-affinity node column; a remote node is one more `Substrate` row, warm-start affinity reorders only the rank-equal tier (no `FarmRouter`). Standalone: a desktop offloads inference to a local farm. Rhino/gh2: a gh2 solve component offloads through the solve-path isolation guard. Compute/intent-and-selection#SUBSTRATE_AXIS · Compute/remote-lane#PROTO_VOCABULARY · Compute/remote-lane#TRANSPORT_AXIS · Compute/progress-and-observation#OBSERVATION_SEAMS · Compute/model-lane#RESULT_CACHE · Persistence/cache-indexes#MODEL_RESULT_INDEX · AppHost/outbound-resilience#DISCOVERY_ATTACH.
- Companion capture-event ingest — `CaptureEvents` client-stream + capture-ingest DropOldest lane + bulk write with self-emitted changefeed. Standalone: capture pipeline into the local store. Rhino/gh2: companions client-stream capture into the host store. Compute/remote-lane#PROTO_VOCABULARY · Compute/scheduling-and-lanes#LANE_AXIS · Persistence/query-rail#BULK_LANE.
- Numeric solver service — `LinearProvider` (managed · native-mkl · native-openblas) selects the dense lane, `SparseFormat` (csr · csc · coo · dok) the sparse lane, and `MatMul`/`Conv`/`Pool` lower to `MatMul` through `KERNEL_LOWERING`; the solve is the same `ComputeService` request projected field-for-field over the wire, the `BenchmarkRow.Claim` gates provider rank, and `Factorization` is one `ComputeReceipt` case. Standalone: a desktop dispatches a dense+sparse decomposition to a local node. Rhino/gh2: a gh2 component offloads a system solve through the solve-path guard. Compute/numeric-lane#DENSE_ALGEBRA · Compute/numeric-lane#SPARSE_SOLVE · Compute/numeric-lane#KERNEL_LOWERING · Compute/tensor-lane#OPERATION_TABLE · Compute/tensor-lane#EQUIVALENCE_INTEROP · Compute/remote-lane#PROTO_VOCABULARY · Compute/receipts-and-benchmarks#BENCHMARK_CLAIMS.
- Self-updating companion farm — `UpdateChannel` rows drive delta-staged self-update with `Notarization`/`SBOM` receipts, a node attaches through the same `ProcessModality` discovery the farm already uses, and a rolling update drains a node through the existing `FanDrain` before swap so an in-flight solve never aborts. Standalone: a desktop fleet self-updates from a feed. Rhino/gh2: a gh2 solve node self-updates between batches. AppHost/provisioning-and-update#UPDATE_RAIL · AppHost/companion-sidecar#PROCESS_MODALITY · AppHost/outbound-resilience#DISCOVERY_ATTACH · AppHost/lifecycle-and-drain#FAULT_SPINE · AppHost/support-bundles#TRIGGER_UNION.
- Fleet-wide rolling update — one update command rolls across N nodes with health-gated progression, draining and rejoining each node through the `ROLLOVER_DRAIN` wave against the multi-peer roster so the fleet advances only while `WIRE_HEALTH` stays serving; the fleet roll is a `FleetRoll` column on `UpdateChannel`, not a second orchestrator over the per-node self-update. Standalone: a desktop fleet self-updates as a wave. Rhino/gh2: a solve farm rolls between batches. AppHost/provisioning-and-update#ROLLOVER_DRAIN · AppHost/companion-sidecar#PROCESS_MODALITY · AppHost/health-and-degradation#WIRE_HEALTH.
- Sharded numeric solve — one logical decomposition fans out across N farm nodes as a `ShardPlan` column on `DenseOps`/`SparseOps` (block-decomposed GEMM, partitioned sparse solve), each sub-block result reused by its content-address through the recency-horizon owner; the shard is a column on the numeric owner riding the substrate axis, never a `FarmRouter`. Standalone: a desktop shards a large solve across a local farm. Rhino/gh2: a gh2 component shards a system solve. Compute/numeric-lane#DENSE_ALGEBRA · Compute/numeric-lane#SPARSE_SOLVE · Compute/intent-and-selection#SUBSTRATE_AXIS · Persistence/cache-indexes#MODEL_RESULT_INDEX.
[SIDECAR]:
- Sidecar ingest pipeline — capture-direction field + memory-scratch placement + `BlobRemote` stream + bounded channel lanes. Standalone: memory-scratch worker. Rhino/gh2: gh2-spawned scratch solver. Persistence/sync-collaboration#TRANSPORT_AXIS · Persistence/store-profiles#PLACEMENT_MATRIX · Persistence/store-profiles#PROFILE_AXIS · Compute/scheduling-and-lanes#LANE_AXIS.
[HUB]:
- Object-graph sync hub (speckle-class) — op-log HLC changefeed + `SyncTransport` diff rows + `ArtifactSync` frames; `CopyGraph` set-difference over content-key `Closure` is the diff transport, `Adjudicate` LWW over (HLC, origin) is convergence. Standalone: N peers sync a shared model. Rhino/gh2: a paired session is the same `SyncTransport` axis with direction flipped — the host writes, the peer diffs against `Closure`. Persistence/sync-collaboration#OPLOG_CHANGEFEED · Persistence/sync-collaboration#TRANSPORT_AXIS · Persistence/sync-collaboration#MERGE_LAW · Compute/remote-lane#ARTIFACT_FRAMES.
- Cloud object-store sync hub — the `ObjectStore` row (s3 · azure-blob · gcs) is one more `BlobRemote` placement behind the same frame law, the content-address `Hash` is the object key so a blob written once is fetched by any peer, and a managed-RDBMS row carries the op-log durably; the cloud face is a placement row on the hub axis, not a second sync engine. Standalone: peers sync a shared model through a cloud object store and a managed durable hub. Rhino/gh2: a Rhino-hosted model's artifacts land in the same object store the peers read. Persistence/remote-stores#OBJECT_STORE · Persistence/store-profiles#PLACEMENT_MATRIX · Persistence/sync-collaboration#TRANSPORT_AXIS · Compute/remote-lane#ARTIFACT_FRAMES.
- Partial-graph checkout — a `Closure`-membership subtree walk drives a partial-fetch diff over the object store so a peer syncs one branch instead of the whole graph; the subtree route is one more `SyncTransport` axis row carrying a content-key diff-set message, never a second checkout engine. Standalone: a peer checks out a single subtree. Rhino/gh2: a gh2 component fetches one referenced sub-graph. Persistence/sync-collaboration#TRANSPORT_AXIS · Persistence/remote-stores#ARTIFACT_SYNC_FEED · Compute/remote-lane#PROTO_VOCABULARY.
- Object-store garbage collection — an unreferenced-blob sweep keyed by live `Closure` membership over the one content-address index reclaims orphaned artifacts; GC is a `RetentionPolicy` sweep row reading the artifact-blob index, never a parallel reachability tracker. Standalone: a hub GCs orphaned artifacts. Rhino/gh2: a host store sweeps dead solve artifacts. Persistence/redaction-retention#RETENTION_SWEEPS · Persistence/remote-stores#OBJECT_RESIDENCE · Persistence/cache-indexes#MODEL_RESULT_INDEX.
- Multi-peer session roster — a host-side fold of attached peers, lease epochs, and presence is the one roster every hub/farm/sidecar topology reads; presence rides the op-log on the Persistence side while the connection roster is a single `ProcessModality`-attached host fold, never a per-topology registry. Standalone: a hub tracks its attached peers. Rhino/gh2: a solve farm tracks its nodes. AppHost/companion-sidecar#PROCESS_MODALITY · Persistence/sync-collaboration#PRESENCE_AND_BLOB · AppHost/health-and-degradation#WIRE_HEALTH.
[SERVICE]:
- Telemetry lake + activity analytics — live OTLP signals (contributor ports → one `UseOtlpExporter()` after three signals, the `MetricsBackend` row env-binding the destination to prometheus/tempo/loki/alloy/otlp-generic) UNION durable receipts (HLC fan-in → DuckDB parquet rollup → dashboard tiles), correlated by one id; a new backend is one `MetricsBackend` row, never a parallel exporter. Standalone: a headless root ships OTLP to a deployed stack; the SPA renders the rollup. Rhino/gh2: a gh2 solve cluster fans receipts in through the same `ReceiptSinkPort`; the desktop contributes instruments with the OTLP row env-absent on the plugin profile. AppHost/diagnostics-and-telemetry#SIGNAL_GOVERNANCE · AppHost/runtime-ports#PORT_RECORDS · Persistence/data-lanes#ANALYTICAL_LANE · AppUi/charts-dashboards#DASHBOARD_TILES · AppUi/diagnostics-evidence#CORRELATION_JOIN.
- Time-series telemetry product — the durable-receipt arm of the lake lands on a TimescaleDB hypertable: `SchemaDdl` provisions the hypertable, continuous-aggregate, retention, and compression rows, the same content-address rollup feeds the analytical lane, and the dashboard reads the aggregate as a series row; a TimescaleDB store is one `StorePlacement` row, not a second store family. Standalone: a headless root rolls receipts into a time-series store and the SPA renders the continuous aggregate. Rhino/gh2: a gh2 solve cluster's receipts roll into the same hypertable. Persistence/store-profiles#PROVISIONING_ROWS · Persistence/schema-rail#EXTENSION_DDL · Persistence/data-lanes#ANALYTICAL_LANE · AppUi/charts-dashboards#SERIES_TABLE.
- Tenant provisioning lifecycle — a create/destroy-tenant action folds over the `TenancyModel` axis to apply the RLS policy, seed the schema through the migration bundle, and register the tenant on the roster, emitting one `TenantReceipt`; the per-tenant config overlay is one `POLICY_VALUES` admission row, not a forked option set. Standalone: tenant onboarding. Rhino/gh2: a per-project tenant provisioned on first sync. Persistence/server-tier#TENANCY_RLS · Persistence/server-tier#MIGRATION_BUNDLE · AppHost/configuration-and-options#POLICY_VALUES.
- Aggregate-refresh push lane — a continuous-aggregate refresh event is an `AggregateRefresh` fact on the `StoreInterceptor` spine that drives a server-push dashboard tile refresh distinct from the row-level op-log changefeed, so the dashboard binds the refreshed aggregate instead of polling; the push lane is one interceptor fact kind, never a second changefeed. Standalone: a dashboard tile refreshes on aggregate update. Rhino/gh2: a solve-cluster metric tile refreshes live. Persistence/server-tier#TIMESCALE_PROVISIONING · Persistence/query-rail#INTERCEPTOR_SPINE · AppUi/charts-dashboards#STREAM_BINDING.
- Secrets-bound identity plane — the `SecretBackend` row (env · user-secrets · keychain · azure-kv · aws-sm · vault) resolves a typed option through the same `OptionsAdmission` admission the config rail uses, the resolved bearer feeds the outbound `CredentialPolicy.Bearer` interceptor, and a rotation is one `ReloadClass` outcome on the existing reload union — no parallel secret store. Standalone: a service root binds secrets from a vault and dials authenticated. Rhino/gh2: a gh2 panel binds a keychain secret to reach a guarded endpoint. AppHost/configuration-and-options#SOURCE_AXIS · AppHost/configuration-and-options#POLICY_VALUES · Compute/remote-lane#CALL_POLICY · AppHost/health-and-degradation#DEGRADATION_RAIL.
- Feature-flagged capability rollout — `FeatureFlag` rows resolve through `FlagBackend` (static · env · file · remote-evaluated) on the same typed-binding rail, a flag flip is the existing `ReloadClass` reload outcome (frozen-class returns its reload verdict, never invents hot-reload), and a gated capability gates browser exposure through the Capabilities verbs and the availability algebra. Standalone: a desktop rolls a capability behind a flag. Rhino/gh2: a gh2 panel command appears only when its flag is admitted. AppHost/configuration-and-options#POLICY_VALUES · AppHost/configuration-and-options#TYPED_BINDING · AppUi/commands-availability#AVAILABILITY_ALGEBRA.
- Headless CI geometry validation — command-journal replay + virtual time + `EquivalenceLaw.Prove` kernel-equivalence + render-hash twin; `Capability.HostDocument` folds host-targeting commands structurally unavailable on headless, so a CI run cannot dial a Rhino document. Standalone: headless CI on a build server. Rhino/gh2: the same journal recorded in a live Rhino session replays headless, host-document commands folded out by capability not by editing the journal. AppUi/diagnostics-evidence#HEADLESS_DERIVATION · AppUi/surface-hosts#HOST_AXIS · AppUi/commands-availability#AVAILABILITY_ALGEBRA · Compute/tensor-lane#EQUIVALENCE_INTEROP · AppUi/charts-dashboards#SERIES_TABLE.
[WEB_FED]:
- Web viewer / dashboard feed — proto vocabulary via connect-es + `SnapshotCodec` wire rows + bearer credential case + co-hosted SPA root over grpc-web. Standalone: SPA on the web-service reads the rollup and the grpc-web endpoint same-origin. Rhino/gh2: dashboards read gh2 solve evidence over the same wire. AppHost/runtime-ports#WIRE_LAW · Compute/remote-lane#TRANSPORT_AXIS · Compute/remote-lane#CALL_POLICY · Persistence/snapshot-codecs#TS_PROJECTION.
[CROSS_TOPOLOGY]:
- GH2 solve cluster feeding dashboards (companion+service) — `ReceiptSinkPort` HLC fan-in + analytical lane parquet rollup + chart projections; the telemetry-lake fold fed by N gh2 solve nodes instead of one root. Standalone: the analytics root. Rhino/gh2: the producing cluster. AppHost/runtime-ports#PORT_RECORDS · Persistence/data-lanes#ANALYTICAL_LANE · AppUi/charts-dashboards#DASHBOARD_TILES.
- Multi-user collaboration (hub+service) — `SyncTransport` variance (PgLogical V4 parallel · HTTP delta · speckle diff) folds into one `Apply`; `Adjudicate` LWW over (HLC, origin) converges; conflict receipts surface in the conflict pane; presence rows ride the op-log. Standalone: peers collaborate over the hub. Rhino/gh2: Rhino sessions present and converge on a shared model. Persistence/sync-collaboration#TRANSPORT_AXIS · Persistence/sync-collaboration#MERGE_LAW · Persistence/sync-collaboration#PRESENCE_AND_BLOB · AppUi/inspector-editing#CONFLICT_RESOLUTION.
- Cross-process result reuse (paired+companion) — warm-start blob, result cache, and model acquisition collapse onto one content-address identity; a cold companion warms from the blob the host wrote; the recency-horizon owner is `Persistence/cache-indexes#MODEL_RESULT_INDEX` and Compute `RESULT_CACHE` plus `BenchmarkRow.Claim` consume it by reference (one horizon, never two mints). Standalone: pairs and reuses across its own host/companion split. Rhino/gh2: reuses a host-computed result in a companion solve. Persistence/cache-indexes#MODEL_RESULT_INDEX · Compute/model-lane#RESULT_CACHE · Persistence/store-profiles#CROSS_PROCESS_LAW · Persistence/sync-collaboration#OPLOG_CHANGEFEED.
- Document transaction over the wire (paired+companion) — one `DocumentTransaction` typed receipt (Rasm.Rhino host-seam sibling) proto-projected field-for-field, committed at the grid edge, dialable from the palette/remote verb projection. Standalone: pairs and commits into a remote host transaction. Rhino/gh2: commits a solve result into the document. Compute/remote-lane#PROTO_VOCABULARY · AppUi/tables-hierarchy#GRID_COMMIT · AppUi/commands-availability#PALETTE_AND_REMOTE.
- Operational control plane (service+companion) — `ControlService` verbs (`captureSupport`, `setDegradation`, `reloadOptions`) are three rows on one verb-union folding onto owned AppHost transition rows: `setDegradation`→`DegradationCell.Force` (the same cell the operator kill-switch uses, a transition-class row not a 2nd owner), `reloadOptions`→`OptionsAdmission.Admit`→`ReloadClass` (frozen-class returns its reload outcome, never invents hot-reload), `captureSupport`→`SupportTrigger`; every verb emits a receipt that fans into the telemetry lake. Standalone: companion control-panel SPA drives the verbs. Rhino/gh2: a gh2 panel command drives them. Compute/remote-lane#PROTO_VOCABULARY · AppHost/configuration-and-options#POLICY_VALUES · AppHost/health-and-degradation#WIRE_HEALTH · AppHost/support-bundles#TRIGGER_UNION.
- Wire-health serving seam (companion+service+web-fed) — a tag-predicate registry projects health to `grpc.health.v1` serving/not-serving; the same evaluation drives the `setDegradation` route and gates browser feature exposure through the Capabilities verbs. Standalone: companions report serving status. Rhino/gh2: gh2 solve clusters report per-node serving for the substrate fold. AppHost/health-and-degradation#WIRE_HEALTH · Compute/remote-lane#PROTO_VOCABULARY.
- Geospatial sync product (hub+service+web-fed) — PostGIS/GeoPackage lanes (NTS interior) emit the one canonical proto geometry family (GeoJSON/NTS/RhinoCommon are boundary projections), GeoJSON-projected at the STJ rail and rendered as a geo chart row; site-context sync rides the same `SyncTransport` axis as the hub with a geometry-typed `DataLane`. Standalone: a hub syncs site context across peers. Rhino/gh2: a Rhino-hosted site model emits `GeometryPayload` field-for-field, the `GeometryEncoding` conformance triad guaranteeing one-to-one mapping. Persistence/data-lanes#GEO_LANES · Compute/remote-lane#PROTO_VOCABULARY · Persistence/snapshot-codecs#CODEC_AXIS · AppUi/charts-dashboards#SERIES_TABLE.
- Crash-recovery choreography (all topologies) — boot markers + host crash markers fold into the support-trigger union, routing restore across siblings: content-addressed snapshot restore, dock-layout restore, crash-restore dialog offer; the `SchemaVerdict×StorePlacement` deployment lattice gates whether a recovered store may migrate or is read-ahead-only. Standalone: recovers its own boot/crash markers. Rhino/gh2: recovers from host crash markers the plugin observes. AppHost/lifecycle-and-drain#FAULT_SPINE · AppHost/support-bundles#TRIGGER_UNION · Persistence/snapshot-codecs#RESTORE_AND_DIFF · AppUi/shell-navigation#DOCK_LAYOUTS · AppUi/dialogs-notifications#DIALOG_INTENTS.
- Correlation drill-down timeline (all topologies) — one boot-minted correlation id + the HLC `SkewBound` stamp is the single ordering primitive; every sibling stamps, never mints; the evidence-view fold joins by correlation id and the `Overlaps` band algebra makes causal-order claims between overlapping bands structurally unrepresentable; the same join holds whether receipts came from one ALC or seven processes. Standalone and rhino/gh2 share the identical join. AppHost/diagnostics-and-telemetry#CORRELATION_SPINE · AppHost/runtime-ports#PORT_RECORDS · AppUi/diagnostics-evidence#CORRELATION_JOIN.
- Container-limit node self-throttle (companion+service) — a farm node grades `SystemResources`/`ResourceQuota` on its own cgroup limit (not host total) through the existing `Pressure`-tagged rules with retained-set hysteresis, and one `CpuBudget` re-caps lane readers, ORT pool, and partition cap coherently when the limit changes — per-node self-throttle, no scaling controller and no `FarmRouter`. Standalone: a containerized worker self-throttles. Rhino/gh2: a containerized gh2 solve node self-throttles. AppHost/health-and-degradation#HEALTH_FOLD · AppHost/health-and-degradation#DEGRADATION_RAIL · Compute/scheduling-and-lanes#CPU_BUDGET.
- Watchdog-mirrored service liveness (service+companion) — every node's liveness is a fold over the receipt stream: `SchedulePort.Run` emits `(Fin<Unit>, DeadlineReceipt)`, `Heartbeat` folds a not-met receipt into `SupportTrigger.WatchdogTimeout`, and `ServiceNotify` mirrors the committed `PhaseReceipt` to `ISystemdNotifier` Ready/Stopping/watchdog; a dead companion is a missed-occurrence sweep, a watchdog is a heartbeat row + a deadline class, structurally not a timer; a systemd/k8s probe reads the same stream the dashboard reads. Standalone: a service root mirrors liveness to the supervisor. Rhino/gh2: a gh2 solve node mirrors liveness across the farm. AppHost/host-profiles#LIFETIME_ADAPTERS · AppHost/time-and-deadlines#SCHEDULE_PORT · AppHost/support-bundles#TRIGGER_UNION.
- Tiered cache fabric (all topologies) — one `CacheResidence` axis (in-memory · sqlite · redis · distributed-pg) tiers the `CACHE_PORT` over the `L2_CONTRIBUTION` implementation, every tier keys on the one content-address identity so an L1 miss promotes from the L2 row without a re-mint, and tag invalidation rides the existing op-log changefeed; a new tier is one `CacheResidence` row, never a second cache owner. Standalone: a desktop tiers an in-memory L1 over a SQLite L2. Rhino/gh2: a gh2 solve cluster shares a redis tier across nodes. AppHost/resource-lanes#CACHE_PORT · Persistence/cache-indexes#L2_CONTRIBUTION · Persistence/cache-indexes#MODEL_RESULT_INDEX · Persistence/sync-collaboration#OPLOG_CHANGEFEED.
- Multi-tenant data isolation (service+hub) — the `TenancyModel` axis (single · schema · RLS · db-per-tenant) selects an isolation row that ties the host `PROFILE_AXIS` to a Postgres RLS policy on `server-tier#TENANCY_RLS`, the `TenantContext` stamped at `runtime-ports#PORT_RECORDS` rides the op-log so sync converges per-tenant, and the same content-address cache keys partition by tenant; isolation is a row on one axis, not a forked store. Standalone: a hosted service partitions tenants by RLS. Rhino/gh2: a gh2 hub session reads only its tenant's shared model. Persistence/server-tier#TENANCY_RLS · AppHost/runtime-ports#PORT_RECORDS · AppHost/host-profiles#PROFILE_AXIS · Persistence/sync-collaboration#OPLOG_CHANGEFEED · Persistence/store-profiles#PLACEMENT_MATRIX.
- Tenant-identity threading (service+hub) — one `TenantContext` stamped at the inbound edge threads the tenant id through op-log order, content-address cache-key partition, receipt, and the RLS policy as a single primitive peer to the correlation id; every sibling stamps the tenant, never mints a second tenant key, and `server-tier#TENANCY_RLS` consumes the threaded id as settled vocabulary. Standalone: a SaaS partitions tenants end-to-end. Rhino/gh2: a hub session reads only its tenant. AppHost/runtime-ports#PORT_RECORDS · Persistence/server-tier#TENANCY_RLS · Persistence/sync-collaboration#OPLOG_CHANGEFEED · AppHost/resource-lanes#CACHE_PORT.
- Spatial-analytical rollup (hub+service+web-fed) — a PostGIS spatial aggregate (proximity · coverage · change-detection) rolls into a TimescaleDB spatial-metric time-series and renders as a geo dashboard series row, the geometry crossing the wire as the one canonical proto family; the rollup is a fold from the geo lane through time-series provisioning to a series row, never a parallel spatial store. Standalone: a site-analytics dashboard. Rhino/gh2: a site model's spatial metrics render live. Persistence/data-lanes#GEO_LANES · Persistence/server-tier#TIMESCALE_PROVISIONING · AppUi/charts-dashboards#SERIES_TABLE.
- World-class dashboard (service+web-fed) — the four Skia-drawn `CustomVisual` kinds (sankey · squarified worst-aspect treemap · waterfall · funnel) LiveCharts cannot supply ride beside the native series table with synthesized a11y peers, the `ColorSpace` axis (srgb · p3 · rec2020 · scrgb-float) carries wide-gamut render fidelity through the offscreen encode identity, and every tile reads the same telemetry-lake rollup; a custom kind is a `CustomVisual` row, never a fork of the chart owner. Standalone: a desktop renders a wide-gamut custom-visual dashboard. Rhino/gh2: a gh2 solve cluster's evidence renders in the same custom tiles. AppUi/custom-visuals#SKIA_KINDS · AppUi/charts-dashboards#DASHBOARD_TILES · AppUi/visuals-offscreen#ENCODE_IDENTITY · Persistence/data-lanes#ANALYTICAL_LANE.
- Cross-tile linked brushing (service+web-fed) — a `CrossFilter` predicate fold over the dashboard surface broadcasts one `FilterState` across tiles and drives a server-side filtered re-query against the analytical lane through a keyset `FilterPredicate`, the board layout and filter state persisting as one `BoardState` row; brushing is a fold on the dashboard owner plus a query-rail predicate, never a per-tile filter engine. Standalone: a desktop dashboard cross-filters. Rhino/gh2: a solve-evidence board cross-filters. AppUi/charts-dashboards#DASHBOARD_TILES · AppUi/charts-dashboards#STREAM_BINDING · Persistence/query-rail#PROJECTION_SHAPES.
[CAPABILITY_PLANE]:
- Capability control plane + MCP/SDK (all topologies) — every operation a typed self-describing effect-classed cost-modeled permission-gated `CapabilityDescriptor` generated from the Compute proto op surfaces and the AppHost/AppUi command surfaces; a shape-discriminated `DiscoveryQuery` fold drives intent planning, the `GrantBroker` dry-run `CostPreview` previews cost before commit, and `SdkCodegen` emits the C#/TS/Python SDK off the one descriptor source. The capability registry is the single op catalog every external agent, MCP client, and polyglot SDK reads — the MCP `ToolProjection` is a projection of it, not a parallel tool registry, and the polyglot SDK is generated from it, not hand-maintained per proto service. Standalone: a desktop exposes its op catalog to an external agent over MCP. Rhino/gh2: a gh2 command set projects as capabilities. AppHost/capability-registry#DESCRIPTOR_AXIS · AppHost/capability-registry#DISCOVERY_FOLD · AppHost/capability-registry#GRANT_BROKER · AppHost/capability-registry#SDK_CODEGEN · AppHost/mcp-projection#METHOD_AXIS · Compute/remote-lane#PROTO_VOCABULARY.
- Capability command transaction (companion+service+web-fed) — a `CommandAlgebra` wraps any `CapabilityDescriptor` invocation in a commit-or-rollback `CommandTxn` threaded through `GrantBroker` admission, lands on the existing Compute `ComputeIntent`/`IntentAdmission` dispatch rail and the `WorkLane` enqueue, and emits a receipt that fans into the telemetry lake; the command is a route onto the intent rail, never a parallel compute entrypoint. Standalone: a desktop drives a transacted op from its palette. Rhino/gh2: a gh2 command commits-or-rolls-back through the broker. AppHost/capability-registry#COMMAND_ALGEBRA · AppHost/capability-registry#GRANT_BROKER · Compute/intent-and-selection#INTENT_FAMILY · Compute/scheduling-and-lanes#LANE_AXIS.
- Sandboxed plugin marketplace (companion+service) — a two-row `SandboxIsolation` axis (WASM-component · out-of-process) loads a verified plugin under no-ambient-authority, the only authority a `PluginInstance` reaches host functionality through is a broker-scoped `GrantHandle` with `BrokeredCall` mediation, `QuotaControl` kills/quarantines, and `SupplyChainGate` admits on signature/SLSA/semver; the out-of-process isolation row admits the child through the existing `PeerAdmission` credential read over the companion-spawn hop. Standalone: a desktop runs a sandboxed third-party plugin. Rhino/gh2: a gh2 plugin runs capability-brokered. AppHost/sandbox-host#ISOLATION_AXIS · AppHost/sandbox-host#GRANT_HANDLE · AppHost/sandbox-host#QUOTA_CONTROL · AppHost/sandbox-host#SUPPLY_CHAIN · AppHost/capability-registry#GRANT_BROKER · AppHost/companion-sidecar#PEER_ADMISSION.
- Hosted solver-plugin negotiation (companion+service) — a seven-row `SolverKind` extension-category axis loads a solver/mesher/optimizer/CAM/material/codec plugin under `SandboxIsolation`, the `SolverPluginContract` proves the plugin's declared representation against the canonical Compute `GeometryEncoding`/`EncodedTensor` before first solve, and `SolverHosting` projects the plugin's `OpDeclaration` rows into the capability registry so a hosted solver dispatches through the same `ComputeIntent` rail. Standalone: a desktop loads a third-party FEA solver. Rhino/gh2: a gh2 component drives a hosted mesher. AppHost/solver-plugin#SOLVER_KIND · AppHost/solver-plugin#PLUGIN_CONTRACT · AppHost/solver-plugin#SOLVER_HOSTING · Compute/tensor-lane#GEOMETRY_ENCODING · Compute/intent-and-selection#INTENT_FAMILY.
[ENGINE_AND_COCKPIT]:
- Closed-loop live-wire studio (all topologies) — an eight-row `ExternalTransport` industrial-binding axis (OPC-UA · Modbus · MQTT · serial · REST · GraphQL · ERP · PLM) under one read/write adapter contract, every inbound `ExternalValue` coercing its unit at the edge into a `CoercedValue` through the existing Compute `UnitAlgebra` before it enters the suite, a `WriteBack` acknowledgement transaction closing the loop, and a `BindingHealth` lifecycle projecting onto the health fold; the closed loop is the generalization of the one-way live-data spine, never a second transport family, and the unit coercion is the one admission fold, never a second unit owner. Standalone: a desktop binds parameters to a PLC and writes back. Rhino/gh2: a gh2 component drives industrial output. AppHost/live-wire#TRANSPORT_AXIS · AppHost/live-wire#BINDING_SPEC · AppHost/live-wire#WRITE_BACK · AppHost/live-wire#BINDING_HEALTH · Compute/units-boundary#DIMENSIONAL_LAW · AppUi/live-data#DATA_SOURCES.
- Multi-physics solve farm + design-space optimizer (companion+service) — a `PhysicsKind×BoundaryCondition×ElementClass` `SolveProblem` axis over a volumetric `MeshKernel`, the `SolveLane` assemble-solve fold dialing the numeric-lane `Factorization`/`SparseOps`/`ShardPlan` machinery and the existing `Solve` rpc fan-out, an `Optimizer` design-space search carrying a `Surrogate` ROM duality and a queryable `ParetoFront`, a `SweepGrid`/`FrameBudget` DOE governor pacing the dependency `JobGraph` scheduler over the existing fan-out, and the differentiable reverse-mode `adjoint` propagating through the tensor-lane operators; the solve composes existing numeric/scheduler/receipt/cache owners, minting no second solve or cache surface, and the field kernel is the `Rasm/Vectors` DDG substrate, never a greenfield solver. Standalone: a desktop offloads FEA + shape-optimization to a local farm. Rhino/gh2: a gh2 component offloads multi-physics with adjoint sensitivities. Compute/solver-and-optimization#DISCRETIZATION_MESH · Compute/solver-and-optimization#SOLVE_CONTRACT · Compute/solver-and-optimization#OPTIMIZER_LANE · Compute/solver-and-optimization#SWEEP_AND_BUDGET · Compute/numeric-lane#KERNEL_LOWERING · Compute/tensor-lane#EQUIVALENCE_INTEROP · Compute/scheduling-and-lanes#LANE_AXIS · Persistence/cache-indexes#MODEL_RESULT_INDEX.
- Digital-twin clash + anomaly loop (companion+service+hub) — a `ClashScale` collision-compute over a persistent `AccelerationStructure` indexing the federated geometry runs incremental delta-only clash re-test, a `DigitalTwin` scores a `TwinSignal` against a `Surrogate`-evaluated simulated baseline emitting an anomaly verdict plus a control suggestion, and the federated `FederatedEntity` graph is read by reference; the twin reads the federated graph and the persistent acceleration structure, minting no second spatial index. Standalone: a hub federates models and clashes them under a live signal. Rhino/gh2: a host model joins the federated graph and twin-scores. Compute/solver-and-optimization#CLASH_AND_TWIN · Persistence/federation#ENTITY_GRAPH · Persistence/federation#ELEMENT_SET_ALGEBRA.
- Construction-document drafting (all topologies) — a locale-aware `SheetSet` with ISO/ANSI/JIS title blocks, a hidden-line `Viewport2D` 3D-to-2D projection kernel reading the Compute geometry payload, the `Dimension`/`Annotation` GD&T families, and a `DraftEmit` DWG/DXF/PDF/SVG dispatch over the `SKDocument` rail; drafting operates on geometry not the path-traced scene, viewport-independent and sellable on its own. Standalone: a desktop emits construction drawings. Rhino/gh2: a host model projects to sheets. AppUi/drafting-sheets#SHEET_SET · AppUi/drafting-sheets#PROJECTION · AppUi/drafting-sheets#DIMENSIONING · AppUi/drafting-sheets#DRAFT_EMIT · Compute/interchange#FORMAT_AXIS.
- GPU-virtualized infinite viewport + sim render (in-process+web-fed) — a `RenderGraph` pass-DAG over a host-shared `GRContext` lease, `MeshletCluster` geometry virtualization with `ResidencyBudget` out-of-core streaming, a `PathTracePass` ReSTIR/denoise GI pass, and `SimVisual` isosurface/volume/streamline/glyph/deformation renders off the Compute field receipts and the `FieldCodec` chunked layout; the viewport is a 3D extension of the host-modality split, GPU passes ride the live shared-context probe with a 2D-Skia fallback shipping today, and the field shape crosses solver↔store↔viz by reference. Standalone: a desktop renders any model under a frame budget. Rhino/gh2: a host-composited Metal path shares the `GRContext`. AppUi/viewport-pipeline#RENDER_GRAPH · AppUi/viewport-pipeline#GEOMETRY_VIRTUAL · AppUi/viewport-pipeline#RESIDENCY_BUDGET · AppUi/viewport-pipeline#PATH_TRACE · AppUi/viewport-pipeline#SIM_VISUAL · Compute/interchange#FIELD_RESULT_CODEC · Compute/solver-and-optimization#DISCRETIZATION_MESH.
- Animation + temporal model (all topologies) — a closed `Track` keyframe-track union with motion-token `Easing`, a deterministic-playhead `Timeline` sampler, a `Scrub` kinematic/transient-field fold reading the Compute `SimField` frame index, and an offline `Walkthrough` render to the encode rail; the camera track interpolates the one portable `Viewpoint` receipt, never a forked camera. Standalone: a desktop authors a walkthrough and scrubs a transient field. Rhino/gh2: a gh2 solve animates a deformation field. AppUi/animation-timeline#TRACK_MODEL · AppUi/animation-timeline#TIMELINE · AppUi/animation-timeline#SCRUB · AppUi/animation-timeline#WALKTHROUGH · AppUi/viewport-pipeline#VIEWPOINT_CODEC.
[COLLABORATION_VCS]:
- GraphFork CRDT branch/merge/time-travel (hub+service+web-fed) — a content-addressed `CommitGraph` DAG with branches/merge-base/version-vectors/Merkle-range, a `CrdtField` RGA/OR-set/LWW-register convergent CRDT superseding the LWW `Adjudicate`, a `TimeTravel` AS-OF reconstruction/diff/blame/scrub engine, and a `StructuralMerge` tree-edit-distance three-way geometry merge; the `OpLogEntry.Payload` carries a `CrdtOp` delta for crdt-family rows and the conflict surfaces through the existing conflict pane. The CRDT op-based merge replaces LWW convergence — a breaking amendment to the one-wire-vocabulary law the TS-web and Python legs decode. Standalone: peers branch/merge a shared model and time-travel. Rhino/gh2: Rhino sessions branch and converge. Persistence/version-control#COMMIT_DAG · Persistence/version-control#CRDT_ALGEBRA · Persistence/version-control#TIME_TRAVEL · Persistence/version-control#STRUCTURAL_DIFF · Persistence/sync-collaboration#MERGE_LAW · AppUi/inspector-editing#CONFLICT_RESOLUTION.
- BCF coordination cockpit + anchor algebra (hub+service+web-fed) — one `Anchor` union binding annotations to node/sub-entity/parameter/world-point targets with re-anchoring against the structural diff, a `Thread` comment/mention/status fold over the op-log, a portable `BcfViewpoint` mirroring the one `Viewport2D` viewpoint receipt, and a `CdeSync` bidirectional BCF 2.1/3.0 + BCF-API OAuth2 topic/comment/viewpoint sync over the AppHost outbound hop; the anchor algebra is the one currency unifying comments/conflicts/presence/refs/blame across five surfaces, never re-minted per surface. Standalone: a hub runs a BCF issue board. Rhino/gh2: host markup anchors through the same algebra. Persistence/annotation#ANCHOR_ALGEBRA · Persistence/annotation#BCF_PROTOCOL · Persistence/annotation#CDE_SYNC · AppUi/viewport-pipeline#VIEWPOINT_CODEC · AppHost/outbound-resilience#HOP_AXIS · AppUi/inspector-editing#CONFLICT_RESOLUTION.
- Lineage provenance engine (service+hub) — a W3C-PROV `ProvEdge` causal DAG over the op-log with backward/forward `LineageSlice` folds powering search/audit/sync-scoping/blame/impact, a hash-chained `AttestedLedger` with head proof over pgaudit, and a redaction-preserving lineage-scoped `LineageCdc` feed; provenance is a causal join dimension over the existing op-log, never a parallel event store. Standalone: a service slices lineage backward/forward. Rhino/gh2: a host op records a provenance edge. Persistence/provenance#CAUSAL_DAG · Persistence/provenance#ATTESTED_LEDGER · Persistence/sync-collaboration#OPLOG_CHANGEFEED.
- Federated geometry search (service+hub+web-fed) — a `FusionRank` reciprocal-rank fusion over the existing pgvectorscale diskann HNSW + PostGIS GiST + pg_search BM25 indexes into one scored result with per-hit lineage, a source-agnostic `FederatedEntity` graph and the polymorphic `ElementSet` selection currency, a `RulePlan` declarative-DSL query lowering, and a cost-based `FederatedPlan` cross-store pushdown; the three indexes exist and are world-class individually, only the fusion-ranking owner is net-new, never a third index or a learned reranker. Standalone: a desktop runs a cross-modal find-similar. Rhino/gh2: a host queries the federated index. Persistence/federation#FUSION_RANK · Persistence/federation#ENTITY_GRAPH · Persistence/federation#FEDERATED_PLAN · Persistence/cache-indexes#MODEL_RESULT_INDEX · Persistence/data-lanes#SEARCH_LANES.
- Quantity-takeoff + 4D schedule (service+hub) — a multi-standard `ClassificationStandard` axis with ltree-pathed hierarchical codes, a federated `ClassificationMap`, a `CostRollup` formula-evaluated quantity-takeoff hierarchical DuckDB rollup over the element set, and a `ScheduleImport` P6-XER/MS-Project-XML activity-network reader over the Sep tabular lane binding activities to the federated element set as a `FourDState` as-of 4D construction-state fold; the foreign-format parse is the host-local Compute companion, the store and 4D fold are Persistence. Standalone: a service runs cost takeoff + 4D simulation. Rhino/gh2: a host model rolls up quantities by classification. Persistence/catalog-cost#CLASSIFICATION_CATALOG · Persistence/catalog-cost#COST_ROLLUP · Persistence/schedule-interchange#SCHEDULE_STORE · Persistence/federation#ELEMENT_SET_ALGEBRA.
- Deterministic replay observatory + reproducible notebook (all topologies) — a `DeterminismKernel` pinning RNG/float-mode/env fingerprint, a hash-chained content-addressed `EventLog` over the op-log, a `ReplayVerify` per-step content-hash proof, a `MacroEngine` record/replay unit, and a content-address `RecomputeGraph` partial-recompute walk; a `NotebookCell` pins a `CapabilityPin` fingerprint and `ReplayBundle` exports the notebook bit-identically, the cell-edit `NotebookOp` projecting onto the op-log `CrdtOp` at the seam. Cross-machine replay-verify consumes the Compute provider-determinism fingerprint (managed/MKL/OpenBLAS bit-divergence) or it is unsound. Standalone: a desktop scrubs a deterministic solve and exports a reproducible notebook. Rhino/gh2: a gh2 solve replays bit-identically across machines. AppHost/determinism-and-replay#DETERMINISM_KERNEL · AppHost/determinism-and-replay#EVENT_LOG · AppHost/determinism-and-replay#REPLAY_VERIFY · AppUi/notebook-document#CELL_MODEL · AppUi/notebook-document#REPLAY_BUNDLE · Compute/numeric-lane#PROVIDER_CLAIMS · Persistence/sync-collaboration#OPLOG_CHANGEFEED.
- Mechanical CAD-STEP interchange (companion+service) — an `InterchangeFormat` AP242 row disjoint from the existing IFC-STEP row (both claim the `.step`/`.stp` extension but are different schema families, force-disambiguated at format dispatch), an in-process IFC/IFC5 GeometryGym semantic-graph ingest, the AP242/native companion two-hop `TessellationRequest` crossing to the IfcOpenShell/OpenCascade companion producing GLB then re-importing through the glTF `IMPORT_RAIL`, and the `XxHash128` `InterchangeIdentity` content-addressing every artifact through the Persistence blob index. Standalone: a desktop imports a mechanical-CAD assembly. Rhino/gh2: a host imports a STEP solid. Compute/interchange#FORMAT_AXIS · Compute/interchange#TWO_HOP_TESSELLATION · Compute/interchange#CONTENT_ADDRESSING · Compute/remote-lane#TRANSPORT_AXIS · Persistence/cache-indexes#ARTIFACT_BLOB_INDEX.
## [3]-[CROSS_PACKAGE_LAWS]

- macOS-only live posture: the live byte path is single-platform — osx-arm64 with the UnixDomainSocket-only local-IPC transport at `Compute/remote-lane#TRANSPORT_AXIS`; the Windows NamedPipe (`PipeSecurity` ACL) and bearer-plus-DACL TcpLoopback rows are DROPPED from the live axis, their per-path security member spellings retained as the design record on `remote-lane#RESEARCH [PIPE_SECURITY]`, re-entering as one row each only behind a host RID that admits the byte path and never blurred into one credential. The single-platform target drops every off-platform native asset by the INFRA-TRUTH law; AppHost owns the redial policy and the UDS-only control intake, Compute owns the UDS byte path; neither re-declares the other.
- One op-surface owner: the `CapabilityRegistry` is the single self-describing op catalog — every operation a typed effect-classed cost-modeled permission-gated `CapabilityDescriptor` generated from the Compute proto op surfaces and the AppHost/AppUi command surfaces; the MCP `ToolProjection`, the polyglot `SdkCodegen`, the `EventLog` command chain, and the `NotebookCell` `CapabilityPin` all read this one registry, never a parallel tool catalog or a per-service hand-maintained SDK. A new operation is a descriptor row; the only authority a sandboxed `PluginInstance` reaches host functionality through is a broker-scoped `GrantHandle`, the `GrantBroker` consuming the tenant identity and the classification taxonomy as settled vocabulary and minting no second limiter or classification axis.
- One wire vocabulary: Compute owns the proto suite; every other package projects into it. The `OpLogEntry.Payload` carries a `CrdtOp` discriminated union for crdt-family rows — a breaking CROSS_PACKAGE_LAW amendment the TS-web and Python legs decode, replacing the prior LWW `Adjudicate` scalar (which survives only as the `LwwRegister` CRDT arm); the `version-control#CRDT_ALGEBRA` `Crdt.Merge` join-semilattice LUB owns convergence and `runtime-ports#WIRE_LAW` owns the cross-package vocabulary change. An additive parallel sync surface is the rejected form.
- One causal primitive: the `ReceiptSinkPort` HLC stamp orders cross-process evidence, deriving `SkewBound` at stamp time; receipts, bundles, and degradation stay process-local and correlate only through the stamp. Every cross-process product (sync op-log order, correlation join, tag invalidation, drill-down) is provable from this one primitive — siblings stamp, never mint. The boot-minted correlation id and the W3C `TraceContext` traceparent/baggage propagated over the UDS/HTTP2 hop as gRPC metadata are the one correlation primitive declared at `AppHost/diagnostics-and-telemetry#CORRELATION_SPINE`; a sibling propagates the traceparent, never mints a second trace id.
- One tenant-identity primitive: `TenantContext` stamped at the inbound edge (`AppHost/runtime-ports#PORT_RECORDS`) is the single tenant key threading op-log order, content-address cache-key partition, receipt, and the Postgres RLS policy (`Persistence/server-tier#TENANCY_RLS`); a 4th cross-package primitive peer to the correlation id, content address, and HLC stamp — every sibling threads the stamped tenant, never mints a second tenant identity, and `server-tier` consumes it as settled vocabulary.
- One content-address identity: the sealed snapshot `Hash` is the one `ContentAddress` and `XxHash128` `Append`/`GetCurrentHash` is the one incremental identity, feeding cache tag, blob lookup, diff `Closure`, model-result key, parquet rollup, and snapshot restore; no product mints a parallel key.
- One frame law: declared at `ArtifactSync`, consumed by `BlobRemote`.
- One canonical wire geometry: the proto geometry family; GeoJSON, NTS, and RhinoCommon shapes are boundary projections.
- One interchange-identity seam: `Compute/interchange#CONTENT_ADDRESSING` is the cross-package geometry/IFC/field seam — `InterchangeIdentity` folds artifact bytes plus deflection and tolerance into one `XxHash128` content key (mirroring `ModelIdentity.Snapshot`), the in-process IFC/IFC5 ingest and the AP242/native two-hop `TessellationRequest` produce GLB through the glTF rail, and `FieldCodec` projects one `FieldSpace`-shaped chunked layout crossing solver↔store↔viz by reference. Compute owns the identity derivation and the geometry seam, Persistence owns blob residence (`cache-indexes#ARTIFACT_BLOB_INDEX`), AppUi owns the render (`viewport-pipeline#SIM_VISUAL`); the interchange seam mints no second cache and no second geometry, and the AP242 `.step` row is disjoint from the IFC-STEP `.step` row at format dispatch.
- One codec table: seam-keyed (config, snapshot, delta, gRPC, dashboard, deep-link) — STJ source-gen, MessagePack, protobuf each own named seams; no codec crosses into another's seam.
- One retry owner per hop, one cache owner, one clock seam, one drain conductor, one recency-horizon owner (`MODEL_RESULT_INDEX`) — the AppHost and Persistence ports; DB retry stays excluded from the outbound retry owner.
- One backend axis per professional concern: `MetricsBackend`, `CacheResidence`, `SecretBackend`, `FlagBackend`, `TenancyModel`, `ObjectStore`, `LinearProvider`, and `UpdateChannel` are each a single smart-enum/union row axis on one owner — a new provider is a row, never a parallel surface. Secret rotation and flag flips are `ReloadClass` outcomes on the one reload union; no concern mints a second config admission.
- One numeric provider seam: `LinearProvider` ranks dense lanes and `SparseFormat` the sparse lanes under the tensor op-table, every binding inherits the one `EquivalenceLaw` proof, and `BenchmarkRow.Claim` is the single rank authority; no lane re-implements a kernel the numeric owner owns.
- One owner per axis, consumers reference: every cross-package fact has exactly one owner pkg/page#cluster recording its mechanics and every other package consumes the owner's symbol as settled vocabulary; the ledger `region-map/seam-splits.md` records each two-package fact as `mechanics at <owner>; consequence at <consumer>`. A consumer re-teaching the owner's mechanics is the duplicate-region defect, routed back to the owner. Cross-package owner type names are unique across the four-package suite, enforced through the ledger `OWNER_SYMBOLS` registry; a recurring name is admitted to the bounded-context carve-out only when the recurrences are genuinely distinct concepts in distinct namespaces AND the ledger records each occurrence with its disambiguating context note. The closed enumerated set splits by verification route: the same-name-distinct-TYPE carve-outs carry a dual `OWNER_SYMBOLS` note (`WireFault`, `CommandReceipt`, `FrameBudget`); the altitude/member homonyms carry a single `SEAM_SPLITS` row and no dual `OWNER_SYMBOLS` entry because they are distinct names or member-level homonyms, not dual-registered owner types (`WorkLane`/`DrainQueue` lane-naming, `Lane` altitude split, `FocusWalk` member homonym).
- No `*Wire` homonym collision: a wire-projection type (`*Wire`) is NEVER admitted to the bounded-context carve-out — two `*Wire` types with the same name project into one polyglot SDK off one descriptor source and collide at codegen, so a recurring wire name is force-disambiguated at the source (the AppHost capability-registry `CapabilityCommandReceiptWire` is distinct from the AppUi command-execution `CommandReceiptWire`). The one polyglot SDK is generated from the one descriptor source; a second `*Wire` of the same name is a codegen collision, not a carve-out.

## [4]-[ROUTING]

- Concept mechanics: `Rasm.<Pkg>/.planning/<page>.md` at the cluster anchor named on each concept.
- Implementation charters: `Rasm.<Pkg>/.planning/README.md`; implementation-start gates and research probes: `Rasm.<Pkg>/ROADMAP.md`.
- Capability-expansion rows feeding Phase 3 integration: `.artifacts/planning-briefs/capability-expansion-register.md` (each gap/new cross-topology concept → owning page#cluster row, with its `[STATE]` and grounding).
- Server topologies (service, hub, web-fed): operator provisioning rows at `Rasm.Persistence/.planning/store-profiles.md#PROVISIONING_ROWS`; physical deploy assets (postgresql.conf and pg_hba fragments, role grants) gate at the first headless/web app root per the ROADMAP tables.
- TS web layer: `libs/typescript/.planning/` (`architecture-posture.md`, `wire-consumption.md`) consumes the TS_PROJECTION clusters listed in each package charter WIRE_PAGES.

## [5]-[IDEATION_2026-06-16] — NET-NEW HIGH-VALUE CONCEPTS

Net-new high-value concepts surfaced by the aggressive-ideation pass; full shortfall detail, ranking, and owner-mapped shortfall register live in `.artifacts/planning-briefs/aggressive-ideation.md`. Each row states its spine, then its owning `page#cluster` route (same discipline as `[2]`) and its dual-mandate split; rows whose owner page does not yet exist carry `[NEW OWNER]` naming the package + the page that must be authored before the concept enters `[2]`. State is never marked here; an admitted concept routes to its owning charter DENSITY_BAR — a `[NEW OWNER]` row has no DENSITY_BAR until its page lands, so it is a Phase-3 page-authoring target, not yet a state-bearing concert row. Every concept whose route already exists carries an explicit reconciliation note against the `[2]` concept it supersedes. Ranked by value × leverage; the capability registry leads on dependency fan-out (it gates the MCP bridge, the event log, the grant broker, and the polyglot SDK, and carries no native/GPU risk).

[LANDING_STATUS]: the engine/cockpit/collaboration-VCS frontier this pass surfaced has now LANDED — the capability plane (`capability-registry`/`mcp-projection`/`sandbox-host`/`solver-plugin`), the engine (`live-wire`/`solver-and-optimization`/`viewport-pipeline`/`drafting-sheets`/`animation-timeline`), and the collaboration depth (`version-control`/`federation`/`provenance`/`annotation`/`catalog-cost`/`schedule-interchange`) are authored pages, ledger-`[FINAL]`, and woven into `[2]` as the `[CAPABILITY_PLANE]`/`[ENGINE_AND_COCKPIT]`/`[COLLABORATION_VCS]` concert groups; their rows below stand as the shortfall-and-reconciliation record. The genuinely-deferred residual the C# corpus does not yet own is the geometry-core substance — the Verified Parametric Kernel, the Appearance Engine, the Fabrication Director, the Drafting hidden-line kernel substance, and the Universal Access 3D-tree half — each gated on the `Rasm.GeometryCore` / `Rasm.Materials` new-package candidate (suite `TASKLOG.md#[6]`) and the live-GPU-context viewport SPIKE; these are the next-loop page-authoring targets, not yet `[2]` concert rows.

- Capability Control Plane + MCP/SDK — every operation a typed self-describing effect-classed cost-modeled permission-gated capability generated from canonical op surfaces; intent planner, dry-run/cost-preview, capability-driven C#/TS/Python SDK codegen. `[NEW OWNER]` AppHost `capability-registry#DISCOVERY_FOLD` (registry generated from Compute proto + AppHost/AppUi command surfaces) + AppHost `runtime-ports#WIRE_LAW` (MCP projection) + Compute `remote-lane#PROTO_VOCABULARY` (op surfaces). Standalone: a desktop exposes its op catalog to an external agent over MCP. Rhino/gh2: a gh2 command set projects as capabilities. SUPERSEDES the per-proto-service SDK posture at `Compute/remote-lane#PROTO_VOCABULARY` — the polyglot SDK is generated from the registry, not hand-maintained per service. Highest leverage (gates V10/V14/MCP), no native risk.
- Infinite Viewport — GPU-virtualized meshlet/cluster-LOD path-traced frame-budgeted 3D scene that never stutters on any model; unblocks the entire 3D-dependent register. `[NEW OWNER]` AppUi `surface-3d#SCENE_PIPELINE` (net-new 3D surface) over `surface-hosts#EMBED_CAPSULE` (the existing shared-`GRContext` Metal spike de-risks it) + Compute `tiling-lane#LOD_GEN` + TS-web WebGPU feed. Standalone: a desktop renders any model under a frame budget. Rhino/gh2: a host-composited Metal path shares the `GRContext`. SUPERSEDES nothing in `[2]` (no 3D surface exists today); EXTENDS the `surface-hosts#HOST_AXIS` host-modality split into 3D. Hard dependency: Appearance Engine (below).
- Appearance Engine — physically-based BSDF library + texture/UV pipeline + photometric light units + node-based appearance graph; the path tracer has nothing to shade without it. `[NEW OWNER]` Rasm.Materials `appearance#MATERIAL_GRAPH` (net-new beside `Bricks/`, today the ONLY Materials content) + AppUi graph editor + Compute light-unit coercion on the `units-boundary#QUANTITY_TABLE`. Standalone: a desktop authors PBR materials. Rhino/gh2: host render materials map to the graph at the boundary. SUPERSEDES nothing; net-new owner. Hard V1 dependency.
- Verified Parametric Kernel — exact/interval-certified predicates + diagnosable constraint solver + persistent topological naming + proof-carrying rebuild receipts + healing/repair, so edits never silently corrupt downstream references. `[NEW OWNER]` Rasm `geometry-kernel#ROBUST_PREDICATES` + `#CONSTRAINT_SOLVER` + `#TOPO_NAMING` + `#REBUILD_RECEIPTS` + `#HEALING` (geometry core; the B-rep substance lives in the Rhino host today). Standalone: a companion runs a certified rebuild. Rhino/gh2: the host kernel is wrapped behind the same receipt discipline. SUPERSEDES nothing in `[2]`; the receipt CULTURE transfers from the existing typed-receipt discipline, the kernel substance is net-new. Second-highest value.
- GraphFork CRDT + branch/merge/time-travel — convergent op-based CRDT replacing LWW adjudication, Git-grade branch/merge-base/rebase, three-way conflict typing, AS-OF time-travel, range diff, per-node blame. Persistence `sync-collaboration#MERGE_LAW` (CRDT replaces LWW `Adjudicate`) + `[NEW OWNER]` Persistence `version-control#COMMIT_DAG` + `#TIME_TRAVEL` + `#STRUCTURAL_DIFF` + AppUi `inspector-editing#CONFLICT_RESOLUTION` + TS-web. Standalone: peers branch/merge a shared model. Rhino/gh2: Rhino sessions branch and converge. SUPERSEDES the `[2]` "Multi-user collaboration (hub+service)" MERGE_LAW spine — that concept rides `sync-collaboration#MERGE_LAW` LWW-over-(HLC,origin); GraphFork ⊃ it by replacing LWW with op-based CRDT. WIRE-LAW AMENDMENT: this changes the op-log wire vocabulary (`OpLogEntry`, `Adjudicate`) that `[3]-[CROSS_PACKAGE_LAWS]` "one wire vocabulary" pins and TS-web/Python consume — a breaking amendment to that law, not an additive surface. Third-highest value.
- Federation & Clash Engine — source-agnostic federated entity graph, persistent BVH/octree collision compute, incremental delta-only clash re-test, element-set as the universal composable BIM currency. `[NEW OWNER]` Persistence `federation#ENTITY_GRAPH` + `#ELEMENT_SET_ALGEBRA` + Rasm `geometry-kernel#SPATIAL_INDEX` (BVH/octree) + Compute intersection compute. Standalone: a hub federates models and clashes them. Rhino/gh2: a host model joins the federated graph. SUPERSEDES nothing; rides the existing PostGIS GiST substrate at `data-lanes#GEO_LANES`.
- Live Wire Studio — typed bidirectional binding of parameters/geometry to OPC-UA/Modbus/MQTT/serial + REST/GraphQL + ERP/PLM, with write-back transactions; closed-loop generalization of the one-way live-data spine. `[NEW OWNER]` AppHost `industrial-transports#BINDING_AXIS` (new transport rows + binding owner) + Compute unit coercion on `units-boundary#QUANTITY_TABLE`. Standalone: a desktop binds parameters to a PLC. Rhino/gh2: a gh2 component drives industrial output. SUPERSEDES nothing; GENERALIZES the one-way `AppUi/live-data#DATA_SOURCES` spine into closed-loop control.
- Solve Farm Studio (physics-on-DDG) — uniform multi-physics solve contract composing the existing `Rasm/Vectors` DDG operator/spectral/heat kernel, dependency job-GRAPH scheduling over the existing fan-out, and parameter spaces / sweeps / Pareto fronts as persistent queryable artifacts. `[NEW OWNER]` Compute `solver-lane#PHYSICS_CONTRACT` + `scheduling-and-lanes#JOB_GRAPH` (extends the existing fan-out) over Rasm `Vectors` field operators (substrate, runtime-proof-pending not absent). Standalone: a desktop offloads FEA to a local farm. Rhino/gh2: a gh2 solve component offloads multi-physics. SUPERSEDES nothing; EXTENDS the `[2]` "Companion compute farm" + "Sharded numeric solve" embarrassingly-parallel sharding into a dependency-graph scheduler. The field kernel is NOT a greenfield solver farm.
- Lineage Provenance Engine — W3C-PROV causal agent/activity/entity DAG as a first-class join dimension powering search, audit, sync-scoping, blame, and impact analysis; hash-chained attested ledger. `[NEW OWNER]` Persistence `provenance#CAUSAL_DAG` over the existing op-log + `redaction-retention#EXPORT_PROOF` (hash-chain over pgaudit). Standalone: a service slices lineage backward/forward. Rhino/gh2: a host op records a provenance edge. SUPERSEDES nothing; the corpus has per-operation evidence but no causal join dimension.
- Universal Access Cockpit — 3D-scene accessibility tree + spatial-audio + alternative-input fabric (3D-mouse/eye-tracking/switch/voice + device output) over the single CommandIntent algebra, plus drafting-standard i18n and live caption. AppUi `input-interaction#POINTER_GESTURES` (extends CommandIntent with alternative-input rows + device output) + AppUi `surface-3d#A11Y_TREE` (3D a11y, blocked by Infinite Viewport) + AppUi `typography-shaping#MARKDOWN_PROJECTION` (drafting-standard i18n). Standalone and rhino/gh2 share the CommandIntent algebra. SUPERSEDES nothing; EXTENDS the existing 2D a11y/i18n rails into 3D. The 2D half is strong; the 3D half is blocked by the viewport.
- DevEx Observatory — user-openable live performance HUD + flamegraphs, solve time-travel debugger, cross-machine deterministic replay-verify, macro record/replay, reproducible notebooks. `[NEW OWNER]` AppUi `observatory#PERF_HUD` + `#NOTEBOOK_MODEL` + AppHost `determinism#REPLAY_KERNEL` + `#EVENT_LOG` over the existing journal-replay + golden-image substrate. Determinism MUST consume the provider-fingerprint contract (managed/MKL/OpenBLAS bit-divergence at `Compute/numeric-lane`) or cross-machine replay-verify is unsound. Standalone: a desktop opens the HUD and scrubs a solve. Rhino/gh2: a gh2 solve replays deterministically. SUPERSEDES nothing; depends on the capability registry (event log) + Verified Kernel (solve to scrub) + Infinite Viewport (HUD metrics).
- BCF Coordination Cockpit + anchor algebra — anchor algebra (node-id/sub-entity/param-path/world-point+view) unifying comments/conflicts/presence/refs/blame, portable viewpoint receipt, BCF 2.1/3.0 + bidirectional CDE sync. `[NEW OWNER]` Persistence `annotation#ANCHOR_ALGEBRA` + `#BCF_PROTOCOL` + AppUi `surface-3d#VIEWPOINT_CODEC` (blocked by viewport) + AppHost CDE OAuth2 on `outbound-resilience#HTTP_PIPELINES` + TS-web. Standalone: a hub runs a BCF issue board. Rhino/gh2: host markup anchors through the same algebra. SUPERSEDES nothing; the anchor algebra is reused across five surfaces (comments/conflicts/presence/refs/blame).
- Drafting Engine (2D construction documents) — 3D→2D section/plan/elevation projection, hidden-line removal, dimensioning + GD&T, title-block templating (ISO/ANSI/JIS), sheet/layout management, DWG/DXF/PDF/SVG emit; the highest-frequency professional AEC deliverable, sellable INDEPENDENT of the path tracer. `[NEW OWNER]` Rasm `geometry-kernel#PROJECTION_HIDDEN_LINE` (the 3D→2D kernel) + AppUi `drafting#SHEET_LAYOUT` (sheet/title-block/dimension UI) over the existing 2D-Skia + PDF surface. Standalone: a desktop emits construction drawings. Rhino/gh2: a host model projects to sheets. SUPERSEDES nothing; viewport-INDEPENDENT (operates on geometry, not the path-traced scene). Was buried in a compound row gated behind the viewport — it is neither.
- Fabrication Director — multi-axis toolpath strategies, FK/IK with collision/reachability checking, nesting + material DB + GD&T stack-up, post-processor dispatch, closed-loop as-built feedback. `[NEW OWNER]` Rasm `geometry-kernel#CAM_MOTION` + `#NESTING` + `#TOLERANCE_GDT` + Compute collision compute. Standalone: a desktop posts toolpaths to a machine. Rhino/gh2: a gh2 component drives CAM. SUPERSEDES nothing; downstream of Verified Kernel (validated geometry) + Solve Farm (twin telemetry). UNITS-BOUNDARY CONFLICT: GD&T stack-up needs INTERIOR dimensional reasoning that `units-boundary#QUANTITY_TABLE` forbids ("a quantity type in an interior signature is the seam violation this table deletes") — resolution required (interior tolerance owner OR ratified relaxation).
- CAD-STEP Bridge — ISO 10303 AP242 solid-model exchange for mechanical CAD/PLM. `[NEW OWNER]` Compute `interchange#IMPORT_RAIL` (new `InterchangeFormat` AP242 row + codec) + Python companion (OpenCascade/pythonocc, the IfcOpenShell pattern). NAMESPACE COLLISION (load-bearing): the existing `InterchangeFormat.Ifc` row already claims the `.step`/`.stp` extension (IFC-STEP / ISO 10303-21 SPF) — CAD-STEP (AP242) is a DIFFERENT schema family and MUST be a disjoint row; the format dispatch must disambiguate the two `.step` consumers. Standalone: a desktop imports a STEP solid. Rhino/gh2: a host imports a mechanical-CAD assembly. SUPERSEDES nothing; corrects the false impression that the IFC `.step` row covers CAD-STEP.
- Generative Optimizer + differentiable geometry + surrogate/ROM duality — pluggable multi-objective optimizers over typed design-var/constraint/objective, Pareto-front as a queryable artifact, ADJOINT/GRADIENT propagation THROUGH the `Rasm/Vectors` DDG operators (the distinctive frontier), every solver carrying an in-scope-inference surrogate behind the same solve contract. `[NEW OWNER]` Compute `optimizer-lane#OBJECTIVE_VOCABULARY` + Rasm `Vectors#ADJOINT_SENSITIVITY` (extends the operator kernel) over the inference substrate. Standalone: a desktop runs a shape-optimization sweep. Rhino/gh2: a gh2 component optimizes a parameter set. SUPERSEDES nothing; the differentiable-geometry arm is the uniquely-positioned move (Tier-5 DDG kernel + ONNX already present). Depends on Solve Farm (solve contract).
- Plugin Marketplace + Capability Sandbox — WASM/process-isolated plugins with zero ambient authority, scoped revocable audited grants, signed/SLSA/semver supply chain. `[NEW OWNER]` AppHost `sandbox#GRANT_BROKER` + `#PLUGIN_REGISTRY` over the existing quotas + process-modality substrate. Standalone: a desktop runs a sandboxed third-party plugin. Rhino/gh2: a gh2 plugin runs capability-brokered. SUPERSEDES nothing; ORTHOGONAL to the in-repo density law — a sandbox is a different trust boundary, not a parallel union surface, so this is a product-scope decision, not a density conflict. Depends on the capability registry.
- Federated Geometry Search — fusion ranking over the existing pgvector HNSW + PostGIS GiST + BM25/tsvector indexes into one scored result with per-hit lineage; highest leverage-per-effort (substrate already world-class). `[NEW OWNER]` Persistence `federation#FUSION_RANK` over `cache-indexes` (HNSW) + `data-lanes#GEO_LANES` (GiST) + the FTS index. Standalone: a desktop runs a cross-modal find-similar. Rhino/gh2: a host queries the federated index. SUPERSEDES nothing; the three indexes EXIST and are world-class individually — only the fusion-ranking owner is net-new.
- Continuous-Query Engine — unified standing-query owner with tumbling/sliding/session windows + watermarks + incremental-view-maintenance, collapsing client DynamicData and server Timescale windowing into one temporal primitive. `[NEW OWNER]` Persistence `query-rail#STANDING_QUERY` (one window/watermark model) over the existing DynamicData + Timescale continuous-aggregate + DuckDB building blocks. Standalone: a dashboard tile is a standing query. Rhino/gh2: a solve-metric tile rides the same primitive. SUPERSEDES nothing; UNIFIES the existing `[2]` "Aggregate-refresh push lane" (server) and "Cross-tile linked brushing" (client) windowing into one temporal value where one definition powers a tile, an alert, a sweep, and a report.
