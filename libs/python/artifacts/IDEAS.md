# [PY_ARTIFACTS_IDEAS]

Artifact ideas extend the standalone host-free publication platform; folder tasks carry promoted work.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[FLOOR_UNGATING]-[COMPLETE]: closed at its own acceptance bar — zero UNEXPLAINED interpreter markers on artifacts rows, every surviving marker naming the break it re-reproduces and the settled lane that serves it, rather than at an empty marker set. `vtk` and `usd-core` re-reproduce two-sourced — no cp315 wheel, no abi3, no sdist anywhere, `usd-core` capping its own metadata below the floor on top of that — and `pyvista` is pure-Python gating only through its hard `vtk` requirement, while all three modules import at the estate floor off the Forge python-overlay `.pth`; that marker-plus-overlay pair IS the settled admission mechanism per `libs/python/.planning/RULINGS.md` `[01]`, the manifest marker gating the reproduced resolution break and the overlay supplying the module, and each marker retires when upstream ships a floor wheel or the estate floor moves. `lets-plot` re-reproduces the same wheel cap with nothing to substitute — it publishes no sdist and nixpkgs carries no python distribution of it — so the `<engine-unavailable>` rail its chart-spec page already models is the PERMANENT stated degrade, not a wait. `PhotoshopAPI` retired whole on the ruling that an unreachable admission is a defect rather than a gate, its manifest row, catalog, and registry card deleted and the PSD/PSB arms re-owned on `psd-tools` under live byte-exact proof. `scikit-image` had already closed on the version-pinned pythran metadata override.
[XML_TRUST_RAILS]-[COMPLETE]: landed — the ONE hardened parse fold homed at `document/model.md` `hardened_parse` with the full explicit posture (entities, DTD, network, decompression), the carded `document/emit.md` homing REFUTED on an import-cycle proof (emit imports model, so the substrate page owns the fold); emit `_VALIDATOR`/`_lxml_transform`, lens `_xml_arm`, and tagged `_declared` compose it, `defusedxml` retired to zero page occurrences with the `types-defusedxml` manifest row dropped (odfpy's own internal `defusedxml` runtime dep stays, catalog-recorded), and trusted fragments keep their in-page builders.
[QUALITY_GATE]-[COMPLETE]: landed as `delivery/gate.md` — `QualityGate` folds four verdict families (Preview raster scores, `StructureAudit`/`PreflightAudit`/`ArchiveAudit`, `ConformanceVerdict`, lens `TableAudit`) under per-kind `KindPolicy`/`Bar` policy rows with `Grade.UNMEASURED` seated between advisory and refuse so absence never reads pass; transmittal `_gated` refuses on `GateVerdict.ships` with contractual purposes refusing an ABSENT verdict; `document/lens.md` gained the `LensOp.TABLE_AUDIT` `debug_tablefinder` projection; the shipped grade rides `TransmittalIssued.gate_grade` into the notice `_GRADE_LADDER`.
[MANAGED_CONFIG_SPACE]-[COMPLETE]: landed at `graphic/color/managed.md` — `ConfigSource` is the closed four-case config graph (memoized per worker, roles named over strings, file-rules classification read off the same graph), `ManageOp.Space`/`View` resolve every end by config name or role, and the ownership tension resolved as a discriminant: the config graph owns MEANING (colorspace-to-colorspace, display/view), the ICC edge owns DEVICES (profile-to-profile at `Managed`/`Plane`), and no arm carries a second authority knob.
[DEEP_MEASUREMENT]-[COMPLETE]: landed across the deep substrate — `fidelity()` at `graphic/texture/plane.md` grades every lossy encode against its own decode round-trip on the receipt band, and `set.md` publishes the measured `tile_score` beside the declaration-sourced `tiled`, absent where no seam-bearing role reached the probe; the host tension resolved per the strata refutation — `raster/measure.md` never becomes the texture owner, the two substrates keep their own score vocabularies.
[DASHBOARD_ARTIFACT]-[COMPLETE]: landed as `visualization/dashboard.md` — `DashboardPlan` folds a `DashPane` deck (chart/table/figure) into one offline document over ONE `vl_convert.javascript_bundle(vl_version=…)` runtime with per-pane `vegaEmbed` mounts, markup assembled through `string.templatelib.Template` under the destination-keyed `_ESCAPE` fold, minting the new banded `ArtifactReceipt.Dashboard` kind; the carded claim that `javascript_bundle` takes `snippet` positionally with no default is REFUTED — both parameters default `None` and the default snippet publishes `vegaEmbed`/`vega`/`vegaLite` onto `window`, which is what makes one shared bundle serve every pane.
[HOOK_POINT_TABLE]-[COMPLETE]: landed as `core/hooks.md` — `ArtifactHook`/`ARTIFACT_POINTS` over the runtime `Hooks` registry with the scope-keyed `Production` register/fire/subscribe surface; fire seams seat at the fold holding the settled evidence — `core/issue.md` (`_issued`/`_planned`/`_driven`), `core/receipt.md` `contribute` for the roster-wide `ReceiptEmitted`, and `delivery/transmittal.md` `_emit` for `TransmittalIssued`; the carded `contribute`-fires-`TransmittalIssued` seat is REFUTED, since that fold sees six case scalars where the announcement needs the whole `TransmittalEvidence`.
[ISSUE_ATTRIBUTION]-[COMPLETE]: landed as the `ArtifactIssue._scoped` bracket — issue scope with parent-respecting tenant baggage under `ISSUE_BAGGAGE`/`TENANT_BAGGAGE` with `bound_contextvars` log keys; tenant metric promotion stays runtime-owned per `core/receipt.md` `[METRIC_SIGNALS]`, the issue scope a log/baggage dimension by cardinality law.
[ISSUE_ANNOUNCEMENT]-[COMPLETE]: landed as `delivery/notice.md` `TransmittalNotice` — one `Project` row over the `TRANSMITTAL_ISSUED` fact answering a `runtime/transport/event#MESSAGE` message envelope, attributes derived off the fired identities, the typed roster slots filled, and DSSE riding a bound signer port; the carded `cloudevents.v1` message envelope with structured/binary rows and a `NOTICE_ISSUED` fire is REFUTED — that legacy tree checks a required-NAME subset alone, and lowering, format, and delivery are rows at the transport owner.
[PRODUCER_BENCH]-[COMPLETE]: landed as `core/bench.md` — `CORPUS` subject rows with typed `BenchFeed` deterministic-input edges, seeded `RECIPES`, and the `benched` feed-resolution entry onto the runtime `Bench.graded` tier.
[DIAGRAM_DXF]-[COMPLETE]: realized as `export/dxf`'s `Diagram` arm — `DiagramLower` lowers the positioned glyph sequence to `DxfEntity` cases under `Standard.seed` regime-pen layers, glyphset owning the shared lowering derivations (`mark`, `Port.seat`, `AreaMark.centroid`, `ER_CAPS`, `ENTITY_BAND`); draw stays two-arm.
[WTPDF_AUDIT]-[COMPLETE]: landed as `UaCheck.WTPDF_ACCESSIBILITY`/`WTPDF_REUSE` clause rows on `document/tagged#ACCESS` — `pdfd:conformsTo` spellings confirmed at the PDF Association source (erratum-canonical `wtpdf#accessibility1.0`/`wtpdf#reuse1.0` with the as-published `/#` forms), read off the raw `/Metadata` packet because the pikepdf mapping view cannot decode the declarations bag.
