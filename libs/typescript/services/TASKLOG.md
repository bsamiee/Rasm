# [SERVICES_TASKLOG]

Open and closed work for the node tier, distilled from `IDEAS.md`. Each open card's leader carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — and three to four scoped bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks; closed work moves to `[2]-[CLOSED]`.

## [1]-[OPEN]

[QUEUED] Author the `observability/` SLO and error-budget sub-domain (from BACKEND_RUNTIME_SLO)
- Author the net-new `observability/` sub-domain: the closed `Objective` vocabulary (`latency`/`availability`/`saturation`/`freshness`), the multi-window multi-burn-rate fast/slow fold over the stored signal series, the `AlertRoute` `Data.TaggedEnum` over the delivery sinks, and the OTel reader that drives them — the node analog of `platform/web-vitals/performance-budget`, distinct from the deploy-time `provisioning/contract#PROVISIONING` `ObservabilityStack.emit` collector.
- Integrate `@effect/opentelemetry` (the `Metrics`/`Tracer` reader), `@effect/sql`/`@effect/sql-pg` (the signal series and burn-rate windows over the one `PgClient`), `@effect/cluster` `ClusterMetrics` (the five runtime gauges `entities`/`singletons`/`runners`/`runnersHealthy`/`shards` as the `saturation` objective's cluster signal source), `effect` (the multi-window fold); alert delivery reuses the `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX` `DeliverySink` family, never a parallel notification path.
- Internal: rides the one `persistence/store-boundary#STORE_BOUNDARY` `PgClient`; the evaluation runs as a `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` durable cron; reads the `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` `ClusterMetrics` gauges rather than a re-derived shard scrape; decodes the C#-minted health level and trace context off the wire as the node-side consumer of the cross-`libs/` `ONE_HEALTH_DEGRADATION_WIRE`/`ONE_DISTRIBUTED_TRACE` master ideas, never re-minting.
- Key consideration: the burn-rate windows are the Google SRE multi-window multi-burn-rate algebra; the objective vocabulary carries its own threshold/window rows so a new objective is one row; the cross-cutting `effect-opentelemetry.md` reader surface and the folder `effect-cluster.md` `ClusterMetrics` gauge surface are already catalogued — no new admission, name both on the page Packages line.

[QUEUED] Author the `secrets/` runtime secret-governance sub-domain (from RUNTIME_SECRET_GOVERNANCE)
- Author the net-new `secrets/` sub-domain: the one `SecretStore` `Effect.Service` over the `ConfigProvider` boundary, the `SecretRef` `Data.TaggedEnum` over the provider axis (ESC env / Doppler / static config admitted; an AWS Secrets Manager arm gated on admission), the TTL-leased cache with rotation-driven `SubscriptionRef` invalidation, and the audit receipt every resolution emits.
- Integrate `effect` (`ConfigProvider`/`Config.redacted`/`SubscriptionRef`), `@pulumi/esc-sdk` (`EscApi.openAndReadEnvironment`, already consumed deploy-time), and `@dopplerhq/node-sdk` — all admitted; the AWS Secrets Manager arm is gated on an orchestrator-owned `@aws-sdk/client-secrets-manager` workspace-catalog admission (absent today), so it lands only after that admission, never as a settled provider row before.
- Internal: wires the resolver to the `authn/verifier#VERIFIER` keys, the `durable-execution/ai-activity#AI_ACTIVITY` provider keys, the `persistence/store-boundary#STORE_BOUNDARY` connection string, and the `object-store` credentials; the deploy-time `provisioning/contract#PROVISIONING` ESC/Doppler resolution and the runtime resolution meet at the one `ConfigProvider` boundary, never two parallel secret schemes. No C# wire coupling.
- Key consideration: a rotation invalidates the lease and the dependent owners re-resolve through the `SubscriptionRef.changes` edge rather than a restart; the audit receipt rides the `persistence/tenancy#TENANCY` GUC; the `.api/` extends `pulumi-esc-sdk.md`/`dopplerhq-node-sdk.md` with the runtime read surface.

[QUEUED] Author the `object-store/` blob-store tier and collapse the scattered presign (from OBJECT_STORE_TIER)
- Author the net-new `object-store/` sub-domain: the one `ObjectStore` `Effect.Service` over the `@effect-aws/client-s3` `S3Service` layer, the `ObjectKey` content-addressed brand (the `interchange` `XxHash128` content key), the `PresignGrant` `Data.TaggedEnum` over the get/put/multipart verbs, and the lifecycle/expiry policy rows — moving the S3 presign and the asset-export codec fan-out out of `persistence/work-and-signals#WORK_AND_SIGNALS`.
- Integrate `@effect-aws/client-s3` (the `S3Service`-layer-as-service binding, beyond the raw `@aws-sdk/client-s3`), `@aws-sdk/s3-request-presigner` (the presign verbs), `@effect/platform` `Multipart` (the streaming upload boundary), and the asset codecs (`exceljs`/`papaparse`/`jspdf`/`jszip`/`sharp`) as one `ObjectStore.put` fan-out; no new package admitted.
- Internal: wires the store to the `interchange` content-key parity (a stored blob is byte-addressable identically across runtimes), the `persistence/tenancy#TENANCY` GUC gating each presign grant, and the durable tier; the codecs move from `persistence/work-and-signals#WORK_AND_SIGNALS`, leaving that page the job/journal/flag concern only.
- Key consideration: the `ObjectKey` reuses the cross-`libs/` `ONE_CONTENT_IDENTITY` seed rather than a second key scheme; the presign-grant rows carry their own TTL/verb scope; the folder `.api/` already carries `effect-aws-client-s3.md` and `aws-sdk-s3-request-presigner.md` — re-point the codec references from `work-and-signals`. The branch cross-cutting `effect-platform.md` catalogue currently scopes OUT the `Multipart` module ("no planning owner stands up an HTTP server, a CLI") — this task's `ObjectStore.put` streaming-upload boundary and the branch `PUBLIC_EDGE_INGRESS` `edge/` folder both need it, so the branch catalogue extension (`Multipart`, and the `HttpApi*`/`HttpServer*`/`OpenApi*` surfaces the `edge/` folder needs) is an orchestrator-owned branch-`.api/` finding, not a folder-`.api/` edit.

[QUEUED] Author an idempotency/replay-dedup ledger owner (deferred frontier)
- A durable idempotency/replay-dedup ledger: a single keyed ledger that records a request/command idempotency key and its terminal outcome so a re-delivered message is short-circuited to its prior result rather than re-executed, giving the outbox relay, the entity actors, and the agent turns one shared exactly-once dedup surface.
- Integrate `@effect/sql`/`@effect/sql-pg` for the keyed ledger over the one `PgClient`; `effect` for the claim-or-replay fold.
- Internal: rides the one `persistence/store-boundary#STORE_BOUNDARY` `PgClient`; serves `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX`, `messaging/entity#ENTITY`, and `agent/session-actors#SESSION_ACTORS` as the shared dedup owner, never a per-consumer ledger. No C# wire coupling.
- Key consideration: a candidate, not yet authored — the cluster `MessageStorage` `SaveResult.Duplicate` and the workflow `idempotencyKey` already carry per-surface dedup, so the ledger admits only the cross-surface dedup the built-in primitives do not cover; record the overlap before authoring.

[QUEUED] Author a quota/backpressure governor owner (deferred frontier)
- A per-tenant quota and backpressure governor: a durable rate/concurrency cap over the remote-lane callouts, the agent turns, and the outbox external sinks, distinct from the workflow `DurableRateLimiter` per-key cap, so a tenant cannot starve the cluster.
- Integrate `@effect/workflow` (`DurableRateLimiter` per-key primitive), `@effect/experimental` (the rate-limiting module backing the aggregate governor), `@effect/cluster` (the entity placement for a per-tenant governor actor); `effect` for the backpressure `Stream` regulation.
- Internal: a per-tenant governor is a `messaging/entity#ENTITY` actor placement candidate; caps the `agent/agent-runtime#AGENT_RUNTIME` agent turns and the `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX` external sink. No C# wire coupling.
- Key consideration: a candidate, not yet authored — the workflow `DurableRateLimiter` is the per-key primitive and `@effect/experimental` carries a rate-limiting module; the governor admits only the per-tenant aggregate backpressure neither covers; record the boundary before authoring.

[QUEUED] Author an embedding-lifecycle owner (deferred frontier)
- An embedding-lifecycle owner: the re-embed scheduling, the embedding-profile migration, and the staleness-driven refresh the `hybrid-search/fused-rank#FUSED_RANK` semantic arm depends on, beyond the single re-embed `WorkQueue` row the search references today.
- Integrate `@effect/ai` (`EmbeddingModel.makeDataLoader` for the windowed batched embed `{ window, maxBatchSize }` and `EmbeddingModel.make` per-call cache `{ capacity, timeToLive }` — the admitted surface no page yet drives, `EmbeddingModel.embed`/`embedMany` for the call), `@effect/sql`/`@effect/sql-pg` for the profile/staleness columns, `@effect/workflow` for the durable re-embed schedule.
- Internal: feeds `hybrid-search/fused-rank#FUSED_RANK` the fresh embeddings and the profile dimension; rides the one `persistence/store-boundary#STORE_BOUNDARY` `PgClient`; the re-embed runs as a `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` durable job. No C# wire coupling.
- Key consideration: a candidate, not yet authored — the search page already owns the staleness column and the re-embed `WorkQueue` row; the lifecycle owner admits only the profile-migration and batch-embed orchestration the search arm does not own; record the split before authoring. The `effect-ai.md` catalogue already carries `EmbeddingModel.makeDataLoader`/`make` — name them on the page Packages line, no new admission.

[QUEUED] Author the reactive-query invalidation owner (from REACTIVE_QUERY_INVALIDATION)
- Author the read-side reactive-query owner: the `@effect/sql` `SqlClient.reactive([keys], query)` query bound over the `@effect/experimental` `Reactivity` key-scoped invalidation substrate, so a write invalidates exactly the touched query keys and a dependent read re-runs as a `Stream` rather than a poll. Lands as a new `persistence/reactive-query` page (the read-side sibling of `store-boundary`/`work-and-signals`), one `ReactiveQuery` owner over the entity-keyed invalidation rows.
- Integrate `@effect/sql` (`SqlClient.reactive`, the reactive-query method on the one client), `@effect/sql-pg` (the `Reactivity.Reactivity` requirement the `PgClient` layer already discharges via the bundled default), `@effect/experimental` (`Reactivity` — the key-scoped mutation/query/stream invalidation, a NEW first-class direct consumer beside the `Sse` decode), `effect` for the `Stream` fold; no new package admitted.
- Internal: rides the one `persistence/store-boundary#STORE_BOUNDARY` `PgClient`; the `interchange` `CommandGateway` mutation and the `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX` relay publish the invalidation keys, the `messaging/internal-rpc#INTERNAL_RPC` query procedures and the `observability/` signal read subscribe; distinct from the browser `projection` decoded-stream fold (this is the server-side query-invalidation edge). No C# wire coupling.
- Key consideration: re-opens the over-narrow `EXPERIMENTAL_RETIRE_OR_KEEP` keep-on-`Sse`-only disposition — `Reactivity` is a second direct `@effect/experimental` consumer; the invalidation keys are entity-scoped so a new reactive read is one `reactive` row keyed by entity, never a parallel change-feed; the `effect-sql.md`/`effect-sql-pg.md`/`effect-experimental.md` catalogues already carry `SqlClient.reactive`/`Reactivity` — name them, no new admission.

[QUEUED] Author the native delayed-delivery and per-key timer owner (from NATIVE_DELAYED_DELIVERY)
- Author the `DeliverAt` delayed-delivery owner on `messaging/entity#ENTITY`: a `DeliverAt`-implementing entity payload schedules its own message for a future `DateTime` through the shard manager, the per-key future-message rail beneath the cluster-wide `runtime-backplane` cron. Lands as a `DelayedDelivery` ROW on `messaging/entity` (not a new page — it is the timer modality of the addressable actor), wiring the per-key retry-after, the deferred saga step, the agent-turn cooldown, and the scheduled tenant action onto the cluster's own delayed-delivery protocol.
- Integrate `@effect/cluster` (`DeliverAt` symbol protocol, `DeliverAt.isDeliverAt`/`DeliverAt.toMillis`, the entity-payload future-delivery scheduling), `effect` (`DateTime` for the delivery instant); no new package admitted.
- Internal: composes with the `messaging/entity#ENTITY` and `agent/session-actors#SESSION_ACTORS` actors and the `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` cron — the cron stays the cluster-wide pinned sweep, `DeliverAt` becomes the per-key future-message rail; the saga `durable-execution/saga#SAGA` deferred-step delay and the `durable-execution/ai-activity#AI_ACTIVITY` retry cooldown compose this rather than `Effect.timeoutTo`/a parallel timer table. No C# wire coupling.
- Key consideration: `DeliverAt` is exactly-once and shard-placed like every cluster message, never a second scheduling substrate; the timer concern is currently spread across the saga timeout, the activity retry schedule, and the cron with no per-key future-delivery owner; the `effect-cluster.md` catalogue already carries `DeliverAt` — name it, no new admission.

[QUEUED] Author the restart-surviving per-key resource owner (from ENTITY_RESOURCE_LIFETIME)
- Author the `EntityResource` per-key resource-lifetime owner on `messaging/entity#ENTITY`: a scoped resource acquired inside an entity (the persisted `Chat` handle, an external tool-host session, a connection pool) that survives runner restarts until idle-TTL elapses or `close` fires, with the `makeK8sPod` arm placing a managed pod resource per key. Lands as an `EntityResource` ROW on `messaging/entity` and is composed by `agent/session-actors#SESSION_ACTORS` for the per-session `Chat`/tool-host lifetime, the resource-durability modality beside the state-durability the actors already own.
- Integrate `@effect/cluster` (`EntityResource.make({ acquire, idleTimeToLive })`, `EntityResource.makeK8sPod`, the `CloseScope` tag, the `K8sHttpClient` backing the pod arm), `effect` (`Scope`/`Duration` for the resource scope and idle-TTL); no new package admitted.
- Internal: composed by `agent/session-actors#SESSION_ACTORS` (the persisted `Chat` handle and the host tool-transport session bound to the session key rather than re-acquired per `Turn`) and `messaging/entity#ENTITY` (the collaborative-session external sink); the k8s-pod arm's pod spec arrives from the `provisioning/contract#PROVISIONING` deploy tier. The actors persist STATE through the `PgClient` today and hold no live RESOURCE bound to the key — the exact gap. No C# wire coupling beyond the agent's own tool transport.
- Key consideration: the resource idle-evicts so an actor's external session is one `EntityResource.make` row, never a re-acquire-per-message cost or an unbounded leak; the `effect-cluster.md` catalogue already carries `EntityResource`/`CloseScope`/`makeK8sPod`/`K8sHttpClient` — name them, no new admission.

## [2]-[CLOSED]

[COMPLETE] Add the MCP agent-transport consumer at `agent/mcp-transport` (from MCP_AGENT_TRANSPORT) — authored the node-tier decode of the settled `csharp:Rasm.AppHost/agent/mcp-projection#TS_PROJECTION` single mint: `catalog` lifts each `McpToolWire` into one `@effect/ai` `Tool.make` with host-validated `payload` passthrough + effect-hint annotations, `progress`/`resume` fold the SSE stream through `@effect/experimental` `Sse`, `costPreview`/`result` decode the cost and `ReceiptEnvelopeWire<ToolResultWire>`. Six decode-only `*Wire` carriers, no re-mint; the lifted `Toolkit` enters `agent/agent-runtime#AGENT_RUNTIME`.

[COMPLETE] Resolve the retire-or-keep gate on `@effect/experimental` (from EXPERIMENTAL_RETIRE_OR_KEEP) — RE-OPENED, re-resolved keep-with-three-direct-consumers: the `Sse` decode (`agent/mcp-transport`), the `Reactivity` key-scoped invalidation (`persistence/reactive-query`), and the rate-limiting module (the quota governor); `EventJournal`/`PersistedQueue`/`Persistence` stay transitive. No move to transitive-only.

[COMPLETE] Author the `authn/verifier` server-side credential-verification page (from AUTHN_VERIFIER) — `Authn`, one `Effect.Service` over the closed `AuthCommand` `Data.TaggedEnum` (TOTP via `otplib`, WebAuthn via the four `@simplewebauthn/server` primitives, OAuth, API-key) folded by one `$match`, verifying every credential the `platform` browser ceremony forwards over `interchange` `CommandGateway`; rides the one `PgClient` inside the `persistence/tenancy#TENANCY` GUC, crosses no .NET wire.

[COMPLETE] Reconcile the `otplib` cross-cutting README line (from OTPLIB_CROSS_CUTTING_RECONCILE) — `otplib` is genuinely branch-cross-cutting (the `platform` browser owns the TOTP ceremony, the `services` `authn/verifier#VERIFIER` owns server-side verification), so it stays in both `[CROSS_CUTTING]` sections with the branch `otplib.md` catalogue; the README line now names the `authn/verifier#VERIFIER` owner. No move to folder-local `[PACKAGES]`.

[COMPLETE] Author the `eventing/transactional-outbox` page (from TRANSACTIONAL_OUTBOX) — the `Outbox` `Model.Class` written in the same `SqlClient.withTransaction` as the domain mutation, the `OutboxRelay` draining via `FOR UPDATE SKIP LOCKED` + `LISTEN`/`NOTIFY`, the `DeliverySink` `Data.TaggedEnum`; shares the one `PgClient`, registers as a `runtime-backplane` singleton, gives `work-and-signals` `EventJournal`/`Notifications` their atomic-publish contract.

[COMPLETE] Author the addressable-`Entity` actor page at `messaging/entity` (from ENTITY_ACTOR_MODEL) — `SessionEntity`, an `Entity.fromRpcGroup`/`toLayer` actor with `ClusterSchema.Persisted` rpcs, `getShardId`/`getShardGroup` single-writer placement, and the `EntityProxy` projection; rides the workflow shard manager, persists per-key state through the one `PgClient`, and the collaborative `Apply` decodes the C# CRDT op-log wire off `interchange` without re-mint.

[COMPLETE] Compose the `agent/` subpath closure: agent-runtime + session-actors (from DURABLE_AGENT) — no stock `DurableAgent` exists, so `agent/agent-runtime` COMPOSES it from `Activity` + `@effect/ai` `Toolkit`/`Tool`/`Chat.layerPersisted` on `ClusterEngine` over the one `ai-activity#AI_ACTIVITY` provider axis, and `agent/session-actors` pins one conversation to a single-writer `Entity`; both ride the one `PgClient` and shard manager.

[COMPLETE] Upgrade `hybrid-search/fused-rank` fusion to RRF-over-BM25 (from RRF_BM25_FUSION) — replaced the weighted-sum lexical arm with BM25 and fused four `RANK()` CTEs by Reciprocal Rank Fusion (scale-invariant, per-signal normalization dropped), the `RerankModel` cross-encoder terminal kept; the BM25 extension (`pg_search`/Tiger) is a RESEARCH item gated on the provisioned Postgres image, the C# `federation#FUSION_RANK` stays distinct.

[COMPLETE] Extend `services/.api/pulumi-pulumi.md` with the automation event-stream catalogue (P1) — section [4]-[AUTOMATION_EVENT_STREAM] transcribed from the installed `@pulumi/pulumi@3.234.0` `automation/events.d.ts`/`stack.d.ts` (`EngineEvent`, the 15-member `OpType`, the 6-member `DiffKind`, the `previewRefresh`/`onEvent`/`changeSummary` driver), clearing the `provisioning/drift#PROVISIONING` RESEARCH flag.

[COMPLETE] Wire the continuous drift sweep at `provisioning/drift` (P2, from CONTINUOUS_DRIFT_SWEEP) — `DriftSweep` ROW: a `runtime-backplane` `ScheduledWork.cron` shard-pinned sweep resolving the ESC policy-pack environment via `@pulumi/esc-sdk`, re-running `computeStackDrift`, and emitting the primitive `StackDriftSummary` to the `provisioning/contract#PROVISIONING` `ObservabilityStack.emit` collector; deploy-time-only, no `@pulumi/*` type crosses to the hot path.

[DROPPED] Declare the implementation-time subpath `exports` — folded into `provisioning/contract#PROVISIONING`: the page Entry line already owns the `./provisioning` subpath `exports` as a transcription-time mechanic, so a pure build-fence step is no standalone idea-driven card; the `./agent` subpath is owned the same way by `agent/agent-runtime#AGENT_RUNTIME`.
