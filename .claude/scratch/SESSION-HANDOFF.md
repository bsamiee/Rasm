# [SESSION_HANDOFF] — geometry campaign + durable-doc repair

Session continuation brief. Read fully before acting. Everything after commit `93f00e08f` is UNCOMMITTED BY INSTRUCTION — the user commits on request only.

## [01]-[MISSION_STATE]

- **CS geometry campaign leg 1 is RUNNING**: run `wf_e5cec0fa-836` (task `wnn6jcp72`, one resume already consumed the plan cache). Brief = `RASM-CS-GEOMETRY-DECISION.md`. Ledger: `.claude/scratch/geometry-run-ledger.md` (leg-2/3/4 invocation rows live in DECISION `[10]`; targets must be repo-relative `libs/csharp/Rasm/.planning/...` paths — the engine aborts on `.planning`-relative ones).
- On leg-1 return: (a) empty `libs/csharp/Rasm/IDEAS.md` + `TASKLOG.md` to the `(none)` boilerplate pattern (the ONLY pool not yet emptied — held for write-race; exemplar = `libs/typescript/.planning/IDEAS.md`); (b) leg-1 `hard_residual` is SKIPPED by default (in-run reconcile fixed the real ones); only a genuinely real OUT-OF-SCOPE item gets ONE fable fix agent, and only when its files cannot collide with a running workflow — else drop and forget, never chase noise; (c) once leg 1 AND the energy implementer are both done: `git add -A` + commit everything (user-authorized gate commit), then read `RASM-CS-GEOMETRY-BRIEF.md` and launch leg 2 fresh per DECISION `[10]` row 2 — no further user signal needed.
- Campaign order after geometry closes: **Persistence next** (user-stated), then per `[TRACK_ORDER]` lines in every CS brief.

## [02]-[LIVE_STATE]

1. **Geometry leg 2 RUNNING** (`wf_219d9ec6-cc4`, task `w33jwc1xx`, current self-contained engine). Leg 1 CLOSED (14 agents, 0 errors; its 3 `hard_residual` items were forward-leg W2/W3/W4 respell advisories — SKIPPED per policy). **GATE COMMIT DONE: `7c0c9d30`** (64 files — leg 1 + energy landing + engine retirement + doc repairs + user card emptying). Phase-C, energy implementer, and the doc sweep are all DONE.
2. **Energy CRITIQUE DONE** (pre-grounding): glazing growth realized in Compute `BuildConstruction`, window double-count fixed (`Host` filter + `OpeningsOf` + typed `SubSurface`), dragonfly `Room3ds` + door lowering made real, dedup-keyed `LowerComposition`, projector statics for the boundary property names, fault discipline confirmed zero-new-cases; its 2 out-of-set residuals (assessment.md stale 3-arg comment; `Node.Object` camelCase params on `Graph/element.md`) are FOLDED INTO Track CS. Save-point commit `80812e8f`. **ALL THREE TRACK INITIALS RUNNING** (fable): Track CS (Energy/ split + full csharp energy pass + the 2 residual fixes), Track PY (13-package energy band at the brief-routed homes, artifacts folder UNTOUCHABLE), Track TS (Speckle/geospatial/energy enablement across areas). On each initial's return: save-point commit → launch that track's critique (then redteam), each over the full scope its initial touched, rebuild-grade.
2b. **`finalize.js` AUTHORED AS DURABLE** (registered in CLAUDE.md roster + prime dispatch): language-agnostic corrections/closure engine — Plan → Strata (folder dependency order from disk) → per-folder [1 opus per page Discover → 1 fable Fix] sequential, downstream residual routing, terminal sweep. QUEUED as geometry LEG 5 (post leg-4; ledger carries the invocation with the user's 7 folders; Drawing/Solving/Vectors addable at launch — ask the user then).
3. **DONE by main agent**: persistence DECISION `[04]` rows 1/8 reworded to LANDED improve/extend framing; card pair emptied BY THE USER (never re-do).

## [03]-[WHAT_WAS_DONE]

Commits (chronological): `54cd740e` baseline (DECISION+scratch+workflow deltas) · `a89e8aa5` pre-leg `[09]` roster motion · `27997d6d` settling rows dropped from all 11 brief EXECUTION tables + plain renumbering · `c7f7e2d77` `[TRACK_ORDER]` line under every CS brief H1 · `3fe638aa5` Discover hardening + REMOVAL DISCIPLINE law in rebuild.js · `93f00e08f` card pools emptied. Everything since = uncommitted.

**Pre-leg roster motion (executed, annotated as EXECUTED PRE-LEG in DECISION `[09]`)**: MathNet HELD at `6.0.0-beta2` — survival sweep found `Integrate.OnCuboid` (Compute `Tensor/quadrature.md:109`) is 6.0-only; blocker comment on the props pins. OpenBLAS kernel reference+catalog dropped (pin stays — Compute references it). Float-lane pins re-grouped to Fabrication label. `api-manifold.md` authored from verified `manifoldc` v3.5.2 header. HighPerformance admitted to kernel. Six GShark catalog corrections. `api-hashing.md:82` XxHash128 truth. Kernel `api-csparse.md` deduped. `api-rhino.md:145` heal-claim corrected (the DECISION's "verified landed" claim was FALSE on disk). `dotnet restore` green.

**Engine changes (`rebuild.js` + `resolve-residuals.js`, parse-verified)**: Discover is now HOSTILE (adversarial parity, whole-folder inventory diff, concrete members + integration shape in `apiUnderutilized`, ZERO removal authority). Shared LAW gained REMOVAL DISCIPLINE (removal only on explicit brief ruling; phantoms the sole exception). Reconcile = read-only opus investigation fan (≤5 via `TRIAGE_CAP`, verdicts real/stale/phantom/duplicate, disk-cited `evidence` FACTS — no fix proposals, fail-open on death) → ONE fable implementer with WHOLE-REPOSITORY write authority + one retry; no verify, no `hard_residual` channel, no handoff. `resolve-residuals.js` DELETED (2026-07-04): all former emitters (`stack-py`/`stack-ts`/`stack-cs`/`rebuild-api`/`ideate`/`align-cards`) now surface `unresolved` in their returns, never hand off; `ideate`/`align-cards` are card-era rails pending a user retirement ruling. Engine facts: plan=sonnet-low, discover=opus-max, implement/critique/redteam/reconcile=fable; 4 pages per implement/critique/redteam agent; plan sorts pages ALPHABETICALLY so **legs are the only dependency barrier** (leg = barrier + residual-clean checkpoint); brief step-count differences are notation (looped vs unrolled), all CS campaigns were 4 legs.

**Card-pool retirement**: every `IDEAS.md`/`TASKLOG.md` under libs/python, libs/csharp, both branch `.planning/`, and `libs/.planning/` emptied to boilerplate + `(none)` (16 folders, 32 files; kernel Rasm pair pending leg-1). Card governance is DEAD repo-wide.

**Wave B — all 15 root RASM-* docs hardened (1 fable agent per doc)**: batch 1 (persistence pair, fabrication pair, compute/apphost/appui briefs, generation spec) = card scrub + findings re-homed as direct law + removal re-audit; batch 2+ (5 py briefs, geometry pair) = EXPANDED mandate: verdict audit/extend-in-place, orphan sweep to zero, feed-verified new admissions, logical corrections, executable surfaces byte-stable (geometry `[10]`/`[EXECUTION]` proven byte-identical by hash/diff; EXECUTED PRE-LEG paragraph preserved).

**Issue classes found + fixed in the docs**:
1. Card governance woven everywhere (disposition tables, `TASKLOG:NN` anchors, BLOCKED-card gates) — removed; every finding re-homed (phantoms→corpus-truth statements, growth cards→growth registers on owning verdicts, BLOCKED gates→named blocker conditions).
2. **INCORRECT REMOVAL LANGUAGE** — the central issue: prunes justified by zero-consumers/"charter misplacement" (persistence PRUNE table: PollinationSDK, ScyllaDB, NTS bridges, EF-Sqlite, Redis). OPERATOR LAW: every admitted package was admitted for a purpose; the disposition is FULL LIB-GRADE INTEGRATION at its proper home (re-home with integration legal, prune not); branches independent lib-grade in isolation, aligned never coupled, never 1-1-1 mirrored (Speckle: cs SDK complete / ts viewer+store ENABLEMENT / py absent by law; energy: py ladybug band in-isolation / cs Compute-runner + Bim exchange / ts none). License-gate rejections (GPL/AGPL) remain sanctioned law, but prefer boundary/out-of-process shapes where posture allows. Wave-B agents already flipped several (3 NTS prunes had live consumers contradicting their proofs); the Phase-C implementer finishes the persistence pair.
3. Stale line anchors corpus-wide — hundreds re-pinned against disk (drift corrected, never treated as refutation).
4. Real contradictions fixed: py-compute `[V9]`⇄E10, py-data `[00]`⇄`[V12]`, geometry E13 vs the executed MathNet hold, false "verified landed" claims.
5. Unnecessary leg barriers — persistence/fabrication merging 4→3 in flight; compute/apphost/appui still 4-leg (user-gated future merge).

**Integration dossiers (binding blueprints on disk, feed input already consumed by live agents)**: `.claude/scratch/integration-fix/dossier-cs-persistence.md` (all 8 prunes→integrations; PollinationSDK three-row store split + Compute counterpart; ScyllaDB wide-column axis + `WideColumnFault` 8450; NTS spatial plane + NEW `Ingest/geospatial.md` 8440; EF-Sqlite embedded provider row on the ONE identity DbContext; Redis L2 backplane + `EgressSink.RedisStream`; linq2db COMMIT; `[V13]` 7→11; NO fifth leg) and `dossier-cross-language.md` (G1 Bim energy phantom → being implemented NOW; G2 PollinationSDK split; G3 cross-language concert seams ride owning-doc LEDGER ROWS, never resurrected cards; py energy fully brief-routed; three C# hinge pins feed-verified current).

## [04]-[REMAINING_WORK] (ordered)

1. DONE: leg 1 returned → residuals skipped (forward-leg advisories) → gate commit `7c0c9d30` → leg 2 launched (`wf_219d9ec6-cc4`).
2. Energy critique returns (leg 2 already launched) → launch THREE PARALLEL TRACKS, each an internal fable initial→critique→redteam chain at full rebuild hostility/ultra-stacking, each composing its `docs/stacks/<lang>/` in full:
   - **TRACK CS**: the split is the OPENING MOTION, never the job — initial = split `Rasm.Bim/.planning/Exchange/energy.md` into NEW folder `Rasm.Bim/.planning/Energy/` as `exchange.md` (entry + `EnergyOp`/carriers/receipts), `projector.md` (five-arm raise + `EnergyClassRows`), `derive.md` (lower fold + `EnergyTranslate` matrix) — NOTHING stays in `Exchange/` (the five format rows + `InterchangeCodec.EnergyModel` on `Exchange/format.md` are the exchange-side boundary and already live there); re-point EVERY `Exchange/energy` anchor (9 files: Bim format/faults/ARCH/README, Compute energy/ARCH/README, persistence DECISION row 8) + Bim codemap/router rows; THEN the FULL rebuild-grade hostile pass over the whole touched scope at `rebuild.js` implement quality (disbelieve every fence, collapse parallel shapes into `[Union]`/`[SmartEnum]`/`[ValueObject]`/folds, OWNER_CHOOSER + KNOB_TEST + aspects + rails per shape, both-tier `.api` ultra-stacking, capability extension citing package/domain/consumer gap sources, phantom deletion) — Bim Energy/+Semantics, Compute Analysis, Element material evidence, Persistence artifact/blob rows, openstudio/energyplus/pollination surfaces mined to depth. Its critique and redteam audit the FULL touched scope at the rebuild.js critique/redteam bar — never bound to the split files. HARD PROHIBITION: never touch `libs/csharp/Rasm/**` while a geometry leg runs.
   - TRACK PROMPT LAW (all three tracks + parity): the initial/critique/redteam prompts are authored FROM the `rebuild.js` doctrine blocks (LAW + ADVERSARIAL + capability-EXTEND + prose/comment hygiene + member-verification rail) with each track's `docs/stacks/<lang>/` read law composed IN FULL — same aggression, same collapse mandates, same write-fully discipline.
   - **TRACK PY**: implement the FULL pyproject energy band into libs/python at the brief-routed homes — `honeybee-core`/`honeybee-energy`/`honeybee-openstudio`/`honeybee-standards`/`honeybee-energy-standards`, `dragonfly-core`/`dragonfly-energy`, `ladybug-core`/`ladybug-geometry`/`ladybug-comfort`, `queenbee`, `lbt-recipes`, `pollination-handlers` (PY-GEOMETRY `[V1_ENERGY_PLANE]` four-page energy ladder, PY-RUNTIME `[V3_RECIPE_OWNER]` `execution/recipe.md`, PY-DATA `[V4]` LCA band) — independent lib-grade in isolation, wire-aligned to cs at content-keyed HBJSON bytes (`XxHash128`), never coupled, serious depth, no guessing.
   - **TRACK TS**: general integration/capability improvement across the relevant libs/typescript areas (store/, ui/, browser/, wire/, kernel/, state/, work/ as the concern demands) enabling the future Speckle app + geospatial + energy evidence handling — agnostic proper-lib style, zero hardcoding, backend-neutral (serves the cs or py producer without differentiation).
   - CROSS-OWNER FRAMING (user-explicit): surgical adjustments to sibling files are capability improvements ON THOSE OWNERS — Compute provides/gains what serves energy, Persistence likewise — woven as the owner's own improvement, never energy-stamped edits. Applies to all three tracks.
   - Tracks 1-3 ALSO update the relevant root RASM-* docs for folders they touch IF AND ONLY IF needed, so later legs can integrate/leverage/improve on the landed capability — update the OPERATIVE doc per campaign (the DECISION for geometry/persistence/fabrication, the brief for compute/apphost/appui and the five py campaigns); enabling, never steering.
   - BRIEFS ARE NOT ARCHIVED mid-campaign (user question, answered with evidence): the DECISIONs anchor into their briefs — persistence and fabrication DECISIONs carry 9 brief references each, and the geometry DECISION cites `RASM-CS-FABRICATION-BRIEF [V5]/[V6]` cross-campaign — so a campaign's pair moves to `.archive/` only at campaign close, per the `RASM-REBUILD-DECISION.md`/`RASM-TS-PLATFORM-DECISION.md` precedent.
3. All three tracks complete (through redteam) → **ENERGY-PARITY WF** (replaces the 3-pass parity track, user-redesigned): launch `Workflow(.claude/workflows/energy-parity.js)` — 6 read-only opus deep-mappers (OWNERS + CROSS lanes per branch: findings census, unexploited-.api-capability inventory, wire-seam parity map, all adversarial) → ONE fable fixer (all three doctrine roots, whole-libs authority minus the untouchable sets) — no verify, no iterative tail. EPHEMERAL: delete the wf after landing. Gate: CS + PY redteams done (TS chain already closed). On completion: save-point commit + delete the ephemeral.
3b. **ARTIFACTS DESIGN RUNNING**: `design-artifacts-restructure.js` run `wf_ba533493-c38` (task `w495492j5`) — survey(5) → draft(4 lenses) → judge(3, five disqualifying gates) → synthesize(fable) → decide(fable, WRITES `RASM-PY-ARTIFACTS-DECISION.md`) → salvage → REFINE (3 sequential fable cold passes BAKED IN: initial/critique/redteam over the written DECISION, energy-data-flow + additive-admissions pressure encoded, surgical artifacts-page pre-adjustments only where a ruling justifies) — the formerly planned loose post-landing 1-2-3 sends are CANCELLED, the workflow owns them. Upstream briefs read as LANDED LAW; integration-first a disqualifying gate; self-contained legs, no spam legs (leg-1 1a/1b ruled explicitly). On completion: verify the return (rulings, legs, refine verdicts) → save-point commit. The ephemeral deletes after the artifacts CAMPAIGN lands (not after the DECISION).
4. Geometry legs 2/3/4 continue sequentially per DECISION `[10]` (current engine: self-contained, no residual step); leg 4 closes on the four acceptance dry-runs; legs re-run cold across sessions until a pass finds nothing.
5. User-gated: commits (only on request); compute/apphost/appui leg merges; any further sweep of residual removal language (`rg -i 'PRUNE|REMOVE|leaves the roster|removal default|strike'` over RASM-*.md — remaining hits are mostly sanctioned license-gate registers and proven-prune records; py-artifacts carries license-gate removal-defaults worth a second look under the boundary-shape preference).
6. After geometry finalizes: run PERSISTENCE campaign (its DECISION now 3 legs, integration-first).

## [05]-[OPERATING_RULES] (user-set, session law)

- COMMIT POLICY (user-set, standing): `git add -A` + commit after EVERY track step completion (each initial/critique/redteam of every track, parity included) and at each geometry leg close — clean save points; committing while sibling agents run is sanctioned (snapshots may carry a sibling's mid-edit state; the next save point completes it). Gate commit `7c0c9d30` already landed.
- Max ~8 concurrent background agents; roll replacements as completions arrive.
- NO find→report→redo loops: fix directly and sequentially; research only where irreducible and feed it straight into the implementing agent; opus for read-only triage/investigation, fable only for writing; 1 agent per doc for heavy semantic passes, 1 shared agent for mechanical multi-doc passes; main agent does surgical wording itself.
- Never introduce removal language; integrate/extend instead; leg merges combine scope, never drop it.
- Never edit a doc a live agent is editing; never touch geometry executable surfaces mid-run; the geometry brief/DECISION section numbering is frozen while the campaign runs.
