# [RASM_APPHOST_CENSUS_DOSSIER]

Read-only census of `libs/csharp/Rasm.AppHost/.planning/` (29 pages, 5 folders: Runtime 11, Agent 6, Wire 6, Sandbox 3, Observability 3) against `ARCHITECTURE.md` seam ledger, `libs/.planning/architecture.md` strata, and the three upstream briefs (Geometry, Persistence, Compute — read fully). Anchors are page-relative unless prefixed `ARCH:` (`Rasm.AppHost/ARCHITECTURE.md`), `PBRIEF:`/`CBRIEF:`/`GBRIEF:` (upstream briefs). Every `Fault` band + every cross-package reference swept.

Coverage: 28 pages deep-read; `Wire/livewire.md` (767 LOC) covered via 5 cross-references (federation SubscriptionLane/ExternalValue consumer, outbound LocalIpc, ARCH:65, IDEAS, WireFault 4720-4725 per federation.md:22) — its owner charter and band verified, interior not line-audited.

## [00]-[HEADLINE]

The 29 fence interiors are genuinely world-class — extreme ADT collapse (`[Union]`/`[SmartEnum<T>]`/`[ValueObject<T>]` + FrozenDictionary dispatch), honest `Fin`/`Validation`/`IO` rails, dense M.E.AI/Polly/OTel/Sigstore/wasmtime/MCP-SDK composition, and the seven-port seam discipline is real. The ROT is the PERIMETER, exactly the Persistence-brief pattern inverted: (1) a **fault-band space with TWO confirmed hard collisions and no type-enforced registry**, whose only collision-guard is a hand-curated, provably-incomplete prose paragraph; (2) a **duplicated `SupplyChainGate` owner shipping the exact deleted-form semver the ARCHITECTURE prohibition forecloses**; (3) **the four Persistence PORT seams shaped AppHost-side over AppHost types with a decorative in-memory fence** that contradicts the Persistence V2 token-VALIDATING ruling; (4) **duplicate membership owners** (`PeerRoster` vs `MembershipView`) with the fleet-roll seam declared against the wrong one; (5) **6+ wired-undeclared cross-package seams and 4 stale/mis-anchored ledger rows**; (6) a **stale IDEAS pool** asserting QUEUED work three landed pages already realize. No per-page cold pass fixes the perimeter. Architecture first.

## [01]-[PER_PAGE]

Grades: interior craft / perimeter-honesty composite, 1-10.

### Runtime/ (spine)

- **ports.md** (184 LOC) — **9.5**. The seven-port seam owner + `TenantContext`/`ReceiptEnvelope`/HLC. Interior award-grade. DEFECTS: `:19` references `Persistence/indexes#L2_CONTRIBUTION` and `Persistence/provisioning#TENANCY_RLS` — both STALE Persistence page names (owner is `Query/cache#L2_CONTRIBUTION` per PBRIEF E6/V12; Tenant-RLS is `Element/identity`). CHARTER: unchanged; correct the two Persistence anchors.
- **time.md** (268 LOC) — **7** (interior 9, but the FencingToken machinery is a load-bearing defect). `ClockPolicy`/`DeadlineClass`(9-row)/`SchedulePort`/`FencingToken`. DEFECT (CAMPAIGN-DEFINING): `LeaseElection.Acquire` (`:324-326`) mints the token from a per-process in-memory `Atom<FencingToken> Latest.Swap(Next())` (`:319,321`), and `LeaseElection.Fence` (`:328-331`) validates against that same in-memory atom — **zero cross-process safety**; two processes mint independent token sequences. This directly contradicts the page's own prose (`:304` "the store fences the token at Persistence/server-tier") and IS the decorative-fence defect PBRIEF V2 forecloses by homing token mint+validate in the PG row-CAS predicate. CHARTER: `FencingToken` becomes a Persistence-decoded carrier (or a shared lower-stratum type); `LeaseElection.Acquire/Fence` collapse to a thin PORT adapter that calls Persistence's coordination op-union and decodes `LeaseFenced` — the in-memory `Atom<FencingToken>` dies.
- **config.md** (456 LOC) — **9.5**. `ConfigSource`(8-row ranked)/`PolicyBinding`/`OptionsAdmission`(+RFC-6902 `PatchSection`)/`OperatorOverride`/`SecretLease`/`CredentialPem`. Owns the KMS-unwrap PORT (`:533`, ARCH:72) + `CredentialPemWire` (ARCH:55,61). Bands ConfigError 4100-4199, SecretFault 4780-4783, PemFault 4790-4793. Clean. CHARTER: unchanged; largest page but four disjoint owners (source axis / typed binding+options / secret lease / PEM) — a `config/secrets.md` split is defensible if it grows, not now.
- **lifecycle.md** (353 LOC) — **9.5**. `RuntimePhase`(8)/`PhaseTrigger`/`FaultSpine`(the `SupportTrigger.FaultTransition` source)/`DrainConductor`/`CancelScope`. Bands LifecycleFault 1200-1201. DEFECT: `:288,394` DRAIN_CONDUCTOR 2PC-in-doubt row consumes `Rasm.Persistence/Query/transaction#TWO_PHASE_COMMIT` — a DELETED Persistence page (PBRIEF V2 folded transaction into `graph#STORE_RAIL`; ARCH:71 stale). Migration pressure: whether the folded store rail still exposes `TwoPhase.InDoubt`/`pg_prepared_xacts` is unproven; if Persistence dropped 2PC the drain row is orphaned. CHARTER: unchanged; re-point the seam + confirm the capability survives Persistence-side.
- **resources.md** (221 LOC) — **9.5**. `CacheLane`(3-row, keyed L2)/`PoolPolicy`/`DrainSpec`+`DrainQueue`. The CACHE_PORT owner (ARCH:69) — L2 `IDistributedCache`+`IHybridCacheSerializerFactory` "single Persistence contribution" (`:19`). Clean. CHARTER: unchanged.
- **determinism.md** (348 LOC) — **9.5**. `DeterminismContext`/`EventLog`(hash-chained)/`ReplayVerify`/`MacroEngine`/`RecomputeGraph`/`AdversarialProbe`. Bands ReplayFault 4760-4763. DEFECT: `:118-126` `EventLog.ToOpLog` constructs a Persistence-owned `OpLogEntry` (references `SyncOpKind.Upsert`, `SnapshotCodec.JsonStj`, `ContentKey`, `Closure`, `OriginStoreId`) — a fairly deep coupling to Persistence `Version/ledger#CHANGEFEED`, WIRED-UNDECLARED (no ledger row). CHARTER: unchanged; declare the determinism→Persistence ledger seam.
- **orchestration.md** (296 LOC) — **8.5**. `WorkflowInstance`/`StepKind`(5-case)/`Orchestrator.Drive`/`StepStateSeam`/`CrashResume`/persistent-job. Bands OrchestrationFault 4770-4774. DEFECT (SEAM-DIRECTION): `StepStateSeam` (`:268`) is an AppHost record-of-delegates parameterized over AppHost types (`Func<WorkflowInstance, WorkflowStep, FencingToken, Fin<Unit>> Persist`) — pushes `WorkflowInstance`/`WorkflowStep`/`FencingToken` DOWN to the Persistence CAS store, contradicting PBRIEF V2 ("no AppHost interface or type crosses down; AppHost PORT adapters DECODE Persistence-owned types"). CHARTER: `StepStateSeam` becomes a PORT adapter serializing `WorkflowInstance`→wire-stable rows at the app root, calling Persistence's coordination op-union, decoding `LeaseFenced`.
- **features.md** (209 LOC) — **9.5** (interior) / band-collided. `FlagDefinition`/`TargetingRule`(5)/`Bucketing`(XxHash3 sticky)/`FlagVerdict`/`KillSwitchFold`. ONE_FEATURE_FLAG_PROJECTION owner (ARCH:81,82,83). DEFECT: **`FeatureFault` band 4700-4704 (`:154-158`) COLLIDES with `SolverFault` 4700-4703 (`solver.md:40-43`)** — both AppHost. CHARTER: unchanged; band re-partition via registry.
- **laneguard.md** (131 LOC) — **9.5**. `LaneGuard`(keyed Polly)/`AdaptiveConcurrency`/`LoadShed`/`ShedVerdict`. The ONE_DEGRADATION_SHED_VERDICT owner crossing to `Rasm.Compute/Runtime/admission` (ARCH:84). Bands LaneFault 4750-4753. Clean.
- **modules.md** (163 LOC) — **9.5**. `ModuleContribution`/`CompositionSurface`/`BoundaryActivation` — the one composition root. Scrutor `Describe`/`DescribeKeyed` discipline, `MakeReadOnly` freeze. No band (no faults). Clean.
- **profiles.md** (271 LOC) — **9.5**. `HostProfile`(8-row)/`RecoveryObjective`/`ProfileBoot`/`ProfileIdentity`/`PowerProbe`. The DR-objective PORT (ARCH:70) + resource identity. Bands ProfileFault 1100-1102. NOTE: `PowerProbe.ReadDarwin` (`:323-326`) is a stub returning hardcoded `(Plugged,Nominal,1d)`; the `[LibraryImport] CopyPowerSourcesInfo` (`:315`) is declared-never-called — honestly flagged tier-3 residual (`:333`), signature-locked, acceptable design state.

### Agent/ (capability surface)

- **capability.md** (487 LOC) — **9.5**. `CapabilityDescriptor`/`CapabilityRegistry`/`CommandAlgebra`/`GrantBroker`+`DistributedBudget`/`SdkCodegen`. Bands CommandFault 4600-4605, GrantFault 4620-4625. The `DistributedBudget` Budget-debit PORT (`:420-423`, ARCH:76) is CORRECTLY shaped — `Debit(TenantId, FencingToken, cost, ceiling)` delegates the fenced CAS to the store (matches PBRIEF V2's compare-and-decrement). DEFECT: the entire command-execution path composes Compute `ComputeIntent`/`IntentAdmission`/`SelectionReceipt`/`AdmittedIntent`/`SubstrateSelection.Dispatch` (`:3,237-238,300,564`) — WIRED-UNDECLARED (no `Agent/capability → Rasm.Compute` ledger row). NOTE: threads AppHost's `FencingToken` into the store (same ownership tension as time.md).
- **reasoning.md** (461 LOC) — **9.5**. `ReasoningSession`(IChatClient loop)/`SemanticDiscovery`(embedding intent)/`ReasoningTranscript`/`ModelGovernance`(the one M.E.AI ChatClientBuilder fold)/`ModalInput`. Reads `FlagVerdict` (ARCH:81). Confirms CBRIEF V15: `IEmbeddingGenerator` production lane is AppHost-governed (Compute embedding.md is retrieval-only) — consistent, no conflict. Clean interior.
- **identity.md** (266 LOC) — **9**. `IssuerTrust`(rotating JWKS)/`TokenValidation`→`Principal`/`CredentialFlow`(OpenIddict)/`PolicyGate`. The identity-store PORT (`:89,308`, ARCH:75). Bands IdentityFault 4640-4648. DEFECT: **band 4640-4648 COLLIDES with `McpFault` 4640-4644 (`mcp.md:125-129`)** — both AppHost. NOTE: IDEAS `RUNTIME_IDENTITY_AUTHZ_PLANE` is only PARTIALLY realized here — `Agent/policy.md` folded into `#POLICY_GATE` (good), but the `IdentityPrincipal` ambient slot + `TokenLease` (DPoP) lifecycle + `PolicyDescriptor [SmartEnum]` are NOT realized (POLICY_GATE uses raw `IAuthorizationRequirement`).
- **mcp.md** (309 LOC) — **9.5**. `McpMethod`/`ToolProjection`(descriptor→AIFunction)/`McpDispatch`/`StreamProgress`. The MCP server projection over the official SDK. Bands McpFault 4640-4644 (COLLIDES with IdentityFault — see above). `:21` itself documents the collision-avoidance-by-namespace argument, tacitly conceding the raw-integer ambiguity. Clean interior.
- **federation.md** (305 LOC) — **9.5** interior / SMOKING-GUN perimeter. `TransportKind`/`FederatedServer`/`FederationProjection`(peer→descriptor inversion)/`FederatedDispatch`/resource-subscription. Bands FederationFault 4800-4805. DEFECT (perimeter): `:22` hand-curates a partial AppHost band-awareness paragraph ("4640 McpFault, 4720-4725 WireFault, 4760 ReplayFault, 4780 SecretFault, 4790 PemFault; cross-pkg 4520-4532 Compute.Remote WireFault") — a manual stand-in for the missing registry that is provably INCOMPLETE (omits FeatureFault 4700, SolverFault 4700, CommandFault 4600, GrantFault 4620, SandboxFault 4660, both SupplyChainFaults, BusFault 4730, OutboxFault 4740, LaneFault 4750, OrchestrationFault 4770) and BLIND to IdentityFault ALSO occupying 4640. Clean interior.
- **runtime.md** (88 LOC) — **8.5**. `CommandDispatch.Run` front door — the declaration-only restructure resolving the spine's dangling dispatch reference (TASKLOG RESTRUCTURE_AGENT_COMMAND_DISPATCH_PAGE). Thin (smallest page) but load-bearing (orchestration + bus drive through it). Consumes Compute intent rail (WIRED-UNDECLARED, `:46`). CHARTER: acceptable as a declaration page; near the fold-into-capability boundary.

### Wire/ (outbound + external binding)

- **outbound.md** (466 LOC) — **9**. `OutboundHop`(7-case)/`HttpLane`/`KeyedLane`/`OutboundSurface`/`Discovery`/`DeliveryFanout`. Bands HopFault 4500-4506. DEFECTS: (a) `DeliveryFanout.Fan` (`:521-523`) does `Dedupe.Swap(_ => pruned.AddOrUpdate(...))` — the `_ =>` DISCARDS the atom's live value, so concurrent fans lose each other's dedupe records (not a read-modify-write). (b) `:153,161` consumes Compute `GrpcChannelPolicy` from `Rasm.Compute/Runtime/channels#TRANSPORT_AXIS` — WIRED-UNDECLARED AND re-points when Compute V13 splits channels.md→transport.md.
- **livewire.md** (767 LOC, biggest) — **9 (assumed)**. Reactive external-binding studio over industrial transport (OPC-UA/MQTT/Modbus/serial), `SubscriptionLane`/`ExternalValue`/`BindingStatusWire`/`CoercedValueWire`/`WriteReceiptWire` (ARCH:65). Bands WireFault 4720-4725. Not line-audited; owner charter + band verified via federation/outbound cross-refs. FLAG: needs a deep pass in the build campaign.
- **companion.md** (659 LOC) — **9.5**. `ProcessModality`(3)/`PeerRoster`/`ControlInbound`/`ServiceHost`/`DegradationCascade`(WRITE side)/`PeerAdmission`(getsockopt ucred/xucred)/`HostBinding`(9-row OS×activation×address). DEFECT: `:23` projects the ledger#PRESENCE seam (RosterReceipt→PresenceRow via `Presence.Beat`) to `Persistence/collaboration#PRESENCE_AND_BLOB` — a STALE Persistence page (owner is `Version/ledger#PRESENCE`, PBRIEF E6); WIRED-UNDECLARED AppHost-side (mandate wants BOTH sides). NOTE: consumes Compute `channels#PROTO_VOCABULARY` (`:23,168`) — re-points on Compute V13.
- **topics.md** (155 LOC) — **9.5**. `Topic`/`TopicHead`/`Subscription`/`EventBus`(BUS_CONDUCTOR). Bands BusFault 4730-4733. DEFECT: `SubscriptionFabric.Dedupe` (`:99-104`) repeats the outbound `Swap(_ => pruned...)` lost-update pattern — same concurrency flaw in a second place (systematic dedupe idiom).
- **outbox.md** (140 LOC) — **9**. `OutboxRow`/`DispatchStatus`/`OutboxRelay`(watermark sweep). The ONE_OUTBOX_EGRESS_SPINE half (ARCH:78). Bands OutboxFault 4740-4743. DEFECT: `:20,165` names the third OutboundHop consumer as `Rasm.Persistence/Sync/egress#EGRESS_SINK` — a DELETED Persistence page (PBRIEF V3 re-homes egress to `Version/egress.md`; ARCH:73 stale). Consumes `CdcEnvelopeWire` — verify naming vs Persistence V3's CloudEvents envelope.
- **coordination.md** (212 LOC) — **8.5**. `RoleResolution`(ServiceDiscovery)/`MembershipView`(probe liveness)/`RoleElection`/`DistributedLock`. The CAS+lease+membership PORT (ARCH:79). Bands CoordinationFault 4520-4525 (cross-pkg integer collision with Compute.Remote WireFault 4520, per federation.md:22). DEFECT: `:60` claims `FleetRoll` reads `MembershipView.Serving` but provisioning.md wires `PeerRoster.Attached` (see duplicate-membership finding). Inherits time.md's decorative-fence defect (all election/lock go through `LeaseElection`).

### Sandbox/ (plugin isolation)

- **isolation.md** (294 LOC) — **6** (interior 9, but ships a top-tier duplication+deleted-form). `SandboxIsolation`(wasm/process)/`GrantHandle`+`Mediate`(operator/agent/plugin)/`QuotaControl`/`SupplyChainGate`. Bands SandboxFault 4660-4664, SupplyChainFault 4680-4684. DEFECT (CAMPAIGN-DEFINING): `#SUPPLY_CHAIN` (`:246-321`) mints a SECOND `SupplyChainGate` (a record with `Admit(PluginArtifact)→Fin`, hand-rolled `Verify` delegate, `XxHash128` digest) + a SECOND `SupplyChainFault` union (4680) + `SemverGate.Compatible` (`:314-320`) that hand-splits a `"lower-upper"` string via `System.Version` — the EXACT deleted form ARCH:148 and `provisioning.md:207` forbid ("a `System.Version` semver check, a hand-split `lower-upper` range string ... is the deleted form"), and a DUPLICATE of provisioning.md's real Sigstore/`VersionRange.Satisfies` gate. `provisioning.md:207` explicitly says the plugin loader SHARES the one gate "never two verify paths." CHARTER: `isolation` consumes `Sandbox/provisioning#SUPPLY_CHAIN_GATE.Admit` (the real Sigstore gate) for plugin artifacts; the second gate + `SemverGate` + second SupplyChainFault DIE.
- **solver.md** (170 LOC) — **8.5**. `SolverKind`(7)/`EncodingKind`/`KindContract`/`SolverPluginContract`/`SolverHosting`. Plugin extensibility over Compute `GeometryEncoding`/`EncodedTensor`. DEFECT: `SolverFault` 4700-4703 (`:40-43`) COLLIDES with `FeatureFault` 4700 (see above). NOTE: `EncodingKind.Field`/`.Toolpath` (`:54-55`) carry "working spelling until the Compute encoding table extends" (`:197`) — a forward dependency on Compute's `GeometryEncoding` case extension.
- **provisioning.md** (332 LOC) — **9.5**. `UpdateRail`(Velopack)/`UpdateChannel`(3)/`SupplyChainGate`(real Sigstore+NuGet.Versioning)/`RolloverDrain`+`RollStrategy`(canary/blue-green/linear-wave)+`FleetRoll`. Bands UpdateFault 1300-1305, SupplyChainFault 1320-1325. Reads `FlagVerdict` (ARCH:82). DEFECT: `:269,273` FleetRoll walks `PeerRoster.Attached`/`RosterEntry` (companion's LOCAL attach set) as "wave membership" — but ARCH:80 + coordination.md:60 say it reads coordination's cluster `MembershipView.Serving` (see duplicate-membership finding). A fleet-wide roll should read cluster liveness, not the local attach set.

### Observability/

- **health.md** (470 LOC) — **9.5** (interior) / stale roster. `HealthContributorRow`/`DriverProbe`(6-row)/`PressurePolicy`/`DegradationCell`(collapsed `Atom<DegradationReading>`)/`WireHealth`/`AlertEngine`. DEFECT (mandate :85): `DriverProbe` (`:100-110`) hardcodes `Cache=("redis",...)` (raw `IConnectionMultiplexer` probe) and `Broker=("kafka",...)` (single broker) — but PBRIEF V7/V3 drops the raw Redis driver for the caching transit (`Microsoft.Extensions.Caching.StackExchangeRedis`) and makes brokers an integration-first egress sink roster (NATS anchor + optional Kafka/RabbitMQ/DotPulsar). The roster must re-write: cache probe through the caching transit, broker rows aligned to the Persistence egress sink roster. ARCH:85's `Npgsql/Redis/Kafka driver` roster carries the stale shape. NOTE: `DriverProbe` is a fixed 6-row `[SmartEnum]` where the broker roster should track the egress sink roster (naivety-on-COVERAGE + generator alignment).
- **telemetry.md** (337 LOC) — **9.5**. `TelemetrySource`/`TelemetryIdentity`/`Correlation`+`TraceContext`(W3C, ARCH:59)/`LogPipeline`/`SignalGovernance`(OtelExport ARCH:66)/`DataClassification`(9-row redaction). DEFECT (minor): `:3` intro says "seven-row `DataClassification` taxonomy" but the enum (`:368-378`) + `:350` have NINE rows — stale count. NOTE: ARCH:60 `Telemetry → ts:wire # BenchmarkClaimWire/HostFingerprintWire` is questionable — `HostFingerprint` is Compute/determinism-owned; telemetry doesn't obviously project these (possible ledger mis-attribution).
- **bundles.md** (287 LOC) — **9.5**. `SupportTrigger`(6-case)/`SupportCapture`/`SupportManifest`/`SupportReceipt`/`SupportLedger`. The support-capture consumer of `FaultSpine`'s `SupportTrigger.FaultTransition`. DEFECT (minor): `ProcessDump.Produce` (`:90`) fails with bare `Error.New(9300, "dump-admission-gated")` — a hardcoded 9300 code, NOT a typed fault case, and 9300 is outside every documented AppHost band (the "bare `Error.New` is the deleted form" law). Should be a typed `SupportFault` case in a registered band.

## [02]-[CROSS_CUTTING]

### [FAULT_BAND_SPACE] — no registry, two confirmed hard collisions

AppHost has ~23 fault unions across two disjoint band clusters (11xx-13xx boot/host; 41xx + 45xx-48xx runtime/agent/wire) with **NO type-enforced band registry**. Collision-avoidance is a hand-curated prose paragraph (`federation.md:22`) that is incomplete and wrong. Confirmed:
- **HARD collision #1**: `IdentityFault` 4640-4648 (`identity.md:119-127`) ≡ `McpFault` 4640-4644 (`mcp.md:125-129`) — both AppHost. `Expected.Code` 4640-4644 is ambiguous by construction.
- **HARD collision #2**: `FeatureFault` 4700-4704 (`features.md:154-158`) ≡ `SolverFault` 4700-4703 (`solver.md:40-43`) — both AppHost.
- **Simple-name collision**: `SupplyChainFault` ×2 — `provisioning.md:212` (1320) + `isolation.md:258` (4680) — two unions, same name (mirrors Persistence-brief E4 `GraphFault`×2).
- **Cross-pkg integer ambiguity**: `CoordinationFault` 4520 ≡ `Rasm.Compute.Remote.WireFault` 4520 (federation.md:22 concedes this, relies on namespace-qualification which does not help the raw telemetry code).
Full band map: ProfileFault 1100 · LifecycleFault 1200 · UpdateFault 1300 · SupplyChainFault#1 1320 · ConfigError 4100 · HopFault 4500 · CoordinationFault 4520 · CommandFault 4600 · GrantFault 4620 · IdentityFault 4640 · McpFault 4640 · SandboxFault 4660 · SupplyChainFault#2 4680 · FeatureFault 4700 · SolverFault 4700 · WireFault 4720 · BusFault 4730 · OutboxFault 4740 · LaneFault 4750 · ReplayFault 4760 · OrchestrationFault 4770 · SecretFault 4780 · PemFault 4790 · FederationFault 4800. FIX: mint an AppHost `[SmartEnum<int>]` `FaultBand` registry (mirroring `RASM-COMPONENT-PARADIGM-DECISION.md:141-149` + PBRIEF V4) where a duplicate integer fails at type initialization; every union's `Code` derives `Band+n`; re-partition the two collisions to unique decades; `federation.md:22`'s prose paragraph dies for a registry pointer.

### [DUPLICATE_MECHANISMS]
1. **`SupplyChainGate` ×2** — provisioning.md (real, Sigstore+`VersionRange.Satisfies`, 1320) vs isolation.md (`:246-321`, hand-rolled `Verify`+`System.Version` `SemverGate`, 4680). isolation ships the ARCH:148-forbidden deleted form AND violates provisioning.md:207's "never two verify paths." isolation.md's `SupplyChainGate`+`SemverGate`+`SupplyChainFault#2` die into a consume of provisioning's gate.
2. **Membership owners ×2** — `PeerRoster` (companion, local accepted-connection attach set, `RosterEntry`, keyed by kernel `PeerCredential.Pid`) vs `MembershipView` (coordination, cluster-wide probe liveness, `MemberRecord`, `NodeState`). FleetRoll's wave membership is declared inconsistently (ARCH:80 + coordination.md:60 say `MembershipView.Serving`; provisioning.md:269,273 wires `PeerRoster.Attached`). Undeclared relationship. FIX: rule the two-tier relationship (is `PeerRoster` the local contribution folded into `MembershipView`? are they orthogonal local-attach vs cluster-liveness?) and re-point FleetRoll to the cluster owner.
3. **Dedupe lost-update idiom ×2** — `DeliveryFanout.Fan` (outbound.md:521-523) + `SubscriptionFabric.Dedupe` (topics.md:99-104), both `Swap(_ => pruned.AddOrUpdate(...))` discarding the live cell value.

### [SEAM_DIRECTION_TENSION] — the four Persistence PORT seams
PBRIEF V2 homes the four PORT contracts on Persistence-owned types with token-VALIDATING fencing; "AppHost PORT adapters DECODE those types and never push an AppHost interface or type downward." Current AppHost state is INCONSISTENT:
- `Agent/capability#DistributedBudget` (`:420-423`) — CORRECT: delegates the fenced CAS to the store, token flows from the store's `Token` delegate.
- `Runtime/orchestration#StepStateSeam` (`:268`) — WRONG: `Func<WorkflowInstance, WorkflowStep, FencingToken, Fin<Unit>>` pushes AppHost types down.
- `Wire/coordination` (RoleElection/DistributedLock) + `Runtime/time#LeaseElection` — WRONG: the fence-check is an in-memory `Atom<FencingToken>` per process (decorative, no cross-process safety).
- `Wire/outbox` — the outbox spine correctly names Persistence as owner but points at the deleted `Sync/egress`.
Underlying: **`FencingToken` is minted in TWO places** — AppHost `Runtime/time#FENCING_TOKEN` (`[ValueObject<ulong>]` + `Admits` + `LeaseElection`) and PBRIEF V2 ("monotonic fencing tokens ... Persistence-owned types"). Since Persistence depends up on `Rasm`+`Rasm.Element` only (never AppHost), Persistence CANNOT reference AppHost's `FencingToken` — so either Persistence owns it (AppHost decodes) or it is a shared lower-stratum type. Cross-brief ownership conflict to resolve.

### [STALE_CROSS_PACKAGE_REFERENCES]
- `lifecycle.md:288,394` → `Rasm.Persistence/Query/transaction` (DELETED; ARCH:71).
- `outbox.md:20,165` → `Rasm.Persistence/Sync/egress` (DELETED; ARCH:73; PBRIEF V3 → `Version/egress`).
- `companion.md:23` → `Persistence/collaboration#PRESENCE_AND_BLOB` (stale; owner `Version/ledger#PRESENCE`).
- `ports.md:19` → `Persistence/indexes#L2_CONTRIBUTION` + `Persistence/provisioning#TENANCY_RLS` (stale page names).
- `health.md:100-110` + ARCH:85 → `Npgsql/Redis/Kafka` driver roster (stale; PBRIEF V7/V3 re-writes).
- outbound/companion/coordination → `Rasm.Compute/Runtime/channels#{TRANSPORT_AXIS,PROTO_VOCABULARY}` (re-points on Compute V13 channels.md split).

### [SEAM_LEDGER_DIFF] (ARCH:[02] vs real graph)
DECLARED-UNWIRED:
- ARCH:67 `Runtime ← Rasm/Geometry/Drawing # EncodedGeometry/PackOp` — NO AppHost fence consumes EncodedGeometry/PackOp; target folder "Runtime" wrong (GBRIEF V14 says pack→AppHost/**Sandbox**). Consumer unbuilt or mis-anchored.
- ARCH:80 `Wire/Coordination → Sandbox/Provisioning # MembershipView.Serving` — provisioning wires companion's `PeerRoster`, not coordination's `MembershipView` (aspirational, unrealized).
- ARCH:86 `Model/agent → Rasm.Compute/Model # M.E.AI middleware` — MALFORMED anchor ("Model/agent" is no AppHost page); the M.E.AI fold is `Agent/reasoning#MODEL_GOVERNANCE` which OWNS it (does not consume Compute/Model). Delete or re-anchor+re-direct.
- ARCH:60 `Telemetry → ts:wire # BenchmarkClaimWire/HostFingerprintWire` — HostFingerprint is Compute/determinism-owned; telemetry doesn't project it (likely mis-attributed).
WIRED-UNDECLARED:
- `Agent/capability → Rasm.Compute` (ComputeIntent/IntentAdmission/SelectionReceipt/AdmittedIntent/SubstrateSelection.Dispatch) — the whole command-execution path (capability.md:3,237-238,300,564; runtime.md:46).
- `Wire/companion → Sandbox/provisioning` (PeerRoster.Attached/RosterEntry as FleetRoll wave membership, provisioning.md:269,273).
- `Wire/companion → Rasm.Persistence/Version/ledger#PRESENCE` (RosterReceipt→PresenceRow, companion.md:23) — mandate wants BOTH sides.
- `Runtime/determinism → Rasm.Persistence/Version/ledger#CHANGEFEED` (EventLog.ToOpLog constructs OpLogEntry, determinism.md:118-126).
- `Wire/outbound(+companion,coordination) → Rasm.Compute/Runtime/channels` (GrpcChannelPolicy + PROTO_VOCABULARY).
PAGE CYCLES: none found. The internal graph is a DAG rooted at the settled-vocabulary base (ports/time/lifecycle/config) up through capability → agent surface → wire/sandbox/observability consumers. Near-miss: coordination↔provisioning is one-directional (coordination provides; provisioning consumes companion, not coordination) — no cycle, but the seam is mis-declared.

### [HARDCODING_VS_GENERATOR]
Corpus is clean on the generator axis (rosters are `[SmartEnum]`/`[Union]`+FrozenDictionary seed DATA) EXCEPT: (a) fault bands are per-page hardcoded integers with no registry (above); (b) `health.md` `DriverProbe` broker roster is a fixed enum that should track the Persistence egress sink roster; (c) `bundles.md:90` bare `Error.New(9300)`.

### [DEAD_CARRIERS / STUBS]
- `profiles.md` `PowerProbe.CopyPowerSourcesInfo` (`:315`) declared-never-called; `ReadDarwin` (`:323`) returns hardcoded defaults (honestly flagged tier-3 residual).
- `solver.md` `EncodingKind.Field`/`.Toolpath` (`:54-55`) "working spelling" pending Compute encoding-table extension.

### [UNMINED_CAPABILITY] (catalog anchors)
Corpus mines its ~70 folder `.api` catalogs to strong depth (M.E.AI ChatClientBuilder decorators, Polly Simmy chaos, OTel GenAI conventions, Sigstore verify, wasmtime fuel/linear-memory, MCP SDK task/sampling/elicitation, IdentityModel ConfigurationManager JWKS rotation, HybridCache keyed L2, ServiceDiscovery resolver). No zero-consumer admitted package found in the manifest — the roster is lean and consumed. The one gap is the health `DriverProbe` roster not yet tracking `AspNetCore.HealthChecks.Kafka` as an integration-first sink-aligned row set (health.md, `api-healthchecks-kafka.md`).

### [FOLDER_ARCHITECTURE] verdicts
Structure is SOUND — 5 folders, each ≥3 pages, each a genuine growth axis, no one-file folders, no flat sprawl. Runtime(11) is dense but coherent (spine/clocks/config/determinism/orchestration/laneguard/features). Agent(6) is the capability surface with `runtime.md` (88 LOC) the one thin page — a deliberate dispatch-consolidation declaration, defensibly separate. Wire(6), Sandbox(3), Observability(3) all clean. No split/merge/move pressure at the folder level; the only page-craft note is config.md (456 LOC, 4 owners) and livewire.md (767 LOC) as future split candidates if they grow. The DEFECT is not folder shape — it is the cross-page duplication (SupplyChainGate, membership) and the perimeter (bands, seams, cards).

### [GOVERNANCE_PERIMETER]
- IDEAS.md OPEN has 3 QUEUED cards whose target pages EXIST and TASKLOG counterparts are COMPLETE: `TARGETED_DELIVERY_EXPERIMENTATION` (→features.md, fully landed) + `CLUSTER_MEMBERSHIP_ELECTION` (→coordination.md, fully landed) should move to CLOSED; `RUNTIME_IDENTITY_AUTHZ_PLANE` (→identity.md) is PARTIALLY landed — re-point to identity.md + re-scope the unrealized `IdentityPrincipal` ambient slot / `TokenLease` / `PolicyDescriptor`. This is the inverse of Persistence-brief E1 phantom-realization: here cards are stale-OPEN for landed work.
- TASKLOG.md OPEN is `(none)` — consistent with realized state.

## [03]-[VERDICT_CANDIDATES]

Ranked, campaign-defining, evidence-anchored.

**V1 — Mint the AppHost `FaultBand` registry; resolve two confirmed hard collisions.** `IdentityFault`≡`McpFault` at 4640 (`identity.md:119`/`mcp.md:125`) and `FeatureFault`≡`SolverFault` at 4700 (`features.md:154`/`solver.md:40`) are ambiguous `Expected.Code` telemetry keys. `SupplyChainFault`×2 simple-name collision. The only guard is `federation.md:22`'s incomplete hand-curated prose. Mirror `RASM-COMPONENT-PARADIGM-DECISION.md:141-149` + PBRIEF V4: one `[SmartEnum<int>]` where a duplicate integer fails at type initialization; every union derives `Band+n`; the prose paragraph dies.

**V2 — Collapse the duplicated `SupplyChainGate`; delete the ARCH:148 deleted-form.** `isolation.md#SUPPLY_CHAIN` (`:246-321`) mints a second gate with a hand-rolled `Verify` + `System.Version` `SemverGate.Compatible` hand-splitting `"lower-upper"` (`:314-320`) — the exact form ARCH:148 and provisioning.md:207 forbid — plus a second `SupplyChainFault` (4680). `provisioning.md:207` already rules the plugin loader shares the ONE Sigstore/`VersionRange.Satisfies` gate. isolation consumes `provisioning#SUPPLY_CHAIN_GATE.Admit`; the duplicate dies.

**V3 — Re-shape the four Persistence PORT seams to token-VALIDATING decode-only; kill the in-memory fence.** `time.md:319-331` `LeaseElection.Acquire/Fence` mint+validate against a per-process `Atom<FencingToken>` — zero cross-process safety, contradicting the page's own `:304` prose and PBRIEF V2. `orchestration.md:268` `StepStateSeam` pushes `WorkflowInstance`/`WorkflowStep` down. Only `capability.md#DistributedBudget` is correct. Rule `FencingToken` ownership (Persistence-owned decoded carrier vs shared lower-stratum type — cross-brief conflict), collapse `LeaseElection` to a PORT adapter over Persistence's coordination op-union, and make orchestration/coordination decode Persistence-owned types.

**V4 — Reconcile the duplicate membership owners; fix the FleetRoll wave-membership seam.** `PeerRoster` (companion, local attach set, `RosterEntry`) vs `MembershipView` (coordination, cluster liveness, `MemberRecord`). ARCH:80 + coordination.md:60 declare FleetRoll reads `MembershipView.Serving`; provisioning.md:269,273 wires `PeerRoster.Attached`. A fleet-wide roll must read cluster liveness. Rule the two-tier relationship + re-point.

**V5 — Re-write the `:85` HEALTH_PROBE driver roster against the Persistence roster motion.** `health.md:100-110` `DriverProbe` hardcodes `Cache=redis` (raw `IConnectionMultiplexer`) + `Broker=kafka` (single). PBRIEF V7/V3: raw Redis leaves for the caching transit (`Microsoft.Extensions.Caching.StackExchangeRedis`); brokers become an integration-first egress sink roster (NATS anchor + optional Kafka/RabbitMQ/DotPulsar). The cache probe routes through the transit; broker rows align to the egress sink roster. ARCH:85's `Npgsql/Redis/Kafka` roster re-writes.

**V6 — Correct the seam ledger both directions (declared-unwired + wired-undeclared + stale).** Re-point 4 stale ledger rows (ARCH:71 transaction, :73 egress, :85 driver roster, :86 malformed `Model/agent`), add ARCH-missing rows for the 6 wired-undeclared seams (capability→Compute intent rail; companion→provisioning PeerRoster; companion→Persistence ledger#PRESENCE; determinism→Persistence ledger#CHANGEFEED; outbound/companion/coordination→Compute channels), fix ARCH:67 EncodedGeometry target (Runtime→Sandbox) + ARCH:80 (coordination→provisioning is aspirational), and correct the 4 stale Persistence page anchors in the fences (ports/lifecycle/outbox/companion).

**V7 — Land the wired-undeclared `Agent/capability → Rasm.Compute` intent-rail seam as first-class.** The entire command-execution path (CommandAlgebra→IntentAdmission→SubstrateSelection.Dispatch→SelectionReceipt) composes Compute's intent-and-selection contract (capability.md:3,237-238,300,564; runtime.md:46; mcp.md:336) with no ledger row — the single most load-bearing undeclared cross-package coupling in the corpus.

**V8 — Consume Compute's `AssessmentOutcome`/`JobState` lifecycle as settled workflow vocabulary; never re-mint a parallel job-state store.** CBRIEF V1 lands assessment `ResultBlob` on the Persistence artifact index + the Failed-lifecycle sweep (`Dispatchable`/`Transient`/`Stale`). AppHost's `orchestration.md` workflow + `laneguard.md` should consume those outcomes as settled vocabulary beside the Persistence coordination owner — verify no parallel job-state store is minted (currently clean, but record as a binding constraint as the Compute sweep lands).

**V9 — Close the governance perimeter: stale IDEAS cards + minor typed-rail breaches.** Move `TARGETED_DELIVERY_EXPERIMENTATION` + `CLUSTER_MEMBERSHIP_ELECTION` to CLOSED (pages landed); re-point + re-scope `RUNTIME_IDENTITY_AUTHZ_PLANE` (identity.md landed partially — no `IdentityPrincipal` ambient / `TokenLease` / `PolicyDescriptor`). Fix `bundles.md:90` bare `Error.New(9300)` → typed fault; `telemetry.md:3` seven-vs-nine `DataClassification` count.

**V10 — Fix the two lost-update dedupe folds.** `outbound.md:521-523` + `topics.md:99-104` both `Swap(_ => pruned.AddOrUpdate(...))` discard the atom's live value — concurrent fans lose dedupe records. Read-modify-write over the live cell.

## [04]-[WATERFALL_PRESSURES] (record for the brief-author; no upstream edits made — census lane)
- **Persistence**: (a) `FencingToken` ownership conflict (V3) — PBRIEF V2 must own the token type or a shared lower-stratum type must exist, since Persistence cannot reference AppHost. (b) lifecycle.md 2PC-in-doubt (V6) — confirm `graph#STORE_RAIL` (the folded transaction owner) still exposes `TwoPhase.InDoubt`/`pg_prepared_xacts`, else AppHost's drain row is orphaned. (c) egress envelope naming — reconcile AppHost `CdcEnvelopeWire` (outbox.md) vs PBRIEF V3 CloudEvents envelope.
- **Compute**: channels.md V13 split (TRANSPORT_AXIS→transport.md, PROTO_VOCABULARY→wire.md) — AppHost outbound/companion/coordination references re-point (Compute-side change, AppHost migration only). `EncodingKind.Field`/`.Toolpath` (solver.md) depend on the Compute `GeometryEncoding` case-table extension.
- **Geometry**: ARCH:67 EncodedGeometry/PackOp consumer is unbuilt AND mis-targeted (Runtime vs Sandbox per GBRIEF V14) — confirm the Drawing/pack→AppHost/Sandbox wire and build the consumer.
