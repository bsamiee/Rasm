# [REGION_MAP] — typescript

Region ledger for the TypeScript stack corpus: one owner per region, snippet entries before code, page entries at finalization, repairs route to the owner.

## [1]-[PAGE_REGIONS]

- shapes.md: domain shape ownership law — the five-axis signature read electing one owner and the collapse comparator returning `(family, identifier, R)` over a discontinuous facet lattice; declaration-chain collapse deleting `object → type → const → typeof → instantiation` in one statement and the bounded reach of each lever; `Schema.Class`/`TaggedClass`/`TaggedError` construction-admission, the field-modifier and default-seam algebra, `extend`/`transformOrFail` widening, refined-owner invariants, `Schema.suspend` recursion, and declaration merging; variant-owner selection by seam cost, the discriminant-coupling spellings, the two-detector dispatch totality, the projection algebra, the `Match`-residual carrier, and the `TaggedRequest` family; structural `Equal`/`Hash` and the dispatch gate, comparator monoids, the four-law fold-safety table, `Kind` channel arithmetic, the witness grid and strength lattice, keyed identity with the order self-propagation anchor, container grain, the projector fold, and the brand wall; the `as const` behavior-column vocabulary, the `keyof typeof` proof object, composite template-literal alphabets, and the admission/dispatch absence isomorphism; the `Model.Class` six-variant fan, marker-as-field-config, the `HasDefault` elision predicate, per-op requirement derivation, and the four orthogonal wire axes.

## [2]-[SNIPPET_REGIONS]

[SHAPES]:
- SH-01 OWNER_CHOOSER: the signature as a self-routed `Data.taggedEnum<{ Nominal; Reference; Structural }>` whose identity axis is the injected `_tag`, `$match` narrowing each arm by `Extract<R, Record<D, P>>`; the family map and precedence defaults ride one `as const satisfies Record<Signature['_tag'], …>` tier-table read by indexed access (`Tier[s._tag].family`, codomain `(typeof Tier)[Signature['_tag']]['family']`) — a bare `$match` arm widens the elected `'Literal'` to `string`, so the `as const` row is what pins the codomain — and the four precedence keys collapse to one `Order.mapInput(Order.tuple(…, Order.reverse(…)), $match(...))` lexicographic order over a `HashSet.isSubset(demanded, covered)` admissibility gate with `HashSet.difference` the hand-spell inventory — the doctrine routing itself by its own law, the four sibling `Order`s and the standalone `family` matcher deleted.
- SH-02 STRUCT_CLASS_OWNER: a `Schema.Class<Self>` born from `Schema.Struct({...}).pipe(Schema.filter(crossFieldPred))` so the cross-field invariant is a definition-time aspect gating `new`/`make`/`decodeUnknown` alike, the parent carrying `static admit`/`static order`, widened by `.extend<Sub>(id)({...})` whose subclass `widest` consumes the parent's `Bounded.order` (an `Order<Bounded>` sorting `Refined` rows by contravariance) — codec/`Equal`/`Hash` thread through the `Inherited` slot while the author static is reused under the parent name (author statics do not propagate onto the subclass type); deletes `class X` + a free `assertValid` + call-site discipline + a parallel subclass + a re-declared subclass comparator.
- SH-03 REQUEST_FAMILY: a `Schema.Union` of `Schema.TaggedRequest<Self>()(tag, { payload, success, failure })` arms dispatched by the point-free `Match.type<typeof Demand.Type>().pipe(Match.tagsExhaustive({...}))` finalizer through `Request.completeEffect(arm, effect)` checking discriminant coverage and per-arm `Exit<SA, FA>` at once; deletes a hand `Schema.Exit({...})` beside a separate `Effect<A, E>` + the per-call `Match.value` wrapper lambda. Distinct from SH-01 (Match over a runnable request family's outcome, not the owner chooser).
- SH-04 KEYED_IDENTITY_FAN: one `Schema.Class` owner fanning `Schema.equivalence` (auto) + `Struct.getOrder` hand-anchored over the literal `keyof` field-record under `satisfies` + `Hash.structureKeys`, the absent `OrderAnnotationId` making the anchor the one site a new field breaks loudly; deletes a parallel `Map` + an `eq`-by-id + an inline comparator drifting beside the owner.
- SH-05 COMPOSITE_VOCABULARY: `Schema.TemplateLiteral(factorA, ".", factorB)` seeding `as const satisfies Record<typeof Code.Type, { readonly flag; readonly rank }>` with the row contract inlined at the `satisfies` (no standalone `type Row`) and per-factor `Match.value(slot)` dispatch over the `TemplateLiteralParser` tuple collapsing `n1*n2` rows to `n1+n2` arms; deletes a hand composite-string allowlist + a product behavior table + a single-consumer `Row` type. Distinct from SH-01/SH-03 (Match over a decomposed-tuple factor slot, not an owner `_tag`).
- SH-06 MODEL_FAN: one `Model.Class<Self>` fanning six variant codecs with `Model.Sensitive(Redacted(inner))` + `JsonFromString(...).pipe(fieldFromKey({...}))` + `Overrideable` stacking the four orthogonal wire axes on single columns; deletes parallel insert/select/json DTOs + hand row mappers.
- SH-07 DECLARATION_MERGING: one `class Span extends Schema.Class<Span>(id)({...})` carrying `static readonly equivalence = Schema.equivalence(Span)`, `static readonly order = Order.mapInput(...)`, and a `static recent(...)` projection — the value-side merge under `erasableSyntaxOnly` where a value-bearing `namespace Span` is rejected by `TS1294`, so derived comparator, equivalence, and projector fuse as STATIC CLASS MEMBERS dotted `Span.order`/`Span.equivalence`/`Span.recent`, the self-referential static legal because its initializer runs after the class binding (TDZ-throwing inside a `namespace` above the class) and the `Schema.Class` static wall (`make`/`fields`/`extend`/`annotations`/`ast` reserved, `TS2417` on collision) bounding which member names a static may claim; deletes a companion `const SpanStatics`, a mirror `interface SpanShape`, a `type Span = typeof Span` re-export, and every loose floating top-level `const` beside the owner. Distinct from SH-04 (the static-member merge mechanism collapsing loose statics onto the owner name, not the keyed-identity fan's comparator content).
- SH-08 VARIANT_TOTALITY: a `Schema.Union` of `Schema.TaggedClass<Self>()` arms each discriminated by the constructor-elided `Schema.tag(literal)` (`HasDefault`-defaulted, decode-validated, encode-emitted at once), dispatched by the point-free `Match.type<typeof Phase.Type>().pipe(Match.withReturnType<Ret>(), Match.tagsExhaustive({...}))` finalizer whose record-key two-sided proof rejects a dropped member as `never` where `Match.tag(removed)` one-sided would still type-check, the four widening vectors visible as the next `Schema.Union(...e, NewArm)` re-opening both detectors; deletes a discriminated `interface` union + a decode switch + a stale-arm-blind `Match.tag` ladder. Distinct from SH-03 (plain tagged-class variant family + dual totality detectors, not a runnable `TaggedRequest` correlating per-arm `Exit`).
- SH-09 ALGEBRA_STRENGTH: one value owner deriving its `Semigroup`/`Monoid` family from one declaration via `Semigroup.struct({...})` keeping each field's channels distinct (reject `combineAll` pinning one `R,O,E,A`), promoted to identity through the one-way `Monoid.fromSemigroup(S, empty)` trust gate proving associativity once at the leaf, with a non-commutative `Semigroup.last`/`String.Monoid` column flagged as the reorder-poison and `Semigroup.intercalate` named as the no-`Monoid` leaf; deletes N scattered hand `combine` closures + a per-fold `empty` re-check + a re-proven associativity site. Distinct from SH-04 (the additive/idempotent strength lattice and unit-promotion trust gate, not `Order`/`Equivalence`/`Hash` keyed identity).

## [3]-[KNOWN_OVERLAPS]

The `Match` family appears in SH-01, SH-03, SH-05, and SH-08 at four structurally distinct surfaces — discriminated-chooser dispatch with a carried residual (SH-01), tag-exhaustive dispatch correlating per-arm outcomes (SH-03), value-match over a decomposed tuple's factor slot (SH-05), and the two-sided record-key totality detector over a plain tagged-class union (SH-08) — admitted variety within the doctrine, not duplication; the snippet stage holds each to its recorded surface.
The `Schema.Class`/`Struct.getOrder`/`Schema.equivalence` triad appears in SH-04 (`Entry`) and SH-07 (`Span`) at two distinct surfaces — SH-04 owns the keyed-identity comparator content (the `keyof`-anchored `Order` table, `Hash.structureKeys`, the absent `OrderAnnotationId` loud-break), SH-07 owns the declaration-merging mechanism that relocates those derivations from loose top-level `const`s onto the owner's static members under `erasableSyntaxOnly` (no value-bearing `namespace`); the comparator is SH-04's spotlight and SH-07's supporting material, the merge is SH-07's spotlight and absent from SH-04.
