export const meta = {
  name: 'survey-gaps-geometry-deep',
  description: 'ADDITIVE, broadened second-pass capability-gap survey-AND-APPLY for the Rasm geometry KERNEL (the greenfield Geometry sub-domain under libs/csharp/Rasm), weighted hard toward computational-geometry kernels, meshing and discrete differential geometry, NURBS/BREP curve-surface, spatial acceleration structures, point-cloud/registration/reconstruction, and exact/robust arithmetic depth so the kernel stops hand-rolling concerns a real ecosystem package owns. The first pass admitted only the exact-precision tier (BigRational, DoubleDouble); this deeper EIGHT-facet pass digs the geometry long tail. SPECIAL LAYOUT: the geometry design pages live under libs/csharp/Rasm/Geometry/.planning while the README/ARCHITECTURE/.api and the csproj live at the libs/csharp/Rasm ROOT; the mature siblings Analysis/Domain/Vectors are NEVER touched. One opus xhigh survey+synthesize agent emits the gaps and eight facets, eight opus xhigh research agents each hunt one facet and self-validate the six-condition gate, one opus xhigh execute agent consolidates and ADDS to the existing Rasm admissions (read from the manifest/README/csproj, never duplicated) then restores and verifies at newest on osx-arm64, and one sonnet agent writes a one-line placeholder .api catalog per NEWLY admitted package. The full .api authoring and design integration are deferred to a focused rebuild-api run and to plan-cs-folders.',
  whenToUse: 'Re-run the Rasm geometry kernel with deeper coverage of computational geometry, meshing/DEC, NURBS/BREP, spatial structures, point clouds, and robust arithmetic, additive over the existing admissions.',
  phases: [
    { title: 'Survey', detail: 'one opus xhigh agent: domain, admitted packages, hand-rolls, gaps, AND eight computational-geometry/meshing/NURBS/spatial/point-cloud research facets', model: 'opus' },
    { title: 'Research', detail: 'eight opus xhigh agents, one facet each: best-in-class modern candidate discovery with a self-validated six-condition gate', model: 'opus' },
    { title: 'Execute', detail: 'one opus xhigh agent: consolidate the eight findings, ADD to the existing admissions, update csproj + README, restore and verify the install at newest on osx-arm64, self-heal or revert', model: 'opus' },
    { title: 'Stub', detail: 'one sonnet agent: write a one-line placeholder .api catalog file per NEWLY admitted package, nothing else', model: 'sonnet' },
  ],
}

// --- [FOLDER] -- the single hardcoded folder this workflow owns (Geometry special layout) -----
const F = { name: 'Geometry', primary: false, doc: 'libs/csharp/Rasm', api: 'libs/csharp/Rasm/.api', planning: 'libs/csharp/Rasm/Geometry/.planning', csproj: 'libs/csharp/Rasm/Rasm.csproj', note: 'SPECIAL LAYOUT: README/TASKLOG/IDEAS/.api live at the libs/csharp/Rasm ROOT; the geometry design pages live under libs/csharp/Rasm/Geometry/.planning (the mature siblings Analysis/Domain/Vectors also live under libs/csharp/Rasm and are NOT touched). Read AND write the .api + README at the Rasm root and the csproj at libs/csharp/Rasm/Rasm.csproj.' }

// --- [CONSTANTS] -------------------------------------------------------------------------
const STALL = 420000
const EXEC_STALL = 900000
const FACET_COUNT = 8
const MANIFEST = 'Directory.Packages.props (central NuGet pins) + Directory.Build.props (TargetFramework net10.0, RuntimeIdentifiers osx-arm64)'
const ADDITIVE = 'This is an ADDITIVE second pass over a kernel that already received a prior survey admission (notably ExtendedNumerics.BigRational, TYoshimura.DoubleDouble, plus the mature incumbents MathNet.Numerics, CSparse, geometry3Sharp, RhinoCommon, and the hand-authored Vectors/Analysis/Domain owners). READ the current admissions (its Directory.Packages.props rows, its README roster, its csproj PackageReference list) and treat every already-admitted package as ALREADY-PRESENT for the no-dup gate — KEEP them all untouched, never duplicate or remove them, and admit only NEW packages this deeper pass surfaces.'

// --- [SCHEMAS] ---------------------------------------------------------------------------
const SURVEY_SCHEMA = { type: 'object', additionalProperties: false, required: ['folder', 'gaps', 'facets'], properties: { folder: { type: 'string' }, domain: { type: 'string' }, packages: { type: 'array', items: { type: 'string' } }, handRolls: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['capability'], properties: { capability: { type: 'string' }, evidence: { type: 'string' } } } }, gaps: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['capability'], properties: { capability: { type: 'string' }, severity: { type: 'string' }, note: { type: 'string' } } } }, facets: { type: 'array', minItems: FACET_COUNT, maxItems: FACET_COUNT, items: { type: 'object', additionalProperties: false, required: ['id', 'direction', 'gap'], properties: { id: { type: 'string' }, direction: { type: 'string' }, gap: { type: 'string' }, mandate: { type: 'string' } } } } } }
const RESEARCH_SCHEMA = { type: 'object', additionalProperties: false, required: ['facet', 'candidates'], properties: { facet: { type: 'string' }, candidates: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['package', 'fills', 'ok'], properties: { package: { type: 'string' }, fills: { type: 'string' }, version: { type: 'string' }, license: { type: 'string' }, bestOf: { type: 'boolean' }, macOk: { type: 'boolean' }, newest: { type: 'boolean' }, licenseOk: { type: 'boolean' }, noLegacy: { type: 'boolean' }, notDup: { type: 'boolean' }, dupOf: { type: 'string' }, ok: { type: 'boolean' }, alternativesConsidered: { type: 'string' }, evidence: { type: 'string' } } } } } }
const EXEC_SCHEMA = { type: 'object', additionalProperties: false, required: ['folder', 'applied'], properties: { folder: { type: 'string' }, applied: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['package', 'apiPath'], properties: { package: { type: 'string' }, version: { type: 'string' }, license: { type: 'string' }, apiPath: { type: 'string' } } } }, skipped: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['package', 'why'], properties: { package: { type: 'string' }, why: { type: 'string' } } } }, files: { type: 'array', items: { type: 'string' } }, green: { type: 'boolean' }, summary: { type: 'string' } } }
const STUB_SCHEMA = { type: 'object', additionalProperties: false, required: ['folder', 'stubs'], properties: { folder: { type: 'string' }, stubs: { type: 'array', items: { type: 'string' } }, summary: { type: 'string' } } }

// --- [LAW] -------------------------------------------------------------------------------
const LAW = [
  'Rasm monorepo, PER-FOLDER capability-gap survey-AND-APPLY of the C# AEC stack (KERNEL -> AEC-DOMAIN -> APP-PLATFORM -> HOST-BOUNDARY -> APP strata, depend strictly upward). This run is the DEEP, ADDITIVE re-pass of the Rasm geometry KERNEL — the RhinoCommon-aware geometry/numeric base every higher stratum consumes (sub-domains Vectors, Analysis, Domain, and the greenfield Geometry). ' + F.note + ' The mission is NOT a per-package keep/replace audit — it is to find AND admit the world-class MODERN packages the kernel is still MISSING in computational geometry / meshing / NURBS / spatial / point-cloud / robust-arithmetic, and to STOP hand-rolling concerns a real package owns. Central pins live in ' + MANIFEST + '. ' + ADDITIVE,
  'BAR: the geometry kernel must be world-class, bleeding-edge, production-ready RIGHT NOW. Treat the first pass as having admitted only the exact-precision tier: assume valuable modern computational-geometry/meshing/NURBS/spatial/point-cloud packages are still missing and hunt them aggressively. Where the kernel HAND-ROLLS a concern a real ecosystem package owns — a Delaunay/constrained triangulation, a remesh, a mesh boolean, a convex hull, an offset, a Voronoi/medial-axis, a BVH/kd-tree, an ICP registration, a NURBS evaluation — that is a NAMED GAP. Geometry/mesh meet at the wire with ONE owner per runtime, and host-neutral capability is in scope where a non-Rhino consumer needs the contract. A large additive set is the GOAL here IF each entry is best-in-class, production-grade, and non-overlapping.',
  'AGGRESSIVE ADDITION, ZERO HEDGING: admit a package whenever it provides unique, modern, production-grade capability appropriate to the geometry-kernel domain. DECLINE ONLY for a real reason — (a) old / unmaintained / low-quality / naive, (b) a strictly stronger alternative is already admitted or recommended (MathNet/CSparse numerics, geometry3Sharp, RhinoCommon, BigRational/DoubleDouble exact tier), or (c) out of the kernel scope. NEVER decline for no current consumer.',
  'VALIDATION GATE — every candidate must pass all six, and the RESEARCH agent self-critiques it BEFORE returning (ok=true only when all six hold; default ok=false on any unproven condition): (1) BEST-OF — the strongest, most COMPLETE package for the gap, not a naive wrapper, and genuinely stronger than the mature incumbent it would sit beside; compare the real alternatives. (2) MAC — works on osx-arm64 (managed AnyCPU, an osx-arm64 native asset, or a Forge-provisioned substrate; reject win/x64-only). (3) NEWEST — current stable release named, actively maintained. (4) LICENSE — OSS (any OSI license) OR free-full commercial (no paid tier/seat cap/usage gate/eval-only); reject fee/tiered AND viral copyleft (GPL/AGPL) for a host-neutral library. (5) NO LEGACY PACKAGING — modern packaging/TFM/abi. (6) NO DUP/OVERLAP — not a duplicate of an already-admitted package (read the current admissions) or a sibling candidate; set dupOf and keep the single best. A package not published on NuGet cannot become a central pin — reject it as a pin (note it only as a capability).',
  'MANDATED DEEP AREAS for the geometry kernel (the survey MUST allocate the eight facets across these, and the research MUST sweep each EXHAUSTIVELY): (A) COMPUTATIONAL-GEOMETRY KERNELS — convex hull, polygon/mesh boolean, offset, Minkowski, Voronoi/Delaunay, medial-axis/straight-skeleton, arrangement. (B) MESHING — (constrained) Delaunay triangulation, tet/quad meshing, remeshing, mesh repair/healing, simplification/decimation, isosurfacing/marching-cubes. (C) DISCRETE DIFFERENTIAL GEOMETRY — curvature, geodesics, parameterization, vector-heat/Laplacian operators. (D) NURBS / BREP / CURVE-SURFACE — host-neutral NURBS evaluation, curve/surface fitting, intersection, tessellation. (E) SPATIAL ACCELERATION — BVH, octree, kd-tree, R-tree, AABB-tree spatial indices. (F) POINT CLOUD — registration (ICP/global), reconstruction (Poisson/ball-pivot), normal estimation, downsampling, segmentation. (G) ROBUST / EXACT ARITHMETIC depth beyond the admitted BigRational/DoubleDouble (robust predicates, interval arithmetic, filtered kernels). (H) any remaining geometry concern the kernel HAND-ROLLS that a package owns. For a server-side or non-NuGet capability, note it but only admit the managed .NET package that drives it.',
  'ASSAY + TFM TRUTH: verify a candidate in the cache via `uv run --frozen python -m tools.assay api`; for a multi-target NuGet package the consumer floor is net10.0 — confirm the lib/<tfm> a net10 consumer actually binds. Confirm versions/license/Mac/packaging against the registry (NuGet flat-container index + nuspec) truthfully; web research for the domain landscape, newest stable version, and maintenance signals.',
  'WRITE DISCIPLINE — strict phase ownership: SURVEY and RESEARCH are READ-ONLY and write NOTHING. ONLY EXECUTE admits packages, and it writes EXACTLY four things, ALL AT THE Rasm ROOT for this special-layout kernel: (a) the central pin in Directory.Packages.props (matching ItemGroup, newest stable version, plus any pure-managed transitive floor pins), (b) the PackageReference in libs/csharp/Rasm/Rasm.csproj, (c) the libs/csharp/Rasm/README.md roster + prose, and (d) the restore/verify of the Rasm project. This is ADDITIVE — KEEP every existing admission, add only NEW packages, report ONLY the newly-admitted packages in applied, and NEVER touch the mature siblings Analysis/Domain/Vectors. EXECUTE does NOT author .api catalogs and does NOT touch any design page (deferred to a focused rebuild-api run and to plan-cs-folders). The STUB stage writes ONLY a one-line placeholder .api file per NEWLY admitted package at the Rasm-root .api.',
].join('\n')

// --- [PROMPTS] ---------------------------------------------------------------------------
const surveyPrompt = (f) => [
  LAW, '',
  'TASK (SURVEY + SYNTHESIZE for the geometry KERNEL — go deepest — READ-ONLY, write nothing): build the full capability picture AND emit exactly ' + FACET_COUNT + ' research facets. Read the README + project file (' + f.csproj + '), every catalog under ' + f.api + '/, the design pages under ' + f.planning + '/ (the greenfield Geometry sub-domains Drawing/Meshing/Numerics/Processing/Spatial), and the central manifest ' + MANIFEST + ' for the Rasm rows. ' + f.note + ' The kernel already admits the exact-precision tier and the mature numerics/geometry incumbents — account for them so the facets target what is still MISSING or still HAND-ROLLED in computational geometry / meshing / NURBS / spatial / point-cloud.',
  'Return: (1) a 1-2 sentence DOMAIN summary; (2) the admitted PACKAGES; (3) HAND-ROLLS — triangulations, mesh booleans, hulls, offsets, Voronoi/medial-axis, spatial indices, ICP registrations, NURBS evaluations implemented by hand that a real package owns (each with an evidence pointer) — the PRIMARY signal for this pass; (4) GAPS — capabilities a world-class geometry kernel must have but the kernel lacks; (5) FACETS — EXACTLY ' + FACET_COUNT + ' non-overlapping research directions covering the MANDATED DEEP AREAS (A-H): allocate facets so computational-geometry kernels, meshing, discrete differential geometry, NURBS/BREP, spatial acceleration, point-cloud, and robust arithmetic each get coverage. Each facet: id, direction, the gap it closes, an optional mandate note. Be skeptical and ambitious, but respect that the numerics/geometry incumbents are strong — only surface genuinely stronger or genuinely new capability. Write nothing.',
].join('\n')

const researchPrompt = (f, survey, facet) => [
  LAW, '',
  'TASK (RESEARCH one facet for the geometry KERNEL — READ-ONLY, write nothing, then SELF-VALIDATE): facet=' + facet.id + ' · direction=' + facet.direction + ' · gap=' + facet.gap + (facet.mandate ? ' · mandate=' + facet.mandate : '') + '. Find the best-in-class, PRODUCTION-GRADE, FULL-FEATURED MODERN package(s) that fill this gap for a net10/osx-arm64 C# AEC geometry kernel and let the kernel STOP hand-rolling the concern. Compare the real alternatives (do not stop at the first hit) and name the strongest, most complete option — NEVER a naive single-feature wrapper, and only when genuinely stronger than the mature incumbent it would sit beside. Use web research for the landscape, newest stable version, maintenance, license, and Mac/osx-arm64 fit; confirm versions/license from the registry and members via `uv run --frozen python -m tools.assay api` when resolvable.',
  'Then SELF-VALIDATE each candidate against the six-condition gate and set ok=true ONLY when ALL pass: bestOf (strongest, most complete, beats the incumbent), macOk (osx-arm64), newest (current stable + active maintenance), licenseOk (OSS or free-full commercial, NOT GPL/AGPL viral for a host-neutral lib), noLegacy (modern packaging/TFM), notDup (not a duplicate of an admitted package — read the current admissions — or a sibling candidate; set dupOf, keep the single best). A package not on NuGet fails the gate as a pin. Default ok=false on any unproven condition. Exclude anything already admitted unless a strictly stronger replacement (set fills to name what it replaces). Return candidate(s) each with the gate fields, what gap it fills, newest version, license, the alternatives compared, and the evidence. Write nothing.',
].join('\n')

const executePrompt = (f, survey, research) => [
  LAW, '',
  'TASK (EXECUTE for the geometry KERNEL — ADDITIVE, WRITE the four owned artifacts only, ALL at the Rasm root): the ' + FACET_COUNT + ' research+self-validation outputs are INLINED at the END of this prompt — consolidate from THAT payload, never run your own package research and never invent candidates beyond it. FIRST consolidate: keep only candidates with ok=true, resolve every remaining dup/overlap across the facets to the single best, drop anything already admitted (read the current admissions) and anything out of the kernel scope — this is the per-folder plan, done here so nothing is dropped. THEN apply each surviving NEW package NOW. ' + f.note + ' The owning targets are: central manifest ' + MANIFEST + ', csproj ' + f.csproj + ', README at libs/csharp/Rasm/README.md, .api root ' + f.api + '.',
  'For EACH surviving NEW package: (a) add the central pin to Directory.Packages.props in the matching ItemGroup at the newest stable version, with a one-line comment, plus any pure-managed transitive floor pins; (b) add the PackageReference to ' + f.csproj + '; (c) update the libs/csharp/Rasm/README.md roster + prose. ADDITIVE — KEEP every existing admission untouched, never remove or duplicate, and NEVER touch the mature siblings Analysis/Domain/Vectors. Do NOT author any .api catalog and do NOT touch any design page (deferred).',
  'Then RESTORE and VERIFY the Rasm project so every new pin resolves on osx-arm64 at the newest stable version and the project restores clean — run the gate via `uv run --frozen python -m tools.assay static --project ' + f.csproj + '` (or restore) and parse the JSON Envelope. SELF-HEAL in place on a red gate. If a package proves non-resolvable or RID-incompatible on osx-arm64, REVERT its admission entirely and record it under skipped with the reason. For EACH NEWLY applied package report a canonical apiPath = ' + f.api + ' + the catalog filename matching the sibling .api naming convention. Report ONLY newly-admitted packages in applied. Return the fix-log: folder, applied [{package, version, license, apiPath}], skipped [{package, why}], files edited, green, and a one-line summary.',
  'THE ' + FACET_COUNT + ' RESEARCH + SELF-VALIDATION OUTPUTS for the geometry kernel (consolidate ONLY the ok=true candidates present in this payload; never fabricate a package, version, or member beyond it; if no ok=true candidate is present, admit nothing and report that under summary):\n' + JSON.stringify(research, null, 1),
].join('\n')

const stubPrompt = (f, ex) => [
  LAW, '',
  'TASK (STUB the .api catalogs for the geometry kernel — WRITE one-line placeholders ONLY, at the Rasm-root .api): the execute stage NEWLY admitted these packages with their canonical catalog paths: ' + JSON.stringify((ex && ex.applied) || []) + '. For EACH applied package, create the file at its apiPath (under the Rasm-root .api ' + f.api + '; ' + f.note + ') containing EXACTLY ONE line: a placeholder marker that names the package and marks the catalog research-pending. NOTHING ELSE — no header block, no member sections, no real members, no prose, no second line. If a directory in the path is missing, create it. Return the list of stub file paths you created.',
].join('\n')

// --- [COMPOSITION] -- single folder: Survey(1) -> Research(8) -> Execute(1) -> Stub(1) ----
log('=== survey-gaps single-folder (DEEP/ADDITIVE, geometry kernel): ' + F.name + ' ===')

phase('Survey')
const survey = await agent(surveyPrompt(F), { label: 'survey:' + F.name, phase: 'Survey', schema: SURVEY_SCHEMA, model: 'opus', effort: 'xhigh', stallMs: STALL })
if (!survey || !(survey.facets && survey.facets.length)) return { folder: F.name, applied: 0, note: 'no survey/facets returned' }
log(F.name + ' survey: ' + survey.facets.length + ' research facets, ' + ((survey.gaps && survey.gaps.length) || 0) + ' gaps')

phase('Research')
const research = (await parallel(survey.facets.map((fc) => () =>
  agent(researchPrompt(F, survey, fc), { label: 'research:' + F.name + ':' + fc.id, phase: 'Research', schema: RESEARCH_SCHEMA, model: 'opus', effort: 'xhigh', stallMs: STALL })
))).filter(Boolean)
const okCount = research.reduce((n, r) => n + ((r.candidates || []).filter((c) => c.ok).length), 0)
log(F.name + ' research: ' + research.length + '/' + survey.facets.length + ' facets returned, ' + okCount + ' gated candidates')

phase('Execute')
const ex = await agent(executePrompt(F, survey, research), { label: 'exec:' + F.name, phase: 'Execute', schema: EXEC_SCHEMA, model: 'opus', effort: 'xhigh', stallMs: EXEC_STALL })
const applied = (ex && ex.applied) || []
log(F.name + ' execute: ' + applied.length + ' newly applied, ' + (((ex && ex.skipped) || []).length) + ' skipped, green=' + (ex && ex.green))

let stub = null
if (applied.length) {
  phase('Stub')
  stub = await agent(stubPrompt(F, ex), { label: 'stub:' + F.name, phase: 'Stub', schema: STUB_SCHEMA, model: 'sonnet', effort: 'low', stallMs: STALL })
  log(F.name + ' stub: ' + ((stub && stub.stubs && stub.stubs.length) || 0) + ' .api stubs created')
}

const apiStubs = (stub && stub.stubs) || []
log('survey-gaps ' + F.name + ' (deep/additive) complete: ' + applied.length + ' packages newly admitted, ' + apiStubs.length + ' .api stubs for the focused rebuild-api run')
return { folder: F.name, applied: applied.length, green: !!(ex && ex.green), packages: applied.map((a) => a.package), apiStubs: apiStubs, skipped: (ex && ex.skipped) || [], summary: (ex && ex.summary) || '' }
