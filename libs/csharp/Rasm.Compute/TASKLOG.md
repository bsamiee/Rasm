# [COMPUTE_TASKLOG]

The open and closed work for measured execution, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup and the residual live-host probes whose owner shape is complete.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[NATIVE-BLAS]-[BLOCKED]: activate the native-OpenBLAS/MKL dense and sparse execution row.
- Capability: `LinearProvider.Select` activates the native dense and sparse provider row only when the RID resolves the admitted OpenBLAS or MKL native asset and the MathNet `Control.TryUseNativeOpenBLAS`/`TryUseNativeMKL` probe succeeds.
- Shape: the `Tensor/blas#DENSE_ALGEBRA` and `Tensor/factor#SPARSE_SOLVE` lanes stay shape-complete as one RID-keyed provider axis, with the managed terminal proved end-to-end on osx-arm64 and the native row entering as provider data rather than a second solve surface.
- Unlocks: win-x64 and linux-x64 hosts can prove the same dense factorization, sparse factorization, residual witness, and receipt path against native acceleration without changing call sites.
- Anchors: `Tensor/blas#DENSE_ALGEBRA`, `Tensor/factor#SPARSE_SOLVE`, `MathNet.Numerics.Providers.MKL`, `MathNet.Numerics.Providers.OpenBLAS`, `MathNet.Numerics.MKL.Win-x64`, and `MathNet.Numerics.MKL.Linux-x64`.
- Tension: blocked on a host RID that resolves the native asset; the managed fallback is the correct cold start until then.

[COMPUTE_QUANTITY_SPECTRAL_BRDF_PORT]-[QUEUED]: Rasm.Compute owns the QuantityFamily illuminance unit algebra the photometric emission admission coerces, the algorithms QR/Levenberg-Marquardt least-squares seam the acquisition BRDF fit composes, and the transitive ONNX Runtime spectral-inference path the SPECTRAL_REFLECTANCE_GROUNDING idea routes the ConductorIor RGB anchors through — the Compute seam the bsdf, photometric, and acquisition pages compose once, never re-minting a unit owner, a solver, or a spectral model.
- Capability: The compute owner supplying the QuantityFamily.Illuminance row the photometric EmissionSpectrum coerces emission luminance through at admission, the algorithms#ROUTE_SPINE thin-QR / Levenberg-Marquardt least-squares the acquisition FitBrdf minimizes the GGX/Smith residual through (MathNet.Numerics transitive, never a direct Materials reference), and the ONNX Runtime spectral-reconstruction model the SPECTRAL_REFLECTANCE_GROUNDING idea routes the RGB conductor anchors through to a per-wavelength complex-IOR curve.
- Shape: A Rasm.Compute QuantityFamily port, an algorithms least-squares seam, and an ONNX spectral-inference surface the Materials photometric/acquisition/bsdf pages compose at the seam, the producer end of the Appearance -> Rasm.Compute [PORT] seams.
- Unlocks: The photometric emission admits its illuminance through the one Compute unit algebra, the measured BRDF fits its parameter vector through the Compute least-squares seam, and the conductor table extends from three RGB bands to a full spectral curve through the Compute ONNX path the EPFL-RGL upstream-blocked leg awaits.
- Anchors: ARCHITECTURE.md [02]-[SEAMS] Appearance/bsdf and photometric <- Rasm.Compute/Symbolic [PORT] QuantityFamily illuminance (lines 46-47); acquisition.md the FitBrdf composes csharp:algorithms#ROUTE_SPINE thin QR (line 81, 'never hand-rolls a Levenberg-Marquardt loop'); bsdf.md the spectral conductor curve upstream-blocked note; the SPECTRAL_REFLECTANCE_GROUNDING ONNX leg and the BRDF_FIT_COMPUTE_SEAM task; realizes the `SPECTRAL_REFLECTANCE_GROUNDING` idea.
- Ripple: counterpart of `Rasm.Materials` `[SPECTRAL_REFLECTANCE_GROUNDING]` idea + `[BRDF_FIT_COMPUTE_SEAM]` task.

[COMPUTE_BSDD_PORT_TRANSPORT]-[QUEUED]: Compute lands the injected BsddPort transport implementation at the composition edge over its TRANSPORT_AXIS so the Bim BsddResolution live-wire leg binds a real buildingSMART bSDD dictionary fetch, the Bim side owning only the BsddPort interface and the LocalShape degrade.
- Capability: Grounds the Bim classification/property/IDS dictionary evidence path against a live transport: Compute owns the in-process request issue and response stream over its channel, projecting the bSDD BsddClassResponse the Bim BsddClass.Of consumes, never a Bim-minted transport.
- Shape: A Compute TRANSPORT_AXIS BsddPort implementation issuing GET /api/Class/v1 against the bSDD dictionary URI and streaming the BsddClassResponse back over the existing companion channel, injected into Bim at the app-platform composition root.
- Unlocks: The Bim T-BSDD-TRANSPORT card unblocks fully: Classification.Classify, the PropertyKey template fold, and the IDS Classification facet resolve live dictionary classes, degrading to LocalShape only on transport miss.
- Anchors: csharp:Rasm.Compute/Runtime/channels#TRANSPORT_AXIS; Semantics/classification#BSDD_RESOLUTION BsddPort.Fetch/BsddClassResponse; realizes the `PROPERTY_TEMPLATE_RESOLUTION` idea.
- Tension: The transport is Compute's channel concern; the bSDD wire contract (GET /api/Class/v1 BsddClassResponse) is the buildingSMART service shape the Bim BsddClass.Of already projects, so Compute owns the channel and Bim owns the response projection.
- Ripple: counterpart of `Rasm.Bim` `[PROPERTY_TEMPLATE_RESOLUTION]` idea + `[T-BSDD-TRANSPORT]` task.

[COMPUTE_IFCTESTER_ORACLE_RPC]-[QUEUED]: Compute and the Python geometry companion land the ifctester cross-tool IDS audit over the settled TWO_HOP companion rpc: Compute orchestrates the IDS-XML-to-ifctester invocation and the per-specification pass/fail GlobalId projection back, the Python ifc-companion runs the IfcOpenShell ids oracle, so the Bim IdsAudit shape diffs against a standards-conformant result on GlobalId-plus-facet.
- Capability: Gives the Bim in-process IdsAudit a deterministic external oracle: the Bim owner authors and parses the IDS spec, Compute orchestrates the companion rpc identically to the tessellation TWO_HOP pattern, and the Python ifctester returns the buildingSMART-audit-test-suite-conformant verdict.
- Shape: A Compute companion-rpc leg passing the IDS XML payload to the Python ifc-companion ifctester and projecting the per-spec pass/fail GlobalId set back into the Bim IdsAudit shape, plus the Python ifctester decoder producing the GlobalId-plus-facet verdict.
- Unlocks: The Bim T-IDS-IFCTESTER-COMPANION card unblocks: requirement-driven model acceptance compares the C# self-audit with the conformant ifctester oracle on GlobalId and facet, never a message diff.
- Anchors: Review/validation#IDS_FACETS IdsAudit; Exchange/tessellation#TESSELLATION_BRIDGE TWO_HOP; csharp:Rasm.Compute/Runtime/codecs#TWO_HOP_TESSELLATION; python:geometry/ifc-companion ifctester.
- Tension: The rpc rides the existing companion path the tessellation bridge proves, so Compute mints no new transport: it adds one ifctester invocation shape beside the IfcConvert one, the Python companion owning the IfcOpenShell ids oracle.
- Ripple: counterpart of `Rasm.Bim` `[T-IDS-IFCTESTER-COMPANION]` task (cross-tool IDS audit oracle).

[COMPUTE_UNITS_CUTPARAM_INGRESS_CONTRACT]-[QUEUED]: State the Rasm.Compute Symbolic units boundary contract canonicalizing Fabrication cut-parameter quantity text (mm/min feed, m/min surface speed, rpm spindle) to SI raw doubles at ingress, the upstream-owner alternative to the in-folder UNITSNET_CUTPARAM_ADMISSION, so the strata adjudication is stated on both sides rather than asserted from Fabrication only.
- Capability: Process/physics admits quantity-bearing cut-parameter text through the Rasm.Compute/Symbolic/units#QUANTITIES UnitsNet boundary, which canonicalizes to the SI scalar the toolpath interior operates on; the physics page (line 3/18/20) forbids a UnitsNet member spelled in-folder and routes all quantity ingress through this Compute boundary. The ripple states the Compute units owner's side: the QUANTITIES boundary exposes a canonicalize-to-SI surface the AEC-domain Fabrication consumer reads, owning the UnitsNet enums so the consumer never re-mints one — the architecture-density counter to UNITSNET_CUTPARAM_ADMISSION that keeps the units owner upstream.
- Shape: A Rasm.Compute Symbolic/units.md page note naming the cut-parameter quantity-ingress contract (feed/speed/depth/spindle text -> SI raw double) as a consumer-facing QUANTITIES export the Fabrication Process/physics boundary reads, reconciling the Fabrication-side routing claim against the Compute owner's actual canonicalization surface.
- Unlocks: A bidirectionally-owned quantity-ingress contract so the Fabrication physics page reads a real Compute surface, the adjudication between cross-strata-read and in-folder-admission stated explicitly rather than duplicating the units boundary.
- Anchors: csharp:Rasm.Compute/Symbolic/units#QUANTITIES the UnitsNet canonicalization boundary; Fabrication/Process/physics#CUT_PARAMETER RemovalParameter.Admit (lines 138-143); the strata law forbidding the AEC->app-platform downward edge; UNITSNET_CUTPARAM_ADMISSION the competing in-folder resolution
- Tension: The strata law forbids Fabrication (AEC-domain) a downward edge to Rasm.Compute (app-platform), yet physics.md routes quantity ingress THROUGH the Compute units boundary — the contract must clarify whether the canonicalization is a legal peer-export the AEC consumer reads or whether the in-folder UnitsNet admission is the strata-correct resolution; the critique wave resolves toward UNITSNET_CUTPARAM_ADMISSION, this ripple holds the upstream alternative.
- Ripple: counterpart of `Rasm.Fabrication` `[UNITSNET_CUTPARAM_ADMISSION]` task (`Symbolic/units` canonicalizes cut-parameter quantities).

[STRUCTURE_PROHIBITIONS_BLOCK]-[QUEUED]: ARCHITECTURE.md stops at `[03]-[SPINE]` with no prohibition block, so the device/sparse/autodiff/stats/UQ/optimizer thrusts have no codified guardrail against the parallel-owner and reimplementation defects each page already names in prose — add a `[04]-[SEAM_PROHIBITIONS]` / `[05]-[PROHIBITIONS]` block that promotes those inline rejections into spine law.
- Capability: One authoritative prohibition table on the package spine that every new owner row checks itself against, collapsing the scattered per-page deleted-form / rejected-form sentences into a single enumerated invariant set with code-band custody.
- Shape: A new ARCHITECTURE.md section after `[03]-[SPINE]` enumerating (1) no package-local tensor wrapper / `TensorService` beside `System.Numerics.Tensors` `Tensor<T>`; (2) no hand-rolled BLAS/factorization beside the MathNet `LinearProvider` stack and CSparse direct factors; (3) symbolic/device/learning faults extend the `ComputeFault` 2200 band at the next-free code (`CacheCorrupt` is 2212, so 2213+) rather than a parallel fault union or a status-code-plus-string terminal; (4) no string-eval in the solver/optimizer/UQ `evaluate` oracle — the `Func<DesignPoint,Fin<Seq<double>>>` contract is the only coupling point; (5) one `HybridCache` per lane, no per-call cache instance; (6) every new accelerator/sparse/AD/estimator capability is a row or case on an existing owner, never a sibling owner or a second state machine.
- Unlocks: Every subsequent deepen/admit task cites one fence, so a reviewer rejects a device-tensor-wrapper or an OR-Tools-as-second-optimizer-owner regression by pointing at one block rather than re-deriving the law from six pages.
- Anchors: `ARCHITECTURE.md` `[03]-[SPINE]` terminus; the `ComputeFault` 2200 band (`Text` 2200 .. `CacheCorrupt` 2212, next-free 2213) in `.planning/Runtime/admission.md`; the `TensorDtype` no-`TensorService` law in `.planning/Tensor/vocabulary.md`; the `LinearProvider`/`DenseOps` no-`RasmMatrix` law in `.planning/Tensor/blas.md`; the `FactoredOp`/`SparseOps` single-storage law in `.planning/Tensor/factor.md`; the `evaluate`-oracle single-coupling law in `.planning/Solver/optimizer.md` and `.planning/Solver/uncertainty.md`; the block's `HybridCache`-per-lane clause aligns with the `Runtime/scheduling` lane custody and its no-parallel-fault clause aligns with the `Runtime/channels` `FaultDetail` projection so device/symbolic faults still cross the wire through the one family.
- Tension: The inferred next-free 2217+ is stale — the live admission page ends at 2212, so the block must cite the real next-free 2213 so symbolic/device fault rows do not collide with an existing code.

[DEEPEN_DEVICE_GEMM_SPMV]-[QUEUED]: Promote the CPU-only `TensorOps` dispatch to a device-resident lane: a device-kernel registry lowers each matrix/structural `TensorOpFamily` row (MatMul, Conv1D/2D/3D, pooling) and the sparse SpMV/SpMM rows to a WebGPU compute-shader dispatch on the shared `ONE_WGPU_DEVICE`, dispatched only when the `OrtResidency.DeviceResident` gate holds and a winning `BenchmarkRow` names the device route.
- Capability: A `DeviceKernels` registry, parallel in shape to the existing `KernelLowering` binding table, mapping a `TensorOpFamily` row to its WebGPU compute pipeline and routing through the device residency gate, so GEMM and SpMV become device ops behind the same `Map`/`Lower` dispatch contract.
- Shape: A `DeviceKernels<T>` `FrozenDictionary` of `TensorOpFamily` -> `Silk.NET.WebGPU` compute pipeline, consulted by `KernelLowering.Lower` (and the sparse SpMV entry of `DEEPEN_SPARSE_SPMM_CONTRACT`) when `ShardPlan`/residency selects device, emitting the existing `TensorRun` receipt with the device residency stamped via `TensorBridge.Stamp`; the page becomes `Tensor/device/kernels.md`.
- Unlocks: Device GEMM for the dense solver and optimizer GP-covariance fits; device SpMV for the iterative sparse solver criterion stack; the dormant `OrtResidency.DeviceResident` row gains a real compute consumer beyond model inference.
- Anchors: `KernelLowering.Lower` + `ShardPlan` + `ConvWindow`/`Im2Col` in `.planning/Tensor/factor.md`; `TensorOpFamily.MatMul`/`Conv*`/`MaxPool` in `.planning/Tensor/vocabulary.md`; `TensorOps` dispatch + `PartitionRoute` in `.planning/Tensor/dispatch.md`; `OrtResidency.DeviceResident` + `TensorBridge.DeviceSink` + `CopyPoint` receipt in `.planning/Tensor/residency.md`; `ComputeReceipt.TensorRun` in `.planning/Runtime/receipts.md`; the device pipelines bind buffers from the AppUi-owned `ONE_WGPU_DEVICE` so a device GEMM output feeding the render lane crosses the existing `ResidencyManifest.Mint` seam rather than a new device-to-render path, and the one shared device descriptor also gates ONNX Runtime Mac execution-provider residency so a model-lane device tensor and this tensor-lane device kernel resolve the same allocator on the same physical device.
- Tension: WebGPU kernels are WGSL compute-pipeline bodies over storage buffers, so the registry value is a compiled pipeline + bind-group layout, not a span delegate like `UnaryKernel<T>` — the device entry mirrors `KernelLowering`'s table shape and never extends the CPU `TensorKernels<T>` delegate tables, so the CPU/device split rides residency, not a fork of the dispatch surface.

[COLLAPSE_SUBSTRATE_RESIDENCY_LATTICE]-[QUEUED]: The device thrust must not spawn a parallel device-state machine: collapse the new `Substrate.DeviceWgpu` selector, the `OrtResidency` device gate, and the `DeviceKernels` routing into the existing five-row `OrtResidency` lattice and four-row `Substrate` axis so device-vs-CPU is one residency discriminant, not a second admission spine.
- Capability: One unified residency+substrate lattice where the device row is a column on `OrtResidency.DeviceResident` and a row on the `Substrate` axis, so admission, dispatch, and receipt all read device-ness from the same discriminant the CPU path already uses.
- Shape: A single extension of the `Substrate` `[SmartEnum]` with a `DeviceWgpu` row carrying its availability predicate / cost-rank / payload-cap / fallback-successor columns, wired to `OrtResidency.DeviceResident`, with the device-kernel registry consulting that one discriminant — no `DeviceSelection` record, no second `SelectionReceipt`; the page becomes `Tensor/device/substrate.md` and `Tensor/device/kernels.md`.
- Unlocks: A device op flows through the same `IntentAdmission` -> `SubstrateSelection` -> `DispatchTable` spine as a CPU op, so backpressure, fallback (device unavailable -> CPU), and `SelectionReceipt` all work unchanged; prevents the device thrust from fragmenting the admission rail.
- Anchors: `Substrate` four-row axis + `SubstrateSelection` ordered-predicate fold + `SelectionReceipt` in `.planning/Runtime/admission.md`; `OrtResidency` five-row lattice in `.planning/Tensor/residency.md`; `ComputeReceipt.Selection` in `.planning/Runtime/receipts.md`; the `DeviceWgpu` row binds the AppUi-owned `ONE_WGPU_DEVICE` and its fallback-successor aligns with the `Runtime/scheduling` lane drain so the device-unavailable degrade-to-CPU reuses the existing `SubstrateUnavailable` fault toward the `Selection` receipt, and the same `SubstrateSelection` fold reads the AppHost `WorkLane` shed verdict (`Runtime/admission ← csharp:Rasm.AppHost` `ONE_DEGRADATION_SHED_VERDICT` seam) so a saturated host defers or rejects the device op through the one admission spine rather than a device-only backpressure path; the one shared device descriptor that this row resolves also gates ONNX Runtime Mac execution-provider residency, so a model-lane device tensor and a tensor-lane device kernel resolve the same allocator on the same physical device through this one discriminant.
- Tension: The `Substrate` axis currently carries `CpuTensor`/`RemoteGrpc` rows used by factor/lowering receipts, so adding the `DeviceWgpu` row must keep the fallback-successor chain total — a device-unavailable intent degrades to `CpuTensor` through the same fold, never a throw or a parallel device-admission path.

[DEEPEN_SPARSE_SPMM_CONTRACT]-[QUEUED]: The `Factor` page owns sparse SOLVE only (`Ingest`/`Factor`/`SolveIterative`); promote it to a full sparse linear-algebra category by adding SpMV/SpMM/add/subtract/transpose/Kronecker/contract as one `SparseTensorOpFamily` fold over the existing `SparseFormat` CSR ingestion, so sparse algebra is a row axis on the same storage, never a second sparse owner.
- Capability: A `SparseTensorOpFamily` `[SmartEnum]` + a `SparseOps` algebra fold (parallel in shape to the dense `TensorOpFamily`/`TensorOps` split) operating on the one `SparseCompressedRowMatrixStorage<double>`, dispatching each sparse operation as a row over MathNet sparse multiply and CSparse CSC arithmetic.
- Shape: `SparseTensorOpFamily` rows spmv/spmm/spadd/spsub/sptranspose/kron/contract, each binding its kernel (`SparseMatrix.Multiply` for SpMV/SpMM, `CompressedColumnStorage` arithmetic for add/transpose, a CSC-pattern Kronecker), folded by one `SparseOps.Apply` entry returning typed CSR storage and a `TensorRun` receipt with nnz; the page becomes `Tensor/sparse/ops.md`.
- Unlocks: Iterative solvers gain device-free fast SpMV; graph-Laplacian and FEM-assembly sparse products ride a typed op axis; the einsum planner of the next task contracts sparse operands; closes the sparse-algebra category gap from solve-only to full algebra.
- Anchors: `SparseFormat` csr/csc/coo/dok + `SparseOps.Ingest`/`Factor` + `SparseCompressedRowMatrixStorage<double>` + `SparseMatrix.OfStorage` in `.planning/Tensor/factor.md`; `TensorOpFamily`/`TensorOpKind` matrix-kind rows in `.planning/Tensor/vocabulary.md`; CSparse `CompressedColumnStorage` in `.api/api-csparse.md`; `ComputeReceipt.TensorRun` in `.planning/Runtime/receipts.md`; the sparse SpMV feeds the iterative criterion stack on the same page, sparse contract feeds the einsum planner, and a device SpMV row crosses to the `DEEPEN_DEVICE_GEMM_SPMV` `DeviceKernels` registry.
- Tension: MathNet sparse owns SpMV/SpMM but not Kronecker or arbitrary contraction, which lower onto CSC pattern construction through CSparse — the fold routes algebra ops to MathNet where it owns the concern and to CSparse `CoordinateStorage` where MathNet does not, never a hand-rolled triple loop.

[DEEPEN_EINSUM_PLANNER]-[QUEUED]: Add an einsum/contract planner as one `EinsumPlan` owner that parses an index-subscript spec, derives a contraction order minimizing intermediate size, and lowers each pairwise step to the dense MatMul (`KernelLowering` GEMM) or the sparse contract (`SparseTensorOpFamily`) route — one polymorphic contraction surface over both dense `Tensor<T>` and sparse storage.
- Capability: A single `EinsumPlan` that turns a subscript string + operand shapes into an ordered sequence of pairwise contractions, each lowered to an existing matrix/sparse row, so arbitrary tensor contraction is a planner over settled kernels, not a new kernel family.
- Shape: An `EinsumPlan` record (parsed subscripts, contraction tree, per-step route discriminant dense|sparse) + a `Contract` entry that folds the plan through `KernelLowering.Lower` (dense) or `SparseOps.Apply` (sparse), emitting one `TensorRun` receipt per step under the same correlation; the page becomes `Tensor/sparse/einsum.md`.
- Unlocks: Multi-operand tensor expressions (attention-style batched contractions, FEM tensor assembly, Tucker/CP intermediate products) ride one planner; the autodiff engine differentiates an einsum by transposing each step's subscripts; bridges dense and sparse algebra under one contraction contract.
- Anchors: `KernelLowering.Lower(MatMul)` + `MatMulGeometry` in `.planning/Tensor/factor.md` and `.planning/Tensor/dispatch.md`; `TensorOpFamily.MatMul`/`Dot` in `.planning/Tensor/vocabulary.md`; `SparseTensorOpFamily.contract` from `DEEPEN_SPARSE_SPMM_CONTRACT`; `Backward.MatMul` operand-transpose in `.planning/Tensor/dispatch.md`; `ComputeReceipt.TensorRun`; the per-step transpose feeds the autodiff JVP/VJP of `AUTODIFF_DUAL_MODE_ENGINE` and the sparse branch crosses to the `DEEPEN_SPARSE_SPMM_CONTRACT` `SparseOps`.
- Tension: Contraction-order optimization is exponential in operand count, so the planner uses a greedy/DP heuristic bounded by intermediate-size cost and a binary-pairwise reduction so each step is exactly one MatMul/contract row, never an n-ary kernel that bypasses the lowering table.

[DEEPEN_CSPARSE_QR_LEAST_SQUARES]-[QUEUED]: The sparse `FactorKind` axis carries qr as a direct-solve row but the page exposes only square `Solve`; promote SparseQR to a first-class overdetermined least-squares route so a rectangular sparse system (FEM normal equations, PCE fit, sparse regression) solves min‖Ax−b‖ through the existing `FactoredOp` capsule, not a dense fallback.
- Capability: A least-squares solve path on `FactoredOp` for the qr `FactorKind` that uses the SparseQR reentrant factorization's solution dimension (which exceeds row count for structurally singular systems) and emits the recomputed true residual against the original rectangular operator.
- Shape: A `FactoredOp.SolveLeastSquares` (or a polymorphic `Solve` discriminating on `Kind==Qr` + rectangular shape) routing through `SparseQR.Solve` over the `SolutionDim`-sized work buffer, witnessed by the existing residual gate, returning the typed `Factorization` receipt; the page becomes `Tensor/sparse/ops.md`.
- Unlocks: Sparse PCE coefficient fits and sparse-Jacobian least-squares (autodiff coloring), the UQ polynomial-chaos QR route, and overdetermined FEM recovery all use the sparse QR rather than densifying; the qr row's reentrant column becomes the one parallel-safe least-squares path.
- Anchors: `FactorKind.Qr` (reentrant: true, transposeSolve via `SparseQR.SolveTranspose`) + `FactoredOp.Solve` residual witness + `SolutionDim` sizing in `.planning/Tensor/factor.md`; `SparseQR.Create`/`Solve` in `.api/api-csparse.md`; `Matrix<double>.QR` least-squares (dense counterpart) in `.planning/Tensor/blas.md`; `ComputeReceipt.Factorization`; the sparse QR least-squares is the fit backend for UQ polynomial-chaos (`FILL_UNCERTAINTY_UQ`) and sparse-Jacobian coloring (`DEEPEN_SPARSE_JACOBIAN_COLORING`).
- Tension: SparseQR's solution dimension exceeds the row count for structurally singular systems, so the work buffer must size from the factorization's `SolutionDim` not the matrix shape (the off-by-augmentation fault the page names), and the residual is computed against the original rectangular A, not the augmented system.

[DEEPEN_FORWARD_JVP_COVERAGE]-[QUEUED]: The `DifferentiableOp` table carries reverse-mode VJP for every op but forward-mode JVP only for diagonal (elementwise) rows — MatMul/SoftMax/geometry rows fault `<no-forward-jvp>`; promote the engine to dual-mode by binding a forward-mode `Jvp` for every non-diagonal row so `AdjointMode.Forward` is total over the `TensorOpFamily` axis.
- Capability: Full forward-mode coverage where each non-diagonal `DifferentiableOp` row gains its JVP (MatMul JVP = A·ṫ + Ȧ·t pushforward, SoftMax JVP = J·t, geometry JVP = linear-operator forward apply A·t), so the `<no-forward-jvp>` fault becomes unreachable and forward-over-reverse composition is possible.
- Shape: Each `DifferentiableOp` row's `Some`-less `Jvp` becomes `Some`, bound through a `Forward` owner mirroring the existing `Backward` owner (`Backward.MatMul`/`SoftMax`/`Operator` -> `Forward.MatMul`/`SoftMax`/`Operator`), dispatched by `AdjointMode.Forward` in `SensitivityLaw.Adjoint`; the page becomes `Tensor/autodiff/forward.md`.
- Unlocks: Forward-mode sensitivities for tall-Jacobian problems (few inputs, many outputs); the forward-over-reverse Hessian-vector product of the next task; directional derivatives for the geometry DEC operators; closes the forward-JVP gap and is the substrate for the Hessian gap.
- Anchors: `DifferentiableOp` Diagonal-gated table + `AdjointMode.Forward`/`Reverse` + `Backward.MatMul`/`SoftMax`/`Operator` + `<no-forward-jvp>` fault + `SensitivityLaw.Adjoint`/`Chain` in `.planning/Tensor/dispatch.md` `[03]-[EQUIVALENCE_INTEROP]`; `GeometryTape`/`GeometryAdjoint` + `OperatorRow.Adjoint` in the same; `TensorOpFamily` geometry rows in `.planning/Tensor/vocabulary.md`; the forward JVP composes with reverse VJP for the HVP of `DEEPEN_HESSIAN_VECTOR_PRODUCT`, and the geometry forward apply reuses the live `DiscreteCalculus` assembly through `OperatorRow`, no second matrix.
- Tension: The geometry rows are linear DEC operators so their JVP equals their forward `Apply` (the operator is its own pushforward), but MatMul JVP needs both operands' tangents (A·ṫ + Ȧ·t) — the `Forward` owner carries the two-tangent form for bilinear ops, never the diagonal cotangent .* f'(primal) body correct only for elementwise rows.

[DEEPEN_HESSIAN_VECTOR_PRODUCT]-[QUEUED]: Add Hessian-vector products via forward-over-reverse composition: run a forward-mode JVP through the reverse-mode tape so Hv is computed without materializing the dense Hessian, exposed as one `SensitivityLaw.Hvp` entry — this HVP is the consistent-tangent operator the FEM constitutive/contact lanes consume.
- Capability: A matrix-free Hv operator that composes `DEEPEN_FORWARD_JVP_COVERAGE`'s forward mode over the existing reverse-mode `Chain`, returning the Hessian action on an arbitrary vector for Newton/trust-region steps and for the FEM tangent stiffness.
- Shape: `SensitivityLaw.Hvp(Seq<GeometryTape> tape, primal, vector) = Forward(Reverse(tape), vector)`, one entry beside the existing `Chain`, feeding the optimizer's gradient-adjoint trust-region and exposing the consistent-tangent K_T to the constitutive lanes; the page becomes `Tensor/autodiff/hessian.md`.
- Unlocks: Second-order optimizer steps (Newton-CG over the matrix-free Hv); FEM consistent linearization for plasticity/hyperelasticity/contact (the two AD constitutive tasks ride this HVP); eigenanalysis of the Hessian for saddle detection — closes the second-order gap and supplies the constitutive tangent.
- Anchors: `SensitivityLaw.Chain` reverse-mode tape + `AdjointMode` forward/reverse + `DifferentiableOp` `Jvp`/`Vjp` in `.planning/Tensor/dispatch.md`; `Optimizer.DescendAdjoint` + `StepControl.TrustRegion` (actual/predicted ratio) in `.planning/Solver/optimizer.md`; `Cholesky<double>`/`Evd` spectral surface in `.planning/Tensor/blas.md` for the Newton solve; the HVP is the consistent-tangent source for `DEEPEN_PLASTICITY_HYPERELASTIC_STRESS_UPDATE` and `DEEPEN_FRICTIONAL_CONTACT_ENFORCEMENT`, feeds the optimizer trust-region predicted-reduction, and the Newton inner solve crosses to the dense Cholesky route in blas.
- Tension: Forward-over-reverse requires the reverse tape to be re-traversable with dual numbers seeded by the forward tangent, so the tape's recorded (op, primal) snapshots must carry enough state for the forward pass — the HVP composes the two existing modes, it does not record a second tape, so a tape that drops the primal snapshot breaks the composition.

[DEEPEN_SPARSE_JACOBIAN_COLORING]-[QUEUED]: Add sparse-Jacobian construction via graph coloring: detect the Jacobian sparsity pattern, color the structurally-orthogonal columns (greedy distance-1/distance-2), and recover the full sparse Jacobian in (#colors) forward-JVP or reverse-VJP passes instead of (#columns) — one `JacobianColoring` owner over the AD tape and the sparse storage.
- Capability: A coloring-compressed Jacobian assembler that turns a dense per-column AD sweep into a per-color sweep, materializing the result directly as the `SparseFormat` CSR storage the sparse lane owns.
- Shape: A `JacobianColoring` record (sparsity pattern, color partition) + an `Assemble` entry that probes each color group with a seed vector through `DEEPEN_FORWARD_JVP_COVERAGE` (forward) or the reverse `Chain`, then scatters the compressed columns into `SparseCompressedRowMatrixStorage<double>`; the page becomes `Tensor/autodiff/hessian.md`.
- Unlocks: Tractable Jacobians for large FEM/network systems where dense AD is infeasible; the assembled sparse Jacobian factors through `DEEPEN_CSPARSE_QR_LEAST_SQUARES`; Newton steps on sparse residuals; completes the dual-mode engine's sparse output path.
- Anchors: `SensitivityLaw` forward/reverse modes + `DifferentiableOp` in `.planning/Tensor/dispatch.md`; `SparseFormat` CSR + `SparseOps.Ingest` + `CoordinateStorage` scatter in `.planning/Tensor/factor.md`; `FactorKind.Qr` least-squares from `DEEPEN_CSPARSE_QR_LEAST_SQUARES`; CSparse AMD ordering in `.api/api-csparse.md`; the colored Jacobian feeds `DEEPEN_CSPARSE_QR_LEAST_SQUARES` for the sparse Newton solve and the seed-vector sweep reuses `DEEPEN_FORWARD_JVP_COVERAGE`.
- Tension: Distance-2 coloring (needed when both forward and reverse compression apply) is NP-hard, so the owner uses a greedy degree-ordered heuristic, and the sparsity pattern must be known or probed first — coloring over an unknown pattern silently under-recovers, so pattern detection precedes the color partition.

[DEEPEN_PLASTICITY_HYPERELASTIC_STRESS_UPDATE]-[QUEUED]: Add a constitutive stress-update axis on the Solver lane — return-mapping plasticity, hyperelastic (Neo-Hookean/Mooney-Rivlin), viscoelastic, and damage models as one `ConstitutiveModel` union whose consistent tangent is the `DEEPEN_HESSIAN_VECTOR_PRODUCT` HVP of the strain-energy, so the material tangent rides AD rather than a hand-derived D-matrix.
- Capability: One `ConstitutiveModel` `[Union]` over strain -> (stress, consistent-tangent) where the tangent is the AD Hessian of the stored-energy function, so adding a material law is one case carrying its energy function, not a hand-coded tangent.
- Shape: `ConstitutiveModel` cases (Plastic return-map, Hyperelastic energy, Viscoelastic Prony, Damage) each supplying a strain-energy/yield function; `StressUpdate` folds the case to (stress = ∂W/∂ε via reverse VJP, tangent = ∂²W/∂ε² via HVP), emitting the `Solve` receipt and consuming the Materials D-matrix source; the page becomes `Solver/constitutive/stress.md`.
- Unlocks: Nonlinear material FEM (plastic hinges, rubber, creep, progressive damage) with an automatically-consistent tangent so Newton converges quadratically; new materials added by their energy function alone; binds the Materials physical-properties seam as the constitutive parameter source.
- Anchors: `DEEPEN_HESSIAN_VECTOR_PRODUCT` `SensitivityLaw.Hvp` consistent tangent; the Solver/contract Physics×BC×element solve fold (referenced via README and ARCHITECTURE Solver folder); `ComputeReceipt.Solve` (Physics/Method/Dofs/Residual/Converged) in `.planning/Runtime/receipts.md`; the `Symbolic/expression` differentiate family for closed-form energy in `.planning/Symbolic/expression.md`; `ObjectiveSense`/`DesignProblem` evaluate oracle in `.planning/Solver/optimizer.md`; the stress-update reads the elastic D-matrix and inelastic parameters from `Rasm.Materials` physical-properties so the material D is sourced once, the analysis element binds the material to a mesh element across the `Rasm.Bim/Model` `AnalysisModel (GeometryKey, PropertyKey)` content-key seam, and the HVP tangent crosses from `DEEPEN_HESSIAN_VECTOR_PRODUCT`.
- Tension: Return-mapping plasticity has a non-smooth yield surface where the AD tangent is the algorithmic (consistent) tangent of the return map, not the continuum tangent — the case differentiates THROUGH the return-mapping iteration, so a naive AD of the elastic predictor gives the wrong tangent and breaks Newton convergence.

[DEEPEN_FRICTIONAL_CONTACT_ENFORCEMENT]-[QUEUED]: Add frictional contact as a `ContactConstraint` axis on the same constitutive page — node-to-surface/mortar normal contact with Coulomb friction enforced by penalty/augmented-Lagrangian/mortar, whose contact stiffness is the AD tangent of the gap/stick-slip potential, so contact rides the same HVP-tangent contract as the material lane.
- Capability: One `ContactConstraint` `[Union]` (normal gap, Coulomb stick-slip) whose enforcement method reuses the optimizer's `ConstraintHandling` augmented-Lagrangian multiplier update and whose consistent tangent is the AD Hessian of the contact potential.
- Shape: `ContactConstraint` cases (NodeToSurface, Mortar) + a `ContactEnforcement` fold binding the existing `ConstraintHandling.AugmentedLagrangian` multiplier update (λ ← λ + ρ·g) to the gap function, with the contact stiffness from the HVP of the regularized contact potential, emitting the `Solve` receipt; the page becomes `Solver/constitutive/contact.md`.
- Unlocks: Contact mechanics (assembly interfaces, bolted/bearing joints, self-contact) with a consistent linearization; reuses the optimizer's feasibility/multiplier machinery so contact is a constraint discipline, not a new solver; completes the constitutive/contact category atop the dual-mode AD engine.
- Anchors: `ConstraintHandling.AugmentedLagrangian` (MultiplierUpdate=true, λ←λ+ρ·g, Penalize/Lagrange) in `.planning/Solver/optimizer.md`; `DEEPEN_HESSIAN_VECTOR_PRODUCT` HVP tangent; the `DEEPEN_PLASTICITY_HYPERELASTIC_STRESS_UPDATE` `ConstitutiveModel` page; `ComputeReceipt.Solve` in `.planning/Runtime/receipts.md`; the Clash acceleration-structure collision (referenced via Solver folder) for the broad-phase contact pair detection, so contact pairs cross from the Clash lane, the augmented-Lagrangian update reuses the optimizer `ConstraintHandling`, and the contact tangent reuses `DEEPEN_HESSIAN_VECTOR_PRODUCT`.
- Tension: Coulomb friction's stick-slip transition is non-smooth and non-associative, so the AD tangent of a regularized (smoothed) friction potential is consistent only within the regularization — the case carries the regularization parameter and the broad-phase pair set from the Clash lane, never re-detect contact pairs.

[DEEPEN_GLM_REGRESSION_ESTIMATORS]-[QUEUED]: Open a Stats lane with one `Estimator(Fit/Predict)` axis: OLS/ridge/lasso regression and the GLM family (logistic/Poisson/gamma via IRLS) as `Estimator` rows over the dense SVD/QR/Cholesky factorization surface and MathNet `Statistics`/`Distributions`, so every regression model is a row on one Fit/Predict contract, never a per-model class.
- Capability: One `Estimator` `[Union]` with a uniform `Fit(X,y) -> FittedModel` and `Predict(FittedModel,X) -> ŷ` contract, the regression/GLM rows fitting through the existing dense `FactorRoute` (QR least-squares, Cholesky normal equations) and link functions from MathNet `Distributions`.
- Shape: `Estimator` cases (LinearRegression, Ridge, Lasso, Glm carrying a `LinkFunction` row) + `EstimatorFold.Fit` dispatching to `Matrix<double>.QR().Solve` / Cholesky normal equations / IRLS over the GLM link, returning a `FittedModel` carrying coefficients + a fit-quality receipt; the page becomes `Stats/estimator.md`.
- Unlocks: The whole supervised-regression category (parametric structural calibration, sensor regression, surrogate trend fitting) under one estimator contract; GLM for count/binary engineering data; the Fit/Predict contract is the spine the clustering/classification/timeseries tasks extend.
- Anchors: `DenseRoute.Solve` `FactorRoute` (Orthonormal QR, DefinitePsd Cholesky) + `Matrix<double>.QR().Solve` in `.planning/Tensor/blas.md`; `MathNet.Numerics.Statistics` + `Distributions` (Normal/Poisson/Gamma) in `.planning/Solver/uncertainty.md` usage and `.api/api-mathnet-providers.md`; `ComputeReceipt.TensorRun`/`Factorization` for the fit receipt; `RbfModel`/`GpModel` regression precedent in `.planning/Solver/optimizer.md`; the Fit/Predict contract is extended by `DEEPEN_CLUSTERING_DIMREDUCTION` and `DEEPEN_HYPOTHESIS_TIMESERIES`, fitted surrogates feed the optimizer `Surrogate.Predict` oracle, and offline studies cross to Python compute/graduation.
- Tension: Lasso has no closed-form solution (coordinate descent or LARS) while OLS/ridge factor directly, so the `Estimator` fold routes the L1 row to an iterative coordinate-descent kernel while the L2/OLS rows ride the dense factorization — one Fit fold, two solution mechanisms by row, never a separate Lasso owner.

[DEEPEN_CLUSTERING_DIMREDUCTION]-[QUEUED]: Extend the `Estimator` axis with unsupervised learning — PCA/kernel-PCA/random-projection dimensionality reduction, k-means/GMM/hierarchical/DBSCAN clustering, and k-NN/SVM/tree classification — as `Estimator` rows over the dense SVD surface and MathNet, so the unsupervised and classification families share the one Fit/Predict contract.
- Capability: Clustering, dimensionality reduction, and classification as additional `Estimator` `[Union]` rows, PCA fitting through the dense `Svd<double>` left-singular basis (the same surface DEIM/POD use), clustering folding `Statistics` distances, classification reusing the regression fit machinery.
- Shape: `Estimator` rows (Pca, KernelPca, KMeans, Gmm, Dbscan, KnnClassifier, plus a kernel SVM) each binding its `Fit` kernel (Svd truncation for PCA, Lloyd iteration for k-means, EM for GMM) and `Predict` (project / assign / classify), all returning a `FittedModel` on the one contract; the page becomes `Stats/estimator.md`.
- Unlocks: The full unsupervised + classification category (design-space embedding, anomaly clustering of sensor data, material-class prediction); PCA reuses the optimizer's POD-SVD basis machinery; closes the clustering/classification and dim-reduction gaps under one estimator surface.
- Anchors: `Svd<double>` + `Matrix<double>.Svd(computeVectors:true)` + `EnergyFraction` truncation in `.planning/Tensor/blas.md` and `.planning/Solver/optimizer.md` (`Orthogonalization.PodSvd`); the `DEEPEN_GLM_REGRESSION_ESTIMATORS` `Estimator` axis; `MathNet.Numerics.Statistics` distances + `Distributions` for GMM in `.api/api-mathnet-providers.md`; `ComputeReceipt.TensorRun` fit receipt; PCA basis reuses `Orthogonalization.PodSvd` from the optimizer, fitted clusterers feed anomaly detection in the Clash/Twin lane, and classification models feed Python graduation studies.
- Tension: GMM (EM) and DBSCAN have no factorization route and are iterative/density-based, so the `Estimator` fold keeps PCA on the dense SVD surface while routing EM/density rows to their own iteration kernels — the shared contract is Fit/Predict, not a shared solution mechanism, so a forced SVD route for k-means is wrong.

[DEEPEN_HYPOTHESIS_TIMESERIES]-[QUEUED]: Add hypothesis testing (t/ANOVA/chi-square/KS/Mann-Whitney) and time-series analysis (AR/ARMA/exponential-smoothing/autocorrelation/changepoint) as one `StatisticalTest` axis and one `TimeSeriesModel` Estimator extension over MathNet `Statistics`/`Distributions`, so inference and temporal modeling ride the same lane as regression.
- Capability: A `StatisticalTest` `[SmartEnum]` returning a typed `TestResult` (statistic, p-value, decision) over MathNet `Distributions` CDFs, and `TimeSeriesModel` Estimator rows (AR/ARMA fit through the dense least-squares route, smoothing/ACF through `Statistics`) on the Fit/Predict contract.
- Shape: `StatisticalTest` rows (t/anova/chi-square/ks/mann-whitney) each computing its statistic and reading the p-value from the matching MathNet `Distributions` CDF; `TimeSeriesModel` Estimator rows (Ar, Arma, ExponentialSmoothing) fitting the lag design through `Matrix<double>.QR` least-squares; both emit a typed receipt; the pages become `Stats/estimator.md` and `Stats/signal.md`.
- Unlocks: Statistical inference (A/B design comparison, residual normality, variance equality) and forecasting/changepoint detection for sensor and monitoring streams; AR/ARMA fits reuse the regression least-squares; completes the statistics category breadth (hypothesis + time-series).
- Anchors: `MathNet.Numerics.Distributions` CDF/InvCDF (Normal/StudentT/ChiSquared/FisherSnedecor) in `.planning/Solver/uncertainty.md` usage and `.api/api-mathnet-providers.md`; `MathNet.Numerics.Statistics` in the same; the `DEEPEN_GLM_REGRESSION_ESTIMATORS` QR least-squares for AR/ARMA; the `ComputeReceipt` union (a new measured concern is one case row); hypothesis tests validate UQ response distributions (`FILL_UNCERTAINTY_UQ`), time-series changepoint feeds the Twin anomaly lane, and the AR design reuses `DEEPEN_GLM_REGRESSION_ESTIMATORS` least-squares.
- Tension: ARMA's moving-average term is nonlinear in the parameters (no closed-form least-squares), requiring conditional/exact MLE, so the `TimeSeriesModel` fold routes the pure-AR row to QR least-squares but the ARMA row to an iterative likelihood maximization — distinct fit mechanisms on the one Estimator contract.

[DEEPEN_SIGNAL_SPECTRAL]-[QUEUED]: Open a signal/spectral `Transform` axis spanning frequency-domain analysis and filtering — FFT/IFFT, STFT, spectrogram, Welch PSD, and wavelet transforms plus FIR (windowed-sinc/Parks-McClellan) and IIR (Butterworth/Chebyshev/elliptic) filter design and filtfilt/overlap-add convolution — as one signal-transform fold over MathNet `IntegralTransforms`/`Window` and the existing Complex tensor kernels, so spectral transforms and filtering ride one axis distinct from the `Estimator` axis but sharing the Stats lane.
- Capability: A `SpectralTransform` `[SmartEnum]` over MathNet `IntegralTransforms` (`Fourier.Forward`/`Inverse`, Hartley) and the `ComplexKernels` (`ComplexExp`/`ComplexAbs`/`Conjugate`) the dispatch table already owns, windowing through MathNet `Window`, plus a `FilterDesign` `[SmartEnum]` producing typed FIR/IIR coefficients applied through the existing Conv1D `KernelLowering` route (time domain) or the FFT overlap-add (frequency domain), so filter application is not a new convolution kernel.
- Shape: `SpectralTransform` rows (fft, ifft, stft, spectrogram, welch-psd, dwt) folding through `MathNet.IntegralTransforms.Fourier` over `Complex[]` derived from the `Tensor<float>` input via the `ComplexKernels` with windowing/overlap as policy columns; `FilterDesign` rows (fir-window, fir-remez, iir-butterworth, iir-chebyshev1/2, iir-elliptic) each emitting a `FilterCoefficients` record whose `Apply` folds either to `TensorOpFamily.Conv1D` via `KernelLowering` or to the spectral overlap-add; both emit a `TensorRun` receipt; the page becomes `Stats/signal.md`.
- Unlocks: Vibration/acoustic/modal signal analysis, frequency-domain feature extraction for the learning lane, modal-test correlation against the eigen lane, and signal conditioning (noise removal, band isolation, anti-aliasing, zero-phase filtfilt) for sensor/monitoring streams feeding the learning and Twin lanes; closes the signal-spectral category gap on one transform axis.
- Anchors: `MathNet.Numerics.IntegralTransforms` (Fourier) and `MathNet.Numerics.Window` for FIR windowing; `ComplexKernels` (`ComplexExp`/`ComplexLog`/`Conjugate`/`ComplexAbs`) + `System.Numerics.Complex` in `.planning/Tensor/dispatch.md`; `TensorOpFamily.ComplexAbs`/`ComplexExp`/`Conv1D` rows in `.planning/Tensor/vocabulary.md`; `KernelLowering.Lower(Conv1D, ConvWindow)` Im2Col-then-GEMM in `.planning/Tensor/factor.md`; the Quadrature spectral operator in `.planning/Tensor/quadrature.md`; `ComputeReceipt.TensorRun`; spectral features feed `DEEPEN_HYPOTHESIS_TIMESERIES` and the classification rows, the spectral transform shares the Complex kernels with the dispatch lane, modal spectra cross to the blas Evd eigen lane, and filtered signals feed the time-series and classification estimators.
- Tension: MathNet `IntegralTransforms` operates on `Complex[]` in place with its own `FourierOptions` scaling conventions, so the transform fold marshals the `Tensor<float>` through the `ComplexKernels` into `Complex[]` once and applies one consistent scaling, never re-implementing the radix-2/Bluestein kernel; and IIR filters are recursive (feedback) so they do NOT lower to the feed-forward Conv1D GEMM — only FIR application rides the convolution route while the `FilterDesign` `Apply` fold routes IIR to a direct-form-II recurrence, two application mechanisms on the one design axis.

[FILL_UNCERTAINTY_UQ]-[QUEUED]: Fill the uncertainty lane to full UQ-method breadth: realize each `UncertaintyMethod` row's distinct kernel (LHS stratification, subset-simulation conditional levels, the analytic FORM branch) and deepen the global-sensitivity and spectral-UQ breadth — Saltelli first-order + total-effect Sobol indices, Morris elementary-effects screening, orthogonal-basis polynomial-chaos expansion with closed-form moments/Sobol, and the SORM second-order companion to FORM — on the one `Uncertainty.Propagate` fold over the existing `LowDiscrepancy`-seeded sampling/quadrature + sweep DOE surface.
- Capability: A complete `Propagate` fold where every `UncertaintyMethod` row carries its realized sample-design and reduction kernel (the `SampleBased` discriminant routing MC/LHS/subset sampling versus PCE/FORM analytic rows without a stub fall-through), plus the full forward-UQ + global-sensitivity + reliability breadth (Saltelli Sobol, Morris screening, PCE spectral, FORM/SORM) with PCE coefficients fitting through the dense/sparse QR least-squares route.
- Shape: The Design/Sample/Reduce fold gains the per-row kernels — LHS `Stratify` finalized, subset-simulation's conditional threshold-exceedance resampling, the FORM most-probable-point search, the PCE row fitting orthogonal-polynomial response coefficients through `Matrix<double>.QR().Solve` (or the sparse QR of `DEEPEN_CSPARSE_QR_LEAST_SQUARES` for high-order) and reading moments/Sobol closed-form from the spectral coefficients, the Sobol row using Saltelli A/B/AB resampling, and SORM adding the curvature correction over the FORM MPP — each a row arm on the existing `Uncertainty` static fold; the pages become `Solver/uncertainty.md` and `Solver/sweep.md`.
- Unlocks: Reliability analysis (FORM β, subset-simulation rare-event pf, SORM curvature) and variance-reduced sampling actually run rather than degrading to plain MC; variance-based global sensitivity ranking the optimizer/sweep consume; spectral surrogate moments without re-sampling; auditable pf/β/Sobol receipts for a reliability verdict; closes the UQ method-breadth gap to the full category.
- Anchors: `UncertaintyMethod` rows + `SampleBased` discriminant + `Uncertainty.Propagate`/`Design`/`Sample`/`Reduce` + `Stratify` + `FailureProbability`/`ReliabilityBeta`/`SobolTotal` + `PolynomialChaos` row in `.planning/Solver/uncertainty.md`; `LowDiscrepancy.Sobol` + `Scramble.DigitalShift` in `.planning/Tensor/quadrature.md`; `RandomVariable.Quantile` inverse-transform; `SensitivityTornado.Of` + `SensitivityMethod` sobol-variance/morris-elementary (BinVariance/Elementary) in `.planning/Solver/sweep.md`; `Matrix<double>.QR` least-squares in `.planning/Tensor/blas.md`; `DEEPEN_CSPARSE_QR_LEAST_SQUARES` sparse QR; `Normal.CDF`/`InvCDF` for β; the Sobol/Morris rows reuse the sweep `SensitivityTornado` variance fold, the PCE sparse fit crosses to `DEEPEN_CSPARSE_QR_LEAST_SQUARES`, the offline learned-input-distribution/PCE fit stays the Python companion's `ONE_GRADUATION_EVIDENCE` `uncertainty-law` artifact decoded by content key, and the reliability β/pf feed robust optimization in `OPTIMIZATION_DEPTH`.
- Tension: Subset-simulation conditions successive populations on intermediate threshold exceedances (a Markov-chain Metropolis step within each level) which is stateful across levels, so the immutable fold accumulator threads the conditional population rather than a per-level mutable resample loop the page forbids; Saltelli Sobol needs a structured A/B/AB sample matrix the current one-point-per-realization draw does not produce; PCE basis size grows combinatorially with input dimension and order, so the fit uses a truncated/sparse basis (hyperbolic truncation) routed to the sparse QR for high dimension, never a dense Vandermonde that overflows; and SORM curvature requires the limit-state Hessian at the MPP, sourced from the `AUTODIFF_DUAL_MODE_ENGINE` HVP.

[DEEPEN_MIP_CPSAT]-[QUEUED]: Realize the optimization-depth breadth atop OR-Tools: CP-SAT/MIP exact solving plus global/multi-start (basin-hopping over the existing metaheuristics) and robust optimization (worst-case/chance-constrained via the UQ reliability evidence) as `OptimizerKind` rows and a robustness policy column, completing the optimizer category beyond the seven heuristic rows.
- Capability: Exact integer/constraint solving (cp-sat, mip), global multi-start restart wrapping any metaheuristic row, and robust/chance-constrained objectives reading the UQ reliability β — all as rows/columns on the one `Optimizer` fold and `OptimizerPolicy`.
- Shape: `OptimizerKind` rows cp-sat/mip (build OR-Tools `CpModel`/`MPSolver` from `DesignProblem`) + a `MultiStart` policy column wrapping any row with restart + a `Robust` column folding the UQ `FailureProbability`/reliability into the objective (chance constraint g + β·σ ≤ 0), each emitting the existing Optimization receipt; the page becomes `Solver/integer.md`.
- Unlocks: Discrete + continuous + robust design under one optimizer; global guarantees via exact MIP or multi-start escape; robust optima that account for input uncertainty via the UQ β — the full optimization category from local heuristic to exact-global-robust.
- Anchors: `Optimizer.Steps` `FrozenDictionary` + `OptimizerKind` + `OptimizerPolicy` + `ConstraintHandling.AugmentedLagrangian` + `DesignVariable.Integer`/`Categorical` in `.planning/Solver/optimizer.md`; `Google.OrTools` `CpModel`/`MPSolver` (from `ADMIT_ORTOOLS`, `.api/api-ortools.md`); `UncertaintyResult.ReliabilityIndex`/`FailureProbability` in `.planning/Solver/uncertainty.md` for the robust column; `ComputeReceipt.Optimization`; the `Robust` column reads the UQ reliability β from `FILL_UNCERTAINTY_UQ`, the inner UQ reuses the surrogate-gated oracle, CP-SAT/MIP results feed the `ParetoFront` crossing to Persistence, and offline integer studies cross to Python compute/graduation.
- Tension: Robust/chance-constrained optimization nests a UQ propagation inside each objective evaluation (inner UQ loop) which is expensive, so the `Robust` column reuses the surrogate-gated `evaluate` oracle where the inner reliability estimate rides the cheap `Surrogate.Predict` and the error bound holds, never a full inner MC per outer candidate.

[DATA_GEOARROW_GLB]-[QUEUED]: Rasm.Compute consumes the data-companion native GeoArrow buffers over the existing C# GLB tessellation wire, mirroring the spatial/geospatial -> Rasm.Compute [SHAPE] edge so the GeoArrow buffer interchange lands at the wire rather than stranded one-sided on the Python producer.
- Capability: A Compute seam row reading the data companion's `geoarrow-rust-compute` GeoArrow buffer layout (zero GEOS/GDAL) directly into the GLB tessellation wire buffer, so a geospatial mesh payload crosses on the one settled GLB binary layout the tessellation bridge already projects, never a second spatial codec.
- Shape: A Compute seam consuming `python:data/spatial/geospatial` `[SHAPE]` GeoArrow buffers, decoding the column-major GeoArrow coordinate/offset buffers into the GLB tessellation wire's vertex/index spans on the existing companion channel, emitting the existing tessellation receipt.
- Anchors: the Compute GLB tessellation wire buffer layout; the `geoarrow-rust-compute` GeoArrow buffer column layout the data companion produces; the tessellation bridge companion channel the GLB payload already rides.
- Ripple: `data` `[GEOSPATIAL_INGRESS_DEEPEN]` — GeoArrow coordinate/offset buffers share the C# GLB tessellation wire buffer layout, so the geospatial interchange is consumed at the wire on both endpoints.
- Atomic: GeoArrow-to-GLB wire buffer consume row.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[ADMIT_SILK_WEBGPU]-[COMPLETE]: `Silk.NET.WebGPU` 2.23.0 (already manifest-pinned) registered in Compute README `[TENSOR_NUMERIC]` and `.api/api-silk-webgpu.md` authored for the shared branch `ONE_WGPU_DEVICE`; compute-shader lowering rides `DEEPEN_DEVICE_GEMM_SPMV`.
[ADMIT_ORTOOLS]-[COMPLETE]: `Google.OrTools` 9.15.6755 pinned + referenced (per-RID natives incl. osx-arm64 auto-resolve); README `[OPTIMIZATION]` group and `.api/api-ortools.md` authored.
