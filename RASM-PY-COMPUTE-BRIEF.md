# [RASM_PY_COMPUTE_BRIEF]

Authoritative campaign input for the ground-up restructure and rebuild of `libs/python/compute/.planning/`. `compute` is the FOURTH and TERMINAL folder in the Python campaign sequence (runtime -> data -> geometry -> compute): `RASM-PY-RUNTIME-BRIEF.md`, `RASM-PY-DATA-BRIEF.md`, and `RASM-PY-GEOMETRY-BRIEF.md` are finalized upstream law this brief consumes at the seam — the identity rename, the offload axis, the reproduction corpus, the frozen `GeometryHandoff` contract, and the `xarray`/`dask`/`sparse` tier motions are upstream rulings compute rewires against, never re-plans. Waterfall edits applied upstream: `RASM-PY-RUNTIME-BRIEF.md` `[V5]` now names `compute` beside geometry/data as a demanding consumer on the offload isolation-modality axis (the JAX x64 process-global flag plus the subinterpreter-unloadable jaxlib/numpy stack pinning PROCESS isolation); `[V7]` now pins `CANONICAL_POLICY` as an exported identity surface with the default-policy key-equality law, names compute's buffer/stream `IdentitySource.lift` modalities, and admits BOTH compute fixtures into the reproduction corpus beside `MATERIAL_LAYER_GOLDEN`/`GLB_BY_KEY` — the `ARRAY_LAYOUT` cross-backend bit-identity parity and the C#-minted `EvidenceBundle` golden bundle backing codegen's decode+emit drift gate; runtime `[04]`'s `universal-pathlib` row now carries compute as the named `UPath` consumer (`experiments/model.md`). `docs/stacks/python/` governs.

## [EXECUTION]

Settled campaign, py track 4/5 and the branch terminal — launch after RASM-PY-RUNTIME/DATA/GEOMETRY land, against a frozen upstream corpus; the page-set delta is fully ruled in `[01]` (zero new pages, zero deletions, one vocabulary re-homing, one hub rebuild in place), so the durable rebuild takes this brief directly (no DECISION pass), its plan step classifying per-page kind from the `[01]` verdicts against disk: step 1 loops once per `[05]-[BUILD_LEGS]` leg in partition order (legs 1-3; each leg self-contained, its pass agents repairing the cross-file ripples their own work exposes in-pass and its serialized per-wave tail closing index docs and central manifests before it returns) and re-runs across sessions against this same brief until a cold pass lands nothing; the campaign closes on the `[05]` acceptance proofs.

| [STEP] | [LAUNCH] | [PRODUCES] |
| :----: | :------- | :--------- |
| 1 | `rebuild` — `{brief: "RASM-PY-COMPUTE-BRIEF.md", leg: N}` (targets derive from the `[05]-[BUILD_LEGS]` leg rows; `leg: [1..3]` is the one-run form) | One `[05]-[BUILD_LEGS]` leg landed per run |

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level exceptional — 26 pages at 8.5-9.5 craft, the strongest table/ADT discipline of the four py folders — and hub-level inverted, seam-level illusory, and governance-level blind. Four proofs, each independently fatal to a world-class claim:

1. **The graduation hub sits on the wrong side of finalized law.** `graduation/handoff.md:61-70` MINTS `type GeometrySubject = Literal[...]` (8 literals) that `RASM-PY-GEOMETRY-BRIEF.md` `[V2]` re-homes to a geometry mint; `handoff.md:21` frames `scan-deviation` as "the PRODUCER literal compute emits ... the geometry `scan/deviation.md#DEVIATION` owner consumes" and `:199` recites every geometry owner as a CONSUMER of compute-produced literals — backwards on every clause: geometry PRODUCES `GeometryHandoff` carriers and compute DECODES them, re-shaping nothing without a geometry ripple. The 8-literal union is stale against the frozen 13 (geometry `[V2]` differentiates `bim-compliance`/`bim-lifecycle`/`section-property`; `[V1]` adds `building-energy`/`thermal-comfort`), so the hub cannot decode the carrier the upstream campaign ships; `ARCHITECTURE.md:50` mistypes the crossing `⇄` (geometry `[V11]` kills the glyph — no import exists in either direction) and `ARCH:52` declares a compute->geometry projection the geometry `[00]` DAG law (runtime/data/itself only) makes unconsumable.
2. **The graduation rail is dead on five of eight axes and wired three incompatible ways.** Only `inference`/`model`/`quantity` import handoff and define `graduates()` (verified: `rg 'def graduates\('` + import census); `convex.md:19-20` lists `graduation/handoff.md#GRADUATION` in its Packages block and claims the `convex_program` crossing while its prelude imports nothing from handoff; `spatial.md:14` claims a geometry-case graduation with no import; `array_layout` has no producing fence anywhere in the corpus; `solver` has no producing fence though `handoff.md:193-199` recites receipt/design/program/convex/symbolic/array/spatial as feeders. A tagged-union axis whose cases have no producers is dead vocabulary wearing a rail.
3. **The folder is unimportable the moment its upstream lands, and its identity-policy rail self-contradicts.** All 20 pages spell `rasm.runtime.content_identity` (21 sites — the exact module runtime `[V4]` renames), including `codegen.md:188`'s `_BARE` frozenset DATA row that ships the stale spelling inside every generated C# stub preamble. Two camps call `ContentIdentity.of` incompatibly: ten pages rest on the `CANONICAL_POLICY` default and name a fresh `IdentityPolicy()` "rejected by the runtime content owner" (`study.md:496`, `statistics.md:264`, `array.md:23`, `quantity.md:482`), while six pass the explicit allocation (`symbolic.md:766`, `history.md:289`, `design.md:507`, `program.md:257`, `field.md:343`, `mesh.md:281`). The smoking gun: `history.md:289` keys the IDENTICAL `("study", design.tobytes())` that `study.md:497` keys without the policy, `history.md:286` asserts the two key identically while `study.md:496` asserts they cannot — if the assertion is false every resume misses the cache and silently falls to `Fresh`, and `CANONICAL_POLICY` is named on NO runtime surface (resolved by this brief's applied `[V7]` waterfall pin).
4. **The concurrency story is illusory and one solver cycle hides under a guard.** Eight pages claim "worker lane" execution; `offload(` occurs ZERO times corpus-wide — every JAX x64 solve, scipy factorization, PyMC sample, cvxpy conic solve, and ONNX session runs synchronously on the event loop, and the JAX carriers pin `jax.config.update("jax_enable_x64", True)`, a process-global flag concurrent in-process solves corrupt (the geometry-E5 dead-lane defect at compute scale). Beside it, `solvers/quadrature.md:57-58` imports mesh's `AssembledSystem` under `TYPE_CHECKING` while `mesh.md:48` imports quadrature's `ElementKind`/`FemForm` at runtime — a guarded cycle-dodge (the data-E2 anti-pattern) rooted in one FEM element vocabulary fractured across two files.

What IS sound: the table-driven dispatch discipline corpus-wide (`_STATUS`/`_QUAD`/`_INTERP`/`_SOLVER`/`_LEVY`/`_JIT_ROUTES`/`_STAT_ROUTES`/`_CONE_KKT`/`SALIB_ROUTES`/`FOURIER_ROUTES`/`Umath` — the next case is one row everywhere), the runtime FIT (`boundary`/`railed`/`traversed`/`Receipt.of`/`@receipted` consumed by symbol, every content key through `ContentIdentity.of` over canonical contiguous buffers, no re-mint, no canonical-writer mirror — the count-prefix law consumed transitively), the internal import graph acyclic modulo the one dodge, exemplary collapses (sensitivity's `DiffSpec`, quadrature's single-row catalogs, convex's per-cone KKT, statistics' two-level routes, codegen's three-interpreter catamorphism), and two deliberate boundaries held clean (statistics graduation-free by charter, sensitivity/study DGSM disjoint). The rot is hub direction, dead axes, unstated graph, and governance blindness — no per-page cold pass fixes it. Hub and graph first.

### [TELOS]

`compute` is the terminal evidence plane of the Python branch: array admission, one route-discriminated solver surface, differentiation, validated numerics, unit-bearing uncertainty, signal/symbolic/spatial analysis, design-of-experiments, Bayesian inference, model assets, and the ONE graduation hub through which every Python-branch result — compute-own and geometry-minted alike — crosses to the C# `Rasm.Compute` managed owner. World-class here means the COMPOSED hub: evidence is produced under receipts, admitted against ceilings, keyed over canonical bytes through one policy convention, decoded from the geometry carrier without re-shaping, and emitted on the unified wire with stub codegen closing the C# loop — every axis case backed by a live producer, every heavy solve off the event loop, every claimed seam a fence. Assume 5x the consumer demand on every surface: the C# graduation gate reads `SolverReceipt` verdicts, `Rasm.Compute` decodes `EvidenceBundle` stubs, geometry ships thirteen subject literals the hub must admit, data frames arrive as study inputs, artifacts renders study/posterior evidence — zero current consumers never lowers the bar. The output bar is the composed graduation chain closed in BOTH directions: outward, a study runs offloaded and content-keyed, resumes from history under provably-equal keys, graduates through the hub beside decoded geometry evidence, and crosses as the fence-pinned self-describing receipt floor the C# graduation gate decodes; inward, the C#-minted `EvidenceBundle` decodes through codegen into importable Python stubs downstream compute types against — every step railed, traced, receipted, no hand-off resting on unstated glue.

### [STRUCTURAL_AUTHORITY]

The campaign owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — the `handoff.md` hub rebuild in place, the FEM element-axis re-homing onto `solvers/mesh.md`, the evidence-weave and scope-table owner sited on `graduation/handoff.md` — are the floor, not the ceiling; a NEW `solvers/element.md` leaf carries the burden of proving mesh's ownership breaks, and no other new page is demanded. Every structural change updates `ARCHITECTURE.md` `[01]`+`[02]`, the `README.md` router and package groups, and BOTH `.api` tiers in the same motion. No page survives on incumbency, and no catalog survives on authorship: three sampler catalogs for string-only backends are pressure to reduce, never evidence of capability.

### [IMPORT_DAG_LAW]

The realized package is `rasm.compute.<sub>.<leaf>`; the corrected codemap names the 26-module set and the import graph honors one topological order — a fence import binds only a strictly-earlier module, braced peers are mutually import-free, and an edge into a later tier is a defect resolved by re-homing or re-tiering, never dodged with `TYPE_CHECKING` guards or function-local imports whose purpose is cycle evasion:

```text
graduation/handoff < {solvers/receipt, graduation/codegen} < numerics/array
    < {solvers/linear, solvers/mesh, numerics/{interval, jit, quantity, statistics}, analysis/transform}
    < {solvers/{quadrature, nonlinear, differential, sensitivity}, analysis/{signal, spatial, symbolic},
       optimization/{convex, design}, experiments/{study, inference, model}}
    < {solvers/field, optimization/program, experiments/history}
```

The chain is the canonical topological ORDER, not an edge list. `handoff` is tier-0 — the hub plus the `[V6]` evidence weave every producer composes, importing only runtime; `receipt` imports handoff (the `solver`-axis graduation projection, `[V2]`); `codegen` imports handoff ONLY (the offline-wire decode plus the `[V6]` weave) and braces with receipt — its former tier-4 siting licensed imports its fences never bind and leg 1 forbids; `array` imports handoff (the `array_layout` crossing). One re-homing makes the solver plane hold: the FEM element vocabulary (`ElementKind`/`FemForm`, today `quadrature`-declared) moves to `solvers/mesh` beside `_CTOR`/`AssembledSystem` — mesh constructs elements and owns assembly, so `quadrature` imports mesh downward for its FEM condense-solve arm, `mesh.md:48`'s back-import dies, `quadrature.md:57-58`'s `TYPE_CHECKING` dodge dies, and `field` reads the whole element axis from mesh. The standing legal edges stay (`history -> study`, `program -> design`, `signal -> transform`, `field -> {mesh, receipt}`, `quadrature -> linear` for `_sparse_receipt` post-`[V8]` promotion) — `field -> quadrature` DIES with the `[V3]` rewire: `ElementKind` (`field.md:40`) is its only typed cargo and the solved DOF vector arrives as a value. The `[V9]` bridge edges point strictly downward: `symbolic -> jit`, `study -> jit`, `quadrature -> jit` — the lowered-spec vocabulary is jit-minted, so a symbolic-derived spec crosses into a consumer as a value, never a symbolic import. Every rebuilt page proves its prelude against this order; the fail-open acceptance agent re-derives the graph from every fence and FIXES any back-edge in place.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it. The live violation class is duplicate MECHANISM and per-page literals, not enumerated instances: the `RESULTS._name_to_item` enum-verdict inversion + batched `jnp.max` code-reduce is hand-spelled four times (`linear.md:312`, `nonlinear.md:221`, `differential.md:259`, `design.md:308` — 21 occurrences, all over `equinox.Enumeration` subclasses, the pages' own prose cross-acknowledging the sameness); eight pages mint a page-local `_TRACER = trace.get_tracer("compute.<x>")` with inconsistent spelling (`interval.md:402` says `compute.rigor`, not `compute.interval`) and no scope vocabulary exists; `_REDACTION: Final[Redaction] = Redaction(classified=Map.empty())` is declared verbatim on eight pages; the `_SLOTS`-driven receipt fold (`zip(_SLOTS[tag], payload, strict=True)` projecting `.facts`/`.status`) replicates near-verbatim across `SolverReceipt`/`MeshReceipt`/`FieldReceipt`. The test: the next solver route, evidence owner, or scope is one row or one composition — never a re-spelling.

### [SEAM_AND_HUB_LAW]

- ONE graduation direction: geometry MINTS `GeometrySubject` and the `GeometryHandoff` carrier (`RASM-PY-GEOMETRY-BRIEF.md` `[V2]`, frozen with compute the named demanding consumer); compute's `HandoffAxis.geometry` case DECODES the carrier at the seam — the frozen geometry-owned kind-literal union, the `graduates(owner, HandoffAxis(geometry=subject), key, measured, ceiling)` admission shape, the residual-over-ceiling `_admit` direction — as wire DATA with zero `rasm.geometry` imports and zero re-declared vocabulary. Compute-own evidence (solver/symbolic/model_asset/array_layout/unit_law/uncertainty_law/convex_program) EGRESSES outward to C# through the same hub; every live axis case has exactly one self-wired producer pattern (`[V2]`), and a case with no producer dies rather than standing as prose.
- ONE identity convention: every content key rides `rasm.runtime.identity` `ContentIdentity.of` over canonical contiguous bytes under the `CANONICAL_POLICY` default (the applied runtime `[V7]` pin) — an explicit `IdentityPolicy()` allocation is a deleted form; buffer/stream modalities ride `IdentitySource.lift`; the count-prefix canonical-bytes law (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` item 1) is consumed transitively through the runtime writer — compute authors no canonical-writer mirror, and `PY_WIRE_ALIGNMENT` is its own campaign with compute downstream.
- ONE offline wire ruling: `EvidenceBundle`/`OwnerDescriptor`/`FieldNode` (`codegen` decode, `ARCH:47`) is the C#-minted OFFLINE graduation-evidence wire — msgspec json/msgpack bytes at rest, never the UDS gRPC leg — so it stays compute-owned and enters NO runtime `transport/shapes` registry row; the drift gate is the carried `schema_version` plus a C#-minted golden bundle admitted to the runtime reproduction corpus as a `CorpusFixture` (the applied `[V7]` waterfall) — the fixture pairs the bundle bytes with the expected `GeneratedModule` projection so the gate proves decode AND emit round-trip byte-stability under the deterministic encoder, and a `schema_version` the decoder does not carry rails as a typed fault band, never a best-effort decode. Deciding criterion for the registry arm: only if the crossing moves onto the gRPC channel does the shape become a demanded `shapes` row through runtime `[V2]`.
- The `ARCHITECTURE.md` seam ledger is enforceable law: every intra-compute import appears as a ledger edge (the ledger is currently blind to all ~24), every cross-`libs/` row names the real owner and direction (`ARCH:51` mis-homes `ContentIdentity` on `runtime/transport` with a false `⇄`; `ARCH:49`/`ARCH:50` collapse to the one geometry/graduation decode row and `ARCH:52`'s projection dies per `[V1]`), and every declared row has a composing fence (`ARCH:53,54` are declared-unwired until `[V10]` lands them).

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as minted data and is only decoded here — the geometry-minted `GeometryHandoff` carrier (subject, evidence key, measured facts, ceilings — the frozen geometry `[V2]` contract, crossing as content-keyed receipt data, never an import), the C#-minted `EvidenceBundle` offline wire (`codegen` decode under the schema-version + golden-fixture gate), data-gated DOE frames and labelled arrays on the study/array admission arms (`[V10]`), the `xarray.DataTree` posterior carrier (`inference.md:45`), and the prior `StudyReceipt` cohort + response cache `history` resumes over (`history.md:9` — a `Map[ContentKey, np.ndarray]` in-memory rail): compute owns NO durable run store — cross-session durability is the C# Persistence reuse ledger's — so the resume proof is key equality over caller-supplied evidence, never storage. What is COMPUTED here under a cross-language parity obligation, never a re-mint — every `ContentKey` over canonical bytes through runtime `ContentIdentity` under `CANONICAL_POLICY`, the `ARRAY_LAYOUT` cross-backend bit-identity proven against the runtime reproduction corpus, and the emitted stub preambles whose rendered names and imports derive from one `_BARE` data row. Solve receipts, study designs, posterior summaries, KKT certificates, and graduation receipts are companion-local evidence, minted freely.

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. Carried obligations, all pre-ruled by the data campaign and executed or verified here: `sparse` retags `[DATA]` -> `[COMPUTE]` (`pyproject.toml:118`; `numerics/array.md:33` is the live consumer; the `python_version<'3.15'` marker stands); `netcdf4`'s `[COMPUTE]` tag (`:62`) dies for `[DATA]` (zero compute consumers) — compute verifies the data-campaign retag landed and drops any implied claim; `dask` (`:50`, untagged) relocates its sole catalog from `compute/.api/dask.md` to branch-tier `libs/python/.api/dask.md` with data and compute both on the owner line (the data `[V12]` catalog re-homing ripple this campaign executes — the compute-tier file strikes), and the manifest row gains the dual-consumer tag comment the branch owner line mirrors; `sympy` (`:119`, untagged) gains `[COMPUTE]`; `meshio` (`:59`), `pint` (`:75`), and `uncertainties` (`:121`) — untagged with `- owner: compute` stubs standing — gain their consumer tags in the same motion (`meshio`'s names compute beside data ONLY — the geometry campaign retags meshio `[DATA]` with zero geometry imports and strikes its geometry-tier catalog, so geometry never enters the tag; `pint`/`uncertainties` take `[COMPUTE]` sole); `rustworkx` (`:85`) sheds the `[COMPUTE_DATA]` tag's compute half — zero compute fences, no compute-tier stub, the data `graph.md` owner real (data brief `[04]`) — the tag rewriting to `[DATA_ARTIFACTS]` (artifacts `drawing/detail` is a live rustworkx fence, `libs/python/artifacts/.planning/drawing/detail.md:53-55`, with its own `artifacts/.api/rustworkx.md` catalog), re-opened for compute only by a live spatial/mesh graph-route consumer landing in the build, never by preference; `universal-pathlib` (`:32`) carries a `[DATA]`-only comment against a runtime-owned branch stub and a live compute consumer (`model.md:36`) — the comment gains the consumer set the stub owner line mirrors; `narwhals` (`:61`, `[DATA]`-only) gains compute in the same motion — the `[V10]` DOE-frame arm lands compute's first `narwhals` import as the study-admission frame lens, and the data-tier stub's owner line mirrors the consumer set. The `numpyro`/`blackjax`/`nutpie` admissions STAND — PyMC's `nuts_sampler` dispatch resolves them at run time, so installed-never-imported is the admission's nature, recorded on each reduced stub (`[V14]`); `blackjax`'s row survives only as that string backend — its raw-kernel charter is a sealed decline (the kernel/SMC/SGMCMC/VI surfaces stay closed, never re-litigated). `findiff` STANDS narrowed — `sensitivity.md:477`'s `coefficients` arbitrary-accuracy stencils are capability `numpy.gradient` lacks; the catalog narrows to that floor (`[V14]`). ONE roster addition: `jaxtyping` (MIT, pure-Python, actively released, already resolved in `uv.lock` as the transitive dependency of the admitted `equinox`/`diffrax`/`lineax`/`optimistix` stack) admits explicitly with a `[COMPUTE]` tag — shape/dtype-annotated array contracts (`Float[Array, "..."]`, `PyTree`) on the JAX-gated carriers and process-lane spec payloads, checked through the existing `beartype` fence (`jaxtyped(typechecker=beartype)`), the `[V4]` integration; first-classing an in-environment capability, never a new supply-chain surface. The categorical-best supersession sweep otherwise returns no strikes and no holes — the one candidate gap (second-order error propagation, `mcerp`/`soerp`) is a sealed decline in favor of the `uncertainties` first-order owner plus the study-sampler Monte-Carlo bridge (both candidates long unmaintained), the sparse-eigen demand is rows on the EXISTING `scipy` catalog, never a package, and the graph-algorithm axis has no compute consumer to serve (the `rustworkx` shed above). `scipy-stubs` rides the `scipy` row as its typing companion — no separate catalog, no tag motion. Paid/gated/proprietary stays rejected.

## [01]-[PHASE_REBUILD]

The rebuild engine runs against this brief directly. Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; the build may exceed a floor, never dilute it; a hedge names its deciding criteria and the implementing agent resolves it with disk evidence, never by preference.

### [V1_GRADUATION_HUB_INVERSION]

`graduation/handoff.md` flips from vocabulary-owner to the multi-domain HUB — the campaign's defining ruling and every other verdict subordinates to it. Ruled: (a) the `GeometrySubject` mint (`handoff.md:61-70`) DIES — the `geometry` case carries the geometry-minted subject as decoded wire data, typed against the frozen geometry-owned kind-literal union `rasm.geometry.graduation` pins (8 standing + `bim-compliance`/`bim-lifecycle`/`section-property`/`building-energy`/`thermal-comfort`), stated in the fence as the inherited contract block, never a compute-authored `Literal` source of truth and never a `rasm.geometry` import; a geometry union change is a geometry ripple, not a compute edit. (b) The producer framing inverts: `handoff.md:21`'s "compute mints the case ... geometry consumes" clause and `:199`'s owner-by-owner consumer recital rewrite decode-side — geometry owners PRODUCE `GeometryHandoff` carriers, the hub admits them through the same `boundary(_admit).bind(_clear).bind(boundary(_emit))` rail its compute-own axes ride, and re-emits on the unified C# wire. (c) The ledger retypes and collapses: `ARCH:49` (`← python:geometry/ifc`) and `ARCH:50` (`⇄ python:geometry/graph`) collapse to ONE `graduation/handoff ← python:geometry/graduation` decode row — geometry `[V2]` sites the carrier mint on `rasm.geometry.graduation`, so that owner is the seam counterpart, and a row naming a geometry interior plane recites a sibling interior while under-counting the 13-subject producer set (scan/graph/ifc/energy); the wildcard `ARCH:46` `* ⇄ csharp:Rasm.Compute` narrows to `→` (compute graduates outward; C# never imports back); `ARCH:47`'s `←` offline-wire decode and `ARCH:48`'s `→` verdict projection stand. (d) `analysis/spatial`'s geometry-case emit DIES with the inversion: `spatial.md:14`'s "graduates outward on the geometry case aligned to the scan companion" claim and the `ARCH:52` projection row are struck — geometry's own reconstruction plane mints `reconstructed-mesh`, geometry imports only runtime/data/itself, and a compute-emitted geometry subject would re-mint the vocabulary this verdict just decoded; the alpha-shape `Boundary` evidence stays a compute-native receipt product (`SpatialEvidence.facts()` unchanged), and re-graduation requires a named consumer plus a compute-owned axis case, never the geometry case. (e) Both crossing halves are FENCES, not framing: the geometry case gains the ONE carrier-decode ingress — a fenced msgspec one-shot admission of the `GeometryHandoff` payload (bytes or builtins — channel-agnostic by construction, the data `[V9]` precedent: in-process composition and a future wire leg admit through the one fence, no carrier-transport ruling owed) against the inherited contract block, an out-of-union subject railing as `Error(BoundaryFault)` on the `("graduation.geometry", "unknown-subject")` band (never an unfenced `ValidationError`, never a silent admit — the drift signal a geometry union extension trips until the compute ripple lands), the decoded `(subject, key, measured, ceiling)` feeding the same two-stage rail; and the outward crossing pins its contract — the `planned` receipt's fact floor (`axis` tag, resolved subject, `evidence_key` hex render, the cleared residual ledger, phase) is fence-pinned SELF-DESCRIBING as the C# graduation gate's decode vocabulary, the data assessment-wire discipline at graduation scale: a free-form facts map the gate can neither attribute nor dedupe is a chain break, never a doc gap. The two-stage admission rail, finiteness refinements, and residual-over-ceiling `_clear` fold are the preserved core — this is a re-siting of AUTHORITY, not of machinery.

### [V2_GRADUATION_WIRING_UNIFICATION]

One producer pattern: every live `HandoffAxis` case has a self-wired `graduates()` producer importing the hub downward — the pattern `inference`/`model`/`quantity` already hold — and a case with no producer dies. Ruled per case: `solver` — `solvers/receipt` hosts the one solver-axis graduation projection (a `graduates`-composing fold beside the `_STATUS`/`_SLOTS` tables it already owns), and the solve owners (`linear`/`nonlinear`/`differential`/`quadrature` via receipt; `design`/`program` via their `OutcomeReceipt`; `interval` via its `Certificate`) feed it rather than each inlining an admission — feeding is the owner CALLING the projection with the `(ledger, ceiling, key)` triple projected from its own receipt, importing receipt downward: the projection is parameterized over that triple and receipt imports neither `OutcomeReceipt` nor `Certificate` nor any downstream type; `convex_program` — `convex` self-wires the `graduates()` its Packages block already claims (`convex.md:19-20` names the hub with no prelude import — the prose-vs-fence split closes fence-side); `symbolic` — `symbolic` self-wires under its own stability law (a derivation graduates only when `Precision.CERTIFIED` or reproducibly `HEURISTIC` — the page's stated bar becomes the admission ceiling); `array_layout` — `array` self-wires over the cross-backend bit-identity proof riding the runtime reproduction `ParityReceipt` (`[V12]`; the standing array-layout graduation obligation realizes here); `geometry` — decode-only per `[V1]`; `model_asset`/`unit_law`/`uncertainty_law` hold as landed. `statistics` stays deliberately graduation-free (its own `:16` boundary) and `sensitivity` stays disjoint from study DGSM — both are preserved law, never gaps. Ceiling law rides the pattern: every graduating family's DEFAULT ceiling is a governed policy row on that family's own policy carrier beside its route table (the `[V4]` modality-row siting; the hub's caller-supplied tighter row stays the override), so the admission bar is seed data with one owner per family — an ad-hoc ceiling literal at a `graduates()` call site is a deleted form. The axis roster is additionally extensible by sibling campaign, never by silent admit: `RASM-PY-ARTIFACTS-BRIEF.md` holds an outward figure-handoff crossing gated on the very hub this verdict realizes — standing consumer pressure for an artifacts-origin axis case, admitted only when the artifacts campaign ships the case AND its self-wired producer in one motion (the geometry-ripple discipline at axis scale); until then the roster closes at the ruled set, and the named blocker condition is the artifacts producer landing, never a compute-side pre-mint.

### [V3_ELEMENT_AXIS_AND_SOLVER_GRAPH]

The FEM element vocabulary unifies on `solvers/mesh` per `[IMPORT_DAG_LAW]`: `ElementKind`/`FemForm` move from `quadrature` to mesh beside `_CTOR`/`AssembledSystem` (one element axis, one owner — mesh constructs elements, assembles systems, and owns the meshio interchange); `quadrature`'s FEM condense-solve arm imports mesh downward at module top and its `TYPE_CHECKING` guard (`quadrature.md:57-58`) dies; `mesh.md:48`'s quadrature import dies; `field` reads `ElementKind` from mesh (`field.md:39-41` rewires). Deleted forms: any `TYPE_CHECKING` import of a runtime-constructed symbol, any function-local import whose purpose is cycle evasion, any second element vocabulary. A standalone `solvers/element.md` leaf fires only if the rebuilt mesh page exceeds ~450 LOC with the axis aboard — the deciding criterion, resolved against the rebuilt page, never preference.

### [V4_WORKER_LANE_REALITY]

The eight "worker lane" prose claims become composed fences or die — ruled COMPOSE: the heavy entries (`linear`/`nonlinear`/`differential`/`quadrature` solves, `design`/`program`/`convex` optimizations, `study` evaluation sweeps, `inference` sampling, `model` sessions, the scan-scale `spatial` queries) become `async def` composing `lane.offload(...)` on the runtime-exported isolation-modality axis (the applied runtime `[V5]` waterfall pin): JAX-gated carriers pin the PROCESS modality — `jax.config.update("jax_enable_x64", True)` is process-global, so concurrent in-process solves corrupt the flag and the isolation IS the correctness fix, not a tuning knob; the interpreter modality is STRUCK for the numeric band because the jaxlib/numpy native stack does not load under isolated subinterpreters, so a JAX family row naming `interpreter` is a deleted form — while scipy factorizations, PyMC sampling, cvxpy conic solves, and ONNX sessions ride the THREAD/process band under the runtime-owned bounds; compute mints zero `CapacityLimiter`s. Modality is a policy ROW per route family, homed on that family's own policy carrier beside its route table — never a per-page literal and never a compute-central lane registry: the runtime owns the axis and its bounds, compute owns only the per-family modality choice. The crossing law rides the modality: a process-lane payload is spec DATA plus a module-level kernel — the declarative intent/policy `Struct`s and array operands cross, the worker resolves the kernel by import, and the module-level x64 gate applies at worker import (`differential.md:237`, `field.md:209` are the standing gates); the crossing operands and the gated carriers' array contracts go shape-typed in the same motion — `jaxtyping` `Float[Array, "..."]`/`PyTree` annotations checked through the existing `beartype(conf=FAULT_CONF)` fence (`jaxtyped(typechecker=beartype)`, the `[00]` admission), so a rank/dtype breach rails at the boundary beside the finiteness refinement rather than surfacing as a mid-solve XLA shape error, and a bare `object`/untyped-`Array` operand annotation on a JAX-gated public entry is a deleted form — and a closure-bearing carrier offloads on the process lane only when its callables are module-qualified importables: `study.md:62`'s `Objective` wraps caller callables, so the study entry resolves its lane by payload shape (module-level objective -> process, closure -> THREAD band) and a lambda objective on the process lane is a deleted form, never a runtime pickle crash. Retry law rides the same motion: compute's solves are deterministic, so a `guarded`/`stamina` wrap over them is a deleted form (the geometry `_FE_RETRY` precedent) and compute adds NO `POLICY` row; the only admissible retry surface is a genuinely flaky external oracle, and none currently exists in the folder. Worker DEATH is not a solve fault: a process-lane broker crash rides `offload(retry=RetryClass.OCCT)` — the runtime row's landed target spans the worker-death band (`anyio.BrokenWorkerProcess`, `resilience.md:254`), and the retry wraps the isolation leg, never the deterministic kernel, so the no-solve-retry law holds.

### [V5_DISPATCH_RAIL_UNIFICATION]

`expression.Map` (`Map.of_seq`) is THE dispatch-table rail — the doctrine's own immutable-lookup owner (`docs/stacks/python/rails-and-effects.md:17`) and the rail runtime `[V8]`/data `[V2]` already forced on the siblings. The three-way bifurcation dies: every `beartype.FrozenDict` table (117 occurrences, 53 of them type-position declarations, across 15 solver/numerics/optimization pages — `receipt.md:28,64-71`, convex's tables, field's) re-types `Final[Map[...]]`, codegen's plain-`dict` `_SCALAR`/`_DECODER` tables join the same rail, and the analysis/experiments `Map` camp becomes the folder convention it already models. `FrozenDict` is real but off-label (a beartype `hint_overrides` helper, not a table substrate), and every page already imports `Map` through `Redaction` — the split is gratuitous. Acceptance: `rg 'FrozenDict' libs/python/compute/.planning` returns zero table declarations; deleted-form prose names the anti-pattern by the `Map`-vs-frozendict contrast.

### [V6_EVIDENCE_WEAVE_AND_SCOPE]

`graduation/handoff.md` additionally owns the shared evidence weave — compute's analogue of geometry `[V5]`. One `evidence_run(scope, subject, dispatch)` fold composes runtime `boundary` + the span + `@receipted`; the eight page-local `_TRACER` mints (`history.md:215`, `inference.md:311`, `model.md:66`, `study.md:366`, `codegen.md:167`, `handoff.md:93`, `interval.md:402`, `statistics.md:213`) and their inline `start_as_current_span` opens collapse onto it; the scope literals become one seed table on the hub (the runtime `[V8]` scope-vocabulary law at compute scale — `compute.rigor`-vs-`compute.interval` inconsistency dies with the literals), and the seed rows derive from the module-leaf vocabulary — `compute.<leaf>` — so a scope spelling disagreeing with its owning module name is a deleted form, never a per-page naming choice; the eight verbatim `_REDACTION` declarations collapse to one hub-exported constant. The span-bearing-vs-span-free emission split (some producers weave spans, others ride bare `@receipted`) unifies on the weave — one owner, one instrumentation shape, a new evidence owner is one scope row. Composing the weave is an observability import, never a graduation admission: statistics' graduation-free charter bans `graduates()` calls, not the hub import, so the graduation-free owners ride the same weave without breaching their preserved boundary.

### [V7_IDENTITY_POLICY_CONVENTION]

`CANONICAL_POLICY` is ruled canonical — the applied runtime `[V7]` waterfall pins it as an exported identity surface with the bytes-law key equality. The six explicit-allocation sites rewire to the default (`symbolic.md:766`, `history.md:289`, `design.md:507`, `program.md:257`, `field.md:343`, `mesh.md:281` — the exact deleted form four sibling pages' prose already names); `history.md:286`'s equality assertion becomes provable law instead of an unproven assumption, so the design-key resume cache (`Map[ContentKey, np.ndarray]`) hits by construction; `study`'s receipt already carries the reuse key — `StudyReceipt.content_key` (`study.md:409`) spreads `content_key.hex` into its facts (`:435`) — the rebuild HOLDS that row, and the resume-equality proof is what makes it load-bearing downstream. Acceptance: `rg 'IdentityPolicy\(\)' libs/python/compute/.planning` returns zero call-site allocations.

### [V8_VERDICT_FOLD_AND_PUBLIC_CONTRACTS]

The four-way enum-verdict mechanism collapses: ONE shared fold parameterized by the `equinox.Enumeration` `RESULTS` class — the `_name_to_item` inversion + batched `jnp.max` code-reduce — hosted on `solvers/receipt` beside the `_STATUS` table its output feeds; the fold takes the gated module handle AND the `RESULTS` class as parameters, so receipt imports neither `jax` nor `equinox` at module top — every solver page, JAX-gated or not, imports receipt, and the x64-gated carrier stays the folder's sole JAX-importing owner; `LinearEngine.verdict` (`linear.md:314`), `NonlinearEngine.verdict` (`nonlinear.md:223`), `SolveEngine.verdict` (`differential.md:254`), and `_result_names` (`design.md:307`) become one-row compositions. The private-but-shared contracts go honest in the same motion: `_status` (cross-imported by mesh/field/design), `_sparse_receipt` (quadrature <- linear), and the element-axis names promote to public surface on their owners or the cross-module reuse folds into the owning entry — a public contract masquerading as private is a deleted form. `linear.md:181`'s reflective `getattr(self, self.tag)` structure read — the exact form receipt's own doctrine names deleted — becomes a total `match`.

### [V9_JIT_AND_LOWERING_BRIDGE]

`numerics/jit.md` (298 LOC, ZERO importing fences — prose references only, `nonlinear.md:5`'s boundary disclaimer, `inference.md:21`, `quantity.md:482`) is ruled WIRED, not deleted: `study`'s `MeasurementMode` batch lane composes `JitBackend.compile` for loop-kernel objectives (the numba njit row) — the "solve/study spine harvests the `Jitted` stream" its receipt prose already describes becomes a fence — and `symbolic`'s discarded `_lower` callable (`symbolic.md:431` materializes then drops it; the "cross-file lowering seam" is currently unwired) gains its consumer: the lowering bridge emits the lowered-callable spec `jit` compiles and the study/quadrature consumers harvest, realizing the symbolic->jit->consumer lowering chain — DAG-lawful by construction: the spec vocabulary is jit-MINTED (a `Cfunc` row beside the standing `Njit`/`Vectorize`/`JaxJit` rows), `symbolic` imports jit downward to type its emission, and `study`/`quadrature` import jit ONLY, a symbolic-derived spec reaching them as a value — `symbolic`/`study`/`quadrature` are braced peers in the `[00]` order, so a consumer importing symbolic is a back-edge, the deleted form; `jit` stays in the first braced tier. Deciding criteria for the charter-down arm: only if the build proves no compute entry legitimately owns a Python-level loop kernel (JAX routes jit internally through their libraries) — then jit's charter narrows to the numba-vectorize lane its live capability actually serves, stated honestly, and the page absorbs into its one consumer.

### [V10_DATA_SEAM_REALIZATION]

Both declared data seams become fences — pre-agreed by the data campaign (`xarray` held branch-tier BECAUSE compute co-consumes). `experiments/study` gains the DOE-frame admission arm: one polymorphic entry discriminating by input shape — the existing `Objective`+axes modality AND a contract-gated data frame (narwhals-typed, decoded to the design matrix + `AxisDist` rows at the boundary) — never a second entry; `numerics/array` gains the labelled-array arm: `ArraySource` admits `xarray.Dataset`/`DataArray`, extracting `.data` + coords into `NamedAxis` rows (the `ARCH:54` row's own claim). Both arms decode at the boundary and import no data interior beyond the published surfaces; the `ARCH` seam rows re-align to the post-data-rebuild page set in the same motion. The render seam is the third fence: the study/posterior evidence artifacts renders (`RASM-PY-ARTIFACTS-BRIEF.md` `[V10]`, the named demanding consumer) crosses as a self-describing tabular frame — one frame projection on the experiments receipts (study cohort, posterior summary) whose columns carry source, unit, identifier, and content key, the physical crossing content-keyed Arrow bytes through the data `columnar` public fold (the geometry `[V1]` precedent), never an artifacts-side re-derivation.

### [V11_PAGE_GRAMMAR]

The `[RESEARCH]` appendix is BANNED folder-wide — the runtime `[V11]`/data `[V11]`/geometry `[V10]` precedent at the track's largest scale: 25 tails die (every page but `model.md`, the standing grammar exemplar; `quadrature.md:572` and `codegen.md:390` are EMPTY headers). The ruling is purge-and-fold: load-bearing member/arity confirmations fold into the owning `Packages` block or the `.api` catalog; "confirmed against the live distribution" framing, version narration, and empty headers delete; UNSETTLED items convert to in-body gated obligations. Invariant-once executes with it: transform's linear-amplitude contract (restated 4x — `transform.md:3,80-87,279,342`), the `FiniteGate` exclusion recital, and the ~6x hand-restated span-then-fence-then-receipt weave prose each state once in their owning block; index/body numbering aligns (`handoff.md:15` `[03]` vs `:201` `[04]`, `codegen.md:7` vs body). This is the folder's largest clean prose reduction and lands per-leg, never as a separate pass.

### [V12_UPSTREAM_REBIND]

The mechanical corpus-wide sweeps — each rename lands per-page IN-PASS as the owning page rebuilds (leg-1 pages first, so later legs inherit clean spellings; the fail-open acceptance sweep fixes stragglers in place): (a) `rasm.runtime.content_identity` -> `rasm.runtime.identity` at all 21 sites on 20 pages INCLUDING the `codegen.md:188` `_BARE` frozenset row — a data row that ships the stale spelling in every generated stub preamble, the rename's one non-import site; (b) the reproduction rebind — `array`'s cross-backend bit-identity proof rides runtime `evidence/reproduction` `ParityReceipt`/`CorpusFixture` (the applied `[V7]` pin), and the C#-minted `EvidenceBundle` golden bundle joins the corpus as codegen's drift fixture per `[SEAM_AND_HUB_LAW]`; (c) the `ARCH:51` seam truth — `numerics/array ⇄ python:runtime/transport` re-homes to `← python:runtime/evidence` (identity is evidence, not transport; array decodes, never re-mints). Acceptance: `rg 'rasm.runtime.content_identity' libs/python/compute/.planning` returns zero.

### [V13_GOVERNANCE_TRUTH]

All three governance surfaces — codemap, seam ledger, README router/rosters — become exact. The `ARCHITECTURE.md` codemap gains its two orphan nodes — `solvers/field.py` and `numerics/jit.py` exist on disk (351 + 298 LOC) yet appear in neither the codemap (`ARCH:11-18` omits field, `:28-32` omits jit) nor the router; the seam ledger declares every intra-compute edge as `->` rows (today ZERO of ~24 — the quadrature⇄mesh cycle was invisible to it), retypes the cross-`libs/` rows per `[V1]`/`[V12]`, and gains the undeclared `experiments/model -> python:runtime/roots` row (`model.md:42` `ResourceRef`). The README router grows to 26 rows (`FIELD`/`JIT` enter); `[02]-[DOMAIN_PACKAGES]` gains the three consumed-but-absent rows (`findiff`/`quadax`/`interpax`) plus the admitted `jaxtyping` (`[00]`) with the leg that lands its first fence; `[03]-[SUBSTRATE_PACKAGES]` strikes never-consumed `pydantic` (`README.md:108`) and `anyio` (`:111` — concurrency arrives through runtime lanes, never direct) and gains consumed `xarray` and `universal-pathlib`. The carried growth obligations land with their owning verdicts, never as a parallel ledger: the `array_layout` graduation via `[V2]`/`[V12]`, the data admission arms via `[V10]`, the seed-provenance vocabulary as policy rows on the owning numerics pages with the numerics leg (numpy `Generator` and jax `PRNGKey` algebras reconciled as data, no new page), the symbolic->jit lowering bridge via `[V9]`, and the sparse-eigen demand's catalog half via `[V14]` — its route realization one `LinearIntent` row with the linear leg, or the linear page states the decline with its deciding criterion.

### [V14_API_TIER_RECONCILIATION]

The compute `.api` tier becomes lean and truthful. The three sampler catalogs (`numpyro.md`/`blackjax.md`/`nutpie.md`, 381 catalog LOC for packages consumed ONLY as `Literal["numpyro","blackjax","nutpie"]` strings — `inference.md:22,50`, the page itself honest: "never imported here") reduce to string-backend CONTRACT stubs: the `nuts_sampler` name, the per-engine `nuts_sampler_kwargs` keys (`nutpie` `backend='jax'/'numba'`, `numpyro` `chain_method`), and a stated, sealed DECLINE of the kernel/SMC/SGMCMC/VI surfaces — never re-opened by catalog authorship. `findiff.md` narrows to the `coefficients(deriv, acc)` floor `sensitivity.md:7,487` mines — the charter's claimed `Diff`/`.matrix` composition no fence performs dies with the over-catalog. `compute/.api/dask.md` strikes; the branch-tier `libs/python/.api/dask.md` lands with data+compute on the owner line (the data-ruled catalog re-homing this campaign executes). `scipy.md` gains the INVERSE gap: the `sparse.linalg.lobpcg`/shift-invert `OPinv`/`ArpackNoConvergence`/full-`svds` rows the sparse-eigen demand names catalogue-unverified. The admitted `jaxtyping` lands its compute-tier catalog with its first fence — the `Float`/`Int`/`Bool`/`Shaped` array annotations, `Array`/`PyTree`, and the `jaxtyped(typechecker=beartype)` weave, members verified against the live distribution, the charter line naming the `[V4]` gated-carrier consumers. Owner lines: `array-api-compat.md`/`array-api-extra.md` gain `- owner: compute`; `xarray.md`'s branch owner line names both consumers (the data-campaign edit, verified here); `universal-pathlib.md`'s runtime-owned branch stub gains compute on the consumer line (`model.md:36` `UPath`); `narwhals`'s data-tier stub gains compute on its owner line (the `[V10]` DOE-frame lens — the manifest comment motion rides `[00]`); no `rustworkx` compute-tier stub exists or lands — the `[COMPUTE_DATA]` -> `[DATA_ARTIFACTS]` rewrite is the manifest motion, the data- and artifacts-tier catalogs the Python owners. Every surviving stub's charter line names its live consuming fence.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every build leg re-verifies its rows against disk. Anchors are `libs/python/compute/.planning/` relative; `ARCH:`/`README:` anchor the package-root governance files; `pyproject.toml:` anchors the repo root manifest.

| # | Class | Evidence |
|---|---|---|
| E1 | Hub inversion | `handoff.md:61-70` `GeometrySubject` mint; producer framing `:21`, owner-by-owner consumer recital `:199`; stale 8-literal union vs the frozen 13 (geometry `[V2]`/`[V1]`); `ARCH:50` `⇄`, `ARCH:46` wildcard `⇄`, `ARCH:52` compute->geometry projection vs geometry's runtime/data/itself DAG law; the hub's only entry admits a pre-destructured tuple (`handoff.md:104-107`) — no carrier-decode fence exists for the geometry case |
| E2 | Dead axes / prose-vs-fence | `graduates()` + hub import only at `inference`/`model`/`quantity`; `convex.md:19-20` Packages names the hub, prelude imports none; `spatial.md:14` geometry-case claim, no import; `array_layout` producer-less corpus-wide; `solver` producer-less vs recitals `handoff.md:193-199` |
| E3 | Identity rename bomb | 21 `rasm.runtime.content_identity` sites on 20 pages; `codegen.md:188` `_BARE` frozenset DATA row shipping the spelling into generated stub preambles; runtime `[V4]` renames the module |
| E4 | Policy bifurcation | explicit `IdentityPolicy()`: `symbolic.md:766`, `history.md:289`, `design.md:507`, `program.md:257`, `field.md:343`, `mesh.md:281`; default camp + rejection prose: `study.md:496-497`, `statistics.md:264`, `array.md:23`, `quantity.md:482`, `interval.md:33`, `statistics.md:34`; smoking gun — `history.md:289` vs `study.md:497` key identical bytes two ways, `history.md:286` asserts equality `study.md:496` denies; `CANONICAL_POLICY` absent from the pre-edit runtime surface |
| E5 | Illusory concurrency | 8 pages claim "worker lane"; `offload(` = 0 corpus-wide; `jax.config.update("jax_enable_x64", True)` process-global in every JAX carrier (`nonlinear.md:5` et al.); zero compute `POLICY` rows, zero limiters — all solves on-loop; `study.md:62` `Objective` carries bare callable fields (the process-lane pickle hazard `[V4]`'s payload law rules); `inference.md:147` sync `draw` |
| E6 | Solver cycle dodge | `quadrature.md:57-58` `TYPE_CHECKING` import of mesh `AssembledSystem`; `mesh.md:48` runtime import of quadrature `ElementKind`/`FemForm`; `field.md:39-41` reads the axis from both — one element concern across three files |
| E7 | Rail bifurcation | 117 `FrozenDict` occurrences (53 type-position declarations) on 15 pages (`receipt.md:28,64-71`; convex 16 hits; field 12); plain `dict` tables `codegen` `_SCALAR`/`_DECODER` (`codegen.md:66`); `Map.of_seq` camp analysis/experiments; doctrine owner `rails-and-effects.md:17` |
| E8 | Verdict-fold duplication | `_name_to_item` inversion + `jnp.max` reduce x4: `linear.md:314`, `nonlinear.md:223`, `differential.md:254`, `design.md:307` (21 occurrences); prose cross-acknowledges sameness; reflective `getattr(self, self.tag)` `linear.md:181` vs receipt's own deleted-form doctrine |
| E9 | Scope/weave duplication | 8 `_TRACER` mints (`history.md:215`, `inference.md:311`, `model.md:66`, `study.md:366`, `codegen.md:167`, `handoff.md:93`, `interval.md:402` `"compute.rigor"`, `statistics.md:213`); 8 verbatim `_REDACTION` declarations; span-bearing vs span-free emission split; no scope vocabulary |
| E10 | Unwired carriers | `numerics/jit.md` zero importing fences (prose references only — `nonlinear.md:5`, `inference.md:21`, `quantity.md:482`); `symbolic.md:431` `_lower` materializes then discards the callable ("cross-file lowering seam" unwired) |
| E11 | Data seams declared-unwired | `ARCH:53` study <- data/tabular and `ARCH:54` array <- data/tabular vs zero `rasm.data`/xarray-Dataset admission fences — page prose states the study input realized while no fence exists |
| E12 | Ledger/codemap blindness | `ARCH:[02]` declares zero of ~24 intra-compute edges; `ARCH:51` mis-homes identity on `runtime/transport` with false `⇄`; undeclared `model -> runtime/roots` (`model.md:42`); codemap omits `solvers/field.py` (`ARCH:11-18`) and `numerics/jit.py` (`ARCH:28-32`); router 24 of 26 rows |
| E13 | Grammar debt | 25 `[RESEARCH]` tails (all but `model.md`); EMPTY headers `quadrature.md:572`, `codegen.md:390`; transform invariant restated 4x (`transform.md:3,80-87,279,342`); index/body numbering drift `handoff.md:15` `[03]` vs `:201` `[04]` |
| E14 | Roster/manifest drift | README `[02]` omits `findiff`/`quadax`/`interpax`; README `[03]` lists unconsumed `pydantic` (`README:108`)/`anyio` (`:111`), omits `xarray`/`universal-pathlib`; `sparse` `[DATA]` (`pyproject.toml:118`) vs live consumer `array.md:33`; `netcdf4` `[COMPUTE]` (`:62`) zero consumers; `dask` (`:50`) untagged; `sympy` (`:119`) untagged; `rustworkx` `[COMPUTE_DATA]` (`:85`) zero compute consumers; `meshio` (`:59`)/`pint` (`:75`)/`uncertainties` (`:121`) untagged over `- owner: compute` stubs; `universal-pathlib` (`:32`) `[DATA]`-tagged against the runtime-owned stub and the `model.md:36` consumer |
| E15 | Catalog rot | `numpyro.md`/`blackjax.md`/`nutpie.md` 381 LOC (159/141/81) for string-only backends (`inference.md:22,50`; `blackjax.md:60` SMC scope — a sealed-decline surface the catalog still authors); `findiff.md` full `Diff` algebra for one live member (`sensitivity.md:7,487`); `compute/.api/dask.md` mislocated (data `[V12]`); `scipy.md` carries `eigsh(sigma)`/basic `svds` (`:99`) but not the `lobpcg`/`OPinv`/`ArpackNoConvergence`/full-`svds` sparse-eigen rows; `array-api-compat.md`/`array-api-extra.md` owner-less |
| E16 | Receipt/contract leaks | private cross-imports `_status` (`mesh.md:49`, `field.md:41`, `design.md:41`), `_sparse_receipt` (`quadrature.md:52`), `_CTOR` (`field.md:39`) — public contracts masquerading private; `study.md:409,435` already carries the `content_key` field + fact the `[V7]` resume proof rides — HOLD it |

Sound surfaces (preserve; cold-pass plus only the deltas a verdict names): `numerics/array.md` (9.5 — the wire-seam masterpiece; gains only the `[V10]` arm + `[V2]` graduation + rename), `analysis/symbolic.md` (9.5 core — FLINT ground-domain routing; gains `[V9]` bridge + `[V2]`), `optimization/convex.md` (9.5 — per-cone KKT engineering; gains its import), `solvers/sensitivity.md` (`DiffSpec` collapse; DGSM boundary held), `solvers/quadrature.md`/`differential.md`/`nonlinear.md` route catalogs, `graduation/codegen.md`'s catamorphism + `_ordered` gray-set guard, `experiments/inference.md` arviz-1.x fidelity + honest never-imported prose, `numerics/quantity.md` fenced consistency verdict, statistics' no-graduation boundary, the corpus-wide `_SLOTS`+`assert_never` receipt discipline, the raise-inside-fence idiom (every raise FENCED), the JAX x64 discipline, and the runtime FIT (single-mint, decode-only, no canonical-writer mirror).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| graduation/handoff | 9 craft / 5 campaign | 10 | decode-hub inversion; carrier-decode fence + unknown-subject fault; pinned self-describing fact floor; frozen-union contract block; one producer pattern; evidence weave + scope table + shared redaction; grammar |
| graduation/codegen | 8 | 9.5 | `_BARE` rename-safe; schema-version fault band + decode/emit round-trip golden fixture; Map rail; empty tail dies |
| solvers (receipt..field) | 8.5-9 | 9.5 | element axis on mesh; dodge dead; shared verdict fold on receipt; public contracts; solver-axis graduation + family ceiling rows; offload composed (PROCESS-pinned JAX, spec-crossing payload); rail |
| optimization | 9 | 9.5 | convex `graduates()` real; design/program feed the solver projection; `_result_names` folds; policy-default keys; offload; rail |
| numerics/array | 9.5 | 10 | xarray admission arm; `array_layout` graduation over `ParityReceipt`; `ARCH:51` truth |
| numerics (interval/jit/quantity/statistics) | 8.5-9.5 | 9.5 | jit wired or narrowed; seed-provenance rows; scope table composed; rail; grammar |
| analysis | 9 | 9.5 | symbolic lowering bridge; spatial seam disposition; invariant-once; rail |
| experiments | 8.5-9.5 | 9.5 | study DOE-frame arm (`content_key` fact HELD, already on disk); history resume-key equality proven over honest in-memory cohort ingress; offloaded sweeps with lane-by-payload-shape study entry; weave-routed spans |
| governance (ARCH/README/`.api`) | drifted | exact | 26-node codemap; full `->` ledger; 26-row router; roster truth; tier motions |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate, the folder tier for domain packages):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Block`/`Map` as the one ADT and dispatch spine (`[V5]` completes it), concurrency ONLY through the runtime lane owner (zero compute limiters, zero direct `anyio` — `[V4]`), retry NEVER over deterministic solves and only through runtime `guarded` at a genuinely flaky oracle (worker-death on the isolation leg rides `offload(retry=RetryClass.OCCT)` per `[V4]`, never a solve retry), `msgspec.Struct` one-shot admission at ingress with the landed `gc=False` scalar-leaf discipline, `beartype(conf=FAULT_CONF)` on public entries with `vale.Is` refinements, one span per fold through the `[V6]` weave — never an inline `start_as_current_span` beside the owned aspects. The corpus already models most of this weave; regressions are defects, not style.
- `numpy` (branch) — the numeric substrate mined to true depth: `ndarray` carriers on every page, `asarray`/`ascontiguousarray` C-order admission feeding every `tobytes()` canonical-bytes key (the `ContentIdentity` buffer law), `linalg` floors, `isfinite` gates, `einsum`/`gradient`/`interp` kernels; the `random.Generator`/`SeedSequence` algebra lands as the seed-provenance policy rows (`[V13]`); the branch stub (`libs/python/.api/numpy.md`) already names compute the rail — a hand-rolled loop over a vectorized numpy owner is a defect, never style.
- `scipy` — the deepest multi-page consume holds (8 importing pages across 9 submodules — optimize, spatial, stats, fft, signal, sparse, integrate, interpolate, linalg — 331 corpus references); the catalog gains the sparse-eigen rows (`lobpcg`, `svds` full surface, shift-invert `OPinv`, `ArpackNoConvergence`) per `[V14]`; the `LinearIntent` route realization is the linear leg's mine-or-decline; `scipy-stubs` rides this row as the typing companion, no separate catalog.
- `sympy`/`python-flint`/`mpmath` — the exemplary FLINT ground-domain tier holds (`_FLINT_MATRIX_ROUTES` subset gating, `flint.good` certified evaluate, arb `real_roots` isolation); `[V9]` wires the lambdify/codegen lowering the `_lower` fold today discards.
- `jax`/`equinox`/`optimistix`/`lineax`/`diffrax`/`optax` — the gated-carrier discipline holds (`LinearEngine`/`NonlinearEngine`/`SolveEngine`/`DiffEngine` x64 contract); the `RESULTS` verdict fold collapses per `[V8]`; the x64 process-global flag is `[V4]`'s isolation proof, never a per-solve toggle.
- `jaxtyping` — the `[00]` admission: `Float`/`Int`/`Bool`/`Shaped` shape/dtype annotations over `Array`/`np.ndarray` operands and `PyTree` structure types on the gated carriers and process-lane spec payloads, runtime-checked through `jaxtyped(typechecker=beartype)` composing the standing `FAULT_CONF` fence (`[V4]`); already resolved transitively via the Kidger stack, so the admission is a manifest tag plus a compute-tier catalog (`[V14]`), never a new wheel.
- `numba` — `jit.md`'s njit/vectorize/guvectorize + `CPUDispatcher` evidence surface gains its consumer per `[V9]` or the charter narrows honestly.
- `cvxpy`/`clarabel` — the DPP `Parameter` warm-re-solve sweep and per-cone dual-certificate recovery hold; the `convex_program` graduation goes live per `[V2]`.
- `pint`/`uncertainties` — held at depth (`Measurement` threading, `correlated_values`, `unumpy`/`ulinalg`, the one `_UREG` application registry); the Monte-Carlo bridge stays routed to the study sampler per the page's own `[MONTE_CARLO_BRIDGE]` law; `mcerp`/`soerp` stay a sealed decline (`[00]`).
- `SALib` — the eight-analyzer `SALIB_ROUTES` collapse holds; study's offload (`[V4]`) and DOE-frame arm (`[V10]`) compose around it, never through it.
- `pymc`/`arviz` — held at depth (arviz-1.x field fidelity, `psense_summary`, `ELPDData`); sampling offloads per `[V4]`; the three NUTS backends stay string-dispatch per `[V14]`.
- `numpyro`/`blackjax`/`nutpie` — contract stubs only: the `nuts_sampler` string + per-engine `nuts_sampler_kwargs` keys; kernel/SMC/SGMCMC/VI surfaces are stated, sealed DECLINES (`[00]`).
- `onnx`/`onnxruntime`/`skl2onnx`/`scikit-learn` — the export/validate/probe weave holds (`PROBE_RANK`, io-name set-equality, dtype-keyed feeds); sessions offload per `[V4]`.
- `scikit-fem`/`meshio` — the assemble/condense/solve and physical-group round-trip surfaces hold; the element axis unifies on mesh per `[V3]`.
- `pywavelets` — the wavedec/swt/cwt/packet multiresolution consume holds on the one `Signal` owner.
- `interpax`/`quadax`/`findiff` — README roster rows land (`[V13]`); `findiff` narrows to the `coefficients` floor (`[V14]`); interpax x64 resample and quadax adaptive/sampled families hold.
- `sparse` — retag `[COMPUTE]` (`pyproject.toml:118`; `array.md:33` the consumer); the folder stub's owner line already correct.
- `array-api-compat`/`array-api-extra` — the resolver + extension stack is the folder spine; both stubs gain `- owner: compute`.
- `xarray` (branch) — `DataTree` co-consume (`inference.md:45`) + the `[V10]` Dataset admission arm; the branch owner line names both consumers.
- `dask` — passive `array_namespace` backend only (`array.md:48,53`); the catalog relocates branch-tier with both consumers named; the compute-tier file strikes (`[V14]`).
- `universal-pathlib` — `model.md:36`'s `UPath` consume enters README `[03]`; the runtime-owned branch stub gains compute on the consumer line and the manifest comment gains the consumer set (`[00]`).
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): array's `FiniteGate` lazy/eager fork + `_host_buffer` residence fold, interval's `_FLOOR_LADDER` + tailrec `_bisect`, statistics' `_STAT_ROUTES`/`_SIGNIFICANCE_CALLS`, history's `CrossStat`-owned kernels, spatial's `NEIGHBOUR_FLOOR` + Chebyshev-centre interior point, codegen's divergence-free `ast.unparse` identity, convex's `_CONE_KKT` closures, quantity's `Umath` arity enum.
- Carried growth obligations, each homed on its owning verdict: the `array_layout` graduation (`[V2]`+`[V12]`), the DOE-frame/labelled-array admission arms (`[V10]`), the seed-provenance policy rows on the numerics leg (`[V13]`), the symbolic->jit lowering bridge (`[V9]`), the sparse-eigen catalog rows + linear-leg mine-or-decline (`[V14]`); the deferred growth themes stay deferred (`[06]`).

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with this brief; the partition is a topological order over the `[IMPORT_DAG_LAW]` graph — every leg's pages import only pages of the same or earlier legs, and each leg's listed sequence is the canonical landing order.

1. **HUB + SUBSTRATE** — `graduation/handoff.md` FIRST (`[V1]` inversion + `[V2]` pattern law + `[V6]` weave/scope/redaction owners), `solvers/receipt.md` (`[V8]` verdict fold + public contracts + the solver-axis graduation projection), `numerics/array.md` (`[V10]` xarray arm + `[V2]` `array_layout` + `[V12]` parity rebind), `graduation/codegen.md` (`[V12]` `_BARE` + drift gate; Map rail). The corpus-wide mechanical sweeps — the identity rename (`[V12]`), the `CANONICAL_POLICY` rewires (`[V7]`), and the dispatch-rail kill (`[V5]`) — land per-page IN-PASS as each owning page rebuilds (this leg's pages first, so later legs inherit clean spellings); the fail-open acceptance sweep fixes stragglers in place.
2. **SOLVERS + NUMERICS** — `solvers/mesh.md` FIRST (`[V3]` element axis lands), `solvers/linear.md` (`[V8]` compose; `match` structure read), `numerics/jit.md` (`[V9]` page shape — the lowered-spec mint lands before its consumers), `solvers/quadrature.md` (dodge dies; imports mesh and jit downward), `solvers/nonlinear.md`, `solvers/differential.md`, `solvers/sensitivity.md`, `numerics/interval.md`, `numerics/quantity.md`, `numerics/statistics.md`, `solvers/field.md` LAST (reads the unified axis); the seed-provenance policy rows land with this leg.
3. **ANALYSIS + OPTIMIZATION + EXPERIMENTS + GOVERNANCE** — `analysis/transform.md`, `analysis/signal.md`, `analysis/spatial.md` (`[V1]`(d) disposition), `analysis/symbolic.md` (`[V9]` bridge; `[V2]` graduation), `optimization/convex.md` (`[V2]`), `optimization/design.md`, `optimization/program.md`, `experiments/study.md` (`[V10]` arm + `[V9]` compose + `content_key` fact), `experiments/inference.md`, `experiments/model.md`, `experiments/history.md` LAST (`[V7]` resume proof); the governance closeout lands through this leg's serialized per-wave tail — `ARCHITECTURE.md` codemap+ledger (`[V13]`), `README.md` router+rosters, the full `[V14]` `.api` motions (sampler reductions, findiff narrow, dask strike+branch authoring, scipy rows, owner lines), and the remaining `pyproject.toml` motions (the `sparse`/`sympy`/`dask`/`meshio`/`pint`/`uncertainties`/`rustworkx`/`universal-pathlib` tag truths; the `netcdf4` retag verified) — the `jaxtyping` admission row and catalog land with the leg that lands its first fence, per the closeout law.

Per-leg closeout obligations: residuals resolve in-run — every pass agent repairs the cross-file ripples its own work exposes in-pass, and the leg's serialized per-wave tail closes index docs and central manifests before it returns; `README.md`/`ARCHITECTURE.md` close in the same leg that changes their facts; the `[V11]` grammar purge applies to every page a leg touches, never as a separate pass; pyproject and `.api` stub motions land with the leg that admits, removes, or relocates the package.

Campaign acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.compute.*` import with zero back-edges and zero `TYPE_CHECKING` cycle-dodges, mechanically re-derived; (b) grep proofs return zero: `rasm.runtime.content_identity`, `[RESEARCH]` headers, `FrozenDict` tables, `IdentityPolicy()` call-site allocations, `type GeometrySubject` under `graduation/`; (c) the hub proof — every `HandoffAxis` case has a live self-wired producer or is gone, the `geometry` case decodes the frozen geometry-owned kind-literal union as data with zero `rasm.geometry` imports through the fenced carrier-decode arm (out-of-union subject -> typed fault), the `planned` fact floor is fence-pinned, and the C# crossing is `→` only; (d) the lane proof — every heavy entry is `async def` composing `lane.offload` with a modality row, JAX routes pinned PROCESS with spec-shaped payloads (no closure crosses the process lane), zero compute-minted limiters, zero inline span opens; (e) the seam proof — the study DOE-frame and array labelled-array arms are fences, `model -> runtime/roots` is ledgered, and `ARCH:51/50/52/46` carry the corrected owners and glyphs; (f) the identity proof — every content key rides the `CANONICAL_POLICY` default, `history`'s resume key provably equals `study`'s design key, and the `ARRAY_LAYOUT` and `EvidenceBundle` decode+emit round-trip fixtures sit in the runtime reproduction corpus; (g) ledger, codemap, router, and manifest agree on one 26-module set and one seam ownership.

## [06]-[OUT_OF_SCOPE]

- The C# seam owners: `Rasm.Compute` (the managed graduation gate, `EvidenceBundle` mint, `SolverReceipt` verdict reader) — compute graduates outward and decodes the offline wire, never re-mints; the `PY_WIRE_ALIGNMENT` canonical-writer campaign is upstream-owned and the count-prefix law is consumed transitively only.
- The runtime interior: the identity rename execution, `CANONICAL_POLICY` export, offload modality implementation, reproduction corpus landing — runtime's own campaign; the applied waterfall edits (`[V5]` compute offload naming, `[V7]` `CANONICAL_POLICY` + the two compute fixtures, the `[04]` `universal-pathlib` consumer line) are the consumed contract.
- The geometry interior: `graduation.md`'s union mint, the `GeometryHandoff` fence, the energy subjects — geometry's own campaign; compute inherits the frozen contract and ships no ripple beyond the `[V1]` decode.
- The data interior: `tabular/contract`/`materialize` shapes, the `xarray`/`dask`/`sparse`/`netcdf4` tier motions — data's own campaign; compute executes only its own side (the dask compute-tier strike, the retag verifications, the admission arms).
- Sibling folder `artifacts` — its pages never change here; cross-folder consequences land as surgical ripple edits on the owning sibling campaign brief, never as compute-side pre-builds.
- Deferred growth themes — the certified interval pipeline, the surrogate-gradient loop, probabilistic graduation, the differentiable-everything rail — genuinely deferred; the hub inversion and wiring law this campaign lands are their prerequisites, not their execution. No blocked work stands in the folder beyond the `[V2]` artifacts-producer gate.
