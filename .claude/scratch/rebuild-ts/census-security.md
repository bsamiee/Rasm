# [CENSUS_SECURITY]

Read-only truthful census of `libs/typescript/security` — its `.planning` pages, `.api` catalogs, `README.md`, `ARCHITECTURE.md`, `IDEAS.md`, `TASKLOG.md`, `project.json`. All nine design pages, all nine `.api` catalogs, and all four governance docs were read in full.

## [A]-[FILE_REGISTER]

| [FILE] | [OWNED_CAPABILITY] | [ENTRY_SURFACES] | [FENCE_MASS] |
| :--- | :--- | :--- | :--- |
| `.planning/crypt/sign.md` (490 lines) | The one crypto authority: argon2id digest-at-rest, HMAC egress signing, opaque tokens, AES-GCM `Shredder` crypto-shredding, jose key admission (RFC 7638 thumbprint), JWT/JWS/JWKS/JWE token authority | `SignFault`, `KeyAlg`, `Material`, `Crypto`, `CredentialVerdict`, `Shredder`, `SealedEnvelope`, `WrappedKey`, `Jwt`, `AccessClaims`, `JwksLedger`, `JwksTransport`, `Keyset`, `Ring`, `KeyHandle` | Heavy — 5 clusters, ~380 fence lines, the folder's largest single owner |
| `.planning/crypt/verify.md` (245 lines) | External-signature ingress: closed dialect table (HMAC/ECDSA/RSA-PKCS1/RSA-PSS) over held request octets, one constant-time verify fold | `VerifyFault`, `Verify`, `Verified`, `PublicKeyStore`, `MacKey`, `PublicKey`, dialect table `_dialects` (github/stripe/hmacHex/hmacBase64/ecdsaPkix/rsaPss/rsaPkcs1/attestation) | Medium — 3 clusters, ~180 fence lines |
| `.planning/crypt/secret.md` (176 lines) | Leased-secret custody over `DopplerSDK` behind `Layer.scoped`: download/get/names, dynamic-lease issue/revoke, auth probe/revoke, fetched-key-to-`Credential` handoff | `SecretFault`, `Secret`, `credential`, `SecretSet`, `LeaseGrant`/`LeaseHandle`/`LeaseSpec` | Medium — 3 clusters, ~130 fence lines |
| `.planning/authn/session.md` (259 lines) | Identity/session spine: `Subject`/`Session`/`CredentialRef`/`TokenPair` vocabulary, rotation with reuse-detection, `SessionStore`/`IdentityJournal` ports, cookie framing + CSRF double-submit | `Subject`, `Session`, `CredentialRef`, `TokenPair`, `SessionFault`, `SessionStore`, `IdentityJournal`, `Token`, `Cookie`, `CookieSpec`, `CsrfFault`, `FramedCookie` | Heavy — 3 clusters, ~230 fence lines |
| `.planning/authn/credential.md` (238 lines) | One digest-at-rest idiom (`Digest`) shared by OTP recovery codes, TOTP/HOTP (`Otp` over otplib v13), and machine API keys (`ApiKey`) | `Digest`, `CredentialFault`, `Otp`, `OtpVerdict`, `RecoverySet`, `ApiKey`, `ApiKeyRecord`, `MintReceipt`, `ApiKeyStore` | Heavy — 3 clusters, ~215 fence lines |
| `.planning/authn/oauth.md` (288 lines) | OAuth 2.0 authorization-code ceremony over arctic: closed provider-row vocabulary (google/github/microsoft/apple/generic), OIDC id_token verify via `Jwt.verifyExternal`, refresh/revoke lifecycle | `Provider`, `OAuthFault`, `OAuthStateStore`, `OAuth` | Heavy — 3 clusters, ~265 fence lines |
| `.planning/authn/webauthn.md` (245 lines) | Both passkey halves as per-runtime subpaths: RP verifier (`./server`, attestation trust + counter replay defense) and browser invocation (`./browser`, capability-gated `navigator.credentials`) | `Passkey`, `WebAuthnFault`, `WebAuthnTrust`, `WebAuthn`, `WebAuthnStore`, `ChallengeStore`, `Passkeys`, `PasskeyFault` | Heavy — 3 clusters, ~225 fence lines, two runtime subpaths |
| `.planning/access/claim.md` (237 lines) | Entitlement vocabulary + RBAC-union-ReBAC evaluation fold under a runtime-owned feature-flag gate | `Role`, `ClaimSet`, `ClaimStore`, `ClaimFault`, `Claim`, `Permission`, `RolePermission`, `Relation`, `RelationTuple`, `PolicyRequest`, `PolicyDecision`, `RelationStore`, `FlagGate`, `Policy`, `PolicyFault` | Heavy — 4 clusters, ~215 fence lines |
| `.planning/access/tenant.md` (60 lines) | The ambient `TenantScope` reference (`Context.Reference`) plus the `app.current_tenant` RLS contract name the data wave enforces | `TenantScope`, `TENANT_GUC`, `Principal` | Light — 2 clusters, ~40 fence lines, deliberately thin (declares a contract, holds no SQL) |
| `README.md` (49 lines) | Folder purpose statement, router to all 9 pages, domain-package roster, substrate-package roster | none (index page) | n/a |
| `ARCHITECTURE.md` (49 lines) | Domain map (codemap), cross-folder seams table, organization narrative, boundary law | none (index page) | n/a |
| `IDEAS.md` (28 lines) | Forward idea pool — both `[OPEN]` and `[CLOSED]` sections are `(none)` | none | n/a |
| `TASKLOG.md` (29 lines) | Open/closed task log — both sections are `(none)` | none | n/a |
| `project.json` (12 lines) | Nx project descriptor: `scope:security`, `runtime:neutral`, `plane:runtime` | none | n/a |

## [B]-[API_ROSTER]

| [CATALOG_FILE] | [PACKAGE] | [DEPTH_SIGNAL] |
| :--- | :--- | :--- |
| `.api/arctic.md` | `arctic` 3.7.0 | Deep — package surface, full public-type/entrypoint tables split by rail (surfaces-and-dispatch/rails-and-effects/boundaries), implementation law, stacks-with cross-refs to effect/platform/jose, explicit accept/reject `RAIL_LAW` |
| `.api/dopplerhq-node-sdk.md` | `@dopplerhq/node-sdk` 1.3.0 | Deep — admits exactly 3 of ~23 services (secrets/dynamicSecrets/auth), full fault-collapse rationale (39 subclasses → statusCode fold), stacks-with effect/platform/sign |
| `.api/jose.md` | `jose` 6.2.3 | Deep — three-axis collapse (crypto op × token profile × serialization), full JWKS-rotation seam, closed `JOSEError` fault family, stacks-with effect/platform/arctic/oslojs-encoding/session |
| `.api/node-rs-argon2.md` | `@node-rs/argon2` 2.0.2 | Deep — native-addon topology, single `Options` cost carrier, PHC self-description, rehash-fold law (package has no `needsRehash`), stacks-with effect/sign-crypto/oslojs-crypto/authn-apikey |
| `.api/oslojs-crypto.md` | `@oslojs/crypto` 1.0.1 | Deep — `HashAlgorithm` polymorphic-axis law, verify-only ECDSA/RSA boundary (never signs), stacks-with effect/oslojs-encoding/otplib/node-rs-argon2 |
| `.api/oslojs-encoding.md` | `@oslojs/encoding` 1.1.0 | Deep — codec matrix (alphabet × case × padding × direction), total-decode contract, stacks-with oslojs-crypto/effect/authn-webauthn |
| `.api/otplib.md` | `otplib` 13.4.1 | Deep — v13 rewrite topology (singleton legacy API banned), `CryptoPlugin`/`Base32Plugin` port law, stacks-with oslojs-crypto/effect/oslojs-encoding/node-rs-argon2 |
| `.api/simplewebauthn-browser.md` | `@simplewebauthn/browser` 13.3.0 | Deep — browser-only ceremony topology, v13 `{optionsJSON}` object-form law (pre-12 positional banned), `WebAuthnAbortService` single-ceremony law |
| `.api/simplewebauthn-server.md` | `@simplewebauthn/server` 13.3.2 | Deep — RP-side 2×2 ceremony pattern, attestation-format internal dispatch, `SettingsService`/`MetadataService` trust anchors, `./helpers` codec surface |

All 9 catalogs are folder-tier (not shared substrate) and every one carries `RAIL_LAW`/`LOCAL_ADMISSION`/`STACKS_WITH` sections at full depth — no stub or thin catalog in this folder.

## [C]-[CAPABILITY_MAP]

The folder's genuine ownership matches its README/ARCHITECTURE claims closely; this is an unusually complete corpus. Findings:

- **Fully realized, no gap**: all 9 router entries in `README.md` `[01]-[ROUTER]` resolve to a design page that is fully fenced (not a stub), and the `ARCHITECTURE.md` codemap's 9 leaf files (`sign.ts`, `verify.ts`, `secret.ts`, `session.ts`, `credential.ts`, `oauth.ts`, `webauthn.ts`, `claim.ts`, `tenant.ts`) map 1:1 to the 9 `.planning` pages with matching capability descriptions.
- **Domain-package roster matches consumption**: `README.md` `[02]-[DOMAIN_PACKAGES]` lists `jose`, `@node-rs/argon2`, `@oslojs/crypto`, `@oslojs/encoding`, `otplib`, `arctic`, `@simplewebauthn/server`, `@simplewebauthn/browser`, `@dopplerhq/node-sdk` — exactly the 9 packages with `.api` catalogs, and every one is genuinely imported in its owning design page's fence.
- **Mismatch — `IdentityJournal` port claim vs. actual webauthn storage port**: `.api/simplewebauthn-server.md` §[04]-[INTEGRATION] states the verified `WebAuthnCredential` is "journalled through the `IdentityJournal` port `session/token` declares," but `webauthn.md` actually declares and uses a distinct `WebAuthnStore` port (`insert`/`byId`/`bySubject`/`updateCounter`) plus a separate `ChallengeStore` port — neither is `IdentityJournal`. `IdentityJournal` (declared in `session.md`) resolves `CredentialRef` → `Subject`, a different concern. The `.api` catalog's cross-reference is stale relative to the design page it describes.
- **Seams table claims vs. design-page reality — all verified present**: every row in `ARCHITECTURE.md` §[02]-[SEAMS] (`access/tenant → data`, `authn/session ← data`, `access/claim ← data`, `crypt/sign → data/journal`, `crypt/verify → runtime/serve`, `authn/session ⇄ runtime/browser`, `authn/oauth ⇄ runtime/browser`, `crypt/secret → iac/kube`) corresponds to a real port or boundary law stated in the owning page (`SessionStore`/`IdentityJournal`, `ClaimStore`/`RelationStore`, `Shredder`'s `WrappedKey` envelope, `Verify`'s held-octets law, `Cookie`'s CSRF double-submit, `OAuth`'s redirect ceremony, `Secret`'s Doppler custody) — no phantom seam found.
- **Forward-pool emptiness is real, not a gap**: `IDEAS.md` and `TASKLOG.md` both carry only template comments and `(none)` — this folder has zero open/queued/blocked work recorded. Given the design pages are already fully fenced with production-grade code (not stubs or outlines), this reads as a completed-and-closed planning corpus rather than an oversight.
- **`FlagGate`/`RelationStore` cross-folder dependency is declared but unverified from here**: `access/claim.md` explicitly documents `FlagGate` as a `security → runtime` consumer port (the ledger-licensed edge) and `RelationStore`/`ClaimStore` as data-wave-satisfied ports — these are named correctly per `ARCHITECTURE.md`'s boundary law, but this census did not open `libs/typescript/runtime` or `libs/typescript/data` to confirm the counterpart Layers exist; flagged as an assumption for downstream phases to verify against those folders' own census.
- **No stray/orphaned files**: `fd` over the full folder tree found no additional `.ts`, stray `.md`, or leftover scaffold files beyond the 9 planning pages, 9 api catalogs, and 4 root docs plus `project.json`.
