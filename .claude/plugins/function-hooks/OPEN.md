# [OPEN]

Open items of the extension round, each with the measurement or edit that settles it and the record it came from, for a later session to land.

| [INDEX] | [ITEM]                                                   | [SETTLES_IT]                                  | [SOURCE]                      |
| :-----: | :------------------------------------------------------- | :-------------------------------------------- | :---------------------------- |
|  [01]   | Quality campaign over Python, TypeScript, and C# unrun   | Agent improving code in place, checks at zero | EXTEND, section 05            |
|  [02]   | Skill and agent pass two over every command unrun        | Command battery against seeded rows           | EXTEND, section 06C           |
|  [03]   | Skill and agent pass three over the step sequence unrun  | Steps run in order, calls reduced             | EXTEND, section 06C           |
|  [04]   | Loop missed one shape and landed one rule of two         | Trigger, prompts, and gates until both agree  | Watch RESULTS; EXTEND         |
|  [05]   | Declarations against the running build unverified        | `/plugin-types`, then check and validate      | EXTEND, section 03A           |
|  [06]   | Build number in plugin, skill, or agent files            | `rg '2\.1\.2'` over the three trees           | EXTEND, section 03A           |
|  [07]   | Test artifacts of this system outside the plugin         | `watch/` and `Shapes.cs` gone, scans empty    | EXTEND, section 03A           |
|  [08]   | Scratchpad path or record name in a tree file            | Scan over the tree returning nothing          | EXTEND, section 03A           |
|  [09]   | `categoryThreshold` 0 under `CLAUDE.md`'s sentence       | User's change, then the default raised        | EXTEND; reconciliation        |
|  [10]   | `agent_id` on a subagent's auto-mode denial unreached    | Interactive auto-mode denial, its row read    | Gaps RESULTS; reconciliation  |
|  [11]   | Auto compaction and three exit reasons unrecorded        | One interactive session per reason            | Gaps RESULTS; reconciliation  |
|  [12]   | `cost.usd` across `/clear` in one process unmeasured     | Two priced turns around `/clear`              | Views RESULTS                 |
|  [13]   | `turn.step.usage` against the final figure               | Declaration change on `TurnStepInput.usage`   | Views RESULTS; reconciliation |
|  [14]   | Two `Stop` rows in one turn undriven                     | Session with a blocking `Stop` hook           | Views RESULTS; reconciliation |
|  [15]   | Installed plugin copy carries no trigger                 | Reinstall, restart, first interactive spawn   | Watch RESULTS; end-to-end     |
|  [16]   | Delivered finding acted on with no `fixed` transition    | Session owning a delivered site, its row      | Watch RESULTS; closeout       |
|  [17]   | Open rows redeliver every session at 0.5 to 1.0 USD      | Close or decline path, redelivery stopping    | Watch RESULTS                 |
|  [18]   | Wrong terminal state no statement re-opens               | Re-open statement, or rows gone               | Watch RESULTS                 |
|  [19]   | `agent_cost` empty for a plugin-spawned agent            | Build recording its stop rows, remeasured     | Watch RESULTS                 |
|  [20]   | Cataloger reply is a summary, `done_when` one line       | Reply of the next spawned run                 | Closeout ITEMS; judgment      |
|  [21]   | Two same-type spawns can misbind the agent's own id      | Spawn-unique field, two concurrent spawns     | Pass one CRITIQUE; judgment   |
|  [22]   | `finding.path` relative with no worktree column          | Worktree column, read from a linked worktree  | Pass one CRITIQUE             |
|  [23]   | Plugin document restates four skill facts                | Restatement out of the plugin's `CLAUDE.md`   | Pass one CRITIQUE             |
|  [24]   | `scratchpad_dir` absent from the declarations            | Regenerated declarations naming the field     | Pass one CRITIQUE             |
|  [25]   | `commits` view stands on the declaration alone           | One committing session, the view's row        | Pass one RECORD; observation  |
|  [26]   | `WorktreeRemove` reaches no plugin on this build         | Later build, the row after a removal          | Findings HANDOFF; naming      |
|  [27]   | Killed session's run row blocks the worktree trigger     | User's word on a clearing form, kill fixture  | Delivery DESIGN; closeout     |
|  [28]   | Builder editing a file the main agent edits              | `edit_churn` rows over the campaign           | End-to-end DESIGN             |
|  [29]   | Cataloger run longer than the boundary cadence           | `span_ms` against the median `Stop` gap       | End-to-end DESIGN             |
|  [30]   | Status line headless behavior reasoned, unmeasured       | Debug line of one headless session            | Status DESIGN                 |
|  [31]   | Grammar recovers ten `sql.ts` statements as `ERROR`      | Grammar release, scan with no `ERROR` node    | Checkpoint two RECORD         |
|  [32]   | Determinism law suppresses `no-repeated-call-expression` | Rule narrowed, or the limit kept              | Checkpoint two RECORD         |
|  [33]   | Commit review agent unbuilt, its trigger removed         | Agent brief, then the option pair back        | Reconciliation REPORT         |
|  [34]   | Two delivery channels and one column with no writer      | Delivery through each, or the values out      | Reconciliation REPORT         |
|  [35]   | Prose check whole planned by no brief                    | Statement naming what the loop needs          | Reconciliation; end-to-end    |
|  [36]   | `views` Nx target waits on the user's request            | User's word, then the target in `nx.json`     | Capabilities RESEARCH         |
|  [37]   | Transcript glob spans every project directory            | Session with a cwd off the root, both globs   | Capabilities RESEARCH         |
|  [38]   | Foundation and synthesis probes unrun                    | Close deciding each probe                     | Reconciliation REPORT         |
