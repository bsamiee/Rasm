# [PY_GEOMETRY_API_CATALOGUE]

`geometry` API catalogue pages carry the verified package surface on the companion interpreter floor (`python_version<'3.13'`). Each `api-*.md` page records public types, entrypoints, and local admission law reflected from the cp312-installed distribution; the surface is verified spelling, never invention.

## [1]-[PACKAGE_PAGES]

[IFC_COMPANION]:
- rail: ifc-companion
- pages:
  - [api-ifcopenshell.md](api-ifcopenshell.md)

[SCAN_PROCESSING]:
- rail: scan-processing
- pages:
  - [api-open3d.md](api-open3d.md)
  - [api-small_gicp.md](api-small_gicp.md)

[GEOMETRY_ALGEBRA]:
- rail: geometry-algebra
- pages:
  - [api-topologicpy.md](api-topologicpy.md)
  - [api-compas.md](api-compas.md)

## [2]-[CATALOGUE_LAW]

[PACKAGE_SCOPE]:
- API pages carry external package API facts and package-rail admission records.
- Planning pages carry owner boundaries and source-transcription law.
- README pages route catalogues without duplicating member tables.

[CAPTURE_FLOOR]:
- Every `geometry` distribution rides the companion interpreter floor `python_version<'3.13'`, divorced from the `>=3.15` runtime/data/artifacts floor.
- All five distributions install cleanly on a cp312 companion interpreter (`ifcopenshell==0.8.5`, `open3d==0.19.0`, `small-gicp==1.0.1`, `compas==2.15.1`, `topologicpy==0.9.43`); each pulls its own native/scientific stack (OCCT, VTK-free open3d core, numpy/scipy, `topologic-core==8.0.4`) and imports without a C-build step on cp312. The earlier un-installable claim is disconfirmed.
- Member capture is verified: every documented type, entrypoint, submodule, knob, and static-method count on each page resolves against the live cp312 distribution by introspection — no phantom; the four spelling corrections (small_gicp variant-specific voxel accessors, topologicpy `Polyskel` module-not-facade, compas function count) were repaired against the installed surface.
- The residual gap is structural, not member-level: the project default `>=3.15` interpreter carries no cp315 wheel for any of the five, so `assay api query --key <dist>` against the project venv returns `status=unsupported`/`no source`. In-place re-reflection through `assay api` requires the sub-3.13 companion lock scope (`tool.uv.environments` fork or dedicated companion lock); until that scope exists, member proof rides a cp312 companion venv, not the default project resolver.
