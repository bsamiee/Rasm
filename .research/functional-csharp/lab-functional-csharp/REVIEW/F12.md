# F12 review: `12-applicatives-and-laws.md`

1. **Location** line 133.
   **Claim** "`MonadLaw<F>.validate` checks the three laws for an instance:"
   **Evidence** `ilspycmd -t 'LanguageExt.Traits.MonadLaw`1'`: `validate` is
   `ApplicativeLaw<F>.validate(equals) >>> leftIdentityLaw(equals) >>> rightIdentityLaw(equals) >>> associativityLaw(equals) >>> recurIsSameAsBind(equals)`.
   It runs the applicative laws first and then a fourth monad check, `recurIsSameAsBind`, which compares `Monad.recur`,
   `Monad.unsafeRecur`, and a `Bind` recursion. That fourth check is the one that throws for `IO` (LAB-NOTES), so the
   sentence hides the reason a law run can fail outside the three laws printed above it.
   **Correction** replace the sentence with:
   `MonadLaw<F>.validate` checks the applicative laws, the two identity laws, associativity, and the equivalence of `Monad.recur` with `Bind`:

2. **Location** line 120.
   **Claim** "`ApplicativeLaw<F>.validate` checks identity, composition, homomorphism, and interchange for an instance."
   **Evidence** same decompile, `ApplicativeLaw`1.validate`: `FunctorLaw<F>.validate(F.Pure(1), equals) >> homomorphismLaw >> interchangeLaw >> compositionLaw >> functorLaw >> identityLaw`.
   The functor laws run inside it, which the sentence omits.
   **Correction** replace the sentence with:
   `ApplicativeLaw<F>.validate` checks the functor laws, then identity, composition, homomorphism, and interchange.

3. **Location** lines 246-249 and 259-262 (the `FailFast` and `HarvestErrors` blocks).
   **Claim** the blocks open at `public static Func<T, Validation<Error, T>> FailFast<T>(...)` with no enclosing type.
   **Evidence** `.scratch/lab-functional-csharp/F12/Validators.cs` lines 3-8 hold both methods inside
   `internal static class Validators {`; `NOTES/F12.md` records the wrapper as scaffolding. Every other member block in
   the file (lines 158-174, 189-216) prints its `internal static class` holder, and the code text rule asks for
   `internal static class` sample holders, so these two blocks are the only ones a reader cannot compile as shown.
   **Correction** wrap each block in the lab holder, keeping the method text unchanged:
   ```csharp
   internal static class Validators {
       public static Func<T, Validation<Error, T>> FailFast<T>(Seq<Func<T, Validation<Error, T>>> validators) =>
           value => validators.TraverseM(validate => validate(value)).As().Map(_ => value);
   }
   ```
   ```csharp
   internal static class Validators {
       public static Func<T, Validation<Error, T>> HarvestErrors<T>(Seq<Func<T, Validation<Error, T>>> validators) =>
           value => validators.Traverse(validate => validate(value)).As().Map(_ => value);
   }
   ```

4. **Location** line 226 (end of the accumulation paragraph).
   **Claim** the file ends the accumulating path at `Validation<Error, PhoneNumber>` and never names the exit.
   **Evidence** the global rulings put `Validation` at the admitting boundary with `ToFin` at the exit, and
   `21-languageext-result-types.md` line 110 states "`Validation` becomes `Fin` at the exit of the admitting boundary".
   `NOTES/F12.md` lists the same gap under what a project still needs. The file is the corpus owner of applicative
   validation and states the boundary nowhere.
   **Correction** append one sentence to line 226:
   The boundary that admits the fields returns `Validation<Error, PhoneNumber>`, `ToFin` converts it at the exit, and the host matches the `Fin`.

5. **Location** line 264.
   **Claim** "On success, traversal temporarily contains one copy of the input per validator; `Map` discards those copies and returns the original value."
   **Evidence** added prose. The prose rules ban semicolons and "-ly" adverbs in added text; this sentence carries both
   ("temporarily", the semicolon).
   **Correction** replace with:
   On success, traversal holds one copy of the input for each validator. `Map` discards those copies and returns the original value.

6. **Location** line 141, first sentence.
   **Claim** "`Option` maps only `null` to `None`, through `Optional` at the null boundary, and `Some(null)` is a `Some` that holds `null`."
   **Evidence** line 299 already states the same fact: "`Optional` maps `null` to `None` at the null boundary, so `Some`
   stays genuine presence." The prose rules forbid restating one fact twice in a file, and line 299 is the adjusted book
   pitfall that must hold its place.
   **Correction** delete the first sentence of line 141 and keep the rest of the line as it stands:
   Checking only successful contained values is not a complete law check; `None` and failure behavior must also remain equivalent.

## Verified, no action

- Tuple `Apply` arity two to ten: `ApplicativeExtensions` declares tuple receivers of two through ten `K<F, _>` elements;
  the arity-3 body is `f.Map(items.Item1).Apply(items.Item2).Apply(items.Item3)`, which also confirms the retained book
  sentence that higher arities curry and delegate to the unary `Apply`.
- `FunctorLaw<F>.validate(fa)` is `identityLaw >> compositionLaw`, so "checks both laws for one value" holds.
- `Option<A>.Where`, and instance `Select` on `Option`, `Fin`, and `Validation<F, A>` all exist, so lines 151 and 182 hold.
- `Error.Count`, `Error.Head`, `Error.IsType<E>`, and `operator +` exist on `LanguageExt.Common.Error`.
- CsCheck `Sample<T1, T2>(this Gen<(T1, T2)>, Func<T1, T2, bool>, ..., long iter = -1, int time = -1, ...)` backs
  "`Sample` throws on a counterexample" and "the case count and the ranges are configurable".
- Code identity: every `csharp` block matches its lab text after dedent. The two class blocks (lines 158, 189) match
  `Queries.cs` and `PhoneNumbers.cs` up to the closing brace the lab keeps open for its probe method.
- Banned tokens, cross-file references, nested `IO<Option<`/`IO<Fin<`, and single-argument `Eff<X>` are all absent.
  `wc -l` is 299; corrections 1, 2, 4, 5, and 6 all edit existing lines and keep it there.
- No contradiction with `21-languageext-result-types.md` or `23-languageext-traits-and-transformers.md`. Addendum 23
  line 245 shows `TraverseM` under `Validation` for first-failure traversal, which matches this file's fail-fast validator.

Blockers: item 1 (the `MonadLaw<F>.validate` claim is false as shipped).
