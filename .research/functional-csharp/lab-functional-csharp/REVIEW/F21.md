# REVIEW F21 — `21-languageext-result-types.md`

Scope checked: fabrication, code-to-lab identity, boundary placement, terminology, coherence with `LAB-NOTES.md` and files 22-24, gaps.

Clean results (not itemised below): all eight `csharp` blocks are byte-identical to `F21/*.cs` minus the `namespace` line; `wc -l` is 241; no cross-file reference; no banned token; no `IO<Option<` or `IO<Fin<`; no single-argument `Eff<X>`; no hedge; no `-ly` adverb in prose; every member named in prose exists (`Option.ToFin`/`ToValidation`/`ToSeq`, `Fin.MapFail`/`BiMap`/`BindFail`/`IfFail`/`ToOption`/`ToEither`/`ToValidation`, `Try.Run`, `IO.RunSafe`, `Eff.Run(rt) : Fin<A>`, `Error.Head`/`Count`/`Filter<E>`/`IsType<E>`/`HasCode`/`Is`, `Errors.TimedOut`, `Errors.None`, `guard`/`when`/`unless`); the table's shape column matches every type declaration in the assembly.

1. **Location** — `NOTES/F21.md` "Corrections and findings", first bullet, and `LAB-NOTES.md` "Verified surface" (`Fin<A>` row) and "Corrections from the per-file labs" (F21 row).
   **Claim** — "CORRECTION to the API document: `Fin.Succ(A)` and `Fin.Fail(Error)` are listed as static constructors. `Fin<A>.Succ` and `Fin<A>.Fail` are nested classes … and the module `Fin` class has no `Succ`/`Fail` functions."
   **Evidence** — `ilspycmd -t 'LanguageExt.Fin'`: `public static Fin<A> Succ<A>(A value) { return new Fin<A>.Succ(value); }`, `public static Fin<A> Fail<A>(Error error)`, `public static Fin<A> Fail<A>(string error)`. Probe build (`net10.0`, `LanguageExt.Core 5.0.0-beta-77`, `using static LanguageExt.Prelude`): `Fin<int> a = Fin.Succ(1); Fin<int> b = Fin.Fail<int>(Errors.None);` builds with zero diagnostics and runs (`True True`). Both module functions exist and resolve; only the nested-class half of the claim is true.
   **Correction** — replace the notes bullet with: "`Fin<A>.Succ` and `Fin<A>.Fail` are nested classes. The module functions `Fin.Succ<A>(A)` and `Fin.Fail<A>(Error)` exist and resolve; the file uses the implicit lift instead, so one construction path stays in the doc." Delete "There is no `Fin.Succ(x)` module function and" from the `LAB-NOTES.md` `Fin<A>` row and remove the same sentence from the F21 row. `.scratch/api-languageext.md` rows [01] and [02] are correct and need no report.

2. **Location** — "One type per concern", the sentence after the block list (line 31).
   **Claim** — "All of them expose `Match` with one function per case."
   **Evidence** — `sigs/IO_1.txt:122: public IO<B> Match<B>(Func<A, B> Succ, Func<Error, B> Fail)`; `sigs/Eff_2.txt:289: public Eff<RT, B> Match<B>(Func<A, B> Succ, Func<Error, B> Fail)`. `Match` on the two effect types returns another effect and does not leave the effect, which the file's own boundary rule ("`Match` … are host operations") reads as an exit.
   **Correction** — "Each of them exposes `Match` with one function per case, and `Match` on `IO` and `Eff` returns an effect that the host runs."

3. **Location** — "One type per concern", `Eff<RT, A>` paragraph (line 29).
   **Claim** — "`Eff<RT, A>` is an `IO<A>` that reads a capability from a runtime `RT`."
   **Evidence** — `sigs/Eff_2.txt:15: public record Eff<RT, A>(ReaderT<RT, IO, A> effect) : K<Eff<RT>, A>`. `Eff<RT, A>` holds a `ReaderT` over `IO`, and `sigs/Eff_2.txt:709` is the implicit conversion from `IO<A>`. File 22 states the same fact as "`Eff<RT, A>` reads a capability from a runtime."
   **Correction** — "`Eff<RT, A>` is an effect that reads a capability from a runtime `RT`."

4. **Location** — "Recovery as use", first paragraph (line 174).
   **Claim** — "The `Catch` overloads select by code, by error value, or by predicate. They return `K<F, A>`, so `.As()` restores the concrete type."
   **Evidence** — `sigs/FallibleExtensions.txt:204` (code) and `:140` (error value) return `K<F, A>`, but `sigs/IO_1.txt:736: public virtual IO<A> Catch(Func<Error, bool> Predicate, Func<Error, K<IO, A>> Fail)` is an instance method that wins over the extension on `IO`. Probe build: `IO<int> caught = io.Catch(static (Error e) => true, static (Error e) => IO.pure(0));` compiles with no `.As()`.
   **Correction** — "The `Catch` overloads select by code, by error value, or by predicate. The code and error-value overloads return `K<F, A>`, so `.As()` restores the concrete type."

5. **Location** — "Recovery as use", the heading (line 172).
   **Claim** — "## Recovery as use".
   **Evidence** — every other heading in the file and in files 22, 23, and 24 is a noun phrase ("Conversions", "Exits at the host", "Transformers", "Shared mutable state"). "as use" is not a corpus term and names nothing in the section.
   **Correction** — "## Recovery".

6. **Location** — "The boundary rule", last sentence (line 70).
   **Claim** — "In the block that follows, `Register` admits the raw form as `Validation`, `Handle` converts with `ToFin` and binds the domain transition, and `Respond` matches at the host." (27 words; the prose rule caps a descriptive sentence at 25.)
   **Evidence** — brief, "Prose rules for added or changed text".
   **Correction** — "In the block that follows, `Register` admits the raw form as `Validation`. `Handle` converts with `ToFin` and binds the domain transition, and `Respond` matches at the host."

7. **Location** — "Conversions", first paragraph (line 110).
   **Claim** — "`Option` needs an `Error` to become `Fin` or `Validation`, because absence has no reason of its own."
   **Evidence** — `Option_1.cs:752: public Fin<A> ToFin() { return ToFin(Errors.None); }` and `sigs/Option_1.txt:859: public Validation<L, A> ToValidation<L>() where L : Monoid<L>`. Argument-free overloads exist and fill in `Errors.None`, so the sentence is false as a statement about the surface while true as the file's ruling.
   **Correction** — "A conversion of `Option` to `Fin` or `Validation` names the `Error`, because absence has no reason of its own."

8. **Location** — "Anti-patterns" table, the `OptionT<IO, A>` row (line 226) and `CorrectForms.Lookup`.
   **Claim** — "An `Option` nested inside an effect forces the consumer to unwrap two layers | `OptionT<IO, A>`, as `Lookup` shows".
   **Evidence** — `Lookup` returns `OptionT<IO, Person>` and the doc shows no exit for it. `F21/Samples.cs` exits with `CorrectForms.Lookup(People, "ada").Run().As().RunSafe()`, which the doc does not carry, and the file may not refer to another file for it. `sigs/OptionT_2.txt` `Run()` returns `K<IO, Option<A>>`.
   **Correction** — add one sentence after the table: "`Lookup` exits at the host with `Run`, `As`, and `RunSafe`, which yields `Fin<Option<Person>>`."

Blockers: none.
