# [RASM_CS_FABRICATION_DECISION] — SYNTHESIS · blueprint of record

Base = `draft-namespace-true-partition` (consensus winner: 2 of 3 judges name it, the third names it runner-up). Seven grafts merged: **A** kernel-seam K1-K23 signature-lock TABLE (3 judges), **B** output-first 8-case `Post`+`Document` entry (3 judges), **C** output-first `ArtifactKind` egress spine collapse onto `owner#atoms` (2 judges), **D** output-first/kernel-seam F1-F4 flagship dry-run closure (2 judges), **E** output-first `CapabilityVerdict`-on-`owner#atoms` plan-admission gate (1 judge + disk check: real gap, base's own input-carry precedent, F2 dry-run needs it), **F** clipper SPLIT to 8 absorb pairs (1 judge + disk check: `clipper.md` holds `ArcAlgebra` arc-space on disk, base's `arcs=new/no-absorb` drops it), **G** package-mining exhaustive 24-row roster format (2 judges). REJECTED by consensus and NOT adopted: package-mining's `Machine→MachineKind` rename (no namespace collision — gratuitous churn), the kernel-seam/output-first frozen-band `2701-2704→Process` misattribution (disk fixes NoFit→Nesting/Unreachable→Kinematics/KerfCollision→Geometry2D), CutterForm-on-`Tooling`/`family` (cycle — mints on `owner#atoms`).

Binding folder/page-set blueprint for the `libs/csharp/Rasm.Fabrication/.planning/` ground-up restructure. `RASM-CS-FABRICATION-BRIEF.md` `[00]`-`[06]` composes unchanged; the geometry DECISION (`RASM-CS-GEOMETRY-DECISION.md`, LANDED) is the signature-lock source. 33 page rows (7 rebuild, 3 improve, 23 new incl. 7 move/split destinations), 13 folders, **8-case** `FabricationPolicy`/`FabricationResult`, **8 absorb pairs**, all V1-V10 / E1-E14 / 14 capability deltas / both refuted `[V12]` bindings / the OpenCAMLib FEASIBLE ruling / 24 roster dispositions / 23 cards disposed — no open item survives.

## [00]-[LENS_THESIS] — standing law, never re-opened mid-leg

The corpus is a subtractive-milling engine wearing production clothing, and the folder map is the disguise: `Process/` hosts four namespaces (`Process`/`ProcessModel`/`ProcessPhysics`/root), `Posting/` hosts two (`Posting`/`Projection`), `Toolpath/` hosts three (`Toolpath`/`Kinematics`/`Additive`), `Polygon/` and `Nesting/` each hide one migrant (`Geometry2D` import, `Fixturing` workholding). The namespace-true lens rules: the fence `namespace` declaration on disk is the ground truth of where a concern BELONGS; the folder is a lie the DECISION corrects. Twelve realized namespaces become thirteen folders (one namespace, `Geometry2D`, carries two concerns — line-space + arc-space — that SPLIT into two pages under one namespace; three mandate lanes — `Verify`/`Spec` + `Tooling`'s machinability — mint new namespaces the roster already provisioned but no fence realized). Every structural motion is namespace-motivated on disk, never invented. Fabrication is ALSO an egress engine (output-first's true observation, absorbed): every flagship terminates in a content-keyed machine-consumable artifact, so the egress concern COLLAPSES onto the entry vocabulary — the `ArtifactKind` discriminant + ONE `ContentHash.Of` fold on `owner#atoms` — rather than fragmenting into a 14th `Egress/` folder.

FOUR lens-distinctive rulings, never re-litigated:

1. **The `Process` namespace/type collision (HIGH hazard — the V1 floor WORSENS it).** `owner.md:39` declares `namespace Rasm.Fabrication.Process`; `family.md:98` declares `public sealed partial class Process` (the `[SmartEnum<string>]`). The brief's V1 floor homes owner AND family both into `Process/`, so both share `namespace Rasm.Fabrication.Process` and the type resolves `Rasm.Fabrication.Process.Process` — a latent `CS0118`-class ambiguity already live where `program.md:28,30` imports both `Rasm.Fabrication.Process` (owner atoms) and `Rasm.Fabrication.ProcessModel` (the `Process` type) then references bare `Process` at `:142,143,150`. **RULING: rename the SmartEnum type `Process` → `ProcessKind`** (family declaration + every `input.Process`/`ProcessKind process`/`FabricationInput.Process`-type reference across owner/physics/motion/program/magazine). This is the namespace-PRESERVING break: renaming the namespace segment would forfeit the literal folder=namespace 1:1 the lens exists to ratify; the type rename keeps `Process/ ↔ Rasm.Fabrication.Process` clean. **`Machine` does NOT collide (no `Machine` namespace) and does NOT rename** — the asymmetry is collision-forced, recorded (package-mining's extra `Machine→MachineKind` rename is gratuitous churn on the de-hardcode axis exemplar and is REJECTED). The brief's V1 line "faults leaves the package root → Process" is binding, so root cannot host the axis; the type must yield.

2. **`CutterForm` mints on `owner#atoms`, not `Tooling/`.** The brief V5 homes the typed `CutterForm` axis on `Tooling/`; but `FabricationPolicy.Cam` re-frames its bare `ToolRadius` double to a `CutterForm` reference (V5), so `owner#atoms` (where `FabricationPolicy` is declared) would depend on `Tooling`, while `Tooling/magazine` composes `owner.Loop` for `HolderEnvelope` — `owner#atoms → cuttingdata(CutterForm) → magazine(ToolAssembly) → owner#atoms(Loop)` cycles. RULING: the `CutterForm` value TYPE mints on `owner#atoms` (the shared cross-plane cutter vocabulary, a pure-scalar leaf — flat/ball/bull/taper/drill/chamfer/thread-mill rows with diameter/corner-radius/taper-angle/flute-length), exactly as `CutStrategy` (family) and `ResidualStock`/`StockSnapshot` (V7) ride the entry vocabulary; `Tooling/cuttingdata` owns the PROJECTION `CutterForm.Of(ToolAssembly)` + the Kienzle machinability keyed by it. The four consumers (surface/removal/guard/cuttingdata) read the `owner#atoms` type; the cycle never forms. This is the lens's fourth latent-acyclicity catch — package-mining sites the type on `Tooling/magazine` (masked owner#atoms⇄Tooling cycle), output-first hosts it ambiguously; both are corrected here.

3. **The two REFUTED reciprocal `[V12]` bindings the brief treats as landed law (upstream dossier §D-2).** `PBRIEF [V12]` is `[V12_GOVERNANCE_RECONCILE]`, NOT an `ArtifactKind` egress index — the real binding is a held-open BLOCKED card `[FABRICATION_PROGRAM_DURABLE_ROWS]` (`PBRIEF:221`, "2701-2710 decode constraint recorded"). `CBRIEF [V12]` is `[V12_DISCIPLINE_COVERAGE]`, NOT a `WasteAreaMm2` rollup — the `ARCH:51` seam is genuinely one-sided/un-landed. RULING: every machine-consumable egress artifact (cut programs, `.cli` stacks, 3MF, NC1, stock snapshots, travelers) mints its content key through the CONFIRMED `ContentHash.Of` seed-zero entry (landed kernel law), and the `ArtifactKind`/durable-row fold is carried as a **Fabrication-AUTHORED demand + blocked cross-package seam residual**, never a composed upstream contract; the Compute waste rollup is a one-sided forward demand, never "waterfall landed." Signature-lock rides the DECISION anchors (stable), not the +14/+16-drifted GBRIEF line pins; `UIBRIEF [V6]`→`[V7]` verdict number corrected; the `[RESEARCH]`-tail census is FIVE (`kinematics:122`, `skeleton:199`, `slicing:219`, `stock:317`, `program:379`), not three.

4. **The entry is 8-case; the egress spine collapses onto `owner#atoms` (grafts B, C, E).** `FabricationPolicy`/`FabricationResult` grow the brief-named `{Additive, Verify, Inspect}` PLUS `{Post, Document}` (output-first-mandated) atop the base `{Cam, HiddenLine, Nest}` — EIGHT cases. Re-posting one `Motion` to five dialects is `Run(Post{Motion, PostDialect})` ×5 (F1); traveler-compose is `Run(Document{results})` (F4). Both are re-invocable terminal transforms over a prior result — under the one-entry law a `Run` policy case carrying the prior result on `owner#atoms`, NEVER a second public `Post()`/`Traveler.Assemble()` fold (this closes the base's underspecified `program.md Post(...)` and the `[05](b)` five-target dry-run). The `ArtifactKind` discriminant is a row family co-located with the egress `FabricationResult` cases on `owner#atoms`, keyed through ONE `ContentHash.Of` fold — never a 14th `Egress/` folder (which would force a compose edge from every plane and re-mint the E9 second-hasher defect). `CapabilityVerdict` mints on `owner#atoms` as a minimal input-carried verdict leaf (pass/fail + Cpk + demanded grade) exactly as `ResidualStock`/`StockSnapshot` do — `Spec/capability.Gate` PRODUCES it wave-4, the plan READS it input-carried, closing spec→plan→verify→capability with no intra-run cycle (the full `CapabilityReport` stays terminal on `capability`; only the verdict leaf rides atoms, keeping the wave-1 surface minimal).

OpenCAMLib probe = **FEASIBLE** (thin `extern "C"` C-shim + source-generated P/Invoke over the shipped C++-ABI SHARED archives; all three RIDs present; LGPL-2.1 dynamic-link clean; strictly-heavier author-kernel fallback) — sited on `Toolpath/surface`, the author-kernel SDF drop-cutter retired to a recorded admission-abandonment fallback. Both ruled kernel hinges carried verbatim and signature-locked (`[05]` K1/K2), never re-opened: `[V10]a` (kernel corner-row offset assembly + medial; Fabrication pure kerf compensation in arc space over CavalierContours on `Geometry2D/arcs`), `[V10]d` (kernel medial carries per-point clearance RADIUS + arbitrary-probe `Clearance(Point3d probe)` as ONE shared result family; `Toolpath/Skeleton.cs` wavefront dies unconditionally, keeps only the trochoidal WALK).

## [01]-[RATIFIED_PARTITION] — 13 folders, namespaces (folder = namespace 1:1), fault clusters

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`, all namespace-motivated on disk): `Polygon/` DIES (both pages leave — clipper SPLITS to `Geometry2D/{algebra,arcs}`, import MOVES to `Ingress/profile`). `Process/` KEEPS (loses magazine → Tooling; gains nothing; the collision-break renames its axis type). `Toolpath/` KEEPS (loses kinematics → Kinematics, slicing → Additive; gains surface + partition). `Nesting/` KEEPS (loses workholding → Fixturing). `Posting/` KEEPS (loses projection → Documentation; gains dialect). MINTED: `Tooling/` · `Geometry2D/` · `Ingress/` · `Kinematics/` · `Additive/` · `Fixturing/` · `Verify/` · `Spec/` · `Documentation/`. Every folder ≥2 pages; the mandate lanes ARE the growth that makes the thin folders legal. `ARCHITECTURE.md` `[01]` codemap + `[02]` seam ledger + the README router rewrite in the wave-1 motion.

Namespace partition — 13 TIER-1 folders ↔ 13 TIER-2 namespaces, BIJECTIVE (no namespace spans two folders; no folder hosts two namespaces). The six ratifications where the FOLDER overrides a realized namespace (fence `namespace` rewrites in the same MOVE): family/faults/physics → `Process`, magazine → `Tooling`, import → `Ingress`, projection → `Documentation`. The four namespace-KEEPING moves (the realized namespace already names the target folder, the FOLDER catches up): clipper `Geometry2D` (SPLIT, folder Polygon→Geometry2D), kinematics `Kinematics` (folder Toolpath→Kinematics), slicing `Additive` (folder Toolpath→Additive), workholding `Fixturing` (folder Nesting→Fixturing).

| # | Folder | Namespace | Pages | Fault cluster | Charter |
|---|---|---|---|---|---|
| 1 | `Process/` | `Rasm.Fabrication.Process` | owner, family, physics, faults | 2700 band owner (union; `OpenLoop` 2704 cross-cutting recorded exception) | the entry, the axes, the physics, the rail |
| 2 | `Tooling/` | `Rasm.Fabrication.Tooling` | magazine, cuttingdata | 2712 `MachinabilityUnknown` | ISO-13399 tool intelligence + machinability |
| 3 | `Geometry2D/` | `Rasm.Fabrication.Geometry2D` | algebra, arcs | 2703 `KerfCollision` (re-attributed from Posting) | the 2D substrate (line-space + arc-space) |
| 4 | `Ingress/` | `Rasm.Fabrication.Ingress` | profile, solid, steel | 2711 `IngressTranslation` | everything entering as geometry |
| 5 | `Toolpath/` | `Rasm.Fabrication.Toolpath` | motion, surface, partition, guard, skeleton | 2705 `InadmissiblePair`, 2706 `Gouge`, 2707 `Collision`, 2713 `SampleStalled` | subtractive CAM |
| 6 | `Kinematics/` | `Rasm.Fabrication.Kinematics` | cell, machine | 2702 `Unreachable`, 2714 `AxisSingularity` | motion topology |
| 7 | `Additive/` | `Rasm.Fabrication.Additive` | slicing, implicit, production | 2708 `NonManifoldSlice`, 2715 `VoxelFault`, 2716 `OrientationInfeasible` | production 3DP |
| 8 | `Nesting/` | `Rasm.Fabrication.Nesting` | nfp, stock | 2701 `NoFit`, 2709 `StockOverflow`, 2710 `Nest` | layout + yield |
| 9 | `Fixturing/` | `Rasm.Fabrication.Fixturing` | workholding, setups | 2717 `SetupInfeasible` | keep-out + setup planning |
| 10 | `Posting/` | `Rasm.Fabrication.Posting` | program, dialect | 2718 `DialectUnsupported`, 2719 `ProgramParse` | machine-code emission |
| 11 | `Verify/` | `Rasm.Fabrication.Verify` | removal, probing | 2720 `ProbeOvertravel` (+ `Gouge` 2706 shared verify-time) | program-level truth |
| 12 | `Spec/` | `Rasm.Fabrication.Spec` | tolerance, capability | 2721 `ToleranceUnsatisfiable`, 2722 `CapabilityShortfall` | production specs |
| 13 | `Documentation/` | `Rasm.Fabrication.Documentation` | projection, traveler | reserved (projection routes kernel `ProjectionFault`; traveler assembles) | shop documentation |

RULED, never re-litigated: (a) the `Process` axis TYPE renames `Process`→`ProcessKind` so folder=namespace ratifies `Process/ ↔ Rasm.Fabrication.Process` without the `CS0118` collision (`[00]` ruling 1); `Machine` unchanged (collision-forced asymmetry recorded). (b) `Geometry2D` is the ONE namespace carrying two pages (algebra line-space + arcs arc-space) — the split is CONCERN-driven (Clipper2 int64 line lattice vs CavalierContours bulge arc lattice, `[V10]a` stratum) not namespace-driven, so folder=namespace 1:1 holds with two pages. (c) `Ingress` carries three pages under ONE polymorphic `Ingress.Admit(IngressSource)` fold — profile/solid/steel own the `Profile`/`Solid`/`Steel` source arms; one entry, three arms, three namespace-mates. (d) `Documentation/` has an EMPTY reserved fault block (projection consumes the kernel `Rasm.Geometry.Projection` `ProjectionFault`; traveler is content-keyed document assembly with no fault producer) — a reserved empty block is mechanically checkable, never asserted. (e) The `owner#atoms`/`owner#run` two-node split is the ONE ratified acyclicity exception: `Process/owner` is both the upstream atom vocabulary and the terminal `Fabrication.Run` fold; the seam ledger evaluates it as two nodes and the page splits physically only if its ledger fails the two-node discipline (it does not — see `[05]`).

Fault-registry design (namespace-true, folder-clustered, growth-law-sized): the 2700 century divides into 13 folder clusters. The landed 2701-2710 are the FROZEN first-instance layer — wire codes retained per the `.archive` "build ON, never re-band" law + the `PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]` 2701-2710 persisted-decode constraint — and are NON-contiguous per folder (Nesting spans 2701/2709/2710) as the recorded consequence of that freeze. The NEW growth arms allocate folder-CONTIGUOUS from 2711 (`+11..` per the brief), so folder = fault-cluster is mechanically checkable on the growth layer; the twelve legacy arms are a recorded frozen exception mirroring the kernel's cross-cutting `DegenerateInput` 2400. Full registry in `[04]`.

## [02]-[PAGE_SET] — 33 rows (paths `libs/csharp/Rasm.Fabrication/.planning/`-relative)

Row schema: path | action → engine lowering (kind new/rebuild/improve; deletePages; absorb {into, from}) | charter (TIER-1 folder · TIER-2 namespace · fault cluster) | entry (owning fold signature + the `FabricationPolicy`/`FabricationResult` case ONLY where the page grows the entry) | seams (out = composes, in = consumed by; one anchor per seam, both ledger directions; `owner` evaluates as TWO nodes — `owner#atoms` upstream-only, `owner#run` terminal) | wave. Waves 1:1 with legs.

### Wave 1 / Leg 1 — STRUCTURE + AXES + RAIL (Process rebuilds + Tooling)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 1 | `Process/owner.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700 owner. The settled `Run` entry + generated total `Switch` PRESERVED VERBATIM (cold-pass core); the rebuild is atom-vocabulary + policy/result growth to **8 cases**: `Loop` widens to `(Arr<Point3d>, bool, Arr<double> Bulges)` + 3-arg ctor + `BulgeAt` (the `[V10]a` arc rail's one shape, 0/tan(θ/4)); `ProjectionDir` mints HERE (the SEAM inversion fix — was `projection.md:42` downstream); `CutterForm` value type mints HERE (`[00]` ruling 2, the shared cutter leaf); `ResidualStock` + per-setup `StockSnapshot` mint HERE (V7 — input-carried, breaks the motion→removal→program→motion cycle); **`CapabilityVerdict` mints HERE** (graft E — minimal verdict leaf, input-carried, breaks the capability→plan cycle); **`ArtifactKind` discriminant mints HERE** (graft C — the egress row family co-located with the `FabricationResult` egress cases, keyed through the ONE `ContentHash.Of` fold; no 14th `Egress/` folder) | `Fin<FabricationResult> Fabrication.Run(FabricationPolicy, FabricationInput)` — **8-case** `FabricationPolicy` {`Cam`, `HiddenLine`, `Nest`, +`Additive(AdditivePolicy)`, +`Verify(VerifyPolicy)`, +`Inspect(InspectPolicy)`, +`Post(FabricationResult.Motion, PostDialect)`, +`Document(Seq<FabricationResult>)`}, **8-case** `FabricationResult` {`Motion`, `HiddenLineResult`, `Placement`, +`AdditiveResult`, +`VerificationResult`, +`InspectionResult`, +`PostedProgram`, +`TravelerDocument`}; egress cases carry the `ArtifactKind` discriminant + `ContentKey` via the one `ContentHash.Of` fold | **owner#atoms** out: predicates `Orient2D`, Vectors `MeshSpace`/`Point3d`, `ContentHash.Of` (content-key seed), family `CutStrategy`/`ProcessKind` (leaf axes) · in: EVERY plane reads the atoms (Loop+Bulges, Edge3, ArcCenter, Move, PartTransform, ProjectionDir, CutterForm, ResidualStock, StockSnapshot, CapabilityVerdict, ArtifactKind, FabricationInput/Policy/Result). **owner#run** out: projection.Hlr, motion.Cam, nfp.Nest, additive, removal, probing, **program/dialect Post-lowering, traveler Document-compose** (terminal dispatch, 8 arms) · in: none (terminal) | 1 |
| 2 | `Process/family.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700. Axis widening + the collision rename: `ProcessKind` (ex-`Process`, `[00]` ruling 1) · `Machine` (UNRENAMED — no collision) · `RemovalModality` · `KinematicClass` deepens to rotary-topology rows (table-table trunnion \| head-head \| head-table \| nutating — `mill-5axis` stops binding `CartesianGantry`, V5/V8) · `HoldingClass` · `PostDialect` grows GRAMMAR-family capability columns (family · canned-cycle grammar · macro/subprogram convention · WCS roster · cutter-comp admission · arc mode · block cap · decimal/modal policy · per-`GCommand` code-override — V4; the flat 3-column render dies) · `CutStrategy` gains a dimensionality column (2.5D \| 3D-surface \| multi-axis, V5). Data-vs-lowering split: the widened axes stay HERE, the emission folds are `Posting/dialect`'s | NONE (pure `[SmartEnum]` axes; `Admits` row-relations) | out: `Rasm.Geometry` `GeometryFault`, predicates · in: owner#atoms (CutStrategy/ProcessKind), physics, motion, program, dialect, magazine, workholding, machine, cell | 1 |
| 3 | `Process/physics.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700. Identity map (V9): ONE `Material` identity carrying `Map<RemovalModality, ModalityPhysics>` (the `stainless`/`stainless-abrasive` + `mild-steel`/`mild-steel-thermal` fragmented rows collapse); `Budget` modality-dispatches into the map; `RemovalBudget.Erosion` lands (EDM discharge/pulse/wire-feed — the `erosion→ThermalBudget` conflation dies) beside `Resin` (exposure/cure) + `Powder` (laser/hatch/scan) + DED-distinct-from-FFF; the `Overrides = Empty` dead carrier DELETES; the `90.0` fallback demotes behind the data table. The `CuttingData` table DONOR-BLOCK excises to `Tooling/cuttingdata` (charter-carried, not absorb-encoded) | `RemovalBudget RemovalParameter.Budget(ProcessKind, Material, ToolAssembly, Operation)` | out: family (`RemovalModality`), UnitsNet, `Rasm.Materials/Properties` (raw doubles, AEC-peer boundary — anchor reconciled to `physical-properties#MATERIAL_PROPERTY`) · in: motion, cuttingdata, surface (chip-load budget) | 1 |
| 4 | `Process/faults.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700 band owner. `FabricationFault` `[Union]` on `FaultBand.Fabrication = 2700`: every legacy arm retypes its bare `string Detail` to a typed payload (`Gouge`→point+CutterForm, `Collision`→zone, `InadmissiblePair`→the typed `(RemovalModality, CutStrategy)`, `NoFit`→part+tried rotations, `Unreachable`→joint diagnostics); the frozen 2701-2710 wire codes PRESERVED; twelve new arms land 2711-2722 folder-contiguous (`[04]`); the `band-2500` root-anchor + `ARCH:16`/`kinematics:3`/`TASKLOG:44`/`.api` 25xx references purge to 2700 | NONE (`FabricationFault.<Case>(...).ToError()` vocabulary; composes kernel band-2400 `GeometryFault` for `DegenerateInput`) | out: `Rasm.Element` `FaultBand` registry (substrate), `Rasm.Geometry` `GeometryFault` · in: every fault-routing owner | 1 |
| 5 | `Tooling/magazine.md` | MOVE (ProcessModel→Tooling) + wire → new; absorb {into: `Tooling/magazine.md`, from: `Process/magazine.md`}; deletePages [`Process/magazine.md`] | Tooling · `Rasm.Fabrication.Tooling` · 2712. MTConnect `CuttingToolAsset` `ToolAssembly`/`Schedule` life-split PRESERVED (the deeply-mined admission); `HolderEnvelope` projection kept; `Runout` reads the real `ShankDiameterMeasurement` value (the literal 0.01/0.005 stub dies, E12); `GenerateHash` reconciles onto `ContentHash.Of` (no second mint); WIRED — the tool-change `Schedule` feeds `Posting/program` (the E2 fiction realized: real `M6`/`G43`/`Tnn` blocks at interval boundaries) | `ToolMagazine.Schedule(Seq<ToolAssembly>, ...)` life-split; `HolderEnvelope.Of(ToolAssembly)` | out: MTConnect `CuttingToolAsset`, `ContentHash.Of`, UnitsNet, physics (Tool), owner#atoms (Loop), Riok.Mapperly (MTConnect rows) · in: cuttingdata (ToolAssembly), guard (HolderEnvelope), program (Schedule) · cross-pkg: ← `Rasm.AppHost` decoded tool-life telemetry (`APPHOST:210`, first consumer) | 1 |
| 6 | `Tooling/cuttingdata.md` | NEW → new | Tooling · `Rasm.Fabrication.Tooling` · 2712. The machinability model (V9): Kienzle unit-cutting-force `kc` + chip-thickness exponent + per-operation surface-speed + feed-per-tooth columns keyed by the `physics` `Material` identity (honest seed DATA behind a CSV/QIF data-INGRESS arm, never a formula pretending measured — FreeCAD cards copyright-removed, gap recorded); the `CutterForm.Of(ToolAssembly)` projection from the ISO-13399 measurement set (the type mints on owner#atoms, `[00]` ruling 2); `program`'s `0.0012` deflection literal reads the `kc` column | `CuttingData CuttingData.Of(Material, CutterForm, Operation)`; `CutterForm CutterForm.Of(ToolAssembly)` | out: magazine (ToolAssembly), physics (Material), owner#atoms (CutterForm), MathNet, UnitsNet · in: program (kc deflection), surface (effective-diameter speed) | 1 |

### Wave 2 / Leg 2 — SUBSTRATE + INGRESS (Geometry2D split, Ingress, skeleton staged early)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 7 | `Geometry2D/algebra.md` | SPLIT from clipper → new; absorb {into: `Geometry2D/algebra.md`, from: `Polygon/clipper.md`}; deletePages [`Polygon/clipper.md`] | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The Clipper2 line-space owner (offset · `DeltaCallback64` variable offset · Boolean clip · Minkowski sum/diff · `ReuseableDataContainer64` scanbeam reuse) — the settled craft, namespace already true. ADDS `NestingOrder` — the inner-before-outer containment-depth sort (namespace-true re-home of the mis-attributed `Posting.Sequence`: contour ordering by nesting depth is a Geometry2D concern via `Loop.Covers`, not machine-code; posting/motion/workholding all compose it, killing the workholding→posting W3→W4 forward edge) | `PolygonAlgebra` — `Offset`/`Clip`/`Minkowski.Sum`/`Minkowski.Diff`/`OffsetVariable`/`NestingOrder` (line-space) | out: Clipper2 `Clipper2Lib`, predicates `Orient2D`, owner#atoms (Loop) · in: nfp, guard, workholding, motion, arcs (kerf base), slicing, program (NestingOrder) | 2 |
| 8 | `Geometry2D/arcs.md` | SPLIT from clipper → new; **absorb {into: `Geometry2D/arcs.md`, from: `Polygon/clipper.md`}** (graft F — clipper.md holds `ArcAlgebra`/`BoolKind`/CavalierContours arc-space on disk `:182-271`; the arc-space craft is carried forward, never dropped on delete) | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The CavalierContours arc-space owner composing the RULED `[V10]a` split: the kernel owns the exact corner-row offset assembly + medial, `ArcAlgebra` owns pure kerf compensation IN ARC SPACE over the kernel-emitted region offsets — the landed `BoolKind` `[SmartEnum<string>]` set-op axis (`or`/`and`/`not`/`xor`→CavalierContours `BooleanOp`) + `ArcOffset`/`ArcBoolean`/`ShapeOffset`/`ArcLength`/`SampleAtLength`, island-preserving `Shape<T>.FromPlines(loops).ParallelOffset`, `StaticAABB2DIndex<T>`, the bulged-`Loop`↔`Polyline<double>` boundary seam (all absorbed from clipper); the kerf/lead/adaptive rails wire through HERE (arc-native, no biarc refit); the `result.Positive` PHANTOM corrects to `result.PosPlines[].Pline` (E3-NEW, `api-cavaliercontours.md:47`); reads the widened `Loop.Bulges`/`BulgeAt`/3-arg ctor (landed wave-1 on owner#atoms) | `ArcAlgebra` — `KerfArc`/`LeadArc`/`AdaptiveArc`/`ArcBoolean(BoolKind)` over kernel `Offsetting.Apply` region offsets | out: CavalierContours (arc-space), kernel `Offsetting.Apply` (region offsets, `[V10]a`), algebra (kerf base), owner#atoms (Loop.Bulges), predicates `Orient2D` · in: program (arc-native kerf/lead), motion (adaptive), skeleton (trochoidal chord) | 2 |
| 9 | `Ingress/profile.md` | MOVE (Polygon/import, ns Geometry2D → Ingress) → new; absorb {into: `Ingress/profile.md`, from: `Polygon/import.md`}; deletePages [`Polygon/import.md`] | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DXF/DWG profile-ingress arm of the ONE polymorphic `Ingress.Admit` fold; ACadSharp `Failsafe`/bulge+NURBS samplers/`Insert.Explode` PRESERVED; the dead `warnings` accumulator wires the strict-mode `NotificationType.Error` escalation (ruled — the claim-deletion form loses ingress-degradation visibility); the netDxf rejection frame re-writes (netDxf removed repo-wide, nothing to reject) | `Fin<AdmittedGeometry> Ingress.Admit(IngressSource)` over `IngressSource` `[Union]` {`Profile` · `Solid` · `Steel`} — profile owns the fold + `Profile` arm | out: ACadSharp, predicates, owner#atoms (Loop) · in: nfp (part library), program (profile program), motion · cross-pkg: ← `Rasm.Bim/Exchange` (shared ACadSharp read pin, `ARCH:52` netDxf parenthetical re-points to the AppUi ACadSharp two-format WRITE leg) | 2 |
| 10 | `Ingress/solid.md` | NEW → new | Ingress · `Rasm.Fabrication.Ingress` · 2711. The OcctNet B-rep solid-ingress arm: `ImportStep`(AP203/214/242)/`ImportIges`/`ImportStl` → `OcctShape` → `Triangulate` → `OcctMesh`; the tessellated `OcctMesh` crosses to kernel vocabulary at the seam (`MeshSpace` admission; dirty STL routed through the kernel predicate-gated `HealOp` heal), so downstream planes consume admitted kernel geometry, never raw soup; the `libTKHLR`/`libTKXCAF` managed-unbound SCOPE_LIMIT is a standing demand row (assembly + AP242 PMI surfacing = wrapper pressure, the `Spec/` plane's secondary source), maintenance risk re-verified each leg | `Ingress.Admit` `Solid` arm (STEP/IGES/STL → kernel `MeshSpace`) | out: OcctNet.Wrapper, kernel `MeshSpace` admission (`mesh.md`), kernel `HealOp` (`repair.md`, dirty STL), Riok.Mapperly · in: slicing, surface, projection (the B-rep the format shops receive) | 2 |
| 11 | `Ingress/steel.md` | NEW → new | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DSTV NC1 steel-exchange READ arm: the record tree (`ST` header + `BO`/`SI`/`SC`/`AK`/`IK`/`KA` blocks) → `Loop` via a Riok.Mapperly generated projection (hand-written seam copyists die); the `KA` bend blocks seed the sheet-metal IDEAS lane (K-factor unfold waits for its consumer); the emission MODEL is shared with the `Posting` NC1 target (mirroring the read) | `Ingress.Admit` `Steel` arm (NC1 → Loop) | out: DSTV.Net, Riok.Mapperly (DSTV-record → Loop), owner#atoms (Loop) · in: nfp, program (NC1 emit model shared) | 2 |
| 12 | `Toolpath/skeleton.md` | REBUILD → rebuild (cross-folder, staged early: wave-3 motion's engagement bound reads its clearance field) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705-2707,2713. Kernel-medial CONSUMER (`[V2]a`/`[V10]d`, RULED): `Toolpath/Skeleton.cs` wavefront (`Wavefront`/`SkeletonEvent`/`Propagate`/`OffsetAt`) DIES UNCONDITIONALLY; the page keeps ONLY the trochoidal constant-engagement WALK strategy over the kernel clearance field; the `[03]-[RESEARCH]` tail + false "no managed library exists" seal purge | `Skeleton.Walk(CurveSkeleton, EngagementPolicy)` (trochoidal WALK only) | out: kernel `Skeletonize.Apply` → `Fin<CurveSkeleton>` + `Offsetting.Apply` clearance-radius/`Clearance(Point3d probe)` (the ONE shared clearance family, 2D+3D, `[V10]d`) · in: motion (Adaptive engagement bound), guard (ClearanceAt reach) | 2 |

### Wave 3 / Leg 3 — CAM + ADDITIVE + NESTING + FIXTURING (listed = dependency order; composed pages first)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 13 | `Toolpath/guard.md` | KEEP + wire → improve | Toolpath · `Rasm.Fabrication.Toolpath` · 2706-2707. Shape-correct interior PRESERVED (swept Minkowski envelope, distinct part-keep/stock-keep-out verdicts, the per-move DESIGN-time floor); WIRED — `Check` consults per committed feed move inside the Cam fold (`Gouge`/`Collision`→typed fault, `Clearance`→substitute lift, `Clear`→commit); reads the CURRENT `StockSnapshot` (per-setup), never the raw blank; the 3 out-seams DECLARED | `Verdict Guard.Check(Move, Part, Stock, Fixture)` | out: skeleton (`ClearanceAt` reach), magazine (`HolderEnvelope`), algebra (Minkowski), kernel `Spatial/index` (BVH broad-phase), owner#atoms (CutterForm swept radius, StockSnapshot) · in: motion (per-move), setups (per-op collision) | 3 |
| 14 | `Fixturing/workholding.md` | MOVE (Nesting, ns Fixturing → Fixturing) + safety rebuild → new; absorb {into: `Fixturing/workholding.md`, from: `Nesting/workholding.md`}; deletePages [`Nesting/workholding.md`] | Fixturing · `Rasm.Fabrication.Fixturing` · 2717. Safety hardening (V3/E9): `Clears`/`Condition` replace 3-point endpoint+midpoint sampling with segment-vs-polygon intersection (the thin diagonal clamp crossing detected); `ForHolding` first-match becomes a TOTAL mapping (`Vise` reachable behind `Clamp`); per-kind footprint SHAPES land as real geometry columns (two-jaw vise, revolved chuck jaw, full-bed vacuum), not a `MarginScale` scalar; `Condition` WIRED into the Cam conditioning fold and composing `NestingOrder` (the re-homed ordering, backward to Geometry2D) | `Fin<Seq<Move>> Workholding.Condition(Seq<Move>, Fixture)`; `ExclusionZone Workholding.Zone(...)` | out: algebra (Offset/Clip, NestingOrder), family (HoldingClass), owner#atoms (Loop) · in: guard (ExclusionZone), motion (Condition), setups (per-setup fixture) | 3 |
| 15 | `Fixturing/setups.md` | NEW → new (MULTI_FIXTURE_SCHEDULE realizes) | Fixturing · `Rasm.Fabrication.Fixturing` · 2717. Multi-fixture setup scheduler (V3/V7): `Setup` `[ComplexValueObject]` (Fixture, WCS datum, ReachableOps); a QuikGraph operation-precedence/datum-lineage graph partitions operations across reorientation setups (the NP-hard SEQUENCING half homes here, magazine eviction greedy stays); OWNS the setup→WCS assignment rows (setup k → the dialect's G54-G59.x/G54.1-Pn roster — posting RENDERS what setups assigns, never a posting-side default); admits op-N workholding against the op-N-1 machined `StockSnapshot` (a clamp on a cut-away face is the plan-time defect the snapshot catches) | `Fin<SetupSchedule> Setup.Schedule(Seq<Operation>, SchedulePolicy)` | out: QuikGraph (precedence graph), workholding (fixture reach), guard (per-op collision), family (`Machine.AxisCount` reach), owner#atoms (StockSnapshot, input-carried) · in: program (WCS rows), probing (per-setup datum), traveler (setup plan) | 3 |
| 16 | `Kinematics/cell.md` | MOVE (Toolpath/kinematics, ns Kinematics → Kinematics) → new; absorb {into: `Kinematics/cell.md`, from: `Toolpath/kinematics.md`}; deletePages [`Toolpath/kinematics.md`] | Kinematics · `Rasm.Fabrication.Kinematics` · 2702. The Robots serial-chain cell HELD (per-manufacturer RAPID/KRL/URScript/VAL3/DRL posts, `Program` look-ahead, the `extern alias R3` boundary exemplar); band-2500→2700; `[03]-[RESEARCH]` tail purge (member confirmations fold into `Packages`/`.api`); its jerk/accel planner NAMES the shared motion-dynamics law homed on `machine` | `Fin<FabricationResult.Motion> RobotProgram.Solve(RobotCell, Seq<Move>)` | out: Robots, Rhino3dm (`extern alias R3`), Riok.Mapperly (R3 seam), machine (motion-dynamics law) · in: motion (articulated-arm dispatch) | 3 |
| 17 | `Kinematics/machine.md` | NEW → new | Kinematics · `Rasm.Fabrication.Kinematics` · 2714. The 5-axis machine-tool topology owner (V5, closing the 5-axis gap beside the Robots cell): rotary-axis inverse + TCP/RTCP admission over the `KinematicClass` rotary-topology rows (table-table/head-head/head-table/nutating); multi-axis orientation interpolation COMPOSES the kernel one-slerp owner (`MotionInterpolation`), never a second slerp site; HOMES the ONE jerk/accel motion-dynamics law posting `Lookahead` reads as a typed policy shape (the Robots planner + posting planner read it, mapped at the R3 edge so they cannot drift) | `Fin<FabricationResult.Motion> MachineTool.Solve(MachineKinematics, Seq<Move>)` | out: kernel `MotionInterpolation` (slerp), family (KinematicClass rotary rows) · in: motion (multi-axis dispatch), program/cell (motion-dynamics law), machine post kinematics | 3 |
| 18 | `Nesting/nfp.md` | KEEP + identity fix → improve | Nesting · `Rasm.Fabrication.Nesting` · 2701,2709. The NFP-feasibility true-shape nesting engine PRESERVED (register 9 minus E9); `Stock.Of()` hashes discriminant + ALL dimensions through `ContentHash.Of` (the area-only collision poisoning `Remnant.Parent` dies); the 3 raw `XxHash128` mints (`:59,88,169`) route the ONE `ContentHash.Of` federation entry; `Ty*1e6+Tx` becomes a `(Ty,Tx)` tuple comparison; the NFP origin-vs-`Anchor` reference-frame incoherence PROVEN or fixed (translate candidates by the placed `(pl.Tx,pl.Ty)`, single consistent frame) on the golden fixture; the rect-fastpath re-homes `RectpackSharp` → `RectangleBinPack.CSharp` `MaxRectsBinPack` heuristic-sweep | `Fin<FabricationResult.Placement> Nest.Solve(NestPolicy, FabricationInput)`; `Nest.Honor(NestPlan)` | out: algebra (Minkowski), `ContentHash.Of` (identity), RectangleBinPack (fastpath), owner#atoms (Placement/Remnant, reads input.Plan) · in: owner#run · cross-pkg: → `Rasm.Persistence` (Placement/Remnant content key via `ContentHash.Of` — DEMOTED to blocked demand on `PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]`, `ARCH:54` re-points off deleted Schema); ← `Rasm.Compute` DRL nest-policy score (injected delegate, strata-correct) | 3 |
| 19 | `Nesting/stock.md` | KEEP → improve | Nesting · `Rasm.Fabrication.Nesting` · 2710. The rectangular cutting-stock YIELD engine HELD (register 9, the scalar exemplar); the `Nesting/stock ← Rasm.Element/MaterialId` ledger row ADDED (was wired-undeclared, E11); `RectangleBinPack.CSharp` central pin re-group from the vacated Materials label VERIFIED; the `[05]-[RESEARCH]` tail purges | `Fin<NestPlan> StockNest.Pack(NestStrategy, Seq<CutPart>)` | out: RectangleBinPack, algebra, `Rasm.Element/MaterialId` (NEW ledger row), owner#atoms · in: nfp (NestPlan via input.Plan) · cross-pkg: → `Rasm.Compute` `NestYield.WasteAreaMm2` (`ARCH:51` — DEMOTED to a ONE-SIDED forward demand, NOT "waterfall landed"; `CBRIEF [V12]` REFUTED, §D-2) | 3 |
| 20 | `Spec/tolerance.md` | NEW → new (cross-folder, staged early: surface's Ra→scallop derivation reads its rows within-wave) | Spec · `Rasm.Fabrication.Spec` · 2721. The typed GD&T vocabulary author-kernel (V8 — NO admissible OSS library exists): ISO 286 fits (grade/deviation tables as GENERATED rows, never enumerated), ISO 1101/ASME Y14.5 feature-control frames + datum systems, ISO 1302 surface texture (Ra/Rz + lay + process marks), MMC/LMC modifiers — QIF-aligned (ISO 23952); the spec DRIVES process: the Ra/Rz→cutter-corner-radius→constant-scallop stepover/stepdown derivation row (`surface` consumes), the ISO 286 IT-grade→finishing-allowance row (`[V5]` pass planning conditions) | `Tolerance.Frame(FeatureControl)`; `double Tolerance.ScallopStep(RaTarget, CutterForm)`; `double Tolerance.Allowance(ItGrade)` | out: UnitsNet (extended Force/Power/Temp/Angle/Torque), owner#atoms (CutterForm) · in: surface (Ra→scallop), motion (IT→allowance), capability (grade), traveler (spec rows) | 3 |
| 21 | `Toolpath/motion.md` | REBUILD → rebuild | Toolpath · `Rasm.Fabrication.Toolpath` · 2705. Real `(RemovalModality, CutStrategy)` generators (V5 — every stub arm becomes a REAL operation): `Turn` a true ZX radial sweep over a revolved stock envelope (the `BarStock` phantom lands as a real shape whose floor composes the kernel `Analysis/measure.md` `Bounds` enclosing-cylinder fit), `helical` a distinct thread/ramp (the `Turn` alias dies), `Peck` real canned-cycle geometry with dwell/retract, `SliceWalk` a real perimeter/infill/travel orderer; rest-machining reads the input-carried `ResidualStock` (never a motion→removal page edge); `Guard.Check` + `Workholding.Condition` WIRED per feed move; adaptive engagement reads the medial clearance field AND the physics chip-load budget on ONE policy row; the `Option<ArcCenter>` chord feeds the arc-native posting rail (not biarc refit) | `Fin<FabricationResult.Motion> Cam.Solve(FabricationPolicy.Cam, FabricationInput)`; `Cam.Generate((RemovalModality, CutStrategy))` — assembles the dialect-NEUTRAL content-keyed `CutProgram` AST inside this fold (the Cam fold is where Guard/Workholding/Magazine execute; `Post` lowers it later) | out: guard (per-move Check), workholding (Condition), cell/machine (dispatch), skeleton (Adaptive clearance), family (Admits), physics (budget), arcs (adaptive arc), algebra, program (neutral CutProgram AST) · in: owner#run | 3 |
| 22 | `Toolpath/surface.md` | NEW → new (OpenCAMLib FEASIBLE) | Toolpath · `Rasm.Fabrication.Toolpath` · 2713. The 3D-surface finishing family (V5): OpenCAMLib owns cutter POSITIONING (drop/push/waterline/adaptive-waterline height sampling) via the thin `extern "C"` C-shim + source-generated `[LibraryImport]` P/Invoke over the shipped SHARED `libocl` (ALC-firebreak/sidecar posture, content-keyed at the wire; author-kernel SDF drop-cutter is the recorded admission-abandonment fallback); path LAYOUT composes the kernel on-mesh machinery — geodesic-parallel/constant-stepover read the kernel heat distance (`geodesics.md` via `fields.md` `Geodesic`) as `extract.md` `ContourPolicy.MeshScalar` isolines, flowline/morph trace `flow.md` streamlines, `segment.md` cross-fields seed flank/swarf orientation (a Fabrication-side on-mesh re-implementation is the `[V2]` defect); waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family rows; the `CutterForm` axis drives OCL cutter selection at the P/Invoke edge | `Fin<Seq<Move>> SurfacePath.Sample(SurfaceStrategy, MeshSpace, CutterForm)` | out: OpenCAMLib (P/Invoke), kernel `geodesics.md`/`extract.md`/`flow.md`/`segment.md` (path layout), kernel `fields.md` SDF (fallback), tolerance (Ra→scallop), owner#atoms (CutterForm), physics (chip-load), System.Numerics.Tensors (SIMD grids) · in: motion (surface arms) | 3 |
| 23 | `Toolpath/partition.md` | NEW → new (SharpVoronoiLib mandate) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705. The Voronoi region-decomposition CAM lane (V5, the `[V1]` realization the kernel supersession left to this brief): `VoronoiPlane` Fortune sites + Lloyd relaxation (`Relax(iterations, strength, reTessellate)`, `VoronoiSite.Centroid` the seed) → pocket-region seeds, Lloyd-relaxed stipple/engrave/even-spacing/pen-plot strategy rows; point-site only (polygon medial stays the kernel's) | `Fin<Seq<Loop>> Partition.Seed(PartitionStrategy, Loop)` | out: SharpVoronoiLib, algebra · in: motion (partition strategy rows) | 3 |
| 24 | `Additive/slicing.md` | MOVE (Toolpath, ns Additive → Additive) + kernel-consumer rebuild → new; absorb {into: `Additive/slicing.md`, from: `Toolpath/slicing.md`}; deletePages [`Toolpath/slicing.md`] | Additive · `Rasm.Fabrication.Additive` · 2708. Kernel slice-stack CONSUMER (`[V2]b`, the `ARCH:48` "when realized" promise FIRED): the `Section`/O(n²) `Chain` author-kernel DIES; keeps the FFF/DED `InfillPattern` + shell composition over the kernel-emitted oriented contours (enriched from gsSlicer prior art — open-sheet, overlapping shells); gyroid/TPMS route to the PicoGK voxel lane (the `InfillPattern`-row false collapse corrects); variable/adaptive layer height as kernel slice-stack POLICY rows (Fabrication's named demand, never a Fabrication-side layer engine); the false author-forever seal + `[03]-[RESEARCH]` tail purge | `Fin<AdditiveResult> Slice.Layers(SliceStack, InfillPolicy)` | out: kernel `Slicing.Apply` → `Fin<SliceStack>` (4 layer policies + 5-channel SoA forest, `[V10]b`), algebra, implicit (gyroid/TPMS route) · in: owner#run (Additive case), production | 3 |
| 25 | `Additive/implicit.md` | NEW → new (PicoGK, THREE lanes) | Additive · `Rasm.Fabrication.Additive` · 2715. PicoGK to depth (V6/V7): `IImplicit → Voxels` TPMS/gyroid/cellular conformal infill (the quartet cannot express TPMS, `api-picogk.md:152`); `Lattice` beam supports + overhang-conditioned scaffolds; SLA/DLP/MSLA grayscale + `.cli` vector layer stacks (`oVectorize → CliIo`, `Vdb2Cli`); voxel offset/shell lightweighting; DECLARES ONCE the net9+native ALC-firebreak/sidecar posture + the content-keyed mesh↔`Voxels` wire (removal composes this declared seam, never a second posture) | `Fin<Voxels> Implicit.Voxelize(ImplicitOp)`; `CliStack Implicit.Cli(Voxels)` | out: PicoGK (`IImplicit`/`Voxels`/`Lattice`/`CliIo`), kernel mesh↔Voxels wire, `ContentHash.Of` (`.cli`/grayscale keys) · in: slicing (gyroid/TPMS), production (lattice/support), removal (declared voxel seam) | 3 |
| 26 | `Additive/production.md` | NEW → new (lib3mf) | Additive · `Rasm.Fabrication.Additive` · 2716. Build-orientation + 3MF egress (V6): author-kernel orientation optimization on the overhang/bottom-area/contour objective (the overhang/bottom terms read the kernel axis-ranked face decomposition + draft capture, `Analysis/select.md` `Faces`/`Curves.Draft` per candidate orientation, never a hand-rolled normal classifier); machine profiles (build volume, nozzle/vat/laser, kinematics class as rows); lib3mf 3MF core/production/beam-lattice writer via the vendored official C# binding + RID-keyed native asset (PicoGK `Lattice` maps directly to the beam-lattice extension; STL-implied hand-off dies) | `Fin<AdditiveResult> Production.Plan(BuildPolicy, MeshSpace)` | out: lib3mf (3MF), implicit (lattice/support), kernel `Analysis/select.md` (orientation faces), `ContentHash.Of` (3MF key) · in: owner#run (Additive case) | 3 |

### Wave 4 / Leg 4 — EGRESS + TRUTH + DOCS (listed = dependency order; composed pages first)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 27 | `Posting/dialect.md` | NEW → new | Posting · `Rasm.Fabrication.Posting` · 2718. The per-dialect emission generator (V4, data-vs-lowering split — the axis is family's, the FOLDS are here; the byte-lowering half of the `Run(Post)` case): `Emit` a generated total `Switch` over the `PostDialect` GRAMMAR family (`WordAddress` RS-274 \| `Conversational` Klartext/Mazatrol/OSP \| `Additive` Marlin/RepRap + resin/powder layer-stack); canned-cycle expansion (G81-G89/G76 single blocks where admitted, expanded moves where not); macro/subprogram lowering (Fanuc macro-B `#`/WHILE/GOTO, M98/M99, Siemens R-params, Heidenhain Q); code-override resolution; the NC1 steel emit TARGET (the DSTV record tree as emission model, mirroring `Ingress/steel`); rows widen as seed DATA (Siemens 840D, Heidenhain TNC, Okuma OSP, Fagor, Centroid) | `GWord Dialect.Emit(PostDialect, GNode)` (generated total Switch; invoked by the `Run(Post{Motion, PostDialect})` case, never a public fold) | out: family (PostDialect grammar columns), steel (NC1 emit model) · in: program (per-family lowering), owner#run (Post case byte-lowering) | 4 |
| 28 | `Verify/removal.md` | NEW → new (PicoGK NC-verify) | Verify · `Rasm.Fabrication.Verify` · 2706 (verify-time Gouge). Voxel material-removal verification (V7): stock `Voxels` minus the accumulated swept-tool volume via PicoGK `BoolSubtract` (a ball-nose sweep IS the capsule beam); emits typed gouge/uncut/overcut/air-cut RECEIPTS against the ACTUAL removed-stock state; PRODUCES the `ResidualStock` field (`[V5]` rest rows read it, input-carried) + content-keyed per-SETUP intermediate `StockSnapshot`s (never one terminal state — `Fixturing/setups` admits op-N against op-N-1, `guard` reads the current snapshot); composes the declared `Additive/implicit` ALC/voxel seam, never a second posture | `Fin<FabricationResult.VerificationResult> Removal.Verify(VerifyPolicy, FabricationInput)` — the `Verify` policy case | out: implicit (declared voxel seam, BoolSubtract), owner#atoms (ResidualStock/StockSnapshot mint, CutterForm swept) · in: owner#run (Verify case) · cross-pkg: → `Rasm.Persistence` (snapshot content key via `ContentHash.Of`, blocked-demand) | 4 |
| 29 | `Spec/capability.md` | NEW → new | Spec · `Rasm.Fabrication.Spec` · 2722. Process capability (V8): Cp/Cpk/Pp/Ppk with the streaming-moment half on the kernel `Domain/stats.md` owners (`Stat.Of` Welford, `Distribution.Of` quantiles, `StatContext.Tolerance` provenance — probing evidence arrives PRE-FOLDED, a re-derived Welford is the dual-paradigm defect) and the distribution-fit + Monte-Carlo tolerance-stackup half over `MathNet.Numerics` (first Fabrication consumer); SPC control-limit rows; the plan-time Cpk GATE (a process row whose Cpk history cannot hold the demanded grade fails admission BEFORE material is cut) PRODUCES the `CapabilityVerdict` minted on `owner#atoms` (graft E — the plan reads it input-carried, closing spec→plan→verify→capability; the full `CapabilityReport` stays terminal here, only the compact verdict leaf rides atoms) | `Fin<CapabilityReport> Capability.Assess(Seq<ResidualSample>, Tolerance)`; `CapabilityVerdict Capability.Gate(ProcessKind, ItGrade)` | out: kernel `Domain/stats.md` (Stat/Distribution), MathNet (fit/Monte-Carlo), tolerance (grade), NodaTime (SPC stamps), owner#atoms (CapabilityVerdict mint) · in: owner#run (Inspect projection), traveler (capability rows), owner#atoms plan-admission (input-carried); reads probing measured ingress (input-carried) | 4 |
| 30 | `Posting/program.md` | REBUILD → rebuild | Posting · `Rasm.Fabrication.Posting` · 2703,2719. AST growth + content key (V3/V4): the flat `Seq<GWord>` block stream gains canned-cycle nodes with typed R/Q/P slots, subprogram units with repeat counts, macro parameter slots (per-family lowering is node expansion via `dialect.Emit`, never string synthesis); the cut-conditioning author-kernel MATH PRESERVED (`Kerf`/`Simplify`/`Compensate`/`Feedrate`/`Lookahead`); the arc rail REALIZES — kerf/lead/adaptive read `arcs` arc-native offsets + `Loop.Bulges` (`g3.BiArcFit2` retained ONLY for genuinely line-sourced kernel mesh-section chains until the owned Bolton biarc fold lands); CONSUMES `Magazine.Schedule` (real `Tnn`/`M6`/`G43`), `Workholding` conditioning, `Setups` WCS assignment; `CutProgram` gains its content key via `ContentHash.Of` + its `ArtifactKind` discriminant; a `Parse` round-trip arm carries the NIST RS274NGC modal-group state machine (motion/plane/distance/units/feed groups; `Parse ∘ Emit = id` on each dialect's admitted subset); the `[03]-[RESEARCH]` tail purges. The public `Post` fold folds into the entry as the `Run(Post{Motion, PostDialect})` case (graft B — program supplies the neutral AST + conditioning body, `dialect` supplies `Emit`; NOT a second public fold); `Post.Parse` stays a distinct ingress arm | `Run(Post{FabricationResult.Motion, PostDialect}) → PostedProgram` (the case body: condition the neutral `CutProgram` AST, lower via `dialect.Emit`); `Fin<CutProgram> Post.Parse(string, PostDialect)` (ingress arm) | out: dialect (Emit lowering), magazine (Schedule), workholding (conditioning), setups (WCS), arcs (arc-native kerf/lead), algebra (NestingOrder), geometry3Sharp (line-sourced biarc only), owner#atoms (Move, ArtifactKind), `ContentHash.Of` (program key) · in: owner#run (Post case), traveler (program facts) · cross-pkg: → `Rasm.Persistence` (CutProgram durable rows, `ARCH:53` re-points off deleted Schema — blocked-demand on `PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]`); → kernel `Drawing/pack.md` `toolpath` `PackKind` (committed motion residency, `GBRIEF [V13]`, a ledger row never a Fabrication encoder) | 4 |
| 31 | `Verify/probing.md` | NEW → new (PROBING realizes) | Verify · `Rasm.Fabrication.Verify` · 2720. In-process metrology (V7): touch-probe cycle vocabulary (G31/G38 rows on the posting AST), work-offset/tool-length metrology, measured-feature receipts; datum best-fit COMPOSES the kernel `Processing/register.md` `AlignKind` ICP dispatcher under one `AlignmentPolicy` (probed points → nominal, never a hand-rolled least-squares); measured-vs-nominal folds through the kernel `Analysis/measure.md` `ConformanceMetric` over `ResidualSample`; QIF (ISO 23952) MeasurementResults feed `Spec/capability`; measured ingress is HONEST — probe values enter through the same AppHost decoded-telemetry seam or typed manual-entry rows (design-time receipts are typed carriers; the `(d)` dry-run composes on fixture-synthetic measurements) | `Fin<FabricationResult.InspectionResult> Probe.Inspect(InspectPolicy, FabricationInput)` — the `Inspect` policy case | out: kernel `register.md` (ICP), kernel `measure.md` (ConformanceMetric), program (G38 rows), setups (per-setup datum), NodaTime (receipt stamps) · in: owner#run (Inspect case), capability (measured ingress) · cross-pkg: ← `Rasm.AppHost` decoded probe telemetry (`APPHOST:210`, second consumer) | 4 |
| 32 | `Documentation/projection.md` | MOVE (Posting, ns Projection → Documentation) + kernel-consumer rebuild → new; absorb {into: `Documentation/projection.md`, from: `Posting/projection.md`}; deletePages [`Posting/projection.md`] | Documentation · `Rasm.Fabrication.Documentation` · kernel-routed. Kernel `DrawingProjection` CONSUMER (`[V2]c`/`[V3]`, RULED): the BSP interior (centroid-classified facets, per-edge average depth, undeduped mesh-edge soup, `1e6` eye) DIES; the PHANTOM `Predicate.Orient2D` import drops (0 fence calls, the page's own forbidden defect); KEEPS the `BooleanSolid` watertight arm (kernel `Arrangement.Apply`/`ToMesh` compose-seam); PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at (`UIBRIEF [V7]`(b)) | `Fin<FabricationResult.HiddenLineResult> Hlr.Solve(FabricationPolicy.HiddenLine, FabricationInput)` | out: kernel `Drawing/view.md` `View.Apply` → `Fin<DrawingProjection>` (`[V3]`), kernel `Meshing/arrangement.md` `Arrangement.Apply`/`ToMesh` (watertight, `ARCH:43` re-points off `Processing/repair`), owner#atoms (Loop) · in: owner#run (HiddenLine case), traveler (views) · cross-pkg: → `Rasm.AppUi/Render` (`HiddenLineResult`, `ARCH:47`, supersession insulated at the receipt) | 4 |
| 33 | `Documentation/traveler.md` | NEW → new | Documentation · `Rasm.Fabrication.Documentation` · reserved. Setup sheets + shop travelers (V8): content-keyed shop documents composing the projection views, magazine tool lists, workholding/setups plans, program facts, and spec rows (the typed document MODEL only — sheet/annotation RENDERING rides the `[06]` artifacts-plane seam); the machine-tooling-intelligence mandate's document egress; the widest fan-in node (validates every upstream owner exposes a typed receipt). `Traveler.Assemble` folds into the entry as the `Run(Document{Seq<FabricationResult>})` case (graft B — a re-invocable terminal transform over a completed result-set, NOT a second public fold) | `Run(Document{Seq<FabricationResult>}) → TravelerDocument` (the case body: compose views/tool-lists/fixture-plans/program-facts/spec-rows into the content-keyed document model) | out: projection (views), magazine (tool lists), workholding/setups (fixture plans), program (facts), tolerance/capability (spec rows), NodaTime (stamps), owner#atoms (ArtifactKind), `ContentHash.Of` (doc key) · in: owner#run (Document case) · cross-pkg: → `Rasm.Persistence` (traveler durable rows, blocked-demand) | 4 |

## [03]-[ENGINE_EXECUTION_MANIFEST]

- **Kind census:** new ×23 (rows 5-11, 15, 17, 20, 22-29, 31, 33 + the 7 move/split destinations 5,7,9,14,16,24,32) · rebuild ×7 (owner 1, family 2, physics 3, faults 4, skeleton 12, motion 21, program 30) · improve ×3 (guard 13, nfp 18, stock 19).
- **deletePages ×7** (the move/split sources, absorbed): `Process/magazine.md`, `Polygon/clipper.md`, `Polygon/import.md`, `Toolpath/kinematics.md`, `Toolpath/slicing.md`, `Nesting/workholding.md`, `Posting/projection.md`. The `Polygon/` folder deletes (both pages leave).
- **absorb pairs ×8** (graft F raises the count from 7 — clipper SPLITS into TWO absorb destinations): {into: `Tooling/magazine.md`, from: `Process/magazine.md`} · {into: `Geometry2D/algebra.md`, from: `Polygon/clipper.md`} (clipper's line-space `PolygonAlgebra`) · **{into: `Geometry2D/arcs.md`, from: `Polygon/clipper.md`}** (clipper's arc-space `ArcAlgebra`/`BoolKind`/CavalierContours seam — disk-confirmed `clipper.md:182-271`, carried forward not dropped) · {into: `Ingress/profile.md`, from: `Polygon/import.md`} · {into: `Kinematics/cell.md`, from: `Toolpath/kinematics.md`} · {into: `Additive/slicing.md`, from: `Toolpath/slicing.md`} · {into: `Fixturing/workholding.md`, from: `Nesting/workholding.md`} · {into: `Documentation/projection.md`, from: `Posting/projection.md`}. `Polygon/clipper.md` deletes ONCE, absorbed into BOTH `algebra` and `arcs` (a SPLIT with two absorb destinations, each destination kind=new).
- **Donor-block excision WITHOUT source deletion** (charter-carried on both rows, never absorb-encoded): `physics.md` (rebuild, row 3) → `Tooling/cuttingdata.md` (new, row 6) — the `CuttingData` `SFM`/chip-load table `:117-125` excises; `physics` keeps the identity map, `cuttingdata` deepens it to the Kienzle `kc` model. One donor, two owners.
- **`.api` motions ride the wave-1 roster manifest, not deletePages:** `api-opencamlib.md` + `api-lib3mf.md` authored with the ADD (wave 1); `api-rectpacksharp.md` deletes with the REMOVE (wave 1); `api-hashing.md` slims to the thin overlay (wave 1). The leg that first COMPOSES an admitted package deepens its stub.
- **Two-touch pages explicit:** owner {W1 atoms/policy growth} + the `Additive`/`Verify`/`Inspect`/`Post`/`Document` compose-seam arms realize in the leg landing each owning plane (W3/W4); every other page is single-touch.

## [04]-[FAULT_REGISTRY] — one 2700-century block per folder (frozen 2701-2710 + folder-contiguous 2711-2722)

`FabricationFault` stays ONE `[Union]` on `FaultBand.Fabrication = 2700` (the substrate mechanism is kernel-owned; `FabricationFault` is the one Fabrication fault family). Layer 1 (frozen 2701-2710) retains wire codes per the `.archive` build-ON law + the `PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]` persisted-decode constraint; every legacy arm RETYPES its `string Detail` to a typed payload. Layer 2 (2711-2722) allocates folder-contiguous, sized by the growth law. Folder = cluster is mechanically checkable on Layer 2; the non-contiguous legacy codes are the recorded frozen exception (mirroring the kernel's cross-cutting `DegenerateInput` 2400). Disk-confirmed against `faults.md:14,39-43`: NoFit→Nesting, Unreachable→Kinematics, KerfCollision→Geometry2D, OpenLoop→Process, InadmissiblePair→Toolpath — the kernel-seam/output-first "2701-2704 as Process arms" attribution is REJECTED.

| Folder | Block | Arms (code · typed payload) |
|---|---|---|
| Process | 2700 owner + cross-cut | `OpenLoop` 2704 `(FabConcern concern, int primitive)` — the ONE cross-cutting fabrication-contract arm (producers motion·nfp·skeleton·program), recorded exception |
| Nesting | 2701, 2709, 2710 (legacy) | `NoFit` 2701 `(int part, Seq<double> triedRotations)` · `StockOverflow` 2709 `(int unplaced, int sheets)` · `Nest` 2710 `(NestFault kind, int part)` |
| Kinematics | 2702, 2714 | `Unreachable` 2702 `(JointDiagnostic diag, int target)` · `AxisSingularity` 2714 `(RotaryAxis axis, double angle)` NEW — RTCP/rotary singularity distinct from reach |
| Geometry2D | 2703 | `KerfCollision` 2703 `(Point3d at, double kerf)` — re-attributed to the kerf-compensation owner (`arcs`), off Posting |
| Toolpath | 2705-2707, 2713 | `InadmissiblePair` 2705 `((RemovalModality, CutStrategy) pair)` · `Gouge` 2706 `(Point3d point, CutterForm tool)` · `Collision` 2707 `(ExclusionZone zone)` · `SampleStalled` 2713 `(SurfaceStrategy strategy, int iteration)` NEW — OpenCAMLib drop-cutter non-convergence |
| Additive | 2708, 2715, 2716 | `NonManifoldSlice` 2708 `(int layer, int openChains)` · `VoxelFault` 2715 `(ImplicitOp op, string witness)` NEW · `OrientationInfeasible` 2716 `(int overhangs, double bestScore)` NEW |
| Ingress | 2711 | `IngressTranslation` 2711 `(SourceKind kind, string witness)` NEW — the ONE polymorphic ingress fault (profile/solid/steel discriminated by payload) |
| Tooling | 2712 | `MachinabilityUnknown` 2712 `(Material material, Operation op)` NEW — no cutting-data row and no admitted formula fallback |
| Fixturing | 2717 | `SetupInfeasible` 2717 `(int operation, int triedSetups)` NEW — no feasible setup assignment / operation unreachable in every fixture |
| Posting | 2718, 2719 | `DialectUnsupported` 2718 `(PostDialect dialect, GNodeKind node)` NEW — a required cycle/macro the grammar cannot express or degrade · `ProgramParse` 2719 `(int line, ModalGroup group)` NEW — G-code ingress modal-group violation |
| Verify | 2720 (+2706 verify-time) | `ProbeOvertravel` 2720 `(Point3d at, double limit)` NEW — the queued PROBING card's fault; a detected verify-time gouge routes the shared `Gouge` 2706 |
| Spec | 2721, 2722 | `ToleranceUnsatisfiable` 2721 `(FeatureControl frame, double achievable)` NEW · `CapabilityShortfall` 2722 `(ProcessKind process, double cpk, double demanded)` NEW — the plan-time Cpk gate (the `CapabilityVerdict` fail arm) |
| Documentation | reserved (empty) | projection routes the kernel `Rasm.Geometry.Projection` `ProjectionFault`; traveler is content-keyed assembly with no fault producer |

Growth law: the next free offset is `+23` (2723); a new concern is one arm on its folder's block, never a new band. `Code` derives `FaultBand.Fabrication + n` per case; message strings for 2701-2710 UNCHANGED. Federation re-plan is NOT needed — 22 arms fit 2701-2722 with 2723-2799 headroom.

## [05]-[SEAM_LEDGER] — both directions, one anchor per seam, counterpart obligations, stale re-points

**Intra-package (arrow = composes; every edge lands equal-or-earlier wave; intra-wave edges point backward in the `[02]` listed order).** The WIRED-PIPELINE law realized (E2 killed): `owner#run → Cam` (W3); `Cam → {guard.Check per feed move, workholding.Condition, program neutral-AST assembly}` (W3, backward); `Run(Post{Motion, PostDialect}) → {program condition, dialect.Emit, magazine.Schedule, workholding conditioning, setups WCS}` (W4, backward to W1/W3/W4-earlier); `Run(Document{results}) → traveler → {projection, program, magazine, setups, tolerance, capability}` (W4, backward); `workholding.Condition → algebra.NestingOrder` (W3→W2 backward — the namespace-true re-home that BREAKS the workholding→posting forward edge); `guard → {skeleton.ClearanceAt, magazine.HolderEnvelope}` (W3→W2/W1); `motion.Adaptive → skeleton` (W3→W2); `setups → {workholding, guard}` (W3, backward); `removal → implicit` (W4→W3, the declared voxel seam); `probing → program` (W4, backward); `capability.Gate → owner#atoms(CapabilityVerdict)` (W4 mint, read input-carried at plan). Owner two-node discipline HOLDS: `owner#atoms` (Loop+Bulges, Edge3, ArcCenter, Move, PartTransform, ProjectionDir, CutterForm, ResidualStock, StockSnapshot, **CapabilityVerdict**, **ArtifactKind**, FabricationInput/Policy/Result, ContentKey seed) is upstream-only — every plane reads it, it reads only family's leaf axes; `owner#run` is terminal (8-arm dispatch: Cam·HiddenLine·Nest·Additive·Verify·Inspect·Post·Document) — composes the plane kernels, nothing composes it. The four cycle-breaks: `ProjectionDir` on owner#atoms breaks the former `owner↔projection` bidirectional; `ResidualStock` input-carry breaks `motion→removal→program→motion`; `CutterForm` on owner#atoms breaks `owner→cuttingdata→magazine→owner` (`[00]` ruling 2); `NestingOrder` re-home to `Geometry2D/algebra` breaks `workholding→posting`. `CapabilityVerdict` input-carry (graft E) breaks `capability→plan` exactly as `ResidualStock` does (produced W4, read as next-run input, never an intra-run back-edge). Graph acyclic (proof in `[11]`).

**Signature-lock — kernel seam member spellings, K1-K23 TABLE (graft A, from the LANDED `RASM-CS-GEOMETRY-DECISION.md` — the E14 "PENDING" branch is CLOSED; lock against the DECISION `FAB:NN` anchors, NOT the +14/+16-drifted GBRIEF line pins).** Each Fabrication page signature-locks these at its leg-1 pass and NEVER re-derives the interior; every entry threads the kernel `Op` value key with `Eff<Env>` carriage (K21), registers on the `IValidityEvidence`+`ValidityClaim` fold reaching `OpAcceptance` with zero oracle edits (K22), and rides `AtomProjection`/`ProjectionRow` output (K23). Leg-1 pass re-verifies each spelling via `assay api` against the restored assembly (or the `.api` tier when blocked).

| # | Kernel member (locked signature) | Namespace · fault | Consuming Fabrication page(s) | Ruled disposition carried | Leg |
|---|---|---|---|---|---|
| K1 | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Op? key = null)` — cases `Skeleton · Offset(JoinType×EndType corner rows) · Medial · Minkowski · Weighted · Clearance(probe)`; per-point clearance **RADIUS** first-class result field + `Clearance(Point3d probe)` arbitrary-probe query | `Rasm.Geometry.Offsetting` · 2416-2419 | `Geometry2D/arcs` (corner-row region offsets), `Toolpath/skeleton` (Medial + RADIUS), `Toolpath/motion` (engagement clearance) | **[V10]a kerf split** — kernel owns exact corner-row assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours ON TOP (one concern per stratum) | 2 |
| K2 | `Fin<CurveSkeleton> Skeletonize.Apply(SkeletonOp, Op? key = null)` — COMPOSES K1's clearance family (same RADIUS + `Clearance(Point3d probe)`, ONE result family across 2D/3D) | `Rasm.Geometry.Offsetting` · `SkeletonStalled 2417`/`CollapseStalled 2418` | `Toolpath/skeleton` (3D MCF curve-skeleton) | **[V10]d + [V2]a** — `Toolpath/Skeleton.cs` DIES UNCONDITIONALLY for `Offsetting.Apply`; page keeps ONLY the trochoidal constant-engagement WALK over the kernel clearance field | 2 |
| K3 | `Fin<SliceStack> Slicing.Apply(SliceOp, Op? key = null)` — ALL FOUR layer policies OPEN (uniform · adaptive · variable-by-slope · support-interface); oriented contours (outer CCW / holes CW); typed OPEN-chain rows; 5-channel SoA forest wire (layers · contours · nesting parent/child index arrays · open chains · elevations) | `Rasm.Geometry.Intersection` · `SectionFault 2425` | `Additive/slicing` | **[V2]b** — `Toolpath/slicing.Section` + O(n²) `Chain` DIE; `ARCH:48` "when realized" REALIZED; adaptive/support-interface layer rows are kernel slice-stack policy seeds on this lane's named demand, never a Fabrication layer engine | 3 |
| K4 | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Op? key = null)` — `IntersectOp.PlaneMesh` Section fold; oriented `Chain`; `IntersectResult` widened beyond loops-only | `Rasm.Geometry.Intersection` · 2424 | `Additive/slicing` (section primitive under K3) | consumed under K3; `FAB:48` | 3 |
| K5 | `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp, Op? key = null)` + `BooleanOp` smart enum — `MeshBoolean · PlanarOverlay · CellComplex`; Manifold tier-3 behind `ArrangementPolicy.ScaleCeiling = 1_000_000` | `Rasm.Geometry.Arrangement` · 2420+2423 | `Documentation/projection` (watertight `BooleanSolid` arm via `Arrangement.Apply(MeshBoolean)`/`ToMesh`) | **[V2]e / FAB:44** — `BooleanOp` anchor re-points `Processing/repair` → `Meshing/arrangement`; `projection`'s `Rasm.Geometry.Healing` import re-points | 4 |
| K6 | `Fin<DrawingProjection> View.Apply(ViewOp, Op? key = null)` — `HiddenLine · Silhouette · Section · Creases`; exact analytic Appel QI (zero sampling) | `Rasm.Geometry.Projection` · 2436-2439 | `Documentation/projection` | **[V2]c / [V3] / FAB:33+46** — the BSP interior DIES; "beside the in-folder BSP solver" clause dies; page PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at (`UIBRIEF [V7]`(b)) | 4 |
| K7 | `Fin<EncodedGeometry> Encode.Apply(PackOp, Op? key = null)` — `PackKind` gains **`toolpath`** case row + active-channel set | `Rasm.Geometry.Encoding` · 2444-2447 | `Posting/program` (committed motion stream residency) | **[V13]** — the channel-set alignment lands as ONE ledger row (Compute decodes residency, AppHost locks `EncodingKind.Toolpath`); NEVER a Fabrication-side encoder | 4 |
| K8 | `SignedDistanceFromMesh` (landed `fields.md` → `reconstruct.md` GWN → `index.md` BVH winding) — the ONE distance-field lane | `Rasm.Geometry` (`Analysis/fields`) | `Toolpath/surface` (target-proximity / gouge field, OCL fallback) | **[V8]** — the kernel mints no second SDF owner; PicoGK stays Fabrication's VOXEL lane (`Additive/implicit`, `Verify/removal`), never a second kernel SDF owner | 3 |
| K9 | `ContentHash.Of` — seed-zero federation entry (`XxHash128.HashToUInt128` reached THROUGH it) | `Rasm` (`Domain/identity`) | ALL egress: `program`, `nfp`, `implicit`, `production`, `removal`, `traveler`, `magazine`, `stock` | **[SEAM_AND_ENTRY_LAW]** — the ONE mint site; every raw `XxHash128`/`GenerateHash` is the second-hasher defect; content keys minted here keyed by the `ArtifactKind` discriminant (graft C), the durable-row fold carried as a Fabrication-AUTHORED demand (§D-2: PBRIEF `[V12]` ArtifactKind REFUTED/BLOCKED) | 1 atoms, per-plane |
| K10 | `Processing/geodesics.md` heat distance (via `fields.md` `Geodesic` case) | `Rasm.Geometry` | `Toolpath/surface` | geodesic-parallel / constant-stepover path LAYOUT | 3 |
| K11 | `Processing/extract.md` `ContourPolicy.MeshScalar` isolines | `Rasm.Geometry` | `Toolpath/surface` | pass extraction | 3 |
| K12 | `Processing/flow.md` streamline over `VectorField` | `Rasm.Geometry` | `Toolpath/surface` | flowline / morph rows | 3 |
| K13 | `Processing/segment.md` cross-field / stripe | `Rasm.Geometry` | `Toolpath/surface` | flank / swarf direction alignment | 3 |
| K14 | `Analysis/measure.md` `Bounds` enclosing-cylinder + `Meshing/delaunay.md` `LowerHull` + `cloud.md` rail | `Rasm.Geometry` | `Toolpath/motion` | `Turn` revolved stock envelope + clearing-stock hull (`[V11]` two-tier) | 3 |
| K15 | `Meshing/mesh.md` `MeshSpace` admission + `Processing/repair.md` `HealOp` (predicate-gated) | `Rasm.Geometry` | `Ingress/solid` | `OcctMesh` crosses to kernel vocabulary; dirty STL healed at the seam | 2 |
| K16 | `Processing/register.md` `AlignKind` ICP dispatcher (one `AlignmentPolicy`) | `Rasm.Geometry` | `Verify/probing` | probe-to-nominal datum best-fit | 4 |
| K17 | `Analysis/measure.md` `ConformanceMetric` over `ResidualSample` | `Rasm.Geometry` | `Verify/probing`, `Spec/capability` | measured-vs-nominal verdicts, pre-folded evidence | 4 |
| K18 | `Domain/stats.md` `Stat.Of` (Welford) / `Distribution.Of` (quantiles) / `StatContext.Tolerance` | `Rasm` | `Spec/capability` | streaming-moment half of Cp/Cpk/SPC (MathNet owns fit/Monte-Carlo half) | 4 |
| K19 | `Parametric/projections.md` `MotionInterpolation` (one-slerp owner) | `Rasm.Geometry` | `Kinematics/machine` | multi-axis orientation interpolation — never a second slerp site | 3 |
| K20 | `Analysis/select.md` `Faces` / `Curves.Draft` axis-ranked face decomposition | `Rasm.Geometry` | `Additive/production` | build-orientation overhang/bottom-area objective — never a hand-rolled normal classifier | 3 |
| K21 | `Domain/rails.md` — `Op` value-key threading + `Eff<Env>` carriage; receipts register into `IValidityEvidence`+`ValidityClaim` reaching the `OpAcceptance` oracle with ZERO oracle edits (a hand-rolled per-receipt `IsValid` is the dual-paradigm defect) | `Rasm` | every entry | substrate rail | per-plane |
| K22 | `Domain/validation.md` — input guards compose the admission vocabulary + the one `OpAcceptance` oracle | `Rasm` | every entry | substrate rail | per-plane |
| K23 | `Numerics/atoms.md` — typed output projection rides `AtomProjection`/`ProjectionRow`, never a `typeof(TOut)` switch | `Rasm` | every egress projection | substrate rail | per-plane |

**Cross-package (both directions; stale targets re-point):**

| Seam (Fabrication side) | Direction | Counterpart / re-point | Leg |
|---|---|---|---|
| `Documentation/projection ← Rasm/Drawing/view` | consumes | kernel `View.Apply → Fin<DrawingProjection>` (`ARCH:46` "beside the in-folder BSP solver" clause DIES; DECISION row 32) | 4 |
| `Documentation/projection ← Rasm/Meshing/arrangement` | consumes | kernel `Arrangement.Apply`/`ToMesh` watertight (`ARCH:43` re-points off `Rasm/Processing/repair`; `FAB:44` follows the kernel `[V5]` `BooleanOp` re-home; DECISION row 10) | 4 |
| `Documentation/projection → Rasm.AppUi/Render` | produces | `HiddenLineResult` receipt (`ARCH:47`; supersession insulated AT the receipt, `UIBRIEF [V7]`(b) — corrected from the brief's `[V6]`) | 4 |
| `Additive/slicing → Rasm/Meshing/intersect` | consumes | kernel `Slicing.Apply → Fin<SliceStack>` (`ARCH:48` "when realized" REALIZED; DECISION row 24) | 3 |
| `Toolpath/skeleton ← Rasm/Meshing/offset+skeleton` | consumes | kernel `Offsetting.Apply`/`Skeletonize.Apply` clearance-radius (`ARCH:22`; `Skeleton.cs` dies unconditionally, `[V10]d`; DECISION rows 8,12) | 2 |
| `Toolpath/surface ← Rasm/Processing/{geodesics,extract,flow,segment}+fields` | consumes | kernel on-mesh path-layout + SDF fallback (`[V5]`/`[V8]`; NEW ledger rows, K10-K14) | 3 |
| `Process/physics ← Rasm.Materials/Properties` | raw doubles | AEC-peer boundary (`ARCH:49` anchor reconciled: `physics.md:3` `physical-properties#MATERIAL_PROPERTY` is canonical over the `Properties` spelling) | 1 |
| `Nesting/stock → Rasm.Compute` | produces (forward demand) | `NestYield.WasteAreaMm2` (`ARCH:51` — the "waterfall landed / `CBRIEF [V12]`" claim REFUTED; genuinely ONE-SIDED/un-named Compute-side; a Fabrication-authored forward demand) | 3 |
| `Nesting/stock ← Rasm.Element/MaterialId` | consumes | NEW ledger row (was wired-undeclared, `stock.md:40`, E11) | 3 |
| `Ingress/profile ← Rasm.Bim/Exchange` | shared read pin | ACadSharp read codec (`ARCH:52` netDxf parenthetical → AppUi ACadSharp two-format WRITE leg, `UIBRIEF [V7]`) | 2 |
| `Posting/program → Rasm.Persistence` | blocked demand | `CutProgram` durable rows (`ARCH:53` off DELETED `Persistence/Schema` → content key via `ContentHash.Of`; the `ArtifactKind` index is `PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]` HELD-OPEN, NOT `PBRIEF [V12]` — §D-2 REFUTED) | 4 |
| `Nesting/nfp → Rasm.Persistence` | blocked demand | Placement/Remnant durable rows (`ARCH:54` off DELETED Schema → `ContentHash.Of`; same blocked card) | 3 |
| `Posting/program → Rasm/Drawing/pack` | residency ledger row | kernel `toolpath` `PackKind` (`GBRIEF [V13]`/DECISION row 30; channel-set alignment, NEVER a Fabrication encoder) | 4 |
| `{Tooling/magazine, Verify/probing} ← Rasm.AppHost` | decoded telemetry | MTConnect `-Common` model slice; BOTH consumers named (`APPHOST:210`: magazine tool-life FIRST, probing measured-feature SECOND) | 1,4 |
| `{Verify/removal, Documentation/traveler, Additive/production} → Rasm.Persistence` | blocked demand | `.cli`/3MF/NC1/snapshot/traveler egress content keys via `ContentHash.Of` (the `ArtifactKind` rows are the Fabrication-authored demand on the HELD-OPEN Persistence card) | 3,4 |

## [06]-[ROSTER_DELTA] — exhaustive 24-package disposition (graft G format; INTEGRATION-FIRST: the four dead admissions are MANDATES, realized as pages)

Central ownership `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.Fabrication.csproj`; folder-tier catalogs `libs/csharp/Rasm.Fabrication/.api/`, shared substrate `libs/csharp/.api/`. License gate: OSS + free-for-OSS-commercial admissible; LGPL native engines admissible as dynamic-link P/Invoke under the golden-fixture gate; AGPL REFERENCE-only. Every package is an explicit row (graft G — the base's grouped rows expand so Robots/MTConnect.NET-Common/ACadSharp/System.IO.Hashing carry their own disposition).

| # | Package | Action | Home page(s) | `.api` obligation + proof | Manifest motion |
|---|---|---|---|---|---|
| 1 | `OpenCAMLib` | **ADD** (LGPL-2.1; probe FEASIBLE) | `Toolpath/surface` | `api-opencamlib.md` NEW: `Operation` lifecycle (`setSTL`/`setCutter`/`setSampling`/`run`/`getCLPoints`), the `Cyl`/`Ball`/`Bull`/`Cone`/`Composite`Cutter → `CutterForm` ctor map, ~18-24-fn `extern "C"` shim ABI, RID assets (`macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64` SHARED `libocl`, all three present), libomp carriage, LGPL-2.1 dynamic-link note, golden-fixture gate. NO NuGet (feed-verified) | ADD to Fabrication label; RID assets recorded; `USE_OPENMP=OFF` build-policy row |
| 2 | `lib3mf` | **ADD** (BSD-2, official C# binding v2.5.x) | `Additive/production` | `api-lib3mf.md` NEW: 3MF core/production/beam-lattice writer; vendored official binding + RID-keyed native asset (NO NuGet — feed-verified; `IxMilia.ThreeMf` dominated); golden-fixture gate; PicoGK `Lattice` → beam-lattice map | ADD to Fabrication label; vendored binding |
| 3 | `PicoGK` | **KEEP-REALIZE** (integration mandate) | `Additive/implicit` (3 lanes), `Verify/removal` | `api-picogk.md`: DROP the `geometry3Sharp DMesh3` permanent-owner coupling (`:5,113,154`) → owned biarc fold + kernel mesh vocab; re-point `Persistence/Schema` → artifact index | held (`PROPS:84`) |
| 4 | `OcctNet.Wrapper` | **KEEP-REALIZE** (integration mandate) | `Ingress/solid` | `api-occtnet-wrapper.md`: re-point `:137 Rasm.Persistence/Schema` → `ContentHash.Of` egress; the `libTKHLR`/`libTKXCAF` SCOPE_LIMIT demand row re-verified each leg | held (`:83`) |
| 5 | `DSTV.Net` | **KEEP-REALIZE** (integration mandate) | `Ingress/steel` (read), `Posting/dialect` (NC1 emit model) | `api-dstv-net.md`: purge band-2500 (`:3,126`)→2700; purge netDxf (`:132,143`) | held (`:79`) |
| 6 | `SharpVoronoiLib` | **KEEP-REALIZE** (integration mandate) | `Toolpath/partition` (the `[V1]` lane the kernel supersession left this brief) | `api-sharpvoronoilib.md`: slim dual-ownership prose (`:3,18`) → Fabrication-sole (kernel `Rasm.csproj` has no reference) | held (`:87`; float-lane re-group LANDED) |
| 7 | `CavalierContours` | **RE-GROUP** "Kernel Geometry" → Fabrication (`[V10]a` stratum) | `Geometry2D/arcs` | `api-cavaliercontours.md`: `PosPlines` authority HOLDS (drives the `result.Positive`→`PosPlines[].Pline` fix); sole-consumed by Fabrication | VERIFY float-lane re-group landed (`:70/:77`) |
| 8 | `Clipper2` | **RE-GROUP** "Kernel Geometry" → Fabrication | `Geometry2D/algebra` | `api-clipper2.md`: clean | VERIFY float-lane re-group landed (`:71/:78`) |
| 9 | `RectangleBinPack.CSharp` | **RE-GROUP** vacated "Materials" → Fabrication | `Nesting/stock` (5 packers), `Nesting/nfp` (MaxRects fast-path) | `api-rectanglebinpack-csharp.md`: subset-superset proof holds; namespace fact `RectangleBinPacking` diverges from pkg id (`stock.md:41`). Feed 1.0.4 current | re-group from Materials label (`:165`) → Fabrication |
| 10 | `RectpackSharp` | **REMOVE** (redundancy) | (nfp fast-path re-homes to RBP) | `api-rectpacksharp.md` DELETE. Proof: strict subset of `RectangleBinPack.CSharp` (`PackingHints.FindBest` = the try-every-heuristic sweep, one `MaxRectsBinPack.Insert` fold); sole consumer nfp re-homes (row 18); `README:65` rejection rewrites | REMOVE from Fabrication label (`:85`) |
| 11 | `netDxf` | **VERIFY-REMOVE** | (none — AppUi's `[V7]` write leg) | netDxf-purge ripples: `README:33,64`, `ARCH:52`, `import.md:20`, `api-dstv-net.md:132,143`, `api-acadsharp:120` re-frame to the AppUi ACadSharp two-format leg | VERIFY-REMOVE surviving orphan (`:357`) |
| 12 | `netDxf.netstandard` | **VERIFY-REMOVE** | (none — Compute FEALiTE2D floor) | Leaves with the Compute `[V8]` FEALiTE2D.Plotting retirement (its lock-verified sole parent); a surviving pin under a parity-kept FEALiTE2D stays a transitive floor, NOT Fabrication residue | stays transitive floor if FEALiTE2D parity-kept (`:468`, NOT `:472`) |
| 13 | `geometry3Sharp` | **REPLACE-candidate** (abandoned 2019, `CSPROJ:17`) | `Posting/program` (owned Bolton biarc fold, line-sourced chains only) | `api-geometry3sharp.md`: `:106` "dropped CavalierContours" self-contradiction dies; `api-picogk:5,113,154` g3-as-sole-owner coupling re-frames. Leaves when the owned fold lands + Bim drops its mesh-importer (cross-package residual `Rasm.Bim.csproj:22`, RECORDED not executed) | held (`:81`); abandonment recorded on the golden fixture |
| 14 | `Robots` | **KEEP-REALIZE** (cell) | `Kinematics/cell` | `api-robots.md`: band-2500→2700 (`:5,94`); `[03]-[RESEARCH]` tail folds into `Packages`/`.api` | held (`:86`) |
| 15 | `MTConnect.NET-Common` | **KEEP** (model-only) | `Tooling/magazine`, `Verify/probing` | `api-mtconnect-net-common.md`: `GenerateHash → ContentHash.Of` reconcile (`:3,116`); re-point `:119 Persistence/Schema` → artifact index; transport half is AppHost livewire's | held (`:82`) |
| 16 | `ACadSharp` | **KEEP** (read-only) | `Ingress/profile` | `api-acadsharp.md`: `:120` stale write-leg → AppUi ACadSharp two-format leg | held (BIM label `:122`) |
| 17 | `MathNet.Numerics` | **VERIFY** (beta2 HOLD; first Fab consumer) | `Spec/capability` | The `6.0.0-beta2→5.0.0` downgrade the brief expected is SUPERSEDED by the geometry DECISION `[09]` (Compute `Integrate.OnCuboid` is 6.0-only → beta2 HOLDS, bump blocker recorded); overlay stays truthful | held `6.0.0-beta2` (`:—`) |
| 18 | `Riok.Mapperly` | **SHARED-MINE** (admitted substrate) | `Kinematics/cell`, `Tooling/magazine`, `Verify/probing`, `Ingress/steel` | DSTV-record→Loop + MTConnect rows + R3 seam (zero-reflection partials); `api-mapperly.md` shared, VERIFIED | held (Foundational Core `:30`) |
| 19 | `System.Numerics.Tensors` | **SHARED-MINE** | `Toolpath/surface`+`motion`, `Nesting/nfp`, `Spec/capability` | SIMD-lower the hot sampling folds (engagement/scallop/NFP-sweep/capability-batch); `api-tensors.md` shared | held (`:42`) |
| 20 | `CommunityToolkit.HighPerformance` | **SHARED-MINE** | `Additive/implicit`, `Verify/removal`, `Toolpath/surface` | `Span2D`/`Memory2D` 2D grids (grayscale/uncut/engagement rasters); `api-highperformance.md` shared | held (`:38`) |
| 21 | `NodaTime` | **SHARED-MINE** | `Documentation/traveler`, `Verify/probing`, `Tooling/magazine`, `Spec/capability` | `Instant` traveler/probing/tool-life/SPC stamps; `api-nodatime.md` shared | held (`:39`) |
| 22 | `QuikGraph` | **SHARED-MINE** | `Fixturing/setups` | precedence/datum-lineage graph (magazine eviction greedy stays); shared tier | held (`:41`) |
| 23 | `System.IO.Hashing` | **KEEP** (via `ContentHash.Of` ONLY) | all egress | `api-hashing.md` SLIMS to the `api-unitsnet.md` thin-overlay pattern; the false "no shared tier" law (`:3,88`) dies, defers to shared `libs/csharp/.api/api-hashing.md`, names `ContentHash.Of` the one mint | held (`:31`) |
| 24 | `UnitsNet` | **OVERLAY-EXTEND** | cut-parameter boundary + `Spec/tolerance` | `api-unitsnet.md` overlay extends Speed/Length/RotationalSpeed/Pressure → +Force/Power/Temperature/Angle/Torque (thin-overlay pattern) | held (`:43`) |

`.api` authoring: 2 NEW (`api-opencamlib.md`, `api-lib3mf.md`), 1 DELETE (`api-rectpacksharp.md`), 9 re-point/slim (hashing, sharpvoronoilib, picogk, occtnet, mtconnect, dstv-net, robots, acadsharp, geometry3sharp). Substrate `api-languageext.md` VERIFIED (never re-authored).

REFERENCE records (never vendored; silence ≠ oversight): `gsGCode` (parse-arm prior art), `gsSlicer` (FFF enrichment), MillSimSharp (voxel-verify corroboration), CAMotics/PrusaSlicer/CuraEngine/libslic3r (concern-validity proofs), FreeCAD `Adaptive2d`+machinability (`kc` schema), FreeCAD_SheetMetal (K-factor unfold), Tweaker-3 (orientation objective), UVtools (resin machine-format breadth), PySLM (Powder hatch/scan), QIF/ISO 23952 (Spec data-model authority), NIST RS274NGC v3 (dialect grammar authority). REJECTED: Fanuc FOCAS (distributor-gated), STEP-NC/ModuleWorks/EncyCAM (pay), Aspose.3D/ODA/CadEx (proprietary), PathCAM (GPL dominated), GcodeParserSharp (no modal-group state), Occt.NET/Macad.Occt/Ab4d.OpenCascade/Xbim.Geometry.Occt (OCCT-wrapper supersession sweep re-ran, `OcctNet.Wrapper` HOLDS). Stratum-split: Manifold is the KERNEL's `[V5]` tier (Fabrication consumes booleans at the arrangement seam, never a pin); MTConnect full-stack/OPC-UA/umati + BACnet are AppHost transport.

## [07]-[VERDICT_DISPOSITIONS] V1-V10

| V | Disposition |
|---|---|
| V1 | RATIFIED at `[01]`: 13 folders ↔ 13 TIER-1 namespaces BIJECTIVE, every folder ≥2 pages, folder=namespace=fault-cluster 1:1. The six folder-overrides-namespace ratifications execute in-MOVE (family/faults/physics→`Process`, magazine→`Tooling`, import→`Ingress`, projection→`Documentation`); the four namespace-keeping moves (clipper split→`Geometry2D`, kinematics→`Kinematics`, slicing→`Additive`, workholding→`Fixturing`) land the folder onto the realized namespace. `Polygon/` dies. The `Process` type→`ProcessKind` collision-break (`[00]` ruling 1) is the ONLY resolution preserving literal 1:1 under the brief-binding "faults→Process"; `Machine` UNRENAMED (no collision — package-mining's `MachineKind` rename REJECTED). `ARCH:[01]/[02]/[03]` + README router + faults taxonomy rewrite in the wave-1 motion; the two-node `owner#atoms`/`owner#run` split is the one ratified acyclicity exception. |
| V2 | The five author-kernels re-frame to CONSUMER under the RULED dispositions (K1-K6/K8 signature-locked): (a) skeleton row 12 keeps ONLY trochoidal WALK, `Skeleton.cs` dies unconditionally (K2, `[V10]d`); (b) slicing row 24 re-routes to `Slicing.Apply`, `Section`/O(n²)`Chain` die (K3, `ARCH:48` fired); (c) projection row 32 BSP dies for `View.Apply` (K6), keeps the watertight `Arrangement` arm (K5) + PRESERVES `HiddenLineResult`; (d) arcs row 8 composes the `[V10]a` split (K1 corner-row assembly consumed, CavalierContours kerf lane Fabrication-side, absorbed from clipper); (e) booleans/SDF kernel-consumed (K5/K8). The FIVE `[RESEARCH]` tails purge (§D-4: kinematics:122, skeleton:199, slicing:219, stock:317, program:379); `rg '\[RESEARCH\]'` → zero. |
| V3 | The pipeline islands wire into ONE fold (E2 killed): `Cam` consults `guard.Check` per feed move + `workholding.Condition` + assembles the neutral `CutProgram` AST; `Run(Post)` consumes `magazine.Schedule` (real Tnn/M6/G43) + `dialect.Emit`; the arc rail REALIZES (kerf/lead/adaptive read `arcs` arc-native + `Loop.Bulges`, g3 line-sourced only); `owner#atoms` lands the widened `Loop`+`Bulges`+3-arg-ctor+`BulgeAt`; the `result.Positive`→`PosPlines` phantom fix (row 8); Fixturing hardens (segment-vs-polygon keep-out, total `ForHolding`, per-kind footprints, `Condition` wired); the Placement-vs-Motion conditioning asymmetry states on the arm; the work-offset chain wires end-to-end (`setups` owns setup→WCS, posting renders, `probing` reconciles). |
| V4 | Posting becomes a real per-dialect generator (rows 2, 27, 30): `PostDialect` gains grammar-family capability columns; `dialect.Emit` a generated total Switch (flat 3-column render dies); the AST grows canned-cycle/subprogram/macro nodes with typed R/Q/P slots (per-family lowering = node expansion); rows widen as DATA (840D/TNC/OSP/Fagor/Centroid); NC1 emit target on the `Run(Post)` case; G-code INGRESS = the `Parse` arm with the NIST modal-group state machine (`Parse ∘ Emit = id`); data-vs-lowering split (axis family's, folds dialect's); the two look-ahead planners read ONE motion-dynamics law homed on `machine`. |
| V5 | `CutStrategy` widens to production CAM on the `(RemovalModality, CutStrategy)` cross-product + dimensionality column (rows 20-23): waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family; every stub becomes a REAL generator (Turn ZX sweep over the `Bounds` cylinder K14, helical distinct, Peck canned, SliceWalk orderer); OpenCAMLib owns cutter POSITIONING (probe FEASIBLE, row 22; author-kernel SDF drop-cutter K8 the recorded fallback), path LAYOUT kernel-composed (K10-K13); `Toolpath/partition` realizes the Voronoi mandate (row 23); engagement reads the medial clearance AND the physics chip-load budget; `Kinematics/machine` closes the 5-axis gap (rotary topology + TCP/RTCP, kernel one-slerp K19); the `CutterForm` axis lands (four consumers) — the type on `owner#atoms` (`[00]` ruling 2), the projection + machinability on `Tooling/cuttingdata`. |
| V6 | Additive becomes a folder (rows 24-26): slicing keeps FFF/DED planar over the kernel slice-stack (K3, `[V2]b`), layer-height demand UPSTREAM as kernel policy rows; implicit realizes PicoGK to depth (TPMS/gyroid/lattice/support/resin-powder `.cli`/grayscale, ALC-firebreak); production owns orientation (kernel `Analysis/select` K20) + machine profiles + lib3mf 3MF; physics mints Resin/Powder/DED budgets. gyroid corrects from the `InfillPattern`-row false collapse to the voxel lane. |
| V7 | NEW Verify plane (rows 28, 31): removal owns voxel material-removal (PicoGK `BoolSubtract`, gouge/uncut/overcut/air-cut receipts) PRODUCING `ResidualStock` + per-setup content-keyed `StockSnapshot` (both mint on `owner#atoms` wave 1 so Verify produces while rest/setups READ input-carried — run N feeds run N+1); guard stays the per-move DESIGN-time floor (two owners, two moments, both wired); probing realizes the queued card (G31/G38 rows, kernel `register.md` ICP K16, kernel `ConformanceMetric` K17 verdicts, QIF→capability, AppHost decoded/manual ingress); the PicoGK net9/native boundary declares ONCE on implicit; `FabricationPolicy` grows `Verify`/`Inspect` — and the `Run(Post)`/`Run(Document)` egress cases (graft B) that make re-post and traveler-compose one-entry. |
| V8 | NEW Spec plane (rows 20, 29): tolerance owns the typed GD&T vocabulary (ISO 286 generated fit rows, ISO 1101/ASME Y14.5 FCF+datums, ISO 1302 finish, MMC/LMC, QIF-aligned); capability owns Cp/Cpk/Pp/Ppk (streaming half on kernel `Stat`/`Distribution` K18, fit+Monte-Carlo on MathNet) + SPC; the spec DRIVES process (Ra→scallop stepover `surface` consumes, IT-grade→allowance conditions pass planning, the plan-time Cpk GATE PRODUCES the `CapabilityVerdict` minted on `owner#atoms` — graft E — the plan reads it input-carried, failing a process before material is cut, spec→plan→verify→capability one rail); traveler owns setup-sheet/traveler document MODELS via the `Run(Document)` case; UnitsNet overlay extends. |
| V9 | physics identity (row 3): ONE `Material` per physical material with `Map<RemovalModality, ModalityPhysics>` (stainless/stainless-abrasive/mild-steel-thermal collapse); `RemovalBudget.Erosion` lands (the `erosion→ThermalBudget` conflation dies) beside Resin/Powder; `CuttingData` moves to `Tooling/cuttingdata` and deepens to the Kienzle `kc` machinability model behind a data-ingress arm (FreeCAD-cards-removed gap honest); the `0.0012` force literal reads `kc`; the dead `Overrides` deletes onto the ingress arm; `90.0` demotes behind the table. |
| V10 | Every stale-law reference closes in one motion (E10): the R6 25xx census (`ARCH:16`, `kinematics:3`, `TASKLOG:44` 2505-2509, `api-dstv-net:3,126`, `api-robots:5,94`) → 2700; the netDxf purge (`README:33,64`, `ARCH:52`, `import:20`, `api-dstv-net:132,143`, `api-acadsharp:120`) → the AppUi ACadSharp two-format leg, pins VERIFY-REMOVE; `ARCH:63` scheduler overclaim → `Fixturing/setups`; `[GUARD]-[QUEUED]` closes COMPLETE; `api-geometry3sharp:106` self-contradiction dies; `api-hashing:3,88` "no shared tier" slims; `api-sharpvoronoilib:3,18` dual-ownership → Fabrication-sole; nfp's two logic defects land (`Stock.Of` full-dimension hash, `Ty*1e6+Tx`→tuple, NFP frame proven). |

## [08]-[EVIDENCE_DISPOSITIONS] E1-E14

| E | Disposition |
|---|---|
| E1 | The four zero-consumer admissions realize as pages (INTEGRATION-FIRST, `[06]` rows 3-6): PicoGK→implicit+removal, OcctNet→solid, DSTV→steel+NC1, SharpVoronoiLib→partition. README `[IMPLICIT_VOXEL]`/`[SOLID_INGRESS]`/`[STEEL_FABRICATION_EXCHANGE]`/`[VORONOI_TESSELLATION]` charters become fence-true. |
| E2 | The three pipeline islands wire (V3): `Cam→guard.Check` (row 21→13), `Run(Post)→magazine.Schedule` (row 30→5), `workholding.Condition` wired + composing `NestingOrder` (row 14→7). The `Run(Cam)→{Guard,Workholding,Magazine}` + `Run(Post)→dialect.Emit` fold EXISTS on disk. |
| E3 | The arc rail realizes (V3): `owner#atoms` lands `Loop`+`Bulges`+`BulgeAt`+3-arg-ctor (row 1); `arcs` reads them + fixes `result.Positive`→`PosPlines[].Pline` (row 8, absorbing clipper's `ArcAlgebra`); `program` reads arc-native offsets (g3 line-sourced only, row 30); the live phantom is the `.Positive` accessor. |
| E4 | Post thinness dies (V4, rows 2/27/30): PostDialect grammar columns, generated `Emit` Switch, structured cycle/subprogram/macro AST nodes, per-dialect code override; the dialect-invariant `Code` table dies; 9-of-21 stub GCommand rows become real emitted blocks or DATA rows. Byte-diversity across the F1 five targets is the kill-test. |
| E5 | Stub generators become REAL (V5, row 21): radial-sweep/helical de-alias from Turn, Turn a true ZX sweep over the `Bounds` cylinder (BarStock real), Peck canned-cycle geometry, SliceWalk a perimeter/infill/travel orderer; the 3D-surface family lands on `surface` (row 22). |
| E6 | physics identity map (V9, row 3): per-modality `Map`, Erosion/Resin/Powder budgets, dead `Overrides` deleted, `90.0` demoted; `0.0012` force → the `kc` column on `cuttingdata` (row 6). |
| E7 | HLR not-CAD-grade dies (V2c, row 32): the centroid BSP + per-edge average depth + undeduped mesh-edge soup + `1e6` eye + the PHANTOM `Orient2D` import all die for the kernel `View.Apply` (K6); the watertight arm (K5) + `HiddenLineResult` receipt preserved (AppUi insulated). |
| E8 | Coverage silences close: waterline/scallop/pencil/rest/swarf (row 22), NC verification (row 28), production specs (rows 20/29/33), additive supports/orientation/profiles/3MF (rows 25/26), G-code READ (row 30 Parse), 5-axis rotary topology (row 17), typed CutterForm (owner#atoms + row 6), spec→plan derivation (row 20). |
| E9 | Identity/logic defects land: `Stock.Of` full-dimension hash via `ContentHash.Of` (row 18), `Ty*1e6+Tx`→tuple (row 18), NFP frame proven/fixed (row 18), workholding segment-vs-polygon + total `ForHolding` + per-kind footprints (row 14). The 3 raw `XxHash128` mints route the one `ContentHash.Of` entry (K9). The `ArtifactKind`-on-owner#atoms spine (graft C) prevents an egress folder re-minting the second-hasher defect. |
| E10 | Stale governance closes in the wave-1 V1 motion + the leg touching each fact (V10): band-2500→2700 corpus+`.api`, netDxf purge, `Persistence/Schema`→artifact-index/`ContentHash.Of`, `TASKLOG:44` codes, `IDEAS:27` closes, `ARCH:63` re-scopes, the two `.api` self-contradictions die. |
| E11 | Ledger gaps land (`[05]`): `stock←Rasm.Element/MaterialId` NEW row; the intra-package web declared (magazine→guard/workholding, guard→skeleton, workholding→algebra.NestingOrder, motion→skeleton/machine/cell); `ARCH:51` Compute rollup RE-FRAMED to a one-sided forward demand (§D-2 REFUTED, NOT "waterfall landed"); `physics.md:3`↔`ARCH:49` anchor reconciled. |
| E12 | Redundancy/overlay: RectpackSharp REMOVE (row 10, ⊂ proof); `api-hashing` "no shared tier" slims (row 23); `magazine.Runout` reads the real measurement (row 5); `README:65` MaxRect/BinPack rejection rewrites (RBP subsumes RectpackSharp). |
| E13 | Page-craft: the FIVE `[RESEARCH]` tails purge (V2, §D-4 corrected — the brief's 3 undercounts stock:317 + program:379); the false "no managed library exists" seals (skeleton:3, slicing:3,221) die (the kernel owners landed). |
| E14 | Upstream bindings signature-lock (`[05]` K1-K23 TABLE): against the LANDED geometry DECISION anchors (E14 "PENDING" branch CLOSED), NOT the +14/+16-drifted GBRIEF pins (§D-1); the two `[V12]` reciprocals DEMOTED (`[00]` ruling 3, §D-2); `UIBRIEF [V6]`→`[V7]` corrected; the kernel members K1-K9 + suites K10-K20 + rails K21-K23 locked; leg-1 `assay api` re-verify. |

## [09]-[DELTA_DISPOSITIONS] — brief `[03]-[CAPABILITY_ESCALATION]`, all 14 planes

| Plane (now→target) | Homed at (rows) |
|---|---|
| Process axes owner/family/faults (8→9.5) | Rows 1,2,4 — the 8-case entry (Additive/Verify/Inspect/Post/Document), dialect grammar columns, CutStrategy dimensionality, KinematicClass rotary topology, typed fault payloads + 12 new arms, `Loop.Bulges` ratified, `ProcessKind` collision-break, ArtifactKind + CapabilityVerdict on owner#atoms, substrate rails (K21-K23). |
| Physics + machining data (6→9) | Rows 3,6 — per-modality identity map, Erosion/Resin/Powder, Kienzle `kc` on `cuttingdata`, data-ingress arm, dead `Overrides` deleted. |
| Tooling (8→9.5) | Rows 5,6 — magazine wired into Post, cuttingdata page, typed `CutterForm` (four consumers, type on owner#atoms), tool-life AppHost seam, `Runout` policy-typed. |
| Geometry2D substrate (8→9.5) | Rows 7,8 — two-owner split (both absorb from clipper — line-space→algebra, arc-space→arcs), `[V10]a` siting (K1), `PosPlines` phantom fix, arc rail consumed downstream, `NestingOrder` re-home. |
| Ingress (5→9.5) | Rows 9,10,11 — profile move, OcctNet solid (kernel `MeshSpace`/`HealOp` seam K15), DSTV steel, ONE polymorphic `Ingress.Admit(IngressSource)` emitting kernel-admissible geometry. |
| Toolpath subtractive CAM (5→9.5) | Rows 12,13,21,22,23 — real generator arms, waterline/scallop/pencil/rest/3+2/swarf, OpenCAMLib surface (path layout kernel-composed K10-K13), Voronoi partition, engagement+finish laws, guard wired, skeleton→kernel-medial (K2). |
| Kinematics (7→9.5) | Rows 16,17 — robot cell held, 5-axis machine topology + TCP/RTCP, kernel one-slerp (K19), one motion-dynamics law with posting. |
| Additive (4→9.5) | Rows 24,25,26 — kernel slice-stack consumption (K3), PicoGK implicit/lattice/support/resin-powder, orientation owner (K20), machine profiles, lib3mf 3MF, budget cases. |
| Nesting (8.5→9.5) | Rows 18,19 — `Stock.Of` identity fix, NFP frame proof, Persistence/Compute seam re-anchors, MaterialId ledger row. |
| Fixturing (6→9.5) | Rows 14,15 — segment-exact keep-out, per-kind footprints, `Condition` wired, multi-fixture setups owning WCS, op-N vs op-N-1 snapshot. |
| Posting (6→9.5) | Rows 27,30 — grammar families + canned/macro/subprogram + WCS + cutter-comp + arc-mode, per-dialect overrides, structured AST nodes, NC1 emit, modal-state `Parse`, `CutProgram` content key + ArtifactKind, the `Run(Post)` one-entry lowering. |
| Verify (—→9.5) | Rows 28,31 — voxel material-removal receipts, residual-stock field, per-setup snapshots, probing cycles + measured features on AppHost ingress, kernel ICP best-fit (K16), `ConformanceMetric` verdicts (K17). |
| Spec (—→9.5) | Rows 20,29 — ISO 286/1101/1302 + ASME Y14.5 QIF-aligned vocabulary, Cp/Cpk/SPC over kernel `Stat`/`Distribution` (K18) + MathNet, process-driving derivation rows + plan-time Cpk gate producing the `CapabilityVerdict`. |
| Documentation (6→9.5) | Rows 32,33 — projection as kernel-`DrawingProjection` consumer (K6, preserved `HiddenLineResult`), setup-sheet/traveler owner composing views/tools/fixtures/specs via the `Run(Document)` case (widest fan-in). |

## [10]-[CARD_DISPOSITIONS] — full IDEAS + TASKLOG pool (23 cards)

**IDEAS (7):** `[GUARD]-[QUEUED]` → CLOSE COMPLETE (the guard page landed; `IDEAS:27`/`ARCH:63` realized-but-open closes, `IDEAS.md`/`README:21` update). `[PROBING]-[QUEUED]` → realizes into `[V7]` `Verify/probing` (row 31; the G31/G38 cycle vocabulary, kernel-ICP datum best-fit, measured-feature receipts). `[MULTI_FIXTURE_SCHEDULE]-[QUEUED]` → realizes into `[V1]` `Fixturing/setups` (row 15; QuikGraph precedence graph, WCS assignment, op-N snapshot admission). `[DRL_NEST_POLICY]-[BLOCKED]` → STAYS BLOCKED (the injected-`Func<NoFitPolygon,PartTransform,double>` delegate column is the standing strata-correct shape; closes when Compute lands its side). `[CSG_SILHOUETTE]-[COMPLETE]` → HOLD (the watertight `Arrangement.Apply`/`ToMesh` arm is PRESERVED on row 32 as projection moves to Documentation). `[MAGAZINE]-[COMPLETE]` → RE-OPEN as a leg-1/leg-4 obligation: the card claims "G43/M6/Tnn blocks emitted by program.md" — E2 FICTION; the wire realizes on rows 5→30. `[FEEDRATE_LOOKAHEAD]-[COMPLETE]` → HOLD (the jerk-limited planner MATH preserved on row 30; now reads the one motion-dynamics law from `machine`, row 17).

**TASKLOG (16):** `[NFP_DRL_POLICY]-[BLOCKED]` → STAYS BLOCKED (Compute-side, strata-forbidden reference; realizes `[DRL_NEST_POLICY]`). COMPLETE cards HELD with rebuild-carried deltas: `[CSG_WATERTIGHT_SILHOUETTE]` (watertight arm preserved, row 32); `[TOOLPATH_STRATEGY_MODALITY_FACTOR]` (the `(RemovalModality,CutStrategy)` Switch grows the dimensionality column, row 21); `[POST_DIALECT_OVERRIDE_SEAM]` (the override resolves against the grammar-family axis, rows 2/30); `[TROCHOIDAL_ENGAGEMENT_LIMIT]` (the WALK survives, the wavefront dies, row 12; the `Option<ArcCenter>` chord feeds the arc-native rail not biarc, row 30); `[NEST_MULTISHEET_SCHEDULE]`/`[NEST_REMNANT_LINEAGE]` (held, the `ContentHash.Of` identity fix + frame proof land, row 18); `[RECTPACK_FASTPATH_ADMISSION]` → SUPERSEDED (RectpackSharp REMOVE, re-home to RBP MaxRects, row 10/18); `[BIARC_ARC_EMISSION_ADMISSION]` → RE-SCOPED (g3 retained ONLY for line-sourced chains; the arc-native rail retires it for arc-sourced, row 30; geometry3Sharp REPLACE-candidate); `[ACADSHARP_SPLINE_INSERT]` (held, moves to `Ingress/profile`, row 9); `[CLIPPER_INNER_FIT]` (held on `Geometry2D/algebra`, row 7); `[FABRICATION_FAULT_BAND_DEEPENING]` → the stale `TASKLOG:44` 2505-2509 codes re-point to 2701-2710 (row 4/E10); `[TOOL_CUTTING_DATA_TABLE]` → the `CuttingData` table MOVES to `Tooling/cuttingdata` + deepens to Kienzle `kc` (row 6, donor excision); `[CLIPPER_VARIABLE_KERF]` (held on `algebra`, row 7); `[FAULT_REBAND_2700]` → the 2701-2710 band is the frozen Layer-1 the `[04]` registry builds ON; `[STOCK_NEST_MOVE]` (held; the `WasteAreaMm2→Compute` ripple RE-FRAMED to a one-sided forward demand, row 19, §D-2).

**IDEAS RESEED (named growth rows, not build-leg pages):** sheet-metal bend/unfold (DSTV `KA` decode lands with `Ingress/steel`; K-factor bend-allowance the Fabrication overlay over the kernel `develop.md` isometric unroll, `GBRIEF [V2]`, never a second unroll engine); 5-axis simultaneous swarf refinement; conversational-dialect breadth beyond the seed rows; the `InCircle`-analog spec-growth (AP242 PMI surfacing pressure on OcctNet); the queued `FabricationProjector: IElementProjection` seam row.

## [11]-[LEG_PARTITION] — acyclic, engine-invocation rows

**Acyclicity proof over the `[05]` ledger:** every edge lands equal-or-earlier wave; intra-wave edges point backward in the `[02]` listed order. Wave 1 composes only landed kernel floors + backward (owner#atoms→family; magazine→owner#atoms; cuttingdata→{owner#atoms, magazine, physics} backward; physics→family). Wave 2: algebra→owner#atoms(W1); arcs→{algebra, kernel Offsetting K1, owner#atoms} backward; profile/solid/steel→{owner#atoms, kernel MeshSpace/HealOp K15} backward; skeleton→kernel(K2,landed). Wave 3 listed order guard→workholding→setups→cell→machine→nfp→stock→tolerance→motion→surface→partition→slicing→implicit→production has guard→{skeleton W2, magazine W1}, workholding→algebra(W2), setups→{workholding, guard} backward, motion→{guard, workholding, cell, skeleton W2, arcs W2} backward, surface→{tolerance, kernel K10-K14}, production→implicit backward — all intra-W3 edges point backward. Wave 4 listed order dialect→removal→capability→program→probing→projection→traveler has program→{dialect, magazine W1, workholding W3, setups W3, arcs W2} backward, probing→program backward, traveler→{projection, program, magazine, setups, tolerance, capability} backward (the `Run(Document)` widest fan-in), removal→implicit(W3) backward. The 8-arm `owner#run` terminal dispatch (Cam·HiddenLine·Nest·Additive·Verify·Inspect·Post·Document) composes plane kernels only; nothing composes owner#run. FOUR page-cycle breaks: `owner↔projection` broken by `ProjectionDir` on owner#atoms; `motion→removal→program→motion` broken by `ResidualStock` input-carry; `owner→cuttingdata→magazine→owner` broken by `CutterForm` on owner#atoms; `workholding→posting` broken by `NestingOrder` re-home to `Geometry2D/algebra`. `capability→plan` broken by `CapabilityVerdict` input-carry (graft E — produced W4, read as next-run input, NEVER an intra-run back-edge, identical to `ResidualStock`). Zero upward strata edges (Materials/Compute raw-double/delegate/seam-row only; Persistence/AppHost/AppUi seam-ledger counterparts only). The `owner` two-node split (owner#atoms upstream, owner#run terminal) needs NO physical page split — its ledger passes the two-node discipline; leg order (1→4) is a valid topological sort of the ledger DAG.

| Leg | Engine invocation | Targets (dependency order) + riders |
|---|---|---|
| 1 | `Workflow(rebuild.js, {targets: [Process/owner.md, Process/family.md, Process/physics.md, Process/faults.md, Tooling/magazine.md, Tooling/cuttingdata.md], brief: "RASM-CS-FABRICATION-DECISION.md"})` — rebuild ×4, new ×2 (magazine move-dest absorb {into: Tooling/magazine.md, from: Process/magazine.md} + deletePages [Process/magazine.md]; cuttingdata new) | + the `[06]` full roster reconciliation (OpenCAMLib/lib3mf `.api` authored, RectpackSharp REMOVE + `.api` delete, netDxf VERIFY-REMOVE, the four re-groups, api-hashing slim, UnitsNet overlay extend); the V1 ratification sweep (ARCH `[01]/[02]/[03]` 13-folder partition + README router + faults taxonomy + `ProcessKind` collision-break); the 8-case entry + ArtifactKind + CapabilityVerdict mints; the physics→cuttingdata donor excision; magazine tool-life AppHost seam; band-2500→2700 corpus+`.api` purge |
| 2 | `Workflow(rebuild.js, {targets: [Geometry2D/algebra.md, Geometry2D/arcs.md, Ingress/profile.md, Ingress/solid.md, Ingress/steel.md, Toolpath/skeleton.md], brief: "RASM-CS-FABRICATION-DECISION.md"})` — new ×5 (algebra split-absorb {into: Geometry2D/algebra.md, from: Polygon/clipper.md} + arcs split-absorb {into: Geometry2D/arcs.md, from: Polygon/clipper.md} + deletePages [Polygon/clipper.md]; profile move-absorb {into: Ingress/profile.md, from: Polygon/import.md} + deletePages [Polygon/import.md]; solid/steel new), rebuild ×1 (skeleton — Skeleton.cs dies) | + `Polygon/` folder deletes; the `[V10]a` split (K1) + `PosPlines` fix; clipper's `ArcAlgebra` carried into arcs (graft F); `ARCH:22/48` kernel-consumer ripples (K1/K2); netDxf-purge frame rewrite; the OcctNet `MeshSpace`/`HealOp` seam (K15); `NestingOrder` lands on algebra |
| 3 | `Workflow(rebuild.js, {targets: [Toolpath/guard.md, Fixturing/workholding.md, Fixturing/setups.md, Kinematics/cell.md, Kinematics/machine.md, Nesting/nfp.md, Nesting/stock.md, Spec/tolerance.md, Toolpath/motion.md, Toolpath/surface.md, Toolpath/partition.md, Additive/slicing.md, Additive/implicit.md, Additive/production.md], brief: "RASM-CS-FABRICATION-DECISION.md"})` — improve ×3 (guard, nfp, stock), new ×9 (workholding/cell/slicing move-absorbs + setups/machine/surface/partition/implicit/production/tolerance) + deletePages [Nesting/workholding.md, Toolpath/kinematics.md, Toolpath/slicing.md], rebuild ×1 (motion) | + the OpenCAMLib compose (probe FEASIBLE, K10-K14 path layout, K8 SDF fallback) + lib3mf compose; the E9 nfp identity/frame fixes; `stock←MaterialId` row; `ARCH:51` Compute demotion to forward demand; the wired-pipeline fold realizes (Cam→guard/workholding + neutral CutProgram AST); tolerance staged early (surface reads Ra→scallop within-wave) |
| 4 | `Workflow(rebuild.js, {targets: [Posting/dialect.md, Verify/removal.md, Spec/capability.md, Posting/program.md, Verify/probing.md, Documentation/projection.md, Documentation/traveler.md], brief: "RASM-CS-FABRICATION-DECISION.md"})` — new ×6 (dialect/removal/capability/probing/traveler + projection move-absorb {into: Documentation/projection.md, from: Posting/projection.md} + deletePages [Posting/projection.md]), rebuild ×1 (program) | + the CutProgram content key + ArtifactKind + Parse arm; the `Run(Post)`×5 / `Run(Document)` one-entry egress (graft B) consuming magazine/workholding/setups + dialect.Emit; the CapabilityVerdict plan-gate closure; the geometry3Sharp REPLACE-candidate (Bolton biarc owned); `ARCH:47` HiddenLineResult insulation (K6); probing AppHost seam (K16/K17); the four `[12]` acceptance dry-runs compose; the blocked Persistence egress demands recorded, never composed |

Residuals resolve per leg before the next launches (`resolve-residuals` on each non-empty `hard_residual`); `README.md`/`ARCHITECTURE.md`/`TASKLOG.md`/`IDEAS.md` close in the leg that changes their facts; residuals naming later-leg pages transfer forward explicitly. Campaign acceptance closes on the artifact — the four `[12]` dry-runs compose entirely from rebuilt fences; a dry-run reaching a missing owner reopens the owning verdict as a residual.

## [12]-[FLAGSHIP_DRY_RUN_CLOSURE] — F1-F4 backward traces (graft D; the DECISION's dry-run-closure proof)

Each flagship is a chain of `Fabrication.Run` invocations over the 8-case policy; the terminal invocation projects a content-keyed artifact. Every page is pulled into existence by ≥1 flagship terminal (or the SharpVoronoiLib integration mandate). A trace reaching a missing owner reopens the owning verdict — all four close on rebuilt fences.

**F1 — five-target posting suite** (`[05](b)`; word-address ×3 grammar-true + conversational + additive + NC1 steel). Artifacts: `fanuc.nc`/`haas.nc`/`grbl.nc` (word-address, grammar-true: canned cycles G81-89/G76 single blocks, macro-B `#`-vars/WHILE, M98/M99 subprograms, G54-59 WCS, G41/42 comp, IJK/R arc mode — each DISTINCT), `klartext.h` (Heidenhain conversational), `marlin.gcode` (FFF additive), `part.nc1` (DSTV steel) — six byte-diverse files, each content-keyed by `ArtifactKind`; `Parse ∘ Emit = id` on the word-address subset. Backward: `Run(Cam)` → `Motion` carrying the neutral content-keyed `CutProgram` AST ⇒ `Toolpath/motion` (row 21), `Posting/program` (row 30). Then `Run(Post{Motion, dialectᵢ})` ×5 ⇒ `Posting/dialect` (generated grammar switch over `PostDialect`, row 27) + `Process/family` (the `PostDialect` axis = seed DATA, row 2). NC1 = a sibling emit on the `Post` fold over the DSTV record tree ⇒ `Ingress/steel` (row 11). `Parse` arm (NIST modal-group state machine) ⇒ `Posting/program`. Content key ⇒ `owner#atoms` `ArtifactKind` + `ContentHash.Of` (row 1). Fault arms (inexpressible-cycle/macro-unsupported/block-cap) ⇒ `Process/faults` `DialectUnsupported` 2718 (row 4). **Closes** on rows 1,2,4,11,21,27,30 — `Run(Cam)→Run(Post)×5`, no second public `Post` fold.

**F2 — verified two-setup multi-tool machining job** (`[05](d)`; spec → plan → guard/workholding/magazine wired → post → voxel verify → capability). Artifacts: one two-setup multi-tool program (per-setup G54-59 WCS blocks), voxel gouge/uncut/overcut/air-cut receipts, the op-2 clamp admitted against the op-1 stock snapshot, probe-reconciled work offsets, rest-machining consuming the residual field, a Cp/Cpk row gating the next plan, one setup sheet + one traveler. Backward: `Spec/tolerance` (GD&T/fits/finish; Ra→scallop + IT-grade→allowance, row 20) + `Spec/capability` (plan-time Cpk gate producing `CapabilityVerdict`, row 29) → `Nesting/stock`+kernel `[V11]` hull (blank envelope, row 19) + `Fixturing/setups` (op→setup partition, setup-k→WCS rows, row 15) + `Fixturing/workholding` (per-kind footprint keep-out, `Condition` wired, row 14) + `Tooling/magazine` (schedule wired into the Cam fold, row 5) + `Tooling/cuttingdata` (Kienzle `kc` feeds/speeds + deflection, row 6) + `Toolpath/{motion (2.5D+rest), surface (3D finish via OCL), guard (per-move), skeleton (medial clearance)}` (rows 21,22,13,12) + `Kinematics/{cell, machine}` (3+2/5-axis topology + TCP, rows 16,17) → `Run(Verify{Motion, stock})` ⇒ `Verify/removal` (PicoGK `BoolSubtract`, receipts, residual field, per-setup snapshot, row 28) + `Run(Inspect{cycles})` ⇒ `Verify/probing` (touch-probe, ICP datum best-fit, ConformanceMetric, row 31) → `Spec/capability` (Cp/Cpk) → `Run(Post)` (per-setup WCS from `setups`) → `Run(Document)` ⇒ `Documentation/{projection, traveler}` (rows 32,33). Owner grows `Verify`/`Inspect`/`Post`/`Document` cases + `ResidualStock`/`StockSnapshot`/`CapabilityVerdict` on `owner#atoms` (row 1). **Closes** the run-N→run-N+1 truth loop on rows 1,5,6,12-22,28,29,31-33; the CapabilityVerdict input-carry makes the plan-time gate composable (graft E is load-bearing here).

**F3 — resin/powder + FFF additive pair** (`[05](c)`; implicit lane + slice stack + 3MF). Artifacts: a STEP solid in; an oriented part scored; lattice supports + TPMS infill; an FFF `.gcode` AND a resin `.cli` layer stack AND a 3MF hand-off — three content-keyed egress. Backward: `Ingress/solid` (OcctNet `ImportStep`→`OcctShape`→`OcctMesh` → kernel `MeshSpace` admission K15, dirty STL through kernel `HealOp`, row 10) → `Additive/production` (build-orientation over overhang/bottom-area/contour reading kernel `Analysis/select` `Faces`/`Curves.Draft` K20; machine profiles; 3MF via lib3mf beam-lattice+production writer, row 26) + `Additive/implicit` (PicoGK `IImplicit→Voxels` TPMS/gyroid, `Lattice` beam supports, SLA/DLP/MSLA grayscale + `.cli` via `oVectorize→CliIo`; ALC-firebreak, row 25) + `Additive/slicing` (FFF/DED planar consuming kernel `Slicing.Apply` slice-stack K3; perimeter/infill/travel order, row 24) → `Run(Post{additive dialect})` ⇒ `Posting/{program, dialect}` (Marlin). `Process/physics` mints `RemovalBudget.{Resin, Powder, DED}` (row 3). Content keys ⇒ `owner#atoms` `ArtifactKind` + `ContentHash.Of`. **Closes** on rows 1,3,10,24,25,26,27,30.

**F4 — issued setup-sheet/traveler package** (`[05](d)` document half). Artifact: one content-keyed setup sheet + one shop traveler (typed document MODEL; rendering rides the `[06]` artifacts-plane seam). Backward: `Run(Document{Seq<FabricationResult>})` ⇒ `Documentation/traveler` (row 33) composing `Documentation/projection` views (row 32) + `Tooling/magazine` tool lists (row 5) + `Fixturing/{workholding, setups}` plans/WCS (rows 14,15) + `Posting/program` facts (row 30) + `Spec/{tolerance, capability}` rows (rows 20,29) into ONE content-keyed model keyed by `ArtifactKind` (row 1). The widest fan-in node validates every upstream owner exposes a typed receipt — receipt discipline is validated corpus-wide by the terminal composer, not asserted per-page. **Closes** on rows 1,5,14,15,20,29,30,32,33 — `Run(Document)` is one entry case, NOT a second public `Traveler.Assemble` fold.
