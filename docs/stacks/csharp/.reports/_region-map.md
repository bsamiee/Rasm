# [REGION_MAP] — csharp

Region ledger for the C# stack corpus: one owner per region, snippet entries before code, page entries at finalization, repairs route to the owner.

## [1]-[PAGE_REGIONS]

- language.md: language-form placement law — extension surfaces; pattern dispatch grammar; immutable carriers; collection composition; generic algebra; text literals; stack kernels; the canonical modern-spelling chooser.
- shapes.md: domain shape ownership law — the five-discriminant owner chooser and shape decision law; value-object admission, key and identity policy, operator algebra, value-trait axes, absence, and conversion direction; smart-enum vocabulary declaration, lookup lifecycle, and behavior rows; union family selection, growth absorption, ad-hoc form, recursive rail arms, and wire surface; fault families, the rail bridge, accumulation algebra, the constraint plane, and wire ownership.
- rails-and-effects.md: result and effect-flow law — carrier choice, representation, and identity; boundary conversion and exception capture; traversal sequencing policy; failure-combinator algebra; effect runtime, schedule policy with schedule-driven iteration, resource brackets, and catch-policy recovery; boundary state cells and the stream-versus-typed receipt split; carrier-polymorphic composition and transformer-stack capability order.
- surfaces-and-dispatch.md: surface and dispatch law — the form chooser; entrypoint collapse, sealed admission, and surface attachment; modal arity absorption and modality folds; parameter algebra with policy values, optional context, supplied-versus-omitted, and the knob test; the five dispatch forms with trait derivation and keyed admission; carrier-polymorphic dispatch, independent joins, and iterative dispatch; type-level open-owner admission and its reach limit; aspect weave taxonomy, construction advice, and stacking order.
- boundaries.md: host, native, and wire boundary law — sentinel and absence admission with probe-sweep policy; resource capsules, ref-safe projection, and scope-axis isolation; subscriptions, host-thread marshaling, cancellation evidence, and handoff; token-gated cell lifecycle, drain-permit isolation, memo-key identity, and publication (cell-primitive mechanics stay rails-owned); protocol-shaped wire contracts, converter ownership, and byte identity.
- algorithms.md: numeric approach law — operand-shaped route dispatch and admission gates; dense factorization, sparse direct, and iterative-witness law; spectral decomposition law; proof-receipt law; owned numeric builds.
- system-apis.md: System.* replacement law — modern BCL surfaces deleting hand-rolled data-shape, runtime-primitive, and system-seam patterns; the unified smell table.
- testing/: proof rails — unit law matrices and property baselines; evidence rails; runtime verification routing; specialized proof tooling.
- domain/runtime.md: hosting and runtime law — the process-modality vocabulary and one boot fold; the sealed composition root with descriptor-row folds, scan-then-decorate, and the composition receipt; options admission and frozen policy publish; the lifecycle phase spine and banded drain ledger; the health rank fold with rank-row hysteresis; HybridCache and ObjectPool retention lanes; one clock seam, calendar vocabulary, and schedule cadence rows.
- domain/concurrency.md: throughput and streams law — the closed channel-lane row vocabulary with mode-derived loss classes, drop receipts, completion grammar, gauge cadence, and dataflow-block admission; one frozen parallelism budget proving cross-axis inequalities (skew term included), parallel iteration and PLINQ admission, partitioner shape, single-flight coalescing, and the cross-process heartbeat lease; rate-limiter policy rows with acquisition verbs, statistics gauges, partitioned/chained/translated composition, and the closed verdict family; reactive stream admission, scheduler and delivery-edge law, subject and disposal vocabularies, activation lifetimes, and the four-crossing seam table; DynamicData source and change-set law with the five-row invalidation taxonomy, refresh matrix, join identity, and chain economy; per-lane two-phase drain participation closing the conservation identity.
- domain/diagnostics.md: telemetry signal law — the generated emission grammar with id-band vocabulary and head-of-pipeline volume policy; one projection policy table with level-switch row folds, the write/audit delivery split, batching squares, fallback chains, and the typed loss rail; one signal root folding catalog rows — source and meter admission, the sampler matrix derived thrice, view shapes, exemplars, once-only OTLP export — with resource-signal governance and instrument polarity; the four-layer correlation spine with enrichment cost classes and the packed causal-stamp algebra carrying skew as evidence; the versioned correlation envelope at transport seams with dual-store law and the context-less verdict family; one classification taxonomy meeting one redaction seam with prevention rows for the seamless surfaces.

- domain/validation.md: boundary validation law — one rule-graph owner per boundary shape with the cascade 2x2 posture, condition scope, rule vocabulary, variant selectors, and composition/discovery law; the severity-partition bridge with the three-tier fault mint, evidence and path roles, and the one-symbol code vocabulary carrying wire-fault and culture law; the seam matrix with the four-stage admission order, provider-exception arms, forced override, and matrix-driven closure proofs; the options rule graph projected once to the host gate and once to the rail.

- domain/resilience.md: transport resilience law — the canonical strategy-order derivation with allotment-class-derived knobs, predicate rows, and ownership-follows-construction disposal; outcome-first seam execution folding the closed rejection taxonomy onto the rail with the typed context side channel; the one-retry-owner hop topology as a registry-claimed row table with conflict receipts, per-row reload generations, and dispose reclaim; the three-row retry-owner split and the idempotency-window-equals-allotment law; HTTP standard/hedging seam posture folded from hop-row idempotency × replayability columns with authority fan-out; capability-group dark control over breaker evidence seats; governed chaos rows below the strategies under test.

[PROVISIONAL — domain pages; each row is replaced by the final page row at that page's finalization]:
- domain/transport.md (provisional): gRPC channel/client law and transport rows; protobuf contract evolution by descriptor diff; server exposure, grpc-web, JsonPatch; UDS/IPC topology, discovery manifests, peer credentials, framing; suite wire-contract composition and temporal serialization bridges.
- domain/persistence.md (provisional): EF Core law (compiled models, complex types, interceptors, conventions); provider-polymorphism axis; schema/identity/migrations; one query-plus-bulk store-op rail.
- domain/durability.md (provisional): embedded SQLite native law (pragmas, WAL, cross-process, epoch fencing); snapshot codec axes with MessagePack/LZ4/hash identity; atomic-write protocol; op-log/LWW sync and presence; retention policy rows and artifact classes.
- domain/postgres.md (provisional): PostgreSQL SQL feature law; Npgsql driver depth (multiplexing, COPY, LISTEN/NOTIFY, logical replication client); extension lanes (vector/geo/text/partition); ops, deployment, provisioning-is-verification-only.
- domain/data-interchange.md (provisional): analytical engine lane; parquet and delimited tabular movement; geo interchange; tabular export law and schema-evolution-by-descriptor-diff.
- domain/compute.md (provisional): tensor/SIMD dispatch vocabulary; staging memory and allocation classes; inference lane (session lifecycle, EP policy, IOBinding); measured dispatch with benchmark-gated receipts; units boundary.
- domain/visuals.md (provisional): render law over one capsule; text shaping before raster; SVG/icon/asset pipeline; chart law; theme-token algebra (variant-by-density grid, atomic swap, perceptual interpolation); motion rows.
- domain/interaction.md (provisional): retained shell/screens/navigation/docking; reactive view-state and the command-intent table with availability algebra; live collection binding and editor factories; dialogs and input rows; theme application; host-surface embedding, a11y/l10n, headless proof seam.

## [2]-[SNIPPET_REGIONS]

[LANGUAGE]:
- L-01 EXTENSION_SURFACE_SITE: extension blocks on a foreign BCL receiver — instance property and method; static property; non-conversion static operator.
- L-02 PATTERN_DISPATCH_SITE: switch-expression pattern grammar — list/slice, positional-in-list, property/relational/logical patterns over `ReadOnlySpan<T>`; constant-string and char list patterns over `ReadOnlySpan<char>`.
- L-03 IMMUTABLE_CARRIER_SITE: inert carrier surface — `required` + `init`; `field` accessor invariant; nested `with`-through-struct update across a sealed record and a readonly record struct.
- L-04 COLLECTION_COMPOSITION_SITE: construction shape — collection-expression spread; `params ReadOnlySpan<T>` and `params IReadOnlyList<T>` element shapes; implicit `^` index assignment inside object initializers.
- L-05 GENERIC_ALGEBRA_SITE: type-level algebra — static abstract members; static virtual default-body fold through the self-constraint; void-returning instance compound assignment.
- L-06 TEXT_LITERAL_SITE: literal forms — `u8` span constants including escape-bearing; `\e` terminal escapes in processed interpolation; `$$` raw-string interpolation with expression holes.
- L-07 STACK_KERNEL_SITE: stack-only kernel — `readonly ref struct` primary constructor; `ref` field with explicit-constructor ref-assign; `ref struct` interface implementation; `scoped ref` + `allows ref struct` step; confined fold loop.

[SHAPES]:
- S-01 FAULT_FAMILIES: union-shaped dual-contract fault family — `Expected` base with private-constructor closure; `IValidationError` static `Create` as the string-bearing tier beside structured cases; `Semigroup` aggregate-case `Combine` fold.
- S-02 RAIL_BRIDGE: generic constrained extension-block admission bridge — covariant fault-base factory-contract constraint; `allows ref struct` raw parameter; one-expression property-pattern projection with implicit rail lifts; three-valued option projection for null-yield owners.
- S-03 OPERATOR_ALGEBRA: operator-axis grant set as declared algebra — per-axis `OperatorsGeneration` declaration; hand-written cross-dimension operator re-entering admission; exact-binary-family generic kernel with admitted seed; key-typed relational threshold gate.
- S-04 DISPATCH_AND_ROWS: smart-enum behavior-row table — composite-returning `[UseDelegateFromConstructor]` delegate column beside a data column; generated constructor row injection; row invocation through the generated method surface.
- S-05 RAIL_ARMS: carrier-polymorphic catamorphism over a recursive generated union — trait-constrained `K<F,B>` fold; tuple-state static arms; applicative tuple combine in the recursive arm.
- S-06 KEY_AND_IDENTITY_POLICY: value-object comparer accessor policy — one accessor type swings equality, hashing, ordering, relational generation, and `CompareTo`; string policy is declared once per layer.
- S-07 AD_HOC_FORM: ad-hoc union parameter absorber — named slots, stateless member storage, mixed collection ingress, and generated probes replace overload families and payload bags.
- S-08 VALUE_TRAIT_AXES: LanguageExt value-trait axis selection — `[ValueObject<TKey>]` implementing `Amount`/`Locus`/`VectorSpace`, homogeneous grants (`OperatorsGeneration.None`) and implicit key egress (explicit `ConversionToKeyMemberType`) suppressed so `Locus` `pos - pos` yields the distance owner while position-plus-position is uncompilable, hand-written cross-dimension operators beside generated comparison, and a `VectorSpace`-constrained routine no `Locus` position can bind.

[RAILS_AND_EFFECTS]:
- R-01 EXCEPTION_CAPTURE: exception capture — `Try.lift<Fin<T>>` + `Run` + `MapFail` + self-flattening `Bind` collapsing the double rail.
- R-02 RAIL_TRAVERSAL: rail traversal sequencing — indexed `Map`; chained `TraverseM(identity).As()` abort path; `Strict()` before boundary transfer.
- R-03 VALIDATION_APPLICATIVE: validation applicative — tuple `Apply` accumulation over `ToValidation` projections with monadic flatten.
- R-04 RUNTIME_RECORD: runtime-as-data — one multi-capability runtime record (policy, clock, state cell, cancellation) with dual `Eff.runtime<RT>().Map(...)` capability projections and `Eff.local` inner-runtime narrowing.
- R-05 RESOURCE_BOUNDARY: drive-polarity resource contract — fail-only `Retry` over the acquiring effect plus `IO.Bracket(Use, Catch, Fin)`.
- R-06 SCHEDULE_POLICY: schedule operator algebra — union magnitude-flooring and intersect gap-flooring over a bounded `Backoff` curve applied through `IO<T>.Retry`.
- R-07 ATOM_STATE: atom memoization — point read; `{ IsSome: true, Case: }` projection; contention-safe idempotent `Swap`; `IfNone` fallback.
- R-08 RECEIPTS: receipt monoid — record-struct fact carrier with identity element, `operator +`, and the `Fold(Empty, +)` collapse of N receipts into one.
- R-09 CAPABILITY_BY_STACK_ORDER: transformer-stack capability — carrier-polymorphic comprehension over `Stateful`+`Readable`+`MonadUnliftIO`+`Monad` with `mapIO` exposing the IO floor through the stack.
- R-10 CATCH_POLICY_CASCADE: recovery-as-data — factory-returned `CatchM` policy value composed first-match-wins through the carrier-polymorphic `K<M,A> | CatchM` ladder over a `Fallible<Error,M>, Monad<M>` body, facet predicates (`IsExceptional`, `expected`) selecting the failure class.
- R-11 CONVERGE: schedule-driven iteration — `RepeatWhile(Schedule, predicate)` over an `Atom`-advanced `IO.lift` effect, `recurs(n) & spaced` bound-meets-cadence and the atom state-advance as supporting material.

[BOUNDARIES]:
- B-01 FOREIGN_ADMISSION: tuple-dispatch sentinel projection at one read site — foreign (state, payload) pairs admitted through the generated rail bridge into `Fin<Option<T>>`, each refused pair carrying its own typed fault evidence, the out-of-range foreign ordinal owning its own arm.
- B-02 CAPSULE_OWNER_CASES: one `[Union]` capsule surface — borrowed and owned cases over one `SafeHandle` whose `ownsHandle` flag mirrors the case, a shared `Use` projection through the `DangerousAddRef`/`DangerousRelease` borrow kernel returning `Fin`, and owned-only `Release`; the kernel's exception conversion is supporting material, never R-01's capture grammar.
- B-03 REF_SAFE_VIEW_PROJECTION: `scoped` delegate plus `readonly ref struct` view proving live foreign memory cannot escape while value projections can; empty-view absence projects to `Option`, never a manufactured sentinel.
- B-04 SUBSCRIPTION_HANDOFF: stored delegate identity, disposable detach token, window-long handle borrow opened at attach and released at detach, queued continuation gate returned live, and channel handoff under a declared drop policy.
- B-05 SIGNED_BYTE_IDENTITY: raw-octet admission for signed wire fields through the exception-capture rail, canonical coordinate recording, and raw pass-through without parse/reserialize loss.
- B-06 TOKEN_GATED_LIFECYCLE: pending/live/failed `[Union]` family in one `Atom` — acquisition outside the CAS, token-gated commit with loser self-release, sticky-failure poisoning via swap-and-return capture, and token-gated teardown that cannot dispose a successor. Distinct from R-07 (lifecycle protocol, not memoization — `Swap` mechanics are rails-owned supporting material) and R-11 (no schedule, no converge loop).
- B-07 SERIALISABLE_PERMIT: three-`Ref` drain coordination — the read-dependent permit under explicit `Isolation.Serialisable`, write-only transitions riding the snapshot default, fenced admission with typed rejection. Distinct from R-07 (STM `Ref` transaction, not `Atom` memoization); rails owns cell-primitive mechanics, this region owns the permit-isolation law.
- B-08 WIRE_FAMILY_CONVERTER: one `JsonConverter` owning a closed `[Union]` wire family — buffered `JsonDocument.ParseValue` discriminant-first read into generated-context case deserialization, the `JsonException` rejection channel, and a state-threaded generated `Switch` write emitting the discriminant with the case. Distinct from SD-01 (wire codec seam, not entrypoint sealing) and SA-02 (no format-parse pipeline).

[SYSTEM_APIS]:
- SA-01 READ_MOSTLY_LOOKUP: span-keyed alternate lookup — frozen ordinal-comparer construction; cached `AlternateLookup` probe field; non-throwing `TryGetAlternateLookup` acquisition with span-keyed `TryAdd` mutation.
- SA-02 FORMAT_AND_PARSE: UTF-8 format-parse pipeline — `Utf8.TryWrite` invariant-culture interpolation; generic numeric round-trip through constraint subsumption; `u8` header constant and span slicing as supporting material.

[ALGORITHMS]:
- A-01 DENSE_ROUTE_DISPATCH: non-uniform factorization route — closed `FactorRoute` union binding five MathNet decompositions (`Cholesky`/`LU`/`QR`/`Evd`/`Svd`) to one shared `ISolver<double>` arrow through generated `Switch`, the rank-revealing case forcing `vectors:true` so absent-vector failure surfaces at admission, one `Bind` serving every case.
- A-02 ITERATIVE_WITNESS_GATE: Krylov solve witness — `TrySolveIterative` over an ordered `Iterator` criterion stack (`Failure`→`Divergence`→`Residual`→`Count`, size-scaled `8·dim` ceiling), the recomputed true-relative-residual against the original operator as the only admission test surviving preconditioning and breakdown substitution.
- A-03 SCHUR_PAIR_DECODE: real-Schur eigenvector lift — `Math.Sign(λ.Imaginary)` strided adjacent-column `Zip` gather over `Enumerate()` reconstructing the complex modal matrix, the eigenvalue vector as the sole pairing discriminant.
- A-04 SPECTRAL_SYMBOL_DRIVE: spectral operator apply — pointwise wavenumber-symbol drive between in-place `Fourier.Forward`/`Inverse` under `AsymmetricScaling`, the split-spectrum `WaveAxis` owner deriving wavenumbers via `Generate.LinearRangeMap`, even-length Nyquist odd-symbol zeroing.
- A-05 SPARSE_EDIT_ALGEBRA: structural-edit cost class — `Edit` union over three primitives dispatched by guarded `switch` with `when op.Kind is FactorKind.Spd`, a `false` rank-1 result folding to `Rebuild` (discard-never-retry) and a `var` catch-all routing every pattern change to reconstruction.
- A-06 CONDITIONING_FALLBACK_CONVERGENCE: primary→secondary route rebind on the concrete Fin rail — `.Bind(Gate)` then `.BindFail(_ => secondary…Bind(Gate))` — both conditioning routes converging on the ONE post-solve true-relative-residual gate, the taken path recorded via `nameof` into the failure; distinct from R-10 (no CatchM policy value, no `|` ladder, no facet predicates) and from A-01/A-02 (route dispatch and witness grammar reused as supporting material only).
- A-07 HELD_HANDLE_REFINEMENT_FOLD: capped iterative refinement — `x ← x + A⁻¹(b − A·x)` as a pure state-threaded `FoldWhile` (pre-step predicate) over the materialized `(Schedule.Forever | Schedule.recurs(cap)).Run()` durations on ONE retained `ISolver<double>`, in-place `Multiply`/`Subtract` into one reused scratch whose residual vector feeds the next solve, `‖b‖` hoisted once, residual always against the ORIGINAL operator, cap exhaustion surfacing as `IterationStatus.StoppedWithoutConvergence` inside `Fin.Succ`. Distinct from R-11 (pure fold over a materialized schedule run — no `RepeatWhile`, no `Atom`, no `IO`) and A-02 (refinement of a held direct factor, not a Krylov criterion stack).
- A-08 FACTORED_OPERATOR_CAPABILITY: typed factored-operator value — `FactoredOp` owning the `ISparseFactorization<double>` instance, kind discriminant, cached `AMD` permutation re-consumed through `SparseLU.Create(A, int[] p, tol)`, pre-populated fill count, and a solution dimension sizing the result buffer (never the RHS); kind-derived `Option<Action<double[],double[]>>` transpose-solve closing over concrete `SparseLU`/`SparseQR`, `SharesScratch => Kind is not FactorKind.Qr`, the post-solve witness owned by the value. Distinct from A-05 (capability recovery on the operator value, not edit-cost dispatch — A-05 consumes this type) and A-01 (CSparse kind capability, not MathNet route selection).
- A-09 QUADRATURE_EVIDENCE_CHANNELS: adaptive quadrature evidence — the `Integrate.GaussKronrod` out-channel overload (`out error, out L1Norm`) with the `|value/L1Norm|` cancellation-ratio gate failing to `Fin.Fail` on breach, the integrand guarded per-evaluation through a finite predicate with skipped-count coverage, one typed evidence record out; the short overload's silent channel discard is the rejected form. Distinct from A-02 (cancellation-ratio diagnostic, not residual witness) and R-08 (library out-channels lifted into one receipt, not operational fact streams).
- A-10 TERMINAL_STATUS_TAXONOMY: terminal status partition — `IterationStatus` three-way route (`Converged` admitted, `StoppedWithoutConvergence` carrying the partial iterate as its own union case never `Fin.Fail`, `Diverged|Failure|Cancelled` failing code-preserving), ordered child-before-parent exception arms (`NumericalBreakdownException` before `NonConvergenceException`) inside `MapFail`, `Error.New(code, message, inner)` preserving domain code plus library evidence. Distinct from A-02/A-06 (verdict taxonomy after the witness, not the witness itself) and R-01 (library exception-ladder conversion, not capture grammar).
- A-11 FINITE_ADMISSION_SPAN_GATE: vectorized finite admission — one `TensorPrimitives.IsFiniteAll<double>` pass over the flat column-major `Values` array shared by `DenseMatrix`/`DenseVector` overloads of one `Admit`, `IsNaNAny` discriminating the coded typed rejection (NaN versus Inf), `Map` re-projecting the admitted carrier. Distinct from A-01 (the admission predicate A-01's route-level `Admit` instantiates per concrete carrier, not route dispatch) and SA-02 (span numerics admission, not UTF-8 format-parse).

[SURFACES_AND_DISPATCH]:
- SD-01 SEALED_ENTRYPOINT: regular-`[Union]` reachability seal — private owner constructor, `ConversionFromValue = ConversionOperatorsGeneration.None` deleting the implicit value-to-union ingress, one hand-written factory ingress over a span-`Validate` verb vocabulary with state-threaded construction `Switch`, extension-block dispatch egress.
- SD-02 MODAL_ARITY_ONE_ARM: one carrier-polymorphic `Lift<F>` arm consumed at two call shapes — `TraverseM` (abort) and `Traverse` over `Validation<Error>` (accumulate) with a monoid fold tail — the carrier alone switching the policy.
- SD-03 CARRIER_POLYMORPHIC_JOIN: three independent `Switch` dispatches joined once through the tuple `.Apply`, carrier-orthogonal accumulate-versus-abort; `Recur` stays prose-only.
- SD-04 TYPE_LEVEL_ADMIT: self-constrained `IObjectFactory<TOwner,TValue,TError>` boundary admitting every owner, span-keyed `allows ref struct` instantiation, owner-chosen `TError.Create`.
- SD-05 STACKING_ORDER: the acquire-once-versus-reacquire bracket/retry pair as two policies from two operator orders.
- SD-06 POLICY_CONTEXT_COLLAPSE: parameter-algebra signature collapse — an open policy-row record (`static readonly` rows carrying canonical context plus step behavior, callers free to compose their own) replacing the `T? a, T? b, bool x` flag tail; `Option<Context> = default` declaration consumed once through `IfNone(policy.Canonical)` so the default derives from the policy owner; zero-branch entrypoint passing the pre-constructed behavior. Distinct from S-04 (no smart enum, no delegate column) and R-04 (no runtime capability record, no `Eff` projections).
- SD-07 KEY_ADMISSION_TRIPLE: one string-keyed `[SmartEnum<string>]` + `[ValidationError<Fault>]` declaration consumed as three dispatch forms at one boundary — span-keyed `Validate` lifting the typed owner fault into `Fin` and feeding a state-threaded total `Switch` in one hop, `Items` swept to the advertised key set, main-key `TryGet` lookup projected to `Option`. Distinct from S-04 (no delegate-row column, no `Lazy<FrozenDictionary>` derived index), SA-01 (generated lookup consumed, never hand-built, no `AlternateLookup` acquisition), SD-01 (projection dispatch, no union construction).

[RUNTIME]:
- RT-01 MODALITY_BOOT_FOLD: closed process-modality vocabulary whose rows carry `HostApplicationBuilderSettings` identity and signal-ownership registrations — service, embedded, and plugin rows, the plugin row setting the `DisableDynamicEngine` switch via tuple-capture — folded by one `Compose` over `Host.CreateApplicationBuilder`/`CreateEmptyApplicationBuilder` with forced `ValidateOnBuild`/`ValidateScopes` and descriptor-row intake. Distinct from S-04 (delegate-column mechanics are supporting material; the spotlight is the default-versus-empty host boot fold).
- RT-02 SEALED_ROOT_DESCRIPTOR_FOLD: module-contribution table of `ServiceDescriptor` rows — `KeyedService.AnyKey` factory row, `TryAddEnumerable` hosted row, dormant module row — folded once, Scrutor scan-then-decorate under `RegistrationStrategy.Throw`, `MakeReadOnly` seal before validated provider construction, and the `RootReceipt` pure fold over the table (per-lifetime counts via `AddOrUpdate`, dormant inventory, `IsDecorated()` decoration depth).
- RT-03 OPTIONS_FROZEN_PUBLISH: one `OptionsBuilder` admission chain over a constructor-bound immutable policy record (`BindConfiguration` + `ErrorOnUnknownConfiguration`, generated `[OptionsValidator]`, relational `Validate`, `ValidateOnStart`) feeding a `ChangeToken.OnChange` frozen-publish fold — content-identical no-op, accepted swap, rejected-write retention with fault facts. Distinct from R-07 (`Atom` mechanics are rails-owned supporting material; the spotlight is the options pipeline plus reload fold).
- RT-04 PHASE_BAND_SPINE: one `[Union]` phase family in one cell advanced by `IHostedLifecycleService` hooks and the `ApplicationStopping` edge; banded drain walk with per-band linked forced tokens (`CancelAfter` over a `Share` of `HostOptions.ShutdownTimeout`), band-ledger facts, admission as a phase projection. Distinct from B-06/B-07 (no token-gated commit, no STM permit — cell mechanics composed, never re-taught).
- RT-05 HEALTH_RANK_FOLD: registration-policy health rows (one probe registered twice under two `FailureStatus`/timeout pairs, per-row `Delay`/`Period`) consumed by an `IHealthCheckPublisher` worst-of rank fold with asymmetric hysteresis — immediate escalation, single-step recovery gated by a rank-row `Window` column — over a retained-capability smart-enum table.
- RT-06 RETENTION_LANES: `HybridCache.GetOrCreateAsync` explicit-`TState` static-factory law with populate-versus-probe `HybridCacheEntryFlags` policy rows and tag invalidation, beside the pooled-lane `IResettable` reset predicate and an `IO.Bracket` lease — one retention law over two value classes. Distinct from R-05 (bracket grammar is supporting material).
- RT-07 CLOCK_CADENCE_FOLD: one frozen clock-seam record (`TimeProvider` + `IClock` + `DateTimeZone`) with `MapLocal` gap/overlap admission and `InstantPattern` stamping; schedule catalog rows (seeded `CronExpression.TryParse` jitter admission, zone, misfire smart-enum) folded by one min-occurrence driver with `GetOccurrences` misfire-inventory dispatch.

[CONCURRENCY]:
- CC-01 LANE_ROW_VOCABULARY: closed channel-lane policy table — multi-axis `static readonly` rows with mode-derived loss class, one receipted `Open<T>` through the bounded `itemDropped` overload, the capacity-0 rendezvous row, and `CreateUnboundedPrioritized` fair-within-class `(Rank, Seq)` comparand; distinct from B-04 (one seam's drop policy at a callback — this region owns the process-wide row vocabulary).
- CC-02 BUDGET_DERIVATION: one frozen parallelism-budget record proving its own cross-axis inequalities — the cross-host `Skew` term inside the lease inequality — through accumulating `Validation` `Apply`, degree derivation as `ParallelOptions` rows, lane-fed `Parallel.ForEachAsync`, and the total PLINQ spelling with a declared merge row; distinct from R-03 (no `ToValidation` projections, no monadic flatten — the record's self-proof is the spotlight).
- CC-03 LIMITER_VERDICT_GATE: limiter policy rows as `RateLimitPartition` factory values carried on the intent, one `PartitionedRateLimiter` resolving the row per acquisition, lease-as-bracket admission dispatching on `IsAcquired`, and dual-coded shed evidence — `MetadataName.RetryAfter` present versus absent as two error codes; bracket grammar is rails-owned supporting material (distinct from R-05/SD-05 — no drive polarity, no operator-order pair).
- CC-04 DELIVERY_EDGE_TRIPLE: the declared observer delivery edge — `Synchronize` grammar serialization, named-scheduler `Buffer` conflation folded to `(Latest, Dropped)` receipted loss, `DistinctUntilChanged` significance gate, one `ObserveOn` at the consumption edge, `TakeUntil(CancellationToken)` lifetime, non-waiting `TryWrite` stream-to-lane crossing, and quorum-plus-linger `Publish().RefCount`.
- CC-05 CHANGESET_RESOLUTION: live-set re-resolution mechanics — `EditDiff` whole-set swap under a significance comparer, predicate-stream `Filter`, `IgnoreUpdateWhen` propagation gate, `Transform(transformOnRefresh: true)`, and source-form `ExpireAfter` returning the expiry-receipt stream; UI binding of change-sets is interaction-owned and absent.
- CC-06 LANE_DRAIN_PARTICIPATION: one lane's two-phase drain participation — coded-fault or clean `TryComplete` selected by an `Option<Error>` abort channel, cooperative `ReadAllAsync` flush under a soft-budget linked token, forced `TryRead` residue sweep, and one `DrainFact` whose `Closes` fold proves the conservation identity against the lane's loss receipts; the band walk and budget shares are runtime-owned (distinct from RT-04 — participation contract, not the phase machine).

[DIAGNOSTICS]:
- DG-01 GENERATED_EMISSION_SURFACE: `[LoggerMessage]` partial as a per-concern emission contract — const event-id band arithmetic, `[LogProperties]` expansion knobs (`OmitReferenceName`/`SkipNullProperties`), `[TagProvider]` projection row for an unannotatable foreign type, exception-channel binding, and the dynamic-`LogLevel` verb; emission grammar only, no pipeline or redaction mechanics.
- DG-02 PROJECTION_POLICY_TABLE: one `LoggerConfiguration` freeze — a `LoggingLevelSwitch` override row table folded into per-`SourceContext` floors, root destructuring caps, `Matching`-keyed `Conditional` routing, the `WriteTo`/`AuditTo` delivery split, a `BatchingOptions` square on an `IBatchedLogEventSink`, and `WriteTo.Fallible` wiring an `ILoggingFailureListener` that folds `LoggingFailureKind` into the fact stream; `Atom` mechanics are rails-owned supporting material (distinct from R-08 — loss-rail wiring, not receipt monoid).
- DG-03 SIGNAL_ROOT_COMPOSITION: one `AddOpenTelemetry` declaration driven by a `SignalCatalog` row record — pinned `serviceInstanceId` resource fold, catalog-fed source/meter admission including native `System.Runtime` and the resource-monitoring meter, the `ParentBasedSampler(TraceIdRatioBasedSampler)` matrix, a view-row fold (`Base2ExponentialBucketHistogramConfiguration`, `MetricStreamConfiguration.Drop`, `TagKeys` + `CardinalityLimit`), `ExemplarFilterType.TraceBased`, capture-flag logging options, and the once-only `UseOtlpExporter`. Distinct from RT-02 (telemetry admission rows, not descriptor fold).
- DG-04 CAUSAL_STAMP_ALGEBRA: the packed 48+16 HLC word — one `Advance` algebra serving local tick and receive, counter saturation spinning to the next millisecond, `CompareTo` as the only ordering verb over fixed-width hex serialization, boot-epoch origin identity, and skew measured into typed `SkewFact` evidence through tuple-capture while the stamp still advances; cell `Swap` mechanics are rails-owned supporting material (distinct from R-07 memoization and B-06 lifecycle gating).
- DG-05 ENVELOPE_TRANSPORT_SEAM: the versioned correlation envelope at one transport seam — owner-held baggage keys behind typed verbs, `CompositeTextMapPropagator` inject/extract over one delegate-adapted carrier, post-advance departure stamping, the once-per-transport receive-advance, the numeric envelope-version gate, and the closed context-less verdict family (`Joined`/`ForeignRoot`/rail refusal). Distinct from B-08 (no wire codec) and B-01 (no sentinel projection).
- DG-06 CLASSIFICATION_REDACTOR_MAP: one taxonomy as sealed `DataClassificationAttribute` rows on contract members — classified flow through `[LogProperties]` on the generated path, the `AddRedaction` map with a configuration-bound `SetHmacRedactor` row, an exact stacked-set `SetRedactor` row, the erasing fallback, and `EnableRedaction` + `ApplyDiscriminator` as the one enforcement seam. Distinct from DG-01 (enforcement map, not emission grammar).

[VALIDATION]:
- VL-01 RULE_GRAPH_DEPTH: one `AbstractValidator<T>` as a boundary shape's complete rule-graph declaration — validator-level cascade 2x2 posture, chain `Cascade(Stop)` dependence, `When(..., ApplyConditionTo.CurrentValidator)` scope, `DependentRules`, memoized block condition, `RuleForEach.Where` + `OverrideIndexer` + child `SetValidator`, `RuleSet` variant, context-arity `Must`, `WithSeverity` provider reading run policy, `WithState` tier-1 fault minting, `Include` pack; the supporting fault family composes S-01 and owns nothing.
- VL-02 OUTCOME_BRIDGE: the one generic severity-partition bridge from `IEnumerable<ValidationFailure>` onto `Validation<Error, Admitted<T>>` — gating/riding partition, three-tier mint (`CustomState` pattern test, frozen `ErrorCode` map, reserved-band string fall-through), `Errors.None` fold, evidence riding the success value, four-field wire-fault projection. Distinct from R-03 (no applicative accumulation grammar) and S-01 (no `Semigroup` combine — `Error` `+` composes as rails-owned supporting material).
- VL-03 SEAM_ROW_DRIVER: the seam matrix as data driven heterogeneously — payload-type row lookup with `CanValidateInstancesOfType` guard, untyped `IValidator.Validate(IValidationContext)` rehydration via `ValidationContext<object>.CreateWithOptions` ruleset selection, `RootContextData` override-policy write, provider-exception arm minting a hand-built `ValidationFailure` with typed `CustomState` into the same bridge, `AdmissionRecord` receipt from `RuleSetsExecuted`, stage-4 generated-owner tail. Distinct from B-08 (no wire codec) and RT-02 (validator rows, not descriptor fold).
- VL-04 CLOSURE_PROOF_FOLD: the four-step closure fold (enumerate source, enumerate registration, bijection-or-exemption, typed rejection) driven off the seam matrix through the non-generic `IValidatorDescriptor` — per-row graph-ruleset-vs-variant-column and payload-member-vs-`GetMembersWithValidators()` sweeps accumulated by applicative `Traverse`, matrix rows against `AssemblyScanner` results, the bridge code map against the declared code vocabulary — every instance from one `Gap`/`Closed` pair joined through tuple `Apply`. Distinct from CC-02 (metadata bijection sweeps, not a record's self-proof).
- VL-05 OPTIONS_DUAL_PROJECTION: one options rule graph projected twice — the `IValidateOptions<T>` host-gate projection partitioning the gating band into `ValidateOptionsResult.Fail`, and the same outcome crossing the rail bridge; named-instance facts as `RootContextData` conditions. Distinct from RT-03 (no options pipeline or reload fold — the adapter-as-second-projection of one FluentValidation graph is the spotlight).

[RESILIENCE]:
- RS-01 PIPELINE_ORDER_ALGEBRA: the canonical five-strategy chain as one ordered declaration — limiter→total→retry→breaker→attempt with the retry bound and breaker sampling window derived from the allotment-class spans, one `PredicateBuilder<T>` row feeding both `ShouldHandle` slots by implicit conversion, builder `TimeProvider`/`ConfigureTelemetry` seams. Distinct from CC-03 (strategy rows inside one builder, not partitioned limiter rows) and SD-05 (declaration-sequence-as-policy, not bracket/retry operator order).
- RS-02 SEAM_OUTCOME_FOLD: `ExecuteOutcomeAsync` closure-free typed-state execution under a leased operation-keyed context carrying the allotment span as a `ResiliencePropertyKey` row, one total `Outcome<T>`→`Fin<T>` fold — caller-cancel versus deadline discriminated by token check, `IsolatedCircuitException` arm before `BrokenCircuitException`, rejection evidence (`Timeout`, `RetryAfter`, `TelemetrySource`) preserved per arm. Distinct from R-01 (outcome-capture kernel, not `Try.lift` grammar) and A-10 (wire rejection taxonomy, not solver terminal partition).
- RS-03 HOP_REGISTRY_CLAIM: hop topology as one closed row record consumed by `ResiliencePipelineRegistry<TKey>` — `TryAddBuilder` first-wins claim verdict with per-generation `AddReloadToken` and `OnPipelineDisposed` reclaim inside the configure body, conflict receipt carrying both declaration symbols and the discarded policy, loser degrading to the incumbent pipeline, `(name, instance)` formatter axes, the table `Materialize` fold and row enumeration as the queryable topology. Distinct from RT-02 (claim cell over deferred builders, not descriptor fold).
- RS-04 HTTP_SEAM_SPLIT: standard-versus-hedging client registration as one fold over hop-row idempotency × replayability columns — allotment-derived slot edits on `HttpStandardResilienceOptions`, `SelectPipelineByAuthority` instance fan-out, ordered route groups built from row data, hedging spans on the paired options record.
- RS-05 CAPABILITY_DARK_CONTROL: one `CircuitBreakerManualControl` bound across a capability group's breakers by one seat-minting fold over hop rows — `isIsolated` boot-into-dark, sticky `IsolateAsync`/`CloseAsync` force-release verbs, per-breaker single-attach `CircuitBreakerStateProvider` evidence seats. Distinct from RT-05 (breaker-set isolation mechanics only; the consuming rank fold is runtime-owned) and B-06 (no token-gated cell).
- RS-06 CHAOS_GOVERNANCE: the four-plane chaos block below the strategies under test — one parameterized `Governed` configurator stamping `EnabledGenerator`/`InjectionRateGenerator` from one posture cell onto every `ChaosStrategyOptions` row, weighted `FaultGenerator`/`OutcomeGenerator<T>` catalogues, the behavior-plane injected-count fact, latency riding the builder clock. Distinct from RT-03 (posture cell read per execution; no options admission re-taught).

[TRANSPORT — provisional]:
- TP-01 TRANSPORT_ROW_AXIS: closed transport-row vocabulary — in-process, UDS companion, remote TLS, browser-translated — as smart-enum rows whose channel column carries the `ConnectCallback` dial, keep-alive/multiplex knobs, the `ServiceConfig` retry row, and `GrpcWebHandler`, materializing one typed port record of call arrows from one `Method<TReq,TRes>` contract value; distinct from RT-01 (host boot fold) and CC-01 (channel-lane rows).
- TP-02 CALL_SEAM_FOLD: per-call seam — built-in metadata stamping via `CallInvoker.Intercept(Func<Metadata,Metadata>)`, deadline minted from the hop row's budget into the `CallOptions` triple, and the `RpcException.StatusCode` taxonomy folded once to `Fin` with `-bin` trailer fault decode; distinct from RS-02 (`Outcome<T>` pipeline fold, token discrimination) and DG-05 (envelope propagator mechanics).
- TP-03 DESCRIPTOR_DIFF_GATE: contract-evolution classifier — canonical checksum over `Fields.InDeclarationOrder()` rows plus reserved ranges and service methods via `XxHash3`, additive-vs-breaking verdict union with reserved-range ceremony deciding removal class; never hashes raw `SerializedData`.
- TP-04 EXPOSURE_RECORD_FOLD: one server exposure record folded into `AddGrpc`/`AddServiceOptions`/`MapGrpcService`/`MapGrpcHealthChecksService`/`UseGrpcWeb`+`EnableGrpcWeb`, tag-predicate health mapping rows, and structured fault projection `RpcException(Status, trailers)` under a `-bin` detail key; distinct from RT-02 (descriptor fold) and VL-03 (seam matrix).
- TP-05 MANIFEST_ATTACH: discovery-manifest lifecycle — owner-only directory mode at creation, same-directory staged write plus atomic `File.Move` publish, two-probe staleness ladder (pid/start-stamp advisory, socket-connect authoritative) folded to stale-listener rejection; distinct from B-06 (no token-gated cell) and RT-04 (no phase machine).
- TP-06 FRAMED_CORRIDOR: raw-lane framing invariant — `CalculateSize` producer pre-check, fixed little-endian header, length gate before any allocation, `XxHash3` checksum-then-parse over a rented buffer, disjoint oversize/truncated/corrupt/drifted fault taxonomy; distinct from B-05 (byte identity, not framing) and SA-02 (UTF-8 format-parse).
- TP-07 SUITE_RESOLVER_MERGE: federated JSON contract merge — per-package `IJsonTypeInfoResolver` disjointness probe accumulating contract-fork conflicts on the validation carrier, `JsonTypeInfoResolver.Combine` plus `WithAddedModifier` suite posture, `MakeReadOnly` freeze, schema-node export hash via `GetJsonSchemaAsNode`; distinct from B-08 (converter mechanics) and the system-apis serializer-surface chooser.

[PERSISTENCE — provisional]:
- PS-01 STORE_PROFILE_AXIS: engine x placement x codec as one store-profile row — provider-admission delegate columns with option-typed capability slots, the placement authority vocabulary, one codec composition fold (`UseSnakeCaseNamingConvention` + `UseThinktectureValueConverters` + tracking row + interceptor intake); distinct from RT-01/RT-02 (options-builder fold over engine rows, not host boot or descriptor fold).
- PS-02 DOCUMENT_SHAPE: one aggregate declaration choosing table-split versus `ToJson` document mapping — aliasing-legal complex value in two slots, JSON-only `ComplexCollection`, generated-type document interior under the converter seam, primitive collection, generated key via `HasKey`.
- PS-03 SUPPRESSION_GATE: unit-of-work interceptor as a typed gate — `InterceptionResult<int>.SuppressWithResult` with declared tracker disposition, mandatory sync/async modality twins over one shared verdict kernel.
- PS-04 MIGRATION_VERDICT_FOLD: boot schema verdict over assembly/applied identifier sets — schema-newer typed rejection carrying unknown identifiers, placement-routed pending/fresh/apply arms, `HasPendingModelChanges` drift gate; distinct from TP-03 (EF migration set algebra, not protobuf descriptor diff) and VL-04 (set difference over store state, not validator metadata bijection).
- PS-05 IDENTITY_ROW_VOCABULARY: identity policy rows — `Guid.CreateVersion7` admission mint beside the deterministic-backfill overload, `ToByteArray(bigEndian: true)` order-preserving transcription, collision-posture column, composed-digest factory row.
- PS-06 RAIL_BRACKET_ARITY: one closed store-op family under one strategy-composed pooled bracket — state-threaded `IExecutionStrategy.ExecuteAsync` with static lambdas, input-shape arity (point/batch/keyset page as `Option<Cursor>` lexicographic predicate), value projection plus `TagWith` provenance, boundary fault conversion sparing cancellation; distinct from R-01 (bracket conversion, not `Try.lift` capture grammar).
- PS-07 WRITE_MASS_OUTPUT: bulk-lane self-emission — statement-bodied `ExecuteUpdateAsync` setters with the zero-affected concurrency fold, bridge merge enlisted in the ambient transaction with declaration-ordered when-clauses and `MergeWithOutputAsync` changefeed projection committed in-transaction.

Entries under [RAILS_AND_EFFECTS] are the finalized snippet regions; R-03 was relabelled from RECOVERY to its proven card, R-05 replaced the statement-exemption capsule with the drive-polarity bracket, and R-09 opened the transformer-stack composition region. The complex-project hardening pass added R-10 (catch-policy cascade under EFFECT_RECOVERY) and R-11 (schedule-driven converge under SCHEDULE_POLICY), folded EFFECT_LIFTING's layer-vs-effect-lift law into CAPABILITY_BY_STACK_ORDER, merged OPERATIONAL_RECEIPTS and ALGORITHM_RECEIPTS into one RECEIPTS card (R-08 relabelled from ALGORITHM_RECEIPTS), and extended R-04's spotlight to the multi-capability runtime record.

## [3]-[KNOWN_OVERLAPS]

The L-07 / S-04 ref-struct fold collision was repaired at the shapes.md snippet assignment: the incumbent manual-owner fold snippet was retired and the replacement set demonstrates no ref-struct kernel.

S-03 and S-08 sit in adjacent value-object cards and demonstrate disjoint algebra: S-03 owns the Thinktecture per-axis `OperatorsGeneration` operator generation (homogeneous operators, the generic-math `IAdditionOperators` kernel, the key-typed relational gate), S-08 owns the LanguageExt value-trait category algebra (`Amount`/`Locus` axis selection, suppression-driven position safety, the axis-constrained generic) — same section, no shared demonstration.

A-01 and A-05 both compose a `[Union]` over a dispatch, but the spotlights are disjoint and scoped apart: A-01 demonstrates the MathNet solver-arrow binding under generated `.Switch`, A-05 the sparse edit-cost-class under a language `switch` expression with a cross-field `when` guard and reconstruction catch-all — no shared demonstration. A-03's three-arm `Math.Sign` relational `switch` is scoped distinct from L-02 (which owns `ReadOnlySpan<T>` pattern grammar): A-03's arms are a numeric discriminant whose payload is the strided eigenvector gather. A-02's `Try.lift` is scoped distinct from R-01 (which owns the `Try.lift`+`MapFail` exception-capture grammar): A-02's capture is minimal plumbing under the numeric residual-witness gate.

SD-01 is scoped distinct from S-04 (no delegate-row table — a construction `Switch`, not behavior rows). SD-02 is scoped distinct from R-02/R-03 (spotlight is one-arm-many-modalities; traversal verbs are supporting material). SD-03 is scoped distinct from S-05/R-09 (independent non-recursive join, no catamorphism, no transformer stack). SD-04 is scoped distinct from S-02 (open-owner-family inversion, not the per-owner rail-lift bridge). SD-05 is scoped distinct from R-05/R-06 (ORDER-as-policy pair; bracket and schedule mechanics are supporting material). SD-07 retroactively proves SD-01's undeclared `[ValidationError<Fault>]` on `Verb`; SD-01's spotlight remains the reachability seal.

The boundaries rebuild settled four contacts by altitude. rails-and-effects.md owns state-cell mechanics — `Swap` replay, STM transaction grammar, schedule-driven iteration — while boundaries.md owns the lifecycle and permit law layered on them (token gating, drain-permit isolation, publication); a boundaries snippet uses `Atom`/`Ref` only as supporting material. B-03 is disjoint from L-07: language.md owns ref-struct kernel grammar (`ref` fields, `scoped ref`, confined loops), B-03's spotlight is escape prevention — scoped view in, detached value out. B-05 consumes R-01's `Try.lift` capture grammar as supporting material; the spotlight is byte-identity admission. boundaries.md §6 states wire-contract seam law at converter altitude while system-apis.md owns the BCL surface-replacement chooser (`JsonTypeInfo`, frozen lookup, hex conversion) — no shared demonstration.

Two further surfaces↔shapes contacts are same-fact, disjoint-decision and stand: both pages state the "rails own outcome transport" rationale (shapes at shape selection, surfaces at request-entrypoint shaping), and both name the non-void factory hook's post-init surface (shapes: evidence validity and rehydration re-derivation; surfaces: the typed side channel threading computation forward with no field, closure, or second pass).

Three audited contacts are disjoint by altitude and stand without repair. shapes.md and surfaces-and-dispatch.md both name `SwitchMapMethodsGeneration.None` legislating two decisions — shapes the shape consequence (a transport carrier or delegate column absorbing item growth), surfaces the sealing law (deleting generated dispatch so an extension-block dispatch algebra cannot be bypassed) — same knob, disjoint decisions. rails-and-effects.md and surfaces-and-dispatch.md state the catch-`|` composition fact at two altitudes — rails the predicate-precedence law (the first matching predicate consumes the failure), surfaces the composition syntax (left-associative handler wrapping, inner handlers see errors first) — and the `.As()` re-anchor law likewise appears at both altitudes (rails the omission consequence, surfaces the recover-once dispatch law); the duplication is tolerated because each page teaches its own layer. Within rails-and-effects.md, RECEIPTS and BOUNDARY_STATE_FAMILIES are disjoint by shape and purpose — the fact stream is `Atom<Seq<FactRecord>>` for observability, the state-family card selects keyed carriers for mutable boundary cells; future batching guidance added to RECEIPTS would cross that boundary and must route here first.
