---
title: Rasm API Stacking Census
input: full_diff
effort: high
conclusion: neutral
include:
  - "libs/**"
---

# [API_STACKING]

Bidirectional capability closure is this pass's one question: whether every touched fence stacks the admitted packages and host SDKs to their full verified depth, and whether every capability the page declares is consumed. Run the census before any finding: enumerate the fence's composed member set against the folder-tier `.api/` catalogue, the language substrate tier `libs/<lang>/.api/`, and the central manifest — `Directory.Packages.props`, `pyproject.toml`, `pnpm-workspace.yaml`. A touched page is presumed under-stacked until the census proves each composition is the deepest applicable form; only census-verified members ground a demand, and phantom-member deletion belongs to the doctrine check, not re-litigated here.

Hunt these classes, each finding naming file, anchor, the shallow or hand-rolled form, and the exact catalogued member demanded:
- Handroll: a fence body re-deriving what an admitted package or host SDK owns at operator depth — a hand-rolled loop, branch ladder, or kernel where a catalogued combinator, generated surface, or algebra owns the invariant; the demand names the member and the composed form that replaces the body.
- Under-stacking: a page composing only shallow members of a package whose catalogue reaches deeper — each shallow call proves itself the deepest applicable form the package ships, or the deeper member is the demand; proven hit shapes: a dirty-region publication member unconsumed beside a full-surface redraw, a host default-roster member completing a current-roster-only projection, host identity-family ids completing a partially-mirrored vocabulary, a dual-overload host family composed through one optional-parameterized owner method.
- Unconsumed capability: a rail-verified host or package member that strengthens a touched owner and sits unconsumed — the extension lands as a row, case, or member on that owner, never a parallel surface.
- Decorative declaration: a capability noun with no anchoring fence member, a receipt nothing mints, or a policy row nothing consumes — demand the consuming member or the deletion; a policy column consumed only by a cache key, receipt, or log annotates rather than governs, and an arm that cannot honor a policy field refuses the combination loudly before provider work.
- Charter-gap ideation: a catalogued capability that unlocks what the folder charter implies yet no touched page carries — the gap is a finding naming the member and the existing owner it extends.

Settled corpus law outranks intuition: a composition the owning page or `.api/` catalogue rules is never a finding, and a manifest-admitted package under a temporary distribution gap stays design-real. A pass that finds nothing after a genuine census states that verdict plainly.
