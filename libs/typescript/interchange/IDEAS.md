# [INTERCHANGE_IDEAS]

The forward pool of higher-order concepts for the wire boundary. Each idea is a card — a bracketed slug leader, the capability, what it unlocks, and the gap or modern technique it draws on. An idea drives one or more `TASKLOG.md` tasks; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition. The trickle-down lands here too: `DESCRIPTOR_EVOLUTION_GATE` realizes the descriptor-diff leg of the `libs/.planning` `ONE_MODEL_THREE_FACES`/`ONE_WIRE_SCHEMA_SOURCE` concert (`@bufbuild/buf` the cross-`libs/` FileDescriptorSet owner); `RECORDED_INTENT_PATCH` realizes the partial-update leg of `ONE_HEALTH_DEGRADATION_WIRE`/`CRDT_COLLABORATION` (the live-wire write-back and op-log delta cross as recorded intent); `GEO_WIRE_VOCABULARY` realizes the embedded-geometry leg of `ONE_TESSELLATION_RAIL` decoded as a typed GeoJSON owner. The `CapabilitySdk` SDK leg of `ONE_CAPABILITY_CATALOG` is a `transport#CODEGEN_TOOLING` codegen row driven by the `CAPABILITY_SDK_CODEGEN` task, not an idea of its own — the descriptor-catalog consumer is `ui#COMMAND_SURFACE_VOCABULARY` over the `CommandGateway`, and the MCP agent surface is the cross-`libs/` `ONE_AGENT_TOOL_PROJECTION` decode at `services/agent/mcp-transport`.

## [1]-[OPEN]

[DESCRIPTOR_EVOLUTION_GATE]: the descriptor set is decoded as values but its drift is never classified, so a peer running a breaking-changed contract is discovered mid-decode rather than at dial time.
- The C# `transport#DESCRIPTOR_GATE` runs a full `Verdict` (Identical/Additive/Breaking) classifier over `FileDescriptor` diffs whose verdict gates peer attach, store open, and replay decode alike; the `@bufbuild/protobuf` runtime ships the entire reflection surface for the TS mirror — `createFileRegistry(fileDescriptorSet)`, `DescMessage`/`DescField`/`DescService`/`DescMethod` with `fieldKind`/`methodKind`, and `mergeFromBinary` — yet the folder reads only `create`/`fromBinary`/`toBinary`/`findDetails` and `@bufbuild/buf`'s `protoc-gen-buf-breaking` binary is admitted-but-unwired. The descriptor-diff classifier is the single largest unexploited admitted capability in the folder.
- Unlocks the new `evolution/descriptor-gate.md` `DescriptorGate`: the running app-root `FileDescriptorSet` (the same one the `transport#CODEGEN_TOOLING` pipeline commits) is classified against the descriptor the generated `WireClients` were built from, the `Identical`/`Additive`/`Breaking` `Data.TaggedEnum` verdict gating dial-time client construction so a breaking-drifted field number, type flip, or singular-repeated flip faults at construction through `FaultDetail.Quarantine` carrying the exact `message.field` path, not a torn decode three calls later. `Additive` (a new field on an unused number, a new enum value) passes — the `UnknownField` round-trip the descriptor runtime already preserves — exactly the additive-evolution tolerance the versioning law relies on.
- Draws on the runtime self-describing descriptor law (`@bufbuild/protobuf` `DescFile` reflection is exactly what the binary carries, no build-step plumbing) the C# floor legislates, mirrored as the one TS verdict owner; the classifier's diff-relevant projection hashes in declaration order so a generator-version reorder is not a false breaking, mirroring the C# `Checksum` `InDeclarationOrder()` law.

[RECORDED_INTENT_PATCH]: partial updates cross the wire as state-diffs decoded whole, losing the `Move`/`Copy`/`Test` semantics and the optimistic-concurrency guard the C# `JsonPatchDocument` and `FieldMask` carry.
- The C# `transport#PATCH_LAW` makes a partial update recorded intent — the six typed verbs constructed at the moment the mutation is decided, `ApplyTo` error-accumulating per operation, `Test` carrying optimistic concurrency inside the document so a replayed patch self-guards — and `FieldMask` is the binary-native sparse-update vocabulary on the proto wire; the `rfc6902` package is cataloged in the workspace yet no folder owns it, an admitted capability with zero consumer. The `csharp:Rasm.AppHost/live-wire` write-back and the op-log delta surfaces cross partial mutations the TS side currently decodes as whole replacements.
- Unlocks the new `codecs/patch-rail.md` `PatchRail`: the wire `JsonPatchDocument` decodes through one six-arm `Schema.Union` over the `op` discriminant (`add | remove | replace | move | copy | test`), `applyPatch` folds the error-accumulating `(error|null)[]` result into one typed `FaultDetail.HopFault` carrying the failing operation index, `createTests` prefixes the optimistic-concurrency guard so a stale apply faults rather than corrupts, and the proto `FieldMask` (the repeated dotted-path string set the descriptor runtime carries) lowers to the same `Operation[]` so the binary sparse-update and the json-stj patch share one apply owner. The patch is a recorded mutation against an already-`DecodeRail`-admitted value, never a second decode.
- Draws on the recorded-intent-not-state-diff law the C# floor legislates and the RFC 6902/6901 standard the `rfc6902` package owns; the patch re-enters domain admission as a whole value after apply (the C# `applying patches to admitted owners bypasses admission` prohibition mirrored), so the patched value passes back through the `quarantine/drift-terminal.md` fold, never straight to a render binding.

[GEO_WIRE_VOCABULARY]: the `geometry` codec row is a thin embedded-`JSON.parse` stub with no GeoJSON-type vocabulary, no CRS posture, and no precision floor — the canonical "thin steel" the folder's domain demands real depth on.
- The C# `data-interchange#GEO_INTERCHANGE` owns a deep geo vocabulary — the NTS interior with exactly two wire projections, WGS84-fixed CRS posture, admission-side `PrecisionModel`, write-only RFC 9746 ring orientation — but the TS `geometry` row "sources no contract of its own, decoding only the GeoJSON embedded in snapshot-delta and evidence-envelope payloads" through a bare `JSON.parse`, with no `Point`/`LineString`/`Polygon`/`Multi*`/`GeometryCollection` type discrimination, no coordinate-shape filter, and no CRS guard. A malformed `type` literal or a projected-coordinate payload passes silently.
- Unlocks the `codecs/decode-rail.md` `geometry` row deepened to a real `GeoJsonWire` `Schema.Union` over the seven GeoJSON geometry types plus the `Feature`/`FeatureCollection` wrappers, the coordinate-tuple `Schema.filter` enforcing 2-or-3-ordinate position shape, the WGS84 longitude/latitude bound (the C# CRS-fixed-by-format law decoded as a filter so an out-of-range coordinate faults rather than rendering corrupt), and the embedded-provenance boundary preserved (the row still sources no contract of its own — it reads the GeoJSON the C# NTS `GeoJsonConverterFactory` emits under the wire profile, transcribed not re-authored). The geometry row becomes a typed-discriminated owner, not a `JSON.parse` passthrough.
- Draws on the NTS-interior-two-wire-projections law and the GeoJSON RFC 7946 type vocabulary; the measure-bearing XYM/XYZM degradation the C# blob projection routes around is the named boundary — the embedded GeoJSON row decodes XY/XYZ positions only, an M-ordinate payload faulting as `Breaking` drift because the GeoJSON text wire structurally cannot carry it.

[STREAMING_SEGMENT_DECODE]: the sync-segment stream is decoded frame-by-frame through the synchronous `Decoder`, leaving the admitted `@msgpack/msgpack` async stream decoders and the snapshot compression-codec axis unexploited.
- The `messagepack` `CODEC` row reads one snapshot frame through `snapshotDecoder.decode(bytes)`, but `@msgpack/msgpack` ships `decodeMultiStream`/`decodeArrayStream` (async generators over a `ReadableStreamLike` that handle chunking and buffering internally) — the natural decoder for the `csharp:Rasm.Persistence/sync/collaboration` sync-segment stream the `OpLogEntryWire` array crosses on — and the `SnapshotHeaderWire.codec` `none`/`lz4`/`zstd` axis is decoded as a literal yet no decompression seam admits a compressed frame, so a `lz4`/`zstd` snapshot frame decodes to garbage.
- Unlocks the `codecs/decode-rail.md` `messagepack` row extended with a `decodeSegmentStream` entry lifting the C# sync-segment `ReadableStream` into a backpressured `Stream<OpLogEntryWire, ParseError>` through `Decoder.decodeArrayStream`, and the compression-codec admission — the `SNAPSHOT_CODEC` literal gates a decompression row (the `lz4`/`zstd` browser decode seam) before the `Decoder` reads, so a compressed snapshot frame decompresses-then-decodes rather than parsing compressed bytes as MessagePack. The decompression provider question is the named RESEARCH spike (a wasm `lz4`/`zstd` browser decoder vs the C# `K4os.Compression.LZ4`/`ZstdSharp` producer).
- Draws on the `@msgpack/msgpack` async-stream-decoder capability the folder leaves unexploited and the symmetric chunk-algebra law (peak memory one chunk wide, backpressure pull-shaped) the C# `data-interchange#CHUNK_ALGEBRA` legislates; the segment stream shares the same `Stream.buffer({ capacity: 1, strategy: "suspend" })` one-frame window the transport framing fold already carries, never a parallel buffering scheme.

## [2]-[CLOSED]

[CAPABILITY_PALETTE_SEAM]: KILLED — speculative cross-folder seam, not a folder concept. The grounded SDK leg (`CapabilitySdk.catalog`/`invoke`/`mcpTools` codegen) is already a `transport#CODEGEN_TOOLING` row the `CAPABILITY_SDK_CODEGEN` task builds; the asserted `ui` command-palette leaf is duplicative (`ui#COMMAND_SURFACE_VOCABULARY` already owns the `cmdk` palette over the `CommandGateway`, not the discovery catalog), and the `platform` in-browser MCP-host edge is anchored speculation with no landing idea and contradicts the cross-`libs/` `ONE_AGENT_TOOL_PROJECTION` decision routing MCP to `services/agent/mcp-transport`. No capability lost — the SDK leg survives in `CAPABILITY_SDK_CODEGEN`.

[CONTENT_KEY_PARITY_HARNESS]: content-key leg realized — `parity/content-key-parity.md` `ContentKeyParity` binds the worker content key bit-identically to the FROZEN `csharp:Rasm/Geometry/topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` row [1] `CANONICAL_BYTE_IDENTITY` through the production `xxHash128Of` LE↔BE normalize; the `typescript:testing/` corpus consumer is the future DRIVER and the HLC two-half leg split to the `HLC_TWO_HALF_PARITY` blocker. One content identity, no second mint.

[CRDT_OP_DECODE]: realized — `codecs/decode-rail.md` `[3]-[CRDT_OP_DECODE]` owns the ten-arm `CrdtOpWire` union decode (`set | write | add | remove | increment | insertAfter | delete | maintain | beat | leave`) over the `crdt`-family `OpLogEntryWire.payload`, sourced verbatim from `csharp:Rasm.Persistence/versioning/version-control#CRDT_WIRE` (the `[tag, [fields]]` array decode, the flat `(physicalTicks, logical)` HLC halves, the `op`-literal normalize); single-mint verified, no `kind`-discriminated CRDT re-mint.

[PROTOCOL_SELECTION_AXIS]: realized — `transport/transport.md` `WireTransportLive` owns the `TransportProtocol` `Match.exhaustive` over `createConnectTransport`/`createGrpcWebTransport`, one interceptor, the per-protocol `TransportCapabilityWire` tuple read from the wire.
[STRUCTURED_DRIFT_REPORT]: realized — `quarantine/drift-terminal.md` owns `quarantineFold`, the three-case `ContractDrift` `Data.TaggedEnum`, `driftReportOf`, and the `ISSUE_DRIFT` issue-tag→`DriftClass` vocabulary; a Breaking drift surfaces as `FaultDetail.Quarantine` carrying the exact failing path.
[DECODE_BUDGET_REFINEMENT]: realized — `refinement/schema-refinement.md` owns `DecodeBudget`/`INGRESS_BUDGET`/`decodeBounded`/`boundedFrames`; `drift-terminal.md` owns the `sanitize` `DOMPurify` row. The ingress depth/frame/byte ceilings and the rendered-text ceiling are typed rows.
[TRANSFERABLE_STREAM_REASSEMBLY]: realized — `artifacts/frame-reassembly.md` owns `transferFrames` (`Stream.toReadableStream`) and `xxHash128Of` (`hash-wasm` `createXXHash128`); `ArtifactFrameRailLive` delegates to `DecodeWorkerPool.reassemble` so the worker holds the single-hash-mint. The `webTransport` raw-byte leg feeds this same transferable seam as its named growth case.
