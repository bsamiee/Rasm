# [RHINO_HOST]

Rhino host truth comes from local RhinoWIP XML, decompile evidence when XML is silent, the API rail, source-owned bridge scenarios, and `libs/csharp/Rasm.Rhino/AGENTS.md`.

## [1][READ_ROUTE]

- Member availability: local RhinoWIP XML and the API rail.
- Missing XML semantics: decompile evidence or runtime bridge scenario.
- Boundary design: `libs/csharp/Rasm.Rhino/AGENTS.md`.
- Runtime proof: source-owned `*.verify.csx` scenarios through the bridge rail.

## [2][BOUNDARIES]

Rhino docs choose proof routes; `Rasm.Rhino` owns public boundary shape. Keep native values, nullable returns, bool failures, resource lifetimes, UI dispatch, and document mutation inside typed boundary rails before exposing project concepts.

## [3][REJECTIONS]

- No RhinoCommon member catalog.
- No wrapper-only native rename.
- No compatibility projection unless live Rhino documents can still emit the native value and the owning boundary maps it to a canonical project value.
