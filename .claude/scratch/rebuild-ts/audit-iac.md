# [AUDIT_IAC] — line-by-line doctrine audit of `libs/typescript/iac`

Scope: 9 design pages (`program/{spec,provider,automation}`, `kube/{workload,traffic,data}`, `operate/{secret,observe,policy}`), 2197 LOC. Doctrine read in full (README + language/derivation/values/computation/shapes/surfaces-and-dispatch/rails-and-effects/services-and-layers/concurrency/streams/boundaries). API verified against `iac/.api/pulumi-pulumi.md`, `iac/.api/pulumi-policy.md`, plus the shared `libs/typescript/.api/` effect tier. Findings only — nothing edited but this file.

## [DOCTRINE_LAW_SET] — the sixteen laws, stated, with bearing on iac

FLOW:
- `EXPRESSION_SPINE` — domain logic is expression-shaped on `Effect<A,E,R>`; dependence→sequence, independence→`Effect.all`/`zipWith`; statements only in measured kernels/platform callbacks. BEARS: the `Effect.async` engine wrap and PulumiFn closures are the only sanctioned statement seams; the mutable event array (automation) tests this.
- `BOUNDARY_ADMISSION` — raw material decodes exactly once (`Schema.decodeUnknown`), interior never re-validates. BEARS: `StackSpec`/`StackOutputs`/`RunReceipt`/`DriftReport` decode correctly at their one seam; the OutputMap FFI reads are ruled boundary.
- `CAPABILITY_CHANNEL` — dependency travels `R` as a Tag, satisfied only by Layer. BEARS: iac deliberately uses NO Layer graph (deploy-plane values, Config reads, Pulumi providers); this is defensible but means the campaign's "single deep owner" pressure lands on the `Dispatch`/`Automation`/`Tier` objects instead.

SHAPE:
- `SCHEMA_AUTHORITY` — the Schema owner is the single shape authority; a hand interface/DTO beside it is the defect. BEARS: honored for decoded values; `Args`/`Exposure`/`Root`/`Issued` are Input-typed foreign live values, legitimately plain types — EXCEPT `_Connection` (multi-page-consumed named product left a Struct) and `Traffic.Exposure` (hand-union).
- `SHAPE_BUDGET` — one deep owner ~⅕ the naive shape count; variants are tags inside one family. BEARS: `Traffic.Exposure` and the `_pairs`/`pairs` twin are the budget breaks.
- `DEEP_SURFACES` — one/two exports, `_`-interior, terminal `[EXPORTS]` block. BEARS: honored; automation's 4 exports sits at the edge.
- `MODAL_ARITY` — one entrypoint owns every modality, discriminating on input value, never suffix/knob. BEARS: the channel-pairs projection exists twice (decoded vs live-Output) instead of one modality-polymorphic owner.
- `ANTICIPATORY_COLLAPSE` — the owner is shaped for the family it absorbs; the diff of the next feature is one row. BEARS: `_preload` hardcodes extension names instead of a row flag; `_PLANES` restates the field set.

DERIVATION:
- `VOCABULARY_VALUES` — one `as const` table is the SoT; secondary surfaces derive; `Match`/`switch` over a keyed domain a row maps is rejected. BEARS: `_arms`/`_tiers`/`_capabilities`/`_map`/`_scale`/`_KEYS`/`_charts`/`_uses`/`FaultPolicy`/`_ops` are strong vocab tables; `_preload` and `Exposure.mode ===` violate it.
- `DERIVED_TYPES` — types compute from one anchor (`typeof`/`keyof typeof`/indexed). BEARS: guard pairs are present and mostly correct; `_PLANES` and `Automation.Options` restate derivable sets.
- `SEMANTIC_NAMING` — canonical term, role, tense; no parallel labels for one concept. BEARS: the rail-vs-throw split is a semantic inconsistency (one precondition class, two handlings).

MATERIAL:
- `LIBRARY_DEPTH` — the Effect ecosystem is the stdlib; use the deepest operator the package reaches. BEARS: `Stream` is unused where the `.api` INTEGRATION row prescribes it (engine events); `Schedule` is unused where `FaultPolicy.retry` demands it.
- `INLINE_COMPOSITION` — wrapping/telemetry/resilience attach at the owner declaration as rail transformers. BEARS: automation attaches NO resilience/telemetry transformer to the run owner.

INTEGRATION:
- `ROOT_REBUILD` — new capability woven into the owner as if always there; no shims. BEARS: broadly honored (tiers, Dispatch, Automation are cohesive owners).
- `ONE_HOP_RESOLUTION` — a name resolves in one hop. BEARS: `_driven`→`Automation.receipt` forward-hop is legal; no alias chains found.
- `COMPOSED_IMPLEMENTATION` — features compose the page owners at full operator depth; flat code is sprawl. BEARS: the psql SQL-string blob and the native `Object`/`Array.prototype` policy validators are the flattest surfaces (both boundary-ruled).

---

## [PER_PAGE_FINDINGS]

### program/spec.md

- **[MEDIUM] `_Connection` is a multi-consumer named product modeled as `Schema.Struct`** — §3, `const _Connection = Schema.Struct({ host, user, port })`. It is exported as the type `StackSpec.Connection` and consumed across pages: `provider.md` `_proven` returns `{ connection: StackSpec.Connection }`, `Bootstrap.Args.connection`, `traffic.md` reads `spec.connection.host`. shapes.md OWNER_FORMS row [01] + OWNER_SELECTION: "a concept with a name is declared as a class first, never migrated to one … a passed-around `Struct` promoted to a `Class` later re-anchors every consumer." STRONGER FORM: `class Connection extends Schema.Class<Connection>("Connection")({ host, user, port })` with an `endpoint`/`ssh` getter absorbing the `ssh://${user}@${host}:${port}` string that `provider.md` hand-builds in the docker arm and `bootstrap` re-assembles. COLLAPSE: the connection concept gets one owner carrying host/user/port + the derived SSH URL getter, deleting the ad-hoc interpolation at three sites. (`_Data`/`_Fanout` are single-consumer profile sub-blocks — legitimately inline Structs; `_Doppler` single-owner — borderline-fine.)

- **[MEDIUM] `_PLANES` restates `keyof StackOutputs.fields`** — §5, `const _PLANES = ["ingress","data","object","fanout","otlp","grafana","sharding"] as const`. The `pairs` getter iterates `_PLANES`; the `_Planes` guard (`_Planes<K extends keyof StackOutputs = (typeof _PLANES)[number]>`) proves `_PLANES ⊆ keyof StackOutputs` but NOT the reverse — a new `StackOutputs` field with no `_PLANES` row compiles and is silently dropped from `pairs`, so the guard is one-directional where derivation.md requires two-sided closure (completeness AND excess). STRONGER FORM: derive the plane list from the field record (`Struct.keys(StackOutputs.fields)` / iterate the class fields) and delete `_PLANES` + its guard entirely — the pairs projection then cannot miss a plane. ANTICIPATORY_COLLAPSE: "a new plane is one `Option` field" becomes literally true instead of "one field plus one `_PLANES` entry."

- **[MEDIUM/HIGH — cross-page] `StackOutputs.pairs` is one half of a duplicated channel-flatten fold** — §5 getter vs `provider.md` `_pairs`. See folder verdict #4; the channel spelling `${plane}.${field}` and the plane iteration exist twice.

- **[MINOR] `StackOutputs.read` iterates the OutputMap twice for the secret-refusal detail** — §5, `Object.values(outputs).every(...)` then a second `Object.keys(outputs).filter(...)`. One `Record.reduce`/`Array.filterMap` pass could produce both the boolean gate and the leaked-key list. Ruled-boundary FFI reads (OutputMap is foreign material), so native `Object.*` is legal here; the doubling is the only nit.

- **[STRENGTH] StackSpec/StackOutputs/Tier are correct owners** — decoded material rides `Schema.Class`; `Tier` is a clean single boundary adapter over Pulumi's class model (`child()` merge-fold + mandatory `seal()`), and the arm/tier vocabulary tables (`_arms`/`_tiers` + guard pair, `primary` getter) are exemplary VOCABULARY_VALUES.

### program/provider.md

- **[MEDIUM/HIGH — cross-page] `_pairs` duplicates `spec.md`'s channel-flatten** — §5, `const _pairs = (planes) => Array.flatMap(Record.toEntries(planes), ([plane, held]) => Array.map(Record.toEntries(held), ([field, value]) => [`${plane}.${field}`, pulumi.output(value).apply(String)]))`. Identical channel derivation to `StackOutputs.pairs`, differing ONLY in the value shaper (`pulumi.output(value).apply(String)` for live Outputs vs `String(value)` for decoded). This is the [12] "functions differ only by a literal/shaper" + [17] fold-duplication collapse trigger, and it defeats spec.md's own claim that "a field rename cannot drift from its channel" — `_pairs` folds the arbitrary `outputs` record built inline in the k8s arm, mechanically unlinked from `StackOutputs`' field set. STRONGER FORM: a single `StackOutputs.pairsOf(source, stringify)` owner (data-first, `Function.dual`) parameterized on the value-stringify, consumed by both the decoded getter and the in-program Output builder — MODAL_ARITY: one entrypoint owns the decoded and live-Output modalities.

- **[MEDIUM — systemic] k8s arm proves only connection+key on the rail; domain/zone/account are left to tier throws** — §5, `_ARMS["selfhosted-k8s"]` calls `_proven(spec, material)` (connection+key → DeployFault) but never lifts `spec.domain`/`spec.zone`/`spec.account` before entering the PulumiFn, so `Traffic`'s constructor throws `RunError` for them (see traffic finding). spec.md's SPEC_OWNER law says a selfhosted arm's absent coordinate "fails as a typed `DeployFault` inside its provider arm, never as an `undefined` read." STRONGER FORM: extend `_proven` (or add an arm-local `_coords` proof) to lift every spec-derived coordinate the arm's tiers require onto the DeployFault rail before returning the closure — unifying the fault rail (see folder verdict #1).

- **[LOW/MEDIUM] `_map` string cells are a hand-synced parallel to `_ARMS`' real constructions** — §2, cells like `"kubernetes.apps/v1.Deployment + core/v1.Service"` are inert descriptive strings with no mechanical link to what `_ARMS["selfhosted-k8s"]` actually builds; the page's "the capability-by-arm data and the arm program … cannot drift" claim is overstated — the table and the constructions CAN silently diverge. This is a capability-matrix doc (read by `column`/`cell`/audits), so full derivation is impractical, but the "cannot drift" framing is unverified; downgrade the claim or make at least the primary column's cells assertions the arm construction can be checked against.

- **[STRENGTH] `_ARMS` is a correct exhaustive HANDLER_RECORD** — `{ readonly [K in StackSpec.Arm]: Dispatch.Arm }` makes a missing arm a compile error; `Pins`/`Material` parameterize every deploy-time fact (no hardcoded versions); one provider seam per arm; `Dispatch` assembles rows + operations under one owner. Prepared arms returning `{}`/minimal are ruled deferrals, consistent with buildout-over-removal.

### program/automation.md

- **[HIGH] The engine-event stream is a mutable `Array` push accumulation, not the Stream bridge the `.api` and streams.md prescribe** — §4, `_driven`: `const events: Array<EngineEvent> = []; const onEvent = (event) => void events.push(event)` inside `Effect.async`, folded after settle by `_folded`. streams.md INGRESS_BACKPRESSURE explicitly rejects "a listener pushing into an outer array" and names `Stream.asyncScoped`/`asyncPush` as the callback-ingress owner; `iac/.api/pulumi-pulumi.md` §06 RAIL row [03] prescribes exactly `opts.onEvent → Stream.asyncPush / Queue.unbounded + Stream.fromQueue … Stream.runFold buckets the OpMap`. The array push is the [17] collapse trigger, and the summary/steps/diagnostics fold that follows is three re-scans of one buffer. STRONGER FORM: bridge `onEvent` through `Stream.asyncPush` (or a `Mailbox`/`Queue` + `Stream.fromQueue`) inside the run's `Scope`, and derive the receipt with `Stream.runFold` (or `Sink` fan) in one pass — matching the admitted capability the `.api` INTEGRATION row was written to force. (The `Effect.async` settle callback is a legitimate platform seam; the per-callback array accumulation across many invocations is the part streams.md refuses.)

- **[HIGH] `FaultPolicy.retry` is a declared-but-dead policy column; the run owner internalizes zero resilience** — §3/§4. `FaultPolicy.concurrent.retry: true` and `DeployFault.byRank` exist, but `Automation.run`/`reconcile`/`_driven` compose NO `Effect.retry`, `Effect.timeout`, or `Schedule` — the page punts recurrence to "a caller-composed `Schedule`." This violates (a) rails-and-effects POLICY_VALUE ("recurrence is a named `Schedule` value composed once beside the fault family it serves") — the family has a `retry` row and no schedule beside it; (b) the campaign RESILIENCE mandate ("circuit breakers, bulkheads, hedging, adaptive retry ride EVERY … backend, and egress owner INTERNALLY … never bolted on by consumers") — the Automation API is the deploy plane's primary egress and internalizes nothing; (c) INLINE_COMPOSITION. STRONGER FORM: a module `_PULSE = Schedule.exponential(...).pipe(Schedule.jittered, Schedule.intersect(Schedule.recurs(n)), Schedule.whileInput((f: DeployFault) => f.policy.retry))` composed on the mutating runs, plus a per-run `Effect.timeoutFail` budget and (optionally) span/metric transformers — so a state-lock collision self-heals and consumers compose capability, not plumbing. Right now `FaultPolicy.retry` is data no rail reads.

- **[MINOR] `Automation.Options` restates the five knob fields of `_RunOpts`** — §4. `_RunOpts` = the five options + `signal`/`onEvent`; `Automation.Options` = the same five, hand-listed. derivation.md: derive `type Options = Omit<_RunOpts, "signal" | "onEvent">` instead of a parallel declaration that can drift.

- **[MINOR] Automation module exports 4 owners** — `{ Automation, DeployFault, FaultPolicy, RunReceipt }`; DEEP_SURFACES prefers one/two. Defensible (driver + fault family + policy anchor + receipt are all genuinely consumed downstream, RunReceipt by policy.md), but at the budget edge — the `FaultPolicy` anchor could ride `DeployFault` as a static namespace to shed one export.

- **[STRENGTH] The ledger dispatch and interruption binding are exemplary** — `_LEDGER` exhaustive record over `RunReceipt.Op` (a sixth op is a compile error), `Effect.async` handing its `AbortSignal` to the op (no orphan updates — verified: `UpOptions.signal` is real per `.api`), `previewRefresh`/`exportStack`/`importStack`/`addEnvironments`/`import` all verified real, `DeployFault.triaged` a correct `Match.instanceOf` foreign-triage with subclasses-before-base ordering and an `alien` `orElse` quarantine (surfaces-and-dispatch FOREIGN_TRIAGE), `ephemeral` release as `Effect.orDie` (never-typed release channel per rails RESOURCE_BRACKET).

### kube/workload.md

- **[STRENGTH — no material findings]** — `_LIFE`/`_scale`/`_KEYS`/`_POLICY` are four correct guarded vocabulary tables; the env seam (`_KEYS` channel→var map, `_rows` policy-merge widening over `_POLICY`, `filterMap` dropping unmapped channels, the open-index read lifted through `Option.fromNullable(_keyed[channel])` per language.md STRICTNESS) is doctrine-exemplary; the `_LIFE` one-anchor→two-projections (grace period + `RUNTIME_LIFE_DRAIN`) is a clean anti-drift collapse; `_labels` single derivation threads selector/template/service. Only nit: the env `rows` mixes the token `secretKeyRef` row with value rows in one array — correct and intended.

### kube/traffic.md

- **[MEDIUM] `Traffic.Exposure` is a hand-rolled discriminated union dispatched by `.mode ===`** — §2 namespace `type Exposure = { mode: "direct"; address } | { mode: "tunnel" }`, built inline (§3, `const exposure: Traffic.Exposure = args.spec.profile.exposure === "direct" ? {...} : {...}`) then consumed by one ternary `exposure.mode === "direct" ? new DnsRecord(...) : _tunneled(...)`. shapes.md TAGGED_FAMILIES rejects "a `kind` field beside `_tag`" and "a string-typed `status` field where a case family owns the states"; OWNER_SELECTION rejects "a shape minted to name an intermediate step." STRONGER FORM: this closed process-local 2-case family is `Data.taggedEnum<{ Direct: { address: string }; Tunnel: {} }>` dispatched by `$match`, OR — since it is constructed and consumed exactly once — collapse the intermediate entirely and dispatch the ternary directly on `args.spec.profile.exposure` (a proven `keyof`-typed literal), deleting the `Exposure` type and the `exposure` const.

- **[MEDIUM — systemic] domain/zone/connection/account preconditions throw `RunError` at construction** — §3, four `Option.getOrThrowWith(args.spec.X, () => new pulumi.RunError(...))` calls in the constructor. Same precondition class the arms prove on the DeployFault rail, handled here by construction-time throw. See folder verdict #1 — the k8s arm should prove these coords on the rail before entering the PulumiFn, minting `DeployFault`, so the fault rail is unified rather than split rail/throw. (Pulumi tier constructors are a platform seam where `RunError` is native, but these values are spec-derived and knowable pre-PulumiFn, so the throw is avoidable.)

- **[MINOR] `_record` is an unused binding** — §3, `const _record = exposure.mode === "direct" ? new DnsRecord(...).name : _tunneled(...)`; the `.name` Output is computed and never read (construction is the effect). Make it a bare expression statement or drop the `.name`.

- **[STRENGTH] `_EDGE`/`_connector` single-anchor projections** — controller class + fence namespace, connector labels + fence admission read one spelling each; the "a fence that blocks its own connector is unspellable" collapse is genuine.

### kube/data.md

- **[MEDIUM] `_preload` hardcodes extension names instead of a row flag** — §4, `_preload = (granted) => Array.filterMap(granted, row => row.extension === "pg_cron" || row.extension === "timescaledb" ? Option.some(...) : Option.none())`. This is the [14] "Match/filter arms restate a keyed correspondence" trigger: the "demands `shared_preload_libraries`" property should be a column on `Pg.rows` (exactly as `_DEMANDS` encodes dependency edges), read as `row.preload`. As written, a third preload-demanding extension added to `Pg.rows` is silently missed by `_preload` and fails only at the runtime startup probe. STRONGER FORM: a `preload: boolean` flag on the matrix row, filtered by `Array.filter(granted, row => row.preload)` — ANTICIPATORY_COLLAPSE, the next preload extension lands as a data row with zero code edit.

- **[LOW] the ensure Job runs a hand-joined SQL string blob via `psql -c`** — §5, `ensures.join(";\n")` concatenated into one `psql -c` argument. This is string-built SQL, which the campaign's NO-NAIVE-SQL law forbids — but it is boundary-ruled: "the deploy host reaches only the Kubernetes API, never the `.svc` network," so no `@effect/sql` `SqlClient` can bind the cluster PG, and the DDL must run in-cluster via a Job over opaque strings the data plane authored with real rails. ACCEPTABLE under the ruling; two residual robustness notes: (a) the blob runs without a `BEGIN;…COMMIT;` wrapper, so `ON_ERROR_STOP=1` leaves partial DDL on mid-roster failure — wrap the join in a transaction; (b) the strings are trusted verbatim — the data plane must own their construction (it claims to).

- **[STRENGTH] the CNPG finalization is dense and correct** — `_granted` two-stage validation (name-resolve then `_DEMANDS` dependency proof), matrix-pinned `extensions` rows, admin/app as two distinct basic-auth secrets, the `-rw` host as a single `pulumi.all` derivation reused as both bind target and `StackOutputs.data.host`, `protect: true` on the cluster, CR catch-all for operator vocabulary. The `_granted` throws are the same rail-vs-throw systemic item (folder #1), not a new defect.

### operate/secret.md

- **[MINOR] `read(key)` throws inside `.apply` via an IIFE** — §2, `result.map[key] ?? (() => { throw new pulumi.RunError(...) })()`. Functionally correct (a missing key aborts the run named, never forges an empty string) and bounded to the apply seam, but the nullish-coalescing-throw IIFE is the ugliest expression in the corpus; a small `_require(map, key)` boundary helper, or lifting the miss to the same DeployFault posture the arms use, reads cleaner. Same rail-vs-throw family as folder #1.

- **[STRENGTH] `_MIRRORS` and the entry discriminant are exemplary dispatch** — the `_MIRRORS` handler record keyed by `Secrets.Mirror["target"]` with per-row `Extract` payload correlation is a textbook surfaces-and-dispatch HANDLER_RECORD (a new destination is one row + one union case, mirror stays FROM-canonical-outward); the entries discriminant via `Predicate.hasProperty(entry, "generate")` is correct structural FORM_CHOOSER dispatch; `_Policy` decoded once and shared; `_uses` vocabulary → `Schema.Literal` spread; `Certs` as an assembled `as const` owner over three members with Input-typed `Root`/`Issued` (correctly plain types, not Schema — they carry live `tls.PrivateKey`/`Output`). No structural defect.

### operate/observe.md

- **[MEDIUM — incompleteness] `_alertRows`/`_sloRows` are `declare const` phantoms** — §4, both declared as bodyless signatures because "the `Alert.Spec` `sli`-to-rule-model compile is the standing design item." Per campaign phantom law these are NOT silent (the page rules them explicitly), so they are deliberate deferrals, not deletions — but the observe plane's ALERTING and SLO realization is unbuilt: `Boards` constructs the folder, data sources, and dashboards for real, then calls two undefined functions. This is the folder's largest single unbuilt capability. Under buildout-over-removal it should be RANKED for realization (the catalogued `alerting.RuleGroup`/`ContactPoint`/`NotificationPolicy`/`slo.Slo` class names are already named), not left as a permanent stub.

- **[STRENGTH] the rest of the tier is correct** — `_charts`/`_urls`/`_SOURCES` vocab tables; two-role endpoint projection (ingest vs query, "never one URL doing both jobs"); `pulumi.jsonStringify(model)` for content-hashed dashboard drift; one provider per stack threaded through `child()`; `storeDashboardSha256: true` as the deploy-plane echo of the content-key discipline. The collector-as-sole-ingest-seam design is clean.

### operate/policy.md

- **[MEDIUM/HIGH — coverage gap] `role-no-superuser` narrows the wrong class for the primary arm** — §3, `_noSuperuser = validateResourceOfType(postgresql.Role, ...)`. The PRIMARY `selfhosted-k8s` arm creates the app role as a CNPG `managed.roles` CR row inside the `Cluster` CustomResource (data.md §4), NOT as a `@pulumi/postgresql.Role` resource — the `postgresql.Role` class only appears on the `selfhosted-docker` cell. So on the primary arm, a `superuser: true` managed role is UNGUARDED. `_protectedData` correctly matches CNPG CRs by `apiVersion`/`kind` props (`resource.props.apiVersion === "postgresql.cnpg.io/v1"`), proving the pattern works — `_noSuperuser` should do the same. STRONGER FORM: add a `StackValidationPolicy` that filters CNPG `Cluster` CRs and reports any `spec.managed.roles[].superuser === true`, so the "data plane cannot escalate" law covers the arm that actually ships. (`validateResourceOfType`, `validateStackResourcesOfType`, `remediateResourceOfType`, `PolicyResource.props/opts.protect`, `getConfig`, `Secret` all verified real per `.api/pulumi-policy.md`.)

- **[LOW] validators use native `Array.prototype`/optional-chaining/`void (cond && report())`** — §3. `@pulumi/policy` callback shape is fixed and receives foreign unwrapped prop bags, so these are boundary-framework kernels (acceptable — effect `Array` cannot key off `deployment.spec?.template.spec?.containers`), but they are unmarked; a `// BOUNDARY ADAPTER` mark on each validator body would make the exemption recoverable per language.md KERNEL_EXEMPTION_SITE.

- **[STRENGTH] the pack and drift owners are correct** — `_policies` grows by rows never arms; `Guard` a pure `PolicyPackArgs` value with a boot-edge one-line analyzer entry (side-effect-free lib); `DriftReport` REUSES `RunReceipt.fields.summary`/`.steps` (real `Schema.Class.fields` — the drift vocabulary cannot fork from the receipt vocabulary, exemplary derivation); `_skewed` a two-sided fold-audit over `RunReceipt.opTypes`; `Drift.sweep` degree via `{ concurrency: "inherit" }` + boundary `Effect.withConcurrency` (concurrency.md EXPLICIT_DEGREE, correct); rotation matched by `type.startsWith("tls:")` prefix, one watch channel.

---

## [FOLDER_VERDICT] — deepest structural weaknesses, ranked by leverage

1. **Dual-paradigm precondition handling — rail `DeployFault` vs constructor `RunError` throw (spans provider/traffic/data/secret).** The arms prove SOME spec-derived coordinates on the DeployFault rail (`_proven` connection+key; prepared arms' `Effect.mapError(spec.region,...)`), but the k8s arm's tiers throw `RunError` at construction for domain/zone/account/connection (traffic §3) and extension validity (data `_granted`), and `Secrets.read` throws for a missing key. One precondition class, three handlings. The campaign's "unified rails, never dual paradigms" and spec.md's own law ("fails as a typed `DeployFault` inside its provider arm, never as an `undefined` read") both demand: every spec-derivable precondition proves on the rail in the arm before entering the PulumiFn. Highest leverage because it is the correctness/consistency spine and the fix is mechanical (extend the arm's `_proven`-style coordinate proof). Values genuinely unknowable pre-PulumiFn stay platform-seam throws; spec-derived ones do not.

2. **Missing internal resilience on the Automation egress; `FaultPolicy.retry` is a dead column (automation).** The deploy plane's primary backend/egress internalizes zero retry/timeout/breaker despite declaring `retry`/`rank` policy data and a `byRank` fold. The campaign's RESILIENCE-AS-A-CONCEPT mandate and rails POLICY_VALUE both require a `Schedule` composed beside the fault family and ridden by the run owner. Currently the retry gate is data no rail reads and consumers must bolt on recurrence — the exact inversion the campaign forbids.

3. **Naive callback accumulation instead of the Stream bridge the `.api` prescribes (automation).** The engine-event mutable-array push + three-scan fold is the folder's one streaming surface, and it ignores the streams.md callback-ingress law AND `iac/.api/pulumi-pulumi.md`'s explicit `Stream.asyncPush … Stream.runFold` integration row. This is the campaign's PUBSUB/STREAMING emphasis landing on the one place iac has a push source, unrealized. (No machine or pubsub gap otherwise — the deploy legs are correctly a record/ledger, not a statechart; the drift sweep's `Effect.repeat` is right.)

4. **Split-brain channel-pairs fold + `_PLANES` hand-parallel (spec + provider).** The `<plane>.<field>` env-channel flatten is implemented twice (`StackOutputs.pairs` over decoded planes, `_pairs` over live Outputs), differing only by value-stringify, mechanically unlinked — defeating the anti-drift claim spec.md makes for itself. `_PLANES` further restates `keyof StackOutputs.fields` with a one-directional guard. Collapse to one modality-polymorphic `pairsOf(source, stringify)` owner and derive the plane set from the field record.

5. **Weak-shape residue: `_Connection` Struct, `Traffic.Exposure` mode-union, `_preload` hardcoded names.** Three small SHAPE/DERIVATION breaks: a named multi-page product left a `Schema.Struct` (should be a `Schema.Class` carrying the SSH-URL getter the corpus hand-builds three times), a closed process-local case family dispatched by `.mode ===` (should be `Data.taggedEnum` + `$match` or collapse the intermediate), and a `pg_cron || timescaledb` filter that should be a `preload` row flag. Individually minor, collectively the campaign's SHAPE-WAR pressure.

6. **Policy coverage gap: `role-no-superuser` misses the primary arm (policy).** The superuser guard narrows `postgresql.Role`, a class only the docker cell uses; the shipping k8s arm's CNPG managed roles are unguarded. `_protectedData` already proves the CNPG-CR matching pattern — mirror it. Correctness, not just coverage.

7. **Unbuilt alerting/SLO realization (observe).** `_alertRows`/`_sloRows` are ruled deferrals, not silent phantoms, but they are the folder's largest unbuilt capability against a fully catalogued class set — rank for buildout.

8. **Minors:** `_map` "cannot drift" overstatement, `StackOutputs.read` double-iteration, `Automation.Options`/`_RunOpts` restatement, unused `_record`, unmarked policy-validator kernels, the ensure-Job transaction wrapper. Low individual leverage; a hygiene sweep.

Net: the folder's OWNERS are strong (Schema classes for all decoded material, exhaustive handler records, guarded vocabulary tables, clean Tier adapter, correct derivation in DriftReport/pairs-getter/label projections, correct interruption/bracket/degree). The weaknesses are RAILS discipline (precondition rail unification, internalized resilience) and the two streaming/fold collapses, not shape spam — the decoded-vs-Input shape split is mostly principled. Fixing #1–#4 converts iac from "correct owners, split rails" to the 13/10 single-entry-point-with-integrated-intelligence bar the campaign targets.
