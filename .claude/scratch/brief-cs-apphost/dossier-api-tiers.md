# [DOSSIER] Rasm.AppHost — api-tiers lane

Scope read COMPLETE: both catalog tiers (`libs/csharp/.api/` = 30 files / 4574 LOC; `libs/csharp/Rasm.AppHost/.api/` = 70 files / 7038 LOC), `Directory.Packages.props` (537 LOC), `Rasm.AppHost.csproj` (103 LOC), `ARCHITECTURE.md` + `README.md` + `IDEAS.md` + `TASKLOG.md`, and 6 planning pages deep-read to judge mined-vs-unmined (`Runtime/modules.md`, `Runtime/orchestration.md`, `Wire/outbound.md`, `Agent/capability.md`, `Agent/reasoning.md`) plus `Sandbox/isolation.md` + `Wire/livewire.md` (targeted). Every AppHost-tier catalog was grepped against the 29-page corpus for its consuming page.

HEADLINE: the AppHost `.api` corpus is NOT naive — it is one of the strongest catalog sets in the tree. The gold catalogs are integration-shaped with `[STACK]`/`[INTEGRATION_STACK]` sections that name exact consuming seams, and the pages mine them to the letter (verified). The defects are structural-hygiene, not depth: cross-tier duplication, one research-tail catalog, one Compute-framed overlay, a WASI Preview-2/Preview-1 catalog-vs-page mismatch, a missing LanguageExt catalog, and a csproj that pulls app-root-reserved packages. This lane's campaign value is TIER HYGIENE + two capability corrections, NOT catalog rewrite.

---

## [01]-[PER-CATALOG VERDICTS]

Verdict scale: 10 = gold, integration-shaped, exact-mined; 8-9 = strong first-class; 6-7 = solid but generic/broad; <6 = defective. `owner charter as it SHOULD be` given only where it diverges from current.

### AppHost tier — Agent cluster (GOLD)

- `api-extensions-ai.md` (10): 180-type provider-agnostic surface; `reasoning.md#REASONING_LOOP/MODEL_GOVERNANCE` mines `IChatClient`/`ChatOptions.Tools|ToolMode|ModelId|Seed`/`ChatResponse.Usage`/`UsageDetails.TotalTokenCount`/`ChatResponseUpdate.ToChatResponse()`/`IEmbeddingGenerator.GenerateAsync`/`DelegatingChatClient` exactly. Over-documented-never-demanded: `ITextToSpeechClient`,`IRealtimeClient`,`IHostedFileClient` (only STT+Image gated in `reasoning.md#MODAL_INPUT`); `CitationAnnotation`/`AnnotatedRegion`/`AIJsonSchemaTransformCache`.
- `api-extensions-ai-middleware.md` (10): the concrete `ChatClientBuilder` + `DelegatingChatClient`/`AnonymousDelegatingChatClient` distinction is mined precisely by `reasoning.md#MODEL_GOVERNANCE.Compose` (`AsBuilder().UseOpenTelemetry().UseDistributedCache().Use(GoverningChatClient).UseFunctionInvocation().Build()`), including the "internal AnonymousDelegatingChatClient is uninstantiable → subclass is the public seam" law.
- `api-mcp.md` (10): full 3-package MCP surface (Core/host/AspNetCore), long-running task/sampling/elicitation session verbs, `AsSamplingChatClient` attribution note. Consumed by `Agent/mcp.md`. Deep and self-correcting (`RequestContext<T>` inheritance caveat).
- `api-ml-tokenizers.md` + `-cl100k.md` + `-o200k.md` (10/10/10): the cost-broker integration is exact — `capability.md#ModelProjection` closes `CostModel.Variable` over `TiktokenTokenizer.CreateForModel`/`CountTokens`, and `capability.md#RESEARCH[TOKEN_PRICING]` cites the catalog entry indices. The two data-package catalogs correctly document zero-public-surface reference-presence activation.
- `api-tensors.md` (10): the `[INTEGRATION_STACK]` embedding-rank rail is mined verbatim by `reasoning.md#SEMANTIC_DISCOVERY.Cosine = TensorPrimitives.CosineSimilarity(query.Vector.Span, candidate.Span)` over the frozen `Embedding<float>` index. Note: this AppHost overlay (134 LOC) is a re-scoped trim of the shared substrate copy (219 LOC) — see cross-cutting duplication.
- `api-authorization.md` (9): host-neutral ABAC via `AddAuthorizationCore()`; consumed by `Agent/identity.md` (ripple `RUNTIME_IDENTITY_AUTHZ_PLANE`). Clean.
- `api-identitymodel-jwt.md` / `-oidc.md` / `-protocols.md` / `-tokens.md` (10/10/10/10): the four interlock into the "two legs meet at the validated token" story with exact version-unification analysis (8.16.0 vs 8.19.1 NuGet unify). `-protocols.md` correctly documents a transitive-only (unpinned) package as the `ConfigurationManager<T>` seam. Consumed by `Agent/identity.md`.
- `api-openiddict-client.md` (10): full RP-client flow surface; `net10.0` first-class asset noted. Consumed by `Agent/identity.md`. Over-documented-never-demanded: device/token-exchange/password/custom-grant/introspection/revocation likely exceed identity.md's credential-flow subset (unverified against identity.md).

### AppHost tier — Observability cluster (GOLD on healthchecks, strong on OTel/Serilog)

- `api-healthchecks-npgsql.md` / `-redis.md` / `-kafka.md` / `-uris.md` / `-system.md` (10 each): EXEMPLARY. Each carries a `[STACK]` section naming the exact seam (`HealthContributorRow.Peer(tag: Store|Remote|Pressure)`, `HealthSurface.Register`, `DEGRADATION_RAIL Rule(...)`, `Wire/companion#HEALTH_SERVICE.SetStatus`) and the shared-driver integration (same `NpgsqlDataSource`/`IConnectionMultiplexer`/`ProducerConfig` the store/cache/topics rail already pools). Consumed by `health.md` + `ports.md` + `livewire.md`. This is the gold standard the whole tree should match.
- `api-health.md` (9): `Microsoft.Extensions.Diagnostics.HealthChecks` base contracts. Consumed by `health.md`.
- `api-resource-monitoring.md` (9): documents the observable-instrument (`MeterListener`) read model AND `ResourceQuotaProvider.GetResourceQuota()` container-quota path, marking `IResourceMonitor.GetUtilization` obsolete-v10.7.0. Consumed by `health.md`+`laneguard.md`+`isolation.md`. Precise.
- `api-otel.md` (9), `-exporter.md` (9), `-hosting.md` (8), `-instrumentation.md` (10): full OTel surface; `-instrumentation.md` enumerates the 17 pre-.NET-9 runtime instruments and the .NET-9 `AddMeter("System.Runtime")` dispatch — decompile-grade. NOTE: `-exporter.md` documents `OpenTelemetry.Exporter.OpenTelemetryProtocol`, an APP-ROOT-reserved surface per `ARCHITECTURE.md:119/139`; its presence in the app-platform tier + csproj is the boundary tension (verdict candidate).
- `api-serilog.md` (9), `-hosting.md` (9), `-sinks.md` (8): full projection surface; `-sinks.md` RAIL_LAW "Reject: sink configuration below AppHost composition" implies AppHost IS the composition root, contradicting `ARCHITECTURE.md:119` "app roots own ... Serilog host bridge and sinks." Boundary tension (verdict candidate).
- `api-telemetry.md` (9), `-abstractions.md` (10): `-abstractions` documents the latency-token/request-metadata/generator-attribute surface precisely, including the `Microsoft.Extensions.Http.Diagnostics` `RequestMetadata` split — mined by `outbound.md#HTTP_PIPELINES` (`AddDownstreamDependencyMetadata`/`SetRequestMetadata`).
- `api-logging.md` (8), `api-redaction.md` (9), `api-pyroscope.md` (9): pyroscope is a tight one-type `BaseProcessor<Activity>` catalog with exact root-span detection. redaction names its `Observability/telemetry#REDACTION_TAXONOMY` + Persistence `ClassificationGuard` seams. NOTE: `api-redaction.md` is byte-identical to the shared-tier copy (duplication).

### AppHost tier — Runtime/Wire/Sandbox clusters

- `api-di.md` (9): `Describe`/`DescribeKeyed`/`TryAddEnumerable`/`ActivatorUtilities.CreateFactory`/`IServiceProviderIsService`/`CreateAsyncScope` mined exactly by `modules.md#SCAN_AND_DECORATE/BOUNDARY_ACTIVATION`. It documents the `AddSingleton/AddScoped/...` family the page explicitly DELETES (correct — the catalog carries the full surface, the page's boundary forecloses the spellings).
- `api-scrutor.md` (9): scan/decorate surface mined exactly by `modules.md` (`FromAssemblies`/`AssignableTo`/`UsingAttributes`/`RegistrationStrategy.Throw`/`Decorate`/`TryDecorate`/`IsDecorated`/`GetDecoratedServices`).
- `api-validation.md` (8) + `-di.md` (9): FluentValidation; `-di` mined by `modules.md#BOUNDARY_ACTIVATION.ValidatorRow`.
- `api-config.md` (8), `-config-providers.md` (9), `-binder.md` (9), `-options.md` (9): the config-composition spine; `-providers` documents the `OptionsBuilder<T>.BindConfiguration` validate-on-start rebind precisely. Consumed by `config.md`.
- `api-hosting.md` (8), `-hosting-lifetimes.md` (9): Generic Host + systemd sd_notify (Linux-server-only, correctly fenced off Windows). Consumed by `lifecycle.md`/`profiles.md`.
- `api-hybrid-cache.md` (8): `GetOrCreateAsync`/`RemoveByTagAsync` stampede-aware cache; consumed by `resources.md`+`reasoning.md` (model-response cache). NOTE: byte-identical to shared-tier copy (duplication).
- `api-objectpool.md` (8): consumed by `profiles.md`+`resources.md`.
- `api-dataflow.md` (9): `BroadcastBlock`/`JoinBlock`/`BatchedJoinBlock`/bounded-block options; mined by `topics.md` (the `DrainSurface`/`DrainKind` bus). `System.Threading.Tasks.Dataflow` is a Transitive-Floor pin, correctly documented as such.
- `api-cronos.md` (9): DST-correct occurrence math + `H` jitter; consumed by `time.md`.
- `api-openfeature.md` (10): the config-backed `InMemoryProvider`/`Flag<T>` sticky-bucketing surface; mined by `features.md` + `reasoning.md#MODEL_GOVERNANCE` (`FlagVerdict` → `ModelRoute.From`).
- `api-hashing.md` (9): `XxHash128`/`XxHash3` non-crypto identity; universally mined (`orchestration.md`/`capability.md`/`reasoning.md` content hashes). AppHost overlay (53 LOC) trims the shared copy (78 LOC).
- `api-nuget-versioning.md` (10): the `SemverGate` integration (`VersionRange.TryParse`+`Satisfies` replacing `System.Version`) is spelled to the call site; consumed by `isolation.md`+`provisioning.md`.
- `api-resilience.md` (9), `api-polly-core.md` (10), `-extensions.md` (9), `-ratelimiting.md` (10): the outbound resilience spine; `-ratelimiting` documents the `System.Threading.RateLimiting` companion limiters (`SlidingWindow`/`TokenBucket`) and `-core` the `FallbackActionArguments<T>`/`ResiliencePropertyKey<T>`/`CircuitBreakerStateProvider` — ALL mined verbatim by `outbound.md#KEYED_PIPELINES/OWNERSHIP_LAW`. Exceptional.
- `api-service-discovery.md` (9): `ServiceEndpointResolver`/round-robin selector; consumed by `coordination.md`.
- `api-grpc-aspnetcore.md` (9): server DI + `HealthServiceImpl` serving-status projection; consumed by `companion.md`. APP-ROOT-reserved server surface (boundary tension). AppHost overlay (142 LOC) trims shared copy (258 LOC).
- `api-grpc-core-api.md` (9): server call-context surface; consumed by `companion.md`. NOTE: byte-identical to shared-tier copy (duplication).
- `api-grpc-client.md` (7 — DEFECT): mined by `outbound.md#DISCOVERY_ATTACH` (`GrpcChannel.ForAddress`+`ConnectCallback` UDS) and `coordination.md`, BUT the catalog's `LOCAL_ADMISSION`/`RAIL_LAW` (`api-grpc-client.md:330-341`) is written for COMPUTE ("Compute remote calls enter through client-side channels only", "Server-side gRPC packages remain outside the Compute package graph", "Generated clients are typed edge adapters over Compute request and receipt algebra"). The AppHost overlay was trimmed from the shared copy (248 changed lines) but the Compute framing was not re-scoped to AppHost's actual consumers (Wire/outbound UDS + Wire/coordination resolver). Over-documented-never-demanded: the custom `Resolver`/`LoadBalancer`/`Subchannel`/`PickResult` client-LB surface (ServiceDiscovery owns LB; outbound uses only `ForAddress`+`ConnectCallback`+channel options).
- `api-jsonpatch.md` (8): RFC 6902 JSON Patch; consumed by `config.md`+`features.md`+`companion.md`. NOTE: byte-identical to shared-tier copy (duplication).
- `api-nodatime.md` (8) + `-stj.md` (9): time substrate; universally mined. `api-nodatime.md` byte-identical to shared copy (duplication); `-stj` is a re-scoped overlay (146 changed lines) documenting the `Wire` precedence law. Over-documented: `Instant64`/`Duration64`/`XmlSerializationSettings`/most zone resolvers (BCL completeness beyond AppHost's receipt-timestamp need).
- `api-thinktecture-json.md` (9): STJ converters for generated owners; universally mined. Re-scoped overlay (149 changed lines) vs shared copy.
- `api-sigstore.md` (10): the offline admit-gate (`TryVerify*Async` → `Fin<T>`, air-gapped `FileTrustRootProvider`) with a full `[INTEGRATION_STACK]` naming the semver/identity/provisioning/resilience/telemetry legs; consumed by `isolation.md`+`provisioning.md`. Gold.
- `api-velopack.md` (8): self-update lifecycle; consumed by `provisioning.md`. Uses raw ```csharp signature``` fences (the only catalog doing so) rather than the table grammar — stylistically inconsistent with the tier but content-complete.
- `api-wasmtime.md` (6 — MISMATCH): documents the CORE-MODULE + WASI-Preview-1 surface (`Engine`/`Module`/`Linker`/`Instance`/`WasiConfiguration`/`Linker.DefineWasi()` = preview-1, `api-wasmtime.md:32,152,163`; 158 types, NO `Component`/Component-Model type). BUT `isolation.md:17` designs "a WebAssembly component instance ... WASI-Preview-2 component" isolation. wasmtime-dotnet 44.0.0 (per the catalog's own inventory) exposes core modules only — the Component-Model surface the page's import-only isolation model needs is neither documented nor available. Catalog-vs-page capability divergence → domain gap (verdict candidate).
- `api-mqtt.md` (10), `api-opcua.md` (10): full industrial client surfaces; mined by `livewire.md#LIVE_BINDING` (`IMqttClient`/`MqttClientFactory`; managed `Opc.Ua.Client` `Session`/`Subscription`/`MonitoredItem` + PubSub `UaPubSubApplication`). opcua over-documents server-side (`Opc.Ua.Server`) + cert-authoring (`CreateSigningRequest`/`RevokeCertificate`) + reverse-connect (livewire uses the managed client only). APP-ROOT-reserved runtime (boundary tension).
- `api-modbus.md` (5 — DEFECT): the SOLE catalog carrying a `[05]-[RESEARCH]` tail (`api-modbus.md:93-95`) with UNVERIFIED member signatures ("the exact generic arity ... confirm against the pinned FluentModbus assembly at admission through the assay binder ... the precise overload set the catalogue settles when the manifest row lands"). Durable catalogs must be member-verified and omit checklist/freshness tails; `livewire.md` BINDS `ModbusTcpClient`/`ModbusRtuClient` (via `CompanionSpawn`), so the unverified `Span<T>`/`Memory<T>` return shapes are load-bearing. Owner charter as it SHOULD be: verify the `ReadHoldingRegisters<T>`/`WriteMultipleRegisters<T>`/`*Async<T>` overload set against the restored assembly, fold the confirmed signatures into the table, delete the RESEARCH section.
- `api-serialport.md` (9): BCL `SerialPort`; mined by `livewire.md` (`Serial(SerialPort Port)` live-client) and reused as the `ModbusRtuClient` line carrier. APP-ROOT-reserved runtime.
- `api-testing-seams.md` (8): TimeProvider.Testing / Diagnostics.Testing / NodaTime.Testing test-only fakes; listed in README `[TEST_SUBSTRATE]`. Placement question: these are cross-package test fakes, arguably shared-test-substrate not AppHost-owned.

### Shared substrate tier — AppHost-relevant catalogs read in full

- `api-mapperly.md` (10 as a catalog; DANGLING ref): world-class integration-shaped Roslyn-generator catalog — but scoped to `Rasm.Element`/`Rasm.Persistence` owners, ZERO AppHost consumers, and it references a PHANTOM `api-languageext.md` (`api-mapperly.md:151`) that exists in neither tier.
- `api-quikgraph.md` (10 as a catalog): exemplary — Element/Persistence/Bim owners, ZERO AppHost consumers (correct: AppHost has no graph algorithm need — see cross-cutting).
- `api-generator-equals.md` (10 as a catalog): Element/Bim/Persistence owners, ZERO AppHost consumers (correct: AppHost identity is XxHash128 content-hash + string keys, not structural equality).
- `api-highperformance.md` (9): CommunityToolkit; `RASM_COMPUTE_API_*` header — a Compute catalog. AppHost's `objectpool` lane covers its pooling need; not an AppHost concern.
- `api-thinktecture-runtime-extensions.md` (9): the ADT generator substrate; universally mined by every AppHost page ([SmartEnum]/[Union]/[ValueObject]). NO AppHost-tier copy — correctly shared-only.
- `api-grpc-common.md` (9), `api-grpc-client-web.md` (8): `RASM_COMPUTE_API_*` headers; grpc-client-web is a Compute remote-lane concern, not AppHost.

Non-AppHost shared catalogs (unicolour/unitsnet/mathnet-numerics/mathnet-providers/csparse/messagepack/protobuf/nodatime-protobuf/thinktecture-messagepack/system-configuration/grpc-tools) are out of AppHost concern; noted only for the tier-placement finding below (several are domain-specific, not "cross-cutting substrate").

---

## [02]-[CROSS-CUTTING]

### DUPLICATION (cross-tier) — structural defect
12 same-named catalogs exist in BOTH `libs/csharp/.api/` and `Rasm.AppHost/.api/`:
- 5 BYTE-IDENTICAL: `api-nodatime.md`, `api-jsonpatch.md`, `api-redaction.md`, `api-hybrid-cache.md`, `api-grpc-core-api.md`. These are redundant copies, NOT "folder-specific overlays" — they violate `CLAUDE.md [04]` ("shared substrate under `libs/csharp/.api/`; package `.api/` folders carry domain catalogues and folder-specific overlays"). Truth is ambiguous (which tier is canonical?).
- 7 RE-SCOPED OVERLAYS (differ, AppHost trims/re-scopes): `api-nodatime-stj.md` (146 lines), `api-hashing.md` (104), `api-tensors.md` (347), `api-thinktecture-json.md` (149), `api-grpc-client.md` (248), `api-grpc-aspnetcore.md` (392), `api-extensions-ai.md` (448). Defensible as consumer-scoped overlays, but carry DRIFT: `api-grpc-client.md` retained Compute-framed `LOCAL_ADMISSION`/`RAIL_LAW`, and any shared-tier truth update must be manually mirrored into 7 overlays.

### CONCERN MIXING / BOUNDARY (csproj vs ARCHITECTURE)
`Rasm.AppHost.csproj` PackageReferences app-root-RESERVED packages against `ARCHITECTURE.md:119` ("app roots own ... OTLP exporter, the MCP HTTP transport, the WASM/industrial-protocol runtimes, Kestrel/gRPC surfaces, Serilog host bridge and sinks") and `:139` ("never Serilog types below composition roots; never OTLP exporter pins below service app roots"):
- `OpenTelemetry.Exporter.OpenTelemetryProtocol` (csproj:45)
- `Serilog.Sinks.Console`/`Serilog.Sinks.File` (csproj:56-57)
- `Grpc.AspNetCore`(+HealthChecks+Web) (csproj:69-71) — server surface
- `ModelContextProtocol.AspNetCore` (csproj:88) — MCP HTTP transport
- `Wasmtime` (csproj:100), `MQTTnet` (csproj:96), `OPCFoundation.NetStandard.Opc.Ua`(+PubSub) (csproj:97-98), `System.IO.Ports` (csproj:99) — WASM + industrial runtimes.
The app-platform LIB pulls the full server+exporter+wasm+industrial stack, contradicting the "reference-light app-platform, app roots adapt" boundary. Sharpened by `livewire.md`: Modbus/Serial actually run companion-side (`OutboundHop.CompanionSpawn`), so referencing FluentModbus/System.IO.Ports in the lib is doubly questionable. Either the `[04]/[05]` boundary is wrong (AppHost IS the composition-root project — its Description "neutral composition doctrine for Rasm app packages" hints at this) or the csproj over-references. The `.api` tier placement of `api-otel-exporter`/`api-serilog-sinks`/`api-grpc-aspnetcore`/`api-mcp`(AspNetCore)/`api-wasmtime`/`api-mqtt`/`api-opcua`/`api-serialport` follows the csproj, so resolving this is a manifest+tier decision, not a per-catalog fix.

### DEAD / MISSING TYPED CARRIERS
- MISSING CATALOG: `LanguageExt.Core` — the single most-used substrate (all 29 AppHost pages compose `Fin`/`IO`/`Seq`/`Atom`/`HashMap`/`Validation`) has NO `.api` catalog in EITHER tier, and `api-mapperly.md:151` references a phantom `api-languageext.md`. High-value roster gap: the pervasive functional rail is undocumented. (Also uncataloged: `JetBrains.Annotations`, `PolySharp` — low-value annotation/polyfill, acceptable.)
- Over-documented capability the target never demands (inventory, not defects — catalogs are first-class references): `api-extensions-ai` TTS/Realtime/HostedFile modal clients; `api-grpc-client` custom Resolver/LoadBalancer/Subchannel LB surface; `api-opcua` server-side + cert-authoring + reverse-connect; `api-nodatime` `Instant64`/`Duration64`/XML/most zone resolvers; `api-mcp` likely the full task-store/elicitation surface beyond tools+sampling.

### HARDCODING-vs-GENERATOR (catalogs are DATA, correctly)
The healthchecks/identity/tokenizer/tensor catalogs are seed DATA rows on the owning polymorphic axes — `capability.md#ModelProjection`/`TensorProjection` GENERATE descriptors from the op surfaces (no hand-listed op table), `health.md` folds contributor ROWS, `outbound.md` is a 7-case `OutboundHop` union with frozen `HopPolicy` rows. No enumerated-instance-where-a-generator-belongs defect found in the catalog corpus; the catalogs correctly document capability the pages then parameterize.

### UNWIRED SEAMS / MINING (verified)
Every AppHost-tier catalog has ≥1 page consumer (grep-confirmed): jsonpatch→config/features/companion; healthchecks→health/ports/livewire; openfeature→features/reasoning; cronos→time; sigstore→isolation/provisioning; velopack→provisioning; service-discovery→coordination; resource-monitoring→health/laneguard/isolation; objectpool→profiles/resources; nuget-versioning→isolation/provisioning; wasmtime→isolation (as `wasmtime-dotnet`); mqtt/opcua/modbus/serialport→livewire; identity/openiddict/authorization→identity; tensors/tokenizers/extensions-ai(+middleware)/mcp→capability/reasoning/mcp. NO never-demanded catalog in the AppHost tier. The gold catalogs' stated seams (`HealthContributorRow.Peer`, `CostModel.Variable`, `TensorPrimitives.CosineSimilarity`, `ChatClientBuilder`, `SemverGate`) are ACTUALLY consumed at those seams — the catalog↔page binding is tight.

### FOLDER-ARCHITECTURE VERDICTS
- AppHost tier: 70 flat files, no sub-folders. Acceptable per the `.api/` convention, but at 70 files it is at the growth edge; natural clusters exist (healthchecks×5, identity×4, otel×4, serilog×3, polly×3, ml-tokenizers×3, grpc×3). Growth-pressure note, not a defect — flat is the established grammar.
- The REAL rot is the 12-file cross-tier overlap (5 identical + 7 drifting) with ambiguous ownership. The clean architecture is: shared substrate lives ONLY in `libs/csharp/.api/`; the AppHost tier carries ONLY the AppHost-specific delta (the integration-shaped overlays that add page-seam context: tensors embedding-rank, extensions-ai model-governance, grpc-client UDS scope, hashing bucketing). The 5 identical copies should be deleted from the AppHost tier; the 7 overlays should be trimmed to the AppHost delta (or the shared version referenced) and the `api-grpc-client.md` Compute framing re-scoped.
- Package-roster discipline is HIGH: every AppHost catalog is mined; the roster is not padded. The only additions the tiers NEED are a `LanguageExt.Core` catalog (shared) and a Component-Model WASM decision (isolation).

---

## [03]-[VERDICT CANDIDATES] (campaign-defining)

1. CROSS-TIER CATALOG DUPLICATION (5 byte-identical + 7 drifting overlays). Evidence: `diff` proves `api-nodatime.md`/`api-jsonpatch.md`/`api-redaction.md`/`api-hybrid-cache.md`/`api-grpc-core-api.md` IDENTICAL across `libs/csharp/.api/` and `Rasm.AppHost/.api/`; 7 others differ 104-448 lines. Ruling: delete the 5 identical AppHost copies (shared tier is canonical per `CLAUDE.md [04]`); trim the 7 overlays to the AppHost delta; establish the tier-ownership rule (shared = substrate truth, package = integration overlay only).

2. WASI PREVIEW-2 vs PREVIEW-1 CATALOG-PAGE MISMATCH (capability/domain gap). Evidence: `api-wasmtime.md:32,152,163` documents core-module + WASI-Preview-1 only (158 types, no Component Model); `isolation.md:17` designs a "WASI-Preview-2 component instance" import-only isolation model. wasmtime-dotnet 44.0.0 does not expose the Component Model. Ruling: DECIDE at campaign time — either the catalog under-documents (research whether a Component-Model-capable binding exists / wasmtime-dotnet added it) or `isolation.md` reverts to core-module + WASI-Preview-1 isolation; a named DOMAIN GAP (Component-Model WASM runtime) if neither.

3. MISSING `LanguageExt.Core` CATALOG + PHANTOM REFERENCE. Evidence: no `api-languageext.md` in either tier; all 29 AppHost pages compose `Fin`/`IO`/`Seq`/`Atom`/`HashMap`; `api-mapperly.md:151` references the phantom. Ruling: author a shared `libs/csharp/.api/api-languageext.md` (LanguageExt.Core 5.0.0-beta, the `Fin`/`Validation`/`Option`/`Eff`/`IO`/`Seq`/`HashMap`/`Atom` rails the whole tree mines) and repair the dangling mapperly reference.

4. csproj REFERENCES APP-ROOT-RESERVED PACKAGES vs `[04]/[05]` BOUNDARY. Evidence: `Rasm.AppHost.csproj:45,56-57,69-71,88,96-100` reference OTLP exporter / Serilog sinks / Grpc.AspNetCore server / MCP AspNetCore / Wasmtime / MQTTnet / OPCFoundation / System.IO.Ports, all reserved to app roots by `ARCHITECTURE.md:119,139,142`. Ruling: resolve the app-platform-vs-composition-root identity of `Rasm.AppHost` (Description says "neutral composition doctrine") and align the csproj + the tier placement of the app-root-surface catalogs (`api-otel-exporter`/`api-serilog-sinks`/`api-grpc-aspnetcore`/`api-mcp`-AspNetCore/`api-wasmtime`/industrial) to whichever way the boundary is decided.

5. `api-modbus.md` RESEARCH-TAIL WITH UNVERIFIED SIGNATURES. Evidence: `api-modbus.md:93-95` is the sole catalog with a `[05]-[RESEARCH]` section admitting its `ReadHoldingRegisters<T>`/`*Async<T>` overload signatures are unverified; `livewire.md` binds `ModbusTcpClient`/`ModbusRtuClient`. Ruling: member-verify against the restored FluentModbus assembly, fold the confirmed overloads into the entrypoint table, delete the RESEARCH tail (durable-catalog hygiene: no checklist/freshness tails, member-verified only).

6. `api-grpc-client.md` (AppHost overlay) RETAINS COMPUTE FRAMING. Evidence: `api-grpc-client.md:330-341` `LOCAL_ADMISSION`/`RAIL_LAW` speak of "Compute remote calls"/"Compute package graph"/"Generated clients ... over Compute request and receipt algebra"; AppHost's actual consumers are `outbound.md#DISCOVERY_ATTACH` (UDS `GrpcChannel.ForAddress`+`ConnectCallback`) and `coordination.md` (`ServiceEndpointResolver`). Ruling: re-scope the overlay's admission/charter to AppHost's UDS-control + service-discovery consumers, or drop the redundant client-LB surface (Resolver/LoadBalancer/Subchannel) ServiceDiscovery owns.

7. CATALOG CORPUS IS GOLD-TIER, NOT NAIVE — REFRAME THE CAMPAIGN. Evidence: healthchecks×5 / identity×4 / tensors / tokenizers×3 / extensions-ai(+middleware) / mcp / sigstore / nuget-versioning / openfeature carry `[STACK]`/`[INTEGRATION_STACK]` sections naming exact seams, verified mined by `capability.md`/`reasoning.md`/`outbound.md`/`modules.md`/`orchestration.md`/`health.md`/`livewire.md`; every catalog has a page consumer; the roster is not padded. Ruling: the api-tiers campaign is TIER HYGIENE (dedup #1, add LanguageExt #3, fix modbus #5, re-scope grpc-client #6) + two capability decisions (WASM #2, csproj boundary #4) — NOT a catalog-depth rebuild. Do not force catalog rewrites where the corpus already exceeds the bar.

8. GENERATOR SUBSTRATE CORRECTLY OMITS AppHost — DO NOT FORCE-MINE. Evidence: `api-mapperly.md`/`api-quikgraph.md`/`api-generator-equals.md` scope to Element/Persistence/Bim with zero AppHost consumers; `modules.md` composes by declaration-order DI fold + `ValidateOnBuild` (no topological sort), `orchestration.md` is a linear-saga `Seq<WorkflowStep>` with reverse-fold compensation (no DAG), `outbound.md` crosses the wire via STJ serialization (no object-mapping), AppHost identity is `XxHash128` content-hash (no structural equality). Ruling: a REJECT of the tempting "AppHost under-mines the generator substrate" claim — these non-uses are architecturally correct; the campaign must NOT add Mapperly/QuikGraph/Generator.Equals to the AppHost roster.
