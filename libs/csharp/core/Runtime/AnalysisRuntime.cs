using Core.Domain;
namespace Core.Runtime;

// --- [MODELS] ----------------------------------------------------------------------------------

public sealed record AnalysisRuntime(GeometryContext Context);
