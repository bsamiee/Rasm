# [TS_REGION_MAP_PAGE_REGIONS]

TS region map — row shapes and protocol live in the [suite standard](../../../csharp/.planning/README.md) ledger-protocol section.

[TYPESCRIPT]:

- architecture-posture.md [PROVISIONAL]: the Effect application altitudes (one TS form per altitude), the closed five-owner app-service budget, the key-discriminated state layer posture, and the co-hosted single-origin host topology.
- wire-consumption.md [PROVISIONAL]: the single contract inventory of the eleven C# TS_PROJECTION clusters, the one descriptor-set codegen pipeline, the per-surface codec posture, and the drift-tolerance versioning law the consumer derives from the C# classification.
- wire-contracts.md [FINAL]: the byte-to-typed boundary; one shared same-origin transport, five decode rails read by codec key plus the embedded GeometryRail, and the QuarantineFold turning every malformed frame or unknown discriminant into a typed staleness marker.
- state-stores.md [FINAL]: the key-discriminated folds collapsing server-streams and receipt sequences into immutable keyed maps with wire-declared ordering, the envelope carrier binding every structured-text payload, and the staleness-forward and availability-gate posture.
- view-surfaces.md [FINAL]: leaf rendering through the one sanctioned atom binding and the query-string deep-link binding, and the read-only observation routes for evidence, benchmark, the maplibre-base/deckgl-overlay geo composition, and collector panels.
- control-edge.md [FINAL]: the browser write edge; the single command gateway over three control verbs and the intent registry keyed for deep-link survival, gated on the availability fold.
- runtime-host.md [FINAL]: the co-hosted SPA root; one Layer graph and runtime providing the five closed app-services, the browser platform layer, the AuthSession credential owner, the self-telemetry export edge, the build and PWA pipeline, and the browser decode-worker pool.
- node-tier.md [PROVISIONAL]: the node-only execution surfaces under one exports root; the resource components and automation lifecycle and policy, the observability-stack provisioning, the durable cluster-workflow tier with ClusterEngine resolved wiring and the proxy-exposed InternalRpc, and the runner-and-scheduling backplane.
- test-strategy.md [PROVISIONAL]: the cross-cutting verification law; the algebraic property-test spine with the per-file LOC and coverage floors, the browser-mode and end-to-end runner for the atom-bound view and deep-link flows, and the mutation kill-ratio gate plus the durable-cluster container harness.
- architecture-tree.md [PROVISIONAL]: the planning atlas; the non-flat domain-folder source tree projected from the eight domain pages, the dual conditional entry split, the descriptor-pipeline placement, and the inward dependency direction re-derived from the finalized owner set each loop.
