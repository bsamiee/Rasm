# REVIEW F06 - 06-higher-order-functions.md

Scope checked: fabrication against the assembly and the lab, code-to-lab identity, boundary and one-path rulings, surgical discipline against Painter ch5 / ch4 and Buonanno ch1, coherence with 22 and 24.

Verified clean, not repeated below: all 13 `csharp` blocks are byte-identical to `F06/*.cs` (modulo the `namespace` line); the inline `flip(Subtract)` and `number => actors.Find(number).IfNone("Unknown")` are lab text; `compose` (`Prelude` line 11332, body `b(a(v))`), `flip(Func<A, B, R>)` (10064), `fun(Action)` (10620), `use<A>(Func<A>) where A : IDisposable` (11839), `unless`/`when` (8540, 8601, both `K<F, Unit>`), `parseInt(string)` (12136), `Range(int from, int count)` (4842), `Try.lift(...).Run()`, `Option.IfNone(Func<A>)`, `Seq` `Do`/`Map`/`Fold`/`ForAll`/`Exists`/`Find`/`Zip`, `HashMap.Find`, `Trampoline`, `Monad.recur` all exist with the stated semantics. The file passes every self-check in the brief (299 lines, no cross-file reference, no banned token, no bare `Eff<X>`). Every correction below is line-neutral: each edits an existing line, so the file stays at 299.

1. **Location** line 97.
   **Claim** "This value-level operation, also called Chain, applies one function to one value; Painter names it `Map`."
   **Evidence** Added prose. The brief bans citations and semicolons in added or changed text, and the next sentence already carries the rename fact ("It is named `Pipe` here so that `Map` keeps its structure-preserving meaning").
   **Correction** Replace the sentence with:

   > This value-level operation, also called Chain, applies one function to one value.

2. **Location** line 82.
   **Claim** "`use` acquires the `IDisposable` stub `Connection` inside an `IO` query and disposes it when the query ends."
   **Evidence** Two defects. "stub" narrates lab scaffolding into the textbook. `IOUseDisposable<X, A>.Invoke` registers the resource with `envIO.Resources.Acquire(...)` (decompile of `LanguageExt.DSL.IOUseDisposable of two type arguments`), so release is bound to the resource scope of the run, not to the query expression; `22-languageext-effects.md` states the same fact as "disposes it when the scope ends".
   **Correction** Replace with:

   > `use` acquires the `IDisposable` `Connection` inside an `IO` query and disposes it when the scope ends.

3. **Location** line 288.
   **Claim** "`Try.lift(f).Run()` captures a throwing synchronous call as a `Fin<A>`, and `IO.lift(f)` captures it on the `IO` error channel."
   **Evidence** The two members are named for one concern with no rule separating them, which reads as alternatives. The global ruling gives `Try<A>` to a throwing synchronous dependency and `IO<A>` to a deferred effect; the lab probes both (`Try.lift(...).Run()` and `IO.lift(...).RunSafe()` both `Fail`).
   **Correction** Replace with:

   > `Try.lift(f).Run()` captures a throwing synchronous dependency as a `Fin<A>`. `IO.lift(f)` defers the same call and carries the failure on the `IO` error channel for the host to run.

4. **Location** line 297.
   **Claim** "Use recursive state transitions only when termination is bounded, and run a deep loop through `Trampoline` or `Monad.recur`."
   **Evidence** "or" offers two paths for one concern. The ruling and the lab separate them: `Trampoline` runs pure recursion (`Trampoline.More` at depth 100000), `Monad.recur` runs an effectful state loop with `Next.Loop`/`Next.Done` over `IO`.
   **Correction** Replace the bullet with:

   > - Use recursive state transitions only when termination is bounded, run a deep pure loop through `Trampoline`, and run a deep effect loop through `Monad.recur`.

5. **Location** line 211 (drift against Painter ch5, "Non-mutating updates to lazy sequences").
   **Claim** The doc drops the whole `ReplaceAt`/`ReplaceWhen` section and its claim that each operation returns a new lazy projection leaving the source unchanged.
   **Evidence** Painter ch5 lines 223-249. `NOTES/F06.md` records every other removal ("Dead code removed with its prose", "Left out") but not this one, so the drop has no recorded cause. LanguageExt supplies the replacement directly: indexed `Seq<A>.Map((item, index) => ...)` (LAB-NOTES, collections row).
   **Correction** Extend line 211 in place (no new line):

   > `Seq.Map` is deferred: the descriptor functions run when the result is enumerated. This can postpone unnecessary work, while repeated enumeration repeats the projection. The indexed form `Map((item, index) => ...)` rewrites one position and returns a new `Seq`, leaving the source unchanged.

   If the drop is intentional, record it in the "Left out" list of `NOTES/F06.md` instead.

6. **Location** line 299 (drift against Buonanno ch1, "Judgment and tradeoffs").
   **Claim** The doc drops the cost claim: "Higher-order functions add callback frames. The performance cost is usually negligible, but debugging call stacks can become less direct."
   **Evidence** Buonanno ch1 line 46. The closing paragraph keeps only the hidden-semantics warning, so the file now states a benefit with no cost, and the fact is still true.
   **Correction** Replace line 299 with:

   > Conciseness is valuable when it exposes intent. Higher-order functions add callback frames, so a debugger shows less direct control flow. If an abstraction hides ordering, effects, missing-value semantics, or termination risk, the removed boilerplate has merely been displaced.

7. **Location** "Encapsulate resource lifecycles" section, line 82 (drift against Buonanno ch1).
   **Claim** The doc drops "Real I/O operations should normally be performed asynchronously" with the `SqlConnection` code and names no asynchronous entry anywhere in the file.
   **Evidence** Buonanno ch1 line 35. `NOTES/F06.md` records the omission, so the cause exists, but the reader is left with no path for an asynchronous body. `IO.liftAsync(Func<Task<A>>)` is verified in LAB-NOTES.
   **Correction** Optional, line-neutral: append to line 82 the sentence

   > An asynchronous body enters the same `IO` through `IO.liftAsync`.

8. **What a project built on this file still needs** (beyond the three items already in `NOTES/F06.md`): the `Fork` join has no failure channel. A prong that can fail returns `Validation<Error, A>` and the join becomes the tuple `Apply((left, right), (a, b) => ...)` with `.As()` at that boundary, so both prong failures accumulate; the file's `Fork` joins raw values only.

Blockers: none.
