# [REGIONS] — domain/data-interchange.md

[PAGE_REGION]:
- domain/data-interchange.md: data interchange law — analytical-engine session posture (one refcounted anchor, `Duplicate()` lanes, read-only `ATTACH` boundary, typed `ErrorType` rejection discriminant, extension sealing, ephemeral scratch topology); the symmetric chunk algebra with named commit points, construction-validated mapped appenders, and the appender-versus-table-function routing; columnar egress as one `COPY` rail over artifact-class policy rows with footer-stamp admission, generation reads, and JSON shredding-as-admission; delimited exchange as one profile record deriving reader, writer, and descriptor with ref-struct row law and modality-invariant projections; artifact identity as descriptor stamp plus content hash under a declared compatibility lattice whose verdict union gates every delivery arm; geo interchange as one NTS interior vocabulary with two policy-frozen wire projections (GeoJSON factory row, GeoPackage blob codec) and container admission law.

[SNIPPET_REGIONS]:
- DI-01 ENGINE_SESSION: analytical engine posture as one frozen record — `DuckDBConnectionStringBuilder` indexer rows (`threads`, `memory_limit`, `temp_directory`, `max_temp_directory_size`, `preserve_insertion_order`) composing the boot tail, shared-in-memory versus file-source modality rows, one anchor connection per session with `Duplicate()` lanes, read-only `ATTACH`/`DETACH` as the foreign-store boundary verbs, and the negative `GetQueryProgress().Percentage` property-pattern projection to `Option`. Distinct from RT-01 (engine boot posture, not host-builder fold) and CC-01 (catalog session, not channel-lane rows); the absence projection is supporting material, not B-01's tuple-dispatch region.
- DI-02 CHUNK_COMMIT: bulk movement as one chunk algebra — the construction-validated `DuckDBAppenderMap<T>` mapped appender with its whole lifetime inside one capture so construction refusals, `AppendRecords`, and the explicit `Close()` commit point all land on the rail, beside the `UseStreamingMode` reader whose failure rail rides mid-iteration `Read()`; error-late surfacing converted at the declared commit point. Distinct from R-01 (capture grammar is supporting material; commit-point law is the spotlight) and CC-06 (no drain band, no conservation fold).
- DI-03 PROJECTION_RAIL: columnar egress as one `COPY (SELECT) TO` statement assembled from artifact-class policy rows — format, compression codec, row-group geometry, optional `PARTITION_BY`, collision verb, footer `KV_METADATA` stamp — through an `Option`-row `Somes()` assembly, plus the `parquet_kv_metadata` footer read as the metadata-cost stamp gate. Distinct from TP-04 (SQL policy-row assembly, not server exposure fold) and RT-02 (egress rows, not descriptor fold).
- DI-04 EXCHANGE_PROFILE: one delimited exchange-profile record deriving reader options, writer options, and the descriptor stamp from one column-row declaration — pinned separator, invariant culture, unescape-plus-trim, thread-safe pooled strings, escape-on-write, `ColNotSetOption` absence spelling — with header indices resolved once from the same column rows via `IndicesOf`, one try-shaped row projection to `Fin` rows materialized inside the reader scope via `Strict()`, and span-first `Set`/`Format` emission keyed by the column rows. Distinct from SA-02 (options-profile derivation, not UTF-8 format-parse) and B-05 (no byte identity).
- DI-05 DESCRIPTOR_VERDICT: schema evolution as descriptor-diff data — name-keyed column-set algebra under one declared compatibility `Lattice` policy value (widening retype rows, nullability widening), duplicate-header rejection before the algebra, revision monotonicity discriminating stale-producer from contract-break, one closed verdict union carrying evidence, the delivery union whose arms each carry locator-plus-stamp-plus-hash provenance through one `Switch` projection, and the delivery gate folding the verdict onto the `Fin` rail through a second state-threaded `Switch`. Distinct from TP-03 (data-shape lattice over name-keyed columns, not protobuf `FileDescriptor` checksum; no reserved ranges), VL-02 (admission verdict over artifacts, not validation-failure bridge), and A-10 (artifact admission taxonomy, not solver terminal partition).
- DI-06 GEO_WIRE_PROFILE: the geo text profile as one `GeoJsonConverterFactory` row carrying the wire policies (admission `GeometryFactory` with fixed `PrecisionModel`, bbox, id property, `RingOrientationOption.EnforceRfc9746`, table mutability), options composed once beside generated contracts and frozen, and the dual-class read rejection — positioned `JsonException` beside the unpositioned type-literal `ArgumentException` — folded at one seam with JSON-null projecting to `Option`. Distinct from B-08 (factory registration and policy coherence, not a hand-written family converter) and TP-07 (one options compose, not resolver-merge law).
- DI-07 GEO_BLOB_GATE: the GeoPackage blob codec as one policy-frozen pair — `GeoPackageGeoReader` rows (`HandleSRID`, `RepairRings`, `HandleOrdinates`) and the writer ordinate cap — behind the `"GP"u8` magic signature gate, with declared-versus-header SRID agreement as a typed rejection and symmetric emit through one codec value. Distinct from TP-06 (signature gate plus codec policy, no framing/checksum corridor) and B-05 (decode admission, not byte identity).
