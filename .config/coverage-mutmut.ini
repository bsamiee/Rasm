# Mutmut-only coverage config, selected via COVERAGE_RCFILE on the mutmut invocation; the main
# suite and the fail_under=90 gate keep pyproject's `relative_files = true` untouched.
# Survives outside pyproject because mutmut keys covered lines by ABSOLUTE mutant path:
# CoverageData.lines(abs) is an exact-key lookup and [tool.coverage.paths] aliases apply only at
# combine, which mutmut's in-process gather (Coverage(data_file=None), in-memory, no file ever
# written) never runs — relative keys collapse every covered set to empty and abort with
# "no covered mutants". INI format (NOT TOML): `source` is a whitespace-separated plain value,
# resolved under the `mutants/` cwd the gather measures in.
[run]
relative_files = false
branch = true
core = sysmon
source =
    tools/assay
