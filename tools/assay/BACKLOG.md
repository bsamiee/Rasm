# Assay — Known Issues & Backlog

Open issues only. **Every item is verified real** (reproduced, or confirmed against installed-library source) — nothing speculative. Closed-wave history lives in git (`git log --follow -- tests/TESTING_BACKLOG.md`, retired 2026-06-10). Companion: `.cache/testing-infra-research-ledger.md` holds the research delta and the rejected-with-rationale list (frozendict, schemathesis, CrossHair-as-dep, atheris, codspeed, ghostwriter — declined on portability/theater grounds; do not re-propose).

## Known bugs (open)

### [BUG] docs rail — `pnpm`-routed tool rejects file arguments
`uv run python -m tools.assay docs check <file.md>` fails for ANY markdown path: the pnpm-routed tool row rejects file arguments (`error: too many arguments. Expected 0 arguments but got 1.`; re-verified 2026-06-10, envelope itself clean — exactly 1 stdout line, status `failed`). Per-file docs checks cannot pass until the catalog row / runner argv is repaired (`tools/assay/composition/catalog.py` + `rails/docs.py`). Owner: the deferred run-and-tune pass.

### [UPSTREAM] mutmut stats-phase crash — fixed locally (`max_stack_depth = -1`); issue still worth filing
The previously documented rootdir/cwd theory is **falsified** (the captured stats argv is well-formed and pytest loads the parent pyproject config — the prepended addopts prove it). Root cause, established empirically with `debug=true`: with `max_stack_depth != -1`, `record_trampoline_hit` (`mutmut/__main__.py:130`) walks caller frames calling `Path(co_filename).resolve(strict=True)`; module-level calls into mutated code during conftest import put `<frozen importlib._bootstrap>` pseudo-filenames on that stack, the strict resolve raises `FileNotFoundError`, the conftest import dies, pytest exits 4, and mutmut raises `BadTestExecutionCommandsException`. The local fix is the upstream-default `max_stack_depth = -1` (repo had 10), which disables the strict-resolve stack walk entirely. Anyone with `max_stack_depth != -1` plus import-time calls into mutated code still hits this — file upstream as-is:

> **TITLE**: record_trampoline_hit crashes with FileNotFoundError on '<frozen importlib._bootstrap>' frames when max_stack_depth != -1 and mutated code runs at import time.
> **BODY**: With max_stack_depth set (e.g. the README's max_stack_depth=8), record_trampoline_hit (mutmut/__main__.py, the `while c and f:` loop) calls Path(f.f_code.co_filename).resolve(strict=True) on every caller frame. When a mutated function is invoked at module import time (module-level constant computed by a mutated function, imported transitively from a conftest), the caller stack contains importlib machinery frames whose co_filename is the pseudo-filename '<frozen importlib._bootstrap>' (also '<frozen importlib._bootstrap_external>', '<string>'). strict=True raises FileNotFoundError('<cwd>/<frozen importlib._bootstrap>'), the conftest import aborts, pytest exits 4, and mutmut raises BadTestExecutionCommandsException — misleadingly suggesting the pytest command itself is malformed.
> **REPRO (minimal)**: src/m.py with `def cap() -> int: return 1`; src/pkg using `X = cap()` at module level; a conftest.py importing pkg; [tool.mutmut] source_paths=['src'], max_stack_depth=8; `mutmut run` -> stats phase exit 4 with the traceback above; with max_stack_depth=-1 the run proceeds.
> **OBSERVED ON**: mutmut 3.6.0, pytest 9.0.3, CPython 3.15.0b1, macOS.
> **FIX CANDIDATES**: (a) skip frames whose co_filename starts with '<' before resolving; (b) resolve with strict=False; (c) wrap the resolve in try/except FileNotFoundError and continue; (d) break on importlib frames the same way 'pytest'/'unittest' filenames break the walk. Additionally, the exit-4 path would be far easier to debug if BadTestExecutionCommandsException included the captured pytest output even without debug=true.

When filing, also mention the forked-children consequence: mutant workers fork from the post-clean parent, so any set-once global poisoned across in-process `pytest.main()` calls silently converts test-infrastructure failures into false "killed" verdicts — an argument for subprocess-isolated stats/clean phases in mutmut core.

## Deferred work (operator-sequenced, 2026-06-10)

- **[DEFERRED] Run-and-tune + README truth sync** — run one representative command per claim family with fd separation (self-test, delta, static report, code search ×2, test list, api query, bridge check, package plan, docs check); audit every envelope (1 stdout line, no stringly notes, N-of-M truncation note on every clip, `source:file:line` id format, defaults visible in `--help`, status/exit coherent); fix findings in place — including the `[BUG]` docs-rail argv above; update `tools/assay/README.md` §4–§8 to observed behavior; acceptance: `docs check tools/assay/README.md` green.
- **[DEFERRED] Quality-parity deep audit** — field-by-field command diff vs the archived reference (`.archive/quality`, kept intact for exactly this): every verb run live on both, output richness compared, gaps folded into assay holistically (py/ts paths benefit too); includes the live rhino-bridge launch/check/verify/quit cycle on both. Do not clean `.archive/` until this runs.
- **[DEFERRED] Polyglot API truth probes** — PEP 695 alias-roster pinning law in the api rail tests (the `_INSPECT_KINDS` `("type", TypeAliasType)` row itself already landed in W2); live envelope probes: C# `api query --key rhino-common --symbol Rhino.Geometry.Mesh`, python `--key expression --symbol Result`, TS `.d.ts` roster via a doctor-selected key.
- **[DEFERRED] W4 test-suite quality wave** — adversarial audit of `tests/python/**` (tautology/over-mock/example-cluster hunting, sabotage-verified findings), consolidation into algorithmic laws (hypothesis properties, parametrized decision tables, `model_based` RBSM, inline-snapshot goldens) under hard invariants (per-file coverage ≥ baseline, MANIFEST law coverage intact), closed by the full mutation run as the regression proof.
- **[DEFERRED] Full mutation pass + live kill-rate gate** — both phase blockers cleared (stats: `max_stack_depth = -1`; clean-tests: the set-once `TracerProvider` fix in `tests/python/_testkit/runtime.py::_otel_provider`, 2× in-process repro 0/0). The gate is ported and wired (`tools/assay/rails/mutation_gate.py`, `--group mutation` splice, 0.80 floor, `do_not_mutate`-protected); it goes live with the first uncapped run. Runs inside W4's closeout.
- **[DEFERRED] rhino-bridge ground-up rebuild** — research corpus at `tools/rhino-bridge/.research/` (review 01–11 grade A−; design 01–06 done, 07–10 deferred: code blueprints, assay-impact plan, design red-team — the object model is UNAUDITED until it runs — then distill); four Phase-0 live probes gate any build code; original tool preserved side-by-side until cutover.

## Code smells / weak sections (low severity)

### `tests/python/_testkit/_seams.py` / assay conftest
- **`SeamProbe.projected[K]` unused** in the assay specialization (`commands`/`checks` read `captured` directly). Retained as a real engine affordance (post-hoc `calls`-log view distinct from the per-call `project` stream) — flag so a reviewer does not strip it as dead.
- **`loopback_server` ty suppression (narrowed, irreducible)** — `ssh_loopback` carries only `# ty: ignore[invalid-argument-type]` (a typed `_port` reader removed the former `unresolved-attribute` code and keeps `get_port` checked). Root cause: ty cannot bind a `@asynccontextmanager`-decorated generic's type parameter `S` at the call site (proven empirically). Full removal would require a hand-rolled generic async-CM class, reintroducing the teardown/ResourceWarning risk the helper exists to prevent — not warranted for one fixture.

### `tests/python/_testkit/_strategies.py`
- **MemoryViewType identity asymmetry** — `st.binary().map(memoryview)` round-trips by equality (msgspec decodes to `bytes`), but a `type()`/identity oracle on a memoryview field would observe the asymmetry. No consumer compares by identity and no memoryview wire field exists, so it is currently inert; a one-line note marks the arm.

## Deferred (real, intentionally held — not yet warranted)

- **`de_eq` fuzzy-eq adapter — deleted 2026-06-09 (unconsumed)**: revival is one line — return the `IsApprox` instance directly (`def de_eq(*, delta: float | None = None): return IsApprox(delta=delta)`) handed to oracle `eq` kwargs as right-operand comparator. Revive only with a named consumer.
- **Parameterization rejected (W1)** — `register_sut` multi-package support, a `_CAP` seam, and a profile registry were each considered and rejected as speculative scaffolding; revisit each only with a named consumer.
- **`register_random`** — still blocked, re-verified 2026-06-09: no private `random.Random` seam exists in the engines (`rg 'random\.Random|import random' tools/assay` returns zero hits); the `tests/python/_testkit/runtime.py` comment names the trigger + the hypothesis-weakref gotcha. Call `register_random(rng)` (strong ref) the moment such a seam is introduced.
- **Coverage per-test contexts** — `--cov-context=test` is documented opt-in (pyproject) but not in default addopts (coverage's dynamic context hard-errors under `--cov` and blocks `-n auto`). Wire it if per-test line attribution becomes worth the constraint.
- **CrossHair symbolic lane** — still blocked, re-verified 2026-06-09: crosshair-tool 0.0.106 (latest) publishes cp38–cp314 wheels only, no cp315 wheel for the CPython 3.15.0b1 interpreter. `rasm-symbolic` profile stays documented in the runtime plugin; enable when a cp315 wheel ships (Hypothesis backend, plugin-only — zero engine change).
- **xdist parallel lane (`-n 2` / `-n auto`)** — still broken, re-verified 2026-06-09: both exit 3 with 32 `INTERNALERROR` lines; `pytest_benchmark.logger.PytestBenchmarkWarning` is escalated to an xdist-worker INTERNALERROR by `filterwarnings = ["error"]`. Benchmark and xdist sessions stay separated (the pyproject session-separation comment carries the re-verified date and signature).
