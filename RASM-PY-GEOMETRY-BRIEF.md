# [RASM_PY_GEOMETRY_BRIEF]

Authoritative campaign input for the ground-up restructure and rebuild of `libs/python/geometry/.planning/`. The campaign is single-phase: the page-set delta is fully ruled here (seven NEW pages — the four-page `energy/` sub-domain, `mesh/serve.md`, `graduation.md`, `graph/analytic.md` — one vocabulary re-homing, zero page deletions), so no ephemeral DECISION pass runs — the durable rebuild engine takes this brief directly, its plan step classifying per-page kind from `[01]` verdicts against disk, its legs running the `[05]` partition in order. `geometry` is the THIRD folder in the Python campaign sequence (runtime -> data -> geometry -> compute): `RASM-PY-RUNTIME-BRIEF.md` and `RASM-PY-DATA-BRIEF.md` are finalized upstream law this brief consumes at the seam — the identity rename, the lanes offload axis, the reproduction corpus, and the data `MeshPayload`/Arrow-bridge/codec-shed seams are upstream rulings geometry rewires against, never re-plans. Waterfall edits applied upstream: `RASM-PY-RUNTIME-BRIEF.md` `[V5]` now pins the `offload` `retry: RetryClass` keyword, the THREAD-modality band, and the `OCCT`/`RPC`/`OCC_NATIVE` POLICY rows as consumer exports with `geometry` the named demanding consumer; `[V7]` now pins the `GLB_BY_KEY` reproduction fixture and the `IdentityPolicy`-as-generic-seed law with `geometry` named; `[V3]` now names geometry's energy plane the recipe rail's `python:` consumer; runtime `[V2]` now pins the C#-minted tessellation wire rows (`TessellationRequest` + response over the `artifact_frame` leg) as `shapes` registry rows carrying geometry's decoded field floor and `[V6]` now pins `ServerHost`/`dispatch` sibling-composable servicer registration, both with `geometry` `mesh/serve` named; `RASM-PY-DATA-BRIEF.md` `[V12]`+`[04]` now keep `networkx` branch-tier with geometry the analysis consumer of record, and `[V12]` now names geometry's `arrow_las` seam decode on the `pyarrow` data-tier owner line. `compute` is DOWNSTREAM and unbriefed: this brief pins the graduation contract compute inherits (`[V2]`) and geometry stops importing it. `docs/stacks/python/` governs unchanged and is never restated here.

## [EXECUTION]

Settled single-phase campaign, py track 3/5 — launched after the RASM-PY-RUNTIME and RASM-PY-DATA campaigns land: the durable rebuild engine runs this brief directly, one launch per `[05]-[BUILD_LEGS]` leg in leg order, hard residuals resolved before the next leg launches; legs re-run across sessions against this brief until a cold pass finds nothing.

| [STEP] | [LAUNCH] | [PRODUCES] |
| :----: | :------- | :--------- |
| 1 | `rebuild` — `{targets: "libs/python/geometry", brief: "RASM-PY-GEOMETRY-BRIEF.md"}` — one launch per `[05]-[BUILD_LEGS]` leg, in leg order | Each leg's pages authored/rebuilt/improved per the `[01]` verdicts; the run returns a `hard_residual` array |
| 2 | `resolve-residuals` — `{hard_residual: <the step-1 run's hard_residual array>}` — whenever non-empty, before the next leg launches | Cross-file residuals closed and adversarially verified on disk |

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level excellent — dense, table-driven, doctrine-fluent, `.api`-grounded — and domain-level, seam-level, and governance-level broken. Four proofs, each independently fatal to a world-class claim:

1. **An entire declared domain is phantom.** `README.md:3` headlines "the out-of-process AGPL Ladybug Tools energy/environmental companion band (climate, HBJSON building-energy, urban-district massing, and thermal comfort)"; the README `[ENERGY]` group rosters 13 packages; `pyproject.toml:130-139` carries ten `[GEOMETRY]`-tagged energy admissions plus the `[RUNTIME]` recipe trio (`:140-142`); thirteen authored `.api` catalogs (~1223 LOC) back the band — `honeybee-energy.md:126-135` fully authors the consuming owner's offload/retry/span/decode rail composition. YET `rg -i 'ladybug|honeybee|dragonfly|queenbee|HBJSON|EnergyPlus|OpenStudio|DataCollection|RecipeInterface'` over all 18 fences returns ZERO, `ARCHITECTURE.md` `[01]` has no energy node, the README router has no energy route, and no IDEAS/TASKLOG card names the band. The exact runtime-E3 recipe-illusion and data-E6 hollow-plane defect, here at DOMAIN scale — the folder's largest capability claim exists in no fence.
2. **The load-bearing wire owner cannot deliver its product.** `mesh/daemon` computes per-element GLB and strands it: `tessellate` returns `self` (`daemon.md:181`), `contribute` yields receipts carrying only `content_key.hex` and counts (`:78-84,219`), the GLB lives solely in `self._cache` (`:171,175`), and the method roster exposes no accessor. `ComputeService`/`ArtifactSync`/`ServerHost` exist as Boundary prose only (`daemon.md:3,20,222`; `cad.md:22`); the five wire/transport seam rows (`ARCH:42-44,46,55`) hang on zero fences. The C# side can never retrieve the folder's flagship output.
3. **Every graduation and identity prelude binds a soon-deleted or unbuilt module.** Twelve pages import `rasm.runtime.content_identity` (`algebra.md:49`, `features.md:44`, `nonmanifold.md:42`, `analysis.md:37`, `costing.md:35`, `structural.md:42`, `brep.md:58`, `cad.md:51`, `daemon.md:36`, `deviation.md:33`, `reconstruction.md:37`, `registration.md:31`) — the exact spelling runtime `[V4]` deletes; `daemon.md:228` anchors `evidence/identity#SEED_REPRODUCTION`, which runtime `[V7]` re-homes. Eleven pages import `rasm.compute.graduation.handoff` (`algebra.md:48`, `features.md:43`, `nonmanifold.md:41`, `analysis.md:35`, `costing.md:33`, `structural.md:40`, `brep.md:57`, `repair.md:35`, `deviation.md:37`, `reconstruction.md:41`, `registration.md:35`) — compute is the LAST campaign folder, so the producer imports its unbuilt consumer, codes against unbuilt page anchors (`structural.md:528`), and is unimportable until the folder AFTER it lands. The folder breaks the moment its upstream lands and cannot import until its downstream exists.
4. **The scan plane's concurrency is illusory and the product carries hard corruption.** All four scan owners declare `lane: LanePolicy | None = None` (`ingestion.md:142`, `registration.md:164`, `deviation.md:233`, `reconstruction.md:231`) and compose it in ZERO fences — the entries are sync `def` (`ingestion.md:144`, `registration.md:166`, `deviation.md:235`, `reconstruction.md:233`) running pdal SMRF, ICP, RANSAC, and Poisson on the event loop against their own Entry prose. Beside it, hard defects in the product: `quality.md:344` is dedented to column 0 inside an 8-space method body (`IndentationError`), `quality.md:365` is a stray fence delimiter, `nonmanifold.md:459-460` leaks literal `</content>`/`</invoke>` tool XML into the page, and three limiter comments truncate mid-sentence (`algebra.md:98-99`, `features.md:139-140`, `nonmanifold.md:98`).

What IS sound: the table-driven dispatch discipline corpus-wide (`MOMENT_KERNELS`/`PROFILE_SAMPLERS`/`FEATURE_OPS`/`ANALYTICS`/`READERS`/`_CONSTRUCT`/`_STAGE`/`IfcApiVerb` — the next case is one row everywhere), the runtime FIT (`boundary`/`@receipted`/`guarded`/`railed`/`Receipt.of(owner, evidence)` consumed by symbol, nothing re-minted), every C# seam decode-only with the seed-zero GLB key a reproduce-under-parity (`daemon.md:228`), the internal import graph ACYCLIC (four edges, all leaf-sink — the one structural win over runtime/data), and two reference pages (`ifc/selector`, `ifc/authoring`). The rot is a phantom domain, an unwired wire, inverted seams, and duplicated cross-cutting mechanisms — no per-page cold pass fixes it. Domain and seams first.

### [TELOS]

`geometry` is the host-free geometry/IFC/BIM evaluation companion of the Python branch — the independent peer producer that meets C# only at the wire (`libs/.planning/architecture.md` `[04]-[GEOMETRY_FLOW]`): the IfcOpenShell GLB tessellation daemon the C# Bim/Compute rail and the TS viewer consume, scan verification (ingest/register/deviate/reconstruct), IFC analysis/authoring/costing/structural evaluation, computational-geometry and graph analytics, and — ruled here — the building-physics energy/climate/comfort band. World-class means the COMPOSED companion: source bytes arrive over the wire, tessellate content-keyed with cache elision, the GLB streams BACK across `ArtifactSync`, scan clouds verify against it, IFC models analyze/author/cost/enrich against it, evidence graduates to compute under differentiated subjects, and energy models simulate through the runtime recipe rail — every step railed, receipted, and off the event loop. Assume 5x the consumer demand on every surface: C# `ArtifactSync` consumes the GLB stream, the TS viewer renders it, compute admits the graduation evidence, data decodes the scan Arrow records, construction-verification and energy evidence feed artifacts reports (the result frames crossing the data seam self-describing per `[V1]`), and the artifacts scene plane's typed mesh ingress decodes this folder's mesh interchange (`RASM-PY-ARTIFACTS-BRIEF.md` `[V9]`/`[V15]` — the massing suite's named upstream) — zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The campaign owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — NEW `energy/{climate,model,district,simulate}.md`, NEW `mesh/serve.md`, NEW `graduation.md`, NEW `graph/analytic.md`, the `GeometrySubject` re-homing — are the floor, not the ceiling. Every structural change updates `ARCHITECTURE.md` `[01]`+`[02]`+`[03]`, the `README.md` router and package groups, and BOTH `.api` tiers in the same motion. No page survives on incumbency, and no catalog survives on authorship: thirteen energy catalogs with zero consuming fences are pressure to realize, never evidence the owner exists.

### [INTERNAL_DAG_LAW]

The realized package is `rasm.geometry.<sub>.<leaf>`; the internal import graph stays acyclic under one topological order — a fence import binds only a strictly-earlier module, braced peers are mutually import-free, and an edge into a later tier is a defect resolved by re-homing or re-tiering, never dodged:

```text
graduation < {graph/analytic, ifc/selector, mesh/cad, mesh/repair}
    < mesh/quality
    < {scan/{ingestion,registration,deviation,reconstruction}, mesh/{spatial,brep},
       graph/{algebra,features,nonmanifold}, ifc/{analysis,authoring,costing,structural}}
    < mesh/daemon < mesh/serve
graduation < energy/climate < energy/model < energy/district < energy/simulate
```

The four standing intra-folder edges stay legal (`daemon.md:35` -> cad; `analysis.md:36`/`costing.md:34`/`structural.md:41` -> selector); the new edges the verdicts mint point strictly downward (producers -> graduation/analytic, quality -> repair, spatial -> repair, reconstruction/deviation -> quality per `[V4]`/`[V7]`, brep -> cad for the `[V3]` `TessellationPolicy` mint, serve -> daemon). The compute forward-import DIES (`[V2]`), so the folder imports only runtime, data, and itself. Every rebuilt page proves its prelude against this order; the terminal reconcile re-derives the graph from every fence and rejects any back-edge.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it. The live violation class is duplicate MECHANISM, not enumerated instances: three independent `CapacityLimiter(4)` mints whose comments name each other and truncate mid-sentence (`algebra.md:100`, `features.md:141`, `nonmanifold.md:99`); `AnalyticValue` authored verbatim twice (`features.md:166` ≡ `nonmanifold.md:112`) with `_leaders`/`_ranked`/census-peak folds triplicated; the `Mesh`/`Mesh64`-by-uint32-ceiling `Manifold` build spelled three times (`repair.md:199`, `spatial.md:176`, `quality.md:206`) against repair's own canonical-owner charter; the six-element evidence span weave hand-authored three times (`analysis.md:126`, `costing.md:168`, `structural.md:78`) beside seven inline `_TRACER` mints; `ReceiptContributor` consumed three incompatible ways. The test: the next analytics op, manifold consumer, graduating owner, or scan kernel is one row or one composition — never a re-spelling.

### [SEAM_AND_WIRE_LAW]

- ONE wire discipline: geometry meets C# only at the wire — GLB bytes plus content identity over the `ComputeService`/`ArtifactSync` contract. The daemon decodes and reproduces, never re-mints: the seed-zero (`Some(0)`) `XxHash128` GLB wire key equals the C# `RepresentationContentHash` entry byte-for-byte (reproduce-under-parity, proven against the runtime `GLB_BY_KEY` reproduction fixture), the policy-folded cache key stays distinct, and the `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` count-prefix law is consumed transitively through the runtime writer — geometry keys GLB blobs, never PropertySet bags, and `PY_WIRE_ALIGNMENT` is its own campaign with geometry downstream.
- ONE graduation direction: the handoff vocabulary is geometry-MINTED (`[V2]`); the crossing to compute is content-keyed receipt data, never an import; the frozen contract is pinned in the `graduation.md` fence for the compute campaign to inherit.
- ONE identity mint: every content key rides `rasm.runtime.identity` `ContentIdentity.of` over canonical bytes (12-site rename, `[V12]`); tessellation knobs ride a geometry-owned `TessellationPolicy` folded into the seed, never `IdentityPolicy` fields (`[V3]`).
- The `ARCHITECTURE.md` seam ledger is enforceable law: every wired edge appears as a ledger row and every row has a composing fence. The ledger is currently blind to all four intra-folder edges and seven of eleven graduation seams, mistypes two declared graduation rows `⇄` (`ARCH:51-52` — geometry only imports), and carries five wire rows with no fence (`ARCH:42-44,46,55`). `[V11]` closes all of it.
- Upstream consumption is pinned exports, not hope: `guarded`/`RetryClass.OCCT`/`RetryClass.RPC`/`RetryClass.OCC_NATIVE`, `offload(retry=)`, the THREAD band, the `GLB_BY_KEY` fixture, the tessellation `shapes` registry rows, and the `ServerHost`/`dispatch` sibling registration are all consumer-exported by this campaign's waterfall edits into runtime `[V5]`/`[V7]`/`[V2]`/`[V6]` — `daemon.md:200`'s assumed `retry=` keyword is now a ruled upstream surface, not a fabrication, and the wire VOCABULARY stays runtime-owned: geometry authors no wire shape (the runtime `shapes` roster today carries `ArtifactFrame` and no tessellation shape, so the rows are demanded, never assumed).

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as minted data and is only decoded here — the C#-minted tessellation request over the runtime `shapes` rows (source modality, SPF/STEP source bytes, the `TessellationPolicy` echo), the deviation reference GLB and its element identity fetched BY CONTENT KEY (`ARCH:47` `Rasm.Bim/Model` — scan never re-tessellates), the `BimWire` IFC ingest vocabulary (`ARCH:45`), scan sensor bytes (E57/LAS/COPC through the data codec seams, `ARCH:60`) and the Arrow point-record bridge (`ARCH:56`), data `MeshPayload` cell blocks (`ARCH:57`), and the energy plane's document bytes — HBJSON models, the IFC SPF source bytes the model plane's BIM-to-BEM modality derives rooms from, EPW/STAT/DDY weather, IDS/BCF documents, standards vocabularies resolved by identifier. What is COMPUTED here under a cross-language parity obligation, never a re-mint — the seed-zero (`Some(0)`) GLB wire key equal to the C# `RepresentationContentHash` byte-for-byte, the policy-folded re-tessellation cache key, and the GLB bytes themselves. Deviation verdicts, section integrals, IDS/clash/BCF evidence, energy result frames, graduation carriers, and all receipts are companion-local products, minted freely.

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. Carried obligations: the ten `[GEOMETRY]` energy rows (`pyproject.toml:130-139`) STAND because `[V1]` realizes their owner — expulsion strikes rows, catalogs, README group, and headline in one motion if the arm fires; the recipe trio's `[RUNTIME]` tags (`:140-142`) stand — runtime `[V3]` owns execution, geometry binds schemas only. `rhino3dm` (`:84`) and `laspy` (`:58`) are `[GEOMETRY]`-tagged with ZERO geometry imports — geometry's own codec-shed seams cede them to data (`ARCH:58`; data-brief VERIFIED `meshio`/`trimesh`/`rhino3dm` `_BACKEND` + laspy COPC) — retag both `[DATA]` and strike their geometry `.api` catalogs; `meshio` (`:59`, untagged) gains `[DATA]` the same way — the terminal tag is data+compute (`solvers/mesh` owns the compute interchange and adds its half in the terminal campaign, `RASM-PY-COMPUTE-BRIEF.md` `[00]`); geometry never enters it. TWO ADDITIONS carry the domain's proof burden into the manifest: `rtree` (named concern: `mesh/spatial`'s Bounds arm is fence-bound to the R-tree backend the page itself admits "the manifest does not declare" — `spatial.md:3,126-130`; categorical-best: the libspatialindex binding trimesh already treats as its optional accel) and `python-fcl` (named concern: the CORE-clearance arm binds `fcl` `CollisionManager` separation, `spatial.md:39-43`; categorical-best: the FCL binding, the one maintained narrow-phase distance engine) — both `[GEOMETRY]` worker-lane rows with folder-tier `.api` stubs, both verified live, maintained, and license-clean: `rtree` MIT over libspatialindex with abi-independent `py3-none` platform wheels (no version marker), `python-fcl` BSD-3-Clause with a cp314 wheel ceiling — admitted `python_version<'3.15'`, the same evidence class as `:78,127,128`, the `[V9]` clearance arm keeping the manifold3d fall-through on the gapped band; `embreex` stays an unadmitted transparent trimesh accel (no fence binds its members). The `python_version<'3.15'` pins on `pye57`/`cadquery-ocp`/`kiss-matcher` (`:78,127,128`) stand on real cp315 wheel gaps. The categorical-best supersession sweep returns ZERO strikes beyond the ruled retags — the apparent overlaps are complementary by boundary: `pye57` owns the typed `ScanHeader` pose/metadata surface `pdal`'s flat `Reader.e57` row drops, `small-gicp` owns the VGICP fine-refinement speed path beside open3d's tensor multiscale/colored arms, and `kiss-matcher`-primary/FGR-fallback is realized selection law, never duplication. `gmsh` stays deferred with no catalog until a consuming fence exists (`GMSH_BUILD_EXCLUSION_MANDATE`). `topologicpy` stays the AGPL opt-in lane; `GEO_TOPOLOGICPY_LICENSE_REPLACE` stays the pooled forward path. `.api` tier motions ride `[V11]`: strike `geometry/.api/{queenbee,lbt-recipes,pollination-handlers}.md` (runtime-tier canonical; divergent by 257/160/127 lines) and `geometry/.api/{rhino3dm,meshio,laspy}.md`; `networkx` stays branch-tier dual-consumer per the applied data-brief edit; `pyarrow`'s data-tier owner line names geometry's `arrow_las` seam decode per the same applied-edit discipline. Paid/gated/proprietary stays rejected.

## [01]-[PHASE_REBUILD]

The rebuild engine runs against this brief directly. Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; the build may exceed a floor, never dilute it; a hedge names its deciding criteria and the implementing agent resolves it with disk evidence, never by preference.

### [V1_ENERGY_PLANE]

Ruled REALIZE, not expel — the campaign's defining coverage ruling. The packages are admitted and tagged, thirteen catalogs author the capability to fence depth, the README charters the band, and planned consumers are design pressure, not license to punt. NEW `energy/` sub-domain, four-page floor: `energy/climate.md` — the ladybug climate/comfort owner (EPW/Wea/Location intake, analysis periods, `Sunpath` solar geometry — the energy plane's solar owner only: artifacts' diagram sun-path furniture stays its own closed-form kernel per `RASM-PY-ARTIFACTS-BRIEF.md` `[V15]`, so `Sunpath` gains no diagram consumer — `DataCollection` algebra, and the comfort tier — PMV/UTCI/adaptive/SolarCal point models and maps over `ladybug-comfort` — as an owner block); `energy/model.md` — the HBJSON building-model owner (honeybee-core object graph + the `check_all` validation gate as the model admission rail — ONE polymorphic admission discriminating by input shape: HBJSON bytes ARRIVE and decode (`Model.from_dict`/`from_hbjson`), and the BIM-to-BEM derivation is COMPUTED here, the modality only this folder can own — `ifcopenshell` bound directly (`by_type("IfcSpace")` + `geom.create_shape` space solids; a package consume, never an ifc-plane page import) lifts through `Polyface3D.from_faces` into `Room.from_polyface3d`, `Room.solve_adjacency(rooms, tolerance)` closes the zone graph, and the `Face3D` aperture generators (`sub_faces_by_ratio*`) mint fenestration where the source model carries none, as ratio policy rows — both modalities fronted by the same `check_all` gate, never a second entry; honeybee-energy constructions/loads/schedules/HVAC properties, the two standards backends resolved by identifier — honeybee-standards defaults and honeybee-energy-standards ASHRAE 90.1/DOE vintage sets as seed rows, never hand-copied data); `energy/district.md` — the dragonfly district owner (2.5-D massing object graph, the `to_honeybee` explosion seam onto `energy/model`, dragonfly-energy URBANopt/DES/OpenDSS translation rows); `energy/simulate.md` — the simulation egress owner (honeybee-openstudio in-process OSM/IDF translation OFFLOADED on the worker lane, the recipe BINDING — geometry constructs queenbee `Job`/`RecipeInterface` and coerces inputs through pollination-handlers, then hands execution to runtime `execution/recipe` per the README's own "runtime owns execution" concession — and SQLite result decode back through ladybug-core into typed result frames crossing the data seam — the crossing fence-pinned SELF-DESCRIBING per the data assessment-wire discipline: output name, unit, analysis period, room/zone identifier, and content key ride the frame columns, the physical crossing content-keyed Arrow bytes composed at the consumer edge, never comment-flow). Every page is a producer: receipts through the `[V5]` weave, graduation through `[V2]`'s `building-energy`/`thermal-comfort` subjects, AGPL isolation at the process boundary per the standing companion-lane charter. The plane's seam rows enter the ledger with the pages: `energy/simulate -> python:runtime/recipe`, `energy -> python:data/tabular` (result frames), `energy -> graduation`. Expulsion arm deciding criteria: only if the build proves the band belongs to a folder that does not exist — and expulsion strikes the ten manifest rows, thirteen catalogs, README group, headline prose, and the runtime `[V3]` consumer clause in the same motion.

### [V2_GRADUATION_INVERSION]

NEW `graduation.md` at the `.planning/` root (module `rasm.geometry.graduation`) — the folder's tier-0 evidence spine; root-siting is deliberate (the one cross-sub-domain leaf; a one-page sub-folder is structure spam). Three rulings. (a) The subject vocabulary re-homes: `GeometrySubject` is minted HERE — the producer owns the vocabulary of what it produces, matching the build order — and all eleven `from rasm.compute.graduation.handoff` imports die; recited compute-page anchors (`structural.md:528`'s union recital, `deviation.md:318`) rewire to the local owner. (b) The subject union DIFFERENTIATES: `"numerical-primitive"` today carries four semantically distinct evidence classes (`algebra.md:323` compas numerics, `analysis.md:121` BIM compliance/clash/BCF, `costing.md:157` 5D/4D lifecycle, `structural.md:74` section integrals); recommended floor — `bim-compliance`, `bim-lifecycle`, and `section-property` join the union, algebra keeps `numerical-primitive`, and `[V1]` adds `building-energy`/`thermal-comfort`; each graduating owner binds its own member. (c) The crossing becomes data: `graduation.md` mints the typed `GeometryHandoff` carrier (subject, evidence key, measured facts, ceilings) every producer's `graduates()` returns, and the fence PINS the frozen boundary contract the compute campaign inherits with geometry the named demanding consumer — the eight standing literals plus the new members, the `graduates(owner, HandoffAxis(geometry=subject), key, measured, ceiling)` admission shape, and the residual-over-ceiling `_admit` direction; compute's `HandoffAxis` hub and `GraduationReceipt` admission fold DECODE this carrier at the seam and re-shape nothing without a geometry ripple. The pin is contract DATA — the frozen union, admission shape, and `_admit` direction stated once in the fence's contract block — never a geometry-typed callable over compute symbols: `HandoffAxis`/`GraduationReceipt` appear in no geometry prelude. Deleted forms: any `from rasm.compute` import, any recited compute interior anchor, any second subject vocabulary.

### [V3_DAEMON_WIRE_EGRESS]

The flagship output becomes reachable and served. Ruled: `tessellate` returns the results — `RuntimeRail[Block[TessellationResult]]` replaces the fluent `self` (`daemon.md:181`), receipts stay on `contribute` — and NEW `mesh/serve.md` binds the daemon behind the C# wire: the geometry servicer registered in the runtime `ServerHost` lifecycle, streaming per-element GLB plus the semantic header over `ComputeService`/`ArtifactSync` with the 64 KiB `FrameEdge` framing, per-frame Crc32, and whole-artifact XxHash128 the daemon's own research already proves (`daemon.md:222`); the five wire/transport ledger rows (`ARCH:42-44,46,55`) re-file onto serve as composed fences or die. Registration direction is boundary law: serve owns the geometry-side composition root — it registers the tessellation servicer as `ServerHost.dispatch` descriptor-id + codec-pair + railed-handler rows and composes the runtime Entrypoint install -> admit -> serve -> drain fold from the geometry side (both runtime `[V6]` waterfall-pinned exports); runtime never imports geometry. The request/response wire shapes import from `rasm.runtime.shapes` — the waterfall-pinned `TessellationRequest`/response registry rows beside `artifact_frame` (`Rasm.Bim/Exchange/tessellation` + `Rasm.Compute/Runtime/codecs` are the C# mints) — never a geometry-authored shape. The decoded FIELD FLOOR is pinned consumer law riding those rows (the runtime `[V2]` waterfall clause carries it): the request delivers source modality, source bytes, and the `TessellationPolicy` echo; the response/receipt delivers the content key, the element/triangle tally, the `SemanticHeader` schema/project, the whole-artifact XxHash128, and the replay provenance (the daemon's `admitted`-vs-`emitted` phase) so the C# artifact index distinguishes by-reference replay from fresh iteration — serve decodes every floor field by symbol; a response the consumer cannot dedupe or attribute is a chain break, never a doc gap. The daemon/serve boundary: daemon tessellates, caches, and keys, never opening a channel or framing bytes; serve registers, frames, and streams, never tessellating, re-keying, or reaching past the daemon's returned results. In the same motion the policy coupling dies: the mesher knobs (`deflection`/`angle_tolerance`/`spec`) leave runtime `IdentityPolicy` for a geometry-owned `TessellationPolicy` — MINTED on `mesh/cad` beside `BridgeFormat` (`StepBridge.tessellate` consumes it in-signature at tier 1; a daemon-sited mint forces a cad -> daemon back-edge), daemon and brep importing it downward — folded into the canonical seed bytes — `daemon.md:15,92-95`, `cad.md:214,217`, and `brep.md:417` rewire, `ARCH:40` rewords, and runtime `[V7]`'s waterfall clause holds `IdentityPolicy` generic. The two-key discipline is preserved law: the policy-folded re-tessellation cache key and the seed-zero GLB wire key stay distinct, the wire key proven against the runtime reproduction corpus's `GLB_BY_KEY` fixture (anchor migrates from `evidence/identity` to `rasm.runtime.reproduction` per runtime `[V7]`). `daemon.md:200`'s `offload(..., retry=RetryClass.OCCT)` stands on the now-pinned runtime surface.

### [V4_SCAN_LANE_REALITY]

The scan family adopts the mesh worker shape: `async def` entries composing `await lane.offload(_dispatch, ...)` at all four owners (`ingestion.md:144`, `registration.md:166`, `deviation.md:235`, `reconstruction.md:233` — the kernels are already module-level and picklable, and the pdal stage graph additionally crosses by its own JSON pickle state, `.api/pdal.md:79,115` `__getstate__`/`__setstate__` round-tripping the pipeline JSON, so ingestion's `_execute` survives even the process-modality fallback), the dead `lane: LanePolicy | None = None` fields become the composed lane, and the "lane accepted yet never composed" form is deleted corpus-wide. The seven inline `_TRACER` span mints (`ingestion.md:74`, `registration.md:68`, `deviation.md:78`, `reconstruction.md:72`, plus the ifc trio `[V5]` collapses) route through the `[V5]` weave — no inline `start_as_current_span` beside the owned aspects. Hardcode folds onto policy: `ingestion.md:94`'s `"Z[0:30]"` crop default becomes a policy row; `deviation.md:70-75` and `registration.md:64-65` module-Final graduation/verticality ceilings fold onto their `Policy` carriers. `registration.md:3`'s stale `mesh-utility` sub-domain name corrects. The verification chain's ingress and precondition harden: `evaluate`'s reference GLB arrives BY CONTENT KEY over the `ARCH:47` `Rasm.Bim/Model` row — the daemon's welded artifact fetched keyed, never re-tessellated in scan — and the watertight precondition stops raising bare `ValueError` (`deviation.md:284`, exception-in-domain against the folder's own rails): it composes `mesh/quality`'s exact-closure fold (quality tiers below the scan producers per `[00]`) and rails a typed `BoundaryFault`. `GEOMETRY_CPU_OFFLOAD` closes over the scan+mesh set and EXTENDS to name the graph owners (`[V6]`).

### [V5_ONE_EVIDENCE_WEAVE]

`graduation.md` additionally owns the shared evidence-run weave — geometry's `faults`-analogue tier. One `evidence_run(scope, subject, dispatch)` fold composes runtime `boundary` + the span + `@receipted`; the triplicated six-element weave (`_TRACER`/`run`/`_ok`/`_emit`/`_REDACTION` at `analysis.md:126`, `costing.md:168`, `structural.md:78`) collapses onto it and the scan inline spans (`[V4]`) ride the same owner. The per-page tracer-scope literals become one seed table on the spine (the runtime `[V8]` scope-vocabulary law at geometry scale). The `ReceiptContributor` law resolves its three-way split — ruled default STRUCTURAL-ONLY: the Protocol is runtime-checkable, the scan trio already conforms without importing it, and a subclass adds nothing — the three capsule subclass sites (`daemon.md:167`, `quality.md:229`, `spatial.md:236`) drop the base, and the fourteen import sites keep only consumed names (`cad.md:53`-class annotation-only imports die).

### [V6_GRAPH_ANALYTICS_SUBSTRATE]

NEW `graph/analytic.md` — the tier-0 graph-analytics leaf: `AnalyticValue` (`@tagged_union` scalar/leaderboard/groups with `as_scalar`/`peak`, today verbatim at `features.md:166` AND `nonmanifold.md:112`), the `_leaders`/`_ranked` ranking folds, and the census-peak projection — one owner all three graph producers compose. The triple limiter dies: `algebra.md:100`, `features.md:141`, `nonmanifold.md:99` collapse onto `lane.offload` THREAD modality under the runtime-owned band (the waterfall-pinned runtime `[V5]` export) — zero geometry-minted `CapacityLimiter`s, and the three truncated comments die with them. `networkx` ownership settles: `graph/features` is the branch's analysis consumer of record (the applied data-brief edit keeps the catalog branch-tier with both consumers named); `features.md`'s `[NETWORKX_ANALYTICS]` citation corrects its tier spelling to `libs/python/.api/networkx.md`. `nonmanifold` hardens: the leaked `</content>`/`</invoke>` XML (`:459-460`) strips, the empty `[04]` dies, and the module-top `topologicpy`/`ifcopenshell` imports go function-local per the page's own AGPL gated-import discipline.

### [V7_MANIFOLD_KERNEL_AND_QUALITY]

One manifold conversion, one closure fold, one name. `repair`'s `_to_manifold` promotes to the repair-owned PUBLIC kernel (repair is the chartered canonical `manifold3d` owner) and `spatial.md:176`/`quality.md:206` compose it — one uint32-ceiling `Mesh`/`Mesh64` law; `repair` sits tier-1 in the `[00]` order so both consumers point downward. The `MeshQuality` collision resolves by composition: `reconstruction.md:103`'s leaf value object and its `fold` (re-computing the watertight/euler/volume/area closure `quality.md:74-93` owns, with a second component-count strategy) DIE — `scan/reconstruction` composes `mesh/quality`'s exact-closure fold (quality tiers below the scan producers), `scan/deviation`'s `[V4]` watertight gate is the second consumer of the same fold, and the component-count strategy is the quality owner's backend row (manifold3d `decompose` when the package resolves, trimesh `body_count` the CORE fall-through), never a per-consumer choice. The two fence defects fix with the page: `quality.md:344` re-indents, `quality.md:365`'s stray fence deletes.

### [V8_IFC_TRUTH_AND_DEPTH]

The ifc plane's ridden-unresolved provider shapes settle — the data-E14 settlement discipline: `analysis.md:416-417`'s clash-group key spelling (`clash_set["clash_groups"]`/`cluster` index) and `TopicHandler.topic.title`, and `costing.md:430-431`'s schedule `.file`/`.xml`/`.work_plan` slot semantics, each confirm against the live distribution via `assay api` or the fence narrows to the confirmed surface — a typed field on an unconfirmed provider shape is a phantom. Depth: `ifctester`'s `Json(ids).report()` Results graph (`percent_checks_pass`/`total_applicable_fail`) lands as the sharper IDS verdict source `analysis.md:418` already names. `structural.md:500`'s mermaid diagram — the only one corpus-wide — drops; the flow is table-stated and the corpus grammar carries no diagram tier. Subject differentiation rides `[V2]`; the weave rides `[V5]`.

### [V9_SPATIAL_BACKEND_ADMISSION]

Admit-or-narrow the native band — fence-bound-unadmitted is the inverse of the dead-admission defect and equally fatal. Ruled: `rtree` and `python-fcl` enter `pyproject.toml` (`[GEOMETRY]` worker-lane rows; `python-fcl` on the `<3.15` band per `[00]`) with folder-tier `.api` stubs per `[00]`; the Bounds and CORE-clearance arms (`spatial.md:39-43,126-130`) stand on admitted packages; `embreex` stays a probe-only transparent accel with no admission and no catalog; the probe band enters `ARCH:[03]-[COMPANION_LANES]` so the `find_spec` set is governed, not implicit. The plane's phantom substrate spelling dies in the same motion: `spatial.md:7,187` recite a `trimesh`/`numpy`/`scipy` spine while no geometry fence imports a scipy member and the only scipy catalog is compute-tier (`pyproject.toml:115` `[COMPUTE]`; `compute/.api/scipy.md`) — the prose corrects to the realized trimesh/numpy members, a genuine scipy consume entering only through a tier-lawful catalog motion naming both consumers. Narrow arm deciding criteria: `rtree`'s wheels are proven gap-free, so the arm concerns only `python-fcl` — if the `<3.15`-banded path additionally proves unviable inside the band, spatial narrows to the manifold3d clearance path, the CORE-clearance arm gains a `[BLOCKED]` card naming the gate, and the probe band records the deferral.

### [V10_PAGE_GRAMMAR]

The `[03]-[RESEARCH]` appendix is BANNED folder-wide (the runtime/data `[V11]` precedent) — all 18 tails die — and geometry's INVENTED second appendix layer dies with it: eleven `[04]-[UPSTREAM]` tails, one `[04]-[CROSS_FOLDER]` label drift (`deviation.md:316`), five of them empty or dead (`brep.md:423,426` both empty, `spatial.md:288,291` both empty, `nonmanifold.md:454`, `costing.md:439`, `quality.md:363`) — cross-folder facts belong in the seam ledger, never per-page prose. The ruling is purge-and-fold: the genuinely load-bearing member confirmations (`selector.md:316+` grammar-parity proof, `analysis.md:414+`, `structural.md:518+`, `algebra.md:409+` compas arity corrections, `nonmanifold.md:449+` topologic handle resolutions, `daemon.md:217+` KEYED_ADMISSION/REPRESENTATION_KEY law) fold into the owning `Packages` block or the folder `.api` catalog; "confirmed against the live distribution" framing, version narration, and every empty header delete. The corruption fixes land here where a verdict has not already claimed them; the ~40-line run-on Boundary walls break into scannable deleted-form clauses, each invariant stated once. This is the folder's largest clean prose reduction (~700-900 lines) and lands per-leg, never as a separate pass.

### [V11_API_TIER_AND_GOVERNANCE_TRUTH]

Both `.api` tiers and all four governance surfaces become exact. Tier motions: strike `geometry/.api/{queenbee,lbt-recipes,pollination-handlers}.md` (runtime-tier canonical per runtime `[V3]`/`[V13]`; the geometry copies diverge by 257/160/127 lines) and `geometry/.api/{rhino3dm,meshio,laspy}.md` (data-owned codecs, zero geometry imports); the ten energy catalogs gain their consuming fences via `[V1]` and their charter lines rewrite to name them; `ifc4d.md:62`'s stacking recital of the compute-owned `"numerical-primitive"` subject rewires to the `[V2]` geometry-minted union (`bim-lifecycle`) with the charter rewrites; the two `[V9]` stubs author with the admissions — resolving the ~40% of the folder tier (~1966 of 4960 LOC: the thirteen consumer-less energy catalogs at 1555 plus the three dead codec catalogs at 411) that is today dead, duplicate, or misplaced. Governance: the README router grows from 13 rows to the full realized set (18 standing pages — `features`/`authoring`/`brep`/`quality`/`spatial` are unrouted today — plus the seven new); the router's false claims die (`README.md:17` calls repair the "mesh-file codec owner" against repair's own codec-shed seam, `ARCH:58`); the `ARCHITECTURE.md` codemap gains the `energy/` nodes, `mesh/serve`, `graduation`, and `graph/analytic`; `ARCH:67`'s companion-lane paragraph drops `rhino3dm`/`laspy` and records the `[V9]` probe band; the seam ledger declares all intra-folder edges and all eleven graduation edges as `->` rows (the `⇄` on `ARCH:51-52` dies — no compute->geometry import exists), kills the wildcard `ARCH:38` csharp-graduation row (the graduation crossing is `python:compute`, `:51-54`'s concern and the `[V2]` carrier; C# consumption rides the compute hub downstream, never a second geometry-owned graduation wire), re-files the wire rows onto serve, and gains the energy rows. TASKLOG truth: `STRUCTURAL_SECTION_PROPS` and `KISS_MATCHER_FALLBACK_FGR` verify-close (realized on disk — the warping tier and the `OPEN3D_FGR` arm are authored); `GEOMETRY_CPU_OFFLOAD` extends to the graph owners and closes with `[V4]`/`[V6]`; `MESH_CODEC_BOUNDARY`/`SCAN_COPC_PARTIAL`/`MESH_TOPOLOGY_SHAPE` close with the legs that verify their seams; `IFC_BYTES_CONSTRUCTOR` and `GMSH_BUILD_EXCLUSION_MANDATE` stay `[BLOCKED]`.

### [V12_UPSTREAM_REBIND]

The mechanical corpus-wide sweeps, landing in leg 1's reconcile so later legs inherit clean spellings: (a) `rasm.runtime.content_identity` -> `rasm.runtime.identity` at all 12 sites — the runtime `[V4]` ruling this folder makes load-bearing; (b) the reproduction anchor migration — `daemon.md:228`'s `evidence/identity#SEED_REPRODUCTION` `GLB_BY_KEY` row retargets `rasm.runtime.reproduction` per runtime `[V7]`; (c) the drain-vocabulary citation truth — daemon's lanes-attributed `DrainReceipt` prose (`daemon.md:13-14`) re-cites `observability/receipts` per runtime `[V1]` (imports are return-typed, prose only); (d) the compute-import kill and graduation rewire (`[V2]`) at all 11 sites. Acceptance is mechanical: grep zero for `content_identity`, `rasm.compute`, and `evidence/identity#SEED_REPRODUCTION` under `libs/python/geometry/.planning/`.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every build leg re-verifies its rows against disk. Anchors are `libs/python/geometry/.planning/` relative; `ARCH:` anchors `libs/python/geometry/ARCHITECTURE.md`; `README:` the folder README; `pyproject.toml:` the repo root manifest.

| # | Class | Evidence |
|---|---|---|
| E1 | Energy phantom | `README:3` headline; README `[ENERGY]` 13-row group; `pyproject.toml:130-139` + `:140-142`; 13 `.api` catalogs (~1223 LOC); `honeybee-energy.md:126-135` authors the nonexistent consumer's rail; zero fence hits corpus-wide; no ARCH node, router route, or card |
| E2 | Daemon strand | `tessellate` returns `self` `daemon.md:181`; GLB only in `self._cache` `:171,175`; `contribute`/`fact` carry `content_key.hex`+counts `:78-84,219`; no accessor in the method roster; `ComputeService`/`ArtifactSync` prose-only `daemon.md:3,20,222`, `cad.md:22`; wire rows `ARCH:42-44,46,55` fence-less |
| E3 | Compute forward-dep | 11 imports: `algebra.md:48`, `features.md:43`, `nonmanifold.md:41`, `analysis.md:35`, `costing.md:33`, `structural.md:40`, `brep.md:57`, `repair.md:35`, `deviation.md:37`, `reconstruction.md:41`, `registration.md:35`; unbuilt-page anchors `structural.md:528`, `deviation.md:318`; compute is the LAST folder in sequence |
| E4 | Identity drift | 12 `rasm.runtime.content_identity` imports (`algebra.md:49`, `features.md:44`, `nonmanifold.md:42`, `analysis.md:37`, `costing.md:35`, `structural.md:42`, `brep.md:58`, `cad.md:51`, `daemon.md:36`, `deviation.md:33`, `reconstruction.md:37`, `registration.md:31`); reproduction anchor `daemon.md:228`; stale drain citation `daemon.md:13-14` |
| E5 | Scan dead lane | sync entries `ingestion.md:144`, `registration.md:166`, `deviation.md:235`, `reconstruction.md:233`; dead `lane` fields `:142/:164/:233/:231`; zero `.offload(` fence calls under `scan/`; inline `_TRACER` mints `ingestion.md:74`, `registration.md:68`, `deviation.md:78`, `reconstruction.md:72` |
| E6 | Product corruption | `quality.md:344` column-0 dedent inside `_metrics` (`IndentationError`); `quality.md:365` stray fence; `nonmanifold.md:459-460` literal `</content>`/`</invoke>`; truncated comments `algebra.md:98-99`, `features.md:139-140`, `nonmanifold.md:98` |
| E7 | Duplicate mechanism | `CapacityLimiter(4)` x3 `algebra.md:100`/`features.md:141`/`nonmanifold.md:99`; `AnalyticValue` verbatim x2 `features.md:166`/`nonmanifold.md:112`; manifold build x3 `repair.md:199`/`spatial.md:176`/`quality.md:206`; span weave x3 `analysis.md:126`/`costing.md:168`/`structural.md:78`; closure fold x2 `reconstruction.md:103-123` vs `quality.md:74-93` |
| E8 | Subject overload | `"numerical-primitive"` carries four evidence classes: `algebra.md:323`, `analysis.md:121`, `costing.md:157`, `structural.md:74` |
| E9 | ReceiptContributor split | 14 import sites; subclass-as-base `daemon.md:167`, `quality.md:229`, `spatial.md:236`; structural-only (no import) `deviation`/`reconstruction`/`registration`; annotation-only imports `cad.md:53`, `repair.md:38`, `brep.md:61` |
| E10 | Ledger blindness | zero rows for the 4 intra-folder edges (`daemon.md:35`, `analysis.md:36`, `costing.md:34`, `structural.md:41`); 7 of 11 graduation seams undeclared (nonmanifold/costing/brep/repair/deviation/reconstruction/registration); `ARCH:51-52` mistyped `⇄`; `ARCH:67` claims `rhino3dm`/`laspy` on the runtime lane against zero imports |
| E11 | Policy coupling | `IdentityPolicy` carries mesher knobs: `daemon.md:15,92-95`, `cad.md:214,217`, `brep.md:417`; `ARCH:40` bakes "deflection/tolerance policy-seed" into the seam row; runtime is host-free stratum zero |
| E12 | `.api` tier rot | recipe-trio catalogs duplicated across the runtime and geometry folder tiers (divergent 257/160/127 diff lines); `rhino3dm.md`/`meshio.md`/`laspy.md` dead (zero imports, 411 LOC); 13 energy catalogs consumer-less (1555 LOC); ~1966 of 4960 LOC (~40%) dead/dup/misplaced; `features.md` `[NETWORKX_ANALYTICS]` cites a wrong-tier path |
| E13 | Governance stale | README router 13 of 18 pages (stops at `[13]-[ALGEBRA]`; `features`/`authoring`/`brep`/`quality`/`spatial` unrouted); `README:17` false repair codec-owner claim vs `ARCH:58`; done-but-BLOCKED `STRUCTURAL_SECTION_PROPS` (24 warping hits) and `KISS_MATCHER_FALLBACK_FGR` (12 FGR hits); `GEOMETRY_CPU_OFFLOAD` omits the graph owners; `registration.md:3` stale `mesh-utility` name |
| E14 | Hardcode + unadmitted band | `ingestion.md:94` `"Z[0:30]"`; `deviation.md:70-75`/`registration.md:64-65` module-Final ceilings; per-page tracer literals (7 sites); `rtree`/`python-fcl`/`embreex` fence-bound (`spatial.md:39-43,126-130`) with no manifest row, no catalog, no ARCH band |

Sound surfaces (preserve; cold-pass plus only the deltas a verdict names): `ifc/selector.md` (9 — grammar-parity demolition; the reference leaf), `ifc/authoring.md` (9 — the verb-table/AOP exemplar; correctly graduation-free), `ifc/structural.md` core (9 — Green's-theorem kernels, tier gating, `ClosedRing` refinement), `scan/deviation.md` domain content (9 — signed-band fold, RANSAC peel), `ifc/costing.md` core (9 — `_token[E: StrEnum]` rail, population-divided drift ledger), `mesh/cad.md` (8.5 — XCAF hop, `READERS`/`_APPLY`), `mesh/spatial.md` routing core (8.5 — capability-aware `_route`, lexsort reduction), the corpus-wide table discipline, the acyclic internal graph, the C# FIT (seed-zero reproduce-under-parity, decode-only seams), and the three data seams (Arrow point bridge, `MeshPayload` cell-block, codec shed).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| graduation spine | — | 9.5 | NEW: geometry-minted subject union (differentiated); `GeometryHandoff` frozen contract; evidence weave + scope table; ReceiptContributor law |
| mesh/daemon | 6 | 9.5 | results exposed; `TessellationPolicy` owned; anchors rebound; two-key law proven against `GLB_BY_KEY` |
| mesh/serve | — | 9.5 | NEW: servicer in the runtime `ServerHost` lifecycle; `FrameEdge` framing; wire rows composed; consumer field floor decoded by symbol |
| mesh worker (repair/spatial/quality/brep) | 8 | 9.5 | public manifold kernel; closure fold composed; fence fixes; `rtree`/`python-fcl` admitted; structural-only receipts |
| mesh/cad | 8.5 | 9.5 | policy rewire; grammar purge; cold otherwise |
| scan | 6.5 | 9.5 | async offload composed; weave-routed spans; policy-folded ceilings; keyed reference ingress; railed watertight gate; grammar |
| graph | 7.5 | 9.5 | analytic substrate owner; THREAD-band offload; corruption stripped; gated AGPL imports; networkx cite corrected |
| ifc | 8.5 | 9.5 | weave composed; differentiated subjects; provider shapes settled; `Json(ids).report()` depth; mermaid dropped |
| energy | — | 9.5 | NEW four-page plane over 13 catalogs; polymorphic model admission (HBJSON decode + BIM-to-BEM derivation); recipe binding onto the runtime rail; validation-gated models; self-describing result frames |
| governance (ARCH/README/TASKLOG/`.api`) | drifted | exact | full router; truthful codemap+lanes; complete `->` ledger; card truth; tier strikes and stubs |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate, the folder tier for domain packages):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Option`/`Block`/`Map` as the one ADT and dispatch spine (already corpus-wide; regressions are defects), `anyio` structured concurrency ONLY through the runtime lane owner (zero geometry-minted limiters per `[V6]`), retry ONLY through `guarded` over the exported `OCCT`/`RPC`/`OCC_NATIVE` rows (never bare `stamina` — `analysis.md:133`'s `_CLASH_RETRY` re-routes through the waterfall-pinned `OCC_NATIVE` in-process transient row; `structural.md:92`'s `_FE_RETRY` dies, the deterministic sectionproperties solve owns no transiency and a retry over it is a deleted form), `msgspec.Struct` admission at ingress, `beartype.vale` refinements on geometric preconditions (`ClosedRing`/`SignedField` are the exemplars), one span per fold through the `[V5]` weave — the span handles mint from the `[V5]` scope table over the branch-tier `opentelemetry-api` catalog (`libs/python/.api/opentelemetry-api.md`; today seven page-local `from opentelemetry import trace` preludes, collapsed onto the graduation owner).
- `ladybug-core` — EPW/Wea/Location intake, `AnalysisPeriod`, `Sunpath` solar geometry, `DataCollection` algebra, SQLite result reads: the `energy/climate` + `energy/simulate` substrate (`.api/ladybug-core.md`).
- `ladybug-comfort` — PMV/UTCI/adaptive/SolarCal models and comfort maps as `energy/climate` owner-block rows; `ladybug-geometry` — the planar/solid value objects the model construction composes, never a second geometry kernel; `Polyface3D.from_faces`/`from_offset_face` and the `Face3D` aperture/fin generators (`sub_faces_by_ratio*`, `contour_fins_*`) are the `[V1]` BIM-to-BEM derivation substrate (`.api/ladybug-comfort.md`, `.api/ladybug-geometry.md`).
- `honeybee-core`/`honeybee-energy` — the HBJSON object graph with `check_all` as the model ADMISSION gate; constructions/loads/schedules/HVAC as typed property rows; the catalog's own rail law (`honeybee-energy.md:126-135`) becomes the `energy/model`/`energy/simulate` fence shape it was authored for; `Room.from_polyface3d`/`solve_adjacency`/`stories_by_floor_height` back the `[V1]` derivation modality.
- `honeybee-standards`/`honeybee-energy-standards` — identifier-resolved seed data (defaults, ASHRAE 90.1/DOE vintage sets) through `honeybee-energy.lib`, never hand-copied constants.
- `honeybee-openstudio` — the in-process OSM/IDF translation, offloaded on the worker lane; `dragonfly-core`/`dragonfly-energy` — 2.5-D massing, `to_honeybee` explosion, URBANopt/DES/OpenDSS rows (`energy/district`).
- `queenbee`/`lbt-recipes`/`pollination-handlers` — BINDING ONLY: `Job`/`RecipeInterface` construction and handler input coercion in `energy/simulate`; execution is runtime `execution/recipe`'s (`[V1]`; the geometry-tier catalogs strike per `[V11]`).
- `ifcopenshell` — the spine holds at depth (six consuming pages); the daemon's in-memory non-SPF band residual (sqlite/zipped variants vs `file.from_string`) settles or narrows with the daemon leg; the `[V1]` BIM-to-BEM modality adds the seventh consumer — `IfcSpace` solids through `by_type` + `geom.create_shape`.
- `ifctester` — `Json(ids).report()` `Results` graph (`.api/ifctester.md:62,79`) into the analysis verdict fold (`[V8]`); `ifcclash`/`ifc5d`/`ifc4d`/`ifcpatch`/`ifcdiff`/`bcf-client` — held at the landed bar (the `Clasher`/`ClashSet`/`smart_group_clashes` weave stacked with bcf-client issue authoring, `analysis.md:339-383,416-417`; the qto/diff/patch/schedule phases, `costing.md:12-16`); one mine-or-decline rides the analysis leg — `Clasher.get_viewpoint_snapshot` (`.api/ifcclash.md:55`) into the BCF topic viewpoint, the `export_json`/`export_bcfxml` same-string round-trips staying deleted forms (`analysis.md:354`).
- `cadquery-ocp` — genuinely unmined surfaces record as mine-or-decline rows with the brep leg: `Geom_*` NURBS curves/surfaces, `BRepBuilderAPI_Sewing`/`NurbsConvert`, `BOPAlgo` `SetFuzzyValue`/`History`/`SectionEdges`, `BRepAlgoAPI_Splitter` (`.api/cadquery-ocp.md:61-71,84-86,227-228`).
- `trimesh` — the most-consumed owner holds; the unmined-by-design declines record once (`load`/`export` -> data, `Scene` -> artifacts, `creation` superseded by `BrepOp`, `registration`/`voxel`/`section` unused).
- `manifold3d` — `batch_boolean`/`Mesh64`/`CrossSection`/`min_gap` held; the SDF/Minkowski/hull surfaces record as declines or rows with the repair leg.
- `open3d` — VERIFIED AT DEPTH across the scan plane: the tensor band (`multi_scale_icp`/Tukey `RobustKernel`/colored estimator, `.api/open3d.md:105-118`), the multiway `PoseGraph`/`global_optimization` fold re-scored through `evaluate_registration` (`:89-103`; `registration.md:12-14`), RANSAC `segment_plane` peel, DBSCAN cluster-split reconstruction, and the Poisson/ball-pivot/alpha `_CONSTRUCT` rows (`:63-86`; `reconstruction.md:16-18`) all consume by symbol; recorded DECLINES — `io.*` (`:134+`, codecs are data-owned), the legacy pairwise `registration_icp`/`registration_generalized_icp` dispatch (`:152`, superseded in-corpus by the tensor arms plus VGICP), and `compute_point_cloud_distance` (deviation rides trimesh `ProximityQuery`); `[V4]` moves execution off the loop, never re-shapes the kernels.
- `small-gicp`/`kiss-matcher` — held at the landed bar: `align(registration_type='VGICP')` with the `T_target_source`/`num_inliers`/`error`/`iterations` readback and the full `KISSMatcherConfig` gain constructor with the staged `match`/`prune_and_solve` decomposition (`registration.md:14,16`; `.api/small-gicp.md:44+`, `.api/kiss-matcher.md:58+`); the FGR fallback is realized law.
- `pdal` — the injected driver factories and `execute()` are consumed (`ingestion.md:16,233-238`); mine-or-decline with the `[V4]` leg: the streaming arms (`execute_streaming`/`iterator`, `.api/pdal.md:75-76,113`) against block-scale clouds, and the `metadata`/`schema`/`srswkt2` output rows (`:81+`) into the ingest receipt; the JSON-pickle offload law (`:79,115`) is the `[V4]` process-modality proof.
- `pye57` — the conditioned `read_scan(transform=True)` intake is realized (`ingestion.md:198-206`); mine-or-decline with the scan leg: `ScanHeader` pose/acquisition metadata and `scan_position` station origin (`.api/pye57.md:41-58,71+`) as scan-provenance receipt facts; the write path (`:60-68`) DECLINES — scan egress is the data seam's; the COPC boundary stays the data codec seam (`ingestion.md:17`).
- `compas`/`compas-dr`/`compas-tna` — held (342-entry `__all__` verified phantom-free); the RPC bring-up rides the exported `RetryClass.RPC` row.
- `topologicpy` — the AGPL opt-in lane holds; imports go function-local (`[V6]`); the permissive replacement stays the pooled `GEO_TOPOLOGICPY_LICENSE_REPLACE` idea.
- `sectionproperties` — the warping/plastic/shear tier is realized; the card verify-closes (`[V11]`).
- `networkx` — the branch catalog serves both consumers per the applied data-brief edit; features' eleven-family `ANALYTICS` table (`features.md:404-469`) is the geometry consume of record; `lark` — held (selector is the reference leaf).
- `rtree` + `python-fcl` — ADDITIONS per `[00]`/`[V9]`: named concerns fence-bound today; stubs authored with the admission; verified live, maintained, license-clean per `[00]`; `embreex` stays probe-only.
- `pyarrow` — ingress decode ONLY: the `arrow_las` arm binds `pyarrow.Table` at boundary scope over the `ARCH:56` point-record bridge (`ingestion.md:12,61`); the catalog stays data-tier (`data/.api/pyarrow.md`) with geometry named on its owner line per the applied data-brief edit; geometry never keys or egresses through it.
- `numpy` — the one direct substrate consume (structural kernels, quality folds, spatial reductions; twelve module-top sites) — held at the branch-tier bar (`libs/python/.api/numpy.md`).
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): the daemon's keyed-drain cache short-circuit and warm-pool discipline, selector's grammar round-trip, authoring's `to_kwargs` railed slot-resolve, structural's centroid-relative `_extreme_fibers` and FE void carving, quality's half-edge incidence fold, spatial's `_nearest_hits` lexsort, deviation's `DeviationBand.fold`, features' `mode_guard` data-gating.
- TASKLOG absorption: `GEOMETRY_CPU_OFFLOAD` -> `[V4]`+`[V6]`; `STRUCTURAL_SECTION_PROPS`+`KISS_MATCHER_FALLBACK_FGR` verify-close (realized); `MESH_CODEC_BOUNDARY`+`MESH_TOPOLOGY_SHAPE` -> leg 2 seam verification; `SCAN_COPC_PARTIAL` -> leg 2 (boundary confirmed, card closes); `IFC_BYTES_CONSTRUCTOR`+`GMSH_BUILD_EXCLUSION_MANDATE` stay `[BLOCKED]` and untouched.

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with this brief; the partition is a topological order over the `[INTERNAL_DAG_LAW]` graph — every leg's pages import only pages of the same or earlier legs, and each leg's listed sequence is the canonical landing order.

1. **SPINE** — NEW `graduation.md` FIRST (`[V2]` vocabulary + contract pin + `[V5]` weave), NEW `graph/analytic.md`, `mesh/repair.md` (public manifold kernel; tier-1), `mesh/quality.md` (fence fixes; closure fold public), `mesh/cad.md` (`TessellationPolicy` MINT + rewire per `[V3]`), `ifc/selector.md` (cold; grammar purge). This leg's reconcile carries the `[V12]` corpus-wide sweeps — the identity rename (12 sites), the compute-import kill and graduation rewire (11 sites), the reproduction/drain anchor migrations — applied to every page, not only this leg's.
2. **PRODUCERS** — `scan/{ingestion,registration,deviation,reconstruction}.md` (`[V4]`), `mesh/spatial.md` (`[V9]` — the `rtree`/`python-fcl` manifest rows and stubs land with this leg), `mesh/brep.md` (`[V7]` compose; cadquery-ocp rows), `graph/{algebra,features,nonmanifold}.md` (`[V6]`), `ifc/{analysis,costing,structural,authoring}.md` (`[V5]` weave, `[V8]` settlements).
3. **WIRE** — `mesh/daemon.md` (`[V3]` results + policy + anchors), NEW `mesh/serve.md` LAST (the servicer composing the daemon; the `ARCH` wire rows re-file here).
4. **ENERGY + GOVERNANCE** — NEW `energy/climate.md` -> `energy/model.md` -> `energy/district.md` -> `energy/simulate.md` (`[V1]`); the governance closeout lands in this leg's reconcile — `ARCHITECTURE.md` codemap+ledger+lanes, `README.md` router+groups+headline truth, TASKLOG card motions, the full `[V11]` `.api` strikes and energy charter rewrites, and the remaining `pyproject.toml` motions (the `rhino3dm`/`laspy` retags, the `meshio` tag).

Per-leg closeout obligations: hard residuals resolve before the next leg launches; `README.md`/`ARCHITECTURE.md`/`TASKLOG.md` close in the same leg that changes their facts; the `[V10]` grammar purge applies to every page a leg touches, never as a separate pass; pyproject and `.api` stub motions land with the leg that admits, removes, or retags the package; residuals naming a later leg's pages transfer forward explicitly in the leg return, never silently.

Campaign acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.geometry.*` import with zero back-edges, and grep returns zero `from rasm.compute` under the folder; (b) grep proofs return zero: `rasm.runtime.content_identity`, `evidence/identity#SEED_REPRODUCTION`, `[03]-[RESEARCH]`, `[04]-[UPSTREAM]`, `[04]-[CROSS_FOLDER]`, `CapacityLimiter(`, `import stamina`, `</content>`, `</invoke>`; (c) the wire proof — `mesh/serve`'s fence streams `TessellationResult.glb` through a named symbol pair whose types agree, its servicer registration composes `ServerHost.dispatch` rows with request/response shapes imported from `rasm.runtime.shapes` (zero geometry-minted wire vocabulary), the seed-zero wire key is proven against the runtime `GLB_BY_KEY` fixture, and the response floor fields (content key, tally, semantic header, artifact hash, replay phase) decode by symbol; (d) the scan proof — all four entries are `async def` composing `lane.offload`, with zero inline span opens corpus-wide, the deviation reference a keyed fetch, and the watertight precondition railed typed; (e) the energy proof — every `[ENERGY]` manifest row has a consuming fence, the recipe binding composes runtime `execution/recipe` by symbol, the `check_all` gate fronts every simulation, the model admission discriminates HBJSON bytes and IFC-derived rooms through one entry, and the result-frame columns are fence-pinned; (f) ledger, codemap, router, manifest, and cards agree on one 25-page set and one seam ownership, every graduation edge a `->` row; (g) the graduation proof — `graduation.md`'s fence pins the frozen union and admission shape, every producer's `graduates()` returns the local carrier, and no page recites a compute interior anchor.

## [06]-[OUT_OF_SCOPE]

- The C# seam owners: `Rasm.Bim` (IFC semantic authority), `Rasm.Element/Graph` (`RepresentationContentHash` mint), `Rasm.Compute/Runtime` (`ComputeService`/`ArtifactSync` contract) — geometry decodes, reproduces under parity, and serves; it never re-mints. The `PY_WIRE_ALIGNMENT` canonical-writer campaign is upstream-owned; the count-prefix law is consumed transitively only.
- The runtime interior: the identity rename execution inside runtime, the lanes `retry=`/THREAD-band implementation, the recipe owner authoring, the `GLB_BY_KEY` fixture landing — runtime's own campaign; the waterfall clauses are already applied and geometry consumes the post-rebuild surface.
- The data interior: `MeshPayload` deepening, the laspy COPC rebind, the `networkx` owner-line motion — data's own campaign; geometry consumes the seams unchanged.
- The compute campaign: the multi-domain `HandoffAxis` hub and `GraduationReceipt` admission fold stay compute's to build — it inherits `graduation.md`'s frozen contract with geometry the named demanding consumer; `RASM-PY-COMPUTE-BRIEF.md` `[V1]` now rules that decode — the frozen 13-literal union inherited as wire data, zero `rasm.geometry` imports, an out-of-union subject railing typed — so a geometry union change ships a compute ripple, never a silent admit.
- The two standing `[BLOCKED]` cards (`IFC_BYTES_CONSTRUCTOR` on the topologicpy AGPL dual-gate, `GMSH_BUILD_EXCLUSION_MANDATE`) — package-admission-gated, not this campaign.
- IDEAS expansion pool (`GEO_IFC_AUTHORING_PIPELINE`, `GEO_POINTCLOUD_INSTANCE_LABELLING`, `GEO_PARAMETRIC_LAYOUT`, `GEO_CLASH_QTO_RECEIPTS`, `GEO_TOPOLOGICPY_LICENSE_REPLACE`) — genuinely deferred; the graduation spine and serve owner this campaign lands are their prerequisites, not their execution.
