# REVIEW F17 — 17-indefinite-loops.md

Scope checked: fabrication, code-to-lab identity, boundary placement, drift against `chpater-9.md` (plus the recursion example lifted from `chpater-2.md`), coherence with 22 and 23.

Verified clean (not restated below): all 8 `csharp` blocks match `.scratch/lab-functional-csharp/F17/{Recursion,Loops,States,Consumption}.cs` verbatim modulo the `namespace` line (mechanical diff); every named member exists with the stated signature — `Trampoline.Pure<A>(A)`, `Trampoline.More<A>(Func<Trampoline<A>>)`, `Trampoline<A>.Run()`, `Trampoline<A>.Bind<B>`, `Monad.recur<M,A,B>(A, Func<A, K<M, Next<A,B>>>)`, `Next.Loop<A,B>`, `Next.Done<A,B>`, `Prelude.tail<A>(IO<A>)`, `IO<A>.RepeatUntil(Schedule, Func<A,bool>)`, `IO<A>.RepeatWhile(Schedule, Func<A,bool>)`, `LanguageExt.List.unfold<S,A>(S, Func<S, Option<(A,S)>>)`, `Seq<A>.Last`/`Head` as `Option<A>` properties, `Seq<A>.Fold`, `Seq<A>.Map`, `Seq<A>.Take`; no run of an effect inside a code block, `RunSafe`/`Run` named only as host operations; no cross-file reference, no banned token, no bare `Eff<X>`; 205 lines. The `toSeq` memoization claim and the empty-`Seq` case for an already-terminal seed are both witnessed by `Samples.StateProbe` (`OnePass == 3`, `Rerun == 4`, `Empty`). No contradiction with 22 or 23 on `tail`, `Monad.recur`, `Trampoline`, or `RepeatUntil`.

1. **Location** line 26, approach table, row 1 strength cell.
   **Claim** "Pure recursion at any depth, final or intermediate values through `Bind`"
   **Evidence** `Trampoline<A>.Bind<B>(Func<A, Trampoline<B>> f)` applies `f` to the value the trampoline resolves to; `Trampoline_1.txt` shows `BindStep.Fix` folding the continuation onto the resumed value only. The lab `Bound` probe binds after a depth-100000 run and observes the final value alone. Line 87 of the same file already states the true form: "`Bind` chains a second `Trampoline` onto the final state." The table row contradicts it. Intermediate values come from row 3 (`LanguageExt.List.unfold`), which is what makes the three rows distinct.
   **Correction** replace the cell text with:
   `Pure recursion at any depth, and `Bind` continues from the final value`

2. **Location** line 27, approach table, row 2 approach cell.
   **Claim** "`Monad.recur`, `RepeatUntil`"
   **Evidence** The row states one strength and one cost, but names two mechanisms that answer different concerns: `Monad.recur` advances a state through `Next.Loop`/`Next.Done`, while `IO<A>.RepeatUntil(Schedule, pred)` reruns one unchanged effect and carries no state between runs. The row therefore reads as alternatives inside one row, and the file's own closing sentence ("stable across all three choices") counts three mechanisms, not four. `RepeatUntil` already owns its own paragraph and code block at lines 132-139, the same treatment `tail` receives.
   **Correction** replace the cell text with:
   `` `Monad.recur` ``

3. **Location** line 163.
   **Claim** "The update-before-check `Step` performs one or more transitions, and an initial state that is already terminal yields an empty `Seq`."
   **Evidence** The sentence is inherited from the book's `MoveNext`, which advanced first and tested the result. The replacement `Step` inverts that: `state.HasExited ? None : Emit(advance(state))` tests the incoming state before it advances, so a terminal seed performs zero transitions. The lab `Empty` probe confirms `States.Trace(new Session(0, HasExited: true), ...)` is empty with no call to `advance`. "update-before-check" and "one or more transitions" are both false for this code, and the second half of the same sentence contradicts them.
   **Correction** replace the sentence with:
   `Step` emits only a state that a transition produced, so an initial state that is already terminal yields an empty `Seq`.

4. **Location** line 130.
   **Claim** "`RunSafe()`, `Map`, and any later `Bind` push a map into the tail and fail."
   **Evidence** `LAB-NOTES.md` records the exit rule as "`RunSafe()`, `Try()`, `Map`, and any later `Bind` push a map into the tail and fail with 'You can't map a tail call'", and `22-languageext-effects.md` line 151 lists `Try` in the same set. The doc drops `Try`, and the very next sentence tells the host to reach for `Try.lift(io.Run).Run()`, so a reader has no warning that `io.Try()` on the tail-recursive value itself fails.
   **Correction** replace the sentence with:
   A `tail`-recursive `IO` exits only through `Run()` or `RunAsync()`. `RunSafe()`, `Try()`, `Map`, and any later `Bind` push a map into the tail and fail.
   (keep the following host sentence unchanged)

5. **Location** line 20.
   **Claim** "The execution mechanism is a library type."
   **Evidence** Two of the three mechanisms the sentence introduces are functions, not types: `Monad.recur<M,A,B>` is a static method on the `Monad` trait class and `LanguageExt.List.unfold<S,A>` is a static method on `LanguageExt.List`. Only `Trampoline<A>` is a type.
   **Correction** replace the sentence with:
   The execution mechanism comes from the library.

6. **Location** line 186.
   **Claim** "`Map` defines a transformation of every yielded state."
   **Evidence** The book bullet it replaces read "`Select` defines a lazy transformation of every yielded state", and `LAB-NOTES.md` (F06) records "`Seq.Map` is lazy and `Do` is strict". The section is titled "LINQ consumption semantics" and its point is that construction does not run the loop; dropping "lazy" removes the book claim that carried it, with nothing in the assembly contradicting the claim.
   **Correction** replace the bullet with:
   - `Map` defines a lazy transformation of every yielded state.

7. **Location** lines 51-56, the added list under "### Recursion in C#".
   **Claim** "1. an end condition that returns the final value; 2. a recursive path that calls the same function with values closer to that condition; 3. a returned value on every path."
   **Evidence** The list is added text, not book prose (`chpater-2.md` states the same facts as one sentence), so the prose rules apply: no semicolons.
   **Correction** replace the three items with:
   1. an end condition that returns the final value
   2. a recursive path that calls the same function with values closer to that condition
   3. a returned value on every path

## What a project built on this file would still need

- The polling cadence in `Polling.Drain` is the literal `TimeSpan.FromMilliseconds(1)`. A host passes the `Schedule` as configuration, because a domain function reads no ambient source.
- `RepeatWhile` is named at line 132 but never shown, and the schedule overload `IO<A>.RepeatWhile(Schedule, Func<A,bool>)` is not exercised in the F17 lab. A project that uses it takes the F22 evidence for the predicate-only form.
- Nothing in the file bounds the iteration count when the stopping predicate depends on external input. An unreachable predicate loops forever under all three mechanisms, which line 199 states as a constraint and no code enforces.

Blockers: none
