# [PY_ARTIFACTS_TASKLOG]

The folder's open and closed work, distilled from `IDEAS.md`. Open tasks are cards in `[1]-[OPEN]` with a `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` leader; closed tasks move to `[2]-[CLOSED]` with `[COMPLETE]`/`[DROPPED]`. Each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[QUEUED] Stand up the document security-and-navigation egress sub-domain on `documents/egress/finish.md` — from `DOCUMENT_SECURITY_AND_NAVIGATION`.
- Author `documents/egress/finish.md`: one `DocumentEgress` (`Struct`, frozen) owner discriminating an `EgressStep` closed `StrEnum` — `ENCRYPT`/`OUTLINE`/`WATERMARK`/`ATTACH`/`IMPOSE` — over one `boundary` entry, each arm a row binding its package call: `ENCRYPT` over pikepdf `Encryption(owner, user, R=6, aes=True, allow=Permissions(...))`/`Pdf.save` and pypdf `PdfWriter.encrypt`; `OUTLINE` over weasyprint `Document.make_bookmark_tree`, pypdf outline-add, pikepdf `Outline`; `WATERMARK` over pikepdf `Page.add_overlay`/`add_underlay`/`as_form_xobject`; `ATTACH` over pikepdf `AttachedFileSpec`/`open_metadata`; `IMPOSE` over pypdf `Transformation`/`merge_page` n-up page composition. The permission set is a `Permissions` row policy, never scattered booleans.
- Integrate `pikepdf` (`Encryption`/`Permissions`/`Pdf.save(encryption=...)`/`Page.add_overlay`/`add_underlay`/`as_form_xobject`/`Outline`/`AttachedFileSpec`/`open_metadata`), `pypdf` (`PdfWriter.encrypt`/outline/`Transformation`/`merge_page`), `weasyprint` (`Document.make_bookmark_tree` on the gated `python_version<'3.15'` band); compose the runtime `content_identity.ContentIdentity`/`faults.RuntimeRail`/`boundary`. No new admission.
- The sub-domain lands beside `documents/document-plan#DOCUMENT`, reading an already-emitted PDF and returning a finished PDF keyed by the content key; it authors no PDF (that stays at the document axis) and re-signs nothing (PAdES routes to `typography/conformance#CONFORM`). Contributes the existing `ArtifactReceipt.Pdf` case carrying the post-finish byte/page count plus the encryption-R/outline-depth/overlay-count facts.
- pikepdf is gated `python_version<'3.15'` (qpdf native, cp313 wheel), so the `ENCRYPT`/`WATERMARK`/`ATTACH`/`IMPOSE` pikepdf arms ride the `anyio.to_process.run_sync` subprocess seam; the pure-Python pypdf arms resolve on the cp315 core; the weasyprint bookmark arm rides the gated band. All pikepdf/pypdf/weasyprint spellings are `.api`-verified.

[QUEUED] Add the text-shaping arm on `typography/conformance.md` — from `SHAPED_TEXT_LAYOUT`.
- Extend `Conformance` with one `SHAPE` `ConformStep` row and the `_shape` fold: the uharfbuzz five-step pipeline (`Blob.from_file_path` -> `Face.create` -> `Font.create` -> `Buffer.create`/`add_str`/`guess_segment_properties` -> `shape(font, buffer, features)` -> read `Buffer.glyph_infos` `GlyphInfo` and `Buffer.glyph_positions` `GlyphPosition`) returning a positioned-glyph run (codepoint/cluster + x/y advance/offset) the document and figure owners place; the COLRv1 `PaintFuncs` color-glyph path and `Font.set_variations` axis pinning are sub-rows, and `Font.draw_glyph_with_pen` bridges the shaped run to a fontTools `pens.svgPathPen.SVGPathPen` for vector glyph outlines.
- Integrate `uharfbuzz` (`Blob`/`Face`/`Font`/`Buffer`/`shape`/`GlyphInfo`/`GlyphPosition`/`PaintFuncs`/`Font.set_variations`/`Font.draw_glyph_with_pen`), `fonttools` (`pens.svgPathPen.SVGPathPen` for the outline bridge, existing admission); compose the runtime content key and rail. No new admission.
- The arm lands on the existing conformance owner beside `SUBSET`/`INSTANCE`/`SIGN`, taking a font and a text run and returning the positioned-glyph run; the shaped run feeds `documents/document-plan#DOCUMENT` text placement and the new `imaging/figure` owner, never a parallel shaping owner. Contributes the `ArtifactReceipt.Pdf`/figure facts via the shaped-run glyph count.
- uharfbuzz is cp315-core (Cython over libharfbuzz, cp315 wheel), so the shaping pipeline resolves in-process; all `shape`/`Buffer`/`GlyphInfo`/`GlyphPosition`/`set_variations`/`draw_glyph_with_pen` spellings are `.api`-verified in the `uharfbuzz` catalogue; the fontTools `SVGPathPen` bridge is `.api`-verified.

[QUEUED] Stand up the figure-composition sub-domain on `imaging/figure/compose.md` — from `FIGURE_COMPOSITION_OWNER`.
- Author `imaging/figure/compose.md`: one `Figure` (`Struct`, frozen) owner discriminating a `FigureOp` closed `StrEnum` — `SCALE_FIT`/`TILE`/`CROP`/`ANNOTATE`/`METADATA` — over one `async_boundary` entry; the vector ops (`SCALE_FIT`/`TILE`/`CROP` bounds-and-transform) fold over the new `svgelements` admission (`SVG.parse`/`Path`/`Matrix.scale`/`translate`/`rotate`/`bbox`) on the cp315 core; the raster ops (`ANNOTATE` draw/text/filter, `METADATA` EXIF read-write) fold over pillow (`ImageDraw`/`ImageFont.FreeTypeFont`/`ImageOps`/`ImageFilter`/`Image.Exif`) on the gated `python_version<'3.15'` subprocess seam.
- Integrate `svgelements` (NEW admission, pure-Python cp315-clean: `SVG.parse`/`Path.d`/`Path.bbox`/`Matrix.scale`/`translate`/`rotate`/`Length.value`/`Color`), `pillow` (`ImageDraw`/`ImageFont`/`ImageOps`/`ImageFilter`/`Image.Exif`, gated, existing admission); manifest row delta for `svgelements` returned to the orchestrator; compose the runtime content key and rail.
- The sub-domain lands under `imaging/` beside `preview.md`, composing the SVG the chart/QR/nanoplot owners emit and the raster the preview owner produces into a placed/annotated/color-correct figure feeding `reporting/report-plan#REPORT` and `documents/document-plan#DOCUMENT`; it rasterizes nothing (that routes to `vl-convert`/`pillow`) and re-renders no chart. Contributes the `ArtifactReceipt.Preview` case carrying the figure dimensions plus the element-count/transform facts.
- `svgelements` is pure-Python cp315-clean (`py3-none-any` wheel, imports on the core) so the vector ops resolve in-process; the pillow draw/annotate/metadata arms ride the gated band; the `imaging/figure/compose.md` design page and the `imaging/` codemap node land in ARCHITECTURE/README. The `.api/svgelements.md` catalogue is authored this pass; all svgelements/pillow spellings are `.api`-verified.

[QUEUED] Add the Typst introspection-and-data-bind arm on `documents/document-plan.md` — from `TYPST_INTROSPECTION_AND_DATA_BIND`.
- Extend the `PDF_TYPST` path: bind the reusable `typst.Compiler` font-cached world for batched renders and add two new `DocumentMode` rows — `TYPST_QUERY` (typst `query(selector, field, one)` structured element extraction, the document-introspection inverse) and `TYPST_DATA` (typst `compile(..., sys_inputs=...)` runtime data injection through `sys.inputs`, no string templating) — each a `BACKENDS` policy row; the `Compiler` is held by the owner across a batch, never a fresh world per document.
- Integrate `typst` (`Compiler`/`Compiler.compile`/`Compiler.query`, `query(input, selector, field, one)`, `eval(input, expression)`, `compile(..., sys_inputs=..., pdf_standards=...)`, `Fonts`/`FontInfo`, existing admission); compose the runtime content key and rail. No new admission.
- The arms land on the existing `DocumentPlan` owner as two `DocumentMode` rows plus the held `Compiler` world; `TYPST_QUERY` returns the structured element data (feeding a TOC/index for `reporting/report-plan#REPORT`), `TYPST_DATA` returns the data-bound PDF; both key by the content key and contribute `ArtifactReceipt.Pdf`/`.Document`. The produce-then-introspect inversion is native over the Typst world, parallel to the PDF document-node seam.
- typst is cp315-core (cp38-abi3 wheel), so the `Compiler`/`query`/`eval`/`sys_inputs` path resolves in-process; all `Compiler`/`query`/`eval`/`sys_inputs`/`Fonts` spellings are `.api`-verified in the `typst` catalogue.

[QUEUED] Add the `tab_options` global-theme and merge-range arms on `tables/table-plan.md` — from `PUBLICATION_TABLE_DEPTH` (deepened).
- Extend `TablePlan` with the `tab_options(**kwargs)` global-theme row (table/heading/column-labels/body/stub/footer theme policy as one data-driven option map) and the `cols_merge_range(col_begin, col_end, sep, autohide)` / `data_color(..., autocolor_text=True)` arms, folded into the same `reduce`-over-spec pipeline as the spanner/format/fill folds, never a per-call style scatter; the theme is one named option-map row carried as the table's publication identity.
- Integrate `great-tables` (`tab_options`/`cols_merge_range`/`data_color(autocolor_text=...)`, existing admission); no new admission.
- The deepening lands on the existing `TablePlan` owner; the theme and merge-range folds compose into the one data-driven build beside the `[5]-[PUBLICATION_TABLE_DEPTH]` header/summary/style/cols folds. Contributes the existing `ArtifactReceipt.Table` case.
- All `tab_options`/`cols_merge_range`/`data_color(autocolor_text)` spellings are `.api`-verified in the `great-tables` catalogue.

[QUEUED] Author the `svgelements` `.api` catalogue and register the admission — coverage move from `FIGURE_COMPOSITION_OWNER`.
- Author `artifacts/.api/svgelements.md` (bare filename) capturing the real `svgelements` surface — `SVG.parse`/`elements`, `Path`/`d`/`bbox`, `Matrix.scale`/`translate`/`rotate`, `Length`/`Color`/`Angle`/`Point`, and the shape primitives (`Rect`/`Circle`/`Ellipse`/`Polygon`/`Polyline`/`SimpleLine`) — and register `svgelements` in the README package list; this catalogue is authored this pass (pure-Python, resolves on the cp315 core).
- Integrate `importlib.metadata` and the runtime `evidence/evidence` `ApiPackage.reflect` surface against the synced environment; the capture runs on the cp315 core (pure-Python `py3-none-any` distribution).
- The catalogue lands in the folder `.api/`; the `FIGURE_COMPOSITION_OWNER` vector-side fence members are settled fence code once the catalogue lands, never authored from memory.
- Manifest row delta for `svgelements` returned to the orchestrator; `svgelements` is pure-Python cp315-clean, no band marker.

[QUEUED] Stand up the semantic document-node algebra and bidirectional seam on `documents/model.md` + `documents/inspection/lens.md` — from `BIDIRECTIONAL_DOCUMENT_SEAM`.
- Author `documents/model.md`: one `DocumentNode` msgspec tagged-union tree (`page`/`section`/`block`/`run`/`table`/`figure`/`field`/`annotation`/`structure_element`) as the single interior representation, plus the `DocumentDelta` diff/merge algebra (typed inserted/deleted/moved/reparametrized node edits keyed by `ContentIdentity`) defined once over the tree. Re-base `documents/document-plan#DOCUMENT` so each backend is a lowering arm folding FROM `DocumentNode` rather than dispatching an opaque payload; emission and extraction become inverses over the one algebra.
- Author the recover-TO half on `documents/inspection/lens.md`: one `DocumentLens` (`Struct`, frozen) discriminating a `LensOp` closed `StrEnum` — `EXTRACT_TEXT`/`EXTRACT_IMAGES`/`WORDS`/`SEARCH`/`EMBEDDED`/`OUTLINE`/`REDACT`/`ANNOTATE`/`STORY` — over one `async_boundary` entry, each arm recovering tree nodes from an emitted PDF; the pymupdf and lxml-bearing arms ride the gated `python_version<'3.15'` subprocess seam (`anyio.to_process.run_sync`), the pure-Python pypdf reader arms resolve on the cp315 core. The extracted-tree corpus keys into the runtime columnar lane (consumed from `data`, never re-minted) so a multi-PDF corpus is one queryable value.
- Integrate `pymupdf` (`Page.get_text` dict/words/json, `get_images`, `search_for`, `apply_redactions`, `Annot`, `Story` reflowable HTML-to-PDF), `pypdf` (`PdfReader.extract_text`/`extract_images`/outline/`xmp_metadata`, `PageObject.Transformation`), `msgspec` (`tagged_union` tree); compose the runtime `content_identity.ContentIdentity`/`faults.RuntimeRail`/`async_boundary`. Contributes a new `receipt/artifact-receipt#RECEIPT` introspection case carrying the content key and the recovered-tree-shape facts (node count / text length / image count / hit count); the lens recovers nodes and never authors a PDF — authoring stays at the lowering arm.
- pymupdf `search_for`/`get_text` and `Story` spellings are `.api`-verified; the runtime columnar corpus lane is the existing `data` query owner, not a second store; OCR over a scanned page (no embedded text) is the one RESEARCH frontier (`ocrmypdf`/Tesseract carries a native binary), carded as a deferred admission, never a phantom fence member.

[QUEUED] Add the conformance-verification verb on `typography/conformance.md` — from `ARCHIVAL_CONFORMANCE_VERIFICATION`.
- Extend `Conformance` with one `ConformAudit` arm folding a signed/archival PDF into a typed `ConformanceVerdict`: PAdES signature validity, `SignatureCoverageLevel`, modification-difference level, DSS/LTV validation-material completeness, and XMP/PDF-version archival-metadata presence; the existing `ConformStep` axis gains the `AUDIT` row and the verdict is a new `receipt/artifact-receipt#RECEIPT` case, never a parallel verify owner.
- Integrate `pyhanko` (`PdfFileReader`, `EmbeddedPdfSignature`/`evaluate_signature_coverage`, `validate_pdf_signature`/`async_validate_pdf_signature`, `add_validation_info`/`collect_validation_info`, `PdfSignatureStatus`; the `ValidationContext` arrives from `pyhanko_certvalidator` at the boundary), `pikepdf` (`Pdf.open_metadata` XMP read, object-model structural probe); compose the runtime content key and rail.
- The verb lands on the existing conformance owner beside `SUBSET`/`INSTANCE`/`SIGN`, reading an already-signed PDF and returning a verdict, never re-signing; the async validation variant rides the runtime `async_boundary` since `async_validate_pdf_signature` is a coroutine.
- All pyhanko validation spellings (`validate_pdf_signature`, `evaluate_signature_coverage`, `PdfSignatureStatus`, `add_validation_info`) and pikepdf `open_metadata` are `.api`-verified; no PDF/A structural validator resolves pure-Python on PyPI, so the structural-archival leg reads version/XMP presence over pikepdf rather than claiming a full veraPDF-grade verdict.

[QUEUED] Build the ICC color-managed output arm on `color-management/colorimetry.md` — from `ICC_COLOR_MANAGED_OUTPUT`.
- Add the `MANAGED` `ColorOp` row and the `_managed` fold: an ICC source/destination-profile transform with a rendering-intent row over pillow `ImageCms`, and a colour-science LUT/CCTF tone-curve chain (`read_LUT`/`LUTSequence`/`cctf_encoding`/`cctf_decoding`) plus a `delta_E` gamut-distance check; the managed transform applies on the raster egress so a preview/chart-raster/print-bound PDF carries a managed profile.
- Integrate `pillow` `ImageCms` (`ImageCmsProfile`, `buildTransform`, `applyTransform`, intent constants) on the gated `python_version<'3.15'` band (pillow is gated, so the ICC raster apply rides the subprocess seam), `colour-science` (`read_LUT`/`write_LUT`/`LUT1D`/`LUT3D`/`LUTSequence`/`cctf_encoding`/`cctf_decoding`/`delta_E`) on the cp315 core; compose the runtime rail.
- The arm lands on the existing color owner as one `ColorOp` row plus the `_managed` acceptor; the ICC raster apply is the one gated leg crossing the subprocess seam (pillow band), the LUT/CCTF color math resolves on the core; the managed raster feeds `imaging/preview#PREVIEW` and the document raster egress, never a parallel color owner.
- `colour-science` LUT/CCTF/`delta_E` and pillow `ImageCms.ImageCmsProfile`/`buildTransform`/`applyTransform` spellings are `.api`-verified; rendering-intent constant spellings (`INTENT_PERCEPTUAL`/`INTENT_RELATIVE_COLORIMETRIC`/...) confirm against the pillow `ImageCms` catalogue or stay a marked RESEARCH item.

[QUEUED] Widen the imaging code arm to the full encoded-mark family on `imaging/preview.md` — from `ENCODED_MARK_OWNER`.
- Replace the QR-only `PreviewOp.QR` arm with an `EncodedMark` symbology axis: a `Symbology` closed `StrEnum` (`QR`/`MICRO_QR`/`CODE128`/`EAN13`/`EAN8`/`UPCA`/`ITF`/`CODE39`/`DATAMATRIX`/`PDF417`) binding the segno arm for QR/Micro-QR and the `python-barcode` arm for linear/2D, each serializing to the dependency-free SVG/PNG vector path; the in-process cp315-core arm stays in-process, no Pillow leak.
- Integrate `segno` (existing, QR/Micro-QR `make`/`save`) and `python-barcode` (new admission, pure-Python cp315-clean linear/2D barcodes via `get_barcode_class`/`SVGWriter`); manifest row delta returned to the orchestrator.
- The arm lands on the existing `Preview` owner as one symbology-row axis beside the raster ops, contributing the existing `ArtifactReceipt.Preview` case; the encoded-mark concern is one axis, never a parallel code class.
- BLOCKED-on-catalogue: `python-barcode` is absent from the active environment, so its `.api/python-barcode.md` catalogue and every `python-barcode` fence member stay a marked RESEARCH item until the package resolves on the next uv-sync; the segno QR/Micro-QR arm is `.api`-verified and lands as settled fence code immediately, the `python-barcode` rows finalize on capture.

[QUEUED] Recover the full publication-table surface on `tables/table-plan.md` — from `PUBLICATION_TABLE_DEPTH`.
- Extend `TablePlan` with the header/footnote/source-note layer (`tab_header`/`tab_footnote`/`tab_source_note`), the summary-row algebra (`summary_rows`/`grand_summary_rows`), `loc`-targeted `tab_style` cell-style injection (text/fill/borders over `loc.body`/`loc.column_labels`/`loc.summary`), the column-control family (`cols_label`/`cols_align`/`cols_merge`/`cols_width`/`cols_hide`), and the `fmt_image` inline-image cell — each folded as a data-driven spec row (a `STYLERS`/`STRUCTURE` row catalogue) beside the existing `FORMATTERS`/spanner/fill folds, never a per-feature method chain.
- Integrate `great-tables` (`tab_header`/`tab_footnote`/`tab_source_note`/`tab_stub`/`summary_rows`/`grand_summary_rows`/`tab_style`/`fmt_image`/`cols_label`/`cols_align`/`cols_merge`/`cols_width`/`cols_hide`, `loc.*`, `style.text`/`style.fill`/`style.borders`); no new admission.
- The deepening lands on the existing `TablePlan` owner; the new structural/style folds compose into the same `reduce`-over-spec pipeline that the spanner/format/fill folds already use, keeping one data-driven build, never an imperative per-cell loop.
- All `tab_*`/`summary_*`/`cols_*`/`loc.*`/`style.*`/`fmt_image` spellings are `.api`-verified in the `great-tables` catalogue.

[QUEUED] Contribute the artifacts measured signals into the one runtime metric stream — trickle-down from branch `ONE_MEASURED_SIGNAL_STREAM`.
- Route every artifacts production duration/byte-volume signal (document emit, chart render, scene render, table build, bundle compression ratio) into the runtime `observability/metrics` `MeterProvider` rather than a local log field, so render-duration histograms and output-byte gauges become first-class observable metrics on the one branch stream.
- Integrate the runtime `observability/metrics` instrument set (consumed, never re-minted) through the `receipt/artifact-receipt#RECEIPT` `contribute` fold; no new artifacts package.
- The signal contribution lands on the `ArtifactReceipt.contribute` path — the receipt is the natural per-production fact carrier — feeding the runtime instruments at composition; this is the artifacts-side consumer of the branch metric stream, never a parallel artifacts metric owner.
- Blocked-aligned on the runtime `observability/metrics` task landing the instrument set; the artifacts contribution is the receipt-fold edge that consumes it.

[QUEUED] Key the artifacts document output into the content-addressed reuse fabric — trickle-down from branch `CONTENT_ADDRESSED_REUSE_FABRIC`.
- Confirm every artifacts producer admits its `(ContentKey, Work)` pair into the runtime lane-admission elision so an identical document/chart/table/bundle at an identical spec short-circuits to the cached content key, and the `ArtifactReceipt` carries the hit/miss distinction — the artifacts owners already key every output by `ContentIdentity.of`, so the elision is inherited, not re-minted.
- Integrate the runtime `concurrency/lanes` `(ContentKey, Work[T])` admission and `frozendict` session cache (consumed); no new artifacts package.
- The change is verification-only on the artifacts side: every `_emit` already returns `ContentIdentity.of(...)`, so the producers thread the same key into the lane admission the runtime owns; the most expensive artifacts output (a signed archival PDF, a rendered notebook) is the natural elision target.
- Aligned on the runtime lane-admission task; the artifacts producers are pre-keyed consumers, never a second cache owner.

[QUEUED] Hold the outward figure handoff to the one HandoffAxis and clear the drift guard — trickle-down from branch `ONE_GRADUATION_RAIL_OUTWARD` + master `CROSS_PACKAGE_DRIFT_GUARD`.
- Confirm every artifacts figure/table/chart/scene that crosses outward to a sibling package travels only as the `compute/graduation` `HandoffAxis` model-asset case keyed by `ContentIdentity`, never a parallel per-artifact handoff; and confirm the artifacts sources re-mint no canonical concept (content-identity seed, receipt rail, retry owner, wire-projection name) so the runtime `evidence/evidence` `Structural.drift` cross-language query finds zero re-mints in any artifacts module.
- Integrate the `compute/graduation` `HandoffAxis` model-asset case and the runtime `evidence` `Structural.drift` query (both consumed, never re-minted); no new artifacts package.
- The change is alignment-and-verification on the artifacts side: every `_emit` already returns `ContentIdentity.of(...)` and every producer contributes one `ArtifactReceipt` case consuming the runtime ports, so the outward edge is the single `HandoffAxis` model-asset case and the source surface is drift-clean by construction.
- Aligned on the `compute/graduation` HandoffAxis task and the runtime `evidence` drift-detector task; the artifacts contribution is the pre-keyed outward consumer and the drift-clean source, never a second owner.

[QUEUED] Widen the test-tool source scope to the artifacts package source — trickle-down from branch `PYTHON_TEST_INFRA_FLOOR`.
- When the artifacts source transcribes, widen the central `pyproject.toml` test-tool scope by one row: `[tool.coverage.run].source`, `[tool.mutmut].source_paths`, the pytest `testpaths`, and the mypy advisory `files` each gain the artifacts source root, so the package ships under the same 95% coverage gate, mutation kill-ratio, and advisory type pass as the tooling.
- Integrate no new package — the kit (`coverage`/`pytest-cov`, `mutmut`, `pytest` + plugins, `hypothesis`, `inline-snapshot`, `mypy`, `ty`) is already admitted in the branch groups; the manifest-row widening is returned to the orchestrator as a central-`pyproject.toml` edit, never a per-package manifest.
- The widening lands in the one central manifest at the artifacts transcription, never before its source exists; source-scope-only, no capability proposed.
- Aligned on the branch `PYTHON_TEST_INFRA_FLOOR` task; the artifacts row is one entry in the per-package widening sequence.

[QUEUED] Author the `python-barcode` `.api` catalogue on capture — coverage move from `ENCODED_MARK_OWNER`.
- Author `artifacts/.api/python-barcode.md` (bare filename, never `api-`-prefixed) capturing the real `python-barcode` surface — `PROVIDED_BARCODES`, `get_barcode_class`/`get`, the `barcode.writer.SVGWriter`/`ImageWriter` writer family, and the per-symbology classes (Code128/EAN13/EAN8/UPCA/ITF/Code39/etc.) — via reflection on the synced `.venv`, and register `python-barcode` in the README package list.
- Integrate `importlib.metadata` and the runtime `evidence/evidence` `ApiPackage.reflect` surface against the synced environment; the capture runs on the cp315 core (pure-Python distribution).
- The catalogue lands in the folder `.api/`; until it lands, the `ENCODED_MARK_OWNER` linear/2D fence members stay a marked RESEARCH seam, and the `[6]-[REVIEW]` fence-truthfulness rule fails any `python-barcode` fence whose member has no present catalogue.
- BLOCKED on the package resolving in the environment: `python-barcode` is absent today (reflection returned `ModuleNotFoundError`), so the catalogue is RESEARCH-capture-pending behind the manifest admission, not authored from memory.

## [2]-[CLOSED]

[COMPLETE] Host-free chart-export floor on `charts/chart-spec.md` — `_export_host_free` folds vega (`vegafusion` subprocess pre-pass then host-free `vl-convert`), plotly (`kaleido` host-Chrome-gated), and matplotlib (gated-band Agg `savefig`); keys `ArtifactReceipt.Chart`, all spellings `.api`-verified.

[COMPLETE] Offscreen software-GL floor on `scene3d/scene.md` — `_render_scene` builds `pv.Plotter(off_screen=True)`/`add_mesh` and folds the PNG `screenshot` / `export_gltf`/`export_vrml` target on the gated `python_version<'3.13'` floor across `to_process.run_sync`; keys `ArtifactReceipt.Scene`, `.api`-verified.

[COMPLETE] Reproducible notebook report on `reporting/report-plan.md` — `ReportPlan` `NOTEBOOK` runs `papermill.execute_notebook` on the nbclient engine and serializes the executed `NotebookNode` via `nbformat.write(NO_CONVERT)` as the content-keyed audit artifact; keys `ArtifactReceipt.Report`, `.api`-verified.

[COMPLETE] Publication-table owner on `tables/table-plan.md` — `TablePlan` folds `tab_spanner`/`FORMATTERS`/`data_color` over `GT`, emits HTML/LaTeX host-free and PDF host-Chrome-gated; keys `ArtifactReceipt.Table`, `.api`-verified. Re-opened as `[PUBLICATION_TABLE_DEPTH]` for the unexploited surface.

[COMPLETE] PDF conformance and signing on `typography/conformance.md` — `_apply` folds fonttools `Subsetter`/`instancer` and pyhanko `SimpleSigner.load`/`PdfSignatureMetadata(use_pades_lta)`/`sign_pdf` with `HTTPTimeStamper`; keys `ArtifactReceipt.Pdf`, `.api`-verified. Re-opened as `[ARCHIVAL_CONFORMANCE_VERIFICATION]` for the unexploited validation surface.

[COMPLETE] Color-managed visual pipeline on `color-management/colorimetry.md` — `Colorimetry._compute` folds CONVERT/SPECTRAL/APPEARANCE (CIECAM02/CAM16)/PALETTE (Oklab) over the colour-science gateway; carries no receipt case, `.api`-verified. Re-opened as `[ICC_COLOR_MANAGED_OUTPUT]` for the unbuilt ICC/LUT layer.

[COMPLETE] Imaging scientific-transform deepening + QR re-base on `imaging/preview.md` — `_gated_raster` folds pillow + scikit-image on the gated band, `_qr` re-based onto segno removing the qrcode Pillow leak (qrcode dropped from manifest); keys `ArtifactReceipt.Preview`, `.api`-verified. Widened by `[ENCODED_MARK_OWNER]` to the full symbology family.
