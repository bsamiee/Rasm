# [RASM_CS_FABRICATION_DECISION]

Binding folder/page-set blueprint for the ground-up restructure of `libs/csharp/Rasm.Fabrication/.planning/` (5 folders → 13; 17 pages → 33; ~2.9k design LOC). The durable rebuild engine executes this DECISION structure-first per the `RASM-CS-FABRICATION-BRIEF.md` `[05]-[BUILD_LEGS]` sequencing law; `RASM-CS-GEOMETRY-DECISION.md` (LANDED) is the signature-lock source; `docs/stacks/csharp/` governs every fence. Every V1-V10, E1-E14, 14 capability plane, 24 roster package, and 23 IDEAS/TASKLOG card is disposed — no open item survives to a build agent.

The blueprint composes the namespace-true partition, the kernel-seam K1-K23 signature-lock TABLE, the 8-case `Post`+`Document` entry, the `ArtifactKind` egress-spine collapse onto `owner#atoms`, the F1-F4 flagship dry-run closure, the `CapabilityVerdict`-on-`owner#atoms` plan gate, the clipper SPLIT under one absorber, and the exhaustive 24-row roster. The neutral `CutProgram` AST is `Posting/program`-local and assembled in the `Run(Post)` case from the `owner#atoms` `Motion` result — never inside the Cam fold — preserving the disk-true decoupling the acyclicity proof depends on (`[00]` ruling 5).

## [00]-[STANDING_LAW] — never re-opened mid-leg

The corpus is a subtractive-milling engine wearing production clothing; the folder map is the disguise. `Process/` hosts four realized namespaces (`Process`/`ProcessModel`/`ProcessPhysics`/root), `Posting/` two (`Posting`/`Projection`), `Toolpath/` three (`Toolpath`/`Kinematics`/`Additive`), `Polygon/` and `Nesting/` each hide a migrant (`Geometry2D` import, `Fixturing` workholding — both already namespace-true on disk). The fence `namespace` declaration is the ground truth of where a concern BELONGS; the folder is the lie the DECISION corrects. Twelve realized namespaces (disk-verified: `Process`·`ProcessModel`·`ProcessPhysics`·root·`Geometry2D`·`Toolpath`·`Kinematics`·`Additive`·`Nesting`·`Fixturing`·`Posting`·`Projection`) become thirteen folders; three mandate lanes (`Verify`·`Spec`·`Tooling` machinability) mint new namespaces the roster provisioned but no fence realized. Fabrication is ALSO an egress engine: every flagship terminates in a content-keyed machine-consumable artifact, so the egress concern COLLAPSES onto the entry vocabulary — the `ArtifactKind` discriminant + ONE `ContentHash.Of` fold on `owner#atoms` — never a fourteenth `Egress/` folder.

FIVE structural rulings:

1. **`ProcessKind` collision-break (HIGH hazard — the V1 floor introduces it).** `owner.md:39` declares `namespace Rasm.Fabrication.Process`; `family.md:98` declares `[SmartEnum<string>] public sealed partial class Process` in `namespace Rasm.Fabrication.ProcessModel`. The V1 ratification homes family into `Rasm.Fabrication.Process`, so the type resolves `Rasm.Fabrication.Process.Process` — and every bare `Process` reference from within `Rasm.Fabrication.*` binds the NAMESPACE `Rasm.Fabrication.Process` before the imported type (namespaces in the enclosing hierarchy win name lookup), a live CS0118 (`owner.md:77` `Process Process` field, `program.md` bare `Process` refs). **RULING: rename the SmartEnum type `Process` → `ProcessKind`** (family declaration + every `input.Process`/`ProcessKind process`/`FabricationInput.Process`-typed reference across owner/physics/motion/program/magazine). This is the namespace-PRESERVING break — renaming the namespace segment forfeits the folder=namespace 1:1 the partition ratifies. **`Machine` does NOT rename** (no `Machine` namespace exists — verified; the asymmetry is collision-forced). A companion `Machine→MachineKind` rename is gratuitous churn, REJECTED.

2. **`CutterForm` mints on `owner#atoms`, never `Tooling/`.** `FabricationPolicy.Cam` re-frames its bare `ToolRadius` double (`owner.md:85`) to a `CutterForm` reference (V5). Siting `CutterForm` on `Tooling/cuttingdata` cycles: `owner#atoms(Cam.CutterForm) → cuttingdata(CutterForm) → magazine(CutterForm.Of(ToolAssembly)) → owner#atoms(HolderEnvelope→Loop)` (`magazine.md:108` `HolderEnvelope(ToolAssembly)→Loop` is the back-edge anchor). RULING: the `CutterForm` value TYPE mints on `owner#atoms` (a pure-scalar leaf — flat/ball/bull/taper/drill/chamfer/thread-mill rows with diameter/corner-radius/taper-angle/flute-length), exactly as `CutStrategy` and `ResidualStock` ride the entry vocabulary; `Tooling/cuttingdata` owns the PROJECTION `CutterForm.Of(ToolAssembly)` + the Kienzle machinability keyed by it. The four consumers (surface/removal/guard/cuttingdata) read the `owner#atoms` type; the cycle never forms.

3. **The two refuted reciprocal `[V12]` bindings.** Disk-verified: `PBRIEF [V12]` is `[V12_GOVERNANCE_RECONCILE]` (`PERSISTENCE-BRIEF:123`), NOT an `ArtifactKind` egress index — the real Fabrication binding is the held-open BLOCKED card `[ARTIFACT_CONTENT_KEY_FEDERATION]` (`PERSISTENCE-BRIEF:221`). `CBRIEF [V12]` is `[V12_DISCIPLINE_COVERAGE]` (`COMPUTE-BRIEF:123`), NOT a `WasteAreaMm2` rollup — and `ARCH:51` already reads "the recorded next-campaign Compute counterpart" (a forward demand). RULING: every machine-consumable egress artifact mints its content key through the CONFIRMED `ContentHash.Of` seed-zero entry (landed kernel law), and the `ArtifactKind`/durable-row fold is carried as a **Fabrication-AUTHORED demand + blocked cross-package seam residual**, never a composed upstream contract; the Compute waste rollup is a one-sided forward demand. Signature-lock rides the geometry DECISION `FAB:NN`/row anchors (stable), not the `+14/+16`-drifted GBRIEF line pins. The `[RESEARCH]`-tail census is FIVE (disk-verified: `kinematics:122`, `skeleton:199`, `slicing:219`, `stock:317`, `program:379`), not the brief's three.

4. **The entry is 8-case; the egress spine collapses onto `owner#atoms`.** `FabricationPolicy`/`FabricationResult` grow the brief-named `{Additive, Verify, Inspect}` PLUS `{Post, Document}` atop the disk `{Cam, HiddenLine, Nest}` — EIGHT cases. Re-posting one `Motion` to five dialects is `Run(Post{Motion, PostDialect})` ×5 (F1); traveler-compose is `Run(Document{results})` (F4) — both re-invocable terminal transforms over a prior result, so each is a `Run` policy case carrying the prior result on `owner#atoms`, NEVER a second public `Post()`/`Traveler.Assemble()` fold. The `ArtifactKind` discriminant is a row family co-located with the egress `FabricationResult` cases on `owner#atoms`, keyed through ONE `ContentHash.Of` fold. `CapabilityVerdict` mints on `owner#atoms` as a minimal input-carried verdict leaf (pass/fail + Cpk + demanded grade); `Spec/capability.Gate` PRODUCES it wave-4, the plan READS it input-carried, closing spec→plan→verify→capability with no intra-run cycle (the full `CapabilityReport` stays terminal on `capability`).

5. **The result-payload discipline (restores the disk-true motion↔program decoupling).** Disk: `Cam.Solve` returns `Fin<FabricationResult>` carrying a `Motion` (`motion.md:15,59,61`); `Posting.Post(FabricationResult, …)` returns `Fin<CutProgram>` (`program.md:15`) — the `Motion`/`Placement` streams "dead-end into" the `CutProgram` AST program OWNS (`program.md:3`). RULING: **every `FabricationResult` egress case declared on `owner#atoms` carries ONLY owner#atoms-safe payloads** (primitives, kernel vocabulary, and the owner#atoms leaves `Move`/`Edge3`/`PartTransform`/`Remnant`/`ArtifactKind`/`ContentKey`/`CutterForm`/`ResidualStock`); the rich plane-internal types (`CutProgram` AST, PicoGK `Voxels`, GD&T frames) stay plane-local and NEVER ride a result case. The neutral `CutProgram` AST is `Posting/program`-local, assembled in the `Run(Post)` case body from the `owner#atoms` `Motion` result — NOT inside the Cam fold (the Cam fold executes Guard/Workholding/Magazine conditioning and emits the conditioned `Motion`; posting-side conditioning `Kerf`/`Feedrate`/`Lookahead` + `dialect.Emit` lowering are program's; the Placement-vs-Motion asymmetry binds which conditioning runs on which egress case — the 2D-cut `Placement` runs `Kerf`/`Lead`/`Pierce`/`Tab`/`Sequence`, the already-conditioned `Motion` NEVER a second kerf pass, V3). `motion` and `program` decouple through the `owner#atoms` `Motion` type — no `motion→program` compose edge exists — and the `PostedProgram` result case carries the lowered blocks + `ArtifactKind` + `ContentKey`, never the AST.

OpenCAMLib probe = **FEASIBLE** — sited on `Toolpath/surface` via a thin `extern "C"` C-shim + source-generated `[LibraryImport]` P/Invoke over the shipped C++-ABI SHARED `libocl` archives; author-kernel SDF drop-cutter (K8) retired to a recorded admission-abandonment fallback (`[00]` probe ruling below, `[06]` row 1). Both ruled kernel hinges carried verbatim and signature-locked, never re-opened: **`[V10]a` kerf split** (K1 — kernel owns the exact `JoinType`×`EndType` corner-row offset assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours on `Geometry2D/arcs`; geometry DECISION line 149) and **`[V10]d` medial radius carriage** (K1/K2 — kernel medial carries the per-point clearance RADIUS + arbitrary-probe `Clearance(Point3d probe)` as ONE shared result family; `Toolpath/Skeleton.cs` wavefront dies unconditionally, keeps only the trochoidal WALK; geometry DECISION lines 123, 222).

**PROBE RULING (OpenCAMLib surface-engine siting — decided from probe evidence).** ADMIT-WITH-SHIM. All three brief-named RIDs ship as SHARED libraries in release `2023.01.11` (`macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64`; `cxxlib.cmake:1` `add_library(ocl SHARED …)`). The exported ABI is C++-mangled with by-reference `STLSurf&`/`CLPoint&`/`std::vector<CLPoint>` arguments — a direct P/Invoke is impossible and a C-shim is MANDATORY (the ruled path, not a defect). The wrap surface is small and uniform: one `ocl::Operation` base (`setSTL`/`setCutter`/`setSampling`/`setThreads`/`run`/`getCLPoints`) unifies `BatchDropCutter`/`PathDropCutter`/`Waterline`/`AdaptiveWaterline`; the `MillingCutter` hierarchy (`Cyl`/`Ball`/`Bull`/`Cone`/`Composite`) maps 1:1 onto the `CutterForm` axis (flat/ball/bull/taper/compound). ~18-24 `extern "C"` functions, all blittable (opaque `void*` handles + flat `double[]` triangles/points); the Boost.Python binding (`src/pythonlib/ocl_*.cpp`) is the exact member-by-member blueprint. LGPL-2.1 (four independent primary sources); dynamic-link P/Invoke through a separate replaceable `libocl.dylib/.so/.dll` satisfies §6 trivially and is ratified campaign policy — keep `libocl` dynamically linked, never statically folded into the shim. Dependency closure is tiny: header-only Boost (`Boost::boost`) + OpenMP; zero submodules; no CGAL/Eigen/TBB. Two admission routes doubly cover the RID matrix: consume the shipped SHARED archives OR build `libocl`+shim from source per RID (`BUILD_CXX_LIB=ON`), Forge-provisioned like the kernel Z3 precedent. No C ABI/C# binding/NuGet exists (feed-verified). The fallback (author-kernel drop-cutter over the kernel `[V8]` SDF) is strictly heavier on BOTH axes — re-derives exact per-cutter-form contact (flat/bull/cone are the cutter-solid lower envelope, NOT a mesh-SDF iso-offset), is resolution-bound where OCL is analytically exact, degrades waterline to marching-squares, and is calendar-HOSTAGE to the UNLANDED kernel `[V8]` SDF lane. One brief correction carried: `[04]`'s "pybind11/emscripten" is imprecise — the canonical upstream binding is Boost.Python (immaterial to feasibility; the `.api` records Boost.Python).

## [01]-[RATIFIED_PARTITION] — 13 folders = 13 namespaces (bijective), fault clusters

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`, all namespace-motivated on disk): `Polygon/` DIES (clipper SPLITS to `Geometry2D/{algebra,arcs}`, import MOVES to `Ingress/profile`). `Process/` KEEPS (loses magazine → Tooling; the collision-break renames its axis type). `Toolpath/` KEEPS (loses kinematics → Kinematics, slicing → Additive; gains surface + partition). `Nesting/` KEEPS (loses workholding → Fixturing). `Posting/` KEEPS (loses projection → Documentation; gains dialect). MINTED: `Tooling/` · `Geometry2D/` · `Ingress/` · `Kinematics/` · `Additive/` · `Fixturing/` · `Verify/` · `Spec/` · `Documentation/`. Every folder ≥2 pages; the mandate lanes are the growth that makes the thin folders legal. `ARCHITECTURE.md` `[01]` codemap + `[02]` seam ledger + the README router rewrite in the wave-1 motion.

Namespace partition — 13 TIER-1 folders ↔ 13 TIER-2 namespaces, BIJECTIVE. The **six folder-overrides-namespace ratifications** (fence `namespace` rewrites in the same MOVE): family/faults/physics → `Process` (from `ProcessModel`/root/`ProcessPhysics`), magazine → `Tooling` (from `ProcessModel`), import → `Ingress` (from `Geometry2D`), projection → `Documentation` (from `Projection`). The **four namespace-KEEPING moves** (the realized namespace already names the target folder, the FOLDER catches up): clipper `Geometry2D` (SPLIT, folder Polygon→Geometry2D), kinematics `Kinematics` (folder Toolpath→Kinematics), slicing `Additive` (folder Toolpath→Additive), workholding `Fixturing` (folder Nesting→Fixturing).

| # | Folder | Namespace | Pages | Fault cluster | Charter |
|---|---|---|---|---|---|
| 1 | `Process/` | `Rasm.Fabrication.Process` | owner, family, physics, faults | 2700 band owner (`OpenLoop` 2704 cross-cutting recorded exception) | the entry, the axes, the physics, the rail |
| 2 | `Tooling/` | `Rasm.Fabrication.Tooling` | magazine, cuttingdata | 2712 `MachinabilityUnknown`, 2724 `NoToolForOp` | ISO-13399 tool intelligence + machinability |
| 3 | `Geometry2D/` | `Rasm.Fabrication.Geometry2D` | algebra, arcs | 2703 `KerfCollision` (re-attributed from Posting) | the 2D substrate (line-space + arc-space) |
| 4 | `Ingress/` | `Rasm.Fabrication.Ingress` | profile, solid, steel | 2711 `IngressTranslation` | everything entering as geometry |
| 5 | `Toolpath/` | `Rasm.Fabrication.Toolpath` | motion, surface, partition, guard, skeleton | 2705 `InadmissiblePair`, 2706 `Gouge`, 2707 `Collision`, 2713 `SampleStalled`, 2723 `PartitionDegenerate` | subtractive CAM |
| 6 | `Kinematics/` | `Rasm.Fabrication.Kinematics` | cell, machine | 2702 `Unreachable`, 2714 `AxisSingularity` | motion topology |
| 7 | `Additive/` | `Rasm.Fabrication.Additive` | slicing, implicit, production | 2708 `NonManifoldSlice`, 2715 `VoxelFault`, 2716 `OrientationInfeasible`, 2725 `Unsupported3mfExtension` | production 3DP |
| 8 | `Nesting/` | `Rasm.Fabrication.Nesting` | nfp, stock | 2701 `NoFit`, 2709 `StockOverflow`, 2710 `Nest` | layout + yield |
| 9 | `Fixturing/` | `Rasm.Fabrication.Fixturing` | workholding, setups | 2717 `SetupInfeasible`, 2726 `DatumLineageBroken`, 2727 `ClampOnMachinedFace` | keep-out + setup planning |
| 10 | `Posting/` | `Rasm.Fabrication.Posting` | program, dialect | 2718 `DialectUnsupported`, 2719 `ProgramParse`, 2728 `BlockCapExceeded` | machine-code emission |
| 11 | `Verify/` | `Rasm.Fabrication.Verify` | removal, probing | 2720 `ProbeOvertravel` (+ `Gouge` 2706 shared verify-time) | program-level truth |
| 12 | `Spec/` | `Rasm.Fabrication.Spec` | tolerance, capability | 2721 `ToleranceUnsatisfiable`, 2722 `CapabilityShortfall`, 2729 `StackupExceeded` | production specs |
| 13 | `Documentation/` | `Rasm.Fabrication.Documentation` | projection, traveler | reserved (projection routes kernel `ProjectionFault`; traveler assembles) | shop documentation |

RULED, never re-litigated: (a) the `Process` axis TYPE renames `Process`→`ProcessKind` (`[00]` ruling 1); `Machine` unchanged. (b) `Geometry2D` is the ONE namespace carrying two pages — the split is CONCERN-driven (Clipper2 int64 line lattice vs CavalierContours bulge arc lattice, `[V10]a` stratum), not namespace-driven, so folder=namespace 1:1 holds. (c) `Ingress` carries three pages under ONE polymorphic `Ingress.Admit(IngressSource)` fold — profile/solid/steel own the `Profile`/`Solid`/`Steel` source arms. (d) `Documentation/` has an EMPTY reserved fault block (projection consumes the kernel `Rasm.Geometry.Projection` `ProjectionFault`; traveler is content-keyed assembly with no fault producer) — mechanically checkable. (e) The `owner#atoms`/`owner#run` two-node split is the ONE ratified acyclicity exception: `Process/owner` is both the upstream atom vocabulary and the terminal `Fabrication.Run` fold; the ledger evaluates it as two nodes and the page never splits physically (its ledger passes the two-node discipline — see `[11]`).

Fault-registry design: the 2700 century divides into 13 folder clusters. The landed **2701-2710 are the FROZEN first-instance layer** (disk-verified against `faults.md:39-61`, one arm per `+n` code) — wire codes retained per the `.archive` "build ON, never re-band" law + the held-open `[ARTIFACT_CONTENT_KEY_FEDERATION]` persisted-decode constraint — and are NON-contiguous per folder (Nesting spans 2701/2709/2710) as the recorded consequence of the freeze. The NEW growth arms allocate folder-grouped from 2711 across two tiers (2711-2722 · 2723-2729), so folder = fault-cluster is mechanically checkable within each growth tier; the ten legacy arms are a recorded frozen exception mirroring the kernel's cross-cutting `DegenerateInput` 2400. Full registry in `[04]`.

## [02]-[PAGE_SET] — 33 rows (paths `libs/csharp/Rasm.Fabrication/.planning/`-relative)

Row schema: path | action → engine lowering (kind new/rebuild/improve; absorb {into, from} — the absorbing agent moves the source content and deletes the source, so absorb IS the delete; deletePages is reserved for sources with no in-set destination and is EMPTY in this run) | charter (folder · namespace · fault cluster) | entry (owning fold signature + the `FabricationPolicy`/`FabricationResult` case ONLY where the page grows the entry) | seams (out = composes; in = consumed by; `owner` evaluates as TWO nodes — `owner#atoms` upstream-only, `owner#run` terminal) | wave. Waves 1:1 with legs. Per-page NEW/REBUILD skeletons follow the docs/stacks/csharp shape law; `[00]` ruling 5 governs every result-case payload.

### Wave 1 / Leg 1 — STRUCTURE + AXES + RAIL (Process rebuilds + Tooling)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 1 | `Process/owner.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700 owner. The settled `Run` entry + generated total `Switch` PRESERVED VERBATIM (cold-pass core); the rebuild is atom-vocabulary + policy/result growth to **8 cases**: `Loop` widens to `(Arr<Point3d>, bool, Arr<double> Bulges)` + 3-arg ctor + `BulgeAt` (the `[V10]a` arc rail's one shape, 0/tan(θ/4)); `ProjectionDir` mints HERE (SEAM inversion fix — `owner.md:30,70` currently imports it downstream from `Projection`); `CutterForm` value type mints HERE (`[00]` ruling 2); `ResidualStock` + per-setup `StockSnapshot` mint HERE (V7, input-carried); `CapabilityVerdict` mints HERE (V8, input-carried); `ArtifactKind` discriminant mints HERE (egress row family, keyed through the ONE `ContentHash.Of` fold). Every egress result case carries ONLY owner#atoms-safe payloads (`[00]` ruling 5) | `Fin<FabricationResult> Fabrication.Run(FabricationPolicy, FabricationInput)` — **8-case** `FabricationPolicy` {`Cam`, `HiddenLine`, `Nest`, +`Additive(AdditivePolicy)`, +`Verify(VerifyPolicy)`, +`Inspect(InspectPolicy)`, +`Post(FabricationResult.Motion, PostDialect)`, +`Document(Seq<FabricationResult>)`}, **8-case** `FabricationResult` {`Motion`, `HiddenLineResult`, `Placement`, +`AdditiveResult`, +`VerificationResult`, +`InspectionResult`, +`PostedProgram`, +`TravelerDocument`}; egress cases carry the `ArtifactKind` discriminant + `ContentKey` (lowered blocks/keys only, never plane AST) | **owner#atoms** out: predicates `Orient2D`, Vectors `MeshSpace`/`Point3d`, `ContentHash.Of` (content-key seed), family `CutStrategy`/`ProcessKind` (leaf axes) · in: EVERY plane reads the atoms. **owner#run** out: projection.Hlr, motion.Cam, nfp.Nest, additive, removal, probing, program/dialect Post-lowering, traveler Document-compose (terminal 8-arm dispatch, arms realize per-plane leg) · in: none (terminal) | 1 |
| 2 | `Process/family.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700. Axis widening + the collision rename: `ProcessKind` (ex-`Process`, `[00]` ruling 1) · `Machine` (UNRENAMED) · `RemovalModality` · `KinematicClass` deepens to rotary-topology rows (table-table trunnion \| head-head \| head-table \| nutating — `mill-5axis` stops binding `CartesianGantry`, disk `family.md:118→100`, V5/V8) · `HoldingClass` · `PostDialect` grows GRAMMAR-family capability columns (family · canned-cycle grammar · macro/subprogram convention · WCS roster · cutter-comp admission · arc mode · block cap · decimal/modal policy · per-`GCommand` code-override — V4; the flat 3-column render dies) · `CutStrategy` gains a dimensionality column (2.5D \| 3D-surface \| multi-axis, V5). Data-vs-lowering: widened axes stay HERE, emission folds are `Posting/dialect`'s | NONE (pure `[SmartEnum]` axes; `Admits` row-relations — a leaf, composes only kernel) | out: `Rasm.Geometry` `GeometryFault`, predicates · in: owner#atoms (CutStrategy/ProcessKind), physics, motion, program, dialect, magazine, workholding, machine, cell | 1 |
| 3 | `Process/physics.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700. Identity map (V9): ONE `Material` identity carrying `Map<RemovalModality, ModalityPhysics>` (the disk `stainless`/`stainless-abrasive` + `mild-steel`/`mild-steel-thermal` fragmented rows `physics.md:91-97` collapse); `Budget` modality-dispatches into the map; `RemovalBudget.Erosion` lands (EDM discharge/pulse/wire-feed — the `erosion→ThermalBudget` conflation dies) beside `Resin` (exposure/cure) + `Powder` (laser/hatch/scan) + DED-distinct-from-FFF; the dead `Overrides` carrier DELETES; the `90.0` fallback demotes behind the data table. The `CuttingData` table (disk `physics.md:117`) DONOR-BLOCK excises to `Tooling/cuttingdata` (charter-carried, not absorb-encoded) | `RemovalBudget RemovalParameter.Budget(ProcessKind, Material, ToolAssembly, Operation)` | out: family (`RemovalModality`), UnitsNet, `Rasm.Materials/Properties` (raw doubles, AEC-peer boundary — anchor reconciled to `physical-properties#MATERIAL_PROPERTY`) · in: motion, cuttingdata, surface (chip-load budget) | 1 |
| 4 | `Process/faults.md` | REBUILD → rebuild | Process · `Rasm.Fabrication.Process` · 2700 band owner. `FabricationFault` `[Union]` on `FaultBand.Fabrication = 2700`: every legacy arm retypes its bare `string Detail` (disk `faults.md:39-48`) to a typed payload (`Gouge`→point+CutterForm, `Collision`→zone, `InadmissiblePair`→typed `(RemovalModality, CutStrategy)`, `NoFit`→part+tried rotations, `Unreachable`→joint diagnostics); the frozen 2701-2710 wire codes PRESERVED; nineteen new arms land 2711-2729 folder-grouped across two growth tiers (`[04]`); the `band-2500` root-anchor + `ARCH:16`/`kinematics:3`/`TASKLOG:44`/`.api` 25xx references purge to 2700 | NONE (`FabricationFault.<Case>(...).ToError()` vocabulary; composes kernel band-2400 `GeometryFault` for `DegenerateInput`) | out: `Rasm.Element` `FaultBand` registry, `Rasm.Geometry` `GeometryFault` · in: every fault-routing owner | 1 |
| 5 | `Tooling/magazine.md` | MOVE (ProcessModel→Tooling) + wire → new; absorb {into: `Tooling/magazine.md`, from: `Process/magazine.md`} (the absorb's delete is the source delete — never a deletePages row for an in-set-absorbed source) | Tooling · `Rasm.Fabrication.Tooling` · 2712,2724. MTConnect `CuttingToolAsset` `ToolAssembly`/`Schedule` life-split PRESERVED; `HolderEnvelope` projection kept; `Runout` reads the real `ShankDiameterMeasurement` value (the disk literal `0.01`/`0.005` stub `magazine.md:161` dies, E12); `GenerateHash` reconciles onto `ContentHash.Of` (no second mint); WIRED — the tool-change `Schedule` feeds `Posting/program` (real `M6`/`G43`/`Tnn` blocks at interval boundaries, the E2 fiction realized) | `ToolMagazine.Schedule(Seq<ToolAssembly>, …)` life-split; `HolderEnvelope.Of(ToolAssembly)` | out: MTConnect `CuttingToolAsset`, `ContentHash.Of`, UnitsNet, physics (Tool), owner#atoms (Loop), Riok.Mapperly · in: cuttingdata (ToolAssembly), guard (HolderEnvelope), program (Schedule) · cross-pkg: ← `Rasm.AppHost` decoded tool-life telemetry (`APPHOST:210`, first consumer) | 1 |
| 6 | `Tooling/cuttingdata.md` | NEW → new | Tooling · `Rasm.Fabrication.Tooling` · 2712. The machinability model (V9): Kienzle unit-cutting-force `kc` + chip-thickness exponent + per-operation surface-speed + feed-per-tooth columns keyed by the `physics` `Material` identity (honest seed DATA behind a CSV/QIF data-INGRESS arm, FreeCAD cards copyright-removed gap recorded); the `CutterForm.Of(ToolAssembly)` projection from the ISO-13399 measurement set (the type mints on owner#atoms, `[00]` ruling 2); `program`'s `0.0012` deflection literal reads the `kc` column | `CuttingData CuttingData.Of(Material, CutterForm, Operation)`; `CutterForm CutterForm.Of(ToolAssembly)` | out: magazine (ToolAssembly), physics (Material), owner#atoms (CutterForm), MathNet, UnitsNet · in: program (kc deflection), surface (effective-diameter speed) | 1 |

### Wave 2 / Leg 2 — SUBSTRATE + INGRESS (Geometry2D split, Ingress, skeleton staged early)

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 7 | `Geometry2D/algebra.md` | SPLIT from clipper → new; absorb {into: `Geometry2D/algebra.md`, from: `Polygon/clipper.md`} — the ONE clipper absorber-deleter (whole-file custody: the line-space half lands HERE, the arc-space block `clipper.md:182-271` lands on row 8's standalone inventory; the absorb's delete is the single clipper delete) | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The Clipper2 line-space owner (offset · `DeltaCallback64` variable offset · Boolean clip · open-subject/open-path clip — trimming open toolpath centerlines + marking/engrave polylines against region boundaries, a distinct Clipper2 capability from closed-region Boolean, brief `[04]` line-space roster · Minkowski sum/diff · `ReuseableDataContainer64` scanbeam reuse) — the settled craft, namespace already true (`clipper.md:50`). ADDS `NestingOrder` — the inner-before-outer containment-depth sort (namespace-true re-home of the mis-attributed `Posting.Sequence`: contour ordering by nesting depth is a Geometry2D concern via `Loop.Covers`, not machine-code; posting/motion/workholding all compose it, killing the workholding→posting cross-wave edge) | `PolygonAlgebra` — `Offset`/`Clip`/`ClipOpen`/`Minkowski.Sum`/`Minkowski.Diff`/`OffsetVariable`/`NestingOrder` (line-space) | out: Clipper2 `Clipper2Lib`, predicates `Orient2D`, owner#atoms (Loop) · in: nfp, guard, workholding, motion, arcs (kerf base), slicing, partition (open-path engrave/marking trim), program (NestingOrder) | 2 |
| 8 | `Geometry2D/arcs.md` | SPLIT from clipper → new — plain new, NO absorb row (`Polygon/clipper.md` travels via row 7's single absorb; a second absorb on the same source is the double-delete race). Content source: `clipper.md:182-271` (`ArcAlgebra`/`BoolKind`/CavalierContours arc-space), fully inventoried in this charter so the build stands alone against the absorber's delete | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The CavalierContours arc-space owner composing the RULED `[V10]a` split: the kernel owns the exact corner-row offset assembly + medial, `ArcAlgebra` owns pure kerf compensation IN ARC SPACE over the kernel-emitted region offsets — the landed `BoolKind` `[SmartEnum<string>]` set-op axis (`clipper.md:186-190` or/and/not/xor→CavalierContours `BooleanOp`) + `ArcOffset`/`ArcBoolean`/`ShapeOffset`/`ArcLength`/`SampleAtLength`, island-preserving `Shape<T>.FromPlines(loops).ParallelOffset`, `StaticAABB2DIndex<T>`, the bulged-`Loop`↔`Polyline<double>` boundary seam (all absorbed from clipper); the kerf/lead/adaptive rails wire through HERE (arc-native, no biarc refit); the `result.Positive` PHANTOM corrects to `result.PosPlines[].Pline` (disk `clipper.md:221`; real member `api-cavaliercontours.md:47`); reads the widened `Loop.Bulges`/`BulgeAt`/3-arg ctor (landed wave-1 on owner#atoms) | `ArcAlgebra` — `KerfArc`/`LeadArc`/`AdaptiveArc`/`ArcBoolean(BoolKind)` over kernel `Offsetting.Apply` region offsets | out: CavalierContours (arc-space), kernel `Offsetting.Apply` (region offsets, K1), algebra (kerf base), owner#atoms (Loop.Bulges), predicates `Orient2D` · in: program (arc-native kerf/lead), motion (adaptive), skeleton (trochoidal chord) | 2 |
| 9 | `Ingress/profile.md` | MOVE (Polygon/import, ns Geometry2D → Ingress) → new; absorb {into: `Ingress/profile.md`, from: `Polygon/import.md`} | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DXF/DWG profile-ingress arm of the ONE polymorphic `Ingress.Admit` fold; ACadSharp `Failsafe`/bulge+NURBS samplers/`Insert.Explode` PRESERVED; the dead `warnings` accumulator wires the strict-mode `NotificationType.Error` escalation (ruled — the claim-deletion form loses ingress-degradation visibility); the netDxf rejection frame re-writes (netDxf removed repo-wide) | `Fin<AdmittedGeometry> Ingress.Admit(IngressSource)` over `IngressSource` `[Union]` {`Profile` · `Solid` · `Steel`} — profile owns the fold + `Profile` arm | out: ACadSharp, predicates, owner#atoms (Loop) · in: nfp (part library), program (profile program), motion · cross-pkg: ← `Rasm.Bim/Exchange` (shared ACadSharp read pin, `ARCH:52` netDxf parenthetical → the AppUi ACadSharp two-format WRITE leg) | 2 |
| 10 | `Ingress/solid.md` | NEW → new | Ingress · `Rasm.Fabrication.Ingress` · 2711. The OcctNet B-rep solid-ingress arm: `ImportStep`(AP203/214/242)/`ImportIges`/`ImportStl` → `OcctShape` → `Triangulate` → `OcctMesh`; the tessellated `OcctMesh` crosses to kernel vocabulary at the seam (`MeshSpace` admission; dirty STL routed through the kernel predicate-gated `HealOp`), so downstream planes consume admitted kernel geometry, never raw soup; the `libTKHLR`/`libTKXCAF` managed-unbound SCOPE_LIMIT is a standing demand row (assembly + AP242 PMI surfacing = wrapper pressure, the `Spec/` plane's secondary source), maintenance risk re-verified each leg | `Ingress.Admit` `Solid` arm (STEP/IGES/STL → kernel `MeshSpace`) | out: OcctNet.Wrapper, kernel `MeshSpace` admission (`mesh.md`), kernel `HealOp` (`repair.md`, dirty STL), Riok.Mapperly · in: slicing, surface, projection (the B-rep the format shops receive) | 2 |
| 11 | `Ingress/steel.md` | NEW → new | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DSTV NC1 steel-exchange READ arm: the record tree (`ST` header + `BO`/`SI`/`SC`/`AK`/`IK`/`KA` blocks) → `Loop` via a Riok.Mapperly generated projection (hand-written seam copyists die); the `KA` bend blocks seed the sheet-metal IDEAS lane (K-factor unfold waits for its consumer); the emission MODEL is shared with the `Posting` NC1 target (mirroring the read) | `Ingress.Admit` `Steel` arm (NC1 → Loop) | out: DSTV.Net, Riok.Mapperly (DSTV-record → Loop), owner#atoms (Loop) · in: nfp, dialect (NC1 emit model shared — the counterpart of row 27's `steel` out-edge) | 2 |
| 12 | `Toolpath/skeleton.md` | REBUILD → rebuild (cross-folder, staged early: wave-3 motion's engagement bound reads its clearance field) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705-2707,2713. Kernel-medial CONSUMER (`[V2]a`/`[V10]d`, RULED): `Toolpath/Skeleton.cs` wavefront (`Wavefront`/`SkeletonEvent`/`Propagate`/`OffsetAt`) DIES UNCONDITIONALLY; the page keeps ONLY the trochoidal constant-engagement WALK strategy over the kernel clearance field; the `[03]-[RESEARCH]` tail (`skeleton.md:199`) + false "no managed library exists" seal purge | `Skeleton.Walk(CurveSkeleton, EngagementPolicy)` (trochoidal WALK only) | out: kernel `Skeletonize.Apply` → `Fin<CurveSkeleton>` + `Offsetting.Apply` clearance-radius/`Clearance(Point3d probe)` (the ONE shared clearance family, 2D+3D, K1/K2), arcs (trochoidal loop chords — same wave, arcs authors earlier in the leg) · in: motion (Adaptive engagement bound), guard (ClearanceAt reach) | 2 |

### Wave 3 / Leg 3 — CAM + ADDITIVE + NESTING + FIXTURING

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 13 | `Toolpath/guard.md` | KEEP + wire → improve | Toolpath · `Rasm.Fabrication.Toolpath` · 2706-2707. Shape-correct interior PRESERVED (swept Minkowski envelope, distinct part-keep/stock-keep-out verdicts, the per-move DESIGN-time floor); WIRED — `Check` consults per committed feed move inside the Cam fold (`Gouge`/`Collision`→typed fault, `Clearance`→substitute lift, `Clear`→commit); reads the CURRENT `StockSnapshot` (per-setup), never the raw blank; the 3 out-seams DECLARED | `Verdict Guard.Check(Move, Part, Stock, Fixture)` | out: skeleton (`ClearanceAt` reach), magazine (`HolderEnvelope`), algebra (Minkowski), workholding (ExclusionZone), kernel `Spatial/index` (BVH broad-phase), owner#atoms (CutterForm swept radius, StockSnapshot) · in: motion (per-move), setups (per-op collision) | 3 |
| 14 | `Fixturing/workholding.md` | MOVE (Nesting, ns Fixturing → Fixturing) + safety rebuild → new; absorb {into: `Fixturing/workholding.md`, from: `Nesting/workholding.md`} | Fixturing · `Rasm.Fabrication.Fixturing` · 2717 (namespace already `Fixturing` on disk `workholding.md:34`). Safety hardening (V3/E9): `Clears`/`Condition` replace 3-point endpoint+midpoint sampling with segment-vs-polygon intersection (the thin diagonal clamp crossing detected); `ForHolding` first-match becomes a TOTAL mapping (`Vise` reachable behind `Clamp`); per-kind footprint SHAPES land as real geometry columns (two-jaw vise, revolved chuck jaw, full-bed vacuum), not a `MarginScale` scalar; `Condition` WIRED into the Cam conditioning fold and composing `NestingOrder` (the re-homed ordering, backward to Geometry2D) | `Fin<Seq<Move>> Workholding.Condition(Seq<Move>, Fixture)`; `ExclusionZone Workholding.Zone(…)` | out: algebra (Offset/Clip, NestingOrder), family (HoldingClass), owner#atoms (Loop) · in: guard (ExclusionZone), motion (Condition), setups (per-setup fixture) | 3 |
| 15 | `Fixturing/setups.md` | NEW → new (MULTI_FIXTURE_SCHEDULE realizes) | Fixturing · `Rasm.Fabrication.Fixturing` · 2717,2726,2727. Multi-fixture setup scheduler (V3/V7): `Setup` `[ComplexValueObject]` (Fixture, WCS datum, ReachableOps); a QuikGraph operation-precedence/datum-lineage graph partitions operations across reorientation setups (the NP-hard SEQUENCING half homes here, magazine eviction greedy stays); OWNS the setup→WCS assignment rows (setup k → the dialect's G54-G59.x/G54.1-Pn roster — posting RENDERS what setups assigns, never a posting-side default); admits op-N workholding against the op-N-1 machined `StockSnapshot` | `Fin<SetupSchedule> Setup.Schedule(Seq<Operation>, SchedulePolicy)` | out: QuikGraph (precedence graph), workholding (fixture reach), guard (per-op collision), family (`Machine.AxisCount` reach), owner#atoms (StockSnapshot, input-carried) · in: program (WCS rows), probing (per-setup datum), traveler (setup plan) | 3 |
| 16 | `Kinematics/cell.md` | MOVE (Toolpath/kinematics, ns Kinematics → Kinematics) → new; absorb {into: `Kinematics/cell.md`, from: `Toolpath/kinematics.md`} | Kinematics · `Rasm.Fabrication.Kinematics` · 2702 (namespace already `Kinematics` on disk `kinematics.md:37`). The Robots serial-chain cell HELD (per-manufacturer RAPID/KRL/URScript/VAL3/DRL posts, `Program` look-ahead, the `extern alias R3` boundary exemplar); band-2500→2700; `[03]-[RESEARCH]` tail (`kinematics.md:122`) purge; its jerk/accel planner NAMES the shared motion-dynamics law homed on `machine` | `Fin<FabricationResult.Motion> RobotProgram.Solve(RobotCell, Seq<Move>)` | out: Robots, Rhino3dm (`extern alias R3`), Riok.Mapperly (R3 seam), machine (motion-dynamics law) · in: motion (articulated-arm dispatch) | 3 |
| 17 | `Kinematics/machine.md` | NEW → new | Kinematics · `Rasm.Fabrication.Kinematics` · 2714. The 5-axis machine-tool topology owner (V5, closing the 5-axis gap beside the Robots cell): rotary-axis inverse + TCP/RTCP admission over the `KinematicClass` rotary-topology rows; multi-axis orientation interpolation COMPOSES the kernel one-slerp owner (`MotionInterpolation`), never a second slerp site; HOMES the ONE jerk/accel motion-dynamics law posting `Lookahead` reads as a typed policy shape (the Robots planner + posting planner read it, mapped at the R3 edge so they cannot drift) | `Fin<FabricationResult.Motion> MachineTool.Solve(MachineKinematics, Seq<Move>)` | out: kernel `MotionInterpolation` (slerp), family (KinematicClass rotary rows) · in: motion (multi-axis dispatch), cell/program (motion-dynamics law) | 3 |
| 18 | `Nesting/nfp.md` | KEEP + identity fix → improve | Nesting · `Rasm.Fabrication.Nesting` · 2701,2709. The NFP-feasibility true-shape nesting engine PRESERVED (minus E9); `Stock.Of()` hashes discriminant + ALL dimensions through `ContentHash.Of` (the area-only collision `nfp.md:88` poisoning `Remnant.Parent` dies); the 3 raw `XxHash128` mints (disk `:59,88,169`) route the ONE `ContentHash.Of` federation entry; `Ty*1e6+Tx` (`:137`) becomes a `(Ty,Tx)` tuple comparison; the NFP origin-vs-`Anchor` reference-frame incoherence PROVEN or fixed on the golden fixture; the rect-fastpath re-homes `RectpackSharp` → `RectangleBinPack.CSharp` `MaxRectsBinPack` heuristic-sweep | `Fin<FabricationResult.Placement> Nest.Solve(NestPolicy, FabricationInput)`; `Nest.Honor(NestPlan)` | out: algebra (Minkowski), `ContentHash.Of` (identity), RectangleBinPack (fastpath), owner#atoms (Placement/Remnant, reads input.Plan) · in: owner#run · cross-pkg: → `Rasm.Persistence` (Placement/Remnant content key via `ContentHash.Of` — blocked demand on `[ARTIFACT_CONTENT_KEY_FEDERATION]`, `ARCH:54` off deleted Schema); ← `Rasm.Compute` DRL nest-policy score (injected delegate, strata-correct) | 3 |
| 19 | `Nesting/stock.md` | KEEP → improve | Nesting · `Rasm.Fabrication.Nesting` · 2710. The rectangular cutting-stock YIELD engine HELD (the scalar exemplar); the `Nesting/stock ← Rasm.Element/MaterialId` ledger row ADDED (wired-undeclared, E11); `RectangleBinPack.CSharp` central pin re-group from the vacated Materials label VERIFIED; the `[05]-[RESEARCH]` tail (`stock.md:317`) purges | `Fin<NestPlan> StockNest.Pack(NestStrategy, Seq<CutPart>)` | out: RectangleBinPack, algebra, `Rasm.Element/MaterialId` (NEW ledger row), owner#atoms · in: nfp (NestPlan via input.Plan) · cross-pkg: → `Rasm.Compute` `NestYield.WasteAreaMm2` (`ARCH:51` — a ONE-SIDED forward demand, "recorded next-campaign counterpart" on disk; `CBRIEF [V12]` REFUTED, `[00]` ruling 3) | 3 |
| 20 | `Spec/tolerance.md` | NEW → new (cross-folder, staged early: surface's Ra→scallop reads its rows within-wave) | Spec · `Rasm.Fabrication.Spec` · 2721. The typed GD&T vocabulary author-kernel (V8 — NO admissible OSS library exists): ISO 286 fits (grade/deviation tables as GENERATED rows, never enumerated), ISO 1101/ASME Y14.5 feature-control frames + datum systems, ISO 1302 surface texture (Ra/Rz + lay + process marks), MMC/LMC modifiers — QIF-aligned (ISO 23952); the spec DRIVES process: the Ra/Rz→cutter-corner-radius→constant-scallop stepover/stepdown derivation row (`surface` consumes), the ISO 286 IT-grade→finishing-allowance row (`[V5]` pass planning conditions) | `Tolerance.Frame(FeatureControl)`; `double Tolerance.ScallopStep(RaTarget, CutterForm)`; `double Tolerance.Allowance(ItGrade)` | out: UnitsNet (extended Force/Power/Temp/Angle/Torque), owner#atoms (CutterForm) · in: surface (Ra→scallop), motion (IT→allowance), capability (grade), traveler (spec rows) | 3 |
| 21 | `Toolpath/motion.md` | REBUILD → rebuild | Toolpath · `Rasm.Fabrication.Toolpath` · 2705. Real `(RemovalModality, CutStrategy)` generators (V5 — every stub arm becomes a REAL operation): `Turn` a true ZX radial sweep over a revolved stock envelope (the `BarStock` phantom lands as a real shape whose floor composes the kernel `Analysis/measure.md` `Bounds` enclosing-cylinder fit), `helical` a distinct thread/ramp (the `Turn` alias `motion.md:73` dies), `Peck` real canned-cycle geometry with dwell/retract, `SliceWalk` a real perimeter/infill/travel orderer; rest-machining reads the input-carried `ResidualStock` (never a motion→removal page edge); `Guard.Check` + `Workholding.Condition` WIRED per feed move; adaptive engagement reads the medial clearance field AND the physics chip-load budget on ONE policy row; the Cam fold produces the conditioned `Motion` result (`[00]` ruling 5 — the `CutProgram` AST is `program`'s, assembled from this `Motion`); the `Option<ArcCenter>` chord feeds the arc-native posting rail (not biarc refit) | `Fin<FabricationResult> Cam.Solve(FabricationPolicy.Cam, FabricationInput)`; `Cam.Generate((RemovalModality, CutStrategy))` (assembles the conditioned `Motion` inside this fold — Guard/Workholding/Magazine execute here) | out: guard (per-move Check), workholding (Condition), cell/machine (dispatch), skeleton (Adaptive clearance), family (Admits), physics (budget), arcs (adaptive arc), algebra, surface (surface arms), partition (partition rows) · in: owner#run | 3 |
| 22 | `Toolpath/surface.md` | NEW → new (OpenCAMLib FEASIBLE) | Toolpath · `Rasm.Fabrication.Toolpath` · 2713. The 3D-surface finishing family (V5): OpenCAMLib owns cutter POSITIONING (drop/push/waterline/adaptive-waterline height sampling) via the thin `extern "C"` C-shim + source-generated `[LibraryImport]` P/Invoke over the shipped SHARED `libocl` (ALC-firebreak/sidecar posture, content-keyed at the wire; author-kernel SDF drop-cutter K8 is the recorded admission-abandonment fallback); path LAYOUT composes the kernel on-mesh machinery — geodesic-parallel/constant-stepover read the kernel heat distance (`geodesics.md` via `fields.md` `Geodesic`) as `extract.md` `ContourPolicy.MeshScalar` isolines, flowline/morph trace `flow.md` streamlines, `segment.md` cross-fields seed flank/swarf orientation (a Fabrication-side on-mesh re-implementation is the `[V2]` defect); waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family rows; the `CutterForm` axis drives OCL cutter selection at the P/Invoke edge | `Fin<Seq<Move>> SurfacePath.Sample(SurfaceStrategy, MeshSpace, CutterForm)` | out: OpenCAMLib (P/Invoke), kernel `geodesics.md`/`extract.md`/`flow.md`/`segment.md` (path layout, K10-K13), kernel `fields.md` SDF (fallback, K8), tolerance (Ra→scallop), owner#atoms (CutterForm), physics (chip-load), System.Numerics.Tensors (SIMD grids) · in: motion (surface arms) | 3 |
| 23 | `Toolpath/partition.md` | NEW → new (SharpVoronoiLib mandate) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705,2723. The Voronoi region-decomposition CAM lane (V5, the `[V1]` realization the kernel supersession left this brief): `VoronoiPlane` Fortune sites + Lloyd relaxation (`Relax(iterations, strength, reTessellate)`, `VoronoiSite.Centroid` the seed) → pocket-region seeds, Lloyd-relaxed stipple/engrave/even-spacing/pen-plot strategy rows; point-site only (polygon medial stays the kernel's); the `extern alias Voronoi` posture (disk `csproj:24`) carried | `Fin<Seq<Loop>> Partition.Seed(PartitionStrategy, Loop)` | out: SharpVoronoiLib (`extern alias Voronoi`), algebra · in: motion (partition strategy rows) | 3 |
| 24 | `Additive/slicing.md` | MOVE (Toolpath, ns Additive → Additive) + kernel-consumer rebuild → new; absorb {into: `Additive/slicing.md`, from: `Toolpath/slicing.md`} | Additive · `Rasm.Fabrication.Additive` · 2708 (namespace already `Additive` on disk `slicing.md:35`). Kernel slice-stack CONSUMER (`[V2]b`, the `ARCH:48` "when realized" promise FIRED): the `Section`/O(n²) `Chain` author-kernel DIES; keeps the FFF/DED `InfillPattern` + shell composition over the kernel-emitted oriented contours (enriched from gsSlicer prior art — open-sheet, overlapping shells); gyroid/TPMS route to the PicoGK voxel lane (the `InfillPattern`-row false collapse corrects); variable/adaptive layer height as kernel slice-stack POLICY rows (Fabrication's named demand, never a Fabrication-side layer engine); the false author-forever seal + `[03]-[RESEARCH]` tail (`slicing.md:219`) purge | `Fin<AdditiveResult> Slice.Layers(SliceStack, InfillPolicy)` | out: kernel `Slicing.Apply` → `Fin<SliceStack>` (4 layer policies + 5-channel SoA forest, K3), algebra, implicit (gyroid/TPMS route) · in: owner#run (Additive case), production | 3 |
| 25 | `Additive/implicit.md` | NEW → new (PicoGK, THREE lanes) | Additive · `Rasm.Fabrication.Additive` · 2715. PicoGK to depth (V6/V7): `IImplicit → Voxels` TPMS/gyroid/cellular conformal infill (the quartet cannot express TPMS, `api-picogk.md:152`); `Lattice` beam supports + overhang-conditioned scaffolds; SLA/DLP/MSLA grayscale + `.cli` vector layer stacks (`oVectorize → CliIo`, `Vdb2Cli`); voxel offset/shell lightweighting; DECLARES ONCE the net9+native ALC-firebreak/sidecar posture + the content-keyed mesh↔`Voxels` wire (removal composes this declared seam, never a second posture); the `Voxels` stay plane-local (never on a result case, `[00]` ruling 5) | `Fin<Voxels> Implicit.Voxelize(ImplicitOp)`; `CliStack Implicit.Cli(Voxels)` | out: PicoGK (`IImplicit`/`Voxels`/`Lattice`/`CliIo`), kernel mesh↔Voxels wire, `ContentHash.Of` (`.cli`/grayscale keys) · in: slicing (gyroid/TPMS), production (lattice/support), removal (declared voxel seam) | 3 |
| 26 | `Additive/production.md` | NEW → new (lib3mf) | Additive · `Rasm.Fabrication.Additive` · 2716,2725. Build-orientation + 3MF egress (V6): author-kernel orientation optimization on the overhang/bottom-area/contour objective (the overhang/bottom terms read the kernel axis-ranked face decomposition + draft capture, `Analysis/select.md` `Faces`/`Curves.Draft` per candidate orientation, never a hand-rolled normal classifier); machine profiles (build volume, nozzle/vat/laser, kinematics class as rows); lib3mf 3MF core/production/beam-lattice writer via the vendored official C# binding + RID-keyed native asset (PicoGK `Lattice` maps directly to the beam-lattice extension; STL-implied hand-off dies) | `Fin<AdditiveResult> Production.Plan(BuildPolicy, MeshSpace)` | out: lib3mf (3MF), implicit (lattice/support), slicing (planar layer stacks — the counterpart of row 24's `production` in-edge), kernel `Analysis/select.md` (orientation faces, K20), `ContentHash.Of` (3MF key) · in: owner#run (Additive case) | 3 |

### Wave 4 / Leg 4 — EGRESS + TRUTH + DOCS

| # | Path | Action → lowering | Charter | Entry (+case) | Seams | W |
|---|---|---|---|---|---|---|
| 27 | `Posting/dialect.md` | NEW → new | Posting · `Rasm.Fabrication.Posting` · 2718. The per-dialect emission generator (V4, data-vs-lowering split — the axis is family's, the FOLDS are here; the byte-lowering half of the `Run(Post)` case): `Emit` a generated total `Switch` over the `PostDialect` GRAMMAR family (`WordAddress` RS-274 \| `Conversational` Klartext/Mazatrol/OSP \| `Additive` Marlin/RepRap + resin/powder layer-stack); canned-cycle expansion (G81-G89/G76 single blocks where admitted, expanded moves where not); macro/subprogram lowering (Fanuc macro-B `#`/WHILE/GOTO, M98/M99, Siemens R-params, Heidenhain Q); code-override resolution; the NC1 steel emit TARGET (the DSTV record tree as emission model, mirroring `Ingress/steel`); rows widen as seed DATA (Siemens 840D, Heidenhain TNC, Okuma OSP, Fagor, Centroid) | `GWord Dialect.Emit(PostDialect, GNode)` (generated total Switch; invoked by the `Run(Post{Motion, PostDialect})` case, never a public fold) | out: family (PostDialect grammar columns), steel (NC1 emit model) · in: program (per-family lowering), owner#run (Post case byte-lowering) | 4 |
| 28 | `Verify/removal.md` | NEW → new (PicoGK NC-verify) | Verify · `Rasm.Fabrication.Verify` · 2706 (verify-time Gouge). Voxel material-removal verification (V7): stock `Voxels` minus the accumulated swept-tool volume via PicoGK `BoolSubtract` (a ball-nose sweep IS the capsule beam); emits typed gouge/uncut/overcut/air-cut RECEIPTS against the ACTUAL removed-stock state (owner#atoms-safe payloads, `[00]` ruling 5); PRODUCES the `ResidualStock` field (`[V5]` rest rows read it, input-carried) + content-keyed per-SETUP intermediate `StockSnapshot`s (never one terminal state — `Fixturing/setups` admits op-N against op-N-1, `guard` reads the current snapshot); composes the declared `Additive/implicit` ALC/voxel seam, never a second posture | `Fin<FabricationResult> Removal.Verify(VerifyPolicy, FabricationInput)` — the `Verify` policy case | out: implicit (declared voxel seam, BoolSubtract), owner#atoms (ResidualStock/StockSnapshot mint, CutterForm swept) · in: owner#run (Verify case) · cross-pkg: → `Rasm.Persistence` (snapshot content key via `ContentHash.Of`, blocked-demand) | 4 |
| 29 | `Spec/capability.md` | NEW → new | Spec · `Rasm.Fabrication.Spec` · 2722,2729. Process capability (V8): Cp/Cpk/Pp/Ppk with the streaming-moment half on the kernel `Domain/stats.md` owners (`Stat.Of` Welford, `Distribution.Of` quantiles, `StatContext.Tolerance` provenance — probing evidence arrives PRE-FOLDED, a re-derived Welford is the dual-paradigm defect) and the distribution-fit + Monte-Carlo tolerance-stackup half over `MathNet.Numerics` (first Fabrication consumer); SPC control-limit rows; the plan-time Cpk GATE (a process row whose Cpk history cannot hold the demanded grade fails admission BEFORE material is cut) PRODUCES the `CapabilityVerdict` minted on `owner#atoms` (the plan reads it input-carried, closing spec→plan→verify→capability; the full `CapabilityReport` stays terminal here) | `Fin<CapabilityReport> Capability.Assess(Seq<ResidualSample>, Tolerance)`; `CapabilityVerdict Capability.Gate(ProcessKind, ItGrade)` | out: kernel `Domain/stats.md` (Stat/Distribution, K18), MathNet (fit/Monte-Carlo), tolerance (grade), NodaTime (SPC stamps), owner#atoms (CapabilityVerdict mint) · in: owner#run (Inspect projection), traveler (capability rows), owner#atoms plan-admission (input-carried); reads probing measured ingress (input-carried) | 4 |
| 30 | `Posting/program.md` | REBUILD → rebuild | Posting · `Rasm.Fabrication.Posting` · 2703,2719,2728. AST growth + content key (V3/V4): the flat `Seq<GWord>` block stream gains canned-cycle nodes with typed R/Q/P slots, subprogram units with repeat counts, macro parameter slots (per-family lowering is node expansion via `dialect.Emit`, never string synthesis); the cut-conditioning author-kernel MATH PRESERVED (`Kerf`/`Simplify`/`Compensate`/`Feedrate`/`Lookahead`, plus `Pierce`/`Tab` for the `Placement` 2D-cut egress — lead-in/pierce-point placement + ordering and retained micro-tab generation, the profile-cutting laser/plasma/waterjet/nest conditioning the F1 word-address/NC1 and the `Toolpath/partition` engrave lane feed; the Placement-vs-Motion asymmetry forbids a `Kerf` pass over the already-conditioned `Motion`, `[00]` ruling 5 / V3); the arc rail REALIZES — kerf/lead/adaptive read `arcs` arc-native offsets + `Loop.Bulges` (`g3.BiArcFit2` `program.md:268` retained ONLY for genuinely line-sourced kernel mesh-section chains until the owned Bolton biarc fold lands); CONSUMES `Magazine.Schedule` (real `Tnn`/`M6`/`G43`), `Workholding` conditioning, `Setups` WCS assignment; the neutral `CutProgram` AST is assembled HERE in the `Run(Post)` case body from the `owner#atoms` `Motion` result (`[00]` ruling 5 — NOT in the Cam fold; `motion`↔`program` decouple through the `Motion` type); `CutProgram` gains its content key via `ContentHash.Of` + its `ArtifactKind` discriminant; a `Parse` round-trip arm carries the NIST RS274NGC modal-group state machine — the five tracked groups (motion · plane · distance · units · feed), the state the `ModalGroup` payload (`ProgramParse` 2719) discriminates over and the round-trip must preserve, without which round-trip is unimplementable (`Parse ∘ Emit = id` on each dialect's admitted subset); the `[03]-[RESEARCH]` tail (`program.md:379`) purges | `Run(Post{FabricationResult.Motion, PostDialect}) → PostedProgram` (the case body: assemble the neutral `CutProgram` AST from the `Motion`, condition, lower via `dialect.Emit`; `PostedProgram` carries lowered blocks + `ArtifactKind` + `ContentKey`, never the AST); `Fin<CutProgram> Post.Parse(string, PostDialect)` (ingress arm) | out: dialect (Emit lowering), magazine (Schedule), workholding (conditioning), setups (WCS), arcs (arc-native kerf/lead), algebra (NestingOrder), geometry3Sharp (line-sourced biarc only), owner#atoms (Move, Motion result, ArtifactKind), `ContentHash.Of` (program key) · in: owner#run (Post case), traveler (program facts) · cross-pkg: → `Rasm.Persistence` (CutProgram durable rows, `ARCH:53` off deleted Schema — blocked-demand on `[ARTIFACT_CONTENT_KEY_FEDERATION]`); → kernel `Drawing/pack.md` `toolpath` `PackKind` (committed motion residency, K7, a ledger row never a Fabrication encoder) | 4 |
| 31 | `Verify/probing.md` | NEW → new (PROBING realizes) | Verify · `Rasm.Fabrication.Verify` · 2720. In-process metrology (V7): touch-probe cycle vocabulary (G31/G38 rows on the posting AST), work-offset/tool-length metrology, measured-feature receipts; datum best-fit COMPOSES the kernel `Processing/register.md` `AlignKind` ICP dispatcher under one `AlignmentPolicy` (probed points → nominal, never a hand-rolled least-squares); measured-vs-nominal folds through the kernel `Analysis/measure.md` `ConformanceMetric` over `ResidualSample`; QIF (ISO 23952) MeasurementResults feed `Spec/capability`; measured ingress is HONEST — probe values enter through the same AppHost decoded-telemetry seam or typed manual-entry rows (the `(d)` dry-run composes on fixture-synthetic measurements) | `Fin<FabricationResult> Probe.Inspect(InspectPolicy, FabricationInput)` — the `Inspect` policy case | out: kernel `register.md` (ICP, K16), kernel `measure.md` (ConformanceMetric, K17), program (G38 rows), setups (per-setup datum), NodaTime (receipt stamps) · in: owner#run (Inspect case), capability (measured ingress) · cross-pkg: ← `Rasm.AppHost` decoded probe telemetry (`APPHOST:210`, second consumer) | 4 |
| 32 | `Documentation/projection.md` | MOVE (Posting, ns Projection → Documentation) + kernel-consumer rebuild → new; absorb {into: `Documentation/projection.md`, from: `Posting/projection.md`} | Documentation · `Rasm.Fabrication.Documentation` · kernel-routed. Kernel `DrawingProjection` CONSUMER (`[V2]c`/`[V3]`, RULED): the BSP interior (centroid-classified facets, per-edge average depth, undeduped mesh-edge soup, `1e6` eye) DIES; the PHANTOM `Predicate.Orient2D` import drops (0 fence calls, the page's own forbidden defect); KEEPS the `BooleanSolid` watertight arm (kernel `Arrangement.Apply`/`ToMesh` compose-seam); PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at (`UIBRIEF [V7]`(b) — corrected from the brief's `[V6]`) | `Fin<FabricationResult> Hlr.Solve(FabricationPolicy.HiddenLine, FabricationInput)` | out: kernel `Drawing/view.md` `View.Apply` → `Fin<DrawingProjection>` (K6), kernel `Meshing/arrangement.md` `Arrangement.Apply`/`ToMesh` (watertight — `ARCH:43` held, `ARCH:44` re-points off `Processing/repair`; K5), owner#atoms (Loop) · in: owner#run (HiddenLine case), traveler (views) · cross-pkg: → `Rasm.AppUi/Render` (`HiddenLineResult`, `ARCH:47`, supersession insulated at the receipt) | 4 |
| 33 | `Documentation/traveler.md` | NEW → new | Documentation · `Rasm.Fabrication.Documentation` · reserved. Setup sheets + shop travelers (V8): content-keyed shop documents composing the projection views, magazine tool lists, workholding/setups plans, program facts, and spec rows (the typed document MODEL only — sheet/annotation RENDERING rides the `[06]` artifacts-plane seam); the widest fan-in node (validates every upstream owner exposes a typed receipt). `Traveler.Assemble` folds into the entry as the `Run(Document{Seq<FabricationResult>})` case (a re-invocable terminal transform over a completed result-set, NOT a second public fold) | `Run(Document{Seq<FabricationResult>}) → TravelerDocument` (the case body: compose views/tool-lists/fixture-plans/program-facts/spec-rows into the content-keyed document model) | out: projection (views), magazine (tool lists), workholding/setups (fixture plans), program (facts), tolerance/capability (spec rows), NodaTime (stamps), owner#atoms (ArtifactKind), `ContentHash.Of` (doc key) · in: owner#run (Document case) · cross-pkg: → `Rasm.Persistence` (traveler durable rows, blocked-demand) | 4 |

## [03]-[ENGINE_EXECUTION_MANIFEST]

- **Kind census:** new ×23 (rows 5-11, 14-17, 20, 22-29, 31-33 — incl. the 7 move/split destinations 5,7,9,14,16,24,32) · rebuild ×7 (owner 1, family 2, physics 3, faults 4, skeleton 12, motion 21, program 30) · improve ×3 (guard 13, nfp 18, stock 19).
- **deletePages EMPTY.** Every deleted source is absorbed by an in-set destination, so its delete travels ON the absorb (the absorbing agent moves the content and deletes the source); a deletePages row for an in-set-absorbed source is the double-delete defect and never appears. The 7 sources leaving disk: `Process/magazine.md`, `Polygon/clipper.md`, `Polygon/import.md`, `Toolpath/kinematics.md`, `Toolpath/slicing.md`, `Nesting/workholding.md`, `Posting/projection.md`. `Polygon/` deletes (both pages leave).
- **absorb pairs ×7, one absorber per source:** {into `Tooling/magazine.md`, from `Process/magazine.md`} · {into `Geometry2D/algebra.md`, from `Polygon/clipper.md`} (whole-file custody — the ONE clipper absorber-deleter) · {into `Ingress/profile.md`, from `Polygon/import.md`} · {into `Kinematics/cell.md`, from `Toolpath/kinematics.md`} · {into `Additive/slicing.md`, from `Toolpath/slicing.md`} · {into `Fixturing/workholding.md`, from `Nesting/workholding.md`} · {into `Documentation/projection.md`, from `Posting/projection.md`}. The clipper SPLIT carries NO second absorb: `Geometry2D/arcs.md` is plain new whose content source (`clipper.md:182-271` arc-space) is standalone-inventoried on row 8 — one absorber, one delete, no race.
- **Donor-block excision WITHOUT source deletion** (charter-carried, never absorb-encoded): `physics.md` (rebuild, row 3) → `Tooling/cuttingdata.md` (new, row 6) — the `CuttingData` `SFM`/chip-load table (disk `physics.md:117`) excises; `physics` keeps the identity map, `cuttingdata` deepens it to the Kienzle `kc` model.
- **`.api` motions ride the wave-1 roster manifest, not deletePages:** `api-opencamlib.md` + `api-lib3mf.md` authored with the ADD (wave 1); `api-rectpacksharp.md` deletes with the REMOVE (wave 1); `api-hashing.md` slims to the thin overlay (wave 1). The leg that first COMPOSES an admitted package deepens its stub.
- **Two-touch pages explicit:** owner {W1 atoms/policy growth} + the `Additive`/`Verify`/`Inspect`/`Post`/`Document` compose-seam arms realize in the leg landing each owning plane (W3/W4) — the `owner#run` dispatch arms fill incrementally so no arm references an unbuilt plane. Mechanism: `Process/owner.md` is a leg-1 target ONLY; the W3/W4 arm-fill edits land as recorded leg residuals closed by the engine's reconcile pass (or `resolve-residuals`), never a second `owner.md` rebuild target in a later leg. Every other page is single-touch.

## [04]-[FAULT_REGISTRY] — one 2700-century block per folder (frozen 2701-2710 + folder-grouped growth 2711-2729)

`FabricationFault` stays ONE `[Union]` on `FaultBand.Fabrication = 2700`. Layer 1 (frozen 2701-2710) retains wire codes per the `.archive` build-ON law + the held-open persisted-decode constraint; every legacy arm RETYPES its `string Detail` to a typed payload. Layer 2 (2711-2729) allocates folder-grouped across two growth tiers (first tier 2711-2722, second tier 2723-2729 — the distinct-mechanism arms split off broader arms), sized by the growth law. Folder = cluster is mechanically checkable within each tier (a code maps to exactly one folder; each folder's arms are a contiguous run per tier); the non-contiguous legacy codes are the recorded frozen exception. Disk-confirmed against `faults.md:39-61`: NoFit→Nesting (2701), Unreachable→Kinematics (2702), KerfCollision→Geometry2D (2703), OpenLoop→Process cross-cutting (2704), InadmissiblePair→Toolpath (2705) — any "2701-2704 as Process arms" re-attribution is REFUTED on disk.

| Folder | Block | Arms (code · typed payload) |
|---|---|---|
| Process | 2700 owner + cross-cut | `OpenLoop` 2704 `(FabConcern concern, int primitive)` — the ONE cross-cutting fabrication-contract arm (disk producers motion·nfp·skeleton·program, `faults.md:14`), recorded exception |
| Nesting | 2701, 2709, 2710 (legacy) | `NoFit` 2701 `(int part, Seq<double> triedRotations)` · `StockOverflow` 2709 `(int unplaced, int sheets)` · `Nest` 2710 `(NestFault kind, int part)` |
| Kinematics | 2702, 2714 | `Unreachable` 2702 `(JointDiagnostic diag, int target)` · `AxisSingularity` 2714 `(RotaryAxis axis, double angle)` NEW — RTCP/rotary singularity distinct from reach |
| Geometry2D | 2703 | `KerfCollision` 2703 `(Point3d at, double kerf)` — re-attributed to the kerf-compensation owner (`arcs`), off Posting |
| Toolpath | 2705-2707, 2713, 2723 | `InadmissiblePair` 2705 `((RemovalModality, CutStrategy) pair)` · `Gouge` 2706 `(Point3d point, CutterForm tool)` · `Collision` 2707 `(ExclusionZone zone)` · `SampleStalled` 2713 `(SurfaceStrategy strategy, int iteration)` NEW — OpenCAMLib drop-cutter non-convergence · `PartitionDegenerate` 2723 `(PartitionStrategy strategy, int sites)` NEW — the `Toolpath/partition` (row 23) SharpVoronoiLib Fortune/Lloyd tessellation degenerates/empties (collinear sites, coincident points); distinct from `SampleStalled`'s OCL surface-sampling stall |
| Additive | 2708, 2715, 2716, 2725 | `NonManifoldSlice` 2708 `(int layer, int openChains)` · `VoxelFault` 2715 `(ImplicitOp op, VoxelBudget budget)` NEW — `budget` = bbox + resolution cap, a typed carrier (the retype-to-typed payload law binds Layer-2 growth arms, not only the frozen legacy arms) · `OrientationInfeasible` 2716 `(int overhangs, double bestScore)` NEW · `Unsupported3mfExtension` 2725 `(ThreeMfExtension extension, ArtifactKind target)` NEW — the `Additive/production` (row 26) lib3mf writer cannot express a required extension (beam-lattice/production) for the target profile; a native egress fault distinct from the `ImplicitOp`-scoped `VoxelFault` and the orientation arm (a raw lib3mf write error lifts to this arm at the boundary) |
| Ingress | 2711 | `IngressTranslation` 2711 `(SourceKind kind, SourceLocus locus)` NEW — the ONE polymorphic ingress fault (profile/solid/steel discriminated by `kind`); `locus` is a discriminated union over the three source sites (DXF entity handle · OCCT shape id · DSTV block+line), so the fold stays single-arm with a typed carrier, never the weak-string escape hatch the retype-to-typed payload law forbids on Layer-2 arms too |
| Tooling | 2712, 2724 | `MachinabilityUnknown` 2712 `(Material material, Operation op)` NEW — no cutting-data row and no admitted formula fallback · `NoToolForOp` 2724 `(Operation op, CutterForm required)` NEW — the `Tooling/magazine` (row 5) `Schedule` fold exhausts the assembly set with no form/diameter match for the operation; a SCHEDULING failure orthogonal to `MachinabilityUnknown`'s missing-`kc`-DATA failure |
| Fixturing | 2717, 2726, 2727 | `SetupInfeasible` 2717 `(int operation, int triedSetups)` NEW — no feasible setup assignment / operation unreachable in every fixture · `DatumLineageBroken` 2726 `(SetupChain chain)` NEW — the `Fixturing/setups` (row 15) QuikGraph datum-lineage graph is cyclic or references an un-machined/un-datumed feature; the graph's acyclicity/reachability check is its producer, a PRECEDENCE failure distinct from `SetupInfeasible`'s kinematic-reach infeasibility · `ClampOnMachinedFace` 2727 `(int operation, Point3d at)` NEW — the op-N workholding admits against the op-N-1 machined `StockSnapshot` (rows 15,28) and the clamp lands on material already cut away; the typed verdict of the snapshot keep-out (brief V3/V7 "the plan-time defect the snapshot catches"), a SAFETY/scrap fault distinct from feasibility and from the per-move `Collision` 2707 |
| Posting | 2718, 2719, 2728 | `DialectUnsupported` 2718 `(PostDialect dialect, GNodeKind node)` NEW — a required cycle/macro the grammar cannot express or degrade · `ProgramParse` 2719 `(int line, ModalGroup group)` NEW — G-code ingress modal-group violation · `BlockCapExceeded` 2728 `(PostDialect dialect, int blocks, int cap)` NEW — the lowered program overruns the dialect's `block cap` capability column (family row 2); SPLIT off `DialectUnsupported` because a program-total-SIZE overflow carries no offending `GNodeKind` — forcing one is the weak-payload defect the typed-payload law forbids (F1 routes block-cap HERE) |
| Verify | 2720 (+2706 verify-time) | `ProbeOvertravel` 2720 `(Point3d at, double limit)` NEW — the PROBING card's fault; a detected verify-time gouge routes the shared `Gouge` 2706 |
| Spec | 2721, 2722, 2729 | `ToleranceUnsatisfiable` 2721 `(FeatureControl frame, double achievable)` NEW · `CapabilityShortfall` 2722 `(ProcessKind process, double cpk, double demanded)` NEW — the plan-time Cpk gate (the `CapabilityVerdict` fail arm) · `StackupExceeded` 2729 `(ToleranceChain chain, double accumulated, double bound)` NEW — the `Spec/capability` (row 29) Monte-Carlo tolerance-stackup accumulates past the assembly bound though each feature is achievable and each process capable; an ASSEMBLY-level accumulation failure orthogonal to the per-feature `ToleranceUnsatisfiable` and the per-process `CapabilityShortfall` |
| Documentation | reserved (empty) | projection routes the kernel `Rasm.Geometry.Projection` `ProjectionFault`; traveler is content-keyed assembly with no fault producer |

Growth law: the next free offset is `+30` (2730); a new concern is one arm on its folder's block, never a new band. `Code` derives `FaultBand.Fabrication + n`; message strings for 2701-2710 UNCHANGED. Federation re-plan NOT needed — 29 arms fit 2701-2729 with 2730-2799 headroom.

## [05]-[SEAM_LEDGER] — both directions, one anchor per seam, counterpart obligations, stale re-points

**Intra-package (arrow = composes).** The compose graph is a DAG; every cross-WAVE edge lands equal-or-earlier wave (leg N never depends on leg N+1, guaranteed by the two-touch `owner#run` mechanism — dispatch arms realize in the plane's own leg). Intra-leg order is ADVISORY (the rebuild engine builds a leg's pages with full cross-visibility). The WIRED-PIPELINE law realized (E2 killed): `owner#run → Cam` (W3); `Cam → {guard.Check per feed move, workholding.Condition, magazine.Schedule consult}` producing the conditioned `Motion` (W3); `Run(Post{Motion, PostDialect}) → {program AST-assembly + condition + dialect.Emit, magazine.Schedule, workholding conditioning, setups WCS}` (W4, program reads the `owner#atoms` `Motion` — `[00]` ruling 5, NO `motion→program` edge); `Run(Document{results}) → traveler → {projection, program, magazine, setups, tolerance, capability}` (W4, backward); `workholding.Condition → algebra.NestingOrder` (W3→W2 — the re-home that BREAKS the workholding→posting cross-wave edge); `guard → {skeleton.ClearanceAt, magazine.HolderEnvelope, workholding.ExclusionZone}` (W3→W2/W1/W3); `motion.Adaptive → skeleton` (W3→W2); `setups → {workholding, guard}` (W3); `removal → implicit` (W4→W3, the declared voxel seam); `probing → program` (W4); `capability.Gate → owner#atoms(CapabilityVerdict)` (W4 mint, read input-carried at plan).

Owner two-node discipline HOLDS: `owner#atoms` (Loop+Bulges, Edge3, ArcCenter, Move, PartTransform, ProjectionDir, CutterForm, ResidualStock, StockSnapshot, CapabilityVerdict, ArtifactKind, FabricationInput/Policy/Result, ContentKey seed) is upstream-only — every plane reads it, it reads only family's leaf axes + kernel; `owner#run` is terminal (8-arm dispatch: Cam·HiddenLine·Nest·Additive·Verify·Inspect·Post·Document) — composes plane kernels, nothing composes it; the flow is `owner#run → planes → owner#atoms → family(leaf)`, linear and acyclic, so the physical page never splits. **FIVE cycle-breaks:** `ProjectionDir` on owner#atoms breaks the former `owner↔projection` (disk `owner.md:30,70` imports it downstream); `ResidualStock` input-carry breaks `motion→removal→program→motion`; `CutterForm` on owner#atoms breaks `owner→cuttingdata→magazine→owner` (`magazine.md:108` `HolderEnvelope→Loop` anchor, `[00]` ruling 2); `NestingOrder` re-home to `Geometry2D/algebra` breaks `workholding→posting` (a wave3⇄wave4 cross-wave cycle otherwise); `CapabilityVerdict` input-carry breaks `capability→plan` (produced W4, read as next-run input, identical to `ResidualStock`). The `[00]` ruling-5 result-payload discipline is the SIXTH latent-cycle guard: no `FabricationResult` egress case carries a plane-internal type, so `owner#atoms` never gains a forward edge to any plane (the disk-true `motion`-produces-`Motion` / `program`-reads-`Motion` decoupling is preserved). Full acyclicity walk in `[11]`.

**Signature-lock — kernel seam member spellings, K1-K23 TABLE (from the LANDED `RASM-CS-GEOMETRY-DECISION.md`; lock against the DECISION `FAB:NN`/row anchors, NOT the `+14/+16`-drifted GBRIEF line pins).** Each page signature-locks these at its leg-1 pass and NEVER re-derives the interior; every entry threads the kernel `Op` value key with `Eff<Env>` carriage (K21), registers on the `IValidityEvidence`+`ValidityClaim` fold reaching `OpAcceptance` with zero oracle edits (K22), and rides `AtomProjection`/`ProjectionRow` output (K23). Leg-1 re-verifies each spelling via `uv run python -m tools.assay api` against the restored assembly (or the `.api` tier when blocked). K1/K2/K3/K5/K6/K7 spellings + namespaces + fault bands are geometry-DECISION-CONFIRMED (rows 8-10,17-18,27-28; cross-package ledger lines 123-127,222,266).

| # | Kernel member (locked signature) | Namespace · fault | Consuming page(s) | Ruled disposition carried | Leg |
|---|---|---|---|---|---|
| K1 | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Op? key = null)` — cases `Skeleton · Offset(JoinType×EndType corner rows) · Medial · Minkowski · Weighted · Clearance(probe)`; per-point clearance **RADIUS** first-class result field + `Clearance(Point3d probe)` arbitrary-probe query | `Rasm.Geometry.Offsetting` · 2416-2419 | `Geometry2D/arcs`, `Toolpath/skeleton`, `Toolpath/motion` | **[V10]a kerf split** — kernel owns exact corner-row assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours ON TOP (geometry DECISION line 149) | 2 |
| K2 | `Fin<CurveSkeleton> Skeletonize.Apply(SkeletonOp, Op? key = null)` — COMPOSES K1's clearance family (same RADIUS + `Clearance(Point3d probe)`, ONE result family across 2D/3D) | `Rasm.Geometry.Offsetting` · `SkeletonStalled 2417`/`CollapseStalled 2418` | `Toolpath/skeleton` (3D MCF curve-skeleton) | **[V10]d + [V2]a** — `Skeleton.cs` DIES UNCONDITIONALLY (geometry DECISION line 123); page keeps ONLY the trochoidal WALK over the kernel clearance field | 2 |
| K3 | `Fin<SliceStack> Slicing.Apply(SliceOp, Op? key = null)` — ALL FOUR layer policies OPEN (uniform · adaptive · variable-by-slope · support-interface); oriented contours; typed OPEN-chain rows; 5-channel SoA forest wire | `Rasm.Geometry.Intersection` · `SectionFault 2425` | `Additive/slicing` | **[V2]b** — `slicing.Section` + O(n²) `Chain` DIE; `ARCH:48` "when realized" REALIZED (geometry DECISION line 124); layer rows are kernel policy seeds, never a Fabrication layer engine | 3 |
| K4 | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Op? key = null)` — `IntersectOp.PlaneMesh` Section fold; oriented `Chain` | `Rasm.Geometry.Intersection` · 2424 | `Additive/slicing` (section primitive under K3) | consumed under K3 | 3 |
| K5 | `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp, Op? key = null)` + `BooleanOp` smart enum — `MeshBoolean · PlanarOverlay · CellComplex`; Manifold tier behind `ArrangementPolicy.ScaleCeiling` | `Rasm.Geometry.Arrangement` · 2420+2423 | `Documentation/projection` (watertight `BooleanSolid` arm) | **[V2]e** — `BooleanOp` anchor re-points `Processing/repair` → `Meshing/arrangement` (geometry DECISION lines 75, 126 — the `FAB:44` re-anchor; the Manifold `ScaleCeiling` gate rides line 266); `projection`'s `Rasm.Geometry.Healing` import re-points | 4 |
| K6 | `Fin<DrawingProjection> View.Apply(ViewOp, Op? key = null)` — `HiddenLine · Silhouette · Section · Creases`; exact analytic Appel QI (zero sampling) | `Rasm.Geometry.Projection` · 2436-2439 | `Documentation/projection` | **[V2]c / [V3]** — the BSP interior DIES; "beside the in-folder BSP solver" clause dies (geometry DECISION line 125); page PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at | 4 |
| K7 | `Fin<EncodedGeometry> Encode.Apply(PackOp, Op? key = null)` — `PackKind` gains **`toolpath`** case row + active-channel set | `Rasm.Geometry.Encoding` · 2444-2447 | `Posting/program` (committed motion residency) | **[V13]** — the channel-set alignment lands as ONE ledger row (Compute decodes residency, AppHost locks `EncodingKind.Toolpath`); NEVER a Fabrication-side encoder | 4 |
| K8 | `SignedDistanceFromMesh` (landed `fields.md` → `reconstruct.md` GWN → `index.md` BVH winding) — the ONE distance-field lane | `Rasm.Geometry` (`Analysis/fields`) | `Toolpath/surface` (OCL fallback) | **[V8]** — the kernel mints no second SDF owner; PicoGK stays Fabrication's VOXEL lane (geometry DECISION line 228), never a second kernel SDF owner | 3 |
| K9 | `ContentHash.Of` — seed-zero federation entry (`XxHash128.HashToUInt128` reached THROUGH it) | `Rasm` (`Domain/identity`) | ALL egress: `program`, `nfp`, `implicit`, `production`, `removal`, `traveler`, `magazine`, `stock` | **[SEAM_AND_ENTRY_LAW]** — the ONE mint site; every raw `XxHash128`/`GenerateHash` is the second-hasher defect; content keys keyed by the `ArtifactKind` discriminant, the durable-row fold a Fabrication-AUTHORED demand (`[00]` ruling 3) | 1 atoms, per-plane |
| K10 | `Processing/geodesics.md` heat distance (via `fields.md` `Geodesic` case) | `Rasm.Geometry` | `Toolpath/surface` | geodesic-parallel / constant-stepover path LAYOUT | 3 |
| K11 | `Processing/extract.md` `ContourPolicy.MeshScalar` isolines | `Rasm.Geometry` | `Toolpath/surface` | pass extraction | 3 |
| K12 | `Processing/flow.md` streamline over `VectorField` | `Rasm.Geometry` | `Toolpath/surface` | flowline / morph rows | 3 |
| K13 | `Processing/segment.md` cross-field / stripe | `Rasm.Geometry` | `Toolpath/surface` | flank / swarf direction alignment | 3 |
| K14 | `Analysis/measure.md` `Bounds` enclosing-cylinder + `Meshing/delaunay.md` `LowerHull` + `cloud.md` rail | `Rasm.Geometry` | `Toolpath/motion` | `Turn` revolved stock envelope + clearing-stock hull (`[V11]` two-tier) | 3 |
| K15 | `Meshing/mesh.md` `MeshSpace` admission + `Processing/repair.md` `HealOp` (predicate-gated) | `Rasm.Geometry` | `Ingress/solid` | `OcctMesh` crosses to kernel vocabulary; dirty STL healed at the seam | 2 |
| K16 | `Processing/register.md` `AlignKind` ICP dispatcher (one `AlignmentPolicy`) | `Rasm.Geometry` | `Verify/probing` | probe-to-nominal datum best-fit | 4 |
| K17 | `Analysis/measure.md` `ConformanceMetric` over `ResidualSample` | `Rasm.Geometry` | `Verify/probing`, `Spec/capability` | measured-vs-nominal verdicts, pre-folded evidence | 4 |
| K18 | `Domain/stats.md` `Stat.Of` (Welford) / `Distribution.Of` (quantiles) / `StatContext.Tolerance` | `Rasm` | `Spec/capability` | streaming-moment half of Cp/Cpk/SPC (MathNet owns fit/Monte-Carlo half) | 4 |
| K19 | `Parametric/projections.md` `MotionInterpolation` (one-slerp owner) | `Rasm.Geometry` | `Kinematics/machine` | multi-axis orientation interpolation — never a second slerp site | 3 |
| K20 | `Analysis/select.md` `Faces` / `Curves.Draft` axis-ranked face decomposition | `Rasm.Geometry` | `Additive/production` | build-orientation overhang/bottom-area objective — never a hand-rolled normal classifier | 3 |
| K21 | `Domain/rails.md` — `Op` value-key threading + `Eff<Env>` carriage; receipts register into `IValidityEvidence`+`ValidityClaim` reaching `OpAcceptance` with ZERO oracle edits | `Rasm` | every entry | substrate rail | per-plane |
| K22 | `Domain/validation.md` — input guards compose the admission vocabulary + the one `OpAcceptance` oracle | `Rasm` | every entry | substrate rail | per-plane |
| K23 | `Numerics/atoms.md` — typed output projection rides `AtomProjection`/`ProjectionRow`, never a `typeof(TOut)` switch | `Rasm` | every egress projection | substrate rail | per-plane |

**Cross-package (both directions; stale targets re-point — disk-verified against `ARCHITECTURE.md`):**

| Seam (Fabrication side) | Direction | Counterpart / re-point | Leg |
|---|---|---|---|
| `Documentation/projection ← Rasm/Drawing/view` | consumes | kernel `View.Apply → Fin<DrawingProjection>` (`ARCH:46` "beside the in-folder BSP solver" clause DIES; row 32) | 4 |
| `Documentation/projection ← Rasm/Meshing/arrangement` | consumes | kernel `Arrangement.Apply`/`ToMesh` watertight (`ARCH:44` re-points off `Rasm/Processing/repair` per the kernel `[V5]` `BooleanOp` re-home; `ARCH:43` already targets the arrangement owner and HOLDS; K5) | 4 |
| `Documentation/projection → Rasm.AppUi/Render` | produces | `HiddenLineResult` receipt (`ARCH:47`; supersession insulated AT the receipt, `UIBRIEF [V7]`(b)) | 4 |
| `Additive/slicing → Rasm/Meshing/intersect` | consumes | kernel `Slicing.Apply → Fin<SliceStack>` (`ARCH:48` "when realized" REALIZED; row 24) | 3 |
| `Toolpath/skeleton ← Rasm/Meshing/offset+skeleton` | consumes | kernel `Offsetting.Apply`/`Skeletonize.Apply` clearance-radius (`ARCH:22`; `Skeleton.cs` dies unconditionally; rows 8,12) | 2 |
| `Toolpath/surface ← Rasm/Processing/{geodesics,extract,flow,segment}+fields` | consumes | kernel on-mesh path-layout + SDF fallback (NEW ledger rows, K10-K14/K8) | 3 |
| `Process/physics ← Rasm.Materials/Properties` | raw doubles | AEC-peer boundary (`ARCH:49`; `physics.md:3` anchor reconciled to `physical-properties#MATERIAL_PROPERTY`) | 1 |
| `Nesting/stock → Rasm.Compute` | produces (forward demand) | `NestYield.WasteAreaMm2` (`ARCH:51` "recorded next-campaign Compute counterpart" — genuinely ONE-SIDED; `CBRIEF [V12]` REFUTED, `[00]` ruling 3) | 3 |
| `Nesting/stock ← Rasm.Element/MaterialId` | consumes | NEW ledger row (wired-undeclared `stock.md:40`, E11) | 3 |
| `Ingress/profile ← Rasm.Bim/Exchange` | shared read pin | ACadSharp read codec (`ARCH:52` netDxf parenthetical → AppUi ACadSharp two-format WRITE leg) | 2 |
| `Posting/program → Rasm.Persistence` | blocked demand | `CutProgram` durable rows (`ARCH:53` off DELETED `Persistence/Schema` → content key via `ContentHash.Of`; the `ArtifactKind` index is the held-open `[ARTIFACT_CONTENT_KEY_FEDERATION]`, NOT `PBRIEF [V12]`) | 4 |
| `Nesting/nfp → Rasm.Persistence` | blocked demand | Placement/Remnant durable rows (`ARCH:54` off DELETED Schema → `ContentHash.Of`) | 3 |
| `Posting/program → Rasm/Drawing/pack` | residency ledger row | kernel `toolpath` `PackKind` (K7; channel-set alignment, NEVER a Fabrication encoder) | 4 |
| `{Tooling/magazine, Verify/probing} ← Rasm.AppHost` | decoded telemetry | MTConnect `-Common` model slice (`APPHOST:210` MTConnect.NET row; magazine tool-life FIRST, probing measured-feature SECOND) | 1,4 |
| `{Verify/removal, Documentation/traveler, Additive/production} → Rasm.Persistence` | blocked demand | `.cli`/3MF/NC1/snapshot/traveler egress content keys via `ContentHash.Of` (the `ArtifactKind` rows are the Fabrication-authored demand on the HELD-OPEN Persistence card) | 3,4 |

## [06]-[ROSTER_DELTA] — 24-package disposition (INTEGRATION-FIRST: the four dead admissions are MANDATES realized as pages)

Central ownership `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.Fabrication.csproj`; folder-tier catalogs `libs/csharp/Rasm.Fabrication/.api/`, shared substrate `libs/csharp/.api/`. License gate: OSS + free-for-OSS-commercial admissible; LGPL native engines admissible as dynamic-link P/Invoke under the golden-fixture gate; AGPL REFERENCE-only. Manifest anchors disk-verified (the brief's line numbers carried `±2` drift; VERIFY-REMOVE/re-group name-locate at leg-1, but the corrected disk lines are recorded here).

| # | Package | Action | Home page(s) | `.api` obligation + proof | Manifest motion |
|---|---|---|---|---|---|
| 1 | `OpenCAMLib` | **ADD** (LGPL-2.1; probe FEASIBLE) | `Toolpath/surface` | `api-opencamlib.md` NEW: `Operation` lifecycle (`setSTL`/`setCutter`/`setSampling`/`run`/`getCLPoints`), the `Cyl`/`Ball`/`Bull`/`Cone`/`Composite`Cutter → `CutterForm` ctor map, ~18-24-fn `extern "C"` shim ABI (Boost.Python `src/pythonlib/ocl_*.cpp` the blueprint — NOT pybind11), RID assets (`macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64` SHARED `libocl`, all three present), the per-RID OpenMP runtime carriage (osx-arm64 bundles `libomp.dylib` — upstream already rpaths it; linux resolves system `libgomp`; win the MSVC `vcomp` redist; `USE_OPENMP=OFF` the build-policy fallback), LGPL-2.1 dynamic-link note, golden-fixture gate. NO NuGet (feed-verified) | ADD to Fabrication label; RID assets recorded; `USE_OPENMP=OFF` build-policy row |
| 2 | `lib3mf` | **ADD** (BSD-2, official C# binding v2.5.x) | `Additive/production` | `api-lib3mf.md` NEW: 3MF core/production/beam-lattice writer; vendored official binding + RID-keyed native asset (NO NuGet — feed-verified; `IxMilia.ThreeMf` dominated); golden-fixture gate; PicoGK `Lattice` → beam-lattice map | ADD to Fabrication label; vendored binding |
| 3 | `PicoGK` | **KEEP-REALIZE** (integration mandate) | `Additive/implicit` (3 lanes), `Verify/removal` | `api-picogk.md`: DROP the `geometry3Sharp DMesh3` permanent-owner coupling (`:5,113,154`) → owned biarc fold + kernel mesh vocab; re-point `Persistence/Schema` → artifact index | held (`PROPS:84`) |
| 4 | `OcctNet.Wrapper` | **KEEP-REALIZE** (integration mandate) | `Ingress/solid` | `api-occtnet-wrapper.md`: re-point `Rasm.Persistence/Schema` → `ContentHash.Of` egress; the `libTKHLR`/`libTKXCAF` SCOPE_LIMIT demand row re-verified each leg | held (`PROPS:83`) |
| 5 | `DSTV.Net` | **KEEP-REALIZE** (integration mandate) | `Ingress/steel` (read), `Posting/dialect` (NC1 emit model) | `api-dstv-net.md`: purge band-2500 (`:3,126`)→2700; purge netDxf (`:132,143`) | held (`PROPS:79`) |
| 6 | `SharpVoronoiLib` | **KEEP-REALIZE** (integration mandate) | `Toolpath/partition` (`extern alias Voronoi`, `csproj:24`) | `api-sharpvoronoilib.md`: slim dual-ownership prose (`:3,18`) → Fabrication-sole (kernel `Rasm.csproj` has no reference) | held (`PROPS:87`; float-lane re-group LANDED) |
| 7 | `CavalierContours` | **RE-GROUP** → Fabrication (`[V10]a` stratum) | `Geometry2D/arcs` | `api-cavaliercontours.md`: `PosPlines` authority HOLDS (`:47`, drives the `result.Positive`→`PosPlines[].Pline` fix); sole-consumed by Fabrication | VERIFY float-lane re-group landed (`PROPS:77`) |
| 8 | `Clipper2` | **RE-GROUP** → Fabrication | `Geometry2D/algebra` | `api-clipper2.md`: clean | VERIFY float-lane re-group landed (`PROPS:78`) |
| 9 | `RectangleBinPack.CSharp` | **RE-GROUP** vacated "Materials" → Fabrication | `Nesting/stock` (5 packers), `Nesting/nfp` (MaxRects fast-path) | `api-rectanglebinpack-csharp.md`: subset-superset proof holds; namespace `RectangleBinPacking` diverges from pkg id (`stock.md:41`). Feed 1.0.4 current | re-group from Materials label (`PROPS:167`) → Fabrication |
| 10 | `RectpackSharp` | **REMOVE** (redundancy) | (nfp fast-path re-homes to RBP) | `api-rectpacksharp.md` DELETE. Proof: strict subset of `RectangleBinPack.CSharp` (`PackingHints.FindBest` = the try-every-heuristic sweep, one `MaxRectsBinPack.Insert` fold); sole consumer nfp re-homes (row 18); `README:65` rejection rewrites | REMOVE from Fabrication label (`PROPS:85`) |
| 11 | `netDxf` | **VERIFY-REMOVE** | (none — AppUi's `[V7]` write leg) | netDxf-purge ripples: `README:33,64`, `ARCH:52`, `import.md:20`, `api-dstv-net.md:132,143`, `api-acadsharp:120` re-frame to the AppUi ACadSharp two-format leg | VERIFY-REMOVE surviving orphan (`PROPS:359`) |
| 12 | `netDxf.netstandard` | **VERIFY-REMOVE** | (none — Compute FEALiTE2D floor) | Leaves with the Compute `[V8]` FEALiTE2D.Plotting retirement (its lock-verified sole parent); a surviving pin under a parity-kept FEALiTE2D stays a transitive floor, NOT Fabrication residue | stays transitive floor if FEALiTE2D parity-kept (`PROPS:470`) |
| 13 | `geometry3Sharp` | **REPLACE-candidate** (abandoned 2019, `csproj:17`) | `Posting/program` (owned Bolton biarc fold, line-sourced chains only) | `api-geometry3sharp.md`: `:106` "dropped CavalierContours" self-contradiction dies; `api-picogk:5,113,154` g3-as-sole-owner coupling re-frames. Leaves when the owned fold lands + Bim drops its mesh-importer (cross-package residual `Rasm.Bim.csproj:22`, RECORDED not executed) | held (`PROPS:81`); abandonment recorded on the golden fixture |
| 14 | `Robots` | **KEEP-REALIZE** (cell) | `Kinematics/cell` | `api-robots.md`: band-2500→2700 (`:5,94`); `[03]-[RESEARCH]` tail folds into `Packages`/`.api` | held (`PROPS:86`) |
| 15 | `MTConnect.NET-Common` | **KEEP** (model-only) | `Tooling/magazine`, `Verify/probing` | `api-mtconnect-net-common.md`: `GenerateHash → ContentHash.Of` reconcile (`:3,116`); re-point `Persistence/Schema` → artifact index; transport half is AppHost livewire's | held (`PROPS:82`) |
| 16 | `ACadSharp` | **KEEP** (read-only) | `Ingress/profile` | `api-acadsharp.md`: `:120` stale write-leg → AppUi ACadSharp two-format leg | held (BIM label) |
| 17 | `MathNet.Numerics` | **VERIFY** (beta2 HOLD; first Fab consumer) | `Spec/capability` | The `6.0.0-beta2→5.0.0` downgrade the brief expected is SUPERSEDED by the geometry DECISION `[09]` (Compute `Integrate.OnCuboid` is 6.0-only → beta2 HOLDS; disk `PROPS:62-63` comment confirms) | held `6.0.0-beta2` (`PROPS:63`) |
| 18 | `Riok.Mapperly` | **SHARED-MINE** (admitted substrate) | `Kinematics/cell`, `Tooling/magazine`, `Verify/probing`, `Ingress/steel` | DSTV-record→Loop + MTConnect rows + R3 seam (zero-reflection partials); `api-mapperly.md` shared, VERIFIED | held (Foundational Core) |
| 19 | `System.Numerics.Tensors` | **SHARED-MINE** | `Toolpath/surface`+`motion`, `Nesting/nfp`, `Spec/capability` | SIMD-lower the hot sampling folds (engagement/scallop/NFP-sweep/capability-batch); `api-tensors.md` shared | held |
| 20 | `CommunityToolkit.HighPerformance` | **SHARED-MINE** | `Additive/implicit`, `Verify/removal`, `Toolpath/surface` | `Span2D`/`Memory2D` 2D grids (grayscale/uncut/engagement rasters); `api-highperformance.md` shared | held |
| 21 | `NodaTime` | **SHARED-MINE** | `Documentation/traveler`, `Verify/probing`, `Tooling/magazine`, `Spec/capability` | `Instant` traveler/probing/tool-life/SPC stamps; `api-nodatime.md` shared | held |
| 22 | `QuikGraph` | **SHARED-MINE** | `Fixturing/setups` | precedence/datum-lineage graph (magazine eviction greedy stays); shared tier | held |
| 23 | `System.IO.Hashing` | **KEEP** (via `ContentHash.Of` ONLY) | all egress | `api-hashing.md` SLIMS to the `api-unitsnet.md` thin-overlay pattern; the false "no shared tier" law (`:3,88`) dies, defers to shared `libs/csharp/.api/api-hashing.md`, names `ContentHash.Of` the one mint | held (`csproj:25`) |
| 24 | `UnitsNet` | **OVERLAY-EXTEND** | cut-parameter boundary + `Spec/tolerance` | `api-unitsnet.md` overlay extends Speed/Length/RotationalSpeed/Pressure → +Force/Power/Temperature/Angle/Torque (thin-overlay pattern) | held (`csproj:26`) |

`.api` authoring: 2 NEW (`api-opencamlib.md`, `api-lib3mf.md`), 1 DELETE (`api-rectpacksharp.md`), 9 re-point/slim (hashing, sharpvoronoilib, picogk, occtnet, mtconnect, dstv-net, robots, acadsharp, geometry3sharp). Substrate `api-languageext.md` VERIFIED (never re-authored).

REFERENCE records (never vendored; silence ≠ oversight): `gsGCode` (parse-arm prior art), `gsSlicer` (FFF enrichment), MillSimSharp (voxel-verify corroboration), CAMotics/PrusaSlicer/CuraEngine/libslic3r (concern-validity proofs), FreeCAD `Adaptive2d`+machinability (`kc` schema), FreeCAD_SheetMetal (K-factor unfold), Tweaker-3 (orientation objective), UVtools (resin machine-format breadth), PySLM (Powder hatch/scan), QIF/ISO 23952 (Spec data-model authority), NIST RS274NGC v3 (dialect grammar authority), E3Studio (dialect preset reference). REJECTED: Fanuc FOCAS (distributor-gated), STEP-NC/ModuleWorks/EncyCAM (pay), Aspose.3D/ODA/CadEx (proprietary), PathCAM (GPL dominated), GcodeParserSharp (no modal-group state), Occt.NET/Macad.Occt/Ab4d.OpenCascade/Xbim.Geometry.Occt (OCCT-wrapper supersession sweep re-ran, `OcctNet.Wrapper` HOLDS with its SCOPE_LIMIT demand row). Stratum-split: Manifold is the KERNEL's `[V5]` tier (Fabrication consumes booleans at the arrangement seam, never a pin); MTConnect full-stack/OPC-UA/umati + BACnet are AppHost transport.

## [07]-[VERDICT_DISPOSITIONS] V1-V10

| V | Disposition |
|---|---|
| V1 | RATIFIED at `[01]`: 13 folders ↔ 13 TIER-1 namespaces BIJECTIVE, every folder ≥2 pages, folder=namespace=fault-cluster 1:1. Six folder-overrides-namespace ratifications execute in-MOVE (family/faults/physics→`Process`, magazine→`Tooling`, import→`Ingress`, projection→`Documentation`); four namespace-keeping moves (clipper split→`Geometry2D`, kinematics→`Kinematics`, slicing→`Additive`, workholding→`Fixturing` — all namespace-true on disk). `Polygon/` dies. The `Process`→`ProcessKind` collision-break (`[00]` ruling 1) is the ONLY resolution preserving literal 1:1 under the brief-binding "faults→Process"; `Machine` UNRENAMED. `ARCH:[01]/[02]/[03]` + README router + faults taxonomy rewrite in the wave-1 motion; the two-node `owner#atoms`/`owner#run` split is the one ratified acyclicity exception. |
| V2 | The five author-kernels re-frame to CONSUMER under the RULED dispositions (K1-K6/K8 signature-locked against the landed geometry DECISION): (a) skeleton keeps ONLY trochoidal WALK, `Skeleton.cs` dies unconditionally (K2); (b) slicing re-routes to `Slicing.Apply`, `Section`/O(n²)`Chain` die (K3); (c) projection BSP dies for `View.Apply` (K6), keeps the watertight `Arrangement` arm (K5) + PRESERVES `HiddenLineResult`; (d) arcs composes the `[V10]a` split (K1, CavalierContours kerf lane Fabrication-side, absorbed from clipper); (e) booleans/SDF kernel-consumed (K5/K8). The FIVE `[RESEARCH]` tails purge (disk-verified: kinematics:122, skeleton:199, slicing:219, stock:317, program:379 — the brief's 3 undercounts stock+program); `rg '\[RESEARCH\]'` → zero. |
| V3 | The pipeline islands wire into ONE fold (E2 killed): `Cam` consults `guard.Check` per feed move + `workholding.Condition`, producing the conditioned `Motion`; `Run(Post)` assembles the neutral `CutProgram` AST from the `Motion` (`[00]` ruling 5) + consumes `magazine.Schedule` (real Tnn/M6/G43) + `dialect.Emit`; the arc rail REALIZES (kerf/lead/adaptive read `arcs` arc-native + `Loop.Bulges`, g3 line-sourced only); `owner#atoms` lands the widened `Loop`+`Bulges`+3-arg-ctor+`BulgeAt`; the `result.Positive`→`PosPlines` phantom fix (row 8); Fixturing hardens (segment-vs-polygon keep-out, total `ForHolding`, per-kind footprints, `Condition` wired); the Placement-vs-Motion conditioning asymmetry states on the arm as a LOAD-BEARING invariant, not a bare label (brief V3): `Placement` (the 2D-cut result) runs the full `Kerf`/`Lead`/`Pierce`/`Tab`/`Sequence` conditioning; `Motion` (the 3D-mill result) is already CAM-conditioned (lead arcs land IN the Cam fold via the arc rail), so a `Kerf` pass over `Motion` DOUBLE-COMPENSATES — the forbidden form, because the toolpath already embeds kerf; the split routes which conditioning runs on which egress case and a build agent that kerf-passes `Motion` ships a silent double-compensation defect; the work-offset chain wires end-to-end (`setups` owns setup→WCS, posting renders, `probing` reconciles). |
| V4 | Posting becomes a real per-dialect generator (rows 2, 27, 30): `PostDialect` gains grammar-family capability columns; `dialect.Emit` a generated total Switch (the flat 3-column render dies); the AST grows canned-cycle/subprogram/macro nodes with typed R/Q/P slots (per-family lowering = node expansion); rows widen as DATA (840D/TNC/OSP/Fagor/Centroid); NC1 emit target on the `Run(Post)` case; G-code INGRESS = the `Parse` arm with the NIST modal-group state machine (`Parse ∘ Emit = id`); data-vs-lowering split (axis family's, folds dialect's); the two look-ahead planners read ONE motion-dynamics law homed on `machine`. |
| V5 | `CutStrategy` widens to production CAM on the `(RemovalModality, CutStrategy)` cross-product + dimensionality column (rows 20-23): waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family; every stub becomes a REAL generator (Turn ZX sweep over the `Bounds` cylinder K14, helical distinct, Peck canned, SliceWalk orderer); OpenCAMLib owns cutter POSITIONING (probe FEASIBLE, ADMIT-WITH-SHIM, row 22; author-kernel SDF K8 the recorded fallback), path LAYOUT kernel-composed (K10-K13); `Toolpath/partition` realizes the Voronoi mandate (row 23); engagement reads the medial clearance AND the physics chip-load budget; `Kinematics/machine` closes the 5-axis gap (rotary topology + TCP/RTCP, kernel one-slerp K19); the `CutterForm` axis lands (four consumers) — the type on `owner#atoms` (`[00]` ruling 2), the projection + machinability on `Tooling/cuttingdata`. |
| V6 | Additive becomes a folder (rows 24-26): slicing keeps FFF/DED planar over the kernel slice-stack (K3), layer-height demand UPSTREAM as kernel policy rows; implicit realizes PicoGK to depth (TPMS/gyroid/lattice/support/resin-powder `.cli`/grayscale, ALC-firebreak); production owns orientation (kernel `Analysis/select` K20) + machine profiles + lib3mf 3MF; physics mints Resin/Powder/DED budgets. gyroid corrects from the `InfillPattern`-row false collapse to the voxel lane. |
| V7 | NEW Verify plane (rows 28, 31): removal owns voxel material-removal (PicoGK `BoolSubtract`, gouge/uncut/overcut/air-cut receipts) PRODUCING `ResidualStock` + per-setup content-keyed `StockSnapshot` (both mint on `owner#atoms` wave 1 so Verify produces while rest/setups READ input-carried — run N feeds run N+1); guard stays the per-move DESIGN-time floor (two owners, two moments, both wired); probing realizes the queued card (G31/G38 rows, kernel `register.md` ICP K16, `ConformanceMetric` K17 verdicts, QIF→capability, AppHost decoded/manual ingress); the PicoGK net9/native boundary declares ONCE on implicit; `FabricationPolicy` grows `Verify`/`Inspect` — and the `Run(Post)`/`Run(Document)` egress cases that make re-post and traveler-compose one-entry. |
| V8 | NEW Spec plane (rows 20, 29): tolerance owns the typed GD&T vocabulary (ISO 286 generated fit rows, ISO 1101/ASME Y14.5 FCF+datums, ISO 1302 finish, MMC/LMC, QIF-aligned); capability owns Cp/Cpk/Pp/Ppk (streaming half on kernel `Stat`/`Distribution` K18, fit+Monte-Carlo on MathNet) + SPC; the spec DRIVES process (Ra→scallop stepover `surface` consumes, IT-grade→allowance conditions pass planning, the plan-time Cpk GATE PRODUCES the `CapabilityVerdict` minted on `owner#atoms` — the plan reads it input-carried, spec→plan→verify→capability one rail); traveler owns setup-sheet/traveler document MODELS via the `Run(Document)` case; UnitsNet overlay extends. |
| V9 | physics identity (row 3): ONE `Material` per physical material with `Map<RemovalModality, ModalityPhysics>` (disk `stainless`/`stainless-abrasive`/`mild-steel-thermal` collapse); `RemovalBudget.Erosion` lands (the `erosion→ThermalBudget` conflation dies) beside Resin/Powder; `CuttingData` moves to `Tooling/cuttingdata` and deepens to the Kienzle `kc` machinability model behind a data-ingress arm (FreeCAD-cards-removed gap honest); the `0.0012` force literal reads `kc`; the dead `Overrides` deletes onto the ingress arm; `90.0` demotes behind the table. |
| V10 | Every stale-law reference closes in one motion (E10): the R6 25xx census (`ARCH:16`, `kinematics:3`, `TASKLOG:44` 2505-2509, `api-dstv-net:3,126`, `api-robots:5,94`) → 2700; the netDxf purge (`README:33,64`, `ARCH:52`, `import:20`, `api-dstv-net:132,143`, `api-acadsharp:120`) → the AppUi ACadSharp two-format leg, pins VERIFY-REMOVE; the `Rasm.Fabrication.csproj:3` Description rewrites (the stale `CAM toolpath motion with DH/IK kinematics` string — the Robots serial-chain cell (row 16) + the new 5-axis `Kinematics/machine` rotary topology (row 17) SUPERSEDE DH/IK, and the description widens off the HLR+CAM+nesting slice to the production-fabrication telos); `ARCH:63` scheduler overclaim → `Fixturing/setups`; `[GUARD]-[QUEUED]` closes COMPLETE; `api-geometry3sharp:106` self-contradiction dies; `api-hashing:3,88` "no shared tier" slims; `api-sharpvoronoilib:3,18` dual-ownership → Fabrication-sole; nfp's two logic defects land (`Stock.Of` full-dimension hash `:88`, `Ty*1e6+Tx`→tuple `:137`, NFP frame proven). |

## [08]-[EVIDENCE_DISPOSITIONS] E1-E14

| E | Disposition |
|---|---|
| E1 | The four zero-consumer admissions realize as pages (INTEGRATION-FIRST, `[06]` rows 3-6): PicoGK→implicit+removal, OcctNet→solid, DSTV→steel+NC1, SharpVoronoiLib→partition. README `[IMPLICIT_VOXEL]`/`[SOLID_INGRESS]`/`[STEEL_FABRICATION_EXCHANGE]`/`[VORONOI_TESSELLATION]` charters become fence-true. |
| E2 | The three pipeline islands wire (V3): `Cam→guard.Check` (row 21→13), `Run(Post)→magazine.Schedule` (row 30→5), `workholding.Condition` wired + composing `NestingOrder` (row 14→7). The `Cam→{Guard,Workholding}` + `Run(Post)→dialect.Emit` fold EXISTS on disk. |
| E3 | The arc rail realizes (V3): `owner#atoms` lands `Loop`+`Bulges`+`BulgeAt`+3-arg-ctor (row 1); `arcs` reads them + fixes `result.Positive`→`PosPlines[].Pline` (row 8, absorbing clipper's `ArcAlgebra`); `program` reads arc-native offsets (g3 line-sourced only, row 30); the live phantom is the `.Positive` accessor (disk `clipper.md:221`; real member `PosPlines` `api-cavaliercontours.md:47`). |
| E4 | Post thinness dies (V4, rows 2/27/30): PostDialect grammar columns, generated `Emit` Switch, structured cycle/subprogram/macro AST nodes, per-dialect code override; the dialect-invariant `Code` table (disk `program.md:42-52`, thread-cycle→"G92") dies; 9-of-21 stub GCommand rows become real emitted blocks or DATA rows. Byte-diversity across the F1 five targets is the kill-test. |
| E5 | Stub generators become REAL (V5, row 21): radial-sweep/helical de-alias from Turn (disk `motion.md:71,73`), Turn a true ZX sweep over the `Bounds` cylinder (BarStock real), Peck canned-cycle geometry, SliceWalk a perimeter/infill/travel orderer; the 3D-surface family lands on `surface` (row 22). |
| E6 | physics identity map (V9, row 3): per-modality `Map`, Erosion/Resin/Powder budgets, dead `Overrides` deleted, `90.0` demoted; `0.0012` force → the `kc` column on `cuttingdata` (row 6). |
| E7 | HLR not-CAD-grade dies (V2c, row 32): the centroid BSP + per-edge average depth + undeduped mesh-edge soup + `1e6` eye + the PHANTOM `Orient2D` import all die for the kernel `View.Apply` (K6); the watertight arm (K5) + `HiddenLineResult` receipt preserved (AppUi insulated). |
| E8 | Coverage silences close: waterline/scallop/pencil/rest/swarf (row 22), NC verification (row 28), production specs (rows 20/29/33), additive supports/orientation/profiles/3MF (rows 25/26), G-code READ (row 30 Parse), 5-axis rotary topology (row 17), typed CutterForm (owner#atoms + row 6), spec→plan derivation (row 20). |
| E9 | Identity/logic defects land: `Stock.Of` full-dimension hash via `ContentHash.Of` (row 18, disk `:88` area-only), `Ty*1e6+Tx`→tuple (row 18, disk `:137`), NFP frame proven/fixed (row 18), workholding segment-vs-polygon + total `ForHolding` + per-kind footprints (row 14). The 3 raw `XxHash128` mints (disk `:59,88,169`) route the one `ContentHash.Of` entry (K9). The `ArtifactKind`-on-owner#atoms spine + the `[00]` ruling-5 result-payload discipline prevent an egress folder re-minting the second-hasher defect. |
| E10 | Stale governance closes in the wave-1 V1 motion + the leg touching each fact (V10): band-2500→2700 corpus+`.api` (disk `ARCH:16` "band-2500"), netDxf purge, `Persistence/Schema`→artifact-index/`ContentHash.Of` (disk `ARCH:53,54`), `TASKLOG:44` codes, `IDEAS:27` closes, `ARCH:63` re-scopes, the two `.api` self-contradictions die. |
| E11 | Ledger gaps land (`[05]`): `stock←Rasm.Element/MaterialId` NEW row; the intra-package web declared (magazine→guard/workholding, guard→skeleton/workholding, workholding→algebra.NestingOrder, motion→skeleton/machine/cell); `ARCH:51` Compute rollup RE-FRAMED to a one-sided forward demand (`[00]` ruling 3, disk "recorded next-campaign counterpart"); `physics.md:3`↔`ARCH:49` anchor reconciled. |
| E12 | Redundancy/overlay: RectpackSharp REMOVE (row 10, ⊂ proof); `api-hashing` "no shared tier" slims (row 23); `magazine.Runout` reads the real measurement (row 5, disk `:161` literal stub); `README:65` MaxRect/BinPack rejection rewrites (RBP subsumes RectpackSharp). |
| E13 | Page-craft: the FIVE `[RESEARCH]` tails purge (V2, the brief's 3 undercounts stock:317 + program:379 — disk-verified); the false "no managed library exists" seals (skeleton:3, slicing:3,221) die (the kernel owners landed). |
| E14 | Upstream bindings signature-lock (`[05]` K1-K23 TABLE): against the LANDED geometry DECISION `FAB:NN`/row anchors (the E14 "PENDING" branch CLOSED), NOT the `+14/+16`-drifted GBRIEF pins; the two `[V12]` reciprocals DEMOTED (`[00]` ruling 3); `UIBRIEF [V6]`→`[V7]` corrected; the kernel members K1-K9 + suites K10-K20 + rails K21-K23 locked; leg-1 `assay api` re-verify. |

## [09]-[DELTA_DISPOSITIONS] — brief `[03]-[CAPABILITY_ESCALATION]`, all 14 planes

| Plane (now→target) | Homed at (rows) |
|---|---|
| Process axes owner/family/faults (8→9.5) | Rows 1,2,4 — the 8-case entry (Additive/Verify/Inspect/Post/Document), dialect grammar columns, CutStrategy dimensionality, KinematicClass rotary topology, typed fault payloads + 19 new arms, `Loop.Bulges` ratified, `ProcessKind` collision-break, ArtifactKind + CapabilityVerdict on owner#atoms, substrate rails (K21-K23). |
| Physics + machining data (6→9) | Rows 3,6 — per-modality identity map, Erosion/Resin/Powder, Kienzle `kc` on `cuttingdata`, data-ingress arm, dead `Overrides` deleted. |
| Tooling (8→9.5) | Rows 5,6 — magazine wired into Post, cuttingdata page, typed `CutterForm` (four consumers, type on owner#atoms), tool-life AppHost seam, `Runout` policy-typed. |
| Geometry2D substrate (8→9.5) | Rows 7,8 — two-owner split (both absorb from clipper — line-space→algebra, arc-space→arcs), `[V10]a` siting (K1), `PosPlines` phantom fix, arc rail consumed downstream, `NestingOrder` re-home. |
| Ingress (5→9.5) | Rows 9,10,11 — profile move, OcctNet solid (kernel `MeshSpace`/`HealOp` seam K15), DSTV steel, ONE polymorphic `Ingress.Admit(IngressSource)` emitting kernel-admissible geometry. |
| Toolpath subtractive CAM (5→9.5) | Rows 12,13,21,22,23 — real generator arms, waterline/scallop/pencil/rest/3+2/swarf, OpenCAMLib surface (path layout kernel-composed K10-K13), Voronoi partition, engagement+finish laws, guard wired, skeleton→kernel-medial (K2). |
| Kinematics (7→9.5) | Rows 16,17 — robot cell held, 5-axis machine topology + TCP/RTCP, kernel one-slerp (K19), one motion-dynamics law with posting. |
| Additive (4→9.5) | Rows 24,25,26 — kernel slice-stack consumption (K3), PicoGK implicit/lattice/support/resin-powder, orientation owner (K20), machine profiles, lib3mf 3MF, budget cases. |
| Nesting (8.5→9.5) | Rows 18,19 — `Stock.Of` identity fix, NFP frame proof, Persistence/Compute seam re-anchors, MaterialId ledger row. |
| Fixturing (6→9.5) | Rows 14,15 — segment-exact keep-out, per-kind footprints, `Condition` wired, multi-fixture setups owning WCS, op-N vs op-N-1 snapshot. |
| Posting (6→9.5) | Rows 27,30 — grammar families + canned/macro/subprogram + WCS + cutter-comp + arc-mode, per-dialect overrides, structured AST nodes, NC1 emit, modal-state `Parse`, `CutProgram` content key + ArtifactKind, the `Run(Post)` one-entry lowering (AST assembled from the `Motion`, `[00]` ruling 5). |
| Verify (—→9.5) | Rows 28,31 — voxel material-removal receipts, residual-stock field, per-setup snapshots, probing cycles + measured features on AppHost ingress, kernel ICP best-fit (K16), `ConformanceMetric` verdicts (K17). |
| Spec (—→9.5) | Rows 20,29 — ISO 286/1101/1302 + ASME Y14.5 QIF-aligned vocabulary, Cp/Cpk/SPC over kernel `Stat`/`Distribution` (K18) + MathNet, process-driving derivation rows + plan-time Cpk gate producing the `CapabilityVerdict`. |
| Documentation (6→9.5) | Rows 32,33 — projection as kernel-`DrawingProjection` consumer (K6, preserved `HiddenLineResult`), setup-sheet/traveler owner composing views/tools/fixtures/specs via the `Run(Document)` case (widest fan-in). |

## [10]-[CARD_DISPOSITIONS] — full IDEAS + TASKLOG pool (23 cards)

**IDEAS (7):** `[GUARD]-[QUEUED]` → CLOSE COMPLETE (the guard page landed; `IDEAS:27`/`ARCH:63` realized-but-open closes). `[PROBING]-[QUEUED]` → realizes into `[V7]` `Verify/probing` (row 31). `[MULTI_FIXTURE_SCHEDULE]-[QUEUED]` → realizes into `[V1]` `Fixturing/setups` (row 15). `[DRL_NEST_POLICY]-[BLOCKED]` → STAYS BLOCKED (the injected-`Func<NoFitPolygon,PartTransform,double>` delegate column is the standing strata-correct shape; closes when Compute lands its side). `[CSG_SILHOUETTE]-[COMPLETE]` → HOLD (the watertight `Arrangement.Apply`/`ToMesh` arm is PRESERVED on row 32). `[MAGAZINE]-[COMPLETE]` → RE-OPEN as a leg-1/leg-4 obligation: the card claims "G43/M6/Tnn blocks emitted by program.md" — E2 FICTION; the wire realizes on rows 5→30. `[FEEDRATE_LOOKAHEAD]-[COMPLETE]` → HOLD (the jerk-limited planner MATH preserved on row 30; now reads the one motion-dynamics law from `machine`, row 17). |

**TASKLOG (16):** `[NFP_DRL_POLICY]-[BLOCKED]` → STAYS BLOCKED (Compute-side, strata-forbidden reference; realizes `[DRL_NEST_POLICY]`). COMPLETE cards HELD with rebuild-carried deltas: `[CSG_WATERTIGHT_SILHOUETTE]` (watertight arm preserved, row 32); `[TOOLPATH_STRATEGY_MODALITY_FACTOR]` (the `(RemovalModality,CutStrategy)` Switch grows the dimensionality column, row 21); `[POST_DIALECT_OVERRIDE_SEAM]` (the override resolves against the grammar-family axis, rows 2/30); `[TROCHOIDAL_ENGAGEMENT_LIMIT]` (the WALK survives, the wavefront dies, row 12; the `Option<ArcCenter>` chord feeds the arc-native rail not biarc, row 30); `[NEST_MULTISHEET_SCHEDULE]`/`[NEST_REMNANT_LINEAGE]` (held, the `ContentHash.Of` identity fix + frame proof land, row 18); `[RECTPACK_FASTPATH_ADMISSION]` → SUPERSEDED (RectpackSharp REMOVE, re-home to RBP MaxRects, rows 10/18); `[BIARC_ARC_EMISSION_ADMISSION]` → RE-SCOPED (g3 retained ONLY for line-sourced chains; the arc-native rail retires it for arc-sourced, row 30; geometry3Sharp REPLACE-candidate); `[ACADSHARP_SPLINE_INSERT]` (held, moves to `Ingress/profile`, row 9); `[CLIPPER_INNER_FIT]` (held on `Geometry2D/algebra`, row 7); `[FABRICATION_FAULT_BAND_DEEPENING]` → the stale `TASKLOG:44` 2505-2509 codes re-point to 2701-2710 (rows 4/E10); `[TOOL_CUTTING_DATA_TABLE]` → the `CuttingData` table MOVES to `Tooling/cuttingdata` + deepens to Kienzle `kc` (row 6, donor excision); `[CLIPPER_VARIABLE_KERF]` (held on `algebra`, row 7); `[FAULT_REBAND_2700]` → the 2701-2710 band is the frozen Layer-1 the `[04]` registry builds ON; `[STOCK_NEST_MOVE]` (held; the `WasteAreaMm2→Compute` ripple RE-FRAMED to a one-sided forward demand, row 19). |

**IDEAS RESEED (named growth rows, not build-leg pages):** sheet-metal bend/unfold (DSTV `KA` decode lands with `Ingress/steel`; K-factor bend-allowance the Fabrication overlay over the kernel `develop.md` isometric unroll, never a second unroll engine); 5-axis simultaneous swarf refinement; conversational-dialect breadth beyond the seed rows; the AP242 PMI-surfacing spec-growth pressure on OcctNet; the queued `FabricationProjector: IElementProjection` seam row.

## [11]-[LEG_PARTITION] — acyclic, engine-invocation rows

**Acyclicity proof over the `[05]` ledger.** The compose graph is a DAG. The certificate is TWO conditions, both proven: (1) **no cycle** — the five page-cycle breaks (`ProjectionDir`/`ResidualStock`/`CutterForm`/`NestingOrder`/`CapabilityVerdict` on `owner#atoms` input-carry or re-home) plus the `[00]` ruling-5 result-payload discipline (no plane-internal type on a `FabricationResult` case) eliminate every back-edge; the two-node owner split gives the linear flow `owner#run → planes → owner#atoms → family(leaf)`; a mechanical walk of the 33 nodes finds no directed cycle. (2) **every cross-WAVE edge lands equal-or-earlier wave** — leg N never composes a leg N+1 page: the `owner#run` dispatch arms are two-touch (each realizes in its plane's own leg, `[03]`), so no wave-1 arm references an unbuilt plane; `motion` (W3) does NOT compose `program` (W4) — both compose the `owner#atoms` `Motion` type (`[00]` ruling 5); every other cross-wave edge (skeleton W2 ← guard/motion W3; arcs W2 ← program W4; algebra W2 ← program/workholding W3/W4; implicit W3 ← removal W4) points BACKWARD to an earlier wave. Intra-leg order is ADVISORY — the rebuild engine builds a leg's pages with full cross-visibility, so the listed order is a discover/implement hint (a plane-grouping, not a strict leaf-before-consumer sort), not a hard sequence. Zero upward strata edges (Materials/Compute raw-double/delegate/seam-row only; Persistence/AppHost/AppUi seam-ledger counterparts only). Leg order 1→4 is a valid topological sort of the wave DAG.

Intra-leg dependency order (advisory grouping hint — NOT a strict leaf-before-consumer sort; the rebuild engine builds each leg as one cross-visible set, so a handful of listings group by plane over leaf-first — motion before its `surface` arms, production before `slicing`, probing before `program` are consumer-before-leaf and harmless under the advisory rule): **Leg 1** family·physics·faults (leaves) → owner#atoms → magazine → cuttingdata → owner#run arms. **Leg 2** algebra → arcs → {profile·solid·steel} → skeleton. **Leg 3** {machine·workholding·nfp·stock·tolerance·implicit·partition} (leaves) → {cell·guard·setups·production} → motion → surface → slicing. **Leg 4** dialect → {removal·capability·probing} → program → projection → traveler (the widest fan-in).

| Leg | Engine invocation | Targets (dependency order) + riders |
|---|---|---|
| 1 | `Workflow(rebuild.js, {targets: ["libs/csharp/Rasm.Fabrication/.planning/Process/family.md", "libs/csharp/Rasm.Fabrication/.planning/Process/physics.md", "libs/csharp/Rasm.Fabrication/.planning/Process/faults.md", "libs/csharp/Rasm.Fabrication/.planning/Process/owner.md", "libs/csharp/Rasm.Fabrication/.planning/Tooling/magazine.md", "libs/csharp/Rasm.Fabrication/.planning/Tooling/cuttingdata.md"], brief: "RASM-CS-FABRICATION-DECISION.md"})` — rebuild ×4, new ×2 (magazine move-dest absorb {into: `Tooling/magazine.md`, from: `Process/magazine.md`} — the absorb deletes the source; cuttingdata new) | + the `[06]` full roster reconciliation (OpenCAMLib/lib3mf `.api` authored, RectpackSharp REMOVE + `.api` delete, netDxf VERIFY-REMOVE, the re-group verifications — CavalierContours/Clipper2/SharpVoronoiLib LANDED on the Fabrication label, RectangleBinPack.CSharp executes from the Materials label — api-hashing slim, UnitsNet overlay extend); the V1 ratification sweep (ARCH `[01]/[02]/[03]` 13-folder partition + README router + faults taxonomy + `ProcessKind` collision-break); the 8-case entry + ArtifactKind + CapabilityVerdict + ResidualStock/StockSnapshot + CutterForm + ProjectionDir mints on owner#atoms; the physics→cuttingdata donor excision; magazine tool-life AppHost seam; band-2500→2700 corpus+`.api` purge; the `Rasm.Fabrication.csproj:3` Description rewrite (DH/IK → Robots cell + `Kinematics/machine` topology, widened to the production telos) |
| 2 | `Workflow(rebuild.js, {targets: ["libs/csharp/Rasm.Fabrication/.planning/Geometry2D/algebra.md", "libs/csharp/Rasm.Fabrication/.planning/Geometry2D/arcs.md", "libs/csharp/Rasm.Fabrication/.planning/Ingress/profile.md", "libs/csharp/Rasm.Fabrication/.planning/Ingress/solid.md", "libs/csharp/Rasm.Fabrication/.planning/Ingress/steel.md", "libs/csharp/Rasm.Fabrication/.planning/Toolpath/skeleton.md"], brief: "RASM-CS-FABRICATION-DECISION.md"})` — new ×5 (algebra the ONE clipper absorb {into: `Geometry2D/algebra.md`, from: `Polygon/clipper.md`} — its delete is the single clipper delete; arcs plain new off the row-8 standalone arc-space inventory; profile move-absorb {from: `Polygon/import.md`}; solid/steel new), rebuild ×1 (skeleton — Skeleton.cs dies) | + `Polygon/` folder deletes; the `[V10]a` split (K1) + `PosPlines` fix; clipper's `ArcAlgebra` carried into arcs; `ARCH:22/48` kernel-consumer ripples (K1/K2); netDxf-purge frame rewrite; the OcctNet `MeshSpace`/`HealOp` seam (K15); `NestingOrder` lands on algebra |
| 3 | `Workflow(rebuild.js, {targets: ["libs/csharp/Rasm.Fabrication/.planning/Kinematics/machine.md", "libs/csharp/Rasm.Fabrication/.planning/Fixturing/workholding.md", "libs/csharp/Rasm.Fabrication/.planning/Nesting/nfp.md", "libs/csharp/Rasm.Fabrication/.planning/Nesting/stock.md", "libs/csharp/Rasm.Fabrication/.planning/Spec/tolerance.md", "libs/csharp/Rasm.Fabrication/.planning/Additive/implicit.md", "libs/csharp/Rasm.Fabrication/.planning/Toolpath/partition.md", "libs/csharp/Rasm.Fabrication/.planning/Kinematics/cell.md", "libs/csharp/Rasm.Fabrication/.planning/Toolpath/guard.md", "libs/csharp/Rasm.Fabrication/.planning/Fixturing/setups.md", "libs/csharp/Rasm.Fabrication/.planning/Additive/production.md", "libs/csharp/Rasm.Fabrication/.planning/Toolpath/motion.md", "libs/csharp/Rasm.Fabrication/.planning/Toolpath/surface.md", "libs/csharp/Rasm.Fabrication/.planning/Additive/slicing.md"], brief: "RASM-CS-FABRICATION-DECISION.md"})` — improve ×3 (guard, nfp, stock), new ×10 (workholding/cell/slicing move-absorbs {from: `Nesting/workholding.md`, `Toolpath/kinematics.md`, `Toolpath/slicing.md`} — each absorb deletes its source — + setups/machine/surface/partition/implicit/production/tolerance), rebuild ×1 (motion) | + the OpenCAMLib compose (probe FEASIBLE, K10-K14 path layout, K8 SDF fallback) + lib3mf compose; the E9 nfp identity/frame fixes; `stock←MaterialId` row; `ARCH:51` Compute demotion to forward demand; the wired-pipeline fold realizes (Cam→guard/workholding producing the conditioned Motion); tolerance staged early (surface reads Ra→scallop within-wave) |
| 4 | `Workflow(rebuild.js, {targets: ["libs/csharp/Rasm.Fabrication/.planning/Posting/dialect.md", "libs/csharp/Rasm.Fabrication/.planning/Verify/removal.md", "libs/csharp/Rasm.Fabrication/.planning/Spec/capability.md", "libs/csharp/Rasm.Fabrication/.planning/Verify/probing.md", "libs/csharp/Rasm.Fabrication/.planning/Posting/program.md", "libs/csharp/Rasm.Fabrication/.planning/Documentation/projection.md", "libs/csharp/Rasm.Fabrication/.planning/Documentation/traveler.md"], brief: "RASM-CS-FABRICATION-DECISION.md"})` — new ×6 (dialect/removal/capability/probing/traveler + projection move-absorb {from: `Posting/projection.md`} — the absorb deletes the source), rebuild ×1 (program) | + the CutProgram content key + ArtifactKind + Parse arm; the `Run(Post)`×5 / `Run(Document)` one-entry egress (program assembles the AST from the `owner#atoms` Motion, `[00]` ruling 5) consuming magazine/workholding/setups + dialect.Emit; the CapabilityVerdict plan-gate closure; the geometry3Sharp REPLACE-candidate (Bolton biarc owned); `ARCH:47` HiddenLineResult insulation (K6); probing AppHost seam (K16/K17); the four `[12]` acceptance dry-runs compose; the blocked Persistence egress demands recorded, never composed |

Residuals resolve per leg before the next launches (`resolve-residuals` on each non-empty `hard_residual`); `README.md`/`ARCHITECTURE.md`/`TASKLOG.md`/`IDEAS.md` close in the leg that changes their facts; residuals naming later-leg pages transfer forward explicitly. Campaign acceptance closes on the artifact — the four `[12]` dry-runs compose entirely from rebuilt fences; a dry-run reaching a missing owner reopens the owning verdict as a residual.

## [12]-[FLAGSHIP_DRY_RUN_CLOSURE] — F1-F4 backward traces

Each flagship is a chain of `Fabrication.Run` invocations over the 8-case policy; the terminal invocation projects a content-keyed artifact. Every page is pulled into existence by ≥1 flagship terminal (or the SharpVoronoiLib integration mandate). A trace reaching a missing owner reopens the owning verdict — all four close on rebuilt fences.

**F1 — five-target posting suite** (`[05](b)`; word-address ×3 grammar-true + conversational + additive + NC1 steel). Artifacts: `fanuc.nc`/`haas.nc`/`grbl.nc` (word-address, grammar-true: canned cycles G81-89/G76 single blocks, macro-B `#`-vars/WHILE, M98/M99 subprograms, G54-59 WCS, G41/42 comp, IJK/R arc mode — each DISTINCT), `klartext.h` (Heidenhain conversational), `marlin.gcode` (FFF additive), `part.nc1` (DSTV steel) — six byte-diverse files, each content-keyed by `ArtifactKind`; `Parse ∘ Emit = id` on the word-address subset. Backward: `Run(Cam)` → `Motion` carrying the conditioned move-stream ⇒ `Toolpath/motion` (row 21). Then `Run(Post{Motion, dialectᵢ})` ×5 ⇒ `Posting/program` assembles the neutral `CutProgram` AST from the `Motion` and lowers via `Posting/dialect` (generated grammar switch over `PostDialect`, rows 30,27) + `Process/family` (the `PostDialect` axis = seed DATA, row 2). NC1 = a sibling emit on the `Post` fold over the DSTV record tree ⇒ `Ingress/steel` (row 11). `Parse` arm (NIST modal-group state machine) ⇒ `Posting/program`. Content key ⇒ `owner#atoms` `ArtifactKind` + `ContentHash.Of` (row 1). Fault arms: inexpressible-cycle/macro-unsupported ⇒ `Process/faults` `DialectUnsupported` 2718; block-cap ⇒ `BlockCapExceeded` 2728 (a program-SIZE overrun carries no offending `GNodeKind`, so it splits off `DialectUnsupported`, row 4). **Closes** on rows 1,2,4,11,21,27,30 — `Run(Cam)→Run(Post)×5`, no second public `Post` fold, no `motion→program` compose edge (`[00]` ruling 5).

**F2 — verified two-setup multi-tool machining job** (`[05](d)`; spec → plan → guard/workholding/magazine wired → post → voxel verify → capability). Artifacts: one two-setup multi-tool program (per-setup G54-59 WCS blocks), voxel gouge/uncut/overcut/air-cut receipts, the op-2 clamp admitted against the op-1 stock snapshot, probe-reconciled work offsets, rest-machining consuming the residual field, a Cp/Cpk row gating the next plan, one setup sheet + one traveler. Backward: `Spec/tolerance` (row 20) + `Spec/capability` (plan-time Cpk gate producing `CapabilityVerdict`, row 29) → `Nesting/stock`+kernel `[V11]` hull (row 19) + `Fixturing/setups` (op→setup partition, setup-k→WCS, row 15) + `Fixturing/workholding` (per-kind footprint keep-out, `Condition` wired, row 14) + `Tooling/magazine` (schedule wired into the Cam fold, row 5) + `Tooling/cuttingdata` (Kienzle `kc`, row 6) + `Toolpath/{motion, surface (OCL), guard, skeleton}` (rows 21,22,13,12) + `Kinematics/{cell, machine}` (rows 16,17) → `Run(Verify{Motion, stock})` ⇒ `Verify/removal` (PicoGK `BoolSubtract`, receipts, residual field, per-setup snapshot, row 28) + `Run(Inspect{cycles})` ⇒ `Verify/probing` (ICP datum best-fit, ConformanceMetric, row 31) → `Spec/capability` (Cp/Cpk) → `Run(Post)` (per-setup WCS from `setups`) → `Run(Document)` ⇒ `Documentation/{projection, traveler}` (rows 32,33). Owner grows `Verify`/`Inspect`/`Post`/`Document` cases + `ResidualStock`/`StockSnapshot`/`CapabilityVerdict` on `owner#atoms` (row 1). **Closes** the run-N→run-N+1 truth loop on rows 1,5,6,12-22,28,29,31-33; the CapabilityVerdict input-carry makes the plan-time gate composable.

**F3 — resin/powder + FFF additive pair** (`[05](c)`; implicit lane + slice stack + 3MF). Artifacts: a STEP solid in; an oriented part scored; lattice supports + TPMS infill; an FFF `.gcode` AND a resin `.cli` layer stack AND a 3MF hand-off — three content-keyed egress. Backward: `Ingress/solid` (OcctNet `ImportStep`→`OcctShape`→`OcctMesh` → kernel `MeshSpace` K15, dirty STL through kernel `HealOp`, row 10) → `Additive/production` (build-orientation over kernel `Analysis/select` K20; machine profiles; 3MF via lib3mf, row 26) + `Additive/implicit` (PicoGK `IImplicit→Voxels` TPMS/gyroid, `Lattice` supports, SLA/DLP/MSLA grayscale + `.cli` via `oVectorize→CliIo`; the `Voxels` stay plane-local; ALC-firebreak, row 25) + `Additive/slicing` (FFF/DED planar consuming kernel `Slicing.Apply` K3, row 24) → `Run(Post{additive dialect})` ⇒ `Posting/{program, dialect}` (Marlin). `Process/physics` mints `RemovalBudget.{Resin, Powder, DED}` (row 3). Content keys ⇒ `owner#atoms` `ArtifactKind` + `ContentHash.Of`. **Closes** on rows 1,3,10,24,25,26,27,30.

**F4 — issued setup-sheet/traveler package** (`[05](d)` document half). Artifact: one content-keyed setup sheet + one shop traveler (typed document MODEL; rendering rides the `[06]` artifacts-plane seam). Backward: `Run(Document{Seq<FabricationResult>})` ⇒ `Documentation/traveler` (row 33) composing `Documentation/projection` views (row 32) + `Tooling/magazine` tool lists (row 5) + `Fixturing/{workholding, setups}` plans/WCS (rows 14,15) + `Posting/program` facts (row 30) + `Spec/{tolerance, capability}` rows (rows 20,29) into ONE content-keyed model keyed by `ArtifactKind` (row 1). The widest fan-in node validates every upstream owner exposes a typed receipt — receipt discipline is validated corpus-wide by the terminal composer, not asserted per-page. **Closes** on rows 1,5,14,15,20,29,30,32,33 — `Run(Document)` is one entry case, NOT a second public `Traveler.Assemble` fold.
