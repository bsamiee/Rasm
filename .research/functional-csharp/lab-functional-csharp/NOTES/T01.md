# T01 notes: Smart Enums

Lab: `.scratch/functional-csharp/lab-functional-csharp/T01`, namespace `Lab.T01`. Build: zero diagnostics. Run: exit 0, every check in `Samples.Run()` passes. Doc code and lab code are the same text: one lab file per doc section, generated from the fences by a sync script, plus `Samples.cs` as scaffolding.


## Verified identifiers

- `SmartEnumAttribute<TKey>` properties `KeyMemberName`, `KeyMemberAccessModifier`, `KeyMemberKind`, `SkipIComparable`, `SkipIParsable`, `SkipISpanParsable`, `SkipIFormattable`, `SkipToString`, `ComparisonOperators`, `EqualityComparisonOperators`, `ConversionToKeyMemberType`, `ConversionFromKeyMemberType`, `SwitchMethods`, `MapMethods`, `SerializationFrameworks`, `DisableSpanBasedJsonConversion`, `SwitchMapStateParameterName` with defaults `"Key"`/`"_key"`, `Public`, `Property`, `Default`, `Implicit`, `Explicit`, `All` — decomp:core
- Keyless `SmartEnumAttribute` properties `EqualityComparisonOperators`, `SwitchMethods`, `MapMethods`, `SwitchMapStateParameterName` — decomp:core
- `SwitchMapMethodsGeneration.{None, Default, DefaultWithPartialOverloads}`, `OperatorsGeneration.{None, Default, DefaultWithKeyTypeOverloads}`, `ConversionOperatorsGeneration.{None, Implicit, Explicit}`, `SerializationFrameworks.{None, SystemTextJson, NewtonsoftJson, Json, MessagePack, All}` — decomp:core
- `UseDelegateFromConstructorAttribute.DelegateName`, `IgnoreMemberAttribute`, `ValidationErrorAttribute<TValidationError>` — decomp:core
- `KeyMemberEqualityComparerAttribute<TAccessor, TKey>`, `KeyMemberComparerAttribute<TAccessor, TKey>`, `IEqualityComparerAccessor<in T>.EqualityComparer`, `IComparerAccessor<in T>.Comparer`, `ComparerAccessors.{StringOrdinal, StringOrdinalIgnoreCase, CurrentCulture, CurrentCultureIgnoreCase, InvariantCulture, InvariantCultureIgnoreCase, Default<T>}` — decomp:core, wiki:Smart-Enums-Customization
- `ISmartEnum<TKey, T, TValidationError>` static abstract `Items`, `Get`, `TryGet`, `Validate` (via `IObjectFactory<T, TKey, TValidationError>`) — decomp:core, probe (`Lookup.All`, `Lookup.Find`, `Lookup.Admit` compile and run)
- `UnknownSmartEnumIdentifierException : KeyNotFoundException`, message `There is no item of type 'OrderStatus' with the identifier 'nope'.` — decomp:core, probe
- `ValidationError.Message`, `ValidationError.ToString()` returns the message — decomp:core, probe
- `Parse` failure throws `FormatException` with the lookup message — probe
- `ThinktectureTypeConverter<T, TKey, TValidationError>` on `TypeConverterAttribute` — probe (`TypeDescriptor.GetConverter`), generated code
- `Thinktecture.Empty.Action` static no-op method — decomp:core
- `TypeParamRef1`..`TypeParamRef5` — decomp:core, probe (`Metric<T>`)
- Generated constructor order key, members, base arguments, delegates; `ValidateConstructorArguments(ref string key, ref string name, ref int numericCode)`; `ArgumentNullException` for a null key — probe generated `Country.SmartEnum.g.cs`
- `Items` in declaration order — probe
- `GetHashCode` from `StringComparer.OrdinalIgnoreCase.GetHashCode(key)` in the constructor; `TryGet("PENDING")` finds `Pending` — probe, generated code
- Generated interfaces for a string key on net10: `IParsable<T>`, `ISpanParsable<T>` (`#if NET9_0_OR_GREATER`), `IComparable`, `IComparable<T>`, `IComparisonOperators`, `IEqualityOperators`, no `IFormattable`; for an int key additionally `IFormattable` — probe (`GetInterfaces`), generated `OrderStatus.SpanParsable.g.cs`
- `Get(ReadOnlySpan<char>)`, `TryGet(ReadOnlySpan<char>, out T)` for string keys; `FrozenDictionary` lookups; `GetAlternateLookup<ReadOnlySpan<char>>()` for the default comparer and `ComparerAccessors`; `TryGetAlternateLookup` plus `key.ToString()` fallback for a custom accessor — probe generated `OrderStatus.SmartEnum.g.cs`, `Ticker.SmartEnum.g.cs`
- `SkipIComparable = true` removes `IComparable`/`IComparable<T>` and keeps the comparison operators, which use `StringComparer.OrdinalIgnoreCase` without `[KeyMemberComparer]` — probe (`Ticker` first variant), generated `Ticker.ComparisonOperators.g.cs`
- `Switch`, `Switch<TResult>`, `Map<TResult>`, state overloads with `where TState : allows ref struct`; `SwitchPartially` with optional `@default` (`Action<T>` / `Action<TState, T>`) and `@default?.Invoke`; value `SwitchPartially`/`MapPartially` with required `@default`; default arm `throw new InvalidOperationException($"Unknown item '{this}'.")` — probe generated `ProductType.SmartEnum.g.cs`, probe run (`Rogue` instance)
- Void `SwitchPartially` without default is a no-op for an unhandled item — probe, lab
- `[UseDelegateFromConstructor]` generates `Func<decimal, decimal> _round`; `DelegateName = "DateParser"` generates nested `private delegate DateTime DateParser(string[] row)` and field `_dateParser`; a `ref Utf8JsonReader` parameter compiles — probe generated code and reflection
- Nested Smart Enum requires a `partial` enclosing type (CS0260 without it); generator emits `sealed partial class` — probe
- Primary constructor on a nested derived class and on a plain base class compiles under the Thinktecture analyzer — lab
- `TTRESG002` (Error), `TTRESG003` (Error), `TTRESG014` (Error), `TTRESG015` (Error), `TTRESG037` (Error), `TTRESG041` (Error), `TTRESG043` (Error), `TTRESG046` (Error), `TTRESG048` (Warning, message "String-based Value Object needs equality comparer"), `TTRESG050` (Error), `TTRESG051` (Error), `TTRESG060` (Error), `TTRESG074` (Error), `TTRESG100` (Warning), `TTRESG101` (Warning), `TTRESG102` (Warning), `TTRESG103` (Warning), `TTRESG105` (Warning), `TTRESG1001` (Info) — decomp:analyzers
- `TTRESG100`, `TTRESG101`, `TTRESG102`, `TTRESG103`, `TTRESG105` emitted by a clean build; no `TTRESG048` for a string-keyed Smart Enum without comparer attributes; no `TTRESG103` when `SkipIComparable = true` — probe build log
- `System.Text.Json`: keyed enum writes `"Pending"`, reads `"shipped"`, unknown key throws `JsonException` with the lookup message, number token throws `JsonException` `Unexpected token "Number" when trying to deserialize "OrderStatus". Expected token: "String" or "PropertyName".` — probe
- Keyless enum under `System.Text.Json`: `Serialize` writes `{}`, `Deserialize` throws `NotSupportedException` — probe
- Generated attributes with the packages referenced: `JsonConverterAttribute` (System.Text.Json and Newtonsoft), `MessagePackFormatterAttribute`; none on a keyless enum — probe reflection
- `ThinktectureJsonConverterFactory()`, `(bool skipObjectsWithJsonConverterAttribute)`, `(bool, Func<Type, bool>? skipSpanBasedDeserialization)` — decomp:json
- `ThinktectureNewtonsoftJsonConverterFactory()` — decomp:newtonsoft.json
- `ThinktectureMessageFormatterResolver.Instance` — decomp:messagepack
- `ThinktectureModelBinderProvider(bool skipBindingFromBody = true)` — decomp:aspnetcore
- `AddThinktectureOpenApiFilters(this IServiceCollection, Action<ThinktectureSchemaFilterOptions>?)`, `ThinktectureSchemaFilterOptions.SmartEnumSchemaFilter`, `.SmartEnumSchemaExtension`, `SmartEnumSchemaFilter.{Default, OneOf, AnyOf, AllOf, FromDependencyInjection}`, `SmartEnumSchemaExtension.{None, VarNamesFromStringRepresentation, VarNamesFromDotnetIdentifiers, FromDependencyInjection}`, `ISmartEnumSchemaFilter`, `ISmartEnumSchemaExtension` — decomp:swashbuckle
- `UseThinktectureValueConverters()` / `(Configuration)` on `DbContextOptionsBuilder`; `AddThinktectureValueConverters()` / `(Configuration)` on `ModelBuilder`, `EntityTypeBuilder`, `OwnedNavigationBuilder`, `ComplexPropertyBuilder`; `HasThinktectureValueConverter()` / `(Configuration)` on `ComplexTypePropertyBuilder<T>`; `Configuration.Default`, `Configuration.NoMaxLength`, `Configuration.SmartEnums { get; init; }`, `SmartEnumConfiguration.MaxLengthStrategy { get; init; }`, `DefaultSmartEnumMaxLengthStrategy.Instance`, `FixedSmartEnumMaxLengthStrategy(int maxLength, bool overwriteExistingMaxLength = false)`, `CustomSmartEnumMaxLengthStrategy(Func<Type, Type, IReadOnlyList<ISmartEnumItem>, MaxLengthChange>, bool)`, `NoOpSmartEnumMaxLengthStrategy.Instance` — decomp:entityframeworkcore10
- `UsingThinktectureRuntimeExtensions(this LoggerDestructuringConfiguration, TypesToRenderAsString renderAsString = None)`, `TypesToRenderAsString.{None, SmartEnums, ValueObjects, AdHocUnions, All}` — decomp:serilog
- Serilog rendering: keyed string key `"Pending"`, int key `3`, keyless falls back to default object destructuring (`{"$type": "MoneyRoundingStrategy"}` without members, `MoneyRoundingStrategy { Name: "Nearest" }` with a `Name` property) — probe, lab
- `Metric<int>.Parse("1", null)` returns `Humidity`, `Metric<double>.Get(0.0)` returns `Temperature`, `INumber<T>` constraint yields `IFormattable`, `IParsable`, `ISpanParsable`, `IComparable`, comparison operators — probe
- Minimal API binding through `IParsable<T>` and the bool-only limitation; MVC 400 through `[ApiController]`; span-based JSON, `DisableSpanBasedJsonConversion` string-only; `SerializationFrameworks` limits generation — wiki:Smart-Enums-Framework-Integration, wiki:Smart-Enums-Customization
- Rounding of the max length to the next multiple of ten and no overwrite of an existing max length — wiki:Smart-Enums-Framework-Integration

## Dropped statements from the existing artifacts

- smart-enums-6, smart-enums-7: "The library originally used reflection ... rewritten after Roslyn source generators arrived with .NET 5" — history with a version reference, no API content, banned by the brief.
- smart-enums-6: "Smart-enum instances ... cannot replace ordinary enum members in attributes that require constant arguments" — kept out, not a library fact and no source in the wiki.
- smart-enums-7: `ProcessCreditCardAsync`, `ProcessBankTransferAsync`, `EmailSender.SendAsync` declared `async` without `await` — CS1998 fails under warnings-as-errors; replaced with synchronous samples.
- smart-enums-8: "`modelBuilder.UseThinktectureValueConverters()`" — the `ModelBuilder` extension is `AddThinktectureValueConverters` (decomp:entityframeworkcore10, wiki).
- smart-enums-8: "`ValueObjectModelBinderProvider`" and "`UseValueObjectValueConverter`" — removed in v10 (wiki:Migration-from-v9-to-v10), replaced with `ThinktectureModelBinderProvider` and `UseThinktectureValueConverters`.
- smart-enums-8: "For EF Core 7 and later" — version phrase, and the current packages are EntityFrameworkCore8/9/10.
- smart-enums-8: "`IParsable<TSelf>` when its key type is parsable, such as `int` or `Guid`" — incomplete: a string key also yields `IParsable<T>` and `ISpanParsable<T>` (probe).
- smart-enums-8: minimal API binding "often works" — replaced with the exact mechanism and limitation from the wiki.
- wiki:Smart-Enums-Customization: "the analyzer will warn you if you don't provide an equality comparer for a string-based Smart Enum" — contradicted. `TTRESG048` is titled "String-based Value Object needs equality comparer", `ValidateKeyMemberComparers` runs with `stringBasedRequiresEqualityComparer: false` for Smart Enums (decomp:analyzers), and a clean probe build of a string-keyed Smart Enum without comparer attributes emits no warning.
- wiki:Smart-Enums (Lazy sample): `new(() => [Processing, Cancelled])` inside a field initializer emits CS8601 (possible null assignment) under nullable analysis, which fails warnings-as-errors; the doc uses static methods instead.
- wiki:Smart-Enums (Best Practices): "`ISpanParsable<T>`: Implemented when the key type implements `ISpanParsable<TKey>`" listed under numeric and date keys only — string implements `ISpanParsable<string>`, and the probe confirms `ISpanParsable<OrderStatus>` for a string key.

## Host-only blocks

- MVC and Swashbuckle registration (`services.AddControllers(...)`, `services.AddSwaggerGen().AddThinktectureOpenApiFilters(...)`): needs `Microsoft.AspNetCore.Mvc`, `Swashbuckle.AspNetCore`, and the Thinktecture AspNetCore and Swashbuckle packages, which the lab does not reference. Identifiers verified against decomp:aspnetcore and decomp:swashbuckle.
- EF Core registration (`services.AddDbContext<ShopDbContext>(...)`, `Configuration` with `FixedSmartEnumMaxLengthStrategy`): needs EF Core, a provider, and the EntityFrameworkCore10 package. Identifiers verified against decomp:entityframeworkcore10.
- MessagePack and Newtonsoft appear in prose only (`ThinktectureMessageFormatterResolver.Instance`, `ThinktectureNewtonsoftJsonConverterFactory`), no block.

## Lab rulings

Lab: `REPO/.scratch/lab-t01` (`LabT01.csproj`, `Program.cs`, `T01/*.cs`). `dotnet build LabT01.csproj -nologo -v q` succeeded with 0 warnings and 0 errors, `dotnet run --project LabT01.csproj --no-build -nologo` exited 0. Every doc block is the corresponding `T01/<Section>.cs` file minus the `namespace Lab.T01;` line; `Samples.cs` and `Program.cs` are scaffolding.

- CS0246 `INumber<T>`: `System.Numerics` is not a global using in the repo; the block spells `System.Numerics.INumber<T>`.
- CS8601 (nullable) on `new(() => [Processing, Cancelled])` in a static field initializer: replaced with `Lazy<T>` over private static methods (`PendingNext`, ...).
- EPC20 (ErrorProne) on generated `Switch`/`Map` default arms of keyless Smart Enums (`$"Unknown item '{this}'."` with the default `object.ToString`): keyless `MoneyRoundingStrategy` gained a `Name` member and a `ToString` override. `SalesCsvImporterType` was dropped from the doc block and its `DelegateName` facts moved to prose.
- MA0193: `decimal.Round(d, 2)` needs a `MidpointRounding` argument; `Nearest` uses `MidpointRounding.ToEven`.
- CA1812 on the accessor class (never instantiated): the comparer implements `IEqualityComparerAccessor<string>` and `IComparerAccessor<string>` itself and is its own accessor.
- CA1812 on `SmsSender`: the lab scaffolding resolves it through the test `IServiceProvider`; block unchanged.
- IDE0290: primary constructors on `IsoCoded(int numericCode)` and on the nested `Typed<TSender>(string key) : NotificationChannel(key)`; TTRESG043 did not fire on the derived class.
- IDE0040: `public` on the interface member `INotificationSender.Send`.
- MA0172 on `!reader.Read() || !reader.Read()` and IDE0046 on `if (...) throw; return ...`: the converter uses a private static `Advance(ref Utf8JsonReader)` helper.
- MA0002, MA0003 (scaffolding only): `SequenceEqual(..., StringComparer.Ordinal)` and `provider: null` in `Samples.cs`.

## Open questions

- Whether the shared lab has global usings for `System.Text.Json` and `Serilog`: the repo has none, so the JSON and Serilog blocks spell `System.Text.Json.*` and `Serilog.*` names in full and use `WriteTo.Sink` instead of the `WriteTo.Console()` extension.
- `ThinktectureSchemaFilterOptions` defaults are `FromDependencyInjection` for both properties (decomp:swashbuckle); the wiki calls `None` the default extension. The doc states the observable default schema (`enum: [...]`, no extension) and not the option default.
- The MVC model error text for an unknown value was not found in decomp:aspnetcore; the doc says "adds a model error" without quoting the wiki's `The enumeration item of type ... is not valid.` text.

## Shared lab rulings

- Host-only fences excluded from the lab: the MVC and Swashbuckle registration fence, the EF Core registration fence. Every identifier in them is checked against `S/decomp/aspnetcore`, `S/decomp/swashbuckle`, `S/decomp/entityframeworkcore10`.
- `EPC20` (ErrorProne.NET, a repo analyzer) is the repo-specific reason a keyless Smart Enum in this lab carries a `ToString` override. The doc states the library fact only: no generated `ToString`, so the type name renders.
- `TTRESG1001` fires on any non-`static` lambda argument, capture or not, and is an error in this repo.
- B item 14 also repairs these notes: `HasThinktectureValueConverter` exists on `PropertyBuilder<T>`, `ComplexTypePropertyBuilder<T>`, and `PrimitiveCollectionBuilder<T>`. Keyless `System.Text.Json`: `Serialize` writes the public instance properties (`{}` for a type without any), and `Deserialize` throws `NotSupportedException`.

## Repo form

- `Lookup.Admit` is generic over `TError : Error, IValidationError<TError>` and returns the typed `Expected` that `[ValidationError<TError>]` names, in place of `Error.New(error.Message)`; `Find` keeps the `ValidationError` constraint.
- The fence declares `Tier` with `[ValidationError<UnknownTier>]` and `UnknownTier : Expected, IValidationError<UnknownTier>` so the sample has a smart enum whose error is an `Error`; `Samples.cs` checks `IsType<UnknownTier>()`, `HasCode(1)`, and that `Get` still throws with the key in its message.
- The `Get` design rule names `[ValidationError<TError>]` with a typed `Expected` record before `Validate` mapped to `Fin<T>`.