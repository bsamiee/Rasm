# [WF1_INVESTIGATION_RESEARCH_VALIDATE]

Complete generative parametric capture per buildable component family for the `element-component` rebuild — RESEARCH (full field set + authoritative seed-table rows + `assay api` member truth) paired with VALIDATE (member ground-truth re-verification, `validatedSpec` verdict, phantom flags). All families hand-roll generative data in-fence per `CATALOG_LAW [4]`; no family admitted a new NuGet pin.

## CMU

Owner: `libs/csharp/Rasm.Materials/.planning/Profiles/cmu.md`. `CmuSection` readonly record struct grows the typed-axis SmartEnums (one row per variant, never per-block types) plus an explicit per-cell list.

Fields (full parametric set):
- Typed axes: `CmuStandard {astm-c90 loadbearing-US, astm-c129 non-loadbearing-US, en-771-3-group1 (≤25% formed voids), en-771-3-group2 (>25% voids)}` carrying `Hollow bool`, `LoadBearing bool`, `IfcSubtype`; `CmuStrength {F1700,F1900,F2000,F2250,F2500,F2750,F3000}` (TMS 602-16/-22 Table 2) carrying `FmMpa`, `NetUnitTypeMSMpa`, `NetUnitTypeNMpa` + `Resolve(netUnitStrengthMpa, MortarType)` inverting the unit-strength method; `CmuDensity {lightweight <1680, medium 1680–2000, normal ≥2000 kg/m³}` carrying `OvenDryKgPerM3` (all classes share min net-area f'c 13.8 MPa); `CmuAggregate {siliceous-gravel, calcareous-limestone, expanded-clay-shale-slate, expanded-slag-pumice}` carrying `OneHourThicknessMm` cn {71.1/68.6/66.0/53.3}; `CmuSpecialShape {stretcher, corner, double-corner-end, bond-beam, knockout-bond-beam, lintel, sash, control-joint, open-end(A), double-open-end(H), pilaster-column, half, cap-solid-top, sill, kerf-allpurpose}` with web-modifier + grout-confinement flags; `CmuFinish {smooth, split-face, scored, ribbed-fluted, ground-face-burnished, slumped}`.
- Section fields: `Standard/Strength/Density/Aggregate/Special/Finish` typed receipts; `ActualWidthMm/ActualHeightMm/ActualLengthMm : PositiveMagnitude` (nominal − 9.5mm; design on MINIMUM specified dims); `FaceShellMinMm` (ASTM C90 Table 1 by width 19.1/25.4/31.8; 16mm solid-grouted); `FaceShellFlareMm` (non-structural top-flare, excluded from net section); `EndWebMinMm` + `CrossWebMinMm` (the `WebThicknessMm`→`EndWebMm`/`CrossWebMm` rename; ASTM/CCMPA end vs centre web); `WebDraftDegrees/CellDraftDegrees`; `NormalizedWebAreaMm2PerM2` Anw (min 45,140 mm²/m²); `Cells : Seq<CmuCell>` (replaces scalar `CellCount`); `BedJointMm` (default 9.5); `HeadJointMm` (NEW, default 9.5 — the 3D head joint the old page lacked); `GroutDensityKgPerM3` (NEW, default 2243, replaces hardcoded 2240.0).
- `CmuCell` per-cell record (replaces homogeneous `GroutFraction` smear): `CenterXMm`, `WidthMm`, `LengthMm`, `TaperWiderLengthMm/TaperNarrowerLengthMm`, `Grouted : bool`, `Reinforced : bool` + `RebarBarMm : Option<double>`.
- Finish geometry: `SplitDepthMm`; `ScoreCount + ScoreSpacingMm + ScoreDepthMm`; `RibCount + RibProjectionMm + RibSpacingMm` (all ±1.6mm molded tolerance).
- Derived projections: `NetSection(key)/GroutedSection(key) : Fin<ComputedSection>` (exact net section via `ParametricSection.Hollow` polygon-integral over explicit per-cell voids); `GroutedCellFraction()` (rename `GroutFraction`→`GroutedCellFraction`, now DERIVED from per-cell booleans); `EquivalentThicknessMm()` = Vn/(L×H); `FireRatingHours()` = clamp((te/cn)^1.7, 0, 4) — POWER LAW replacing the old linear te·RatingFactor (1.7 = inverse of the 0.59 in ACI 216.1 multi-wythe form); `SelfWeightKnPerM2()` (Density over net solid + GroutDensity over grouted cells); `RValueM2KPerW()` (NCMA TEK 6-2C series-parallel/isothermal-planes multi-cell R — NEW); `PrismStrength()`, `ToCoring()`, `ToUnit(ctx,key)`.

Seed-row coverage gathered (transcription-complete): ASTM C90-14 Table 1 (face-shell + web min + Anw by nominal width); ASTM C90-14 Table 2 density classification (all classes min net-area f'c 2000 psi/13.8 MPa); standard hollow CMU dimensional rows `cmu.4in`…`cmu.16in-hollow` + solid (actual = nominal − 3/8in; 2-cell = 2 end + 1 cross web); TMS 602-16/-22 Table 2 unit-strength method (F1700–F3000, Type M/S vs Type N columns — REPLACES the stale pre-2013 values, 2000psi unit + Type S ⇒ f'm=2000); NCMA TEK 7-1C / ACI 216.1 Table 1 fire equivalent-thickness te by aggregate (power-law cn rows); NCMA TEK 7-1C Table 2 typical te + %-solid by width; densities (grout 2243, mortar 2003, unit LW/MW/NW); EN 771-3:2011+A1:2015 (Group 1 ≤25% / Group 2 >25%, 440×215, declared strengths 3.6–18 N/mm², Euroclass A1); special-unit web/grout-confinement geometry; finish geometry rows.

Verified members (`assay api`, restored VividOrange lib/net8.0): `VividOrange.Profiles.American` (2299 AISC v16.0 typed designations, decompile 2304 lines); `VividOrange.Profiles.European` (~558 EN 10365, 563 lines); `CatalogueFactory.CreateAmerican(American)→ICatalogue` / `CreateEuropean(European)→ICatalogue` (body mints typed singletons); sample singleton `W44x408 : SingletonAmericanBase<W44x408>, IIParallelFlange, II, IProfile, ITaxonomySerializable` (UnitsNet geometry, `Shape=>AmericanShape.W`, `Label="W44X408"`); `VividOrange.Sections.SectionProperties.SectionProperties : ISectionProperties` elastic-only members `{Area, MomentOfInertiaYy/Zz, ElasticSectionModulusYy/Zz, RadiusOfGyrationYy/Zz, Perimeter, Centroid}` (the path `ParametricSection.Hollow` composes for the Green's-theorem polygon integral with void subtraction). `SteelStiffness` CONFIRMED ABSENT (`assay api query SteelStiffness --key VividOrange.Sections.SectionProperties` → status=unsupported) — it is the hand-rolled `Rasm.Materials.Profiles.Steel` design-fence static; the `Wpl/It/Iw/Av` gap is genuine (SectionProperties is elastic-only).

VALIDATE verdict: VALIDATES — all 6 VividOrange members assay-confirmed verbatim, `SteelStiffness` assay-confirmed absent, `[4]` split honored, `pinsNeeded=[]` correct. Every CMU field maps to the `[2]` mandate with correct standard values; FIX-1 power-law (1.7=1/0.59) and FIX-2 grout-density (2243) accurate; TMS 602-16/-22 strength revision current.

Phantoms: NONE (empty). Three doc/design-accuracy flags (not phantoms, not value-unsupported): (1) IFC-subtype fidelity gap — `CmuStandard.IfcSubtype` maps hollow units to `IfcRectangleHollowProfileDef` (single-uniform-void), which cannot carry the multi-cell web geometry the spec models via `Seq<CmuCell>` + EndWeb/CrossWeb; the faithful profile is `IfcArbitraryProfileDefWithVoids` (reserve `IfcRectangleHollowProfileDef` for genuine single-void shapes) — a wrong-choice egress mapping to resolve in the WF-1 seam; (2) strength-standard citation — `CmuStrength` values are authored by TMS 602-16/-22 Table 2, NOT in `[2]`'s bare CMU citation ("ASTM C90/NCMA TEK; EN 771-3"); the page must cite TMS 602; (3) seed-anchor inconsistency — the CCMPA net-section anchor "(190mm/8in unit): gross 93,600 / net 49,600 / void 20,600 mm²" is internally inconsistent (49,600+20,600=70,200 ≠ 93,600) and 93,600 mm² corresponds to a 240mm unit not 8in; reference-only (model computes from explicit per-cell geometry, does not propagate) but the seed should carry a consistent gross/net/void triple.

pinsNeeded: NONE.

## brick/masonry

Owner: `libs/csharp/Rasm.Materials/.planning/Profiles/masonry.md`. Current page owns bond/orientation/cut/closure/special-shape/mortar-type/mortar-profile vocabulary plus a scalar `Coring` void-fraction band; the rebuild ADDs/DEEPENs seven generative axes.

Fields (full parametric set):
- Size: `UnitSize.WorkSizeWidth/Height/LengthMm` (manufacturer-declared work/specified size the solid generates from); `CoordinatingWidth/Height/LengthMm` (work size + 1 joint, CO module; UK 215×102.5×65 → 225×112.5×75 at 10mm); `NominalWidth/Height/LengthMm + ModularGridIncrementMm` (US 4-in modular grid, BIA TN 10); `ToleranceCategory {T1,T2,Tm}` + per-dimension ± limit mm (EN 771-1 mean-size deviation); `RangeCategory {R1,R2,Rm}` + per-dimension max-minus-min mm; `VerticalCoursing` module (3C=203, 5C=406, 1C=102, Roman 2C=102).
- Frog (currently ABSENT): `FrogKind {none, single, double}`; `FrogDepthMm` (10–20mm; deep frog >9.5mm counts as void per ASTM C652); `FrogFootprintWidthMm × FrogFootprintLengthMm + FrogInsetMm` (~100×40mm recess inset); `FrogTaperDegrees` (de-mould draft).
- Voids/perforation (REPLACES scalar `Coring.VoidFraction`): `VoidClass {solid, cored, cellular, perforated, hollow}`; `CoreCount` + per-core (X,Y) grid + `CoreDiameterMm` (round) or `CoreWidthMm × CoreLengthMm` (rect) — core = void ≤968mm² (1.5 in²); `CellCount` + per-cell (X,Y) + `CellWidthMm × CellLengthMm` — cell = void >968mm² (reinforcable); `VoidFraction` (derived); `MinFaceShellMm` by width (solid 19.1/25.4/32/35/38; cored-or-double N/A/38/38/41/51); `MinWebMm` (void-to-cell 15.9, void-to-core 19.1); `MinVoidToEdgeMm` (exposed 6.4 core / 9.5 cell; unexposed 12.7); `MinEndShellMm` (Class H60V extra, 19.1/25.4/25.4/29/29).
- Mortar joint: `MortarJointProfile {concave, vee, weathered, struck, flush, raked, beaded, extruded, grapevine, prism, weeping}`; 3D recess `RecessDepthMm + ProfileAngleDegrees + ProfileRadiusMm` (REPLACES scalar `DepthFactor`); `HeadJointWidthMm` + `BedJointWidthMm` (US 9.5/12.7, UK 10); `JointWeatherTight bool + ShadowLine`.
- Bond/packing: `BondName {running/stretcher, common-american, english, flemish, stack, english-cross/dutch, flemish-garden/sussex, monk, herringbone, basketweave, pinwheel, diaper}`; `BondCourseTemplate` per course (Orientation sequence + OffsetFraction + PerUnitRotationDegrees); `PackingCell {RepeatUnits, BlockUnits, RotationDegrees, AspectBand[lo,hi]}`; `HeaderFrequency`; `Orientation {stretcher, header, soldier, sailor, rowlock, shiner}` with RunFraction/RiseFraction/FaceShown.
- Cuts: `CutAxis {transverse(bat), longitudinal(closer), diagonal(king/bevel/mitre)}`; `Cut.Bat {half, three-quarter, quarter, bevelled-bat}`; `Cut.Closer {queen-closer-half, queen-closer-quarter, king-closer, bevelled-closer, mitred-closer}`; `CutPlanePositionFraction + CutPlaneAngleDegrees + CutPlaneNormalAxis`.
- Special shapes/arches: `SpecialShape {none, bullnose-single, bullnose-double/cownose, plinth, coping(saddleback/half-round/featheredge/wash), cant, squint, birdsmouth, voussoir, capping}` (BS 4729) with RadiusFraction/ChamferDegrees/SetbackFraction/WashFraction; `ArchType {jack, segmental, semicircular, multicentered, horseshoe, gothic, elliptical, triangular, relieving, bullseye}`; `VoussoirTaperDegrees + VoussoirMethod {tapered-unit, rectangular-unit, slanted}` (REPLACES identity-only `Voussoir`); `SkewbackAngleDegrees`; `ArchRise/Span/Depth/RadiusMm + CamberMm`.
- Classification: `Grade {SW, MW, NW}`; `Type` facing-C216 `{FBS, FBX, FBA}` / hollow-C652 `{HBS, HBX, HBA, HBB}`; `Class` hollow-C652 `{H40V, H60V}`; EN density `{HD >1000, LD ≤1000 kg/m³}`; EN configuration `Group {1 ≤25%, 2 >25–55% vertical, 3 horizontal-perforated, 4}`; EN freeze-thaw `{F0, F1, F2}` + soluble-salts `{S0, S1, S2}`; `Body material {fired-clay, shale, calcium-silicate}`; `MortarType {M,S,N,O,K}` (ASTM C270 17.2/12.4/5.2/2.4/0.5 MPa) + EN 998-2 M-class + proportions + flexural-bond; `NetAreaCompressiveStrengthMpa/GrossAreaStrengthMpa` (C652 SW 20.7 / MW 17.2 MPa; net = 1.33–1.66× H40V, 1.66–2.5× H60V).

Seed-row coverage gathered (transcription-complete): US modular brick (BIA TN 10 Table 1, full set modular…double-meridian); US non-modular (Table 2, standard/king/queen); regional sizes (UK EN771-1/BS, DIN NF/DF/2DF, AU AS 4773, IS 1077); EN 771-1 tolerance/range limits (T1/T2/R1/R2 derived); ASTM C652 cross-sectional minimums (BIA TN 41 Table 3); void/coring classification thresholds (core ≤968mm² / cell >968mm² / deep-frog >9.5mm); mortar joint profiles (IMI 01.410.1001 / BIA TN); bond patterns + decorative packing cells (BIA TN 30); arch types + voussoir taper rules (BIA TN 31); grades/types/classes (ASTM + EN).

Verified members (`assay api`): `VividOrange.Profiles.CatalogueFactory.CreateAmerican(American)→ICatalogue` / `CreateEuropean(European)→ICatalogue` (verbatim switch minting typed singletons); `American` (~2299), `European` (exactly 558, e.g. `IPEAA80`…`CH432x102x65`); `AmericanShape` (13 verbatim), `EuropeanShape` (25 verbatim); geometry interfaces `II/IChannel/ITee/IAngle/IDoubleAngle/IRectangularHollow/ICircularHollow/IHollowStructuralSection` all present; `SectionProperties` elastic-ONLY (Area, MomentOfInertiaYy/Zz, ElasticSectionModulusYy/Zz, RadiusOfGyrationYy/Zz, Perimeter, Centroid, Extends + backing fields). `SteelStiffness` is the hand-rolled `Rasm.Materials.Profiles.Steel` design-page static (`Derive` + OpenShape/ClosedShape/RoundTube/ClosedRectJ/SolidShape) — NOT a compiled member; `Wpl/It/Iw/Av` gap genuine. Brick/masonry generative data has NO VividOrange/NuGet owner.

VALIDATE verdict: VALIDATES — capture survives ground-truth attack. Every VividOrange member assay-confirmed (American exactly 2299, European exactly 558, AmericanShape 13, EuropeanShape 25, 8 geometry interfaces, SectionProperties elastic-only with decompile grep for plastic|torsion|warp|venant|shear returning none); `[4]` split hard-confirmed (VividOrange family is entirely steel/loads/concrete — no masonry surface); nearly all 44 fields grounded in the cited `[2]` standards; the capture's IS 1077 seed (190×90×90 / 230×110×70) CORRECTS the page's wrong IS row (100×70×200).

Phantoms: `MortarJointProfile.prism` — UNSUPPORTED by the cited joint-profile sources (IMI 01.410.1001 / BIA TN). A masonry "prism" is an ASTM C1314 compressive-strength TEST SPECIMEN (stacked-unit assemblage), NOT a tooled mortar-joint finish; no IMI/BIA tooled-joint family contains a "prism" profile. DROP this case. Build corrections (not phantoms): re-grade `JointWeatherTight` for beaded to NOT weather-tight (ledge → poor/fair); collapse `extruded`+`weeping` into one squeezed-joint case (weeping is the drooping variant); treat EN 771-1 T1/T2/R1/R2 per-dimension mm values as derived (round-consistent with the v-formula) and transcribe exactly.

pinsNeeded: NONE.

## glazing IGU

Owner: `libs/csharp/Rasm.Materials/.planning/Profiles/glazing.md`. Existing page models EN 673 center-of-glass Ug, EN 410 solar/light transmittance, and a mass-law acoustic spectrum well; the rebuild expands four SmartEnum axes + Pane/Cavity records and adds three NEW vocabularies (Interlayer, Muntin/Grid, VIG). Composes NO VividOrange member (an IGU is `IfcMaterialLayerSet`, not `IfcProfileDef`).

Fields (full parametric set):
- `GlassType` (axis): `NormalEmissivity`, `ConductivityWmK`, `DensityKgM3` (NEW, replaces 2500.0 literal in `MassLawSpectrum`/`GlazingGwp`), `SpecificHeatJKgK` (NEW FIX, replaces hardcode 840.0), `GwpPerKg` (NEW FIX, replaces flat `GlassGwpPerKg=1.20`), `ProcessingGwpAdderPerM2` (NEW FIX), `BaseSolarTransmittance`, `BaseSolarReflectanceFront/Back` (NEW spectral), `BaseSolarAbsorptance` (NEW, α=1−τ−ρ), `BaseLightTransmittance`, `BaseLightReflectance` (NEW), `Laminated bool`, `Safety bool`, `FireClass {None,E,EW,EI}` (NEW FIX, replaces hardcoded `FireRating.A1`), `FireDurationMin {0,15,20,30,45,60,90,120,180}` (NEW FIX, replaces hardcoded `FireResistance.Ei(30)`); cases float, low-iron, tempered, heat-strengthened, laminated, fire-rated (KEEP) + NEW ceramic-fire, intumescent-fire, electrochromic, vig-lite.
- `Coating`: `CorrectedEmissivity : Option<double>` (NEW TYPE, replaces `double.NaN` sentinel; None → glass NormalEmissivity), `SolarMultiplier`, `LightMultiplier`, `SolarReflectanceUplift` (NEW spectral), `GwpAdderPerM2` (NEW ~2.67 kgCO2e/m²), `Process {pyrolytic-hardcoat (ε~0.16–0.20), sputtered-softcoat (ε~0.02–0.04)}` (NEW); cases none, hard-coat-lowe, soft-coat-double-silver, soft-coat-triple-silver, solar-control.
- `CavityGas`: `ConductivityWmK/DensityKgM3/ViscosityPaS/SpecificHeatJKgK` (EN 673 Annex B); cases air, argon, krypton, xenon (+ optional sf6). `Cavity`: `Gas`, `WidthMm : PositiveMagnitude` (6–24), `FillFraction` (NEW, EN 1279-3 ≥0.90 nominal / ≥0.80 weathered), `BalanceGas` (NEW, air balance for the 1−FillFraction remainder; volume-weighted mixture conductance), `VacuumPressurePa : Option<double>` (NEW VIG, <0.1 Pa; None=gas-filled).
- `Pane`: `Glass`, `ThicknessMm : PositiveMagnitude` (EN 572-2 nominal {2…25}), `Coating`, `CoatedSurface : int` (0=outboard/1=inboard), `FaceWidthMm/FaceHeightMm : PositiveMagnitude` (NEW, replaces `FrameRebateModuleMm=1200.0` placeholder), `Interlayer : Option<Interlayer>` (NEW TYPE, replaces scalar `InterlayerMm`).
- `Interlayer` (NEW VOCAB): `Kind {pvb, ionoplast-sgp, eva, tpu, pet, cast-resin, fep}`, `PlyThicknessMm`, `PlyCount`, `DensityKgM3`, `ShearModulusMpa` (PVB ~1–10, SGP ~100–300), `AcousticBonusDb` (replaces `InterlayerMm>0` +3dB literal), `RefractiveIndex`.
- `SpacerType`: `Material {aluminum, stainless-steel, hybrid-steel-plastic, thermoplastic-tps, foam}`, `PsiWmK` (EN ISO 10077-1 ψg), `ConductivityWmK` (NEW), `WallThicknessMm` (NEW), `SightlineDepthMm` (NEW ws 6.0–9.3). `Spacer`: `PrimarySealKind {pib} + PrimarySealThicknessMm` (NEW 0.2–0.6), `SecondarySealKind {silicone, polyurethane, polysulfide, hot-melt-butyl, epoxy} + SecondarySealWidthMm` (NEW ~4), `Desiccant {molecular-sieve-3a-zeolite, silica-gel, integrated-tps}` (NEW), `CornerConstruction {bent-and-keyed, corner-key-injected, extruded-continuous}` (NEW).
- `MuntinGrid` (NEW VOCAB): `Mode {none, tdl, sdl, sdls, gbg, interior}`, `BarWidthMm`, `BarDepthMm`, `BarProfile {ogee, square, colonial}`, `Pattern {colonial-grid, prairie, diamond, six-over-six, cathedral, custom}`, `Material {aluminum, pvc, wood, internal-spacer-bar}`.
- `Vig` (NEW VOCAB): `PillarDiameterMm` (~0.3–0.6), `PillarHeightMm` (0.15–0.30 = vacuum gap), `PillarSpacingMm` (~20–40), `PillarPattern {uniform-grid, graded-center-to-edge}`, `PillarMaterial {metal, ceramic}`, `EdgeSeal {solder-glass-frit, metal-indium}`, `EvacuationPort {pump-out-tube-capped, tubeless-chamber}`, `Getter bool`.
- `GlazingBuild` derived from `Panes.Count` {double=2, triple=3} + NEW quad=4 (VIG is a lite/cavity variant, not a parallel owner). `GlazingThermal` EN 673 surface films he=23, hi=8; σ=5.67e-8; Tm=283K; ΔT=15K; Nu=max(1, 0.035·Ra^0.38) (KEEP).

Seed-row coverage gathered: GLASS TYPE table (EN 572-1 physical + EN 410/ISO 9050 optical + EN 15804 GWP + EN 13501-2 fire — float/low-iron/tempered/heat-strengthened/laminated/fire-rated-intumescent/ceramic-fire/electrochromic); EN 572-2 nominal thicknesses; COATING table (EN 673 ε override + EN 410 multipliers + EN 15804 adder); CAVITY GAS table (EN 673 Annex B at 283K) + GAS FILL (EN 1279-3 / ASTM E2190 concentration, mixture conductance); INTERLAYER table (EN 12543/EN 14449); SPACER table (EN ISO 10077-1 ψ + LBNL/H.B.Fuller) + EDGE SEAL + DESICCANT + CORNER; MUNTIN/GRID (TDL/SDL/GBG bar widths); VIG (ISO 19916 / EN 1279 pillar geometry); GWP DECOMPOSITION (EN 15804 split 78% lites / 12% fabrication / 10% coating+heat-treat); STANDARD IGU BUILDS (EN 1279, double/triple rows + NEW vig + fire builds).

Verified members (`assay api`): `VividOrange.Sections.SectionProperties.SectionProperties` (v0.1.0, lib/net9.0) — `Centroid(ILocalPoint2d)`, `Perimeter(Length)`, `Extends(ILocalDomain2d)`, `Area(Area)`, `ElasticSectionModulusYy/Zz(Volume)`, `MomentOfInertiaYy/Zz(AreaMomentOfInertia)`, `RadiusOfGyrationYy/Zz(Length)`; ctors `(IProfile)`/`(ISection)` — EXPOSES ONLY ELASTIC COLUMNS (no plastic modulus, torsion, warping, shear area — the gap `SteelStiffness` fills). `VividOrange.Profiles.Catalogue` 2867 typed-designation classes + `CatalogueFactory.CreateAmerican/CreateEuropean`. `VividOrange.Materials` 13 types (steel/concrete EN grade data only) — NO glass/glazing/IGU type, confirming glazing is correctly hand-rolled. `SteelStiffness` is the Rasm-local hand-rolled gap-filler (steel sibling composes it; glazing does not). This VividOrange/SteelStiffness verification is the campaign-wide owns-vs-hand-roll confirmation, not a glazing.md citation.

VALIDATE verdict: structurally sound and correctly scoped — hand-rolled in-fence per `[4]`, composes no VividOrange member, four FIX items + three NEW vocabularies map directly onto the `[2]` gap list. NO external-package phantom members; the EN 673 Ug chain, EN 410/ISO 9050 optics with α=1−τ−ρ energy balance, mass-law acoustic spectrum, EN 13501-2 fire, Option-emissivity, EN 1279-3 fill fraction, EN 12543/EN 14449 interlayer, ISO 19916 VIG, EN 572-1 density/conductivity, and EN ISO 10077-1 spacer datum are all standard-supported. The defects are standard-citation/value issues, not member existence.

Phantoms (standard-citation/value, correctness-critical for the fence): (1) `GlassType.SpecificHeatJKgK=840` attributed to EN 572-1 is UNSUPPORTED — EN 572-1 specifies soda-lime c = 720 J/(kg·K), not 840 (840 is a generic non-EN materials-database figure); the current fence hardcode 840.0 is equally non-EN; the FIX must set 720 (EN 572-1) or recite the real source, not claim EN 572-1 for 840. (2) GWP DOUBLE-COUNT in the seed — per-GlassType `GwpPerKg` is inflated by processing for same-substance float-derived panes (tempered 1.95 / heat-strengthened 1.80 / laminated 1.70 / fire-rated 2.10 vs float 1.43) AND a separate `ProcessingGwpAdderPerM2` (tempered +~6, coated+tempered +~11) is added on top, counting heat-treat/laminate carbon twice; the per-kg base must stay the raw-substance float value (~1.43), with low-iron and borosilicate the only legitimate substance variants, and ALL secondary-process carbon confined to the per-m² adders. (3) `ceramic-fire` (borosilicate) seed conductivity λ=1.40 W/mK contradicts the field's own stated borosilicate ~1.1–1.2 (real ~1.14; 1.40 is glass-ceramic territory, e.g. Robax/Keralite, not borosilicate SCHOTT Pyran). (4) `MuntinGrid` bar widths (16/22/29/49/61 mm) cited as "EN/ASTM divided-lite" are NOT standard-prescribed — no EN/ASTM standard publishes a muntin bar-width table; these are manufacturer product dimensions, so the standard attribution is unsupported. (5) EN 673 Annex B drift (KEEP-value soft flag) — `CavityGas` air conductivity 0.0241 and argon viscosity 2.10e-5 deviate from EN 673 Annex B at 283K (air λ ~0.0250, argon μ ~2.164e-5); reconcile against the exact Annex B table. (6) Trivial — the `VividOrange.Profiles.Catalogue` breakdown "2299 American + 558 European" sums to 2857, not the verified total 2867 (off by 10; total correct, split not).

pinsNeeded: NONE.

## fastener

Owner: `libs/csharp/Rasm.Materials/.planning/Connection/fastener.md`. Page already owns the thread axis, grade bands, single-bolt capacity family, and the bolt+nut+washer preload/slip assembly; the GAP is the dimensional layer a generator needs to build real bolt/nut/washer SOLIDS.

Fields (full parametric set):
- Thread-form: `FlankAngleDeg=60` (ISO 68-1, one constant for metric+UNC — GAP); `MajorDiameterMm(d)`, `PitchMm(P)`, `PitchDiameterMm(d2=d−0.649519P)`, `MinorDiameterExternalMm(d3=d−1.22687P)` (EXISTING); `MinorDiameterInternalMm(D1=d−1.08253P)` (GAP, nut basic minor); `ToleranceClass(6g external / 6H internal)` per ISO 965-1 (GAP); `TensileStressAreaMm2(As=(π/16)(d2+d3)²)`, `NominalAreaMm2((π/4)d²)` (EXISTING derived).
- Head: `AcrossFlatsMm(s)` (EXISTING); `AcrossCornersMinMm(e≈1.1547s)` (GAP); `HeadHeightMm(k)` (GAP); `BearingFaceDiaMm(dw)`, `TransitionDiaMm(da)`, `ShankDiaMm(ds≈d)` (GAP, beyond `[2]` named set but ISO 4014-real).
- Length: `NominalLengthMm(L)` (EXISTING, carried as `ShankLengthMm` — semantic misnomer); `ThreadLengthMm(b)` = 2d+6 (L≤125) / 2d+12 (125<L≤200) / 2d+25 (L>200) ISO 4014, ≈L ISO 4017 (GAP); `ThreadRunoutMm(u≈2P)` (GAP); `UnthreadedShankMm(=L−b)` (GAP, selects shank-vs-thread shear plane).
- Nut: `NutAcrossFlatsMm(s)`, `NutThicknessMm(m)` (GAP); `NutAcrossCornersMinMm(e)` (GAP, beyond `[2]`).
- Washer (wholly absent from page): `WasherInnerDiaMm(d1)`, `WasherOuterDiaMm(d2)`, `WasherThicknessMm(h/t)`, `WasherHardnessHV` (200 HV up to grade 8.8 / 300 HV up to 10.9) — ISO 7089.
- Grade (EXISTING): `ProofStressMpa(Sp)`, `TensileStrengthMpa(Rm=fub)`, `MinimumYieldMpa(Re=fyb)`, `ShearStrengthFactor(αv, EN 1993-1-8 Table 3.4)`, `Preloadable` (8.8/10.9/A325/A490 only).
- Axes (EXISTING): `FastenerKind {bolt/nut/screw/anchor/dowel/rivet/coupler}`, `FastenerStandard {ISO 898-1 / SAE J429 / ASTM F3125}`, `BoltCategory {EN 1993-1-8 A/B/C/D/E}` (cites `En1993Part.Part1_8`), `FayingSurface {slip A/B/C/D μ=0.50/0.40/0.30/0.20}`. Assembly (EXISTING `FastenerAssembly`): `GripPlies + ShearPlanes`, `WithWasher`, `PreloadKn(Fp,C=0.7·fub·As)`, `SlipResistanceKn` — grow `WithWasher` → washer `ThreadSize` ref + count for the complete-assembly geometry.

Seed-row coverage gathered (transcription-complete): SEED A (ISO 261/724 metric-coarse thread M6–M36: d/P/d2/d3ext/D1int + ISO 4014/4017 head s/e/k/dw + ISO 4032 nut m); SEED B (ISO 7089 plain washer, normal series, grade A, 200 HV, M6–M36: d1/d2/h; 300 HV variant identical dims); SEED C (ISO 898-1 / SAE J429 / ASTM F3125 grade bands — CONFIRMS the page table row-for-row); SEED D (ASME B1.1 UNC inch, page's existing rows; inch As=0.7854(d−0.9743/n)²); THREAD-LENGTH RULE (ISO 4014 b verified M12→30/36/49, M20→46/52/65).

Verified members (`assay api`, decompiled 0.1.0): `CatalogueFactory.CreateAmerican(American)→ICatalogue` / `CreateEuropean(European)`; `American` (2299 AISC v16.0), `European` (558 EN 10365), `AmericanShape` (13), `EuropeanShape` (25); `SectionProperties` elastic-only (no `Wpl/It/Iw/Av` → `SteelStiffness` gap REAL); `EnSteelFactory.CreateLinearElastic(IEnSteelMaterial[, Length])` (Table3_1 fy/fu thickness bands); `EnSteelGrade {S235,S275,S355,S420,S450,S460}` — structural-member grades ONLY, ZERO ISO 898-1 / SAE J429 / ASTM F3125 bolt property classes → `FastenerGrade` bands MUST be hand-rolled in-fence; `En1993Part.Part1_8 [Description("Joints")]` present (the `BoltCategory` cite). `SteelStiffness` hand-rolled (not a member). One out-of-scope steel-family residual: `steel.md` `SteelGrade` omits the real `EnSteelGrade` member `S450`.

VALIDATE verdict: VALIDATED — decompile-accurate, NO phantom members, no field unsupported by the cited standard SET, all SEED tables transcribe correctly (metric As M12=84.3mm² exact; SEED C confirms the page grade table row-for-row). `[4]` split fully confirmed; `pinsNeeded=[]` correct (no .NET package owns ISO 898-1 bolts; GeometryGym is IFC-mirror-only).

Phantoms: NO PHANTOM MEMBERS. Four flags (correctness-critical): (1) PAGE GROUND-TRUTH CONTRADICTION — `fastener.md` line 54 maps `FastenerKind.Coupler→ifcPredefinedType:'USERDEFINED'` and line 330 asserts COUPLER "is not in the predefined enum", but `assay api` confirms `IfcMechanicalFastenerTypeEnum.COUPLER` IS a real member (VersionAdded IFC4X2); a bar coupler must land the `COUPLER` token, not `USERDEFINED`; the line-330 "VERIFIED" enum list (13 members) is also INCOMPLETE (omits `RAILJOINT/RAILFASTENING/CHAIN/ROPE`, IFC4X3); `Nut→USERDEFINED` is correct (no `NUT` member). (2) The capture's nut-e annotation says "[2] names nut s/e" but `[2]` names nut (s/m) — nut across-corners `e` is ISO 4032-real but beyond `[2]`'s named scope. (3) `dw/da/ds` exceed `[2]`'s head enumeration (s/e/k/b/u) yet are ISO 4014-real and honestly flagged. (4) INCH As DISCREPANCY — the page applies the ISO `As=(π/16)(d2+d3)²` uniformly to inch UNC rows, overestimating ~3% vs the ASME B1.1 Unified `As=0.7854(d−0.9743/n)²` (3/8-16: ~51.6 vs 50.0 mm²); inch ProofLoadKn/TensileLoadKn run ~3% high — branch the inch series to the Unified formula. Also noted: `ShankLengthMm` is used as overall length L (semantic misnomer pending the b/shank split); `EnSteelGrade.S450` is a genuine member `steel.md` omits (steel-family residual, out of fastener scope).

pinsNeeded: NONE.

## rebar

Owner: `libs/csharp/Rasm.Materials/.planning/Connection/reinforcement.md`. Page is strong on grades/sizes/usage/surface/RC-section; two real `[2]` gaps — rib/deformation geometry (absent) and the partial bend/hook schedule. VividOrange owns the RC section/reinforcement engine + EN grade data; rib geometry + ASTM/CSA tables + bend/hook schedule hand-rolled.

Fields (full parametric set):
- Designation/material (EXISTING): `BarSize` (#3..#18 / 10M..55M / EN-ISO 6..50mm), `NominalDiameterMm(d_b)`, `NominalAreaMm2(A_n=0.7854d²)`, `UnitWeightKgM(=0.006165d²)`, `RebarStandard {A615/A706/CSA-G30.18/EN-10080-ISO-6935-2} + Weldable bool`, `RebarGrade + MinimumYieldMpa(f_yk) + Option<EnRebarGrade>`, `RebarUsage` (IfcReinforcingBarTypeEnum role MAIN/LIGATURE/SHEAR/PUNCHING/EDGE/RING/ANCHORING/SPACEBAR/STUD) + `Stirrup flag`, `RebarSurface {TEXTURED, PLAIN}`.
- Rib/deformation geometry (NEW, the major `[2]` gap, ISO 6935-2 Table 3): `RelativeRibArea/f_R` (EC2/EN/fib min 0.035 d≤6 / 0.040 6<d≤12 / 0.056 d>12; f_R=A_R/(π·d·c) per ISO 15630-1); `TransverseRibHeightMm/a` (min 0.05·d uniform / 0.065·d crescent); `TransverseRibSpacingMm/c` (0.5d–0.7d for 6≤d<10; uniform 0.5d–1.0d, crescent 0.5d–0.8d for d≥10); `LongitudinalRibHeightMm` (≤0.15d); `RibFlankInclinationDeg/α` (≥45°); `RibInclinationDeg/β` (35–90° uniform / 35–75° crescent); `RiblessPerimeterFraction/Σf_i` (≤0.25·π·d); `RibPattern {UniformHeight, Crescent}`; `TransverseRibRowCount` (≥2); `LongitudinalRibCount` (0/1/2).
- Bend/hook (NEW/REFINED): `HookKind` splitting `DevelopmentHook{90,180}` (ACI Table 25.3.1) vs `StirrupTieHook{90,135}` (ACI Table 25.3.2) vs `SeismicHook{135 non-circular, 90 circular}` (ACI §25.3.4); `MinInsideBendDiameter` (db-multiple, REFINED — current single `BendFactor` conflates the two ACI tables: dev 6/8/10·d_b, stirrup 4·d_b #3-#5 / 6·d_b #6-#8); `MandrelDiameterMm` (NEW, EN 1992 §8.3 Table 8.1N / BS 8666 Table 2 / VividOrange `Link.MinimumMandrelDiameter` 4φ≤16 / 7φ>16); `StraightExtensionMm` (db-multiple + absolute floor, REFINED); `BendAngleDeg(90/135/180)` (EXISTING).
- Scheduling: `ShapeCode` (BS 8666:2020 — EXPAND from {00,13,14,15,51} to the full 37-code set + per-code cutting-length formula); `SchedulingRadiusMm(r) + MinEndProjectionMm(P) + AnticipatedHookDiameterMm(q)` (NEW, BS 8666:2020 Table 2 per size).
- RC-section (EXISTING, VividOrange-owned): `RebarLayout [Union]` (FaceCount/FaceSpacing/PerimeterCount/PerimeterSpacing over SectionFace), `CoverMm`, `EnConcreteGrade`, link `BarDiameter`, `MinimumReinforcementSpacing`.

Seed-row coverage gathered: ISO 6935-2:2007 Table 2 + EN 10080/BS 4449 metric series (D6–D50: d/A_n/mass/f_R — page MISSING H8/H14/H28/H32/H40/H50); ASTM A615/A706 imperial series + Table 1 deformation requirements (#3–#18: db/A_n/wt/max-spacing/min-height/max-gap); CSA G30.18 metric (verified); ISO 6935-2:2007 Table 3 rib geometry (the gap); ACI 318-19 Table 25.3.1 (development hooks); Table 25.3.2 (stirrup/tie/hoop hooks) + §25.3.4 seismic + §25.3.5 crosstie; EN 1992-1-1 §8.3 Table 8.1N + BS 8666:2020 Table 2 (mandrel/radius/end-proj/hook-dia); BS 8666:2020 full 37 shape codes with cutting-length formulas; grade table (A615 Gr40–Gr100, A706 Gr60/Gr80, CSA 400W/500W, EN B500A/B/C + VividOrange B450A/B450C/B550B).

Verified members (`assay api`, restored net8.0): `VividOrange.Sections.Reinforcement.BarDiameter` = {None=0,D6,D8,D10,D12,D13,D14,D16,D20,D25,D28,D32,D40,D50} (D13 maps soft-metric #4); `EnRebarGrade` = {B450A,B450C,B500A,B500B,B500C,B550B} (page uses only B500B/B500C); `EnRebarMaterial(EnRebarGrade, NationalAnnex)` (Type=Reinforcement(6), PartialFactor=1.15, Accidental=1.0); `EnRebarFactory.CreateLinearElastic<T> where T:Enum` (parses grade digits → f_yk, Es=200 GPa) + `CreateBiLinear(EnRebarGrade)→IBiLinearMaterial` (ductility A=1.05/B=1.08/C=1.15); `Rebar : IRebar` ctors `(IMaterial,Length)`/`(IMaterial,BarDiameter)` (throws `InvalidMaterialTypeException` if Type≠Reinforcement); `Link : Rebar, ILink` with `MinimumMandrelDiameter` lazy = `d.Millimeters<=16 ? 4*d : 7*d` (the EN 1992 §8.3 mandrel rule baked in); `LongitudinalReinforcement` {Position, Rebar, CountPerBundle}; `ConcreteSection : IConcreteSection` (Rebars via CollectRebars, Link, Cover, MinimumReinforcementSpacing, AddRebarLayer/ClearRebars); `FaceReinforcementLayer`/`PerimeterReinforcementLayer` (GetPath/GetRebars); `ReinforcementLayoutByCount`/`BySpacing`; `MinimumReinforcementSpacing.GetMinimumReinforcementSpacing(Length)`; `SectionFace` = {Top,Left,Right,Bottom,Sides} — NO `Perimeter` member (page claim correct). NEGATIVE: `SteelStiffness` is NOT a VividOrange member (zero hits across all 19 Materials .api + substrate .api); IRRELEVANT to round rebar (no plastic/torsion/warping/shear gap).

VALIDATE verdict: VALIDATED — zero phantoms, zero unsupported fields. All thirteen VividOrange member claims verified-local with EXACT signature/enum/IL agreement (including `Link.MinimumMandrelDiameter` 4·d(≤16)/7·d(>16) confirmed in decompiled IL); `SteelStiffness` negative claim confirmed; `[4]` split honored exactly; every NEW field grounds in a cited `[2]` standard (ISO 6935-2 Table 3; ACI 318-19 Tables 25.3.1/25.3.2/§25.3.4; EN 1992-1-1 §8.3 Table 8.1N; BS 8666:2020 Table 2). Page-gap diagnosis accurate: rib geometry entirely absent; the single `BarSize.BendFactor` conflates ACI Table 25.3.1 (development) with 25.3.2 (stirrup/tie) so a #5 stirrup wrongly inherits 6·d_b instead of 4·d_b; mandrel uncomputed on-schedule though VividOrange `Link` supplies it; `ShapeCode` set is the partial {00,13,14,15,51}; H8/H14/H28/H32/H40/H50 (D8/D14/D28/D32/D40/D50) unexploited.

Phantoms: NONE (empty). Minor non-blocking notes only: β and the BS 8666 r/P/q scheduling dims extend beyond `[2]`'s literal parenthetical but sit inside the cited ISO 6935-2 / BS 8666:2020 standards; the `Σf_i` descriptor ("max longitudinal gap between rib rows") is a loose gloss of the summed rib-free perimeter (correct value ≤0.25·π·d); the seed-comment `BarDiameter` list cosmetically omits the real `D13` member.

pinsNeeded: NONE.

## headed stud

ISO 13918 Type SD welded shear connector (≡ AWS D1.1 Type B; EN 1994-1-1 §6.6 / AISC 360 §I8). Owner: `libs/csharp/Rasm.Materials/.planning/Connection/joint.md` (`StudClass`, lines 103-122, 236-237). Current `StudClass [SmartEnum<string>]` is naive (diameterMm/standardHeightMm/ultimateMpa + Rg/Rp/SteelShearKn over 4 imperial rows); the rebuild expands to full SD geometry + grade discriminant while keeping the capacity rail and the `Rasm.Compute` concrete-side join unchanged.

Fields (full parametric set, ISO 13918:2017 §4 + Table 10): `d1` (nominal shank diameter, design valid 16≤d1≤25, light-duty 10/13); `d2` (diameter at weld area before welding ≈d1, optional); `d3` (WELD-COLLAR diameter — needed to build the base weld-collar fillet solid that transmits shear per EN 1994-2; OMITTED by `[2]`); `d5` (head diameter, AWS min d5≥1.5·d1); `h3` (head height/thickness, AWS min h3≥0.4·d1); `h4` (WELD-COLLAR height at the weld END — `[2]` MISLABELED this "underhead neck"; SD head abuts shank directly, no separate neck); `alpha` (weld-end face angle 140°±7°); `l1` (overall length before welding); `l2` (as-welded/design length = l1−burnOff, the height EN hsc and AISC h measure on); `burnOff` (= l1−l2, {3,3,4,4.5,5,5.5} for d1={10,13,16,19,22,25}; +~5mm through-deck); `grade {SD1, SD2, SD3, AWS-TypeA, AWS-TypeB}` (replaces hardcoded ultimateMpa=450); `fu` (SD1/TypeB=450, SD2=400–550, SD3=500, TypeA=420); `fy` (SD1/TypeB=350, SD2=235, SD3=350, TypeA=340); `Asc=π·d1²/4` (derived, present as AreaMm2); `Rg` (AISC §I8.2a group factor {1.0, 0.85, 0.70}, default 1.0); `Rp` (position factor {0.75, 0.60}, default 0.75); `hsc/d1` (as-welded height-to-diameter ratio; AISC ≥4; EN α=1.0 for hsc/d1>4, α=0.2·(hsc/d1+1) for 3≤hsc/d1≤4).

Seed-row coverage gathered: ISO 13918:2017/2018 Type SD metric series (Table 10, transcription-complete: d1/d5/d3/h3/h4/burnOff/l2-available per row d10–d25; grade SD1 fu=450 fy=350; alpha=140° all); AWS D1.1 Type B imperial series (Nelson S3L geometry, 1/2"–1": d1/d5/h3/burnOff, satisfies AWS minimums); ISO Table A.4 l2 grid; grade table (ISO 13918:2017 Table 2 / AWS D1.1: SD1/SD2/SD3/TypeA/TypeB fu/fy/A5%/material/use). Capacity rails CONFIRMED already on `StudClass` (not new): AISC §I8.2a `SteelShearKn=Rg·Rp·Asc·fu`; EN 1994-1-1 §6.6.3.1 `P_Rd=0.8·fu·(π·d1²/4)/γV` (γV=1.25); concrete side stays the `Rasm.Compute` composite-section join.

Verified members (`assay api`, restored net9.0): `VividOrange.Profiles.American`/`European` typed-designation enums + `CatalogueFactory` EXIST; `VividOrange.Sections.SectionProperties.{Area, Perimeter, ElasticSectionModulusYy/Zz, MomentOfInertiaYy/Zz, RadiusOfGyrationYy/Zz, Centroid, Extends}` EXIST (decompile lines 35–191) — plastic modulus / St-Venant torsion / warping / shear area ABSENT (grep = 0 hits), so the `Wpl/It/Iw/Av` gap is REAL → `SteelStiffness` hand-roll justified. `SteelStiffness` is the hand-rolled `Profiles/steel.md:201` static (`Derive(SteelClass, SectionDims, double sx, double sy) → (double Zx, double Zy, double J, double Iw, double Avy, double Avz)`), NOT a NuGet member. NO Stud/Shear/Connector/Fastener type anywhere in VividOrange (every query "no match") — headed stud correctly hand-rolled in-fence.

VALIDATE verdict: CLEAN — zero phantom members; field set source-grounded; `[4]` split + `pinsNeeded:[]` correct. Both cross-file corrections against `[2]` are LEGITIMATE: `[2]`'s "underhead neck" label for `h4` is the naive summary the rebuild overwrites (ISO Clause 4 defines `h4`=weld-collar HEIGHT); `[2]` omits `d3`=weld-collar DIAMETER needed for the base fillet. Added `d2/alpha/grade(fu,fy)` are within the cited ISO 13918 Clause 4/Table 10/Table 2 + AWS + AISC §I8.2a. ONE minor imprecision (not a phantom): `CatalogueFactory.CreateAmerican/CreateEuropean` return `ICatalogue`, not `IProfile` directly — the IProfile is reached through it; the rebuild owner must compose `CatalogueFactory→ICatalogue→IProfile`. CAVEAT: the ISO Table 10 / AWS imperial ROW values are standard-PDF transcriptions unverifiable by an `assay` rail (internally consistent: every d5≥1.5·d1, h3≥0.4·d1, l1=l2+burnOff) — transcription-verify at fence-authoring.

Phantoms: NONE (empty).

pinsNeeded: NONE.

## weld/joint

The fourth `ConnectionFamily` (`JointKind ∈ {Weld, Adhesive, Stud}`). Owner: `libs/csharp/Rasm.Materials/.planning/Connection/joint.md` → `Connection/Joint.cs`; folds into the unified `Component/` sub-domain per `[5]`. Standards AWS D1.1:2020 (Fig 5.1 prequalified joints, Cl.4 design, Cl.7/8 profile+tolerance) + AISC 360 Chapter J2. The current `JointSection.Weld(WeldType, ElectrodeClass, LegMm, DepthMm, LengthMm)` is a scalar smear; the rebuild adds three new in-fence sub-records.

Fields (full parametric set):
- EXISTING (keep): `JointKind {Weld, Adhesive, Stud} [SmartEnum<string>]` + IfcPredefinedType (WELD/GLUE/STUDSHEARCONNECTOR); `ElectrodeClass {E60,E70,E80,E90,E100,E110}` TensileMpa; `AdhesiveClass {Epoxy,Methacrylate,Polyurethane,SiliconeStructural}` LapShearMpa/PeelNmm/ServiceCelsius; `StudClass {S13,S16,S19,S22}` Diameter/Height/Ultimate + Rg/Rp/Area/SteelShearKn.
- `WeldType` — collapse to {Fillet, Groove, Plug, Slot, FlareBevel, FlareV} + `LineWeld` flag + `EffectiveThroat(legMm,depthMm)` delegate (NOT the 14-way overlap; see phantoms).
- `WeldProfile` sub-record (NEW): `WeldFace {Concave, Convex, Flat}`, `Leg1Mm/Leg2Mm` (fillet legs = AWS "size"), `ConvexityMm` (bead crown, no design throat), `ReinforcementMm` (groove-weld face reinforcement), `ToeRadiusMm` (fatigue-critical stress riser), one Root field.
- `GroovePrep` sub-record (groove arm, NEW): `GrooveType {Square, SingleV, DoubleV, SingleBevel, DoubleBevel, SingleU, DoubleU, SingleJ, DoubleJ}`, `Penetration {Cjp, Pjp}`, `RootOpeningMm(R)`, `RootFaceMm(f, land)`, `GrooveAngleDeg(α)`, `BevelAngleDeg(β)`, `GrooveRadiusMm(r)` (J=10mm / U=6mm per TABLE A), `DepthOfPrepMm(S)`, `BackingType {None, SteelBacking, Backgouge} + BackingThicknessMm + BackingWidthMm`.
- `PlugSlot` sub-record (NEW): `HoleDiameterMm` (plug), `SlotLengthMm + SlotWidthMm`, `EndRadiusMm`, `DepthOfFillingMm`, `SpacingMm`.
- Adhesive arm (NEW + EXISTING): `FilletLegMm`, `BiteMm` (SSG structural bite, wind-load sized) + existing `BondLineMm`, `OverlapMm` (min lap = 5× thinner part ≥25mm), `WidthMm`.
- Throat algebra (DERIVED): fillet equal-leg = 0.707·leg; unequal = (w1·w2)/√(w1²+w2²); SAW deep-pen te = a (a≤3/8") or 0.707a+0.11 (a>3/8"); CJP = thinner-part thickness; PJP = groove depth (−3mm for 45°≤α<60°, SMAW/GMAW/FCAW — see phantoms); concavity reduces throat, convexity does not raise it.
- Cross-cutting + design projection: `AppearanceId`, `CapacityKey(metal.steel base)`, IfcEntity selection (`IfcFastener` WELD/GLUE vs `IfcMechanicalFastener` STUDSHEARCONNECTOR), `NominalMm` dispatch; `DesignShearKn` = line-weld 0.6·FEXX·throat·length | plug/slot 0.6·FEXX·(π/4·d²) | adhesive LapShearMpa·overlap·width | stud Rg·Rp·Asc·Fu; directional factor `kds = 1.0 + 0.50·sin^1.5(θ)` (AISC Eq. J2-5); Fnw=0.60·FEXX; φ=0.75 shear / 0.80 tension-normal (Ω=2.00/1.88).

Seed-row coverage gathered (10 tables A–J, transcription-complete): TABLE A (AWS D1.1:2020 Figure 5.1 prequalified groove joints B-L1a/B-U2/B-U3/B-U4a/TC-U4a/B-U8/B-U9 with exact angle/opening/face/radius/backing); TABLE B (AISC J2.4 min fillet by thinner-part t); TABLE C (AISC J2.3 min PJP effective throat, 7-band); TABLE D (AISC J2.1 PJP effective throat E by groove+angle); TABLE E (AISC J2.2 / AWS Table 4.1 flare-groove throat by process×flare, R≈2t); TABLE F (AWS electrode min FEXX); TABLE G (AWS Type-B stud); TABLE H (AWS weld-profile acceptance/tolerance); TABLE I (AISC J2.3b plug/slot geometry); TABLE J (structural-adhesive lap-shear/peel/service, SSG bite).

Verified members (`assay api`/decompile): `VividOrange.Profiles.American` (2299), `European` (558), `AmericanShape` (13), `EuropeanShape` (25), `CatalogueFactory.CreateAmerican/CreateEuropean`, `SingletonAmericanBase/SingletonEuropeanBase`; `IIParallelFlange`/`II` exposing FlangeThickness/WebThickness (the connected-part thickness t governing min fillet / PJP throat). `SteelStiffness` hand-rolled (`Profiles/steel.md`, `Derive → Zx/Zy/J/Iw/Avy/Avz`), verified ABSENT from every VividOrange assembly; joint base-metal shear check reads the connected member `AvyMm2` from it. `GeometryGym.Ifc.IfcFastenerTypeEnum {NOTDEFINED, USERDEFINED, GLUE, MORTAR, WELD}`; `IfcMechanicalFastenerTypeEnum {...SHEARCONNECTOR, STUDSHEARCONNECTOR, BOLT, ANCHORBOLT, COUPLER(4x2), RAILJOINT/RAILFASTENING/CHAIN/ROPE(4x3)...}`; `IfcReinforcingBarTypeEnum {...STUD...}` (cast-in bar, NOT the welded connector — the C6 distinction the page enforces).

VALIDATE verdict: VALIDATED — ZERO phantom members (all 11 verifiedMembers exist locally). Standard grounding strong: TABLES B/C/E/I faithful to AISC 360-22 J2.4/J2.3/J2.2/J2.3b; F/G electrode+stud values correct; design projection (kds, Fnw, φ, Ω) correct; AWS D1.1:2020 clause/figure numbering internally consistent; TABLE A prequalified designations dimensionally consistent.

Phantoms (ZERO phantom MEMBERS; field-level flags, the first three build-blocking): (1) STRUCTURAL parallel-vocabulary collapse (violates `[01]-[WORKSPACE_LAW]` no-split) — `WeldType` grown to a 14-member enum INCLUDES the 9 groove geometries {SingleV,DoubleV,SingleBevel,DoubleBevel,SingleU,DoubleU,SingleJ,DoubleJ,Square} while `GrooveType` is ALSO defined with the SAME 9; collapse `WeldType` to {Fillet, Groove, Plug, Slot, FlareBevel, FlareV} and keep the V/Bevel/U/J × single|double prep ONLY in `GroovePrep.GrooveType` (dropping Butt/Groove from the current {Fillet,Groove,Butt,Plug} is otherwise correct — a butt is a joint type, not a weld type). (2) STANDARD CONTRADICTION — the throat-algebra field says "minus 3mm for 45°≤α<60° SMAW/SAW" but TABLE D says "SMAW/SAW/GMAW/FCAW → E=D−1/8\""; both mis-state the set (both wrongly include SAW which gets deep-penetration credit; the field also omits GMAW/FCAW) — per AISC J2.1 the 1/8" deduction applies to SMAW/GMAW/FCAW only. (3) FIELD-vs-TABLE CONTRADICTION — `GrooveRadiusMm` prose "(J=10mm, U larger)" contradicts TABLE A (B-U8 single-J radius=3/8"=10mm, B-U9 single-U radius=1/4"=6mm, i.e. U SMALLER than J); TABLE A matches AWS D1.1 prequalified values; DROP the "U larger" prose. Minor flags: (4) member-attribution precision — FlangeThickness/WebThickness are declared on the BASE interface `II` (which `IIParallelFlange:II` inherits); the .api citation must attribute the thickness members to `II`, not `IIParallelFlange` (members real, reachable); (5) E80–E110 are AWS A5.5 low-alloy, not A5.1 (A5.1 is carbon-steel E60/E70 only) — FEXX values correct, fix the per-band label; (6) `BackingType {None, SteelBacking, Backgouge}` conflates a backing-bar TYPE with a root-treatment PROCESS (backgouging) — model backgouge as a separate root-treatment axis; (7) `RootMm/RootConditionMm` (one concept, two names) overlaps groove `RootOpeningMm` + `RootFaceMm` — pick one definition, avoid triple-modeling the root; (8) SCOPE NOTE — the `[2]` header "AWS D1.1 + AISC 360 J2" does not govern the adhesive arm (ASTM D1002/D1876 + ASTM C1401/EN 13022 SSG); keep per-arm citation.

pinsNeeded: NONE.

## steel-sections + timber

The WF-1 structural `ProfileFamily` pair: `ProfileFamily.Steel` catalogue-grounded via VividOrange + `SteelStiffness` gap-fill; `ProfileFamily.Timber` hand-rolled EN/APA grades. Pages: `libs/csharp/Rasm.Materials/.planning/Profiles/steel.md` + `timber.md`.

Fields (full parametric set):
- STEEL: identity (American|European typed-designation enum value, 2857 sealed singletons → `ProfileId 'steel.<designation>'`); family axis `SteelClass {i-shape, u-shape, l-shape, double-angle, hss-rect, hss-round, tee, composite, cold-formed}` carrying `SteelTopology {open, closed, solid}` + IfcProfileDef subtype (folded from AmericanShape(13)/EuropeanShape(25) via `SteelClass.OfShape`); geometry `SectionDims` (DepthMm, FlangeWidthMm, WebThicknessMm, FlangeThicknessMm, FilletMm via `IIParallelFlange.FilletRadius`, BackToBackMm via `IDoubleAngle.BackToBackDistance`); elastic columns from VividOrange (AreaMm2, IxMm4, IyMm4, SxMm3, SyMm3, RxMm, RyMm, HeatedPerimeterMm); plastic/torsion/warping/shear columns from hand-rolled `SteelStiffness.Derive` per topology (ZxMm3, ZyMm3, JMm4, IwMm6 positive OPEN only, AvyMm2, AvzMm2); the canonical 17-field `ComputedSection` receipt; grade `SteelGrade [SmartEnum]` (NominalYieldMpa + Option<EnSteelGrade>; AISC A36/A992/A572, EN S235–S460 → `EnSteelFactory.CreateLinearElastic` Table 3.1 fy by thickness band + NationalAnnex); design (`CompactnessClass {Compact,Noncompact,Slender}`, `DesignCapacity`); composite (`CompositeDetail`: SlabEffectiveWidth/Depth, ConcreteFc, StudClass, StudsPerMetre); cold-formed (`ColdFormedDetail`: GrossWidth, EffectiveWidth, CornerRadius, DesignThickness, EffectiveSectionModulusRatio).
- TIMBER: form `TimberForm {sawn, glulam, clt, lvl, psl}` (IfcRectangleProfileDef, MaterialShapeKind, CrossPly bool, `BetaNSoftwoodMmPerMin` charring + density-adjusted `BetaN`); grade `TimberGrade` full EN characteristic vector (Fmk, Ft0k, Fc0k, Fc90k, Fvk, FRvk rolling shear, E0Mean, E005, E90Mean, GMean, GRollMean, DensityK); EC5 kmod axis (`ServiceClass {sc1,sc2,sc3}` + KdefCreep, `LoadDuration {permanent…instantaneous}` carrying per-service-class kmod triple); layup `PlyLayup` (PlyThicknessMm, PlyAngles 0°/90°, LongitudinalIndices, EffectiveStiffness gamma-method); `TimberSection` (Form, Grade, Width, Depth, Layup → Section/OrthotropicLaw/ToLayerSet/ToProperties/ToUnit); capacity `TimberCapacity` (EN 1995-1-1: Bending/Compression/Shear/BearingPerp/Torsional, RelativeSlenderness, Kmod; `ResidualSection` EN 1995-1-2); design factors (`gammaM` PER-FORM — CORRECTION below, plus k_h size, k_c buckling over E005, k_cr crack, k_shape torsion).

Seed-row coverage gathered: STEEL recommended dense seed exercising ALL 9 `SteelClass` cases (every token assay-verified-existing) — current `AmericanSeed` exercises only 7 of 9; `HssRound` (Pipe12STD/Pipe8SCH40) and `DoubleAngle` (DoubleL4x4x1over2/DoubleL5x5x3over8) are DECLARED but NEVER seeded; STEEL GRADE table (EnSteelFactory EN 1993-1-1 Table 3.1: A36/A992/A572 + S235–S460 fy by thickness band, S450 exists/unused, fu tabulated); TIMBER GRADE DATA authoritative (EN 14080:2013 + EN 338:2016 + EN 14374: glulam Gl24h–Gl32h homogeneous EXACT, Gl24c/Gl28c combined, sawn C16/C24/C30/D40, LVL Lvl48p) with 4 corrections flagged; TIMBER kmod EN 1995-1-1 Table 3.1 (VERIFIED EXACT) + load-duration Table 2.1 + KdefCreep Table 3.2; TIMBER section seed (EN/APA products); TIMBER charring EN 1995-1-2 betaN.

Verified members (`assay api`, restored 0.1.0): `CatalogueFactory.CreateAmerican/CreateEuropean→ICatalogue`; `American` enum = 2299 (all 13 current + 12 recommended seed tokens exist); `European` = 558 (all 5 current + recommended exist); `AmericanShape` (13), `EuropeanShape` (25) exact-match the `OfShape` folds; geometry interfaces `II{Height,Width,FlangeThickness,WebThickness}`, `IIParallelFlange:II{+FilletRadius}`, `IChannel/ITee/IAngle`, `IDoubleAngle:IAngle,IBackToBack{+BackToBackDistance}`, `IRectangle/ICircle/IRectangularHollow/ICircularHollow/IHollowStructuralSection{Thickness}` — all ReadDims arms valid; `SectionProperties` elastic-only (gap confirmed by negative check: NO PlasticSectionModulus/TorsionConstant/WarpingConstant/ShearArea; NO `SteelStiffness` anywhere in VividOrange → the hand-rolled `SteelStiffness.Derive` is the sole correct path); `LinearElasticOrthotropicMaterial(MaterialType, Pressure Ex,Sx,Ey,Sy,Ez,Sz)` (EXACT match to timber `OrthotropicLaw`); `MaterialType.Timber` non-obsolete (SolidTimber/GluedLaminatedTimber deprecated→Timber); `EnSteelGrade {S235,S275,S355,S420,S450,S460}`; `EnSteelFactory.CreateLinearElastic(IEnSteelMaterial)/(,Length)` + `CreateBiLinear`; `EnSteelMaterial(EnSteelGrade, NationalAnnex)`; `ILinearElasticMaterial {ElasticModulus, Strength, PeakStrain}`; `NationalAnnex` (RecommendedValues@0 + ~35 nations); `SectionProperties.Utility` static kernels. NOTE: assembly ships exactly TWO national catalogues (2299+558=2857), NOT the scope-doc's "12 nations/~2914".

VALIDATE verdict: VALIDATED — overwhelmingly accurate; every VividOrange member, namespace, ctor, unit accessor steel.md/timber.md compose is assay-verified to exist exactly (counts EXACT; geometry interfaces + IBackToBack/IHollowStructuralSection/ICatalogue.Label exact; the 7-arg orthotropic ctor, MaterialType.Timber, EnSteelGrade S235–S460+S450, EnSteelFactory, EnSteelMaterial, ILinearElasticMaterial.Strength all exact). SectionProperties elastic-only GAP confirmed → `SteelStiffness.Derive` is the sole correct path; `[4]` split verified-correct; project-internal seam types (ComputedSection 17-field order, ParametricSection.Rectangle, ProfileUnit.Of, StudClass, MaterialPropertySet.OfOrthotropic) consistent across sibling pages; EN kmod/KdefCreep/GL-homogeneous data EXACT. The 4 timber data corrections + steel seed-gap are all standard-grounded WITHIN-FILE build-leg fixes (not cross-file residuals): (a) `gammaM` hardcoded flat 1.25 for all forms — EN 1995-1-1 Table 2.3 requires per-form sawn 1.30 / glulam 1.25 / LVL,PSL 1.20 / CLT 1.25 (must become a per-`TimberForm` field; flat 1.25 is unconservative for sawn, over-conservative for LVL); (b) combined glulam GL24c/GL28c `E90Mean=250` is the 05-fractile — EN 14080:2013 Table 4 `E90,g,mean=300`; (c) EN 338 version drift — C24 ft0k 14.0→14.5, C30 ft0k 18→19 & fc0k 23→24 (align all C-classes to EN 338:2016); (d) the D40 hardwood row is suspect (fc90k 2.9 implausibly low, rhoK 700 > normative D40 550, fc0k 26 vs 27, E0Mean 11000 vs ~13000) — re-transcribe against EN 338:2016 Table 3. Steel seed-coverage gap: `AmericanSeed` never instantiates HssRound (Pipe) or DoubleAngle (DoubleL) though both cases + ReadDims arms exist — add the verified-existing tokens so the seed exercises all 9 cases.

Phantoms (TWO defects, both build-blocking within-file): (1) `European.HEM300` — PHANTOM. The EN 10365 catalogue identity does NOT exist under that token; the real member is `European.HE300M` (naming HE<size><series>, series ∈ {AA,A,B,C,M}; HE300AA/HE300A/HE300B/HE300C/HE300M all present, no HEM* members). `HEM` is only a valid `EuropeanShape` FAMILY enum value, never a section-identity member. The capture asserts `HEM300` as VERIFIED in both its verifiedMembers list and recommended dense seedRows; following it into `EuropeanSeed` yields a CS0117 compile error — correct to `European.HE300M`. (steel.md's CURRENT EuropeanSeed does not use it — the phantom is only in the recommendation.) (2) BEHAVIORAL (steel.md within-file) — `SteelGrade.S420`/`SteelGrade.S460` EN-yield is non-functional as the page invokes it: `SteelGrade.YieldMpa` builds `new EnSteelMaterial(g, annex)` carrying the default `EnSteelSpecification` (DeliveryCondition=AsRolled), routing `EnSteelFactory.CreateLinearElastic` to `EN1993_1_1_Table3_1_AR`, whose decompiled dictionary holds ONLY keys 0/1/2/4 (S235/S275/S355/S450); S420 (key 3) and S460 (key 5) are EN 10025-3 Normalized / -4 Thermomechanical grades absent from AR, so the factory throws (ArgumentException ≤40mm via ContainsKey guard, KeyNotFoundException >40mm), trapped to `ProfileFault.Family` — these two declared grades ALWAYS rail to fault, contradicting the `[EN_STEEL_GRADE_YIELD]` REALIZED claim. The capture's fy values (S420 420/390, S460 460/430) are correct but live only in `EN1993_1_1_Table3_1_N/_M`, unreachable without setting DeliveryCondition=Normalized/Thermomechanical on the spec first.

pinsNeeded: NONE.

## [PHANTOMS_AND_PINS]

Consolidated phantoms (correctness-critical for the follow-on) across all families:

- glazing IGU (6, all standard-citation/value, not member-existence): (1) `GlassType.SpecificHeatJKgK=840` misattributed to EN 572-1 — real EN value c=720 J/(kg·K); set 720 or recite the true source. (2) GWP double-count — per-GlassType `GwpPerKg` is process-inflated for same-substance float-derived panes AND `ProcessingGwpAdderPerM2` is added on top; keep per-kg base = raw-substance float (~1.43, only low-iron/borosilicate as substance variants), confine all secondary-process carbon to the per-m² adders. (3) `ceramic-fire` borosilicate λ=1.40 W/mK wrong — real ~1.14 (1.40 is glass-ceramic). (4) `MuntinGrid` bar widths (16/22/29/49/61mm) falsely cited "EN/ASTM" — manufacturer dims, no standard table exists. (5) EN 673 Annex B drift — air λ 0.0241→~0.0250, argon μ 2.10e-5→~2.164e-5; reconcile. (6) trivial — Catalogue split "2299+558=2857" vs verified total 2867 (off by 10; total right, split wrong).
- brick/masonry (1): `MortarJointProfile.prism` is UNSUPPORTED — a "prism" is an ASTM C1314 compressive TEST SPECIMEN, not a tooled mortar-joint finish; DROP the case (real squeezed joints are extruded/weeping).
- fastener (1, member-level ground-truth): `fastener.md` mis-routes `FastenerKind.Coupler→'USERDEFINED'` on a FALSE "not in the predefined enum" claim — `IfcMechanicalFastenerTypeEnum.COUPLER` IS real (IFC4X2); a coupler must land the `COUPLER` token; the line-330 enum list is also incomplete (omits `RAILJOINT/RAILFASTENING/CHAIN/ROPE`, IFC4X3). (No phantom external members; nut-e/dw/da/ds over-scope and the ~3% inch-As overestimate are non-phantom notes.)
- weld/joint (3 build-blocking; rest minor; zero phantom members): (1) `WeldType` 14-way enum DUPLICATES `GrooveType`'s 9 groove geometries — collapse `WeldType` to {Fillet, Groove, Plug, Slot, FlareBevel, FlareV}, keep V/Bevel/U/J only in `GroovePrep.GrooveType`. (2) PJP-throat −1/8" deduction process-set self-contradictory and both forms wrongly include SAW — AISC J2.1 deducts for SMAW/GMAW/FCAW only. (3) `GrooveRadiusMm` prose "U larger" contradicts TABLE A (J=10mm > U=6mm) — drop it. Minor: E80+ are A5.5 not A5.1; `BackingType` conflates backing-bar with backgouging; `RootMm/RootConditionMm` triple-modeling risk; adhesive arm is ASTM/SSG-governed not AWS/AISC; FlangeThickness/WebThickness attribute to `II` not `IIParallelFlange`.
- steel-sections + timber (2, both build-blocking within-file): (1) `European.HEM300` PHANTOM — real member is `European.HE300M`; HEM is only an `EuropeanShape` family value; the capture lists HEM300 as verified + in recommended seed → CS0117 if followed. (2) BEHAVIORAL — `SteelGrade.S420`/`SteelGrade.S460` always rail to `ProfileFault.Family`: the default AsRolled spec selects `EN1993_1_1_Table3_1_AR` (keys S235/S275/S355/S450 only); S420/S460 are Normalized/Thermomechanical-only, so the factory throws; fy values correct but unreachable without DeliveryCondition=Normalized/Thermomechanical.
- CMU (0 phantoms): three doc/design-accuracy flags only — IFC-subtype fidelity (`IfcRectangleHollowProfileDef` → should be `IfcArbitraryProfileDefWithVoids` for multi-cell), TMS 602 citation missing from `[2]`, CCMPA seed-anchor numeric inconsistency.
- rebar (0 phantoms): minor non-blocking notes only (β and BS 8666 r/P/q beyond `[2]`'s literal but in cited standards; `Σf_i` loose gloss; seed-comment cosmetically omits the real `D13`).
- headed stud (0 phantoms): one non-phantom imprecision — `CatalogueFactory.CreateAmerican/CreateEuropean` return `ICatalogue` (reach `IProfile` through it), not `IProfile` directly.

Consolidated pinsNeeded across all eight families: NONE. Every family hand-rolls its generative data in-fence per `CATALOG_LAW [4]`; no new NuGet package is admitted, and all `pinsNeeded` arrays are empty. The only composed external is the already-admitted VividOrange tier (steel section catalogues + EN grade factories + elastic/concrete section properties + the RC reinforcement engine); `SteelStiffness` is the Rasm-local hand-rolled gap-filler for the `Wpl/It/Iw/Av` columns VividOrange leaves elastic-only.
