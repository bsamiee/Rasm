# [PY_GEOMETRY_IDEAS]

The forward pool of higher-order concepts for `geometry`, grounded in the host-free companion role. Each idea is a card — slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on — and spawns one or more tasks in `TASKLOG.md`. `[1]-[OPEN]` holds live concepts; `[2]-[CLOSED]` records dispositions so an idea is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[GEO_IFC_AUTHORING_PIPELINE]-[QUEUED]: a content-keyed incremental-DAG campaign layer over the already-transactional verb-script owner.
- Capability: `IfcAuthor` already folds one ordered `tuple[AuthorOp, ...]` script under one `@transactional`/`@stamped` rollback-bounded fold; this card adds ONLY the cross-script campaign layer absent today — a content-keyed DAG sequencing many such scripts into one model-construction pipeline, each existing script the transacted unit, the campaign re-running only the stages whose `ContentIdentity` seed changed (incremental re-authoring), folding every stage `AuthorReceipt` into one campaign provenance digest — without minting a second authoring owner, a parallel pipeline object, or re-authoring the single-script transaction the page already owns.
- Shape: an `AuthorCampaign` carry threading an ordered set of `tuple[AuthorOp, ...]` scripts through the runtime `Keyed[T]` content-addressed `StagePlan`, each stage REUSING the existing `IfcAuthor.apply` rail and its `@transactional` `begin_transaction`/`end_transaction`/`undo` boundary unchanged, the campaign re-running only the stages whose `ContentIdentity` seed changed and folding every stage `AuthorReceipt` into one campaign `ReceiptContributor` — the spatial-then-element-then-relationship build order a `graphlib`-resolved dependency edge set, not a hardcoded sequence.
- Unlocks: full IFC model authoring from a high-level program (storey grid -> element population -> property/quantity stamping -> relationship wiring) as one reproducible campaign the C# owner system drives over the graduation rail, incremental re-authoring on a changed input without re-running the whole model, and a deterministic author-then-validate loop pairing the authoring campaign with the `ifc/analysis` IDS/clash verbs.
- Anchors: `ifc/authoring.md#AUTHORING` `IfcApiVerb` row table / `AuthorPayload` tagged union / `@transactional` / `@stamped` / `AuthorCarry` left-fold, `runtime/execution/lanes.md` `Keyed[T]`/`StagePlan`/`LanePolicy.cached`, `graphlib` DAG resolution, `ifcopenshell.api` usecase callables, `guid.compress` identity, the compute `HandoffAxis` geometry case.
- Tension: the campaign DAG rides the runtime `StagePlan` content-addressed cache that `runtime` owns; the authoring side supplies only the verb-script stages and their `ContentIdentity` seeds, and a mid-campaign stage fault rolls only its own transaction while the campaign rail short-circuits — the cross-stage rollback policy stays a campaign-carry concern, never a global model snapshot.

[GEO_POINTCLOUD_INSTANCE_LABELLING]-[QUEUED]: a `ScanDeviation.SEGMENT` instance-clustering and non-planar-primitive deepening, never a parallel segmentation owner.
- Capability: `ScanDeviation` widens its existing `DeviationStage.SEGMENT` arm — already iterative multi-plane RANSAC with `PrimitiveClass` (wall/slab/column/generic) axis classification — with two genuinely absent modes: DBSCAN/connected-component INSTANCE labelling that splits the inlier set of one plane into discrete element instances (two coplanar walls, three columns sharing a normal), and non-planar primitive fits (cylinder for pipe/duct, sphere) the plane-only `segment_plane` loop cannot extract, so the partition is denser rows on the one stage-discriminated owner, never a second `ScanSegmentation` surface racing the `_segment` fold.
- Shape: a `SegmentMode` sub-enum on the `SEGMENT` arm (`PLANAR_PEEL` the present iterative `segment_plane`, `INSTANCE_CLUSTER` the `cluster_dbscan` label fold over each plane's inlier index set, `CYLINDER`/`SPHERE` the non-planar RANSAC fits) plus a per-`Segment` `instance_id` field and a `PrimitiveClass.PIPE` row, the `numpy` label fold reusing the existing `DeviationBand.fold` per instance — the `ATTRIBUTED` stage already groups deviation per `Segment`, so instance labelling is one more grouping key, never a new result shape.
- Unlocks: instance-level scan-to-BIM correspondence (this column vs that column, not "a column plane"), pipe/duct as-built extraction the plane-only loop misses, and a per-instance `DeviationBand` the `ifc/analysis` clash and graduation overlay read keyed to the element GlobalId.
- Anchors: `scan/deviation.md#DEVIATION` `DeviationStage.SEGMENT`/`_segment` iterative `segment_plane` peel / `Segment.classify` / `PrimitiveClass` / `DeviationBand.fold` / `ATTRIBUTED` per-segment grouping, `open3d` `PointCloud.cluster_dbscan` and the cylinder/sphere RANSAC fit, `numpy` label folds, the `scan-deviation` `GeometrySubject`.

[GEO_PARAMETRIC_LAYOUT]-[QUEUED]: a constraint-driven placement case on `ComputationalGeometry`, distinct from the analytics it composes.
- Capability: `ComputationalGeometry` grows ONE `Layout(program, constraints, engine)` case generating spatial arrangements from a program and constraints — grid/packing/adjacency-driven placement and graph-based space allocation — feeding the authoring DAG with a resolved spatial program; the case CONSUMES the existing `graph/features.md#FEATURES` networkx centrality/community analytics and the existing `graph/algebra.md` `Network` adjacency, never re-deriving them, so the only new surface is the placement solver that turns an adjacency program into placed coordinates.
- Shape: a `Layout` case keyed by a `LayoutEngine` sub-enum (grid-pack / adjacency-graph / force-directed) carrying a typed `LayoutParams` value object, folding `compas.geometry` placement primitives and a `networkx` force-directed layout (`spring_layout`) over the adjacency the `features`/`Network` owners produce into a placed `Assembly`, emitting the `mesh-algebra` `GeometrySubject` keyed through the existing `_subject` total match — never a new bare-`str` subject.
- Unlocks: program-to-spatial-layout generation (room adjacency graphs to placed footprints, packing of fabrication parts, structural grid generation) that seeds `[GEO_IFC_AUTHORING_PIPELINE]` with a resolved storey program, and a layout-then-author-then-validate generative loop over the IFC graduation rail.
- Anchors: `graph/algebra.md#ALGEBRA` `ComputationalGeometry` `Network`/`FormFinding`/`Datastructure` cases / `_subject` total match / `compas` datastructures / `compas.rpc.Proxy` heavy-band bridge, `graph/features.md#FEATURES` networkx centrality/community analytics (the consumed analytics, not re-derived), the `ifc/authoring.md#AUTHORING` DAG consumer, the `mesh-algebra` `GeometrySubject`.

[GEO_CLASH_QTO_RECEIPTS]-[QUEUED]: a unified cross-verb BIM-evidence rollup over the IFC analysis and lifecycle owners.
- Capability: a `BimEvidence` rollup folding the `ifc/analysis` clash/IDS/space-program verdicts and the `ifc/costing` quantity-takeoff/cost rows into one content-keyed evidence digest per model revision, so the standards-conformance and quantity dimensions cross the graduation rail as one correlated receipt rather than per-verb runs the C# owner re-correlates.
- Shape: a campaign that runs the `AnalysisKind` CLASH/IDS/SPACE_PROGRAM and `LifecyclePhase` QUANTITY/COST verbs over one validated element set through the shared `IfcSelector` gate, folds every `AnalysisRow`/`LifecycleRow` into one `BimEvidence` digest keyed by `ContentIdentity` over the model bytes, and graduates the digest as the `numerical-primitive` subject so a clash count, an IDS failure fraction, and a quantity total reach the C# owner as one revision-keyed evidence row.
- Unlocks: revision-over-revision BIM dashboards (clash trend, compliance trend, quantity/cost delta) the C# owner consumes from one digest, the author-validate-cost loop closing `[GEO_IFC_AUTHORING_PIPELINE]`, and a reproducible model-quality gate keyed to the model content hash.
- Anchors: `ifc/analysis.md#ANALYSIS` `AnalysisKind`/`AnalysisRow` clash/IDS/space-program verbs, `ifc/costing.md#LIFECYCLE` `LifecyclePhase` QUANTITY/COST rows, `ifc/selector.md#SELECTOR` the shared validated gate, `runtime` `ContentIdentity`/`Receipt`/`ReceiptContributor`, the compute `HandoffAxis` geometry `numerical-primitive` case.
- Tension: the rollup composes existing verb owners and never duplicates their provider logic — it folds their typed rows, never re-runs `ifcclash`/`ifc5d` outside the owning surface; the digest is content-keyed over model bytes so a revision delta is a key change, and the cost-spreadsheet export stays the data boundary's columnar product.

[GEO_TOPOLOGICPY_LICENSE_REPLACE]-[BLOCKED]: a permissive non-manifold topology runtime replacing the AGPL `topologicpy` in the default server build.
- Shape: the same `TopologyOp`-discriminated owner over a permissive backend — the OCCT `cadquery-ocp` topology-explorer hierarchy (`TopExp_Explorer`/`TopoDS` shape decomposition the `mesh/brep` owner already imports) folding cell/face/edge adjacency, or a `compas`/`networkx` dual-graph projection over the in-process B-rep — so the cell-complex algebra is a row family over an admitted permissive package, never the AGPL package, and the IFC-bytes constructor `[IFC_BYTES_CONSTRUCTOR]` resolves through the permissive path.
- Unlocks: non-manifold cell/aperture/adjacency analysis in the shipped server (not just the opt-in lane), the `topology-graph` `GeometrySubject` reaching the graduation rail by default, and the `[IFC_BYTES_CONSTRUCTOR]` IFC-derived topology landing without the dual gate.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[COMPANION_CPU_OFFLOAD]-[DROPPED]: absorbed into the `TASKLOG.md` `[GEOMETRY_CPU_OFFLOAD]` task — the geometry-CPU-to-runtime-offload-lane concept is one task consuming the runtime `LanePolicy.offload` seam across `daemon`/`registration`/`repair`/`ingestion`/`reconstruction`, never a standing idea; recorded so the offload concept is never re-litigated as a parallel idea card.

[GEO_BREP_GENERATIVE_KERNEL]-[DROPPED]: redundant against the realized `mesh/brep.md#BREP` owner — `BrepOp` already authors the full generative case family this card claimed as new: `Construct(verb, params)` with box/sphere/cylinder/cone/torus `ConstructVerb` rows over `BRepPrimAPI_*`, the `Offset` arm's `EXTRUDE`/`REVOLVE`/`LOFT`/`SWEEP` rows over `BRepOffsetAPI`/`BRepPrimAPI` (linear extrude/revolve deliberately living on the `Offset` profile leg, not a duplicate `Construct` primitive), `Feature` fillet/chamfer, the n-ary `Boolean` algebra, the in-place `Tessellate` arm, the `mesh-algebra` `GeometrySubject` graduation, `ContentIdentity`/`IdentityPolicy` keying, and the `LanePolicy.offload` PEP 734 hand-off. A new primitive/set/offset/feature verb is already one row plus one dispatch-table entry per the page Growth bullet; a separate `BrepGen`/`BrepConstruct` surface is the rejected parallel per-operation family the page's Boundary bullet explicitly deletes. Recorded so the generative-B-rep concept is never re-litigated as a second modeling owner.
