# [DOSSIER_CENSUS] Rasm.Compute/.planning

READ-ONLY census. Target: `libs/csharp/Rasm.Compute/.planning/` (41 pages, 17,101 LOC markdown, 7 sub-folders). Deep-read: Analysis/* (6/6 full), Runtime/{codecs,receipts,scheduling,admission} full, Model/embedding full, Solver/{contract,clash} full; charter+seam pass on the remaining 26. Governance read full: `libs/.planning/architecture.md`, `Rasm.Compute/ARCHITECTURE.md`+README+TASKLOG+IDEAS+csproj, `RASM-CS-GEOMETRY-BRIEF.md`, `RASM-CS-PERSISTENCE-BRIEF.md`, `RASM-COMPONENT-PARADIGM-DECISION.md` (Compute ripple rows), Element seam `Assessment/assessment.md`, Materials `Component/{capacity,steel}.md`.

## [00]-[HEADLINE]

The corpus is world-class at the page-interior level and stale at the SEAM/LIFECYCLE perimeter. Every fence read is 9–9.5/10: extreme ADT collapse (`[Union]`/`[SmartEnum<TKey>]` + FrozenDictionary dispatch, rosters as seed DATA), honest LanguageExt `Fin`/`Validation`/`IO` rails, the one `XxHash128` seed-zero hasher, aggressive "deleted form" self-policing, and full parameterization. There is NO page-interior rot, NO stale old-paradigm reference (item 4 clean), NO phantom-realization card, and the folder architecture is a genuine 7-folder growth structure with zero one-file folders. The campaign is therefore NOT a rebuild — it is (a) a consumer-wiring campaign closing ONE campaign-defining unwired seam (the Element Failed-lifecycle assessment capability, fully built and explicitly naming Compute as consumer, that `Analysis/assessment` is blind to), (b) a seam-ledger reconciliation re-pointing ~6 stale sibling anchors invalidated by the Persistence/Geometry campaigns, (c) a bounded set of forward capability additions in the four assessment runners (typed `SolvePhase`/`FailureKind`, member EC2 shear), and (d) two consistency repairs (the stale FaultDetail wire count, the section-dispatch strata mis-attribution). Architecture is sound; the perimeter is the work.

## [01]-[PER_PAGE_VERDICTS]

Format: `page` — verdict/10 · charter-as-is · defects(file:line) · split/merge/move.

### Analysis/ (6 pages, 2532 LOC) — the scope epicenter

- `Analysis/assessment.md` — **8.5** · The discipline-assessment spine: `AssessmentRoute`/`AssessmentRequest`/`AssessmentResult`/`RerunPolicy`/`Analysis.Assess`, content-keyed `Node.Assessment` write-back over the neutral `Assign`/`Assessment` edge, `ComputeFault` 2217-2219, `ComputeReceipt.Assessment`. Interior craft is 9.5 — the fact-stream collapse, the H7 `NodeId.Content` self-hash, the CanonicalBytes count-prefix discipline are textbook. **THE campaign-defining defect is a coverage hole, not a bug**: it consumes ONLY `AssessmentPayload.Computed`/`.Pending`; it is BLIND to the seam's full lifecycle capability (`assessment.md:245` `graph.Find<Node.Assessment>(nodeId)` blanket-short-circuits ANY cached node to the 412-noop regardless of `Outcome`; `:248` `Run(...).Bind(WriteBack)` aborts a runner failure to `ComputeFault` and writes NO `Failed` node, so deterministic failures never cache and re-run every call; no `Outcome.Dispatchable` re-solve sweep entry exists; no `Diagnostic.Kind.Transient` retry gate). Owner-as-should-be: lifecycle-aware `Assess` (a `Coherent`-gated `Outcome` inspection deciding recompute vs 412-noop vs Failed-cache-hit), a `WriteBack` Failed leg composing `Diagnostic.Of(phase,kind,message,key)` + `AssessmentPayload.Failed(...)`, a `Sweep` entry dispatching `Outcome.Dispatchable` rows (Pending/Stale) and skipping Queued/Running, a `Transient`-gated retry. No split (the spine is one owner); the lifecycle additions are rows/legs on it.
- `Analysis/structural.md` — **8** · The `Discipline.Structural` runner: `FrameModel` idealization, `FrameBackend` (BFE-3D/FEALiTE-2D over shared CSparse `SparseLU`), the `(DesignCode, LimitState)` capacity table of real slenderness-aware delegates. Interior is 9.5 (the FE-library confinement to the sparse `BarElement` path, the real buckling/LTB/deflection, the timber Orthotropic shear closure). **Two forward gaps**: (1) the `en1992` shear cells (`:636-637`) are the PLAIN-concrete `ConcreteVc·Av` bound only — the member EC2 §6.2.3 `V_Rd,s=(Asw/s)·z·f_ywd·cotθ` stirrup pairing Materials explicitly defers here (`Rasm.Materials/.planning/Component/capacity.md:253-254`) is absent; (2) the runner returns opaque `ComputeFault.AnalysisRunFailed($"<bfe-solve-break:...>")` (`:285`, `:389`) — a stringly fault, not the typed `SolvePhase`/`FailureKind` the assessment lifecycle rebuild demands. No split.
- `Analysis/physics.md` — **9.5** · The collapsed `Thermal`/`Acoustic`/`Fire` `BuildingPhysics` kernel — ISO 6946 series-U, EN ISO 13788 Glaser lower-convex-hull condensation-rate, EN ISO 10077-1 whole-window `Uw`, ISO 12354 mass-law STC via the seam `RatingContour`, EN 1993-1-2 steel critical-temperature march, EN 1992-1-2 concrete tabulated check. Reference-grade; the SI-`Dimension`-algebra fact construction and the window-vs-envelope dispatch are exemplary. Failure path shares the opaque-`ComputeFault` gap (item 1). No change beyond the runner-typing rebuild.
- `Analysis/aggregator.md` — **9.5** · The relocated multi-ply `AssemblyAggregator` (`Aggregate`/`AggregateEnvironmental`/`AggregateCost`/`AggregateWindow`) with per-discipline `[FOLD_STATE]` accumulators and the shared basis-aware `DeclaredQuantity`. The NaN-decoupled-per-discipline projection and the one `RatingContour.Stc.Fit` share are best-in-class. Clean.
- `Analysis/energy.md` — **9.5** · The `Discipline.Energy` OpenStudio-in-process + EnergyPlus-subprocess runner with the parameterized `EnergyToolchain` discovery, the SWIG `Optional<T>`/`IDisposable`/`Path` boundary discipline, the full per-end-use `SqlFile` fold. The native-handle bracketing and version-lock guard are meticulous. Failure path shares the opaque-`ComputeFault` gap (item 1); `AssessmentResult.ResultBlob` egress-sink is documented-pending (`:76`, the injected artifact-sink cross-stratum gap). No change beyond runner-typing.
- `Analysis/lifecycle.md` — **9.5** · The `Environmental`/`Cost` runner: the hand-thin EC3/openEPD `Ec3Service` (GET-only, `HybridCache` success-only content-key, boundary-throw-to-`Fin`), the basis-aware `Normalize` ingress, the pure-sync `RunCarbon` after async `EnrichCarbon` `GraphDelta`. The domain-scalar `kgCO2e` discipline (never `UnitsNet.Mass`) and the `MeasurementBasis` fold are exemplary. Clean.

### Runtime/ (7 pages, 2797 LOC) — admit-to-receipt boundary

- `Runtime/receipts.md` — **9.5** · The 25-case `ComputeReceipt` union (24 inline + the `Assessment` partial from `assessment.md`), the fold-projection family, the `WireStamps` NodaTime-protobuf bridge, the fingerprint-gated `BenchmarkClaim`. The optional-`init`-evidence collapse (Factorization) and the `ReceiptFolds` derive-on-read discipline are reference. `Rasm.AppHost`+`Rasm.Persistence` project refs correct. Clean.
- `Runtime/scheduling.md` — **9.5** · Five bounded `WorkLane` rows, `LaneRuntime` enqueue capsule, `CpuBudget`, the `JobGraph` batch-wave dependency scheduler (speculative/preemptible/spill/content-key reconcile/rolled progress) with `JobState` 8-row lifecycle, `JobCheckpoint`/`CheckpointPort` Persistence-spill, `GraphCyclic` 2220. The `Reconcile` content-key reactive engine over the existing identity+cache rails is a genuine incremental-compute engine. Clean. NOTE: `JobState` (job-scheduling lifecycle) is ORTHOGONAL to the assessment `AssessmentOutcome` (discipline-node lifecycle, item 1) — do not conflate.
- `Runtime/admission.md` — **9.5** · The `ComputeIntent` 6-case union + `Spec`, the `Substrate` 5-row axis with the one derived `Veto`, the `ComputeFault` 2200-band CANONICAL owner (2200-2212 core; band custody map at `:173`), `SubstrateSelection` ordered-degrade fold. The eligible-chain-IS-degrade-order collapse and the single `!Providers.Contains(Key)` gate are exemplary. Clean. Confirms the discipline lanes (`Assess`/`Solve`/`Fit`/`Compile`) are self-contained entries, NOT admitted through `ComputeIntent` — so the item-1 re-solve `Sweep` is a new Analysis-rail entry.
- `Runtime/codecs.md` — **9** · Field/result/geometry-delta codecs, the two-hop IFC tessellation bridge, `GeoArrowBuffer.ToImportedGeometry` (item 6 — the GLB wire layout GeoArrow shares, RECORDED correctly, no second codec; `:53-114`), the ifctester IDS relay, 3D-Tiles octree, policy-seeded `InterchangeIdentity`. The FastCDC gear-hash chunker and the true-spatial-stencil residual predictor are real. Interior clean. Perimeter: composes `Rasm.Persistence (project)` generically — the ARCHITECTURE.md:111/115 seam anchors that name it point at DELETED pages (item 5).
- `Runtime/channels.md` — **8.5** · Five proto services (Client-in-package/Server-at-roots), the descriptor-diff contract-evolution law, the `FaultDetail`/`WireFault` projection, the `WireDocument` parity flagship, `GraphDiff`/`SubtreeFetch` ledger seam. **Concrete defect**: the `FaultDetail` rail is a stale "seventeen-code taxonomy" (`:9`, a 15-row `FrozenDictionary` StatusRail) — the ComputeFault band is now 21 cases (2200-2220); the analysis lane (2217-2219, the item-1 faults) and scheduling lane (2220) are UNCOVERED, so those faults cannot cross the one `FaultDetail` family — a direct violation of the ARCHITECTURE.md [05]#3 "every fault crosses the wire through the one FaultDetail family" law. Perimeter: ARCHITECTURE.md:119 names `Persistence/Sync` (deleted, item 5).
- `Runtime/payload.md` — **9** · The GPU-ready residency PAYLOAD codec (meshlet/quantized/splat) → AppUi `ResidencyManifest.Mint` seam (ARCHITECTURE.md:78). meshoptimizer-composed, HOST-LOCAL. Charter sound.
- `Runtime/progress.md` — **9** · Monotonic `ProgressPhase`/`ProgressCell` (Atom-backed CAS-rank-guarded zero-alloc marks), `SubscriptionPolicy` cadence, the AppUi/wire/aggregate seam fold. Charter sound; `JobGraph` composes its `Aggregate`.

### Model/ (7 pages, 1333 LOC) — ONNX identity/session/inference/generative

- `Model/embedding.md` — **9.5** · One `VectorEncoding`×`VectorScore`×`EmbeddingVector` `Encode`/`Score`/`Rank` fold over `TensorPrimitives`, content-key crossing to Persistence by reference only, honest two-stage retrieval (binary coarse gate → fine rerank). The `ProductCodebook` is Persistence-trained (`Query/lane#VECTOR_CODEBOOK`, `:5`/`:272`). Interior reference-grade. Perimeter: the VECTOR_CODEBOOK anchor (`Query/lane`) is the seam the Persistence campaign renames to `Query/retrieval` (item 5b) — the seam is declared honestly Compute-side; the anchor follows the extraction.
- `Model/inference.md` — **9.5** (charter) · The OrtValue-only run-mode fold, `RunInput` polymorphic admission composing `Tensor/residency` `BoundFlow`/`TensorBridge`, the `RESULT_CACHE` seam (`ModelResultKey`→Persistence `ModelResultIndex`, ARCHITECTURE.md:102). Charter reference; Persistence seam anchor (`Query/cache`) SURVIVES the Persistence campaign.
- `Model/identity.md` — **9.5** (charter) · `ModelIdentity`/`ModelSource`/`ModelFingerprint`; `Checksum` composes the kernel seed-zero `XxHash128` (ARCHITECTURE.md:100), `ArtifactIndexRow` (`:101`). Charter sound; seams survive.
- `Model/sessions.md` — **9.5** (charter) · One shared `InferenceSession` per checksum, device-keyed fleet-shared compiled context, `ArtifactIndexRow.EpContext/OnnxProfile` (ARCHITECTURE.md:101). Charter sound.
- `Model/providers.md` — **9** (charter) · EP-parameterized rows (CPU/CoreML), autoEP `OrtEpDevice` discovery, `ModelPrecision` posture. Charter sound.
- `Model/generative.md` — **9** (charter) · ORT-GenAI token-streaming, one `GenerationEvent` polymorphic stream, EOS oracle, LoRA hot-swap. AppHost `IEmbeddingGenerator`/`IChatClient` port (ARCHITECTURE.md:118). Charter sound.
- `Model/extension.md` — **8.5** (charter, 66 LOC — thinnest page) · `CustomOps` custom-op registration + the non-tensor (`string`-tensor/`ZipMap`) boundary. Legit distinct concern; MINOR merge-consideration only (could fold into `sessions.md`'s `SESSION_CAPSULE` admission it already extends) — but it owns a real egress concern, so NOT a one-file-folder defect. Keep or fold is a judge call, low priority.

### Solver/ (6 pages, 4470 LOC) — discretize→solve→optimize spine

- `Solver/contract.md` — **9.5** · The `PhysicsKind`×`BoundaryCondition`×`ElementClass` uniform `SolveProblem`/`SolveLane` fold + `CoupledLane`; the `Bᵀ·D·B` assembly over the numeric lane, the modified Newton-Raphson with genuine nonlinear residual, the `ConstitutiveModel` stress-update whose tangent is the AD HVP. Reference-grade (the deleted-fake self-policing on the penalty/residual/witness is exhaustive). Composes Element `MechanicalOf` (ARCHITECTURE.md:123) + Persistence result artifact. Clean.
- `Solver/clash.md` — **9.5** · `ClashScale` narrow-phase over the geometry-owned `AccelerationStructure` node-link wire (`CLASH_SEAM`, ARCHITECTURE.md:121) + `DigitalTwin` Kalman/CUSUM ROM loop. NEVER builds/refits the index (seam-owned). The complete two-direction Möller-Trumbore + Ericson closest-distance and the CLASH_GOLDEN byte-identical fixture are real. Clean. NOTE: depends on the Geometry campaign landing `SpatialIndex.ToAcceleration`/`NodeLinkProjection` (Geometry brief V8); ARCHITECTURE.md:120's reverse "Geometry solver consumes Compute LM" claim is SPURIOUS (Geometry brief V14 kills it).
- `Solver/discretization.md` — **9** (charter) · One volumetric `MeshKernel` (tet/hex/boundary-layer, adaptive h/p/hp), `ElementClass` isoparametric `Sample` collapsing 12 element types. Charter reference.
- `Solver/optimizer.md` — **9.5** (charter, 1128 LOC, largest page) · One `Optimizer` axis, per-family genuine kernels (GeneticSharp NSGA, OR-Tools CP-SAT/MILP via typed builder, surrogate ROM/GP/RBF), one `Optimize` entry. Charter reference; the string-eval prohibition (ARCHITECTURE.md [05]#4) is load-bearing.
- `Solver/sweep.md` — **9** (charter) · `SweepGrid` DOE → `ParetoFront` + `SensitivityTornado`, `FrameBudget` early-stop. Charter sound.
- `Solver/uncertainty.md` — **9.5** (charter) · One `UncertaintyMethod` axis (LHS/PCE/FORM/SORM/Saltelli/Morris/subset), `RandomVariable` inverse-transform, one `Propagate` over `LowDiscrepancy`. Charter reference.

### Tensor/ (9 pages, 3765 LOC) — CPU tensor + BLAS numeric core

- `Tensor/dispatch.md` — **9.5** (charter, 958 LOC) · Arity kernel-delegate dispatch, claim-gated `ParallelHelper` partition, the differentiable-adjoint equivalence law, `DEVICE_KERNELS` WGSL lowering. Seams: `Rasm/Vectors` `OperatorRow`/`MeshAdjointSnapshot` (ARCHITECTURE.md:124), AppUi `ONE_WGPU_DEVICE` (`:112`). Charter reference.
- `Tensor/factor.md` — **9.5** (charter) · `SparseFormat` CSR ingestion, `FactoredOp`/`IterativeMethod` criterion-stack, `EinsumPlan` greedy contraction, `ShardPlan.Blocked`→Persistence `ModelResultIndex` reuse (ARCHITECTURE.md:103). Charter reference.
- `Tensor/blas.md` — **9** (charter) · `LinearProvider` RID-keyed MathNet+TorchSharp-ATen, `LevenbergMarquardt`/thin-QR, shape-routed. The `[NATIVE-BLAS]-[BLOCKED]` TASKLOG card (the only open Compute card) is honest (blocked on a host RID resolving the native asset). Charter sound.
- `Tensor/residency.md` — **9.5** (charter) · `OrtResidency` 5-gate lattice, `TensorBridge` sole OrtValue C-data factory, `EncodedTensor` wrapping the kernel `EncodedGeometry` (Geometry `Drawing`, ARCHITECTURE.md:125), AppHost `PackKind` alignment (`:126`). Charter reference.
- `Tensor/vocabulary.md` — **9** (charter) · `Tensor<T>`+`TensorDtype`+the 196-row `TensorOpFamily` table under `ToleranceClass`. Charter sound.
- `Tensor/quadrature.md` — **9** (charter) · Accuracy-routed quadrature, adaptive integrator, periodic spectral operator, typed evidence carriers. Charter sound.
- `Tensor/sampling.md` — **9** (charter) · Owned Sobol/Halton (`JoeKuo`/prime-base, no MathNet surface) + RBF scatter reconstruction, replicate families with spread. Charter sound.
- `Tensor/memory.md` — **9** (charter) · `AllocationClass` staging, `Grant` rail, `AllocationEvidence`/`StagingEventKind` fact stream, RecyclableMemoryStream. Charter sound.
- `Tensor/layout.md` — **9** (charter, 145 LOC) · `LayoutForm`/`Contiguity`/`AxisPermutation`/`ReshapeOp<T>` total-Switch shape-edit family. Charter sound.

### Symbolic/ (4 pages, 757 LOC) — closed CAS + units boundary

- `Symbolic/expression.md` — **9.5** (charter) · `SymbolicExpr` `[ComplexValueObject]` over MathNet.Symbolics F# `Expression`, identity = canonical-NF `XxHash128` alone, `BuildSpec` admission, `SymbolicFault` 2213-2215. Charter reference.
- `Symbolic/dimensional.md` — **9.5** (charter) · `DimensionMonomial` ℚ⁷ fold, `DimensionProof` `Validation`-accumulating, `DimensionMismatch` 2216; the ℚ⁷↔Element `Dimension` ℤ⁷ align-not-couple seam (ARCHITECTURE.md:97). Charter reference.
- `Symbolic/lowering.md` — **9** (charter) · `CompiledExpr` content-keyed cache over `Compile.compileExpression{,1-4}`, analytic-Jacobian arm, Persistence `Query/cache` seam (ARCHITECTURE.md:98, SURVIVES). Charter sound.
- `Symbolic/units.md` — **9** (charter) · 21 `QuantityFamily` rows, UnitsNet boundary admit-once, dual `UnitEvidence`, the host-free-peer wire seam (ARCHITECTURE.md:96). Charter sound.

### Stats/ (2 pages, 1447 LOC) — a legitimate 2-page folder

- `Stats/estimator.md` — **9.5** (charter, 869 LOC) · One `Estimator`/`EstimatorModel` union over regression/GLM/PCA/cluster/classify/hypothesis/time-series on one Fit/Predict contract. Charter reference.
- `Stats/signal.md` — **9** (charter) · `SpectralTransform` FFT/STFT/PSD/wavelet + `FilterDesign` FIR/IIR over MathNet IntegralTransforms. Charter sound.

## [02]-[CROSS_CUTTING]

- **DUPLICATION**: none material. Every axis is single-owner (`ComputeFault` one band, `ComputeReceipt` one union, one `LinearProvider`/`SparseOps`, one `RatingContour` fit shared seam-side, one `AssemblyAggregator`). The corpus polices its own deleted-forms exhaustively. The only "duplication" is the FaultDetail wire lagging the fault band (count, not a second union) — a consistency repair, not a dedup.
- **CONCERN MIXING**: none. Discipline runners read the concrete graph and return the one `AssessmentResult` stream; the seam owns declarations, Compute decodes/writes. `Model/extension.md` (66 LOC) is the only thin-page merge-consideration, and it owns a genuine non-tensor-egress concern.
- **HARDCODING-vs-GENERATOR**: clean. Rosters are seed DATA (design-code capacity table, substrate axis, `TensorOpFamily` 196 rows, `UncertaintyMethod`, `EndUse` fuel taxonomy from `EndUses.fuelTypes()`). Physical constants are named/cited (`StandardGravity`, `RsiWM2K`, `MassLawConstantDb`, EN/AISC factors). Energy toolchain is env→configured→bundled parameterized. No fragile stringy logic on a hot path.
- **DEAD CARRIERS**: none found in the deep-read set — every `init`-optional evidence column has a stamping factory, every fold state projects. (Contrast the Geometry brief's E11 dead-carrier census: Compute has no analogue.)
- **UNWIRED SEAMS (the campaign work)**: (1) **the Element Failed-lifecycle capability** — `AssessmentOutcome` 8 rows (Pending/Queued/Running/Computed/Failed/Cancelled/Stale/Superseded) with `Dispatchable`/`Coherent`/`Next`, `Diagnostic.Of(phase,kind,message,key)`, `FailureKind.Transient`, `AssessmentPayload.Failed` — is FULLY BUILT in `Rasm.Element/.planning/Assessment/assessment.md:70-169` and its comments EXPLICITLY name "the Rasm.Compute sweep" (`:60`) and "a Diagnostic.Kind.Transient policy in Compute" (`:59`) as the consumer, yet `Analysis/assessment.md` consumes only `Computed`/`Pending`. This is the single largest unwired-seam in the corpus. (2) the FaultDetail wire 17→21 code gap (channels.md:9).
- **UNMINED CAPABILITY**: the corpus is heavily mined. The one named-open capability is `AssessmentResult.ResultBlob` egress (the injected artifact-sink through `Assess`/`Run`, `assessment.md:76`/`energy.md`) — a deferred cross-stratum sink the runner threads `None` for today; a genuine forward capability, not naivety.
- **FOLDER ARCHITECTURE**: SOUND — a graded positive. Seven folders, all ≥2 pages, all real growth axes (Tensor numeric core, Symbolic CAS, Model ONNX, Solver spine, Stats learning, Runtime boundary, Analysis discipline rail). Stats at 2 pages is a legitimate minimum, not a one-file folder. No flat sprawl, no cram. The `ARCHITECTURE.md` codemap, seam ledger, and prohibition blocks are the best-structured governance in the reviewed briefs. No split/merge/move is warranted.

## [03]-[SEAM_LEDGER_DIFF] (both directions)

STALE-ANCHOR (declared in ARCHITECTURE.md, anchor page DELETED by the Persistence campaign; verified zero fence references — ledger-only edits):
- `ARCHITECTURE.md:111` `Runtime ← Persistence/Sync [PROJECTION] FastCDC` → `Sync/` deleted; re-point to the `Version/timetravel`/`Store` FastCDC snapshot-frame owner (Persistence brief E6/COMPUTE:111).
- `ARCHITECTURE.md:115` `Runtime/codecs ⇄ Persistence/Query/pipeline [PORT] parse-to-canonical-bytes` → `Query/pipeline` deleted (phantom, Persistence brief E1); re-point to `columnar`.
- `ARCHITECTURE.md:116` `Compute → Persistence/Store/quality [SHAPE] geometry-derived anomaly rule source` → `Store/quality` deleted (phantom); re-point to the columnar/provisioning verification surfaces (Persistence brief V12). NOTE: thin/near-vestigial — `clash.md:18` explicitly disclaims owning any quality plan/store; verify the SHAPE seam still has a real producer or drop it.
- `ARCHITECTURE.md:119` `Runtime/channels → Persistence/Sync [WIRE] Confluent.SchemaRegistry.Serdes.Protobuf Kafka` → `Sync/` deleted AND the Kafka/Confluent cluster is Persistence-brief-default-REMOVE; re-point to `Version/egress` (NATS/CloudEvents/pg_net) or re-scope — Compute owns the Protobuf frame; the Persistence egress owner no longer runs a Confluent Kafka Protobuf leg.
- `ARCHITECTURE.md:99` `Model/embedding ⇄ Persistence/Query/lane [CONTENT_KEY] VECTOR_CODEBOOK` → `lane.md` extracts `retrieval.md` (Persistence brief V5b); re-point VECTOR_CODEBOOK to `Query/retrieval`. Compute-side declaration is honest (item 5b); anchor follows the extraction.

GEOMETRY-COUNTERPART DRIFT:
- `ARCHITECTURE.md:120` `Solver ⇄ Rasm/Geometry [SHAPE] ... Geometry/Processing/solver consumes the Compute LM/constraint Solver rail` — the reverse clause is SPURIOUS (Geometry `solver.md:295-347` composes MathNet directly; Geometry brief V14 kills it). Correct to one-directional: Compute consumes the Geometry `SpatialIndex.ToAcceleration` node-link wire (`:121`); Geometry does NOT consume Compute's LM.

WIRED-UNDECLARED (composed in a fence, no ledger row) — none material found; the ledger is unusually complete. The Persistence project-ref is composed generically across codecs/channels/scheduling/receipts/factor/inference/identity/sessions/lowering/embedding, all covered by existing rows (:98-109) — the defect is stale anchors, not missing rows.

PAGE-LEVEL CYCLES: none. Every seam is upward (kernel/Element) or lateral-to-app-platform (Persistence/AppHost/AppUi); the ARCHITECTURE.md [04] SEAM_PROHIBITIONS + [05] PROHIBITIONS enforce the acyclic strata (Compute never references the AEC peers Materials/Bim except at the companion-rpc wire).

## [04]-[UPSTREAM_CONSUMPTION]

GEOMETRY brief (`RASM-CS-GEOMETRY-BRIEF.md`, finalized law):
- CONSUME: the rebuilt `Spatial/index` `ToAcceleration` returns `Fin<>` over a KERNEL-owned acceleration wire (V8) — `clash.md`'s `AccelerationStructure` decode consumes it; verify the CLASH_GOLDEN 160-byte fixture stays byte-identical after the Geometry `NodeLinkProjection` rebuild. The `EncodedGeometry` residency wire (`Drawing`, V13) `Tensor/residency` wraps as `EncodedTensor`.
- INVALIDATED ASSUMPTION: `ARCHITECTURE.md:120`'s reverse "Geometry solver consumes Compute LM" edge dies (Geometry V14); the `index⇄Compute` bidirectional overstatement collapses to one-directional. `clash.md`/`contract.md` interiors are unaffected (they already consume, never provide).

PERSISTENCE brief (`RASM-CS-PERSISTENCE-BRIEF.md`, finalized law):
- CONSUME: `ModelResultIndex` (the ONE cross-process recency horizon, `cache.md` survives) — `inference.md`/`factor.md`/`receipts.md` read it by reference; the `blob` retention class (`Version/retention`) the persisted `AssessmentPayload` registers in (`assessment.md:188`); the object-store `ContentAddress` residence the tessellation bridge dedups against (`codecs.md`).
- INVALIDATED ASSUMPTIONS (the item-5 re-points above): `Sync/` deleted (→ `:111`/`:119`), `Query/pipeline` deleted (→ `:115`), `Store/quality` deleted (→ `:116`), `Query/lane`→`Query/retrieval` (→ `:99`), the Confluent/Kafka egress leg removed (→ `:119`). All are ledger-anchor edits — zero fence interior changes (verified: the fences compose Persistence generically).

WATERFALL RIPPLE (this target → upstream brief edits): NONE REQUIRED. The Persistence brief already RECORDS all five Compute→Persistence stale rows as counterpart obligations (E6: COMPUTE:111,115,116,119; the VECTOR_CODEBOOK re-anchor under V5b/V12) — the demanding-consumer edit is already present upstream. The Geometry brief already records the `:120`/`:121` corrections (V14). No upstream brief lacks a capability this target demands.

## [05]-[SCOPE_MANDATE_DISPOSITION]

1. **COMPUTE_ASSESSMENT_RUNNERS** — CONFIRMED, campaign-defining. Seam capability fully built + names Compute (Element `assessment.md:59-60,70-169`); Compute blind. Realize: lifecycle-aware `CacheFirst` (inspect `Outcome`, not blanket-`Find`), a `WriteBack` Failed leg (`Diagnostic.Of`+`AssessmentPayload.Failed`), a `Dispatchable`-driven `Sweep` (dispatch Pending/Stale, skip Queued/Running), a `Transient` retry gate — GATED on rebuilding the four runners to surface typed `SolvePhase`/`FailureKind` (they return opaque `ComputeFault.AnalysisRunFailed` today: `structural.md:285,389`, `energy.md:279`, `physics.md`, `lifecycle.md`).
2. **Section→capacity-overload dispatch** — CONFIRMED as a STRATA-MIS-ATTRIBUTED residual (verdict candidate). Compute is APP-PLATFORM and CANNOT reference Materials `SteelDesign.Capacity`/`ColdFormedSections.Of` (AEC-DOMAIN downward edge, forbidden). `structural.md` reads the arm-ERASED neutral `SectionProperties` (M7-baked) — the `Catalogued`/`ColdFormedC` `SectionProfile` arm is invisible seam-side. Compute already discriminates cold-formed via the `aisi-s100` DesignCode ROUTE (gross-section bound, effective-width deferred to Materials `ColdFormedDetail`). Disposition: the DECISION must rule the overload dispatch Materials-internal (capacity.md/component.md resolver) OR seam-baked (a cold-formed effective-section on the graph the AISI cells read) — it is NOT a `SteelDesign.Capacity`-in-Compute concern.
3. **Member EC2 §6.2.3 V_Rd,s shear** — CONFIRMED forward Compute DESIGN_CHECK concern. Materials `capacity.md:253-254` explicitly defers `V_Rd,s=(Asw/s)·z·f_ywd·cotθ` (stirrup spacing is member-scope) here and returns only the web-crushing ceiling `V_Rd,max`; `RcElastic.ShearLinkAreaMm2` (`capacity.md:144`) carries `Asw`. Compute `structural.md` en1992 shear (`:636-637`) is plain-`V_Rd,c` only. Add the member shear-reinforcement pairing as a `(en1992, shear-*)` capacity refinement reading the seam-baked `Asw` + a member stirrup-spacing input.
4. **Component-paradigm re-point** — CLEAN. Zero stale `ComponentSection` references (grep empty). All Analysis pages consume `graph.SectionOf`/`SectionProperties`/`ProfileSet` (the post-DECISION `Sectioned`-pinned twenty-column seam); the CONSUMER_RIPPLE ZERO-structural-ripple rule (DECISION:72) holds.
5. **Persistence seam re-point + VECTOR_CODEBOOK** — CONFIRMED, §[03] above. Four stale anchors (ARCHITECTURE.md:111/115/116/119) + the VECTOR_CODEBOOK anchor (:99) re-point; all ledger-only (zero fence edits).
6. **DATA_GEOARROW_GLB** — CONFIRMED RECORDED. `codecs.md:53-114` `GeoArrowBuffer.ToImportedGeometry` records Compute as the GLB-wire-layout owner GeoArrow shares (no second spatial codec); TASKLOG `[DATA_GEOARROW_GLB]-[COMPLETE]`. No re-mint.

## [06]-[VERDICT_CANDIDATES] (campaign-defining, evidence-ranked)

1. **THE ASSESSMENT LIFECYCLE IS THE CAMPAIGN SPINE.** The Element seam ships a complete 8-row `AssessmentOutcome` lifecycle (`Failed`/`Stale`/`Dispatchable`/`Coherent`/`Next`), `Diagnostic.Of(phase,kind,message,key)`, and `FailureKind.Transient`, with comments naming "the Rasm.Compute sweep" and "a Diagnostic.Kind.Transient policy in Compute" as the consumer (`Rasm.Element/.planning/Assessment/assessment.md:59-60,70-169`). `Analysis/assessment.md` consumes only `Computed`/`Pending` and blanket-short-circuits any cached node (`:245`) — the largest unwired seam in the corpus. Realizing it (lifecycle CacheFirst + Failed write-back leg + Dispatchable Sweep + Transient retry) is the campaign's defining work and gates the four-runner typed-`SolvePhase`/`FailureKind` rebuild.

2. **THE CORPUS IS A CONSUMER-WIRING CAMPAIGN, NOT A REBUILD.** Every fence is 9–9.5/10; the folder architecture is sound; there is zero page-interior rot, zero dead carrier, zero old-paradigm reference, zero page cycle. The build legs must be surgical seam-wiring + forward-capability additions on settled owners — NOT the ground-up hostile rebuild the Geometry/Persistence corpora needed. Treating Compute pages as greenfield-to-rebuild would destroy award-grade interiors; the leg partition is (a) assessment lifecycle, (b) seam-ledger reconciliation, (c) two runner refinements (shear, typed failure), (d) two consistency repairs.

3. **THE FAULTDETAIL WIRE IS STALE AT SEVENTEEN CODES vs A 21-CASE BAND.** `channels.md:9` projects a "seventeen-code taxonomy" (2200-2216); the band is now 2200-2220 (analysis 2217-2219 + scheduling 2220). The item-1 assessment faults CANNOT cross the one `FaultDetail` family — a direct violation of ARCHITECTURE.md [05]#3. Thread 2217-2220 into the `StatusCode→WireFault` rail + `FaultDetailWire` in the same leg as the assessment lifecycle.

4. **SECTION→CAPACITY DISPATCH IS A STRATA MIS-ATTRIBUTION, NOT A COMPUTE OWNER.** The mandate's "SteelDesign.Capacity in the Compute structural resolver" is architecturally impossible — Compute (APP-PLATFORM) cannot reference Materials (AEC-DOMAIN); `structural.md` reads the arm-erased neutral `SectionProperties`. The DECISION must rule the Catalogued/ColdFormedC overload dispatch Materials-internal or seam-baked; Compute already route-discriminates cold-formed (`aisi-s100`). Recording this prevents a build leg from authoring the forbidden downward edge.

5. **THE SEAM LEDGER IS ITS OWN DELIVERABLE — SIX STALE SIBLING ANCHORS, ALL LEDGER-ONLY.** Five Persistence anchors (ARCHITECTURE.md:111/115/116/119/99) point at pages the Persistence campaign deletes/renames; one Geometry anchor (:120) carries a spurious reverse edge. All are ARCHITECTURE.md edits with ZERO fence interior changes (verified: fences compose Persistence generically). The Persistence/Geometry briefs already record these as counterpart obligations — no upstream waterfall edit needed. This is a pure governance-truth pass.

6. **THE FOUR RUNNERS SHARE ONE OPAQUE-FAULT DEFECT THAT THE LIFECYCLE REBUILD RESOLVES UNIFORMLY.** `StructuralAnalysis`/`BuildingPhysics`/`EnergySimulation`/`LifecycleAssessment` all return `Fin<AssessmentResult>` aborting to `ComputeFault.AnalysisRunFailed($"<...stringly...>")` (`structural.md:285,389`; `energy.md:279`). The typed `SolvePhase`/`FailureKind` the Failed-lifecycle Diagnostic needs is one uniform refinement across the four `Run` folds — not four separate rebuilds — because they share the `AssessmentResult`/`Fin` contract.

7. **EC2 MEMBER SHEAR IS THE ONE GENUINE FORWARD CAPABILITY IN THE STRUCTURAL RUNNER.** Materials hands off `V_Rd,s` with `Asw` on the seam (`Rasm.Materials/.planning/Component/capacity.md:144,253-254`); `structural.md` en1992 shear is `V_Rd,c`-only. This is a clean `(en1992, shear-*)` capacity-cell refinement + a member stirrup-spacing input on `StructuralPolicy`/the request — additive on the existing capacity table, no new surface.

8. **FOLDER ARCHITECTURE NEEDS NO CHANGE — A GRADED POSITIVE.** Seven ≥2-page growth-axis folders, zero one-file folders, zero flat sprawl; the ARCHITECTURE.md codemap + seam ledger + [04]/[05] prohibition blocks are the strongest governance surface across the reviewed briefs. The only micro-consideration is `Model/extension.md` (66 LOC) — a legit distinct concern, keep-or-fold is a low-priority judge call, NOT a defect.
