<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <Description>sqlite-vec loadable vector search extension staged per runtime identifier</Description>
        <PackageLicenseExpression>MIT OR Apache-2.0</PackageLicenseExpression>
        <!-- Upstream release loadables pinned in release.json replace a vcpkg port -->
        <VersionManifestFileName>release.json</VersionManifestFileName>
    </PropertyGroup>
</Project>
