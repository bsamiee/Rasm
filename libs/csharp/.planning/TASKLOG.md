# [CSHARP_BRANCH_TASKLOG]

Cross-package C# work no single folder owns: the cross-folder seam arbitrations and the host-boundary seams the future app root composes. Per-folder work lives in each package's own task pool; cross-language amendments (the CRDT wire-vocabulary change, the SDK-codegen consumption) live at the cross-libs tier and are referenced as wire seams, never restated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open task card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — altitude only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
- Atomic: <present only on a minor-scope task; names the small unit so a later session sizes its turn>.
Capability, Shape, Unlocks, and Anchors are required on every open card, Atomic included; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Tasks state landing-grain work decomposing an idea.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[STORE_RETRY_EXECUTOR]-[COMPLETE]: the Tension was REFUTED — `HopOutcome` is transport-neutral and only its additive exception arms are HTTP-shaped. `[LAYER_SPLIT]` seats each band by seam: `RemoteStoreFault` and `CacheFault` on row `[02]` through `OutboundHop.ObjectStore`/`WideColumn` on `HopTransport.StoreRail`, `CoordinationFault` on row `[01]` under `Coordinate.Verified`, `EmbeddedFault` on row `[03]` through `Reoffer`'s four `RetryShape` routes. `Transport` gained `ObjectVerb` and `ContentAddress`; `StoreRedrivePort` is the root-bound seam, unbound degrading to one pass.
[SUBSTRATE_TIER_PROMOTION]-[COMPLETE]: every roster entry re-verified against the live csproj census and promoted whole — `geometry3Sharp` (Bim+Fabrication, `[MESH_PROCESSING]`), `ACadSharp` (Bim+Fabrication+AppUi three-partition merge, the AppUi WRITE block lifted out of `api-drafting-export.md` which re-cut to OpenXml + registration; the AC1012 read floor and `net10.0` asset decompile-verified), `NREL.OpenStudio.macOS-arm64` + `PollinationSDK` (`[ENERGY_SIMULATION]`), `Apache.Arrow` + `Microsoft.Data.Sqlite` (`[DATA_SUBSTRATE]`; the Persistence multi-package Arrow file dual-home-split — core merged up, the ADBC/Flight/FlightSql/Compression train survives as `Rasm.Persistence/.api/api-arrow-egress.md`), `NATS.Net` + the three `CloudNative.CloudEvents` rows (`[EVENT_TRANSPORT]`; the Kafka binding split out as `Rasm.Persistence/.api/api-cloudevents-kafka.md`), the two `NetTopologySuite.IO` rows (`[PLANAR_GEOMETRY]`), `PeterO.Numbers` (`[NUMERIC_SUBSTRATE]`), the two `Speckle` rows (`[GEOMETRY_INTERCHANGE]`), and `MessagePack` + its `MessagePackAnalyzer` companion (`[WIRE_CODEGEN]`; the Materials carve lifted mid-pass, both Materials touch points landed directly). `Alimer.Bindings.MeshOptimizer` was already branch-tier and re-seated from `[GPU_DEVICE]` to `[MESH_PROCESSING]` with its stray Bim `[02]` row moved to `[03]`. Twenty-nine folder catalogs deleted into fifteen branch catalogs, `api-drafting-export.md` re-cut to its surviving package, every citing anchor repointed, folder `[03]` sections mirror the registry labels (AppUi's `[GEOMETRY_SUBSTRATE]`/`[MEMORY_SUBSTRATE]` label drift repaired in passing), and the manifests aligned same-pass: `Directory.Packages.props` re-clustered (`Compute/BIM Interchange` dissolved into `Geometry Interchange`/`Mesh Processing`/`Energy Simulation`; `Geospatial Substrate`, `Event Transport`, `Data Substrate` minted; the stale geometry3Sharp disposition comment deleted) with every consuming csproj's `ItemGroup` labels regrouped to match. No census-failed entries.
[TEXTURE_ROSTER_SEAM]-[COMPLETE]: the seam landed whole, both ends same-pass — `TextureRoster`/`TextureCandidate` DECLARED at `Rasm.Element` `Projection/projection.md` beside `TypeCandidate` under the same shared-owner law (neutral columns only: canonical channel token, polarity, repeat/coordinate-set binding facts, the five-double uv frame the producer lowers AT MINT); `Rasm.Bim` `Semantics/appearance.md` `AppearanceProjection.RosterOf` the producer (URL-referenced resolved-mode textures row; Blob/Pixels payloads stay on `StyledAppearance` for the root's byte admission — classification is a NAME fold — and every app-root-edge deferral comment re-cut to the seam route); `Rasm.Materials` `Raster/set.md` `SetIngest.Roster` the consumer, lifting frame/repeat/coordinate-set onto `RosterBinding` policy rows BESIDE the manifest so the set key stays frame-free; the `Declared` arm re-cut from stem re-inference to declared-row VALIDATION (index idempotence + the shared `Refuted` probe refutations) because re-inferring a filename silently discarded the declaration the arm carries — the Materials owner's call on the `UvTransform`→`UvFrame` law recorded at the set page (the Element row now the one meeting point, `ClassifiedMap.Inverted` generalized to the declared linear-domain inversion covering SHININESS and TRANSPARENCYMAP); the fourth cross-edge mirrored at both `ARCHITECTURE#SEAMS` registries.
[BCFZIP_CODEC_CUSTODY]-[COMPLETE]: adjudicated ONE custodian — `Rasm.Bim` `Review/issues#BCF_ARCHIVE` `BcfArchive` owns the `.bcfzip` wire (every live consumer — AppUi board and tour, TS census, contracts manifest — already bound `BcfTopic`/`BcfArchive`, while the Persistence codec was a consumer-less hand-rolled `ZipArchive`/`XDocument` reimplementation of the catalogued Smino toolkit; the Eto-twins plural fails because both ends compose in one app); `Rasm.Persistence/Ingest/issue` rebuilt as the row seam — admission, GlobalId correlation, `Reconcile`, `store.issue.*` facts over root-handed `IssueTopic` rows, `Instant` stamps and typed camera absence per the `BcfTopic`⇄`IssueTopic` correspondence law — the `Review/coordination` custody self-contradiction repaired at both sites, the `[SHAPE]: BcfTopic⇄IssueTopic` seam mirrored at both `ARCHITECTURE#SEAMS`, the ruling landed at branch `RULINGS.md` `[02]-[SHAPE]`.
[GRPC_TOOLS_ROW_RECUT]-[COMPLETE]: the branch README `Grpc.Tools` row states the disk-verified per-consumer codegen split — `Rasm.Element` message-only `GrpcServices=None`, `Rasm.Compute` client stubs `GrpcServices=Client`, both `PrivateAssets=all` build-only, server codegen at app roots; the card's Compute-vs-Persistence framing missed `Rasm.Element` as the second `Grpc.Tools` consumer, and `Rasm.Persistence` carries no `Grpc.Tools` row at all (`Query/federation.md` zero-codegen posture holds as stated).
[BENCHMARK_CORPUS_MANIFEST]-[COMPLETE]: `BenchCorpus` at `tests/csharp/_benchmarks/Corpus.cs` realizes the declared corpus slug roster — eleven slugs transcribed verbatim from `BimBenchClaims`, the corpus's sole slug declarer (kernel and Fabrication claims are algorithmic `Corpus: None`; Materials binds corpus through injected content keys, Compute and Persistence carry measured `Option<UInt128>`) — with `Admit` a bijective fold over `Manifests.Corpus` discovery wired into the `gate` verb: a declared slug with no committed fixture and an undeclared fixture both print typed `CORPUS` rows and breach the exit, so declarations gate on real fixtures and the declaration direction stays folder-owned. Fixture bytes are owed under `tests/csharp/_benchmarks/corpus/`; until committed the gate reports every slug as a typed missing-fixture breach by design.
[BENCH_CASE_PRODUCERS]-[COMPLETE]: every `FabricationBenchClaims` row has its measuring case landed beside its lane owner — `NestBench` (`Nesting/nfp`), `ProbeBench` (`Verify/probing`), `SkeletonBench` (`Toolpath/skeleton`, a fully page-built literal chain-graph demand), `BrakeBench` (`Forming/brake`, pinned literal operating envelope), `ClearanceBench` (`Toolpath/guard`, both lanes over one probe census with the sequential cost as reference evidence) — each a workload admission plus the measured fold in folder types, receipts staying the bench edge's under the AppHost claim-field map per the Fabrication strata boundary (no AppHost type reaches the folder). `IcpProbeFit` re-cut on evidence: its lanes spelled `ProbeRoute.Measured`/`ProbeRoute.Reference` — a guard route union duplicating `ClearanceParallel`'s comparison and naming no probing member — and now spell `Probe.Inspect` both columns as the fourth no-regression row. The card's tests-seat literal was refuted on disk: a `BenchCase` registry row today is a phantom `Regression.RegistryParity` fails, since no Fabrication `[Benchmark]` method can compile before fence realization.
[L3_REFERENCE_PROSE_RECONCILE]-[COMPLETE]: the branch `[02]-[STRATA]` rows state the realized app-platform order and verify exactly against the live `ProjectReference` set — `Rasm.AppHost` holds `{Rasm}`, `Rasm.Persistence` `{Rasm, Rasm.Element}`, `Rasm.Compute` `{Rasm, Rasm.Element, Rasm.AppHost, Rasm.Persistence}`, `Rasm.AppUi` `{Rasm, Rasm.AppHost, Rasm.Compute, Rasm.Persistence}`. `Rasm.AppUi/ARCHITECTURE.md`'s lead carried the surviving half — a peers-by-contract clause its own csproj refutes — and now names the S4 consuming-leaf reference set.
[BATCH_SEAM_LEDGER]-[COMPLETE]: every producer→landing pair mirrors at both endpoint `ARCHITECTURE#SEAMS` registries — `Rasm.Element` and `Rasm.Materials` carry `[WIRE]: AnalyticsSchema` against `Rasm.Persistence` for the relational residence handoff, `Rasm.Compute` carries `[WIRE]: LakeGeneration` for the lake landing. Both open claims refuted on disk: the Compute counterpart had landed already, and it was MISLABELLED `AnalyticsSchema`, a contract no Compute fence composes. No kernel pair exists or may exist — `Rasm` sits at S0 and reaches no landing coordinate.
[CLAIM_FIELD_MAP]-[COMPLETE]: `#CLAIM_FIELD_MAP` carries rows `[08]` `BudgetBreach` and `[09]` `CaptureBreach` beside the seven standing families, each filling median and p95 from its one measured cost and its producing bound as `ReferenceEvidence.Median`; a single-sample law seats that projection so no adapter fabricates a distribution one judgment never produced.
[NPGSQL_ROOT_COMPOSITION]-[COMPLETE]: `SignalGovernance.StoreDriver` registers `AddNpgsql` + `AddEntityFrameworkCoreInstrumentation` tracing and `AddNpgsqlInstrumentation` metrics at the store-composing root (Rasm.AppHost `Observability/telemetry#SIGNAL_GOVERNANCE`); `NpgsqlDataSourceBuilder.Name` assigns per logical database in the Persistence `Store/provisioning` `Source` fence.
[PLUGIN_ALC_PROVIDERS]-[COMPLETE]: `PluginTelemetryHost.Open` realizes the per-ALC trace-and-metric capsule — `AddMetrics` `IMeterFactory`, explicit provider handles, idempotent unload-ordered `ForceFlush` then `Dispose`; exporter disk retry remains process-scoped (Rasm.AppHost `Observability/instruments#PROVIDER_LIFETIME`).
[KAFKA_CONSUMER_INSTRUMENTATION]-[COMPLETE]: `StoreWire<TKey,TValue>` registers `AddKafkaConsumerInstrumentation` on both providers at the signal root; the Persistence ingress owner landed at `Rasm.Persistence/.planning/Version/ingress.md` handing the `CdcIngress.Bind` instrumented builder.
[RHINO_TELEMETRY_ANCHOR]-[COMPLETE]: `Microsoft.Extensions.Telemetry.Abstractions` and `Microsoft.Extensions.Logging.Abstractions` anchored in the Rasm.Rhino manifest and README registry; the HostUtils cloud-log/exception tap card lives at the Rasm.Rhino folder tier.
