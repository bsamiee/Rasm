# [APPUI_IDEAS]

Forward concept pool for the product UI engine: each open idea is a higher-order folder concept grounded in the AEC/Rhino purpose and current rendering, reality-capture, and coordination research, and each drives one or more `TASKLOG.md` cards.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[DARK_IDENTITY_RAMP]-[COMPLETE]: the appearance seed became the generation at `libs/csharp/Rasm.AppUi/.planning/Theme/tokens.md` — `AppearanceSeed` expands through one pure `Resolve` fold into the whole role ladder under typed `TokenKey`s, `ThemeVariantRow` and `DensityRow` project that same generation rather than carrying parallel columns, the `SemiCorrespondence` and `GraphSkin` slot closures fold into one `ThemeEmission.Partition`, and the contrast and CVD candidate rosters DERIVE from the ladder so a new role reaches the access sweep with no roster edit.
[ELEVATION_MATERIAL_SYSTEM]-[COMPLETE]: depth and translucency became algebra at `tokens#TOKEN_CATALOG` — layered shadow stacks and material rows generate beside the paint ladder, every overlay tier (dialog, flyout, toast, palette, rail, card) binds one row, and the dock, graph, and chart chrome read those same tiers through the one emission; the dead-end depth tuple that reached no control is gone.
[VFX_PLANE]-[COMPLETE]: the effects plane landed as the new `Vfx/` sub-domain — `Vfx/material.md` the layer algebra with its sample-invalidation contract and per-draw filter rows, `Vfx/shader.md` the SkSL program roster with uniform frames and byte-ceiling recorded tiles, `Vfx/compose.md` the closed composition slot vocabulary with its keyframe mint, trigger maps, and render-thread tick — each carrying its router row and codemap node, with `Theme/motion` keeping what motion FEELS like and this plane owning where that timing EXECUTES.
[TYPOGRAPHIC_SYSTEM]-[COMPLETE]: type became a two-axis generation at `Theme/typography.md` — the role table generates size, leading, tracking, and feature intent with emphasis moving the weight rung alone, the capability-keyed face cabinet and the itemizing shaping rail landed whole, tracking is EM everywhere with pixels a single projection, and density and text-scale re-derive the estate through the one `TokenKey` mint so a bare font literal at a call site is the named defect.
[LOCALIZATION_COMPLETENESS]-[COMPLETE]: locale became a full projection axis at `Theme/locale.md` — `LocalePolicy`/`ResolvedLocale` carry script, collation, calendar, and pattern resolution, `locale#MIRRORING_LAW` `MirrorSubject` rules what mirrors and what never does, and `locale#MEASUREMENT_FORMAT` `MeasureRole` gives forms, HUD chips, tables, charts, and exports one quantity spelling — including the four analysis-probe roles whose SI-only unit truth is stated rather than guessed.
[CONTROL_VARIANT_ALGEBRA]-[COMPLETE]: the closed union reached the professional roster at `Shell/controls.md` — thirty-one `ControlIntent` cases over one `ControlFactory`, with variant, icon, and pending as COLUMNS on `IntentBinding` rather than name suffixes, the `[PACKAGE_ADMISSION]` roster closing every extended-control candidate as admitted, seated, or refused with its reason, and the TS projection crossing every closed vocabulary as a literal union.
[CHROME_RAIL_FAMILY]-[COMPLETE]: floating chrome became one slot family at `Shell/navigation.md` `[SHELL_CHROME]` — seven `ChromeSlot` rows each carrying their own mount admission, `ChromeContent` the per-slot payload union, and `AdaptiveLayout` selecting chrome programs by breakpoint tier, so the rail, mode toolbar, status footer, viewport HUD, context menus, and the diagnostics HUD compose one row grammar and a mode is a catalog route.
[COMMAND_PALETTE_SURFACE]-[COMPLETE]: the engine earned its face at `Shell/commands.md` — `commands#PALETTE_FEDERATION` streams provider slices carrying rows AND status on one emission, merging under one rank comparer, and `commands#PALETTE_SURFACE` renders the top-anchored overlay as a frame STACK (results, actions, arguments) seated on the canvas stack, with `commands#BINDING_EDITOR`'s `ShortcutEditor.CaptureIntent` making the palette and the shortcut editor one assignment path.
[OVERLAY_MODALITY_CHROME]-[COMPLETE]: modality became law at `Shell/dialogs.md` `[SESSION_ALGEBRA]` — two stack owners over a derived topology, `OverlayShape` rows carrying chrome, depth, material, and choreography per modality, and the confirm ladder grading friction by tier (inline confirm, undo-toast, confirm, typed-confirm), so palette, drawer, and peek each seat on a STATED stack with stated occupancy.
[NOTIFICATION_SYSTEM]-[COMPLETE]: the notification plane became one severity system at `dialogs#ACTIVITY_CENTER` — `ToastRow`/`BannerRow` under one severity ladder, `ToastGate.Admit` folding phase, degradation, quiet window, and piercing into one verdict, and the persistent activity inbox windowing through the one virtualization fabric under its own newest-first comparer so a missed toast is recoverable and a degraded state persists instead of expiring.
[WORKSPACE_FABRIC]-[COMPLETE]: the dock became a workspace system at `navigation#DOCK_LAYOUTS` — `RegionProgram` rows make the topology data, `WorkspaceRow` names the arrangements a mode carries, `SurfaceKey` mints per document instance so two floats of one route partition disjointly, tear-out rides `FloatDockable` with clamped float geometry, and restore resolves every dockable through the same admission navigation takes so a restored layout carries live view-models.
[SELF_SERVICE_SURFACES]-[COMPLETE]: the product manages itself at `Shell/screens.md` — `ScreenRoster` seats settings, shortcuts, history, first-run, landing, fault-report, run-queue, layer-stack, compare-grid, and compare-session as ordinary catalog rows, and `screens#SETTINGS_SURFACE` projects the registry off the schema engine with each persisted-policy owner supplying its own `Settings` registration (theme, locale, gesture sets) and every mutation routing back through that owner's swap capsule.
[FORM_SECTION_GRAMMAR]-[COMPLETE]: forms read as sectioned surfaces at `Editing/forms.md` — `FormSection` is the wizard-step shape that deleted the parallel step roster, `FormField` carries entry row, requiredness, fallback, help, measure, and commit posture, `FieldState` is the ranked five-row ink axis, `forms#FORM_CHROME` seats the mechanism's own label geometry, and `PendingForm` batches marked writes into one re-solve so a costly parameter stops re-solving per keystroke.
[CHART_DATA_GRAMMAR]-[COMPLETE]: the chart plane gained its grammar at `Charts/dashboards.md` — layered `ChartSpec` over dual axis lists with per-layer indices, the declared `TransformRow` chain between feed and series, `ThresholdList` the ordered step family, facet grids as one tile column, `LegendSpec` the one legend declaration with its two package arms and its drawn plane, and one `BoardContext` carrying cross-filter, zoom sync, and time scrub so no AEC or ops chart class is unrepresentable.
[DARK_CHART_GRAMMAR]-[COMPLETE]: the chart plane joined the token system — every paint on both chart owners resolves from a `ChartChrome` row on generated rungs (including the dashed comparison ghost), `Charts/custom.md` composes its whole style through `CustomVisualStyle.Of` over `PaintFamily`, `ChartChrome`, and `TypographyRole`, and `Charts/telemetry.md` and `Charts/basemap.md` read that same resolve, so the light-mode package defaults and the resolved-at-mount prose are gone and a stalled feed raises rather than rendering stale.
[MOTION_MICRO_INTERACTION]-[COMPLETE]: motion became interaction physics at `Theme/motion.md` — `MotionAxis` rows carry their own transition constructor, kind, and lane, `MotionPlan` rows carry choreography as DATA (dialog, drawer, flyout, toast, page, cascade, hover, press, indicator, disclosure, notice, skeleton), `LatencyTier` picks the feedback form from a table, and `motion#MOTION_HANDOFF` retargets an interrupted spring instead of snapping.
[CONTENT_SURFACE_DARKENING]-[COMPLETE]: content surfaces landed on the one rail at `Editing/inspector.md` and `Document/media.md` — `inspector#CODE_EDITING` inverts the grammar-theme direction so `EditorInk` rows project the resolve INTO the gui-color vocabulary through `TokenRawTheme`, the fourteen ranked editor rows are descriptor-driven, markdown materializes all eleven arms against real table and fence owners, and `Theme/assets.md` closes the icon roster on one ranked source rail.
[VIEWPORT_CHROME]-[COMPLETE]: the viewport earned its chrome at `Render/pipeline.md` — `ViewRegistry` is the one named-view owner over standard views, bookmarks, captures, and traversal recalled through the camera-track projection, `SectionDrag` manipulates the six-plane box on the overlay plane, `MeasureSession` takes source-addressed measurements that ARE viewpoint annotations, `DisplayPosture` folds colour-by, participation, and wireframe onto the one override vocabulary, and every readout rides `ViewChrome` rows on the settled chrome slot.
[ANALYSIS_PLANE]-[COMPLETE]: sealed studies became first-class layers as the new `Analysis/` sub-domain — `Analysis/layers.md` the unbounded provenance-stamped stack with its probe channel, its ONE adoption site the run queue reaches, and the four bake verbs; `Analysis/compare.md` the synced compare grid over option, analysis, and time; `Analysis/context.md` the one environmental coordinate every layer, climate diagram, and bound series reads — each with its router row, codemap node, screen seating, and telemetry tiles.
[TEMPORAL_SURFACES]-[COMPLETE]: time got its editing surfaces — `Editing/history.md` `TimelineSurface` makes undo scrubbable over the revert algebra, `Render/animation.md` `[TIMELINE_EDITOR]` lands lane board, snap vocabulary, keyframe edit union, and the ONE `TransportVerb` grammar, `Charts/dashboards#CHART_GRAMMAR` renders construction sequencing planner-grade, and `Document/media.md` CONSUMES that same transport so the media playhead and the animation playhead reconcile on one vocabulary.
[MEDIA_PRESENTATION]-[COMPLETE]: the media engine earned its faces at `Document/media.md` — `media#PLAYBACK_TRANSPORT` binds the settled `TransportVerb` grammar to the typed `MpvContext` with event-driven observation, `CaptionTrack` transcribes a source into a player-timed sidecar, `GallerySurface` composes filmstrip, lightbox, and zoom from existing owners, and `DiffSeats` mounts the compare session's structured diff, so media stops being mount-only.
[REVIEW_PRESENTATION]-[COMPLETE]: collaboration became visible across `Collab/` — `sync#PRESENCE_CHROME` renders canvas presence with the follow lease and its release verb, `Collab/issues.md` projects the openBIM board with triage, mentions, and the redline tool family, `Collab/tour.md` gives presenter and audience chrome over a camera-track projection, `Collab/session.md` seats the roster, notices, and connection strip, and the compare session landed as its own interactive screen row.
[DOCUMENT_SEARCH]-[COMPLETE]: landed as `libs/csharp/Rasm.AppUi/.planning/Document/search.md` — the closed query shape, source-coverage rows over the landed owners (notebook cells, media prose, issue comments, evidence payloads), ranked source-attributed results through the `VirtualWindow` fabric, highlight navigation on the `SearchStrategyFactory.Create` mint, and `SearchFault` on `AppUiFaultBand.Search` (6430) — WITH the Persistence counterpart landed the same pass: `Query/retrieval.md` `[06]-[DOCUMENT_CORPUS]` bm25 index lane and the `DocumentQuery`/`DocumentHit` wire named at both ends.
[SUPPORT_BUNDLE]-[COMPLETE]: blocker fell — AppHost `SupportManifest.Entry` gained the post-redaction, post-cap `ContentKey` (`Option<string>` hex, `None` on every no-byte arm including the bundle-cap drop) across every construction, and the AppUi golden leg pins content identity as the third fact of the `ProofLaw.BundleShape` pair; the pre-redaction `BundleMember.ContentKey` and the archive key agree exactly where nothing was masked or truncated, an inequality naming redaction or a cap rather than corruption.
[RAY_CONE_CURVATURE]-[COMPLETE]: the producer counterpart landed (`Rasm.Compute` `ResidencyMeshlet.Curvature`, measured normal-variation bound on `ResidencyPayload.Clusters`) and the whole consumer chain realized — `ResidencyMeshletView` projects the column, `SurfaceAttributes.Curvature` fills at hit resolve, `RayCone.Advanced(distance, curvature)` widens spread by `2 * curvature * distance` beside the distance term, and `MeshletWire`/`MeshletWireOf` carry the column under the bumped `Schema = 3`; a planar hit reads a measured 0 and widens by distance alone.
[HOST_SPRING_PARITY]-[COMPLETE]: the parity-map table landed in `libs/csharp/Rasm.AppUi/.planning/Theme/motion.md` `[02]-[MOTION_AXIS]` — `SpringValue` members (`Response`, `DampingFraction`, `Shape`) beside the host surface classes mirroring them, a host preset and a shell token evaluating the SAME kernel `SpringShape.OfResponse` closed form; the host preset table itself seats at the composition root per the branch spring-parity ruling, AppUi the parity source with zero surface change.
[COLLAB_SESSION_GOVERNANCE]-[COMPLETE]: landed as `libs/csharp/Rasm.AppUi/.planning/Collab/session.md` — `SessionRole`/`SessionCapability` `[SmartEnum<string>]` vocabulary with delegate-column grants, `MembershipState` total lifecycle rows, `MembershipOp` landing durably as `EditIntent.Membership`, and `SessionGate.Required`/`Admit` the total admission fold the `sync.md` merge authority composes ahead of `LedgerAppend`; rejections derive through the new `AppUiFaultBand.Session` (6530) registry row and presence stays a projection over the landed awareness channel.
[COLLAB_WIRE_CONTEXT]-[COMPLETE]: blocker fell — AppHost landed `Wire/topics` `Topic.Collab`/`Topic.Presence` and the generic `TraceContext` `Inject`/`Extract`/`Continue` spine whose boundary law seats a domain carrier's concrete bodies beside the consuming egress leg; `Collab/sync.md` `CollabCarrier` now binds the frame's getter/setter pair with `TenantAdoption.Adopted`, and the named `[COLLAB_DELTA_FEED]` cluster never existed — the topic rows are the reciprocal.
[MATH_TYPESET_OWNER]-[COMPLETE]: landed as `MathStyle`/`MathBox`/`MathTypeset`/`MathRun`/`MathInlineVisual` on `libs/csharp/Rasm.AppUi/.planning/Document/media.md` `[02]-[MARKDOWN_INLINES]` — one painter serves the measure and the draw, the block and inline arms both materialize through `MathStyle`, math composites into the leased canvas through the settled `ICustomDrawOperation` vehicle, and the typography projection stays typeset-free as ruled.
[PROFILE_FLAME_JOIN]-[COMPLETE]: the AppHost `ProfileCapture.Bind` producer landed at `Observability/benchmarks.md` `[04]-[PROFILE_CORRELATION]`, so `FlameNode.Of` now folds published `ProfileSample` values; the row widened with `ThreadId`, `Kind`, and the `ProfileFrameForm` posture, and `FromSamples` lane-groups by thread with the posture on the lane name.
[TELEMETRY_BOARD_PAGE]-[COMPLETE]: store-profile tiles read the landed `SeriesKind.Telemetry` projection through `StoreProfileTrack`'s `(domain, slot, measure)` facets and one injected read arrow, so every board tile binds a producer.
[MULTI_MEASUREMENT_LEVELS]-[COMPLETE]: keyed level families ride the kernel `InstrumentKind.Levels` `InstrumentSpec.Create` declaration over `LevelCells.Reader`, declared beside each producer and swapped by the `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[03]-[TELEMETRY_SPINE]` fan arms.
[COLLAB_FORENSICS]-[COMPLETE]: landed as `LiveWire.TapPreCommit`/`ExportJson` with the devloop `CollabPreCommit`/`CollabJson` verbs sealing `PreCommitFact` evidence in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/devloop.md`.
[FRAME_BENCH_LANE]-[COMPLETE]: landed as `BenchLane`/`ProofEngine.Bench` with `ProofLaw.FrameBench`/`Divergence` gating through the AppHost `BenchmarkGate` in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/proof.md`.
[TENANT_USAGE_FOLD]-[COMPLETE]: landed as `TenantUsage`/`TenantUsageFold.Fold` beside `EvidenceJoin` with the `TenantUsageWire` crossing in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[04]-[CORRELATION_JOIN]`.
[FRAME_SLO_FEED]-[COMPLETE]: landed as the `ViewportObjectives` latency indicators in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[03]-[TELEMETRY_SPINE]`, consumed by `SloTiles`/`BurnFeed` over the kernel burn table.
