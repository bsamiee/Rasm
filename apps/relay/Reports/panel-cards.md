# Panel cards and gauges
Agent task: refine the Relay menu bar panel cards and usage gauges (`RelayMenu.swift`, `UsageGauge.swift`), 2026-09-13

Changes, each with its basis:
1. Gauges: label leading and `percentage · reset` trailing in `.subheadline` monospaced digits on one line, a 6pt `Capsule` track (`.quaternary`) with a leading fill below. Basis: HIG Progress indicators ("a track that fills from the leading side to the trailing side"); 6pt is the drawn track of the native `NSProgressIndicator` `.bar` at `.small` (measured headless: small draws 6pt in a 12pt frame, regular and SwiftUI `.linear` draw 8pt in a 20pt frame). A custom bar stays because `.tint` does not color the linear `ProgressView` on macOS (Apple forum thread 802822, SO 79091478). Mask, luminance, contrast stroke, and reduce-transparency branches are gone; stale = gray fill, secondary label, tertiary reading.
2. Rows and buttons: email row, provider mark, and the gear `Menu` use `.buttonStyle(.accessoryBar)`, the system style with hover and press highlight that Apple documents as applying to `Menu` (AccessoryBarButtonStyle doc; Xcode's scheme selector is this style). Nothing else is a button; gauges keep `.help` and `.focusable()`. `.onHover` machinery not needed.
3. Animation: `.animation(.default, value: quota)` on each gauge with `.contentTransition(.numericText())` on the reading, `value: isExpanded` on the gauge column, `value: presentation.isSelected` on the checkmark, `value: running` on the session dot. No global animation.
4. Layout: `.offset` on the dot removed (it sits at `.bottomTrailing` of the mark's frame, the empty corner of both round marks; the Dock marks a running app with a dot the same way). Column-width constants, `.frame(width: 16)`, and the `iconSize + iconSpacing` padding hack are gone: the issue line lives in the gauge column, so it aligns with the gauges by structure. Spacing is 4/8/16/24, semantic fonts only, panel stays 384. The provider mark and the gauge column share an `HStack(alignment: .top)`; both the mark and the email row carry the same accessory-bar inset, so the email text and mark share a leading edge. The single retained size is the 20pt artwork frame.
5. Empty and loading states unchanged: one `Add Account…` push button, one small `ProgressView`.
6. Lines: `RelayMenu.swift` 279 -> 253, `UsageGauge.swift` 194 -> 125 (imports `SwiftUI` only). `usagePercentage`, `sessionCountdown`, the reset label cases ("<1%", "Ready", "Awaiting update", "Unavailable"), accessibility element, value, and hint are unchanged.
7. Performance: the card-level `TimelineView` is gone and `now` leaves every signature. Each `UsageGauge` and the `ProviderSessionButton` own a `TimelineView(.periodic(by: 60))`, so the minute tick re-evaluates only the reading text, the dot, and their accessibility strings; the email row, selection, reset line, issue line, and card `VStack` are outside any timeline. The whole gauge sits inside its timeline because its accessibility value and hint read the remaining time.

Store: nothing new needed. `identity.plan` exists but the panel does not show it (upstream shows "Claude · Pro"); a subtitle would be a design decision, not a store gap.

Exit codes: `swift-format format` 0, `swift-format lint --strict` 0, `xcodebuild` BUILD SUCCEEDED with zero compiler warnings (a full build prints one `appintentsmetadataprocessor` notice unrelated to these files). This report file was written at the coordinator's request; the app was not launched.
