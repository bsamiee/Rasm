# [DOSSIER] — design-cs-geometry — lane: numerics-spatial

READ-ONLY survey. Scope: ROBUST-CORE `Numerics/{predicates,faults}` + `Spatial/{index,naming,reconciliation}` deep-read; landed siblings (`Numerics/{atoms,matrix,integrate,spectral,calculus}`, `Spatial/{support,cloud,neighbors,transport,fields}`) + `README.md`/`ARCHITECTURE.md` as standing-law context. Every anchor re-verified on disk. Stance HOSTILE. No edits outside this folder.

Bottom line: the register is ACCURATE for this lane — every assigned E-row HOLDS on disk (zero REFUTED). `predicates.md` is genuinely award-grade and mostly discharges its own charter (two V14 capability gaps, not defects). `faults.md` is sound-but-thin (stringly majority + the ARCH:112 seam it never states). `index.md` is a strong kernel wearing a mis-placed `PointCloud` arm that forces three exception escapes, a silent empty that contradicts its own prose, an inward-rounded-bounds false-negative, a returns-Compute's-type strata breach, and stale self-inconsistent counts — one entangled structural defect. `naming.md`+`reconciliation.md` carry a SEVERE, airtight vertex mis-key bug that corrupts every downstream signature, plus namespace-lessness and an anchor-drifted Persistence seam.

---

## [REGISTER_VERDICTS] (assigned rows: E3·E4·E5·E6·E9·E10·E11·E12·E15·E16, my-page portions)

| Row | Status | On-disk verification |
|---|---|---|
| E3 | HOLD | `index.md:27` `using Rasm.Compute.Solver;` — exact. (The deleted-`Analysis/Intersect.cs` citations + phantom-namespace sites are curve/intersect/pack/solver, other lanes.) |
| E4 | HOLD | `naming.md:113` passes `entity.IncidentVertices[0]`; `reconciliation.md:55-56` builds `star[v]` = self∪neighbours sorted, `:62` sets `IncidentVertices = star[v].ToArray()`. Mechanism anchor to ADD: `naming.md:100-104` `With` keys `VertexNames` by that arg. Bug real and severe (below). |
| E5 | HOLD | `predicates.md`, `naming.md`, `reconciliation.md` declare NO `namespace` (grep-confirmed). `index.md:34` declares `Rasm.Geometry.Spatial`; `faults.md:28` declares bare `Rasm.Geometry`. Placeholder row `ARCHITECTURE.md:110`. |
| E6 | HOLD | `index.md:27` upward using + `:572` `public AccelerationStructure ToAcceleration()` returns Compute's union type directly (`:576-579`); vs landed wire row `ARCHITECTURE.md:86`. |
| E9 | HOLD | GWN ×2: `index.md:383-444` Barnes-Hut (`Winding`/`NodeMoment`/`SolidAngle`) vs landed `reconstruct.md` exact per-triangle. BEYOND-REGISTER: kd-tree ×2 (below). |
| E10 | HOLD | Throws `index.md:130,574,579` (exact). Silent empty `:358` `new QueryResult.Hits(Seq<int>())` vs prose `:350-351` "never a silent empty". Bounds narrowing `:79` `(float)value` round-to-nearest → false-negative prune at `:453,474,518` (all exact). |
| E11 | HOLD | `index.md:14` "SpatialIndex cases Bvh·LinearOctree (2)" + "SpatialKind rows bvh·octree·agglomerative (3)"; fence has 3 `SpatialIndex` cases (`:109,110,118`) + 4 `SpatialKind` (`:41-44`). `:707` CASES col says "3" but KIND text names only "(Bvh/LinearOctree)". Self-inconsistent. |
| E12 | HOLD | Index wire: `ToAcceleration` (`index.md:572`) returns Compute's type, does NOT compose the declared node-array wire (`ARCHITECTURE.md:86`). Reconciliation triple anchor: `reconciliation.md:3` `Persistence/version-control#STRUCTURAL_DIFF` vs `ARCHITECTURE.md:79` `Rasm.Persistence/Query` — two spellings on disk today. |
| E15 | HOLD | `faults.md:20` typed-payload law. Fence `:37-58`: 9 cases carry exactly `(string Detail)`; `EncodingFault(:58)` carries `(string Channel, string Detail)` → 10 stringly of 17. Brief's "10 of 17" is right if EncodingFault counts; precise "only `string Detail`" = 9. Substance HOLDS. |
| E16 | HOLD | ARCH:112 (`ARCHITECTURE.md:112`) asserts "`Numerics/Faults.cs`…state the seam"; `faults.md` states NO two-family seam (grep-confirmed absent). README:86 names the PeterO directed-rounding interval filter; `predicates.md` composes NO `ERounding.Floor`/`Ceiling` filter (uses `EContext.Unlimited` exact). README:42/65/99 rows present on disk. CLASH_GOLDEN (`index.md:696`) byte stream self-consistent/derivable. CANONICAL_BYTE_IDENTITY digest (`reconciliation.md:147`) unverifiable statically — harness reproof stands. |

Nothing DRIFT, nothing REFUTED. Conversion-era line drift on my pages was zero-to-negligible; anchors landed where the register pointed.

---

## [PER_PAGE]

### Numerics/predicates.md (574 LOC) — grade now 9, target 9.5

VERDICT: crown jewel, preserve-and-harden. Four-tier ladder (`double` filter → `ddouble` 106-bit `Refine` → `Expansion` sign-exact → `Fraction`/`EFloat`/`ERational` four-way oracle) is transcription-complete and genuinely award-grade. The implicit-point family (`OrientLPI`/`OrientTPI`/`InCircleLPI`/`InSphereTPI`, `:228-255`) is exact-only-by-construction with the `lambda`-denominator flip read exactly (`RationalOracle.InCircum` `:347-350`) — the `[V4]` implicit-point floor the arrangement/delaunay/repair collapse rides ALREADY EXISTS here. `Expansion` (`:402-503`) is one owner, FMA `TwoProduct` (`:419-423`), Shewchuk fast-expansion-sum (`:429-445`). Namespace-less (E5).

Defects / capability gaps (all V14 index-doc obligations, NOT correctness bugs):
- `[GAP:namespace]` No `namespace` declaration in either fence. Charter: declare the ratified TIER-2 concept namespace (`Rasm.Geometry.Numerics` per `[V1]`); `Point3d`/`Sign` seam unchanged.
- `[GAP:interval-filter]` `predicates.md` RESEARCH `:571` names `EFloat`/`EContext.Unlimited` EXACT adjudication only; README:86 + `api-peteronumbers.md:15-16` name the `ERounding.Floor`/`Ceiling` DIRECTED-ROUNDING interval sign-filter tier — absent from the fence. Charter: add the interval-bracket filter as a named tier between `ddouble.Refine` and the `Expansion` exact branch (a cheap directed-rounding straddle bracket), OR the DECISION re-scopes README:86 per `[INDEX_DOC_OBLIGATIONS]`.
- `[GAP:strict-IEEE/RID]` No strict-IEEE-754/RID invariant on `NumericsPolicy` (`:382-399`). `TwoProduct`/`TwoSum` are only bit-safe under strict IEEE (x87 extended precision defeats Shewchuk EFTs). Charter: state the strict-IEEE/RID invariant on `NumericsPolicy` as the precondition making the EFTs sound.
- `[GAP:FMA-free-row]` `Expansion.TwoProduct` (`:419-423`) calls `Math.FusedMultiplyAdd` unconditionally; the Dekker-split fallback is RESEARCH-named only (`:378`, `:571`) with `NumericsPolicy.Splitter` (`:385`) seated but unused. Charter: fence the FMA-free `TwoProduct` fallback as one code row gated on the RID FMA-capability, per `[GENERATOR_LAW]` (a row, not a second product type).
- `[EXPORT:consumer-boundary]` `Predicate.Orient2D`/`Orient3D`/`InCircle`/`InSphere` are already `public static` (`:58,69,87,104`) — the `[V14]` package-boundary-consumable `Orient3D`/`InSphere` export for the Compute CDTet discretization consumer (`RASM-CS-COMPUTE-BRIEF [V7]`) is SATISFIED by shape; the DECISION need only declare the cross-package seam row (`ARCHITECTURE.md:89` already carries `Predicates.cs ← Fabrication/Posting` — Compute's edge is unrecorded). Charter: add the `Predicates.cs ⇄/← Rasm.Compute` seam row.

`api`-member discipline: clean. `ddouble.AdjustScale`/`Sign`/`Abs`/`Epsilon`, `Fraction.Sign`/`(Fraction)double`, `EFloat.FromDouble`/`EContext.Unlimited.WithBlankFlags`/`FlagInexact`, `ERational.FromDouble`, `Math.FusedMultiplyAdd` — all real (api-doubledouble/bigrational/peteronumbers, README:86). No phantom.

### Numerics/faults.md (110 LOC) — grade now 7.5, target 9.5

VERDICT: single-union band shape is sound (the `[VERDICT]` "what IS sound" list); the payload discipline is half-built and the seam statement the index doc credits it with is missing.

Defects:
- `[E15:stringly]` `faults.md:20` legislates "each case is typed to its failure with its real payload"; 10 of 17 cases (`:37,38,43,44,49,52,53,55,56,58`) carry bare/two-`string` payloads (`DegenerateInput`, `IndexMismatch`, `UnrepairableMesh`, `NativeAssetMissing`, `DegenerateOffset`, `DegenerateArrangement`, `IntersectionFault`, `ParameterizationFault`, `ProjectionFault`, `EncodingFault`) against 7 richly-typed proofs (`NameCollision`/`HashMismatch`/`OverConstrained`/`SingularSystem`/`SkeletonStalled`/`FitFault`/`DecimationFault`). Charter: re-type the 10 to their real payloads (e.g. `DegenerateArrangement(int cellCount, string manifoldWitness)`, `IntersectionFault(PrimitiveKind a, PrimitiveKind b)`), the page's own 5 rich cases the template.
- `[E16/ARCH:112:seam-absent]` `ARCHITECTURE.md:112` asserts "`Numerics/Faults.cs` and `Domain/Rails.cs` each state the [two-family] seam"; `faults.md` states NOTHING about the `Rasm.Domain` `Expected`/`Fault` substrate family coexisting — the whole page reads as if `GeometryFault` is the only fault union. Charter: add the two-family seam statement (`GeometryFault` band-2400 robust-core faults ⟂ `Rasm.Domain.Fault` kernel-substrate faults, neither absorbs the other), mirroring `rails.md`'s side.
- `[V1/V12:cluster-taxonomy]` `faults.md:3,14` names 12 sibling clusters on a 4-wide stride (2400-2447), headroom 2448-2449 (`:19`). The `topology` cluster (2404-2407: `NameCollision` 2404, `HashMismatch` 2405) must RENAME to `naming` with the `[V1]` phantom-`Rasm.Geometry.Topology` kill. New planes (`[V2]` subdivision/develop/panelize, `[V10]` slicing, `[V11]` remesh) need homes: concern-adjacent shares (parameterization-family widened; slicing under intersection-family) per `[V12]`, the last stride 2448-2449 spent deliberately. Charter: the cluster taxonomy = the ratified `[V1]` namespace partition, 1:1 concept-namespace↔sub-band.
- `[GAP:namespace-tier]` `faults.md:28` declares bare `Rasm.Geometry` (cross-cutting fault owner) — defensible, but `[V1]` must rule whether the fault owner sits at the root or a `Rasm.Geometry.Faults` concept namespace; the 13-namespaces-vs-12-clusters census (`Tessellation` owns no cluster, `Parametric` borrows `ParameterizationFault`) is disposed here.

### Spatial/index.md (710 LOC) — grade now 7, target 9.5

VERDICT: strong first-principles kernel (SAH-BVH + Morton octree + agglomerative PLOC + GWN + degradation-refit, all pure-managed transcription-complete) sabotaged by ONE entangled structural defect: the `PointCloud` arm baked into the `SpatialIndex` union. It duplicates `neighbors.md`'s landed kd-tree, forces three `throw`s + a silent empty, and desyncs every case count. `[V8]` (lift `PointCloud` onto `neighbors.md`) resolves E9/E10/E11 in one motion.

Defects:
- `[V8/E9:kd-tree-dup]` `index.md:118` `PointCloud(KDTree<double,double,int> Tree, Point3d[] Points)` + `:154` `KDTree.Create(points, payload, DistanceMetrics.EuclideanDistance)` is BYTE-FOR-BYTE the landed `neighbors.md:104` `StaticCase(KDTree<double,double,int> Tree, Point3d[] Points)` + `:131` `KDTree.Create(coordinates, payloads, DistanceMetrics.EuclideanDistance)`. Same Supercluster lane owned twice. Compounded: `index.md:114,372` claims the leaf "feeds `Processing/fit` MLESAC + ICP" — the EXACT claim README:93 assigns to `neighbors.md`. Double owner on type AND consumer. Charter: DELETE `SpatialIndex.PointCloud` (case `:118`, `SpatialKind.PointCloud` `:44`, `BuildPointCloud` `:151-155`, `KdNearest`/`KdRange` `:372-381`, the PointCloud guards `:130,354-359,542,558,573`); `index` keeps primitive broad-phase (BVH/octree/agglomerative); point k-NN/radius routes `neighbors.md`.
- `[E10:throws]` Three exception escapes, ALL symptoms of `PointCloud`-in-union: `:130` `Store` accessor `throw new InvalidOperationException("point-cloud:no-node-store")`; `:574` + `:579` `ToAcceleration` `throw ("acceleration:point-cloud-not-a-clash-structure")`. `[SEAM_AND_RAIL_LAW]` forbids rail exception escapes. Charter: with `PointCloud` lifted, `Store`/`ToAcceleration` are total over `{Bvh, LinearOctree}` — zero throws.
- `[E10:silent-empty]` `:358` `_ => new QueryResult.Hits(Seq<int>())` returns a silent empty for Ray/Overlap/Winding over a `PointCloud`, DIRECTLY contradicting `:350-351` "a Ray/Overlap/Winding over a PointCloud is a kind-mismatch, never a silent empty". Charter: dies with the `PointCloud` lift; a genuine kind-mismatch routes `Fin` fail with a typed `GeometryFault`, never `Query`'s pure rail.
- `[E10:bounds-narrowing]` `:79` `static float Single(double value) => (float)value;` rounds to NEAREST; `NodeStore.Write` (`:73-77`) stores both `BoundsMin` and `BoundsMax` through it, so a min can round UP and a max round DOWN → the float node bound is TIGHTER than the true double bound → false-negative pruning at `:453` (range `Intersects`), `:474` (ray `Slab`), `:518` (overlap `Intersects`), and the `NodeLinkProjection` wire (`:590-591` `(float)` casts). Charter: min rounds toward −∞ (`BitDecrement`/`MathF.BitDecrement` after cast, or round-down), max toward +∞ — the descent bound is a conservative OUTER hull, never an inner one. Same fix applies to `:590-591`.
- `[E6/E12:strata-wire]` `:572` `public AccelerationStructure ToAcceleration()` returns Compute's `AccelerationStructure` union directly (`:576-578` `AccelerationStructure.Bvh`/`.Octree`), importing `Rasm.Compute.Solver` (`:27`). Violates `libs/.planning/architecture.md:35`; the landed ledger `ARCHITECTURE.md:86` already declares the edge as kernel-owned "BVH/octree node arrays". Charter: `ToAcceleration` returns `Fin<(float[] Bounds, long[] Nodes)>` (the `NodeLinkProjection` `:582-601` output IS the wire); DELETE `using Rasm.Compute.Solver`; Compute decodes per its own `:121` clash-decode row.
- `[E11:stale-counts]` `:14` says SpatialIndex "(2)" and SpatialKind "(3)"; `:707` CASES col says "3" while its KIND text names only "(Bvh/LinearOctree)"; `:9` `[01]` index says "Bvh/LinearOctree union". Fence has 3 union cases + 4 kinds. Charter: after the `[V8]` lift, counts resync to 2 union cases (`Bvh`/`LinearOctree`) + 3 kinds (`bvh`/`octree`/`agglomerative`) everywhere.
- `[V8:GWN-single-owner]` `:383-444` Barnes-Hut tree-code GWN vs landed `reconstruct.md` exact per-triangle winding + `fields.md` `SignedDistanceFromMesh`. `[V8]` rules index owns the BVH-accelerated winding query; `reconstruct`/`fields` compose it through a declared seam — that IS the `[TELOS]` one-distance-field lane (`fields.SignedDistanceFromMesh → reconstruct GWN → index BVH`). Charter: keep `Winding` here as the accelerated owner; add the `index → reconstruct/fields` GWN seam row; `reconstruct`'s own per-triangle method composes, never re-owns.

Favorable: `[CLASH_GOLDEN]` (`:696`) is arithmetically self-consistent on disk — `Nodes[0]=(1<<21)|2=2097154` → LE `02 00 20 00 …`, `Nodes[1]=-5` → `fb ff…`, `Nodes[2]=-8388613` → `fb ff 7f ff…`, `13.5f=0x41580000` → `00 00 58 41`; the 160-byte stream derives. Harness byte-equality reproof is realization-phase, not a design defect. The three build kernels + four query bodies + refit are transcription-complete and pure-managed — preserve verbatim under the `PointCloud` excision.

### Spatial/naming.md (161 LOC) — grade now 6.5 (joint), target 9.5

VERDICT: one clean naming algebra (`TopoName` over `EntityKind`, no `Vertex/Edge/FaceName` triple) carrying a SEVERE, airtight vertex mis-key that corrupts every signature. Namespace-less.

Defects:
- `[E4:vertex-mis-key]` (severe, airtight) `naming.md:113` `state.Table.With(bound.Entry, entity.IncidentVertices[0])`. For a Vertex entity, `reconciliation.md:62` sets `IncidentVertices = star[v].ToArray()`, and `star[v]` (`reconciliation.md:55-56`) is the SortedSet `{v} ∪ neighbours(v)`. So `IncidentVertices[0] = min({v}∪neighbours)` = the star MINIMUM, not `v`. `With` (`naming.md:100-104`) does `VertexNames.AddOrUpdate(vertexIndex, entry.Name)` for Vertex kind → vertex `v`'s name is stored under key `min(star(v))`, and any two vertices sharing a star-minimum COLLIDE (last-writer-wins via `AddOrUpdate`). `ResolveBoundary` (`naming.md:97-98`) then reads `VertexNames.Find(edge.IncidentVertices)` (an edge's `{Min,Max}`, `reconciliation.md:63`) → the wrong names → wrong `TopoSignature` (`naming.md:119`) → Survive/Migrate/Born misclassifies every rebuild. Charter (`[V8]`): `RebuiltEntity` (`naming.md:84`) gains an explicit `int Self` field; `reconciliation.md:62` sets it to `v`; `With` keys `VertexNames` by `Self`, never `IncidentVertices[0]`; `Track` (`:113`) passes `entity.Self`.
- `[V8:Subsumes-direction]` `TopoSignature.Subsumes` (`naming.md:62-66`) tests `prior ⊂ rebuilt` (prior boundary a PROPER SUBSET of the rebuilt, `:66` `priorBoundary.Length < rebuiltBoundary.Length`), used in `MigrateOrBirth` (`:135`) as `Subsumes(prev.Boundary=parent, rebuiltBoundary=child)` — requiring the PARENT boundary be a proper subset of the CHILD. For a face SPLIT the child is SMALLER (parent boundary is the superset), so the test detects a MERGE while `Migrated` is documented (`:16`) as the split case. Direction inverted for the documented intent. Charter: fix the subset direction for splits (child ⊂ parent), or re-found the migration predicate on shared-name overlap fraction (the vertex-set-subset model is fragile — an edge-split child shares only 2 of 3 parent-cycle names).
- `[V8:Survive×Migrate-residual]` `naming.md:161` names the open injectivity residual. The `!claimed.Contains(prev.Name)` guard (`:135`) means once a survivor claims parent P, no sibling child can adopt P as `Parent` → a split where one child preserves the parent signature DENIES its siblings their lineage provenance (they fall to `Born`, `:142`). Not a crash (fresh names carry `born.Value`, `:74-75`, so cross-generation injectivity holds by construction), but a lineage-loss semantic. Charter: resolve the Survive×Migrate cross-case in the property harness AND decide whether a claimed survivor should still seed sibling `Parent` provenance.
- `[GAP:namespace]` No `namespace` (E5). Charter: declare the ratified naming concept namespace (`[V1]` rules `Rasm.Geometry.Naming`, the `topology`→`naming` fault cluster rename riding it).

Landed composition verified real: `Rasm.Domain.ContentHash.Of` (`:59,77`) → `identity.md:31` `public static UInt128 Of(ReadOnlySpan<byte>)`. No phantom.

### Spatial/reconciliation.md (147 LOC) — grade now 6.5 (joint), target 9.5

VERDICT: one clean naming↔hash bridge with the frozen canonical byte layout (the corpus's sole host-derived REAL fixture). `Encode` ALREADY composes `ContentHash.Of` (`[V8]` sub-item satisfied). Namespace-less; the vertex mis-key originates in its `BuildEntities`.

Defects:
- `[E4:star-construction]` `reconciliation.md:55-56` `star[v]` includes `v` itself (`star[min].Add(min); star[max].Add(max)`) and `:62` emits it as `IncidentVertices` — the upstream half of the naming mis-key. Charter: add `Self=v` to the `RebuiltEntity` emission (`:62`), keeping `star` for the histogram only; the naming page reads `Self` for `VertexNames` keying.
- `[E5/compile:unqualified-hash]` `NamingHashOps.Encode`/`Reconcile` (`:101,109`) call `ContentHash.Of` UNqualified with no `namespace` and no `using Rasm.Domain` — will not resolve as written (naming.md uses the qualified `Rasm.Domain.ContentHash.Of`). Charter: declare the concept namespace + `using Rasm.Domain` (or qualify), same motion as E5.
- `[E12:Persistence-anchor-drift]` `reconciliation.md:1,3,16` cite `Persistence/version-control#STRUCTURAL_DIFF`; the fixture table `:129` cites `Rasm.Persistence/Version/commits#CRDT_ALGEBRA`; `ARCHITECTURE.md:79` cites `Rasm.Persistence/Query`; the brief adds `Rasm.Persistence/ARCHITECTURE.md:55`. Three-to-four spellings for one seam. Charter (`[V8]`/`[V14]`): unify to ONE anchor per seam, one row, matching the counterpart's own spelling.
- `[GAP:namespace]` No `namespace` (E5). Same rule as naming.
- `[E16/fixture-reproof]` `CANONICAL_BYTE_IDENTITY` digest `0x9462A71A5DD13DCFA3B1D6D225FCBE70` (`:126,147`) is asserted REAL/host-validated and "never re-asserted from a fresh host probe" (`:147`). The 52-byte stream is structurally self-consistent on disk (VertexCount 4 + EdgeCount 4 + 3 edges×8 + FaceCount 4 + 1 cycle(4+3×4) = 52), but the XxHash128 digest is UNVERIFIABLE statically. `[SEAM_AND_RAIL_LAW]` mandates harness reproof before any page cites it REAL. Charter: keep as the frozen reference but flag the digest as harness-confirmed-once (`XxHash128.HashToUInt128` over the exact stream), not asserted.

Favorable: the `OfMesh` canonical-order encoder (`:31-52`), the single-owner frozen field layout (`:21`), and the `Encode`↔Persistence byte-identity contract are the settled spine to preserve. `MeshSpace.DuplicateNative` (`:32`) → `mesh.md:141` real.

---

## [CROSS_CUTTING]

1. `[STRUCTURAL:PointCloud-arm]` (highest-leverage) The single defect knot in this lane. `SpatialIndex.PointCloud` (`index.md:44,118,151-155,348-381,542,558,573-579`) is a landed-law DUPLICATE of `neighbors.md`'s `StaticCase` kd-tree, and its presence FORCES E10's three throws + silent empty and E11's count desync. `[V8]`'s lift resolves E9(kd-tree)+E10(throws+silent)+E11(counts) in ONE excision. The register treats these as separate rows; they are one structural cause. The DECISION's `index.md` skeleton must state the excision as a single motion, and the `neighbors.md` seam (point k-NN/radius/MLESAC/ICP) as the new home per README:93.

2. `[STRATA:one-wire-law]` `index.md` is the lane's only upward-strata breach: `using Rasm.Compute.Solver` (`:27`) + `ToAcceleration` returning Compute's type (`:572`). The landed ledger `ARCHITECTURE.md:86` ALREADY declares the correct shape (node arrays). Fence follows the ledger: `Fin<(float[],long[])>` over `NodeLinkProjection`. Zero other lane page imports upward (predicates/faults/naming/reconciliation are clean).

3. `[PARADIGM:dual-rails]` `[V5]`/`[SEAM_AND_RAIL_LAW]` "one paradigm" — my pages ride bare `Fin` with ad-hoc guards, NOT the landed `Op? key = null` threading (`rails.md`) or the `IValidityEvidence` validity fold. `index.md` `Build`/`Refit` (`:157,536`), `naming.md` `Track` (`:109`), `reconciliation.md` `Reconcile` (`:104`) all return `Fin<>` with no `Op` key and no validity registration. `predicates.md` is exempt (total pure, no rail — correct). Charter: the robust-core rebuilds adopt the `Op?` threading + admission vocabulary while `GeometryFault` stays the fault family (two-family seam). This is corpus-wide, seeded here.

4. `[SEAM:conformance]` `[V14]` ledger-conformance for this lane's declared rows: `ARCHITECTURE.md:86` index⇄Compute node-array wire — NOT composed (returns Compute's type); `ARCHITECTURE.md:79` reconciliation→Persistence content-key — composed by `Encode` but anchor-drifted; `ARCHITECTURE.md:89` `Predicates.cs ← Fabrication/Posting` — the surface is `public`, seam sound, but the Compute CDTet consumer edge (`RASM-CS-COMPUTE [V7]`) is unrecorded and must enter `[02]-[SEAMS]`. `ARCHITECTURE.md:84,85` `Index.cs ↔ Fabrication` broad-phase seams are one-directional prose today.

5. `[INDEX_DOC:two unmet-on-fence]` The two E16 rows squarely in this lane are BOTH real and BOTH unmet on their fence: ARCH:112 (faults two-family seam — absent) and README:86 (PeterO interval filter — predicates carries exact adjudication, not the directed-rounding filter). Neither index doc may keep its claim unless the fence lands the capability; the DECISION rules land-or-rescope per `[INDEX_DOC_OBLIGATIONS]`. README:42/65/99 rows are present and their fence-side falsification is the Meshing/Drawing lanes.

6. `[NAMESPACE:four→ratified]` `[V1]` must mint namespaces for the three lane-page namespace-less fences (`predicates`, `naming`, `reconciliation`) + rule the `faults` bare-root vs concept-namespace question. Recommended homes: `Rasm.Geometry.Numerics` (predicates), `Rasm.Geometry.Naming` (naming + reconciliation, per `[V1]`'s ruled default with the `topology`→`naming` cluster rename), `faults` at root `Rasm.Geometry` as the cross-cutting owner. `index.md` already correct at `Rasm.Geometry.Spatial`. The 13-namespace-vs-12-cluster census (`faults.md:14`) resolves: `naming` replaces `topology`; `Parametric`'s borrowed `ParameterizationFault` ends with `[V2]`/`[V12]`.

7. `[PHANTOM_SWEEP:clean]` All external members my pages compose verified real: `ContentHash.Of` (identity.md:31), `MeshSpace.DuplicateNative` (mesh.md:141), `TopologyReceipt` (mesh.md:196), Supercluster `KDTree.Create`/`NearestNeighbors`/`RadialSearch`/`DistanceMetrics.EuclideanDistance` (api-kdtree.md, mirrored in landed neighbors.md), `ddouble`/`Fraction`/`EFloat`/`ERational` predicate members (api-doubledouble/bigrational/peteronumbers). The ONLY non-real type is `AccelerationStructure` — real but UPWARD (Compute), the strata breach not a phantom. No fabricated member in this lane.
