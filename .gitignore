# --- [DEPENDENCIES] -----------------------------------------------------------

node_modules/
pnpm-debug.log*

# --- [BUILD_OUTPUTS] ----------------------------------------------------------

dist/
build/
out/
html/
public/build/
.next/
.nuxt/
.turbo/
.docusaurus/
.rollup.cache/
.genkit/

# --- [TYPESCRIPT_ARTIFACTS] ---------------------------------------------------

*.tsbuildinfo
**/dist/**/*.d.ts
**/dist/**/*.d.ts.map
*.js.map

# --- [PYTHON_ARTIFACTS] -------------------------------------------------------

__pycache__/
*.py[cod]
*$py.class
*.so
# Vendored RID-keyed natives are deliberate LFS assets, exempt from the build-output *.so rule.
!**/vendor/runtimes/**/*.so
.Python
*.egg
*.egg-info/
.eggs/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.venv/
venv/
ENV/
env/
.uv/
.pytest_cache/
.ruff_cache/
.ty_cache/
*.prof
*.lprof

# --- [TEST_COVERAGE] ----------------------------------------------------------

.coverage
coverage.xml
coverage/
htmlcov/
*.lcov
.approval_tests_temp/
.benchmarks/

# Verify
*.received.*
*.received/

# --- [CACHES] -----------------------------------------------------------------

.cache/
# .artifacts/build/{bin,obj}/<token>/ is the centralized .NET build-output root (Directory.Build.props); .artifacts/{assay,coverage,rhino} hold per-run scopes and agent scratch.
**/.artifacts/
/.remember/
.nx/*
.parcel-cache/
.pulumi/
.swc/
.vite/
.scannerwork/

# --- [.NET_ARTIFACTS] ---------------------------------------------------------

**/bin/
**/obj/
**/TestResults/
diagnostics.sarif
*.rsuser
*.suo
*.lscache

# Grasshopper2.dll extracts embedded resources (icons, color dictionaries,
# .3dm crystal models, .ghicon bitmaps, .ghdic) to the cwd of `dotnet build`
# whenever Grasshopper2 is referenced. Root-anchored so we don't shadow the
# real `apps/grasshopper/` tree.
/Grasshopper.*

# --- [ENVIRONMENT_VARIABLES] --------------------------------------------------

.env
.env.local
.env.*.local
.env.development
.env.production
.secrets
gha-creds-*.json

# --- [LOGS] -------------------------------------------------------------------

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- [IDE_AND_EDITOR] ---------------------------------------------------------

.vscode/
.idea/
.history/
*.swp
*.swo
*.tmp
*.bak
*.orig
*~

# --- [MACOS] ------------------------------------------------------------------

.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
Thumbs.db

# --- [TRANSIENT_TREES] ---------------------------------------------------------

.archive/
_archive/
scratch/
.scratch/
_tmp/
.tmp/

# --- [AI_TOOLING] -------------------------------------------------------------

.claude/agent-memory-local/
.claude/skills/**/.cache/
.claude/settings.local.json
.claude/worktrees/
.playwright-mcp/

# Audit raw API catalogs (regenerable via Mono.Cecil sweep; multi-MB)
.planning/audit/*.api-catalog.jsonl

# Gemini CLI auto-generated files (keep commands/ and extensions/ tracked)
.gemini/telemetry.log
.gemini/settings.json
.gemini/.DS_Store

# --- [SECRETS] ---------------------------------------------------------------

*.pem
*.key
*.secret
*.cert
*.crt
credentials.json
