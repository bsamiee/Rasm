# [PROJECTION_TASKLOG]

Open and closed work distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` when open; `[COMPLETE]` or `[DROPPED]` when closed — and three to four bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[QUEUED] Standing-query live-changefeed replay confirmation.
- Confirm `standing-query/window-fold#WINDOW_FOLD` tumbling/sliding/session bucketing and the `windowMerge` late-row signed-delta retraction-plus-restate against a running op-log replay with real out-of-order arrival, asserting the sliding signed-delta maintenance and the watermark `late` tally hold.
- Integrate `@effect/vitest` and `fast-check` for the out-of-order arrival arbitraries; the Z-set arithmetic grounds on the DBSP retraction law.
- Internal to `standing-query/`; consumes the decoded `OpLogEntryWire` changefeed projected at `csharp:Rasm.Persistence/sync/collaboration#TS_PROJECTION` through the wire fields only — the C# standing-query owner is a peer engine, not a consumer dependency, and no `IQueryable` shape crosses. From DATAFLOW_REFOUNDING.
- Validates the hand-rolled fold before the dataflow re-founding lands on top of it; the silent drop must never occur, only the tracked correction.

[QUEUED] Differential-dataflow re-founding of the event-time window engine.
- Re-found `standing-query/window-fold#WINDOW_FOLD` on a typed `@electric-sql/d2ts` operator graph — `map`/`filter`/`keyBy`/`reduce`/`join`/`groupBy`/`consolidate` over versioned Z-set `MultiSet` deltas — keeping `WindowKind`/`bucketSet` as the windowing reduce stage on top of the dataflow primitives and the watermark as the `d2ts` frontier antichain. The grouped progress and runtime-plus-health roll-ups ride `groupBy` with the catalogued `sum`/`avg`/`min`/`max`/`median`/`mode` aggregate functions, the one multi-aggregate primitive the engine composes rather than a hand-rolled reduce per aggregate.
- Integrate `@electric-sql/d2ts` for the versioned operator graph whose frontier antichain the event-time watermark maps onto; `@electric-sql/d2mini` stays the `live-query/reactive-query#REACTIVE_QUERY` versionless path and is never imported into the windowed engine. The `d2ts` catalog admission already landed (`@electric-sql/d2ts: 0.1.8` in the root `pnpm-workspace.yaml`), so the remaining precondition is the replay confirmation only.
- Internal to `standing-query/`; the engine owns windowing semantics on top of `d2ts` and never reinvents the dataflow itself, mapping `WindowKind`/`bucketSet` onto a reduce stage and the watermark onto the `d2ts` frontier. From DATAFLOW_REFOUNDING.
- Unlocks joined and grouped event-time incremental queries; precondition is the catalog admission of `d2ts` and the replay confirmation; a join or distinct over the decoded op-log must hold the Z-set retraction law the current `signTable` arithmetic already encodes.

[QUEUED] Frontier garbage collection wiring across every keyed map.
- Wire `frontier-gc/frontier-gc#FRONTIER_GC` `frontierGc` to the window-bucket, convergence-tombstone, and presence-row event times so the `Frontier` antichain advances with the watermark and `finalizeBelow` evicts every below-horizon cell in one pass.
- Integrate `effect` `HashMap`/`Data.TaggedEnum` now; `@electric-sql/d2ts` `Antichain`/`sendFrontier` replaces the scalar horizon once the dataflow re-founding lands.
- Spans `standing-query/`, `convergence/lww-merge#LWW_MERGE` (tombstone set), and `convergence/presence#PRESENCE` (TTL rows); unifies presence and tombstone TTL under one frontier rule rather than per-row `expiresAt` scans. From FRONTIER_GARBAGE_COLLECTION.
- Closes the late-arrival horizon and makes replay deterministic; the `d2ts` `Antichain` representation swap rides the re-founding task.

[QUEUED] Causal-delivery buffer and stability frontier.
- Author `causal-delivery/causal-buffer#CAUSAL_BUFFER` — `CausalBuffer`, the per-entity pending queue admitting an `OpLogEntryWire` only when `dominates(heldCursor, opContext)` holds (modulo the op's own slot) and releasing the dependency-satisfied suffix in causal order through the `originFold` cursor, and `stabilityFrontier`, the `SortedSet`-of-cursors meet (greatest lower bound over the per-origin held cursors) naming the causally-settled horizon. A buffered op never paints a state no peer held; the released suffix folds through `lww-merge#LWW_MERGE`/`crdt-semilattice` in causal order.
- Integrate `effect` `SortedSet`/`SortedMap`/`HashMap`/`Option`/`Order`; the `dominates`/`vectorOrder` algebra and the `OriginCursor` arrive owned from `causality-graph/version-vector#VERSION_VECTOR`, no parallel ordering surface.
- New `causal-delivery/` sub-domain composing the version-vector cursor and `dominates`; the stability frontier is the input `frontier-gc#FRONTIER_GC` reads to finalize causally and the `MERKLE_ANTI_ENTROPY` reconciliation reads as the stable prefix unit. From CAUSAL_STABILITY_DELIVERY.
- Gap-free causal display is the capability; the released-suffix order is deterministic so two peers fold identically; the buffer stays bounded because the stable prefix evicts through the same frontier.

[QUEUED] Ordered-index collapse onto the effect sorted-collection owners.
- Replace the `evidence/evidence-correlation#EVIDENCE_CORRELATION` `byHlc` whole-array re-sort (`[...rows, e].sort(byHlc)`, O(n log n) per row) with a `RedBlackTree` keyed by the composed HLC `Order` (one O(log n) `insert`, a since-T window through the `greaterThanEqual` cursor walk that `SortedMap` does not expose), the `causality-graph/version-vector#CRDT_SEMILATTICE` RGA `cells` linear array with a `SortedMap` keyed by the dense `(after, id)` order position (logarithmic `set`/tombstone, in-order replay through `entries`), and the `frontier-gc#FRONTIER_GC` antichain plus the `CAUSAL_STABILITY_DELIVERY` meet with `SortedSet` cursor extremes read via `headOption`/`lastOption`.
- Integrate `effect` `RedBlackTree` (the `greaterThanEqual`/`at`/`insert`/`removeFirst` range owner), `SortedMap` (the keyed map; range walk is the in-order `entries`, not `greaterThanEqual`), and `SortedSet` (admitted in the one `effect` package; spelling from the branch effect-catalogue coverage seam) and the `Order.combine` HLC comparator already composed in `evidence`.
- Spans `evidence/`, `causality-graph/`, `frontier-gc/`, and `causal-delivery/`; collapses three linear re-sort/scan sites onto one ordered-structure owner per index, the flat-code surface sprawl the density floor names. From ORDERED_INDEX_COLLAPSE.
- Bounded-cost reads a long session never degrades; the persistent ordered structure shares structure under insert so reconnect-replay re-inserts identically; no capability removed, the order materialized once rather than per row.

[QUEUED] As-of temporal-query axis.
- Author `temporal-query/as-of-query#AS_OF_QUERY` — `AsOf`, the `Data.TaggedEnum` temporal coordinate (`Version` against the `d2ts` antichain, `EventTime` against the `watermark#WATERMARK` nanos, `Stable` against the `causal-delivery` stability frontier), and `asOfQuery`, the projection materializing the keyed map as of the coordinate through the `d2ts` `Index.reconstructAt` version-trace for the windowed path and the event-time replay-to-mark fold for the versionless path, plus a two-coordinate diff.
- Integrate `@electric-sql/d2ts` `Index`/`reconstructAt` (the retained version-indexed trace the as-of read materializes) and `effect` `Data.TaggedEnum`/`Match`/`HashMap`; the watermark and stability frontier arrive owned.
- New `temporal-query/` sub-domain reading the `d2ts` trace the `DATAFLOW_REFOUNDING` engine retains and the watermark event-time; the conflict inspector and evidence timeline are the consumers. From AS_OF_TEMPORAL_QUERY. Precondition is the `DATAFLOW_REFOUNDING` `d2ts` engine landing for the version coordinate.
- Time-travel reads without a second store, the coordinate discriminating one query surface; a diff of two as-of snapshots shows what a concurrent edit changed.

[QUEUED] Subscribable projection face on every store.
- Author `fold-core/projection#PROJECTION` — `Projection<A>`, the `Subscribable<A>` adapter lifting any store's `SubscriptionRef` into the `{ get, changes }` contract `Atom.subscribable` consumes, and `derive`, the combinator mapping a store into a projected view (filtered map, sorted slice, scalar gate) as one `Subscribable`. Every store (`feedStore`, `causalStore`, `crdtStore`, `availabilityStore`, `evidenceProjection`, `queryStore`) exposes its read as one `Projection` row rather than a raw ref.
- Integrate `effect` `Subscribable` (the `{ get, changes }` contract plus `Subscribable.make`; spelling from the branch `.api/effect.md` coverage seam) and `Stream.changes`/`SubscriptionRef.changes`.
- `fold-core/` owns the adapter every other store composes; the `ui` atom bridge binds each `Subscribable` at the boundary, never importing the fold interior — the `projection` half of the branch `ONE_FOLD_ONE_BINDING` concert, aligned to the `ui` atom-native-binding-collapse idea. From SUBSCRIBABLE_PROJECTION_FACE.
- One projection face routes every store, so a derived read is one `derive` row, never a parallel bind-site fold; no store returns a raw ref past the projection boundary.

[QUEUED] Degradation-gated availability fold.
- Extend `availability/availability-gate#AVAILABILITY_GATE` with a second input arm joining the `feed-stores/live-cells#LIVE_CELLS` health-degradation cell against each per-intent availability row through a `gatePolicy` row set (the `DegradationLevel` ordering deciding which command tiers a level admits), so `isEnabled` returns the conjunction of the wire `available` flag and the live degradation gate rather than the flag alone.
- Integrate `effect` `Match`/`Order`/`SubscriptionRef`; the `DegradationLevel` and the retained-capability set arrive owned from `feed-stores/live-cells#LIVE_CELLS`, the `CommandAvailabilityWire.level` column already folded.
- Internal to `availability/`, reading the `feed-stores/live-cells#LIVE_CELLS` health cell; aligns to the cross-libs `ONE_HEALTH_DEGRADATION_WIRE` seam (`csharp:Rasm.AppHost/observability/health-and-degradation#TS_PROJECTION` mints the level, the TS gate consumes it at dial time). From DEGRADATION_GATED_AVAILABILITY.
- A `Degraded` capability greys its command at the one `interchange` `CommandGateway` egress; a new health-gated surface is one `gatePolicy` row, never a second health model.

[QUEUED] Wire the convergence-law harness onto the frozen fixture corpus.
- Re-found the `convergence/convergence-law#CONVERGENCE_LAW` `opLogEntryArb`/`conflictReceiptArb` arbitraries on the cross-libs `ONE_WIRE_FIXTURE_CORPUS` content-addressed golden bytes (the CRDT op-set, the HLC two-half stamps) read through the new branch `typescript:testing/` fixture reader rather than folder-local hand-minted bytes, so a parity drift surfaces as one corpus mismatch.
- Integrate `fast-check` (the arbitrary spine reads the frozen corpus as a `constantFrom` seed set) and `@effect/vitest`; the corpus derives from the one `@bufbuild/buf`-pinned descriptor source the C# wire authors.
- Internal to `convergence/`, reading the branch `testing/` fixture corpus (the `ONE_WIRE_FIXTURE_CORPUS` named TS consumer); the law unchanged, only the arbitrary seed source. From CONVERGENCE_PROOF_HARNESS re-open.
- The op vocabulary stays decode-admitted; the corpus is the one frozen source rather than per-folder fixtures, the cross-language parity drift caught as a single mismatch.

[QUEUED] De-hardcode the convergence, watermark, and stream-policy baked-in literals.
- Lift the `convergence/convergence-law#CONVERGENCE_LAW` `ORIGINS`/`KEY_BYTES` peer and key-byte collision sets, the `standing-query/watermark#WATERMARK` `defaultLateness` 30-second literal, and the `fold-core/stream-policy#STREAM_POLICY` `defaultStreamPolicy` buffer/throttle/batch literals into one parameterized policy value each surface inherits — the arbitrary's collision sets a generated bound, the lateness a `WindowSpec` value, the stream policy a named row — so a new posture is a row value, never a re-baked literal.
- Integrate `effect` `Duration`/`Schedule`; `fast-check` `constantFrom` reads the parameterized collision set.
- Spans `convergence/`, `standing-query/`, `fold-core/`; no new surface — each literal becomes a named policy value on its owning vocabulary. From the parameterization dimension.
- Anything that varies is a row value; the lateness, the collision bound, and the reconnect posture are policy, not hardcodes baked into the fold body.

[QUEUED] Merkle anti-entropy range reconciliation.
- Build a read-side `MerkleRangeWire` digest-comparison handshake over the `causality-graph/version-vector#VERSION_VECTOR` `dominates` algebra so a reconnecting peer reconciles its `CAUSAL_STABILITY_DELIVERY` stable prefix by set-difference rather than a full changefeed re-fold.
- Integrate `effect` `HashMap`/`SortedMap`/`Order`; the `MerkleRangeWire` shape arrives from `csharp:Rasm.Persistence/versioning/version-control#TS_PROJECTION`.
- Internal to `causality-graph/`; depends on the version-vector wiring, the causal-stability frontier (the reconciliation prefix unit), and the `MerkleRangeWire` decode landing. From MERKLE_ANTI_ENTROPY.
- Bounds reconnect cost to the divergent range; the digest names the slice a peer pulls, never an unbounded re-scan.

## [2]-[CLOSED]

- [RE-TIERED] Effect sorted-collection and subscribable catalogue coverage → branch `TASKLOG.md` — the `RedBlackTree`/`SortedMap`/`SortedSet`/`Subscribable`/`Equivalence`/`Stream`-combinator extension lands on the single-owner branch `libs/typescript/.api/effect.md`, never mutated folder-side; the `projection` ordered-index and projection-face tasks read it as a branch seam (the prior folder task miscatalogued `greaterThanEqual`/`insert`/`removeFirst` as `SortedMap` members — corrected to `RedBlackTree` in the re-tiered branch task).
- [COMPLETE] Version-vector concurrency read model wiring — `causality-graph/version-vector#VERSION_VECTOR` `causalStore` reads the live changefeed (`vectorOf` off the `origin` cursor or `CommitNodeWire.vector`, `bandOf` off the skew band) so each op carries a `Before`/`After`/`Concurrent`/`ConcurrentUncertain` verdict; unblocked by the decode-rail surfacing the full `OpLogEntryWire` + commit vector. From SKEW_AWARE_ORDERING.
- [COMPLETE] CrdtOpWire join-semilattice arm on the convergence read model — `causality-graph/version-vector#CRDT_SEMILATTICE` lifts the 10-case `CrdtOpWire` union as the join-semilattice decode (`write` context → `vectorJoin`, `add`/`remove` → OR-set, `beat`/`leave` → `EphemeralMap` delta) under the `convergence-law` delivery-order independence; unblocked by `decode-rail#CRDT_OP_DECODE`. From CONVERGENCE_PROOF_HARNESS.
- [COMPLETE] Convergence law harness — `convergence/convergence-law` authored: the `opLogEntryArb`/`conflictReceiptArb` arbitraries, the `shuffledSubarray` permutation, and the `it.scoped.prop` delivery-order-independence, idempotent-replay, and tombstone-monotonicity laws over `conflictStep`, mutation-gated by `@stryker-mutator/core`. From CONVERGENCE_PROOF_HARNESS.
- [COMPLETE] Derived reactive-query surface — `live-query/reactive-query` authored on `@electric-sql/d2mini`: the `LiveQuery`/`queryStore` combinator wires a `MultiSet` `PipedOperator` pipeline through `consolidate`/`output` and folds the sink into one `SubscriptionRef`, owned at the `projection` altitude so `ui` carries no view-state library. From REACTIVE_QUERY_SURFACE.
- [COMPLETE] Frontier garbage collection page — `frontier-gc/frontier-gc` authored: the `Frontier` antichain, the `Reclaimable` three-kind vocabulary, `advanceFrontier`, `finalizeBelow`, and the `frontierGc` unified-trim fold; the wiring carries forward as the queued GC task. From FRONTIER_GARBAGE_COLLECTION.
- [COMPLETE] Skew-aware ordering page — `causality-graph/version-vector` plus the `clock-uncertainty/skew-band` `bandsOverlap` ordering input authored: the `VectorOrderKind` partial order reconstructs `Concurrent` and `skewVerdict` downgrades it to `ConcurrentUncertain` when the bands overlap; the live-changefeed wiring carries forward as the blocked version-vector task. From SKEW_AWARE_ORDERING.
- [RESOLVED] Wire RESEARCH items closed against the authoritative `csharp:Rasm.Persistence/sync/collaboration#TS_PROJECTION` and `csharp:Rasm.AppUi/commands/commands-availability#TS_PROJECTION` seams: `ConflictOutcomeKind` (`LocalWin`/`RemoteWin`/`Merged`/`Rejected`) on `lww-merge`, the `actor`-on-`entityKey` presence identity on `presence`, and the `CommandAvailabilityWire.key`/`available` spellings on `availability-gate` (the prior `enabled` member was a defect, corrected to `available`).
