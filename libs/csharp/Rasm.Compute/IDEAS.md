# [COMPUTE_IDEAS]

The forward pool of higher-order concepts for measured execution, each grounded in the folder's domain and current platform capability — some deepen a thin owner into a fuller axis, others bind a concrete technique to a settled abstract surface. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[SYMBOLIC_CAS_SUBDOMAIN]: the `symbolic/` sub-domain grown from one expression page into a closed three-page CAS arm — `expression` (the `SymbolicExpr` algebra), `dimensional` (symbolic dimension proof feeding the units boundary), and `lowering` (the content-keyed compiled-delegate cache and the differentiable-program bridge to the optimizer adjoint and the integrator seed) — so the numeric, solver, and units lanes compose CAS capability by expression rather than a per-call delegate, a string `eval`, or a finite-difference gradient.
- The `symbolic/expression` page mines `MathNet.Symbolics` to full capability through one `SymbolicExpr` owner: the F# `Expression` algebra, `Calculus.differentiate`/`taylor`/`tangentLine` analytic gradients, `Rational`/`Algebraic`/`Trigonometric`/`Polynomial` simplification and `partialFraction`/`gcd`, `Structure.substitute`, `Compile.compileExpression{,1,2,3}` delegate lowering, and `Infix.parse` (over `FParsec`) with `LaTeX`/`Infix` projection; the `symbolic/dimensional` page lowers a parsed `SymbolicExpr` onto a symbolic unit-dimension monomial the `units/quantities` boundary proves consistent before numeric admission (a dimension mismatch is a `SymbolicFault`, never a silent numeric coercion); the `symbolic/lowering` page owns the canonical-normal-form `XxHash128` cache that reuses a compiled delegate and emits the symbolic Jacobian as a `solver/optimizer#OPTIMIZER_LANE` `GradientSource.Symbolic` row beside the existing `GeometryTape` adjoint and the finite-difference fallback.
- Unlocks an exact analytic Jacobian for `solver/optimizer#OPTIMIZER_LANE` where the gradient-adjoint today falls to finite-difference or the neural surrogate (a closed-form adjoint where the DDG tape is degenerate and finite-difference is noisy), a parsed-and-typed formula DSL the `units/quantities` boundary dimension-checks before numeric admission, a `numeric/algebra#OWNED_BUILDS` symbolic-Jacobian seed for the quadrature/integrator owner, and the constitutive-curve admission a parametric temperature/strain law needs — every consumer composing one `SymbolicExpr`, never a parallel CAS or a hand-bound lambda.
- Draws on the admitted-but-ownerless `[SYMBOLIC]` manifest rows catalogued at `.api/api-mathnet-symbolics.md` (the verified twelve-module surface); the named gap is the three-page owner, not the catalogue. The folder-local realization of the branch `csharp:IDEAS#SYMBOLIC_PARAMETRIC_ALGEBRA` seam: the branch idea owns the cross-package wire (the Persistence cost-catalog/QTO-formula consumers and the cross-libs `symbolic` graduation axis); this idea owns the Compute `symbolic/` build. DEEPENED from the prior single-page `SYMBOLIC_EXPRESSION_OWNER`: the dimensional-proof and lowering arms were latent consumers folded into one expression page; they earn their own pages because the dimension monomial and the differentiable-program cache are distinct concerns, not expression members.

[UNCERTAINTY_PROPAGATION_LANE]: a forward-uncertainty/reliability sub-domain on the solver sweep axis — the unfilled C# producer of the cross-libs `uncertainty-law` graduation axis — turning a parametric solve into a distribution-valued result with polynomial-chaos surrogates, Monte-Carlo confidence bands, and reliability/RBDO failure-probability scoring, so a design point carries its uncertainty rather than a single deterministic objective.
- A new `solver/uncertainty` page owns one `UncertaintyMethod` `[SmartEnum<string>]` axis (monte-carlo · latin-hypercube-mc · polynomial-chaos · first-order-reliability · subset-simulation) and one `RandomVariable` `[Union]` input-distribution case set (normal · lognormal · uniform · weibull · beta · empirical) over the admitted `MathNet.Numerics.Distributions` surface, propagating each `RandomVariable` through the `optimizer#OPTIMIZER_LANE` `evaluate` oracle (the same `SolveLane.Solve`-or-`Surrogate.Predict` contract the sweep drives) and reducing to a `UncertaintyResult` carrying the response mean/variance/quantiles, a Sobol total-effect decomposition, and a failure-probability with its reliability index β; the PCE row fits the orthogonal-polynomial expansion through the `numeric#DENSE_ALGEBRA` least-squares/QR route and the MC rows draw the `numeric#OWNED_BUILDS` `LowDiscrepancy` sampler the sweep `Sobol` axis already owns.
- Unlocks robust/reliability-based design optimization with no second model surface — an RBDO loop scores a failure probability against the same surrogate the deterministic optimizer fits, a tolerance-stack analysis propagates fabrication variance through the FEA solve, and a chance-constrained sweep prunes infeasible-with-confidence points before the full DOE — feeding the AEC simulation bridge a distribution-valued envelope, and the offline PCE/learned-distribution fit crosses as the `libs/.planning` `ONE_GRADUATION_EVIDENCE` `uncertainty-law` axis the C# determinism closure re-imports by content key.
- Draws on the 2025-2026 uncertainty-aware design maturation (PCE surrogates and subset-simulation reliability over expensive solvers) meeting the settled Compute sweep/optimizer and the content-keyed result cache; the C# side owns the propagation, the variance decomposition, and the reliability score, never an in-process distribution-learning loop (the learned input distribution is the offline-science companion's). FRONTIER RE-SEED: the `uncertainty-law` graduation axis has no C# producer today, and the sweep `SensitivityTornado` ranks effects but never propagates a distribution — the whole forward-UQ/reliability domain is absent.

[STREAMING_RESIDENCY_PAYLOAD]: a meshlet/quantized/splat residency-PAYLOAD codec arm on the interchange lane — the content-keyed payload bytes the AppUi `ResidencyManifest.Mint` references, NOT a second manifest mint — exploiting the admitted `meshoptimizer` meshlet/cluster-bounds/spatial-sort/encode surface the tile partition leaves to `Simplify`/`OptimizeVertexCache` alone, and giving the `PointScan`/splat payload a content-addressed residency home that supersedes the `point-catalogue-pending` fault, so the WebGPU raster path streams cluster-culled meshlets, quantized vertex buffers, and splat tiles the GLB-per-tile rail does not cover.
- A new `interchange/residency` page owns one `ResidencyKind` `[SmartEnum<string>]` payload axis (meshlet-cluster · quantized-vertex · point-splat · gaussian-splat) and one `ResidencyPayload` content-keyed buffer carrier that lowers an `ImportedGeometry` octree leaf onto `Meshopt.BuildMeshlets`/`ComputeMeshletBounds` cluster-cone bounds for GPU cluster culling, `Meshopt.EncodeVertexBuffer`/`EncodeIndexBuffer` + `QuantizeHalf`/`QuantizeFloat` quantized buffers, and `Meshopt.SpatialSortRemap` locality ordering, plus a `point-splat` arm decoding the SPZ/SOG splat bytes the Python companion returns — every buffer keyed by the `interchange#CONTENT_ADDRESSING` `XxHash128` identity so the bytes the AppUi manifest references and the TS worker fetches byte-match the C# producer.
- Unlocks the residency-payload bytes the triangulated-GLB tile content never served — a meshlet-cluster buffer with per-cluster cone bounds, a quantized vertex buffer, and a content-addressed splat tile the reality-capture `SplatSource` feeds — every payload a `ResidencyKind` row content-addressed under one identity, the AppUi `ResidencyManifest` referencing the bytes by content key, never a second tiling owner, a second manifest mint, or a parallel residency store.
- Draws on the meshlet/cluster-culling streaming-geometry standard the `meshoptimizer` library owns (`BuildMeshlets`/`ComputeClusterBounds`/`SpatialSortRemap`/`EncodeVertexBuffer`/`QuantizeHalf`, all catalogued at `.api/api-alimer-meshoptimizer.md`) and the one-owner-per-wire-shape law: the cross-libs `WEB_GEOMETRY_RESIDENCY_WIRE` manifest mint+decode leg is `[COMPLETE]` and the SINGLE manifest owner is `csharp:Rasm.AppUi/viewport/viewport-pipeline#TS_PROJECTION` `ResidencyManifest.Mint` — Compute owns the content-keyed PAYLOAD bytes that manifest references, never the manifest itself. The unexploited gap is real (the tile partition uses only `Simplify`/`OptimizeVertexCache` of the rich meshopt encode surface, and the `PointScan`/splat codec faults `point-catalogue-pending`); the splat-payload decode is the exact Compute residual the AppUi `ResidencyManifest` boundary names `[UPSTREAM-BLOCKED]`. Realizes the `csharp:IDEAS#REALITY_CAPTURE_TO_BIM` splat-payload seam at the content-keyed payload boundary.

[EMBEDDING_VECTOR_LANE]: a model-lane embedding/retrieval arm deepening the `models/inference` `Embed` run from a single mean-pool projection into a closed vector-compute owner — embedding generation, quantized vector encoding, and exact/approximate nearest-neighbour scoring over the `OrtIoBinding` device-resident path — so the BIM/geometry retrieval and the RAG-over-model-catalog use cases read one vector surface, never a hand-rolled similarity loop.
- A new `models/embedding` page owns one `VectorEncoding` `[SmartEnum<string>]` axis (float32 · int8-scalar-quantized · binary-hamming · product-quantized) and one `VectorScore` `[SmartEnum<string>]` metric axis (cosine · dot · euclidean · hamming) over the `inference#INFERENCE_MODES` `Embed` run and the `BoundLoop` steady-state path, lowering each scoring kernel onto the `tensors#KERNEL_DISPATCH` `TensorPrimitives.CosineSimilarity`/`Dot`/`Distance` SIMD members and the int8 quantize onto `TensorDtype` `ConvertSaturating` with `QuantizationPolicy`, the candidate set crossing to the `Rasm.Persistence` vector lane by content-key reference.
- Unlocks geometry/BIM-element retrieval and model-catalog RAG with no second similarity surface — a point-cloud-segment embedding scores against the federated element vectors, a quantized vector halves the residency footprint, and a binary-Hamming pre-filter prunes before the exact rerank — every metric a `VectorScore` row over the SIMD primitive, every encoding a `VectorEncoding` row, the retrieval crossing the Persistence vector lane by reference rather than re-storing.
- Draws on the `TensorPrimitives.CosineSimilarity` SIMD member and the `OrtIoBinding` device-resident embedding path both already catalogued, and the in-place scalar/product quantization the `TensorDtype` map already carries; the offline index build stays the Persistence vector-lane owner, Compute owns only the embed-and-score. FRONTIER RE-SEED: `Embed` is one mean-pool projection arm with no quantization, no scoring metric axis, and no retrieval owner — the embedding/vector-compute domain the AI/RAG pipelines need is a latent run case, not a sub-domain.

## [2]-[CLOSED]

[NEURAL_FIELD_SURROGATE] [REALIZED]: `solver#OPTIMIZER_LANE` carries the `SurrogateKind.NeuralField` row, the `NeuralFieldModel` content-keyed coordinate-MLP/Fourier-feature field, and `Surrogate.OfField`/`Predict` threading the leased `inference#INFERENCE_MODES` `RunOps.Infer` run behind the identical `(Seq<double>, double)` surrogate contract — the network fit is the Python offline-science companion, never an in-proc ORT-Training loop.

[NEURAL_RESIDUAL_FIELD_CODEC] [REALIZED]: `interchange#FIELD_RESULT_CODEC` carries the `FieldCodecPolicy.ResidualPredict` column, the `ResidualPredictor` model-lane chunk predictor, and the `FieldCodec.ResidualEncode` fold predicting each grid-coordinate-indexed chunk from its neighbours and storing the bounded residual with lossless deflate as the terminal fall-through — the grid-coordinate index is preserved so a content-defined-byte rewrite is the rejected form.

[TILES_METADATA_LAYER] [REALIZED]: `interchange#TILE_PARTITION` carries the `MetadataProperty`/`PropertyTable`/`TileMetadata`/`FeatureBand` `EXT_structural_metadata` semantic layer joining the `Rasm.Bim` IFC classification and the `solver#DISCRETIZATION_MESH` field values at the shared content-key, with `meshoptimizer` LOD on the leaf geometry — the SharpGLTF raw glTF-extension binary write (property-table buffer views, `_FEATURE_ID_0`) is the `[EXT_STRUCTURAL_METADATA]` RESEARCH leaf.

[COMPILED_EP_CONTEXT] [REALIZED]: `models#SESSION_CAPSULE` carries the `Compile` member driving `OrtModelCompilationOptions` and the `FleetContextKey` `(checksum, OrtEpDevice fingerprint)` device-keyed `ArtifactIndexRow.EpContext` publish, so a matching-hardware peer warms from the same blob-lane owner over `remote#ARTIFACT_FRAMES` instead of recompiling — a mismatched fingerprint addresses a distinct row and falls back to a fresh compile.

[REACTIVE_SOLVE_GRAPH] [REALIZED]: `scheduling#JOB_GRAPH` carries the `JobNode.InputBytes`/`NodeKey` content seed, `JobGraph.Keys`/`Reconcile` diffing new node digests against the prior key map to derive the moved set, and the clean-node replay from `models#RESULT_CACHE` — the dirty mark is the one `interchange#CONTENT_ADDRESSING` `XxHash128` content-key inequality over the existing edges, never a per-node version stamp or a second memoization owner.

[FIELD_DELTA_UNIFICATION] [DROPPED]: A content-defined-chunk merge of the geometry-delta and field codecs destroys the grid-coordinate locality the residual predictor depends on (it conflicts with NEURAL_RESIDUAL_FIELD_CODEC) and is an internal collapse, not a capability — the genuine incremental-transmission value it chased is owned by REACTIVE_SOLVE_GRAPH at node granularity instead.

[SYMBOLIC_EXPRESSION_OWNER] [SUPERSEDED]: the single `symbolic/expression` page is absorbed into and widened by the open `SYMBOLIC_CAS_SUBDOMAIN` (a three-page sub-domain: expression + dimensional-proof + lowering). The expression-page build, its `T-SYMBOLIC-OWNER` task, and the `csharp:IDEAS#SYMBOLIC_PARAMETRIC_ALGEBRA` seam carry forward unchanged under the wider idea; no capability is dropped, the dimensional and lowering arms are added.
