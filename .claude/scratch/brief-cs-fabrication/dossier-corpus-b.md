# DOSSIER — corpus-b — Rasm.Fabrication/.planning

Lane: corpus-b. Deep-read (SECOND HALF, alphabetical): `Posting/`, `Process/`, `Toolpath/` — 12 pages, 1859 LOC. Skim-for-seams (first half): `Nesting/` (nfp/stock read fully as Posting+owner seam counterparts; workholding via ARCHITECTURE), `Polygon/` (clipper/import via grep+ARCHITECTURE). Governing reads: Fabrication `ARCHITECTURE.md`/`README.md`/`csproj`/`IDEAS.md`/`TASKLOG.md`; `libs/.planning/architecture.md` strata; upstream briefs `RASM-CS-GEOMETRY-BRIEF.md` [V10], `RASM-CS-PERSISTENCE-BRIEF.md`, `RASM-CS-COMPUTE-BRIEF.md`, `RASM-CS-APPUI-BRIEF.md` [V7] (grepped for the named seams); `.api` catalogs picogk/occtnet/dstv/sharpvoronoilib/cavaliercontours/geometry3sharp (member verification).

Stance verdict up front: this corpus is **craft-excellent for the milling instance and structurally sound at the owner layer, but production-thin on coverage and mis-partitioned for growth**. The de-hardcoding spine (`Process/family`, `Process/owner`) is genuinely world-class; the failure axis is (1) admitted packages with zero consumers that ARE the production scope, (2) a post-processor that renders 8 dialects through 2 columns, (3) three author-kernels the finalized kernel gate supersedes, and (4) a folder map that cannot absorb the mandated scope.

---

## [01] PER-PAGE VERDICTS

### Deep-read lane

**Posting/program.md — 7/10.** The typed dialect-neutral G-code AST + biarc refit + jerk-limited look-ahead is high craft; biarc members (`BiArcFit2`, `Arc1IsSegment`, `Arc2d.SampleT`) verified against `api-geometry3sharp.md`; the jerk-vs-accel dimensional discipline (`:16`, `:284-289`) is correct and load-bearing. **Defects:** (a) the per-dialect `Emit` render collapses to TWO columns — `PostDialect.Comment`/`LineNumbers` (`family.md:81-88`) — so fanuc/haas/mazak render byte-identical except line numbers; NO canned cycles (G81/82/83 drill, G76 thread), NO macro/subprogram (M98/M99, `#`vars, WHILE/IF-GOTO), NO work-coordinate systems (G54-G59 beyond a single `LengthOffset` row), NO per-dialect arc mode (R vs IJK), NO decimal-format/modal-suppression policy — this is the production post-processor-breadth gap the mandate names. (b) `GCommand`→`Code` is dialect-INVARIANT (`:41-65`) yet real controllers diverge — `thread-cycle`→`"G92"` (`:51`) is Fanuc-lathe-specific; on a Haas mill G92 is coordinate-set, unexpressible in one invariant row. (c) hardcoded magic: `radialForce = 0.0012 * policy.RemovalRate` (`:183`) — a fabricated cutting-force coefficient that should be a material-derived (Kienzle `kc`) column, not a literal. (d) lead arcs are hand-built `ArcCcw` GWords via manual center computation (`LeadArc`, `:215-223`) instead of the admitted arc-native offset owner. Owner charter as it SHOULD be: the ONE machine-code posting owner over a dialect-neutral AST whose `PostDialect` carries full capability columns (arc-mode, canned-cycle grammar, subprogram convention, WCS, macro/flow, block-cap, decimal-format), with DSTV/NC1 as a sibling post arm — not a 2-column text render.

**Posting/projection.md — 6/10 craft, MISFILED.** The BSP front-to-back HLR + Clipper2 screen clip + kernel-arrangement watertight silhouette (via `BooleanSolid`) is well-built and the CSG_SILHOUETTE compose-seam is COMPLETE (`IDEAS.md:54`, composes `Arrangement.Apply`/`ToMesh`); SpatialIndex/Orient2D/Arrangement all composed not re-minted. **But hidden-line removal is a DRAFTING/documentation concern misfiled in the machine-output folder** — it shares nothing with `program.md` (G-code) except the folder name (concern-mixing, see V4). Migration note: GEOMETRY-BRIEF:166 lands a kernel `Drawing/view` visibility engine "Fabrication consumes `DrawingProjection`" — if that lands, this BSP author-kernel becomes a consumer. Owner charter as it SHOULD be: a Documentation/Drafting-plane owner (with the mandate's setup sheets, shop travelers, GD&T/tolerance, surface-finish, process-capability specs), NOT `Posting/`.

**Process/family.md — 9/10.** The crown jewel and the whole folder's de-hardcode spine. Two axes over one concept (`Process` removal-physics × `Machine` kinematics+holding), total row-relations `Machine.Admits(Process)` / `RemovalModality.Admits(CutStrategy)` / `PostDialect.Admits(RemovalModality)`, constructor-bound behavior columns, generated total `Switch`. The retirement of the flat 11-row `ToolpathKind` onto the orthogonal `CutStrategy` axis is exactly right. **Coverage gaps (seed DATA, cheap rows — but APPROACH-limiting where noted):** (a) **APPROACH — `KinematicClass` (4 rows) cannot express 5-axis rotary configuration**; `mill-5axis` binds `CartesianGantry` (`:118`) which is kinematically wrong for a trunnion (table-table vs head-head vs nutating), starving the post-processor and any 5-axis IK of the rotary-axis topology. (b) `RemovalModality` has ONE additive row — resin (vat-photopolymer), powder-bed (SLM/DMLS), and DED are distinct modalities with distinct budgets/strategies, all absent (blocks the PicoGK lane). (c) `CutStrategy` (8) lacks rest-machining (mandate-named), waterline/Z-level, scallop/constant-cusp, pencil/corner, flow/morphed-spiral, 3+2 positional, thread-mill. (d) `Machine` (13) lacks Swiss-lathe, HMC, tube-laser, press-brake, punch/turret, sinker-EDM, grinder. Owner charter: keep the axis factoring; deepen `KinematicClass` to carry the multi-axis rotary topology, add the additive sub-modalities, expand the strategy roster.

**Process/physics.md — 8/10.** Strong cross-product (`Process×Material×Tool×Operation` → `RemovalBudget` union), `CuttingData` SFM/chip-load table defeats the 90.0 hardcode, `UnitsNet` in-folder boundary + Materials peer-boundary raw doubles are both strata-correct (validated against `architecture.md:37-38`). **Defects:** (a) **`erosion` modality has NO dedicated budget** — `:136` routes `erosion → ThermalBudget`, a conflation; EDM physics (discharge energy, pulse on/off, wire feed, flushing pressure) needs a `RemovalBudget.Erosion` case (dead/degenerate modality). (b) `RemovalBudget.Additive` (`:109`) is FFF-only (extrusion-width/layer-height/print-speed/melt-temp) — resin (exposure/cure) and powder (laser-power/hatch/scan-speed) budgets absent, blocking PicoGK resin/powder. (c) fallback constants (`:152,157,162` — PierceTime 0.5, JetPressure 380, MeltTemp 210) are hardcoded defaults; acceptable as fallbacks but the production bar wants them table-sourced. Owner charter: the ONE removal-physics table with a budget case PER modality (erosion + resin + powder cases minted), fallbacks demoted behind a fuller `CuttingData`.

**Process/magazine.md — 8.5/10.** The model page. MTConnect `CuttingToolAsset` ISO-13399 tool-data model (members verified in `api-mtconnect-net-common.md`), `ToolAssembly` holder geometry, minimal-swap `Schedule` with tool-life split + sibling reload, `HolderEnvelope` shared with Guard+workholding, content-hash tool identity. Genuinely deep tool management, model-only slice (XML/HTTP/MQTT firewalled). **Minor:** `Runout` is a stub returning literal 0.01/0.005 (`:160-161`) not a measured value; swap-order is a greedy slot fold (`:129-140`) not true optimization. Owner charter is essentially met; the only structural note is placement (a Tooling concern in `Process/`, see V4).

**Process/owner.md — 9/10.** Polymorphic `Fabrication` entrypoint: `FabricationPolicy` [Union] (HiddenLine/Cam/Nest), one `Run` generated total `Switch`, `FabricationInput` SETTLED across every growth (Process/Machine, Inventory `Seq<Stock>`, `Option<NestPlan> Plan`, dialect override, Profiles/Keepouts keep/remove split, `Option<RobotCell> Cell`). `Loop.Covers` is the ONE containment owner every kernel composes. Near-perfect. Only forward note: production scope will add union cases (a dedicated Additive/resin-powder result, a Probe result, a Setup/Fixturing schedule, a solid-Ingress) — the page is explicitly extensible for this (`:19`), so this is growth not defect.

**Process/faults.md — 9/10.** Clean band. `FaultBand.Fabrication`=2700 is landed law (TASKLOG `FAULT_REBAND_2700` COMPLETE `:47`), 10 offset-derived arms, composes kernel `GeometryFault` for `DegenerateInput`, the R6 25xx census closed. **Note (APPROACH):** all 10 arms carry only `string Detail` — GEOMETRY-BRIEF:113 levels exactly this critique at the KERNEL `faults.md` ("stringly majority ... against the page's own 'typed to its failure with its real payload'"); by parallel pressure `Gouge` should carry the `Point3d`+tool, `Collision` the `ExclusionZone`, `InadmissiblePair` the typed `(RemovalModality, CutStrategy)` pair, `NoFit` the part id+tried rotations. Typed-payload deepening warranted.

**Toolpath/motion.md — 7.5/10.** Excellent factoring skeleton, thin flesh. The `(RemovalModality, CutStrategy)` cross-product + `adaptive`-over-straight-skeleton with `Option<ArcCenter>` for posting biarc + robot-cell handoff is the right shape. **But 5 of 8 arms are placeholder-grade:** `Turn` (`:76-79`) is a trivial per-vertex `Y - radius - k·stepOver` — not a real lathe radial-sweep (no Z-vs-radius profile, no stock envelope); `radial-sweep` AND `helical` both route to `Turn` (`:71,73`) — a conflation (helical threading ≠ radial roughing); `Plunge`/`Peck` (`:81-84,105-108`) are 2-move centroid stubs; `SliceWalk` (`:86-87`) maps layer vertices with no perimeter/infill ordering or travels. **Absent entirely:** rest-machining (mandate-named — re-cut leftover as a swept-volume Boolean difference), waterline/Z-level, scallop, pencil, 5-axis tilt (prose "one orientation column" `:19`, unrealized). SharpVoronoiLib (zero consumers) is the region-decomposition pocketing owner. Owner charter: keep the cross-product dispatch; realize the stub arms as real generators and expand the strategy roster to production CAM.

**Toolpath/kinematics.md — 9/10.** Exemplary. `Robots` (visose) cell owner supersedes hand-rolled DH/IK; folds `Program.Errors`→`Unreachable` (no-exception channel); the RhinoCommon↔Rhino3dm `extern alias R3` pose boundary-map is precise; collision correctly firewalled (`Robots.CheckCollisions` throws `NotSupportedException` on the rhino3dm build — verified-unavailable, routed to Guard, `:125`). Members catalog-anchored. Structural note: this is articulated-ROBOT kinematics; 5-axis CNC machine-tool post kinematics (trunnion/head-head rotary inverse) has NO owner — ties to the `family.md` `KinematicClass` gap.

**Toolpath/slicing.md — 7/10.** Strong FFF planar-section author-kernel (`Orient3D` exact plane-side, contour `Chain`, Clipper2 shells, `InfillPattern` hatch-clip). **Three findings:** (a) **APPROACH-naive false collapse** — `:19` claims "gyroid, triangular, cubic is one `InfillPattern` row" but `api-picogk.md:152` states the rectilinear/honeycomb quartet CANNOT express TPMS/gyroid (it needs the voxel/SDF path); gyroid is a 3D implicit surface, not a 2D hatch row. (b) **MIGRATION** — the "no managed slicer exists, Clipper2 is 2D-only" justification (`:3,221`) is superseded by GEOMETRY-BRIEF:168 landing a KERNEL "slice-stack owner with oriented contours + typed open sections"; the page's own seam (`ARCHITECTURE.md:47` "re-routes if one lands") now fires. (c) **INTEGRATION** — PicoGK (zero consumers) owns the entire resin/powder/SLA/DLP/MSLA layer-stack + lattice supports + conformal infill lane (mandate-named), all unrealized. The `Chain` assembler (`:114-135`) is an O(n²) mutable-soup walk the kernel slice-stack would supersede. Owner charter: FFF planar section consuming the kernel slice-stack substrate; a SIBLING voxel/SDF additive owner (PicoGK) for resin/powder/lattice/TPMS.

**Toolpath/guard.md — 8.5/10.** Solid mandatory safety floor. Swept Minkowski stadium + part-keep vs stock-keep-out DISTINCT verdicts + BVH broad-phase + `ClearanceAt` reach + safe-Z `Lift`; everything composed (SpatialIndex, Minkowski, skeleton, magazine holder), `Verdict` [Union] clean. **Minor:** `Lift` (`:81-84`) is a fixed 3-move up/over/down assuming the clearance plane clears all (stated construction, not a true collision-aware route); 5-axis tilting-tool swept envelope is growth. Consistent with owner.md's settled Profiles/Keepouts split.

**Toolpath/skeleton.md — 8/10 craft, MIGRATION-pressured.** Sophisticated wavefront straight-skeleton (`SkeletonEvent` Edge/Split, `Orient2D`-exact bisector/reflex, `ClearanceAt` clearance field = wavefront-time inscribed radius via the unit-speed `1/sin(θ/2)` scaling — genuinely elegant). **But** GEOMETRY-BRIEF:161/168 lands the KERNEL medial-with-clearance-radius + MCF curve-skeleton owner, superseding the "no managed straight-skeleton library exists, author from first principles" justification (`:3`, repeated). The split (GEOMETRY-BRIEF:206): kernel owns the medial/clearance SUBSTRATE, Fabrication keeps the trochoidal STRATEGY. Perf note: `Rebuild` (`:166-171`) recomputes ALL bisectors per event via a fresh `Wavefront.Of` — O(n²)/event, O(n³) total. Owner charter: consume the kernel medial+clearance-radius substrate; keep only the constant-engagement walk strategy.

### Skim lane (seam-relevant)

**Nesting/nfp.md — 8.5/10 (read fully as owner/posting seam counterpart).** Deep NFP/IFP kernel (Minkowski via Geometry2D, `Stock` union 7 cases, multi-sheet scheduler spilling forward, `Remnant` XxHash128 lineage, RectpackSharp fast-path, `Nest.Honor` consuming stock's `NestPlan`). `NFP_DRL_POLICY` correctly strata-gated (no Compute reference; delegate carries a raw `double`). Live seams into my lane: `Placement`→`Posting/program`, and the Persistence durable-row (see V5).

**Nesting/stock.md — 8.5/10 (read fully as `FabricationInput.Plan` seam counterpart).** Clean cutting-stock yield engine; `NestStrategy` [Union] collapsing 5 `RectangleBinPack` packers + heuristic sweep; per-sheet `SheetYield` ledger; the `[EXPRESSION_SPINE]` stateful-packer exemption is well-reasoned. `NestYield.WasteAreaMm2`→Compute rollup is the recorded next-campaign counterpart (V5).

**Polygon/clipper.md — flagged, not fully graded (corpus-a lane).** Grep exposed a package-name divergence (V8): the arc-offset owner is `CavalierContours` in csproj/README/.api but `lnContours`/`PlineOffset.ln<>` in the code fence (`:5,169-227`).

**Polygon/import.md, Nesting/workholding.md — corpus-a lane, seam-noted only.** import = DXF/DWG ingress via ACadSharp (Bim read seam); workholding = keep-out family + multi-fixture scheduler (fixturing concern misfiled in Nesting, V4).

---

## [02] CROSS-CUTTING FINDINGS

**Unmined admitted capability (catalog-anchored) — the dominant theme.** Four packages are admitted (`csproj:13-26`), fully catalogued, and have ZERO `.planning` consumers (grep-confirmed across all pages):
- **PicoGK** (`api-picogk.md`, 162 lines, Apache-2.0) — implicit/SDF/voxel AM kernel: TPMS/gyroid/cellular conformal infill (`:152`), `Lattice` supports (`:153`), SLA/DLP/MSLA grayscale + `.cli` vector layer-stack posting (`:118-131`), 3D SDF Boolean + `Shell`/`Fillet`/`OverOffset` lightweighting. ALC-firebreak: net9-only + native core, sidecar/AppHost posture (`:146`), NOT in-Rhino plugin. Zero consumers.
- **OcctNet.Wrapper** (`api-occtnet-wrapper.md`, LGPL-clean) — `ImportStep` (AP203/214/242)/`ImportIges`/`ImportStl` → `OcctShape` B-rep → `OcctMesh` (`:59-61`). The dominant machined-part interchange (shops receive STEP, not meshes). Zero consumers.
- **DSTV.Net** (`api-dstv-net.md`, pure-managed MIT) — DSTV/NC1 steel-fabrication exchange (`DstvReader`). The AEC steel-fab machine format (saws/drill-lines/copers). Zero consumers. NOTE: `.api` frames it READ-only ("reader"); README:36 implies WRITE/handoff — scope ambiguity for NC1 posting.
- **SharpVoronoiLib** (`api-sharpvoronoilib.md`, MIT) — Fortune point-site Voronoi + Lloyd relaxation for region-decomposition CAM (pocket seeds, stipple/engrave, even-spacing). Zero consumers.

Per INTEGRATION-FIRST law each is an integration MANDATE, never a removal candidate — and each maps 1:1 onto the scope-expansion mandate (resin/powder, solid ingress, steel-fab post, advanced strategies). This is V1.

**Concern mixing (folder-architecture).** (a) `Posting/` = G-code post-processing (program) + hidden-line DRAFTING (projection) — disjoint. (b) `Nesting/` = material-yield placement (nfp/stock) + FIXTURING (workholding). (c) `Process/` holds `magazine` (a Tooling concern). See V4.

**Hardcoding vs generator.** `program.md:183` `0.0012` cutting-force coefficient (should be material `kc` column); `physics.md:152,157,162` fallback constants; `magazine.md:160-161` `Runout` literal stub; `motion.md` stub arms with `Feed: 1.0` literals (`:79,87,93`). Rosters (`Tool` 9, `Material` 8, `Machine` 13, `CutStrategy` 8) are legitimately seed DATA per the brief; the above are the true hardcoding.

**Dead/conflated typed carriers.** `physics.md:136` erosion→ThermalBudget (no `RemovalBudget.Erosion`); `RemovalBudget.Additive` FFF-only; `faults.md` 10 arms stringly `Detail`; `motion.md` radial-sweep+helical both → `Turn` (one stub owns two distinct cycles). See V6/V7.

**Unwired / stale seams (migration pressure from finalized upstream).** `ARCHITECTURE.md:16` "band-2500" (stale; 2700 landed); `ARCHITECTURE.md:51`+`README:33,64` netDxf write-leg (stale; APPUI [V7] makes it ACadSharp two-format, netDxf removed); `ARCHITECTURE.md:52-53` Persistence/Schema seams (census re-points to blobstore content rows + cache artifact index, decode 2701-2710); `slicing.md:47`/`skeleton.md:3` author-kernel-forever (GEOMETRY [V10] kernel gate lands the substrate). See V3/V5.

**Duplication / parallel rails.** Low — the owner discipline is strong (one `Loop.Covers`, one polygon owner, one fault rail, one dispatch). The one real duplication risk is the arc-native offset: `program.md` hand-builds lead arcs (`:215-223`) while the arc-native offset owner exists (mis-cited, V8), and posting biarc-recovery vs arc-native-offsetting are correctly held disjoint per README:49.

**Folder-architecture verdict.** The 5-folder/17-page map cannot absorb production scope (no home for solid ingress, resin/powder additive, Voronoi strategies, rest-machining, canned-cycle/macro posting, production docs) and carries 3 concern-mixings. See V4 for the proposed growth partition.

---

## [03] VERDICT CANDIDATES (strongest, evidence-first)

**V1 — Package integration is the campaign's center of gravity: 4 admitted, fully-catalogued packages have ZERO page consumers, each = a scope-mandate lane.** PicoGK (resin/powder/lattice/TPMS/conformal-infill), OcctNet.Wrapper (STEP/IGES/STL B-rep ingress), DSTV.Net (steel NC1), SharpVoronoiLib (Voronoi CAM). Evidence: `csproj:13-26` vs grep-confirmed zero `.planning` consumers; `api-picogk.md:152-161`, `api-occtnet-wrapper.md:59-61`, `api-dstv-net.md`, `api-sharpvoronoilib.md`. Ruling: each is an integration MANDATE (new page/arm on the owning axis), driving an Additive voxel/resin-powder page, a solid-Ingress page, a DSTV post arm, and Voronoi strategy arms — never a removal.

**V2 — The post-processor is production-thin: 8 dialects render through 2 columns; no canned cycles / macros / subprograms / WCS / per-dialect arc mode.** Evidence: `program.md:121-137` `Emit` reads only `Dialect.LineNumbers`/`Comment` (`family.md:81-88`); `GCommand`→`Code` dialect-invariant (`:41-65`) cannot express G92=thread-cycle-on-Fanuc-lathe vs coord-set-on-Haas-mill. Ruling: `PostDialect` gains capability columns (arc-mode R|IJK, canned-cycle grammar, subprogram M98/M99, WCS G54-59, macro-var/flow, block-cap, decimal-format) and `Emit` branches on them; DSTV/NC1 lands as a sibling post arm — likely a `Posting/` sub-structure (dialect/canned/macro pages). Directly answers the mandate's "controller families beyond the current AST, canned cycles, macro programming."

**V3 — The kernel toolpath gate [V10] supersedes three author-kernels: skeleton, slicing, and the motion/posting offset rails RE-POINT from "author forever" to consume the kernel substrate.** Evidence: `RASM-CS-GEOMETRY-BRIEF.md:105,161,168,206` lands KERNEL medial-with-clearance-radius, slice-stack (oriented contours + typed open sections), and JoinType×EndType corner offset (CavalierContours composed kernel-side); Fabrication owns the STRATEGY, kernel the SUBSTRATE. `skeleton.md:3`/`slicing.md:3,221` assert "no managed library exists" — now false. Ruling: rewrite the three justifications; re-point `ARCHITECTURE.md:47` seam from "if one lands" to "consumes the landed owner"; keep the trochoidal walk + infill patterns Fabrication-side. WATERFALL: where Fabrication demands more than [V10] gives (clearance-radius at arbitrary query point; weighted/variable-offset medial for variable-kerf), edit RASM-CS-GEOMETRY-BRIEF surgically as named consumer pressure.

**V4 — Folder-architecture overhaul: the 5-folder/17-page map cannot absorb production scope; re-partition into growth axes and fix 3 concern-mixings.** Evidence: `Posting/` mixes G-code (program) + drafting HLR (projection); `Nesting/` mixes yield (nfp/stock) + fixturing (workholding); `Process/` holds tooling (magazine); no home for OCCT ingress, PicoGK additive, Voronoi, rest-machining, canned/macro posting, production docs. Proposed growth structure: `Process` (owner/family/physics/faults) · `Tooling` (magazine/cutting-data/probing) · `Substrate` (clipper/skeleton/voronoi) · `Ingress` (import-dxf/import-solid) · `Toolpath` (motion/guard/strategies/rest) · `Kinematics` (robot-cell/multi-axis-post) · `Additive` (slicing-fff/voxel-resin-powder/supports/lattice) · `Nesting` (nfp/stock) · `Fixturing` (workholding/setups) · `Posting` (program/dialects/canned/dstv) · `Documentation` (projection/setup-sheet/traveler/tolerance-gdt). Every folder a growth axis; no loose one-file combos.

**V5 — Stale seams / migration pressure from finalized upstream briefs: `ARCHITECTURE.md`/`README.md` carry 3 confirmed-stale references.** (a) `ARCHITECTURE.md:16` "band-2500" — landed law is 2700 (`TASKLOG:47` FAULT_REBAND_2700 COMPLETE; `faults.md`). (b) `ARCHITECTURE.md:51`+`README:33,64` "netDxf" write-leg — APPUI-BRIEF [V7] (`:93,127,170`) makes the WRITE leg ACadSharp two-format (DXF+DWG), netDxf removed repo-wide; the whole netDxf REJECTED framing (`README:64`) re-points. (c) `ARCHITECTURE.md:52-53` Persistence/Schema seams — census re-points to post-Persistence-campaign owners (blobstore content rows + cache artifact index) and durable fabrication receipts decode 2701-2710 (not 25xx). Surgical doc fixes, load-bearing for cross-package coherence.

**V6 — Motion's non-adaptive arms are placeholder-grade; rest-machining and real multi-axis/turning strategies are absent.** Evidence: `motion.md:76-108` — `Turn`/`Plunge`/`Peck`/`SliceWalk` are 2-move/per-vertex stubs; radial-sweep AND helical both → `Turn` (`:71,73`). The mandate demands "multi-axis strategies, rest machining, tool-engagement control"; rest-machining (swept-volume Boolean difference of prior-tool leftover) is entirely absent. Ruling: expand `CutStrategy` (family.md) with rest-machine/waterline/scallop/pencil/flow-spiral/3+2/thread-mill; realize the stub `Generate` arms; deepen `KinematicClass` for 5-axis rotary so the tilt strategy and a CNC 5-axis post kinematics owner become real (pairs with the kinematics.md robot-only gap).

**V7 — Dead/conflated typed carriers: erosion has no budget, additive budget is FFF-only, faults are stringly.** Evidence: `physics.md:136` erosion→ThermalBudget (EDM needs a `RemovalBudget.Erosion` case — discharge energy/pulse/wire-feed/flushing); `physics.md:109` `RemovalBudget.Additive` FFF-only (resin exposure/cure + powder laser-power/hatch/scan cases absent, blocking PicoGK); `faults.md:39-48` all 10 arms carry only `string Detail` — the same critique GEOMETRY-BRIEF:113 makes of the kernel faults.md (typed payloads: Gouge→Point3d+tool, Collision→zone, InadmissiblePair→typed pair).

**V8 — Package-name divergence (consistency): the arc-offset owner is `CavalierContours` in csproj/README/.api but `lnContours` in `clipper.md`'s code fence.** Evidence: `csproj:14` `CavalierContours`; `README:49` "CavalierContours (oberbichler)"; `api-cavaliercontours.md` `PlineOffset.ParallelOffset`; but `clipper.md:5,169-227` uses `lnContours` namespaces + `PlineOffset.ln<>`. The fence won't compile against the admitted package, or the manifest/.api are stale — a package-roster-law consistency defect spanning `Polygon/clipper.md` (corpus-a) + csproj + README + .api. Secondary: DSTV.Net .api is READ-only ("reader") while README:36 implies WRITE/handoff — clarify whether NC1 POSTING needs this package (insufficient) or a write owner.

---

## [04] LANDED LAW (do NOT re-litigate — confirmed on disk + TASKLOG)

`FabricationFault` on `FaultBand.Fabrication`=2700, offsets 2701-2710 (Nest at +10) — `faults.md`, `TASKLOG:47`. Rectangular cutting-stock engine at `Nesting/stock.md` (`StockNest.Pack`→`NestPlan`→`Nest.Honor` on `FabricationInput.Plan`; RectangleBinPack.CSharp admitted; Materials Construction/ folder gone) — `stock.md`, `TASKLOG:48`. `FabricationInput` settled shape carrying all growths — `owner.md:68-78`. CSG_SILHOUETTE composes kernel arrangement (no in-folder CSG) — `projection.md`, `IDEAS.md:54`. Robots-cell supersedes hand-rolled DH/IK — `kinematics.md`. Toolpath strategy×modality factoring retiring ToolpathKind — `motion.md`, `TASKLOG:35`. Build ON these; never re-band, re-move, or re-derive.

## [05] STALE CARDS (minor)

`IDEAS.md:27` `[GUARD]-[QUEUED]` — `guard.md` is a full realized page; card should be CLOSED/COMPLETE. `ARCHITECTURE.md:26` `Probing.cs # QUEUED (IDEAS)` is correct (genuinely unrealized). PROBING and MULTI_FIXTURE_SCHEDULE (`IDEAS.md:34,41`) are legitimately queued (production metrology + multi-setup — both mandate-relevant, both anchored).
