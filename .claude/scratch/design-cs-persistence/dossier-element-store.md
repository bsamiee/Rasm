# DOSSIER — lane `element-store` (Element/{graph,codec,identity} + Store/{blobstore,provisioning} + package README/ARCHITECTURE/TASKLOG/IDEAS)

Read on disk in full: `Element/graph.md` (290), `Element/codec.md` (454), `Element/identity.md` (652), `Store/blobstore.md` (626), `Store/provisioning.md` (631), `README.md` (192), `ARCHITECTURE.md` (118), `IDEAS.md` (62), `TASKLOG.md` (52). Register rows re-verified: E1 (cards, in-scope docs), E4 (graph/codec/provisioning halves), E5, E6 (my seams), E7, E9 (codec + blobstore halves), E12, E13 (identity + Store), E14. Member verification: `assay api` is wired ONLY to the `rhino-common` key here (`--sources` does NOT repoint; AWSSDK.S3 is restored at `~/.nuget/packages/awssdk.s3/4.0.25.3` but unqueryable), so external members fall to the `.api` catalogs per the brief's stated fallback rail.

VERDICT: the five fence interiors confirm the brief's [VERDICT] — `graph`/`codec`/`blobstore`/`provisioning`/`identity` are genuinely 9-grade. Every defect found is a PERIMETER or bounded in-fence defect the register already predicts, PLUS four findings the register undercounts (a third bare-`Error.New` breach in provisioning, dishonest checksum rows on two blobstore providers, a prose-vs-code drift in codec, and an uncataloged verify-or-die S3 member). No fence interior is naive or illusory; the rot is bands, dead carriers, hand-mapping, and card phantoms.

---

## [01] REGISTER RE-VERIFICATION (assigned rows + my-page anchors)

| Row | Status | Anchor verdict |
|---|---|---|
| E1 (card phantoms) | HOLD | `IDEAS:38` `FederatedEntity`/`EntityGraph` "is realized", `IDEAS:45` `FederatedPlan` "is realized", `TASKLOG:24` `Query/federation#ENTITY_GRAPH` `SourceKind.SignedArtifact` "is realized" — grep of `FederatedEntity|FederatedPlan|PlanNode|SourceKind|Query/federation|EntityGraph` over all 18 pages = **0 structural hits** (verified). Six stale `-[COMPLETE]` cards cite deleted pages: `TASKLOG:37` (`Query/transaction.md`), `:41` (`Store/quality.md`), `:42` (`Query/pipeline.md`), `:43` (`Sync/egress.md`), `:44` (`SchemaDdl.Sql`), `:48` (`Version/snapshots`) — none exist on disk. `TASKLOG:44` vs `:45` contradiction confirmed exact. |
| E4 — graph half | DRIFT | `GraphFault` union declared at `graph.md:154` (register cited `:155`, which is the `private GraphFault()` ctor line); codes 8300-8302 at `:162-164`. Simple-name collision with `cypher.md` `GraphFault` (836x) stands. Corrected anchor `:154`. |
| E4 — codec half | HOLD (no defect) | `CodecFault` 8310/8320+Rank/8330 (`codec.md:287-289`) = the legal 831x-833x multi-decade stride [V4] blesses. Not a collision; band is clean. |
| E4 — provisioning half | DRIFT + UNDERCOUNT | `ServerFault` 8350-8352 collision confirmed but codes at `:219-221` (register cited `:220-222`, off by one). Loose `Error.New`: `FailureRank` 8371-8373 `:72-74` (exact); readiness 8374-8375 at `:292-293` (register `:293-294`); `Admit` 8379 `:320`, 8380 `:325` (register `:321,326`). **NEW breach register MISSED**: `EmbeddedStore.Open` rails bare `Error.New(7701)` foreign-store `:433` and `Error.New(7702)` epoch-ahead `:440` through the `Refused` helper `:455-456` — loose 770x codes OUTSIDE the typed `EmbeddedFault` 7711-7714 map, bypassing the existing `EmbeddedFault.Refused` case `:525`. |
| E5 (coordination gap) | HOLD | None of the five pages (nor, per the corpus, any of the 18) owns a fenced lease, CAS primitive, membership row, or named outbox. `ARCHITECTURE.md` SEAMS declares four `[PORT]` rows to AppHost/Runtime (`:49,53,54,64`) — none is the coordination/lease/CAS/outbox/membership port `APPHOST:76-79` demands. Stale `APPHOST:71` (`Query/transaction`) / `:73` (`Sync/egress`) target deleted pages. |
| E6 (my-page seams) | HOLD | `ARCH:53` `Element/identity ⇄ AppHost/Runtime [PORT]: ObjectAcl identity store, TenantId RLS, KMS...` confirmed; `ARCH:54` graph PORT; `ARCH:59-60` blobstore content-key seams; `ARCH:63` provisioning HEALTH_PROBE. The `[V5]a` split obligation (own `ARCH:53` PORT row splits `ObjectAcl`→authority, KMS/RLS stays identity) is a real un-actioned ledger edit. |
| E7 (EF stack) | HOLD | `identity.md:82` `Cell` (`H3Cell`) mapped `HasColumnName` only — relies on the Thinktecture auto-converter; `identity.md:120` hand-writes the `NodeId` converter (`n => n.Value`, `s => NodeId.Create(s)`). One violates `ARCH:113` (no hand converters) whichever way `UseValueObjectValueConverter()` registration falls. Confirmed. |
| E9 — codec half | HOLD | `HashPolicy.Wide` (`:219`) and `FrameWide` (`:220`) have ZERO consumers (grep: `XxHash64`/`Crc64` used only in own row delegates). `Compute` (`:225`) and `ByDomainId` (`:226`) have ZERO call sites. `Content`/`Frame`/`Identity.Compute` bypassed by direct static calls (`XxHash128`/`Crc32`/`XxHash3` at `:318,320,353,354,356,437`). |
| E9 — blobstore half | HOLD | `MultipartTransfer.Upload` (`:362`) has ZERO callers across all 18 pages (grep confirmed). `BlobTransferReceipt` (`:323`) produced only by `Upload` → never constructed. `BlobResidence.Correlation` (`:160-161`) is `CorrelationId.None` at every `From`/`with` site → never populated. |
| E12 (mandate 1+2) | HOLD | `codec.md:151-158` composes the seam `ContentAddress.Of`/`OfGraph`/`Of(UInt128)` VERBATIM — no local node-canonical golden vector pinned. `IDEAS:26-32` `[FABRICATION_PROGRAM_DURABLE_ROWS]` anchors off the dead `csharp:Rasm.Persistence/Schema/ddl#IDENTITY` (`IDEAS:30`, `:23`) — the re-anchor + 2701-2710 forward-constraint obligation is un-actioned. |
| E13 (identity + Store overload) | HOLD | `identity.md` `[04]-[AUTHORITY]` spans `:273-524` fusing authz (`Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl` `:287-341`; `Authority.Effective`/`LapsedFor`/`Admit` `:465-484`) + signing + envelope on one page — the `[V5]a` extraction target. `retention⇄blobstore` cycle: `blobstore.md:528` imports the retention GC surface, imports `StorageTier` frozen-vocab. `recovery←blobstore.md ObjectStore.Head`, `cypher←provisioning ServerExtension.CreateSql` confirmed as the leg-4-vocab-consumed-earlier edges. |
| E14 (parameterization) | HOLD (benign) | `provisioning.md:133` `H3Postgis` `BaseType("h3")` row vs `README:75` `h3-pg` parenthetical; `README:60` "MUST agree". Superset legitimately holds (provisioning 20 rows = README 19 + the `h3_postgis` base-type row). Soft consistency point, no code defect. |

---

## [02] PER-PAGE VERDICTS

### Element/graph.md — grade 9/10 (register 10; concur ~9.5). Defects: 1 (band collision, shared).
Interior SOUND: one materializer (`GraphDelta.ReplayOnto` the sole delta→graph fold the inline projection AND AS-OF reconstruction run, `:136,140,198-200`), co-transactional identity (`Stage` stamps blame headers + `IdentityStore.Stamp` + one `SaveChangesAsync`, `:285-292`), generated-total `GraphStoreOp.Switch` (`:258-265`, compile-exhaustive), one-boundary fault conversion (`Lift` at `:321-327`). The `Received<T>` fold (`:310-313`) owns the receipt+absence projection once across three read legs — genuinely non-naive.
- **Defect (E4, DRIFT)**: `GraphFault : Expected` (`:154`, ns `Rasm.Persistence`, codes 8300-8302 `:162-164`) simple-name-collides with `cypher.md`'s `GraphFault` (ns `Rasm.Persistence.Query`, 836x). `Expected.Code` is the telemetry key; two same-named unions in one package is a source ambiguity. [V4] ruling: cypher renames to `CypherFault`, graph keeps `GraphFault`; graph's codes register into the [V4] `[SmartEnum<int>]` band registry sited on this page's `[FAULT_TABLES]` owner block.
- Charter-as-it-should-be: unchanged interior; `GraphFault` becomes registry-derived (`Band + n` over the Graph 830x decade); the `[V4]` `[FAULT_TABLES]` registry owner block lands HERE (the store-rail root every rail composes), carrying the re-partitioned 83xx map + PINNED MIRROR rows for foreign neighborhoods.

### Element/codec.md — grade 9.5/10 (register 10 minus E9). Defects: 2 (HashPolicy dead rows + prose drift).
Interior SOUND: seam-composed `ContentAddress` (`:151-158` composes the seam entries in one hop, no local hash owner), 8-rank `RejectTier` ladder rejecting before any decoder binds (`:349-360`), single-pass atomic seal with zeroed placeholder + `Flush(flushToDisk)` + `File.Move` (`:373-390`), CBOR `Strict`-reader trust boundary (`:119-132`), FastCDC content-defined chunk dedup (`:429-453`). The AOT resolver-list swap keeping the `InstantFormatter` head in both chains (`:96-109,145-146`) is precise.
- **Defect A (E9, HOLD)**: `HashPolicy` roster over-provisioned. `Wide` (xxhash64, `:219`) and `FrameWide` (crc64, `:220`) have ZERO consumers. `Compute` (`:225`) and `ByDomainId` (`:226`) have ZERO call sites — Seal/Verify/chunker call `XxHash128`/`Crc32`/`XxHash3` statically (`:318,320,353,354,356,437`), never through the row delegate. `Content`/`Frame` rows are likewise bypassed (dead-as-rows). [V10] ruling: either Seal/Verify route through `HashPolicy.Compute`/`ByDomainId` (making the rows load-bearing) OR the roster shrinks to the two live rows (`Identity`, `Content`) with the header `HashDomain` byte's forward-compat law stated.
- **Defect B (prose drift, NEW — register-adjacent)**: boundary prose `:175` asserts "`Verify` resolves it through `ByDomainId`" but `Verify` (`:354`) compares `artifact[5] != HashPolicy.Identity.DomainId` directly — the `ByDomainId` resolution the prose promises does not exist in the fence. Prose overstates the mechanism.
- **Verify-or-die (cross-package, note)**: `ContentAddress.Of(Node, tolerance)` named as a distinct Entry at `:153` beside `ContentAddress.Of(ReadOnlySpan<byte>)` — this is the `Of(Node,Tolerance)` overload the brief's `[SEAM_AND_RAIL_LAW]` flags as the `timetravel:187`-vs-`merge:160`-span-form split. codec composes the Node overload; the Element-seam overload set must be verified or the spelling unified (Element planning-scoped, unverifiable from here — DECISION obligation).
- Charter-as-it-should-be: shrink `HashPolicy` to `{Identity, Content}` (the only live rows) + state the `HashDomain` forward-compat law, OR route Seal/Verify through the policy; delete the `:175` `ByDomainId` prose claim; unify the `ContentAddress.Of(Node,...)` overload spelling against the Element seam.

### Element/identity.md — grade 9/10 (register: the 653-LOC/4-concern overload page). Defects: 3 (E7 hand-mapping, E13 overload, package mis-ref).
Interior SOUND: the co-transactional Marten-document stamp (`Stamp` the lone primitive `GraphStore.Commit` composes, `:129-133`), the `IdentityPolicy` big-endian order-preserving key axis (`:242-256`), the `Grant`/`GrantSet` set-algebra with `Admits` superuser-awareness (`:307-315`), deny-over-allow `Authority.Effective` (`:465-473`), the two-keyring-one-`KmsProvider`-axis envelope+signing fold (`:435-523`), the `SchemaGate` two-leg (Marten+EF) boot verdict (`:614-643`). The `SigningAlgorithm`×`KmsProvider` disjoint-arm law is genuinely deep.
- **Defect A (E7, HOLD)**: `Cell` (`H3Cell`) at `:82` mapped `HasColumnName` only (auto-converter path); `NodeCell.Node` (`NodeId`) at `:120` hand-writes its converter. One violates `ARCH:113` whichever way `UseValueObjectValueConverter()` (Thinktecture.EF) registers. [V6] ruling: register `UseValueObjectValueConverter()` + `UseSnakeCaseNamingConvention()`; the hand `NodeId` converter and the ~13 hand `HasColumnName` calls (`IdentityShape:79-108`, `NodeCellShape:118-124`) die; the `Option<Vector>`/`Seq<NodeId>`/`HashMap` LanguageExt-type converters (`:87-105`) STAY (Thinktecture.EF does not own those).
- **Defect B (E13, HOLD)**: `[04]-[AUTHORITY]` `:273-524` fuses authz + signing + envelope on the 652-LOC page. [V5]a ruling: extract `Element/authority.md` (the `Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit`+`Effective`+`LapsedFor` pure set-algebra, `:287-341,465-484`); identity keeps the relational tier + `IdentityPolicy` + KMS custody (signing AND envelope — the `KmsProvider` axis stays) + `SchemaVerdict`. Split is authz-vs-crypto, NOT signing-vs-envelope (that co-location is settled, `:279-280`).
- **Defect C (package mis-ref, NEW)**: Packages line `:19` lists `Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite` for "`Envelope`/`Point`", but those types come from `NetTopologySuite.Geometries` core (`using` at `:35`) as in-memory args to `H3Index.FromPoint` (`:141`) — no persisted NTS geometry column exists. The EF-NTS bridge contributes ZERO used members, matching E7/[ROSTER] "NTS bridges = zero mines; PRUNE". Correct the Packages line to `NetTopologySuite` core (transitive via `pocketken.H3`).
- **Defect D (schema-owner gap, E7-adjacent)**: `SchemaVerdict` gates on "EF-migration state" (`:614-643`) but NO authored DDL owner exists (`MigrationBuilder` = 0 hits corpus-wide; `element_identity`/`node_cell` DDL unauthored). [V6] sites the DDL on identity's schema section (or a migration page) and earns `Microsoft.EntityFrameworkCore.Design` its admission.
- Charter-as-it-should-be: identity slims to relational-tier + policy + KMS-custody + verdict (~410 LOC after extraction); `UseValueObjectValueConverter()`/`UseSnakeCaseNamingConvention()` registered, hand mapping gone; `element_identity`/`node_cell` DDL authored; Packages `:19` de-lists the pruned EF-NTS bridge.

### Store/blobstore.md — grade 9/10 (register 9 minus E9). Defects: 3 (E9 orphans, dishonest checksum rows, Correlation).
Interior SOUND: nine-delegate `ObjectLeg` (`:340-349`) the four providers fill once, one `bracketIO` packing fold (`:395-410`) with `Fin`-arm `Abort` on every exit, write-once 412-noop seal as the concurrency primitive (`:256-265,317`), whole-manifest dedup short-circuit (`:257-260`), the `RemoteStoreFault` `Lift` structural fold with `Transport.IsTransient` the sole retry gate (`:427-457`), write-blob-first + `PendingWrite` in-flight fence routing through the ONE `RetentionSweep` (`:548-612`), WORM `WormEvict` defense-in-depth (`:583-594`). `ObjectEncryption`/`ObjectLock` `ApplyS3`/`ApplyGcs`/`ApplyMinio` per-provider wire stamps (`:81-146`) are genuinely applied, not decorative.
- **Defect A (E9, HOLD)**: `MultipartTransfer.Upload` (`:362`) + `BlobTransferReceipt` (`:323`) are orphaned — ZERO callers corpus-wide (grep confirmed); `Placement.Put` (`:281`) and `WriteBlobFirst` (`:550`) route through `store.Put`/`provider.Put` directly, bypassing `Upload`. [V10] ruling: `Upload`/`BlobTransferReceipt` become THE composed receipt path (all callers route through them) OR both die.
- **Defect B (E9, HOLD)**: `BlobResidence.Correlation` (`:160-161`) is `CorrelationId.None` at every construction (`From` `:161`; `Multipart` `with` `:407` never sets it; dedup `existing with` `:259,264` inherits `Head`'s None). Never populated. [V10]: populate or drop the field.
- **Defect C (checksum honesty, [V10] HALF-UNFIXED — register-adjacent)**: the `Gcs` (`:239`) and `Minio` (`:240`) `ObjectStore` rows declare `ObjectChecksum.XxHash128`, but `GcsLeg.Seal` (`:495`) and `MinioLeg.Seal` (`:512`) never call `store.Integrity.Wire` — only `S3Leg.Seal` (`:467`) supplies `ChecksumXXHASH128`. `Wire` (`:45-49`) returns `Some` only for `XxHash128`, so the column is DECORATIVE on GCS/Minio (they actually ride SDK-native CRC32C/CRC64). The `:16` prose states the S3-only honesty but the ROWS contradict it. [V10]: set GCS/Minio rows to `Crc64`/`None` (honest) OR apply per-provider.
- Charter-as-it-should-be: `Upload`/`BlobTransferReceipt` become the one receipt path OR delete; `Correlation` threaded from the write op OR dropped; GCS/Minio checksum rows read the honest SDK-native stance; interior otherwise preserved.

### Store/provisioning.md — grade 9.5/10 (register 9.5 minus E4 loose codes). Defects: 2 (band collision + 3 bare-`Error.New` sites).
Interior SOUND: the one six-command `NpgsqlBatch` verification fold (`:239-260`) data-volume-independent, `FailureRank.Absorb` as behavior-carrying absence policy (`:66-78`), the `ExtensionAdmission` `.api`-gated install preconditions (`:80-113,318-325`), `RestartClass.Max` worst-disruption aggregation (`:62-63`), the embedded residency-split ritual (`:425-460`) with defensive `sqlite3_db_config` + IMMEDIATE first-opener gate, the `EngineOps` `Handle`-bridge capsule (`:560-620`) converting every raw crossing to closed `EmbeddedFault`. The slot-lag/`confirmed_flush_lsn` distinction (`:16`) is a precise, non-naive gauge.
- **Defect A (E4, DRIFT + collision)**: `ServerFault` 8350-8352 (`:219-221`) collides with `ColumnarFault` 835x. [V4]: Columnar keeps 835x, `ServerFault`+receipt codes re-band to a fresh decade via the registry.
- **Defect B (E4/[SEAM_AND_RAIL_LAW], HOLD + UNDERCOUNT)**: bare `Error.New` on rails is the deleted form, and provisioning is a live breach at MORE sites than the register lists: `FailureRank` 8371-8373 (`:72-74`), readiness 8374-8375 (`:292-293`), `Admit` 8379/8380 (`:320,325`), AND — **register-missed** — `EmbeddedStore.Refused` 7701/7702 (`:433,440` via `:455-456`) as loose 770x codes outside the typed `EmbeddedFault` 7711-7714 map, when `EmbeddedFault.Refused` (`:525`) already exists to carry them. [V4]: every loose code becomes a typed case or registered sub-band row; `Refused` rails `EmbeddedFault.Refused` not `Error.New`.
- Charter-as-it-should-be: `ServerFault` re-bands to a fresh decade in the [V4] registry; all seven loose `Error.New` sites (8371-8375, 8379-8380, 7701-7702) become typed cases; `EmbeddedStore.Open`'s `Refused` helper rails `new EmbeddedFault.Refused(...)` (or a new sub-banded case) — closing the last bare-`Error.New` in the lane.

---

## [03] CROSS-CUTTING FINDINGS

### C1 — Verify-or-die member UNCONFIRMED: `CompleteMultipartUploadRequest.ChecksumXXHASH128`
blobstore.md `S3Leg.Seal` (`:467`) composes `ChecksumXXHASH128 = store.Integrity.Wire(key)...`. The `.api` catalog (`api-objectstore.md`) carries `ChecksumAlgorithm.XXHASH128` (row [21], `:58`) and `ChecksumType.FULL_OBJECT` (row [22], `:59`) as decompile-verified rows, but the request property `CompleteMultipartUploadRequest.ChecksumXXHASH128` is NOT cataloged — row [06] (`:43`) lists only `PartETags`. `assay api` cannot confirm it here (wired to `rhino-common` only; `--sources` does not repoint; AWSSDK.S3 restored at 4.0.25.3 but unqueryable). This is exactly the brief `[04]`'s "open confirm before the blobstore leg closes". Status PLAUSIBLE — the DECISION/leg must confirm the property via assay (AWSSDK.S3 registered as a source) or nuget MCP and add the catalog row; the two enum members it depends on ARE verified.

### C2 — Egress seam `BlobRemote.Placement` has no in-corpus consumer (defensible, not a defect)
`BlobRemote` (`blobstore.md:292`) produced by `Placement` (`:277`) has ZERO callers across the 18 pages; the `topology.md:126` "Placement" hit is a false positive (QuikGraph context). Prose (`:272-289`) declares the consumer is the above-seam `Rasm.Compute` runner's app-wired `Graph/element#NODE_MODEL` `GeometrySource` port — a legitimate downstream-owner egress seam per `[SEAM_AND_RAIL_LAW]` (consumer recorded where the owner is upstream). NOT a dead carrier, but the DECISION's seam ledger should carry the `Store/blobstore#Placement ⇄ Rasm.Compute GeometrySource` row explicitly (currently only prose).

### C3 — Naivety axes
- **Axis-1 (naive/shallow)**: identity hand-mapping (E7 `:82,120` + ~13 `HasColumnName`) where Thinktecture.EF + snake-case convention should own the form; provisioning's seven bare-`Error.New` rails bypassing the typed-fault discipline the rest of the corpus holds; blobstore's decorative `XxHash128` checksum rows on GCS/Minio. These are the only shallow spots — all bounded, all register-predicted (except the 7701/7702 breach and the checksum-row honesty, which the register undercounts).
- **Axis-2 (over-provisioned/dead)**: codec `HashPolicy` 5 rows / 3+ dead (Wide/FrameWide/Compute/ByDomainId zero-consumer); blobstore orphaned `Upload`/`BlobTransferReceipt`/`Correlation`. No object/type proliferation elsewhere — the pages hold extreme ADT collapse (one `ObjectLeg` for four providers, one `GraphStoreOp` for six ops, one `KmsProvider` axis for two keyrings). The corpus does NOT over-engineer; it under-prunes dead rows.

### C4 — Card governance (in-scope IDEAS/TASKLOG)
Every OPEN federation card (`IDEAS:34-47` `[REUSE_WIRE]`/`[SUBSTRAIT_FEDERATION_SEAM]`, `TASKLOG:20` `[ARTIFACT_CONTENT_KEY_FEDERATION]`) asserts a realized Persistence-side federation owner that does not exist (E1). `IDEAS:26-32` `[FABRICATION_PROGRAM_DURABLE_ROWS]` anchors off dead `Schema/ddl#IDENTITY` (`:30`). Six `-[COMPLETE]` cards (`TASKLOG:37,41,42,43,44,48`) verify-close pages the tree does not contain — governance is decoupled from disk. [V12]/[V1] disposition: phantom-realization prose corrects to honest BLOCKED status re-anchored to the real substrate (`ElementSet`/`cache`/`commits` currency), the six stale COMPLETE cards re-point to their real fold owners, `[FABRICATION_PROGRAM_DURABLE_ROWS]` re-anchors to the blobstore content rows + cache artifact index with the 2701-2710 forward constraint.
