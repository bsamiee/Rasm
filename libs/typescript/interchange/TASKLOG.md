# [INTERCHANGE_TASKLOG]

Open and closed work distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` when open; `[COMPLETE]` or `[DROPPED]` when closed — and bullets naming the capability or file to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks.

## [1]-[OPEN]

[QUEUED] DESCRIPTOR_EVOLUTION_GATE — author `evolution/descriptor-gate.md` `DescriptorGate`.
- Build the new `evolution/descriptor-gate.md` page: the `DescriptorVerdict` `Data.TaggedEnum` (`Identical | Additive | Breaking`, the `Breaking` arm carrying the `ReadonlyArray<DriftPath>` of failing `message.field` paths), the `classifyDescriptor(held, observed)` fold over `@bufbuild/protobuf` `createFileRegistry`/`DescMessage`/`DescField`/`DescService` reflection mirroring the C# `transport#DESCRIPTOR_GATE` `Classify` diff algebra (removed message/method, field-number reuse, `fieldKind` change, list-flip, oneof-membership change all `Breaking`; new message/field-on-unused-number/enum-value/method `Additive`), and the declaration-order projection checksum so a generator-version reorder is `Identical`, not a false breaking. The `DescriptorGateLive` `Effect.Service` reads the committed app-root `FileDescriptorSet` (the same `transport#CODEGEN_TOOLING` input) and gates `WireClients` construction: a `Breaking` verdict faults dial-time through `faults/fault-family.md` `FaultDetail.Quarantine`.
- Integrate `@bufbuild/protobuf` `createFileRegistry`/`createRegistry`/`DescMessage.fields`/`DescField.fieldKind`/`DescService.methods`/`DescMethod.methodKind`/`UnknownField`; `@bufbuild/buf` `protoc-gen-buf-breaking` is the build-time governance leg the `buf.gen.yaml` carries, the runtime classifier is the dial-time leg — two legs, one verdict vocabulary.
- Integrates at the `transport/transport.md` `WireTransportLive` dial seam (the verdict gates client construction beside the `available` capability bit) and mirrors the C# `csharp:Rasm.Compute/remote/remote#TS_PROJECTION` descriptor source; the verdict is the TS reading of the C#-emitted `Verdict`, classified once against the wire descriptor, never a per-call re-classify.
- The classifier reads the runtime descriptor exactly as the binary carries it (no build-step plumbing); `Additive` tolerance rides the `UnknownField` round-trip the descriptor runtime preserves by default, never a branch-side allow-list; a generator-version descriptor-byte reorder hashes identical under the declaration-order projection, never a false `Breaking`.

[QUEUED] RECORDED_INTENT_PATCH — author `codecs/patch-rail.md` `PatchRail`.
- Build the new `codecs/patch-rail.md` page co-located in `codecs/`: the six-arm `PatchOpWire` `Schema.Union` over the `op` discriminant (`add | remove | replace | move | copy | test`, `add`/`replace`/`test` carrying `value`, `move`/`copy` carrying `from`+`path`), the `applyPatch` fold mapping the error-accumulating `(MissingError|TestError|InvalidOperationError|null)[]` result into one typed `FaultDetail.HopFault` keyed by the failing operation index, the `createTests` optimistic-concurrency guard prefix, and the proto `FieldMask` lower (the repeated dotted-path string set the `@bufbuild/protobuf` descriptor runtime carries) into the same `Operation[]` so binary sparse-update and json-stj patch share one apply owner. The patched value re-enters `quarantine/drift-terminal.md` admission as a whole value, never straight to a render binding.
- Integrate `rfc6902` `applyPatch`/`apply`/`createTests`/`Pointer.fromJSON`; `effect` `Schema.Union`/`Schema.Literal`/`Schema.decodeUnknown` for the six-arm decode; `@bufbuild/protobuf` `FieldMask` well-known-type for the binary sparse-update lower.
- Decodes the `csharp:Rasm.AppHost/live-wire/live-wire#TS_PROJECTION` write-back partial-update wire and the proto `FieldMask` sparse-update; integrates the `refinement/schema-refinement.md` `JsonPointer` brand (RFC 6901 path) the patch `path`/`from` slots carry, so a raw string never enters a pointer slot.
- A `test` op mismatch faults rather than corrupts (the optimistic-concurrency self-guard); the patch is recorded intent decoded off the wire, never a `createPatch` diff re-derived at the consumer; an unknown `op` faults at the `Schema.Union` decode boundary as `Additive`/`Breaking` drift, never as an `InvalidOperationError` slot after a partial apply.

[QUEUED] GEO_WIRE_VOCABULARY — deepen the `codecs/decode-rail.md` `geometry` row to a real GeoJSON vocabulary.
- Extend the `decode-rail.md` `[2]-[DECODE_RAIL]` `geometry` `CODEC` row from the bare `JSON.parse` stub to a `GeoJsonWire` `Schema.Union` over the seven RFC 7946 geometry types (`Point | LineString | Polygon | MultiPoint | MultiLineString | MultiPolygon | GeometryCollection`) plus the `Feature`/`FeatureCollection` wrappers, the `Position` `Schema.Tuple` filter enforcing 2-or-3 ordinates, the WGS84 longitude `[-180,180]`/latitude `[-90,90]` `Schema.filter` bound (the C# CRS-fixed-by-format law as a decode filter), and the absent-CRS-member guard; the row still sources no contract of its own, decoding the GeoJSON the C# `GeoJsonConverterFactory` emits, transcribed not re-authored.
- Integrate `effect` `Schema.Union`/`Schema.Tuple`/`Schema.Literal`/`Schema.filter`/`Schema.Array` for the geometry-type discrimination and the coordinate-shape floor; no new package.
- Mirrors `csharp:Rasm.Compute` and `csharp:Rasm.Persistence` embedded-GeoJSON payloads (snapshot-delta, evidence-envelope) against the C# `data-interchange#GEO_INTERCHANGE` `[TEXT_RAIL]` NTS-interior two-wire-projection law; the geometry row composes the `quarantine/drift-terminal.md` fold for the malformed-`type`-literal case the C# `JsonException`/`ArgumentException` taxonomy carries.
- The embedded row decodes XY/XYZ positions only — an XYM/XYZM measure-bearing payload faults as `Breaking` drift because the GeoJSON text wire structurally cannot carry the M ordinate (the C# blob projection routes measure data, never the text wire); a projected (non-WGS84) coordinate faults the longitude/latitude bound rather than rendering corrupt; the geometry provenance stays embedded-only, never an invented standalone contract.

[QUEUED] STREAMING_SEGMENT_DECODE — extend the `messagepack` row with async stream decode and the compression-codec admission.
- Extend the `decode-rail.md` `[2]-[DECODE_RAIL]` `messagepack` row with `decodeSegmentStream` lifting the `csharp:Rasm.Persistence/sync/collaboration` sync-segment `ReadableStream` into a backpressured `Stream<OpLogEntryWire, ParseError>` through `@msgpack/msgpack` `Decoder.decodeArrayStream`, and the `SNAPSHOT_CODEC` (`none`/`lz4`/`zstd`) decompression admission gating the `Decoder` read so a compressed snapshot frame decompresses before decode; the segment stream shares the transport framing fold's `Stream.buffer({ capacity: 1, strategy: "suspend" })` one-frame window.
- Integrate `@msgpack/msgpack` `Decoder.decodeArrayStream`/`decodeMultiStream` (the async-generator stream decoders) and `Stream.fromAsyncIterable`; the `lz4`/`zstd` browser decompression provider is the RESEARCH spike (a wasm decoder vs the C# `K4os.Compression.LZ4`/`ZstdSharp` producer — a NAMED package-admission candidate the spike resolves).
- Decodes the `csharp:Rasm.Persistence/snapshots/codecs#TS_PROJECTION` compressed snapshot frame and the `sync/collaboration#TS_PROJECTION` segment stream against the C# `data-interchange#CHUNK_ALGEBRA` symmetric-chunk law; integrates the `refinement/schema-refinement.md` `boundedFrames` budget so an unbounded segment stream faults before exhaustion.
- [RESEARCH on the wasm lz4/zstd browser decompression provider — a NAMED package admission the spike resolves: the C# producer is `K4os.Compression.LZ4`/`ZstdSharp`, the browser decode seam needs a verified wasm equivalent before the decompression row is fence-code, gating the compressed-frame leg while the `decodeSegmentStream` async-decode leg lands unblocked.]

[BLOCKED] HLC_TWO_HALF_PARITY — assert the bigint two-half round-trip against the frozen stamp.
- The `parity/content-key-parity.md` `HlcTwoHalfParity` round-trip algebra is fenced — `compose` over the two 32-bit halves and the `DataView` `setBigUint64`/`getBigUint64(_, false)` big-endian round-trip plus the `@msgpack/msgpack` `useBigInt64` bigint mapping the `decode-rail.md` `decodeCrdtOp` and `decodeSnapshotHeader` carry — but the cross-runtime byte-equality assertion needs the FROZEN two-half HLC reference stamp.
- Integrate the verified `@msgpack/msgpack` `Decoder({ useBigInt64: true })` and the `DataView` big-endian read; the algebra is settled, only the producer-frozen stamp gates the equality.
- Asserts the C#-owned two-64-bit-half order the `projection` conflict-presence fold reads — an HLC `logical` off-by-one-half folds a fresh op as stale with no other signal, so the half-order assertion is load-bearing.
- Blocked on the absent `csharp:Rasm/Geometry/topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` row [6] `HLC_TWO_HALF`, itself DESIGN-PIN on `csharp:Rasm.AppHost/ports/runtime-ports#HLC_FANIN` — a producer cluster the runtime-ports page does not yet carry (its `HlcStampWire` is the receipt-envelope `logical: number` stamp, not the two-half bigint fixture). No fabricated stamp stands in. [UPSTREAM-BLOCKED on csharp HLC_FANIN#HLC_TWO_HALF fixture pin]

[BLOCKED] CAPABILITY_SDK_CODEGEN — admit the second codegen plugin.
- Build the second `buf.gen.yaml` plugin row and the `CapabilitySdkLive` `Effect.Service` fold in `transport/transport.md` `CODEGEN_TOOLING` deriving the typed effect-classed command surface and the MCP tool projection from the generated `capabilities_pb.ts`; the `CapabilitySdk` SERVICE CONTRACT is already fixed on that page, the `*Live` fold lands here against the confirmed generated `CapabilityClient.discover`/`invoke`/argument-schema members.
- Integrate `@bufbuild/buf`, `@bufbuild/protoc-gen-es`, and the capability-descriptor codegen plugin.
- The upstream C# `csharp:Rasm.AppHost/capability/registry#SDK_CODEGEN` producer is SETTLED — the `SdkTarget.Typescript` renderer emits `{Method}(args): Promise<ReceiptEnvelopeWire<CapabilityCommandReceiptWire>> { return this.run(...) }`, the `DiscoveryResultWire`/`CapabilityCommandReceiptWire` descriptor shapes are fixed at `#TS_PROJECTION` and transcribed verbatim on `transport.md`, and the `invoke`-by-descriptor / per-descriptor `JsonSchemaExporter` `inputSchema` law is producer-fixed. The remaining gate is NOT the descriptor shape but the emitted `capabilities_pb.ts` module: `protoc-gen-capability-es` is a local buf plugin that does not yet exist, so `CapabilityClient.discover`/`invoke`/`argumentSchema` are not `.d.ts`-confirmable and no `*Live` fold calls unverified generated members.
- Blocked on the absent `protoc-gen-capability-es` plugin emitting `capabilities_pb.ts`; `invoke` is one polymorphic method keyed by descriptor id, never a sibling method per descriptor; the `inputSchema` is the generated per-descriptor JSON Schema, never a hand-built stub. [UPSTREAM-BLOCKED on the absent protoc-gen-capability-es generated capabilities_pb.ts]

## [2]-[CLOSED]

[COMPLETE] CONTENT_KEY_PARITY_HARNESS — `parity/content-key-parity.md` `ContentKeyParity` fences the binding asserting the worker-minted 16-byte `ContentKey` reproduces the FROZEN `csharp:Rasm/Geometry/topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` row [1] `CANONICAL_BYTE_IDENTITY` digest bit-identically through the production `xxHash128Of`, normalizing LE↔BE once at the `h128` boundary (`digest("binary").reverse()`) and comparing via `bytesEqual`. The `typescript:testing/` corpus consumer is the future DRIVER; the HLC two-half leg split to `HLC_TWO_HALF_PARITY`.

[COMPLETE] CRDT_OP_DECODE — `codecs/decode-rail.md` `[3]-[CRDT_OP_DECODE]` fences the ten-arm `CrdtOpWire` union (`set | write | add | remove | increment | insertAfter | delete | maintain | beat | leave`) decoded from a `columnFamily === "crdt"` `OpLogEntryWire.payload`, sourced verbatim from `csharp:Rasm.Persistence/versioning/version-control#CRDT_WIRE` (the `[tag, [fields]]` array decode through the reused `useBigInt64` `Decoder`, the `CRDT_OP_TAG` index→`op` normalize, the flat `(physicalTicks, logical)` HLC halves); `decodeOpPayload` routes only the `crdt` family, single-mint verified.

[COMPLETE] PROTOCOL_SELECTION_AXIS — `transport/transport.md` `WireTransportLive` fences the `TransportProtocol` `Match.exhaustive` over `createConnectTransport`/`createGrpcWebTransport`, one interceptor stamping correlation/traceparent/bearer through one `Runtime.runPromise`, the `TransportCapabilityWire` per-protocol method tuple read from the wire.
[COMPLETE] WEBTRANSPORT_FORWARD_CASE — the `webTransport` `available: false` row faults at construction via `FaultDetail.HopFault`; the retry rail is realized: `retrySchedule` (`Schedule.exponential`/`jittered`/`union`(`recurs(3)`)) plus `retryUnary` over the verified `Effect.retry(self, { schedule, while: retryableWire })` options arm keying the typed `FaultDetail` error channel, the prior `Schedule.whileInput`/`intersect` RESEARCH item resolved against the installed `effect@3.21.3` `Retry.Options<E>` surface.
[COMPLETE] STRUCTURED_DRIFT_REPORT — `quarantine/drift-terminal.md` fences `quarantineFold` (lenient `preserve`/strict `error` double-decode), the three-case `ContractDrift` `Data.TaggedEnum`, `driftReportOf` over `ArrayFormatter.formatErrorSync`, and the `ISSUE_DRIFT` `Record` vocabulary keying each issue tag to its `DriftClass`.
[COMPLETE] DECODE_BUDGET_REFINEMENT — `refinement/schema-refinement.md` fences `DecodeBudget`, `INGRESS_BUDGET`, `decodeBounded` (`depthOf` recursion cap), and `boundedFrames` (`Stream.take`/`mapAccum`/`filterMap` frame-count + assembled-byte ceiling); `drift-terminal.md` fences the `sanitize` `DOMPurify.sanitize` row.
[COMPLETE] TRANSFERABLE_STREAM_REASSEMBLY — `artifacts/frame-reassembly.md` fences `transferFrames` (verified `Stream.toReadableStream` data-last destructor + `CountQueuingStrategy` one-frame window) and `xxHash128Of` (the `hash-wasm` `createXXHash128(0, 0)` `IHasher` binding, `digest("binary").reverse()` to the C# big-endian `ContentKey`); `ArtifactFrameRailLive` delegates to `DecodeWorkerPool.reassemble` so the worker owns the single-hash-mint, consuming back through `Stream.fromReadableStream`/`fromReadableStreamByob`. The 128-bit-provider gate is closed and the cross-runtime byte-identity ASSERTION is realized at `parity/content-key-parity#CONTENT_KEY_PARITY` against the FROZEN `CANONICAL_BYTE_IDENTITY` corpus fixture.
