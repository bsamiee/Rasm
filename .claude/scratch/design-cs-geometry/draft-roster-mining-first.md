# [DRAFT] ROSTER-MINING-FIRST — RASM-CS-GEOMETRY structural blueprint

## [00]-[LENS_AND_THESIS]

Lens: derive every owner from what the surviving roster + the 34 landed kernel planes can ACTUALLY do — every `[04]` roster row and every probe finding gets a home before any consumer wish shapes a page. Mining the roster to depth forces three structural facts the other lenses discover late:

1. **The GShark access walls make the vendored NURBS substrate a PAGE, not a package.** Probe G1/G2 are access-modifier walls with zero in-envelope closure; the no-re-mint clause (`api-gshark.md:171`) forbids owned re-implementation; the one-engine law forbids a package+vendored split. The only lawful mining of the roster's NURBS capability is vendor-and-own NOW — so the parametric tier's floor is `Parametric/nurbs.md`, a kernel-owned vendored engine page, and the GShark pin dies in wave 1.
2. **The implicit-point probe makes the `[V4]` exact path a PREDICATES+DELAUNAY co-motion, not a meshing patch.** The floor's implicit family is position-locked, its constructions defective (rounded `Denominator`, Z-dropped `Tpi`, pre-rounded leaf differences, no negative-λ flip), and the vertex table explicit-only. Probe gaps 1-5 land as case-row widenings on the two floor owners in the same legs that consume them — and the PeterO directed-rounding interval filter (`README:86`) stops being optional hardening: it is the throughput tier that makes exact implicit predicates affordable at arrangement scale.
3. **The weld primitive is ARENA capability.** `[V5]`/`[V6]` as briefed leave `Kernels.WeldDuplicates`/`RepairPolicy.WeldTolerance` in wave-3 repair while wave-2 arrangement composes them — the meshing dossier's partition defect. Dedup-on-the-working-set is an arena operation: the weld sites in `Meshing/edit.md` (wave 1), which is what actually breaks the arrangement→repair cycle.

Every capability is improved or extended, never dropped: the Minkowski convolution completes (arc-fans + symmetric direction), the medial lands real against the Voronoi-dual accessor (probe-decoupled), the exact hull projection lands as a `Tessellation` byproduct, quad remesh lands as the stripes-guided row, and the dtype-strided residency lands real on the verified `ReadOnlyTensorSpan<T>` dispatch.

## [01]-[PROBE_RULINGS] (both hinges ruled from probe evidence, baked into page rows, never re-opened mid-leg)

**GShark hinge — VENDOR-AND-OWN NOW, whole set.** Ruling grounds: G1 (surface ctor `internal`, no subclass path — wire ingress dead, SpineRef surface resolution dead) and G2 (derivatives `internal` + open #234; public API metric-blind) are access walls; dormancy is two-source confirmed (2023-08), so every open defect is frozen permanent; the one-engine law makes a package+vendored-surface split illegal; MIT permits the vendor cleanly. Consequences executed by this blueprint: (a) `Parametric/nurbs.md` NEW in wave 1 owns the vendored algorithm set — arbitrary-knot construction for BOTH carriers (kills G1 incl. the curve-side `protected`-ctor workaround), curve+surface rational derivatives with #234 order-flip fixed (kills G2), fundamental forms + principal/Gaussian/mean curvature as kernel projections over the derivatives (kills G3), Piegl-Tiller surface interpolation/approximation lane (kills G5's fitting half), Wang-2008 RMF with closure-redistribution policy row (#373) and coincident-sample guard, Bezier-decomposed Gauss-Legendre arc-length at 1e-6, parameterized `ClosestParameter` knobs (kills G7), public knot algebra (`Normalize`/`Refine`/clamp) with the clamp-and-normalize-at-admission law (G6/G10) so canonical bytes hash the NORMALIZED form — one curve, one content key; (b) the `GShark` pin (`PROPS:73`, `Rasm.csproj:18`) and kernel `api-gshark.md` DELETE in the wave-1 roster motion (the probe's six catalog corrections are absorbed into the `nurbs.md` fence as member truth); (c) G4 widens the host-deferred charter row to the FULL intersection triple (SSI + surface-plane + curve-surface); (d) G8 closes as a kernel deviation-refinement + trim op over the crossing lattice in `curve.md`; (e) G9 closes as the standard boundary-kernel catch to the `Fin` rail.

**Implicit-point hinge — the `[V4]` EXACT PATH LANDS; no honesty re-scope.** Probe gaps 1-5 are mandatory companions, homed: (1) `delaunay.md` vertex payload widens to the closed two-kind row store `Explicit(Point3d)` | `Crossing(defining input-entity ids)` (SSI-2D: 4, LPI-3D: 5, TPI-3D: 9) under the depth-1 law — implicit rows reference INPUT vertices only, recovery-time constraint×constraint crossings re-express as TPI-3D over original entities, sub-segment splits re-anchor on the `Constraint` carrier; (2)+(3) `predicates.md` re-founds `Lpi` as SSI-2D beside NEW LPI-3D (three numerators + λ) and `Tpi` over nine input points with X,Y,Z numerators, error-free-transform leaf differences, the rounded `Denominator` dies, the orient pair gains the negative-λ flip, and the family widens ~21 members for the 2D path (`Orient2D` {IEE,IIE,III} + axis-projected variants, `LessThanOn{X,Y,Z}` {IE,II}) with a directed-rounding interval-filter tier on EVERY implicit member (PeterO `ERounding.Floor`/`Ceiling` brackets — kills the false no-filter rationale and the BigInteger-per-cavity-test cost); (4) in-circum ruled MINIMAL: `TessellationPolicy` gains a constrained-only mode for implicit-bearing builds (the reference pipeline's exact shape — validity, not Delaunayhood); the `InCircle` implicit combos {IEEE..IIII} are a recorded growth row gated on a Delaunay-restoration-over-implicit consumer; `InCircleLPI`'s single-query shape stays for the explicit-corner Steiner case; (5) `intersect.md`/`arrangement.md` carrier plumbing — `Crossing` carries defining-entity ids (the `bool IsImplicit` + `PlaneCrossPoint` die), interning and chain-merge key on defining entities, `.Round()` is the ONE emission-seam materialization, and both pages state the signs-exact/coordinates-rounded-at-emission contract; (6) the 3D `Tetrahedralization`/`Facet` implicit story (orient3d/inSphere multi-implicit) is the recorded CDTet growth burden, gated on the Compute consumer. `[V10]`b rides the cheapest sub-family (LPI-3D + projected Orient2D + LessThan; no in-circum). `[V10]`c is DECOUPLED: the medial CDT input is all-explicit (simple polygon ring, zero Steiner points) — the Voronoi-dual accessor lands regardless, as exact DT adjacency + circumcenters materialized at the emission seam; the exact circumcenter side-of test is a recorded growth row.

## [02]-[RATIFIED_PARTITION] — [V1] folders · namespaces · fault homes

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`; each executes in the leg that moves its pages, updates `ARCHITECTURE.md` `[01]`+`[02]`+`[03]`, `README.md` router/roster, `faults.md` taxonomy, and runs the `[LANDED_KERNEL_LAW]` (b) inbound sweep):

- `Solving/` MINTED (wave 3): `solver.md` + `fit.md` move out of Processing — the `[V7]` one-LM family; ≥2 pages, real growth axis (constrained optimization, registration-class solvers).
- `Drawing/` → `Emission/` RENAMED (wave 4, the `[V1]` ruled default executed): charter = downstream emission = projection + residency encoding; `view.md` + `pack.md` move with it; no landed page lives in Drawing, so the rename sweeps only robust-core paths + ledger/counterpart anchors.
- All other folders keep their landed pages' homes; `Parametric/` keeps its ruled name beside landed `projections.md`/`locate.md`.

Ratified TIER-2 namespace ↔ fault-home partition (replaces the `ARCH:110` placeholder row; single-home law: every fault-minting namespace maps to exactly ONE home; namespaces may share a stride only by the explicit concern-adjacency rulings below; no namespace spans two folders; the four namespace-less pages gain declarations):

| Folder | Namespace | Pages | Fault home |
|---|---|---|---|
| Numerics/ | `Rasm.Geometry` (root) | faults | band owner 2400-2449; core cluster 2400-2403 (`DegenerateInput`, `IndexMismatch`) shared by all |
| Numerics/ | `Rasm.Geometry.Numerics` | predicates (MINTED) | NONE — total pure floor, no fault routing (the recorded exception) |
| Spatial/ | `Rasm.Geometry.Spatial` | index | existing spatial cluster; kind-mismatch re-typed onto it |
| Spatial/ | `Rasm.Geometry.Naming` | naming, reconciliation (both MINTED) | 2404-2407 RENAMED `topology`→`naming` (`NameCollision` 2404, `HashMismatch` 2405) |
| Parametric/ | `Rasm.Geometry.Parametric` | nurbs, curve, surface, patternmap | `ParametricFault` 2449 (headroom spend; the `ParameterizationFault` borrow ends) |
| Parametric/ | `Rasm.Geometry.Subdivision` | subdivide | `SubdivisionFault` 2433 (widened parameterization stride) |
| Parametric/ | `Rasm.Geometry.Development` | develop | `DevelopabilityFault` 2434 (same stride) |
| Parametric/ | `Rasm.Geometry.Panelization` | panelize | `PanelizationFault` 2435 (same stride) |
| Meshing/ | `Rasm.Geometry.Meshing` | edit | routes core admission cases; mints nothing (substrate carrier) |
| Meshing/ | `Rasm.Geometry.Tessellation` | delaunay | `TessellationFault` 2448 (headroom spend — the unowned-cluster census defect closes) |
| Meshing/ | `Rasm.Geometry.Intersection` | intersect, slice | 2424-2427: `IntersectionFault` 2424 re-typed + `SliceFault` 2425 (slicing under intersection-family per `[V12]`) |
| Meshing/ | `Rasm.Geometry.Arrangement` | arrangement | 2420-2423: `DegenerateArrangement` 2420 re-typed |
| Meshing/ | `Rasm.Geometry.Offsetting` | offset, skeleton | offsetting cluster: `DegenerateOffset` re-typed, `SkeletonStalled` kept, `Skeleton3dStalled` case row |
| Meshing/ | `Rasm.Geometry.Remeshing` | remesh | `RemeshFault` 2441 (simplification stride share) |
| Processing/ | `Rasm.Geometry.Healing` | repair, receipts (receipts MINTED) | healing cluster: `UnrepairableMesh` re-typed, `NativeAssetMissing` re-typed (tier-3 scale reserve) |
| Processing/ | `Rasm.Geometry.Simplification` | decimate | 2440-2443: `DecimationFault` 2440 |
| Processing/ | `Rasm.Geometry.Parameterization` | flatten | 2432-2435: `ParameterizationFault` 2432 |
| Solving/ | `Rasm.Geometry.Constraints` | solver | constraints cluster: `OverConstrained`, `SingularSystem` |
| Solving/ | `Rasm.Geometry.Fitting` | fit | 2428-2431: `FitFault` 2428 |
| Emission/ | `Rasm.Geometry.Projection` | view | projection cluster: `ProjectionFault` re-typed |
| Emission/ | `Rasm.Geometry.Encoding` | pack | encoding cluster: `EncodingFault` re-typed |

Phantom `Rasm.Geometry.Topology` dies at all three sites in wave 1 (`pack.md:33` import, `pack.md:18` packages row, `solver.md:13` owner prose → `Rasm.Geometry.Naming`); the Fabrication `BooleanOp` ledger row (`FAB:44`) follows the `[V5]` re-home in wave 2.

Band plan ([V12]): the 12 existing clusters keep their strides; headroom 2448-2449 is spent deliberately (`TessellationFault` 2448, `ParametricFault` 2449); new concerns share concern-adjacent strides as ruled above — NO 13th cluster, NO federation escalation. The 10 stringly cases re-type to real payloads, ruled shapes: `DegenerateInput(EntityKind Kind, int Index, double Measure)` · `IndexMismatch(EntityKind Kind, int Expected, int Actual)` · `UnrepairableMesh(HealOp Op, int Iterations, int RemainingDefects)` · `NativeAssetMissing(string Rid, string Asset)` · `DegenerateOffset(int VertexCount, double EventTime)` · `DegenerateArrangement(int CellCount, int UnclassifiedFaces)` · `IntersectionFault(PrimitiveKind A, PrimitiveKind B, int Pair)` · `ParameterizationFault(ParamOp Op, int Chart, double Distortion)` · `ProjectionFault(ViewOp Op, int Edge)` · `EncodingFault(PackKind Kind, int Channel, int Slot)`. New case rows: `SliceFault(int Layer, double Elevation, int OpenChains)` · `RemeshFault(int Iteration, double TargetLength)` · `TessellationFault(int Simplex, int Constraint)` · `ParametricFault(int OpCase, double Parameter)` · `SubdivisionFault(int Scheme, int Level)` · `DevelopabilityFault(int Strip, double IsometryError)` · `PanelizationFault(int Symmetry, int Panel)` · `Skeleton3dStalled(int Iteration, double Volume)`.

One-paradigm law (corpus-wide, stated once, bound per row): every rebuilt entry carries `Op? key = null` threading per `rails.md:14,234-238`; receipts register into the `IValidityEvidence` validity fold (per-receipt hand-rolled `IsValid`/`Converged`/`Improved` guards die); input guards route the `validation.md` admission vocabulary; `GeometryFault` stays the robust-core family per the two-family seam (`ARCH:112` — `faults.md` states its side at rebuild); exception escapes and unguarded union downcasts are rail defects everywhere. Store contract (corpus-wide): SoA stores are MUTABLE single-writer build arenas publishing by freeze; immutable-record prose dies where fences mutate (`delaunay.md:5`, `arrangement.md:488`, `offset.md:5`); content-addressing binds only frozen emitted results through `reconciliation.Encode`→`identity.md`; `ParallelHelper` binds to frozen or partition-disjoint spans only.

## [03]-[PAGE_SET] — 28 rows

Schema: path | action → engine lowering | charter (folder · namespace · fault home) | entry | seams (OUT = composes, one anchor each; IN = composed-by/counterpart) | wave.

### Numerics

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 1 | `Numerics/predicates.md` | REBUILD → `rebuild` | `Rasm.Geometry.Numerics`; fault NONE (total). Preserve verbatim: four-tier direct ladder + four-way oracle + `Expansion` owner. Land: strict-IEEE-754/RID invariant on `NumericsPolicy` (EFT soundness precondition); FMA-free `TwoProduct` Dekker-split row gated on RID FMA capability (`Splitter:385` becomes load-bearing); PeterO `ERounding.Floor`/`Ceiling` directed-rounding interval-filter tier between `ddouble.Refine` and `Expansion` AND on every implicit member (`README:86` lands); probe re-founding — `Lpi`→SSI-2D + NEW LPI-3D (3 numerators + λ), `Tpi`→nine input points with Z numerator, EFT leaf differences, rounded `Denominator` dies, negative-λ flip on the orient pair; family widening ~21 members (`Orient2D` {IEE,IIE,III} + xy/yz/zx projections, `LessThanOn{X,Y,Z}` {IE,II}); 3D multi-implicit family recorded as CDTet growth; precise rejection rationale (no maintained NuGet indirect-complete predicate library) replaces `:3`. ENTRY: NONE — total static `Predicate` family (`Orient2D/3D`, `InCircle/InSphere`, implicit + projected + `LessThanOn` members), public = the package-boundary-consumable export. SEAMS OUT: none (externals: ddouble/BigRational/PeterO). IN: delaunay, intersect, arrangement, offset, decimate, flatten, view, repair; cross-package `Predicates ← Fabrication/Posting` (ARCH:89) + NEW `Predicates ← Rasm.Compute` CDTet row ([V14]). | 1 |
| 2 | `Numerics/faults.md` | REBUILD → `rebuild` | `Rasm.Geometry` root; band owner. Land: 10 stringly cases re-typed (ruled payloads in [02]); two-family seam statement (`GeometryFault` band-2400 ⟂ `Rasm.Domain` `Expected`/`Fault`, neither absorbs — ARCH:112 discharged); full band plan + cluster taxonomy = the [02] partition (topology→naming rename; new case rows homed; headroom spent); the taxonomy table IS the ratified partition's fence-side mirror. ENTRY: NONE (vocabulary — the one `GeometryFault` union + band registry). SEAMS IN: every robust-core owner routes it. | 1 |

### Spatial

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 3 | `Spatial/index.md` | REBUILD → `rebuild` | `Rasm.Geometry.Spatial`; spatial cluster. ONE excision resolves E9+E10+E11: `SpatialIndex.PointCloud` lifts OUT (case `:118`, kind `:44`, `BuildPointCloud :151-155`, `KdNearest`/`KdRange :372-381`, guards `:130,354-359,542,558,573`) onto landed `neighbors.md` (README:93 lane); `Store`/`ToAcceleration` become total over {Bvh, LinearOctree} — zero throws; the `:358` silent empty dies (kind mismatch routes `Fin` + typed fault); bounds narrowing rounds OUTWARD (min `BitDecrement`, max `BitIncrement` post-cast — `:79` round-to-nearest false-negative prune at `:453,474,518` and the `:590-591` wire casts die); the wire lands: `Wire` op case returns `Fin<SpatialWire>` over kernel-owned `(float[] Bounds, long[] Nodes)` = `NodeLinkProjection` output (ARCH:86 composed; `using Rasm.Compute.Solver` dies; Compute decodes per COMPUTE:121); GWN single-owner: index keeps the BVH-accelerated `Winding`; `reconstruct.md` GWN + `fields.md` `SignedDistanceFromMesh` compose it through the declared seam = the ONE distance-field lane (Fabrication ingress); counts resync (2 union cases, 3 kinds); `CLASH_GOLDEN` harness reproof before cited REAL. Preserve verbatim: SAH-BVH/Morton-octree/PLOC builds, four query bodies, degradation-refit. ENTRY: `Fin<SpatialAnswer> Spatial.Apply(SpatialOp op, Op? key = null)` — `SpatialOp`: `Build(SpatialKind, BuildPolicy)` · `Refit` · `Query(SpatialQuery)` · `Wire`. SEAMS OUT: faults; wire → Compute (ARCH:86/COMPUTE:121). IN: arrangement + intersect (broad-phase), decimate (Hausdorff), view (QI seed tests), reconstruct+fields (GWN, landed 1-hop ripples), Fabrication broad-phase (ARCH:84-85). fit→index DIES (rides neighbors). | 1 |
| 4 | `Spatial/naming.md` | REBUILD → `rebuild` | `Rasm.Geometry.Naming` (MINTED); naming cluster 2404-2407. Land: `RebuiltEntity` gains explicit `int Self`; `With`/`Track` key `VertexNames` by `Self` (the `:113` star-minimum mis-key + `AddOrUpdate` collision corruption die); `Subsumes` re-founded — migration predicate = shared-boundary-name overlap (parent names cover a policy-thresholded fraction of child names, direction corrected for splits; the inverted proper-subset test `:62-66` dies); Survive×Migrate injectivity ruled: a claimed survivor still seeds sibling `Parent` provenance (provenance is a projection, not a name claim; fresh names keep injectivity by construction) — `:161` residual closes; `topology`→`naming` cluster rename rides here. ENTRY: `Fin<TrackResult> Naming.Apply(NamingOp op, Op? key = null)` — `Track(prior, rebuilt)` · `Resolve(boundary)`. SEAMS OUT: faults, `identity.md` `ContentHash.Of`. IN: reconciliation (builds carriers), solver (`EntityKind` re-anchor), pack (via reconciliation Encode). | 1 |
| 5 | `Spatial/reconciliation.md` | REBUILD → `rebuild` | `Rasm.Geometry.Naming`; naming cluster. Land: `BuildEntities` emits `Self=v` (`:62`; `star` stays histogram-only); namespace + `using Rasm.Domain` (the `:101,109` unqualified `ContentHash.Of` compiles); ONE Persistence anchor — the seam row unifies to the counterpart spelling `Rasm.Persistence/ARCHITECTURE.md:55` (the `:3`/`:129`/ARCH:79 drift dies); `CANONICAL_BYTE_IDENTITY` digest flagged harness-confirmed-once + reproved (`XxHash128.HashToUInt128` over the exact 52-byte stream) before cited REAL; the `Encode` family gains the PARAMETRIC canonical-bytes case row — `EncodeForm`: `Mesh(MeshSpace)` · `Cloud` · `Parametric(NurbsCurveForm | NurbsSurfaceForm)` (degree · knots · weights · control net, hashed POST-normalization per probe G10) — a COMPONENT of the Element `ToCanonicalBytes(tolerance)` fold, never a sibling SpineRef key; kernel emits digest, Persistence mints key. Preserve: `OfMesh` canonical-order encoder, frozen field layout. ENTRY: `Fin<ReconcileResult> Reconciliation.Apply(ReconcileOp op, Op? key = null)` — `Encode(EncodeForm)` · `Reconcile(prior, rebuilt)` · `BuildEntities(MeshSpace)`. SEAMS OUT: naming, `identity.md` (ARCH:79 row composed). IN: pack, curve, surface, Persistence (`:55` one row). | 1 |

### Parametric

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 6 | `Parametric/nurbs.md` | NEW → `new` | `Rasm.Geometry.Parametric`; `ParametricFault` 2449. The vendored NURBS engine owner (hinge ruling [01] executed): validated form carriers `NurbsCurveForm`/`NurbsSurfaceForm` admitting arbitrary degree/knots/weights/net for BOTH carriers, normalized+clamped at admission (G1/G6/G10 law); rational evaluation + arbitrary-order derivatives BOTH carriers with #234 convention fixed (G2); fundamental forms + principal/Gaussian/mean curvature as kernel projections (G3); knot algebra (`Normalize`/`Refine`/clamp/multiplicities/periodic vocabulary); Bezier decomposition + Gauss-Legendre arc-length (1e-6) + per-segment inversion; Wang-2008 RMF with coincident-sample guard + closure-redistribution policy row (#373); parameterized projection (`ClosestParameter` iteration/tolerance knobs — G7); curve interpolation/approximation + Piegl-Tiller surface interpolation lane (G5); adaptive + range sampling; boundary throws → `Fin` (G9); MIT provenance notice per license obligation. ENTRY: SUBSTRATE — `Fin<NurbsCurveForm> NurbsCurveForm.Of(...)` / `Fin<NurbsSurfaceForm> NurbsSurfaceForm.Of(...)` (the `MeshSpace.Of` admission pattern) + evaluation kernel statics; op tiers live in curve/surface. SEAMS OUT: faults. IN: curve, surface; Rhino wire at control points+knots+weights (`projections.md` states the host side). | 1 |
| 7 | `Parametric/curve.md` | REBUILD → `rebuild` | `Rasm.Geometry.Parametric`; `ParametricFault` 2449 (borrow ends). Full-body: the six stubs (`:116-140`) die; STATION/FRAME family first-class — arc-length stations (`Divide`/`ParameterAtLength`/`PointAtLength` equivalents on the vendored engine) sweeping batch RMF `PerpendicularFrames` over the SpineRef `[T0,T1]` window, emitting `(station, frame)` pairs (the `PathRow`/`Placement` producer: `StationMm`/`PathAngleDegrees`/`Orientation`); `Offset` promoted to a first-class `ParametricOp` case = vendored seed + kernel deviation-refinement + self-intersection trim through the `[V4]` crossing lattice (G8); intersection-triple disposition row: SSI + surface-plane + curve-surface HOST-DEFERRED (probe G4 widening); citation re-anchor SPLIT two-target — evaluation peer → landed `projections.md`, intersection → landed `relations.md` (the four sites `:3,20,170,190`); the anomalous `using Rhino.Geometry` dies (host-neutral thesis); canonical-bytes emission through `reconciliation.Encode(Parametric ...)`; `ToMesh` trimmed-fill keeps routing arrangement `PlanarOverlay`. ENTRY: `Fin<ParametricAnswer> Parametric.Apply(ParametricOp op, Op? key = null)` — `Evaluate` · `Measure` · `Stations(window, policy)` · `Frames(RmfPolicy)` · `Divide` · `Split` · `Offset(distance, plane, policy)` · `Reconstruct` · `Intersect2D` · `Encode`. SEAMS OUT: nurbs, reconciliation, intersect (trim), arrangement (fill), projections.md + relations.md (boundary statements), faults. IN: Generation `SpineRef`/`PathRow` (spec `:138,164`); ledger gains the Parametric consumer-fan rows ([V14]). | 4 |
| 8 | `Parametric/surface.md` | NEW → `new` | `Rasm.Geometry.Parametric`; `ParametricFault` 2449. NURBS surface ops over the vendored substrate: isoline FAMILIES (fold over per-parameter iso-curve extraction + knot refinement); geodesic POLYLINES via UV pullback — composes landed `fields.md` `Geodesic` case + `geodesics.md` heat/MMP solvers through the pullback; NORMAL-OFFSET surfaces = Greville-abscissae normal offset + vendored surface-interpolation refit (G5 closed — the REAL offset, not the tessellation re-scope); curvature-domain sampling over the nurbs fundamental-form projections (G3 closed); UV-PROVENANCED tessellation sampler — kernel-owned UV-grid sampler emitting per-vertex `(u,v)` beside the `MeshSpace` (the tier-wide UV PULLBACK obligation: field results pull back to the parametric domain BY CONSTRUCTION; `ClosestParameter` only the rare foreign-point fallback); canonical-bytes via `reconciliation.Encode`. ENTRY: `Fin<SurfaceAnswer> Surface.Apply(SurfaceOp op, Op? key = null)` — `Isolines(family)` · `Geodesic(pullback)` · `NormalOffset` · `CurvatureSample` · `Tessellate(UvPolicy)` · `Project(fallback)` · `Encode`. SEAMS OUT: nurbs, fields.md, geodesics.md, mesh.md (`MeshSpace` beside UV), reconciliation, faults. IN: develop, panelize, patternmap; Generation gate (isolines/geodesics/offsets). | 4 |
| 9 | `Parametric/subdivide.md` | NEW → `new` | `Rasm.Geometry.Subdivision`; `SubdivisionFault` 2433. Catmull-Clark/Loop as STENCIL ROWS over ONE refinement fold ([GENERATOR_LAW] — the next scheme is a data row); limit-surface evaluation as limit-stencil rows; crease/boundary rules as row data; operates on the edit arena, admits/emits `MeshSpace`. OpenSubdiv recorded rejected-until-proven-demand (no managed binding; admission = P/Invoke burden class). ENTRY: `Fin<SubdivisionResult> Subdivision.Apply(SubdivideOp op, Op? key = null)` — `Refine(scheme, levels)` · `Limit(eval)`. SEAMS OUT: edit, mesh.md, faults. IN: Generation gate (region subdivision). | 4 |
| 10 | `Parametric/develop.md` | NEW → `new` | `Rasm.Geometry.Development`; `DevelopabilityFault` 2434. Guaranteed-ISOMETRIC developable-strip unroll — distinct from flatten's conformal maps AND segment.md's host LSCM capture (tier boundary stated, one anchor each): geodesic-strip decomposition (strip boundaries as geodesics), ruling-line extraction, per-strip unroll with an ISOMETRY WITNESS receipt (distortion bound as evidence fields); chart seeds from flatten's `ChartAtlas` seams. ENTRY: `Fin<DevelopResult> Develop.Apply(DevelopOp op, Op? key = null)` — `Unroll(StripPolicy)` with witness fields. SEAMS OUT: flatten (ChartAtlas), surface (UV tess), geodesics.md, faults. IN: Fabrication sheet/plywood/fabric + the `[05]`b unroll dry-run; Generation gate (developable). | 4 |
| 11 | `Parametric/panelize.md` | NEW → `new` | `Rasm.Geometry.Panelization`; `PanelizationFault` 2435. Cross-field-guided panelization: ONE mapper with `int symmetry` owning the quad/diagrid/tri family AS DATA; composes landed `fields.md` `CrossField` case + `segment.md` Knöppel globally-optimal cross-fields + stripe patterns + the remesh substrate + `sample.md` CCVT/blue-noise panel-distribution seeds; UV-provenanced surfaces in, panel graphs + panel frames out (kernel-owned SoA wire). ENTRY: `Fin<PanelResult> Panelize.Apply(PanelOp op, Op? key = null)` — `Panels(symmetry, PanelPolicy)`. SEAMS OUT: remesh, surface, fields.md, segment.md, sample.md, faults. IN: Generation gate (panelization). | 4 |
| 12 | `Parametric/patternmap.md` | NEW → `new` | `Rasm.Geometry.Parametric`; `ParametricFault` 2449. Pattern-to-surface: 2D symmetry-GROUP tiling rows over ONE tangent-log-map instancing fold (the next tiling group is a data row); each instance carries a frame parallel-transported by the landed vector-heat transport — position without orientation is half an instance; emits the Generation PATTERN/TILING instance stream with per-instance frames. ENTRY: `Fin<InstanceStream> PatternMap.Apply(PatternOp op, Op? key = null)` — `Map(group, TilePolicy)`. SEAMS OUT: surface (UV pullback), geodesics.md (log/exp maps + vector-heat), faults. IN: Generation PATTERN/TILING plane. | 4 |

### Meshing

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 13 | `Meshing/edit.md` | NEW → `new` (donor lift from `repair.md:76-102`, source page survives) | `Rasm.Geometry.Meshing`; routes core cases. The robust-core MUTABLE-ARENA tier beside landed `mesh.md`: `MeshEdit` extracted and RE-FOUNDED as a true single-writer SoA arena (the immutable-copy-per-op `Arr<T>` shape dies — O(V+F)-per-op copying was the repair-era artifact), publishing by FREEZE into `MeshSpace`; the TWO-CARRIER seam stated (`MeshSpace` = immutable admission snapshot, `MeshEdit` = predicate-gated build arena; no third carrier); the WELD PRIMITIVE sites here (`Kernels.WeldDuplicates` + weld-tolerance policy — dedup-on-the-working-set is arena capability; the arrangement→repair forward edge dies); the soup adapter ×3 collapses here (`intersect:326` `Soup(MeshSpace)`, `view:386`, `arrangement:352-365` → ONE substrate extraction, operand `DuplicateNative` triplication collapsed); the corpus-wide store-mutability + arena-concurrency contract is LEGISLATED here (single-writer, freeze-publish, `LaplacianCache` the exemplar); `CommunityToolkit.HighPerformance` admission RULED IN here — `Span2D`/`MemoryOwner<T>` into arenas, `ParallelHelper` into frozen/partition-disjoint parallel folds only. ENTRY: SUBSTRATE — `Fin<MeshEdit> MeshEdit.Of(MeshSpace | soup, Op? key = null)` + arena ops (`Weld` · `Spawn` · `Kill` · `Freeze → Fin<MeshSpace>`). SEAMS OUT: mesh.md (two-carrier), validation.md, faults. IN: intersect, arrangement, view, decimate, receipts, repair, remesh, subdivide, slice, skeleton. | 1 |
| 14 | `Meshing/delaunay.md` | REBUILD → `rebuild` | `Rasm.Geometry.Tessellation`; `TessellationFault` 2448. The hardest transcription: the seven `[STORE_OPS]` stubs (`:192-198` `Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces`) fenced transcription-complete; vertex payload widens to the two-kind row store `Explicit(Point3d)` | `Crossing(defining ids)` under the depth-1 law (probe gap 1); `TessellationPolicy` gains CONSTRAINED-ONLY mode for implicit-bearing builds (probe gap 4 minimal; `InCircle` multi-implicit = recorded growth row); the VORONOI-DUAL accessor lands as a `Tessellation` projection — exact DT adjacency + circumcenters materialized at the emission seam (probe: decoupled, all-explicit; README:99 lands); the EXACT HULL projection lands as the lifted-paraboloid lower-hull `Tessellation` projection ([V11] exact tier; boundary vs landed `cloud.md` rail stated both pages); `SimplexStore` = explicit mutable arena (`:5` prose dies); honest rejection rationale (`:3` → Triangle.NET is MIT; the load-bearing bar is float-epsilon + no exact CDT/CDTet; TetGen AGPL-rejected); Morton insertion order preserved. ENTRY: `Fin<TessellationAnswer> Tessellation.Apply(TessellationOp op, Op? key = null)` — `Build(points, constraints, policy)` · `Insert` · `Recover` · `Project(Mesh · VoronoiDual · LowerHull)`. SEAMS OUT: predicates (incl. implicit family), edit, mesh.md, faults. IN: arrangement (`Build`), offset (dual), repair (CDT re-mesh), Fabrication stock-envelope hull (via [V11] ruling). | 2 |
| 15 | `Meshing/intersect.md` | REBUILD → `rebuild` | `Rasm.Geometry.Intersection`; `IntersectionFault` 2424. The CHAIN becomes real (the lane's top defect): assembly keys crossing endpoints by DEFINING-ENTITY ids (probe gap 5 — the cross-face merge key `Section` never recorded) with exact `LessThanOn` ordering; links ACROSS segments; closed loops close, open curves run end-to-end; emits ORIENTED loops (outer CCW/holes CW) + typed OPEN-chain rows (`IntersectResult` widens per [V10]b — the "Loops"-labelled disconnected 2-point segments die); the dead `Next` column DELETES (entity-id merge table owns chaining); `Crossing` carries defining-entity ids (the re-founded `Lpi`/LPI-3D payload — the `Denominator`-vs-`Lambda` dual-field risk dies with the predicates re-founding); the E7 collapse LANDS by consumers (arrangement/offset/repair route `Intersection.Apply`/`Crossing` — no fourth kernel); `SegmentSegment`/`LineLine` typed 2D restriction (`Intersect2D` case; the silent Z=0 dies); the three `QueryResult` hard casts (`:222,233,241`) route the `Fin` rail; both `Analysis/Intersect.cs` cites (`:5`,`:388`) re-anchor to landed `relations.md` (altitude boundary stated both directions); soup via edit; slice-stack producer seam declared; the `contract` label becomes true with the bodies. ENTRY: `Fin<IntersectResult> Intersection.Apply(IntersectOp op, Op? key = null)` — six cases + `Intersect2D`. SEAMS OUT: predicates, index (broad-phase), edit, relations.md, faults. IN: arrangement, offset, repair, slice, view, curve. | 2 |
| 16 | `Meshing/arrangement.md` | REBUILD → `rebuild` | `Rasm.Geometry.Arrangement`; `DegenerateArrangement` 2420. `BooleanOp` HOMES here ([V5]; the smart enum + regularized keep-predicate; ARCH:42 sited); `BooleanReceipt` collision resolves to ONE type — arrangement owns it, receipts COMPOSES it as the boolean case payload; the implicit-point `Constraint` case LANDS (probe gap 5: `ImplicitCrossing`+`PlaneCrossPoint` die; operand crossings ride defining-entity `Crossing` into `Tessellation.Build`; interning keys on defining entities; `.Round()` at emission only; README:42 lands); Manifold tier-3 gate NAMED — `elalish/manifold` via thin in-house `manifoldc` P/Invoke boundary adapter fenced HERE, finite `ScaleCeiling` (the `int.MaxValue` dead code dies), golden-boolean fixture law gates activation, `NativeAssetMissing` reserved for the scale route, the `Manifold` NuGet-ID homonym recorded REJECTED; "retires the tier-3 native CSG gate" re-scopes to: managed exact arrangement = correctness owner, Manifold = scale companion; weld + soup consumption re-anchors to edit (wave 1 — the repair edge dies); store honesty (`:488` dies); `SegmentOf` keeps interior coplanar crossings (middle-crossing drop dies); exactness contract stated. ENTRY: `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp op, Op? key = null)` — `MeshBoolean(BooleanOp)` · `PlanarOverlay` · `CellComplex`. SEAMS OUT: delaunay, intersect, index (`Winding`/`Overlap`), edit, manifoldc adapter, faults. IN: repair (boolean heal, backward edge), receipts (payload), view + curve (`PlanarOverlay` fill), offset (`PlanarOverlayResolve`), FAB:44 (re-anchored counterpart). | 2 |
| 17 | `Meshing/offset.md` | REBUILD → `rebuild` | `Rasm.Geometry.Offsetting`; `DegenerateOffset` + `SkeletonStalled`. Medial made REAL: `MedialFrom` reconciles against the delaunay Voronoi-dual accessor (the discarded `.Map(_ => ...)` build dies; `:16,478` become true; parabolic-arc refinement at reflex vertices = a policy row over the dual reconciliation); per-point CLEARANCE-RADIUS carried as first-class result fields + clearance query at ARBITRARY probe point (the [V10] mandate — a radius-less medial is a defect); `JoinType`(Miter/Round/Bevel/Square)×`EndType` corner rows over ONE offset assembly on the exact wavefront ([V10]a; CavalierContours stays the Fabrication kerf lane on top); `MinkowskiConvolution` COMPLETES — convex-vertex arc-fans + the symmetric edge×vertex direction (capability never dropped); `PlanarOverlayResolve` fixed — self-overlapping boundaries feed arrangement as segment constraints through `Tessellation`, never `Mesh.CreateFromClosedPolyline` (which nulls on exactly the self-intersecting input); `SegmentsCross` (`:440-444`) dies → `Intersection.Apply`; `WavefrontStore` = mutable arena with geometric-growth sizing (the 2n under-allocation `:195` dies); `:5` immutable prose dies. ENTRY: `Fin<OffsetResult> Offsetting.Apply(OffsetOp op, Op? key = null)` — `Skeleton(weighted rows)` · `Medial(clearance)` · `Offset(JoinType, EndType, distance)` · `Minkowski` · `Clearance(probe)`. SEAMS OUT: delaunay (dual), intersect, arrangement, predicates, faults. IN: skeleton (clearance vocabulary), FAB:22 (2D half — `Offsetting.Apply` + clearance field). | 2 |
| 18 | `Meshing/slice.md` | NEW → `new` | `Rasm.Geometry.Intersection`; `SliceFault` 2425. [V10]b slice-stack owner: ONE section fold composing `IntersectOp.PlaneMesh` over a parallel-plane family → per-layer contour-nesting trees; layer-height POLICY ROWS (uniform · adaptive seed rows; variable-by-slope + support-interface recorded growth rows for Fabrication [V6]); ORIENTED contours (outer CCW/holes CW — exact signs over implicit crossings per probe V10b's cheapest sub-family) + typed OPEN-chain rows for non-watertight sections (never silent closure); nesting forest computed in-kernel via the QuikGraph bounded lane, EMITTED as kernel-owned SoA wire (layers + contours + nesting + open chains + elevations). ENTRY: `Fin<SliceStack> Slicing.Apply(SliceOp op, Op? key = null)` — `Stack(mesh, direction, LayerPolicy)` · `Nest`. SEAMS OUT: intersect (fold + widened result), QuikGraph (in-kernel), faults. IN: Fabrication `Toolpath/slicing` (FAB:48 promise resolves), Compute circulation (COMPUTE [V12]a story-elevation contours, wire-decoded). | 4 |
| 19 | `Meshing/skeleton.md` | NEW → `new` | `Rasm.Geometry.Offsetting`; `Skeleton3dStalled` row. [V10]d 3D medial/curve-skeleton: author-kernel mean-curvature-flow skeletonization composing LANDED machinery — `geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow` case (no permissive engine exists; CGAL GPL rejected); per-point clearance radius + arbitrary-probe clearance query as first-class result fields (shared clearance vocabulary composed from offset.md — one result family across 2D/3D); skeleton topology via the QuikGraph bounded lane → kernel-owned SoA wire. ENTRY: `Fin<SkeletonResult> Skeleton.Apply(SkeletonOp op, Op? key = null)` — `Mcf(SkeletonPolicy)` · `Clearance(probe)`. SEAMS OUT: geodesics.md, fields.md, offset (clearance vocabulary), edit, QuikGraph, faults. IN: FAB:22 (3D half — `Toolpath/Skeleton.cs` dies unconditionally). | 4 |
| 20 | `Meshing/remesh.md` | NEW → `new` | `Rasm.Geometry.Remeshing`; `RemeshFault` 2441. [V11] remesh substrate: Botsch-Kobbelt ISOTROPIC remesh (split/collapse/flip/tangential-relax, genus-preserving) over the edit arena — NON-DEFERRABLE (panelize + Fabrication mesh-prep + Compute [V7] boundary-conditioning gate on it); QUAD remesh LANDS as the stripes-guided extraction ROW on the same fold (roster carries it: landed `segment.md` Knöppel cross-fields + stripes ARE the guidance; deferral criterion moot); tier boundary vs segment.md's landed HOST-capture tier (QuadRemesh/Reduce) stated both pages, one anchor each; `decimate.md` `VoxelRemesh` stays a distinct row there. ENTRY: `Fin<RemeshResult> Remesh.Apply(RemeshOp op, Op? key = null)` — `Isotropic(policy)` · `QuadFromStripes(guidance)`. SEAMS OUT: edit, segment.md, mesh.md, faults. IN: panelize; Compute [V7] (recorded growth, wire-decoded); Fabrication mesh-prep. | 4 |

### Processing

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 21 | `Processing/repair.md` | REBUILD (W3) → `rebuild`, plus TWO surgical pre-motions: W1 `improve` (MeshEdit donor block `:76-102` excised to edit.md; imports re-anchor), W2 `improve` (`HealOp.Boolean` vocabulary + `BooleanArrangement :288-292` excised to arrangement) | `Rasm.Geometry.Healing`; healing cluster. W3 rebuild: TOTAL topology status — the heal rail composes ONE `TopologyReceipt.Project` `ProjectionRow` EXTENSION landed on `mesh.md:196-206` through its Growth law (`(int Euler, Option<int> Genus, int BoundaryComponents, bool Manifold, bool Oriented)` un-gated row — the Genus=None partiality that fails the healer on exactly its defective-mesh input class dies; a `[LANDED_KERNEL_LAW]` (c) bounded extension, 1-hop ripple this leg); `SelfIntersectResolve` becomes a REAL CDT re-mesh through the substrate — segment-not-point tri-tri crossings, BOTH faces re-meshed, multi-crossings kept (the `patched.ContainsKey` drop dies) — composing intersect `Crossing` + delaunay CDT + the edit arena; topology threads forward (`before[n] = after[n-1]`, the 2N recompute dies); `((QueryResult.Hits)hits)` (`:243`) routes the `Fin` rail; the crossing kernels (`:346-372`) die → `Intersection.Apply` ([V4] fourth copy); `GapClose` pairs on DIRECTED boundary edges (the index-sorted orientation loss dies); host boundary honesty: composes only `RebuildNormals` at the working-set boundary. Preserve: `HealOp` union + `Standard` order, `DegenerateCollapse`/`DominantAxis` sliver logic, `WeldDuplicates` idempotence law (now composed from edit). ENTRY: `Fin<HealSession> Heal.Apply(HealPlan plan, Op? key = null)` over the `HealOp` union (Boolean case composes arrangement). SEAMS OUT: edit, intersect, delaunay, arrangement, mesh.md (ProjectionRow), intent.md, receipts, validation.md, faults. IN: receipts; FAB:44 re-anchors AWAY (to arrangement). | 3 |
| 22 | `Processing/receipts.md` | REBUILD → `rebuild` | `Rasm.Geometry.Healing` (namespace MINTED — one of the four namespace-less pages). `Converged` inversion dies TWICE over: the boolean case tests topological SUCCESS from the composed arrangement receipt (manifold witness), never the tier-3 gate flag (`:65,99`); the standalone hand-rolled `Converged`/`Improved` guards die — receipts REGISTER into the landed `IValidityEvidence` validity fold; the duplicate `BooleanReceipt` shape (`:44`) dies — the boolean case CARRIES arrangement's `BooleanReceipt` as payload; `manifoldRepair` `Improved` heuristic corrected (edge-splitting INCREASES boundary components — the `<` test dies); before/after status rides the Genus-tolerant total topology seam. Preserve: the 7-case `RebuildReceipt` union, `Of` mint discrimination, `Affected` re-anchor seed, `ToLog` fold. ENTRY: VOCABULARY — `RebuildReceipt` union + `Of` mint; registers `IValidityEvidence`. SEAMS OUT: arrangement (payload), mesh.md/intent.md (status), validation.md, faults. IN: repair. | 3 |
| 23 | `Processing/decimate.md` | REBUILD → `rebuild` | `Rasm.Geometry.Simplification`; `DecimationFault` 2440. The vacuous flip guard dies — `Orient3D(face, face+normal)` ≡ Positive; the REAL guard tests the moved apex against the ORIGINAL supporting plane via exact `Orient3D` (the predicate-gated headline becomes true); boundary link condition admits `shared == 1` (open meshes decimate; the spurious fault dies); the vertex-vertex `Adjacency` extends to a vertex→incident-face incidence index feeding `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` (the O(F²) full-array scans die → O(F log F) drain); the PHANTOM `field.IsoSurfaceDetailed` dies — `Voxelize` composes the real static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key)` (`reconstruct.md:609`); the second hard cast (`:459` `(QueryResult.Nearest)`) routes the `Fin` rail; `MeshEdit` from edit; meshoptimizer/Compute stratum split recorded. Preserve: `ddouble` quadric fold (README:86 row already true here), lazy version-skip queue, `matrix.md`-routed `OptimalPosition` (the [V7] exemplar), BVH Hausdorff bound. ENTRY: `Fin<DecimateResult> Simplify.Apply(SimplifyOp op, Op? key = null)`. SEAMS OUT: edit, predicates, matrix.md, reconstruct.md, index (Hausdorff), faults. IN: none robust-core (Compute split recorded). | 3 |
| 24 | `Processing/flatten.md` | REBUILD → `rebuild` | `Rasm.Geometry.Parameterization`; `ParameterizationFault` 2432. Dirichlet corrected via the ELIMINATE-BOUNDARY-ROWS formulation (the 1e-9-on-every-diagonal + value·1e9 rhs corruption dies; numerically clean, no penalty conditioning); LSCM assembled SPARSE — conformal-energy least-squares smallest-eigenpair through the landed `matrix.md` sparse owners (the dense `(2n)²` + `DecomposeSvd` O(n³) dies; `:3` becomes true); ARAP global assembly O(F) — the in-scope face index `f` is passed (`IndexOf` O(F) scan + return-0-on-miss dies); `FactorNonZeros` reports the actual Cholesky factor fill via the `CholeskySparse` rail (the `D1.NonZeros` wrong-operator dies; CSparse `Refactorize`/`Update` amortization noted for the factor cache); dead `ChartStore.Dead`/`FreeList`/`Kill` DELETE (islands stay BFS); `OrderLoop` hardened — directed boundary-edge traversal (the min-keyed `next` overwrite dies); `DiscreteCalculus` sparse shape verified against landed `spectral.md:191`; the dense-LA framing unifies to the `matrix.md` owner law (one vocabulary); the developable seam to `develop.md` declared. Preserve: `MeshAdjointSnapshot`/DEC composition, per-face Jacobian-SVD distortion scoring, exact `Orient2D` flip guard, seam-cut island walk. ENTRY: `Fin<FlattenResult> Flatten.Apply(ParamOp op, Op? key = null)` — `Harmonic` · `Lscm` · `Bff` · `Arap` · `Atlas`. SEAMS OUT: matrix.md, dec.md/spectral.md, mesh.md, predicates, faults. IN: develop (ChartAtlas); flatten→Fabrication/AppUi ledger rows stay composed. | 3 |

### Solving (minted W3)

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 25 | `Processing/solver.md` → `Solving/solver.md` | MOVE+REBUILD → `new`@`Solving/solver.md` + delete-with-absorb {into: `Solving/solver.md`, from: `Processing/solver.md`} | `Rasm.Geometry.Constraints`; `OverConstrained`/`SingularSystem`. The ONE damped-Gauss-Newton owner GENERALIZED: `Iterate`/`Step` become a residual+Jacobian FUNCTOR engine over the existing `SolvePolicy` λ-ladder (`LambdaUp`/`LambdaDown`/`StepFloor`/`InitialLambda` — the policy record already exists; only the residual source abstracts); the functor surface is public for fit's instantiation; the `ddouble` Σd² objective accumulation lands (README:86, narrowing at readout); the phantom `Rasm.Geometry.Topology` owner prose (`:13`) dies → `EntityKind` homes in `Rasm.Geometry.Naming` (W1 surgical pre-kill); dense-LA through landed `matrix.md` only. Preserve: `LambdaCeiling` singular guard, `SolveReceipt` all-finite gate, `WitnessAnalyze` DOF refinement, bounded reject chain, the deliberately name-distinct `ConstraintSolveReceipt`. ENTRY: `Fin<Solution> ConstraintSolver.Apply(SolveOp op, Op? key = null)` + the public residual+Jacobian functor row. SEAMS OUT: matrix.md, naming (`EntityKind`), validation.md, faults. IN: fit; COMPUTE:120 spurious reverse edge DIES (counterpart edit this wave). | 3 |
| 26 | `Processing/fit.md` → `Solving/fit.md` | MOVE+REBUILD → `new`@`Solving/fit.md` + delete-with-absorb {into: `Solving/fit.md`, from: `Processing/fit.md`} | `Rasm.Geometry.Fitting`; `FitFault` 2428. `Refine` COMPOSES the solver functor (the `:560-627` LM re-implementation + hardcoded `1e-3`/`×10.0`/`1e12` ladder die); λ THREADS through `RefineState` on the policy ladder (the reset-every-outer-iteration dies); dead `FitStore` (`:246-255`) DELETES (`Candidate` is the live carrier; prose/density-bar resync); the unqueried Consensus BVH dies — MLESAC seeded draw/range prefilter rides the landed `neighbors.md` kd-tree lane (README:93; landed `register.md` the exemplar); `Line` chart de-degenerates (foot-of-perpendicular anchor — the frozen seed-Z dies; `MinimalSamples` 3→2 resyncs with the 2-point `MinimalLine`); cloud-PCA vocabulary composes landed `cloud.md` `CloudKernel` + `stats.md` `SampleMoment` (consumer, never a second covariance owner); `ddouble` Σd²; the `:18` transcription artifact repaired. Preserve: MLESAC/PROSAC truncated-cost sampler, adaptive budget, six closed-form minimal solvers, analytic per-kind `Distance`/`Gradient` arms. ENTRY: `Fin<FitResult> Fit.Apply(FitOp op, Op? key = null)`. SEAMS OUT: solver (functor), neighbors.md, cloud.md, stats.md, matrix.md, faults. IN: `Rasm.Bim` reconstruction (`FitReceipt`, declared consumer row). | 3 |

### Emission (renamed from Drawing, W4)

| # | Page | Action → lowering | Charter · entry · seams | W |
|---|---|---|---|---|
| 27 | `Drawing/view.md` → `Emission/view.md` | MOVE+REBUILD ground-up → `new`@`Emission/view.md` + delete-with-absorb {into: `Emission/view.md`, from: `Drawing/view.md`} | `Rasm.Geometry.Projection`; `ProjectionFault`. ENGINE RULED: exact analytic APPEL QUANTITATIVE-INVISIBILITY — invisibility counts updated ONLY at silhouette crossings computed through the crossing lattice, zero sampling (roster-mining ground: it composes the rebuilt exact floor — silhouette locus `FacesOppose`/`Orient3D` + intersect crossings + index seed ray-tests — where the BSP alternative demands a second arrangement-class face-splitting machine). The ENTIRE dead BSP apparatus dies (`BspNode`/`Partition`/`Split`/`Spawn`/`Kill`/`FreeList`, the phantom `PaintBackToFront` prose, the dead `EmitsHidden`/`NeedsBsp` columns — `EmitsHidden` returns as a CONSULTED op-case data column); `ToPolylines` chains by CONNECTIVITY (defining-entity successor walk riding the rebuilt chain machinery; visible and hidden never merge; the kind-key concat garbage dies); the `MeshSpace`→soup→native→`MeshSpace` round-trip collapses onto edit; Section delegation to `IntersectOp.PlaneMesh` preserved; polygon fill composes arrangement `PlanarOverlay` (README:99 row composed); the select boundary stated view-side (`select.md:593` reciprocated: host-native capture tier ⟂ predicate-exact robust tier); README:65 re-scopes to the honest engine wording in the same motion. ENTRY: `Fin<DrawingProjection> View.Apply(ViewOp op, Op? key = null)` — `HiddenLine` · `Silhouette` · `Section`. SEAMS OUT: predicates, intersect, index, arrangement, edit, select.md (boundary), faults. IN: Fabrication (FAB:33 `Posting/Projection.cs` DIES for kernel `DrawingProjection`; FAB:46 "beside" clause dies; FAB:47 `HiddenLineResult` = thin projection), AppUi (ARCH:91). | 4 |
| 28 | `Drawing/pack.md` → `Emission/pack.md` | MOVE+REBUILD → `new`@`Emission/pack.md` + delete-with-absorb {into: `Emission/pack.md`, from: `Drawing/pack.md`} | `Rasm.Geometry.Encoding`; `EncodingFault`. Dtype-strided residency MADE REAL (ruled default executed — the dossier verified the mechanism): `EncodedStore.Payload` becomes dtype-strided BYTE storage with per-channel descriptors; the `System.Numerics.Tensors` seam survives as descriptor-DISPATCHED typed views (`ReadOnlyTensorSpan<float>`/`ReadOnlyTensorSpan<Half>` selected on the descriptor `Dtype` row — element-type-generic span verified `api-tensors:14,29,175-176`; the false "`Half` halves residency" over `float[]` dies); the phantom `Rasm.Geometry.Topology` import + `:18` packages row die → `Rasm.Geometry.Naming` (W1 surgical pre-kill); the `:3` `OrientedNormals` provenance corrects to cloud→neighbors (fit CONSUMES, never owns); PRESERVED VERBATIM: the `ScalarField.SampleDetailed` composition (`:346,463` onto `fields.md:367`) and the `VectorCloudMetric.OrientedNormals` composition; `PackKind` gains the `field` · `toolpath` DATA rows with active-channel sets (Compute decodes as the residency wire case axis, Compute ARCH:126; AppHost signature-locks `EncodingKind.Field`/`.Toolpath`, `Sandbox/solver.md:54-55`); ledger rows ARCH:92-93 stay composed; span-feed law binds (SoA buffers feed operators directly, no tensor re-pack). ENTRY: `Fin<EncodedGeometry> Encode.Apply(PackOp op, Op? key = null)` — `PackKind` rows (mesh · cloud · existing ×2 · field · toolpath). SEAMS OUT: reconciliation (`Encode`), identity.md, fields.md (preserved), cloud.md/neighbors.md (preserved), faults. IN: AppHost (ARCH:92), Compute (ARCH:93). | 4 |

Engine-lowering totals: `rebuild` ×14 (rows 1-5, 7, 14-17, 21-24) · `new` ×14 (rows 6, 8-13, 18-20 + the four move destinations 25-28) · `deletePages` ×4 with absorb pairs (`Processing/solver.md`, `Processing/fit.md`, `Drawing/view.md`, `Drawing/pack.md`) · surgical `improve` pre-motions ×4 (repair W1 + W2; pack + solver W1 phantom kills, folded into their later main motions).

## [04]-[SEAM_LEDGER] — corrected, both directions, one anchor per seam

In-kernel composition spine (arrow = composes; every edge equal-or-earlier wave; intra-wave edges point backward in [03] listed order): predicates ← {delaunay, intersect, arrangement, offset, decimate, flatten, view, repair}; faults ← all; edit ← {intersect, arrangement, view, decimate, receipts, repair, remesh, subdivide, slice, skeleton}; index ← {arrangement, intersect, decimate, view}; naming ← {reconciliation, solver, pack-via-Encode}; reconciliation ← {pack, curve, surface}; nurbs ← {curve, surface}; delaunay ← {arrangement, offset, repair}; intersect ← {arrangement, offset, repair, slice, view, curve}; arrangement ← {repair, receipts, view, curve, offset}; offset ← {skeleton}; solver ← {fit}; flatten ← {develop}; remesh ← {panelize}; surface ← {develop, panelize, patternmap}.

Landed-plane seams (ambient floors + declared extensions): `matrix.md` ← solver/fit/flatten/decimate (the ONE LA boundary); `neighbors.md` ← fit (MLESAC) + the index PointCloud boundary statement; `mesh.md` ← edit (two-carrier seam) + delaunay/arrangement/repair (`MeshSpace.Of`) + the ONE `TopologyReceipt.Project` `ProjectionRow` Growth extension (repair leg, [LANDED_KERNEL_LAW] (c)); `intent.md` ← repair/receipts (`Topology` projection `intent.md:214`); `fields.md` ← surface (`Geodesic`), panelize (`CrossField`), skeleton (`MeanCurvatureFlow`), pack (`SampleDetailed` — preserved); `fields.md`+`reconstruct.md` → index (GWN seam: `SignedDistanceFromMesh` → reconstruct GWN → index BVH = the ONE distance-field lane; landed 1-hop ripples, wave 1); `reconstruct.md` ← decimate (`IsoSurface.Detailed`); `geodesics.md` ← surface/patternmap/skeleton/develop; `segment.md` ← panelize/remesh (capture-tier boundary stated both pages); `sample.md` ← panelize; `identity.md` ← reconciliation (ARCH:79)/naming/pack; `cloud.md` ← fit (PCA consumer) + the [V11] hull tier boundary vs delaunay `LowerHull` (stated both pages); `stats.md` ← fit; `relations.md` ← curve+intersect (altitude boundary, both directions); `projections.md` ← curve (evaluation boundary — the dossier's two-target re-anchor split); `locate.md` ⟂ curve (no second location algebra — boundary row); `select.md` ⟂ view (capture law, both sides); `spectral.md` ← flatten (sparse-shape verification row); `rails.md`/`validation.md` ← all (one-paradigm motion).

Cross-package counterpart ledger edits (1-hop, land with the leg that changes the seam): W1 — COMPUTE:121 (index wire direction: Compute decodes kernel node arrays), `Rasm.Persistence/ARCHITECTURE.md:55` (the ONE reconciliation anchor), Compute CDTet → predicates NEW row ([V14]). W2 — FAB:44 (`BooleanOp` anchor re-homes repair→arrangement). W3 — COMPUTE:120 (spurious "Geometry solver consumes Compute LM" DIES; solver path updates to `Solving/`). W4 — FAB:22 (skeleton/medial → `Offsetting.Apply` + skeleton.md, clearance-radius payload), FAB:23 (`Slicing.cs` dies → slice.md), FAB:33 (`Posting/Projection.cs` BSP HLR dies → `DrawingProjection`), FAB:46 ("beside the in-folder BSP" clause dies), FAB:47 (`HiddenLineResult` = thin projection), FAB:48 (slicing re-route promise resolves), Compute ARCH:126 + AppHost `Sandbox/solver.md:54-55` (pack `field`/`toolpath` rows verified against counterpart spellings), the Parametric consumer fan enters `[02]-[SEAMS]` as rows (curve.md:192 prose dies). Index-doc rows re-scope with their fences: README:42+ARCH:42 (W2, claims become TRUE + the native-gate re-wording), README:99 (W2 dual + fill land), README:65 (W4 re-words to the honest QI engine; ARCH:63 already accurate — E16 pairing was drift), README:86 (W1 predicates filter + W3 ddouble rows land), README:93 (W3 fit lane lands), ARCH:112 (W1 faults statement lands).

## [05]-[CONSUMER_GATE_HOMING] — both gates fully homed

Generation KERNEL GATE (`RASM-GENERATION-SPEC.md:179,226,164,138,173`): SpineRef content-key resolution → reconciliation `Encode(Parametric)` + curve/surface canonical-bytes emission (component of `ToCanonicalBytes`; Persistence mints the key) | arc-length stations + RMF frames over `[T0,T1]` → curve.md `Stations`/`Frames` over nurbs.md | surface isolines → surface.md `Isolines` | surface geodesics → surface.md `Geodesic` (UV pullback) | curve offset → curve.md `Offset`; surface normal-offset → surface.md `NormalOffset` | region subdivision → subdivide.md | developable → develop.md (isometry witness) | panelization → panelize.md | pattern-to-surface → patternmap.md (per-instance frames). Fabrication toolpath gate: corner-strategy offsets → offset.md `JoinType`×`EndType` rows | slice stacks (oriented + nesting + open chains) → slice.md | 2D medial + clearance radius → offset.md `Medial`/`Clearance`; 3D curve-skeleton + clearance → skeleton.md; arbitrary-probe clearance query → both (one clearance vocabulary) | manufacturing-tolerance booleans → arrangement.md (+ Manifold scale gate) | developable/unroll → develop.md | stock-envelope hull → delaunay `LowerHull` projection + landed cloud.md rail ([V11] two-tier ruling) | distance field → the [V8] ONE lane (fields → reconstruct GWN → index BVH) | plan/section HLR → view.md `DrawingProjection` (zero sampling misses by construction — analytic QI). All four `[05]` acceptance dry-runs compose from these owners plus landed fences; none reaches for a missing owner.

## [06]-[ROSTER_DELTAS] — every [04] row disposed, with .api obligations (wave-1 motion unless noted)

| Roster row | Disposition |
|---|---|
| GShark 2.3.1 | VENDOR-AND-OWN NOW ([01] ruling): pin dies (`PROPS:73`, `Rasm.csproj:18`); kernel `api-gshark.md` DELETES (probe's six catalog corrections absorbed into nurbs.md as member truth); `Parametric/nurbs.md` owns the algorithm set; MIT notice per license obligation; no second NURBS engine exists anywhere |
| MathNet.Numerics stack | DOWNGRADE to 5.0.0 stable — feed-verified feasible for the WHOLE stack (core + Providers.OpenBLAS + Providers.MKL all have 5.0.0, 2022-04-03); the sweep covers all FOUR beta pins (`PROPS:62-64` + `MathNet.Numerics.FSharp` `PROPS:467` — the register's completeness gap); beta survives only on a recorded 6.0-only fence member; kernel overlay catalogs re-verify as subsets against the 5.0.0 surface after the downgrade |
| MathNet.Numerics.Providers.OpenBLAS | DROP the kernel reference (`Rasm.csproj:11`) + kernel-tier `api-mathnet-providers.md` overlay — evidence-backed decorative capability (lib/-only package, zero arm64 assets in the global cache, catalog self-admission `:12,:167`); the central pin drops if the wave-1 repo-wide reference scan finds no other consumer; landed `matrix.md` provider rows re-scope to the managed-path law (1-hop ripple) |
| Supercluster.KDTree.Net | KEEP — landed neighbors.md owner; the [V8] PointCloud lift RIDES it (index excision, no re-own) |
| TYoshimura.DoubleDouble | KEEP — predicate tier + the QEM quadric fold (decimate, already true) + LM Σd² (solver, W3); ~200-member special-function surface stays a recorded no-consumer reserve |
| ExtendedNumerics.BigRational | KEEP — `Fraction` oracle + exact ordering; `Mediant` stays recorded reserve |
| PeterO.Numbers | KEEP + MINE: the directed-rounding `ERounding.Floor`/`Ceiling` interval filter lands as a predicates ladder tier AND the implicit-member filter tier ([01] — load-bearing, not optional) |
| CSparse | KEEP — through landed `matrix.md` `CholeskySparse` only; `Refactorize`/`Update`/`Downdate` noted for flatten's factor cache; LGPL-2.1 recorded; catalog DEDUPE to shared tier (kernel copy deletes — byte-identical, multi-stratum) |
| MIConvexHull | KEEP — consumer-backed cloud.md rail; [V11] two-tier ruling declares the exact-tier boundary (delaunay `LowerHull`); removal DEAD |
| QuikGraph | KEEP — the bounded-lane law GENERALIZES: in-kernel combinatorial computation only (slice nesting forests, skeleton topology, naming migration structure), results emit as kernel-owned SoA wires; never the spectral path; shared catalog `:11` downstream-view verified against the landed admission |
| Manifold (ADD) | ADMIT — `elalish/manifold` via thin in-house `manifoldc` P/Invoke (boundary adapter fenced in arrangement.md); RID assets recorded at admission; `api-manifold.md` AUTHORED kernel-tier spelling `manifoldc`; the `Manifold` NuGet-ID homonym (CLI/MCP source-gen framework, 2026-03) NAMED-AND-REJECTED beside SISL/TetGen/etc.; golden-boolean fixture gates activation; finite `ScaleCeiling` W2 |
| CommunityToolkit.HighPerformance | ADMIT with [V6] — kernel reference added; `Span2D`/`MemoryOwner<T>` into the edit/SoA arenas, `ParallelHelper` into frozen/partition-disjoint parallel BVH/QEM/MLESAC folds only (arena-concurrency law); the W1 leg deepens the stub on first composition |
| System.Numerics.Tensors | KEEP — span-feed law stands (`api-tensors:248`); descriptor-dispatched `ReadOnlyTensorSpan<float>/<Half>` views land in pack (W4); non-exact paths only; catalog RECLASSIFIED overlay pair — NOT deduped (kernel additive superset with `IsFiniteAll` validity rows is load-bearing; E13 correction) |
| System.IO.Hashing | KEEP — seed-zero `XxHash128` through landed `identity.md` ONLY; shared `api-hashing.md:82` falsehood CORRECTED (`XxHash128.HashToUInt128` seed-zero → `UInt128`; `XxHash3` may appear only as a generic fast-root example) |
| LanguageExt catalog | DISCHARGED on disk (`api-languageext.md`, 170 LOC shared-tier, the +1 that makes 31) — VERIFY, never re-author (E13 refuted) |
| api-rhino.md:134 | REFUTED on disk (blank line; correction already applied in the 2h edit wave) — verify only |
| Fabrication-lane pins | RE-GROUP: `CavalierContours:70`/`Clipper2:71`/`SharpVoronoiLib:75` move OUT of the mis-labelled `Kernel Geometry` group (`PROPS:69`) into a `Fabrication` label; `geometry3Sharp:72` disposition routes to its consuming campaigns; `LibTessDotNet` stays retired (no pin, no catalog); Fabrication `.api` copies are the folder-tier catalogs |
| WATCH card | RE-KEY: geometry4Sharp "on first NuGet release" is DEAD (1.0.0 on NuGet 2022-11); re-keys to a post-2022 release signal; `CSparse.Extensions` unpublished, TVGL 2019-stale, `Kemsekov.GraphSharp` 3.1.2 recorded |
| Judged shared-tier stubs | RECORDED no-mine: Generator.Equals (Thinktecture owns kernel identity), Riok.Mapperly (wires are columnar span transcription), UnitsNet (radian `double` in-kernel; `Angle` is the AppUi bridge); the transport/app set carries zero geometry-plane obligation |
| Stratum-split records | RECORDED: Alimer.Bindings.MeshOptimizer = Compute; PicoGK = Fabrication voxel/implicit (never the kernel's second SDF owner — the [V8] one-lane ruling — and never its slicer); OcctNet.Wrapper = Fabrication |
| Named-and-rejected | STANDING + one addition: the `Manifold` NuGet-ID homonym joins SISL, TetGen, CGAL, SolveSpace, CuraEngine/Slic3r, Instant Meshes/NeurCross, MCUT, OCCT/LNLib, MeshLib, rhino3dm(-as-algorithm-owner), SkeletonNet/CGAL_StraightSkeleton_Dotnet, OpenSubdiv (admissible only on proven limit-eval-at-scale demand), V-HACD/CoACD (deferred until a consumer names it) |
| TASKLOG/IDEAS | `[T-BOOLEAN-NATIVE-ASSET]-[BLOCKED]` DISPOSED into the [V5] arrangement row (`NativeAssetMissing` reserved for the tier-3 scale route); `[HOST_BOUNDARY_REENTRY]` untouched (realization-phase, out of scope); the DECISION's growth rows (InCircle multi-implicit, CDTet 3D family, adaptive-slope slice rows, exact circumcenter side-of, OpenSubdiv demand card) seed the idea pool |

## [07]-[VERDICT_DISPOSITIONS] V1-V14

| V | Disposition |
|---|---|
| V1 | Ratified partition = [02] table (placeholder row replaced; four namespace-less pages minted; phantom dies at three sites W1; topology→naming rename; `Solving/` minted W3; `Drawing/`→`Emission/` renamed W4 per the ruled default; every delta runs the (b) inbound sweep) |
| V2 | Tier = nurbs (vendored substrate, [01] hinge) + curve rebuild + surface/subdivide/develop/panelize/patternmap NEW (rows 6-12); chain obligations: (a) parametric canonical-bytes = `Encode(Parametric)` row on the ONE reconciliation seam (component of `ToCanonicalBytes`); (b) UV pullback = surface.md UV-provenanced tessellation by construction; SSI disposition = host-deferred TRIPLE row (probe-widened); Offset promoted; borrowed fault ends |
| V3 | View engine RULED: exact analytic Appel QI through the crossing lattice (row 27); BSP apparatus dies whole; connectivity chaining; select boundary both sides; README:65 re-scopes with the fence; FAB:33/46/47 counterpart edits |
| V4 | Exact path LANDS (probe ruling [01]): collapse executed (arrangement/offset/repair copies die → `Intersection.Apply`); implicit `Constraint` case + defining-entity carriage + the mandatory floor companions (predicates re-founding + widening, delaunay hybrid table, constrained-only mode); typed 2D restriction; host-delegation re-anchored to `relations.md` |
| V5 | `BooleanOp` homes in arrangement (W2); `BooleanReceipt` = ONE type (receipts composes it as payload); Manifold NAMED (manifoldc P/Invoke, finite ceiling, fixture gate, homonym rejected); `[T-BOOLEAN-NATIVE-ASSET]` disposed; cycle break COMPLETED by the [V6] weld siting (the dossier's partition defect) |
| V6 | `Meshing/edit.md` minted W1 (row 13): true mutable arena, two-carrier seam, soup ×3 collapse, WELD PRIMITIVE sited, store-mutability + arena-concurrency contract legislated, HighPerformance admission ruled IN |
| V7 | One LM: solver generalizes to the residual+Jacobian functor over `SolvePolicy`; fit instantiates (re-impl dies, λ threads); dense-LA = landed `matrix.md` owner law corpus-wide; ddouble Σd² + QEM rows land (README:86); `Solving/` folder carries the family |
| V8 | PointCloud lift (one excision resolving E9+E10+E11); zero throws; outward bounds; kernel wire composed (ARCH:86); GWN single-owner = the ONE distance-field lane; counts resync; naming `Self` + re-founded `Subsumes` + injectivity ruling + namespace mints + fixture reproofs + ONE Persistence anchor |
| V9 | flatten (eliminate-boundary-rows Dirichlet, sparse LSCM, O(F) ARAP, true factor fill, dead carriers deleted, `spectral.md:191` verified); decimate (real flip guard, boundary collapse, incidence index, real `IsoSurface.Detailed`, second cast railed); repair (CDT re-mesh, threading, cast, Genus-tolerant ProjectionRow extension); receipts (`Converged` semantics fixed + validity fold); fit (FitStore deleted, neighbors lane, Line chart, functor composition) |
| V10 | (a) corner rows in offset.md; (b) slice.md NEW; (c) medial REAL against the landed dual accessor (probe-decoupled); (d) skeleton.md NEW over landed MCF; clearance-radius + arbitrary-probe query mandate carried on BOTH (c)+(d); (e) `Toolpath/Skeleton.cs` dies unconditionally |
| V11 | Hull two-tier: exact `LowerHull` `Tessellation` projection LANDS + landed cloud.md rail stands, boundary both pages; remesh.md NEW — isotropic non-deferrable + quad-from-stripes row LANDS (deferral criterion moot; Compute [V7] growth recorded) |
| V12 | 10 stringly cases re-typed (ruled payloads); two-family seam statement lands (ARCH:112); band plan = [02] (shares + deliberate headroom spend; no 13th cluster; no federation escalation); topology→naming with [V1] |
| V13 | pack dtype-strided residency REAL (descriptor-dispatched typed views — mechanism dossier-verified); phantom import dies; two landed compositions preserved verbatim; `field`·`toolpath` rows land for the named Compute/AppHost consumers |
| V14 | Ledger fence-conformance (index wire, reconciliation identity entry, pack rows — all composed in-fence); Parametric fan → rows; COMPUTE:120 dies, COMPUTE:121 resolves; anchors unified (reconciliation triple; FAB:22 split resolves offset-2D/skeleton-3D); false rationales corrected (delaunay:3 honest split; predicates:3 precise); strict-IEEE/RID invariant + FMA-free row + PeterO filter land; the Compute-consumable `Orient3D`/`InSphere` export = already-public surface + the NEW declared seam row |

## [08]-[EVIDENCE_DISPOSITIONS] E1-E16

| E | Disposition |
|---|---|
| E1 | HOLD (sharpened: phantom `PaintBackToFront`, dead `EmitsHidden`/`NeedsBsp`, dead `Kill`/`FreeList`) → row 27 kills the fake engine for analytic QI; connectivity chaining; README:65 re-scope |
| E2 | HOLD → rows 6-12 land the tier; both gates homed ([05]); Offset promoted; stubs die |
| E3 | HOLD + TWO DRIFT corrections adopted: intersect cite is {`:5`,`:388`}; curve cites are {`:3,20,170,190`} with the TWO-TARGET re-anchor (evaluation→projections.md, intersection→relations.md); delaunay:3 rationale corrected; index upward using dies; phantom three-site kill W1 |
| E4 | HOLD ×5 → flatten Dirichlet + LSCM (row 24), naming mis-key via `Self` (rows 4-5), receipts `Converged` (row 22), decimate boundary + O(F²) (row 23) |
| E5 | HOLD → [V1] ratification + the one-paradigm motion ([02] law); four namespace mints; FAB:44 follows [V5] |
| E6 | HOLD → index wire composed, upward using dies (row 3); COMPUTE:120 counterpart dies (W3) |
| E7 | HOLD → crossing collapse landed (rows 15-17, 21); medial real (row 17); fit functor composition (row 26); arrangement implicit lands (row 16) |
| E8 | HOLD → FAB:33/22/23 duplicates die for kernel owners (rows 27, 17/19, 18); FAB:48 resolves |
| E9 | HOLD → LM ×2 dies (rows 25-26); GWN single-owner (row 3); `BooleanReceipt` one type (rows 16, 22); soup ×3 collapses (row 13); dense-LA one law (row 24); kd-tree dup dies with the PointCloud lift (row 3) |
| E10 | HOLD → index throws + silent empty + inward bounds die (row 3); repair `:243` + decimate `:459` (beyond-register twin) route the rail (rows 21, 23); intersect's three casts (beyond-register) route the rail (row 15) |
| E11 | HOLD → BSP apparatus (row 27), `FitStore` deleted (row 26), `ChartStore` carriers deleted + `FactorNonZeros` (row 24), pack Half-residency made real (row 28), index counts resync (row 3), `WavefrontStore` prose + sizing (row 17), intersect dead `Next` column deleted (row 15, beyond-register) |
| E12 | HOLD → ledger conformance + anchor unification + fan rows + counterpart edits ([04]) |
| E13 | SPLIT adopted: hashing `:82` CORRECT (live defect); csparse DEDUPE; tensors REFUTED-as-dupe → overlay pair preserved; mathnet overlays verified (re-verify post-downgrade); LanguageExt REFUTED-as-absent → discharged, verify only |
| E14 | ALL HOLD, feed-verified → downgrade/drop/re-group motions in [06]; +the 4th MathNet pin (`PROPS:467`) joins the sweep; the `Manifold` homonym trap recorded |
| E15 | HOLD → faults re-type (row 2); delaunay seven stubs transcribed (row 14) |
| E16 | HOLD + ONE DRIFT adopted (ARCH:63 carries no falsifiable BSP claim — README-only pairing) → every index-doc row lands or re-scopes with its fence ([04] wave assignments) |

## [09]-[ESCALATION_DELTAS] — [03] table, 17 rows disposed

| Plane | Target row disposed by |
|---|---|
| Numerics/predicates 9→9.5 | Row 1 (invariant + rationale + FMA row + PeterO filter + Compute export seam + probe re-founding/widening) |
| Numerics/faults 7.5→9.5 | Row 2 (re-type + seam statement + band plan = taxonomy) |
| Parametric tier 3→9.5 | Rows 6-12 (full-body curve + stations/frames + surface ops with UV pullback + subdivision + develop + panelize + patternmap + identity + SSI-triple disposition + offset promoted) |
| Spatial/index 7→9.5 | Row 3 (lift + zero throws + outward bounds + wire + GWN single-owner + counts) |
| Spatial/naming+reconciliation 6.5→9.5 | Rows 4-5 (Self + Subsumes + namespaces + injectivity + fixtures + one anchor + `Encode` composition + parametric projection row) |
| Meshing/delaunay 6→9.5 | Row 14 (seven stubs + hull ruling + dual accessor + honest rationale + hybrid table) |
| Meshing/intersect 8.5→9.5 | Row 15 (real chain — the dossier's revise-down repaired — + collapse landed + 2D restriction + slice seam + re-anchor) |
| Meshing/arrangement 7.5→9.5 | Row 16 (implicit lands + BooleanOp home + Manifold gate + cycle broken) |
| Meshing/offset+toolpath 5→9.5 | Row 17 (medial real + clearance payload + corner rows + Minkowski complete + sizing + prose) |
| Meshing substrate 0→9.5 | Row 13 (arena + two-carrier seam + soup collapse + weld + contracts) |
| Processing/solver+fit 8→9.5 | Rows 25-26 (functor + instantiation + λ thread + FitStore + Line chart + matrix law + ddouble + neighbors lane) |
| Processing/repair+receipts 7→9.5 | Rows 21-22 (CDT re-mesh + Converged + extraction + threading + collision + total-topology ProjectionRow) |
| Processing/decimate 7→9.5 | Row 23 (incidence + boundary + real guard + ddouble + split recorded) |
| Processing/flatten 6→9.5 | Row 24 (Dirichlet + sparse LSCM + O(F) ARAP + fill + carriers + develop seam + spectral verify) |
| Drawing/view 4→9.5 | Row 27 (analytic QI engine + chaining + select boundary + Fabrication consumes) |
| Drawing/pack 7→9.5 | Row 28 (real dtype striding + phantom dead + preserved compositions + field/toolpath rows) |
| Slice/skeleton/hull/remesh 0→9.5 | Rows 18-20 + delaunay `LowerHull` (oriented contours + open chains; MCF skeleton; hull tier ruling; isotropic+quad substrate) |

## [10]-[LEG_PARTITION] — acyclic, engine-invocation rows

| Leg | Engine invocation targets (dependency order = listed order) | Rider motions |
|---|---|---|
| 1 FLOOR+CONTRACTS | `Numerics/faults.md`, `Numerics/predicates.md`, `Meshing/edit.md` (new), `Spatial/index.md`, `Spatial/naming.md`, `Spatial/reconciliation.md`, `Parametric/nurbs.md` (new) | Roster motion ([06]: GShark vendor kill, MathNet downgrade + sweep, OpenBLAS drop, Manifold + HighPerformance admissions, catalog corrections/dedup/reclass, pin re-group, WATCH re-key); [V1] ratification (ARCH `[03]` replacement, README router, phantom kills at `pack.md:33/:18` + `solver.md:13`); repair donor excision (surgical improve); GWN seam 1-hop ripples on `reconstruct.md`/`fields.md`; COMPUTE:121 + Persistence:55 counterpart edits; both frozen fixtures harness-reproved |
| 2 MESHING LATTICE | `Meshing/delaunay.md`, `Meshing/intersect.md`, `Meshing/arrangement.md`, `Meshing/offset.md` | repair boolean-block excision (surgical improve); FAB:44 counterpart; README:42/ARCH:42/README:99 rows land |
| 3 PROCESSING+SOLVING | `Solving/solver.md` (new, absorb from `Processing/solver.md`), `Solving/fit.md` (new, absorb from `Processing/fit.md`), `Processing/repair.md`, `Processing/receipts.md`, `Processing/decimate.md`, `Processing/flatten.md`; deletePages: `Processing/solver.md`, `Processing/fit.md` | `Solving/` folder mint + inbound sweep; `mesh.md` `TopologyReceipt` ProjectionRow Growth extension (1-hop); COMPUTE:120 dies; README:86/93 rows verified landed |
| 4 NEW OWNERS + PARAMETRIC + EMISSION | `Meshing/slice.md` (new), `Meshing/skeleton.md` (new), `Meshing/remesh.md` (new), `Parametric/curve.md`, `Parametric/surface.md` (new), `Parametric/subdivide.md` (new), `Parametric/develop.md` (new), `Parametric/panelize.md` (new), `Parametric/patternmap.md` (new), `Emission/view.md` (new, absorb from `Drawing/view.md`), `Emission/pack.md` (new, absorb from `Drawing/pack.md`); deletePages: `Drawing/view.md`, `Drawing/pack.md` | `Emission/` rename + inbound sweep; FAB:22/23/33/46/47/48 counterparts; Parametric fan ledger rows; the four `[05]` acceptance dry-runs |

Acyclicity proof: every [04] edge lands equal-or-earlier — W1 pages compose only W1 pages + landed floors (faults ← predicates? no: predicates routes nothing; edit→faults, index→faults, naming→faults, reconciliation→naming, nurbs→faults — all backward in listed order); W2 composes W1 + backward intra-wave (arrangement→{delaunay, intersect}, offset→{delaunay, intersect}); W3 composes W1/W2 + backward (fit→solver; repair→{edit W1, intersect/delaunay/arrangement W2}; receipts→{repair-vocabulary backward, arrangement W2}); W4 composes W1-W3 + backward (develop→{flatten W3, surface}; panelize→{remesh, surface}; patternmap→surface; view→{intersect/arrangement W2, index/edit W1}; pack→{reconciliation/naming W1}). The arrangement→repair forward edge is DEAD (weld sits in W1 edit); repair→arrangement is backward (W3→W2); zero upward-strata edges (the one breach, `index.md:27`, dies W1). Two-touch pages are explicit: repair (W1 improve, W2 improve, W3 rebuild), pack + solver (W1 phantom-kill improve, main motion W4/W3).

## [COUNTS]

page_rows 28 (18 existing disposed: 14 rebuild-in-place + 4 move-lowered; 10 new) · verdicts_disposed 14/14 · evidence_disposed 16/16 · escalation_deltas_disposed 17/17 · probe_rulings 2/2 · gate_items_homed: Generation 9/9, Fabrication 8/8 · deletePages 4 (move sources) · counterpart ledger edits 13 · roster rows disposed 22.
