# [RASM_CS_APPHOST_BRIEF]

[TRACK_ORDER]: Geometry (1) -> Persistence (2) -> Compute (3) -> **AppHost (4)** -> AppUi (5) -> Fabrication (6) — run the cs campaigns in this order; this campaign is 4/6, after the Persistence DECISION emits.

Authoritative campaign input for the restructure-and-harden of `libs/csharp/Rasm.AppHost/.planning/` — the APP-PLATFORM runtime spine (29 pages, 5 folders: Runtime 11 · Agent 6 · Wire 6 · Sandbox 3 · Observability 3). Upstream law honored throughout: the landed kernel corpus (`libs/csharp/Rasm/.planning/` + the kernel seam ledger, composed as fully realized law — the `Domain/identity.md` `ContentHash.Of` federation identity entry and the `Drawing/pack.md` `PackOp`/`EncodedGeometry`/`EncodingChannel` wire AppHost's Sandbox decodes, never re-implements), `RASM-CS-PERSISTENCE-BRIEF.md` (the V2 token-VALIDATING coordination store and V3 egress owner AppHost's PORT adapters DECODE — no AppHost interface or type crosses down), `RASM-CS-COMPUTE-BRIEF.md` (the V1 assessment lifecycle and JobState vocabulary AppHost consumes settled; the V13 channels split AppHost re-points against), and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (the `FaultBand` registry shape at `:141-149`; AppHost occupies its own APP-PLATFORM band neighborhood, never the AEC 23xx-27xx registry). Six surgical waterfall edits are landed and verified in place: the Persistence `[V2]` per-unit vector debit, the Persistence `[V2]` coordination READ cases (in-flight/expired-lease enumeration), the Persistence `[V3]` wire-native delivery-honesty read, the Persistence `[V12]` presence-driver correction, the Compute `[V13]` channels-split AppHost re-anchor, and the Compute `[02]` `GeometryEncoding` `Field`/`Toolpath` consumer-pressure row. `docs/stacks/csharp/` governs.

## [EXECUTION]

Settled campaign, cs track 4/6, no DECISION pass — launched after the Persistence DECISION emits (the `[V2]`/`[V9]`/`[V10]` signature-locks re-verify against it at leg entry): the durable rebuild engine runs directly against this brief per the four-leg `[05]` partition — the `[05]` leg rows ARE the page-set source, each leg self-contained, its pass agents repairing the cross-file ripples their own work exposes in-pass and its serialized per-wave tail closing index docs and central manifests before it returns — and legs re-run across cold sessions against the same brief until a pass finds nothing.

| [STEP] | [LAUNCH] | [PRODUCES] |
| :----: | :------- | :--------- |
| 1 | `Workflow(rebuild, {brief: "RASM-CS-APPHOST-BRIEF.md", leg: 1})` | PERIMETER FLOOR — `[V1]` registry + `[V4]`/`[V12]` extractions + `[V15]`/`[V16]`/`[04]` roster-catalog-csproj motion + `[V17]` ledger re-emit |
| 2 | `Workflow(rebuild, {brief: "RASM-CS-APPHOST-BRIEF.md", leg: 2})` | RUNTIME SPINE landed |
| 3 | `Workflow(rebuild, {brief: "RASM-CS-APPHOST-BRIEF.md", leg: 3})` | AGENT + WIRE planes landed |
| 4 | `Workflow(rebuild, {brief: "RASM-CS-APPHOST-BRIEF.md", leg: 4})` | SANDBOX + OBSERVABILITY closeout + final `[V17]` governance truth pass + the `[05]` acceptance dry-runs — campaign landed |

One-run form: `Workflow(rebuild, {brief: "RASM-CS-APPHOST-BRIEF.md", leg: [1, 2, 3, 4]})` lands all four legs behind per-wave barriers in one invocation; legs 3 and 4 carry no cross-dependency beyond the leg-1 registry + leg-2 ports and may run as parallel waves.

## [00]-[SHARED_LAW]

### [VERDICT]

The 29 fence interiors are genuinely world-class — extreme ADT collapse, `Fin`/`Validation`/`IO` rails, `ClockPolicy`-owned time, `XxHash128` content addressing, seed-DATA rosters, a DAG page graph, a lean fully-consumed manifest — and the PERIMETER around them is fiction. Six proofs, each independently fatal to a world-class claim:

1. **The fault-code space is collided and guarded by wrong prose.** Two confirmed hard integer collisions: `IdentityFault` 4640-4648 (`Agent/identity.md:119`) ≡ `McpFault` 4640-4644 (`Agent/mcp.md:125`) and `FeatureFault` 4700-4704 (`Runtime/features.md:154`) ≡ `SolverFault` 4700-4703 (`Sandbox/solver.md:40`) — `Expected.Code` is the telemetry key, ambiguous by construction. Plus a simple-name collision (`SupplyChainFault` ×2: `Sandbox/provisioning.md:200` band 1320 vs `Sandbox/isolation.md:261` band 4680), the 1xxx `SpineLog` event band overlapping `ProfileFault` 1100/`LifecycleFault` 1200/`UpdateFault` 1300 with nothing enforcing event-vs-fault disjointness, a cross-package integer ambiguity (`CoordinationFault` 4520, `Wire/coordination.md:172` ≡ Compute Remote `WireFault` 4520-4532), an orphan bare `Error.New(9300)` outside every documented band (`Observability/bundles.md:90`), and a bare `Error.New` on the fencing rail itself (`Runtime/time.md:331`). The ONLY collision guard is a hand-curated prose paragraph (`Agent/federation.md:22`) that is provably incomplete — it omits seventeen occupied bands and is blind to `IdentityFault` also holding 4640. No type-enforced registry exists.
2. **The fence is decorative.** `Runtime/time.md#FENCING_TOKEN` declares AppHost OWNS `FencingToken` with `LeaseElection.Acquire` minting from a per-process `Atom<FencingToken>.Swap(Next)` and `Fence` validating against that same in-memory atom (`time.md:319-331`) — ZERO cross-process safety, two processes mint independent sequences — directly contradicting the page's own prose (`:304` "the store fences the token at Persistence/server-tier") and inverting the finalized Persistence `[V2]` token-VALIDATING law. `Runtime/orchestration.md:268-271` pushes three AppHost types (`WorkflowInstance`/`WorkflowStep`/`FencingToken`) down into the Persistence CAS store through `StepStateSeam.Persist`; `Wire/coordination.md`'s election and lock ride the same in-memory rail. Only `Agent/capability.md:420-423` `DistributedBudget` delegates the CAS correctly — and it still crosses AppHost's token and `CostVector` types downward.
3. **The prohibited deleted form ships beside its own prohibition.** `Sandbox/isolation.md:246-321` mints a SECOND `SupplyChainGate` (hand-rolled `Verify` delegate) + a SECOND `SupplyChainFault` (4680) + `SemverGate.Compatible` hand-splitting a `"lower-upper"` string through `System.Version.TryParse` (`:314-320`) — the exact form `ARCHITECTURE.md:148` and `Sandbox/provisioning.md:207` name as deleted, while provisioning's canonical Sigstore+`VersionRange.Satisfies` gate asserts "both the self-update release and a downloaded plugin/companion artifact verify through this one `Admit`, never two verify paths." Disk carries two. `Sandbox/solver.md:190-191` feeds the load path an all-empty `PluginArtifact` (empty digest, empty signature, `None` provenance) the real gate must reject — an incoherent placeholder wired into `SolverHosting`.
4. **The wire context is forked and phantom-bearing.** `Sandbox/provisioning.md:148` dereferences `AppHostWireContext.Default.UpdateReceipt` while `Runtime/ports.md:122-139` registers 18 `[JsonSerializable]` rows with NO `UpdateReceipt` — a phantom member. `Wire/livewire.md:806-825` mints a standalone `LiveWireOptions.Json` + `LiveWireContext` with its own converter factory and `MakeReadOnly()` — a parallel wire-options owner against ports' own WIRE_LAW ("one merge per app root") — while `ports.md:137` registers a `WriteReceipt` livewire's own `WriteReceiptWire` supersedes.
5. **The cross-package perimeter is stale and inverted in both directions.** Stale-DELETED: `ARCHITECTURE.md:71` (`Persistence/Query/transaction` — folded into `graph#STORE_RAIL`) still backs `Runtime/lifecycle.md:288`'s 2PC in-doubt drain row; `ARCHITECTURE.md:73` (`Persistence/Sync/egress` — re-homed to `Version/egress`) still backs `Wire/outbox.md:20,165`; `Wire/companion.md:23` lands presence at stale `collaboration#PRESENCE_AND_BLOB` (owner: `Version/ledger#PRESENCE`); `Runtime/ports.md:19` cites `Persistence/indexes#L2_CONTRIBUTION` and `Persistence/provisioning#TENANCY_RLS` (owners: `Query/cache#L2_CONTRIBUTION`, `Element/identity`). Malformed/wrong: `ARCHITECTURE.md:86` anchors the M.E.AI seam at phantom folder `Model/agent`; `:67` targets `Runtime` for the pack wire whose consumer is Sandbox; `:80` declares FleetRoll reads `MembershipView.Serving` while `provisioning.md:269,273` wires companion's `PeerRoster.Attached` — duplicate membership owners with the seam declared against the wrong one. Wired-UNDECLARED: the entire command path composes Compute's `ComputeIntent`/`IntentAdmission`/`SelectionReceipt`/`SubstrateSelection` (`capability.md:3,237-238,300,564`; `Agent/runtime.md:46`) with zero ledger row; `Runtime/determinism.md:118-125` constructs the Persistence `OpLogEntry` positionally (14 fields, hardcoded `"determinism.command"`/`"command"`/`SnapshotCodec.JsonStj`) with its own RESEARCH tail admitting the spelling is unverified. Meanwhile `ports.md:3`/`ARCHITECTURE.md:18` claim the seven inward ports are the ONLY cross-package seam and "the dependency never reverses."
6. **Concurrency defects ship on the hot rails.** Two lost-update dedupe folds — `Wire/outbound.md:521-523` and `Wire/topics.md:99-104` both read `cell.Value`, filter, then `Swap(_ => pruned.AddOrUpdate(...))` DISCARDING the atom's live value, so concurrent fans lose each other's dedupe records — and the outbox relay's fenced watermark advance is bound then thrown away (`Wire/outbox.md:106-114`: `_advance` unused in the returned `DeliveryReceipt`, `Defer` yielding constant `0UL`) — delivery accounting is notional, not wired.

What IS sound (preserve; harden ONLY per the verdict that names it): the seven-port + `PortCardinality` discipline, `ReceiptEnvelope` HLC two-half stamp, `TenantContext` causal frame, `ClockPolicy`/`DeadlineClass` universality (zero bare duration literals, zero `DateTime.UtcNow`), the `XxHash128` content-addressing design (the chain/log shape is sound; its hashing path re-composes through the kernel `ContentHash.Of` entry per `[V18]`), the `ModuleContribution` composition root, the `CommandDispatch` single front door, the M.E.AI `GoverningChatClient` fold, the MCP/federation projection pair, the OIDC identity boundary, the OpenFeature flag plane, the Polly keyed lanes, the Velopack update rail, the OPC-UA/MQTT/Modbus/serial certified-client studio, the `SupportTrigger` capture fan, the `DriverProbe` one-adapter shape, and a `.api` tier whose gold catalogs (`[STACK]`/`[INTEGRATION_STACK]` sections naming exact seams) are grep-verified consumed by their pages. The page graph is a DAG; the roster is lean with zero never-demanded catalogs. The rot is the PERIMETER — bands, seam direction, duplicate gates, forked wire contexts, stale anchors — and no per-page cold pass fixes the perimeter. Perimeter law first.

### [TELOS]

`Rasm.AppHost` is the federation's runtime spine: the composition-root doctrine every app root folds, the reproducibility kernel (pinned RNG/float-mode, hash-chained content-addressed command log, replay/bisect/counterfactual), the crash-durable workflow engine over one `CommandDispatch`, the agentic capability surface (self-describing op catalog, commit-or-rollback command algebra, fenced grant/cost broker, MCP projection + external-server federation, governed model reasoning), the industrial external-binding studio, the fleet provisioning/supply-chain/coordination conductor, and the observability plane — one body. Every owner grades against the 5x consumer bar: every app root composes the spine, every plugin crosses the sandbox, every fleet node rides the roll conductor, every agent session meters through the broker, every sibling package lands receipts through the seven ports. World-class additionally means the DISTRIBUTED claims are honest: a fence the store never validates is decorative (Kleppmann law — the authoritative check is the Persistence CAS predicate); delivery honesty is stated per sink, never assumed; membership is two-tier explicit (local attach vs cluster liveness); a dedupe cell under concurrent fans is a read-modify-write, never a stale-read swap; a crash-durable claim covers node DEATH, not just process reboot — a dead holder's in-flight work reclaims under a fresh fenced lease, never orphans; a replay claim covers process RESTARTS — the recorded chain rehydrates from the durable store and re-verifies before it replays, never surviving only the recording process's memory. And the perimeter is machine-checkably true: one type-enforced fault-band registry, a seam ledger complete in both directions, catalogs anchored to live pages, and governance prose whose every claim resolves against disk. Zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The legs own the page-set within the verdict envelope. The five-folder partition is RATIFIED — every folder ≥3 pages, a real growth axis, zero one-file folders, zero flat sprawl; NO new top-level folder is warranted by any verdict (a new runtime concern is a page in its owning folder; a new probe, sink, hop, transport, flag rule, or strategy is one data row on the owning axis). Exactly two page-set changes, both decided NOW: `[V4]` extracts `Sandbox/admission.md` (the ONE supply-chain admission owner; Sandbox 3->4) and `[V12]` extracts `Runtime/secrets.md` (the credential-material lifecycle out of `config.md`; Runtime 11->12). The post-campaign page-set is sealed so no leg guesses a name: Runtime 12 · Agent 6 · Wire 6 · Sandbox 4 · Observability 3 = 31 pages, same five folders. Runtime stays flat at 12 with its split axis RECORDED, never executed: spine (profiles/lifecycle/time/resources/modules/ports) · policy plane (config/secrets/features/laneguard) · reproducibility (determinism/orchestration) partitions only if the folder grows past ~13 — the recorded-valve law the Persistence brief seals for `Version/`. Every structural change updates `ARCHITECTURE.md` `[01]`-codemap/`[02]`-seams and `README.md` router and package groups in the same motion. No page survives on incumbency, and no page falls to it either: a 9.5-grade fence is rebuilt only where a verdict names its defect. `Wire/livewire.md` (the one page never line-audited) runs a FULL adversarial harden regardless.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md:18-21,38`): AppHost is APP-PLATFORM, depending UP on `Rasm` (kernel) only, PORT-peering with `Rasm.Persistence`/`Rasm.Compute`/`Rasm.AppUi`, and NEVER referencing an AEC-domain package or a host SDK — the `ARCHITECTURE.md:150` GeometryGym-free ArchUnitNET rule and the `ProjectionContext` primitives-only contribution stay law. Two consequences sealed here: (a) the four Persistence PORT contracts are homed Persistence-side on Persistence-owned types with token-VALIDATING fencing — AppHost PORT adapters project requests to wire-stable primitives and DECODE Persistence-owned results; an AppHost interface or type crossing downward is the deleted form (`[V2]`); (b) outward composition of sibling-owned vocabularies (`ComputeIntent`, `GrpcChannelPolicy`, the coordination op-union, the changefeed row) is a NAMED shared wire-vocabulary tier declared in the ledger (`[V7]`), never a reversed interior dependency and never smuggled under "the seven ports are the only seam." App roots own process attachment and exporter/sink pins per the `[V15]` parameterized boundary rule; the existing `[04]`/`[05]` prohibition blocks carry forward unamended except where `[V15]` re-scopes the reservation sentence.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — `HostProfile`/`CacheLane`/`DeadlineClass`/`ModelRoute`/`TargetingRule`/`OutboundHop`/`ExternalTransport`/`DrainKind`/`HealthContributorRow` are all `[SmartEnum]`/`[Union]` + FrozenDictionary seed DATA — so the law binds the NEW planes: fault bands are rows in ONE type-enforced registry where a duplicate integer fails at type initialization (`[V1]`), never per-page literals or prose neighborhoods; broker health probes are rows that TRACK the Persistence egress sink roster (`[V6]`), never a fixed enum drifting beside it; delivery honesty is a policy column on the one `HopPolicy` row family (`[V9]`); supply-chain admission is ONE gate with policy rows (`[V4]`); membership is two declared tiers with one fold between them (`[V5]`). The test: the next band, probe, sink, hop, strategy, or flag rule is one data row, never a new arm, a loose integer, or a second gate.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — the corpus norm (`CommandDispatch.Run`, `Assess`-shaped folds, `OutboundSurface`, `SupplyChainGate.Admit`), sealed for the new owners; `CommandDispatch` stays the single dispatch front door — a second dispatcher is the deleted form.
- Every fault is a typed `[Union] : Expected` case in a registry-assigned band; bare `Error.New` on a rail is the deleted form (`bundles.md:90` and `time.md:331` are the two live breaches); `throw` in an expression fold is the same class (`orchestration.md:218`, the five `resources.md:233-255` arms) — total dispatch or typed rail failure.
- Fencing is store-validated or it is not fencing: every guarded write carries the Persistence-issued token and the store's CAS predicate is the authoritative check; a client-side `Admits` is a pre-check at most, stated as such (`[V2]`).
- Atom folds are read-modify-write: a `Swap` that closes over a stale `.Value` read is the deleted form (`[V13]`); the fold runs INSIDE `Swap(current => ...)`.
- Cross-package member claims verify or die: `uv run python -m tools.assay api` over restored assemblies is truth for every composed member (`AppHostWireContext.Default.*` dereferences, the FluentModbus generic overload set, `JsonSchemaExporter.GetJsonSchemaAsNode`, `PemEncoding`/`PosixSignalRegistration`, any wasmtime Component-surface claim); a member you cannot verify is a phantom. Kernel members verify against the LANDED kernel fences — the conversion left no kernel assembly to decompile, so `ContentHash.Of` (`Rasm/.planning/Domain/identity.md`), `Encode.Apply(PackOp)`/`EncodedGeometry`/`EncodingChannel`/`PackKind` (`Rasm/.planning/Drawing/pack.md`), and every composed kernel spelling read the landed page fences plus the kernel `ARCHITECTURE.md` seam ledger as truth.
- Consumer contracts are RECORDED where the owner is upstream, never re-homed: the coordination op-union/token/outbox/egress shapes are Persistence's (`[V2]`/`[V9]`), the assessment lifecycle and `JobState` are Element/Compute's (`[V3]`), the `Field`/`Toolpath` encoding rows are kernel `PackKind` + Compute residency-wire rows (LANDED — never a residency-side `GeometryEncoding` owner, per the Compute seam ledger's own deleted-form naming; the landed `pack.md` fence carries the rows — a `field` row packing `geodesic`/`weight`, a `toolpath` row packing `position`/`weight` — and `solver.md:54-55,197` signature-locks on them), the pack wire is the kernel's (`[V17]`); AppHost signature-locks and decodes.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped; never `dotnet add`) + `Rasm.AppHost.csproj` (103 lines); per-package catalogs under `libs/csharp/Rasm.AppHost/.api/` (70), shared substrate under `libs/csharp/.api/`. The roster is LEAN, not fully consumed — the consumed-census claim breaks exactly twice, and each break disposes by the integration-first law, never as a bare REMOVE: `System.CommandLine` (`csproj:59`, 2.0.9 stable central pin, ZERO page consumers — `config.md:103`'s `AddCommandLine` belongs to `Microsoft.Extensions.Configuration.CommandLine`, a different package) draws the `[04]` CLI integration mandate; `Grpc.AspNetCore.Web` (`csproj:71`, zero fence consumers — `UseGrpcWeb` is composition-root middleware, no fence carries its types; the sole corpus token is `companion.md:650`'s catalog-inventory mention) joins the `[V15]` MOVE set by that verdict's own rule. The manifest motion lands in leg 1: ADD `AspNetCore.HealthChecks.Nats` (Xabaril `AspNetCore.Diagnostics.HealthChecks` family — the same family as the five admitted gold health catalogs; Apache-2.0; nuspec-verified to bind `NATS.Net`, so the pooled-connection probe binding holds against the central 2.8.2 pin; member set `assay api`-gated at admission) as the `[V6]` NATS probe row with its `.api` stub; ADD `Microsoft.Diagnostics.NETCore.Client` (MIT, dotnet/diagnostics; the central pin already sits in the Bridge Runtime group) + `Microsoft.Diagnostics.Tracing.TraceEvent` (3.2.4 feed-verified, MIT, perfview active — the `.gcdump` graph format's one categorical owner, `DotNetHeapDumpGraphReader`, and the process-dump row's charter already states dump AND gcdump capture, `bundles.md:186`) as the `bundles.md:326` `[DUMP_ADMISSION]` fill; ADD `BACnet` (MIT, ela-compil, netstandard2.0 asset) as one livewire `ExternalTransport` row; MOVE `OpenTelemetry.Exporter.OpenTelemetryProtocol` (`csproj:45`), `Serilog.Sinks.Console`/`Serilog.Sinks.File` (`csproj:56-57`), and `Grpc.AspNetCore.Web` (`csproj:71`) out of the lib csproj to the app roots per `[V15]` (central pins stay; `telemetry.md`'s own law routes exporter projection and Serilog sinks to composition roots); KEEP the protocol runtimes (`Wasmtime`, `MQTTnet`, `OPCFoundation.NetStandard.Opc.Ua`(+PubSub), `System.IO.Ports`, `FluentModbus`, `Grpc.AspNetCore`(+HealthChecks), `ModelContextProtocol.AspNetCore`) per the `[V15]` fence-signature rule, with `ARCHITECTURE.md:119/:139` re-scoped in the same motion. The substrate weave (`LanguageExt.Core`, base `Thinktecture.Runtime.Extensions`, the shared `JetBrains.Annotations` identity) enters through `Directory.Build.props:199-201` `UseWorkspaceLibraries` injection, never per-csproj rows — a leg "repairing" the csproj with those references is the deleted form. Catalog hygiene is a first-class obligation (`[V16]`): the 5 byte-identical cross-tier copies delete (md5-verified: `api-nodatime`/`api-jsonpatch`/`api-redaction`/`api-hybrid-cache`/`api-grpc-core-api` — shared tier canonical), the 7 re-scoped overlays trim to the AppHost delta, `api-grpc-client.md`'s retained Compute framing re-scopes, `api-modbus.md`'s RESEARCH tail member-verifies and dies, the absent `api-languageext.md` authors at the substrate tier (ONE authoring shared with the identical Geometry/Compute/Persistence brief obligations — whichever campaign runs first lands it, the others verify; the geometry campaign runs first in the track, so this campaign expects to VERIFY), and the flagged BCL members gain substrate catalog rows. Recorded REJECT, never re-proposed: force-mining `Riok.Mapperly`/`QuikGraph`/`Generator.Equals` into AppHost — the non-uses are architecturally correct (declaration-order DI fold, linear saga `Seq` with reverse-fold compensation, STJ wire crossing, `XxHash128` content identity; no object mapping, no graph algorithm, no structural equality concern exists). License gate: OSS and free-for-OSS-commercial admissible; pay-tiered/seat-licensed/proprietary-gated REJECTED.

## [01]-[CAMPAIGN_VERDICTS]

The legs must land every verdict below. Recommended shapes follow each; a leg may exceed them, never dilute them.

### [V1_FAULT_BAND_REGISTRY]

Decidable NOW: mint the AppHost band registry mirroring the federation `FaultBand` (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149`) — a `[SmartEnum<int>]` whose generated key lookup makes a duplicate band fail at type initialization; every fault union's `Code` derives through the registry row, never a `base(detail, NNNN)` literal. Ruled siting: a `[FAULT_TABLES]` owner block on `Runtime/lifecycle.md` (the `FaultSpine` root every fault transition already folds through). The re-partition it must emit: existing disjoint bands register as-is (`ConfigError` 4100-4199 as a legal multi-decade stride, `HopFault` 4500, `CommandFault` 4600, `GrantFault` 4620, `SandboxFault` 4660, `WireFault` 4720, `BusFault` 4730, `OutboxFault` 4740, `LaneFault` 4750, `ReplayFault` 4760, `OrchestrationFault` 4770, `SecretFault` 4780, `PemFault` 4790, `FederationFault` 4800); the two hard collisions re-partition (ruled: `McpFault` keeps 4640 — its JSON-RPC band intent is argued at `mcp.md:111` — and `IdentityFault` re-bands to 4630; `FeatureFault` keeps 4700 and `SolverFault` re-bands to 4710); `CoordinationFault` re-bands off the Compute Remote `WireFault` 4520 ambiguity (ruled: 4540) and the registry carries PINNED MIRROR rows for foreign neighborhoods (Compute 2200-2220/4520-4532, Persistence 5xxx/771x/82xx-83xx, the AEC 23xx-27xx registry, the kernel pair — `GeometryFault` 2400-2449 hard-bounded (landed: `Rasm/.planning/Numerics/faults.md`), and the `Rasm.Domain` substrate `Fault`'s single coded discriminant 9104 (`rails.md`'s category-keyed family; the sole foreign 9xxx occupant once the orphan 9300 dies) — and the AppUi 6xxx neighborhood — `RASM-CS-APPUI-BRIEF.md` `[V1]` re-bands every AppUi union out of the foreign 41xx-50xx squat space (this registry's 4xxx neighborhood plus the Persistence 5xxx); AppUi the demanding consumer, its registry pinning this one reciprocally) exactly as the paradigm registry pins the kernel 2400 — cross-package disjointness becomes a row, not a prose hope. The 1xxx space partitions: `SpineLog` event identifiers take 1000-1099 as registered EVENT rows, the boot-fault decades (`ProfileFault` 1100, `LifecycleFault` 1200, `UpdateFault` 1300, `SupplyChainFault` 1320) register as fault rows — event-vs-fault disjointness type-enforced. Bare errors die: `bundles.md:90`'s `Error.New(9300)` becomes a typed `SupportFault` case in a registered band (ruled: 4810 — the next free decade above `FederationFault` 4800, disk-verified unoccupied; the orphan 9300 dies with it); `time.md:331`'s fence error becomes a typed case landing with `[V2]`. `federation.md:22`'s hand-kept band census — already wrong — dies for one registry pointer; every page's band prose becomes a registry reference.

### [V2_PERSISTENCE_PORT_DECODE]

The accumulated-census core: the four Persistence PORT adapters re-shape to DECODE-ONLY per the finalized Persistence `[V2]` (op-union, fencing tokens, membership rows, receipts are Persistence-owned; the store is token-VALIDATING; no AppHost interface or type crosses down). (a) `Runtime/time.md#FENCING_TOKEN` re-charters from token MINT to token CARRIER: `FencingToken` becomes the decoded carrier of the store-issued lease generation; `LeaseElection.Acquire` becomes the PORT adapter calling the Persistence coordination op-union and decoding the lease grant; the per-process `Atom<FencingToken>` mint (`time.md:319-321`) and the in-memory `Fence` validation (`:328-331`) DIE — the authoritative fence is the store's row-CAS predicate, a stale holder is a decoded `LeaseFenced` fault; `Admits` survives only as a documented client-side pre-check; the fence-path bare `Error.New` becomes a typed registry case. (b) `Runtime/orchestration.md#STEP_STATE_SEAM` re-shapes: `Persist` takes a PROJECTED durable row of wire-stable primitives (instance id, workflow id, step ordinal + status key, serialized `StepKind` payload, the opaque store token) — never `WorkflowInstance`/`WorkflowStep` records — and `Rehydrate` decodes the store row back through the adapter; the `StepStateSeam.InFlight` enumeration (`orchestration.md:271`) and the `[V3]` reclaim's expired-lease scan decode the same way, riding the coordination op-union's READ cases (the sixth waterfall clause this brief landed in the Persistence `[V2]`) — the crash-resume flagship's ingress, never an AppHost-side table scan; the page's own ":259 wire-stable keys" prose becomes the fence's truth. (c) `Agent/capability.md#GRANT_BROKER` `DistributedBudget` re-spells its request as the per-`CostUnit` debit/ceiling projection riding the store's VECTOR fenced compare-and-decrement (the Persistence `[V2]` extension this brief waterfalled: `WHERE token >= held AND balance_i >= debit_i ∀i`); the token flows store->AppHost only. (d) `Wire/coordination.md` `RoleElection`/`DistributedLock` re-route through the same adapter — one election rail, zero in-memory fencing. Every adapter decodes Persistence-owned result types; the projection lives AppHost-side, the types Persistence-side.

### [V3_WORKFLOW_LIFECYCLE_CONSUMPTION]

`Runtime/orchestration.md` consumes the settled assessment lifecycle instead of running beside it (Compute `[V1]`; mandate law): a `StepKind.Activity` dispatching a Compute assessment maps seam outcomes at the step boundary — `Transient`-kinded failure -> retry-eligible re-drive under a policy row (bounded, At-age backoff mirroring Compute `[V1]`c), `Dispatchable` -> re-dispatch riding the sweep and the Persistence coordination store, `Terminal`/`Failed` -> the saga compensation fold — with `StepStatus` staying AppHost's workflow-instance vocabulary MAPPED at the boundary, never merged, and the `AssessmentOutcome`/`JobState` vocabularies composed strata-legally, never re-declared: the outcome/kind keys DECODE from the widened `AssessmentWire` receipt (Compute `[V1]` lands the failure columns, `receipts.md:416`) because `ARCHITECTURE.md:150` forbids any `Rasm.Element` reference — the Element-owned lattice never crosses as a CLR type; `JobState` rides the lawful AppHost->Compute compose direction (`[V7]`). Sealed as law: NO parallel job-state store lands beside `ONE_FENCED_LEASE_STORE` (currently clean — the executor is `CommandDispatch`, the state seam is `[V2]`b's projected row; this verdict makes the constraint explicit so the Compute sweep's landing cannot induce one). Same-page repairs ride the motion: the prose-vs-fence `Begin` signature drift (`orchestration.md:18` omits the `FencingToken` param `:93` carries) corrects; the `:218` `LastOrDefault() ?? throw new UnreachableException()` joins the typed rail as an `OrchestrationFault` case. Two flagship links RAISE this verdict past boundary repair, both on settled vocabulary. (a) THE PARKED-STEP WAKE: a `Dispatchable`/in-flight outcome parks the step `waiting` on its `Signal` channel, and the wake law is two-tier mirroring the bus law (`topics.md:20` — in-process fast path, durable correctness path): the fast path is a decoded durable-drain delivery mapped to `WorkflowExecutor.Signal(instanceId, channel)` (delivery honesty per the hop's `[V9]` row, dedupe by `id`=`ContentKey`), the correctness path is the existing `SignalTimeout` `ScheduleEntry` bound (`orchestration.md:165-186`) whose fire re-drives the step — and the re-dispatch resolves through Compute's `[V1]` lifecycle-aware cache, so a meanwhile-completed assessment returns its cached verdict and a still-running one re-parks; an unbounded poll and a wake-only shape that hangs on a lost delivery are both deleted forms. (b) FENCED ORPHAN-INSTANCE RECLAIM — the AppHost mirror of Compute `[V1]`'s orphan-recovery leg: `CrashResume.Resume` (`orchestration.md:283-293`) generalizes from boot-only self-recovery to the lease-reclaim sweep — a serving node enumerates in-flight instances whose store lease expired (the lease-expiry membership semantics Persistence `[V2]` already carries), re-acquires under a fresh store-issued token, and re-drives from the committed cursor, while the dead holder's late advance rejects store-side as the decoded `LeaseFenced` fault — one node death never wedges a workflow forever; the reclaim rides the `[V2]`d election rail and the waterfalled op-union READ cases, never a second recovery store, never a scan the port law forbids.

### [V4_ADMISSION_OWNER]

Extract `Sandbox/admission.md` — THE one supply-chain admission owner: provisioning's canonical gate (Sigstore `TryVerifyDigestAsync` offline trust root + `NuGet.Versioning` `VersionRange.Satisfies` + in-toto SLSA provenance), `TrustPolicy` rows, and the ONE `SupplyChainFault` union in its registry band (1320). Three consumers compose it: `provisioning` (`UpdateRail.Stage` precondition for releases), `isolation` (`SandboxRows.Load` plugin-artifact admission), `solver` (`SolverHosting.Loaded`). `isolation.md`'s duplicate gate record + `SemverGate` (`System.Version` hand-split — the `ARCHITECTURE.md:148` deleted form) + `SupplyChainFault`#2 (4680) DIE (`isolation.md:246-321`). `solver.md:190-191`'s all-empty `PluginArtifact` stub becomes a real artifact carrying digest/signature/provenance/component bytes resolved from the manifest source — or the load path rejects `AttestationMissing` by construction, never admits a hollow artifact. Sandbox becomes a 4-page growth axis (admission/isolation/provisioning/solver), un-fusing provisioning's two concerns. Deciding criterion: the extraction survives only if the gate fences partition cleanly from the `UpdateRail` state machine (disk says they do — the gate is a static `Admit` over policy rows); on failure the one gate stays sited in provisioning with isolation/solver consuming — either way exactly ONE gate, one fault union, one band.

### [V5_MEMBERSHIP_TWO_TIER]

Rule the duplicate membership owners into two declared tiers of one concern: `PeerRoster` (`Wire/companion` — the LOCAL kernel-credentialed attach set, `RosterEntry` keyed by peer credential) and `MembershipView` (`Wire/coordination` — CLUSTER probe-driven liveness, `MemberRecord`/`NodeState`), with the local roster a CONTRIBUTION folded into the cluster view, never a substitute. `FleetRoll` wave membership re-points to `MembershipView.Serving` — a fleet-wide roll reads cluster liveness (`RollStrategy.Plan` takes the serving member set), while each node's actual roll still dials the local node over the companion control hop — reconciling `provisioning.md:269,273` (wires `PeerRoster.Attached`) with `ARCHITECTURE.md:80` and `coordination.md:60` (declare `MembershipView.Serving`) to one truth. The presence seam lands on the same ruling: the beat PRODUCER is companion's `PeerRoster` (`Presence.Beat`), the resources `DrainSurface` lane is the in-process transport, the landing owner is Persistence `Version/ledger#PRESENCE` — `companion.md:23`'s stale `collaboration#PRESENCE_AND_BLOB` re-points, BOTH sides declare the seam (the Persistence `[V12]` driver wording corrected by this brief's waterfall), and the beat row crosses as a Persistence-owned wire row so no AppHost type is referenced downward.

### [V6_HEALTH_ROSTER_INTEGRATION_FIRST]

The mandate's `:85` obligation: `Observability/health.md#HEALTH_PROBE` re-writes against the Persistence roster motions. The `Cache` row stops probing a raw `IConnectionMultiplexer` singleton (`health.md:76-77,101`) — the Persistence `[V7]` prune drops the direct Redis driver — and probes the L2 caching transit (`IDistributedCache` through `Microsoft.Extensions.Caching.StackExchangeRedis`, the one surface the CACHE_PORT actually rides). The `Broker` rows become seed DATA tracking the Persistence `[V3]` egress sink roster (integration-first: brokers land as sink rows before any removal): the NATS anchor row lands NOW (`AspNetCore.HealthChecks.Nats` admitted per `[ROSTER_RECONCILIATION]`, binding the pooled `NATS.Net` connection exactly as the npgsql row binds its `NpgsqlDataSource`), Kafka survives as a sink-tracking row tied to the Persistence `Confluent.Kafka` sink-row disposition (the Persistence `[V3]` law lands broker rows by default; a broker row leaving on that DECISION's redundancy proof takes its probe row with it), and a sink row admitted Persistence-side lands here as one probe row — the fixed 6-row `DriverProbe` enum stops drifting beside the roster it probes. `ARCHITECTURE.md:85`'s `Npgsql/Redis/Kafka driver` wording re-writes; the README `[OBSERVABILITY]` health-check group re-aligns; the five gold health catalogs re-anchor where their driver bindings move.

### [V7_SHARED_WIRE_TIER]

Formally NAME the outward composition tier the "seven ports only" prose denies: the settled sibling-owned vocabularies AppHost composes by reference — Compute `ComputeIntent`/`IntentAdmission`/`SelectionReceipt`/`AdmittedIntent`/`SubstrateSelection` (the whole command execution path), Compute `GrpcChannelPolicy` + the proto vocabulary, the Persistence coordination op-union/token/receipt shapes (`[V2]`), the Persistence changefeed row (`[V10]`), and AppHost's own outward exports (`ShedVerdict` -> Compute admission, `DataClassification` -> Persistence `ClassificationGuard`, `FlagVerdictWire` -> typescript:core/interchange/codec) — declared in `ARCHITECTURE.md` `[02]` as `[WIRE_VOCABULARY]` rows: composed at the boundary, never re-declared, never a reversed interior dependency, never a port. Each row names its vocabulary OWNER and its DECODER, and per sibling pair the CLR reference direction is SINGLE: AppHost->Compute carries the intent/channel/proto compose, so every Compute<-AppHost edge (`ShedVerdict` -> admission `Rasm.Compute/ARCHITECTURE.md:113`, the governed M.E.AI draw `:118`, the livewire write-back `:122`, the `EncodingKind` alignment `:126`) crosses as a wire row or port contribution decoded Compute-side, never an AppHost CLR type referenced back — the mirror of `[V2]`'s decode law; the Persistence pair reads identically (AppHost decodes downward; `DataClassification` crosses on the M.E. compliance substrate `telemetry.md:355`, the presence beat as a Persistence-owned wire row per `[V5]`). A row with two CLR-referencing ends is the deleted form — the assembly cycle the strata law forbids. `ports.md:3` and `ARCHITECTURE.md:18` re-state honestly: seven inward DELEGATE ports are the only INWARD seam; the outward tier is named. The single most load-bearing undeclared coupling gains its ledger row: `Agent/capability` -> `Rasm.Compute` intent rail (`capability.md:3,237-238,300,564`; `Agent/runtime.md:46`; `mcp.md:336`). The Compute `[V13]` channels split re-points AppHost's references in the same motion (`TRANSPORT_AXIS` -> `transport.md`, `PROTO_VOCABULARY` -> `wire.md` — the counterpart obligation this brief waterfalled into the Compute brief).

### [V8_WIRE_CONTEXT_UNIFICATION]

One merge per app root, made true: `UpdateReceipt` registers on `AppHostWireContext` (the `provisioning.md:148` phantom dies) and EVERY `AppHostWireContext.Default.X` dereference audits against the `[JsonSerializable]` registry (`assay`-gated). Ruled default: `LiveWireContext` FOLDS into the `SuiteContracts.Wire` merge as a context argument — the WIRE_LAW's own merge form — and the standalone `LiveWireOptions.Json` (`livewire.md:806-825`) dies; the dead `WriteReceipt` row (`ports.md:137`) resolves against livewire's `WriteReceiptWire` (one of the two dies with the fold). Deciding criterion: livewire keeps a private context ONLY if a fence proves a per-lane serializer divergence (naming policy/ignore condition) the merged options cannot carry — then the split declares itself in ports' WIRE_LAW and the dead row still dies. The ruling executes in leg order: ports' leg RULES (the one live divergence to test is livewire's `DefaultIgnoreCondition.WhenWritingNull` at `livewire.md:813` against the merge options) and lands the final registry-row set; livewire's later leg EXECUTES the ruled fold — the registry never waits on a later page's harden.

### [V9_EGRESS_HONESTY]

Mandate item 2: `Wire/outbound.md` `HopPolicy` gains the delivery-honesty axis — `HopDelivery` (`BestEffort` | `AtLeastOnce` | `ExactlyOnceEffective`) as a per-hop policy column every `OutboundHop` row states (NATS ack-durable, webhook at-least-once, local-ipc best-effort, the wire-native gRPC/Arrow hop reading its row — the Persistence `[V3]` pump composes this axis per the waterfalled clause). `Wire/outbox.md` repairs land in the same motion: the fenced watermark advance THREADS — the bound-then-discarded `_advance` (`outbox.md:106-114`) lands in the returned `DeliveryReceipt` and `Defer`'s constant `0UL` dies; the three unused `Instant at` params (`:54,57,59`) become the row's dispatched-at column or die; the envelope DECODES the Persistence `[V3]` CloudEvents law (`id` = `OpLogEntry.ContentKey`, `Sequence` extension, `partitionkey` = `EntityKey`) with `CdcEnvelopeWire` re-anchored to the Persistence-owned envelope, never a parallel shape; the third-consumer row re-points `Sync/egress` -> `Version/egress` (`outbox.md:20,165`; `ARCHITECTURE.md:73`).

### [V10_DETERMINISM_CHANGEFEED_SEAM]

`Runtime/determinism.md` stops constructing the Persistence interior: `EventLog.ToOpLog` (`:118-125`) builds the 14-field `OpLogEntry` positionally with hardcoded `"determinism.command"`/`"command"`/`SnapshotCodec.JsonStj` and its own RESEARCH row (`:406`) admits the spelling is unverified. Ruled default: AppHost projects a NEUTRAL determinism log row (its own wire shape) and the PORT adapter maps it through the Persistence-owned changefeed vocabulary; the `EntityKind`/`ColumnFamily` literals become policy rows either way; the seam gains its ledger row (`Runtime/determinism` -> `Persistence/Version/ledger#CHANGEFEED`). Deciding criterion: direct construction survives ONLY if the Persistence DECISION publishes `OpLogEntry` as an explicit shared-wire type with a named factory — then AppHost composes the factory, never a positional `new` that shatters on a Persistence shape shift. Either branch, the seam is BIDIRECTIONAL — the reproducibility flagship dies without the read half: `Replay`/`Bisect`/`Counterfact` all ingress `Seq<LogEntry>` (`determinism.md:133,337,355`) and NO owner loads a recorded chain from the durable store (`ToOpLog` `:118` is the sole durable crossing, write-only; `ports.md:139` registers the wire row), so replay today survives only the recording process's memory — an in-memory-only replay under a crash-durable spine is a fiction. The PORT adapter gains the read case: fetch the projected rows by origin/sequence window, decode to `LogEntry`, and re-verify the hash chain through the existing verify fold (`ChainBroken` on breach, `:115`; every digest recomputed through the kernel `ContentHash.Of` entry per `[V18]`) BEFORE any `Replay`/`Bisect`/`Counterfact` fold consumes it — the durable log stays Persistence-owned rows read through the port, never a second AppHost log store, and cross-restart replay/bisect becomes the acceptance fact, not an aspiration. Forward consumer pressure on the page's exported vocabulary: `Rasm.AppUi` `Document/notebook.md` composes `RecomputeGraph` at per-cell granularity (RASM-CS-APPUI-BRIEF.md `[V11]`, AppUi the demanding consumer) — the leg keeps node identity caller-keyed and granularity-neutral; a granularity-hostile hardening forces AppUi's local-engine survival branch, the exact duplicate the one-owner claim exists to prevent.

### [V11_2PC_DRAIN_DISPOSITION]

The mandate's `:71` obligation: `Runtime/lifecycle.md:288`'s Stores-band 2PC in-doubt drain row reads `Rasm.Persistence/Query/transaction TwoPhase.InDoubt(db)` — a DELETED page — and the finalized Persistence single-`IDocumentSession` + same-tx outbox model mints no prepared transactions. Ruled default: RETIRE the 2PC in-doubt drain row (a drain of a nonexistent in-doubt set is dead apparatus; the maintenance-lease handoff row it sits beside survives untouched); it re-points to `graph#STORE_RAIL` ONLY if `RASM-CS-PERSISTENCE-DECISION.md` lands a `pg_prepared_xacts` in-doubt owner there — the deciding criterion, checked when the DECISION emits, before this page's leg runs. `ARCHITECTURE.md:71` re-points or dies with it.

### [V12_SECRETS_EXTRACTION]

`Runtime/secrets.md` extracts `config.md`'s `[06]-[SECRET_LEASE]` + `[07]-[CREDENTIAL_PEM]` (`config.md:304-499` — the credential-material lifecycle: acquire/renew/zeroize, RFC-7468 PEM bundle wire, the KMS-unwrap PORT `ARCHITECTURE.md:72` riding with it); `config.md` remains the pure config-admission owner (source axis / typed binding / policy values / kill switch). `identity.md`'s stale `#SECRET_MATERIAL` anchors (`identity.md:3,171,307` — config has no such section) re-point to the new page; `SecretFault` 4780 + `PemFault` 4790 ride over with registry rows. Runtime = 12 coherent pages. Forward consumer pressure: `Rasm.AppUi` `Document/export.md`'s PDF digital-signature arm (RASM-CS-APPUI-BRIEF.md `[V8]`) composes this owner's lease-scoped credential export for its `IDigitalSigner` material — acquire/renew/zeroize applies to the signing credential exactly as to any lease, and AppUi never holds raw key bytes; AppUi the demanding consumer.

### [V13_VERIFIED_DEFECT_SET]

The confirmed-defect set lands as repairs with the owning page's rebuild, never patches beside it. `outbound.md:521-523` + `topics.md:99-104`: both dedupe folds become read-modify-write INSIDE `Swap(current => ...)` — prune and add fold over the live cell value, the dedupe decision derives from the swap result, concurrent fans never lose records (one idiom, two sites, one fix shape). `laneguard.md:114-116`: the inline `0.90d`/`0.75d` CPU thresholds become named policy consts per the page's own const discipline and outbound's ":241 never an inline literal" law. `resources.md:233-255`: the five `throw new UnreachableException()` union-arm projections become total dispatch or typed rail failures. `profiles.md:315,323-326`: the declared-never-called `[LibraryImport] CopyPowerSourcesInfo` beside a hardcoded `ReadDarwin` stub — the dead declaration leaves until the honestly-flagged tier-3 residual realizes (signature-locked prose stays). `telemetry.md:3`: "seven-row `DataClassification`" corrects to the nine rows the fence carries (`:368-378`). `livewire.md`: the full adversarial harden per `[STRUCTURAL_AUTHORITY]`.

### [V14_WASM_COMPONENT_RULING]

`Sandbox/isolation.md:17,338` designs WASI-Preview-2 COMPONENT isolation while `api-wasmtime.md:32,152,163` documents core-module + WASI-Preview-1 only (158 types, no Component type; feed-verified: `Wasmtime` 44.0.0 is current). DECIDE at the leg with the criterion: `assay api` the pinned Wasmtime for a component-model surface; if absent (the expected outcome), isolation RE-SCOPES honestly to core-module + WASI-Preview-1 isolation — the granted-descriptor import-table law, fuel metering, and linear-memory caps all survive; the Preview-2 component claim dies — and the Component-Model runtime is NAMED as a recorded domain-gap growth line re-opened when the binding lands it. A vendored P/Invoke over the wasmtime C-API component surface is admissible ONLY on a consumer-named demand with the maintenance burden recorded — never a speculative admission.

### [V15_CSPROJ_BOUNDARY]

The csproj-vs-`ARCHITECTURE.md:119/:139` contradiction resolves by a PARAMETERIZED rule, not per-package taste: a package whose types appear in an AppHost fence signature or owner union STAYS a lib reference — `Wasmtime` (isolation embeds `Engine`/`Linker`), `MQTTnet`/`OPCFoundation`/`System.IO.Ports`/`FluentModbus` (livewire's `LiveClient` union carries `IMqttClient`/`Session`/`SerialPort`/`ModbusTcpClient`), `Grpc.AspNetCore` (companion's `ServiceHost` hosts the UDS server), `ModelContextProtocol.AspNetCore` (the mcp HTTP transport projection); a package only a composition root instantiates MOVES to app roots — `OpenTelemetry.Exporter.OpenTelemetryProtocol` (`csproj:45`), `Serilog.Sinks.Console`/`.File` (`csproj:56-57`), `Grpc.AspNetCore.Web` (`csproj:71` — zero fence consumers, `UseGrpcWeb` is app-root pipeline middleware) — matching `telemetry.md`'s own law (lib emits `ILogger` + minted sources only; exporter projection and Serilog sinks belong to composition roots). `ARCHITECTURE.md:119/:139` re-scope to exactly that rule (the reservation names exporter/sink/Kestrel-public-binding pins, not the protocol runtimes the fences carry); the `.api` tier placement follows the outcome (`api-otel-exporter.md`/`api-serilog-sinks.md` gain the app-root-reserved header note; the protocol catalogs stay AppHost-tier); central pins unchanged.

### [V16_API_TIER_HYGIENE]

The catalog motion executes as a verdict, not a chore: the 5 byte-identical AppHost copies DELETE (`api-nodatime`/`api-jsonpatch`/`api-redaction`/`api-hybrid-cache`/`api-grpc-core-api` — md5-verified; shared tier canonical per the two-tier law); the 7 re-scoped overlays (`api-nodatime-stj`/`api-hashing`/`api-tensors`/`api-thinktecture-json`/`api-grpc-client`/`api-grpc-aspnetcore`/`api-extensions-ai`) trim to the AppHost DELTA over the shared base; `api-grpc-client.md:330-341`'s Compute-framed `LOCAL_ADMISSION`/`RAIL_LAW` re-scopes to the real AppHost consumers (outbound `DISCOVERY_ATTACH` UDS `GrpcChannel.ForAddress`+`ConnectCallback`; coordination's resolver) and the custom `Resolver`/`LoadBalancer`/`Subchannel` surface ServiceDiscovery owns demotes; `api-modbus.md:93-95`'s RESEARCH tail — the sole catalog with unverified member signatures under a load-bearing livewire binding — member-verifies the `ReadHoldingRegisters<T>`/`*Async<T>` overload set against the restored FluentModbus assembly, folds the confirmed rows into the table, and deletes the tail; `api-languageext.md` AUTHORS at the substrate tier and `api-mapperly.md:151`'s phantom reference repairs; the flagged BCL debt gains substrate rows (`PemEncoding`/`X509Certificate2.CreateFromPem`/`CryptographicOperations.ZeroMemory`, `PosixSignalRegistration`/`PosixSignal`, `JsonSchemaExporter`/`JsonSchemaExporterOptions`, `JsonPatchError` — the `config.md:529-534` and `ports.md:114` open-verification flags close); `api-testing-seams.md` gains the cross-package test-substrate placement note. Every kept catalog's page anchor re-verifies against the post-campaign 31-page set.

### [V17_GOVERNANCE_TRUTH]

The perimeter becomes machine-checkably true in one motion. Ledger, stale rows: `ARCHITECTURE.md:71` per `[V11]`; `:73` -> `Version/egress`; `:85` per `[V6]`; `:86`'s phantom `Model/agent -> Rasm.Compute/Model` re-writes — AppHost OWNS the M.E.AI governance fold at `Agent/reasoning#MODEL_GOVERNANCE` (the row re-anchors there and its direction corrects or it dies); `:67`'s pack-wire consumer corrects `Runtime` -> `Sandbox` (the kernel row `Rasm/ARCHITECTURE.md:109` still targets `Runtime` — its correction is a RECORDED kernel-side counterpart obligation, AppHost the demanding consumer; the kernel `pack.md` names the AppHost-side consumer as the `GeometryPacking` sandbox capsule reading `Encode.Apply` + the `EncodingChannel` vocabulary, so the corrected row anchors on `solver.md`'s capsule and the solver consumer signature-locks on the landed kernel pack wire). GAP ROW: no AppHost page carries a `GeometryPacking` capsule anchor today though the kernel `pack.md` and Compute both cite it — the Sandbox leg MINTS the `Sandbox/solver.md#GeometryPacking` capsule section (the 6 `EncodingKind` rows signature-locked 1:1 on kernel `PackKind` keys, `Field`/`Toolpath` included), closing the unrealized obligation; `:80` per `[V5]`; `:60`'s `HostFingerprintWire` attribution verifies against determinism's ownership or re-anchors. Ledger, wired-undeclared rows land: capability->Compute (`[V7]`), determinism->ledger#CHANGEFEED (`[V10]`), determinism->kernel `Domain/identity#CONTENT_KEY` (`[V18]` — the second declared kernel edge beside the pack row), companion->ledger#PRESENCE + companion->provisioning `PeerRoster` (`[V5]`), outbound/companion/coordination->Compute wire/transport (post-split re-points, `[V7]`), telemetry `DataClassification`->Persistence `ClassificationGuard` (`telemetry.md:355`), profiles `FidelityScale`->Compute scheduler (`profiles.md:256`), plus the forward AppUi row declared both sides: topics/bus ← AppUi `Collab/sync` live-delta broadcast — session-ephemeral Loro wire as opaque payload rows on the one topics law, never durable truth (RASM-CS-APPUI-BRIEF.md `[V2]`b, AppUi the demanding consumer). Intra-package anchors: identity `#SECRET_MATERIAL` per `[V12]`; `ports.md:19` corrects to `Query/cache#L2_CONTRIBUTION` + `Element/identity` tenancy with ONE anchor per seam (the CACHE_PORT triple-anchor drift dies). Identity-plane truth: identity.md landed the runtime identity/authz plane PARTIAL, and the unrealized remainder REALIZES in identity.md's harden — the `IdentityPrincipal` ambient slot (the harden additionally MINTS the `Agent/identity.md#PRINCIPAL` section anchor: Persistence `Element/graph.md:222` + `identity.md:280` cite `Agent/identity#PRINCIPAL` and no such anchor exists on the page today), the `TokenLease` lifecycle, the `PolicyDescriptor` `[SmartEnum]` over the raw `IAuthorizationRequirement` gate (the DPoP binding inside `TokenLease` is verify-gated: the OpenIddict/IdentityModel member surface proves via the catalogs before fencing; absent support, the lease lands as expiry/refresh custody with DPoP a recorded growth line); features.md's targeted-delivery experimentation and coordination.md's cluster-membership election are LANDED capability — zero residual obligation, never re-opened. README router/groups and the `ARCHITECTURE.md` codemap re-emit the final 31-page set.

### [V18_KERNEL_IDENTITY_COMPOSE]

The conversion landed the federation identity owner and AppHost is the last per-call-site holdout. Kernel `Domain/identity.md` fences `ContentHash.Of(ReadOnlySpan<byte>)` → seed-zero `XxHash128.HashToUInt128` → `UInt128` under the law "every content hash in the federation composes this entry — one algorithm, one seed, no second hasher: a second hashing path anywhere in the federation forks identity and is the deleted form"; the kernel ledger binds Element/Persistence/Compute plus the python/ts peers (`Rasm/ARCHITECTURE.md:87-92`), and the Compute brief carries the peer clause ("never a per-call-site XxHash128"). AppHost's content-identity sites re-compose the kernel entry: `determinism.md`'s hex-string per-call-site digests (`:45` host fingerprint, `:103-104` append, `:183` rederive, `:216` recompute node, `:358` perturbation) and its LOCAL `ContentHash` `[ValueObject<string>]` (`:83-84`) — a colliding mint of the kernel's frozen `Rasm.Domain` name whose 16-hex `Genesis` is width-inconsistent with its own 32-hex digests and whose "a new hash algorithm is one `ContentHash` policy value" Growth row (`:75`) states the exact algorithm fork the federation law forbids; `orchestration.md:227`; `reasoning.md:224,247` (response/transcript digests); `capability.md:519` (attestation body digest); `config.md:356,437,494` (credential digests — riding to `secrets.md` with `[V12]`); `isolation.md:283` (component digest). The shape: every 128-bit content digest calls `Rasm.Domain.ContentHash.Of` over canonical bytes; `UInt128` is the identity currency and hex/two-lane-`ulong` encodings are boundary projections at the consuming seam (the identity page's own boundary law); the local carrier re-shapes to the typed chain-link value over the kernel digest and RENAMES (ruled: `ChainHash` — the frozen-name law reserves `ContentHash` for the kernel; the ports `[JsonSerializable]` row re-anchors inside `[V8]`'s registry audit); the `[V10]` read-back re-verify recomputes through the same entry, so cross-package and cross-runtime chain verification is one algorithm by construction (the ts `HostFingerprintWire` identity gate at `ARCH:60` inherits parity from the kernel's ts seam, `Rasm/ARCHITECTURE.md:92`). Manifest motion: `Rasm.AppHost.csproj` gains `<ProjectReference Include="../Rasm/Rasm.csproj" />` — the strata edge tier-0 assigns app-platform (`libs/.planning/architecture.md` `[02]`) and the peer csprojs already carry (`Rasm.Compute.csproj:8`, `Rasm.Persistence.csproj:8`); it lands in leg 1 with the roster motion, `packages.lock.json` regenerated. Precision carve-outs, recorded so no leg force-mines: `XxHash3` 64-bit ALLOCATION hashing is not content identity and stays raw `System.IO.Hashing` (`features.md` sticky bucketing, `determinism.md:54` stream-key derivation); the kernel `Deterministic` splitmix64 owner is kernel-algorithm-internal derivation, never AppHost's stream seeding; the kernel `Op`/`Eff<Env>` rails and the `Analysis` runtime are kernel-interior paradigm — AppHost's LanguageExt rails stay its own law. Ledger: `Runtime/determinism → csharp:Rasm/Domain/identity` lands per `[V17]`; the kernel-side counterpart (the `ARCHITECTURE.md:87-89` identity consumer family gaining its AppHost row) is RECORDED with AppHost the demanding consumer — never edited into the kernel by an AppHost leg.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every leg re-verifies its rows against disk before editing. Anchors are `libs/csharp/Rasm.AppHost/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md`; `CSPROJ:` the package csproj; `API:`/`SUBAPI:` the domain/substrate catalog tiers; `PBRIEF:`/`CBRIEF:` the upstream briefs (kernel anchors are `libs/csharp/Rasm/.planning/` pages); `DECISION:` `.archive/RASM-COMPONENT-PARADIGM-DECISION.md`.

| # | Class | Evidence |
|---|---|---|
| E1 | Band collisions | `Agent/identity.md:119` 4640 ≡ `Agent/mcp.md:125` 4640; `Runtime/features.md:154` 4700 ≡ `Sandbox/solver.md:40` 4700; `SupplyChainFault` name ×2 `provisioning.md:200`(1320)/`isolation.md:261`(4680); `Wire/coordination.md:172` 4520 ≡ Compute Remote `WireFault` 4520-4532; 1xxx event band vs `ProfileFault` 1100/`LifecycleFault` 1200/`UpdateFault` 1300; bare `Error.New(9300)` `bundles.md:90`; bare fence error `time.md:331`; `federation.md:22` hand census omits seventeen bands and its own 4640 twin; no registry anywhere |
| E2 | Port-seam inversion | `time.md:319-321` in-memory mint, `:328-331` in-memory fence, vs `:304` own store-fences prose; `orchestration.md:268-271` `Persist(WorkflowInstance, WorkflowStep, FencingToken)` pushes AppHost types down vs `:259` wire-stable-keys prose; `capability.md:420-423` `Debit`/`Token` cross `FencingToken`+`CostVector` downward (CAS delegation itself correct); PBRIEF V2 token-VALIDATING law + the waterfalled vector-debit clause; `capability.md:62-67` five-unit `CostVector` |
| E3 | Duplicate mechanisms | `SupplyChainGate` ×2: `isolation.md:293` record + `:314-320` `SemverGate` (`Version.TryParse` + `Split('-',2)` — the ARCH:148 deleted form) vs `provisioning.md:207` "never two verify paths"; membership ×2: `PeerRoster` (companion) vs `MembershipView` (coordination) with `provisioning.md:269,273` wiring `PeerRoster.Attached` against ARCH:80/`coordination.md:60`; dedupe lost-update idiom ×2 `outbound.md:521-523`/`topics.md:99-104` |
| E4 | Stale cross-package refs | `lifecycle.md:288` `Query/transaction TwoPhase.InDoubt` (deleted; PBRIEF E5); `outbox.md:20,165` `Sync/egress` (deleted; PBRIEF V3 -> `Version/egress`); `companion.md:23` `collaboration#PRESENCE_AND_BLOB` (owner `Version/ledger#PRESENCE`, PBRIEF E6); `ports.md:19` `indexes#L2_CONTRIBUTION`+`provisioning#TENANCY_RLS` (owners `Query/cache`/`Element/identity`); ARCH:71/:73/:85; `outbound.md:153,161`+`companion.md:23,168` Compute `channels#TRANSPORT_AXIS`/`PROTO_VOCABULARY` (re-point on CBRIEF V13 — counterpart clause waterfalled) |
| E5 | Ledger diff | Declared-unwired: ARCH:67 pack-wire target `Runtime` wrong (consumer Sandbox; the kernel row `Rasm/ARCHITECTURE.md:109` carries the same drift — a recorded kernel-side counterpart); ARCH:80 aspirational vs the wired `PeerRoster`; ARCH:86 phantom `Model/agent` folder; ARCH:60 `HostFingerprintWire` attribution questionable. Wired-undeclared: capability->Compute intent rail (`capability.md:3,237-238,300,564`; `Agent/runtime.md:46`; `mcp.md:336`); determinism->ledger#CHANGEFEED; companion->provisioning `PeerRoster`; companion->ledger#PRESENCE; telemetry `DataClassification`->`ClassificationGuard` (`telemetry.md:355`); profiles `FidelityScale`->Compute scheduler (`profiles.md:256`). Page cycles: none — the internal graph is a DAG |
| E6 | Wire-context drift | `provisioning.md:148` dereferences `AppHostWireContext.Default.UpdateReceipt`; `ports.md:122-139` registers 18 rows, `UpdateReceipt` ABSENT, `WriteReceipt` at `:137` unused; `livewire.md:806-825` standalone `LiveWireOptions.Json`+`LiveWireContext`+own converter factory vs ports WIRE_LAW one-merge |
| E7 | Lifecycle-seam blindness | `orchestration.md` `StepStatus` (pending/running/committed/waiting/compensated/failed) never maps `AssessmentOutcome` (`Dispatchable`/`Transient`/`Terminal`) or Compute `JobState`; CBRIEF V1 lands the sweep + Failed lifecycle AppHost must consume; mandate item 3; `orchestration.md:18` vs `:93` `Begin` fence carries the `FencingToken` the prose omits |
| E8 | Egress honesty gaps | `outbound.md` `HopPolicy` carries `HopIdempotency`/`HopTransport`/`HopRateLimit`, NO delivery-guarantee axis; `outbox.md:106-114` `_advance` discarded + `Defer` constant `0UL`; `:54,57,59` unused `Instant at`; `CdcEnvelopeWire` vs PBRIEF V3 CloudEvents envelope (`id`=`ContentKey`/`Sequence`/`partitionkey`) |
| E9 | Verified logic defects | `outbound.md:521-523`+`topics.md:99-104` stale-read `Swap(_ =>)`; `laneguard.md:114-116` inline `0.90d`/`0.75d`; `resources.md:233-255` five `UnreachableException` arms; `orchestration.md:218` throw on the rail; `solver.md:190-191` all-empty `PluginArtifact` feeding the load path; `profiles.md:315` declared-uncalled `[LibraryImport]` beside `:323-326` hardcoded stub |
| E10 | Governance drift | identity.md landed the runtime identity/authz plane PARTIAL — no `IdentityPrincipal` ambient/`TokenLease`/`PolicyDescriptor` (`[V17]` realizes the remainder); coordination.md cluster-membership election + features.md targeted-delivery experimentation are LANDED capability with zero residual obligation; `telemetry.md:3` "seven-row" vs nine fence rows (`:368-378`) |
| E11 | Catalog tier defects | 5 byte-identical cross-tier copies (md5-verified: nodatime/jsonpatch/redaction/hybrid-cache/grpc-core-api) + 7 re-scoped overlays; `API:api-grpc-client.md:330-341` Compute-framed admission prose; `API:api-modbus.md:93-95` RESEARCH tail, unverified overloads under a live livewire binding; `api-languageext.md` absent BOTH tiers + `SUBAPI:api-mapperly.md:151` phantom ref; BCL debt flagged at `config.md:529-534`/`ports.md:114`; `API:api-wasmtime.md:32,152,163` Preview-1-only vs `isolation.md:17,338` Preview-2 design; `api-commandline.md` zero page consumers (`config.md:103` is `Microsoft.Extensions.Configuration.CommandLine`) + `Grpc.AspNetCore.Web` zero fence consumers (`CSPROJ:71`; sole corpus token `companion.md:650` catalog-inventory prose); `bundles.md:326` open `[DUMP_ADMISSION]` beside the `:186` process-dump row; `Sigstore.Verification` phantom id in prior `[04]` (real: `Sigstore` 0.5.0, `props:244`) |
| E12 | csproj boundary | `CSPROJ:45` OTLP exporter, `:56-57` Serilog sinks, `:69-70` Grpc.AspNetCore server, `:88` MCP AspNetCore, `:96-100` MQTTnet/OPCUA×2/System.IO.Ports/Wasmtime — all named app-root-reserved by ARCH:119/:139 while livewire/companion/mcp/isolation fences carry their types in signatures; `:71` Grpc.AspNetCore.Web is the exception (zero fence tokens — it MOVES per `[V15]`) |
| E13 | Health roster staleness | `health.md:100-106` `Cache=("redis")` over `:76-77` singleton `IConnectionMultiplexer` + `Broker=("kafka")` single row vs PBRIEF V7 (raw Redis pruned; L2 rides the caching transit) + PBRIEF V3 (NATS spine anchor; Kafka a conditional sink row); ARCH:85 carries the stale roster; `AspNetCore.HealthChecks.Nats` 9.0.0 feed-verified absent from the manifest |
| E14 | Mandate-clean confirmations | No parallel job-state store exists (executor is `CommandDispatch`; state rides `StepStateSeam`) — `[V3]` seals, nothing tears down; `outbox.md` names three keyed consumers over ONE op-log consistent with PBRIEF V3; `capability.md#DistributedBudget` CAS delegation shape confirmed store-side; reasoning/Compute embedding split confirmed both sides (CBRIEF V15; `reasoning.md` owns the generative M.E.AI port, Compute retrieval-only); rosters are seed DATA corpus-wide, zero bare duration literals, zero `DateTime.UtcNow`/`Stopwatch` |
| E15 | Flagship chain breaks | `determinism.md:133` `Replay(runtime, Seq<LogEntry>, live)`, `:337` `Bisect(Seq<LogEntry>, ...)`, `:355` `Counterfact` — all ingress in-memory logs; `ToOpLog` (`:118`) write-only, zero read-back/rehydrate owner corpus-wide (`ports.md:139` registers the wire row only); `orchestration.md:283-293` `CrashResume` boot-only over `:271` `StepStateSeam.InFlight` — no lease-expiry reclaim, a dead node's running instances orphan; `:165-186` Signal park + `SignalTimeout` bound exist with no named wake producer for the `[V3]` Dispatchable path; PBRIEF V2's op-union names write cases only — the READ cases are the sixth waterfall clause |
| E16 | Landed-kernel integration gaps | Kernel `identity.md:13-14` one-hasher law + consumer ledger `Rasm/ARCHITECTURE.md:87-92`; the Compute-brief peer clause; AppHost per-call-site `XxHash128` digests `determinism.md:45,103-104,183,216,358` + the local `ContentHash` mint `:83-84` (frozen-name collision; 16-hex `Genesis` vs 32-hex digests; `:75` algorithm-as-policy Growth row), `orchestration.md:227`, `reasoning.md:224,247`, `capability.md:519`, `config.md:356,437,494`, `isolation.md:283`; `Rasm.AppHost.csproj` carries ZERO ProjectReference while `Rasm.Compute.csproj:8`/`Rasm.Persistence.csproj:8` carry the kernel edge; kernel `pack.md` names the AppHost `GeometryPacking` capsule (no AppHost page carries the anchor — the gap row) + kernel `ARCHITECTURE.md:109` pack row still targets `Runtime`; the kernel substrate `Fault` discriminant 9104 (`rails.md` `[04]`) un-mirrored by any AppHost band prose |

Sound surfaces (preserve; touch only where a verdict names them): `modules.md` (9.5 — the composition-root exemplar), `ports.md` (9.5 minus E5/E6 rows), `config.md` (9.5 minus the `[V12]` extraction), `determinism.md` (9.5 minus E5's `ToOpLog`), `resources.md` (9.5 minus E9 throws), `features.md` (9.5 minus the E1 band), `laneguard.md` (9.5 minus E9 literals — the `ShedVerdict` seam is correctly homed and Compute-confirmed), `reasoning.md` (9.5), `mcp.md` (9.5 minus the band), `federation.md` (9.5 minus the `:22` census), `identity.md` (9.5 minus band/anchors; gains the E10 realization), `capability.md` (9.5 minus E2/E5 rows), `telemetry.md` (9.5 minus the count), `bundles.md` (9 minus the 9300), `health.md` (9.5 minus the roster), `companion.md` (9.5 minus the anchor), `outbound.md` (9.5 minus E8/E9), `coordination.md` (8.5 — rides `[V2]`/`[V5]`), `topics.md`/`outbox.md` (9 minus repairs), `provisioning.md` (9.5 minus the membership row), `solver.md` (8.5 minus band/stub), and the `ARCHITECTURE.md` `[04]`/`[05]` prohibition blocks (kept verbatim except the two `[V15]`-re-scoped sentences).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; the verdicts carry the shapes that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Fault space (registry) | 2 | 9.5 | type-enforced `FaultBand` registry; two collisions re-partitioned; event/fault disjointness; foreign-neighborhood mirror rows; zero bare `Error.New`; prose censuses dead |
| Coordination + fencing (time/coordination/step-state/budget) | 5 | 9.5 | decode-only PORT adapters; store-validated fence (in-memory mint dead); projected step-state row; per-unit vector debit; one election rail |
| Workflow orchestration | 8.5 | 9.5 | `AssessmentOutcome`/`JobState` boundary map (Transient retry, Dispatchable sweep, Terminal saga); two-tier step wake (drain signal + `SignalTimeout` re-drive against the lifecycle-aware cache); fenced orphan-instance reclaim (node death, not just reboot); no-parallel-store sealed; prose/fence drift + throw repaired |
| Reproducibility (determinism/replay) | 7 | 9.5 | bidirectional oplog seam — durable read-back + chain re-verify feeding `Replay`/`Bisect`/`Counterfact` across process restarts; neutral projected row (literals -> policy rows); the changefeed ledger row landed; every content digest composed through the kernel `ContentHash.Of` entry with the local carrier renamed (`[V18]`) |
| Agent plane (capability/reasoning/mcp/federation/identity/runtime) | 9 | 9.5 | registry bands; declared Compute intent-rail seam; identity `TokenLease`/`PolicyDescriptor`/ambient-principal realization; stale anchors dead |
| Wire egress (outbound/outbox/topics) | 8.5 | 9.5 | `HopDelivery` honesty axis; watermark threaded into receipts; dedupe folds race-free; CloudEvents envelope decoded, not re-minted |
| Wire external (livewire/companion/coordination) | 8.5 | 9.5 | one merged wire context; presence seam declared both sides on the corrected driver; livewire full harden; membership two-tier |
| Sandbox | 6.5 | 9.5 | one admission owner (dup gate + `SemverGate` dead); real `PluginArtifact`; WASM ruling honest; `EncodingKind` rows signature-locked on the kernel `PackKind` `field`/`toolpath` rows + the waterfalled Compute residency extension; the `GeometryPacking` capsule anchor landed (`[V17]`) |
| Observability | 9 | 9.5 | integration-first probe roster (NATS row, transit-L2 cache probe, sink-tracking brokers); typed `SupportFault`; nine-row count |
| Governance perimeter (ledger/catalogs/manifest/csproj) | 4 | 9.5 | ledger complete both directions; 31-page codemap; catalogs deduped + delta-trimmed + BCL/languageext authored; csproj boundary rule applied |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` + the `.Json` adapter as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Atom` rails at every entry (the `[V16]` catalog pins the beta surface), `System.IO.Hashing` `XxHash3` allocation hashing with every 128-bit content digest composing the kernel `ContentHash.Of` entry (`[V18]` — never a per-call-site `XxHash128`), NodaTime under `ClockPolicy`, redaction binding `DataClassification` at every exporter seam — a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package.
- `Rasm` (kernel ProjectReference, `[V18]`) — the APP-PLATFORM stratum's one downward compile edge, first-class composition material: `Rasm.Domain.ContentHash.Of` is the content-identity entry every 128-bit digest composes; `Drawing/pack.md`'s `Encode.Apply(PackOp)` + `EncodedGeometry`/`EncodingChannel`/`PackKind` is the sandbox geometry-marshalling vocabulary the `solver.md`/`isolation.md` legs compose (the kernel `pack.md` names the AppHost `GeometryPacking` capsule as its declared consumer — the Sandbox leg mints the anchor). Recorded non-obligations, never force-mined: the kernel `Deterministic` splitmix64 owner (kernel-algorithm-internal derivation, not AppHost's stream seeding), the `Op`/`Eff<Env>` substrate rails, and the `Analysis` runtime — kernel-interior paradigm; AppHost's LanguageExt rails stay its own law.
- `AspNetCore.HealthChecks.Nats` (ADD, Apache-2.0 — Xabaril family): the `[V6]` NATS probe row binding the pooled `NATS.Net` connection; member set `assay api`-gated at admission; catalog stub with the admission, deepened at first compose — the five sibling health catalogs are the authoring template.
- `Microsoft.Extensions.AI`(+middleware) — the governance fold stays the reference mine (`GoverningChatClient` subclass law, `UseOpenTelemetry`/`UseDistributedCache`/`UseFunctionInvocation`); the TTS/Realtime/HostedFile clients stay recorded-unconsumed inventory, never force-mined.
- `ModelContextProtocol`(×3) — the task/sampling/elicitation session verbs stay mined; the AspNetCore transport rides the `[V15]` fence-signature rule; `AsSamplingChatClient` attribution note carries into the reasoning rewire.
- `Sigstore` (0.5.0, MIT — the stub's `[01]` package row and the `props:244` pin; `Sigstore.Verification` is a PHANTOM id, feed-verified absent) + `NuGet.Versioning` — consumed by the ONE `[V4]` admission owner (`TryVerifyDigestAsync` stub-confirmed, `api-sigstore.md:103`); the `[INTEGRATION_STACK]` legs (semver/identity/provisioning/resilience/telemetry) re-anchor to `Sandbox/admission.md`.
- `Velopack` — `UpdatePendingRestart`/staged-resume rows stay; the raw ```csharp fence styling normalizes to the tier's table grammar during the `[V16]` pass.
- `Wasmtime` — the `[V14]` ruling owns it: core-module + Preview-1 depth (fuel, epochs, linear-memory caps, `Linker.DefineWasi`) documents to full honesty; any Component claim is `assay`-gated.
- `FluentModbus` — the `[V16]` member-verify closes the RESEARCH tail; `livewire.md`'s `ModbusTcpClient`/`ModbusRtuClient` bindings re-verify against the confirmed overloads.
- `OPCFoundation.NetStandard.Opc.Ua`(+PubSub) + `MQTTnet` + `System.IO.Ports` — livewire's certified stack stays; the opcua server-side/cert-authoring/reverse-connect over-documentation demotes to inventory notes (client-only charter).
- `Polly.Core`/`.Extensions`/`.RateLimiting` + `Microsoft.Extensions.Http.Resilience` — mined verbatim by outbound/laneguard; the `[V9]` honesty axis is policy DATA over the existing rows, zero new resilience surface.
- `OpenFeature` + `Cronos` + `Microsoft.Extensions.ServiceDiscovery` + `System.Threading.Tasks.Dataflow` + `Microsoft.Extensions.Caching.Hybrid` + `Microsoft.Extensions.ObjectPool` + `Scrutor` + `FluentValidation`(+DI) — mined-confirmed, zero action beyond registry-band touches.
- IdentityModel ×4 + `OpenIddict.Client` — the `[V17]` `TokenLease` realization deepens the token-lifecycle rows; the DPoP member surface verifies via catalogs before any fence claims it.
- `Microsoft.ML.Tokenizers`(+cl100k/o200k) + `System.Numerics.Tensors` — the cost-broker and embedding-rank mines stay the tier exemplars; the tensors overlay trims per `[V16]`.
- BCL catalog debt (`[V16]`): `System.Security.Cryptography` (`PemEncoding.Write/TryFind`, `X509Certificate2.CreateFromPem/RawData`, `CryptographicOperations.ZeroMemory`), `System.Runtime.InteropServices` (`PosixSignalRegistration`), `System.Text.Json.Schema` (`JsonSchemaExporter`), `JsonPatchError` — substrate rows closing the flagged open verifications.
- Host spine mined-confirmed (`api-hosting.md`/`api-hosting-lifetimes.md`/`api-di.md`/`api-options.md`/`api-binder.md`/`api-config.md`/`api-config-providers.md`/`api-logging.md`): lifecycle.md's `IHostedLifecycleService` boot/drain fold, the Systemd sd_notify/watchdog lifetime, modules.md's declaration-order DI fold, config.md's source axis + `IOptionsMonitorCache` invalidation (`config.md:244`) — zero action beyond registry-band touches; `[V12]` re-anchors the secret-source rows its extraction moves.
- Observability spine mined-confirmed (`api-otel.md`/`api-otel-hosting.md`/`api-otel-instrumentation.md`/`api-telemetry.md`/`api-telemetry-abstractions.md`/`api-pyroscope.md`/`api-resource-monitoring.md`/`api-health.md`/`api-serilog.md`/`api-serilog-hosting.md`): telemetry.md's `SignalGovernance`/`GlobalLogBuffer`/latency-context/`SpanProfileProcessor` weave plus laneguard.md+health.md `IResourceMonitor` — AUDITED SEAL KEPT: `telemetry.md:227`'s EF/gRPC-instrumentation rejection re-verified (native `Activity` emission carries those spans); no OTel instrumentation ADDs.
- `Microsoft.AspNetCore.Authorization` (`api-authorization.md`) — identity.md's raw `IAuthorizationRequirement` gate is the consumer; the `[V17]` `PolicyDescriptor` realization is the deepen, riding the stub's no-`HttpContext` `AddAuthorizationCore` law.
- gRPC tier (`api-grpc-aspnetcore.md`/`api-grpc-client.md`) — companion's `ServiceHost` UDS host + `MapGrpcHealthChecksService` (`companion.md:272`) and outbound's `DISCOVERY_ATTACH` mine them; the `Grpc.AspNetCore.Web` `[PACKAGE_SURFACE]` block (`api-grpc-aspnetcore.md:21-24`) gains the app-root-reserved header note with its `[V15]` MOVE.
- `System.CommandLine` (INTEGRATE, MIT, 2.0.9 stable, `api-commandline.md`): zero page consumers, so the roster law demands the row before any removal — the app-root verb table lands as seed DATA on `modules.md`'s composition doctrine: `ParseResult`-driven projection into `CommandDispatch.Run`, the `[V10]` replay/bisect ingress, and bundles' `ExternalCommand` capture verb — one boundary adapter, never a second dispatcher; REMOVE only if the leg proves every verb already rides `ControlService` for every host modality (the named redundancy).
- `Microsoft.Diagnostics.NETCore.Client` (ADD, MIT — dotnet/diagnostics): fills `bundles.md:326` `[DUMP_ADMISSION]` for the process-dump artifact row (`bundles.md:186`) — `DiagnosticsClient.WriteDump(DumpType.*)` + `EventPipeSession` bound inside the capture fan's caps/redaction/truncation law; `.api` stub at admission; `Microsoft.Diagnostics.Tracing.TraceEvent` (ADD, MIT — perfview) rides the same fill RULED, not conditional: the row's charter already states dump AND gcdump capture (`bundles.md:186`), the `.gcdump` graph format's one categorical owner is `DotNetHeapDumpGraphReader`, and raw EventPipe alone cannot write it — deleting the gcdump column to dodge the admission is capability deletion, the forbidden form.
- `BACnet` (ADD, MIT — ela-compil, netstandard2.0 asset): the building-management protocol the industrial studio lacks — one `ExternalTransport` row + one `LiveClient` case landing with livewire's full harden, members `assay api`-gated; KNX stays a recorded growth line (no maintained categorical owner — `KNX.Net` dormant; admission re-opens when a maintained owner emerges); the row's named forward consumer is the Compute inverse-UQ/twin-calibration lane's metered-data ingress, decoded at the seam exactly as MTConnect feeds Fabrication (RASM-CS-COMPUTE-BRIEF.md `[V12]` recorded growth).
- `MTConnect.NET` (ADD, MIT — TrakHound): the machine-tool connectivity lane the AEC-fabrication chain demands — client/SHDR/HTTP/MQTT streaming as one `ExternalTransport` row + one `LiveClient` case beside BACnet, members `assay api`-gated; `Rasm.Fabrication` `Process/magazine` mid-job tool-life reload is the named demanding consumer, and `Verify/probing` measured-feature/work-offset observations the second (both pin the `-Common` model slice only and firewall transport here, `RASM-CS-FABRICATION-BRIEF.md` `[04]`/`[V7]`); OPC-UA/umati stays on the kept `OPCFoundation` runtime, license posture unchanged.
- Shared-tier non-consumers (`api-csparse`/`api-mathnet-*`/`api-unitsnet`/`api-unicolour`/`api-messagepack`/`api-thinktecture-messagepack`/`api-protobuf`/`api-nodatime-protobuf`/`api-highperformance`/`api-system-configuration`/`api-grpc-common`/`api-grpc-tools`/`api-grpc-client-web`) are sibling-owned substrate with no AppHost consumer — no force-mining, mirroring the recorded Mapperly/QuikGraph/Generator.Equals REJECT.
- ROSTER rows: ADD `AspNetCore.HealthChecks.Nats` + `Microsoft.Diagnostics.NETCore.Client` + `Microsoft.Diagnostics.Tracing.TraceEvent` + `BACnet` + `MTConnect.NET` (each per its row above); REFERENCE `Rasm` (the `[V18]` kernel ProjectReference — a project edge, not a package pin); INTEGRATE `System.CommandLine`; MOVE `OpenTelemetry.Exporter.OpenTelemetryProtocol` + `Serilog.Sinks.Console`/`.File` + `Grpc.AspNetCore.Web` to app roots (`[V15]`; central pins stay); REMOVE — none (both zero-consumer finds dispose as integrate/move; supersession sweep verdict: `MQTTnet`/`FluentModbus`/`OPCFoundation`/`Cronos`/`Velopack`/`Wasmtime`/`Scrutor`/`OpenFeature` re-verified categorical-best, zero REPLACE demanded); REJECT recorded — Mapperly/QuikGraph/Generator.Equals force-mining (per `[ROSTER_RECONCILIATION]`), a distributed-lock or outbox library beside the Persistence-owned coordination store (PBRIEF V2's `DistributedLock.Postgres`/`WolverineFx` rejections bind AppHost symmetrically — the PORT adapters are the only lawful shape), hosted crash-reporting services for process evidence (pay-tiered gate; the `[DUMP_ADMISSION]` fill owns it).
- Recorded growth register — the deferred-capability lines this brief owns, each anchored on its ruling verdict: the Component-Model WASM runtime (`[V14]`), the DPoP binding if verify-gated out (`[V17]`), the PowerProbe tier-3 realization (`[V13]`/`[06]`), a Persistence-DECISION-gated 2PC re-point if `[V11]`'s criterion fires, and KNX when a maintained categorical owner emerges (the `BACnet` row above).

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with THIS brief as the campaign parameter. Within a leg the listed order IS dependency order. The sequencing law:

1. **PERIMETER FLOOR** — the `[V1]` registry landing COMPLETE on `Runtime/lifecycle.md` (every band assigned including the re-partitioned 4630/4710/4540 rows, the event stride, the foreign mirrors — a duplicate integer unrepresentable from the first leg; each union's re-derivation lands with its owning page's leg; later legs touching `lifecycle.md` treat the landed `[FAULT_TABLES]` block as settled law — rows extend, never re-derive); the two extractions land as pages with surgical donor excision (`[V12]` `Runtime/secrets.md` from config; `[V4]` `Sandbox/admission.md` from provisioning — full donor rebuilds ride their own legs); the `[V16]` catalog motion + `[V15]` csproj boundary + the `[04]` roster motion (Nats/diagnostics-client/TraceEvent/BACnet admissions, the `[V18]` kernel ProjectReference, the `Grpc.AspNetCore.Web` move — the manifest gates every later page's imports and claims); the `[V17]` own-ledger corrections and ARCH/README codemap re-emit to the 31-page set.
2. **RUNTIME SPINE** — `time.md` (`[V2]`a fencing re-charter), `orchestration.md` (`[V2]`b + `[V3]` + the `:227` `[V18]` digest in one touch), `config.md` (post-extraction pure-admission rebuild; the `[V12]`-moved credential digests carry their `[V18]` re-route into `secrets.md`), `lifecycle.md` (`[V11]` drain disposition — its criterion checked against the emitted Persistence DECISION before this leg runs), `determinism.md` (`[V10]` + `[V18]` — the chain digests compose the kernel entry, the local `ContentHash` carrier renames), `resources.md` (`[V13]` throws), `ports.md` (`[V7]` WIRE_LAW re-statement + `[V8]` registry rows + the E4 anchor corrections), `modules.md` (+ the `[04]` `System.CommandLine` verb-table row)/`profiles.md`/`features.md`/`laneguard.md` cold-harden with their registry bands and `[V13]` literal repairs.
3. **AGENT + WIRE** — `capability.md` (`[V2]`c budget projection + the `[V7]` ledger row + the `:519` `[V18]` digest), `identity.md` (`[V17]` realization + `[V12]` re-anchors + the 4630 re-band), `reasoning.md` (bands + the `:224,247` `[V18]` digests)/`mcp.md`/`federation.md` (bands; the `:22` census dies)/`Agent/runtime.md` cold; `outbound.md` (`[V9]` honesty axis + `[V13]` dedupe), `outbox.md` (`[V9]` watermark/envelope/anchor), `topics.md` (`[V13]`), `coordination.md` (`[V2]`d + `[V5]` + the 4540 re-band), `companion.md` (`[V5]` presence re-point), `livewire.md` (`[V8]` context fold + the full `[V13]` harden + the `[04]` `BACnet` transport row).
4. **SANDBOX + OBSERVABILITY + CLOSEOUT** — `isolation.md` (`[V4]` consume + `[V14]` WASM ruling + the `:283` `[V18]` digest), `provisioning.md` (`[V4]` consume + `[V5]` membership re-point), `solver.md` (`[V4]` real artifact + the 4710 re-band + the `EncodingKind` gate stated against the kernel `PackKind` `field`/`toolpath` rows and the waterfalled Compute residency rows, with the `[V17]` `GeometryPacking` capsule anchor landed), `health.md` (`[V6]`), `telemetry.md` (`[V13]` count + `[V17]` rows), `bundles.md` (typed `SupportFault` + the `[04]` `[DUMP_ADMISSION]` fill); the final `[V17]` governance truth pass (README/ARCHITECTURE re-emit); acceptance dry-runs.

Per-leg closeout: residuals resolve in-run — every pass agent repairs the cross-file ripples its own work exposes in-pass, and the leg's serialized per-wave tail closes index docs and central manifests before it returns; `ARCHITECTURE.md`/`README.md` close in the leg that changes their facts; `.api` stubs and manifest edits land with the leg that admits, moves, or first composes the package, and the leg that first composes an admitted package deepens its stub; cross-package counterpart obligations (the Persistence coordination op-union/token wire `[V2]`, the Persistence egress envelope + honesty read `[V9]`, the Compute `GeometryEncoding` rows and channels re-anchors, the Element/Compute lifecycle vocabulary `[V3]`) are RECORDED in leg returns with AppHost named as the demanding consumer — never edited into sibling interiors; the six pre-landed waterfall clauses are verified, not re-applied; every `[V2]`/`[V9]`/`[V10]` signature-lock against a Persistence-owned spelling resolves against the emitted `RASM-CS-PERSISTENCE-DECISION.md` when it exists and otherwise locks on the Persistence brief's ruled defaults with the re-verify recorded as a leg residual (the `[V11]` gate generalized); the E4 channels re-points execute against the landed Compute split — absent it, the ledger row records the post-split target gated on Compute `[V13]`. Campaign acceptance closes on composed contracts, not the ledger: (a) a coordination dry-run — a fenced Budget debit whose stale-token replay REJECTS store-side as a decoded `LeaseFenced` fault, a step-state CAS persisting the projected primitive row and rehydrating it, an outbox drain whose fenced watermark advance lands IN the returned `DeliveryReceipt`, a redelivered event deduping by `id`=`ContentKey`, and a recorded command chain read back through the `[V10]` port in a FRESH process re-verifying through the kernel `ContentHash.Of` entry (`ChainBroken` unreachable on an untampered log) and replaying `Matched` — composes the `[V2]`+`[V9]`+`[V10]` adapters against the PORT rows (the exact counterpart of the Persistence brief's own acceptance (b)); (b) a fault-space audit — every `Expected.Code` resolves through exactly one registry row, `rg 'base\(detail, [0-9]'` returns zero literal-banded unions, the 4640/4700 collisions unrepresentable; (c) a supply-chain dry-run — a release asset AND a plugin artifact admit through the ONE gate, an all-empty artifact rejects `AttestationMissing` on the solver load path; (d) a lifecycle dry-run — a Compute assessment step returning `Transient` re-drives bounded, `Dispatchable` parks and wakes off the drain signal (or re-drives at its `SignalTimeout` bound against the lifecycle-aware cache), `Terminal` compensates, a killed node's running instance reclaims under a fresh fenced lease from its committed cursor while the dead holder's late advance rejects `LeaseFenced`, with zero parallel job-state store; (e) the perimeter audit — every ledger row composed in a fence and every fence crossing ledgered, every catalog anchoring a live page, `dotnet restore` clean after the manifest motion. A dry-run reaching for a missing owner reopens `[V2]`/`[V4]`/`[V9]` as residuals.

## [06]-[OUT_OF_SCOPE]

- `Rasm.Persistence` interiors: the coordination op-union, token-validating CAS, outbox cursor, and egress pump implementations are PBRIEF `[V2]`/`[V3]`'s — AppHost decodes; the four waterfall clauses this brief landed there (vector debit, coordination READ cases, presence driver, honesty read) are verified in place; no further upstream edit is open.
- `Rasm.Compute` interiors: the `JobGraph`/assessment sweep, the channels split, and the `GeometryEncoding` case rows are CBRIEF `[V1]`/`[V13]`/Tensor-leg work — the two waterfall clauses this brief landed there are verified in place; AppHost re-points and signature-locks.
- Kernel interiors: the pack/`EncodedGeometry` wire is the kernel's (`Drawing/pack.md`, landed); AppHost corrects its ledger row target and signature-locks the Sandbox consumer. The landed kernel pages (`Domain/identity.md`, `Drawing/pack.md`) are composed as-is; the kernel-side counterpart rows — the `Rasm/ARCHITECTURE.md:87-89` identity consumer family gaining its AppHost row, the `:109` pack row's Runtime->Sandbox consumer correction — are RECORDED kernel-side obligations, never edited by AppHost legs.
- AEC-domain references — forbidden by standing law (`ARCH:150` ArchUnitNET rule); the `ProjectionContext` primitives-only contribution stays.
- App-root composition: Kestrel public binding, OTLP exporter wiring, Serilog sink projection, host-event attachment — the future APP stratum consumes the `[V15]`-clarified boundary; AppHost never grows a composition root of its own.
- The `PowerProbe` IOKit/SMC native realization — an honestly-flagged tier-3 live-host residual, signature-locked, not this campaign's closeout blocker.
- The federation-wide fault-band renumber beyond the AppHost registry — escalation path only if the `[V1]` re-partition cannot fit the 1xxx/4xxx space it already occupies.
- TypeScript/host projections beyond the declared wires (`FlagVerdictWire`, `BindingStatusWire`/`CoercedValueWire`/`WriteReceiptWire`, the suite wire law) — consumed at the boundary, never re-planned here.
