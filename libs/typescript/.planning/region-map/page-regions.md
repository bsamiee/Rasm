# [TS_REGION_MAP_PAGE_REGIONS]

TS branch ledger — one ledger with five per-domain blocks plus the branch non-domain block, mirroring the C# suite ledger shape. Row shapes and protocol live in the [suite standard](../../../csharp/.planning/README.md) ledger-protocol section. Per the single-state-surface law, page-finalization state lives only in the owning charter PAGE_INDEX `[STATE]` column; this ledger carries the one-sentence concern law per page and no page-state cell, routing to the charter for state. The browser `web` folder is split into two bounded domains: `ui` (UI/UX/components, `./ui`) and `platform` (SPA infrastructure entry, `./web`).

[INTERCHANGE]:

- interchange/transport.md: WireTransport + WireClients over one shared grpc-web transport, TransportCapabilityWire gating the grpcWeb method set, the polymorphic correlation/trace/credential interceptor, and the buf descriptor pipeline as the build-time codegen input edge emitting gen/*_pb.ts.
- interchange/codec-rails.md: the single DecodeRail six-codec family read by codec key, EncodeRail as the direction-keyed write face, SchemaRefinement brand/filter decode-enforcement rows, GeometryRail embedded-geometry, ArtifactFrameRail content-addressed reassembly, and FaultDetailRail reconstructing the full ComputeFault/StoreFault/HopFault/ConfigError set as one Data.TaggedError family bound by Match.tagsExhaustive.
- interchange/gateway-and-quarantine.md: QuarantineFold tolerance terminal, the absorbed CONTRACT_INVENTORY and versioning-law fences, and CommandGateway + IntentRegistry as the outbound-verb face of WireClients co-located in the transport-owning domain by outbound-effect altitude.

[PROJECTION]:

- projection/fold-algebra.md: the one StreamPolicy bounded-Schedule reconnect + Stream-operator vocabulary, the keyedFold combinator over a Stream/receipt sequence into a SubscriptionRef keyed map, and the five stream stores RuntimeFeed/HealthStore/SnapshotFeed/ProgressStore/ConflictPresenceStore, transport-free.
- projection/envelope-and-evidence.md: ReceiptStore/EvidenceFeed/AvailabilityStore as fold rows of the same algebra, the ReceiptEnvelopeCarrier binding every structured-text payload, and the SkewBand HLC correlation fold as a first-class confidence-interval projection output.

[UI]:

- ui/binding.md: AtomBinding the sole React state binding, DeepLinkBinding query-string state, the url-as-state/undo-redo/offline client-state folds over the projection stores, and the development-build atom-inspector row; emits intents only through the interchange CommandGateway, holds no domain state, authors no decode.
- ui/render-surfaces.md: the read-only observation routes EvidenceTimelineRoute (HLC-ordered with SkewBand confidence interval), BenchmarkRoute (fingerprint-gated), and CollectorPanel, plus GeoSeriesSurface over one Schema.Literal GeoSeriesLayer union owner; GlbViewport a REFINEMENT_HORIZON row.
- ui/component-system.md: the one InteractionRole Schema.Literal vocabulary owner-block over the eight roles with one RoleBehavior contract per role, ThemeTokens color-space tokens, and CssVarSync runtime CSS-var sync; the react-aria per-component .tsx patterns discarded.

[PLATFORM]:

- platform/host-runtime.md: CompositionRoot one Layer graph providing the closed five app-services and composing ui+interchange+projection into one runtime, BrowserPlatform, AuthSession arctic OIDC/PKCE credential boot edge, and RuntimeConfig typed-config owner; the browser SPA application entry.
- platform/platform-substrate.md: SelfTelemetry host OTLP-web export edge plus MetricRegistry bounded vocabulary over the WebSdk including the Core Web Vitals instrument family, BuildPipeline build pipeline emitting the PWA worker asset, DecodeWorkerPool Worker.makePool transferable-buffer offload including off-main-thread ArtifactFrameRail reassembly, and LocalPersistence over the Effect KeyValueStore composing idb-keyval as backing only.
- platform/routing-navigation.md: AppRouter the client routing + navigation owner over one Schema.Literal route-key axis with a RouteParamCodec round-trip composing nuqs over a history SubscriptionRef, navigation as typed Effect push/replace/back transitions, NavigationGuard gating a route on AuthSession status + projection AvailabilityStore, and the scroll-restoration + pending-navigation cell; ZERO routing package admitted.
- platform/service-worker.md: ServiceWorkerHost the service-worker/PWA owner over one Schema.Literal CacheStrategy axis with the install/activate/skipWaiting update lifecycle as an Effect-native fold, the offline-first navigation fallback, BackgroundSyncReplay draining the platform-substrate LocalPersistence offline command queue into the interchange CommandGateway on redial, and the registration/update-available SubscriptionRef the ui surfaces a refresh from; BuildPipeline emits the worker asset, ServiceWorkerHost owns its runtime lifecycle.
- platform/error-boundary.md: CrashTelemetry the error-boundary + crash/exception telemetry sink over react-error-boundary, the global window.onerror/unhandledrejection capture marshalled into the interchange FaultDetail family (never a parallel error type), the crash-report fold shipping a sanitized CrashReport envelope through SelfTelemetry to the collector (the only telemetry path), the recovery-affordance SubscriptionRef the ui error fallback reads, and the session-breadcrumb ring buffer.
- platform/feature-flags-config.md: RemoteConfig the browser READ-side feature-flag + remote-config owner — a RuntimeConfig-fed remote-config fetch decoded once into one FlagSet SubscriptionRef, FlagEvaluation a total Match dispatch over a Schema.Literal flag-key axis consuming the services persistence FeatureFlags percentage-bucket/variant vocabulary as settled (never re-authored), and the poll/refresh Schedule; the FlagSet decode shape is the only platform-local Schema.
- platform/web-vitals.md: PerformanceBudget the performance-budget/web-vitals observability owner — the Core Web Vitals (LCP/INP/CLS/TTFB/FCP) capture over one Schema.Literal VitalMetric axis feeding the platform-substrate MetricRegistry Core-Web-Vitals instrument rows (not a parallel metric construction), a data-driven BudgetThreshold Record gating the budget-exceeded fold, the long-task/resource-timing PerformanceObserver marshalled into Effect Stream signals, and the budget-breach span shipped through SelfTelemetry; captures via native PerformanceObserver only, ZERO web-vitals package admitted.

[SERVICES]:

- services/durable-execution.md: WorkflowOwner + ActivityOwner closed durable-unit vocabulary, ClusterEngine cluster-backed WorkflowEngine wiring, the ai-activity over the sole AiProvider Schema.Literal axis, the AgentJournal durable agent ledger, and the Resilience circuit-breaker/retry/rfc6902 primitives.
- services/persistence.md: SqlBoundary the single PgClient/Migrator owner, the ~15-entity EntityRegistry bound to ONE Model.Class per entity with Model.fields/Schema.pick projections, TenantScope multi-tenant RLS, WorkQueue/JobDlq, EventJournal, Notifications matrix, the AssetTransfer export-codec axis, and FeatureFlags percentage buckets — the sole declaration site of the flag bucket/variant vocabulary.
- services/hybrid-search.md: HybridSearch the single fused weighted-rank owner over semantic (HNSW efSearch) + lexical (tsvector) + trigram (pg_trgm) + phonetic signals with embedding-profile dimensioning and staleness tracking.
- services/internal-rpc.md: InternalRpc one RpcGroup with WorkflowProxy derived over the same wire Schema, plus RunnerBackplane 4-row protocol/message-storage/runner-storage/runner-health backplane with the snowflake id source and ScheduledWork cluster singleton + shard-pinned cron.
- services/provisioning.md: TierStack data/compute/observe ComponentResources with two-mode DeployMode dispatch (cloud K8s vs self-hosted compose+Traefik), AutomationDriver the @effect/cli up/preview/refresh/destroy verb tree over the Automation API, the entry-thin/impl-dense split behind the ./provisioning subpath, StackOutputs cross-stack topology, SecretResolver Doppler boundary, PolicyGuard engine/program guardrails, the idempotent bootstrap, the self-hosted equivalence map, and ObservabilityStack Alloy->Prometheus->Grafana.

[BRANCH] (CROSS_PACKAGE_LAWS, non-domain):

- architecture-posture.md: the seven-altitude one-form grammar, the closed five-app-service budget, the platform-neutrality strata, the held Effect-v3 runtime + dual-compiler tsgo floor, the admission doctrine, the anti-spam cluster law + altitude-co-location law, and the RULE_ENFORCEMENT guard table.
- test-strategy.md: the algebraic PBT spine with per-file LOC and coverage floors, the one browser-mode runner covering both ui and platform specs and the end-to-end runner, and the mutation kill-ratio gate plus durable-cluster harness — per-domain project rows in the centralized root vitest setup.
- architecture-tree.md: the flat five-domain single-package source tree, the inter-domain folder-stratum import graph, the browser/node/neutral publication split as a subpath-export attribute, the descriptor-pipeline placement in interchange, the ui->platform intra-browser dependency direction, and the GlbViewport refinement-horizon entry.
