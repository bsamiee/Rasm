# [APPUI_IDEAS]

The forward concept pool for the product UI engine. Open ideas are higher-order folder concepts grounded in the AEC/Rhino purpose and current rendering, reality-capture, and coordination research; each drives one or more task cards in `TASKLOG.md`. A finished or dropped idea moves to closed with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[CONTROL_MATERIALIZATION_SYSTEM]-[QUEUED]: one typed `ControlIntent` family materializes any interactive control from a declarative shape so a screen is authored as a control-intent stream rather than per-screen XAML literals.
- Capability: A `ControlIntent` `[Union]` whose single `ControlFactory` fold projects each intent — button, text/number/date/path input, select, slider, toggle, radio, grid, tree, menu, toolbar, tab, accordion, panel, dock, splitter — onto its Avalonia control, binds its `ReactiveCommand` exclusively through the existing `BehaviorRail.Intent` bridge, resolves visuals through `Theme/tokens` roles, and routes accessibility through the `Shell/accessibility` automation-identity rail, so one vocabulary materializes hundreds of controls and never a parallel control framework.
- Shape: A new `Shell/Controls` sub-domain (`Shell/controls.md`) becomes the generative substrate every screen body composes; `ScreenCatalog` rows shift from XAML-literal bodies to control-intent streams authored over `ScreenBase.Wire`.
- Unlocks: Screens authored declaratively; agent, remote, and wire control synthesis; one place to enforce binding, theming, accessibility, and command law across every control; the substrate the forms owner and the virtualization fabric consume.
- Anchors: `BehaviorRail.Intent` as the only C# binding bridge, the `CommandIntent`/`CommandDeck` rails, the `EditorFactory` eleven-row precedent already proving typed-shape-to-control in `PropertyGrid` cells, `Theme/tokens` `TokenRow`, `Shell/accessibility` automation identity, and Avalonia `ControlTemplate`/`DataTemplate`/`ControlTheme`; the `csharp:Rasm.AppUi/Controls` mint emits `ControlIntentWire` over the `ControlIntent` family that `typescript:interchange` decodes and `typescript:ui/render` materializes, so a web/remote caller materializes the same control vocabulary the desktop renders, and `Shell/accessibility` derives each materialized control's automation name from the one intent row.
- Ripple: counterpart of the cross-libs `[ONE_UI_INTENT_WIRE]` seam (`csharp:Rasm.AppUi/Controls` mints `ControlIntentWire` over the `ControlIntent` family the web head materializes).

[CONSTRAINT_LAYOUT_ENGINE]-[QUEUED]: a declarative constraint-layout engine replaces width-breakpoint knobs with a real solver so responsive, self-sizing, and adaptive layouts resolve from typed constraints across desktop, web, and immersive surfaces.
- Capability: A `LayoutConstraint` algebra of equalities, inequalities, and priorities over edge, size, and anchor variables, solved once per surface by a `Kiwi` owner that folds into Avalonia's measure/arrange pass through one custom `Panel`, so flex, grid-track, and auto-layout become constraint rows rather than parallel layout panels.
- Shape: A new `Shell/Layout` sub-domain (`Shell/solver.md`) owns the constraint vocabulary and the solver capsule; every adaptive screen consumes it.
- Unlocks: True responsive adaptation beyond breakpoints; self-sizing forms and dashboards; one layout language shared by desktop, web, and XR; design-tool-grade auto-layout.
- Anchors: The Avalonia `Panel`/`Layoutable` measure-arrange seam, `Theme/tokens` `Metric` rows for spacing, and the `CONTROL_MATERIALIZATION_SYSTEM` owner that emits the controls being laid out; `Kiwi` is the simplex solver admission, pinned in `Directory.Packages.props`, registered as the `README` `[LAYOUT]` group, and evidenced at `.api/api-kiwi.md`, internalized into the one `Shell/Layout` owner rather than a thin wrapper; the `typescript:ui/render` `LayoutConstraintWire` bands solve the same layout the desktop solves.
- Tension: An under-constrained Cassowary system admits many valid assignments, so the `Kiwi` desktop tableau and the `kiwi.js` web tableau converge to identical positions only when `LayoutConstraintWire` carries the full ordered constraint program — variable-introduction order, the edit-variable set, and the suggested-value sequence the desktop solver fed — not just the relation set; an order-free constraint dump is the silent per-viewport drift defect, so the `csharp:Rasm.AppUi/Layout` mint emits the ordered program and the web head re-solves it, matching the `ONE_UI_INTENT_WIRE` strength/relation-parity invariant.
- Ripple: counterpart of the cross-libs `[ONE_UI_INTENT_WIRE]` seam (`csharp:Rasm.AppUi/Layout` mints `LayoutConstraintWire` carrying the `Kiwi`-authored ordered constraint program the `kiwi.js` head re-solves).

[GENERIC_VIRTUALIZATION_FABRIC]-[QUEUED]: a surface-agnostic virtualization fabric materializes only the visible window of arbitrary lists, trees, grids, and canvases so a million-row table, a deep model tree, and an infinite drafting canvas all render at constant cost from one owner.
- Capability: A `VirtualWindow` owner mapping viewport-range to a realized-item set with control recycling, sticky headers, variable-extent measurement, and hierarchical flatten for trees, folding over `DynamicData` `IChangeSet` so windowing is incremental — one fabric every windowed surface consumes, never a per-surface virtualizer.
- Shape: A new `Shell/Virtualization` sub-domain (`Shell/virtualization.md`) the `Editing/tables` tree-flatten, the notebook cell list, the dashboard tile grid, and the drafting canvas all route through.
- Unlocks: Constant-cost rendering for arbitrarily large data; uniform recycling and measurement law; the prerequisite for the tables, notebook, and canvas surfaces to scale to production data volumes.
- Anchors: The `DynamicData` `IChangeSet` operators already serving as the `LiveData` spine, Avalonia `ItemsControl`/`Layoutable`, the `Editing/tables` column-metadata family plus tree-flatten fold, and the `CONTROL_MATERIALIZATION_SYSTEM` grid/tree intents; `Editing/tables` routes its tree-flatten projection through the one `VirtualWindow` rather than a tables-local virtualizer.

[FORMS_TRANSACTION_AND_SELECTION]-[QUEUED]: a declarative forms-and-interaction-transaction owner family delivers schema-driven forms with validation and wizard flows, revertible client-side undo/redo, and multi-selection batch editing over the admitted `PropertyModels` infrastructure with zero new package.
- Capability: A `FormSchema` of typed field rows, validation rules, and wizard steps materialized through the `ControlFactory` owner and validated through the screens `ReactiveUI.Validation` lift; an `EditHistory` wrapping the admitted `CancelableCommandRecorder`/`CommandHistoryViewModel` so every edit is a revertible command sealed through the existing `EditReceipt` sink with `EditOutcome.Reverted`; and a `Selection` model over the admitted `ICheckedList`/`ISelectableList` driving batch-edit intents that fold to one combined `CommandReceipt`.
- Shape: A new `Editing/Forms` sub-domain (`Editing/forms.md`) plus an `EditHistory` page (`Editing/history.md`), the selection and history capsules riding the existing `EditReceipt`/`CommandReceipt` rails.
- Unlocks: Declarative forms and wizards as a product capability; revertible client-side history across every edit surface; batch editing of many selected items in one transaction — the everyday application interactions the shell currently lacks.
- Anchors: The admitted `PropertyModels` `CancelableCommandRecorder` (MaxCommand=20), `CommandHistoryViewModel` `CanUndo`/`CanRedo`, `ICheckedList`/`ISelectableList`, the `[ConditionTarget]`/`[PropertyVisibilityCondition]`/`[DependsOnProperty]` annotations, the `EditReceipt`/`EditOutcome.Reverted` rail, the screens `Validation<Error,T>` lift, and the `CommandExecution.Combine` batch fold; each edit records through the `CancelableCommandRecorder` as a `RevertibleOp` carrying its forward and inverse delta, and an undo past the `MaxCommand=20` client window resolves against the durable `Rasm.Persistence/Sync` `OpLogEntry` inverse stream keyed by `ContentIdentity` so client history and durable sync fold one inverse algebra rather than two history schemes.
- Ripple: counterpart of the `csharp:Rasm.AppHost` branch `[ONE_REVERT_VOCABULARY]` idea (AppUi records `RevertibleOp` forward/inverse deltas, Persistence replays the same vocabulary as a `SyncOpKind` row).

[RICH_CONTENT_AND_MEDIA]-[QUEUED]: a rich-content-and-media owner renders markdown to live Avalonia inlines through a `Markdig` custom renderer and plays image/video/audio through one `MediaSurface` over codec rows so documentation cells, help, and embedded media become first-class content surfaces beside the code editor.
- Capability: A `MarkdownProjection` walking the `Markdig` block/inline AST through `RendererBase` and `Descendants<T>` into theme-token-styled Avalonia inlines and visuals, and a `MediaSurface` `[Union]` over codec rows — image already `AsyncImageLoader`-partial, video/audio through `HanumanInstitute.LibMpv.Avalonia` on one `SurfaceSeam` — so one content vocabulary serves every rich surface and a new codec is one row.
- Shape: A new `Editing/Content` sub-domain (`Editing/media.md`) the notebook markdown cells, help, and inspector docs consume; `MediaSurface` mounts on the single render `SurfaceSeam`.
- Unlocks: Markdown documentation cells rendered to visuals rather than HTML; embedded video and audio playback with codecs; a uniform rich-content rail for help, notebooks, and reports.
- Anchors: `Markdig` `RendererBase` plus block/inline AST and `Descendants<T>` (`.api/api-markdig.md`), the `Theme/typography` `MarkdownProjection` hook already referenced by notebook, `AsyncImageLoader` (`.api/api-asyncimageloader.md`, image partial), and the `SurfaceSeam` render-host axis; `HanumanInstitute.LibMpv.Avalonia` is the admitted video/audio backend (`.api/api-libmpv.md`, `README` `[MEDIA]` group) internalized into the one `MediaSurface` owner on the `SurfaceSeam`, with the libmpv native provisioned at the app-host distribution layer; `Theme/typography` owns the inline-styling roles the renderer resolves so markdown styling rides the one typography vocabulary.

[INPUT_DEVICE_FABRIC]-[QUEUED]: one typed `InputDevice` fabric folds SpaceMouse, gamepad, haptic, and MIDI streams into the existing `BehaviorRail.Intent` bridge so a 3D navigator, a controller, a rumble actuator, and a control surface drive the shell through one device vocabulary rather than four parallel input stacks.
- Capability: An `InputDevice` `[Union]` whose single `DeviceFold` projects each backend — `HidSharp` raw-HID SpaceMouse six-axis translation/rotation deltas, `Silk.NET.Input` `IGamepad`/`IJoystick` button-and-thumbstick state, `Silk.NET.SDL` `SDL_Haptic` rumble/force-feedback effects, and `Melanchall.DryWetMidi` `InputDevice`/`NoteOnEvent` control-change streams — onto the one `BehaviorRail.Intent` command bridge, with each device's continuous axes folded through the `Shell/input` pan-zoom canvas algebra and discrete events mapped onto the `CommandIntent` vocabulary, so a new device class is one union arm reading the shared intent rail, never a parallel input framework.
- Shape: An extension to the existing `Shell/input.md` page (`Shell/input.cs`) deepening the command-derived-hotkey and behavior trigger/action rows with a device-source axis; the four admitted SDKs internalize into the one `InputFabric` owner that emits `CommandIntent` and pan-zoom deltas.
- Unlocks: SpaceMouse-driven viewport navigation, gamepad design review, haptic feedback on constraint/clash events, and MIDI control-surface parameter binding — the alternative-input modalities a professional design suite exposes — all over the one command rail with zero per-device command registry.
- Anchors: The `Shell/input` `BehaviorRail.Intent`/`BehaviorRail.RejectViewBinding` law and pan-zoom canvas algebra, the `CommandIntent`/`CommandDeck` vocabulary, the admitted `HidSharp` (`.api/api-hidsharp.md`), `Silk.NET.Input` (`.api/api-silk-input.md`), `Silk.NET.SDL` (`.api/api-silk-sdl.md`), and `Melanchall.DryWetMidi` (`.api/api-drywetmidi.md`) surfaces internalized into the one `InputFabric` owner rather than four thin wrappers, and the new `[05]-[PROHIBITIONS]` block; eye-gaze, switch-access, and speech-to-text have no viable cross-platform net10 backend and stay out of the fabric.

[GPU_SHADING_AND_IMMERSIVE]-[QUEUED]: realize the two PLANNED Render pages — a GPU shading owner with a per-backend pipeline-state cache and a layered-BSDF surface-shade pass, and an OpenXR immersive design-review surface with stereo swapchain and FB passthrough — both over the one shared Wgpu device, and split the 750-line Viewport god-page into `Render/{Pipeline, Meshlets, PathTrace}`.
- Capability: A `ShaderAsset` cache keyed per `GpuBackend` (`SKRuntimeEffect` for Ganesh, `Silk.NET.WebGPU` pipeline-state for Wgpu) feeding the path tracer's `SurfaceShade`, and an `Immersive` owner binding `Silk.NET.OpenXR` stereo plus `XR_FB_passthrough` onto the same Wgpu device the viewport leases — folding flat where the OpenXR loader is absent — with the viewport pass algebra relocated into focused Render pages.
- Shape: Two newly realized Render pages (`Render/shading.md`, `Render/immersive.md`) plus a structural split of `Render/viewport.md` into `Render/Pipeline` (RenderGraph pass-DAG, `RenderTargetFactory`, resolve ladder), `Render/Meshlets` (`MeshletCluster`, residency), and `Render/PathTrace` (BVH, ReSTIR, denoise), capability conserved with the codemap and SEAMS updated.
- Unlocks: Material/BSDF shading at the path tracer; on-site mixed-reality design review with passthrough; a viewport authored at its real granularity instead of one 750-line page.
- Anchors: The `GpuBackend.Wgpu` row plus `RenderTargetFactory` (designed), `Silk.NET.WebGPU`/`OpenXR`/`Extensions.FB` (all pinned 2.23.0), the shared-Wgpu-device `EMBED_CAPSULE` law that the shared-branch `ONE_WGPU_DEVICE` owner mints, `SKRuntimeEffect`, `ICompositionGpuInterop`, and the `Rasm.Materials/Appearance` `LayeredBsdf` seam that lowers to `SurfaceShade` so the shading owner consumes the materials BSDF rather than minting an appearance model; shaders ride the `SurfaceSeam` plus `GpuBackend` factory with `SKSurface` confined to the `Offscreen` capsule, SPIKE-gated at runtime exactly as the viewport while the design pages are authorable now.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
