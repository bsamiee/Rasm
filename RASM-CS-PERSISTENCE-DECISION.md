# [RASM_CS_PERSISTENCE_DECISION]

The binding restructure blueprint for `libs/csharp/Rasm.Persistence/.planning/` — the APP-PLATFORM durable-state spine persisting the `Rasm.Element` `ElementGraph` as its system of record. The durable rebuild engine executes this DECISION unmodified: `[02]` is the final page-set in both action vocabularies (semantic action + rebuild-engine lowering), `[03]` the type-enforced band registry, `[04]` the corrected seam ledger with the twelve stale sibling rows as counterpart obligations, `[05]` the roster delta with `.api` obligations, `[06]` the leg partition proven acyclic with per-leg invocation rows, `[07]`-`[09]` the full verdict/evidence/card disposition. Upstream honored, never re-planned: `RASM-CS-GEOMETRY-DECISION.md`/`RASM-CS-GEOMETRY-BRIEF.md` (Persistence is the kernel key-minting consumer; the `[V2]` parametric content-identity clause waterfalls the `[V9]` demand) and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (frozen-wire rule, `[AMENDMENTS]` count-prefix law, 25xx→27xx re-band gate, the `FaultBand` registry shape at `:141-149`). `docs/stacks/csharp/` governs every fence unchanged. Every open item is RULED; this DECISION stands alone.

## [00]-[DECISION_SUMMARY]

The blueprint is folder-architecture-primary: the page-set partitions by the five folder growth axes ([A.4]), satisfies INTEGRATION-FIRST + HARDENING LAW on the six scale-out backends through per-package ceiling proofs ([05]), produces `[V13]`'s first-class AXIS MAP, and carries churn-honest kind labels — a page is `rebuild` only where a split sheds real weight, `improve` everywhere else. No capability is dropped anywhere in the lowering.

- **Page-set**: 24 pages — Element 4 · Version 8 · Query 7 · Ingest 2 · Store 3; zero one-file folders.
- **Lowering**: 6 `new` (authority, egress, retrieval, federation, schedule, coordination) · 2 `rebuild` (identity, lane — split-remainders shedding ~37%) · 16 `improve` · 2 paired SPLIT extractions · 0 `deletePages` · 0 engine `absorb` fields — engine absorb is whole-page absorb-then-DELETE and both split sources survive as rebuilds; each extraction travels as the paired `new`+`rebuild` kinds under `[02]`'s split contract.
- **Four disqualifying gates all pass**: (1) ACYCLIC — zero upward strata edges after the csproj:10 removal + the corpus-wide frame-shape injection re-home (Persistence-owned port-input shapes DEFINED leg 1, each referencing page re-threaded in its own leg — [A.1]), four frozen-vocab cross-leg contracts, one forward-only intra-leg egress→coordination cursor; (2) DISPOSITION-COMPLETE — V1-V13, E1-E14, 30 cards, every roster package, twelve `[10]` capability planes; (3) BAND-UNIQUE — 19 distinct own decades, duplicate-fails-at-type-init, Egress tight at 827x, Commit 826x MINTED; (4) PORT-COMPLETE — 4/4 AppHost PORT seams on `CoordinationOp` + the APPHOST:80 + `Wire/coordination.md:79` `MembershipView.Serving` consumer.
- **Four overturns of ruled defaults, each disk/feed-justified**: (a) `[V4]` Commit "register as-is" → MINT `CommitFault` (disk: `commits.md:358/400/411` bare `Error.New(8261/8264/8263)`, ZERO union); (b) `[V1]` wire-form hand-transcriber + `Grpc.Tools` codegen → shipped-public `SubstraitDeserializer.Deserialize` (~2 lines) + DROP `Grpc.Tools` (decompile falsifies `api-flowtide-substrait.md:151`); (c) Ara3D `1.0.1` "latest" → `1.6.1` (nuget feed: published 2026-07-03); (d) coordination-first kernel-rehome-primary → INJECTION-primary with the kernel re-home demoted to a contingent KERNEL/APPHOST counterpart obligation.

## [01]-[SHARED_LAW]

### [A.1]-[STRATA] — the csproj:10 reversal, injection-primary

`Rasm.Persistence.csproj:10` carries `<ProjectReference Include="../Rasm.AppHost/Rasm.AppHost.csproj" />` — a DOWN/PEER edge reversing the strata (AppHost is a HOST-BOUNDARY PORT peer Persistence contributes rows to; Persistence is up-only on `Rasm` + `Rasm.Element`). **RULING: REMOVE it** (leg-1 structural fix; the `dotnet restore` acyclicity gate confirms). The `[V2]` coordination op-union, fencing tokens, membership rows, `CostVector` balances, and receipts are Persistence-OWNED types AppHost's `Wire/Coordination`/`Wire/outbox` adapters DECODE — no AppHost type crosses down.

The frame ingredients currently resolving through that reference — `graph.md:253` annotates `actor` as `Rasm.AppHost/Agent/identity#PRINCIPAL`, and `graph.md:219,256,285` type the `Run`/`Stage` entry as `(… Principal actor, Guid storeId, ClockPolicy clocks, CorrelationId correlation …)` — resolve by INJECTION, not kernel re-home. No page writes a `using Rasm.AppHost` import; the type resolves purely through the ProjectReference, and `graph.md:287` uses only `actor.Subject` (a string, matching `provenance.md:291` `AgentKey(string actor)`). **RULING (load-bearing for the acyclicity gate)**: `graph`'s leg-1 improve RE-HOMES `Principal` to the Persistence-DEFINED `StoreActor` value-object (`Subject` + minimal claims) that AppHost's composition root MAPS its richer `Principal` onto at the PORT boundary — the AppHost simple name never crosses down, mirroring the Grant/`Capability` never-share-a-name law (`identity.md:280`); the `:253` annotation is rewritten to name `StoreActor`. `ClockPolicy`/`CorrelationId`/`TenantContext`/`RecoveryObjective` enter as injected `ProjectionContext`/`ResolvedProfile` shapes Persistence DEFINES and AppHost SUPPLIES; `CostUnit` crosses as its `[SmartEnum<string>]` STRING key (`capability.md:54` confirms string-keyed, so the Budget row is `HashMap<string,long>` per unit and AppHost maps its smart-enum at the boundary); `TraceSlot`/`TenantId` cross as wire values. **The spread is CORPUS-WIDE, not `graph.md`-local**: `ClockPolicy`/`CorrelationId`/`TenantContext` are referenced by simple name across ~18 pages spanning ALL FOUR LEGS (`codec.md:368`/`identity.md` leg 1; `ledger.md:284`/`timetravel.md:134`/`recovery.md:134`/`retention`/`merge`/`commits`/`provenance` leg 2; `lane`/`topology`/`columnar`/`cypher`/`cache` leg 3; `blobstore`/`provisioning` leg 4), and `Principal` saturates `identity.md` (`:277-486`) and `provenance.md` (`:166-343`) beside `graph.md` — and AppHost's OWN `Runtime/time.md:18` declares `ClockPolicy` NEVER crosses the strata DAG downward ("a `ClockPolicy` parameter on an AEC-DOMAIN or APP-PLATFORM signature is the named inversion"), so every such Persistence signature is a strata breach until re-threaded. **RULING**: the Persistence-owned port-input shapes (the `StoreActor` value-object + the `ClockPolicy`/`CorrelationId`/`TenantContext`/`RecoveryObjective` ingredients carried on the injected `ProjectionContext`/`ResolvedProfile` forms) are DEFINED in leg 1, and EVERY referencing page re-threads onto them IN ITS OWN LEG as part of that page's rebuild (leg 1 defines the shapes AND re-threads `graph`/`codec`/`identity`/`authority`; legs 2-4 re-thread their pages as each rebuilds). Injection stays the smallest-blast-radius fix because the shapes are constructor VALUES and the only `Principal` member used is `.Subject`; the stronger frame-re-home-to-kernel framing DEMOTES to a contingent KERNEL/APPHOST counterpart obligation (re-homing an AppHost identity type into the geometry/identity kernel carries a larger cross-campaign blast radius). **Effect**: after the leg-1 shape definition + the per-leg re-thread, zero upward strata edges and zero AppHost namespace imports remain; a page not yet re-threaded is a carried leg residual, never a silent upward edge — the acyclicity gate closes on the completed corpus re-thread, not on `graph` alone.

### [A.2]-[SEAM_BACKBONE] — the four cross-runtime wire seams

The own seam ledger is organized around the four cross-runtime seams every consumer resolves through the ONE kernel `ContentHash.Of` seed-zero entry, so a Persistence-owned type never mints a second identity:

- **S1 Substrait plan ingress → `SetExpr`** — owner `Query/federation.md` (NEW, V1). Wire = shipped-public `Substrait.Protobuf.Plan.Parser.ParseFrom` + `new SubstraitDeserializer().Deserialize` (~2 lines); plan digest = `ContentHash.Of(wireBytes)`.
- **S2 content-addressed CloudEvents egress** — owner `Version/egress.md` (NEW, V3). `id`=`OpLogEntry.ContentKey`, `Sequence`=outbox cursor, `Partitioning.partitionkey`=`EntityKey`; per-sink dedup as composition.
- **S3 `AsOfKey`=`Checkpoint.Address` icechunk seam** — owner `Version/timetravel.md` (IMPROVE, V12). ONE content digest (the seam `ContentAddress.OfGraph` at the cut) serving the icechunk cross-runtime seam AND the recovery content-identity oracle; the prior-linked `Checkpoint.Hash` chain (`ChainHash(prior, Address)`, `timetravel.md:154-158`) stays the tamper-evidence link — not reproducible from graph content alone, never the cross-runtime key.
- **S4 `CrdtOpWire` bit-parity law** — owner `Version/commits.md` (IMPROVE, V4-MINT). The `[MessagePack.Union]` `[Key]` sequence IS the wire; MINT `CommitFault`/`CrdtWireFault` (H1); PIN `CRDT_OP_SET`; re-anchor the raw mints.

### [A.3]-[FROZEN_VOCABULARY] — the four cross-leg inversion contracts (V5c/d)

The DECISION freezes four vocabulary shapes at decision time so an earlier-leg page consumes the frozen CONTRACT (its on-disk shape at decision time), never the later leg's rebuilt page — acyclic by contract. All four CONFIRMED on disk. A leg that alters a frozen vocabulary reopens the consuming leg as a hard residual.

| Frozen vocabulary | Owner (later leg) | Consumer (earlier leg) | Contract |
|---|---|---|---|
| `TimeCut` cases | `Version/timetravel` (2) | `Element/graph` (1) | the AS-OF cut case-set frozen; `graph.md:229-232`/`:302` reads it |
| `StorageTier` rows | `Store/blobstore` (4) | `Version/retention` (2) | the storage-class row-set frozen; `retention.md:100,121-124` reads it |
| `ObjectStore.Head` delegate shape | `Store/blobstore` (4) | `Version/recovery` (2) | the object-replica probe delegate frozen; `recovery.md:175,179,204` reads it |
| `ServerExtension.CreateSql` row shape | `Store/provisioning` (4) | `Query/cypher` (3) | the extension install-row frozen; `cypher.md:3` installs through it |

The `Store/tier` neutral-vocabulary extraction is NOT taken: the deciding criterion (a leg-4 owner must amend a frozen vocabulary mid-campaign) is unmet on disk, and preference alone never overturns the documented pivot. (See `[07]` V5.)

### [A.4]-[FOLDER_GROWTH] — the per-folder growth axis + the Version split threshold

Each folder is a growth axis with a stated "the next concern lands as a page HERE" law — the forward-placement charter that won partition-elegance. The durable engine places a future concern by this table, never by flat sprawl and never by a new one-file folder.

| Folder | Axis owned | The next concern lands as a page HERE |
|---|---|---|
| `Element/` | the ElementGraph store-load roundtrip + content identity + access tier | a new content-key domain, a new identity axis, or a new authz scope |
| `Version/` | the version-control engine projecting from Marten events | the next event-derived durable concern; STAYS FLAT at 8 — the core/read/governance sub-partition is the split axis ONLY if it grows past ~9 |
| `Query/` | the read lanes split by consistency demand + the reuse index | a new consistency stance, a new engine surface, or a new plan-lowering |
| `Ingest/` | the file-codec ingress axis | the next foreign-file codec into the record rail |
| `Store/` | the durable-home + coordination substrate | a new durable-home / provider axis or coordination concern |

### [B]-[HASHER_RE_ANCHOR] — the H2/H3 enumeration (H1 is the commits `CommitFault` MINT, `[03]`)

Every Persistence digest mint composes the landed kernel `ContentHash.Of` seed-zero entry (`Rasm/.planning/Domain/identity.md` — `Of(span) ⇒ XxHash128.HashToUInt128(span)` seed-zero), directly for opaque preimages, transitively through the seam `ContentAddress` for node/graph keys. Value-identical, so pure call-path collapse, never an identity migration. `[SEAM_AND_RAIL_LAW]` enumerated the codec-tier targets (leg 1); disk proves the VERSION ENGINE mints raw `XxHash128` keys leg 2 must also re-anchor. The decision-complete table:

| Mint | Anchor (disk) | Re-anchor | Leg |
|---|---|---|---|
| `SnapshotHeader.Seal`, `ContentChunker`, cache identity, V1 plan digest | `codec.md` codec-tier | `ContentHash.Of` / seam `ContentAddress` | 1 |
| `OpLogEntry.ContentKey` | `ledger.md:153` `XxHash128.HashToUInt128(payload.Span)` | `ContentHash.Of(payload.Span)` | 2 |
| `CommitNode` content key | `commits.md:91` `XxHash128.HashToUInt128(canonical.WrittenSpan)` | `ContentHash.Of(canonical.WrittenSpan)` | 2 |
| `CrdtWire.ContentKey` | `commits.md:354` `XxHash128.HashToUInt128(EncodeCompanion(op).Span)` | `ContentHash.Of(EncodeCompanion(op).Span)` | 2 |
| `ParityVector.Pin/Stamped/Contribute` | `commits.md:375-376,401` raw `XxHash128.HashToUInt128(Canonical.Span)` | route through `ContentHash.Of`; fold the dead `ContentParityCorpus.Seed=0L` into the mint or delete | 2 |
| `AgentKey` (durable PROV node identity) | `provenance.md:291` `XxHash128.HashToUInt128(Encoding.UTF8.GetBytes(actor))` | `ContentHash.Of(Encoding.UTF8.GetBytes(actor))` | 2 |
| timetravel EDGE keys (H3) | `timetravel.md:203-204` raw `XxHash128.HashToUInt128(e.ToCanonicalBytes(tol).Span)` | `ContentAddress.Of(edge.ToCanonicalBytes(tolerance).Span)` — the SEAM form, matching `merge.md:160` | 2 |

DEFENSIBLE-LOCAL (not re-anchored, a transparency-log construction, not a content-key mint): the `provenance` internal Merkle rolling digests (`provenance.md:229,321,349,418`), the `commits.md:119` `MerkleRange` peer digest, and the `timetravel` checkpoint `ChainHash` prior-link (`:154-158` — the chain digest is tamper evidence over the seam `Address`, never a content key). The blobstore multipart/chunk folds whose payloads outgrow a one-shot span compose the `identity.md` Growth-row streaming member (`XxHash128.Append` + `GetCurrentHashAsUInt128`, seed zero) on landing; the `ContentAddress.Of(UInt128)` wrap of a precomputed digest is the documented interim.

## [02]-[PAGE_SET]

24 rows; paths are `libs/csharp/Rasm.Persistence/.planning/`-relative. Column schema: `path` · `semantic action + engine lowering` (`kind` = the rebuild-engine verb; a SPLIT travels as the paired `new`+`rebuild` kinds — the `new` page names its SPLIT-from sections and the paired `rebuild` sheds them in the same leg batch; the engine `absorb` field stays UNSET corpus-wide because engine absorb is whole-page absorb-then-DELETE and both split sources survive; `deletePages`=0 corpus-wide) · `owner charter / folder home / per-page skeleton / band` (per `[03]`) · `entry signature` (the op-union, or NONE for vocabulary/set-algebra pages) · `seams (in ← / out →, one anchor per crossing, both ledger directions)` · `leg`.

### `Element/` — 4 pages (the ElementGraph store-load roundtrip + identity + authz)

| # | path | action · lowering | owner charter · home · band | entry signature | seams (in ← / out →) | leg |
|---|---|---|---|---|---|---|
| 1 | `Element/graph.md` | KEEP · `improve` | Store-rail root: one-materializer event sourcing (`GraphDelta.ReplayOnto`), co-txn identity commit, one-boundary fault conversion. **HOSTS the `[FAULT_TABLES]` `FaultBand` `[SmartEnum<int>]` registry owner block** (`[03]`). **Re-homes `Principal` to the Persistence-owned `StoreActor`** ([A.1]); the `:253` AppHost annotation rewritten. Band **Graph 8300**. `Element/` | `GraphStore.Run(session, identity, GraphStoreOp, StoreActor actor, Guid storeId, ProjectionContext frame) : IO<Fin<GraphReceipt>>` (the post-[A.1] re-thread shape — the frame carries clock/correlation/tenant) + `FaultBand` registry | `← Rasm.Element` (ElementGraph SoR, ARCH:41); `← Rasm` (kernel seed-zero, ARCH:42); `← Rasm.AppHost/Runtime` (ClockPolicy/CorrelationId/TenantContext/Principal as INJECTED port-input VALUES on Persistence-owned shapes, ARCH:54); `← timetravel#TimeCut` (frozen-vocab, V5d); `← APPHOST:71` (prepared-tx RETIRE counterpart — the single-`IDocumentSession` spine mints no `pg_prepared_xacts`); `→ Compute:115` (parse-to-canonical Extract counterpart) | 1 |
| 2 | `Element/codec.md` | KEEP · `improve` | Seam-composed `ContentAddress` (kernel `ContentHash.Of` seed-zero, no second hasher), CBOR trust boundary, single-pass atomic seal, FastCDC chunker. `[V10]` HashPolicy shrink to `{Identity,Content}` + `HashDomain` forward-compat byte law; kill the `:175` `ByDomainId` prose drift. **THE kernel-hash re-anchor root** (all codec-tier mints compose here, [B]). Band **Codec 8310** (831x/832x/833x stride). `Element/` | `SnapshotCodec` `[SmartEnum]` axis + `ContentAddress.Of/OfGraph/Of(UInt128)` seam-compose (NONE — vocabulary + static ops) | `← Rasm` (kernel seed-zero `XxHash128`, ARCH:42 — the ONE hasher entry); `→ typescript:wire` (SnapshotHeader canonical-CBOR, ARCH:43); `← Compute:111` (FastCDC content-key delta → `#CONTENT_CHUNKER`) | 1 |
| 3 | `Element/identity.md` | SPLIT · `rebuild` (split-out — sheds the SPLIT-from sections row 4 names) | Relational identity tier (Marten doc), `IdentityPolicy` key axis, KMS custody (`SigningKeyring`+`EnvelopeKeyring` on the one `KmsProvider` axis, V5a — authz-vs-crypto split), `SchemaVerdict` boot fold. `[V6]` EF commit (`UseValueObjectValueConverter()`+`UseSnakeCaseNamingConvention()`; hand `NodeId` converter + the 10 hand `HasColumnName` calls die; the LanguageExt `Option<Vector>`/`Seq<NodeId>` converters `:87-105` stay — Thinktecture.EF does not own those) + the DDL/migration owner (`element_identity`/`node_cell` DDL; `ServerExtension.CreateSql` commits through this rail; `EF.Design` earns its admission here). Slims 651→~410 LOC. Band **Identity 8340**. `Element/` | `IdentityStore.Stamp` + `SchemaGate` + `IdentityPolicy`/`KmsProvider` axes (NONE — relational surface) | `⇄ Rasm.AppHost/Runtime` (TenantId RLS + KMS SigningKeyring/EnvelopeKeyring unwrap PORT — ARCH:53 SPLIT, KMS/RLS stay; APPHOST:72 unchanged) | 1 |
| 4 | `Element/authority.md` | NEW · `new` · SPLIT-from `Element/identity.md#[04]-[AUTHORITY]` (`:287-341,465-484`; identity's paired `rebuild` sheds the sections in the same leg batch — engine `absorb` UNSET, the source survives) | Object-ACL authz set-algebra: `Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit`+`Effective`+`LapsedFor` — pure deny-over-allow set-algebra, zero `KmsProvider`, total. Band **NONE** (sub-bands under Identity 8340 only if a rail is later added). `Element/` | `Authority.Admit(GrantSet, AclScope) → Effective` (NONE — pure set-algebra) | `⇄ Rasm.AppHost/Runtime` (ObjectAcl identity-store PORT — the ARCH:53 `ObjectAcl` half moves HERE); `→ Version/commits#Movable` (ACL gate on GrantSet) | 1 |

### `Version/` — 8 pages (the engine projecting from Marten events; stays flat at 8)

| # | path | action · lowering | owner charter · home · band | entry signature | seams (in ← / out →) | leg |
|---|---|---|---|---|---|---|
| 5 | `Version/ledger.md` | KEEP · `improve` | Changefeed projection off Marten `SubscriptionBase`, `(Hlc,OriginStoreId)` LWW, presence/awareness drain. `[V10]` `SyncOpKind.Truncate`+`WholeRelation` wire-or-delete; `OpLogEntry.Codec`→`Family`-derived accessor; `ProcessEventsAsync` batches the range into ONE fold (`:119`); `OpLogEntry.ContentKey` re-anchors `ContentHash.Of` ([B]). Band **Sync 8250**. `Version/` | `OpLog.Project` + `SyncOpKind` `[Union]` + `ChangefeedSubscription` (NONE — projection surface) | `⇄ python:runtime/transport` (OpLogEntry CRDT delta, ARCH:48); `⇄ Rasm.AppHost/Runtime` (HLC+TraceSlot PORT, ARCH:49); `← Rasm.AppHost/Wire/companion` (PRESENCE PeerRoster beats over DrainSurface, ADD, `ledger.md:468`); `← Rasm.AppUi/Collab/Editing` (edit-intent projection + per-doc replay-window READ) + `← Rasm.AppHost/Runtime/determinism` (neutral-log window-read — ONE windowed-read case parameterized by origin/entity/window serves BOTH); `← BIM:101` (durable-annotation CDE); `← BIM:104` (Speckle Base import); `→ typescript:wire` | 2 |
| 6 | `Version/commits.md` | KEEP · `improve` | Content-addressed commit-DAG, six-type CRDT algebra, `CrdtOpWire` `[MessagePack.Union]` flagship (S4). **`[V4]/H1` MINT `CommitFault`/`CrdtWireFault : Expected`** — disk `:358`/`:400`/`:411` bare `Error.New(8261/8264/8263)`, ZERO union today; NOT "register as-is". `[V10]` near-linear `MergeBase` (one reverse-reachability generation-mark pass + dominance filter, killing the per-candidate `Rank` re-run `:108-113`; BIM:94 the named consumer). [B] raw-hash re-anchor (`CommitNode` `:91`, `CrdtWire.ContentKey` `:354`, parity corpus `:375-376,401`). PIN `CRDT_OP_SET` (`MvRegister`/`opMerge` converging byte-identically — `reconciliation.md#ONE_WIRE_FIXTURE_CORPUS` DESIGN-PIN → REAL). `[V10]` dead `VectorOrder`/`Order` + `ContentParityCorpus.Seed` wire-or-fold. Band **Commit 8260** (MINT). `Version/` | `Crdt.Apply` (`CrdtField` dispatch) + `CommitGraph.MergeBase` + `ContentParityCorpus` (NONE — static surfaces) | `→ typescript:wire` (CrdtOpWire msgpack union, ARCH:44); `⇄ python:runtime/transport` (None-companion + parity corpus, ARCH:45); `→ typescript:state` (ARCH:46); `← BIM:92/94` (align spelling to `commits#MergeBase`) | 2 |
| 7 | `Version/timetravel.md` | KEEP · `improve` | ONE materializer AS-OF (`GraphDelta.ReplayOnto` via `AggregateStreamAsync`), two-axis RangeDiff, monotone bisect, non-crypto checkpoint chain. **`[V12]/S3` `AsOfKey`=`Checkpoint.Address` arm** (the seam `ContentAddress.OfGraph` digest; the field exists `:84`; ONE content digest serving icechunk + recovery oracle, while the prior-linked `Hash` chain (`:154-158`) stays tamper evidence per [B]; `recovery.md:65` `RecoveryPoint.AsCut()` resolves to it). [B] edge keys → `ContentAddress.Of(span)` (H3, `:203-204`). `[V9]` Scrub/Bisect re-shape to incremental `OfGraph(prior,delta)` (interim whole-graph recompute documented). `[V10]` `AggregateStreamToLastKnownAsync` drop-or-compose. Band NONE (composes graph/codec digests). `Version/` | `TimeLog.Reconstruct/Diff/Scrub/Bisect` + `Checkpoint.Anchor` + `TimeCut` vocabulary (NONE) | `← python:data/gridded/virtual` (icechunk AsOfKey over the kernel seed, ARCH:50 — target LIVE, capability realized); `→ graph` (TimeCut FROZEN, V5d); `→ recovery#ReAttest` (AsOfKey oracle) | 2 |
| 8 | `Version/merge.md` | KEEP · `improve` | Re-ingest `Reconcile` GlobalId alignment before diff, two-axis three-way merge, `always-succeeds-with-annotations` `MergeOutcome`. `[V8]b` Type-correlation key row (classification-independent Type natural key mirroring `ExternalKey`, so a re-keyed Type diffs as RENAME; COUPLED to V8a — reads the kernel seed on landing OR ships its own classification-excluded seed interim). `[V10]` thread `ElementJson.Options` STJ through the `JsonPatchDocument` insert arm (`:390`). MemberPath NodeId typing carried VERIFIED-CLEAN with a leg-2 re-verify obligation (`[11]`). Band NONE (`MergeConflict` is a conflict class). `Version/` | `StructuralMerge.Reconcile` + `EditOp`/`EntityEdit` + `ExternalKey`(+Type key) (NONE — diff surface) | `← Rasm/Spatial/reconciliation` (GeometryHash RE-TARGETED OFF topology ONTO `Version/merge#STRUCTURAL_DIFF`; `GraphNode.GeometryHash` the named consumer, ARCH:55; adjacency-vs-Representations digest gap surfaced to the geometry `[V8]` counterpart, `Rasm/ARCHITECTURE.md:79` re-points); `→ typescript:wire` (JsonPatch RFC-6902, ARCH:47) | 2 |
| 9 | `Version/provenance.md` | KEEP · `improve` | Exact W3C-PROV-O typing, independent-`digestOf` attested verify (makes `Unauthored` reachable), Merkle transparency-log proofs — the federation-wide verify template. `[V10]` Principal-derived agent class (read the class off `Principal`, not unconditional `Person` `:257`). [B] `AgentKey` re-anchor (`:291` → `ContentHash.Of`); the internal Merkle rolling digests (`:229,:321`) stay defensible-local. Band NONE (`AttestVerdict.Unauthored` is a verdict). `Version/` | `ProvNode.Of` + `AttestedLedger.Append/Verify` (NONE) | `← python:artifacts/provenance` (signed-artifact content-key binding + attested-ledger authority, ARCH:51 — the `[ARTIFACT_CONTENT_KEY_FEDERATION]` home); `→ Query/federation#SourceKind.SignedArtifact` (binding resolution) | 2 |
| 10 | `Version/retention.md` | KEEP · `improve` | Six-row `RetentionClass`, fail-closed `RetentionCeiling`, full-history reachability `Mark` over EVERY `TimeCut`, ONE receipted executor. `[V10]` `StorageLane.Durable` consume-in-a-guard-or-delete (`:36`). `[V5]c` FROZEN imports: `StorageTier` (blobstore-owned) + `TimeCut` (timetravel-owned). Band **Retention 8280**. `Version/` | `RetentionSweep.Run` + `RetentionClass` `[SmartEnum]` (NONE — one receipted fold) | `← Rasm.Compute` (content-keyed Assessment blobs, ARCH:52); `← Store/blobstore#StorageTier` (frozen-vocab, V5c); `← Version/timetravel#TimeCut` (frozen-vocab) | 2 |
| 11 | `Version/recovery.md` | KEEP · `improve` | Real `(Timeline,Lsn)` `RecoveryPoint` via Npgsql `IdentifySystem`, six-step ranked restore, `ReAttest` content-identity commit-point. `[V10]` the two verified RPO bugs: `ObjectReplica` real freshest-replicated-blob lag (killing `Duration.FromMinutes(absent.Count)` `:180`); `PgPitr` WAL-throughput → explicit policy row (killing 16-MiB-segment-as-rate `:171`). `[V12]/S3` terminal proof: reconstructed `OfGraph` == checkpoint `AsOfKey`, `RecoveryFault.VerifyFailed` unreachable on a clean restore. `[V5]c` FROZEN import `ObjectStore.Head`. Band **Recovery 8290**. `Version/` | `RecoveryRoutes.Run` + `RecoveryRoute` `[SmartEnum]` + `RestoreStep` (NONE — static choreography) | `← Rasm.AppHost/Runtime` (RPO/RTO objective INJECTED inputs, ARCH:64); `← Store/blobstore#ObjectStore.Head` (frozen-vocab, V5c); `← Version/timetravel#AsOfKey` (content-identity oracle) | 2 |
| 12 | `Version/egress.md` | NEW · `new` | **`[V3]/S2` CDC egress owner** (ruled site: beside the changefeed; `Sync/` revival refused for lack of a second sync sibling). ONE `EgressPump` fold draining the `[V2]` outbox cursor past `OpLogEntry` rows into `EgressSink` delivery rows under ONE CloudEvents envelope. Exactly-once EFFECT: `id`=`OpLogEntry.ContentKey`, `Sequence`=cursor `long`, `Partitioning.partitionkey`=`EntityKey`. Per-sink dedup as composition: `Nats` maps `id`→`Nats-Msg-Id` + Settle-ack advances; `Webhook`(`pg_net`) advances ONLY on `net.request_status`=SUCCESS reconciliation (PENDING holds, ERROR/timeout dead-letters, UNLOGGED crash → held cursor re-posts under the idempotency-key header); wire-native gRPC/Arrow READS AppHost `HopPolicy` delivery-honesty. Sinks: webhook/nats/kafka(+serdes)/rabbitmq/pulsar/wire-native as seed DATA. **Durable-ops-ONLY boundary**: the pump drains the durable outbox/`OpLogEntry` stream; presence/awareness stays `ledger#PRESENCE` (the lossy `AwarenessLane` `DrainSurface`, `ledger.md:467`) and NEVER enters the exactly-once CDC envelope. Typed dead-letter + replay; `EgressFault` = `DeadLetter`/`SinkRefused`/`CursorStall`/`DeliveryUnconfirmed` (the last types the `pg_net` PENDING/ERROR unconfirmed state the advance law reconciles). Band **Egress 8270** (827x — tight Version-decade packing). `Version/` | `EgressPump.Drain(SyncCursor, EgressSink) → EgressReceipt` (`EgressSink` `[Union]` axis) | `← Store/coordination#OUTBOX_CURSOR` (drains the cursor — intra-leg, no reverse edge); `← Version/ledger#CHANGEFEED` (OpLogEntry rows); `→ Rasm.AppHost/Wire/outbox` (keyed OutboundHop egress counterpart, APPHOST:73); reads AppHost `HopPolicy` (RASM-CS-APPHOST-BRIEF `[V9]`) | 4 |

### `Query/` — 7 pages (read lanes split by consistency demand + the reuse index)

| # | path | action · lowering | owner charter · home · band | entry signature | seams (in ← / out →) | leg |
|---|---|---|---|---|---|---|
| 13 | `Query/lane.md` | SPLIT · `rebuild` (split-out — sheds the SPLIT-from sections row 14 names) | Drops to routing + set-algebra: `ReadRouter` (sync authoritative vs async analytical), `StalenessWatermark` sequence-gap, `ElementSet`/`SetExpr`/`ElementSetAlgebra.Evaluate` + length-framed `Canonical` parity contribution. The `FUSION_AND_CACHE`+`VECTOR_CODEBOOK` ANN subsystem leaves to `retrieval`. Route `WaitForNonStaleProjectionDataAsync` through `IProjectionDaemon`/`IMartenDatabase`, not `TestingExtensions`. Band NONE (set-algebra total post-split). `Query/` | `ReadRouter.Route` + `ElementSet.Evaluate(SetExpr, SetResolve)` (`SetExpr` `[Union]`) | `⇄ python:data/tabular/query` (ElementSet receipt currency STAYS here, ARCH:57a); `→ Query/federation#SetExpr` (lowering target); `→ retrieval#VECTOR_CODEBOOK` (absorb-out) | 3 |
| 14 | `Query/retrieval.md` | NEW · `new` · SPLIT-from `Query/lane.md#[04]-[FUSION_AND_CACHE]`+`#[05]-[VECTOR_CODEBOOK]` (`:267-520`; lane's paired `rebuild` sheds the sections in the same leg batch — engine `absorb` UNSET, the source survives) | The coupled ANN subsystem: `FusionRank.Fuse` (weighted n-ary reciprocal-rank fusion, pgvector SQL binding) + `VectorCodebook.Train`/`AdcScan` (PQ k-means, coarse→fine ADC scan). **`[V4]/V5b` MINT `RetrievalFault : Expected`** (kills the lane codebook's bare `Error.New(8360..8363)` `:432-441` — the only un-banded lane owner). `[V6]` pgvector `<->`/`<=>` LINQ server-side ANN row beside the in-process PQ/ADC hot-set lane (residency is row data on the one retrieval axis; the column-mapping-only demotion is NOT taken — corpus-scale ANN belongs to the pgvector/pgvectorscale indexes while the PQ/ADC row keeps the hot set, so the server-side row is not redundant beside the PQ lane). Band **Retrieval 8410**. `Query/` | `Retrieval.Fuse` + `VectorCodebook.Train`/`AdcScan` (`RetrievalOp` `[Union]`) | `⇄ Rasm.Compute/Model/embedding` (VECTOR_CODEBOOK — `VectorRow.ContentKey` ↔ `EmbeddingVector.ContentKey`; COMPUTE:99 declares, post-split targets `retrieval`); `← pgvector` column map; `→ cache` fusion result | 3 |
| 15 | `Query/topology.md` | KEEP · `improve` | In-process QuikGraph view, frozen incidence, the DEFAULT synchronous topology owner. **`[V10]/NEW` `Lca` DAG-gate** (`Lca`/`Ancestor` pre-gate `tree.IsDirectedAcyclicGraph()` and rail `TopologyFault.Cyclic`, symmetric with the already-gated `Order`; `api-quikgraph:26` flags `OfflineLeastCommonAncestor` unsound over cyclic input; topology models containment cycles as real). `[V10]` `TypedEdge.IsContainment`/`Kind` dead-accessor prune. Band **Topology 8370** (8370-8371, keeps 837x). `Query/` | `TopologyView.Of` + `TopologyQuery` `[Union]` | `← Rasm.Bim` (soft anchors → cache); `→ Element seam` (per-snapshot `ContentAddress.OfGraph` memo stays). **ARCH:55 NO LONGER targets topology** (re-pointed to merge) | 3 |
| 16 | `Query/columnar.md` | KEEP · `improve` | ONE in-process DuckDB engine, co-transactional `BimOpenSchemaProjection : FlatTableProjection` (Persistence-owned generic branch — this DECISION rules it, aligning ARCH:56; Bim supplies the typed schema seed only), ParquetSharp, ADBC. `[V10]/E14` typed trust gate (`Identifier`/`StorePath`/`SecretName` value-object family gates `Mount`/`Secret`/`Egress {projection}`/`Generation`; `{projection}`→composed typed projection, never raw SQL). `[V10]` posture constant (the four `"80%"`/`"90%"` literals `:80-83` collapse to one). `[V10]` phantom-spellings closed (`ExecuteQueryAsync`→`ExecuteQuery`; `BimData.WriteDuckDB`→`frames.ToDataSet().WriteToDuckDB`). **ADD `ColumnarExtension.Substrait` row** (DuckDB `from_substrait(blob)`, V1 lane). Band **Columnar 8350** (8350-8356, the WHOLE decade — collision winner). `Query/` | `Columnar.Run(ColumnarQuery)` over `ColumnarProfile`/`ColumnarExtension` axes | `← Rasm.Bim/Model` (BimOpenSchema seed, ARCH:56); `⇄ python:data/tabular` (Arrow over ADBC, ARCH:62); `← BIM:81` (GDAL/OGR GeoParquet columnar ingest — HOMED, the 13th sibling row); `← Query/federation` (tabular-subtree execution) | 3 |
| 17 | `Query/cypher.md` | KEEP · `improve` | Injection-safe server-side `format('%L')` composition, three-space result discipline (AGE→ElementSet, pgrouting→H3Cell, pgr_bridges→raw long). **`[V4]` `GraphFault`→`CypherFault` RENAME** (disk simple-name ×2: `graph.md:154` `GraphFault` ns `Rasm.Persistence` vs `cypher.md:60` ns `Rasm.Persistence.Query`; graph keeps `GraphFault`). `[V10]` `AgtypePath.Weight`-always-0.0 dead carrier (populate or collapse to `Seq<NodeId>`). Band **Cypher 8360** (8360-8363, keeps 836x). `Query/` | `Cypher.Run(GraphQuery)` (`GraphQuery` `[Union]`) | `← Store/provisioning#ServerExtension.CreateSql` (frozen-vocab install rail, V5c); optional/demoted beneath QuikGraph | 3 |
| 18 | `Query/cache.md` | KEEP · `improve` | The compute-result reuse index: `ArtifactIndexRow` blob index + `ModelResultIndex` recency horizon + `BenchmarkRow` claim gate, sync-over-async `IBufferDistributedCache` L2. The `cache.md:20` one-`ArtifactKind`-row growth law is load-bearing for Compute/Fabrication/Materials durable rows (accepted `IDistributedCache` boundary — batching note, never a rebuild). Band NONE (storage leg). `Query/` | `Cache.Lookup/Register` over `ArtifactKind` axis + `CacheL2Store:IBufferDistributedCache` | `← Rasm.Compute` (ArtifactIndexRow/ModelResultIndex/BenchmarkRow INDEX, ARCH:58); `⇄ Rasm.AppHost/Runtime` (L2 partition CACHE_PORT — TenantId RLS + IBufferDistributedCache, ADD, APPHOST:69) | 3 |
| 19 | `Query/federation.md` | NEW · `new` | **`[V1]/S1` REINTRODUCE** (hinge ruled FEASIBLE_WITH_GAPS — retire NOT triggered; a router/lowerer over standing lanes, no second engine). Substrait protobuf bytes → `Substrait.Protobuf.Plan.Parser.ParseFrom` + `new SubstraitDeserializer().Deserialize` (shipped PUBLIC, ~2 lines — falsifies `api-flowtide-substrait.md:151`) → a `LoweringTarget` `RelationVisitor` (~150-250 LOC; reference `SubstraitToDifferentialComputeVisitor` = 139 LOC/9 kinds) onto `SetExpr` (SetRelation→Union/Intersect/Difference 3-for-3, ReadRelation+Filter→Predicate, VirtualTableRead→Literal, bounded Iteration→Closure, key-semijoin Join→Intersect) — the `SetExpr` arm executes over the store's EXISTING GiST/GIN indexes via `lane#SetResolve` (`lane.md:209`; `:139` GIN `jsonb_ops`, `:153` GiST), so the key-selection half rides no second engine — OR the columnar/ADBC lane (Project/Aggregate/Sort/TopN/Window→columnar; Write→REJECT fail-closed; Exchange→DROP). Plan digest = `ContentHash.Of(wireBytes)` (never a local `XxHash128`). Executes against ONE `TimeCut` (default: the `StalenessWatermark.HeadSequence` head, `lane.md:40` — a producer-supplied-no-cut plan resolves deterministically); returns the `ElementSet` receipt + Arrow batch; the `(plan-digest·cut·watermark)` triple content-addresses the result; replay is one `ArtifactKind` reuse row. **Receipt honesty**: the pinned cut is the LOCAL coordinate, so that replay row is deterministic ONLY for the local `ElementSet` subtree — external-source `SourceKind` rows (ADBC/warehouse) carry READ-TIME currency, never falsely presented as consistent-as-of the local cut. `SourceKind` capability axis (Substrait-native durable-store/signed-artifact/ADBC vs SQL-only staged). DROP `Grpc.Tools`; ADD DuckDB substrait extension row; `Google.Protobuf@3.35.1` (shared, admitted) the sole runtime dep. Band **Federation 8420**. `Query/` | `Federation.Execute(FederationPlan, TimeCut) → FederatedResult` (`FederationPlan` on `SourceKind`) | `← python:data/tabular/query` (Substrait portable-plan half, ARCH:57b SPLIT, signature-locked; ElementSet currency stays lane); `→ Query/lane#SetExpr` + `→ Query/columnar` (intra-leg lowering/execution); `← provenance#SignedArtifact`; `← BIM:91` (federation AuditEntry log counterpart) | 3 |

### `Ingest/` — 2 pages (the one-file-folder violation ends; a real codec-ingress axis)

| # | path | action · lowering | owner charter · home · band | entry signature | seams (in ← / out →) | leg |
|---|---|---|---|---|---|---|
| 20 | `Ingest/tabular.md` | KEEP · `improve` | One rectangular-data owner: MiniExcel spreadsheet + **`Sep` as EXPLICIT owner law** (a fenced SIMD delimited-lane surface, the `[V11]` charter made real). `[V10]/NEW` `TabularWire.Wire` disposition (compose the `DynamicExcelColumn` `CustomFormatter` into `Policy().DynamicColumns`, OR delete `Wire` — the STJ `Bind<T>` is the single wired path). `linq2db.EntityFrameworkCore` CONDITIONAL: composes a real BulkCopy fence → KEEP, else LEAVES (DuckDB `COPY` + Npgsql binary import already own the bulk lane, so absent a composed fence it leaves). `[V4]` `TabularFault` RE-BAND off 837x. Band **Tabular 8390**. `Ingest/` | `TabularSource.Run(TabularSpec)` (`[Union]`) | `→ Rasm.Element` (row-shape only, ARCH:61); `← Compute:115` (Extract counterpart → graph/Ingest) | 3 |
| 21 | `Ingest/schedule.md` | NEW · `new` | **`[V11]` schedule-file codec** (`MPXJ.Net` .mpp/XER/PMXML → the record rail) + durable schedule rows — the persisted `TaskRelation` predecessor/successor/lag activity-network DAG (the load-bearing payload a P6/MS-Project store keeps, MPXJ's relation model), the Persistence half of the relocated Bim schedule domain (TASKLOG:47 moved the CPM/4D COMPUTATION to `Rasm.Bim`; the durable P6/MS-Project STORE + file codec is ingest-shaped). Mints because BIM:102 supplies the durable-schedule consumer (fold-up alternative NOT taken). Band **Schedule 8400** (`ScheduleFault` = `CodecReject`/`UnknownDialect`). `Ingest/` | `ScheduleSource.Run(ScheduleSpec)` (`[Union]`) + the `TaskRelation` durable-DAG rows | `← Rasm.Bim/schedule` (P6/MS-Project 4D → HOME, BIM:102 counterpart); `→ Rasm.Element` (schedule→element row-shape) | 3 |

### `Store/` — 3 pages (durable-home + coordination substrate)

| # | path | action · lowering | owner charter · home · band | entry signature | seams (in ← / out →) | leg |
|---|---|---|---|---|---|---|
| 22 | `Store/blobstore.md` | KEEP · `improve` | Nine-delegate `ObjectLeg` (four providers fill once), write-once 412-noop seal, `RemoteStoreFault` structural `Lift`, write-blob-first + full-history GC. `[V10]` `MultipartTransfer.Upload`+`BlobTransferReceipt` become THE composed receipt path (or both die). **E9 checksum honesty** (`ObjectChecksum` axis has `XxHash128`/`Crc64`/`None` rows `:32-37`; GCS/Minio rows read the SDK-native `Crc64`/`None` stance `:42-43`, NOT the decorative `XxHash128` only `S3Leg.Seal` supplies via `Integrity.Wire`). `[V10]` `BlobResidence.Correlation` thread-from-write-op-or-drop. `StorageTier` FROZEN vocabulary (V5c — blobstore-owned; `Store/tier` extraction NOT taken). Band **RemoteStore 5400**. `Store/` | `BlobStore.Put/Get/Seal` over `ObjectStore` `[SmartEnum]` (4 providers) | `← Rasm.Compute` (GLB by RepresentationContentHash, content-keyed write-first, ARCH:59); `← Rasm.Bim/Exchange` (IFC/BREP IfcRepHash GLB, ARCH:60); `→ Rasm.Compute GeometrySource` (`Placement` egress row, C2, currently prose — rowed explicit); `← Rasm.AppUi/Collab/sync` (snapshot-accelerator rows, content-keyed derivable-class retention, never SoR) | 4 |
| 23 | `Store/provisioning.md` | KEEP · `improve` | Verification-first six-command `NpgsqlBatch` fold, `FailureRank.Absorb` absence policy, `ExtensionAdmission` install preconditions, `EngineOps` `Handle`-bridge capsule, embedded residency-split ritual. **`[V4]` `ServerFault` RE-BAND off 835x** (Columnar keeps 835x; ServerFault→838x, absorbs the loose `Error.New` 8371-8375/8379/8380 `:72-74,292-293,320,325` as typed cases). **`EmbeddedStore.Refused` 7701/7702 → `EmbeddedFault.Refused`** (disk `:433/:440`, the fourth breach the E4 census omitted, in-banded to Embedded 771x). `[V6]` embedded charter HONEST (Marten PG-only → `StoreProfile.Embedded` (SQLite) carries NO ElementGraph event-sourcing path — the real charter is relational identity floor + `EngineOps` checkpoint/snapshot/backup + read-only/offline tier, never SoR). `ServerExtension.CreateSql` FROZEN install rail (V5c). `Npgsql.OpenTelemetry` observability row. Bands **Server 8380** + **Embedded 7710**. `Store/` | `ClusterProvision.Verify/Admit/Reload` + `EmbeddedStore.Open` over `ServerExtension`/`StoreProfile` axes | `← Rasm.AppHost/Observability` (Npgsql-ONLY HEALTH_PROBE, ARCH:63; APPHOST:85 drops Redis/Kafka); hosts the `[V13]` embedded/KV-floor + SoR-spine axes | 4 |
| 24 | `Store/coordination.md` | NEW · `new` | **`[V2]` the four AppHost PORT contracts** on ONE token-VALIDATING fenced-lease store. `CoordinationOp` `[Union]`: `BudgetDebit`, `StepStateCas`+`StepStateInFlight`(READ)+`StepStateLoad`, `LeaseAcquire`/`Renew`/`Release`+`ExpiredScan`(READ), `MembershipUpsert`/`MembershipScan`(READ), `OutboxAdvance`. **ONE internal shape** — every guarded-write case folds through ONE fenced-CAS predicate; every READ case (`StepStateInFlight`/`ExpiredScan`/`MembershipScan`/`StepStateLoad`) through ONE `Received`-style projection; `Coordinate.Run` discriminates the closed `[Union]` via the generated total `Switch`, EXACTLY `graph#STORE_RAIL`'s idiom (`graph.md:217`). ALL READ cases carry the frame's tenant RLS predicate STRUCTURALLY (the same tenant guard the writes hold), so no read leaks cross-tenant in-flight/lease/membership state. **Budget = fenced predicated compare-and-decrement, PER-UNIT VECTOR** — `UPDATE budget_ledger SET balance_i = balance_i - debit_i WHERE tenant = @t AND lease_token >= @held AND balance_i >= debit_i` FOR EVERY requested unit atomically `RETURNING` (N unit balances mirroring `CostVector`, `capability.md:54-62`; a scalar debit is falsified by the multi-unit consumer; a stale token is a typed `LeaseFenced`, never a lost update). Kleppmann fencing: the guarded write REJECTS a token older than the row's lease generation via the SAME row-CAS predicate; `LeaseAcquire` MINTS that generation MONOTONICALLY (`++` via PG row-CAS `RETURNING generation`) — the mint side that makes the token VALIDATED, not merely issued. READ cases beside guarded writes (`StepStateInFlight` for `CrashResume` `orchestration.md:271,284`; `ExpiredScan` orphan-reclaim). **`ONE_OUTBOX_EGRESS_SPINE` NAMED** (the Marten event stream IS the outbox — same-`IDocumentSession` guarantee; mints the durable drain cursor PER-SINK — `outbox_cursor(SinkKey, long Sequence)`, so each `EgressSink` drains the one stream independently (distinct from the per-origin `SyncCursor` `ledger.md:114`) — with the at-least-once advance law; `OutboxAdvance(SinkKey Sink, long Through)` is the cursor-advance case the pump calls; `[V3]` egress consumes it). Composes Marten `FetchForWriting`/`QueueSqlCommand` + Npgsql `pg_advisory_xact_lock` + LISTEN/NOTIFY — never a second event store, never a distributed-lock sidecar. `CoordinationReceipt` projects PER-OP: `BudgetDebit`→the POST-debit `HashMap<string,long>` balance vector (the metering consumer needs no follow-up read), the CAS/lease/membership writes→their committed row, the four READ cases→their loaded rows. Band **Coordination 8430**. `Store/` | `Coordinate.Run(CoordinationOp, Option<LeaseToken>, ProjectionContext frame) : IO<Fin<CoordinationReceipt>>` (the frame carries tenant/clock/correlation per [A.1]) | `⇄ Rasm.AppHost/Agent/capability` (fenced per-tenant Budget debit, APPHOST:76); `⇄ Rasm.AppHost/Runtime/orchestration` (step-state CAS + InFlight, APPHOST:77); `⇄ Rasm.AppHost/Wire/outbox` (transactional outbox same-tx, APPHOST:78); `⇄ Rasm.AppHost/Wire/Coordination` (CAS+lease+membership, APPHOST:79) — **`MembershipView.Serving` (`Wire/coordination.md:79`) is the in-process consumer of the membership rows**; `→ Version/egress#OUTBOX_CURSOR` (the pump drains it, no reverse edge) | 4 |

## [03]-[BAND_REGISTRY] — the re-partitioned 83xx map (duplicate fails at type-init)

ONE `FaultBand` `[SmartEnum<int>]` sited on `Element/graph.md#[FAULT_TABLES]` (the store-rail root every rail composes), mirroring `.archive/RASM-COMPONENT-PARADIGM-DECISION.md:141-149`. Each fault union derives `Code => Band + n`; a duplicate band integer FAILS at type initialization. Every loose receipt code is a typed case or registered sub-band. Per-page decade prose (`topology:166`, `columnar:128`, `cypher:49`) dies for one registry pointer. Lands COMPLETE in leg 1 (all 19 own + new-owner + re-banded decades assigned, so a duplicate is unrepresentable from leg 1); each union re-derives in its owning leg (the `CypherFault` rename executes leg 3).

### Own bands — 19 unique own decades (all three collisions + Commit re-partitioned)

| Band | Union | Owning page | Codes | Disposition |
|---|---|---|---|---|
| 5400 | `RemoteStoreFault` | `Store/blobstore` | 540x | register as-is |
| 7710 | `EmbeddedFault` | `Store/provisioning` | 7711-7714 **+ Refused (7701/7702 absorbed)** | register + absorb the census-missed loose 770x cluster (disk `:433/:440`) |
| 8250 | `SyncFault` | `Version/ledger` | 8251-8256 | register as-is (the one correct typed band on disk) |
| 8260 | `CommitFault`/`CrdtWireFault` | `Version/commits` | 8261,8263,8264 | **MINT typed `[Union]:Expected`** (H1 — disk: bare `Error.New`, ZERO union; NOT "register as-is") |
| 8270 | `EgressFault` | `Version/egress` | 8271-8274 | **NEW** (827x — the free Version-decade gap between Commit and Retention) — `DeadLetter` · `SinkRefused` · `CursorStall` (held-cursor) · `DeliveryUnconfirmed` (types the `pg_net` `net.request_status`=PENDING/ERROR reconciliation the advance law describes) |
| 8280 | `RetentionFault` | `Version/retention` | 8281-8283 | register as-is |
| 8290 | `RecoveryFault` | `Version/recovery` | 829x | register as-is |
| 8300 | `GraphFault` | `Element/graph` | 8300-8302 | register as-is (keeps the simple name; cypher renames) + registry HOST |
| 8310 | `CodecFault` | `Element/codec` | 8310/8320/8330 | register as-is (legal 831x-833x multi-decade stride) |
| 8340 | `IdentityFault` | `Element/identity` | 834x | register as-is (authority composes it, no new band) |
| 8350 | `ColumnarFault` | `Query/columnar` | 8350-8356 | KEEP 835x — the WHOLE decade (7 cases, disk `:143-149`), collision winner |
| 8360 | `CypherFault` | `Query/cypher` | 8360-8363 | KEEP 836x + **RENAME from `GraphFault`** — collision winner + simple-name ×2 resolved |
| 8370 | `TopologyFault` | `Query/topology` | 8370-8371 | KEEP 837x — collision winner |
| 8380 | `ServerFault` | `Store/provisioning` | re-banded 838x | **RE-BAND off 835x** + the loose `FailureRank`/readiness/`Admit` 8371-8375/8379/8380 become typed cases |
| 8390 | `TabularFault` | `Ingest/tabular` | re-banded 839x | **RE-BAND off 837x** (was 8370-8373, three-way collision) |
| 8400 | `ScheduleFault` | `Ingest/schedule` | 8401-8402 | **NEW** — `CodecReject` (bad .mpp/XER/PMXML) · `UnknownDialect` (the MPXJ codec-ingress fault surface) |
| 8410 | `RetrievalFault` | `Query/retrieval` | 841x | **MINT** (kills the lane codebook bare `Error.New(8360..8363)` `:432-441` — the only un-banded lane owner) |
| 8420 | `FederationFault` | `Query/federation` | 8421-8425 | **NEW** — `SubstraitParse` · `UnsupportedRelation` (fail-closed) · `SourceUnreachable` (external ADBC/Flight endpoint down — a distinct availability fault, the owner reaches LIVE external warehouses) · `WriteRejected` · `SourceUncapable` (a SQL-only `SourceKind` cannot accept a Substrait plan — the capability axis's negative case) |
| 8430 | `CoordinationFault` | `Store/coordination` | 8431-8436 | **NEW** — `LeaseFenced(LeaseToken Stale, long Current)` · `CasConflict(WorkflowKey, StepKey, StepState Expected, StepState Found)` · `BudgetExhausted(string Unit, long Requested, long Available)` (string unit key per [A.1]) · `LeaseExpired(LeaseKey, HolderId)` · `MembershipLapsed(MembershipKey, MemberId)` · `OutboxDrain(SinkKey Sink, long Through)` (failed `OutboxAdvance` cursor-CAS — the coordination-side write the V3 pump depends on, kept inside the fenced store's own rail, never an `EgressFault` delivery fault) |

**Folded-transaction concurrency disposition** (completes the BAND-UNIQUE gate for the deleted-page fold): the deleted `Query/transaction` folded `StoreFault.Concurrency` (7001) into `graph#STORE_RAIL` (`TASKLOG:37`); the register census finds ZERO 700x/`StoreFault` on disk, so the ruling is FOLD into `GraphFault` 8300 — a concurrency conflict raised by the folded rail registers as the `GraphFault` sub-band row 8303, NEVER a loose 7001 integer. **Federation-wide-renumber escalation NOT triggered**: the re-partition fits the 5xx/77x/82x-84x space entirely with 8440+ growth headroom above `CoordinationFault` 8430, so the brief `[06]` federation-wide fault-band renumber escalation stays un-triggered.

No-band pages (total algebras / verdict cases / storage legs): `Element/authority`, `Version/timetravel`, `Version/merge`, `Version/provenance`, `Query/lane`, `Query/cache`.

### Pinned-mirror foreign rows (cross-package disjointness is a row, never prose)

AppHost 1xxx/4100-4810 · Compute 2200-2299 + Remote `WireFault` 4520-4532 · AppUi 6xxx · AEC registry 2300/2350/**2400 (mirror of kernel `GeometryFault` 2400-2449, `Rasm/.planning/Numerics/faults.md`)**/2450/2470/2500/2600/**2700→2701-2710** · kernel-substrate `Fault.UnsupportedCode` 9104. Precedent: `RASM-CS-APPHOST-BRIEF.md` `[V1]` reciprocal pin.

## [04]-[SEAM_LEDGER]

### Own-row corrections (`ARCHITECTURE.md#SEAMS` ARCH:41-64)

| Row | Correction |
|---|---|
| ARCH:50 icechunk AsOfKey (**S3**) | KEEP target (LIVE `Version/timetravel`); capability realized — `AsOfKey`=`Checkpoint.Address` (V12; the prior-linked `Hash` chain stays tamper evidence, [B]); the phantom was the member, not the page |
| ARCH:53 identity KMS PORT | SPLIT — `ObjectAcl` store → `Element/authority`; TenantId RLS + KMS keyrings → `Element/identity` (APPHOST:72 unchanged) |
| ARCH:54 frame ingredients | re-express as INJECTED port-input shapes Persistence DEFINES (ClockPolicy/CorrelationId/TenantContext + the Persistence-owned `Principal` value-object, not an AppHost reference, [A.1]); csproj:10 AppHost ProjectReference DELETES; frame-to-kernel re-home recorded as a contingent KERNEL/APPHOST counterpart obligation |
| ARCH:55 reconciliation GeometryHash | RETARGET `Query/topology` → `Version/merge#STRUCTURAL_DIFF` (topology zero reconciliation refs; merge's `GraphNode.GeometryHash` the consumer); adjacency-vs-Representations digest gap → geometry `[V8]` counterpart (`Rasm/ARCHITECTURE.md:79` re-points) |
| ARCH:56 columnar BimOpenSchema | RE-SCOPE "Bim-implemented" → Persistence-implemented generic `BimOpenSchemaProjection : FlatTableProjection`; Bim supplies the typed schema seed only |
| ARCH:57 lane ElementSet + Substrait (**S1**) | SPLIT — ElementSet currency stays `Query/lane`; the Substrait portable-plan half → `Query/federation` (V1 reintroduce) |
| ARCH:63 provisioning HEALTH_PROBE | KEEP Npgsql-only; the stale side is APPHOST:85 (drops Redis/Kafka) |

### ADD (wired-undeclared own rows + minted-owner seams)

- `Query/retrieval ⇄ Rasm.Compute/Model/embedding` VECTOR_CODEBOOK (post-V5b; COMPUTE:99 declares)
- `Query/cache ⇄ Rasm.AppHost/Runtime` CACHE_PORT L2 partition + TenantId RLS (APPHOST:69 declares; distinct from ARCH:58 Compute INDEX)
- `Version/ledger ← Rasm.AppHost/Wire/companion` PRESENCE PeerRoster beats over DrainSurface (`ledger.md:468` — the roster produces, DrainSurface transports; Persistence-owned wire row, no AppHost type down)
- `Version/ledger#CHANGEFEED ← Rasm.AppUi/Collab/Editing` edit-intent projection + per-doc replay-window READ **AND** `← Rasm.AppHost/Runtime/determinism` neutral-log window-read — ONE windowed-read case parameterized by origin/entity/window serves BOTH (RASM-CS-APPUI `[V2]` / RASM-CS-APPHOST `[V10]`; text-container contingency rides the EXISTING `CrdtOpWire` `RgaSequence`, zero new wire row)
- `Store/blobstore ← Rasm.AppUi/Collab/sync` snapshot-accelerator rows (content-keyed, derivable-class retention, never SoR)
- `Store/blobstore#Placement ⇄ Rasm.Compute GeometrySource` (C2 — prose-only today, rowed explicit)
- The four `Store/coordination` PORT counterparts (APPHOST:76-79) + **`MembershipView.Serving` (`Wire/coordination.md:79`) in-process consumer**
- `Version/egress → Store/coordination#OUTBOX_CURSOR` (intra-leg cursor edge, forward-only — coordination never reads the pump — **S2**)
- `Query/federation → Query/lane#SetExpr` + `→ Query/columnar` (intra-leg lowering/execution edges — **S1**)

### The twelve stale sibling → Persistence rows (counterpart obligations; sibling interiors OUT of edit scope)

Each row re-points in its own campaign from this table; this DECISION lists the corrected target. All twelve confirmed on disk at the cited anchor.

| Sibling row | Dead target | Corrected target | Campaign |
|---|---|---|---|
| COMPUTE:111 FastCDC content-key delta | `Sync` | `Element/codec#CONTENT_CHUNKER` | Compute |
| COMPUTE:115 parse-to-canonical Extract | `Query/pipeline` | `Element/graph#STORE_RAIL` / `Ingest` | Compute |
| COMPUTE:116 anomaly rule source | `Store/quality` | `Query/columnar` + `Store/provisioning` verification surfaces | Compute |
| COMPUTE:119 Protobuf Kafka topics | `Sync` | retire with the `[V3]`/`[V7]` streaming disposition | Compute |
| BIM:91 federation AuditEntry log | `Query/federation` | `Query/federation` (V1 REINTRODUCED — owner re-lives) | Bim |
| BIM:95 IFC validation rules | `Store/quality` | `Query/columnar` + `Store/provisioning` quality surfaces | Bim |
| BIM:101 durable annotation + CDE | `Sync/annotation` | `Version/ledger#CHANGEFEED` | Bim |
| BIM:102 P6/MS-Project 4D | `Sync/schedule` | `Ingest/schedule.md` (V11 NEW) | Bim |
| BIM:104 Speckle Base import | `Sync` | `Version/ledger` (Speckle SyncTransport) | Bim |
| APPHOST:71 drain 2PC in-doubt | `Query/transaction` | `Element/graph#STORE_RAIL` — prepared-tx RETIRE (the single-`IDocumentSession` spine mints no `pg_prepared_xacts`) | AppHost |
| APPHOST:73 keyed OutboundHop egress | `Sync/egress` | `Version/egress.md` (V3 NEW) | AppHost |
| APPHOST:85 health-probe driver roster | Npgsql/Redis/Kafka | `Store/provisioning`, Npgsql-only (matches ARCH:63) | AppHost |

Additional recorded sibling rows (not in the twelve; counterpart obligations): BIM:81 (`Rasm.Bim/ARCHITECTURE.md:81` `Semantics/geospatial ← Rasm.Persistence/Store`) GDAL/OGR GeoParquet → `Query/columnar` (HOMED, the 13th sibling row; the Bim campaign re-points the interior); BIM:94 `CommitGraph.MergeBase` spelling → align to `commits#MergeBase` (static, no `CommitGraph` type); COMPUTE:99 `Model/embedding ⇄ Query/lane` VECTOR_CODEBOOK → `Query/retrieval` (the post-V5b page home; the Compute campaign re-points the target).

## [05]-[ROSTER_DELTA] — INTEGRATION-FIRST; per-package `[V13]` ceiling proofs; `.api` obligations in the same motion

Central owner `Directory.Packages.props` Persistence block (`:251` label-grouped, hand-edited) + `Rasm.Persistence.csproj`. Manifest motion lands in leg 1 — every ruled prune is zero-page-consumer, so the prune is REVERSIBLE by construction: a leaving package takes its `.api` catalog + README group row + csproj `PackageReference` + AppHost probe row in ONE motion, and re-admission (when a later leg names a consumer) is the same one-row motion reversed, admitted through `[V13]` as a data row. License gate: OSS/free-for-OSS-commercial admissible; pay-tiered/seat-licensed/proprietary REJECTED.

### PRUNE (each on a per-package charter/redundancy proof — never a bare zero-consumer default)

| Package | Proof |
|---|---|
| `PollinationSDK` (`:307`) | charter misplacement — a domain cloud SDK with no store-class concern; re-admission belongs to whichever package names a consumer |
| `Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite` (`:300`), `Npgsql.NetTopologySuite` (`:302`), `NetTopologySuite.IO.GeoJSON4STJ` (`:137`), `NetTopologySuite.IO.GeoPackage` (`:138`) | no persisted NTS geometry column; H3 derivation rides `pocketken.H3` on the transitive core-NTS crossing (`api-h3.md:3` corrects to core `NetTopologySuite` 2.6.0, which survives) |
| `Microsoft.EntityFrameworkCore.Sqlite` (`:292`) | the embedded floor is raw ADO by design (`provisioning.md:369-434`) |
| `StackExchange.Redis` (`:314`, direct ref) | the L2 swap row keeps `Microsoft.Extensions.Caching.StackExchangeRedis` (an `IBufferDistributedCache`), which transits the driver |
| `ScyllaDBCSharpDriver` (`:311`) | **the single scale-out prune** — `[V13]` wide-column "only with a named owning axis"; the store perimeter's closed axes carry NO wide-column owner and no consumer names one (SoR spine + columnar + vector + embedded/KV + object + egress cover every store class); the wide-column concern is redundant against the columnar-OLAP ∪ LSM-KV union. FORWARD RE-ADMISSION recorded: re-admits as a wide-column-axis row if a consumer ever names a partition-key/clustering-column tunable-consistency workload the LSM-KV + columnar union provably cannot serve (not abandonment — no current axis owner) |

### KEEP as `[V13]` axis rows — per-package ceiling PROOF (INTEGRATION-FIRST, not asserted)

The `[V13]` STORE_AXIS_MAP is first-class content; every store-class concern names its axis, provider rows (deployment/policy DATA — any app composes any admitted provider through the one axis surface with zero central edits), and selection policy. Each kept scale-out row carries the PROVEN ceiling the in-PG/in-process owner cannot reach. Every provider row carries its provisioning/health/recovery posture through the verification-first fold.

| Axis | Owning page | Provider rows (seed DATA) | Selection | Scale-out ceiling proof |
|---|---|---|---|---|
| Object store | `Store/blobstore` | s3 · azure-blob · gcs · minio | `ObjectStore` `[SmartEnum]` | (landed) |
| Egress sink | `Version/egress` | webhook · nats · kafka · rabbitmq · pulsar · wire-native | `EgressSink` `[Union]` | `[V3]` |
| Read-lane/analytic engine | `Query/columnar` | duckdb-in-process · pg_duckdb-in-PG · **clickhouse-scaleout** | `ColumnarEngine` axis | `ClickHouse.Driver` KEEP (`:267`) — the horizontally-sharded columnar MPP row for billion-row cross-model OLAP the single-node in-process DuckDB and in-PG TimescaleDB provably cannot own at cluster scale (distributed merge-tree, not a second SoR) |
| Lakehouse interchange | `Query/columnar` | ducklake(extension, forward) · **delta** | format row | `DeltaLake.Net` KEEP (`:276`) — the Delta transaction-log wire for external-warehouse interop (Spark/Databricks) DuckDB's in-process catalog cannot emit; a format row, not an engine |
| Vector search | `Query/retrieval` | pgvector-in-PG · pgvectorscale-diskann · pq-adc-in-process · **qdrant-scaleout** | `VectorBackend` axis | `Qdrant.Client` KEEP (`:308`) — the billion-scale sharded ANN row (quantization + horizontal shard routing) over the in-PG pgvector/pgvectorscale ceiling at corpus scale |
| Embedded/KV floor | `Store/provisioning` | sqlite(raw-ADO) · **rocksdb-lsm** · **lmdb** | `EngineOps`-tier row | `rocksdb`+`LightningDB` KEEP (`:310`,`:285`) — write-optimized LSM (`rocksdb`) + read-optimized memory-mapped B+tree MVCC (`LightningDB`) `EngineOps`-tier rows over the transactional-SQLite floor the single-writer WAL engine cannot match on high-write-fan-out / zero-copy-read workloads |
| Relational SoR spine | `Store/provisioning` + `Element/graph` | postgres-18 (SINGULAR) | SEALED | the ONE event store · ONE materializer · ONE identity · ONE changefeed — unchallengeable; `ARCHITECTURE.md:115` re-scopes to exactly this spine boundary |

### `[V3]` streaming/messaging sink rows (INTEGRATION-FIRST — each leaves ONLY on a per-package redundancy proof)

KEEP-COMMIT the spine anchors: `NATS.Net`, `CloudNative.CloudEvents`(+`.SystemTextJson`), `pg_net` (extension row — server-side webhook). KEEP as sink ROWS (default): `Confluent.Kafka` + `Confluent.SchemaRegistry`(+`.Serdes.Avro`/`.Json`/`.Protobuf`) with `Chr.Avro`(+`.Binary`+`.Confluent`) as the codec column, `CloudNative.CloudEvents.Kafka`, `RabbitMQ.Client`, `DotPulsar`. A broker row leaves only on proven redundancy against an admitted sink of the same delivery semantics or feed-verified abandonment, taking its serdes/codec satellites + `.api` catalog + README group row + AppHost probe row (RASM-CS-APPHOST-BRIEF `[V6]`) with it.

### `[V1]` federation rows (KEEP — hinge ruled FEASIBLE)

`FlowtideDotNet.Substrait@0.15.0` (`:281` — the shipped-public `SubstraitDeserializer.Deserialize` is the transcription; `api-flowtide-substrait.md:151` corrected), `Apache.Arrow.Adbc@0.23.0` + `.Drivers.{Apache,BigQuery}` (source rows), `Apache.Arrow.Flight@23.0.0` (bulk-result hop). **ADD** a DuckDB `substrait` community-extension row (`ColumnarExtension.Substrait` — extension row, NOT a NuGet; verify a build for the pinned `DuckDB.NET.Data.Full` 1.5.x runtime, fail-closed on unsupported relations). **DROP** the `Grpc.Tools` codegen assumption (`:55` shared) — unnecessary AND counterproductive (regenerating substrait `.proto` mints a duplicate CLR `Plan` the shipped deserializer rejects by identity); `Google.Protobuf@3.35.1` (`:47`, already admitted) is the sole runtime dep.

### `[V6]`/`[V11]`/observability/Ara3D

- COMMIT (`[V6]`): `EFCore.NamingConventions`, `Microsoft.EntityFrameworkCore.Design` (earns admission at the identity DDL/migration owner), `Thinktecture.Runtime.Extensions.EntityFrameworkCore10`, `Pgvector.EntityFrameworkCore` (the `<->`/`<=>` LINQ server-side ANN row on `Query/retrieval`).
- `MPXJ.Net@16.4.1` (`:296`, `[V11]`): KEEP — `Ingest/schedule.md` mints (BIM:102 consumer clears the deciding criterion).
- `linq2db.EntityFrameworkCore` (CONDITIONAL): composes a real BulkCopy fence in `Ingest/tabular` → KEEP, else LEAVES (DuckDB `COPY` + Npgsql binary import own the bulk lane).
- `Npgsql.OpenTelemetry` (ruled KEEP): the provisioning observability row (tracing+metrics builder subscriptions at the AppHost composition root); the brief's rejection criterion was not met — KEEP stands.
- **`Ara3D.BimOpenSchema`(+`.IO`): KEEP on the live columnar consumer + BUMP 1.0.1 → 1.6.1** — nuget feed CONFIRMED 1.6.1 published 2026-07-03 (the brief's "1.0.1 latest" parenthetical is feed-REFUTED; feed-truth wins). Re-decompile at 1.6.1 on the bump: retire the DEBUG-IL caveat if `AssemblyConfiguration("Debug")` is gone, else escalate to in-corpus absorption (`[11]`). The `.IO` transitive `Parquet.Net`/`ClosedXML` writers are file-format interop only, never a second managed Parquet lane.
- **EF/vector/Arrow substrate bases (KEEP — DISPOSITION-COMPLETE fill; no consumer-absence prune)**: `Npgsql.EntityFrameworkCore.PostgreSQL` (`:299` — the EF provider base under Thinktecture.EF + EFCore.NamingConventions + the NodaTime provider), `Npgsql.EntityFrameworkCore.PostgreSQL.NodaTime` (`:301` — the `Instant`/`Duration` column mapper the V6 identity DbContext relies on), `Pgvector` (`:305` — the vector-type base under `Pgvector.EntityFrameworkCore`), `Apache.Arrow.Compression` (`:256` — the Arrow codec satellite under ADBC/Flight): all KEEP by composition, each a base the disposed `.EntityFrameworkCore`/driver row already rides.

### `.api` catalog obligations (leg-1 motion)

Re-anchor all 77 package catalogs to LIVE pages (dead-page map: `Store/{profiles,remote,encryption,quality,server,tenancy}`, `Query/{federation,pipeline,transaction,rail,lanes}`, `Version/snapshots`, `Sync/*`, `Schema/*` → live). The 21 anchor-less catalogs (disk-corrected from the register's 16) gain anchors; core spine `api-marten`→graph/provisioning, `api-npgsql`→provisioning, `api-objectstore`→blobstore first-class. `api-npgsql.md` GAP: add advisory-lock (`pg_advisory_xact_lock`) + LISTEN/NOTIFY rows (V2 lease + V3 pump wake); `Npgsql.Replication` pgoutput stays recorded-unconsumed (the changefeed is Marten's daemon; logical replication the named escalation if a raw-WAL CDC consumer ever lands). `api-flowtide-substrait.md:151` FLIP + re-anchor to `Query/federation`; `SubstraitSerializer` internal (retain wire bytes, never re-serialize). DELETE the misfiled shared `libs/csharp/.api/api-messagepack.md` (byte-identical dup of the Persistence copy, `shasum 9a7ba81f`, tagged `RASM_PERSISTENCE`; the `Rasm.Materials` copy `cb59c0f1` correctly DIVERGES and stays) or re-scope to a neutral `RASM_API` substrate catalog. UNION the 9 double-cataloged overlays (`hashing`, `highperformance`, `hybrid-cache`, `jsonpatch`, `messagepack`, `nodatime-stj`, `nodatime`, `redaction`, `thinktecture-json`) into shared owners FIRST, then collapse to one-line pointers (8 DIVERGE — never blind-delete; `messagepack` is the byte-identical dup above). `api-h3.md:3` correct the NTS-IO claim → transitive core-`NetTopologySuite` 2.6.0. Uniform provenance tag + `[STACKING]` section across the kept tier. The DuckDB substrait extension row overlays `api-duckdb`. The absent substrate `api-languageext.md` is DISCHARGED (verify-only per the track-shared authoring law; the first campaign to run authored the ONE substrate file). Admissions carry `.api` stubs in the same motion.

### Recorded rejections carried forward (never re-proposed blind; re-admission needs a named consumer)

- `LoroCs` — the cross-runtime bit-parity wire law (`CrdtOpWire` `[Key]` sequence reproduced in Python/TS) forecloses a Rust-binding CRDT whose wire is its own; stays App-UI's live-collaboration concern.
- `WolverineFx` — its envelope-table durable outbox is a parallel store beside the stream-IS-outbox law; its handler runtime is AppHost's process model.
- `DistributedLock.Postgres` — no fencing tokens; `[V2]`'s token-VALIDATING CAS is strictly stronger than any lease library.
- `MemoryPack` — C#-only wire against the frozen msgpack bit-parity law.
- `Apache.Arrow.Flight.Sql` — CONDITIONAL-FORWARD: admitted only if the `[V1]` owner ever charters a standards Flight-SQL endpoint for external BI consumers; the ADBC+Flight pair otherwise owns the concern.

## [06]-[LEG_PARTITION] — proven acyclic; per-leg rebuild-engine invocation rows

Within a leg the listed order IS dependency order, never prose order. Per-leg closeout: hard residuals resolve before the next leg launches; `README.md` router/groups, `ARCHITECTURE.md` codemap/seams, and TASKLOG/IDEAS cards close in the leg that changes their facts; `.api` stubs, catalog re-anchors, and manifest edits land with the leg that admits/removes/first-composes the package; residuals naming a later leg's pages transfer forward explicitly in the leg return.

| Leg | Rebuild-engine invocation | Targets (dependency order) | Charter |
|---|---|---|---|
| 1 SPINE + PERIMETER | `Workflow(rebuild.js, {targets: [libs/csharp/Rasm.Persistence/.planning/Element/graph.md, libs/csharp/Rasm.Persistence/.planning/Element/codec.md, libs/csharp/Rasm.Persistence/.planning/Element/identity.md, libs/csharp/Rasm.Persistence/.planning/Element/authority.md], brief: "RASM-CS-PERSISTENCE-DECISION.md"})` | graph, codec, identity, authority | `[V4]` band registry COMPLETE (all 19 own + new-owner + re-banded decades — a duplicate integer unrepresentable from leg 1; each union re-derives in its owning leg); `Element/` (authority extraction V5a SPLIT, EF commit + migration owner V6, codec consumer-contracts V9 + kernel-hash re-anchor [B] + HashPolicy V10, the `graph` `Principal` injection re-home [A.1]); **the leg-1 DEFINITION of the Persistence-owned frame-input shapes (`StoreActor` VO + `ClockPolicy`/`CorrelationId`/`TenantContext`/`RecoveryObjective` ingredients on the injected `ProjectionContext`/`ResolvedProfile` forms) and the re-thread of the leg-1 pages (`graph`/`codec`/`identity`/`authority`) onto them, each later leg re-threading its own ~14 frame-referencing pages as it rebuilds ([A.1] acyclicity prerequisite);** the FULL roster reconciliation + catalog re-anchor/dedup/stub motion (V7, `[05]` — gates every later import); the own-ledger seam corrections (V12 half); the csproj:10 AppHost-reference removal |
| 2 VERSION ENGINE | `Workflow(rebuild.js, {targets: [libs/csharp/Rasm.Persistence/.planning/Version/ledger.md, libs/csharp/Rasm.Persistence/.planning/Version/commits.md, libs/csharp/Rasm.Persistence/.planning/Version/timetravel.md, libs/csharp/Rasm.Persistence/.planning/Version/merge.md, libs/csharp/Rasm.Persistence/.planning/Version/provenance.md, libs/csharp/Rasm.Persistence/.planning/Version/retention.md, libs/csharp/Rasm.Persistence/.planning/Version/recovery.md], brief: "RASM-CS-PERSISTENCE-DECISION.md"})` | ledger, commits, timetravel, merge, provenance, retention, recovery | `[V10]` repairs; the `AsOfKey` arm (S3); the `CRDT_OP_SET` parity pin (commits, S4) + [B] version-engine hasher re-anchor (H2/H3, extended from leg-1's codec-only enumeration); H1 `CommitFault` MINT; the V8 Type-correlation path + named migration case; the V9 Scrub/Bisect incremental re-shape (interim documented) |
| 3 QUERY + INGEST | `Workflow(rebuild.js, {targets: [libs/csharp/Rasm.Persistence/.planning/Query/lane.md, libs/csharp/Rasm.Persistence/.planning/Query/retrieval.md, libs/csharp/Rasm.Persistence/.planning/Query/topology.md, libs/csharp/Rasm.Persistence/.planning/Query/columnar.md, libs/csharp/Rasm.Persistence/.planning/Query/cypher.md, libs/csharp/Rasm.Persistence/.planning/Query/cache.md, libs/csharp/Rasm.Persistence/.planning/Query/federation.md, libs/csharp/Rasm.Persistence/.planning/Ingest/tabular.md, libs/csharp/Rasm.Persistence/.planning/Ingest/schedule.md], brief: "RASM-CS-PERSISTENCE-DECISION.md"})` | lane→retrieval (V5b SPLIT), topology, columnar, cypher, cache, then federation (lowers onto retrieval/columnar/lane), then tabular, schedule | retrieval extraction + RetrievalFault (before the federation owner that lowers onto it); columnar trust gates + posture + DuckDB substrait extension row; the V1 federation owner; cache/cypher (CypherFault rename)/topology (Lca gate) cold passes; Ingest growth (tabular Sep explicit + schedule V11) |
| 4 STORE + COORDINATION + EGRESS | `Workflow(rebuild.js, {targets: [libs/csharp/Rasm.Persistence/.planning/Store/blobstore.md, libs/csharp/Rasm.Persistence/.planning/Store/provisioning.md, libs/csharp/Rasm.Persistence/.planning/Store/coordination.md, libs/csharp/Rasm.Persistence/.planning/Version/egress.md], brief: "RASM-CS-PERSISTENCE-DECISION.md"})` | blobstore, provisioning, then coordination (before the egress pump), then egress | blobstore V10 repairs + E9 checksum honesty; provisioning loose-code typing (ServerFault re-band + 7701/7702); `Store/coordination.md` (V2); the egress owner (V3) over the leg-2 changefeed + the leg-4 outbox spine |

Between every pair of legs: `Workflow(resolve-residuals.js, {hard_residual: <prior-leg return, when non-empty>})`.

### Acyclicity proof

**Zero upward strata edges.** Persistence is up-only on `Rasm` + `Rasm.Element`; the csproj:10 AppHost ProjectReference DELETES ([A.1]) and the frame ingredients (`Principal`/`ClockPolicy`/`CorrelationId`/`TenantContext`/`RecoveryObjective`) — referenced by simple name across ~18 pages in all four legs, not `graph.md` alone — re-thread onto Persistence-DEFINED port-input shapes (defined leg 1, each page re-threaded in its own leg per [A.1]); no page imports the AppHost namespace once its leg lands, and a not-yet-re-threaded page is a carried leg residual, never a silent upward edge. The four AppHost PORT rows and the `MembershipView.Serving` (`Wire/coordination.md:79`) consumer are AppHost→Persistence READs/decodes (correct HOST-BOUNDARY→APP-PLATFORM direction), no down edge.

**Frozen-vocab inversions (cross-leg, resolved by freeze-at-decision-time).** `graph(1)←timetravel(2)` `TimeCut` · `retention(2)←blobstore(4)` `StorageTier` · `recovery(2)←blobstore(4)` `ObjectStore.Head` · `cypher(3)←provisioning(4)` `ServerExtension.CreateSql` — each an earlier-leg page reading the DECISION-frozen shape ([A.3]); the shape at decision time IS the contract, so no later-leg page-authoring is a prerequisite. A leg-4 (or leg-2) change to a frozen vocabulary reopens the consuming leg as a hard residual (the V5 symmetric binding).

**Intra-leg edges (forward-only, ordered).** `egress(4)→coordination(4)#OUTBOX_CURSOR` — coordination lands first in leg 4; the pump reads the cursor; coordination never reads the pump. `federation(3)→lane(3)#SetExpr` + `→columnar(3)` + `→retrieval(3)` — retrieval/columnar/lane land before federation in leg 3.

No cycle exists on the strata graph, the cross-leg frozen-contract graph, or the intra-leg order graph. ∎

### Campaign acceptance (post-leg-4 composed dry-runs; a dry-run reaching a missing owner reopens V1/V2/V3 as residuals)

(a) VERSION ENGINE — commit → three-way merge across a simulated Type re-key reading as RENAME → AS-OF scrub incremental-shaped → attested verify → retention sweep → verified restore whose terminal proof is `reconstructed OfGraph == checkpoint AsOfKey` (`RecoveryFault.VerifyFailed` unreachable) beside a real RPO lag. (b) COORDINATION — a fenced Budget debit whose stale-token replay is REJECTED (`LeaseFenced`), a step-state CAS, an `InFlight` read serving `CrashResume`, an outbox drain to a CloudEvents sink dedup'd by `id`=`ContentKey`. (c) FEDERATION — a Substrait plan lowered to `SetExpr` executing on the columnar lane. (d) PERIMETER AUDIT — every fault code → exactly one registry row, every digest mint → `ContentHash.Of`/seam `ContentAddress`, every catalog + card anchor → disk, `dotnet restore` clean.

## [07]-[VERDICT_DISPOSITION] — V1-V13 (the four hinges RULED from evidence)

| V | Ruling |
|---|---|
| V1 FEDERATION *(hinge)* | **REINTRODUCE `Query/federation.md` as ruled, with gap-closes.** The ruled-default WIRE-FORM premise is OVERTURNED in the owner's favor by decompile evidence: `FlowtideDotNet.Substrait@0.15.0` ships `Substrait.Protobuf.Plan : IMessage<Plan>` with `public static MessageParser<Plan> Parser` AND `public class SubstraitDeserializer` with `Deserialize(Substrait.Protobuf.Plan)`/`Deserialize(string json)`/`DeserializeFromJson(string)` — all public in-assembly, falsifying `api-flowtide-substrait.md:151`. Transcription collapses to ~2 lines (`Plan.Parser.ParseFrom(bytes)` then `SubstraitDeserializer.Deserialize`); `Grpc.Tools` codegen is DROPPED (regenerating substrait `.proto` mints a duplicate CLR `Plan` the shipped deserializer rejects by identity); `Google.Protobuf@3.35.1` (admitted) is the sole runtime dep. The only substantive new code is the LOWERING visitor (~150-250 LOC; reference `SubstraitToDifferentialComputeVisitor` = 139 LOC/9 kinds), so the transcription-heavier-than-lowering retire trigger runs OPPOSITE. ONE-ENGINE TEST passes: a total lowering onto `SetExpr` alone is correctly impossible (SetExpr is a set-of-NodeId-keys algebra with no columns/measures/order), but the TWO-target partition (SetExpr for key-selection; the EXISTING in-process DuckDB + external-warehouse ADBC lanes for the tabular subset) mints NO new engine — the owner is a router/lowerer over standing lanes. SetExpr lowering exact-fit: SetRelation→Union/Intersect/Difference (3-for-3), ReadRelation+Filter(SetPredicate)→Predicate, VirtualTableRead→Literal, bounded Iteration→Closure, key-semijoin Join→Intersect; no-SetExpr-target ops (Project/Aggregate/Sort/TopN/Window/general-Join) route to columnar, none un-executable; Exchange→DROP, Write→REJECT. Tabular EXECUTION support scoped: DuckDB's `substrait` community extension (`from_substrait(blob)`, MIT, DuckDB-version-coupled — verify a 1.5.x build, fail-closed on unsupported rels) closes the in-process lane as ONE `ColumnarExtension` row; `AdbcStatement.SubstraitPlan` is driver-dependent and NOT guaranteed on the admitted BigQuery Beta driver, so V1's committed tabular federation SCOPES to Substrait-capable sources via a `SourceKind` capability axis, staging SQL-only warehouses as a growth row through `AdbcStatement.SqlQuery`. Roster: KEEP `FlowtideDotNet.Substrait` + `Arrow.Adbc(.Drivers.{Apache,BigQuery})` + `Arrow.Flight`; ADD the DuckDB substrait extension row; DROP `Grpc.Tools`. **Receipt honesty**: the pinned `TimeCut` is the LOCAL coordinate, so the content-addressed `(plan-digest·cut·watermark)` `ArtifactKind` replay row is deterministic only for the local `ElementSet` subtree — external-source `SourceKind` rows carry read-time currency, never a false consistent-as-of-cut claim. `FederationFault` = `SubstraitParse`/`UnsupportedRelation`/`SourceUnreachable`/`WriteRejected`/`SourceUncapable` ([03]). Three BLOCKED cards re-anchor honest (SetExpr subset zero-gap fences; tabular subset scoped; BLOCKED on the `python:data` producer). Retire NOT triggered. |
| V2 COORDINATION | NEW `Store/coordination.md` — token-VALIDATING fenced lease; per-unit-vector fenced compare-and-decrement Budget; step-state CAS + READ cases (InFlight/ExpiredScan/Members/StepLoad); membership lease-expiry (`MembershipView.Serving` consumer); NAMED outbox spine + cursor. csproj strata reversal resolved ([A.1]). Store→3. 4/4 AppHost PORT rows homed. |
| V3 EGRESS | NEW `Version/egress.md` (beside the changefeed — no `Sync/` revival). ONE pump over the outbox cursor; exactly-once-EFFECT envelope (`id`=`ContentKey`, `Sequence`=cursor, `partitionkey`=`EntityKey`); NATS dedup + Settle-ack advance, pg_net response-reconciled advance (never fire-and-forget); sink rows as seed DATA. Egress→coordination edge acyclic. Band 827x. |
| V4 BAND REGISTRY | `[SmartEnum<int>]` on `graph.md#[FAULT_TABLES]`; 19 own bands + pinned mirrors ([03]). **Commit 826x MINTED** (H1 — disk-confirmed bare `Error.New(8261/8263/8264)` with ZERO union; OVERTURNS "register as-is"). Cypher renames `CypherFault`. Loose codes (incl. 7701/7702) → typed cases. Three collisions re-partitioned (Server→838x, Tabular→839x, Retrieval 841x). |
| V5 SPLITS+CYCLES *(hinge)* | **(a)** `identity`→+`authority` (authz set-algebra SPLIT). **(b)** `lane`→+`retrieval` (fusion+codebook + RetrievalFault SPLIT). **(c)/(d) FROZEN across all four inversions**: `TimeCut` (timetravel-owned), `StorageTier` (blobstore-owned), `ObjectStore.Head` (blobstore-owned), `ServerExtension.CreateSql` (provisioning-owned) freeze at decision time as cross-leg contracts consumed by an earlier leg. The `Store/tier` neutral-vocabulary extraction is NOT taken — the deciding criterion (a leg-4 owner must amend a frozen vocabulary mid-campaign) is unmet, and preference alone never overturns the documented pivot. A leg change to a frozen vocabulary reopens the consuming leg as a hard residual. |
| V6 EF+EMBEDDED *(hinge)* | **EF-commit**: COMMIT `UseValueObjectValueConverter()`+`UseSnakeCaseNamingConvention()`; the hand `NodeId` converter (`identity.md:120`) + the 10 hand `HasColumnName` calls die; the LanguageExt `Option<Vector>`/`Seq<NodeId>` converters (`:87-105`) stay (Thinktecture.EF does not own those forms). The hand-mapping die-off is the mine's proof. **Migration owner**: on `Element/identity`'s schema section (`element_identity`/`node_cell` DDL; `ServerExtension.CreateSql` commits through this rail; `EF.Design` earns its admission). **Embedded-boundary HONEST**: Marten is PG-only (`identity.md:3` same-`IDocumentSession` Marten commit), so `StoreProfile.Embedded` (SQLite) carries NO ElementGraph event-sourcing path — the real charter is relational identity floor + `EngineOps` checkpoint/snapshot/backup + read-only/offline tier, never SoR. **pgvector-lane MINE**: `Pgvector.EntityFrameworkCore` maps the column (`identity.md:62,86-92`) while lane's in-process PQ/ADC keeps the hot set; the `<->`/`<=>` LINQ server-side ANN row lands on `Query/retrieval` (residency is row data on the one retrieval axis). The column-mapping-only demotion is NOT taken — corpus-scale ANN belongs to the pgvector/pgvectorscale indexes while the PQ/ADC row keeps the hot set, so the server-side row is not redundant beside the PQ lane. |
| V7 SCALE-OUT+ORPHAN | INTEGRATION-FIRST: 5 scale-out KEEP as `[V13]` axis rows with per-package ceiling PROOFS ([05]); ScyllaDB PRUNE (no wide-column axis owner; forward re-admission recorded). PollinationSDK/4 NTS bridges/EF-Sqlite/direct-Redis PRUNE in one leg-1 motion. SoR-spine seal stands. |
| V8 TYPE-REKEY | (a) consumer contract RECORDED (exclude `Classifications` from the Type seed — Element/Materials owns; Persistence the demanding consumer; `element.md:236` carries `Classifications` in the Object shape, so the Type seed is classification-dependent today). (b) `merge.Reconcile` gains the `ObjectKind.Type` correlation-key row NOW (classification-independent natural key → RENAME); executing wave stays `identity#IDENTITY_POLICY` expand/flip/contract. |
| V9 INCREMENTAL OFGRAPH | consumer contract on `codec#CONTENT_ADDRESS` (`OfGraph(prior,delta)` is `Rasm.Element/Projection/address`'s; Scrub/Bisect re-shape; whole-graph recompute interim). Geometry `[V2]` parametric digest waterfalled SHARPENED (a COMPONENT of `ToCanonicalBytes`, not a sibling key — else `OfGraph` blind to a parametric-body edit, a false green on the AsOfKey/OfGraph proofs). Kernel hasher never re-homed. Zero current Persistence-side edit beyond the recorded contract. |
| V10 DEFECT SET | all repairs land with the owning page's rebuild ([02]): recovery real RPO lag + WAL policy row; near-linear MergeBase; ledger Truncate/Codec/batch fold; merge STJ options + MemberPath verified-clean (leg-2 re-verify `[11]`); blobstore receipt-path + E9 checksum honesty + Correlation; retention StorageLane.Durable; provenance Principal-agent-class; columnar trust gates + posture constant + phantom-spellings; codec HashPolicy shrink; timetravel AggregateStreamToLastKnown. PLUS H1/H2/H3 + provisioning 7701/7702 + topology Lca DAG-gate + tabular Wire. |
| V11 INGEST GROWTH *(hinge)* | **MINT `Ingest/schedule.md`.** Deciding criterion CLEARED: BIM:102 (`schedule ⇄ Persistence/Sync/schedule` P6/MS-Project + 4D) is the durable-schedule consumer on disk — TASKLOG:47 moved the CPM/4D COMPUTATION to `Rasm.Bim` but the durable P6/MS-Project STORE + file codec stays Persistence (ingest-shaped). `MPXJ.Net` KEEPS; the fold-up alternative is NOT triggered; `Ingest/tabular` KEEPS (Sep explicit owner law; linq2db conditional). Folder law satisfied at 2 pages. |
| V12 GOVERNANCE | every phantom-realization clause corrects; the stale `-[COMPLETE]` cards re-point ([09]); `[FABRICATION_PROGRAM_DURABLE_ROWS]` re-anchors off dead `Schema/ddl#IDENTITY` onto blobstore rows + cache artifact index (2701-2710 decode constraint, KIND-AGNOSTIC `ArtifactKind` rows under one `ContentHash.Of` fold); ledger both directions complete ([04], incl. both AppUi crossings); AsOfKey/reconciliation/Substrait own rows corrected; prepared-tx RETIRE. |
| V13 STORE AXIS | store perimeter PARAMETERIZED — the full AXIS MAP first-class ([05]) with per-package ceiling proofs. SoR spine SINGULAR (one event store/materializer/identity/changefeed — unchallengeable seal); `ARCHITECTURE.md:115` re-scopes to that spine boundary (a perimeter-axis engine row carrying unreachable capability is a legal DECISION-level admission, never "a new relational engine row" by definition). |

## [08]-[EVIDENCE_DISPOSITION] — E1-E14 (re-verified on disk; drifts carried)

| E | Verdict | Disposition (disk correction carried) |
|---|---|---|
| E1 phantom realization | V1/V12 | HOLD; +un-enumerated IDEAS:57-61, TASKLOG:46-47; `:44`↔`:45` `SchemaDdl.Sql` contradiction resolves (→ V6 migration owner) |
| E2 orphaned roster | V7/V13 | DRIFT — **Ara3D "1.0.1 latest" REFUTED (1.6.1 feed-confirmed, nuget MCP, published 2026-07-03)**; scale-out present at pinned versions (ClickHouse 1.2.0, DeltaLake 0.32.0, Flowtide 0.15.0, LightningDB 0.21.0, Qdrant 1.18.1, rocksdb 10.10.1.1747, ScyllaDB 3.22.0.3, PollinationSDK 1.10.0) |
| E3 catalog drift | V7/ROSTER | DRIFT — anchor-less is 21 not 16; messagepack byte-identical dup confirmed |
| E4 band collisions | V4 | DRIFT — Columnar 8350-8356 (whole decade); 837x THREE-way; **+commits H1 (826x bare `Error.New`, disk `:358/400/411`)**; +provisioning 7701/7702 (fourth cluster, disk `:433/440`) |
| E5 coordination gap | V2 | HOLD — bilateral; AppHost fully-specified (`orchestration.md:257-284` StepStateSeam+STEP_STATE_RIPPLE:340, `capability.md:54-62` CostVector, `Wire/coordination.md:79` MembershipView.Serving) |
| E6 seam-ledger drift | V12 | HOLD — ARCH:55 target DRIFT → `Version/merge`; all 12 sibling rows HOLD (disk-confirmed at every anchor); +GeometryHash digest-semantics gap to geometry `[V8]` |
| E7 EF stack | V6 | HOLD — identity `:82`/`:120` mapping conflict; `MigrationBuilder`=0 (no DDL owner); embedded no ElementGraph path (Marten PG-only) |
| E8 verified logic bugs | V10 | DRIFT anchors — recovery `:180`/`:171`; MergeBase `:108-113` HOLD; MemberPath carried VERIFIED-CLEAN (leg-2 re-verify obligation `[11]`) |
| E9 dead carriers | V10 | HOLD — +commits `VectorOrder`/`Order` + `ContentParityCorpus.Seed`; +AgtypePath.Weight, TypedEdge.Kind, TabularWire.Wire; GCS/Minio dishonest checksum (ObjectChecksum axis `:32-37`, SDK-native honesty `:42-43`) |
| E10 type-rekey | V8 | HOLD — sharper at `element.md:236` (`Classifications` in the Object shape → Type seed classification-dependent today; V8a↔V8b coupling) |
| E11 OfGraph hot paths | V9 | HOLD — Of(Node,Tol)-vs-span overload REFUTED as a defect (both real, distinct id-inclusive/exclusive semantics); the genuine issue is H3 (timetravel EDGE keys raw `:203-204`) |
| E12 mandate clean | V12 | HOLD — count-prefix ZERO-structural; re-band gate; Fabrication 2701-2710 forward constraint on IDEAS:26-32 |
| E13 folder+page overload | V5/V11 | DRIFT — Ingest one-file CONFIRMED; lane retrieval span `:267-520`; Sep prose `:5,18,20,297` |
| E14 parameterization | V10 | DRIFT — columnar surface WIDER (`{projection}` raw SQL); posture literals `:80-83` confirmed |

Beyond-register findings disposed: H1 (commits MINT, [03]), H2 (version-engine hasher enumeration, [B]), H3 (timetravel edge keys, [B]), provisioning 7701/7702 (`EmbeddedFault.Refused`), GCS/Minio checksum honesty, codec `:175` `ByDomainId` prose (dies), topology Lca DAG-gate, tabular `Wire` dead carrier, columnar 2 phantom spellings, `api-flowtide-substrait.md:151` FALSIFIED, csproj:10 reversal + the `graph` `Principal` injection re-home ([A.1]).

## [09]-[CARD_DISPOSITION] — all 30 (11 IDEAS + 19 TASKLOG; disk-verified counts)

### IDEAS (11 — 4 open + 7 closed)

| Card | Status | Disposition |
|---|---|---|
| :19 `[PERSISTENCE_LIBRARY_TABLES]` | QUEUED | KEEP QUEUED honest; re-anchor onto real durable owners (blobstore content rows + cache `ArtifactKind` index + identity/coordination tier); lands when Materials catalogue owners pin content keys |
| :26 `[FABRICATION_PROGRAM_DURABLE_ROWS]` | QUEUED | KEEP QUEUED; re-anchor off dead `Schema/ddl#IDENTITY` onto blobstore content rows + cache artifact index + `[V2]`/Store growth axis; KIND-AGNOSTIC `ArtifactKind` rows (programs/.cli/3MF/NC1/stock-state/travelers under one `ContentHash.Of` fold); forward constraint: durable receipts decode 2701-2710, never 25xx |
| :34 `[REUSE_WIRE]` | BLOCKED | KEEP BLOCKED (python:data-gated); correct the `:38` phantom → the SetExpr/ElementSet substrate realized as `Query/federation` fences; re-anchor V1 |
| :41 `[SUBSTRAIT_FEDERATION_SEAM]` | BLOCKED | KEEP BLOCKED; correct the `:45` phantom → `Query/federation` lowers Substrait onto SetExpr (V1, real per the hinge ruling); re-anchor V1 |
| :55 `[PERSISTENCE_BIM_SYNC_CRDT]` | COMPLETE | LEGIT KEEP CLOSED (`commits#CRDT_ALGEBRA` real); align BIM:94 `CommitGraph.MergeBase` spelling → `commits#MergeBase` |
| :56 `[DURABILITY_RECOVERY_OBSERVATORY]` | COMPLETE | LEGIT KEEP CLOSED; the V10 RPO repairs + V12 AsOfKey verify strengthen the "re-establishes CONTENT IDENTITY" claim into an assertable proof |
| :57 `[TRANSACTION_CONCURRENCY_CONTROL]` | COMPLETE (STALE) | RE-POINT off deleted `Query/transaction.md` → `Element/graph#STORE_RAIL` (fold); prepared-tx RETIRE |
| :58 `[ENVELOPE_ENCRYPTION_KMS]` | COMPLETE (STALE) | RE-POINT off deleted `Store/encryption.md` → `Element/identity` (KMS custody, post-V5a) + `Store/blobstore#BLOB_GC` (object-SSE) |
| :59 `[DATA_QUALITY_INTEGRITY_FRAMEWORK]` | COMPLETE (STALE) | RE-POINT off deleted `Store/quality.md` → `Query/columnar` + `Store/provisioning` verification surfaces |
| :60 `[BULK_ETL_INTERCHANGE_PIPELINE]` | COMPLETE (STALE) | RE-POINT off deleted `Query/pipeline.md` → `Query/columnar` (bulk ingest/egress over ArrowChunk) |
| :61 `[CDC_STREAMING_EGRESS]` | COMPLETE (STALE) | RE-POINT off deleted `Sync/egress.md` → `Version/egress.md` (V3 NEW owner; re-owned + re-realized leg 4) |

### TASKLOG (19 — 1 open + 18 closed)

| Card | Status | Disposition |
|---|---|---|
| :20 `[ARTIFACT_CONTENT_KEY_FEDERATION]` | BLOCKED | KEEP BLOCKED (python:artifacts-gated); correct the `:24` phantom → `Version/provenance#ATTESTED_LEDGER` signed-artifact binding (ARCH:51) + the V1 `Query/federation` `SourceKind.SignedArtifact` source row |
| :34 `[PERSISTENCE_BIM_ARTIFACT_INDEX]` | COMPLETE | LEGIT KEEP CLOSED (`cache#ARTIFACT_BLOB_INDEX` real) |
| :35 `[RECOVERY_PAGE_AUTHOR]` | COMPLETE | LEGIT KEEP CLOSED (recovery real; V10 RPO repairs land with the improve) |
| :36 `[RECOVERY_OBJECTIVE_COLLAPSE_RECOVERYFACT]` | COMPLETE | LEGIT KEEP CLOSED (RecoveryFault 829x real) |
| :37 `[TRANSACTION_PAGE_AUTHOR]` | COMPLETE (STALE) | RE-POINT off deleted `Query/transaction.md`/`.cs` → the fold into `graph#STORE_RAIL` (TxnScope/IsolationPolicy/2PC through `GraphStoreOp`); prepared-tx RETIRE |
| :38 `[CONFLICTOUTCOME_DISCRIMINANT_COLLAPSE]` | COMPLETE | LEGIT KEEP CLOSED (`ledger#MERGE_LAW ConflictResult` real `:225`) |
| :39 `[ENCRYPTION_PAGE_AUTHOR]` | COMPLETE | LEGIT KEEP CLOSED (records the settled fold — KMS→`identity`, SSE→`blobstore#BLOB_GC`) |
| :40 `[SQLCIPHER_RESEARCH_PROMOTE]` | DROPPED | LEGIT DROPPED-hold (embedded floor rejects the `e_sqlcipher` cipher bundle) |
| :41 `[QUALITY_PAGE_AUTHOR]` | COMPLETE (STALE) | RE-POINT off deleted `Store/quality.md`/`.cs` → `Query/columnar` + `Store/provisioning` verification surfaces |
| :42 `[PIPELINE_PAGE_AUTHOR]` | COMPLETE (STALE) | RE-POINT off deleted `Query/pipeline.md`/`.cs` → `Query/columnar` |
| :43 `[EGRESS_PAGE_AUTHOR]` | COMPLETE (STALE) | RE-POINT off deleted `Sync/egress.md`/`.cs` → `Version/egress.md` (V3; the COMPLETE was FALSE — owner deleted — flip to the V3 re-authoring pointer, re-realized leg 4) |
| :44 `[SCHEMADDL_SQL_COLLAPSE]` | COMPLETE (STALE + self-contradicting) | RE-POINT off `SchemaDdl.Sql` (absent, contradicts :45) → the V6 identity migration owner; extension DDL is `ServerExtension.CreateSql`; NO `SchemaDdl.Sql` owner |
| :45 `[STORE_SERVER_SPLIT]` | COMPLETE | LEGIT KEEP CLOSED (the truth-source contradicting :44; provisioning + tenancy-into-identity) |
| :46 `[ANNOTATION_RELOCATE_TO_BIM]` | COMPLETE (STALE) | RE-POINT off deleted `Sync/annotation.md` + phantom `Query/federation#ENTITY_GRAPH` join → `Version/ledger#CHANGEFEED` (BIM:101); join on real GlobalId |
| :47 `[SCHEDULE_RELOCATE_TO_BIM]` | COMPLETE (STALE) | RE-POINT off deleted `Sync/schedule.md` → `Ingest/schedule.md` (V11 NEW; MPXJ codec + durable rows); 4D/CPM computation to Rasm.Bim/schedule (BIM:102) |
| :48 `[ICECHUNK_ASOF_CONTENT_KEY]` | COMPLETE (STALE + phantom) | RE-POINT off deleted `Version/snapshots#SNAPSHOT_PROTOCOL AsOfKey` → `Version/timetravel` (`AsOfKey`=`Checkpoint.Address`, V12; re-realized leg 2) |
| :49 `[KMS_PACKAGE_ADMISSION]` | COMPLETE | LEGIT KEEP CLOSED (KMS trio pinned) |
| :50 `[ARROW_FLIGHT_PACKAGE_ADMISSION]` | COMPLETE | LEGIT KEEP CLOSED; correct the ripple ("rides BULK_ETL_INTERCHANGE_PIPELINE", deleted) → rides V3 `Version/egress` + V1 `Query/federation` |
| :51 `[KAFKA_CLOUDEVENTS_PACKAGE_ADMISSION]` | COMPLETE | LEGIT KEEP CLOSED; the Kafka/CloudEvents ride the V3 sink rows |

Totals: 30 · 5 open (3 BLOCKED + 2 QUEUED, corrected + held) · 25 closed (11 LEGIT KEEP + 1 LEGIT DROP + 13 stale re-pointed: IDEAS:57-61, TASKLOG:37,41,42,43,44,46,47,48).

## [10]-[CAPABILITY_ESCALATION] — the brief `[03]` plane targets (Phase-2 acceptance bar)

| Plane | Now→Target | Carried by |
|---|---|---|
| Element (graph/codec/identity+authority) | 9→9.5 | authority extraction; EF commit + snake-case + generated converters; DDL/migration owner; band registry sited; V9/parametric/streaming-identity consumer contracts; [B] hash re-anchor; HashPolicy ruling; `Principal` injection re-home |
| Version core (ledger/commits/timetravel) | 9→9.5 | near-linear MergeBase; dead vocab wired-or-dead; batched fold; AsOfKey=Checkpoint.Address; CRDT_OP_SET pin; Scrub incremental; [B] version-engine re-anchor; H1 CommitFault MINT |
| Version merge | 8.5→9.5 | Type-correlation key; MemberPath verified; STJ options; re-key migration case |
| Version governance (provenance/retention/recovery) | 9→9.5 | real RPO lag; WAL policy row; Principal-agent-class; StorageLane.Durable |
| Query read lanes (lane/topology/columnar/cypher/cache) | 8.5→9.5 | retrieval extracted; CypherFault rename; columnar trust gates + posture + substrait row + phantom-spellings; pgvector; topology Lca gate |
| Query retrieval (split) | —→9.5 | fusion + PQ codebook + RetrievalFault; Compute codebook seam declared |
| Query federation (new) | 0→9 | Substrait→SetExpr lowering; public-Deserialize ingress; SourceKind; ADBC lane; cut-pinned receipt; python:data wire; honest cards |
| Ingest (tabular+schedule) | 6.5→9 | folder law; Sep explicit; MPXJ codec + durable rows |
| Store (blobstore/provisioning) | 9→9.5 | receipt path; E9 checksum honesty; loose codes typed (incl. 7701/7702); README bridge row |
| Store coordination (new) | 0→9.5 | token-VALIDATING lease; fenced per-unit-vector Budget CAS; CAS/membership op-union (MembershipView consumer); named outbox + cursor; four PORT rows homed |
| Changefeed egress (new) | 0→9 | one pump; exactly-once envelope; sink dedup honesty; dead-letter; replay |
| Governance perimeter | 3→9.5 | zero phantoms; ledger both directions (both AppUi crossings); 77 catalogs live; zero orphans; type-enforced registry |

## [11]-[OPEN_ITEMS] — build-leg verification obligations (all RULED, none blocking the DECISION)

1. `CompleteMultipartUploadRequest.ChecksumXXHASH128` — confirm via `uv run python -m tools.assay api` (AWSSDK.S3 restored assembly) before the blobstore leg (4) closes; the two enum members `ChecksumAlgorithm.XXHASH128`/`ChecksumType.FULL_OBJECT` already verified.
2. DuckDB `substrait` community-extension — verify a build exists for the pinned `DuckDB.NET.Data.Full` 1.5.x runtime; fail-closed (`FederationFault.UnsupportedRelation`) on an unsupported relation. Leg 3.
3. Ara3D DEBUG-IL re-verify at the feed-confirmed 1.6.1 — re-decompile for `AssemblyConfiguration("Debug")` on the leg-1 bump; retire the DEBUG-IL caveat if gone, escalate to in-corpus absorption if it persists.
4. `merge` `MemberPathSegment.Value is NodeId` typing under Generator.Equals `[UnorderedEquality]` — carried VERIFIED-CLEAN; re-verify in the leg-2 merge rebuild (if string keys emit, member-granular patch silently degrades to whole-node replace — repair in place).
5. The Persistence-owned frame-input shapes (`StoreActor` VO + the `ClockPolicy`/`CorrelationId`/`TenantContext`/`RecoveryObjective` ingredients on the injected `ProjectionContext`/`ResolvedProfile` forms) are DEFINED leg 1 and every referencing page (~18, spanning all four legs) re-threads onto them in its own leg ([A.1], [06]) — this IS a corpus-wide Persistence edit, owned per-leg, NOT deferred and NOT unowned. The stronger frame-to-kernel re-home (moving the frame types into the `Rasm` kernel) is the contingent KERNEL/APPHOST counterpart obligation that never lands as a Persistence edit; the injection-of-Persistence-owned-shapes is the primary resolution this campaign executes.
6. BIM:81 GDAL/OGR GeoParquet — anchor RESOLVED at `Rasm.Bim/ARCHITECTURE.md:81` (`Semantics/geospatial ← csharp:Rasm.Persistence/Store`); homed at `Query/columnar` as a sibling counterpart obligation (the Bim campaign re-points the row interior).
