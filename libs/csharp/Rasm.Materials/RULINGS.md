# [RASM_MATERIALS_RULINGS]

`Rasm.Materials` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `SixLabors.ImageSharp` holds at the last major granting Apache-2.0 unconditionally, never the successor line whose build-time licence gate has no open-source opt-out; `Directory.Packages.props` owns the pin. That hold costs real capability and the codec engine table states the cost rather than hiding it — the held major carries NO OpenEXR container and NO `Rgb96`/`Rgba128` HDR pixel format, so `Raster/codec#RASTER_CODEC` routes `Exr` AND `ExrDeep` to `TinyEXR.NET` and keeps `Png16`/`Tiff16`/`WebP`/`Qoi` here; a fence citing an ImageSharp EXR encoder, or any member the successor line introduced, names a member the admitted assembly does not carry.
- `TinyEXR.NET` owns OpenEXR WHOLE — flat, tiled, mip-levelled, deep, multi-part, and the full compression roster — rather than splitting the container between two engines by capability tier. A per-feature split would put the flat-versus-deep decision in the caller's hands and make a deep write a different code path from a flat one; one engine per CONTAINER keeps `RasterFormat` the only axis a caller names.
- `TextureCompressor` is PINNED EXACT and isolated behind the one internal `KtxGate`: a `0.1.0` single-maintainer package with an unstable surface earns a version pin and a single composing site, never a floating range and never a member spelled across the estate. The provisioned `ktx` CLI is the KTX2 encode FLOOR in all three language branches and this package is the in-process ACCELERATION row above it, so the gate answers with the CLI arm when the in-process arm refuses and the whole dependency retires by deleting one arm rather than by re-plumbing every caller. Retirement fires when a maintained managed KTX2 writer ships or when the CLI arm alone meets the throughput bar.
- `Magick.NET-Q16-HDRI-AnyCPU` is DECLINED and recorded as a card rather than silently absent: it would bring AVIF, HEIF, and QOI breadth on a verified osx-arm64 dylib, and the estate declines it because the admitted set already covers every container a texture-grade asset crosses on, while a second full imaging stack doubles the native surface a plugin `AssemblyLoadContext` must load. Re-opening needs a consumer contract naming a container the admitted engines cannot write.
- `Silk.NET.WebGPU` and its `Extensions.WGPU`/`Native.WGPU` companions are BRANCH SUBSTRATE consumed by three folders, and `Raster/gpu#PRESS_DEVICE` is this folder's SOLE composer: Silk.NET 2.x is maintenance-mode and 3.x reshapes the binding, so the raw function table stays behind one internal seam whose swap is a single-page edit. `press#TEXTURE_PRESS` names a `PressBackend` ROW and never a WebGPU type, so the accelerator lane's whole dependency footprint is one page wide.

## [02]-[SHAPE]

- `DesignBasis` receipt columns with per-basis kernel dispatch carry every second design code for an already-cased section family, never a sibling `SectionCapacity` case — realized case names each bind one code (`SteelLrfd` AISC, `TimberEc5` EN 1995, `MasonryCompression` TMS), so a per-code sibling reads as precedent and forks the closed `GoverningAction`/`Utilisation` verdict vocabulary the Compute design-check consumer keys on; a basis-tagged re-cut opens only on the consumer's co-sign.
- Every admitted photo-to-PBR model enters the `Appearance/neural` registry as a `ModelCard` ROW carrying a `LicenseClass` column, and the class alone decides the grant — permissive, copyleft, OpenRAIL, and research-class artefacts all admit, a payment-gated artefact rejects outright, and an artefact whose weight card is SILENT enters `Blocked`, recorded so the estate knows it exists and carrying no grant to run it. `WeightPolicy` states redistribution and carries NO address — the app-root import boundary resolves a `ModelCardId` to bytes for a redistributable and a caller-supplied row alike, so the registry stays a vocabulary and never a distribution channel, and an address column every row fills with absence is the second truth the omission forecloses; a stage naming a model, a boolean beside the class, or a model admitted by prose rather than a row each re-mint the gate the column exists to hold.
- Generative super-resolution products are admissible as `base_color` ALONE — the `ModelCard.PhysicalChannelForbidden` column carries the law and `StageResult.Admit` refuses any other channel from a card bearing it. An up-sampler invents plausible high-frequency detail: on an albedo that is authoring, while on a normal, roughness, metalness, or height plane the shading model then integrates fabricated physics as if it were measured, and no downstream gate can separate the two.
- Text-to-material generation is an EXTERNAL-SERVICE seam and mints no `PbrStage`, `ModelCard`, or provider row — the only locally runnable candidate loses tileability at export, so its output could not pass the `Raster/tile#TILE_SYNTH` coherence gate every estate-produced set clears. A service-produced set enters through `Raster/set#SET_INGEST` classification like any third-party asset, carrying its provenance and licence class as ingest evidence; re-opening needs a local generator whose output survives that gate.
- The standard-sky family is the ISO 15469 gradation × indicatrix ALGEBRA with its fifteen admitted types as rows on `CieSkyType`, never a single hardcoded overcast case — the distribution is one five-parameter formula, so a national-annex or newly standardized sky is a row over the existing groups and the wire carries the whole family on one `skyModel` key spelled `cie-standard-NN`. Hosek-Wilkie coefficients stay a content-keyed data asset with a digest row, the one carve-out from the generated-table law, because a published least-squares fit has no defining sequence to generate from.
- The photo-to-PBR inference hop crosses as a C#-interior content-keyed `[WIRE]` recorded at both the `Rasm.Materials/Appearance/neural` and `Rasm.Compute/Model/inference` `[03]-[SEAMS]` maps — Materials owns the vocabulary, Compute owns execution, and the branch strata forbid a reference in either direction.
- A `PbrStage` row declares CONSUMES and EMITS and nothing else: the plan derives its dependency edges, its order, and each request's input binding from that relation, so a hand-authored predecessor list that could contradict a model's own graph inputs is unrepresentable and a stage's input is always its PRODUCER's output rather than the source photograph. A stage answering a channel demand is a `Cover` row entering a greedy set cover — the row answering the most uncovered channels wins, declaration order breaking ties, so requesting four channels selects one estimator rather than every estimator that answers any one — and a row refining an already-produced product is a `Refine` row the target extent alone admits.
- A stage product that no `TextureSet` carries is a `PriorField` ROW, never a `TextureChannel`: `delight` emits `delit` and `depth` emits `depth`, both dropped at `Acquisition.Import`, so a de-lit observation never seats itself in a set as measured reflectance and no stage emits `height`, whose origin stays the `Raster/filter#PLANE_OP` integration of `geometry_normal` under the depth prior. `StageProduct` closes both vocabularies on one union so the plan folds one relation.
- `IblProducts` are STORED-FRAME: the SH vector, the specular level set, and the luminance guide integrate the dome as authored, and `EnvironmentLight` applies rotation and intensity at every read. Baking either policy into the products would make a re-orientation or a re-exposure a re-bake while the same digest addresses both, forking every consumer reading that blob at a different orientation — the read-time claim and the content-addressed blob are only compatible under this split.
- Tileability is EVIDENCE, never a flag: `TextureSet.Tiled` is `Option<TileProof>`, `Raster/tile#TILE_GATE` `TileGate.Grade` is the only mint, and the proof carries the score it measured. A caller-settable boolean lets a press, an ingest, or a peer manifest assert a property nothing measured, and the assertion then rides the wire as though a gate had passed it; the wire's boolean `tiled` is the projection of the proof's presence, never its source, and a third-party set declaring itself tileable carries absence until this estate grades it.
- A summary row is MEASURED, never read off a pyramid tail: `Raster/set#SET_BIND` `Average` folds each channel's base level through the plane's own decoded row rail. Only a box fold's `1×1` tail is the arithmetic mean — a Kaiser window, a normal renormalization, and a variance coupling each move it — so publishing a tail as the mean fabricates the number the seam `AppearanceSummary` key then carries forever. The measured fold costs one pass over planes the press just wrote, needs no pyramid, and therefore admits the single-level set the tail read had to refuse.
- A GPU preview covers its subject's FULL algebra or refuses it: `Raster/gpu#WGSL_KERNEL` `noiseField` carries every `NoiseBasis`, `FractalMode`, and `CellularDistance` × `CellularReturn` row of the 2D lattice, and `Raster/press#PRESS_PLAN` refuses a solid 3D source at admission rather than lowering it through the 2D arm. A preview that silently substitutes one basis, one fractal trajectory, or one dimension for another is worse than no preview — the operator reads a plausible image of a field the bake will not produce, and the parity workload grades the difference as backend divergence rather than as the missing kernel it is.
- Every GPU uniform crosses as WORDS and every golden fixture builds its block through the same `KernelUniform` writer: each `Params` struct interleaves `f32` with `u32` and `i32`, so a float-typed carrier writing `4f` into a `u32` slot hands the shader `0x40000000` read as one billion, on every extent, octave count, seed, and op code that crosses. A fixture whose expected value is the ANALYTIC limit of a discrete kernel is the same defect wearing a derivation — the `irradianceSh` vector pins the kernel's own midpoint sum, whose gap from `2√π` IS the quadrature error the fixture resolution fixes.
- Persisted plane bytes are ALWAYS CPU-minted and the `webgpu` backend is an ACCELERATOR whose output is never content-addressed. The veto is STRUCTURAL rather than empirical: GPU `f32` cannot reproduce the CPU `f64` procedural lattice, so a GPU-keyed plane forks the content key at its preimage and two runs of one plan on two machines would address different blobs. `press#PRESS_RECEIPT` `PressProduct.Preview` carries planes with no `TextureSet` and therefore no key at all, `interchange#TEXTURE_EGRESS` `TextureSetWire.Of` refuses a receipt whose backend is not content-authoritative, and the parity benchmark grades the divergence as TELEMETRY — a tolerance under which a GPU plane could be keyed is exactly the equivalence this ruling denies.
- Neighbourhood operations live at `Raster/filter#PLANE_OP` over a whole plane and NEVER as an `AppearanceNode` case. The node DAG is evaluated per shading point, so a node has no neighbours to read: a blur, a normal-from-height integration, an occlusion sweep, or a curvature fold would have to either fabricate a neighbourhood or force every sample to carry a plane. The press bakes the DAG's pointwise field FIRST and folds the plane algebra after, so the two owners compose in one direction and a "blur node" is unrepresentable rather than merely discouraged.
- Seamless tiling is a SOURCE property before it is a plane repair: `texture#TEXTURE_UV` `NoisePeriod` wraps the integer lattice cell index so a wrappable procedural is periodic BY CONSTRUCTION, and `Raster/tile#TILE_SYNTH` heals only what could not be — an ingested photograph, an image source, an OpenSimplex2 field whose skewed cells meet no integer wrap. Healing a field that could have been authored periodic spends a graph-cut on a problem the lattice already solves and leaves the result approximately seamless where the wrap is exact. Periodicity is a CHAIN property, so `Noise.Of` proves the basis wrappable and the lacunarity, frequency, and warp frequency integral against the period rather than gating the basis alone.
- The seam `AppearanceKey` preimage is FROZEN at the seven neutral PBR values, and every richer appearance product rides BEHIND it as a payload column: `TextureSetWire.appearanceKey` names the key its set refines, and the set key is a field of the document rather than an eighth summary column. Widening the summary would fork the `Rasm.Bim` dedup key and re-id every `Node.Appearance` in the estate for a field only a texture consumer reads, and a material with and without a baked set would stop deduplicating — which is wrong, because the set REFINES one appearance rather than describing a second.
- `KtxPayload` decides WIRE LEGALITY, not merely compression: `uastc` and `etc1s` are Basis-transcodable and cross a corpus-borne document, while `rawBcn` holds BC block data direct and is a branch-local desktop payload the estate's own viewer cannot read — the TypeScript `ktx-parse` + basis-transcoder path has no decoder for it. `TextureSet.Of` and `TextureSetWire.Of` both refuse it, so the file a consumer would fail on never reaches a manifest-borne wire, and a desktop consumer reads it off the object store by address instead.
- `Bake` is a name this folder does NOT take: `Rasm.Rhino` `TextureBake` is the host evaluator gate and the seam `Node` carries a `Bake` inheritance fold, so the texture-generation owner is `TexturePress`/`Press` throughout — page, type, receipt, benchmark row, and instrument alike. Three owners of one verb across one estate makes every cross-folder sentence ambiguous, and the cost of the third is borne by the newest arrival.
- `EnvironmentMap` admits `linear`, `pq`, and `hlg` alone and lifts ONE scene-linear sampler PER LAYER. It is the one surface in the corpus where the display-referred pair is legal, `srgb` and `raw` refuse because an sRGB dome cannot carry a sun and a raw dome declares no light quantity, and the per-layer lift is structural: `Raster/plane#TEXTURE_PYRAMID` `AsImage` carries one layer by construction, so a six-face cube admitted as one sampler would refuse the bridge and every layered arm would be declared capability that cannot run.

## [03]-[COLLAPSE]

- The appearance graph carries ONE evaluation rail: `graph#MATERIAL_GRAPH` `CompiledGraph.ShadeSpan` folds a slot-addressed scratch whose order IS the compiled sort, and the per-point `Shade` the integrator holds re-enters that rail over a one-element window rather than folding a second port environment. Two representations over one node algebra are a divergence nothing type-checks — the batched arm would silently answer a malformation the per-point arm rails on — and the immutable-map projection the per-point arm once paid rebuilt a persistent map per node per sample, strictly more allocation than the scratch that replaces it. The collapse is what makes the compile-time ANSWERABILITY proof sufficient: a slot read has no cell for a port no node declares OR a port whose node produces nothing, so `MaterialGraph.Compile` refuses both against the `AppearanceNode.Produces` COLUMN and every read on the rail is total. A port reader expressed as a delegate is unspellable regardless — a lambda may not capture a `Span<T>` — so a second environment shape minted to satisfy one is the fork this row forecloses.
- Raster ENCODE owners stay PLURAL across the branch and the third PNG writer is earned rather than tolerated: `Rasm.Rhino` `RasterPolicy` encodes a viewport capture, `Rasm.AppUi` `VisualCodec` encodes a UI capture, and `Raster/codec#RASTER_CODEC` encodes a CONTENT-KEYED texture asset at 16-bit and float depth with ICC, block compression, and a KTX2 container none of the other two can express. Collapsing the three would put a plugin-tier imaging dependency inside a host-shell capture path and force a viewport screenshot through a content-addressing rail it has no key for; the discriminant is the PRODUCT — a display artefact versus an addressable appearance asset — never the file extension they happen to share.

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- (none)
