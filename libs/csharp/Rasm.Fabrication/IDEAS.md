# [FABRICATION_IDEAS]

The forward concept pool for `Rasm.Fabrication`. Each idea is a card — a bracketed slug leader plus a few bullets stating the capability, what it unlocks, and the gap or modern technique it draws on. An idea drives one or more `TASKLOG.md` tasks; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[DRL_NEST_POLICY]-[BLOCKED]: learned placement scoring becomes a policy column over the existing NFP primitive.
- Capability: a learned placement-score slot that deepens `Nesting/nfp#NESTING` without changing the no-fit-polygon geometry owner.
- Shape: `NestPolicy.Score` carries an injected `Func<NoFitPolygon, PartTransform, double>` delegate ranking candidate transforms inside the existing bottom-left/genetic folds.
- Unlocks: higher utilization on irregular sheet-metal nesting without a Fabrication-side `Rasm.Compute` reference, second runtime, or learned-vs-heuristic packer split.
- Anchors: `Rasm.Compute/Model/inference#INFERENCE_MODES`, `RunOps.Infer`, `Nesting/nfp#NESTING`, and the raw-scalar ingress pattern used by `RemovalBudget`.
- Tension: the trained placement-ranking model asset and consumer-side wiring must exist before the learned column joins the bottom-left/genetic folds.
- Ripple: realized by the `[NFP_DRL_POLICY]` task in this folder's `TASKLOG.md`.

[GUARD]-[QUEUED]: A collision-and-gouge guard sub-domain owns the swept tool-plus-holder envelope every feed move passes through — gouge against the finished part, collision against stock/fixture keep-outs, and the safe-Z lift trajectory routing a blocked move around both — the mandatory professional-CAM safety floor the milling-biased folder lacks.
- Capability: A Guard static owner producing a SweptVolume (the tool+holder profile dragged along each Move via the realized Polygon/clipper MinkowskiSum precision-correct disc-along-segment plus the holder offset ring) and a closed Verdict [Union] (Clear / Gouge carrying the violated part-surface point / Collision carrying the obstacle keep-out / Clearance carrying the lifted retract trajectory) Cam.Solve consults before committing each feed move. The reach check reads the realized StraightSkeleton.ClearanceAt(Loop, Point3d) inscribed-radius lookup (the wavefront-time channel half-width per point) so a stickout-limited tool tests its local reach against the channel rather than re-deriving a distance transform. The part-keep geometry is held distinct from the stock-remove geometry so a gouge separates from a collision. The Lift verdict is the collision-aware retract the motion page names as growth but never owns; the broad-phase prune reads the kernel SpatialIndex BVH.
- Shape: A new Toolpath/guard design page: SweptVolume record over the Polygon/clipper Minkowski substrate; Verdict [Union] (Clear/Gouge/Collision/Clearance) read by a generated total Switch; Guard static surface owning Sweep (move -> swept polygon), Check (swept vs Part/Stock/Fixture exclusion), Lift (blocked-move -> clearance-plane retract); GuardPolicy carrying clearance-plane height, gouge tolerance, holder-included flag.
- Unlocks: Cam.Solve gains a real safety contract — a feed move that gouges the finished surface or collides with a clamp routes a typed Gouge/Collision fault instead of silently cutting through, and Lift becomes the collision-aware retract; the guard reads the SKELETON clearance field and the MAGAZINE holder envelope.
- Anchors: Toolpath/motion#CAM_MOTION Cam.Solve Move stream + the named collision-aware-retract growth row (motion.md line 19); Polygon/clipper#POLYGON_ALGEBRA realized MinkowskiSum (precision-correct decimalPlaces) + Offset for the swept envelope; Nesting/workholding#WORKHOLDING ExclusionZone keep-out; csharp:Rasm/Geometry/Spatial#SPATIAL SpatialIndex BVH broad-phase; Process/owner#FABRICATION_OWNER Loop.Covers; MAGAZINE holder envelope; Toolpath/skeleton#STRAIGHT_SKELETON realized ClearanceAt clearance-radius lookup
- Tension: Gouge detection needs the part-keep geometry distinct from the stock-remove geometry; the guard page defines a Part-vs-Stock input pair Cam.Solve supplies, growing the FabricationInput Profiles list into a Part/Stock keep/remove split the guard reads — FabricationInput today carries one Profiles Arr<Loop>, so the keep/remove split rides the same record the MULTISHEET Seq<Stock> and dialect-override changes touch, landing the input-shape growth once.

[PROBING]-[QUEUED]: An in-process probing-and-metrology sub-domain owns the touch-probe cycle vocabulary, the work-offset and tool-length measurement routines, and the measured-feature receipt closing the loop from cut geometry back to corrected coordinates — the G38/G10 metrology floor every professional CNC controller carries and the folder has no owner for.
- Capability: A Probe static owner producing a closed ProbeCycle [Union] (SingleTouch / BoreCenter / WebCenter / CornerFind / SurfaceZ / ToolLengthSet) each projecting to a typed ProbeResult (touched point set, derived feature parameter, residual versus nominal), plus a WorkOffset projection folding a measured datum into a G54-G59 coordinate correction and a tool-length offset Posting emits. The measured residual feeds back as an adaptive coordinate shift so the next operation cuts against the as-set workpiece.
- Shape: A new Toolpath/probing design page: ProbeCycle [Union] carrying approach geometry and G38 dialect words; ProbeResult [Union] one-to-one with the cycles carrying the typed measured feature; Probe static surface owning Cycle (non-cutting Move stream with G38.2 touch words), Reduce (touched-points -> feature parameter), Offset (measured datum -> WorkOffset correction); ProbePolicy carrying probe-tip radius, overtravel limit, feedrate; the bore/web center fit reads the kernel Predicate.Orient2D.
- Unlocks: Adds the probing GWord rows (G38.2/G38.3 touch, G10 offset-set) and the measured WorkOffset Posting injects, and the closed-loop adaptive shift Cam applies between roughing and finishing — the metrology consumer AppUi and downstream controllers expect but the folder has no owner for.
- Anchors: Toolpath/motion#CAM_MOTION Move stream (non-cutting probe Move); Posting/program#CUT_PROGRAM GCommand/GWord (G38/G10 rows); Process/family#PROCESS_FAMILY Machine (probe-capable column); Process/owner#FABRICATION_OWNER FabricationResult (a Probe receipt arm); csharp:Rasm/Geometry/Numerics#ROBUST_PREDICATES Predicate.Orient2D bore/web fit; FabricationFault probe-overtravel arm
- Tension: The measured WorkOffset is a runtime feedback the static fold cannot observe; the page defines the offset as a typed correction a re-run consumes — the probing cycle emits the G-code, the operator runs it, the measured residual re-enters as input state, never an in-folder controller round-trip.

[MULTI_FIXTURE_SCHEDULE]-[QUEUED]: Workholding deepens from a single-fixture keep-out into a multi-fixture-setup scheduler partitioning a job's operations across reorientation setups, sequencing setups to minimize re-fixturing, and tracking the per-setup datum so a part machined in three setups carries a consistent coordinate lineage — the multi-setup planning professional CAM owns above the single-clamp keep-out.
- Capability: A Setup [ComplexValueObject] carrying its Fixture, its work-coordinate datum, and the operation subset reachable under that fixture's keep-out and the Machine.AxisCount; a Schedule fold partitioning the operation set across setups by reach-and-clearance feasibility, ordering setups to minimize re-fixturing cost, and emitting the per-setup datum-shift Posting renders as a new G54-G59 block per setup — the collision-aware operation-to-setup assignment plus setup ordering turning the single-fixture keep-out into a real multi-setup job plan.
- Shape: Extends Nesting/workholding#WORKHOLDING: a Setup record (Fixture, Datum, ReachableOps) the Schedule fold partitions over; a SetupSchedule fold assigning each operation to its first feasible setup (Clear verdict + AxisCount reach) and ordering setups by a re-fixture-cost weight; the per-setup WorkOffset Posting emits as a coordinate-system block; a SchedulePolicy carrying the re-fixture-cost weight and the max-setup cap.
- Unlocks: Replaces the implicit one-fixture-per-job assumption with a multi-setup plan Cam and Posting honour — a part needing top-face then back-face machining gets two setups with two datums and a clean per-setup G-code section, and GUARD tests each operation against the setup it is assigned to, not a single global fixture.
- Anchors: Nesting/workholding#WORKHOLDING Fixture/Clamp/ExclusionZone + the named multi-setup growth; Process/family#PROCESS_FAMILY Machine.AxisCount reach; Posting/program#CUT_PROGRAM Sequence + a per-setup coordinate-system block; GUARD per-setup collision input; PROBING per-setup datum measurement; Thinktecture [ComplexValueObject]
- Tension: The reachability test that assigns an operation to a setup needs the Machine kinematic envelope (AxisCount + tilt range), which today is a scalar count, not a workspace — the schedule page reads a coarse AxisCount-keyed reach class or defers exact reach to the Toolpath/kinematics IK solver.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[CSG_SILHOUETTE]-[COMPLETE]: projection.md Hlr.Solve composes the kernel arrangement seam — Arrangement.Apply(MeshBoolean)/ToMesh re-emits the watertight kept-cell mesh off the HiddenLine.Watertight Option<BooleanSolid> operand, the per-facet HLR kernel projecting it unchanged, no in-folder CSG; Ripple: Rasm/Geometry [CSG_WATERTIGHT_SILHOUETTE].
[MAGAZINE]-[COMPLETE]: magazine.md owns the Magazine [SmartEnum<string>] carousel/turret/manual slot map, the ToolAssembly [ComplexValueObject] holder geometry, the minimal-swap Schedule fold, and the HolderEnvelope projection GUARD and workholding read, the G43/M6/Tnn blocks emitted by program.md; the cutting-data depth rides the Process/physics Tool payload, no parallel ToolMaterialTable.
[FEEDRATE_LOOKAHEAD]-[COMPLETE]: program.md Lookahead jerk-limited block-buffering planner smooths the per-block Feedrate seed into an S-curve velocity profile over the LookaheadPolicy MaxAcceleration/MaxJerk/Window/JunctionDeviation caps reading Machine.AxisCount, the forward/backward trapezoidal sweep bounded by the v²=v₀²+2as envelope, the dimensional jerk-vs-accel discipline load-bearing.
