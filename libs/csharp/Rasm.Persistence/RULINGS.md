# [PERSISTENCE_RULINGS]

`Rasm.Persistence` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `age` (Apache AGE) admits `ExtensionAdmission.Standalone`, never `Preload` — `CREATE EXTENSION age` succeeds with no `shared_preload_libraries` and the per-session `LOAD 'age'` is a runtime connection concern, not an install gate; re-adding `Preload("age")` — the sweep move reading AGE's `_PG_init` planner/parse hooks as a preload requirement — makes the verification fold emit a spurious `MissingPreload` `shared_preload_libraries` diff for a correctly per-session-`LOAD` deployment, and re-litigation opens only when AGE upstream hard-errors on `CREATE EXTENSION` without preload.

## [02]-[SHAPE]

- A rank vocabulary declares the projected expression and DERIVES its order fragment, never the reverse — `Score` is the primary delegate and `Rank` derives as `"{Score} DESC"`, so an arm cannot project one expression while ordering by another; the foreclosed form baked the direction into the score expression, leaving no caller able to project the score as a column. Instantiated at `Query/retrieval#LEXICAL_ALGEBRA`.
- Retention DERIVING from provenance lands as two `ArtifactKind` rows and one selector reading the discriminant off a value the producer already holds and returning every projection of that value — `ArtifactKind.Texture(Option<UInt128> planKey)` answers `TextureSet` (`Cache`, rebuildable from its recorded graph/plan/seed triple) or `TextureAcquired` (`Blob`, durable because a retired model card and a drifted execution provider make the bytes unreproducible) BESIDE the `SourceKey` the admission records, so a caller cannot pair acquired bytes with a rebuildable class the sweep then evicts and cannot claim rebuildability on a row whose own index carries no origin; the recurring moves are an origin flag beside the value that already discriminates, a single row whose retention column a caller overrides, and one discriminating value read twice at a call site so its two projections drift, and re-litigation opens only if `ArtifactIndexRow` stops deriving retention from its kind.
- `LandingArm` is the DATASET SHAPE, never the producing package — one package holds as many arms as it lands datasets whose generations prune on different segments, because a shared arm splits that arm's hive tree at a segment neither scan can prune on and strands every question behind a full-arm read; the recurring move folds a package's second dataset under its first arm for roster tidiness, and re-litigation opens only if the hive layout stops carrying the arm's partition noun.
- `ProjectionContext` is the one Persistence time AND causal frame — it seats the clock delegates beside the kernel `CorrelationId`/`TenantContext` pair as VALUES, so an AppHost `ClockPolicy` or `Principal` parameter on a Persistence signature is the named strata inversion while the S0 causal pair rides the frame directly; re-litigation opens only if `ProjectionContext` stops carrying the sampled instant or the causal pair.
- `Store/Schema` composes contract artifacts only from owners this package holds — a sibling AEC peer contributing an artifact row reverses the strata the seam fixes, so a peer's durable shape enters as an `ElementGraph` projection this package then declares, never as a foreign artifact the contract admits directly.
- `Crdt.Apply` and `GraphDelta.Apply` are the only materializers — projection, live merge, and AS-OF reconstruction each fold the one delta, and a second materializer beside them forks replay from live state so the two disagree silently at the first conflicting op.
- Usage levels carry `rasm.tenant` alone — retention class and storage tier stay `UsageReceipt` and census-wire facts, so the estate tenant series cap governs one keyed family instead of a class-by-tier fan-out of it, and the chargeback breakdown reads off the evidence plane the page already names billing truth; re-adding class or tier as meter dimensions is the recurring move that multiplies a capped series, and re-litigation opens only if the chargeback fact table stops carrying the breakdown.
- `OpenTelemetry.Instrumentation.ConfluentKafka` owns the Kafka producer-to-consumer causal edge and this package composes none of its own — the instrumented consumer already extracts each record's W3C context onto that record's process span as an `ActivityLink`, so a `TraceCarrier.Link` fold at `CdcIngress` mints a second edge for one cause and re-implements shipped capability, while the poll-driven batch commit takes no link because links bind at span creation and the fold learns its members only after; the kernel fan-in bracket serves a drain that reads its members BEFORE it opens, and re-litigation opens only if the instrumentation stops attaching the link or the consume loop stops being poll-driven.
- Egress settlement fans every column `EgressReceipt.Drained` partitions, duplicates included — a sink's own dedup already absorbed the replay, so a duplicate settles on the delivered side and dropping its row strands rows the receipt counted outside the denominator the settlement share divides by; the recurring move reads "duplicate" as neither delivered nor failed and omits it, which reports a delivery rate above the traffic that produced it, and re-litigation opens only if the drain stops conserving its drained count across the settlement columns.
- Tenancy crosses this package as the kernel types and never their key scalars — the frame seats `TenantContext`, a durable partition column seats `TenantId`, and the raw `UInt128` is reached through `TenantId.Value` only where a byte pack, an object-name prefix, or a digest input demands the scalar; the kernel root row IS the absent tenant, so `Partitions`, `Entry`, and `Tags` all read that row and a page-local zero test, a `rasm.tenant` string literal, an empty-string tag value, or a per-seam raw-to-`TenantContext` lift each re-mint the sentinel Tier-0 forecloses.
- Each analytics residence spells its own PHYSICAL literals and reads its dataset's own spine — the 16-byte tenant key renders per row as `bytea`, `FixedString(16)`, or a hive segment, an instant through that dialect's own cast, partition and TTL columns off `AnalyticsSchema.Time`; quoted hex compared against a binary key matches nothing and raises nothing, so the sort key stops pruning while reads still return, and re-litigation opens only if one encoding serves all three dialects.
- Read SCOPE parts from read SHAPE at the residence entry — the tenant rides the frame and the window rides `ResidenceWindow`, so a Substrait plan carries filters, projections, and folds alone and an unbounded or cross-tenant scan has no shape that expresses it; the recurring move puts the window in the plan as a bare number, which then compares against three distinct timestamp types, and re-litigation opens only if a residence stops partitioning on time.
- Rollups materialise SUMMARY STATE and their readers name the accessor — a continuous aggregate over an irregular series stores `time_weight` beside `percentile_agg` and the read projects `average`/`approx_percentile`, so a cheap tile and a raw-chunk investigation answer one statistic; the recurring move materialises `avg`/`min`/`max` because a scalar view needs no accessor, which ships a mean the accelerated read never claimed and hides the divergence behind one caption, and re-litigation opens only if a series family stops carrying irregular timesteps.
- Columnstore segment lists carry BOUNDED keys alone and identity keys order instead — each column's declared `ColumnType` decides the split, never the key roster, because segmenting on a `KeyHex` content key mints one compressed batch per row and deletes the compression the columnstore exists for while the rollup's own `GROUP BY` keeps the whole key so each stream holds its buckets; re-litigation opens only if a series identity becomes bounded.
- Every residence relation resolves through ONE lower-cased `AnalyticsSchema.Table` that DDL and read both quote — PostgreSQL folds an unquoted identifier, ClickHouse folds nothing, and a producer domain carrying a mixed-case `TelemetrySource` key therefore provisions one relation while the quoted plan addresses another that never existed; the recurring move quotes the read alone because the read is where the failure shows, and re-litigation opens only if every dialect adopts one folding rule.
- Plan narrowings render through the column's own declared `ColumnType`, and a type Substrait carries no literal for is read SCOPE rather than plan shape — a text operand against a numeric column raises on ClickHouse and silently coerces on PostgreSQL, so the two dialects disagree about a filter neither reports; the recurring move renders every match as a string because the seam takes text, and re-litigation opens only if Substrait grows timestamp and binary literals.
- Container column types GENERATE over the scalar roster and never enumerate — `ColumnShape` carries `List`, `Map`, and `Dictionary` as cases over `ColumnType`, so an OTLP attribute map, a span-event run, and a low-cardinality dimension are values of one case set and each residence answers four composer columns; a flat `map-string-string` row per element pair strands every pair nobody named and forks the dialect vocabulary across roster and arm, and re-litigation opens only if a container arrives whose element type is not itself a column shape.
- `ColumnType` and `ColumnShape` are BRANCH-LOCAL vocabulary — no `tests/contracts/MANIFEST.md` entry names a column type, so Tier-0 `[03]-[UNIVERSAL_VS_CAPTURE]` withholds cross-language meaning and a peer runtime planting a residence relation reaches this custodian's `ResidenceDdl` rather than mirroring these keys; the recurring move reads mirrored spellings as a correspondence and forks both rosters on the first row either side adds, and re-litigation opens only when a manifest entry beside its conformance fixture lands.
- This custodian reads NO metrics store and owes no `Residence` row for one — Prometheus, VictoriaMetrics, and Mimir are TSDBs defined by a per-series cardinality ceiling, the one column this family refuses to grow because unbounded dimensionality IS the capability, so the branch's metric-side obligation is EMISSION through `Store/observability#STORE_INSTRUMENTS` onto the OTLP wire and the Tier-0 `[08]` conformance table holds with zero C# metrics-store reader; a TSDB row imports that cap and forks the one lowered-plan currency onto a second query language, and re-litigation opens only if a metrics store drops its cardinality ceiling.
- Residence policy health measures OUTCOME at the family and self-report only where an engine publishes one — `ResidenceRead.Health` reads each residence's resident time extent against its declared horizon so a stalled expiry surfaces on every tier, and `SeriesLane.Jobs` adds the Timescale bgworker history naming which policy stalled; a family probe transcribing one engine's catalog measures one tier and reports a healthy silence for the other two, and re-litigation opens only if every residence engine publishes a comparable scheduler catalog.
- Every `ResidenceFault` names its residence beside one engine-neutral `EngineFault` pair — a SQLSTATE, a ClickHouse numeric code, and a DuckDB error type are three alphabets for one question, so each renders to text at its own row's `Diagnose` column; a per-backend case carrying a column only one engine fills makes the family answerable by one row and forces every sibling through a case named for someone else's engine, and re-litigation opens only if the engines converge on one diagnostic vocabulary.
- Custodian-stamped columns sit outside the SUPPLIED roster the arity gate proves against — the tenant leads, a `TimeSpine.Landing` dataset's instant trails, and `ResidenceLanding.Supplied` is `Payload` minus both, so a producer sends exactly the cells its category obliges and one batch reads the landing clock once; proving arity against the whole `Payload` demands a cell for the column the landing category forbids a producer to carry, which refuses every correct landing-time producer on row arity while reading as a working gate, and re-litigation opens only if a producer earns the right to stamp the custodian's clock.
- The read phase ledger and the read receipt are TWO rails over disjoint questions — `ReadRouter.Observed` brackets one pooled `ILatencyContext` whose checkpoints answer where every read spends across route, wait, connect, and execute, while `store.query.wait` answers how ONE read resolved and carries the watermark and the measured elapsed as typed fields; the recurring moves lift a phase duration out of the ledger into a receipt field or duplicate the staleness gap onto the ledger as a measure, each minting a second owner for one number that then disagree on the first edit. The pooled context never escapes its bracket because `LatencyData` projects its spans over backing the pool re-leases on release, and every phase name is a `ReadPhase` row the composition's own registration folds — a literal at either end resolves a positionless token whose writes drop with nothing raised; re-litigation opens only if the receipt stops carrying the elapsed wait.
- `TimeSpine` travels on the DECLARATION and the seam refuses a category its columns contradict — `Admit` takes the category beside the optional `time`, faults a `Landing` dataset naming its own clock and an `Event` dataset naming none, and appends the landing column only on the category that hands the axis over; inferring the category from whether `time` arrived is what makes the drift invisible, since the append that correctly serves a landing dataset silently re-dates every fact on an event-time one, and re-litigation opens only if a third clock owner enters the vocabulary.

## [03]-[COLLAPSE]

- (none)

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- `ElementSet` membership widening re-cuts the frozen `elementset` parity vector in the same pass — the length-framed content-address and its `ContentParityCorpus.Contribute(ParitySlot.ElementSet, …)` freeze are one atomic contract; a membership change landed without the re-freeze diverges one selection's hash across the three runtimes.
