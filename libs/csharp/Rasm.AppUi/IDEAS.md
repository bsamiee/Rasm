# [APPUI_IDEAS]

The forward concept pool for the product UI engine. Open ideas are higher-order folder concepts grounded in the AEC/Rhino purpose and current rendering, reality-capture, and coordination research; each drives one or more task cards in `TASKLOG.md`. A finished or dropped idea moves to closed with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[SHADER_ASSET_OWNER]: the `shading/` sub-domain — a content-keyed WGSL/SKSL shader-source-and-pipeline-state owner the render-graph passes bind below the `RenderTargetFactory` column, named in `ARCHITECTURE.md`/`README.md` with no design page yet.
- A `ShaderModule` keys each WGSL (the `Wgpu`/`WebGpu` families) and SKSL (the `SkiaGanesh`/`SkiaRaster` families) source by its `XxHash128` content hash so an identical shader compiles once across frames and backends, a `PipelineState` cache memoizes the `Silk.NET.WebGPU` `RenderPipeline`/`ComputePipeline` and the SkiaSharp `SKRuntimeEffect` per `(ShaderModule, RenderTargetFactory.Backend, vertex-layout)` key so the meshlet mesh-shader pass, the ReSTIR path-trace compute pass, the volume ray-march, and the splat rasterizer each bind a compiled pipeline rather than recompiling per frame, the `ShaderCompileReceipt` seals the WGSL/SKSL diagnostic stream through the one `ReceiptSinkPort` so a shader-compile failure is attributable evidence not a silent black frame, and the dev-loop shader-watch recompiles a changed source under the HotAvalonia hot-reload so a shader edit re-keys and re-binds without a viewport restart.
- Unlocks the render-graph passes binding compiled pipelines from one cache instead of each pass owning its shader-compile path — a new render pass is one shader-source row plus one pipeline-state key, never a per-pass compile site, and the `wgsl`-versus-`sksl` divergence resolves at the `GpuFamily` axis the cache keys on, the per-backend pipeline-state spelling owned once where every pass reads it.
- Draws on the GPU-driven-rendering pipeline-state-object cache technique, the `Silk.NET.WebGPU` `DeviceCreateShaderModule`/`DeviceCreateRenderPipeline`/`DeviceCreateComputePipeline` surface (`.api/api-silk-webgpu.md`) and the `Silk.NET.WebGPU.Extensions.WGPU` `SetLogCallback` native-diagnostic routing (`.api/api-silk-webgpu-wgpu.md`), the SkiaSharp `SKRuntimeEffect` runtime-shader compile, and the content-addressed-identity law the viewport residency already keys on — a per-pass shader compile or a non-cached pipeline rebuild is the named defect the shader owner retires.

[IMMERSIVE_XR_SESSION]: the `immersive/` sub-domain — an OpenXR stereo design-review session projecting the one `RenderGraph` into a headset over the shared `Wgpu` device, named in `ARCHITECTURE.md`/`README.md` with no design page yet and the heaviest light-external-surface gap in the folder.
- An `XrSession` is created against the `Silk.NET.OpenXR` graphics-binding chain over the very `Device`/`Queue` the `Wgpu` `GpuBackend` family already owns (one GPU lifetime across flat and immersive), a `StereoSwapchain` allocates the per-eye image array the render-graph passes target, the `XrFrameLoop` runs `WaitFrame`/`BeginFrame`/`EndFrame` against a `CompositionLayerProjection` on the predicted-display-time clock so a frame composites at head-pose-corrected time, an `XrViewpoint` projects the controller-navigated camera and the LiDAR/snap measurement and BCF-`Viewpoint` markup into the headset so an issue authored in VR round-trips the same portable view-state the flat viewport mints, and an `EnvironmentBlend` passthrough case folds the camera feed under the `FB_passthrough`/`XR_ENVIRONMENT_BLEND_MODE` surface for mixed-reality on-site review, the whole session folding to the flat viewport when the host OpenXR loader is absent so the immersive path is a render-target case, never a parallel scene model.
- Unlocks an on-headset coordination walkthrough where the BCF issue, the saved viewpoint, the measurement, and the section box are the same owners the flat viewport carries — a new XR interaction is one action-set row over the OpenXR controller model and a new composition layer one swapchain target, never a second viewport engine, and the stereo frame is the one `RenderGraph` projected through a per-eye `RenderTargetFactory` rather than a re-authored render path.
- Draws on the OpenXR 1.1 stereo session and `KHR`/`EXT`/`FB` extension surface (`.api/api-silk-openxr.md`), the shared-`Wgpu`-device one-GPU-lifetime law the viewport `RenderGraph` already binds, the `Viewpoint`/BCF codec the coordination board owns, and the 2025-2026 AEC mixed-reality on-site-review maturation — the env-blend passthrough is the `Silk.NET.OpenXR.Extensions.FB` admission the README registry and the manifest both lack today.

[NARRATED_REVIEW_TOUR]: the `presentation/` sub-domain — an ordered saved-`Viewpoint` review-tour sequencer narrated over the animation playhead and projected from a BCF topic set, named in `ARCHITECTURE.md`/`README.md` with no design page yet.
- A `ReviewTour` is an ordered `TourStop` sequence each binding a saved `Viewpoint`, a per-stop dwell and transition easing off the `motion/` token vocabulary, and a narration-annotation track the typography rail shapes; the `TourPlayhead` drives the camera interpolation between stops on the animation `Track`/`Scrub` clock so a tour scrubs re-entrantly with no drift exactly as the animation timeline does, the offline walkthrough render emits the tour through the `visuals/` `SKDocument`/codec export so a review packages as a portable frame sequence or paged deliverable, and a `TopicTour` projection folds a `Rasm.Bim` BCF topic set into a stop sequence so a coordination issue list becomes a guided walkthrough — the presentation owner composing the settled viewpoint, animation, motion, typography, and coordination owners, never a parallel scene or camera model.
- Unlocks the client-facing design-review deliverable the platform exists to produce — a narrated tour of saved views with timed transitions, an annotation track, and an offline-rendered walkthrough — each a projection over an existing owner, so a new transition is one motion-token row and a new tour source one projection arm, never a second animation or camera engine.
- Draws on the saved-`Viewpoint`-sequence law, the animation `Track`/`Scrub` re-entrant-no-drift playhead, the `motion/` reduced-motion token algebra, and the BCF-topic-to-tour projection the coordination board's `Issue`/`Viewpoint` binding already makes possible — a wall-clock tour animation or a tour-local camera snapshot is the named defect.

[GPU_PROFILER_TIMELINE]: a hardware-measured per-pass GPU-timing-and-pipeline-statistics frontier on `evidence/diagnostics-evidence.md` replacing the encoder-projected GPU-time the `FrameReceipt` per-pass seq carries with resolved timestamp-query measurement.
- The `FrameReceipt` already folds a per-pass `Duration` seq, but its GPU figure is the CPU-side accumulated-cost projection the budget gate sums, never measured GPU wall time; a `GpuTimingPass` writes `Silk.NET.WebGPU` `QueryType.Timestamp` queries at each render-graph pass boundary and resolves the `QuerySet` through the non-blocking `DevicePoll` so the per-pass figure becomes resolved GPU nanoseconds (cull, geometry, path-trace, sim, composite), a `QueryType.PipelineStatistics` query captures vertices-shaded, primitives-culled, and fragment-invocations so a slow pass attributes to a specific bottleneck not just a duration, and a `GpuTimeline` projection correlates the measured GPU seq against the projected CPU seq on the diagnostics timeline keyed by the frame ordinal so a projection-versus-measurement divergence is itself attributable evidence.
- Unlocks a render-graph profiler where the perf-budget governor degrades the genuinely-overrunning pass on measured GPU cost rather than the projected sum — the `QualityTier` hysteresis band reads which pass actually overran, a new profiled pass is one timestamp-query pair, and the GPU timeline is one evidence-receipt projection, never a second telemetry surface beside the `FrameReceipt`.
- Draws on the GPU timestamp-query / pipeline-statistics-query technique, the `Silk.NET.WebGPU` `DeviceCreateQuerySet`/`CommandEncoderWriteTimestamp`/`CommandEncoderResolveQuerySet` surface (`.api/api-silk-webgpu.md`), the `Silk.NET.WebGPU.Extensions.WGPU` non-blocking `DevicePoll` that lets the resolve complete without a blocking fence, and the settled `PerfBudget`/`QualityVerdict` governor and `FrameReceipt` per-pass fold the page already owns — a budget gate degrading on projected rather than measured GPU cost is the defect the timestamp timeline retires.

[ANTIALIAS_RESOLVE_LADDER]: a temporal-antialiasing and super-resolution resolve owner on `viewport/viewport-pipeline.md` deepening the `Composite` pass into a settled image-quality ladder rather than a single raw raster.
- A `ResolvePass` family carries the `Msaa`/`Taa`/`Fsr`/`Smaa` rows as a `[SmartEnum]` policy axis the render graph selects after the geometry and path-trace passes — the `Taa` row jitters the camera sub-pixel per frame and reprojects the prior frame through the motion vector buffer with a neighborhood-clamp history rejection so a static scene converges and a moving scene ghosts no tail, the `Fsr` spatial-upscale row resolves a sub-resolution render to the display resolution under the VRAM budget so a 4K viewport renders at a fraction of the pixel cost, and the `ResolvePolicy` binds to the `QualityTier` so the perf-budget governor drops from `Taa` to `Smaa` to `Msaa` under frame pressure on the same hysteresis band that degrades the render passes — the resolve ladder a `RenderPass.Composite` policy column, never a parallel post-process engine.
- Unlocks a quality-versus-cost ladder the perf governor drives — a constrained machine renders sub-resolution with `Fsr` upscale and a capable machine renders full-resolution `Taa`, each a policy row the `QualityTier` selects, so a new resolve technique is one `ResolvePass` row and a new quality tier one policy binding, never a second composite path.
- Draws on the temporal-antialiasing motion-vector-reprojection and spatial-super-resolution techniques the 2025-2026 desktop render landscape standardized, the `Silk.NET.WebGPU` compute-pass resolve and the SkiaSharp `SKRuntimeEffect` SKSL resolve below the `RenderTargetFactory` column, and the settled `FrameBudget`/`QualityTier`/`PerfBudget` governor — a fixed single-sample raster with no quality ladder is the gap the resolve owner closes.

## [2]-[CLOSED]

[GPU_BACKEND_PORT]: Realized — `GpuBackend` SmartEnum grows the `GpuFamily` column and the `Wgpu`/`WebGpu` rows beside the `Metal`/`Vulkan`/`OpenGl`/`Software` Ganesh rows, the `RenderTargetFactory` owner binds a backend-provided target factory the `Lease` and every `RenderPass`/`CapturePass`/`CustomVisual` emit body consumes, and the offscreen capsule mirrors the factory; the `Wgpu` row binds the stable .NET Foundation `Silk.NET.WebGPU` wgpu/Dawn surface presenting through the Avalonia 12 `ICompositionGpuInterop` import, the `Software` Skia Ganesh raster row is the shippable floor, and Skia Graphite re-admits as one `SkiaGraphite` row the moment SkiaSharp ships its Recorder/Context surface — no alpha pin carried, the archived VelloSharp/VelloSharp.Avalonia.Vello rows retired with no shim and no Vello identity surviving anywhere in the manifest, README, or `.api/` (`viewport-pipeline.md#RENDER_GRAPH`, `visuals-offscreen.md#DRAW_CAPSULE`).

[SPLAT_REALITY_CAPTURE]: Realized — `realitycapture/reality-capture.md` authored with `SplatSource`/`PointCloudSource` decoded off the Compute splat/point payloads, the `CapturePass` `Splat`/`Point` cases over the backend target factory, the LiDAR-anchored `MeasureOverlay` bound to the `Viewpoint`, and the `CaptureClip` on the animation playhead; the offline LAZ/SOG scan decode is the Python companion's external producer crossing as a Compute payload, so AppUi carries no scan-decode package.

[WEB_VIEWPORT_WIRE]: Realized — the viewport `TS_PROJECTION` deepened into the `ResidencyManifest` single-mint producer of the `WEB_GEOMETRY_RESIDENCY_WIRE` (`GeometryResidencyWire`/`ResidencyTileWire`/`MeshletClusterWire`/`SplatTileWire`) the TS web leg consumes, each tile keyed by its `XxHash128` content key off the same Compute `GeometryPayload` residency; the single-mint invariant is graded at the cross-libs master (`viewport-pipeline.md#TS_PROJECTION`).

[COORDINATION_ISSUE_BOARD]: Realized — `coordination/issue-board.md` authored with `Issue` composing the AppUi `Viewpoint` and the `Rasm.Bim` `BcfTopic`/`BcfComment`/`BcfViewpoint` at the boundary, the `CommentThread` CRDT over the notebook replicated-document law, the `IssueTile` dashboard projection, and `IssueBoard` owning the issue-to-viewpoint binding and the BCF round-trip; the BCF schema stays `Rasm.Bim`-owned, composed never re-minted.

[PERF_BUDGET_GOVERNOR]: Realized — the `PERF_BUDGET` cluster on `evidence/diagnostics-evidence.md` adds the `QualityTier` smart enum, the `PerfSample` telemetry fold, and the `PerfBudget` governor folding the `FrameReceipt`/`ResidencyBudget`/HUD telemetry into one `QualityVerdict` that degrades render passes, the residency watermark, and the motion tokens together under a hysteresis band so the render-hash lanes stay attributable.

[NOTEBOOK_REPRODUCIBILITY_TO_DETERMINISM]: Realized — the `CapabilityPin` composes the AppHost `DeterminismContext`/`EnvFingerprint` and `DeterminismKernel.Reproduces` as its environment identity, the `ReplayManifest`/`NotebookReplay.Verify` composes the `ContentHash` content-addressed chain and the `ReplayVerify` per-step proof through `ProofEngine.Replay`, and the `RecomputePlan` cell DAG aligns to the AppHost `RecomputeNode` content-address node identity — one reproducibility-proof owner across the runtime and the document (`notebook-document.md#CELL_MODEL`, `#REPLAY_BUNDLE`).
