# [PY_GEOMETRY_TASKLOG]

Open and closed work for `geometry`, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` open; `[COMPLETE]` or `[DROPPED]` closed — and carries the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations. `[1]-[OPEN]` holds live work; `[2]-[CLOSED]` records finished or dropped tasks.

## [1]-[OPEN]

[QUEUED] Author the `ifc-analysis/costing.md` 5D/4D lifecycle page — from `IFC_LIFECYCLE_DEPTH`.
- Author the design page owning one `IfcLifecycle` `LifecyclePhase`-discriminated surface: `QUANTITY` (the `ifc5d.qto.edit_qtos` rule-driven take-off over `qto.SCHEMA` writing `IfcElementQuantity` base quantities), `COST` (`ifc5d.cost.calculate_cost` schedule rollup plus `cost.Csv`/`cost.Ods` report export), `SCHEDULE` (the `ifc4d` 4D construction sequence), `PATCH` (the `ifcpatch.execute` named-recipe transformation over the `recipes` namespace), and `DIFF` (the `ifcdiff.IfcDiff(old, new).diff()` revision comparison over `deepdiff`). Each phase folds a `LifecycleReceipt` carrying the phase, the subject GlobalIds, and the structured rows, graduating through the geometry case.
- Integrate `ifc5d` (`qto.edit_qtos`/`qto.SCHEMA`/`cost.calculate_cost`/`cost.Csv`/`cost.Ods`), `ifc4d` (the scheduling entry), `ifcpatch` (`execute`/`write`/the `recipes` namespace), `ifcdiff` (`IfcDiff`/`diff`/`change_register`/`export`), composing `ifcopenshell` and the runtime `RuntimeRail`/`ReceiptContributor`. Catalogues `ifc5d.md`/`ifcpatch.md`/`ifcdiff.md` are folder-`.api`-present; `ifc4d` admits as a manifest row (delta returned to the orchestrator) plus a folder `.api/ifc4d.md` capture step on the companion interpreter.
- The page lands in the `ifc-analysis` sub-domain beside `analysis.md`; the `QUANTITY` quantity arm leaves `analysis.py` for this owner's `ifc5d` kernel, so `analysis.py` retains only Pset/IDS/clash/space-program/BCF; the boundary is the `ifcopenshell.file` model, never a durable store or a managed-interior coupling; the C# `IfcSemanticModel` spatial projection is never re-derived.
- The four ecosystem siblings are `0.8.5` companion-lane distributions over the `ifcopenshell` core; the `ifc5d.qto.SCHEMA` rule-table key shape and the `cost.Reporter.report` arity are the live-run-confirmed members; the `ifc4d` scheduling entry stays a marked RESEARCH item until the `ifc4d.md` catalogue lands.

[QUEUED] Author the `ifc-analysis/selector.md` validated-grammar page — from `VALIDATED_SELECTOR_GRAMMAR`.
- Author the design page owning one `IfcSelector` `lark`-grammar surface: an EBNF grammar over the IFC selection vocabulary (entity class, attribute predicate, pset/property predicate, classification, spatial containment), a `Transformer` fold from the parse `Tree` to a validated structured `SelectorQuery`, and the `UnexpectedInput` parse failure lifted into the runtime fault rail once; the validated query then drives `ifcopenshell.util.selector.filter_elements`. The structured query is reused across the analysis and costing arms' subject derivation.
- Integrate `lark` (`Lark`/`Tree`/`Token`/`Transformer`/`v_args`/`UnexpectedInput`/`parse`/`transform`), composing `ifcopenshell.util.selector` and the runtime `RuntimeRail`/`boundary`. The `lark.md` catalogue is folder-`.api`-present (cp315-clean, no companion gate); `lark` admits as a manifest row (delta returned to the orchestrator).
- The page lands in the `ifc-analysis` sub-domain; the `analysis.py` and `costing.py` selector arms consume the validated query rather than a raw string; the boundary is the query admission — a malformed selector is a typed fault at the boundary, never a silent empty match. No second selection engine: the grammar feeds `filter_elements`, never a parallel filter.
- `lark` is pure-Python cp315-clean, so the page settles on the project venv with no companion gate; the grammar EBNF is owner-authored vocabulary, every terminal tracing to the IFC selection axis.

[QUEUED] Author the `scan-registration/ingestion.md` raw-scan preprocessing page — from `SCAN_INGESTION_PIPELINE`.
- Author the design page owning one `ScanIngestion` `IngestStage`-discriminated pipeline: `pdal`'s `Reader | Filter | Writer` stage graph (`filters.smrf`/`filters.pmf` ground classification, `filters.outlier` statistical/radius removal, `filters.decimation`/`filters.voxeldownsize`, `filters.range` cropping) executed over `Pipeline.execute`/`arrays`, plus `laspy` COPC octree streaming (`CopcReader.query`) and `pye57` E57 structured-scan ingestion (`E57.read_scan_raw`/header pose), all folding the cleaned structured-array points into one registration-ready `o3d.t.geometry.PointCloud` the registration owner consumes.
- Integrate `pdal` (`Pipeline`/`Reader`/`Filter`/`Writer`/`execute`/`arrays`/the `|` pipe composition — full filter-graph exploitation), `laspy` (`CopcReader.open`/`query`/`LasData.xyz`), `pye57` (`E57.read_scan_raw`/`scan_count`), composing the data-branch `data/mesh/exchange#POINTCLOUD` columnar point-record bridge and `open3d`. `pdal`/`pye57` are manifest-present (gated `<'3.15'`); a folder `.api/pdal.md`/`pye57.md` exists (pdal-present, pye57-present), and the `pdal` filter-stage `type` strings are the live-run-confirmed members.
- The page lands in the new `scan-registration/ingestion.md`; `registration.py` consumes the ingestion output rather than a synthetic cloud; the boundary is the data-branch columnar bridge inbound (a `pyarrow.Table` of points, never a pdal pipeline object crossing the seam) and the `o3d.t.geometry.PointCloud` outbound; no IFC parse, no durable store. The deviation/reconstruction chain starts from this owner's output.
- `pdal` rides the `<'3.15'` band (sdist-only, function-local import) and `pye57` rides the `<'3.15'` band; the `pdal` filter `type` vocabulary (`filters.smrf`/`outlier`/`decimation`/`range`) is the owner-local stage table; this resolves the branch-flagged `pye57`-catalogue-present-consumer-absent exploitation gap.

[QUEUED] Author the `scan-registration/reconstruction.md` scan-to-mesh page — from `SCAN_TO_MESH_RECONSTRUCTION`.
- Author the design page owning one `ScanReconstruction` `ReconstructionMethod`-discriminated surface over open3d: `estimate_normals` + `orient_normals_consistent_tangent_plane` as the shared pre-step, then `POISSON` (`TriangleMesh.create_from_point_cloud_poisson`, the watertight default), `BALL_PIVOTING` (`create_from_point_cloud_ball_pivoting`, detail-preserving), and `ALPHA_SHAPE` (`create_from_point_cloud_alpha_shape`, concave-hull), with `cluster_dbscan` segmenting a multi-object scan before per-cluster reconstruction; the reconstructed `TriangleMesh` exports GLB and routes the `mesh-utility` `Repair` arm before graduating as `reconstructed-mesh`.
- Integrate `open3d` (`PointCloud.estimate_normals`/`orient_normals_consistent_tangent_plane`/`cluster_dbscan`/`TriangleMesh.create_from_point_cloud_poisson`/`create_from_point_cloud_ball_pivoting`/`create_from_point_cloud_alpha_shape`/`simplify_quadric_decimation`/`filter_smooth_taubin` — the unexploited reconstruction surface), composing `mesh-utility/repair.md#MESH` `MeshOp.Repair` and the runtime rail. The `open3d.md` catalogue carries every reconstruction member; no new admission.
- The page lands in the new `scan-registration/reconstruction.md`; the registered pose from `registration.md` is the precondition; the reconstructed mesh routes `mesh-utility` for watertight conditioning, never re-implementing repair; this owner is the producer of the `reconstructed-mesh` `GeometrySubject` the compute graduation union already declares but no owner fed. Boundary: no registration (that is the sibling), no mesh repair (that is `mesh-utility`).
- The reconstruction algorithm is a static constructor per the open3d law, not a runtime flag, so `ReconstructionMethod` selects the constructor; the Poisson `depth`/`scale` and ball-pivoting `radii` knobs are owner-local policy fields; `open3d` rides the cp312 companion floor the daemon already hosts.

[QUEUED] Land the `scan-registration/deviation.md` internal design choice and the cross-folder graduation seam — from `SCAN_TO_BIM_DEVIATION`.
- The page is authored and the `open3d` `segment_plane`/`select_by_index` and `trimesh.proximity.closest_point`/`signed_distance` members are folder-`.api`-confirmed (`open3d.md`/`trimesh.md`); the `signed_distance` over/under-build band — the inside-versus-outside design-surface variant distinguishing over-build from under-build before the `compliant` verdict — is an owner-local reduction choice (`deviation#3-CLOSEST_POINT_RETURN`), and the iterative re-segmentation point-count guard is the owner-local heuristic.
- Integrate no new package; `open3d`/`trimesh`/`numpy` are present and consumed.
- Cross-folder: the `scan-deviation` `GeometrySubject` literal admits to `compute/graduation/receipt.md#GRADUATION` before the `DEVIATE` arm's `GraduationReceipt` keys the element-deviation handoff (`deviation#4-GRADUATION_SUBJECT`); the synthesis tier reconciles the single-literal seam against the compute owner.
- The deviation reference is the `tessellation/daemon.md` GLB (never re-tessellated) and the registered pose is the `registration.md`/`ingestion.md` precondition (never re-derived); learned semantic segmentation is out of host-free CPU scope.

[QUEUED] Add the IFC-bytes non-manifold topology constructor — from the `topology/nonmanifold.md` `IFC_BYTES_CONSTRUCTOR` residual.
- Resolve the `Topology.ByIFCFile`/`ByIFCPath` path-only constructor against a bytes-capable IFC constructor or an `ifcopenshell.file`-to-`topologicpy` bridge so the `CONSTRUCT` arm admits IFC alongside B-rep through the same byte-stream admission the daemon uses, never a temp-file detour the byte-stream owner avoids elsewhere; the IFC-bytes arm is one `TopologyOp.CONSTRUCT` growth on the existing owner.
- Integrate `topologicpy` (`Topology.ByIFCFile`/`ByIFCPath` plus the bytes-capable or `ifcopenshell.file` bridge), composing `ifcopenshell.open` over `guess_format` for the in-memory model.
- The arm lands inside `topology/nonmanifold.md`'s `CONSTRUCT` case, not a new owner; the boundary stays the non-manifold cell/aperture analysis the C# `IfcSemanticModel` does not extract, never the in-process BIM space-graph.
- The bytes-capable constructor resolves against the branch `topologicpy` catalogue on the companion interpreter before the IFC-bytes arm settles; until then the IFC ingestion path stays a marked RESEARCH item.

[QUEUED] Hand the geometry CPU kernels to the runtime offload lane — trickle-down from `python:GEOMETRY_KERNEL_OFFLOAD_LANE`.
- Each geometry owner running a heavy CPU kernel (the `registration.py` ICP/global loops, the `tessellation/daemon.py` OCCT iteration, the `ingestion.py` `pdal` filter graph, the `reconstruction.py` open3d reconstruction, the `mesh-utility` `manifold3d` boolean) hands its kernel to the runtime `concurrency/lanes` `anyio.to_interpreter.run_sync` CPU-offload `LanePolicy` variant under the one `CapacityLimiter` and `DrainReceipt`, the lane never importing the kernel.
- Integrate the runtime `concurrency/lanes` `LanePolicy` CPU-offload variant (branch-owned, `anyio.to_interpreter.run_sync` with `to_process.run_sync` fallback); no geometry-side package admission.
- The lane owner stays branch-side; geometry is the consumer that hands its kernels across the boundary, never a second concurrency surface in geometry; the offloaded callable is picklable on the `to_process` fallback path.
- This is the geometry-side realization of the cross-package offload-lane idea; the lane variant lands on the runtime owner first (its branch task), and the geometry owners compose it once it exists.

## [2]-[CLOSED]

[COMPLETE] `step-bridge/cad.md` — the `StepBridge` CAD-STEP tessellation hop. STEP/IGES bytes plus tolerance into per-element GLB over `cadquery-ocp` (`STEPCAFControl_Reader`/`IGESCAFControl_Reader` → XCAF `TDocStd_Document` → `BRepMesh_IncrementalMesh` → native `RWGltf_CafWriter`), returning raw GLB the `tessellation/daemon.md` CAD arms key through the shared `ContentIdentity`. The `pythonocc-core` conda-only blocker is resolved by `cadquery-ocp` (cp310-cp314 wheels, `<'3.15'` companion band — `cad#4-WHEEL_BAND`); the XCAF root-shape enumeration and the `RWGltf_CafWriter.Perform` `fileInfo` map type are the residual `cad#3-RESEARCH` members. Aligned at the wire to the C# `StepIso10303` codec.

[COMPLETE] `scan-registration/registration.md` deepened to the full modern stack. The `RegistrationMode`-discriminated owner carries `GLOBAL` (`kiss_matcher.KISSMatcher.estimate` initialization-free Faster-PFH/ROBIN/GNC), `MULTISCALE` (tensor `multi_scale_icp` + robust kernels), `COLORED_ICP`, `VGICP` (`small_gicp.align`), and `MULTIWAY` (`global_optimization` over `kiss_matcher` pairwise edges). `kiss_matcher` (manifest, `<'3.13'` band) closes the under-utilization where the admitted global matcher was unused; the `kiss_matcher` array-overload dtype and `open3d` `global_optimization` triple are the residual `registration#3-RESEARCH` members.

[COMPLETE] `mesh-utility/repair.md` — the `MeshOp` robust repair/boolean/codec owner. Watertight/winding/normal/hole-fill over `trimesh.repair`, exact CSG over the `trimesh.boolean` `manifold3d` backend (`BooleanOp` row), and mesh-file decode/encode across `trimesh`/`rhino3dm`/`meshio` (`MeshFormat` row), as one `@tagged_union`. Consumed by `tessellation`, `scan-registration` reconstruction, and `step-bridge`; `ifc-analysis` clash-volume composes the `Boolean(meshes, INTERSECTION)` arm. `manifold3d` rides the `<'3.15'` companion band (`repair#4-WHEEL_BAND`); the `trimesh.util.wrap_as_stream` helper and cross-format `Codec` re-encode are the residual `repair#3-RESEARCH` members.

[COMPLETE] `ifc-analysis/analysis.md` IDS/clash/BCF triad realized against the native ecosystem. IDS over `ifctester` (`ids.open`/`Ids.validate`/`Specification.status`, `reporter.Bcf` export), clash over `ifcclash` (`Clasher(ClashSettings())`/`ClashSet`/`ClashSource`/`ClashResult`), BCF round-trip over `bcf-client` (`bcf.v3.bcfxml.BcfXml.create_new`/`add_topic`/`get_topics`/`TopicHandler`). The hand-rolled IDS rule fold and OCCT-overlap clash are the deleted forms; the triad is catalogued in the folder `.api` (`bcf-client` pure-Python wheel, `ifctester`/`ifcclash` companion lane). Residual `analysis#3-RESEARCH`: the `Clasher.clash()` internal `clashes` dict timing and the `TopicHandler.topic.title` field spelling. The `QUANTITY` quantity arm is superseded by `costing.md`'s `ifc5d` rule-driven take-off.
