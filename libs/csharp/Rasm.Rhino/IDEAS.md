# [RASM_RHINO_IDEAS]

`Rasm.Rhino`'s forward pool holds higher-order host-boundary concepts: RhinoCommon document/display/command/exchange capture and native Eto UI composition over the `Rasm` kernel. `[1]-[OPEN]` holds active ideas as cards; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[MULTI_PLUGIN_COEXISTENCE]-[COMPLETE]: every process-global surface now arbitrates on plugin identity — `MountRegistry` seats one binding per point with keyed riders (divergent bindings and duplicate riders refuse typed, the seat frees when the last rider leaves, so an ALC unload hands the seat structurally), `ObjectsTelemetry` and `HostTap` fan or ride keyed, named callbacks claim by token, panels/pages ledger per `(PluginKey, seat)`, `AppSettings` writes through one first-mount writer seat, resolver and latency seats carry plugin attribution, style keys and codec rows claim and retire per plugin; the census on `Document/events.md` `[06]` is the acceptance record, one cardinality-classified row per surface.
[PLUGIN_DOMAIN]-[COMPLETE]: the `Rhino.PlugIns` domain landed whole as `.planning/Plugin/` — `lifecycle.md` (staged `PluginPhase` custody on the one `RasmPlugIn` derivation, ALC unload-flush law, diagnostics ledger), `census.md` (one polymorphic resolve over the id/path hexad, installed census, load protection), `licensing.md` (the full `LicenseUtils`/CloudZoo rail with detached evidence and the decompile-proven Eto `Control` prompt parent), `document.md` (`ReadDocument`/`WriteDocument` over `ArchiveIo` framing, settings-saved facts on the Persistence rail); page-collection callbacks route to `HostUi/pages.md` and file dialogs to `Exchange/formats.md` per the card's own Tension — no second seats minted.
[HOST_SCRIPT_ENGINE]-[COMPLETE]: script-engine capability landed on `HostUi/shell.md` `[06]-[RUNTIME]` — `HostScripts` compile/run custody over the `ScriptRun` request family, `ScriptUnit`/`ScriptOutcome` evidence, engine census as `HostProbe.Scripting`.
[HOST_TAP_EGRESS]-[COMPLETE]: host process-wide exception and cloud-log streams capture as `HostTap` through the `ObjectsTelemetry` egress on `Objects/authoring.md`.
[HOST_HOOK_REGISTRY]-[COMPLETE]: `RhinoPoint`/`MountRegistry` spine with the full point census and failure-symmetric `MountAll` custody landed on `Document/events.md` `[06]-[HOOK_REGISTRY]` — grant custody carries its own name because it is a different concern than the kernel's composition-frozen point mount; every point owns a mount — `DocumentHooks`, `DisplayHooks`, `ConduitHooks`, `ObjectsHooks`, `PanelHooks`, `ShellHooks`, `ContentHooks`.
[HOST_INSTRUMENT_PARTITION]-[COMPLETE]: `RhinoInstrumentPartition.Rows` kind partition landed on `Objects/authoring.md` with the `MarshalLatency` checkpoint ledger and `DurationInstrument` label on `HostUi/shell.md`; `RhinoInstruments` contributed meter rows landed beside it — adjudicated distinct concerns, keyed-sink kind partition versus contributed rows, never folded.
[HOST_BENCH_HARVEST]-[COMPLETE]: `BenchBand`/`BenchEvidence` band landed on `Modeling/solids.md` `[02]-[MODEL_GATE]` with operation-family identity; `Captures.Run` measures and stamps each request case inside its `HostThread.Run` body on `Viewport/capture.md`.
[DOCUMENT_ANALYTICS_CENSUS]-[COMPLETE]: `DocumentCensus` receipt and `Objects.Census` entry landed on `Objects/state.md` `[06]-[ASK_ENTRY]`, composing the canonical `Objects.Ask` snapshot window, `Layers.Ask`, and `BlockGraph.Ask`.
[REDACTED_HOST_EGRESS]-[COMPLETE]: app-root-aligned `HostSensitivity` rows and classified `HostFaultFact`, `HostLogFact`, and cached `HostStaticFact` payloads landed on `Objects/authoring.md` `[02]-[OBJECT_PROGRAM]`; every generated event composes the static fact before the fail-closed redactor seam.
[POST_EFFECT_AUTHORING]-[COMPLETE]: the post-effect authoring half landed on `Display/render.md` `[04]-[POST_AND_TEXTURE]` — `EffectProgram`/`EffectHost` over the host abstract, `EffectPass` with its `ChannelView`, `GpuHandle`, and `CopyDown` ports, `BuiltinEffect` deferred-uuid identity seeding `EffectId`, and `Effects.Register` over both `RegisterPostEffect` overloads; the GPU texture handle homes here, not on `WindowOp`, because `PostEffectChannel.GPU()` is its one managed producer.
[FRAMEBUFFER_ADJUST]-[COMPLETE]: `WindowOp.Adjust(GammaValue, DitherMethod)` landed on `Display/render.md` `[02]-[BATCH_SESSION]` as read-modify-write over `GetAdjust`/`SetAdjust`, the shape `ImageAdjust`'s internal constructor forces.
[HATCH_LINE_REVISION]-[COMPLETE]: incremental hatch-generator revision landed as `LineEdit`/`HatchProgram.Reline` on `Annotation/hatch.md` `[04]-[MUTATION]`, mirroring the linetype `SegmentEdit`/`Resegment` pair over a native `HatchPattern` copy that carries its own name.
[RENDER_EDITOR_SHELL]-[COMPLETE]: render-editor seating landed as `ShellRow`/`RenderShellProgram`/`RenderShell` on `Render/registry.md` `[02]-[FACTORY_REGISTRY]` with the `EditorFacts` bridge read; arming is `Registry.Run` and draining is the plug-in's own one-shot register overrides.
[PBR_NAME_DERIVATION]-[COMPLETE]: the PBR name space landed as `PbrChannel` keyed on `TextureType` with `ContentParameter` on `Render/fields.md` `[04]-[BINDING_AND_PARAMS]`; `ParamScope` now takes a vocabulary, never a literal, and no roster mirrors `ChildSlotNames.PhysicallyBased.FromTextureType`.
[RUN_COMPLETION_NOTICE]-[COMPLETE]: the completion-notice row landed as `RunOutcome`/`NoticeSpec.OfRun` on `HostUi/shell.md` `[08]-[NOTICES]` with `RenderReceipt.Summary` and `CaptureArtifact.Summary` projecting into it, so no render or capture type crosses into the shell.
[RHINO_ACCOUNTS_TOKEN_RAIL]-[COMPLETE]: the boundary-side rail landed whole on `HostUi/shell.md` `[06]-[RUNTIME]` — `TokenAsk` (acquire, acquire-scoped, try-cached) dispatched inside `ExecuteProtectedCodeAsync` with `SecretKey` structurally confined, `TokenLease` holding the live pair with refresh/revoke consuming its own tokens and only detached `OpenIdEvidence`/`OauthEvidence` leaving, `LoginPulse`/`LoginPhase` progress facts, and `HostProbe.Entitlement` answering `CloudHostUtils` headless; the live-provider-in-headless question died with `[INPROCESS_HEADLESS_BOOT]` — the interactive host is the only boot the platform permits.
[COMPUTE_ENDPOINT_ROWS]-[COMPLETE]: landed on `HostUi/shell.md` `[06]` — `ComputeEndpoints.Register` binds the decompile-proven `(endpointPath, Type)` append-only roster under stated process-lifetime custody (no unregister exists), `HostProbe.Compute` folds `GetCustomComputeEndpoints()` into the capability census, and entitlement rides `HostProbe.Entitlement`; the compute-server boot itself is app-stratum and out of this boundary by ruling.
[INPROCESS_HEADLESS_BOOT]-[DROPPED]: macOS WIP structurally refuses in-process boot — `[HEADLESS_BOOT_PROBE]`'s live thread-sample proves the ctor's `Interop.LaunchInProcess` path deadlocks in a modal license alert (`CRhinoLicenseManager::Initialize` → `RhinoMessageBox` → `NSAlert runModal`) no headless process can answer, and `WindowStyle` is ignored on the macOS arm, so no boot-shape variation escapes it; re-litigation opens only on a WIP build whose license initialization stops raising modal UI under `LaunchInProcess`.
[VECTOR_PRIMITIVE_STREAM]-[DROPPED]: `ViewCaptureWriter` stays outside the `Viewport/capture.md` delivery vocabulary — `Draw` takes the native print-info pointer, `ViewCaptureSettings.ConstPointer()` is internal, and no public member accepts the writer, so the row publishes capability nothing drives; the debt rides `Viewport/capture.md` `[06]-[RESEARCH]` `[VECTOR_WRITER]`.
