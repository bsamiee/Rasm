# [CLOSER_DISPATCH]

Closer prompt for routed-row closure after all fix lanes land; closers run concurrently over disjoint territories. Sizing law: opus closes small single-file mechanical rows — consumer re-spells, one-case additions; ONE fable with fuller arming closes family, design, or capability work — multi-file moves, owner mints, miner-roster judgment.

- [CORPUS]: `<working-root>` is the territory and `<work-product>` the deliverable; never any git command in any form.
- [ARMING]: sized to weight — a small closer reads the 2-3 `docs/stacks/<language>/` pages owning the row's concern plus each target page in full before its edit; a fable closer reads the full stack root set plus the ARCHITECTURE strata map when rows cross folders.
- [DEMAND]: each routed row's COMPLETE text is the contract — inlined, or a pointer to the `<round-dir>/lane-*-report.json` `.routing[]` arrays with the instruction to grep them per target file before editing; a paraphrase drops constraints.
- [STANCE]: per row — rail-verify every named member first (`results[0].text` matches the requested FQN exactly); judge the demand against the target page's own charter; land or refute — a row the charter refuses returns `refuted` WITH the citation, never forced value; fix at the root and exceed where the owner admits; extensions land on existing owners as rows, cases, or modalities, never parallel surfaces; zero functionality loss.
- [FRESHNESS]: re-read every target from current disk before its edit — concurrent agents land in parallel; a row disk already carries reports `already-landed` with the evidence, never re-done.
- [VERIFICATION]: prose gate once, batched over touched pages; no builds or tests — proof follows the work product the corpus block declares.
- [RETURN]: JSON only — `{"rows": [{"id": "<id-or-file>", "verdict": "landed"|"refuted"|"already-landed"|"blocked", "note": ""}], "files": ["<path>"]}` — one row per routed demand, refutations carrying their citation.
