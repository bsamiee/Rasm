# [TOPOLOGY]

Coupling map: editing a surface obligates its counterparts in the same change. Heavy workflow runs re-prove every coupling against the live tree — a coupling that no longer exists is culled in the same pass, and a coupling discovered mid-run lands as a new one. A self-contained coupling rides the table; a coupling whose obligation fans out or carries a proof condition is a card below.

## [01]-[COUPLINGS]

Reviewer configs are `.coderabbit.yaml`, `.greptile/`, and `.macroscope/`; each surface below obligates its counterpart in the same change.

| [INDEX] | [SURFACE]                             | [OBLIGATES]                           | [WHY]                                 |
| :-----: | :------------------------------------ | :------------------------------------ | :------------------------------------ |
|  [01]   | `docs/stacks/<language>/` page        | every reviewer config                 | reviewer prose derives from doctrine  |
|  [02]   | `libs/.planning/architecture.md` edge | `tests/csharp/_architecture` specs    | strata law lands at both ends         |
|  [03]   | repo root file census                 | `test_policy.py` allowlist            | root residency is a closed allowlist  |
|  [04]   | `.claude/skills/` master trees        | sibling-repo skill copies             | propagation is byte-identical copy    |
|  [05]   | `CLAUDE.md` fact                      | `AGENTS.md` cross-reference           | one fact serves its acting reader     |
|  [06]   | `.config/dotnet-tools.json` tool row  | `tools/assay` decompile rails         | manifest tools are assay runtime deps |
|  [07]   | workflow codex-lane dispatch shape    | `workflow-creator` and `codex` skills | canon lives in the owning skill       |

## [02]-[CONDITIONED]

A coupling whose obligation fans out or carries a proof condition states that condition as law.

[MANIFEST_ADMISSION]:
- Surface: a central manifest dependency row — `Directory.Packages.props`, `pyproject.toml`, or `pnpm-workspace.yaml`.
- Obligates: the folder `README.md` and the `.api/` catalog tiers, because dependency admission is a multi-surface fact.
- Law: a `Directory.Packages.props` row also obligates the consuming `.csproj`.

[CATALOG_MEMBER]:
- Surface: a folder `.api/` catalog member.
- Obligates: every planning fence citing the member, because a fence proves spelling, accessibility, and arity.
- Law: the coupling binds both directions — a catalog consumer or stacking anchor names only landed planning-page anchors, and the pass that lands or moves the consumer repairs the catalog row; a catalog anchor naming an unlanded consumer is aspiration wearing verification's clothes.
- Law: a claimed absence — a missing member, parameter, or capability — proves only by a failed live resolve against the installed distribution, catalog silence never sufficing, and a member that verifies lands as a catalog row in the same pass.
- Law: a catalog row contradicting a doctrine page is unverified until live reflection decides, and the losing surface repairs in the same change.

[FENCE_SEAM]:
- Surface: a planning-fence seam member.
- Obligates: every foreign fence composing the member, because no compiler exists and the sweep is the build.
- Law: a composing fence binds the seam member's current landed declaration — spelling, accessibility, arity, rail shape, and owning namespace with its prelude `using` — never name presence, so a member edit on any of those axes sweeps every composing fence repo-wide in the same change.
- Law: a recorded behavioral trap — a fault on the estate's canonical input type, a lane or modality restriction — binds every composing fence to a structural close, never a prose acknowledgment.
- Law: a code-generated binding's row records the binding's own field types, optionality and collection cardinality as generated, never the source schema's.
