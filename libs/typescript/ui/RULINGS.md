# [TS_UI_RULINGS]

`typescript/ui` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `react`/`react-dom` HOLD on the canary channel — handler bodies ride `useEffectEvent` and the transition ladder's top tier rides `<ViewTransition>`/`addTransitionType`, canary-only members a stable-channel bump silently drops; the channel choice is the admission ruling, not a stale pin, and it reopens only when every composed canary member lands stable.

## [02]-[SHAPE]

- Semantic tone homes at the token color authority as one closed vocabulary, not on the surfaces — the surface-local unions are drift, never decision, so every `_tone` table keys the declared set and a per-surface tone member is the fork re-entering.
- Contract vocabularies materialize field-for-field — a clamp, remap, or local default forks the emitting peer's semantics into cross-language drift.
- Viewer animation reads one time authority — the rAF-fed atom clock the spatial surfaces assert; a second clock mint beside it un-reconciles construction-sequence scrub, so mixer deltas derive from the same coordinate, never a parallel timer.
- This package measures no Core Web Vital and mints no rating table — `runtime:otel/vital` owns the capture, the shipped cutoff pairs, and both instruments, so this floor windows raw entries and republishes graded rows onto its replay point through the app bridge; a capture function registered here double-observes the five metrics, a local cutoff table forks the standard's grades, and `web-vitals` stays admitted for the performance globals its types build augments alone.
- Windowing a family the telemetry owner's registrars also read stays admitted — the browser serves every registered observer from one buffer, so a second reader costs a callback and forks no accounting while this floor grades nothing and holds no instrument; the coupled value is the `event-timing` floor, arriving as the same policy number `runtime:otel/vital` hands `web-vitals`.
- Every window bound and capture floor arrives as policy, never as a module constant — one `Vital.Policy` row the composing root supplies carries the sample cap and the interaction floor, because a constant window is a compile-time assumption about a consumer this package never meets and a floor literal forks the estate value its telemetry peer parameterizes.
- Environment read policy applies at the read — intensity and rotation land on the scene's environment fields under the stored-frame law, so re-exposing or re-orienting a dome re-keys no blob and reruns no prefilter; baking either into decoded planes or prefilter output forks every consumer reading the same digest.
- Repeating a held environment key is that policy law's own arrival, never a no-op — re-exposing and re-orienting are exactly the edits the stored frame leaves keyless, so the repeat re-commits its read policy over the carried handles while decoding, prefiltering, and retiring nothing; dropping it strands the new policy with no digest change left to carry it, and re-decoding it spends the prefilter pass the stored frame exists to save.
- One transcoding texture loader instance serves a viewport — such a loader owns a worker pool and refuses to parse until its support probe fills the worker config, so the instance the glTF pipeline binds is the instance every standalone decode of that container rides; a second instance either doubles the transcoder wasm fetch and the worker set or throws on its first file, and publishing the instance beside the loader that consumes it is what keeps the reuse spellable.
- Byte ports whose consumer hands `.buffer` to a platform decoder declare the non-shared generic — `Uint8Array<ArrayBuffer>` is the whole-buffer contract made structural, while the bare spelling widens the backing store to `ArrayBufferLike` and every such decoder refuses it; a narrowing guard at the call site re-proves per site what one port declaration proves once.
- Prefiltered domes bind to their renderer while their decoded sources do not — backend re-init re-derives the target from the held source through the fresh prefilter, and the source disposes only on replacement or scope close; a prefilter target surviving renderer identity is the leak.
- Environment consumption is producer-agnostic — container magic selects the decoder, never a filename, manifest producer, or branch identity, so the C#-baked and python-assembled set documents serve one fold and a producer-keyed decode arm has no place to exist.
- Wire irradiance harmonics answer as a CPU read, never as a second scene light — a prefiltered dome already carries the diffuse term, so seating the same coefficients on a probe beside it doubles the irradiance; the analytic probe row seats the pre-dome floor alone and the harmonics answer directional queries off the GPU path.
- Harmonic bands transcribe verbatim and the BASIS moves at the read — the producer's `+Z`-up query normal derives by un-rotating the consumer's own dome orientation, so a permuted, negated, or re-projected band set is the axis fork, and the producer's single-band pole fixture is the landing proof.

## [03]-[COLLAPSE]

- (none)

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- (none)
