# [RASM_BIM_RULINGS]

`Rasm.Bim` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- `GeometryGymIFC_Core` sole-IFC-model-surface KEEP — do NOT consolidate the IFC model surface onto the admitted xBIM leaves: none carries the full IFC4.3 entity vocabulary with schema-versioned STEP/ifcXML/ifcJSON write the egress re-author rides, so consolidation trades the one `DatabaseIfc` authority for a capability hole the leaf packages cannot fill; reopens only on an xBIM release owning that full write surface.

## [02]-[SHAPE]

- Ingested `IfcClass`/`PredefinedType` tokens admit BARE at `SemanticProjector` ingress, validity deferring to the `Emit` egress gate [PREDEFINED_TOKEN_RULING] — ingress validation aborts a whole import on one unknown entity and forks the token vocabulary between ingress and egress.
- `UnitScale.Coerce`/`Declare` are the folder's ONE native-to-SI transform pair and no call site multiplies a bare factor — a `Factor`-shaped member returning a raw multiplier re-opens the eleven-site fan the pair closed.
- A measure's coercion axis is a `MeasureDimensions` ROW COLUMN, never a measure-type-name compare — the angle rows sign `Dimensionless` and no exponent vector can carry their factor.
- Egress quantity election keys on the seam `QuantityType` the ingress stamped, never on `Dimension` — four vocabulary rows share the zero vector, so a dimension key cannot separate an integral tally from a real one.
- Every derived base quantity admits through `OfSi(QuantityType, Dimension, double)` carrying its QTO identity — a dimension-anonymous derived takeoff silently strips the type off whatever occurrence row derived-wins supersedes.
- Native-lane governance grain is declared, never assumed — where the bound native surface publishes no interrupt, the token gates managed boundaries and the abort claim states that grain; an unqualified cancellable claim over an uninterruptible native call is the overclaim.
- Every `IfcRel*` name, directionality, and inverse-attribute pair lives on `IfcRelKind` rows lowering onto the neutral `Relationship` edge, the typed case carrying only `SubKind` and `Generic` alone carrying wire-name and attribute bag [NEUTRAL_EDGE_RULING] — a typed `IfcRel*` seam case leaks GeometryGym below the seam and forks the neutral edge algebra.
- Content keys ride ONE kernel seed-zero hasher across every federation, solver, cache, and diff edge — a per-page hash, a second scheme, or a `Guid`-keyed join forks the content space Compute's content-addressing lane shares, and a downward `InterchangeIdentity` reference from Bim inverts the strata.
- Texture bindings ride BESIDE the neutral appearance summary and never inside it — `StyledAppearance` pairs the content-keyed `Node.Appearance` with its `SurfaceTexture` roster, so an IFC texture style survives ingest whole while the frozen seven-value `AppearanceSummary` preimage stays sealed; folding a map's mean into a scalar channel is the averaged-map defect and widening the summary forks the dedup key this projector mints against, so a FIELD-valued presentation read grows a roster beside the summary and a SCALAR read grows an element-select arm onto it. Re-litigation opens only if the seam retires the frozen preimage.
- This branch CLASSIFIES and CARRIES texture payloads and decodes none — `TextureMode` resolves an IFC mode token to its canonical channel and its gloss/transparency polarity, `SurfaceTexture` carries the URL, blob, or pixel-extent payload, and the glTF rail binds bytes a texture owner already sealed through `TextureBuilder.PrimaryImage`; an image codec, a texel resample, or the polarity inversion executed here mints a second raster owner inside a host-neutral exchange leaf and hides the divergence behind a channel that still renders. Re-litigation opens only if a raster owner seats inside this package.
- Every glTF extension a payload obliges registers from the PAYLOAD, unioned with the policy roster — a bound KTX2, WebP, DDS, or transform-bearing map names its own `KhrExtension` row, so a caller who omitted the row from `InterchangePolicy.Extensions` still emits a registered extension rather than an unresolvable block; trusting the policy roster alone is the recurring move, and it fails silently because the writer emits the block either way. Re-litigation opens only if SharpGLTF registers every in-box extension unconditionally.
- Canonical channel names are the only vocabulary crossing this package's texture surfaces, and every foreign channel space reaches them through one owning roster — `TextureMode` resolves an IFC mode token onto the canonical name and `GltfChannel` resolves that name onto its glTF `KnownChannel` targets, so a call site choosing a `KnownChannel` is the unowned correspondence both rosters exist to delete and a canonical name no row claims REFUSES at admission rather than lighting a nearest slot that still renders. One canonical name may target SEVERAL glTF channels — the `orm` pack is one image glTF reads through both an occlusion and a metallic-roughness reference — so the row carries a target list and a per-channel copy of identical bytes is the deleted form. Re-litigation opens only if the canonical channel roster stops being closed.
- Seam `AppearanceSummary` is the WHOLE factor source of an exported glTF material and each of its channels stays in its declared domain — the scene-linear base colour enters `baseColorFactor` unencoded while the display-referred `0xRRGGBBAA` column is its egress projection through the one sRGB OETF this package owns, metalness and roughness are written on EVERY material because the glTF factor defaults are both unity and an unwritten material renders as rough metal, sub-unit opacity alone selects `AlphaMode.BLEND`, and the `Transmissive` bit writes `KHR_materials_transmission` and never alpha mode. Recurring moves pack the summary into a byte tint and feed that tint to the linear factor, or read transmission off a sub-unit alpha — each silently re-renders every exported element. Re-litigation opens only if the seam retires the neutral summary.
- Rostering a glTF extension a producer cannot FILL mints the deleted phantom — a policy preset declares write capability, the payload's own obliged rows are the truth, and the two union at registration, so a row naming a factor the seam summary drops or a scene object this rail never authors governs nothing while reading as support. Every row lands WITH its arm and returns the moment a finish column or a scene arm carries its value. Re-litigation opens only if a preset stops being a capability declaration.
- Kernel `Rasm.Numerics` `CellLattice` is the ONE raster placement and kernel `Rasm.Drawing` `ChannelDtype` the ONE sample-storage roster, so `Semantics/geospatial`'s GDAL raster boundary composes the kernel's host-typed affine primitives to build that placement — spelled qualified and confined to the lattice mint so the crossing stays countable. That section's host-neutrality law still bans host GEOMETRY (`Brep`, `Mesh`, `Curve`) whole; the discriminant is that a lattice IS the seam's admitted placement with no host-free mint, where a `Brep` never is. Package-local six-coefficient descriptors, a degeneracy re-check beside `CellLattice.Of`'s own admission, and a sample-type enum beside `ChannelDtype` each re-open a concept three owners already settled. Re-litigation opens only if the kernel publishes a host-free affine mint.
- `GeoClassifier`'s key carries geometry kind as an OPTIONAL axis and every landed mapping binds `Any`, because a class follows its TAG and not the shape the source drew it as — a mandatory kind axis multiplied every mapping by the kinds it ignored and doubled the CityJSON taxonomy on ingest. Rows spelling `Some(kind)` win the ladder wherever the kind genuinely discriminates, so the axis survives for the case that needs it; a per-kind copy of a kind-agnostic mapping is the deleted form.
- Envelope derivation is SHARED and single across the energy arms — `Energy/derive`'s honeybee envelope and dragonfly massing plate read ONE `Envelope` fold, and a Room2D floor-boundary SEGMENT joins to a space's vertical bounding SURFACE in plan, so the two emitted documents cannot disagree about which wall carries which boundary condition or which window. Per-arm boundary walks are the deleted form that let one graph emit two contradicting buildings. Ring AREA rides the Newell fold over the seam `Vector3` algebra, never the planar NTS algebra, because a vertical aperture ring projects to near-zero area there.
- An upstream payload PRESENT on the parsed graph yet sealed against every public read or mint binds through ONE `UnsafeAccessor` capsule at the owning boundary page, pinned to the central manifest's package version [SEALED_PAYLOAD_RULING] — the compiler resolves each field and constructor by name and signature, so the path carries no reflection and stays trim- and AOT-safe, and a release that moves a member breaks LOUDLY at the first call instead of degrading to a render no consumer can distinguish from correct; the capsule is the SINGLE seam, projecting detached values rather than the live collection field of a live entity, so a second copy of any accessor anywhere forks the version pin and a bump re-probes every binding in one place. A STEP re-parse beside the model surface and a vendored fork are the two recurring moves and both mint a second reader or writer inside the package that owns exactly one; a host-coupled constructor that is an entity's ONLY public author is confined to that same capsule as a construction argument, never a type crossing a projector signature. Re-litigation opens only if the upstream publishes the payload.
- Model identity is SPAN-grade, never a metric dimension [MODEL_SLOT_RULING] — models mint unbounded, so `rasm.bim.model` multiplies every instrument by the live model count while a sampler-thinned span carries it free; the slot carries the package namespace a sibling also needs, `BimTelemetry.Traced` stamps it from its OWN required argument — a slot left to caller discipline is the slot no caller stamps — and "just the active model" re-mints that cardinality behind a bounded-sounding qualifier.
- A companion port declares the governance its transport can carry and nothing else — `TessellationCompanion.Cross` takes the `CancellationToken` the two-hop rpc threads and NO `IProgress<double>`, because the tessellating daemon already beats its in-flight fraction onto its own observability tap while the rpc publishes no progress channel in either direction. A sink the implementation can only ignore reads complete to a scan while reporting nothing, and threading one forks a fraction that already publishes elsewhere into a second channel with a worse drop law. A managed progress lane lands as a transport row FIRST and reaches a port contract second. Reopens only on the transport publishing a fraction row.
- The sRGB transfer is READ off `RgbProfile.Srgb`, never spelled — `Configuration.Rgb.ToLinear`/`FromLinear` are the estate's one IEC 61966-2-1 curve and a local piecewise body beside them forks it. Discriminant: the hand-rolled copy additionally clamped to [0,1], flattening the negative-input reflection the row's own transfer models. The pair stays TOTAL and per-channel because the unit-range gate belongs to the seam factory, so a second admission at the transfer would admit one fact in two stages. Reading a transfer delegate off a kernel row is not a host crossing: no `Unicolour` type is spelled and none reaches a signature.

## [03]-[COLLAPSE]

- (none)

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- (none)
