id: no-event-layer-in-policy
valid:
    - |
      const next = row.rewrite?.(state.e, facts); return next === undefined ? state : next;
    - |
      import { decode } from '../host/store.ts';
    - |
      const replaced = e.yaml.replace(_YAML_TYPESCRIPT, '$<key>tsx');
    - |
      const text = `$ ${cost}`;
    - |
      const record = (io: Io, row: Scan): Promise<void> => io.set(key('scan'), row);
    - |
      export { decode } from '../host/store.ts';
invalid:
    - |
      const record = ($: Engine, row: Scan): Promise<void> => io.set(key('scan'), row);
    - |
      const pass = (e: ToolCallInput, next: Next) => next(e);
    - |
      import { toolCall } from '../events/tool-call.ts';
    - |
      export { toolCall } from './events/tool-call.ts';
    - |
      const io = { $, now };
    - |
      const { $ } = engine;
