# [SNIPPET_CREATION]

- `<TARGET>` =
- `<DOCTRINE>` =
- `<WORKSPACE>` =

`<TARGET>` exists with its cards complete and no fenced code. This work selects the cards that earn snippets and authors the snippet set through independent attempts and adjudicated synthesis — code that exemplifies world-class implementation of its card and stands as the pattern the codebase follows.

[GROUNDING]:
- Every agent — the orchestrator and every attempt agent — fully reads `<DOCTRINE>` before any other work: the laws, the collapse scan, the page craft, the snippet law, and the placeholder discipline. Every line of code answers to them.
- Every agent reads all of `<TARGET>` — every section, every card — every finalized sibling page whole, and every snippet in the sibling pages, before judging or writing anything. Where code touches a concern a finalized page legislates, it is built at that page's standard, with that surface as supporting material — the spotlight region stays this card's own.

[SELECT]:
- The orchestrator selects the cards that earn snippets: at least four, never every card — only where an advanced mechanism needs proof beyond prose, and only where the card already stands without it. A snippet deepens a card; it never rescues one.
- Assign each selected card a distinct surface region that no snippet in the page or its sibling pages demonstrates. The region map is fixed before any code is written and is the duplication guarantee. The region is the snippet's spotlight demonstration; finalized surfaces composed as supporting material occupy no region.

[ATTEMPTS]:
- The orchestrator drafts its own full snippet set first and grades each snippet with pros and cons — the baseline and the lessons learned.
- Dispatch ten independent agents with the same brief, in isolation: the grounding reads, the full `<TARGET>` read, and the region map, with no sight of any other attempt. Each authors the full set and returns it with self-grades.
- Snippets are authored from the card, the doctrine, verified library truth in `<WORKSPACE>`, the reservoirs of finalized pages where the orchestrator supplies them, and installed library source — never from repository source code, and never by inventing an API member.

[SYNTHESIZE]:
- The orchestrator grades every attempt per card with pros and cons, identifies the strongest approach per card and the elements worth taking from the others, and composes the final snippet per card from that strength — synthesis, not selection alone.
- Integrate each final snippet beside its card with the fence language and intent label the sibling pages use, then re-read the page cold and grade the set. The completed page, fences included, stays inside the corpus page ceiling — at or under 350 lines, landing near 300 when integration is clean; the ceiling is a hard stop, not a target.

[SNIPPET_LAW]:
- A snippet fully captures its card's content before any additional sophistication; implicit advancement — stacked library capability, composed standards, advanced operators — comes after coverage, never instead of it.
- Every snippet compiles as written under the active surface — grammatical, type-correct, and complete.
- 15-25 lines per fence, and never fill the budget without cause: every line is load-bearing, with no decoration, no scaffolding, and no theater.
- Legal neutral identifiers; placeholder strings only inside literals; no project, host, or domain concept anchors a snippet.
- The code states the law by showing it; no commentary beside a snippet narrates what it demonstrates.

[GRADE]:
- Score each snippet one to ten on five axes — card adherence, sophistication, density, placeholder discipline, footprint — against a bar of 9.2 on each. Grading is critical, harsh, but fair — not a 9.2 out of convenience, not low for nit-picking. Two automatic fails override any score: an invented or unverified API member, and a demonstration any other snippet in the page or its siblings already shows. Rework until every snippet clears.

[COMMIT]: stage and commit `<TARGET>`.
