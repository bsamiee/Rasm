---
description: Rasm typed quality operator — static, test, bridge, and API rails under tools/quality
globs: "tools/quality/**/*.py"
alwaysApply: false
---

# Rasm Quality Operator

Read `.claude/skills/coding-python/SKILL.md` before editing `tools/quality/`.

## Canonical entry

```bash
uv run python -m tools.quality <rail> <verb> [args…]
```

| [INDEX] | [RAIL]          |
| :-----: | --------------- |
|   [1]   | Static C#       |
|   [2]   | Unit + mutation |
|   [3]   | Rhino bridge    |

[COMMAND]
- [1] `quality static check` / `quality static full`
- [2] `quality test run` / `list` / `coverage`
- [3] `quality bridge …` / `quality api …`

`pnpm test:cs` and `pnpm verify:rhino` in `package.json` wrap the test and verify rails.

## Conventions

- Keep orthogonal rails separate — static, unit/mutation, and Rhino runtime never merge.
- Per-rail MSBuild cache: `.artifacts/quality/<rail>/` via `ArtifactScope`; static keeps build servers and node reuse enabled. No flock, stamp files, or global coordinators.
- Default `quality test run` (managed `Rasm` target) runs MTP unit tests only. Stryker is explicit through `--mutation changed|full` and fail-fast on the mutation lock.
- Bridge verify/check/package and live Rhino remain single-flight. See `tools/rhino-bridge/AGENTS.md`.

## Validation

See `CLAUDE.md` §5.2 for quality gates. Operator self-test: `uv run python -m tools.quality self-test`.
