# [APPUI_TASKLOG]

Open and closed work for the product UI engine, distilled from `IDEAS.md`; live-host probes and tool-gated arms track as blocked tasks against the substrate that unblocks them.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open task card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
- Atomic: <present only on a minor-scope task; names the small unit so a later session sizes its turn>.
Capability, Shape, Unlocks, and Anchors are required on every open card, Atomic included; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Tasks state landing-grain work decomposing an idea.
-->

[CHART_SOLAR_SEAM]-[QUEUED]: the kernel solar crossing into the chart planes carries a registered seam edge like every other kernel shape.
- Capability: the seam registry stays a complete census — a kernel shape a plane spells at its own fences owns an edge naming that plane.
- Shape: `ARCHITECTURE.md` `[03]-[SEAMS]` first fence — one `[SHAPE]` edge from the kernel to `Charts`, joining the collapse group already there.
- Unlocks: a reader tracing solar geometry reaches the consuming signature from the fence alone, without re-censusing the package for spellings.
- Anchors: `Charts/climate.md` spells `SunPosition` in its packages row and its `Sun` fold; `Render` and `Analysis` already carry their own edges.
- Atomic: one edge and one pool renumber; the surrounding fence needs no re-derivation.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[LIGHT_RIG_SUN_ARM]-[COMPLETE]: the environment arm consumes the widened seam whole at `Render/pathtrace#LIGHT_RIG` — the Law roster answers `Radiance` composing the diffuse field with the disc's limb read at the direction's own radius, `Sample` as the `EnvironmentSample` `[Union]` whose `Dome`/`Sun` arms share base `Direction`/`Radiance`/`Pdf` with the Sun arm carrying its `DiscRadius`, ONE sun-selection/guided-dome combined balance density, and `Sun : Option<SolarDisc>` filled by the `Of(…, Option<SkySource>, Op)` mint at the composition root — so the NEE candidate arm and the miss fold read the union through its base columns as ONE estimator class (an arm switch would be two spellings of one fold) and a camera ray through the sun draws the real disc with correct MIS. The no-double-count split crossed as a structural gate: `LightRig.Studio` now answers `Fin` and REFUSES a `LightSource.Sun` lamp beside a sun-bearing dome, with `SunAt` and the `SunStudy` sweep stated as the dome-less rig's arm, because the doubled beam hides inside a single render. Three stale spellings fell with the landing — the `using SolarPosition` alias whose Materials same-name collision the `SolarFrame` rename dissolved, the fence comment naming that adapter `SolarPosition.Of`, and the index row crediting the almanac to Compute instead of the kernel.
[CONFLICT_DECK_FAMILY]-[COMPLETE]: the arming hoist and the deck family landed as one pass. `ConflictIntent` hoisted the eight resolution keys onto a non-generic `[SmartEnum]` at `Editing/inspector#CONFLICT_RESOLUTION` — each row carrying its grain (`PerHunk` deriving the admitted payload domain), its `Option<ConflictSide>`, and its default chord, `ForHunk` the gutter's side-to-key read, `Project` seeding `ResolutionIntents` from `Keys` — and `Shell/commands#INTENT_TABLE` gained the generated `DeckRows.Conflict` family (one Dialog-scoped row per roster row, the first claimant of the `Dialog` rung whose attach owner `Shell/input` already names), the `DeckArrows.ConflictResolve` column, family-table row `[25]`, and the Boundary clause seating `ConflictIntent` beside `SessionAction`/`BakeVerb` in the generates-here law. A resolution chord, a gutter press lowered through `ForHunk` per the lifting-arrow ruling, and a replayed journal entry over the one `Invoke` route now raise ONE frozen row; a ninth resolution verb is one `ConflictIntent` row with zero deck edit; the reachability law landed as a folder `RULINGS` `[02]-[SHAPE]` row.
[REDLINE_RASTER_ARM]-[COMPLETE]: the raster leg landed at `Collab/issues#REDLINE_TOOLS` as a `MarkupLeg` axis whose two rows carry their own projection, elected by a `Leg` column on `RedlineTool` — so the exchange decision sits where the Tension said it must, on the tool row, and `StrokeCapture.ToMarkup` collapsed to one dispatch that never learns what a raster is. Shape and text elect the raster leg because a dragged box is an EDGE and a callout IS its glyphs, neither of which a polyline can carry; the pen, highlighter, and eraser keep the line leg. The raster projection composes settled owners and mints none: `DrawSource.Owned` the one Skia surface, the frozen `PaintCatalog` paint read under the tool's own key (making a `PaintSpec` row per tool a growth obligation), `ShapingSurface` under `RenderPosture.Paged` for the caption, and `VisualCodec.Encode` for the payload, its blob key doubling as the `BcfBitmap.Reference`. Placement is derived rather than supplied — origin and height unproject off the mark's own screen box while normal and up read `CameraFrame`, so a corner that hits nothing REFUSES rather than pinning the mark to an arbitrary depth. Two gaps closed with it: `RedlineTool.Captioned` plus `RedlineToolState.Caption` give the text tool the words it never carried (an uncaptioned callout now refuses at capture rather than rastering an empty box), and `ViewpointMarkup.Bitmap` carries the `MediaSurface` row that HOLDS its payload beside the record that references it, so a bitmap markup naming a reference nothing resolves is unrepresentable. `StepAnnotations.Commit` at `Collab/tour.md` moved onto the `IO<Fin<…>>` rail the leg requires.
[RADIO_SELECTION_SLOT]-[COMPLETE]: the exclusive choice landed at `Shell/controls#MATERIALIZE_FOLD` on a SELECTING host — a `ListBox` in single-selection mode whose per-option containers carry the resolved label and their own `Value` on `Tag`, with `SelectedValueBinding` resolving the two-way binding against that `Tag` exactly as the closed select posture already does, so a radio, a drop-down, and a segmented rail all round-trip the option VALUE and never the container. The Tension resolved as its own text directed: exclusivity has ONE owner and it is the host's selection, so the group-name flag stops existing rather than surviving as a fallback that could disagree with it. The item theme is RESOLVED and refused on rather than iterated past, because a segment that loses its skin is still visibly a segment while a radio row that loses its container theme is a plain list row with no exclusive affordance at all. Three touch-points closed with it — the `ControlSkin.RadioItem` address, the `ListBox` arm on the per-type value-slot table, and the `[32]` radio-item row at `Theme/tokens#CONTROL_THEMES` — and the Arms' catalog debt landed as the `ListBox`/`ListBoxItem` roster row, the `SelectionModeProperty` slot row, and the `SelectionMode` arity note at `.api/api-avalonia.md`.
[MARKDOWN_DARK_ARMS]-[COMPLETE]: every markdown arm materializes at `Document/media#MARKDOWN_BLOCKS`, over a `MarkdownStyling` widened from one ink into a resolved `MarkdownSkin` carrying surface, border, code-surface, quote-bar, rule, and per-kind callout tint slots — the callout tints deriving from a `CalloutRow` roster keyed on the status paint ladder at two rungs, so a wash and its edge track a seed change together and a sixth alert kind is one row. The fence is a REGISTRY LOOKUP through `RasmRegistry.Scope` with a stated refusal on a still-readable mono block, so the three-scope closure that made every foreign language unhighlightable is gone and an absent grammar names itself. Retained grids project onto real `TableColumnRow` values with a `SpanVerdict` counting the merged cells the grid backend cannot span, so a flattened table reads as flattened. Media links mint `MediaSurface` rows through one `MediaCodecRow` extension table whose vector row refuses an unadmitted asset rather than opening a second SVG store. `Heading.Anchor` is consumed as a `MarkdownAnchor` outline whose navigation rides the settled `SearchOpen.ProsePane` request, so the anchors that were retained-then-dropped now feed both the outline and the deep link with no second link grammar minted.
[TRANSPORT_CHROME]-[COMPLETE]: the transport landed across `Document/media#PLAYBACK_TRANSPORT` and `#TRANSPORT_CHROME` on a CONSUMED grammar — the nine-row `Render/animation` `TransportVerb` roster rides the `MediaCommand.Grammar` arm with its `transport.*` intent keys untouched, and only the payload-bearing media-local commands (seek, volume, mute, lane, sidecar, section, scrub, grab, playlist) live here, so a clip and a sequence cannot spell one verb two ways. Observation rebuilt EVENT-DRIVEN off each typed wrapper's own `Changed` event, which self-registers `ObserveProperty` under the wrapper's property name and format and unregisters with the subscription — killing both the six raw property strings and every fabricated reading, since each payload carries a genuine nullable. The bar renders scrub with the demuxer's own cache time as the buffered extent, locale-formatted timecode through `ResolvedLocale.Span`, the frame-step pair, the speed menu on the shared ladder, loop-region handles with their repeat count, lane menus off the player's own indexed track roster with `auto`/`off` sentinels, and volume; the caption band renders the player's own timed cue through the caption typography role. The frame grab produces a `MediaSurface` ROW whose key is what `IssueOp.Attach` consumes. The clock law is enforced at the RAISE: under a follower posture the shared verbs route to the sequence clock and seek refuses, so a followed clip cannot own its own playhead.
[GALLERY_SURFACE]-[COMPLETE]: the gallery landed at `Document/media#GALLERY_SURFACE` as composition over four settled owners and zero new machinery — the filmstrip rides the `Render/capture` `Gallery`/`GalleryRetina` thumbnail variants that had no consumer, its cells resolve through the same shared image-loader arm the codec union owns so a thumbnail already realized in a list is a cache hit, the lightbox is a `DialogIntent.Editor` on the canvas stack inheriting the full-surface posture and teardown, zoom is `ZoomBorder` with the settled clamps and fit ladder, and load and failure states come from the control's own `IsLoading`/`CurrentImage` projections rather than a page-held flag. Capture, upload, and grab are three `GallerySource` rows over one item shape, with grabs arriving as the media rows the transport minted so a gallery item and an issue attachment name one still.
[BOARD_CANVAS]-[COMPLETE]: the board landed as the new `Document/board.md` with router row `[38]` and its `Board.cs` codemap node, plus a `6440` band row at `Diagnostics/evidence#FAULT_TABLES`. The load-bearing law is that a frame's crop and scale are BOARD state: an item holds a view KEY beside its own normalized crop and pixel scale, the crop lands as a clip on the frame's presenter, and nothing a board does reaches the camera the registry row holds. Every case is a placement over a settled owner — frames over `ViewRegistry`, cards over `StatAnatomy` through a re-bindable `MetricBinding`, sheets over the drafting `Sheet`, text through the markdown materialization, ink over the pen-axis samples — and every reference resolves LIVE through composition-bound seams, so a re-opened board reads current sources and an item whose reference died renders its own absence rather than vanishing. The canvas is `ZoomBorder` with a `Canvas` child, so pan, zoom, grid, snap, history, and saved views are the viewport owner's; picking and marquee run in content space through its own mapping and hand hits to the ONE selection fold. Templates lift references into named slots keyed by item identity so strip and re-bind are inverse folds, and the publish arm splits honestly: the snapshot folds items into `ReportBlock` rows through the one pagination owner, while the live arm delivers STRUCTURE so a shared deliverable re-reads rather than going stale.
[SEARCH_RESULTS_PANEL]-[COMPLETE]: the results surface landed at `Document/search#RESULTS_PANEL` as presentation over the ranked window — source bands ranking by their best hit with badges reading the PLANE's totals rather than the realized slice, per-hit previews whose emphasis re-runs the query's own strategy so the bolded characters are the matched ones, keyboard walking that clamps at both ends with peek-on-focus raising a canvas-stack `DialogIntent.Peek` that mints no back entry, commit raising the settled `SearchOpen`, scope chips composing the plane's own admission through `SearchScope.Refine` so narrowing costs no round-trip, and bounded recent-query rows holding admitted `SearchQuery` values. Three owed repairs landed with it: the lead re-cut onto the comparer authority, the `OrderedChangeSet` construction re-shaped onto a real `IComparer<SearchResult>` (the two-argument shape the fabric requires — the prior ordering-snapshot spelling did not typecheck), and the graph coverage seat threaded whole as `SearchSource.Node`, `SearchProjections.Nodes`, `SearchResult.Node`, `SearchOpen.GraphCanvas`, and its wire decode arm.
[NOTEBOOK_CELL_CHROME]-[COMPLETE]: cell chrome landed at `Document/notebook#CELL_CHROME` as presentation over the settled cell graph and recompute. `CellVerb` is a seven-row roster carrying its own command key and its own affected-cell projection, so a toolbar press, a menu, and a shortcut compute one cell set — and the projection reads DOCUMENT ORDER rather than the dependency closure, because "above" is what the user can see above while the closure is what the recompute decides once the run starts. Execution state reads the settled `CellStateOverlay` with elapsed derived from the recompute's own start mark and the fault peeked off the `CellOutput.Error` the evaluation already materialized, so no second state machine exists. Reorder commits `NotebookCoedit.Move`, with the drop indicator a POSITION between rows rather than a highlighted target. Output collapse is a height ceiling with the overflow measured, so a user reads how much is hidden and a fitting output carries no affordance. The outline is the markdown anchor projection over every markdown cell, navigating through the settled prose request.
[EXPORT_OPTIONS_FORM]-[COMPLETE]: export configuration landed at `Document/export#EXPORT_FORM` as schema rather than dialogs — each `ExportTarget` row carries its honoured-capability roster and BUILDS its own `FormSchema` from that roster through the settled field vocabulary, with sections derived from the fields' own tags so the partition the schema gate proves is constructed rather than authored; the preflight readout derives from the same roster as `Honoured`/`Degraded`/`Refused` notes, so it names what the arm will do rather than restating the options just set. Progress hands to the run queue as an ordinary `RunCard` whose strips are the preflight notes, and completion projects `OutputRow` open and reveal verbs the queue raises. Destination rows recall most-recent-first scoped by format. The PDF arm gained its bookmark outline: composition retains each heading's own layout node, the renderer's page walk answers which page it landed on, and the level ladder nests each bookmark under its nearest shallower ancestor. Two CORRECTIONS rows landed with it — the MigraDoc native chart DOM stated as a carve with its reason (the chart plane's grammar has no representation there, so routing report charts through it would mean a second weaker chart vocabulary), and `ExportDelivery.Deliver` gained its `ReadOnlyMemory<byte>` overload writing the span straight through the file handle so the streamed table export drops its final copy.
[ANALYSIS_FOLDER_PAGES]-[COMPLETE]: the Analysis sub-domain landed as `libs/csharp/Rasm.AppUi/.planning/Analysis/layers.md`, `Analysis/compare.md`, and `Analysis/context.md`, with router rows `[25]`-`[27]` at the folder `README.md` (the CHARTS band gaining `[24]` and every band below renumbering to 53), the `Analysis/` codemap node and four boundary rows at `ARCHITECTURE.md`, and the `69xx` fault decade at `Diagnostics/evidence.md` `[05]-[FAULT_TABLES]`. The completeness bar is STRUCTURAL rather than asserted: all four sealed output classes ride ONE `ResultPayload` whose `Samples` column is exactly what the probe reads, so a kind that could not answer the probe is unspellable, and `Faces`/`Field` are the two admission columns the kind rows gate rather than a second payload family. `ResultDomain` decides the colormap CLASS and the legend ARM together — the `PropertyDomain` precedent applied to a measured field — so the scene `TransferFunction` and the `LegendSpec` are one derivation read twice, which is the owed legend consumption closed at its source; a stepped domain samples at its severity RANK so the scene paints the compliance bands its list declares rather than a gradient the list never had, and a coded domain refuses to ramp at all. The four kinds collapse onto TWO render folds that MINT `SimVisual` cases rather than passes, with the march arrow and the world-to-raster projection riding an injected `ResultRuntime` because a projection authored here would be a second camera no viewpoint could capture. Provenance is a HISTORY seq newest-first, so a re-run appends and the prior reading stays addressable for a compare cell to bind. The probe is one coordinate to every layer, hidden ones included, with barycentric interpolation inside a declared face and a RADIUS-BOUNDED nearest read outside one — an unbounded nearest read answers a value at every coordinate in the world, which is why absence is a real verdict here. Bake is four verbs on the `IO` rail, three lifted and one genuinely effectful, each composing a settled owner and minting nothing.
[TEMPORAL_CLIMATE_AXIS]-[COMPLETE]: the one environmental coordinate landed as `Analysis/context.md` `[02]-[TEMPORAL_AXIS]` and `[03]-[SCRUB_BINDING]`. `TemporalGrain` carries the WINDOW each grain spans, so the span a shadow study integrates, a radiation accumulation sums, and a chart axis bounds is one fold at four column values; a range grain with no months and a non-range grain carrying months are both REFUSED at the mint rather than normalized, because a silently dropped range is a study over a span the operator asked for and never got. `ClimateScenario` shifts the YEAR a weather record is read in and never the sun, since orbital mechanics carry no emissions pathway and a scenario that moved the sun would render a projection as an astronomical claim. The kernel almanac is composed unchanged — `SolarPosition.At`/`SunPath` over a validated `SolarSite` — so no second truncation order enters and the frame projection stays at each consuming edge. The channel publishes PROJECTIONS rather than being copied: a `Track.Parameter` scrub whose easing is `MotionToken.Instant` because a curved playhead accelerates the sun through midday and every derived shadow inherits the distortion; an ABSOLUTE `TimeRange` for chart binding, since a relative range would slide a design-day study forward every time a board was left open; and a bounded `BoardVariable` so a baseline-versus-projected read is `CompareOffset.Scenario` over the settled ghost machinery rather than a third comparison dialect.
[COMPUTE_BUDGET_METER]-[COMPLETE]: the pre-solve readout landed as `Analysis/context.md` `[04]-[BUDGET_METER]`. The device ceiling DERIVES from the one settled bound — `min(device VRAM, watermark x the governor's factor)` over the resident cost of one cell — so the meter and the frame read one budget authority and a watermark mirrored into a second field is the drift the residency ruling already deletes. The estimate is the exact median of the prior sealed run durations through the settled order-statistic reducer, and a study with no prior run at this tier answers ABSENT rather than an extrapolation, because a fabricated number is what teaches an operator to stop reading the meter. `FidelityTier` is three policy columns rather than a name: `Pitch` scales the lattice so the PREVIEWED lattice is the SOLVED lattice, `Share` bounds the fraction of the ceiling a tier may claim, and `Surrogate` states whether the answer came through the reduced model — and the elected key stamps `LayerProvenance.Tier` at adoption, so a rapid-surrogate reading can never be mistaken for a detailed one on a board, in a report, or in a compare cell. The preview IS the settled `FieldSites.Declared` declaration the solve takes, bounded to a corner of a large lattice beside the honest cell count, and `Admit` REFUSES an over-ceiling request rather than coarsening it, because a silently coarsened result carries a resolution the operator never chose under provenance that claims their tier.
[CLIMATE_DIAGRAMS]-[COMPLETE]: the climate family landed as `libs/csharp/Rasm.AppUi/.planning/Charts/climate.md` with router row `[24]` and the `Charts/Climate.cs` codemap node, its four payload cases and six catalog rows REGISTERED on the `Charts/custom.md` catalog per that page's growth law (folds declared under the same `CustomVisuals` partial the plan cluster already established, and the `[KIND]` table extended to twenty-two rows). The POLAR SPLIT is stated as law with a per-family verdict and its structural reason: a rose is filled sector BANDS a polyline has no shape for, a sun path is MULTIVALUED in azimuth so an angle axis resolves one bearing to several hours and every tooltip names the wrong one, a sky dome is a patch field rather than a series of any arity, and a comfort chart is cartesian on a SKEWED frame no axis expresses — with a `directional` counterexample row kept in the table so the package's own polar surface stays visibly the first choice. Two card premises were REFUTED and landed as their honest forms: the carpet plot is NOT a payload — the calendar reshape already writes the cell row as the second magnitude a weighted coordinate reads, so it is one `TransformRow.Calendar` and one `ChartSeriesSpec.Heat` layer, the sparkline refusal running in the other direction — and psychrometric and adaptive comfort are ONE row rather than two, because they differ in frame skew, zones, and curves (all payload data) and in nothing a fold does. The rose's BIN COUNT is the legend's segment count by construction, since `RoseLegend.Of` reads the payload's own band roster, and a pinned maximum rides the payload rather than the style because it is a property of the comparison being built. `DomeProjection` is explicitly `[NOT]` a `GeoProjection` — no datum, no CRS, no ground coordinate — and its stereographic row is the non-linear radial map that would defeat a polar radius axis even where the multivalued arcs did not.
[SYNC_PRESENCE_CHROME]-[COMPLETE]: presence became visible on every plane as new cluster `libs/csharp/Rasm.AppUi/.planning/Collab/sync.md` `[07]-[PRESENCE_CHROME]`, with the entity-scoped half at `Collab/session.md` `[06]-[ENTITY_CHROME]`. `PresencePlane` is the co-edited plane axis whose ROW carries its own overlay projection, so text carets, node halos, and viewport frustums are three row answers rather than a ladder whose default arm renders nothing; the caret transports the loro `Cursor`'s own encoded bytes and re-anchors through `GetCursorPos` on the receiving replica, so a remote caret sits at its post-merge position and a gc'd anchor renders nothing rather than landing on the wrong glyph, while the halo and the frustum both read the peer's published `Viewpoint` so one portable receipt carries camera, section, and selection. `PeerTint` derives colour from the peer identity through the kernel one-hasher onto the qualitative colormap, so every replica paints peer N identically and a join never repaints the board. `PresenceFollow` is ad-hoc and UNGATED per the settled presence-is-display ruling, breaking on any local viewport INTENT rather than a camera delta (a followed camera moves continuously, so no delta test separates a nudge from the target's travel) and stating itself through one `ControlIntent.Banner`. `PresenceSignals` binds the two document-store subscriptions the chrome needs — `SubscribeFirstCommitFromPeer` for the join signal and container-scoped `Subscribe(ContainerId, Subscriber)` for scoped feeds, so a scoped activity list is a subscription rather than a filter over the root feed. Entity chrome landed the TTL-swept `SeatCluster` over one `ControlIntent.Avatar` with its join handoff, and `ActivityNotice` carrying its MINT INSTANT and its row's lifetime so the remaining fraction is a pure read and no notice owns a timer, every notice terminating in a `CommandHandoff` its kind row supplies.
[ISSUE_BOARD_PRESENTATION]-[COMPLETE]: the BCF board earned its three views as new clusters `libs/csharp/Rasm.AppUi/.planning/Collab/issues.md` `[06]-[ISSUE_REGISTER]` and `[07]-[BOARD_PRESENTATION]`. Triage became DURABLE: `IssueOp` closes transition, assign, label, rank, and attach, `EditIntent.IssueCommit` carries it on the one union, and `IssueRegister` owns the ingress-plus-decode split every collaborative surface takes — each issue one guid-keyed mergeable map under the new `CollabRoot.Issues` root, the label SET its own keyed mergeable level so concurrent labelling converges, and an unassignment ERASING rather than writing a blank the exchange would emit as a real assignee. `IssueStatus` gained a `Needs` capability column so closing takes the resolve grant and reopening takes authoring, which `SessionGate.Required`'s new recursing arm reads. `Issue` widened with assignee, labels, and attachment, `ToTopic` now writes assignment and labels (the exchange line runs where a column means something to a foreign reader — the media key and the peer ordinal stop at the board), and `Synced` reads comments and triage in ONE pass folding per column. `BoardSurface` folds one filtered row set into kanban lanes total over the status vocabulary, a virtualized list, and a detail pane; `BoardSchema` is a `FilterSchema<IssueTile>` so chips, sort, grouping, and saved views are the settled algebra with the status and priority domains DECLARED; drag-to-transition mints the settled intent and moves NOTHING locally, so a refused drop needs no rollback; `BoardMentions` builds the `MentionRouter` off the same roster the picker offers, which is why `MembershipOp.Invite` and `MemberRow` gained the handle column.
[REDLINE_TOOL_SURFACE]-[COMPLETE]: redlining became a typed tool surface, rebuilding `libs/csharp/Rasm.AppUi/.planning/Collab/issues.md` `[06]-[REDLINE_AUTHORING]` into `[08]-[REDLINE_TOOLS]`. `RedlineTool` closes pen, highlighter, shape, text, and eraser with every behaviour as a COLUMN — pressure consumption, base weight, `PaintRole` ink, and erase routing — so the capture fold branches on no tool name and a redline carrying a literal colour is unspellable. `StrokeCapture` folds the LANDED pen rows whole: `PointerTrack.Pen` mints one `PenSample` per intermediate point so the coalesced burst is drawn from rather than its last sample, `PenAxis.Pressure` scales the tool's base weight per point, and `PenAxis.Eraser` routes the WHOLE stroke to removal regardless of the selected tool, because a stroke half-erasing and half-drawing is a shape no undo can invert as one unit. ONE captured `RedlineStroke` feeds BOTH commit legs — the viewport leg crossing the viewport's own composition-bound unprojection because `BcfLine` carries world vectors, and the basemap leg the landed `Charts/basemap#REDLINE` recorder hop — so history binding is the one revert vocabulary on every plane. Review-posture attribution reads `PeerTint`, so a redline's author colour and that author's caret colour are one value.
[PRESENTER_CHROME]-[COMPLETE]: tours gained presenter and audience chrome as new cluster `libs/csharp/Rasm.AppUi/.planning/Collab/tour.md` `[06]-[PRESENTER_CHROME]`. `PresenterStrip` is BOUNDED rather than modular — a presentation has a first and a last stop, so stepping past either end holds while a direct `Seek` rides the tour's own out-of-range rail — and its elapsed indicator is the step's OFFSET into the tour rather than a wall stopwatch, which keeps the page's no-local-stopwatch law and makes the strip and the timeline agree by construction; the step peek titles each step from its narration so captions and step list are one text. `TourFollow.Playhead` was rebuilt into the public `Presenter()` answering the SEAT beside the frame, so the face an audience reads and the camera their viewport takes resolve once and can never name two peers; `AudienceChrome` reads it for who-presents, follow toggle, and a caught-up frame tolerance. Publishing still crosses the settled gate at its own producer, so this chrome adds no second capability read. `StepAnnotations` binds each stop's viewpoint to the settled redline tool surface, minting no markup model.
[SESSION_STATE_CHROME]-[COMPLETE]: session trust state became one glance as new cluster `libs/csharp/Rasm.AppUi/.planning/Collab/session.md` `[07]-[SESSION_CHROME]`. `SessionAction` carries the capability that REVEALS each governance verb, so the roster panel and the admission gate read one `SessionRole.Holds` and cannot drift, and the remove verb hides on the last governing member so a user never learns the sole-governor invariant by being refused. `SyncHealth` closes live, reconnecting, degraded, and offline with three columns — the `FeedHealth` correspondence keeping the board's severity ladder the board's, the `BannerSeverity`, and an `Edits` column that IS the offline posture as data. The fold takes three settled sources and derives none of them: the `Editing/livedata` staleness projection answers freshness, the overlay ledger's own pending gauge answers outstanding work, and the `Diagnostics/governor` `QualityTier` answers degradation. `ConnectionStrip` produces both a footer `ChromeContent.Pane` and an OPTIONAL banner, because a quiet session must say nothing — a persistent notice restating that everything is fine trains a user to stop reading banners. `OfflinePosture` disables nothing: an intent is a typed row that queues and presents as its own optimistic row under the pending posture, so the online and offline answers to "what happens to my edits" are one chrome.
[COMPARE_SESSION]-[COMPLETE]: version comparison became a session as new cluster `libs/csharp/Rasm.AppUi/.planning/Collab/sync.md` `[08]-[COMPARE_SESSION]`. `BaselineProvider` ranks saved-version, live-remote, and scenario so an unnamed baseline elects the first that resolves, and entry from any option folds `OptionSet.Against` onto that same axis — an option compare, a chart period ghost, and a version ghost address their baselines through one vocabulary. `DiffLegend` IS the visible set, so toggling a class removes its override rather than repainting it transparent and the legend cannot disagree with the `VersionGhost.Project` render. The change list rides the settled filter algebra and windowing fabric with grouping as a `ViewState.Group` row. `DiffSurface` composes the `Editing/inspector#CONFLICT_RESOLUTION` three-way machinery run DEGENERATE — the baseline occupies both the base and local legs, so every divergent region is one-sided, `Conflicted` is false throughout, and `ConflictSide.Both` is exactly the inline layout's own read — which keeps the region law, the line ceiling, and the band chrome in one place instead of forking a compare-only differ; `DiffLayout` makes side-by-side and inline two seat geometries over one hunk sequence, and unchanged-region collapse is a retained-context span that expands in place rather than a filtered list. The session is a capability class holding two `Frontiers` cuts under the native-lifetime law and is READ-ONLY over both, because a compare that committed would be the time-travel rail.
[FILTER_VIEW_ALGEBRA]-[COMPLETE]: one filter grammar and its compiler landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Editing/livedata.md` `[03]-[FILTER_ALGEBRA]`, with the view axis beside it at `[04]-[VIEW_STATE]`. The operator vocabulary is arity-AGNOSTIC — one `FilterOperator` row's `Exists` fold reads identically at one operand and at twenty, so the cardinality morph is the row's `LabelKey` plus its admitted `FilterArity` band and a plural row beside the singular one is unspellable; `FilterExpr` is the recursive term/all/any/not tree whose canonical `Open` is a vacuous `All`, so absence of a filter is a value rather than a nullable. `FilterSchema<TRow>` is the ONE compiler and answers three questions off one property roster — predicate, comparer, and group key — so a column a surface can filter is one it can sort and group by construction. `FilterLink` is one codec for the deep link and the `Shell/screens#SCREEN_STATE` checkpoint, carrying no kind tag because the schema declares each property's domain, bounding nesting and term count at `FilterPolicy` before the tree exists, and riding `Uri.EscapeDataString` so its five structural delimiters cannot appear in a payload. Consumers re-pointed BOTH ends: `Editing/tables#GRID_SUBSTRATE` deleted `TableFilterOp`/`TableFilterRow` and produces a `FilterSchema<FlatNode<TRow>>` off its column roster instead, `Editing/tables#VIEW_STATE` now carries an encoded filter, the shared `ViewState`, and grid-mechanism column cells alone (visibility moved to the domain axis), and `Document/search#RANKED_WINDOW` gained `SearchScope` refining its answered set through the plane's own `FilterOnObservable` admission with coverage and refinement kept as two owners. `FilterPace` moved here from tables as the shared edit cadence.
[OPTIMISTIC_OVERLAY_ROW]-[COMPLETE]: the optimistic overlay landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Editing/livedata.md` `[06]-[OVERLAY_SPINE]`, with its backpressure axis on `[02]-[DATA_SOURCES]` and its instruments on `[08]-[AGGREGATION_SPINE]`. The overlay is a MERGE leg rather than a write-back: `MergeChangeSets` under `OverlayRank` publishes the pending row while it exists, and the decompile-proved tracker fallback (`OnItemRemoved` -> `UpdateToBestValue` re-looks-up every remaining source) makes acknowledgment and rollback the same drop, so no owner remembers a value to restore and a late authoritative echo is suppressed structurally rather than filtered. `OverlayEcho` is three-armed because the authority answers in three shapes — `Acked` on the round-tripped revision, `Converged` on value equality for the CRDT import path, `Refused` flipping to a posture that still outranks settled so the rejected value renders before it falls back after `OverlayPolicy.Linger`. Revisions are ledger-local and monotone rather than clock-derived. Backpressure landed as `FeedPace` = Coalesced | Gated on `SourcePolicy`, applied once at the new `Opened.Feed` consumer connect through `Batch`/`BatchIf`, which buffer and flatten rather than drop — a `Throttle`/`Sample` over a change-set is now the named deleted form estate-wide. The staleness projection landed as `Opened.Watch` emitting `FeedFreshness(StreamKey, FeedHealth, LastRefresh, Age)` on a probe cadence so age advances without the feed, with the dashboards consumer end unchanged.
[OPTION_SET_MODEL]-[COMPLETE]: design options landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Editing/livedata.md` `[09]-[OPTION_SETS]`. `OptionVerb` closes create, rename, duplicate, regenerate, and prefer over one `OptionSet.Apply` fold; lineage is one `DesignOption.Parent` field so duplicate and regenerate-similar share it, and preference is one key on the set so two preferred options are unrepresentable. Regeneration DECLARES and never generates — the verb mints its `OptionRequest` seed in the same fold that mints the option, so a request never names an absent option. Per-option KPI columns fold through the live `Group` form, whose `IGroup` cache makes one option's edit re-emit that option's reading alone, and a candidate missing a metric contributes nothing rather than a zero. Option-scoped filtering and KPI-sorted candidate lists are the `[03]` algebra: `OptionSet.Schema` projects the option key as a bounded `member` property and each KPI as a `number` property, so min/max bounds are the existing operators and KPI ordering is `ViewState.Order` — a `CandidateBound` vocabulary is unspellable. The comparison join landed on both settled vocabularies: `OptionSet.Variable` answers the `Charts/dashboards#BOARD_CONTEXT` `BoardVariable` whose domain IS the live roster, and `Against` answers `CompareOffset.Scenario`.
[SETTINGS_SURFACE]-[COMPLETE]: settings landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Shell/screens.md` `[08]-[SETTINGS_SURFACE]`, projected from a `SettingsRegistry` of per-owner `SettingsRow` registrations and seated as the `settings` roster row on the `DialogIntent.Editor` canvas bound. The surface RENDERS and never writes: every mutation returns through the registering owner's own swap capsule, so an apply answers that owner's `ReloadOutcome` and a refusal keeps prior values live on the reload stream exactly as an inspector-driven write does — a settings-local persistence path is unspellable. Sections, fields, planning, and the section bodies are the schema engine's own `FormSection`/`FormField`/`FormSurface.Plan`/`FormSurface.Panel`, so no settings-only control path, validation rail, or dialog framework exists; the anchor rail is DESKTOP CHROME through `FormChrome.Rail` rather than a body member, the same intent-versus-seated split the form owner already makes, so a head with no form mechanism receives the whole tree and scrolls it. PROVENANCE is a `SettingScope` (default, user, workspace, machine) rather than the form's `ValueOrigin`, because origin answers whether a value was authored and scope answers WHICH writer set it — the fact a reset verb needs and the only one that distinguishes a machine policy from a user write when both read as authored — and the scope map is the row's own live arrow, since a field's scope moves when an administrator lands a policy and a schema frozen at construction cannot follow. `SettingsQuery` parses `section:`, `source:`, and `modified` into ONE value whose three cuts each take the axis the others cannot: the section term cuts whole rows first so a scoped search folds one schema, the form filter cuts fields on text and modification, and the scope term cuts last because scope is a live read. Reset is one fold over one key or a section's whole roster, routed through `FormSchema.Seat` so a default the policy's own admission now rejects refuses at reset rather than landing a value the next reload throws away.
[PRODUCT_SCREEN_ROWS]-[COMPLETE]: the four product surfaces landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Shell/screens.md` `[09]-[PRODUCT_SCREENS]` with their roster seating at `[02]-[SCREEN_CATALOG]`. First run is the EMPTY-RESTORE fact off `LayoutLedger.Restore` rather than a flag, so a profile whose checkpoint was pruned lands there exactly as a new profile does and a "seen first run" marker cannot drift from what the shell actually restored; the crash half of the report reads `LayoutLedger.Offer`'s own verdict over the fault spine's `HostCrashMarker` and re-derives nothing. `SaveState` projects THREE independent facts — dirty, autosave stamp, recovery blob — into four rows carrying their own verb, so a dirty document with a recent autosave and one with none stay distinguishable and the chip a row wears is the verb it offers; recovery is an OFFER on the row, never an automatic restore, because a recovered document that silently replaces a saved one destroys the saved one. Coach marks are PREDICATE-completed rather than counted, so an operator who discovered a feature unaided never sees its mark, dismissal writes a key into a set rather than deleting the row so a profile reset restores the sequence, and an anchor the profile does not mount drops the row rather than floating a bubble over nothing. The report is CONSENT-BEARING per member over the contributors' own `SupportArtifact` declarations — classification and estimate are the contributor's columns rather than restatements, consent defaults to the classification's posture so anything narrower than operational is opt-in, an unconsented member never reaches the capture, and assembly stays AppHost's `SupportCapture` fold. Landed beside them: the seven bespoke screen classes collapsed into ONE `ScreenProgram`-driven `ProductScreen` whose named cell bag serves the value channel, the re-projection edge, and the state snapshot at once, and `ScreenCatalogRow` lost its body column because a body is the model's own projection.
[RUN_QUEUE_SURFACE]-[COMPLETE]: the queue landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Shell/screens.md` `[10]-[RUN_QUEUE]` with its stat band at `Charts/telemetry.md` `[02]-[BOARD_ROWS]`. THREE PLANES carry three status columns and no roll-up fold: a transiently failing step under an eventually succeeding run never wears the run's failure and a run that failed after two green steps never reads green — a maximum-severity roll-up produces both errors at once. `WorkStatus` carries terminality, severity, and the verb key its card's ONE action button binds, so cancel and retry are the same affordance at two moments and a status naming no verb renders no button rather than a disabled one; `RunDirection` makes a bidirectional transfer one card discriminating on a column rather than a second kind; strips are APPENDED and fixed-height so a card never changes size class mid-run and the button stays where the operator was reaching. The card list is `ControlIntent.Tree` over the one `VirtualWindow` fabric — a run and its steps are a genuine hierarchy, so the flatten already owned realizes both under one item template and the tile union stays list-free by design, with the queue's aggregates going the other way as four stat tiles the board folds from this surface's own instruments (depth reads MAXIMUM because it is a levels instrument; the three counters sum). The drill-down is `RunOrigin`, a two-arm join key rather than nullable columns: the study arm rides the diagnostics owner's own `EvidenceJoin.Run` over the `StudySubmission` the study form seals, the verb arm narrows the same source by correlation, and both answer one timeline — so a queue-local evidence log has nothing to be. `RunReport` orders severity-first then EXECUTION order, because a second sort on time scatters a retried step away from the failure that caused it, and the count chips hide at zero since a chip filtering to nothing teaches nothing. Sealed outputs hand off by RAISING the layer plane's adoption verb, so the analysis plane keeps one construction site for what a sealed study becomes.
[GRAPH_MODEL_WIDENING]-[COMPLETE]: the node concept reached the package's own at `libs/csharp/Rasm.AppUi/.planning/Editing/graph.md` `[02]-[GRAPH_MODEL]`, `[03]-[ADMISSION_GATE]`, `[04]-[PALETTE_INGRESS]`, and `[05]-[CANVAS_VERBS]`. `GraphNodeRow` carries containment, extent, rotation, lock, and visibility, which resolves the contradiction the declared hierarchy-move op stood on — the op committed a parent and an index onto the co-edit tree against a model with no parent to rehydrate, so a group, a frame, and a collapsed subgraph were durable and unreadable; containment now re-seats inside the SAME staging pass the wiring does, off the index the fold already holds, because an in-order write drops exactly the forward references a re-parented group carries. The edge SPLIT identity from presentation: `GraphLink` is the endpoint pair the duplicate and fanout folds key on, so waypoints, per-edge routing mode, orientation, arrow pair, offset, and label travel on `GraphWire` without two renderings of one wiring ever reading as two edges. `GraphModelAdapter` collapsed from seven delegate columns to four by making the read-side projections static folds over the package contracts — a pin's key is its own `Name`, direction and bus width come off the optional `IConnectablePin` typing with an untyped port reading bidirectional at unit width rather than refusing — leaving only the two constructions the package cannot perform without the product's types. `GraphPalette` freezes the template registry every `TemplateKey` finally resolves against and feeds the package `Toolbox` verbatim, so browsing, double-tap insert, and toolbox drag read one roster and a missing key refuses by NAME rather than as a generic model rejection whose repair points at the adapter. `GraphDropTarget` implements the package drop contract over the one transfer rail, admitting every path before a node exists and walking the lattice so a multi-file drop lands a readable column. `GraphInk` mints strokes from the landed pen fold and the package's own capture is REFUSED — that capture writes a constant unit pressure and reads only the current point, discarding the coalesced burst a stroke is drawn from — while `InkLayer` stays the renderer, which draws strokes unconditionally yet takes one pen width per stroke and reads per-point pressure nowhere, so pressure lands as a bounded run ladder rather than as a fork of the renderer. `GraphVerbs` GENERATES its layout rows off the package's own alignment, distribution, and z-order case sets beside the lock, visibility, and selection rows, so a package enum gaining a case gains its verb with no roster edit. Three rulings landed: duplicate-type alias direction, per-bind latch custody, and shipped-readout ownership.
[GRAPH_NAVIGATION_SURFACE]-[COMPLETE]: navigation landed as `Editing/graph.md` `[05]-[CANVAS_VERBS]` and `[06]-[NAVIGATION_SURFACE]`. `GraphNav` is the typed camera union admitting every argument before dispatch on the same `Fin` rail every sibling entry answers, with the two direction-bearing rows collapsing four verbs into two cases carrying a bounded signed rung; `GraphCamera` binds the PINNED viewport and seats the discrete ladder, the history ring, and the grid from the same `GraphRouting` row the position write and the decorator read. The minimap is the settled overview primitive under a source key — content off the node extents the widened row now carries, viewport off the control's own visible-content bounds, and the four decoration lanes over the settled lane vocabulary — published on `OverviewAxis.Plane`, which fits uniformly, since a graph summarized under an independently-scaled fit renders a distorted map of the thing it exists to make navigable; the strip's drag publishes a content-space point through a lifting arrow onto the existing keyed payload case, because handing a control that publishes geometry the row's own materialized command throws on the first drag. `GraphFind` contributes CANDIDATES to the one search plane and consumes ranked results, owning only the cursor, the highlight set, and the camera walk, which wraps because a canvas has no visible beginning or end. `GraphView` round-trips the whole exported viewport state, so a restore returns the matrix, stretch, rotation, and clamp posture rather than a reconstruction that never carried them, and a refused decode leaves the live view untouched and typed. The guide overlay finally has its consumer, gated on the settings row's own guides column. The zoom HUD is RULED to the package's shipped in-viewport indicator over a chrome chip that would need a change subscription, a second formatter, and a placement tracking a viewport it sits outside.
[GRAPH_CANVAS_THEME_ROWS]-[COMPLETE]: the canvas joined the variant identity at `Editing/graph.md` `[07]-[CANVAS_SKIN]`. `GraphSkin.Slots` re-seeds every pin, connector, crossing, rejection, node-handle, and rotation-readout key from the resolved role ladder as ordinary shipped-key correspondence rows folded into the one emission, so the canvas follows a swap through the same partition every other surface follows and this page writes no brush; a graph-local dictionary merged beside the emission is refused, because the swap rewrites its own partition alone and the sibling would go stale on the next re-seed. The consumed-but-undefined closure landed with it: the two editor background keys take the surface ladder and the four chrome icon keys resolve through the asset rail as materialized images under the swap's tinted-asset roster, since a tinted glyph holds its pigment in its own bitmap and cannot observe a dictionary. The guide key the shipped theme binds to BOTH the alignment overlay and the selected-connector overlay is split, so recolouring a guide no longer recolours a selected wire. Grid sizing landed beside the theming rather than after it: the decorator's cell columns default to zero and an unsized grid mounts, resolves, measures, and paints nothing, so `GraphRouting` lifts a non-positive cell onto the resolved space step and the decorator, the position snap, and the viewport grid all read that one row.
[DEVLOOP_SURFACES]-[COMPLETE]: the dev loop's instruments earned their faces as new cluster `libs/csharp/Rasm.AppUi/.planning/Diagnostics/devloop.md` `[04]-[LOOP_SURFACES]`, with the governor's readout landing at `Diagnostics/governor.md` `[02]-[PERF_BUDGET]` and the flame's draw row at `Charts/custom.md` `[02]-[SKIA_KINDS]`. `FlameView` re-ROOTS on zoom rather than scaling a viewport — a two-percent leaf becomes the whole width and its own children become legible, where scaling keeps every sibling at sub-pixel width and is the very reason a hot leaf is unreachable — and a stale focus path after a re-fold widens back to the node it reached rather than rendering an empty view an operator reads as a quiet frame; the lane a span carries is its depth-one ancestor's frame, so a profile subtree's thread roots group naturally and zooming into that subtree promotes them with no special case, and a zero-total subtree drops WHOLE because a total is self plus children. The layout is not this page's: `SunburstArcs` widened into `CustomVisuals.WedgeSpans`, answering the parent-share nesting in UNIT FRACTIONS with each wedge's index riding along, so the ring scales it into the full turn, the new `CustomVisual.Flame` row scales it into the raster width, and `FlameView.Hit` compares it against a normalized pointer — ONE arithmetic, three readings, and the rectangle the pointer resolves is the rectangle the plane drew. The flame is a row over the EXISTING `VisualPayload.Wedge`, the gantt-and-timeline precedent applied to the other nested payload, so no span payload was minted. `ScrubTransport` binds the landed `TransportVerb` roster over the shared `TransportState`, minting one playhead at one ordinal per unit and never reading its position, because a solve journal has no wall-clock rate and binding recorded instants to it makes a step skip ordinals exactly where an operator is stepping; the comparison window is the transport's own range, so a scrub-local from/to pair is unspellable. `ReplBlock` makes the REPL and the log ONE ordered stream — a typed line, its refusal, a captured burst, and a correlated timeline, each carrying the ordinal, the instant, and the ONE `Query` column the filter reads so no case is invisible to a search that should answer it — with extents DECLARED off content arity rather than measured (a measured window realizes the whole stream to scroll to its end), the height index a running prefix, bookmarks on the STREAM keyed by ordinal because a block is an immutable record and a bookmark is a reader's annotation over it, and the window riding `VirtualWindowSpec.Measured` on the one fabric. `DiagnosticsChrome` seats every chip as an ordinary `ChromeRow` on `ChromeSlot.Hud`, and `GovernorReadout` answers tier, attribution, the tightest axis's remaining HEADROOM, the instant the tier last moved, and a policy-bounded `TierTransition` ring recorded inside the same CAS swap that moved the rank — an unbreached tier reads the tightest axis rather than a blank, because "nothing is breaching" and "no idea what is closest" are different answers. Four rulings landed: the presentation-seals-nothing law, the one-nesting-arithmetic law, the golden-posture-column law, and the one-transport-grammar law.
[PACKAGE_SKEW_GUARDS]-[COMPLETE]: the three live skew risks landed as new cluster `libs/csharp/Rasm.AppUi/.planning/Diagnostics/proof.md` `[05]-[GUARD_REGISTRY]` beside a golden roster that registers every committed artifact against the entry that writes it — including `LayoutWireGolden.Parity`, named here rather than re-spelled, since a second `VerifyJson` over the same wires commits a second file that drifts silently from the first. The validation package reads `fails` with the DROP as its resolution and no guard entry at all, because nothing is left to bind and a row promising a re-proof over an absent assembly fails forever while naming a risk that no longer exists; the dock skin reads `covers` with `SkewGuards.DockTheming` answering per `(type, variant)` cell rather than per type, since the skin defines no variant-local values of its own and a key resolving under one variant and not the other is exactly the incoherence version proximity cannot see — the standing obligation is therefore VARIANT COHERENCE, not version proximity, and the control package may be lagged freely; the graphics pair reads `binds` with `SkewGuards.SkiaBoundary` leasing the api feature at the live render boundary, because a declared dependency is a FLOOR the loader satisfies with a higher major under a matching public key, so a silent restore proves nothing and only the crossing itself is honest — the guard refuses by name on an absent lease rather than passing vacuously under stub drawing. Verdicts recorded on the `Semi.Avalonia.Dock` and `Avalonia.Skia` registry rows. Also landed here: `CaptureRow` and `RenderHashLane` gained the `Posture` column carrying a `RenderPosture` row, with the cell key spelling it and `RenderHashGrid` crossing postures as an axis beside scales and gamuts, so a screen golden and a paged-export golden are two files rather than one two lanes overwrite; and `ProofLaw.SemiConformance` folds `SemiRoster.Walk` into `ThemeCatalog.SemiCovered` on the SAME live application the accessibility sweep stands up, deriving the shipped roster per run because the theme compiles its keys into XamlClosure bodies where no metadata read can reach them — a captured roster artifact freezes a vocabulary the next bump moves while still reading as a pass.
[SELECTION_SURFACE]-[COMPLETE]: selection became one producing algebra at `libs/csharp/Rasm.AppUi/.planning/Editing/forms.md` `[05]-[SELECTION_MODEL]` with its gesture ingress at `Shell/input.md` `[04]-[POINTER_GESTURES]`. `SelectionGesture` states the anchor, range, and toggle grammar ONCE as five rows — replace, add, remove, toggle, extend — each folding over the non-generic backing exactly as the mode rows do so the two compose without either becoming generic, and each carrying its own anchor custody, with the range row alone preserving the anchor and the modifier fold reading the platform primary off the one `GesturePolicy.Primary` value rather than testing for the meta key; `Selection.Raise` is the single entry a click, a modifier-click, a resolved shift-range, and a marquee release all take, and `Selection.Span` resolves the anchor-to-target slice from the plane's own order so no plane keeps range state. `SelectionBand` retains its ORIGIN rather than a normalized rectangle, because the drag direction is the selector — left-to-right takes what the band contains, right-to-left takes what it touches — and resolves its extent once per sweep rather than per candidate. `SelectionFacet` makes select-similar a signature match over chosen facets where an absent facet REFUSES the query per seed instead of matching everything. `SelectionSet` gained its document key and a refusal for operands spanning two documents, and `ApplySet` collapsed onto the replace gesture so set recall and an unmodified marquee are one exact projection. `SelectionChannel.Snapshots` is the one producer — the backing's own `SelectionChanged` its one edge, distinct-keyed past the snapshot's reference-equating kind set — feeding command availability, the status-footer `selection.count` readout, and the screen-state checkpoint; `SelectionSetStore` carries the durable columns and the recall verbs stay command-table intents. Three rulings landed: source/actuator symmetry, the shared gesture grammar with model-held anchor custody, and absence refusing a similarity match.
[INPUT_MODALITY_CLOSURE]-[COMPLETE]: the input fabric's stated charter closed across `Shell/input.md` `[04]-[POINTER_GESTURES]`, `[06]-[INPUT_FABRIC]`, and `[07]-[DEVICE_DRIVERS]`. `PenAxis` lands pressure, tilt-x, tilt-y, twist, barrel, and eraser as rows minting `DeviceAxis` values on the DEVICE FABRIC'S OWN channel grammar, so a markup surface binds one axis resolver whether a level came from a nib or a MIDI fader; `PointerTrack.Pen` gates on `IPointer.Type` because a mouse reports a constant mid pressure rather than absence, decodes the whole coalesced burst through `GetIntermediatePoints` because reading only the current point discards every sample the platform batched, folds `IsEraser` and `IsInverted` into one eraser channel, and CLAMPS an overshooting field rather than dropping it, since a refused pen sample is a hole in a stroke. `DeviceOutput.MidiFeedback` closes the input-output charter as a channel-roster FAN driven by one send burst, and `Arm` now returns `DeviceSink` — receipt, sink, and teardown, symmetric with `DeviceSession` — so the gamepad stops both motors before releasing its context, the haptic stops and closes its effect before the joystick it bridged from, and the MIDI surface takes `TurnAllNotesOff` ahead of dispose; the `Armed` fold is the actuator twin of `Admit` with its own resolved/absent instrument pair rather than a second value on the source slot. The MIDI capsule gained the hot-plug generation boundary it lacked, switching on the `DevicesWatcher.Instance` add/remove pair exactly as the HID capsule switches on `DeviceList.Changed`. `HidUsage.Channels(driver, buttons)` folds the ordinal Button page into the one packed-usage table so a button block is a count, and the decode discriminates on `DataItem.IsBoolean` because the bipolar scaling reports a released button as full negative deflection.
[VIEW_NAVIGATION_SURFACE]-[COMPLETE]: the viewport earned orientation and named views as new cluster `libs/csharp/Rasm.AppUi/.planning/Render/pipeline.md` `[05]-[VIEW_REGISTRY]`. `GizmoTarget` DERIVES the orientation cube's twenty-six pick targets from the twenty-seven signed axis triples less the origin — six faces, twelve edges, eight corners as one derivation rather than an authored roster — each carrying its own normalized direction, its rank (which breaks a corner-versus-face tie toward the smaller feature), and a key spelled in `AxisLabel`'s own letters that a gizmo pick, a standard row, and a deep link all share; `Orientation.Pick` hit-tests in the cube's LOCAL frame so aiming at the drawn face selects the axis the drawing showed, and `Orientation.Standards` seeds the registry off the same derivation with the LIVE lens re-posed (a snap-to-front on a perspective camera never silently becomes orthographic) and an up vector chosen against the degenerate cross product. `ProjectionToggle.Flip` preserves the FRAMED EXTENT so a double toggle is the identity — `2·d·tan(f/2)` becomes the view height and the inverse re-derives the distance — with the XR asymmetric lens passing through unchanged. `NamedView` carries camera, section, and visibility context TOGETHER under a `ViewOrigin` provenance column (standard, bookmark, capture, visited), so ONE registry and one key space serve bookmarks, standard views, captures, and traversal history; `ViewRing` stores KEYS with a cursor and truncates its forward tail on a visit, so a renamed row keeps its history position and a deleted row's entries retire with it; `ViewRegistry.Recall` mints a two-keyframe camera `Track` under the row's own `MotionToken` into a `Timeline` the scrub drives, so a standard-view snap, a bookmark jump, and a back step are ONE arrow through the one pose-interpolation owner, with section and override state landing at the transition's end rather than easing. `NamedView.Link` is the deep-link address and `ViewRegistry.KeyOf` its one resolve; `ViewChrome` seats the orientation, projection, and section chips as ordinary `ChromeContent.Chip` rows on `ChromeSlot.Hud` and names the recall verbs as command keys. The 2D `ZoomBorder` canvas registry is stated as a DISJOINT owner rather than a rung of this one.
[SECTION_BOX_MANIPULATOR]-[COMPLETE]: sectioning became direct manipulation as new cluster `Render/pipeline.md` `[06]-[SECTION_MANIPULATOR]`. `SectionPlane` is the six-row vocabulary where each row owns its axis, its outward sign, and the reader/writer pair touching its ONE box ordinate — so the drag fold, the outline draw, the enablement clamp, and the BCF projection all dispatch over one vocabulary and a plane cannot touch an ordinate that is not its own — with `Opposite` derived from the sign rather than paired in a table, `Normal` derived through `AxisLabel.Write`, and `Centre` the face midpoint both the handle projection and the outline read. `SectionState` holds the box beside a per-plane enablement SET and the outline row, its `Clipped` fold opening a disabled plane to the model extent rather than rewriting the box (so re-enabling restores the exact dragged ordinate) and its `Chip` answering the HUD fact. `SectionDrag.Drag` projects the screen delta onto the plane's own on-screen axis direction and divides by the measured pixels-per-world-unit at the face centre, so a drag tracks the handle at every camera angle and a near-edge-on plane REFUSES below a two-pixel floor rather than accelerating without bound; a face dragged past its opposite refuses rather than inverting, because an inverted box clips everything and reads as an empty model with no fault. `Handles`/`Hit` are screen-space proximity reads over the enabled planes alone so a handle grab can never select geometry behind it, and `Passes` emits ONE `RenderPass.Overlay` drawing the twelve edges derived from the eight corners (adjacency being exactly one differing ordinate, never a transcribed pair list) plus a handle mark per enabled plane. Manipulation commits through the settled `SectionBox` the viewpoint captures and the codec projects to six BCF planes, so no second section model exists.
[MEASURE_READOUT_SURFACE]-[COMPLETE]: measurement became a mode as new cluster `Render/pipeline.md` `[07]-[MEASURE_MODE]`. `MeasureKind` is the five-row vocabulary carrying vertex arity, readout `MeasureRole`, and its OWN fold — point-to-point distance, perpendicular drop to a line (the clearance, not the diagonal), subtended angle, the Newell vector area of a picked ring (so a non-planar pick answers its own best-fit plane rather than an axis projection), and coordinate elevation — every fold refusing a degenerate pick by name rather than returning a zero the panel would display as a measurement, with `Open` marking area as the one kind the user's own terminator closes. `MeasureRow` carries its source-addressed `ViewMeasurementPoint` vertices, its pin state, its axis-delta posture (components rendered under the SAME role as the total), its `Annotation` projection onto `ViewMeasurement` — so a pinned row IS a viewpoint member crossing to BCF as `BcfLine` rows with no second annotation model — and its `Sources` the highlight channel brushes. `MeasureSession` is the arity counter whose `Take` closes the row and clears the pick buffer in one transition, leaving the picks in place on a refusal, and whose `Annotations` is exactly the pinned set. `MeasurePanel` seats by corner-plus-offset, renders through its own `MeasurePolicy` settings, and publishes hover onto `HighlightChannel`. `MeasureExpression` landed the unit-aware label evaluator — a shunting-yard over a `MeasureTerm` quantity/scalar family whose family rules demand one quantity family for `+`/`-` and exactly one scalar operand for `*`/`/`, so `2*1200mm + 300mm` reads as a length and `1200mm + 45°` refuses by name, with a bare magnitude taking the role's own elected unit and a suffix resolved through the package parser under the role's family. `SelectionReadout` seats the live selection figure as a `ChromeContent.Pane` on the status centre under `MeasureRole.Extent`, so the footer figure renders through the one `ShellChrome.Readout` fold.
[DISPLAY_POSTURE_FAMILY]-[COMPLETE]: display postures became one family on the override channel at `Render/pipeline.md` `[04]-[VIEWPOINT_CODEC]`. `VisibilityAction` gained the `Highlight` row beside `Xray` — the TRANSIENT hover posture ghosting lightly and emitting a row for EVERY element so a scan across a table clears the previous hover instead of strobing, against the user-issued verb that ghosts hard and emits nothing for the picked set — and `HighlightChannel` landed as the pipeline end its three named consumers read (`Charts/telemetry#METRIC_PANEL` row hover, the measure panel's row hover, `Editing/history` touched-element projection), with `Clear` publishing the explicit empty and `Over` composing a highlight atop a posture by last-writer-per-element while preserving the posture's own tint. `PropertyDomain` DERIVES categorical-versus-sequential from the values themselves and elects palette and legend TOGETHER — `Colormap.Tableau` under `ColormapClass.Qualitative` for categories, `Colormap.Viridis` for magnitude, and a `LegendSpec` on the `LegendDomain.Categorized`/`Continuous` arm — with `Position` sampling a categorical member at its ordinal share and a sequential value in its measured extent, a degenerate extent sampling the ramp midpoint rather than painting every element its first colour. `ParticipationRole` landed target/obstacle/excluded with the obstacle row distinctly inked because a context building left as a target is the most expensive analysis mistake and is invisible in uniform shading. `DisplayPosture` is the three-case fold — colour-by-property, participation, precision wireframe — every case answering the same `VisibilityOverride` rows over resolved `(element, value)` pairs so a posture is pure, composes with isolate/hide/x-ray through one merge, is captured by a viewpoint, and is stepped by an animation visibility track with no posture-aware consumer anywhere. Also landed on this page: the `Strokes` fidelity repair banding by `(Ink, Style, Pigment)` over one scratch paint with per-band dash disposal and a single path-release sweep, mirroring the widened Charts `Record` walk exactly.
[SHEET_COMPOSER_SURFACE]-[COMPLETE]: sheet composition earned its editor as new cluster `libs/csharp/Rasm.AppUi/.planning/Render/drafting.md` `[06]-[SHEET_COMPOSER]`, over placement rows widened IN PLACE rather than a second frame model. `SheetRegion` gained its source named-view key, its layer-context overrides, and a `NorthPosture` whose `Oriented` composition rotates the frame through the new `ProjectionBasis.Roll` — an in-plane rotation applied after the projection and before the scale, so a north posture never mutates the saved camera a frame merely names and two frames of one view state two norths; the rect IS the crop, so no second extent exists. `TitleBlock` became a BOUND projection over the eleven-row `TitleField` family, each row deriving its own label key and reading its value off the block, with `TitleLayout`'s cell pitch derived from the roster so an added cell re-spaces every standard instead of overflowing its frame. `SheetCard` landed as the placeable, per-option re-bindable metric card rendering through the sheet's own unit frame off the new `DraftSeams.CardFacts` reader, sharing ONE bounds predicate with the frames. `FrameEdit` is the eight-case verb family whose `Rewrite` folds onto the placement row, and `SheetComposer.Apply`/`Rebind` re-run `Sheet.Compose` so an interactive edit passes the identical admission an authored sheet passes. `PlotColor` landed the plot-colour posture carrying its pigment election and its `Document/export` `ColorTarget` device row — monochrome collapsing to the policy ink, greyscale walking the READABLE role ladder by a weight rank computed once off the style roster, colour targeting the press — and `PlotCanvas` landed the display scale that never reaches a printed weight, so `SheetComposer.Preview` renders the emitter's OWN `DraftEmit.Page` run through `DraftEmit.Raster` under paints differing by the display scale alone. ALSO landed here: the locale-owed repair. `DraftUnits` became a `UnitPosture` reading its length and angle tokens off `MeasureRole.Distance`/`Angle` rather than authoring a pair beside them, its `Text` pinning the SHEET's posture onto the locale's measure policy while keeping the locale's denominator, formats, and grammar; `Dimension.Entities`, `Dimension.Label`, and `Annotation.Entities` moved onto the `Fin` rail naming their readout role at every call site, the redundant per-limb reprojection deleted, and surface roughness stated as the one carve-out rendering in its authored unit.
[XR_REVIEW_CHROME]-[COMPLETE]: immersive review earned its interaction plane as new cluster `libs/csharp/Rasm.AppUi/.planning/Render/immersive.md` `[06]-[XR_REVIEW_CHROME]`. `XrPanel` is a world-anchored `CompositionLayerQuad` over its OWN swapchain — pose in the reference space, extent in metres, pixel extent DERIVED from that extent times the panel's pixels-per-metre so two sizes for one surface are unrepresentable — rendering the SETTLED `ControlIntent` tree through the one control factory and paint catalog, so a button in a headset and a button on a desktop are one row with one command key; `XrChrome.Mount` creates the swapchain against the shared device and pushes its handle onto the session ledger, `Paint` brackets the raster in the same acquire/wait/release triple an eye pass takes and contributes its own `(pass, elapsed)` row to the frame receipt, and `Layers` chains the quads ABOVE the projection layer in the one `EndFrame` array. `XrChrome.Aim` intersects the controller aim ray against each panel's plane and answers PANEL PIXELS with the surface's own Y-down convention, so every desktop hover, hit-test, and press path is reachable unchanged and the nearest forward hit wins rather than seq order deciding which of two overlapping panels a user can press; a hit outside the extent answers `None` rather than clamping to an edge. `XrComfortPolicy` landed the comfort rows as POLICY — `XrLocomotion` carrying its own vignette DEMAND (teleport produces no optical flow at all, so a vignette strength under it is a knob with no effect), the strength read only during motion, `Turn` quantizing yaw under a snapping mode, and `XrStance` selecting the reference space (`Local` seated, `Stage` standing) so a recentre is a space re-creation rather than an app-maintained offset. `XrAnnotation` binds a completed anchor's `Space` and holds no pose of its own, and `XrReviewVerb` names six controller-reachable verbs by their existing command keys, resolving against the frozen deck so a row naming an absent key is a roster defect surfaced at bind. The catalog gained `CompositionLayerQuad`, `Extent2Df`, `EyeVisibility`, and `ReferenceSpaceType` rows, all decompile-verified.
[TIMELINE_EDITOR_SURFACE]-[COMPLETE]: the keyframe timeline became an editor as new cluster `libs/csharp/Rasm.AppUi/.planning/Render/animation.md` `[06]-[TIMELINE_EDITOR]`, over a track union and clock widened in place. `Track` gained the `Media` case whose `MediaCue` maps a timeline instant onto a SOURCE instant, with `TrackInterp.Cue` interpolating source time while holding the media key — so scrubbing drives the frame the video shows and the playhead is the one time authority — plus `Instants`, `Easings`, and `Rebuilt` as the projections the editor reads, so a seventh track kind cannot ship a lane with no glyphs and every structural edit re-enters the `Of*` admission rather than rewriting a carrier the binary-search bracket then walks unsorted. `Playhead` gained frame-indexed in/out bounds (a wall-clock range over a frame-indexed clock re-introduces the exact rounding drift the index deletes) that `Advance`, loop, and ping-pong reflection all read, with `Ranged` refusing an inverted range by name. `LaneRow`/`LaneBoard` landed per-track lanes with ONE audibility resolution — a non-empty solo set narrows and mute applies only where nothing is soloed — applied by the editor as a composition through `Heard` so the sampler never learns about lanes, beside one pixel projection the glyphs, playhead, range handles, and drags all share. `KeySnap` is the five-row snap ladder each carrying its own candidate reader over a `SnapContext`, resolved by nearest-within-reach in PIXELS because a time-valued reach snaps differently at every zoom. `KeyEdit` is the five-case manipulation family addressing keyframes by ORDINAL (two keys can share an instant mid-drag), with `Retime` scaling about an anchor as one ratio and a delete that would empty a track refusing where the user is rather than at the gate. `TransportVerb` landed the ONE transport grammar — play, pause, stop, step, jump-to-bound, loop cycling the three modes, and speed walking its own ladder — each row carrying its command key and its state fold, with `TransportState.Tick` deriving the cadence so speed scales the WAIT and never the frame step; the media transport at `Document/media` reads this same roster.
[TABLE_GRID_CLOSURE]-[COMPLETE]: the grid reached professional closure at `libs/csharp/Rasm.AppUi/.planning/Editing/tables.md`, all four clusters rebuilt. `[02]-[GRID_SUBSTRATE]`: `TableCellSlot` replaced the five-parameter kind delegate, `TableCellEdit<TRow>` landed as the ONE edit authority (its `Set` fold is the editor commit, the paste cell admission, and the cell-validation producer; its absence IS the read-only verdict), `TableCellKind` gained `Spark` over `Sparkline.Render` and its `Template` row now writes `CellEditingTemplate` from that same value — the CORRECTIONS repair for the editable template column that entered edit with no editor — with `Admit()` as the new kind/slot/measure/classification reconciliation; `TableColumnMeasure<TRow>` landed as the numeric axis over a `TableMeasure` union (`Quantity` folding through `ResolvedLocale.Quantity` under its `MeasureRole` and converting to the elected unit before aggregating, `Scalar` through the resolved `Formats`), carrying the `AggregateSpec` roster the grouping fold and the footer both read and the `TableCellFormat` whose `ThresholdList.Cell` colour paints the value-driven format cell — the dashboards threshold family's tables-side consumer; `TableFilterOp` closed the seven-row per-column filter vocabulary and `TableSurface` gained `Predicate`, `Specs`, `Totals`, and `Grouping`. `[03]-[VIEW_STATE]`: the snapshot gained `Seq<TableFilterRow>` plus `FilterPace` (throttle-merged-with-sample, replacing the single unpaced global scan) and `Seq<TableColumnState>` for the column axis, `Apply`/`Capture` moved onto `DataGrid`, and the sort axis was re-cut onto the control's own descriptor identity — `FromComparer` for a comparer-bearing column, `FromPath` otherwise, capture mapping back through `HasPropertyPath` or `DataGridComparerSortDescription.SourceComparer` — with the multi-sort gesture law stated from the control's own `ProcessSort`. `[04]-[TREE_FLATTEN]`: `TreeRow<TRow>` DELETED for `FlatNode<TRow>`, `TableProjectionFeed` carries rows, window bounds, and the band roster off one subscription, the `Virtualized` case took `IComparer<TRow>` (dropping the `Func<Seq<TKey>> Order` the comparer authority made redundant) and its window now reads `ExtentLedger.StartIndex`/`Size`/`Live`, the `Grouped` case routes to `GroupFlatten.Grouped` with a roster-minted `GroupPlan` and splits band nodes onto `GroupHeaderStamp`, and `PivotFold.Cross` was rebuilt as one `GroupBy(ColumnAxis)` per row group under a stated `ColumnCeiling` with the no-column-recycling verdict. `[05]-[GRID_COMMIT]`: `PasteFold` landed the RFC-4180 `PasteScan` parser, whole-block seat admission, and fill-down over the same fold, `TableCommit.Batch` executes N persists under one intent invocation, `Attach` gained the `CellEditEnding` `DataValidationErrors.SetErrors` write that makes `:invalid` reachable, and `Encode` streams into one `ArrayBufferWriter<byte>` testing the byte ceiling per row — the CORRECTIONS repair for the join-then-`GetBytes` double materialization.
[HISTORY_TIMELINE_SURFACE]-[COMPLETE]: the revert algebra earned its timeline at `libs/csharp/Rasm.AppUi/.planning/Editing/history.md`. `RevertibleOp` widened with the `Actor` column the durable arm reads off `OpLogEntry.Actor` and a `Touched` projection a composite answers from its children, `RevertKind` gained the glyph key its row paints, and `RevertDelta.Children` became the total disclosure projection. `ClientLog` landed as the ONE client-side typed-op roster beside the recorder — replacing the `ClientHead` delegate column on `RevertScope`, so a lane is an instance of the algebra and the timeline projects real ops rather than the command names the recorder retained — with `RevertDirection.Reach` as the one head-index column. `RevertScope.Walk` landed as the absolute jump (N single steps, TOTAL `RevertWalk` receipt keeping the applied prefix), `SolveGate`/`SolvePosture` batch a scrub into one regeneration, and `EditHistory` gained the actor, fault sink, and `Jump` entry. New cluster `[05]-[TIMELINE_SURFACE]` renders it: `TimelineKey` self-ordering, `TimelineEntry`, `RevertPhase` (applied/marker/suppressed/refused carrying ink, inertness, and lane) deriving roll-to-here by subtraction, `TimelineBand` folding the four landed `OverviewLane` rows over one predicate column, and `TimelineSurface` composing `HierarchyFlatten` for composite disclosure, `VirtualWindow.Lease` for the virtualized strip, `VirtualWindow.Overview` for the `OverviewFrame` feed under `OverviewAxis.Vertical`, `ControlIntent.Tree`+`ControlIntent.Overview` for the body, and `VisibilityAction.Highlight` over `RevertibleOp.Touched` for the two-way cross-highlight. The `Point`-to-payload seam landed as `ScrubPoint` lowering onto `CommandPayload.Fields` with `HistoryIntents.Scrub` as the lifting arrow — the probe-backed repair for `ReactiveCommandBase.ICommandExecute` throwing on a non-payload parameter — and the fabricated `Seq.At` in the durable arm was repaired to `Seq.Skip(offset).Head`.
[STROKE_LABEL_COLUMNS]-[COMPLETE]: the custom plane's ink gained mark geometry and a declutter policy at `libs/csharp/Rasm.AppUi/.planning/Charts/custom.md` `[02]-[SKIA_KINDS]` — `StrokeStyle` landed as the mark vocabulary (fill, hairline, solid, dashed, dotted, dash-dot) carrying paint style, the `MetricFamily.Stroke` STEP its width resolves from, dash intervals in width multiples, cap, and join, with one `Write` returning the band-scoped `SKPathEffect`; `VisualStroke` widened to carry it and every open-contour fold now names its row, which is the repair for the pinned `SKPaintStyle.Fill` that rendered network edges, parallel-coordinate polylines, geo arcs, and trip legs as nothing at all, and `Record` re-cut its banding from distinct INK to distinct `(Ink, Style)` so one kind mixes a filled mark with a stroked one in a single emission (network edges beside node dots, trip legs beside their heads). Labels became `LabelMark` carrying anchor, `LabelPlacement` row, and the element's own significance as priority, decluttered by `LabelPolicy` through a priority-ordered `SKRect.IntersectsWith` fold with padding, edge nudging, and a caption ceiling, the survivors' count and drops sealed onto the record as `Suppressed` and contributed on its own instrument. `CustomVisualStyle.Of` re-cut onto TYPED vocabulary — `PaintFamily` supplying both anchor role and colormap, `ChartChrome` supplying the label rung and alpha, `TypographyRole` the text style — resolving through `ResolvedTheme.Paint` on the `Fin` rail with a total generated stroke-width table, and the label channel split into the measure-then-draw `LabelChannel` over one `ShapedCache` lease. `VectorTwin` stopped re-running `Layout`: the record now carries `Seq<VectorMark>` sealed by the one pack and counted against the same `RecordCeiling`, so the export leg replays instead of packing twice. `Radar` DELETED per the polar split — `ChartSeriesSpec.PolarLine` over `ChartCanvas.Polar` ships the axes, hit testing, tooltip, and animation the hand-rolled trigonometric path lost — and the sparkline REFUTATION landed on the catalog boundary, closing it as `Sparkline.Render` over the chart rail rather than leaving the absence silent.
[SCHEDULE_GRAMMAR]-[COMPLETE]: the lane primitive grew planner semantics as new cluster `custom#PLAN_GRAMMAR` — the four-column span roster was REPLACED by `VisualPayload.Plan` carrying `PlanTask` (scheduled interval, baseline interval, progress fraction, critical flag, milestone flag, and the state a status reading merges on), `PlanLink` with the `PlanLinkKind` four-modality anchor pair mirroring the planning owner's own wire keys, the data date, the non-working spans, the tier rulers, and the resolved locale. `PlanScale` is the one instant-to-pixel projection floored to the coarsest tier that every emitter crosses, `TimescaleTier` is the year/quarter/month/week/day ruler catalog whose `Floor`/`Step` columns drive one `Cells` calendar walk and whose `LocalDatePattern` binds the viewer's own culture, and the gantt fold emits shading, tier rulers, dashed baseline bars under current bars, clipped progress fills, milestone diamonds, lagged dependency elbows anchored by kind columns, and the data-date rule. The timeline row landed beside it over the SAME payload, merging equal consecutive states per track and preserving null gaps. `PlanFeed.Of` is the planning receipt seam — every column a READ off `ScheduleNetwork`, `CriticalPath`, and `WorkCalendar`, with the dependency modality projected on the `Fin` rail rather than defaulted — and the CPM solve, float derivation, and calendar arithmetic stay the Bim owner's, which needed one surgical extension: `WorkCalendar.NonWorking` landed at `libs/csharp/Rasm.Bim/.planning/Planning/schedule.md` as the enumerable complement of the working-time walk, merged where spans abut, since `WorkingBetween` measures content and cannot say where the gaps are.
[BASEMAP_DARK_ROWS]-[COMPLETE]: the geo plane joined the dark identity honestly at `libs/csharp/Rasm.AppUi/.planning/Charts/basemap.md` — `BasemapSource` landed as the tile-source catalog (osm, carto-light, carto-dark) carrying URL template, zoom band, `Attribution`, and dark-variant affinity with `For(ThemeVariantRow)` selecting by variant, so a dark basemap is a SOURCE SWAP and the boundary states why a post-effect is unspellable (the raster path exposes no tint, blend, or colour-matrix hook). `TilePolicy` landed the cache-and-agent posture over `FileCache` and a per-source `ConfigureHttpRequestMessage` agent write, `Map.BackColor` seats the resolved surface token, and ATTRIBUTION became a `Build` admission — a tile row whose source declares no credit refuses by name rather than shipping a licence breach — surfaced through the layer's own `HyperlinkWidget` that `GetWidgetsOfMapAndLayers()` publishes. `TileHealth` landed loading, ready, failed, and offline as rendered facts folded from each layer's `Busy` transitions and its `DataChanged` exception payload, with socket failure classified offline and everything the server answered classified failed, over a `Watch` subscription and its own declared instrument. `WidgetRow` replaced the bare widget factories with rows carrying the map-taking constructor each signature actually demands (`ScaleBarWidget(Map)`, `MapInfoWidget(Map, …)` — the zero-arg mints did not exist) beside the token re-tint each widget's own colour members take, matched to a mounted widget by a declared `Shape` type and crossing from `ChartInk.Tint` into the map's colour value at one place. Pick gained `PickPolicy` tolerance resolved through the hit's own `MapInfo.Resolution`, multi-hit disambiguation over the whole `MapInfoRecords` roster ranked nearest-first by the admitted geometry engine's `Distance` (an absent distance admitted last rather than dropped), and `Hover` answering the top hit with its ambiguity count. `GeoOverlay.Choropleth` landed the graduated seam projecting `Colormap.HeatMap` stops through one `ColorBlend` into the package's own `GradientTheme`. `Navigate` unified onto `IO<Fin<Unit>>` with per-verb argument admission, and the redline commit landed its missing recorder hop: `RedlineSession` snapshots the target's prior payload off the marks layer BEFORE `EndEdit` seals, every durable verb records one `RevertDelta.Set(before, after)` through `EditHistory.Record` with the same layer-write apply fold the committed-intent re-render uses, and the payload rides the feature beside its id so the next snapshot reads the exact ledger bytes.
[FORM_SECTION_ROWS]-[COMPLETE]: the sectioned form grammar landed at `libs/csharp/Rasm.AppUi/.planning/Editing/forms.md` — `forms#FORM_SCHEMA` gained `FormSection` as the wizard-step shape carrying its own skip gate, which DELETED `WizardStep` and the parallel step roster whole (the flat form and the wizard now walk one partition, and `FormSchema.Create` refuses a roster that is not a partition of the field set, so a field rendering nowhere is a construction fault); `FormField` widened from seven columns to eleven with entry row, requiredness predicate, declared fallback, help key, measure, and commit posture, and the per-field `AdmitValue` closure collapsed onto the nine-row `FieldEntry` family whose rows declare which control intents they may sit behind and own the whole erased-boundary fold, checked against the control at construction. `FieldMeasure` landed the unit column — parse against `QuantityInfo.ValueType` through the boxed quantity parse, family agreement, and clamping on the scalar in the `MeasureRole`-elected display unit, with the reason `UnitMath.Clamp` is unreachable (its `IComparable, IQuantity` constraint needs the closed family type) stated at the site; expression entry rides the `Rasm.Compute` symbolic owner end to end (`SymbolicBuild.Build` over `BuildSpec.Infix`, free-symbol binding from sibling numeric values, `SymbolicExpr.Evaluate`), and `FormField.Formula` derives its dependency edges from the expression's own free symbols so the QuikGraph cycle oracle sees every real edge. `FieldState` landed as the ranked five-row ink axis over a `FieldFacts` predicate table with `ValueOrigin` as the provenance axis carrying its own resettability, `FormFilter`/`FilterFacet` landed in-panel parameter search over the resolved culture's `CompareInfo`, and `FormSurface.Layout` rebuilt into `Plan` (pure `SectionPlan`/`FieldPlan` projection with the read-only display string resolved once) plus `Panel` (the nested-intent fold a head with no form mechanism still receives). New section `forms#FORM_CHROME` landed the materialization capsule over the admitted `Form`/`FormGroup`/`FormItem` mechanism with `FormGeometry` in the mechanism's own types, the four-row `SectionChrome` family (grouped, divided, boxed, described) carrying its row and host columns, the `Anchor` section-rail scroll-spy, and the trailing operation cluster seating the provenance badge and the reset verb. Probing corrected three premises the page had assumed: the form row's label, required, and no-label properties are ATTACHED and the host copies them onto the row it wraps a bare control in, label-for association is the control's own re-targeting hook, and no `:required` pseudo-class exists at all. The stale `new IntentBinding("surface", None, None, None)` construction is gone with the fold that carried it.
[PENDING_COMMIT_BATCH]-[COMPLETE]: the pending-commit posture landed at `forms#BATCH_EDIT` — `CommitPosture` is a per-FIELD row (deferred marks, immediate writes through) carrying its own seat fold, so a live slider and a costly parameter share one write entry and the expensive-solve gate is schema data rather than a caller branch; `PendingForm` holds committed state beside its marked map with `Projected` shadowing committed values so validation, visibility, dependency propagation, and the section plan all read what the operator typed before any of it commits, `Cancel` drops marks alone so committed state is never rewritten to undo an uncommitted edit, and `Apply` is ONE command execution — the projected state validates whole, the marked keys cross ordinally sorted so the payload digest is stable, the schema's own commit intent runs once under one correlation, and the batch records as a single `RevertDelta.Composite` of per-field `Set` deltas. `ParameterLane` landed the parameter-scoped revert lane as an INSTANCE of the settled `Editing/history#REVERT_SCOPE` algebra rather than a second one — its own recorder, content identity, cursor, and stamp, with its durable window answering empty by construction so a turn past the client window seals `NothingToUndo` instead of reaching the document ledger, and a pushed-op roster beside the recorder it mirrors so a turn's receipt names the op rather than the delegate pair. `ParameterSet` landed value-set export and import with members re-admitted applicatively against the pre-import state and each stale member named individually. `CommitReceipt` and the commit instrument pair landed beside the existing batch fold.
[STUDY_RECIPE_FORM]-[COMPLETE]: the study input surface landed as new section `forms#STUDY_FORM` — `StudyRecipe` carries an integer revision and its declared `RecipeInput` rows, `RecipePin` is the pin-or-track axis and `RecipeCatalog.Resolve` REFUSES an absent pin rather than falling forward (a study silently re-configured against a newer recipe is a result nobody can reproduce), `RecipeInputKind` is the four-row concern family (source, number, words, choice) each carrying the control intent it materializes and the admission row that intent sits behind, and `StudySchema.Compile` projects identity, required, and optional partitions onto one `FormSchema` under the landed section grammar — so a study form IS the form surface and no per-analysis screen exists to drift from it. A number input compiles as an EXPRESSION field so `span / 3` is admissible wherever a study takes a dimension, per-input help compiles into the field help key the chrome row seats as its hint, a recipe-declared live input compiles under the immediate posture while every other input batches into one apply, and study identity is two ordinary fields validating on the same rail. `StudySchema.Submit` validates the projected state and runs the schema's submit intent once, sealing `StudySubmission` over the settled `CorrelationId` and naming no queue type.
[VIRTUALIZATION_GROUPING]-[COMPLETE]: grouping landed as a first-class projection of the one windowing fabric at `Shell/virtualization#HIERARCHY_FLATTEN` — `FlatNode<TItem>` became a `[Union]` of `Row` and `Band` carrying the shared indent columns, so a synthetic group heading is a REAL node with its own key, its own place in the extent ledger, and its own entry in the screen-state expansion set; `GroupFlatten.Grouped` folds `DynamicData.Group` through `TransformOnObservable` into a per-group slice combining the group's own `Cache.Connect()` collection with its aggregate streams over one published connection, so a member edit re-emits its group alone and a collapsed group emits one node while the ledger retires its members' ordinals like any removal. `AggregateMeasure` (count/sum/avg/min/max) carries one generic fold over the `DynamicData.Aggregation` operators, `AggregateSpec` is the per-column request and `AggregateCell` its column-addressed answer, so a band subtotal and the tables grand total are one shape at two altitudes and `GroupBand.CountColumn` is always present. The delegate-over-realized-rows grouping is DELETED: `StickyProjection` shed its `groupOf` and `depthOf` delegates, retargeted onto the `FlatNode` window, and now pins the nearest realized `Band` above the viewport top — so the pinned header carries the group's own label and aggregates instead of a name re-derived from a row, which could neither collapse nor count nor occupy an ordinal. Probing corrected a landed premise: `Virtualise` takes an `ISortedChangeSet` and an `IVirtualRequest` stream, so `OrderedChangeSet` collapsed from a change-set-plus-order-snapshot pair to a change-set-plus-COMPARER pair, `Realize` sorts before windowing, and `ExtentLedger.Admit` takes the sorted change-set whole and reads its ordinal projection off that same value's `SortedItems` — one ordering authority where two could disagree.
[OVERVIEW_STRIP]-[COMPLETE]: one minimap primitive landed across both owners — the downsample model at `Shell/virtualization#OVERVIEW_PROJECTION` (`OverviewFrame` content-space bounds/viewport/lanes, `OverviewBand` with its typed `OverviewLane` rows for change, search, error, and selection marks, `OverviewAxis` carrying fit and drag freedom in one row, and `OverviewScale` the ONE transform whose `Project` and `Locate` read the same factors so a mark and the click that lands on it address the same content offset), and the control at `Shell/controls` as `ControlIntent.Overview` plus `OverviewStrip : AuthoredControl<OverviewStrip>` under the `tokens#CONTROL_THEMES` authoring capsule with its `overview-strip` skin row minted at both tiers. Producers publish CONTENT SPACE and never pixels, so a resize re-scales one transform instead of re-deriving every mark at every producer; lane marks carry their lane key and semantic paint role as style classes so the control theme paints them and the strip writes no brush; click-to-jump and drag are the same publish through the intent's jump command, so the strip never owns a scroll position and the surface entitled to refuse the move is the one that owns it. `VirtualWindow.Overview` is the virtualized-list producer over `ExtentLedger.Total`, so a long list's strip and its scrollbar cannot disagree.
[PALETTE_PRESENTATION]-[COMPLETE]: the command palette became a presentation surface over the federation at `libs/csharp/Rasm.AppUi/.planning/Shell/commands.md` — `commands#PALETTE_FEDERATION` rebuilt the provider shape whole: a `PaletteProvider` now answers an `IObservable<PaletteSlice>` whose slice carries the leg's rows AND its `PaletteStatus` (pending, streaming, settled, refused-with-its-error) on ONE emission, each leg lowers through `EditDiff` so a narrowed answer removes what it dropped, and the legs merge through `MergeChangeSets` under the one `PaletteHit.ByRank` comparer that also orders the result list — decompile proved `ToObservableChangeSet` upserts and never removes, so the additive lowering would have kept every earlier query's hits. `PaletteKind` and `PaletteScope` landed as typed vocabularies: a scope prefix parses off the raw query and narrows which legs open BEFORE any ranking, and the kind row carries the reveal verb that makes activation ONE fold — a command hit invokes itself, every other kind invokes its kind's verb with the hit key. `PaletteHit` widened to icon, secondary text, kind-and-source badge, and gesture chips with grouping DERIVED from the badge. `commands#PALETTE_SURFACE` landed the top-anchored overlay as a frame STACK (`Results`, `Actions`, `Arguments`) so a per-hit action panel recurses in place and retreat is one pop, with the action roster a filter over the one command table through the new `CommandIntent.Targets` column, inline typed argument mini-forms materialized through `FormSurface.Plan/Panel` off the new `CommandIntent.Arguments` schema column and committed through `Compose` onto the new fifth `CommandPayload.Fields` case, and honest loading, empty, and broken states derived as READS of the feed's status map. The `CommandProjections.Score`-is-private correction landed structurally: every anchor now goes through `Federate` and `deck.Search`, and `deck.Raise` is the one non-wire invocation every path ends at. `Document/search#RANKED_WINDOW`'s dropped-source gap closed at that end too — `SearchPlane.Provider` now streams source-badged hits and drives its own bound `Run`, which deletes the app-root ordering obligation that made an undriven plane answer a stale window. Palette overlay, keycap, and badge control-theme rows landed at `Theme/tokens#CONTROL_THEMES` with their authored arms; the canvas seating landed as the `DialogIntent.Palette` case and route at `Shell/dialogs#SESSION_ALGEBRA`, binding the `OverlayShape.Palette` row that had no dispatcher.
[SHORTCUT_EDITOR]-[COMPLETE]: the binding law earned its editing face at `commands#BINDING_EDITOR` — `BindingOverlay` landed as the per-user gesture-and-alias overlay folded AHEAD of the deck freeze, so the authored table stays data a reset restores, an explicit unbind is a present entry carrying `None` rather than an absent one, aliases widen the freeze-built palette index (and the duplicate-identity admission now spans labels and aliases together), and a whole named keymap swaps by naming one `ShortcutPolicy` row on the options rail exactly as the theme section swaps a variant. `CommandDeck.Claimants` landed as the ONE chord-ownership oracle and `GestureConflicts` collapsed into a filter over it, so the freeze's refusal and the editor's assignment refusal are the same computation and a conflict surfaces at assignment instead of at dispatch. `ShortcutEditor` projects every command bound and unbound with its `BindingSource` column and its live contested list, `ShortcutProbe` makes search-by-text and search-by-captured-keystroke one polymorphic entry, and `CheatSheet` groups the same rows by the `CommandScope` that owns their attachment. The capture cell landed as `KeycapCell` over the shipped `Ursa.Controls.KeyGestureInput`, and decompile pinned two traps the page now states: `ConsiderKeyModifiers: false` writes the bare gesture and then FALLS THROUGH to the modifier switch that overwrites it (so it strips nothing and only drops a keystroke class), and a lone modifier press under its own modifier records as a bare-key gesture of that modifier — refused on this page's rail. Seating landed as the `DialogIntent.Editor` case and its modal route at `Shell/dialogs#SESSION_ALGEBRA`, binding the `OverlayShape.Editor` full-surface row. The `activity.open`/`activity.clear` and `workspace.enter`/`workspace.save`/`workspace.reset` deck rows landed as `ActivityVerbs.Rows` and `WorkspaceVerbs.Rows` beside `ViewportVerbs`.
[OVERLAY_STACK_OWNER]-[COMPLETE]: the Ursa overlay canvas landed as a peer owner beside the DialogHost session stack at `libs/csharp/Rasm.AppUi/.planning/Shell/dialogs.md` `dialogs#SESSION_ALGEBRA` — `StackOwner` is the total case-to-stack projection and `OverlayShape` the canvas modality rows (palette, peek, drawer, full-surface editor) carrying depth tier, material tier, motion plan, modal contribution, and dismissibility with their own options mint; the seam LAW states the split as the RESULT HANDLE (one root's awaited close parameter cannot answer two decisions, so occupancy is a session invariant, while the canvas holds an ordered layer list where each layer owns its own mask, modal contribution, and awaited task and co-residency is the point), and the reservation now reads the stack owner first so a canvas modality never touches the session cell; the drawer's one owner resolved to `OverlayDrawer` on decompiled evidence that `Ursa.Controls.Drawer` is `[Obsolete]` and forwards every member verbatim; both registries proved dishonest about an absent host — the DialogHost static surface throws on zero, unmatched, and ambiguous identifiers while the Ursa registry is internal and answers an unregistered id with a silent no-op or `DialogResult.None` — so a mount-bound presence fact is the first admission of every crossing and `DialogFault.HostUnregistered` is its refusal.
[TOPOLOGY_KEY_DERIVATION]-[COMPLETE]: the authored product-named topology rows collapsed to a projection at `dialogs#SESSION_ALGEBRA` — `DialogSurface.Derive(profile, mount, seam)` mints the surface key as the host key crossed with the mount kind exactly as a catalog route key derives from its row key, the session identifier and canvas host id derive from that one key (which also forecloses both the static surface's ambiguous-identifier throw and the canvas registry's silent try-add drop), and `MountPolicy` carries stacking, click-away, blur, canvas admission, and the toast anchor per mounting shape; the six product rows are gone and the remaining table is the five-row projection source, so a new host substrate is one AppHost descriptor row and zero edits here.
[DIALOG_CHROME_COLUMNS]-[COMPLETE]: dialog chrome re-pointed onto the landed tier families at `dialogs#DIALOG_CHROME` — `ChromeColumns` carries scrim role, ring role, radius and stroke steps, `DepthTier`, `MaterialTier`, blur, and safe padding as ADDRESSES, and `DialogChrome.Bind` binds both overlay roots through the theme rail's resource observable so no resolved paint is ever written; the confirm intent gained its `ConfirmFriction` ladder (acknowledge, ordinal-exact typed verification against the target identifier with the affirmative's canExecute reading the one `Armed` predicate, and an inline tier driving an already-mounted `PopConfirm` with no re-parenting), the peek and drawer cases landed on the canvas stack with the drawer positioning through its own edge column rather than the session stack's popup positioner, the full-surface modal editor landed as the `OverlayShape.Editor` modality rather than a root of its own, and the retreat veto landed as a REAL fold over the mount-projected session stack that refuses before the close runs while the framework's own `DialogClosing` veto stays the independent second guard. Decompile corrected the premise: `DialogHost.Pop` is a content-keyed RAISE-to-front with no single-argument overload, so the page's raise verb is spelled honestly and retreat routes through `DialogSession.Close`.
[TOAST_BANNER_FAMILY]-[COMPLETE]: the notification family landed as one severity system at `dialogs#NOTIFICATIONS` plus a control arm — `ToastRow` gained the `Pending` row and a `Pierces` column, `ToastPlane` derives the shipped toast manager and owns what its show path forecloses (a correlation-addressed card, an in-place severity-and-body morph whose linger starts at the morph, a hover pause riding the toast plan's own hover intent, and a stack reflow through the plan's `Stacked` projection), the ceiling binds `MotionPlan.Toast.Cap` and the flush horizon `MotionApplication.ToastHorizon`, quiet hours enter the one `ToastGate.Admit` fold as its quiet term, and `ToastAnchor` is a PLANE column so a stack with two anchors is two stacks; the banner family materialized as `ControlIntent.Banner` at `Shell/controls` with `BannerSeverity` owning dismissibility as a posture, verbs and evidence as child intents over the command deck, and `BannerPlacement` as chrome classes; the shipped `Banner*`, `NotificationCard*`, and `ToastCard*` key families re-tint through `SemiCorrespondence` slot overrides under one severity fold at `tokens#TOKEN_CATALOG`, with `ToastCard`'s absent shadow key stated rather than fabricated. The dismissal instrument moved onto its own `AppUiTelemetry.CauseSlot`, minted at the evidence roster.
[ACTIVITY_CENTER]-[COMPLETE]: the persistent notification plane landed at `dialogs#ACTIVITY_CENTER` — `ActivityCenter` is a projection over the toast receipt stream and never a second evidence log, `Accrue` reads the CLOSE CAUSE first (a user-action seal was read and accrues nothing; a timeout, a displacement, and a drop accrue) with every warning and error row accruing whatever its cause, progress-bearing entries refuse dismissal on the rail while their fraction is short of complete so a remote invocation and a rendered button are refused by one rule, clear-all skips running entries rather than failing whole, per-entry invocation and clear ride command keys, `QuietHours` folds a midnight-spanning window under its own zone with per-source exemptions and feeds the toast gate's quiet term, the inbox realizes through the one virtual-window fabric over the correlation key, and the unread count publishes as one level instrument a chrome affordance binds.
[CHROME_FAMILY_ROWS]-[COMPLETE]: the chrome-slot family widened whole at `libs/csharp/Rasm.AppUi/.planning/Shell/navigation.md` `navigation#SHELL_CHROME` — `ChromeSlot` grew from four rows to seven (rail, HUD, and context joining menu, toolbar, status, tray) and each row now carries its OWN per-mount admission column plus the `Shell/solver` `ChromeProgram` it expands into, so the visibility matrix is slot-owned data a row predicate can narrow but never widen; content moved onto a four-case `ChromeContent` union (rail/toolbar `Entry` with icon, badge, flyout group, and the package's own `OverflowMode`; three-zone `Pane` carrying kind, `StatusZone`, and fact source; corner `Chip`; command-derived `Items`) and `ShellChrome.Materialize` resolves every case to one `ControlIntent`, with context items derived from the command deck by the target kind each row's `Accepts` set admits; `ProgressLocation.Select` landed as the status-versus-toast-versus-inline policy over expected duration and blocking posture; readout chips and status readouts bind `TypographyRole.Numeric` for tabular advance; the flyout origin re-seats from live placement through `ShellChrome.Origin`. Solver counterpart landed at `solver#LAYOUT_PRESETS` as the `ChromeProgram` row set with its panel-owner mint, and the breakpoint-tier-selects-program seam is stated at BOTH ends — `BreakpointRow.Rail`/`RailPosture` here, the no-width-column law there.
[DOCK_FABRIC_REBUILD]-[COMPLETE]: the dock fabric rebuilt at `navigation#DOCK_LAYOUTS` against the whole admitted surface — the local three-row `DockCapability` rename collapsed onto the package's own six-member enum; `DockZone`, `RegionRow`, and `RegionProgram` replaced the single hardcoded tool-plus-document split with a band-and-column fold over zone, orientation, proportion, and rank (contiguity and duplicate-zone refusals on the new `NavFault.RegionProgramRejected`); restore rehydration landed as `ShellDockFactory.InitLayout` binding `ContextLocator`/`DockableLocator` off the frozen roster so a restored graph carries live view-models and a stale id seals `NavFault.UnknownRoute` and drops; drop legality landed as policy ROWS on the package's own precedence ladder with the row writing the weakest rung so zone and root policies can still narrow, plus `DockGroup` for cross-group refusal; `PinnedPosture` landed alignment, display mode, and keep-visible with reveal on the preview trio; `DropVerdict` binds the drag ghost's `Status` to the manager's own `DiagnosticMessage` over recycled preview content; the dock's own overlay stack is REJECTED by boundary sentence in favour of the two admitted stacks while the drop selector stays admitted; `IDockState` is composed for the in-process workspace snapshot pair with the serializer kept as the cross-process carrier. The probe's variant-inert dock-theme verdict landed as the standing variant-coherence obligation on that boundary.
[WORKSPACE_PRESETS]-[COMPLETE]: the spatial fabric closed at `navigation#DOCK_LAYOUTS` — `WorkspaceRow` rows carry key, label, region program, chrome-visibility overrides, and default route with the enter/save/reset verbs named on the command deck, and `WorkspaceCell` switches them through per-workspace `IDockState` cells so switching away and back returns the exact arrangement; `SurfaceKey` landed the monotone per-(workspace, route) instance mint every persisted carrier partitions on; `DocumentInstances` spawns through the package's own `CanCreateDocument`/`CreateDocument`/`DocumentFactory`/`AddDocument` path; `TearOut` builds float hosts through the owned window chrome and `WindowPlacement.Clamp` folds every saved float rectangle against the live working-area set BEFORE `InitLayout`, which gives the `Shell/hosts` display-fact promise its one consumer; the restore order of the four persisted carriers landed as stated Law with its collision rule. The hosts display seam widened to carry real geometry — `SurfaceFact.DisplayChanged` now carries working areas rather than a screen count, and `SurfaceSeam`/`SurfaceRow`/`SurfaceSession` gained the `Displays` column a restore reads before any topology fact fires.
[WINDOW_CHROME_FAMILY]-[COMPLETE]: owned-window construction re-pointed onto one family at `libs/csharp/Rasm.AppUi/.planning/Shell/hosts.md` `hosts#HOST_AXIS` — `WindowChrome` rows (shell, tear-out, splash, bare) carry title-bar admission, the four caption gates, and the managed-resizer gate with one `Apply` column writer, `ShellWindow : ReactiveUrsaWindow<ShellRoot>` is the one owned window class with the reactive base election stated as LAW against the router's `IViewFor<T>` resolution, `ShellSplash : SplashWindow` owns the cold-boot handoff through `CreateNextWindow` under a countdown floor, and `WindowTitle` binds the caption as a projection of the active route rather than a write at any navigation site; both mount rows now build through the family, so the capture lane and the shipped shell differ by exactly the chrome columns their rows declare.
[LOCALE_ROW_COMPLETION]-[COMPLETE]: the locale row widened to the full projection concept at `libs/csharp/Rasm.AppUi/.planning/Theme/locale.md` — `locale#LOCALE_AXIS` carries per-script BCP-47 tails (`ScriptTags`, `Tags(Script)`) feeding `typography#SHAPING_RAIL` through the one `ResolvedLocale.Face` seam, a `CalendarSystem` column reaching the date pattern and the zoned projection, a `CollationPosture` vocabulary minting culture comparers and sort keys with the natural-ordering indexing trap stated, a per-row `Break` oracle answering `LineBreaker.Wrap` through `ResolvedLocale.Wrap` with `LocaleBreaks.Ideographic` carrying kinsoku line-start prohibition, and `PseudoPosture` over a length-banded `ExpansionBand` making proofing a first-class variant; `locale#MESSAGE_REGISTRY` landed the context-then-length variant walk over `MessageVariant`/`MessageLength` and key-coverage conformance reading each row's OWN satellite set with the pseudo rows exempt; `locale#CULTURE_COMPOSITION` widened `ResolvedLocale` with calendar, collator, and measurement columns, replaced the bare propagation delegate with the typed `LocaleSeams` record whose `ThemeLocale` column is the policy row the three `tokens#THEME_APPLICATION` Semi constructions read (string culture, not format culture), and re-cut the fault union onto the freed 6610 details; `locale#SPEECH_POLICY` landed `SpeechPosture`/`AnnouncementPhrase` as the vocabulary `Shell/accessibility` reads. Three rulings landed at the folder tier; `libs/csharp/.api/api-unitsnet.md` and `api-nodatime.md` gained the format-bearing render, customary-split, and calendar-conversion rows the fences bind.
[RTL_MIRRORING_LAW]-[COMPLETE]: one mirroring law landed at `locale#MIRRORING_LAW` — `MirrorSubject` closing the flipping set (layout flow, chrome zone, dock side, directional icon, breadcrumb, pagination, drawer and peek anchors) against the never-flipping set (numeric axis, geometry viewport, code surface, timeline, media transport) with each row naming its `MirrorMechanism`, one `Flow(LocaleRow)` projection every operation derives from so a never-flipping subject PINS left-to-right at its own root, `Order` and `Side` folding off that single predicate, and `Orphaned` proving every mechanism has a subject; icon mirroring stays wholly at `assets#ICON_AXIS` through the landed `IconRow.Mirror` kernel `Option<MirrorAxis>` column whose mechanism derives at the materializer from the resolved source, so this page contributes `LocaleRow.Flow` alone and the predecessor's locale-side directional-asset roster is deleted.
[MEASUREMENT_FORMAT_POLICY]-[COMPLETE]: one measurement policy landed at `locale#MEASUREMENT_FORMAT` — `MeasureRole` naming both unit tokens per readout with its grammar, decimals, and tabular declaration, `UnitPosture` electing the system, `MeasurePolicy.Render` converting through the elected token and folding architectural feet-inches with denominator election and GCD reduction, degree-minute-second angular rendering, and magnitude-derived decimal precision, `MeasureRole.Typography` declaring tabular participation against the `typography#TYPE_SCALE` numeric role, and `ElapsedGrammar` electing the coarsest nonzero unit off one calendar-accurate `Period` under the row's own calendar with the measured-duration grammar kept separate; `ResolvedLocale.Quantity` now takes its role, so a dimensioned value renders in its surface's elected unit and a bare scalar has no path to a measured label.
[ICON_SOURCE_COMPLETION]-[COMPLETE]: the asset vocabulary closed at `libs/csharp/Rasm.AppUi/.planning/Theme/assets.md` onto the KERNEL `Rasm/Interaction/asset` owner — the folder `IconSource` origin union, its `MirrorPosture` column, and the local `AssetKey` value object are deleted, `IconRow` survives as the AppUi registration row over `(AssetKey Key, AssetOrigin Source, PaintRole Tint, int Rung, Option<MirrorAxis> Mirror)`, and `IconSurface` composes each row into one kernel `IconRender` — origin, `IconPose` over the kernel `AssetExtent`, ordered `IconFilter` chain — dispatching over the whole `AssetOrigin` family with `Render` refused by name because the kernel draw replays onto an Eto target; host-typed payloads stay ROWS per the kernel's own law, so the FluentIcons `Symbol`/`Icon` pairs and the Semi resource keys ride a frozen `HostGlyph` binding table the `Vector` arm resolves through under keys disjoint from the avares catalogue's, and the fallback ORDER moved from the deleted case-rank fold onto declaration order in `IconCatalog.Rows`; the size axis stays a STEP on `Theme/tokens` `MetricFamily.Icon` with `Elected` gating on the package's own `IsAvailable` predicates, now admitted through `AssetExtent.Of` so ceiling, overflow-safe pixel count, and device extent hold one owner; the tint column stays `PaintRole` and rung read through `ResolvedTheme.Paint` and enters the render as `IconFilter.Tinted` carrying a `PerceptualColor`, with the chain folding to one colour and coverage before any arm draws, `Selected` refusing typed because selection is a token-role election, and `Greyscale`/`Fading` composing the kernel perceptual owner's `Simulate` and `Mix`; the `SKSurface` path rasterizer stays deleted for a resolution-independent `DrawingImage` over `GeometryDrawing` under one fit-and-reflect matrix, and both product forms stay collapsed onto one walk through `GlyphForm`/`AssetProduct`. `AssetProduct` and the budgeted cache STAY at the boundary under the kernel's host-specific-stays clause: an Avalonia `IImage` and a `Cursor` are shapes no `AssetRaster` case names, so the cache keys on kernel values and holds a host product, while the one arm reading the kernel raster carrier asks `RasterStack.Pixels` and uploads the answered rows under their declared `AlphaLayout`. `assets#CURSOR_ROWS` keeps its `CursorRow` vocabulary over a two-case `CursorOrigin` with the grab pair drawn; `assets#ASSET_CATALOG` carries both boot rosters — icon rows and host glyph bindings — deciding each glyph's tint and reflection axis once. Two rulings stand at the folder tier.
[ASSET_CACHE_LIFECYCLE]-[COMPLETE]: asset lifecycle landed at `assets#ASSET_CACHE` as one `AssetCache` owning every materialized product — a generation stamp raised from the `Theme/tokens` swap's own `Rematerialize.TintedAsset` roster so a re-seed can never serve a stale-tinted glyph, a byte ceiling over device-pixel cost with least-recently-touched retirement and a `BudgetRejected` refusal for a product exceeding the whole budget, two-phase retirement releasing a payload one cycle after unlink so no mounted surface holds a disposed image, and `Rescale` binding the `Shell/hosts` `SurfaceFact.ScaleChanged` fact to retire every product elected for the old backing scale; `CacheReceipt` seals both edges onto the evidence stream. `assets#SVG_PIPELINE` `ScenePolicy` gained `Cache`, `Filters`, and `Wireframe` columns with a fourth `Inspected` row and a `Mount` writer inverting the shipped control's negative filter spelling once; `assets#RASTER_ASSETS` `Pick` now answers the elected variant beside its declared scale so the receipt records what served; `assets#ASSET_CATALOG` replaced the preload boolean with a `PreloadPartition` set elected from `SurfaceMount`, so an embedded panel pays chrome alone.
[VFX_FOLDER_PAGES]-[COMPLETE]: the Vfx sub-domain landed as three pages under `libs/csharp/Rasm.AppUi/.planning/Vfx/` with router rows at the folder `README.md` `[01]-[ROUTER]` and codemap nodes at `ARCHITECTURE.md` — `material.md` carrying the two-arm layer algebra, the witnessed bounds-local-or-driven sample-invalidation contract with its `MaterialHost`/`MaterialOperation` in-tree vehicle, the seven-case per-draw `FilterRow` algebra (distant-lit rim and inset, displacement refraction, generated tint matrix, lerp crossfade, luma, generated curve tables, shipped high-contrast config), and the `MaterialSpec` capsule executing the token catalogue's `MaterialValue` and `WashRow` rows with an arithmetic-blender crossfade; `shader.md` carrying the closed four-program SkSL roster on the `CreateShader`-out-errors refusal rail with declared-name conformance, the `UniformFrame` reset-and-bind fold, the five-case `PathRow` pattern family, and the byte-ceiling `TileCache` with generation-guarded least-recently-touched eviction; `compose.md` carrying the closed `ComposeSlot` property-name vocabulary, the `ComposeTrack` mint with the duration-floor trap and the reduced-motion collapse to assignment, the expression-keyword trigger map, and the `VfxHandler` render-thread tick. `Render/capture#DRAW_CAPSULE` gained the one parameterized `SaveLayer` site (`LayerGround` two-arm union, `LayerSpec`) and the `FxEffect.Coloring` slot arm; `Diagnostics/evidence#FAULT_TABLES` gained the `Vfx 68xx` decade and the `EvidenceReceipt.Effect` case; four rulings landed at the folder tier.
[SURFACE_LADDER_ROWS]-[COMPLETE]: the appearance seed and its generated role ladder landed at `libs/csharp/Rasm.AppUi/.planning/Theme/tokens.md` `tokens#TOKEN_CATALOG` — `AppearanceSeed`/`RampPolicy`/`SurfacePosture` seed rows, `PaintRole` over a five-case `PaintDerivation` (tonal, posture, contrast-solved readable, bounded-chroma cast, alpha veil), `ThemeCatalog.Expand` folding in declaration order with a forward-reference refusal, typed `TokenKey` addressing every bucket, light and high-contrast emitted as `VariantProjection` projections of one generation, `Apart` gating adjacent rungs on the kernel `Difference` under the seed's declared floor, and `ThemeRail.ContrastCandidates`/`CvdCandidates` derived from the ladder rather than hand-listed; the derived-contrast primitive landed at the kernel owner as `PerceptualColor.ToneFor` with its `ToneSweep` row set.
[SEMI_SLOT_COMPLETION]-[COMPLETE]: total Semi slot custody landed at `tokens#TOKEN_CATALOG` — `SemiCorrespondence.Slots` covering the seven role-state families, the four disabled slots, the numbered surface/fill/text ramps with their colour-only twins, the link and highlight families, the global disabled set, the nav and overlay backgrounds, the focus trio, every role-named extent, and the elevation slots; `SemiExclusion` carrying nine verdict rows with their reasons; and conformance split across `SemiMints` at boot and `SemiCovered` over a `SemiRoster.Walk` reading in the headless proof lane, the walk descending `Styles.Resources` -> `MergedDictionaries` -> `ThemeDictionaries` -> `ControlTheme.Resources` per the ledger's witnessed mechanism.
[ELEVATION_MATERIAL_ROWS]-[COMPLETE]: the depth rebuild landed at `tokens#TOKEN_CATALOG` — the flat `Depth` tuple deleted whole and replaced by `DepthTier` rows carrying ordered `ShadowLayer` stacks (ring layer first, doubled dark alphas, inset top-highlight rim) resolving to `BoxShadows`, the high-contrast arm resolving an empty stack against the shipped dictionaries' absent shadow slots and substituting stroke widening, `MaterialTier` rows over the acrylic knobs with the opaque tinted fallback as the embedded floor, the scrim landing as a `PaintDerivation.Veil` role rather than a fourth family, module-keyed `WashRow` data with its luminance ceiling, and the new `SemiSlot.Shade` case minting the boxed shadow stack for every shipped elevation slot.
[SPACING_RADIUS_STROKE_SCALES]-[COMPLETE]: the generated metric scales landed at `tokens#TOKEN_CATALOG` as `MetricFamily` rows (base, ratio, step count, snap grid, density lens, high-contrast widening) with `MetricFamily.Inner` as the concentric nesting law, and `tokens#DENSITY_AXIS` widened from the two-value row to three `DensityPolicy` rows with `DensityRow.Elect` electing density per surface — the per-metric density column deleted.
[PREFERENCE_PROBE_FAMILY]-[COMPLETE]: the typed host-preference family landed at `tokens#VARIANT_AXIS` — `PreferenceRow` over appearance, increased contrast, reduced motion, reduced transparency, and text scale, `PreferenceValue` as its typed answer union, and `PreferenceCell` carrying the host read, the change stream, and disposable per-row pins for proof lanes; `PreferenceCell.OfPlatform` binds `IPlatformSettings.GetColorValues`/`ColorValuesChanged` for the two rows Avalonia publishes and routes the other three to the host-attach seam, with that gap stated on the boundary.
[CONTROL_THEME_ROWS]-[COMPLETE]: the control-theme table landed at `tokens#CONTROL_THEMES` with rows carrying target, base derivation, pseudo-classes, and token keys, beside the composition law on the boundary — shipped-arm truth (`BorderlessButton` disabled-only, `SolidButton` size-less, `OutlineButton`'s five intents), the focus trio binding, banner and toast slot overrides with the toast's absent shadow slot stated, and the generated `ButtonGroup` brush grid.
[AUTHORED_CONTROL_CAPSULE]-[COMPLETE]: the templated-control authoring capsule landed at `tokens#CONTROL_THEMES` — `AuthoredPart`/`AuthoredSpec` declaring parts, states, automation type, and token seats, and `AuthoredControl<TSelf>` resolving parts through `TemplateAppliedEventArgs.NameScope`, refusing a missing required part as a typed fault, owning the pseudo-class write face, and deriving its automation identity from the spec key through a `ControlAutomationPeer`.
[EDITOR_INSPECTOR_CHROME]-[COMPLETE]: the editor and inspector chrome landed on the token rail at `libs/csharp/Rasm.AppUi/.planning/Editing/inspector.md` — `inspector#CODE_EDITING` gained `EditorInk`, the sixteen-row correspondence whose gui-color key, owning `PaintRole` rung, and styled-property bind come from one table, feeding both the per-pane bind set through `ThemeRail.Bind` and the projected gui-color block `TokenRawTheme` hands the tokenizer, with the three pen members folding ink and stroke width from two dynamic reads, the `#RRGGBBAA`-versus-`#AARRGGBB` rotation stated, and the registration quirks recorded as boundary law (`ColumnRulerPen` under the Avalonia name `ColumnRulerBrush`, `TextArea.SelectionBorder` as concrete `Pen`, `SearchResultsBrush` writes dropped before panel mount); `EditorOptionsRow` landed as the enablement owner over the two false-by-default knobs the current-line and ruler inks need, correcting the page's former "defaults never render" claim; `IndentGuides` landed as an owned `IBackgroundRenderer` on `TextView.BackgroundRenderers` over `BackgroundGeometryBuilder`, with `InsertLayer` recorded as the refusing rail. `inspector#INSPECTOR_SURFACE` gained `InspectorChrome` — the category card as a `ControlTheme` replacing the one unpinned member against four local-value-pinned setters and a force-written presenter margin, the `CustomNameBlock` seam stated MANDATORY against the static-constructor foreground freeze, and the operation column's two-stage seam — plus per-row class tags through `[ControlClasses]` and the `CellState` pseudo-class write; `inspector#EDITOR_FACTORIES` re-cut the picker palette onto `TokenPalette` over the resolved ladder.
[EDITOR_BEHAVIOR_PLANE]-[COMPLETE]: the editor-behavior plane landed at `inspector#CODE_EDITING` — `LanguagePlan` projecting the registry's own `LanguageConfiguration` once into comment markers with a change-block-scoped `Toggle`, bracket pairs, auto-closing pairs honouring their `NotIn` and `AutoCloseBefore` columns, surrounding pairs, marker fold regions feeding the one `Fold` resync, and the enter rules; `PlanIndentation` binding those patterns onto `TextArea.IndentationStrategy`; `RasmRegistry` landing as the one product `IRegistryOptions` composing the bundled corpus, answering product scopes from its own rows, exposing `GetInjections` as the injection seam with embedded-language maps stated as grammar-JSON declarations beside the standalone `GrammarForScopeName` form, and carrying the file-backed `LoadFromLocalDir`/`LoadFromLocalFile` growth path; the fence-grammar closure widened from a three-row enumeration to `RasmRegistry.Scope`, a registry-backed product-then-language-id-then-extension lookup refusing by name; and the undo-ownership verdict landed as a plane partition — the rope-edit `UndoStack` against the cancelable-command recorder — with `DeclareChangeBlock` and `StartUndoGroup`/`LastGroupDescriptor` grouping named.
[EDITOR_FACTORY_WIDENING]-[COMPLETE]: descriptor-driven editor selection landed at `inspector#EDITOR_FACTORIES` — the accept column re-keyed from `Type` to `PropertyDescriptor` so declaration attributes reach the walk, `Presentation.Read` folding the whole shipped attribute vocabulary (trackable, progress, precision, increment, watermark, multiline, unit, path-browse, image preview, control classes) into one value with enum filtering collapsed onto the single `IEnumValueAuthorizeAttribute` contract, a `Flags` row ranked above `Choice` binding the shipped `CheckedMaskModel`, the operation column bound as the reset-verb and override-badge seat through the replacement and two-stage default seams, the grid's three chrome slots hosting the multi-select header notice, and the missing factory-contract members landed with their reasons — `Clone` against the base's `Activator.CreateInstance`, `HandleReadOnlyStateChanged` against the base's root-level `IsEnabled`, `HandlePropagateVisibility` against the base's null defer — beside the deletion of the `Accept` override that had narrowed a clone-admission predicate to the inspected type and kept the factory out of every grid's set.
[MIXED_VALUE_EDITING]-[COMPLETE]: honest multi-target editing landed at `inspector#INSPECTOR_SURFACE` — `CellState` as the four-row cell vocabulary carrying its pseudo-class, `MergedCell.Read` folding `GetValues` distinctness rather than the merged descriptor's disagreement sentinel and refusing a target array whose length does not match the descriptor count, `ApplyAll` fanning one write through the cell factory so N targets cost one command, one veto edge, one recorder entry and one receipt, the new `EditOutcome.Fanned` case carrying the target count distinctly from `Committed`, and `MergeNotice` naming both the target count and the merge remainder in the grid's top chrome slot; per-editor mixed forms ride the `CellState` pseudo-class every materialized editor now wears.
[CONFLICT_SURFACE_WIDENING]-[COMPLETE]: the merge surface reached editor grade at `inspector#CONFLICT_RESOLUTION` — `HunkSegment`/`HunkBands`/`HunkMargin` landing hunk bands as an `IBackgroundRenderer` over a document-bound `TextSegmentCollection` whose `UpdateOffsets` keeps every band live across resolutions, with the per-hunk verbs in an `AbstractMargin` sharing the bands' one `GetRectsForSegment` projection; `ConflictSide` gaining its `Both` arm as a concatenation with the `conflict.hunk-both` intent; the seven-delegate extraction collapsed to one `ConflictFacts` record; `DiffPolicy` parameterizing the former `LineCeiling` fence literal; and the diff-package census landing as a VERDICT on the cluster boundary — `Verify.DiffPlex` proof-cluster-only and two-way with no diff3 form, `LoroCs` merging operation history between frontiers rather than three unrelated texts, `JsonPatch` applying patches and computing none — so the alignment stays page-owned. `inspector#CODE_EDITING` `CompletionKind` weights likewise moved to the `CompletionPolicy` rows.
[DOCK_KEY_CLOSURE]-[COMPLETE]: the two consumed-but-undefined dock chrome keys landed at `tokens#TOKEN_CATALOG` — `DockSurfaceWorkbenchBrush` on the new `PaintRole.Workbench` well posture and `DockSeparatorBrush` on `PaintRole.Separator`, both registered on `SemiRoster.Minted` so the conformance rail admits a claimed key with no shipped definition only where the correspondence mints it.
[CONTROL_VARIANT_COLUMNS]-[DROPPED]: collapsed into [CONTROL_UNION_WIDENING] — the variant/icon/pending columns and the case additions widen one closed union whose switches break once, so two cards on the same three clusters were one polymorphic owner split in half.
[CONTROL_CASE_ADDITIONS]-[DROPPED]: collapsed into [CONTROL_UNION_WIDENING] with the case set widened to the full professional roster the union must carry.
[CHROME_RAIL_ROWS]-[DROPPED]: collapsed into [CHROME_FAMILY_ROWS] — rail, footer, and HUD are three row sets on one chrome-slot grammar and three separate cards guaranteed three incompatible column sets on one row type; the refuted rail-program and breakpoint anchors are corrected in the collapsed card.
[STATUS_FOOTER_PANES]-[DROPPED]: collapsed into [CHROME_FAMILY_ROWS]; the per-mount visibility truth (status renders only on status-admitting mounts) rides the collapsed card's boundary.
[HUD_CHIP_ROWS]-[DROPPED]: collapsed into [CHROME_FAMILY_ROWS] — chips reseat as a chrome-slot case with placement through the package's own corner vocabulary, deleting the screens-local row family and the parallel corner enum.
[DEPTH_STACK_REBUILD]-[DROPPED]: collapsed into [ELEVATION_MATERIAL_ROWS] — both cards rebuilt the same flat depth tuple, so the layered stacks, material rows, and slot case land as one token-case rebuild.
[MATERIAL_WASH_ROWS]-[DROPPED]: collapsed into [ELEVATION_MATERIAL_ROWS] with the wash policy and execution seam intact.
[CHART_LANE_POLAR_PRIMITIVES]-[DROPPED]: split at its seam — the lane/region half grows into [SCHEDULE_GRAMMAR] with real planning semantics, and the polar half lands in [CLIMATE_DIAGRAMS] over the package's own polar coordinate surface, whose existence the original card never reconciled.
[SEMI_CLAIM_REPAIRS]-[DROPPED]: correction-grade, not capability — the refuted-claim repairs (follow member, high-contrast variants, locale pins, and the custom high-contrast variant-key miss) ride the corrections pass over the token page, never a capability card.
[PANANDZOOM_ALIAS_GUARD]-[DROPPED]: correction-grade — the two-assembly type collision resolves as a reference-row alias plus a page-boundary repair in the corrections pass, with the third pan-zoom owner and the capability-parity question recorded there.
[INSTRUMENT_WRITER_CLOSURE]-[COMPLETE]: the card's six-page "unwritten" roster was stale for five of six (producers verified via the `Count` delegate and the evidence fan); the real remainder landed whole — `Editing/livedata.md` `Audit` fences the `CollectUpdateStats` -> `ChangeSummary` fold sealing `EvidenceReceipt.LiveData` per slot (the last producer-less instrument, sealing `Latest` never `Overall` so the fan cannot double-count), two same-class instances found and closed (`Shell/dialogs.md` toast admission/dismissal `InstrumentSpec` rows + `Observe(InstrumentSet, ToastReceipt)`; `Render/immersive.md` `Observe` pair beside its `TelemetryRow`), and the dimension-consistency half holds corpus-wide — `Shell/navigation.md` declares the verb dimension both `NavigateInstrument`/`RouteMissInstrument` writes tag through the dispatch state slot, and every touched page verified declaration slots == writer tag keys == description.
[SEARCH_QUERY_SHAPE]-[COMPLETE]: the closed `SearchQuery` shape, source-coverage rows, and the `DocumentQuery`/`DocumentHit` consumed wire landed in `libs/csharp/Rasm.AppUi/.planning/Document/search.md` `[02]-[QUERY_SHAPE]`/`[06]-[INDEX_WIRE]`, the seam ceiling admitted at BOTH ends against one constant and the local leg building the wire shape through the same decode gate the store's answers take.
[BUNDLE_MANIFEST]-[COMPLETE]: the AppHost manifest carries content identity — `SupportManifest.Entry.ContentKey` landed across `Written`/`Faulted`/`Capped` with absence spelled `None` on every no-byte arm, the native manifest carrying the column whole to its `Entries`, and the AppUi `ProofLaw.BundleShape` golden pair now pins content identity through the extracted `manifest.json`.
[SPRING_PARITY_MAP]-[COMPLETE]: the three-row parity map landed in `libs/csharp/Rasm.AppUi/.planning/Theme/motion.md` `[02]-[MOTION_AXIS]` with the six `MotionToken` spring values as the parity values and the composition-root seating stated per the branch ruling.
[SESSION_ROLE_ROWS]-[COMPLETE]: the role/capability vocabulary, `MembershipState` lifecycle rows, and the `SessionGate` admission seam landed whole in `libs/csharp/Rasm.AppUi/.planning/Collab/session.md` with the gate folded into `sync.md` `IntentLedger.Project` ahead of `LedgerAppend`; rejections derive through `AppUiFaultBand.Session` (6530), not a `Collab` code — session governance is a distinct band from sync merge faults.
[SHADER_COMPILE_METER]-[COMPLETE]: the recording projection landed beside the compile fold — `Render/shading.md` `[02]-[SHADER_ASSET]` `Observe(InstrumentSet, ShaderReceipt)` writes the compiled-or-failed row under `AppUiTelemetry.BackendSlot` and drives the whole six-row `TelemetryRow` contribution, plane admissions fanned mint/reuse/refuse on `AppUiTelemetry.OutcomeSlot` so the byte-ceiling and layered-chain refusals count as a partition of one keyed series; the corpus-wide half of the claimed bar was false and `[INSTRUMENT_WRITER_CLOSURE]` owns the real remainder.
[MATH_ENGINE_VERIFY]-[COMPLETE]: the painter path is proven at fence depth — the `LaTeX` setter's `Result` parse rail probed through `ErrorMessage`, `Measure(float)`, the absolute-origin `Draw(SKCanvas, SKPoint)` into the `DrawSource.Borrowed` lease, and the headless `DrawAsStream(width, SKEncodedImageFormat.Png, quality, TextAlignment)` encode, every member verified against `.api/api-csharpmath-skia.md`; the two residual unknowns (the `LocalTypefaces` element-type bridge, the aligned-draw parameter semantics) carry as research rows on the owning page rather than blocking the arm.
[CAPTURE_ROW_FACTORY_CALLS]-[COMPLETE]: both twins re-key the proof owner's `RenderHashLane` and mint through `RenderHashLane.Row` — `CustomVisual.RenderTwin` and `ChartSeriesSpec.Baseline` now return `Fin<CaptureRow>` carrying the lane's gamut and tick policy, so the bare four-argument construction against the five-column private constructor is gone at both sites.
[BOARD_TILE_ROWS]-[COMPLETE]: tile-row table [01]-[10] and the `TelemetryBoard` spine landed in `libs/csharp/Rasm.AppUi/.planning/Charts/telemetry.md` `[02]-[BOARD_ROWS]`.
[LEVEL_KIND_ROW]-[COMPLETE]: keyed `LevelCells` families and their fan-arm swaps landed in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[03]-[TELEMETRY_SPINE]`; the kind roster and its `Levels` declaration home at the kernel instrument mechanism.
[WIRE_CONTEXT_FIELDS]-[COMPLETE]: carriage landed as `CollabWireContext`/`CollabFrame` frame metadata in `libs/csharp/Rasm.AppUi/.planning/Collab/sync.md` `[04]-[LIVE_WIRE]` with the ingest edge re-emitting message-envelope context unchanged; the `CommitWith` origin-metadata claim was refuted — `CommitOptions` seals only change origin, timestamp, and message.
[CARRIER_SEATING_PRECEDENT]-[COMPLETE]: `Collab/sync#COLLAB_WIRE` follows the carrier-owned seating: NATS adapters sit beside their binding, while CloudEvents uses the kernel's generic `EventCarrier.Read`/`.Write` over attributes declared by each generated consumer projection.
[PRECOMMIT_TAP]-[COMPLETE]: `SubscribePreCommit` tap and `ExportJsonUpdates` window export landed as `LiveWire.TapPreCommit`/`ExportJson` with the devloop `CollabPreCommit`/`CollabJson` verbs.
[BENCH_MATRIX_ROW]-[COMPLETE]: `BenchLane` derivation and the `FrameBench`/`Divergence` gate landed in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/proof.md`, judged through the AppHost `BenchmarkGate` with the dashboards benchmark layout fed off the message-envelope stream.
[FLAME_SOURCE_PORT]-[COMPLETE]: `ProfileSampleSource` reads the AppHost `UiSchedulerPort.ProfileSamples` feed by correlation, and `FlameNode.Of` prefix-merges its AppHost-owned `ProfileSample` values in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/devloop.md` `[02]-[DEV_LOOP]`.
[USAGE_RECEIPT_SHAPE]-[COMPLETE]: `TenantUsage` record, message-envelope partition fold, and `TenantUsageWire` landed in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[04]-[CORRELATION_JOIN]`.
[SLO_COORD_PIN]-[COMPLETE]: superseded — the viewport indicators land as `ViewportObjectives` `Sli.Latency` rows against the composed frame budget in `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[03]-[TELEMETRY_SPINE]`, and the window, factor, severity, and burn fold derive from the kernel SLO algebra.
[VIEWPORT_PACK_ON_PORT]-[COMPLETE]: viewport reliability policy rides the contributor port — `libs/csharp/Rasm.AppUi/.planning/Diagnostics/evidence.md` `[03]-[TELEMETRY_SPINE]` collapsed `ViewportObjectives` onto one `Pack(FrameBudget)` entry binding panels beside objectives off one row table, `AppUiTelemetry.Contribute` gained its pack-bearing twin, `Render/pipeline.md` `RenderGraph.TelemetryRow(version, budget)` carries that pack on the port declaring the three viewport series, and `Charts/telemetry.md` `SloTiles.Rows` reads its objectives.
[SIGNAL_CAPSULE_COMPOSE]-[COMPLETE]: the folder `InstrumentKind`/`InstrumentSpec` twins and the `SloCoordinate` burn partial deleted against the kernel mechanism and SLO algebra; `AppUiTelemetry` keeps scope identity, the dimension-slot vocabulary, contribution, and mount, with every sibling `TelemetryRow` re-cut onto them. Kind vocabulary collapsed onto the `[JsonDerivedType]` roster behind `EvidenceOps.KindOf`/`Kinds`/`Probe`, `EvidenceFan` parks refused measurements on a composition cell, `TenantUsageFold` accrues off the decoded union, and `ViewportObjectives` dropped its window knob.
[CONTROL_UNION_WIDENING]-[COMPLETE]: the control union reached the professional roster at `libs/csharp/Rasm.AppUi/.planning/Shell/controls.md` — `controls#CONTROL_INTENT` widened from eighteen to twenty-nine cases (Label, Segmented, Chip, Progress, ColorInput, Avatar, Breadcrumb, Tooltip, EmptyState, MultiSelect, Range added) with emphasis, icon, pending, and hint landing as `IntentBinding` COLUMNS under a six-row `ControlEmphasis` ladder whose quiet, inverted, and link rows author their own state arms; the thin payloads rebuilt in place as `ColumnRow` (header key, cell intent, editor intent as the read-only verdict, `DataGridLength` extent, sort key, alignment), a flat windowed `Tree` with shipped level-to-padding indentation and expansion arriving on the flattened node, `Select`/`MultiSelect` over a windowed `OptionSource` with closed/editable and bound/free postures plus group keys, structured `MenuRow`s (check posture, submenu, separator, gesture hint, icon), and a `TemporalKind` column over the typed picker family with bounds expressed as blackout spans; the typed-numeric law re-cut entry onto the eleven-row `NumericKind` family over a four-arm `NumericRange` narrowed by checked generic math, deleting the decimal cast that lost `NaN` and range; link landed as an emphasis row, and search input, split button, toolbar overflow, badge, busy overlay, and eight further extended-control candidates landed as explicit refusals on the package-admission table. `controls#MATERIALIZE_FOLD` rebuilt whole: the fold now writes NO resolved appearance value — emphasis and posture resolve one `ControlSkin` row onto `StyledElement.Theme` while the semantic role and typographic role land as style classes the theme's own selectors match — the context-column admission law landed as a thirteen-row table deleting the date, path, grid, tree, token, and metric columns, `ControlTypeOf` collapsed into the two-answer `ShapeOf` projection, and `Refresh` re-dresses thirteen poolable leaf kinds. `controls#TS_PROJECTION` carries the twenty-nine kind literals with big-integer bounds crossing as ordinal decimal strings. Counterpart theme rows (secondary, inverted, link button, avatar cluster) landed at `Theme/tokens#CONTROL_THEMES`; three rulings landed at the package tier; `api-ursa.md` gained the numeric, picker, tag-input, avatar, closable-tag, and level-converter member blocks.
[TYPE_SCALE_ROLES]-[COMPLETE]: the type ladder became a two-axis generated table at `libs/csharp/Rasm.AppUi/.planning/Theme/typography.md` `typography#ROLE_AXIS` — eleven `TypographyRole` rungs authoring an INTEGER base size, a `LeadingClass`, a weight rung, and role-intrinsic policy alone, crossed with a `TypeAxis` carrying `TypeEmphasis` as a step on the shipped `WeightLadder`, the `DensityPolicy.Type` factor, a hinged-at-midpoint text-scale multiplier read from `PreferenceRow.TextScale` through `PreferenceCell`, and a `TypeSlant` row carrying both its variable axis and its declared synthetic skew; `TypeScale.Resolve` generates every derived value — the integer em admission, the grid-snapped line box floored at the em, and the Inter dynamic-tracking curve evaluated at the resolved size under a DECLARED em unit whose one pixel projection is `TextStyleRow.TrackingPx` — so the unreached `LineHeight` and unapplied `TrackingEm` constants are deleted in favour of the generation; `NumeralModality`, `TypeCasing`, and `TrimPolicy` land the numeral, casing, and overflow columns over a `FeatureIntent` vocabulary with the corrected feature semantics (`zero` slashed zero, `tnum` tabular, `ss01` alternate digits, `cv`-series disambiguation); `typography#TEXT_METRICS` gained the even half-leading distribution, the first-baseline law, and cap-height icon centring. `tokens#TOKEN_CATALOG` landed the counterpart whole — `ResolvedTheme.Types` carrying the `TypeScale.Expand` table with its `Type(role, emphasis)` read, `FontChain` threaded through `ThemeCatalog.Resolve`/`ThemeEmission.Emit`/`ThemeCell`, `SemiSlot.Size`/`Weight` re-keyed onto the `(role, emphasis)` cell so the shipped bold slot is body-at-strong rather than a second role, and `TypeScale.Emission` writing the geometric leaves once per role beside the per-emphasis weight leaf.
[TEXT_ITEMIZATION_RAIL]-[COMPLETE]: the shaping rail became correct-by-construction at `typography#SHAPING_RAIL` and `typography#FONT_ADMISSION` — `TextItemizer` segmenting script (HarfBuzz unicode functions), direction (per-script horizontal direction under a paragraph base, with the no-embedding-override carve stated), and face coverage into maximal `TextSegment` runs with the windowed-ingress edge flags, so an uncovered codepoint is a `TypographyFault.CoverageRejected` refusal rather than a notdef box; `FontChain.Elect`/`Cover` re-keyed on capability through `SKFontManager.MatchFamily(family, SKFontStyle)` and `MatchCharacter`, `EmbeddedFace` admitting the owned variable, mono, and symbol assets beside the shipped static collection, `FaceInstance` cloning the variation position from the face's published axes and electing its colour palette from the face's own palette flags, holding the design scale as the face's `UnitsPerEm` and the discretionary features a SHAPED PROBE proved, and `FaceCabinet` keying one capsule per face instance; `ShapingSurface.Shape` folding segments into a `ShapedText` of `ShapedRun`s with `ClusterMark` source, offset, and unsafe-to-break records, `LineBreaker` wrapping on safe cluster boundaries under a declared `BreakClass` vocabulary with a locale-supplied oracle seam, `ShapedCache` leasing under a byte ceiling with generation-guarded least-recently-touched release per the folder's budgeted-cache ruling, and `RenderPosture` declaring live, golden, paged, and layer postures over every host-variable `SKFont` knob; `typography#TEXT_METRICS` gained `DecorationGeometry` reading the face's own underline and strikeout metrics with the OpenType-metrics fallback and skip-ink complement from `SKTextBlob.GetIntercepts`, plus `CaretGeometry` folding caret and selection over clusters alone. CORRECTIONS `[07]` applied whole: the blob draws through `SKCanvas.DrawText(SKTextBlob, x, y, SKPaint)`, `AllocateRawPositionedRun` returns `SKRawRunBuffer<SKPoint>`, the nullable `Build()` is guarded, `SKFont.ScaleX` carries through the horizontal projection, and `Tag.Parse` is refuted as throwing — it coerces silently, so `FeatureAdmission` validates the four-character shape before the parse. Three rulings landed at the folder tier; `Collab/tour` and `Charts/custom` re-pointed onto the cabinet-and-cache seam.
[MOTION_BINDING_ROWS]-[COMPLETE]: the binding law landed at `libs/csharp/Rasm.AppUi/.planning/Theme/motion.md` as the new `motion#MOTION_BINDING` section — `MotionAxis` carrying nine rows over the shipped transition family (opacity, colour, brush, shadow, corner, effect, transform, extent, inset), each with its transition constructor, its `MotionKind`, and its `MotionLane`, with `Bind` folding reduction, refusing a direct property before the list throws, refusing an overshooting token on a clamping channel, and mounting nothing at all for a resolved instant; the spatial/effects damping split landed as the `MotionKind.AdmitsOvershoot` column and `MotionTiming.Overshoots` — analytic on the spring arm and a curve-lattice read on the tween arm, so an imported curve classifies itself; `MotionTravel` landed as the extent-inflated token ladder under the variance ban; `LatencyTier` landed as the five-rung feedback ladder keyed on expected duration with each rung naming its choreography plan; `RouteCarrier` binds the page plan onto `TransitioningContentControl.PageTransition` as a composite cross-fade and anchor-axis slide with asymmetric easings, against the host's own inline-duration default; micro-interaction plan rows (hover, press, indicator, disclosure, skeleton) landed with asymmetric enter/exit tokens and dwell/linger intent columns; `MotionToken` gained the ambient looping grade for shimmer and wash sweeps; the composition seam names `ComposeSlot`, `ComposeTrack.Start`/`Floor`, `ImplicitPlan.Of`, and `VfxMessage.Advance` by member, and the floating-chrome law landed as boundary law and a package ruling. `api-avalonia.md` gained the full transition roster, the page-transition member block, `AvaloniaProperty.IsDirect`, and the transitioning-host default-value trap.
[OVERLAY_CHOREOGRAPHY_ROWS]-[COMPLETE]: choreography landed as plan DATA at `motion#MOTION_APPLICATION` — `MotionOrigin` (pivot plus outward direction), `MotionPose` (opacity, scale, offsets, and a travel fraction of the element's own extent resolved through `Resolve`), and `Choreography` (composed axes, origin, entry and departure poses) widened `MotionPlan` to eleven rows: dialog rise-and-settle with a shallower departure, a drawer travelling its own extent under the quintic-out token, an anchor-side flyout slide-with-zoom, a spring toast entering on transform and extent, page, cascade, hover, press, indicator, disclosure, and skeleton; `Poses` collapses both ends to opacity under reduction; `Delay` gained the capped geometric falloff so a long list entrance is bounded in closed form; `Stacked` landed as the toast stack projection (depth-scaled collapse, expand-on-hover, cap as stack ceiling) so a dismissal reflows through one re-read; `Disclosure.Span`/`Release` landed the measured-extent technique against unanimatable auto height; `Intent` landed hover intent as the asymmetric dwell/linger fold beside the throttle, pulse, and serial pacing arms.
[MOTION_HANDOFF_PHYSICS]-[COMPLETE]: interactive physics landed as the new `motion#MOTION_HANDOFF` section — `MotionTrack` the first-order velocity smoother over pointer samples, `MotionDecay` the normal and fast retention rows over the kernel `DecayShape`, `HandoffSpec.Release` projecting live velocity into a resting displacement, quantizing it onto the optional grid, and taking ONE threshold test against the dismissal fraction before seeding the completion spring with that same velocity, `MotionRelease` the dismiss-or-restore outcome carrying its settling duration from `SpringValue.Settling` at the declared pixel epsilon, and `GestureBlend.Blend`/`Retarget` the pickup and interruption folds; `MotionTiming.Retargets` landed as the retargetability column with its table, and the composed lane's absent velocity read landed as boundary law. Gesture ingress binds the settled `Shell/input#POINTER_GESTURES` rows as values — this owner subscribes to nothing. The kernel gained both missing primitives at `libs/csharp/Rasm/.planning/Parametric/projections.md`: `SpringShape.Settle` inverting each regime's decay curve in closed form (the critical arm folding its linear term through t·e^(−ωt/2) ≤ 2/(eω)) and `DecayShape` as the free-decay twin with rate, projection, advance, and settling; the cross-libs spring-parity ruling widened to cover both.
[CHART_PAINT_RESOLVER]-[COMPLETE]: one paint-resolver owner landed at `libs/csharp/Rasm.AppUi/.planning/Charts/dashboards.md` `dashboards#CHART_PAINTS` — `ChartChrome` is the twenty-four-row chrome vocabulary addressing generated `PaintRole` rungs by key (so the chart mints NO token of its own and the axis chrome widened past the single grid role into separators, subseparators, ticks, subticks, zero line, and the crosshair line-label-chip trio), `ChromeInk` closes the stroke-fill-text-chip mint with the chip arm existing because `ICartesianAxis.CrosshairLabelsBackground` takes an `LvcColor` and never a `Paint`, `PaintFamily` binds one anchor role beside one `Colormap` so a single-series stroke, a categorical legend, and a heat gradient read one row, `ChartSeverity` is the ranked status vocabulary, and `ChartInk` holds live `SolidColorPaint` instances the swap writes in place; `ChartComposition.Register` is the ONE `LiveCharts.Configure` call (a second silently discards every rule the first chained, since `HasTheme` replaces the whole `Theme`) seeding the dark theme and chaining the axis, frame, series, tooltip, and legend rules, and `AxisChrome.Apply` writes through `IPlane` narrowed to `ICartesianAxis` so one fold styles every axis kind. The re-materialization truth landed as measured law rather than a hedge: chrome paints re-tint live because a `Paint` is a draw task the mounted charts hold by reference, the process `Theme` is a VALUE so the swap re-registers and folds `ApplyStyleToSeries` over attached series — deterministic and idempotent because `GetSeriesColor` is exactly `Colors[SeriesId % Length]` — the tooltip and legend are factory products re-assigned as property writes, and the offscreen twins are products re-rendered rather than re-tinted, so NOTHING re-mounts.
[CHART_GRAMMAR_REBUILD]-[COMPLETE]: the per-tile spec rebuilt from one-series-one-axis to a LAYER LIST at `dashboards#CHART_GRAMMAR` — `ChartDatum` is the canonical allocation-free point carrying five inline magnitude slots beside its arity, weight, group key, and stamp, `ChartEncoding` closes the coordinate arity vocabulary onto the package's own `Coordinate` ctors (xy, weighted, financial, five-number summary, error-bounded) with a short vector answering `Coordinate.Empty` — which is the package's real null gap `EnableNullSplitting` breaks a line on, so a hole renders as a hole rather than a zero — `ChartLayer` binds kind, feed, transform rows, both axis indices, family, ink, and the label, error, and toggle columns, and `ChartSpec.Admit` is the whole shape law in one place (canvas agreement, axis-index resolution, and per-layer chain arity against the kind's encoding). The axis roster gained categorical rows (the package's own `Labels` under `MinStep` one and `ForceStepToMin`, so a category is an ordinal with a label), dual axes as the axis LIST with per-layer indices, and measure-role axes; every series shell now closes over `ChartDatum` so `Mapping` is reachable and typed where the erased shells left it unspellable. The four bare-string policy keys are typed per CORRECTIONS `[03]` — `MotionPlan`, `TypographyRole`, `ChartChrome`, `PaintFamily` — and the interaction policy widened with `ChartFind` over the package's `FindingStrategy`, legend series-toggle, spec-level data labels, null-gap splitting, and error columns; composition registration landed once at the root through `ChartComposition.Register`.
[TRANSFORM_PIPELINE]-[COMPLETE]: declared transform rows landed between feed and series at `dashboards#STREAM_BINDING` — `TransformRow` is the seven-case family (bin under a closed `BinPolicy` of bucket count, width, or declared extent; aggregate over group keys; moving window; calendar reshape; cumulative; load-duration; downsample), `ChartShape` declares the series-grouped-matrix-summary-span vocabulary carrying magnitude arity beside its keyed posture, and `TransformChain.Admit` folds the whole chain at DECLARATION so an unsatisfiable step refuses when the board is admitted and its terminal arity is what `ChartSpec.Admit` checks the layer encoding against. Reducers ride the exact order-statistic substrate — `SortedArrayStatistics` after ONE sort per group, with the nearest-rank quantile spelled `QuantileCustom(sorted, tau, QuantileDefinition.R1)` so a declared p95 is an observation the population contains, and `FiveNumberSummary` reordered once into the box coordinate's own magnitude order — and the weighted mean permutes weights with values so a sorted array never pairs a value with a stranger's population. Calendar reshape rides NodaTime through one injected `CalendarPolicy` zone so hour-by-day carpets, month rollups, season rollups, and weekday folds resolve the same civil calendar a time brush and an axis label resolve; the settled `Lttb` downsampler joined as a ROW, which deleted `ChartStream.Buckets` and with it the possibility of an inert bucket policy on a row nothing folds, and the feed table's bucket column became declared shape rows.
[BOARD_CONTEXT_OWNER]-[COMPLETE]: board-scoped context and responsive placement landed at `dashboards#BOARD_CONTEXT` — `BoardVariable` is a BOUNDED vocabulary by construction (the domain is simultaneously the admission rule and the dropdown, so a free-text variable interpolated into a feed key is unspellable and a deep link cannot smuggle one in), `BoardRange` closes absolute against relative with `Relative` resolving at read so a board left open re-resolves rather than pinning its opening window, `TimeRange` carries the shift column that makes period comparison a column rather than a second range, `BoardContext` owns one range with per-tile override plus the refresh cadence as board policy distinct from each feed's sampling cadence, and `BoardContext.Ticks` rides the surface scheduler's virtual time so a proof lane advances a board deterministically. `BoardLink` owns the QUERY half of a deep link alone — the scheme, verb, and route key stay the navigation owner's grammar — refusing an ambiguous absolute-plus-relative link and refusing whole when a decoded variable falls outside its declared domain. Responsive placement landed as a breakpoint axis on `TilePlacement` over the settled `BreakpointRow` vocabulary with overlap admitted PER tier, `PlacementGrid` carrying the column count per tier, and `PlacementFlow.Band`/`Flow`/`Layout` deriving every column, span, and wrap; `Charts/telemetry.md` `TelemetryBoard.Layout` re-derived on those folds per CORRECTIONS `[03]`, so sloColumns 4, sloWidth 3, and the 6/3/12 spans are gone and no placement literal survives on either page.
[CHART_THRESHOLD_FAMILY]-[COMPLETE]: the ordered step family landed at `dashboards#THRESHOLDS_AND_COMPLIANCE` — `ThresholdList` carries a base severity below the first crossing plus ascending `ThresholdStep` rows under a `ThresholdBasis` of absolute or percentage and a `ThresholdMode` render column, so a value always classifies and a gap between steps is unrepresentable; `At` is the one classification every renderer reads, `Bands` projects axis bands, `Fills` ordered gauge stops, and `Cell` one table-cell colour, each mode-gated so a crossing set authored for one range cannot silently colour another surface. `ChartSection` widened to four independent `Option<double>` bounds (the package extends a null edge to the draw margin, so a horizontal band, a vertical band, and a rectangle are one value at three coordinate populations) with both-axis coordinates, its own edge label rather than a floating visual, per-axis indices, and a `Tint` severity override so a status band reads as status ink and a chrome band as chrome. Severity ink rides the settled `ChartSeverity` rows over the status `PaintRole` ladder, so a band, a watch badge, and a table cell are one pigment. The tables-side receiving seam is named as a real counterpart — the value-driven format column on `TableColumnRow` — and recorded RIPPLE-OWED for the tables leg; `Charts/telemetry.md` consumes the family on its burn gauges, whose fill steps are the SAME crossing the watch rule arms on.
[TILE_STATE_UNION]-[COMPLETE]: the per-tile state union and full stat anatomy landed at `dashboards#DASHBOARD_TILES` — `TileState` closes loading, ready, empty, failed, and cramped with `TilePresentation` as a PROJECTION of the state (loading holds the prior frame under the held veil at reduced opacity so a refresh never blanks a board a viewer is reading, empty states the gap in words because a zero drawn for absent data is a measurement nothing made, failed dims and carries the retry the fault rail already offers, cramped carries the drop depth), `TileDrop` is the ordered progressive-drop ladder so a narrow mount loses its legend before its axis, and `TileGate` binds pause and hold to the package's REAL update-gating members `AutoUpdateEnabled` and `Paint.IsPaused`. `StatAnatomy` carries value, ratio delta, `DeltaPolarity`, trend caption, background spark samples, and percentile rows, so a rising error rate and a rising throughput cannot both read green. Tile sourcing unified onto ONE axis: `TileSource` gained `Streamed` and `Rows` beside `Folded` and `Derived`, every `DashboardTile` case carries exactly one `Source`, and `DashboardTile.Admit` refuses a scalar tile wearing a row source — which is also where `DashboardTile.Table(string)` became the real seam vocabulary `TileSource.Rows` plus the named `TableSourcePort` per CORRECTIONS `[03]`. The axis-less sparkline landed as `Sparkline.Render` over the package's own offscreen cartesian chart with every chrome slot nulled, shared by stat tiles and table cells; the custom-plane sparkline row REFUTES and is recorded as a refusal rather than an obligation, because the chart rail's own admission law rejects a bespoke Skia surface drawing chart semantics and a sparkline is exactly that with the chrome removed.
[WATCH_STALENESS_ROWS]-[COMPLETE]: the watch vocabulary gained its freshness half at `dashboards#DASHBOARD_TILES` — the comparator now reads a `WatchSample` carrying value AND age, which is what makes `WatchComparator.Stale` expressible at all, and `WatchFold.Samples` probes on the rule's own cadence so age advances when the feed does not (a stalled feed breaches no value predicate, so every level rule read as satisfied while the surface showed a frozen number under a caption claiming currency). `WatchRule` widened with `Severity` on the ranked `ChartSeverity` family so warn and critical on one tile are two rows of one family, `PendingFor` distinct from `Quiet` (the breach must HOLD before it raises, then re-raises are suppressed on a declared cadence — collapsing them made a slowly-rising breach and a fast-flapping one indistinguishable), and `Probe`; `WatchFold.Worst` makes a tile badge read the worst live crossing rather than the most recent, and crossings fold onto `BoardTelemetry.WatchCrossingsInstrument` under a severity dimension. A feed that NEVER delivered renders as `TileState.Empty` rather than raising, since an alert about a series that never existed names a breach of nothing. The consumer end of the producer seam landed as `FeedHealth` and `FeedFreshness`; the producing staleness projection is recorded RIPPLE-OWED for the livedata leg. `Charts/telemetry.md` arms a staleness rule per SLO stream at four times its board refresh.
[LEGEND_ALGEBRA]-[COMPLETE]: one legend declaration landed as the new `dashboards#LEGEND_ALGEBRA` cluster — `LegendDomain` is the whole discriminant across series, continuous, stepped, categorized, and ordinal-dictionary arms (a presentation column beside it would admit a ramp over a series domain, which no renderer answers), a populated `LegendColumn` roster makes a table legend the same domain carrying settled `ChartReducer` reductions over the layer's own rows rather than a sixth form, `LegendDock` carries BOTH its package side and its viewport corner with an accumulated drag offset and a board-wide `ResetIntent`, `LegendFold.Entries` is the one entry resolution every arm reads (series swatches take the chart's own ramp position, stepped bands read the now-public `ThresholdList.Edges` every threshold projection already paints, ordinal codes colour by palette position because interpolating between two codes is meaningless), and `LegendFold.Spelled` is the one printed-value projection so a legend bound and its axis tick carry one elected unit and one decimal separator. Both render arms were pinned by decompile rather than assumed: `SKDefaultLegend` emits exactly one `GetMiniatureGeometry` plus one `ISeries.Name` per visible series and admits no caller-supplied entries, `SKHeatLegend` reads the FIRST visible heat series' own `HeatMap`/`WeightBounds` and draws a gradient with exactly two `Formatter` labels, and both derive orientation from `LegendPosition` alone — so `LegendRenderer.Render` is a VALUE-shaped arm verdict routing statistics tables, stepped bands, categorized members, ordinal dictionaries, and every corner dock to the drawn arm, and a richer declaration silently degrading to the poorer legend is unspellable. The continuous clamp lands on the DATA through the new `TransformRow.Clamp` row, because the heat arm prints the series' own measured bounds and a legend-side clamp would caption a range the ramp does not paint. `ChartChrome` legend-title, legend-value, and legend-frame rows cover the drawn arm on EXISTING generated rungs, so tokens.md carries no obligation at all; the `Analysis/layers` consumption and the `Charts/custom.md` legend payload row are RIPPLE-OWED.
[FACET_GRID]-[COMPLETE]: small multiples landed as a column on the tile spec at `dashboards#CHART_GRAMMAR` — `FacetAxis` partitions on a value the canonical datum already carries (the aggregation group, or the civil cell the settled `CalendarAxis` rows fold) so a member is data rather than an invented expression, `FacetSpec` carries the tile-local column count, the cap, the row span, and the shared-scale posture, and `FacetFold.Partition` splits the materialized rows into member copies of the whole spec. Every copy carries the PARENT's `ScaleGroup`, so `ChartSync.Pair` shares one min-max across the grid and the members pan and zoom as one scale, and the legend stays the parent tile's single declaration so two cells cannot disagree about what a swatch means. Cursor sync is explicit through the verified `ICartesianAxis.InvalidateCrosshair`/`ClearCrosshair` pair, because a shared range pairs the SCALE and never the pointer. The cap folds every member past it into ONE residual member captioned through the locale with its own count, so a two-hundred-zone partition renders twelve cells and a thirteenth reading the rest rather than twelve cells and a silent lie. Placement is the board's OWN `PlacementFlow.Flow` at a tile-local `PlacementGrid`, so sub-chart arrangement and board arrangement are one fold read at two widths and a facet-local column arithmetic is unspellable.
[ANNOTATION_LAYER]-[COMPLETE]: the annotation plane landed at the renamed `dashboards#AXES_AND_MARKS` — `ChartAnnotation` carries Point, Region, and Event arms, each naming the LAYER it annotates so it inherits that layer's axis indices at admission and moves when its series moves, and `ChartSpec.Admit` refuses a name the roster lacks rather than seating a mark on axis zero. Decompile killed the assumed landing: `XamlDrawnLabelVisual` and its `DrawnLabelVisual` base carry `X`/`Y` as raw pixel floats with NO `LocationUnit`, `ScalesXAt`, or `ScalesYAt` (only `XamlNeedle` and `XamlAngularTicks` carry that trio), so a label visual anchored to a datum drifts on every pan and the visual plane is unreachable for marks. Every arm therefore projects onto an owner this page already has — region and event materialize as `ChartSection` values (the event at `From == To`, a full-height hairline carrying its own flag label), and the point arm mints a one-datum pinned scatter `ChartLayer`. That drove two settled-owner widenings: `ChartLayer.Pinned` makes a literal-valued layer a population of the one shape rather than a second layer family, and the `bool Labelled` knob collapsed into the `DataLabel` row family carrying both the text projection and its chrome — whose value row renders through the locale off the settled numeric-axis `CompositeFormat` and whose caption row reads the datum's group off `ChartPoint.Context.DataSource`, never `AsDataLabel`, which resolves through `GetDataLabelText` back into the formatter being defined and recurses without terminating. `AnnotationFold.Cluster` collapses marks inside a declared axis-unit tolerance per layer and arm, captions the collapse through the locale's plural rules, and takes the WORST member's severity so a critical mark is never buried under thirty-nine nominal ones.
[COMPARISON_OVERLAY_ROWS]-[COMPLETE]: comparison landed as a layer posture at `dashboards#CHART_GRAMMAR` — `ChartLayer.Compare` carries a `CompareOffset` whose three arms are all EXPRESSIONS rather than captured windows (`Period` shifts by the duration `TimeRange.Shifted` already understands, `Ordinal` shifts by index, `Scenario` re-binds the stream under a named board-variable member so a comparison cannot smuggle in a value the variable's domain refuses), and `CompareFold.Expand` mints the ghost beside its host under `ChartChrome.Ghost`, untoggleable and one draw layer below so the live series always draws over its shadow. Alignment is the new `TransformRow.Shift` row appended to the ghost's OWN chain, so the shifted window is a declared reshape the settled evaluator runs and replays offscreen from a captured snapshot with no live feed — and it moves the stamp and the axis ordinal together, since a shift that moved only `X` would leave every ghost point brushing at its original instant. `ChartSpec.Expand` folds ghosts and marks into the layer list once, ahead of every materialization and every facet split, so a ghost facets with its subject. The DASHED presentation is bound as a concept and left unspellable per the paint tier's own law: `SkiaPaint.PathEffect`/`DashEffect(float[], float)` exist but no chrome row sets one, so the exact stroke columns are RIPPLE-OWED to the custom plane.
[CROSSFILTER_REBUILD]-[COMPLETE]: the cross-filter surface collapsed to one entry at `dashboards#DASHBOARD_TILES` — five per-field mutators (`Brush`, `BrushDimension`, `BrushRegion`, `Clear`, `Restore`) became ONE `Push(source, FilterDelta)` whose seven-arm union owns its own state fold, so each brushed column is written in exactly one place, the admission gate and the source stamp run once instead of five times, and restore and reset are deltas rather than side doors that bypassed the gate. The `Predicate`/`Apply` duplication collapsed to the predicate alone — the deleted arm's whole body was `source.Filter(Predicate(…))` and its signature let a caller drop the projectors — and the four optional delegate parameters collapsed into one `BrushLens<TRow>` VALUE per tile, so a tile's brushable axes are declared once and every reader sees the same set. `FilterState` gained `Highlight` beside its filter columns and `CrossFilter.Emphasis` beside `Predicate`, making highlight and filter one channel at two intensities. The spatial brush was VERIFIED rather than re-landed: the chassis leg's `PolygonBrush` composes `IndexedPointInAreaLocator` and `Location.Interior or Boundary` against catalogued NTS rows, and the touch-point set it left open is now aligned (csproj `PackageReference`, README `[GEOMETRY_SUBSTRATE]` row). `ChartBrush.From` lands pixel-to-data mapping on the verified `ScalePixelsToData(LvcPointD, int, int)` at the layer's own axis indices, ordering corners after conversion because an inverted axis maps a top-left drag onto a bottom-right domain rectangle, and pushing a time delta on an instant scale and a dimension delta otherwise. The bitset kernel's package CENSUS reached a verdict of composition, not replacement: admitted `CommunityToolkit.HighPerformance` `BitHelper` now carries every per-bit read and write at its `ulong` width, but owns no bitmap, no set algebra, and no iteration; `System.Collections.Immutable` ships no bitset; in-box `BitArray` is one-dimensional, boxes a `bool` per element on enumeration, and cannot walk only set bits, so a selection would cost the `O(rows)` scan the kernel exists to delete; `SwiftCollections.Lean` is a Bim-folder 3D broad phase holding no keyed bitmap. The named statement exemption stands, narrowed to the keyed bitmap, the AND-of-unions, and the output-sensitive projection.
[COMPLIANCE_SCORECARD]-[COMPLETE]: constraint profiles landed at the renamed `dashboards#THRESHOLDS_AND_COMPLIANCE` — `ConstraintRow` names its metric, its `BoundDirection`, its bound, and the `MeasureRole` the bound stands in, `ConstraintProfile` is a saved, project-scoped, generation-sealed artifact crossing the ONE composition-bound wire the board snapshot crosses under the same record ceiling, and `ConstraintVerdict` carries the margin, the ratio, the failing driver, and the severity. Unit safety is by CONSTRUCTION rather than by conversion: bound and measured value both stand in the role's canonical metric unit and the display unit is elected at render exactly as an axis title's is, so a profile authored in millimetres reads in fractional inches to a viewer whose policy elected them and a transcribed abbreviation is unspellable. Margin is signed distance in that unit and ratio is that distance over the bound, because a scorecard ranking gross area against a daylight factor against cost can only sort on the ratio and a designer can only act on the margin; `BoundDirection.Within` reports the TIGHTEST edge, since the wider distance reports comfort the check does not have. Severity grades the shortfall through the profile's own percentage-basis `ThresholdList`, so a check earns its amber band by declaring crossings rather than by growing a second grading vocabulary. The card never computes a metric: `DashboardTile.Scorecard` binds the new `TileSource.Composed` slot bundle whose every part is a scalar arm subscribed through the same live-data scalar-fold edge a stat tile takes, admission proves every profile row's metric has a part, and evaluation runs only once every slot has reported — grading an absent metric as zero would print a passing verdict for a check nothing measured.
[METRIC_TABLE_PANEL]-[COMPLETE]: the persistent metrics panel landed as the new `telemetry#METRIC_PANEL` cluster and its board registry row — `MetricRow` names its measure role, its optional target, the element keys a hover publishes, and the scalar source it reads, `MetricSection` declares whether its rows COMPOSE (two means are not a sum), and `MetricPanel.Read` folds rows, subtotals, and the panel total in one pass so no total can disagree with the rows above it. Columns are `TableColumnRow` values over the settled grid vocabulary, so keyboard navigation, sorting, classification redaction, and clipboard projection all arrive from the tables owner and this page contributes rows rather than a second grid; every measured column binds `TableCellKind.Numeric`, whose tabular figures keep a live readout from jittering the rows beneath it. Per-cell unit election rides `MeasureRole` at render, a section of incommensurable roles answers no absolute subtotal (a summed area-against-factor carries no unit), percent-of-target is unitless and therefore the one column that totals across a mixed section, a row with no target carries no percent cell because absent is not met, and `ReadingSwap` is a HOVER posture so the second read costs no click and no mode memory. The ONE highlight channel is `CrossFilter.Push` with a `FilterDelta.Highlight` carrying the hovered row's element keys: the scene folds them through the settled `VisibilityAction.Xray` so the unmatched rest ghosts at the transparency the viewpoint receipt already carries, and every bound chart reads `CrossFilter.Emphasis` for its per-mark opacity — one publish, two subscriptions, no per-surface highlight code. Also repaired on this page from the commands leg's decompile refutation: the store-profile bucket-snapshot fold moved from `ToObservableChangeSet` (which upserts every item and removes NONE) to `EditDiff`, so a bucket the window slid past, an aggregate dropped, or a tenant filter narrowed away no longer stands forever inside the stat tiles reducing it.
