// Shared TS test kit: corpus readers, law combinators, harness layers, gauge thresholds-as-data,
// and e2e drivers. The kit body lands at TS buildout; specs import it through the workspace graph.
export {};
