# [PY_CAD_IDEAS]

`python/cad` ideas track exact-modeling and exchange capability the OCCT kernel already reaches but the wire does not yet carry.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[DURABLE_SELECTION]-[COMPLETE]: landed as the generated `Correspondence` on `ExecuteResponse` with `brep/provenance#READING` `rekeyed` remapping every image onto the sealed artifact's decoded order — a STEP round trip permutes edge and vertex ordinals deterministically and idempotently, so `(artifact digest, grain, ordinal)` is the durable name and a fillet after a fuse names its edge from the reply; the producer-token tension resolved to the reseal proof, which carries no provider state.
[PART_IDENTITY]-[COMPLETE]: landed as `TessellateResponse.parts` of `PartIdentity` — the emitter pins `ProductAndInstanceAndOcaf` node naming, `exchange/assembly#PARTS` walks the label tree, and `tessellation/mesh#MESH` joins the walk to the node names the census read back on the OCAF entry; layers ride the roster because glTF drops them, colour does not because glTF carries it as a material; geometry `mesh/cad#BODIES` joins each decoded placement on `node`.
[NATIVE_MESH_HANDOFF]-[DROPPED]: GLB is the estate's one discrete carrier and a native buffer arm would be a second tessellation producer; float32 promotion sits below every admitted deflection, and the premise that geometry may not decode the emitted GLB was refuted on disk — the boundary forbids a second census, not consumption, so the consumer decode landed at `geometry/mesh/cad#BODIES` over the existing artifact.
[EXACT_HEALING]-[COMPLETE]: the recorded non-monotonic IGES observation re-proved as a unit-regime artefact; live probes over open shells, gapped wires, slivers, and re-read IGES fixed each operator's law — `UnifySameDomain` count-monotonic and volume-exact, `ShapeFix_Shape` copy-on-write with lying status flags, small-edge merging safe strictly below the least edge, `ShapeFix_Solid` and `FixSmallFace` refused — and landed as `HealOp` steps under `brep/healing#HEALERS` with a `HealingReceipt` stating census, tolerance, and validity before and after.
[EXCHANGE_SYMMETRY]-[COMPLETE]: landed as `SealedBody` with a STEP or IGES format arm, `Emission` on every execute request, and `exchange/iges#CODEC` writing manifold B-rep entities under the thousandfold scale the writer's fixed millimetre assumption demands; every arm models from either format and the receipt's dimensional ladder states a surface body's measurement.
[INERTIA_EVIDENCE]-[COMPLETE]: landed as `Inertia` and `Principal` on `BrepKernelReceipt` at solid dimension — centroidal tensor, principal moments, axes, gyration radii, symmetry flags — read at `metrology/properties#LADDER` and projected by geometry `mesh/brep#BREP` as the 3-D inertial evidence beside structural's 2-D section spine.
[ONE_FAULT_FAMILY]-[COMPLETE]: landed as `.planning/faults.md` — one `CadFault` value over a `FaultRow` roster keyed on independent leg and case columns, replacing three parallel frozen-dataclass Exception families whose nested `Literal` kind sets paid a re-wrap tax at every leg boundary and reconstructed the wire ordinal again at the serve edge.
[SUBDOMAIN_STRUCTURE]-[COMPLETE]: landed as five sub-domain folders over one root spine — `exchange`, `brep`, `metrology`, `tessellation`, and `service` — replacing the flat page set that made this the only Python package with no sub-domain tier; `metrology` seating below both native owners dissolved the import edge that had the exchange page composing the B-rep page for its receipt.
