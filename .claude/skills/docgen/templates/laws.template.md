# [<LAW_FAMILY_TOKEN>]

<charter-1-2-sentences: the law family this page owns, who re-proves its rows, and where a row moves when another owner absorbs its law>

One law family per page, carried in the representation the family needs — never a mandated skeleton. Pick the shape below that fits (or mix by section); every shape binds the register: timeless law, real repo surfaces in exact code-span spellings, one owner per fact, no provenance or process narration, and a clean prose gate.

```markdown shape-menu
# SHAPE A — comparison table, when rows answer one shared question

## [01]-[SECTION_TOKEN]

| [INDEX] | [RUBRIC_A]       | [RUBRIC_B]     |
| :-----: | :--------------- | :------------- |
|  [01]   | <atomic-law-row> | <atomic-value> |

# SHAPE B — marker-led cards, when each law owns its own rejected/accepted contrast
[CARD_TOKEN]:
- Law: <the invariant, stated once>
- Rejected: <the forbidden forms>
- Example: `<one neutral-identifier shape>`

# SHAPE C — numbered doctrine sections, for a universal law each clause elaborates

## [01]-[CLAUSE_TOKEN]

<the clause as prose, with rejected/accepted bullets and one conceptual fence when a shape clarifies it>
```
