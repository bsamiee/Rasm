# [DOSSIER] — Fabrication design survey, lane = UPSTREAM-BINDINGS

Read-only survey. Scope: the Fabrication-relevant clauses of all six upstream documents, every upstream-anchored register row (E7, E10, E11, E13, E14), the R6 25xx census, plus the namespace/consumer/seam census as it bears on the bindings. Every anchor re-verified ON DISK. Verdict grammar: HOLD (anchor exact) · DRIFT (real, anchor moved — corrected anchor supplied) · REFUTED (disk falsifies — line cited).

The headline: the **geometry DECISION has LANDED** (`RASM-CS-GEOMETRY-DECISION.md`, disk-dated after both briefs), so the E14 "spelling-lock PENDING" contingency does NOT fire — the exact kernel seam-member spellings are final and extracted in §B. But the E14 GBRIEF line-anchors have **systematically drifted +14 to +16 lines**, and **two reciprocal `[V12]` bindings the brief treats as landed law are REFUTED** (Persistence ArtifactKind egress index; Compute WasteAreaMm2 rollup counterpart) — both are actually held-open/un-landed forward demands. These are the load-bearing findings the DECISION must absorb.

---

## [A] — REGISTER VERDICTS (assigned rows)

| Row | Status | Evidence |
|---|---|---|
| **R6 25xx census** | **HOLD** (all 5 anchors exact) | `ARCH:16` "FabricationFault band-2500 composing kernel band-2400" ✓; `kinematics.md:3` "…folding the planner's reach/limit/singularity diagnostics into the typed band-2500 `FabricationFault`" ✓; `TASKLOG:44` "InadmissiblePair 2505/Gouge 2506/Collision 2507/…" inside a `[COMPLETE]` card ✓; `api-dstv-net.md:3` + `:126` "the band-2500 `FabricationFault` rail/source location" ✓; `api-robots.md:5` + `:94` "the typed band-2500 `FabricationFault` (composing the kernel band-2400 `GeometryFault`)" ✓. Every stale-25xx site is exactly where the brief pins it; the mechanical gate `rg 'band-2500\|25[0-9][0-9]'` must return zero post-motion. |
| **E7** (HLR upstream half) | **HOLD** (binding intact; one cross-ref drifted) | Upstream supersession CONFIRMED: `GBRIEF [V3_VIEW_ENGINE]` "the kernel `DrawingProjection` supersedes BOTH, Fabrication consumes" — but at **GBRIEF:102**, not the cited `GBRIEF:86` (which is the [01] Judge+Decide bullet). AppUi insulation is an EXACT quote: `RASM-CS-APPUI-BRIEF.md:162` [03] drafting row reads verbatim "Geometry supersession insulated at the Fabrication receipt", and `UIBRIEF:108` [V7](b) states "AppUi's producer STAYS the Fabrication `HiddenLineResult` receipt, so the supersession is invisible at this seam." The Fabrication-disk BSP defects (`projection.md:74-77/100/145/150`) are the HLR/naivety lane's to re-verify; the upstream binding holds. |
| **E10** (stale governance) | **HOLD** (every anchor exact) | `ARCH:16` band-2500 ✓; `ARCH:52` "netDxf in AppUi owns the distinct WRITE leg" ✓; `ARCH:53,54` `Rasm.Persistence/Schema` (deleted folder) ✓ (both program→Schema and nfp→Schema rows present); `api-acadsharp.md:120` credits "Rhino owns the host-bound native write" (stale under `[V7]` — the write is AppUi's ACadSharp two-row fold) ✓; `api-geometry3sharp.md:106` self-contradiction "the dropped CavalierContours arc-offset engine … the one-Clipper2 law forbids" ✓ (contradicts the `[V2]d` admitted-CavalierContours ruling); `api-dstv-net.md:132,143` netDxf write-leg refs ✓. |
| **E11** (ledger gaps) | **HOLD on-disk / REFUTED reciprocal** | `stock.md:40` `using Rasm.Element; // MaterialId` — wired, and ABSENT from `ARCH:[02]` (the seam ledger has no `Nesting/stock ← Rasm.Element/MaterialId` row; ARCH:49/50/51 cover Materials/nfp/Compute only) ✓ HOLD. `ARCH:51` `NestYield.WasteAreaMm2 → AggregateEnvironmental/AggregateCost` seam is exact on the Fabrication side ✓ — BUT its embedded parenthetical "(waterfall landed: `CBRIEF [V12]`)" is **REFUTED** (see §D-2). `physics.md:3` owner anchor exists (`RemovalParameter … process × material × tool × operation`); the ARCH:49-vs-physics.md:3 label consistency is a seam-lane item. |
| **E13** (page-craft) | **DRIFT** (anchor SET undercounts) | The author-forever seals HOLD and are now-false as claimed: `skeleton.md:3` "the one fabrication kernel wh[ere no managed library exists]", `slicing.md:3` author-kernel framing + `slicing.md:221` "the one genuinely-new geometry op the fabrication folder owns" — all superseded by `GBRIEF [V10]b/d` landing the kernel slice/skeleton owners (DECISION rows 17, 18). But the `[RESEARCH]`-tail census is **incomplete**: the brief names three (`kinematics.md:122`, `skeleton.md:199`, `slicing.md:219`); **disk carries FIVE** — add `stock.md:317` `## [05]-[RESEARCH]` and `program.md:379` `## [03]-[RESEARCH]`. The V2 mechanical gate ("`rg '\[RESEARCH\]'` returns zero") catches all five, but the enumerated anchor set must expand by two. |
| **E14** (upstream bindings) | **DRIFT + 2 REFUTED sub-anchors** | Six GBRIEF sub-anchors drifted +14..+16 lines (§D-1 table); PBRIEF `[V12]` and CBRIEF `[V12]` sub-anchors REFUTED (§D-2); UIBRIEF `[V6]` → `[V7]` (§D-3); APPHOST `[04]` and `.archive:56,75` HOLD exact. Detailed in §C. |

---

## [B] — THE SIGNATURE-LOCK: exact landed kernel seam-member spellings

The E14 clause "the exact kernel seam-member spellings this campaign signature-locks … finalize at the geometry DECISION" is **RESOLVED, not PENDING** — `RASM-CS-GEOMETRY-DECISION.md` landed with every Fabrication-consumed seam member spelled. The Fabrication `.planning` pages lock these EXACT signatures at their leg-1 pass. Source = the DECISION [02] page rows + [04] seam ledger + [08] Fabrication gate + [05] V10 disposition (line 149).

| Kernel concern | Locked entry signature | Modalities / payload | DECISION anchor |
|---|---|---|---|
| Region/polygon offset + medial + clearance | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Op? key = null)` | `Skeleton · Offset(join×end rows) · Medial · Minkowski · Weighted · Clearance(probe)`; per-point clearance **RADIUS** first-class result field + **`Clearance(Point3d probe)`** arbitrary-probe query; `JoinType`(Miter/Round/Bevel/Square)×`EndType` corner rows | row 10 (`Meshing/offset.md`, ns `Rasm.Geometry.Offsetting`, 2416-2419) |
| 3D MCF curve-skeleton | `Fin<CurveSkeleton> Skeletonize.Apply(SkeletonOp, Op? key = null)` | COMPOSES offset.md's clearance family (same RADIUS + `Clearance(Point3d probe)` across 2D/3D — ONE result family, never a sibling) | row 18 (`Meshing/skeleton.md`, ns `Rasm.Geometry.Offsetting`, `CollapseStalled 2418`/`SkeletonStalled 2417`) |
| Slice stack | `Fin<SliceStack> Slicing.Apply(SliceOp, Op? key = null)` | ALL FOUR layer policies (uniform · adaptive · variable-by-slope · support-interface, family OPEN); oriented contours (outer CCW/holes CW); typed OPEN-chain rows; **5-channel SoA forest wire** (layers · contours · nesting parent/child index arrays · open chains · elevations) | row 17 (`Meshing/slice.md`, ns `Rasm.Geometry.Intersection`, `SectionFault 2425`) |
| Plane section primitive | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Op? key = null)` | `IntersectOp.PlaneMesh` Section fold; re-founded oriented `Chain`; `IntersectResult` widened beyond loops-only | row 8 (`Meshing/intersect.md`, ns `Rasm.Geometry.Intersection`, 2424) |
| Booleans (mfg-tolerance) | `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp, Op? key = null)` + `BooleanOp` smart enum | `MeshBoolean · PlanarOverlay · CellComplex`; Manifold tier-3 behind `ArrangementPolicy.ScaleCeiling = 1_000_000` | row 9 (`Meshing/arrangement.md`, ns `Rasm.Geometry.Arrangement`, 2420+2423) |
| Hidden-line / projection | `Fin<DrawingProjection> View.Apply(ViewOp, Op? key = null)` | `HiddenLine · Silhouette · Section · Creases`; exact analytic Appel QI (no sampling) | row 27 (`Drawing/view.md`, ns `Rasm.Geometry.Projection`, 2436-2439) |
| Toolpath residency encode | `Fin<EncodedGeometry> Encode.Apply(PackOp, Op? key = null)` | `PackKind` gains **`toolpath`** case row + active-channel set; Compute decodes residency axis, AppHost locks `EncodingKind.Toolpath` | row 28 (`Drawing/pack.md`, ns `Rasm.Geometry.Encoding`, 2444-2447) |
| Content-key federation | `ContentHash.Of` seed-zero entry | the ONE mint site (`XxHash128.HashToUInt128` reached through it) | landed — `Rasm/.planning/{Drawing/pack, Spatial/naming, Spatial/reconciliation}.md` |

**Both ruled kernel hinges are carried in the landed DECISION V10 disposition (line 149), never re-opened:**
- **[V10]a kerf split** — "(a) corner rows `JoinType`×`EndType` author-kernel on the exact wavefront (row 10; **CavalierContours = Fabrication lane, pin re-grouped**)." Kernel owns corner-row assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours. One concern per stratum.
- **[V10]d medial radius carriage** — "(d) skeleton.md (row 18) MCF over landed machinery; **the clearance-radius + arbitrary-probe query is ONE shared result family minted in offset.md and composed by skeleton.md** … (e) `Toolpath/Skeleton.cs` **dies unconditionally**." The radius payload is mandate — `Toolpath/Skeleton.cs` keeps ONLY the trochoidal constant-engagement WALK; `Offsetting.Apply` owns the medial.

**Phantom-member check (kernel side):** `ContentHash.Of` fenced in 3 kernel pages; `TopologyDetailed`/`TopologyReceipt` frozen names (`mesh.md:5`); `SampleDetailed` fenced `fields.md:370` (DECISION row 28). All three "designed public surfaces" per `GBRIEF [LANDED_KERNEL_LAW]` — NOT phantoms. The seven NEW/REBUILD members above are DECISION-minted design surfaces (kernel is pre-realization `.planning`), signature-locked by the DECISION, verified against the landed page rows.

**Fabrication-side seam anchors (DECISION [04] `FAB:NN` = `Rasm.Fabrication/ARCHITECTURE.md:NN`) — all HOLD exact:** `FAB:22` `Skeleton.cs` (ARCH:22 "…with the wavefront clearance-radius field" ✓); `FAB:23`+`FAB:48` `Slicing.cs` (ARCH:23 slicer ✓ / ARCH:48 "…re-routes to the kernel `IntersectOp.PlaneMesh` … when realized" ✓ — the promise the DECISION REALIZES); `FAB:33`+`FAB:46` `Projection.cs` BSP (ARCH:33 "BSP front-to-back HLR" ✓ / ARCH:46 "…beside the in-folder BSP solver" ✓ — the "beside" clause dies); `FAB:44` repair→arrangement re-home (ARCH:44 `BooleanOp` WIRE ✓); `FAB:47` `HiddenLineResult` (ARCH:47 → AppUi Render ✓).

---

## [C] — PER-UPSTREAM-DOCUMENT BINDING VERDICTS

### GBRIEF — `RASM-CS-GEOMETRY-BRIEF.md` — DRIFT (all E14 line-anchors +14..+16)
Every Fabrication-relevant clause is present on disk; the E14 line pins are stale (see §D-1). Substance HOLD: `[V10]` toolpath gate a-e (GBRIEF:128-130); `[V3]` kernel DrawingProjection (GBRIEF:100-102); `[V5]` Manifold `ScaleCeiling` tier-3 (GBRIEF:108-110); `[V8]` one distance-field lane `SignedDistanceFromMesh` through index-accelerated winding (GBRIEF:122); `[V11]` hull/stock-envelope two-tier (GBRIEF:132-134); `[04]` re-scopes — float-lane pin re-group (GBRIEF:210), geometry3Sharp/MKL kernel-csproj drop with "pins stay for Bim/Fabrication/Compute; their disposition is those campaigns'" (GBRIEF:78), MathNet 6.0.0-beta2→5.0.0 downgrade naming Fabrication a co-consumer (GBRIEF:78), Fabrication toolpath dry-run acceptance (GBRIEF:226(b)).

### GDECISION — `RASM-CS-GEOMETRY-DECISION.md` — HOLD (landed; signature-lock source)
The DECISION is the authoritative signature-lock (§B). Probe rulings landed: R1 GShark VENDOR-AND-OWN (`Parametric/nurbs.md` row 20; pin dies wave-4); R2 implicit-point V4 EXACT PATH lands. V10 disposition (line 149) carries both Fabrication hinges. Seam ledger rows 123-127 bind the five Fabrication ARCH anchors exactly. **Spelling-lock is RESOLVED — the E14 "absent → PENDING" branch does not fire.**

### PBRIEF — `RASM-CS-PERSISTENCE-BRIEF.md` — **REFUTED** (the `[V12]` ArtifactKind binding does not exist)
The FAB brief attributes to "PBRIEF `[V12]`" an ArtifactKind egress index over machine-consumable artifacts. Disk falsifies (§D-2): PBRIEF `[V12]` is **`[V12_GOVERNANCE_RECONCILE]`** (card corrections), not an artifact index. No `ArtifactKind`, `.cli`, `NC1`, `traveler`, `3MF`, or `stock-state` token anywhere in the brief. The Fabrication egress binding exists ONLY as a **held-open BLOCKED card**: `PBRIEF:221` [06] out-of-scope — "Blocked/queued cards held open by design: … `[FABRICATION_PROGRAM_DURABLE_ROWS]` (Fabrication wire pins; **2701-2710 decode constraint recorded**)." The `[V4_FAULT_BAND_REGISTRY]` (PBRIEF:93) pins the "AEC 23xx-27xx registry" as a foreign mirror generically (2400 = kernel `GeometryFault`), so the 2700 century is registry-visible, but the specific 2701-2710 receipt DECODE is the blocked-card constraint. Persistence egress proper (`[V3_CHANGEFEED_EGRESS]`, PBRIEF:89) is CDC/CloudEvents over the ElementGraph outbox — orthogonal to fabrication artifacts.

### CBRIEF — `RASM-CS-COMPUTE-BRIEF.md` — HOLD strata / **REFUTED** WasteAreaMm2 waterfall
`[00]` strata law HOLD (CBRIEF:41 "Compute is APP-PLATFORM, depending UP on `Rasm` … and `Rasm.Element`" — no AEC-peer reference; the FAB `physics`/`nfp` peer-boundary posture is correct). **REFUTED:** the FAB brief's "`CBRIEF [V12]` WasteAreaMm2 row" (cited as a waterfall clause "verified in place, not re-edited" in FAB [05]) — CBRIEF `[V12]` is **`[V12_DISCIPLINE_COVERAGE]`** (Circulation/egress/life-safety disciplines); zero `WasteAreaMm2`/`AggregateEnvironmental`/`AggregateCost`/`NestYield` tokens anywhere in the brief. The `aggregator.md` sound surface (CBRIEF:161) owns the Environmental/Cost rollup but is "interiors untouched" (CBRIEF:171) and never names the Fabrication waste input. GLB: `[DATA_GEOARROW_GLB]` is a `[COMPLETE]` Compute card (CBRIEF:207) — the "mesh/GLB wire layout law (decode, never re-mint)" HOLDS as Compute ownership, Fabrication decodes.

### APPHOST — `RASM-CS-APPHOST-BRIEF.md` — HOLD (exact, both consumers named)
`[04]` MTConnect.NET row (APPHOST:210): "`Rasm.Fabrication` `Process/magazine` mid-job tool-life reload is the named demanding consumer, and **`Verify/probing` measured-feature/work-offset observations the second** (both pin the `-Common` model slice only and firewall transport here, `RASM-CS-FABRICATION-BRIEF.md` `[04]`/`[V7]`)." Exactly the E14/[05] "BOTH Fabrication consumers" claim. BACnet (APPHOST:209) added — its named forward consumer is the Compute inverse-UQ/twin-calibration lane, NOT Fabrication; the FAB `[00]`/`[06]` correctly places BACnet-class transports as livewire's (out of Fabrication scope). MTConnect transport/agent/OPC-UA/umati all AppHost-owned.

### UIBRIEF — `RASM-CS-APPUI-BRIEF.md` — HOLD substance / DRIFT verdict-number
`[V7_DRAFTING_WRITE_LEG]` (UIBRIEF:106-109) is the CAD write leg — the FAB `[V7]` citation is CORRECT. Two-row ACadSharp `DxfWriter`+`DwgWriter` over ONE `CadDocument` fold (UIBRIEF:108(a), :187); netDxf REMOVES; `FAB:52` "netDxf in AppUi owns the distinct WRITE leg" refines to the ACadSharp two-format leg (UIBRIEF:142 E7). HiddenLineSeam both-sides declared (UIBRIEF:108 "the seam declared BOTH sides — `ARCH:75-76` ⇄ `Rasm.Fabrication/ARCHITECTURE.md:47`"); geometry-supersession receipt insulation at `[V7](b)` + `[03]` drafting row (UIBRIEF:162, EXACT quote). **DRIFT:** FAB E14 cites "UIBRIEF `[V6]`" for HiddenLineSeam both-sides — it is `[V7]`(b) (UIBRIEF `[V6]` is `[V6_FENCE_REALISM_SET]`). Also: `Rasm.AppUi/ARCH:72` Geometry `DrawingProjection` row RETIRES (UIBRIEF:108(b)) — "Fabrication is the kernel engine's consumer, never AppUi" — reinforcing FAB `[V2]c`.

### ARCHIVE — `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` — HOLD (exact, landed law)
`:56` "the ONLY integer change is the collision-forced Fabrication 2501-2509 to 2701-2709 re-band, carried per `[LEG-FABRICATION]` behind its persistence check" ✓. `:75` "`Nest.Honor` rebinds to `NestPlan`; the mirror deletes; the re-band lands **2701-2710** behind the persistence check. Next campaign: any cross-process reader filtering 2501-2509 re-points (the R6 census)" ✓ — both the Nest.Honor rebind AND the 2701-2710 span (offsets 2701-2709 preserved + Nest re-homed at +10 = 2710, lines 700-706). `:149` `FaultBand.Fabrication = new(2700, owner: "Rasm.Fabrication (re-banded off the live 2501-2509 collision inside 2500)")` ✓. `:146` `Element = new(2500)` confirms `faults.md:33`'s "Element's 2500 band" reference is a LIVE-CORRECT rationale note, not stale. Nesting/stock landing (`:91,100,101,102`, `:686-687`) — "build ON, never re-band or re-move" HOLD.

---

## [D] — CROSS-CUTTING FINDINGS

### D-1 — Systematic E14 GBRIEF anchor drift (+14 to +16 lines) — DRIFT, corrected table
The geometry brief gained ~14-16 lines above line 62 after the Fabrication brief froze its E14 pins; every GBRIEF sub-anchor is stale by a near-constant offset. Substance intact throughout — DRIFT, never REFUTED. Corrected anchors:

| E14 sub-claim | FAB-cited | Corrected on disk | Δ |
|---|---|---|---|
| `[V10]` toolpath gate | `GBRIEF:114` | `GBRIEF:128-130` (`[V10_TOOLPATH_PLANE]`) | +14/+16 |
| `[V3]` view engine | `GBRIEF:86` | `GBRIEF:100-102` (`[V3_VIEW_ENGINE]`) | +14/+16 |
| `[V5]` Manifold scale tier | `GBRIEF:94` | `GBRIEF:108-110` (Manifold named `:110`) | +14/+16 |
| float-lane pin re-group | `GBRIEF:194` | `GBRIEF:210` (Fabrication-lane records) | +16 |
| geometry3Sharp/MKL kernel drop | `GBRIEF:62` | `GBRIEF:78` (`[ROSTER_RECONCILIATION]`; VERDICT ctx `:31`) | +16 |
| toolpath dry-run acceptance | `GBRIEF:210` | `GBRIEF:226(b)` ([05] acceptance) | +16 |

The DECISION mints the `FAB:NN` anchors permanently (DECISION [04] rows 123-127) — Fabrication should signature-lock against the **DECISION** anchors (stable), not the drifted brief line pins.

### D-2 — Two REFUTED `[V12]` reciprocal bindings — the load-bearing risk
The FAB brief cites two upstream `[V12]` verdicts as landed law; both cited verdicts carry entirely different content, and the actual bindings are un-landed forward demands.

**PBRIEF `[V12]` ArtifactKind (REFUTED).** FAB line 3 / line 58 / E14 assert "every machine-consumable egress artifact … an `ArtifactKind` row on the one content-keyed artifact index over blobstore content rows + cache artifact index (`PBRIEF [V12]`), durable receipts decoding 2701-2710." Disk: PBRIEF `[V12]` = `[V12_GOVERNANCE_RECONCILE]`; no `ArtifactKind` type, no egress-artifact index, no `.cli`/`NC1`/`3MF`/`traveler`/`stock-state` vocabulary. The real home is `PBRIEF:221` held-open card `[FABRICATION_PROGRAM_DURABLE_ROWS]` ("Fabrication wire pins; 2701-2710 decode constraint recorded") — a BLOCKED/QUEUED forward dependency, plus the generic AEC-27xx mirror in `[V4]` (PBRIEF:93). `[ARTIFACT_CONTENT_KEY_FEDERATION]` is a second held-open, python-producer-gated card.

**CBRIEF `[V12]` WasteAreaMm2 (REFUTED).** FAB [05] lists "`CBRIEF [V12]` WasteAreaMm2 row" among "the four waterfall clauses this brief landed upstream … verified in place, not re-edited"; FAB [06] repeats "the `WasteAreaMm2` rollup runner (`CBRIEF [V12]` counterpart row)". Disk: CBRIEF `[V12]` = `[V12_DISCIPLINE_COVERAGE]` (Circulation/egress); zero waste-rollup tokens brief-wide. The FAB `ARCH:51` seam is one-sided — genuinely "un-named Compute-side" (as E11 admits) — and the "waterfall landed" half is false.

Impact: the Fabrication DECISION must NOT treat persistence egress (cut programs, `.cli`, NC1, 3MF, stock snapshots, travelers) or the Compute waste rollup as settled upstream contracts. Both are FORWARD demands — the persistence one blocked by design on the Persistence side. The `CutProgram` content-key + `ArtifactKind` egress fold is a Fabrication-authored demand on a Persistence card that is explicitly held open; the DECISION should record it as a cross-package residual/blocked seam, not a composed binding, and mint the content key through the confirmed `ContentHash.Of` entry (which IS landed) rather than a presumed Persistence `ArtifactKind` index.

### D-3 — UIBRIEF verdict-number drift (DRIFT)
FAB E14 "UIBRIEF `[V6]` HiddenLineSeam both-sides" → the binding lives in `[V7_DRAFTING_WRITE_LEG]`(b) (UIBRIEF:106-109). `[V6]` is `[V6_FENCE_REALISM_SET]` (unrelated). The `[03]` drafting-row insulation citation is exact (UIBRIEF:162). Correct the verdict number to `[V7]`.

### D-4 — `[RESEARCH]`-tail census undercount (DRIFT)
E13/V2 enumerate three tails; disk carries FIVE: `kinematics.md:122`, `skeleton.md:199`, `slicing.md:219` (cited) **plus** `stock.md:317` (`[05]-[RESEARCH]`) and `program.md:379` (`[03]-[RESEARCH]`) (uncited). The V2 zero-hit gate covers all five; the anchor set must expand.

### D-5 — NAMESPACE census (HOLD — 12 realized namespaces, 5 folders)
Fence-declared `namespace` per page confirms the V1 claim exactly ("12 realized namespaces"): `Process`(owner) · root/`Rasm.Fabrication`(faults) · `ProcessModel`(family, magazine) · `ProcessPhysics`(physics) · `Geometry2D`(clipper, import) · `Toolpath`(motion, skeleton, guard) · `Kinematics`(kinematics) · `Additive`(slicing) · `Nesting`(nfp, stock) · `Fixturing`(workholding) · `Posting`(program) · `Projection`(projection). Folder≠namespace schisms (V1's six ratifications) all disk-confirmed: `family`/`magazine`=ProcessModel (folder Process), `physics`=ProcessPhysics, `faults`=root, `import`=Geometry2D (folder Polygon), `projection`=Projection (folder Posting); plus `workholding`=Fixturing (folder Nesting), `kinematics`=Kinematics + `slicing`=Additive (folder Toolpath), `clipper`=Geometry2D (folder Polygon). Every MOVE the recommended partition names is namespace-motivated on disk.

### D-6 — CONSUMER census (HOLD — E1 four-package zero-consumer confirmed)
Word-boundary grep over all 17 pages: **PicoGK 0 · OcctNet 0 · DSTV 0 · SharpVoronoiLib 0 · Voronoi 0**. Contrast (consumed): Clipper2 11 · Robots 6 · CavalierContours 2 · RectangleBinPack 2 · RectpackSharp 2 · ACadSharp 1. E1 "the roster is fiction — four admitted packages have ZERO page consumers" is exact. (RectpackSharp still referenced in 2 pages — the E12 redundancy REMOVE target; its consumer re-homes to RectangleBinPack per [04].)

### D-7 — SEAM census (cross-package edges, upstream-facing)
`ARCH:[02]` cross-package edges verified against upstream reciprocals: `Posting/projection ← Rasm/Drawing/view` (FAB:46 ⇄ DECISION row 27 `in: Fabrication`) HOLD; `Posting/projection → Rasm.AppUi/Render HiddenLineResult` (FAB:47 ⇄ UIBRIEF:108 ARCH:75-76) HOLD; `Posting/projection ← Rasm/Processing/repair BooleanOp` re-points to `Meshing/arrangement` (FAB:44 ⇄ DECISION row 9) HOLD; `Toolpath/slicing → Rasm/Meshing/intersect` (FAB:48 ⇄ DECISION row 17 `Slicing.Apply`) HOLD; `Process/physics ← Rasm.Materials/Properties` raw-double peer boundary HOLD; `Nesting/stock → Rasm.Compute WasteAreaMm2` reciprocal REFUTED (D-2). Undeclared intra/cross edges the DECISION ledger must add: `Nesting/stock ← Rasm.Element/MaterialId` (stock.md:40, wired-undeclared); the intra-package web (magazine→guard/workholding, guard→skeleton, workholding→posting, motion→skeleton/slicing/kinematics). Stale targets to re-point: `ARCH:53,54 Rasm.Persistence/Schema` (deleted → blobstore/cache owners, subject to D-2's un-landed caveat).

---

## [E] — CHARTER-AS-IT-SHOULD-BE (upstream-bindings corrections the DECISION must carry)

1. **Signature-lock against the DECISION, not the drifted brief.** Lock the eight §B members and the `FAB:22/23/33/44/46/47/48` anchors against `RASM-CS-GEOMETRY-DECISION.md` (stable), retiring the E14 GBRIEF line pins (D-1 corrected table). The E14 "spelling-lock PENDING" branch is closed — mark it RESOLVED.

2. **Demote the two REFUTED `[V12]` bindings to forward demands.** Re-scope FAB line 3 / line 58 / E14 / [05] / [06]: persistence egress is a **BLOCKED cross-package dependency on Persistence `[FABRICATION_PROGRAM_DURABLE_ROWS]`** (not landed "PBRIEF `[V12]` ArtifactKind"); the Compute waste rollup is a **one-sided forward seam** (not landed "CBRIEF `[V12]` WasteAreaMm2 … verified in place"). Mint `CutProgram`/egress content keys through the confirmed `ContentHash.Of` entry; carry the `ArtifactKind`/durable-row fold as a Fabrication-authored demand + seam-ledger residual, never a composed upstream contract.

3. **Fix verdict-number + anchor-set drift.** UIBRIEF `[V6]`→`[V7]`(b) for HiddenLineSeam; expand the `[RESEARCH]`-tail set to five (add `stock.md:317`, `program.md:379`); add the `Nesting/stock ← Rasm.Element/MaterialId` ledger row.

4. **The R6 25xx purge is exact and complete** across ARCH/kinematics/TASKLOG/api-dstv-net/api-robots; `faults.md:33`'s "Element's 2500 band" reference STAYS (Element genuinely owns 2500, archive:146) — do not scrub it as stale.

**Method note (kernel-member verification):** kernel is pre-realization `.planning` — no restored assembly — so member verification READS the landed DECISION page rows + kernel fences per E14, corroborated for the frozen names (`ContentHash.Of` ×3 pages, `TopologyDetailed`/`TopologyReceipt` `mesh.md:5`, `SampleDetailed` `fields.md:370`). No `nuget MCP`/web tier was needed — every consumed member resolves against the landed geometry DECISION.
