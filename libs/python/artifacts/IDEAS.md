# [PY_ARTIFACTS_IDEAS]

The folder's forward pool of higher-order concepts, each grounded in artifact production and the host-free companion charter. Open ideas are cards in `[1]-[OPEN]`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition. Each idea drives one or more `TASKLOG.md` tasks.

## [1]-[OPEN]

[DOCUMENT_SECURITY_AND_NAVIGATION]:
- Stand up the missing publication-egress sub-domain — `documents/egress` — as ONE `DocumentEgress` owner discriminating a closed `EgressStep` over the security-and-navigation finishing layer that lowers onto an already-emitted PDF: AES encryption with a permission policy (pikepdf `Encryption(owner=..., user=..., R=6, aes=True, allow=Permissions(...))` over `Pdf.save`, pypdf `PdfWriter.encrypt`), outline/bookmark-tree authoring (weasyprint `make_bookmark_tree`, pypdf outline, pikepdf `Outline`), page-overlay/underlay watermarking and stamping (pikepdf `Page.add_overlay`/`add_underlay`/`as_form_xobject`), embedded-attachment binding (pikepdf `AttachedFileSpec`), and n-up/booklet imposition (pypdf `Transformation`/`merge_page` page composition) — each step a row binding its package call, never an `if encrypt` branch.
- Unlocks the finishing half a publication companion owns and no current owner reaches: a distributed report needs an owner password and an extract-disabled permission set, a quoted brief needs a confidential watermark, a bound document needs a clickable outline and an attached source dataset, a print job needs 2-up imposition — today the document axis emits a flat byte stream with no security, no navigation tree, no watermark, and no imposition, so every published artifact is unsealed and un-navigable. This is a frontier RE-SEED: a new sub-domain, not a restatement.
- Draws on the gap that the pikepdf catalogue documents the entire `Encryption`/`Permissions`/`Outline`/`add_overlay`/`add_underlay`/`as_form_xobject`/`AttachedFileSpec`/`open_metadata` surface and the pypdf catalogue documents `PdfWriter.encrypt`/outline-authoring/`Transformation`/`merge_page`, while `documents/document-plan#DOCUMENT` exploits only `pikepdf` `Pdf.save(linearize=True)` repair and `pypdf` `PdfWriter` assembly — the second-largest unexploited admitted surface in the folder, and the one finishing concern whose intent is a security-and-navigation algebra over an emitted PDF, never a flat re-emit.

[SHAPED_TEXT_LAYOUT]:
- Promote typography from subset/instance/sign to true TEXT SHAPING: one `Shaping` arm on the typography owner over the entire admitted-but-idle uharfbuzz pipeline — `Blob.from_file_path`/`Face.create`/`Font.create`, `Buffer.create`/`add_str`/`guess_segment_properties`, `shape(font, buffer, features)`, then the `GlyphInfo`/`GlyphPosition` advance/offset read — producing a positioned-glyph run the document and figure owners place, plus the COLRv1 `PaintFuncs` color-glyph path and `Font.set_variations` axis pinning, with `Font.draw_glyph_with_pen` bridging the shaped run to a fontTools `SVGPathPen` for vector glyph outlines.
- Unlocks correct multilingual, right-to-left, and complex-script (Arabic/Devanagari/Thai) layout no current owner can produce: a publication companion that subsets and signs fonts but never SHAPES text cannot lay out a single ligature, kerned pair, or RTL run correctly — the glyph positions are wrong without OpenType layout, so any non-trivial typographic artifact is mis-set. Shaping is the load-bearing missing leg between font embedding and document text placement.
- Draws on the gap that uharfbuzz is admitted, manifest-declared, fully catalogued (`shape`/`Buffer`/`GlyphInfo`/`GlyphPosition`/`PaintFuncs`/`Font.set_variations`/`draw_glyph_with_pen`), and ENTIRELY unexploited — the conformance page names it only as a transparent fontTools repacker, never as the shaping engine it is. A light external surface on an admitted shaping core is the highest deeper-research signal in the folder.

[FIGURE_COMPOSITION_OWNER]:
- Stand up the `imaging/figure` sub-domain as ONE `Figure` owner over two complementary composition surfaces that turn emitted graphics into placed, annotated, color-correct figures: the vector side over the new `svgelements` admission — `SVG.parse`/`Path`/`Matrix.scale`/`translate`/`rotate`/`bbox` for scale-to-fit, n-up tiling, crop, and bounds query over the SVG the chart/QR/nanoplot owners emit — and the raster side over the unexploited pillow annotation surface (`ImageDraw` primitives/text, `ImageFont.FreeTypeFont`, `ImageOps` autocontrast/fit/pad, `ImageFilter`, `Image.Exif` EXIF/XMP metadata read-write) on the gated band. A `FigureOp` closed `StrEnum` discriminates the composition operation; each op a row, never a parallel figure class.
- Unlocks the figure-assembly layer between raw render and document binding: a report binds a scale-to-fit chart with a caption overlay and a confidence-band annotation, a preview carries a watermark and a corrected EXIF orientation, a multi-panel figure tiles four charts on a grid — today the chart owner emits one SVG, the preview owner thumbnails one raster, and nothing composes, annotates, transforms, or sets metadata on the emitted graphic before it crosses into the document. The figure is the unit a publication binds; the folder has no owner for it.
- Draws on the gap that the SVG the chart/QR/nanoplot owners produce has no transform/compose/query owner (svgelements fills it, pure-Python cp315-clean), and the pillow catalogue documents `ImageDraw`/`ImageFont`/`ImageOps`/`ImageFilter`/`Image.Exif` — a full draw/annotate/metadata surface — while `imaging/preview#PREVIEW` exploits only `thumbnail`/`save`/`montage`/`slic`. A frontier RE-SEED: a new sub-domain over one new admission plus one large idle surface.

[TYPST_INTROSPECTION_AND_DATA_BIND]:
- Widen the Typst path from one-shot compile to a queryable, data-bound document world: bind the typst `Compiler` reusable font-cached world and add the `query(selector, field)`/`eval(expression)` introspection arm and the `sys_inputs` data-injection arm — so a Typst report is a parameterized template whose outline, headings, figures, and labels are extracted as structured data (the document-introspection inverse of emission) and whose runtime data binds through `sys.inputs` without string templating, with the cached `Compiler` amortizing font load across a batched render of many documents.
- Unlocks the reproducible data-driven document the reporting axis needs but the current single-shot `typst.compile` cannot reach: a study report whose tables, captions, and cross-references derive from injected data and whose structure is queried back for a TOC or an index — the same produce-then-introspect inversion the document-node seam owns for PDF, but native over the Typst world that already typesets PDF/A, with one cached compiler instead of a fresh world per document in a batch.
- Draws on the gap that the typst catalogue documents `query`/`eval`/`sys_inputs`/the reusable `Compiler`/`Fonts`/`FontInfo`, while `documents/document-plan#DOCUMENT` `PDF_TYPST` exploits only the single-shot `compile(source, output=None, pdf_standards=...)` — the entire introspection, data-injection, and font-caching surface sits idle. This deepens the existing Typst leg rather than re-seeding a sub-domain.

[BIDIRECTIONAL_DOCUMENT_SEAM]:
- Introduce the semantic document-node algebra as the SINGLE interior representation the `documents` axis lowers from and recovers to: one `DocumentNode` msgspec tagged-union tree (`page`/`section`/`block`/`run`/`table`/`figure`/`field`/`annotation`/`structure_element`) that backend emission folds FROM — each `DocumentPlan` backend a lowering arm over the tree rather than over an opaque payload — and one `DocumentLens` extraction verb recovers TO (text/image/word-geometry/region/search/embedded/outline/redaction/annotation/`Story`), so production and extraction are inverses over one node algebra and `diff`/`merge`/`query` are defined once. The recover-TO arm rides pymupdf `Page.get_text`(dict/words/json)/`get_images`/`search_for`/`apply_redactions`/`Annot`/`Story` plus pypdf `PdfReader.extract_text`/`extract_images`/`Transformation`/outline-and-XMP; the corpus-query surface keys an extracted-tree table by content key over the runtime columnar lane so a multi-PDF corpus is one queryable value.
- Unlocks the produce-extract-re-produce identity-stable inversion the charter names but no owner reaches: emission lowers an opaque payload one-directionally today, so a quoted-source report, a privacy redaction pass, a structured-text mine, and a document `diff` each have no owner — the node algebra makes the document a queryable value, not an emit-only byte stream, and the same `DocumentDelta` the geometry object-graph diff reuses is defined here once.
- Draws on the gap that `documents/document-plan#DOCUMENT` dispatches backends over an opaque payload with NO interior representation, the `pymupdf` catalogue documents the entire MuPDF extraction/redaction/annotation/`Story`/OCG/embedded-file surface, and the `pypdf` catalogue documents `extract_text`/`extract_images`/outline/XMP — the largest single unexploited admitted surface in the folder, and the one concern whose intent is bidirectional inversion over a shared tree, not a flat read-verb scatter.

[ARCHIVAL_CONFORMANCE_VERIFICATION]:
- Extend the typography close from emit-and-sign to emit-sign-and-PROVE: one `ConformAudit` verb on the conformance owner that reads a signed/archival PDF and returns a structured conformance verdict — PAdES signature validity, coverage level, modification-difference analysis, DSS/LTV validation-material completeness, XMP/PDF-version archival-metadata presence — over the pyhanko `validate_pdf_signature`/`EmbeddedPdfSignature.evaluate_signature_coverage`/`add_validation_info`/`PdfSignatureStatus` surface and the pikepdf `open_metadata`(XMP)/object-model structural-inspection surface, folding one typed `ConformanceVerdict` receipt case.
- Unlocks the verification half of conformance: an archival pipeline that signs but never proves the signature validates, covers the whole document, and carries its LTV material is a half-owner — the "conformance" the folder name promises is structural-and-cryptographic PROOF, not signing alone, and the audit closes the open loop where pyhanko's entire validation surface sits idle beside the signing surface it already consumes.
- Draws on the gap that `typography/conformance#CONFORM` consumes only the pyhanko signing surface (`SimpleSigner.load`/`sign_pdf`) while the `pyhanko` catalogue documents `validate_pdf_signature`/`async_validate_pdf_signature`/`evaluate_signature_coverage`/`collect_validation_info`/`add_validation_info`/`PdfSignatureStatus` as a full verification pipeline, and pikepdf's `open_metadata` XMP and object-model inspection prove the archival-metadata layer no current owner reads.

[ICC_COLOR_MANAGED_OUTPUT]:
- Promote the color owner from palette/array derivation to true color MANAGEMENT: one `ColorManaged` arm binding an ICC source/destination profile pair, rendering intent, and a LUT/CCTF transform chain across the raster and document egress — pillow `ImageCms.ImageCmsProfile`/`buildTransform`/`applyTransform` profile conversion on the raster side and colour-science `read_LUT`/`LUTSequence`/`cctf_encoding`/`cctf_decoding`/`delta_E` LUT-and-tone-curve operators on the color side — so a preview, a chart raster, and a print-bound PDF carry a managed, intent-correct color space rather than naive sRGB bytes.
- Unlocks the "color-managed assets" the folder charter promises but no owner delivers: today `color-management` emits palettes and conversions as raw arrays and the visual owners consume them, but no owner attaches an ICC profile, applies a rendering intent, or runs a LUT/tone-curve transform on an emitted raster — print-grade and wide-gamut output is unreachable.
- Draws on the gap that the `pillow` catalogue documents `ImageCms.ImageCmsProfile` and the `colour-science` catalogue documents `read_LUT`/`write_LUT`/`LUT1D`/`LUT3D`/`LUTSequence`/`cctf_encoding`/`cctf_decoding`/`delta_E` — a full color-management and LUT pipeline — while `color-management/colorimetry#COLOR` exploits only `convert`/`sd_to_XYZ`/`XYZ_to_Oklab` and the imaging owner attaches no profile, so the management layer is entirely unbuilt above the colorimetry primitives.

[ENCODED_MARK_OWNER]:
- Widen the imaging code-generation arm from QR-only to the full machine-readable-mark family: one `EncodedMark` axis discriminating symbology — QR/Micro-QR over the existing segno arm and linear/2D barcodes (Code128, EAN-13/8, UPC-A, ITF, Code39, DataMatrix, PDF417) over an admitted `python-barcode` arm — each symbology a row binding its encoder, all serializing to the dependency-free SVG/PNG vector path the segno arm already owns.
- Unlocks the labeling/identification half of a publication companion: a fabrication traveler, a shipping label, a product code, or an inventory tag needs a linear or matrix barcode that QR cannot express, so a folder that emits only QR is a thin single-product code owner — the same shape Materials avoids by owning real vocabularies rather than one named product.
- Draws on the gap that `segno` owns QR/Micro-QR exclusively (the catalogue's `PROVIDED` set is QR-only) while linear and matrix symbologies have no owner, and the encoded-mark concern is one symbology-row axis, not a parallel code class — `python-barcode` is the pure-Python cp315-clean owner-to-extend, its catalogue and fence spellings held RESEARCH until the package resolves on the sync.

[PUBLICATION_TABLE_DEPTH]:
- Recover the full great-tables publication surface the table owner leaves on the floor: extend `TablePlan` with the header/footnote/source-note layer (`tab_header`/`tab_footnote`/`tab_source_note`), the summary-row algebra (`summary_rows`/`grand_summary_rows`), the `loc`-targeted `tab_style` cell-style injection (text/fill/borders over `loc.body`/`loc.column_labels`/`loc.summary` selectors), the column-control family (`cols_label`/`cols_align`/`cols_merge`/`cols_width`/`cols_hide`), and the `fmt_image` inline-image cell — folding each as a data-driven spec row beside the existing spanner/format/fill folds, never a per-feature method chain.
- Unlocks the difference between a styled grid and a publication table: titled, footnoted, summary-row-bearing, cell-styled, column-merged tables are the real artifact a report binds, and the current owner exposes only spanners, value formats, and data-driven fill — a fraction of the admitted surface.
- Draws on the gap that the `great-tables` catalogue documents `tab_header`/`tab_footnote`/`tab_source_note`/`tab_stub`/`summary_rows`/`grand_summary_rows`/`tab_style`/`fmt_image`/`cols_*` and the entire `loc`/`style` submodule while `tables/table-plan#TABLE` consumes only `tab_spanner`/`fmt_*`/`data_color`/`as_raw_html`/`as_latex`/`save`. Deepened: add the `tab_options(**kwargs)` global-theme row (table/heading/column-labels/body/stub/footer theme policy) and the `cols_merge_range`/`autocolor_text` arms, so a table carries a named theme and a luminance-derived auto text color, never a per-call style scatter — the theme is the publication identity the report binds.

[CONTENT_KEYED_REUSE_AND_SIGNAL]:
- Realize the two branch fabrics on the one receipt edge every producer already owns: thread each producer's `(ContentKey, Work)` pair into the runtime `concurrency/lanes` admission elision so an identical spec at an identical content key short-circuits to the cached key with the `ArtifactReceipt` carrying the hit/miss distinction, AND route every production duration/byte-volume/compression-ratio signal through the runtime `observability/metrics` `MeterProvider` — both folded through the single `receipt/artifact-receipt#RECEIPT` `contribute` path, never a parallel cache owner or metric owner, so the receipt is the one fact carrier the reuse fabric and the metric stream both consume.
- Unlocks the inheritance the architecture names: every `_emit` already returns `ContentIdentity.of(...)`, so the most expensive outputs (a signed archival PDF, a rendered notebook, an offscreen 3D scene) become elision targets and observable render-duration histograms with zero new surface — the producers are pre-keyed consumers of the branch fabrics, not second owners.
- Draws on the branch `CONTENT_ADDRESSED_REUSE_FABRIC` and `ONE_MEASURED_SIGNAL_STREAM` seeding: the artifacts side is the receipt-fold consumer of both, aligned on the runtime lane-admission and `MeterProvider` tasks landing the instrument set.

[OUTWARD_FIGURE_HANDOFF_AND_DRIFT]:
- Hold the artifacts outward edge to the one branch `HandoffAxis` and the cross-language drift guard: a figure/table/chart that crosses outward to a sibling package travels only as the one `compute/graduation` `HandoffAxis` model-asset case keyed by `ContentIdentity`, never a parallel per-artifact handoff, and the artifacts sources admit the `runtime/evidence` `Structural.drift` query so a re-minted content-identity seed, receipt rail, or wire-projection name in any artifacts module is detected — the artifacts package re-mints no canonical concept and exports figures only through the managed owner.
- Unlocks the branch invariant that no package grows a private handoff or re-mints a canonical concept: the artifacts producers consume `ContentIdentity`/`RuntimeRail`/`Receipt`/`ReceiptContributor` from runtime and contribute one `ArtifactReceipt` case, so the drift guard finds zero re-mints and the outward figure path is the single `HandoffAxis` model-asset case.
- Draws on the branch `ONE_GRADUATION_RAIL_OUTWARD` and master `CROSS_PACKAGE_DRIFT_GUARD` seeding: the artifacts contribution is verification-and-alignment, the figure-handoff edge and the drift-clean source surface, never a new owner.

## [2]-[CLOSED]

- [HOST_FREE_RENDER_FLOOR] — Closed: `charts/chart-spec#EXPORT` folds vega through vl-convert (primary, cp315-core) with the gated `vegafusion` transform pre-pass and matplotlib render on the `anyio.to_process.run_sync` gated-band seam; kaleido is the host-Chrome-gated degraded arm.
- [REPRODUCIBLE_NOTEBOOK_REPORT] — Closed: `reporting/report-plan#REPORT` `NOTEBOOK` kind runs `papermill.execute_notebook` on the nbclient engine and serializes the executed notebook through `nbformat.write` as the content-keyed audit artifact.
- [PUBLICATION_TABLE_OWNER] — Closed: `tables/table-plan#TABLE` over great-tables `GT` folds spanners/formats/fills and emits HTML (`as_raw_html`) and LaTeX (`as_latex`) host-free, PDF (`save`) host-Chrome-gated. Re-opened as `[PUBLICATION_TABLE_DEPTH]` for the unexploited header/summary/style/cols surface.
- [PDF_CONFORMANCE_AND_SIGNING] — Closed: `typography/conformance#CONFORM` folds fonttools `Subsetter`/`instancer` and pyhanko `SimpleSigner.load`/`sign_pdf` with `use_pades_lta` and the `HTTPTimeStamper` authority. Re-opened as `[ARCHIVAL_CONFORMANCE_VERIFICATION]` for the unexploited validation surface.
- [COLOR_MANAGED_VISUAL_PIPELINE] — Closed: `color-management/colorimetry#COLOR` over colour-science folds CONVERT/SPECTRAL/APPEARANCE through the universal `convert` gateway and derives the Oklab-interpolated perceptually-uniform palette. Re-opened as `[ICC_COLOR_MANAGED_OUTPUT]` for the unbuilt ICC/LUT management layer above the colorimetry primitives.
- [SCIENTIFIC_IMAGE_ANALYSIS] — Closed: `imaging/preview#PREVIEW` `TRANSFORM` arm folds scikit-image `slic`/`mark_boundaries`/`regionprops_table` on the gated-band worker beside the pillow raster arms; segno owns the in-process QR arm. Code-generation widened by `[ENCODED_MARK_OWNER]` to the full symbology family.
