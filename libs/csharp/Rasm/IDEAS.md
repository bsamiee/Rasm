# [RASM_IDEAS]

The forward pool of higher-order kernel concepts, grounded in the robust-geometry domain and the monorepo geometry-flow. Each is a card: a bracketed slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on. `[1]-[OPEN]` carries live ideas; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[EXACT_ARRANGEMENT_BOOLEAN]: a fully-managed exact-arithmetic mesh/polygon arrangement owner in a new `Arrangement` robust-core sub-domain that retires the tier-3 native CSG gate — one `Arrangement` `[Union]` (`MeshBoolean`/`PlanarOverlay`/`CellComplex`) built over the constrained tetrahedralization the `Tessellation` owner emits, with cells classified inside/outside by the `spatial/index#GENERALIZED_WINDING` GWN scalar.
- The Cherchi-Attene exact mesh-boolean technique made managed by the landed `numerics/predicates#INDIRECT_PREDICATES` LPI/TPI family: every triangle-triangle intersection segment is an `Lpi`/`Tpi` implicit point whose in-circum and orientation signs are exact without a rounded materialized coordinate, so the arrangement of two triangle soups is built without epsilon snapping, each cell classified by the exact per-triangle solid-angle GWN, and the `BooleanOp` selection (`Union`/`Difference`/`Intersection`) is a cell-keep predicate over the classified complex welded through the healing `DuplicateWeld`. This is the managed companion the `healing/repair#BOOLEAN_NATIVE_ASSET` UPSTREAM-BLOCKED row names — the kernel already carries every floor it needs (exact implicit predicates, constrained tetrahedralization, GWN inside/outside), so the missing owner is the arrangement that composes them, not a native asset.
- Unlocks the fully-managed exact boolean/CSG frontier with zero native-deploy burden — the `healing/repair#HEALING` boolean row gains its real body, the branch `ROBUST_ARRANGEMENT_SUBSTRATE` consumers (`Rasm.Bim` solid classification, `Rasm.Fabrication` NFP/silhouette, `Rasm.Compute` volumetric mesher seed) read ONE arrangement keyed by the exact predicates, and a planar 2D overlay (polygon boolean, the exact alternative to the float Clipper2 the Fabrication lane carries) rides the same cell-complex algebra at triangulation arity.
- The kernel owns the predicate-exact arrangement substrate (constrained Delaunay/tetrahedralization, implicit points, GWN) but no owner assembles them into a boolean — a managed exact arrangement is precisely the missing concept between the substrate and the gated native asset, and the Cherchi 2020 / Diazzi-Cherchi-Attene 2023 indirect-predicate mesh-arrangement line proves the float-free path is a complete managed boolean kernel, not a half-correct approximation.

[ROBUST_INTERSECTION_LATTICE]: a predicate-exact curve/surface/mesh intersection owner in a new `Intersection` robust-core sub-domain — one `IntersectOp` `[Union]` (`SegmentSegment`/`SegmentTriangle`/`TriangleTriangle`/`RayMesh`/`MeshMesh`) over an exact-sign intersection lattice, the robust 3D counterpart the mature `Analysis/Intersect.cs` Rhino-delegated surface never owns at predicate exactness.
- The intersection result carrier is an exact implicit point (`Lpi`/`Tpi`) whose existence and ordering are decided by the `numerics/predicates` `Orient3D` straddle sign, never a float tolerance: a segment-triangle hit is a `Tpi` three-plane point gated by the symmetric straddle the healing `SelfIntersectResolve` already spells, a triangle-triangle crossing is the `Lpi`/`Tpi` segment whose endpoints sort by exact orientation, and a mesh-mesh intersection curve is the ordered chain the arrangement owner consumes. The lattice composes the `spatial/index` BVH broad-phase so the narrow-phase exact test runs only on candidate pairs, and re-emits the crossing as a `Vectors` `Polyline`/point set at the seam.
- Unlocks robust intersection every higher owner currently re-derives ad-hoc — the healing self-intersect split, the arrangement segment subdivision, the offsetting `SegmentsCross` test, and the AEC-domain silhouette/section all read ONE exact intersection lattice instead of four epsilon-tolerant crossing tests; a downstream section/contour cut becomes one `IntersectOp.Apply` against a cutting plane, never a host Make2D round-trip.
- The kernel has the exact-straddle crossing logic scattered inline in `healing/repair` (`TriangleCrossPoint`) and `offsetting/offset` (`SegmentsCross`) and the float-epsilon Rhino-delegated `Analysis/Intersect.cs`, but no single exact intersection owner — three near-identical exact crossing tests across three pages is the collapse signal, and the robust-mesh-intersection literature (Guigue-Devillers exact triangle-triangle, the implicit-point segment lattice) is the settled algorithm the one owner internalizes. DEEPENED: the `PlaneMesh` section arm is now a NAMED consumer — the new `HIDDEN_LINE_PROJECTION` `ViewOp.Section` reads `IntersectOp.Apply` against a cutting plane as its drawing-cut producer, so the section/contour case the AEC-domain reads is one `IntersectOp` arm, not a fourth host Make2D round-trip, and the `IntersectKind` `plane-mesh` row is the one growth case the projection owner composes.

[PRIMITIVE_FIT_FRONTIER]: a robust geometric primitive-fitting owner in a new `Fitting` robust-core sub-domain — one `FitOp` `[Union]` (`Plane`/`Sphere`/`Cylinder`/`Cone`/`Torus`/`Line`) over a RANSAC-plus-refit fold returning a typed `FitReceipt` (inlier set, residual, the fitted primitive), the scan-to-BIM segmentation primitive the kernel cloud algebra and the branch `REALITY_CAPTURE_TO_BIM` consumer need.
- The fitting fold is a MLESAC/PROSAC robust-consensus sampler over the `spatial/index` BVH for inlier neighbourhood queries, each minimal-sample primitive solved in closed form (three points for a plane, four for a sphere) and refined by a Levenberg-Marquardt geometric-distance minimization reusing the `constraints/solver` LM iterate over the primitive's parameter vector — so the fitting owner composes the existing constraint-solver kernel rather than re-minting a second nonlinear least-squares loop. The `FitReceipt` carries the typed inlier mask, the orthogonal-distance residual, and the fitted primitive as a `Vectors` value, feeding the BIM reconstruction `ElementPredicate` classification by reference.
- Unlocks the as-built reconstruction frontier the platform's reality-capture pipeline needs — point cloud in, classified planar/cylindrical/conical primitives out — with no second nonlinear solver and no second spatial structure; a new primitive (ellipsoid, freeform patch) is one `FitOp` case reading the same RANSAC fold and the same LM refine, and the offline learned-segmentation peer fit graduates inward onto the same `FitReceipt` shape.
- The kernel has the LM iterate (`constraints/solver`), the BVH neighbourhood query (`spatial/index`), and the cloud PCA/covariance vocabulary (`Vectors/Cloud.cs`) but no robust-consensus primitive-fit owner that composes them — the Schnabel-Wahl-Klein efficient-RANSAC and the MLESAC robust-consensus line is the settled technique, and the branch `REALITY_CAPTURE_TO_BIM` idea names `Rasm/Geometry/spatial` as a segmenter with no fitting owner behind it, so the fitting owner is the named gap the reconstruction pipeline composes.

[SURFACE_PARAMETERIZATION_OWNER]: a robust mesh parameterization / UV-flattening owner in a new `Parameterization` robust-core sub-domain — one `ParamOp` `[Union]` (`Harmonic`/`LSCM`/`ARAP`/`BFF`) over the `Vectors` DEC operator substrate, returning a typed `ChartAtlas` (per-chart UV layout, distortion receipt, seam set), the flattening primitive the fabrication unroll/nesting and texture pipelines need at kernel quality.
- The parameterization solves compose the `Vectors` `DiscreteCalculus` cotangent-Laplacian and the `LaplacianCache` Cholesky factor the spectral substrate already assembles: harmonic mapping is one Dirichlet solve over the boundary-pinned Laplacian, LSCM (Lévy least-squares conformal) is the complex-linear conformal-energy minimization, ARAP (Liu-as-rigid-as-possible) is the local-global alternation, and BFF (Sawhney-Crane boundary-first flattening) reads the same connection-Laplacian the `Vectors` SHM rail carries — every solve a composition over the settled DEC operators, never a re-assembled mesh Laplacian. The `ChartAtlas` carries the per-triangle distortion (conformal/area/quasi-conformal) as a typed receipt the fabrication nesting reads.
- Unlocks the flattening/unrolling frontier the AEC fabrication pipeline needs — a developable panel unrolls, a curved facade tile flattens to a cutting layout, a texture atlas packs — each a `ParamOp` case over the same DEC substrate, so a new flattening method is one case, never a parallel mesh-parameterization library; the chart atlas feeds the `Rasm.Fabrication` nesting and the AppUi texture path by reference.
- The kernel composes the richest DEC operator surface in the monorepo (`Vectors/Spectral.cs` owns d0/d1/star/cotangent-Laplacian/connection-Laplacian/spectral basis) yet exposes NO parameterization owner that turns those operators into a UV flattening — the mature `Vectors` `Mesh.cs` carries unwrap as a Rhino-delegated rail, not a kernel-quality conformal/ARAP solve; the Crane DDG parameterization line (LSCM, ARAP, BFF) is the settled algorithm the one owner internalizes over operators the kernel already assembles.

[OFFSETTING_FRONTIER]: a predicate-exact offsetting owner in the `Offsetting` robust-core sub-domain — straight-skeleton, medial-axis, and Minkowski-sum/offset over one closed `OffsetOp` algebra. (DEEPENED: the page is authored; the open work is the `[WAVEFRONT]`/`[PROJECTIONS]` body transcription targets plus the weighted/curved-input growth row, and the new managed-arrangement seam below.)
- The straight-skeleton wavefront-propagation kernel (Aichholzer-Aurenhammer shrinking-polygon edge/split-event queue) grounded on the exact `Predicate.Orient2D` turn sign so a reflex-vertex split event never mis-fires on a near-collinear edge; the medial axis read off the skeleton plus the constrained-Delaunay/Voronoi dual the `Tessellation` owner emits; and the Minkowski sum/offset as the convolution of edge-normals the same exact-turn predicate orders. DEEPENED: the `Minkowski` convolution and the `Offset` snapshot now ALIGN to the new `EXACT_ARRANGEMENT_BOOLEAN` planar-overlay arm for the boundary-loop assembly (`Assemble`) so a self-overlapping offset resolves through the exact cell complex rather than a float loop-stitch, and the weighted/mitered straight-skeleton (Aurenhammer-Walzl 2017 weighted skeletons) is the one named growth row the page reserves.
- Unlocks robust variable-distance polygon/curve offsetting, roof/wall straight-skeleton generation, medial-axis spine extraction for shape analysis, and Minkowski-based clearance/tool-path offsetting — the AEC-domain fabrication/nesting and facade-panelization consumers compose `OffsetOp.Apply` and the skeleton `ToMesh` projection through the `Vectors` `MeshSpace` seam, never by coupling into the wavefront interior.
- The kernel owns the exact-turn predicate and the constrained-Delaunay arrangement substrate but no offsetting owner consumes them; naive float-offset of a polygon self-intersects on reflex vertices and produces non-simple output exactly where the wavefront event ordering must be exact — straight-skeleton/medial-axis pipelines build directly on the predicate-exact event queue, and the offset is the skeleton's offset-curve family extracted at a propagation distance.

[HIDDEN_LINE_PROJECTION]: a predicate-exact hidden-line/silhouette projection owner in a new `Projection` robust-core sub-domain — one `ViewOp` `[Union]` (`Silhouette`/`HiddenLine`/`Section`/`Outline`) over a BSP-tree visibility kernel returning a typed `DrawingProjection` (visible/hidden 2D segment sets, the per-segment depth key, the silhouette/crease/boundary edge classification), the 2D-drawing-geometry primitive the branch `DRAFTING_DOCUMENT_PRODUCTION` rail and the AEC-domain hidden-line-removal consumers need at kernel quality. (FRONTIER RE-SEED.)
- The visibility fold composes the landed kernel floors rather than re-minting them: the silhouette edge set is the sign-change locus of the per-face view-direction dot product computed exactly through the `numerics/predicates#ROBUST_PREDICATES` `Orient3D` straddle (a silhouette edge is the boundary between a front-facing and a back-facing triangle, decided by the exact sign so a grazing-angle edge never flickers in/out of the silhouette), the depth-sorted visibility is a Newell-Sutherland BSP partition over the same `Orient3D` plane-side sign, the section cut is the `Intersection` `MeshMesh` lattice against the cutting plane (a `ViewOp.Section` is one `IntersectOp.Apply` re-projected to the view basis, never a host `Make2D` round-trip), and the crease/boundary classification reads the `topology/naming` edge-dihedral angle. The `DrawingProjection` carries the visible and hidden segment families plus the edge-kind classification as one typed receipt the drafting sheet reads.
- Unlocks the construction-drawing frontier the platform exists to produce — a 2D drawing with visible/hidden line discrimination, a section/elevation cut, a silhouette outline for the facade-panel layout — each a `ViewOp` case over the same BSP visibility kernel, so a new projection (an axonometric, an unfolded development) is one case, never a parallel `Make2D` re-implementation; the `DrawingProjection` feeds the `Rasm.Fabrication/projection/hidden-line` HLR and the AppUi drafting sheet by reference, and the AEC-domain section/elevation reads ONE projection owner instead of a host round-trip.
- The kernel owns the exact `Orient3D` straddle (`numerics/predicates`), the `MeshMesh` section lattice (the new `Intersection` owner), the BVH front-to-back traversal (`spatial/index`), and the edge-dihedral topology (`topology/naming`) but no owner assembles them into a hidden-line projection — the mature `Analysis`/`Vectors` surface delegates Make2D to the host and never owns a managed BSP visibility kernel; the Appel hidden-line/Newell-Newell-Sancha BSP-painter and the exact-sign silhouette-extraction line is the settled technique, and the branch `DRAFTING_DOCUMENT_PRODUCTION` idea names `Rasm/Geometry` arrangement as the 2D-drawing-geometry producer with no projection owner behind it, so the projection owner is the named gap the drawing rail composes.

[ROBUST_MESH_DECIMATION]: a predicate-guarded mesh simplification / LOD owner in a new `Simplification` robust-core sub-domain — one `SimplifyOp` `[Union]` (`QuadricCollapse`/`ProgressiveMesh`/`VoxelRemesh`/`FeaturePreserve`) over a quadric-error-metric edge-collapse priority queue returning a typed `DecimationReceipt` (the simplified `MeshSpace`, the per-LOD vertex/face budget, the Hausdorff error bound, the preserved feature set), the LOD/tessellation-budget primitive the branch `GEOMETRY_FLOW_SEAM` content-addressed tile pyramid, the `AEC_SIMULATION_BRIDGE` coarse-mesh solver seed, and the `WEB_GEOMETRY_RESIDENCY_WIRE` meshlet residency tier need at kernel quality. (FRONTIER RE-SEED.)
- The decimation fold is a Garland-Heckbert quadric-error-metric edge-collapse over a priority queue keyed by the collapse cost, each candidate collapse gated by the exact `numerics/predicates#ROBUST_PREDICATES` `Orient3D` sign so a collapse never flips a triangle's orientation or produces a non-manifold fold (the predicate floor decides the topology-preserving validity exactly, never an epsilon area test), the feature-preserving variant pinning the crease/boundary edges the `topology/naming` dihedral classification marks, the progressive-mesh variant recording the collapse sequence as a reversible vsplit stream for continuous LOD, and the voxel-remesh variant reading the `Vectors` mesh SDF for a topology-repairing resample. The `DecimationReceipt` carries the one-sided Hausdorff error against the source as a typed bound so a downstream LOD selection is error-budgeted, not face-count-blind.
- Unlocks the LOD/multiresolution frontier every geometry consumer needs — a 3D-tile pyramid coarsens per zoom level under a screen-space error budget, a coarse FEA mesh seeds the simulation solver at a bounded geometric error, a meshlet residency tier streams progressively — each a `SimplifyOp` case over the same QEM queue, so a new simplification method (an appearance-preserving or a curvature-adaptive decimation) is one case, never a parallel mesh-decimation library; the `DecimationReceipt` feeds the `Rasm.Compute` tile-partition and the `Rasm.Bim` tessellation-bridge by reference.
- The kernel composes the richest mesh operator surface in the monorepo (`Vectors/Mesh.cs` owns the cotangent Laplacian, the IDT flip, the mesh SDF, the remesh kernels) and the exact `Orient3D` predicate, yet the mature `Vectors/Mesh.cs` remesh/reduce rail delegates decimation to the host `Mesh.Reduce` and never owns a predicate-guarded QEM collapse with an error receipt — a host reduce produces no Hausdorff bound and no reversible vsplit stream; the Garland-Heckbert QEM and the Hoppe progressive-mesh line is the settled technique over the operators the kernel already assembles, and the branch tile/residency/solver-seed consumers name a budgeted coarsen with no decimation owner behind them, so the simplification owner is the named gap the multiresolution pipeline composes.

[GEOMETRY_ENCODING_PACK]: a canonical geometry-encoding / serialization-packing owner in a new `Encoding` robust-core sub-domain — one `PackOp` `[Union]` (`PointCloud`/`MeshPatch`/`VoxelGrid`/`BrepPatch`) over a per-feature channel lattice returning a typed `EncodedGeometry` (the per-channel `ReadOnlyMemory<float>` payload, the channel descriptor set, the lossless-round-trip witness), the kernel-side producer of the canonical representation vocabulary the branch `ENCODED_GEOMETRY_PLUGIN_FRONTIER` `GeometryPacking` boundary capsule and the `Compute/tensors/residency#GEOMETRY_ENCODING` tensor lane read. (FRONTIER RE-SEED, branch trickle-down.)
- The packing fold materializes each representation case's per-feature `EncodingChannel` rows (position/normal/color-rgba/curvature/geodesic/intensity/occupancy/weight) by composing the live kernel readers rather than a plugin-private representation: the `MeshPatch` position/normal channels read the `Vectors` `MeshSpace`, the curvature channel the `Vectors` principal-curvature metric, the geodesic channel the `Vectors` heat-geodesic, the `PointCloud` intensity/normal channels the `Vectors/Cloud` PCA-oriented-normal metric, the `VoxelGrid` occupancy channel the mesh SDF sign, and the round-trip witness is the canonical-adjacency byte identity the `topology/reconciliation#CANONICAL_BYTE_IDENTITY` encoder already mints — so the encoded geometry proves lossless decode against the one content hash, never a silent quantization. The `EncodedGeometry` is the kernel value the Compute tensor lane wraps in `EncodedTensor`, the kernel owning the channel materialization and the Compute lane owning the tensor residency.
- Unlocks the geometry-encoding frontier the plugin/tensor/proto-wire consumers share — a sandboxed mesher declares a brep-in mesh-out contract in the SAME channel vocabulary the in-process tensor lane reads, a remote proto geometry family carries the SAME channels, a model-zoo conformance fixture reads the SAME representation — each a `PackOp` case over the same channel lattice, so a new representation (a field grid, a toolpath stream) is one case the Compute encoding table extends, never a parallel representation surface per consumer.
- The kernel owns every per-channel reader (`Vectors` mesh/cloud/SDF/curvature/geodesic) and the canonical-byte-identity round-trip witness (`topology/reconciliation`) but no owner assembles them into a canonical geometry encoding — the branch `ENCODED_GEOMETRY_PLUGIN_FRONTIER` idea names the `GeometryPacking` capsule materializing the `PointCloud`/`MeshPatch`/`VoxelGrid`/`BrepPatch` cases "through `Rasm`" with the kernel reader as the unstated owner, so the encoding owner is the named kernel-side gap the plugin and tensor lanes compose, and the lossless-round-trip witness is precisely the content-identity invariant the kernel already mints.

## [2]-[CLOSED]

[CONSTRAINED_DELAUNAY]: REALIZED — `tessellation/delaunay#BOWYER_WATSON_DELAUNAY`/`#CONSTRAINT_RECOVERY` the author-kernel constrained Delaunay triangulation/tetrahedralization `[Union]` over a flat `SimplexStore`, the exact `InCircle`/`InSphere`-driven Bowyer-Watson cavity insertion, and the predicate-guarded segment/facet recovery now composing the landed `OrientLPI`/`OrientTPI` Steiner-point family. The `[STORE_OPS]` cluster bodies and the `DelaunayLaws` law-matrix are the remaining transcription/test obligations, not open design fences; the constrained-Delaunay arrangement substrate the offsetting/arrangement consumers compose is authored.
[INDIRECT_PREDICATES]: REALIZED — `numerics/predicates#INDIRECT_PREDICATES` `OrientLPI`/`OrientTPI` over `Lpi`/`Tpi` exact homogeneous constructions; the constructed line-line / three-plane intersection point's orientation sign is exact without rounding. Unblocks the healing self-intersect split, the Delaunay Steiner recovery, the managed boolean companion path, and the new exact-intersection lattice.
[GENERALIZED_WINDING]: REALIZED — `spatial/index#GENERALIZED_WINDING` `SpatialQuery.Winding` + `QueryResult.Scalar` Barill/Jacobson tree-based GWN best-first descent over the existing BVH (per-node dipole/centroid moments, far-field β approximation, exact per-triangle solid angle at leaves). A query case, not a new structure; the inside/outside classifier the exact-arrangement boolean cell-keep predicate composes.
[WITNESS_DOF]: REALIZED — `constraints/solver#WITNESS_DOF` `DofAnalysis.RedundantConsistent` + `ConstraintSolver.WitnessAnalyze` (numeric Jacobian rank + `Svd`-tail residual-consistency at the seed witness) wired into `Solve` so a redundant-but-consistent system solves rather than faulting.
[DEGRADATION_REFIT]: REALIZED — `spatial/index#DEGRADATION_REFIT`/`#AGGLOMERATIVE_BUILD` `BuildPolicy.RefitDegradationLimit`, the `SpatialKind.Agglomerative` `Builders` row over the shared `NodeStore`, `Bvh.BuildCost`/`Builder` columns, and the `SahCost`-keyed `Refit` rebuild trigger — the Compute clash seam unchanged.
