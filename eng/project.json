{
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "name": "eng",
    "targets": {
        "provision": {
            "command": "uv run python -m eng.scripts.provision",
            "cache": false,
            "parallelism": false
        }
    }
}
