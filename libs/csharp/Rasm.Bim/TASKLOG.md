# [RASM_BIM_TASKLOG]

Open and closed work distilled from `IDEAS.md`. `[1]-[OPEN]` carries task cards with a `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]`/`[UPSTREAM-BLOCKED]`/`[HOST-PROBE-DEFERRED]` leader; `[2]-[CLOSED]` carries `[COMPLETE]`/`[DROPPED]` cards. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[UPSTREAM-BLOCKED] Ground the `BsddResolution` live wire shape against the bSDD service contract — `classification/systems.md#BSDD_RESOLUTION` `[4]-[RESEARCH]` `[BSDD_SERVICE_CONTRACT]`.
- The `BsddResolution` resolution shape, the `BsddPort` transport seam, the degradation to the row's local code-shape policy, and the `BsddClass`/`BsddProperty` evidence are authored and settled; the live bSDD RESTful/GraphQL response projection into `BsddClass` is the only open leg.
- EXTERNAL dependency: the bSDD is a live HTTP/GraphQL service with no decompiled assembly to catalogue. Companion path: the `BsddPort.Fetch` response member spellings ground against the published bSDD API specification at the cross-folder `csharp:Compute/remote/channels#TRANSPORT_AXIS` transport alignment; the in-process degradation to the local code-shape regex is the present-tense fallback so ingest never blocks on the unreachable service.

[UPSTREAM-BLOCKED] Ground the IDS cross-tool audit against the ifctester companion — `validation/ids.md#IDS_FACETS` `[3]-[RESEARCH]` `[IFCTESTER_COMPANION]`.
- The `IdsSpecification`/`IdsFacet`/`IdsAudit` in-process fold over `BimModel`, the IDS v1.0 XSD parse over `System.Xml.Schema`, and the spec authoring are authored and settled; the in-process fold is the immediate self-audit.
- EXTERNAL dependency: the IfcOpenShell ifctester deterministic cross-tool audit is a `python:geometry/ifc-companion` PyPI surface reached only through Compute's companion rpc. Companion path: the companion request/response shape grounds at the cross-folder companion alignment identically to the `exchange/tessellation-bridge#TESSELLATION_BRIDGE` two-hop pattern; the companion is the external-conformance oracle, never a transport minted here.

[QUEUED] Ground the GeometryGym georeferencing projection — `georeferencing/coordinate-reference.md#GEO_REFERENCE` `[3]-[RESEARCH]` `[MAP_CONVERSION_PROJECTION]` and the import-rail `MapConversionRow` widening.
- The host-neutral `GeoReference` record, the `ProjectedCrs` value-object, the `ToTransform` kernel-transform composition, and the `FrameNormalization.Georeference` overload are authored and settled.
- INTERNAL decompile work (NOT an external block): `GeometryGymIFC_Core` is an admitted, decompilable managed assembly carrying the full IFC4.3 entity vocabulary; the `IfcMapConversion`/`IfcProjectedCRS`/`IfcCoordinateOperation` entities ship in the assembly but are simply absent from the present `.api/api-geometrygym-ifc` roster, which enumerates spatial/relationship/representation families. Closure path: extend the folder-local `assay api` catalogue with the georeferencing-entity scope (the `Eastings`/`Northings`/`OrthogonalHeight`/`XAxisAbscissa`/`XAxisOrdinate`/`Scale`/`SourceCRS`/`TargetCRS` and `Name`/`GeodeticDatum`/`MapProjection`/`MapZone` member spellings and the `IfcGeometricRepresentationContext.HasCoordinateOperation` traversal), then widen the `exchange/import-rail#IMPORT_RAIL` `Semantic` extract with the `MapConversionRow` projection; the kernel `Rasm` transform composition is settled.

[UPSTREAM-BLOCKED] Promote the candidate `step-iso10303`/`mesh-text`/`iges-ansi`/native-companion codec bodies — `exchange/import-rail.md#IMPORT_RAIL`·`exchange/tessellation-bridge.md#TESSELLATION_BRIDGE` `[AP242_CODEC]`/`[NATIVE_FORMAT_BRIDGES]`.
- The `InterchangeFormat` rows, the `InterchangeCodec` discriminants, the `StepProtocol` column, the frame columns, and the `CataloguePending` markers are settled; the import fold faults `import-catalogue-pending` on a candidate row and `import-needs-companion` on a companion-gated row (IFC/STEP/IGES/native), which is the correct present-tense behavior. IGES carries the distinct `iges-ansi` companion codec, not `step-iso10303`, because the ANSI IGES section-based grammar shares neither the STEP physical-file token grammar nor the GeometryGym entity surface.
- EXTERNAL dependency: the STL/3MF/OBJ/PLY mesh-text reader packages and the managed AP242 STEP reader are unadmitted (`<stl-3mf-obj-ply-reader-pending>`, `<step-ap242-reader-pending>`), and the IGES, Revit/Navisworks/DWG native readers have no managed loader. Companion path: each candidate codec promotes in place — capability columns flip and the codec body grounds — when the named package catalogue lands; IGES, native-format, and STEP-solid geometry evaluation route through the Compute companion rail (OpenCascade serves the IGES/STEP solid read in the same two-hop the IFC tessellation uses) because the managed branch carries no kernel.

[QUEUED] Widen the `IfcClass` entity vocabulary to the IFC4.3 schema breadth — `model/elements.md#ELEMENT_MODEL` and `query/element-set.md#ELEMENT_SET` — from `IFC_VOCABULARY_BREADTH` axis (a) ENTITY-CLASS.
- Extend the `IfcClass` `[SmartEnum<string>]` from 12 rows to the IFC4.3 domain breadth as one `IfcDomain`-discriminated row table: the architectural built-element family (`IfcCovering`/`IfcCurtainWall`/`IfcRailing`/`IfcRamp`/`IfcRoof`/`IfcStair`/`IfcPlate`/`IfcMember`/`IfcFooting`/`IfcPile`), the MEP distribution domain (`IfcFlowSegment`/`IfcFlowFitting`/`IfcFlowTerminal`/`IfcFlowController`/`IfcDistributionPort`), the structural-analysis domain (`IfcStructuralCurveMember`/`IfcStructuralSurfaceMember`/`IfcStructuralPointConnection`), and the infrastructure breadth the `.api/api-geometrygym-ifc` catalogue enumerates (`IfcCourse`/`IfcEarthworksFill`/`IfcEarthworksCut`/`IfcGeotechnicalStratum`/`IfcBorehole`/`IfcRail`/`IfcAlignment`); widen `IfcDomain` from three cases to the buildingSMART partition (Architecture/Structural/HvacFire/Electrical/Plumbing/Infrastructure/Geotechnical).
- No new package — every entity row is a `GeometryGymIFC_Core` class; the breadth is row data on the existing SmartEnum, not a new owner. The infrastructure/earthworks/geotechnics family (`IfcCourse`/`IfcEarthworksFill`/`IfcEarthworksCut`/`IfcGeotechnicalStratum`/`IfcBorehole`/`IfcRail`/`IfcAlignment`) is on the present `.api/api-geometrygym-ifc` roster; the architectural built-element family (`IfcCovering`/`IfcCurtainWall`/`IfcRailing`/`IfcRamp`/`IfcRoof`/`IfcStair`/`IfcPlate`/`IfcMember`/`IfcFooting`/`IfcPile`), the MEP distribution family (`IfcFlowSegment`/`IfcFlowFitting`/`IfcFlowTerminal`/`IfcFlowController`/`IfcDistributionPort`), and the structural-analysis family ship in `GeometryGymIFC_Core` but are absent from the roster — INTERNAL decompile work extends the catalogue with the architectural/MEP/structural entity-class scope before the SmartEnum row table widens.
- The `model/elements#ELEMENT_MODEL` `BimModel.Project` fold and the `assembly/spatial-structure#ASSEMBLY_TREE` `IfcClass.TryGet` projection read the widened vocabulary unchanged; the `query/element-set#ELEMENT_SET` `ByDomain` predicate discriminates on the widened `IfcDomain` so a domain query needs no per-element type, and every real building/MEP/structural/infrastructure element stops collapsing onto `IfcBuildingElementProxy`.
- The breadth is the single largest recovery of the folder-intent never-lose; a new entity class stays one `IfcClass` row, and the predefined-type discriminant rides the sibling `IFC_VOCABULARY_BREADTH` axis (b) PREDEFINED-TYPE task, not this entity-class row set.

[QUEUED] Open the `material/` sub-domain for real construction-material composition — `material/composition.md` — from `BIM_MATERIAL_OWNER`.
- Author the new `material/composition` design page: a `BimMaterial` owning the named material plus its `IfcMaterialLayerSet` (ordered thickness-keyed layer assembly for walls/slabs/roofs), `IfcMaterialProfileSet` (cross-section profile material for linear members), and `IfcMaterialConstituentSet` (named constituent fractions for composites), each projected from the GeometryGym `IfcMaterialLayerSetUsage`/`IfcMaterialProfileSet`/`IfcMaterialConstituentSet` surface the `.api/api-geometrygym-ifc` catalogue lists, plus the `IfcSurfaceStyleRendering`/`IfcSurfaceStyleShading` appearance projected onto a host-neutral appearance record; the `exchange/import-rail#IMPORT_RAIL` `IfcSemanticModel.MaterialRow` widens from a name string to the layered/profiled/constituent composition family.
- No new branch package — `GeometryGymIFC_Core` owns the material composition and surface-style entities (catalogued); the appearance record reconciles with the `Rasm.Materials` engineering-property and OpenPBR owner at the cross-folder seam (`csharp:OPEN_MATERIAL_STANDARD_WIRE` `Rasm.Materials/appearance/interchange#WIRE_PROJECTION`), never a second appearance mint.
- The `model/elements#ELEMENT_MODEL` `BimElement.Materials` column promotes from `Seq<string>` to the typed `BimMaterial` binding; the `properties/property-sets#PROPERTY_SETS` base-quantity derivation reads the layer thicknesses for a layered-wall volume takeoff; the appearance seam meets `Rasm.Materials` at the content key, never re-implementing the OpenPBR algebra.
- Trickle-down realization of `csharp:OPEN_MATERIAL_STANDARD_WIRE` — the BIM material composition is the IFC source the branch material-standard wire carries; the layer build-up, the section material, and the constituent mix each carry their real composition.

[QUEUED] Add the `PredefinedType` discriminant axis — `model/elements.md#ELEMENT_MODEL` and `query/element-set.md#ELEMENT_SET` — from `IFC_VOCABULARY_BREADTH` axis (b) PREDEFINED-TYPE.
- Add a `PredefinedType` `[ValueObject<string>]` column on `BimElement` projected from the GeometryGym `IfcWall.PredefinedType`/`IfcSlab.PredefinedType` member through the `ParserIfc.IdentifyIfcClass(name, out predefinedConstant)` split the `.api/api-geometrygym-ifc` catalogue lists, validated against the `IfcClass` row's valid-predefined set, with the `ELEMENTUSERDEFINED`/`OBJECTTYPE` user-defined fallback resolving the `ObjectType` string; the `query/element-set#ELEMENT_SET` gains a `ByPredefinedType` predicate arm.
- No new package — `ParserIfc.IdentifyIfcClass(string, out string predefinedConstant)` is on the present `.api/api-geometrygym-ifc` roster and splits the predefined token at parse; the per-class `IfcWall.PredefinedType`/`IfcSlab.PredefinedType` member enums ship in `GeometryGymIFC_Core` but are absent from the roster, so INTERNAL decompile work extends the catalogue with the per-class `PredefinedType` member scope before the fence reads the member directly; the `exchange/import-rail#IMPORT_RAIL` `ProductRow` projection widens to carry the predefined-type string.
- The `IfcClass` row carries its valid-predefined-set so the predefined axis discriminates within a class without a per-class type; the `ByPredefinedType` arm composes the existing `ElementPredicate` algebra, never a second selection surface.
- The predefined-type axis is the second-level breadth the entity-class `IFC_VOCABULARY_BREADTH` axis (a) ENTITY-CLASS task does not carry; a "load-bearing wall" / "exterior door" query becomes filterable.

[QUEUED] Promote the thin `TypeRow` to a first-class type-object owner — `model/elements.md#ELEMENT_MODEL` (a `BimType` cluster) — from `IFC_VOCABULARY_BREADTH` axis (c) TYPE-OCCURRENCE.
- Promote the `exchange/import-rail#IMPORT_RAIL` `IfcSemanticModel.TypeRow(GlobalId, EntityType, Name)` into a `BimType` record carrying the `IfcElementType`/`IfcTypeProduct` discriminant, the type-bound `PropertySet`/`material`/`PredefinedType`, and the `IfcRepresentationMap` instanced-geometry library a `IfcMappedItem` occurrence references; the `BimElement.TypeGlobalId` resolves the `BimType` so a 4000-window model carries one type plus 4000 occurrence references.
- No new package — `IfcTypeProduct`/`IfcElementType`/`IfcRepresentationMap`/`IfcMappedItem` are `GeometryGymIFC_Core` surface (catalogued); the import-rail `Extract<IfcTypeObject>` projection widens to carry the type's property/material/representation-map bindings.
- The `properties/property-sets#PROPERTY_SETS` `QTO_TYPEDRIVENOVERRIDE` inheritance fold already reads the type-row property family by reference; this task gives that fold a typed `BimType` source rather than a re-filtered flat row set, and the `IfcMappedItem` instancing reads the `BimType` representation map.
- Deepens the thin `TypeRow` into the type-occurrence factoring the property-inheritance fold already depends on; the occurrence reads the type by reference, never inlining the type geometry.

[QUEUED] Open the `zoning/` sub-domain for the spatial-zone/program grouping overlay — `zoning/grouping.md` — from `SPATIAL_ZONE_PROGRAM`.
- Author the new `zoning/grouping` design page: a `BimZone` carrying its `IfcZoneTypeEnum`/grouping kind, its assigned element/space set (a many-to-many overlay distinct from the single-parent `assembly/spatial-structure#ASSEMBLY_TREE` containment tree), and the `IfcRelReferencedInSpatialStructure` cross-reference, projecting `IfcZone`/`IfcSpatialZone`/`IfcGroup`/`IfcSystem`/`IfcBuildingSystem` and `IfcRelAssignsToGroup` from the `IfcSemanticModel`; the `query/element-set#ELEMENT_SET` gains a `ByZone` predicate traversing the grouping overlay.
- INTERNAL decompile work — `IfcZone`/`IfcSpatialZone`/`IfcGroup`/`IfcSystem`/`IfcRelAssignsToGroup`/`IfcRelReferencedInSpatialStructure` ship in `GeometryGymIFC_Core` but are absent from the present `.api/api-geometrygym-ifc` roster; extend the catalogue with the grouping/zone entity scope before the projection fence transcribes.
- The grouping overlay is orthogonal to the containment tree (an element belongs to one spatial container but many zones); the `analysis/` thermal-zone selection and the `systems/` MEP-system grouping both read this overlay, never a per-discipline grouping mint.
- The cross-cutting grouping layer the single-parent `BimAssembly` tree cannot express; a fire-compartment query, a thermal-zone energy selection, and a functional-program area takeoff each fold one overlay.

[QUEUED] Open the `reconstruction/` sub-domain for scan-to-BIM primitive fitting — `reconstruction/primitives.md` — from `SCAN_TO_BIM_RECONSTRUCTION` (and `csharp:REALITY_CAPTURE_TO_BIM`).
- Author the new `reconstruction/primitives` design page: a `ReconstructionPrimitive` closed `[Union]` (plane/cylinder/torus/freeform) folding the kernel-registered segmented cloud into `BimElement` rows with `ElementPredicate`-classified `IfcClass`, a per-element confidence band on the `properties/property-sets#PROPERTY_SETS` Pset, and a source-cloud lineage key joining the reconstructed element to its capture through the Compute content-key.
- No new branch package — the registration is the kernel's (`Rasm/Vectors` `Align`/cloud-ICP, `Rasm/Geometry/spatial` segmentation), the splat/point payload is Compute's (`Rasm.Compute/interchange/codecs`), and the offline RANSAC/learned-segmentation fit crosses as the `libs/.planning` graduation `reconstructed-mesh`/`topology-graph` `geometry` axis the C# side re-imports.
- The reconstruction arm composes the one `BimModel` and the `query/element-set#ELEMENT_SET` algebra, never a parallel model surface; `Rasm.Persistence/federation` joins the reconstructed element to its source-cloud lineage by `(GeometryHash, content-key)` and `Rasm.AppUi/realitycapture` plays the capture back over the reconstructed model.
- Trickle-down realization of `csharp:REALITY_CAPTURE_TO_BIM` — the single new sub-domain that closes the cloud-to-semantics gap; the primitive fit is the only new owner, the registration and tessellation reused. (Mirrors the branch `REALITY_CAPTURE_TO_BIM` reconstruction-arm task, landed folder-side.)

[QUEUED] Open the `analysis/` sub-domain for the structural-analysis-domain projection — `analysis/structural-model.md` — from `ANALYSIS_MODEL_BRIDGE` (and `csharp:AEC_SIMULATION_BRIDGE`).
- Author the new `analysis/structural-model` design page: a host-neutral analysis-element/load/support graph projecting `IfcStructuralAnalysisModel`/`IfcStructuralCurveMember`/`IfcStructuralSurfaceMember`/`IfcStructuralPointConnection`/`IfcStructuralLoadGroup`/`IfcRelConnectsStructuralMember` the Compute solver reads by `(GeometryHash, PropertyHash)`, the idealized analysis member binding the physical `BimElement` by GlobalId so the analysis model is a view of the federated model, never a re-modeled mesh.
- INTERNAL decompile work — the `IfcStructural*` entity family ships in `GeometryGymIFC_Core` but is absent from the present `.api/api-geometrygym-ifc` roster; extend the catalogue with the structural-analysis-domain scope before the projection fence transcribes.
- The boundary conditions ride the `properties/property-sets#PROPERTY_SETS` Pset load/support sets; the `query/element-set#ELEMENT_SET` `ElementPredicate` selects the analysis domain; the Compute solver consumes the analysis graph by reference at the `csharp:AEC_SIMULATION_BRIDGE` seam, never a parallel FEA model.
- Trickle-down realization of `csharp:AEC_SIMULATION_BRIDGE` — the folder owns no structural analysis-element vocabulary to project the analysis domain the branch simulation bridge reads; this owner anchors the boundary conditions the AISC section the profiles table carries solves against.

[QUEUED] Open the `sequencing/` sub-domain for the 4D construction-scheduling network — `sequencing/schedule.md` — from `FOUR_DIMENSIONAL_SEQUENCING`.
- Author the new `sequencing/schedule` design page: a `ConstructionTask` carrying its `IfcTaskTime` schedule as a NodaTime `Interval` over `ZonedDateTime` start/finish, its `IfcRelSequence` finish-to-start/start-to-start/finish-to-finish dependency lag as a `Period`, and its `IfcRelAssignsToProcess` element assignment joining the task to the `query/element-set#ELEMENT_SET`-selected `BimElement` set, projecting `IfcTask`/`IfcWorkSchedule`/`IfcWorkPlan`/`IfcWorkCalendar`/`IfcRelSequence`/`IfcRelAssignsToProcess` from the `IfcSemanticModel`; a construction-state snapshot at any `Instant` reads the element set whose task interval contains it.
- Exploits the admitted-but-unexploited `NodaTime` calendar/interval surface (`.api/api-nodatime` — `Interval`/`Period`/`ZonedDateTime`/`DateInterval`/`PeriodBuilder`/`Interval.Contains`), used today only for `Instant` receipt stamps; INTERNAL decompile work extends the `.api/api-geometrygym-ifc` roster with the `IfcTask`/`IfcWorkSchedule`/`IfcRelSequence`/`IfcTaskTime` scheduling-domain scope before the projection fence transcribes.
- The 4D phasing the `Rasm.AppUi` schedule report and the `Rasm.Persistence` activity network consume; the element-set selection is the `query/element-set#ELEMENT_SET` predicate, never a second selection surface, and the cost/resource pairing rides the sibling `COST_RESOURCE_5D` task.
- FRONTIER re-seed exploiting the NodaTime interval surface — the 4D BIM time-phasing every professional coordination platform carries, with no owner today.

[QUEUED] Open the `cost/` sub-domain for the 5D cost-and-resource network — `cost/estimate.md` — from `COST_RESOURCE_5D`.
- Author the new `cost/estimate` design page: a `CostItem` carrying its `IfcCostValue` rate joined to the `properties/property-sets#PROPERTY_SETS` `QuantitySet` quantity by element GlobalId, the `IfcConstructionResource` (labor/material/equipment) assignment joined to the `sequencing#ConstructionTask` it resources, and the `IfcCostSchedule` rollup folding the cost items into a schedule total, projecting `IfcCostSchedule`/`IfcCostItem`/`IfcCostValue`/`IfcConstructionResource`/`IfcRelAssignsToControl` from the `IfcSemanticModel`; a 5D estimate is one fold over the (quantity × rate × resource) join.
- INTERNAL decompile work — the `IfcCost*`/`IfcConstructionResource`/`IfcRelAssignsToControl` family ships in `GeometryGymIFC_Core` but is absent from the present `.api/api-geometrygym-ifc` roster; extend the catalogue with the cost/resource-domain scope before the projection fence transcribes.
- The cost source the cross-libs `csharp:TELEMETRY_LAKE_ANALYTICS` 5D cost catalog and the `Rasm.AppUi` estimate report consume; the quantity is the `QuantitySet.Derive` takeoff, the schedule the `sequencing#ConstructionTask` network, joined by element GlobalId — never a second quantity or schedule source.
- FRONTIER re-seed — the 5D cost domain pairing the 4D sequencing idea, the cost source the persistence cost catalog names but the BIM model produces nothing for today.

[QUEUED] Land the geodetic datum-to-datum reprojection leg over `ProjNET` — `georeferencing/coordinate-reference.md#GEO_REFERENCE` (a `GEODETIC_TRANSFORM` cluster) — from `GEODETIC_CRS_TRANSFORM`.
- Add the datum-bridging leg to the `GeoReference` owner: the EPSG-keyed `ProjectedCrs` resolves a `CoordinateSystemServices.GetCoordinateSystem(srid)`, `CoordinateSystemServices.CreateTransformation(sourceSrid, targetSrid)` builds the `ICoordinateTransformation`, and `MathTransform.Transform` applies the Bursa-Wolf datum shift plus projection so a multi-CRS federation reconciles every model onto the shared projected frame before the rigid map-conversion offset the kernel `Rasm` transform applies; the `ProjectedCrs.Epsg` Option drives the SRID lookup.
- ADMIT `ProjNET` (2.1.0, MIT, pure-managed, NetTopologySuite-ecosystem peer of the admitted `Npgsql.NetTopologySuite`) — catalogued at `.api/api-projnet` (`CoordinateSystemServices`/`CoordinateTransformationFactory`/`ICoordinateTransformation`/`MathTransform.Transform`/`ProjectedCoordinateSystem.WGS84_UTM`); the manifest-row delta is the orchestrator's, the catalogue and README registry land folder-side.
- The map-conversion stays the local-engineering-frame placement the kernel `Rasm` transform owns; the CRS reprojection is the new leg distinct from `ToTransform`, composed once on the `GeoReference` so the datum shift precedes the rigid offset; the `FrameNormalization.Georeference` overload applies the composed transform at the import `Framed` site, never a hand-rolled datum shift.
- The `GeoReference.Project` map-conversion projection stays `[UPSTREAM-BLOCKED]` on the GeometryGym georeferencing-entity catalogue widening (the existing georeferencing task); this leg is additive — the reprojection runs once the EPSG identity resolves, the non-georeferenced model returning `GeoReference.Identity` unchanged.

[QUEUED] Land the LOD/meshlet residency pyramid leg over `Alimer.Bindings.MeshOptimizer` — `exchange/export-rail.md` (a `BIM_LOD` cluster) — from `BIM_LOD_RESIDENCY_PYRAMID`.
- Add the `BimLod` leg to the export rail: derive the per-element LOD chain through `Meshopt.Simplify`/`SimplifySloppy` keyed by target triangle ratio, the meshlet residency band through `Meshopt.BuildMeshlets` for the WebGPU raster path, and the per-LOD content key so the `csharp:Rasm.Compute#TILE_PARTITION` tile pyramid content-addresses each detail level; the export emits a progressive-detail pyramid, never a per-element full-resolution mesh.
- No new package — `Alimer.Bindings.MeshOptimizer` is admitted and catalogued at `.api/api-alimer-meshoptimizer`; this leg exploits the `Meshopt.Simplify`/`BuildMeshlets`/spatial-clustering surface the encode-only `exchange/export-rail#TILE_METADATA` leg leaves unexploited.
- The per-LOD content key meets `Rasm.Compute#TILE_PARTITION` at the seam; the meshlet residency band feeds the cross-libs `WEB_GEOMETRY_RESIDENCY_WIRE` splat/meshlet manifest the AppUi projection mints, the web peer streaming each LOD by view distance — never a second LOD or residency owner.
- Closes the `Alimer.Bindings.MeshOptimizer` LOD/meshlet under-exploitation; a new detail level is one content-keyed row on the pyramid, the `TileMetadata` per-tile semantic riding each LOD unchanged.

## [2]-[CLOSED]

[COMPLETE] BIM fault owner (ALIGNMENT-DEFECT A) — authored `faults/faults.md#FAULT_BAND` as the `BimFault` closed `[Union]` band-2600 (`ModelRejected`/`UnmappedClass`/`DanglingReference`/`CodecReject`/`CapabilityMiss`) with `Code`/`ToError`/`Message`, composing the kernel `GeometryFault`; rewrote the ~8 scattered stringly-typed `new BimFault.ModelRejected($"<token:detail>")` sites across `model/elements`, `classification/systems`, `assembly/spatial-structure`, `exchange/format-axis`, `exchange/import-rail`, `exchange/export-rail`, and `exchange/tessellation-bridge` to lift the typed case and lower it through `.ToError()`, the band recoverable by `HasCode`/`Is`.

[COMPLETE] IDS validation owner — authored `validation/ids.md#IDS_FACETS`: the `IdsFacet` `[Union]` (Entity/Attribute/Property/Classification/Material/PartOf) `ToPredicate` lowering onto the `query/element-set#ELEMENT_SET` algebra, the `IdsSpecification.Audit`/`Parse`/`Author` over `System.Xml.Schema`/`System.Xml.Linq`, and the `IdsAudit` receipt; the validation predicate IS the query predicate.

[COMPLETE] BCF coordination owner — authored `coordination/issue-exchange.md#BCF_ARCHIVE` (the `BcfTopic`/`BcfComment`/`BcfViewpoint` family, the `.bcfzip` codec over `ZipArchive`/`XDocument`, the `BcfApi` REST projection, anchored on IFC GlobalIds, NOT a geometry-format-axis row) and `coordination/model-diff.md#MODEL_DIFF` (the `ModelDiff.Between` fold joining two `BimModel` snapshots by GlobalId plus the Compute content-key into the `ElementChange` added/modified/removed/moved arms).

[COMPLETE] bSDD dictionary binding — authored `classification/systems.md#BSDD_RESOLUTION`: the `BsddResolution.Resolve` over the injected `BsddPort` transport seam degrading to the row's local code-shape policy, the `BsddClass`/`BsddProperty` evidence, and the `Classification.CodeShape` local-policy column; the live wire shape stays `[UPSTREAM-BLOCKED]`.

[COMPLETE] Property and quantity owner — authored `properties/property-sets.md#PROPERTY_SETS`: the `PropertyKey` `[SmartEnum<string>]` standard-Pset vocabulary, the `PropertyValue` `[Union]` typed value, the `PropertySet.Resolve` type-vs-occurrence inheritance fold, the `QuantitySet.Derive` base-quantity derivation, and the `IfcRelDefinesByProperties` round-trip; promotes the flat `PropertyRow`/`QuantityRow` projections into the one typed owner.

[COMPLETE] Georeferencing owner — authored `georeferencing/coordinate-reference.md#GEO_REFERENCE`: the host-neutral `GeoReference` record, the `ProjectedCrs` value-object, the `ToTransform` kernel-transform composition, and the `FrameNormalization.Georeference` CRS overload distinct from `Canonicalize`; the GeometryGym projection and import-rail widening stay `[UPSTREAM-BLOCKED]`.

[COMPLETE] Universal wire projection — authored `exchange/wire.md#WIRE_PROJECTION`: the `BimWire` content-keyed snapshot, the source-generated `BimWireContext` `JsonSerializerContext`, the `BimWireOptions` `ThinktectureJsonConverterFactory` registration, and the `Encode`/`Decode` gating through each owner's `Validate`; the closed families serialize by key/case discriminant, no hand-authored DTO mirror.

[COMPLETE] 3DTiles per-tile metadata (under-utilization closure) — authored `exchange/export-rail.md#TILE_METADATA`: the `TileMetadata.Attach` `EXT_structural_metadata` schema/class/property-table over the `BimElement` semantic bound through `EXT_mesh_features` `FeatureIDBuilder`, exploiting the admitted-but-unexploited `SharpGLTF.Ext.3DTiles` surface catalogued at `.api/api-sharpgltf-3dtiles`.

[COMPLETE] Speckle `Base` object-graph import seam — extended `exchange/import-rail.md#SPECKLE_SEAM` (anchor `[3]-[SPECKLE_SEAM]`) with the `BimIo.ImportSpeckle`/`ImportSpeckleSemantic` entrypoints folding a deserialized `Speckle.Sdk.Models.Base` tree onto the canonical `ImportedGeometry`/`IfcSemanticModel` carriers; a third arm on the existing `BimIo` capsule symmetric to `ImportGeometry`/`ImportIfc`, composing `Speckle.Sdk`/`Speckle.Objects` only in the host-neutral exchange assembly.

[COMPLETE] Doc-model migration — re-homed `Model/.planning/object-model.md` into the source-mirroring `model/elements.md`, `query/element-set.md`, `classification/systems.md`, and `assembly/spatial-structure.md`, and `Exchange/.planning/interchange.md` into the split `exchange/*` pages under the single `.planning/`; rebuilt `ARCHITECTURE.md` as a codemap, `README.md` as router-plus-package-registry, and authored `IDEAS.md`/`TASKLOG.md`.

[COMPLETE] glTF extension-axis drift — closed the Drako/meshopt manifest-vs-page mismatch (the `KhrExtension` compression rows compose `Openize.Drako` and `Alimer.Bindings.MeshOptimizer`, `GlbBytes` routes the encode through them), fixed the `KHR_meshopt_compression` spelling, and clarified the USD rows as scene-graph coexistence.

[COMPLETE] Exchange god-page split — split the one `exchange/interchange.md` into the source-mirroring `exchange/format-axis.md`, `exchange/import-rail.md`, `exchange/export-rail.md`, and `exchange/tessellation-bridge.md`, distributed the seven RESEARCH items to their owning pages, and rewrote every intra-folder reference to the split-page form.

[COMPLETE] Exchange fence verification — corrected the `IfcBuildingElementProxy` ctor catalogue row to the real GeometryGym `(IfcObjectDefinition host, IfcObjectPlacement, IfcProductDefinitionShape)` and catalogued the `IfcBuildingStorey`/`IfcMaterial` ctors; replaced the import-rail `new ReadContext()` with the catalogued `ReadContext.CreateFromDictionary` text-context path carrying `Validation = ValidationMode.Strict`; collapsed the duplicated canonical-frame guard into one row-owned `InterchangeFormat.IsCanonicalFrame` predicate.
