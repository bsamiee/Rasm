# [FABRICATION_TASKLOG]

Open work owned by this folder; closed items do not appear. `[STATUS]` is one of `QUEUED`, `ACTIVE`, `BLOCKED`, `SPIKE`; owner state is read at `ARCHITECTURE.md` `[OWNER_REGISTRY]`. Every `SPIKE` row names the probe that flips its owner registry cell to `FINALIZED`.

## [1]-[PROBES]

The residual probes the fabrication pages carry — verification or native-asset gates.

| [INDEX] | [ITEM] | [PAGE#CLUSTER] | [STATUS] |
| :-----: | ------ | -------------- | :------: |
| [1] | CAM/motion validation: hidden-line clip, IK convergence through kinematic singularities, and NFP placement assert against the law-matrix; the deferred CSG-boolean watertight-silhouette tier-3 asset stays gated | toolpath#CAM_MOTION · hidden-line#PROJECTION_HIDDEN_LINE | SPIKE |
| [2] | CSG boolean watertight-silhouette: the tier-3 native deploy-asset-gate for a boolean-combined solid outline; no admissible managed CSG library exists, the per-RID native asset is an [ADMISSIONS_RECORD] amendment outside this package's write-scope | hidden-line#PROJECTION_HIDDEN_LINE | SPIKE |
| [3] | Fault-band reconciliation: the moved kernels route a band-2400 fault union — reconcile the package-owned `FabricationFault` band against the kernel `Rasm/Geometry` `GeometryFault` band so the `OpenLoop`/`NoFit`/`DegenerateInput` cases own one home, and confirm the `FabricationKeyPolicy` ordinal accessor against the smart-enum key-comparer attributes | hidden-line#FABRICATION_OWNER | SPIKE |

## [2]-[TOOLING_AND_TRANSCRIPTION]

The catalogue gap and the implementation sequence.

| [INDEX] | [ITEM] | [PAGE#CLUSTER] | [STATUS] |
| :-----: | ------ | -------------- | :------: |
| [1] | `.api` catalogue gap: author the `Rasm` kernel catalogue (Predicate/SpatialIndex/Matrix surface) in the tooling pass; flip the `README.md` `[ADMISSIONS_RECORD]` row off `catalogue-pending` | hidden-line#FABRICATION_OWNER | QUEUED |
| [2] | Transcribe the fabrication source per `ARCHITECTURE.md` `[SOURCE_TREE]` build order (`Projection/Fabrication.cs` through `Faults.cs` last); each file transcribes its page clusters verbatim and resolves the RESEARCH rows those pages carry | hidden-line#FABRICATION_OWNER | QUEUED |
