# [API_CATALOGUE] rfc6902

`rfc6902` supplies the RFC 6902 JSON Patch and RFC 6901 JSON Pointer surface: the `Operation` discriminated union over the six patch verbs (`add | remove | replace | move | copy | test`), the error-accumulating `applyPatch` that returns one nullable error slot per operation rather than throwing, the `createPatch` diff generator, the `createTests` optimistic-concurrency guard builder, and the `Pointer` RFC 6901 path evaluator. It owns the recorded-intent partial-update vocabulary the C# wire crosses as `JsonPatchDocument<TModel>` operations, decoded on the TS side as the same six-verb union rather than a state-diff endpoint.

## [1]-[PACKAGE_SURFACE]

[PACKAGE_SURFACE]: `rfc6902`
- package: `rfc6902`
- module: `rfc6902` (barrel `index.d.ts`); deep modules `rfc6902/diff`, `rfc6902/patch`, `rfc6902/pointer`
- asset: runtime library, zero dependencies, ESM + CJS
- rail: patch

## [2]-[PUBLIC_TYPES]

[PUBLIC_TYPE_SCOPE]: operation union
- rail: patch

| [INDEX] | [SYMBOL]           | [TYPE_FAMILY]       | [RAIL]                                  |
| :-----: | :----------------- | :------------------ | :-------------------------------------- |
|   [1]   | `Operation`        | discriminated union | `op`-keyed union of the six patch verbs |
|   [2]   | `AddOperation`     | `op: 'add'` arm     | `path: string`, `value: any`            |
|   [3]   | `RemoveOperation`  | `op: 'remove'` arm  | `path: string`                          |
|   [4]   | `ReplaceOperation` | `op: 'replace'` arm | `path: string`, `value: any`            |
|   [5]   | `MoveOperation`    | `op: 'move'` arm    | `from: string`, `path: string`          |
|   [6]   | `CopyOperation`    | `op: 'copy'` arm    | `from: string`, `path: string`          |
|   [7]   | `TestOperation`    | `op: 'test'` arm    | `path: string`, `value: any`            |
|   [8]   | `Patch`            | array alias         | `Operation[]`                           |

[PUBLIC_TYPE_SCOPE]: patch options and errors
- rail: patch

| [INDEX] | [SYMBOL]                | [TYPE_FAMILY] | [RAIL]                                             |
| :-----: | :---------------------- | :------------ | :------------------------------------------------- |
|   [1]   | `Options`               | options shape | `implicitArrayCreation?: boolean`                  |
|   [2]   | `MissingError`          | typed error   | `path: string`; pointer target absent              |
|   [3]   | `TestError`             | typed error   | `actual: any`, `expected: any`; `test` op mismatch |
|   [4]   | `InvalidOperationError` | typed error   | `operation: Operation`; unrecognized `op`          |

[PUBLIC_TYPE_SCOPE]: pointer evaluation
- rail: pointer

| [INDEX] | [SYMBOL]            | [TYPE_FAMILY]   | [RAIL]                                          |
| :-----: | :------------------ | :-------------- | :---------------------------------------------- |
|   [1]   | `Pointer`           | path evaluator  | RFC 6901 token path with `evaluate`/`get`/`set` |
|   [2]   | `PointerEvaluation` | evaluation cell | `{ parent: any; key: string; value: any }`      |
|   [3]   | `Diff`              | diff fn type    | `(input, output, ptr) => Operation[]`           |
|   [4]   | `VoidableDiff`      | diff fn type    | `(input, output, ptr) => Operation[] \| void`   |

## [3]-[ENTRYPOINTS]

[ENTRYPOINT_SCOPE]: patch application and generation
- rail: patch

| [INDEX] | [SURFACE]                             | [ENTRY_FAMILY] | [RAIL]                                                                                                       |
| :-----: | :------------------------------------ | :------------- | :----------------------------------------------------------------------------------------------------------- |
|   [1]   | `applyPatch(object, patch, options?)` | apply          | mutates `object`; returns `(MissingError\|TestError\|InvalidOperationError\|null)[]`, one slot per operation |
|   [2]   | `apply(object, operation, options?)`  | single apply   | one operation; returns `MissingError\|InvalidOperationError\|TestError\|null`                                |
|   [3]   | `createPatch(input, output, diff?)`   | diff           | minimal `Operation[]` transforming `input` into `output`                                                     |
|   [4]   | `createTests(input, patch)`           | guard builder  | `TestOperation[]` asserting the pre-patch state of touched paths                                             |

[ENTRYPOINT_SCOPE]: per-verb operation functions
- rail: patch

| [INDEX] | [SURFACE]                              | [ENTRY_FAMILY] | [RAIL]                 |
| :-----: | :------------------------------------- | :------------- | :--------------------- |
|   [1]   | `add(object, operation, options?)`     | verb apply     | `MissingError \| null` |
|   [2]   | `remove(object, operation, options?)`  | verb apply     | `MissingError \| null` |
|   [3]   | `replace(object, operation, options?)` | verb apply     | `MissingError \| null` |
|   [4]   | `move(object, operation, options?)`    | verb apply     | `MissingError \| null` |
|   [5]   | `copy(object, operation, options?)`    | verb apply     | `MissingError \| null` |
|   [6]   | `test(object, operation, options?)`    | verb apply     | `TestError \| null`    |

[ENTRYPOINT_SCOPE]: pointer evaluation
- rail: pointer

| [INDEX] | [SURFACE]                    | [ENTRY_FAMILY] | [RAIL]                                       |
| :-----: | :--------------------------- | :------------- | :------------------------------------------- |
|   [1]   | `Pointer.fromJSON(path)`     | static factory | parses an RFC 6901 `"/a/b"` string to tokens |
|   [2]   | `pointer.evaluate(object)`   | evaluate       | `PointerEvaluation` `{ parent, key, value }` |
|   [3]   | `pointer.get(object)`        | read           | value at the pointer or `undefined`          |
|   [4]   | `pointer.set(object, value)` | write          | mutates `object` at the pointer target       |
|   [5]   | `pointer.add(token)`         | extend         | new `Pointer` with one appended token        |
|   [6]   | `pointer.parent()`           | ascend         | new `Pointer` minus the last token           |

[ENTRYPOINT_SCOPE]: diff internals
- rail: patch

| [INDEX] | [SURFACE]                                | [ENTRY_FAMILY] | [RAIL]                                    |
| :-----: | :--------------------------------------- | :------------- | :---------------------------------------- |
|   [1]   | `diffAny(input, output, ptr, diff?)`     | diff kernel    | dispatches array/object/scalar diff       |
|   [2]   | `diffArrays(input, output, ptr, diff?)`  | diff kernel    | longest-common-subsequence array diff     |
|   [3]   | `diffObjects(input, output, ptr, diff?)` | diff kernel    | key-set add/remove/replace diff           |
|   [4]   | `isDestructive({ op })`                  | predicate      | true for `remove`/`replace`/`copy`/`move` |

## [4]-[IMPLEMENTATION_LAW]

[PATCH_TOPOLOGY]:
- `applyPatch` is error-accumulating, never throwing: it returns one slot per operation, `null` for success and the typed error for failure, so a partial-failure patch reports every per-operation outcome in order rather than aborting at the first fault
- `applyPatch` mutates the target in place; the caller clones before applying when the pre-patch value must survive, because no immutable-apply variant exists
- `apply` is the single-operation form of `applyPatch`; its `op` discriminant selects the verb function and an unrecognized `op` returns `InvalidOperationError`
- `test` carries optimistic concurrency: a `TestOperation` whose `value` deep-inequals the pointer target returns `TestError(actual, expected)`, so a patch prefixed with `createTests(...)` self-guards against a replayed or stale apply
- operation order is significant — `move`/`copy` reference a `from` pointer evaluated against the post-prior-operation state, so reordering changes the result

[POINTER_LAW]:
- `Pointer.fromJSON` parses the RFC 6901 escape forms `~0` → `~` and `~1` → `/`; a raw `/`-joined string with unescaped tokens is the rejected path form
- `pointer.evaluate` returns `{ parent, key, value }` so a setter mutates `parent[key]` without re-walking; `get` returns `undefined` for an absent path and never throws
- the empty pointer `""` addresses the whole document root; `"/-"` addresses the array-append position on `add`

[LOCAL_ADMISSION]:
- decode the wire patch through `Schema.decodeUnknown` over the six-arm `Schema.Union` before `applyPatch`, so an unknown `op` faults at the decode boundary as `Additive`/`Breaking` drift rather than as an `InvalidOperationError` slot after a partial apply
- fold the `(error | null)[]` result into one typed `FaultDetail` rather than reading the array per index — a non-null slot is a `HopFault` carrying the failing operation index and `op` in evidence
- `createPatch` is a development-time diff generator; a contract patch is recorded intent decoded off the wire, never a re-derived diff at the consumer

[RAIL_LAW]:
- Package: `rfc6902`
- Owns: RFC 6902 JSON Patch apply/diff and RFC 6901 JSON Pointer evaluation
- Accept: a decoded six-verb `Operation[]`; a target object to mutate in place
- Reject: a state-diff endpoint re-deriving the patch at the consumer; throwing apply that loses per-operation outcome; an undecoded raw-wire patch applied before the `op` union admits it
