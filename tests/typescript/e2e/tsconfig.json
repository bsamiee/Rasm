{
    "extends": "../../../tsconfig.base.json",
    "compilerOptions": {
        // Spec-plane package: nothing emits declarations, so the stated-annotation gate does not bind.
        "declaration": false,
        "isolatedDeclarations": false,
        "rootDir": ".",
        "types": ["node"]
    },
    "include": ["fixtures.ts", "platform/**/*.ts", "engine/**/*.ts", "gpu/**/*.ts", "load/**/*.ts"]
}
