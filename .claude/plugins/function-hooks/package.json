{
    "name": "function-hooks",
    "private": true,
    "type": "module",
    "nx": {
        "targets": {
            "typecheck": {
                "inputs": ["default", "typescript", { "externalDependencies": ["typescript"] }, { "runtime": "claude --version" }]
            }
        }
    }
}
