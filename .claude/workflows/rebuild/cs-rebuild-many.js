export const meta = {
  name: 'cs-rebuild-many',
  description: 'Per-FOLDER (not per-file) hostile ground-up rebuild of the C# AEC design pages to the docs/stacks/csharp bar AND integration of every newly-admitted .api package into the design corpus. Marries plan-cs quality/focus (the 16 named laws, ADT collapse, LanguageExt rails, two-weave AOP, full .api stacking, justified in-place capability extension, the harsh adversarial stance) with implement-cs folder dispersal: ONE agent per folder across any number of C# AEC planning folders, each folder run through a 3-step ADVERSARIAL cycle implement(max) -> critique(xhigh) -> redteam(max), every stage hostile (assume naive/illusory until it survives attack). Each agent integrates the folder newly-filled .api capabilities into the EXISTING owning design page where one fits (grow the owner in place per ROOT_REBUILD), or authors a NEW design page (and sub-domain) where the concept is a genuinely new owner with no existing home, justified and built ground-up per docs/stacks/csharp and the .planning page grammar, updating ARCHITECTURE + README. Then a cross-folder reconcile, and a FINAL whole-stack 1-2-3 alignment pass (align -> critique -> redteam by one series of agents over all folders at once) that aligns every seam/wire/port/boundary, kills duplication and strata violations, and ensures every folder fully leverages the Geometry kernel. args = optional folder path or array; empty = all C# AEC planning folders (Persistence, AppHost, AppUi, Bim, Fabrication, Compute, Materials, and the Geometry kernel with its special Rasm-root layout).',
  phases: [
    { title: 'Realize', detail: 'per folder (1 agent/folder, pooled): implement(max) -> critique(xhigh) -> redteam(max), every stage adversarial; integrate each new .api into the existing owner or a new justified page; collapse + capability-extend the whole folder corpus, own-folder-only, cross-folder seams logged as residuals' },
    { title: 'Reconcile', detail: 'consume cross-folder residuals: union-find cluster by shared file -> fix(max) -> adversarial verify(xhigh); hard residuals reported for resolve-residuals' },
    { title: 'Final-Align', detail: 'one series of agents over ALL folders at once (align(max) -> critique(xhigh) -> redteam(max)): align every cross-folder seam/wire/port/boundary, kill duplication + strata violations, ensure every consumer fully leverages the Geometry kernel, catch gaps no single-folder pass could see' },
  ],
}

// --- [HARNESS] -- steady bounded pool: <=CAP in flight AND a serialized launch gate ----------
const STAGGER_MS = 1500
const sleep = (ms) => new Promise((res) => setTimeout(res, ms))
const pool = async (items, cap, worker) => {
  const out = new Array(items.length)
  let next = 0
  let gate = Promise.resolve()
  const launch = () => { gate = gate.then(() => sleep(STAGGER_MS)); return gate }
  const run = async () => { while (next < items.length) { const i = next++; await launch(); out[i] = await worker(items[i], i) } }
  await Promise.all(Array.from({ length: Math.min(cap, items.length) }, () => run()))
  return out
}
const CAP = 8

// --- [SCOPE] -- the seven C# AEC folders; Geometry has the special nested layout --------------
const ALL_FOLDERS = [
  { name: 'Persistence', planning: 'libs/csharp/Rasm.Persistence/.planning', api: 'libs/csharp/Rasm.Persistence/.api', root: 'libs/csharp/Rasm.Persistence', note: '' },
  { name: 'AppHost', planning: 'libs/csharp/Rasm.AppHost/.planning', api: 'libs/csharp/Rasm.AppHost/.api', root: 'libs/csharp/Rasm.AppHost', note: '' },
  { name: 'AppUi', planning: 'libs/csharp/Rasm.AppUi/.planning', api: 'libs/csharp/Rasm.AppUi/.api', root: 'libs/csharp/Rasm.AppUi', note: '' },
  { name: 'Bim', planning: 'libs/csharp/Rasm.Bim/.planning', api: 'libs/csharp/Rasm.Bim/.api', root: 'libs/csharp/Rasm.Bim', note: '' },
  { name: 'Fabrication', planning: 'libs/csharp/Rasm.Fabrication/.planning', api: 'libs/csharp/Rasm.Fabrication/.api', root: 'libs/csharp/Rasm.Fabrication', note: '' },
  { name: 'Compute', planning: 'libs/csharp/Rasm.Compute/.planning', api: 'libs/csharp/Rasm.Compute/.api', root: 'libs/csharp/Rasm.Compute', note: '' },
  { name: 'Materials', planning: 'libs/csharp/Rasm.Materials/.planning', api: 'libs/csharp/Rasm.Materials/.api', root: 'libs/csharp/Rasm.Materials', note: '' },
  { name: 'Geometry', planning: 'libs/csharp/Rasm/Geometry/.planning', api: 'libs/csharp/Rasm/.api', root: 'libs/csharp/Rasm', note: 'SPECIAL LAYOUT: the geometry design pages live under libs/csharp/Rasm/Geometry/.planning, but the governing ARCHITECTURE.md/README.md/IDEAS.md/TASKLOG.md and the .api catalogs live at the libs/csharp/Rasm ROOT (one level up). Read the Rasm-root docs + .api as the governing context and shared capability tier; author NEW pages only under Rasm/Geometry/.planning; NEVER touch the mature siblings Analysis/Domain/Vectors.' },
]
const norm = (t) => { const s = String(t).trim(); return s.replace(/^libs\/csharp\//, '').replace(/\/$/, '') }
const wanted = Array.isArray(args) ? args.filter(Boolean).map(norm)
  : (typeof args === 'string' && args.trim() && args.trim().toUpperCase() !== 'ALL') ? [norm(args)]
  : null
const FOLDERS = wanted ? ALL_FOLDERS.filter((f) => wanted.some((w) => w === f.name || w === 'Rasm.' + f.name || (f.name === 'Geometry' && (w === 'Rasm' || w === 'Rasm/Geometry')))) : ALL_FOLDERS

// --- [MODELS] -- the doctrine blocks woven into every prompt (plan-cs depth, folder scope) -----
const LAW = [
  'Rasm monorepo, libs/csharp planning corpus (markdown specs of intended C# package designs). CLAUDE.md manifest + WORKSPACE_LAW strata govern (KERNEL -> AEC-DOMAIN -> APP-PLATFORM -> HOST-BOUNDARY -> APP; depend strictly upward; a host-neutral owner only where a non-Rhino runtime consumes the contract). This pass works ONE FOLDER end-to-end: the whole folder design corpus under its `.planning/**` plus its `.api/` capability tier, NOT a single page. C# PLANNING-HOMING: for the `Geometry` target the design pages live at `libs/csharp/Rasm/Geometry/.planning/**` while the governing `ARCHITECTURE.md`/`README.md`/`IDEAS.md`/`TASKLOG.md` and `.api/` catalogs live at the `libs/csharp/Rasm/` package ROOT; read those root docs + `.api/` as governing context and never trample the mature siblings `Analysis`/`Domain`/`Vectors`.',
  'MANDATORY STANDARDS — docs/stacks/csharp/ is the FLOOR, not the ceiling: every fence MUST meet docs/stacks/csharp/ (README, language, shapes, surfaces-and-dispatch, rails-and-effects, boundaries, algorithms, system-apis) AND the specialized docs/stacks/csharp/domain/ shard(s) relevant to the page concern (compute, concurrency, data-interchange, diagnostics, durability, interaction, persistence, postgres, resilience, runtime, transport, validation, visuals), then PUSH PAST it to the objectively strongest form the doctrine admits. READ the relevant shard(s) and conform exactly — a hard gate enforced by the `tools/cs-analyzer` compiled-doctrine gate (a true positive is architecture pressure, fix the shape; a false positive is rule pressure, never a suppression). Cite only host/NuGet members confirmed via `uv run python -m tools.assay api`; back bridge claims with EvidenceCertificate + reviewed ReferenceEvidence.',
  'TWO JOBS, ONE PASS: (1) INTEGRATE every newly-admitted package — each folder `.api/api-*.md` catalog that a recent survey added and a focused rebuild-api filled — into the folder design corpus so its capability is COMPOSED, not merely catalogued; (2) HOSTILE GROUND-UP REBUILD of the folder pages to the ULTRA bar (collapse surfaces/types, deepen bleeding-edge spellings, maximize admitted-library capability, close concept capability gaps). Both are untied to any card: improve the corpus objectively wherever the doctrine admits a stronger form.',
  'WRITE-FULLY MANDATE: every fix you identify you MUST make NOW via Edit/Write directly in the file — the structured fix-log you return is a REPORT of edits ALREADY MADE, never a to-do list, a ledger, or a would/should-fix hedge; leave nothing behind except genuine cross-FOLDER items (report those in residual_high).',
].join('\n')
const INTEGRATE = [
  'NEW-CAPABILITY INTEGRATION (the distinguishing mandate of this pass): for EVERY package newly admitted to this folder (read its filled `.api/api-*.md` catalog to full depth + verify members via `uv run python -m tools.assay api`), the capability MUST land in the design corpus as COMPOSED design, never a dangling catalog. PLACEMENT, most-specific wins: (a) GROW AN EXISTING OWNER — when an existing design page already owns the concept the package serves, weave the package capability INTO that owner in place (a case in the closed family, a row/richer data on the smart-enum, a field or composed `[ValueObject]`/`[ComplexValueObject]`, an operation on the existing surface, a policy value), reshaped per ROOT_REBUILD + COMPOSED_IMPLEMENTATION as if it had always carried it — NEVER a parallel surface beside the owner; (b) AUTHOR A NEW PAGE — ONLY when the package introduces a genuinely NEW owner/concern that no existing page owns, author a new design page (and a new `.planning/<sub-domain>/` folder when the concern warrants its own sub-tree) at one-page-per-eventual-source-file granularity, built GROUND-UP to the full .planning page grammar (H1, lead paragraph, numbered cluster sections, card + transcription-complete signature fences) and the docs/stacks/csharp bar, then UPDATE the folder `ARCHITECTURE.md` codemap + `[02]-[SEAMS]` and the `README.md` router/package roster so the structure stays truthful.',
  'NEW-FILE DISCIPLINE: a new page is justified ONLY by a genuinely new domain owner — NEVER to dodge a collapse, reduce a page size, or split one concept across files. The collapse law still rules WITHIN every page: 3+ parallel shapes collapse into ONE owner in the SAME page. Before authoring a new page, prove no existing page is the rightful owner; a capability that belongs to an existing owner is grown there, not spun out. A new page that fragments an existing concept is a defect; a new page for a real new owner the folder was missing is correct.',
].join('\n')
const ADVERSARIAL = [
  'ADVERSARIAL STANCE — EVERY stage (implement, critique, AND red-team) is HOSTILE: assume the existing fences are NAIVE, SHALLOW, JUNIOR, or ILLUSORY until they survive an aggressive attack; the burden of proof is ON THE CODE, never on you. "Mature", "already strong", "good enough", "done", "polished", and a prior `clean` verdict are REJECTED self-assessments — MOST of this corpus is naive, surface-level code dressed in the right vocabulary, and it is NOT tolerable. Default to "this fence is naive and must be rebuilt to the strongest form the doctrine admits" and MAKE that rebuild; a no-edit verdict is reached ONLY after a genuinely aggressive attack on the real domain + the verified package surface finds nothing — never a first-read concession, never to avoid work. Reject "good enough" categorically.',
  'ILLUSORY / FAKE CODE is the PRIMARY target — the MOST dangerous code PRETENDS to be advanced: it uses the doctrine vocabulary (`[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]`/`Fold`/the rails), cites packages, reads dense and confident — yet is HOLLOW. Treat dense, confident-looking fences with MORE suspicion, and DISBELIEVE every claim the page makes about itself until you verify it against the real domain and the catalogued package surface. HUNT: a name/signature/prose PROMISING capability the body does not implement; a "rich" owner that is a thin slice of its concept (a 2-case union for a 20-case domain; the obvious 3 fields where the concept carries fifteen); decorative density carrying no real capability; a stub/sketch dressed as a finished design; a newly-admitted `.api` cited in prose but never actually composed into a fence; a phantom member cited but unverifiable. Every illusion is a DEFECT to rebuild.',
].join('\n')
const ULTRA = [
  'OPERATIVE DOCTRINE — the 16 named laws of docs/stacks/csharp/README.md, held as fact: [FLOW] EXPRESSION_SPINE (domain logic is expression-shaped; dependent steps `Bind` monadically, independent ones accumulate applicatively; the carrier, never a flag, selects the algebra; statements survive only in measured `ref struct`/span kernels that name the exemption) + BOUNDARY_ADMISSION (raw admitted EXACTLY ONCE into an evidence-carrying owner; interior never re-validates or sees null/sentinel/provider shape). [SHAPE] SHAPE_BUDGET (one concept owns ONE type; variants are cases in one closed family) + DEEP_SURFACES + MODAL_ARITY (one entrypoint owns every modality, discriminating on input shape) + ANTICIPATORY_COLLAPSE (shape the owner for the family it will absorb). [DERIVATION] POLICY_VALUES + DERIVED_LOGIC + DERIVED_TYPES + SYMBOLIC_REFERENCE + SEMANTIC_NAMING. [MATERIAL] LIBRARY_DEPTH + DEFINITION_TIME_ASPECTS. [INTEGRATION] ROOT_REBUILD (weave new capability into the owner as if always present; no shims/aliases/[Obsolete]/migration layers) + ONE_HOP_RESOLUTION + COMPOSED_IMPLEMENTATION.',
  'ULTRA-ADVANCED COLLAPSE MANDATE: COLLAPSE >=3 parallel types / sibling factory methods / repeated switch arms / single-call private helpers into ONE polymorphic owner IN THE SAME FILE via `[Union]` / `[Union<T1,...>]` ad-hoc / `[SmartEnum<TKey>]` / `[SmartEnum]` keyless / `[ValueObject<T>]` / `[ComplexValueObject]` / source-generated case families / `Fold` algebra / frozen data tables — never extract a new file to reduce LOC, never delete capability.',
  'LIFECYCLE SPINE (BOUNDARY_ADMISSION): every fence flows raw -> admit ONCE (generated factory + validation partial admits/rejects; one rail bridge lifts the generated outcome into `Fin<T>` / `Validation<Error,T>`; `Option<T>` carries absence; exceptions convert at the owning boundary only) -> canonical owner -> unified rail -> projection -> egress. Interior code never re-validates, never sees `null`-as-failure, sentinels, or provider shapes; parameterize BOTH ingress AND egress so the same owner sources and sinks across many consumers without interior edits.',
  'STACK CAPABILITY: FIRST mine the package `.api/*.md` catalogs (the curated, integration-shaped capability surface at `libs/csharp/<Package>/.api/`; for the `Geometry` effort they live at `libs/csharp/Rasm/.api/`) AND the universal Thinktecture / LanguageExt rails — C# has NO central `.api/` tier, so the universals are Thinktecture (generated domain shape) / LanguageExt (rails, effects, schedules, immutable collections) plus full docs/stacks/csharp doctrine, with MathNet / CSparse owning numeric algorithms. Compose EVERY relevant host API + admitted NuGet package + catalog member into single dense owners woven as ONE rail (source-generated owners, `Fold` algebra, data tables), ALWAYS layering the universal Thinktecture/LanguageExt rails onto the domain packages, NOT flat one-shot per-API uses. Use the DEEPEST operator/combinator/generated surface each package itself reaches (LIBRARY_DEPTH); reject surface-level subsets, BCL-first reflexes, and thin rename wrappers; verify novel members with `uv run python -m tools.assay api`.',
  'PRESERVE all capability (densify, never delete functionality). Where a fence is already dense, deepen; where it is flat/naive, rebuild ground-up. Never regress correctness or boundary/strata law.',
].join('\n')
const EXTEND = [
  'CAPABILITY EXTENSION (justified, in-place, never flat spam) — structural collapse and `.api`-stacking are NECESSARY but NOT SUFFICIENT. A page can be fully collapsed into one polymorphic owner and STILL be capability-thin: modeling a NAIVE slice of its domain concept. Structural completeness and CAPABILITY completeness are ORTHOGONAL. A FULL rebuild ALSO closes the capability gap so the page OWNS ITS DOMAIN CONCEPT COMPLETELY. Per COMPOSED_IMPLEMENTATION + the DOCTRINE growth law (capability grows sublinearly; growth lands as cases/rows/policy-values INSIDE existing owners), every real missing concern lands as a CASE in the existing closed family, a ROW or richer data on the existing smart-enum, a FIELD or a composed `[ValueObject]`/`[ComplexValueObject]` on the existing record, an OPERATION on the existing surface, or a POLICY_VALUE — reshaping the owner per ROOT_REBUILD; NEVER a parallel type or flat appended code.',
  'GAP SOURCES (every extension MUST cite exactly one): (a) PACKAGE — a member the admitted package/host surface exposes that the concept ADMITS but the page IGNORES (LIBRARY_DEPTH; verify via `uv run python -m tools.assay api`) — this is the PRIMARY source for the newly-admitted .api packages this pass integrates; (b) DOMAIN — an attribute, metric, sub-kind, relationship, state, or operation the REAL concept demands but the page omits; (c) CONSUMER — a contract a sibling or downstream owner will require that has no composed spelling here yet.',
  'JUSTIFIED, NOT RANDOM: if after a real domain + package + consumer sweep the concept is genuinely complete, prove it by adding nothing — never invent capability to look busy or pad with flat fields. Every added case/row/field/operation is load-bearing, cites a package member / domain attribute / consumer contract, and composes the existing rails; preserve ALL existing capability.',
].join('\n')
const PATLAW = [
  'C# PATTERN LAW: model the domain precisely — NEVER weak/unbounded/erased types where the language can express the domain; NEVER exception control flow in domain logic (use the LanguageExt typed rails / ROP); NEVER imperative branching where a bounded vocabulary, frozen table, generated `Switch`, match, or `Fold` owns the variation; NEVER mutable accumulation for domain transforms. Total generated `Switch` with compile-time exhaustiveness (a new case breaks every dispatch site — NEVER a runtime-silent `_` arm). Typed algorithm receipts (NEVER a generic `IReceipt`/ledger) when fields carry route/status/sampling/solver/spectral/mesh/extraction/benchmark/host evidence. The fault type is a CLOSED `[Union]` family deriving from `Expected`.',
  'Latest stable C# 14 on `net10.0` to the metal (`Nullable enable`, NRT enforced): primary constructors, collection expressions with spread, `params` collections (incl. `params ReadOnlySpan<T>`), list/slice/relational/logical pattern matching, switch expressions, `required` members, `file`-scoped types, `field` accessors, extension blocks (`extension(Receiver)`) and extension operators, generic math / static abstract+virtual interface members, `with` expressions, `nameof` with unbound generics, `System.Threading.Lock`, raw string + `u8` literals where they fit. Apply the docs/stacks/csharp file-organization and section-order law (`[Union]`/`[SmartEnum]`/`[ValueObject]` and generated case families stay inside the declaring owner block; canonical section order TYPES -> CONSTANTS -> MODELS -> ERRORS -> SERVICES -> OPERATIONS -> COMPOSITION -> EXPORTS).',
  'Keep conventions IDENTICAL across every package; place each package on its canonical stratum and depend strictly upward; geometry/mesh/IFC meet at the wire with one owner per runtime; never leak a host type into a host-neutral owner. SEMANTIC_NAMING: one canonical bounded-context term per concept (one word default, three the ceiling); arity/filter/provider/modality live in request shape, case, or policy row, never parallel `Get`/`GetMany`/`GetBy<Key>`/`List`/`Search` names; ONE_HOP_RESOLUTION.',
].join('\n')
const BOUNDARIES = 'BOUNDARY LAW: keep every package owner strictly in its lane and on its stratum; geometry/mesh/IFC meet at the wire with one owner per runtime; internal code uses canonical names and shapes with mapping only at the edge; do not trample a sibling owner while densifying; never introduce a downward dependency or leak a host type into a host-neutral owner. This pass realizes ONLY the current folder pages; a concern owned twice across a runtime, a folder mixing unrelated concerns, or coupling to a sibling owner INTERIOR (vs its seam/wire) is a defect to fix in-folder or log as a cross-folder residual.'
const PROSE = [
  'PROSE QUALITY — apply docs/standards/style-guide.md. The page is a design SPEC: high-signal prose ONLY. Lead each section with the controlling rule/contract; one idea per paragraph; close on the consequence or boundary. Cut noise: no provenance, process narration, freshness disclaimers, report framing, or empty hedges (may/might/probably/generally/where possible). Prefer a table, a typed signature block, or a tight bullet wherever it carries the design better than a paragraph. Prose that ASSERTS capability the fence does not implement is a defect.',
  'BACKTICK ALL CODE: wrap every symbol, type, field, method, operator, package ID, path, command, flag, and literal value in backticks. Name the exact member/type/rail in backticks instead of paraphrasing behavior. Trimming prose MUST NOT reduce technical density or remove design content.',
].join('\n')
const COMMENTS = 'COMMENT HYGIENE: code fences are agent-facing — comment for the next agent, never as a tutorial. KEEP the canonical section-divider headers (language-comment marker + space + `---` + bracketed `[UPPERCASE_LABEL]` + dash-fill). Beyond dividers, comment ONLY where intent is not already obvious from names, types, and signatures: default to ZERO comments on self-evident code; at most 1 line where a comment genuinely earns its place; 1-2 lines only for a truly subtle invariant. NO restating the code, no narration, no task/process/session/history/proof/review comments, no XML-doc bloat.'
const DOCTRINE = [LAW, '', INTEGRATE, '', ADVERSARIAL, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', PROSE, '', COMMENTS].join('\n')

// --- [SCHEMAS] -------------------------------------------------------------------------------
const FIXLOG_SCHEMA = { type: 'object', additionalProperties: false, required: ['folder', 'verdict', 'summary'], properties: { folder: { type: 'string' }, verdict: { type: 'string', enum: ['rebuilt', 'refined', 'clean'] }, integrated: { type: 'array', items: { type: 'string' } }, newPages: { type: 'array', items: { type: 'string' } }, collapsed: { type: 'string' }, extended: { type: 'string' }, residual_high: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['files', 'claim'], properties: { files: { type: 'array', items: { type: 'string' } }, claim: { type: 'string' } } } }, summary: { type: 'string' } } }
const RESIDUAL_FIX_SCHEMA = { type: 'object', additionalProperties: false, required: ['files', 'verdict', 'summary'], properties: { files: { type: 'array', items: { type: 'string' } }, verdict: { type: 'string', enum: ['fixed', 'clean'] }, summary: { type: 'string' } } }
const RECONCILE_VERIFY_SCHEMA = { type: 'object', additionalProperties: false, required: ['overall', 'claims'], properties: { overall: { type: 'boolean' }, claims: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['claim', 'status'], properties: { claim: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'invalid', 'open'] }, evidence: { type: 'string' } } } } } }

// --- [OPERATIONS] -- per-folder prompt builders: implement -> critique -> redteam --------------
const scopeLine = (f) => 'FOLDER: `' + f.name + '` — design pages under `' + f.planning + '/**`, capability catalogs under `' + f.api + '/`, governing docs at `' + f.root + '`.' + (f.note ? ' ' + f.note : '')
const implementPrompt = (f) => [DOCTRINE, '',
  'TASK: PER-FOLDER HOSTILE REBUILD + NEW-.api INTEGRATION of `' + f.name + '`. ' + scopeLine(f),
  'Read EVERY design page under `' + f.planning + '/**`, the folder `.api/api-*.md` catalogs (especially the newly-admitted ones a recent survey added and rebuild-api filled), the governing `ARCHITECTURE.md` + `README.md`, the docs/stacks/csharp/ core + the relevant domain/ shard(s) for each concern, and the universal Thinktecture/LanguageExt rails; VERIFY every cited host/NuGet member via `uv run python -m tools.assay api` (a member you cannot verify is a phantom — delete it). Then, across the WHOLE folder corpus: (1) INTEGRATE every newly-admitted package capability into the design — GROW the existing owner page where one fits (a case/row/field/operation/policy-value per ROOT_REBUILD + COMPOSED_IMPLEMENTATION), or AUTHOR a NEW page (and sub-domain folder) ONLY where the package is a genuinely new owner with no existing home, built ground-up to the .planning page grammar + the docs/stacks/csharp bar, and UPDATE `ARCHITECTURE.md` codemap + `[02]-[SEAMS]` + `README.md`; (2) HOSTILE GROUND-UP REBUILD of the folder pages to the ULTRA bar — construct in LIFECYCLE order (admit raw ONCE -> canonical owner via OWNER_CHOOSER -> two-weave aspects over a thin pure core -> ONE unified `Fin`/`Validation`/`Option`/`Eff` rail with total generated `Switch` -> project + egress, both ingress and egress parameterized), collapse >=3 parallel shapes into one `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject<T>]`/`[ComplexValueObject]`/source-generated case family IN THE SAME page, drive cases with a `Fold` algebra or frozen table, and CLOSE the concept capability gaps in place (each extension citing a package member / domain attribute / consumer contract). Realize ONLY `' + f.name + '` pages; never edit a sibling folder page. Modern C# 14 / net10 to the metal, high-signal prose all-backticked, comment hygiene, fix-in-place. Return verdict + integrated (each new .api woven in, and where) + newPages (any new page authored, with its justification) + collapsed (before->after counts) + extended (each addition + its cited source) + residual_high (each {files:[every repo-relative path the cross-folder fix spans], claim} for a CROSS-FOLDER item) + summary.'].join('\n')
const critiquePrompt = (f) => [DOCTRINE, '',
  'TASK: HOSTILE DOCTRINAL-CONFORMANCE AUDIT + CAPABILITY-COMPLETENESS + INTEGRATION AUDIT + FIX IN PLACE across `' + f.name + '`. ' + scopeLine(f) + ' You are an ULTRA-HARSH, UNAGREEABLE auditor: assume a violation exists in EVERY fence until you prove otherwise, trust NOTHING the prior pass or the prose claims, and "good enough"/"mature" is rejected outright. Read the folder pages, the `.api/` catalogs, the operative docs/stacks/csharp/ pages AND the relevant domain/ shard(s), and the universal Thinktecture/LanguageExt rails. Run these MECHANICAL checklists line-by-line and REPAIR every hit in place (a fix, never a ledger note):',
  '(1) COLLAPSE_SCAN — sibling prefix/suffix names -> one modality-polymorphic entrypoint; same return rail differing only by arity -> input-shape discrimination; functions differing only by a literal -> a POLICY_VALUE; a `bool`/`mode`/`batch` parameter selecting two bodies -> one derived body or policy value; a method calling exactly one other -> delete the hop; parallel dispatch arms repeating structure -> a `Fold` algebra or frozen table; several types sharing fields for one concept -> one closed family; a `Get`/`GetMany`/`GetBy<Key>`/`List`/`Search` family -> one input-keyed polymorphic operation; a wrapper renaming a package API -> the package surface directly; 3+ parallel types / sibling factories / repeated switch arms / single-call helpers -> ONE `[Union]` / `[SmartEnum<TKey>]` / `[ValueObject<T>]` / `[ComplexValueObject]` / source-generated case family IN THE SAME page.',
  '(2) OWNER_CHOOSER — re-derive each owner from the 5 discriminants (admission, identity regime, variant arity, payload timing, openness), most-specific wins: invariant-bearing scalar -> `[ValueObject<TKey>]`; N-field one-concept product no discriminator -> `[ComplexValueObject]`; bounded vocabulary wire-keyed identity -> `[SmartEnum<TKey>]`; bounded vocabulary process-local behavior -> `[SmartEnum]` keyless; closed alternatives per-occurrence payload -> `[Union]`; one value over 2-5 unrelated types -> `[Union<T1,...>]` ad-hoc; interior product no invariant -> `record`/`readonly record struct`; combinable capability set -> a frozen set; cross-product/external policy key -> a frozen table; foreign wire enum / ABI bits / kernel ordinal -> a language `enum` AT THE SEAM ONLY. Kill every parallel DTO, one-field wrapper, field-rename shape, nullable-as-failure, and struct-`default` ghost.',
  '(3) KNOB_TEST — delete each parameter; if the value reconstructs what it carried it was a knob -> collapse a `bool`/`mode`/`strict`/`batch` flag into a policy value or input-shape discriminant; a nullable flag tail -> one `Option<ContextRecord>`; move every `timeout`/`retry`/`deadline`/`CancellationToken` OFF the signature onto the carrier or a composition-time effect aspect.',
  '(4) ASPECTS — definition-time concerns (admission, identity, dispatch, serialization, grammar, logging) attach via attribute-directed SOURCE GENERATION; composition-time concerns attach as effect transformers — retry as `Schedule`-driven `IO<T>.Retry(Schedule)`/`Prelude.retry`, recovery as named catch combinators (`@catch`/`catchOf`/`CatchM` composed via `|`), resource lifetime as `Bracket`/`BracketIO`/`Finally`; the two weaves meet at EXACTLY ONE seam, the admission rail bridge. 2-4 co-occurring wrappers collapse into one aspect; an aspect NEVER raises into domain flow.',
  '(5) RAILS — RAIL_CHOOSER, the narrowest carrier chosen ONCE at admission: `Option<T>` absence, `Fin<T>` synchronous fallibility, `Validation<Error,T>` independent accumulated faults, `Eff<RT,T>` runtime capability, `IO<T>` deferred boundary work, `Schedule` retry policy; the fault type is a CLOSED `[Union]` family deriving from `Expected` (recovery identity via `Is`/`HasCode`/`IsType<E>`, never `==`); accumulate-vs-abort disposition correct (`Apply`/`&`/`.Traverse` for independents, `Bind`/`.TraverseM`/query for dependents); total generated `Switch` (NO `_` arm hiding a case); `.Fold`/`.Traverse`/`.Choose` with the mandatory `.As()` re-anchor; NO exception control flow in domain logic, NO mutable accumulation.',
  '(6) STRATA/MEMBERS/MODERN — strata correctness (depend strictly upward; NO downward dependency, NO host-type leak into a host-neutral owner; geometry/mesh/IFC meet at the wire with one owner per runtime); cite ONLY host/NuGet members confirmed in the `.api/` catalog (verify novel members via `uv run python -m tools.assay api`); latest modern C# 14 on net10; FULL docs/stacks/csharp + the relevant domain/ shard conformance; BOTH the `.api/` catalogs AND the universal Thinktecture/LanguageExt rails maximized; the `tools/cs-analyzer` doctrine-gate clean.',
  '(7) INTEGRATION-COMPLETENESS + CAPABILITY-COMPLETENESS + ILLUSION — for EVERY newly-admitted folder `.api` package, verify its capability is actually COMPOSED into a fence (not just named in prose); an admitted package whose `.api` exposes capability no owner exploits is a named gap to close NOW (grow the owner, or — only if a genuinely new owner — a new page). Independently, structural collapse and capability completeness are ORTHOGONAL: a fully-collapsed owner can still model a NAIVE slice of its concept — close it in place. Reject the inverse: a speculative/padding field, decorative ceremony, an UNJUSTIFIED new page that fragments an existing owner, or prose asserting capability the fence lacks is deleted/folded back.',
  'Also enforce the docs/stacks/csharp file-organization + section-order law, cross-package convention consistency, and prose + comment hygiene. EDIT the `' + f.name + '` pages to fix every hit; OVERRIDE any earlier residual you can now resolve. Return verdict + integrated + newPages + collapsed + extended + residual_high + summary.'].join('\n')
const redteamPrompt = (f) => [DOCTRINE, '',
  'TASK: ADVERSARIAL ARCHITECT RED-TEAM + FIX IN PLACE across `' + f.name + '`. ' + scopeLine(f) + ' You are the LAST and MOST AGGRESSIVE pass: assume the implement and critique missed things and that the chosen design is naive or illusory until PROVEN the strongest, the burden of proof ON THE DESIGN; trust nothing the prior passes or the prose claimed. Open the `.api/` catalogs and the universal Thinktecture/LanguageExt rails, the sibling pages, the operative docs/stacks/csharp/ pages, and the relevant domain/ shard. Attack from every direction and REPAIR every defect in place — no soft-pedalling, a fix never a ledger.',
  'PRIMARY LENS: (A) COUNTERFACTUAL on each core choice — is the owner, the algebra (`Fold`/generated `Switch`/data table), and the dispatch form categorically the strongest the doctrine admits, or does a denser owner or a DEEPER admitted-package primitive (LanguageExt/Thinktecture/MathNet/CSparse) collapse the whole fence? Rebuild to the stronger form; never defend the incumbent. (B) ANTICIPATORY_COLLAPSE — compute the DIFF OF THE NEXT FEATURE: the next case/dimension/knob/modality/provider lands as ONE case/row/policy value with every consumer untouched or broken LOUDLY at compile time (total generated `Switch`, no silent `_`)? If it would touch multiple sites, reshape the growth axis. (C) LONG-TAIL + MULTI-DIMENSIONAL — attack every input/output/edge/failure mode (empty, singular, plural, stream, malformed, concurrent, cancelled, partial-failure, version-skew); accumulate-vs-abort correct for the REAL boundary; BOTH ingress AND egress parameterized. (D) STRATA + BOUNDARY-INTEGRITY — a downward dependency, a host-type leak, a concern owned twice in a runtime, a folder mixing concerns, geometry/mesh/IFC not meeting at ONE wire owner per runtime, coupling to a sibling owner INTERIOR, OR a sibling page left STALE by this folder change (seam drift) is a defect: fix in-folder or log as a cross-folder residual. (E) SURFACE-SPRAWL-IN-TIME + PHANTOMS — an admitted package whose `.api` or the universal rails expose capability the fence re-derives by hand, flat code below the operator depth the packages reach, a phantom member, or a thin wrapper: collapse to package depth and verify the member (via `assay api`). (F) INTEGRATION + CAPABILITY-COMPLETENESS — counterfactually attack each owner for domain-completeness AND verify every newly-admitted .api capability is genuinely composed into a fence; an UNJUSTIFIED new page (fragmenting an existing owner) is folded back, a MISSING owner (a real new concern with no page) is authored.',
  'ALSO — FULL COLD ADVERSARIAL RE-REVIEW (every time): re-attack every conformance dimension with fresh hostile eyes — the COLLAPSE_SCAN signals, OWNER_CHOOSER per shape, the KNOB_TEST per param, the two-weave ASPECT taxonomy, rail + closed-`Expected`-fault discipline, integration + capability-completeness per owner, strata correctness, modern-C# 14 typing, docs/stacks/csharp + domain-shard conformance, `.api` + Thinktecture/LanguageExt maximization, the `tools/cs-analyzer` doctrine-gate, and prose/comment hygiene — and fix every defect. The folder must end objectively denser, MORE CAPABLE, more correct than the critique left it; if the strongest form is genuinely already present, prove it by finding nothing — never invent churn. Realize ONLY `' + f.name + '` pages; log cross-FOLDER items as residual_high. Return verdict + integrated + newPages + collapsed + extended + residual_high + summary.'].join('\n')

// --- [STAGES] ----------------------------------------------------------------------------------
const STAGES = [
  { key: 'implement', build: implementPrompt, effort: 'max' },
  { key: 'critique', build: critiquePrompt, effort: 'xhigh' },
  { key: 'redteam', build: redteamPrompt, effort: 'max' },
]
// --- [FINAL-ALIGN] -- whole-stack cross-folder coherence prompts (one series of agents) -------
const ALIGN_SCHEMA = { type: 'object', additionalProperties: false, required: ['verdict', 'summary'], properties: { verdict: { type: 'string', enum: ['aligned', 'clean'] }, aligned: { type: 'array', items: { type: 'string' } }, residual: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['files', 'claim'], properties: { files: { type: 'array', items: { type: 'string' } }, claim: { type: 'string' } } } }, summary: { type: 'string' } } }
const ROSTER = FOLDERS.map((f) => f.name + ' (' + f.root + (f.name === 'Geometry' ? ', design pages under ' + f.planning : '') + ')').join('; ')
const FINAL = [
  'WHOLE-STACK CROSS-FOLDER ALIGNMENT — this is the FINAL pass, after every folder ran its own per-folder implement -> critique -> redteam and the residual reconcile. Hold ALL these C# AEC folders in view at once and align them as ONE coherent stack: ' + ROSTER + '. Read each folder ARCHITECTURE.md ([1]-[DOMAIN_MAP] + [2]-[SEAMS]) and README, the central manifest, and every design page a cross-folder seam spans. This is the ONLY pass that edits ACROSS folders — but it aligns at the seam and never trespasses a folder owner interior.',
  'ALIGN OBJECTIVES (fix every one in place across the spanned folders): (1) SEAMS/WIRES — every cross-folder producer/consumer relationship is recorded in BOTH endpoint folders ARCHITECTURE [02]-[SEAMS] with mirrored glyphs and ONE shared shape; a stale, missing, or one-sided seam is a defect. (2) PORTS/BOUNDARIES — a capability a folder now needs from a sibling has a named port/boundary on both sides; add the new port where a per-folder pass created the need but could not reach the sibling. (3) NO DUPLICATION — a concept owned twice across the runtime collapses to the ONE rightful stratum owner; the sibling consumes the seam, never a second mint. (4) NO STRATA VIOLATION — depend strictly upward (KERNEL -> AEC-DOMAIN -> APP-PLATFORM -> HOST-BOUNDARY -> APP); no downward dependency, no host-type leak into a host-neutral owner, geometry/mesh/IFC meet at the wire with one owner per runtime. (5) GEOMETRY-KERNEL LEVERAGE — every folder that depends on the Rasm geometry KERNEL FULLY and PROPERLY leverages it (its geometry types, predicates, exact-precision numerics, and operations) for BOTH existing and new functionality, NEVER re-implementing a kernel capability; a consumer hand-rolling what the kernel owns is a defect to rewire to the kernel. (6) GAPS/OVERSIGHTS — hunt the mistakes no single-folder pass could see from inside one folder, and fix them.',
].join('\n')
const finalAlignPrompt = () => [DOCTRINE, '', FINAL, '', 'TASK: WHOLE-STACK ALIGN — walk every cross-folder seam/wire/port/boundary across all folders and ALIGN them in place. Record every seam in both endpoint ARCHITECTURE [02]-[SEAMS] with mirrored glyphs, add the new ports/boundaries the per-folder passes surfaced, collapse any concept owned twice to its rightful stratum owner (the sibling consumes the seam), fix any strata violation or host-type leak, and rewire any consumer that hand-rolls a Geometry-kernel capability to leverage the kernel instead. Read the folders ARCHITECTURE + README + central manifest + the spanned design pages; verify any host/NuGet member via `uv run python -m tools.assay api`. Fix every defect in place across the spanned folders, regressing none. Return verdict + aligned (each alignment made, naming the folders + the seam/port) + residual (each {files, claim} for anything you could not fully resolve) + summary.'].join('\n')
const finalCritiquePrompt = () => [DOCTRINE, '', FINAL, '', 'TASK: ADVERSARIAL AUDIT of the whole-stack alignment + FIX IN PLACE. Assume a cross-folder defect remains until you prove otherwise; trust nothing the align pass claimed. Re-walk EVERY seam/wire/port/boundary across all folders: a one-sided or stale seam, a missing mirror glyph, a concept still owned twice, a residual strata violation or downward dependency or host-type leak, a consumer still hand-rolling a Geometry-kernel capability, a new port the align pass missed, a gap between two folders. Repair every hit in place across the spanned folders. Return verdict + aligned + residual + summary.'].join('\n')
const finalRedteamPrompt = () => [DOCTRINE, '', FINAL, '', 'TASK: ADVERSARIAL RED-TEAM of the whole-stack alignment — the LAST and most aggressive whole-stack pass. Trust nothing the align/critique claimed. COUNTERFACTUALLY attack the cross-folder ownership: is each shared concept on the RIGHT stratum owner; is each seam the strongest shared shape; does any consumer still under-leverage the Geometry kernel or duplicate a sibling; is any strata edge, boundary, or wire still wrong; will the next cross-folder growth axis (a new shared case/port/provider) land cleanly without touching multiple interiors? Fix every defect in place across the spanned folders; if the stack is genuinely coherent, prove it by finding nothing — never invent churn. Return verdict + aligned + residual + summary.'].join('\n')

const processFolder = async (f) => {
  const logs = {}
  for (const st of STAGES) {
    const r = await agent(st.build(f), { label: st.key + ':' + f.name, phase: 'Realize', schema: FIXLOG_SCHEMA, effort: st.effort, stallMs: 600000 })
    if (r === null) break
    logs[st.key] = r
  }
  return { folder: f.name, logs, ok: Object.keys(logs).length === STAGES.length }
}

// --- [COMPOSITION] -----------------------------------------------------------------------------
log('plan-cs-folders: ' + FOLDERS.length + ' folders, per-folder implement -> critique -> redteam, pooled at CAP=' + Math.min(CAP, FOLDERS.length))
phase('Realize')
const done = (await pool(FOLDERS, CAP, (f) => processFolder(f))).filter(Boolean)

// --- [RECONCILE] -- consume the cross-FOLDER residuals the per-folder passes deferred ----------
const allRes = []
for (const r of done) for (const st of ['implement', 'critique', 'redteam']) { const l = r.logs && r.logs[st]; if (l && l.residual_high) for (const x of l.residual_high) allRes.push({ files: (x.files && x.files.length ? x.files : [r.folder]), claim: x.claim }) }
const uniq = [...new Map(allRes.map((r) => [r.files.slice().sort().join(',') + '|' + r.claim, r])).values()]
const clusters = (() => {
  const parent = new Map(); const find = (f) => { let p = f; while (parent.get(p) !== p) p = parent.get(p); return p }; const add = (f) => { if (!parent.has(f)) parent.set(f, f) }
  for (const r of uniq) { r.files.forEach(add); for (let i = 1; i < r.files.length; i++) parent.set(find(r.files[i]), find(r.files[0])) }
  const by = new Map()
  for (const r of uniq) { const root = r.files.length ? find(r.files[0]) : '__none__'; (by.get(root) || by.set(root, []).get(root)).push(r) }
  return [...by.values()]
})()
log('Realize: ' + done.filter((r) => r.ok).length + '/' + FOLDERS.length + ' folders complete; reconcile ' + uniq.length + ' residuals -> ' + clusters.length + ' clusters')
let reconciled = []
if (clusters.length) {
  phase('Reconcile')
  reconciled = (await pool(clusters, CAP, async (cl, i) => {
    const fix = await agent([LAW, '', INTEGRATE, '', ADVERSARIAL, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', 'TASK: RECONCILE these cross-FOLDER residuals the implement/critique/redteam passes deferred. There is NO severity — treat EVERY residual as must-address. Read EVERY listed file. For each: if it is a real cross-folder defect, FIX it in place (unify the shared type/seam/rail, repair the strata/boundary issue, align a stale sibling page, or extend the shared owner in place to close a capability gap that spans folders), preserving all capability and regressing no file; if a residual is FACTUALLY INCORRECT, leave it and say why — never silently skip a real one. Residuals:\n' + JSON.stringify(cl, null, 1)].join('\n'), { label: 'reconcile-fix:' + i, phase: 'Reconcile', schema: RESIDUAL_FIX_SCHEMA, effort: 'max', stallMs: 600000 })
    if (!fix) return null
    const verify = await agent([LAW, '', BOUNDARIES, '', 'TASK: ADVERSARIAL VERIFY, one verdict per claim. Read the named files from disk and classify each residual: status "fixed" (real defect now genuinely resolved), "invalid" (the claim is factually wrong — cite why), or "open" (real defect still NOT resolved). Default "open" on any doubt; mark "invalid" ONLY when you can show the claim is wrong. Claims:\n' + JSON.stringify(cl, null, 1) + '\nFiles the fixer touched: ' + JSON.stringify(fix.files)].join('\n'), { label: 'reconcile-verify:' + i, phase: 'Reconcile', schema: RECONCILE_VERIFY_SCHEMA, effort: 'xhigh', stallMs: 600000 })
    return { cluster: cl, fix, verify }
  })).filter(Boolean)
}
const claimsAll = reconciled.flatMap((r) => (r.verify && r.verify.claims) || [])
const openClaims = new Set(claimsAll.filter((c) => c.status === 'open').map((c) => c.claim))
const hard_residual = uniq.filter((r) => openClaims.has(r.claim))
const dropped = claimsAll.filter((c) => c.status === 'invalid').map((c) => c.claim)
const integratedAll = done.flatMap((r) => Object.values(r.logs || {}).flatMap((l) => l.integrated || []))
const newPagesAll = done.flatMap((r) => Object.values(r.logs || {}).flatMap((l) => l.newPages || []))
log('Reconcile: ' + clusters.length + ' clusters; ' + hard_residual.length + ' open (hard residual), ' + dropped.length + ' dropped as invalid')

// --- [FINAL-ALIGN] -- whole-stack cross-folder 1-2-3 by one series of agents (sequential) ------
phase('Final-Align')
const ALIGN_STAGES = [
  { key: 'align', build: finalAlignPrompt, effort: 'max' },
  { key: 'critique', build: finalCritiquePrompt, effort: 'xhigh' },
  { key: 'redteam', build: finalRedteamPrompt, effort: 'max' },
]
const alignLogs = {}
for (const st of ALIGN_STAGES) {
  const r = await agent(st.build(), { label: 'final-' + st.key, phase: 'Final-Align', schema: ALIGN_SCHEMA, effort: st.effort, stallMs: 900000 })
  if (r === null) break
  alignLogs[st.key] = r
}
const alignedAll = Object.values(alignLogs).flatMap((l) => (l && l.aligned) || [])
const finalResidual = Object.values(alignLogs).flatMap((l) => (l && l.residual) || [])
log('Final-Align: ' + Object.keys(alignLogs).length + '/3 whole-stack passes; ' + alignedAll.length + ' alignments, ' + finalResidual.length + ' residual')

return { folders: FOLDERS.map((f) => f.name), complete: done.filter((r) => r.ok).length, incomplete: done.filter((r) => !r.ok).length, integrated: [...new Set(integratedAll)], newPages: [...new Set(newPagesAll)], clusters: clusters.length, hard_residual: hard_residual, dropped: dropped, finalAligned: alignedAll, finalResidual: finalResidual }
