# [PLATFORM_IDEAS]

The forward pool of higher-order platform concepts, each a card with a bracketed slug leader plus a few bullets — the capability, what it unlocks, and the gap or modern technique it draws on. An idea drives one or more `TASKLOG.md` tasks; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition.

## [1]-[OPEN]

## [2]-[CLOSED]

[SOCKET_TRANSPORT_MODALITY]: REALIZED — `realtime/transport-modality.md#TRANSPORT_MODALITY` owns the closed `TransportModality` `Data.TaggedEnum` (WebSocket/WebTransport) axis, the `dialModality` total `$match` fold yielding a per-connection `SocketDial` `{ inbound, send }` over the verified `BrowserSocket.layerWebSocketConstructor` (`Socket.makeWebSocket`/`socket.runRaw`/`socket.writer` for the WebSocket case, native `WebTransport` datagrams for the growth case), the one closed `SocketFault` `Data.TaggedEnum` (`DialFailed`/`Closed`/`WriteFailed`), and the exported `socketReconnect` `StreamPolicy`; `realtime/socket-transport.md#SOCKET_TRANSPORT` owns the `SocketTransport` `Effect.Service` that drives `dialModality`, drains `dial.inbound` into a bounded `Queue`, folds frames through `Schema.decodeUnknown`, and retries the session on `socketReconnect.reconnect` — re-exporting `SocketFault`, never re-minting the dial or a second ad-hoc `WebSocket`.

[OIDC_REDIRECT_CONTINUITY]: REALIZED — `identity-session/auth-session.md#AUTH_SESSION` owns the `arctic` mint-persist-redirect / extract-match-validate-decode round-trip (`generateState`/`generateCodeVerifier` -> the `auth-flow` `StoreDomain` `PendingFlow` record -> `createAuthorizationURLWithPKCE` -> callback `state` CSRF match -> `validateAuthorizationCode` -> `decodeIdToken` into `SessionStatus.Authenticated`), the `revokeToken` logout, and the `AuthFault` flat-`reason`-union COLLAPSE into a closed `Data.TaggedEnum` (Denied/Expired/RefreshFailed/ProtocolError) where `OAuth2RequestError.code` folds to `ProtocolError` under total dispatch.

[LOCAL_STORE_TOPOLOGY]: REALIZED — `local-persistence/local-persistence.md#LOCAL_PERSISTENCE` owns the `StoreDomain` `Schema.Literal` axis (`snapshot`/`offline-queue`/`auth-flow`/`viewpoint-cache`) mapped through one frozen `Record<StoreDomain, UseStore>` table to per-domain `idb-keyval` `createStore` named stores, the Schema-encoded `lastGood` snapshot over a `KeyValueStore.make` adapter (resolving the false `BrowserKeyValueStore`-backs-IndexedDB premise), the atomic `entries`-then-`clear` offline-queue drain, and the single-entry `pendingFlow` record the OIDC chain consumes.

[TRACE_CONTINUATION_EDGE]: REALIZED — `observability/metric-registry.md#TRACE_PROPAGATION` owns the `tracePropagation` layer registering the `CompositePropagator([W3CTraceContextPropagator, W3CBaggagePropagator])` extract-and-continue propagator on a self-constructed `@opentelemetry/sdk-trace-web` `WebTracerProvider.register({propagator})` (the in-catalogue path, since `WebSdk.Configuration` carries no propagator field and exposes no provider handle); the inject leg stays settled at `interchange/transport/transport#WIRE_TRANSPORT` and this owner mints no fresh root where an inbound parent exists.

[BROWSER_CAPABILITY_POLICY]: REALIZED — `capabilities/browser-capability.md#BROWSER_CAPABILITY` owns the one `Permissions`-backed `BrowserCapability`: the closed `CapabilityKind` `Schema.Literal` axis (`notifications`/`clipboard-read`/`clipboard-write`/`geolocation`/`persistent-storage`), the per-kind `PermissionState` `Data.TaggedEnum` in ONE `Record<CapabilityKind, SubscriptionRef<PermissionState>>` cell table, the `query`/`request` total-`Match` dispatch composing the verified `Permissions`/`Clipboard`/`Geolocation` capsules plus the native `navigator.storage.persist`/`estimate` and `Notification.requestPermission` ceremonies, the `PermissionStatus.change` fold over `scopedEventStream`, and the `storageRetained` cell `capability-rank.md` joins as a denied-storage->`OfflineOnly` health input.

[RUNTIME_STATE_SPINE]: REALIZED — the runtime-state spine landed as `runtime-composition/app-lifecycle.md` (the closed `Phase` `Data.TaggedEnum` over the merged `visibilitychange`/`pagehide`/`beforeunload` fold), `capability-rank.md` (the ordered `Rank` vocabulary under escalate-fast/recover-slow hysteresis, PROJECTS capability), `connectivity/connectivity.md` (the one `online` cell + redial edge + native `SyncManager` wake read by four concerns), and `runtime-composition/scoped-event-stream.md` (the one generic non-`WindowEventMap` `Stream.asyncScoped` bridge); the three private `visibilitychange` ingresses collapsed onto the `AppLifecycle` `transitions` axis.

[NAVIGATION_API_ROUTER]: REALIZED — `routing/app-router.md` re-founds `AppRouter` on the Baseline Navigation API: the `navigate`-event scoped interception over `window.navigation`, `event.intercept` with `scroll: "after-transition"`, and the `NavigateEvent` `signal` pending/abort state replaced the `popstate` `SubscriptionRef` + manual `pushState`/`replaceState` + scroll-offset map; ZERO router package admitted.

[VIEW_TRANSITION_FOLD]: REALIZED — `routing/app-router.md#VIEW_TRANSITION` `viewTransition` wraps the guard-admitted location commit as one `Effect.acquireRelease` over `document.startViewTransition` (the commit runs in the update callback, `ready`/`updateCallbackDone`/`finished` convert through `Effect.tryPromise`, `skipTransition()` on scope exit), gated by one `window.matchMedia("(prefers-reduced-motion: reduce)")` row; ZERO animation dependency.

[SOFT_NAV_VITALS]: REALIZED — `web-vitals/performance-budget.md` hoists the CLS/INP accumulators and the `(vital, route)` dedupe to service-level `Ref`s, resets them on each `AppRouter` `location.changes` commit, and flushes terminal CLS/INP to the `MetricRegistry` gauges plus a final breach check on the `AppLifecycle` `transitions` `Hidden`/`Draining` edge (never a private `visibilitychange` ingress).

[TRACE_CORRELATED_REPLAY]: REALIZED — `session-replay/session-recorder.md` owns the flag-gated (`RemoteConfig` `session-replay`) sampled (`RuntimeConfig.replaySampleRate`) `MutationObserver`/interaction recorder over `scopedEventStream`, the redaction-at-capture fold, the bounded `Ref<Chunk<RecordedEvent>>` ring, and the `windowId` `SubscriptionRef` `crash.report`/`web.vital.breach` spans annotate as the one `session.replay`-correlated attribute; no vendor replay SDK, no third telemetry path.

[STREAMING_FLAG_DELIVERY]: REALIZED — `feature-flags/flag-stream.md` reads the raw `text/event-stream` body of an `HttpClient` streaming request, decodes it to text through `Stream.decodeText`, and feeds each chunk into the admitted `@effect/experimental` `Sse.makeParser` frame decoder whose emitted `Event` folds through `FlagDelta` `Schema.decodeUnknown` to patch the one `RemoteConfig` `FlagSet` `SubscriptionRef` in place, while `Sse.Retry.lastEventId` seeds the `Last-Event-ID` reattach header on reconnect; the `remote-config.md` fixed-interval poll demoted to the reconnect/backfill fallback under the reused `projection` `StreamPolicy` reconnect, the two feeding one fold. The native `EventSource` was DROPPED: it owns its own frame parsing and reconnect, so it cannot host `Sse.makeParser` (which reads the raw `data:`/`event:`/`id:`/`retry:` byte stream) — the raw-body ingress is the same discipline `services` `agent/mcp-transport` consumes, the parser a transport-decode mechanic, not a second flag source.

[OPENFEATURE_PROVIDER_SHAPE]: DROPPED — a thin `resolveBooleanEvaluation`-style projection of the existing total `Match` dispatch is a task-sized shaping anchored on one named external standard, below the higher-order-folder-idea bar; the OpenFeature reason/variant alignment, if it earns admission, is one growth row on `remote-config`'s existing `evaluate`, not a folder idea.
