# [PY_GEOMETRY_TASKLOG]

Open and closed work for `geometry`, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` open; `[COMPLETE]` or `[DROPPED]` closed — and carries the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations. `[1]-[OPEN]` holds live work; `[2]-[CLOSED]` records finished or dropped tasks.

## [1]-[OPEN]

[BLOCKED] Add the IFC-bytes non-manifold topology constructor — from the `graph/nonmanifold.md` `IFC_BYTES_CONSTRUCTOR` residual. Blocked-gated on a `topologicpy` companion-interpreter assay confirming the `Topology.ByIFCFile(file)` constructor arity; the sharpened RESEARCH gate is landed, the arm fence waits.
- Resolve the `Topology.ByIFCFile(file)` constructor arity (does it accept an `ifcopenshell.file` object or strictly a path string? the catalogue's bare `(file)` spelling is ambiguous) against the three-path resolution authored at `nonmanifold.md#3-IFC_BYTES_CONSTRUCTOR`: (1) hand the `ifcopenshell.open`-over-`guess_format` model object straight in if `ByIFCFile` accepts it; (2) the `IFC` integration facade's model-object entry; (3) the `Topology.ByOCCTShape(shape)` handle bridge. The temp-file detour is the FORBIDDEN form; the IFC-bytes arm is ONE `TopologyOp.CONSTRUCT` growth on the existing owner, never a new owner.
- Integrate `topologicpy` (`Topology.ByIFCFile`/`ByIFCPath`/`ByOCCTShape` plus the `IFC` facade), composing `ifcopenshell.open` over `guess_format` for the in-memory model. Secondary gate: `topologicpy` is manifest-absent (consumed by `nonmanifold.md` AND `graph/algebra.md`), so the `<'3.15'` companion-band row must land alongside `ifcopenshell` before the arm resolves.
- The arm lands inside `graph/nonmanifold.md`'s `CONSTRUCT` case; the boundary stays the non-manifold cell/aperture analysis the C# `IfcSemanticModel` does not extract, never the in-process BIM space-graph.
- Close-condition: a branch `topologicpy` companion-interpreter `assay api` reflection confirms one of the three constructor arities (post-approval runtime-action band); until then the IFC ingestion path stays the marked `nonmanifold#3` RESEARCH gate.

[BLOCKED] Hand the geometry CPU kernels to the runtime offload lane — trickle-down from `python:GEOMETRY_KERNEL_OFFLOAD_LANE`. Sequence-after the runtime `execution/lanes#LANES` `LanePolicy` CPU-offload variant; the five geometry owner fences carry the gated one-call hand-off seam, the call settles once the branch lane lands.
- Each geometry owner running a heavy CPU kernel (the `registration.py` ICP/global loops, the `mesh/daemon.py` OCCT iteration, the `ingestion.py` `pdal` filter graph, the `reconstruction.py` open3d reconstruction, the `mesh-utility` `manifold3d` boolean) hands its kernel to the runtime `execution/lanes` `anyio.to_interpreter.run_sync` CPU-offload `LanePolicy` variant under the one `CapacityLimiter` and `DrainReceipt`, the lane never importing the kernel.
- Integrate the runtime `execution/lanes` `LanePolicy` CPU-offload variant (branch-owned, `anyio.to_interpreter.run_sync` with `to_process.run_sync` fallback); no geometry-side package admission.
- The lane owner stays branch-side; geometry is the consumer that hands its kernels across the boundary, never a second concurrency surface in geometry; the offloaded callable is picklable on the `to_process` fallback path. The `daemon.md#DAEMON`, `registration.md#REGISTRATION`, and `repair.md#MESH` Growth bullets already carry the gated hand-off seam; `ingestion.md`/`reconstruction.md` carry it as the two remaining consumers.
- Close-condition: the runtime `execution/lanes#LANES` `LanePolicy` CPU-offload variant lands on the branch runtime owner; the geometry-side realization is then a uniform one-call growth across the five owner fences.

## [2]-[CLOSED]

[COMPLETE] `scan/ingestion.md` — the `ScanIngestion` raw-scan preprocessing pipeline owner. One `IngestStage`-discriminated `pdal` `Reader | Filter | Writer` stage graph (`filters.smrf`/`filters.pmf` ground classify, `filters.outlier` removal, `filters.decimation`/`filters.voxeldownsize`, `filters.range` crop) over `Pipeline.execute`/`arrays`, plus the `ScanSource`-discriminated `pye57` E57 structured-scan-plus-pose path, folding cleaned structured arrays into one registration-ready `o3d.t.geometry.PointCloud` `registration.py` consumes. The inbound boundary is the data-branch `data/spatial/mesh#POINTCLOUD` `pyarrow.Table` (never a pdal `Pipeline` object across the seam, never re-owning `laspy` LAS decode); `pdal`/`pye57` ride the `<'3.15'` companion band (function-local import), and the `open3d` egress shares the manifest-admission delta with `reconstruction.md`. Resolves the branch-flagged `pye57`-catalogue-present-consumer-absent gap.

[COMPLETE] `scan/reconstruction.md` — the `ScanReconstruction` registered-cloud-to-watertight-mesh owner, the producer of the `reconstructed-mesh` graduation subject the compute union already declared. The shared `estimate_normals` + `orient_normals_consistent_tangent_plane` pre-step, then a `match` over `ReconstructionMethod` selecting the STATIC open3d constructor (`create_from_point_cloud_poisson` watertight default, `ball_pivoting` detail, `alpha_shape` concave hull — a constructor choice per `open3d.md#105`, never a runtime flag), `cluster_dbscan` optional multi-object split, GLB export, and a route to `mesh/repair.md#MESH` `MeshOp.Repair` for watertight conditioning (never re-implementing repair). Disambiguated from the compute `analysis/spatial.md#SPATIAL` "reconstructed boundary, never crossing as a geometry-branch mesh" same-axis subject; `open3d` admits the shared manifest row; `create_from_point_cloud_alpha_shape` is a folder-`.api` member row (`open3d.md` row [15]), the `reconstruction#3` note narrowed to the optional `tetra_mesh`/`pt_map` reuse overload.

[COMPLETE] `scan/deviation.md` deepened — the signed scan-vs-model deviation owner, both open residuals resolved. The `DEVIATE` arm folds `trimesh.proximity.signed_distance` (positive inside the watertight design solid, negative outside) into under-build (`signed.max`)/over-build (`signed.min`) extrema and the absolute `max`/`mean`/`std` band against tolerance for the construction-verification verdict, composing `proximity.closest_point` triangle ids only for the colored-overlay egress (`deviation#3-SIGNED_BAND_REDUCTION`); the `SEGMENT` arm iterates `segment_plane`/`select_by_index` oversegmentation. The `scan-deviation` `GeometrySubject` literal admits as ONE new row on the compute `graduation/handoff.md#GRADUATION` owner (cross-folder seam, synthesis-reconciled, never authored into the compute interior), distinct from the present `reconstructed-mesh` and the compute spatial `reconstructed-boundary` subjects (`deviation#4-GRADUATION_SUBJECT`). The offload-lane hand-off seam is noted in the Growth bullet (gated on the runtime lane).

[COMPLETE] `ifc/costing.md` — the `IfcLifecycle` 5D/4D model-lifecycle owner. One `LifecyclePhase`-discriminated `@staticmethod` capsule folds `QUANTITY` (`ifc5d.qto.quantify`/`edit_qtos` keyed by the `qto.rules` `RULE_SET` table, superseding the deleted `analysis.py` `NetFloorArea` fold), `COST` (`ifcopenshell.api.cost.calculate_cost_item_resource_value` per-item rollup plus the `CostReport` `ifc5d.ifc5Dspreadsheet.Ifc5DCsvWriter`/`Ifc5DOdsWriter` export), `SCHEDULE` (the `ifc4d` `ScheduleFormat` `<Format>2Ifc` named parsers populating `IfcWorkSchedule`/`IfcTask`/`IfcRelSequence`), `PATCH` (`ifcpatch.execute`/`write` over the `recipes` namespace), and `DIFF` (`ifcdiff.IfcDiff(old, new).diff()` over `deepdiff`), each folding a `LifecycleReceipt` through the geometry case under one `boundary(f"lifecycle.{phase}")` admission. The `0.8.5` siblings ride the `ifcopenshell` companion lane (function-local boundary-scope import) and return as one manifest-admission batch with `ifcopenshell` (delta to the orchestrator). Residuals `costing#3`: the `qto.rules` `RULE_SET` literal set, the `IfcCostItem.CostValues` `AppliedValue` measure shape + writer contract, the `<Format>2Ifc` slot-population semantics, and the `ifcpatch.write` output-type dispatch.

[COMPLETE] `mesh/cad.md` — the `StepBridge` CAD-STEP tessellation hop. STEP/IGES bytes plus tolerance into per-element GLB over `cadquery-ocp` (`STEPCAFControl_Reader`/`IGESCAFControl_Reader` → XCAF `TDocStd_Document` → `BRepMesh_IncrementalMesh` → native `RWGltf_CafWriter`), returning raw GLB the `mesh/daemon.md` CAD arms key through the shared `ContentIdentity`. The `pythonocc-core` conda-only blocker is resolved by `cadquery-ocp` (cp310-cp314 wheels, `<'3.15'` companion band — `cad#4-WHEEL_BAND`); the XCAF root-shape enumeration and the `RWGltf_CafWriter.Perform` `fileInfo` map type are the residual `cad#3-RESEARCH` members. Aligned at the wire to the C# `StepIso10303` codec.

[COMPLETE] `scan/registration.md` deepened to the full modern stack. The `RegistrationMode`-discriminated owner carries `GLOBAL` (`kiss_matcher.KISSMatcher.estimate` initialization-free Faster-PFH/ROBIN/GNC), `MULTISCALE` (tensor `multi_scale_icp` + robust kernels), `COLORED_ICP`, `VGICP` (`small_gicp.align`), and `MULTIWAY` (`global_optimization` over `kiss_matcher` pairwise edges). `kiss_matcher` (manifest, `<'3.13'` band) closes the under-utilization where the admitted global matcher was unused; the `kiss_matcher` array-overload dtype and `open3d` `global_optimization` triple are the residual `registration#3-RESEARCH` members.

[COMPLETE] `mesh/repair.md` — the `MeshOp` robust repair/boolean/codec owner. Watertight/winding/normal/hole-fill over `trimesh.repair`, exact CSG over the `trimesh.boolean` `manifold3d` backend (`BooleanOp` row), and mesh-file decode/encode across `trimesh`/`rhino3dm`/`meshio` (`MeshFormat` row), as one `@tagged_union`. Consumed by `tessellation`, `scan-registration` reconstruction, and `step-bridge`; `ifc-analysis` clash-volume composes the `Boolean(meshes, INTERSECTION)` arm. `manifold3d` rides the `<'3.15'` companion band (`repair#4-WHEEL_BAND`); the `trimesh.util.wrap_as_stream` helper and cross-format `Codec` re-encode are the residual `repair#3-RESEARCH` members.

[COMPLETE] `ifc/analysis.md` IDS/clash/BCF triad realized against the native ecosystem. IDS over `ifctester` (`ids.open`/`Ids.validate`/`Specification.status`, `reporter.Bcf` export), clash over `ifcclash` (`Clasher(ClashSettings())`/`ClashSet`/`ClashSource`/`ClashResult`), BCF round-trip over `bcf-client` (`bcf.v3.bcfxml.BcfXml.create_new`/`add_topic`/`get_topics`/`TopicHandler`). The hand-rolled IDS rule fold and OCCT-overlap clash are the deleted forms; the triad is catalogued in the folder `.api` (`bcf-client` pure-Python wheel, `ifctester`/`ifcclash` companion lane). Residual `analysis#3-RESEARCH`: the `Clasher.clash()` internal `clashes` dict timing and the `TopicHandler.topic.title` field spelling. The `QUANTITY` quantity arm is superseded by `costing.md`'s `ifc5d` rule-driven take-off.

[COMPLETE] `ifc/selector.md` — the `IfcSelector` validated selection-grammar owner. One `SelectorGrammar` `lark` EBNF over the five IFC selection axes (entity class, attribute predicate, pset/property predicate, classification reference, spatial containment), one `SelectorTransformer` (`@v_args(inline=True)`, one method per rule, bottom-up fold) producing a frozen `SelectorQuery`, and `IfcSelector.parse` lifting `UnexpectedInput` into the runtime fault rail once via `boundary("selector.parse", ...)`; the validated query re-serializes to the `filter_elements` string `ifcopenshell` consumes (`IfcSelector.filter` binds the parse rail into `util.selector.filter_elements`), no second selection engine, no raw-string passthrough. `lark` is manifest-present (pure-Python cp315-clean, no companion gate — `selector#3-RESEARCH` `TRANSFORMER_FOLD_BINDING`); the published `filter_elements` query-syntax terminal trace is the residual `selector#3-RESEARCH` `SELECTOR_FILTER_GRAMMAR` member confirming against live `ifcopenshell.util.selector`. Reconciles the `analysis.md` `IDS_SELECTOR_GRAMMAR` residual without duplicating it; the analysis/costing selector arms consume the validated query.
