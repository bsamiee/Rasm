# T06 notes: Source Generator Configuration

Lab: `.scratch/functional-csharp/lab-functional-csharp/T06`, namespace `Lab.T06`. Build: zero diagnostics. Run: exit 0, every check in `Samples.Run()` passes. Doc code and lab code are the same text: one lab file per doc section, generated from the fences by a sync script, plus `Samples.cs` as scaffolding.


## Verified identifiers

- `ThinktectureRuntimeExtensions_SourceGenerator_LogFilePath` (decomp:sourcegenerator `GetLoggingOptions`, package `build/Thinktecture.Runtime.Extensions.props`, probe)
- `ThinktectureRuntimeExtensions_SourceGenerator_LogFilePathMustBeUnique`, default `true`, `bool.TryParse` (decomp:sourcegenerator, props, probe: `false` yields `gen.txt`)
- `ThinktectureRuntimeExtensions_SourceGenerator_LogLevel`, default `Warning`, `Enum.TryParse` ignoreCase (decomp:sourcegenerator `GetLoggingOptions`, props, probe: `information` lowercase accepted, `Bogus` yields no file like the default)
- `ThinktectureRuntimeExtensions_SourceGenerator_LogMessageInitialBufferSize`, default `100`, minimum `100`, invariant `int.TryParse` (decomp:sourcegenerator, props; wiki-absent; not probed, unobservable from outside)
- `ThinktectureRuntimeExtensions_SourceGenerator_GenerateJetBrainsAnnotations`, `IsFeatureDisabled`: `disable`/`disabled`/`false`/`0` (decomp:sourcegenerator `GetGeneratorOptions`, props, probe)
- `ThinktectureRuntimeExtensions_SourceGenerator_Counter`, `IsFeatureEnabled`: `enable`/`enabled`/`true`/`1` (decomp:sourcegenerator, props; wiki-absent; probe with `true` and `enabled`)
- `CompilerVisibleProperty` items, six, in `build/Thinktecture.Runtime.Extensions.props` of the core package (package file)
- `build_property.<Name>` keys read from `AnalyzerConfigOptionsProvider.GlobalOptions` (decomp:sourcegenerator `GetGeneratorOptions`, `GetLoggingOptions`)
- `LogLevel` enum members `Trace, Debug, Information, Warning, Error, None` in that order (decomp:sourcegenerator)
- `LoggerFactory.CreateLogger` creates `InformationLogger`, `WarningLogger`, `ErrorLogger` for those three levels and `SelfLogErrorLogger` otherwise (decomp:sourcegenerator; probe: `Trace`, `Debug`, `None` produce no file while `Information` does)
- `LoggerBase.IsEnabled`: level at or above minimum and below `None` (decomp:sourcegenerator)
- `GetFileInfo`: existing file, existing directory, missing file in existing directory, missing directory with `SelfLog` line `Log folder path does not exist. Checked paths: '...' and '...'.` (decomp:sourcegenerator, probe with `UseSharedCompilation=false`)
- `CreateLogFileOrNull`: unique `{name}_{yyyy}{MM}{dd}_{HH}{mm}{ss}_{guid:N}{ext}` from `DateTime.UtcNow`, default name `ThinktectureRuntimeExtensions_logs`, default extension `.log` (decomp:sourcegenerator; probe: `gen_<utc>_<guid>.txt`, `gen.txt`, `ThinktectureRuntimeExtensions_logs_<utc>_<guid>.log`)
- `FileSystemLoggingSink.TryCreateWriter`: `File.Open(OpenOrCreate, Write, ReadWrite | Delete)`, opened lazily on the first queued message (decomp:sourcegenerator; probe: default level creates no file on a clean build)
- Log line format `[yyyy-MM-dd HH:mm:ss:fff | Level] [Source] Message` with UTF-8 BOM, local `DateTime.Now` (decomp:sourcegenerator `WriteInternalAsync`, `WriteDateTimeAsync`; probe log file)
- `SelfLog` file `ThinktectureRuntimeExtensionsSourceGenerator.log` under `Path.GetTempPath()`, append, `_dontWrite` after first exception, line `[<O timestamp>] <message>` (decomp:sourcegenerator; probe)
- `WriteInternalAsync` mirrors every `Error` item into `SelfLog` (decomp:sourcegenerator)
- `FileSystemSinkProvider` keys sinks by `(fullPath, filePathMustBeUnique)` and releases them when no generator owns them (decomp:sourcegenerator)
- `AnnotationsSourceGenerator` adds `Thinktecture.Annotations.g.cs` when no source class `InstantHandleAttribute` in `JetBrains.Annotations`, no metadata module `JetBrains.Annotations.dll`, and the option is on (decomp:sourcegenerator; probe: file present by default, absent with `false`, absent with the `JetBrains.Annotations` package referenced)
- Generated attribute: `internal sealed class InstantHandleAttribute : Attribute`, `[AttributeUsage(AttributeTargets.Parameter)]`, property `RequireAwait` (decomp:sourcegenerator `AddAnnotations`; probe generated file)
- `Switch`, `SwitchPartially`, `Map` emit `[global::JetBrains.Annotations.InstantHandleAttribute]` on every delegate parameter inside `#pragma warning disable CS0436` / `restore` (decomp:sourcegenerator; probe generated `Switch`)
- `GenerateJetBrainsAnnotations=false` without the package fails with `CS0122: 'InstantHandleAttribute' is inaccessible due to its protection level`, one per delegate parameter (probe; the core assembly holds an internal copy, decomp:core)
- `GenerateJetBrainsAnnotations=false` with the `JetBrains.Annotations` package referenced compiles (probe)
- Counter header `// COUNTER: <n padded 8>` followed by a blank line, `Interlocked.Increment` on a static counter, one header per generated file (decomp:sourcegenerator `GenerateCode`; probe)

## Dropped statements

- Wiki: `Information` is the default `LogLevel`. Decompile `GetLoggingOptions` falls back to `LogLevel.Warning`, and the probe without `LogLevel` produced no file while `Information` produced one. The file states `Warning`.
- Wiki: `Trace` and `Debug` are selectable log levels. The decompile routes them to `SelfLogErrorLogger`, and the probe produced no file for either. The file states that only `Information`, `Warning`, `Error` create a file logger.
- Wiki: the log file is created when logging is activated. The sink opens the file on the first written message, so a clean build at `Warning` creates no file. The file states the lazy creation.
- Wiki: the self-log is written when the logger throws. The decompile also writes it for a missing log folder and mirrors every `Error` message. The file states all three triggers.
- No existing artifact for this lane exists under `REPO/.scratch/thinktecture/`, so nothing else was dropped.

## Host-only blocks

None. The file has one `xml` fence and one `text` fence and no `csharp` fence.

## Open questions

- The self-log for the missing-folder case did not appear under the caller's temp folder when the compiler server hosted the generator. It appeared with `UseSharedCompilation=false`. The server's temp folder was not located. The file states the process-dependent location without naming a path.
- `LogMessageInitialBufferSize` has no observable effect from outside the process. Its default and minimum come from the decompile alone.
- Whether the wiki's `Trace`/`Debug` levels are a documentation lag or a regression in the generator is unknown.

## Lab rulings

- Lab `LabT06.csproj` under `.scratch/lab-t06` with `RootNamespace` `Lab`, generator properties in a `PropertyGroup Label="Thinktecture Generator"`: `dotnet build` exit 0, zero warnings, `dotnet run --no-build` exit 0.
- Property names that worked in the csproj: `ThinktectureRuntimeExtensions_SourceGenerator_LogFilePath` (`$(MSBuildProjectDirectory)/logs/generator.txt`), `ThinktectureRuntimeExtensions_SourceGenerator_LogLevel` (`Information`), `ThinktectureRuntimeExtensions_SourceGenerator_LogFilePathMustBeUnique` (`true`), `ThinktectureRuntimeExtensions_SourceGenerator_GenerateJetBrainsAnnotations` (`true`).
- Log file name pattern observed with `LogFilePathMustBeUnique` at `true`: `generator_<yyyyMMdd>_<HHmmss>_<32 hex guid>.txt`, in the `logs` folder that existed before the build.
- Log content: three `Information` lines, one per generator (`ObjectFactorySourceGenerator`, `SmartEnumSourceGenerator`, `ValueObjectSourceGenerator`), each `Code generator for System.Text.Json will participate in code generation`. MessagePack and Newtonsoft.Json lines are absent because the lab references neither package. The doc gained one sentence on this.
- Lab types: `[SmartEnum<string>] ShippingMethod`, `[Union<int, string>] IdOrName`, checked through `Switch` with `static` lambdas. The `Switch` parameter names are the camel-cased item names (`standard`, `express`) and the type-derived names (`int32`, `@string`).
- The doc holds no `csharp` fence, so the lab forced no correction to a code block. No analyzer diagnostic fired on the generated code with the annotation on.

## Merge

Review B items 1-22 applied in full. No A/B disagreement required a ruling. The `xml` fence dropped the JetBrains element, the `text` fence shows the line format alone.

## Shared lab probe

The four generator properties were set in the shared `Lab.csproj`, the build stayed green under the repo analyzers, one file `generator_<yyyyMMdd>_<HHmmss>_<guid>.txt` appeared with three `Information` lines (one per logging generator, System.Text.Json alone), and the properties were removed again.
