// attribution.text, the model's commit or pull request text (AttributionTextInput, AttributionTextResult) by AttributionTextKind, no kind row yet

// --- [IMPORTS] -------------------------------------------------------------------------

import type { On } from 'claude-code';
import type { Options } from '../host/options.ts';

// --- [REGISTRATION] --------------------------------------------------------------------

const attributionText = (_on: On, _options: Options): void => undefined;

// --- [EXPORTS] -------------------------------------------------------------------------

export { attributionText };
