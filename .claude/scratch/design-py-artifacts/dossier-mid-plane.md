# DOSSIER — mid-plane (visualization / scene / composition / document)

Read on disk, fully: `visualization/{table, chart/spec, chart/export, chart/transform, diagram/draw, diagram/glyphset, diagram/layout}`, `scene/{render, stage, export}`, `composition/{compose, imposition, sheet}`, `document/{emit, model, report, egress, lens, tagged}`. 19 pages. Register re-verified on disk; `.api/vtk.md`/`.api/pyvista.md` grepped for E11; E13 census run across the lane.

The mid-plane splits cleanly into TWO grades. The chart axis, the scene scientific-viz owners, model.md, tagged.md, egress.md, lens.md, and the imposition/sheet press machinery are DEEP, 8-9.5-grade pages that mine their packages hard. The AEC-diagram machinery (V15), the typography seam (V3/E7), the color rehoming (E2/V4), the entry contract (E1/V6), the press-fold + Pdf.from_html + egress-imposition duplications (E6/V7), and the whole E13 track-law layer are UNMET — the rebuild deepened the general/technical capability but did NOT land the AEC-documentation plane the telos demands. Every register row assigned survives on disk except one sub-anchor (E3 model.md, E11 stage collapse-premise).

---

## [REGISTER VERDICTS] (assigned rows + every register anchor in the lane)

### E1 — Entry fiction — HOLD
- `emit.md:538` `async def produced(...) -> RuntimeRail[Block[ContentKey]]` — CONFIRMED, exact anchor. The `produced` parallel batch rail is live.
- `report.md:809` `async def rendered(...) -> RuntimeRail[Block[ContentKey]]` — CONFIRMED, exact anchor (method at :809, prose at :15). The `rendered` parallel batch rail is live.
- Neither `produced` nor `rendered` is on the `emit() -> ArtifactWork` contract; both are `Block[ContentKey]` fan-out rails V6 says must die. `core/format`'s `_fanned` is the third (foundational, out of lane).
- Extension: NO mid-plane producer exposes `emit()`. `Sheet.of`/`contribute` (sheet.md:531/540), `Imposition.of`/`contribute` (imposition.md:509/532), `Figure.of`/`contribute` (compose.md:387/414), `Scene3d.render` (render.md:431), `DiagramDraw.render` (draw.md:97), `DiagramLayout.assign` (layout.md:203), `TablePlan.render` (table.md:664), `ChartExport.render` (export.md:247) are the six live carrier shapes E1 names. Prose at `compose.md:13` and `imposition.md:7` claims "every figure/imposition routes through the single `core/plan#PLAN` `ArtifactPipeline` production entry as a producer node" — UNBACKED by any `emit()` fence or `ArtifactPipeline` construction: this is E1's "single production entry is fiction" surfacing as prose-vs-fence drift.

### E5 — Broken paths (the table render form) — HOLD
- `table.md` `build()` (`:682-704`) constructs great-tables through the polars `.style` seam (`shaped.style.with_id(ident)`, `:687`) or `GT(shaped, ...)` (`:689`) — NEVER `GT(frame.to_pandas())`. V10's ruling is explicit: `GT(frame.to_pandas())` is the safe floor, the polars style path "stays a probed capability, never the default." The page has it INVERTED — polars `.style` is the default (`_seam` property `:677-680`), the pandas floor is absent entirely.
- The `Stub(group=)` (`:394`) + `Summary`/`GrandSummary` (`:561`/`:571`) → `tab_stub(groupname_col=)`/`summary_rows`/`grand_summary_rows` fold (`_fold` `:865/927/929`) is the exact row-group/subtotal render form E5 records as verified-broken on the polars backend. The owner's render path routes it through the broken polars seam; no `[03]-[RESEARCH]` row proves the polars group/summary render works on great-tables 0.22. The E5 defect is UNADDRESSED in the owner.

### E6 — Duplication (press fold + Pdf.from_html + egress algebra) — HOLD
- **Press fold**: `_mint_groups`/`_draw_one`/`_oc_state`/`_configure_layers` are implemented INDEPENDENTLY and near-identically in `sheet.md` (`:734/742/771/775`) and `imposition.md` (`:622/631/652/656`) — same names, same pymupdf `add_ocg`+`set_ocmd`+`set_layer` OCG/OCMD-binding-and-serialize mechanism. `sheet.md:698` carries a clone-admission comment ("mirroring the sibling imposition `Marks.finished`"); both pages cross-reference "exactly as the sibling `_configure_layers` does." V7's "ONE owner (imposition) keeps the fold; sheet and egress compose it" is NOT executed — the mechanism is cloned across three owners. (Anchor drift: E6 said `sheet.md:398,669-703` / `imposition.md:291-299`; the fold is now at `sheet.md:734-785` / `imposition.md:622-665`.)
- **Two `Pdf.from_html` roots**: `emit.md:764` (`PDF_OXIDE` arm, `_pdf_oxide_create`, `Pdf.from_html(to_html(plan.node), ...)`) ↔ `report.md:746` (`_AUTHOR_BUILD[AuthorSource.HTML]`, `Pdf.from_html(spec.source, ...)`) — BOTH exact anchors HOLD. V12's "the duplicate `Pdf.from_html` root dies" is NOT done; worse, report EXPANDED terminal-PDF authoring (added `ReportKind.AUTHOR` `report.md:90` + `ReportKind.REFLOW` `:89`, both minting terminal PDF bytes inside the tree-composer V12 says should route through emit). Report defends them (`:20` "REFLOW is the directional inverse of lens STORY; AUTHOR is the commercial-safe REFLOW peer") but the raw duplication of `Pdf.from_html` stands.
- **Egress imposition algebra**: `egress.md` `Imposition.order` (`:269-276`) computes a BOOKLET saddle-stitch page order (`:275` `(padded-1-2*i, 2*i, 2*i+1, padded-2-2*i)`) and `_impose` (`:623-653`) folds pypdf `Transformation`+`merge_page` — duplicating `imposition.md`'s `_saddle` (`:401-404`) / `_folded` (`:412-426`) centerfold algebra. INTERNAL CONTRADICTION: `imposition.md:887` [EGRESS_DISTINCT] claims "egress computes no booklet, signature, work-and-turn, creep, or verso rotation" — FALSIFIED by `egress.md`'s own `ImposeLayout.BOOKLET` (`:99`) + saddle fold (`:275`). Both pages compute booklet saddle order.

### E7 — Unwired seams (emit←shape/layout) — HOLD
- `ARCH:153/156` (`emit←shape/layout`): `emit.md` imports ONLY `artifacts.document.model` (`:40-69`); NO `artifacts.typography.shape` / `artifacts.typography.layout` import anywhere in the fence. The declared seam is composed in ZERO arms. The reportlab `PDF_AUTHOR` arm re-derives text through `_run_markup` (`:637-651`, weight/italic/color/decoration only) and `setFont("Helvetica", 8)` (`:718`) — it drops shaping, bidi, script itemization, and kerning-correct placement. V3's "reportlab text placement re-derives metrics and drops bidi" is exactly this.

### E11 — Scene seams — HOLD (one sub-premise REFUTED)
- `render.md:38` cycle-admission comment "cyclically coupled peer (export imports render) — deferred to break the cycle" — CONFIRMED exact anchor (`lazy from artifacts.scene.export import ...` `:34-38`). The render⇄export cycle is admitted, not broken (V9 wants `SceneTarget` hoisted).
- `render.md:14` key+receipt tuple carrier — CONFIRMED. `Scene3d.render -> RuntimeRail[tuple[ContentKey, ArtifactReceipt]]` (`:431`); every arm returns `(key, ArtifactReceipt.Scene(...))`. The Receipt bullet at `:14` describes it. This is one of the six E1 carriers V6 collapses.
- `scene/stage.md` composed by no page outside `scene/` — HOLD (imported only by `export.md:33`, `render.md:34`). But the "498 lines serving one flat-mesh caller" collapse-candidate premise is REFUTED: stage.md is now a 488-LOC deep USD authoring owner (`PrimKind` recursive scene-graph, `PointInstancer`, `Camera`, `Material` PBR graph, `UsdSemantics.LabelsAPI`, `BBoxCache` extent), NOT a thin flat-mesh page. V9's stage-collapse framing is falsified by the rebuild; the composed-only-within-scene fact holds.
- `vtkPolyDataSilhouette`/`vtkFeatureEdges`/`vtkGL2PSExporter` absent from `.api/vtk.md` AND `.api/pyvista.md` — CONFIRMED via `rg` (NO MATCHES in either tier). The scene plane's `FieldFilter` (20 cases, `render.md:151-313`) has NO silhouette/feature-edge/hidden-line case; `SceneTarget` (`export.md:44-52`) has NO GL2PS vector target (PNG/GLTF/VRML/OBJ/HTML/USD/USDZ only). V9's silhouette/feature-edge/GL2PS trio is uncatalogued AND unbuilt.

### E12 — Output-grade gaps — HOLD (comprehensively)
- `draw.md:171,248,305` kind-blind glyph dispatch — CONFIRMED (`_style_of` `:170`, `_lower` `:248`, `_lower_drawio` `:305` dispatch on glyph `tag`, never `DiagramKind`). Kind-awareness moved to `layout.md:_emit`, but the AEC geometry it emits is generic (see V15 below).
- `glyphset.md:16` closed five-mark law — CONFIRMED (`:16` Growth: "the five marks cover every data-driven diagram ... never a new glyph type"; no polygon/area case exists — `DiagramGlyph` cases are Node/Edge/Swimlane/Annotation/Marker `:152-159`).
- `layout.md:180,188` CIRCULATION/SECTION_CALLOUT spring defaults — CONFIRMED exact anchors (`_KIND_POLICY` `:180` `CIRCULATION: LayoutPolicy.Force()`, `:188` `SECTION_CALLOUT: LayoutPolicy.Force()`). V15's "spring defaults die for plan-anchored placement" undone.
- no solar computation corpus-wide — CONFIRMED. `_solar_arc` (`:451-454`) projects CALLER-SUPPLIED `_attr(data, "azimuth")`/`_attr(data, "altitude")` stereographically; zero date/latitude → azimuth/altitude kernel; no `pvlib` import anywhere in `layout.md`. Azimuth rides only as a supplied column (`_emit` SUN_PATH `:252`).
- `render.md:130` parameterless `enable_shadows` — CONFIRMED (`_FEATURE[RenderFeature.SHADOWS] = lambda plotter: plotter.enable_shadows()` `:130`; no directional sun/date/latitude light policy).
- `render.md:349-352,408-412` untyped `dataset`/`mesh`/`grid: object` ingress — DRIFT on anchor (page rebuilt), HOLD on defect: `SceneOp.image: tuple[object, RenderSpec]` (`:401`), `render_plotter(grid: object, ...)` (`:501`), `render_image(grid: object, ...)` (`:560`). V9's typed mesh-ingress contract (geometry `mesh` / data `MeshPayload`) is absent; ingress is still bare `object`.
- `sheet.md:287` one `TitleBlock` — CONFIRMED (`class TitleBlock` `:287`, one shape). `:77-80` engine-only variants — CONFIRMED (`Engine` StrEnum `:77-81`; title-block varied only by render engine). Zero cover-sheet concept — CONFIRMED (`SheetOp` cases Frame/Place/Fill/Stamp/Preview `:451`; no cover-sheet producer; `SheetSet` `:558` has numbered/audited/frame/registered/scheduled, no cover). `:851,1007` emit-as-analogy — CONFIRMED (`:851` "exactly as document/emit's PDF_AUTHOR afterFlowable mints bookmarkPage+addOutlineEntry"; `:1007` "mirroring document/emit#DOCUMENT's PDF_AUTHOR"). The sheet←emit general-notes typesetting seam is prose analogy, never a call.
- `emit.md:713-719` static running head — CONFIRMED (`_title_block` `paint` `:715-723` paints one static `spec.header_text or spec.title` string; weasyprint `@page` margin-box/`string-set` running content unmined, absent from `_weasyprint_html` `:740-758`). `:732` hardcoded `Frame` — CONFIRMED (`Frame(20*mm, 18*mm, ...)` `:732`; furniture literals `20*mm`/`12*mm` `:719-722`). `:355,696` lone TOC arm — CONFIRMED (page-numbered TOC only in `_SheetDocTemplate.afterFlowable` multiBuild `:701-709/736`; weasyprint/typst/pdf_oxide arms carry no cross-arm TOC). `:750` `pdf_forms` dead — CONFIRMED (`pdf_forms=spec.forms` `:750` threaded, but no HTML lowering emits `<input>`/`<form>` markup; `FieldNode`→AcroForm exists only in `PDF_UA` `_ua_field` `:768`).
- zero corpus hits: type scale — CONFIRMED (no type-scale ratios; `GlyphStyle.text: TextRun` `glyphset.md:149` is a dead carrier — draw hardcodes label sizes `10.0/8.0/9.0` `draw.md:258/270/274`). overprint — CONFIRMED (no overprint rows). TrimBox/BleedBox authored nowhere — CONFIRMED (`sheet.md:1009` [CROPMARKS_ACROFORM_DEFERRED] explicitly defers cropmarks/media-box; no MediaBox/TrimBox/BleedBox/CropBox authoring in the corpus).

### E2 — Color orphan — HOLD
- `chart/spec.md:30` `type Palette = NDArray[np.float64]` — CONFIRMED. `:128` `def hex_ramp(palette: Palette) -> list[str]` — CONFIRMED. The de-facto palette owner is STILL `chart/spec`: `Palette` + `hex_ramp` minted locally, NO `graphic.color.derive` import in the fence prelude (`:17-27`). Imported by `draw.md:37`, `chart/export.md:36`. Prose at `spec.md:3/12` claims "Color arrives from `graphic/color/derive#DERIVE` as the `ColorReceipt.coords` palette array" — but the fence never imports derive, and `ColorReceipt` is the parallel receipt rail V4 collapses. Prose-vs-fence split.
- `scene/render.md:45` third alias — CONFIRMED: `type Palette = str | tuple[str, ...]` (a THIRD distinct Palette alias). V4's "scene's third alias dies" undone.

### E3 — Dual text engine — HOLD (draw); model sub-anchor REFUTED
- `draw.md:114` (`ziafont.config.precision`/`svg2`), `:186-191` (`_label` `ziamath.Text(...)` for `$math$` else `face.text(...)`) — CONFIRMED. draw.md hand-builds ziafont/ziamath directly (`import ziafont` `:26`, `lazy import ziamath` `:40`); it does NOT route through a typography/math owner or `typography/shape` shaping. V3's "entire drawing/diagram annotation plane runs on ad-hoc ziafont/ziamath calls" holds for the diagram plane.
- `document/model.md:319-323` — REFUTED. Current `FormulaNode` (`:319-326`) is a clean tree-resident LaTeX `tex` source carrier lowering via Typst `mitex`/`mi` (`_typst_body` `:683-688`), NOT a ziamath hand-build. The FormulaNode rebuild resolved model.md's E3 defect; the `:323` comment "the ziamath renderer lowers to SVG" names a downstream consumer, not an in-page hand-build.

### E8 — Hardcoding sweep — HOLD
- `sheet.md:153-172` pt tables — CONFIRMED (`_SIZES` `:153-173`). `:181,818,825` Helvetica magic — CONFIRMED (`_CELL_CSS` Helvetica `:181`; `setFont("Helvetica", 7)` `:818`; `setFont("Helvetica-Bold", 8)` `:825`). The sheet author does NOT compose the typography plane (V3); raw reportlab font names/sizes.
- `emit.md:349-403` ~48-field `EmitSpec` knob bag — CONFIRMED (`EmitSpec` `:349-404`, ~48 flat fields, NOT lifted into grouped value objects per V12). `:719,732` furniture/frame literals — CONFIRMED.
- `layout.md:160-170` diagram dims + stringly ELK — CONFIRMED (`_NODE_W`/`_NODE_H`/`_LAYER_GAP` `:158-160`; `_ELK_OPTIONS` stringly `:165-168`; inline stringly `{"elk.hierarchyHandling": "INCLUDE_CHILDREN"}` `:592`, `{"elk.portConstraints": "FIXED_ORDER"}` `:611`). `:251-288` diagram dims — CONFIRMED (hardcoded `48.0/32.0/200.0/30.0/80.0/24.0/160.0/120.0` throughout `_emit`).
- `draw.md:56-58` ink/font/port literals — CONFIRMED (`_PRECISION`/`_INK`/`_ENTITY_BAND` `:56-58`; `_INK = "#000000"` `:57` is the fixed black ink V4 says becomes a derive contrast pick). `:234` port-lane — CONFIRMED (`lane = min(0.85, 0.5 + 0.18 * port.index)` `:234`).

### E9 — Dead/stringly — HOLD (and extended)
- `glyphset.md:83-98` `EndCap` declared never consumed — CONFIRMED (12-member `EndCap` `:83-98`; not imported by `draw.md` or `layout.md`).
- `glyphset.md:101-111` `SubLayout` unread — CONFIRMED (7-member `SubLayout` `:101-111`; not consumed anywhere).
- `draw.md:188-211` 7-of-19 shapes — CONFIRMED. `glyphset.NodeShape` has NINETEEN members (`:43-66`: +DOCUMENT/MULTI_DOCUMENT/PREDEFINED_PROCESS/MANUAL_INPUT/MANUAL_OPERATION/OFF_PAGE/STORED_DATA/DISPLAY/DELAY/CONNECTOR/CARD/TAPE); `draw.md:_shape` (`:199-230`) renders ONLY 7 (RECTANGLE/ENTITY/OVAL/DIAMOND/PARALLELOGRAM/HEXAGON/CYLINDER) then `case _ as unreachable: assert_never(shape)` — the match is NON-TOTAL against a 19-member enum, so `assert_never(shape)` FAILS typecheck and 12 shapes are dead/type-error. `_DRAWIO_STYLE` (`:64-70`) maps only 5. V10's "12 are currently dead/type-error" holds.
- EXTENDED dead carriers: `TextRun` (`glyphset.md:120-129`) is carried on `GlyphStyle.text` (`:149`) but NEVER read by `draw.md` (labels hardcode size/ink `:258/270/274`); `Port.at`/`width`/`height`/`label` (`:135-139`) declared but `_elk_node` (`layout.md:610`) reads only `port.side`/`port.index`, `draw.md:_port_xy` (`:233`) only `side`/`index`; `GlyphStyle.corner` (`:148`) never read by `_paint`/`_shape`. V10's "EndCap, SubLayout, Port.at, TextRun consumed or deleted" — all still dead, plus `corner`, `Port.width/height/label`.

### E10 — Ledger drift — HOLD
- `model.md:15` "ten-variant" against `:7`'s eleven — CONFIRMED. `:7` [INDEX] "eleven-variant", `:12` [Owner] "eleven `msgspec.Struct` variants", the real union has 11 (`:351-353`) + `NodeKind` 11 members (`:45-56`); but `:15` [Entry] STILL reads "`DocumentNode` is a `type` alias over the ten-variant `Union`". Stale "ten" at `:15`.

### E13 — Track-law debt — HOLD (comprehensively across the lane)
- `from builtins import frozendict` in 16 lane pages (all but `compose.md`, `chart/spec.md`, `diagram/glyphset.md`). Every import raises (not a CPython builtin); every `Final[frozendict[...]]` table re-types `Final[Map[...]]`.
- `rasm.runtime.content_identity` in 14 lane pages (15 hits; `model.md` ×2). Runtime `[V4]` renames to `rasm.runtime.identity`.
- `[03]-[RESEARCH]` / `[RESEARCH]` headers in 15 lane pages (absent only in `egress.md`, `model.md`, `scene/render.md`, `scene/export.md`). Folder-wide ban.
- Folder-minted limiters: `compose.md:153` (`_GATE = CapacityLimiter(4)`) + `:159` (`_TRANSIENT = stamina.AsyncRetryingCaller(...)`), `imposition.md:150`, `sheet.md:151` — the four E13/[04] names, all present. BROADER: additional minted limiters at `chart/export.md:66` (`_RENDER_LIMITER`) + `:69` (`_TRANSIENT` stamina), `table.md:149` (`_LANES`), `scene/render.md:78` (`_SCENE_LIMITER`), `diagram/draw.md:55` (`_DRAW_LANES`), `diagram/layout.md:193` (`_LAYOUT_LANES`). Every mid-plane producer mints its own `CapacityLimiter` + `stamina` retry rather than routing through the runtime lane owner (`lanes.offload`) / runtime `guarded`. [04] SHARED-TIER LAW: "concurrency ONLY through the runtime lane owner ... zero artifacts-minted limiters ... retry ONLY through runtime `guarded`."

---

## [PER-PAGE VERDICTS]

### visualization/table.md — REBUILD (E5 fix + V10 completions)
DEEP great-tables mining (22-case `TableOp`, 20-verb `FMT_TABLE`, `Reshape` polars pre-pass, `LOC_TABLE`/`VALS_TABLE`, deterministic `_stable_id`, offload). Charter as-it-should-be: adopt `GT(frame.to_pandas())` as the DEFAULT render floor with polars `.style` demoted to a probed capability (V10/E5); add pagination/continuation-across-sheets (the AEC schedule pressure point — currently only `Slice`/`Head` row-limit, no page break); compose `kiwisolver` for column-width solving (currently manual `Width(dict[str,str])` `:434`); add units-sub-rows to the structure vocabulary. Retype `frozendict`→`Map` (`:37`); rename content_identity (`:44`); purge `[RESEARCH]` (`:959-970`); route the `_LANES` limiter (`:149`) onto the runtime lane. Entry `render() -> RuntimeRail[ArtifactReceipt]` (`:664`) converges to `emit()`.

### visualization/chart/spec.md — MOVE Palette/hex_ramp OUT + REBUILD
Strong altair Vega mining (typed `ChartTheme` `*Kwds`, `_themed` deep-merge, `catch(SchemaValidationError)` rail). CORE DEFECT: E2 — this is the de-facto palette owner. `type Palette = NDArray` (`:30`) and `hex_ramp` (`:128`) MOVE to `graphic/color/derive#DERIVE` (V4); the fence imports derive rather than minting the alias; the `ColorReceipt.coords` prose (`:3/12`) becomes a real import (or the `ColorReceipt` rail collapses into `ArtifactReceipt` first, V4). Purge `[RESEARCH]` (`:136-141`). No frozendict/content_identity here (clean).

### visualization/chart/export.md — MERGE transform.md IN + cold-improve
9-grade host-free render dispatch (vl-convert/lets-plot `VL_RENDER`/`LP_RENDER` tables, offload+stamina+span, evidence weave). V10: `transform.md` merges INTO this owner (both still separate files). Rewire the `Palette`/`hex_ramp` import (`:36`) to derive after the V4 move. Retype `frozendict` (`:26`, `VL_RENDER`/`LP_RENDER` `:213/228`); rename content_identity (`:32`); route `_RENDER_LIMITER`/`_TRANSIENT` (`:66/69`) onto the runtime lane; purge `[RESEARCH]` (`:281-289`). Entry `render() -> RuntimeRail[ArtifactReceipt]` (`:247`) converges to `emit()`.

### visualization/chart/transform.md — ABSORB into export.md
Deep vegafusion pre-pass (`PrePass`/`PrePassEvidence`, recursive `_has_transform`, plan cost-gate). V10 dissolves this into `export.md` (the transform→export merge). Stray `</content>` tag at EOF (`:314`) — a literal closing tag left in the markdown, delete. frozendict (`:19`); `[RESEARCH]` (`:309-313`). No content_identity (pure policy page).

### visualization/diagram/draw.md — REBUILD (V3 rewire + V10 shape parity + V13 type-system)
Strong dual egress (drawsvg named-layer + drawpyo `.drawio`, one grammar). THREE defects: (1) E3/V3 — hand-builds ziafont/ziamath (`:26/40/114/186-191`); route label outlining + math through a typography/math owner + `typography/shape` shaping, not local `ziafont.config`/`face.text`/`ziamath.Text`. (2) E9/V10 — `_shape` (`:199-230`) renders 7 of 19 NodeShapes; the 12 new flowchart shapes are dead/type-error (`assert_never(shape)` fails typecheck); render all 19 or the glyphset contracts honestly. (3) E8/V4/V13 — `_INK = "#000000"` (`:57`) becomes a derive contrast pick; `GlyphStyle.text` (`TextRun`) must be READ (currently hardcoded `10.0/8.0/9.0`). E2: rewire `Palette`/`hex_ramp` import (`:37`) to derive. frozendict (`:28`); content_identity (`:32`); `[RESEARCH]` (`:343-348`); `_DRAW_LANES` (`:55`).

### visualization/diagram/glyphset.md — REBUILD (dead-carrier purge + V15 area case)
Rich mark vocabulary but MANY dead carriers (E9): `EndCap` (`:83-98`), `SubLayout` (`:101-111`), `TextRun`-via-`GlyphStyle.text` (`:120-149`), `Port.at/width/height/label` (`:135-139`), `GlyphStyle.corner` (`:148`) — all declared, none consumed by draw/layout. Charter: consume or delete each; ADD the V15 area/polygon glyph case (the five-mark closure `:16` cannot express room/parcel/footprint polygons — area law needs a sixth case or a polygon-bearing `Node`). `[RESEARCH]` (`:197-201`). No frozendict/content_identity (clean, pure vocabulary).

### visualization/diagram/layout.md — REBUILD (V15 machinery — the biggest lane gap)
Genuinely DEEP graph-layout engine (5 `LayoutPolicy` cases, 4 `HierarchyEngine` providers rustworkx/fast-sugiyama/grandalf/pyelk, kiwisolver constrained, projection table). But V15 AEC machinery is UNMET: (1) solar ephemeris — `_solar_arc` (`:451`) projects supplied azimuth/altitude, ZERO date/latitude computation, no `pvlib`; the `[04]` pvlib admission has no consumer. Sun-path furniture is thin (ticks+arcs+compass `:250-258`, no horizon circle/altitude rings/labeled date arcs). (2) plan-geometry ingress — untyped `_attr(data, key)` reads (`:389`), no typed coordinate-column contract, no real polygons. (3) area law — STACKING/PROGRAM/SITE use fixed dims (`Swimlane(...,200.0,30.0)` `:271`, `Node(...,48.0,32.0)` `:276`, `Swimlane(...,80.0,80.0)`+`Node(...,24.0,24.0)` `:284-285`), not area-proportional; CIRCULATION/SECTION_CALLOUT spring-defaulted (`:180/188`). (4) massing suite — depends on absent V9 scene egress. V10: stringly ELK options (`:165-168` etc) → typed layout-option vocabulary over `.api/pyelk.md`. `grandalf` is a LIVE consumer (parity oracle + the ONLY spline router, `_grandalf_router` `:525`); its [PYPROJECT] removal is gated on fast-sugiyama parity + a SPLINES re-home. frozendict (`:30`); content_identity (`:41`); `[RESEARCH]` (`:699-705`); `_LAYOUT_LANES` (`:193`).

### scene/render.md — REBUILD (V9 drawing bridge + V6 entry + E11 cycle)
Very deep pyvista scientific-viz owner (5 `SceneOp` modalities, 20-case `FieldFilter`, rich `RenderSpec`, USD metadata band). V9 AEC drawing-bridge UNMET: no standard-view family (plan/elevation/section/axo/iso presets — only raw `CameraPose` `:145`); no silhouette/feature-edge/hidden-line `FieldFilter` case; no directional sun policy (`enable_shadows` parameterless `:130`); no GL2PS 3D→vector; untyped `grid: object` ingress (`:401/501`). E11: render⇄export cycle admitted (`:38`), hoist `SceneTarget`; key+receipt carrier (`:431`) converges to `emit()`. `.api/vtk.md`/`.api/pyvista.md` need the silhouette/feature-edge/GL2PS catalog rows. frozendict (`:25/485`); content_identity (`:29`); `_SCENE_LIMITER` (`:78`). No `[RESEARCH]` here.

### scene/stage.md — KEEP (cold-improve) — E11 collapse-premise REFUTED
Excellent USD/USDZ authoring owner (`PrimKind` scene-graph, instancer/camera/material/labels, `BBoxCache`). Handles the `usd-core` census row correctly (`lazy from pxr` inside worker `:34`). NOT the "498-line flat-mesh" collapse candidate E11/V9 framed. frozendict (`:29`); content_identity — none (worker page); `[RESEARCH]` (`:492-498`). V6 entry: contributes via the render `Export` arm (no own entry) — sound.

### scene/export.md — KEEP (cold) — but zlib-ng re-home + E6 bundle-clone
Clean `SceneTarget`/`Sink`/`ROW`/`_EXPORTER` policy-table export owner. CROSS-CUTTING: `_captured` (`:219-237`) hand-reaches `zlib_ng.compressobj` (`:234`) + `_EPOCH`/`_ZIP_LEVEL`/`ZIP_AUTO` reproducible-bundle logic (`:63-66/228`) explicitly to match `package/archive#ARCHIVE` "byte-identical" — this DUPLICATES the reproducible-ZIP mechanism package/archive owns, and is the [PYPROJECT] `zlib-ng` re-entry site: the strike ruling says a `package/`-plane consuming fence admits `zlib-ng`, not a scene-side hand-reach. Charter: scene/export composes the package/archive ZIP path, not a `get_compressobj` clone. frozendict (`:28`); content_identity — none. No `[RESEARCH]`.

### composition/compose.md — cold-improve + V6 entry + E13
Strong post-render placement owner (imports `graphic/vector` one-hop, 13-case `DrawOp`, `MarkKind` overlay, gated pillow/vl_convert). E13/[04]: `_GATE` (`:153`) + `_TRANSIENT` stamina (`:159`) route onto the runtime lane. Entry `Figure.of()/contribute()` (`:387/414`) converges to `emit()`; prose "routes through the single `core/plan` `ArtifactPipeline` production entry" (`:13`) is UNBACKED (E1). content_identity (`:39`); `[RESEARCH]` present (per census); imports `export/layered.Layer` (`:56`, the [05](c) upward fan). No frozendict (clean).

### composition/imposition.md — REBUILD (E6 press-fold owner + E13 + V6)
Massive, deep press-imposition engine (9 local + 4 provider schemes, `PLANS`/`_PDFIMPOSE_SCHEMAS`, `ProofPolicy` ICC soft-proof, `PressMark` set). E6: OWNS the press fold V7 assigns it, but sheet/egress still clone it (see E6). Charter: the shared OCG-bind mechanism (`_mint_groups`/`_draw_one`/`_configure_layers`) becomes a composable owner sheet+egress import, not three copies. E13: MIXED state — imports `Map` (`:32`) AND `frozendict` (`:30`); complete the migration (`Composed.scores` `:341`, `_PDFIMPOSE_SCHEMAS` `:380`, `_mint_groups` return re-type). `_GATE` (`:150`). content_identity (`:35`); `[RESEARCH]` (`:880-891`). Entry `of()/contribute()` (`:509/532`) converges to `emit()`; prose "every imposition routing through the single `core/plan` production entry" (`:7`) UNBACKED (E1). Imports `export/layered.Layer` (`:39`, [05](c)).

### composition/sheet.md — REBUILD (V3 lettering + V7 completions + [05](b) inversion + E13)
Deep sheet-set owner (ISO 5457 zones, ISO 7200 title block, `Viewport` scale-affine, `SheetSet` projections). DEFECTS: (1) E8/V3 — Helvetica magic (`:181/818/825`); compose `standard.lettering()` + shaped runs, not raw `setFont`. (2) V7 completions — one `TitleBlock` (`:287`), no cover-sheet, sheet←emit seam prose-only (`:851/1007`), TrimBox/BleedBox deferred (`:1009`), the `scheduled()`→compose bridge (`:619`) prose-only. (3) V13 — no title-block grid VARIANTS (sheet families). (4) [05](b) inversion — imports `drawing/standard.ScaleRatio/SheetId` (`:48`, wave-2 importing wave-3); the DECISION re-homes the standard vocabulary slice below or re-waves. (5) [05](c) — imports `export/layered.Layer` (`:49`). E13: `_GATE` (`:151`); frozendict (`:39`); content_identity (`:44`); `[RESEARCH]` (`:993-1009`). Entry `of()/contribute()` (`:531/540`) converges to `emit()`.

### document/emit.md — REBUILD (E1 entry + E7 typography seam + E8/V12 knob-bag + E12 gaps)
Huge, deep lowering axis (28 `DocumentMode`, `BACKENDS`/`Band` table, run-fidelity markup, odf/docx/xlsx typed cells, PDF/A variant matrix). DEFECTS: (1) E1 — `produced` batch rail (`:538`) converges to `emit()`. (2) E7/V3 — NO typography/shape/layout import; reportlab re-derives (`:637-651/718`). (3) E8/V12 — ~48-field `EmitSpec` knob bag (`:349-404`) → grouped value objects. (4) E12 — static running head (`:715`), lone-arm TOC (`:701-709`), dead `pdf_forms` (`:750`), no weasyprint `@page`/`string-set` running content, forms not cross-target. (5) E6 — `Pdf.from_html` (`:764`) dup with report. `document/model` fold is the sound base to lower from. frozendict (`:29`); content_identity (`:35`); `[RESEARCH]` present; `_OFFLOAD` (`:219`).

### document/model.md — KEEP (cold-improve) — E10 fix + E13
EXCELLENT substrate tree (11-variant `DocumentNode`, full ISO 14289 `StructEltKind`, `to_corpus`/`to_typst`/`to_html`/`to_c14n`/`to_markdown`/`to_latex`, `FormulaNode` mitex, `DocumentDelta`). E3 RESOLVED here (clean FormulaNode). DEFECTS: E10 — `:15` "ten-variant" stale count (should be eleven). E13 — frozendict (`:30`, the many markup tables `:378-434`); content_identity (`:35`, ×2). Not a producer (no emit/of) — V6 N/A. No `[RESEARCH]` (clean). The one node algebra the brief calls sound; preserve.

### document/report.md — REBUILD (E1 rail + E6 Pdf.from_html + V12 TOC/terminal-PDF)
Deep report-composition (5 `COMPOSE_ARMS`, `Section`/`SectionBlock`/`TableData` tree, jinja/papermill/nbclient/nbconvert/pymupdf.Story/pdf_oxide). DEFECTS: (1) E1 — `rendered` batch rail (`:809`) converges to `emit()`. (2) E6/V12 — `Pdf.from_html` (`:746`) dup with emit; report mints terminal PDF (REFLOW `:666`, AUTHOR `:712`) inside the tree-composer V12 says route through emit — the rebuild EXPANDED this. (3) E12/V12 — `_toc` (`:513`) lists headings with NO page numbers. frozendict (`:31`); content_identity (`:39`); `[RESEARCH]` present; `_OFFLOAD` (`:414`).

### document/egress.md — cold-improve — E6 egress-imposition + E13
Deep 13-step finishing close (`FINISHERS` table, `LicenseLane` AGPL/permissive, full value-object bundle). E6: `Imposition.order`/`_impose` (`:269-276/623-653`) duplicate imposition.md's saddle algebra AND the [EGRESS_DISTINCT] "no booklet" claim is falsified (`ImposeLayout.BOOKLET` `:99`); V7 says egress composes imposition's fold. E13: MIXED — imports `Map` (`:39`) + `frozendict` (`:37`); complete migration. content_identity (`:43`). No `[RESEARCH]` (clean). Entry `finish()/contribute()` (`:498/504`) converges to `emit()` at leg-1 reconcile.

### document/lens.md — KEEP (cold-improve, V12) — E13
Deep recover-TO owner (23-op `LensOp`, `_ROUTES` table, pdf_oxide/pymupdf/pdfplumber/etc providers, polymorphic `_node`, typed `NodeSlot`). V12 cold-improve: OCR pre-flight gate + in-package PDF/A egress mining (`ocrmypdf` `pdfinfo`/`pdfa` surfaces). E13: frozendict (`:29`); content_identity (`:68`); `[RESEARCH]` present. Entry `recover()/contribute()` (`:249/253`) converges to `emit()`. Sound base; cold.

### document/tagged.md — KEEP (cold) — E13 only
Deep, sound PDF/UA structural close (two-source pikepdf+pdf_oxide oracle reconciliation, PDF/A + PDF/X, model-vocabulary consumption, `NumberTree` parent-tree, depth-safe `_walk`). Brief-graded 9. E13: frozendict (`:32`); content_identity (`:37`); `[RESEARCH]` present. Entry `author()/contribute()` (`:258/261`) converges to `emit()` at leg-1 reconcile. Preserve.

---

## [CROSS-CUTTING FINDINGS]

1. **The AEC-documentation plane is the systemic gap.** The rebuild deepened GENERAL/TECHNICAL capability (chart grammar, scientific viz, graph layout, PDF/UA, USD) to 8-9.5 grade but did NOT land the AEC machinery the telos demands: V15 (solar computation, plan polygons, area law, massing) entirely absent in `diagram/layout`; V9 (standard views, silhouette, GL2PS, sun policy) entirely absent in `scene`; V3 (typography plane) uncomposed by `emit`/`sheet`/`draw`; V13 (type-system, sheet-family, design-language-as-data) absent — `TextRun`/`GlyphStyle.text` dead, one `TitleBlock`, no type scale. The flagship diagram/sheet-set artifacts would render library-default, reopening V13/V15 as the brief's flagship-review clause warns.

2. **No mid-plane producer is on the `ArtifactWork` `emit()` contract (E1/V6).** Six live carrier shapes (`of()/contribute()` ×3, `render()` key+receipt ×3, `produced`/`rendered` batch rails ×2). Prose in compose/imposition claims routing through `core/plan ArtifactPipeline` with no backing fence. This is a corpus-wide leg-1 reconcile obligation the mid-plane must accept.

3. **Three cloned mechanisms (E6).** The OCG-press-fold (`_mint_groups`/`_draw_one`/`_configure_layers`) across sheet+imposition; `Pdf.from_html` across emit+report; the saddle-stitch imposition algebra across imposition+egress. Each needs ONE owner the others compose (V7 rules imposition + emit as the owners). The egress [EGRESS_DISTINCT] research row is internally false (claims no booklet, has `ImposeLayout.BOOKLET`).

4. **E13 track-law is total, not partial.** 16 frozendict imports, 14 content_identity spellings, 15 `[RESEARCH]` tails, 8+ folder-minted `CapacityLimiter`/`stamina` mints (broader than the 4 named). imposition + egress are MID-migration (import both `Map` and `frozendict`). The concurrency defect is corpus-wide: every producer mints its own lane instead of `lanes.offload`/`guarded`.

5. **The color foundation is still orphaned (E2/V4), with a prose-vs-fence lie.** `chart/spec` mints `Palette = NDArray` + `hex_ramp`; scene mints a third `Palette` alias; draw/export/scene import the spec alias; NO page imports `graphic/color/derive`. `chart/spec.md:3/12` prose claims "color arrives from derive as `ColorReceipt.coords`" while the fence imports nothing from derive. `draw.md:57` `_INK="#000000"` and `schedule`-style literals persist; contrast/CVD/gamut law unreachable.

6. **Stray artifact:** `chart/transform.md:314` carries a literal `</content>` closing tag inside the markdown (delete on rebuild).

---

## [INTEGRATION HOMES for orphan-looking admissions in the lane] (targets, never removals)

- **`schemdraw`** — ORPHANED: no `diagram/schematic.md` exists; the diagram folder is draw/glyphset/layout only; `glyphset.md:3/199` MENTIONS schemdraw but no page imports it. HOME: V10's NEW `visualization/diagram/schematic.md` owning the 226-element `schemdraw.elements` catalog (logic/flow/dsp, Kmap/Timing/BitField) as the named-symbol schematic class the five marks cannot express. The `Segment*`/`ElementCompound` geometry spine stays with `drawing/symbol` (out of lane).
- **`pvlib`** (brief `[04]` admission) — NO consumer in `diagram/layout`. HOME: V15's solar-ephemeris owner (SPA azimuth/altitude from date/latitude, sunrise/transit/solstice, numpy-vectorized date sampling) feeding `ProjectionKind.SOLAR_ARC` and the sun-path furniture; the owned closed-form kernel is the declined-admission fallback.
- **`grandalf`** — LIVE but supersession-gated: it is the parity oracle AND the sole SPLINES router (`layout.md:_grandalf_router:525`). Integration-first HOME: keep as the `HierarchyEngine.GRANDALF` parity/spline arm until fast-sugiyama parity signs off AND the SPLINES route re-homes (its removal has a real routing ripple, not a zero-consumer strike).
- **`zlib-ng`** — LIVE in `scene/export.md:36/234` (byte-parity ZIP bundle). HOME per [PYPROJECT] re-entry ruling: the `package/`-plane compression band owns the admission; `scene/export` composes `package/archive`'s reproducible-ZIP path rather than hand-reaching `zlib_ng.compressobj` (the current clone).
- **`drawpyo`** (`.drawio` round-trip) — integrated in `draw.md` (`DrawTarget.DRAWIO`), but the `load_diagram` template round-trip (`:13/346`) and the TOML-validated style vocabularies are under-mined; deepen at the existing owner, never remove.
- **`ziafont`/`ziamath`** — LIVE in `draw.md` but at the WRONG tier (hand-built beside the typography owners). HOME: the typography plane (V3 math owner + shape outlining); draw composes the shaped/outlined runs, does not set `ziafont.config` locally.
- **`altair`/`lets-plot`/`matplotlib`/`vegafusion`/`vl-convert`** — well-integrated in the chart axis; lets-plot's second-engine charter (V10) must narrow to a consumed-capability row or gain its theme/flavor/element seam.
- **`great-tables`/`polars`/`kiwisolver`** — table well-mined EXCEPT kiwisolver (declared dep, not composed for width-solving) and the pandas-floor E5 fix.
- **`pyelk`/`fast-sugiyama`/`rustworkx`/`kiwisolver`** — deeply integrated in `diagram/layout`; the ELK stringly-option surface (V10) wants a typed layout-option vocabulary.
