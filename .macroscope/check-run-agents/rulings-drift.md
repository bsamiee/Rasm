---
title: Rasm Rulings Drift
input: full_diff
effort: medium
conclusion: neutral
include:
  - "libs/**"
  - "tests/**"
---

# [RULINGS_DRIFT]

Ruling fidelity is this pass's one question: whether the diff honors the settled decision registries — `RULINGS.md` files universal at every registry-carrying tier, package folders through cross-libs to the `tests/` estate — and whether the registries themselves stay lawful. A ruling row is settled law: one decision with the one why that guards its re-litigation. Read every `RULINGS.md` on the diff's path — folder, branch, cross-libs; language tree and estate root for `tests/` — before any finding; strata composition belongs to strata-leverage, boundary placement to architecture-integrity.

Hunt these classes, each finding citing the ruling row verbatim beside the breaching anchor:
- Ruling re-litigation: a diff re-admitting a rejected package, folding a ruled-plural surface, re-homing a ruled owner, or re-adding a retired structure — the ruling refutes the edit, and the demanded fix routes as a card (an IDEA at unifying altitude or a TASK at landing grain), never an inline workaround.
- Derivable creep: a `RULINGS.md` row whose fact AND why both live at one durable surface — that row deletes and the durable owner stands; a page stating the what while the discriminant, lost guarantee, or refuted objection stays homeless ADMITS the row, so a deletion demand citing fact-carriage alone is itself the defect.
- Registry drift: a row contradicting landed pages or manifests, a ruling homed above the narrowest tier containing it, a duplicate across tiers, a section outside the closed vocabulary (`[PACKAGES]`/`[SHAPE]`/`[COLLAPSE]`/`[STRUCTURE]`/`[PROCESS]`), or an empty pre-seeded instance.
- Anticipated capability landed: a diff realizing what a ruling anticipated — name the conformance so the ruling's growth path stays visible, never a defect; a diff whose stronger form a ruling anticipates receives the ruling-aligned deeper demand, since a fix landing below the ruled bar is unfinished.
- Homeless discriminant: a diff exposing two lawful twin surfaces whose separating rule lives nowhere durable — the finding demands the new ruling row at the narrowest owning tier, stating the discriminant and the guarantee each side keeps.

Settled rulings outrank intuition in both directions: a finding contradicting a ruling row is void with the row as refutation, and a diff contradicting a ruling is a finding regardless of how clean it reads. A pass that finds nothing after reading every registry on the path states that verdict plainly.
