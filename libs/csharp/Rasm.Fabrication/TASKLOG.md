# [FABRICATION_TASKLOG]

The open and closed work for `Rasm.Fabrication`, distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed — plus bullets naming the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations.

## [1]-[OPEN]

[QUEUED] PROCESS_MACHINE_AXIS — from [PROCESS_MACHINE_AXIS]:
- Add a `process/family` design page owning `Process` `[SmartEnum<string>]` (`mill`/`turn`/`route`/`laser`/`plasma`/`waterjet`/`additive`/`oxyfuel`/`edm-wire`) and `Machine` `[SmartEnum<string>]` (`mill-3axis`/`mill-5axis`/`router-gantry`/`lathe-2axis`/`lathe-millturn`/`laser-flatbed`/`plasma-table`/`waterjet-5axis`/`fff-cartesian`/`fff-delta`); each `Process` row carries a `RemovalModality` (subtractive/thermal/abrasive/additive/erosion) column, a kinematic-class column, and a `PostDialect` default column; each `Machine` row carries its `Process` set, its holding class, and its axis count. The axis is the one fabrication discriminant `cut-parameter`/`motion`/`posting` read to select their per-process behavior.
- Packages: Thinktecture.Runtime.Extensions (`[SmartEnum<string>]` with constructor-delegate behavior columns), LanguageExt.Core, BCL inbox.
- Integration: a new `process/` sub-domain under the folder; the `frontier/owner#FABRICATION_OWNER` `FrontierInput` carries the selected `Process`/`Machine` as input state every kernel reads, and `cut-parameter`/`motion`/`posting` discriminate on the `Process` row. No new entrypoint — the axis is policy data on the existing `Run` input.
- Considerations: `Process` and `Machine` are two axes over one concept (process = removal physics, machine = kinematics + holding), not a single flattened enum; the `RemovalModality` column is the discriminant `REMOVAL_PHYSICS_FAMILY`'s `RemovalBudget` union switches on, so the two ideas share the one modality vocabulary, never two parallel modality enums.

[QUEUED] REMOVAL_BUDGET_UNION — from [REMOVAL_PHYSICS_FAMILY]:
- Recast `process-physics/cut-parameter#CUT_PARAMETER`: grow the `Material` axis to carry every modality's column (subtractive surface-speed, thermal kerf-width/pierce-time/assist-gas, abrasive jet-pressure/abrasive-rate, additive melt-temp/bond-window) and replace the `CutBudget` record with a `RemovalBudget` `[Union]` (`SubtractiveBudget`/`ThermalBudget`/`AbrasiveBudget`/`AdditiveBudget`) the `Budget` projection selects by the `Process.RemovalModality` row; each budget case carries the raw scalars its modality reads. The `Budget` entry becomes `RemovalBudget Budget(Process process, Material material, Tool tool, Operation operation)`, the projection a per-modality formula fold over the axis columns.
- Packages: Thinktecture.Runtime.Extensions (`[Union]` for `RemovalBudget`, `[SmartEnum<string>]` for the grown `Material`/`Tool`/`Operation`), LanguageExt.Core, BCL inbox; UnitsNet composed ONLY through the `Rasm.Compute/units/quantities#QUANTITIES` boundary at ingress, never spelled in-folder.
- Integration: the `Process` axis from `PROCESS_MACHINE_AXIS` is the modality discriminant; the `toolpath/motion#CAM_MOTION` and `toolpath/skeleton#STRAIGHT_SKELETON` generators read the modality-specific budget case; the constitutive scalars the upstream `MaterialProperty.Thermal`/`Mechanical` cases own (thermal conductivity, specific heat, density) admit as raw doubles — a minimal one-directional AEC-domain-peer read of the `Rasm.Materials/physical-properties#MATERIAL_PROPERTY` `MaterialProperty` family is legal (the additive melt-temp and process-only thermal columns are NOT upstream engineering properties and stay owned by the in-folder `Material` axis row, never claimed from the peer seam) (both packages are AEC-domain peers and the dependency stays upward and acyclic, distinct from the forbidden app-platform `Rasm.Compute` downward edge the DRL idea gates), the boundary admitting the raw scalar so the table never holds a `MaterialProperty` type in a generator signature — the same raw-double quantity discipline the milling table already holds.
- Considerations: the milling `SubtractiveBudget` is the existing `CutBudget` formula unchanged — this is a widening, not a rewrite; a per-cell measured override stays one frozen `(Process, Material, Tool, Operation)` row; the canonical "materials own real specific vocabularies, not a thin steel" depth lands as the per-modality `Material` columns.

[QUEUED] TOOLING_STOCK_FIXTURE_VOCABULARY — from [TOOLING_STOCK_FIXTURE_VOCABULARY]:
- Three widenings on settled owners. (1) `process-physics/cut-parameter` `Tool` grows to a process-discriminated tooling family (`endmill`/`drill`/`turning-insert`/`laser-head`/`plasma-torch`/`waterjet-nozzle`/`fff-nozzle`), each row carrying the geometry its process reads. (2) `nesting/nfp` replaces the `SheetBounds` width/height/remnant rectangle with a `Stock` `[Union]` (`Sheet`/`Plate`/`BarStock`/`TubeStock`/`Billet`/`Filament`/`Remnant`) owning a `Bounds`/`Contains`/`Area`/`Of`-content-key total fold; the existing `Sheet`+`Remnant` arms become two cases, the NFP feasibility kernel unchanged. (3) `fixturing/workholding` grows `Clamp` to a `Workholder` family (`Clamp`/`Vise`/`Chuck`/`VacuumTable`/`Magnet`/`SacrificialBed`) keyed by the machine holding class, the exclusion-offset keep-out kernel unchanged.
- Packages: Thinktecture.Runtime.Extensions (`[Union]` for `Stock`, `[SmartEnum<string>]` for the tool/workholder families), `System.IO.Hashing` (`XxHash128` — the `Stock` content key, the one federation hash unchanged), LanguageExt.Core, BCL inbox.
- Integration: the `Stock` union is the `nesting/nfp#NESTING` feasibility-set stock the `Contains`/`Area` reads and the `posting/program#CUT_PROGRAM` Sequence reads; the `Machine` holding class from `PROCESS_MACHINE_AXIS` selects the `Workholder` family; the content key stays the kernel `XxHash128`.
- Considerations: the `Stock` union collapses the virgin-sheet-vs-remnant `Option<Remnant>` discriminant and the new bar/tube/billet/filament cases into one closed family with one `Contains`/`Area` fold — never a per-stock packer; a tube/bar nest packs onto the 1-axis-revolved envelope, the NFP feasibility unchanged for the planar cases.

[QUEUED] PROCESS_TOOLPATH_FAMILY — from [PROCESS_TOOLPATH_FAMILY]:
- Recast `toolpath/motion#CAM_MOTION`: grow `ToolpathKind` with the non-milling generators — `turn-rough`/`turn-finish`/`face`/`groove`/`thread` (radial-sweep turning passes over the `BarStock` envelope), `thermal-contour` (a contour generator the laser/plasma/waterjet share, the pierce/lead differing by `Process` row), `slice-layer` (the additive perimeter-and-infill generator reading `additive/slicing`) — and dispatch `Generate` by the `(Process, ToolpathKind)` pair, the milling rows (`contour`/`pocket`/`drill`/`trochoidal`) unchanged. The turning generator reads the `BarStock` profile, the thermal generator reads the `ThermalBudget`, the slice generator reads the `additive/slicing` layer set.
- Packages: Clipper2 (via `geometry2d/clipper#POLYGON_ALGEBRA` — every process toolpath is a polygon offset/clip fold), Thinktecture.Runtime.Extensions, LanguageExt.Core, BCL inbox.
- Integration: the `Process` row from `PROCESS_MACHINE_AXIS` is the generator discriminant; the `RemovalBudget` case from `REMOVAL_BUDGET_UNION` is the per-process budget; the thermal pierce/lead conditioning lands at `posting/program` (a thermal contour differs from a milling contour only at posting), the additive layer set at `additive/slicing#SLICING`.
- Considerations: the four milling toolpaths are unchanged cases — a widening; the turning pass is a 1-axis radial sweep, not a 2D-pocket offset; the thermal contour shares the `contour` generator and differs only in pierce/lead, so the `Generate` arm is thin and the conditioning is at posting.

[QUEUED] ADDITIVE_SLICING_SUBDOMAIN — from [ADDITIVE_SLICING_SUBDOMAIN]:
- Author an `additive/slicing` design page owning `Slicing` — the planar-section slice of a solid at each `LayerHeight` (the per-layer contour set), the perimeter shells (successive `geometry2d/clipper#POLYGON_ALGEBRA` inward `Offset` rings), the infill (a parametric hatch-line set clipped against the inner perimeter through the `Clip` Boolean), an `InfillPattern` `[SmartEnum<string>]` (`rectilinear`/`concentric`/`honeycomb`/`grid`) selecting the hatch geometry, and the `SliceLayer`/`SlicePolicy` records. The slice is an author-kernel over the admitted Clipper2 substrate — no managed slicer exists on NuGet, the author-kernel posture is the `STRAIGHT_SKELETON` precedent.
- Packages: Clipper2 (via `geometry2d/clipper#POLYGON_ALGEBRA` — perimeter `Offset`, infill `Clip`), `Rasm`/Vectors (`MeshSpace`/`Point3d` — the planar-section input), `Rasm.Geometry.Numerics` (`Predicate.Orient2D` — the section/winding verdict), Thinktecture.Runtime.Extensions, LanguageExt.Core, BCL inbox.
- Integration: the `Process.Additive` row from `PROCESS_MACHINE_AXIS` drives the slice; the planar section reads the `frontier/owner#FABRICATION_OWNER` model input; the layer set feeds the `toolpath/motion#CAM_MOTION` `slice-layer` generator and the `posting/program#CUT_PROGRAM` additive dialect; the `AdditiveBudget` from `REMOVAL_BUDGET_UNION` carries the layer-height/print-speed/melt-temp.
- Considerations: the planar section of a mesh at a Z-plane is the one genuinely-new geometry op (a plane-vs-triangle intersection producing the layer contour); the perimeter and infill compose the one Geometry2D owner, never a second polygon engine; a new infill pattern is one `InfillPattern` row over the same hatch-clip primitive.

[QUEUED] PORTABLE_POST_DIALECT_FAMILY — from [PORTABLE_POST_DIALECT_FAMILY]:
- Recast `posting/program#CUT_PROGRAM`: grow `GCommand` with the non-milling word sets (lathe `G96`/`G92` constant-surface-speed and threading, thermal `M07`/torch-height-control/pierce/assist-gas, additive `M104`/`M109`/`E`-axis extrusion/bed-temp) and add a `PostDialect` `[SmartEnum<string>]` axis (`linuxcnc`/`grbl`/`fanuc`/`haas`/`marlin`/`reprap`/`hypertherm`/`mazak`) selecting the dialect render over the one dialect-neutral `CutProgram` AST; `Emit` becomes `Emit(PostDialect dialect)` dispatching the dialect arm. Add the thermal pierce-and-lead conditioning (`PierceDwell`/`AssistGas`) and the additive extrusion-and-temp conditioning the process generators feed.
- Packages: Thinktecture.Runtime.Extensions (`[SmartEnum<string>]` for `PostDialect`, the grown `GCommand`), LanguageExt.Core, BCL inbox; `System.Text` (`StringBuilder` — the dialect render fold, the existing text boundary).
- Integration: the `PostDialect` default comes from the `Process`/`Machine` row in `PROCESS_MACHINE_AXIS`; the `Motion`/`Placement` streams from `toolpath/motion` and `nesting/nfp` dead-end here; the thermal/additive conditioning reads the `RemovalBudget` case.
- Considerations: the `CutProgram` AST stays dialect-neutral and a new dialect is one `Emit` arm — the page growth line already names this; the milling RS-274 render is the existing `Emit` unchanged, one `PostDialect.LinuxCnc` arm; THC and pierce are real `GCommand` rows, not string injection.

[QUEUED] PROFILE_INGRESS_BOUNDARY — from [PROFILE_INGRESS_BOUNDARY]:
- Author a `geometry2d/profile-import` design page owning `ProfileImport` — the boundary admitting external 2D part profiles (DXF closed `LwPolyline`/`Polyline2D`/`Line`/`Arc`/`Circle`/`Spline` entities) into the `Loop` vocabulary through the `ACadSharp` `DxfReader.Read` → `CadDocument.Entities`/`ModelSpace` surface, tessellating arc/bulge entities to `Loop` vertices at the owner's chord tolerance, the boundary the one place a foreign CAD entity crosses into the canonical `Loop`. Route a `GeometryFault.DegenerateInput` on an unreadable file or a non-closed profile demanded closed.
- Packages: `ACadSharp` (the pure-managed AnyCPU DXF/DWG reader — already manifest-admitted under the App UI label; this folder is a second consuming owner, no manifest row delta, a folder `.api/api-acadsharp.md` catalogue and a README registry entry), `Rasm`/Vectors (`Point3d` — the tessellated vertices), LanguageExt.Core, BCL inbox.
- Integration: the imported `Loop` set feeds the `nesting/nfp#NESTING` feasibility set and the `posting/program#CUT_PROGRAM` part library; the boundary is host-neutral and coexists with Rhino-native I/O (architecture coexistence rule — Rhino owns the native read, this owns the portable profile contract), never thinned to feed it.
- Considerations: ACadSharp is AnyCPU IL with no native asset, so the boundary carries no RID burden; the arc/spline tessellation tolerance is one `SlicePolicy`-style chord-deviation knob; the precise `LwPolyline.Vertices`/bulge-to-arc member spellings stay a `.api` RESEARCH flag until decompile-verified, never settled fence code before catalogue verification.

[QUEUED] PROFILE_IMPORT_API_CATALOGUE — from [PROFILE_INGRESS_BOUNDARY]:
- Author `.api/api-acadsharp.md` decompile-verifying the `ACadSharp` reader surface the `geometry2d/profile-import` boundary names: `DxfReader.Read(string, NotificationEventHandler)`/`DwgReader.Read`, `CadDocument`, the `Entities`/`ModelSpace`/`BlockRecords` collections, the `LwPolyline`/`Polyline2D`/`Line`/`Arc`/`Circle`/`Spline` entity types and their vertex/bulge/center/radius accessors, and the `NotificationEventHandler` callback shape. Mark any unverified vertex/bulge accessor spelling a RESEARCH item until the assay api decompile confirms it.
- Packages: catalogue-only (the `assay api` decompile rail over the installed `ACadSharp` distribution).
- Integration: the catalogue is the resource the `profile-import` boundary fence transcribes from; the README `[2]-[PACKAGES]` registry gains `ACadSharp`.
- Considerations: ACadSharp's entity geometry (the bulge-to-arc conversion `Arc` factory the README names) is the one surface the boundary leans on — verify it before the fence settles; the catalogue follows the per-folder domain-seam rule (ACadSharp is a folder-local domain seam, a per-folder catalogue, never a branch `.api/`).

[UPSTREAM-BLOCKED] NFP_DRL_POLICY — from [DRL_NEST_POLICY]:
- Add a DRL-guided placement `NestPolicy` column in `nesting/nfp#NESTING` carrying an injected `Func<NoFitPolygon, PartTransform, double>` placement-score delegate ranking placements over the existing NFP primitive; the delegate slot is Fabrication-author, the inference that fills it is consumer-author.
- Strata + external dependency: Fabrication is AEC-domain and the strata law forbids the downward edge to the app-platform `Rasm.Compute`, so the inference lane is never referenced from this folder — the app-platform consumer runs the realized `Rasm.Compute/models/inference#INFERENCE_MODES` `RunOps.Infer` over a trained placement-ranking model and fills the delegate with a raw scalar, exactly as `REMOVAL_BUDGET_UNION` admits quantity ingress as raw doubles. The inference lane is realized; the blocking residual is the trained placement-ranking model asset, outside this folder's write-scope.
- Fallback path: phase-1 ships the realized bottom-left/genetic folds with the Geometry2D-routed NFP and the content-keyed `Remnant`/`Stock` arm; the learned column lands as one `NestPolicy` delegate column once the consumer-side inference wiring and the model asset exist, the NFP and heuristic folds unchanged. Consumer seam (not a Fabrication reference): `Rasm.Compute/models/inference#INFERENCE_MODES`.

[UPSTREAM-BLOCKED] CSG_WATERTIGHT_SILHOUETTE — from [CSG_SILHOUETTE]:
- A watertight-solid silhouette arm on `projection/hidden-line#PROJECTION_HIDDEN_LINE` producing the exact outline of a boolean-combined solid by composing the kernel `Rasm.Geometry/tessellation#BOWYER_WATSON_DELAUNAY` constrained arrangement and the `Rasm.Geometry/offsetting#STRAIGHT_SKELETON` wavefront — the managed exact path the C# branch `ROBUST_ARRANGEMENT_SUBSTRATE` provides, not a native CSG asset.
- Trickle-down dependency: the kernel exact-arrangement owner (`Build`/`ToMesh`/`Apply`/`SkeletonGraph` settled wires) is the upstream the silhouette composes; the admission is no longer an external NuGet block but a compose-the-kernel-arrangement task held until those kernel wires land, never an in-folder CSG author-kernel and never coupling into the kernel's `SimplexStore` interior.
- Fallback path: the per-facet `projection/hidden-line#PROJECTION_HIDDEN_LINE` HLR kernel stays the only silhouette owner and remains pure-managed with no native asset until the kernel arrangement wires land; the curved-surface analytic silhouette growth arm stays a per-facet-builder extension.

## [2]-[CLOSED]

[COMPLETE] GEOMETRY2D_CLIPPER_SUBSTRATE — `geometry2d/clipper#POLYGON_ALGEBRA` is the one Clipper2 owner with `Offset`/`Clip`/`MinkowskiSum`/`ClipOpenPath`/`Simplify`, the `ClipOp`/`OffsetEnds` axes, the `ToPath`/`FromPaths` boundary map, and the `Predicate.Orient2D` fold-back winding; the `Simplify` operation realizes the catalogued `Clipper.SimplifyPaths` collinear-run collapse the posting `Condition` routes before the ring walk; Clipper2 registered as the folder's first admission, the buggy `Triangulate` excluded.

[COMPLETE] CONTAINMENT_COLLAPSE — the four parallel hand-rolled point-in-CCW-polygon `Orient2D` loops (`nesting/nfp` `Remnant.Holds`/`NoFitPolygon.Feasible`, `posting/program` `Encloses`, `fixturing/workholding` `ExclusionZone.Covers`) collapse onto one `frontier/owner#FABRICATION_OWNER` `Loop.Covers` exact-`Orient2D` containment member every consumer composes; a per-page re-rolled containment loop is the deleted form.

[COMPLETE] PROJECTION_CLIP_ONTO_CLIPPER2 — `projection/hidden-line#PROJECTION_HIDDEN_LINE` `ClipEdge` routes the `geometry2d/clipper#POLYGON_ALGEBRA` `ClipOpenPath` open-path Boolean (intersection→hidden, difference→visible) and unprojects each clipped endpoint by parameter; the BSP front-to-back ordering, the `SpatialIndex` broad-phase, and the `Orient2D` silhouette view-dot stay author-kernel.

[COMPLETE] TROCHOIDAL_STRAIGHT_SKELETON — `toolpath/skeleton#STRAIGHT_SKELETON` realizes the `NextEvent`/`Advance`/`Apply` wavefront-event kernel (the `RayMeet` edge-collapse and `SplitMeet` reflex-split over the `Orient2D` floor, the `1/sin(θ/2)` bisector speed, the `Count²+8` budget) and the `trochoidal` `ToolpathKind` row reads its medial axis; the convex-inset cross-check is the Clipper2 `Offset`.

[COMPLETE] PORTABLE_CUT_PROGRAM — `posting/program#CUT_PROGRAM` realizes the `Condition`/`Sequence`/`Kerf`/`CutPath`/`TabbedRing` cut-conditioning over the typed `GWord` AST: half-width `Offset` kerf (`KerfCollision` on collapse), `LeadArc` lead-in/out, `TabSpacing` micro-bridge gaps, and the `Encloses` inner-before-outer sequence through the exact `Orient2D` containment; `PostPolicy.Profiles`/`Placed` resolves each `PartTransform`. The RS-274-milling dialect re-opens as the `LinuxCnc` instance of `[PORTABLE_POST_DIALECT_FAMILY]`.

[COMPLETE] FIXTURING_EXCLUSION — `fixturing/workholding#WORKHOLDING` owns the `Fixture`/`Clamp`/`ExclusionZone` keep-out model: the safety-margin exclusion `Offset`, the `Clears` keep-out test over the exact `Orient2D` `Covers` containment, and the `Condition` fixture-constrained move conditioning routing `FabricationFault.KerfCollision` on a blocked feed move and composing the posting `Sequence`, never a second collision owner. The single clamp re-opens as one `Workholder` case of `[TOOLING_STOCK_FIXTURE_VOCABULARY]`.

[COMPLETE] CUT_PARAMETER_TABLE — `process-physics/cut-parameter#CUT_PARAMETER` owns the `material × tool × operation` cross-product table projecting to the raw `CutBudget` (spindle rpm, feed mm/min, depth mm, MRR mm³/min) over the `Material`/`Tool`/`Operation` `[SmartEnum<string>]` axes; quantity ingress admits through the `Rasm.Compute/units/quantities#QUANTITIES` boundary and the table stores raw doubles. The milling table re-opens as the `SubtractiveBudget` instance of `[REMOVAL_PHYSICS_FAMILY]`.

[COMPLETE] STOCK_REMNANT_ARM — `nesting/nfp#NESTING` carries the `Remnant`/`SheetBounds.Stock` arm content-keyed by `XxHash128.HashToUInt128` (the one federation hash), the `Option<Remnant> Stock` discriminant routing the `Contains` test and the `Area` denominator so a remnant re-enters the same NFP feasibility set as a virgin sheet. The sheet/remnant re-opens as two cases of the `Stock` union in `[TOOLING_STOCK_FIXTURE_VOCABULARY]`.

[COMPLETE] CLIPPER2_API_CATALOGUE — the `.api/api-clipper2.md` catalogue exists and ratifies the `Clipper2Lib` member spellings the `geometry2d`/`projection`/`toolpath`/`nesting`/`posting` fences name; the residual Triangulate prose re-grounds on the author's buggy caveat under the registry-truth sweep, not a re-open.

[COMPLETE] KINEMATICS_SPLIT — `kinematics/serial-chain#SERIAL_CHAIN` (`DhJoint` DH forward kinematics + the damped-least-squares `Ik.Solve` over the `Rasm`/Vectors `Matrix` rail) is authored as its own owner split from the CAM page; the `toolpath/motion#CAM_MOTION` `Cam` fold drives it.

[COMPLETE] FAULT_BAND_OWNERSHIP — `faults/faults#FAULT_BAND` `FabricationFault` (band 2500) mints ONLY the fabrication-specific cases (`NoFit`, `Unreachable`, `KerfCollision`, `OpenLoop`) and composes the kernel band-2400 `GeometryFault` for the one shared failure (`DegenerateInput`); a non-closed boundary mints `FabricationFault.OpenLoop` (the kernel union declares none), never a synthesized kernel case, a parallel band re-casing the shared failure, or a `GeometryFault.OpenLoop` the kernel never declares. `FabricationFault` mirrors the kernel `GeometryFault` shape (`Code`/`Message`/`ToError`) so both lower onto the one `Fin<FrontierResult>` rail.
