# [APPHOST_IDEAS]

The forward pool of higher-order concepts for the runtime spine, each grounded in the folder's domain and current platform capability — some are new sub-domain folders that deepen a thin owner, others bind a concrete admitted package to a settled abstract surface. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[RUNTIME_IDENTITY_AUTHZ_PLANE]-[QUEUED]: a runtime principal, token-lease, and ABAC policy engine closes the identity gap the grant broker never spanned.
- Capability: `AuthPlane` mints the `Principal` from OIDC, leases credentials through `SecretLease`, and gates `CapabilityDescriptor` invocation through an attribute-based `Evaluate(IdentityPrincipal, CapabilityDescriptor, EnvironmentFacts)` verdict feeding `GrantBroker.Admit` as a precondition, covering interactive operator login, service-to-service, and plugin-delegated identity on one acquisition surface.
- Shape: a new `Agent/identity.md` owns the `IdentityPrincipal` `[ValueObject]` family and the `TokenLease` record (access/refresh/expiry/DPoP-thumbprint) on a `RuntimeContextSlot<IdentityPrincipal>` ambient peer to `TenantContext.Ambient`, with one polymorphic `Acquire(GrantShape)` discriminating grant by request shape; a new `Agent/policy.md` owns the `PolicyDescriptor` `[SmartEnum]` row family compiled into `IAuthorizationRequirement` sets and the verdict that folds onto `GrantBroker.Admit`; the token-lease lifecycle reuses the `SECRET_LEASE` acquire/renew/zeroize machinery.
- Unlocks: operator sign-in, delegated service tokens, and claims-derived `GrantScope`, turning identity from authentication into authorization so the targeting rows, cost-metering, and distributed-quota debit all consume a settled principal.
- Anchors: `GrantBroker.ScopeOf` and the object-set × op-class × cost-ceiling × time-window admission algebra (`Agent/capability.md` GRANT_BROKER), `SecretLease.Acquire/Renew/Zeroize` + `CredentialPemWire` and `DeadlineClass.HopTotal` refresh deadlines (`Runtime/config.md` SECRET_LEASE, `Runtime/time.md`), `TenantContext.Ambient`/`Stamp` and the `ReceiptSinkPort.Send` token-event evidence stream (`Runtime/ports.md`); the admitted `OpenIddict.Client` 7.5.0 (`.api/api-openiddict-client.md`) is the OIDC token-acquisition spine and the admitted `Microsoft.AspNetCore.Authorization` 10.0.9 (`.api/api-authorization.md`) is the host-neutral ABAC evaluation core, both registered under the `Agent/identity` and `Agent/policy` charter registry rows and routed through `docs/stacks/csharp` on the `Fin`/`Eff` rails; `PermissionShape` + `CapabilityDescriptor.Permission` stay the sole op-metadata owner the verdict reads, and the durable legs land the identity-store table (principal registry + refresh-token rotation ledger + policy/role rows) under the `TenantId` RLS predicate in `Rasm.Persistence` plus the OIDC authorization-code+PKCE web flow in `typescript:security` + `typescript:browser`; policy/role admin surface in `typescript:ui` (over `security/authz`).
- Tension: `Rasm.AppHost` references only `OpenIddict.Client` (token client, no server or validation handler) and drives `AspNetCore.Authorization` host-neutrally over the evaluation core; the OIDC provider and web flow are the TS/Persistence leg. The `AuthPlane` and `Evaluate` verdict build against the `Agent/identity ⇄ Rasm.Persistence` identity-store seam interface, the durable principal/refresh-token/role store landing in parallel as the branch `ONE_IDENTITY_STORE` leg, so a per-process principal table that bypasses the `TenantId`-RLS store is the rejected form.
- Ripple: `csharp:Rasm.Persistence` `[ONE_IDENTITY_STORE]` (durable identity store) + `libs/.planning` `[ONE_OIDC_AUTH_FLOW]` (PKCE web token flow).

[CLUSTER_MEMBERSHIP_ELECTION]-[QUEUED]: a cluster membership, leader-election, and distributed-lock owner reuses `FencingToken`, turning the single-writer lease into a multi-node coordination surface.
- Capability: maintains a `MembershipView` via probes, elects a leader per role through a store-backed lease minting a fenced `FencingToken`, and grants distributed locks checked via `Admits`, covering multi-instance singleton execution, safe cross-process locks, and a live membership view as one coordination surface.
- Shape: a new `Wire/coordination.md` owns `MembershipView`/`NodeState`, a `RoleElection` fold, `DistributedLock`, and probe cadence projected as `SchedulePort` heartbeats; endpoint resolution feeds the existing `OutboundHop.HttpApi`/`Grpc` authority binding through a `ServiceEndpointResolver` so leader-election and lock rows address peers by logical role, with the lease store carried as a `Rasm.Persistence` ripple.
- Unlocks: multi-instance singleton execution, safe cross-process locks, a published membership view, and logical-name endpoint resolution the coordination rows reuse to address the cluster over the existing resilience hops.
- Anchors: `FencingToken.Admits`/`Acquire` Kleppmann reject-lower + `LeaseElection.Acquire` and `LeasePolicy.Maintenance` (`Runtime/time.md` FENCING_TOKEN), `SchedulePort` heartbeat cadence (`Runtime/time.md`), `OutboundHop.HttpApi(Uri Authority)`/`Grpc(Uri Address)` + `HopPolicy` rows and `SelectPipelineByAuthority` named-client binding (`Wire/outbound.md` OUTBOUND, WEB_PIPELINES), `DeliveryTarget` `[Union]` endpoint-vs-peer-manifest and the `DiscoveryManifest` in-app companion UDS attach owner (`Wire/outbound.md`, `Wire/companion.md`); the admitted `Microsoft.Extensions.ServiceDiscovery` 10.7.0 (`.api/api-service-discovery.md`: `ServiceEndpointResolver`/`AddServiceDiscovery`/`ServiceEndpointQuery`/round-robin selector/`ConfigurationServiceEndpointProvider`) resolves outbound logical-name endpoints + client LB only, registered under the `Wire/coordination` charter row and routed through `docs/stacks/csharp`, with `LanguageExt.Core` and `NodaTime` carrying the fold and instants; the durable leg lands the coordination/lease page in `Rasm.Persistence` with atomic compare-and-set + a fenced-token column under the `TenantId` RLS predicate, doubling as the membership backing store the resolver reads, and the distributed-quota debit (idea RUNTIME_IDENTITY_AUTHZ_PLANE) fences through the same lock store.
- Tension: the lease needs a shared store for compare-and-set and the store fences the token; `Wire/coordination` builds against the `⇄ Rasm.Persistence` CAS + fenced-lease seam interface, the durable store landing in parallel as the branch `ONE_FENCED_LEASE_STORE` leg, so the growth path is the same maintenance-lease `Acquire` with zero new surface — no Orleans, no Consul — and `ServiceDiscovery` resolves only outbound network endpoints while the in-app companion UDS attach stays the `DiscoveryManifest` owner.
- Ripple: `csharp:Rasm.Persistence` `[ONE_FENCED_LEASE_STORE]` (CAS + fenced-lease + membership backing store).

[TARGETED_DELIVERY_EXPERIMENTATION]-[QUEUED]: a targeting, progressive-rollout, and experimentation owner extends the static config kill-switch into rollouts, targeting rules, sticky bucketing, and A/B variants via OpenFeature.
- Capability: implements an OpenFeature `FeatureProvider` over config targeting rows — evaluating via deterministic sticky bucketing (`XxHash3` of subject+flag), assigning variants, and gating capability descriptors — covering kill-switch, gradual rollout, per-tenant targeting, and experiment buckets as one evaluator.
- Shape: a new `Runtime/features.md` owns the `FlagDefinition`/`TargetingRule`/`Variant`/`RolloutSegment` rows and the config-backed OpenFeature provider with one `Evaluate(FlagKey, EvaluationContext) -> FlagVerdict` surface; flag changes ride `ReloadClass.Transition` + `Overlay`, and `KillSwitchConfig.ForcedLevel`/`OperatorOverride` becomes one forced-off targeting row rather than a parallel switch.
- Unlocks: canary exposure, per-tenant/segment targeting, A/B experiments, and a standard OpenFeature client API the model-routing rows, fleet-roll strategy axis, and the TypeScript feature gates all consume.
- Anchors: `Config` `Overlay` + `OperatorOverride.From(KillSwitchConfig, Instant)` + `ReloadReceipt.PatchTrigger` and `OptionsAdmission.PatchSection` (`Runtime/config.md` POLICY_VALUES, reload), the eight-row `ConfigSource` chain on the one `ConfigurationManager` (`Runtime/config.md` SOURCE_AXIS), `XxHash3.HashToUInt64` fleet-spread seed precedent and `TenantContext.Slug` targeting attribute (`Runtime/time.md` SCHEDULE_PORT, `Runtime/ports.md`), `CapabilityDescriptor` gating; the admitted `OpenFeature` 2.13.0 (`.api/api-openfeature.md`: `Api.Instance`, `FeatureClient.GetBooleanValue`/`GetObjectValue`, `EvaluationContext`/`EvaluationContextBuilder`, `FeatureProvider`/`ResolutionDetails`, `FlagEvaluationDetails`, `Reason`/`ErrorType`, in-memory provider in the core SDK) is the evaluation contract and the admitted `System.IO.Hashing` 10.0.9 (`.api/api-hashing.md`) the bucketing hash, both registered under the `Runtime/features` charter row and routed through `docs/stacks/csharp` on the `Fin` rail; the `FlagVerdict` deepens the `Agent/reasoning.md` MODEL_GOVERNANCE `ChatClientBuilder.Compose` fold with a `UseModelSelection` routing arm and the `Sandbox/provisioning.md` `FleetRoll` conductor with a `RollStrategy` `[SmartEnum]` axis (canary/blue-green/linear-wave), and the durable leg exposes the same OpenFeature client provider in `typescript:host` (`host/flag`).
- Tension: the provider is in-process and config-backed — targeting rows bind through the existing eight-source `ConfigSource` chain and `OptionsAdmission`, the kill-switch forced-level stays the one `OperatorOverride` union, and a parallel flag store beside the `ConfigurationManager` chain is the rejected form; a remote flag SaaS would be one provider row later.
- Ripple: `libs/.planning` `[ONE_FEATURE_FLAG_PROJECTION]` (TS/edge OpenFeature client over the same evaluation contract).

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[DURABLE_ORCHESTRATION_SPINE]-[COMPLETE]: `Runtime/orchestration.md` owns the crash-durable workflow — the five-case `StepKind` `[Union]` (`Activity`/`Timer`/`Signal`/`Compensation`/`PersistentJob`), the `Orchestrator.Drive` executor folding each step through the one `Agent/runtime#DISPATCH_FRONT_DOOR` `CommandDispatch.Run` (no parallel dispatcher), the `StepStateSeam` fenced-CAS step-state Persistence ripple, `CrashResume` rehydrating from the one `SupportTrigger.FaultTransition` boot fact, persistent-job cadence on the one `SchedulePort`, and the TS instance/step projection; Ripple: `csharp:Rasm.Persistence` `[ONE_FENCED_LEASE_STORE]` + `[ONE_OUTBOX_EGRESS_SPINE]` (seams landed).
[EVENT_BUS_OUTBOX_FABRIC]-[COMPLETE]: `Wire/topics.md` owns the in-process bus topology (`BroadcastBlock` topic fan, bounded `BufferBlock`/`ActionBlock` subscriptions, `JoinBlock`/`BatchedJoinBlock` correlate/coalesce as `DrainSurface` builders over the one `DrainKind` union) and `Wire/outbox.md` owns the transactional outbox + dead-letter + watermark-advancing dispatch sweep on the one `SchedulePort`, the relay feeding `EventBus.Dispatch` and the durable `OutboundHop`; the `DeliveryFanout` dedupe cell is reused for exactly-once-effective delivery; Ripple: `csharp:Rasm.Persistence` `[ONE_OUTBOX_EGRESS_SPINE]` (seam landed).
[WORK_LANE_RESILIENCE_GOVERNOR]-[COMPLETE]: `Runtime/laneguard.md` owns the in-process `LaneGuard` — one keyed Polly `ResiliencePipeline` per `WorkLane` (bulkhead/breaker/timeout/hedge + test-host Simmy chaos) mirroring `KeyedLane.Register`, the `AdaptiveConcurrency` arm reading `ResourceMonitoring` and the `LoadShed` arm reading the atomic `DegradationReading`, the per-lane `ShedVerdict` minted once and crossed to Compute admission; Ripple: `csharp:Rasm.Compute` `[ONE_DEGRADATION_SHED_VERDICT]` (seam landed).
