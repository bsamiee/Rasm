# [PERSISTENCE_IDEAS]

The forward pool of higher-order concepts for the durable-state spine, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[PERSISTENCE_LIBRARY_TABLES]-[QUEUED]: Rasm.Persistence owns the durable content-hash identity and federation tables backing the MaterialLibrary catalogue, the ProfileCatalogue/ConnectionCatalogue registered rows, and the MaterialPropertyLibrary property sets so a material/profile/connection/property row persists with a content key and federates across the workspace, never an in-memory FrozenDictionary the runtime rebuilds.
- Capability: The persistence owner giving the MaterialId-keyed MaterialParameters rows, the ProfileId/ConnectionId-keyed catalogue rows, and the MaterialId-keyed MaterialPropertySet rows a durable content-hash identity and a federation table so the catalogue is a persisted, content-addressed library a workspace shares rather than a compile-time seed.
- Shape: A Rasm.Persistence library/property durable table set keyed by the Materials content keys (MaterialId/ProfileId/ConnectionId), the durable backing the in-memory FrozenDictionary catalogues seed from, the producer end of the durable-library federation concern.
- Unlocks: A material/profile/connection/property library row persists and federates with a content hash, a user-authored material admits into the durable catalogue, and the workspace shares one content-addressed library across host/web/companion consumers.
- Anchors: graph.md MaterialLibrary.Rows (the FrozenDictionary catalogue seed); profile.md/connection.md the ProfileCatalogue/ConnectionCatalogue registered-row tables (connection.md lines 92-101); properties.md MaterialPropertyLibrary.Rows (properties.md lines 176-192); ARCHITECTURE.md Rasm.Persistence content-hash identity/federation; the CLAUDE manifest Persistence-owns-durable-identity law. No durable library-table fence is authored yet; the `Rasm.Materials` catalogue owners that supply the content-keyed rows are a separate-target-set concern, so the Persistence-side durable schema lands once those owners pin their content keys.
- Ripple: counterpart of `Rasm.Materials` catalogue owners behind `[STEEL_DESIGN_OBJECT]`/`[SUSTAINABILITY_PROPERTY_DISCIPLINE]` (durable `MaterialLibrary`/`ProfileCatalogue`/`ConnectionCatalogue` rows).

[FABRICATION_PROGRAM_DURABLE_ROWS]-[QUEUED]: A Rasm.Persistence fabrication-program durable schema persists the toolpath/nesting/workholding/post-dialect receipts as content-addressed rows so a posted cut-program, a nest layout, and a fixture setup survive as queryable durable records, closing the persistence gap the host-local Fabrication receipts have no durable owner for.
- Capability: Every Fabrication kernel produces a host-local receipt (Motion joint stream, Placement transforms + utilization, CutProgram GWord AST, Fixture exclusion set) that crosses only the in-process seam and dead-ends — no durable persistence owner, so a posted program or nest layout is recomputed every session with no provenance, diff, or version. This ripple-idea authors the Persistence-side durable schema: content-addressed fabrication-program rows (keyed by the same XxHash128 content identity the nesting Remnant already mints) carrying the toolpath/nesting/post-dialect/workholding receipts as queryable durable records the Persistence Schema/Store owners hold.
- Shape: A new Rasm.Persistence Schema/Store fabrication-program durable-row owner: content-addressed rows for the CutProgram AST, the nest Placement, the Fixture setup, and the resolved (Process, Machine, PostDialect) job axis, keyed by the XxHash128 content digest, composing the existing Persistence DDL/converters/identity surfaces — the durable owner the host-local Fabrication receipts project into at the wire boundary, never a Fabrication-side persistence reference.
- Unlocks: Durable, queryable, content-versioned fabrication programs (a posted G-code program diffable across revisions, a nest layout recalled by content key, a fixture setup persisted), and a second real consumer of the XxHash128 content identity beyond the in-folder nesting memo.
- Anchors: csharp:Rasm.Persistence/Schema/ddl#IDENTITY and Store the DDL/converters/identity/engine owners; Fabrication/Posting/program#CUT_PROGRAM CutProgram, Nesting/nfp#NESTING Placement/Remnant XxHash128 (`Remnant.Of` folds `XxHash128.HashToUInt128` over CCW vertex bytes, nfp.md), Nesting/workholding#WORKHOLDING Fixture; ARCHITECTURE.md `Schema ← Rasm.Fabrication/Posting # [WIRE]: CutProgram AST content-addressed durable-row projection` and `Schema ← Rasm.Fabrication/Nesting # [WIRE]: Placement / Remnant XxHash128 content-keyed durable row` seams; the host-local receipt wire boundary. No durable fabrication-program fence is authored yet; the `Rasm.Fabrication` receipt owners that project the wire form are a separate-target-set concern, so the Persistence-side durable schema lands once those wire projections pin.
- Tension: The Fabrication receipts are host-local types the page law says never sit between wire and rail — the durable rows consume a WIRE PROJECTION of the receipts (a serialized program/nest/fixture shape), not the in-process records, so the persistence owner reads the projected wire form and Fabrication never references Persistence.
- Ripple: counterpart of `Rasm.Fabrication` `Posting/program#CUT_PROGRAM` (`CutProgram`) + `Nesting/nfp#NESTING`/`Nesting/workholding#WORKHOLDING` owners (durable program/nesting/fixture rows).

[REUSE_WIRE]-[BLOCKED]: Persistence `Query/Federation` federates the durable content-addressed reuse ledger over the `python:data`-stamped `ContentKey`, the XxHash128 seed being the one C#-owned identity both runtimes reproduce bit-identically rather than a second per-runtime hash.
- Capability: `Query/Federation` receives the data-stamped `ContentKey` on the existing wire and federates the durable reuse ledger; the XxHash128 content-key seed is the single C#-owned identity the Python data outputs reproduce, so a reuse row stamped in the data lane resolves against the durable federation table without re-minting.
- Shape: A federation seam row on `Query/Federation` consuming the `python:data` `ContentKey`, riding the existing op-log/content-addressed-snapshot substrate with no new engine.
- Unlocks: A durable, content-addressed reuse ledger federated across the C# durable spine and the Python data lane off one shared identity seed, the producer/consumer pair the architecture law assigns C# Persistence as the sole durable-identity owner.
- Anchors: `Rasm.Persistence/Query/Federation.cs` source-agnostic federated entity graph (`SourceKind` axis carries the source-agnostic projection), `Version/Commits` content-addressed commit-DAG, the XxHash128 content-key seed, the existing `python:runtime/transport` wire; ARCHITECTURE.md content-hash identity/federation owner. The Persistence half — the source-agnostic `FederatedEntity`/`EntityGraph` ready to fold the data-stamped `ContentKey` — is realized; the `python:data` `[REUSE_WIRE]` counterpart that stamps and projects the reuse `ContentKey` has not landed, so the cross-language federation seam stays open until the data lane projects the ledger row.
- Ripple: `data` `[REUSE_WIRE]` — Persistence `Query/Federation` federates the durable reuse ledger over the C#-seed `ContentKey` data stamps; XxHash128 is the one C#-owned identity both runtimes reproduce.

[SUBSTRAIT_FEDERATION_SEAM]-[BLOCKED]: Persistence `Query/Federation` decodes the portable Substrait binary plan and the ibis `to_sql` SQL the Python data query owner emits, federating multi-source pushdown over the existing entity graph rather than stranding the data plan-portability edge.
- Capability: `Query/Federation` consumes a Substrait binary plan or portable SQL as a language-agnostic query-plan interchange, lowering the federated multi-source pushdown onto the existing `ElementSet` query algebra so the data query owner's plan executes against the C# federated entity graph.
- Shape: A `Query/Federation` seam row consuming `python:data/tabular/query` `[WIRE]`, riding the one changefeed/PostGIS substrate with no second SQL engine.
- Unlocks: Cross-runtime query federation where one portable plan, authored in the Python data lane, executes over the C#-owned federated entity graph, the live seam the locked Substrait/ibis portability decision sets up.
- Anchors: `Rasm.Persistence/Query/Federation.cs` source-agnostic federated entity graph, `ElementSet` query algebra, and `FederatedPlan`/`PlanNode` cross-engine planner (the lowering target a portable plan would decode into); the Substrait binary plan, ibis `to_sql` portable SQL; the existing `python:runtime/transport` CRDT wire is the only current Python seam this extends. The Persistence-side lowering target (`ElementSet`/`SetExpr` algebra + `FederatedPlan`) is realized; the Substrait/ibis decode that translates a portable plan into a `SetExpr`/`PlanNode` is the cross-language counterpart the `python:data` query owner emits, not yet landed.
- Tension: Persistence carries no Python query/federation seam today; the Substrait plan-portability and `to_sql` SQLgate edges become live design pressure now so the data `[SUBSTRAIT_PORTABILITY]` and `[QUERY_IR_AND_SQLGATE]` edges mirror to a real C# counterpart rather than dangling.
- Ripple: `data` `[SUBSTRAIT_PORTABILITY]` (and `[QUERY_IR_AND_SQLGATE]`) — Persistence `Query/Federation` consumes the portable Substrait/ibis plan over the wire as the C# federation counterpart.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[PERSISTENCE_BIM_SYNC_CRDT]-[COMPLETE]: `Version/commits#CRDT_ALGEBRA` `CrdtField`/`Crdt.Apply` join-semilattice (RGA/OR-set/MV-register/PN-counter/LWW/EphemeralMap) plus `CommitGraph.MergeBase` lowest-common-ancestor and `Version/ledger#SYNC_TRANSPORTS` op-stream fold converge concurrent multi-party `ElementChange` edits against the commit-DAG common ancestor; Ripple: `Rasm.Bim` `[VERSIONED]`.
[DURABILITY_RECOVERY_OBSERVATORY]-[COMPLETE]: `Version/recovery.md` one recovery owner verifies backup + point-in-time restore across the closed `RecoveryRoute` substrate axis (`pg-pitr` base-backup-plus-WAL, `object-replica` content-addressed object-store, `snapshot-archive` sealed-checkpoint), folds per-substrate evidence into one backup-side `RecoveryFact` stream plus one restore-side `RestoreLedger` proving measured RPO/RTO against the AppHost-settled `RecoveryObjective`, the restore re-establishing CONTENT IDENTITY through the `Snapshots.Verify` ladder run in reverse to the real `RecoveryPoint` coordinate; no second PG backup tool, no `sqlite-paged`/`file-snapshot` engine, no `RecoveryDrill`.
[TRANSACTION_CONCURRENCY_CONTROL]-[COMPLETE]: `Query/transaction.md` makes isolation/MVCC-lock/savepoint/2PC/SQLSTATE-deadlock one closed axis on `StoreOp` reaching `StoreFault.Concurrency` 7001, the classifier reusing the shared `ConflictVerdict` and the 2PC in-doubt set draining through AppHost.
[ENVELOPE_ENCRYPTION_KMS]-[COMPLETE]: `Store/encryption.md` makes envelope DEK/KEK, cloud-KMS custody, rotation, and at-rest verification one closed axis across sqlite (promoted SQLCipher)/PG-TDE-verify/object-SSE, promoting the SQLCipher gate and the Personal-only `DemandsEncryption`.
[DATA_QUALITY_INTEGRITY_FRAMEWORK]-[COMPLETE]: `Store/quality.md` makes validation rules, cross-federation referential integrity, and anomaly/near-duplicate one closed `QualityRule` axis lowering each rule to the cheapest server-side site and emitting an `ElementSet`.
[BULK_ETL_INTERCHANGE_PIPELINE]-[COMPLETE]: `Query/pipeline.md` orchestrates large import/export/transform across Arrow/Parquet/CSV/IFC/Speckle as one back-pressured `BulkPipeline` fold over `ArrowChunk` with staging, in-flight validation, `ArtifactClassRow` idempotency, typed `PipelineReject` dead-letter, and `BulkShed` back-pressure.
[CDC_STREAMING_EGRESS]-[COMPLETE]: `Sync/egress.md` fans the one op-log changefeed to webhook/NATS/Kafka/RabbitMQ/Arrow-Flight-push/gRPC sinks with at-least-once delivery, typed dead-letter, and cursor-replay over the CloudEvents envelope, no second changefeed.
