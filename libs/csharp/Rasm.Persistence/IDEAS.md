# [PERSISTENCE_IDEAS]

The forward pool of higher-order concepts for the durable-state spine, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[PERSISTENCE_LIBRARY_TABLES]-[QUEUED]: Rasm.Persistence owns the durable content-hash identity and federation tables backing the MaterialLibrary catalogue, the ProfileCatalogue/ConnectionCatalogue registered rows, and the MaterialPropertyLibrary property sets so a material/profile/connection/property row persists with a content key and federates across the workspace, never an in-memory FrozenDictionary the runtime rebuilds.
- Capability: The persistence owner giving the MaterialId-keyed MaterialParameters rows, the ProfileId/ConnectionId-keyed catalogue rows, and the MaterialId-keyed MaterialPropertySet rows a durable content-hash identity and a federation table so the catalogue is a persisted, content-addressed library a workspace shares rather than a compile-time seed.
- Shape: A Rasm.Persistence library/property durable table set keyed by the Materials content keys (MaterialId/ProfileId/ConnectionId), the durable backing the in-memory FrozenDictionary catalogues seed from, the producer end of the durable-library federation concern.
- Unlocks: A material/profile/connection/property library row persists and federates with a content hash, a user-authored material admits into the durable catalogue, and the workspace shares one content-addressed library across host/web/companion consumers.
- Anchors: graph.md MaterialLibrary.Rows (the FrozenDictionary catalogue seed); profile.md/connection.md the ProfileCatalogue/ConnectionCatalogue registered-row tables (connection.md lines 92-101); properties.md MaterialPropertyLibrary.Rows (properties.md lines 176-192); ARCHITECTURE.md Rasm.Persistence content-hash identity/federation; the CLAUDE manifest Persistence-owns-durable-identity law.
- Ripple: counterpart of `Rasm.Materials` catalogue owners behind `[STEEL_DESIGN_OBJECT]`/`[SUSTAINABILITY_PROPERTY_DISCIPLINE]` (durable `MaterialLibrary`/`ProfileCatalogue`/`ConnectionCatalogue` rows).

[PERSISTENCE_BIM_SYNC_CRDT]-[QUEUED]: Persistence introduces a CRDT sync layer over the Bim BimWire OpLog face so concurrent multi-party edits to one federated BimModel converge through the typed ElementChange op-stream, the Bim BimCommit content-addressed commit-DAG providing the commit objects the CRDT merges against.
- Capability: A convergent multi-party BIM editing substrate: the OpLogWire ElementChange op-rows are the CRDT operations, the content-key fingerprint is the element identity, and the BimCommit common-ancestor merge fold reconciles divergent branches, collaborative BIM the pairwise diff cannot express.
- Shape: A Persistence Sync owner folding the Bim OpLogWire op-stream into a CRDT over GlobalId-keyed ElementChange operations through the `Sync/collaboration#CRDT_ALGEBRA` `MvRegister`/`opMerge` union, converging concurrent revisions against the BimCommit DAG common ancestor.
- Unlocks: Real-time collaborative model editing with convergent merge, the multi-author platform the federation diff and the BimCommit DAG together set up.
- Anchors: Exchange/wire#WIRE_PROJECTION OpLogWire op-log face (ONE_MODEL_THREE_FACES confirmed); Review/diff#MODEL_DIFF ElementChange; csharp:Rasm.Persistence/Sync/collaboration#CRDT_ALGEBRA; ARCHITECTURE.md `Sync ← Rasm.Bim/Exchange # [TRANSPORT]: OpLogWire ElementChange op-stream CRDT convergence` and `Sync ← Rasm.Bim/Review # [SHAPE]: BimCommit DAG common-ancestor merge substrate` seams; realizes the `VERSIONED` idea.
- Tension: The CRDT is the Persistence Sync owner's concern consuming the Bim OpLog face and the BimCommit commit objects: Bim produces the typed op-stream and the commit-DAG, Persistence owns convergence, so the merge algebra split stays clean between the two owners.
- Ripple: counterpart of `Rasm.Bim` `[VERSIONED]` idea (CRDT sync over the `OpLogWire` op-log face against the `BimCommit` DAG).

[FABRICATION_PROGRAM_DURABLE_ROWS]-[QUEUED]: A Rasm.Persistence fabrication-program durable schema persists the toolpath/nesting/workholding/post-dialect receipts as content-addressed rows so a posted cut-program, a nest layout, and a fixture setup survive as queryable durable records, closing the persistence gap the host-local Fabrication receipts have no durable owner for.
- Capability: Every Fabrication kernel produces a host-local receipt (Motion joint stream, Placement transforms + utilization, CutProgram GWord AST, Fixture exclusion set) that crosses only the in-process seam and dead-ends — no durable persistence owner, so a posted program or nest layout is recomputed every session with no provenance, diff, or version. This ripple-idea authors the Persistence-side durable schema: content-addressed fabrication-program rows (keyed by the same XxHash128 content identity the nesting Remnant already mints) carrying the toolpath/nesting/post-dialect/workholding receipts as queryable durable records the Persistence Schema/Store owners hold.
- Shape: A new Rasm.Persistence Schema/Store fabrication-program durable-row owner: content-addressed rows for the CutProgram AST, the nest Placement, the Fixture setup, and the resolved (Process, Machine, PostDialect) job axis, keyed by the XxHash128 content digest, composing the existing Persistence DDL/converters/identity surfaces — the durable owner the host-local Fabrication receipts project into at the wire boundary, never a Fabrication-side persistence reference.
- Unlocks: Durable, queryable, content-versioned fabrication programs (a posted G-code program diffable across revisions, a nest layout recalled by content key, a fixture setup persisted), and a second real consumer of the XxHash128 content identity beyond the in-folder nesting memo.
- Anchors: csharp:Rasm.Persistence/Schema/ddl#IDENTITY and Store the DDL/converters/identity/engine owners; Fabrication/Posting/program#CUT_PROGRAM CutProgram, Nesting/nfp#NESTING Placement/Remnant XxHash128 (`Remnant.Of` folds `XxHash128.HashToUInt128` over CCW vertex bytes, nfp.md), Nesting/workholding#WORKHOLDING Fixture; ARCHITECTURE.md `Schema ← Rasm.Fabrication/Posting # [WIRE]: CutProgram AST content-addressed durable-row projection` and `Schema ← Rasm.Fabrication/Nesting # [WIRE]: Placement / Remnant XxHash128 content-keyed durable row` seams; the host-local receipt wire boundary.
- Tension: The Fabrication receipts are host-local types the page law says never sit between wire and rail — the durable rows consume a WIRE PROJECTION of the receipts (a serialized program/nest/fixture shape), not the in-process records, so the persistence owner reads the projected wire form and Fabrication never references Persistence.
- Ripple: counterpart of `Rasm.Fabrication` `Posting/program#CUT_PROGRAM` (`CutProgram`) + `Nesting/nfp#NESTING`/`Nesting/workholding#WORKHOLDING` owners (durable program/nesting/fixture rows).

[DURABILITY_RECOVERY_OBSERVATORY]-[QUEUED]: One recovery owner verifies backup, PITR, and replication across every engine to prove RPO and RTO, never spawning a second PG backup tool.
- Capability: The durability owner closing the backup/DR/PITR/replication gap that today reaches no further than the sqlite paged-backup primitive, giving every engine a measured restore objective and a replication-verification path instead of an unverified durability assumption.
- Shape: A `Version/recovery.md` owner minting a `RecoveryProfile` SmartEnum crossed with a `BackupKind` Union and a `RecoveryObjective`, where the PG arm is verification-only through `ClusterConfig.Verify` on `StoreProfile.Replication`, the sqlite arm drives `SqliteMaintenance.Backup`, the object arm carries `ObjectResidence` plus `LegalHold`, and `RecoveryDrill` stamps measured RTO; per-engine restore evidence folds into one typed `RecoveryFact` stream feeding `StoreFact` while `RestoreReceipt`/`TransferReceipt` stay typed.
- Unlocks: Verified disaster recovery, measured RPO/RTO, and restore-drill evidence the workspace audits rather than assumes.
- Anchors: `StoreCeremony.Restore`, `StoreProfile.Replication`, `SqliteMaintenance.Backup`, `ClusterConfig.Verify`, `SyncCursor.Lsn`, and the typed `RestoreReceipt`/`TransferReceipt`/`StoreEvidence` carriers; `Npgsql` drives PG verification while admitted `AWSSDK.S3` and `Azure.Storage.Blobs` carry object-store residence and transfer; the `libs/csharp/.planning` branch-tier `DR_OBJECTIVE_FABRIC` and the `Rasm.AppHost/Runtime` `ResolvedProfile` DR-objective inputs are the touch-point seams; `Rasm.Persistence/README.md` gains a `RECOVERY` row and `ARCHITECTURE.md` a `Version/Recovery.cs` node.

[TRANSACTION_CONCURRENCY_CONTROL]-[QUEUED]: One transaction owner makes isolation, locking, savepoints, 2PC, and deadlock classification a closed axis on `StoreOp`, reaching the dormant `StoreFault.Concurrency` rail.
- Capability: The concurrency owner closing the isolation/MVCC-lock/savepoint/2PC/SQLSTATE-deadlock-classification gap, projecting concurrency state onto the existing fault and retry surfaces rather than a parallel concurrency error family.
- Shape: A `Query/transaction.md` owner minting a `TxnScope` `IsolationPolicy` SmartEnum crossed with a `LockMode` Union plus a `Savepoint` ladder, all as an arm-family on the `StoreOp` 8-case surface routed through `StoreRail.Run`; it projects `StoreProfile.ConcurrencyToken` into `StoreFault.Concurrency` 7001, classifies SQLSTATE 40001/40P01 into a verdict the existing `EnableRetryOnFailure` owner consumes, and folds 2PC through `PrepareTransactionAsync`.
- Unlocks: Concurrent correctness, SKIP LOCKED work queues, nested rollback, and cross-store commit.
- Anchors: the `StoreOp` 8-case surface plus `StoreRail.Run`, `StoreFault.Concurrency` 7001 plus `From(Error)` and `TransactionCommittedAsync`, and `StoreProfile.ConcurrencyToken` plus `EnableRetryOnFailure`; `Npgsql`, `Microsoft.Data.Sqlite`, and `linq2db.EntityFrameworkCore` carry the per-engine transaction surfaces; the `Rasm.AppHost/Runtime` drain surfaces the 2PC in-doubt set and the `libs/csharp/.planning` branch-tier owns cross-store commit-coordination; `Rasm.Persistence/README.md` gains a `TRANSACTION` row and `ARCHITECTURE.md` a `Query/Transaction.cs` node.

[ENVELOPE_ENCRYPTION_KMS]-[QUEUED]: One key-management owner makes envelope encryption, KMS custody, rotation, and at-rest verification a closed axis across sqlite, PG, and object-store, promoting the research-held SQLCipher gate and the Personal-only `DemandsEncryption`.
- Capability: The key-custody owner closing the envelope DEK/KEK, cloud-KMS, and rotation gap, binding the embedded sqlite cipher and the per-tenant isolation to a real key source rather than a stubbed encryption demand.
- Shape: A `Store/encryption.md` owner minting a `KeyEnvelope` `KmsProvider` SmartEnum crossed with an `EnvelopeScope` Union plus a `RotationPolicy`, where the sqlite arm drives `EncryptionGate.Sqlcipher` from a KMS-unwrapped DEK bound through its PRAGMA key/rekey ceremony, the PG arm verifies the TDE GUC read-only via `ClusterConfig.Verify`, the object arm carries SSE-KMS, and the Personal-only `DemandsEncryption` reads as mandatory; SQLCipher relocates out of Research into this owner.
- Unlocks: At-rest encryption, cloud-KMS custody, per-tenant isolation, and rotation.
- Anchors: `DemandsEncryption` Personal-only, `EncryptionGate.Sqlcipher` (relocated from Research, plus its Ceremony rows and an `EnvelopeKeyring` unwrap delegate), `ObjectResidence` SSE, and `TenancyModel.Rls` plus `ClusterConfig.Verify`; the admitted `AWSSDK.KeyManagementService` (`.api/api-aws-kms.md`), `Azure.Security.KeyVault.Keys` (`.api/api-azure-keyvault.md`), and `Google.Cloud.Kms.V1` (`.api/api-google-kms.md`) carry the wrap/unwrap members under the README `[ENCRYPTION_KMS]` group beside the admitted `AWSSDK.S3`/`Azure.Storage.Blobs`/`Google.Cloud.Storage.V1`; the `Rasm.AppHost/Runtime` per-open key handle becomes the KMS-unwrap port through the authored `Store/encryption ⇄ Rasm.AppHost/Runtime # [PORT]: KMS-unwrap port` seam and the `libs/csharp/.planning` branch-tier owns key custody; `Rasm.Persistence/README.md` gains an `ENCRYPTION` row and `ARCHITECTURE.md` a `Store/Encryption.cs` node plus the KMS-unwrap seam.

[DATA_QUALITY_INTEGRITY_FRAMEWORK]-[QUEUED]: One constraint owner makes validation rules, cross-federation referential integrity, and anomaly detection a closed rule axis lowering to the cheapest server-side site.
- Capability: The integrity owner closing the declarative-rule, cross-document referential-integrity, and anomaly/near-duplicate gap that is wholly absent today, lowering each rule to the least-cost enforcement site rather than re-checking in process.
- Shape: A `Store/quality.md` owner minting a `QualityRule` Union (`NotNullSet`/`UniqueAcross`/`ReferentialEdge`/`RangeBound`/`RegexShape`/`JsonSchemaShape`/`StatisticalAnomaly`/`NearDuplicate`) that a `QualityPlan` fold lowers to CHECK/EXCLUDE/FK constraints, `jsonb_matches_schema`, a changefeed-fold, or a DuckDB pass, riding `CrossDocLink`/`StalePins` and emitting `ElementSet`.
- Unlocks: Enforced integrity, quality gates, and cross-document referential consistency.
- Anchors: `SetExpr`/`ElementSet` plus `RulePlan` and `CrossDocLink`/`StalePins`, `jsonb_matches_schema`, `ContentChunk.ShortTag`, and `btree_gist` EXCLUDE; the admitted `pg_jsonschema` (`.api/api-pg-server-bgworkers.md` `jsonb_matches_schema`), `DuckDB.NET.Data.Full` (`.api/api-duckdb.md`), and `Pgvector.EntityFrameworkCore` (`.api/api-pgvector-ef.md`) carry the schema-validation, columnar-anomaly, and near-duplicate sites; the `Rasm.Bim/Model` authors the IFC validation rules as `QualityRule` rows and `Rasm.Compute` is the geometry-derived anomaly rule source through the authored `Store/quality ← Rasm.Bim/Model` and `Store/quality ← Rasm.Compute` seams; `Rasm.Persistence/README.md` gains a `QUALITY` row and `ARCHITECTURE.md` a `Store/Quality.cs` node.

[BULK_ETL_INTERCHANGE_PIPELINE]-[QUEUED]: One pipeline owner orchestrates large import/export/transform across Arrow, Parquet, CSV, IFC, and Speckle with staging, validation, idempotency, and back-pressure.
- Capability: The orchestration owner closing the ETL/bulk-pipeline gap that today holds partial primitives but no orchestrator, turning resumable staging and in-flight validation into one back-pressured fold instead of ad hoc batch loops.
- Shape: A `Query/pipeline.md` owner minting a `PipelineStage` Union (`Extract`/`Stage`/`Validate`/`Transform`/`Load`/`Reconcile`) that a `BulkPipeline` drives as a back-pressured fold over `ArrowChunk`, made idempotent via chunk index plus an idempotency-dedup `ArtifactClassRow`, with a typed dead-letter `PipelineReject`; `ArrowEgress` and the `BulkRoute` op-log terminate the flow.
- Unlocks: Resumable staging, in-flight validation, idempotent re-runs, dead-letter capture, and back-pressure.
- Anchors: `ArrowChunk`/`ArrowPlane`/`ArrowEgress` plus `BulkRoute`/`BulkShed`, `TabularExportSpec`/`LakehouseCatalog`, `ContentChunker` plus the idempotency-dedup `ArtifactClassRow`, and `SpeckleSend`/`Receive` plus `ScheduleImport.ReadXer`; the admitted `Apache.Arrow.Flight`/`Apache.Arrow`/`Apache.Arrow.Adbc` (README `[COLUMNAR_ARROW]`, `.api/api-arrow.md` covering Flight and ADBC) carry the full-streaming plus Flight-egress activation and the `ArrowPlane` C-data path beside `DuckDB.NET.Data.Full` (`.api/api-duckdb.md`) and `Sep` (`.api/api-sep.md`); the `Rasm.Compute/Exchange` confirms parse-to-canonical-bytes for `Extract` and the `typescript:interchange` leg needs the Flight ticket plus ADBC schema; `Rasm.Persistence/README.md` gains a `PIPELINE` row and `ARCHITECTURE.md` a `Query/Pipeline.cs` node and the authored `Sync/pipeline ⇄ Rasm.Compute/Exchange # [PORT]: parse-to-canonical-bytes (Extract)` seam.

[CDC_STREAMING_EGRESS]-[QUEUED]: One egress owner fans the op-log changefeed to external sinks with at-least-once delivery, dead-letter, and cursor-replay, without a second changefeed.
- Capability: The egress owner closing the CDC/streaming-egress gap where `LineageCdc.Feed` filters internally but pushes nowhere, draining one durable op-log to webhook, broker, and Arrow-Flight-push sinks rather than minting a parallel feed.
- Shape: A `Sync/egress.md` owner minting an `EgressSink` SmartEnum (webhook/nats/kafka/rabbitmq/arrow-flight-push/grpc-stream) crossed with `DeliveryGuarantee` and `SinkScope`, where an `EgressPump` drains the op-log past a durable `SyncCursor`, projects via `LineageCdc.Feed`, advances on durable ack, and routes failures to a typed `EgressDeadLetter`; the envelope reuses `CdcEnvelopeWire` (its `TraceContext` realizing `ONE_DISTRIBUTED_TRACE`) inside CloudEvents.
- Unlocks: Real-time propagation, delivery guarantees, redaction scoping, dead-letter capture, and replay.
- Anchors: `LineageCdc.Feed`/`CdcEnvelope`, `OpLog`/`SyncCursor`/`OpLogEntryWire`, `ArrowEgress`/`ArrowChunk`, the AppHost `OutboundHop`, and `CdcEnvelopeWire`; the admitted `Confluent.Kafka` (`.api/api-kafka.md`) and `CloudNative.CloudEvents`/`.Kafka`/`.SystemTextJson` (`.api/api-cloudevents.md`) carry broker delivery and the standard envelope under the README `[STREAMING_EGRESS]` group beside `Apache.Arrow.Flight`; the `python:runtime/transport` leg decodes the CloudEvents `CdcEnvelopeWire` and the `Rasm.AppHost/Runtime` registers each webhook/grpc egress as a keyed `OutboundHop` through the authored `Sync/egress ⇄ Rasm.AppHost/Runtime # [PORT]: keyed OutboundHop egress` seam; `Rasm.Persistence/README.md` gains an `EGRESS` row and `ARCHITECTURE.md` a `Sync/Egress.cs` node.
- Tension: `Confluent.Kafka` is heavy and droppable to webhook/grpc/Flight while retaining the CloudEvents envelope if broker scope is rejected; the `[ONE_OUTBOX_EGRESS_SPINE]` branch idea binds this egress, the AppHost `Wire/outbox` relay, and the AppHost durable-orchestration dispatch as keyed `OutboundHop` consumers over the one `CdcEnvelopeWire`, so a second outbox owner or a relay-side re-pack is the drift defect.

[REUSE_WIRE]-[QUEUED]: Persistence `Query/Federation` federates the durable content-addressed reuse ledger over the `python:data`-stamped `ContentKey`, the XxHash128 seed being the one C#-owned identity both runtimes reproduce bit-identically rather than a second per-runtime hash.
- Capability: `Query/Federation` receives the data-stamped `ContentKey` on the existing wire and federates the durable reuse ledger; the XxHash128 content-key seed is the single C#-owned identity the Python data outputs reproduce, so a reuse row stamped in the data lane resolves against the durable federation table without re-minting.
- Shape: A federation seam row on `Query/Federation` consuming the `python:data` `ContentKey`, riding the existing op-log/content-addressed-snapshot substrate with no new engine.
- Unlocks: A durable, content-addressed reuse ledger federated across the C# durable spine and the Python data lane off one shared identity seed, the producer/consumer pair the architecture law assigns C# Persistence as the sole durable-identity owner.
- Anchors: `Rasm.Persistence/Query/Federation.cs` source-agnostic federated entity graph, `Version/Commits` content-addressed commit-DAG, the XxHash128 content-key seed, the existing `python:runtime/transport` wire; ARCHITECTURE.md content-hash identity/federation owner.
- Ripple: `data` `[REUSE_WIRE]` — Persistence `Query/Federation` federates the durable reuse ledger over the C#-seed `ContentKey` data stamps; XxHash128 is the one C#-owned identity both runtimes reproduce.

[SUBSTRAIT_FEDERATION_SEAM]-[QUEUED]: Persistence `Query/Federation` decodes the portable Substrait binary plan and the ibis `to_sql` SQL the Python data query owner emits, federating multi-source pushdown over the existing entity graph rather than stranding the data plan-portability edge.
- Capability: `Query/Federation` consumes a Substrait binary plan or portable SQL as a language-agnostic query-plan interchange, lowering the federated multi-source pushdown onto the existing `ElementSet` query algebra so the data query owner's plan executes against the C# federated entity graph.
- Shape: A `Query/Federation` seam row consuming `python:data/tabular/query` `[WIRE]`, riding the one changefeed/PostGIS substrate with no second SQL engine.
- Unlocks: Cross-runtime query federation where one portable plan, authored in the Python data lane, executes over the C#-owned federated entity graph, the live seam the locked Substrait/ibis portability decision sets up.
- Anchors: `Rasm.Persistence/Query/Federation.cs` source-agnostic federated entity graph and `ElementSet` query algebra, the Substrait binary plan, ibis `to_sql` portable SQL; the existing `python:runtime/transport` CRDT wire is the only current Python seam this extends.
- Tension: Persistence carries no Python query/federation seam today; the Substrait plan-portability and `to_sql` SQLgate edges become live design pressure now so the data `[SUBSTRAIT_PORTABILITY]` and `[QUERY_IR_AND_SQLGATE]` edges mirror to a real C# counterpart rather than dangling.
- Ripple: `data` `[SUBSTRAIT_PORTABILITY]` (and `[QUERY_IR_AND_SQLGATE]`) — Persistence `Query/Federation` consumes the portable Substrait/ibis plan over the wire as the C# federation counterpart.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
