# [TS_REGION_MAP_SEAM_SPLITS]

TS branch ledger — row shapes and protocol live in the [suite standard](../../../csharp/.planning/README.md) ledger-protocol section. Every seam records the wire SHAPE mechanics at the C# owner and the consumption consequence at the TS domain; the branch authors no shape. Anchors are bare-domain-qualified. The browser stratum is two folders: `ui` (UI/UX/components, the AppUi-analog library) and `platform` (the AppHost-analog SPA infrastructure entry); the SPA entry `platform` composes `ui`, never the reverse.

[C#->TS WIRE SEAMS] (eleven, the branch consumes; no TS-authored shape):

- Lifecycle and drain: mechanics at AppHost/lifecycle-and-drain#TS_PROJECTION (json-stj phase and drain shapes); consequence at projection/fold-algebra#FOLD_ALGEBRA (RuntimeFeed fold, surfaced through the ui binding).
- Health and degradation: mechanics at AppHost/health-and-degradation#TS_PROJECTION (json-stj health and degradation shapes); consequence at projection/fold-algebra#FOLD_ALGEBRA (HealthStore fold gating reachable ui surfaces).
- Support bundles: mechanics at AppHost/support-bundles#TS_PROJECTION (json-stj support trigger, manifest, and receipt shapes); consequence at interchange/gateway-and-quarantine#GATEWAY_AND_QUARANTINE (capture-support verb on CommandGateway).
- Runtime ports envelope: mechanics at AppHost/runtime-ports#TS_PROJECTION (json-stj package marker, HLC stamp, receipt-envelope carrier); consequence at projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE (ReceiptEnvelopeCarrier binding every structured-text payload, feeding the runtime and evidence folds); the converter-precedence and HLC-stamp discipline reads from AppHost/runtime-ports#WIRE_LAW, never for token grounding.
- Snapshot codecs: mechanics at Persistence/snapshot-codecs#TS_PROJECTION (messagepack header, delta, catalog, empty-extension shapes plus the GeoJSON projection); consequence at interchange/codec-rails#CODEC_RAILS (binary rail and embedded-geometry GeometryRail) and projection/fold-algebra#FOLD_ALGEBRA (SnapshotFeed fold).
- Sync and collaboration: mechanics at Persistence/sync-collaboration#TS_PROJECTION (messagepack cursor, op-log, segment, presence shapes); consequence at interchange/codec-rails#CODEC_RAILS (binary rail, multi-object segment stream) and projection/fold-algebra#FOLD_ALGEBRA (ConflictPresenceStore fold).
- Remote lane: mechanics at Compute/remote-lane#TS_PROJECTION (proto service shapes, transport-capability shape, fault-detail and artifact-frame shapes; client-stream and bidi structurally excluded on the browser row, but the server-streamed ArtifactFrameWire type is browser-reachable); consequence at interchange/transport#TRANSPORT_AND_CLIENTS (WireTransport, generated clients, TransportCapabilityWire) plus interchange/codec-rails#CODEC_RAILS (FaultDetailRail reconstructing the full ComputeFault/StoreFault/HopFault/ConfigError set as one Match.tagsExhaustive family, and ArtifactFrameRail reassembling the server-streamed frames into a Crc32-verified XxHash128 content-addressed blob) and interchange/gateway-and-quarantine#GATEWAY_AND_QUARANTINE (control-service verbs on CommandGateway).
- Progress and observation: mechanics at Compute/progress-and-observation#TS_PROJECTION (proto progress-phase and mark shapes); consequence at projection/fold-algebra#FOLD_ALGEBRA (ProgressStore monotonic fold).
- Receipts and benchmarks: mechanics at Compute/receipts-and-benchmarks#TS_PROJECTION (json-stj receipt union, host-fingerprint and benchmark-claim shapes); consequence at ui/render-surfaces#RENDER_SURFACES (BenchmarkRoute fingerprint-gated) and projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE (ReceiptStore fold).
- Commands and availability: mechanics at AppUi/commands-availability#TS_PROJECTION (json-stj payload, outcome, intent, availability, invocation, receipt shapes); consequence at interchange/gateway-and-quarantine#GATEWAY_AND_QUARANTINE (CommandGateway and IntentRegistry) and projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE (AvailabilityStore gate, a transport-free read fold the gateway reads across the package boundary).
- Diagnostics and evidence: mechanics at AppUi/diagnostics-evidence#TS_PROJECTION (json-stj evidence, surface, native-asset, skew-band, row, timeline shapes; the evidence row carries the envelope whole with no literal geometry field); consequence at ui/render-surfaces#RENDER_SURFACES (EvidenceTimelineRoute) and projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE (EvidenceFeed HLC fold; SkewBand is elevated to a first-class confidence-interval projection output rendered without recomputing the fold, embedded geometry decoded transitively through the interchange GeometryRail).

[TS-INTERNAL CROSS-DOMAIN SEAMS] (branch-authored; no C# wire shape):

- Availability read gate: AvailabilityStore (projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE) is a transport-free read fold; CommandGateway (interchange/gateway-and-quarantine#GATEWAY_AND_QUARANTINE) reads it as a dial-time gate through the invoke R channel so a disabled command never fires — co-location follows altitude/outbound-effect, not read-dependency, so the dialing gateway lives in the interchange folder (the transport-owning domain) and never in the platform-neutral projection fold interior. Because BOTH folders are stratum neutral, the stratum graph does not separate this intra-neutral seam, so the projection/** no-restricted-imports ban on @connectrpc/* is the SOLE mechanical guard keeping @connectrpc/connect-web out of the fold tier; the read is an intra-package module edge, not a published-package boundary.
- AuthSession credential stamp: AuthSession (platform/host-runtime#HOST_RUNTIME, arctic OIDC-PKCE + SubscriptionRef session fold + Schedule silent refresh) owns the per-call browser bearer token; WireTransport (interchange/transport#TRANSPORT_AND_CLIENTS) reads the live token via AuthSession.tokenHeader (the assembled Bearer string, the Redacted unwrapped inside the platform folder and never crossing the seam) over a captured Effect.runtime snapshot, resolving the token and the active span in ONE Runtime.runPromise per call as the credential row on the polymorphic correlation+trace+credential interceptor stamp, never a closed-over snapshot and never a per-call double promise; interchange declares no OIDC dependency; the stamp is designed-only growth gated on the cross-origin activation.
- Auth session view leaf: AuthSession status (platform/host-runtime#HOST_RUNTIME) is read by the login-logout / session-status leaf subscriber (ui/binding#BINDING) through the one AtomBinding, never a second state binding; an expired-or-rejected token folds to the interchange FaultDetail typed re-auth fault, never a silent decode-side redirect. This is a ui<-platform read across the intra-browser seam: the ui surfaces a host-owned session value through the one binding, and platform never reaches into a ui state binding.
- Typed config provider: RuntimeConfig (platform/host-runtime#HOST_RUNTIME) owns the one effect Config schema and the browser ConfigProvider.fromJson provider layer; WireTransport reads the api base url, AuthSession reads the OIDC authority and client id, MetricRegistry/SelfTelemetry (platform/platform-substrate#PLATFORM_SUBSTRATE) read the collector OTLP endpoint, and RemoteConfig (platform/feature-flags-config#FEATURE_FLAGS_CONFIG) reads the remote-config endpoint.
- Stream reconnect policy: StreamPolicy (projection/fold-algebra#FOLD_ALGEBRA) owns the one Schedule reconnect-and-backoff vocabulary and the Stream operator posture every fold composes; the staleness-forward retry value the availability cluster carries grounds in this policy, so reconnect mechanics live on the fold owner and the staleness consequence reads the same value rather than re-deriving a retry loop.
- Off-main-thread reassembly: DecodeWorkerPool (platform/platform-substrate#PLATFORM_SUBSTRATE) runs the ArtifactFrameRail (interchange/codec-rails#CODEC_RAILS) server-stream frame stitch, the per-frame Crc32 verify, and the whole-artifact XxHash128 content-key derivation off the main thread via Worker.makePool, each frame payload crossing the worker port as a Transferable Uint8Array; co-location follows the offload altitude — the rail owns the content-addressing mechanics in the neutral wire interior, the pool owns the main-thread offload in the platform domain.

[INTRA-BROWSER COMPOSITION SEAMS] (ui<->platform inside stratum browser; no C# wire shape):

- Platform composes UI: CompositionRoot (platform/host-runtime#HOST_RUNTIME) is the SPA application entry that composes the ui library (AtomBinding, the render surfaces, the component system) together with interchange + projection into one Layer graph and one runtime; the dependency direction is platform->ui only — platform/** may import ui/**, ui/** must NEVER import platform/**. The ui surfaces read host-owned values (AuthSession status, the LocalPersistence last-good cell, the ServiceWorkerHost/CrashTelemetry refresh affordances) only through the one AtomBinding, never by importing a platform owner; a ui->platform import is the named browser-internal coupling defect the ui/**-specific no-restricted-imports rule rejects (RULE_ENFORCEMENT row [7]), mirroring the inward interchange<-projection direction in the neutral stratum.
- AppRouter navigation guard: NavigationGuard (platform/routing-navigation#ROUTING_NAVIGATION) gates a route transition on AuthSession.status (platform/host-runtime#HOST_RUNTIME) and the projection AvailabilityStore (projection/envelope-and-evidence#ENVELOPE_AND_EVIDENCE) read fold, so a route to a disabled or unauthenticated surface never resolves; the ui surfaces a route-resident value through the one AtomBinding read, never duplicating route state into component state; AppRouter admits ZERO routing package — Effect Schema + nuqs + a hand-held history SubscriptionRef own it.
- Background-sync redial drain: BackgroundSyncReplay (platform/service-worker#SERVICE_WORKER) drains the platform-substrate LocalPersistence offline command queue (platform/platform-substrate#PLATFORM_SUBSTRATE, LocalPersistence.offlineQueue) into the interchange CommandGateway (interchange/gateway-and-quarantine#GATEWAY_AND_QUARANTINE) on redial — the SAME offline-queue seam platform-substrate names, ServiceWorkerHost owning the SW-triggered redial drive and not a parallel queue; BuildPipeline (platform/platform-substrate#PLATFORM_SUBSTRATE) emits the SW worker asset at build time while ServiceWorkerHost owns its runtime install/activate/cache lifecycle — distinct altitudes, one concern each, never a two-owner split.
- Crash telemetry sink: CrashTelemetry (platform/error-boundary#ERROR_BOUNDARY) captures the React error-boundary fault (through ErrorBoundaryBinding over react-error-boundary) and the global window.onerror/unhandledrejection, reconstructs each uncaught fault as an interchange FaultDetail (interchange/codec-rails#FAULT_FAMILY) HopFault/ConfigError arm (never a parallel error type), and ships a sanitized CrashReport through SelfTelemetry (platform/platform-substrate#PLATFORM_SUBSTRATE) to the collector — the collector is the only telemetry path, never a third path; the ui error fallback reads the recovery-affordance SubscriptionRef through the one AtomBinding.
- Remote-config flag read: RemoteConfig (platform/feature-flags-config#FEATURE_FLAGS_CONFIG) is the browser READ-side consumer of the services persistence FeatureFlags vocabulary (services/persistence#WORK_AND_SIGNALS) — the percentage-bucket/variant Schema.Literal axis is declared ONCE in services and consumed as settled vocabulary, NEVER re-authored (the anti-spam cluster law); FlagEvaluation is a total Match dispatch over that bucket/variant resolution, RemoteConfig's only platform-local Schema is the FlagSet decode shape, and the ui reads a flag through the one AtomBinding.
- Web-vitals instrument feed: PerformanceBudget (platform/web-vitals#WEB_VITALS) captures the Core Web Vitals over a Schema.Literal VitalMetric axis and feeds the EXISTING MetricRegistry Core-Web-Vitals instrument rows (platform/platform-substrate#PLATFORM_SUBSTRATE) — MetricRegistry owns the instrument rows and PerformanceBudget owns the capture + budget-gating mechanics, never a parallel metric construction; the budget-breach span ships through SelfTelemetry to the collector, and capture is via the native PerformanceObserver only, ZERO web-vitals package admitted.

[TS-INTERNAL NODE-TIER SEAMS] (within services):

- Runner placement substrate: RunnerBackplane and ScheduledWork (services/internal-rpc#RUNNER_AND_SCHEDULING) own the runner-protocol/message-storage/runner-storage/runner-health rows, the snowflake id source, the cluster singletons, and the durable cron; ClusterEngine (services/durable-execution#DURABLE_EXECUTION) layers WorkflowEngine over the Sharding shard-manager those rows compose; SqlBoundary (services/persistence#STORE_BOUNDARY) is the one PgClient surface both the message and runner stores ride, and HybridSearch (services/hybrid-search#HYBRID_SEARCH) rides the same client.
- Stack topology contract: StackOutputs (services/provisioning#PROVISIONING) reads the .NET-published server-topology outputs through one typed StackReference (Postgres/Timescale DSN, object-store bucket+endpoint, Redis URL, collector OTLP endpoint, SPA-host origin); SqlBoundary and the RunnerBackplane SQL stores consume the DSN, ObservabilityStack consumes the OTLP endpoint, and the platform browser entry consumes the SPA origin — the topology contract is infrastructure config a deployment publishes, structurally distinct from the eleven runtime wire contracts and crossing NONE of them. The provisioning owners are reachable only behind the ./provisioning exports subpath so the @pulumi/* closure never loads on the durable runtime hot path; provisioning is INSIDE the consuming node domain, so the prior phantom-wire-dependency and crosses-no-wire caveats vanish.

[INTER-DOMAIN DEPENDENCY-DIRECTION SEAMS] (folder-scoped no-restricted-imports strata inside one package):

- Neutral interior: the interchange folder (stratum neutral) and the projection folder (stratum neutral) import only neutral folders; projection imports interchange decoded shapes, dials nothing, and bans @connectrpc/* via the projection/** no-restricted-imports rule.
- Browser publication: the ui folder and the platform folder (both stratum browser) import only browser + neutral — interchange, projection, @effect/platform-browser, react, @effect-atom/*, maplibre-gl, @deck.gl/*, arctic, react-error-boundary, workbox-build, workbox-window, vite-plugin-pwa, nuqs — and a browser folder importing a node folder is the named coupling defect the folder-stratum lint rejects. WITHIN the browser stratum the dependency direction is one-way: platform/** -> ui/** is ALLOWED (the SPA entry composes the UI library), ui/** -> platform/** is FORBIDDEN by a ui/**-specific no-restricted-imports rule banning every ../platform and platform/** import path (RULE_ENFORCEMENT row [7] ui-no-platform-import) — ui is the lower AppUi-analog library, platform the AppHost-analog entry above it, so a ui->platform import is the named browser-internal coupling defect, mirroring the inward interchange<-projection direction. The browser stratum is authored as TWO rules — the shared browser<->neutral/node rule applied to both folders PLUS the ui/**-specific platform ban — never one combined { ui/**, platform/** } group, which silently passes a ui->platform import.
- Node publication: the services folder (stratum node, ./node entry) imports interchange + projection; a node folder importing a browser folder is the named coupling defect the folder-stratum lint rejects; the @pulumi/* set is reachable only behind the ./provisioning subpath export.

[CROSS-BRANCH PRECONDITION-DAG SEAMS] (TS-downstream; SIGNATURE_LAW forbids pre-authoring an unsettled wire shape):

- GlbViewport visual render (four-end, TS is the LAST node): the GLB byte/artifact layer is NOT on this DAG — interchange ArtifactFrameRail already reassembles the server-streamed GLB artifact bytes into a Crc32-verified XxHash128 content-addressed blob over the existing #TS_PROJECTION fence; this DAG gates only the WebGL MESH RENDER. (a) C# remote-lane#TS_PROJECTION must promote GeometryPayload(mesh)/MeshTensor from [PROTO_VOCABULARY] into the projection fence — the single true blocker and smallest amendment, since the mesh bytes already cross the proto wire via Infer/Solve and only the fence transcription is missing, emitted UPSTREAM to the C# branch; (b) C# interchange.md authoring DISCHARGED (SharpGLTF + GeometryGym centrally pinned and catalogued); (c) the Python libs/python/compute branch authors the IFC->GLB two-hop companion (IfcConvert->GLB) and discharges its IfcOpenShell placeholder — a precondition the TS branch OBSERVES, never forces; (d) only then ui (ui/render-surfaces REFINEMENT_HORIZON / ui/render-surfaces#RESEARCH) admits a WebGL viewer and authors GlbViewport with a Schema.Literal renderer-backend axis admitting three/babylon/model-viewer and a webgpu literal. ZERO WebGL packages admitted until (a) exists.
- GraphFork CRDT / branch-merge-time-travel: waits on the C# sync-collaboration#MERGE_LAW op-log amendment before projection ConflictPresenceStore (fold-algebra#FOLD_ALGEBRA) folds a CRDT op vocabulary; until it lands the C#-aligned product hand-rolls that surface, never the TS branch.
- Capability-descriptor SDK + MCP client: waits on the C# capability-registry#CAPABILITY_CATALOG descriptor before interchange transport#CODEGEN_TOOLING emits the typed effect-classed capability codegen row through one more buf-style codegen stage.
- BCF anchor algebra (node-id / sub-entity / param-path / world-point+view): waits on the C# annotation#ANCHOR_ALGEBRA before a ui render-surface + an interchange anchor-codec; until it lands the TS branch authors no anchor shape.
- Standing-query window vocabulary: waits on the C# query-rail#STANDING_QUERY window vocabulary before projection fold-algebra#FOLD_ALGEBRA lands the tumbling/sliding/session + watermark arm on the keyedFold algebra.
