# [PY_RUNTIME_IDEAS]

The forward pool of higher-order concepts for `runtime`, grounded in the folder's domain and the monorepo purpose. Each open idea is a card — a bracketed slug, the capability, what it unlocks, and the gap or modern technique it draws on — and spawns one or more `TASKLOG.md` tasks. A finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition. The design pool is realized at the C# parity bar: the metrics spine, the subinterpreter offload lane, the content-addressed lane cache, the structural drift guard, the proto-transcode codec, the credential-axis decode, the MessagePack op-log decode, the capability-SDK consume, the seed-reproduction parity binding, and the cp315 catalogue capture are closed. The open pool is the inbound observability frontier the cross-`libs/` concert names this folder to own — the W3C trace-context inbound extract, the C#-minted causal/tenant inbound frame, the OTLP span/metric egress wiring the receipts and metrics owners assume installed, the streaming-transport and gRPC client-span depth the catalogues admit but no fence consumes — plus the orphaned-package collapse the `[CREDENTIAL_PEM]` decode left, and the op-log LZ4 decompression genuinely [UPSTREAM-BLOCKED] on the cp315 `lz4` wheel.

## [1]-[OPEN]

[TRACE_CONTEXT_INBOUND_EXTRACT]:
- Gain the inbound extract-and-continue primitive on `observability/receipts#RECEIPT` so the companion seeds its root span from the C#-minted `traceparent`/`tracestate` carrier on the inbound gRPC metadata rather than minting an independent root, the `serve#SERVE` interceptor reading the carrier off `ServicerContext.invocation_metadata()` and the receipts owner extracting through `opentelemetry.propagate.extract` over a `dict[str, str]` carrier into a `Context` it attaches before the measured operation opens its span.
- Unlocks one correlated distributed trace across the three runtimes: a browser INP span, the C# `ActivitySource` solve span, and the companion tessellation/serve span join under one trace id, so a regression reads end-to-end instead of three disconnected per-process root traces; the `trace_context` processor today only injects the active span's `trace_id`/`span_id` and never extracts the parent, so the trace flattens per process exactly as the cross-`libs/` `ONE_DISTRIBUTED_TRACE` names.
- Draws on the W3C Trace Context propagation standard and the branch `opentelemetry-api.md` `propagate.extract`/`get_global_textmap`/`TextMapPropagator`/`Context` surface (catalogue-backed, no new admission); the producer `csharp:Rasm.AppHost/observability/diagnostics-and-telemetry#CORRELATION_SPINE` registers the `TraceContextPropagator`/`BaggagePropagator` composite as `Propagators.DefaultTextMapPropagator`, and the companion extract-and-continue at the wire is the single un-realized leg — a fresh root span in the peer is the named cross-language drift defect.

[CAUSAL_TENANT_INBOUND_FRAME]:
- Add the `context/admission` causal/tenant inbound frame the companion threads from the inbound receipt slot: `serve#SERVE` decodes the C#-minted `hlc_physical`/`hlc_logical` two-half stamp and the `tenant` partition off the `ReceiptEnvelope` slot, and `context/admission#CONTEXT` carries the decoded `(Hlc, Tenant)` frame on `RuntimeContext` so every receipt, lane, and metric the companion emits attributes to the same causal/tenant frame the host minted, never a re-minted stamp or a second tenant scheme.
- Unlocks one causal-and-tenant frame the whole companion orders and attributes by — a receipt produced in the companion carries a stamp the host orders against and tenant isolation is a wire fact, not a per-runtime reinvention; the `serve#PROTO_TRANSCODE` `FaultDetail` decode already reads `hlc_physical`/`hlc_logical` off the slot but no owner threads the causal/tenant frame into the context the lane and receipts read, so the frame is decoded and dropped.
- Draws on the cross-`libs/` `ONE_CAUSAL_TENANT_IDENTITY` seam: the producer `csharp:Rasm.AppHost/ports/runtime-ports#PORT_RECORDS` mints the HLC two-half stamp on the `ReceiptEnvelope` and threads `TenantContext` across every hop, the companion decodes and propagates the same frame inbound, and the HLC two-64-bit-half order rides the same parity gate the content seed proves (a logical off-by-one-half folds a fresh op as stale); `Hlc` already exists on `serve#CRDT_DECODE` as the op-log cell, so the context frame composes the existing `Hlc` shape, never a second.

[OTLP_EGRESS_INSTALL_OWNER]:
- Collapse the OTLP signal-egress wiring the `observability/receipts#RECEIPT` and `observability/metrics#METRIC` owners each assume installed into one composition-root install owner on `observability` that mints the shared `Resource`, the `TracerProvider`/`MeterProvider`/`LoggerProvider` trio over the one `OTLPSpanExporter`/`OTLPMetricExporter`/`OTLPLogExporter` family, the `BatchSpanProcessor`/`BatchLogRecordProcessor` batch processors, and installs the global propagator — so the receipts log-egress `LogRecordProcessor`/`OTLPLogExporter` leg, the metrics `MeterProvider`, and the trace provider the `serve` interceptor and the trace-extract owner read all resolve from one install gate keyed by the `RuntimeProfile` `emit_otel` policy row.
- Unlocks the three signals carrying one `service.name`/`Resource` and one batched export lifecycle the host drain participates in, rather than the metrics owner constructing its own `MeterProvider` while the receipts owner wires a separate `LogRecordProcessor` and the serve interceptor reads a third provider — the egress is named on three pages as "arrives installed at the composition root" with no owner that installs it.
- Draws on the C# `diagnostics` parity bar (the `UseOtlpExporter` once, one root sampler derived thrice, one `Resource` joined across the suite) and the branch `opentelemetry-sdk.md`/`opentelemetry-exporter-otlp-proto-http.md` provider/processor/exporter surfaces; the install gate reads the `PROFILE_POLICY` `emit_otel` row so a `PACKAGE`/`TEST` profile installs the no-op provider and a `SIDECAR`/`TOOL` profile installs the batched OTLP trio, the volume/sampler floor a per-profile row, never a per-signal toggle.

[STREAMING_TRANSPORT_ACQUISITION]:
- Deepen `resources/roots#RESOURCE` to the streaming acquisition the catalogues admit but the full-body reads discard: `TransportResource.http` streams large artifact bodies through `httpx.AsyncClient.stream`/`Response.aiter_bytes` under an explicit `httpx.Timeout`/`httpx.Limits` rather than a full-body `client.get`, `ResourceRoot.reader` yields a chunk `AsyncIterator[bytes]` modality beside the whole-payload `bytes` so a large object-store or filesystem read folds straight into the `identity/content-identity#IDENTITY` streaming `xxh3_128` updater and the lane without buffering the whole artifact, and the `obstore` arm reads the `GetResult.stream` chunk iterator.
- Unlocks memory-bounded acquisition of the large GLB/IFC/scan artifacts that dominate companion pulls — the streaming content-key derivation, the lane offload, and the egress all consume chunks by reference, so a multi-gigabyte tessellation result keys and caches without a full-buffer tax; the `reader`/`acquire` return only `bytes` today, forcing a whole-artifact buffer the streaming `ContentIdentity.of` modality and the `httpx`/`obstore` streaming surfaces were built to avoid.
- Draws on the `httpx.md` `stream`/`aiter_bytes`/`Timeout`/`Limits` and the `obstore` streaming `GetResult` rows the catalogues carry but the fences leave unexploited (a named external-capability gap), and the existing `Iterable[bytes]` streaming modality on `ContentIdentity.of` — the acquisition streams into the modality already present, never a second hashing pass; `MODAL_ARITY` recovers the chunk-versus-whole modality from the value shape, never a `streaming=True` knob.

[GRPC_CLIENT_SPAN_AND_HOOKS]:
- Exploit the `opentelemetry-instrumentation-grpc` client-leg and span-hook surface the serve owner leaves on the table: the companion's outbound gRPC legs (the capability-invoke dispatch, any host call-back channel) wire `aio_client_interceptors(tracer_provider, filter_)` so the outbound call propagates the active span and continues the parent trace, and the serve interceptor admits `request_hook`/`response_hook` enriching the `SERVER` span with the decoded descriptor id, tenant, and fault case rather than a bare method-name span.
- Unlocks the outbound companion call joining the same distributed trace the inbound extract seeds (the client leg propagates `traceparent` the same composite the server leg reads) and a richer serve span carrying the capability/tenant/fault dimensions the metrics and receipts owners already compute, so a trace flame graph shows the descriptor and tenant per RPC, not an opaque method name.
- Draws on the `opentelemetry-instrumentation-grpc.md` `aio_client_interceptors`/`client_interceptor`/`request_hook`/`response_hook` rows the serve page catalogues but consumes only `aio_server_interceptor` for — the client interceptors and the hooks are catalogued-but-unconsumed external capability (a named gap), composing the `TRACE_CONTEXT_INBOUND_EXTRACT` propagator so the inbound and outbound legs share one trace, never a second tracer.

[SECRET_BOUNDARY_OWNER]:
- Resolve the orphaned `keyring` package the `[CREDENTIAL_PEM]`/`[KEYRING_CATALOGUE]` decode left: either fold `keyring` into one real `context/admission#SETTINGS` secret-resolution boundary that reads OS-keystore-backed credentials for the OUTBOUND transport legs (`TransportResource.ssh` passphrase, `TransportResource.http` bearer) through the `get_credential`/`get_password` facade with the `NoKeyringError` headless-fallback to the `pydantic-settings` `secrets_dir` source, OR remove `keyring` from the folder entirely as a manifest-row delta and drop its `.api/` catalogue.
- Unlocks the one credential-admission boundary the transport legs read — the `asyncssh`/`httpx` credential law each catalogue ("settings-model credentials, never inline literals") resolves through one OS-keystore-then-secrets-file boundary rather than two inline reads — OR collapses a genuinely-orphaned package the serve decode stranded; today `keyring` carries a member-rich `.api/` catalogue with ZERO design-page consumer after the serve leg dropped it, which is the `README`-vs-consumer mismatch the coverage audit forbids.
- Draws on the `keyring.md` `get_credential`/`get_password`/`NoKeyringError`/`SimpleCredential` facade and the `pydantic-settings` `secrets_dir`/`file_secret_settings` source the `[3]-[SETTINGS]` owner already wires — the secret boundary composes the existing settings source chain, the keystore the highest-priority source and the secrets-file the headless fallback, never a second config owner; the unattended-keystore-read hazard the campaign-method names (a keychain prompt the session cannot answer) bounds the resolution to a settings-admitted, profile-gated read, never an eager probe.

[CRDT_OPLOG_LZ4_DECODE]:
- Decompress the C# `MessagePackCompression.Lz4BlockArray` op-log envelope on the cp315 core so the settled `msgspec.msgpack` op-log decode reads the compressed delta the `OpLogEntry.Payload` carries.
- Unlocks the cp315-core collaborative op decode end to end; the MessagePack decode through `msgspec.msgpack.Decoder` is cp315-clean and only the LZ4 decompression is absent.
- [UPSTREAM-BLOCKED] on the cp315/abi3 `lz4` wheel (manifest `lz4; python_version<'3.15'`, no cp315 wheel) AND the producer-owned `Lz4BlockArray` framing: the envelope is a MessagePack-csharp block-array (a msgpack `ext` wrapping LZ4 blocks) no Python package reads natively, so the cross-agent `CRDT_OPLOG_WIRE_AMENDMENT` delta carries the framing decision (a `MessagePackCompression.None` companion lane or the published envelope spec); the companion `<3.15` band decodes the present build today.

## [2]-[CLOSED]

- [SERVE_WIRE_CONSUMPTION]: closed — `server/serve#SERVE` boots the `grpc.aio` `ServerHost` against the C# `ComputeService`/`DocumentService`/`ControlService`/`ArtifactSync` descriptors under the `anyio` runner, the credential axis decoded to `insecure_loopback` over the UDS leg (`#CREDENTIAL_PEM` resolved: kernel peer-credential, never a serve PEM), the `WireProtoCodec` `(Struct, Message)` proto transcode and the `CapabilityInvoke` descriptor-driven invoke settled; the runtime mints no wire vocabulary and awaits only the generated `_pb2` input. The op-log decode split from this idea: its MessagePack leg closed below, its LZ4 leg is the open `[CRDT_OPLOG_LZ4_DECODE]`.
- [CRDT_OPLOG_MSGPACK_DECODE]: closed — `server/serve#CRDT_DECODE` decodes the C# `Rasm.Persistence/versioning#CRDT_WIRE` `CrdtOpWire` `[MessagePack.Union]` ten-arm op union (tags 0-9, LWW surviving only as `set`) through `msgspec.msgpack.Decoder` over a `msgspec.Struct` tagged array-union, the prior protobuf `WireCodec` model corrected as a decode defect (the op-log is the durability MessagePack codec, not the gRPC proto wire); the companion authors no op kind the wire does not carry.
- [CAPABILITY_SDK_CONSUME]: closed — `server/serve#CAPABILITY_INVOKE` consumes the C# `Rasm.AppHost/capability/registry#SDK_CODEGEN` Python target as one polymorphic `_run(descriptor_id, args)` invoke, the descriptor catalog decoded through `msgspec.json` from `DiscoveryResultWire[]`, the per-descriptor schema the C# `SuiteContracts.Schema` projection the three SDKs bind, the command receipt read through `ReceiptEnvelopeWire`; the companion re-authors no capability shape.
- [CONTENT_IDENTITY_SEED_PARITY]: closed — `identity/content-identity#SEED_REPRODUCTION` binds the Python `ContentIdentity.of` reproduction to the FROZEN `csharp:Rasm/Geometry/topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` row [1] `CANONICAL_BYTE_IDENTITY` (the 52-byte int32-LE adjacency stream, the seed-zero digest `0x9462A71A5DD13DCFA3B1D6D225FCBE70`, the 16-byte LE memory), the child-fold corrected to `to_bytes(16, "little")` matching the C# `WriteUInt128LittleEndian` writer; the binding re-mints no digest and authors no fixture byte.

- [OTEL_METRICS_SPINE]: closed — `observability/metrics` `Metrics` registers the request-duration `Histogram`, the `lane.drained` `ObservableCounter` (the four `DrainReceipt` columns folded onto one `outcome`-keyed counter), and the `process.memory.rss`/`process.cpu.utilization` `ObservableGauge` instruments against one `MeterProvider`/`RUNTIME_RESOURCE` over the shared `OTLPMetricExporter`; the callbacks read a shared `MetricState` without a lane lock.
- [INTERPRETER_OFFLOAD_LANE]: closed — `concurrency/lanes` `LanePolicy.offload` routes a caller-supplied CPU kernel through `anyio.to_interpreter.run_sync` (PEP 734 per-subinterpreter GIL, no pickle round-trip) under the one `CapacityLimiter`, the `BrokenWorkerInterpreter` crossing the one `async_boundary`; the lane offloads, never imports the kernel.
- [CONTENT_ADDRESSED_LANE_CACHE]: closed — `concurrency/lanes` `LanePolicy.cached` admits `(ContentKey, Work[T])` pairs against an immutable `expression` `Map[ContentKey, T]`, a key already present short-circuiting and the `DrainReceipt` `hit` count distinguishing it from `completed`; only an `Ok` caches, session-local in-memory, never durable.
- [STRUCTURAL_DRIFT_GUARD]: closed — `evidence/evidence` `Structural.drift` folds per-grammar identifier-binding query spans into a name-to-languages `Map` and emits a `DriftSpan` only where a canonical name binds across more than one language namespace, the namespace-multiplicity filter rejecting a legitimately distinct same-named concept.
- [API_CATALOGUE_CAPTURE]: closed (split) — `obstore`, `apscheduler` (3.11.2), and `keyring` are reflection-confirmed on the cp315 core and their `resources`/`concurrency`/`server` member spellings are settled; `xxhash` stays [UPSTREAM-BLOCKED] at `[XXHASH_PARITY]` (no cp315/abi3 wheel synced), the `python_version<'3.15'` companion band carrying the present build.
- [INCREMENTAL_CONTENT_IDENTITY]: closed — `identity/content-identity` folds whole-payload, streaming `xxh3_128`, and child-`ContentKey` Merkle modalities into one `ContentIdentity.of`; the `[XXHASH_PARITY]` capture is the install-gated cp315 wheel.
- [OBSTORE_TRANSPORT_FAST_PATH]: closed — `resources/roots` `ResourceRoot.reader` dispatches `OBJECT_STORE_SCHEMES` to the reflection-confirmed `obstore` async API and every other scheme to `UPath`, one polymorphic reader.
- [APSCHEDULER_TRIGGER_UNION]: closed — `concurrency/lanes` carries the `CronTrigger`/`IntervalTrigger`/`DateTrigger` union, the `from_crontab` cron-string intake, and the `fired` feeder over the 2-arg `Trigger.get_next_fire_time`; `aiocron` is purged, cron owned solely by `apscheduler`.
- [STRUCTLOG_OTEL_TRACE_BRIDGE]: closed — `observability/receipts` carries the `trace_context` processor and the `merge_contextvars`/JSON chain with the reflection-confirmed OTLP log egress.
- [XXHASH_ADMISSION]: closed — `xxhash` admitted (`xxhash>=3.7.0`, abi3); the cp315 wheel install and the C# digest-endianness parity remain `[XXHASH_PARITY]`.
