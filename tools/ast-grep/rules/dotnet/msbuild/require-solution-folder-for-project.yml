# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: require-solution-folder-for-project
language: xml
severity: error
files: ['*.slnx']
rule:
    matches: {msbuild-element: {name: {regex: '^Project$'}}}
    not: {inside: {kind: content, inside: {matches: {msbuild-element: {name: {regex: '^Folder$'}}}}}}
message: The solution project sits outside the Folder named for its directory chain
note: Move the Project into <Folder Name="/<area>/<language>/<group>/">, the solution tree matches the repository layout
