# [RASM_CS_FABRICATION_BRIEF]

Campaign: cs track 6/6, the terminal AEC consumer — run after the geometry DECISION (the dual-carried kernel hinges resolve there). Phase 1: `Workflow(design-cs-fabrication.js)` (OpenCAMLib-probed) emits `RASM-CS-FABRICATION-DECISION.md`; Phase 2: `Workflow(rebuild.js, {targets: <per-leg pages>, brief: "RASM-CS-FABRICATION-DECISION.md"})` legs per the DECISION, structure first.

Authoritative campaign input for the ground-up restructure and rebuild of `libs/csharp/Rasm.Fabrication/.planning/` — the AEC-DOMAIN host-neutral digital-fabrication owner over `{Rasm, Rasm.Element}` (17 pages, 5 folders, ~2.9k design LOC; 14 folder-tier `.api` catalogs). Upstream law honored throughout: `RASM-CS-GEOMETRY-BRIEF.md` (the kernel toolpath plane `[V10]`, `DrawingProjection` `[V3]`, boolean ownership `[V5]`, the one distance-field lane `[V8]`, hull `[V11]`, and the `[04]` roster re-scopes — Fabrication CONSUMES, never re-implements), `RASM-CS-PERSISTENCE-BRIEF.md` (the re-anchored `[FABRICATION_PROGRAM_DURABLE_ROWS]` card: blobstore content rows + cache artifact index, durable receipts decoding 2701-2710 never 25xx), `RASM-CS-COMPUTE-BRIEF.md` (the `NestYield.WasteAreaMm2` rollup counterpart; the mesh/GLB wire layout law — decode, never re-mint), `RASM-CS-APPHOST-BRIEF.md` (transport/telemetry stratum: machine connectivity is livewire's, never a Fabrication interior), `RASM-CS-APPUI-BRIEF.md` (`[V7]`: the CAD WRITE leg is AppUi's two-row ACadSharp `DxfWriter`+`DwgWriter` fold, netDxf removed repo-wide; the `HiddenLineSeam` consumes Fabrication's receipt with the kernel supersession insulated AT that receipt), and `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` (the 2701-2709 re-band + `Nesting/stock.md` landing — build ON, never re-litigate). `docs/stacks/csharp/` governs unchanged and is never restated here.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is craft-excellent in its interiors and structurally naive as a production system: a 2.5D-milling + true-shape-nesting + HLR engine wearing production-fabrication clothing. The four proofs, each independently fatal to a world-class claim:

1. **The roster is fiction — four admitted packages ARE the production scope and have ZERO page consumers.** `PicoGK` (resin/powder grayscale + `.cli` layer stacks, TPMS/gyroid/lattice, supports, voxel Booleans), `OcctNet.Wrapper` (STEP/IGES/STL B-rep ingress — the format shops actually receive), `DSTV.Net` (steel NC1 exchange), and `SharpVoronoiLib` (Voronoi region-decomposition CAM) are pinned (`Rasm.Fabrication.csproj:16,19,20,24`), fully catalogued (14 `.api` files), and README-chartered — word-boundary grep over all 17 pages returns zero consumers for all four. The README `[IMPLICIT_VOXEL]`/`[SOLID_INGRESS]`/`[STEEL_FABRICATION_EXCHANGE]`/`[VORONOI_TESSELLATION]` charters describe capability no fence delivers.
2. **The professional floor does not execute — three realized owners are pipeline islands, and the arc rail is compile-broken.** `guard.md:3` claims `Cam.Solve` consults `Guard.Check` before each feed move; `motion.md:46-62` never calls it. `magazine.md:18,22` claims `Post` emits each `ToolChange` as `G43`/`M6`/`Tnn`; `program.md:141-164` consumes no `Magazine.Schedule`. `workholding.md` claims `Condition` conditions the toolpath; no fold calls it. `clipper.md:5,33` declares `ArcAlgebra` RETIRES the Clipper2-offset-then-`g3.BiArcFit2` refit; `program.md:247-282` still refits and reads no `Bulges`; and the widened `Loop` the arc rail requires (`clipper.md:257,271` calls `BulgeAt(i)` + a 3-arg ctor) was never landed by the atom owner — `owner.md:42` declares `Loop(Arr<Point3d>, bool)` with no bulge column.
3. **The post-processor is a two-column render wearing eight dialects.** `PostDialect` carries `Rs274`/`Comment`/`LineNumbers` only (`family.md:80-96`); `CutProgram.Emit` varies output by `LineNumbers` alone (`program.md:122-136`), so a Fanuc post and a Haas post emit near-identically. `GCommand -> Code` is dialect-INVARIANT (`program.md:41-65`; `thread-cycle -> "G92"` is Fanuc-lathe-specific and wrong on a Haas mill). No canned cycles (G81-G89/G76), no macro programming (Fanuc macro-B, Q/R-params), no subprograms (M98/M99), no work offsets (G54-G59), no cutter comp (G41/G42), no arc-mode (R vs IJK), no conversational family — Heidenhain Klartext and Sinumerik are structurally unexpressible in the word-address AST, and `PostDialect.Admits` is a binary `Rs274 ? modality != Additive : modality == Additive` (`family.md:94-95`).
4. **Coverage is a thin slice over placeholder arms, and whole mandate planes are silent.** Five of eight `CutStrategy` generator arms are stubs: `radial-sweep` AND `helical` both alias `Turn` (`motion.md:71,73`), which is a per-vertex `v.Y - radius - k*stepOver` offset, not a lathe ZX sweep (`motion.md:76-79`; `BarStock` is a prose phantom); `Plunge`/`Peck` are 2-move centroid stubs (`:81-84,105-108`); `SliceWalk` orders nothing (`:86-87`). No waterline/scallop/pencil/rest-machining/3D-surface strategy exists; `Material` fragments one physical identity into modality-keyed rows (`stainless` vs `stainless-abrasive`, `physics.md:91-99`) against the page's own anti-flat-record prose; `erosion` routes to `ThermalBudget` (`physics.md:136`); and NO owner anywhere carries NC verification, production specs (tolerance/finish/capability/setup-sheet/traveler), additive supports/orientation, or G-code ingress.

What IS sound (preserve; cold-pass plus ONLY the deltas a verdict names): `Process/family.md` (9 — the de-hardcode axis exemplar), `Process/owner.md` (9 — the settled polymorphic entry), `Process/faults.md` (9 — the landed 2700 band), `Nesting/nfp.md`/`stock.md` (9 — the nesting engines), `Process/magazine.md` (8.5), `Toolpath/kinematics.md` (9 — the Robots boundary exemplar), `Toolpath/guard.md` (8.5 — shape correct, unwired), `Polygon/clipper.md`/`import.md` (9 — craft). The rot is structural — dead admissions, unwired pipelines, thin axes, mis-partitioned folders — and no per-page cold pass fixes it. Architecture first.

### [TELOS]

`Rasm.Fabrication` is a production digital-fabrication engine: one polymorphic `Fabrication.Run` closing 3D model -> verified machine program across subtractive CNC/CAM at full depth (2.5D through 3D surface finishing, multi-axis, rest machining, tool-engagement control), additive manufacturing at production depth (FFF/DED planar + resin/powder implicit lanes, supports, orientation, lattice/TPMS, machine profiles, 3MF hand-off), 2D true-shape nesting + rectangular cutting-stock yield, steel NC1 exchange, post-processing at controller-family breadth (word-address, conversational, canned cycles, macros, subprograms), NC verification by voxel material removal, on-machine probing, and production specs (GD&T/tolerance/surface-finish vocabulary, process capability, setup sheets, shop travelers). It CONSUMES the kernel toolpath plane at the seam — corner-strategy offsets, slice stacks, medial-with-clearance-radius, `DrawingProjection`, manufacturing-tolerance booleans, the one distance-field lane — and sharpens demand rows where production scope exceeds the kernel floor, never re-implementing it. Capability coverage is the sole density measure; assume 5x the consumer demand on every surface: the posting plane will feed real shop controllers, the verify plane will gate real material, the spec plane will ride real travelers — zero current consumers never lowers the bar. Host posture: RhinoCommon is the kernel vocabulary (host-neutral means not-Rhino-plugin-bound, never host-free); `stock.md` stays the scalar exemplar; the Rhino3dm second stack rides the `extern alias R3` boundary; net9-native lanes (PicoGK, OpenCAMLib) ride the ALC-firebreak/sidecar posture, content-keyed at the wire.

### [STRUCTURAL_AUTHORITY]

Phase 1 owns the folder/page-set absolutely: split any page, merge pages, move concerns across folders, delete pages after absorption, mint new folders — `workholding.md` leaving `Nesting/` for `Fixturing/`, `projection.md` leaving `Posting/` for `Documentation/`, `import.md` leaving `Polygon/` for `Ingress/`, `slicing.md` leaving `Toolpath/` for `Additive/`, and `clipper.md` splitting into a two-owner `Geometry2D/` are the calibration examples, not the ceiling. The folder-architecture law: folders = namespaces = growth axes, ratified ONCE — the current 5-folder map hides 12 realized namespaces (`Process`/root/`ProcessModel`/`ProcessPhysics`/`Geometry2D`/`Toolpath`/`Kinematics`/`Additive`/`Nesting`/`Fixturing`/`Posting`/`Projection`; the same folder≠namespace schism the kernel `[V1]` ruled, where the realized-namespace scheme WINS), so the DECISION ratifies one namespace per folder and re-homes every page to its namespace-true folder. No one-file folders: every folder carries >=2 pages, and the mandate's new lanes ARE the growth that makes thin folders legal. Every structural change updates `ARCHITECTURE.md` `[01]`+`[02]` and the `README.md` router in the same motion. No page survives on incumbency.

### [STRATA_AND_KERNEL_LAW]

The strata law is absolute (`libs/.planning/architecture.md`): Fabrication is AEC-DOMAIN over `{Rasm, Rasm.Element}`, referenced BY app-platform (`Rasm.Compute`, `Rasm.AppUi` consume its receipts), and NEVER references a sibling AEC peer or an app-platform package — the Materials physics scalars arrive as raw doubles at the peer boundary (`physics.md` posture, correct), the Compute DRL score arrives as an injected delegate (`nfp.md` posture, correct), and Persistence/AppHost/AppUi appear only as seam-ledger counterparts. The kernel-consumption law is this campaign's spine: five author-kernels re-frame from OWNER to CONSUMER because the geometry campaign lands their concern kernel-side with Fabrication the named demanding consumer — corner-strategy offset (`GBRIEF [V10]a`), slice stack (`[V10]b`), medial-with-clearance-radius (`[V10]c-e`), `DrawingProjection` (`[V3]`), manufacturing-tolerance booleans + Manifold scale tier (`[V5]`) — plus the one distance-field lane (`[V8]`: `SignedDistanceFromMesh` through the index-accelerated winding; PicoGK is Fabrication's voxel/implicit lane, never a second kernel SDF owner) and the hull owner (`[V11]`: stock/clearing envelopes). A consumer page may not hand-roll what the kernel owns; a kernel floor too thin for production demand is upstream pressure recorded by waterfall ripple (already landed in `GBRIEF [V10]`/`[04]`), never license to re-implement. Where the geometry DECISION has not yet ruled (CavalierContours siting, medial radius carriage), this campaign's pages carry BOTH dispositions explicitly and signature-lock the seam.

### [GENERATOR_LAW]

An enumerated instance where a parameterized generator belongs is the campaign's canonical defect (COVERAGE naivety's twin). `family.md` is the exemplar — rosters are seed DATA on closed `[SmartEnum]` axes with constructor-bound behavior columns and generated total `Switch` — and the law extends: a new controller is a `PostDialect` row over GRAMMAR-family capability columns, never a ninth hand-written emitter; a new strategy is a `(RemovalModality, CutStrategy)` row whose generator arm is a REAL operation; a new material is one identity carrying a per-modality physics map, never a second suffixed row; a new machine is a `KinematicClass` topology row; a new fault is an offset-derived arm with a typed payload. Empirical magic dies onto owning tables: the `0.0012 * RemovalRate` cutting-force coefficient (`program.md:183`) becomes a Kienzle-`kc` column on the `Tooling/cuttingdata` machinability table (`[V9]` — `physics` keeps the identity map; one owner each), the `90.0` fallback (`physics.md:141`) demotes behind the data table, `1e6` eye-distance and `Ty*1e6+Tx` heuristics (`projection.md:100`, `nfp.md:137`) become policy rows and tuple comparisons. The test: the next controller, strategy, material, machine, or spec row is one data row — never a new arm, a new magic constant, or a second lowering.

### [SEAM_AND_ENTRY_LAW]

- ONE entry: `Fabrication.Run` over the settled `FabricationInput` + `FabricationPolicy` `[Union]` stays the only public fold; new planes land as policy cases (`Additive`, `Verify`, `Inspect` join `HiddenLine`/`Cam`/`Nest`) with matching `FabricationResult` cases and `Option`-carried input growth — one arity owns all modalities, never a second entry point.
- THE WIRED-PIPELINE LAW: every "professional floor" owner EXECUTES in the fold — `Run -> Cam -> {Guard.Check per feed move, Workholding.Condition, Magazine.Schedule} -> Post` (acyclic: owner -> cam -> conditioning owners -> posting); a page asserting a consumer that does not call it is the E2 defect class and may not survive.
- ONE fault rail: `FabricationFault` on `FaultBand.Fabrication = 2700`, offsets 2701-2710 landed (Nest at +10) — build ON, never re-band; new concerns take +11.. offsets sized by the growth law; every arm carries its typed payload (`Gouge` -> point + tool, `Collision` -> zone, `InadmissiblePair` -> the typed pair, `NoFit` -> part + tried rotations), never bare `string Detail`; durable receipts decode 2701-2710, never 25xx, and the R6 first-instance census closes with the stale-25xx purge.
- Receipts are content-keyed at the persistence boundary: `CutProgram` gains its `XxHash128` content key (today it "computes no hash", `program.md:3`) feeding the re-anchored blobstore content rows + cache artifact index (`PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]`), and the key law covers EVERY machine-consumable egress artifact on one identity fold — `.cli` layer stacks, 3MF payloads, NC1 text, and the `[V7]` stock-state snapshots mint the same `XxHash128` into the same kind-agnostic rows (a keyed program beside an unkeyed layer stack re-mints the E9 identity defect); `Remnant.Of`'s full-vertex hash is the identity exemplar; `Stock.Of()`'s area-only hash (`nfp.md:88`) is the deleted form.
- The `ARCHITECTURE.md` seam ledger is enforceable law, both directions: every cross-package edge appears as a ledger row (the wired-undeclared `Nesting/stock <- Rasm.Element/MaterialId` at `stock.md:40` lands), every declared edge is composed in a fence, the intra-package web (magazine->guard/workholding `HolderEnvelope`, guard->skeleton `ClearanceAt`, workholding->posting `Sequence`, setups->posting WCS rows, removal->setups/guard stock snapshots, motion->skeleton/slicing/kinematics) is declared, and the stale targets re-point (`ARCH:52,53` `Rasm.Persistence/Schema` is DELETED -> blobstore/cache owners; `ARCH:16` band-2500 -> 2700; `ARCH:51` netDxf -> the AppUi ACadSharp two-format write leg).

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.Fabrication.csproj`; per-package catalogs under `libs/csharp/Rasm.Fabrication/.api/` (14) with the shared substrate under `libs/csharp/.api/`. INTEGRATION-FIRST is law: the four zero-consumer admissions are integration MANDATES realized as named pages/rows (never removal candidates); a REMOVE requires proven redundancy on a stronger admitted owner, feed-verified abandonment, or a license conflict. Carried obligations this campaign disposes: the `netDxf` pin (`Directory.Packages.props:359`) leaves with the AppUi `[V7]` leg-1 motion and `netDxf.netstandard` (`:472`) with the Compute `[V8]` parity-gated frame-backend retirement (its lock-verified sole parent is `FEALiTE2D.Plotting`) — this campaign VERIFY-REMOVES whichever pin survives at its leg-1 manifest pass, and a surviving `netDxf.netstandard` under a parity-kept FEALiTE2D stays a transitive floor, never Fabrication residue; `RectpackSharp` REMOVE on proven redundancy (`RectangleBinPack.CSharp` strictly subsumes it — `[04]`); `geometry3Sharp` disposition rides the arc-rail verdict (`[V3]`; the kernel strike at `GBRIEF [04]` leaves Fabrication its last consumer); the kernel `[04]` re-groupings land here — `Clipper2` RE-SCOPEs to the Fabrication label, `CavalierContours` sites at the ONE `[V10]a`-ruled stratum (never two manifests), `SharpVoronoiLib` re-groups from the Kernel-Geometry label to Fabrication's, `RectangleBinPack.CSharp` re-groups from the vacated Materials label. New pressure lands per `[04]`. License gate: OSS and free-for-OSS-commercial admissible; AGPL/pay-tiered/proprietary-gated REJECTED (AGPL engines are REFERENCE-only, never vendored); LGPL native engines are admissible as dynamic-link P/Invoke assets under the golden-fixture gate the kernel's Manifold admission normalized.

## [01]-[PHASE_ARCHITECTURE]

ONE ephemeral design workflow runs survey -> draft -> judge -> decide -> salvage and emits `RASM-CS-FABRICATION-DECISION.md`. Shape: survey agents re-verify `[02]` against disk and sweep the namespace/consumer/seam census, plus ONE feasibility probe — the OpenCAMLib C-shim/P-Invoke binding over the shipped C++-ABI archives, its result ruling the `[V5]` surface-engine siting AT decision time; 4 drafts propose COMPLETE folder/page-set blueprints (one row per page: action KEEP/REBUILD/SPLIT/MERGE/MOVE/DELETE/NEW, owner charter, namespace, entry signature + result/receipt case, in/out seam edges, wave) under four lenses (namespace-true partition, kernel-seam-first, package-mining-first, output-first — working BACKWARD from the flagship programs: a five-target posting suite, a verified two-setup multi-tool machining job, a resin/powder + FFF additive pair, an issued setup-sheet/traveler package); judges score against `[00]` law + `[03]` targets with disqualifying gates (verdict disposition, folder=namespace 1:1, >=2 pages per folder, seam graph acyclic under the `[V1]` two-node entry rule, both kernel-DECISION dispositions carried); salvage mines the losing drafts with anchor-verified findings. The DECISION must resolve every verdict below; recommended shapes are floors, never ceilings.

### [V1_FOLDER_PARTITION]

The 5-folder/17-page map re-partitions into the namespace-true growth structure; the realized namespaces already NAME the decomposition the folders obscure. Recommended floor (folder = namespace = fault-cluster; every folder a growth axis; the DECISION owns final names and the exact page-set):

| Folder | Pages (action) | Charter |
|---|---|---|
| `Process/` | owner (KEEP+grow cases), family (REBUILD: axis widening), physics (REBUILD: identity map), faults (KEEP+payloads) | the entry, the axes, the physics, the rail |
| `Tooling/` | magazine (MOVE+wire), cuttingdata (NEW: machinability model + data ingress) | ISO-13399 tool intelligence |
| `Geometry2D/` | algebra (SPLIT from clipper: Clipper2 line-space), arcs (SPLIT: CavalierContours arc-space or kernel-seam consumer per `[V10]a`) | the 2D substrate |
| `Ingress/` | profile (MOVE from Polygon/import), solid (NEW: OcctNet STEP/IGES/STL), steel (NEW: DSTV NC1 read) | everything entering as geometry |
| `Toolpath/` | motion (REBUILD: real arms), surface (NEW: 3D finishing), partition (NEW: Voronoi strategies), guard (KEEP+wire), skeleton (REBUILD: kernel-medial consumer) | subtractive CAM |
| `Kinematics/` | cell (MOVE from kinematics), machine (NEW: 5-axis rotary topology + TCP) | motion topology |
| `Additive/` | slicing (MOVE+re-route to kernel slice-stack), implicit (NEW: PicoGK voxel lane), production (NEW: orientation/profiles/3MF) | production 3DP |
| `Nesting/` | nfp (KEEP+identity fix), stock (KEEP) | layout + yield |
| `Fixturing/` | workholding (MOVE+safety fix), setups (NEW: multi-fixture scheduler — the queued card realizes) | keep-out + setup planning |
| `Posting/` | program (REBUILD: AST growth + content key), dialect (NEW: grammar families, canned cycles, macros, NC1 emit target) | machine-code emission |
| `Verify/` | removal (NEW: voxel material-removal), probing (NEW: the queued card realizes) | program-level truth |
| `Spec/` | tolerance (NEW: GD&T/fits/finish vocabulary), capability (NEW: Cp/Cpk/SPC + stackup) | production specs |
| `Documentation/` | projection (MOVE+rebuild as kernel-`DrawingProjection` consumer), traveler (NEW: setup sheets + shop travelers) | shop documentation |

Every MOVE re-homes the page to its namespace; `ARCHITECTURE.md` `[01]`/`[02]` + the README router rewrite in the same motion; the sheet-metal bend/unfold lane (DSTV `KA` bend blocks, K-factor unfold over OcctNet faces) seeds IDEAS as a named growth row on `Ingress/`+`Spec/`, not a build-leg page — its geometric floor is the kernel `develop.md` isometric unroll (`GBRIEF [V2]` already names Fabrication sheet work the consumer), the K-factor bend-allowance table the Fabrication-owned overlay, never a second unroll engine.

Where the ratified partition overrides a realized namespace, the folder WINS and the fence namespace rewrites in the same MOVE — six ratifications: `family`/`magazine` leave `ProcessModel` (-> `Process`/`Tooling`), `physics` leaves `ProcessPhysics` (-> `Process`), `faults` leaves the package root (-> `Process`), `import` leaves `Geometry2D` (-> `Ingress`), `projection` leaves `Projection` (-> `Documentation`). The DECISION's fault-registry table allocates one 2700-century offset block per folder (+11.. onward, sized by the growth law) so folder = fault-cluster is mechanically checkable, never asserted.

The post-campaign folder graph is acyclic with ONE ratified exception: `Process/owner` is both the atom vocabulary (`Loop`+`Bulges`, `Edge3`/`Move`, `FabricationInput`/`FabricationPolicy`/`FabricationResult` — upstream of every plane; `guard.md:3` composes it today) and the terminal `Fabrication.Run` fold (downstream of every plane). The seam ledger and the acyclicity gate evaluate the page as TWO nodes — `owner#atoms` (upstream-only declarations) and `owner#run` (terminal, compose edges only) — and a draft splits the page physically only when its ledger fails the two-node discipline. Cross-plane RESULT data rides the entry vocabulary, never a page-compose edge: the `[V7]` residual-stock field mints its shape on `owner#atoms`, so the motion->removal->program->motion type cycle never forms.

### [V2_KERNEL_PLANE_CONSUMPTION]

The five author-kernels re-frame from owner to consumer, each with its disposition carried BOTH ways until the geometry DECISION rules: (a) `skeleton` consumes the kernel medial-with-clearance-radius (`GBRIEF [V10]c-e`) and keeps ONLY the trochoidal constant-engagement WALK strategy — `Toolpath/Skeleton.cs` dies for `Offsetting.Apply` IF the kernel medial carries radius; a radius-less medial keeps the author-kernel alive on honest need, stated inline; (b) `slicing.Section` re-routes to the kernel slice-stack (`[V10]b` oriented contours + typed open chains) per its own ledger promise (`ARCH:47` "re-routes if one lands" — the gate FIRED; the O(n²) `Chain` walk at `slicing.md:114-135` dies); (c) `projection`'s BSP interior dies for the kernel `DrawingProjection` (`[V3]`: "the kernel supersedes BOTH, Fabrication consumes") — the page keeps the `BooleanSolid` watertight arm (kernel `Arrangement.Apply`/`ToMesh` compose-seam, landed law) and PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at (`UIBRIEF:146`); the centroid-classified BSP without facet splitting (`projection.md:74-77`), per-edge average depth (`:150`), and undeduped crease-free mesh-edge soup (`:102,145`) are the proof the interior was never CAD-grade; (d) offset rails converge on the `[V10]a` ruling — if the kernel owns the offset assembly, `Geometry2D/arcs` re-scopes to the seam consumer and the CavalierContours pin moves to the kernel group; if pure-kerf compensation rules Fabrication-side, `ArcAlgebra` stays THE arc owner and the kerf/lead/adaptive rails wire through it (`[V3]`); (e) booleans and SDF stay kernel-consumed (`[V5]`/`[V8]`; `projection`'s `Rasm.Geometry.Healing` import re-points with the kernel `BooleanOp` re-home). The three `[03]-[RESEARCH]` tails (`kinematics.md:122-125`, `skeleton.md:199-202`, `slicing.md:219-222`) purge folder-wide — load-bearing member confirmations fold into `Packages` blocks or the `.api` catalog; acceptance is mechanical: `rg '\[RESEARCH\]'` returns zero under `.planning/`.

### [V3_EXECUTION_WIRE]

The pipeline islands wire into ONE executing fold. `Cam.Solve` consults `Guard.Check` per committed feed move and `Workholding.Condition` for keep-out conditioning; `Post` consumes `Magazine.Schedule`, emitting real `Tnn`/`M6`/`G43` blocks at tool boundaries; the arc rail's declared retirement REALIZES — kerf (`program.md:174-180`), lead arcs (`:215-223`, today hand-built `ArcCcw` via manual center math), and the adaptive/morphed-spiral rails read arc-native offsets (`ArcAlgebra` or the kernel seam per `[V2]d`), with `g3.BiArcFit2` retained ONLY for genuinely line-sourced chains (kernel mesh-section polylines) until the owned biarc fold lands (`[04]` geometry3Sharp row). The atom owner ratifies the widened `Loop`: `owner.md` lands the parallel `Arr<double> Bulges` column (0 straight, tan(θ/4) arc), the 3-arg ctor, and `BulgeAt` — the one shape both algebra owners read; the fence-vs-manifest divergence in `clipper.md` (`lnContours` namespaces + `PlineOffset.ln<>` at `:169-227` vs the admitted `CavalierContours` pin/README/.api) resolves to the admitted package's real member surface. Fixturing hardens to its own safety charter: `Clears`/`Condition` replace 3-point endpoint+midpoint sampling (`workholding.md:73-83`) with segment-vs-polygon intersection (a thin diagonal clamp crossing is currently undetected); `ForHolding`'s first-match (`:49-50`, `Vise` unreachable behind `Clamp`) becomes a total mapping; the kind-specific footprint SHAPES the prose claims (`:3,14`) land as real per-kind geometry columns, not a `MarginScale` scalar. The motion-arm conditioning asymmetry rules NOW as arm law: `Placement` carries part outlines and runs the kerf/lead/pierce/tab/sequence conditioning; `Motion` (`program.md:147-148`) carries tool-center-line moves already CAM-conditioned — lead arcs land in the Cam fold via the arc rail, and a kerf pass over `Motion` double-compensates, the forbidden form — the split states on the arm. The work-offset chain wires end-to-end: `Fixturing/setups` OWNS the setup->WCS assignment rows (setup k -> the dialect's G54-G59.x/G54.1-Pn roster), posting renders what setups assigns — never a posting-side default — and `Verify/probing`'s work-offset metrology receipt reconciles the assignment against the measured datum.

### [V4_POST_GRAMMAR]

The posting plane becomes a real per-dialect generator. `PostDialect` gains GRAMMAR-family capability columns: dialect family (`WordAddress` RS-274 | `Conversational` Klartext/Mazatrol/OSP | `Additive` Marlin/RepRap + the resin/powder layer-stack lane), canned-cycle grammar (G81-G89 drill/bore/tap, G76 thread — emitted as single blocks where the family admits, expanded moves where it does not), macro/subprogram convention (Fanuc macro-B `#`-vars/WHILE/GOTO, M98/M99, Siemens R-params, Heidenhain Q-params), work-offset roster (G54-G59.x), cutter-comp admission (G41/G42 — distinct from geometry-side kerf, both stated), arc mode (IJK | R | none), block cap, decimal-format/modal-suppression policy, and a per-dialect `GCommand` code-override column (the dialect-invariant `Code` table at `program.md:41-65` is the deleted form; `Dwell`/`Pierce` both "G4" collapse to one command + flag). `Emit` becomes a generated total switch over the family axis; the flat two-column render dies. The AST grows structure with the grammar: the flat `Seq<GWord>` block stream (`program.md:118-121`) gains canned-cycle nodes with typed R/Q/P parameter slots, subprogram units with repeat counts, and macro parameter slots, so per-family lowering is node expansion, never string synthesis. Rows widen on the same axis as seed DATA: Siemens 840D, Heidenhain TNC, Okuma OSP, Fagor, Centroid join the eight. Ownership splits data-vs-lowering: the widened `PostDialect` axis and its capability columns stay `Process/family`'s (wave-1 seed rows); the emission folds those columns drive — canned-cycle expansion, macro/subprogram lowering, code-override resolution, the NC1 target — are `Posting/dialect`'s, never a second axis. The NC1 steel lane lands as a sibling emit TARGET on the one `Post` fold (the `DSTV.Net` record tree as the emission model, mirroring `Ingress/steel`'s read — README:36's claim made real). G-code INGRESS lands as the author-kernel `Parse` arm on the same AST (round-trip law: `Parse ∘ Emit = id` on each dialect's admitted subset, the `Parse` side carrying the NIST modal-group state machine — motion/plane/distance/units/feed groups — without which round-trip is unimplementable; re-post/verify/DNC round-trip is the consumer; `gsGCode` is REFERENCE prior art, never vendored — source-only, geometry3Sharp-coupled). The two look-ahead planners (Robots `Program` compile, `api-robots.md:72`; posting `Lookahead`, `program.md:290`) stay per-machine-class but read ONE shared jerk/accel motion-dynamics law HOMED on `Kinematics/machine` as a typed policy shape — posting consumes rows, the Robots boundary maps them at the `extern alias R3` edge, so they cannot drift. NO admissible managed post-processor library exists (feed-verified: StepTools/ModuleWorks/EncyCAM proprietary; E3Studio an app) — the author-kernel AST is validated; NIST RS274NGC v3 is the free grammar authority.

### [V5_CAM_DEPTH]

`CutStrategy` widens to production CAM on the settled `(RemovalModality, CutStrategy)` cross-product with a dimensionality column (2.5D | 3D-surface | multi-axis) feeding an axis-count admission: waterline/Z-level, constant-scallop, pencil/corner-trace, flowline/morph, REST-MACHINING (residual-stock re-cut reading the input-carried `[V7]` residual field per the `[V1]` entry-vocabulary law — never a motion->removal page edge), 3+2 positional, swarf/flank, thread-mill, and the drill-cycle family join the eight rows. Every stub arm becomes a real generator: `Turn` a true ZX radial sweep over a revolved stock envelope (the `BarStock` phantom lands as a real shape), `helical` a distinct thread/ramp generator (the alias dies), `Peck` real canned-cycle geometry with dwell/retract policy, `SliceWalk` a real perimeter/infill/travel orderer. The 3D-surface family lands on `Toolpath/surface` — ADMIT `OpenCAMLib` (LGPL-2.1 relicensed, active, precompiled osx-arm64/win/linux `libocl`) as the drop-cutter/push-cutter/waterline sampling engine via a thin in-house C-shim + P/Invoke (upstream ships C++-ABI archives only; the Manifold-class burden: RID assets recorded at admission, golden-fixture gated, content-keyed at the wire; NO C# binding or NuGet package exists — Phase 1 runs the binding feasibility probe, and the author-kernel drop-cutter over the kernel `[V8]` index/SDF lane stands ONLY if the probe fails). `Toolpath/partition` realizes the `SharpVoronoiLib` mandate: Voronoi region-decomposition pocketing seeds, Lloyd-relaxed stipple/engrave/even-spacing paths as strategy rows. Tool-engagement control becomes stated law: the adaptive engagement bound reads the medial clearance field AND the physics chip-load budget on one policy row. `Kinematics/machine` closes the 5-axis gap: `KinematicClass` deepens from 4 flat rows to rotary-topology rows (table-table trunnion | head-head | head-table | nutating) so `mill-5axis` stops binding `CartesianGantry` (`family.md:118`), and the machine-tool post kinematics owner (rotary-axis inverse, TCP/RTCP admission) lands beside the Robots cell — articulated-arm IK stays `cell`'s. The cutter-FORM vocabulary lands where four consumers already wait: `Tooling/` gains the typed `CutterForm` axis — flat/ball/bull/taper/drill/chamfer/thread-mill rows with diameter/corner-radius/taper-angle/flute-length columns PROJECTED from the ISO-13399 measurement set `ToolAssembly` already carries (`magazine.md:5,161` — the data is admitted, the typed projection is the gap) — read by `surface` (OpenCAMLib cutter selection at the P/Invoke edge), `Verify/removal` (the per-move swept implicit; a ball-nose sweep IS the PicoGK capsule beam, `api-picogk.md:49,90-92`), `guard` (the swept-disc radius stops being a bare scalar), and `cuttingdata` (effective-diameter speed derivation on ball/taper engagement); the `Cam` policy's bare `ToolRadius` double (`owner.md:85`) re-frames to the form-row reference.

### [V6_ADDITIVE_PRODUCTION]

The additive plane becomes a folder, not a page. `Additive/slicing` keeps the FFF/DED planar owner (validated: every mature FFF engine — CuraEngine/PrusaSlicer/libslic3r — is AGPL, REFERENCE-only) consuming the kernel slice-stack per `[V2]b`, enriched from `gsSlicer` prior art (open-sheet handling, overlapping shells) — and its production layer-height demand lands UPSTREAM as data: the variable-by-slope adaptive and support-interface layer rows are kernel slice-stack policy-family seed rows landed on this lane's named demand (`GBRIEF [V10]b`), slicing consuming the policy axis, never a Fabrication-side layer-height engine. `Additive/implicit` realizes PicoGK to depth: `IImplicit -> Voxels` TPMS/gyroid/cellular conformal infill (the `slicing.md:19` claim that gyroid is a 2D-hatch `InfillPattern` row is the proven false collapse — `api-picogk.md:152` states the quartet cannot express TPMS; the row corrects to route through the voxel lane), `Lattice` beam supports, overhang-conditioned support scaffolds, SLA/DLP/MSLA grayscale + `.cli` vector layer stacks (`oVectorize -> CliIo`), voxel offset/shell lightweighting — net9 + native core rides the ALC-firebreak/sidecar posture (`api-picogk.md:146`), never in-Rhino. `Additive/production` owns build-orientation optimization (author-kernel on the overhang/bottom-area/contour objective — Tweaker-3 is REFERENCE), machine profiles (build volume, nozzle/vat/laser, kinematics class as rows), and 3MF egress — ADMIT `lib3mf` (BSD-2, official C# binding, v2.5.x) as the 3MF core + beam-lattice + production-extension writer via the vendored official binding + RID-keyed native asset (no NuGet distribution exists — feed-verified; golden-fixture gated; PicoGK `Lattice` output maps directly to the beam-lattice extension; STL-implied hand-off dies). `physics` mints the budget cases the lane needs: `RemovalBudget.Resin` (exposure/cure), `.Powder` (laser power/hatch/scan speed), and DED distinct from FFF — the single FFF-only `Additive` case (`physics.md:109`) and the missing `Erosion` case (`[V9]`) are one defect family.

### [V7_VERIFY_PLANE]

NEW plane: program-level truth. `Verify/removal` owns voxel material-removal verification — stock `Voxels` minus the accumulated swept-tool volume via PicoGK `BoolSubtract` (the admitted, dead voxel engine turned NC-verify owner; `GBRIEF` out-of-scope already names CAM material-removal simulation a Fabrication interior; MillSimSharp 0.1.0/0.2.0-beta MIT corroborates the SDF/voxel approach as REFERENCE, its 1-contributor beta maturity the recorded non-admission reason; CAMotics GPL is REFERENCE) — emitting typed gouge/uncut/overcut/air-cut receipts against the ACTUAL removed-stock state and the residual-stock field the `[V5]` rest-machining rows consume; the `ResidualStock` and per-setup snapshot shapes mint on `owner#atoms` (wave 1) so Verify PRODUCES while the rest arm and the wave-3 `Fixturing/setups` admission READ input-carried data — run N's verify receipt feeds run N+1's rest input through the fold. Removal emits content-keyed per-SETUP intermediate stock snapshots, never one terminal state: `Fixturing/setups` admits op-N workholding against the op-N-1 machined state (a clamp on a face already cut away is the plan-time defect the snapshot catches) and `guard`'s stock keep-out reads the CURRENT snapshot, never the raw blank. The PicoGK net9/native boundary declares ONCE: `Additive/implicit` owns the ALC-firebreak/sidecar posture and the content-keyed mesh<->`Voxels` wire, `removal` composes that declared seam, never a second posture. `guard` stays the per-move design-time floor (swept Minkowski envelope, distinct part-keep/stock-keep-out verdicts) — two owners, two moments, both wired. `Verify/probing` realizes the queued `PROBING` card: touch-probe cycle vocabulary (G31/G38 rows on the posting AST), work-offset/tool-length metrology, measured-feature receipts — the QIF (ISO 23952) MeasurementResults alignment feeds `Spec/capability`. Measured ingress is honest: probe values are machine-origin — they enter through the SAME AppHost decoded-telemetry seam the magazine tool-life row rides (waterfall landed: the `APPHOST [04]` MTConnect.NET row names `Verify/probing` its second decoded Fabrication consumer) or as typed manual-entry rows; design-time receipts are typed carriers, and the `(d)` dry-run composes on fixture-synthetic measurements. The `FabricationPolicy` grows its `Verify`/`Inspect` cases per the entry law.

### [V8_SPEC_PLANE]

NEW plane: production specs, an author-kernel — NO admissible OSS library exists (feed-verified: only standards, the free QIF/ISO 23952 schemas, and proprietary tools). `Spec/tolerance` owns the typed GD&T vocabulary: ISO 286 fits (the grade/deviation tables as generated rows, never enumerated), ISO 1101/ASME Y14.5 feature-control frames + datum systems, ISO 1302 surface texture (Ra/Rz + lay + process marks), MMC/LMC modifiers — QIF-aligned so measurement plans/results interchange cleanly; Fabrication owns the VOCABULARY (the artifacts brief already routes frame DRAWING to the py drawing plane from supplied values — this page is that supplier). `Spec/capability` owns process capability: Cp/Cpk/Pp/Ppk over `MathNet.Numerics` (shared substrate, zero Fabrication consumers today), Monte-Carlo tolerance stackup, SPC control-limit rows — probing receipts are its measured ingress. The spec plane DRIVES process, never only documents it: the surface-finish law (Ra/Rz target + cutter corner radius -> constant-scallop stepover/stepdown) is a derivation row `Toolpath/surface` consumes, the fits law (ISO 286 IT grade -> finishing-pass allowance) conditions `[V5]` pass planning, and the capability gate runs at PLAN time — a process row whose Cpk history cannot hold the demanded grade fails admission before material is cut — closing spec -> plan -> verify -> capability as one rail. `Documentation/traveler` owns setup sheets and shop travelers as content-keyed shop documents composing the projection views, magazine tool lists, workholding plans, program facts, and spec rows — the machine-tooling-intelligence mandate's document egress (the typed document MODEL only — sheet/annotation RENDERING rides the `[06]` artifacts-plane seam). UnitsNet's folder overlay extends to Force/Power/Temperature/Angle/Torque on the same thin-overlay pattern.

### [V9_PHYSICS_IDENTITY]

`Material` carries ONE identity per physical material with a per-modality physics map (`Map<RemovalModality, ModalityPhysics>`); `Budget` modality-dispatches into it; the fragmented `stainless`/`stainless-abrasive`/`mild-steel-thermal` rows (`physics.md:91-97`) collapse. `RemovalBudget.Erosion` lands (EDM discharge energy/pulse on-off/wire feed/flushing — the `erosion -> ThermalBudget` conflation at `physics.md:136` dies) beside the `[V6]` Resin/Powder cases. `CuttingData` moves off `physics.md:117` onto `Tooling/cuttingdata` and deepens to the machinability model: Kienzle unit-cutting-force `kc` + chip-thickness exponent + per-operation surface-speed + feed-per-tooth columns keyed by the `physics` `Material` identity (the FreeCAD machinability model is the REFERENCE shape; the numeric DATASET gap is real and confirmed un-fillable — FreeCAD removed its cards for copyright — so the table stays honest seed DATA behind a CSV/QIF data-INGRESS arm, never a formula pretending to be measured); `program`'s deflection compensation reads the `kc` column and the `0.0012` literal dies. The `Overrides = FrozenDictionary.Empty` dead carrier (`physics.md:114-115`) DELETES — shop-measured parameter rows enter through the `cuttingdata` ingress arm, never a second override lookup rail; the `90.0` fallback demotes behind the table.

### [V10_GOVERNANCE_TRUTH]

Every stale-law reference closes in one motion. The R6 25xx census: `ARCH:16` ("band-2500 composing kernel band-2400"), `kinematics.md:3` ("band-2500"), and the `TASKLOG:44` superseded 2505-2509 codes inside a `[COMPLETE]` card re-point to 2700/2701-2710 (the `.api` tier ripples with them: `api-dstv-net.md:3,126`, `api-robots.md:5,94`). The netDxf purge: `README.md:33,64`, `ARCH:51`, `import.md:20`, `api-dstv-net.md:132,143`, `api-acadsharp.md:120` re-frame to the AppUi ACadSharp two-format write leg (the read/write split is intra-ACadSharp over one central pin; the `[REJECTED]` netDxf row reframes — netDxf no longer exists to reject), and the manifest pins (`props:359,472`) VERIFY-REMOVE per `[ROSTER_RECONCILIATION]`'s owner attribution. `ARCH:63`'s multi-fixture-scheduler overclaim re-scopes to the `[V1]` `Fixturing/setups` realization; the `[GUARD]-[QUEUED]` IDEAS card closes COMPLETE (the page landed); `api-geometry3sharp.md:106`'s "dropped CavalierContours / one-Clipper2 law" self-contradiction dies; the Fabrication `api-hashing.md` overlay slims to the `api-unitsnet.md` pattern (its "C# has no shared tier" law at `:3,88` is FALSE — the shared `libs/csharp/.api/api-hashing.md` exists) — a thin nesting-identity overlay deferring surface docs to the shared tier. `nfp.md`'s two logic defects land: `Stock.Of()` hashes discriminant + all dimensions (the area-only collision poisons `Remnant.Parent` lineage), and the `Ty*1e6+Tx` heuristic becomes a tuple comparison; the NFP origin-vs-`Anchor` reference-frame consistency (`nfp.md:144` vs `:300,331`) is proven or fixed in the same pass.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — Phase 1 re-verifies every row against disk. Anchors are `libs/csharp/Rasm.Fabrication/.planning/`-relative; `ARCH:`/`README:`/`TASKLOG:`/`IDEAS:` anchor the package root; `CSPROJ:` the project file; `PROPS:` `Directory.Packages.props`; `GBRIEF:`/`PBRIEF:`/`CBRIEF:`/`UIBRIEF:` the upstream briefs.

| # | Class | Evidence |
|---|---|---|
| E1 | Dead admissions | `CSPROJ:16,19,20,24` PicoGK/OcctNet.Wrapper/DSTV.Net/SharpVoronoiLib pinned; word-boundary grep over 17 pages: zero consumers for all four; README charters `[IMPLICIT_VOXEL]`/`[SOLID_INGRESS]`/`[STEEL_FABRICATION_EXCHANGE]`/`[VORONOI_TESSELLATION]` fence-realized nowhere; `.api` catalogs 121-161 LOC each |
| E2 | Unwired pipeline | `guard.md:3` "Cam.Solve consults" vs `motion.md:46-62` (no `Guard.Check` call); `magazine.md:18,22` "Post emits ToolChange" vs `program.md:141-164` (no `Magazine.Schedule` consumer); `workholding.md` `Condition` called by no fold; `ARCH:63` presents the queued scheduler as owned |
| E3 | Arc-rail fiction | `clipper.md:5,33` retirement claim vs `program.md:247-282` still `g3.BiArcFit2`-refitting; `owner.md:42` `Loop(Arr<Point3d>, bool)` — no `Bulges`/3-arg ctor/`BulgeAt` the consumer calls (`clipper.md:257,271`); fence namespaces `lnContours`/`PlineOffset.ln<>` (`clipper.md:169-227`) vs admitted `CavalierContours` (CSPROJ/README:49/.api); hand-built lead arcs `program.md:215-223` |
| E4 | Post thinness | `family.md:80-96` three columns; `:94-95` binary `Admits`; `program.md:122-136` `Emit` varies by `LineNumbers` only; `:41-65` dialect-invariant `GCommand.Code` (`thread-cycle -> "G92"` Fanuc-lathe-only); `Dwell`/`Pierce` both "G4" (`:46,54`), `Feed`/`Extrude` both "G1" (`:43,58`); zero canned-cycle/macro/subprogram/WCS/cutter-comp/arc-mode nodes |
| E5 | Stub generators | `motion.md:71,73` `radial-sweep` AND `helical` -> `Turn`; `:76-79` `Turn` = `v.Y - radius - k*stepOver` (no ZX profile; `BarStock` prose-only); `:81-84,105-108` `Plunge`/`Peck` centroid stubs; `:86-87` `SliceWalk` orders nothing; prose claims "over the BarStock revolved envelope" (`:14,16`) |
| E6 | Physics identity | `physics.md:99` single `ModalityPhysics`; `:91-97` `stainless`/`stainless-abrasive` fragmented identities vs `:3` anti-flat-record prose; `:136` `erosion -> ThermalBudget`; `:109` FFF-only `Additive` budget; `:114-115` `Overrides = Empty` dead table; `:141` `90.0` fallback; `program.md:183` `0.0012 * RemovalRate` magic force |
| E7 | HLR not CAD-grade | `projection.md:74-77` centroid-classified BSP, no facet splitting; `:150` per-edge average depth; `:102,145` all mesh edges undeduped, no crease filter; `:100` `1e6` eye distance; superseded by `GBRIEF:77` (`[V3]` kernel `DrawingProjection`, "Fabrication consumes"); AppUi insulated at the receipt (`UIBRIEF:146`) |
| E8 | Coverage silences | zero corpus hits: waterline/scallop/pencil/rest-machining/swarf; NC verification (guard is per-move only, `guard.md:59-67`); production specs (no tolerance/finish/capability/traveler owner); additive supports/orientation/machine-profiles/3MF; G-code READ; `KinematicClass` 4 rows — `mill-5axis` binds `CartesianGantry` through `Process.Mill`'s row (`family.md:100,118`); DSTV `KA` bend blocks unread; no typed cutter-form vocabulary (`ToolAssembly` carries the ISO-13399 measurement set, `magazine.md:5,161`, no form axis; `Cam.ToolRadius` a bare double, `owner.md:85`); specs egress-only — no spec->plan derivation anywhere |
| E9 | Identity/logic defects | `nfp.md:88` `Stock.Of()` hashes `Area` alone (equal-area stocks collide; `Remnant.Parent` lineage poisoned vs `:59` full-vertex exemplar); `:137` `Ty*1e6+Tx`; `:144` origin-referenced NFP vs `:300,331` `Anchor`-referenced test unverified; `workholding.md:73-83` 3-point keep-out sampling; `:49-50` `ForHolding` first-match (`Vise` unreachable); `:39-44` `MarginScale` scalar vs kind-shape prose |
| E10 | Stale governance | `ARCH:16` band-2500; `ARCH:51` netDxf write-leg; `ARCH:52,53` `Rasm.Persistence/Schema` (deleted folder); `README:33,64` netDxf; `import.md:20` netDxf rationale; `kinematics.md:3` band-2500; `TASKLOG:44` 2505-2509 in a COMPLETE card; `api-dstv-net.md:3,126,132,143` + `api-robots.md:5,94` band-2500/netDxf; `api-acadsharp.md:120` stale write-leg; `api-geometry3sharp.md:106` "dropped CavalierContours"; `PROPS:359,472` netDxf/netDxf.netstandard pins (owners: AppUi `[V7]` / Compute `[V8]` FEALiTE2D.Plotting floor); `IDEAS:27` `[GUARD]-[QUEUED]` realized-but-open |
| E11 | Ledger gaps | `stock.md:40,83,99` `Rasm.Element/MaterialId` wired-undeclared (absent from `ARCH:[02]`); intra-package web undeclared (magazine->guard/workholding `HolderEnvelope`, guard->skeleton `ClearanceAt`, workholding->posting `Sequence`, motion->skeleton/slicing/kinematics); `physics.md:3` anchor drift vs `ARCH:48`; `ARCH:50` Compute rollup counterpart un-named Compute-side (waterfall landed: `CBRIEF [V12]`) |
| E12 | Redundancy/overlay | `PROPS` RectpackSharp (`:86`) + RectangleBinPack.CSharp (`:167`) both MaxRects/Guillotine AABB engines — RBP strictly richer (4 packers + `SingleBinPack` + `FreeRectangles` remnants, `api-rectanglebinpack:27-32`); `README:65` rejects MaxRect/BinPack.NET "superseded by RectpackSharp" while RBP subsumes RectpackSharp; `api-hashing.md:3,88` false "no shared tier" law (shared `libs/csharp/.api/api-hashing.md` exists; README:70); `magazine.md:160-161` `Runout` literal stub |
| E13 | Page-craft | `[03]-[RESEARCH]` tails: `kinematics.md:122-125`, `skeleton.md:199-202`, `slicing.md:219-222`; author-forever seals now false: `skeleton.md:3`/`slicing.md:3,221` "no managed library exists" vs `GBRIEF [V10]b/d` landing the kernel owners (and gsSlicer as managed prior art) |
| E14 | Upstream bindings | `GBRIEF:105` `[V10]` toolpath gate (CavalierContours ONE stratum; clearance-radius carriage; slice policy rows); `GBRIEF:77` `[V3]`; `GBRIEF:85` `[V5]` Manifold scale tier; `GBRIEF:184-185` Clipper2 RE-SCOPE to Fabrication + geometry3Sharp/SharpVoronoiLib kernel strike; `GBRIEF:201` Fabrication toolpath dry-run acceptance; `PBRIEF:108,204` durable-rows re-anchor + 2701-2710 decode; `CBRIEF:32` no-AEC-peer-reference law; `UIBRIEF:93,146` HiddenLineSeam both-sides + receipt insulation; `.archive/RASM-COMPONENT-PARADIGM-DECISION.md:56,75` re-band + `Nest.Honor` rebind landed |

Sound surfaces (preserve; cold-pass plus ONLY the additive deltas a verdict names): `Process/family.md` (9), `Process/owner.md` (9), `Process/faults.md` (9), `Nesting/nfp.md` (9, minus E9), `Nesting/stock.md` (9), `Process/magazine.md` (8.5), `Toolpath/kinematics.md` (9), `Toolpath/guard.md` (8.5, wire-only), `Polygon/clipper.md` (9 craft, `[V2]d` siting), `Polygon/import.md` (9, move + netDxf purge).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar for Phase 2; the DECISION carries the per-page skeletons that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Process axes (owner/family/faults) | 8 | 9.5 | policy cases Additive/Verify/Inspect; dialect grammar columns; strategy dimensionality column; `KinematicClass` rotary topology; typed fault payloads + new arms; `Loop.Bulges` ratified |
| Physics + machining data | 6 | 9 | per-modality identity map; Erosion/Resin/Powder budgets; Kienzle `kc` machinability columns (homed `Tooling/cuttingdata`); data-ingress arm; dead `Overrides` deleted onto the ingress arm |
| Tooling | 8 | 9.5 | magazine wired into Post; cutting-data page; typed `CutterForm` axis projected from ISO-13399 (four named consumers); tool-life telemetry seam (AppHost livewire counterpart); `Runout` policy-typed with the probing-measured upgrade named |
| Geometry2D substrate | 8 | 9.5 | two-owner split; `[V10]a` siting executed; lnContours divergence resolved; arc rail consumed downstream |
| Ingress | 5 | 9.5 | profile move; OcctNet STEP/IGES/STL solid page (`OcctShape -> OcctMesh` feeding slicing/surface/projection); DSTV NC1 read page; one polymorphic admit discriminating on source kind |
| Toolpath (subtractive CAM) | 5 | 9.5 | real generator arms; waterline/scallop/pencil/rest/3+2/swarf rows; OpenCAMLib surface engine; Voronoi partition page; engagement-control + `[V8]` finish-derivation laws; guard wired per-move; skeleton -> kernel-medial consumer |
| Kinematics | 7 | 9.5 | robot cell held; 5-axis machine-tool topology + TCP/RTCP owner; one motion-dynamics law with posting |
| Additive | 4 | 9.5 | kernel slice-stack consumption; PicoGK implicit/lattice/support/resin-powder lane; orientation owner; machine profiles; lib3mf 3MF egress; budget cases |
| Nesting | 8.5 | 9.5 | `Stock.Of()` identity fix; NFP frame-consistency proof; Persistence/Compute seam re-anchors; MaterialId ledger row |
| Fixturing | 6 | 9.5 | segment-exact keep-out; per-kind footprint geometry; `Condition` wired; multi-fixture setups page owning WCS assignment rows; op-N admission against `[V7]` stock snapshots |
| Posting | 6 | 9.5 | grammar families + canned cycles + macros/subprograms + WCS + cutter comp + arc mode; per-dialect code overrides; structured cycle/subprogram AST nodes; NC1 emit target; modal-state `Parse` round-trip arm; WCS rendered from setups assignment; `CutProgram` content key |
| Verify | — | 9.5 | voxel material-removal receipts (gouge/uncut/overcut/air-cut); residual-stock field for rest rows; per-setup content-keyed stock snapshots; probing cycles + measured features on AppHost-decoded ingress |
| Spec | — | 9.5 | ISO 286/1101/1302 + ASME Y14.5 typed vocabulary, QIF-aligned; Cp/Cpk/SPC + Monte-Carlo stackup over MathNet; process-driving derivation rows (Ra->scallop stepover, IT-grade->allowance, plan-time Cpk gate) |
| Documentation | 6 | 9.5 | projection as kernel-`DrawingProjection` consumer with preserved `HiddenLineResult`; setup-sheet/traveler owner composing views/tools/fixtures/specs |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fence rides the C# spine — LanguageExt `Fin`/`Option`/`Seq` rails, Thinktecture `[Union]`/`[SmartEnum<T>]`/`[ValueObject]` generated families, `System.IO.Hashing` `XxHash128` content identity (nesting overlay slimmed per `[V10]`), `UnitsNet` at the cut-parameter boundary (overlay extends: Force/Power/Temperature/Angle/Torque), `MathNet.Numerics` for the `[V8]` capability statistics (shared substrate, first Fabrication consumer; the central pin rides the Geometry `[ROSTER_RECONCILIATION]` 5.0.0 downgrade ruling), `QuikGraph` composing the `Fixturing/setups` operation-precedence/datum-lineage graph — operation SEQUENCING is the NP-hard half and homes on setups, while the magazine eviction greedy stands (keep-tool-needed-soonest is OPTIMAL for a fixed sequence), never a second magazine rail (admitted substrate, zero Fabrication consumers); a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package; the substrate `api-languageext.md` VERIFIES under the track-shared authoring law (the first upstream campaign lands the ONE substrate file; this campaign verifies, never re-authors).
- Shared-tier mine rows (uncited tier-1 stubs judged against the target scope; anchors verified): `Riok.Mapperly` — generated boundary mappers own the two-`Rhino.Geometry` `extern alias R3` Robots seam, the MTConnect decoded-telemetry rows, and the DSTV-record -> `Loop` ingress projection (`api-mapperly.md:3,21-23`; zero-reflection attributed partials — hand-written seam copyists die); `System.Numerics.Tensors` — `TensorPrimitives` SIMD-lowers the hot sampling folds: engagement fields, the scallop/drop-cutter grids where the `[V5]` probe fails over to the author-kernel, NFP sweep distances, capability batch statistics (`api-tensors.md:3,13-14`); `CommunityToolkit.HighPerformance` — `Span2D`/`Memory2D` carries the 2D grids (grayscale layers, uncut/overcut maps, engagement rasters) without jagged-array spam (`api-highperformance.md:26-29`; the hashing overlay's `HashCode<T>.Combine` rejection holds — no second digest owner); `NodaTime` — `Instant` stamps traveler/probing/tool-life/SPC receipts (`api-nodatime.md:23`). Judged OUT with reasons: `CSparse` (no sparse system in the owned scope; MathNet owns the dense capability statistics), `Generator.Equals` (Thinktecture generated families already own receipt value equality), MathNet native providers (small-N statistics), the wire family MessagePack/protobuf/gRPC + `HybridCache` (sidecar wire and caching ride the Compute/Persistence seam law, `[06]`).
- `PicoGK` — the densest dead admission, THREE lanes: (1) implicit/TPMS/gyroid/cellular conformal infill (`IImplicit -> Voxels`), (2) `Lattice` supports + SLA/DLP/MSLA grayscale/`.cli` layer stacks (`oVectorize -> CliIo`, `Vdb2Cli`), (3) voxel Boolean NC-verification (`BoolSubtract` stock-minus-swept, `[V7]`); net9 + native core = ALC-firebreak/sidecar posture (`api-picogk.md:146,151-155`).
- `OcctNet.Wrapper` — `ImportStep` (AP203/214/242)/`ImportIges`/`ImportStl` -> `OcctShape` -> `Triangulate` -> `OcctMesh` (`api-occtnet-wrapper.md:59-61`); the recorded SCOPE_LIMIT (`:120-124` — `libTKHLR`/`libTKXCAF` managed-UNBOUND, single-shape, v0.1.1 single-maintainer) is a standing demand row: assembly + AP242 PMI surfacing is consumer pressure on the wrapper (the `Spec/` plane's secondary source), and the maintenance risk is re-verified each leg. Boundary law: `Ingress/solid` is manufacturing B-rep ingress feeding CAM/slicing/projection — a semantic STEP BIM-interchange leg would be a `Rasm.Bim` `Format` row (none realized today), never grown here.
- `DSTV.Net` — the NC1 record tree (`ST` header + `BO`/`SI`/`SC`/`AK`/`IK`/`KA` blocks) as READ (`Ingress/steel`) and the emission MODEL for the posting NC1 target (`api-dstv-net.md:135` routes emission to the posting owner); the `KA` bend rows seed the sheet-metal IDEAS lane.
- `SharpVoronoiLib` — `VoronoiPlane` Fortune sites + Lloyd relaxation (`Relax(iterations, strength, reTessellate)`, `api-sharpvoronoilib.md:85`; `VoronoiSite.Centroid` the Lloyd/spiral-pocket seed, `:99`): pocket-region seeds, stipple/engrave/pen-plot spacing (`Toolpath/partition`); point-site only — polygon medial stays the kernel's.
- `CavalierContours` — `PlineOffset.ParallelOffset<O,T>` (`api-cavaliercontours.md:115`), the island-preserving `Shape<T>.FromPlines(loops).ParallelOffset(offset, ShapeOffsetOptions<T>)` (`:147`), the `FindPointAtPathLength` arc-length sampler (`:103`), `StaticAABB2DIndex` (`:56`) — consumed at the `[V10]a`-ruled stratum; the fence's `lnContours` spelling corrects to the admitted package's real namespace surface (member-verify via `assay api` at the leg).
- `Clipper2` — the line-space owner (offset/`DeltaCallback64` variable offset/Boolean/Minkowski/open-path clip); RE-SCOPE lands: the central pin moves from the Kernel-Geometry label to Fabrication's (`GBRIEF:184`); the `ReuseableDataContainer64` scanbeam-reuse stacking law holds.
- `RectangleBinPack.CSharp` — the ONE rectangle engine post-`[V12]`-removal: MaxRects/Skyline/Guillotine/Shelf + `SingleBinPack` mass-cut + `FreeRectangles` remnants; nfp's rect-fastpath re-homes to a `MaxRectsBinPack` heuristic-sweep fold; pin re-groups from the vacated Materials label.
- `Robots` — the cell owner held (per-manufacturer RAPID/KRL/URScript/VAL3/DRL posts, `Program` look-ahead, `extern alias R3` boundary); its jerk/accel planner names the shared motion-dynamics law with posting `Lookahead` (`[V4]`).
- `MTConnect.NET-Common` — model-only slice held (ISO-13399 `CuttingToolAsset`, `GenerateHash` reconciled with `XxHash128`); the transport/agent half is AppHost livewire's (waterfall landed: `APPHOST [04]` MTConnect.NET row; the magazine mid-job reload AND the `[V7]` probing measured-feature ingress consume the streamed telemetry at that seam).
- `ACadSharp` — read-only ingress held (`Failsafe` rail, bulge/NURBS samplers, `Insert.Explode`); the dead `warnings` accumulator (`import.md:54-55` captured, never read, contradicting `:5,20`) wires the strict-mode `NotificationType.Error` escalation — ruled; the claim-deletion form loses ingress-degradation visibility and is forbidden; write stays AppUi's.
- ADD `OpenCAMLib` (LGPL-2.1; release archives ship precompiled `macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64` libs) — drop-cutter/push-cutter/waterline/adaptive-waterline 3D-surface sampling; the shipped ABI is C++ with pybind11/emscripten the only upstream bindings, so the thin in-house binding is C-shim + P/Invoke, never a direct bind — the Phase-1 probe rules shim feasibility (`[V5]`); RID assets recorded at admission; golden-fixture gated; `.api` catalog authored with the admission.
- ADD `lib3mf` (BSD-2, official C# binding, native core; NOT on NuGet — vendored-binding admission mechanics recorded) — 3MF core/production/beam-lattice writer for the additive hand-off (`[V6]`; a manufacturing hand-off, never a `Rasm.Bim/Exchange` format row); `.api` catalog authored with the admission; golden-fixture gated; the sole managed alternative `IxMilia.ThreeMf` (MIT) is unpublished on any feed, dormant, and core-spec-only — no beam-lattice/production extensions — dominated.
- REMOVE `RectpackSharp` — proven redundancy: strict subset of RectangleBinPack.CSharp (`PackingHints.FindBest`, the try-every-heuristic densest-result sweep `RectanglePacker.Pack` defaults to — `api-rectpacksharp.md:61,72` — reproduces as one heuristic-sweep fold over `MaxRectsBinPack.Insert`); the one consumer (nfp fast-path) re-homes; `README:65`'s rejection rationale rewrites.
- VERIFY-REMOVE `netDxf` + `netDxf.netstandard` residue (`PROPS:359,472`) — `netDxf` leaves with the AppUi `[V7]` motion, `netDxf.netstandard` with the Compute `[V8]` FEALiTE2D.Plotting retirement (its sole lock parent); this campaign removes only a surviving orphan per `[ROSTER_RECONCILIATION]`.
- REPLACE-candidate `geometry3Sharp` — abandoned upstream (kernel strike, `GBRIEF:185`; Fabrication its last consumer via `BiArcFit2`): the arc-native rail (`[V3]`) retires the refit for arc-sourced paths; line-sourced chains (kernel mesh sections) land an owned Bolton biarc fold on the posting page; geometry3Sharp leaves the manifest when both land — it stays pinned (re-grouped to Fabrication) ONLY if the owned fold cannot meet refit tolerance on the golden fixtures, the abandonment recorded.
- REFERENCE records (never vendored; recorded so silence is never mistaken for oversight): `gsGCode` (Boost, source-only — parse-arm prior art), `gsSlicer` (MIT, source-only — FFF enrichment prior art), MillSimSharp (MIT beta — voxel-verify corroboration), CAMotics/PrusaSlicer/CuraEngine/libslic3r (GPL/AGPL — the concern-validity proofs), FreeCAD `Adaptive2d`+machinability model (LGPL — adaptive validation + `kc` schema), FreeCAD_SheetMetal (LGPL — K-factor unfold algorithm), Tweaker-3 (orientation objective), UVtools (AGPL, feed-live v6 — the resin machine-format breadth proof: CTB/Photon/SL1-class conversion stays downstream shop tooling, the neutral `.cli`/3MF hand-off holds), PySLM (LGPL Python — powder-bed hatch/scan-strategy prior art for the `[V6]` Powder rows), QIF/ISO 23952 free schemas (the `Spec` data-model authority), NIST RS274NGC v3 (the dialect grammar authority), E3Studio (MIT app — dialect preset reference). REJECTED: Fanuc FOCAS (proprietary/distributor-gated), STEP-NC `Generate`/ModuleWorks/EncyCAM (pay-tiered), Aspose.3D (pay), ODA/CadEx PMI SDKs (proprietary), PathCAM (GPL managed, dominated), NetTopologySuite-for-medial (float model loses to the exact posture), GcodeParserSharp (Apache-2.0 FFF print-analyzer — no dialect grammar, no modal-group state; the author-kernel `Parse` arm dominates); the OCCT-wrapper supersession sweep re-ran and `OcctNet.Wrapper` HOLDS with its SCOPE_LIMIT demand row — Occt.NET (7.9.x, no license expression, binary-only donation-ware), Macad.Occt (MIT but C++/CLI win-only, app-coupled, unpublished on any feed), Ab4d.OpenCascade (pay), Xbim.Geometry.Occt (BIM-build-coupled) all fail the gate.
- Stratum-split records: Manifold is the KERNEL's `[V5]` scale tier (Fabrication consumes booleans at the arrangement seam — never a Fabrication pin); MTConnect.NET full stack + OPC-UA (GPL2/RCL dual) + umati are AppHost transport; machine telemetry enters Fabrication only as decoded seam data.
- TASKLOG/IDEAS absorption: `[GUARD]-[QUEUED]` closes COMPLETE; `PROBING` realizes into `[V7]`; `MULTI_FIXTURE_SCHEDULE` realizes into `[V1]` `Fixturing/setups`; `NFP_DRL_POLICY` stays BLOCKED (the injected-delegate column is the standing strata-correct shape; closes when Compute lands its side); the IDEAS pool reseeds with the named growth rows (sheet-metal bend/unfold, 5-axis simultaneous swarf refinement, conversational-dialect breadth beyond the seed rows).

## [05]-[BUILD_LEGS]

Phase 2 runs the durable rebuild engine per leg with `RASM-CS-FABRICATION-DECISION.md` as brief. The DECISION owns the exact partition, proven acyclic against the corrected seam ledger (arrow = composes; every edge within-wave or earlier). The sequencing law it must honor:

1. **STRUCTURE + AXES + RAIL** — the `[V1]` re-partition lands corpus-wide (moves, splits, namespaces, `ARCHITECTURE.md` `[01]`/`[02]`, README router) with the FULL roster reconciliation (`[04]` adds/removes/re-groups, netDxf purge, `.api` stubs, overlay slimming) — it gates every later page's imports and catalog claims; `Process/` rebuilds (owner entry growth as signature-locked case SHAPES — each new arm's compose seam realizes in the leg landing its owning plane — plus `Loop.Bulges` and the `[V7]` `ResidualStock` + per-setup stock-snapshot vocabulary on `owner#atoms`; family axis widening — grammar columns, strategy + dimensionality rows, `KinematicClass` topology, modality rows; physics identity map + budgets; faults payloads + new arms + 25xx closure); `Tooling/` lands (magazine wire-ready; cuttingdata — the `physics.md:117` table moves in and deepens to the `kc` machinability model).
2. **SUBSTRATE + INGRESS** — `Geometry2D/` (two-owner split, `[V10]a` siting executed both-dispositions, lnContours correction, arc rail exported), `Ingress/` (profile move + netDxf purge, solid, steel), the `Toolpath/skeleton` page rebuilds HERE as the kernel-medial consumer (cross-folder, staged early: wave-3 motion's engagement bound reads its clearance field); `slicing`'s slice-stack re-route stays staged for wave 3.
3. **CAM + ADDITIVE + NESTING + FIXTURING** — `Toolpath/` (motion real arms + engagement law + guard wired; surface over the OpenCAMLib probe result; partition), `Kinematics/` (cell held, machine), `Additive/` (slicing consumer, implicit, production + lib3mf), `Nesting/` (identity fix, seam re-anchors, MaterialId ledger row), `Fixturing/` (workholding safety + footprints + `Condition` wired, setups); `Spec/tolerance` rebuilds HERE (cross-folder, staged early: surface's Ra->scallop derivation and the `[V5]` finishing-allowance conditioning read its rows within-wave).
4. **EGRESS + TRUTH + DOCS** — `Posting/` (program AST growth + content key + magazine/workholding consumption; dialect grammar families + canned/macro + NC1 emit + `Parse` arm), `Verify/` (removal, probing), `Spec/` (capability; tolerance staged wave 3), `Documentation/` (projection consumer with preserved `HiddenLineResult`, traveler).

Per-leg closeout: hard residuals resolve before the next leg launches; `README.md`/`ARCHITECTURE.md`/`TASKLOG.md`/`IDEAS.md` close in the leg that changes their facts; `.api` stubs and manifest edits land in wave 1, and the leg that first composes an admitted package deepens its stub; cross-package counterpart edits are 1-hop ripples recorded in the leg return (the four waterfall clauses this brief landed upstream — `GBRIEF [V10]` slice/medial demand rows, `GBRIEF [04]` pin-regroup scoping, `CBRIEF [V12]` WasteAreaMm2 row, `APPHOST [04]` MTConnect.NET row (now carrying BOTH Fabrication consumers: magazine tool-life + `Verify/probing` measured features) — are verified in place, not re-edited). Residuals naming a later leg's pages transfer forward explicitly, never silently.

Campaign acceptance closes on the artifact, not the ledger — four dry-runs compose entirely from rebuilt fences: (a) the `GBRIEF:201(b)` toolpath dry-run's Fabrication side — corner-strategy offsets, an oriented slice stack, a medial clearance-radius read, a distance-field read, ZERO Fabrication-side geometry re-implementation; (b) a five-target posting dry-run — one part posts to fanuc/haas/klartext/grbl + NC1 with byte-DIVERSE output, canned cycles emitted as single blocks where the family admits, a `Parse` round-trip on the word-address subset, and the `CutProgram` content key minted; (c) an additive dry-run — STEP solid in, orientation scored, lattice supports + TPMS infill, an FFF program AND a resin `.cli` stack AND a 3MF hand-off out, all three content-keyed; (d) a production-truth dry-run — a TWO-SETUP multi-tool program voxel-verified (gouge/uncut receipts), the op-2 clamp admitted against the op-1 stock snapshot, work offsets setups-assigned and probe-reconciled, rest-machining rows consuming the residual field, probing receipts feeding a Cp/Cpk row that gates the next plan admission, one setup sheet + traveler emitted. A dry-run that reaches for a missing owner reopens the owning verdict as a residual.

## [06]-[OUT_OF_SCOPE]

- Kernel interiors: corner-strategy offset, slice stack, medial/clearance, `DrawingProjection`, exact booleans + Manifold tier, the distance-field lane, hull — consumed at seams (`GBRIEF [V3]/[V5]/[V8]/[V10]/[V11]`); the waterfall demand rows this brief landed there are verified in place, never re-planned here.
- `Rasm.Compute` interiors: the `WasteAreaMm2` rollup runner (`CBRIEF [V12]` counterpart row), the mesh/GLB wire layout law (decode, never re-mint), DRL nest-policy learning (the injected delegate stays the boundary).
- `Rasm.Persistence` interiors: blobstore content rows, cache artifact index, the durable-row mechanics — Fabrication mints content keys and decodes 2701-2710 (`PBRIEF [FABRICATION_PROGRAM_DURABLE_ROWS]`).
- `Rasm.AppHost` transport: machine connectivity (MTConnect agent/SHDR/MQTT, OPC-UA/umati, BACnet-class industrial transports) — livewire's; Fabrication consumes decoded tool-life telemetry only.
- `Rasm.AppUi`: the CAD WRITE leg (ACadSharp `DxfWriter`+`DwgWriter`), `Viewport2D` rendering — Fabrication supplies the insulated `HiddenLineResult` receipt and never grows a write arm.
- `Rasm.Bim`/`Rasm.Element`/`Rasm.Materials` interiors: IFC semantics, the `ElementGraph`, material property tables — peer boundaries stay raw-double/delegate/seam-row shaped; the future `FabricationProjector: IElementProjection` row stays queued.
- GD&T frame DRAWING and sheet/annotation rendering — the artifacts/drawing plane's (Fabrication owns the `Spec` vocabulary it draws from).
- The sheet-metal bend/unfold lane — seeded IDEAS growth (DSTV `KA` decode lands with `Ingress/steel`; K-factor unfold waits for its consumer).
- The federation-wide fault-band renumber beyond the Fabrication registry — escalation path only if the new +11.. arms cannot fit the 2700 century.
