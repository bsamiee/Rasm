# DOSSIER — corpus-b — libs/csharp/Rasm.AppHost/.planning/

Lane: corpus-b. Deep-read = SECOND HALF of the alphabetical subfolder set: **Runtime (11), Sandbox (3), Wire (6)** = 20 pages / 5,997 LOC. First half (Agent, Observability) skimmed only at crossing seams. Cross-checked against `Rasm.AppHost/ARCHITECTURE.md`, `libs/.planning/architecture.md`, `RASM-CS-PERSISTENCE-BRIEF.md` (E5/E6/V3), `RASM-CS-COMPUTE-BRIEF.md` (V1), `RASM-COMPONENT-PARADIGM-DECISION.md` band references.

## STANCE CALIBRATION (honest adversarial read)

This corpus is **world-class, not naive**. COVERAGE naivety is largely absent — every page owns a full polymorphic axis with closed cases, typed rails (`Fin`/`Validation`/`IO`), receipt evidence, TS projection, and a research tail that names the exact external members. APPROACH naivety (enumerated-where-parameterized) is rare — rosters ARE seed data on smart-enum/union axes. The defects below are **surgical precision failures + cross-cutting seam reconciliations**, not fundamental-approach rot. The single genuine duplicate-mechanism defect is the Sandbox supply-chain gate. Grades cluster 8.5–9.5 because the bar is already met; the value is in the seam disposition, not a rebuild mandate.

---

## PER-PAGE VERDICTS

### Runtime/ (the spine — 11 pages, cohesive folder)

**ports.md — 9.5** — Seven inward port records (`ReceiptSinkPort` … `HealthContributorPort`), `PortCardinality` conserved-invariant (8th = defect), HLC two-half `ReceiptEnvelope`, boot-minted `TenantContext`/`TenantId` (UInt128 Thinktecture VO), `SuiteContracts.Wire` merge + `AppHostWireContext` source-gen JSON, TS projection. Owner charter is exactly right: the ONE cross-package seam, everything else composes as settled vocabulary.
- DEFECT (:114): `System.Text.Json.Schema.JsonSchemaExporter.GetJsonSchemaAsNode` + `JsonSchemaExporterOptions.TreatNullObliviousAsNonNullable` self-flagged "the one open verification before transcription" — real .NET 9+ members but uncatalogued (phantom-risk, low).
- DEFECT (:122-139): `AppHostWireContext` `[JsonSerializable]` list (18 rows) is **missing `UpdateReceipt`**, which provisioning.md:148 dereferences as `AppHostWireContext.Default.UpdateReceipt` → phantom member (see VC-4).

**orchestration.md — 9** — Crash-durable workflow: `WorkflowInstance` hash-chained over `EventLog.Chain`, 5-case `StepKind` union (Activity/Timer/Signal/Compensation/PersistentJob), saga compensation fold, `StepStateSeam` (Persistence CAS ripple), `CrashResume` over the collapsed `SupportTrigger.FaultTransition`. Executor IS `CommandDispatch.Run` (no parallel dispatcher) — the dangling `orchestration ⇄ CommandAlgebra` ref resolved.
- DEFECT (:18 vs :93): prose Entry sig `WorkflowInstance.Begin(workflowId, plan, tenant, at)` omits the `FencingToken fence` param the :93 fence carries → prose-vs-fence drift.
- DEFECT (:218): `instance.Steps.LastOrDefault() ?? throw new UnreachableException()` — `throw` in an otherwise-ROP fold (invariant assertion; doctrine prefers typed rail).
- GAP (SCOPE MANDATE / Compute [V1]): `StepStatus` (pending/running/committed/waiting/compensated/failed) never maps the Element/Compute `AssessmentOutcome` lattice (Dispatchable/Transient/Terminal). See VC-5.

**laneguard.md — 9** — In-process `LaneGuard` keyed Polly pipeline per `WorkLane` mirroring `Wire/outbound#KEYED_PIPELINES`, adaptive concurrency (ResourceMonitoring), load-shed (`DegradationReading`), Simmy chaos (test-host only), one-`ShedVerdict`-two-consumers crossing to `Compute/Runtime/admission` (ONE_DEGRADATION_SHED_VERDICT). Correctly homed.
- DEFECT (:114-116): `AdaptiveConcurrency.Resize` inline CPU literals `cpu >= 0.90d`, `cpu >= 0.75d` are **bare literals not named consts** — directly inconsistent with `LaneGuard`'s own named `BulkheadPermits`/`QueueLimit`/`ChaosInjectionRate` consts and with outbound.md's explicit "every strategy value traces to its declaring field, never an inline literal" (outbound.md:241). ARCHITECTURE.md prohibition on bare bounds applies.

**lifecycle.md — 9** — 8-phase `RuntimePhase` CAS transition law, `Lifecycle` Atom capsule, 4-case `FaultSource` collapsing capture-delegate+FaultCommitted into one `SupportTrigger.FaultTransition`, rank-band `DrainConductor`, `CancelScope` provenance spine. Excellent fault-source collapse.
- DEFECT (:288, :394 + RESEARCH [TWO_PHASE_DRAIN]): the Stores-band 2PC in-doubt reconciliation seam references `Rasm.Persistence/Query/transaction#TWO_PHASE_COMMIT` `TwoPhase.InDoubt(db)`. **Persistence brief E5 confirms `APPHOST:71 Query/transaction` STALE** and line 108 folds `transaction → graph#STORE_RAIL`. Stale seam target. See VC-3.

**determinism.md — 9.5** — Reproducibility kernel: `DeterminismContext` (seed/float-mode/`EnvFingerprint`), content-addressed hash-chained `EventLog`, `ReplayVerify`, `MacroEngine`, `RecomputeGraph` (minimal-cone incremental), `AdversarialProbe` (chaos-replay / log-time bisection / counterfactual). Genuinely deep — the recompute-graph + counterfactual is award-tier.
- DEFECT (:118-125 + RESEARCH [OPLOG_PROJECTION] :406): `EventLog.ToOpLog` constructs `new OpLogEntry(Sequence, EntityKind, EntityKey, ColumnFamily, Kind, Codec, Payload, Image, ContentKey, Closure, Actor, OriginStoreId, Physical, Logical)` — a Persistence `Version/ledger#CHANGEFEED`-owned 14-field record **inline, positionally**, with hardcoded `"determinism.command"`/`"command"`/`SnapshotCodec.JsonStj`. :406 admits the spelling is unverified. AppHost should DECODE Persistence types, not CONSTRUCT their interior wire shape positionally. See VC-7.

**config.md — 9.5** — 8-source ranked `ConfigSource` chain, source-gen binder on `Validation<ConfigError,T>`, validate-once-frozen `OptionsAdmission`, RFC-6902 `PatchSection` reload, `OperatorOverride` kill-switch, `SecretLease` acquire/renew/zeroize (with KMS-unwrap `[PORT]` to `Persistence/Element/identity#KEY_ENVELOPE` — ARCHITECTURE:72, correctly DECODEd), canonical RFC-7468 `CredentialPem`. Deepest config surface in the corpus.
- MINOR: several `Boundary:` paragraphs are single sentences spanning 15+ lines (:188, :313) — dense but at the edge of prose legibility (allowed by doctrine).

**time.md — 9.5** — One temporal law: `ClockPolicy` (TimeProvider+IClock pair), 9-row `DeadlineClass` (every suite duration bound traces here), `SchedulePort` single scheduler over Cronos (cron/period/annual + fleet-spread `XxHash3` seed), `FencingToken` (`[ValueObject<ulong>]` Kleppmann `Admits` reject-lower). The FencingToken IS the ONE_FENCED_LEASE_STORE token owner consumed by orchestration/coordination/provisioning — token-VALIDATING fencing, store-enforcement at Persistence. Clean.

**profiles.md — 8.5** — 8-row `HostProfile` variance axis, per-modality `RecoveryObjective(Rpo,Rto)` DR column (→ `Persistence/Version/recovery`, ARCHITECTURE:70), `ProfileBoot` lifetime adapters (systemd sd_notify), `ProfileIdentity` OTel resource, `PowerCell`/`FidelityScale` energy-aware scaling.
- DEFECT (:315 vs :323-326): `PowerProbe.CopyPowerSourcesInfo` `[LibraryImport("…/IOKit", EntryPoint="IOPSCopyPowerSourcesInfo")]` declared, but `ReadDarwin()` returns a hardcoded `(Plugged, Nominal, 1d)` tuple and **never calls it** — dead native declaration; the real read is a tier-3 residual ([POWER_NATIVE]). Decorative P/Invoke until realized.

**features.md — 9** — `FlagDefinition`/`TargetingRule`/`RolloutSegment` compiled into one OpenFeature `InMemoryProvider`, deterministic `XxHash3` sticky bucketing, one `FlagVerdict` seam feeding `Agent/reasoning#MODEL_GOVERNANCE` (ModelRoute) + `Sandbox/provisioning#ROLLOVER_DRAIN` (RollStrategy), `KillSwitchFold` collapsing `OperatorOverride`→prepended `ForcedOff` rule. Clean one-direction seam.
- MINOR (page-craft): uses inline `// --- [TYPES]` / `[MODELS]` / `[SERVICES]` fence dividers (:24,:55,:63,:73) that sibling Runtime pages (config/time/lifecycle) do NOT use → cross-page fence-craft inconsistency.

**modules.md — 9** — One composition root: `ModuleContribution` frozen table (Scan/Services/Keyed/Defaults/Contributors/Registrars/Decorations columns), `CompositionSurface.Compose` one-pass receipted fold, `BoundaryActivation` (probe/scope/decorate/validator). Scrutor `FromAssemblies` only, `Describe`/`DescribeKeyed` only. Rigorous DI law.

**resources.md — 9** — `CacheLane`/`CacheSurface` HybridCache read-through with keyed L2 (the **CACHE_PORT** — ARCHITECTURE:69, L2 `IDistributedCache`+`IHybridCacheSerializerFactory` as "the single Persistence contribution" — correctly DECODEd; Persistence E6 declares the `cache#L2_CONTRIBUTION` counterpart), `PoolPolicy` object pools, `DrainSpec`/`DrainQueue`/`DrainSurface` (Channel + Dataflow Broadcast/Join/BatchedJoin).
- DEFECT (:233,:238,:243,:248,:255): five `throw new UnreachableException()` in `DrainSurface` union-arm projections — assertion guards on by-construction-impossible cases; doctrine prefers total dispatch.
- SEAM MISMATCH: `DrainSurface` is the owner **named by the Persistence brief as the presence-seam driver** (`ledger#PRESENCE ← AppHost DrainSurface`) but carries ZERO presence logic. See VC-2.

### Wire/ (outbound + inbound + bus — 6 pages, cohesive folder)

**outbound.md — 9.5** — `OutboundHop` 7-case union + frozen `HopPolicy` rows, `HttpLane` (Http.Resilience standard/hedging), `KeyedLane` (Polly per-hop, one-retry-owner law), `OutboundSurface` (admit/dispatch/`RetryOwners` conflict cell/receipt), `DiscoveryAttach` (UDS/manifest/checksum/spawn), `DeliveryFanout` (multi-channel dedupe). Model of named-const discipline (:170-180 all `const`/`static readonly`, :241 explicit "never an inline literal"). Owns the egress vocabulary outbox/coordination/topics/livewire consume.
- GAP (SCOPE MANDATE item 2): `HopPolicy` carries `HopIdempotency`/`HopTransport`/`HopRateLimit` but **no delivery-honesty column** (best-effort | at-least-once | exactly-once-effective). Persistence V3 egress mandates "delivery-honesty stated per sink". See VC-6.

**livewire.md — 9** — Industrial-transport studio: 8-row `ExternalTransport` (OPC-UA/Modbus/MQTT/serial/REST/GraphQL/spreadsheet/ERP-PLM), one `SubscriptionLane` (bounded Channel DropOldest) + `Gate`/`LiveClient` token-lifecycle serving every protocol, edge unit-coercion via Compute `QuantityFamily.Admit`, `WriteBack` transaction, `BindingHealth`. Heavy certified-client integration (OPCFoundation/MQTTnet/FluentModbus/System.IO.Ports — the admitted stack per ARCHITECTURE:142). `.api` members verified against pinned catalogs ([TRANSPORT_CLIENTS]).
- DEFECT (:806-825): mints a **standalone `LiveWireOptions.Json` + `LiveWireContext` `JsonSerializerContext`** with its own `ThinktectureJsonConverterFactory` + `MakeReadOnly()` — a parallel wire-options owner beside `SuiteContracts.Wire`/`AppHostWireContext` (ports.md WIRE_LAW: "one merge per app root; every JSON wire surface reads through the merged options"). ports.md:137 registers `WriteReceipt` that livewire never uses. See VC-4.

**companion.md — 9** — Inbound serving counterpart: 3-row `ProcessModality` (companion/sidecar/paired-peer), `PeerRoster` lease-epoch attached-set, `ControlInbound` (3 verbs → degradation/options/support owners), `ServiceHost` (gRPC/UDS), `DegradationCascade` (parent→child floor), `PeerAdmission` (accept-side SO_PEERCRED/LOCAL_PEERCRED credential read), `HostBinding` (9-row OS×activation×address table, systemd/launchd socket activation). Very deep (native P/Invoke authored from man-pages, tier-3 residuals honestly flagged).
- DEFECT (:23): presence value declared to land at `Persistence/collaboration#PRESENCE_AND_BLOB` via `Presence.Beat`, driven by `PeerRoster`/`RosterReceipt`. **Persistence brief E6 + ledger.md:468 name `ledger#PRESENCE ← AppHost DrainSurface`** — different Persistence page (collaboration vs ledger) AND different driver (PeerRoster vs DrainSurface). See VC-2.

**coordination.md — 9.5** — `RoleResolution` (Microsoft.Extensions.ServiceDiscovery), `MembershipView` (probe-driven liveness fold over `WireHealth`), `RoleElection` + `DistributedLock` over the one `FencingToken`/`LeaseElection` (CAS+fenced-lease+membership → Persistence ONE_FENCED_LEASE_STORE, ARCHITECTURE:79-80; Persistence E5 confirms zero-Persistence-owner-yet, AppHost DECODEs correctly). Declares the no-Orleans/no-Consul law. Clean.

**topics.md — 9** — In-process `EventBus`: `Topic`/`Subscription` over `DrainSurface` Broadcast/Buffer/Action builders, HLC offset ordering, `DeliveryFanout` dedupe cell reuse, fed by the outbox sweep. Correctly states in-process = best-effort, at-least-once = the durable outbox leg.

**outbox.md — 9** — Transactional outbox: `OutboxRow` (same-tx enqueue, ONE_OUTBOX_EGRESS_SPINE + SEAM_OUTBOX_AND_WORKFLOW_PERSISTENCE_TABLE — ARCHITECTURE:78), `OutboxRelay.Sweep` over one `SchedulePort` cadence advancing a fenced `(ConsumerId, Hlc)` watermark, `DeadLetter` lane. Names the three keyed-`OutboundHop` consumers over one op-log (this relay / workflow-step dispatch / Persistence Sync/egress) draining one `CdcEnvelopeWire` CloudEvents envelope — **consistent with Persistence [V3]** (CloudEvents = the ONE egress envelope, brief:167).
- DEFECT (:54,:57,:59): `Enqueue`/`Relayed`/`Deferred` each take an `Instant at` param that is **unused** (the row carries no dispatched-at column) → dead parameters.
- DEFECT (:106-114): in `Relay`, the `_advance` binding (fenced watermark advance vs `Defer`) is discarded and `Defer` always yields `0UL` — the watermark threading is notional, not wired into the returned `DeliveryReceipt`.

### Sandbox/ (3 pages — the WEAKEST folder; see VC-1, VC-8)

**provisioning.md — 9.5** — `UpdateRail` (Velopack post-fetch state machine), 3-row `UpdateChannel`, **canonical `SupplyChainGate`** (Sigstore `TryVerifyDigestAsync` + `NuGet.Versioning` `VersionRange.Satisfies` + in-toto SLSA, offline `FileTrustRootProvider`), `RolloverDrain`+`FleetRoll` (`RollStrategy` canary/blue-green/linear-wave health-gated waves over `PeerRoster`, FLAG_VERDICT-selected, FencingToken conductor election). Consumes companion `PeerRoster.Attached` + `WireHealthRow` correctly (ARCHITECTURE:80,82). Clean.
- Note (:207): explicitly claims "both the self-update release AND a downloaded plugin/companion artifact verify through this one `Admit`, never two verify paths" — **contradicted on disk by isolation.md**. See VC-1.

**isolation.md — 7.5** — 2-row `SandboxIsolation` (wasm-component/process), `GrantHandle` + unified `Mediate` (operator/agent/plugin caller-modality on one broker), `QuotaCell`/`Quarantine` kill-rail. The grant-mediation and no-ambient-authority law are strong (9-tier).
- MAJOR DEFECT (:246-320): ships its **OWN `SupplyChainGate`** (record with `Func<…,bool> Verify` delegate) + **OWN `SupplyChainFault`** (4680 band vs provisioning's 1320) + **`SemverGate.Compatible` using `System.Version.TryParse` + `range.Split('-', 2)` lower-upper string** — the EXACT "System.Version-based semver check, a hand-split lower-upper range string" that ARCHITECTURE.md:148 AND provisioning.md:207 name as **THE DELETED FORM**. Two same-named types for one concept; the prohibited naive form ships beside the canonical one. See VC-1.

**solver.md — 8.5** — 7-row `SolverKind` extension axis, `EncodingKind` projecting onto Compute `GeometryEncoding`, `KindContract`/`SolverManifest`/`SolverHosting` (contract-validate → negotiate → sandbox-load → registry-project). Plugin ops become first-class `CapabilityDescriptor` rows, dispatched/metered/brokered as built-ins. Clean contract law.
- DEFECT (:190-191): `Artifact(manifest)` constructs `new PluginArtifact(PluginId, string.Empty, PluginId, ReadOnlyMemory<byte>.Empty, None, ContractRange, ReadOnlyMemory<byte>.Empty)` — all-empty digest/signature/component/None-provenance → an artifact that isolation.md's `SupplyChainGate.Admit` would REJECT (AttestationMissing/SignatureInvalid). Incoherent placeholder feeding the load path.
- FORWARD-OBLIGATION (:197): `EncodingKind.Field`/`.Toolpath` are "working spelling until [Compute] table extension finalizes" — pending Compute `GeometryEncoding` case extensions. Waterfall to the Compute brief.

---

## CROSS-CUTTING

**Duplication.** ONE genuine duplicate mechanism: `SupplyChainGate`/`SupplyChainFault` in isolation.md (4680) AND provisioning.md (1320) — different types, different impls, same concept, contradictory ownership claims (VC-1). Otherwise dedupe is exemplary: `FencingToken`, `DeadlineClass`, `SchedulePort`, `ClockPolicy`, `DeliveryFanout` dedupe-cell, `OutboundHop`, `DrainSurface` are each single owners consumed as settled vocabulary across pages.

**Concern mixing.** provisioning.md fuses the self-update rail (UpdateRail/UpdateChannel/SupplyChainGate) with the fleet-roll conductor (RolloverDrain/FleetRoll/RollStrategy) — two concerns, splittable. The supply-chain gate belongs to neither exclusively (shared admission infra — see VC-8).

**Hardcoding-vs-generator.** Mostly clean (rosters ARE seed data on smart-enum/union axes; DeadlineClass/KeyedLane consts named). Two exceptions: laneguard.md:114-116 inline CPU thresholds (0.90/0.75) not named; isolation.md `SemverGate` hand-split range string (also a prohibition violation).

**Dead carriers / unwired.** outbox.md:54,57,59 unused `Instant at`; outbox.md watermark-advance discarded; profiles.md `PowerProbe.CopyPowerSourcesInfo` declared-uncalled; solver.md all-empty `PluginArtifact` stub; ports.md `WriteReceipt` `[JsonSerializable]` registered but livewire serializes via its own context.

**Unwired seams (the core value).** (1) presence: `ledger#PRESENCE` (Persistence) vs `collaboration#PRESENCE_AND_BLOB` (companion.md) vs `DrainSurface` (named driver, no logic) — three-way (VC-2). (2) `Query/transaction` stale (VC-3). (3) `UpdateReceipt` unregistered wire type (VC-4). (4) Compute assessment-lifecycle (Dispatchable/Transient) unconsumed by orchestration (VC-5). (5) delivery-honesty-per-sink missing on HopPolicy (VC-6).

**Prose-vs-fence splits.** orchestration.md:18 (FencingToken param omitted). Otherwise fences and prose track tightly.

**Unmined capability (catalog anchors).** `.api/api-jsonpatch.md` (`JsonPatchDocument.ApplyTo(JsonObject, logErrorAction)` — config.md leans on it, correctly); `.api/api-sigstore.md` + `.api/api-nuget-versioning.md` (the CANONICAL supply-chain owner isolation.md should compose instead of hand-rolling — VC-1); `.api/api-opcua.md`/`api-mqtt.md`/`api-modbus.md`/`api-serialport.md` (livewire, well-mined); `.api/api-service-discovery.md` (coordination, well-mined); `.api/api-velopack.md` (provisioning, well-mined). The BCL `System.Text.Json.Schema.JsonSchemaExporter` (ports.md) + `System.Security.Cryptography.PemEncoding` (config.md) are uncatalogued BCL-inbox members (flag for `.api/` transcription).

**Folder-architecture verdict.** The five-folder split (Runtime/Agent/Wire/Sandbox/Observability) is **logical and growth-conducive** — each page a closed owner-axis, no loose one-file-one-folder combos, no flat sprawl. Runtime (11, the spine) and Wire (6, the boundary) are the load-bearing cohesive folders. **Sandbox is the weak folder**: 3 pages where the supply-chain gate is duplicated across isolation/provisioning and provisioning carries two concerns; a `Sandbox/admission.md` owning the ONE `SupplyChainGate` (consumed by both plugin-load and release-update) would resolve VC-1 structurally and lift the folder (VC-8).

---

## VERDICT CANDIDATES (strongest, evidence-first)

**VC-1 — Collapse the duplicate SupplyChainGate to one owner; delete the prohibited SemverGate.** isolation.md:246-320 ships `SupplyChainGate`(record)+`SupplyChainFault`(4680)+`SemverGate.Compatible` (`System.Version`+`range.Split('-',2)`) — the exact deleted form ARCHITECTURE.md:148 + provisioning.md:207 forbid — while provisioning.md:198-261 owns the canonical `SupplyChainGate`(static, Sigstore+`NuGet.Versioning`, 1320). provisioning.md:207 asserts one shared Admit "never two verify paths"; disk contradicts. RULING: delete isolation's gate/fault/SemverGate; `SandboxRows.Load` (isolation) and `SolverHosting.Loaded` (solver) route through provisioning's `SupplyChainGate.Admit`; unify `PluginArtifact`/`VelopackAsset` admission on the Sigstore+NuGet.Versioning owner; one `SupplyChainFault` band. *Strongest — prohibition violation + duplicate mechanism + contradictory on-disk ownership.*

**VC-2 — Reconcile the presence seam three ways (both-sides + waterfall).** Persistence brief E6 / ledger.md:468: `ledger#PRESENCE ← AppHost DrainSurface`. AppHost companion.md:23: presence lands at `collaboration#PRESENCE_AND_BLOB` via `PeerRoster`/`Presence.Beat`. resources.md `DrainSurface` (the named driver): zero presence logic. RULING: the real AppHost driver is `PeerRoster` (companion.md); the Persistence owner is `ledger#PRESENCE`. Re-point companion.md:23 `collaboration#PRESENCE_AND_BLOB → ledger#PRESENCE`; WATERFALL-EDIT the Persistence brief/decision to name `AppHost PeerRoster` (not `DrainSurface`) as the driver. Declare both sides.

**VC-3 — Re-point the stale Persistence/Query/transaction 2PC-drain seam.** lifecycle.md:288,394 (+RESEARCH [TWO_PHASE_DRAIN]) reference `Rasm.Persistence/Query/transaction#TWO_PHASE_COMMIT` `TwoPhase.InDoubt(db)`. Persistence brief E5 confirms `APPHOST:71 Query/transaction` STALE; line 108 folds `transaction → graph#STORE_RAIL`. RULING: re-point lifecycle.md's Stores-band in-doubt-reconciliation seam AND ARCHITECTURE.md:71 from `Query/transaction#TWO_PHASE_COMMIT` to the `graph#STORE_RAIL` (`[V3]`) `pg_prepared_xacts` InDoubt owner. Both-sides.

**VC-4 — Close the wire-context ownership drift (phantom member + parallel owner).** (a) provisioning.md:148 dereferences `AppHostWireContext.Default.UpdateReceipt` but ports.md:122-139 has no `[JsonSerializable(typeof(UpdateReceipt))]` — phantom member (verified: 18 records registered, UpdateReceipt absent). (b) livewire.md:806-825 mints a standalone `LiveWireOptions.Json`+`LiveWireContext` parallel to `SuiteContracts.Wire` (ports.md WIRE_LAW "one merge per app root"), while ports.md:137 registers a `WriteReceipt` livewire never uses. RULING: register `UpdateReceipt` (+audit every `AppHostWireContext.Default.X` against the list); fold `LiveWireContext` into `SuiteContracts.Wire(contexts…)` and delete `LiveWireOptions.Json`, OR make the split explicit and drop the dead `WriteReceipt` row.

**VC-5 — Orchestration must consume the Compute/Element assessment-lifecycle, not run beside it.** SCOPE MANDATE / Compute [V1]: AppHost workflow/scheduling "consumes those lifecycle outcomes as settled vocabulary (Dispatchable sweep, Transient retry) and never re-mints a parallel job-state store." orchestration.md `StepStatus` (pending/running/committed/waiting/compensated/failed) + `StepKind.Activity`→`CommandDispatch.Run` never reference the Element-owned `AssessmentOutcome` lattice (Usable/Terminal/Dispatchable/Coherent/Transient/Failed, Compute brief:42). A workflow step dispatching a Compute assessment returning `Transient` has no retry-vs-terminal mapping. RULING: map the seam lifecycle onto StepStatus (Transient→retry-eligible re-drive, Dispatchable→sweep over the Persistence coordination store, Terminal/Failed→saga), consuming the vocabulary — no parallel job-state store beside ONE_FENCED_LEASE_STORE.

**VC-6 — Add delivery-honesty per sink to HopPolicy (waterfall to Persistence V3 egress).** SCOPE MANDATE item 2: Persistence V3 egress "names the wire-native gRPC/Arrow hop where AppHost's OutboundHop law already carries it — delivery-honesty stated per sink." outbound.md `HopPolicy` carries `HopIdempotency`/`HopTransport`/`HopRateLimit` but **no delivery-guarantee axis**; outbox.md states exactly-once-effective only at the relay level, not per sink (NATS ack-durable vs webhook at-least-once vs gRPC best-effort differ). RULING: add a `HopDelivery` honesty axis (best-effort | at-least-once | exactly-once-effective) to `HopPolicy` (or a per-sink honesty column on the outbox egress-consumer rows); named forward-obligation the Persistence V3 egress owner reads.

**VC-7 — Stop constructing the Persistence OpLogEntry inline; DECODE, don't build.** determinism.md:118-125 `EventLog.ToOpLog` instantiates the Persistence `Version/ledger#CHANGEFEED` `OpLogEntry` positionally (14 fields, hardcoded `"determinism.command"`/`"command"`/`SnapshotCodec.JsonStj`); :406 admits the spelling is unverified. AppHost DECODEs Persistence types per architecture, not CONSTRUCTs their interior. RULING: either AppHost hands a neutral `LogEntry` and Persistence owns the OpLogEntry mapping, or Persistence publishes a named factory AppHost calls — not a 14-field positional inline that shatters on a Persistence shape shift.

**VC-8 — Restructure Sandbox around a shared admission owner.** The Sandbox folder is the weakest (3 pages): the supply-chain gate is duplicated (VC-1), and provisioning fuses self-update + fleet-roll. RULING: introduce `Sandbox/admission.md` owning the ONE `SupplyChainGate` (Sigstore+NuGet.Versioning) consumed by both `isolation` (plugin load) and `provisioning` (release update); optionally split provisioning's fleet-roll conductor from the update rail. Resolves VC-1 structurally and makes the folder growth-conducive.
