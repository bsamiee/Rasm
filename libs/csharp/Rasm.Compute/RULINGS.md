# [RASM_COMPUTE_RULINGS]

`Rasm.Compute` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- (none)

## [02]-[SHAPE]

- `Model/inference#STAGE_EXECUTION` mirrors the photo-to-PBR request and result records but transcribes no stage, product, or model-card roster: those vocabularies grow by row at `Rasm.Materials`, and a mirrored roster turns every admitted model into a Compute edit. Only `LicenseClass` re-declares here, the one family this end dispatches on, because a grant enforced solely where the request is built trusts the caller's word.
- ONNX output tensor NAMES are the product roles. Stage requests carry no output roster, so the executor emits one product per `InferenceSession.OutputNames` entry under that name and the specifying end resolves it against its own vocabulary; binding model outputs to declared roles by ordinal couples two independently versioned rosters by position and silently mis-labels a re-exported model's planes.
- ONE tiled grid carries EVERY product a forward pass emits. `Model/inference#TILED_INFERENCE` `TilePlan` holds an ordered `TileProduct` roster and `InferTiled` scatters each result of one run into its own plane, so an estimator emitting base colour, normal, and roughness together costs one grid; a fold that opens a flow per output name re-infers the whole image once per plane the model already produced, and the multiplier is invisible because each pass looks correct alone.
- `Model/providers#EP_AXIS` owns every cross-boundary spelling of its own rows as a `WireKey` column on `ExecutionProvider` and `ModelPrecision`, so records crossing a boundary hold no translation table and a row landing later crosses by declaring one string. Unresolvable PROVIDERS degrade to the floor because the consuming record reports what ran; unresolvable PRECISIONS refuse because no wire column reports a substituted posture, and a default there runs one precision while the receipt names another.
- `FieldMask` has one mint and one apply — `WireServices.Mask` alone constructs and validates masks at the wire edge (`FieldRef` admission under the `FieldMask.IsValid` gate), while `FrameEdge.Patch` alone merges validated masks onto live messages under the transport frame law; a dedup sweep reading the twin `Union`/`Normalize` calls as duplication folds the apply leg into the mint and couples frame application to wire-edge admission — one mask-shape change then breaks both seams in one edit.

- Building-scale modal solve is STATIC CONDENSATION onto the inertia-bearing rows, then this lane's own dense generalized `Evd` — the `Solver/contract#SOLVE_CONTRACT` `condensed-evd` row. Reducing a generalized pencil to standard form demands a positive-definite inertia factor, and a lumped-mass frame holds none while its rotational rows sit in the pencil, so no eigensolver of any class reaches the problem until those rows leave.
- Condensation is EXACT here, never an approximation of a better route: a lumped-mass frame carries no rotary inertia, so its condensed rows hold exact zeros and the reduction drops no inertia term.
- Two refuted substrates close that decision. ARPACK-class packages do not exist on the feed. TorchSharp `lobpcg` is non-public AND an unimplemented body — an accessor bound to its exact decompiled signature resolves, the body runs, it throws — so its presence in the assembly grants nothing.
- Hand-rolled Lanczos beside the reduction stays the deleted form.
- Kernel `Numerics/matrix` generalized-eigen densifies BOTH operands at full order and factors the WHOLE inertia operator, so it neither scales at building order nor admits a singular lumped mass; it stays the small-model owner it was built as.
- Reduced pencils ride Compute's own dense funnel (`Admission.Definite` · `DenseOps.Decompose` · `SpectralOps.Decompose`) under the two-funnel adjudication, never a round-trip into kernel sparse storage: the reduced blocks are already `Matrix<double>`, so re-entry densifies, re-sparsifies, and re-densifies one small operator to reach the same congruence.
- Lumped inertia is REAL mass wherever the material form is elastic: `MaterialField` carries density beside the two elastic constants because the seam `Mechanical` case supplies it, and a geometry-only vector reports metres as kilogrammes to every dynamic and modal arm reading it.
- Mass-singular free rows refuse by NAME, never by rescue: the mass-normalizing dense modal route and the inertia-dividing explicit march each name the row and the route that does serve it, because scaling by the reciprocal of a floor emits a spurious mode and freezing at zero publishes stillness as motion.
- Native execution substrates are admitted by EXECUTION, never by inventory: `Tensor/blas#DENSE_ALGEBRA` `AtenFloor.Resident` runs the bring-up once and folds its refusal class onto the determinism tag, because a vendored payload sits wholly present and still fails to load on a dependency it resolves through an absolute path outside its own package. RID predicates and file-presence gates in that slot publish an accelerated route that executes nothing while the receipt names a substrate that never ran.

- Eigenvalue weighting has ONE vocabulary — the kernel `Numerics/spectral#FILTER_ALGEBRA` `SpectralFilter`, whose `Weight` is public precisely so a second package composes it. `Tensor/blas#SPECTRAL_LAW` composes the owner and enumerates no case, so a new transfer form is one kernel case with zero Compute edit; a lane-local weight `[SmartEnum]` re-mints the algebra minus parameterized time, the wave and biharmonic cases, and `Compose`.
- Held sparse GEMV carries direction and accumulation as ONE `GemvForm` payload, never a `transpose` flag or a direction-named sibling entrypoint: `y = αA'x + βy` at `(1, 0)` IS `y = A'x`, so the four CSparse overloads are one closed family and every adjoint leg binds `TransposeMultiply` instead of allocating a second CSC through `A.Transpose()`.
- Every Compute geometry read over the seam `ImportedGeometry` resolves BY DESCRIPTOR through the kernel `EncodedGeometry` arena — one `Lane` unpack per channel, `EncodingChannel.<row>.Arity` for every stride, descriptor presence as the absence test. Named-column reads, per-lane branches, and literal component strides re-mint the per-column carrier the `STRATA_TWIN` collapse deleted, silently excluding every lane the branch omits — which is how a tile content key stops moving as its roster grows.
- Every `Model/providers#EP_AXIS` row carries a `WarmForm` and the floor row is not exempt — the EP-context blob and the managed optimized graph are two mechanisms under ONE `ContextKey` derivation, the form's suffix separating their artifacts and `RuntimeKeyed` adding the runtime version only where the artifact embeds no compat info; a second artifact key, a second filename scheme, or a managed path written and never read back each re-open the cold-optimization cost the axis exists to remove.
- Cold session construction is deadline-bound through `SetLoadCancellationFlag` registered on `PlannedLoad`, and `ModelSessions.Faulted` is the ONE native-fault classifier the run side composes — a run-side copy lets one expiring deadline report two faults depending on which half it landed on.
- Chained stage input re-enters through `StagePorts.Read` and never bypasses it — the producer's transfer and format are host-owned, so a retained-plane or device-resident bypass forks the numeric path of one plan; a device-to-device copy homes at `Tensor/residency#ORT_BRIDGE` over a resident pair.
- Parity custody is one key across two tiers — the process memo and the `ParityPort` artifact row derive from `(card, provider ResultKey, host fingerprint)`, and a verdict measured on other silicon never gates an acceleration this machine did not grade.
- Dense factorization runs TWO funnels by adjudication — the kernel `Numerics/matrix#SOLVE_KERNEL` `MatrixKernel` stays the kernel solvers' one MathNet/CSparse access path and stays `internal` to `Rasm`, while `Tensor/blas#DENSE_ROUTE` funnels Compute's dense leg over the `DenseSubstrate` axis (managed `Matrix<double>` · native ATen) the kernel has no equivalent for; a public kernel re-entry minted for Compute, or a Compute re-mint of the kernel's factorization routes, each collapse a funnel the other side's substrate cannot serve.

## [03]-[COLLAPSE]

- Tiling plans are CONSTRUCTED once and compared never. `Model/inference#STAGE_EXECUTION` `StageRequest.Plan` folds the wire's extent, bucket, overlap, pad, and scale columns into the `TilePlan` whose validator is the sole spelling of the fixed-bucket law, and that value seats the bound flow and drives the fold.
- Request-side predicates restating a plan law, a session-supplied plan beside the built one, and a compare catching the two drifting are ONE defect in three parts: the compare reports a disagreement it never explains, and each rule edit lands thrice or silently once.
- `TilePlan` owns the taper-weight accumulation and the layout rows own only the product kernels. Weight is per-texel geometry every product shares, so a scatter kernel carrying it accumulates one taper mass per plane per window and forces every new layout row to re-derive that geometry identically.

- Integration has ONE floor and `Rasm.Compute` owns none of it — the kernel `Rasm/Numerics/integrate` quadrature owner (`QuadratureRoute`/`IntegrationDomain`/`ReferenceElement`/`QuadratureRule`/`QuadratureControl`/`QuadratureEvidence`/`Quadrature.Integrate`) and its step algebra (`IntegratorKind`/`ButcherTableau`/`IntegrationModule`/`StepControl`/`FieldIntegrator`/`IntegrationStep`/`DenseOutputSpan`) compose whole at `Tensor/quadrature` and `Solver/discretization`.
- Package-local Butcher tableaux, rooted-tree order walks, additive-module interfaces, Gauss tables, Smolyak folds, and accuracy-route rows are the deleted form; a second element-quadrature vocabulary inside one package is the defect that collapse closed.
- Compute's integration surface sits ABOVE that floor and holds nothing else: the batched accumulate entry, the solver-domain carrier with its per-component scaled error norm, the adaptive DRIVER (reject loop, run terminal partition, step-underflow floor, total-step budget, dense-station harvest) the kernel leaves to its consumer, and the `ComputeReceipt`/`ComputeFault` projection.
- Kernel `Norm` reads the delta alone, so scale rides a carrier re-minted at each accepted state and the adaptive tolerance stays the dimensionless 1.0 target — a module minted once from the initial state silently reverts to absolute control.
- Each `ElementClass` declares a reference domain and an integration ORDER, never a rule constant: the kernel `ReferenceElement` row elects the smallest owned rule at or above that order and clamps to its highest, so a 2-D element can never index a 3-D rule and a full-versus-reduced posture is one integer.
- Cache tags reach `HybridCache` only through `CacheLane.Tag`, and drift invalidation is MODEL-scoped through the lane's own `Invalidate` — a raw checksum stamped at a write is a tag no lane minted, which is exactly what leaves a tag-scoped purge unable to find it.

## [04]-[STRUCTURE]

- Kernel spellings are frozen and the Compute end moves: `Solver/optimizer`'s line-search axis is `LineSearch`, not `StepControl`, because the kernel `Rasm/Numerics/integrate` already owns `StepControl` as the adaptive step-size policy and both are reachable in one file composing the integrator. Two genuinely distinct concepts never share a name across the strata boundary, and the lower stratum keeps the name.

## [05]-[PROCESS]

- (none)
