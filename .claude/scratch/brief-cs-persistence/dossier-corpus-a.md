# [DOSSIER-CORPUS-A] — Rasm.Persistence .planning (Element, Ingest, Query) + governance

Lane: corpus-a. Deep-read FULLY, first-hand, every page: `Element/{identity(653),codec(463),graph(339)}`, `Ingest/tabular(308)`, `Query/{lane(521),columnar(493),cypher(450),topology(324),cache(252)}` = 9 pages; `ARCHITECTURE.md(118)`, `README.md(192)`, `TASKLOG.md`, `IDEAS.md`. Cross-refs read for FIT: `libs/.planning/architecture.md` strata, upstream `RASM-CS-GEOMETRY-BRIEF.md`, `RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]`/`[LEG-FABRICATION]`, `docs/stacks/csharp` doctrine, full-corpus grep of fault codes + phantom-page anchors.

Alphabetical subfolder split confirmed: corpus-a = {Element, Ingest, Query} vs corpus-b = {Store, Version}. NO root-level `.planning` page exists (only subfolders). README `[01]-[ROUTER]` lists exactly the 18 pages on disk and `ARCHITECTURE.md` codemap matches — BUT `TASKLOG.md`/`IDEAS.md` are NOT reconciled to that tree (see [D0], the finding the prior dossier missed entirely).

Stance: hostile, evidence-first. The fence corpus is genuinely world-class IN ISOLATION (dense ADT, seam-composed hashing, honest fault rails, injection-safe SQL), so nearly every finding is STRUCTURAL — cross-page band allocation, card-pool/tree drift, folder architecture, page-split pressure, seam-ledger drift, unowned admitted packages — not per-fence polish. This revision CORRECTS the prior dossier on two points ([D0] governance drift was absent; [D3] split direction was backwards relative to a settled decision) and adds mechanical evidence for [D1].

---

## [A] SCOPE-MANDATE CENSUS DISPOSITIONS (the accumulated census this brief must dispose)

**(1) Count-prefix wire law (DECISION `[AMENDMENTS]#1`) — CLEAN, no-op by correct composition.** The amendment appends `w.Ordinal(bag.Values.Count)` to the PropertySet/QuantitySet arms of `Node.ToCanonicalBytes`; it is SEAM-owned (`Rasm.Element/Projection/address#CANONICAL_WRITER`). `codec.md:151-153,156` composes `ContentAddress.Of(node.ToCanonicalBytes(...))` and `ContentAddress.OfGraph(graph)` VERBATIM and mints ZERO local node/graph hasher — so every PropertySet/QuantitySet content key forks automatically when the seam writer changes. Corpus-a pins NO PropertySet/QuantitySet golden vectors: `codec.md:158` "the literal digest values stamp on the host-validation pass, never an un-run asserted value"; the length-framed parity preimages (`lane.md:214-230` `ElementSetAlgebra.Canonical`, `cache.md:83-106` `ModelResultKey.Content`) freeze NodeId-KEY framing, not property-bag bytes. `codec.md:157` Growth already anticipates it ("a new canonical-byte rule is one clause on the seam"). → NO store-side edit required; the amendment IS the migration (planning-phase, no persisted graph). Record the disposition.

**(2) Fabrication 25xx→27xx re-band + persisted-boundary check (`[LEG-FABRICATION]` gate) — STORE-SAFE.** Persistence NEVER references `Rasm.Fabrication` (`ARCHITECTURE.md:5,95,116` closed NEVER-list). The only cross-band integer any corpus-a fence carries is `graph.md:158` `GraphFault.DeltaRejected(string Detail, int InvariantCode)`, populated at `graph.md:328` from `error.Code` (the seam `ElementFault` code, a 2500-band value). This is a TRANSIENT fault on the `#STORE_RAIL` bracket rail (`graph.md:134,322-328`), NEVER a persisted event body — the event bodies are `GraphEvent.{GraphCreated,GraphRevised,GraphRetired}` carrying the seam `GraphDelta`, no fault code (`graph.md:63-80`). The 25xx→27xx reband evacuates FABRICATION out of the 2500 band; genuine `ElementFault` 2500 codes (the InvariantCode carries) are unaffected. → the reband crosses no persisted/wire boundary in corpus-a; record the disposition (LEG-FABRICATION is not demoted by any corpus-a crossing).

**(3) PERSISTENCE_GRAPH_REBUILD over ElementGraph + GraphDelta — LANDED in `graph.md` (verdict 9).** A complete realized rebuild: stream-per-`ModelId` (`graph.md:88` `StreamIdentity.AsGuid`, never per-`NodeId`), `GraphDelta` event bodies (`GraphEvent` union `:63-80`), inline `SingleStreamProjection` read-your-writes (`:101,190-205`), `AggregateStreamAsync` AS-OF (`:271-276`), co-transactional identity commit in one `IDocumentSession` (`:286-294`), ONE `GraphDelta.ReplayOnto` materializer shared by projection Create/Apply AND AS-OF (`:197-201,220`), total generated `Switch` dispatch (`:259-266`), provider-fault conversion at exactly one bracket boundary (`:322-328`). No naivety-of-approach defect. Residual: the `GraphFault` simple-name collision with cypher.md (see [D2]) — a rename, not a rebuild.

**(4) Materials-leg residual — derived-Type re-key: MECHANISM present, SCENARIO binding absent.** DECISION line 53: a next-campaign Type-seed change (exclude Classification from `NodeId.RootedType` like Representations) re-keys every DERIVED Type NodeId, rippling to every `Bake`-inheriting occurrence. `identity.md` carries the generic migration MECHANISM: `IdentityPolicy` expand-wave/derivation-flip/contract-wave (`:189` "never an `AlterColumn` — it is an expand-wave second key … a derivation flip, and a contract-wave drop, the only identity migration preserving foreign references and AS-OF cursor validity at once") PLUS `namespace-key` RFC-4122-v5 stable-derived-id minting (`:234,248,258-269`). A derived-Type re-key IS an instance of this (a derivation flip over the amended seed + a contract-wave drop). But the SPECIFIC scenario — "when Materials re-seeds `RootedType`, Persistence runs the expand/contract wave over derived Type NodeIds AND re-points every occurrence→Type reference" — is NOT named as a migration case. → the mechanism is sufficient; record the derived-Type-seed re-key as a NAMED consumer contract the next Materials campaign triggers, not a fence gap.

**(5) Streaming/incremental `ContentAddress.OfGraph` — consumer contract UNRECORDED.** `codec.md:151-158` correctly composes the seam whole-graph `OfGraph` fold and re-homes no hasher, but records only the WHOLE-graph shape. `topology.md:76` `TopologyView.Of` recomputes `ContentAddress.OfGraph(graph).Value` per snapshot as the memo identity; every `GraphDelta` mints a fresh `GraphProjection` (`graph.md:196-201`) → a fresh `TopologyView` → a full O(n) OfGraph recompute. The INCREMENTAL variant (kernel maintains a running hash updated per delta) is the natural large-graph consumer contract. → record in `codec.md#CONTENT_ADDRESS` that incremental/streaming OfGraph is the KERNEL-OWNER's to provide and Persistence composes it (never re-homes) — the per-snapshot full recompute is a documented interim, not the permanent shape.

---

## [B] UPSTREAM (GEOMETRY BRIEF) CONSUMER-SEAM CENSUS

Persistence depends UP on the `Rasm` kernel + `Rasm.Element` seam; the geometry campaign lands capabilities Persistence consumes at the edge.

- **[V2] parametric content-identity — ALIGNED; named opportunity.** Geometry `[V2]`: the parametric tier owns a canonical-bytes projection (degree·knots·weights·control-net), the kernel emits the digest, Persistence mints the key. `codec.md:152-153,164` ALREADY carries the precomputed-digest wrap path (`ContentAddress.Of(UInt128)` "wraps a precomputed snapshot/chunk digest"), so a parametric-form `UInt128` digest wraps through it identically; `cache.md:29-34` `ArtifactKind` is the natural home for a `parametric-form` artifact row. → NO current defect (the wrap path exists); record the parametric-form digest as a consumer of `codec.md`'s `Of(UInt128)` wrap when the geometry Parametric tier lands.

- **[V8]/[E12] reconciliation→Persistence seam anchor DRIFT — CONFIRMED first-hand (see [D6]).** Geometry brief flags `ARCHITECTURE.md:55` as one of three drifting reconciliation→Persistence anchors, re-targeted by the geometry namespace rename (phantom `Topology`→`Rasm.Geometry.Naming`/`.Spatial`). First-hand: `topology.md` contains ZERO `Geometry`/`Spatial`/`GeometryHash` references; its memo key (`:3,76,115`) is the `Rasm.Element` seam `ContentAddress.OfGraph`. So the declared `Query/topology ← Rasm/Geometry/Spatial GeometryHash` seam is UNWIRED in the fence.

- **WATERFALL-RIPPLE disposition: NO surgical edit to `RASM-CS-GEOMETRY-BRIEF.md` owed from corpus-a.** The brief already homes Persistence as consumer in `[V2]` (parametric key-mint) and `[V8]` (reconciliation seam), and Persistence demands no capability the brief lacks. (Read-only survey; ripple authority is the author agent's — recorded here for that agent, none owed.)

---

## [C] PER-PAGE VERDICTS (first-hand)

### Element/identity.md — VERDICT 8/10 (world-class content, 4-concern overload; split direction CORRECTED)
Sections: `[02]ELEMENT_IDENTITY` (relational `ElementIdentity`+`NodeCell` co-transactional tier, H3/pgvector/RLS join columns), `[03]IDENTITY_POLICY` (`StoreKey`/`IdentityPolicy`/`Collision` key axis + migration mechanism), `[04]AUTHORITY` (~250 LOC, `:273-523`), `[05]SCHEMA_VERDICT` (`Placement`/`SchemaVerdict`/`IdentityFault` 834x boot fold). 653 LOC — the largest corpus-a page.
- **SPLIT PRESSURE (top-tier, [D3]).** `[04]AUTHORITY` fuses THREE concerns: (a) object authorization — `Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit` (`:287-341,464-484`), a pure authz set-algebra with ZERO KmsProvider; (b) asymmetric signing — `KmsProvider`/`SigningAlgorithm`/`OpDigest`/`SigningKeyring`/`Attest`/`Verify`; (c) symmetric DEK envelope — `EnvelopeAad`/`WrappedKey`/`EnvelopeKeyring`/`KeyState`/`Wrap`/`Unwrap`/`Rewrap` (`:416-522`). The `AuthDecision` union (`:442-461`) carries 14 cases spanning all three. CRUCIAL NUANCE the prior dossier missed: co-locating signing+envelope on ONE `KmsProvider` axis is SETTLED and DEFENSIBLE — TASKLOG `[ENCRYPTION_PAGE_AUTHOR]-[COMPLETE]` explicitly folded `Store/encryption` INTO this tier and forbade a separate envelope page, and `identity.md:279-280` codifies "a separate `Store/encryption` page … is the deleted form." The WEAK seam is authz-vs-crypto (`Admit` is a pure fold, no KmsProvider), NOT signing-vs-envelope.
- Charter as it SHOULD be: `identity.md` = relational tier + `IdentityPolicy` migration axis + `KmsProvider` crypto custody (signing + envelope, one axis) + `SchemaVerdict`; extract `Element/authority.md` = the object-ACL authz vocabulary (`Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit`+`Effective`+`LapsedFor`). The AppHost seam `ARCHITECTURE.md:53` splits cleanly the same way (Tenant-RLS identity vs ObjectAcl fine-grant).
- Sound surfaces to preserve: `H3Cell` `[ValueObject<long>]` bit-exact `h3-pg` convention (`:49-53`); the two-grain cell discipline (per-model `ElementIdentity.Cell` vs per-element `NodeCell.Cell`, `:69-73,156-170`); `SchemaGate.Admit` total boot fold over Marten-assert + EF-migration state (`:615-643`); `Placement` realizing the route-prescribed shape with no concrete route type (`:537-551`).

### Element/codec.md — VERDICT 9/10 (crown jewel; mandate-1 & -5 compliant)
Owner: `SnapshotCodec`+`ContentAddress`+`CompressionPolicy`/`HashPolicy`+`SnapshotHeader`/`RejectTier`/`CodecFault`(831x-833x)+`ContentChunking`. Coherent single codec/framing domain across 5 sections. Composes the seam `ContentAddress.Of`/`OfGraph`/`Verify` and mints NO local node/graph hasher (`:151,157,163` — exactly mandate-1/5's "record the consumer contract, never re-home"). Exemplary: the 8-rank ordered `RejectTier` ladder rejecting before any decoder binds (`:350-361`), single-pass atomic seal with zeroed placeholder + `Flush(flushToDisk)` + `File.Move` (`:374-391`), CBOR `Canonical`/`Strict` trust boundary (`:113-132`).
- **DEAD/ANTICIPATORY CARRIER (enrichment over prior dossier).** `HashPolicy` (`:215-227`) declares 5 rows + a `Compute` delegate + `ByDomainId` resolver, but the ACTUAL hot paths bypass all of it: `SnapshotHeader.Seal` (`:319,321`) calls `XxHash128.HashToUInt128`/`Crc32.HashToUInt32` STATICALLY, and `Snapshots.Verify` (`:354,357`) hardcodes `Crc32`/`XxHash128` directly — only `HashPolicy.Identity.DomainId` (a byte constant, `:318,355`) is read. So `Content`(XxHash3, used at `:404,438`) and `Identity`(XxHash128) have a consumer, but `Frame`(Crc32), `Wide`(XxHash64), `FrameWide`(Crc64) plus the `Compute`/`ByDomainId` dispatch surface are anticipatory carriers, not live dispatch, in corpus-a. Defensible as forward-proofing (the header `HashDomain` byte self-describes for a future wider address, `:175`) — flag as "dead-until-load-bearing," not a hard defect.

### Element/graph.md — VERDICT 9/10 (mandate-3 rebuild, landed)
See [A3]. One `GraphDelta.ReplayOnto` materializer, total `Switch`, co-transactional identity, one-boundary fault conversion — all present and correct.
- **Cross-cutting mark:** `GraphFault` (`:155`, ns `Rasm.Persistence`, 830x) collides in SIMPLE NAME with cypher.md's `GraphFault` (ns `Rasm.Persistence.Query`, 836x) → [D2].
- Seam consumption is clean: `TimeCut` from `Version/timetravel#TIME_TRAVEL` (`:229-232`), `GraphDelta`/`Header`/`ElementGraph.Of` from `Rasm.Element` — correct cross-page/seam composition, not defects.

### Ingest/tabular.md — VERDICT 8/10 (excellent owner, wrong folder)
Owner: ONE `TabularSource` over MiniExcel; `TabularSpec`+`Origin` `[Union]` collapses path/stream + typed/dynamic/reader/probe/egress modalities onto one value (`:64-80,155-255`); per-column `RedactionPlan` resolving a `Redactor` per `DataClassificationSet` (`:145-152`); BOTH read+write legs fold `TabularFault` through one `Lift` funnel (`:126-131,226-229`); the typed read deliberately avoids the `Query<T>` POCO binder to route cells through the STJ wire factory (`:265-266`). Genuinely strong single-concern design.
- **FOLDER-ARCHITECTURE DEFECT (see [D5]).** `Ingest/` is a ONE-FILE folder (tabular.md only). `Sep` is named "the delimited sibling" (`:3,5,18,20,298`) with NO owning page — the delimited codec is HOMELESS. README `[INTERCHANGE_CODECS]`/`[WIRE_SERIALIZATION]` admit `MiniExcel`,`Sep`,`Chr.Avro*`,`System.Formats.Cbor`,`MPXJ.Net` — a real multi-codec ingress roster with no folder structure.
- Fault-band collision: `TabularFault` 8370-8373 (`:104-108`) collides with `TopologyFault` 8370-8371 → [D1]. Notably, tabular's band prose (`:82-89`) names ONLY the 2500/2600 federation base, not its 83xx neighbors — evidence for [D1].

### Query/lane.md — VERDICT 7/10 (richest page, 4-concern overload, sole band violator)
Owner: `[02]READ_ROUTING`+`[03]ELEMENT_SET_ALGEBRA`+`[04]FUSION_AND_CACHE`+`[05]VECTOR_CODEBOOK`. 521 LOC / 4 concerns. Each is superb in isolation: `ReadRouter` routing law with every `QueryShape` bit load-bearing (`:75-80`); `StalenessWatermark` measuring the honest SEQUENCE GAP not the recording-clock confound (`:40-48,87-115` — a genuinely sophisticated correctness surface); length-framed collision-free `ElementSet` receipt (`:198-230`); weighted n-ary RRF fusion (`:339-347`); PQ k-means/ADC codebook (`:430-499`).
- **BARE-ERROR.NEW + BAND COLLISION (top-tier, [D1]).** `VectorCodebook.Train` rails `Error.New(8360)`/`(8361)`/`(8362)`/`(8363)` (`:432,435,437,441`) — BARE `Error.New`, the ONLY corpus-a owner with no typed `[Union]:Expected` band, violating every sibling's pattern AND the pages' own "a bare `Error.New` is the deleted form" law (e.g. `codec.md:276`, `topology.md:164`) — AND colliding EXACTLY with cypher `GraphFault` 8360-8363.
- **SPLIT PRESSURE ([D4]).** `FUSION_AND_CACHE` (`:267-377`) + `VECTOR_CODEBOOK` (`:379-521`, ~142 LOC PQ k-means/ADC/VectorIndex) are TIGHTLY coupled (the fusion vector branch reads the codebook's ADC-ranked rows, `:388`) — one coherent "retrieval/ANN" subsystem fused into the read-ROUTING page. Extract `Query/vector.md` (or `retrieval.md`) = fusion + codebook + the missing typed `CodebookFault` band; lane.md drops to routing + set-algebra. The codebook's Persistence LOCATION is justified/settled (avoids a `Persistence→Compute` cycle, `:388`); only its PAGE placement is the defect.

### Query/columnar.md — VERDICT 8/10 (deep DuckDB owner)
Owner: `COLUMNAR_LANE` (refcounted `Duplicate()` anchor + `INSTALL`/`LOAD` extension-profile bootstrap + ADBC Arrow bridge + `DuckDBErrorType`-discriminated `ColumnarFault` 835x) + `ARTIFACT_EGRESS` (one `COPY (SELECT) TO` rail) + `FLAT_TABLE_EGRESS` (co-transactional BimOpenSchema `FlatTableProjection`). Extension roster as SQL policy (`:69`), `DuckDBParameter`-bound query/secret values.
- **UNEVEN PARAMETERIZATION (enrichment).** `ArtifactClass.Egress` (`:356-361`) raw-string-interpolates `destination`/`stamp` into COPY SQL (`TO '{destination}'`, `KV_METADATA {{ stamp: '{stamp}' }}`); `Mount` (`:256`) interpolates `store`/`alias`; `Generation` (`:391`) interpolates `root` — while `Query` (`:225-227`) and `Secret` (`:271`) bind VALUES through `DuckDBParameter`. Identifiers/paths cannot always be bound params in DuckDB, so this is partly unavoidable, but there is NO stated trust gate on the interpolated args — a `'` in `destination`/`stamp` breaks or injects. Flag: declare these trusted-internal-only with a validation gate, or bind where DuckDB allows.
- **PACKAGE-QUALITY note.** `:411` documents `Ara3D.BimOpenSchema[.IO]` ships DEBUG-built IL (JIT optimizations disabled) — a hot ingest/codec path pays the deopt penalty. A package-roster quality defect worth flagging for the roster review, though it is the BimOpenSchema reference impl.
- Minor hardcoding: all 4 `ColumnarProfile` rows repeat identical `"80%"`/`"90%"` memory/spill literals (`:80-83`) — not per-profile policy, a package-wide posture constant repeated 4×.
- Fault-band: `ColumnarFault` 8350-8356 collides with corpus-b `provisioning.md` (unverified here) and its prose (`:128`) names 836x/837x neighbors but not lane's 836x squat → [D1].

### Query/cypher.md — VERDICT 8/10 (rich optional graph lane)
Owner: `GRAPH_SESSION` (self-hosted `CypherEnablement` gate + per-physical-connection `LOAD 'age'` init hook, `:96-111`) + `GRAPH_QUERY` (ONE `GraphQuery` union over ALL AGE openCypher + pgrouting verbs under one total `Switch`, `:211-262`). Genuinely deep roster (Dijkstra/A*/bidi/KSP/DrivingDistance/TSP/maxFlow/components). Exemplary correctness: server-side injection-safe `format('%L', %L::bigint[])` TSP matrix (`:381`, with a precise account of WHY a bare `%s` injects); NODE/CELL space discipline (a cut-EDGE id on `Severed` is NEVER `CellOf`'d, `:240,289,422`); `H3Cell` node space shared bit-exact with in-process `pocketken.H3`.
- **NAME + BAND COLLISION (top-tier, [D2]/[D1]).** `GraphFault` (`:60`, ns `Rasm.Persistence.Query`, 836x) collides in SIMPLE NAME with graph.md's `GraphFault` (830x) AND in BAND with lane's codebook 8360-8363. Rename → `CypherFault`/`GraphLaneFault` and re-band under the [D1] registry.
- Minor: `Reach` (`:255`) concatenates the `*min..max` int range into the Cypher body — the one string-concatenated body, injection-safe (ints), acceptable documented exception.

### Query/topology.md — VERDICT 9/10 (exemplary)
Owner: `GRAPH_TOPOLOGY` (kind-filtered `TypedEdge` view off the seam-FROZEN incidence, never re-derived, `:82-106`) + `TRAVERSAL` (QuikGraph `AlgorithmExtensions` facade, `:195-308`). Honest `Fin`/`TopologyFault` 837x rail with `Rooted`/`Paired` absent-root guards (`:213-224`, never a silent empty); `Spatial`(Contain∪Aggregate) vs narrow `Containment` filter correctness (`:52-53,321`); one shared SCC probe for Components/Cycles/Order (`:274-282`); no re-homed hasher (memo on seam `ContentAddress.OfGraph`, `:76`).
- Marks: shares the 837x collision with tabular ([D1], an allocation defect not this page's logic — and `:166` prose names 835x/836x neighbors but NOT tabular's 837x, proving the collision is accidental); it is the fence-half of the `ARCHITECTURE.md:55` seam drift ([D6]).

### Query/cache.md — VERDICT 8/10 (strong reuse substrate + L2)
Owner: `ARTIFACT_BLOB_INDEX`+`MODEL_RESULT_INDEX` (the single recency-horizon owner, gate folded INTO `Lookup`, `:120-125`)+`BENCHMARK_INDEX`+`L2_CONTRIBUTION` (`IBufferDistributedCache` buffer contract sparing the array copy, one MessagePack `CacheCodecFactory`, `TenantId`-scoped `Scoped` key). Content-keyed by the one seam `XxHash128`; `ModelResultKey.Content` length-framed preimage matching the `ElementSet` Canonical law (`:83-106`). Legitimately carries NO fault band (Lookup/Publish/Claim are total; "miss" is `Option.None`).
- Minor perf: `CacheL2Store` sync bridges block sync-over-async (`:192,202,210,216` `.GetAwaiter().GetResult()`) and `SetAsync`/`RemoveAsync` open a Marten session per op (`:205-207,217`). Standard `IDistributedCache`-contract shape and L2 is a rare L1-miss fallback, so bounded — but a session-per-op note on hot paths, no batching discussed.
- No structural defect; strata one-directionality (host fingerprint as string, `:78,111,147`) is clean.

---

## [D] CROSS-CUTTING FINDINGS & VERDICT CANDIDATES

### [D0] ★★★ GOVERNANCE DRIFT — TASKLOG/IDEAS reference ~9 PHANTOM pages/owners the realized 18-page tree does not contain (the prior dossier missed this entirely).
The `ARCHITECTURE.md` codemap and README `[01]-[ROUTER]` were rebuilt to the current 18-page tree, but `TASKLOG.md`/`IDEAS.md` were NOT reconciled. Full-corpus grep (`FederatedEntity`, `SourceKind`, `TxnScope`, `BulkPipeline`, `EgressSink`, `QualityRule`, `SnapshotProtocol`, `AsOfKey`, `SchemaDdl`) → ALL `<none>`. The stale cards, several `-[COMPLETE]` with `.cs` nodes claimed "landed":
- `Query/federation#ENTITY_GRAPH` / `Query/Federation.cs` / `FederatedEntity`/`SourceKind.SignedArtifact` — claimed "realized" by THREE cards (TASKLOG `[ARTIFACT_CONTENT_KEY_FEDERATION]`, IDEAS `[REUSE_WIRE]`, IDEAS `[SUBSTRAIT_FEDERATION_SEAM]`). Absent. (Query = corpus-a; this is squarely a corpus-a finding.)
- `Query/transaction.md` / `Query/Transaction.cs` / `TxnScope`/`IsolationPolicy`/`StoreFault.Concurrency 7001` — TASKLOG `[TRANSACTION_PAGE_AUTHOR]-[COMPLETE]`. Absent.
- `Query/pipeline.md` / `Query/Pipeline.cs` / `BulkPipeline`/`PipelineStage` — TASKLOG `[PIPELINE_PAGE_AUTHOR]-[COMPLETE]`. Absent.
- `Sync/egress.md` / `Sync/Egress.cs` / `EgressSink`, `Sync/annotation.md`, `Sync/schedule.md` — the entire `Sync/` folder is gone; TASKLOG `[EGRESS_PAGE_AUTHOR]`/`[ANNOTATION_RELOCATE_TO_BIM]`/`[SCHEDULE_RELOCATE_TO_BIM]`-[COMPLETE]. Absent.
- `Store/quality.md` / `Store/Quality.cs` / `QualityRule` — TASKLOG `[QUALITY_PAGE_AUTHOR]-[COMPLETE]`. Absent (Store = corpus-b, but the card is package-wide governance).
- `Version/snapshots#SNAPSHOT_PROTOCOL` / `AsOfKey` — TASKLOG `[ICECHUNK_ASOF_CONTENT_KEY]-[COMPLETE]`. Absent (folded into timetravel per ARCHITECTURE desc, but the anchor is dangling).
- `Schema/ddl#EXTENSION_DDL` / `SchemaDdl.Sql` — TASKLOG `[SCHEMADDL_SQL_COLLAPSE]-[COMPLETE]` claims `SchemaDdl.Sql` exists; TASKLOG `[STORE_SERVER_SPLIT]-[COMPLETE]` states "there is no `SchemaDdl.Sql` owner." TWO COMPLETE cards DIRECTLY CONTRADICT.
- IDEAS `[FABRICATION_PROGRAM_DURABLE_ROWS]-[QUEUED]` anchors `Schema/ddl#IDENTITY` + `ARCHITECTURE.md Schema ← Rasm.Fabrication/…` seams — the `Schema/` folder AND those ARCHITECTURE seams do not exist in the current codemap.
RULING: the card pool is decoupled from the realized tree — the highest-severity governance defect in the package. The brief must schedule a full `align-cards`/`hygiene-sweep` reconcile: every phantom anchor either re-points to the surviving fold owner (transaction→`graph.md#STORE_RAIL`? pipeline→`columnar.md`? federation→a page that must be authored) or flags a LOST capability that needs re-homing (see [D3']). A `-[COMPLETE]` card claiming a non-existent `.cs` node is a verify-remove failure by construction.

### [D1] ★★★ No package-wide fault-band registry; the 83xx DECADE ALLOCATION collides (mechanical evidence).
Persistence allocates per-owner 83xx sub-bands ad-hoc "next free decade" with NO central registry (unlike the component-paradigm DECISION `[03]` federation `FaultBand`: 2300/2400/2500/2600/2700). Full corpus-a code map:
| decade | owner | codes | file |
|---|---|---|---|
| 830x | `GraphFault` (graph) | 8300-8302 | graph.md:163-165 |
| 831x | `CodecFault.NoMutualCodec` | 8310 | codec.md:288 |
| 832x | `CodecFault.SnapshotRejected` (8320+Rank, 8 tiers) | 8321-8328 | codec.md:289 |
| 833x | `CodecFault.ReassemblyDrift` | 8330 | codec.md:290 |
| 834x | `IdentityFault` | 8341-8344 | identity.md:589-592 |
| 835x | `ColumnarFault` | 8350-8356 | columnar.md:144-150 |
| 836x | `GraphFault` (cypher) | 8360-8363 | cypher.md:68-71 |
| 836x | lane `VectorCodebook` (BARE `Error.New`) | 8360-8363 | lane.md:432-441 → **COLLIDES cypher** |
| 837x | `TopologyFault` | 8370-8371 | topology.md:180-181 |
| 837x | `TabularFault` | 8370-8373 | tabular.md:105-108 → **COLLIDES topology** |
MECHANISM: `CodecFault` consumed THREE decades (831/832/833) because of the tier ladder + 3 distinct codes, exhausting the clean-decade budget; by lane and tabular there was no free decade, so two pairs collided. PROSE EVIDENCE that no owner holds the map: each band author hand-tracks a DIFFERENT partial neighborhood — topology.md:166 names "835x/836x," columnar.md:128 names "836x/837x," cypher.md:49 names "836x," while tabular.md:82 and graph/codec name ONLY the 2500/2600 federation base — mutually inconsistent, none aware of the collisions. SECONDARY VIOLATION: lane's bare `Error.New` is the only corpus-a owner without a typed `[Union]:Expected` band. Since `Expected.Code` is the telemetry key, colliding codes make bands telemetry-ambiguous. RULING: author a package-wide Persistence `FaultBand` registry (mirroring the federation `FaultBand`), give lane's codebook a typed `CodebookFault` band, re-partition 836x/837x so every owner's sub-band is unique. `ARCHITECTURE.md:114`'s typed-receipt roster needs the same central treatment for fault BANDS.

### [D2] ★★★ Two `GraphFault` types — semantic-name collision across sibling namespaces.
`graph.md:155` `GraphFault : Expected` (ns `Rasm.Persistence`, 830x, graph-store faults) vs `cypher.md:60` `GraphFault : Expected` (ns `Rasm.Persistence.Query`, 836x, AGE/pgrouting faults) — same simple name, same base, same package. A consumer with both `using`s hits an ambiguous `GraphFault`; telemetry cannot disambiguate by type. Violates CLAUDE.md "one canonical semantic name per bounded concept." RULING: rename the cypher lane band → `CypherFault`/`GraphLaneFault`; the graph-STORE fault keeps `GraphFault`. Fold into the [D1] registry pass.

### [D3] ★★★ identity.md is a 4-concern 653-LOC page — extract the AUTHZ concern (CORRECTED direction).
`[04]AUTHORITY` (`identity.md:273-523`) welds object-authorization (`Grant`/`ObjectAcl`/`Admit`, zero KMS) to cryptographic custody (signing + envelope, one `KmsProvider` axis). The prior dossier proposed extracting signing+envelope — that is BACKWARDS: signing+envelope on one `KmsProvider` axis is the SETTLED shape (TASKLOG `[ENCRYPTION_PAGE_AUTHOR]-[COMPLETE]` folded `Store/encryption` in and forbade a separate envelope page; `:279-280` codifies it). The genuine weak seam is authz — `Grant`/`GrantSet`/`AclScope`/`AclEntry`/`ObjectAcl`/`Authority.Admit`+`Effective`+`LapsedFor` share NOTHING with the keyrings except the "AUTHORITY" label. RULING: extract `Element/authority.md` = the object-ACL authz vocabulary; `identity.md` keeps relational tier + `IdentityPolicy` + `KmsProvider` crypto custody + `SchemaVerdict`. Element/ grows 3→4 pages (a healthy axis); the AppHost seam `ARCHITECTURE.md:53` splits the same way.

### [D3'] ★★★ The entire streaming-egress + messaging package cluster is UNOWNED (dead admission, unifies with [D0]).
README admits `Confluent.Kafka`, `NATS.Net`, `RabbitMQ.Client`, `DotPulsar`, `CloudNative.CloudEvents(.Kafka/.SystemTextJson)`, `Confluent.SchemaRegistry(.Serdes.Avro/.Protobuf/.Json)` — an entire `[STREAMING_EGRESS]`+`[MESSAGING_PROTOCOLS]` cluster — but `EgressSink`/`Sync/egress` resolve to `<none>`, and TASKLOG `[CDC_STREAMING_EGRESS]-[COMPLETE]` cites `Sync/egress.md` which does not exist. Likewise `FlowtideDotNet.Substrait` (ARCHITECTURE:57 seam, IDEAS `[SUBSTRAIT_FEDERATION_SEAM]-BLOCKED`) has no `FederatedPlan`/`PlanNode` owner; `linq2db.EntityFrameworkCore` bulk-copy (tabular.md:5,18) is referenced not homed; `Apache.Arrow.Flight` (TASKLOG `[ARROW_FLIGHT_PACKAGE_ADMISSION]-[COMPLETE]` defers activation to the phantom `BULK_ETL_INTERCHANGE_PIPELINE`) has no Flight-egress owner. RULING: a large admitted-but-unwired surface — the campaign must either re-home these onto real owners (an egress folder/page, a federation page) or flag them for roster removal under the license/roster law. The Sync/ deletion ([D0]) orphaned the egress+messaging cluster.

### [D4] ★★ lane.md is 4 concerns / 521 LOC; extract the retrieval subsystem to Query/vector.md.
`FUSION_AND_CACHE` (`:267-377`) + `VECTOR_CODEBOOK` (`:379-521`, PQ k-means/ADC/`VectorIndex`) are one tightly-coupled ANN/search subsystem (fusion's vector branch reads the codebook ADC rows, `:388`) fused into the read-routing page. RULING: extract `Query/vector.md` (or `retrieval.md`) = fusion + codebook + the missing typed `CodebookFault` band ([D1]); lane.md drops to routing + set-algebra (2 coherent concerns). Query/ grows 5→6. Also resolves the lane bare-`Error.New` band violation.

### [D5] ★★ Ingest/ is a one-file folder; the delimited/schedule/Avro codecs are homeless (unifies with [D0]/[D3']).
`Ingest/` = tabular.md alone. `Sep` is named "the delimited sibling" (`tabular.md:3,5,18,20,298`) with no page; `MPXJ.Net` (schedule files, intended for phantom `Sync/schedule.md`) and `Chr.Avro*` (row-codec, intended for phantom `Sync/egress`) are homeless. RULING: grow `Ingest/` into a multi-page codec-ingress folder (tabular + delimited/Sep + schedule/MPXJ + row-codec/Avro), OR fold tabular into a broader ingress owner. A one-file folder is a false growth axis (folder-architecture law). CBOR is already `codec.md`-owned — scope the sibling set carefully.

### [D6] ★★ Seam-ledger drift: ARCHITECTURE.md:55 `Query/topology ← Rasm/Geometry/Spatial GeometryHash` is UNWIRED.
`topology.md` has ZERO `Geometry`/`Spatial`/`GeometryHash` references; its memo key (`:3,76,115`) is the `Rasm.Element` seam `ContentAddress.OfGraph`. The declared `[CONTENT_KEY]` seam has no fence consumer; the "federation/diff reads" half legitimately belongs to `Version/merge` (corpus-b), not the topology lane. Geometry brief `[V8]`/`[E12]` independently flags this exact anchor, re-targeted by the geometry namespace rename. RULING (waterfall-aware): re-anchor `ARCHITECTURE.md:55` to the true owner — any real Geometry adjacency-GeometryHash seam feeds `Version/merge`, and topology's OfGraph memo already reads the `Rasm.Element` seam (`ARCHITECTURE.md:42`). Align to whatever the geometry DECISION unifies.

### [D7] ★ Secondary carrier/hardcoding scan.
- `HashPolicy` `Compute`/`ByDomainId` + 3 of 5 rows (Frame/Wide/FrameWide) are anticipatory carriers — Seal/Verify hardcode XxHash128/Crc32 (codec.md:319,321,354,357). Dead-until-load-bearing; defensible forward-proofing.
- columnar egress/mount/generation raw-string-interpolate `destination`/`stamp`/`store`/`alias`/`root` while Query/Secret bind via `DuckDBParameter` — uneven parameterization, no stated trust gate (columnar.md:256,356-361,391).
- `columnar.md:80-83` four profiles repeat identical `"80%"`/`"90%"` posture literals — a package constant, not per-profile policy.
- `Ara3D.BimOpenSchema` ships DEBUG-built IL (columnar.md:411) — package-quality defect for the roster review.
- Otherwise corpus-a is strongly generator-driven — SmartEnum/Union/FrozenSet rosters as seed DATA (`ArtifactKind`, `ColumnarExtension`, `VectorMetric`, `EmbeddingArity`, `EdgeFilter`, `GraphQuery`, `SigningAlgorithm`) are correctly one-row-per-case; no enumerated-instances-where-a-generator-belongs defect found.

---

## [E] SOUND SURFACES (preserve; harden, never re-derive)
`codec.md` seam-composing `ContentAddress` + 8-rank `RejectTier` ladder + single-pass atomic seal + CBOR `Canonical`/`Strict` trust boundary; `graph.md` one-materializer event-sourcing + total `Switch` + co-transactional identity; `topology.md` seam-frozen incidence + honest `Fin` absent-root guards + one shared SCC + `Spatial`/`Containment` filter correctness; `lane.md` honest sequence-gap `StalenessWatermark` (not the recording-clock confound) + length-framed collision-free `ElementSet` receipt; `cache.md` single-horizon gate-folded-into-`Lookup` + `IBufferDistributedCache` buffer contract + `TenantId`-scoped key; `cypher.md` injection-safe server-side `format('%L')` TSP + NODE/CELL space discipline + one polymorphic `GraphQuery` verb surface; `tabular.md` one `TabularSpec`+`Origin` modality discriminant + both-leg `TabularFault` fold + wire-factory typed read; `identity.md` `H3Cell` bit-exact convention + two-grain cell discipline + total `SchemaGate` boot fold. Corpus-wide the kernel `Rasm.Domain.Expected` fault base, external-member `.api` discipline, and strata one-directionality are intact.
