# [PROJECTION_TASKLOG]

Open and closed work distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` when open; `[COMPLETE]` or `[DROPPED]` when closed — and three to four bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[BLOCKED] Wire the convergence-law harness onto the frozen fixture corpus.
- Re-found the `convergence/convergence-law#CONVERGENCE_LAW` `opLogEntryArb`/`conflictReceiptArb` arbitraries on the cross-libs `ONE_WIRE_FIXTURE_CORPUS` content-addressed golden bytes (the CRDT op-set, the HLC two-half stamps) read through the new branch `typescript:testing/` fixture reader rather than folder-local hand-minted bytes, so a parity drift surfaces as one corpus mismatch.
- Integrate `fast-check` (the arbitrary spine reads the frozen corpus as a `constantFrom` seed set) and `@effect/vitest`; the corpus derives from the one `@bufbuild/buf`-pinned descriptor source the C# wire authors.
- Internal to `convergence/`, reading the branch `testing/` fixture corpus (the `ONE_WIRE_FIXTURE_CORPUS` named TS consumer); the law unchanged, only the arbitrary seed source. From CONVERGENCE_PROOF_HARNESS re-open.
- The op vocabulary stays decode-admitted; the corpus is the one frozen source rather than per-folder fixtures, the cross-language parity drift caught as a single mismatch.
- Close-condition: the `de-hardcode` task landed the named `CollisionBounds` frozen table on `convergence/convergence-law#CONVERGENCE_LAW` (the `[3]-[RESEARCH]` `CORPUS_SEED_BINDING` note records the swap), so the re-founding is a one-line re-pointing of `CollisionBounds` to the fixture reader's `constantFrom` seed once the branch `typescript:testing/` fixture reader exists — confirmed absent today, and `TESTING_LIB_FOLDER` is not a committed big idea. Realizes when the fixture reader lands.

[BLOCKED] Merkle anti-entropy range reconciliation.
- Build a read-side `MerkleRangeWire` digest-comparison handshake over the `causality-graph/version-vector#VERSION_VECTOR` `dominates` algebra so a reconnecting peer reconciles its `CAUSAL_STABILITY_DELIVERY` stable prefix by set-difference rather than a full changefeed re-fold.
- Integrate `effect` `HashMap`/`SortedMap`/`Order`; the `MerkleRangeWire` shape arrives from `csharp:Rasm.Persistence/versioning/version-control#TS_PROJECTION`.
- Internal to `causality-graph/`; depends on the version-vector wiring, the causal-stability frontier (the reconciliation prefix unit), and the `MerkleRangeWire` decode landing. From MERKLE_ANTI_ENTROPY.
- Bounds reconnect cost to the divergent range; the digest names the slice a peer pulls, never an unbounded re-scan.
- Close-condition: the forward `causality-graph/version-vector#MERKLE_RECONCILE` stub cluster is named (owner, cases, boundary), but the fence body is blocked-gated on (1) `MerkleRangeWire` arriving decode-admitted on the `libs/typescript/interchange` decode rail (confirmed absent: 0 hits across `interchange/.planning`) and (2) the now-landed `causal-delivery/stability-frontier#STABILITY_FRONTIER` stable prefix as the reconciliation unit. Realizes the fence when the C# `version-control#TS_PROJECTION` `MerkleRangeWire` decode lands.

## [2]-[CLOSED]

- [COMPLETE] Standing-query live-changefeed replay confirmation — `standing-query/window-fold#REPLAY_LAW` owns `outOfOrderArrivalArb` (a `fast-check` `shuffledSubarray` full-length out-of-order arrival arbitrary) and `windowReplayLaw` (the `it.scoped.prop` suite folding the real `windowFold` owner over the shuffled changefeed in a forked `Scope`), asserting the signed-delta retraction-plus-restate is order-invariant against in-order delivery and the watermark `late` tally tracks every correction; validated the hand-rolled fold before the `d2ts` re-founding landed on the same owner. From DATAFLOW_REFOUNDING.
- [COMPLETE] Differential-dataflow re-founding of the event-time window engine — `standing-query/window-fold#DATAFLOW` owns `windowGraph`, the `windowFold` re-founded on a typed `@electric-sql/d2ts` operator graph (`D2`/`newInput`/`groupBy`/`consolidate`/`output`, `groupBy` owning its own `{ bucket }` keying so no preceding `keyBy`) keeping `WindowKind`/`bucketSet` as the windowing reduce stage and the watermark as the `d2ts` frontier antichain via `sendFrontier`, the roll-ups riding `groupBy` with the catalogued `sum`/`avg`/`min`/`max`/`median`/`mode` aggregates and the retained `Index#reconstructAt` trace feeding the as-of Version coordinate; precondition replay-confirmation landed first. From DATAFLOW_REFOUNDING.
- [COMPLETE] Frontier garbage collection wiring across every keyed map — `frontier-gc/frontier-gc#SOURCE_WIRING` owns `windowReclaimable`/`tombstoneReclaimable`/`presenceReclaimable` and `reclaimableFeed`, the `Stream.merge` of the three projected event-time sources `frontierGc` folds so one `finalizeBelow` pass evicts a window bucket, a `lww-merge#LWW_MERGE` convergence tombstone, and a `presence#PRESENCE` row in one trim; the per-row `expiresAt` scans in `lww-merge`/`presence` unify under the one frontier rule. From FRONTIER_GARBAGE_COLLECTION.
- [COMPLETE] Causal-delivery buffer and stability frontier — `causal-delivery/causal-buffer#CAUSAL_BUFFER` owns `CausalBuffer` (the per-entity pending queue admitting an op only when `dominates(heldCursor, opContext)` holds modulo its slot, releasing the dependency-satisfied suffix in causal order), `DeliveryVerdict` (Held/Released), and `causalDelivery`; `causal-delivery/stability-frontier#STABILITY_FRONTIER` owns `stabilityFrontier` (the `SortedSet`-of-cursors greatest-lower-bound meet) naming the causally-settled horizon `frontier-gc` finalizes against — one discipline over the version-vector `dominates` algebra, no second ordering surface. From CAUSAL_STABILITY_DELIVERY.
- [COMPLETE] Ordered-index collapse onto the effect sorted-collection owners — `evidence/evidence-correlation#EVIDENCE_CORRELATION` owns the `RedBlackTree` timeline keyed by the composed HLC `Order` (O(log n) `insert`, since-T window through `greaterThanEqual`) replacing the `[...rows, e].sort(byHlc)` whole-array re-sort, `causality-graph/version-vector#CRDT_SEMILATTICE` owns the RGA `SortedMap` keyed by the dense `(after, id)` order position (O(log n) `set`/tombstone, in-order `entries`) replacing the linear `cells` array, and the `SortedSet` stability-meet arm landed in `causal-delivery/stability-frontier#STABILITY_FRONTIER`. From ORDERED_INDEX_COLLAPSE.
- [COMPLETE] As-of temporal-query axis — `temporal-query/as-of-query#AS_OF_QUERY` owns `AsOf` (the closed three-case `Data.TaggedEnum` coordinate — `Version` against the `d2ts` antichain via `Index#reconstructAt`, `EventTime` against the watermark nanos, `Stable` against the causal-delivery stability frontier) and `asOfQuery` (the total `$match` materializing the keyed map as of the coordinate), and `temporal-query/as-of-diff#AS_OF_DIFF` owns `asOfDiff` (the two-coordinate snapshot diff the conflict inspector reads) — one query surface, no second history store. From AS_OF_TEMPORAL_QUERY.
- [COMPLETE] Degradation-gated availability fold — `availability/availability-gate#GATE_POLICY` owns `DegradationLevel` (the five-case ordered `Data.TaggedEnum` mirroring the C# health vocabulary), `DegradationOrder`, and `gatePolicy`/`admits` (the frozen level-to-admitted-tier comparison), and `#AVAILABILITY_GATE` adds the second arm joining the live `feed-stores/live-cells` degradation cell so `isEnabled` returns the conjunction of the wire `available` flag and the live degradation gate — a new health-gated surface is one floor on an availability row, never a second health model. From DEGRADATION_GATED_AVAILABILITY.
- [COMPLETE] De-hardcode the convergence, watermark, and stream-policy baked-in literals — `fold-core/stream-policy#STREAM_POLICY` owns the `PolicyProfile` `as const satisfies Record` posture table (`interactive`/`bulk`/`idle`) with `streamPolicy` materializing a row, `standing-query/watermark#WATERMARK` owns the `LatenessProfile` table (`tight`/`standard`/`relaxed`) with `lateness` lifting a row into a `WindowSpec.allowedLateness` `Duration`, and `convergence/convergence-law#CONVERGENCE_LAW` owns the `CollisionBounds` frozen table (`origins`/`keyBytes`) `fc.constantFrom` reads — anything that varies is a row value, never a re-baked literal. From the parameterization dimension.
- [COMPLETE] Subscribable projection face on every store — `fold-core/projection#PROJECTION` authored: `Projection<A>` aliases the `effect` `Subscribable<A>` `{ get, changes }` contract, `projectStore` lifts a store's `SubscriptionRef` into the baseline `Projection` via `Subscribable.make` deduplicated through `Stream.changes`, and `derive`/`deriveEffect` map a store into a projected view (filtered map, sorted slice, scalar gate) as one `Subscribable` the `ui` `Atom.subscribable` bridge consumes verbatim, so every store exposes its read as one `Projection` row and a derived read is one `derive` row at the `projection` altitude, never a parallel bind-site fold; the `[3]-[BINDING_SEAM]` cluster documents the `@effect-atom/atom` consumer contract held off the fold interior. From SUBSCRIBABLE_PROJECTION_FACE.
- [RE-TIERED] Effect sorted-collection and subscribable catalogue coverage → branch `TASKLOG.md` — the `RedBlackTree`/`SortedMap`/`SortedSet`/`Subscribable`/`Equivalence`/`Stream`-combinator extension lands on the single-owner branch `libs/typescript/.api/effect.md`, never mutated folder-side; the `projection` ordered-index and projection-face tasks read it as a branch seam (the prior folder task miscatalogued `greaterThanEqual`/`insert`/`removeFirst` as `SortedMap` members — corrected to `RedBlackTree` in the re-tiered branch task).
- [COMPLETE] Version-vector concurrency read model wiring — `causality-graph/version-vector#VERSION_VECTOR` `causalStore` reads the live changefeed (`vectorOf` off the `origin` cursor or `CommitNodeWire.vector`, `bandOf` off the skew band) so each op carries a `Before`/`After`/`Concurrent`/`ConcurrentUncertain` verdict; unblocked by the decode-rail surfacing the full `OpLogEntryWire` + commit vector. From SKEW_AWARE_ORDERING.
- [COMPLETE] CrdtOpWire join-semilattice arm on the convergence read model — `causality-graph/version-vector#CRDT_SEMILATTICE` lifts the 10-case `CrdtOpWire` union as the join-semilattice decode (`write` context → `vectorJoin`, `add`/`remove` → OR-set, `beat`/`leave` → `EphemeralMap` delta) under the `convergence-law` delivery-order independence; unblocked by `decode-rail#CRDT_OP_DECODE`. From CONVERGENCE_PROOF_HARNESS.
- [COMPLETE] Convergence law harness — `convergence/convergence-law` authored: the `opLogEntryArb`/`conflictReceiptArb` arbitraries, the `shuffledSubarray` permutation, and the `it.scoped.prop` delivery-order-independence, idempotent-replay, and tombstone-monotonicity laws over `conflictStep`, mutation-gated by `@stryker-mutator/core`. From CONVERGENCE_PROOF_HARNESS.
- [COMPLETE] Derived reactive-query surface — `live-query/reactive-query` authored on `@electric-sql/d2mini`: the `LiveQuery`/`queryStore` combinator wires a `MultiSet` `PipedOperator` pipeline through `consolidate`/`output` and folds the sink into one `SubscriptionRef`, owned at the `projection` altitude so `ui` carries no view-state library. From REACTIVE_QUERY_SURFACE.
- [COMPLETE] Frontier garbage collection page — `frontier-gc/frontier-gc` authored: the `Frontier` antichain, the `Reclaimable` three-kind vocabulary, `advanceFrontier`, `finalizeBelow`, and the `frontierGc` unified-trim fold; the wiring carries forward as the queued GC task. From FRONTIER_GARBAGE_COLLECTION.
- [COMPLETE] Skew-aware ordering page — `causality-graph/version-vector` plus the `clock-uncertainty/skew-band` `bandsOverlap` ordering input authored: the `VectorOrderKind` partial order reconstructs `Concurrent` and `skewVerdict` downgrades it to `ConcurrentUncertain` when the bands overlap; the live-changefeed wiring carries forward as the blocked version-vector task. From SKEW_AWARE_ORDERING.
- [RESOLVED] Wire RESEARCH items closed against the authoritative `csharp:Rasm.Persistence/sync/collaboration#TS_PROJECTION` and `csharp:Rasm.AppUi/commands/commands-availability#TS_PROJECTION` seams: `ConflictOutcomeKind` (`LocalWin`/`RemoteWin`/`Merged`/`Rejected`) on `lww-merge`, the `actor`-on-`entityKey` presence identity on `presence`, and the `CommandAvailabilityWire.key`/`available` spellings on `availability-gate` (the prior `enabled` member was a defect, corrected to `available`).
