# [REGIONS] — durability (final fragment)

Final region rows for `domain/durability.md`; orchestrator consolidates into `_region-map.md`.

## [1]-[PAGE_REGION]

- domain/durability.md: embedded durability law — the idempotent open-ritual fold with pragma-residency, hardening, and capability rows under the IMMEDIATE first-opener-migrates gate; WAL cross-process topology, busy-retry taxonomy, and the receipted maintenance/backup/snapshot-pin schedule; binary contract law (dense keys, union-tag retirement, generated resolver chain) and frozen codec profiles split write-trusted versus restore-untrusted; the sealed fixed-offset artifact header with contract-stamp and version ratchets, single-pass seal, ordered eight-tier rejection ladder, and seven-step receipted restore choreography under one epoch token; the one op-log with LWW guarded-set adjudication, conservation receipts, version-vector cursors, manifest duality, tombstone fencing, and presence-as-ephemeral-class; retention class rows with the hold-first sweep fold, budget-versus-ceiling overflow split, evidence windows, and export proofs.

## [2]-[SNIPPET_REGIONS]

- DR-01 OPEN_RITUAL_FOLD: the idempotent embedded-store open ritual — declared per-connection pragma rows AND declared capability rows (`CreateFunction`/`CreateAggregate` thunks with `isDeterministic: true`) folded to receipts, `raw.sqlite3_db_config` hardening with out-echo receipt, and the `BeginTransaction(IsolationLevel.Serializable, deferred: false)` first-opener-migrates gate over `application_id`/`user_version` with typed foreign-store and epoch-ahead rejections. Distinct from RT-01 (host boot fold, not native store ritual) and VL-03 (seam matrix).
- DR-02 CONTRACT_RESOLVER_FREEZE: the binary contract surface — dense integer `[Key]` records under a `[MessagePack.Union]` root, `[GeneratedMessagePackResolver]` compile-time formatter closure, `CompositeResolver.Create` specificity chain seating `ThinktectureMessageFormatterResolver.Instance`, frozen write/restore option profiles split by `WithSecurity(MessagePackSecurity.UntrustedData)` plus depth ceiling, buffer-writer encode, sequence decode minting the ladder's tier-8 code, `MessagePackStreamReader` segment framing. Distinct from B-08 (STJ wire-family converter) and TP-07 (STJ suite resolver merge — different codec, different seam).
- DR-03 SINGLE_PASS_SEAL: the sealed-artifact write protocol — observed-compression `LZ4Codec.Encode` with incompressible store-raw fallback, class-seeded `XxHash3` stored-bytes identity, fixed-offset little-endian header carrying contract stamp and its own checksum packed via `BinaryPrimitives`, placeholder-header/seek-zero/finalize choreography, `Flush(flushToDisk: true)` then atomic `File.Move`. Distinct from TP-06 (wire framing corridor — stream receive-order guard, no at-rest seal or rename commit), TP-05 (manifest attach lifecycle — the staged-write rename there is supporting material for discovery, here the seal protocol IS the spotlight with header identity), and B-05 (signed byte-identity admission, not artifact sealing).
- DR-04 RESTORE_CHOREOGRAPHY (capstone): the receipted restore — seven-step row fold (fence via `SqliteConnection.ClearPool`, ordered seven-tier raw-byte rejection ladder including the contract-stamp ratchet, observed-row decompress materialize, sidecar-set delete, atomic rename, epoch-register bump, ritual reopen) accumulating a `StepFact` ledger that flushes on failure, dual-channel `(Ledger, Outcome)` return, one code band for ladder tiers and one for choreography steps. Distinct from TP-05 (manifest attach lifecycle) and RT-04 (phase spine — no drain bands).
- DR-05 LWW_BATCH_ADJUDICATION: the op-log apply transaction — STRICT/WITHOUT ROWID one-log schema with the fixed three-index plan, `json_each`-shredded dedup insert RETURNING fresh seqs, window-ranked per-entity lattice-max upsert guarded by the row-value `(excluded.stamp, excluded.origin) > (stamp, origin)` comparison with RETURNING as the applied set, one two-sum loss probe partitioning Superseded versus TombstoneSuppressed, cursor advance inside the same IMMEDIATE transaction, equality-only epoch gate, conservation-checked `MergeReceipt`. Distinct from DG-04 (stamp algebra — stamps consumed here as packed integers only, never re-derived) and PS-06 (EF store-op rail — this is the raw ADO adjudication lane).

Retention sweep mechanics (hold-first fold, verdict vocabulary, conservation receipt, evidence windows, export proofs) are carried at card altitude on the page; no snippet region is claimed for them.
