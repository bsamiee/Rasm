# Geometry-close redteam residuals — Phase B writer input (cross-package obligations, verified)

1. `Rasm.Compute` `Tensor/residency.md:190` + Compute `ARCHITECTURE.md:178` name the kernel entry `Encoding.Apply`; the landed owner is `Encode.Apply` (`Drawing/pack.md`) — compute-brief refresh captures the correction.
2. `Rasm.AppHost` has no `GeometryPacking` capsule page though `pack.md` and Compute cite it — unrealized obligation for the apphost brief.
3. `Rasm.Persistence/Version/merge.md` geometry-axis no longer anchors the reconciliation byte-identity harness — persistence DECISION refresh arbitrates.
4. `.api` gaps: `api-rhino.md` misses the Analysis-consumed surface; `api-languageext.md` misses `WrappedErrorExpectedException` — a focused rebuild-api note, not a root-doc row.
5. `Rasm.Domain.Expected` shadows `LanguageExt.Common.Expected` cross-package under the global using — one disambiguation note wherever a doc designs cross-package `Expected` handling.
