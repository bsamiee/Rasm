id: no-duplicate-project-instance
valid:
    # The DependsOnTargets form, one instance
    - |
      <Target Name="PublishOnBuild" AfterTargets="Build" DependsOnTargets="Publish" Condition="'$(_IsPublishing)' != 'true'" />
    # Query of the referenced projects without global properties
    - |
      <MSBuild Projects="@(ProjectReference)" Targets="GetSchemaFiles" BuildInParallel="$(BuildInParallel)" SkipNonexistentTargets="true" />
    # Call of the project itself without global properties, the instance is shared
    - |
      <MSBuild Projects="$(MSBuildProjectFullPath)" Targets="Publish" />
    # Empty Properties value, no global property and the same instance
    - |
      <MSBuild Projects="$(MSBuildProjectFullPath)" Targets="Publish" Properties="" />
    # Item holding the parameters as metadata, no build starts
    - |
      <ItemGroup><_Publish Include="self" Projects="$(MSBuildProjectFullPath)" Properties="_IsPublishing=true" /></ItemGroup>
    # The project path passed as a property value names no project to build
    - |
      <MSBuild Projects="@(ProjectReference)" Targets="Build" Properties="Caller=$(MSBuildProjectFullPath)" />
    # Referenced project built with global properties, another instance by design
    - |
      <MSBuild Projects="@(ProjectReference)" Targets="Build" Properties="Configuration=$(Configuration)" />
    # Empty single-quoted Properties value
    - |
      <MSBuild Projects="$(MSBuildProjectFullPath)" Targets="Publish" Properties='' />
invalid:
    # Single-quoted Properties value
    - |
      <MSBuild Projects="$(MSBuildProjectFullPath)" Targets="Publish" Properties='_IsPublishing=true' />
    # Call of the project itself with a Properties value, another instance shares bin and obj
    - |
      <MSBuild Projects="$(MSBuildProjectFullPath)" Targets="Publish" Properties="_IsPublishing=true" />
