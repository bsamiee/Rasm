# [DOSSIER_MESHING] — robust-core Meshing lane

Read-only survey, HOSTILE stance. Scope: robust-core `Meshing/{delaunay,intersect,arrangement,offset}.md` FULL; landed substrate `Meshing/{mesh,dec,reconstruct}.md` as standing-law context (member-verification + seam truth). All anchors `libs/csharp/Rasm/.planning/Meshing/`-relative unless prefixed. Disk re-verified 2026-07-03.

Folder census: four robust-core concept namespaces (`Rasm.Geometry.Tessellation` delaunay:36, `Rasm.Geometry.Intersection` intersect:39, `Rasm.Geometry.Arrangement` arrangement:41, `Rasm.Geometry.Offsetting` offset:39) coexist in `Meshing/` with the three landed `Rasm.Vectors`-namespace pages (mesh/dec/reconstruct all `namespace Rasm.Vectors`, mesh:36/dec:33/reconstruct:35). Fence labels: delaunay+offset `csharp signature`, intersect+arrangement `csharp contract` (:22 each) — a label discipline the completeness claims below violate.

---

## [01] REGISTER RE-VERIFICATION (E-rows anchored in robust-core Meshing pages)

| Row | Verdict | Disk finding |
|---|---|---|
| E3 (delaunay:3 false Triangle-GPL) | HOLD | delaunay:3 verbatim "No external geometry library is admitted (Triangle/TetGen are GPL and rejected)". False per [V14]: Triangle.NET is MIT; TetGen is AGPL; the load-bearing rejection is float-epsilon + no exact CDT/CDTet. Rationale survives the package purge. |
| E3 (intersect:388 `Analysis/Intersect.cs` cite) | DRIFT | Cite lives at BOTH intersect:5 AND intersect:388 (`rg` confirms two sites). Register anchors :388 only — corrected anchor set is {:5, :388}. Both re-point to landed `Analysis/relations.md` per [PLACEMENT_LAW]. |
| E5/E11 store-mutability (delaunay:5,66-74) | HOLD | delaunay:5 "The `SimplexStore` IS the hash-friendly immutable record". Contradicted by `SimplexStore.Spawn` (:66-72 mutates `Vertices`/`Neighbours`/`Dead`) and `Kill` (:74 `Dead[simplex]=true; FreeList.Push`). Mutable build arena mislabelled immutable. |
| E5/E11 store-mutability (arrangement:87-91,488) | HOLD | arrangement:488 "immutable SoA record"; `ArrangementStore.Write` (:87-91) mutates `Vertices`/`Faces`/`FaceOrigin`/`FaceInside` in place. Contradiction confirmed. |
| E11 store-mutability (offset:5 vs :13) | HOLD | offset:5 "The `WavefrontStore`/result carriers are the hash-friendly immutable records"; offset:13 "the struct-of-arrays flat propagation memory the event queue **mutates**". Direct self-contradiction; `Spawn`/`Kill` (:79-88) + `Collapse`/`Divide` write `Position`/`Next`/`Prev` in place (:232-233,251-253). |
| E11 offset:195 WavefrontStore 2n under-alloc | HOLD | `Seed` (:195) allocates `Prev`/`Next`/`Dead`/`SpawnTime`/`Origin` at `2*n`, `Position`/`Velocity` at `4*n` (=2n vertices). `Spawn` grows `Count` past `n` with no bounds guard (:80 `Count` fallback when FreeList empty); `Divide` nets +1 vertex/event (spawn 2, kill 1, :249-253). A split-heavy reflex polygon exceeds 2n → IndexOutOfRange. |
| E7 crossing-collapse owner (intersect:388) | HOLD | intersect:388 [EXACT_CROSSING_COLLAPSE] names itself the single owner and enumerates the three inline copies to collapse. |
| E7 inline copy (arrangement:242-262) | HOLD | `EdgeCrossings` (:242-247), `InTriangle` (:249-256), `PlaneCrossPoint` (:258-262) are the live inline exact-straddle copies. |
| E7 inline copy (offset:440-444) | HOLD | `SegmentsCross` (:440-444) four-`Orient2D` straddle; two internal callers `SimplePolygon` (:435) and `SelfOverlaps` (:394). |
| E7 medial dual claim (offset:16,478) vs discard (:325-326) | HOLD | offset:16 + :478 claim the medial "reconciles against the constrained-Delaunay Voronoi dual"; `MedialFrom` (:316-327) calls `Tessellation.Build(...).Map(_ => new MedialAxis(...))` — the tessellation is bound to `_` and **discarded** (:325-326); the `MedialAxis` is the skeleton trace alone. No Voronoi reconciliation occurs. |
| E7 arrangement implicit framing (:467) vs rounded (:258-262) | HOLD | arrangement:467 asserts implicit-point exactness; `PlaneCrossPoint` (:258-262) rounds the operand crossing to `double t = (n·(a−u))/(n·(v−u))`, flagged `IsImplicit:true` (:246) but materialized-rounded, then interned as an explicit `Constraint.Segment` (:285-288) into `Tessellation.Build`. Headline boolean is exact-sign, rounded-vertex. |
| E9 BooleanReceipt name ×2 (arrangement:115) | HOLD | `public sealed record BooleanReceipt(int Classified, int Kept, int Welded, bool AssetGated)` at arrangement:115, namespace `Rasm.Geometry.Arrangement`. Collides with receipts.md:44 (out-of-scope, different shape/namespace per register). |
| E9 soup adapter ×3 (intersect:326) | HOLD | `Soup(MeshSpace)` (:326-329) `MeshEdit.OfMesh(mesh)` → `(Vertices,Faces)`. |
| E9 soup adapter ×3 (arrangement:352-365) | HOLD | `Triangles(MeshSpace)` (:352-362) + `Soup(Triangle[])` (:364-365) — the two-step soup extraction. |
| E15 seven stub bodies (delaunay:192-198) | HOLD | `Seed` (:192), `Retriangulate` (:193), `RecoverOne` (:194), `StripSuper` (:195), `LastLive` (:196), `TouchesSuper` (:197), `AddSimplexFaces` (:198) — signature-only, terminated by `;`. The "hardest transcription in the tree" ([03]). |
| E16 delaunay Voronoi-dual (README:99) | HOLD | No Voronoi-dual accessor/case/surface exists in delaunay.md (`rg "Voronoi\|dual"` hits only :252 CONSTRAINT_RECOVERY prose). README:99 asserts "`Meshing/delaunay` owns the constrained Voronoi dual" — an unfulfilled index-doc obligation; offset:16 depends on the missing accessor. |
| E16 arrangement implicit-point/BooleanOp/native-gate (README:42, ARCH:42) | HOLD | Fence side: implicit-point rounded (above); `BooleanOp` composed from Healing not homed here (:18,189); native gate is `ScaleCeiling:int.MaxValue` dead (:61). |
| E8 counterpart (intersect PlaneMesh ↔ FAB:23 Slicing.cs; arrangement ↔ FAB Posting silhouette) | HOLD (counterpart) | intersect:389 + arrangement:478 declare the consume-when-realized seams; cross-package, one anchor each, land as [V10]/ [V5] ripples. |

No REFUTED rows in the Meshing lane — every assigned register pointer survives disk verification. The register UNDERSTATES the Meshing defect surface: see [03].

---

## [02] PER-PAGE VERDICTS

### delaunay.md — `Rasm.Geometry.Tessellation` — grade 6, honest-incomplete

Sound spine, honestly marked incomplete (`signature` fence). `Tessellation` `[Union]` (Triangulation/Tetrahedralization) over one `SimplexStore`, `Build`/`Insert`/`Recover`/`ToMesh` (:104-138), exact-predicate `Locate`/`ExitFace`/`Cavity`/`InCircum` walk (:141-189) are transcription-complete and correctly compose `Predicate.Orient2D/Orient3D/InCircle/InSphere`. `Delaunay.InsertionOrder`/`Morton`/`Expand10` (:201-223) is a real 30-bit Z-order spatially-coherent insertion order. The union-over-one-store collapse (:20 Boundary) is genuinely award-grade design.

Defects (register): E3 false-GPL (:3); E5 immutable-mislabel (:5,66-74); E15 seven stubs (:192-198); E16 no Voronoi-dual accessor.

Beyond register:
- **Density-bar honesty holds.** :247-248 claims the walk "transcription-complete" and the `[STORE_OPS]` cluster "signature-fixed" — accurate; the `signature` fence label matches. Contrast intersect/arrangement below.
- **The Steiner/LPI-TPI exactness is prose-only.** :16,20,252 assert constructed Steiner points route `Predicate.OrientLPI/OrientTPI` for exact-sign in-circum; the body carrying it (`RecoverOne` :194) is a stub. The exactness claim is unrealized until [V4]/E15 transcription lands.
- **Fault-cluster gap ([V1]).** `Tessellation` routes only generic `GeometryFault.DegenerateInput`/`UnrepairableMesh` (:106,113,121,148) — no dedicated sub-band. Confirms the [V1] census "Tessellation owns no cluster" — the concept-namespace↔fault-cluster 1:1 law fails here alone among the four Meshing owners. delaunay's fault home is the DECISION's call.
- **Cross-lane member risk.** delaunay's `MeshSpace.Of(mesh, tolerance)` (:137) verifies against mesh:129 `Of(Mesh, Context, MeshAssemblyPolicy?=null, Op?=null)` — VALID (defaults). `Constraint.Segment`/`Facet` (:81-82) self-owned. `Predicate.OrientLPI/OrientTPI`, `Lpi`, `Tpi` (:18,252) are Numerics-lane members (predicates.md, out of scope) — flag for the Numerics surveyor.

Charter-as-it-should-be: KEEP + REBUILD. Transcribe the seven `[STORE_OPS]` bodies (the E15 target); MINT the Voronoi-dual accessor as a `Tessellation` projection (lifted-paraboloid lower-hull dual, a Bowyer-Watson byproduct — [V10]c/[V11] hull-fold ruling), so offset:16 medial and README:99 land; the store-mutability contract makes `SimplexStore` an explicit mutable arena, content-addressing binding the FROZEN `MeshSpace` projection via `reconciliation.Encode`, never the mid-build store; rewrite :3 rejection rationale to the honest split (Triangle.NET MIT, float-epsilon/no-exact-CDT the real bar); rule the fault home. Adopt the substrate Op-threading + `IValidityEvidence` per [SEAM_AND_RAIL_LAW].

### intersect.md — `Rasm.Geometry.Intersection` — grade 8.5 → REVISE DOWN: the chain does not chain

Genuinely strong narrow-phase: `IntersectOp` `[Union]` six cases over one `IntersectStore`, one `Apply` (:135-144), Guigue-Devillers exact tri-tri (`CrossTriangles` :181-195) with `SameSide`/`Canonical`/`Order` exact-sign helpers, exact `OrderKey`+`RationalKey` endpoint ordering within a tri-tri segment (:206-216). BVH broad-phase composed (:219-243). This is the crown of the Meshing lattice — but the register's 8.5 masks a headline-deliverable failure.

**PRIMARY beyond-register defect — `Chain` emits disconnected 2-point segments, never contours (E1-class).** `Chain` (:266-285) builds `adjacency` by linking ONLY the two endpoints of each consecutive store-slot pair (:269-270 `Link(adjacency, endpoints[s], endpoints[s+1])`), then walks. Each connected component is exactly one 2-node segment → each emitted `Polyline` is a single crossing segment (2 points). It NEVER links endpoints coincident across different crossing segments, so no section contour closes and no mesh-mesh curve runs end to end. Compounding:
  - the `IntersectStore.Next` successor column (:75) is written to `-1` on every `Add` (:85) and NEVER re-linked by `Accumulate` (:250) or `Section` (:261) — dead column;
  - the exact `OrderKey`/`RationalKey` (:63) are consumed only inside `CrossTriangles` endpoint ordering, NEVER by `Chain` — the "exact OrderKey sort" (mermaid :363, prose :16,387) is not realized in chain assembly;
  - `IntersectResult.Chains(Seq<Polyline> Loops)` (:100) labels these open 2-point segments "Loops".
This is the exact non-robustness class E1 catalogs for `view.md` ("concatenates all segments of a kind by Edge.Key regardless of connectivity — garbage polylines"). It falsifies: [05] Fabrication slice-stack acceptance ("oriented contours + nesting tree"), the documentation dry-run ("connectivity-chained section polylines"), the [V10]b premise ("`IntersectResult.Chains` is loops-only today" — reality: disconnected segments), and view.md `ViewOp.Section`'s delegation to `IntersectOp.PlaneMesh` (view:349-353). The [03] density-bar claims the `[CHAIN]` cluster "transcription-complete" (:382) under a `contract` fence — the claim is false.

Other beyond-register:
- **`Analysis/Intersect.cs` cite drifts to two sites** (:5, :388) — see E3 DRIFT.
- **Unguarded `QueryResult` casts** (:222 `(QueryResult.RayHit)`, :233 `(QueryResult.Pairs)`, :241 `(QueryResult.Hits)`) — hard downcasts on the Spatial query union that throw on a kind miss; the E10 rail-breach class the brief bans (`index.md` throws) recurring on the consumer side. Must route `Fin` with a typed `GeometryFault`.
- **`Crossing` carries both `Denominator` and `Lambda`** (:63 `Crossing(Point3d Site, Expansion OrderKey, Fraction RationalKey, ...)`; :293 reads `point.Denominator` AND `point.Lambda` on `Lpi`) — the `Lpi` shape claim (`.Numerator.X`, `.Denominator`, `.Lambda`, `.Estimate()`, `.ToFraction()`) is a cross-lane phantom risk against predicates.md; two distinct homogeneous-denominator field names on one `Lpi` is suspicious. Flag for Numerics surveyor.
- **SegmentSegment silent Z=0** (:293 hardcodes `0.0` third coord; `CrossSegments` :160-167 runs `Orient2D` on 3D `Line`) — E7/[V4] typed-2D-restriction target, confirmed.
- **Soup adapter** (:322-329) via `MeshEdit.OfMesh` — [V6] collapse target; imports `Rasm.Geometry.Healing` (:31) which re-anchors to the new substrate.

Charter-as-it-should-be: KEEP + REBUILD the chain assembly. `Chain` must key crossing endpoints by coincident `Site` (exact `OrderKey`/`RationalKey`, the fields that already exist), link ACROSS segments sharing a vertex, and walk true connectivity — closed loops close, open curves run end to end, emitting ORIENTED loops (outer CCW / holes CW) + typed OPEN-chain rows for non-watertight sections ([V10]b widens `IntersectResult`). Wire or delete the `Next` column. Land the E7 consumer collapse (arrangement/offset/repair route through `Apply`/`Crossing`); type `SegmentSegment`/`LineLine` as a 2D restriction ([V4]); re-anchor the two `Analysis/Intersect.cs` cites to `relations.md`; route the `QueryResult` casts through `Fin`; expose the slice-stack producer seam. Drop the `contract` label until the chain is real.

### arrangement.md — `Rasm.Geometry.Arrangement` — grade 7.5, exact-sign/rounded-vertex + partition tension

Deepest composition on the lane: `Arrangement` `[Union]` (MeshBoolean/PlanarOverlay/CellComplex) over one `ArrangementStore`, `Arrangers` FrozenDictionary dispatch (:147-152), the subdivide→classify→keep→weld algebra (:170-336) correctly composing delaunay `Tessellation.Build`/`ToMesh`, `SpatialQuery.Winding` GWN, `SpatialQuery.Overlap` broad-phase, and `Kernels.WeldDuplicates`. The `CellComplex` retain-full-classification path and the `BooleanOp` regularized keep-predicate (:189-194) are sound. Composition-depth is genuinely award-grade.

Defects (register): E9 BooleanReceipt collision (:115); E5 Write-mutation vs immutable claim (:87-91,488); E7 rounded PlaneCrossPoint fed as explicit constraint (:258-262 vs :467); E9 soup ×3 (:352-365); E16 implicit-point/native-gate.

Beyond register:
- **PARTITION DEFECT — arrangement (wave 2) forward-depends on repair (wave 3).** arrangement composes from `Rasm.Geometry.Healing`: `Kernels.WeldDuplicates` (:16,334), `MeshEdit` (:333), `RepairPolicy` (:63 `ArrangementPolicy.Weld => RepairPolicy.Canonical with {...}`), the `DuplicateWeld` kernel (:4). [V5] re-homes `BooleanOp` INTO arrangement and [V6] extracts `MeshEdit` to the wave-1 substrate — but `Kernels.WeldDuplicates` and `RepairPolicy` are left in repair (wave 3), so the arrangement→repair edge PERSISTS after [V5]/[V6], merely renamed from the boolean edge to the weld edge. The [V5] cycle-break is INCOMPLETE unless the DECISION also moves the low-level weld primitive (`Kernels.WeldDuplicates` + its `RepairPolicy.WeldTolerance` knob) into the [V6] wave-1 MeshEdit arena — dedup-on-the-working-set IS an arena operation, not a healing-domain op. This is the sharpest structural finding on the lane; it strengthens both [V5] and [V6].
- **Dead implicit-point metadata.** `ImplicitCrossing.IsImplicit` (:391) is set true at :246 but NO consumer branches on it; the `ImplicitCrossing.Explicit` factory (:392) is never called. Aspirational dead carrier — the vestige of the intended implicit path [V4] must actually wire (as a `Constraint` implicit case carrying `Lpi` into `Tessellation.Build`, not a rounded `PlaneCrossPoint`).
- **AssetGated:false feeds the receipts.md Converged inversion.** `BooleanReceipt.Empty`/`Apply` mint `AssetGated:false` (:16,90,94,116). receipts.md:99 (E4d, out-of-scope) requires `AssetGated` true for `Converged` — so every successful managed boolean here reports non-converged downstream. The bug lives in receipts.md but arrangement is the input source; note the cross-page coupling.
- **Redundant native duplication.** `Apply` calls `a.DuplicateNative()`/`b.DuplicateNative()` (:155), then `Arrange`→`Triangles` re-duplicates (:353), then `ScorePatches`→`Soup` rebuilds from triangles (:316). Three native materializations per operand. Minor, but the [V6] soup-adapter collapse should also dedup the duplication.
- **SegmentOf drops middle crossings** (:234 takes `endpoints[0]`/`endpoints[^1]` only) — coplanar-overlap intersections lose interior structure; the "capability preserved" claim (:20) is weak for coplanar pairs. Low priority.
- **GWN is a CORRECT [V8] consumer, not a duplicate.** arrangement composes `SpatialQuery.Winding` (:444) from the index owner — this is the right side of the [V8] GWN-single-owner ruling (index owns BVH-accelerated winding; cf. reconstruct.md `SdfMeshMethod.GeneralizedWindingNumber` :77 which must ALSO compose index per [V8]). arrangement is not the E9 GWN duplicate — that is index-vs-reconstruct.

Charter-as-it-should-be: KEEP (composition spine) + REBUILD (implicit-point + partition). Land the [V5] `BooleanOp` home and resolve the `BooleanReceipt` collision to one type; make the store an explicit mutable arena (kill :488 immutable prose); replace `ImplicitCrossing`/`PlaneCrossPoint` rounding with the [V4] `Constraint` implicit case (Lpi into `Tessellation.Build`) — or honest re-scope README:42 + :467 in the same motion per the implicit-point probe; NAME Manifold behind the finite `ScaleCeiling` ([V5]); route soup through the [V6] substrate; and — critically — get the weld primitive (`Kernels.WeldDuplicates` + `RepairPolicy` knob) sited so no wave-2→wave-3 edge survives.

### offset.md — `Rasm.Geometry.Offsetting` — grade 5, fake medial + incomplete Minkowski

Real Aichholzer-Aurenhammer wavefront: `Seed`/`Propagate`/`Collapse`/`Divide`/`EnqueueAll` (:173-296) with exact `Orient2D` reflex/collapse gates and the `Weighted` per-edge `EdgeSpeed` modality as one row over the shared queue (:14,50,200). Genuine skeleton engine — but two headline projections are hollow.

Defects (register): E7 medial dual claim vs discard (:16,478 vs :325-326); E11 immutable-prose vs mutating store (:5 vs :13); E11 2n under-alloc (:195); E7 inline `SegmentsCross` (:440-444).

Beyond register:
- **`MedialFrom` discards the substrate — the medial axis is the skeleton trace, not the Voronoi dual.** :325-326 `Tessellation.Build(...).Map(_ => new MedialAxis(trace.Graph.Nodes, kept, bisectors))` — the CDT is built and thrown away; `MedialAxis` is assembled purely from the straight-skeleton arcs (`kept`/`bisectors` split at :319-322 by `Reflex`). The parabolic medial arcs at reflex vertices the prose promises (:478) never appear. The medial is a linear skeleton subset, exactly the "reflex-biased subset" :16 claims to transcend. Confirmed E7; the fix is blocked on delaunay's missing Voronoi-dual accessor (E16/[V10]c).
- **`MinkowskiConvolution` omits half the convolution — no vertex arc-fan, one direction only.** :349-360 emits ONLY ring-edge×element-vertex segments selected by a coarse half-plane test (`Orient2D(Origin, ToPoint(normal), element[j]) != Negative`). The Auto prose (:16 "each convex vertex its arc fan") and research (:479) claim the vertex arc-fan; the body has NONE. It also omits the symmetric ring-vertex×element-edge contribution — a full polygon Minkowski sum is edges-of-A×vertices-of-B AND edges-of-B×vertices-of-A. The convolution is under-specified and asymmetric — a capability gap the register's offset deltas do not list.
- **`PlanarOverlayResolve` self-defeats on the case it exists for.** :399-412 resolves a self-overlapping boundary by `PlanarMesh`→`Arrangement.Apply(PlanarOverlay, soup, soup, Union)`→`GetNakedEdges`. But `PlanarMesh` (:405-411) builds the soup via `Mesh.CreateFromClosedPolyline(loop)` per loop, which fails (returns null, silently skipped :409) on a self-intersecting polyline — precisely the input `PlanarOverlayResolve` is invoked to fix (`chained.Exists(SelfOverlaps)`, :364). The self-overlap resolver is fed a mesh built from the loops it cannot triangulate. Latent correctness gap.
- **Store-mutability contradiction is the sharpest on the lane** (:5 immutable vs :13 mutates vs `Spawn`/`Kill`/direct-array writes :79-88,232-233,251-253) — plus the 2n under-alloc (:195). The [SEAM_AND_RAIL_LAW] arena contract lands here.
- **`using Rasm.Domain`** (:29) for `Context` — verify against Domain (the tolerance type); benign but note the import.
- **Fence `signature` label is more honest than the :473 "transcription-complete" claim** — offset admits incompleteness in its label while claiming completeness in the density bar. The medial + Minkowski gaps prove the density-bar claim false.

Charter-as-it-should-be: KEEP (wavefront) + REBUILD (medial + Minkowski + arena). Make the medial REAL against delaunay's Voronoi-dual accessor (or re-scope :16,478 + README:99 per the implicit-point/[V10]c ruling); complete the Minkowski convolution (arc-fans + both directions) or re-scope the prose; carry the per-point CLEARANCE-RADIUS field + arbitrary-probe clearance query as first-class result fields ([V10]c/d mandate — a radius-less kernel medial is a [V10] defect); land the store-mutability + sizing contract (unbounded `Spawn` capacity); add the `JoinType`×`EndType` corner-strategy rows over the exact wavefront ([V10]a, CavalierContours stays Fabrication-lane); route `SegmentsCross` through `Intersection.Apply` ([V4]); fix `PlanarOverlayResolve` to feed the arrangement a valid soup.

### mesh.md / dec.md / reconstruct.md — landed substrate, STANDING LAW (context only)

Composed, never re-litigated. Member-verification anchors for the robust-core pages:
- `MeshSpace.Of(Mesh, Context, MeshAssemblyPolicy?=null, Op?=null) → Fin<MeshSpace>` (mesh:129) — delaunay:137 / arrangement:348 / offset:411 `MeshSpace.Of(mesh, tolerance)` VALID (defaults).
- `MeshSpace.DuplicateNative() → Mesh` (mesh:141) — arrangement:155,300,353 / offset:403 VALID. `MeshSpace.Tolerance : Context` (mesh:137) — arrangement:173,199 VALID. `.Native` is `internal` (mesh:139) — no robust-core page reaches it (correct).
- `TopologyReceipt` triple projection `(int Euler, int Genus, int BoundaryComponents)` (mesh:196-207) — the ONLY `ProjectionRow` today; the receipts.md manifold-fields extension lands one row here per [V9] (out-of-lane).
- `MeshKernel.TopologyDetailed` (mesh:539-556), `Cotangent.OfLengths/OfEdges` (mesh:393-403), `DecAssembly.Build → DiscreteCalculus` (dec:114-132), `SdfMeshMethod.GeneralizedWindingNumber` (reconstruct:77) — not composed by the Meshing robust-core pages; relevant only as the [V8] GWN-owner context (index owns BVH-winding; reconstruct + fields compose it, `SignedDistanceFromMesh` the ONE distance-field lane).
- **`MeshEdit` is NOT in mesh.md** — it lives in repair.md (`Rasm.Geometry.Healing`), confirmed by mesh:647 "settled `Processing/repair` owns `MeshEdit` — this page mints no edit surface". So `MeshEdit.OfMesh` (intersect:327), `new MeshEdit(Arr<Point3d>, Arr<(int,int,int)>, Set<int>, Set<int>)` (arrangement:333), `MeshEdit.Vertices`/`.Faces`, `Kernels.WeldDuplicates` are the [V6] EXTRACTION SURFACE — verified against repair.md (out-of-lane), flagged for the substrate page mint. These are the members the [V6] two-carrier seam (`MeshSpace` snapshot / `MeshEdit` arena) must publish at the wave-1 substrate.

No defects to report in the landed pages — they are the freshest `[GENERATOR_LAW]` exemplars (policy-record collapse, one-cotangent ownership, `IValidityEvidence` folds, `ConditionalWeakTable` cache semantics). The robust-core pages should ADOPT their substrate mechanisms (Op-threading, validity fold, admission vocabulary), which today they do NOT — every Meshing robust-core fence rides bare `Fin`+`GeometryFault` with hand-rolled `Validate` guards (delaunay:105-113, intersect:146-153, arrangement:155-164, offset:161-169), the dual-paradigm the substrate forbids (E5).

---

## [03] CROSS-CUTTING FINDINGS (lane-wide)

1. **The register understates intersect.** intersect (8.5) ships an E1-class connectivity failure (`Chain` emits disconnected 2-point segments, §[02]) that the register's deltas ("collapse landed; typed 2D restriction; slice-stack seam; host-delegation re-anchored") do not name. This is the single most consequential undiscovered defect on the lane — it silently breaks the Fabrication slice-stack, the documentation section HLR, and view.md's `Section`. Grade should drop to ~6.5 until the chain assembles real connectivity.

2. **Partition acyclicity is not yet satisfied.** arrangement (wave 2) composes `Kernels.WeldDuplicates` + `RepairPolicy` from repair (wave 3). [V5]/[V6] move `BooleanOp` and `MeshEdit` but leave the weld primitive dangling — the arrangement→repair edge persists, merely relocated. The DECISION MUST site the low-level weld (dedup-on-arena) in the wave-1 [V6] substrate, or the leg partition has a forward edge and [V5]'s "cycle break" is incomplete. Same class: intersect/arrangement soup via `Rasm.Geometry.Healing` resolves only if [V6] absorbs the soup adapter into wave 1.

3. **"transcription-complete" is a claim to verify, not accept.** intersect:382 (`contract`) claims the `[CHAIN]` cluster complete — false (no chaining). arrangement:491 (`contract`) claims exactness — overstated (rounded PlaneCrossPoint). offset:473 (`signature`) claims complete — false (medial discard, Minkowski half). Only delaunay (`signature`, :247) is honest. The `contract` label on intersect/arrangement should be withdrawn until the bodies match.

4. **Fault-cluster partition ([V1]/[V12]).** Concept-namespace↔cluster 1:1 holds for Intersection (`IntersectionFault` 2424, intersect:16,389), Arrangement (`DegenerateArrangement` 2420, arrangement:20,474), Offsetting (`DegenerateOffset`+`SkeletonStalled`, offset:15,164-183) — but FAILS for Tessellation (delaunay routes generic `DegenerateInput`/`UnrepairableMesh` only). delaunay's fault home is unruled. Stringly-vs-rich split on the routed cases: `SkeletonStalled(int,double)` (offset:181) is rich; `DegenerateOffset`/`DegenerateInput`/`IntersectionFault`/`DegenerateArrangement` are `string Detail` — [V12] re-typing targets (faults.md owns the type; the Meshing pages route).

5. **Cross-lane member ledger (verify against out-of-lane owners).** Numerics/predicates: `Predicate.OrientLPI/OrientTPI`, `Lpi.Of`, `Lpi.Numerator.X/.Y`, `Lpi.Denominator`, `Lpi.Lambda`, `Tpi`, `Expansion.Sum/TwoProduct/Difference/SignOf/ToFraction/Estimate` (delaunay:16, intersect:293-317, arrangement:18). Spatial/index ([V8]-rebuilt): `SpatialIndex.Build`, `SpatialQuery.Overlap/Range/Ray/Winding`, `QueryResult.Pairs/Hits/RayHit/Scalar`, `SpatialKind.Bvh`, `BuildPolicy.Canonical` (intersect:219-243, arrangement:209-444). External: `Fraction.Divide/CompareTo`, `(Fraction)double` (ExtendedNumerics.BigRational — arrangement:18,237-240, intersect:296). The `Lpi.Denominator`-vs-`Lpi.Lambda` dual-field-name (intersect:293) is the highest phantom risk.

6. **Store-mutability contract is a lane-wide motion.** Every robust-core SoA store (`SimplexStore` delaunay:56, `IntersectStore` intersect:67, `ArrangementStore` arrangement:73, `WavefrontStore` offset:65) is a mutable build arena; three of four mislabel themselves immutable (intersect is honest — its immutable claim :5 targets the value-record `Crossing`/`IntersectResult`, correctly). The [SEAM_AND_RAIL_LAW] arena/concurrency contract + content-addressing-binds-the-frozen-projection lands across all four in one motion, with offset's 2n sizing defect the concrete fix.

7. **Namespace partition ([V1]).** Four Meshing concept namespaces are folder-local (no cross-folder span — [V1] TIER-2 law satisfied). Phantom `Rasm.Geometry.Topology` confirmed ABSENT from Meshing (`rg` empty) — it is a Drawing/Processing concern (pack/solver). The four namespaces enumerate cleanly into `ARCHITECTURE.md:[03]`, replacing the `Rasm.Geometry.*` placeholder for the Meshing slice.
