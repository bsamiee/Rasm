# interaction — snippet region fragment

[INTERACTION — provisional; consolidated by the orchestrator]:
- IX-01 SURFACE_ROW_MOUNT: closed surface-row table as smart-enum rows carrying the backend builder-fold delegate, shutdown policy, frame class, and interactive gate — windowed and headless rows of one axis folded by one mount over `AppBuilder.Configure` + `StartWithClassicDesktopLifetime`. Distinct from RT-01 (host boot fold, not UI mount).
- IX-02 SCREEN_CATALOG_SPINE: one screen-catalog row consumed by router push, dock-role placement, and stack-manipulation session restore with unresolvable keys folded to the default row — the closed navigation-verb `[Union]` totally dispatched onto `RoutingState` command verbs (`Navigate`/`NavigateAndReset`/`NavigateBack`).
- IX-03 ACTIVATION_VALIDATED_VIEWSTATE: `ReactiveValidationObject` + `IActivatableViewModel` screen — OAPH knob triad (`initialValue`/`deferSubscription`), constructor-versus-`WhenActivated` subscription split, settled-outcome `ValidationState` rule projection, `IsValid()` published as an availability input, `ThrownExceptions` fact-stream edge, `Interaction` auto-answer override, disposal-owned validation lifetime. Distinct from VL-02 (screen projection of an already-bridged outcome; no severity partition or fault mint).
- IX-04 COMMAND_ROW_FOLD: the frozen command-intent row table — trigger-`[Union]`-parameterized `ReactiveCommand.CreateFromTask` construction with provenance-bearing receipts, one all-true `CombineLatest` availability fold per row over seeded inputs, gesture-column key-table projection with a composition-time conflict fault, and the merged command-as-`IObservable` receipt spine. Distinct from S-04/SD-07 (record row table + reactive command algebra, no generated-dispatch spotlight).
- IX-05 BINDING_EDGE_PROJECTION: the change-set-to-screen seam — projection-union mode dispatch over one source (flat transform versus `TransformToTree` pivot with an expansion-predicate stream onto one depth-bearing row contract), `DisposeMany` at the edge, one `ObserveOn` hop, `SortAndBind` with `SortAndBindOptions` knobs and a materialized-path comparer into one invariant bound collection. Distinct from CC-05 (binding seam only; resolution/refresh mechanics absent) and CC-04 (no conflation triple).
- IX-06 GATED_PRESENTATION: presentation gating — moment rows carrying present/hold/drop posture columns, the dialog-versus-notice severity split as one `[Union]`, `DialogHost` keyed awaited sessions with `UpdateContent` morph and closing veto, the queue cell with tuple-capture take-all and horizon-aged resume flush. Distinct from RT-04 (consumes a moment projection; no phase machine) and R-07 (queue cell is supporting material).
- IX-07 VARIANT_APPLICATION_FOLD: one idempotent theme-application fold (platform color values × pinned override × density × contrast preference) onto `RequestedThemeVariant`, a `FluentTheme.Palettes` row, and `DensityStyle` — explicit platform-variant conversion ingress, inheriting custom brand variant, `ThemeVariantScope` region row. Distinct from visuals' token algebra (application only, zero token resolution).
