# DOSSIER — corpus-b — libs/python/compute/.planning (SECOND HALF)

Scope deep-read: `numerics/` (array, interval, jit, quantity, statistics) + `optimization/` (convex, design, program) + `solvers/` (receipt, linear, nonlinear, differential, sensitivity, quadrature, mesh, field) = 16 pages. Seam skim (first half): `graduation/handoff.md` (the graduation pivot every in-scope evidence owner feeds). Subfolders sorted: analysis, experiments, graduation | numerics, optimization, solvers — second half is numerics/optimization/solvers.

Baseline: compute is the FOURTH/LAST py folder (runtime->data->geometry->compute), DOWNSTREAM + UNBRIEFED. The corpus is page-level EXCEPTIONAL — every second-half page grades 9–9.5 on a hostile read; the folder's craft (table-driven dispatch, ADT collapse, JAX x64 discipline, single-mint FIT, receipt/status unification) is the strongest of the four py folders. The rot is NOT page-level naivety; it is UPSTREAM-MIGRATION debt (identity rename, geometry-subject re-home), SEAM inconsistency (graduation wired 3 ways, one dead owner, one self-contradicting policy rule), and RAIL divergence from the sibling folders (FrozenDict vs Map, RESEARCH appendix). The internal import DAG is ACYCLIC (the one structural win over runtime/data). Graph/seam first; per-page cold passes fix nothing structural.

---

## [FIT + MIGRATION LEDGER] — upstream consumer pressures (waterfall)

Compute CONSUMES, decode-only at the seam:
- runtime: `ContentIdentity`/`identity`, `faults` (`boundary`/`railed`/`traversed`/`BoundaryFault`/`FAULT_CONF`), `receipts` (`Receipt`/`ReceiptContributor`/`Redaction`/`receipted`/`Signals`), `CANONICAL_POLICY`, `IdentitySource.lift` STREAM modality (mesh/field/array key `Iterable[bytes]`), `IdentityPolicy`.
- data: `xarray`/`dask` labelled-array study-input shapes (array.md, experiments/study), `sparse` (retagged INTO compute).
- geometry: the `GeometryHandoff`/`GeometrySubject` graduation carrier (the re-home pivot).

Compute PRODUCES: graduation evidence to C# `Rasm.Compute` (`* ⇄ csharp:Rasm.Compute`), `SolverReceipt` projection (`solvers/receipt → csharp`), `analysis/spatial` alpha-shape boundary to `geometry/scan`.

Named migration pressures the upstream briefs impose on this folder:
- **M1 [runtime V4]** identity rename `rasm.runtime.content_identity` -> `rasm.runtime.identity`: **21 import lines across ALL 20 pages, ZERO already-correct**. Folder is unimportable the moment runtime lands. Verified: `rg 'rasm.runtime.content_identity'` = 21.
- **M2 [geometry V2]** `GeometrySubject` RE-HOMES to `rasm.geometry.graduation`; all `from rasm.compute.graduation.handoff` imports in geometry die; the crossing becomes DATA (geometry mints `GeometryHandoff`, compute DECODES). Compute `handoff.md:61-64` STILL OWNS `GeometrySubject` as the source-of-truth `Literal[...]` with 8 literals (`registration-transform`/`reconstructed-mesh`/`topology-graph`/`network-graph`/`form-finding`/`numerical-primitive`/`mesh-algebra`/`scan-deviation`) — geometry's minted union differs (adds `bim-compliance`/`bim-lifecycle`/`section-property`/`building-energy`/`thermal-comfort` per geometry `[V2]`). DUAL-MINT + literal-set mismatch. See VC-2.
- **M3 [data E12]** `sparse` `[DATA]`->`[COMPUTE]` retag — compute IS the named live consumer (`array.md:33` `import sparse`, `ArraySource.Sparsify`/`SparseFrom`, `SparseFacts`). Confirm manifest tag + `compute/.api/sparse.md` owner line.
- **M4 [data E12]** `netcdf4` retagged AWAY from compute (zero compute consumers) — compute must not claim it.
- **M5 [data E12]** `xarray` STAYS branch-tier because compute co-consumes `DataTree` (`experiments/inference.md:45`, first half) — owner line names both consumers.
- **M6 [data V12/E12]** `dask` catalog relocates to branch-tier `libs/python/.api/dask.md`; superseded `compute/.api/dask.md` removal is a RIPPLE this campaign lands.
- **M7 [component AMENDMENTS]** count-prefixed canonical-bytes law consumed TRANSITIVELY through the runtime `ContentIdentity` writer; compute feeds canonical bytes, authors NO canonical-writer mirror (`PY_WIRE_ALIGNMENT` is its own campaign, compute downstream). Verified compliant: every content key rides `ContentIdentity.of` over `ascontiguousarray(...).tobytes()` buffers; no page hand-rolls a digest.

WATERFALL-RIPPLE candidates (compute demands upstream lacks) — edit the named upstream brief:
- **W1 -> RASM-GEOMETRY-BRIEF.md [V2]**: the `GeometryHandoff` frozen-contract clause must pin the FULL subject-literal SET compute's `HandoffAxis.geometry` decodes AND state the production direction for compute's `analysis/spatial` boundary evidence (`ARCH:52 analysis/spatial → python:geometry/scan`) — geometry [V2] currently names compute as decoding consumer only, silent on compute-as-producer of the `scan-deviation`/`reconstructed-mesh` boundary literal. Name compute `analysis/spatial.md#SPATIAL` + `graduation/handoff.md#GRADUATION` as the demanding consumers.
- **W2 -> RASM-RUNTIME-BRIEF.md [V7]**: `IdentitySource.lift` STREAM modality is pinned with data/geometry named; compute (`mesh.md`/`field.md`/`array.md`) is an additional STREAM-modality consumer (mesh-field/resample/array buffers). Add compute to the [V7] named-consumer list (an owner-extension clause, not a rewrite).

---

## [PER-PAGE DOSSIER]

### solvers/receipt.md — VERDICT 9.5
Charter (as-is, correct): the ONE method-discriminated `SolverReceipt` `@tagged_union` (`direct`/`iterative`/`least_squares`/`eigen`), the `SolveStatus` termination `StrEnum` every backend folds into, `_SLOTS`/`_STATUS`/`_TOL`/`_CONVERGENT` seed tables, total `.status`/`.facts` folds via `zip(_SLOTS[tag], payload, strict=True)`.
Defects: (a) `receipt.md:28,65,71,82` tables are `beartype.FrozenDict` — rail divergence (VC-5). (b) `_status` is underscore-private yet cross-imported by mesh/field/quadrature/design (VC-10). (c) `[03]-[RESEARCH]` tail `:179` (VC-6). No identity import (clean on M1). Owner charter SHOULD stay: the single termination vocabulary + slot-projection mechanism; it should additionally HOST the shared enum-verdict fold (VC-8) since it already owns `_STATUS` the verdict feeds.

### solvers/linear.md — VERDICT 9
Charter: `LinearIntent` (dense/sparse/eigen/operator) over one `LinearMap` operand + `MatrixStructure`/`SolveShape`/`SpectralMode`/`LinearPolicy` axes; `LinearEngine` gated jax/lineax carrier; scipy Krylov+lineax typed non-convergence.
Defects: (a) `linear.md:185` `LinearMap.structure` uses reflective `getattr(self, self.tag)[-1]` — the EXACT pattern receipt.md's own doctrine names a DELETED form for `.status`/`.facts` ("reflective `getattr(self,self.tag)` whose `object` residual makes `assert_never` a lie"). Internal doctrine contradiction; `structure` should be a total `match`. (b) `LinearEngine.verdict:316-329` is the duplicate enum-inversion mechanism (VC-8). (c) `content_identity`? No — linear imports only receipt+faults+receipts (clean on M1 directly, but transitively depends). (d) FrozenDict `_ISTOP`/`_TAG_NAMES`. (e) `[03]-[RESEARCH]:566`.

### solvers/nonlinear.md — VERDICT 9.5
Charter: `NonlinearIntent` (root/minimise/fixed_point/least_squares) over optimistix; `_SOLVER`/`_INNER`/`_BEST`/`_route_cells` seed tables; `SolverProfile`-keyed `build_solver`; 5-axis `SolverPolicy`; `NonlinearEngine` 7-module gated carrier.
Defects: (a) `nonlinear.md:5` prose asserts "Loop-kernel and XLA acceleration is owned by `numerics/jit.md#JIT`" — the ONLY corpus reference to jit, and it is a boundary-disclaimer, not a consume: jit is UNWIRED (VC-3). (b) `NonlinearEngine.verdict:222-233` duplicate mechanism (VC-8). (c) FrozenDict tables. (d) `[03]-[RESEARCH]:449`.

### solvers/differential.md — VERDICT 9.5
Charter: `DifferentialIntent` (ode/sde/cde) over diffrax; `_SOLVER`/`_LEVY`/`_LANGEVIN`/`_ADJOINT`/`_EVENT` tables; `SolveEngine` gated carrier; `_forced_pid` adaptivity gate; underdamped-Langevin term-pair.
Defects: (a) `SolveEngine.verdict:254-265` duplicate mechanism — prose EXPLICITLY cross-references "the same reduction `solvers/nonlinear.md#NONLINEAR`/`solvers/differential.md#DIFFERENTIAL` run" and "`optimization/design.md#DESIGN` `_result_names` inversion" (VC-8). (b) FrozenDict `_LEVY`/`_SOLVER`/`_ADJOINT`/`_EVENT`. (c) `[03]-[RESEARCH]:498`.

### solvers/sensitivity.md — VERDICT 9.5
Charter: `Differentiation` (8 AD modes) over `_SPEC` `(DiffTarget,DiffModeTag)` table + `_PRODUCT`; collapses a former `_TRANSFORM`/`_DIRECTIONAL`/`_KEYWORDS` triple into one `DiffSpec` (exemplary collapse); `DiffEngine` gated carrier; findiff floor.
Defects: (a) FrozenDict `_SPEC`/`_PRODUCT`. (b) `[03]-[RESEARCH]:491`. Correctly AVOIDS the verdict pattern (autodiff, not iterative solve) and keeps `experiments/study` DGSM DISJOINT ([DGSM_SEAM]) — a clean boundary to preserve. `content_identity`? imports only faults+receipts (clean). Strong.

### solvers/quadrature.md — VERDICT 9.5
Charter: `QuadratureIntent` (integrate/interpolate/fem) over quadax/interpax/scipy/numpy ladder; `_QUAD`/`_INTERP` single-row catalogs replacing prior triple tables (exemplary); FEM `condense->solve` consuming mesh's `AssembledSystem`; `ElementKind`/`FemForm` declared here, imported by mesh/field.
Defects: (a) `quadrature.md:559-560` `[03]-[RESEARCH]` header is EMPTY — the empty-appendix form runtime/data/geometry briefs delete-blind (VC-6). (b) FrozenDict `_QUAD`/`_INTERP`. (c) imports `_sparse_receipt`/`_status`-family privately from linear/receipt (VC-10). Clean catalog collapse; strong.

### solvers/mesh.md — VERDICT 9.5
Charter: `MeshField` topology value-object + `MeshExchange` (assemble/read/write) over skfem+meshio; `_CTOR` `(Mesh*,Element*,cell-type)` triple; `MeshReceipt` `_SLOTS`-driven; `@railed` `_dispatch` + `_field` content-key mint.
Defects: (a) **M1: `mesh.md:50` `from rasm.runtime.content_identity import ContentIdentity, ContentKey, IdentityPolicy`** — old spelling. (b) **`mesh.md:285` passes explicit `IdentityPolicy()` to `ContentIdentity.of`** — the DELETED form the CANONICAL_POLICY-pages' prose names (VC-7). (c) imports private `_status`/`SolveStatus` from receipt (VC-10). (d) FrozenDict `_CTOR`/`_TOL`/`_SLOTS`. (e) `[04]-[RESEARCH]:371`.

### solvers/field.md — VERDICT 9.5
Charter: `FieldQuery` (interpolate/project/resample) over skfem+interpax; imports `_CTOR`/`MeshField` from mesh, `ElementKind` from quadrature; `basis.project` physical-point residual; `FieldEngine` gated resample; `FieldReceipt` `_SLOTS`-driven.
Defects: (a) **M1: `field.md:42` `content_identity` import**. (b) **`field.md:345` `ContentIdentity.of(label, buffers, IdentityPolicy())`** — explicit-alloc deleted form (VC-7). (c) private `_status`/`_CTOR` cross-imports (VC-10). (d) FrozenDict. (e) `[03]-[RESEARCH]:349`. `_dispatch` rail-join shape is exemplary (matches array/mesh).

### numerics/array.md — VERDICT 9.5–10 (the wire seam; masterpiece)
Charter: backend-agnostic Array-API admission; `ArrayPayload.admit(source, axes, finite, mode, bound)` over `ArraySource`(input)×`AdmitMode`(output) orthogonal axes; `FiniteGate` lazy/eager fork; `SparseLayout`/`DenseBound`/`SparseFacts`; `array-api-compat` resolver + `array-api-extra` extension stack; `_host_buffer` backend-residence fold; railed `ContentIdentity`.
Defects: (a) **M1: `array.md:38` `content_identity` import** (rests on `CANONICAL_POLICY` default correctly — array.md:23 prose names `IdentityPolicy()` a DELETED form). (b) **SEAM MISFILE: `ARCH:51 numerics/array ⇄ python:runtime/transport [WIRE]: ContentIdentity...`** — array.md consumes `ContentIdentity` from `runtime/evidence/identity`, NOT `transport`; the row names the wrong owner + wrong direction (`⇄` — array decodes, never re-mints). (c) M3: array.md IS the `sparse` consumer justifying the `[COMPUTE]` retag. (d) FrozenDict absent here (uses tagged_union+StrEnum). (e) `[03]-[RESEARCH]:302`. World-class; the folder's spine every analysis/* + optimization/* + experiments/* page composes.

### numerics/interval.md — VERDICT 9.5
Charter: `IntervalNumerics.run` over Arb->mpmath->numpy `_FLOOR_LADDER`; `Interval` inclusion-monotone value-object algebra; `Enclosure`/`Floor`/`Certificate`; `IntervalOp` with `Yield` union output; tailrec `_bisect`; flint `real_roots` certified isolation.
Defects: (a) **M1: `interval.md:33` imports `CANONICAL_POLICY, ContentIdentity, ContentKey` from `content_identity`** (relies on default — consistent with the CANONICAL_POLICY camp). (b) FrozenDict `_FLOOR_LADDER`. (c) `[03]-[RESEARCH]:440`. Graduates on `solver` axis (Arb/Mpmath proof, Numpy advisory) but consumer-driven — no `graduates()`, no handoff import (VC-4).

### numerics/quantity.md — VERDICT 9.5
Charter: `UncertainQuantity` threading `uncertainties.UFloat` through `pint.Measurement`; `Magnitude`/`Covariance`/`Propagation`/`CohortView` policies; `Umath` SmartEnum-with-arity; one shared `_UREG` application registry; `QuantityReceipt.graduates` on `unit_law`.
Defects: (a) **M1: `quantity.md:51` `content_identity`** (CANONICAL_POLICY camp). (b) **SELF-WIRES `graduates()` (`:294`) + imports `GraduationReceipt, HandoffAxis` (`:50`)** — the self-wired graduation pattern (VC-4 inconsistency partner). (c) `[MONTE_CARLO_BRIDGE]:518` names `mcerp`/`soerp` as UNADMITTED candidate packages — package-pressure note, not defect. (d) FrozenDict minimal (2). (e) `[03]-[RESEARCH]:509`.

### numerics/statistics.md — VERDICT 9.5
Charter: `TestIntent` (KS/AD/Shapiro/MannWhitney/Fit) over scipy.stats; `_STAT_ROUTES`/`_SIGNIFICANCE_CALLS` two-level table collapse; `Decision`/`Verdict`/`Distribution`/`Goodness`/`Alternative` vocabularies; NO numpy floor (scipy IS the test).
Defects: (a) **M1: `statistics.md:34` `content_identity`** (CANONICAL_POLICY camp; `:357` prose names `IdentityPolicy()` a deleted form). (b) FrozenDict `_STAT_ROUTES`/`_SIGNIFICANCE_CALLS`. (c) `[03]-[RESEARCH]:352`. CORRECTLY does NOT graduate (frequentist reject/retain is not a HandoffAxis case; `:16,358` state the boundary) — a clean, deliberate boundary to preserve.

### numerics/jit.md — VERDICT 8 (structurally UNWIRED)
Charter: `JitBackend` collapsing numba njit/vectorize + jax jit over `_JIT_ROUTES`; `JitEvidence` discriminated lowered-IR carrier; `Specimen` warm-probe; `Jitted` receipt.
Defects: (a) **UNWIRED OWNER — zero fences import `rasm.compute.numerics.jit` or compose `JitBackend.compile`.** Verified: `rg 'numerics.jit|JitBackend|\.compile\('` returns only jit.md itself + nonlinear.md:5 prose. The receipt prose ("the consuming solve/study spine harvests the one `contribute` stream where it composes the `Jitted`") describes a composition that exists in NO fence (VC-3). (b) **M1: `jit.md:30` `content_identity`** (CANONICAL_POLICY camp; `:289` names `IdentityPolicy()` deleted). (c) FrozenDict `_JIT_ROUTES`. (d) `[03]-[RESEARCH]:286`. The page is well-built; the defect is placement/wiring, not craft.

### optimization/convex.md — VERDICT 9.5–10 (deep KKT mathematics)
Charter: `ConvexProgram` (linear/quadratic/soc/exp/sdp) over cvxpy+Clarabel; shared `_assemble` + `_CONE_ROWS` table; `ConvexEvidence` KKT-residual object; per-cone `_CONE_KKT` slack/dual-feasibility/primal-feasibility closures; DPP `Parameter` warm-re-solve sweep; distinct `ConvexReceipt` (never folds into `OutcomeReceipt`).
Defects: (a) **PROSE-VS-FENCE GAP: convex.md prose (`:7,19`) claims `convex_program` graduation, but the fence does NOT import `graduation.handoff` and shows NO `graduates()` method** (unlike quantity/inference/model). The `convex_program` `HandoffAxis` case (handoff.md:74,83) has NO self-wiring producer — consumer-driven-but-no-consumer-shown (VC-4). (b) **M1: `convex.md:37` `content_identity`** (CANONICAL_POLICY camp — relies on default per `:20`). (c) imports `SolveStatus` from receipt (good coherence). (d) FrozenDict `_CONVEX_STATUS`/`_CONE_ROWS`/`_CONE_KKT`/`_SENSE`. (e) `[03]-[RESEARCH]:514`. The per-cone KKT engineering is world-class; preserve.

### optimization/design.md — VERDICT 9.5
Charter: `DesignProblem` (field/mesh/density) inverse design over Optimistix/Equinox/optax; `Descent` engine-as-policy + `_DEFAULT_DESCENT`; `Feasible` projection chain via `_feasible()`; implicit-adjoint; multi-start ensemble; OWNS the shared `OutcomeReceipt` (design/program cases) + `_OUTCOME_SLOTS`.
Defects: (a) **M1: `design.md:42` `content_identity`**. (b) **`design.md:520` `ContentIdentity.of(..., IdentityPolicy())`** — explicit-alloc deleted form (VC-7). (c) `_result_names():306-316` is the 4th duplicate enum-inversion (VC-8). (d) imports private `_status` from receipt (VC-10). (e) CONSUMER-DRIVEN graduation ("a consumer drives graduation through `GraduationReceipt.graduates`", `:25`) — no `graduates()`, no handoff import (VC-4). (f) FrozenDict. (g) `[03]-[RESEARCH]:567`.

### optimization/program.md — VERDICT 9.5
Charter: `ProgramIntent` (linear/integer/global/constrained/assignment) over scipy.optimize; `GlobalMethod` engine-as-policy (DE/annealing/simplicial/direct); `Termination` adjudicator; `_PROGRAM_ROUTES`/`_project`; imports `OutcomeReceipt` from design; NO numpy floor.
Defects: (a) **M1: `program.md:40` `content_identity`**. (b) **`program.md:251,520` `ContentIdentity.of(..., IdentityPolicy())`** — explicit-alloc deleted form (VC-7). (c) CONSUMER-DRIVEN graduation (`:24`) — no `graduates()`, no handoff import (VC-4). (d) imports `SolveStatus` from receipt. (e) FrozenDict `_PROGRAM_ROUTES`/`_PROGRAM_STATUS`. (f) `[03]-[RESEARCH]:398`.

### graduation/handoff.md (first-half seam skim) — the graduation pivot
`HandoffAxis` `@tagged_union` (8 cases: solver/symbolic/model_asset/array_layout/unit_law/uncertainty_law/geometry/convex_program). `GraduationReceipt.graduates` = the one admission gate (`_admit` refinement -> `_clear` ceiling fold -> fenced `_emit`). **OWNS `GeometrySubject` (`:61-64`) — the M2 re-home conflict.** M1: `:56` `content_identity` import. Feeds from in-scope owners: `solver` (receipt/design/program/interval), `convex_program` (convex — UNWIRED), `array_layout` (array — QUEUED card, unbuilt), `unit_law` (quantity — self-wired). `[03]-[RESEARCH]:208`. Corpus-a owns the deep handoff analysis; flagged here as the seam every second-half evidence owner crosses.

---

## [CROSS-CUTTING]

**Duplication / duplicate mechanism.** (1) The enum-verdict fold — `<mod>.RESULTS._name_to_item` inversion + batched `jnp.max` code-reduce — is hand-spelled 4x: `LinearEngine.verdict` (linear:316), `NonlinearEngine.verdict` (nonlinear:222), `SolveEngine.verdict` (differential:254), `_result_names` (design:306). 21 `_name_to_item` occurrences. All read `equinox.Enumeration` subclasses (lx/optx/dfx RESULTS) — the pages' prose EXPLICITLY cross-acknowledge the sameness. Parameterized generator belongs (VC-8). (2) The `_SLOTS`-driven receipt fold (`.facts`/`.status`/`.converged`/`contribute` via `zip(_SLOTS[tag], payload, strict=True)`) is replicated across `SolverReceipt`/`MeshReceipt`/`FieldReceipt` — near-verbatim, differing only in owner string + rows. Medium (tagged_union composition constrains extraction; a shared projection helper on the receipt owner is the collapse).

**Concern mixing / rail bifurcation.** `beartype.FrozenDict` is the uniform lookup/dispatch-table rail folder-wide (77 occurrences, every page), while `expression.Map` appears almost only as `Map.empty()` for `Redaction(classified=...)`. Sibling folders were forced onto `expression.Map` for the SAME table role (runtime [V8] `SLOTS`/`FAULT_OUTCOME`->Map; data [V2] `Final[frozendict]`->`Final[Map]`). Divergence, not a builtins-frozendict bug (VC-5).

**Hardcoding-vs-generator.** Rosters are correctly seed DATA over generative shapes everywhere (the folder's strength): `_STATUS`/`_QUAD`/`_INTERP`/`_SOLVER`/`_INNER`/`_LEVY`/`_JIT_ROUTES`/`_STAT_ROUTES`/`_CONE_KKT`/`_PROGRAM_ROUTES`/`_DEFAULT_DESCENT`/`Umath`. No enumerated-instances-where-generator-belongs defect at page level. The APPROACH-naivety is the verdict fold (VC-8), not the tables.

**Dead / unwired carriers.** (1) `numerics/jit.md` — entire 231-LOC owner, zero importing fences (VC-3). (2) `convex_program` `HandoffAxis` case — declared, no self-wiring producer (VC-4). (3) `array_layout` axis — explicitly QUEUED/unbuilt (`TASKLOG ARRAY_LAYOUT_GRADUATION`). (4) `GeometrySubject` (handoff) — stale dual-mint (M2/VC-2).

**Unwired / inconsistent seams.** (1) Graduation rail fed 3 ways: self-wired `graduates()` (quantity/inference/model — 3 pages import handoff), consumer-driven no-`graduates()` (design/program/convex/receipt/interval), unbuilt (array_layout). Verified: only inference/model/quantity have `def graduates(` + handoff import (VC-4). (2) `ARCH:51` seam misfiles `numerics/array ⇄ runtime/transport` (should be `← runtime/evidence`, ContentIdentity). (3) `ARCH:50 graduation/handoff ⇄ python:geometry/graph` bidirectional must become `←` under geometry [V2] inversion (VC-2).

**Self-contradicting rule.** `ContentIdentity.of(..., IdentityPolicy())` explicit-alloc in design:520/program:251/field:345/mesh:285 — the EXACT "deleted form" array:23 / quantity:516 / statistics:357 / jit:289 prose names ("a fresh `IdentityPolicy()` allocation where the `of` `CANONICAL_POLICY` default already keys the canonical path"). 10 pages rely on the default, 4 violate it (VC-7).

**Page grammar.** Every page carries a `[03]-[RESEARCH]` (or `[04]-[RESEARCH]`) appendix — quadrature:559 is EMPTY. Exactly the form runtime [V11]/data [V11]/geometry [V10] BANNED folder-wide (purge-and-fold: load-bearing member confirmations into `Packages`/`.api`, framing/empty deleted). Compute, as the unbriefed downstream, has not had the purge (VC-6).

**Unmined capability (catalog anchors).** Package roster is deeply mined (each page's `Packages` block cites real `.api` catalog members). Genuine gaps: `mcerp`/`soerp` (quantity Monte-Carlo bridge, unadmitted) — deferred forward-pool, not a defect. `blackjax` admitted (README [93]) but inference [BLACKJAX_RAW_ALGEBRA] DROPPED it as out-of-charter — confirm the admission still earns its manifest row or strike. `dask` admitted (README [86]) but compute's dask CONSUME is `experiments/study` (first-half) + the `[SHAPE]` seam — the `compute/.api/dask.md` relocation (M6) needs the branch-tier owner-line naming both consumers.

**Sound surfaces (preserve; cold-pass + only verdict deltas).** Internal import DAG ACYCLIC (numerics/array base -> analysis/*; receipt -> linear -> {quadrature,nonlinear,differential,sensitivity}, mesh/field -> quadrature; graduation base; design -> program; quantity -> handoff) — the one structural win over runtime/data, verified by the clean `from rasm.compute` census. Runtime FIT single-mint/decode-only (no re-minted content key, no canonical-writer mirror — M7 compliant). Unified `SolveStatus` termination vocabulary + `_SLOTS` receipt discipline. JAX x64 float64-promotion discipline consistent across every gated carrier (`LinearEngine`/`NonlinearEngine`/`SolveEngine`/`DiffEngine`/`QuadEngine`/`FieldEngine`). Exemplary collapses: sensitivity's `DiffSpec` (triple-table->one), quadrature's `_QUAD`/`_INTERP` (triple->one), convex's `_CONE_KKT`, statistics' two-level `_STAT_ROUTES`/`_SIGNIFICANCE_CALLS`. Deliberate clean boundaries: statistics no-graduation, sensitivity/study DGSM disjoint-rail.

---

## [VERDICT CANDIDATES] (campaign-defining structural rulings, most-severe first)

**VC-1 — UPSTREAM IDENTITY REBIND (primary mechanical migration).** `rasm.runtime.content_identity` -> `rasm.runtime.identity` at all 21 sites across all 20 pages (verified `rg` count). Compute is unimportable the moment runtime [V4] lands; ZERO pages already correct. Land in the leg-1 reconcile so later legs inherit clean spellings. Evidence: `rg 'rasm.runtime.content_identity libs/python/compute/.planning'` = 21; `rasm.runtime.identity` = 0.

**VC-2 — GRADUATION-SEAM INVERSION + GEOMETRY-SUBJECT RE-HOME (the campaign's defining structural ruling).** geometry [V2] mints `GeometrySubject` + the `GeometryHandoff` carrier in `rasm.geometry.graduation`; compute `handoff.md:61-64` still OWNS a stale `GeometrySubject` `Literal` whose 8 literals mismatch geometry's minted union (geometry adds bim-compliance/bim-lifecycle/section-property/building-energy/thermal-comfort). Ruled default: compute DECODES geometry's carrier (compute's `HandoffAxis.geometry` case reads the geometry-minted subject as wire DATA), deletes its source-of-truth `GeometrySubject` mint, and re-rules the production direction of `analysis/spatial → geometry/scan` (ARCH:52) + the consumption `graduation ← geometry` (ARCH:49). `ARCH:50 graduation/handoff ⇄ python:geometry/graph` becomes `←` (decode-only; no compute->geometry import). Triggers W1 upstream edit. Evidence: handoff.md:5,20,21,61-64,206; geometry-brief [V2]/[V11]; ARCH:49,50,52.

**VC-3 — jit.md IS AN UNWIRED OWNER.** `numerics/jit.md` (231 LOC) has ZERO importing fences; the sole reference is nonlinear.md:5 prose ("acceleration is owned by numerics/jit.md#JIT") — a boundary-disclaimer, not a consume. jit's receipt prose describes a "solve/study spine" composition present in no fence. Rule: WIRE `JitBackend.compile` into the study/solver kernel-compile path the solvers' prose gestures at (numba njit loop-kernels + jax XLA study kernels), OR rule its charter down. Evidence: `rg 'numerics.jit|JitBackend|\.compile\('` (excl. jit.md) = 0 imports; jit.md:14 receipt prose; nonlinear.md:5.

**VC-4 — UNIFY THE GRADUATION-WIRING PATTERN.** The 8-case handoff rail is fed 3 incompatible ways: SELF-WIRED `graduates()` + handoff import (quantity/inference/model — unit_law/uncertainty_law/model_asset), CONSUMER-DRIVEN no-`graduates()` (design/program/convex/receipt/interval — solver/convex_program), UNBUILT (array_layout QUEUED). `convex_program` has NO realized producer (convex.md prose-claims it, no handoff import, no `graduates()`). Rule ONE pattern: either every evidence owner self-wires `graduates()` (uniform, closes the convex_program/array_layout gaps), or all defer to a single consumer spine (then the spine must be a real fence). Evidence: `rg 'def graduates\('` = inference/model/quantity/handoff only; `import GraduationReceipt` = inference/model/quantity only; convex.md:7,19 vs its import block :36-39.

**VC-5 — DISPATCH-TABLE RAIL (FrozenDict vs expression.Map).** `beartype.FrozenDict` is the uniform module-level dispatch/lookup-table rail folder-wide (77 occurrences), diverging from the sibling `expression.Map`-for-tables precedent (runtime [V8], data [V2] which re-typed every `Final[frozendict]`->`Final[Map]`). `expression.Map` in compute appears almost only as `Map.empty()` for redaction. Rule the corpus rail: ruled default = compute joins the `Map` convention its siblings hold (one persistent-collection spine); counter-argument to weigh = `FrozenDict` is hashable + beartype-native + lookup-only (Map's functional-fold API is unused). This is a ruling, not a bug (compute's tables work). Evidence: `rg -c FrozenDict` across 15 pages; `Map.empty` almost sole Map usage.

**VC-6 — [RESEARCH]-APPENDIX PURGE-AND-FOLD.** Every page carries a `[03]/[04]-[RESEARCH]` appendix (quadrature:559 EMPTY) — the exact form runtime [V11]/data [V11]/geometry [V10] BANNED folder-wide. Purge-and-fold per sibling precedent: load-bearing member/arity confirmations fold into the owning `Packages` block or `.api` catalog; "confirmed against the live distribution" framing, version narration, and empty headers delete; lands per-leg. Largest clean prose reduction in the folder. Evidence: `[03]-[RESEARCH]` on all 16 second-half pages; quadrature.md:559-560 empty.

**VC-7 — CANONICAL_POLICY SELF-CONTRADICTION.** design:520 / program:251 / field:345 / mesh:285 pass explicit `IdentityPolicy()` to `ContentIdentity.of` — the EXACT "deleted form" array:23 / quantity:516 / statistics:357 / jit:289 prose names ("a fresh `IdentityPolicy()` allocation where the `of` `CANONICAL_POLICY` default already keys the canonical path"). Reconcile the 4 violators to the `CANONICAL_POLICY` default the 10-page majority holds. Evidence: `rg 'ContentIdentity.of\([^)]*IdentityPolicy\(\)'` = design/program/field/mesh; `CANONICAL_POLICY` import = array/interval/jit/quantity/statistics/convex + 4 experiments.

**VC-8 — COLLAPSE THE DUPLICATE ENUM-VERDICT MECHANISM.** The `<mod>.RESULTS._name_to_item` inversion + batched `jnp.max` code-reduce is hand-spelled 4x (`LinearEngine.verdict` linear:316, `NonlinearEngine.verdict` nonlinear:222, `SolveEngine.verdict` differential:254, `_result_names` design:306), differing ONLY by gated module — all `equinox.Enumeration` subclasses. The pages' prose explicitly cross-acknowledge the sameness. Parameterize into ONE shared fold parameterized by the RESULTS class, hosted on the receipt owner (which already owns `_STATUS` the verdict name feeds into). Evidence: `rg '_name_to_item'` = 21 occurrences across linear/nonlinear/differential/design.

**VC-9 — MANIFEST + SEAM-LEDGER RECONCILIATION (data-brief ripples land here).** (a) `sparse` `[DATA]`->`[COMPUTE]` — array.md IS the consumer (M3); confirm manifest tag + `compute/.api/sparse.md` owner line. (b) `netcdf4` retagged AWAY from compute (M4). (c) `dask` catalog relocates branch-tier; `compute/.api/dask.md` removal is this campaign's ripple (M6); (d) `ARCH:51` seam misfile `numerics/array ⇄ runtime/transport` -> `← runtime/evidence` (ContentIdentity is evidence/identity, decode-only, not transport, not `⇄`). (e) `blackjax` admitted but inference DROPPED its raw-kernel charter — confirm the row earns its place. Evidence: data-brief E12; array.md:38 imports; ARCH:51; TASKLOG BLACKJAX_RAW_ALGEBRA DROPPED.

**VC-10 — PUBLIC-vs-PRIVATE `_status` CONTRACT.** `_status`/`_sparse_receipt`/`_CTOR` are underscore-private yet cross-imported across modules (mesh/field/quadrature/design import `_status` from receipt; quadrature imports `_sparse_receipt` from linear; field imports `_CTOR` from mesh). A public shared contract masquerading as private. Rule: promote the genuinely-shared termination-floor + element-triple to public surface, or fold the cross-module reuse into the owning entry. Minor; can ride VC-8's receipt-owner consolidation. Evidence: mesh.md:49, field.md:41, quadrature.md:52-53, design.md:41.
