# [APPHOST_TASKLOG]

The open and closed work for the runtime spine, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup.

## [1]-[OPEN]

## [2]-[CLOSED]

[T-MCP-LONG-RUNNING-CATALOG] [COMPLETE] -- `.api/api-mcp.md` carries the MCP server long-running surface at its true declaring types: the `McpServerOptions` property scope adds `TaskStore` [9] (`IMcpTaskStore?`) + `SendTaskStatusNotifications` [10] (`bool`), the new `McpServer` session long-running verb entrypoint table holds `SampleAsync`/`AsSamplingChatClient`/`ElicitAsync`/`ElicitAsync<T>`/`GetTaskAsync`/`GetTaskResultAsync<T>`/`WaitForTaskResultAsync<T>`/`PollTaskUntilCompleteAsync`, the request-context type table records `MessageContext` as the declaring owner of `.Server`/`.Services`/`.User`/`.Items` with `RequestContext<T> : MessageContext` adding `.Params`/`EnablePollingAsync`, and `WithStdioServerTransport` is row [14] on the host-package `McpServerBuilderExtensions` (NOT `.Core`); `AsSamplingChatClient` is filed as the `McpServer` server-sampling-to-`IChatClient` bridge, never an MEAI member. The `mcp-projection#[6]-[RESEARCH]` `[SDK_LONG_RUNNING]` source-map stamp dropped — every long-running member is catalogue-verified.

[T-INDUSTRIAL-POLL-TRANSPORTS] [COMPLETE] -- `live-wire/live-wire.md`#TRANSPORT_BINDING owns the real poll bodies: `HttpPoll.Read`/`Write` (REST `GetAsync`, GraphQL `PostAsync` query body, spreadsheet read-only range, ERP-PLM vendor REST, write over `PutAsync`) over `OutboundHop.HttpApi`, `ModbusLane.Read`/`Write` over `ModbusClient.ReadHoldingRegistersAsync<short>`/`WriteMultipleRegistersAsync`, `SerialLane.Read`/`Write` over `SerialPort.ReadLine`/`WriteLine` plus `SerialLane.Attach` for the `DataReceived` subscribe stream — each projecting one `ExternalValue` and riding the row's `OutboundHop`; the `PollRead`/`PollWrite` `IO.fail` stubs are gone, the eight-arm `Transport.Switch` routes every transport to a real lane, the spreadsheet row stays non-writable, register-window/line-grammar carried as the new `PollPolicy` `[Union]` on `BindingSpec`.

[T-OPCUA-PUBSUB-LANE] [COMPLETE] -- `live-wire/live-wire.md`#TRANSPORT_BINDING owns `PubSubLane.Subscribe`/`Read` composing `UaPubSubApplication.Create(configPath, telemetry, dataStore)`/`Start`/`Stop`, the `DataReceived` `SubscribedDataEventArgs` dataset fan projecting each `DataSet.Fields` field into one `ExternalValue` and draining into the SAME bounded `DropOldest Channel<ExternalValue>` the per-node OPC-UA subscription drains into; the `MessageMapping`/`TransportProtocol` pair collapses to one `WireProtocol` `[SmartEnum<string>]` column (mqtt-json/mqtt-uadp/udp-uadp) on `TransportRow` over `UaPubSubApplication.SupportedTransportProfiles` — a read-shape PubSub variant on the OPC-UA row, one application per process, never an eighth transport.

[T-FIELDBUS-PACKAGE-ADMIT] [COMPLETE] -- `live-wire/live-wire.md`#TRANSPORT_BINDING `ModbusLane`/`SerialLane` bind the catalogued `FluentModbus` (`.api/api-modbus.md` — `ModbusClient` base register read/write, `ModbusEndianness`) and `System.IO.Ports` (`.api/api-serialport.md` — `SerialPort` line-policy + `DataReceived`) owners; the verified-wrong Modbus row Hop is re-seated off the `OutboundHop.HttpApi("http://localhost")` placeholder onto `OutboundHop.CompanionSpawn("rasm-modbus")` matching the Serial row, and the README §2 `[SANDBOX_LIVE_WIRE]` registry + ARCHITECTURE live-wire charter already list both packages. The central-manifest admission (`Directory.Packages.props` FluentModbus 5.3.2 line 89 + System.IO.Ports 10.0.9 line 90, with the explicit 10.0.9 pin retained over the FluentModbus transitive 5.0.0) is VERIFIED already landed — the task collapses (disposition runtime-action) into `T-INDUSTRIAL-POLL-TRANSPORTS` with the Hop-correction sub-fence its only residual, now closed.

[T-RESILIENT-REPLAY-FRONTIER] [COMPLETE] -- `determinism/determinism-and-replay.md`#ADVERSARIAL_FRONTIER owns `AdversarialFrontier.RecordChaos` (`[CHAOS_REPLAY]` — a `ChaosDecision` over the `FaultKind` `[SmartEnum]` latency/fault/outcome Simmy strategies folded through `EventLog.Append` as a deterministic fault-injection `LogEntry`), `AdversarialFrontier.Bisect` (`[DIVERGENCE_BISECT]` — `log₂(n)` binary search over the content-hash chain `EventLog.VerifyChain` proves, the found step cryptographically pinned), and `AdversarialFrontier.Counterfact` (`[COUNTERFACTUAL]` — perturbed-argument hash re-derivation feeding `RecomputeGraph.Invalidate` to return the downstream cone over the immutable recorded log); every cluster composes the kernel's own `EventLog`/`VerifyChain`/`RecomputeGraph` owners, the `outbound/outbound-resilience` Simmy pipeline read as the fault source, no second determinism surface.

[T-OPCUA-MANAGED-CLIENT-CATALOG] [COMPLETE]: Promote the `Opc.Ua.Client` managed-client cluster into `.api/api-opcua.md` so the `live-wire#TRANSPORT_BINDING` `OpcUaLane` fence rests on catalogue-verified members rather than the `[TRANSPORT_CLIENTS]` RESEARCH residual. Added the `Opc.Ua.Client` managed-client type scope (`Session`/`ISession`/`Subscription`/`MonitoredItem`/`MonitoredItemNotificationEventArgs`/`MonitoredItemNotification`/`ReverseConnectManager`/`ITelemetryContext`/`MonitoringMode`), the address-space/value primitive scope (`NodeId`/`DataValue`/`Variant`/`StatusCode`/`Attributes`/`WriteValue`/`ReadValueId`/the collections/`TimestampsToReturn`), the managed session/subscription/monitored-item entrypoint table (`Session.CreateAsync` 10-arg overload, `AddSubscription`, `Subscription(ITelemetryContext)`/`AddItem`/`CreateAsync`/`CurrentPublishingInterval` `double`, `MonitoredItem(ITelemetryContext)`/`Notification`/`DetachNotificationEventHandlers`, inherited `ReadAsync`/`WriteAsync`), the managed-value projection note, and the `[MANAGED_CLIENT_LAW]` rail clause. The live-wire `[8]-[RESEARCH]` `[TRANSPORT_CLIENTS]` OPC-UA residual now resolves against the catalogue; the page's `OpcUaLane` member spellings are catalogue-of-record. The PubSub binding landed under `T-OPCUA-PUBSUB-LANE`.

[T-AGENT-REASONING-RUNTIME] [COMPLETE]: `agent/reasoning-runtime.md` owns the in-process agent loop — `[2]-[REASONING_LOOP]` the `ReasoningSession` over `IChatClient.GetStreamingResponseAsync` with `ChatOptions.Tools` set to the brokered `CommandAIFunction` minted at the one `agent/mcp-projection#METHOD_AXIS` tool-adoption seam (two front doors, never a second projection) and the `ReasoningTurn` `[Union]` stream fold, `[3]-[SEMANTIC_DISCOVERY]` the `IEmbeddingGenerator` cosine fold over `TensorPrimitives.CosineSimilarity` adding `DiscoveryQuery.ByIntent(string)` as ONE new case extending `capability/registry#DISCOVERY_FOLD` (total-dispatch break) with the frozen composition-time `EmbeddingIndex`, `[4]-[REPLAYABLE_TRANSCRIPT]` the transcript-to-`CommandReceipt`/`LogEntry` chain over `determinism#EVENT_LOG`/`MACRO_ENGINE` content-addressed by `XxHash128`, and `[6]-[MODAL_INPUT]` the gated `[Experimental("MEAI001")]` `ModalClient` `[Union]` (`ISpeechToTextClient`|`IImageGenerator`) reading the same descriptor catalog. Mined `Microsoft.Extensions.AI.Abstractions` to the streaming + embedding + modal depth; the `ToChatResponse()` aggregation extension and `FunctionResultContent` accessors are `[8]-[RESEARCH]` `[STREAM_ACCUMULATION]`, the `System.Numerics.Tensors` admission `[EMBEDDING_RANK]` (returned to the orchestrator). No manifest delta — `Microsoft.Extensions.AI.Abstractions` already admitted.

[T-GENAI-MODEL-GOVERNANCE] [COMPLETE]: `agent/reasoning-runtime.md`#[5]-[MODEL_GOVERNANCE] owns the ONE model-governance fold — `ModelGovernance.Compose` chains `AsBuilder().UseOpenTelemetry().UseDistributedCache().UseFunctionInvocation().Build()` into the single delegating pipeline both front doors share (the in-process loop + the MCP server-sampling leg), `ChatResponse.Usage`→`CostVector` charging `CostUnit.ModelTokens` through `GrantBroker.Admit` over the `agent.reasoning` descriptor, and the response-digest `LogEntry` the `EventLog` content-addresses for cached-replay bit-identity; composes `capability/registry#GRANT_BROKER`, `resources/resource-lanes#CACHE_PORT` `CacheLane.ModelResult` `HybridCache`-as-`IDistributedCache`, the `observability/diagnostics-and-telemetry#PROFILE_SIGNAL` GenAI span, and `determinism#EVENT_LOG`. Mined the concrete `Microsoft.Extensions.AI` middleware catalogue (`ChatClientBuilder`/the three decorators/`UsageDetails`); VERIFIED no manifest delta (`Microsoft.Extensions.AI` already admitted with the `Concrete model-governance middleware` row). The `SampleAsync`/`AsSamplingChatClient` second-front-door spellings are catalogue-verified at `.api/api-mcp.md` server-session rows [1]-[3] (`T-MCP-LONG-RUNNING-CATALOG` landed them); the `[8]-[RESEARCH]` `[SDK_SAMPLING_FRONT_DOOR]` card now states the governed-client composition over the `AsSamplingChatClient` bridge as settled, no longer gated.

[T-AGENT-TOOL-FEDERATION] [COMPLETE]: `agent/tool-federation.md` owns the MCP-client federation — `[2]-[FEDERATION_AXIS]` the `TransportKind` `[SmartEnum<string>]` (stdio/http/streamable, each carrying its `IClientTransport` factory) + `FederatedServer` `[ValueObject]` admission row + `TrustScope` floor + `FederationFault` `[Union]` (fresh 4800-4805 band, distinct from the `determinism#REPLAY_VERIFY` `ReplayFault` 4760-4763), `[3]-[FEDERATION_PROJECTION]` the `McpClient.ListToolsAsync` → `McpClientTool` → brokered `CapabilityDescriptor` inversion under `federated.{server}.{tool}` reusing the one `CommandAIFunction` (never a second `AIFunction` subclass) admitted through `DescriptorSurface.Describe`, `[4]-[FEDERATED_DISPATCH]` the brokered call through `CommandAlgebra.Run`/`GrantBroker`/cost-preview routing `McpClient.CallToolAsync` over `OutboundHop` and reusing `mcp-projection#TOOL_DISPATCH` `ToolResult`, `[6]-[TS_PROJECTION]` federated rows additive to the one capability catalog. Client surface verified against `.api/api-mcp.md`; the `ComputeIntent.Federated` compile-target arity is `[7]-[RESEARCH]` `[FEDERATED_INTENT]`.

[T-FEDERATION-RESOURCE-PROMPT] [COMPLETE]: `agent/tool-federation.md`#[5]-[RESOURCE_PROMPT_FOLD] owns the resource/prompt/template extension — the `FederationProjection.ProjectResources` fold admits `McpClient.ListResourcesAsync`/`ListPromptsAsync`/`ListResourceTemplatesAsync` rows as brokered descriptors under `federated.{server}.resource.{uri}`/`prompt.{name}`/`template.{uri}` (read/pure effect-class filter mirroring the server projection), and `FederationSubscription.Subscribe` binds the `McpClient.SubscribeToResourceAsync(string uri, Func<ResourceUpdatedNotificationParams, CancellationToken, ValueTask> handler, RequestOptions?, CT)` per-uri overload — registering the update handler at subscribe and holding the returned `IAsyncDisposable` — so a peer resource-update drains into the ONE bounded `live-wire#TRANSPORT_BINDING` `SubscriptionLane` the OPC-UA/MQTT subscriptions drain into, never a parallel handler and never the non-existent `McpClientHandlers.ResourceUpdatedHandler` slot. `McpClient.ListResourceTemplatesAsync(RequestOptions?, CT)` is catalogued at `.api/api-mcp.md` client-construction row [9] (`[7]-[RESEARCH]` `[TEMPLATE_LIST_VERB]` resolved); the `ComputeIntent.Federated` compile-target arity is the one cross-folder `[7]-[RESEARCH]` `[FEDERATED_INTENT]` residual against `Rasm.Compute` intent-and-selection#INTENT_FAMILY.

[T-API-COVERAGE-SWEEP] [FOLDED]: Dissolved — its two legs are not both pending. The `Microsoft.Extensions.AI` middleware-decorator catalogue is already authored (`.api/api-extensions-ai-middleware.md` carries `ChatClientBuilder`/`UseFunctionInvocation`/`UseDistributedCache`/`UseOpenTelemetry`, the three decorators, `UsageDetails` token counts), and the MCP **client** federation surface is already catalogued in `.api/api-mcp.md` (`McpClient.*`, the stdio/http/streamable transports, `McpClientTool`/`McpClientResource`/`McpClientPrompt`/`McpClientResourceTemplate`, `IMcpTaskStore`/`InMemoryMcpTaskStore`). The single residual — the MCP **server** long-running session leg (`McpServer.SampleAsync`/`AsSamplingChatClient`/`ElicitAsync`/`GetTaskAsync`/`WaitForTaskResultAsync<T>`/`PollTaskUntilCompleteAsync`, `McpServerOptions.TaskStore`/`SendTaskStatusNotifications`, `RequestContext`/`RequestContext<T> : MessageContext`, `EnablePollingAsync`, `WithStdioServerTransport`) — folds back into `T-MCP-LONG-RUNNING-CATALOG`, which is the live ledger. A coverage-sweep card duplicating an already-authored catalogue is the deleted form.

[T-DEEPEN-ALIGN] [COMPLETE]: Deepen-and-align sweep — closed the `JsonPatch.SystemTextJson` under-utilization by deepening `configuration#POLICY_VALUES` `OptionsAdmission.PatchSection` (RFC-6902 intake, section-keyed `revalidate`, the `companion#CONTROL_SERVICE` `DispatchPatch` route), `[PATCH_ERROR_SHAPE]` RESEARCH; synced the stale frontier (`SECRET_LIFECYCLE`/`FENCING_COORDINATION` moved to CLOSED as row clusters, `ARCHITECTURE` `coordination/`/`secrets/` rewritten to the "= ROWS" law).

[T-HOST-BINDING] [COMPLETE]: The §19 ports host-binding owner + macOS-correct peer-admission landed in `companion/companion-sidecar.md` — `[6]-[PEER_ADMISSION]` reads the peer over `Socket.GetRawSocketOption` (Linux `ucred`/macOS `xucred`), `[7]-[HOST_BINDING]` folds OS×ActivationSource×BindAddress into one `HostBinding.Acquire/Release/Identify` over a 9-row table; the systemd/launchd/SecItem/reuseport `[LibraryImport]` adapters are header-authored, never executed live, signatures RESEARCH.

[T-SECRET-LIFECYCLE] [COMPLETE]: The credential lifecycle landed as the `[5]-[SECRET_LEASE]` cluster on `configuration-and-options.md` extending `ConfigSource.SecretsStore` (not a `secrets/` page) — `SecretLease`/`Acquire`/`Renew`/`Zeroize`, `SecretFault` (4780-4783), `CryptographicOperations.ZeroMemory` under `DrainBand.Stores`, RID-dispatched credential store; native signatures RESEARCH.

[T-FENCING-COORDINATION] [COMPLETE]: Fenced single-writer election landed as the `[5]-[FENCING_TOKEN]` cluster on `time-and-deadlines.md` extending `SchedulePort` (not a `coordination/` page) — `FencingToken` `[ValueObject<ulong>]` with monotone `Next` + Kleppmann reject-lower `Admits`, the resource checking the token; the store fences at Persistence/server-tier, aligned not coupled.

[T-DESCRIPTOR-AUTOPROJECT] [COMPLETE]: The single-source descriptor→MCP→SDK auto-projection landed across `capability/registry.md` and `agent/mcp-projection.md` — one `CapabilityDescriptor.Of(...)` row drives the MCP tool, the three SDK renderers, and the dashboard catalog off one descriptor source; the two `WireFault` unions stay namespace-fenced, `ComputeIntent.Spec` arity + `SuiteContracts.Schema` digest the cross-page RESEARCH.

[T-MCP-SDK] [COMPLETE]: The MCP serving surface design landed in `agent/mcp-projection.md` — the registry projects through the official SDK (`CommandAIFunction : AIFunction` adopted by `McpServerTool.Create`, `McpDispatch`, `StreamProgress`); the SDK member spellings + 4640→`-32xxx` fault map are `[6]-[RESEARCH]`.

[T-PROFILE-SIGNAL] [COMPLETE]: The fourth-signal design landed in `observability/diagnostics-and-telemetry.md` — `TelemetrySignal` carries the trace/metric/log/profile set, `SpanProfileProcessor` links CPU profiles to the minted span, the `gen_ai.*` GenAI conventions ride the trace/metric signals.

[T-GRANT-ATTESTATION] [COMPLETE]: The signed-grant design landed across `capability/registry.md` (detached signature over canonical `GrantAttestation` bytes) and `determinism-and-replay.md#EVENT_LOG` (the hash-chain seat), closing the `T-CEDAR-BROKER` auditable-decision re-target.

[T-LIVE-WIRE-BINDING] [COMPLETE]: The OPC-UA + MQTT per-case `Read`/`Write` binding landed in `live-wire.md` `[3]-[TRANSPORT_BINDING]` — `TransportRows` dispatch the eight-arm switch to `OpcUaLane`/`MqttLane`, both foreign callbacks draining into one bounded `DropOldest` `Channel<ExternalValue>` so neither protocol thread runs the interior; MQTT verified against `.api/api-mqtt.md`, the high-level `Opc.Ua.Client` cluster `[TRANSPORT_CLIENTS]` RESEARCH (decompile correction: `CurrentPublishingInterval` is `double`).

[T-INDUSTRIAL-TRANSPORT] [COMPLETE]: The certified-transport design landed in `live-wire.md` — `[TRANSPORT_CLIENTS]` composes `OPCFoundation.NetStandard.Opc.Ua`(`.PubSub`) and `MQTTnet` behind the one `TransportRow` adapter over their `OutboundHop` rows.

[T-WASM-RUNTIME] [COMPLETE]: The WASM-runtime design landed in `sandbox/sandbox-host.md` — the wasm-component isolation row hosts on `wasmtime-dotnet` (WASI-Preview-2 import table scoped to the granted descriptors, `Store` fuel/memory quota cell), the `componentize-dotnet` WIT-binding distinction `[WASM_RUNTIME]` RESEARCH.

[T-CEDAR-BROKER] [DROPPED]: Route the grant-broker through Cedar — dropped with its parent idea; no Cedar .NET binding exists, the broker keeps its typed `PermissionShape`×`GrantScope.Covers` predicate, auditable-decision re-targeted to `T-GRANT-ATTESTATION`.

[T-DOC-MIGRATE] [COMPLETE]: Re-homed the 19 design pages into the single `.planning/<sub-domain>/<page>.md` tree, rebuilt `ARCHITECTURE.md`/`README.md`, authored `IDEAS.md`/`TASKLOG.md`.
