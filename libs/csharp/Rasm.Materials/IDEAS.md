# [MATERIALS_IDEAS]

The forward pool of higher-order concepts for the host-neutral materials owner, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[CONNECTION_REINFORCEMENT_FAMILY]: Open the `connection/` sub-domain — the structural-fastener, reinforcement-bar, and connector vocabulary the AEC material domain centers on and the folder entirely lacks.
- Model rebar (bar size/grade/bend), joist hangers and framing connectors, and the fastener family (bolts/nuts/screws/anchors) as one `ConnectionItem` `[Union]` over a closed `ConnectionFamily` axis (`reinforcement`/`hanger`/`fastener`/`anchor`), each case a registered-row catalogue keyed `connection.<designation>` exactly as `profile#PROFILE_OWNER` `ProfileCatalogue.Build` folds the five `ProfileFamily` builders — a #5 rebar is a `Reinforcement` row over an `RebarGrade`/`BarSize` axis, a Simpson-style hanger a `Hanger` row, a 3/8" hex bolt a `Fastener` row, never a `Rebar`/`Bolt`/`Hanger` per-item type. Each item carries its `MaterialId` (`metal.iron`/`metal.steel`) so the appearance engine shades it and its `MaterialPropertySet` mechanical row so the connection-design seam reads its capacity.
- Unlocks the actual construction-material spine the prompt names: a `construction/layout` run reinforces with a `Reinforcement` schedule, a `ProfileSet` member fastens with a `Connection`, and the BIM wire serializes the fastener/rebar as the IFC 4.3 `IfcMechanicalFastener`/`IfcReinforcingBar`/`IfcReinforcingMesh`/`IfcFastener` element the federation reads — turning the folder from "profiles + appearance" into the full parameterized material catalogue (sections, units, reinforcement, fasteners, connectors).
- Draws on the ASTM A615/A706 rebar bar-size/grade tables, the ACI 318 standard bend/hook geometry, the ISO 898 / SAE J429 bolt-grade tables, and the IFC 4.3 `IfcReinforcingElement`/`IfcMechanicalFastener` schema; the `connection/layout` placement reads the realized `construction/layout#ASSEMBLY_FOLD` station/elevation cursor so a rebar schedule and a fastener pattern are station-stepped placements over the same fold, never a parallel layout owner.

[PIGMENT_FINISH_ENGINE]: Open the `appearance/finish` sub-domain grounding architectural finishes (paint, coating, stain, plaster) in the Unicolour Kubelka-Munk pigment model the folder admits but never calls.
- Author one `FinishMix` over the closed `Pigment[]`/weight Kubelka-Munk reflectance construction (`new Unicolour(Pigment[], double[])`) and the admitted `Wacton.Unicolour.Datasets` `ArtistPaint` 19-pigment reflectance set, plus a `FinishLayer` coat-stack (primer/base/topcoat) lowering to the `bsdf#OPENPBR_SLAB` coat slab — a finish is a pigment mix plus a coat stack, never a hand-keyed `MaterialParameters` base-color triple; the resolved scene-linear reflectance seeds the library row's `BaseColor` so a painted/stained material carries a physically-mixed color with measured pigment provenance, not an authored guess.
- Unlocks the finishes leg of the material domain (the prompt's named finish family) as the one place architectural color is specified by pigment recipe — a custom paint is a `Pigment[]` weight vector resolving through Kubelka-Munk, a wood stain a translucent pigment over the `wood.oak` substrate, a plaster a high-scattering single-constant pigment — and exploits the admitted-but-dormant `Pigment`/`ArtistPaint` surface (a named external-capability gap) end to end.
- Draws on the Kubelka-Munk two-constant reflectance theory Unicolour's `Pigment` ctor owns and the Golden `ArtistPaint` measured reflectance set; the finish color round-trips through the `bsdf#SPECTRAL_UPSAMPLE` SPD pipeline and validates against `graph#MATERIAL_LIBRARY` `NearestChecker`, the coat-stack lowering riding the realized `bsdf#OPENPBR_SLAB` `Coat` slab.

[ENGINEERING_PROPERTY_AGGREGATION]: Deepen `physical-properties` from a per-material lookup into the layered/composite property-aggregation engine the construction model demands.
- Land the rule-of-mixtures and series-resistance aggregation folds the `properties#RESEARCH` flags only as probes: a `LayerSet` U-value is the series thermal-resistance sum `1/U = Σ(t_i/λ_i)` over each ply's `MaterialProperty.Thermal`, a `ConstituentSet` effective density/conductivity the constituent-`Fraction`-weighted rule-of-mixtures sum, and a layered STC the mass-law/coincidence aggregation over the wall buildup — each a fold over the `construction/assembly#MATERIAL_ASSIGNMENT` reading the per-material `MaterialPropertySet`, producing an `AssemblyProperty` receipt the BIM Pset and the `AEC_SIMULATION_BRIDGE` analysis seam read, never a parallel composite owner.
- Unlocks the assembly being a full engineering object — a wall's U-value, STC, and fire rating computed from its layer buildup rather than re-keyed per assembly — so the `construction/layout` resolved `Layout` carries its thermal/acoustic/fire envelope and the property model serves the structural/thermal/energy analysis the C# concert `AEC_SIMULATION_BRIDGE` consumes.
- Draws on the IFC `IfcMaterialConstituentSet.Fraction` rule-of-mixtures, the ISO 6946 series-resistance U-value method, and the ISO 12354 / mass-law acoustic-transmission aggregation; the fold composes the realized `assembly#MATERIAL_ASSIGNMENT` `LayerSet.TotalThickness`/`Materials` and the `properties#MATERIAL_PROPERTY` per-discipline cases, no new package.

[STRUCTURAL_FAMILY_VOCABULARY]: Land the remaining queued `ProfileFamily` vocabularies as source-backed cross-section data.
- Author each family as one `ProfileFamily` case with its own `ProfileUnit` projection — timber as sawn/glulam/CLT rows, cmu as cell/face-shell rows, glazing as IGU pane/spacer/frame rows — each on its own `profiles/<family>` page the way masonry carries `Coring`/`BondName`/`Orientation` and steel carries `SteelClass`/`SteelSection`.
- Unlocks a full cross-section catalogue so `construction` extrudes cmu/timber/glazing members through the same `Profile` owner and the same `ConstructionLayout.Resolve` fold, with `IfcProfileDef`-shaped profiles on the wire and a `ProfileSet` assignment per member.
- Draws on the standard timber/CLT property tables and the cmu cell/face-shell tables; masonry and steel (`profiles/steel`, the AISC Shapes Database v16.0 W-shape seed) are realized today, so the three remaining siblings are the named growth axis.

[ACOUSTIC_SPECTRAL_DEPTH]: Deepen the `physical-properties` `Acoustic` case from a scalar STC/absorption pair into the per-octave-band acoustic spectrum the discipline measures.
- Replace the single `AbsorptionCoefficient` with the standard six-band absorption spectrum (125/250/500/1k/2k/4k Hz `α` per ISO 354) and carry the sound-reduction-index spectrum the layered STC aggregation reads, so an acoustic material is a banded spectrum exactly as a `bsdf` spectral upsample is a banded reflectance — one `Acoustic` case carrying a fixed-band `ReadOnlyMemory<double>` coefficient vector, never a scalar collapse that loses the frequency dependence the design seam needs.
- Unlocks acoustic materials grounded in the measured octave-band data the architectural-acoustics discipline works in (NRC computed from the bands, the layered STC from the per-band sound-reduction indices) feeding the `ENGINEERING_PROPERTY_AGGREGATION` mass-law fold and the `AEC_SIMULATION_BRIDGE` acoustic analysis — a material's NRC is a derived projection of its band spectrum, never a re-keyed scalar.
- Draws on ISO 354 (reverberation-room absorption), ASTM C423 (NRC/SAA), and ISO 717-1 (weighted sound-reduction index); the band vocabulary mirrors the `bsdf#SPECTRAL_UPSAMPLE` fixed-interval spectral carrier so the two spectral families share the fixed-band fold shape, no new package.

## [2]-[CLOSED]

[MEASURED_SPECTRAL_LIBRARY]: Realized internal at `bsdf#CONDUCTOR_IOR` — the `ConductorIor` measured complex-IOR `(η, k)` per-RGB-band table (gold/copper/aluminum/silver/iron/chromium/titanium/brass from the Johnson-Christy / `refractiveindex.info` tables) grounds every `metal.*` `graph#MATERIAL_LIBRARY` row's `bsdf#LOBE_FAMILY` `Conductor` lobe from the measured Fresnel, `ConductorIor.Resolve(family, name)` mapping the `MaterialId` to its `ConductorMetal`, the `(η, k)` the shading truth and the `BaseColor` the perceptual preview seed; round-trips in-gamut through the `bsdf#WHITE_FURNACE_HARNESS` and the `graph#MATERIAL_LIBRARY` `NearestChecker` ColorChecker ΔE seam. The only live residue is the measured isotropic 195-wavelength spectral BRDF (EPFL RGL goniophotometer, brdf-loader `.bsdf` format) admitted through `bsdf#SPECTRAL_UPSAMPLE` `ToSpd` per band, which stays [UPSTREAM-BLOCKED] on a vendored managed `.bsdf` reader at `acquisition#EPFL_RGL_BRDF_LOADER` — a host-edge binary-format leg, not a forward concept.

[OPENPBR_SLAB_LAYERING]: Realized as `bsdf#OPENPBR_SLAB` — the `SlabKind` axis, the closed `Slab` `[Union]` (fuzz/coat/emission/base), the `SlabStack` outermost-to-base albedo-scaling layering algebra, `SlabStack.Lower` (the one OpenPBR Surface 1.1 construction from a `MaterialParameters` row), and `ToLayered` (the energy-preserving collapse into the `bsdf#LAYERED_COMPOSITION` `LayeredBsdf` the integrator shades) reached the decision-complete bar with a transcription-complete fence; the `MaterialParameters` columns align to the OpenPBR parameter groups and the fuzz slab is the realized seventh-lobe `Sheen` position. The `LayeredBsdf` weighted-lobe fold is the lowering target by design, not the predated form to delete; the wire projection lives under the branch `OPEN_MATERIAL_STANDARD_WIRE` seam.

[MATERIALX_GRAPH_INTERCHANGE]: Realized as `interchange#MATERIAL_WIRE` and `interchange#MATERIALX_DOCUMENT` — the `MtlxDocument`/`MtlxNode`/`MtlxInput` MaterialX 1.39 node-graph shape, the `NodeCategory` `[SmartEnum<string>]` category axis mapping the six `AppearanceNode` cases (`constant`/`image`/`multiply`/`mix`/`normalmap`/`open_pbr_surface`) and the `PortValue` polarities onto the MaterialX typed ports, `Mtlx.FromGraph`/`ToOpenPbr` bidirectional `.mtlx` projection over `System.Xml.Linq` at the host boundary, and the `MaterialWire` OpenPBR-vector wire the TS/Py peers decode reached the decision-complete bar with a transcription-complete fence. Residue: the per-input port-name calibration against a reference MaterialX standard-node library and the host-boundary schema validation are the `[4]-[RESEARCH]` `[MATERIALX_GRAPH_INTERCHANGE]`/`[MTLX_SCHEMA_VALIDATION]` probes; the `texture#MATERIALX_NODE_PARITY` Worley/color-ramp node-category mapping is the matching `texture` probe.

[IFC_MATERIAL_ASSIGNMENT]: Landed as the `construction/assembly` design page — the `MaterialAssignment` closed-union owner over the IFC 4.3 layer-set/profile-set/constituent-set trichotomy reached the decision-complete bar with a transcription-complete fence; the remaining work is the implementation task, not a forward concept. The downstream consumption (a `LayerSet`/`ProfileSet` resolving to a placement stream) is realized at `construction/layout#ASSEMBLY_FOLD` (`StationStep` plus the opening/corner/arch/pier/layer-buildup stages), the `[ACTIVE]` layout-calibration task in `TASKLOG.md` carrying the residual edge-cut and multi-leaf-corner detailing depth.

[PHYSICAL_PROPERTIES]: Landed as `physical-properties/properties` — the `MaterialProperty` `[Union]` (mechanical/thermal/acoustic/fire) keyed by `MaterialId` over the IFC `IfcMaterialProperties` set, the `MaterialPropertySet` bundle, the `FireRating`/`MaterialPropertyKind` bands, and the `UnitsNet`-composing `Admit` coercion seam reached the decision-complete bar with a transcription-complete fence. Residue: the dedicated `ThermalConductivityUnit` enums are an `.api` catalogue gap riding catalogued proxy units ([UNITSNET_CANONICAL_UNIT] research item).

[WEATHERING_AGING]: Landed as `appearance/weathering` — the `Weathering.Apply` aging fold over the closed `WeatheringEffect` `[Union]` (patina/oxidation/soiling/uv-fade) driving a `MaterialParameters` row forward along an `AgeParameter` through Unicolour scene-linear `Mix`, re-admitting through `MaterialParameters.Of`, reached the decision-complete bar with a transcription-complete fence. The aging operator targets the realized `bsdf#OPENPBR_SLAB` `SlabStack` columns directly (chalking raises `coat_roughness`, soiling raises `fuzz_weight`, patina swaps the base `ConductorMetal`), the flat `MaterialParameters` interpolation the form until a consumer drives the slab columns.

[MATERIAL_ACQUISITION]: Landed as `appearance/acquisition` — the `Acquisition.Import` fold over the closed `CaptureSource` `[Union]` (measured-brdf/svbrdf-map/spectral-reflectance) producing a `MaterialParameters` row with measured `Provenance`, grounding through `bsdf#SPECTRAL_UPSAMPLE`, reached the decision-complete bar with a transcription-complete fence. The angular-sample fit and field-average arms are internal; the EPFL RGL `.bsdf` binary decode and the neural-SVBRDF inference stay [UPSTREAM-BLOCKED] at the host import edge.
