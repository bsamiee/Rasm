# [CSHARP_BRANCH_IDEAS]

The cross-package C# concert: ideas that couple two or more C# packages into one capability, distilled from the eight folder pools. A concept that lives inside one folder stays in that folder's `IDEAS.md`; a concept that spans C#, Python, and TypeScript at once lives in `libs/.planning/IDEAS.md` and is referenced here as a wire seam, never restated. Each idea is a card — a bracketed slug leader plus the capability, what it unlocks, and the gap or technique it draws on. `[1]-[OPEN]` holds live ideas; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition.

## [1]-[OPEN]

[CAPABILITY_CONTROL_PLANE]: one self-describing op catalog spanning the runtime spine, the execution lane, and the UI.
- Every operation is a typed effect-classed cost-modeled permission-gated `CapabilityDescriptor` minted in `AppHost/Agent/capability` from the `Compute/Runtime/channels` proto op surfaces and the `AppUi/Shell/commands` intent table; a shape-discriminated discovery fold drives intent planning, the grant/cost broker previews cost, the command algebra wraps any invocation in a commit-or-rollback transaction routed onto the `Compute/Runtime/admission` rail, and the MCP projection and SDK codegen are projections of the one descriptor source.
- Unlocks one operation surface every consumer reads — the MCP agent tools, the in-process function-calling, the command palette, and the polyglot SDK all derive from a single catalog, so a new operation is a descriptor row, never a parallel client or a second op-metadata owner.
- Draws on the dependency-policy never-hand-roll rule and the official MCP C# SDK now owning the protocol: the registry is the one place op metadata lives, and the cross-language SDK-codegen consumption is the `libs/.planning` capability-catalog seam.

[ONE_CONTENT_IDENTITY_FABRIC]: the single `XxHash128` content-address seed threading cache, blob residence, diff, model-result reuse, and the codecs.
- The sealed snapshot hash, the Compute interchange identity, the incremental topology naming hash, and the per-node reactive-graph key are all one `XxHash128` law: `Rasm.Geometry/Spatial/reconciliation` emits the canonical geometry bytes, `Compute/Runtime/codecs` folds artifact bytes plus deflection and tolerance into the key, `Persistence/Query/cache` content-addresses blob residence and the model-result horizon, and `Compute/Model/inference` plus the reactive scheduler reuse a sub-result by key.
- Unlocks fetch-by-content-key across every C# package — a warm-start blob, a result cache hit, a deduped snapshot chunk, and a clean reactive-graph node all collapse onto one identity, so no package mints a parallel key and a re-tessellation at a new deflection re-keys geometry and metadata together.
- Draws on the content-addressed-build-graph and self-adjusting-computation technique over the one seed; the cross-runtime bit-identical reproduction of this seed is the `libs/.planning` content-address seam.

[INCREMENTAL_COMPUTE_PIPELINE]: a content-keyed reactive recompute engine spanning the execution lane, the durable store, and the UI.
- `Compute/Runtime/scheduling` keys every job-graph node on the content digest of its inputs, schedules only the transitive dirty closure on re-run, and replays every clean node from the `Compute/Model/inference` result cache; the `Compute/Solver/sweep` frame-budget governor bounds the dirty closure per frame, the `Persistence/Sync/collaboration` op-log changefeed records the moved nodes, and `AppUi` receives only the genuine delta for a parametric edit.
- Unlocks interactive parametric editing across the AEC pipeline — a single-parameter tweak re-runs only the affected tessellate/solve/encode chain while every unchanged branch returns from cache, and a coupled geometry-and-field iteration transmits its true delta with no second versioning scheme.
- Draws on demand-driven incremental computation over the one content-address seed and the deterministic result cache, composing the identity and receipt owners the lanes already sit beside rather than minting a second memoization or dependency tracker.

[DETERMINISTIC_REPLAY_OBSERVATORY]: a reproducibility kernel binding the runtime spine, the durable op-log, and the notebook surface.
- `AppHost/Runtime/determinism` pins RNG/float-mode/environment-fingerprint and owns the hash-chained content-addressed event log over the `Persistence/Sync/collaboration` op-log changefeed; replay-verify proves each step by content hash, the macro engine records and replays a unit, and `AppUi/Editing/notebook` pins a capability and exports a replay bundle bit-identically with the cell-edit op projecting onto the op-log CRDT delta.
- Unlocks bit-identical replay and a self-verifying audit trail — a notebook re-runs to an identical result, a grant decision re-derives from the chain alone, and a regulatory reviewer reconstructs who computed what and when.
- Draws on the chained-event-log and verifiable-credential technique over the one content-address seed; cross-machine replay-verify consumes the Compute provider-determinism fingerprint, and a signed grant attestation chained into the event log closes the in-process-only authorization gap.

[GEOMETRY_FLOW_SEAM]: one geometry/IFC flow from the kernel through the codec, the store, and the renderer.
- The kernel `Rasm.Geometry` owns the C# geometry source-of-truth; `Compute/Runtime/codecs` content-addresses every artifact and runs the two-hop IFC-to-geometry tessellation bridge, `Rasm.Bim/Exchange/tessellation` builds the tessellation request and `Rasm.Bim/Exchange/import` owns the IFC semantic graph, `Persistence/Query/cache` owns blob residence and the `Persistence/federation` entity graph ingests the IFC model, and `AppUi/Render/viewport` renders the field receipts — each concern owned once, the runtimes meeting only at the content-keyed wire.
- Unlocks a single geometry pipeline with no concern owned twice: the spatial index the kernel builds feeds the Compute clash compute, the canonical adjacency bytes the kernel emits feed the Persistence structural diff, and the BSDF the Materials engine evaluates shades the viewport path trace — every seam a content-keyed reference, never a re-implementation.
- Draws on the one-owner-per-runtime geometry law; the kernel clash-seam node-link contract and the Compute clash decode are the agreed frozen contract, the residual two-sided golden fixture riding the folder tasks.

[FEDERATED_COORDINATION_COCKPIT]: an openBIM coordination surface composing BIM semantics, durable annotation, and the UI viewport.
- `Rasm.Bim/coordination` owns the BCF 3.0 topic/component/comment exchange semantics and the GlobalId-stable model diff, `Persistence/annotation` owns the anchored-annotation algebra and the CDE sync, `Persistence/federation` owns the federated entity graph and the rule engine, and `AppUi/coordination` composes the viewpoint view-state, a CRDT comment thread, and a snapshot tile into the board projection — the BCF semantic model owned once in Bim, the board owned once in AppUi.
- Unlocks in-app clash and issue coordination by composing existing owners with zero new persistence owner and zero second BCF model: the diff joins by GlobalId plus content-key, the IDS audit and the rule engine ride the one federated graph, and the issue round-trips through the op-log changefeed.
- Draws on BCF 3.0 and IDS 1.0 as the openBIM coordination and validation standards, the bSDD dictionary binding the classification axis, and the anchor algebra as the currency unifying comments, conflicts, presence, and blame.

[TELEMETRY_LAKE_ANALYTICS]: a four-signal observability lake folding live signals and durable receipts into one analytics surface.
- `AppHost/Observability/telemetry` governs traces/metrics/logs/profiles through minted identities and the redaction taxonomy, the `AppHost/Runtime/ports` HLC fan-in orders cross-process receipts, the `Persistence/Store/profiles` in-stores classification + 5D cost catalog and the analytical lane roll the durable receipts into a DuckDB/DuckLake columnar tier, and `AppUi/Charts/dashboards` reads the aggregate as series and cross-filter-brushed tiles correlated by one id.
- Unlocks production flame graphs scoped to a span, a time-series telemetry product over the TimescaleDB hypertable, and a world-class dashboard fed by N solve nodes — a new backend is one row on the metrics axis, never a parallel surface.
- Draws on the OTel 2025-2026 profiles and GenAI semantic conventions, the DuckLake lakehouse over object-store Parquet, and the Arrow Flight zero-copy egress, all riding the one correlation primitive and the one analytical engine.

[CROSS_PROCESS_TOPOLOGY_FABRIC]: one host-modality switch driving the in-process, paired, companion, sidecar, hub, and service topologies.
- The host-profile axis at `AppHost/Runtime/profiles`, the placement fold at `Persistence/Store/profiles`, and the surface-host axis at `AppUi/Shell/hosts` are one dual-mandate switch — universal-standalone and first-class Rhino/GH2 — never a fork; a companion compute farm is one substrate row with warm-affinity reordering, an object-graph sync hub is one sync-transport row over the op-log HLC changefeed, a sidecar ingest pipeline is one capture-direction placement, and the fenced single-writer election on `AppHost/Runtime/time#FENCING_TOKEN` (a row cluster over `SchedulePort`/`LeasePolicy.Maintenance`, never a `coordination/` sub-domain) owns the monotone token every guarded write carries.
- Unlocks every multi-process shape the desktop/host suite ships — paired editing, a self-updating farm with health-gated rolling updates, cross-process result reuse by content-address, and a multi-peer session roster — each a row or column on a settled axis, never a second orchestrator.
- Draws on the fencing-token correctness law for distributed locks, the workload-identity short-lived-credential shift `AppHost/Runtime/config#SECRET_LEASE` owns (a row cluster extending `ConfigSource.SecretsStore`, never a `secrets/` sub-domain), and the certified OPC-UA + MQTTnet stack the live-wire studio binds; the browser-consumer leg of any web-fed topology is the `libs/.planning` web-fed wire seam. The seven host-free port records (`ReceiptSinkPort`/`TelemetryContributorPort`/`DrainParticipantPort`/`HostAttachPort`/`UiSchedulerPort`/`SupportContributorPort`/`HealthContributorPort`) are the `Runtime/ports` contract, with secrets (`SecretLease`), coordination (`FencingToken`), and tenancy threaded as rows on existing axes rather than an eighth port, and the Unix+macOS host-binding owner resides beside `Wire/companion#SERVICE_HOST` (launchd/systemd P/Invoke) so a new OS is a new adapter-row set and a new transport a new `BindAddress` case.

[ENCODED_GEOMETRY_PLUGIN_CONTRACT]: one geometry-encoding vocabulary binding the sandboxed solver plugins to the tensor lane and the kernel.
- `AppHost/Sandbox/solver#SOLVER_KIND` declares each extension kind's input and output representation as an `EncodingKind` row projecting one-to-one onto the finalized `Compute/Tensor/residency#GEOMETRY_ENCODING` `GeometryEncoding` case axis — a mesher declares a brep-in mesh-out contract in the same representation vocabulary the Compute tensor lane reads, never a plugin-private representation — and the hosting fold's `Negotiate` proves the plugin's representations admit lossless round-trip through the canonical `EncodedTensor` shape before projecting the plugin's ops into `AppHost/Agent/capability`; the `GeometryPacking` boundary capsule that materializes each case's per-feature `EncodingChannel` rows reads host geometry through `Rasm` so the `PointCloud`/`MeshPatch`/`VoxelGrid`/`BrepPatch` cases trace to the kernel geometry the tensor lane encodes.
- Unlocks a third-party solver/mesher/optimizer/CAM-post/field-codec plugin-contract space where a new extension is one kind contract over the shared representation axis and one capability descriptor, never a parallel representation surface — the same encoding the in-process tensor lane, the remote proto geometry family, and the model-zoo conformance triad already speak.
- Draws on the finalized `GeometryEncoding` four-case representation axis and its eight-row `EncodingChannel` feature lattice (position/normal/color-rgba/curvature/geodesic/intensity/occupancy/weight) and the kind-contract effect-class defaulting; the `Field` and `Toolpath` representations the `EncodingKind` solver and CAM-post contracts speak land as two `GeometryEncoding` case extensions in the `Compute/Tensor/residency#GEOMETRY_ENCODING` encoding table — the `Field` arm against the `Compute/Solver/discretization#DISCRETIZATION_MESH` `FieldSpace` and the `Toolpath` arm against the `Rasm.Fabrication/Toolpath/motion#CAM_MOTION` `Motion` stream — never a solver-page or plugin-page literal.

[CROSS_PACKAGE_TRANSPORT_CONCERT]: one gRPC tuning algebra over three disjoint transport axes meeting only at the proto vocabulary and content identity.
- The intra-suite `Compute/Runtime/channels#GrpcChannelPolicy` centralizes channel tuning (max send/receive, reconnect backoff, pooled-idle, keep-alive, multiple-HTTP2, plus the `HttpVersion`/`Http3Forward` forward row gated to QUIC-capable RIDs); `AppHost/live-wire#ExternalTransport` owns the external edges (OPC-UA/MQTT as `[SmartEnum]` rows whose QoS/retain/LWT/session-expiry ride as policy data, never new cases) and `Persistence/Sync/collaboration#SyncTransport` owns durable peer/CRDT/object-graph sync (Speckle as the `SpeckleLikeDiff` case), the three axes aligned only at `#PROTO_VOCABULARY` (`GraphDiff`/`SubtreeFetch`) and `ContentIdentity`.
- Unlocks every transport shape as a row on a settled axis — a new transport is one `[SmartEnum]` row, a new external system one case on its correct axis, a new agent integration one `CapabilityDescriptor` row that auto-projects to the MCP tool and the C#/TS/Python SDKs — never a parallel `ExternalExchange` grab-bag or a hand-authored tool divorced from a descriptor.
- Draws on the deliberate `DisableResolverServiceConfig=true` (the AppHost keyed Polly pipeline owns hop retry, so a channel ServiceConfig retry/hedge/load-balancing owner is the rejected second owner), the inbox-only `Gzip`/`Deflate` compression providers (a zstd/brotli codec is one `CompressionProviders` row, never a package), and the C#-terminates-external law that makes a TS Speckle/MQTT client the named drift defect.

[BRANCH_TEST_NODE_PROVISIONING]: one shared C# test rail — a single golden-fixture corpus plus the per-package proof nodes that read it — provisioned before any spec lands.
- One shared test corpus node holds the two cross-package golden fixtures every consuming spec reads rather than re-deriving — the `CANONICAL_BYTE_IDENTITY` single-triangle 52-byte stream and `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest (host-derived-confirmed, freeze-ready) and the `CLASH_GOLDEN` 8-primitive `BoundingBox[]` clash set (design-pin-then-freeze); the `Rasm.AppUi.Tests` node admits Avalonia.Headless/Verify.XunitV3 so the `evidence/#HEADLESS_DERIVATION` proof matrix, render-hash lanes, and command-journal replay run under virtual time; the `Rasm` kernel testkit node carries the `System.Numerics.BigInteger` exact oracle and the five law-matrix harnesses (predicate sign-exactness, Delaunay correctness, re-anchor injectivity, LM convergence, the implicit-point exact-sign extension); and BenchmarkDotNet/SharpFuzz confirm their test-project membership on the manifest.
- Unlocks every C# spec, render-hash lane, benchmark, and fuzz harness against a provisioned rail: a parity drift surfaces as one corpus mismatch instead of N divergent re-derivations, the headless proof matrix unblocks (`T-HEADLESS-PROOF`), and a new law-matrix is a harness row on the kernel testkit node rather than a parallel oracle.
- Draws on the `libs/.planning` `ONE_WIRE_FIXTURE_CORPUS` seam (the shared C# corpus is one of its named consumers), the host-derived-confirmed `CANONICAL_BYTE_IDENTITY` bytes, and the `testing-cs` rail-class separation (static-managed laws, evidence rails, specialized benchmark/fuzz/architecture rails stay orthogonal). The `CLASH_GOLDEN` 8 `BoundingBox(min,max)` coordinate tuples are now PINNED in the kernel `Spatial/index#CLASH_GOLDEN` design (`NodeCount == 3`, the 160-byte LE stream, the 4-pair clash set), so the corpus freezes both fixtures and the two two-sided byte-equality specs remain the only test artifact.

[ROBUST_ARRANGEMENT_SUBSTRATE]: the kernel exact-arithmetic constrained-Delaunay/straight-skeleton arrangement is the single planar-and-volumetric substrate every AEC-domain geometry consumer composes, retiring the native CSG dependency through the fully-managed exact path.
- The kernel `Rasm.Geometry/tessellation#BOWYER_WATSON_DELAUNAY` constrained triangulation/tetrahedralization and the `Rasm.Geometry/offsetting#STRAIGHT_SKELETON` wavefront owner — both grounded on the `Numerics/predicates#INDIRECT_PREDICATES` LPI/TPI exact-sign floor and the `Spatial/index#GENERALIZED_WINDING` GWN inside/outside scalar — are the ONE arrangement the AEC-domain reads: `Rasm.Fabrication/Nesting/nfp#NESTING` and `Posting/projection` compose the Minkowski/offset and the silhouette arrangement, `Rasm.Materials/Construction/layout#ASSEMBLY_FOLD` reads the medial-axis spine for corner/arch placement, `Rasm.Bim/exchange` resolves solid boolean classification by tetrahedralizing the arrangement and classifying cells by GWN rather than a native mesh-arrangement asset, and `Rasm.Compute/Solver/discretization#DISCRETIZATION_MESH` seeds the volumetric mesher from the same constrained tetrahedralization.
- Unlocks the fully-managed exact boolean/offset/medial geometry with zero native-asset deploy burden — the kernel `Processing/repair#BOOLEAN_NATIVE_ASSET` UPSTREAM-BLOCKED row gains its managed companion body, the Fabrication `CSG_SILHOUETTE` and the Materials arch/medial conditions land on the kernel arrangement rather than four per-folder float-offset reinventions, and the boolean classification, the nesting NFP, and the solver mesh all read one arrangement keyed by the exact predicates.
- Draws on the constrained-tetrahedralization-as-arrangement technique (Cherchi/Attene exact mesh-boolean over implicit points) the landed LPI/TPI family makes managed, and the one-owner-per-runtime geometry law: the AEC-domain consumers align to `Build`/`ToMesh`/`Apply`/`SkeletonGraph` as settled wires, never coupling into the wavefront or `SimplexStore` interior.

[AEC_SIMULATION_BRIDGE]: a structural/thermal/energy analysis owner binding the Compute physics solver to the BIM element semantics and the Materials engineering properties — analysis as one solve over the federated model, never a re-modeled mesh.
- The Compute solver sub-domain owns the full design-space loop across `Solver/contract#SOLVE_FOLD` (physics×BC×element assembly over the `Solver/discretization` volumetric mesher), `Solver/optimizer` (design-space search with a surrogate reduced-order duality), and `Solver/sweep` (the N-dim DOE sweep under the frame-budget governor); the field substrate is the `Rasm/Vectors` DDG kernel, the numeric factorization rides the `Compute/Tensor/blas` provider table, and the neural-field surrogate evaluates through the `Compute/Model/inference` run-mode lane. It reads constitutive data from the `Rasm.Materials/physical-properties#PROPERTY_SETS` `MaterialProperty` mechanical/thermal/acoustic family and its analysis domain from the `Rasm.Bim/Model/query#ELEMENT_SET` `ElementPredicate` selection over the one `BimModel`, the boundary conditions ride the `Rasm.Bim/properties` Pset/Qto load/support sets, and `Rasm.Persistence/Query/federation#RULE_PLAN` joins the analysis receipt back onto the federated entity graph by `(GeometryHash, PropertyHash)` so a clash/QTO/IDS rule and a simulation result share one element identity.
- Unlocks AEC-grade analysis with no second model surface — a structural frame solves against the AISC `SteelSection` section properties the `Profiles/steel` table already carries, a thermal envelope solves against the `MaterialProperty.Thermal` conductivity rows, and the surrogate-accelerated DOE sweep scores thousands of parametric points against a learned error-bounded field instead of the full FEA/CFD solve, falling through to the true solve when the surrogate error bound is exceeded and scoring the digital-twin clash loop against the federated graph by reference; a new physics is one `solve-contract` row reading the same material/element vocabulary, never a parallel FEA package.
- Draws on the model-based-analysis convergence (IFC element semantics as the analysis domain, the EN 1993/ASHRAE constitutive tables as material data) and the 2025-2026 neural-operator/implicit-neural-field surrogate maturation meeting the settled Compute solver and the content-keyed result cache; the offline surrogate fit crosses as the `libs/.planning` graduation-evidence `model-asset` ONNX artifact, the C# side owning inference and harvest only, never an in-process training loop.

[OPEN_MATERIAL_STANDARD_WIRE]: the OpenPBR/MaterialX material-standard convergence as one branch-wide appearance interchange — minted once in Materials, carried on the BIM exchange wire, shaded in the viewport.
- `Rasm.Materials/Appearance/bsdf#OPENPBR_SLAB_LAYERING` lowers the closed lobe family from the OpenPBR Surface 1.1 stack-of-slabs and `Appearance/interchange#WIRE_PROJECTION` mints the `MaterialWire` OpenPBR-vector and the `MtlxDocument` MaterialX 1.39 node-graph once; `Rasm.Bim/exchange#TILE_METADATA` carries the material id on the per-tile `EXT_structural_metadata` band joined at the content key, the `Rasm.Compute/Runtime/codecs#TILE_PARTITION` tile pyramid content-addresses the material payload, and `Rasm.AppUi/Render/viewport#PATH_TRACE` shades the ReSTIR path-trace pass from the decoded OpenPBR vector — the OpenPBR algebra, the conductor IOR table, and the MaterialX schema owned once in Materials, every consumer decoding by reference.
- Unlocks a material that round-trips to any OpenPBR/MaterialX DCC tool and renders identically across the in-app viewport, the BIM exchange, and the web peer — a new measured material is one `MaterialLibrary` row that auto-carries to the IFC/glTF wire and the path tracer, never a per-surface re-mint; the measured spectral library and the MaterialX node interchange land on the same wire the viewport already shades.
- Draws on the OpenPBR Surface 1.1 (ASWF Aug 2025) and MaterialX 1.39.4 (Sep 2025) standard convergence the CG/AEC ecosystem adopted, the EPFL RGL goniophotometer measured-BRDF route, and the C#-mints/peers-decode wire law — a second OpenPBR or MaterialX mint in any C# consumer or peer is the named drift defect, the Python/TypeScript legs the `libs/.planning` cross-language decode seam.

[SYMBOLIC_PARAMETRIC_ALGEBRA]: a closed symbolic-expression algebra owner — the unfilled `Rasm.Compute/Symbolic/` sub-domain — that turns parametric formulas, dimensional unit laws, and analytic gradients into one CAS surface every numeric, solver, units, and persistence consumer composes by expression rather than a per-call delegate.
- A new `Rasm.Compute/Symbolic/expression` owner internalizes `MathNet.Symbolics` (the MIT F# CAS: `Expression` algebra, `Rational`/`Algebraic`/`Trigonometric` simplification, `Calculus.differentiate`, `Compile.compileExpression` to a `Func<double,double>`, and `Infix`/`LaTeX` projection) with `FParsec` owning the infix-string parse: the `5D cost catalog` formula rows at `Rasm.Persistence/Store/profiles` and the `RuleAst`/`Qto` derived-quantity formulas at `Rasm.Persistence/Query/federation` compose one parsed-and-validated `SymbolicExpr` instead of an `eval`-string or hand-bound lambda, the `Rasm.Compute/Tensor/quadrature#OWNED_BUILDS` quadrature/integrator owner reads a symbolically-differentiated Jacobian where the `Solver/optimizer` gradient-adjoint today demands a finite-difference or surrogate fallback, the `Rasm.Compute/Symbolic/units` boundary checks dimensional consistency by symbolic dimension rather than a frozen `QuantityFamily` row pair, and the `Rasm.Materials/physical-properties` constitutive curves admit a parametric temperature/strain law as one expression.
- Unlocks exact analytic gradients for the design-space optimizer (a closed-form adjoint where the surrogate is overkill and finite-difference is noisy), a parsed-and-typed cost/quantity formula DSL across the persistence cost catalog and the federation QTO rules with no string `eval`, and a symbolic dimensional-consistency proof the units boundary enforces before numeric admission — and it is the missing C# producer of the cross-libs `symbolic` graduation axis, so a Python `sympy`/`symengine`-derived analytic result graduates inward onto a C# `SymbolicExpr` the determinism log re-imports by content key, never a re-parsed string.
- Draws on the admitted-but-ownerless `MathNet.Symbolics`/`MathNet.Numerics.FSharp`/`FParsec` manifest rows — README-registered under `[SYMBOLIC]` and catalogued at `Rasm.Compute/.api/api-mathnet-symbolics.md` (the verified twelve-module surface: `Calculus.differentiate`, `Compile.compileExpression`, `Infix.parse`, `Rational`/`Algebraic`/`Trigonometric` simplify, `Evaluate.evaluate`), with no `Rasm.Compute/Symbolic/expression` design-page owner yet (the named gap is the owner, not the catalogue) — the content-addressed-build-graph law (a `SymbolicExpr` keys by its canonical `XxHash128`-hashed normal form so an identical formula reuses its compiled delegate), and the `libs/.planning` `ONE_GRADUATION_EVIDENCE` `symbolic` `HandoffAxis` arm as the inward seam. The catalogue's `[4]-[RESEARCH]` flag (the precise `Compile.compileExpression{,1,2,3}`/`compileComplexExpression` arity and `Func<>` return shape, plus the `SymbolicExpression` operator/conversion surface) is the only API confirmation the owner page must close before a compile fence transcribes.

[REALITY_CAPTURE_TO_BIM]: a scan-to-model pipeline binding the kernel registration/cloud algebra, the Compute splat/point payload, the BIM semantic graph, and the AppUi capture viewport into one as-built reconstruction pipeline — point cloud in, classified BIM elements out.
- The kernel `Rasm/Vectors` `Align`/cloud-ICP registration algebra and the greenfield `Rasm/Geometry/spatial` index register and segment the captured cloud, the `Rasm.Compute/Runtime/codecs` splat/point payload content-addresses the residency tiles, a planned `Rasm.Bim/reconstruction` arm folds the segmented planar/cylindrical primitives into `BimElement` rows with `ElementPredicate`-classified `IfcClass` and a confidence band on the `properties` Pset, the `Rasm.Persistence/Query/federation` graph joins the reconstructed element to its source-cloud lineage by `(GeometryHash, content-key)`, and `Rasm.AppUi/Render/reality` plays the capture back with the LiDAR-anchored measurable overlay over the reconstructed model.
- Unlocks an as-built workflow with no second geometry owner — the registration is the kernel's, the tessellation/splat payload is Compute's, the BIM semantics are Bim's, the lineage is Persistence's, the capture viewport is AppUi's; a new primitive-fit (plane/cylinder/torus/freeform) is one `ReconstructionPrimitive` row reading the kernel cloud algebra, never a parallel scan engine, and the offline RANSAC/learned-segmentation fit crosses as the `libs/.planning` graduation `reconstructed-mesh`/`topology-graph` `geometry` axis the C# side re-imports.
- Draws on the 2025-2026 scan-to-BIM/Gaussian-splat-reconstruction maturation, the one-owner-per-runtime geometry law (the kernel owns C# cloud registration, Python `open3d` is the offline peer producer, the splat payload meets at the content-keyed wire), and the existing `realitycapture`/`SPLAT_REALITY_CAPTURE` AppUi owner plus the `WEB_GEOMETRY_RESIDENCY_WIRE` residency manifest — the BIM `reconstruction` arm is the one new sub-domain that closes the cloud-to-semantics gap between them.

[DRAFTING_DOCUMENT_PRODUCTION]: one branch-wide construction-documentation rail — drawings, schedules, and reports — emitted from the federated model through the kernel hidden-line projection, the Fabrication HLR, the AppUi sheet engine, and the OOXML/DWG export surface, never a per-format document writer.
- The `Rasm.Fabrication/Posting/projection` BSP visibility kernel and the kernel `Rasm/Geometry` arrangement produce the 2D drawing geometry, the `Rasm.AppUi/Render/drafting` ISO/ANSI/JIS sheet-set + ASME Y14.5 dimensioning engine composes the sheet, the `Rasm.Persistence/Query/federation` `ElementSet` query and the `Rasm.Persistence/Sync/schedule` activity network feed the schedule/quantity tables, and the `Rasm.AppUi/Render/capture` OOXML/DWG/DXF export (ACadSharp/netDxf/DocumentFormat.OpenXml, admitted and catalogued at `drafting-export`) emits the deliverable — one drawing-and-schedule rail composing settled owners, the sheet owned once in AppUi, the projection once in Fabrication/kernel.
- Unlocks the actual AEC deliverable the platform exists to produce — a coordinated drawing set with live dimensions, a quantity takeoff bound to the federated model, and a 4D construction-state schedule report — each a projection over an existing owner, so a new sheet standard is one title-block row and a new export format one writer row, never a second drafting or document model.
- Draws on the admitted-but-thin `ACadSharp`/`netDxf`/`DocumentFormat.OpenXml` drafting-export surface (catalogued, lightly exploited), the AppUi `drafting` and Fabrication `Posting/projection` settled owners sharing the viewport camera basis, and the federated-model-as-single-source-of-truth law — the drawing is a view of the model, never a re-modeled document.

## [2]-[CLOSED]

(none)
