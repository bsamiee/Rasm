# [APPHOST_TASKLOG]

The open and closed work for the runtime spine, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup.

## [1]-[OPEN]

[T-MCP-LONG-RUNNING-CATALOG] [QUEUED]: Catalog the MCP server long-running surface so the `mcp-projection#STREAM_PROGRESS`/`SDK_PROGRESS` cards rest on `.api/`-verified members rather than a source-map. Verified against the pinned `ModelContextProtocol(.Core)` 1.4.0 XML/decompile: `McpServerOptions.TaskStore`/`SendTaskStatusNotifications` (Core), the `RequestContext<T>` request-context surface (`Params`/`MatchedPrimitive`/`JsonRpcRequest`/`EnablePollingAsync(TimeSpan, CT)`, the `McpServer` arriving through the `(McpServer, JsonRpcRequest, T)` ctor), and the `McpServer.GetTaskAsync`/`GetTaskResultAsync<T>`/`WaitForTaskResultAsync<T>`/`PollTaskUntilCompleteAsync`/`SampleAsync` (the `(CreateMessageRequestParams, CT)` and `(IEnumerable<ChatMessage>, ChatOptions, JsonSerializerOptions, CT)` overloads)/`AsSamplingChatClient(JsonSerializerOptions)`/`ElicitAsync`(`(ElicitRequestParams, CT)`)/`ElicitAsync<T>(string, RequestOptions, CT)` session verbs are real Core members used by the projection but absent from `.api/api-mcp.md`. Two attribution facts the catalog must record correctly: `WithStdioServerTransport(IMcpServerBuilder)` lives on `Microsoft.Extensions.DependencyInjection.McpServerBuilderExtensions` in the **`ModelContextProtocol`** host package (NOT Core, beside `WithTools`/`WithPrompts`/`WithResources`); and `.Server`/`.Services`/`.User`/`.Items` are declared on the `MessageContext` base type that `RequestContext<T>` extends, so the page records `MessageContext` as the declaring owner and `RequestContext<T> : MessageContext` as the inheritor — never `RequestContext<T>.Server` as a direct property. This is an INTERNAL verification blocker, not external: the assay binder adds these to `api-mcp.md` (extend the `McpServerOptions` property scope past `ResourceCollection` with `TaskStore`/`SendTaskStatusNotifications`, add a `McpServer` session-verb table + `MessageContext`/`RequestContext<T>` type tables, add the stdio builder entrypoint to the host-package `McpServerBuilderExtensions` row). Capability/packages: ModelContextProtocol(.Core), assay api. The `Microsoft.Extensions.AI` middleware-decorator catalogue (`api-extensions-ai-middleware.md`) and the MCP **client** federation surface (`McpClient.*`, the stdio/http/streamable transports, `McpClientTool`/`McpClientResource`/`McpClientPrompt`/`McpClientResourceTemplate`, `IMcpTaskStore`/`InMemoryMcpTaskStore`) are ALREADY catalogued, so the only residual `.api/` gap is this MCP-server long-running session leg. Closes when `api-mcp.md` carries the server long-running members at their true declaring types (`MessageContext` owns `.Server`/`.Services`/`.User`/`.Items`; the stdio builder lives in the host `ModelContextProtocol` package, not Core) and the `mcp-projection#[6]-[RESEARCH]` `[SDK_LONG_RUNNING]` source-map card drops its stamp.

[T-AGENT-TOOL-FEDERATION] [QUEUED]: Author `agent/tool-federation.md` — the missing **client** leg of the `agent/` sub-domain that consumes external MCP servers and folds their tools into the one capability registry. Capability: a `FederatedServer` `[SmartEnum<string>]`/row carrying the `IClientTransport` admission (stdio/http) and the trust scope; a `FederationProjection` fold that lists a peer server's tools via `McpClient.ListToolsAsync`, wraps each `McpClientTool : AIFunction` in a brokered `CapabilityDescriptor` under a `federated.{server}.{tool}` surface key, and admits them through `DescriptorSurface.Describe` so a federated call routes through `CommandAlgebra.Run`, the `GrantBroker`, the cost preview, and the `EventLog` chain exactly as a native op; a `FederationFault` `[Union]` in a fresh band; the structured tool-result projection back through `ToolResult`. Packages: ModelContextProtocol(.Core) (client surface — `McpClient.CreateAsync`/`ListToolsAsync`/`CallToolAsync`, `McpClientTool`, `StdioClientTransport`/`HttpClientTransport`/`StreamableHttpClientSessionTransport`, all already catalogued in `.api/api-mcp.md`), Microsoft.Extensions.AI.Abstractions, LanguageExt.Core, Thinktecture.Runtime.Extensions, NodaTime. Integration: composes `capability/registry#DESCRIPTOR_AXIS`/`COMMAND_ALGEBRA`/`GRANT_BROKER`, reuses the `agent/mcp-projection#TOOL_DISPATCH` `ToolResult` and the `determinism#EVENT_LOG` chain; the external server is reached over `OutboundHop` so its bytes ride the existing resilience. Considerations: a federated tool MUST NOT bypass the broker or mint a second catalog — the projection inverts `mcp-projection` (peer tool → descriptor vs descriptor → tool), and the agent transport remains the one registry. The peer-tool JSON Schema becomes the descriptor's argument schema, content-keyed for the SDK-codegen identity gate. Closes when the page carries the federation fold over the catalogued client surface and the descriptor inversion is fenced at one seam.

[T-AGENT-REASONING-RUNTIME] [QUEUED]: Author `agent/reasoning-runtime.md` — the in-process agent loop the registry implies but no page owns. Capability: a `ReasoningSession` over `IChatClient.GetStreamingResponseAsync` whose `ChatOptions.Tools` are the `CommandAIFunction` instances the MCP projection already mints (one tool-adoption seam, two front doors — MCP server and in-process chat); a `SemanticDiscovery` fold that indexes each `CapabilityDescriptor` op surface through `IEmbeddingGenerator<string, Embedding<float>>.GenerateAsync` and ranks descriptors by cosine similarity so `DiscoveryQuery` gains a `ByIntent(string)` case resolving intent to capability by meaning; the function-invocation transcript projected to `CommandReceipt` rows the `EventLog` chains and the `MacroEngine` records so an agent reasoning run replays bit-identically; an optional modal-input row (`ISpeechToTextClient`/`IImageGenerator`) reading the same descriptor catalog. Packages: Microsoft.Extensions.AI.Abstractions (`IChatClient`, `ChatOptions`, function-invocation, `IEmbeddingGenerator`, the catalogued modal clients — all in `.api/api-extensions-ai.md`, today single-member-exploited), LanguageExt.Core, NodaTime, System.IO.Hashing. Integration: extends `capability/registry#DISCOVERY_FOLD` with the `ByIntent` query case, composes `agent/mcp-projection#METHOD_AXIS` `CommandAIFunction`, chains into `determinism#EVENT_LOG`/`MACRO_ENGINE`. Considerations: the agent loop owns no second tool-adoption — it reuses the brokered `CommandAIFunction`; the embedding index is a frozen projection over the registry, not a runtime mutation; an agent reasoning trace is a replayable command sequence, never an opaque LLM call. The modal clients carry `[Experimental("MEAI001")]` and stay behind a feature row. Closes when the page carries the chat-loop, the embedding-ranked `ByIntent` discovery, and the replayable transcript.

[T-FEDERATION-RESOURCE-PROMPT] [QUEUED]: Deepen the planned `agent/tool-federation.md` beyond tool-only consumption so the federation folds a peer MCP server's resource, prompt, and resource-template surfaces — not just its tools — and subscribes to peer resource updates. Capability: extend the `FederationProjection` fold to admit `McpClient.ListResourcesAsync`/`ListPromptsAsync`/`ListResourceTemplatesAsync` rows as brokered `CapabilityDescriptor`s under `federated.{server}.resource.{uri}`/`federated.{server}.prompt.{name}` surface keys (a peer resource projects to a `read`-effect descriptor, a peer prompt to a `pure`-effect descriptor, mirroring the server projection's effect-class filter), and bind `McpClient.SubscribeToResourceAsync(uri, ct)` so a peer resource-change notification drains into the same bounded lane the live-wire subscriptions drain into, re-projecting the changed resource as one inbound command. Packages: ModelContextProtocol(.Core) (`McpClient.ListResourcesAsync`/`ListPromptsAsync`/`ListResourceTemplatesAsync`/`SubscribeToResourceAsync`, `McpClientResource`/`McpClientPrompt`/`McpClientResourceTemplate` — all catalogued in `.api/api-mcp.md`, today unexploited beyond `ListToolsAsync`/`CallToolAsync`), LanguageExt.Core, Thinktecture.Runtime.Extensions, NodaTime. Integration: extends `agent/tool-federation.md` (`T-AGENT-TOOL-FEDERATION`), composes `capability/registry#DESCRIPTOR_AXIS`/`GRANT_BROKER` and the `live-wire#TRANSPORT_BINDING` bounded-lane shape for the subscription drain. Considerations: a peer resource/prompt enters only as a brokered descriptor through the one registry, never a second resource catalog; the resource subscription is a read-shape variant on the federation fold, not a parallel notification handler; the `McpClientHandlers` notification registry is the one subscription seam. Closes when the federation fold admits the peer resource/prompt/template rows and the resource-update subscription drains into the one lane over the catalogued client surface.

[T-INDUSTRIAL-POLL-TRANSPORTS] [QUEUED]: Deepen `live-wire/live-wire.md` `[3]-[TRANSPORT_BINDING]` so the six poll transports stop being `IO.fail` stubs. Capability: real `PollRead`/`PollWrite` bodies per transport — REST over `OutboundHop.HttpApi` GET/PUT with the address as the resource path, GraphQL over an `HttpApi` POST carrying the query/mutation body, spreadsheet over a read-only range fetch (write rejected at the row), ERP-PLM over the vendor REST contract, Modbus over its bound client (register read/write), serial over the `CompanionSpawn` adapter's line protocol — each projecting its raw read into one `ExternalValue` and riding the row's `OutboundHop` resilience. Packages: BCL `System.Net.Http`/`System.Text.Json` over the existing `OutboundSurface`, `FluentModbus`/`System.IO.Ports` (the `modbus`/`serial` arms, admitted + catalogued under `T-FIELDBUS-PACKAGE-ADMIT`), LanguageExt.Core, NodaTime. Integration: composes `live-wire#TRANSPORT_AXIS` `OutboundHop` rows and `outbound/outbound-resilience` — no per-transport retry loop; the serial leg reuses the `companion#CONTROL_SERVICE` spawn-attach. Considerations: a new poll transport is one bound arm over its hop, never a parallel poller; the spreadsheet row stays non-writable; the `modbus`/`serial` arms depend on `T-FIELDBUS-PACKAGE-ADMIT` landing the manifest admission over the already-authored `.api/api-modbus.md`/`.api/api-serialport.md` catalogues. Closes when all eight transports carry a real binding and the stub-fault literals are gone.

[T-OPCUA-PUBSUB-LANE] [QUEUED]: Land the OPC-UA PubSub dataset leg in `live-wire/live-wire.md` as the high-throughput fan-in path beside the per-node monitored-item subscription. Capability: a `PubSubLane` composing `UaPubSubApplication.Create(IUaPubSubDataStore, ITelemetryContext)`/`Start`/`Stop`, the `SubscribedDataEventArgs` dataset-notification fan draining into the same bounded `Channel<ExternalValue>` the monitored-item path drains into, and the `MessageMapping`/`TransportProtocol` (MQTT-JSON/MQTT-UADP/UDP-UADP) row columns on `TransportRow` so a PubSub transport is one row variant, never a parallel transport. Packages: OPCFoundation.NetStandard.Opc.Ua.PubSub (`UaPubSubApplication`, `IUaPubSubDataStore`, `SubscribedDataEventArgs`, `TransportProtocol`/`MessageMapping`/`EnumMqttProtocolVersion` — catalogued in `.api/api-opcua.md`). Integration: extends `live-wire#TRANSPORT_BINDING` `SubscriptionLane`, reuses the one bounded lane and the `BINDING_HEALTH` lifecycle; the PubSub-over-MQTT leg shares the MQTT broker the `MqttLane` already binds. Considerations: one PubSub application per process (`UaPubSubApplication` lifecycle); the dataset fan crosses on the PubSub interval-runner thread and never runs the interior; the dataset path is a fan-in the per-item subscription cannot scale to, so it is a read-shape variant on the OPC-UA row, not an eighth transport. Closes when the dataset-subscriber leg drains into the one lane and the PubSub row columns are settled fence code against the catalogue.

[T-FIELDBUS-PACKAGE-ADMIT] [QUEUED]: Admit and catalogue the missing Modbus and serial-port client owners the live-wire transport axis names but the manifest never carried, then bind the `modbus`/`serial` poll-transport bodies in `live-wire/live-wire.md` `[3]-[TRANSPORT_BINDING]`. Capability: author the folder `.api/api-modbus.md` (decompile-verified `FluentModbus` — `ModbusTcpClient`/`ModbusRtuClient` `Connect(...)`/`ReadHoldingRegisters<T>(unitId, startingAddress, count)`/`WriteMultipleRegisters<T>(unitId, startingAddress, ReadOnlySpan<T>)`/`ReadWriteMultipleRegisters<T>`, the `ModbusEndianness` byte-order policy, the `Span<T>` register-window decode) and `.api/api-serialport.md` (`System.IO.Ports.SerialPort` — `Open`/`Close`/`ReadLine`/`WriteLine`/`ReadExisting`/the `DataReceived` event, `BaudRate`/`Parity`/`DataBits`/`StopBits`/`Handshake` line-policy properties); deepen the `live-wire#TRANSPORT_BINDING` `PollRead`/`PollWrite` `modbus`/`serial` arms so the register block read projects one `ExternalValue` (raw register, declared unit, good flag, source instant) and the serial line-frame parse projects one `ExternalValue`, both riding the row's `OutboundHop`. Packages: FluentModbus (ADMIT), System.IO.Ports (ADMIT), LanguageExt.Core, NodaTime, Thinktecture.Runtime.Extensions. Integration: binds `live-wire#TRANSPORT_BINDING` `modbus`/`serial` arms over `live-wire#TRANSPORT_AXIS` `OutboundHop`, the serial leg attaching through the `companion#CONTROL_SERVICE` spawn-attach; resolves the `live-wire#[8]-[RESEARCH]` `[TRANSPORT_CLIENTS]` "Modbus and serial client surfaces ride admitted packages catalogued at app-root creation" residual against the new catalogues. Considerations: a register window is policy data on the binding spec (start address, count, endianness), never a parallel Modbus poller; the serial line grammar is a binding-spec column, not a transport variant; the manifest-row deltas are returned to the orchestrator (single-owner `Directory.Packages.props`), the `.api/` and README registry written here. Closes when both catalogues carry the verified client surface, the README registry lists both packages, and the `modbus`/`serial` poll bodies are settled fences over them. Prerequisite for `T-INDUSTRIAL-POLL-TRANSPORTS` `modbus`/`serial` arms.

[T-GENAI-MODEL-GOVERNANCE] [QUEUED]: Author the `agent/reasoning-runtime.md` `[MODEL_GOVERNANCE]` cluster so every model call — the in-process reasoning loop and the MCP server-sampling leg — is metered, content-cached, and chained into the determinism event log. Capability: a `ModelGovernance` fold that composes the `IChatClient` through `ChatClientBuilder.UseDistributedCache(HybridCache)` (content-keyed response cache over the resources lane's `HybridCache`), `UseOpenTelemetry` (the GenAI `gen_ai.*` span the observability fourth-signal carries), and `UseFunctionInvocation` (whose tools are the brokered `CommandAIFunction`); a `ChatResponse.Usage`→`CostVector` projection charging `CostUnit.ModelTokens` through `GrantBroker.Admit` before the call and a response-digest `LogEntry` the `EventLog` chain content-addresses so a recorded reasoning turn replays from cache bit-identically. Packages: Microsoft.Extensions.AI (ADMIT — the concrete middleware package `ChatClientBuilder`/`FunctionInvokingChatClient`/`DistributedCachingChatClient`/`OpenTelemetryChatClient`; Microsoft.Extensions.AI.Abstractions stays for the contracts), Microsoft.Extensions.Caching.Hybrid, LanguageExt.Core, NodaTime, System.IO.Hashing. Integration: extends the planned `agent/reasoning-runtime.md` (`T-AGENT-REASONING-RUNTIME`), composes `capability/registry#GRANT_BROKER` cost metering, `resources/resource-lanes#HYBRID_CACHE`, `observability/diagnostics-and-telemetry#PROFILE_SIGNAL` GenAI spans, and `determinism/determinism-and-replay#EVENT_LOG`; reuses the `agent/mcp-projection#STREAM_PROGRESS` `SampleAsync`/`AsSamplingChatClient` server-sampling leg as the second model front door behind the one governance fold. Considerations: a model call is a content-addressed `LogEntry` keyed by the response digest, never a fresh non-deterministic draw — an identical prompt-under-context replays from the `HybridCache` content key; the middleware pipeline is the one model-governance owner, a second cache or a per-call OTel span beside the decorators being the deleted form; the concrete `Microsoft.Extensions.AI` admission is the named package gap the projection's `[BUILD_ORDER]` deliberately left unpulled, now required by the reasoning runtime — the `.api/api-extensions-ai-middleware.md` concrete-package middleware-decorator catalogue (`ChatClientBuilder`/`UseFunctionInvocation`/`UseDistributedCache`/`UseOpenTelemetry`, the three decorators, `UsageDetails` token counts) is already authored beside the `.api/api-extensions-ai.md` abstractions catalogue, so the only residual is the central-manifest admission of the concrete `Microsoft.Extensions.AI` package (returned to the orchestrator as a single-owner `Directory.Packages.props` delta; `Microsoft.Extensions.AI.Abstractions` already admitted). Closes when the `[MODEL_GOVERNANCE]` cluster carries the middleware fold, the token→cost→ledger projection, and the cached-response replay over the catalogued concrete package.

[T-RESILIENT-REPLAY-FRONTIER] [QUEUED]: Extend `determinism/determinism-and-replay.md` with three adversarial-reproducibility clusters. Capability: `[CHAOS_REPLAY]` records the `outbound/outbound-resilience` Simmy chaos decisions into the `EventLog` as deterministic fault-injection `LogEntry` rows so a recorded chaos campaign replays bit-identically (the injected fault is a log entry, not live randomness); `[DIVERGENCE_BISECT]` binary-searches the first divergent step over the content-hash chain in log-time rather than the current linear `ReplayVerify` walk; `[COUNTERFACTUAL]` replays a recorded log with one command's bound arguments overridden and composes `RecomputeGraph.Invalidate` over the perturbed input to return the downstream cone the change would alter. Packages: System.IO.Hashing (the chain key), LanguageExt.Core, NodaTime. Integration: composes `determinism#EVENT_LOG`/`REPLAY_VERIFY`/`RECOMPUTE_GRAPH`, reads the `outbound/outbound-resilience` Simmy chaos pipeline as the fault source. Considerations: every cluster composes the kernel's own owners — no second determinism surface; the chaos decisions are deterministic log entries the chain already orders; the counterfactual is the recompute graph applied to history, not a live edit; the bisection rides the tamper-evident chain so a found divergence is cryptographically pinned. Closes when the three clusters carry settled fences over the existing kernel owners.

[T-OPCUA-MANAGED-CLIENT-CATALOG] [COMPLETE]: Promote the `Opc.Ua.Client` managed-client cluster into `.api/api-opcua.md` so the `live-wire#TRANSPORT_BINDING` `OpcUaLane` fence rests on catalogue-verified members rather than the `[TRANSPORT_CLIENTS]` RESEARCH residual. Added the `Opc.Ua.Client` managed-client type scope (`Session`/`ISession`/`Subscription`/`MonitoredItem`/`MonitoredItemNotificationEventArgs`/`MonitoredItemNotification`/`ReverseConnectManager`/`ITelemetryContext`/`MonitoringMode`), the address-space/value primitive scope (`NodeId`/`DataValue`/`Variant`/`StatusCode`/`Attributes`/`WriteValue`/`ReadValueId`/the collections/`TimestampsToReturn`), the managed session/subscription/monitored-item entrypoint table (`Session.CreateAsync` 10-arg overload, `AddSubscription`, `Subscription(ITelemetryContext)`/`AddItem`/`CreateAsync`/`CurrentPublishingInterval` `double`, `MonitoredItem(ITelemetryContext)`/`Notification`/`DetachNotificationEventHandlers`, inherited `ReadAsync`/`WriteAsync`), the managed-value projection note, and the `[MANAGED_CLIENT_LAW]` rail clause. The live-wire `[8]-[RESEARCH]` `[TRANSPORT_CLIENTS]` OPC-UA residual now resolves against the catalogue; the page's `OpcUaLane` member spellings are catalogue-of-record. The PubSub binding stays open under `T-OPCUA-PUBSUB-LANE`.

## [2]-[CLOSED]

[T-API-COVERAGE-SWEEP] [FOLDED]: Dissolved — its two legs are not both pending. The `Microsoft.Extensions.AI` middleware-decorator catalogue is already authored (`.api/api-extensions-ai-middleware.md` carries `ChatClientBuilder`/`UseFunctionInvocation`/`UseDistributedCache`/`UseOpenTelemetry`, the three decorators, `UsageDetails` token counts), and the MCP **client** federation surface is already catalogued in `.api/api-mcp.md` (`McpClient.*`, the stdio/http/streamable transports, `McpClientTool`/`McpClientResource`/`McpClientPrompt`/`McpClientResourceTemplate`, `IMcpTaskStore`/`InMemoryMcpTaskStore`). The single residual — the MCP **server** long-running session leg (`McpServer.SampleAsync`/`AsSamplingChatClient`/`ElicitAsync`/`GetTaskAsync`/`WaitForTaskResultAsync<T>`/`PollTaskUntilCompleteAsync`, `McpServerOptions.TaskStore`/`SendTaskStatusNotifications`, `RequestContext`/`RequestContext<T> : MessageContext`, `EnablePollingAsync`, `WithStdioServerTransport`) — folds back into `T-MCP-LONG-RUNNING-CATALOG`, which is the live ledger. A coverage-sweep card duplicating an already-authored catalogue is the deleted form.

[T-DEEPEN-ALIGN] [COMPLETE]: Deepen-and-align sweep — closed the `JsonPatch.SystemTextJson` under-utilization by deepening `configuration#POLICY_VALUES` `OptionsAdmission.PatchSection` (RFC-6902 intake, section-keyed `revalidate`, the `companion#CONTROL_SERVICE` `DispatchPatch` route), `[PATCH_ERROR_SHAPE]` RESEARCH; synced the stale frontier (`SECRET_LIFECYCLE`/`FENCING_COORDINATION` moved to CLOSED as row clusters, `ARCHITECTURE` `coordination/`/`secrets/` rewritten to the "= ROWS" law).

[T-HOST-BINDING] [COMPLETE]: The §19 ports host-binding owner + macOS-correct peer-admission landed in `companion/companion-sidecar.md` — `[6]-[PEER_ADMISSION]` reads the peer over `Socket.GetRawSocketOption` (Linux `ucred`/macOS `xucred`), `[7]-[HOST_BINDING]` folds OS×ActivationSource×BindAddress into one `HostBinding.Acquire/Release/Identify` over a 9-row table; the systemd/launchd/SecItem/reuseport `[LibraryImport]` adapters are header-authored, never executed live, signatures RESEARCH.

[T-SECRET-LIFECYCLE] [COMPLETE]: The credential lifecycle landed as the `[5]-[SECRET_LEASE]` cluster on `configuration-and-options.md` extending `ConfigSource.SecretsStore` (not a `secrets/` page) — `SecretLease`/`Acquire`/`Renew`/`Zeroize`, `SecretFault` (4780-4783), `CryptographicOperations.ZeroMemory` under `DrainBand.Stores`, RID-dispatched credential store; native signatures RESEARCH.

[T-FENCING-COORDINATION] [COMPLETE]: Fenced single-writer election landed as the `[5]-[FENCING_TOKEN]` cluster on `time-and-deadlines.md` extending `SchedulePort` (not a `coordination/` page) — `FencingToken` `[ValueObject<ulong>]` with monotone `Next` + Kleppmann reject-lower `Admits`, the resource checking the token; the store fences at Persistence/server-tier, aligned not coupled.

[T-DESCRIPTOR-AUTOPROJECT] [COMPLETE]: The single-source descriptor→MCP→SDK auto-projection landed across `capability/registry.md` and `agent/mcp-projection.md` — one `CapabilityDescriptor.Of(...)` row drives the MCP tool, the three SDK renderers, and the dashboard catalog off one descriptor source; the two `WireFault` unions stay namespace-fenced, `ComputeIntent.Spec` arity + `SuiteContracts.Schema` digest the cross-page RESEARCH.

[T-MCP-SDK] [COMPLETE]: The MCP serving surface design landed in `agent/mcp-projection.md` — the registry projects through the official SDK (`CommandAIFunction : AIFunction` adopted by `McpServerTool.Create`, `McpDispatch`, `StreamProgress`); the SDK member spellings + 4640→`-32xxx` fault map are `[6]-[RESEARCH]`.

[T-PROFILE-SIGNAL] [COMPLETE]: The fourth-signal design landed in `observability/diagnostics-and-telemetry.md` — `TelemetrySignal` carries the trace/metric/log/profile set, `SpanProfileProcessor` links CPU profiles to the minted span, the `gen_ai.*` GenAI conventions ride the trace/metric signals.

[T-GRANT-ATTESTATION] [COMPLETE]: The signed-grant design landed across `capability/registry.md` (detached signature over canonical `GrantAttestation` bytes) and `determinism-and-replay.md#EVENT_LOG` (the hash-chain seat), closing the `T-CEDAR-BROKER` auditable-decision re-target.

[T-LIVE-WIRE-BINDING] [COMPLETE]: The OPC-UA + MQTT per-case `Read`/`Write` binding landed in `live-wire.md` `[3]-[TRANSPORT_BINDING]` — `TransportRows` dispatch the eight-arm switch to `OpcUaLane`/`MqttLane`, both foreign callbacks draining into one bounded `DropOldest` `Channel<ExternalValue>` so neither protocol thread runs the interior; MQTT verified against `.api/api-mqtt.md`, the high-level `Opc.Ua.Client` cluster `[TRANSPORT_CLIENTS]` RESEARCH (decompile correction: `CurrentPublishingInterval` is `double`).

[T-INDUSTRIAL-TRANSPORT] [COMPLETE]: The certified-transport design landed in `live-wire.md` — `[TRANSPORT_CLIENTS]` composes `OPCFoundation.NetStandard.Opc.Ua`(`.PubSub`) and `MQTTnet` behind the one `TransportRow` adapter over their `OutboundHop` rows.

[T-WASM-RUNTIME] [COMPLETE]: The WASM-runtime design landed in `sandbox/sandbox-host.md` — the wasm-component isolation row hosts on `wasmtime-dotnet` (WASI-Preview-2 import table scoped to the granted descriptors, `Store` fuel/memory quota cell), the `componentize-dotnet` WIT-binding distinction `[WASM_RUNTIME]` RESEARCH.

[T-CEDAR-BROKER] [DROPPED]: Route the grant-broker through Cedar — dropped with its parent idea; no Cedar .NET binding exists, the broker keeps its typed `PermissionShape`×`GrantScope.Covers` predicate, auditable-decision re-targeted to `T-GRANT-ATTESTATION`.

[T-DOC-MIGRATE] [COMPLETE]: Re-homed the 19 design pages into the single `.planning/<sub-domain>/<page>.md` tree, rebuilt `ARCHITECTURE.md`/`README.md`, authored `IDEAS.md`/`TASKLOG.md`.
