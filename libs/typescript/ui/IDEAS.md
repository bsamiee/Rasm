# [TS_UI_IDEAS]

Forward pool of higher-order folder concepts grounded in the interface domain and the monorepo purpose. `[1]-[OPEN]` carries the active ideas as cards; each card names the capability, what it unlocks, and the gap or technique it draws on. `[2]-[CLOSED]` carries the finished or dropped ideas with a one-line disposition so the same idea is never re-litigated. Ideas drive one or more `TASKLOG.md` tasks.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[SURFACE_COMPLETION]-[COMPLETE]: the SURFACES.md campaign landed whole and the file dissolved — six new view owners (`shell`, `status`, `content`, `media`, `canvas`, `presence`), the form roster census closed with `[WIZARD]`/`[CEREMONY]` clusters, the a11y charter on the primitive/act floor, and the token axes close-out; every category verified on disk before deletion.
[EDITOR_ENGINE]-[COMPLETE]: raw `prosemirror-*` admitted as the one editor engine — the `Content.Block` roster derives the PM Schema, the document codec, the DOM rules, and the wire form from one table; collab rides `prosemirror-collab` JSON Steps over the `Sequencer` port with generation-pinned refusal; quarantine is stored-decode-only; rejections and never-land rows (`yjs`, tiptap-on-gate, lexical, slate family) are `RULINGS.md` [01] rows.
[ANCHOR_ALGEBRA]-[COMPLETE]: `Anchor.Space<L, C>` rows (kind/surface/locator/resolve/carry/epoch) with the closed `pin|float|rail` posture vocabulary land at `view/presence.md`; content/canvas/media export space rows as composition VALUES; the ephemeral `caret` axis landed at `core/state/presence.md` both ends, and durable text anchors ride the content thread-mark row.
[CANVAS_ENGINE]-[COMPLETE]: `@xyflow/react` and `elkjs` admitted — `Canvas.useEdge` mirrors the engine through the one-writer adapter (engine keeps its own recognizers by ruling), elk solves ride revision-stamped proposals with `applied|superseded` accounting through the worker-only `elk-api` form, and the timeline composes `scaleUtc` × `scaleBand` after the whole gantt field rejected.
[SEAL_POSTURE]-[COMPLETE]: persisted-grain custody landed at `system/atom#STORE_ROOT` — stored state is a decode admission at one stable key from the `Store.key` mint with dot-free segment types, `Store.sealed` rides the generation inside the parcel, and the `hold`/`discard` residue column decides what a refused parcel leaves; every landed grain re-seated on the mint in the same pass.
[MATERIAL_UPGRADE]-[COMPLETE]: the card's `copy()` mechanism was REFUTED by execution (widening `.copy` throws mid-corrupt; `setValues` clobbers `type`/`uuid` and aliases `defines`) — the landed idiom is `MeshStandardMaterial.prototype.copy.call(physicalTarget, source)` with the `{ STANDARD, PHYSICAL }` defines re-stamp: scene `[08]` gained `_demands`/`_upgraded` with a `physical` column on all 36 `_ROLE_SLOT` rows, `[05]`'s `_dressed` upgrades in place and re-points the ledger, and `<material-unphysical>` narrows to genuinely unseatable cases; the idiom is a `RULINGS.md` `[02]` row.
[CELL_RANGE_SELECTION]-[COMPLETE]: `cellSelectionFeature` registered with its four-corner range slice persisted whole (drag is non-reactive instance data), `[05]-[GRID_SEMANTICS]` authored around `getTabIndex`/start/extend handlers, and `_EDGE` folding physical edges onto logical `Grid.Edge` once before any class; the selected band reaches export's `Rows` source and the scene echo through the identity meta.
[SPANNING_CELLS]-[COMPLETE]: `cellSpanningFeature` registered with role-driven `spanRows` on category columns, the covered-cell skip law (`rowspan="0"` is span-to-end) with its recompute boundaries in `[05]`, and the anchor-row measurement law in `[06]`.
[FOLD_STORE_BINDING]-[COMPLETE]: `Grid.edge` mints the `@tanstack/store` adapter Atom over the effect-atom fold's cell (`get`/`set`/`subscribe` all delegate), estate-owned slices bind through `options.atoms`, transient slices stay on `baseAtoms`; the one-writer law is a `RULINGS.md` `[02]` row and `system/atom.md`'s foreign-store crossing law.
[GRID_SORT_VOCABULARY]-[COMPLETE]: the registry stays domain-blind — one generic `rank` entry via `constructSortFn` (`resolveDataValue` normalizing absent ranks to the bottom), review's phantom `reviewChange` key deleted with `_rank` now one owner for board order and column sort; the rank-as-data direction is a `RULINGS.md` `[02]` row.
[PIVOT_WORKSPACE]-[COMPLETE]: `Chart.pivot` widened to the workspace grain — `_workspace`/`_panel`/`_saved`/`_board`/`_moved` over the closed `Chart.Move` family, `Chart.Origin` arms naming their tables, the `_Config` Schema + `rasm.ui.chart.workspace` kvs key realizing the persisted atom, panel.md's board composing the grain whole; the element-grain persistence law is a `RULINGS.md` `[02]` row.
[PIVOT_WINDOW_COLUMNS]-[COMPLETE]: `Chart.Windows` landed beside the expression gate with the ordered-entries local gate (schema ∪ expression aliases ∪ sibling keys, frame-trio exclusivity, JSON last-wins caught before the engine), the engine refusal roster and `Features.window_aggregates` transcribed, and `window-refused` joining the fault family as `invalid`.
[ENGINE_HEAP_CEILING]-[COMPLETE]: `Chart.boot(bitness)` is the one module-scope registration owner ahead of the bracket — thunk pair into `init_server({wasm32, wasm64})`, lone-wasm64 honored regardless of the memoized probe, warn-and-fallback only when wasm32 co-registers, download deferred to first `worker()`; the shipped Memory64 binary raises the heap ceiling 4GB to 16GB.
[ZERO_COPY_FRAME_LANE]-[COMPLETE]: `Chart.Lend` + `_borrow` compose `_snapshot` so the lend inherits the one view lifetime and never becomes a stream element; settlement ends the borrow, `float32` narrowing (Date32/Timestamp exempt), zero-filled nulls, and the forced `__ROW_PATH_N__` names all recorded as traps.
[SVG_MARK_MOTION]-[COMPLETE]: motion arrives as the `motion.create` wrapper over the visx mark under the `isValidProp` subtree gate — `Chart.Target` carries only the WAAPI-accelerated pair (opacity + inner-frame clipPath) with a spring row off `system/act`'s continuous plane, `Chart.Lens.series` now cutting per-series marks through `_marked`; interpolating `d` is rejected with reason.
[POINTER_INVOCATION]-[COMPLETE]: overlay `[04]` gained `scope` on `Overlay.Command` with `Overlay.scoped` the one selector and select-then-open keeping `run` nullary; the positional surface is the anchored host's point reference (`Overlay.point` virtual anchor beside RAC `MenuTrigger` at `trigger: "contextMenu"`), never a fourth host; act `[03]` gained the unified invocation gesture (`useContextMenu`, long-press structurally exclusive on iOS).
[COMMAND_KEYMAP]-[COMPLETE]: `binding` column on `Overlay.Command` — `Overlay.bindings` projects into `useKeyboard`'s `KeyboardShortcutBindings`, `Overlay.chord` yields display tokens, and `Overlay.commands` refuses malformed chords and colliding normalizations with both `Mod` expansions censused apart; the Control-never-Ctrl parser trap is a `RULINGS.md` `[01]` row.
[TOKEN_FIELD_VALUE]-[COMPLETE]: the alpha blocker was refuted on the installed rail — the TokenField family ships stable-named at all three tiers — and the field row landed in form `[03]` with the `Tokenized` subclass owning live segmentation (`tokenize` seam) and `Form.TokenCodec` the one `Schema` codec at the commit boundary.
[GPU_GLYPH_RENDER]-[COMPLETE]: geo `[09]` `Glyph.Row` gained the `Drawn` arm with `Glyph.Kit` per-context handles rebuilt from the descriptor on every draw (context loss re-adds without `onAdd`), the fixed-slot, cadence, and scissor-carve laws, behind the same `hasImage` guard; the maplibre catalogue carries the `StyleImageWebGLData` contract.
[POINT_CLOUD]-[COMPLETE]: scan and survey clouds are first-class — geo's `_scan` LAS lane (point-record-format descriptor, roster-served worker) and `_depth` eye-dome row, mark's BVH-accelerated pick pipes (`firstHitOnly`, `_marqueeScene` volume descent, one `Selection.picked` family), scene's `_accelerate` visitor with the serialize band, and the data plane's `points` category (`Asset.Lod` decimation/order/prune rows) close the chain producer to pick.
[TONE_VOCABULARY]-[COMPLETE]: `token.md` `[03]-[TONE_VOCABULARY]` declares the closed eight-tone roster (`neutral`/`accent`/`success`/`caution`/`danger`/`added`/`removed`/`changed`) with hue+chroma rows, one perceptual ramp policy, APCA-floored pair guards, and the dark plane derived by reading the same ladder reversed; every surface `_tone` table (panel, mark, probe, vital, primitive, review) now keys `Theme.Tone` and the five divergent inline unions are gone.
[ASSET_CACHE]-[COMPLETE]: `system/cache.md` stands the content-keyed OPFS residency cache — band ledger, integrity gate on read-back, quota sweep over the transcribed `Opfs.Budget` vocabulary, the multi-leaf `bvh` snapshot band under the geometry's own `ContentKey`; egress stays `view/export.md`'s per the recorded boundary.
[DIFF_SURFACE]-[COMPLETE]: `viewer/review.md` joins decoded `Wire.ModelDiff` and `Wire.BcfTopic` per `GlobalId` into grid, selection, and camera projections.
[EXPORT_PLANE]-[COMPLETE]: `view/export.md` is the one export owner — `_formatRows` vocabulary, `Export.Parcel` Schema owner, the `_MATRIX` mapped serializer record making an illegal source-format pair a compile error, `ExportFault` over `FaultClass.family`, the `Egress` capability Tag beside the clipboard port, and the `RecordBatchWriter.throughDOM` streaming lane.
[GEO_REASON_SPLIT]-[COMPLETE]: `tile-unreachable` classifies `unavailable` beside `frame-refused` at `malformed` on `_geoFamily`, and the scan lane's fused fetch-and-decode gained the `_scanFault` `Match.instanceOf(FetchError)` triage so a transport failure is never quarantined as a payload defect.
[ASSET_IDENTITY]-[COMPLETE]: `Glb.AssetRoster` owns the slug-unique served-asset identity, `Glb.asset` resolves one row with `codec-absent` on absence, `Glb.assetPath` derives the immutable file address, and `Glb.assetDir` the one-digest directory form multi-leaf decoders consume.
[ENVIRONMENT_DOME]-[COMPLETE]: `viewer/scene.md` `[06]-[ENVIRONMENT_FOLD]` folds container-sniffed HDR/EXR arrivals through one backend-matched prefilter per arrival — keyed slot with the analytic floor as its boot row, read policy at the scene fields, producer-agnostic across both set-manifest producers.
[DOME_IRRADIANCE]-[COMPLETE]: the dome slot carries the producer's nine-band irradiance beside its GPU handles and answers directional queries by moving the BASIS at the read, so the wire bands stay verbatim, the prefiltered dome keeps sole ownership of the diffuse term, and the analytic probe row loses its double-counting claim.
[DOME_COMMIT_UNIFICATION]-[COMPLETE]: one commit owns every dome write and three entries reach it — a key change decodes and prefilters, a repeat re-reads stored-frame policy over carried handles, a backend loss re-derives the target — with retirement identity-keyed so a torn dome is unreachable rather than guarded, and each container row owning its whole decode so a callback-shaped deep store rides the lane HDR and EXR already ride.
[HOOK_RAIL]-[COMPLETE]: `.planning/system/hook.md` and six owning endpoints carry the open `Points` seam and runtime rows; veto consultation is payload-selected, adopted sources merge before registration, publish is bounded, and tap faults isolate.
[VITAL_PLANE]-[COMPLETE]: `.planning/system/vital.md` owns the browser evidence this floor can see — bounded LoAF and event-timing windows over one multi-part measure table, id-keyed Profiler windows, compiler diagnostics — publishes every row through the replay hook, and spells the runtime plane's grade vocabulary for tone alone; Core Web Vitals collapsed onto `runtime:otel/vital`.
[SIGNAL_WEAVE]-[COMPLETE]: observe seams land at their owners — AtomRpc `spanPrefix`, Form's definition-seam preflight/outcome trip, chart pivot span/frame count, and scene's spanned graft metrics with one adopted residency-fact stream.
