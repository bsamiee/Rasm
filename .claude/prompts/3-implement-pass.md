GOAL: We are going to implement ALL small/medium tasks and 1-3 "big" tasks from each category (per folder, per language, lib-wide, etc), properly, IDEAS are larger units, and we must identify the appropriate way to integrate them, as they inherently create questions on tasks, touch points, etc... so the workflow sequence MAY require iterative passes at scopes to do tasks starting at folder level (atomic/smaller), then per language, and REFINING the tasks above in topology as we go, to then finalize them, afterwards, to then go with ideas, to identify how to do that properly.

Note: The "big" tasks are up to subjective understanding, usually, it's for new "files". Tasks are OUTLINES, not fully details for files, so we must make the file 12/10 in quality out of the gate, to identify not just the exact task statement for the file, but all the logical things it will also provide, potentially, and how that integrates with the folder, the language wide packages if relevant for non-coupled alignment, and cross-lib, if relevant. Which requires deep understanding of all external libs available in that folder + cross-cutting in the language, to ensure we never underpower/underutilize anything for new files, as well as existing. Do not state a small/medium as being "big" we want to be ambitious, but still maintain ultra-high standards that confirms to the FLOOR of quality as seen in `docs/stacks/csharp/` (all files, equivalent for all languages, use this for all languages as benchmark of density/complexity/richness, and collapsing of code to parameterized/complex objects, never loose/flat objects/tyes, always unified rails, etc...)

Never commit, do not worry about commiting, focus on implementing all the work, this is a large series of work, workflows, and requires YOU to maintain excellent context management, delegate with Sonnet agents initially for broad exploration/understanding, and just list directories, read the critical planning docs/campaign docs, but otherwise keep context THIS IS PRIMARILY FOR BEHAVIOR AFTER LEAVING PLAN MODE; the plan MUST state that you WILL read the full plan, and not waste time re-litigating items, the plan if made right is the blueprint, and shouldn't require additional exploration/questions, if it does, fan many smaller/tightly scoped Sonnet agents. For all work that is in any way serious or durable, use Opus.

ANYTHING MARKED [BLOCKED] will be investigated/researched NOW, for py related blockers, read pyproject.toml + our `tools/assay/README.md` UNDERSTAND that we have a full provisioning tool to confirm/check things, during plan mode especially. Our Parametric_Forge project also provides py substrates for us on packages that we couldn't add due to our hard requirement of being on py 315, though we can version floor other tools as needed, READ:

`/Users/bardiasamiee/Documents/99.Github/Parametric_Forge/modules/home/programs/languages/python-tools.nix` + `/Users/bardiasamiee/Documents/99.Github/Parametric_Forge/modules/home/programs/languages/scientific-tools.nix`

Anything we can personally identify how to unblock, we will do, to also implement ideally, few are truly "resolvable", for cross-cutting blockers, requiring work in other folders, language, etc we properly account for that, which requires intelligent sequencing of work, or, proper details/allocation so agents working in parallel will still build properly, and that is why we have multiple phases of refinement within a workflow to align/improve/correct things like this, as well as other considerations.

[IMPORTANT]: We MUST maximize the entire planning mode time we have; ALL changes must be exhaustively reviewed, planned, and made with clear changes in ALL planning directories/files. We must have 4+ pages of planning content, during planning mode (we are in it right now) we can make local/tmp artifacts if needed with changes, additions, and ensure the plan we have has no gaps. When we leave the planning mode, we MUST have everything already identified to do, we do NOT want to do more research that we can't do now in planning, once we leave planning mode, everything needs to be identified to be done in exhaustive detail. The plan MUST be made with critique + red team passes for all the major phase work, and refined/pushed in quality, consideration, and detail, the expectaitons of our plan is 4x higher than what you think; do not waste the planning mode, we must use it for extreme detail, refinement/hardening of all content we will do this session. The plan must be created in a structured, detailed way, ALL work that we do should be done with workflows WHILE WE ARE IN PLANNING MODE, AND ONCE WE LEAVE IT, you are an orchestrator as much as possible, doing personal work only where surgical/needed. At the start of leaving the planning mode, you MUST re-read the entire plan + all artifacts we made, if any, create a proper task list. ALL agents/workflows/teams must re-read the plan for their phase work, fully, and they MUST read `docs/stacks/csharp` regardless of language target, understand these files, our `docs/stacks/csharp/README.md` for code doctrine, and other files in root of `docs/stacks/csharp` the code standard here is the FLOOR, ts/py code if we touch/consider it is far far weaker, and always needs refining/hardening to reduce surface, reduce object/shape/types with ultra-advanced/modern/bleeding-edge replacements to reduce LOC + surface + objects without losing functionality. This is a concern at all levels, we are NEVER making code files, we ALWAYS work on design docs, we can improve/refactor all tooling + configs at repo root like `pnpm-workspace.yaml`, `pyproject.toml`, `NuGet.config`, `global.json`, `Directory.Packages.props`, `Directory.Build.targets`, `Directory.Build.props`, and `.config`, etc... external packages/tooling is a big concern of ours, always refining/improving, adding most advanced/new tooling/configs when possible, and updating docs for them to be integrated (.api/ folders if needed, readme manifests, root repo docs if needed, etc)

THE PLANNING MODE CANNOT BE LEFT UNLESS IT IS EXHAUSTIVE, DETAILED, ADVANCED, COMPLEX/RICH, NO FLUFF/CHAFF, ALL CONSIDERATIONS/TASKS FULLY INVESTIGATED, RESOLVED, CRITIQUED/REFINED + RED-TEAMED

[CRITICAL]: ALL CRITIQUE, AND REDTEAM AGENTS IMPLEMENT CORRECTIONS/REFINEMENTS AS THEY FIND IT, NOT LEDGER NOTES FOR YOU TO DO, WORKFLOWS SHOULD BE SELF-CONTAINED, CREATE THE WORKFLOWS WITH ALL PHASES YOU BELIEVE ARE BEST, JUST ADD THESE MANDATORY PASSES; PRIOR TO FINALIZING THE PLAN FOR PRESENTATION, USE MULTIPLE SUB-AGENTS (OPUS) TO RE-READ ALL PLAN, TO OPTIMIZE IT, ENSURE NO GAPS/MISTAKES, STRUCTURE OPTIMIZATION, INCREASE SIGNAL/RESOLUTION FOR VALUE, AND HELP WITH REFINING ALL WORKFLOW SCRIPTS IF MADE DURING PLAN MODE, TO ENSURE ALL CRITIQUE/REDTEAM ARE LOOKING AT ACTUAL/IMPORTANT ITEMS NOT NONSENSE/HOUSEKEEPING, BUT HIGH VALUE IMPROVEMENTS/CORRECTIONS/REFINEMENT/ALIGNMENT NEEDS.

[IMPORTANT]: Start with the `libs/.planning/` for ideas + tasks first, focusing on first identifying ALL tasks across all libs, languages, per folder, we MUST identify ALL changes per scope, so that our plan is comprehensive, and has all instructions ready to be deployed, if possible create a DURABLE tmp file to have as reference for workflows. In any event, we need to craft the plan with this folw, so that we don't trample/cascade task closeout incrrectly. 

[IMPORTANT]: During planning phase we MUST identify ALL `IDEAS.md` + `TASKLOG.md` files, understanding topology flowfrom `libs/.planning/` to language specific to folder specific entries. These are the starting point for all work we will do, however, we are NOT limited to just these, tasks call-out work needed, but not the specifics or minutia of detail; WE DO THAT NOW, IN PLANNING MODE, FOR ALL TASKS. Ensure we understand all "big" ideas from top to bottom in topology to also guide a coherent thread of work across languages + folders, the central planning folder to language specific. ALL folders MUST be improved/refined regardless, however, there is another set of workflows for "big" idea from central, to language, to folder specific idea implementations. We MUST define ALL work in exhaustive detail NOW in planning mode, once we leave planning mode, there must be NO ad-hoc work done in design docs, it's all pre-defined, and only adjustment allowed is objective improvements, do not waste a phase 0/1 on another "exploration" pass, we should have that done before leaving plan mode.

[IMPORTANT]: Server and native closure evidence comes from Assay `provision` reports. Planning prompts consume that evidence when a task needs live PG18, extension, DuckDB, or scientific-native facts. Read `tools/assay/README.md` for provison tool usage, ensure agents that need it are aware of it and how to use it.

[IMPORTANT]: For all workflows, it is 1-2 agents PER folder, the per folder pass, language-wide pass (not 1 agent for multiple folders), lib-wide pass (multiple agents per language) with synthesis and orchestration handled properly, never overload any agent to read more than 1 planning folder. Never burst a workflow, scope properly to have a phase slowly unroll agents (mainly a concern for lib-wide pass)

[PASS] IMPLEMENT over the `libs/` planning corpus. IMPLEMENT realizes open tasks and ideas into deep code fences, mines every admitted package to its full capability, and crushes surface sprawl into fewer, richer, optimized owners with zero functionality loss.

[METHOD] The campaign method, stated inline so the task is self-contained on entry; the routes below carry the full law and the complete doctrine.
- Surface: work ONLY on the `.planning/` design corpus — design pages and the transcription-complete code FENCES inside them — plus the four index docs per tier, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain. A FENCE is a markdown fenced block, the work product itself, never a `.cs`/`.py`/`.ts` source file.
- Bar: world-class library code — dense and polymorphic, one owner per axis, one rail family and one entrypoint per concern, total dispatch, no wrappers/shims/aliases, every admitted package mined to its full capability through the owning axis, capability conserved absolutely (a collapse proves every case, field, and method survived). Files are as large as the concern requires. `docs/stacks/<lang>/` is the floor and `docs/stacks/csharp/` the universal parity bar every branch meets whatever its language.
- Principles: one unified body of functionality — a new capability is a row, case, or column on an existing axis, never a parallel surface; full modality completeness from the root; everything parameterized; plug-in-play composition aligned only at the wire/companion seams, never coupled to a sibling's interior.
- Tiering + stages: run as fanned workflows across three altitudes — per-folder, then a full per-language pass, then the cross-`libs/` master, run BOTTOM-UP so each higher tier deepens a settled lower one. Each higher tier is a FULL pass at broader scope (the same research → produce → critique → red-team depth), never a thin reconcile: it deepens and aligns the settled lower tier, adds the fences and tasks that only emerge at that altitude, and applies the boundary-integrity check — a concern owned twice within a runtime, a folder mixing unrelated concerns, a concern scattered across folders, or a cross-folder/cross-language touchpoint coupled to a sibling's interior is a defect it fixes or records. Producer → critique → red-team, every stage applying its own fixes; a single un-adversaried author is never the final word.

[TARGETS]: READ ALL OF: `libs/.planning/planning-targets.md`
- Contains all planning infrastructure folders that this session will target (lib scope, language scope, per folder).
- Read: `AGENTS.md` (repo root)
- Read: `README.md` (repo root)

[ROUTES]:
- `libs/.planning/campaign-method.md` — the full law behind `[METHOD]`: the campaign scope, the bar, the universal principles, the IMPLEMENT pass, the workflow orchestration, the loop.
- `libs/.planning/architecture.md` — the strata, dependency direction, geometry/IFC flow, and per-language roles; every fence lands inside the folder -> language -> repo framework, never as a random addition and never mixed-sourced.
- `libs/.planning/README.md` — the design-page grammar and the signature law (transcription-complete, comment-free fences), the doc-set, the notation.
- `docs/standards/information-structure.md`, `formatting.md`, `style-guide.md` — agent-first form for the surrounding pages; code-as-agent-only inside fences.
- `docs/stacks/<lang>/` for the tier's language is the governing code doctrine, read in full at every stage; `docs/stacks/csharp/` is the universal density bar every branch matches whatever its language. A fence is held to its own language doctrine first and the C# bar second.
- The branch and folder targets named by the session, each target's own `<pkg>/.api/` catalogues, and the admitted packages themselves.

[SCOPE] Push content into every existing file and add files and folders as the framework warrants, realizing the open tasks and ideas. Planning is thorough so the corpus knows exactly what to build: read every external package the target admits — its `.api/` catalogue and its real code — find where the owners do not yet capture the package's full capability, then deepen the existing owner's fence body or add a new owner, file, or sub-domain to capture it. A fence is the densest form that stays transcription-complete: an implementer copies it verbatim, so it is never over-compressed past that point. Read the folder's IDEAS for the concept and intent and its TASKS for the actionable spec; ideas are not actionable, tasks are. Realize the tasks, and close an idea only when its full concept is captured in fences, not merely its narrowest tasks. On closing a task or idea, compact its card to a one- or two-line `[CLOSED]` entry — the high-level intent plus the detail needed to recognize it — so the closed history stays scannable, never a verbose log.

[WORKFLOW] Construct the pass as fanned workflows, never one-off agents. Bottom-up tiering: folder, then a full language pass, then the cross-`libs/` master — each higher tier a FULL pass at broader scope (the same research/implement/critique/red-team depth), never a thin reconcile: it deepens and aligns the settled lower tier, adds the fences and tasks that only emerge at that altitude, and improves the lower tier's owners where the broader view raises value. Each workflow chains research -> implement -> critique -> red-team, every stage ULTRA-HARSH; ambitious synthesis runs producer -> red-team -> merge, never a single agent. Assign a capable model to authoring, red-team, and synthesis and a lighter model only to exploration and decompile reads. Keep concurrent heavy workflows at two or three. The orchestrating session is the sole ledger writer and never runs git from an agent. The same loop runs for every language — C# is the density bar, the Python companion and the TypeScript platform each deepen their own native owners against their own doctrine first and the C# bar second, consuming a sibling only at the wire. 

[HUNT] At implement, critique, and red-team alike, from multiple facets:
- Under-captured capability: an admitted package whose `.api` and code expose capability no owner exploits is a named gap, closed by deepening an existing fence or adding one.
- New concepts: a capability the package or the domain admits that the corpus has not conceived becomes a new fence within the framework; when it is a bigger concept it is recorded as a CLOSED idea only if its fences land this turn, otherwise it is left as an open idea or task for IDEATE rather than ideated top-down here.
- Surface sprawl: parallel types, enums, methods, or near-duplicate shapes collapse into one parameterized owner in the language's own collapse vocabulary — a tagged/discriminated union, fold algebra, dispatch table, or the language doctrine's generated-owner form — that replaces them with no functionality removed; fewer objects, richer ones.
- Rail unification: one entrypoint family per rail, one fault family per package, total dispatch, unified in the fences without bleeding a rail/axis/lane term into an ARCHITECTURE sub-domain name.
- Optimization: correctness first, then performance and sophistication — allocation, span/SoA layout, dispatch shape, algorithmic complexity — not only line-count reduction.
- New work surfaced: api gaps, newer or stronger packages, and tasks the implementation exposes are realized or recorded the same turn. Adopting a replacement or admitting a new package extends the canonical owner first — never a parallel surface — updates the README registry and the central language manifest, adds the folder's `.api/<package>.md` catalogue, and introduces no per-folder manifest.

[BOUNDARY] IMPLEMENT realizes; it does not run the top-down ideation or coverage-audit task-emission that belongs to IDEATE. Every fence is authored INSIDE a markdown design page under `.planning/`; IMPLEMENT writes no source file (`.cs`/`.py`/`.ts` and the like) and lays down no source tree — to realize a task is to author or deepen its design-page fence, never to transcribe that fence into code. Source generation is a separate future project outside this campaign's passes that the session never enters and defines no kickoff or pass for; every pass operates only on the `.planning/` design corpus, the four index docs, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain, so a "file" or "folder" IMPLEMENT adds is always a design page, sub-domain, or its `.api/` catalogue, never a source file. A new design page follows the page grammar — H1, lead, numbered sections, each cluster a card then transcription-complete comment-free fences then at most one Mermaid — and every external member is fence-written only after the folder's `.api/` catalogue verifies its spelling; an unverified member is a RESEARCH item, never prose or an invented signature. Every collapse conserves capability: every case, field, and method survives the denser owner. Density and rail-unification stay inside the fences and never bleed a rail/axis/lane term into an ARCHITECTURE sub-domain name.

[BAR] A high-value IMPLEMENT turn leaves every owner capturing the full capability of every package it admits, every sprawl collapsed into one denser owner with no capability lost, and every fence transcription-complete against the verified `.api`. The critique pass guards capability conservation and density; the red-team pass attacks every fence for a surface that could still collapse, a thin wrapper, a silent functionality drop during a refactor, a missed package capability, or a framework violation, and fixes each in place.
