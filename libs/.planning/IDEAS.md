# [CROSS_LIBS_IDEAS]

Cross-language ideas only — concepts that span two or more of C# / Python / TypeScript at the wire and the companion/offline seams, distilled from the three branch concerts rather than re-aggregated from branch content. Each idea is a card grounded in the wire law: a bracketed slug, the capability, what it unlocks, and the shared-owner seam it draws on. A concept that couples packages within one language lives in that branch's `IDEAS.md`, never here. `[1]-[OPEN]` holds the live concert; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[THE_CONCERT]: the flagship tri-language pipeline — one model authored in the C# host suite, computed across the Python science stack, and surfaced live in the TS web/edge platform.
- A model sealed in `csharp:Rasm.Persistence` emits its canonical wire shapes once; the `python` companion recomputes them offline and the `typescript` web platform renders them, every leg decoding the one C#-owned wire vocabulary and reproducing the one content-identity seed bit-identically.
- Unlocks three peer libraries meeting only at the wire contracts and the companion/offline seams, never at one another's interiors — a browser SPA, a node backend, and an offline companion that never hand-write a per-language client.
- Draws on the shared-owner law: content-address identity, the proto wire vocabulary, the suite wire law, the op-log CRDT payload, the capability descriptor + SDK source, tenant/causal identity, the GLB tessellation rail, and graduation evidence are each named once and owned once across the three branches.

[ONE_MODEL_THREE_FACES]: one sealed model emits snapshot, op-log delta, and gRPC once; three runtimes decode the same descriptor source.
- The `csharp:Rasm.Compute` suite wire (`Runtime/channels#PROTO_VOCABULARY`/#FAULT_PROJECTION) and `csharp:Rasm.Persistence` snapshot/op-log surfaces are the one wire vocabulary; the `typescript:interchange` codec rail decodes it over grpc-web and the `python:runtime/transport/serve` companion decodes the same proto, neither minting a parallel.
- Unlocks a single canonical model shape every consumer reads — a re-decode in any runtime is field-for-field identical, so a new face is a decode row, never a second wire mint.
- Draws on the C#-owns-the-wire / peers-decode law: a second mint of a shared wire shape in any branch is the named cross-language drift defect.

[ONE_CONTENT_IDENTITY]: the `XxHash128` content-address seed minted in C# is reproduced bit-for-bit in Python and read in TypeScript.
- `csharp:Rasm.Compute/Runtime/codecs#CONTENT_ADDRESSING` mints the seed (seed zero, two-64-bit-half order); `python:runtime/evidence/identity` reproduces it through `xxhash` and `typescript:interchange` reproduces it through a 128-bit wasm hash, each keying its own most-expensive output by that same key.
- Unlocks fetch-by-content-key across runtimes — a Python-recomputed artifact reuses a C#-computed sub-result and a TS-assembled blob is byte-addressable identically; the gap is proving byte-identical reproduction against the one seed in each branch. The parity legs are package-gated: the TS leg is admitted through `hash-wasm` (retiring the 128-bit-incapable `xxhash-wasm`), and the Python core leg is blocked on an upstream `xxhash` cp315/abi3 wheel — the sub-3.15 companion consumes `xxh3_128_intdigest` today.
- Draws on the content-addressed-build-graph technique over the one seed; the HLC two-half encoding rides the same parity because a logical off-by-one-half folds a fresh op as stale with no other signal.

[ONE_TESSELLATION_RAIL]: the two-hop `IFC → IfcOpenShell → GLB` companion is Python-native, producing the one GLB every runtime consumes.
- `python:geometry` serves the IfcOpenShell GLB tessellation daemon; `csharp:Rasm.Bim/exchange/interchange` builds the tessellation request and `csharp:Rasm.Compute/Runtime/codecs#TILE_PARTITION` content-addresses the result, and `typescript:ui` renders the GLB viewport — the AP242 CAD-STEP bridge rides the same OpenCascade companion shape.
- Unlocks one geometry-evaluation companion with no duplicate mesh pipeline per runtime: meshing has one owner per runtime (Rasm-DEC, Py-scan, TS-render) and IFC one semantic owner per runtime (`Rasm.Bim`, `ifcopenshell`), meeting only at the content-keyed GLB.
- Draws on the one-owner-per-runtime geometry law and the IfcOpenShell/OpenCascade companion two-hop; the per-tile `EXT_structural_metadata`/`EXT_mesh_features` schema carries the IFC class and field band on the same content key. The AP242 STEP arm is blocked on the `cadquery-ocp` companion admission, and the per-tile schema is emitted through the native `SharpGLTF.Ext.3DTiles` builders (`FeatureIDBuilder`/`PropertyTable`/`EXTStructuralMetadataRoot`), never a hand-rolled extension write.

[ONE_CAPABILITY_CATALOG]: one capability-descriptor source emits the C#/TS/Python SDKs and the MCP agent projection.
- `csharp:Rasm.AppHost/Agent/capability#SDK_CODEGEN` mints every operation as a typed effect-classed cost-modeled `CapabilityDescriptor`; the MCP projection adopts each through `McpServerTool.Create`, and the `typescript:interchange` and `python:runtime/transport/serve` SDKs derive their command surfaces from the generated descriptor, never a hand-written client.
- Unlocks three SDKs and an agent surface from one catalog — a new operation is a descriptor row, never a parallel client or a second op-metadata owner. The descriptor-to-SDK leg lives here; the MCP agent-tool projection (effect-hints, cost-preview, progress, resume token) is the sibling `ONE_AGENT_TOOL_PROJECTION` idea, a distinct decode surface beyond bare codegen.
- Draws on the official MCP C# SDK owning the protocol and the never-hand-roll rule; the polyglot SDK consumption is the capability-catalog wire seam, and the signed grant attestation chains into the determinism event log at the op-log wire. `@bufbuild/buf` is the cross-`libs/` FileDescriptorSet owner the codegen leg pins — the one schema source the three projections derive from, never a per-branch descriptor mint.

[CRDT_COLLABORATION]: the op-log CRDT payload (LWW surviving only as the register arm) is a breaking amendment to the one wire vocabulary that the TS and Python legs decode.
- `csharp:Rasm.Persistence/Version/commits#CRDT_ALGEBRA` owns the `MvRegister`/`opMerge` op union and `Sync/collaboration#TS_PROJECTION` projects the changefeed; the `typescript:projection/convergence#LWW_MERGE` fold and the `python` companion decode the amended payload as the one vocabulary, never an additive parallel surface.
- Unlocks multi-runtime collaborative editing over the single op-log changefeed — divergent-delivery folds of the same op-set converge to a byte-identical state, the convergence law mechanically verified.
- Draws on the closed-CRDT prohibition (a second composition surface broke it once and was dropped): a new op kind is one arm on the existing register-arm fold once it lands on the wire, never a second op vocabulary the fold authors branch-side.

[FAULT_WIRE_ROUNDTRIP]: the fault wire is a two-sided coherence contract — C# packs `(package, code, case)` and TS reconstructs it losslessly. (C#↔TS only.)
- `csharp:Rasm.Compute/Runtime/channels#FAULT_PROJECTION` packs every typed fault into one `FaultDetail` family with disjoint code bands (ComputeFault 2200, HopFault 4500, WireFault 4520-4532, store/config at their app roots) so package selects the family and code selects the arm; `typescript:interchange/Ingress/fault` reconstructs the identical literal-discriminated union through `faultTagOf`/`FAULT_CTOR`, folding an unmapped package to `Quarantine` rather than throwing.
- Unlocks one typed fault rail end-to-end — a status code plus string is never the terminal shape, and a misbanded code is a producer fault caught before emit rather than a wire ambiguity downstream.
- Draws on the package-exclusive band law: neither `package` nor `code` alone is a total key, so the wire triple is the lossless identity and a round-trip property is the gate any new fault family passes. Python mints `FaultDetail` outbound but is not a package-keyed decoder, so the round-trip is C#↔TS.

[ONE_CAUSAL_TENANT_IDENTITY]: the tenant/causal-identity wire is minted once in C# and read — never re-minted — by the Python companion inbound decode and the TS ordering fold.
- `csharp:Rasm.AppHost/Runtime/ports#PORT_RECORDS` mints the HLC two-half stamp on the `ReceiptEnvelope` and threads `TenantContext` across every cross-process hop; the `python:runtime/transport/serve#SERVE` companion decodes `hlc_physical`/`hlc_logical` and the `tenant` field off the inbound receipt slot and propagates the same causal/tenant frame, and the `typescript:projection` skew-aware ordering fold reads the HLC band off the wire to mark concurrent-uncertain rows rather than forcing a spurious total order. The Python `runtime/observability/receipts#RECEIPT` page is NOT this consumer — it owns local evidence and explicitly disclaims the AppHost envelope, so the causal/tenant inbound is the server decode leg.
- Unlocks one causal-and-tenant frame every runtime orders and attributes by — a receipt produced in any runtime carries a stamp the others can order against, and tenant isolation is a wire fact, not a per-runtime reinvention.
- Draws on the HLC two-64-bit-half encoding that rides the same parity as the content seed (a logical off-by-one-half folds a fresh op as stale); the C#-owns-the-wire law makes a second causal stamp or tenant scheme in any peer the named drift defect. The parity legs inherit the content-seed package gate (the same `hash-wasm`/upstream-`xxhash`-wheel preconditions), since the HLC two-half order is proven in the same multi-runtime fixture.

[ONE_GRADUATION_EVIDENCE]: the graduation-evidence rail is the single contract a Python offline result crosses outward on, content-keyed and read by the C# determinism closure.
- `python:compute/graduation#GRADUATION` packs every graduatable offline result into the one `HandoffAxis` `Literal`-discriminated evidence shape keyed by `ContentIdentity` — the seven axes `solver`, `symbolic`, `model-asset`, `array-layout`, `unit-law`, `uncertainty-law`, and `geometry` (the `geometry` axis sub-discriminated by `GeometrySubject`: `registration-transform`, `reconstructed-mesh`, `topology-graph`, `network-graph`, `form-finding`, with the ONNX surrogate fit crossing as the `model-asset` axis after `ModelAssetManifest` validation); `csharp:Rasm.AppHost/Runtime/determinism#EVENT_LOG` re-imports it into the hash-chained event log and the cross-machine replay-verify references it by the one key, never a per-result handoff.
- Unlocks a single inward contract for offline science — a result graduates the same way regardless of which Python package produced it, so the C# owner consumes one rail and the determinism chain references one key.
- Draws on the one-graduation-rail-outward law (Python holds the rail singular) meeting the C#-owns-determinism law at the content-keyed seam; the surrogate fit crosses as a content-keyed ONNX artifact the C# side runs inference over, never an in-process training loop. The `HandoffAxis` evidence shape carries the `(checksum, OrtEpDevice)` provider-determinism fingerprint (the C# `SESSION_CAPSULE` key); the Vectors DDG adjoint coefficients are landed (`T-DDG-ADJOINT-ROWS` closed) so the gradient-adjoint optimizer the surrogate feeds is non-degenerate, leaving the C# determinism consumer's open leaf as the offline graduation handoff itself.

[ONE_WIRE_FIXTURE_CORPUS]: one content-addressed golden-fixture corpus every runtime's parity harness reads.
- `csharp:Rasm.Compute/Runtime/codecs#CONTENT_ADDRESSING` and `csharp:Rasm/Geometry/Spatial/reconciliation#CANONICAL_BYTE_IDENTITY` mint the frozen bytes once — the eight-primitive clash bytes, the 52-byte `0x9462A71A5DD13DCFA3B1D6D225FCBE70` adjacency digest, the `FaultDetail` `(package, code, case)` triples, the CRDT op-set, GLB-by-key, and the HLC two-half stamps; consumers `python:runtime/evidence/identity#IDENTITY`, `typescript:interchange/Codec/frame#CONTENT_HASHING`, the new `typescript:testing/`, and the C# shared test corpus each read the one frozen corpus.
- Unlocks the missing parent of `CONTENT_IDENTITY_PARITY` + `FAULT_WIRE_ROUNDTRIP` + `TRI_LANGUAGE_WIRE_PARITY`: every parity harness reads one corpus rather than re-deriving its own fixtures, so a parity drift surfaces as a single corpus mismatch instead of three divergent reproductions.
- Draws on the content-addressed identity law — the corpus is keyed by the one `XxHash128` seed — and seeds the C# `[BRANCH_TEST_NODE_PROVISIONING]` shared-corpus task, the Python `[PYTHON_TEST_INFRA_FLOOR]`, and the TS `testing/` top-level.

[WEB_GEOMETRY_RESIDENCY_WIRE]: a second geometry wire shape — a splat/meshlet residency manifest distinct from the triangulated GLB — for the WebGPU raster path the GLB-per-tile rail does not cover. (GATED.)
- `csharp:Rasm.AppUi/Render/viewport#TS_PROJECTION` mints the content-keyed residency manifest; consumers `typescript:platform/Transport/decode#RESIDENCY_DECODE` (the operator-named decode leg under `platform`) and `typescript:ui/render/glb#GLB_VIEWPORT` read it, and it binds the Python splat-decode companion (SPZ/SOG).
- Unlocks the WebGPU residency path without a second mint: absent a master owner the manifest is minted twice (the C# AppUi projection and the TS worker interior), so the one owner is the AppUi projection and the single-hash-mint invariant holds at `platform/worker/`. The manifest mint+decode leg is landed (`WEB_GEOMETRY_RESIDENCY_WIRE` task `[COMPLETE]` leg): the `ResidencyManifest` producer byte-matches the consumers; the residual is the Python splat-payload decode and the live WebGPU upload.
- Draws on the one-owner-per-wire-shape law; GATED on `ONE_CONTENT_IDENTITY` parity, the `platform/worker/` decode leg, and the `SPLAT_REALITY_CAPTURE` C# capability.

[ONE_DISTRIBUTED_TRACE]: one W3C trace-context frame every runtime injects and extracts, so a browser interaction, a C# solve, and a Python tessellation join into one correlated trace at the collector — not three disconnected root traces.
- `csharp:Rasm.AppHost/Observability/telemetry#CORRELATION_SPINE` owns the `traceparent`/`tracestate` propagation fold and registers it as `Propagators.DefaultTextMapPropagator`; the `python:runtime/observability/receipts#RECEIPT` companion gains the inbound `Extract`/`Continue` primitive that seeds its span from the C#-minted parent context rather than only reading its own active span, and the `typescript:platform/Observability/telemetry#METRIC_REGISTRY` `WebSdk` leg propagates the `traceparent` on the outbound grpc-web call and continues the parent trace inbound rather than minting an independent `ParentBasedSampler` root.
- Unlocks one correlated distributed trace across the three runtimes: a flame graph and a span tree span the browser INP span, the C# `ActivitySource` solve span, and the Python tessellation span under one trace id, so a regression is read end-to-end instead of stitched by guesswork across three collector silos.
- Draws on the W3C Trace Context propagation standard and the OTel GenAI/MCP span conventions the C# spine already carries: C# owns the propagation fold (the `TraceContextPropagator`/`BaggagePropagator` composite), the peers extract-and-continue at the wire, and a second tracer or a fresh root span in any peer is the named cross-language drift defect — the gap is that the peers inject their own active span today but neither extracts the C# parent across the wire, so the trace flattens per process.

[ONE_HEALTH_DEGRADATION_WIRE]: the C#-minted `HealthSnapshot`/`DegradationLevel` is the one capability-health frame the web and node peers read to gate UI exposure and command dispatch, never a second health scheme.
- `csharp:Rasm.AppHost/Observability/health#TS_PROJECTION` mints the wire-neutral `HealthSnapshot`, the five-level `DegradationLevel`, and the alert shapes; the `typescript:projection/evidence/availability#AVAILABILITY_GATE` fold reads the level off the wire so a degraded capability disables its commands at the one `interchange:CommandGateway` egress, and the `typescript:ui/render/dashboard#LIVE_BINDING_DASHBOARD` renders the level and the retained-capability set — never re-deriving a parallel health rail.
- Unlocks one usable-failure frame every consumer reads: a `Degraded` capability keeps serving but the browser greys its command, the node tier routes around the unhealthy peer, and the operator dashboard shows one level value per capability, so a new health-gated surface is one availability-fold row, never a second health model.
- Draws on the usable-failure degradation law the C# spine owns (the `grpc.health.v1` wire-health mapping where degraded keeps serving) meeting the TS availability gate: the C# `WireHealth` projects the registry onto the standard wire health service, the TS availability fold consumes the level as the dial-time gate input, and the gap is that the health wire is minted with an explicit TS_PROJECTION cluster C#-side yet no master seam binds it to the TS availability gate that already gates the command face.

[ONE_AGENT_TOOL_PROJECTION]: the C# capability descriptors project to one MCP tool catalog — effect-hints, cost-preview, progress, and resume token — that the TS agent runtime decodes once, distinct from the bare SDK-codegen leg.
- `csharp:Rasm.AppHost/Agent/mcp#TS_PROJECTION` projects each `CapabilityDescriptor` to one `Microsoft.Extensions.AI` `CommandAIFunction : AIFunction` (the descriptor JSON Schema as the `JsonSchema` override, the `EffectClass` driving the `readOnlyHint`/`destructiveHint`/`idempotentHint` annotations, an `irreversible` arm wrapped in `ApprovalRequiredAIFunction`) that `McpServerTool.Create(AIFunction, McpServerToolCreateOptions)` adopts into the MCP `tools/list` surface; the `typescript:services/agent/mcp#MCP_TRANSPORT` decodes that surface into the `McpToolWire` Schema carrier (descriptor schema + hint annotations + `estimatedCost` vector) and lifts each into one `@effect/ai` `Tool.make` whose sole agent-facing parameter is the host `payload`, decodes the `CostPreviewWire` dry-run and the `ProgressNotificationWire` SSE stream, and reattaches by the `ResumeTokenWire` cursor — the host's own projection decoded once, never a re-minted branch-side tool definition. The C#-minted named carriers are the `ToolResult` record and the `ReceiptEnvelopeWire<ToolResultWire>` envelope (`Runtime/ports#TS_PROJECTION`); the tool-list/cost/progress/resume shapes are the SDK-native projection the TS side schema-decodes.
- Unlocks one agentic surface every runtime drives: the MCP tool catalog, the effect-hint gating, the cost preview, and the resumable progress stream all derive from the one descriptor source the SDK codegen also reads, so a new agent tool is one descriptor row that auto-projects to the MCP tool and the polyglot SDK, never a hand-authored client divorced from a descriptor.
- Draws on the official MCP C# SDK owning the protocol and the `@effect/ai` `Toolkit`/`Tool.make` owning the agent-side decode: the existing `ONE_CAPABILITY_CATALOG` owns the descriptor-to-SDK leg, but the MCP agent-tool projection with effect-hints/cost-preview/progress/resume is now a concrete realized contract beyond bare codegen — the gap is that the agent-tool wire (`McpToolWire`/`CostPreviewWire`/`ProgressNotificationWire`/`ResumeTokenWire`) is its own decode surface the master tier must own so the C# host and the TS agent runtime meet at one tool catalog, the Python companion serving the same tools over its server-host leg.

## [2]-[CLOSED]

(none)
