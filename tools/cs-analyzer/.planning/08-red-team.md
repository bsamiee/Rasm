# [08] Red-Team — Attacks, Resolutions, and Forced Design Deltas

Adversarial sweep of the target design, every claim checked against repo source. Verdict codes: [VALID] (attack lands, design changes), [VALID-minor]/[VALID-trivial]/[VALID-citation] (lands, small fix), [REFUTED] (attack fails, design stands). The five design-FORCING [VALID]s are already resolved into docs 04/05/07; this doc records the provenance and the REFUTED attacks so the next session does not re-litigate settled ground. NET verdict at the end.

## [A]-[ADD_A_RULE_PROMISE]

- **A1 [VALID]** The "1 catalog line + 1 rule file + 1 test file" contract is arithmetically false by the design's own mechanics: activating real RS2000-RS2008 makes every new descriptor REQUIRE an `AnalyzerReleases.Unshipped.md` row or the analyzer build errors RS2000; `helpLinkUri` anchors require a `docs/stacks/csharp/analyzer.md` edit — a file that does NOT exist (verified). → RESOLVED doc 04 §1, doc 05 §8: contract restated as **catalog row + rule file + test file + Unshipped row + doc anchor**; meta-test asserts the Unshipped row exists so omission fails locally.
- **A2 [REFUTED]** Keeping a hand-list `Catalog.All` is NOT the old AD0001 crash class: Driver registers FROM `All`, so an omitted row leaves the rule INERT and its mandated positive markup test fails; the meta-test double-covers. Residual surfaces only in the test rail — acceptable under the repo's split gates. → doc 04 §2.1.
- **A3 [VALID-minor]** Meta-test (b) keys rule↔test pairing on FILE naming; key it on RuleRow statics + descriptor ID instead. Blanket one-file-per-rule for the ~20 trivial syntax-kind bans collides with the repo surface-preference doctrine (CLAUDE.md 5.4). → RESOLVED doc 04 §3, doc 06 §4: per-category ROW TABLES below a predicate-complexity bar; rules with real algebra (0005, 0729, 0742) keep their own files; pairing keyed on RuleRow ID.

## [B]-[PERF_CLAIMS]

- **B1 [VALID]** Closure-detection re-base is technically wrong as written: `IFlowCaptureOperation` exists ONLY in ControlFlowGraph lowering and never fires in `RegisterOperationAction` trees; `AnalyzeDataFlow` needs a per-lambda SemanticModel pass and is strictly costlier. The demolition mischaracterizes the existing walk — `RuntimeRules.cs:47-59` `CapturedOuterSymbols` already walks IOperation (`ILocalReferenceOperation`/`IParameterReferenceOperation` + `ContainingSymbol` compare), symbol-correct and cheap, NOT a "hand descendant walk." → RESOLVED doc 04 §1 (Walkers), doc 01 §3: carry the existing operation walk; CFG only with `RegisterOperationBlockAction` + `ControlFlowGraph.Create` and an explicit cost budget if ever desired.
- **B2 [VALID-citation]** "4-5x rebind cost, roslyn#25259" is numeric folklore; the bindable authority is **RS1030** (`DoNotUseCompilationGetSemanticModel`). → RESOLVED doc 03 §5a, doc 04 §9.
- **B3 [VALID]** Sec 2.2 registration error: CSP0724 FlagsEnumOveruse needs compilation-wide bitwise-callsite facts (current impl accumulates across the whole compilation, `AnalyzerState.cs:25-26`); a SymbolStart scoped to the enum cannot see callsites in other types. The same paragraph listed 0501 under SymbolStart AND reserved CompilationEnd for "0501 cross-type" — self-contradiction. → RESOLVED doc 04 §2.2, doc 05 §2: 0724/0501/0503 stay **CompilationEnd** (documented batch-only); only genuinely per-type accumulators (0729) move to SymbolStart.
- **B4 [REFUTED]** Dropping global `OperationKind.Literal` registration is justified: `Uri.TryCreate` over every string literal verified at `RuntimeRules.cs:211-218`, Literal registered compilation-wide today. → doc 04 §9, doc 05 §5 (0605 dropped).
- **B5 [REFUTED]** The 0740 per-compilation index claim is sound: the recursive global-namespace triple walk is real (`SymbolFacts.cs:1015-1024`; `UnionOpsGenerator.cs:40-49`). → doc 04 §2.3.

## [C]-[ID_STABILITY_MIGRATION] (the strongest lane)

- **C1 [VALID — strongest]** The collapse policy semantically MUTATES shipped IDs, poisoning agent memory: today's CSP0002 is narrowly "Match is boundary-only" (`RuleCatalog.cs:32`) but absorbs 0705+0303 into a broad terminality rule; today's CSP0007 says "inject NodaTime.IClock" (`:45`) and the rebuild FLIPS its remediation to TimeProvider — previously-compliant NodaTime sites start firing under the same ID and any existing 0007 suppression silently suppresses the new semantics. Self-defeating: MEMORY.md cites CSP0705, the exact ID being tombstoned into 0002. (The TimeProvider retarget itself is doctrine-correct; only the ID reuse is wrong.) → RESOLVED doc 05 §1, §3, doc 07 §3: semantic broadening or polarity flip ⇒ NEW CSP09xx ID (**0002/0303/0705→CSP0910 TerminalCollapse**; **0007/0401/0714→CSP0911 TimeDiscipline**), all constituents tombstoned, old→new map in `analyzer.md` + MEMORY.md update; survivor-ID reuse only for message-compatible strictly-narrower-or-equal merges.
- **C2 [VALID-trivial, corpus-poisoning]** The design violated its own lowest-constituent-ID rule and miscounted: the genuine ID violation was **0725←0718** (0718 < 0725, so the survivor must be **0718**, NOT 0725 as written; 0606←0704 was already correct, 0606<0704); and "LAW CARRY clean (33)" was claimed against a list of 34 distinct IDs that did not balance. → RESOLVED doc 05 §3 (survivor is 0718 ImperativeAccumulator), §6 (corrected exhaustive ledger, verified disjoint sum = 87: 34 Law CARRY + 12 reuse-survivors + 6 old-narrow-retired + 12 reuse-absorbed + 9 Pressure-active + 4 pressure-absorbed + 10 DROP; **48 Law + 9 Pressure = 57 active from the carried, + 6 net-new = 63 total active; 32 genuinely-shipped retired**).
- **C3 [VALID-minor]** 0016/0716/0721/0722 never appear in `AnalyzerReleases.Shipped.md` (verified: empty 2-line header, 0 source hits) — a "Removed Rules" tombstone REQUIRES a prior shipped entry, so tombstoning never-shipped IDs fabricates release history. → RESOLVED doc 05 §7: reserved-ID list in `Catalog.Reserved` + meta-test; tombstone only genuinely-shipped retirees.

## [D]-[OLD_TOOL_SHAPES_KEPT_OUT_OF_HABIT]

- **D1 [VALID]** `CspScope=Exempt` resurrects the Skip-list as a scope value with NO inventory — CSP0903 covers only attribute sites. → RESOLVED doc 04 §4: **`Exempt` is NOT a scope value**; tooling projects skip the analyzer reference (already diffable in Build.props).
- **D2 [VALID]** Assembly-level scope is COARSER than today and the Scope×Rule gating matrix was never defined — the most consequential behavior surface of the rebuild was implicit. `Rasm` is ONE csproj containing `Analysis/` (a HotPath sub-tree; `Rasm.csproj` + `Markers.AnalysisPath SymbolFacts.cs:65-66`), and boundary classification is namespace/file-level. Declaring `Rasm.Rhino CspScope=Boundary` wholesale strips domain-flow enforcement from its pure kernels — an unacknowledged regression. → RESOLVED doc 04 §4, doc 06 §6, doc 07 §1: **per-tree scope override via `.editorconfig` `csp.scope` key** read from `AnalyzerConfigOptionsProvider.GetOptions(tree)` AND **type-level `[CspScope]`**; the **Rule×Scope applicability matrix is PUBLISHED in the catalog** and meta-test-asserted complete.
- **D3 [REFUTED]** Deleting `IsRegexMatchCall` is safe: `IsLanguageExtMatch` is namespace-anchored with no name-only fallback (`SymbolFacts.cs:303-308`), so the `Regex.Match` exclusion can never be load-bearing. → doc 05 §3 (CSP0910 deletes the dead logic).

## [E]-[DOCTRINE_VIOLATIONS_IN_THE_NEW_DESIGN]

- **E1 [VALID]** `Diagnostic.Properties` bag + `helpLinkUri` are speculative scaffolding under the design's OWN consumption contract: assay decodes console lines `error|warning CSP####` only; bags and help links flow solely through SARIF/IDE, which the agent rail does not use. → RESOLVED doc 04 §7, doc 07 §4: keep `fix`/`doctrine`/`scope` strictly in the single-line message; populate the bag + `helpLinkUri` ONLY IF assay adopts `/errorlog` SARIF (doc 07 §2) — then the bag earns its keep; otherwise the seam is reserved, not populated.
- **E2 [REFUTED]** Kernel god-file risk: `Facts.cs` (~150 LOC, three concerns) is within repo doctrine (fewer deep surfaces); dissolving SymbolFacts' 1,495 LOC into owning rules is doctrine-compliant co-location. No change beyond A3's granularity nuance.

## [F]-[AGENT_CONSUMPTION_GAPS] (critical wiring)

- **F1 [VALID — critical, two verified wiring facts]** The tier taxonomy is dead on arrival against current wiring: (i) `Directory.Build.props:75` `TreatWarningsAsErrors=true` repo-wide, so `Pressure=Warning` promotes to error — build-failing and indistinguishable from Law; (ii) assay's build runs `dotnet build --no-restore -v:quiet /clp:ErrorsOnly` (`catalog.py:160`), so un-promoted warnings NEVER reach the agent rail, and CSP0903 Info is invisible under both. "This is the only assay-side change" is FALSE. → RESOLVED doc 04 §6, doc 07 §2: `<WarningsNotAsErrors>` for the Pressure band (or category-severity config); assay console logger must include warnings OR adopt `/errorlog` SARIF; CSP0903 Info needs a non-console emission path.
- **F2 [VALID-minor]** `helpLinkUri` targets a nonexistent file via a relative path (not a URI); agents never see helpLink in console. → RESOLVED doc 02 §4, doc 07 §4/§6: make `analyzer.md` the companion deliverable's concrete output; treat anchors as documentation policy, not diagnostic plumbing.
- **F3 [VALID]** CSP0902 as originally specified is unimplementable/bypassable: `#pragma` has no Justification field, and once `NotConfigurable` is dropped, `dotnet_diagnostic.CSP####.severity = none` in any `.editorconfig` silently disables a Law rule with zero audit. → RESOLVED doc 05 §9: CSP0902 BANS `#pragma` for CSP (suppression is `[SuppressMessage]`-with-Justification only) AND reads `dotnet_diagnostic.CSP*` severity keys (global + per-tree) at CompilationStart, erroring on Law-tier downgrades.
- **F4 [VALID — hardening]** "Config absent ⇒ data-driven rule silent" converts deletion/rename of `CspBannedSymbols.txt` into silent disablement of five Law rules (0011/0007→0911/0723/0742/0708) — exactly the fragility the charter forbids. → RESOLVED doc 04 §5, doc 05 §9: CSP0901 (extended) errors when a data-driven rule is enabled in a non-tooling scope without its AdditionalFiles section; the data files ship IN the rebuild change.

## [G]-[MONOREPO_WIRING_FRAGILITY]

- **G1 [VALID]** Compile-linking `CspContracts.cs` into EVERY project re-creates the CS0436 collision the Skip list dodges. The file is ALREADY zero-dependency, so "made zero-dependency so the Skip list dies" misdiagnoses the cause; the Skip set (`:84-93`) matches exactly the IVT-connected projects (`:250-256`). Public attributes → CS0436 across ProjectReference chains; internal → CS0436 via IVT; TWAE makes either fatal. → RESOLVED doc 04 §1, doc 07 §1: a tiny `netstandard2.0` contracts PROJECT (`Csp.Contracts.csproj`) referenced normally everywhere (one assembly = one identity); retires `SkipBoundaryExemptionContracts`.
- **G2 [VALID-minor]** `global.json` has NO `sdk` pin (verified: only the MTP runner block), so "single Roslyn target 5.3.0" rests on the ambient SDK; an older host compiler refuses the analyzer (CS8032 → fatal under TWAE). → RESOLVED doc 07 §1: pin `sdk.version` as part of the rebuild.
- **G3 [VALID-minor]** `Build.props:85` hardcodes `CsAnalyzer.csproj` in `LocalCSharpAnalyzerProject`; the rename to `Csp.Analyzer.csproj` AND the UnionOps generator extraction to `libs/csharp/Rasm.Generators` both need explicit Build.props wiring (generator referenced as Analyzer ONLY by Rasm-family projects). → RESOLVED doc 04 §8, doc 07 §1.
- **G4 [REFUTED]** `ProjectReference OutputItemType=Analyzer` + no NuGet + no `roslynX.Y` multi-targeting is sound for an agent-only CLI monorepo; the 5.3.0 pin is real (`Directory.Packages.props` pins both `Microsoft.CodeAnalysis.CSharp` and `.Analyzers` 5.3.0). → doc 07 §1.

## [H]-[TESTING_HARNESS]

- **H1 [VALID — gap]** Real packages fix name-only matching for LanguageExt, but rules asserting over Thinktecture GENERATED surfaces (0734 generated state-threaded Switch, 0733 alias-of-generated-factory) need the generator to RUN in-test or hand-stubbed generated partials — `CSharpAnalyzerVerifier` does NOT execute source generators. → RESOLVED doc 06 §3: per-rule-family strategy — generator-run path via `SolutionTransforms` adding the Thinktecture generator (0733/0734/0737/0740/0745/0802), hand-stubbed-partial path where only the member shape matters, documented per rule.
- **H2 [REFUTED]** The remaining harness upgrades are genuine and evidence-verified: hand TPA harness with `concurrentAnalysis:false` (`AnalyzerTestHarness.cs:43`), fake `namespace LanguageExt { class Fin<T> }` shims (`RuleBehaviorTests.cs:288-289`, 10+ sites), first-match severity-only assertions (`:902-922`), regex re-implementation of release tracking (`ReleaseDisciplineTests.cs:100-108`). → doc 06 §1, §2, §5.

## [I]-[BASELINE_TRUTH_CHECK]

- **I1 [REFUTED]** "87 descriptors, gaps 0016/0716/0721/0722, all Error + NotConfigurable" — verified by direct count of `RuleCatalog.All` (87 IDs, `:215-228`) and the `Err` factory (`:21,26`). Re-verified this session.
- **I2 [REFUTED]** All demolition claims against current code verified in source: substring scope classification (`SymbolFacts.cs:49-71,161-206`), 21 hardcoded Rhino type names (`:222-225`), receipt name-fragment sets (`:231-240`), stringly `ToDisplayString` matching (`:392-440`), terminal-Match analysis (`:469-486`), recursive namespace walks (`:1015-1024`), generator Compilation capture + `Rasm.Domain` coupling + caseless-union silent skip (`UnionOpsGenerator.cs:17-21,13,53-55`), ~70 uniform `Check*` bodies (`FlowRules.cs:16-60`), 0729 397-prime hand hash (`ShapeRules.cs:225-233`) + asymmetric seed admission (`:267-280`), 0503 counting flaws (`AnalyzerState.cs:74-82`). → doc 01 (the demolition survey stands).

## [NET]

The catalog surgery (carry/collapse/drop partition, tier split, data-driven config, demolition evidence) is sound and source-grounded. The design REQUIRED change in five places before the rebuild — all five are now resolved into docs 04/05/07:

1. **ID policy** — new IDs for semantically-mutated rules (0910/0911), the 0718-not-0725 lowest-ID fix, the corrected ledger, reserved (not tombstoned) never-shipped IDs (C1/C2/C3 → doc 05).
2. **Severity plumbing** — `<WarningsNotAsErrors>` band + assay logger/SARIF change + non-console path for 0903, or the tier taxonomy and 0903 are inert under TWAE + `/clp:ErrorsOnly` (F1/F3 → doc 04 §6, doc 07 §2).
3. **Scope granularity** — per-tree `csp.scope` override + type-level `[CspScope]` + a published Rule×Scope matrix, or Rhino/GH projects lose domain enforcement and HotPath is unexpressible; `Exempt` is not a scope value (D1/D2 → doc 04 §4).
4. **Contracts type-identity** — a real `netstandard2.0` contracts PROJECT, not a Compile-linked source file, or CS0436 under IVT reinstates the Skip list (G1 → doc 04 §1, doc 07 §1).
5. **Registration truth** — 0724/0501/0503 stay CompilationEnd (batch-only); closure detection stays an IOperation walk, NOT `IFlowCaptureOperation` (B1/B3 → doc 04 §2.2, Walkers).

Secondary forced fixes folded in: A1 (add-a-rule contract restated), A3 (per-category row tables), B2 (RS1030 citation), E1 (Properties bag conditional on SARIF), F2 (`analyzer.md` is the companion deliverable), F4 (config-absence integrity), G2 (sdk pin), G3 (Build.props rename + generator condition), H1 (generated-surface test strategy). REFUTED attacks (A2, B4, B5, D3, E2, G4, H2, I1, I2) leave the design standing on those points — do not re-open them.
