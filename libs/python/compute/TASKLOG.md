# [PY_COMPUTE_TASKLOG]

The open and closed work distilled from `IDEAS.md`. `[1]-[OPEN]` carries task cards whose leader holds a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — and three to four scoped bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. `[2]-[CLOSED]` carries `[COMPLETE]` and `[DROPPED]` items. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[QUEUED] Author the inverse-design optimization page — `GRADIENT_DRIVEN_INVERSE_DESIGN`.
- Author `optimization/design.md` owning one `DesignProblem`-discriminated optimizer that drives an Equinox-parameterized objective (field problem over `simframe` assembly / parametric mesh / material-density distribution) to a stationary point through Optimistix `minimise`/`least_squares`, folding one content-keyed `OptimizationReceipt` over iterates, objective trace, and the first-order/KKT residual.
- Integrate `optimistix` (`minimise`/`least_squares`/`OptaxMinimiser`/the `Solution` stats), `equinox` (`partition`/`combine` PyTree parameterization), `jax` (the cotangent the sensitivity owner pulls back); the optional `optax` first-order-descent axis row is one admission step (manifest row delta returned to the orchestrator), the `optax.adam`/`chain`/`apply_updates` spellings verified against `compute/.api/optax.md` (present) under the `optimistix.OptaxMinimiser` wrapper, never a phantom dependency.
- Lands in the new `compute/optimization/` sub-domain reading `differentiation/sensitivity.md#SENSITIVITY` for the implicit-adjoint gradient and composing `solvers/{linear,nonlinear,differential}.md` and `simframe/mesh_field.md#MESH_FIELD`; the runtime `ContentIdentity`/`RuntimeRail`/`Receipt` carry identity and the rail; the converged design graduates as the `solver` `HandoffAxis` case on the one graduation rail.
- Offline optimal-design only — never a training loop, a production solver session, or a parallel optimizer beside the solve; the `jax`/`optimistix`/`equinox`/`optax` stack carries the `python_version<'3.15'` jaxlib floor, so the body is authored against the documented API on the gated band, the `optimistix.minimise`-over-Equinox and `optax`-descent spellings settled by `compute/.api/optimistix.md` and `compute/.api/optax.md` (both present).

[QUEUED] Author the constrained/discrete math-program page — `CONSTRAINED_DISCRETE_PROGRAM`.
- Author `optimization/program.md` owning one `ProgramIntent`-discriminated owner with cases `Linear` (`scipy.optimize.linprog` over HiGHS), `Integer` (`scipy.optimize.milp` with an integrality vector and `Bounds`), `Global` (`scipy.optimize.differential_evolution` over a bounds box), `Constrained` (`scipy.optimize.minimize` threading `Bounds`/`LinearConstraint`/`NonlinearConstraint`), and `Assignment` (`scipy.optimize.linear_sum_assignment`), every route folding the `OptimizeResult` success flag, objective value, and max constraint-violation residual into one content-keyed `ProgramReceipt`.
- Integrate `scipy.optimize` (`linprog`, `milp`, `differential_evolution`, `minimize`, `linear_sum_assignment`, `Bounds`, `LinearConstraint`, `NonlinearConstraint`, `OptimizeResult`) — all already catalogued in `compute/.api/scipy.md`'s `scipy.optimize` entrypoint table, no new admission; `numpy` for the problem-data canonical buffer and the constraint-violation reduction.
- Lands in the `compute/optimization/` sub-domain beside `design.md` as the second owner on the same sub-domain; keys each program by `ContentIdentity.of` over the canonical problem data (cost vector, constraint matrix, bounds, integrality), graduates the certified optimum as the `solver` `HandoffAxis` case, and reads `arrays/payload.md#PAYLOAD` for the constraint-matrix admission.
- Constrained, global, and discrete optimization that the differentiable gradient loop structurally cannot reach — the discriminant (constraint/integrality structure versus a differentiable objective) is recoverable from the problem value, so this is a sibling owner on the `optimization` sub-domain, never a duplicated optimizer; offline evidence only, no production solver session. The `scipy.optimize` global/constrained/discrete bodies carry the `python_version<'3.15'` scipy marker with no numpy floor (HiGHS/MILP is the gated capability itself).

[QUEUED] Author the convex-conic certificate page — `CONVEX_CONIC_CERTIFICATE`.
- Author `optimization/convex.md` owning one `ConvexProgram` builder over `cvxpy` — `cvxpy.Variable`/`Parameter`/`Minimize`/`Maximize`/`Problem`, the constraint algebra, and `problem.solve(solver=...)` over a conic backend — reading the primal value, the per-constraint `constraint.dual_value`, the optimal `problem.value`, and `problem.status` into one `ConvexReceipt` carrying the optimality certificate (duality gap plus dual multipliers).
- Integrate `cvxpy` (`Variable`, `Parameter`, `Minimize`, `Maximize`, `Problem`, `Problem.solve`, `Problem.value`, `Problem.status`, `Constraint.dual_value`, the `CLARABEL`/`ECOS`/`SCS` conic-solver selector, `installed_solvers`) and the conic backend `clarabel`; author `compute/.api/cvxpy.md` and `compute/.api/clarabel.md` against the reflected/decompiled surface and return the two manifest-row deltas to the orchestrator.
- Lands in the `compute/optimization/` sub-domain as the third owner beside `design.md` and `program.md`; keys each program by `ContentIdentity.of` over the canonical problem expression, graduates on a new `convex-program` `HandoffAxis` literal added to `graduation/receipt.md#GRADUATION`, and registers `cvxpy`/`clarabel` in the README `[PACKAGES]` registry under a new "Convex optimization" line.
- Disciplined convex programming with a dual-certificate proof of global optimality — a distinct proof object from the local first-order residual and the heuristic global search, the convex analogue of the `validated_numerics` certified-enclosure ladder; classical convex programming only, no neural or non-convex relaxation. `cvxpy` resolves on a near-current Python band with native conic backends, so the body is authored against the documented API on the band the manifest admission settles.

[QUEUED] Extend the DSP owner with the multiresolution case family — `MULTIRESOLUTION_SIGNAL`.
- Extend `signal/dsp.md`'s `SignalOp` with a `Wavelet` case family — `Decompose(wavelet, level)` over `pywt.wavedec` and `pywt.waverec`, `Scalogram(wavelet, scales)` over `pywt.cwt`, and `Packet(wavelet, maxlevel)` over `pywt.WaveletPacket` — folding the per-level energy distribution, the dominant scale, and the denoised-reconstruction residual into the existing `SignalReceipt` beside the Welch band power, never a second signal owner.
- Integrate `pywt` (`wavedec`, `waverec`, `cwt`, `WaveletPacket`, `Wavelet`, `wavelist`, `threshold`, `dwt_max_level`); author `compute/.api/pywavelets.md` against the reflected surface and return the manifest-row delta to the orchestrator; `numpy` for the per-level energy reduction and the reconstruction residual.
- Lands as a case-family extension on the existing `Signal` owner in `signal/dsp.md`; the `SignalReceipt` gains the wavelet-level energy and dominant-scale fields, the dominant band still reads through the existing Welch fold for the stationary cases; registers `pywavelets` in the README `[PACKAGES]` "Signal processing" registry.
- Time-frequency and multiresolution evidence (transient localization, multiscale energy, wavelet-threshold denoising) as cases on the one `Signal` owner — the discriminant is the op value, so it is a `SignalOp` family extension, never a parallel signal surface; classical wavelet analysis only, no learned filter. `pywt` is C-over-numpy and resolves where numpy does, so the wavelet bodies sit on the cp315 core beside the gated `scipy.signal` path rather than under the jaxlib floor.

[QUEUED] Close the scipy `signal`/`stats`/`spatial` catalogue coverage gap — `API_CATALOGUE_COVERAGE` (trickle-down) + folder coverage audit.
- Extend `compute/.api/scipy.md` with three new `[ENTRYPOINT_SCOPE]` tables — `scipy.signal` (`butter`, `sosfiltfilt`, `welch`, `resample_poly`, `firwin`, `filtfilt`, `spectrogram`, `find_peaks`), `scipy.stats.qmc` (`Sobol`, `Halton`, `LatinHypercube`, `scale`, `discrepancy`), and `scipy.spatial` (`cKDTree`/`.query`/`.query_ball_point`, `ConvexHull`, `Delaunay`, `Voronoi`, `distance.cdist`) — so the `signal/dsp.md`, `experiments/study.md`, and `spatial/query.md` RESEARCH items that flag these as uncaptured resolve to settled fence code.
- No package admission — `scipy` is already admitted and catalogued; the work is widening the one folder catalogue's entrypoint tables under a uv-sync reflection pass on the gated band so the three consuming pages' external members verify against the catalogue rather than staying RESEARCH items.
- Lands in `compute/.api/scipy.md` only; unblocks the `[SCIPY_SIGNAL]`, `[SCIPY_QMC]`, and `[SCIPY_SPATIAL]` RESEARCH items on three design pages from RESEARCH to settled, the single highest-leverage coverage move in the folder.
- The reflected spellings carry the `python_version<'3.15'` scipy marker; the catalogue captures them on the gated band the existing scipy rows already sit on, so no interpreter assumption changes.

[QUEUED] Widen the compute test-tool source scope to the package owner — `PYTHON_TEST_INFRA_FLOOR` (trickle-down).
- Return the central `pyproject.toml` row delta widening `[tool.coverage.run].source`, `[tool.mutmut].source_paths`, the pytest `testpaths`, and the mypy `files` to name `libs/python/compute` as its source lands, so the transcribed owner inherits the 95% coverage gate, the mutation kill-ratio, the hypothesis property spine, and the advisory mypy pass the moment its source exists.
- No new dependency — `coverage`, `mutmut`, `pytest` + plugins, `hypothesis`, `inline-snapshot`, `mypy`, and `ty` are already in the `dev`/`mutation` groups; the work is the central source-scope widening only, returned to the orchestrator as a manifest-row delta, never a per-folder manifest.
- Lands as a central-manifest row delta (orchestrator-owned), aligned to the branch test-infra floor; the compute owner is named in the test-tool config the same campaign turn its source transcribes.
- Keeps the test-tool config centralized in the one `pyproject.toml`; the folder never carries a test-runner config.

## [2]-[CLOSED]

[COMPLETE] Author the `optax` first-order-descent catalogue — `API_CATALOGUE_COVERAGE`. `compute/.api/optax.md` catalogues the `GradientTransformation` carrier, the alias optimizers (`adam`/`sgd`/`rmsprop`/`adamw`/`lbfgs`/`lion`), the `chain`/`apply_updates`/`multi_transform`/`masked` combinators, the `scale_by_*`/`clip*` transformations, the learning-rate schedules, and the `optax.losses` family; the inverse-design fence's `optimistix.OptaxMinimiser`-over-`optax` descent spelling now verifies against a present catalogue on the gated band, the lone genuinely-absent catalogue the branch coverage audit named.

[COMPLETE] Confirm each graduation axis against its C# wire-seam owner — from the cross-language wire. The Tier-0 seam owner `libs/.planning/TASKLOG.md` names the producer `python:compute/graduation#HANDOFF` and the single consumer `csharp:Rasm.AppHost/determinism/determinism-and-replay#EVENT_LOG`, which re-imports the `HandoffAxis` graduation evidence by the one `ContentIdentity` seed key and references the offline result by key. Every `HandoffAxis` and `GeometrySubject` literal crosses on that one content-keyed rail; the receipt names the axis alone and the consuming owner is settled, so no routing literal bakes a C# owner row into the receipt. The only downstream residual is the C# determinism consumer's empty `Rasm.Compute` `DesignProblem.OperatorRows`, populated by the Vectors adjoint-coefficient task `T-DDG-ADJOINT-ROWS` — a C#-branch task that does not block the compute-side axis confirmation, which the settled seam vocabulary already discharges.

[COMPLETE] Rebuild array admission on the Array API standard — `ARRAY_API_ADMISSION`. `arrays/payload.md` `ArrayPayload.admit` folds `array_namespace`/`device`/`to_device` namespace dispatch into one frozen payload keyed by `ContentIdentity.of` over the host buffer; `array-api-compat`/`array-api-extra` RESEARCH-flagged on the cp315 core.

[COMPLETE] Add the Lineax autodifferentiable linear-operator case — `JAX_SOLVER_FAMILY`. `solvers/linear.md` carries the `LinearIntent.Operator` case and `_operator_receipt` folding `lineax.linear_solve`/`MatrixLinearOperator`/`LU`/`QR` `Solution.stats` into the one `SolverReceipt` inside `_dispatch`; gated jaxlib band, RESEARCH-flagged.

[COMPLETE] Adopt Optimistix for the nonlinear routes — `JAX_SOLVER_FAMILY`. `solvers/nonlinear.md` `NonlinearIntent` root/minimise/fixed-point/least-squares ride one tag-keyed `_optimistix_receipt` route table with a reachable numpy central-difference floor; `accelerate` numba row kept distinct; RESEARCH-flagged on the gated band.

[COMPLETE] Add the Diffrax differential-equations route — `DIFFERENTIAL_EQUATIONS`. `solvers/differential.md` `DifferentialIntent` ODE/SDE/CDE over `diffeqsolve` with `Tsit5`/`PIDController`/adjoint modes, no numpy floor (the gated capability itself); feeds `sensitivity` and the study parametric-trajectory case; RESEARCH-flagged.

[COMPLETE] Replace the hand-rolled sensitivity with SALib — `SALIB_SENSITIVITY`. `experiments/study.md` composes the SALib sampler-and-analyzer pair across Morris/Sobol/FAST plus the mined PAWN/DGSM/HDMR rows (`analyze.pawn`/`dgsm`/`hdmr` reading `median`/`dgsm`/`Sa`); numpy LHS/factorial/polynomial floors on cp315; result keys RESEARCH-flagged.

[COMPLETE] Add the sampler-backend axis to inference — `BAYESIAN_BACKEND_AXIS`. `inference/bayesian.md` `SamplerBackend` dispatches PyMC-native, NumPyro JAX, Nutpie Rust/Numba, and the mined BlackJAX JAX (`sample_blackjax_nuts`) backends through one `_draw` match with arviz scoring all four; blackjax verified against `compute/.api/blackjax.md`, gated band.

[COMPLETE] Add the mpmath enclosure floor — `MPMATH_ENCLOSURE_FLOOR`. `validated_numerics/enclosure.md` `_certified_evaluate` lifts to an `mpmath.mpi` interval at `mp.prec` beneath the gated Arb path with the numpy `nextafter` band as the uncertified last resort; mpmath cp315-clean.

[COMPLETE] Open the spatial computational-geometry page — `SPATIAL_GEOMETRY_QUERIES`. `spatial/query.md` `SpatialQuery` folds KD-tree neighbour/radius, hull, Delaunay, Voronoi, and the alpha-shape circumradius boundary into one `SpatialReceipt` keyed by `ContentIdentity.of`; numpy brute-force neighbour floor on cp315; `scipy.spatial` members RESEARCH-flagged (absent from `compute/.api/scipy.md`, now opened as a coverage task).

[COMPLETE] Open the simframe mesh-and-field interchange page — `SIMFRAME_MESH_FIELD`. `simframe/mesh_field.md` `MeshField` lowers a weak form to the sparse stiffness/load pair through `skfem.Basis`/`asm` over the shared `ElementKind`/`FemForm` axis, `MeshExchange` rounds the mesh-and-field through meshio; assembly-and-interchange only, the solve stays on the quadrature route; verified against `compute/.api/scikit-fem.md` and `meshio.md`.

[COMPLETE] Verify every runtime-boundary composition against the runtime owner spelling — `RUNTIME_BOUNDARY_PARITY`. Every `contribute`/admission body across the eleven sub-domains composes `Receipt.of(phase, owner, subject, facts)`, `BoundaryFault(<case>=payload)`, and `ContentIdentity.of(fmt, source, policy)` over the flat `rasm.runtime.{receipts,faults,content_identity}` imports; the new `spatial`/`simframe` pages conform; audited against the runtime owner pages.
