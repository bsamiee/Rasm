# [GEOMETRY_CENSUS_DOSSIER]

Read-only adversarial census of `libs/python/geometry/.planning/` (18 pages, 5235 LOC Markdown; code fences the product). Governing law: `docs/stacks/python/`, `libs/.planning/architecture.md`, upstream `RASM-RUNTIME-BRIEF.md` + `RASM-DATA-BRIEF.md`, `RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]`. Anchors are `libs/python/geometry/.planning/`-relative unless prefixed; `ARCH:` = `libs/python/geometry/ARCHITECTURE.md`; `README:` = `libs/python/geometry/README.md`; `pyproject:` = repo-root manifest.

## [00]-[TOPOLOGY]

Folder tree: `scan/`(4) `mesh/`(6) `graph/`(3) `ifc/`(5). Corpus is page-level strong (dense, table-driven dispatch, doctrine-fluent) and graph-level/governance-level rotted along the SAME axes the runtime+data briefs already ruled: identity-module drift, banned-appendix prose rot, a phantom capability plane, blind seam ledger, duplicate cross-cutting mechanisms, and a forward-dependency on an unbuilt+unbriefed sibling. No page-level import CYCLE exists (the internal graph is a 4-edge DAG), which is the one structural win over runtime/data.

Real intra-geometry import edges (only 4, all leaf-sink, acyclic):
- `mesh/daemon → mesh/cad` (`daemon.md:35` `BridgeFormat, StepBridge`)
- `ifc/analysis → ifc/selector` (`analysis.md:36`)
- `ifc/costing → ifc/selector` (`costing.md:34`)
- `ifc/structural → ifc/selector` (`structural.md:41`)

`ifc/selector` and `mesh/cad` are pure sinks; no back-edge. `ARCH:[02]-[SEAMS]` declares ZERO of these 4 intra-folder edges (ledger blind to the whole internal graph — the exact data-`E13` defect class).

## [01]-[PER_PAGE_VERDICTS]

Grades are page-level quality against the corpus bar (runtime/data graded 5–9.5); the structural defects below are what a rebuild closes.

### scan/ingestion.md — 8.5
Frozen `ScanIngestion` over `ScanSource` tagged-union; `_STAGE`/`_FILTER` injected-factory tables; `pdal` `|` fold; `IngestReceipt` structural `ReceiptContributor`. Defects: no `content_identity` import (clean) but sibling drift context; `[03]-[RESEARCH]:271` + `[04]-[UPSTREAM]:275` banned appendices; the `[02]-[INGESTION]` Owner/Boundary walls are ~40-line run-ons (Boundary `:17` restates every deleted-form once = the invariant-once violation). Charter as-should-be: unchanged owner; purge both appendices, fold the `pdal` member anchors into `Packages`, break the Boundary wall into scannable clauses.

### scan/registration.md — 8.5
Excellent N-cloud `RegistrationSession` (PEP-646 `>=2` arity in the type); `_from_tensor` collapse; `RegistrationResult.graduates` two-key `inlier_rmse`/`misfit` ledger. Defects: `content_identity` drift `:31`; `[03]-[RESEARCH]:330` + `[04]-[UPSTREAM]:340`; forward-dep on unbuilt `rasm.compute.graduation.handoff` `:35`. Charter: preserve; migrate identity import, purge appendices.

### scan/deviation.md — 9
Signed-band construction verification (`over/under_extreme`, two-key `max_distance`/`noncompliant_fraction` ledger); `SignedField` `beartype.vale.Is` fence; `Block.unfold` RANSAC peel. Defects: `content_identity` drift `:33`; `[03]-[RESEARCH]:311`; **`[04]-[CROSS_FOLDER]:316`** — section-label DRIFT (siblings use `[04]-[UPSTREAM]`), proving the appendix vocabulary is unstandardized across the corpus. Charter: preserve; migrate identity, purge appendices, kill the label drift.

### scan/reconstruction.md — 9
`_CONSTRUCT` static-constructor table; `MeshQuality.fold` one-pass closure algebra; two-key `nonwatertight`/`noncontiguous` ledger (catches a Poisson balloon closing into 2 shells). Defects: `content_identity` drift `:37`; forward-dep compute `:41`; `[03]-[RESEARCH]:278` + `[04]-[UPSTREAM]:285`. Naming collision risk: local `MeshQuality` here vs `mesh/quality.md` `MeshQuality` capsule — two unrelated types share one name across the folder (semantic-consistency defect). Charter: preserve; rename one `MeshQuality` (this leaf → `ReconQuality` or fold into the shared quality owner); migrate identity.

### mesh/daemon.md — 9 (load-bearing X-boundary owner)
Keyed-drain cache short-circuit; SOURCE-keyed `ContentIdentity.of`; seed-zero (`Some(0)`) GLB wire-key aligned to C# `Rasm.Element/Graph RepresentationContentHash`; `RetryClass.OCCT` transient offload. Defects: `content_identity` drift `:36`; **stale-owner citation `:18`** attributes `DrainReceipt` to `execution/lanes#LANE` — runtime `[V1]` re-homes `DrainReceipt` to `observability/receipts`; **assumes `LanePolicy.offload(..., retry=RetryClass.OCCT)` `:199`** — a `retry=` keyword the runtime lanes `[V5]` verdict does NOT pin (migration risk); **references `evidence/identity#SEED_REPRODUCTION` `GLB_BY_KEY` corpus row `:223`** — runtime `[V7]` splits `SeedReproduction` into `evidence/reproduction.md`, so this anchor + the `GLB_BY_KEY` golden fixture must migrate and be pinned in runtime's reproduction corpus. `[03]-[RESEARCH]:217`. Charter: preserve mechanism; migrate all three upstream anchors; consume the reproduction fixture as a named seam.

### mesh/cad.md — 8.5
Clean OCCT XCAF hop; `READERS`/`_APPLY` tables; `BridgeView` `@overload`; `BridgeFault.of` message factory. Defects: `content_identity` drift `:51`; imports `ReceiptContributor` `:53` but `CadReceipt` never subclasses it — likely dead/annotation-only import (see cross-cutting [C4]); **EMPTY `[04]-[UPSTREAM]:260`** (dead header); `[03]-[RESEARCH]:255`. Charter: preserve; migrate identity; resolve the ReceiptContributor import law; delete the empty appendix.

### mesh/repair.md — 8.5
`MeshRepairOp` two-case union; `manifold3d.batch_boolean` n-ary owner; `RepairStep` step-set parameterization; explicit "`execution.lanes` import path is a deleted form; canonical is `rasm.runtime.lanes`" `:21` (correct). Defects: `GeometrySubject` from unbuilt compute `:35`; imports `ReceiptContributor` `:38`, `MeshResult` conforms structurally (no subclass) — dead-import pattern; EMPTY `[04]-[UPSTREAM]:260`; `[03]-[RESEARCH]:254`. `mesh/repair → python:compute/graduation` is WIRED-UNDECLARED in `ARCH:[02]`. Charter: preserve; declare the graduation seam; resolve ReceiptContributor law.

### mesh/brep.md — 8
Five-case `BrepOp`; `_built` OCCT command-pattern fold over `OcctBuilder` Protocol; `manifold3d.CrossSection` 2D-offset woven into OCCT loft; `_evidence` closure cross-check. Defects: `GeometrySubject` from unbuilt compute `:57`; `content_identity` drift `:58`; imports `ReceiptContributor` `:61`, structural conformance (dead-import); **BOTH appendices EMPTY** (`[03]-[RESEARCH]:423`, `[04]-[UPSTREAM]:426`) = pure dead headers; WIRED-UNDECLARED graduation seam. Charter: preserve the deep evaluator; declare seam; delete both empty appendices.

### mesh/spatial.md — 8.5
Capability-aware `_route` (core-sync vs native-backend-offload); one exhaustive `_dispatch`; `SpatialBackend`/`_NATIVE` `find_spec` probes; `_nearest_hits` lexsort. Defects: BOTH appendices EMPTY (`[03]:288`, `[04]:291`); `MeshSpatial(ReceiptContributor)` subclasses (pattern C, vs siblings' structural); undeclared native band `fcl`/`rtree`/`embreex` (`:3,124`) absent from README `[02]`/`[03]` AND `ARCH:[03]-[COMPANION_LANES]` — a real optional-dependency band that no manifest/architecture surface records. Charter: preserve; record the native-backend probe band in `ARCH:[03]`; delete empty appendices.

### mesh/quality.md — 7.5 (carries a FENCE BUG)
Dense metrology (`ExactTopology` manifold3d tier, `QualityMetrics` five-number distributions, half-edge incidence fold). **FENCE DEFECT: `quality.md:344` `area = exact.area ...` is unindented** — flush-left inside the `_metrics` method body whose surrounding lines are 8-space indented, an `IndentationError` in the product fence. **`quality.md:365` stray closing ` ``` `** after an empty `[04]-[UPSTREAM]:363`. `[03]-[RESEARCH]:358` populated. `MeshQuality(ReceiptContributor)` subclass. Charter: FIX the indentation bug; delete the stray fence + empty appendix; migrate.

### graph/algebra.md — 8
`ComputationalGeometry` union; `CASE`/`NUMERICAL`/`DATASTRUCTURE`/`_FORM` tables; `compas.rpc.Proxy` async bridge; `RetryClass.RPC`+`guarded`. Defects: `content_identity` drift `:49`; forward-dep compute `:48`; **truncated comment `:98-99`** ("…each mint INDEPENDENTLY — not a" then a non-sequitur line — an incomplete sentence mid-edit); **`_SOLVER_LIMITER = CapacityLimiter(4)` `:100`** = one of THREE duplicate limiters (see [C1]); `[03]-[RESEARCH]:409` (no `[04]`). Charter: preserve; migrate identity; collapse the limiter to a runtime-owned band; repair the truncated comment.

### graph/features.md — 8.5
`FEATURE_OPS` detect/project table; `MARK_PROJECT` space-keyed projection (3 arms / 4 kinds); `AnalyticValue` `@tagged_union` with dual `as_scalar`/`peak`; 11-family `ANALYTICS` table with `mode_guard`/`backend=` rows. Defects: **truncated comment `:132`** ("…each mint INDEPENDENTLY, never a" cut off); `_ANALYTIC_LIMITER = CapacityLimiter(4)` `:133` = duplicate #2 ([C1]); forward-dep compute `:43`; `[03]-[RESEARCH]:505` (no `[04]`). `graph/features → python:compute/graduation` IS declared in `ARCH:[02]`. Charter: preserve; collapse limiter; repair truncated comment.

### graph/nonmanifold.md — 7.5 (carries CONTENT CORRUPTION)
`TopologyOp` union; `_CONSTRUCT`/`_BOOLEAN`/`_ANALYSIS`/`GRAPH_ANALYTIC` tables; `AnalyticValue` reuse of the features shape; `IFC_BYTES_CONSTRUCTOR` `SourceKind.IFC` realized. **CONTENT-CORRUPTION DEFECT: `nonmanifold.md:455-457` leaks literal `</content>` and `</invoke>` tool-scaffolding tags into the design page** (raw tool-call XML written into the `.md`). **Truncated comment `:98`** ("…each mint INDEPENDENTLY, never a"); `_ANALYTIC_LIMITER = CapacityLimiter(4)` `:99` = duplicate #3 ([C1]); `content_identity` drift `:42`; forward-dep compute `:41`; EMPTY `[04]-[UPSTREAM]:454`; `[03]-[RESEARCH]:449`. WIRED-UNDECLARED graduation seam. Charter: STRIP the leaked tags; repair truncated comment; collapse limiter; declare seam; migrate identity.

### ifc/selector.md — 9 (clean leaf)
Faithful `lark` grammar mirror of `filter_elements_grammar`; `Facet` 4-case union over 11 upstream facets; `Transformer_NonRecursive`; `@cache` engine; `SelectorQuery` IS the `ReceiptContributor`. No compute dep, no `content_identity` import (clean). Defect: `[03]-[RESEARCH]:316` (6 heavily-loaded load-bearing member confirmations — the exact purge-and-fold-into-`.api` target of `[V11]`). Charter: preserve verbatim; fold the member confirmations into `.api`/`Packages`, delete the framing.

### ifc/analysis.md — 9
`AnalysisRow` union with `of_*` constructors + self-projecting `facts`; `IfcSelector.filter` gate; `_CLASH_RETRY` in-page `stamina.retry`; kind-specific `evidence` ledger; `content.analysis` self-owned span. Defects: `content_identity` drift `:37`; forward-dep compute `:35`; `[03]-[RESEARCH]:414` (6 loaded confirmations, no `[04]`). `ifc/analysis → python:compute/graduation` declared in `ARCH:[02]`. Charter: preserve; migrate identity; purge/fold appendix.

### ifc/costing.md — 9
`LifecycleRow` union; generic `_token[E: StrEnum]` rail-validated vocabulary; `PHASE_DELIMITER`/`DIFF_AXIS` tables; `DiffChange.of_register` marker fold; three-surface `IfcDiff` fold with real `population` denominator. Defects: `content_identity` drift `:35`; forward-dep compute `:33`; `[03]-[RESEARCH]:426` + `[04]-[UPSTREAM]:439` (empty). WIRED-UNDECLARED graduation seam (`ifc/costing`, only `ifc/analysis`+`ifc/structural` declared for the ifc plane). Charter: preserve; migrate identity; declare seam; purge appendices.

### ifc/authoring.md — 9 (cleanest ifc page)
`IfcApiVerb` table IS the closed usecase vocabulary; `Capability` `enum.Flag`; `AuthorPayload` union; `@transactional`/`@stamped` AOP; `railed` `effect.result` slot-resolve; `AuthorReceipt` structural `ReceiptContributor`. NO compute dep (correctly — authoring mints no `GeometrySubject`), NO `content_identity` import (clean). Defect: `[03]-[RESEARCH]:311` (5 confirmations, no `[04]`). Charter: preserve; purge/fold appendix. Model page for the others.

### ifc/structural.md — 9 (largest, deepest)
`MOMENT_KERNELS` Green's-theorem weight table; `PROFILE_SAMPLERS` parametric-subtype table; `Enrichment` union collapsing 9 optional slots + loose tuple; tier-gated `CORE`/`IFC_ENTITY`/`WARPING`; `ClosedRing` `beartype.vale.Is`; `_FE_RETRY` `stamina`; realized `sectionproperties` warping tier. Defects: `content_identity` drift `:42`; forward-dep compute `:40` + deep anchors into unbuilt compute pages (`handoff.md#155`/`#100-128` `:528`, `numerics/quantity.md` cohort `:298`); `[03]-[RESEARCH]:518` + `[04]-[UPSTREAM]:526` (both populated, load-bearing). Carries a mermaid diagram `:500` (unusual for the corpus — no other page has one). Charter: preserve; migrate identity; purge/fold appendices; the mermaid is defensible as a flow aid but is the only one — standardize (keep or drop corpus-wide).

## [02]-[CROSS_CUTTING]

### [C0]-[PHANTOM_ENERGY_PLANE] — campaign-defining, the geometry analogue of runtime-E3 + data-E6
An ENTIRE declared capability plane exists in ZERO code fences. `README:[PY_GEOMETRY]` prose (`README:3`) asserts geometry "owns... the out-of-process AGPL Ladybug Tools energy/environmental companion band (climate, HBJSON building-energy, urban-district massing, and thermal comfort) emitting HBJSON/`DataCollection`/result evidence across the wire." `README:[02]-[ENERGY]` (`README:59-72`) rosters 13 packages: `ladybug-geometry`, `ladybug-core`, `ladybug-comfort`, `honeybee-core`, `honeybee-energy`, `honeybee-openstudio`, `honeybee-standards`, `honeybee-energy-standards`, `dragonfly-core`, `dragonfly-energy`, `queenbee`, `lbt-recipes`, `pollination-handlers`. The `.api/` folder carries 13 authored catalogs for exactly these (`ladybug-*.md`, `honeybee-*.md`, `dragonfly-*.md`, `queenbee.md`, `lbt-recipes.md`, `pollination-handlers.md`, ~200KB). YET: `rg -i "ladybug|honeybee|dragonfly|queenbee|lbt-recipes|pollination|RecipeInterface|DataCollection|HBJSON|EnergyPlus|OpenStudio"` over all 18 fences returns ZERO; `ARCH:[01]-[DOMAIN_MAP]` has NO energy/climate/comfort/massing sub-domain node; `README:[01]-[ROUTER]` has NO energy route (routes stop at 13 = `[ALGEBRA]`). This is a declared folder capability with no owning page, no codemap node, no router route, and no fence — 13 catalogs backing capability no fence delivers, the exact E3 "recipe rail illusion"/E6 "impact hollow plane" defect at plane scale. RULING SHAPE: either (a) stand up an `energy/` sub-domain (climate/`honeybee`-building-energy/`dragonfly`-massing/`ladybug`-comfort owners) with real fences + codemap nodes + router routes, or (b) expel the plane wholesale — strike the 13 README ENERGY rows, the 13 `.api/` catalogs, the pyproject rows, and the README prose claim. A brief must pick; incumbency of the catalogs is not evidence the owner belongs.

### [C0b]-[RECIPE_TRIO_DOUBLE_CLAIM]
`queenbee`/`lbt-recipes`/`pollination-handlers` are tagged `[RUNTIME]` in `pyproject:140-142` and are runtime `[V3]`'s NEW `execution/recipe.md` owner. Geometry `README:70-72` ALSO claims them as ENERGY packages ("geometry binds `RecipeInterface`/`Job` while `runtime` owns execution"). No geometry fence binds `RecipeInterface`/`Job`. The trio is double-claimed and phantom on the geometry side. Resolution: runtime owns execution ([V3]); geometry's claim collapses into [C0]'s ruling (energy plane realized-or-expelled). If (a), geometry's recipe-BINDING (schema `Job`/`RecipeInterface` construction) is a real seam onto runtime's recipe executor; if (b), the README claim + trio rows leave geometry.

### [C1]-[TRIPLE_LIMITER_DUPLICATION] — duplicate mechanism, one owner belongs
Three pages each mint an INDEPENDENT `CapacityLimiter(4)` for the SAME concern (bounding the shared `anyio` thread pool for blocking out-of-process solver/analytics offload via `anyio.to_thread.run_sync`): `algebra.md:100` `_SOLVER_LIMITER`, `features.md:133` `_ANALYTIC_LIMITER`, `nonmanifold.md:99` `_ANALYTIC_LIMITER`. The comments SELF-INDICT: each names the other two as "the per-owner 4-slot PATTERN the siblings each mint INDEPENDENTLY" (`algebra.md:97-98`, `features.md:131-132`, `nonmanifold.md:97-98`) — and all three comments are truncated mid-sentence. This is a cross-cutting concurrency bound that belongs to ONE owner. Runtime `[V5]` gives `LanePolicy.offload` an isolation-modality axis (interpreter | process | THREAD) with `WORKER_BAND` as the process-wide bound; the graph pages' hand-rolled `to_thread.run_sync` + triple `CapacityLimiter(4)` is exactly the thread-modality band runtime should own. RULING SHAPE: collapse the three onto a runtime-owned thread-band offload (`LanePolicy.offload(..., modality=thread)` under `WORKER_BAND`); geometry composes one seam, mints zero limiters.

### [C2]-[IDENTITY_MODULE_DRIFT] — 12 pages break the moment runtime lands
`from rasm.runtime.content_identity import ...` at 12 sites: `algebra.md:49`, `features.md:44`, `nonmanifold.md:42`, `analysis.md:37`, `costing.md:35`, `structural.md:42`, `brep.md:58`, `cad.md:51`, `daemon.md:36`, `deviation.md:33`, `reconstruction.md:37`, `registration.md:31` (imported names span `ContentKey`/`ContentIdentity`/`CANONICAL_POLICY`/`IdentityPolicy`). Runtime `[V4]` DELETES this spelling for `rasm.runtime.identity` (the same drift data-`[V4]` fixes at 22 sites). Every geometry consumer breaks on runtime landing. Compounded: prose already uses the post-split path (`daemon.md:3` `evidence/identity#IDENTITY`, `deviation.md:3` `evidence/identity#IDENTITY`) while the import says `content_identity` — a split-brain identical to data's. Named migration: rewrite all 12 to `rasm.runtime.identity`; the `SeedReproduction`/`GLB_BY_KEY` anchor (`daemon.md:223`) additionally moves to `rasm.runtime.reproduction` per runtime `[V7]`.

### [C3]-[WIRED_UNDECLARED_GRADUATION_SEAMS] — ledger blind to 7 of 11
`rasm.compute.graduation.handoff` is IMPORTED in 11 pages (`algebra`, `features`, `nonmanifold`, `analysis`, `costing`, `structural`, `brep`, `repair`, `deviation`, `reconstruction`, `registration`). `ARCH:[02]-[SEAMS]` declares `python:compute/graduation` for only 4 (`graph/algebra ⇄`, `graph/features ⇄`, `ifc/analysis →`, `ifc/structural →`). WIRED-UNDECLARED (7): `graph/nonmanifold`, `ifc/costing`, `mesh/brep`, `mesh/repair`, `scan/deviation`, `scan/reconstruction`, `scan/registration`. Also: the 4 declared rows are `⇄` (bidirectional) for the graph pairs — but geometry only IMPORTS FROM compute (consumes `GeometrySubject`/`GraduationReceipt`/`HandoffAxis`); a `⇄` implies compute imports geometry, unproven. Plus all 4 intra-geometry edges ([00]) are ledger-invisible. RULING: the seam ledger declares every wired graduation edge (as `→`, not `⇄`, unless a compute→geometry import is proven) + every intra-folder edge; a wired seam without a ledger row is a defect on both surfaces.

### [C4]-[RECEIPTCONTRIBUTOR_IMPORT_INCONSISTENCY] — three patterns, one contradicts itself
`ReceiptContributor` (a runtime-`@runtime_checkable` Protocol) is consumed three incompatible ways: (A) structural-only, NO import — `deviation`, `reconstruction`, `registration` (satisfy `contribute` structurally); (B) import + subclass as base — `daemon.md:167`, `spatial.md:236`, `quality.md:229`, `nonmanifold.md:177` (capsule/tagged-union bases); (C) import WITHOUT subclass — `cad.md:53`, `repair.md:38`, `brep.md:61`, `analysis`, `costing`, `authoring`, `selector`, `algebra`, `features`, `structural` (Struct carriers; several are dead/annotation-only imports). `ingestion.md:17` EXPLICITLY names "a `ReceiptContributor` import standing in for a typed receipt where `IngestReceipt` owns its own `contribute()`" as a DELETED FORM — yet 14 sibling pages import it. The corpus contradicts its own stated law. RULING: pick one — either structural-only everywhere (ingestion's law, delete the dead imports in cad/repair/brep) or import-for-base-class only where a capsule actually subclasses; standardize and enforce.

### [C5]-[FORWARD_DEPENDENCY_ON_UNBRIEFED_COMPUTE] — architectural tension
Campaign order is runtime → data → geometry → compute (`RASM-RUNTIME-BRIEF.md:5`), yet geometry depends UP on `rasm.compute.graduation.handoff` (11 pages) AND cites specific unbuilt compute page anchors: `structural.md:528` `handoff.md#155`/`#100-128`, `structural.md:298` `numerics/quantity.md` cohort, `deviation.md:318`/`reconstruction.md:287` an 8-member `GeometrySubject` union (`registration-transform`, `reconstructed-mesh`, `scan-deviation`, `topology-graph`, `network-graph`, `form-finding`, `numerical-primitive`, `mesh-algebra`), the `GraduationReceipt.graduates(owner, HandoffAxis, key, measured, ceiling)` signature, and the `_admit` residual-over-ceiling fence. NO `RASM-COMPUTE-BRIEF.md` exists. Geometry codes against an unbuilt+unruled surface; if the compute campaign re-shapes `GraduationReceipt.graduates` / the `GeometrySubject` union / `HandoffAxis`, all 11 consumers break. The geometry brief cannot re-plan compute, but MUST pin these as a frozen consumer contract (the 8 literals, the 5-arg graduate signature, the residual-ledger direction) so the compute campaign inherits geometry as the named demanding consumer — the same waterfall shape runtime→data used.

### [C6]-[BANNED_APPENDIX_ROT] — 18/18 + a second invented appendix
Every page carries `[03]-[RESEARCH]` (banned folder-wide by runtime `[V11]` + data `[V11]`). Geometry ADDS a second appendix layer: 11 pages carry `[04]-[UPSTREAM]`, 1 carries `[04]-[CROSS_FOLDER]` (`deviation.md:316` — label drift), 6 carry no `[04]`. Several are DEAD/EMPTY headers: `brep.md:423,426` (both empty), `spatial.md:288,291` (both empty), `cad.md:260`, `costing.md:439`, `nonmanifold.md:454` (empty), plus `quality.md:363` empty + a stray ` ``` ` at `:365`. This is the folder's largest clean prose reduction. RULING (per `[V11]`): purge-and-fold — genuinely load-bearing member confirmations (selector, analysis, authoring, structural, algebra `[03]` bodies) fold into `Packages`/`.api`; "confirmed against the live distribution" framing + version narration + all empty headers delete; the `[04]` appendix layer dies entirely (cross-folder facts belong in the seam ledger, not per-page prose).

### [C7]-[GOVERNANCE_STALE_BLOCKED] — done-but-BLOCKED cards
`TASKLOG.md` `[STRUCTURAL_SECTION_PROPS]-[BLOCKED]:53` is REALIZED on disk: `structural.md` fully authors the `_warping` tier (`calculate_warping_properties`/`WarpingEvidence`, 11 hits). `[KISS_MATCHER_FALLBACK_FGR]-[BLOCKED]:56` is REALIZED: `registration.md` authors the `OPEN3D_FGR` arm + `_engine` selector (9 hits). `[IFC_BYTES_CONSTRUCTOR]-[BLOCKED]:20` code is realized (`nonmanifold.md` `SourceKind.IFC` row) but legitimately BLOCKED on the `topologicpy` AGPL dual-gate. RULING: verify-close STRUCTURAL_SECTION_PROPS + KISS_MATCHER_FALLBACK_FGR (realized, no blocker); keep IFC_BYTES_CONSTRUCTOR BLOCKED (package-admission gated, code authored) — same GRAPH_DEEPEN/GRAPH_COMMUNITY stale-QUEUED class data-`[V12]` closed.

### [C8]-[FENCE_AND_CONTENT_DEFECTS] — hard errors in the product
- `quality.md:344` `area =` unindented inside `_metrics` → `IndentationError` in the fence.
- `quality.md:365` stray ` ``` ` after empty `[04]`.
- `nonmanifold.md:455-457` literal `</content>`/`</invoke>` tool-scaffolding XML leaked into the design page.
- Truncated mid-sentence comments: `algebra.md:98-99`, `features.md:132`, `nonmanifold.md:98`.

### [C9]-[NAMING_COLLISION] — one name, two concepts
`MeshQuality` names two unrelated types: the `scan/reconstruction.md:103` leaf value object (closure algebra) AND the `mesh/quality.md:229` capsule (`ReceiptContributor` metrology owner). Cross-folder semantic-consistency violation. RULING: one canonical name per bounded concept — rename the reconstruction leaf (e.g. `ReconClosure`) or route both through the `mesh/quality` owner.

### [C10]-[UNDECLARED_NATIVE_BACKEND_BAND]
`mesh/spatial.md:3,124` probes `fcl` (python-fcl), `rtree`, `embreex` via `find_spec` as offloadable native backends "the manifest does not declare and no package ships." Legitimate probe-and-offload design, but the band is invisible to `README:[02]`, `README:[03]`, and `ARCH:[03]-[COMPANION_LANES]`. RULING: record the optional native-backend band in `ARCH:[03]` (the same place `manifold3d`/`open3d`/`cadquery-ocp` worker lanes are recorded) so the probe set is governed, not implicit.

## [03]-[UPSTREAM_CONSUMPTION_LEDGER]

### Runtime capabilities geometry ALREADY composes (preserve as consumer contract):
`faults`: `boundary`/`async_boundary`/`RuntimeRail`/`BoundaryFault`/`FAULT_CONF`/`Disposition`/`traversed`/`railed`/`identity`. `receipts`: `Receipt.of(owner, evidence)`/`ReceiptContributor`/`Redaction`/`receipted`/`Phase`/`Signals.emit`+`emit_async`. `lanes`: `LanePolicy.offload`/`.drain`/`.capacity`/`Admit(keyed=)`/`DrainReceipt` (`.cache`/`.faults`/`.hit`). `resilience`: `RetryClass.RPC`/`RetryClass.OCCT`/`guarded`. `identity`: `ContentIdentity.of`/`ContentKey`/`IdentityPolicy`/`CANONICAL_POLICY`/seed-zero `Some(0)` wire-key.

### Upstream capabilities geometry could consume instead of hand-rolling (opportunities):
1. **Runtime `[V5]` thread-modality offload + `WORKER_BAND`** → collapse the triple `CapacityLimiter(4)` ([C1]) onto one runtime-owned band; geometry stops minting concurrency primitives.
2. **Runtime `[V3]` `execution/recipe.md` `RecipeExecution`** → if the energy plane lands ([C0]), geometry's recipe binding composes runtime's recipe executor rather than owning Luigi/queenbee execution (README already concedes "runtime owns execution").
3. **Runtime `[V7]` `evidence/reproduction.md` `ParityReceipt`/`CorpusFixture`** → `daemon.md`'s `GLB_BY_KEY` cross-runtime GLB-by-seed-zero-key golden vector is a reproduction-corpus consumer, not a hand-proven claim (`daemon.md:223`).
4. **Runtime `IdentitySource.lift` three modalities ([V7])** → `daemon`/`cad` whole-buffer content-keying is the buffer modality; geometry should be named alongside `data` as a demanding consumer of the seed-zero wire-key contract.
5. **Data `spatial/mesh` `MeshPayload` (`rasm.data.spatial.mesh`)** — already the declared codec owner geometry defers all mesh-file IO to (`repair`/`brep`/`spatial`/`quality`/`reconstruction` Boundary bullets); the `mesh ← data/spatial [SHAPE]`/`[CODEC_SHED]` seams are real. Data `[V6]`/`[V12]` deepen `MeshPayload` (type-keyed `cell_data`/`point_data`, `.3dm` row) — geometry consumes the deepened shape unchanged (TASKLOG `MESH_TOPOLOGY_SHAPE` confirms).
6. **Data `spatial` Arrow point-record bridge** — `scan/ingestion.md`'s `arrow_las` `ScanSource` case consumes the data `pyarrow.Table` COPC/LAS decode; data `[V5]`'s pushdown ingress + the `laspy.copc` rebind (TASKLOG `SCAN_COPC_PARTIAL`) leaves the bridge shape unchanged — geometry keeps its full `pdal` filter-graph owner.

### Assumptions upstream campaigns INVALIDATE (named migration pressures):
1. `rasm.runtime.content_identity` module path (12 sites) → `rasm.runtime.identity` (runtime `[V4]`). [C2]
2. `DrainReceipt` owned by `execution/lanes` (`daemon.md:18` citation) → `observability/receipts` (runtime `[V1]` re-home). Return-typed, so imports unaffected; prose citation stale.
3. `evidence/identity#SEED_REPRODUCTION` anchor (`daemon.md:223`) → `evidence/reproduction` (runtime `[V7]` split).
4. `LanePolicy.offload(..., retry=RetryClass.OCCT)` (`daemon.md:199`) assumes a `retry=` keyword the runtime lanes `[V5]` verdict does NOT pin — migration RISK, see waterfall demand [W1].
5. Compute `GraduationReceipt.graduates` 5-arg signature + 8-member `GeometrySubject` union + `HandoffAxis` — coded against an UNBRIEFED compute surface ([C5]); any compute-campaign reshape breaks 11 consumers.

## [04]-[WATERFALL_RIPPLE_DEMANDS]

Recorded for a later authoring lane (census lane is read-only; no upstream edits applied here). Each names geometry as the demanding consumer:

- **[W1] runtime `[V5]` (lanes/offload) — pin `LanePolicy.offload`'s `retry=RetryClass` leg.** `mesh/daemon.md`'s OCCT transient-retry (`daemon.md:199`, `retry=RetryClass.OCCT`) DEMANDS an offload-leg retry the runtime lanes verdict does not currently pin. Add a consumer-export clause: `offload` accepts an optional `retry: RetryClass` wrapping the isolation-leg in `guarded`, geometry (daemon) the named consumer.
- **[W2] runtime `[V5]` (lanes/offload) — pin the THREAD isolation modality + process-wide bound as a geometry consumer export.** `graph/{algebra,features,nonmanifold}`'s blocking out-of-process solver/analytics offload ([C1]) DEMANDS `LanePolicy.offload(modality=thread)` under one `WORKER_BAND` bound, replacing the triple `CapacityLimiter(4)`. Name geometry's graph pages as demanding consumers of the thread modality.
- **[W3] runtime `[V5]` (resilience) — name geometry as demanding consumer of the `RetryClass.OCCT` + `RetryClass.RPC` POLICY rows.** Runtime `[V5]` keeps occt/rpc rows but names only `data` as the demanding consumer; geometry is the REAL OCCT consumer (`daemon`, 5 sites) and RPC consumer (`algebra` compas.rpc.Proxy, 8 sites). Add geometry to the consumer-export clause so the rows are preserved as exported names.
- **[W4] runtime `[V7]` (evidence/reproduction) — carry a `GLB_BY_KEY` cross-runtime golden fixture.** `mesh/daemon.md:223` demands a seed-zero-GLB-by-content-key parity fixture in the reproduction corpus (alongside `MATERIAL_LAYER_GOLDEN`); geometry (daemon) the named consumer of the `ParityReceipt` rail.
- **[W5] compute (unbriefed) — freeze the graduation consumer contract.** The 8-member `GeometrySubject` union, the `GraduationReceipt.graduates(owner, HandoffAxis(geometry=subject), key, measured, ceiling)` signature, and the residual-over-ceiling `_admit` direction are consumed by 11 geometry pages. When a compute brief is authored it MUST inherit these as a frozen contract with geometry the named demanding consumer (no compute-side reshape without a geometry ripple). Cannot edit (no brief exists) — flagged for the compute campaign.

## [05]-[VERDICT_CANDIDATES]

1. **PHANTOM ENERGY PLANE — realize-or-expel.** 13 `.api/` catalogs + 13 README ENERGY rows + README prose asserting a full Ladybug Tools energy/climate/comfort/massing band, with ZERO consuming fences, NO codemap node, NO router route ([C0]). Geometry's exact analogue of runtime-E3 (recipe illusion) + data-E6 (impact hollow plane). Evidence: `rg -i "ladybug|honeybee|dragonfly|queenbee" .planning/` = 0 fence hits; `README:3,59-72`; `ARCH:[01]` no node; 13 `.api/` files. A brief MUST rule stand-up (new `energy/` sub-domain with real owners) or wholesale expulsion; catalog incumbency is not owner evidence.

2. **TRIPLE LIMITER → RUNTIME-OWNED THREAD BAND.** Three independent `CapacityLimiter(4)` for one cross-cutting concern, self-indicted by their own comments ([C1]). Compose runtime `[V5]`'s thread-isolation-modality offload under `WORKER_BAND`; mint zero geometry limiters. Evidence: `algebra.md:100`, `features.md:133`, `nonmanifold.md:99` + truncated comments naming each other.

3. **IDENTITY DRIFT — 12 breaking imports.** `rasm.runtime.content_identity` at 12 sites breaks on runtime `[V4]` landing; split-brain with prose already using `evidence/identity` ([C2]). Mechanical corpus-wide rewrite to `rasm.runtime.identity` + the `daemon.md:223` reproduction anchor migration. Evidence: 12 import lines listed.

4. **SEAM LEDGER BLIND — declare the real graph.** 7 of 11 graduation seams wired-undeclared, all 4 intra-folder edges undeclared, 4 declared rows mis-typed `⇄` ([C3]). `ARCH:[02]` must declare every wired edge as `→` (unless compute→geometry proven) + every intra-folder edge. Evidence: import census vs `ARCH:[02]` diff.

5. **BANNED-APPENDIX PURGE + INVENTED `[04]` LAYER DEATH.** 18/18 `[03]-[RESEARCH]` (banned by `[V11]`) plus a geometry-invented `[04]-[UPSTREAM]`/`[04]-[CROSS_FOLDER]` appendix (11+1 pages, several empty) ([C6]). Purge-and-fold load-bearing member confirmations into `.api`/`Packages`; delete the `[04]` layer (cross-folder facts belong in the seam ledger). Largest clean prose reduction. Evidence: section-header census (28 headers across 18 pages).

6. **FORWARD-DEPENDENCY ON UNBRIEFED COMPUTE — freeze the contract.** 11 pages code against `rasm.compute.graduation.handoff` (8-literal union, 5-arg graduate signature, `_admit` direction, unbuilt page anchors) with NO compute brief ([C5]). The geometry brief must pin the consumer contract as frozen so compute inherits geometry as demanding consumer. Evidence: no `RASM-COMPUTE-BRIEF.md`; `structural.md:298,528` anchors into unbuilt compute pages.

7. **RECEIPTCONTRIBUTOR LAW — resolve the self-contradiction.** Three import patterns (structural-only / subclass / dead-import), with `ingestion.md:17` declaring the very import 14 siblings use a DELETED FORM ([C4]). Standardize one law + delete dead imports (cad/repair/brep). Evidence: per-page import-vs-subclass census.

8. **FENCE + CONTENT DEFECTS — hard errors in the product.** `quality.md:344` `IndentationError`; `quality.md:365` stray fence; `nonmanifold.md:455-457` leaked `</content>`/`</invoke>` tool XML; 3 truncated comments ([C8]). These are non-negotiable pre-rebuild fixes — the fences do not parse / the pages carry non-design content.

9. **GOVERNANCE TRUTH — verify-close stale-BLOCKED cards.** `STRUCTURAL_SECTION_PROPS` + `KISS_MATCHER_FALLBACK_FGR` realized on disk but BLOCKED; `MeshQuality` name-collision across reconstruction/quality ([C7],[C9]). Verify-close the two realized cards; rename one `MeshQuality`. Evidence: `structural.md` 11 warping hits, `registration.md` 9 FGR hits vs `TASKLOG.md:53,56`.

10. **NATIVE-BACKEND BAND — govern the probe set.** `mesh/spatial.md` `fcl`/`rtree`/`embreex` probe-and-offload band invisible to README/ARCHITECTURE ([C10]). Record in `ARCH:[03]-[COMPANION_LANES]`. Evidence: `spatial.md:3,124` vs `ARCH:[03]`/`README:[02-03]`.
