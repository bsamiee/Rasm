id: checkout-credentials-to-unpersisted
valid:
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                fetch-depth: 0
                persist-credentials: false
    - |
      jobs: {build: {steps: [{uses: actions/checkout@v7, with: {persist-credentials: 'false'}}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {persist-credentials: False}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                persist-credentials:
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {persist-credentials: '${{ inputs.persist }}'}
    - |
      jobs:
        release:
          permissions:
            contents: write
          steps:
            - uses: actions/checkout@v7
    - |
      jobs: {release: {permissions: write-all, steps: [{uses: actions/checkout@v7}]}}
    - |
      permissions:
        contents: write
      jobs:
        release:
          steps:
            - uses: actions/checkout@v7
    - |
      permissions: write-all
      jobs: {release: {steps: [{uses: actions/checkout@v7}]}}
    - |
      runs:
        using: composite
        steps:
          - uses: actions/checkout@v7
    - |
      jobs:
        build:
          steps:
            - uses: other/checkout@v7
              with: {fetch-depth: 0}
    - |
      jobs:
        build:
          steps:
            - name: actions/checkout@v7
              run: git status
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
    - |
      jobs: {build: {steps: [{uses: actions/checkout@main}]}}
    - |
      jobs: {build: {steps: [{uses: actions/checkout@v3}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: &inputs {fetch-depth: 0}
            - uses: other/action@v1
              with: *inputs
    - |
      jobs:
        build:
          env: &inputs {persist-credentials: false}
          steps:
            - uses: actions/checkout@v7
              with: *inputs
    - |
      jobs:
        build:
          steps:
            - &checkout {uses: actions/checkout@v7}
            - *checkout
    - |
      jobs:
        build:
          steps:
            - &checkout
              uses: actions/checkout@v7
            - *checkout
    - |
      jobs:
        build:
          steps:
            -   uses: actions/checkout@v7
    - |
      jobs:
        build:
          steps:
            - # The repository at the event ref
              uses: actions/checkout@v7
    - |
      jobs:
        build:
          tasks:
            - uses: actions/checkout@v7
    - |
      jobs: {build: {steps: [{uses: local/action, with: {data: {steps: [{uses: actions/checkout@v7}]}}}]}}
    - |
      metadata: {jobs: {build: {steps: [{uses: actions/checkout@v7}]}}}
    - |
      jobs:
        native:
          uses: ./.github/workflows/native.yml
    - |
      jobs:
        build:
          permissions: {}
          steps:
            - run: git status
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {persist-credentials: &keep true}
            - uses: other/action@v1
              with: {flag: *keep}
    - |
      jobs: {build: {steps: [{uses: actions/checkout@v7, with: {persist-credentials: yes}}]}}
    - |
      perms: &shared {contents: write}
      jobs: {build: {permissions: *shared, steps: [{uses: actions/checkout@v7}]}}
    - |
      perms: &shared {contents: write}
      permissions: *shared
      jobs: {build: {steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {fetch-depth: 0 # The whole history
                }
    - |
      jobs:
        build:
          steps:
            - {uses: actions/checkout@v7 # The repository
              }
    - |
      jobs: {release: {permissions: {"contents": 'write'}, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs:
        release:
          permissions: "write-all" # Every scope
          steps:
            - uses: actions/checkout@v7
invalid:
    - |
      permissions:
        contents: read
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                fetch-depth: 0
                filter: tree:0
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {fetch-depth: 0,}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
    - |
      jobs:
        build:
          steps:
            - name: Checkout
              uses: actions/checkout@v7
            - run: git status
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7 # The repository at the event ref
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                fetch-depth: 0 # The whole history
    - |
      jobs:
        build:
          steps:
            - {uses: actions/checkout@v7}
    - |
      "jobs": {build: {'steps': [{"uses": 'actions/checkout@v7', 'with': {"fetch-depth": 0}}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                persist-credentials: true # The default
    - |
      jobs: {build: {steps: [{uses: actions/checkout@v7, with: {'persist-credentials': "TRUE"}}]}}
    - |
      permissions:
        contents: write
      jobs:
        build:
          permissions:
            contents: read
          steps:
            - uses: actions/checkout@v7
    - |
      permissions: write-all
      jobs: {build: {permissions: {}, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs: {build: {permissions: read-all, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs: {build: {permissions: {packages: write}, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs: {build: {env: {contents: write}, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs: {build: {steps: [{parallel: [{uses: actions/checkout@v7}]}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              env: {persist-credentials: true}
    - |
      jobs:
        build:
          steps:
            - "uses": 'actions/checkout@v7.1.0'
              'with': {"fetch-depth": 0}
    - |
      jobs: {build: {steps: [{uses: actions/checkout@v10}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                permissions: {contents: write}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                sparse-checkout: |
                  docs
                  libs
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with:
                fetch-depth: 0
                # Later inputs
            - run: git status
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {fetch-depth: 0 # The whole history
                , filter: tree:0}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              env:
                GIT_TRACE: "1"
    - |
      jobs:
        build:
          steps:
            - parallel:
                - uses: actions/checkout@v7
    - |
      jobs:
        build:
          steps:
            - with:
                fetch-depth: 0
              uses: actions/checkout@v7
    - |
      jobs: {build: {permissions: {contents: none}, steps: [{uses: actions/checkout@v7}]}}
    - |
      permissions: write-all
      jobs: {build: {permissions: {contents: read}, steps: [{uses: actions/checkout@v7}]}}
    - |
      jobs:
        build:
          steps:
            - uses: actions/checkout@v7
              with: {persist-credentials: True # The default
                }
    - |
      permissions:
          contents: read
      jobs:
          build:
              steps:
                  - uses: actions/checkout@v7
                    with:
                      fetch-depth: 0
                      filter: tree:0
                  - uses: ./.github/actions/setup
