# REVIEW F15 — `15-lazy-try-continuations.md`

Checks that passed and are not itemized: all 16 `csharp` blocks match `F15/*.cs` contiguously (line-for-line, modulo the `namespace` line and class indentation); no cross-file reference, no banned token, no `IO<Option<`/`IO<Fin<`, no single-argument `Eff<X>`; 254 lines; `memo`/`Memo<A>.Value`, `Option<A> operator true`/`false`/`|`, `IfNone(A)`/`IfNone(Func<A>)`, `Try<A>(Func<Fin<A>> runTry)` with `Select`/`SelectMany`/`Run`, `Reader.ask`/`asks`/`local`/`With`, `ReaderT.ask`/`with`, `use` overloads, `Bracket(Use:, Fin:)`, `IO.lift(fun(Action))` all exist with the stated signatures. `RunSafe`/`IfNone`/`Run` are named as host operations only; no code block runs an effect.

1. **Location** — section 5, the paragraph after the `Patient`/`FromApp` block (line 158).
   **Claim** — "`ReaderT.with` maps a larger environment, and `Run(env)` returns the `IO<A>` that the host runs"
   **Evidence** — `DECOMP/sigs/ReaderT_3.txt:279: public K<M, A> Run(Env env)`. The lab exits with `queried.Run(settings).As().RunSafe()` (`F15/Samples.cs:95`), and LAB-NOTES F15 records `Run(env).As().RunSafe()`. `23-languageext-traits-and-transformers.md` states the same rule: "`Run` removes one layer", with `Run(settings)` yielding a `K<...>`. The doc as written tells a reader that `Run` hands back an `IO<A>` directly, which does not compile.
   **Correction** — replace the clause with: `ReaderT.with` maps a larger environment, and `Run(env)` returns `K<IO, A>` that `.As()` narrows to the `IO<A>` the host runs with `RunSafe()`.

2. **Location** — section 6, the sentence after the `Time` block (line 220).
   **Claim** — "`Fin` runs on success and on failure."
   **Evidence** — `IO<A>.Bracket(Use, Fin)` is `Bind(x => Use(x).Finally(Fin(x)))` (`DECOMP/IO_1.cs:364-367`) and `IOFail<A>.Finally<X>(K<IO, X>) => this` (`ilspycmd -t 'LanguageExt.DSL.IOFail\`1'`, line 132). Probe built on the F15 lab's own `Scopes.Time`: `Time(a, IO.fail<int>(new Rejected())).RunSafe()` printed `finRan=False`; `Time(b, IO.lift<int>(() => new Rejected())).RunSafe()` printed `finRan=True`. The F15 lab never exercises the failure path through `Time` (`ScopeProbe` runs `DeleteOldLogs` and `DeleteOrder` only), so the claim is unbacked as well as partly false. `22-languageext-effects.md` already carries the correct rule: "A pre-built `IO.fail` node drops the `Finally` effect. A failure raised during execution runs it."
   **Correction** — replace the sentence with: `Fin` runs after a successful continuation and after a failure raised while the continuation runs. A pre-built `IO.fail` passed as the continuation drops `Fin`, so the work handed to `Time` stays a deferred effect.

3. **Location** — section 5, last sentence of the paragraph that begins "This avoids carrying the environment" (line 169).
   **Claim** — "The Reader itself may also be returned so further transformations can be added before `Run`."
   **Evidence** — added prose (the source chapter has no Reader section anywhere in `.scratch/buonanno-functional-csharp/`), and BRIEF forbids `may`/`might`/`could` in added text. It is also the only `may` in the file that is not book prose.
   **Correction** — replace with: A returned Reader accepts further transformations before `Run`.

4. **Location** — section 5, final paragraph (line 171).
   **Claim** — "Reader, a stateful computation, tuples, `Aggregate`, and delegate injection can overlap in purpose. The useful choice is the smallest structure that makes the required flow explicit."
   **Evidence** — added prose with no book source (`grep -n 'Aggregate\|delegate injection\|overlap in purpose'` over the book corpus returns nothing from the lazy-computations chapter). BRIEF: the file shows one path per concern and adds no alternatives; "a stateful computation" and "delegate injection" are neither LanguageExt names nor corpus terminology, and `Aggregate` is a LINQ operator this file never uses.
   **Correction** — delete both sentences, or replace them with the one fact that survives: The environment type is the smallest structure the workflow reads.

5. **Location** — section 1 (line 17) and section 2 (line 72).
   **Claim** — line 17: "the host runs the chosen effect with `RunSafe()`"; line 72: "`RunSafe()` remains the explicit execution boundary and returns `Fin<A>` to the host."
   **Evidence** — the same fact is stated twice; BRIEF forbids restating one fact in a file, and line 72 is the adjusted book sentence that owns the boundary claim.
   **Correction** — trim line 17 to: `IO.lift(Func<A>)` wraps the work as `IO<A>`. The receiving function owns the decision to return it.

6. **Location** — section headings, whole file.
   **Claim** — headings `## 5. Reader defers a shared environment` … `## 8. Ordering determines scope`.
   **Evidence** — the source chapter has seven numbered sections; section 5 (Reader) is new and pushes the book's "Continuations model setup and teardown scopes", "Flat resource pipelines", and "Ordering determines scope" from 5/6/7 to 6/7/8. The book prose and heading text of those three are otherwise kept intact, and the Reader material is lab-verified, so the only question is authorization. BRIEF: "Do not add sections that the integration row does not ask for."
   **Correction** — none if the integration row assigns `Reader`/`ReaderT` to this file (the review task statement lists Reader among this chapter's topics). If it does not, move section 5 out and restore the book numbering 5/6/7.

7. **Location** — section 5 and section 6, what a project still needs.
   **Claim** — n/a (gap, not a false statement).
   **Evidence** — the file names `Run(env)` for `ReaderT` but never the `RunSafe()` that turns the inner `IO<A>` into `Fin<A>`, while sections 1 and 2 do name it for a bare `IO<A>`. A reader who builds a `ReaderT<Env, IO, A>` workflow from this file has no stated exit.
   **Correction** — folded into item 1: naming `K<IO, A>`, `.As()`, and `RunSafe()` in that clause closes the gap with no new sentence.

Blockers: none
