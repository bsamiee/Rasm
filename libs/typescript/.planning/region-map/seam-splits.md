# [TS_REGION_MAP_SEAM_SPLITS]

TS region map — row shapes and protocol live in the [suite standard](../../../csharp/.planning/README.md) ledger-protocol section. Every seam records the wire SHAPE mechanics at the C# owner and the consumption consequence at the TS domain; the branch authors no shape.

- Lifecycle and drain: mechanics at AppHost/lifecycle-and-drain#TS_PROJECTION (json-stj phase and drain shapes); consequence at typescript/state-stores#STREAM_FOLDS (RuntimeFeed fold, surfaced through runtime-host).
- Health and degradation: mechanics at AppHost/health-and-degradation#TS_PROJECTION (json-stj health and degradation shapes); consequence at typescript/state-stores#STREAM_FOLDS (HealthStore fold gating reachable view surfaces).
- Support bundles: mechanics at AppHost/support-bundles#TS_PROJECTION (json-stj support trigger, manifest, and receipt shapes); consequence at typescript/control-edge#COMMAND_GATEWAY (capture-support verb on CommandGateway).
- Runtime ports envelope: mechanics at AppHost/runtime-ports#TS_PROJECTION (json-stj package marker, HLC stamp, receipt-envelope carrier); consequence at typescript/state-stores#RECEIPT_AND_ENVELOPE_FOLDS (envelope carrier binding every structured-text payload, feeding the runtime and evidence folds); the converter-precedence and HLC-stamp discipline reads from AppHost/runtime-ports#WIRE_LAW, never for token grounding.
- Snapshot codecs: mechanics at Persistence/snapshot-codecs#TS_PROJECTION (messagepack header, delta, catalog, and empty-extension shapes plus the GeoJSON projection); consequence at typescript/wire-contracts#DECODE_RAILS (binary rail and embedded-geometry GeometryRail) and typescript/state-stores#STREAM_FOLDS (SnapshotFeed fold).
- Sync and collaboration: mechanics at Persistence/sync-collaboration#TS_PROJECTION (messagepack cursor, op-log, segment, and presence shapes); consequence at typescript/wire-contracts#DECODE_RAILS (binary rail, multi-object segment stream) and typescript/state-stores#STREAM_FOLDS (ConflictPresenceStore fold).
- Remote lane: mechanics at Compute/remote-lane#TS_PROJECTION (proto service shapes, transport-capability shape, fault-detail and artifact-frame shapes; client-stream and bidi structurally excluded on the browser row); consequence at typescript/wire-contracts#TRANSPORT_AND_CLIENTS (WireTransport and generated clients) plus typescript/wire-contracts#DECODE_RAILS (FaultDetailRail) and typescript/control-edge#COMMAND_GATEWAY (control-service verbs).
- Progress and observation: mechanics at Compute/progress-and-observation#TS_PROJECTION (proto progress-phase and mark shapes); consequence at typescript/state-stores#STREAM_FOLDS (ProgressStore monotonic fold).
- Receipts and benchmarks: mechanics at Compute/receipts-and-benchmarks#TS_PROJECTION (json-stj receipt union, host-fingerprint and benchmark-claim shapes); consequence at typescript/view-surfaces#OBSERVATION_ROUTES (BenchmarkRoute fingerprint-gated) and typescript/state-stores#RECEIPT_AND_ENVELOPE_FOLDS (ReceiptStore fold).
- Commands and availability: mechanics at AppUi/commands-availability#TS_PROJECTION (json-stj payload, outcome, intent, availability, invocation, and receipt shapes); consequence at typescript/control-edge#COMMAND_GATEWAY (CommandGateway and IntentRegistry) and typescript/state-stores#STALENESS_AND_AVAILABILITY (AvailabilityStore gate).
- Diagnostics and evidence: mechanics at AppUi/diagnostics-evidence#TS_PROJECTION (json-stj evidence, surface, native-asset, skew-band, row, and timeline shapes; the evidence row carries the envelope whole with no literal geometry field); consequence at typescript/view-surfaces#OBSERVATION_ROUTES (EvidenceTimelineRoute) and typescript/state-stores#RECEIPT_AND_ENVELOPE_FOLDS (EvidenceFeed HLC fold with skew bands, embedded geometry decoded transitively through the wire-contracts GeometryRail).
