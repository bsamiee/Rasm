# [RASM_CS_GEOMETRY_BRIEF]

Campaign: cs track 1/6 — run FIRST (every cs campaign consumes the kernel planes). Phase 1: `Workflow(design-cs-geometry.js)` emits `RASM-CS-GEOMETRY-DECISION.md`; Phase 2: `Workflow(rebuild.js, {targets: <per-leg pages>, brief: "RASM-CS-GEOMETRY-DECISION.md"})` legs per the DECISION partition.

Authoritative campaign input for the ground-up restructure and rebuild of the ROBUST-CORE design corpus inside `libs/csharp/Rasm/.planning/` — the 18 settled `Rasm.Geometry.*` pages (namespace root, `ARCHITECTURE.md:110`) merged by the kernel conversion into the shared eight-folder root (`Numerics/{predicates,faults}`, `Spatial/{index,naming,reconciliation}`, `Parametric/curve`, `Meshing/{delaunay,arrangement,intersect,offset}`, `Processing/{repair,receipts,decimate,flatten,fit,solver}`, `Drawing/{view,pack}`) plus every NEW plane the verdicts mint. They sit beside 34 landed kernel pages (`Rasm.Domain` 7, `Rasm.Vectors` 21, `Rasm.Analysis` 6) that re-express the retired `Vectors/`/`Domain/`/`Analysis/` source ground-up — those pages are STANDING LAW this campaign composes and extends through declared seams, never re-litigates (`[LANDED_KERNEL_LAW]`). The kernel has no sibling reference; `libs/csharp/Rasm/Rasm.csproj` owns the manifest (12 references), `libs/csharp/Rasm/ARCHITECTURE.md` owns the codemap/seams/namespace matrix, `README.md` the router and roster. `docs/stacks/csharp/` governs unchanged and is never restated here. Two downstream campaigns are GATED on this one: `Rasm.Generation` stand-up (`RASM-GENERATION-SPEC.md:179` KERNEL GATE) and the `Rasm.Fabrication` scope expansion — every verdict grades against both.

## [00]-[SHARED_LAW]

### [VERDICT]

The robust core is world-class in isolation and naive as a system: the exact-predicate floor is genuinely award-grade, and the structure above it ships dead engines, unrealized collapses, a missing consumer-gated plane, and fences describing a system the conversion has already replaced. Six proofs, each independently fatal to a world-class claim:

1. **The advertised HLR engine does not exist.** `Drawing/view.md`'s thesis is a Newell-Newell-Sancha BSP painter with Appel quantitative-invisibility (`view.md:3,16`); the fence builds the full BSP apparatus (`Partition`/`Split`/`BspNode`, `view.md:263-300`) and never consumes it — `Paint` takes `bsp` as a parameter (`view.md:302`) and its body never references it. Visibility is uniform `SampleStep` ray-sampling (`Clip`/`OccludedAt`, `view.md:320-347`): an occluder edge between two samples is missed, which is precisely the sampling non-robustness the page claims the exact floor eliminates. `ToPolylines` concatenates all segments of a kind by `Edge.Key` regardless of connectivity (`view.md:130-135`) — garbage polylines. The rebuilt kernel README already advertises "one `ViewOp` over a BSP visibility kernel" (`README.md:65`): the index doc states the target, the fence ships the fake.
2. **The plane both gated consumers demand is absent while the corpus over-invests in what nobody gates on.** The KERNEL GATE (`RASM-GENERATION-SPEC.md:179`) demands surface geodesics/offsets, region subdivision, developable/panelization, and pattern-to-surface mapping; the merged corpus owns every enabling primitive — `ScalarField.Geodesic`/`MeanCurvatureFlow`/`SignedDistanceFromMesh` and `CrossField`/`VectorHeat`/`GeodesicTangent`/`TangentLogMap` cases (`Spatial/fields.md`), isoline extraction (`Processing/extract.md`), streamline tracing (`Processing/flow.md`), log/exp maps + vector-heat transport (`Processing/geodesics.md`), Knöppel cross-fields + stripes and the host QuadRemesh/unwrap capture tier (`Processing/segment.md`) — and no owner one level up composes them into the gate items: zero subdivision/developable/panelization/pattern-map owners anywhere in the corpus (the segment.md host unwrap is a capture tier, not the guaranteed-isometric developable owner). `curve.md`'s `CurveFrom`/`SurfaceFrom`/`CurveApply`/`SurfaceApply`/`ToPolyline`/`ToMesh` are signature-only stubs and `Apply` a two-arm dispatch into them (`curve.md:116-140`), and curve `Offset` — a gate item — is demoted to Growth (`curve.md:19`). Fabrication's toolpath gate is equally unowned: `offset.md` is miter-only straight-skeleton (no `JoinType`/`EndType` corner strategies), `intersect.md` `PlaneMesh` is a single cut (no slice stack), so Fabrication declares its OWN skeleton, HLR, and planar section (`Rasm.Fabrication/ARCHITECTURE.md:22,23,33`) — the kernel's charter leaks downstream by absence.
3. **The settled fences describe a retired system.** The conversion landed a coherent kernel: manifest, README roster, and index docs agree (12 references; the float-domain polygon packages ruled to Fabrication, `README.md:99`), yet the fences still speak the OLD world — `delaunay.md:3` rejects Triangle as "GPL" (Triangle.NET is MIT; the package is already purged — the false rationale survives it), `curve.md:170,190` and `intersect.md:388` cite the deleted `Analysis/Intersect.cs` as the host intersection owner when that capability now lives in `.planning/Analysis/relations.md`, `index.md:27` imports the upward `Rasm.Compute.Solver`, `pack.md:33` imports a `Rasm.Geometry.Topology` namespace that now COLLIDES with the landed `Rasm.Domain` `Topology` smart enum (`Domain/normalization.md`). Worse, the rebuilt index docs assert robust-core capability the fences do not carry — the `[INDEX_DOC_OBLIGATIONS]` ledger — so the kernel's own front door misdescribes its stock.
4. **Five confirmed math/logic bugs ship inside transcription-complete fences.** (a) `flatten.md`'s Dirichlet pin is inverted: `Policy.MassShift` (1e-9) is added to EVERY diagonal (`flatten.md:359`) and the pinned rhs is `value / MassShift` = value·1e9 (`flatten.md:364`) with no large penalty on pinned rows — the interior harmonic/BFF solve is corrupted before `Scatter` overwrites the boundary (`flatten.md:369`); (b) LSCM claims a sparse operator (`flatten.md:3`) and allocates dense `(2n)²` + full `DecomposeSvd` (`flatten.md:377,173`) — O(n³), non-scaling; (c) `naming.md:113` keys `VertexNames` by `entity.IncidentVertices[0]` — the star minimum, not the vertex itself, because `reconciliation.md:55-62` builds `star[v]` as self+neighbours sorted with no self-index field — every vertex not smallest in its star registers under a neighbour, corrupting `ResolveBoundary` and every downstream `TopoSignature`; (d) `receipts.md` mints boolean receipts `AssetGated: false` (`receipts.md:65`) while `Converged` requires `b.AssetGated` true (`receipts.md:99`) — every heal session containing a successful managed boolean reports non-converged; (e) `decimate.md` hardcodes the interior link condition `shared == 2` (`decimate.md:310`) so boundary edges (`shared == 1`) never collapse — `TargetFraction` unreachable on open meshes, spurious `DecimationFault` — while `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` enumerate the full `edit.Faces` array per collapse (`decimate.md:293,316-360`): the vertex-vertex `Adjacency` the store maintains across collapses (`decimate.md:87,328-333`) never feeds a vertex→incident-face index, so the drain is O(F²).
5. **The robust-core namespace partition is unratified and the corpus rides two paradigms.** The landed matrix (`ARCHITECTURE.md:105-110`) freezes four roots and holds the whole robust core as a placeholder — `Rasm.Geometry.*`, "the geometry campaign owns its namespace reconciliation" — while the fences declare ~14 concept namespaces, four pages declare none (`predicates.md`, `receipts.md`, `naming.md`, `reconciliation.md`), the collision-proven phantom `Rasm.Geometry.Topology` survives at three live sites (`pack.md:33` import, `pack.md:18` packages row, `solver.md:13` owner prose), and Fabrication's ledger consumes the kernel heal/boolean edge cross-package (`Rasm.Fabrication/ARCHITECTURE.md:44`, `csharp:Rasm/Processing/repair`). Separately the corpus ships a DUAL operation/validity paradigm: the landed kernel legislates the ONE Op-threading law (`Op` explicit value key, `Eff<Env>` carriage, `rails.md:14,234-238`), the `IValidityEvidence` validity fold, and the canonical admission vocabulary (`Domain/validation.md`), while every settled fence rides bare `Fin` entries with hand-rolled guards and per-receipt `IsValid` — exactly the dual paradigm the substrate forbids. And the kernel still references app-platform UPWARD: `index.md:27` `using Rasm.Compute.Solver` with `ToAcceleration` returning Compute's type (`index.md:572-579`) — the violation `libs/.planning/architecture.md:35` forbids, made stark by the landed seam ledger already declaring the edge as a kernel-owned node-array wire (`ARCHITECTURE.md:86`).
6. **Declared collapses are unrealized — the corpus states its own law and ships the violation.** `intersect.md:388` names itself the single exact-crossing owner and lists the three inline copies to collapse; all three still live (`arrangement.md:242-262` `EdgeCrossings`/`InTriangle`/`PlaneCrossPoint`, `offset.md:440-444` `SegmentsCross`, `repair.md:346-372` `TriangleCrossPoint`/`EdgesCrossTriangle`). `offset.md:16,478` claims the medial axis "reconciles against the constrained-Delaunay Voronoi dual" while `MedialFrom` builds the tessellation and DISCARDS it (`offset.md:325-326`) — `delaunay.md` exposes no Voronoi-dual surface even though the rebuilt README already asserts "delaunay owns the constrained Voronoi dual" (`README.md:99`). `fit.md:740` claims the `Refine` body reuses "the `Processing/solver#CONSTRAINT_SOLVER` damped Gauss-Newton λ-ladder"; its fence re-implements the ladder (`fit.md:575-600` hardcoded `1e-3` seed, `×10.0` climb, `1e12` ceiling, no λ carry-down on the accept branch) beside `solver.md:303-346`, whose `SolvePolicy` (`solver.md:77-84`) already carries the `LambdaUp`/`LambdaDown` ladder as policy. `arrangement.md` claims implicit-point exactness (`arrangement.md:467`) while `PlaneCrossPoint` rounds the operand crossing to a `double` (`arrangement.md:258-262`) and feeds it as an explicit `Constraint.Segment` — the README already advertises "implicit-point crossings" (`README.md:42`); the headline boolean is exact-sign, rounded-vertex.

What IS sound (preserve; harden, never re-derive): the `predicates.md` four-tier ladder with the four-way independent oracle (the corpus crown jewel), the `faults.md` single-union band shape, `solver.md`'s analytic-Jacobian LM with witness DOF, `index.md`'s BVH/octree/PLOC/GWN breadth, `flatten.md`'s DEC composition shape over the landed `MeshAdjointSnapshot` seam, `arrangement.md`'s composition depth, `reconciliation.md`'s frozen fixture spine, and the corpus-wide external-member discipline. The rot is structural — dead engines beside live claims, missing consumer-gated planes, duplicate owners across strata, an unratified namespace tier, a substrate law the robust core ignores — and no per-page cold pass fixes it. Architecture first.

### [TELOS]

`Rasm`'s robust core is the federation's exact-arithmetic computational-geometry floor AND the operational geometry plane two gated campaigns compose — one body. The robust tier (adaptive exact predicates, exact CDT/CDTet, exact arrangement/boolean, exact crossing lattice, robust skeleton/medial, predicate-gated heal/decimate/flatten/fit/solve) is necessary but not sufficient; the consumer tier (Generation's SpineRef forms: stations/frames, isolines/geodesics/offsets, subdivision, panelization, pattern-to-surface mapping; Fabrication's toolpath grade: corner-strategy offsets, slice stacks, 3D medial/skeleton, booleans at manufacturing tolerance, developable/unroll, hull) sits ON TOP and composes it plus the landed kernel planes. Every owner grades against both tiers at once. The hand-roll law is calibrated, not absolute: author from first principles where no admissible package carries the robustness guarantee (verified TRUE for predicates, CDT, arrangement, crossing, skeleton, constraint-LM); compose the categorical-best engine where the ecosystem owns the concern (GShark NURBS, kd-tree leaf, Manifold at scale, MIConvexHull hull complex); NEVER both for one concern. Assume 5x consumer demand: the parametric tier feeds every generated form, the toolpath plane feeds every CNC/AM program, the encoding plane feeds every GPU residency lane, the naming plane feeds every persistence diff — zero current consumers never lowers the bar. One geometry owner per runtime; the kernel meets hosts at the wire.

### [LANDED_KERNEL_LAW]

The 34 conversion-landed pages are standing law with settled charters; the campaign composes them, extends them only through seams the DECISION declares, and never re-opens their landed rulings — the validity-fold mechanism and Op-threading law (`Domain/rails.md`), the `ContentHash`/splitmix64 determinism owner (`Domain/identity.md`), the canonical admission vocabulary (`Domain/validation.md`), the `AtomProjection`/`ProjectionRow` projection dispatch (`Numerics/atoms.md`), the `matrix.md` solve/factor/eigen owners, the one neighborhood substrate (`Spatial/neighbors.md`), the `MeshSpace`/`LaplacianCache`/`IntrinsicMesh` substrate with the one cotangent owner and restricted power diagram (`Meshing/mesh.md`), the DEC assembly (`Meshing/dec.md`), the reconstruction/SHM/mesh-SDF spine (`Meshing/reconstruct.md`), the field algebra with the public `SampleDetailed` rail (`Spatial/fields.md:367`), the `VectorIntent` rail with the `Topology(MeshSpace)` projection (`Processing/intent.md:214`), and the conversion's drop/absorb register (blueprint D1-D9: `PoissonPolicy` octree knobs dead, `GeometryRequest`/`GeometryProjection`/`AnalysisAcceptance` absorbed, the splitmix64/cotangent/RTree/Combine/slerp/hex-spacing duplicates collapsed to single owners). Consequences that bind every verdict: (a) kernel-member verification READS THE LANDED FENCES — the formerly-unverifiable seam members are now designed public surfaces (`ScalarField.SampleDetailed` at `fields.md:367`, `MeshKernel.TopologyDetailed`/`TopologyReceipt` at `mesh.md`, `DiscreteCalculus` sparse shape at `spectral.md:191` over the `matrix.md` `SparseMatrix` CSR owners) — plus the `.api` catalogues for external members; `assay api` has no kernel assembly to decompile and is not this campaign's verification spine; (b) a robust-core structural change (rename/move/merge/split) sweeps the LANDED pages' inbound references to the changed names/paths as 1-hop ripples in the same motion — the landed corpus consumes the robust core (`relations.md` names `Meshing/intersect`'s altitude boundary, `query.md` routes `locate.md`, twelve landed pages compose `MeshSpace` against settled consumers); (c) where a verdict below names a landed page, the duty is a seam declaration or a bounded extension riding that page's own Growth law, never a charter rewrite.

### [STRUCTURAL_AUTHORITY]

Phase 1 owns the ROBUST-CORE page-set absolutely: split any settled page into a folder, merge pages, move concerns across sub-domain boundaries, delete pages after absorption, mint new folders, rename — all inside the ONE `.planning/` root: a new folder mint (`Solving/`) is in-authority, and the landed eight folders keep their landed pages' homes. Calibration examples, not the ceiling: `Solving/` lifting the `[V7]` one-LM family (solver, fit) out of the Processing grab-bag; `MeshEdit` extracting from `repair.md` into the Meshing substrate plane beside the landed `mesh.md`; `BooleanOp` re-homing from Processing to Meshing. The folder-architecture law binds: folders are ≥2-page domain groups and real growth axes (the landed partition already satisfies this — the one-file `Parametric/` violation is resolved: curve.md sits beside the landed `projections.md`/`locate.md`); TIER-2 concept namespaces ratify per `[V1]`. Every structural change updates `ARCHITECTURE.md` `[01]-[DOMAIN_MAP]`+`[02]-[SEAMS]`+`[03]-[NAMESPACE_MAP]`, `README.md` router/roster paragraphs, and the `faults.md` cluster taxonomy in the same motion, and runs the `[LANDED_KERNEL_LAW]` (b) inbound-reference sweep. No page survives on incumbency: a page that exists is not evidence it belongs.

### [PLACEMENT_LAW]

The strata law is absolute: `Rasm` references no sibling (`libs/.planning/architecture.md:35`). The robust core composes the landed kernel planes and admitted external packages, NEVER `Rasm.Compute`/`Rasm.Fabrication`/any upward stratum — every wire type the kernel emits is kernel-owned and downstream-decoded (the `index.md:27` inversion dies; the landed seam row `ARCHITECTURE.md:86` already declares the edge as node arrays — the page follows the ledger). Float/production geometry engines live at their consuming stratum — now landed law, not proposal: the kernel README rules the float polygon lanes (`Clipper2`, `CavalierContours`, `SharpVoronoiLib`, `LibTessDotNet`) to `Rasm.Fabrication`, never the robust core (`README.md:99`); their central pins re-group to the Fabrication label with final disposition in `RASM-CS-FABRICATION-BRIEF.md` `[04]`. Concerns owned twice across strata resolve to the kernel owner with the downstream consuming through the seam: Fabrication's `Posting/Projection.cs` BSP HLR dies for `DrawingProjection`, its `Toolpath/Skeleton.cs` dies for `Offsetting.Apply`, its in-folder planar section dies for the slice-stack owner (`Rasm.Fabrication/ARCHITECTURE.md:22,23,33`). Host parametric meets the kernel at the wire: the landed `Analysis/relations.md` is the Rhino-parametric intersection owner (in-kernel, RhinoCommon-composing per the Rhino ruling — the kernel remains RhinoCommon-aware end to end, the pure-numeric floor host-neutral-shaped without a host-free assembly), `curve.md` the GShark host-neutral owner, meeting at control points + knots + weights; the settled fences' citations of the deleted `Analysis/Intersect.cs` re-anchor to `relations.md`.

### [FOUNDATIONS_FIRST]

Build order is a dependency law: the Numerics floor before every consumer; the mesh substrate before the Meshing lattice; the Meshing lattice before Processing; Processing before the Parametric tier and Drawing. The landed kernel planes are ambient floors — already realized, never re-waved: a robust-core page composes `matrix.md`/`neighbors.md`/`mesh.md`/`fields.md`/`geodesics.md` freely in any wave. A consumer page may not hand-roll what its floor owns (the crossing-collapse law); a floor too thin to serve its consumers is the floor's defect, never license to reinvent (the delaunay Voronoi-dual accessor). A seam declared in `ARCHITECTURE.md` and not composed in a fence is a defect on BOTH pages. The leg partition is a topological order over the corrected seam ledger; an edge into a later wave is a partition defect resolved by re-homing or re-waving, never tolerated.

### [GENERATOR_LAW]

The corpus norm — every owner a `[Union]`/`[SmartEnum]` + FrozenDictionary data-table dispatch with rosters as seed DATA, the landed pages the freshest exemplars (policy records over knob spam: `sample.md`'s preset collapse of the 22-param CCVT, `intent.md`'s case-family constructors) — binds the NEW planes: subdivision schemes are stencil rows over ONE refinement fold (Catmull-Clark/Loop as data, never sibling subdividers); corner strategies are `JoinType`×`EndType` rows over ONE offset assembly; slice stacks are policy rows (uniform/adaptive height) over ONE section fold composing `PlaneMesh`; panelization families are policy rows over ONE cross-field-guided mapper; pattern tilings are symmetry-group rows over ONE log-map instancing fold; fault growth is one case row in the owning cluster's sub-band. The test: the next scheme, join, layer policy, panel family, or tiling group is one data row, never a new arm, a second lowering, or a new namespace.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner: `public static Fin<TResult> Apply(TOp op, ...)` discriminating on the closed `[Union]` — already the corpus norm; sealed as law for every new owner (no `Build*`/`Get*` sibling families).
- ONE paradigm: robust-core rebuilds adopt the landed substrate mechanisms — the `Op? key = null` threading per the `rails.md` threading law, receipts registering into the `IValidityEvidence` validity fold (killing per-receipt hand-rolled `IsValid`), input guards through the `validation.md` admission vocabulary — while `GeometryFault` stays the robust-core fault family per the two-family seam (`ARCHITECTURE.md:112`): substrate mechanisms, geometry faults, no dual rails.
- Exception escapes are forbidden on the rail: `index.md`'s `throw new InvalidOperationException` (`index.md:130,574,579`) and the Ray/Overlap/Winding-over-PointCloud silent empty (`index.md:358` vs its own ":351 never-a-silent-empty" prose) are the breach class; kind mismatch routes `Fin` fail with a typed `GeometryFault`; `ToAcceleration` returns `Fin<>` over the kernel-owned wire the ledger declares.
- The exactness contract is honest per owner: a page states where SIGNS are exact and where COORDINATES are rounded; an overstated claim (`arrangement.md:467` implicit-point framing over `:258-262` rounded crossings) is repaired by a richer implicit-point-capable `Constraint` case or an honest re-scope — never left as prose, and the index-doc row re-scopes in the same motion.
- The store-mutability contract settles once, corpus-wide: SoA stores (`SimplexStore`/`WavefrontStore`/`IntersectStore`/`ArrangementStore`/`QuadricStore`/`NodeStore`) are MUTABLE build arenas; "hash-friendly immutable record" prose dies where the fence mutates (`delaunay.md:5,66-74`, `arrangement.md:87-91,488`, `offset.md:5` naming `WavefrontStore` immutable against `:13`'s "the event queue mutates"); content-addressing binds the frozen emitted result through `reconciliation.md`'s `Encode` composing the landed `Domain/identity.md` `ContentHash.Of` (the ledger row `ARCHITECTURE.md:79` already states it), never a mid-build store; `WavefrontStore`'s `2n` allocation vs unbounded `Spawn` (`offset.md:195`) is the sizing defect the contract fixes; and the contract settles the arena CONCURRENCY law in the same motion — an arena is single-writer for its build and publishes by freeze into the immutable result, only frozen surfaces feed parallel folds, the landed `LaplacianCache` Lock-guarded success-only-memo semantics the exemplar, and the `[V6]` `ParallelHelper` admission binds to frozen or partition-disjoint spans only.
- Cross-page member claims verify against the landed fences and `.api` catalogues per `[LANDED_KERNEL_LAW]` (a); a member neither fenced nor catalogued is a phantom.
- Frozen fixtures are harness-confirmed, never asserted: the `0x9462A71A…` digest (`reconciliation.md:147`) and the `CLASH_GOLDEN` 160-byte stream re-prove against actual `XxHash128`/`NodeLinkProjection` output before any page cites them as REAL.

### [INDEX_DOC_OBLIGATIONS]

The rebuilt kernel index docs assert robust-core capability the settled fences do not yet carry. Each row is a campaign obligation: LAND the capability in the owning fence, or the DECISION rules the honest re-scope and edits the index row in the same motion — an index doc never keeps a claim its fence falsifies. The ledger: `README.md:42` + `ARCHITECTURE.md:42` arrangement "implicit-point crossings" + "`BooleanOp` cell welds" + "retires the tier-3 native CSG gate" (`[V4]`/`[V5]`); `README.md:99` "`Meshing/delaunay` owns the constrained Voronoi dual" + "`PlanarOverlay` is the exact 2D polygon boolean and messy-winding fill" — the LibTessDotNet replacement claim (`[V10]`c/`[V3]`); `README.md:65` + `ARCHITECTURE.md:63` view "one `ViewOp` over a BSP visibility kernel" (`[V3]`); `README.md:86` the `TYoshimura.DoubleDouble` 106-bit accumulation INTO the decimate QEM quadric fold and the fit LM Σd² objective, and the `PeterO.Numbers` directed-rounding interval filter (`[V9]`/`[V14]`); `README.md:93` the kd-tree lane "feeds the `Processing/fit` MLESAC … and the `Processing/register` ICP correspondences" — fit's fence composes no `neighbors.md` lane yet (`[V9]`); `ARCHITECTURE.md:112` "`Numerics/Faults.cs` and `Domain/Rails.cs` each state the seam" — `rails.md` does, `faults.md` does not (`[V12]`).

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.csproj` (12 references); per-package catalogs under `libs/csharp/Rasm/.api/` (12), shared substrate under `libs/csharp/.api/` (30). The conversion LANDED the bulk of the prior reconciliation — Triangle purged (pin + reference + kernel catalog), `geometry3Sharp` and `MathNet.Numerics.Providers.MKL` dropped from the kernel csproj (pins stay for Bim/Fabrication/Compute; their disposition is those campaigns'), the float polygon four (`Clipper2`/`CavalierContours`/`SharpVoronoiLib`/`LibTessDotNet`) out of the kernel manifest with their kernel-tier catalogs deleted and the Fabrication-lane law written (`README.md:99`), `QuikGraph` admitted kernel-side with one bounded lane (`neighbors.md` Prim-MST; `README.md:121` — segment's normalized cut stays spectral through `matrix.md`, never a combinatorial cut), `MIConvexHull` consumer-backed (`cloud.md` hull rail, `README.md:100`), the kernel `api-mathnet-providers.md` trimmed to OpenBLAS-only. Remaining obligations, feed-verified: RESOLVE the `MathNet.Numerics 6.0.0-beta2` floor (`Directory.Packages.props:62-64`; `6.0.0-beta2` remains the newest release, no stable 6.0 exists — ruled default: downgrade to `5.0.0` stable, the beta pin surviving only on a fence-composed 6.0-only member recorded with a bump blocker — and the survival sweep covers EVERY central-pin consumer's fences: the landed kernel pages (`matrix.md`/`integrate.md`/`spectral.md`/`dec.md`/`reconstruct.md` and every MathNet-composing fence), the robust-core solve rails, Compute's solve/SVD/statistics rails, and Fabrication's capability statistics — `RASM-CS-COMPUTE-BRIEF.md`/`RASM-CS-FABRICATION-BRIEF.md` the co-consumers); VERIFY the OpenBLAS keep (`MathNet.Numerics.Providers.OpenBLAS` survives as the sole opt-in native accel documented by `matrix.md` — the survey proves an osx-arm64 native asset exists or the keep is decorative and the drop re-opens with evidence); ADMIT Manifold (Apache-2.0) as the named tier-3 boolean engine per `[V5]`; EVALUATE `CommunityToolkit.HighPerformance` admission (`Directory.Packages.props:38` pinned, kernel-blind) when the `[V6]`/`[V9]` SoA-arena motion lands — admit-on-composition, never blind referencing; AUTHOR the absent LanguageExt catalog — `LanguageExt.Core 5.0.0-beta-77` (`Directory.Packages.props:29`, published 2025-12, active) is the rail every fence composes and NO `.api` file exists in either tier (a track-shared obligation: the first campaign to run lands the ONE substrate-tier authoring, later campaigns verify, never re-author); CORRECT the shared-tier `api-hashing.md:82` falsehood (`XxHash3.HashToUInt64`/`ulong` — the truth is the kernel overlay's `XxHash128.HashToUInt128` seed-zero/`UInt128` federation entry); DEDUPE the byte-identical cross-tier copies (`api-csparse.md`, `api-tensors.md`) to one owning tier each, and VERIFY the deliberate kernel overlays (`api-mathnet-numerics.md` FFT-trimmed, `api-mathnet-providers.md` OpenBLAS-only) stay proper folder-specific subsets rather than drifting forks; RE-KEY the `IDEAS.md` `[WATCH_PACKAGES]` card — `geometry4Sharp` HAS a NuGet release (1.0.0, 2022-11), so "on first NuGet release" is a dead trigger; the card re-keys to a post-2022 release signal (`CSparse.Extensions` remains unpublished; `TVGL` remains 2019-stale; `Kemsekov.GraphSharp` 3.1.2, 2024-02). License gate: OSS and free-for-OSS-commercial admissible; GPL/AGPL/pay-tiered/seat-licensed REJECTED — recorded rejections that are never re-proposed: SISL (AGPL), TetGen (AGPL), CGAL straight-skeleton/skeletonization (GPL), SolveSpace (GPL), CuraEngine/Slic3r cores (AGPL), Instant Meshes (license NOASSERTION, dormant since 2022), MeshLib (free-non-commercial/paid-commercial), MCUT (LGPL/commercial, dominated by Manifold).

## [01]-[PHASE_ARCHITECTURE]

ONE ephemeral design workflow (`design-cs-geometry.js`, deleted after landing) runs survey -> draft -> judge -> decide -> salvage and emits `RASM-CS-GEOMETRY-DECISION.md`. Shape:

- **Survey**: 5 read-only plane agents partitioned over the merged corpus — robust-core Numerics+Spatial / robust-core Meshing / robust-core Processing / Parametric+Drawing+consumer gates / both `.api` tiers+manifests — each deep-reading its settled pages FULLY and its folder's landed kernel pages as standing-law context, re-verifying `[02]-[EVIDENCE_REGISTER]` against disk (the register is initial pointer, never ceiling; conversion-era line drift is expected and corrected, never treated as refutation), resolving kernel members against the landed fences and external members against the `.api` catalogues + the nuget MCP, and sweeping every `using Rasm.*` and cross-package ledger edge into the raw seam census. TWO FEASIBILITY PROBES run beside the lanes and their verdicts are draft evidence (a draft assuming FEASIBLE against an INFEASIBLE probe loses at judge): (i) the GShark envelope probe — every member the `[V2]` floor names and every member the NEW pages would plausibly compose verified against `api-gshark.md` + feed/upstream corroboration, numerical-quality signals swept — verdict FEASIBLE / FEASIBLE_WITH_GAPS (each gap named with its vendor-and-own or owned-op closure) / INFEASIBLE; (ii) the implicit-point probe — the `[V4]`/`[V10]`c hinge (and the `[V10]`b orientation guarantees riding it) read at algorithm depth: the constraint-recovery walk, the vertex representation, where a rounded coordinate first enters, the landed `predicates.md` `Lpi`/`Tpi` floor — verdict with the minimal re-scope named.
- **Draft**: 4 independent COMPLETE page-set proposals — one row per robust-core or NEW page carrying action (KEEP/REBUILD/SPLIT/MERGE/MOVE/DELETE/NEW), owner charter with namespace + fault cluster, entry signature (`Apply` op-union or NONE for vocabulary pages), in/out seam edges (landed-page seams included), and wave — drafted under four lenses (floor-maximalist, consumer-gate-first working backward from the Generation SpineRef and a Fabrication toolpath program, roster-mining-first, folder-architecture-first). A draft leaving an evidence row undisposed, a verdict unanswered, or an `[03]` delta homeless is incomplete and loses at judge; so is a draft that re-litigates a landed charter.
- **Judge + Decide**: three adversarial judge lenses (law-gates, consumer-reach, partition-elegance) score against `[00]` law + `[03]` targets — verdict disposition, strata acyclicity (zero upward edges, zero page cycles), and both consumer gates fully homed are disqualifying; capability reach then surface collapse then elegance rank survivors; the decision is one blueprint with grafts, and the red-team decide gate RULES BOTH PROBE HINGES from probe evidence — the GShark hinge (floor-as-is / per-gap owned ops / vendor-and-own NOW) and the implicit-point hinge (the `[V4]` exact path, or the honesty re-scope cascading across `[V4]`+`[V10]`c in one motion) — baked into the affected page rows, never deferred mid-leg.
- **Salvage**: per-draft miners + overturn auditor + ONE writing integrator; a mined finding is accepted only with a disk or `.api` file:line anchor the auditor re-verifies.
- **Emit**: `RASM-CS-GEOMETRY-DECISION.md` at the repo root carrying (a) the final page-set table with per-page skeletons in BOTH action vocabularies — the semantic action (KEEP/REBUILD/SPLIT/MERGE/MOVE/DELETE/NEW) and its rebuild-engine lowering (kind `new|rebuild|improve`, delete rows, absorb `{into, from}` pairs) so the durable engine executes the DECISION unmodified, (b) the ratified robust-core namespace/fault-cluster partition inside the landed folder root, (c) the corrected seam ledger (owner + counterpart ledgers, one anchor per seam, landed-page seams as explicit rows), (d) roster deltas with `.api` obligations, (e) the leg partition proven acyclic with per-leg engine-invocation rows, (f) the `[T-BOOLEAN-NATIVE-ASSET]` card disposition and the two probe rulings as standing law.

The DECISION must resolve every verdict below. Recommended shapes follow each; the design pass may exceed them, never dilute them.

### [V1_STRUCTURE_RATIFICATION]

The robust-core namespace tier is ratified ONCE, discharging the duty the landed matrix assigns ("the geometry campaign owns its namespace reconciliation", `ARCHITECTURE.md:110`). The landed law fixes TIER-1: folder is domain grouping, fence namespace is contract, and the frozen roots (`Rasm.Domain`/`Rasm.Vectors`/`Rasm.Analysis`) span folders BY CONTRACT — the two-tier equation binds the ROBUST CORE only: its TIER-2 concept namespaces map 1:1 to fault sub-bands, a concept namespace never spans two folders, and each folder charter enumerates its namespaces in `ARCHITECTURE.md:[03]`, replacing the `Rasm.Geometry.*` placeholder row with the enumerated partition. The realized ~14 fence declarations are the truth candidate; the four namespace-less pages (`predicates.md`, `receipts.md`, `naming.md`, `reconciliation.md`) gain declarations; the census is not 1:1 (13 concept namespaces vs 12 clusters at `faults.md:14`: `Tessellation` owns no cluster — rule delaunay's fault home — and `Parametric` borrows, `[V12]` mints) — the ratified partition disposes both. The phantom `Rasm.Geometry.Topology` dies everywhere: the collision guard is now PROVEN, not feared — `Rasm.Domain` mints a landed `Topology` smart enum (`Domain/normalization.md`) the props global usings inject — so the phantom's THREE live sites re-anchor to the ratified naming/reconciliation namespace (ruled: `Rasm.Geometry.Naming`, with the `topology` fault cluster 2404-2407 renamed in the same motion): the `pack.md:33` import, the `pack.md:18` packages row crediting `CanonicalTopology.OfMesh`/`NamingHashOps.Encode` to the phantom, and the `solver.md:13` owner prose homing `EntityKind` there — and Fabrication's `BooleanOp` ledger row (`Rasm.Fabrication/ARCHITECTURE.md:44`, `csharp:Rasm/Processing/repair`) follows the `[V5]` re-home. Folder deltas stay within `[STRUCTURAL_AUTHORITY]`: the recommended floor keeps the six robust-core-bearing folders and lifts `Solving/` (solver, fit — the `[V7]` one-LM family) out of Processing; `Drawing/`'s charter question stands (view + pack share no concern today; ruled default: the folder charter becomes downstream emission = projection + residency encoding, renamed to match, and pack re-homes only if that charter fails the growth-axis test); every folder delta updates the codemap and runs the `[LANDED_KERNEL_LAW]` (b) inbound-reference sweep.

### [V2_PARAMETRIC_TIER]

The campaign-defining coverage verdict: `Parametric/` grows into the surface-development tier that closes the Generation KERNEL GATE (`RASM-GENERATION-SPEC.md:179,226`). The landed pages sharpen the tier's boundaries: `projections.md` (Rhino-native evaluation selectors, THE single shape-operator and pose-slerp owners) and `locate.md` (the Rhino-altitude `Locator`/`LocationValue`/`Division` location algebra, `Rasm.Analysis`) are the HOST-side captures; the `[V2]` tier is the HOST-NEUTRAL algorithm owner riding GShark, meeting them at the wire per the runtime-split law — stations/frames land in `curve.md` on GShark's surface, never as a second location algebra beside `locate.md`. The recommended-shape floor, all owners composing the landed kernel planes and the flatten `ChartAtlas`, never re-deriving them: `curve.md` REBUILT full-body (the stubs at `curve.md:116-140` die; the retired-source citations at `curve.md:170,190` re-anchor to `Analysis/relations.md`; curve `Offset` promoted from Growth (`curve.md:19`) to a first-class `ParametricOp` case; the station/frame family lands as first-class cases — arc-length stations via `Divide(maxSegmentLength, equalSegmentLengths)`/`ParameterAtLength`/`PointAtLength` sweeping rotation-minimizing `PerpendicularFrames`, all `.api`-verified (`api-gshark.md:81-88`), evaluated over the `SpineRef` `[T0,T1]` window on GShark's normalized-domain convention (`api-gshark.md:165`) — the `PathRow` placement input `RASM-GENERATION-SPEC.md:138,164` consumes; SSI disposition explicit — GShark has no surface-surface intersection; ruled default: the host-deferred charter row); `surface.md` NEW (NURBS surface ops the gate names: isoline families beyond the single `IsoCurve`, geodesic POLYLINES on parametric surfaces composing the landed `fields.md` `Geodesic` case + `geodesics.md` heat/MMP solvers through the UV pullback, normal-offset surfaces, curvature-domain sampling); `subdivide.md` NEW (Catmull-Clark/Loop as stencil rows over ONE refinement fold per `[GENERATOR_LAW]`; limit-surface evaluation; the OpenSubdiv admission carries the burden of proving limit-eval-at-scale demand); `develop.md` NEW (guaranteed-isometric developable-strip unroll — distinct from flatten's low-distortion conformal maps AND from segment.md's host LSCM unwrap capture — feeding Fabrication sheet/plywood/fabric); `panelize.md` NEW (cross-field-guided quad/diagrid panelization composing the landed `fields.md` `CrossField` case and `segment.md`'s Knöppel globally-optimal cross-fields + stripe patterns — one `int symmetry` argument owns the quad/diagrid/tri family as data — plus the remesh substrate `[V11]`; `sample.md`'s CCVT/blue-noise seeds panel distributions); `patternmap.md` NEW (the landed `geodesics.md` tangent log maps + 2D symmetry-group tiling -> surface-mapped instance stream — the Generation PATTERN/TILING plane's exact input — with each instance carrying a frame parallel-transported by the landed vector-heat transport; position without orientation is half an instance). Two tier-wide chain obligations bind every owner above: (a) PARAMETRIC CONTENT IDENTITY — `SpineRef` resolves geometry by content key (`RASM-GENERATION-SPEC.md:164`); the tier owns a canonical-bytes projection for parametric forms (degree · knots · weights · control net, canonicalized) riding the ONE reconciliation seam family — the digest composes the landed `Domain/identity.md` `ContentHash.Of` seed-zero entry (the one federation hasher, `ARCHITECTURE.md:76-81`) through `reconciliation.md`'s `Encode` family, kernel emits the digest, Persistence mints the key — and the projection is a COMPONENT of the parametric node's `ToCanonicalBytes(tolerance)` contribution the Element seam `ContentAddress.Of(Node)`/`OfGraph` folds (`Rasm.Persistence/.planning/Element/codec.md:151-152`), never a sibling SpineRef-resolution key beside it; (b) UV PULLBACK — every "composes the field planes" claim crosses the parametric↔discrete boundary twice, so `surface.md`'s sampler emits UV-provenanced tessellation (per-vertex `(u,v)` beside the `MeshSpace`) and field results pull back to the parametric domain by construction, `NurbsSurface.ClosestParameter` (`api-gshark.md:111`) the foreign-point fallback — a world-space polyline with no surface binding cannot feed `panelize.md` or `patternmap.md`. Fault home per `[V12]`; `curve.md`'s borrowed `ParameterizationFault` ends. The folder name ruled default is `Parametric/` beside the landed pages; a rename needs a judge-scored win and the inbound-reference sweep.

### [V3_VIEW_ENGINE]

`Drawing/view.md`'s visibility engine is rebuilt ground-up — the page cannot be polished because the advertised engine does not exist (`[VERDICT]` proof 1) and the index docs already promise it (`README.md:65`). The exact silhouette locus (`FacesOppose` exact `Orient3D`, `view.md:235-241`) and the Section delegation to `IntersectOp.PlaneMesh` (`view.md:349-353`) are the salvage. The rebuild is ONE of: a REAL BSP painter `Paint` actually consumes (back-to-front traversal driving the visibility solve), or exact analytic Appel quantitative-invisibility (invisibility count updated ONLY at silhouette crossings, computed through the crossing lattice — no sampling). The dead `BspNode`/`Partition`/`Split` apparatus becomes load-bearing or dies with the choice. `ToPolylines` chains by connectivity (successor-linked walk), never by kind-key concatenation. The tier boundary against the landed `Analysis/select.md` is stated on both pages: select's `Silhouette.Compute`/draft-curve capture is the host-native RhinoCommon tier, view's `DrawingProjection` the predicate-exact robust tier — coexisting per the capture law, one anchor each. The polygon-fill concern the purged LibTessDotNet claimed re-lands where the README already routes it: the arrangement `PlanarOverlay` messy-winding fill (`README.md:99`) — the `[V3]`/`[V13]` rebuild composes it or the DECISION re-scopes the README row per `[INDEX_DOC_OBLIGATIONS]`. Downstream in the same ruling: Fabrication's `Posting/Projection.cs` BSP HLR dies — its own ledger already routes `HiddenLineResult` to AppUi as "the BSP visibility solver superseding the AppUi painter sort" (`Rasm.Fabrication/ARCHITECTURE.md:47`); the kernel `DrawingProjection` supersedes BOTH, Fabrication consumes.

### [V4_CROSSING_COLLAPSE_LANDED]

The `intersect.md:388` collapse is executed, not re-declared: `arrangement.md` (`EdgeCrossings`/`SegmentOf`/`PlaneCrossPoint`/`InTriangle`, `:242-262`), `offset.md` (`SegmentsCross`, `:440-444`), and `repair.md` (`TriangleCrossPoint`/`EdgesCrossTriangle`, `:346-372`) re-route through `Intersection.Apply`/`Crossing` — no fourth crossing kernel. The arrangement rounding gap resolves in the same motion — ruled default: `Constraint` gains the implicit-point case carrying the `Lpi` crossing into `Tessellation.Build` so operand crossings ride the same exact path as internal Steiner points (the README already asserts it, `README.md:42` — land or re-scope BOTH per `[INDEX_DOC_OBLIGATIONS]`); the floor already exists — `predicates.md` fences the `OrientLPI`/`OrientTPI`/`InCircleLPI`/`InSphereTPI` family and `arrangement.md:18` already routes constructed Steiner crossings through it inside the substrate's own constraint recovery, so the implicit-point `Constraint` case is a carrier widening, not a new lattice — and the exact-sign/rounded-vertex honesty re-scope on both pages survives only on the implicit-point probe's INFEASIBLE verdict, ruled at DECISION time and cascaded across `[V4]`+`[V10]`c in one motion, never re-opened mid-leg. `SegmentSegment`/`LineLine`'s silent Z=0 projection (`intersect.md:293`) becomes a typed 2D restriction on the op case. The altitude boundary stays crisp both directions: `intersect.md:388`'s host-delegation sentence re-anchors from the deleted `Analysis/Intersect.cs` to the landed `Analysis/relations.md` (the Rhino-parametric lattice, `IntersectionHit`/`RayQuery` frozen names), which already states the coexistence from its side.

### [V5_BOOLEAN_OWNERSHIP]

`BooleanOp` re-homes from `repair.md:57` (Processing/Healing) to the boolean owner `arrangement.md` — the landed codemap already sites "`BooleanOp` cell welds" there (`ARCHITECTURE.md:42`) — killing the lower-stratum-consumes-upper-type shape plus the repair⇄arrangement page cycle (`arrangement.md:18` composes Healing; `repair.md:288-292` composes Arrangement). The duplicate `BooleanReceipt` name collision (`arrangement.md:115` vs `receipts.md:44` — same name, different shape and namespace) resolves to one type or two names. The tier-3 native gate is NAMED: Manifold (`elalish/manifold`, Apache-2.0, guaranteed-manifold boolean, C FFI `manifoldc`) is the categorical-best engine for the reserved scale path — admitted behind a finite `ScaleCeiling` (`ArrangementPolicy.ScaleCeiling: int.MaxValue` at `arrangement.md:61` is dead code today), bound via a thin in-house P/Invoke over `manifoldc` (`ManifoldNET` remains stalled at `1.0.7-alpha`, 2024-08 — pinning it requires recorded risk), gated behind the page's own golden-boolean fixture law. This ruling disposes the `[T-BOOLEAN-NATIVE-ASSET]` BLOCKED card, whose landed text already frames the shape (`HealOp.Boolean` composes `Arrangement.Apply(ArrangementKind.MeshBoolean, ...)`; `NativeAssetMissing` reserved for the tier-3 scale route). The managed exact arrangement stays the correctness owner; Manifold is the scale companion, never a second correctness rail — the codemap's "retires the tier-3 native CSG gate" re-scopes to that exact meaning in the same motion.

### [V6_MESH_SUBSTRATE]

`MeshEdit` — the de-facto shared mutable working set five settled pages consume (`repair.md:76-102`; intersect/arrangement/view/decimate via `using Rasm.Geometry.Healing`, receipts unqualified in `Of`'s signature) — extracts to the Meshing substrate plane as the robust-core MUTABLE-ARENA tier BESIDE the landed `mesh.md` (whose `MeshSpace` validated-snapshot law, `LaplacianCache`, `IntrinsicMesh`, one-cotangent ownership, and power diagram are settled — the conversion deliberately left `MeshEdit` with `repair.md`, so the extraction is this campaign's structural call): sited by the DECISION as a new robust-core `Meshing/` page under its ratified concept namespace, with repair's donor block excised surgically in wave 1 and repair's full `[V9]` rebuild staying wave 3. The two carriers state their seam: `MeshSpace` the immutable admission snapshot, `MeshEdit` the predicate-gated build arena — one page each, no third mesh carrier. The triangle-soup adapter collapses from three near-identical mesh-soup extractions (`Soup(MeshSpace)` at `intersect.md:326` and `view.md:386`; `Triangles(MeshSpace)`+`Soup(Triangle[])` at `arrangement.md:352-365`) onto the substrate owner. The `[SEAM_AND_RAIL_LAW]` store-mutability contract lands here for every robust-core SoA store in one motion, and the `CommunityToolkit.HighPerformance` admission question (`[ROSTER_RECONCILIATION]`) is ruled on the arena shapes this page settles.

### [V7_ONE_LM_ENGINE]

One damped-Gauss-Newton owner: `solver.md`'s `Iterate`/`Step` (`solver.md:303-346`) generalizes to a residual+Jacobian functor over its existing `SolvePolicy` λ-ladder (`solver.md:77-84` `LambdaUp`/`LambdaDown`/`StepFloor` — the policy record exists; the engine stays private and `Constraint`-coupled through `LinearizeDense`/`LinearizeNormal`, `solver.md:262,310`); `fit.md`'s `Refine` re-implementation dies — its `Distance`/`Gradient` per-primitive rows ARE the functor, exactly the reuse `fit.md:740` already CLAIMS — with λ threaded through the refine state on the policy ladder (the hardcoded `1e-3` seed/`×10.0` climb/`1e12` ceiling at `fit.md:575-600` and the no-carry-down accept branch die); `decimate.md` already composes the one solve rail (`CholeskyResult.SolveDetailed`, `decimate.md:18`) — the pattern the remaining two adopt. The dense-LA rule unifies on the LANDED owner: `Numerics/matrix.md` is the ONE provider boundary corpus-wide — MathNet dense receipts for small normal-equation systems, `CholeskySparse` for sparse — exactly the law the landed `register.md` already enforces ("ALL linear algebra through `matrix.md` owners, kills the raw-MathNet bypass"); the robust-core solve rails (`solver`/`fit`/`decimate`/`flatten`) adopt it, so `flatten.md:18`'s wrapper discipline and the direct-MathNet scatter stop being two laws for one concern. The 106-bit accumulation rows the README asserts (`README.md:86`) land here: the QEM quadric fold and the LM Σd² objective accumulate in `ddouble`, narrowing to `double` at readout.

### [V8_SPATIAL_CONTRACT]

`Spatial/index.md`: the `PointCloud` case lifts OUT of the `NodeStore`-centric union and ONTO the landed neighborhood substrate — `Spatial/neighbors.md` owns kNN/radius/graph point queries over Rhino `RTree` + `Supercluster.KDTree.Net` (the landed law: the kd-tree leaf is "additive to, not a replacement for" the BVH/octree primitive broad-phase, `README.md:93`) — killing the fourth parallel point-query rail; index keeps primitive broad-phase (BVH/octree/PLOC) and the throws (`index.md:130,574,579`) and the silent-empty breach (`index.md:358`) die on the `Fin` rail; bounds narrowing rounds OUTWARD (`Single` round-to-nearest at `index.md:79` shrinks internal-node bounds below the child union — the descent prune at `:453,474,518` can produce range/ray/overlap FALSE NEGATIVES; broad-phase min rounds down, max up); `ToAcceleration` returns `Fin<>` over the kernel-owned node-array wire the landed ledger already declares (`ARCHITECTURE.md:86`; the `using Rasm.Compute.Solver` at `index.md:27` dies; Compute decodes per its own `:125` residency row). The GWN double-owner reconciles across the merged corpus: index's Barnes-Hut tree-code vs the landed exact per-triangle winding in `reconstruct.md`'s mesh-SDF methods — ruled default: index owns the BVH-accelerated winding query; `reconstruct.md`'s GWN method and the `fields.md` `SignedDistanceFromMesh` case compose it through a declared seam — and that ruling IS the Fabrication distance-field ingress: `SignedDistanceFromMesh` through the index-accelerated winding is the ONE distance-field lane, the kernel mints no second SDF owner. Stale Cases counts (`index.md:14,707`) resync. `Spatial/naming.md`+`reconciliation.md`: `RebuiltEntity` gains an explicit `Self` index (fixing the `[VERDICT]` proof-4c mis-key), `Subsumes` fixes its subset direction for splits (`naming.md:62-66`), the Survive×Migrate injectivity residual (`naming.md:161`) resolves, `Encode` composes the landed `identity.md` `ContentHash.Of` per the ledger row, and both frozen fixtures re-prove per `[SEAM_AND_RAIL_LAW]`. The reconciliation→Persistence seam anchor unifies to one row (`reconciliation.md:3` / `ARCHITECTURE.md:79` / `Rasm.Persistence/ARCHITECTURE.md:55` today).

### [V9_PROCESSING_REPAIRS]

The confirmed-defect set lands as rebuilds, not patches. `flatten.md`: correct Dirichlet (large penalty on pinned rows only with rhs = penalty·value, or the eliminate-boundary-rows formulation); LSCM assembled SPARSE as the conformal-energy least-squares smallest-eigenpair through the `matrix.md` owners (the dense `(2n)²`+SVD dies); ARAP global assembly O(F) (pass the in-scope face index `f`; `IndexOf`'s O(F) scan with `return 0` on miss at `flatten.md:482-485` dies); `FactorNonZeros` reports the actual Cholesky factor fill, not `D1.NonZeros` (`flatten.md:330`); `ChartStore.Dead`/`FreeList`/`Kill` wired or deleted; the `DiscreteCalculus` sparse-shape assumption (`flatten.md:349`) re-verifies against the landed `spectral.md:191` fence. `decimate.md`: the vertex-vertex `Adjacency` the store already maintains (`decimate.md:87,328-333`) extends to the vertex→incident-face index `FanFaces`/`CollapsedFaces`/`NoAdmissibleCollapse` ride — killing the full-`edit.Faces` scans at `:293,316-360` (O(F log F) drain); boundary link condition (`shared == 1` admitted at the `decimate.md:310` gate) so open meshes decimate; the `ddouble` quadric accumulation per `README.md:86`. `repair.md`: `SelfIntersectResolve` becomes a real CDT re-mesh through the substrate; topology status threads forward (`before[n] = after[n-1]`, killing the 2N recompute at `repair.md:382-385`); the `((QueryResult.Hits)hits)` hard cast joins the typed rail. `receipts.md`: `Converged` for a boolean tests success, not the gate flag (`receipts.md:65,99`); the fence already composes the landed `intent.md:214` `Topology` projection for its Euler/Genus/Boundary triple (`receipts.md:13`) — the triple is today the ONLY `ProjectionRow` on the landed `TopologyReceipt.Project` (`mesh.md:196-206`), so a rebuild needing `IsManifold`/`NonManifoldEdges` lands one `ProjectionRow` extension through that page's Growth law, never a second topology rail. `fit.md`: `FitStore` (`fit.md:246-253`) wired or deleted; MLESAC consensus sampling routes through the landed `neighbors.md` kd-tree lane per `README.md:93`; the `Line` chart de-degenerates (Z-fixed anchor biases near-vertical lines; `minimalSamples: 3` at `fit.md:52` vs the 2-point `MinimalLine` at `:477` resyncs); its cloud-PCA vocabulary composes the landed `cloud.md` `CloudKernel`/`stats.md` `SampleMoment` owners — a consumer, never a second covariance owner.

### [V10_TOOLPATH_PLANE]

The Fabrication gate closes kernel-side where the concern is kernel-shaped, with Fabrication consuming: (a) **corner-strategy offset** — `JoinType`(Miter/Round/Bevel/Square)×`EndType` rows over ONE author-kernel offset assembly on the exact wavefront; the stratum question the prior brief left open is now landed law: the arc-bulge kerf lane is Fabrication's (`CavalierContours` — ISC, port fresh at 1.0.0 2026-06 — lives at the Fabrication stratum per `README.md:99`), the kernel emits the exact skeleton/medial + corner-row offset assembly + clearance radius through the seam, Fabrication composes CavalierContours for pure kerf compensation on top — one concern per stratum, never two manifests for one engine; offset ownership stays domain-partitioned three ways — region/polygon (this owner), parametric curve (`[V2]` `ParametricOp` case), surface normal-offset (`[V2]` surface.md); (b) **slice stack** — a NEW owner folding `IntersectOp.PlaneMesh` over a parallel-plane family into per-layer contour-nesting trees with uniform/adaptive layer-height policy rows (author-kernel; AGPL slicer cores rejected; the policy family OPEN as seed rows — Fabrication's production-additive lane demands variable-by-slope adaptive and support-interface rows, `RASM-CS-FABRICATION-BRIEF.md` `[V6]` the named consumer); layer contours land ORIENTED (outer CCW / holes CW — the convention the downstream arc-offset engine demands) and non-watertight sections emit typed OPEN-chain rows, never silent closure (`IntersectResult.Chains` is loops-only today — the fold widens the result); Fabrication's `Toolpath/slicing` re-routes per its own ledger promise (`Rasm.Fabrication/ARCHITECTURE.md:48`), and the Compute circulation runner is the SECOND named consumer (story-elevation floor-plate contours decoded through the wire, `RASM-CS-COMPUTE-BRIEF.md` `[V12]`a); (c) **medial made real** — ruled default: `Tessellation` exposes the Voronoi-dual accessor the medial trim reconciles against — the README already asserts delaunay owns the constrained Voronoi dual (`README.md:99`), so the accessor lands or BOTH the claim and `offset.md:16,478` re-scope per `[INDEX_DOC_OBLIGATIONS]`; the discarded build (`offset.md:325-326`) dies either way; the `WavefrontStore` hash-friendly-immutable prose (`offset.md:5`) dies under the `[SEAM_AND_RAIL_LAW]` store-mutability contract; (d) **3D medial/curve-skeleton** — author-kernel mean-curvature-flow skeletonization composing the LANDED machinery (`geodesics.md` implicit MCF + the `fields.md` `MeanCurvatureFlow` case; no permissive engine exists, CGAL GPL rejected), sited by the DECISION as the growth page of the offset/skeleton concern; across (c)+(d) the seam payload carries the per-point CLEARANCE-RADIUS function — Fabrication's consuming demand is exactly the "wavefront clearance-radius field" (`Rasm.Fabrication/ARCHITECTURE.md:22`), sharpened to a clearance query at ARBITRARY probe point plus weighted/variable-speed wavefront rows (`RASM-CS-FABRICATION-BRIEF.md` `[V5]`) — the radius payload is mandate, not negotiation — the kernel medial/skeleton carries the per-point radius and the arbitrary-probe clearance query as first-class result fields, so `Toolpath/Skeleton.cs` dies unconditionally and a radius-less kernel medial is a `[V10]` defect repaired kernel-side, never license for the downstream duplicate; (e) Fabrication's duplicate `Toolpath/Skeleton.cs` dies for `Offsetting.Apply` per `[PLACEMENT_LAW]`.

### [V11_HULL_AND_REMESH]

Hull reconciles as a two-tier seam, not a removal: the landed `cloud.md` hull rail is standing law (native `Mesh.CreateConvexHull3D` for Convex3D, `MIConvexHull`'s Delaunay complex for the realized concave/alpha kinds — `README.md:100`; MIConvexHull is consumer-backed and its removal is DEAD), and the lifted-paraboloid lower hull remains available as a `Tessellation` projection on the exact floor — the DECISION rules whether the exact-tier fold lands (a Bowyer-Watson byproduct serving predicate-gated construction: Fabrication stock/clearing envelopes, Generation bounding/containment demand it at robust grade) and declares the tier boundary on both pages, or records the cloud rail as the one hull owner with the exact fold a growth row. Remeshing gains its substrate with the tier boundary stated: Botsch-Kobbelt isotropic remesh (split/collapse/flip/tangential-relax, genus-preserving) + cross-field-guided quad remesh composing the landed `segment.md` cross-fields — the AUTHOR-KERNEL robust tier beside segment.md's landed HOST-capture tier (QuadRemesh/Reduce/`QuadTarget`/`RemeshKind`), coexisting per the capture law with one anchor each; `decimate.md`'s `VoxelRemesh` stays a distinct row. Deciding criterion for deferral: quad remesh may defer to a growth card ONLY if `panelize.md` proves its grid family lands on the isotropic substrate + segment.md guidance alone; the isotropic owner itself is not deferrable — panelization and Fabrication mesh-prep both gate on it, and Compute's volumetric discretization names it the boundary-conditioning pre-step (`RASM-CS-COMPUTE-BRIEF.md` `[V7]` recorded growth, decoded through the wire, never a Compute-side remesher).

### [V12_FAULT_TYPING_AND_BAND]

`faults.md` re-types its stringly majority — 10 of 17 cases carry only `string Detail` against the page's own "typed to its failure with its real payload" law (`faults.md:20`); the five rich cases prove the shape. The page gains the two-family seam statement the landed matrix already attributes to it (`ARCHITECTURE.md:112`): `GeometryFault` (band 2400, robust core) and the kernel-substrate `Rasm.Domain` `Expected`/`Fault` union are two families by explicit decision — `rails.md` states its side today, `faults.md` states its side at rebuild, neither absorbs the other. The band plan lands with the new planes: the geometry century 2400-2449 is hard-bounded (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md:144` pins the 2400 mirror; Material owns 2450), 12 clusters occupy 2400-2447, headroom is 2448-2449 (`faults.md:19`), and `[V2]`/`[V10]`/`[V11]` mint new concerns needing homes (parametric today borrows `ParameterizationFault`). The DECISION emits the plan: concern-adjacent new owners share existing clusters (subdivision/develop/panelize under a widened `parameterization`-family stride, slicing under `intersection`-family), the last stride 2448-2449 is spent deliberately, the `topology` cluster renames with `[V1]`, and a federation re-plan escalates ONLY if the ruled cluster map cannot fit — never a silent 13th-cluster squeeze.

### [V13_PACK_RESIDENCY]

`Drawing/pack.md`: the phantom `Topology` import dies (`[V1]` — collision-proven against the landed `Rasm.Domain.Topology`); the dtype residency claim becomes real or dies — `EncodedStore.Payload` is always `float[]` (`pack.md:126-129`) and `ChannelDtype.Pack` quantizes-then-widens back to `float` (`pack.md:53`), so "a `Half` halves residency" (`pack.md:13`) is false today; ruled default: the store becomes dtype-strided byte storage with per-channel descriptors — the `System.Numerics.Tensors` seam at `pack.md:465` survives as descriptor-dispatched typed views (`ReadOnlyTensorSpan<float>`/`ReadOnlyTensorSpan<Half>` selected on the descriptor's `Dtype` row; the tensor span is element-type-generic, so the strided store is a dispatch row, not a seam defeat; the `[TELOS]` residency lane binds) — and the Dtype column re-scopes to round-trip-error-only honesty only if drafting proves the typed-view dispatch unviable on the wire. Two seam compositions are VERIFIED LANDED, not duties: pack already composes the public `ScalarField.SampleDetailed` rail (`pack.md:346,463` onto `fields.md:367`) and already attributes `VectorCloudMetric.OrientedNormals` to the landed cloud→neighbors owners (`pack.md:5,463`; `cloud.md:176` delegates to `neighbors.md` `OrientNormals`) — the rebuild preserves both compositions verbatim. Consumer pressure lands the two-row modality growth the page's own Growth law anticipates (`pack.md:19`): `PackKind` gains `field` · `toolpath` case rows with their active-channel sets — `Rasm.Compute` decodes them as the residency wire's case axis (`Rasm.Compute/ARCHITECTURE.md:126`) and `Rasm.AppHost` signature-locks its `EncodingKind.Field`/`.Toolpath` spellings on them (`Rasm.AppHost/.planning/Sandbox/solver.md:54-55,197`) — two data rows per `[GENERATOR_LAW]`, never a downstream encoding owner; the landed ledger rows `ARCHITECTURE.md:92-93` (pack→AppHost, pack→Compute residency view) are the declared seams the fence composes.

### [V14_LEDGER_AND_DOCS]

The seam ledger becomes enforceable law, both directions. The conversion landed the ledger spine (`ARCHITECTURE.md:73-99` — pack→AppHost/Compute, flatten→Fabrication/AppUi, view→Fabrication/AppUi, index⇄Compute as a wire, the identity/reconciliation content-key family, the dormant host edges): the campaign's duty is FENCE-CONFORMANCE — every declared row composed in its owning fence (`index.md` returns the declared node-array wire, `reconciliation.md` composes the declared identity entry, pack composes its two rows) — plus the residual ledger edits: the Parametric consumer fan enters `[02]-[SEAMS]` when the `[V2]` tier lands (curve.md's fan prose at `curve.md:~190` becomes rows); the mis-stated counterpart edges die (Compute `:120`'s spurious "Geometry solver consumes Compute LM" disproven by `solver.md:303-346`; the index⇄Compute direction resolves against Compute `:121`); anchor drift fixes to one anchor per seam (`[V8]`'s reconciliation triple; intersect→Fabrication's `Posting` vs `Toolpath/slicing` split resolves with `[V10]`). `README.md` router rows and `ARCHITECTURE.md` codemap rows re-scope wherever a verdict re-scopes a fence (`[INDEX_DOC_OBLIGATIONS]`). The two false rejection rationales correct (`delaunay.md:3` "Triangle/TetGen are GPL" -> the honest split: TetGen is AGPL and stays rejected on the license gate, Triangle's encumbrance is the Shewchuk non-commercial license — Triangle.NET is MIT — and the load-bearing rejection is float-epsilon arithmetic + no exact CDT/CDTet; `predicates.md:3` "no library carries a robustness guarantee" -> no maintained, NuGet-published, indirect-complete predicate library exists); `predicates.md` gains the strict-IEEE-754/RID invariant on `NumericsPolicy` that makes `TwoSum` safe (x87 extended precision defeats Shewchuk EFTs; the target RIDs are strict) plus the FMA-free `TwoProduct` fallback row (Dekker-split EFT product where the target lacks hardware FMA), and evaluates the `PeterO.Numbers` directed-rounding interval filter the README already names (`README.md:86`). Consumer pressure on the same hardening: `Rasm.Compute` (`RASM-CS-COMPUTE-BRIEF.md` `[V7]` — the CDTet in-sphere/orientation sign-decision floor) composes the exact predicate family cross-package through a declared seam row, so the hardened surface designs a package-boundary-consumable `Orient3D`/`InSphere` member family (adaptive-filtered entry, fenced as public surface — realization proves it compiled) — a kernel-internal-only predicate floor forces that consumer onto a vendored Shewchuk duplicate, the exact re-own `libs/.planning/architecture.md:54` forbids.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — Phase 1 re-verifies every row against disk; conversion-era drift of a few lines on the touched settled pages (`decimate`, `fit`, `solver`, `curve`) is corrected, never treated as refutation. Anchors are `libs/csharp/Rasm/.planning/`-relative; `ARCH:`/`README:` anchor the rebuilt kernel index docs; `FAB:`/`COMPUTE:` anchor those packages' `ARCHITECTURE.md`; `PROPS:`/`CSPROJ:` anchor the manifests.

| # | Class | Evidence |
|---|---|---|
| E1 | Dead HLR engine | `view.md:302` `Paint` never reads `bsp`; `:263-300` Partition/Split built-discarded; `:320-347` uniform `SampleStep` sampling not Appel QI; `:3,16` the claimed engine; `:130-135` kind-key polyline concat; `README:65` advertises the BSP kernel |
| E2 | Gate-plane absence | `RASM-GENERATION-SPEC.md:179,226` gate roster; zero subdivision/develop/panelize/pattern-map owners corpus-wide (grep; segment.md host unwrap is capture-tier only); `curve.md:116-140` stub bodies, `:19` Offset demoted; landed enabling primitives real (`Spatial/fields.md` Geodesic/CrossField/VectorHeat/TangentLogMap cases, `Processing/{extract,flow,geodesics,segment}.md`); `intersect.md` single-plane only; `offset.md` miter-only |
| E3 | Settled fences describe the retired system | `delaunay.md:3` false "Triangle GPL" (package purged; rationale survives); `curve.md:170,190` + `intersect.md:388` cite deleted `Analysis/Intersect.cs` (capability now `Analysis/relations.md`); `index.md:27` upward using; `pack.md:33` import + `pack.md:18` packages row + `solver.md:13` owner prose — the phantom namespace's three live sites colliding with the landed `Rasm.Domain.Topology` (`Domain/normalization.md`); landed truth: 12-reference `CSPROJ`, float-lane law `README:99` |
| E4 | Confirmed math bugs | `flatten.md:359,364` inverted Dirichlet; `:377,173` dense LSCM vs `:3` sparse prose; `naming.md:113`+`reconciliation.md:55-62` vertex mis-key; `receipts.md:65,99` Converged inversion; `decimate.md:310` boundary freeze, `:293,316-360` full-face scans unfed by the `:87` adjacency — O(F²) |
| E5 | Unratified namespaces + dual paradigm | `ARCH:105-110` frozen matrix with `Rasm.Geometry.*` placeholder row + campaign-owned reconciliation; ~14 fence declarations, 4 pages declare none; the phantom's three live sites (`pack.md:33,18`, `solver.md:13`); `FAB:44` cross-package repair/`BooleanOp` anchor (`csharp:Rasm/Processing/repair`); `rails.md:14,234-238` threading law + validity fold + `Domain/validation.md` admission vocabulary vs settled bare-`Fin`/hand-rolled-IsValid fences |
| E6 | Strata violations | `index.md:27` `using Rasm.Compute.Solver`, `:572-579` returns Compute's type — vs `libs/.planning/architecture.md:35` and the landed wire row `ARCH:86`; `COMPUTE:120` spurious reverse seam disproven by `solver.md:303-346` |
| E7 | Unrealized collapses | `intersect.md:388` owner-declared vs live copies `arrangement.md:242-262`, `offset.md:440-444`, `repair.md:346-372`; `offset.md:16,478` medial dual claim vs `:325-326` discarded build; `fit.md:740` "reuses the solver λ-ladder" vs `:575-600` re-implementation vs `solver.md:303-346`; `arrangement.md:467` implicit framing vs `:258-262` rounded `PlaneCrossPoint` |
| E8 | Duplicate owners cross-strata | `FAB:33` Posting BSP HLR vs `view.md`; `FAB:22` Toolpath skeleton vs `offset.md`; `FAB:23` in-folder `Slicing.cs` planar section vs `intersect.md` `PlaneMesh` (`FAB:48` promises the re-route) |
| E9 | Duplicate mechanisms in-kernel | LM ×2 (`solver.md:303-346` policy-laddered vs `fit.md:575-600` hardcoded; decimate already composes `CholeskyResult.SolveDetailed`, `decimate.md:18`); GWN ×2 (`index.md:383-444` Barnes-Hut vs landed `reconstruct.md` exact per-triangle); `BooleanReceipt` name ×2 (`arrangement.md:115` vs `receipts.md:44`); soup adapter ×3 (`intersect.md:326`/`view.md:386`/`arrangement.md:352-365`); dense-LA two rules (`flatten.md:18` wrappers vs direct MathNet elsewhere vs the landed `matrix.md` owner law) |
| E10 | Rail breaches | `index.md:130,574,579` throws; `:358` silent empty vs `:351` prose; `:79` round-to-nearest bounds narrowing -> false-negative prune at `:453,474,518`; `repair.md:243` hard cast |
| E11 | Dead/misleading carriers | `BspNode` apparatus (E1); `fit.md:246-253` `FitStore` wiring unproven; `flatten.md:81` `ChartStore.Dead/FreeList/Kill` unused, `:330` wrong-operator `FactorNonZeros`; `pack.md:13` false Half-residency over `:53` widen-back + `:126-129` `float[]`; `index.md:14,707` two-case counts vs the fenced third `PointCloud` arm (`:130,358,572-579`); `offset.md:5` `WavefrontStore` immutable-prose falsehood vs `:13` mutating store; `offset.md:195` `WavefrontStore` 2n under-allocation |
| E12 | Seam-ledger residue | landed spine `ARCH:73-99` vs fences not yet composing declared rows (index wire, reconciliation identity entry, pack rows); reconciliation triple anchor (`reconciliation.md:3` / `ARCH:79` / `Rasm.Persistence/ARCHITECTURE.md:55`); `COMPUTE:120,121` stale counterpart rows; Parametric consumer fan prose-not-rows (`curve.md:~190`) |
| E13 | Catalog debt | shared `libs/csharp/.api/api-hashing.md:82` `XxHash3`/`ulong` falsehood vs kernel-overlay truth; `api-csparse.md`/`api-tensors.md` byte-identical cross-tier dupes; `api-mathnet-numerics.md`/`api-mathnet-providers.md` deliberate kernel overlays (verify subset, not fork); NO LanguageExt catalog in either tier |
| E14 | Version/platform facts (feed-verified) | `PROPS:62-64` MathNet `6.0.0-beta2` newest release, no stable 6.0; `PROPS:29` LanguageExt `5.0.0-beta-77` newest, actively published 2025-12; GShark `2.3.1` dormant since 2023-08; `ManifoldNET` stalled `1.0.7-alpha` 2024-08; CavalierContours `1.0.0` 2026-06 active (Fabrication-lane pin); `geometry4Sharp` 1.0.0 ON NuGet since 2022-11 (WATCH-card trigger stale); `CSparse.Extensions` unpublished; TVGL 2019; `Kemsekov.GraphSharp` 3.1.2 2024-02; MIConvexHull `1.1.19.1019` 2019 (consumer-backed); QuikGraph `2.5.0` 2022 (admitted, bounded lane); `Supercluster.KDTree.Net` `1.0.22` 2025-08 |
| E15 | Stringly faults + stub fences | `faults.md:20` typed-payload law vs 10 of 17 cases carrying bare `string Detail`; `delaunay.md:192-198` seven signature-only `[STORE_OPS]` bodies (`Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces`) — the `[03]` delaunay transcription target |
| E16 | Index-doc obligations | `README:42`+`ARCH:42` arrangement implicit-point/BooleanOp/native-gate claims; `README:99` delaunay Voronoi dual + PlanarOverlay fill; `README:65`+`ARCH:63` view BSP kernel; `README:86` ddouble QEM/LM accumulation + PeterO interval filter; `README:93` kd-tree feeds fit/register; `ARCH:112` faults.md seam statement absent from `faults.md` |

## [03]-[CAPABILITY_ESCALATION]

Target grades are the Phase-2 acceptance bar; the DECISION carries the per-page skeletons that reach them.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| Numerics/predicates | 9 | 9.5 | RID/strict-IEEE invariant; precise precursor rationale; FMA-free fallback row; PeterO directed-rounding interval filter (`README:86`); package-boundary-consumable `Orient3D`/`InSphere` export for the Compute discretization consumer |
| Numerics/faults | 7.5 | 9.5 | ten stringly cases re-typed; two-family seam statement landed; band plan with new-plane homes; cluster taxonomy = ratified partition |
| Parametric tier | 3 | 9.5 | full-body curve (stubs die, retired-source refs re-anchor); arc-length station/RMF-frame family on the host-neutral tier beside landed `locate.md`/`projections.md`; surface ops (isolines/geodesic paths/offset) with UV pullback; subdivision; developable/unroll; panelization composing landed cross-fields; pattern-to-surface with vector-heat instance frames; parametric canonical-bytes identity through landed `identity.md`; SSI disposition; offset promoted |
| Spatial/index | 7 | 9.5 | PointCloud lifted onto landed `neighbors.md`; zero throws; outward-rounded bounds; kernel-owned acceleration wire composed (ledger row exists); GWN single-owner with `reconstruct.md`/`fields.md`; counts resynced |
| Spatial/naming+reconciliation | 6.5 | 9.5 | Self index; corrected `Subsumes`; namespaces declared; injectivity resolved; fixtures harness-proven; one Persistence anchor; `Encode` composes landed `ContentHash.Of`; parametric canonical-bytes projection on the one `Encode` seam |
| Meshing/delaunay | 6 | 9.5 | the seven stub bodies fenced (the hardest transcription in the tree); hull projection ruling; Voronoi-dual accessor (`README:99` obligation); honest rejection rationale |
| Meshing/intersect | 8.5 | 9.5 | collapse landed by consumers; typed 2D restriction; slice-stack producer seam; host-delegation re-anchored to `relations.md` |
| Meshing/arrangement | 7.5 | 9.5 | implicit-point `Constraint` or honest re-scope (README row follows); `BooleanOp` homed; Manifold tier-3 named + finite ceiling; repair cycle broken |
| Meshing/offset+toolpath | 5 | 9.5 | medial real or re-scoped; clearance-radius seam payload; JoinType×EndType corner rows author-kernel (CavalierContours = Fabrication lane); 3D MCF skeleton composing landed `geodesics.md`; store sizing; prose resync |
| Meshing substrate (new) | 0 | 9.5 | `MeshEdit` arena page beside landed `mesh.md` with the two-carrier seam; soup adapter ×3 collapsed; store-mutability + arena-concurrency contract landed |
| Processing/solver+fit | 8 | 9.5 | one generalized LM (functor+policy); fit instantiates; λ ladder threaded; `FitStore` resolved; Line chart fixed; dense-LA law = landed `matrix.md` owners; ddouble Σd² accumulation; MLESAC via landed `neighbors.md` |
| Processing/repair+receipts | 7 | 9.5 | real CDT self-intersect re-mesh; `Converged` fixed; MeshEdit extracted; topology threaded forward; `BooleanReceipt` collision resolved; `receipts.md:13` triple projection kept on landed `intent.md:214`, manifold fields via one `TopologyReceipt.Project` row (`mesh.md:196-206`) |
| Processing/decimate | 7 | 9.5 | maintained face incidence (O(F log F)); boundary collapse; ddouble quadric fold; meshoptimizer/Compute split recorded |
| Processing/flatten | 6 | 9.5 | correct Dirichlet; sparse LSCM eigenpair via `matrix.md`; O(F) ARAP; true factor fill; dead carriers resolved; developable seam to the parametric tier; `spectral.md:191` shape verified |
| Drawing/view | 4 | 9.5 | a real visibility engine (consumed BSP or analytic QI); connectivity chaining; select.md silhouette tier boundary stated; Fabrication consumes `DrawingProjection` |
| Drawing/pack | 7 | 9.5 | real dtype-strided residency or honest re-scope; phantom import dead; the landed `SampleDetailed` (`pack.md:346`) and `OrientedNormals` compositions preserved verbatim; `field`/`toolpath` `PackKind` rows landed for the Compute residency + AppHost solver consumers |
| Slice/skeleton/hull/remesh (new) | 0 | 9.5 | slice-stack owner with oriented contours + typed open sections; MCF curve-skeleton over landed MCF machinery; hull tier ruling (exact `Tessellation` projection vs landed cloud rail); isotropic (+quad) remesh substrate beside the landed host capture tier |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every kernel fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` as the ADT backbone (stays law), LanguageExt `Fin`/`Error` rails at every entry (catalog authoring per `[ROSTER_RECONCILIATION]`), `System.IO.Hashing` `XxHash128` seed-zero through the landed `Domain/identity.md` entry ONLY (never a second call site), `CommunityToolkit.HighPerformance` `Span2D`/`MemoryOwner<T>`/`ParallelHelper` (`api-highperformance.md:26,35,54`) into the SoA build arenas and parallel BVH/QEM/MLESAC folds once the `[V6]` admission rules it in, `System.Numerics.Tensors` `TensorPrimitives` for elementwise reduces on the non-exact float/double paths only (never the exact-predicate path); the span-feed law stands at `api-tensors.md:248` — the kernel's SoA coordinate buffers feed operators DIRECTLY, a tensor-shaped re-pack is the rejected double-layout.
- `GShark` — mined to the instance surface (`NurbsBase`/`NurbsSurface`/`Fitting.Curve`/`Intersection.Intersect`, all `.api`-verified); the `[V2]` tier widens consumption (the station/frame spine `Divide`/`ParameterAtLength`/`PointAtLength`/`PerpendicularFrames` at `api-gshark.md:81-88`, `NurbsSurface.ClosestParameter` for UV pullback, `DivideByChordLength`, `DecomposeIntoBeziers`, `Offset`, the `From*` factory family). Upstream is DORMANT — last release AND push 2023-08 (feed re-verified) — and `[V2]` makes it the load-bearing engine of the campaign-defining tier, so the hinge is ruled AT DECISION TIME from the `[01]` envelope probe — floor-as-is, per-gap owned kernel ops, or vendor-and-own NOW — never discovered mid-leg: MIT permits vendor-and-own of the composed algorithm set in-kernel, and never a second NURBS engine beside it (the managed field is bare — `NurbsSharp` is a toy).
- `Supercluster.KDTree.Net` (1.0.22, 2025-08 active) — the landed `neighbors.md` mining owner; namespace `SuperClusterKDTree`, squared-L2 `DistanceMetrics`; the `[V8]` PointCloud lift RIDES this lane rather than re-owning it.
- `TYoshimura.DoubleDouble` — the 106-bit predicate tier stays; the QEM/LM accumulation rows land per `README:86`/`[V7]`/`[V9]`; the ~200-member special-function surface remains a recorded no-consumer reserve.
- `ExtendedNumerics.BigRational` — `Fraction` oracle + exact ordering keys stay; `Mediant` Stern-Brocot remains a recorded no-consumer reserve.
- `PeterO.Numbers` — the differential-oracle sources stay; the directed-rounding `Floor`/`Ceiling` interval filter (`api-peteronumbers.md:153`) is the one genuinely unmined ladder enhancement — `[V14]` evaluates it (the README already names it).
- `CSparse` — reached through the landed `matrix.md` `CholeskySparse` owner only (the `[V7]` rule); `Refactorize`/`Update`/`Downdate` amortization noted for the flatten factor cache; LGPL-2.1 weak-copyleft recorded at the license gate.
- `MathNet.Numerics` — the `[ROSTER_RECONCILIATION]` beta ruling with the WIDENED survival sweep (landed kernel fences included); robust-core consumption narrows to the landed `matrix.md` owner boundary; the OpenBLAS keep verifies its arm64 asset or re-opens.
- `MIConvexHull` (1.1.19.1019) — consumer-backed by the landed `cloud.md` hull rail (`README:100`); the `[V11]` ruling declares the exact-tier boundary; removal is dead.
- `QuikGraph` (2.5.0) — ADMITTED kernel-side with one bounded lane: the landed `neighbors.md` Prim-MST normal orientation (`README:121`); the law generalizes — kernel graph RESULTS (skeleton/medial topology `[V10]`d, slice nesting forests `[V10]`b, naming migration structure) emit as kernel-owned SoA wires, QuikGraph serves in-kernel combinatorial computation only where the landed lane pattern holds (never the spectral path — segment's normalized cut rides `matrix.md`), and the shared-tier catalog's downstream-view claims (`api-quikgraph.md:11`) re-verify against the landed admission.
- Manifold (ADD) — Apache-2.0, upstream active; the `[V5]` tier-3 engine; thin in-house P/Invoke over `manifoldc` (RID assets recorded at admission; `ManifoldNET` feed-verified stalled at `1.0.7-alpha`, 2024-08); `.api` catalog authored with the admission; golden-boolean fixture gates activation.
- Fabrication-lane records (landed law, `README:99` — the `CavalierContours`/`Clipper2`/`SharpVoronoiLib` pins live in the props float-lane group at `PROPS:70-76` with zero kernel references; `LibTessDotNet` is retired outright — no pin, no catalog in any tier): `CavalierContours` (ISC, 1.0.0 2026-06 — the `[V10]`a kerf/arc lane at the Fabrication stratum; its central pin re-groups to the Fabrication label), `Clipper2` (BSL-1.0, 2.0.0 2025-12 — Fabrication NFP/silhouette lane; Compute circulation the second downstream consumer), `SharpVoronoiLib` (superseded kernel-side by the exact CDT/arrangement owners; Fabrication decides its fate in `RASM-CS-FABRICATION-BRIEF.md` `[04]`); the kernel-tier catalogs are already deleted — the pin re-grouping is the campaign's wave-1 manifest motion, and the standing `Rasm.Fabrication/.api` copies (`api-cavaliercontours`/`api-clipper2`/`api-sharpvoronoilib`) serve as the folder-tier catalogs.
- Catalog hygiene: the `[ROSTER_RECONCILIATION]` api-hashing correction + csparse/tensors dedupe + overlay verification + LanguageExt authoring; `api-rhino.md:134`'s claim that repair composes the Rhino heal surface corrects — repair composes only `RebuildNormals` at the working-set boundary (`repair.md:89`) and author-kernels welding/hole-filling; `api-mathnet-providers` (kernel overlay) keeps the osx-arm64 managed-fallback + OpenBLAS-only headline.
- Judged shared-tier stubs, NOT mined kernel-side (recorded so silence is never mistaken for oversight): `Generator.Equals` — Thinktecture owns kernel value identity; the structured-diff surface is the seam's. `Riok.Mapperly` — kernel wires are columnar span transcription, never object mapping. `UnitsNet` — kernel angles stay raw radian `double`; `Angle` is the AppUi↔kernel boundary bridge. The transport/app set (grpc ×7, protobuf, nodatime ×3, messagepack + thinktecture adapters, hybrid-cache, jsonpatch, redaction, system-configuration, extensions-ai, unicolour) carries no geometry-plane member — zero mine obligation.
- Stratum-split records (never mistaken for kernel candidates or dead weight): `Alimer.Bindings.MeshOptimizer` is Compute's; PicoGK (Apache-2.0, active) is Fabrication's voxel/implicit lane — never the kernel's second SDF owner (the `[V8]` one-distance-field-lane ruling) and never its slicer; OcctNet.Wrapper is Fabrication's own pin — the OCCT rejection below is KERNEL-scoped.
- Named-and-rejected (never re-proposed): SISL, TetGen, CGAL, SolveSpace, CuraEngine/Slic3r, Instant Meshes (license NOASSERTION AND dormant since 2022)/NeurCross, MCUT, OCCT/LNLib (LGPL, host-overlap — the kernel meets Rhino at the wire); MeshLib (the most capable maintained managed mesh-processing binding — REJECTED on the license gate: custom free-non-commercial/paid-commercial); rhino3dm (MIT, active — representation/IO only; GShark stays the host-neutral ALGORITHM owner); SkeletonNet/CGAL_StraightSkeleton_Dotnet (unmaintained float port / GPL wrapper — dominated by the author-kernel exact wavefront); OpenSubdiv admissible ONLY on proven limit-surface-at-scale demand, and no managed binding exists — admission means an in-house P/Invoke, the Manifold class of burden; V-HACD/CoACD (BSD/MIT convex decomposition) recorded as an admissible future arrangement-adjacent owner, deferred until a consumer names it.
- TASKLOG/IDEAS absorption: `[T-BOOLEAN-NATIVE-ASSET]-[BLOCKED]` resolves into `[V5]` (the DECISION disposes the card); `[HOST_BOUNDARY_REENTRY]-[BLOCKED]` is realization-phase law — OUT of this campaign's scope, never disposed here; `IDEAS.md`'s `[WATCH_PACKAGES]` card re-keys per `[ROSTER_RECONCILIATION]`, and the DECISION's growth rows seed the geometry idea pool fresh.

## [05]-[BUILD_LEGS]

Phase 2 runs the durable rebuild engine per leg with `RASM-CS-GEOMETRY-DECISION.md` as brief. The DECISION owns the exact partition, proven against the post-campaign seam graph (arrow = composes; every edge lands in an equal-or-earlier wave, and an intra-wave edge points backward in the leg's listed page order — the lists below ARE dependency order). The LANDED kernel pages are ambient floors in every wave, never leg targets: `matrix.md`/`neighbors.md`/`mesh.md`/`dec.md`/`reconstruct.md`/`fields.md`/`geodesics.md`/`segment.md`/`identity.md`/`stats.md`/`cloud.md` are composed freely; a leg touching a landed page does so only through a `[LANDED_KERNEL_LAW]` (b) inbound-reference ripple or a DECISION-declared seam extension. Robust-core edges: predicates+faults ← every owner; substrate `[V6]` ← intersect/arrangement/view/decimate/receipts/repair/remesh/slice/skeleton; index ← arrangement (broad-phase); naming+reconciliation ← pack (`Encode`); delaunay ← arrangement (`Tessellation.Build`), offset (dual, `[V10]`c), repair (CDT re-mesh, `[V9]`); intersect ← arrangement/offset/repair (`[V4]`), slice (`[V10]`b), view (Section, `[V3]`); arrangement ← repair (booleans — never the reverse post-`[V5]`/`[V6]`); solver ← fit/decimate (`[V7]`); flatten ← develop (`[V2]`); remesh ← panelize (`[V2]`/`[V11]`). The sequencing law it must honor:

1. **FLOOR + CONTRACTS** — Numerics (predicates hardening, faults re-type + two-family seam + band plan), the `[V1]` structure ratification landing corpus-wide (namespaces, codemap, cluster taxonomy, phantom-import kill), Spatial (`[V8]` contract fixes, the wire composed, naming/reconciliation bug fixes + fixture re-proof + identity composition), the `[V6]` mesh substrate extraction with the store-mutability contract. The residual roster motion (MathNet ruling + sweep, OpenBLAS verification, pin re-grouping, LanguageExt + Manifold catalogs, hashing correction, dedupe, WATCH re-key) lands HERE — it gates every later page's imports and catalog claims.
2. **MESHING LATTICE** — delaunay (stub bodies, hull ruling, dual accessor), intersect (consumer collapse landed, host-delegation re-anchored), arrangement (`BooleanOp` home, implicit constraint ruling, Manifold gate, cycle break), offset (`[V10]` a+c — corner rows, medial — plus store sizing; slice is `[V10]`b, wave 4).
3. **PROCESSING** — solver+fit (`[V7]` one LM over `matrix.md`), repair+receipts (`[V9]` fixes over the wave-2 lattice), decimate, flatten.
4. **NEW MESH OWNERS + PARAMETRIC TIER + DRAWING** — first the `[V10]`-b/d + `[V11]` slice/skeleton/remesh owners (over wave-2 intersect/offset, the wave-1 substrate, and the landed MCF/cross-field machinery), then the `[V2]` surface-development pages (panelize composes remesh + landed segment guidance; develop composes wave-3 flatten; all compose the landed field/geodesic planes), with view (`[V3]` rebuild over wave-2 intersect + wave-1 index) and pack (`[V13]` over wave-1 reconciliation) independent of the parametric tier and parallelizable beside it.

Per-leg closeout: hard residuals resolve before the next leg launches; `ARCHITECTURE.md`/`README.md`/`TASKLOG.md`/`IDEAS.md` close in the leg that changes their facts (`[INDEX_DOC_OBLIGATIONS]` rows explicitly); `.api` edits land in wave 1 with the roster motion, and the leg that first composes an admitted package deepens its stub; cross-package counterpart ledger edits (`FAB:22,23,33,44,46-48`, `COMPUTE:120,121`, `Rasm.Persistence/ARCHITECTURE.md:55`) land as 1-hop ripples with the leg that changes the seam (waves 1, 2, and 4). Residuals naming a later leg's pages transfer forward explicitly in the leg return, never silently. Campaign acceptance closes on the consumers, not the ledger: after wave 4, (a) a Generation SpineRef dry-run — the spine RESOLVED by content key through the `[V2]` parametric-identity seam (landed `ContentHash.Of` + `Encode`), arc-length stations carrying rotation-minimizing frames over the `[T0,T1]` window, geodesic + isoline families on a surface pulled back to UV, one subdivision pass, one panelized region, one pattern-mapped instance stream with per-instance frames — composes entirely from rebuilt + landed fences; (b) a Fabrication toolpath dry-run — corner-strategy offset family, a slice stack with oriented contours + nesting tree, a medial-axis clearance-RADIUS read, a distance-field read through the `[V8]` lane (`fields.md` `SignedDistanceFromMesh` → `reconstruct.md` GWN → index BVH), a developable-strip unroll with its isometry witness (`[V2]` develop), and a stock-envelope hull read through the `[V11]` tier ruling — composes from the kernel seams with zero Fabrication-side geometry re-implementation; (c) a documentation dry-run — plan + section HLR over a healed model emitting connectivity-chained visible/hidden/silhouette/section polylines with zero sampling misses (the E1 failure class) — composes view+intersect+index, feeding the `FAB:47` `HiddenLineResult` route; and (d) a reality-capture dry-run — cloud admission → MST-oriented normals (landed `cloud.md`→`neighbors.md`) → MLESAC plane/cylinder fits through the `[V9]` kd-tree lane → predicate-gated heal + boundary-capable decimate at manufacturing tolerance → a flatten chart atlas under the corrected Dirichlet pin → the naming re-anchor over the harness-proven fixtures — composes the `[V8]`/`[V9]` repairs end to end. A dry-run that reaches for a missing owner reopens `[V2]`/`[V3]`/`[V8]`/`[V9]`/`[V10]`/`[V11]` as residuals.

## [06]-[OUT_OF_SCOPE]

- The 34 landed kernel pages' charters (`[LANDED_KERNEL_LAW]`): composed as standing law; extended only through DECISION-declared seams and their own Growth rows; their landed rulings (validity fold, Op-threading, projection dispatch, one-cotangent, SHM spine, policy-record collapses, the D1-D9 drop/absorb register) are never re-litigated.
- The frozen contract namespaces (`Rasm.Domain`/`Rasm.Vectors`/`Rasm.Analysis`) and frozen names (`ARCHITECTURE.md:105-110`; the blueprint frozen-name law): never re-decided; `[V1]` ratifies the robust-core `Rasm.Geometry.*` tier only.
- Fabrication interiors: kinematics, G-code posting, nesting/cutting-stock, CAM material-removal simulation, adaptive-clearing STRATEGY (the kernel supplies medial/offset/slice substrate; pocketing strategy is Fabrication's); the CavalierContours/Clipper2 kerf/NFP lanes at the Fabrication stratum; its duplicate-owner deletions are 1-hop ledger ripples only.
- Rhino host runtime: the frozen `Rasm.Rhino`/`Rasm.Grasshopper` source and the `[HOST_BOUNDARY_REENTRY]` card are realization-phase law; the kernel's RhinoCommon-aware compile surface and the two doc-facing adapters (`Context.Of(RhinoDoc)`, `Analyze.From(RhinoDoc)`) stand per the conversion's Rhino ruling — no host-free assembly is minted, no Make2D/host-UI concern enters the kernel.
- Compute interiors: meshlet/vertex-cache/encode, GPU clash traversal, the LM/tensor rails; Compute consumes the kernel wire.
- IFC semantics (`Rasm.Bim`), Element/Persistence interiors — seam counterparts only, one anchor each.
- `Rasm.Generation` surfaces: the spec composes the kernel; nothing here reaches up; form-finding/dynamic-relaxation solvers are flagged for the Generation campaign, not built here.
- The federation-wide fault-band renumber beyond the geometry century — escalation path only if `[V12]`'s ruled cluster map cannot fit 2400-2449.
