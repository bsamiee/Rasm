# [RASM_BIM_TASKLOG]

Open and closed work distilled from `IDEAS.md`. `[01]-[OPEN]` carries task cards with `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` leaders; `[02]-[CLOSED]` carries `[COMPLETE]` or `[DROPPED]` cards. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[T-BSDD-TRANSPORT]-[QUEUED]: ground the `BsddResolution` dictionary-evidence path — the `LocalShape` degrade builds now, the live `BsddPort.Fetch` binds the Compute transport at the composition edge.
- Capability: live bSDD class/property resolution for the `Classification` axis, with service misses degrading to the row's local code-shape policy.
- Shape: `BsddResolution.Resolve` builds the dictionary-class URI, calls the injected `BsddPort.Fetch`, projects `BsddClassResponse` through `BsddClass.Of`, and falls back to `LocalShape` only on transport miss; the in-process `LocalShape` leg is buildable today and the live-fetch leg is injected at the composition edge.
- Unlocks: classification, property templates, and IDS classification facets share one dictionary evidence path without blocking ingest or minting a Bim-owned transport.
- Anchors: `Semantics/classification.md#BSDD_RESOLUTION`, `Semantics/properties.md#PROPERTY_SETS`, `Review/validation.md#IDS_FACETS`, and `csharp:Compute/Runtime/channels#TRANSPORT_AXIS`.
- Tension: the `LocalShape` leg builds now; the live dictionary fetch rides the Compute transport binding at the composition edge.
- Ripple: counterpart Compute transport is `[COMPUTE_BSDD_PORT_TRANSPORT]` in `Rasm.Compute`.

[T-IDS-IFCTESTER-COMPANION]-[QUEUED]: ground the IDS audit — the in-process `IdsAudit` self-audit builds now, the cross-tool ifctester oracle rides the settled companion RPC.
- Capability: deterministic IDS conformance evidence pairs the in-process `IdsAudit` fold with the IfcOpenShell ifctester oracle.
- Shape: `IdsSpecification`/`IdsFacet`/`IdsAudit` fold over `BimModel`, parse and author IDS v1.0 XML through `System.Xml.Schema`, and serialize audit receipts through `BimWireContext`; the self-audit leg is fully Bim-owned and buildable, and the cross-tool oracle compares on GlobalId plus facet.
- Unlocks: requirement-driven model acceptance can compare C# self-audit output with the Python companion's standards-conformant ifctester result on GlobalId and facet.
- Anchors: `Review/validation.md#IDS_FACETS`, `Exchange/wire.md#WIRE_PROJECTION`, `Exchange/tessellation.md#TESSELLATION_BRIDGE`, and `python:geometry/ifc-companion`.
- Tension: the self-audit leg builds now; the companion oracle rides the settled Compute companion RPC and the Python ifctester decoder.
- Ripple: counterpart oracle RPC is `[COMPUTE_IFCTESTER_ORACLE_RPC]` in `Rasm.Compute`.

[T-IMPORT-CODECS]-[QUEUED]: route the remaining companion-required import codecs through the tessellation bridge.
- Capability: the managed mesh-text legs (OBJ/STL/OFF via `geometry3Sharp`; PLY/3MF/STEP via the host-neutral `PlyReader`/`ThreeMfReader`/`StepReader` fences) import in-process; IGES, Revit, and Navisworks intake route through the out-of-process companion as row promotions, never new importer families.
- Shape: `InterchangeFormat` rows, `InterchangeCodec` discriminants, and `StepProtocol` stay settled; `BimIo` faults `import-needs-companion` only for `iges-ansi`, Revit, and Navisworks while every managed mesh-text and STEP row imports managed.
- Unlocks: IGES (`iges-ansi`), Revit, and Navisworks intake complete the import fold once the companion CAD bridge serves them; DWG is owned by `ACADSHARP_MANAGED_DWG`.
- Anchors: `Exchange/format.md#FORMAT_AXIS`, `Exchange/import.md#IMPORT_RAIL`, `Exchange/tessellation.md#TESSELLATION_BRIDGE`, `python:geometry/mesh/cad#BRIDGE`, and `csharp:Rasm.Compute/Runtime/codecs#TWO_HOP_TESSELLATION`.
- Tension: IGES stays the distinct `iges-ansi` codec because ANSI IGES is not ISO 10303 STEP; Revit/Navisworks are proprietary and companion-only.

[QUANTITY_DERIVE_FOLD]-[QUEUED]: Replace the confirmed no-op QuantitySet.Derive body with a per-IfcClass base-quantity fold reading the bound GeometryHandle, thread the BsddResolution ClassProperty set into PropertyKey template resolution so the standard-Pset vocabulary is dictionary-driven, and collapse the stringly KindOf switch into a UnitsNet-grounded QuantityKind coercion, making the QuantitySet a real takeoff owner the 5D cost line already depends on.
- Capability: NetVolume/GrossArea/Length derive from the GeometryHandle volume/area/length per element class rather than passing occurrence quantities through unchanged, PropertyKey template rows resolve from the live BsddClass.Properties rather than the frozen Pset_* table that drifts, and unit coercion rides the UnitsNet SI-base resolver instead of a string match.
- Shape: An expansion of properties#PROPERTY_SETS replacing the no-op Derive (L73-78) with a per-IfcClass base-quantity fold over GeometryHandle, threading BsddResolution.Resolve ClassProperty into PropertyKey template resolution, and replacing the KindOf string-switch (L80-87) with a UnitsNet quantity/unit coercion; the PropertySet/QuantitySet records and QTO_TYPEDRIVENOVERRIDE inheritance stay settled.
- Unlocks: A 5D Cost rate joins a real derived QuantitySet by GlobalId, the IDS Property facet matches a typed measure with its SI unit, and a new standard Pset becomes a bSDD dictionary-URI row not a hardcoded template.
- Anchors: Semantics/properties#PROPERTY_SETS QuantitySet.Derive/PropertyKey/KindOf; Semantics/classification#BSDD_RESOLUTION BsddClass.Properties; Model/elements#GEOMETRY_HANDLE; Planning/cost#ESTIMATE CostItem.ValueOf->QuantityOf (L147 calls Derive); Review/validation#IDS_FACETS Property facet; realizes the `PROPERTY_TEMPLATE_RESOLUTION` idea.
- Tension: Base-quantity derivation reads the kernel geometry by reference and a re-tessellation here is the named seam violation; UnitsNet is ALREADY pinned (Directory.Packages.props L158) and consumed by AppUi/Compute/Materials — the QuantityKind enum maps onto its quantity/unit surface, verified against a new Bim-folder .api/api-unitsnet.md before the KindOf collapse, not a new props pin.

[CRITICAL_PATH_NETWORK]-[QUEUED]: Widen schedule#SCHEDULE in place with a CriticalPath fold computing forward/backward-pass early/late start-finish, total float, and free float over the existing SequenceRel dependency edges so the ScheduleNetwork carries the critical chain and per-task float the current Interval+lag projection cannot express, turning the activity network from a static window store into a schedulable CPM graph.
- Capability: A forward pass deriving each ConstructionTask early-start/early-finish from predecessor SequenceRel lags, a backward pass deriving late-start/late-finish from the project finish, total-float = late-start minus early-start, free-float against the earliest successor early-start, and the zero-total-float critical-path set, all as one immutable fold over the SequenceRel edge set, never a mutable PERT accumulator.
- Shape: schedule#SCHEDULE widened with a CriticalPath value record per task (EarlyStart/EarlyFinish/LateStart/LateFinish as Instant, TotalFloat/FreeFloat as Duration, bool IsCritical) and a ScheduleNetwork.Schedule fold running the forward/backward pass over the SequenceRel adjacency by topological order, the three FinishToStart/StartToStart/FinishToFinish arms each contributing their lag Period to the pass.
- Unlocks: Critical-chain-aware schedule report and 4D playback, the per-task float window WBS leveling and earned-value join read.
- Anchors: Planning/schedule#SCHEDULE ScheduleNetwork/ConstructionTask/SequenceRel; schedule.md [TASK_TIME_INTERVAL] under-utilization note names this the first calendar-arithmetic consumer beyond Instant; NodaTime Duration/Period; realizes the `SCHEDULE_NETWORK_DEPTH` idea.
- Tension: The forward/backward pass requires a topological order over the SequenceRel DAG; a cyclic dependency edge lowers BimFault.ModelRejected rather than looping, and the StartToStart/FinishToFinish arms shift the pass differently than FinishToStart so the float algebra discriminates the union arm rather than assuming finish-to-start.

[CALENDAR_WORKING_TIME]-[QUEUED]: Widen schedule#SCHEDULE with an IfcWorkCalendar working-time fold so task durations resolve against a real construction calendar (work-week, shift hours, holiday and weather exception days) rather than continuous NodaTime Interval span, turning elapsed-time windows into working-time-accurate durations as the schedule Growth note explicitly intends.
- Capability: A WorkCalendar record folding IfcWorkCalendar WorkingTimes/ExceptionTimes (IfcWorkTime over IfcRecurrencePattern) onto a NodaTime working-time function mapping an elapsed Interval onto working hours, so a five-working-day task spanning a weekend resolves its finish past the non-working days and a weather/holiday exception extends the window.
- Shape: schedule#SCHEDULE widened with a WorkCalendar record (work-week pattern, daily shift LocalTime span, ExceptionDay set folded from IfcWorkCalendar.ExceptionTimes IfcRecurrencePattern) threaded onto ScheduleNetwork as the working-time function the IntervalOf/Effective fold consults so a duration-driven task computes its finish in working time, recovering the NodaTime ZonedDateTime/LocalTime/Period surface beyond Instant.
- Unlocks: Working-time-accurate finishes, holiday/weather exception extension, shift-aware 4D playback, working-day float for the CriticalPath fold.
- Anchors: Planning/schedule#SCHEDULE ConstructionTask Interval/IntervalOf; schedule.md Growth note (L17) names 'a new calendar binding (an IfcWorkCalendar working-time exception) is one column on ScheduleNetwork read by the same fold'; GeometryGym IfcWorkCalendar/IfcWorkTime/IfcRecurrencePattern; realizes the `SCHEDULE_NETWORK_DEPTH` idea.
- Tension: The IfcRecurrencePattern weekly/monthly/by-day grammar narrows to the construction work-week and exception-day cases without minting a parallel recurrence engine; NodaTime owns the date arithmetic and the calendar function composes it, never re-deriving day math; the IfcWorkCalendar/IfcWorkTime/IfcRecurrencePattern member spellings ground against the GeometryGym surface before the fold is final.

[RUNTIME_4D_SCHEDULE_ANIMATION]-[QUEUED]: Reinstate the Wave A dormant-SharpGLTF.Runtime keyframe leg as the 4D-emit major realizing SCHEDULE_NETWORK_DEPTH: widen export#EXPORT_RAIL with an AnimateSchedule arm baking the ScheduleNetwork ConstructionState.At(Instant) snapshots into per-element glTF visibility/transform keyframe tracks through the admitted-but-unexercised ModelRoot.CreateAnimation surface, so the 4D schedule exports as one animated GLB a web viewer scrubs rather than a per-instant re-render.
- Capability: Each ConstructionTask's scheduled Interval drives a glTF Animation channel/sampler keyframe track over the LINEAR/STEP AnimationInterpolationMode, keyed by the Interval bounds projected to glTF time-in-seconds via NodaTime Duration, binding KHR_node_visibility (already on the format axis) to the per-element appearance keyframe so the schedule's ConstructionState.At fold feeds a streamed 4D timeline reusing the existing TileMetadata feature-ID binding.
- Shape: export#EXPORT_RAIL gains an AnimateSchedule arm composing ModelRoot.CreateAnimation + Animation channel/sampler keyframes, and schedule#SCHEDULE ConstructionState.At gains a companion AnimationTrack projection so the Interval.Contains snapshot and the keyframe author share one time axis; one new KhrExtension binding (KHR_node_visibility) drives the per-element appearance keyframe, no new InterchangeFormat row.
- Unlocks: 4D construction-state animation as an exported GLB (Cesium/three.js timeline scrub) and the under-utilized SharpGLTF.Runtime keyframe API becomes load-bearing instead of dead surface.
- Anchors: Exchange/export#EXPORT_RAIL BimExport; Planning/schedule#SCHEDULE ConstructionState.At; .api/api-sharpgltf.md Animation row 14, AnimationChannel row 15, ModelRoot.CreateAnimation entrypoint 07, AnimationInterpolationMode row 07 (all admitted, all unexercised); KHR_node_visibility KhrExtension already on format.md; realizes the `SCHEDULE_NETWORK_DEPTH` idea.
- Tension: No new package — SharpGLTF.Runtime is csproj-referenced (L17) yet only IMeshDecoder/SceneTemplate decode is exercised; the keyframe leg exploits the existing pin; the animation track and ConstructionState.At share one Interval-to-Instant time axis, never two; the KHR_node_visibility row already exists so the appearance keyframe binds settled vocabulary.

[EARNED_VALUE_CHANGE_ORDER]-[QUEUED]: Widen Planning/cost in place with a change-order/contingency/earned-value layer so the CostSchedule carries a ChangeOrder revision arm, a Contingency reserve column, and an EarnedValue fold (planned-value, earned-value, actual-cost, CPI/SPI, variance-at-completion) joining the schedule's actual progress to the priced lines, turning the static rollup into a tracked 5D estimate.
- Capability: A ChangeOrder record (the priced delta against a baseline CostSchedule with its approval state and IfcCostSchedule revision link), a Contingency reserve carried as a CostCategory row, and an EarnedValue fold reading the ConstructionTask Actual interval and the CostItem rate to derive BCWS/BCWP/ACWP, the cost-performance and schedule-performance indices, and the estimate/variance-at-completion, one fold over the priced-line and progress join.
- Shape: cost#ESTIMATE widened with a ChangeOrder record (baseline GlobalId, priced CostItem delta set, ChangeOrderStatus [SmartEnum<string>]), a Contingency column on CostSchedule, and a CostSchedule.EarnedValue fold joining the schedule ConstructionTask Actual progress to the line rate (BCWP = percent-complete x budgeted value, CPI = BCWP/ACWP, SPI = BCWP/BCWS, EAC = budget/CPI) onto a typed EarnedValueReport receipt, never a generic ledger.
- Unlocks: Tracked cost performance (over-budget/behind-schedule/forecast), change-order audit against baseline, contingency drawdown.
- Anchors: Planning/cost#ESTIMATE CostSchedule/CostItem/Rollup; Planning/schedule#SCHEDULE ConstructionTask Actual interval; GeometryGym IfcCostSchedule revision; cost.md CostCategory/MonetaryAmount.
- Tension: Earned value joins the cost line to the schedule task by GlobalId so percent-complete reads the schedule's ConstructionTask Actual interval rather than re-deriving progress; the change-order delta is a priced revision against a baseline CostSchedule and reuses the existing CostItem/CostValue algebra, not a parallel revision store.

[ASSEMBLY_REL_EXTRACTION]-[QUEUED]: Populate all five AssemblyRel arms (Aggregates/Nests/ContainedIn/Voids/Connects) the closed union declares but the import Semantic fold extracts only Aggregates for, and add a polymorphic BimAssembly.Traverse owning ancestor/descendant/contained/void walks over the spatial tree as one discriminated fold rather than implied per-level loops.
- Capability: A void relationship (IfcRelVoidsElement), a nesting (IfcRelNests), a spatial containment edge (IfcRelContainedInSpatialStructure), and an element connection (IfcRelConnectsElements) each ride their AssemblyRel arm so the decomposition graph is complete, and a storey-to-element or element-to-void query is one Traverse direction not a hand-rolled recursion.
- Shape: An expansion of import.md Semantic to extract IfcRelNests/IfcRelContainedInSpatialStructure/IfcRelVoidsElement/IfcRelConnectsElements into the IfcSemanticModel.Decomposition family, and a structure.md BimAssembly.Traverse(string globalId, TraversalDirection dir) polymorphic fold over the SpatialContainer tree discriminating Ancestors/Descendants/Contained/Voids; the AssemblyRel union and SpatialContainer record stay settled.
- Unlocks: The BySpatialContainer query arm, the IDS PartOf facet (which already lowers to BySpatialContainer over an AssemblyRelKind), and a void/opening reconciliation all read a complete decomposition graph, and the spatial walk is one polymorphic Traverse the GeoReference root-reconciliation and Diff move-detection compose.
- Anchors: Model/structure#ASSEMBLY_TREE AssemblyRel/BimAssembly.Assemble; Exchange/import#IMPORT_RAIL Semantic IfcRelAggregates extract (L167-168, only Aggregates); Review/validation#IDS_FACETS PartOf/AssemblyRelKind; Model/query#ELEMENT_SET BySpatialContainer; .api/api-geometrygym-ifc IfcRelConnectsElements row 11.
- Tension: The four unextracted arms confirm their IfcRelNests/IfcRelContainedInSpatialStructure/IfcRelVoidsElement/IfcRelConnectsElements member spellings against the catalogued GeometryGym surface before the import fold carries every closed-union arm, since structure.md RESEARCH (L58) flags only Aggregates as currently grounded.

[TESSELLATION_OUTCOME_RECEIPT]-[QUEUED]: Deepen the bare tessellation page from a request into a request-plus-typed-outcome owner: a TessellationOutcome receipt carrying the re-imported ImportedGeometry, the resolved GLB ArtifactKey, the deflection the content-key folded, a CacheHit fact discriminating a content-key reuse from a fresh companion cross, and the explicit CapabilityMiss-degrade posture stating what Bim returns when the companion rail is unreachable.
- Capability: Closes the unstated managed-degrade gap: the page deletes the in-process BRep evaluator but never states the fallback, so the CapabilityMiss arm becomes the declared posture rather than an implied hole, and a re-tessellation at the same deflection reads a typed CacheHit receipt instead of re-crossing the companion blindly.
- Shape: An expansion of tessellation#TESSELLATION_BRIDGE adding a TessellationOutcome record (ImportedGeometry, ArtifactKey, double Deflection, bool CacheHit, Instant At) the bridge returns on a Fin rail, a TessellationRequest.Resolve(InterchangeIdentity cache) reading the Persistence artifact index by ArtifactKey before issuing, and a Boundary clause naming the CapabilityMiss-on-companion-unreachable degrade; the request signature stays, the receipt is the new typed evidence.
- Unlocks: A takeoff or render consumer reads one typed TessellationOutcome carrying cache-hit evidence and the content-key join to the Persistence GLB artifact, and a companion-unreachable model surfaces CapabilityMiss as a declared rail rather than an exception or silent empty mesh.
- Anchors: Exchange/tessellation#TESSELLATION_BRIDGE TessellationRequest.Plan/ArtifactKey (L33); Exchange/import#IMPORT_RAIL BimIo.ImportGeometry GLB re-entry; Model/faults#FAULT_BAND BimFault.CapabilityMiss; csharp:Rasm.Compute/Runtime/codecs#TWO_HOP_TESSELLATION; csharp:Rasm.Persistence artifact index.
- Tension: The cache read joins the Persistence artifact index by ArtifactKey but Bim depends strictly upward and mints no Persistence reference: the cache-hit join rides the Compute content-key the artifact is addressed by, the durable lookup staying the app-platform caller's concern at the seam.

[MEP_INTERFERENCE_CHECK]-[QUEUED]: Widen Model/systems with an InterferenceCheck fold detecting clearance violations and hard clashes between distribution-system runs and across the structural/architectural element set so the connectivity graph gains the coordination check the MEP model needs beyond flow-reachability, ranking each interference by clearance deficit.
- Capability: An Interference fold over the DistributionSystem member geometry pairs (and against the structural AnalysisModel members) detecting hard clashes (overlapping solids) and clearance clashes (insufficient maintenance/insulation gap), each Interference row carrying the clashing GlobalId pair, the ClashKind, the measured penetration/clearance-deficit, and the owning DistributionSystemKind disciplines so a cross-discipline clash ranks above an intra-discipline one.
- Shape: systems#SYSTEM_TRACE widened (or a [3]-[INTERFERENCE] section) with an Interference record (clash GlobalId pair, ClashKind hard/clearance, measured deficit, the two DistributionSystemKind/IfcDomain disciplines) and a DistributionSystem.Interferences fold pairing the member geometry by reference through the kernel proximity test, the clearance threshold read from the member's insulation/maintenance Pset, never re-tessellating in this owner.
- Unlocks: Ranked MEP clash report (duct-vs-beam, pipe clearance, tray-vs-structure) feeding Review/coordination ClashProposal and BCF.
- Anchors: Model/systems#CONNECTIVITY DistributionSystem/DistributionSystemKind; Model/structural#ANALYSIS_MODEL members; Semantics/properties#PROPERTY_SETS clearance Pset; Review/coordination#COORDINATION ClashProposal; realizes the `COORDINATION` idea.
- Tension: The interference test is geometric proximity binding the kernel Rasm geometry by reference; the systems owner keeps the Interference row as host-neutral scalar evidence (GlobalId pair + measured deficit) and the solid-intersection/distance test routes to the kernel geometry owner, the same host-neutrality law the connectivity graph already holds.

[AUDIT_TRAIL]-[QUEUED]: Widen Review/diff with a model-mutation audit trail folding the chained ModelDiff change-sets across a version sequence into an immutable AuditEntry log (who/when/what-changed per element GlobalId) so every model mutation carries a tamper-evident content-keyed provenance the federation and compliance consumer read, distinct from the geometry-asset XMP lineage.
- Capability: An AuditEntry fold projecting each ModelDiff ElementChange across a version sequence onto an immutable log row (element GlobalId, change kind, baseline/revision content-key, author, Instant, version pointer), chained by the prior entry's content-key so the trail is tamper-evident, and an AuditTrail.For(GlobalId) query folding every mutation an element underwent into its lifecycle history.
- Shape: diff#MODEL_DIFF widened (or an [AUDIT] section) with an AuditEntry record (GlobalId, ElementChange kind, content-key pair, author, Instant, parent-entry content-key) and an AuditTrail fold chaining the per-version ModelDiff change-sets into the append-only log, each entry keyed on the prior entry's hash so a retroactive edit breaks the chain, queryable by element GlobalId.
- Unlocks: Compliance audit (who/when/from-what), tamper-evident model history, per-element lifecycle anchoring BCF and the Versioned merge.
- Anchors: Review/diff#MODEL_DIFF ElementChange/ElementFingerprint (L21-29 confirmed); Review/versioning#VERSIONED ModelVersion lineage; NodaTime Instant; System.IO.Hashing chain hash; realizes the `VERSIONED` idea.
- Tension: The audit trail is a model-mutation log (who/when/what-changed-semantically), explicitly distinct from the Wave A tile-XMP geometry-asset provenance (who-minted-this-GLB); the two stay separate, the audit trail keying on the diff ElementChange and version lineage, never the export artifact content-key, the durable append-only store the Persistence concern.

[INCREMENTAL_DELTA_REIMPORT]-[QUEUED]: Widen Exchange/import with an incremental delta reimport folding a changed-source-bytes reimport against the prior BimModel snapshot through the Review/diff content-key so only changed elements re-project, turning a full re-ingest into an incremental patch the federation cache reads.
- Capability: A BimIo.Reimport fold that ingests a revised source and, joining against a prior BimModel by GlobalId plus the Review/diff content-key, re-projects only the added/modified/moved elements while reusing the unchanged element rows verbatim, producing a patched BimModel plus the ModelDiff change-set in one fold so a large model's minor revision costs the delta, not the whole graph.
- Shape: Exchange/import#IMPORT_RAIL widened with a BimIo.Reimport(bytes, BimModel prior) fold that projects the revised IfcSemanticModel, fingerprints each element through the Review/diff#MODEL_DIFF content-key, reuses the prior element row when the content-key matches, and re-projects only the changed set, emitting the patched BimModel and ModelDiff together.
- Unlocks: Delta-cost minor revisions, federation patch storage, the diff falling out of the reimport for free.
- Anchors: Exchange/import#IMPORT_RAIL BimIo; Review/diff#MODEL_DIFF ElementFingerprint content-key (L21,L51-54); Model/elements#ELEMENT_MODEL BimModel.Project; ARCHITECTURE seam Model <- csharp:Rasm.Persistence/Query content-key; realizes the `VERSIONED` idea.
- Tension: The reimport reuses the Review/diff content-key fingerprint as the change-detection key rather than a second identity scheme; the unchanged-element reuse preserves the kernel geometry handle by reference (no re-tessellation), and the IFC semantic graph still parses fully at ingest so the incrementality is in the projection-and-fingerprint stage, not the byte parse.

[ROUNDTRIP_VERIFICATION_MATRIX]-[QUEUED]: Widen Exchange/export with a lossless round-trip verification matrix folding an import-then-export-then-reimport cycle across the IFC STEP/ifcXML/ifcJSON serializations into a typed RoundTripReport that witnesses per-element and per-property field fidelity by content-key, so the codec proves losslessness rather than asserting it.
- Capability: A RoundTrip fold running a BimModel through each InterchangeFormat serialization and back, joining the source and reimported element sets by GlobalId, and folding the per-element content-key delta through the Review/diff fingerprint into a RoundTripReport (lossless-element count, lossy-field set per format, content-key match ratio).
- Shape: Exchange/export#EXPORT_RAIL (or a [ROUNDTRIP] section) authoring a RoundTrip.Verify(BimModel, InterchangeFormat) fold that exports through BimExport, reimports through BimIo, and folds the source-vs-reimported ModelDiff into a RoundTripReport receipt partitioned by InterchangeFormat, reusing the diff fingerprint content-key rather than a second comparison surface, covering the IFC STEP/XML/JSON triad GeometryGym serializes.
- Unlocks: A per-format fidelity matrix proving which serialization preserves which field, an interchange-policy losslessness witness, a codec regression oracle.
- Anchors: Exchange/export#EXPORT_RAIL BimExport; Exchange/import#IMPORT_RAIL BimIo; Exchange/format#FORMAT_AXIS InterchangeFormat STEP/XML/JSON; Review/diff#MODEL_DIFF ElementFingerprint content-key.
- Tension: The round-trip fold reuses the Review/diff content-key fingerprint as the fidelity metric rather than minting a second element-comparison surface; the geometry leg crosses the tessellation companion so the matrix witnesses semantic-graph and property fidelity in-process while geometry fidelity rides the same companion, and the verification couples to no host geometry type.

[ACADSHARP_MANAGED_DWG]-[QUEUED]: Reinstate the Wave A ACadSharp DWG/DXF admit the synth dropped: route the conflated Dwg/Dxf InterchangeFormat row from the native-companion two-hop to a managed in-process ACadSharp 3.6.29 codec, the pure-managed osx-arm64 DWG+DXF owner already consumed by Fabrication and AppUi, so DWG/DXF round-trip lands managed without crossing the Python companion.
- Capability: DWG/DXF import-export becomes a managed in-process codec on the FORMAT_AXIS rather than a native-companion two-hop: BimIo gains an ACadSharp DxfDocument/CadDocument decode arm folding entities (LINE/LWPOLYLINE/CIRCLE/ARC/MESH/3DFACE/INSERT) onto ImportedGeometry triangle-soup and the layer/block hierarchy onto IfcSemanticModel SpatialNode rows, the single decompile-verified ACadSharp surface serving AppUi drafting, Fabrication polygon import, and Bim exchange.
- Shape: format#FORMAT_AXIS InterchangeCodec.NativeCompanion on the Dwg row swaps to a new InterchangeCodec.AcadSharp managed=true row (canImport/canExport true, tessellationRequiresCompanion=false); import#IMPORT_RAIL BimIo gains the ACadSharp decode arm; format.md RESEARCH NATIVE_FORMAT_BRIDGES loses the DWG companion claim. Full admission chain: add the ACadSharp PackageReference to Rasm.Bim.csproj + a Bim/.api/api-acadsharp.md catalogue via tools/assay api + README [02]-[DOMAIN_PACKAGES] Interchange registry; the central pin (Directory.Packages.props L307) already exists.
- Unlocks: Managed DWG/DXF round-trip without crossing the Python companion; aligns Bim with the cross-package ACadSharp owner; promotes the codec by row data not an if(dwg) branch per CODEC_PROMOTION_DISCIPLINE.
- Anchors: Exchange/format#FORMAT_AXIS InterchangeCodec/InterchangeFormat.Dwg (format.md L107 Dwg row routes .dwg/.dxf to NativeCompanion); Exchange/import#IMPORT_RAIL BimIo; ACadSharp 3.6.29 pinned (Directory.Packages.props L307) + catalogued at Fabrication/.api/api-acadsharp.md; consumed by Rasm.Fabrication + Rasm.AppUi csproj.
- Tension: ACadSharp is pure-managed and RID-safe (osx-arm64, already consumed by two packages); netDxf is NOT dropped from the manifest (AppUi consumes it) but is NOT admitted to Bim because ACadSharp supersedes it (managed DWG AND DXF, where netDxf reads DXF only) — admitting a second managed CAD lib for one row is the rejected form; the ACadSharp DxfDocument/CadDocument entity surface grounds against the Bim-folder api-acadsharp catalogue before the decode arm is final.

[BIM_CONNECTION_IFC_WIRE]-[QUEUED]: Rasm.Bim owns the IFC 4.3 serialization of the ConnectionItem axis (IfcReinforcingBar/IfcReinforcingMesh/IfcMechanicalFastener/IfcFastener plus the new joint IfcMechanicalFastener / IfcRelConnectsWithRealizingElements), the MaterialAssignment trichotomy, the MaterialPropertySet Psets plus the new sustainability Pset_EnvironmentalImpactValues, and the BimAppearance content-key — the boundary every Materials owner defers to as portable scalar data here.
- Capability: The BIM semantic owner mapping the ConnectionItem Family/Section columns to the IfcReinforcingBar/IfcMechanicalFastener structural elements (and the new joint weld/stud to IfcMechanicalFastener + IfcRelConnectsWithRealizingElements), the Profile family to the IfcProfileDef subtypes (including the new composite/cold-formed SteelClass subtypes), the MaterialAssignment cases to the IFC trichotomy, the MaterialPropertySet/AssemblyProperty/sustainability Environmental to Pset_MaterialMechanical/Thermal/Acoustic/EnvironmentalImpactValues + Uniclass/OmniClass classification, and the SurfaceShade/MaterialKey to the BimAppearance content-key.
- Shape: A Rasm.Bim wire projection reading the Materials ConnectionId/ProfileId/MaterialId-keyed portable data and emitting the IFC entities, the consumer end of every Materials seam that says serializes to IFC 4.3 at the Rasm.Bim boundary, host-neutral here.
- Unlocks: A connection round-trips to IFC by its family discriminant, the property set federates by MaterialId, the sustainability discipline federates to Pset_EnvironmentalImpactValues and Uniclass, and the appearance content-key binds the BimAppearance the Rasm.Semantics seam reads.
- Anchors: ARCHITECTURE.md [02]-[SEAMS] Connection/Properties/assembly -> Rasm.Bim wire+projection (lines 48-50); connection.md the IfcReinforcingBar/IfcMechanicalFastener boundary (line 16); properties.md IfcMaterialProperties Pset mapping (line 284); the new JOINT_CONNECTION_FAMILY, SUSTAINABILITY_PROPERTY_DISCIPLINE, and STEEL_COMPOSITE_AND_COLDFORMED seams; realizes the `JOINT_CONNECTION_FAMILY` idea.
- Ripple: counterpart of `Rasm.Materials` `[JOINT_CONNECTION_FAMILY]` idea (IFC 4.3 serialization of the weld/stud/adhesive ConnectionItem).

[BIM_COORDINATION_ANNOTATION]-[QUEUED]: Ground the `coordination` sub-domain as the BCF/coordination DOMAIN owner relocated from `Rasm.Persistence/Sync/annotation`: `Rasm.Bim/coordination` owns the `BcfTopic`/`BcfComment`/`BcfViewpoint` record family and the `.bcfzip`/`BcfApi` codec, while Persistence retains the generic durable-annotation + op-log/CDE-sync store joining by `GlobalId` content-key, settling the two-package BCF double-ownership the `[COORDINATION]` idea records.
- Capability: The host-neutral BCF domain owner — `BcfTopic` carrying title/`BcfStatus`/type/priority/author plus the comment and viewpoint sets, the `BcfArchive` `.bcfzip` codec over `ZipArchive`, and the `BcfApi` REST projection riding Compute transport — lands wholly in `Rasm.Bim/coordination`, and durable anchored-annotation rows plus the CDE op-log sync stay in Persistence, the two joined by the `GlobalId` content-key rather than a second BCF schema.
- Shape: `Rasm.Bim/coordination` retains the `Review/issues#BCF_ARCHIVE` `BcfTopic`/`BcfComment`/`BcfViewpoint` family, the `BcfArchive.Read`/`Write` `.bcfzip` fold over `ZipArchive`, and the `BcfApi.Project` REST shape; Persistence drops the BCF codec/REST claim and keeps the generic durable-annotation + op-log/CDE-sync owner, the cross-package join resolving the double-ownership the `[COORDINATION]` idea surfaced.
- Unlocks: A single BCF codec/REST owner the CDE round-trips, the durable annotation op-log federating BCF topics by `GlobalId` content-key without re-minting the schema, and the `[COORDINATION]` `SignOff` state machine reading a settled `BcfStatus` lifecycle.
- Anchors: `Review/issues#BCF_ARCHIVE` `BcfTopic`/`BcfComment`/`BcfViewpoint`/`BcfArchive`/`BcfApi` at BCF 3.0 (the branch `[FEDERATED_COORDINATION_COCKPIT]` BCF 3.0 topic/component/comment schema, distinct from the 2.1 `.bcfzip` markup layout the codec also reads); `Model/elements#ELEMENT_MODEL` `GlobalId` anchor; `csharp:Compute/Runtime/channels#TRANSPORT_AXIS`; the `[COORDINATION]` idea's recorded BCF double-ownership resolution; ARCHITECTURE.md `coordination ⇄ csharp:Rasm.Persistence/Sync/annotation` seam.
- Tension: Bim owns the BCF record family plus `.bcfzip`/`BcfApi` codec and Persistence owns the durable op-log/CDE-sync store; the two never re-mint the BCF schema across the boundary, joining only on the `GlobalId` content-key the durable annotation row carries.
- Ripple: counterpart of `Rasm.Persistence` `[ANNOTATION_RELOCATE_TO_BIM]` (BCF/coordination domain relocated out of `Sync/annotation`).

[BIM_COORDINATION_ISSUES]-[QUEUED]: Ground the `coordination` sub-domain as the BCF issue-board DOMAIN owner relocated from `Rasm.AppUi/Editing/issues`: `Rasm.Bim/coordination` owns the issue-board semantics over the `BcfTopic` contract, AppUi keeps only the board PROJECTION, and the `[COORDINATION]` `SignOff` state machine reads the Bim `BcfStatus` lifecycle.
- Capability: The issue-board domain — topic threading, status/priority/type discrimination, viewpoint-anchored selection, and the `SignOff` state machine over the `BcfStatus` lifecycle — lands in `Rasm.Bim/coordination` over the `BcfTopic`/`BcfComment`/`BcfViewpoint` record family, leaving `Rasm.AppUi/Editing/issues` a host-local board projection that materializes the Bim contract and never re-mints a BCF schema.
- Shape: `Rasm.Bim/coordination` carries the issue-board fold over the `Review/issues#BCF_ARCHIVE` `BcfTopic` family and the `[COORDINATION]` `SignOff` `[SmartEnum]` over the `BcfStatus` transitions; `Rasm.AppUi/Editing/issues` consumes the Bim `BcfTopic` contract at the package edge as a board projection, the relocation leaving `SignOff` on a settled Bim owner.
- Unlocks: A single BCF issue-board domain the desktop and any future head project over one contract, the `SignOff` lifecycle bound to the Bim `BcfStatus`, and the `[COORDINATION]` `ClashProposal` anchoring its proposed fix to the Bim `BcfTopic`.
- Anchors: `Review/issues#BCF_ARCHIVE` `BcfTopic`/`BcfStatus`; the `[COORDINATION]` `SignOff` `[SmartEnum]` state machine and `ClashProposal` fold; ARCHITECTURE.md `coordination <- csharp:Rasm.AppUi/Editing/issues # [PORT]: BCF issue-board projection` seam.
- Tension: Bim owns the issue-board domain over the `BcfTopic` contract and AppUi owns only the board projection; the `SignOff` state machine stays in Bim reading the `BcfStatus` lifecycle, so the AppUi head re-mints no BCF schema and reads the domain at the package edge.
- Ripple: counterpart of `Rasm.AppUi` `[ISSUES_RELOCATE_TO_BIM]` (BCF issue-board domain relocated out of `Editing/issues`, AppUi keeps the board projection).

[BIM_SCHEDULE]-[QUEUED]: Ground the `schedule` sub-domain as the 4D construction-state + CPM DOMAIN owner: `Rasm.Bim/schedule` owns the host-neutral `CriticalPath`/`ConstructionState.At` fold over the IFC-projected `SequenceRel` network, while `Rasm.Persistence/Sync/schedule` retains the durable P6/MS-Project schedule rows + sync, consistent with the `[SCHEDULE_NETWORK_DEPTH]` parallel-CPM boundary note.
- Capability: The 4D/CPM domain — the `ScheduleNetwork` forward/backward-pass `CriticalPath` fold over the `SequenceRel` edges, the `WorkCalendar` working-time fold, and the `ConstructionState.At(Instant)` element-set snapshot over the IFC-sourced `IfcWorkPlan` network — lands in `Rasm.Bim/schedule`, while the durable Primavera P6 XER and MS-Project XML schedule rows plus their sync stay in `Rasm.Persistence/Sync/schedule`.
- Shape: `Rasm.Bim/schedule` keeps the `Planning/schedule#SCHEDULE` `ScheduleNetwork`/`ConstructionTask`/`SequenceRel` host-neutral CPM/4D fold over the IFC-projected edges; `Rasm.Persistence/Sync/schedule` owns the durable P6/MS-Project store and its `CpmPass` over the external `TaskRelation` DAG, the two reading disjoint source lanes per the `[SCHEDULE_NETWORK_DEPTH]` boundary note.
- Unlocks: A single host-neutral CPM/4D domain fold the report, 4D playback, and earned-value join read, the durable external-scheduler store federating by `GlobalId`, and the `[SCHEDULE_NETWORK_DEPTH]` `CriticalPath`/`ConstructionAnimation` folds scoped strictly to the IFC network.
- Anchors: `Planning/schedule#SCHEDULE` `ScheduleNetwork`/`ConstructionTask`/`SequenceRel`/`ConstructionState.At`; the `[SCHEDULE_NETWORK_DEPTH]` parallel-CPM boundary note (IFC `SequenceRel` vs external P6/MS-Project `TaskRelation` lanes); ARCHITECTURE.md `schedule ⇄ csharp:Rasm.Persistence/Sync/schedule # [WIRE]: durable schedule store` seam.
- Tension: Bim owns the host-neutral CPM/4D fold over the IFC-projected `SequenceRel` network and Persistence owns the durable P6/MS-Project store and its `CpmPass` over external `TaskRelation` edges; a Bim `CriticalPath` reading a P6 relation or a Persistence `CpmPass` re-deriving the IFC network is the cross-package drift the relocation makes explicit.
- Ripple: counterpart of `Rasm.Persistence` `[SCHEDULE_RELOCATE_TO_BIM]` (4D construction-state + CPM domain relocated out of `Sync/schedule`, Persistence keeps the durable store).

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
