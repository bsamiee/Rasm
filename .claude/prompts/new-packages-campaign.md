[PLANNING vs IMPLEMENTATION — two modes, never interleaved.] This prompt governs PLANNING. In planning we do ALL the discovery — research, decompile and spike resolution, gap-hunt, ideation, inherited-pool binding — and emit a decision-complete, implementation-ready plan where every change is specified to its file, owner, row/case, and signature. Every artifact a session builds — the briefs and registers under `.artifacts/planning-briefs/`, the integration work-list manifest, the spike proofs — is a PLANNING output that makes the plan decision-complete, NOT an implementation-time activity. IMPLEMENTATION is a later, separate run that does no discovery: it dispatches workflows that apply the finalized plan and finds nothing new. So: when planning, find and map everything; when implementing, just send the workflows to apply what was mapped.

[FENCE_LAW] THE UNIT OF WORK IS THE CODE FENCE, NEVER PROSE: for every owner, author the transcription-complete C# declaration — named members, union/enum cases, record fields, method signatures AND bodies where logic exists, exact API spellings — so implementation copy-pastes it. Implementing or ENRICHING an item means authoring or deepening that code fence; the measure of a done item is new or expanded code INSIDE the fence, not prose, a card, a bare DENSITY_BAR/FEATURES/ROADMAP row, a TASKLOG closure, or a ledger registration around it. A pass that added page insertions but no new or deepened code fence has NOT implemented or enriched anything — it is undone. Every per-folder lane prompt MUST carry this law and the lane's own red-team MUST fail a prose-only enrichment.

Ensure the planning phase is used properly to fully scope all work in detail that we can for implementaiton, so that we can do parallel work and have agents read the plan and just follow it, and if it's done from A to Z in detail, we can have faster work in the first few phases; leverage sonnet + opus for exploration, info gathering, and use Opus for each folder, cross-cutting to create the finalized detailed plan per scope, and these plans must be fully features. 

Use `loc` and `tree` commands we have (local on disk) they take a single <arg> that is a path. When making your workflow scripts, please be careful of arg dropping.

READ all of: `libs/csharp/.planning/campaign-method.md` undertand the approach we have fully. We want to ensure we are exhaustive in our findings of enrichment to add to ALL existing files, capturing all existing packages we use, ensuring they are all integrated, each file feature/functionality/capability is fully pushed, and if we add new files, we properly update the architecture and have a clean organized system; we want to ensure no category is left overlooked. `libs/csharp/.planning/FEATURES.md` contains "larger" ideas that we can use to distill/capture concepts from as well, imagining those apps we'd want, but lack the lib grade code to do. Likewise, all folders have a `libs/csharp/.planning/TASKLOG.md` for existing open work to finalize, in addition to new additions. 

ALL CONTENT MUST BE MADE ROOT/GROUND-UP, per our `docs/stacks/csharp/README.md` this is the opportunity to ensure the densest/complex/richest code now prior to a future implementation, so we can improve/refine all pipelines, ensuring alignment, no ad-hoc/tacked on code, etc.., but deep integration/unified where possible.

List all files in `libs/csharp/.planning`, read all of: `libs/csharp/.planning/campaign-method.md` this defines the purpose of the session, we are doing exhaustive planning phase implementations for our packages, to ensure all feature sets, capabilities, functionalities are fully considered, added, and optimized in implementation, no gaps remain in any capacity. We are building these for rhino/gh2, but also in isolation, each folde ris meant to be feature full/rich on it's own, and made in concert to be complimentary. 

Read ALL of: `docs/stacks/csharp/README.md` this contains our code doctrine, which is law, and adhere to the page-craft for all work. Read ALL root files in `docs/stacks/csharp/` as well for guide patterns we must adhere to. List all files in `docs/stacks/csharp/domain` and read them all; all agents during implementation must read all the same files: root files (core) + README.md, and their respective/relevant files from `docs/stacks/csharp/domain`. Treat these as skill files essentially, and law to adhere to, expecially code doctrine.

Target directories:
- `libs/csharp/Rasm.AppHost`
- `libs/csharp/Rasm.AppUi`
- `libs/csharp/Rasm.Compute`
- `libs/csharp/Rasm.Persistence`

IMPORTANT: All folders above have a tasklog, these are all items that during planning we need proper implementaiton (full) generated to implement, these are additive to new ideas/concepts we generate/form DURING planning phase. Implementaiton will be done together, all open tasklog items + new items we find, and the iterative/reinfment/enriching of the existing content + new files if needed, all prior to a final feature ideation in the central libs/csharp/.planning/ to then update the features in that folder, to identify gaps in our existing packages, either for existing features (where they are surface level/simple) as well as new ones, either within existinf design docs, or new ones necessary to properly add to the right tasklogs. CRITICAL: The central `libs/csharp/.planning/TASKLOG.md` is something we also identify how to implement fully during planning phase, so that we have a thorough, comprehensive implemnetation phase, in parallel, per folder, and then refining/hardening/critiquing per folder, follower by a cross-cutting all folder review/alignment/improvement pass. ALL changes/implementaitons/etc must be done according to `docs/stacks/csharp/` (read all content + all agents do as well), to ensure we always re-factor all design docs consistently as new features/capabilities come to keep it all integrated/ground-up/root-up built, we can refactor aggressively, to ensure no surface/object bloat to keep collapsing types/etc as we identify possibilities to have dense/complex/rich code.

IMPORTANT: Read all of `libs/csharp/.planning/FEATURES.md` as a source of features/ideas we came up with, to use as a source of ideation for the planning phase, focus on the number of ideas we can ensure full implementation, rather than a grab-bag, identify any and have investigation to see what we have vs don't and need to add; once we finalize an idea fully, move it to the end of the file in a "implemented" section, so we have a history of feautre ideas we implemented.

SIBLING LIBRARY BRANCHES (first-class, each on its own cadence with its own workflow passes; no code — lib-grade guidance, architecture, and API evidence only, held to the `docs/stacks/csharp/README.md` doctrine applied to each language's idioms):
- `libs/typescript/.planning` — the web/edge branch (TS7/tsgo + an improved Effect-native package set), kept lib-grade and aligned to the C# wire contracts AND the geometry/IFC interchange projections; it is a branch of the lib with proper higher-order domains, never loose folders. Ultra-dense Effect/Schema/Match dispatch, Schema as the single source of truth; the named defect class to refuse is loose schema spam and branded-type/type/const proliferation.
- `libs/python/.planning` — the agnostic/universal Python library branch (the single root `pyproject.toml` is the only manifest), aligned hand-in-glove with the C# packages through the companion/offline seams it owns (the two-hop IFC tessellation rail via the IfcOpenShell companion, data/artifact bundle shapes, graduation evidence) — complementary, never coupled, and built for far more than one purpose. Re-derive its package/folder/domain set from first principles; refine the central `pyproject.toml` with the strongest modern libs at newest stable; finalize every package's `.api/` catalogue with zero gaps. Ultra-dense `expression` ROP + match dispatch + msgspec/pydantic models; the named defect class to refuse is class/type/string/constant spam.

Both branches are fragile languages: every design decision earns an iterative ultra-harsh critique pass, a distinct red-team pass, and a hardening pass before any design doc is final. 

Within each folder, there are roadmaps and features. We want to implement all features/roadmap items properly, in the unified/dense surface, we are building root-up, the opportunity to create the design docs now, so that prior to coding in a future session, everything has been considered, no gaps; each existing functionality is pushed to 11/10, and exhaustive, all roadmap items are done, and we leverage `libs/csharp/.planning/FEATURES.md` and similar files now afterwards to ideate potential features we could make with this suite of packages, to then go back and implement the changes, either to existing files, or new ones, and consistently iterate/reifne all files to align. 

All folders have a `.api` folder with all external packages being used, and already extracted for API information. We MUST maximize + fully integrate each external package, uniftying functionality/features to be in an optimized/dense footprint. Understand the approach we are taking per the campaign-method, it defines the package design philosophy, modalities, and needs. We want to ensure all roadmap items are implemented/done per package, all the planning features ideas are fully understand from A to Z for the packages we have to integrate the functionality within the relevant packages, so that we can achieve those ends, we want to fully identify all ts related functionality we need to map/identify it, and rebuild the docs in there to be useful, we will be on ts 7 + new packages/improved. TS heavily follows csharp doctrine just adjusted for ts standards, minimal shape/objects, maximize density/complexity/richness. 

It is crucial that we first implement all roadmap + feature derived ideas, then, do a full folder to folder cross-cutting refinement pass, where we read all functionality, ensure it's aligned and adjusted as needed. THen, we want to do a focused per folder (parallel) ultra-deep dive to ensure everything is maximized in functionality/capability, and code is refined to continue being complex/dense/rich and adhering to our docs/stacks/csharp/ doctrine + files. 

We want to ensure no stone is unturned, all functionality we could ever need for any type of rhino/gh2 app is defined, extremely rich/powerful UI, optimized compute/persistence/threading, full parameterization for flexible usage, agent first understanding, unified rails/entry, minimal surface + maximum functionality, FULLY capturing the API capabilities of all external libs, pushing to the limit, and adding any new packages/tooling as needed to ensure that we have no gaps.  IMPORTANT: We also want to ensure these are all ready now for full professional tooling concerns, things like metrics/telemetry, etc are all setup, integration/preparation for backends being different, or reddis, caching, etc not as one set solution, but parameterized, and set now for full usage, but the ability to have the other things easily integrated/added, and we've considered all complex considerations for local/remote/headless/etc behaviors pipelines, considerations for application type, sourcing, etc

For example, on the transport/web end, that we can effectively make a speckle style project, we have remote, local, companion, on host, side-car and standalone full app features, shell, etc capabilities. Regarding pushing functionality to the maximum, for example, in UI - creating all the possible charts in a unified shape, pushing them further, not leaving any value out, and then, adding higher-order capabilities/functionalities via new functions/methods to make the libs even stronger, provide all the tools an app in the future will need now with the flexibility to handle them, and use the .planning/features file as a place to draw INSPIRATION for new features to add to packages, or cross cutitng. 

We want to have an agent ideate and add new sections to the features file of high impact/value concepts, that are relevant to what we are trying to do, so that between phases we have rich miniming material. It's crucial we ensure persistence is also setup fully/properly with all stores and maximizing all extensions for them, pg especially, we have proper provisioning/deployment logic in the right place if possible, understanding the context our apps willbe made/used in and with, local, remote, server, side-car, etc and that at all stages, we ensure all code will be made to properly work/integrate with rhino/gh2, where features, like UI have limitaitons, we move past them anyway, but still design code so that is workable on rhino/gh2, but not a cause to limit capabilities, so standalone apps are sitll feature rich, companion, etc.. 

Use the appropriate models for more simple tasks/exploration/research, and use opus for all ambitious tasks, do not over granularize but ensure proper task assingment for self critiquing content, approaches to the code/design in general, adherance to doctrine + docs/stacks/csharp/ root files, and to be ambiitous/deep in all functionality added, we have powerful feature sets, leveraging all external libs, all extensions for them, and our code is made from the ground/root up for all feautre sets, and each package has no blind spot in it's domain, and is going to integrate with the others perfectly.

During planning phase, use a mixture of sonnet + opus agents for the file exploration and simple tasks, use opus where justified to help create the full task work during implementation per project for all roadmap work + features derived ideas (opus to help derive ideas) + existing code refinement/refaactoring to do. We will first have one agent per folder refactor all code, as defined in the plan, then, proceed to finalize all roadmap work, then implement the derived ideas, at which point we will then have a broader pass agent to read all the packages to align all code properly where needed, implicit alignment. 

During that same time, we will have two seperate stream of work, 1 agent per folder to read existing content/functionality/capabilities, and the API avialable, critique where we are coming short in capabilities in existing bodies/concepts, where we have gaps in capabilities/new ones, and high value higher order concepts to add, to rebuild the roadmap, removing any finalized/implemented work, then a seperate stream, which is just one agent to read all the packages, (be aware of ts end as well), the main features file, and ideate new, better ideas, where we can understand the main purpose of our monorepo, the extensions/external libs we have, existing functionality, and what kind of apps/plugins we can make with them potentially, to find 5-10 high value concepts, and also identify all the existing gaps that are stopping us from achieving it, so we don't just get new concepts/ideas, but this time, we also implement all the features/capabilities that are missing per folder and cross-cutting that are keeping us from it, we do not want huge categorical gaps that would belong in a new lib package, but the existing ones + the planned ts lib, if any ts items are needed, a better, more detailed features set is needed in there.

The ts folder should be closed out with a refined set of docs, an architecture for the lib (it's not a single package, its a branch of our lib), all the folders needed, with the high level concepts, proper lib folder structuring/domains not mini-concepts, and the necessary functionality, features + alignment information so that when that folder is pushed in design and code once we finalize the stack for it, we wil lhave no doubt about the features/functionality needed and the details; assume a full package of effect libs, and whatever is the most advanced/objective best for any other gaps (pnpm-workspace we have), during planning use 2-3 opus agents to identify any gaps in existing package set, the planned packages we are going to add, and if any better/moder advanced alternatives exist for those or existing (if we have deployment/provisioning assume Pulumi, and that we hav consideration for workers, jobs, etc as needed for our libs app content, and general universal needs). During the closeout, we can use 1-2 opus agents to refine all of it to be better defined/higher resolution (without code, but details/concepts/mapping) to ensure it's fully ready for design docs in a future session.

STANDING MANDATES (every pass, every folder, every file — not reserved for new content):
- ENRICH ALWAYS: every existing file's capability suite is refined and pushed every pass, never left as-is. Telemetry, diagnostics, logging, and metrics are deepened to cover every new and existing capability (proper instrumentation, not silence); UI specs are pushed until no gap remains; every owner is graded at the world-class bar and a surface-level owner is a defect, not a baseline. New functionality always carries its observability with it. Enrichment is measured in the owner's CODE FENCE — new or deepened members/cases/fields/method-bodies/signatures — never in prose, a card, or a bare row; a page whose enrichment is only prose or a DENSITY_BAR/FEATURES/ROADMAP row is UNDONE, not done (the [FENCE_LAW]).
- ROOT-UP INTEGRATION: new capability lands as a row, case, column, fold, or policy value on the densest existing owner — woven into the existing body — or as a justified new design page only when a genuinely new owned concern warrants one. Never a loose arm, parallel rail, sibling surface, rename adapter, or tacked-on helper. A cross-cutting concern has exactly one owner consumed everywhere; consumers reference that owner and never re-mint it.
- ALIGN + REFINE: after authoring, every file is re-read and aligned cross-folder so vocabulary, seams, and owners stay consistent without coupling; drift is cleaned, duplication collapsed, sanctioned altitude-splits preserved. Alignment is a required pass, not an afterthought.
- APP-IDEATION -> SHORTFALL -> IMPLEMENT (close-out, and identified IN the pass): the close-out ideates the most complex, powerful apps the suite must build (speckle-class sync, rich multi-modal UI, optimized compute/persistence/threading, full remote/companion/sidecar/standalone modalities), then identifies — in the same pass — exactly where each folder AND the cross-cutting surface fall short of building them. Every shortfall is recorded as a concrete task/item on the owning folder's roadmap or tasklog AND implemented now in this campaign, wherever it lands on an existing owner or a justified new page — never deferred to a ledger or a future to-do. Only a genuinely new categorical concern that would warrant a new package is surfaced as such; everything else is absorbed into the existing owners or the planned TS branch.

- INHERIT-THE-POOL-FIRST: a session opens by capturing the entire prior-session closeout — the refreshed concept/feature/capability pool, the per-file and per-folder next-loop task lists, every folder + suite TASKLOG, and the FEATURES concept rows — as the binding, decision-complete starting work-list, and integrates it IN FULL before ideating anything new. The loop never restarts from a blank slate or from random ideas; new ideation happens only AFTER the inherited pool and every in-session finding is FINALIZED.
- RESEARCH-TO-ARTIFACT, FINALIZED IN-SESSION: every open question, spike, uncatalogued surface, version/compatibility unknown, and investigation item opens a parallel research-and-artifact track at session start — decompile/reflection, throwaway-harness probes against the real packages, native libraries, servers, and runtimes, package-capability deep-reads, and currency/compatibility dossiers — each writing a gitignored brief under `.artifacts/planning-briefs/`. The verified result folds back to FINALIZE the dependent fence and flip its `[STATE]` in the SAME session; admission-gated probes run inside the gate that admits the pin. Research generated in a session is implemented in that session, never punted; the residual SPIKE set at exit is exactly the tier-3 live-host/hardware probes. The only forward-deferred artifact is the closeout ideation pool for the next session.
- INTEGRATE-THEN-IDEATE ORDER: the session first drives every inherited-pool item, every gap-hunt finding, and every in-session research result to FINALIZED (the integration gate), and only then runs the closeout deep-read + app-driven ideation that emits the next session's refreshed pool. Integration completeness gates the close; a single unintegrated finding fails the turn.
- PLANNING FINDS AND MAPS EVERYTHING, FULLY: this campaign is PLANNING, never implementation. Planning finds, maps, and FINALIZES the entire work-set — every inherited item, every gap, every spike, every package, every capability — to the extreme bar, so the design corpus is decision-complete and copy-paste-ready. Implementation is a later session that transcribes; its own first step executes what planning fully mapped and discovers nothing new. The research reservoir, inherited-pool integration, gap-hunt, AND every working artifact a session builds — the briefs and registers under `.artifacts/planning-briefs/`, the integration work-list manifest, the spike proofs — are PLANNING outputs completed in planning to make the plan decision-complete; none is an implementation-time activity, and none is rediscovered or rebuilt in implementation. (Phase confusion to avoid: planning's research/find/manifest step is not implementation's first step; planning does the finding and the mapping, implementation only dispatches workflows that apply what was mapped.)
- BLEEDING-EDGE PACKAGES + CENTRALIZED, CLEAN BUILD: during planning ALWAYS hunt the most advanced, modern, strongest external library for every concern — replace a legacy package when a stronger modern equivalent exists (the Xbim-to-GeometryGym class of swap), at newest stable, verify-before-pin against the registry/source. Centralize everything: versions, targets, RIDs, and settings in `Directory.Packages.props` and `Directory.Build.props`; project files stay versionless and clean. `.csproj` edits ARE permitted — remove dead RIDs and off-platform native assets (a single-platform target carries no foreign-RID native asset), add references, or centralize a setting — keeping the root build clean and centralized.
- PACKAGE-AND-API LANE (standing, parallel): a dedicated subset of agents works ONLY the package manifest and each target folder's `.api/` catalogues — every admitted package is fully added and integrated into the owning project, and carries a full, deep `.api/` catalogue with zero member gaps (every load-bearing public surface decompile-verified, matching the construction of sibling `.api` pages). An admitted package with a thin, stale, or missing catalogue, or a manifest pin no project integrates, is a defect this lane closes.
- PROVISIONING ARCHITECTURE + KEYCHAIN WARNING: the tier-2 provisioning/infra architecture is full, feature-rich, powerful, and kept current — the complete server, extension, and native surface (PostgreSQL at newest major with its full extension set, the cache and object-store backends, the native runtimes) provisioned cleanly and ADDITIVELY (public images only, operator containers sacrosanct — never stopped or destroyed) so every server, native, and headless spike is reachable, then torn down to the pre-run state except where a later phase reuses it; the architecture is improved and updated every campaign, never left thin. [WARNING] NEVER run an interactive probe unattended: a macOS keychain read (`SecItem*` / Security.framework), DPAPI, a biometric or credential-store read, a signal-to-self (SIGHUP), or a `docker login` all raise an OS dialog that blocks and spams the operator with a password request that has no answer. Keychain, secret-store, DPAPI, biometric, and signal routes are TIER-3 INTERACTIVE — proven by tier-1 decompile member-shape ONLY, never an unattended live read.

ULTIMATE GOAL: All existing roadmap items, gaps in capability, and external packages + extensions are fully leveraged/integrated, all code is optimized, dense, taking advanctage of the planning phase to change anything as we need, all the main concepts from features are derived for ideas and implemented, as well as all subsequent pass roadmap + feature ideas, as well as anything agents identify as they go; never deferring to a ledger or to-do later, but implementing now, comprehensively, and ensuring no feature is absent. We want these packages all 100% feature finished, best-in class, OPTIMIZED performance, maintence considered as well, downstream agent devs will be able to use them to create extremely powerful apps easily, and we can easily extend/add functionality/features to any package easily due to proper foundational infrastructure approach. Design docs are implementation ready for future session to essentially copy-paste.