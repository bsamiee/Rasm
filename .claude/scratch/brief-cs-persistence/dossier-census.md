# [PERSISTENCE_CENSUS_DOSSIER]

Read-only census of `libs/csharp/Rasm.Persistence/.planning/` (18 pages, 6924 LOC md) + the 4 root governance docs + the cross-package seam graph. Corpus verdict: **world-class fence interiors, drifting governance perimeter.** Every one of the 18 fences is 9-10/10 — dense ADT collapse, seam-composed content addressing, one-materializer discipline, typed fault bands over the kernel `Rasm.Domain.Expected` federation base, full parameterization. The defects are NOT in the code fences; they are (1) dead IDEAS/TASKLOG carriers anchored to a collapsed pre-rebuild owner, (2) **severe bidirectional cross-package seam-ledger drift**, (3) two genuine capability gaps the sibling ledgers demand and the corpus does not provide, and (4) folder/coupling structure. The rebuild collapsed a ~28-page pre-rebuild corpus (`Sync/`, `Schema/`, `Query/{federation,pipeline,transaction}`, `Store/{quality,encryption,server,tenancy}`, `Version/snapshots`) into today's 18-page Element/Version/Query/Ingest/Store set — cleanly at the page level, but the ledger perimeter (own `ARCHITECTURE.md#SEAMS` + IDEAS/TASKLOG + the four sibling `ARCHITECTURE.md` files) still references the dead structure.

Codemap: `Element/{graph,codec,identity}` · `Version/{ledger,commits,timetravel,merge,provenance,retention,recovery}` · `Query/{lane,topology,columnar,cypher,cache}` · `Ingest/{tabular}` · `Store/{blobstore,provisioning}`.

---

## [00]-[SCOPE_DISPOSITION]

The five accumulated-census items the brief binds this survey to dispose:

**(1) Count-prefix wire law (PropertySet/QuantitySet content keys fork).** DISPOSED — transparent to Persistence. `RASM-COMPONENT-PARADIGM-DECISION.md:63` assigns the amendment to `resolve-residuals` on the **seam** `Node.ToCanonicalBytes` propertySet/quantitySet arms (`w.Ordinal(bag.Values.Count)`), and `:73` states the Persistence ripple is "ZERO structural — no seam shape, hasher, or canonical-byte change... the amendment IS the migration (planning-phase store, no persisted graph)". The store/decode side builds against the amended form BY COMPOSITION: `codec.md#CONTENT_ADDRESS:151-154` composes `seam ContentAddress.Of(node.ToCanonicalBytes(tolerance))`, `merge.md:160,173` `PropertyHash = ContentAddress.Of over Node.ToCanonicalBytes`, `timetravel.md:187-188` `ContentAddress.Of(n, Header.Tolerance)` — all pick up the seam's amended canonical writer with no Persistence edit. NO hardcoded digest forks: `codec.md:158` "the literal digest values stamp on the host-validation pass, never an un-run asserted value"; `commits.md#CRDT_WIRE` `ParityVector.Digest` is `Option<UInt128>` (design-pin gap, `Stamped()` on host validation). Note: `Element/Graph/delta.md:90-96` GraphDelta.ToCanonicalBytes ALREADY count-prefixes every collection (`w.Ordinal(...Count)`); the amendment closes only the `Node`-level bag arms. Persistence-owned preimages (`lane.md#ELEMENT_SET_ALGEBRA` `Canonical`, `cache.md` `ModelResultKey.Content`) are `NodeId`-key length-framed, orthogonal to the seam bag preimage — no interaction.

**(2) 25xx→27xx Fabrication fault re-band + persisted-boundary check (LEG-FABRICATION gated).** DISPOSED with a forward note. `RASM-COMPONENT-PARADIGM-DECISION.md:679` gates the re-band on a `[LEG-FABRICATION]` entry probe "that no 25xx Fabrication code crosses a persisted or wire boundary". Store-side finding: **the probe PASSES in the current corpus.** No Persistence page persists a foreign 25xx code — every stored receipt (`GraphReceipt`, `SyncApplyReceipt`, `SnapshotCatalogRow`, `RecoveryFact`, `SweepReceipt`, `BlobTransferReceipt`, `RetentionFact`, `StepFact`, `TabularFact`) carries only Persistence's OWN bands (5401, 771x, 825x, 828x, 829x, 830x, 834x, 835x, 836x, 837x). The one place a Fabrication fault code WOULD persist is `IDEAS.md [FABRICATION_PROGRAM_DURABLE_ROWS]` (the durable toolpath/nesting/post-dialect receipt schema) — and it is **QUEUED/unrealized** (no fence authored). Store-side disposition: the persisted-boundary check is satisfied today; the QUEUED fabrication-rows card must, when realized, decode the re-banded **2701-2710** codes, never 2501-2509. Record as a forward constraint on that card, not a current defect.

**(3) PERSISTENCE_GRAPH_REBUILD (Rasm.Element TASKLOG queued card).** DISPOSED — landed. `Rasm.Element/TASKLOG.md:20` `[PERSISTENCE_GRAPH_REBUILD]-[QUEUED]: full Rasm.Persistence rebuild over the ElementGraph + GraphDelta event body`. The realized 18-page corpus IS that rebuild: `graph.md#STORE_RAIL` `GraphStoreOp` over the `GraphDelta` event body (`GraphEvent.{GraphCreated,GraphRevised,GraphRetired}`), the inline `GraphProjection` folding `GraphDelta.ReplayOnto`, co-txn identity. The Element card is stale-open (should verify-close); ripple `[ELEMENT_GRAPH_PERSISTENCE]` (`Element/TASKLOG.md:23`) is satisfied. **Sibling-side stale card** — flag for the Element campaign, out of Persistence edit scope.

**(4) Materials-leg residual: Type-path classification stamping re-keys derived Type NodeIds.** OPEN — real forward migration pressure with a **store-side gap**. `Element/Graph/element.md:48` `NodeId.RootedType(typeSeed)` is the deterministic Type identity over a "representation-excluded seed" (`:40,46`). A next-campaign Type-seed amendment that excludes Classifications from the seed (as Representations already are) re-keys every derived Type `NodeId`. Store counterpart: `merge.md#STRUCTURAL_DIFF` `Reconcile` (`:272-284`) is the ONLY re-key aligner, and it correlates **exclusively on `Node.Object.ExternalId`** (the 1:1 IFC GlobalId) via `ExternalKey` — a node with `ExternalId: None` "stays NodeId-identified" (`:282-284`). A derived **Type** node is content-rooted (`RootedType`), typically carries no `ExternalId` — so a Type-seed re-key would appear to the diff as **delete-old-Type + insert-new-Type**, NOT a rename, because `Reconcile` cannot correlate it. Store-side disposition to plan: either (a) the next-campaign Type-seed change EXCLUDES Classifications (keeping the Type key stable, no store action), or (b) if it re-keys, Persistence needs a Type-correlation path in `Reconcile` (a stable Type-natural-key beyond `ExternalId`) OR an explicit expand→backfill→contract identity migration (`identity.md#IDENTITY_POLICY:189` already names this as "the only identity migration preserving foreign references and AS-OF cursor validity"). Verdict-candidate.

**(5) Element-leg residual: streaming/incremental `XxHash128 ContentAddress.OfGraph` kernel-owner boundary pressure.** OPEN — consumer-contract note, do not re-home. `codec.md#CONTENT_ADDRESS:157-158` binds `OfGraph` as a **seam** owner (`Rasm.Element/Projection/address`): "a Persistence-local NodeHash/GraphHash forwarding owner over those one-hop seam entries is the deleted form". Persistence composes `OfGraph` in hot paths that recompute the WHOLE-graph fold O(events × N): `timetravel.md#TIME_TRAVEL` `Scrub` computes `ContentAddress.OfGraph` for BOTH `Before` AND `After` of EVERY event in the window (`:236`), `Bisect` recomputes per probe (`:259`), the `Checkpoint` chain per anchor (`:143,265`), and `topology.md#GRAPH_TOPOLOGY` `TopologyView.Of` per snapshot (`:76`). Consumer contract to record: Persistence's Scrub/Bisect/checkpoint lanes need a **delta-composable incremental `OfGraph`** the SEAM owns — `Element/Graph/delta.md:55` already states a "DELTA_MONOID... address law" (`a.Merge(b).ReplayOnto(g) == b.ReplayOnto(a.ReplayOnto(g)) AND the address law`), the hook for an incremental `ContentAddress.OfGraph(prior, delta)`. **Waterfall-ripple target = Rasm.Element (`Projection/address` owner), NOT the geometry brief** (the geometry brief owns `Rasm/Geometry`, a disjoint concern). Persistence never re-homes the hasher; it composes the incremental seam entry when it lands.

---

## [01]-[PER_PAGE_VERDICTS]

Format: `page {verdict/10} | defects (file:line) | split/merge/move | charter-as-it-should-be`. Line refs are `.planning/<page>`.

**Element/graph.md — 10.** Zero material defects. `GraphStoreOp` `[Union]` rail through the generated total `Switch` (`:257-266`), co-transactional `IdentityStore.Stamp`+event+inline-projection in ONE `SaveChangesAsync` (`:286-294`), one `GraphDelta.ReplayOnto` materializer shared with AS-OF (`:196-204`), the `Received` fold owning receipt+absence once (`:311-314`), provider-fault conversion at one boundary (`:322-328`). Charter: the ElementGraph store-of-record over Marten, stream-per-model, GraphDelta event body, inline read-your-writes projection, one txn owner. Realized exactly.

**Element/codec.md — 10.** `SnapshotCodec`×`CompressionPolicy`×`HashPolicy` delegate-row smart-enums, `SnapshotHeader` fixed-72-byte trust boundary with the 8-tier `RejectTier` ladder gating before any decoder (`:350-361`), FastCDC content chunking, `ContentAddress` composed from the seam (no second hasher, `:151-158`). The `messagepack`-seam-graph phantom is explicitly deleted (`:22`). Charter: the one codec/compression/hash/seal/chunk axis + the seam-composed content address. Flawless.

**Element/identity.md — 9.5.** Largest page (653 LOC), justified: the unified `KmsProvider` axis serves BOTH `SigningKeyring` (Sign/Verify) AND `EnvelopeKeyring` (Mint/Unwrap/Rewrap/Probe) so the former `Store/encryption` page correctly folded here (`TASKLOG [ENCRYPTION_PAGE_AUTHOR]-COMPLETE`). `ElementIdentity` co-txn Marten doc, `IdentityPolicy` big-endian key axis, `Grant`/`GrantSet` set-algebra (not `[Flags]`), `Authority.Admit/Attest/Verify/Wrap/Unwrap/Rewrap`, `SchemaVerdict` two-DDL-owner boot fold. Only note: density is at the ceiling — a future `#KEY_ENVELOPE` growth would pressure a split, but today the one-axis-two-keyrings collapse is correct, not over-stuffed. Charter: the one relational identity tier + key axis + object-ACL + KMS keyrings + boot verdict.

**Store/blobstore.md — 9.5.** Four-provider `ObjectClient` `[Union]` with `ObjectIo.For` dispatch, `ObjectLeg` nine-delegate per-provider row (one transfer engine, `:340-349`), write-once `412`-noop seal, `ObjectEncryption`/`ObjectLock` `Apply*` per request-type (`:81-146`), `BLOB_GC` routes through the ONE `RetentionSweep` (`:605-612`), write-blob-first + in-flight fence. Structural note (see [02]): the `retention ↔ blobstore` `StorageTier`/`RetentionSweep` bidirectional page reference. Charter: content-keyed geometry object store, provider-agnostic, GC via the one retention executor.

**Ingest/tabular.md — 9.** One `TabularSource` over MiniExcel, `TabularSpec` `[ComplexValueObject]` discriminant (typed/dynamic/reader/probe/egress all dispatch on it, `:15`), `Origin` `[Union]` folding path-vs-stream, both legs fold faults through one `TabularFault.Lift`, per-column redaction before `SaveAs`. **Folder note:** `Ingest/` is a **one-page folder** — the same one-file-folder shape the geometry brief flags as a standing violation (`Parametric/`). Either `Ingest/tabular` folds up (a top-level page or into a broader `Ingest` with a sibling), or a second ingest concern is planned. Charter: the one tabular ingress/egress codec, element-agnostic (app owns tabular→element).

**Version/ledger.md — 9.5.** `OpLogEntry` changefeed PROJECTED from Marten events (`ChangefeedSubscription:118-124`), `ColumnFamily`×`MergeStance` lane rows, `SyncMerge.Apply` exact-conservation fold (each entry one counter, `:324-338`), `FirstWriter` inverse comparison correctly handled (`:304-312`), `SyncTransports` 3-case + `SyncFlow`, `Presence`/`Awareness` lossy lane. `TraceSlot` total hex admission (`:81-89`). Charter: the changefeed projection + merge law + sync transports + presence, all off the one Marten stream.

**Version/commits.md — 9.5.** `CommitNode` content-addressed DAG (canonical `(parents,opkeys,cell)` preimage, `CommitMessage` excluded, `:86-98`), `MergeBase` maximal antichain (`:108-114`), full CRDT algebra (6 types, join-semilattice `Merge`/`Apply`/`Compact`, `:200-251`), `CrdtOpWire` `[MessagePack.Union]` amendment, `ContentParityCorpus` one-directional `Contribute` (refuses `MintedHere`, `:398-401`). The `Rank` mutable-work-list is a declared EXPRESSION_SPINE kernel exemption. Charter: the content-addressed history + convergent CRDT + the flagship wire + the parity corpus.

**Version/retention.md — 9.5.** `RetentionClass` 6-row closed axis (5 decisions each), `RetentionCeiling` seam-local rank table (`DataClassification` carries no ordinal, `:103-115`), pure state-threaded `Sweep` fold with declared order holds→cool→age→count→size (`:258-276`), full-history reachability `Mark` over every AS-OF cut, `Cool` cold-tiering, ONE `Execute` mutation surface. Note the blobstore coupling ([02]). Charter: the one classification+retention+GC owner every lane routes through.

**Version/timetravel.md — 9.5.** AS-OF `Reconstruct`/`Diff`/`Blame`/`Scrub`/`Bisect`/`BranchFromPast` over `AggregateStreamAsync`, one `GraphDelta.ReplayOnto` materializer, `Checkpoint` reproducibility chain (non-crypto, defers authenticity to provenance, `:80-84`), two-axis `RangeDiff` (node `Inequalities` + edge content-set-diff, `:178-211`). SCOPE-5 OfGraph hot-path pressure lives here (`Scrub`/`Bisect`). Charter: the AS-OF read layer projecting from Marten events, one materializer, content-addressed checkpoint chain.

**Version/merge.md — 9.5.** `Reconcile` GlobalId re-ingest align BEFORE diff (`:272-297`), `GraphNode` forest off `Relationship.Compose`, two-axis merge (Object forest + content-node axis, so a single-side PropertySet edit APPLIES, `:170-173`), typed `MergeConflict` family, member-granular RFC 6902 `Patch`, `ContainmentCycle` detection. SCOPE-4 Type-rekey gap lives here (`Reconcile` ExternalId-only). Charter: the structural three-way merge + re-ingest aligner, both axes, typed conflicts, one conflict receipt vocabulary.

**Version/provenance.md — 9.5.** W3C-PROV-O exact typing (`WasGeneratedBy`/`WasAssociatedWith`/`WasAttributedTo`/`WasRevisionOf` correctly distinguished, `:170-200`), `ProvNode.Of` mints the right PROV class (`:144-145`), `AttestedLedger` Merkle audit with independent `digestOf` recomputation making `Unauthored` reachable (`:364-380`), `Authority.Verify`-composed. Charter: the causal-lineage PROV-O graph + the KMS-attested tamper-evidence ledger, the one authenticity authority.

**Version/recovery.md — 9.5.** `RecoveryRoute` 3-substrate axis, real `RecoveryPoint(Timeline,Lsn)` via `IdentifySystem` (never wall-clock, `:56-66`), `PointInTimeRestore` 6-step ranked choreography with `Snapshots.Verify` in reverse, content-identity commit point (`ReAttest` AS-OF `OfGraph` compare, `:327-349`), proven head-caught-up `WaitForNonStaleData`. Charter: the verified backup+PITR that re-establishes content identity, objectives read settled from AppHost.

**Query/lane.md — 9.5.** `ReadRouter` consistency-demand routing (every `QueryShape` bit READ, `:75-80`), `ElementSet` content-addressed selection currency with length-framed collision-free receipt, `SetPredicate`/`SetExpr` typed algebra (`SpatialOp`/`JsonComparison` vocabularies), `FusionRank` n-ary RRF, `VectorCodebook` PQ training (Compute→Persistence one-way). `StalenessWatermark` measured off verified Marten members (sequence gap, not the recording-clock `Timestamp` confound, `:99-115`). VECTOR_CODEBOOK↔Compute seam is wired-undeclared ([03]). Charter: the read-routing + selection-currency + fusion + PQ-codebook lane.

**Query/topology.md — 9.5.** In-process QuikGraph `TypedEdge` kind-filtered views over the seam-frozen incidence (never re-derived), `EdgeFilter` neutral vocabulary, `Traversals` full `AlgorithmExtensions` facade, `Rooted`/`Paired` absent-root typed faults, symmetric connection adjacency, `Spatial` vs narrow `Containment` filter correctly split. `Query/topology ← Geometry/Spatial GeometryHash` seam is declared-unwired ([03]). Charter: the DEFAULT synchronous authoritative topology owner, AGE demoted.

**Query/columnar.md — 9.5.** One refcounted `ColumnarSession` DuckDB anchor + `Duplicate()`, `ColumnarProfile` posture+`INSTALL`/`LOAD` roster, `Secret`/`Mount`/`ArrowStream` ADBC bridge, `ArtifactEgress` `COPY` rail with footer stamp, co-transactional `BimOpenSchemaProjection:FlatTableProjection` + native `ParquetSharp.Arrow`. Charter: the one DuckDB analytical engine + artifact egress + co-txn BIM columnar egress.

**Query/cypher.md — 9.** AGE + pgrouting OPTIONAL self-hosted lane, `GraphQuery` ONE `[Union]` collapsing every openCypher AND pgrouting verb under a total `Switch`, `H3Cell` node space (bit-exact with `pocketken.H3`), server-side `format('%L',...)` injection-safe TSP matrix, `CleaveKind.NodeValued` keeping cut-EDGE ids out of cell space. Densest cross-package composition in the corpus. Charter: the optional self-hosted graph analytical lane, demoted beneath QuikGraph, driven through raw Npgsql.

**Query/cache.md — 9.5.** `ArtifactIndexRow` content-keyed, `ModelResultIndex` the ONE recency-horizon owner (gate folded into `Lookup`, `:120-125`), `BenchmarkRow` fingerprint-gated claim, `L2_CONTRIBUTION` `IBufferDistributedCache` buffer-contract store + one `IHybridCacheSerializerFactory` + `TenantId` partition. `Query/cache ⇄ AppHost CACHE_PORT` L2 seam is wired-undeclared ([03]). Charter: the durable reuse substrate (artifact/result/benchmark indexes) + the L2 cache contribution.

**Store/provisioning.md — 9.5.** Verification-first `ServerExtension` authoritative roster (superset of README, base-type/preload/AM/standalone `ExtensionAdmission` gates, `:118-150`), `FailureRank.Absorb` delegate-as-policy, one 6-command `CreateBatch` verify fold, `EmbeddedRitual`/`EmbeddedStore.Open` residency-split fold, `EngineOps` raw-handle capsule (WAL checkpoint/snapshot pin/paged backup/blob lane). Never `ALTER SYSTEM`; emits repair artifacts. Charter: the verification-first PG server tier + the embedded SQLite floor, closed two-engine sweep.

---

## [02]-[CROSS_CUTTING]

**Dead typed carriers (governance, not fences).** The IDEAS/TASKLOG OPEN cards anchor to a **federation owner the rebuild collapsed**: `Query/Federation.cs`, `Query/federation#ENTITY_GRAPH`, `SourceKind`, `FederatedEntity`/`EntityGraph`, `FederatedPlan`/`PlanNode`, `SetExpr` federation planner. Grep proof: `rg federation|FederatedEntity|SourceKind|Query/federation|EntityGraph|FederatedPlan|Substrait .planning/` returns ZERO structural hits — every "federation" is the fault-band base or `codec.md:162` "snapshot/chunk/diff/federation share it" prose. Yet three cards claim the Persistence half "is realized" pointing at these phantoms:
- `IDEAS.md [REUSE_WIRE]-[BLOCKED]:34-39` — `Query/Federation.cs source-agnostic federated entity graph (SourceKind axis)`, "The Persistence half — the source-agnostic FederatedEntity/EntityGraph ready to fold... — is realized". **False: no such owner exists.** The concern folded into `lane.md#ELEMENT_SET_ALGEBRA` (ElementSet) + `cache.md` (reuse index).
- `IDEAS.md [SUBSTRAIT_FEDERATION_SEAM]-[BLOCKED]:41-47` — `Query/Federation.cs... FederatedPlan/PlanNode cross-engine planner`, "The Persistence-side lowering target (ElementSet/SetExpr algebra + FederatedPlan) is realized". **False: `FederatedPlan`/`PlanNode` do not exist.**
- `TASKLOG.md [ARTIFACT_CONTENT_KEY_FEDERATION]-[BLOCKED]:20-26` — `Query/federation#ENTITY_GRAPH SourceKind.SignedArtifact decoding the wire-projected content-key binding — is realized`. **False.**

These are the single largest own-side governance defect: three OPEN cards asserting realized capability against a deleted owner. Ruling: re-anchor to the real owners (`lane#ELEMENT_SET_ALGEBRA`, `cache#ARTIFACT_BLOB_INDEX`/`MODEL_RESULT_INDEX`, `commits#CRDT_WIRE ContentParityCorpus.Contribute(ParitySlot.ElementSet)`) or re-scope the cards — the cross-runtime reuse ledger that federation named is genuinely NOT owned by a single "federated entity graph" any more; it is the `ElementSet` receipt currency + the artifact/result indexes + the parity corpus. This is a campaign-defining ruling: decide whether Persistence RE-INTRODUCES a first-class federation/`SourceKind` owner (the cards' intent) or FORMALLY retires it into the ElementSet+cache substrate (the rebuild's realized shape). `libs/csharp/.api/`-mining candidate: `FlowtideDotNet.Substrait` is pinned+catalogued (`.csproj:79`, `ARCHITECTURE.md:95` "backing the Query federation/pipeline rail") but composed by ZERO fences — a dead admitted package whose only named consumer is the collapsed federation owner.

**Unmined admitted capability (package-vs-fence).** `FlowtideDotNet.Substrait` (pinned, catalogued as the federation/pipeline plan IR) — zero fence consumers; either wire the Substrait decode into a re-introduced federation/plan owner or drop the pin. Every other pinned package IS composed by a fence (verified: DuckDB/Marten/Npgsql/pgvector/H3/Arrow/ParquetSharp/BimOpenSchema/the four object clients/the four KV+columnar backends/Speckle/Kafka/NATS/RabbitMQ/Pulsar/ClickHouse/ScyllaDB/Qdrant/DeltaLake all appear). Note `ClickHouse.Driver`/`ScyllaDBCSharpDriver`/`Qdrant.Client`/`DeltaLake.Net`/`rocksdb`/`LightningDB` (`ARCHITECTURE.md:49-57`, `.csproj:22,50,49,27,58,57`) are declared store-backends but **composed by no fence** — the `StoreProfile` sweep is explicitly closed to PostgreSQL+SQLite (`provisioning.md:20,378`), so these six scale-out backends are admitted-but-unrealized roster entries. Not necessarily wrong (forward roster), but a coverage gap: the roster promises ClickHouse/Scylla/Qdrant/Delta/RocksDB/LMDB lanes the corpus provides no owner for. Verdict-candidate.

**Duplication / concern-mixing — NONE material.** The corpus is rigorously de-duplicated: one `ContentAddress` (seam), one `Hlc`, one `OpLogEntry` changefeed, one `GraphDelta.ReplayOnto` materializer, one `RetentionSweep` executor, one `ModelResultIndex` horizon, one `ElementSet`. Cross-references are one-directional and port-injected where a cycle would form (`lane#ELEMENT_SET_ALGEBRA` `SetResolve.Expand` threads the topology incidence as a delegate, `:206-209`, keeping the reachability owner in `topology`). The `KMS envelope`, `object-SSE`, `redaction`, `transaction/2PC`, `quality`, `pipeline`, `egress`, `annotation`, `schedule`, `tenancy` concerns all correctly FOLDED into their real owners (TASKLOG closed records) rather than proliferating pages.

**Hardcoding-vs-generator — CLEAN.** Every roster is seed DATA over a polymorphic owner: `RetentionClass` 6 rows, `ServerExtension` 20 rows, `SnapshotCodec`/`CompressionPolicy`/`HashPolicy` delegate rows, `ObjectStore` 4 providers, `ColumnarExtension` 15 rows, `VectorMetric`/`EmbeddingArity`/`RetrievalBranch`, `CrdtOp`/`CrdtField` 6+10, `ProvRelation` 11 W3C terms, `GraphQuery` verb union, `TopologyQuery` 13 cases. No enumerated-instance-where-a-generator-belongs defect. Growth clauses uniformly state "a new X is one row/case".

**Page-level cycle: `Version/retention ↔ Store/blobstore`.** The corpus's one bidirectional page dependency. `blobstore.md` imports `RetentionClass`/`RetentionFact`/`SweepReceipt`/`Hold`/`Reachability`/`RetentionSweep` (`:528`, GC routes through retention's executor). `retention.md` imports `StorageTier` (`:100,121-124` `RetentionCeiling.Colder`/`Demote` maps the blobstore-owned tier vocabulary, `RetentionFact.Tier`, `SweepVerdict.Cool`). Intra-assembly-legal (both in `Rasm.Persistence`), and the design INTENDS it (`retention.md:117` "a retention POLICY over the blobstore-owned StorageTier vocabulary"). But it is the tightest coupling in the corpus and the only true page cycle. Structural ruling candidate: either accept it (document `StorageTier` as the shared pivot) or extract `StorageTier` + the tier ladder into a neutral `Store/tier` owner both compose downward — the latter breaks the cycle and gives the six unrealized scale-out backends ([above]) a tier home. Low-severity but campaign-relevant if the Store folder grows.

**Folder architecture.** Sound overall (5 sub-domains mirroring the eventual source tree), TWO notes: (1) `Ingest/` is a **one-page folder** (`tabular.md` only) — the one-file-folder shape the strata law and the geometry brief both reject; fold it up or plan a second ingest concern. (2) `Store/` (2 pages: blobstore, provisioning) is the growth axis for the six unrealized backends + the fabrication-durable-rows QUEUED card + any `Store/tier` extraction — well-positioned. `Version/` (7) and `Query/` (5) are correctly the deepest folders. No flat sprawl.

---

## [03]-[SEAM_LEDGER_DRIFT] (the campaign-defining structural finding)

Diffed the real fence graph against `ARCHITECTURE.md#SEAMS` (own ledger, `:40-64`) BOTH directions, then against the four sibling `ARCHITECTURE.md#SEAMS` ledgers (Element/Compute/Bim/AppHost). Element↔Persistence is CLEAN and bidirectionally consistent (`Element/ARCHITECTURE.md:61-64` matches the Persistence fences exactly). The drift is with Compute/Bim/AppHost and within the own ledger.

**A. Own-ledger DECLARED-UNWIRED (row in `ARCHITECTURE.md#SEAMS`, no fence composes it):**
- `:55 Query/topology ← csharp:Rasm/Geometry/Spatial [CONTENT_KEY]: adjacency-derived GeometryHash`. `topology.md` composes `ContentAddress.OfGraph` from the SEAM (`Element/Projection/address`), never a `Geometry/Spatial` adjacency GeometryHash — no `Rasm/Geometry` reference exists in the page. **Also flagged from the geometry side** (`RASM-CS-GEOMETRY-BRIEF.md:140` E12 "Seam-ledger drift — reconciliation→Persistence three anchors (reconciliation.md:2 / ARCH:45 / Rasm.Persistence/ARCHITECTURE.md:55)"). Two-sided confirmed drift.
- `:50 Version/timetravel ← python:data/gridded/virtual [CONTENT_KEY]: icechunk as-of snapshot identity`. `timetravel.md` has no `AsOfKey`/icechunk. `TASKLOG [ICECHUNK_ASOF_CONTENT_KEY]-COMPLETE:48` claims it landed at `Version/snapshots#SNAPSHOT_PROTOCOL AsOfKey` — a **page that no longer exists** (snapshots collapsed into timetravel). Declared-unwired + stale-closed-card.
- `:57 Query/lane ⇄ python:data/tabular/query [WIRE]: ElementSet receipt currency + Substrait portable plan`. The ElementSet currency half IS wired (`lane#ELEMENT_SET_ALGEBRA` + `commits ContentParityCorpus.Contribute`). The **Substrait portable plan half is unwired** (it was the dead federation owner's, [02]).

**B. Own-ledger WIRED-UNDECLARED (fence composes a cross-package seam the ledger omits):**
- `Query/lane#VECTOR_CODEBOOK → csharp:Rasm.Compute/Model/embedding [CODEBOOK]: ProductCodebook Compute encodes against`. `lane.md:388` "Compute imports it by its Rasm.Persistence (project) reference... the dependency runs Compute → Persistence". Compute DECLARES it (`Compute/ARCHITECTURE.md:99 Model/embedding ⇄ Rasm.Persistence/Query/lane [CONTENT_KEY]: EmbeddingVector.ContentKey (modelKey×encoding×codebook Id...)`). **Persistence `ARCHITECTURE.md#SEAMS` has NO row for it.** Add it.
- `Query/cache#L2_CONTRIBUTION ⇄ csharp:Rasm.AppHost/Runtime/resources#CACHE_PORT [PORT]: CacheL2Store IBufferDistributedCache + CacheCodecFactory + TenantId L2 partition`. `cache.md#L2_CONTRIBUTION` composes it; AppHost DECLARES it (`AppHost/ARCHITECTURE.md:69 Runtime → Rasm.Persistence/Query/cache [PORT]: TenantId RLS + cache L2 partition`). Persistence ledger mentions "the cache port" only in BOUNDARIES prose (`:100`), **no SEAMS row.** Add it.
- (Minor) `Version/ledger#PRESENCE ← csharp:Rasm.AppHost DrainSurface [PORT]: awareness DropOldest lane` — `ledger.md:468` composes `spec.Open<AwarenessBeat>`; the ledger's AppHost port row (`:49`) names only HLC/TenantContext/TraceSlot.

**C. SIBLING-side STALE seams pointing at collapsed Persistence pages (waterfall-ripple; sibling-interior, out of Persistence edit scope — flag for the owning campaigns).** The rebuild collapsed `Sync/`, `Query/{federation,pipeline,transaction}`, `Store/quality` but the sibling ledgers still target them:
- `Compute/ARCHITECTURE.md`: `:111 Runtime ← Rasm.Persistence/Sync [PROJECTION]`, `:115 Runtime/codecs ⇄ Query/pipeline [PORT]`, `:116 Compute → Store/quality [SHAPE]`, `:119 Runtime/channels → Sync [WIRE]`. FOUR stale (Sync/pipeline/quality gone → now `Version/ledger`, folded, folded).
- `Bim/ARCHITECTURE.md`: `:90 Review/diff → Query/federation [CONTENT_KEY]: AuditEntry chained ElementChange` (the SAME dead federation), `:92 Exchange/wire ⇄ Sync [TRANSPORT]`, `:94 Model → Store/quality [SHAPE]`, `:100 coordination ⇄ Sync/annotation [WIRE]`, `:101 schedule ⇄ Sync/schedule [WIRE]`, `:103 Exchange/import ⇄ Sync [TRANSPORT]: Speckle`. SIX stale — and `Sync/annotation`+`Sync/schedule` were RELOCATED INTO Bim (`TASKLOG [ANNOTATION_RELOCATE_TO_BIM]`/`[SCHEDULE_RELOCATE_TO_BIM]-COMPLETE`), so those two seams are doubly wrong (Bim now owns the concern).
- `AppHost/ARCHITECTURE.md`: `:71 Runtime → Query/transaction [PORT]: drain 2PC in-doubt set`, `:73 Runtime → Sync/egress [PORT]: keyed OutboundHop egress`. TWO stale (`Query/transaction` folded into `StoreOp` per closed card; `Sync/egress` folded into `ledger#SYNC_TRANSPORTS`).

That is **TWELVE stale sibling→Persistence seams** across three sibling ledgers, all consequences of the un-rippled Persistence rebuild. Ruling: the census records them; the owning-campaign briefs (Compute/Bim/AppHost) must re-point them to the collapsed owners' real homes. This is the dominant cross-package hygiene debt.

**D. SIBLING-declared seams with NO Persistence counterpart owner (genuine capability gap, NOT stale — the concern is real and unowned).** `AppHost/ARCHITECTURE.md` declares Persistence as the backing store for FOUR coordination concerns the corpus provides no owner for:
- `:76 Agent/capability ⇄ Rasm.Persistence [PORT]: fenced per-tenant Budget debit (ONE_FENCED_LEASE_STORE)`
- `:77 Runtime/orchestration ⇄ Rasm.Persistence [PORT]: workflow step-state CAS (ONE_FENCED_LEASE_STORE)`
- `:78 Wire/outbox ⇄ Rasm.Persistence [PORT]: transactional outbox same-tx (ONE_OUTBOX_EGRESS_SPINE)`
- `:79 Wire/Coordination ⇄ Rasm.Persistence [PORT]: CAS + fenced-lease + membership backing (ONE_FENCED_LEASE_STORE)`

The Persistence corpus has optimistic stream-version concurrency (`graph.md` `Append(stream, expectedVersion)`) and the identity tier, but **no `ONE_FENCED_LEASE_STORE` owner** (fenced-lease + CAS + membership) and **no named transactional-outbox owner**. The outbox is ARGUABLY satisfied by the changefeed-off-events design (events ARE the outbox, drained by `ChangefeedSubscription`) — but it is not NAMED as the `ONE_OUTBOX_EGRESS_SPINE`, so the AppHost law dangles. The fenced-lease/CAS/membership store is a real missing capability: AppHost's per-tenant Budget debit, workflow step-state CAS, and Coordination membership all expect a Persistence-backed fenced-lease store that does not exist. **This is the single largest coverage gap** — plan a `Store/coordination` (or `Element/identity`-extension) owner for the fenced-lease/CAS/membership/outbox spine, or formally re-home these to the AppHost interior with Persistence supplying only the Marten CAS primitive. Top verdict-candidate.

**E. Page-level cycles (intra-package).** One true cycle: `retention ↔ blobstore` ([02]). All other cross-references are one-directional or port-broken (`lane→topology` via `SetResolve.Expand` port; `identity→blobstore` one-way KEY_ENVELOPE consume; `commits←lane` one-way `Contribute`). No cycle among the Version engine pages (they fan down to Element/codec + Element/graph + commits' Hlc). Acyclic except the one noted.

---

## [04]-[UPSTREAM_BRIEF_CONSUMPTION] (RASM-CS-GEOMETRY-BRIEF.md)

The geometry brief owns `Rasm/Geometry` (the KERNEL robust-core), a stratum BELOW Persistence. Persistence depends UP on `Rasm` (kernel content-hash) + `Rasm.Element` (seam) only; it references `Rasm/Geometry` in exactly ONE place — the declared-unwired `:55 Query/topology ← Rasm/Geometry/Spatial GeometryHash` seam.

**Capabilities Persistence COULD consume instead of hand-rolling (opportunities):**
1. **The reconciliation/naming plane → the persistence diff.** Geometry brief TELOS (`:24`): "the naming plane feeds every persistence diff". Geometry's `reconciliation.md` owns stable `TopoSignature`/`NodeLinkProjection`/`CLASH_GOLDEN` content-addressing. Persistence's `merge.md#STRUCTURAL_DIFF` `GeometryDigest` (`:305-315`) currently folds the Object's `Representations.ByIdentifier` **kernel-minted representation content keys** — sound, but it does NOT compose a Geometry-derived adjacency/topo-signature. If the E12 `Geometry/Spatial GeometryHash` seam is wired, `merge`/`timetravel` diff could key topology change on a Geometry stable signature rather than (or beside) the representation-hash digest. Consumer-contract note, not a demand.
2. **`ContentAddress.OfGraph` incremental (SCOPE-5)** — but this owner is `Rasm.Element/Projection/address`, NOT the geometry brief's `Rasm/Geometry`. The geometry brief does not carry it; the waterfall-ripple target is the Element brief.

**Assumptions the geometry brief could INVALIDATE — NONE.** (a) The brief PURGES geometry packages (LibTess/geometry3Sharp/SharpVoronoiLib/Triangle/MIConvexHull) and admits Manifold — Persistence references none of them. (b) The brief RESTRUCTURES `Rasm/Geometry` namespaces + the `Spatial` owner — Persistence's only touchpoint is the unwired `Geometry/Spatial GeometryHash` seam, so a restructure breaks no Persistence fence. (c) The brief `ROSTER_RECONCILIATION` (`:53`) CORRECTS `api-hashing.md` to `XxHash128.HashToUInt128` seed-zero — this ALIGNS with Persistence's assumption (`codec.md#CONTENT_ADDRESS` composes exactly this), no conflict; it FIXES a shared-tier catalog divergence Persistence silently relies on. Net: the geometry brief is upstream-neutral to Persistence except the one shared seam-ledger drift both sides already flag.

**Waterfall-ripple edits performed:** NONE required into the geometry brief — the two Persistence pressures (SCOPE-5 incremental `OfGraph`, SCOPE-4 Type-rekey) target `Rasm.Element` (`Projection/address`, `Graph/element`), which is not the geometry brief's scope. Recorded here as cross-package pressures on the Element campaign; no surgical edit to `RASM-CS-GEOMETRY-BRIEF.md` is warranted (its E12 already carries the one shared seam).

---

## [05]-[VERDICT_CANDIDATES]

Campaign-defining structural rulings, most-severe first, each with evidence:

**VC1 — Retire-or-reintroduce the federation owner; re-anchor the 3 dead IDEAS/TASKLOG cards.** Evidence: `rg federation|FederatedEntity|SourceKind|FederatedPlan .planning/` = zero structural hits, yet `IDEAS.md:34-47` + `TASKLOG.md:20-26` claim `Query/Federation.cs`/`Query/federation#ENTITY_GRAPH`/`FederatedEntity`/`FederatedPlan` "is realized". The rebuild collapsed federation into `lane#ELEMENT_SET_ALGEBRA` + `cache` indexes + `commits ContentParityCorpus`. `FlowtideDotNet.Substrait` (pinned, `ARCHITECTURE.md:95`) is composed by zero fences. Ruling required: FORMALLY retire federation into the ElementSet+cache substrate (re-anchor the cards, drop the Substrait pin) OR re-introduce a first-class `Query/federation`/`SourceKind`/Substrait-plan owner (the cards' intent). The three BLOCKED cards cannot stay asserting realized phantoms.

**VC2 — Fill the AppHost `ONE_FENCED_LEASE_STORE`/outbox coverage gap.** Evidence: `AppHost/ARCHITECTURE.md:76-79` declares Persistence as the backing store for per-tenant Budget-debit fenced-lease, workflow step-state CAS, transactional-outbox same-tx, and Coordination membership — FOUR `ONE_FENCED_LEASE_STORE`/`ONE_OUTBOX_EGRESS_SPINE` seams with NO Persistence owner. The corpus has stream-version optimism + the identity tier but no fenced-lease/CAS/membership/outbox owner. Ruling: plan a `Store/coordination` (fenced-lease + CAS + membership + named transactional-outbox spine over Marten) owner, or formally re-home to the AppHost interior with Persistence exposing only the Marten CAS primitive. Largest true capability gap.

**VC3 — Reconcile the bidirectional seam-ledger drift; add the 2 wired-undeclared rows, close the 3 declared-unwired rows.** Evidence: own `ARCHITECTURE.md#SEAMS` has 3 declared-unwired (`:50` icechunk, `:55` Geometry/Spatial GeometryHash, `:57` Substrait-half) and 2 wired-undeclared (VECTOR_CODEBOOK↔Compute/Model/embedding `lane.md:388`+`Compute/ARCH:99`; Query/cache↔AppHost CACHE_PORT `cache.md#L2_CONTRIBUTION`+`AppHost/ARCH:69`). Twelve SIBLING seams (Compute×4, Bim×6, AppHost×2) point at collapsed Persistence pages (`Sync/*`, `Query/{federation,pipeline,transaction}`, `Store/quality`). Ruling: correct the own ledger in-campaign; flag the 12 sibling seams for the Compute/Bim/AppHost campaigns (waterfall-ripple, out of Persistence edit scope).

**VC4 — Close the SCOPE-4 Type-rekey store gap in `merge.md Reconcile`.** Evidence: `merge.md:272-284` `Reconcile` correlates ONLY on `Node.Object.ExternalId`; a `RootedType` node (`Element/element.md:48`) carries no `ExternalId` and "stays NodeId-identified". A next-campaign Type-seed amendment that re-keys derived Type NodeIds would read as delete+insert, not rename. Ruling: decide the store counterpart NOW — either the Type-seed amendment excludes Classifications (Type key stable, no store action) OR `Reconcile` gains a stable Type-correlation path / an expand-backfill-contract migration (`identity.md#IDENTITY_POLICY:189`). Forward migration pressure the brief mandates planning here.

**VC5 — Record the SCOPE-5 incremental-`OfGraph` consumer contract (Element-brief ripple).** Evidence: `timetravel.md:236` `Scrub` computes `ContentAddress.OfGraph(Before)+OfGraph(After)` per event O(events×N); `Bisect:259` per probe; `codec.md:157` forbids a Persistence-local graph hasher. `Element/Graph/delta.md:55` "DELTA_MONOID address law" is the hook. Ruling: record the delta-composable incremental-`OfGraph` consumer contract as a pressure on the `Rasm.Element` `Projection/address` owner (waterfall-ripple to the Element brief, NOT geometry); Persistence composes the incremental seam entry when it lands, never re-homes.

**VC6 — Dispose the six unrealized scale-out store backends + the Ingest one-file folder.** Evidence: `ClickHouse.Driver`/`ScyllaDBCSharpDriver`/`Qdrant.Client`/`DeltaLake.Net`/`rocksdb`/`LightningDB` (`ARCHITECTURE.md:49-57`) are admitted but composed by zero fences (the `StoreProfile` sweep is closed to PG+SQLite, `provisioning.md:378`); `Ingest/` is a one-page folder (the strata/geometry-brief one-file-folder violation). Ruling: either plan owners for the scale-out backends (a `Store/backends` axis beside the closed relational sweep) + a second `Ingest` concern, or drop the unrealized backend pins and fold `Ingest/tabular` up. Roster-vs-realization coherence.

**VC7 — Resolve the `retention ↔ blobstore` `StorageTier` page cycle (low-severity, structural).** Evidence: `blobstore.md:528` imports the retention GC surface; `retention.md:100,121-124` imports the blobstore-owned `StorageTier`. Intra-assembly-legal, design-intended (`retention.md:117`), but the one true page cycle. Ruling: accept + document the `StorageTier` pivot, OR extract `StorageTier`+tier-ladder into a neutral `Store/tier` owner both compose downward (also homes the VC6 scale-out tiers). Trigger only if the Store folder grows.

**VC8 — Verify-close the stale-open governance cards.** Evidence: `Element/TASKLOG.md:20 [PERSISTENCE_GRAPH_REBUILD]-[QUEUED]` is realized (the 18-page corpus) but still open; the Persistence federation cards (VC1) assert realized phantoms. Ruling: the rebuild landed — mark the Element rebuild card COMPLETE (Element-campaign action) and reconcile the Persistence card pool to the realized owner set. Governance-hygiene, pairs with VC1/VC3.
