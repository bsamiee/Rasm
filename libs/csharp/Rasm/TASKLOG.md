# [RASM_TASKLOG]

The kernel's open and closed work, distilled from the ideas and the design-page RESEARCH residuals. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]` open, `[UPSTREAM-BLOCKED]`/`[HOST-PROBE-DEFERRED]` blocked on an external dependency, `[COMPLETE]`/`[DROPPED]` closed — plus the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations. `[1]-[OPEN]` carries live work; `[2]-[CLOSED]` records settled or dropped tasks.

## [1]-[OPEN]

[QUEUED] Exact-arrangement boolean owner design page (`Geometry/.planning/arrangement/arrangement.md`, idea `EXACT_ARRANGEMENT_BOOLEAN`).
- A NEW `arrangement/arrangement.md` design page authoring the `Arrangement` `[Union]` (`MeshBoolean`/`PlanarOverlay`/`CellComplex`), the `ArrangementKind` `[SmartEnum<string>]` discriminant (binding `GeometryKeyPolicy`), the `ArrangementStore` flat cell/face/edge SoA layout, the `BooleanOp` selection (composed from the existing `healing/repair#HEALING` `BooleanOp` SmartEnum, never a second discriminant), and the one `Apply` fold: build the arrangement of two triangle soups by subdividing each face on its `Tessellation` constrained re-tetrahedralization, every intersection segment an `Lpi`/`Tpi` implicit point, classify each cell inside/outside each operand by the `spatial/index#GENERALIZED_WINDING` GWN scalar, keep the `BooleanOp`-selected cells, and weld through `healing/repair#HEALING` `DuplicateWeld`. The `PlanarOverlay` arm is the same cell-complex algebra at triangulation arity (the exact 2D polygon boolean).
- Integrate `Rasm.Geometry.Numerics` (`Predicate.OrientLPI`/`OrientTPI`, `Lpi`/`Tpi`), `Rasm.Geometry.Tessellation` (`Tessellation.Build`/`Insert` constrained tetrahedralization), `Rasm.Geometry.Spatial` (`SpatialQuery.Winding` GWN + BVH broad-phase), `Rasm.Geometry.Healing` (`DuplicateWeld`), Thinktecture/LanguageExt.
- Internal to a new `Arrangement` sub-domain; the `healing/repair#BOOLEAN_NATIVE_ASSET` row's `Apply` re-routes from `NativeAssetMissing` to `Arrangement.Apply` for the common managed cases (a `healing/repair.md` edit on the source pass, the seam ALIGNED here). The branch `ROBUST_ARRANGEMENT_SUBSTRATE` consumers (`Rasm.Bim`, `Rasm.Fabrication`, `Rasm.Compute`) align to `Arrangement.Apply`/`CellComplex` as future wires, never coupling into the `ArrangementStore` interior.
- The exact-arithmetic arrangement is the Cherchi-Attene/Diazzi-Cherchi indirect-predicate technique made managed by the landed LPI/TPI family — no native asset, no epsilon snap; the page must carry the cell-classification GWN composition and the implicit-point segment ordering as transcription-complete fences, and the `ArrangementKind` `mesh-boolean`/`planar-overlay` rows must trace the `BooleanReceipt` (kept-cell count, classified-cell count, weld-collapse count) typed evidence.

[QUEUED] Geometry fault sub-band extension for the seven new owners (`faults/faults#FAULT_BAND`).
- The `faults/faults.md` `GeometryFault` `[Union]` gains its new sibling sub-band cases on the existing band-2400 family: `DegenerateArrangement` (2450, two triangle soups whose intersection is non-manifold or the cell classification is ambiguous), `IntersectionFault` (2460, a narrow-phase exact test on a degenerate primitive pair), `FitFault` (2470, a RANSAC consensus that never reaches the inlier-fraction floor within the sample budget), `ParameterizationFault` (2480, a flattening solve that diverges or a non-disk-topology chart), `ProjectionFault` (2490, a BSP partition stalls or a silhouette extraction hits a non-manifold view), `DecimationFault` (2500, a QEM collapse cannot satisfy the topology-preservation gate within the face budget), `EncodingFault` (2510, a channel pack whose round-trip witness fails the content-hash identity). The `Code` and `Message` switch arms extend with the seven cases; the sub-band decade assignment (arrangement 2450–, intersection 2460–, fitting 2470–, parameterization 2480–, projection 2490–, decimation 2500–, encoding 2510–) is the one ordinal extension and the `Growth` line's "a further sibling claims 2450+" prediction is realized through 2510.
- Integrate Thinktecture (`[Union]`), LanguageExt (`Error`/`Fin`), BCL.
- Internal to the faults sub-domain — the single fault owner the seven new sub-domains route through, never a per-sub-domain fault union; each new owner page composes `GeometryFault.<Case>(...).ToError()` exactly as the existing siblings do.
- The decade-per-sibling sub-banding is the settled fault law; the seven cases close the named failures the seven new owners route, and a `try`/`catch` in any new owner is the forbidden form (every failure routes the `Fin` rail). The `DENSITY_BAR` table's `[CASES]` count updates from 10 to 17.

[QUEUED] Robust intersection lattice owner design page (`Geometry/.planning/intersection/intersect.md`, idea `ROBUST_INTERSECTION_LATTICE`).
- A NEW `intersection/intersect.md` design page authoring the `IntersectOp` `[Union]` (`SegmentSegment`/`SegmentTriangle`/`TriangleTriangle`/`RayMesh`/`MeshMesh`), the `IntersectKind` `[SmartEnum<string>]` discriminant, the `Crossing` result carrier (an exact `Lpi`/`Tpi` implicit point plus its parametric ordering key), and the one `Apply` fold: a segment-triangle hit is a `Tpi` three-plane point gated by the symmetric `Orient3D` straddle, a triangle-triangle crossing the `Lpi`/`Tpi` segment whose endpoints sort by exact orientation (Guigue-Devillers), a mesh-mesh intersection the ordered crossing chain, every narrow-phase test running only on the `spatial/index` BVH candidate pairs. The result re-emits as a `Vectors` `Polyline`/point set at the seam.
- Integrate `Rasm.Geometry.Numerics` (`Predicate.Orient3D`/`OrientLPI`/`OrientTPI`, `Lpi`/`Tpi`), `Rasm.Geometry.Spatial` (`SpatialQuery.Overlap`/`Range` broad-phase), `Rasm`/Vectors (`Polyline`/`Point3d`), Thinktecture/LanguageExt.
- Internal to a new `Intersection` sub-domain; the `healing/repair#HEALING` `SelfIntersectResolve` `TriangleCrossPoint`, the `offsetting/offset` `SegmentsCross`, and the new `arrangement/arrangement` segment subdivision ALIGN to `IntersectOp.Apply` as the one exact crossing owner (the source pass collapses the three inline exact crossing tests onto this owner — the alignment is recorded here, the collapse edits land on the consuming pages' source). The mature `Analysis/Intersect.cs` Rhino-delegated surface stays the host-native parametric-curve/Brep intersection feature and is NOT thinned (the robust lattice owns the exact-predicate mesh/segment intersection, the host owner the NURBS parametric — they meet at no interior).
- Three near-identical inline exact crossing tests across `healing`/`offsetting`/`arrangement` is the collapse signal; the page must carry the Guigue-Devillers exact triangle-triangle test and the implicit-point segment lattice as transcription-complete fences, the `Crossing` carrier typed to the implicit point (never a rounded `Point3d` until the seam re-emit), and the parametric ordering key exact (a float sort of crossings on a chain re-introduces the non-robustness the lattice removes).

[QUEUED] Primitive-fit RANSAC owner design page (`Geometry/.planning/fitting/fit.md`, idea `PRIMITIVE_FIT_FRONTIER`).
- A NEW `fitting/fit.md` design page authoring the `FitOp` `[Union]` (`Plane`/`Sphere`/`Cylinder`/`Cone`/`Torus`/`Line`), the `FitKind` `[SmartEnum<string>]` discriminant carrying the per-kind minimal-sample-size column, the `FitReceipt` typed evidence (inlier `BitArray`/`Set<int>` mask, orthogonal-distance residual, the fitted primitive as a `Vectors` value, the consensus score), and the one `Apply` fold: a MLESAC/PROSAC robust-consensus sampler drawing minimal samples (3 points for a plane, 4 for a sphere), each candidate solved in closed form then scored by inlier count over the `spatial/index` BVH radius query, the winning consensus refined by a geometric-orthogonal-distance Levenberg-Marquardt minimization composing the `constraints/solver` LM iterate over the primitive's parameter vector.
- Integrate `Rasm.Geometry.Spatial` (`SpatialQuery.Nearest`/`Range` inlier neighbourhood), `Rasm.Geometry.Constraints` (`ConstraintSolver.Solve` LM iterate reused for the geometric refine), `Rasm`/Vectors (`Cloud` PCA/covariance, `Point3d`/`Plane`/`Sphere`/`Cylinder`), Thinktecture/LanguageExt, BCL (`System.Random`/`RandomNumberGenerator` for the seeded sampler).
- Internal to a new `Fitting` sub-domain; the branch `REALITY_CAPTURE_TO_BIM` `Rasm.Bim/reconstruction` `ReconstructionPrimitive` and `ElementPredicate` classification ALIGN to `FitOp.Apply`/`FitReceipt` as future wires; the offline RANSAC/learned-segmentation peer fit graduates inward onto the same `FitReceipt` shape (the `libs/.planning` graduation `reconstructed-mesh`/`topology-graph` axis). The LM refine reuses the constraint solver's `Solve` rather than minting a second nonlinear least-squares loop.
- The Schnabel-Wahl-Klein efficient-RANSAC and MLESAC robust-consensus line is the settled technique; the page must carry the per-primitive minimal solver and the orthogonal-distance residual as transcription-complete fences, the consensus loop deterministic under a seeded RNG (a reproducible reconstruction), and the LM refine composing the existing `ConstraintSystem`/`Solve` (a primitive-fit is one entity over a residual-row-per-point system, not a new solver). The fitting must preserve capability — a low-inlier primitive routes `FitFault` rather than returning a meaningless best-fit silently.

[QUEUED] Surface-parameterization owner design page (`Geometry/.planning/parameterization/flatten.md`, idea `SURFACE_PARAMETERIZATION_OWNER`).
- A NEW `parameterization/flatten.md` design page authoring the `ParamOp` `[Union]` (`Harmonic`/`LSCM`/`ARAP`/`BFF`), the `ParamKind` `[SmartEnum<string>]` discriminant carrying the per-kind conformal/area-preserving column, the `ChartAtlas` result (per-chart UV layout, the typed `DistortionReceipt` of conformal/area/quasi-conformal error, the seam set), and the one `Apply` fold composing the `Vectors` `DiscreteCalculus` DEC operators: harmonic mapping a Dirichlet solve over the boundary-pinned cotangent-Laplacian, LSCM the complex-linear conformal-energy minimization, ARAP the local-global rigid-fit alternation, BFF the boundary-first connection-Laplacian flattening — every solve reusing the `LaplacianCache` Cholesky factor the `Vectors` spectral substrate assembles, never a re-assembled mesh Laplacian.
- Integrate `Rasm`/Vectors (`MeshSpace`, `DiscreteCalculus` d0/d1/star/cotangent-Laplacian, `LaplacianCache.For(space).Calculus()`, the `MeshAdjointSnapshot` public boundary handle), MathNet.Numerics (dense conformal-energy eigen/solve for the LSCM Cauchy-Riemann system), Thinktecture/LanguageExt.
- Internal to a new `Parameterization` sub-domain; it composes the `Vectors` DEC operator surface through the same `MeshAdjointSnapshot.Of(space)`/`DiscreteCalculus` seam the `Rasm.Compute` DDG adjoint consumer uses (a public handle, never a reach into the internal `IntrinsicMesh`). The `Rasm.Fabrication` nesting/unroll and the AppUi texture path ALIGN to `ChartAtlas` as future wires; the branch `DRAFTING_DOCUMENT_PRODUCTION` panel-unroll reads the chart atlas by reference.
- The Crane DDG parameterization line (LSCM Lévy 2002, ARAP Liu 2008, BFF Sawhney-Crane 2017) is the settled algorithm over operators the `Vectors` spectral substrate already assembles; the page must carry the four solves as transcription-complete fences composing the live `DiscreteCalculus`/`LaplacianCache` surface (no re-assembled Laplacian), the `DistortionReceipt` typed to the parameterization energy, and the chart-seam set as the typed cut the flattening admits. The mature `Vectors/Mesh.cs` Rhino-delegated unwrap stays the host-native rail; this owner is the kernel-quality conformal/ARAP/BFF solve, never a thinning of the existing unwrap.

[QUEUED] Offsetting wavefront/projection body transcription + arrangement seam + weighted-skeleton growth (`offsetting/offset#STRAIGHT_SKELETON`, `#MEDIAL_AXIS`, `#MINKOWSKI_CONVOLUTION`).
- The `offsetting/offset.md` `[WAVEFRONT]` cluster (`Seed` bisector construction, `EnqueueAll` candidate-event computation, `Collapse` edge-event re-link, `Divide` split-event ring-split) and the `[PROJECTIONS]` cluster (`MedialFrom` skeleton-trim plus `Tessellation` Voronoi-dual reconciliation, `OffsetSnapshot` ring-snapshot at distance `t`, `Assemble` Minkowski segment-loop assembly) are signature-fixed body-transcription targets to fill on the page. DEEPENED: the `Assemble` Minkowski/offset boundary-loop assembly re-routes through the new `arrangement/arrangement#PLANAR_OVERLAY` exact cell complex so a self-overlapping offset resolves through the exact arrangement rather than a float loop-stitch (a seam ALIGNED to the arrangement owner), and the page adds the `OffsetKind` `weighted` growth row (Aurenhammer-Walzl weighted/mitered straight skeleton) reading the SAME wavefront propagation with a per-edge speed column on `OffsetPolicy`.
- Integrate `Rasm.Geometry.Numerics` (`Predicate.Orient2D`), `Rasm.Geometry.Tessellation` (Voronoi dual), `Rasm.Geometry.Arrangement` (`PlanarOverlay` loop assembly — the new seam), `Rasm`/Vectors (`Polyline`/`MeshSpace`), Thinktecture/LanguageExt/BCL.
- Internal to the `Offsetting` sub-domain; the AEC-domain fabrication/nesting/facade-panelization consumers ALIGN to `Apply`/`OffsetCurves`/`SkeletonGraph` as future wires, the `Assemble` arrangement seam ALIGNED to the arrangement owner.
- The wavefront propagation is total over the `Fin` rail; the cluster bodies are the body-transcription work and the `SkeletonLaws` law-matrix the test obligation. The weighted-skeleton row is the named growth, not a parallel skeletonizer.

[QUEUED] Implicit-point in-circum predicate extension (`numerics/predicates#INDIRECT_PREDICATES`, growth row).
- The `numerics/predicates.md` `Predicate` family gains the `InCircleLPI`/`InSphereTPI` implicit-point in-circum members the constrained-Delaunay Steiner recovery and the exact-arrangement boolean compose: each evaluates the in-circle/in-sphere sign against a CONSTRUCTED `Lpi`/`Tpi` intersection point over the homogeneous numerator/denominator `Expansion`s (exact-only, no filter row, the same construction the orientation implicit members use). These are the in-circum companions of the landed `OrientLPI`/`OrientTPI` that the `tessellation/delaunay#CONSTRAINT_RECOVERY` page names as the Steiner-point flip-robustness anchor and the `arrangement` cell test composes.
- Integrate `Rasm.Geometry.Numerics` (`Expansion.Multiply`/`Scale`, `Lpi`/`Tpi`), Thinktecture, BCL (`System.Math` FMA).
- Internal to the numerics sub-domain — two members on the existing `Predicate` static owner riding the same `Expansion` fold and `Sign` verdict, never a sibling implicit in-circum type; the `tessellation/delaunay` Steiner recovery and the `arrangement` cell classification ALIGN to the new members.
- The two members are the named `numerics/predicates` growth (`InCircleLPI`/`InSphereTPI`) — exact-only by construction (a filter would materialize the rounded constructed coordinate the homogeneous form avoids); the `PredicateLaws` law-matrix extends one BigInteger-oracle row per new member, no host probe.

[QUEUED] Predicate sign-exactness law-matrix (`numerics/predicates#NUMERIC_DETERMINISM`, `#INDIRECT_PREDICATES`).
- The design fences are transcription-complete (the four direct predicates plus the landed `OrientLPI`/`OrientTPI` and the new `InCircleLPI`/`InSphereTPI` implicit-point members). The open work is the `PredicateLaws` CsCheck property suite asserting every `Predicate` member — including the implicit-point in-circum pair over a constructed `Lpi`/`Tpi` point — returns the exact sign of the `BigInteger` rational determinant on perturbed near-degenerate input, plus antisymmetry, filter-vs-exact agreement, and translation invariance.
- Integrate CsCheck and the active repo testkit under `testing-cs`; `System.Numerics.BigInteger` as the exact oracle.
- Internal to the numerics sub-domain — proves the predicate floor every higher owner rides; no cross-package wire. The FMA-availability assumption (a future FMA-free RID routes the Dekker-split fallback) is the only deferred numeric probe, gating a fallback row, never the FMA-path predicates.
- The kernel is total by construction; the harness is the law authority, not a structure gate. The design is the test obligation only — no design fence remains open.

[QUEUED] Re-anchor injectivity harness (`topology/naming#REANCHOR_INJECTIVITY`).
- The naming fences are transcription-complete; the per-`EntityKind` `claimed` set already guards both the Survive×Survive and the Survive×Migrate cross-case in `Anchor`/`MigrateOrBirth`. The open work is the tier-2 property harness driving rebuild-matching: apply a labelled topological operation (rigid move/face split/vertex insert/edge collapse), assert the `TrackOutcome` matches the expected lineage class and the next-generation `NameTable` is injective per `EntityKind`.
- Integrate CsCheck under `testing-cs`; a golden rebuild fixture frozen against `HashMap` enumeration order.
- Internal to the topology sub-domain; the `TopoSignature.Subsumes` parent-subset predicate and the deterministic smallest-`TopoName` tiebreak are under test.
- The boundary-storage shape is pinned (`NameEntry.Boundary` + `NameTable.VertexNames`); only the injectivity proof remains — a test obligation, no open design fence.

[QUEUED] LM convergence harness (`constraints/solver#LM_CONVERGENCE`, `#WITNESS_DOF`).
- The solver fences are transcription-complete (the LM iterate plus the landed `WitnessAnalyze` numeric-rank verdict). The open work is the `ConstraintLaws` CsCheck property suite asserting `Solve` converges on every feasible system, the analytic Jacobian matches a central-finite-difference Jacobian, an over-constrained-inconsistent system routes `GeometryFault.OverConstrained` while a redundant-consistent one returns `RedundantConsistent`, rigid-transform invariance, and idempotence.
- Integrate CsCheck and the testkit under `testing-cs`; MathNet.Numerics (`Cholesky`/`Svd`) as the linear-solve substrate under test.
- Internal to the constraints sub-domain. The singular-Jacobian λ-ladder path (including NaN-trial rejection on a numerically-indefinite damped matrix) and the bounded inner reject-chain budget are the convergence-edge probes.
- The LM iterate is a total damped descent by construction; the harness asserts the worst-case step budget terminates at the λ ceiling — a test obligation, no open design fence.

[QUEUED] Delaunay correctness law-matrix (`tessellation/delaunay#BOWYER_WATSON_DELAUNAY`, `#CONSTRAINT_RECOVERY`).
- The tessellation fences are transcription-complete and the Steiner-point flip composes the landed `OrientLPI`/`OrientTPI` implicit-point predicates. The open work is the `DelaunayLaws` CsCheck property suite asserting the empty-circumcircle/sphere property after every insertion against a `BigInteger` exact Delaunay oracle, the valid-simplicial-complex invariant, rigid-transform invariance, and that every constraint segment/facet is present in the recovered complex within the flip/Steiner budget.
- Integrate CsCheck and the testkit under `testing-cs`; `System.Numerics.BigInteger` as the exact in-circum oracle; the `Rasm.Geometry.Numerics` `Predicate` floor under test.
- Internal to the `Tessellation` sub-domain. The `healing/repair#HEALING` `SelfIntersectResolve`, the new `arrangement` boolean gate, and the AEC-domain consumers ALIGN to `Build`/`ToMesh` as future wires, never by coupling into the `SimplexStore` interior.
- The constrained-recovery law row is now unblocked (the LPI/TPI family landed); a test obligation, no open design fence.

[QUEUED] Offsetting law-matrix (`offsetting/offset#STRAIGHT_SKELETON`, `#MEDIAL_AXIS`, `#MINKOWSKI_CONVOLUTION`).
- The `offsetting/offset.md` page realizes the predicate-exact offsetting owner. The open work is the `SkeletonLaws`/offset property suite asserting the swept-trace is a valid planar straight skeleton, the offset polygon at distance `t` is simple, the medial axis is a deformation retract, the Minkowski boundary is simple, and (with the weighted growth row) the weighted skeleton's per-edge speeds order the events deterministically.
- Integrate CsCheck and the testkit under `testing-cs`; composes the `Predicate` exact-turn floor, the `Tessellation` Voronoi dual, and the new `arrangement` planar-overlay loop assembly under test.
- Internal to the `Offsetting` sub-domain; the AEC-domain fabrication/nesting/facade-panelization consumers ALIGN to `Apply`/`OffsetCurves`/`SkeletonGraph` as future wires.
- The wavefront propagation is total over the `Fin` rail by construction; the `[WAVEFRONT]`/`[PROJECTIONS]` cluster bodies are signature-fixed transcription targets (the body task above) — a test obligation plus the body transcription on the source edit, no open design-decision fence.

[QUEUED] New-owner law-matrices (seven idea consumers).
- The seven new owner pages each carry their tier-2 law-matrix obligation: `ArrangementLaws` (the boolean result is manifold and watertight, the cell classification agrees with a BigInteger-oracle inside/outside, the Union/Difference/Intersection De Morgan identities hold); `IntersectionLaws` (the exact crossing point lies on both primitives, the triangle-triangle test agrees with the BigInteger oracle, the chain ordering is total, the plane-mesh section curve lies on both the plane and the mesh); `FittingLaws` (the consensus is deterministic under a fixed seed, the orthogonal residual decreases under the LM refine, a synthetic primitive with known inliers recovers within tolerance); `ParameterizationLaws` (the harmonic map is a bijection on a disk-topology chart, LSCM minimizes the conformal energy, ARAP converges, the distortion receipt bounds the energy); `ProjectionLaws` (a silhouette edge separates a front-facing from a back-facing triangle by the exact sign, the BSP painter order is a valid back-to-front total order, a hidden segment is occluded by a visible one, the section curve closes); `DecimationLaws` (a collapse never flips a triangle orientation, the Hausdorff bound is monotone non-decreasing across LODs, the vsplit stream reconstructs the source exactly, a feature edge survives the `FeaturePreserve` budget); `EncodingLaws` (the round-trip witness recovers the source within the channel dtype tolerance, the content-hash identity holds, every active channel's arity matches its descriptor).
- Integrate CsCheck and the testkit under `testing-cs`; `System.Numerics.BigInteger` exact oracle for the arrangement/intersection/projection sign laws; MathNet for the parameterization energy; the `CANONICAL_BYTE_IDENTITY` frozen digest for the encoding round-trip.
- Internal to each new sub-domain; the law-matrices land once the seven owner pages' fences are authored (the design-page tasks above gate these test obligations).
- Each is a test obligation, not a design fence; the harness rows ride the kernel testkit node the branch `BRANCH_TEST_NODE_PROVISIONING` provisions.

[QUEUED] Hidden-line/silhouette projection owner design page (`Geometry/.planning/projection/view.md`, idea `HIDDEN_LINE_PROJECTION`).
- A NEW `projection/view.md` design page authoring the `ViewOp` `[Union]` (`Silhouette`/`HiddenLine`/`Section`/`Outline`), the `ViewKind` `[SmartEnum<string>]` discriminant (binding `GeometryKeyPolicy`) carrying the per-kind `EmitsHidden`/`NeedsBsp` columns, the `BspNode` flat partition store (front/back half-space child ranges over the shared SoA layout the `spatial/index` precedent fixes), the `DrawingProjection` result carrier (the visible/hidden 2D segment SoA, the per-segment depth key, the `EdgeKind` `silhouette`/`crease`/`boundary`/`intersection` classification), and the one `Apply` fold: the silhouette edge set is the exact-sign locus where a front-facing and a back-facing triangle meet (the per-face view-dot sign decided by `Predicate.Orient3D` so a grazing edge never flickers), the hidden-line removal is a Newell-Newell-Sancha BSP painter partition over the same `Orient3D` plane-side sign traversed front-to-back through the `spatial/index` BVH, the `Section` arm is one `IntersectOp.Apply` against the cutting plane re-projected to the view basis, and the crease/boundary classification reads the `topology/naming` edge-dihedral angle.
- Integrate `Rasm.Geometry.Numerics` (`Predicate.Orient3D` exact view-dot/plane-side sign), `Rasm.Geometry.Intersection` (`IntersectOp.Apply` `PlaneMesh`/`MeshMesh` for the section cut — the new owner), `Rasm.Geometry.Spatial` (`SpatialQuery.Ray`/BVH front-to-back traversal), `Rasm.Geometry.Topology` (edge-dihedral crease classification), `Rasm`/Vectors (`MeshSpace`/`Polyline`/`Plane`), Thinktecture/LanguageExt.
- Internal to a new `Projection` sub-domain; the branch `DRAFTING_DOCUMENT_PRODUCTION` `Rasm.Fabrication/projection/hidden-line` HLR and the `Rasm.AppUi/drafting` sheet engine ALIGN to `ViewOp.Apply`/`DrawingProjection` as future wires (the Fabrication BSP visibility kernel and this owner meet at the `DrawingProjection` carrier, never coupling into the `BspNode` interior — one of the two owns the managed BSP and the other reads its drawing output; this kernel owns the predicate-exact visibility, Fabrication the sheet-side HLR composition). The mature `Analysis`/`Vectors` host-Make2D delegation stays the host-native parametric-curve projection; this owner is the predicate-exact mesh silhouette/hidden-line, never a thinning of the host rail.
- The Appel hidden-line / Newell-Newell-Sancha BSP-painter and the exact-sign silhouette extraction is the settled technique; the page must carry the BSP partition and the silhouette sign-change locus as transcription-complete fences, the silhouette decided by `Predicate.Orient3D` (a float view-dot threshold re-introduces the grazing-edge flicker the predicate removes), the `DrawingProjection` typed to the visible/hidden segment families with the `EdgeKind` classification, and the `Section` arm composing `IntersectOp.Apply` rather than a fourth inline crossing test. The projection preserves capability — a hidden segment is classified and retained, never discarded, so a downstream dashed-hidden render reads the full set.

[QUEUED] Robust mesh decimation / LOD owner design page (`Geometry/.planning/simplification/decimate.md`, idea `ROBUST_MESH_DECIMATION`).
- A NEW `simplification/decimate.md` design page authoring the `SimplifyOp` `[Union]` (`QuadricCollapse`/`ProgressiveMesh`/`VoxelRemesh`/`FeaturePreserve`), the `SimplifyKind` `[SmartEnum<string>]` discriminant carrying the per-kind `Reversible`/`PreservesTopology` columns, the `QuadricStore` flat per-vertex 4×4 error-quadric SoA plus the collapse-cost priority-queue state, the `DecimationReceipt` typed evidence (the simplified `MeshSpace`, the per-LOD vertex/face budget, the one-sided Hausdorff error bound against the source, the preserved crease/boundary feature set, the reversible vsplit stream for the progressive case), and the one `Apply` fold: a Garland-Heckbert quadric-error-metric edge-collapse over a priority queue keyed by the collapse cost, each candidate gated by the `numerics/predicates#ROBUST_PREDICATES` `Orient3D` sign so a collapse never flips a triangle's orientation or produces a non-manifold fold, the `FeaturePreserve` variant pinning the `topology/naming` crease/boundary edges, the `ProgressiveMesh` variant recording the reversible vsplit sequence, and the `VoxelRemesh` variant reading the `Vectors` mesh SDF for a topology-repairing resample.
- Integrate `Rasm`/Vectors (`MeshSpace`, principal-curvature metric, mesh SDF for the voxel-remesh resample), `Rasm.Geometry.Numerics` (`Predicate.Orient3D` collapse-validity sign), `Rasm.Geometry.Topology` (crease/boundary feature classification), Thinktecture/LanguageExt, BCL (`PriorityQueue<TElement,TPriority>`).
- Internal to a new `Simplification` sub-domain; the branch `GEOMETRY_FLOW_SEAM`/`Compute/interchange/codecs#TILE_PARTITION` tile pyramid, the `AEC_SIMULATION_BRIDGE`/`Compute/solver/discretization` coarse-mesh solver seed, and the `WEB_GEOMETRY_RESIDENCY_WIRE` meshlet residency tier ALIGN to `SimplifyOp.Apply`/`DecimationReceipt` as future wires. The mature `Vectors/Mesh.cs` host `Mesh.Reduce`/remesh rail stays the host-native quick-reduce; this owner is the predicate-guarded error-bounded QEM decimation with a reversible vsplit stream the host produces neither of, never a thinning of the existing remesh.
- The Garland-Heckbert QEM and the Hoppe progressive-mesh line is the settled technique; the page must carry the per-vertex quadric accumulation, the collapse-cost ordering, and the `Orient3D` collapse-validity gate as transcription-complete fences, the `DecimationReceipt` carrying the one-sided Hausdorff bound as a typed error budget (a face-count-blind reduce is the rejected form), and the vsplit stream exact (a reversible continuous-LOD selection re-applies the recorded collapses). The decimation preserves capability — a `FeaturePreserve` collapse pins a crease rather than smoothing it away, so no LOD drops a sharp feature to satisfy a budget.

[QUEUED] Geometry-encoding packing owner design page (`Geometry/.planning/encoding/pack.md`, idea `GEOMETRY_ENCODING_PACK`).
- A NEW `encoding/pack.md` design page authoring the `PackOp` `[Union]` (`PointCloud`/`MeshPatch`/`VoxelGrid`/`BrepPatch`), the `PackKind` `[SmartEnum<string>]` discriminant carrying the per-kind active-channel column, the `EncodingChannel` `[SmartEnum<string>]` eight-row feature lattice (`position`/`normal`/`color-rgba`/`curvature`/`geodesic`/`intensity`/`occupancy`/`weight`) carrying the per-channel arity/dtype column, the `EncodedGeometry` result carrier (the per-channel `ReadOnlyMemory<float>` payload SoA, the active-channel descriptor set, the lossless-round-trip witness keyed by the canonical-adjacency content hash), and the one `Apply` fold materializing each case's channels by composing the live kernel readers: the `MeshPatch` position/normal from `Vectors` `MeshSpace`, the curvature channel from the `Vectors` principal-curvature metric, the geodesic channel from the `Vectors` heat-geodesic, the `PointCloud` intensity/normal from the `Vectors/Cloud` PCA-oriented-normal metric, the `VoxelGrid` occupancy from the mesh SDF sign, the round-trip witness from `topology/reconciliation#CANONICAL_BYTE_IDENTITY`.
- Integrate `Rasm`/Vectors (`MeshSpace`, `Cloud` PCA/oriented-normal, principal curvature, heat-geodesic, mesh SDF), `Rasm.Geometry.Topology` (`CanonicalTopology`/`Encode` the round-trip content hash), `System.Numerics.Tensors` (`ReadOnlyTensorSpan`/`TensorPrimitives` for the channel-payload pack — the kernel materializes the channel float payload the Compute lane wraps), Thinktecture/LanguageExt, BCL (`ReadOnlyMemory<float>`).
- Internal to a new `Encoding` sub-domain; the branch `ENCODED_GEOMETRY_PLUGIN_FRONTIER` `AppHost/sandbox/solver-plugin#SOLVER_KIND` `GeometryPacking` boundary capsule and the `Compute/tensors/residency#GEOMETRY_ENCODING` `GeometryEncoding`/`EncodedTensor` tensor lane ALIGN to `PackOp.Apply`/`EncodedGeometry`/`EncodingChannel` as future wires — the kernel owns the channel materialization through the `Vectors` readers, the Compute lane owns the `EncodedTensor` residency, meeting at the `EncodedGeometry` carrier and the shared `EncodingChannel` vocabulary, never coupling into the channel-pack interior. The `Field`/`Toolpath` representations the branch names land as two future `PackKind`/`EncodingChannel` rows, never a parallel surface.
- The branch `GeometryEncoding` four-case representation axis and its eight-row `EncodingChannel` feature lattice is the settled vocabulary; the page must carry the per-channel reader composition and the lossless-round-trip witness as transcription-complete fences, the witness keyed by the `CANONICAL_BYTE_IDENTITY` content hash (a silent quantization with no round-trip proof is the rejected form), and the channel payload as a `ReadOnlyMemory<float>` SoA the tensor lane wraps without a re-pack. The encoding preserves capability — a curvature/geodesic channel is materialized from the live `Vectors` metric, never approximated, so a downstream learned-segmentation consumer reads the real feature.

[QUEUED] Plane-mesh intersection growth row + section consumer alignment (`intersection/intersect#ROBUST_INTERSECTION_LATTICE`, idea deepened).
- The `intersection/intersect.md` `IntersectOp` `[Union]` gains the `PlaneMesh` case and the `IntersectKind` `plane-mesh` row reading the SAME exact-sign crossing fold: a plane-mesh section is the ordered crossing chain of the cutting plane against each candidate triangle (a `Tpi`/`Lpi` segment per straddling triangle gated by the symmetric `Orient3D` plane-side sign over the `spatial/index` BVH candidate set), re-emitted as the `Vectors` `Polyline` section curve — the one section/contour producer the new `HIDDEN_LINE_PROJECTION` `ViewOp.Section` and the AEC-domain section/elevation read, never a host Make2D round-trip.
- Integrate `Rasm.Geometry.Numerics` (`Predicate.Orient3D` plane-side straddle, `Lpi`/`Tpi`), `Rasm.Geometry.Spatial` (`SpatialQuery.Range` plane-slab broad-phase), `Rasm`/Vectors (`Plane`/`Polyline`), Thinktecture/LanguageExt.
- Internal to the `Intersection` sub-domain — one case on the existing `IntersectOp` union riding the same `Crossing` carrier and the same chain-ordering fold, never a sibling section type; the new `projection/view#HIDDEN_LINE_PROJECTION` `Section` arm and the AEC-domain consumers ALIGN to `IntersectOp.Apply`/`Crossing` as the one section owner.
- The plane-mesh case is the named `intersection` growth (`plane-mesh`) reading the same exact-sign lattice — the section curve's crossing points are `Lpi`/`Tpi` implicit (never a rounded `Point3d` until the `Polyline` re-emit) and the chain ordering exact; the `IntersectionLaws` law-matrix extends one row asserting the section curve lies on both the plane and the mesh.

[QUEUED] Mature-folder de-hardcode + capability audit (`Domain/Geometry.cs`, `Analysis/Topology.cs`, `Analysis/Intersect.cs` — improvement dimension).
- A read-only audit-into-tasks of the three mature folders for baked-in single-product/single-shape literals that a polymorphic owner-plus-vocabulary collapses: the `Domain/Kind`/`Topology` coercion table's per-`Type` membership sets (`CurvePrimitives`/`SurfacePrimitives`/`BrepSources`) are the canonical kind→capability axis and any future geometry kind is a row, not a parallel set — confirm the `CanCoerceTo`/`CanReadVertices` predicates stay table-driven as the kind roster grows; the `Analysis/Topology` `TopologyScalar` family and the `Analysis/Intersect` `IntersectionKind` discriminant are the settled measure/intersection vocabularies the robust-core `Intersection` lattice composes at the seam, not re-mints. The audit names any genuine hardcode (a magic reflection cap, a fixed reflection bound `MaxReflections <= 1000`) as a `BuildPolicy`-style column candidate, never a bloat split.
- Integrate Thinktecture (`[SmartEnum]`/`[Union]`), LanguageExt, BCL (`FrozenSet`/`FrozenDictionary`).
- Internal to the mature sub-domains; the robust-core `Intersection`/`Projection` owners ALIGN to `Domain/Kind` and `Analysis/IntersectionKind` as composed vocabulary, never coupling into the mature interior — the audit confirms the seam, it does not rewrite the mature source.
- The mature coercion/measure/intersection tables are already polymorphic owners; the audit is a capability-conservation pass (a baked-in cap becomes a policy column only where a real downstream varies it) and a confirmation that the new robust-core owners compose the mature vocabularies at the seam rather than re-minting a second kind/intersection discriminant — no mature owner is thinned, no functionality removed.

[QUEUED] Surface-confirmation sweep for the six mature `.api` catalogues (`.api/*.md` — improvement dimension, coverage audit).
- The six catalogue H1 titles were copied from sibling folders (`# [RASM_COMPUTE_API_*]`, `# [RASM_APPUI_API_RHINO]`) and are CORRECTED to the owning-folder `# [RASM_API_<PACKAGE>]` token; `api-rhino.md` was a WHOLESALE mis-file (the AppUi host document/view/command/display Rhino surface — `RhinoDoc`/`RhinoApp`/`Command`/`RhinoView`/`DisplayConduit`/`ViewCapture`/`ObjectTable`) and is REWRITTEN to the kernel's real `Rhino.Geometry` value-type surface (the `Point3d`/`Vector3d`/`Transform`/`Interval`/`Plane`/`BoundingBox`/`Box`/`Sphere`/`Cylinder`/`Cone`/`Torus`/`Circle`/`Arc`/`Ray3d`/`Line`/`Curve`/`NurbsCurve`/`Polyline`/`Mesh`/`MeshFace`/`Brep` the kernel composes through `Vectors`, with the document/view/command/display surface explicitly rejected as the host-boundary stratum's concern) — landed this pass. The open work is the surface confirmation that each catalogue's tables still match the folder's real composition as the new owners land: the MathNet `Integrate`/`FindRoots`/`Interpolate`/`SpecialFunctions` rows the new `Fitting` RANSAC and the `Flow` integration compose, the Tensors `ReadOnlyTensorSpan`/`TensorPrimitives` rows the new `Encoding` channel pack composes, the CSparse factorizations the constraint solver and the new `Parameterization` solves compose, and the `Rhino.Geometry` value-type rows the kernel reads through `Vectors`.
- Integrate no package change — a catalogue surface-confirmation edit only.
- Internal to the folder `.api/`; the README registry stays unchanged (it lists the packages, not the catalogue titles). The six catalogues are the folder's verified-API resource the new owner pages transcribe against.
- The five numeric/tensor/hashing catalogue surface tables are decompile-verified and correct; the rewritten `api-rhino.md` now documents the `Rhino.Geometry` value vocabulary the kernel actually composes (the prior AppUi-host body was a boundary leak, not a kernel surface). The confirmation that the new `Fitting`/`Encoding`/`Flow`/`Parameterization` owners' MathNet/Tensors/CSparse members are present in the catalogue rows before a fence transcribes against them is the coverage check — an absent member is a RESEARCH item on the owner page, never settled fence code.

[UPSTREAM-BLOCKED] Boolean tier-3 native arrangement asset gate (`healing/repair#BOOLEAN_NATIVE_ASSET`).
- The `HealOp.Boolean` row is fence-complete in shape (the case, the `BooleanOp` discriminant, the `BooleanReceipt`, the `Apply` gate routing `NativeAssetMissing`) but carries no managed CSG kernel; the algorithm contract is exact-arithmetic mesh-arrangement classification with a robust ray-parity inside/outside test grounded on `Predicate.Orient3D`.
- EXTERNAL DEPENDENCY: a robust exact-arithmetic mesh-arrangement native asset that does not yet exist (no admissible managed manifold library; ManifoldNET alpha rejected). DEEPENED: the new `EXACT_ARRANGEMENT_BOOLEAN` managed owner (`arrangement/arrangement.md` task above) is now the seated managed companion that RETIRES this native dependency for the common cases — the `Apply` gate re-routes from `NativeAssetMissing` to `Arrangement.Apply` once the arrangement page's fences land, the native asset reserved only for a future performance/scale path the managed exact arrangement does not cover. The native-asset admission remains a charter amendment with its RID burden assessed.
- Internal to the healing sub-domain; stamped UPSTREAM-BLOCKED on the native-asset admission, with the managed companion now a CONCRETE owner page (no longer a held abstract path).

## [2]-[CLOSED]

[COMPLETE] Clash-seam node-link golden fixture (`spatial/index#CLASH_SEAM`, `#CLASH_GOLDEN`). The node-link wire is settled and `[CLASH_GOLDEN]` is fully PINNED — the 8 `BoundingBox(min,max)` coordinates, `NodeCount == 3`, the 160-byte `Bounds`/`Nodes` LE golden stream (descriptors `2097154`/`-5`/`-8388613`, identity `Order` tail), and the 4-pair clash set `{(0,1),(2,3),(4,5),(6,7)}` are byte-derivable on both sides; the corpus row in `topology/reconciliation#ONE_WIRE_FIXTURE_CORPUS` is REAL. The two-sided byte-equality spec is the only remaining test artifact (a `testing-cs` obligation, not a design gap).

[COMPLETE] Canonical-adjacency byte-identity golden fixture (`topology/reconciliation#CANONICAL_BYTE_IDENTITY`). The geometry-side fixture is REAL and frozen (the 52-byte single-triangle stream + `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest, morph and topology-break discriminators). The remaining cross-package byte-equality assertion against the Persistence `GeometryHash` is the synthesis tier's two-package test scope — a `csharp:Persistence/...#STRUCTURAL_DIFF` alignment, outside this folder's write-scope.

[COMPLETE] Degradation-keyed refit and agglomerative builder (idea `DEGRADATION_REFIT`, `spatial/index#DEGRADATION_REFIT`, `#AGGLOMERATIVE_BUILD`). Realized: `BuildPolicy.RefitDegradationLimit` column, the `SpatialKind.Agglomerative` `Builders` row writing the shared `NodeStore` (`BuildAgglomerative` Morton-presorted nearest-neighbour clustering), `Bvh.BuildCost`/`Builder` columns, and the `SahCost`-vs-baseline degradation-keyed `Refit` rebuild trigger — the Compute clash seam rides the same `ToAcceleration` projection unchanged.

[COMPLETE] Indirect-predicate family extension (idea `INDIRECT_PREDICATES`, `numerics/predicates#INDIRECT_PREDICATES`). Realized: `OrientLPI`/`OrientTPI` members over `Lpi`/`Tpi` exact homogeneous constructions evaluating the orientation sign over a constructed line-line / three-plane intersection point without rounding, with `OrientLPIBound`/`OrientTPIBound` filter rows. Unblocks the healing self-intersect split, the Delaunay Steiner-point recovery, and the new exact-arrangement boolean and intersection-lattice owners. The `InCircleLPI`/`InSphereTPI` in-circum companions are the OPEN growth task above.

[COMPLETE] Generalized-winding-number query (idea `GENERALIZED_WINDING`, `spatial/index#GENERALIZED_WINDING`). Realized: `SpatialQuery.Winding` case + `QueryResult.Scalar` + the `Winding`/`NodeMoment`/`SolidAngle` Barill/Jacobson tree-based GWN best-first descent over the existing BVH — a query case, not a new structure; the inside/outside classifier the new exact-arrangement boolean cell-keep predicate composes.

[COMPLETE] Witness-configuration DOF analysis (idea `WITNESS_DOF`, `constraints/solver#WITNESS_DOF`). Realized: `DofAnalysis.RedundantConsistent` row + `ConstraintSolver.WitnessAnalyze` (numeric Jacobian rank at the seed witness, `Svd`-tail residual-consistency test distinguishing redundant-consistent from over-constrained-inconsistent) wired into the `Solve` over-constraint gate so a redundant-but-consistent system solves.

[COMPLETE] Mature-folder `Geometry` naming reconciliation (`ARCHITECTURE#NAMESPACE_LAW`). Settled: `Domain/Geometry.cs` (namespace `Rasm.Domain`, types `Topology`/`Kind`/`CurveForm`) and the `Rasm.Geometry.*` robust-core tree are DISJOINT namespace roots; the bare `Geometry` token is a filesystem-path coincidence the disjoint roots already resolve. No rename, no namespace re-scope, no source move — authored as settled kernel ARCHITECTURE vocabulary, the "before the source tree" trigger dropped (source-tree creation is out of scope).
