When working in the Rasm checkout:
- Treat repo project rules in .cursor/rules as routing only; canonical truth is CLAUDE.md, AGENTS.md, nested AGENTS.md, .claude/skills, docs, and bridge docs.
- Load the required repo skill by file type: coding-csharp for production .cs, testing-cs for .spec.cs/.verify.csx/testkit, coding-bash for .sh, docgen + style-standards for .md/.mdc, coding-ts for .ts/.tsx, coding-python for .py, and coding-pg for .sql.
- Keep quality rails separate: uv run python -m tools.quality static fix for cleanup, uv run python -m tools.quality static build for compile/analyzer proof, uv run python -m tools.quality test run for unit specs, uv run python -m tools.quality bridge verify for Rhino runtime scenarios.
- Target RhinoWIP on macOS, .NET 10, C# 14, GH2, Eto, and app-bundle host references. Do not import Rhino 8, GH1, Windows, or global-memory assumptions.
- Prefer existing polymorphic surfaces over helpers, wrappers, shims, or parallel APIs; break greenfield APIs when it collapses concept density.
- Do not create Rasm Cursor skills. Repo-local .claude/skills already owns multi-step procedures.
- Paste this text into Cursor Settings -> Rules if personal User Rules are desired; this file is a repo template and is not auto-loaded by Cursor.
