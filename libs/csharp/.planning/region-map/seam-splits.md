# [REGION_MAP_SEAM_SPLITS]

Suite region map — row shapes and protocol live in the [suite standard](../README.md) ledger-protocol section.

- Config reload propagation: mechanics at AppHost/configuration-and-options#POLICY_VALUES (reload-class transition + ReloadReceipt); consequence at Persistence user-settings write and the op-log HLC tag-invalidation cursor (cross-process peer observation); frozen reload-class requires process restart.
- Operator kill-switch: mechanics at AppHost/configuration-and-options#KILL_SWITCH (KillSwitchConfig row, OperatorOverride union); consequence at AppHost/health-and-degradation#DEGRADATION_RAIL (forced DegradationLevel input to the fold) and the ControlService set-degradation/reload-options inbound verbs for service modalities.

- HybridCache: mechanics at AppHost/resource-lanes#CACHE_PORT (port, stampede, tags, entry options); consequence at Persistence/cache-indexes#L2_CONTRIBUTION (IDistributedCache + serializer rows).
- Outbound retry: mechanics at AppHost/outbound-resilience#KEYED_PIPELINES (one keyed Polly pipeline per hop); consequence at Compute/receipts-and-benchmarks#RECEIPT_UNION (Conflict case, zero gRPC-native retry). Database retry is excluded from the hop law: execution-strategy rows live at Persistence/store-profiles.
- Correlation: mechanics at AppHost/diagnostics-and-telemetry#CORRELATION_SPINE (W3C TraceContext + Baggage); consequence on every sibling signal and across the UDS hop as gRPC metadata.
- Drain order: mechanics at AppHost/lifecycle-and-drain#DRAIN_CONDUCTOR (frozen rank-band table; inbound-admission cessation precedes band 100; 100s AppUi, 200s Compute, 300s Persistence, telemetry last; store-dependency constraint column); consequence at each sibling's DrainParticipantPort registrations.
- DataClassification taxonomy: mechanics at AppHost/diagnostics-and-telemetry#REDACTION_TAXONOMY (classification axis); consequence at Persistence/redaction-retention#CLASSIFICATION_ENFORCEMENT (store-side enforcement rows).
- Lane naming: AppHost owns process-level drainable queues (DrainQueue); Compute owns solve-path bounded channels (WorkLane). The homonym is resolved by altitude.
- Profile variance: single residence at AppHost/host-profiles#PROFILE_AXIS (HostProfile row columns + ResolvedProfile record); Persistence consumes the resolved record and owns no profile-keyed table.
- Wire vocabulary: mechanics at Compute/remote-lane#PROTO_VOCABULARY (ComputeService, DocumentService, ControlService, ArtifactSync, grpc.health.v1); consequence at AppHost/runtime-ports#WIRE_LAW (suite wire law + TS tooling map).
- Store paths: mechanics at AppHost/host-profiles#RESOURCE_IDENTITY (per-user root computation); consequence at Persistence/store-profiles#PROFILE_AXIS (resolved-record consumption, never path derivation).
- Receipt sinks: mechanics at AppHost/runtime-ports#PORT_RECORDS (ReceiptSinkPort); consequence at Compute/receipts-and-benchmarks, Persistence/query-rail, AppUi/diagnostics-evidence projection rows.
- Telemetry contribution: mechanics at AppHost/runtime-ports#PORT_RECORDS (TelemetryContributorPort); consequence at Persistence (AddNpgsql tracer/meter rows) and Compute (ActivitySource registration rows).
- Clock seam: mechanics at AppHost/time-and-deadlines#CLOCK_SPLIT (IClock + TimeProvider injection); consequence at Persistence TTL/retention/HLC/lease stamping and Compute elapsed measurement.
- ArtifactSync framing unification: mechanics at Compute/remote-lane#ARTIFACT_FRAMES (64 KiB frames, Crc32 per frame, XxHash128 whole-artifact); consequence at Persistence/sync-collaboration#PRESENCE_AND_BLOB and Persistence/store-profiles#PROFILE_AXIS (BlobRemote) — frame constants consumed, never re-declared.
- Pooled-context factory residence: store-profiles rows carry Connect/Configure/Seed delegates only; one PooledDbContextFactory per placement is built at the composition root over the row's Configure output (query-rail leases via CreateDbContextAsync) — neither page re-declares the other's half.
- Idempotency dedup window: Compute/remote-lane#PROTO_VOCABULARY fixes the server dedup guarantee at the DeadlineClass.HopTotal allotment (the one retry owner's horizon); Persistence/redaction-retention#CLASSIFICATION_ENFORCEMENT bounds the dedup-record lifetime with the idempotency-dedup AgeBound 24h row — two altitudes, neither re-declares the other.
- Fault-code projection: query-rail StoreFault.From projects schema-rail SchemaFault 530x codes into StoreFault.NewerSchema 7005 — single projection site.
- Compiled-model fingerprint slot: store-profiles StoreOpenReceipt carries the fingerprint as bare ulong (no forward reference); schema-rail SchemaFingerprint is the typed owner; BUILD_ORDER places vocabulary owners first.
- Drain ranks: Persistence store rows rank 310-350 inside the AppHost Stores band (300s); Compute and AppUi registrations rank inside their own bands, never copying these literals.
- Model-result cache: mechanics at Persistence/cache-indexes#MODEL_RESULT_INDEX (IndexSurface HybridCache extension); consequence at Compute/model-lane#RESULT_CACHE (cache.Result read path) — never a direct AppHost CacheSurface read.
- Warm-start affinity: mechanics at Compute/remote-lane#TRANSPORT_AXIS (NodeSelection affinity column, EP-context node routing); consequence at Compute/intent-and-selection#SUBSTRATE_AXIS (warm-affinity reorder of the rank-equal eligible chain, SelectionReceipt warm-affinity flag) — affinity is a tie-break column inside the rank tier, never a rank override and never an `if(warm)` branch.
- EP-context warm-start blob: mechanics at Compute/model-lane#SESSION_CAPSULE (ModelSessions.Lease mints the content-addressed ArtifactIndexRow.EpContext under WarmStartClassification/WarmStartRetention on first compiled open); consequence at Persistence/cache-indexes#ARTIFACT_BLOB_INDEX (the blob lane stores and serves the compiled context bytes so a cold companion warms from the same blob the host wrote) — Compute owns the fingerprint and classification, Persistence owns blob residence; neither re-declares the other.
- AppUi telemetry contribution: mechanics at AppUi/diagnostics-evidence#RECEIPT_UNION (AppUiTelemetry.Contribute(version, params instruments) — the one parameterized TelemetryContributorPort factory every AppUi owner calls with its own instrument-name constants); consequence at AppHost/runtime-ports#PORT_RECORDS (TelemetryContributorPort fan-in) and at every AppUi page owner's TelemetryRow (surface-hosts, shell-navigation, screens-activation, live-data, tables-hierarchy, inspector-editing, commands-availability) — instrument names stay owned by the contributing page, a per-owner hand-rolled port factory is the defect.
- Package-cumulative resolution: within a package, fences resolve against the package's full FINAL symbol set (the intent/dispatch spine composes its axis vocabularies by design); strict earlier-position order applies across packages only.
- Key-policy posture: one comparer accessor per axis owner, package-local; cross-package accessor reuse is a defect — the scheduling-and-lanes WorkLane accessor is ComputeKeyPolicy, never the AppHost LaneKeyPolicy.
- Grpc.Core.Api decompile route: transitive package without an assay key — proofs route through the spec-compile rail until the assay source map registers it (charter PROOF_GATES row).
- Gesture-conflict evidence: mechanics at AppUi/commands-availability#INTENT_TABLE (CommandDeck.GestureConflicts + ConflictKind at deck freeze); consequence at AppUi/input-interaction#HOTKEY_DERIVATION (Bindings consumes the frozen deck, first-wins).
- Focus-walk: execution at AppUi/diagnostics-evidence#HEADLESS_DERIVATION (ProofCheck.FocusWalk); audit law at AppUi/accessibility#COMPLIANCE_PROOF (AccessAudit.FocusWalk folds the engine result) — homonym is a seam, not a duplicate.
- Export-union medium split: tables-hierarchy ExportDestination owns tabular text egress (Clipboard/File/BlobLane); visuals-offscreen VisualDestination owns rendered visual egress (FilePath/BlobLane/Bundle) — distinct media, two owners.
- Asset-key law: icons-assets AssetKeys is nameof-derived; every cross-page asset reference uses the nameof spelling, never kebab-case.
- Phase-set coupling guard: motion-tokens PHASE_MAPPING keys mirror the Compute ProgressPhase nine-case set; the headless conformance sweep (Map keys == ProgressPhase.Items) fails the AppUi proof lane on any Compute phase change by design.
- Clock-seam conformance: surface-hosts Surfaces.Mount and theme-tokens ThemeCell carry ClockPolicy per the AppHost one-clock-seam law; a bare IClock parameter on any AppUi entrypoint is the named defect.
- Theme-grid typing: AccessProof.Sweep and ProofEngine.Derive share the typed (ThemeVariantRow, DensityRow) grid; theme-tokens row key literals stay string-stable.

- HMAC redactor resolution: mechanics at AppHost/diagnostics-and-telemetry#REDACTION_TAXONOMY (RedactionRegistration AddRedaction/SetHmacRedactor/SetRedactor<ErasingRedactor>/SetFallbackRedactor fold under EXTEXP0002; RedactorKind axis); consequence at Persistence/redaction-retention#EXPORT_PROOF (ClassificationGuard resolves RedactorKind.Hmac via the AppHost-registered IRedactorProvider at egress — Hmac join-preserves HostIdentity/Personal, None passes Operational, unmapped fails closed by ErasingRedactor; Persistence never registers a redactor). One taxonomy, two consumers; the store-side CLASSIFICATION_ENFORCEMENT split (above) bounds the class axis, this split owns the egress redactor resolution.
- Loopback transport security (macOS UDS-only collapse): the live local-IPC byte path is UnixDomainSocket-only at Compute/remote-lane#TRANSPORT_AXIS — the Windows-only NamedPipe (PipeSecurity ACL) and bearer-plus-DACL TcpLoopback rows are DROPPED from the live macOS axis, their per-path security member spellings retained as the design record on remote-lane#RESEARCH [PIPE_SECURITY], re-entering as one row each only behind a host RID that admits the byte path and never blurred into one credential; consequence at AppHost/outbound-resilience#KEYED_PIPELINES (KeyedLane LocalIpc redial is one AddFallback whose FallbackStrategyOptions<HopOutcome>.FallbackAction re-reads the peer manifest and reconnects over the UDS hop — a hop-keyed policy value, never a coded loop, riding the H6 redial law) and at AppHost/companion-sidecar#SERVICE_HOST (ControlTransport collapses to the single UnixDomainSocket intake case, the WindowsNamedPipe case dropped with PipeHardening kept as design record). Compute owns the UDS byte path; AppHost owns the redial policy and the UDS-only control intake; neither re-declares the other.
- Web-fed co-host: mechanics at AppHost/host-profiles#LIFETIME_ADAPTERS + runtime-ports#WIRE_LAW (ProfileBoot web app-root row mounting static-asset middleware same-origin plus CORS/grpc-web, app-root-only ASP.NET Core surface); consequence at Compute/remote-lane#TRANSPORT_AXIS (RemoteTransport GrpcWeb unary-and-server-stream-only row over GrpcWebMode.GrpcWeb binary, ArtifactSync bidi and CaptureEvents client-stream structurally excluded). AppHost owns the co-host root, Compute owns the browser transport row; both ride the [WEB_ROOT] SPIKE.
- Control-plane verb triad: mechanics at Compute/remote-lane#PROTO_VOCABULARY (ControlService SetDegradation/ReloadOptions/CaptureSupport verbs project onto owned transitions, never new state machines); consequence at AppHost/configuration-and-options#KILL_SWITCH (set-degradation lands on the OperatorOverride forcing union under ControlTrigger) and #POLICY_VALUES (reload-options enqueues the same ReloadOutcome transition under ControlTrigger), AppHost/health-and-degradation#WIRE_HEALTH (DegradationCell.Force inbound route admits the level key through DegradationLevel.Validate), and AppHost/support-bundles#TRIGGER_UNION (SupportTrigger.ExternalCommand admits the capture-support verb). One override/reload/capture owner per concern; the wire verb is the service-modality route in, never a parallel owner. OperatorOverride is the forcing [Union] at #KILL_SWITCH (AH-04d) and the reload-options route-in lands ReloadOutcome at #POLICY_VALUES (AH-04c) — two distinct anchors, never re-minted.
- Recency-horizon ownership: mechanics at Persistence/cache-indexes#MODEL_RESULT_INDEX (ModelResultKey.RecencyHorizon is the suite's sole cross-process result-reuse recency bound); consequence at Compute/model-lane#RESULT_CACHE (CacheOps reads the horizon by reference, no second Duration horizon parameter) and Compute/receipts-and-benchmarks#BENCHMARK_CLAIMS (BenchmarkRow.Claim/HostFingerprint.Claim default the staleness horizon to ModelResultKey.RecencyHorizon, narrowable but never re-owned). Distinct from the Model-result cache split (above), which owns the cache read path; this split owns the horizon value every reuse and route-selection surface traces to.
- Geospatial chart feed: mechanics at Persistence/snapshot-codecs#CODEC_AXIS (GeoJsonProjection wraps one GeoJsonConverterFactory deriving the GeoJSON projection of every NetTopologySuite geometry riding the STJ rail, RFC-9746-orientation rings, SRID-4326 default GeometryFactory, bbox and id policy columns; it PROJECTS off the Compute GeometryPayload proto oneof and never mints a second geometry — XR7); consequence at AppUi/charts-dashboards#SERIES_TABLE (ChartSeriesSpec.Geo carries an absent series delegate and a GeoAssetKey resolved through the asset rank fold, feeding the GeoMap control off the proto GeometryPayload oneof; heat-land payload binding rides the GEO_PAYLOAD research item). Persistence owns the geometry codec, AppUi owns the chart row; geometry crosses as projected payload, never a shared geometry instance. The GeoJsonProjection.Factory ctor arity and GeometryFactory.Default SRID-4326 spelling ride the snapshot-codecs#RESEARCH [GEOMETRY_FACTORY_DEFAULT] item.
- Native pg enum/composite registration: mechanics at Persistence/schema-rail#EXTENSION_DDL (SchemaDdl.Enum with Of<TEnum> factory and SchemaDdl.Composite are the type-declaration rows; MapEnums/MapComposites fold each onto NpgsqlDataSourceBuilder and Declare folds HasPostgresEnum into model annotations); consequence at Persistence/store-profiles#PROFILE_AXIS (StoreRows.Postgres composes MapEnums(MapComposites(builder)) into the pg connect delegate) and Persistence/store-profiles#PROVISIONING_ROWS (ExtensionRequirement.VerifyTypes folds SchemaDdl.Enum/Composite names against the live source's resolved PostgresType set on the type-resolution open-proof). One declaration owner on schema-rail; the profile connect delegate and the provisioning verify fold consume the Enum/Composite symbols as settled vocabulary, never a second type-registration surface. The MapEnum<TEnum>/MapComposite arity and the CREATE TYPE emission path ride schema-rail#RESEARCH [ENUM_DDL] and [COMPOSITE_DDL]; the resolved-type accessor rides store-profiles#RESEARCH [TYPE_RESOLUTION_ACCESSOR].
- Watchdog liveness mirror: mechanics at AppHost/host-profiles#LIFETIME_ADAPTERS (ServiceNotify projects each RuntimePhase transition to its ServiceState sd_notify mirror — Ready on ready commit, Stopping on draining commit — and MirrorService registers one Lifecycle.Subscribe observer for service rows); consequence at AppHost/time-and-deadlines#SCHEDULE_PORT (the service-manager keep-alive rides the watchdog-heartbeat schedule row at 3 × health-probe rather than a second timer; Missed reads GetPreviousOccurrence against the last-fired stamp and Window audits GetOccurrences for the missed-occurrence sweep, Heartbeat folds a not-met receipt into SupportTrigger.WatchdogTimeout). One liveness mirror over the receipt and schedule streams; a watchdog is a heartbeat row plus a deadline class, never a service type. The watchdog-keepalive notify payload rides the [WATCHDOG_PING] SPIKE.
- Container-limit self-throttle: mechanics at AppHost/health-and-degradation#HEALTH_FOLD (PressurePolicy.Container grades CPU/memory utilization against SystemResources/ResourceQuota so a cgroup-throttled process degrades on its own limit, never the host total); consequence at Compute/scheduling-and-lanes#CPU_BUDGET (CpuBudget sources its processors input from the container-limit grade when a quota is present, so one limit signal re-caps lane readers, the ORT thread pool, and the partition cap together — a concurrency axis sized off the host total under a container limit is the named defect). One budget signal, one re-cap of every concurrency axis; both ride the [CONTAINER_LIMIT_READ] research item.

- Update apply asset: mechanics at AppHost/provisioning-and-update#UPDATE_RAIL (UpdateRail composes UpdateManager directly, ApplyUpdatesAndRestart takes found.TargetFullRelease as VelopackAsset, never the UpdateInfo; VelopackApp.Build()...Run() bootstrap and VelopackHook registration stay app-root-only, never a rail fence); the UpdateCheck(ReleaseIdentity) detect-leg stays the outbound poll at AppHost/outbound-resilience#HOP_AXIS — two altitudes, the detect hop never re-declares the post-fetch rail and the rail never re-polls the feed.
- Update rollover drain: mechanics at AppHost/lifecycle-and-drain#DRAIN_CONDUCTOR (DrainConductor.Drain over the frozen rank-band table); consequence at AppHost/provisioning-and-update#ROLLOVER_DRAIN (RolloverDrain.Conduct settles the drain before ApplyUpdatesAndRestart so the replaced process leaves no half-flushed write); the cooperative/forced budgets are the DrainCooperative/DrainForced DeadlineClass rows, never inline literals — the rollover composes the conductor, never a second drain owner.
- Inbound control-service ownership: mechanics at AppHost/companion-sidecar#CONTROL_SERVICE (ControlInbound is the serving-side handler folding SetDegradation onto DegradationCell.Force, ReloadOptions onto OptionsAdmission.Invalidate landing ReloadReceipt.ControlTrigger wrapping ReloadOutcome.Applied, CaptureSupport onto SupportTrigger.ExternalCommand+SupportCapture.Capture) and AppHost/companion-sidecar#SERVICE_HOST (ServiceHost AddGrpc/MapGrpcService<ControlServiceImpl>/HealthServiceImpl.SetStatus registration); the wire vocabulary stays at Compute/remote-lane#PROTO_VOCABULARY (ControlService proto + grpc.health.v1) and the owned transitions stay at AppHost/configuration-and-options#KILL_SWITCH (OperatorOverride under ControlTrigger), #POLICY_VALUES (ReloadOutcome under ControlTrigger), AppHost/health-and-degradation#WIRE_HEALTH (DegradationCell.Force) and #DEGRADATION_RAIL, AppHost/support-bundles#TRIGGER_UNION (SupportTrigger.ExternalCommand) — the verb is the service-modality route in, the handler folds onto each existing owner, never a ControlReceipt ledger or a second state machine. This split RESOLVES the prior control-plane-verb-triad seam's open handler-ownership question: the inbound handler now lives at companion-sidecar#CONTROL_SERVICE, not a thin row on health-and-degradation#WIRE_HEALTH.
- Cross-process degradation cascade: mechanics READ at AppHost/health-and-degradation#DEGRADATION_RAIL (this process's own derived Level value, the DegradationCell.Cascade floor admit, the Derive fold keeping the worse of cascaded and derived ranks); WRITE at AppHost/companion-sidecar#DEGRADATION_CASCADE (DegradationCascade.Cascade forwards a parent's effective level to a child over the control hop on DegradesChild-eligible rows, Apply calls DegradationCell.Cascade; release passes None and the cell re-derives off its own snapshots) — the cascade is a write consumer of the existing cell, never a second DegradationLevel or DegradationCell owner; the cross-process wire delivery rides the [CASCADE_CONVERGENCE] tier-3 live-host SPIKE.
- Accept-side peer credential: mechanics at AppHost/companion-sidecar#PEER_ADMISSION (PeerAdmission.Read over the platform getsockopt P/Invoke — Linux SOL_SOCKET=1/SO_PEERCRED=17 fills Ucred 12 bytes, macOS SOL_LOCAL=0/LOCAL_PEERCRED=1 fills Xucred with XUCRED_VERSION=0; the managed Socket.GetSocketOption path is rejected, SocketOptionName.BlockSource=17 shares the integer only by coincidence); the DiscoveryManifest self-asserted pid at AppHost/outbound-resilience#DISCOVERY_ATTACH defers to this kernel-reported credential — the serving side reads the connecting peer's uid and pid once at accept and never trusts the manifest. The accept-side peer credential is owned at companion-sidecar#PEER_ADMISSION; the outbound discovery manifest defers to it.
- Parent-to-child drain fan: mechanics at AppHost/outbound-resilience#DISCOVERY_ATTACH (Discovery.DrainFan runs the LocalIpc hop with a drain control message through OutboundSurface.Run, FanOf threads it into CompanionChild.FanDrain as the drainFan arg); consequence at AppHost/companion-sidecar#PROCESS_MODALITY (ModalityRows.Attach passes the drainFan delegate into Discovery.Spawn so the modality row's child carries a real drain producer) and AppHost/provisioning-and-update#ROLLOVER_DRAIN (a fleet node's parent drain registration fans the signal to the child over the local-ipc hop before the parent rolls over) — one drain-fan producer, two consumers, never a dangling delegate field.

- Numeric solve wire leg: mechanics at Compute/numeric-lane#KERNEL_LOWERING (ShardPlan.Blocked row-block GEMM fans each sub-block sub-solve through Substrate.RemoteGrpc) + the SolveRequest/SolveResponse shape at Compute/remote-lane#PROTO_VOCABULARY (ComputeService.Solve unary, numeric decomposition field-for-field); the join is a Factorization-receipt aggregation. Compute owns the wire frame and the fan-out column; no FarmRouter, no second substrate.
- Graph-diff wire vs diff algebra: mechanics (wire frame) at Compute/remote-lane#PROTO_VOCABULARY (ComputeService.GraphDiff/SubtreeFetch rpcs + messages carrying the content-key delta field-for-field); consequence (diff computation) at Persistence/sync-collaboration#TRANSPORT_AXIS (the CopyGraph set-difference over Closure that produces the added/removed content-key set). Compute owns the wire shape, Persistence owns the diff algebra.
- Sub-block content-address reuse: mechanics at Compute/numeric-lane#KERNEL_LOWERING (each ShardPlan.Blocked sub-block keys on a sub-block content-address); consequence at Persistence/cache-indexes#MODEL_RESULT_INDEX (by reference — a re-run reuses computed blocks). Numeric lane reads the index, never mints a second cache.
- Numeric provider-rank claim authority: mechanics at Persistence (BenchmarkRow.Claim resolves the most-recent fingerprint-matching row against ModelResultKey.RecencyHorizon); consequence at Compute/numeric-lane#PROVIDER_CLAIMS (LinearProvider.Select consumes the resolved Option<BenchmarkRow> claim, never re-resolves; BenchmarkClaim.Provider carries the winning LinearProvider key). One horizon owner, one resolver; native-BLAS rank is fingerprint-gated like a SIMD route.
- Remote token-streaming leg: mechanics at Compute/model-lane#GENERATIVE_RUN (GenerativeRun.Stream IAsyncEnumerable<string> token loop) + the wire frame at Compute/remote-lane#PROTO_VOCABULARY (ComputeService.Generate server-stream GenerateRequest->TokenChunk riding the Progress-class server-stream pattern). The generate request mirrors GenerationPolicy field-for-field; no second request shape, no chat-client wire surface.
- Generative tool-call dispatch: mechanics (the loop pause + SetGuidance grammar + AppendTokens resume) at Compute/model-lane#GENERATIVE_RUN; consequence (the parsed tool request dispatch) at AppHost/companion-sidecar#CONTROL_SERVICE. Loop owned here, dispatch there.
- Object-store residence behind BlobRemote: mechanics at Persistence/remote-stores#OBJECT_STORE (ObjectStore SmartEnum projects a BlobRemote placement per provider via the ObjectClient resolved-SDK union; one MultipartTransfer algebra dispatches on the row, never three upload services); consequence at Persistence/store-profiles#PROFILE_AXIS (the blob-remote profile row's BlobRemote set is the ObjectStore provider rows; StoreRows.NoConnection stays correct — object-store residence carries no DbConnection). One blob contract; the prior single deferred app-root registration row is deleted.
- Object-store sync-hub op-log seam: mechanics at Persistence/sync-collaboration#OPLOG_CHANGEFEED (the store-assigned Sequence + HLC-stamped op-log changefeed is the one durable transport); consequence at Persistence/remote-stores#ARTIFACT_SYNC_FEED (ArtifactSyncFeed.Announce appends one content-key-keyed op-log row after the seal so a peer's changefeed cursor advances and the blob is fetched by content; never a second sync engine). The HLC stamp stays the causal primitive.
- Object-store frame integrity: mechanics at Compute/remote-lane#ARTIFACT_FRAMES (64-KiB frame width, Crc32-per-frame, XxHash128 whole-artifact identity); consequence at Persistence/remote-stores#MULTIPART_TRANSFER (MultipartTransfer.Windows partitions each part/chunk window to span a whole number of 64-KiB frames; #ARTIFACT_SYNC_FEED surfaces Crc32/XxHash128 as fact rows). Compute owns the frame constants, remote-stores consumes them, never re-declaring a frame width or a second hash.
- Tenancy threading to RLS: mechanics at AppHost/runtime-ports#PORT_RECORDS (TenantContext is the tenancy-threading port record); consequence at Persistence/server-tier#TENANCY_RLS (RlsPolicy.Policy emits CREATE POLICY USING (tenant = current_setting('rasm.tenant')::uuid) consuming the tenant id) and Persistence/cache-indexes (per-tenant content-address cache-key partitioning). AppHost owns the tenancy port, server-tier consumes the tenant id as settled vocabulary, never minting it. (TenantContext is a new 4th CROSS_PACKAGE_LAW peer — authored on AppHost at Gate 2D.)
- RLS policy to audit-category binding: mechanics at Persistence/server-tier#TENANCY_RLS (RlsPolicy owns the per-tenant CREATE POLICY mechanics + the TenancyModel single/schema/rls/db-per-tenant axis); consequence at Persistence/redaction-retention#AUDIT_BINDING (AuditBinding.BindTenant ties a tenant-id-bearing classification to one pgaudit category). server-tier owns the RLS mechanics, redaction-retention owns the audit-category consequence.
- Redis L2 residence: mechanics at AppHost/resource-lanes#CACHE_PORT (CacheLane/CacheSurface own the cache port, stampede, tags); consequence at Persistence/cache-indexes#L2_CONTRIBUTION (CacheResidence registration-row axis selects which IDistributedCache the one CacheContribution capsule reads — Redis registers RedisCache : IBufferDistributedCache via AddStackExchangeRedisCache, content-address-keyed so an L1 miss promotes from any tier without a re-mint). AppHost owns the port, Persistence contributes the residence row, never a second cache owner.
- AppUi custom-visual encode-path consumption: mechanics at AppUi/visuals-offscreen#DRAW_CAPSULE (DrawSource.Owned the only Skia-surface owner) and #ENCODE_IDENTITY (VisualCodec.Encode the only encode path, VisualRuntime.Measure the named-instrument phase-duration record); consequence at AppUi/custom-visuals#SKIA_KINDS (CustomVisual.Materialize composes the layout fold through DrawSource.Owned.Materialize then VisualCodec.Encode at the ColorSpaceAxis row, folding layout-fold elapsed onto the layout-elapsed instrument distinctly from encode-elapsed) — custom-visuals mints zero Skia-surface or encode owner.
- AppUi wide-gamut color-space vocabulary: mechanics at AppUi/custom-visuals#COLOR_SPACE (ColorSpaceAxis the single suite-wide 4-row gamut vocabulary carrying the SKColorSpace working-space factory + SKColorType surface + VisualCodec.EncodeRow); consequence at AppUi/visuals-offscreen#ENCODE_IDENTITY (VisualCodec.ColorPolicy DisplayP3/Rec2020/ScrgbFloat rows are the encode-side projection) and AppUi/diagnostics-evidence#RECEIPT_UNION (RenderReceipt.ColorSpace tags one of the four axis keys) — one color-space vocabulary, no parallel Gamut enum.
- AppUi custom-tile board placement: mechanics at AppUi/charts-dashboards#DASHBOARD_TILES (DashboardTile.Custom fifth case places a CustomVisual kind in a board, analytical-flow named dashboard over the residence-selected feed); consequence at AppUi/custom-visuals#SKIA_KINDS (the custom kind supplies its render twin) — the custom tile is a DashboardTile case, never a fork of ChartSeriesSpec.
- AppUi custom-visual telemetry contribution: mechanics at AppUi/diagnostics-evidence#RECEIPT_UNION (AppUiTelemetry.Contribute the one parameterized TelemetryContributorPort factory); consequence at AppUi/custom-visuals#SKIA_KINDS (CustomVisuals.TelemetryRow contributes rasm.appui.customvisual.rendered + rasm.appui.customvisual.layout-elapsed) — instrument names owned by the contributing page, no per-owner port factory and no local meter.

- W3C trace-context propagation: mechanics at AppHost/diagnostics-and-telemetry#CORRELATION_SPINE (TraceContext.Inject/Extract/Continue over the Correlation composite, registered as Propagators.DefaultTextMapPropagator with the TraceContextPropagator+BaggagePropagator pair); consequence at AppHost/companion-sidecar#CONTROL_SERVICE (the inbound handler calls TraceContext.Continue(Metadata, name) to extract the parent ActivityContext + seed Baggage and start the continued server Activity; the outbound dial calls Inject) — propagation owned at the spine, the gRPC-metadata crossing consumed at the control hop, never a parallel identifier or a flat per-process trace. The Grpc.Core.Api Metadata carrier rides the G7 spec-compile gate ([GRPC_METADATA_CARRIER]).
- Multi-peer session roster: mechanics at AppHost/companion-sidecar#PROCESS_MODALITY (PeerRoster.Boot/Admit/Renew/Drop/Sweep is the single host-side attached-peer/lease-epoch roster, RosterReceipt the per-mutation projection); consequence at Persistence/sync-collaboration#PRESENCE_AND_BLOB (presence VALUE rides the op-log, store-borne) and at AppHost/provisioning-and-update#ROLLOVER_DRAIN (FleetRoll reads PeerRoster.Attached as wave membership) and AppHost/health-and-degradation#WIRE_HEALTH (per-peer serving status) — one roster owner; the host-side connection/lease state is AppHost-owned, the presence value is the Persistence op-log consequence, never a second roster.
- Fleet-wide rolling-update wave: mechanics at AppHost/provisioning-and-update#ROLLOVER_DRAIN (FleetRoll.Roll paces the health-gated wave, each node's roll is RolloverDrain.Conduct dialed over the control hop, FleetRollReceipt the per-wave projection); consequence reads AppHost/companion-sidecar#PROCESS_MODALITY (PeerRoster.Attached membership) and AppHost/health-and-degradation#WIRE_HEALTH (WireHealthRow per-node recovery gate halting the wave on the first unrecovered node) — a conductor over existing owners, never a new update surface or parallel fleet instrument.
- Tenant lifecycle + quota: mechanics at Persistence/server-tier#TENANCY_RLS (TenantProvision.Provision/Destroy per-TenancyModel DDL + TenantQuota ALTER ROLE CONNECTION LIMIT/statement_timeout consuming the resolved TenantContext slug/id); consequence at AppHost/runtime-ports#PORT_RECORDS (TenantContext.TenantId/Slug resolution), Persistence/query-rail#BULK_LANE (runtime write-path quota enforcement — one bound, never a second limiter), and Persistence/redaction-retention#AUDIT_BINDING (AuditBinding.BindTenant per-tenant pgaudit category). Lifecycle+quota owned at server-tier, the identity at AppHost, enforcement and audit are downstream consequences.
- Hybrid RAG retrieve: mechanics at Persistence/data-lanes#SEARCH_LANES (HybridRetrieve.Fuse composes the dense diskann VectorQuery branch over the EmbeddingArity column and the pg_search BM25 branch into one reciprocal-rank-fusion CTE, rrfConstant 60, degrades to native ts_rank without pg_search); consequence at Compute/model-lane#INFERENCE_MODES (the dense embedding the vector branch probes is generated upstream by the Embed run mode) and Persistence/server-tier#SEARCH_PROVISIONING (the diskann + BM25 index DDL) — retrieval+fusion owned at Persistence, embedding generation at Compute, the index DDL at server-tier; never a third index or a learned reranker.
- Spatial change-detection: mechanics at Persistence/data-lanes#GEO_LANES (SpatialOp proximity/coverage/spatial-join family over GeoLayer + PostGIS/NTS, SpatialDiff change-detection fold over op-log OLD/NEW geometry producing SpatialDelta); consequence at Persistence/server-tier#SEARCH_PROVISIONING (GiST/spatial index DDL) and AppUi/charts-dashboards#SERIES_TABLE (GeoOverlay reads the SpatialDiff IChangeSet for the live geometry overlay) — geo ops owned at Persistence, index DDL and chart overlay are downstream.
- Cross-tile linked brushing: mechanics at AppUi/charts-dashboards#DASHBOARD_TILES (CrossFilter/FilterState board-scoped brush over one BehaviorSubject<FilterState> driving the DynamicData dynamic-predicate Filter across non-source tiles on the shared Connect() spine; source tile self-excludes); consequence at Persistence/query-rail#PROJECTION_SHAPES (FilterPredicate server-side filtered re-query on KeysetPage<TRow>) — brush state owned at AppUi, the re-query owned at Persistence, never re-minted.
- Board layout + filter-state persistence: mechanics at AppUi/charts-dashboards#STREAM_BINDING (BoardState snapshot of DashboardLayout + FilterState through the STJ IDockSerializer with brush reapply at mount); consumes the IDockSerializer four-method contract owned at AppUi/shell-navigation#DOCK_LAYOUTS (distinct from pane docking, no second serializer).
- Live geometry overlay: mechanics at AppUi/charts-dashboards#SERIES_TABLE (GeoOverlay incremental land swap onto HeatLandSeries.Lands over the shared IChangeSet/Connect() spine under the SyncContext lock, never a DrawnMap re-load); consequence at Persistence/data-lanes#GEO_LANES (SpatialDiff produces the IChangeSet<HeatLand,string>), the proto-to-land projection riding the settled GEO_PAYLOAD wire boundary.

- Capability command-to-compute dispatch: mechanics at AppHost/capability-registry#COMMAND_ALGEBRA (CommandAlgebra wraps any CapabilityDescriptor invocation in a commit-or-rollback CommandTxn, threading the invocation through GrantBroker admission); consequence at Compute/intent-and-selection#INTENT_FAMILY (the descriptor invocation lands on the existing ComputeIntent/IntentAdmission dispatch rail and Compute/scheduling-and-lanes#LANE_AXIS WorkLane enqueue) — AppHost owns the capability catalog and the transaction, Compute owns the dispatch; the command is a route onto the intent rail, never a parallel compute entrypoint.
- Capability cost-and-tenancy threading: mechanics at AppHost/capability-registry#GRANT_BROKER (GrantBroker meters every admission against an object-set × op-class × cost-ceiling × time-window GrantScope with Budget metering and Consent elevation); consequence at AppHost/runtime-ports#PORT_RECORDS (TenantContext scopes the GrantScope and the Budget per tenant) and AppHost/diagnostics-and-telemetry#REDACTION_TAXONOMY (DataClassification gates the descriptor permission shape) — the broker consumes the tenancy identity and the classification taxonomy as settled vocabulary, minting no second limiter or classification axis.
- MCP tool projection: mechanics at AppHost/mcp-projection#METHOD_AXIS (ToolProjection folds CapabilityRegistry descriptors into the MCP tool/resource/prompt catalog) and #TOOL_DISPATCH (McpDispatch brokers the call through CommandAlgebra and the dry-run CostPreview through GrantBroker); the streaming leg at #STREAM_PROGRESS rides AppHost/outbound-resilience#HOP_AXIS (OutboundHop.ServerStream) and AppHost/lifecycle-and-drain#CANCEL_SPINE (CancelScope) — the MCP surface is a projection of the registry and a route over the existing server-stream and cancel spines, never a new transport or a parallel tool registry.
- Sandbox capability mediation: mechanics at AppHost/sandbox-host#GRANT_HANDLE (GrantHandle is the only authority a PluginInstance reaches host functionality through, BrokeredCall mediating each call); consequence at AppHost/capability-registry#GRANT_BROKER (the handle is scoped by a GrantScope the broker issues) and AppHost/companion-sidecar#PEER_ADMISSION (an out-of-process isolation row admits the child through the existing PeerAdmission credential read over OutboundHop.CompanionSpawn/Discovery) — the sandbox consumes the grant broker and the peer-admission read as settled, minting no second authority or spawn surface.
- Solver-plugin representation negotiation: mechanics at AppHost/solver-plugin#SOLVER_HOSTING (SolverHosting loads a verified plugin under SandboxIsolation and proves its declared representation contract against the canonical Compute encoding before first solve, projecting OpDeclaration rows into CapabilityRegistry); consequence at Compute/tensor-lane#GEOMETRY_ENCODING (GeometryEncoding/EncodedTensor are the canonical representation the Negotiation validates against) and Compute/intent-and-selection#INTENT_FAMILY (the hosted solver's ops dispatch through the same ComputeIntent rail) — AppHost owns the plugin contract and the sandboxed hosting, Compute owns the canonical encoding and the dispatch rail.
- Live-wire edge unit coercion: mechanics at AppHost/live-wire#BINDING_SPEC (every inbound ExternalValue coerces its unit at the edge into a CoercedValue before it enters the suite); consequence at Compute/units-boundary#DIMENSIONAL_LAW (the coercion runs through the existing UnitAlgebra over QuantityFamily/UnitPolicy at admission) — AppHost owns the binding edge, Compute owns the dimensional law; the unit coercion is the one admission fold, never a second unit owner.
- Determinism event-log over the changefeed: mechanics at AppHost/determinism-and-replay#EVENT_LOG (EventLog appends every executed command as a content-addressed LogEntry whose ContentHash chains to its predecessor, ReplayVerify re-executes and proves per-step content-hash identity); consequence at Persistence/sync-collaboration#OPLOG_CHANGEFEED (the durable transport is the existing HLC-stamped OpLog/OpLogEntry, the command log is a content-address chain riding the changefeed) and AppHost/runtime-ports#PORT_RECORDS (the HLC stamp arrives on ReceiptEnvelope/ReceiptSinkPort) — AppHost owns the determinism kernel and the replay proof, Persistence owns the durable changefeed; never a second log store.
- Notebook reproducible-cell pin: mechanics at AppUi/notebook-document#CELL_MODEL (each NotebookCell pins a CapabilityPin fingerprint and ReplayBundle exports the notebook bit-identically); consequence at AppHost/capability-registry#DESCRIPTOR_AXIS (the pin is a CapabilityDescriptor fingerprint plus the receipt determinism) and Persistence/sync-collaboration#OPLOG_CHANGEFEED (NotebookCrdt co-edit merge and the replay bundle ride the op-log) — AppUi owns the document model, AppHost owns the capability fingerprint, Persistence owns the op-log. The notebook cell-edit delta is NotebookOp (projecting onto the Persistence OpLogEntry/version-control#CRDT_ALGEBRA CrdtOp at the seam) and the bit-identity check is NotebookReplay — suite-unique names distinct from version-control CrdtOp and determinism-and-replay ReplayVerify.
- Viewport Compute-receipt render: mechanics at AppUi/viewport-pipeline#SIM_VISUAL (SimVisual renders isosurface/volume/streamline/glyph/deformation off a SimField projection of the Compute field receipts) and #RENDER_GRAPH (RenderGraph leases the host-shared GRContext through the embed capsule); consequence at Compute/receipts-and-benchmarks#RECEIPT_UNION (the field receipt the SimField projects), Compute/solver-and-optimization#DISCRETIZATION_MESH (the FieldSpace the receipt carries), and AppHost/runtime-ports#PORT_RECORDS (FrameReceipt sinks through ReceiptSinkPort, the frame-budget rides the deadline class) — AppUi owns the GPU pipeline and the viewpoint codec, Compute owns the field receipt and field space, AppHost owns the receipt sink; GPU passes ride the live-GPU-context SPIKE.
- Viewpoint cross-page reuse: mechanics at AppUi/viewport-pipeline#VIEWPOINT_CODEC (Viewpoint is the one portable BCF-compatible view-state receipt with section-box/visibility/color-override); consequence at AppUi/animation-timeline#TRACK_MODEL (camera Track keyframes interpolate Viewpoint cameras), AppUi/drafting-sheets#PROJECTION (ProjectionBasis derives the view direction from a Viewpoint), and Persistence/annotation#BCF_PROTOCOL (BcfViewpoint mirrors the same portable receipt shape) — one viewpoint receipt owned at the viewport pipeline, consumed by the animation, drafting, and annotation surfaces; BcfViewpoint is the Persistence-side BCF projection, never a forked geometry.
- SimField scrub reuse: mechanics at AppUi/viewport-pipeline#SIM_VISUAL (SimField is the Compute field-receipt projection indexed by frame); consequence at AppUi/animation-timeline#SCRUB (Scrub indexes the transient field by SimField frame for kinematic and transient-field scrubbing) — one field projection owned at the viewport pipeline, the animation scrub reads it by frame index, never a second field carrier.
- Interchange artifact content-addressing: mechanics at Compute/interchange#CONTENT_ADDRESSING (InterchangeIdentity folds artifact bytes plus deflection and tolerance into one XxHash128 content key, mirroring ModelIdentity.Snapshot); consequence at Persistence/cache-indexes#ARTIFACT_BLOB_INDEX (the artifact lands content-addressed through ArtifactIndexRow.Admit) — Compute owns the identity derivation, Persistence owns blob residence; never a second cache, mirroring the existing EP-context warm-start blob split.
- Interchange field-codec solver handoff: mechanics at Compute/interchange#FIELD_RESULT_CODEC (FieldCodec projects a FieldSpace-shaped result into a Zarr/VTK-class chunked layout with error-bounded lossy/exact lossless residence and a zero-copy solver↔store↔viz handoff); consequence at Compute/solver-and-optimization#DISCRETIZATION_MESH (the FieldSpace the SolveResult writes) and AppUi/viewport-pipeline#SIM_VISUAL (the SimVisual volume/isosurface render reads the chunked field) — the interchange codec owns the chunked field layout, the solver owns the field space, the viewport owns the render; one field shape crosses all three by reference.
- Interchange two-hop tessellation: mechanics at Compute/interchange#TWO_HOP_TESSELLATION (TessellationRequest crosses IFC geometry evaluation to the IfcOpenShell companion via IfcConvert producing GLB then re-imports through the glTF IMPORT_RAIL path); consequence at Compute/remote-lane#TRANSPORT_AXIS (the request rides the existing Substrate.RemoteGrpc companion rpc, host-local in posture) — interchange owns the bridge, remote-lane owns the transport; never a new transport.
- Solver compute-machinery composition: mechanics at Compute/solver-and-optimization#SOLVE_CONTRACT (SolveLane assembles the discrete operator over a DiscreteMesh and dispatches to the numeric solve) and #SWEEP_AND_BUDGET (SweepLane fans grid points onto the scheduler); consequence at Compute/numeric-lane#DENSE_ALGEBRA + #SPARSE_SOLVE + #KERNEL_LOWERING (the Factorization/SparseOps/ShardPlan solve machinery and the remote-lane Solve rpc fan-out), Compute/scheduling-and-lanes#LANE_AXIS (WorkLane/LaneRuntime), Compute/receipts-and-benchmarks#BENCHMARK_CLAIMS (the fingerprint gate) and Persistence/cache-indexes#MODEL_RESULT_INDEX + Persistence/data-lanes#SEARCH_LANES (the artifact and vector indexes) — the solver lane composes the existing numeric, scheduler, receipt, and persistence owners, minting no second solve or cache surface.
- Digital-twin live-signal loop: mechanics at Compute/solver-and-optimization#CLASH_AND_TWIN (DigitalTwin scores a TwinSignal against a Surrogate-evaluated simulated baseline emitting an anomaly verdict plus a control suggestion, ClashScale runs the collision-compute over the persistent acceleration structure); consequence at Persistence/federation#ENTITY_GRAPH (the federated geometry the AccelerationStructure indexes) — Compute owns the twin loop and clash compute, Persistence owns the federated geometry; the twin reads the federated graph by reference.
- Annotation CDE outbound sync: mechanics at Persistence/annotation#CDE_SYNC (CdeSync runs the bidirectional BCF-API topic/comment/viewpoint sync with OAuth2 token threading and conflict-aware merge); consequence at AppHost/outbound-resilience#HOP_AXIS (the BCF-API REST calls and the OAuth2 token mint ride the existing outbound hop) and AppUi/inspector-editing#CONFLICT_RESOLUTION (the CDE-side/local-side conflict surfaces through the existing conflict pane) — Persistence owns the CDE protocol and merge, AppHost owns the outbound hop, AppUi owns the conflict UI; never a second HTTP client.
- Annotation overlay consumption: mechanics at Persistence/annotation#ANCHOR_ALGEBRA (Anchor binds an annotation to a node/sub-entity/parameter/world-point target with re-anchoring against the structural diff, Thread carries the comment fold); consequence at AppUi (the pins/markup overlay renders anchored threads against the live model) and the TS-web issue board — the annotation backbone is owned at Persistence, the AppUi overlay and the web board consume the wire projection, never re-minting the anchor or thread shapes.
- Schedule activity-bytes companion: mechanics at Persistence/schedule-interchange#SCHEDULE_STORE (ScheduleImport ingests the canonical P6 XER / MS-Project XML activity bytes through the Sep tabular reader); consequence at Compute (the P6/XER parse companion produces the canonical activity bytes) — Persistence owns the store and the 4D fold, the Compute companion owns the foreign-format parse; the parsed bytes cross host-local, never a parallel schedule parser in Persistence.
- CRDT wire-vocabulary amendment: mechanics at Persistence/version-control#CRDT_ALGEBRA (Crdt.Merge is the join-semilattice LUB and CrdtField the convergent field family; OpLogEntry.Payload now carries a CrdtOp delta for column-family=crdt rows); consequence at AppHost/runtime-ports#WIRE_LAW (the one suite wire vocabulary gains the CrdtOpWire discriminated union, a breaking CROSS_PACKAGE_LAW amendment the TS-web and Python legs decode) and Persistence/sync-collaboration#MERGE_LAW (the prior LWW Adjudicate scalar survives only as the LwwRegister CRDT arm) — version-control owns the CRDT algebra, the wire law owns the cross-package vocabulary change; an additive parallel surface is the rejected form. The notebook-document cell-edit delta is the suite-unique NotebookOp (AppUi/notebook-document#CRDT_COEDIT), projecting onto this CrdtOp at the op-log seam — not a re-mint of this name.