# [RASM_PY_RUNTIME_BRIEF]

Campaign: py track 1/5 — run FIRST (data/geometry/compute/artifacts consume its exports). Single-phase: `Workflow(rebuild.js, {targets: "libs/python/runtime", brief: "RASM-PY-RUNTIME-BRIEF.md"})`, legs per `[05]`; hard residuals between legs via `resolve-residuals.js`.

Authoritative campaign input for the ground-up restructure and rebuild of `libs/python/runtime/.planning/`. The campaign is single-phase: the page-set delta is small and fully ruled here (three NEW pages, two vocabulary re-homings, zero deletions), so no ephemeral DECISION pass runs — the durable rebuild engine takes this brief directly, its plan step classifying per-page kind from `[01]` verdicts against disk, its legs running the `[05]` partition in order. `runtime` is the FIRST folder in the Python campaign sequence (runtime -> data -> geometry -> compute); every later folder brief inherits this one as upstream consumer pressure. `docs/stacks/python/` governs unchanged and is never restated here.

## [00]-[SHARED_LAW]

### [VERDICT]

The corpus is page-level excellent and graph-level broken: 13 dense, doctrine-fluent pages whose composed module graph cannot import, whose wire codec is generic over a vocabulary that does not exist, and whose README/manifest/`.api` tier asserts an execution rail no page owns. Four proofs, each independently fatal to a world-class claim:

1. **The module graph crashes at import time.** Six of thirteen modules form one strongly-connected import cluster — every edge an eager `from X import <name>`: `lanes ⇄ metrics` (`execution/lanes.md:51` imports `Metrics`; `observability/metrics.md:40` imports `DRAIN_COLUMNS`/`DrainOutcome`/`DrainReceipt` and constructs `ZERO_DRAIN` at load, `metrics.md:85`), `lanes ⇄ receipts` (`lanes.md:52` ⟷ `receipts.md:53`, the `drained` case binding `DrainReceipt` at class-body time), `resilience -> metrics -> lanes -> resilience` (`resilience.md:43` plus the module-level `Metrics.retry_hook()` CALL inside `RETRY_HOOKS`, `resilience.md:305`; `lanes.md:53`), and `admission -> resilience -> metrics -> telemetry -> admission` (`admission.md:237`; `metrics.md:41` importing `latched`; `telemetry.md:74` importing `PROFILE_POLICY`). Whichever module loads first, the re-entrant name import raises `ImportError: cannot import name … from partially initialized module`. The foundation every Python sibling composes is unimportable as designed, and no page acknowledges the cycle.
2. **The wire vocabulary is phantom.** `transport/wire.md:116-119` imports 16 canonical `msgspec.Struct` wire types from `rasm.runtime.shapes`; `_PROTO_VOCABULARY` (`wire.md:164-183`) binds each to a `channels_pb2` message; `serve`'s whole dispatch is generic over them. NO `shapes` page, codemap node, or fence-declared class exists anywhere in the corpus — `FaultDetail`'s nine fields live only in prose (`wire.md:102`), and the generated `rasm.runtime._pb2.channels_pb2` module (`wire.md:114`) has no owner fixing the codegen contract. The transport plane is a codec with no vocabulary: its entire interior half is illusory.
3. **The recipe rail is an owner-less assertion.** Three admitted, `[RUNTIME]`-tagged packages (`pyproject.toml:140-142`) carry full catalogs each chartering "the runtime recipe owner" (`.api/queenbee.md:3`, `.api/lbt-recipes.md:3`, `.api/pollination-handlers.md:3` — 385 catalog LOC wiring lanes/deadline/stamina/receipt rails), and the README `[02]`-`[RECIPE]` group lists all three — yet `ARCHITECTURE.md` maps 6 sub-domains/13 pages with no recipe node, the README router has no recipe route, and zero fences import any of the three. A declared execution capability of the folder exists in no code fence.
4. **The governance surfaces lie about the folder.** The seam ledger files the C# credential-axis decode on the wrong owner (`ARCH:41` `admission ← CredentialPem`; admission carries no such wire — the real decoder is `serve.md:69-89`'s five-row `CredentialPolicy` union, undeclared in serve's rows) and double-attributes clock's seam (`ARCH:40` ⇄ `CausalFrame` duplicating `ARCH:37`); the codemap draws nested modules (`observability/receipts.py`, `ARCHITECTURE.md:9-30`) while 100% of fence imports are flat `rasm.runtime.<leaf>`; the codemap's `identity.py` node is spelled `rasm.runtime.content_identity` by every consuming fence (`lanes.md:49`), and `identity.md`'s own §3 prelude imports the module §2 defines (`identity.md:226`) — a self-import proving an undeclared second module.

The corpus also carries systemic prose rot: ten of thirteen pages end in a forbidden `[RESEARCH]` provenance/checklist tail (two of them EMPTY headers — `identity.md:373`, `wire.md:392`), and the region block schema restates single invariants 4-7x per page; the once-stale admission `RetryClass.SECRET` seam claim has healed on disk (admission consumes the landed row, `admission.md:237,378-380` vs `resilience.md:68,247` — E5 records the closure `[V12]` holds). What IS sound: `faults.md` (the linchpin rail — CLASSIFY order-law, disposition overloads), `clock.md` (single-mint decode exemplar), `roots.md` (credential-leak-proof pooling, deterministic teardown), the table-driven generator discipline corpus-wide (`SLOTS`/`CLASSIFY`/`POLICY`/`INSTRUMENTS`/`SIGNAL_SPECS`/`ADMIT_TABLE`/`SECRET_LADDER`/`_PROTO_VOCABULARY`), and the C# FIT (single-mint held end-to-end; the count-prefix wire law correctly recorded as a DESIGN-PIN obligation, never fabricated bytes). The rot is structural — graph, phantom owners, misfiled seams, base-tier coupling — and no per-page cold pass fixes it. Graph first.

### [TELOS]

`runtime` is stratum zero of the Python branch: the host-free execution foundation every `libs/python` sibling composes — the fault rail, retry table, logical clock, content identity, admission, lanes, receipts/metrics/telemetry, transport roots, wire codec, and companion server. World-class here means FOUNDATION-GRADE PROOF, not feature reach: the module set imports in isolation with zero sibling packages installed; the import graph is a provable DAG with a stated topological order; every shared vocabulary is minted exactly once and consumed by symbol; every cross-`libs/` seam decodes a C#-minted shape and re-mints nothing; every declared capability — recipe rail, wire vocabulary, health service, worker band — is a fence, never a catalog assertion. Assume 5x the consumer demand on every surface: artifacts/data/geometry/compute pipelines schedule through `lanes`, every sibling fold returns through `faults`, every retry in the branch rides `POLICY`, the assay rail reads `evidence`, the C# host drives `serve` — a defect here multiplies across the branch, and zero current consumers never lowers the bar. The output bar is the composed daemon: a companion process that installs telemetry, admits settings, serves the C# wire, schedules keyed work with elision, executes recipes, and drains deterministically — every step railed, traced, and receipted through the one rail family.

### [STRUCTURAL_AUTHORITY]

The campaign owns the page-set absolutely: split any page, merge pages, move concerns across sub-domain boundaries, mint new pages, re-home vocabularies. The ruled deltas — NEW `transport/shapes.md`, NEW `execution/recipe.md`, `evidence/identity.md` splitting out `evidence/reproduction.md`, the drain taxonomy re-homing into `receipts`, `latched` re-homing into `faults` — are the floor, not the ceiling. Every structural change updates `ARCHITECTURE.md` `[01]-[DOMAIN_MAP]`+`[02]-[SEAMS]`, the `README.md` router and package groups, and the `.api` tier in the same motion. No page survives on incumbency: a page that exists is not evidence it belongs, and a catalog that exists is not evidence its owner does.

### [IMPORT_DAG_LAW]

The realized package is FLAT (`rasm.runtime.<leaf>`); the corrected codemap names the real module set and the import graph honors one topological order — an edge into a later tier is a defect resolved by re-homing the vocabulary or re-tiering the consumer, never tolerated:

```text
faults < clock < identity < receipts < metrics < resilience < admission < telemetry
       < {reproduction, evidence, roots, shapes} < wire < lanes < recipe < serve
```

The chain is the canonical topological ORDER, not an edge list: a fence import binds only a strictly-earlier module; braced peers are mutually import-free and unordered among themselves; the generated `rasm.runtime._pb2` package imports nothing intra-package and sits immediately below `shapes`.

Three re-homings make it hold: the drain vocabulary (`DrainOutcome`/`DRAIN_COLUMNS`/`DrainReceipt`, today `lanes.md:60,79,94-102`) moves to `receipts` — a drain receipt IS local evidence, the `Receipt.drained` case already binds it at load, and `receipts -> identity` (for the `cache: Map[ContentKey, T]` column) points downward; `latched` (today `telemetry.md:222-237`, a zero-dependency generic one-shot aspect) moves to `faults` beside the other cross-cutting rails, severing `metrics -> telemetry`; `SeedReproduction` becomes the declared second module `reproduction.py` — its `identity.md:239-241` prelude already imports `identity` AND `receipts`, which is a hard cycle inside one file and a valid module above `receipts` as two. `lanes`' `drained`/`feed` observers (`lanes.md:227-262`) and resilience's `RETRY_HOOKS` composition (`resilience.md:303-307`) then point strictly downward and stay where they are. Every rebuilt page proves its prelude against this order; the campaign's terminal reconcile re-derives the graph from every fence and rejects any back-edge.

### [GENERATOR_LAW]

The corpus's table discipline is its strength — preserve it and close the residuals. Rosters are seed DATA over a generative shape; the test: adding the next instance is one row, never a second declaration site, a new classmethod, or a hand-mirrored literal. Live violations: the synchronous-instrument definition is spread across FOUR sites (`SyncInstruments` fields `metrics.md:134-139`, `_seed_sync` name/unit literals `:171-179`, `_HIST_SLOT` `:143-147`, `INSTRUMENTS` rows `:183-196`) where one `INSTRUMENTS` fold must derive the carrier; `record_artifact` (`metrics.md:251-258`) bakes an artifacts-domain method into the base spine where a table-keyed recorder over the same rows belongs; `type Trigger` closes at 3 of apscheduler's 6 trigger types (`lanes.md:58` vs `.api/apscheduler.md:37-39`) under Growth prose claiming "one Trigger row"; instrumentation-scope literals mint per page (`"rasm.wire"` `wire.md:39`, `"rasm.runtime.resilience"`, `"rasm.runtime.content_identity"`, `METER_NAME` `metrics.md:71`, `"rasm-companion"` `telemetry.md:109`) with no scope vocabulary (`[V8]` rules the owner: `faults`); `SECRETS_MOUNT = "/run/secrets"` (`admission.md:266`) hardcodes an infra path a settings field owns; and the two plain-`dict` tables (`FAULT_OUTCOME` `metrics.md:84`, `SLOTS` `clock.md:165`) sit beside a corpus of `expression.Map` rails.

### [SEAM_AND_PROSE_LAW]

- The `ARCHITECTURE.md` seam ledger is enforceable law: every cross-`libs/` decode sits on the page that wires it, no seam is attributed twice, and a ledger row without a composing fence is a defect on BOTH surfaces. A page names the sibling symbol it consumes — it NEVER recites a sibling owner's closed vocabulary, asserts what a sibling "does not yet have," or enumerates sibling-package interior folders in its prose (the proven drift vector: the healed admission SECRET recital, E5, and the standing `lanes.md:83-89` sibling enumeration).
- One mechanism, one owner: a cross-cutting aspect lives at the lowest tier that owns no heavier concern (`latched` -> `faults`); a span/attribute/status fold exists once and is composed, never re-opened inline beside its own aspect (`identity.md:152` vs `:211`); one canonical name per concept — two `Credential` types in one package (`admission.md:271` struct vs `serve.md:70` union) is the standing violation.
- Page grammar: the `[RESEARCH]` appendix is BANNED — ten pages carry it (`clock.md:214`, `evidence.md:284`, `identity.md:373` EMPTY, `admission.md:388`, `lanes.md:295`, `metrics.md:286`, `receipts.md:294`, `telemetry.md:281`, `roots.md:336`, `wire.md:392` EMPTY); genuinely load-bearing `.api` anchors fold into the owning `Packages` block, the rest deletes. Each invariant states ONCE in its owning block — the Owner/Cases/Entry/Auto/Packages/Growth/Boundary schema restating one point 4-7x (clock's packed-key law at seven sites) is the same defect class at prose altitude. Index numbering matches body regions (`wire.md:15-17` vs its `[02]`-`[04]` sections).

### [INGRESS_LEDGER]

Honest ingress: what ARRIVES as C#-minted data and is only decoded here — the proto request/response messages over the UDS serve leg, the `DiscoveryResultWire[]` capability-catalog JSON, the CRDT op-log MessagePack under the `Lz4BlockArray` envelope, the `CausalFrame` carrier headers, the five `CredentialPolicy` rows, and the `FaultDetail` trailer on the client leg. What is COMPUTED here under a cross-language parity obligation, never a re-mint — the `ContentKey` over count-prefixed canonical bytes (proven against the C# golden corpus), the continued W3C trace context, and the `FaultDetail` egress when the companion is the faulting server (Python populates the C#-owned proto shape through the `fault_detail` registry row, authoring no field the `.proto` does not carry). Receipts, metrics, and drain tallies are companion-local evidence, minted freely.

### [PYPROJECT_RECONCILIATION]

Package changes centralize in root `pyproject.toml` with `.api` stubs per admission. Carried obligations: `s3fs`/`gcsfs` are DEAD in runtime — `roots.md:203-204` routes object-store schemes to `obstore` and everything else to `UPath`, both catalogs' own boundary notes say pick one transport per root (`.api/s3fs.md:99`), and the manifest already tags all four storage packages `[DATA]` (`pyproject.toml:25-32`) against the README's `[STORAGE_ROOTS]` claim — strike the `s3fs`/`gcsfs` rows and both runtime stubs, re-home the group's LIVE survivors `universal-pathlib` (roots' `from upath import UPath`, `roots.md:68`) and `fsspec` (its dispatch substrate, `ARCH:58`) beside `obstore` under `[TRANSPORT]`, and leave data-tier admission to the data campaign; `lz4` is manifest-tagged `[ARTIFACTS]` (`pyproject.toml:178`) and UPSTREAM-BLOCKED at the injected `DecompressFn` seam — its full 103-LOC runtime catalog reduces to a blocked stub naming `CRDT_OPLOG_LZ4_DECODE`; the `queenbee`/`lbt-recipes`/`pollination-handlers` `[RUNTIME]` tags stand only because `[V3]` lands their owner — the trio's AGPL-3.0 network copyleft is the binding admission flag both catalogs carry, admissible only under the companion's process-boundary, non-distributed execution charter, recorded beside the ruling; if the recipe ruling flips to expulsion, the tags, catalogs, and README group move in the same motion; `asyncssh` (EPL/GPL copyleft) survives NARROWED — roots' `_sftp_session` chain (`roots.md:63` import; `connect`/`start_sftp_client`/`SFTPClient.open`) and admission's `known_hosts` loader are realized fence consumers, so the charter and catalog narrow to the SFTP-read + known-hosts + connection-options slice, never expulsion; `trio`'s `[RUNTIME]` tag (`pyproject.toml:39` — "backend anyio runs on") is contradicted by the daemon itself — `grpc.aio` is asyncio-only (`.api/grpcio.md:3`), the serve leg pins the anyio asyncio backend, and zero fences import `trio` branch-wide — the tag dies; the package stands only where a consuming owner outside runtime names it (its own catalog charters the deterministic-test kit and guest-mode surfaces), a retag never a runtime consume — no sibling brief across the track names one (data/geometry/compute/artifacts all silent, zero branch-wide fence imports), so the leg-3 governance closeout resolves the row: retag to the test-kit owner the catalog charters or strike row and stub in the same motion. Two ADDITIONS carry the domain's proof burden into the manifest: `grpcio-health-checking` (named concern: `serve` filters health-check RPCs from tracing at `serve.md:34,111,286` yet registers NO health servicer — the canonical gRPC health protocol implementation closes the illusion) and `google-cloud-secret-manager` (named concern: `SecretTier.cloud` is a dead placeholder — the deferred `Ok(Nothing)` fold `admission.md:337` and the deferred-row comment `:380`; the categorical-best official client backs the already-catalogued `GoogleSecretManagerSettingsSource`, `libs/python/.api/pydantic-settings.md:45`). Paid/gated/proprietary stays rejected.

## [01]-[PHASE_REBUILD]

The rebuild engine runs against this brief directly. Verdicts are binding structural rulings: each carries a recommended-shape floor and a ruled default; the build may exceed a floor, never dilute it; a hedge names its deciding criteria and the implementing agent resolves it with disk evidence, never by preference.

### [V1_IMPORT_DAG_RESTORATION]

The 6-module SCC dies via the `[IMPORT_DAG_LAW]` re-homings — this is the campaign's primary ruling and every other verdict subordinates to it. Ruled: `receipts` owns `DrainOutcome`/`DRAIN_COLUMNS`/`DrainReceipt` (lanes keeps `Admit`/`ADMIT_TABLE` and produces receipts it imports downward); `faults` owns `latched`; `reproduction` is a declared module. Deleted forms: any function-scope deferred import used to hide a cycle, any `import X`-then-attribute dodge, any second drain vocabulary. `metrics.md`'s `ZERO_DRAIN`, `receipts.md`'s `drained` case, and `resilience.md`'s module-load `Metrics.retry_hook()` all bind eagerly against the new order and prove it. Acceptance is mechanical: the topological order in `[00]` holds over every `from rasm.runtime.*` import in every fence with zero back-edges.

### [V2_WIRE_VOCABULARY_OWNER]

NEW `transport/shapes.md` — the Python half of the wire vocabulary: all 16 canonical `msgspec.Struct` shapes `_PROTO_VOCABULARY` binds (`wire.md:164-183`), `FaultDetail` with its nine fields promoted from prose (`wire.md:102` — `package`/`code`/`case`/`message`/`evidence`/`correlation`/`hlc_physical`/`hlc_logical`/`tenant`), the 64-bit proto3-JSON decimal-string contract each shape's `Meta` bounds carry, and the `_pb2` codegen seam as a stated contract (which C# `.proto` compiles to `channels_pb2`, via `grpcio-tools`, with a `descriptor_pool`-backed drift gate asserting the compiled descriptors match the registry rows). Boundary law between the adjacent owners: `shapes` owns the vocabulary, the `_PROTO_VOCABULARY` binding registry (today `wire.md:164-183`), and the drift gate — the registry rows ARE the vocabulary's wire identity, and a wire-resident registry would force a `shapes -> wire` back-edge from the gate; `wire` imports the registry and owns only transcode machinery. This page is where the `.archive/RASM-COMPONENT-PARADIGM-DECISION.md` `[AMENDMENTS]` count-prefixed canonical-bytes law (`:63`) and the queued `PY_WIRE_ALIGNMENT` card (`Rasm.Element/TASKLOG.md:30`) get their standing Python anchor — recorded as the boundary contract the alignment campaign builds against, never a re-mint (the op-log msgpack arrays are self-delimiting; the amendment governs canonical BYTES for content keys). Single-mint law: every shape decodes the C#-minted proto, no shape is authored wire-first here. The registry is seed data with a sibling-consumer contract: the C#-minted tessellation leg — `TessellationRequest` and its response/receipt shapes (`Rasm.Bim/Exchange/tessellation` + `Rasm.Compute/Runtime/codecs` mints, streamed over the existing `artifact_frame` leg) — lands as registry rows with `geometry` `mesh/serve` (`RASM-PY-GEOMETRY-BRIEF.md` `[V3]`) the named demanding consumer, geometry's decoded field floor riding the rows (request: source modality, source bytes, tessellation-policy echo; response/receipt: content key, element/triangle tally, semantic header, whole-artifact XxHash128, replay phase); a sibling imports the rows by symbol and mints no wire shape. In the same motion `wire.md` demotes the speculative NESTED framing (`wire.md:344-371` — standing decoder machinery for a producer framing the `CRDT_OPLOG_WIRE_AMENDMENT` keyed-FLAT contract deprecates) to a proven-need row, keeps FLAT as the sole realized path, and deletes its empty `[05]-[RESEARCH]` stub (`wire.md:392`).

### [V3_RECIPE_OWNER]

NEW `execution/recipe.md` — ruled default AUTHOR, not expel: the packages are admitted and `[RUNTIME]`-tagged, the three catalogs already charter runtime rails, and recipe execution (queenbee schema -> lbt-recipes `Recipe(name)`/`RecipeSettings`/`run` -> pollination-handlers IO coercion) is genuine host-free companion work — local Radiance/EnergyPlus subprocess orchestration. Recommended shape: ONE `RecipeExecution` owner whose single entry runs a recipe under the composed rails — `lanes` offload (the process-resource lane), `admission` deadline/settings, `resilience` `guarded` with an engine-precheck row, `roots` `Transfer` for recipe-folder/asset acquisition, `@receipted` + one span per fold — with the handler catalog as a data table, never per-recipe arms. The floor is the typed deliverable, not the run: the entry returns the handled `output_value_by_name` readback (the catalog's result law — the product is the typed output, never the raw project folder) plus a `RecipeReceipt` carrying the simulation id, the three `version.check_*` engine-gate verdicts, and the parsed `luigi_execution_summary`/`error_summary`/`failure_message` evidence — the subprocess exit code is never the sole success signal (the catalog's own subprocess law), and `write_inputs_json` is the one input-coercion seam. Deciding criteria for the expulsion arm: only if the build proves the concern belongs to a downstream simulation folder (geometry's energy plane, `RASM-PY-GEOMETRY-BRIEF.md` `[V1]`, is now that named `python:` consumer — it binds queenbee `Job`/`RecipeInterface` construction to this owner's execution, so expulsion additionally breaks a named downstream consumer); expulsion moves tags, catalogs, and README group in the same motion and records the decision in the ledger.

### [V4_LEDGER_AND_CODEMAP_TRUTH]

`ARCHITECTURE.md` becomes exact: the `CredentialPem` row re-files from admission to `transport/serve` (`ARCH:41` -> serve's rows; the decoder is `serve.md:69-89`); the double-attributed `admission ⇄ CausalFrame` row (`ARCH:40`) dies — clock's `ARCH:37` row is the seam, admission consumes `rasm.runtime.clock` symbols in-language; serve's restatement of wire's cross-`libs/` codec seams (`ARCH:43,46,47` duplicating `:38,39`) collapses to the rows serve itself decodes (DiscoveryResult/CommandReceipt, HLC metadata carrier, credential axis); the W3C inbound-extraction row (`ARCH:42`) re-files to `transport/serve` — `[V6]`'s interceptor owns the gRPC extraction, receipts keeping only in-language surfaces; the codemap redraws as the FLAT realized module set — the `.planning/` sub-folders stay authoring organization, each node the real `rasm.runtime.<leaf>` file — and gains the missing nodes (`shapes`, `reproduction`, `recipe`). The identity module name unifies: ruled default `rasm.runtime.identity` (codemap, folder, and every seam endpoint already say `identity`; the fences' `content_identity` spelling is the drift — `lanes.md:49`, `identity.md:226` rewire).

### [V5_BASE_PURITY]

The host-free foundation imports NO sibling-tier package at module top and names NO sibling interior in prose. `resilience.md:31-37`'s `adbc_driver_manager`/`daft.exceptions`/`obstore.exceptions` imports die via the page's own `_named` qualname hook (`resilience.md:125` — built precisely to dodge gated imports and already used for deltalake/pyiceberg/compas rows; `_adbc_transient` reads the status by name, never the enum import); the sibling-domain `POLICY` rows (occt/rpc/lake-commit/remote-db/streaming, `resilience.md:74-78,258+`) STAY — rows are data and the one branch-wide retry table is correct — but every row is import-free. The resilience entry surface is additionally a branch-consumer contract: `libs/python/data` fences bind BOTH fused envelopes (`guarded` async + `guarded_sync` sync, `resilience.md:212,221` — egress/lakehouse bind the sync leg at 15 sites) and the `OBJECT_STORE`/`HTTP`/`WIRE`/`RPC`/`LAKE_COMMIT`/`REMOTE_DB`/`STREAMING` rows, and `gridded/virtual` binds faults' `railed` result builder (`faults.md:208`), and `gridded/store` binds roots' `OBJECT_STORE_SCHEMES` scheme table (`roots.md:97`; data's tensorstore-vs-zarr backend dispatch, `store.md:35`) — the rebuild preserves both envelopes, those rows, `railed`, and `OBJECT_STORE_SCHEMES` as exported names (the `[V12]` roots rename never sweeps the constant); narrowing any of them is a cross-folder break, never a cleanup. `WORKER_BAND` (`lanes.md:89` — declared, referenced nowhere, its comment enumerating sibling-folder interiors) is ruled WIRED, not deleted: `offload` gains the isolation-modality axis (interpreter | process | thread) with `WORKER_BAND` as the process-modality's one process-wide native-worker bound, the charter rewritten parameterized (no sibling enumeration — the comment's seven recited sibling interiors die; consumers arrive as ledger `python:` rows when siblings land), the `RetryClass.OCCT` comment example neutralized. Three lane/resilience consumer exports pin with the axis, `geometry` the named demanding consumer: `offload` accepts an optional `retry: RetryClass` wrapping the isolation leg in resilience's `guarded` (geometry `mesh/daemon` retries transient `anyio.BrokenWorkerInterpreter` subinterpreter cold-starts through the `OCCT` row while keeping its content-keyed `Admit` — never a fourth keyed-and-retried `Admit` case); the THREAD modality carries its own process-wide band bound beside `WORKER_BAND` (geometry's graph analytics owners collapse three hand-rolled `CapacityLimiter(4)` onto it); and the `OCCT`/`RPC` POLICY rows export with `geometry` beside `data` as demanding consumers — the `OCCT` target keyed on the whole worker-death band (`anyio.BrokenWorkerInterpreter | BrokenWorkerProcess`, the landed `resilience.md:254` pair; an interpreter-only narrowing is a cross-folder break — artifacts and compute bind the process half), the `RPC` row consumed at `compas.rpc.Proxy` bring-up — and the POLICY table gains an `OCC_NATIVE` row (target the in-process transient OCC `RuntimeError` band, tight attempts, import-free like every row) with geometry `ifc/analysis`'s clash-tree boundary the named demanding consumer. `compute` joins geometry and data as a named demanding consumer on the offload axis: its JAX-gated solve carriers pin `jax.config.update("jax_enable_x64", True)` — a process-global flag concurrent in-process solves corrupt — so JAX-backed solves pin the PROCESS modality — the interpreter modality is inadmissible for compute's numeric band, whose jaxlib/numpy native stack does not load under isolated subinterpreters — and the scipy-factorization, PyMC-sampling, cvxpy-conic, and ONNX-session workloads ride the THREAD/process band under the runtime-owned bounds (`RASM-PY-COMPUTE-BRIEF.md` `[V4]`); compute binds the exported axis, crosses spec-data payloads with module-level kernels on the process lane, and mints no limiter. `artifacts` joins as the fourth named demanding consumer: its `core/plan` pipeline schedules keyed work through lanes (the `ARTIFACT_PIPELINE_KEYED_CONSUMER` seam), its `Admit` `retried(RetryClass)` admission binds the exported `RetryClass` vocabulary, its native press/raster/render folds ride the THREAD/process band under the runtime-owned bounds — the three artifacts-minted `CapacityLimiter(4)`s (`artifacts/.planning/composition/compose.md:153`, `imposition.md:150`, `sheet.md:151`) collapse onto the axis, worker-death retry rides `offload(retry=RetryClass.OCCT)` (the row's landed target spans `anyio.BrokenWorkerProcess`), and the bare `stamina.AsyncRetryingCaller` (`compose.md:159`) re-routes through `guarded` — and the POLICY table gains an `ORACLE` row (import-free predicate over flaky external-oracle faults — veraPDF/JHOVE subprocess verdicts; tight attempts) with artifacts `exchange/conformance` the named demanding consumer. `RuntimeRail` additionally pins as an exported name beside `guarded`/`guarded_sync`/`railed` — the faults-owned railed-result spine every branch producer returns (`clock.md:70` mints the import shape; geometry daemon/serve and artifacts `emit()` entries type against it, `artifacts/.planning/export/layered.md:256` and `export/indesign.md:302`) — renaming or narrowing it is a cross-folder break. `metrics`' artifacts-domain surface (`record_artifact` + the two `artifact.*` histogram rows, `metrics.md:134-135,183-186,246-253`) generalizes to a table-keyed domain-histogram recorder over `INSTRUMENTS` rows so a new domain distribution is one row, never a new classmethod — `artifacts` is the named demanding consumer: its `CONTRIBUTE_ARTIFACTS_MEASURED_SIGNALS_RUNTIME` `[BLOCKED]` card (gated on the branch `ONE_MEASURED_SIGNAL_STREAM`) unblocks on exactly this recorder, so the generalized rows are its admission surface, never a re-minted stream.

### [V6_SERVE_CONTEXT_AUTHORITY]

One trace-context authority at the gRPC ingress. Today `aio_server_interceptor` natively extracts W3C context and opens the SERVER span (`serve.md:113`) while `inbound` re-extracts via `Signals.continue_inbound` (`:121`) and `dispatch` re-activates the REMOTE parent around the handler (`Signals.attach(parent)`, `:170`) — re-rooting handler spans as siblings of SERVER and fracturing the local tree. Ruled: the interceptor owns extraction and SERVER-span parenting; the manual pair narrows to reading the current context for `CausalFrame`/`RuntimeContext` admission; `attach(remote-parent)` around the body dies. The narrowing may NOT orphan receipts' pair: serve is `continue_inbound`/`attach`'s only cross-module consumer (`serve.md:121,170`) while lanes' offload stitch hand-runs the identical `context.attach(propagate.extract(carrier))` token dance inline (`traced_kernel`, `lanes.md:231-236`; its own `:302` prose admits the same pair backs `Signals.continue_inbound`/`Signals.attach`) — ruled: the worker-side `traced_kernel` composes the receipts pair (`receipts < lanes`, DAG-legal), the inline duplicate dies, and the pair keeps a real consumer. `run()`'s per-call descriptor attribute moves off the ambient span (`serve.md:316` sets `rasm.descriptor` BEFORE `_dispatch` opens the CLIENT span — thread it through the client `request_hook` the page already composes). The health illusion closes: `serve` filters health-check RPCs from tracing (`serve.md:34,113,294`) yet serves no health protocol — land `grpcio-health-checking`'s servicer in the `ServerHost` lifecycle (with graceful-shutdown state flips in `aclose`) or the filter claim dies. `inbound` additionally threads the caller's budget: `ServicerContext.time_remaining()` (the grpcio catalog's deadline introspection) lifts into the admitted `RuntimeContext` `Deadline` — the corpus mints the whole deadline rail (admission `Deadline` -> `LanePolicy.deadline` -> `CLASSIFY` deadline row) yet no fence reads the one real inbound budget source, so every handler today runs unbounded by the C#-dialed deadline. Serve's three co-located concerns (inbound host, outbound `CapabilityInvoke`, cyclopts `Entrypoint`) stay ONE page — shared wire/descriptor vocabulary; a split carries the burden of a non-companion `CapabilityInvoke` consumer. The `Entrypoint` is additionally the daemon COMPOSITION ROOT — post-`[V1]` telemetry has zero intra-package importers (`metrics.md:41` was its only one) and the `[TELOS]` daemon bar is otherwise owner-less: the serve command composes install (`telemetry`) -> settings admission (`admission`) -> `ServerHost.serve` -> deterministic drain as ONE ordered receipted fold — health `NOT_SERVING` flip, `server.stop(grace)`, lanes/recipe drain, `CapabilityInvoke.aclose` + roots teardown, `Telemetry.shutdown` LAST — each stage a rail accumulated through `traversed(ACCUMULATE)` (the telemetry flush idiom) into one typed shutdown receipt the exit code folds; a fire-and-forget stage sequence, an unowned drain ORDER across the five drainable owners, or a first-fault abort leaving later stages undrained is the deleted form. `ServerHost` construction, the `dispatch` registration surface (descriptor id + codec pair + railed handler), and the Entrypoint stage fold are additionally sibling-composable consumer exports: a downstream folder's own composition root registers its servicer rows and composes install -> admit -> serve -> drain strictly downward — `geometry` `mesh/serve` (`RASM-PY-GEOMETRY-BRIEF.md` `[V3]`) the named demanding consumer registering the tessellation servicer; runtime never imports a sibling. A recipe command binds `[V3]`'s owner; the new `serve -> telemetry | lanes | recipe` imports are legal at the terminal tier.

### [V7_IDENTITY_MECHANISM]

`key`/`of` collapse onto one span-fold core: `of` renders through the `derived` aspect (`identity.md:152`) while `key` re-opens `content.derive` inline — a second `start_as_current_span`, `is_recording` gate, `set_attributes`, `032x` render, and `set_status(OK)` (`identity.md:211-218`) — forking the one tracing mechanism across two methods on the page whose Boundary claims they "differ ONLY in the fallibility contract." One private span-fold both compose; `key` additionally fences or narrows its type-erased path — the shared `lift`'s `Struct` arm (`identity.md:109-110`) folds through the canonical encoder and raises `EncodeError` UNFENCED from `key` while `of` rails it. The page splits per `[V1]`: `evidence/identity.md` (ContentIdentity/ContentKey/IdentitySource/derived) and NEW `evidence/reproduction.md` (`SeedReproduction`/`CorpusFixture`/`ParityReceipt` corpus fold — the DESIGN-PIN discipline, `MATERIAL_LAYER_GOLDEN` obligation, and `planned`-phase law carry over intact); the dead empty `[04]-[RESEARCH]` header (`identity.md:373`) dies with the split. Both split halves are additionally branch-consumer contracts with `data` the named demanding consumer: `IdentitySource.lift`'s three payload modalities (whole-buffer, chunk-stream, merkle `tuple[ContentKey, ...]`) are exported law — data's egress keys operation bytes through the buffer/stream arms and its derived-snapshot Merkle key through the merkle arm — and `ParityReceipt`/`CorpusFixture` admit a data-authored fixture corpus for the icechunk snapshot-seed cross-runtime reproduction (`RASM-PY-DATA-BRIEF.md` `[V6]`); the span-fold collapse may not narrow either surface. `geometry` joins `data` as a named demanding consumer: `mesh/daemon` keys GLB wire bytes through the buffer modality under the seed-zero (`Some(0)`) C# `RepresentationContentHash` parity contract and admits a `GLB_BY_KEY` golden fixture into the reproduction corpus beside `MATERIAL_LAYER_GOLDEN` (`RASM-PY-GEOMETRY-BRIEF.md` `[V3]`), and `IdentityPolicy` stays a generic content-seed carrier — domain knobs (geometry's tessellation deflection/angle) ride a consumer-owned policy folded into the canonical seed bytes, never `IdentityPolicy` fields. `compute` joins data and geometry as a named demanding consumer with the default-policy law pinned: the identity surface exports `CANONICAL_POLICY` — the one default the `of` `policy` parameter binds — and key equality is bytes-law (`of(fmt, source)` under the default and `of(fmt, source, CANONICAL_POLICY)` mint the same key), the compute `experiments/study`/`experiments/history` design-key resume cache the demanding proof (`RASM-PY-COMPUTE-BRIEF.md` `[V7]`); compute keys buffer and stream payloads through `IdentitySource.lift`, and two compute fixtures join the reproduction corpus beside `MATERIAL_LAYER_GOLDEN`/`GLB_BY_KEY` — the `ARRAY_LAYOUT` cross-backend bit-identity parity and the C#-minted `EvidenceBundle` golden bundle backing codegen's offline-wire drift gate, the bundle fixture pairing its bytes with the expected generated-stub projection so the corpus proves decode AND emit round-trip (`RASM-PY-COMPUTE-BRIEF.md` `[V12]`).

### [V8_INSTRUMENT_TABLE_COLLAPSE]

`metrics` derives its synchronous carrier from the one `INSTRUMENTS` table — `_seed_sync` deletes, `_HIST_SLOT` derives from row metadata, `SyncInstruments` construction folds over the table against the meter, and the Growth claim ("one row + one slot") becomes true; `ZERO_DRAIN` spells `hit=0` for column parity (`metrics.md:85` vs `DRAIN_COLUMNS`); `FAULT_OUTCOME` (`metrics.md:81`) and clock's `SLOTS` (`clock.md:163`) join the corpus `Map` rail — ruled; the `[03]` escalation deltas already bind both conversions. `faults` hardens with its new tenants: `latched` lands beside `boundary`/`trapped`/`traversed`; the instrumentation-scope vocabulary lands as seed data — one table owning every tracer/meter/service scope literal (`"rasm.wire"` `wire.md:39`, `METER_NAME` `metrics.md:71`, `"rasm-companion"` `telemetry.md:109`, the resilience/identity tracer strings), each consumer minting its handle from its row, `faults` the one tier below every consumer in the `[00]` order; the `CLASSIFY` deadline row stops erasing evidence (`faults.md:124` drops the cause message and defaults budget `0.0` — carry the cause payload so `[V9]`'s per-signal identity survives classification); `traversed(PARTITION)`'s never-`Error` rail (`faults.md:190,201-202`) keeps its documented uniformity rationale or tightens.

### [V9_TELEMETRY_DRAIN_IDENTITY]

The host-drain rail keeps per-signal identity: `flush` hardcodes subject `"resource"` for every signal (`telemetry.md:212`) while `_drained` raises `TimeoutError(signal.value)` (`:198`) whose message the deadline classify row drops — a wedged exporter cannot be attributed to traces-vs-logs-vs-metrics in the accumulated fault, against the page's own headline claim. Thread `subject=kv[0].value` through the boundary; the meter-last drain order and the `_batched` kernel stay as landed.

### [V10_EVIDENCE_CHARTER]

`evidence.md` STAYS in runtime — ruled, with the charter stated honestly: it is the dev-tooling evidence owner feeding the external `assay code`/`api` rails, consumed by zero runtime modules, host-free by construction; the README charter already names it and relocation would orphan the folder's only structural-parsing owner. Two obligations: `ApiCatalogue.reflect` closes the charter gap — it delivers entry-point/version metadata only (`evidence.md:258-267`) against an `ARCHITECTURE.md:27` charter of "ApiPackage/ApiMember reflection"; ruled default CODE-UP to a real member scan (import the distribution's top module under the existing `@trapped(catch=ImportError)` fence, walk `__all__`/public members into `MemberFact` rows) with charter-down (rename to distribution catalogue) only if the build proves import-cost breaks the trapped fence. The query hot loop adopts the catalog's own id law: capture names resolve to integer ids once at registry build (`Language.id_for_node_kind`/`field_id_for_name`, `.api/tree-sitter.md:121,123`) instead of per-node string compares (`evidence.md:241`).

### [V11_PAGE_GRAMMAR]

The `[RESEARCH]` purge and invariant-once dedupe from `[SEAM_AND_PROSE_LAW]` execute on every page the campaign touches — ten tails deleted (two empty), load-bearing `.api` anchors folded into `Packages` blocks, the 4-7x block-schema restatement collapsed to one statement per owning block, `wire.md`'s index-vs-body numbering aligned, run-on ~30-40-line Boundary walls broken into scannable deleted-form clauses. This is the largest clean prose reduction in the folder and lands per-leg, never as a separate pass.

### [V12_NAMING_AND_PROSE_TRUTH]

One name per concept and no stale seam claims: admission's local `Credential` struct (`admission.md:271`) renames (`BasicCredential` floor) clearing the collision with serve's `CredentialPolicy` union (`serve.md:70`); admission's `RetryClass.SECRET` seam is landed truth on disk (the resilience import `admission.md:237`, the ladder comment naming the consumed row `admission.md:378-380`, the member `resilience.md:68`, the policy row `resilience.md:247`) — the rebuild HOLDS it, names only the consumed row, and re-introduces no sibling member recital and no claimed-open obligation; the same hold governs serve's catalog prose — a resident-catalog capability claim contradicting the branch-tier `opentelemetry-api` surface is the deleted form; `roots`' `_object_client` renames (`_transport_client`) off its false object-store affinity; the `SECRETS_MOUNT` literal (`admission.md:266`) parameterizes into `SettingsAdmission` with `/run/secrets` as the default value row.

### [V13_API_TIER_RECONCILIATION]

The runtime `.api` tier becomes lean and truthful: the four full substrate re-catalogs (`msgspec` 131, `protobuf` 159, `grpcio` 148, `opentelemetry-api` 171 — the only four of ~16 consumed substrate packages carrying dups) convert to integration OVERLAYS (LOCAL_ADMISSION + the runtime-specific delta, pointing at the canonical branch tier) or delete; `s3fs.md`/`gcsfs.md` delete and `lz4.md` reduces per `[PYPROJECT_RECONCILIATION]`; `fsspec.md` re-scopes to the roots read-slice — `UPath`-shared resolution and the async `_`-mirror under `Transfer` (`.api/fsspec.md:105,117`) — its transaction/`cat_ranges`/block-cache/`FSMap`/`rsync` surfaces routing to the data folder and its `s3fs`/`gcsfs` backend references (`:118`) dying with the strike; `cyclopts.md`'s full CLI-framework surface re-scopes — runtime keeps the entry-grammar slice `Entrypoint` consumes, the config/validators/types/meta/completion surface routes to the Assay owner the page itself reserves public commands to (`serve.md:324`); `asyncssh.md` narrows to the SFTP-read + known-hosts + connection-options slice per the reconciliation ruling; the recipe trio's catalogs stand as `[V3]`'s data; the two ADDITIONS land with stubs. Every surviving stub's charter line names its live consuming fence.

### [V14_FAULT_ROUND_TRIP]

The typed fault crosses the wire in BOTH directions or `fault_detail` is dead vocabulary: NO fence produces or consumes `FaultDetail` — `wire.md:102` claims it rides "the receipt slot or the grpc-status-details-bin trailer" and no page writes or reads either. Three obligations. Server egress: `settle`'s `Error` arm projects `BoundaryFault` -> `FaultDetail` (one total fold off `facts()` plus the inbound frame's hlc/tenant), encodes through the `fault_detail` registry row, and sets `grpc-status-details-bin` via `ServicerContext.set_trailing_metadata` before `abort` — the C# `WireFault.Decode(FaultDetail)` consumer decodes the typed conflict, the current `":".join(k=v)` string (`serve.md:137`) demoting to the human-readable `details`. Client ingress: the invoke's fault path lifts `AioRpcError.trailing_metadata()` and decodes the `fault_detail` row when the trailer carries it — the grpcio catalog's own client-fault law (code/details/trailing_metadata lifted at the channel boundary into a typed Result), never the current erasure to `boundary=("wire","AioRpcError")`. Retry reality: the `wire` `POLICY` row `target=(ConnectionError,)` (`resilience.md:245`) can never match `grpc.aio.AioRpcError` — serve's outbound dispatch already composes `guarded(RetryClass.WIRE, ...)` (`serve.md:305-306`), so the claimed transient-code retry is illusory — the row's target becomes an import-free status predicate over stamina's `BackoffHook` discriminator (the page's own `_retry_after`/`_adbc_transient` predicate shape; a code-name compare, no grpc import under `reliability/`) admitting the transient codes the grpcio client-fault law names and staying terminal on the rest.

## [02]-[EVIDENCE_REGISTER]

Initial pointers, never ceiling — every build leg re-verifies its rows against disk. Anchors are `libs/python/runtime/.planning/` relative; `ARCH:` anchors `libs/python/runtime/ARCHITECTURE.md`; `pyproject.toml:` anchors the repo root manifest.

| # | Class | Evidence |
|---|---|---|
| E1 | Import SCC | `lanes.md:51-53` ⟷ `metrics.md:40-41`/`receipts.md:53`; `resilience.md:42-44` + module-load hook call `:305`; `telemetry.md:74` -> `admission.md:237`; eager bindings `metrics.md:85` (`ZERO_DRAIN`), receipts `drained` case; taxonomy pivot `lanes.md:60,79,94-102`; `latched` pivot `telemetry.md:222-237` consumed `metrics.md:41` |
| E2 | Phantom wire vocabulary | `wire.md:116-119` imports 16 undefined Structs; `wire.md:164-183` `_PROTO_VOCABULARY` binds them; `FaultDetail` fields prose-only `wire.md:102`; codegen module unowned `wire.md:114`; NESTED dead machinery `wire.md:344-371`; empty `[05]-[RESEARCH]` `wire.md:392` |
| E3 | Recipe rail illusion | `.api/queenbee.md:3`, `.api/lbt-recipes.md:3`, `.api/pollination-handlers.md:3` each charter "the runtime recipe owner"; README `[02]`-`[RECIPE]` group; `pyproject.toml:140-142` `[RUNTIME]` tags; zero importing fences corpus-wide; no ARCHITECTURE node, no router route |
| E4 | Ledger/codemap drift | `ARCH:41` CredentialPem on admission vs decoder `serve.md:69-89`; `ARCH:40` duplicates `ARCH:37`; `ARCH:43,46,47` restate `ARCH:38,39`; nested codemap `ARCH:9-30` vs 100% flat imports; `identity.py` node vs `content_identity` spelling `lanes.md:49`; self-import `identity.md:226` proving undeclared second module |
| E5 | Stale seam prose | CLOSED on disk — admission consumes the landed row (`admission.md:237` import, ladder comment `:378-380`) against `resilience.md:68` (member) and `:247` (policy row), and serve's false resident-catalog claim is gone; `[V12]` HOLDS the closure and keeps both deleted forms named so no rebuild re-introduces them |
| E6 | Dead carriers | `WORKER_BAND` `lanes.md:89` single occurrence, unused by `drain`/`offload`; empty `[RESEARCH]` headers `identity.md:373`, `wire.md:392`; NESTED framing (E2); `SecretTier.cloud` deferred placeholder (`admission.md:337` `Ok(Nothing)` fold, `:380` deferred-row comment) |
| E7 | Duplicate mechanism | `key` re-implements `derived`'s span fold inline `identity.md:198-205` vs `:187`; sync-instrument definition spread four ways `metrics.md:134-139,143-147,171-179,183-196` with `_seed_sync` duplicating the install fold |
| E8 | Base-tier coupling | `resilience.md:31-37` module-top `adbc_driver_manager`/`daft`/`obstore` imports (all `[DATA]`-tagged, absent from runtime README manifest); `lanes.md:83-89` comment enumerating seven sibling interiors; `lanes.md:170` OCCT example; `metrics.md:134-135,183-186,246-253` artifacts-domain instruments in the base spine |
| E9 | Hardcoding | `SECRETS_MOUNT` `admission.md:266`; per-page tracer/meter/service literals `wire.md:39`, `metrics.md:71`, `telemetry.md:109`; drain subject `"resource"` for every signal `telemetry.md:212` with message dropped at `faults.md:124`; capability path template `serve.md:295` |
| E10 | `.api` tier rot | s3fs/gcsfs stubs dead (`roots.md:203-204` routing; `.api/s3fs.md:99` own boundary note; `pyproject.toml:25-32` `[DATA]` tags vs README `[STORAGE_ROOTS]`); lz4 full catalog, wrong tier (`pyproject.toml:178`); four full substrate dups vs branch-canonical law; cyclopts/asyncssh over-scoped for their consume |
| E11 | Name collision | two `Credential` types `admission.md:271` vs `serve.md:70`; `identity` vs `content_identity` vs `evidence/identity` three-way drift (E4) |
| E12 | Coverage gaps | `Trigger` 3-of-6 `lanes.md:58` vs `.api/apscheduler.md:37-39`; `reflect` entry-points-only `evidence.md:258-267` vs `ARCH:27` charter; string capture compares `evidence.md:241` vs id law `.api/tree-sitter.md:121,123`; health filtered-never-served `serve.md:34,113,294`; `key`'s unfenced `Struct` path `identity.md:109-110,190` |
| E13 | Duplicate context pair | `continue_inbound`/`attach` owners `receipts.md:258,263`; sole cross-module consumer `serve.md:121,170` (the pair `[V6]` narrows); lanes' inline `context.attach(propagate.extract(carrier))` duplicate `lanes.md:231-236` with `:302` prose admitting the same pair backs the receipts surface |
| E14 | Composition root absent | `companion_app` binds only serve/drain commands (`serve.md:318-335`); telemetry's sole intra-package importer is `metrics.md:41` (`latched`, re-homed by `[V1]`); no fence composes install -> admit -> serve -> drain against the `[TELOS]` daemon bar; five drainable owners (`serve.md:185-186,311-315`, `telemetry.md:281-289`, lanes' `_events` finally, roots teardown) with no ordering owner |
| E15 | Fault round-trip dead | `FaultDetail` produced/consumed by zero fences vs claim `wire.md:102`; string abort `serve.md:137`; trailer never read on the invoke fault path (`serve.md:289-306`); `wire` row `target=(ConnectionError,)` `resilience.md:245` vs the composed `guarded(RetryClass.WIRE)` dispatch `serve.md:305-306` |
| E16 | Deadline rail unwired | `Deadline`/`LanePolicy.deadline`/`CLASSIFY` deadline row all minted; `ServicerContext.time_remaining()` read by zero fences (`inbound` `serve.md:115-120` admits no budget) — the one real inbound budget source never feeds the rail |

Sound surfaces (preserve; cold-pass plus only the deltas a verdict names): `reliability/faults.md` (9.5 — gains `latched`, the scope vocabulary, deadline-evidence fidelity), `clock/clock.md` (9 — `SLOTS` Map + grammar only), `transport/roots.md` (9 — rename + grammar only), `receipts`' chain-placement/redaction machinery, `wire`'s transcode core and `CrdtArm` array-like decode, `lanes`' drain/offload concurrency core, `admission`'s ladder/gate tables, `telemetry`'s `SIGNAL_SPECS`/`_batched` fold, the corpus-wide single-mint FIT (decode C#-minted shapes, re-mint nothing — verified end-to-end).

## [03]-[CAPABILITY_ESCALATION]

Target grades are the acceptance bar; a page at target survives a hostile cold read with zero structural findings.

| Plane | Now | Target | The delta that closes it |
|---|---|---|---|
| reliability/faults | 9.5 | 10 | `latched` tenant; scope-vocabulary tenant; deadline row keeps cause evidence; PARTITION rationale; grammar |
| reliability/resilience | 6 | 9.5 | import-free rows via `_named`; hooks order proven against V1 DAG; `wire`-row transient-status predicate (V14); scope-literal vocabulary; grammar |
| clock | 9 | 9.5 | `SLOTS` -> Map; invariant-once dedupe; RESEARCH purge |
| evidence/identity(+reproduction) | 7 | 9.5 | one span-fold core; fenced/narrowed `key`; module split + canonical name; empty header dies |
| evidence/evidence | 7 | 9 | honest tooling charter; member-level `reflect`; integer-id hot loop; RESEARCH purge |
| execution/admission | 7 | 9.5 | landed SECRET seam truth held (E5); `BasicCredential` rename; parameterized secrets mount; cloud tier REAL over the admitted client; grammar |
| execution/lanes | 7 | 9.5 | taxonomy re-home consumed; `Trigger` 6-of-6; offload isolation-modality axis wiring `WORKER_BAND`; sibling enumeration dies |
| execution/recipe | — | 9.5 | NEW owner composing lanes/admission/resilience/roots/receipts over the three admitted packages; typed `output_value_by_name` readback + parsed-log `RecipeReceipt`; engine-gate rows; handler catalog as data |
| observability/receipts | 8 | 9.5 | drain vocabulary OWNED here; lanes import dies; grammar |
| observability/metrics | 7 | 9.5 | one-table instrument derivation; domain-histogram generalization; `latched` import flips to faults; Map rail |
| observability/telemetry | 8 | 9.5 | `latched` leaves; per-signal drain subject; grammar |
| transport/shapes | — | 9.5 | NEW: 16 Structs + `FaultDetail` real; owns `_PROTO_VOCABULARY`; codegen contract + descriptor drift gate; count-prefix law anchored |
| transport/wire | 5 | 9.5 | phantom resolved via shapes; NESTED demoted; index/grammar; stub deleted |
| transport/serve | 6 | 9.5 | one context authority; descriptor on the CLIENT span; health service real; `FaultDetail` egress/ingress (V14); inbound `time_remaining` -> `Deadline`; Entrypoint daemon fold with ordered receipted drain; ledger rows owned; catalog claim corrected |
| transport/roots | 9 | 9.5 | `_transport_client` rename; grammar |
| governance (ARCH/README/`.api`) | drifted | exact | flat codemap + full module set; corrected ledger; router + package groups truthful; tier per V13 |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; the branch tier at `libs/python/.api/` is canonical for substrate — a runtime-tier full re-catalog is a defect per `[V13]`):

- SHARED-TIER LAW: every production fold demands the branch weave — `expression` `tagged_union`/`Result`/`Option`/`Block`/`Map` as the one ADT and persistent-collection spine (the corpus `Map` rail `[V8]` completes), `anyio` task-group/`CapacityLimiter` at every native/subprocess seam (one process-wide band per `[V5]`), `stamina` ONLY at flaky external oracles through the one `POLICY` table (never around pure transforms), `structlog`+`opentelemetry` span per fold through the owned aspects (`Decode`/`derived`/`drained`/`measured`/`guarded` — never inline span opens beside them), `msgspec` one-shot admission at ingress with `convert(strict=False)` reserved for the two proto3-JSON/carrier-string legs that own it. The corpus already models this weave; regressions are defects, not style.
- `apscheduler` — complete the trigger union: `CalendarIntervalTrigger`/`AndTrigger`/`OrTrigger` (`.api/apscheduler.md:37-39,117-118`) join `type Trigger` so the Growth claim is true; persistent jobstores/executors stay correctly unmined (session-local charter).
- `watchfiles` — the lanes consume is real (`awatch`/`PythonFilter`/`BaseFilter`/`Change`, `lanes.md:47`); two residuals close it: `Change.raw_str()` becomes the receipt serialization form for watch facts (`.api/watchfiles.md:22,83`) and the `debounce`/`step` batching axis surfaces as `watched`-case data, never baked defaults (`:53`); the `run_process`/`arun_process` reload drivers stay correctly unmined — the runtime owns no second process supervisor (`:79`).
- `grpcio-health-checking` — ADDITION: `HealthServicer` + per-service status set + graceful-shutdown flip wired into `ServerHost` lifecycle (`[V6]`); stub authored with the admission.
- `google-cloud-secret-manager` — ADDITION: graduates `SecretTier.cloud` from placeholder to a `SECRET_LADDER` row through the catalogued `GoogleSecretManagerSettingsSource` (`libs/python/.api/pydantic-settings.md:45`); stub authored with the admission; absorbs the cloud-secret slice of IDEAS `STRUCTURED_SETTINGS_SCHEMA`.
- `protobuf` — `descriptor_pool`/file-descriptor surface backs `[V2]`'s codegen drift gate (compiled `channels_pb2` proven against `_PROTO_VOCABULARY` rows); WKT wrappers/text_format stay unmined by design.
- `grpcio-tools` — `[V2]`'s codegen contract names its entry: `protoc.main` static codegen (`libs/python/.api/grpcio-tools.md:41`) mints `channels_pb2`; the `sys.meta_path` dynamic-stub importer backing `grpc.protos()` (`:20`) is REJECTED for the pinned vocabulary — runtime imports the generated module, never generates at import time.
- `tree-sitter` — `Language.id_for_node_kind`/`field_id_for_name` at registry build; `TreeCursor` for hot walks per the catalog's own traversal law (`.api/tree-sitter.md:115,121,123,133`); incremental `edit` stays correctly unmined (one-shot scans).
- `obstore` — the runtime read-slice charter is law: `get_async`/`list`/`sign_async`/streaming reads stay; write/conditional/`PutMode` surfaces remain the data folder's to mine — the runtime stub says so instead of cataloguing them.
- `httpx` — the auth lane is ruled: the custom bearer `auth_flow` stays the charter — `_BearerAuth` is realized (`roots.md:130`) and httpx ships no bearer `Auth`; `BasicAuth` stands as the forward `CREDENTIAL`-shape row already seamed at `SecretBoundary.resolve`'s `@overload` (`admission.md:207,218`); the unconsumed `NetRCAuth`/`DigestAuth` family re-scopes out of the runtime stub per `[V13]`'s live-consumer law.
- `grpcio` — mine the fault/deadline surface the catalog already laws: `ServicerContext.set_trailing_metadata` + `time_remaining` and `AioRpcError.trailing_metadata()` (`.api/grpcio.md:65,122,124,136`) back `[V14]`'s round-trip and `[V6]`'s budget threading; no new admission.
- `opentelemetry-instrumentation-grpc` — `[V6]` executes over this catalog: one composed `filters.Condition` predicate stays the sole trace selector and `request_hook`/`response_hook` the client-span enrichment seam the descriptor threads through (`.api/opentelemetry-instrumentation-grpc.md:95-96` — `response_hook` receives the unary `call.details()` string; the status code is interceptor-set, never hook-set); the `OTEL_PYTHON_GRPC_EXCLUDED_SERVICES` auto-fold (`:95`) records as the deployment-exclusion seam composing with the code predicate, never a second filter type.
- `fsspec`/`universal-pathlib` — the runtime slice is `UPath`-shared resolution plus blocking-read offload under roots' `Transfer` (`roots.md:68,124-125`; one resolved filesystem via the registry cache, `.api/fsspec.md:105,117`); the transaction/`cat_ranges`/block-cache/`FSMap`/`rsync` surfaces stay the data folder's to mine — the re-scoped stub says so per `[V13]`; compute is a named `UPath` consumer (`experiments/model.md` model-asset resolution — compute-campaign consumer pressure), so the branch stub's consumer line carries compute beside data and the re-scope never drops it.
- `lbt-recipes`/`queenbee`/`pollination-handlers` — `[V3]`'s data rows: `write_inputs_json` (coercion seam), `output_value_by_name` (typed readback), `luigi_execution_summary`/`error_summary`/`failure_message` (receipt evidence), `version.check_*` (engine-gate rows); `queenbee` stays schema-only (no `[cli]` extra, no bundled urllib transfer — `cyclopts` and the roots rails own those), handlers resolve only through the lbt-recipes chain.
- `cyclopts`/`asyncssh`/`lz4`/`s3fs`/`gcsfs` — per `[PYPROJECT_RECONCILIATION]` and `[V13]`.
- VERIFIED AT DEPTH (hold the landed bar; regressions are defects): `keyring` — the keystore arm holds the catalog's missing-vs-error law (`get_credential` `None` -> `Option.of_optional` next-tier fall-through, `NoKeyringError` floored to `Nothing`, credential's own `username` authoritative; `admission.md:301-305` vs `.api/keyring.md:104`), selection stays env/config (`:101`) with `set_keyring` test-boundary only; `xxhash` — pinned `xxh3_128` family + fixed seed + C# `XxHash128` byte-parity with the big-endian `digest()` declaration (`libs/python/.api/xxhash.md:95-96`); `psutil` — `oneshot()` batched reads, `PROCESS_FAULTS` taxonomy, `hasattr`-guarded `num_fds` (`metrics.md:102` vs `libs/python/.api/psutil.md:57,139`); `opentelemetry-sdk`/`-exporter-otlp-proto-http` — `ParentBased(root=ALWAYS_ON)` honors the C#-minted sampling decision (`telemetry.md:115` vs `libs/python/.api/opentelemetry-sdk.md:49,134`), reader interval and `Compression` ride as settings data (`telemetry.md:69,126`); `beartype` — one CLASSIFY row + one shared domain `BeartypeConf` (`faults.md:126,131`); `tree-sitter-python`/`tree-sitter-typescript` — both capsules wrapped exactly once into `Map[Lang, Grammar]` with `tsx` a real `Lang` member (`evidence.md:24,47` vs `.api/tree-sitter-typescript.md:34,44`), bundled HIGHLIGHTS/LOCALS/TAGS query sources correctly unmined (editor surfaces, not evidence probes).
- TASKLOG absorption: `ARTIFACT_PIPELINE_KEYED_CONSUMER` closes with the lanes leg (the `Keyed[T]`/`StagePlan` seam confirmed, mirrored ledger row), `DATA_LINEAGE_RECEIPT` with the receipts leg (one `ReceiptContributor` row), `DATA_TRANSPORT_DSN` with the roots leg (the four `[TRANSPORT]`/`[PORT]` edges mirrored); `CRDT_OPLOG_LZ4` stays `[BLOCKED]` and untouched.

## [05]-[BUILD_LEGS]

The durable rebuild engine runs per leg with this brief; the partition is a topological order over the `[IMPORT_DAG_LAW]` graph — every leg's pages import only pages of the same or earlier legs, and pages inside a leg land in the `[00]` order.

1. **BASE** — `reliability/faults.md` (gains `latched`; deadline evidence), `clock/clock.md`, `evidence/identity.md` (V7 split source; canonical module name lands corpus-wide in this leg's reconcile), `observability/receipts.md` (drain vocabulary lands HERE; lanes import dies; the `DrainReceipt.cache` `Map[ContentKey, T]` column makes `receipts -> identity` real), NEW `evidence/reproduction.md` LAST (V7 split; its prelude binds `identity` AND `receipts` downward, `identity.md:226-228`).
2. **MECHANISM** — `observability/metrics.md` (taxonomy import flips; instrument collapse; domain generalization), `reliability/resilience.md` (import-free rows; hooks proven; `wire`-row status predicate per V14), `execution/admission.md` (V12 prose truth; cloud tier real — the `google-cloud-secret-manager` manifest row and `.api` stub land with this leg), `observability/telemetry.md` (`latched` gone; per-signal subject), `evidence/evidence.md` (charter + reflect + ids).
3. **TRANSPORT/EDGE + GOVERNANCE** — NEW `transport/shapes.md` FIRST, then `transport/roots.md` (cold), `transport/wire.md` (phantom resolved; NESTED demoted), `execution/lanes.md` (Trigger; offload modality axis + `WORKER_BAND`; taxonomy import flips), NEW `execution/recipe.md`, `transport/serve.md` LAST (context authority; health; V14 fault egress/ingress; inbound deadline; ordered receipted drain; ledger rows); the governance closeout lands in this leg's reconcile — `ARCHITECTURE.md` codemap+ledger, `README.md` router+groups, the full `[V13]` `.api` motions, and the remaining `pyproject.toml` motions — the `grpcio-health-checking` addition, the `s3fs`/`gcsfs` strikes, and the retags.

Per-leg closeout obligations: hard residuals resolve before the next leg launches; `README.md`/`ARCHITECTURE.md`/`TASKLOG.md` close in the same leg that changes their facts; the `[V11]` grammar purge applies to every page a leg touches, never as a separate pass; pyproject and `.api` stub motions land with the leg that admits or removes the package; residuals naming a later leg's pages transfer forward explicitly in the leg return, never silently.

Campaign acceptance closes on proofs, not prose: (a) the `[00]` topological order holds over every `from rasm.runtime.*` import in every fence — zero back-edges, mechanically re-derived; (b) every `_PROTO_VOCABULARY` row binds a fence-declared Struct, the registry and drift gate live in `shapes` with zero `shapes -> wire` edge, and `FaultDetail` has a fence-level producer (`settle`'s trailer egress) AND consumer (the invoke's trailer decode) per `[V14]`; (c) the recipe rail has an owning page whose entry composes the named rails, or the packages/catalogs/README group are gone; (d) grep proofs return zero: `[RESEARCH]` headers, `rasm.runtime.content_identity`, sibling-interior folder names in runtime prose, module-top `[DATA]`-package imports under `reliability/`; (e) ledger, codemap, router, and manifest agree on one module set and one seam ownership; (f) the daemon proof — the Entrypoint drain fold is ordered health-flip -> server -> lanes/recipe -> channels/roots -> telemetry-LAST returning one accumulated shutdown receipt, and `time_remaining()` reaches the admitted `RuntimeContext` deadline.

## [06]-[OUT_OF_SCOPE]

- The C# producers: the `.proto` files, `CredentialPolicy` mint, HLC mint, `CrdtWire` encode, `CanonicalWriter` — runtime decodes, never re-mints; the `PY_WIRE_ALIGNMENT` execution (`Rasm.Element/TASKLOG.md:30`, QUEUED) is its own campaign — this one lands only the `shapes` owner and the recorded count-prefix law it builds against.
- `CRDT_OPLOG_LZ4_DECODE` — upstream-blocked at the C# `MessagePackCompression.Lz4BlockArray` envelope; the `DecompressFn` seam stays injected and open.
- Sibling folders (`artifacts`/`data`/`geometry`/`compute`) — their pages never change here; cross-folder consequences travel as Ripple counterpart cards, and the three QUEUED TASKLOG ripples close as `[04]` states.
- The assay tool implementation (`tools/`) — `evidence` feeds its rails, never authors them.
- IDEAS expansion pool (`LIFECYCLE_SUPERVISION_SPINE`, `DISTRIBUTED_CLOCK_SCHEDULE`, `EXECUTION_RESOURCE_IAC`, `CONTENT_REUSE_FEDERATION_WIRE`) — genuinely deferred; `STRUCTURED_SETTINGS_SCHEMA`'s cloud-secret slice lands via `[04]`'s addition, its remote-config remainder stays pooled.
