id: chain-to-unpacking
snapshots:
  ? |
    def flatten(rows):
        from itertools import chain

        return chain(*rows)
  : fixed: |
      def flatten(rows):
          from itertools import chain

          return [*item for item in rows]
    labels:
    - source: chain(*rows)
      style: primary
      start: 63
      end: 75
  ? |
    from itertools import chain

    flat = chain(first, second)
  : fixed: |
      from itertools import chain

      flat = [*first, *second]
    labels:
    - source: chain(first, second)
      style: primary
      start: 36
      end: 56
    - source: (first, second)
      style: secondary
      start: 41
      end: 56
  ? |
    from itertools import chain

    flat = chain(first, second, third)
  : fixed: |
      from itertools import chain

      flat = [*first, *second, *third]
    labels:
    - source: chain(first, second, third)
      style: primary
      start: 36
      end: 63
    - source: (first, second, third)
      style: secondary
      start: 41
      end: 63
  ? |
    from itertools import chain

    flat = chain.from_iterable(rows)
  : fixed: |
      from itertools import chain

      flat = [*item for item in rows]
    labels:
    - source: chain.from_iterable(rows)
      style: primary
      start: 36
      end: 61
  ? |
    from itertools import chain

    flat = list(chain(*rows))
  : fixed: |
      from itertools import chain

      flat = [*item for item in rows]
    labels:
    - source: list(chain(*rows))
      style: primary
      start: 36
      end: 54
  ? |
    from itertools import chain

    flat = list(chain(first, second))
  : fixed: |
      from itertools import chain

      flat = [*first, *second]
    labels:
    - source: list(chain(first, second))
      style: primary
      start: 36
      end: 62
    - source: (first, second)
      style: secondary
      start: 46
      end: 61
    - source: chain(first, second)
      style: secondary
      start: 41
      end: 61
    - source: chain(first, second)
      style: secondary
      start: 41
      end: 61
  ? |
    from itertools import chain

    flat = list(chain.from_iterable(rows))
  : fixed: |
      from itertools import chain

      flat = [*item for item in rows]
    labels:
    - source: list(chain.from_iterable(rows))
      style: primary
      start: 36
      end: 67
  ? |
    from itertools import chain

    flat = sorted(chain(*rows))
  : fixed: |
      from itertools import chain

      flat = sorted([*item for item in rows])
    labels:
    - source: chain(*rows)
      style: primary
      start: 43
      end: 55
  ? |
    from itertools import chain as link

    flat = link(*rows)
  : fixed: |
      from itertools import chain as link

      flat = [*item for item in rows]
    labels:
    - source: link(*rows)
      style: primary
      start: 44
      end: 55
  ? |
    from itertools import starmap, chain

    flat = chain(*rows)
  : fixed: |
      from itertools import starmap, chain

      flat = [*item for item in rows]
    labels:
    - source: chain(*rows)
      style: primary
      start: 45
      end: 57
  ? |
    import itertools

    flat = itertools.chain(*rows)
  : fixed: |
      import itertools

      flat = [*item for item in rows]
    labels:
    - source: itertools.chain(*rows)
      style: primary
      start: 25
      end: 47
  ? |
    import itertools as it

    flat = it.chain.from_iterable(rows)
  : fixed: |
      import itertools as it

      flat = [*item for item in rows]
    labels:
    - source: it.chain.from_iterable(rows)
      style: primary
      start: 31
      end: 59
