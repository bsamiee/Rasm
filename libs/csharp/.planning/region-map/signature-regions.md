# [REGION_MAP_SIGNATURE_REGIONS]

Suite region map — row shapes and protocol live in the [suite standard](../README.md) ledger-protocol section.

- AH-01 PROFILE_AXIS|LIFETIME_ADAPTERS|RESOURCE_IDENTITY: HostProfileKeyPolicy, ShipVehicle, RuntimeAttachment, ProfileFault, HostProfile, ResolvedProfile, ProfileSurface, ProfileBoot (+ Watchdog(ISystemdNotifier) emitting the WATCHDOG=1 ServiceState on the schedule-port heartbeat), ProfileRoots, ProfileIdentity — spotlight: the one host-variance axis and its resolved record; systemd watchdog keep-alive notify (residual [WATCHDOG_INTERVAL] cadence-source tier-3).
- AH-02 PHASE_FAMILY|FAULT_SPINE|DRAIN_CONDUCTOR|CANCEL_SPINE|TS_PROJECTION: CorrelationId, PhaseKeyPolicy, RuntimePhase, PhaseTrigger, LifecycleFault, PhaseReceipt, PhaseSubscription, Lifecycle, FaultSource, FaultRecord, BootMarker, FaultSpine, DrainBand, DrainOutcome, DrainStep, DrainReceipt, DrainConductor, CancelScope, RuntimePhaseKey, DrainOutcomeKey, PhaseReceiptWire, BootMarkerWire, FaultRecordWire, DrainStepWire, DrainReceiptWire — spotlight: total phase law, drain bands, fault spine, boot-minted correlation identity.
- AH-03 CLOCK_SPLIT|DEADLINE_TAXONOMY|SCHEDULE_PORT: ClockPolicy, TimeKeyPolicy, DeadlineClass, DeadlineOutcome, DeadlineReceipt, DeadlineOps, OccurrenceSpec (+ Fleet case), CronCadence (jitter-template SmartEnum Yearly/Weekly/Monthly/Daily/Hourly/EveryMinute WithJitter, EverySecond rejected), LeasePolicy, ScheduleEntry (+ Spread deriving jitterSeed from the schedule key via XxHash3.HashToUInt64), SchedulePort — spotlight: clock seams, deadline taxonomy, cron schedule port, fleet-spread jitter cadence.
- AH-05 MODULE_TABLE|SCAN_AND_DECORATE|BOUNDARY_ACTIVATION: ModuleContribution (+ Decorations column, Registrars), DecorationRow, ContributionReceipt (+ Decorated count), CompositionSurface, BoundaryActivation (+ Available/Scoped/Decorate/Decorated) — spotlight: one composition root, contribution rows, typed keyed-decoration pass-law.
- AH-06 CACHE_PORT|OBJECT_POOLS|DRAIN_QUEUES: LaneKeyPolicy, CacheLane, CacheSurface (+ AddKeyedHybridCache per Store lane), PoolPolicy<T>, Pools (Default<T> registration sub-surface), DrainSpec (+ DrainKind topology discriminant + Greedy/MaxGroups/PropagateCompletion/Batch columns), DrainKind (SmartEnum pipe/network/fan-out/correlated-join/dual-coalesce), DrainQueue<T>, DrainSurface (+ Broadcast/Join/Coalesce builders over BroadcastBlock/JoinBlock/BatchedJoinBlock; BoundedCapacity on base DataflowBlockOptions, GroupingDataflowBlockOptions carries only Greedy/MaxNumberOfGroups) — spotlight: hybrid cache port, pool policy rows, drainable queues with Dataflow fan-out/correlated-join/dual-coalesce topologies.
- AH-07 TELEMETRY_IDENTITY|CORRELATION_SPINE|LOG_PROJECTION|SIGNAL_GOVERNANCE|REDACTION_TAXONOMY: SignalKeyPolicy, TelemetrySource, InstrumentRow, TelemetryIdentity (+ HostMetrics source-gen [Counter]/[Gauge]/[Histogram] partial), Correlation, RootEnricher (IStaticLogEnricher), CausalEnricher (ILogEnricher), TraceContext (W3C traceparent/tracestate Inject/Extract/Continue over Grpc.Core.Api Metadata carrier; CompositeTextMapPropagator[TraceContextPropagator,BaggagePropagator] as Propagators.DefaultTextMapPropagator; ActivitySource.StartActivity continued-span seed), LogPipeline, SpineLog, SerilogProjectionPolicy, SpineLossFold (ILoggingFailureListener), TelemetrySignal, SignalGovernance, LatencyCheckpoint, LatencySpine, RedactorKind, DataClassification, RedactionRegistration — spotlight: telemetry identity, W3C spine mapping with distributed trace-context propagation over the control hop, classification taxonomy, in-flight latency checkpoints, redaction registration fold; residual [GRPC_METADATA_CARRIER] G7 spec-compile until Grpc.Core.Api assay source map registers.
- AH-08 HEALTH_FOLD|DEGRADATION_RAIL|WIRE_HEALTH|TS_PROJECTION: HealthContributorRow, PressurePolicy (+ Quota/CgroupV2/DiskIoMetrics columns, Canonical/Container rows, ResourceQuota limit grade via ResourceQuotaProvider.GetResourceQuota), UtilizationCell (MeterListener over meter Microsoft.Extensions.Diagnostics.ResourceMonitoring — process.cpu.utilization/dotnet.process.memory.virtual.ratio — replacing the obsolete GradePublisher IResourceUtilizationPublisher seat; Monitor folds onto AddResourceMonitoring), HealthSnapshot, HealthSurface, HealthKeyPolicy, Capability, DegradationLevel, DegradationState (+ Cascade floor field), DegradationPolicy, DegradationCell (+ Cascade(Option<DegradationLevel>) parent-forced floor; Floor keeps the worse of cascaded/derived), WireHealthRow, WireHealth — spotlight: OTel-instrument health fold, container-limit pressure grading, cross-process degradation-cascade floor, grpc.health mapping; residual [MEMORY_RATIO_SEMANTIC]/[CONTAINER_LIMIT_READ].
- AH-09 TRIGGER_UNION|CAPTURE_PIPELINE|MANIFEST_RECEIPT|TS_PROJECTION: SupportTrigger (ExternalCommand admits the capture-support control verb from companion-sidecar#CONTROL_SERVICE — consumer route-in, no new case), SupportTriggerOps, SupportArtifact, SupportPolicy, SupportRuntime, SupportCapture, SupportManifest, SupportReceipt, SupportLedger — spotlight: bounded redacted capture with receipts; control-hop capture route-in.
- AH-10 HOP_AXIS|HTTP_PIPELINES|KEYED_PIPELINES|OWNERSHIP_LAW + DISCOVERY_ATTACH (outbound-resilience.md), one Outbound.cs closure: OutboundHop, HopFault, HopIdempotency, HopTransport, ReleaseIdentity, HopPolicy, HopRows, HttpLane (+ ConfigureWeightedGroups/IsTransientHttpFailure routing, DisableFor(Patch) non-idempotent guard), GrpcChannelPolicy, KeyedLane (+ StateOf read-side, Register redial FallbackStrategyOptions param, RateLimiterStrategyOptions head + RetryAfter DelayGenerator, CircuitBreakerManualControl/StateProvider split), DiscoveryManifest, CompanionChild, Discovery (+ DrainFan/FanOf producing CompanionChild.FanDrain over the LocalIpc hop; the accepted-socket peer-credential read MOVED OUT to companion-sidecar#PEER_ADMISSION), HopOutcome, HopReceipt, OutboundRuntime (+ BreakerState from CircuitBreakerStateProvider), OutboundSurface (+ ExecuteOutcomeAsync over pooled ResilienceContextPool.Shared, RouteKey property), KeyedLane (+ HopPolicy SlidingWindow/TokenBucket/Concurrency rate-limiter strategy-selector) — spotlight: hop axis, one retry owner, keyed breaker state, per-hop rate-limiter strategy; discovery manifest + drain-fan producer spotlight on the outbound-resilience DISCOVERY_ATTACH cluster.
- AH-11 PORT_RECORDS|WIRE_LAW|TS_PROJECTION: ReceiptEnvelope (+ Tenant field carrying the 4th cross-process primitive beside the HLC stamp), TenantId ([ValueObject<UInt128>] KeyMemberName=Value public; RLS uuid cast + cache-key partition identity), TenantSlot (const rasm.tenant GUC/Baggage/meter-tag spelling), TenantContext (record port; Root single-tenant default, Ambient RuntimeContextSlot, Current, Stamp, Tag — AppHost mints+threads; consumed by Persistence/server-tier#TENANCY_RLS + cache-indexes#L2_CONTRIBUTION never re-minted), ReceiptSinkPort (Send 5-arg CorrelationId/TenantContext/package/kind/payload; Advance HLC), TelemetryContributorPort, DrainParticipantPort, HostAttachPort, UiSchedulerPort, SupportContributorPort, HealthContributorPort, AppHostWireContext (+ [JsonSerializable] TenantContext/RosterReceipt/FleetRollReceipt rows), SuiteContracts, NodaPatterns, RasmPackage, HlcStampWire, TenantContextWire, ReceiptEnvelopeWire (+ tenant: TenantContextWire field) — spotlight: the seven ports plus the boot-minted TenantContext tenancy primitive (4th cross-package law), suite wire law, NodaTime pattern surface, contract merge; an eighth port is the named defect.
- AH-12 UPDATE_RAIL|CHANNEL_AXIS|ROLLOVER_DRAIN (provisioning-and-update.md), one Provisioning.cs closure: UpdateKeyPolicy, UpdatePhase, UpdateOutcome, UpdateFault, UpdateReceipt, UpdateMetrics (source-gen [Counter]/[Histogram] partial keyed on UpdateChannel), UpdateRail (UpdateManager handle, Stage/Rollover/Resume rails, IsUpdatePendingRestart/UpdatePendingRestart staged-pending probe), UpdateChannel (Feed/ExplicitChannel/AllowVersionDowngrade columns, From over ReleaseIdentity.Channel), RolloverDrain (Conduct/ConductPending over DrainConductor), FleetRoll (static fleet conductor; Roll walks PeerRoster.Attached in fixed-size health-gated batches over RolloverDrain.Conduct dialed across the control hop, gating each batch on the post-roll WireHealth.Evaluate serving probe, takes ReceiptSinkPort/IClock/TenantContext), FleetRollReceipt (readonly record struct; wave index, node pid, terminal UpdateOutcome, post-roll serving status, nodes-remaining) — spotlight: post-fetch download/stage/rollover/rollback state machine over Velopack with per-phase receipts, three-channel feed axis, drain-before-swap handshake, fleet-wide rolling-update wave gated node-by-node on recovered serving status; ApplyUpdatesAndRestart takes UpdateInfo.TargetFullRelease.
- AH-13 PROCESS_MODALITY|CONTROL_SERVICE|SERVICE_HOST|DEGRADATION_CASCADE|PEER_ADMISSION (companion-sidecar.md), one Companion.cs closure: ProcessModalityKeyPolicy, ProcessModality, ModalityRow, CompanionPeer, ModalityReceipt, ModalityRows (Row extension Switch + Attach over Discovery.Spawn/Connect/Read), PeerRoster (Atom-backed serving-side attached-connection set with monotone lease epoch; Boot/Admit/Renew/Drop/Sweep folds; Admit keys by kernel-reported PeerCredential.Pid, lease deadline from LeasePolicy.Maintenance.CrashStaleness; Attached read by FleetRoll/ForwardWrite/WireHealth; presence consequence projects PresenceRow at Persistence/sync-collaboration#PRESENCE_AND_BLOB), RosterEntry (nested per-connection lease record), RosterReceipt (readonly record struct; transition kind, peer pid+uid, lease epoch, attached-count, Instant — fanned through ReceiptSinkPort.Send), RosterTransition (Joined/Renewed/Dropped/Swept fold cases), ControlRuntime, VerbReceipt, ControlInbound (SetDegradation->DegradationCell.Force, ReloadOptions->OptionsAdmission.Invalidate landing ReloadReceipt.ControlTrigger wrapping ReloadOutcome.Applied, CaptureSupport->SupportTrigger.ExternalCommand; Fan threads TenantContext.Current), ControlTransport ([Union] UnixDomainSocket-only intake leg on the macOS axis; the WindowsNamedPipe case is DROPPED — PipeHardening/NamedPipeServerStreamAcl.Create stays the design record re-entering as one case only behind a Windows RID, never a second server-host owner), PipeHardening (design-record only on the macOS axis), ServiceHost (AddGrpc/MapGrpcService<ControlServiceImpl>/HealthServiceImpl.SetStatus/Bind folding the Unix sun_path Kestrel endpoint), DegradationCascade (Cascade write over the control hop, Apply->DegradationCell.Cascade), CascadeReceipt, Ucred, Xucred, PeerCredential, PeerAdmission (LibraryImport getsockopt SO_PEERCRED/LOCAL_PEERCRED) — spotlight: inbound gRPC serving counterpart to Outbound.cs; modality axis, host-side attached-peer roster with lease-epoch admit/renew/drop/sweep + presence fan, three-verb fold onto existing owners, UDS+hardened-named-pipe intake, parent-to-child cascade write, accept-side peer-cred read; G7 spec-compile gate covers the generated ControlService base/client and request/reply messages.
- PS-01 PROFILE_AXIS|STORE_LIFECYCLE|CROSS_PROCESS_LAW|PLACEMENT_MATRIX|PROVISIONING_ROWS: StoreKeyPolicy, StoreProfile, StoreRows, BlobRemote (+ nested Descriptor), StoreLifecycle, StoreOpenReceipt, StoreCeremony, StoreLeaseRow, StoreLocality, StorePlacement, ExtensionRequirement — spotlight: six-row engine axis, lifecycle ceremony, cross-process lease, placement fold, provisioning probes.
- PS-02 LANE_AXIS|DOCUMENT_LANE|SEARCH_LANES|GEO_LANES|ANALYTICAL_LANE: DataLane, KvEntry, JsonIndex (+ ColumnType/OpClass columns: jsonb/json + opclass rows), VectorMetric (+ Hamming/Jaccard bit rows, Bit column), FullTextMode, EmbeddingArity (SmartEnum<string> dense/half/sparse/bit precision axis reusing StoreKeyPolicy, clrType/storeType columns), EmbeddingIdentity, VectorQuery<TRow>, FullTextQuery, HybridRetrieve (reciprocal-rank-fusion fold over the vector diskann route + pg_search BM25 route, rrfConstant 60 default, degrades to native ts_rank), GeoLayer, SpatialOp (SmartEnum<string> proximity/coverage/spatial-join translated-predicate row family), SpatialDeltaKind (SmartEnum enter/exit/move/reshape), SpatialDelta, SpatialDiff (change-detection fold over op-log OLD/NEW geometry, feeds AppUi/charts-dashboards#SERIES_TABLE GeoOverlay), TabularExportSpec (+ ColNotSet/Spec round-trip columns), TabularDirection, TabularSpec (+ Fault DuckDBException arm), AnalyticalTraversal (Materialize changefeed-to-parquet fold), DuckDBOpLogMap, ParquetSchemaStamp, RelationSource, RelationSchema, RelationLane (DuckDB RegisterTableFunction<T> in-process managed-sequence relation fold, IO<Unit> Register rail) — spotlight: seven-lane capability map with the closed four-precision embedding arity, reciprocal-rank hybrid retrieval, spatial-query and spatial-change-detection folds over the changefeed, in-process DuckDB table-function relation, HLC-ordered changefeed materialization.
- PS-03 IDENTITY_POLICY|MIGRATION_LAW|GENERATED_COLUMNS|EXTENSION_DDL|CONVERTER_RAIL: IdentityPolicy, SchemaFault, SchemaFingerprint, MigrationReceipt, DerivedColumn, SchemaDdl (+ Composite case + Enum case with Of<TEnum> factory, MapComposites/MapEnums folds), TemporalShape (SmartEnum<string> PG18 WITHOUT OVERLAPS temporal-key shape: primary-key/unique/foreign-key, GiST exclusion via btree_gist), TemporalKey (Shape projection emitting the WITHOUT OVERLAPS/PERIOD clause), ConverterRail (+ SqlitePatterns object-valued NodaTime pattern table) — spotlight: identity axis, migration law, extension DDL rows, composite- and native-enum-type registration, PG18 temporal-key shape over btree_gist, Thinktecture converter rail.
- PS-04 OPERATION_ALGEBRA|PROJECTION_SHAPES|BULK_LANE|INTERCEPTOR_SPINE: StoreFault, StoreOp<T>, StoreOpCompose (Then Kleisli combinator), StoreRail, KeysetPage<TRow>, ProjectionRail (+ BridgeListAsync/BridgeArrayAsync/ReentryListAsync linq2db terminators, DurationRollup NodaTime aggregate), BulkRoute, BulkReceipt, BulkDelta<TRow>, StoreFact, InterceptPolicy, StoreInterceptor, StoreObservability (+ Meters Action<NpgsqlMetricsOptions> shape) — spotlight: total store-op dispatch, self-composing Kleisli Then, pooled-context lease at composition, bulk lane with self-emitted changefeed.
- PS-05 PRAGMA_TABLE|COMPILE_SURFACE|MAINTENANCE_OPS|EXTENSION_GATES: SqliteKeyPolicy, SqliteFactKind, SqliteFact, SqlitePragma, PragmaOps, SqliteCompileSurface, SqliteMaintenancePolicy, FunctionRegistration, SqliteMaintenance, ExtensionGateState, ExtensionGate, EncryptionGate, DbConfig (Hardened db_config flag set, Apply), ExtensionOps — spotlight: e_sqlite3 compile-flag surface, pragma axis, per-connection db_config hardening, gated extensions.
- PS-06 CODEC_AXIS|COMPRESSION_HASHING|SNAPSHOT_PROTOCOL|RESTORE_AND_DIFF|TS_PROJECTION: SnapshotKeyPolicy, WireSurface, PersistenceWireContext, InstantFormatter, SnapshotCodec, GeoJsonProjection (XR7 GeoJsonConverterFactory projection off the Compute GeometryPayload oneof, SRID-4326, RFC-orientation rings), GeneratedMessagePackResolver, PersistenceResolver (AOT [CompositeResolver]/[GeneratedMessagePackResolver] landmark), CompressionPolicy, HashPolicy, SnapshotHeader, SealedSnapshot, SnapshotCatalogRow, Snapshots, RestoreReceipt, SnapshotDelta, SnapshotRestoreOps — spotlight: codec axis with wire-surface membership, AOT resolver and GeoJSON projection landmarks, sealed snapshot protocol, restore choreography.
- PS-07 L2_CONTRIBUTION|MODEL_RESULT_INDEX|ARTIFACT_BLOB_INDEX|BENCHMARK_INDEX: CacheContribution, CacheResidence (SmartEnum<string> in-memory/sqlite/redis/distributed-pg L2 residence axis, each row carrying its IDistributedCache registration; Redis row registers RedisCache : IBufferDistributedCache via AddStackExchangeRedisCache), CacheResidenceKeyPolicy (package-local comparer accessor), ModelResultKey, CacheIndexFact, IndexSurface, ArtifactIndexRow (+ EpContext warm-start class), BenchmarkRow (+ Route column, Claim fingerprint-match recency-bounded gate, RouteOf projection, BenchmarkRowClass) — spotlight: one CacheContribution capsule selecting its L2 residence over the closed four-tier CacheResidence axis, result/artifact/benchmark indexes, recency-bounded benchmark claim.
- PS-08 OPLOG_CHANGEFEED|MERGE_LAW|TRANSPORT_AXIS|PRESENCE_AND_BLOB|TS_PROJECTION: SyncKeyPolicy, SyncOpKind, OpLogEntry, SyncCursor, OpLog, ConflictReceipt, ConflictOutcome, SyncApplyReceipt, SyncSession, SyncMerge, SyncTopology, SyncDirection, SyncTransport, SyncPump, PresenceRow, Presence — spotlight: HLC op-log changefeed, merge law, transport axis with M1-M5 fields, presence.
- PS-09 CLASSIFICATION_ENFORCEMENT|RETENTION_SWEEPS|EXPORT_PROOF|AUDIT_BINDING: ArtifactClassRow, ArtifactClasses, ClassificationGuard, RetentionKeyPolicy, RetentionPolicy, ArtifactFacts, SweepReceipt, RetentionSweep, ExportProof, StoreEvidence, AuditBinding — spotlight: classification enforcement consuming the AppHost taxonomy, receipted sweeps, audit binding.
- PS-10 OBJECT_STORE (remote-stores.md): ObjectStore (SmartEnum<string> three-provider axis reusing StoreKeyPolicy, UseDelegateFromConstructor Put/Fetch/Head partials, Placement projecting BlobRemote), ObjectClient (Union S3/Azure/Gcs over IAmazonS3/BlobContainerClient/StorageClient), RemoteStoreFault (Union Text/NotFound/Conflict/Aborted/Transport Expected dual-tier Create, Lift folding AmazonS3Exception/RequestFailedException/GoogleApiException) — spotlight: one provider axis behind BlobRemote, boundary-exception lift at the single edge.
- PS-11 MULTIPART_TRANSFER (remote-stores.md): TransferReceipt (provider/content-key/bytes/parts/resumed-parts/abort/Duration/Instant/CorrelationId), MultipartTransfer (Upload reads realized Parts/ResumedParts from provider Put; Windows part/chunk partition spanning whole 64-KiB frames), ObjectRows (S3Multipart Initiate/UploadPart/Complete with ListParts resume, AzureBlocks StageBlock/CommitBlockList with GetBlockList resume, GcsResumable UploadObject chunked; Fetch/Head/Delete/List) — spotlight: real part-by-part resumable ceremony, skip-on-committed-ETag; SPIKE get/head/list/delete member spellings (OBJECT_MEMBER_SPELLINGS).
- PS-12 OBJECT_RESIDENCE (remote-stores.md): ObjectResidence (Descriptor/PartSize/ChunkSize/UploadId/ConditionToken/Parts/ResumedParts/Correlation; From/Name/Metadata/OfMetadata/Key/OfHead/OfProperties/OfObject/Descriptors provider-response projections) — spotlight: content-key descriptor round-trip, one OfMetadata projector per provider head.
- PS-13 ARTIFACT_SYNC_FEED (remote-stores.md): ObjectTransferFact (kind/provider/content-key/bytes/part-index/Duration/Instant/CorrelationId; Part/Abort/FetchHit constants), ArtifactSyncFeed (Announce appends op-log row, Fetch emits fetch-hit fact) — spotlight: cloud-hub seam riding the settled BlobRemote + OPLOG_CHANGEFEED, never a second sync engine.
- PS-14 TIMESCALE_PROVISIONING (server-tier.md): TimescaleProvisioning (Hypertable/ContinuousAggregate/ContinuousAggregatePolicy/RetentionPolicy/ColumnstorePolicy SQL projections; Provision folds MigrationBuilder.Sql steps) — spotlight: TimescaleDB raw-SQL provisioning over the OpLogEntry rollup, HLC Physical time column.
- PS-15 SEARCH_PROVISIONING (server-tier.md): SearchProvisioning (DiskAnnIndex memory_optimized storage_layout, Bm25Index key_field; Provision folds MigrationBuilder.Sql) — spotlight: pgvectorscale diskann + pg_search BM25 index DDL, no EF translator, preload-gated.
- PS-16 CLUSTER_CONFIG (server-tier.md): ClusterConfig (Rows (setting,value,fallback) triples incl io_method io_uring/worker, SettingsProbe, Verify admitting value-or-fallback) — spotlight: deploy-time GUC fragments verified read-only, io_uring portability as a row triple not a pinned literal.
- PS-17 TENANCY_RLS (server-tier.md): TenancyModel (SmartEnum<string> single/schema/rls/db-per-tenant reusing StoreKeyPolicy), RlsPolicy (Policy projecting ENABLE ROW LEVEL SECURITY + CREATE POLICY on current_setting('rasm.tenant')::uuid only for the rls row) — spotlight: one isolation axis tying host profile to RLS via the AppHost TenantContext, never a forked store.
- PS-18 MIGRATION_BUNDLE (server-tier.md): MigrationPlan (Apply/ExpandBeforeContract/DestructiveApprovalRequired), MigrationBundle (Plan folding applied/known/pending against the deploy gate, store-newer-than-binary rejection) — spotlight: idempotent ScriptMigration + self-contained bundle deploy gate, expand-precedes-contract.
- CP-01 INTENT_FAMILY|SUBSTRATE_AXIS|DISPATCH_SPINE: ComputeIntent (+ nested Spec), AdmittedIntent, IntentAdmission, ComputeKeyPolicy, BenchmarkRank, SelectionContext (+ warm-affinity reorder column, boot-frozen Providers), SubstrateProbes, Substrate, ComputeFault, SelectionHop, SelectionReceipt (+ warm-affinity flag), SubstrateSelection, DispatchTable, ComputeIntent.Generate (6th union case: Generate(UInt128 Model, string Prompt, GenerationPolicy Policy)), Substrate.GenAi (4th SmartEnum row), SubstrateProbes.GenAi — spotlight: typed intent spine composing the package's axis vocabularies; total dispatch with warm-affinity tie-break; the Generate intent case and GenAi substrate row admit token-streaming through the one admission fold.
- CP-02 TENSOR_VOCABULARY|LAYOUT_ALGEBRA|GEOMETRY_ENCODING|OPERATION_TABLE|KERNEL_DISPATCH|EQUIVALENCE_INTEROP: TensorKeyPolicy, QuantizationPolicy, TensorDtype, TensorVocabulary, ToleranceClass, LayoutForm, TensorLayout, EncodingChannel, GeometryEncoding, EncodedTensor, TensorOpKind (12 kind rows), TensorOpFamily (84-row table under OPERATION_TABLE), kernel delegate family (Unary/Binary/Ternary/Shift/Convert/Fold/PairFold/Index under KERNEL_DISPATCH), TensorKernels<T>, IntegerKernels<T>, ConvertKernels<TFrom,TTo>, TensorOps, EquivalencePolicy, EquivalenceProof, EquivalenceLaw — spotlight: TensorPrimitives families with the operation table split from the kernel dispatch, geometry-to-tensor encoding. (Ledger anchor repair: the prior OPERATION_FAMILIES cluster was split into OPERATION_TABLE + KERNEL_DISPATCH on the live page.)
- CP-03 MODEL_IDENTITY|SESSION_CAPSULE|EP_AXIS|EXTENSION_OPS|INFERENCE_MODES|RESULT_CACHE|GENERATIVE_RUN: ModelSource, ModelIdentity (+ Slot), SessionPolicy (+ WarmStartClassification/WarmStartRetention), ModelSessions (Boot global pool from CpuBudget OrtIntraOp/OrtInterOp/SpinControl, Lease returns warm-start ArtifactIndexRow), ModelKeyPolicy, ExecutionProvider (Cpu/CoreMl rows: 2 EP rows on the single osx-arm64 RID; CoreML seven option keys with CoreMLFlags + ModelCacheDirectory/MLComputeUnits/SpecializationStrategy; the Cuda/DirectMl GPU rows are DROPPED from the live macOS axis — their AppendExecutionProvider_CUDA(0)/_DML(0) member spelling is the FINALIZED design record on [EP_EXECUTION], re-entering as one row each only behind a GPU-carrying RID), CustomOps, RunOps (+ RunConfig fold into AddRunConfigEntry, Terminate latch, InferBound RunWithBinding over OrtIoBinding, InferBoundNamed name-keyed RunWithBindingAndNames projecting Seq<(string Name, OrtValue Value)> without the forbidden DisposableNamedOnnxValue collection, BoundLoop zero-allocation steady-state Pulse hot path, Chunked + Embed + Classify argmax-over-logits + ClashScore scalar-output run-mode cases over the interchange PointScan/ClashPair encodings), RunConfig, CachePolicy, CacheOps, GenerationPolicy (search-option + prompt-assembly record, SetSearchOption(string,double|bool) fold, ApplyChatTemplate prompt assembly, SetGuidance), GuidanceKind (SmartEnum<string> none/json-schema/regex/lark-grammar/choice), GenerativeRun (boundary capsule over Microsoft.ML.OnnxRuntimeGenAI — Stream IAsyncEnumerable<string> / Collect / Receipt, OnnxRuntimeGenAIException lifts to ComputeFault.ModelRejected, OnnxRuntimeGenAIChatClient:IChatClient projection) — spotlight: session capsule with capture-then-dispose eviction, EP-context warm-start route, EP-parameterized provider axis, run-config posture table, ORT-GenAI token-streaming generative run with grammar-constrained guidance and chat-template prompt assembly; Chunked/Embed run modes.
- CP-04 PROTO_VOCABULARY|CONTRACT_EVOLUTION|FAULT_PROJECTION|TRANSPORT_AXIS|CALL_POLICY|ARTIFACT_FRAMES|TS_PROJECTION: WireServices, ContractDrift, ParseGuard, ContractGuard, WireFault, FaultDetail (proto), WireKeyPolicy, StreamShape, NodeSelection (affinity populated), ComputeEndpoint, RemoteTransport (4 rows on the live macOS axis: Http2/GrpcWeb/UnixDomainSocket/InProcess; the Windows-only NamedPipe PipeSecurity-ACL row and the bearer-plus-DACL TcpLoopback row are DROPPED — the two security-shape member spellings stay the design record on [PIPE_SECURITY], re-entering as one row each only behind a host RID that admits the byte path, never blurred into one credential), WireChannels (attach/open/observe/redial), CredentialPolicy (5 rows InsecureLoopback/Tls/Mtls/Bearer/Composed projecting ChannelCredentials; Composed stacks ≥2 CallCredentials.Compose mints onto the TLS channel, single-mint collapsing to a bare FromInterceptor bind and empty to plain SecureSsl), CompressionProviders (SmartEnum<string> Identity/Gzip/Deflate claim-gated wire-encoding axis projecting inbox ICompressionProvider; Register materializes GrpcChannelOptions.CompressionProviders, CallSpine.Compressed stamps the grpc-internal-encoding-request RequestEncodingKey per winning Option<CompressionProviders> claim, absent claim leaves the call uncompressed), CallSpine (the one client interceptor stamping correlation + traceparent + deadline + payload edges), FrameEdge, ArtifactFrame (proto), Solve rpc (SolveRequest->SolveResponse unary, numeric-lane decomposition field-for-field, dialed by the ShardPlan row-block sub-solve), Generate rpc (GenerateRequest->TokenChunk server-stream, remote token-streaming leg), GraphDiff rpc (GraphDiffRequest->GraphDiffResponse unary, content-key delta — diff algebra is Persistence sync-collaboration#TRANSPORT_AXIS, this carries the wire shape only), SubtreeFetch rpc (SubtreeFetchRequest->GraphChunk server-stream, partial-graph checkout) — bringing ComputeService to 18 rpc rows; spotlight: the suite wire vocabulary (five services), transport rows incl. in-process and designed-only byte paths, descriptor-diff evolution, frame law, numeric-solve + remote-generate rpc legs and the GraphDiff/SubtreeFetch content-key delta message family.
- CP-05 ALLOCATION_AXIS|PLANE_VIEWS|STREAM_POOL: StagingKeyPolicy, AllocationClass, AllocationEvidence, StagingViews (BitHelper voxel mask, span tokenizers, ArrayPool grow), StreamPoolPolicy, StreamPool, PoolEvidence (eleven-event sink projection, drop-callback evidence) — spotlight: allocation classes, HighPerformance plane views, eleven-event pooled stream evidence.
- CP-06 LANE_AXIS|SOLVE_GUARD|CPU_BUDGET|DRAIN_CANCEL: WorkLane, LaneHandle, WorkItem, LaneRuntime, CpuBudget (+ SpinControl latency-vs-CPU posture, OrtIntraOp/OrtInterOp), LaneDrain — spotlight: bounded channel lanes, solve-path guard, one linked cancellation chain, ORT spin posture.
- CP-07 PHASE_FAMILY|PROGRESS_CELL|OBSERVATION_SEAMS|TS_PROJECTION: ProgressKeyPolicy, ProgressPhase, ProgressMark, SubscriptionPolicy, ProgressCell, ProgressSeams — spotlight: monotonic progress with mechanical no-regress gate.
- CP-08 QUANTITY_TABLE|DIMENSIONAL_LAW|PARSE_FORMAT: QuantityKeyPolicy, QuantityFamily, UnitPolicy, UnitAlgebra, UnitEvidence — spotlight: conversion-at-admission, three-arity admit.
- CP-09 RECEIPT_UNION|FOLD_PROJECTIONS|WIRE_STAMPS|BENCHMARK_CLAIMS|TS_PROJECTION: ComputeReceipt, ComputeReceipt.Factorization (Provider, Decomposition, Rows, Cols, Nnz, Format — numeric-lane dense+sparse solve evidence), ComputeReceipt.Generate (ModelChecksum, Ep, ModelType, Tokens, TokensPerSecond, GuidanceKind, ConstrainedTokens, ToolCalls — generative-run evidence, also carries the Embed run slot), ComputeWireContext, ReceiptSurface, ReceiptFolds, WireStamps, HostFingerprint, BenchmarkClaim — spotlight: 15-case receipt union with Factorization + Generate cases wired into the [JsonDerivedType] rows and ComputeReceiptWire/ComputeReceiptKind literals, NodaTime-protobuf stamps, fingerprint-gated claims.
- CP-10 DENSE_ALGEBRA|SPARSE_SOLVE|KERNEL_LOWERING|PROVIDER_CLAIMS (numeric-lane.md), one Numeric/Lane.cs closure: NumericKeyPolicy, LinearProviderProbes, LinearProvider (managed/native-mkl/native-openblas 3 RID-keyed rows, Control.TryUse* boolean probe + activate delegate, Select consumes resolved Option<BenchmarkRow> claim), FactorizationKind (5 rows), Factorization (Union LU/QR/Cholesky/SVD/EVD over MathNet decompositions, Kind/Solve folds), DenseOps (Decompose/Gemm/Receipt, PooledMemory AllocationClass), SparseFormat (csr/csc/coo/dok ingestion axis over SparseCompressedRowMatrixStorage<double>), SparseOps (Ingest via Of* factories, SolveDirect over CSparse CompressedColumnStorage<double>.OfIndexed + SparseCholesky/SparseLU/SparseQR), ShardPlan (Union Single/Blocked(int Tile) row-block GEMM tiling), ConvWindow (record Kernel/Stride/Padding/Dilation/Channels/Filters/Spatial lowering geometry descriptor), KernelLowering (Lower MatMul->GEMM live; the convolution overload Lower(row, input, kernel, ConvWindow, plan) drives Im2Col patch projection then one Matrix<double>.Multiply GEMM live for Conv1D/Conv2D/Conv3D inheriting the MatMul ToleranceClass.Tight proof; Pool strided-window TensorPrimitives fold; a Conv row without a ConvWindow returns <lowering-row-miss>) — spotlight: RID-keyed BLAS provider table with managed terminal, five-case factorization union, CSR-backed sparse ingestion + CSparse direct solve, kernel-lowering binding for tensor matrix/structural rows, row-block shard fan-out dialing remote-lane Solve rpc; FINALIZED — managed terminal proved end-to-end on osx-arm64, native MKL/OpenBLAS execution is a per-RID deploy-asset gate tracked in Compute TASKLOG [1]-[RESIDUAL_TIER3_PROBES], never a page RESEARCH probe.
- AU-01 HOST_AXIS|EMBED_CAPSULE|SCHEDULER_BOUNDARY|NATIVE_ASSETS|SCALE_FOCUS: SurfaceHost, SurfaceFault, SurfaceSeam, SurfaceRow, SurfaceReceipt, SurfaceSession, Surfaces, EmbedOptions, EmbedCapsule, SurfaceScheduler, NativeAssetReceipt, NativeAssetRow, NativeAssets, SurfaceFact (+ AppearanceChanged(bool Dark) 4th case beside ScaleChanged/VisibilityChanged/FocusChanged, folded onto Surfaces.FactInstrument per case kind through the one AppUiTelemetry.Contribute spine; macOS NSScreen.BackingScaleFactor + appearance flips feed host-side, appearance facts drive the host-matched variant re-probe) — spotlight: one host axis, one mount transaction, embed capsule, scheduler boundary, native-asset receipts, the four-case closed host-fact union with the appearance signal.
- AU-02 ROUTING_SPINE|DOCK_LAYOUTS|SHELL_CHROME|ADAPTIVE_LAYOUT: NavRequest, ShellRoot, DockableRow, ShellDockFactory, ShellPolicy, LayoutCheckpoint, LayoutPersistence, LayoutLedger, ChromeKeyPolicy, ChromeSlot, ChromeRow, ShellChrome, BreakpointRow, AdaptiveLayout — spotlight: routing spine, dock layouts with checkpoint and crash restore, chrome derivation, adaptive breakpoints.
- AU-03 SCREEN_CATALOG|ACTIVATION_SCOPES|DERIVED_STATE|VALIDATION_UX|SCREEN_STATE: ScreenCatalogRow, ScreenCatalog, ScreenRuntime, ScreenBase, ScreenFault, DerivedOps, AdmissionState, ScreenValidation, ScreenStatePolicy, ScreenState, ScreenStateOps — spotlight: screen-catalog derivation, activation scopes, derived state, validation lift, state snapshots.
- AU-04 INTENT_TABLE|AVAILABILITY_ALGEBRA|EXECUTION_RECEIPTS|PALETTE_AND_REMOTE|TS_PROJECTION: CommandIntent, CommandPayload, CommandDeck, CommandGate, CommandOutcome, CommandReceipt, CommandExecution (+ TelemetryRow via AppUiTelemetry contributing command-outcome/elapsed instruments), CommandProjections, CommandIntentWire, CommandAvailabilityWire, CommandInvocationWire, CommandPayloadWire, CommandOutcomeWire, CommandReceiptWire — spotlight: one intent table with derived menus/toolbars/palette/deep-links/remote, availability algebra, execution receipts, telemetry contribution.
- AU-05 DATA_SOURCES|CHANGE_PIPELINES|BINDING_CAPSULE|AGGREGATION_SPINE: HostDocumentFact, SourcePolicy, DataSource<TRow,TKey>, PipelineInputs<TRow>, BindingCapsule, LiveDataOps — spotlight: closed sourcing axis, change-set pipelines, one binding capsule, aggregation folds.
- AU-06 GRID_SUBSTRATE|VIEW_STATE|TREE_FLATTEN|GRID_COMMIT: TableCellKind, TableColumnRow<TRow>, TableSurface, TableViewState (+ Window field), WindowState, ViewStateSurface (Apply constructs WindowState from realized Page/Virtualise bounds), TreeRow<TRow>, ExpansionState<TKey>, TableProjection<TRow,TKey>, ProjectionFold, ExportDestination, TableExportSpec, TableCommit<TRow>, CommitSurface (+ TelemetryRow commit-outcome/export-volume) — spotlight: column metadata family, explicit view state with paging/virtual window restore, tree-flatten fold, grid-edit commit path.
- AU-07 INSPECTOR_SURFACE|EDITOR_FACTORIES|COMMIT_VALIDATION|OPTIONS_INSPECTOR|CONFLICT_RESOLUTION|CODE_EDITING: InspectorPolicy, InspectorSurface, EditorKeyPolicy, EditorFactory, EditFault, EditOutcome, EditReceipt, EditGate, OptionsInspector<T>, ConflictPane<TReceipt>, CodePane, CompletionRow — spotlight: inspector surface, ranked editor rows, commit validation, options inspector, conflict pane, code panes.
- AU-08 SERIES_TABLE|AXES_SECTIONS|CHART_INTERACTION|STREAM_BINDING|DASHBOARD_TILES: ChartKeyPolicy, ChartCanvas, ChartSeriesSpec (Baseline derives the matching CaptureRow per (ThemeVariantRow, DensityRow) cell — render-hash twin), ChartAxisKind, ChartSection, ChartAnchor, ChartPolicy, ChartStream, ChartFolds, GeoOverlay (in-place HeatLand land-swap fold over the Persistence SpatialDiff IChangeSet<HeatLand,string> under the chart SyncContext lock, never a DrawnMap re-load per delta), DashboardTile (+ Custom 5th case), TilePlacement, DashboardLayout, DashboardSurface (RenderReceipt sinks via ReceiptSinkPort, chart-render telemetry via TelemetryContributorPort), CrossFilter (board-scoped linked-brushing fold over DashboardSurface via DynamicData dynamic-predicate Filter(IObservable<Func<TRow,bool>>); source tile self-excludes), FilterState (brush-state carrier reapplied at mount) — spotlight: series axis, scale rows, one interaction policy, stream binding, dashboard tiles with the Custom kind, in-place geo overlay land-swap, cross-tile linked brushing, render-hash baselines.
- AU-09 DRAW_CAPSULE|THUMBNAIL_PIPELINE|PREVIEW_SURFACES|ENCODE_IDENTITY|DOCUMENT_EXPORT: DrawSource, Offscreen, VisualRuntime, ThumbnailRow, Thumbnails, PreviewRow<TReceipt>, RenderReceipt, NativeAssetFact, VisualCodec, VisualCodec.EncodeRow (+ PngP3/PngRec2020/PngScrgb gamut rows over ColorPolicy DisplayP3/Rec2020/ScrgbFloat, the encode-side projection of the custom-visual ColorSpaceAxis sourcing SKColorSpace.CreateRgb(SKColorSpaceTransferFn,SKColorSpaceXyz) on the per-row Surface SKColorType), VisualCodec.ColorPolicy (Display/WideGamut/DisplayP3/Rec2020/ScrgbFloat with Working/Output SKColorSpace + Surface SKColorType columns), VisualDestination, VisualExportSpec (+ BreakRule policy, HeaderFooterBand band), FlowBlock (Text/Tile/Rule cases), HeaderFooterBand, FlowLayout (SKDocument flow-fold paginator), VisualExport — spotlight: borrowed/owned draw capsule, thumbnails, previews, content-hashed float-pipeline encode identity with the four-row wide-gamut ColorPolicy projection of ColorSpaceAxis, paginated document export.
- AU-10 TOKEN_CATALOG|VARIANT_AXIS|DENSITY_AXIS|CONTROL_THEMES: TokenRow, ResolvedTheme, ThemeCatalog, ThemeKeyPolicy, ThemeVariantRow, DensityRow, ThemeSwitchReceipt, ThemeCell, ThemeRail — spotlight: frozen token catalog, orthogonal variant and density axes, atomic theme swap with receipts.
- AU-11 ROLE_AXIS|FONT_ADMISSION|SHAPING_RAIL|MARKDOWN_PROJECTION|TEXT_METRICS: TypographyKeyPolicy, TypographyRole, TextStyleRow, FontChain, FontAdmission, ShapingSurface, MarkdownRow, InlineRun, MarkdownDocumentRows (Body/FrontMatter/Footnotes; Project fold over TrackTrivia source spans), MarkdownProjection, TextMetricsPolicy — spotlight: role axis, deterministic font admission, shaping rail, markdown projection with front-matter/footnote rows, text metrics.
- AU-12 ICON_AXIS|SVG_PIPELINE|RASTER_ASSETS|ASSET_CATALOG: AssetFault, IconSource, IconRow, IconSurface, ScenePolicy (PictureOnly/RetainedScene/Animated), SvgPipeline (Load with ScenePolicy + animation handler), RasterRow, RasterAssets, AssetKeyPolicy, AssetKey, AssetKind, AssetRow, AssetReceipt, AssetKeys, AssetCatalog — spotlight: icon sourcing with fallback ranks, retained-scene SVG pipeline, raster rows, nameof-derived asset catalog.
- AU-13 DIALOG_INTENTS|SESSION_ALGEBRA|NOTIFICATIONS|PICKERS_HOST_MODALITY: DialogIntent, DialogFault, DialogTopology, DialogSurface, NoticeKeyPolicy, ToastRow, ToastOutcome, ToastReceipt, ToastGate, PickKind, PickFilter, PickOps — spotlight: dialog intents with typed results, session topology, toast suppression fold, host-agnostic pickers.
- AU-14 HOTKEY_DERIVATION|BEHAVIOR_RAIL|POINTER_GESTURES|DRAG_CLIPBOARD|INPUT_FABRIC: GesturePolicy, BehaviorRail, PanZoomRow, DragPayload, ClipboardRow, InputDevice ([Union] alternative-input source family — 3Dconnexion/MIDI/gamepad/stylus/eye-tracker), DeviceAxis (normalized continuous-axis sample), DeviceOutput ([Union] device-output sink family — haptic/LED), InputFabric (device-to-intent and intent-to-device fold onto the CommandIntent table, no second input owner) — spotlight: hotkey derivation from the frozen deck, behavior rail, pan-zoom rows, typed drag and clipboard payloads, the alternative-input device fabric folding onto the one command-intent table.
- AU-15 MOTION_AXIS|MOTION_APPLICATION|PHASE_MAPPING|REDUCED_MOTION: MotionKeyPolicy, SpringValue, MotionToken, MotionPlan, MotionApplication, PhaseMotion, MotionProbeRow, MotionReceipt, ReducedMotion — spotlight: motion axis with reduced pairs, application plans, phase mapping, one reduced-motion switch.
- AU-16 AUTOMATION_PEERS|KEYBOARD_NAV|CONTRAST_GATE|COMPLIANCE_PROOF: AnnouncementRow (+ custom-visual live-region row carrying AutomationLiveSetting/Texts/Synthesized), AccessOps, FocusOps, ContrastReceipt, ContrastGate, AccessAudit, AccessProof — spotlight: automation peers including the synthesized custom-visual announcement row, focus law, the single contrast gate, compliance proof grid.
- AU-17 LOCALE_AXIS|STRING_TABLES|CULTURE_COMPOSITION|RTL_MIRRORING: LocaleKeyPolicy, LocaleRow, LocaleStrings, LocalePolicy, ResolvedLocale, LocaleRuntime, MirrorPolicy — spotlight: locale axis, nameof string tables, atomic culture composition, RTL mirroring.
- AU-18 RECEIPT_UNION|CORRELATION_JOIN|CAPTURE_LANES|HEADLESS_DERIVATION|DEV_LOOP|TS_PROJECTION: EvidenceReceipt, EvidenceOps, AppUiTelemetry (Contribute(version, params instruments) — the one parameterized TelemetryContributorPort surface every AppUi owner calls), AppUiWireContext, SkewBand, EvidenceRow, EvidenceTimeline, EvidenceJoin, CaptureRow (+ Scale DPI column, Ticks deterministic-frame column, Grab Func<double,Func<IO<Unit>>,IO<SKImage>> delegate; custom-visual lane is one CaptureRow per kind×gamut cell whose Grab materializes through CustomVisual.Materialize and encodes through VisualCodec at the kind ColorSpaceAxis row, keyed key@scale×gamut), Captures, EvidenceKeyPolicy, ProofCheck, ProofSpec, ProofEngine, DevLoop, EvidenceReceiptWire, SurfaceReceiptWire, NativeAssetFactWire, SkewBandWire, EvidenceRowWire, EvidenceTimelineWire — spotlight: evidence union, correlation join, capture lanes incl. the per-kind×gamut custom-visual render-hash cell, headless proof derivation, dev loop, suite-shared telemetry contribution surface.
- AU-19 SKIA_KINDS|COLOR_SPACE (custom-visuals.md): CustomVisual (Union sankey/treemap/waterfall/funnel), CustomVisualData, CustomVisuals (Kind/RenderedInstrument/LayoutInstrument constants, TelemetryRow via AppUiTelemetry contributing rendered + layout-elapsed instruments, Sankey/Treemap/Waterfall/Funnel layout folds, Bruls squarified Pack/Worst/LayoutRow), ColorSpaceKeyPolicy, ColorSpaceAxis (4 rows srgb/display-p3/rec2020/scrgb-float carrying Working SKColorSpace factory + SKColorType surface + VisualCodec.EncodeRow Encode) — spotlight: four Skia diagram kinds as pure layout folds composed through DrawSource.Owned with per-cell render-hash twins, the single suite-wide wide-gamut color-space vocabulary the codec working-space consumes.

- AH-04a SOURCE_AXIS: ConfigSource [SmartEnum<string>], ConfigSourceKeyPolicy, ReloadClass [SmartEnum<string>], ConfigLayer — spotlight: the rank+reload-class source axis with per-source factory delegate and precedence fold.
- AH-04b TYPED_BINDING: PolicyBinding, ConfigError — spotlight: source-generated AOT-safe bind into immutable policy records with fail-closed unknown-key admission folding into Validation<ConfigError,T>.
- AH-04c POLICY_VALUES: OptionsAdmission, ReloadReceipt, ReloadOutcome [Union] — spotlight: validate-once frozen policy publish, reload-class-gated OptionsMonitor transition, ControlService reload-options consequence.
- AH-04d KILL_SWITCH: KillSwitchConfig, OperatorOverride [Union] — spotlight: operator-forced degradation as a reload-class:transition config row feeding the health-and-degradation fold.

- AH-14 DESCRIPTOR_AXIS|DISCOVERY_FOLD|COMMAND_ALGEBRA|GRANT_BROKER|SDK_CODEGEN|TS_PROJECTION (capability-registry.md): EffectClass (SmartEnum<string> five-row effect taxonomy), CapabilityKeyPolicy (the one capability-suite ordinal accessor reused by live-wire/mcp-projection/sandbox-host/solver-plugin), Idempotency, CostUnit, CostModel, PermissionShape, CapabilityDescriptor, DescriptorReceipt, CapabilityRegistry (frozen catalog + alternate-lookup probe), DiscoveryQuery [Union], DiscoveryResult, CommandTxn [Union], CommandFault [Union 4600 band], CommandReceipt (repo homonym with AppUi CommandReceipt), CommandAlgebra (commit-or-rollback over GrantBroker + Compute ComputeIntent/IntentAdmission dispatch), GrantScope, Consent [Union], Budget, GrantFault [Union 4620 band], GrantBroker, SdkTarget (3 emission targets), SdkArtifact, SdkCodegen, CapabilityDescriptorWire, CapabilityCommandReceiptWire (force-disambiguated from AppUi CommandReceiptWire), DiscoveryResultWire — spotlight: one self-describing op catalog over the effect/idempotency/cost/permission axis, the shape-discriminated discovery fold, the command-algebra transaction onto the Compute dispatch rail, the scoped grant-and-cost broker, polyglot SDK codegen; consumes ComputeIntent/WorkLane/TenantContext/DegradationLevel/ReceiptSinkPort/DataClassification, mints no eighth port.
- AH-15 DETERMINISM_KERNEL|EVENT_LOG|REPLAY_VERIFY|MACRO_ENGINE|RECOMPUTE_GRAPH|TS_PROJECTION (determinism-and-replay.md): FloatMode (SmartEnum<string>), DeterminismContext, EnvFingerprint, DeterminismKernel, LogEntry (content-addressed command-log entry), ContentHash (chain-hash value object), EventLog (append-and-verify over OpLog/OpLogEntry + HostFingerprint), ReplayOutcome [Union], ReplayFault [Union 4760 band], ReplayVerify (per-step content-hash replay proof; the notebook bit-identity check is the suite-unique NotebookReplay riding this), Macro, MacroParameter, MacroEngine, RecomputeNode, RecomputeGraph, LogEntryWire, ReplayOutcomeWire, DeterminismContextWire — spotlight: the reproducibility kernel pinning RNG/float/env, the hash-chained content-addressed event log, the per-step content-hash replay proof, the macro record/replay unit, the partial-recompute dependency walk; consumes CommandReceipt/CommandAlgebra/ReceiptSinkPort/CorrelationId/TenantContext, mints no eighth port.
- AH-16 TRANSPORT_AXIS|BINDING_SPEC|WRITE_BACK|BINDING_HEALTH|TS_PROJECTION (live-wire.md): ExternalTransport (SmartEnum<string> eight-row OPC-UA/Modbus/MQTT/serial/REST/GraphQL/spreadsheet/ERP-PLM under CapabilityKeyPolicy), TransportRow, TransportRows (frozen table + total dispatch), WireFault [Union 4720 band] (repo homonym with Compute WireFault), ExternalValue, BindingDirection [Flags], BindingSpec, CoercedValue (unit-coerced over Compute UnitAlgebra/UnitPolicy at the edge), LiveWire (reactive binding engine), WriteBack [Union], WriteReceipt, WriteBackSurface (commit-or-rollback), BindingState (SmartEnum<string> connect/subscribe/stale/fault), BindingHealth (projects onto the health fold), BindingStatusWire, WriteReceiptWire, CoercedValueWire — spotlight: the bidirectional industrial-binding axis under one adapter contract, the binding spec with edge unit coercion, the write-back acknowledgement transaction, the per-binding health lifecycle; consumes QuantityFamily/OutboundHop/SchedulePort/CommandAlgebra/DeadlineClass, mints no eighth port.
- AH-17 METHOD_AXIS|TOOL_DISPATCH|STREAM_PROGRESS|TS_PROJECTION (mcp-projection.md): McpMethod (SmartEnum<string> under CapabilityKeyPolicy), ToolProjection (descriptor-to-tool fold over CapabilityRegistry), McpTool, McpResource, McpPrompt, McpFault [Union 4640 JSON-RPC band], CostPreview (dry-run pricing over GrantBroker), ToolResult, McpDispatch (brokered dispatch over CommandAlgebra/ControlInbound.DispatchTool), ProgressFrame [Union], ResumeToken, AgentSession (per-agent stream-and-backpressure cell), StreamProgress (server-stream fan over OutboundHop.ServerStream + CancelScope), McpToolWire, ProgressFrameWire, CostPreviewWire — spotlight: the MCP serving surface projecting the registry onto a tool catalog, the dry-run cost preview and brokered dispatch, the resumable server-stream progress fan with keyed backpressure; consumes CapabilityRegistry/DiscoveryQuery/CommandAlgebra/GrantBroker/CancelScope/TenantContext, mints no eighth port.
- AH-18 ISOLATION_AXIS|GRANT_HANDLE|QUOTA_CONTROL|SUPPLY_CHAIN (sandbox-host.md): SandboxIsolation (SmartEnum<string> two-row WASM-component/out-of-process under CapabilityKeyPolicy), SandboxRow, SandboxRows (frozen table + total dispatch), PluginInstance (loaded-plugin capsule), SandboxFault [Union 4660 band], GrantHandle (brokered capability handle; zero ambient authority), BrokeredCall, GrantHandleSurface, QuotaShape, QuotaCell (live-metering boundary capsule), Quarantine [Union], QuotaControl, PluginArtifact, Attestation (SLSA provenance), SemverGate, SupplyChainFault [Union 4680 band], SupplyChainGate — spotlight: the capability-brokered plugin sandbox with no-ambient-authority load, the brokered grant handle, the CPU/memory/wall/egress quota cell and kill-quarantine rail, the signature/SLSA/semver supply-chain gate before load; consumes CapabilityDescriptor/GrantBroker/GrantScope/CommandAlgebra/OutboundHop.CompanionSpawn/Discovery/PeerAdmission/CancelScope/DegradationCell, mints no eighth port.
- AH-19 SOLVER_KIND|PLUGIN_CONTRACT|SOLVER_HOSTING (solver-plugin.md): SolverKind (SmartEnum<string> seven extension-category rows under CapabilityKeyPolicy: solver/mesher/optimizer/CAM-post/material-model/field-codec), KindContract, KindContracts (frozen table + total dispatch), SolverFault [Union 4700 band], SolverManifest, OpDeclaration, SolverPluginContract (contract validation), HostedSolver (loaded-and-projected capsule), Negotiation (representation negotiation), SolverHosting (sandboxed load over SandboxIsolation/PluginInstance, projects declared ops into CapabilityRegistry, proves the representation contract against GeometryEncoding/EncodedTensor before first solve) — spotlight: the extensibility contract binding a sandboxed plugin to the Compute dispatch rail, the declared representation/op/descriptor contract, the hosting projection with pre-solve representation negotiation; mints no eighth port.

- CP-11 FORMAT_AXIS|IMPORT_RAIL|EXPORT_RAIL|TWO_HOP_TESSELLATION|FIELD_RESULT_CODEC|GEOMETRY_DELTA|CONTENT_ADDRESSING (interchange.md): InterchangeKeyPolicy, InterchangeFormat (SmartEnum<string> rows: media-type, extension set, CanImport/CanExport, codec-owner, TessellationRequiresCompanion, UpAxis/Handedness columns), InterchangeCodec (SmartEnum<string> codec-owner vocabulary), UpAxis, Handedness, FrameNormalization (coerce imported coordinate into the canonical DDG-kernel frame), InterchangeIo (import + export fold dispatching managed glTF/GLB over SharpGLTF SceneBuilder/MeshBuilder + Draco/Meshopt + 3D-Tiles, STL/3MF/OBJ/PLY, E57/LAS/LAZ/PTS, CGNS/EnSight/VTK/Zarr, in-proc IFC/IFC5 over GeometryGym DatabaseIfc, the AP242/native companion two-hop), ImportedGeometry, PointScan, FieldArtifact, IfcSemanticModel, ExportArtifact, TessellationRequest (two-hop IFC→IfcOpenShell IfcConvert→GLB re-import over the existing remote-lane companion rpc, host-local, never a new transport), FieldCodecPolicy, FieldCodec (FieldSpace→Zarr/VTK chunked layout, error-bounded lossy/exact lossless, zero-copy solver↔store↔viz), GeometryDeltaKind (SmartEnum<string>), GeometryDelta, DeltaCodec (FastCDC-chunked structural diff over mesh/B-rep/point-cloud/NURBS, quantization-aware bounded-lossy, columnar, progressive), InterchangeIdentity (XxHash128 content-key folding bytes + deflection + tolerance, lands on ArtifactIndexRow.Admit, mirrors ModelIdentity.Snapshot) — spotlight: the interchange format/codec axis with capability/companion/frame columns, the managed import/export fold, the IFC graph projection, the field and geometry-delta codecs, the two-hop bridge, the content-addressed identity; host-local, no TS_PROJECTION.
- CP-12 DISCRETIZATION_MESH|SOLVE_CONTRACT|OPTIMIZER_LANE|SWEEP_AND_BUDGET|CLASH_AND_TWIN (solver-and-optimization.md): SolverKeyPolicy, ElementClass (SmartEnum<string> element-topology rows), MeshAlgorithm (SmartEnum<string> generation-strategy rows), FieldStation (SmartEnum<string> nodal/integration-point/cell), MeshPolicy, MeshKernel (DiscreteMesh from boundary ImportedGeometry/MeshSpace with adaptive h/p refine), DiscreteMesh, FieldSpace (integration-point/nodal scalar/vector/tensor field over FieldStation, consumed by interchange FieldCodec + animation transient scrub), SolvePolicy, OptimizerPolicy, OptimizationResult, ClashPolicy, TwinVerdict, PhysicsKind (SmartEnum<string> FEA/CFD/thermal/daylight/energy/acoustic), BoundaryCondition [Union], SolveMethod (SmartEnum<string> linear/iterative), SolveProblem (uniform physics/BC/element/assembled-operator record), SolveLane (assemble over DiscreteMesh, dispatch to numeric-lane Factorization/SparseOps/iterative), SolveResult, OptimizerKind (SmartEnum<string> NSGA/Bayesian-GP/gradient-adjoint/topology/simulated-annealing), DesignVariable [Union], ObjectiveSense (SmartEnum<string>), DesignProblem, Optimizer (search fold by OptimizerKind), Surrogate (reduced-order/learned duality column), ParetoFront (queryable non-dominated artifact), SweepGrid, SweepAxis [Union], FrameBudget (early-stop governor over the iterative per-iteration residual receipt), SensitivityTornado, SweepLane (fan-out onto WorkLane/LaneRuntime, reduce to ParetoFront + tornado), AccelerationStructure [Union] (spatial-index cases over federated geometry), ClashScale (collision-compute fold over the persistent index), ClashPair, TwinSignal, DigitalTwin (ROM-eval loop scoring TwinSignal against a Surrogate baseline, anomaly verdict + control suggestion) — spotlight: the uniform physics×BC×element solve axis over a volumetric mesher, the numeric-lane-backed assemble-solve fold, the design-space optimizer with surrogate duality and Pareto front, the DOE sweep with frame-budgeted early-stop, the acceleration-structure clash and digital-twin loop; host-local behind the remote-lane Solve rpc, no TS_PROJECTION.

- AU-20 TRACK_MODEL|TIMELINE|SCRUB|WALKTHROUGH (animation-timeline.md): Keyframe<T>, Track [Union] (parameter/camera/visibility/transient-field), Easing (motion-token interpolation projection), Playhead (deterministic playback clock), Timeline (track composition), TimelineSample, ScrubState, Scrub (kinematic + transient-field scrub over the Compute SimField frame index), WalkthroughSpec, Walkthrough (offline frame-sequence render through the visuals encode rail) — spotlight: the closed keyframe-track union with motion-token easing, the deterministic-playhead sampler, the kinematic/transient scrub, the offline walkthrough export; substrate is the motion-token easing, the Viewpoint camera, the SimField frame index, the visuals encode rail, the AppHost clock.
- AU-21 SHEET_SET|PROJECTION|DIMENSIONING|DRAFT_EMIT (drafting-sheets.md): SheetSize (SmartEnum<string> ISO/ANSI/JIS catalog), TitleBlock (locale-aware), Sheet, SheetSet, ProjectionBasis, Viewport2D, HiddenLine (visible-edge fold over the Compute geometry payload), Dimension [Union], Tolerance, Annotation [Union] (GD&T + text), GdtFrame (feature-control frame), DraftFormat (SmartEnum<string> DWG/DXF/PDF/SVG), DraftFault [Union], DraftEmit (multi-format dispatch over SKDocument; PDF/SVG ship on the 2D-Skia substrate, DWG/DXF fence-complete SPIKE-gated on the entity-writer surface) — spotlight: the locale-aware sheet-set and title-block axis, the hidden-line viewport projection kernel, the dimension and GD&T families, the multi-format emit dispatch over the offscreen document rail; substrate is SkiaSharp 2D behind DrawSource.Owned + SKDocument.
- AU-22 CELL_MODEL|DEPENDENCY_GRAPH|CRDT_COEDIT|REPLAY_BUNDLE (notebook-document.md): CapabilityPin (pinned-capability fingerprint over the Compute capability registry + receipt determinism), NotebookCell [Union] (code/markdown/chart/render/viewpoint/parameter), CellOutput [Union], Notebook, DependencyGraph (cell DAG), RecomputePlan (affected-closure), NotebookOp [Union Insert/Delete/Edit] (notebook cell-edit delta projecting onto Persistence/version-control#CRDT_ALGEBRA CrdtOp + OpLogEntry at the op-log seam), NotebookCrdt (conflict-free replicated document over the Persistence op-log), ReplayManifest, ReplayBundle, NotebookReplay (notebook bit-identity check riding AppHost/determinism-and-replay#REPLAY_VERIFY proof law) — spotlight: the closed cell-kind union with per-cell pinned-capability fingerprint, the dirty-recompute DAG fold over the affected closure, the op-log CRDT co-edit merge, the bit-identical replay bundle; substrate is the Compute capability registry, AvaloniaEdit, the chart/render owners, the Persistence op-log, the AppHost clock/HLC.
- AU-23 RENDER_GRAPH|GEOMETRY_VIRTUAL|RESIDENCY_BUDGET|PATH_TRACE|SIM_VISUAL|VIEWPOINT_CODEC|TS_PROJECTION (viewport-pipeline.md): RenderPass [Union], RenderGraph (pass-DAG executor over the host-shared GRContext leased via the embed capsule), RenderTarget, FrameReceipt, ViewportFault [Union], Meshlet, MeshletCluster, ClusterCull (GPU-culling fold), BindlessTable, ResidencyTile, ResidencyBudget (VRAM-budget residency), Prefetch, InstanceBuffer, Bvh, Reservoir (ReSTIR), PathTracePass (progressive accumulation), Denoiser (edge-aware), SimField (Compute field-receipt projection, consumed by animation transient scrub), SimVisual [Union] (isosurface/volume/streamline/glyph/deformation), TransferFunction, Viewpoint (portable BCF-compatible view-state receipt, consumed by animation camera tracks + drafting projection basis, mirrors Persistence BcfViewpoint), SectionBox, VisibilityOverride, ViewpointCodec (BCF-compatible serializer), ViewpointWire, ViewCameraWire, SectionBoxWire, VisibilityOverrideWire, FrameReceiptWire — spotlight: the render-graph pass algebra over a shared-GRContext lease with frame-budget invariant and 2D-Skia fallback, geometry virtualization and out-of-core residency, the path-trace and simulation render passes, the BCF-compatible viewpoint codec; substrate is SkiaSharp 3 GPU backends (GRContext/GRMtlBackendContext/GRVkBackendContext/SKRuntimeEffect) via ISkiaSharpPlatformGraphicsApiLease; GPU passes SPIKE-gated on the live host-shared context.

- PS-19 ANCHOR_ALGEBRA|BCF_PROTOCOL|CDE_SYNC|TS_PROJECTION (annotation.md): Anchor [Union] (node-id/sub-entity/parameter-path/world-point-plus-view), Thread (threaded comments/mentions/status/assignment over the op-log), Mention, AnnotationStatus (SmartEnum<string>), Anchors (anchor projection, re-anchor against the structural diff, thread fold), BcfTopic, BcfComment, BcfViewpoint (portable viewpoint receipt, mirrors AppUi Viewpoint), BcfVersion (SmartEnum<string> 2.1/3.0), Bcf (archive read/write + topic/comment/viewpoint lifecycle), BcfApiEndpoint, CdeSession (OAuth2-backed over the AppHost outbound hop), CdeSync (bidirectional sync, conflict-aware merge), AnchorWire, ThreadWire, CommentWire, BcfTopicWire, BcfViewpointWire — spotlight: the durable annotation backbone over one anchor algebra with re-anchoring, the BCF 2.1/3.0 archive and BCF-API REST + OAuth2 CDE sync; consumes the op-log/content-address/federation entity keys/structural-diff node identity/ClockPolicy/ReceiptSinkPort as settled.
- PS-20 CLASSIFICATION_CATALOG|COST_ROLLUP (catalog-cost.md): ClassificationStandard (SmartEnum<string> Uniclass/OmniClass/MasterFormat/IfcClassification), ClassificationCode (hierarchical code carrying the ltree path), ClassificationMap (federated-entity-to-codes), Catalog (catalog load, hierarchical lookup, cross-standard mapping, entity-classification fold), CostCode (classification-to-cost-code mapping with rate), CostLineItem (formula-evaluated takeoff line), CostRollup (formula eval, quantity-takeoff projection, hierarchical DuckDB rollup over data-lanes#ANALYTICAL_LANE) — spotlight: the multi-standard classification and cost catalogs powering quantity takeoff and 5D cost; consumes federation ENTITY_GRAPH/ELEMENT_SET_ALGEBRA, the DuckDB analytical lane, the ltree hierarchy as settled.
- PS-21 ENTITY_GRAPH|ELEMENT_SET_ALGEBRA|CROSS_DOC_LINKS|RULE_PLAN|FUSION_RANK|FEDERATED_PLAN (federation.md): FederatedEntity (source-agnostic element keyed by stable composite identity), EntityIdentity (five-axis identity value), SourceRef, EntityGraph (identity derivation, upsert, spatial-containment fold, source-agnostic merge), ElementSet (polymorphic composable selection with stable content-addressed set receipt, consumed by catalog-cost QTO + schedule-interchange 4D link), SetExpr [Union], ElementSetAlgebra (literal/boolean/spatial/property/classification combinators, stable-receipt fold), CrossDocLink (typed inter-doc reference, pin-versus-float), LinkKind (SmartEnum<string>), ImpactNode, LinkStore (link upsert, pin/float resolution, transitive forward/backward impact, change-propagation fold), RuleAst [Union], RuleResult [Union pass/fail/element-set/viewpoint], RulePlan (lower rule AST into a predicate/query plan over the graph + element-set algebra), FusionCandidate, FusionWeights, FusionRank (fuse pgvector HNSW + PostGIS GiST + pg_search/FTS into one scored result with per-hit lineage), PlanNode [Union], EngineCost, FederatedPlan (predicate pushdown across stores, cost-based engine selection, partial-pushdown, cross-store join) — spotlight: the source-agnostic federated entity graph and the universal BIM query currency riding it; rides the PostGIS GiST + jsonb + ltree substrate on the PostgresServer profile, content-address identity, op-log changefeed, structural-diff node identity as settled.
- PS-22 CAUSAL_DAG|ATTESTED_LEDGER|LINEAGE_CDC|TS_PROJECTION (provenance.md): ProvEdge [Union] (W3C-PROV causal relation family), ProvNode (agent/activity/entity vertex), LineageSlice, Provenance (edge emission over the op-log, backward/forward slicing, blame, impact, deterministic replay), AttestedEntry (hash-chained ledger row + pgaudit category), LedgerHead (chain-head proof), AttestedLedger (chain-append fold, head-digest, verification, redaction-preserving projection), CdcScope, CdcEnvelope (redaction-aware change record), LineageCdc (lineage-scoped CDC filter, redaction-preserving payload, per-consumer feed fold), ProvEdgeWire, LineageSliceWire, AttestedEntryWire, LedgerHeadWire, CdcEnvelopeWire — spotlight: lineage as a first-class join dimension over a W3C-PROV DAG, the tamper-evident attested ledger with head proof, the redaction-preserving lineage-scoped CDC feed; consumes the op-log/ContentKey/ExportProof/RedactorKind/AuditBinding/DataClassification as settled.
- PS-23 SCHEDULE_STORE|TASK_LINK_4D|TS_PROJECTION (schedule-interchange.md): ScheduleFormat (SmartEnum<string> P6 XER, MS-Project XML), ScheduleTask (typed activity), TaskRelation (predecessor/successor dependency), ScheduleImport (XER tab-delimited + MS-Project XML read over data-lanes#ANALYTICAL_LANE Sep reader, activity-network projection, WBS fold), TaskElementLink (schedule-activity-to-ElementSet binding over federation#CROSS_DOC_LINKS), FourDStatus (SmartEnum<string> per-element construction-state), FourDState (task-element link, as-of 4D state fold over version-control#TIME_TRAVEL, planned-vs-actual variance), ScheduleTaskWire, TaskRelationWire, TaskElementLinkWire, FourDStateWire — spotlight: the construction-schedule store and its 4D coupling, the P6/MS-Project activity-network reader and the as-of 4D construction-state fold; the Compute P6/XER parse companion produces the canonical activity bytes this store ingests.
- PS-24 COMMIT_DAG|CRDT_ALGEBRA|TIME_TRAVEL|STRUCTURAL_DIFF|TS_PROJECTION (version-control.md): CommitNode (content-addressed commit), BranchRef (named-branch pointer with per-branch ACL), VersionVector (per-origin sequence map), MerkleRange (reconciliation node), CommitGraph (hash, parent-link, merge-base, vector-compare, Merkle range-fold), CrdtField [Union RgaSequence/OrSet/LwwRegister] (convergent op-based/delta-state field family, strict superset of sync-collaboration#MERGE_LAW Adjudicate which survives only as the LwwRegister arm), Crdt (Merge join-semilattice LUB commutative/associative/idempotent over the op multiset, HLC-only ordering; CrdtOp delta carried by OpLogEntry.Payload for column-family=crdt rows — a breaking CROSS_PACKAGE_LAW wire amendment recorded as a seam-split), AsOfQuery, RangeDiff, BlameRow, ScrubFrame, TimeTravel (AS-OF materialization, range diff, blame fold, scrub iteration, branch-from-past over the HLC op-log + content-addressed snapshots), GraphNode, EditOp [Union] (tree-edit-distance operation family), MergeConflict [Union] (typed conflict-class family), StructuralMerge (node-identity match, cheapest-edit-script tree diff, three-way merge, topological brep/mesh delta classification), CommitNodeWire, BranchRefWire, VersionVectorWire, CrdtOpWire, MergeConflictWire, BlameRowWire, RangeDiffWire — spotlight: Git-grade version control over the durable object graph — the content-addressed commit-DAG, the RGA/OR-set/LWW-register convergent CRDT superseding the LWW Adjudicate scalar, the AS-OF time-travel engine, the geometry-aware structural diff/merge; consumes OpLogEntry/HLC stamp/Closure manifest/Snapshots.ContentAddress/XxHash128/ConflictReceipt/ClockPolicy/ReceiptSinkPort/TenantContext as settled.

- AH-01b POWER_AND_FIDELITY (host-profiles.md, deepening cluster on the AH-01 closure): PowerState (SmartEnum<string> battery/charging/AC), ThermalPressure (SmartEnum<string> nominal/fair/serious/critical), FidelityScale, PowerCell (IOPowerSources/thermal observable boundary capsule), PowerProbe (sample fold to FidelityScale) — spotlight: power/thermal-derived render+compute fidelity scaling on the host-variance axis; residual [POWER_SOURCE_READ] live-host probe.
- AH-10b DELIVERY_FANOUT (outbound-resilience.md, deepening cluster on the AH-10 Outbound.cs closure): DeliveryTarget [Union], DeliveryChannel (SmartEnum<string> email/webhook/slack/sms), DeliveryMessage, DeliveryRuntime, DeliveryFanout (multi-channel fanout over the outbound hop) — spotlight: alert/notification multi-channel delivery riding the one outbound hop, AlertReceipt-routed, never a parallel HTTP client.
- AH-08b ALERT_ENGINE (health-and-degradation.md, deepening cluster on the AH-08 closure): AlertSeverity (SmartEnum<int> info/warning/error/critical), AlertCondition [Union threshold/anomaly-band/forecast-band], AlertRule (versioned, hysteresis+debounce), AlertState (readonly record struct firing cell), AlertReceipt (readonly record struct; fired/recovered/escalated transition, routed to DeliveryFanout), AlertEngine (Observe/Evaluate/Backtest fold over the health snapshot stream) — spotlight: declarative alerting over the continuous health stream with back-test against history; one value-fold, two front doors.
- CP-06b JOB_GRAPH (scheduling-and-lanes.md, deepening cluster on the CP-06 Lanes/LaneRuntime closure): JobState (SmartEnum<string> 8-row lifecycle), JobCheckpoint, JobNode (dependency node + scheduling columns), JobGraph (topological DAG scheduler over LaneRuntime; speculative/preemptible/fair-share/accelerator-affinity/spill-to-store + MarkDirty incremental dirty-subgraph re-solve) — spotlight: the dependency-graph scheduler layering distributed solve orchestration + real-time parametric re-solve over the bounded lanes, never a FarmRouter/JobScheduler/DagRunner sibling; reuses Substrate warm-affinity + AppHost PeerRoster the selection fold consumes.
- CP-11b EXPORT_RAIL|GEOMETRY_DELTA (interchange.md, deepening on the CP-11 closure): InterchangePolicy (deflection/tolerance/compression/quantization, Canonical/Web rows), TileNode, TileSet (3D-Tiles octree partition), DeltaPolicy (FastCDC chunk + quantization/tolerance/progressive) — spotlight: the glTF/3D-Tiles export policy + octree tileset partition and the FastCDC geometry-delta policy; grounds at the SharpGLTF toolkit extension surface.
- PS-08b PRESENCE_AND_BLOB (sync-collaboration.md, deepening on PS-08): AwarenessKind (SmartEnum<string>), Awareness (ephemeral awareness-beat fold), CheckoutDimension (SmartEnum<string>), ReplicationQuery, WorkingSet, Replication (partial-replication working-set fold over the op-log) — spotlight: ephemeral cursor/selection/viewport awareness beats and partial-replication working sets over the settled op-log, no second sync engine.
- PS-02b GEO_LANES (data-lanes.md, deepening on PS-02): CrsSource, MapConversion, CrsTransform, CrsReconcile (CRS reprojection fold over NTS/PROJ); RelationSource<TData,TProjection> (corrects the prior bare RelationSource arity) — spotlight: coordinate-reference reconciliation on the geo lane.
- PS-03b IDENTITY_POLICY (schema-rail.md, deepening on PS-03): AclGrant (enum), AclScope (SmartEnum<string>), ObjectAcl, SignedAuthorship, Authz (object-ACL + signed-authorship fold) — spotlight: per-object access control and signed authorship folded onto the identity policy.
- PS-06b SCHEMA_EVOLUTION (snapshot-codecs.md, deepening on PS-06): WireFormat (SmartEnum<string> codec wire-format version), SchemaEvolution (forward/backward codec-lineage migration fold) — spotlight: codec schema-version evolution beside the fuzz harness.
- PS-09b RETENTION_SWEEPS (redaction-retention.md, deepening on PS-09): ClosureGc (Closure-membership unreferenced-blob GC sweep reading the artifact-blob index) — spotlight: object-store GC as a retention sweep, never a parallel reachability tracker.
- PS-04b STANDING_QUERY|ARROW_PLANE (query-rail.md, deepening on PS-04): WindowKind (SmartEnum<string> tumbling/sliding/session), StandingQuery<TRow>, StandingQueries (incremental-view-maintenance fold collapsing DynamicData + Timescale + DuckDB windowing), ArrowCarrier, ArrowPlane (Arrow IPC zero-copy egress) — spotlight: the unified standing-query/continuous-query temporal primitive and the Arrow zero-copy columnar plane.
- PS-KP KEY_POLICY (per-package comparer accessors, one per axis owner, package-local, never cross-package reuse): VersionKeyPolicy (version-control#COMMIT_DAG), FederationKeyPolicy (federation#ENTITY_GRAPH), ProvenanceKeyPolicy (provenance#CAUSAL_DAG), AnnotationKeyPolicy (annotation#ANCHOR_ALGEBRA), CatalogKeyPolicy (catalog-cost#CLASSIFICATION_CATALOG), ScheduleKeyPolicy (schedule-interchange#SCHEDULE_STORE) — spotlight: the six deepening-page key-policy accessors the uniqueness/no-cross-package-reuse review consumes.
- AU-08b DASHBOARD_TILES (charts-dashboards.md, deepening on AU-08): DimensionIndex<TRow,TKey> (word-aligned bitset categorical cross-filter index, O(changed-words) maintenance off the live IChangeSet), PolygonBrush (even-odd ray-cast point-in-polygon spatial brush, the one point-in-polygon law) — spotlight: the categorical bitmap-index and spatial-ring brush absorbing cross-filtering, no per-tile GroupBy re-scan and no second geometry predicate.
- AU-09b DOCUMENT_EXPORT (visuals-offscreen.md, deepening on AU-09): OfficeFormat (SmartEnum<string> xlsx/docx/pptx), OfficeSpec, OfficeSheet [Union], OfficeExport (open-xml emit), BreakRule (SmartEnum<string> paginator page-break policy) — spotlight: office-document export beside the SKDocument flow paginator.
- AU-10b TOKEN_CATALOG (theme-tokens.md, deepening on AU-10): Colormap (SmartEnum<string> perceptual colormap ramp, consumed by viewport TransferFunction) — spotlight: the perceptual colormap vocabulary on the token catalog.
- AU-07b CONFLICT_RESOLUTION (inspector-editing.md, deepening on AU-07): ConflictSide (SmartEnum<string> base/local/remote), ThreeWay (three-way merge fold over the conflict pane) — spotlight: three-way conflict resolution beside the conflict pane.
- AU-16b AUTOMATION_PEERS (accessibility.md, deepening on AU-16): SceneAccessNode (3D-scene accessibility-tree node with spatial cue) — spotlight: the 3D-scene accessibility tree on the automation-peer fold.
- AU-18b DEV_LOOP (diagnostics-evidence.md, deepening on AU-18): ReloadIntent [Union], FlameNode (flamegraph node), SolveFrame, SolveScrub (solve time-travel scrub), ReplResult [Union], Repl (live REPL over the command deck) — spotlight: the DevEx observatory dev-loop — perf HUD/flamegraph, solve time-travel debugger, live REPL — over the headless proof + command-deck substrate.
- AU-23b RENDER_GRAPH|GEOMETRY_VIRTUAL|RESIDENCY_BUDGET|PATH_TRACE (viewport-pipeline.md, deepening on AU-23): GpuBackend (SmartEnum<string> Metal/Vulkan/GL), ViewportClock, FrameBudget (per-frame triangle/VRAM/duration budget; bounded-context homonym with Compute FrameBudget), LodPolicy, MeshSource, ResidencyPlan, SamplePolicy (SmartEnum<string> ReSTIR sampling policy) — spotlight: the GPU-backend axis, frame-budget invariant, cluster-LOD policy, residency plan, and ReSTIR sampling policy on the render-graph pipeline; GPU passes SPIKE-gated on the live shared context.
