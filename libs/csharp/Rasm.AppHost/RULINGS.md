# [RASM_APPHOST_RULINGS]

`Rasm.AppHost` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- (none)

## [02]-[SHAPE]

- Energy pressure grades on the existing `Pressure` contributor row — `PressurePolicy` carries thermal ceilings beside its CPU and memory ceilings and `Gauge` folds the live `PowerCell` rank as a third input, so a thermally-throttled host escalates the degradation rail it already runs under; a second thermal probe row, a thermal-only `DegradationLevel`, and a power alarm beside the rail each fork the pressure axis into two graders that disagree under load. Reopens only on a host exposing thermal state as a published meter the `UtilizationCell` listener can record directly.
- Process utilization homes at `PressureSource`, not `Microsoft.Extensions.Diagnostics.ResourceMonitoring` — discriminant: the package adds its metrics services and RETURNS on every host that is neither Windows nor Linux, so a design reading its meter as the universal utilization owner grades a permanently-zero ratio on the darwin hosts the Rhino and Grasshopper capsules run in, and it resolves `ResourceQuotaProvider` on every Linux host but only inside a Windows job object; the BCL counter arm is the floor every host serves and the two meter arms are the sharper limit-relative and process-relative reads where the package does register. Reopens only on the package registering a darwin snapshot source.
- `PowerAuthority` carries one row per platform, and a row whose read has not landed REFUSES rather than synthesizing a reading — the `PowerCell` holds against that refusal, so an unread cell grades `Balanced` fidelity and `Nominal` thermals; discriminant: a synthesized plugged-at-nominal-at-full-charge triple is indistinguishable from a measured one at `FidelityScale.Grade` and at the `Pressure` health row, so a laptop on battery bursts its parallelism and a throttled host grades healthy, and no consumer can tell which. Reopens only on a platform landing its authority as one `PowerProbe` row.
- `HostInstruments` declares the AppHost `BoardPack`, not `Observability/health` — discriminant: `PanelSpec.Admit` and `Slo.Admit` both resolve their series against the mounted `InstrumentSet` while an `AlertRule` resolves a `HealthSignal` against the degradation reading, so a pack seated beside the alert engine resolves instrument names across a page boundary and re-opens the metric-versus-reading split that boundary closes.
- Durable OTLP egress owns the TRANSPORT, never the exporter's own persistence — the branch-typed `OtlpOfflineQueue` installs through `OtlpExporterOptions.HttpClientFactory` and the exporter's `OTEL_DOTNET_EXPERIMENTAL_OTLP_RETRY=disk` pair stays unset wherever it is selected; discriminant: the exporter's persistence handler is `internal`, constructs its own `FileBlobProvider` at a hardcoded cadence, and accepts no provider, so arming both gives one batch two independent owners writing two directories under no shared capacity accounting, and neither can project a receipt. Stored blobs carry the request BODY alone and each replay copies headers off the live request that just succeeded, so credential material never reaches disk, a rotated token applies to the whole tail, and no stored endpoint replays a coordinate the deploy plane has since moved. Plugin ALC capsules open no queue, since a blob outliving its load context replays through a disposed provider.
- Durable transport carries BOTH message-handler legs and the composition owns the queue set's lifetime — discriminant: the http+protobuf export client calls `HttpClient.Send`, which dispatches to `HttpMessageHandler.Send` and never to `SendAsync`, so a `DelegatingHandler` overriding the async leg alone drops out on every host admitting a synchronous send, and the durable charter buys nothing while reading as landed; that same client is never disposed by its exporter, so the handler chain releases no provider and the set opens at composition and closes on the telemetry drain band. One queue minted inside an exporter options delegate stays unreachable and unreleasable by construction, since the SDK invokes that delegate from its processor factory past a sealed service collection. Reopens only on the exporter publishing a disposal seam for its export client.
- `RasmPackage` mirrors the kernel `TelemetrySource` key roster one-to-one at the TS decode seam — the envelope's `package` slot is that roster's projection, so a package minted at the capsule reaches the dashboard with no second admission and a hand-narrowed wire roster silently refuses envelopes the sink port legitimately stamps; a wire type listing a subset of the minted rows is the drift this pins.
- Durable OTLP queue residence reads the deploy-declared volume ahead of local disk and never the `LocalStore` column — discriminant: `LocalStore` answers where a document store lives, and the topologies that always export (`service`, `edge`) carry it false while resolving a per-user base into a container layer a reschedule erases, so a queue armed on that column disarms buffering exactly where the wire is least reliable and arms three idle directories and their maintenance timers on every desktop launch. Arming therefore takes durable disk BESIDE a bound OTLP provider, and every queue path scopes by host key so co-resident processes under one mount never lease each other's batches. Reopens only on an in-process probe distinguishing a mounted volume from an image layer.
- Ingress tenancy is ADMITTED per carrier through `TenantAdoption`, never inherited from the extracted baggage — discriminant: the promotion processor tags spans off the OTel baggage store while receipts, RLS predicates, and the metric fold all read the kernel `AsyncLocal` slot, so an entry seated by propagation alone reports one tenant on spans and root everywhere else, and an entry trusted from a foreign edge escalates privilege on a store the propagation seam cannot authorize. Trust is a carrier property only a transport owner knows, so the row carries no default, an adopting leg stamps the kernel slot, and a refusing leg clears the wire entry so both planes answer one tenant. Reopens only on an authorization owner publishing an admitted-tenant roster the seam can resolve against.
- Latency names fold into ONE composition-wide registration across all three axes and the lookup is boot-strict — `LatencySpine.Register` folds this root's `LatencyCheckpoint` roster with every contributed `LatencyRoster` and arms `LatencyContextOptions.ThrowOnUnregisteredNames`; discriminant: `ILatencyContextTokenIssuer` resolves an unregistered name to a POSITIONLESS token whose writes drop with nothing raised, so a package recording phases under a roster the registration never folded — and a checkpoint-only registration leaves every measure and tag name a contributor keys on in exactly that state — is instrumented in prose and silent on the wire, indistinguishable at the exporter from a phase that never ran. A contributor reaching its own `RegisterCheckpointNames` call splits the table so a name resolves in one registration and not the other. The roster rides `TelemetryComposition` as three name sequences rather than the port column, because a name is plain text this platform resolves while a `Planes` roster carries types it cannot. Reopens only on the issuer publishing a registered-name probe a composition can assert against.
- `GovernanceRuntime` carries `IServiceProvider`, never `IServiceCollection` — the record is request-time capability the woven decorators read, and DI registration is composition-root surface: the root reaches the governed pipelines through the `AddChatClient`/`AddEmbeddingGenerator` `Func<IServiceProvider, …>` factory overloads, whose provider is the one `ChatClientBuilder.Build` runs under and the one `GovernanceRuntime.Services` composes from, so `ModelGovernance.Compose` stays a pure pipeline fold; a self-registering pipeline folds build-time registration into the runtime record and forks the composition-freeze law.
- A capability descriptor's progress admission is one `Option<SubscriptionPolicy>` column the owning surface answers at projection, and `CommandAlgebra` seats it verbatim on the spine-declared `Spec`, whose Compute-owned vocabularies cross as smart-enum KEYS (`Allocation`, `Cache`, `Forced`) that Compute admission resolves through its generated `Validate`/`TryGet` onto the typed rail — discriminant: Compute's `ProgressCell.Mint` already gates the leaf cell on that column, so a host-side fraction channel, an `IProgress<T>` thread through `CommandAlgebra.Run`, and a boolean the dispatch re-derives a cadence from each manufacture a policy the owning surface withheld while the one real refusal sits unread. The column carries no default because a projection that drops it declares a reporting posture nothing chose. Reopens only on a producer publishing interior fractions the host must interpolate, which the `Agent/mcp` frame-ownership split already forecloses.
- Classification federation rides the kernel `TelemetryContributorPort.Classifications` column and nothing beside it — a branch publishes its `(taxonomy, value)` pairs as `ClassifiedValue` rows on its own port, the composition PROJECTS its `ClassificationRoster` set from the contributed ports with each port's `Scope` as the provenance a refusal names, and `RedactionRegistration.Federated` proves every pair against the owner roster at boot; discriminant: a classifications argument supplied beside the contributed ports is a twin carriage that lets the proof read a roster no port declared while the port column drifts unproven, and a branch-side type reference to the owner re-couples the federation the string-pair form exists to avoid. Reopens only on a classification fact plain text cannot carry.
- Lane registration is proven CLOSED at composition, boot-strict on the same discriminant class as the latency-name row — the proof folds `WorkLane.Items` against the built provider (`TryGetPipeline` per lane key), refuses on `Fault.InvalidValue` at the validation band, and both the proof and `Run` read ONE key projection so a key-format edit cannot desynchronize them; discriminant: an unproven lane reaches `GetPipeline` at first dispatch and throws in the request path, exactly the silent-until-load failure the boot-strict registration class exists to foreclose. Reopens only on a lane roster that stops being enumerable at composition.
- The committed op-log IS the outbox — `Rasm.Persistence` `ONE_OUTBOX_EGRESS_SPINE` rules the separate envelope table deleted, so `Wire/outbox` demands no durable table of its own: the drain source is `ReplayWindow.DurableOps` past the per-sink `OutboxCursor`, the advance is the fenced `CoordinationOp.OutboxAdvance`, and `OutboxRow` is the relay's in-process carrier over a decoded `OpLogEntry`; discriminant: a second envelope table gives one domain commit two durable owners with no shared transaction, so a crash between them loses or duplicates exactly the event the outbox exists to guarantee. `DeadLetterRow` likewise declares once, at `Version/egress#EGRESS_PUMP`, and crosses as wire-stable primitives. Reopens only on a sink whose delivery obligation the op-log cannot express.
- Cache tiers are one decision read at two seats — an `InHost` capsule opens no keyed `HybridCache` and frames every entry through `CacheLane.Capsuled`, so no L2 row survives a collectible load context; discriminant: a keyed builder and per-entry flags let registration and framing disagree, and an L2 row written by a serializer the unload disposes is decoded by whichever context loads next. Reopens only on a capsule proving stable cross-load serializer identity.
- A dispatch seat names a `WorkLane` or a `DeadlineClass`, never both — `LaneClass` binds the class to the lane's own `Rank` (`LaneAttempt` at or below the latency rank, `LaneFold` above), so `LanePolicy.Attempt` derives and `CommandAlgebra.Spec` picks a lane and takes the class that lane already fixes. Discriminant: the law was stated at `Runtime/laneguard` and `Runtime/time` with no member enforcing it, so a hand-supplied column contradicted its lane and the brokered command path hardcoded `HopTotal` + `Interactive` for every op — a whole-model fold on a sixteen-slot latency lane under a transport hop's budget. The lane itself derives from the descriptor's `Option<SubscriptionPolicy> Progress`, a declared reporting posture being the long-running witness. Reopens only on a lane whose rank does not predict its duration class.
- The MCP endpoint serves the stateless revision — `2026-07-28` on the transport's own `Stateless = true` default — as settled law with the cost named: every capability the down-level session rail keeps is `[Obsolete]` at the installed SDK (the whole SSE event-store family, the sampling/roots/client-logging families), so the `AsSamplingChatClient` bridge and `Last-Event-ID` frame replay are deleted whole — a federated tool draws no client model, every draw being the endpoint's own governed plane, and a dropped stream replays nothing, progress re-reading on the request-polling leg and the terminal answer recovering by client retry under the echoed `RequestState`, idempotent at the brokered dispatch; server-to-client round trips ride the input-required rail alone with `McpServer.IsMrtrSupported` the one guard, because `ElicitAsync` and handler-suspension bridging both refuse on a stateless transport by the SDK's own construction. Reopens only on the SDK un-obsoleting a session-rail family.
- An artifact-manifest content key mints over the FINAL written bytes — post-redaction, post-cap — and clears to absent on every arm that wrote nothing: a contributor fault, a cleanup refusal, and a budget drop alike. Discriminant: a key over the pre-redaction or pre-cap payload names bytes no reader can extract, so de-duplication matches archives that differ and a verify fails on an intact bundle; a key retained across a budget drop and a digest over the empty span are the same forged identity, both reading as a measurement to every consumer. Reopens only on an archive format carrying the uncapped payload beside the capped member.

## [03]-[COLLAPSE]

- Metric views collapse to ONE `AddView` predicate over the contributed roster — a named row beside a wildcard row is the deleted form, since every MATCHING view mints its own stream and the pair therefore exports one instrument twice, once tag-projected and once whole, so the projection guarantees nothing and the classification seam it carries is void. Each stream derives `TagKeys` from the `InstrumentSpec` that declared the instrument beside the one tenancy key under one series budget, foreign instrumentation falls through to the budget alone keeping its convention-owned dimensions, and a per-row cardinality literal is the second deleted form because tenant fan-out — not the declared dimension count — sizes every rostered stream.
- Log-record identity collapses onto the resource projection and `AddApplicationLogEnricher` does NOT re-enter — the shipped enricher writes an unqualified `service.name` and the deprecated `deployment.environment` spelling as package-owned tags, so seating it puts two values of one dimension on the log and resource planes and a query joining a record to a series matches neither; `RootEnricher` stamps `ProfileIdentity.ResourceAttributes` verbatim, `AddProcessLogEnricher` survives on `ThreadId` alone because the resource already carries `process.pid`, and the boot correlation reaches records through `CausalEnricher` rather than a third per-provider spelling of one process fact.
- `AlertEngine` and the kernel SLO algebra stay two evaluators over disjoint evidence — the engine folds the `DegradationReading` the health cell already publishes and never a metric series, while error-budget burn compiles from an `Objective` through `Slo.Specs` onto the deploy plane; a burn arm on `AlertCondition` mints the second metric source the engine's boundary forecloses, and a `HealthSnapshot`-only entry re-opens the stale-level read the coherent reading exists to foreclose. Severity is the kernel's two-row routing axis in both, so the incident ladder rides `Rank`/`Escalated` and no local severity vocabulary returns.
- `Idempotency` and `HopIdempotency` stay two typed owners, never one merged smart-enum — discriminant: the op row keys END-TO-END command repeat-safety while the hop row keys PER-HOP transport dedup, and the row sets do not interchange (`NonIdempotent` exists only op-side where no dedup key exists, `MethodDerived` only hop-side as HTTP-method-derived safety with no op-level meaning); a dedup sweep reading the capability page's one-repeat-safety-semantic clause as a merge license erases the two dedup scopes — that clause means the `Keyed` row carries one meaning on both layers, never one type serving both edges; reopens only on proof that a single dedup scope serves both the command edge and the transport hop.
- Where a policy cut decides a counted length, a keyed preimage, and a written payload, the cut is one named projection on the policy row and the three are reads of it — three `Min` spellings of one bound is how a manifest count, a content key, and an archived member drift into describing three slices of one payload while every site reads correct in isolation.

## [04]-[STRUCTURE]

- (none)

## [05]-[PROCESS]

- (none)
