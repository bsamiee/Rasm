# [PROJECTION_IDEAS]

The forward pool of higher-order concepts for the fold-algebra owner. Each open idea is a card — a bracketed slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on — grounded in the read-side domain and the branch's host-free differentiated value. An idea drives one or more `TASKLOG.md` tasks; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition.

## [1]-[OPEN]

[BLOCKED] [MERKLE_ANTI_ENTROPY]: a read-side Merkle-DAG range-reconciliation handshake over the version-vector frontier.
- The `causality-graph/version-vector` `dominates` algebra drives a `MerkleRangeWire` digest comparison so a reconnecting browser peer reconciles its retained prefix against a peer range by set-difference rather than a full op-log re-scan, the range digest naming the divergent slice.
- Unlocks bounded reconnect cost (a peer pulls only the slices whose digests differ), a deterministic anti-entropy convergence on top of the existing version-vector, and a `frontier-gc`-trimmed prefix that is the reconciliation unit.
- Draws on the C# `MerkleRangeWire` range-reconciliation wire shape and the timely-dataflow antichain lower-bound; the gap is that a reconnect today re-folds the whole changefeed through `stream-policy`, so a large-history peer pays full re-scan cost where a digest handshake would name the divergent range. Depends on the `causality-graph/version-vector` `CrdtOpWire`/`origin` wire transcription landing and the `CAUSAL_STABILITY_DELIVERY` stable prefix being the reconciliation unit.
- Close-condition: the forward `causality-graph/version-vector#MERKLE_RECONCILE` stub cluster is named, but the fence body is gated on (1) `MerkleRangeWire` arriving decode-admitted on the `libs/typescript/interchange` decode rail from `csharp:Rasm.Persistence/versioning/version-control#TS_PROJECTION` (confirmed absent today) and (2) the `causal-delivery/stability-frontier#STABILITY_FRONTIER` stable prefix landing as the reconciliation unit. Realizes when the C# wire decode lands.

## [2]-[CLOSED]

- [DATAFLOW_REFOUNDING] — realized as `standing-query/window-fold#DATAFLOW`: `windowGraph` re-founds `windowFold` on a typed `@electric-sql/d2ts` operator graph (`D2`/`newInput`/`groupBy`/`consolidate`/`output`, `groupBy` owning its own `{ bucket }` keying so no preceding `keyBy`) keeping `WindowKind`/`bucketSet` as the windowing reduce stage and the watermark as the `d2ts` frontier antichain, the runtime-plus-health roll-ups riding `groupBy` with the catalogued `sum`/`avg`/`min`/`max`/`median`/`mode` aggregates and the retained `Index#reconstructAt` trace feeding the as-of Version coordinate; precondition `standing-query-replay-confirmation` landed first as the `#REPLAY_LAW` cluster.
- [CAUSAL_STABILITY_DELIVERY] — realized as `causal-delivery/causal-buffer#CAUSAL_BUFFER` plus `causal-delivery/stability-frontier#STABILITY_FRONTIER`: `CausalBuffer` is the per-entity pending queue admitting an op only when `dominates(heldCursor, opContext)` holds modulo its own slot and releasing the dependency-satisfied suffix in causal order, `DeliveryVerdict` the Held/Released dispatch, and `stabilityFrontier` the `SortedSet`-of-cursors greatest-lower-bound meet naming the causally-settled horizon `frontier-gc` finalizes against — one discipline over the `version-vector` `dominates` algebra, never a second ordering surface.
- [ORDERED_INDEX_COLLAPSE] — realized as `evidence/evidence-correlation#EVIDENCE_CORRELATION` plus `causality-graph/version-vector#CRDT_SEMILATTICE`: the evidence timeline is a `RedBlackTree` keyed by the composed HLC `Order` (O(log n) `insert`, since-T window through `greaterThanEqual`) replacing the `[...rows, e].sort(byHlc)` whole-array re-sort, and the RGA sequence is a `SortedMap` keyed by the dense `(after, id)` order position (O(log n) `set`/tombstone, in-order `entries` replay) replacing the linear `cells` array; the `SortedSet` stability-meet arm landed in `causal-delivery/stability-frontier#STABILITY_FRONTIER`.
- [AS_OF_TEMPORAL_QUERY] — realized as `temporal-query/as-of-query#AS_OF_QUERY` plus `temporal-query/as-of-diff#AS_OF_DIFF`: `AsOf` is the closed three-case `Data.TaggedEnum` coordinate (`Version` against the `d2ts` antichain via `Index#reconstructAt`, `EventTime` against the watermark nanos, `Stable` against the causal-delivery stability frontier), `asOfQuery` the total `$match` materializing the keyed map as of the coordinate, and `asOfDiff` the two-coordinate snapshot diff the conflict inspector reads — one query surface, never a second store holding parallel history.
- [DEGRADATION_GATED_AVAILABILITY] — realized as `availability/availability-gate#GATE_POLICY`: `DegradationLevel` is the five-case ordered `Data.TaggedEnum` mirroring the C# health vocabulary with `DegradationOrder` over the rank, `gatePolicy`/`admits` the one frozen level-to-admitted-tier comparison, and the `availabilityStore` second arm joins the live `feed-stores/live-cells` degradation cell so `isEnabled` returns the conjunction of the wire `available` flag and the live degradation gate — a new health-gated surface is one floor on an availability row, never a second health model.
- [SUBSCRIBABLE_PROJECTION_FACE] — realized as `fold-core/projection#PROJECTION`: `Projection<A>` aliases the `effect` `Subscribable<A>` `{ get, changes }` contract, `projectStore` lifts any store's `SubscriptionRef` into the baseline `Projection` deduplicated through `Stream.changes`, and `derive`/`deriveEffect` map a store into a projected view (filtered map, sorted slice, scalar gate) as one `Subscribable` the `@effect-atom/atom` `Atom.subscribable` bridge consumes verbatim at the `ui` boundary, so a new derived read is one `derive` row at the `projection` altitude and `ui` carries no bind-site recombination — the `projection` half of the branch `ONE_FOLD_ONE_BINDING` concert.
- [REACTIVE_QUERY_SURFACE] — realized as `live-query/reactive-query`: the `LiveQuery`/`queryStore` combinator folds any `d2mini` `MultiSet` pipeline into one `SubscriptionRef` at the `projection` altitude, so a composite view is one declared derivation the `@effect-atom/atom` bridge consumes and `ui` carries no view-state library.
- [DIFFERENTIAL_DATAFLOW_BACKBONE] — split: the versionless non-windowed half landed as `live-query/reactive-query` over `d2mini`; the event-time windowed re-founding over `d2ts` carries forward as [DATAFLOW_REFOUNDING] above, disjoint by frontier requirement.
- [FRONTIER_GARBAGE_COLLECTION] — realized as `frontier-gc/frontier-gc`: the `Frontier` antichain plus `finalizeBelow` unifies the window-bucket, tombstone, and presence TTL under one closed late-arrival horizon, bounding a long-lived session and making replay the frontier-trimmed prefix; the `d2ts` `Antichain` representation swap rides [DATAFLOW_REFOUNDING] and the causal-stability horizon rides [CAUSAL_STABILITY_DELIVERY].
- [CONVERGENCE_PROOF_HARNESS] — realized as `convergence/convergence-law`: the `fast-check` permutation law folds the op-set in two divergent orders through `conflictStep` and asserts `Equal`-identical `ConflictPresenceState`, encoding commutativity, idempotence, and tombstone-monotonicity as the gate any future CRDT op kind passes; the harness reads the cross-libs `ONE_WIRE_FIXTURE_CORPUS` frozen corpus rather than folder-local arbitraries (carried as the queued corpus-binding task).
- [SKEW_AWARE_ORDERING] — realized as `causality-graph/version-vector` plus the `clock-uncertainty/skew-band` `bandsOverlap` ordering input: the version-vector reconstructs the `Concurrent` verdict the HLC total order erases and the skew band downgrades it to `concurrent-uncertain` when the intervals overlap, making `SkewBand` load-bearing in both render and ordering — the TS leg of the cross-libs `ONE_CAUSAL_TENANT_IDENTITY` seam.
