<Project Sdk="Microsoft.NET.Sdk">
    <ItemGroup>
        <ProjectReference Include="../../../libs/csharp/Rasm/Rasm.csproj" />
    </ItemGroup>
    <ItemGroup>
        <!-- The bridge cargo runner drives the Rasm.Bridge.Scenarios SDK internals (context
             construction, scope drain, capture hook); scenario authors see only the public SDK.
             The SDK law specs pin those same internal semantics (FactCount is the facts.empty
             trigger; Hook is the run-bracket capture seam). -->
        <InternalsVisibleTo Include="Rasm.Bridge.Cargo" />
        <InternalsVisibleTo Include="Rasm.Bridge.Scenarios.Tests" />
    </ItemGroup>
</Project>
