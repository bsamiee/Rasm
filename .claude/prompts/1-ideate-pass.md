# [IMPLEMENT_WORKFLOW]

[CRITICAL]: ENSURE AGENTS ARE AWARE OF sibling folders whose card pools already had a similar pass, so cross-cutting seams and expected functionality are known — avoid trampling, ideally leverage/integrate, and assume planned functionality will exist when adding ideas/tasks for `<TARGET>`.

Neither you nor sub-agents can claim a package is "deep" it's not about having a tidy or full signature, it's about being comprehensive in each dimension the folder should have, and including the existing ones. Simple question, could this folder be used to create a world-class, bleeding-edge, advanced, and powerful app for all things its folder domain name would entail? No? Then not well done. ANY NEW FILE OR FOLDER SUGGESTIONS SHOULD BE 1 SEMANTIC WORD, NOT x-and-y.md ONE word. IMPORTANT: All tasks/ideas we make, we need to understand the ripple they have across our planning folders outside of `<TARGET>`, all tasklog/ideas files have a `Ripple:` field that must be used for any cards we make outside of `<TARGETS>` to link back to what we make.

`<TARGET>` = `<target folder path(s)>`

NEVER ALLOW TASKS/IDEAS TO BE PLANNED/MADE FOR "TESTING" OR WORK TO DO IN TEST DIRECTORIES, OR META TASKS LIKE UNBLOCKING ANOTHER TASK, WE JUST DO THAT MANUALLY ONCE WE LEAVE PLAN MODE WITH THE INFORMATION/INVESTIGATION WE GAINED DURING PLANNING.

[NOTE]: Use 3-4 sonnet agents initially per `<TARGET>` to establish context/mapping/grounding, they only help understand existing scope truthfully, to ensure no fake "depth" is perceived, because the targets are not deep.
- Then, once all initial grounding/questions is done, have a workflow that uses 2-3 opus agents per `<TARGET>` each followed by a synthesize phase to harmonize findings, dedupe, throw out totally wrong tasks/ideas, and improve/refine good ones.
- Then, we can collect all the synthesized reports per `<TARGET>`, we will then run 2-3 critique agents on the final/collected "initial" plan/findings, they make their edits/suggestions. THESE ARE ADVERSARIAL CRITICS, they look at all quality considerations, scope, implications, lack of scope, or underutilized external libs, or adding new libs/etc as needed, and validating any new lib suggestions from the synthesized findings.
- Then, we use 2-3 redteam agents, these are ULTRA-HARSH, they attack the plan from every angle possible (each), multi-faceted, directional, they consider all implications, they consider the impact of ideas for example, to ensure it's not heading in the wrong direction, they do counterfactuals, to identify possibilities of alternatives, or adjusting to a different angle, or extending/expanding scope properly for deeper foresight, parameterization, long-term/long-tail considerations, quality of content, quality of output from the idea/task that is being suggested (the structure/instructions), etc...

[BRIEF] You must be an orchestrator, not a writer, unless the task is focused, and small, otherwise, delegate all tasks. We are in plan mode, and MUST maximize it fully, we cannot and will not leave plan mode until we have a plan that is extensively researched, investigated, and detailed; all the actions that will be done once we leave plan mode must be predetermined with great detail and we jump STRAIGHT to creating a workflow or surgical actions, the top section must demand action from you with clear instructions, and to trust the plan. You must use opus agents to help compile the plan throughout, reviewing it as you build it parallel to any other work we are doing, they will critique, refine, and improve it, ensuring the plan is signal, focused, and structured properly, no vagueness or ambiguity in it, providing enough detail to immediately jump into workflows once we leave plan mode. Prior to presenting the plan to user, review the entire "finalized" plan line by line, and identify the appropriate series of workflows that will be needed, plan them NOW to have agents also review/critique it compared to plan, to ensure we have well made phase structure, without any gaps, each phase is high impact/value, and is sequenced properly, and we leverage parallel work within a workflow, as well as parallel workflows where we can. Do not commit, focus on implementing all the work, user will commit themselves at the end.

[NEVER] burst sub-agents, use sonnet only for surgical, well scoped low-stakes tasks, exploration, navigation, establishing a baseline, but then Opus takes over. Further, keep total running agents between 9-12, that can be divided among as many workflows as needed, or within a workflow, but it needs to be phased logically, any combination of single agent, workflow, or workflows, so long as the number of active agents is within 9-12 (push to 12 mostly) we will be fine to not hit burst limit; ensure it's a proper roll-out, not a sudden burst, a few seconds, ramping up.

[APPROACH]: Fully understand topology of lib-wide, language-wide, and per-folder, and the necessity to have foresight on the impact of tasks/ideas waterfalling across so that each is implemented holistically, and fully, but done in a complementary manner, not as dual paradigms, and this may necessitate refinement/interpretation of the tasks/ideas. Even if scope is limited, to a folder, or folder(s), or just a language, regardless of scope, understanding the topology is important to understand ALL RIPPLES, so we can keep all seams and other alignment considerations updated, even for folders OUTSIDE of scope.

ASK THE USER AS MANY QUESTIONS AS POSSIBLE, EARLY DURING PLAN MODE, DURING PLAN MODE, MIDDLE OF PLAN MODE, AND WELL BEFORE FINAL PRESENTATION TO ENSURE PLAN IS DONE WITHOUT MISUNDERSTANDING. THE PLAN MUST BE EXTREMELY DETAILED, ALL WORKFLOWS MUST REFERENCE/READ IT AND OTHER STANDARDS NEEDED PRIOR TO PROCEEDING, YOU AND ALL WORKFLOW AGENTS MUST TREAT THE PLAN AS TRUTH, ADJUST ONLY BASED ON EARLIER SESSION WORK TO IMPLEMENT LATER WORK PROPERLY.

[READ]:
- Planning Projects: `libs/.planning/planning-targets.md`
- Mandated Approach: `libs/.planning/campaign-method.md`
- Planning Infrastructure: `libs/.planning/README.md`
- Planning Topology: `libs/.planning/architecture.md`
- MUST READ: `CLAUDE.md` + `AGENTS.md` + `README.md` + `tools/assay/README.md`

## [WORKFLOWS]
- PLAN: build the plan with read-only exploration agents per `<TARGET>` (decision-complete IDEAS/TASKS blueprint, never edits), one `<TARGET>` at a time; fold each blueprint into this plan and resolve its open questions with the user. The live workflow roster is `ls .claude/workflows/`; a plan shape with no fitting workflow gets a one-off workflow via `.claude/skills/workflow-creator`, deleted after landing.
- EXECUTE on plan-leave, one scope at a time (never one long run): `Workflow({ name: 'ideate', args: '<folder>' })` per `<TARGET>` folder to author the card pool, plus `Workflow({ name: 'rebuild', args: '<folder>' })` for any design page the plan rebuilds; then `Workflow({ name: 'align-cards', args: '<scope>' })` and `Workflow({ name: 'hygiene-sweep', args: '<scope>' })` to settle ripples, manifests, and ARCHITECTURE/README.

[CODE_DOCTRINE]: `docs/stacks/<lang>/` for the target's language is the governing code doctrine, read in full at every stage — all ROOT files, plus `docs/stacks/csharp/domain/` when a C# domain is in scope. Every branch meets the same density, complexity, and advancement bar; sibling stacks are the floor, never the ceiling, and flat code is unacceptable in any language.

[API_SOURCES]
- The branch and folder targets named by the session, each target's own `<pkg>/.api/` catalogues, and the admitted packages themselves.
- These MUST be utilized and understood whenever working on design docs, idea or task creation, we treat external libs/packages as FIRST CLASS and PREFERRED over stdlib.

[LIVE_PROOF]
- If a task or idea requires live probing, launch Rhino WIP (not rhino 8) locally on machine to run it, it is available, between assay, rhino-bridge, and our rhino-mcp skill there is nothing we can properly diagnose/extract/identify/understand regarding rhino behavior for any functionality need.
- We have: `tools/rhino-bridge/README.md` available as well, and pairs with `tools/assay`, and `tests/csharp/_testkit/Scenarios` for scenarios if needed.
- We have: `.claude/skills/rhino-mcp` as well if needed for direct app usage/manipulation, script injection, probing, etc...
- NEVER NEVER LAUNCH RHINO 8, ONLY LAUNCH `RHINO WIP`.

## [GOALS]

All tasks + Ideas we make must be heavily refined/improved and critiqued, a critique pass, then a red team pass, to replace ideas/tasks, improve them, extend their reach/consideration, or resolution/signal for clarity/guidance, or improving further in their direction, etc... critique should be extremely focused on the content, and ensuring that it's fully scoped, ripples identified, external libs aren't left out, etc, redteam MUST BE heavily geared towards fundamental direction/concept, identifying alternatives that are better, or counterfactuals, or gaps/mistakes/oversights, ensuring we never implement a larger idea with a naive/basic agent style approach, that we considered all available external libs to do it, or a fundamentally better way, for example, using models or persistent data over loose json, or durable code over "generating" things, using FULL IaC (infrastructure-as-code) over yml manifests, and the tooling/approach for that. Most critical, they also ensure that we aren't making ideas/code that leads to poor parameterization/pigeonholing, and that we always think in the most advanced/complex way for foresight, functionality is inherently designed to be complex/multi-capable/polymorphic from the start, a service for example wouldn't be for a "single" type of service, but the service code would be to make any type of service at a world class 11/10 way, to make dozens, hundreds with a fraction of the code through increased density/complexity/parameterization/polymorphism, programmatic driven logic, no hardcoding/fragile code, etc...

[GOALS]:
1. Identify new `IDEAS.md` and `TASKLOG.md` entries for the `<TARGET>`. Both are explained further below. We are focused ENTIRELY on creating tasks, and ideas, and ensuring they are ultra-advanced, well-considered, advanced isn't "complex" but detailed, high-impact, and leads to improved functionality, code quality, sophistication, features/capabilities, richness, etc... MANY of our `libs/.planning/planning-targets.md` are illusory in scope/quality, we are heavily under-utilizing/under-considering capabilities. 
2. Refine, refactor, rebuild, remove, or replace any pre-existing ideas/tasks, pay close attention to ideas that are more like "tasks", move it if it's a valid and quality item, but remove it from ideas to tasks.
3. Identify improvements in content to all docs within the `.planning/` folder of `<TARGET>`, focusing on functionality, capabilities, code quality, code density/complexity/richness, and collapsing flat/spam code, reducing any dual/multi-paradigms, ensuring unified rails, no sprawl is occurring. REMEMBER: our core purpose of building these doc files prior to the real code is to AGGRESSIVELY refactor, rebuild, and replace code, so that any functionality within it (existing), or added is done ROOT/GROUND-UP, as if it was there from the beginning, to optimize the code to the most minimal surface, unified shape/objects, parameterization, no fragility, coupling, unusual behavior, etc...
4. Identify all existing external packages the `<TARGET>` uses, by reading the `README.md` files, with the manifest, as well as listing ALL files in the `<TARGET>` `.api/` folder(s) to do the following: Identify underutilized packages, where we can add new functionality/features, ideally within existing code bodies, or refactor them, standalone is acceptable as well, as well as driving a new file creation or folder if justified. CRITICALLY analyze all package selection and dedicate a minimum of 8 sub-agents per `<TARGET>` for deeper external package considerations such as replacements (of existing selection if better exists), removing redundant ones, where a package we have is able to replace many, OR replacement of one or many with a new, better lib, as well as adding a new source just for increased value/quality, unlocking new functionality/capabilities, either currently existing (to replace handrolling), as well as creating/driving new capabilities, extending existing code, creating new code. External libs are a big request of ours, and we always want to MAXIMIZE the integration of them, not by quantity, but by quality, ensuring we understand what we have available to us to drive ideation as well as task creation.

READ: `libs/.planning/IDEAS.md` + `libs/.planning/TASKLOG.md` to see the marker/shape of ideas/tasks to also get a grounding of expectations; ensure agents that deploy are aware of it as well. Also, read/use `docs/standards/style-guide.md` to create precise, well formed prose for content, to reduce hedging, meta-commentary, and nonsense, ensuring all items (either type), are made with declarative instructions and clear, not vague.
- Example of the depth bar: a materials folder that stops at "steel" is weak — steel is a material type, not a component; beams, plates, connectors, washers, nuts, and bolts are all steel things a world-class materials folder owns, with a true profile/catalogue treatment rather than a flat label.
- Example of the shape bar: prefer a parametric classification/type solution over spamming hundreds of type definitions — appearance end, construction end, and meaningful properties all realized, not a smattering of "good" DIRECTIONS where no direction is fully pushed.
- JUDGE EVERY FOLDER THROUGH THIS VIEW: many plausible directions, none fully realized, is the default failure mode. Tasks improve folders fundamentally — architecture, professional/industry-standard sub-domain names and categories, and full mining of the admitted libs.

[CRITICAL]: NEVER make a task that is meta, such as unblocking another task, or idea, etc, just unblock it properly once we leave the planning mode, if we have the solution to make as a card, we just fix it ourself instead of making an unnecessary mid-step. NEVER NEVER NEVER MAKE META TASKS LIKE THIS, FIX IT PROPERLY/DIRECTLY OURSELF BASED ON THE INVESTIGATION/RESEARCH/LEARNING WE HAD IN PLAN MODE, IF SUCH A TASK EVEN COMES ACROSS.

[IDEAS]: These are meant to be larger and conceptual, not a prescribed task, though they must have enough guidance for implementation understanding. For example, improving all socket/port/boundary code between folders, and/or languages, leveraging gRPC, or a newer/better standard or other means to enable communication from app level code to a ts backend, or remote location, or driving a web view, etc, likewise, it can connect to pg, this would be a larger core lib "idea" in `libs/.planning/` the scope would be reduced as we go down the topology, to language specific, and folder specific. Folder specific example would be implementing higher order concept capabilities/functionalities to existing files in a folder, or creating a new file within a folder (when justified), or even a new sub-domain within the folder. For example, adding all the possible UI primitives from Skia and Avalonia, and whatever other UI libs we have in `Rasm.AppUi`, to allow our lib to generate any type of element (panel, dock, buttons, etc), or a new way to drive UI in some capacity.

[TASKS]: Tasks are more concrete/focused than "ideas", they can be "small" to "large" though always done without qualification; they are more detailed, and scoped than ideas, and they are focused on actionable, focused work; adding an external lib, then adding it to central package manager, csproj (if cs related), adding to `README.md` manifest, and then creating the `.api/` file using `tools/assay`, and ensuring it's formatted properly per `docs/standards/formatting.md`, `docs/standards/information-structure.md`, `docs/standards/style-guide.md`, for example. Or, a scoped/focused refactoring, identifying collapse targets, or poor code quality patterns, leveraging the owning `docs/stacks/<lang>/` standards (all content) to identify flat/tacked-on/spam code, or in general opportunities to push complexity/density/sophistication and reduce LOC, reducing object/shape/surface count, reducing type count, or identifying underutilized external libs, to replace handrolled code, or to improve/extend the capabilities of existing code, etc... Tasks are meant to be more plentiful, and high impact, but we want many to guide future implementation passes.

[METHODOLOGY]:
- Start from either lib-wide -> language-wide -> folder specific if within `<TARGET>` or ground-up, (opposite direction), the direction we take regardless of scope, when two or more `<TARGETS>` exist, even at a same language, folder spec, ripples/alignment considerations, etc... IDEAS within folders should be focused on THAT folder, though there may be relevant seams/boundaries/wires/ports/etc that will need outside consideration, but that is a given, the idea/task belongs to that folder. 
- NEVER NEVER NEVER make tasks/ideas for creating new files, or creating testing infrastructure code/implementations, we are not interested in that. 
- We want to make tasks first, in isolation across `<TARGETS>` based on deep research (of code, and external knowledge such as external libs, etc...), for code quality, features, functionality, etc and above. Then, we want to make IDEAS, it's important that once we get the finalized set of refined/improved ideas/tasks, we then REVISIT tasks, to create new ones, if needed based on the IDEA, because an IDEA is larger, it will often, but not always, have tasks associated/distillable from it, or require adjustments/refinement to existing tasks to ensure everything is aligned and complementary.
- We want to have a healthy amount of tasks, and a more focused set of ideas that are high value, understand, adding new features/capabilities/functionality to existing files/code either as new code bodies, or adding/integrating into existing is a TASK LEVEL SCOPE, regardless of complexity, it's not "large" enough to be an idea. An idea would be a new sub-domain, or a larger feature set, or higher-order abstraction within the existing files, etc..

[IMPORTANT]: During the planning phase we MUST identify ALL tasks + ideas + derived/distilled tasks if any (from the approved/refined ideas) afterwards. During implementation we MUST go to implementation. AFTERWARDS, we will do 3 cycles of ideation/task creation/refinement, where we use workflows for extensive review, investigation, and consideration, we are NOT interested in spamming ideas/tasks, but subsequent turns should be focused on higher value additions quality over quantity, AS WELL AS REFINING EXISTING TASKS/IDEAS, either due to the creation of new tasks/ideas, or just general refinement/improvement/corrections. All `<TARGETS>` are given equal focus, nothing is shallow level. 

[MOST_IMPORTANT]: During this entire plan mode + post-plan-mode, critically evaluate the architecture of `<TARGET>` to judge whether it is fundamentally going in the right direction, creating the tasks/ideas to re-adjust: split god files, grow one-file folders, deepen thin files, and attack any "surface"-level implementation — a folder of concepts or domains masquerading as files is the signature defect.
- For example, sub-domains like scheduling, cost, or profiles can each be heavily pushed in their own right and extensively expanded with real professional standards/sources, so no content is nonsense in its values or in the grouping of things into an object/shape. Push the foundational feature sets to 100% in a TRUE professional sense BEFORE adding shiny new concepts; a foundational surface that is poorly scoped for foresight is the defect to fix first.

All the code we make in libs must be world-class bar NONE, and NOT by spamming nonsense, but through dense optimization/complexity/richness/parameterization, more code doesn't mean better. However, we look at feature sets/capabilities, coverage of capabilities. Essentially: 
- FOR EVERY FOLDER, SUB-FOLDER, FEATURE SET, IMAGINE THE MOST ADVANCED PROFESSIONAL SOFTWARE WITH SIMILAR SUITES, WE TREAT THAT AS THE FLOOR, NOT THE CEILING. We should be able to make world-class 12/10, bleeding-edge, ultra-powerful apps with these libs, imagine a construction suite tool, or having all the materials/properties for parametric model creation in Rhino/gh2 with extreme precision/detail, injecting metadata, IFC, etc (these are cross-cutting/language, and would be wired at app, but needing the feature sets in the right locations and being aligned), material integration that is agnostic, but also aligned/complementary to then work in Rhino/gh2, where we have the profile/details/markers to have it automatically drive values in rhino.
- For example, there is infinite things to consider, and most are done in a complex way because it is NOT about making the hardcoded end state desired, but the lower-level lib code at a superb level, and aligning it without coupling to work in the end-state "app" imagination/desired state. Properties of materials for analysis of all kind, having complex layout engine/generation to create full assemblies of things, walls, columns, with all considerations of hollowness, or curved arches, complex arches, domes, etc these are all the things we are overlooking, even in materials. 
- For example, a layout that would take a beam, plates, with hole defined, then defined nuts/bolts with even the proper screw rotation/orientation/information, and washers, all defined properly with their properties, and then at layout/app end-state they all come together, leveraging our libs/csharp/Rasm.Rhino/ with blocks, or imagine a brick wall, a brick alone isn't enough, the complexity of spacing, the dozens of runs/bonds that exist, mixture of brick types, orientations, and the mortar/joints alone, spacing between them, anticipating parameterization for spacing, curvature on walls, domes, around openings that are simple, and complex (3 point arches, ogee, etc...). For persistence, having the ability to choose any store sourcing, from sqlite/pg/json/etc, having FAR FAR more complex/full feature sets, this is a huge part of any serious application, as is apphost and compute, and all of ours are elementary at best with scattered "directions" that are solid, but heavily undercooked; but again, that doesn't mean code spam, it means fundamentally improving the foundational approach, root/ground-up refactoring, deep consideration/foresight, more powerful external lib integration, additions, cross-lib alignment (without coupling) within csharp but also other languages for thinking about all features we could drive with them. (We would add tasks/ideas to folders/files/languages/etc that are OUT OF SCOPE.
- IF an Idea or task in our `<TARGET>` would require it to ensure the functionality is fully realized A-Z and alignment/boundary/seam considerations, which inherently drive functionality, not just contracts/protocols, but also adding the idea/tasks on the other end for it to be realized, if that makes sense)

## [PLAN_EXIT_PROTOCOL]

On leaving plan mode, before any execution turn, the orchestrating session runs one handshake: re-read the entire plan end to end; derive the complete, exhaustive task and phase list — identify all the tasks/ideas we will make; read the full owning `docs/stacks/<lang>/` doctrine for every language in scope. Every dispatched workflow and sub-agent then reads its plan phase before authoring any idea or task.