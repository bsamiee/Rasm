# LAB-NOTES

Verified in `F00/*.cs` under the repo analyzers (`latest-all`, warnings as errors, `.editorconfig`). Every row below compiled and ran. A row marked CORRECTION contradicts the plan or the API document.

## Verified surface

- `Option<A>`: `Some`, `Optional`, `Filter`, `Do`, `Map`, `IfNone(Func<A>)`, `IfNone(A)`, `ToFin(Error)`, `ToValidation<Error>(Error)`, `ToSeq`, `IsSome`, `IsNone`, `==` with `Some(x)`; `parseInt(string) : Option<int>` and `parseInt<Option>(string).As()`; `Seq<Option<A>>.Somes()`.
- CORRECTION: `Some(null)` does not throw and does not return `None`. `Prelude.Some(A)` calls `new Option<A>(value)` with no null check, so `Some(null)` is a `Some` that holds `null`. The implicit conversion `Option<A>(A?)` and `Optional(x)` map `null` to `None`. The null boundary is `Optional`, never `Some`.
- `Fin<A>`: implicit from `A`, `Error` (and `Expected` subclasses), `Pure<A>`, `Either<Error, A>`; `Map`, `MapFail`, `BiMap`, `Bind`, `BindFail`, `Match(Succ:, Fail:)`, `IfFail`, `IfSucc`, `Exists`, `ToEither`, `ToOption`, `ToValidation`, `IsSucc`, `IsFail`, `==` with `Pure(x)`; LINQ with `guard(bool, Error)`; `Catch(code, f).As()`. `Fin<A>.Succ` and `Fin<A>.Fail` are nested classes, and the module functions `Fin.Succ<A>(A)` and `Fin.Fail<A>(Error)` exist and resolve (reviewer probe); the docs use the implicit lift from `A` or `Error`, or `Pure(x).ToFin()`, as the one construction path.
- `Validation<Error, A>`: implicit from `A` and from `Error`; tuple `Apply((va, vb), (a, b) => ...)` returns `K<Validation<Error>, R>`, so `.As()` follows; `&` returns `Validation<Error, Seq<A>>`; `|`; `Do`; `MapFail`; `ToFin`; `Match(Fail:, Succ:)` (Fail first); `IsSuccess`; `IsFail`; `Fin.ToValidation()`.
- `Try<A>`: `Try.lift(Func<A>)`, `Run() : Fin<A>`; exceptions arrive as `Error` with `IsExceptional == true`.
- `Error`: `sealed record X() : Expected("message", code)`; `Is`, `HasCode`, `IsType<E>`, `Filter<E>`, `Count`, `Head`, `IsEmpty`, `Inner`, `+` accumulates into `ManyErrors`, `Error.New(string, Error inner)`, implicit from `(int, string)`.
- Domain values: `[ValueObject<int>]` on a `readonly partial struct Age` with `[ValidationError<InvalidAge>]`, where `sealed record InvalidAge() : Expected(message, code), IValidationError<InvalidAge>` declares `public static InvalidAge Create(string message) => new()`; the hook `static partial void ValidateFactoryArguments(ref InvalidAge? validationError, ref int value)` assigns the record; the generated `Validate(value, provider: null, out Age item)` returns `InvalidAge?` (a struct takes `out T item`, a class or smart enum takes `out T? item` and the adapter spells `item!`); `From` is `is { } error ? error : item`; `Create` throws `ValidationException` whose text is `Expected.ToString()`, which is `Message`; the implicit conversion to the key resolves `age < 60`, `age >= 18`, `age + 1`, `total + age`, and `age == 70` through the builtin `int` operators while `Age < Age` stays generated; the default arithmetic operators route through `Create` (`Age.Create(60) + Age.Create(70)` throws) and raise no diagnostic while unused. `[SmartEnum<string>]` with `[ValidationError<X>]` returns the record from `Validate` for an unknown key through `X.Create(message)`, and its string key compares case-insensitive by default (`Validate("MOBILE")` finds `Mobile`). A string-keyed struct value object with both `ComparerAccessors.StringOrdinal` attributes compiles with a hook.
- `IO<A>`: `IO.lift(Func<A>)`, `IO.lift(Func<Fin<A>>)` (folds `Fail` onto the error channel), `IO.lift<Fin<A>>(Func<Fin<A>>)` (carries the `Fin`), `IO.lift(Fin<A>)`, `IO.liftAsync(Func<Task<A>>)`, `IO.liftAsync(Func<EnvIO, Task<A>>)` with `env.Token`, `IO.pure`, `IO.fail<A>(Error)`, LINQ, `RunSafe() : Fin<A>`, `Try().runFin.As().Run() : Fin<A>`, `Run()` throws (captured through `Try.lift(io.Run).Run()`), `Catch(code, f).As()`, `|` alternative, `Bracket(Use:, Fin:)`, `use(Func<A>)` for `IDisposable`, `use(Func<A>, Action<A>)`, `Fork()` with `ForkIO<A>.Await`, `awaitAll(Seq<IO<A>>)`, `awaitAny(Seq<IO<A>>)`, `timeout(TimeSpan, IO<A>)`, `Retry(Schedule)`, `Repeat(Schedule)`, `RepeatUntil(Func<A, bool>)`.
- `Eff<RT, A>`: implicit from `IO<A>`; `Eff<RT, A>.LiftIO(IO<A>)`; `Eff.runtime<RT>()`; `Run(rt) : Fin<A>`; a runtime record `Runtime(ConsoleIO Console) : Has<Eff<Runtime>, ConsoleIO>` implements `static K<Eff<Runtime>, ConsoleIO> Has<Eff<Runtime>, ConsoleIO>.Ask => Eff.runtime<Runtime>().Map(static rt => rt.Console)`; `Console<RT>.writeLine(string)` and `Console<RT>.readLine` under `where RT : Has<Eff<RT>, ConsoleIO>`; `LanguageExt.Sys.Test.Runtime.New()` with `rt.Env.Console : MemoryConsole` (`WriteKeyLine` feeds input, enumeration reads output); `LanguageExt.Sys.Test.Implementations.ConsoleIO(MemoryConsole)` is the test console.
- CORRECTION: `Test.Runtime` and `Live.Runtime` resolve only as `LanguageExt.Sys.Test.Runtime` and `LanguageExt.Sys.Live.Runtime` from an unrelated namespace. `Eff.lift<RT, A>(Func<RT, A>)` is ambiguous with the `Func<RT, Fin<A>>` overload when the lambda returns a subtype of `A`; `Eff.runtime<RT>().Map(...)` avoids it.
- Schedules: `Schedule.spaced(TimeSpan)`, `Schedule.exponential(TimeSpan)`, `Schedule.recurs(n)`, `Schedule.jitter()`, `Schedule.maxDelay(TimeSpan)`, `|` and `&` between a `Schedule` and a `ScheduleTransformer`, `Schedule.Run()` enumerates the delays. `TimeSpan` converts implicitly to `Duration`.
- Loops: `Trampoline.Pure`/`Trampoline.More(Func<Trampoline<A>>)`/`Run()` at depth 100000; `Monad.recur<IO, A, B>(seed, a => IO.pure(Next.Done<A, B>(b)) | IO.pure(Next.Loop<A, B>(a)))` then `.As().RunSafe()` at depth 100000; `tail(...)` as the last bind continuation after a deferred effect (`IO.lift(...).Bind(next => tail(Loop(next)))`) at depth 100000.
- CORRECTION: a `tail`-recursive `IO` exits only through `Run()` or `RunAsync()`. `RunSafe()`, `Try()`, `Map`, and any later `Bind` push a map into the tail and fail with "You can't map a tail call". The host captures with `Try.lift(io.Run).Run()` when it needs a `Fin`. `Monad.recur` and `RepeatUntil` have no such restriction.
- CORRECTION: `MonadLaw<IO>.recurIsSameAsBind()` throws inside the library ("Bug in MonadLaw or Monad.unsafeRecur"). The law checks run against `Option` and `Fin` (`FunctorLaw<Option>.validate(Some(1))`, `ApplicativeLaw<Option>.validate()`, `MonadLaw<Option>.validate()`, `MonadLaw<Fin>.validate()`), all returning `Validation<Error, Unit>`.
- Collections: `Seq<A>.Zip(Seq<B>) : Seq<(A First, B Second)>` and `Zip(Seq<B>, Func<A, B, C>)`; `toSeq(Range(1, 3))`; `Seq.At(Index) : Option<A>` (from `FoldableExtensions`); `Choose`; `Scan(seed, f)` EMITS THE SEED FIRST (`Seq(1, 2, 3).Scan(0, +)` is `[0, 1, 3, 6]`); `Partition` deconstructs to `(First, Second)`; `Map((x, i) => ...)` indexed; `0.Cons(Seq(1))`; `List(1, 2, 3) : Lst<int>` with `Insert`; `HashMap(("a", 1)).Find("a")`; `Lens<A, B>.New(get, b => a => ...)` with `Update(f, a)` and `Set(b, a)`; `toSeq(LanguageExt.List.unfold(seed, Step))`.
- CORRECTION: `List.unfold` does not resolve by simple name because the static import of `Prelude` makes `List` bind to `Prelude.List<T>()`. Spell it `LanguageExt.List.unfold`. `Cons(1, seq)` does not resolve either: `LanguageExt.Pretty.Cons<A>` is a type. The extension form `1.Cons(seq)` resolves.
- CORRECTION: Seq equality between `Seq<(int First, string Second)>` and `Seq<(int, string)>` is ambiguous (CS9342). Declare the expected value with the same tuple element names.
- Traversal: `Seq.Traverse(f).As()` accumulates under `Validation` (two errors from three inputs) and short-circuits under `TraverseM` (one error); `Traverse` under `IO` and `TraverseM` under `IO` both return `K<IO, Seq<B>>` and need `.As()`; `Seq<K<IO, A>>.PartitionFallible().As()` returns `IO<(Seq<Error> Fails, Seq<A> Succs)>`; `Option` traversal returns `Option<Seq<B>>` after `.As()`.
- Shared state: `Atom(0).Swap(f)` returns the new value; `SwapMaybe(f)` keeps the state on `None` and returns the current value; `AtomHashMap<string, int>()` with `TryAdd` (second add ignored), `SwapKey(key, Func<V, V>)`, `SwapKey(key, Func<Option<V>, Option<V>>)`, `Find`; `Ref(100)` with `atomic(() => { _ = swap(a, f); return swap(b, g); })`; `memo(Func<A, B>)`; `memo(Func<A>) : Memo<A>` with `.Value`.
- Streams: `Source.lift(IObservable<A>)`, `Source.lift(IEnumerable<A>)`, `Source.merge(params Source<A>[])`, instance `source.Zip(other) : Source<(A First, B Second)>`, `Reduce(seed, Func<S, A, S>) : IO<S>`, `ReduceIO(seed, (s, a) => Reduced.DoneIO(s) | Reduced.ContinueIO(s))`, `Event.from(ref Action<A>)` then `Subscribe() : IO<Source<A>>`, `Conduit.make(Buffer<A>.Unbounded)`, `conduit.Post(a) : IO<Unit>`, `conduit.Complete()`, `conduit.Reduce(seed, ReducerIO)` running under `Fork()` while a client posts and reads `replies.Source.Take(1).Last()`, `conduit.Sink.Comap(f).Post(x)`.
- CORRECTION: `Source.zip` is declared as an instance method on the non-instantiable `Source` class and cannot be called. The instance `Zip` on `Source<A>` is the working form.
- CORRECTION: `Source<A>.Fold(f, init)` and `Fold(Schedule, f, init)` emitted nothing for a lifted finite sequence, with or without a schedule. `Reduce` is the fold that yields a value.
- CORRECTION: a `Sink` rejects `Post` after `Complete()`.
- Laws: `FunctorLaw<F>.validate(fa)`, `ApplicativeLaw<F>.validate()`, `MonadLaw<F>.validate()`, `identityLaw`, `compositionLaw`, `homomorphismLaw`, `interchangeLaw`, `leftIdentityLaw`, `rightIdentityLaw`, `associativityLaw`, `recurIsSameAsBind`. CsCheck: `Gen.Int.Sample(static x => predicate)` throws on a counterexample.

## Analyzer and formatter rulings

- No `var`: every local names its type (IDE0008). A local assigned a literal is `const` (RCS1118).
- Every non-capturing lambda is `static` (MA0217). A lambda that captures a local or parameter is not.
- A method with one `return` is expression-bodied (IDE0022). K&R braces: `{` on the same line as the declaration or statement (IDE0055). A single-statement `if` has no braces (RCS1002).
- An interpolated string with a number, date, or other culture-sensitive hole is `string.Create(CultureInfo.InvariantCulture, $"...")` (MA0076). An interpolated string whose holes are all strings or objects stays a plain `$"..."` (MA0185 rejects `string.Create` there).
- `Console.WriteLine("literal")` compiles: CA1303 excludes `Console`.
- A `bool` literal argument is named: `Pure(value: true)` fails MA0003 unless named; the probes use `Exists(...)` instead of `== Pure(true)`.
- An expression statement whose value is `Unit` or any other value is discarded: `_ = seq.Iter(...)` (IDE0058).
- A local that holds a new `IDisposable` fails CA2000 unless disposed on every path. Samples acquire inside `use(static () => new Connection())` or `IO.lift(static () => new Connection()).Bracket(...)`, never in a local.
- No `catch (Exception)` (CA1031, ERP022): `Try.lift` and `IO.lift` capture. No `Random` (CA5394): a seeded generator is `State<int, A>`.
- `Cons(1, seq)` is a compile error; `1.Cons(seq)` compiles. `List.unfold` is a compile error; `LanguageExt.List.unfold` compiles.
- A single-use local that feeds the next statement fails RCS1124; a chain or a query names its stages instead.
- Multi-line LINQ queries with one clause per line pass IDE0055; a one-line query also passes.
- Types are `internal`; sample holders are `internal static class`; records are `internal sealed record`; a class whose members do not read instance state fails CA1822 (make the member static or read state).
- A property whose getter returns a private field is an auto-property (IDE0032).
- Global usings from `Directory.Build.targets` cover `Thinktecture` (when `Thinktecture.Runtime.Extensions` is referenced), `LanguageExt`, `LanguageExt.Common`, `LanguageExt.Effects`, `LanguageExt.Pretty`, `LanguageExt.Traits`, `LanguageExt.Traits.Domain`, static `LanguageExt.Prelude`, `LanguageExt.Pipes`, `LanguageExt.Sys`, `LanguageExt.Sys.IO`, `LanguageExt.Sys.Traits`, `LanguageExt.Sys.Diag`, and `System.Globalization` (lab csproj); a file-level `using` for any of them fails IDE0005.

## Corrections from the per-file labs

- F21: RETRACTED: `Fin.Succ<A>(A)` and `Fin.Fail<A>(Error)` ARE module functions (the F21 notes' correction to the API document is wrong; the API rows stand). `Fail(new TooLarge())` infers `Fail<TooLarge>` and does not convert to `Fin<int>`; spell `Fail<Error>(...)` and `guard<Error>(flag, new X())`. `ManyErrors.IsType<E>()` searches the leaves, so accumulation is read with `Count` or `is ManyErrors`. `IO<A>.Catch(predicate, f)` is an instance method returning `IO<A>`; the code and `Error` overloads are extensions that need `.As()`.
- F22: `RunSafe(EnvIO)` does not capture a pre-cancelled token (`TaskCanceledException` escapes); `Try.lift(() => io.Run(env)).Run()` returns `Fail`. `IO.fail(e).Finally(x)` drops `x` (`IOFail<A>.Finally => this`); a failure raised at run time runs the finalizer. `LanguageExt.Sys.Test.Runtime` builds `FileIO` over a real temp directory at `Env.RootPath` and deletes it on `Dispose`. `MemoryConsole` enumeration returns written lines only. `|` between two schedules takes the shorter delay and `&` the longer; `Repeat(recurs(2))` runs the effect three times. `Buffer<A>.Newest(2)` keeps the last two items and `Latest` the last one. A working `Pipes` pipeline: `ProducerT.yieldAll | PipeT.map | ConsumerT.repeat(awaiting().Bind(...))` fused to `EffectT<IO, Unit>`.
- F11: `Seq<string>.Contains(x)` is ambiguous with LINQ (CS0121); use `Exists`. `==` on strings fails MA0006; use `string.Equals(a, b, StringComparison.Ordinal)`. `Regex` literals steer toward `[GeneratedRegex]`; a character-class check stands in for a format check.
- F08: `Fin<A>` has no `Filter`/`Where`; `Pure(x)` does not convert into `Seq<A>` (`F.Pure(value)` under `Applicative<F>` is the general form); `Seq<int>.Average()` and `Sum()` are ambiguous with LINQ (CS0121), `Fold` replaces them; `Seq<A>.Empty` in expression context fails CS0119, `Seq<A>()` is the empty form; `IO.lift(Action)` compiles as an observer step.
- F12: `Seq(...).Contains(x)` is ambiguous (CS0121); `Set(...).Contains` resolves. `x is "a" or "b"` on strings fails MA0127. Static `Gen.Select(a, b)` fails RCS1196; the instance form `a.Select(b)` is required. Unbounded `Gen.Int` products overflow under `CheckForOverflowUnderflow`; use `Gen.Int[min, max]`. The instance `Apply` on `Option<Func<A, B>>` returns `Option<B>` with no `.As()`.
- F18: `IOApply.Invoke` starts both operands before awaiting, so the tuple `Apply` on `IO` overlaps. `Seq<IO<A>>` needs `Map<K<IO, A>>(...)` to become the `PartitionFallible` receiver. `GetAwaiter().GetResult()` fails VSTHRD002; await `RunAsync` inside `IO.liftAsync(...)` then `RunSafe()`. The `Unit` adapter `IO.liftAsync(async () => { await task.ConfigureAwait(false); return unit; })` compiles.
- F14: `guard(flag, new X())` with an `Expected` subclass infers `Guard<X, Unit>` and `ToFin` fails (CS1929); spell `guard<Error>(flag, new X())`. A LINQ query over `IO` opens from a `guard<Error>(...)` clause through `IOGuardExtensions.SelectMany`. `Atom(Seq<A>)` with `ValueIO` and `SwapIO` is an in-memory store.
- F09: `compose<T1, T2, T3>` needs explicit type arguments with method groups. `OptionT.Match` returns `K<M, B>`; the boundary that turns `None` into a typed error is `lookup.Run().As().Bind(static o => IO.lift(o.ToFin(new NotFound())))`. IDE0040 requires `public` on interface members.
- F15: `use(Func<A>, Action<A>)` runs its release on every exit, including failure, so a commit is a separate `IO.lift(fun(transaction.Commit))` step after the statements and `use(connection.BeginTransaction)` disposes (rolls back) an uncommitted transaction. `Option | Option` evaluates the right operand eagerly; `Option || Option` is lazy through `operator true`. `Reader.ask<Env>()`, `Reader.asks`, `Reader.local`, instance `Run(env)` and `With<Env1>`; `ReaderT.ask<IO, Env>()`, `ReaderT.with`, `Run(env).As().RunSafe()` compile and run.
- F17: `toSeq(LanguageExt.List.unfold<S, A>(seed, Step))` memoizes; reading one `Seq` three ways runs the transition once per state, a second `unfold` reruns it. `IO<A>.RepeatUntil(Schedule, pred)` compiles and runs. `Trampoline<A>.Bind` chains after a deep `Run`.
- F16: `State<S, A>` constructs from `Func<S, (A Value, S State)>`; `State.gets<S, A>`, `State.pure<S, A>`, `State.modify<S>`, `StateT.get<M, S>`, `StateT.put<M, S>`, `StateT.modify<M, S>`, `StateT.pure<S, M, A>`, `StateT.liftIO<S, M, A>` need explicit type arguments; `StateT<S, IO, A>.Run(s)` returns `K<IO, (A, S)>` then `.As().RunSafe()`; a bare `IO<B>` binds in a `StateT` query. `StateT<S, M>` implements only `Maybe.MonadUnliftIO`, so `fork` over a `StateT` fails CS0311; a `Fork`ed `IO` inside the transformer reads and writes no state. `Stateful.state<State<int>, int, int>(...).As()` and `Stateful.local<State<int>, int, int>(...)` compile and run.
- F20: `atomic(Func<R>, Isolation.Serialisable)`, `swap`, and `commute` (returns the committed value) compile and run over `Ref`. `Reduced.DoneIO` ends a `Conduit.Reduce` fold so `Await` returns without `Complete`. `AtomHashMap.FindOrAdd(key, value)` is the atomic register-or-return step (the plan's `TryAdd` then `Find` in one call). `OptionT.Some<IO, A>`, `OptionT.None<IO, A>()`, and an `IO<B>` bound inside an `OptionT<IO, A>` query compile.
- F05: the seedless monoid `Fold()` combines each element in front of the state (`Seq(Seq(1), Seq(2, 3)).Fold()` is `[2, 3, 1]`); name only the seeded `Fold`/`FoldBack`. There is no `Seq<A>.Sort` instance; `Order()` from LINQ then `toSeq` compiles (`OrderBy(x => x)` fails MA0159). `Traverse(MethodGroup)` fails CS0407; pass a lambda. `Iterable.Map` chains and `ToSeq()` materializes. `Map<K, V>.AddOrUpdate(K, Func<V, V>, V)` folds a grouping.
- F13: lens composition is `Prelude.lens(la, lb)` (no `Lens.Compose`; `Lens.tuple` pairs). `Seq.Tail` on an empty `Seq` is empty (no throw); `Seq.Match(Empty:, Tail:)` exists. `Map.Add` throws on a present key and `SetItem` on an absent one. `toSeq(IList<A>)` copies eagerly. `Option<A>` as an optional parameter `= default` is `None`. IDE0090 demands target-typed `new(...)` in expression bodies; RCS1161 demands explicit enum values.
- F10: `OptionT.lift<IO, A>(IO.lift(() => Optional(x)))` lifts a nullable read; `OptionT.Match<B>(Some:, None:)` returns `K<IO, B>` then `.As()`. Two lambdas returning different record types in `Option.Match` need the explicit `Match<Base>` type argument. A bare `init` property of a non-nullable reference type fails nullable-as-error; use `required` or positional records. MA0007 requires a trailing comma after the last `switch` arm. IDE0130 forces a type whose name clashes into its own folder namespace.
- F06: `compose(f, g)` applies `f` then `g` (body `b(a(v))`); `flip(Func<A, B, R>)` exists; `unless`/`when` over `IO` return `K<IO, Unit>` and need `.As()`; `Option.IfNone(Func<A>)` is lazy; `Seq.ForAll`/`Exists` short-circuit; `Seq.Map` is lazy and `Do` is strict; a static class named `Pipes` collides with the `LanguageExt.Pipes` namespace; `GroupBy` on strings needs `StringComparer.Ordinal` (MA0002); `Math.Round` needs `MidpointRounding` (MA0193).
- F02: `Range(int from, int count)` forwards to `Range.fromCount` (the second argument is a count). CA1308 rejects `ToLowerInvariant` on a string; `char.ToLowerInvariant` per character passes. A runtime record can implement two `Has<Eff<RT>, T>` traits; `RT.Ask.Map(f).As()` reads a capability under a `Has<Eff<RT>, T>` constraint. Primary constructors are required (`csharp_style_prefer_primary_constructors = error`).
- F07: `par(MethodGroup, x)` fails CS0411 and `fun(MethodGroup)` resolves to `fun(Action)`; explicit lambda parameter types let `par` and `curry` infer. MA0003 fires on a `bool` literal passed to `par`; expose named `const bool` values. `Eff.SelectMany` has no `Fin` overload; `IO.lift(Fin<A>)` or `Eff<RT, A>.Lift(Func<Fin<A>>)` is the entry. `Has<Eff<RT>, RT, ConnectionIO>.ask.As()` reads a user-defined capability. `flip` has 2- and 3-argument and curried forms.
- F04: `Validation<Error, int>.Success(4)` binds to an overload and fails (CS1503); the implicit lift `Validation<Error, int> v = 4;` constructs it. MA0127 rejects a constant-string subpattern inside an `is` expression (a `switch` arm passes). MA0101 rejects a verbatim multi-line interpolated string. `CS8618` on a record `init` property of a non-nullable reference type needs `required`.
- F19: `Source.Combine` is sequential concatenation (the `Concat`/`StartWith` form); `Source.Take` takes a count only; `Source.lift(IObservable)` subscribes inside `Reduce`; `Event<A>` buffers per subscription; `Zip` of a source with its own `Skip(1)` pairs adjacent values; `toSeq(hashMap)` is ambiguous (`HashMap.Filter(...).Keys` or `AsIterable()` resolve); `Source.pure`, `Skip`, `Collect()`, and LINQ `SelectMany` over `Source<A>` compile and run.
- F01: the implicit `DateTime` to `DateTimeOffset` argument fails MA0132 (not MA0133). Bare `None` converts into `Option<A>` through `Fail<Unit>`. `M.Pure` under `Monad<M>` and `F.Pure` under `Applicative<F>` compile in a generic function.
- F24: `AtomHashMap.FindOrMaybeAdd(K, Func<Option<V>>)` returns `Some(default(V))` for a value type when the factory returns `None` (decompile and run); not named in the docs. `FoldM` walks tail-to-head and `FoldBackM` head-to-tail (run). `HashSet<int>` is ambiguous with `System.Collections.Generic.HashSet<T>`; spell `LanguageExt.HashSet<int>`. VSTHRD200 treats an `Iterable<A>` return as awaitable. `memoK<IO, int>` builds once and runs on every call (witnessed). `IterableNE.create` and `Seq.AsIterableNE() : Option<IterableNE<A>>` compile.
- F23: the `TraverseM` extension is declared on the function (`Func<A, K<M, B>>.TraverseM(K<T, A>)`), so the trait calls `T.Traverse`/`T.TraverseM` or the instance members on `Seq` are the forms in use; no `Apply(this K<F, Func<A, B>>, K<F, A>)` extension exists, the trait call is `F.Apply`; `Deriving.MonadIO` over `StateT` does not compile, `Deriving.Monad` plus `Deriving.Stateful` is the smallest derivation; a witness class with only static trait members must be `abstract` (CA1812); `Trampoline<int>` summing to 100000 overflows under checked arithmetic. NOTE: `Prelude.tailIO<M, A>(K<M, A>) where M : MonadUnliftIO<M>` DOES exist (decompile line 303 of the Prelude signatures); the F23 notes' claim that `tailIO` is only a parameter name is wrong.

## Set-wide corrections from the reviews (apply everywhere)

- Traversal under `IO`: instance `Traverse` folds through `Applicative<IO>.Apply`, whose `IOApply.Invoke` starts both operands with `RunAsync` before it awaits either (decompile `LanguageExt.DSL.IOApply`). It starts every element effect before awaiting any, and it allocates NO thread. Only `Fork` takes one `TaskCreationOptions.LongRunning` thread per fork. `awaitAll(Seq<IO<A>>)` is `Task.WhenAll` over `RunAsync` and forks nothing; the `Seq<ForkIO<A>>` and `Seq<IO<ForkIO<A>>>` overloads await forks. Every sentence that says `Traverse` under `IO` forks a thread per element (files 18 and 23, and the brief's traversal row) is wrong: the bound applies to `Fork` fan-out, and chunking bounds the overlap of `Traverse`.
- `IO.Run()`/`RunAsync()` throw the error as an exception: an `Expected` throws its `ErrorException`, an `Exceptional` rethrows the original exception, and cancellation leaves `RunAsync` cancelled.
- `RepeatWhile(pred)` is `RepeatUntil(not(pred))`: it polls while the value satisfies the predicate.
- `Retry(Schedule)` on a pre-built `IO.fail` node returns the failure with no rerun (`IOFail<A>.RetryUntil => this`); it reruns a deferred effect that failed. The same node rule as `Finally`.
- `Try.lift(io.Run).Run()` returns the original `Error` unchanged (an `Expected` keeps its type and code); only a genuine exception becomes `Exceptional`. The API document line that says the round trip "does not preserve the original exception" needs "An `Error` thrown by the effect returns unchanged."
- `Prelude.lpar` has ONE overload: it fixes the second argument of a two-argument function.
- `DomainType<SELF, REPR>` is retired. Thinktecture owns the domain value type, the generated `Validate` feeds `Fin<T>` through a hand-written `From`, and `Create` replaces `FromUnsafe`. Under `[Union]` (F10, F14 and the F99 probe): a record case is `sealed` (`TTRESG055`), the base declares a private constructor for shared state because `TTRESG043` rejects a primary constructor, positional cases pass the shared value to that constructor and synthesize no second property, `required` init members compile on the base and on the cases, `Switch(state, arms...)` infers the result when every arm returns one type, `Switch<TResult>` names it when arms differ, and a `static` lambda is mandatory in every arm (`TTRESG1001`). The Thinktecture `Map` (one value per case) is not used in the corpus because the functor `Map` owns the name.
- `Source<A>` is a stream of values (not "pull"); `Fork` has no "pool".
- `commute` applies its function inside the transaction (against the in-transaction value) and applies it again at the commit point against the last committed value (`STM.Commute`, `CommitCommutes`); `atomic(Func<R>)` returns the in-transaction result. Owner decision: file 24 owns `Ref`/`atomic`/`swap`/`commute`; file 20 drops its `Transfers.Move` block and keeps one sentence that names `Ref` under `atomic`.
- `Seq<A>` is memoized, not strict: `toSeq` copies an array, a list, or a collection at once and wraps any other `IEnumerable<A>` in a lazy memoizing sequence; `Seq.Map`, indexed `Map`, `Filter`, and `TakeWhile` return lazy sequences (`SeqLazy`). `Iterable<A>` holds no memory and re-runs its source. `Traverse` under `IO`: `IO.liftAsync` effects overlap without a bound; `IO.lift` effects run in order on the calling thread (F23 probe timings).
