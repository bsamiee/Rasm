# [PAGE_SEAL]

- `<TARGET>` =
- `<DOCTRINE>` =
- `<WORKSPACE>` =

`<TARGET>` exists with prose and snippets complete. This work is the publication gate: the standards' own validation, the corpus-fit check, and the atlas change that makes the page active.

[GROUNDING]:
- Fully read `<DOCTRINE>`, then `docs/standards/style-guide.md`, `docs/standards/information-structure.md`, and `docs/standards/formatting.md`, before any other work: the laws, the collapse scan, the page craft, and the prose, container, and rendering standards.
- Read the sibling pages in `<TARGET>`'s folder, then all of `<TARGET>`.

[VALIDATE]:
- Run the validation checklists that close `docs/standards/style-guide.md`, `docs/standards/information-structure.md`, and `docs/standards/formatting.md` against the page, and fix every miss.
- Verify the page grammar end to end: an opening at full signal whose first screen stands alone as an operating brief, narrow index tables, cards beside the rules they govern, snippets beside the cards they prove, collapse tests, and the closing validation list.
- Confirm zero meta-commentary, zero sourcing, zero links or cross-file pointers, and placeholder discipline in every fence.
- Confirm every snippet's demonstration is unique across the page and its sibling pages, and that every fence holds 15-25 load-bearing lines.
- Confirm every table holds its bounds: bracketed semantically rich header rubrics, atomic cells, no cell past 8 words, at most one prose column, and no links in cells.

[INTEGRATE]:
- Update `<DOCTRINE>` where it lags the page: an atlas row still marked planned moves to active, and any roadmap scope for the page leaves the tail — the scope lives in the page.
- Leave `<WORKSPACE>` intact; the reservoir outlives the page.

[GRADE]:
- Read the page cold and score one to ten on five axes — signal, coverage, card power, structure, doctrine fit — against a bar of 9.2 on each. Grading is critical, harsh, but fair — not a 9.2 out of convenience, not low for nit-picking. The page publishes only when every axis clears; anything short returns for a targeted fix before sealing.

[COMMIT]: stage and commit `<TARGET>` and `<DOCTRINE>`.
