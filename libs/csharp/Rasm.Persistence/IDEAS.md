# [PERSISTENCE_IDEAS]

The forward pool of higher-order concepts for the durable-state spine, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[TRACE_CONTEXT_CHANGEFEED]: the op-log changefeed and the lineage CDC feed carry no W3C trace-context frame, so a browser interaction, a C# solve, and a Python tessellation that all mutate the durable graph land as three disconnected root traces at the collector — the `ONE_DISTRIBUTED_TRACE` concert seam has no Persistence-side anchor.
- Capability: thread the C#-minted `traceparent`/`tracestate` (the `AppHost/observability/diagnostics-and-telemetry#CORRELATION_SPINE` propagation fold, owned never re-minted) through the `OpLogEntry` envelope and the `provenance#LINEAGE_CDC` `CdcEnvelope` so an op committed under an active span carries the parent trace context, and the `sync/collaboration#TRANSPORT_AXIS` drain and the `stores-remote/object-store#ARTIFACT_SYNC_FEED` announce extract-and-continue the parent context at the wire rather than minting an independent root.
- Unlocks: one correlated distributed trace across the three runtimes joined at the op-log — a flame graph spans the browser INP span, the C# `ActivitySource` solve span, and the Python tessellation span under one trace id, so a write that crosses runtimes reads end-to-end, not stitched by guesswork across three collector silos; the attested-ledger entry and the CDC envelope both carry the originating trace so an audit row is trace-correlated.
- Draws on: the W3C Trace Context propagation standard and the `ONE_DISTRIBUTED_TRACE` cross-libs idea (C# owns the propagation fold, peers extract-and-continue); the trace-context bytes ride the `OpLogEntry` `Payload`-adjacent envelope slot, never a second correlation owner — `Npgsql.OpenTelemetry` already instruments the driver span, so this binds the changefeed write into the same `ActivitySource` lineage the spine carries, the gap being that the op-log commits with no parent-context field today so the trace flattens per process.

[GRADUATION_LEDGER_IMPORT]: `ARCHITECTURE` names Persistence consuming AppHost graduation/determinism as settled, but no design page owns the inward seam where a Python offline result graduates into the durable spine — the `ONE_GRADUATION_EVIDENCE` `HandoffAxis` evidence and its `(checksum, OrtEpDevice)` provider fingerprint land in the determinism event log with no Persistence residence binding, so a graduated model-asset is durably unaddressed.
- Capability: a `provenance#ATTESTED_LEDGER` cluster row admitting the content-keyed `HandoffAxis` graduation evidence as one `AttestedEntry` so a graduated offline result chains into the tamper-evident ledger under its `ContentIdentity` key, the `model-asset` axis ONNX artifact (post-`ModelAssetManifest` validation) residing on the `cache/indexes#ARTIFACT_BLOB_INDEX` as a new `GraduationAsset` kind row content-addressed identically to every other artifact, and the `(checksum, OrtEpDevice)` fingerprint folding into the attestation tuple so the determinism closure references one durable key.
- Unlocks: a single inward contract for offline science durably homed — a Python result graduates the same way regardless of which package produced it, the C# determinism chain references one content key, and a re-import re-verifies the fingerprint so a provider-divergent surrogate fit is caught at admission rather than at inference; the graduation asset rides the existing object-store residence and closure GC so a stale surrogate is collected by the one reachability sweep.
- Draws on: the `ONE_GRADUATION_EVIDENCE` cross-libs idea (Python holds the rail singular, C# owns determinism at the content-keyed seam) and the C# `SESSION_CAPSULE` key; the surrogate crosses as a content-keyed ONNX artifact the C# side runs inference over, never an in-process training loop, so this owner adds the durable residence and the attested-ledger chaining net-new while AppHost owns the determinism event log and Compute owns the inference.

[REDIS_LIVE_FABRIC]: the cache page exploits `StackExchange.Redis` only as a passive `IBufferDistributedCache` L2 tier, leaving its pub/sub, RESP3 server-assisted client-side caching, Lua/`FunctionLoad` scripting, and Stream consumer-group surfaces — the package's real distributed-coordination capability — entirely unmined behind a single registration row.
- Capability: a `cache/indexes#L2_CONTRIBUTION` deepening where the `CacheResidence.Redis` tier earns the live-fabric role its own SDK already owns — RESP3 `ConnectionMultiplexer` server-assisted client-side caching invalidating the L1 on a key change through the `__redis__:invalidate` channel rather than TTL-bounded staleness, a `ISubscriber` keyspace-notification stream feeding the existing op-log HLC cursor as the cross-process tag-transition backplane the page today declares it deliberately lacks, and an atomic `IServer`/`IDatabase` `ScriptEvaluate`/`FunctionLoad` Lua surface for the stampede single-flight and the cross-process writer-lease fence the suite owns.
- Unlocks: a Redis tier that is a real distributed-cache coordinator, not a passive blob store — L1 staleness collapses from TTL-bounded to invalidation-driven, the tag-transition backplane the page defers to peer op-log replay gains a push path where Redis is present, and the writer-lease/stampede fence rides an atomic server-side script rather than a managed compare-loop; absent Redis the existing TTL-bounded path is the unchanged baseline, so the fabric is additive capability, never a new dependency.
- Draws on: the full `StackExchange.Redis` capability surface (`.api/api-redis.md` catalogues the multiplexer/database/subscriber/server roots but not the RESP3 client-side-caching, keyspace-notification, or `FunctionLoad` members this binds — the decompile fold of those members is this idea's API-pass leg); AppHost still owns the cache port, stampede policy, and tag vocabulary, so this deepens the one Persistence contribution row, never a second cache owner.

[CONTENT_PARITY_CORPUS]: the `XxHash128` content-address seed and the `Hlc` two-half stamp are minted across a dozen Persistence owners (commit-DAG, element-set receipt, embedding identity, snapshot seal, op content key, attested-ledger digest) but no owner emits the frozen golden bytes the `ONE_WIRE_FIXTURE_CORPUS` parity harness reads, so a cross-runtime reproduction drift surfaces as three divergent reproductions rather than one corpus mismatch.
- Capability: a `versioning/version-control#CRDT_WIRE` cluster row minting the frozen content-identity and HLC-stamp fixture bytes Persistence owns — the canonical `(SortedParents, SortedOpKeys, Hlc)` commit-key bytes, the sorted-element-set receipt bytes, the `CrdtOpWire` op-set encoding, the `EmbeddingIdentity` seed, and the `Hlc.WriteTo` 16-byte cell — as the Persistence leg of the one content-addressed golden-fixture corpus every runtime's parity harness reads.
- Unlocks: byte-identical reproduction provable per seam — a Python-recomputed CRDT op-set, a TS-read content key, and a C#-minted commit key reconcile against one frozen corpus, so an off-by-one-half HLC encoding or a sort-order drift fails a single corpus assertion rather than silently folding a fresh op as stale across runtimes.
- Draws on: the `ONE_CONTENT_IDENTITY` and `ONE_WIRE_FIXTURE_CORPUS` cross-libs ideas (the corpus is keyed by the one `XxHash128` seed, two-64-bit-half order); the seed and the HLC two-half encoding ride the same parity because a logical off-by-one-half folds a fresh op as stale with no other signal, so the corpus is the shared owner the `[BRANCH_TEST_NODE_PROVISIONING]` shared-corpus task reads, never a per-harness re-derivation.

[SCHEDULE_BASELINE_CASCADE]: the schedule sub-domain reads P6/MS-Project into one activity network and folds 4D/5D state, but the construction-schedule domain's real depth — multi-baseline variance, resource-loaded leveling, retained-logic progress, and the CPM forward/backward float pass — sits at a thin critical-path stub (`CriticalPath` returns the single longest-duration task), so the activity-network owner is shallow against the domain it claims.
- Capability: a `schedule/schedule-interchange#SCHEDULE_STORE` deepening where the activity network earns the real CPM algebra it implies — a forward/backward float pass computing early/late start-finish and total/free float over the `TaskRelation` DAG (the same DAG-walk the commit-DAG and lineage-DAG use), a `ScheduleBaseline` content-addressed snapshot axis so a baseline-versus-current variance is a multi-baseline content-key diff rather than a two-key compare, retained/progressed-logic scheduling honoring `ActualStart`/`ActualFinish` against the data date, and resource-loading columns so a resource-leveled schedule and an over-allocation surface as activity-network folds.
- Unlocks: the schedule store owning real construction-scheduling depth, not a P6 reader plus a 4D paint — a critical path is the float-zero activity set, a schedule slip is a typed float-erosion delta, a re-baseline is one content-keyed snapshot, and the 4D/5D state the viewport scrubs reads the float-correct activity dates; one DAG algebra serves CPM, lineage, and the commit-DAG so no per-metric calculator enters.
- Draws on: the standard CPM forward/backward-pass algorithm over the relation DAG and the content-addressed snapshot identity the spine already owns; the schedule rides the Sep tabular reader and the federated cross-doc link as settled, so this deepens the one activity-network owner with the float algebra, the baseline axis, and the resource-loading columns rather than a per-format schedule library or a Primavera SDK.

## [2]-[CLOSED]

[DUCKLAKE_LAKEHOUSE]: [CAPTURED] — `data-lanes#ANALYTICAL_LANE` parquet export promoted to a DuckLake v1.0 SQL-catalog lakehouse (`ATTACH`-per-backend, HLC-drained ACID snapshots through the existing `DuckDBOpLogMap` appender, `AT (VERSION => n)` time-travel); catalog on the PG tier, data on object-store under the one content key, parquet-export the `Available`-false fallback.

[IDS_SPECIFICATION_MODEL]: [CAPTURED] — buildingSMART IDS 1.0 folded into `federation#RULE_PLAN` as data (`Parse`/`Scope`/`Lower` projecting facets and cardinalities into the settled `RuleAst.Requires`/`Forbids`), an unsupported facet a typed rejection, the `IdsConformance` receipt aligned to `annotation#BCF_PROTOCOL`.

[TRANSPARENCY_PROOF]: [CAPTURED] — `provenance#ATTESTED_LEDGER` lifted to an RFC 9162 Merkle transparency log (O(log n) inclusion/consistency proofs over the `AttestedEntry` leaves, `ECDsa` head-seal the sole crypto surface, redaction-preserving leaf, export through `redaction-retention#EXPORT_PROOF`).

[SHORT_TAG_CONSUMER]: [CAPTURED] — `snapshots#COMPRESSION_HASHING` `HashPolicy.Content` (64-bit `XxHash3`) bound as the `#CONTENT_CHUNKING` `ContentChunk.ShortTag` bloom pre-filter ahead of the authoritative 128-bit `ContentKey` compare; dedup identity stays `HashPolicy.Identity`, and the no-use-site boundary prose is corrected.

[ARROW_FLIGHT_EGRESS]: [CAPTURED] — the in-process Arrow plane bound to a zero-copy Flight SQL + ADBC egress at `query-rail#ARROW_EGRESS` (one `#ARROW_PLANE` carrier schema as `RecordBatch`, `FlightServer` serve, `AdbcStatement`/`QueryResult` read-back, standing-query batches over one `DoExchange`); the server-Flight/ADBC member fold is the `[ARROW_FLIGHT_SERVE]` API-pass leg.

[CDC_DEDUP_STORAGE]: [CAPTURED] — fixed-window framing replaced by FastCDC content-defined chunking at `snapshots#CONTENT_CHUNKING` keyed by `XxHash128`, the `cache/indexes#ARTIFACT_BLOB_INDEX` deduping chunks across snapshots and peers; the boundary survives an interior insertion, aligned to `Compute/interchange#GEOMETRY_DELTA` at the rolling-hash level only.

[LAYERED_COMPOSITION]: [DROPPED] — product-anchored (USD) and draft-standard-anchored (IFC5); minted no capability the `versioning#COMMIT_DAG` named branches plus `versioning#CRDT_ALGEBRA` `MvRegister` concurrent-keep and `TimeTravel.BranchFromPast` do not already own. (DO-NOT-RE-ADD.)
