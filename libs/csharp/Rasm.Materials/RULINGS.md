# [RASM_MATERIALS_RULINGS]

Per-folder decision registry for the materials catalogue — the settled rulings agents re-litigate for lack of a home. A row is one decision with the one why that guards its re-litigation: a package admission or rejection, an owner-choice discriminant, a collapse refusal naming the guarantee the denser form loses, a structure retirement, or a folder-learned working law. Append-mostly; a row earns its seat while its why stays homeless, and dies only when fact and why both live at one durable surface.

## [01]-[SHAPE]

- A second design code for an already-cased section family enters as a `DesignBasis` receipt column with per-basis kernel dispatch, never a sibling `SectionCapacity` case — the realized case names each bind one code (`SteelLrfd` AISC, `TimberEc5` EN 1995, `MasonryCompression` TMS), so a per-code sibling reads as precedent while forking the closed `GoverningAction`/`Utilisation` verdict vocabulary the Compute design-check consumer keys on; a basis-tagged re-cut of that verdict vocabulary opens only on the consumer's co-sign.
