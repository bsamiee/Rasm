# [IDEATE_WORKFLOW]

[BRIEF] You must be an orchestrator, not a writer, unless the task is focused, and small, otherwise, delegate all tasks. We are in plan mode, and MUST maximize it exhaustively, we cannot and will not leave plan mode until it is extensively researched, investigated, and detailed; all the actions that will be done once we leave plan mode must be predetermined with great detail in plan mode. You must use opus agents to help compile the plan through-out, reviewing it as you build it parallel to any other work we are doing, they will critique, refine, and improve it, ensuring the plan is signal, focused, and structured properly, no vagueness or ambiguity in it, providing enough detail to immediatly jump into workflows once we leave plan mode. Prior to presenting the plan to user, review the entire "finalized" plan line by line, and identify the appropriate series of workflows that will be needed, plan them NOW to have agents also review/critique it compared to plan, to ensure we have well made phase structure, without any gaps, each phase is high impact/value, and is sequenced properly, and we leverage parallel work within a workflow, as well as parallel workflows where we can. Never commit, do not worry about commiting, focus on implementing all the work, user will commit themselves at the end.

For the approach, fully understand topology of lib-wide, language-wide, and per-folder, and the neccessity to have foresight on the impact of tasks/ideas waterfalling across so that each is implemented holistically, and fully, but done in a complimentary manner, not as dual paradigms, and this may necessitate refinement/interpretation of the tasks/ideas. ASK THE USER AS MANY QUESTIONS AS POSSIBLE, EARLY DURING PLAN MODE, DURING PLAN MODE, MIDDLE OF PLAN MODE, AND WELL BEFORE FINAL PRESENTATION TO ENSURE PLAN IS DONE WITHOUT MISUNDERSTANDING. THE PLAN MUST BE EXTREMELY DETAILED, ALL WORKFLOWS MUST REFERENCE/READ IT AND OTHER STANDARDS NEEDED PRIOR TO PROCEEDING, YOU AND ALL WORKFLOW AGENTS MUST TREAT THE PLAN AS TRUTH, ADJUST ONLY BASED ON EARLIER SESSION WORK TO IMPLEMENT LATER WORK PROPERLY.

[READ]:
- Session Targets: `libs/.planning/planning-targets.md`
- Mandated Approach: `libs/.planning/campaign-method.md`
- Planning Infrastructure: `libs/.planning/README.md`
- Planning Topology: `libs/.planning/architecture.md`
- MUST READ: `CLAUDE.md` + `AGENTS.md` + `README.md` + `tools/assay/README.md`

[CODE_DOCTRINE] - Universal, regardless of language, all other languages must meet these same standards of density, complexity, advancement, no flat code.
- Foundation: `docs/stacks/csharp/README.md`
- Core (read all ROOT files in dir): `docs/stacks/csharp`
- Specialized (read all files in dir): `docs/stacks/csharp/domain`

Note: `docs/stacks/<lang>/` for the tier's language is the governing code doctrine, read in full at every stage; `docs/stacks/csharp/` is the universal density bar every branch matches whatever its language. A fence is held to C# bar FIRST its own language doctrine SECOND.

[API_SOURCES]
- The branch and folder targets named by the session, each target's own `<pkg>/.api/` catalogues, and the admitted packages themselves.
- These MUST be utilized and understood whenever working on design docs, idea or task creation, we treat external libs/packages as FIRST CLASS and PREFERRED over stdlib.

[LIVE_PROOF]
- If a task or idea requires live probing, launch Rhino WIP (not rhino 8) locally on machine to run it, it is available, between assay, rhino-bridge, and our rhino-mcp skill there is nothing we can properly diagnose/extract/identify/understand regarding rhino behavior for any functionality need.
- We have: `tools/rhino-bridge/README.md` available as well, and pairs with `tools/assay`
- We have: `.claude/skills/rhino-mcp` as well if needed

## [GOALS]











[IMPORTANT]: This is more than just external lib evaluation, removals/additions, we want ultra-deep, line by line reading of all folders in isolation and holistically, (use a workflow),



code quality issues per `docs/stacks/csharp` for our `<TARGET>`, that is the floor (ultra-dense,rich, complex code over flat spam, no indirection, etc), ensuring no handrolling where api the package has available can replace, improving/extending all functionality within the existing design docs, adding new files, or sub-folders (these are tasks, not work we do, but must be grounded in logic/fact). Essentially, we want to create complete, full per folder tasks, in isolation, THEN, we do the cross-language tasks, to add to the <language>/.planning/ tasks, but ALSO updating/refining all folders within it, adding tasks related to those central language changes, refining/massaging existing tasks if they need to, for cross-folder alignment where relevant, etc, the lib wide is essentially the same, adding the tasks from big ideas to relevant folder locations, refining/massaging where cross-language concern is relevant (without coupling), things like wires, projections, grpc, ports, etc especially, but also in other ways where we want to keep all capability aligned where relevant.

[CRITICAL]: ALL EXTERNAL LIB/PACKAGE RELATED FINDINGS ARE IMPLEMENTED ONCE WE LEAVE PLANNING MODE, NOT RELEGATED TO TASKS/IDEAS, WE REPLACE/ADD/REMOVE/ETC ALL - UPDATING CENTRAL PACKAGE MANAGERS - REMOVING OLD .API/ FILES AS WELL AS CREATING NEW ONES IF NEEDED. Do not just focus on external libs, they are a huge source of ideation/potentiality for ideas, tasks, new folder ideas/concepts, new files within existing folders, new capabilities/functionalities within existing design docs, cross-cutting additions to existing design docs, etc but not the only source of ideation (general modern/current research as well, with dedicated agents). IF there are any existing tasks/ideas related to implementing/adding external libs/api, we invesitgate/evaluate the quality/correctness of it, and if valid/appropriate we WILL implement the task/idea. WE CAN REMOVE ANY IDEA AT ANY LOCATION, AS WELL AS TASKS, AND REPLACE WITH NEW AS NEEDED, SO LONG AS ALL DESIGN DOCS ARE FULLY READ IN ALL FOLDERS (REQUIRED), FOR PROPER UNDERSTANDING OF CHANGES, IF ANY NEEDED. Ideas must be well scopped/defined, and tasks even more so, within the guidelines of the campaign method. WE ARE NOT WORKING ON DESIGN DOCS; PRIMARILY FOCUSED ON IDEAS, TASKS, AND EXTERNAL PACKAGE ADDITIONS, REPLACEMENTS, REMOVALS, AND THEIR .API/ FILE CREATION/REMOVAL, ETC

NOTE: For all folders we change external libs (adding, removing, etc), update the README.md to update manifest, for csproj folders, update the packaging integration, ALWAYS add to central package manager properly, ensure newest version. This is done by a WORKFLOW once we leave planning mode.

NOTE: All tasks especially driven from external libs/packages must also reconcile with our `docs/stacks/csharp/` content, and identify the most optimzed/dense/rich way to integrate things, root/ground-up, not ad-hoc, no flat code/spam, ideally, identifying how to integrate into existing objects/shapes to increase density where possible.

[IMPORTANT]: Server and native closure evidence comes from Assay `provision` reports. Planning prompts consume that evidence when a task needs live PG18, extension, DuckDB, or scientific-native facts. Read `tools/assay/README.md` for provison tool usage, ensure agents that need it are aware of it and how to use it.

[IMPORTANT]: For each language, we need a workflow that will iterate over each folder, go one language at a time, one folder at a time, with findings logged vy agents, and synthesized to not drop any findings, but to organize, and structure for per folder, per language, and lib wide findings. Ensure each folder is fully examined for external lib/package integration, examine all existing ext libs/packages being used, identify overlaps, where one tool can replace many, have agents do search for modern/advanced/powerful replacements for any sources we currently use, either 1-1 replacement, or 1-many, or a pure addition that is appropriate for the folder, and enables new ideas, functionalities, and capabilities. External packages/api's should also be heavily understood to identify gaps in existing design docs, tasklogs, or ideas, where we wan't fully realizing their value, replacing handrolling, identifying capabilities/functionality to add from 1 or many ext sources in isolation or by integrating multiple to achieve far more powerful code capabilities. 

[IMPORTANT]: During the planning phase, fully survey all target folders, collect all ideas and tasklog files, we want to first use a workflow to synthesize all of them, identify a refactoring/structuring to align them; from top down to have concepts flow properly to all locations, folders may have folder specific ideas/tasks not relevant to a broader whole, but for general alignment and bigger ideas, those ideas/tasks must be refined, and ensure they aree aligned to be cohesive. For "ideas" we want to go lib wid -> language wid -> folder specific. However, each language and folder should also independent ideas/task creation flows in isolation as well, so that we have cross-cutting ideas where multiple languages need to align, as well as language specific, and folder specific. Always leverage external libs/packages to help with creating ideas/tasks.

[IMPORTANT]: We want to have proper external lib/package hygiene passing; identifying poor selections (outright removal), alternative/better tooling, gaps in tooling/packages regardless of current catalog size, however, if a planning target is light one external libs, it's a high signal for deeper research, and ensuring any new ideas/suggestions are appropriate for the folder and it's place in the strate.

[IMPORTANT]: For all workflows, it is 1-2 agents PER folder, the per folder pass, language-wide pass (not 1 agent for multiple folders), lib-wide pass (multiple agents per language) with synthesis and orchestration handled properly, never overload any agent to read more than 1 planning folder. Never burst a workflow, scope properly to have a phase slowly unroll agents (mainly a concern for lib-wide pass)

[PASS] IDEATE over the `libs/` planning corpus. IDEATE builds the forward pool and distills it into tasks across all three tiers; it adds ideas and tasks, writes no code fence, and implements nothing.

[METHOD] The campaign method, stated inline so the task is self-contained on entry; the routes below carry the full law and the complete doctrine.
- Surface: work ONLY on the `.planning/` design corpus — design pages and the transcription-complete code FENCES inside them — plus the four index docs per tier, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain. A FENCE is a markdown fenced block, the work product itself, never a `.cs`/`.py`/`.ts` source file.
- Bar: world-class library code — dense and polymorphic, one owner per axis, one rail family and one entrypoint per concern, total dispatch, no wrappers/shims/aliases, every admitted package mined to its full capability through the owning axis, capability conserved absolutely (a collapse proves every case, field, and method survived). Files are as large as the concern requires. `docs/stacks/<lang>/` is the floor and `docs/stacks/csharp/` the universal parity bar every branch meets whatever its language.
- Principles: one unified body of functionality — a new capability is a row, case, or column on an existing axis, never a parallel surface; full modality completeness from the root; everything parameterized; plug-in-play composition aligned only at the wire/companion seams, never coupled to a sibling's interior.
- Tiering + stages: run as fanned workflows across three altitudes — per-folder, then a full per-language pass, then the cross-`libs/` master, run TOP-DOWN so the cross-`libs/` concert seeds the language concert and the language concert seeds the folder pool. Each altitude is a FULL pass at broader scope (the same research → produce → critique → red-team depth), never a thin aggregation: it adds what only emerges at that altitude and re-grades, aligns, and corrects the lower tier, applying the boundary-integrity check — a concept owned twice within a runtime, a folder mixing unrelated concerns, a concept scattered across folders, or a cross-folder/cross-language touchpoint coupled to a sibling's interior is a defect it fixes or records. Producer → critique → red-team, every stage applying its own fixes; a single un-adversaried author is never the final word.

[TARGETS]: READ ALL OF: `libs/.planning/planning-targets.md` 
- Contains all planning infrastructure folders that this session will target (lib scope, language scope, per folder).
- Read: `AGENTS.md` (repo root)
- Read: `README.md` (repo root)

[ROUTES]:
- `libs/.planning/campaign-method.md` — the full law behind `[METHOD]`: the campaign scope, the bar, the universal principles, the IDEATE pass, the workflow orchestration, the loop.
- `libs/.planning/architecture.md` — the strata, dependency direction, geometry/IFC flow, per-language roles, and the `.planning/` lifecycle. Every idea is grounded here, never in speculation.
- `libs/.planning/README.md` — the doc-set, the four index-doc templates (the IDEAS and TASKLOG card form especially), the notation, the language law.
- `docs/standards/information-structure.md`, `formatting.md`, `style-guide.md` — the form an idea card and a task card take.
- `docs/stacks/<lang>/` for the tier's language and `docs/stacks/csharp/` as the parity bar — the doctrine an idea's eventual fences meet, the measure of how big a world-class implementation of the domain is.

[SCOPE] Author the forward pool top-down and distill it into scoped tasks: the cross-`libs/` concert first, then each language's branch concert, then each folder's three to five higher-order ideas, each tier refining the one above it. An idea is a bigger concept — a new file, sub-domain, capability, or package — grounded in the folder's domain, the monorepo purpose, and current research; never a low-value restatement of existing capability, never anchored on a single named product. Each idea spawns one or more tasks at the tier it lands; a task names the exact file or sub-domain, the external packages to integrate, the integration points and boundaries/wires (aligned, never coupled), and the key considerations. An idea that only re-confirms existing capability is dropped, not recorded. Every task and idea is PLANNING work: it authors or deepens a design page and its code fence, adds a design page, sub-domain, or folder to design, admits an external package or tooling, or refines a doc — it NEVER targets writing a source file (`.cs`/`.py`/`.ts` and the like) or transcribing or implementing code into source. A task naming a source file as the thing to build is out of scope; source-transcription is the separate mode behind an explicit code-creation kickoff.

[WORKFLOW] Construct the pass as fanned workflows, never one-off agents. Top-down tiering: a cross-`libs/` workflow seeds the master `IDEAS.md`/`TASKLOG.md`, then one workflow per language fans master -> branch -> folder. Each altitude is a FULL pass at broader scope — the same research -> author -> critique -> red-team depth as the folder tier, never a thin aggregation: it adds the ideas and tasks that only emerge at the language or cross-`libs/` altitude AND reviews, refines, and massages the lower tier's ideas and tasks for cross-folder and cross-language alignment, surgically widening scope or integration where it raises value and correcting or removing an item formulated in isolation of its folder. Each workflow chains research -> author -> critique -> red-team; assign a capable model to authoring, red-team, and synthesis and a lighter model only to exploration and decompile reads. Keep concurrent heavy workflows at two or three. The orchestrating session is the sole ledger writer and never runs git from an agent. A cross-language idea with no language idea or folder task yet lands at its touchpoint the same turn; no tier is left stranded. The same loop runs for every language — the C# stack is the parity bar, not the only producer; the Python science/compute/data/geometry/IFC companion and the TypeScript web/edge platform each author their own native concepts against their own doctrine, consuming a sibling only at the wire, never mirroring a sibling's layout.

[HUNT] From multiple directions, never one:
- Open ideas not fully realized: re-examine each against the corpus; a partially-realized idea is sharpened and its missing tasks spawned.
- Closed ideas: re-verify each is realized in fences; re-open any that is not, with the gap named.
- Coverage audit into tasks (recorded, never implemented now): every README-registry-vs-central-manifest mismatch (a package claimed but absent from the language manifest, or present but unregistered), every missing `.api/<package>.md` catalogue, every newer or stronger package, and the per-language testing and tooling gaps. A package add or replacement spawns a task that names the canonical owner to extend first — never a parallel surface — the README registry and central language manifest to update, and the folder's `.api/<package>.md` catalogue to add; no per-folder manifest is ever introduced. A testing-infra gap is seeded as an idea that spawns its infra tasks, not a bare lone task.
- Weak-folder pressure: a one-file sub-domain with genuine growth potential earns ideas and tasks to grow it; one with none is flagged for REFINE to restructure.
- Domain growth areas (grow the WIP folder set): hunt the conceptual domains a branch or a folder lacks ENTIRELY, not only gaps within existing owners. A new top-level folder is a candidate only for a genuine full conceptual domain, never a mini file or thin concept at the `libs/<lang>/` level — a lower bar for Python (a distinct science/data/artifact capability) and TypeScript (a backend/infra domain such as SQL/Postgres, provisioning, or an edge tier), a HIGH bar for C# where the strata is near-complete and growth is mostly a new SUB-DOMAIN inside an existing folder (a `Rasm.Bim` model concern, a kernel `Rasm/Geometry` robust-core owner, an app-platform surface). A new top-level package is raised as a candidate card naming its charter, its stratum placement per `architecture.md`, and why it is a higher-order domain rather than a mini sibling; a new sub-domain is an idea on the owning folder, run at the folder tier, while a folder-spanning or new-package concept is a language-tier idea. Host-boundary growth (`Rasm.Rhino`/`Rasm.Grasshopper`) is mature-source work on the branch `TASKLOG.md`, never an IDEATE design page. Every candidate is validated against the strata, the branch role, and the boundary-integrity check so growth is a logical aligned addition, never a wrong, duplicative, or mini one. Cap OPEN new-top-level-folder candidates at three per branch: beyond three, refine the existing candidates — sharpen the charter, the scope, and the tasks they spawn — rather than adding a fourth, and the red-team REPLACES a weak candidate with a stronger alternative rather than accumulating. New sub-domain ideas are not capped beyond the owning folder's three-to-five pool.

[BOUNDARY] Source generation is a separate future project outside this campaign's passes; the session never enters it and defines no kickoff or pass for it. Every pass operates only on the `.planning/` design corpus (design pages and the transcription-complete code FENCES inside them), the four index docs, the central manifests, the per-folder `.api/` catalogues, and the Nix toolchain. To ideate is to add ideas and tasks that target a design page, a code FENCE, a package or tooling admission, or a doc refinement — never the writing of a `.cs`/`.py`/`.ts` source file or source tree. IDEATE adds ideas and tasks and writes no fence; a task naming source-file creation or code transcription is out of scope.

[BAR] A high-value IDEATE turn lands a domain-grounded concept the corpus has not conceived, sized against a world-class implementation of the domain, with its tasks spawned at the right tier and its seams aligned. The red-team kills every low-value, speculative, anchored, or duplicative idea; the critique re-balances the tasks each surviving idea spawns. Ideas read as IDEAS cards and tasks as TASKLOG cards per the standard — a card or record where one carries it, never an md-table that is not a true matrix.
