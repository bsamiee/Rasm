# [RASM_BIM_TASKLOG]

Open and closed work distilled from `IDEAS.md`. `[1]-[OPEN]` carries task cards with a `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]`/`[UPSTREAM-BLOCKED]`/`[HOST-PROBE-DEFERRED]` leader; `[2]-[CLOSED]` carries `[COMPLETE]`/`[DROPPED]` cards. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

## [1]-[OPEN]

[UPSTREAM-BLOCKED] Ground the `BsddResolution` LIVE-WIRE transport leg against the Compute transport seam — `classification/systems.md#BSDD_RESOLUTION` `[3]-[BSDD_RESOLUTION]`.
- The spec-grounding leg is CLOSED this pass: the `BsddPort.Fetch` response shape is grounded against the published buildingSMART bSDD API contract (`BsddClassResponse` `Code`/`Name`/`ClassType`/`Uri`/`Definition` + `ClassProperties` with `PropertyCode`/`DataType`/`PropertySet`/`PredefinedValue`/`IsRequired`, the `/api/Class/v1`·`/api/Dictionary/v1` endpoints), projected through `BsddClass.Of`. The in-process degradation to the row's local code-shape policy (`LocalShape`) is the verified settled present-tense fallback.
- REMAINING upstream dependency: the live in-process transport that ISSUES the request and streams the response binds at the cross-folder `csharp:Compute/remote/channels#TRANSPORT_AXIS` transport alignment; the `BsddPort` transport rides the injected Compute transport at the composition edge, never a transport minted here. Close-condition: the Compute transport seam lands the injected `BsddPort` implementation.

[UPSTREAM-BLOCKED] Ground the IDS cross-tool audit against the ifctester companion — `validation/ids.md#IDS_FACETS` `[3]-[RESEARCH]` `[IFCTESTER_COMPANION]`.
- The `IdsSpecification`/`IdsFacet`/`IdsAudit` in-process fold over `BimModel`, the IDS v1.0 XSD parse over `System.Xml.Schema`, and the spec authoring are authored and settled; the in-process fold is the immediate self-audit and the `IdsAudit` rides the `exchange/wire#WIRE_PROJECTION` `BimWireContext` (closed this pass).
- EXTERNAL dependency: the IfcOpenShell ifctester deterministic cross-tool audit is a `python:geometry/ifc-companion` PyPI surface reached only through Compute's companion rpc. Close-condition: the companion request/response shape grounds at the cross-folder companion alignment (the `exchange/tessellation-bridge#TESSELLATION_BRIDGE` two-hop pattern) once the py geometry ifc-companion lands; the companion is the external-conformance oracle, never a transport minted here.

[UPSTREAM-BLOCKED] Promote the candidate `step-iso10303`/`mesh-text`/`iges-ansi`/native-companion codec bodies — `exchange/import-rail.md#IMPORT_RAIL`·`exchange/tessellation-bridge.md#TESSELLATION_BRIDGE` `[AP242_CODEC]`/`[NATIVE_FORMAT_BRIDGES]`.
- The `InterchangeFormat` rows, the `InterchangeCodec` discriminants, the `StepProtocol` column, the frame columns, and the `CataloguePending` markers are settled; the import fold faults `import-catalogue-pending` on a candidate row and `import-needs-companion` on a companion-gated row (IFC/STEP/IGES/native), which is the correct present-tense behavior. IGES carries the distinct `iges-ansi` companion codec, not `step-iso10303`, because the ANSI IGES section-based grammar shares neither the STEP physical-file token grammar nor the GeometryGym entity surface.
- EXTERNAL dependency: the STL/3MF/OBJ/PLY mesh-text reader packages and the managed AP242 STEP reader are unadmitted (`<stl-3mf-obj-ply-reader-pending>`, `<step-ap242-reader-pending>`), and the IGES, Revit/Navisworks/DWG native readers have no managed loader. Close-condition: each candidate codec promotes in place — capability columns flip and the codec body grounds — when the named package catalogue lands (per-package manifest admission); IGES, native-format, and STEP-solid geometry evaluation route through the Compute companion rail (OpenCascade serves the IGES/STEP solid read in the same two-hop the IFC tessellation uses) because the managed branch carries no kernel.

## [2]-[CLOSED]

[COMPLETE] CATALOGUE_EXTENSION_GEOMETRYGYM — extended `.api/api-geometrygym-ifc` with the ten absent entity scopes (architectural built-element / MEP distribution / structural-analysis / scheduling-cost-resource / grouping-zone-distribution-system / georeferencing families), the per-class `PredefinedType`/domain enum vocabulary, `IfcRepresentationMap` (verified absent, distinct from `IfcMappedItem`), and the type-occurrence/predefined/georeferencing/scheduling traversal entrypoints as one internal decompile pass feeding every W1 consumer a settled roster.

[COMPLETE] IFC_ENTITY_CLASS_BREADTH — `model/elements.md#ELEMENT_MODEL` widens the `IfcClass` `[SmartEnum<string>]` to ~40 IFC4.3 rows across the architectural/MEP/structural/infrastructure/geotechnical domains (each row carrying its `IfcDomain` and frozen valid-predefined set), widens `IfcDomain` to the seven-case buildingSMART partition, and `query/element-set.md#ELEMENT_SET` adds the `ByDomain` union + Match Switch arm so every real element stops collapsing onto `IfcBuildingElementProxy`.

[COMPLETE] PREDEFINED_TYPE_DISCRIMINANT — `model/elements.md#ELEMENT_MODEL` adds the `PredefinedType` `[ValueObject<string>]` column on `BimElement` admitted via `IfcClass.AdmitPredefined` against the row's frozen valid set with the `USERDEFINED`/`OBJECTTYPE` fallback, `exchange/import-rail.md#IMPORT_RAIL` widens `ProductRow` with the predefined/objectType split via `ParserIfc.IdentifyIfcClass`, and `query/element-set.md#ELEMENT_SET` adds the `ByPredefinedType` arm.

[COMPLETE] TYPE_OCCURRENCE_BIM_TYPE — `model/elements.md#BIM_TYPE` promotes the thin `TypeRow` into the `BimType` type-object owner carrying the `BimTypeKind` discriminant, the type-bound property/material/predefined bindings, and the `IfcRepresentationMap` instanced-geometry content key, threading the existing `ByType`/`TypeGlobalId` pair (no second selection surface); `exchange/import-rail.md#IMPORT_RAIL` widens the `TypeRow` projection.

[COMPLETE] MATERIAL_SUBDOMAIN_OPEN — `material/composition.md#MATERIAL_COMPOSITION` owns the `BimMaterial` + `BimMaterialComposition [Union]` and `material/appearance.md#MATERIAL_APPEARANCE` the `BimAppearance` PBR record reconciled with Rasm.Materials; `model/elements.md#ELEMENT_MODEL` swaps `BimElement.Materials` from `Seq<string>` to `Seq<BimMaterial>` and `exchange/import-rail.md#IMPORT_RAIL` carries the typed material projection.

[COMPLETE] ZONING_SUBDOMAIN_OPEN — `zoning/grouping.md#ZONE_GRAPH` owns the `BimZone` many-to-many overlay (`BimZoneKind [SmartEnum]` + `ZoneAssignment [Union]`); `query/element-set.md#ELEMENT_SET` adds the `ByZone` arm reading the `BimModel.Zones` index, and `model/elements.md#ELEMENT_MODEL` carries the `Zones` index `BimZone.IndexOf` populates from the import-rail `ZoneRow` family.

[COMPLETE] RECONSTRUCTION_SUBDOMAIN_OPEN — `reconstruction/primitives.md#RECONSTRUCTION` owns the `ReconstructionPrimitive [Union]` (plane/cylinder/torus/freeform), the `ElementClassifier` primitive->IfcClass table, and the `ReconstructionLineage` source-cloud key, consuming `csharp:ROBUST_ARRANGEMENT_SUBSTRATE` at the kernel seam by reference, composing the one `BimModel` and query algebra.

[COMPLETE] ANALYSIS_SUBDOMAIN_OPEN — `analysis/structural-model.md#ANALYSIS_MODEL` owns the host-neutral `AnalysisModel` graph (`AnalysisMember [Union]` over `IfcStructural*` plus `LoadGroup`/`Support`), the idealized member binding the physical `BimElement` by GlobalId, the Compute solver reading it by `(GeometryHash, PropertyHash)` reference at the `csharp:AEC_SIMULATION_BRIDGE` seam.

[COMPLETE] SEQUENCING_SUBDOMAIN_OPEN — `sequencing/schedule.md#SCHEDULE` owns the `ConstructionTask` (`IfcTaskTime` as a NodaTime `Interval`), the `SequenceRel [Union]` lag as a `Period`, and `ConstructionState.At(Instant)`, exploiting the admitted-but-unexploited NodaTime calendar/interval surface beyond `Instant`.

[COMPLETE] COST_SUBDOMAIN_OPEN — `cost/estimate.md#ESTIMATE` owns the `CostItem` (rate x `QuantitySet` quantity by GlobalId), the `ConstructionResource [Union]` (Labor/Material/Equipment) joined to `sequencing#ConstructionTask`, and the `CostSchedule.Rollup` fold the cross-libs `csharp:TELEMETRY_LAKE_ANALYTICS` cost catalog consumes.

[COMPLETE] MEP_SYSTEM_TOPOLOGY — `systems/connectivity.md#CONNECTIVITY` owns the `DistributionSystem` (`DistributionSystemKind [SmartEnum]`), the `PortConnection [Union]` (ConnectsPortToElement/ConnectsPorts) connection-edge graph, and the `SystemTrace` graph fold, distinct from the element-breadth `IfcClass` MEP rows.

[COMPLETE] MAP_CONVERSION_PROJECTION — `georeferencing/coordinate-reference.md#GEO_REFERENCE` authors the `GeoReference.Project` body reading the new `exchange/import-rail.md#IMPORT_RAIL` `MapConversionRow` (the `IfcGeometricRepresentationContext.HasCoordinateOperation` → `IfcMapConversion`/`IfcProjectedCRS` extract); the georeferencing entities are W0-catalogued.

[COMPLETE] GEODETIC_TRANSFORM_LEG — `georeferencing/coordinate-reference.md#GEODETIC_TRANSFORM` owns the net-new `GeoReference.Reproject` datum-to-datum leg over `ProjNET` `CoordinateSystemServices.CreateTransformation`/`MathTransform.Transform` composed before the rigid map-conversion offset; the README ProjNET row flips PLANNED->admitted.

[COMPLETE] BIM_LOD_MESHLET_PYRAMID — `exchange/export-rail.md#BIM_LOD` owns the `BimLod` per-element LOD pyramid through `Meshopt.Simplify`/`SimplifySloppy`, the meshlet residency band through `Meshopt.BuildMeshlets`, and the per-LOD content key the `csharp:Rasm.Compute#TILE_PARTITION` pyramid addresses, closing the `Alimer.Bindings.MeshOptimizer` LOD/meshlet under-exploitation.

[COMPLETE] WIRE_THREE_FACES_FIXTURE_FOLD — `exchange/wire.md#WIRE_PROJECTION` folds `lib:ONE_MODEL_THREE_FACES` (the `BimWireFace [SmartEnum]` Snapshot/OpLog/Grpc faces + `OpLogWire`/`BimWireDescriptor` records on the one `BimWireContext`), adds the missing `IdsAudit` `[JsonSerializable]` row, and folds `lib:ONE_WIRE_FIXTURE_CORPUS` (the `WireFixture` `XxHash128`-keyed golden bytes), all additive rows on the one context, no second serializer.

[COMPLETE] BSDD_SERVICE_CONTRACT (spec-grounding leg) — `classification/systems.md#BSDD_RESOLUTION` grounds the `BsddPort.Fetch` response shape against the published buildingSMART bSDD API contract (`BsddClassResponse`/`ClassProperty` field spellings, `/api/Class/v1`·`/api/Dictionary/v1` endpoints) projected through `BsddClass.Of`; the LIVE-WIRE transport leg stays [UPSTREAM-BLOCKED] in [1]-[OPEN] on the Compute transport seam.

[COMPLETE] BIM fault owner (ALIGNMENT-DEFECT A) — authored `faults/faults.md#FAULT_BAND` as the `BimFault` closed `[Union]` band-2600 (`ModelRejected`/`UnmappedClass`/`DanglingReference`/`CodecReject`/`CapabilityMiss`) with `Code`/`ToError`/`Message`, composing the kernel `GeometryFault`; rewrote the ~8 scattered stringly-typed `new BimFault.ModelRejected($"<token:detail>")` sites across `model/elements`, `classification/systems`, `assembly/spatial-structure`, `exchange/format-axis`, `exchange/import-rail`, `exchange/export-rail`, and `exchange/tessellation-bridge` to lift the typed case and lower it through `.ToError()`, the band recoverable by `HasCode`/`Is`.

[COMPLETE] IDS validation owner — authored `validation/ids.md#IDS_FACETS`: the `IdsFacet` `[Union]` (Entity/Attribute/Property/Classification/Material/PartOf) `ToPredicate` lowering onto the `query/element-set#ELEMENT_SET` algebra, the `IdsSpecification.Audit`/`Parse`/`Author` over `System.Xml.Schema`/`System.Xml.Linq`, and the `IdsAudit` receipt; the validation predicate IS the query predicate.

[COMPLETE] BCF coordination owner — authored `coordination/issue-exchange.md#BCF_ARCHIVE` (the `BcfTopic`/`BcfComment`/`BcfViewpoint` family, the `.bcfzip` codec over `ZipArchive`/`XDocument`, the `BcfApi` REST projection, anchored on IFC GlobalIds, NOT a geometry-format-axis row) and `coordination/model-diff.md#MODEL_DIFF` (the `ModelDiff.Between` fold joining two `BimModel` snapshots by GlobalId plus the Compute content-key into the `ElementChange` added/modified/removed/moved arms).

[COMPLETE] bSDD dictionary binding — authored `classification/systems.md#BSDD_RESOLUTION`: the `BsddResolution.Resolve` over the injected `BsddPort` transport seam degrading to the row's local code-shape policy, the `BsddClass`/`BsddProperty` evidence, and the `Classification.CodeShape` local-policy column; the live wire shape stays `[UPSTREAM-BLOCKED]`.

[COMPLETE] Property and quantity owner — authored `properties/property-sets.md#PROPERTY_SETS`: the `PropertyKey` `[SmartEnum<string>]` standard-Pset vocabulary, the `PropertyValue` `[Union]` typed value, the `PropertySet.Resolve` type-vs-occurrence inheritance fold, the `QuantitySet.Derive` base-quantity derivation, and the `IfcRelDefinesByProperties` round-trip; promotes the flat `PropertyRow`/`QuantityRow` projections into the one typed owner.

[COMPLETE] Georeferencing owner — authored `georeferencing/coordinate-reference.md#GEO_REFERENCE`: the host-neutral `GeoReference` record, the `ProjectedCrs` value-object, the `ToTransform` kernel-transform composition, and the `FrameNormalization.Georeference` CRS overload distinct from `Canonicalize`; the GeometryGym projection and import-rail widening stay `[UPSTREAM-BLOCKED]`.

[COMPLETE] Universal wire projection — authored `exchange/wire.md#WIRE_PROJECTION`: the `BimWire` content-keyed snapshot, the source-generated `BimWireContext` `JsonSerializerContext`, the `BimWireOptions` `ThinktectureJsonConverterFactory` registration, and the `Encode`/`Decode` gating through each owner's `Validate`; the closed families serialize by key/case discriminant, no hand-authored DTO mirror.

[COMPLETE] 3DTiles per-tile metadata (under-utilization closure) — authored `exchange/export-rail.md#TILE_METADATA`: the `TileMetadata.Attach` `EXT_structural_metadata` schema/class/property-table over the `BimElement` semantic bound through `EXT_mesh_features` `FeatureIDBuilder`, exploiting the admitted-but-unexploited `SharpGLTF.Ext.3DTiles` surface catalogued at `.api/api-sharpgltf-3dtiles`.

[COMPLETE] Speckle `Base` object-graph import seam — extended `exchange/import-rail.md#SPECKLE_SEAM` (anchor `[3]-[SPECKLE_SEAM]`) with the `BimIo.ImportSpeckle`/`ImportSpeckleSemantic` entrypoints folding a deserialized `Speckle.Sdk.Models.Base` tree onto the canonical `ImportedGeometry`/`IfcSemanticModel` carriers; a third arm on the existing `BimIo` capsule symmetric to `ImportGeometry`/`ImportIfc`, composing `Speckle.Sdk`/`Speckle.Objects` only in the host-neutral exchange assembly.

[COMPLETE] Doc-model migration — re-homed `Model/.planning/object-model.md` into the source-mirroring `model/elements.md`, `query/element-set.md`, `classification/systems.md`, and `assembly/spatial-structure.md`, and `Exchange/.planning/interchange.md` into the split `exchange/*` pages under the single `.planning/`; rebuilt `ARCHITECTURE.md` as a codemap, `README.md` as router-plus-package-registry, and authored `IDEAS.md`/`TASKLOG.md`.

[COMPLETE] glTF extension-axis drift — closed the Drako/meshopt manifest-vs-page mismatch (the `KhrExtension` compression rows compose `Openize.Drako` and `Alimer.Bindings.MeshOptimizer`, `GlbBytes` routes the encode through them), fixed the `KHR_meshopt_compression` spelling, and clarified the USD rows as scene-graph coexistence.

[COMPLETE] Exchange god-page split — split the one `exchange/interchange.md` into the source-mirroring `exchange/format-axis.md`, `exchange/import-rail.md`, `exchange/export-rail.md`, and `exchange/tessellation-bridge.md`, distributed the seven RESEARCH items to their owning pages, and rewrote every intra-folder reference to the split-page form.

[COMPLETE] Exchange fence verification — corrected the `IfcBuildingElementProxy` ctor catalogue row to the real GeometryGym `(IfcObjectDefinition host, IfcObjectPlacement, IfcProductDefinitionShape)` and catalogued the `IfcBuildingStorey`/`IfcMaterial` ctors; replaced the import-rail `new ReadContext()` with the catalogued `ReadContext.CreateFromDictionary` text-context path carrying `Validation = ValidationMode.Strict`; collapsed the duplicated canonical-frame guard into one row-owned `InterchangeFormat.IsCanonicalFrame` predicate.
