# [CROSS_LIBS_TASKLOG]

Open and closed cross-language tasks — the wire seams that span two or more of C# / Python / TypeScript. Per-language and per-folder work lives in the branch and folder `TASKLOG.md`; this node carries only the seam each branch consumes at the boundary, never a re-aggregation of branch work. Each task names its producer and consumer touchpoints in `lang:pkg/page#CLUSTER` notation plus the considerations that scope it; a closed task compacts to one or two lines.

## [1]-[OPEN]

[TS_CONSUMES_CSHARP_WIRE] [QUEUED]:
- The TS interchange tier decodes the C# wire contracts only — the suite proto method shapes, the tenant-context wire, and W3C trace context — with no C# interior coupling, selecting the grpc-web transport for the live legs.
- Producer `csharp:Rasm.Compute/Runtime/channels#PROTO_VOCABULARY` to consumer `typescript:interchange/Transport/transport#PROTOCOL_SELECTION_AXIS`.
- Considerations: the wire is the only coupling; a host-local C# surface never appears in the TS consumed set, and the capability tuple is read from the upstream wire, never invented branch-side. The four-signal telemetry lake stays `csharp:Rasm.AppHost/Observability/telemetry` owned across the wire; the browser reads the collector as one more decoded feed and never mints a second telemetry surface.

[PYTHON_COMPANION_SERVES_WIRE] [BLOCKED]:
- The Python `runtime/transport/serve` companion serves the existing C# `ComputeService`/`ArtifactSync` gRPC over the UDS/InProcess transport leg; `ContentIdentity` reproduces the C# `XxHash128` seed; data/artifact bundles and graduation evidence cross the offline seam.
- Producer `csharp:Rasm.Compute/Runtime/channels#PROTO_VOCABULARY` to consumers `python:runtime/transport/serve#SERVE` and `python:runtime/evidence/identity#IDENTITY`.
- Considerations: never reach a C# interior; reproduce the seed bit-identically; the companion mints no second wire vocabulary. Blocked on the Python sub-3.15 companion-floor admission gate and the `[CREDENTIAL_PEM]` `\n--SEP--\n` placeholder gating `SERVE_HOST`, both carried on the Python branch `TASKLOG.md`.

[TESSELLATION_RAIL_TRILANG] [QUEUED]:
- The two-hop `IFC → IfcOpenShell → GLB` (and the AP242 CAD-STEP `STEP → OCCT → GLB` companion) is Python-native and serves the one GLB every runtime consumes: C# builds the tessellation request and content-addresses the result, Python serves the daemon, TS renders the GLB by the same content key; meshing has one owner per runtime (Rasm-DEC, Py-scan, TS-render) and IFC one semantic owner per runtime, meeting only at the content-keyed GLB.
- Producers `python:geometry/mesh/daemon#TESSELLATE` (serving over the `python:runtime/transport/serve#SERVE` companion contract) and `csharp:Rasm.Bim/exchange/interchange#TESSELLATION_REQUEST` (request build) to consumers `csharp:Rasm.Compute/Runtime/codecs#TILE_PARTITION` (content-address the result) and `typescript:ui/render/glb#GLB_VIEWPORT` via `typescript:interchange/Codec/frame#CONTENT_HASHING`.
- Considerations: one geometry-evaluation companion, no duplicate mesh pipeline per runtime; the per-tile `EXT_structural_metadata`/`EXT_mesh_features` schema carries the IFC class and field band on the same content key, so a re-tessellation at a new deflection re-keys geometry and metadata together. The result is keyed by the one `ContentIdentity` seed, so this seam trusts the content-identity parity precondition; the CAD-STEP hop rides the same `SourceFormat`-discriminated request, never a second daemon. The GLB arm is [QUEUED]; the AP242 STEP arm is [BLOCKED] on the `cadquery-ocp` companion admission. Blocked downstream on the Python companion-floor admission gate carried on the Python branch `TASKLOG.md` and the TS mesh-shape promotion carried on the TS `ui` `TASKLOG.md`.

[CONTENT_IDENTITY_PARITY] [BLOCKED]:
- Prove the `XxHash128` content-address seed reproduces byte-identically across the three runtimes against the one C#-owned seed, including the HLC two-64-bit-half order a logical off-by-one-half would corrupt.
- Producer `csharp:Rasm.Compute/Runtime/codecs#CONTENT_ADDRESSING` to consumers `python:runtime/evidence/identity#IDENTITY` and `typescript:interchange/Codec/frame#CONTENT_HASHING`.
- Considerations: the seed (seed zero, two-half order) is the single source; the TS leg resolves a 128-bit-capable wasm hash or downgrades the C#-owned seed to XXH64; the Python leg consumes `xxh3_128_intdigest`. The seam is the precondition the off-thread digest and the convergence presence fold both trust as cross-runtime. Blocked on two package preconditions — the TS `hash-wasm` admission (retiring the 128-bit-incapable `xxhash-wasm`) and an upstream `xxhash` cp315/abi3 wheel for the Python core leg (the sub-3.15 companion consumes `xxh3_128_intdigest` today).

[CRDT_OPLOG_WIRE_AMENDMENT] [QUEUED]:
- The op-log CRDT-op union is a breaking amendment to the one wire vocabulary (LWW survives only as the register arm); the TS-web codec leg and the Python companion decode the amended payload, never an additive parallel surface.
- Producer `csharp:Rasm.Persistence/Version/commits#CRDT_WIRE` (+`#CRDT_ALGEBRA`, `#TS_PROJECTION`) + `csharp:Rasm.Persistence/Sync/collaboration#TS_PROJECTION` to consumers `typescript:interchange/Codec/codec#CRDT_OP_DECODE`, `typescript:projection/causality/vector#CRDT_SEMILATTICE`, `typescript:ui/overlay/presence#PRESENCE_OVERLAY`, and `python:runtime/transport/serve#CRDT_DECODE`.
- Considerations: the producer owns the ten-arm op vocabulary (`set`/`write`/`add`/`remove`/`increment`/`insertAfter`/`delete`/`maintain`/`beat`/`leave`, the `beat`/`leave` arms carrying the `EphemeralMap` presence delta); no consumer authors an op kind the wire does not carry. All four decode legs land: interchange transcribes the full `OpLogEntryWire` plus the `CrdtOpWire` union and the `CommitNodeWire`/`VersionVectorWire`/`HlcWire` commit shapes, projection folds the join-semilattice arm and the presence cell at `causality/vector#CRDT_SEMILATTICE`/`#ORIGIN_CURSOR`, ui renders the two settled presence arms, python decodes the msgpack delta. Residuals are honest and producer-gated: the python `Lz4BlockArray`-envelope decompression (`[CRDT_OPLOG_LZ4]`) and the ui `Awareness` beat `state` byte-encoding pin (`[BEAT_PAYLOAD]`). The Lz4 framing gap is tri-language: the C# `CrdtWire.Encode` compresses under `MessagePackCompression.Lz4BlockArray` — a msgpack-csharp `ext` block-array framing (independently-LZ4-block-compressed chunks), not a raw LZ4 frame `lz4.block`/`lz4.frame`/`msgspec` reads natively, and no cp315-clean Python lib decodes it; the resolution is the producer-owned decision — a `MessagePackCompression.None` companion lane OR a published envelope spec so the C#, TS, and Python runtimes decode one frame. Live tri-language decode parity stays gated at `TRI_LANGUAGE_WIRE_PARITY`.

[CAPABILITY_SDK_CODEGEN] [QUEUED]:
- One capability-descriptor source emits the C#/TS/Python SDKs and the MCP projection; the sibling branches consume the generated SDK, never a per-service hand-written client.
- Producer `csharp:Rasm.AppHost/Agent/capability#SDK_CODEGEN` to consumers `typescript:interchange/Transport/transport#CODEGEN_TOOLING` and `python:runtime/transport/serve#SERVE`.
- Considerations: the descriptor source is owned once in AppHost; `invoke` is one polymorphic method keyed by descriptor id, the `inputSchema` the generated per-descriptor JSON Schema. The TS consumer is blocked on the upstream descriptor source; the Python SDK-codegen consumption rides the companion server-host.

[FAULT_WIRE_ROUNDTRIP] [QUEUED]:
- The fault wire is a two-sided coherence contract: C# packs `(package, code, case)` with package-exclusive code bands (an enforced producer invariant) and TS reconstructs the wire triple losslessly. Bands are disjoint (ComputeFault 2200, HopFault 4500, WireFault 4520-4532, store/config at app roots), so package selects the family and code selects the arm; neither field alone is a total key.
- Producer `csharp:Rasm.Compute/Runtime/channels#FAULT_PROJECTION` `WireFault`/`FaultDetail` pack site to consumer `typescript:interchange/Ingress/fault` `faultTagOf`/`FAULT_CTOR`; NOT tri-language — Python mints `FaultDetail` outbound and is not a package-keyed decoder.
- Considerations: the C# pack site asserts code-in-band-of-package before emit so a misbanded code is a producer fault, never a wire ambiguity; the TS `FAULT_CTOR` already threads the `case` field and folds an unmapped package to `Quarantine`. The remaining cross-language obligation is the round-trip parity property asserting `(package, code, case)` reconstructs losslessly, added to both suites.

[CAUSAL_TENANT_IDENTITY_WIRE] [BLOCKED]:
- The HLC two-half causal stamp and tenant context are minted once in C# and read by the peers: the Python receipt fan-in propagates the same causal/tenant frame inbound and the TS ordering fold reads the HLC band off the wire, neither re-minting a parallel stamp or tenant scheme.
- Producer `csharp:Rasm.AppHost/Runtime/ports#PORT_RECORDS` (the `ReceiptEnvelope` HLC two-half stamp + `TenantContext` partition) to consumers `python:runtime/transport/serve#SERVE` (decodes `hlc_physical`/`hlc_logical` and `tenant` off the inbound receipt slot, propagating the same causal/tenant frame, never re-minting) and `typescript:projection/convergence#SKEW_ORDERING`.
- Considerations: the HLC two-64-bit-half order rides the same parity gate as the content seed, so the causal-identity parity is proven in the same multi-runtime fixture; the TS leg consumes the band as an ordering input (concurrent-uncertain rows), never a render-only leaf; a second causal stamp or tenant scheme in any peer is the named drift defect. Inherits the content-seed parity package gate (the `hash-wasm`/upstream-`xxhash`-wheel preconditions).

[GRADUATION_EVIDENCE_INWARD] [QUEUED]:
- The Python graduation rail is the single content-keyed contract every offline result crosses outward on; the C# determinism closure re-imports it into the hash-chained event log and references it by the one content key, never a per-result handoff.
- Producer `python:compute/graduation#GRADUATION` to consumer `csharp:Rasm.AppHost/Runtime/determinism#EVENT_LOG`.
- Considerations: the evidence is keyed by the one `ContentIdentity` seed (so this seam trusts the content-identity parity precondition); `HandoffAxis` is the seven-arm `Literal` union (`solver`/`symbolic`/`model-asset`/`array-layout`/`unit-law`/`uncertainty-law`/`geometry`, the geometry arm sub-discriminated by `GeometrySubject`), so a new graduatable kind is one axis literal, never a parallel handoff shape; the surrogate fit crosses as the content-keyed `model-asset` ONNX artifact the C# side runs inference over (after `ModelAssetManifest` validation), never an in-process training loop; Python holds the `HandoffAxis` rail singular so the C# owner consumes one rail, never a handoff family. The Vectors DDG adjoint surface and the `Rasm.Compute` `DesignProblem.OperatorRows` registry are landed (`T-DDG-ADJOINT-ROWS` closed), so the gradient-adjoint optimizer the surrogate feeds is non-degenerate; the C# determinism consumer's remaining open leaf is the offline graduation-evidence handoff itself, not the adjoint surface.

[ONE_WIRE_FIXTURE_CORPUS] [QUEUED]:
- One content-addressed golden-fixture corpus every runtime's parity harness reads — the eight-primitive clash bytes, the 52-byte `0x9462A71A5DD13DCFA3B1D6D225FCBE70` adjacency digest, the `FaultDetail` `(package, code, case)` triples, the CRDT op-set, GLB-by-key, and the HLC two-half stamps minted once C#-side.
- Producers `csharp:Rasm.Compute/Runtime/codecs#CONTENT_ADDRESSING` + `csharp:Rasm/Geometry/Spatial/reconciliation#CANONICAL_BYTE_IDENTITY` to consumers `python:runtime/evidence/identity#IDENTITY`, `typescript:interchange/Codec/frame#CONTENT_HASHING`, `typescript:testing/`, and the C# shared test corpus.
- Considerations: the missing parent of `CONTENT_IDENTITY_PARITY` + `FAULT_WIRE_ROUNDTRIP` + `TRI_LANGUAGE_WIRE_PARITY`; keyed by the one `XxHash128` seed and frozen once so every harness reads it rather than re-deriving its own fixtures. The corpus index lives at `csharp:Rasm/Geometry/Spatial/reconciliation#ONE_WIRE_FIXTURE_CORPUS` (the producer owner of the sole host-derived REAL byte fixture); the `CANONICAL_BYTE_IDENTITY` 52-byte stream + `0x9462A71A5DD13DCFA3B1D6D225FCBE70` digest is host-validated REAL and frozen, and the five remaining fixtures (CLASH_GOLDEN, FAULT_TRIPLES, CRDT_OP_SET, GLB_BY_KEY, HLC_TWO_HALF) stay DESIGN-PIN until each producer pins its byte-deriving input — no fabricated byte set stands in. The one open pin blocking corpus completion is `csharp:Rasm/Geometry/Spatial/index#CLASH_GOLDEN` (the 8 `BoundingBox(min,max)` coords + `NodeCount` + clash-pair count), pinned by the I-pass kernel agent. Seeds the C# `[BRANCH_TEST_NODE_PROVISIONING]`, Python `[PYTHON_TEST_INFRA_FLOOR]`, and TS `testing/` infra tasks.

[WEB_GEOMETRY_RESIDENCY_WIRE] [BLOCKED]:
- A second geometry wire shape — a content-keyed splat/meshlet residency manifest distinct from the triangulated GLB — for the WebGPU raster path the GLB-per-tile rail does not cover.
- Producer `csharp:Rasm.AppUi/Render/viewport#TS_PROJECTION` (mints the residency manifest) to consumers `typescript:platform/Transport/decode#RESIDENCY_DECODE` and `typescript:ui/render/glb#GLB_VIEWPORT`, binding the Python splat-decode companion (SPZ/SOG).
- Manifest mint+decode leg [COMPLETE]: the AppUi `ResidencyManifest` producer wire now byte-matches the consumers — the `:x32`-string content keys, the `[x,y,z,r]` bounds tuple, the `ViewpointWire` grouped camera, and the `uint?` color cross as `platform/Transport/decode#RESIDENCY_DECODE` decodes them through the `ContentKeyHex` `Schema.transform` into the one `interchange` `ContentKey` brand, single-mint held (AppUi mints, the worker decodes, `ui/glb-viewport` consumes by reference).
- Considerations: the one owner is the AppUi projection (absent it the manifest is minted twice, AppUi + the TS worker interior), so the single-hash-mint invariant holds at `platform/worker/`. Residuals stay BLOCKED: the splat-payload decode `[UPSTREAM-BLOCKED: Python SOG/PLY/LAZ + xxhash cp315 wheel]` (the SPZ/SOG two-hop into `SplatSource`) and the WebGPU cluster-LOD upload `[HOST-PROBE-DEFERRED: live WebGPU device]`. Inherits `ONE_CONTENT_IDENTITY` parity for the splat leg's `:x32` reproduction.

[ONE_DISTRIBUTED_TRACE] [QUEUED]:
- One W3C trace-context frame every runtime injects and extracts so a browser interaction, a C# solve, and a Python tessellation join into one correlated trace at the collector; today each runtime injects only its own active span and no peer extracts the C# parent across the wire, so the trace flattens per process into three disconnected root traces.
- Producer `csharp:Rasm.AppHost/Observability/telemetry#CORRELATION_SPINE` (owns the `traceparent`/`tracestate` `TraceContextPropagator`/`BaggagePropagator` composite registered as `Propagators.DefaultTextMapPropagator`) to consumers `python:runtime/observability/receipts#RECEIPT` (gains the inbound `Extract`/`Continue` primitive seeding its span from the C# parent context) and `typescript:platform/Observability/telemetry#METRIC_REGISTRY` (propagates `traceparent` on the outbound grpc-web call and continues the parent trace inbound rather than minting an independent `ParentBasedSampler` root).
- Considerations: C# owns the propagation fold; the peers extract-and-continue at the wire, never minting a second tracer or a fresh root span. The Python leg extracts from the inbound gRPC metadata carrier (the companion already injects `trace_id`/`span_id` into structlog from the active span — the gap is the inbound extract), and the TS leg reads the `traceparent` off the grpc-web response headers and seeds the browser span from it. The GenAI/MCP span conventions (`gen_ai.*`, MCP tool spans) ride the same trace so the agentic surface shares one trace taxonomy. A second tracer or a fresh-root span in any peer is the named cross-language drift defect; the propagation context, not the content seed, is the carrier, so this seam is independent of the `xxhash` parity gate.

[ONE_HEALTH_DEGRADATION_WIRE] [QUEUED]:
- The C#-minted `HealthSnapshot`/`DegradationLevel`/alert wire is the one capability-health frame the web and node peers read to gate UI exposure and command dispatch; the health wire is minted with an explicit TS_PROJECTION cluster C#-side yet no master seam binds it to the TS availability gate that already gates the command face.
- Producer `csharp:Rasm.AppHost/Observability/health#TS_PROJECTION` (mints the wire-neutral `HealthSnapshot`, the five-level `DegradationLevel`, and the alert shapes) to consumers `typescript:projection/evidence/availability#AVAILABILITY_GATE` (folds the level into the dial-time `isEnabled` gate at the one `interchange:CommandGateway` egress) and `typescript:ui/render/dashboard#LIVE_BINDING_DASHBOARD` (renders the level and the retained-capability set).
- Considerations: the C# `WireHealth` projects the registry onto the standard `grpc.health.v1` wire-health service where degraded keeps serving (usable failure), so the TS availability fold consumes the level as the gate input — a `Degraded` capability keeps serving but the browser greys its command and the node tier routes around the unhealthy peer. A second health model, a parallel degradation rail, or a TS-side level re-derivation is the named drift defect; the level is a C#-owned wire fact the peers read, never re-mint. The availability gate already exists in `projection`; the master leg is binding the C# health wire as its upstream input.

[ONE_AGENT_TOOL_PROJECTION] [QUEUED]:
- The C# capability descriptors project to one MCP tool catalog — effect-hints, cost-preview, progress, and resume token — the TS agent runtime decodes once; this is a distinct decode surface beyond the bare SDK-codegen leg `CAPABILITY_SDK_CODEGEN` owns.
- Producer `csharp:Rasm.AppHost/Agent/mcp#TS_PROJECTION` (projects each `CapabilityDescriptor` to a `CommandAIFunction : AIFunction` adopted by `McpServerTool.Create(AIFunction, McpServerToolCreateOptions)` into the MCP `tools/list` surface, plus the dry-run cost, SSE progress, and `Last-Event-ID` resume legs the SDK task primitives carry; the C#-minted named carriers are the `ToolResult` record and the `ReceiptEnvelopeWire<ToolResultWire>` envelope) to consumer `typescript:services/agent/mcp#MCP_TRANSPORT` (schema-decodes the surface into the `McpToolWire`/`CostPreviewWire`/`ProgressNotificationWire`/`ResumeTokenWire` carriers and lifts each into one `@effect/ai` `Tool.make` whose sole agent-facing parameter is the host `payload`, decodes the cost dry-run and the SSE progress stream, and reattaches by the resume cursor), with the Python companion serving the same tools over `python:runtime/transport/serve#SERVE`.
- Considerations: the host JSON Schema is the validating authority, so the transport carries no static parameter mirror of a host descriptor; the `annotate(Tool.Readonly|Destructive|Idempotent, …)` reference annotations transcribe the host effect hints; the C# producer mints to the SDK-native `tools/list`/cost/progress projection (not a bespoke `McpToolWire` struct), the TS side names the decoded shapes, and a second mint of any shape is the drift defect. The `ReceiptEnvelopeWire<ToolResultWire>` structured result decodes through the `interchange` `ReceiptEnvelopeCarrier`. The SDK-codegen leg (`CAPABILITY_SDK_CODEGEN`) and this agent-tool leg share the one descriptor source; the gap is that the agent-tool wire is its own decode surface the master tier owns so the C# host, the TS agent runtime, and the Python companion meet at one tool catalog.

[TRI_LANGUAGE_WIRE_PARITY] [BLOCKED]:
- Live tri-language decode parity: the TS-web leg and the Python companion decode the finalized C# wire — including the CRDT amendment, the generated SDK, the agent-tool catalog, the distributed-trace context, and the health-degradation wire — field-for-field against multi-runtime receipts, with the content and causal seeds reproduced bit-identically.
- Spans every shared owner; blocked on every branch and folder task finalizing first. This is the final cross-language close-out before implementation.

## [2]-[CLOSED]

(none)
