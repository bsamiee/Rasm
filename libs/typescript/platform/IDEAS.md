# [PLATFORM_IDEAS]

The forward pool of higher-order platform concepts, each a card with a bracketed slug leader plus a few bullets — the capability, what it unlocks, and the gap or modern technique it draws on. An idea drives one or more `TASKLOG.md` tasks; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition.

## [1]-[OPEN]

[BROWSER_CAPABILITY_POLICY]: the browser permission/host-capability spine — one `Permissions`-backed capability owner the SPA queries and prompts through, so a feature gates on a real grant state, not an unchecked native call.
- The folder gates affordances on `CapabilityRank` (a health rank) and the `RemoteConfig` flag set (a rollout decision) but owns NO permission-grant axis: the notification, clipboard, geolocation, persistent-storage, and (future) Web-Push surfaces each resolve a `PermissionState` (`granted`/`denied`/`prompt`) the browser owns, and today a host owner reaching `navigator.notification`/`navigator.clipboard`/`navigator.storage.persist()` directly is an ungated native call with no typed grant. One `BrowserCapability` owner under a new `capabilities/` sub-domain composes the admitted `@effect/platform-browser` `Permissions`/`Clipboard`/`Geolocation` capsules behind one closed `CapabilityKind` `Schema.Literal` axis, folds each `PermissionStatus.change` event through `scopedEventStream` into one `PermissionState` `SubscriptionRef` per kind, and exposes one `query`/`request` entrypoint plus the `navigator.storage.persist()`/`estimate()` persistent-storage grant the offline cache depends on for quota durability.
- Unlocks one capability-grant cell `ui` reads to grey a denied affordance exactly as it reads `CapabilityRank.retains`, the persistent-storage grant that keeps the `LocalPersistence` IndexedDB store from eviction under memory pressure, and a `CapabilityRank` health input (a denied-storage grant degrades the offline-only rank), so a permission becomes a first-class host fact rather than a try/catch at each call site.
- Draws on the admitted-but-uncomposed `@effect/platform-browser` `Permissions`/`Clipboard`/`Geolocation` service capsules (an admitted capability no owner exploits is the named coverage gap), the native `Permissions.query`/`PermissionStatus.change` and `StorageManager.persist`/`estimate` APIs, and the `scopedEventStream` grant-change ingress — the same scoped-bridge law the lifecycle and SSE ingresses ride.

[SOCKET_TRANSPORT_MODALITY]: the browser real-time socket leg — one `BrowserSocket`-backed bidirectional transport modality the SSE-only ingress lacks, the seam a `WebTransport`/`WebSocket` raw-byte leg lands on.
- `flag-stream` and `decode-pool` each NAME a `WebTransport` raw-byte growth leg, and `flag-stream` rides a one-directional native `EventSource`, but the folder stands up NO bidirectional socket owner: the admitted `@effect/platform-browser` `BrowserSocket.layerWebSocket` yields a duplex `Socket` no host owner composes, so a live bidirectional channel (a collaborative op-log push-back, a presence heartbeat, a server-initiated command) has no transport modality and would force a second ad-hoc `WebSocket`. One `SocketTransport` owner under a new `realtime/` sub-domain composes `BrowserSocket.layerWebSocket`/`layerWebSocketConstructor` into one duplex `Channel`, the inbound frame stream folding through `Schema.decodeUnknown` exactly as the SSE frame fold does and the outbound write riding the same scoped resource, with the reconnect on the reused `projection` `StreamPolicy` shape.
- Unlocks one bidirectional transport row the `flag-stream` SSE delta, a future presence/heartbeat cell, and the CRDT op-log push-back each compose as a modality rather than a parallel socket, the WebTransport leg `decode-pool`/`flag-stream` name landing as one more transport case on this owner, and a server-initiated command arriving over the same decoded-frame law the inbound wire already obeys.
- Draws on the admitted-but-uncomposed `BrowserSocket.layerWebSocket` duplex `Socket` (the named coverage gap), the Effect `Channel`/`Socket` duplex primitive, the WebTransport modality two existing pages reference, and the `StreamPolicy` reconnect the branch already owns.

[OIDC_REDIRECT_CONTINUITY]: the PKCE round-trip survival leg — the `codeVerifier`/`state`/`returnTo` continuity the authorization redirect crosses, persisted across the full-page navigation `arctic` cannot itself span.
- `auth-session` designs the PKCE acquisition and the silent refresh but is SILENT on the authorization-redirect round-trip: the `arctic` authorization-code-with-PKCE flow mints a `codeVerifier` and a CSRF `state` before a full-page redirect to the IdP and must read them BACK after the IdP redirects to the callback URL — a continuity the in-memory `SubscriptionRef` cannot span because the document reloads, so absent a persisted leg the verifier is lost and the callback validation fails. The acquisition extends to mint `state`/`codeVerifier` through `arctic` `generateState`/`generateCodeVerifier`, persist the `{ state, codeVerifier, returnTo, nonce }` pending-flow record through one `LocalPersistence` named store (single-entry, cleared on completion), match the callback `state` against the persisted value as the CSRF check, exchange the code through `OAuth2Client.validateAuthorizationCode`, and `decodeIdToken` the OIDC claims into the `SessionStatus.Authenticated` subject — the `returnTo` driving the post-login `AppRouter` push.
- Unlocks a correct, CSRF-protected OIDC login that survives the full-page IdP redirect, the `revokeToken` logout that invalidates the refresh token server-side rather than only clearing the cell, and the `OAuth2RequestError.code` RFC-6749 reason folding to the typed `AuthFault` reason so a denied/expired grant is a typed arm not a console error.
- Draws on the admitted `arctic` `OAuth2Client`/`generateState`/`generateCodeVerifier`/`decodeIdToken`/`revokeToken`/`OAuth2RequestError` surface mined to full capability (PKCE acquisition + refresh alone leaves the round-trip, revocation, and id-token decode unexploited), the OAuth2-for-browser-apps PKCE redirect-continuity contract, and the `LocalPersistence` named-store seam.

[LOCAL_STORE_TOPOLOGY]: the browser-local store topology — named per-domain stores and atomic bulk drain over the `idb-keyval` surface the single-key store leaves flat.
- `LocalPersistence` is one flat KeyValueStore where the last-good snapshot, the offline command queue, and the new OIDC pending-flow record all collide in one `keyval` object store, and the offline-queue drain reads-then-clears element-by-element where `idb-keyval` owns an atomic `setMany`/named-store transaction: the admitted `idb-keyval` `createStore` (per-domain `dbName`+`storeName` isolation), `setMany`/`getMany` (all-or-nothing bulk), `entries`/`keys` (cursor drain), and `update` (read-modify-write in one transaction) are unexploited under the `@effect/platform-browser` `KeyValueStore` abstraction. `LocalPersistence` gains one `StoreDomain` `Schema.Literal` axis (`snapshot`/`offline-queue`/`auth-flow`/`viewpoint-cache`) each mapped to its own named backing store, and the offline-queue drain becomes one atomic `entries`-then-`clear` transaction rather than a per-element read-clear loop a crash can tear.
- Unlocks per-domain store isolation so a `snapshot` clear never evicts the offline queue, an atomic offline-queue drain a mid-drain crash cannot half-apply, and the `auth-flow` pending record and the new `viewpoint-cache` residency-manifest store each landing as one `StoreDomain` row rather than a key-prefix convention.
- Draws on the admitted `idb-keyval` bulk/cursor/named-store surface mined to full capability (`createStore`/`setMany`/`getMany`/`entries`/`keys`/`update` unexploited), the IndexedDB per-store transaction-isolation law, and the `StoreDomain` vocabulary-as-data pattern.

[TRACE_CONTINUATION_EDGE]: the W3C trace-context continuation — the `traceparent` the browser injects outbound and the C# parent it extracts inbound, so a browser interaction joins the one cross-runtime distributed trace rather than minting a disconnected root.
- The `metric-registry` `WebSdk` leg binds a `ParentBasedSampler` over a `TraceIdRatioBasedSampler` root and ships spans to the collector, but it propagates NO `traceparent` on the outbound grpc-web/HTTP call and extracts NO C#-minted parent context inbound — so a browser INP span, the C# `ActivitySource` solve span, and the Python tessellation span land as three disconnected root traces at the collector. `MetricRegistry` registers the W3C `W3CTraceContextPropagator`/`W3CBaggagePropagator` composite as the `Propagators.DefaultTextMapPropagator`, the `interchange` `WireTransport` interceptor injects the active span's `traceparent`/`tracestate` onto every outbound request header, and an inbound C#-minted parent context seeds the browser span rather than a fresh root, so the `crash.report`/`web.vital.breach`/`session.replay` spans the folder ships hang under the one cross-runtime trace id.
- Unlocks one correlated distributed trace spanning the browser interaction, the C# solve, and the Python tessellation under one trace id, so a regression reads end-to-end as one flame graph rather than three collector silos, and the existing `replayWindow` span correlation rides the same one trace.
- Draws on the cross-`libs/` `ONE_DISTRIBUTED_TRACE` seam (this folder is the named `typescript:platform/observability/metric-registry#METRIC_REGISTRY` consumer that today mints an independent root), the W3C Trace Context propagation standard, and the `@effect/opentelemetry` `WebSdk` propagator registration the C# spine's `TraceContextPropagator`/`BaggagePropagator` composite pairs with.

## [2]-[CLOSED]

[RUNTIME_STATE_SPINE]: REALIZED — the runtime-state spine landed as `runtime-composition/app-lifecycle.md` (the closed `Phase` `Data.TaggedEnum` over the merged `visibilitychange`/`pagehide`/`beforeunload` fold), `capability-rank.md` (the ordered `Rank` vocabulary under escalate-fast/recover-slow hysteresis, PROJECTS capability), `connectivity/connectivity.md` (the one `online` cell + redial edge + native `SyncManager` wake read by four concerns), and `runtime-composition/scoped-event-stream.md` (the one generic non-`WindowEventMap` `Stream.asyncScoped` bridge); the three private `visibilitychange` ingresses collapsed onto the `AppLifecycle` `transitions` axis.

[NAVIGATION_API_ROUTER]: REALIZED — `routing/app-router.md` re-founds `AppRouter` on the Baseline Navigation API: the `navigate`-event scoped interception over `window.navigation`, `event.intercept` with `scroll: "after-transition"`, and the `NavigateEvent` `signal` pending/abort state replaced the `popstate` `SubscriptionRef` + manual `pushState`/`replaceState` + scroll-offset map; ZERO router package admitted.

[VIEW_TRANSITION_FOLD]: REALIZED — `routing/app-router.md#VIEW_TRANSITION` `viewTransition` wraps the guard-admitted location commit as one `Effect.acquireRelease` over `document.startViewTransition` (the commit runs in the update callback, `ready`/`updateCallbackDone`/`finished` convert through `Effect.tryPromise`, `skipTransition()` on scope exit), gated by one `window.matchMedia("(prefers-reduced-motion: reduce)")` row; ZERO animation dependency.

[SOFT_NAV_VITALS]: REALIZED — `web-vitals/performance-budget.md` hoists the CLS/INP accumulators and the `(vital, route)` dedupe to service-level `Ref`s, resets them on each `AppRouter` `location.changes` commit, and flushes terminal CLS/INP to the `MetricRegistry` gauges plus a final breach check on the `AppLifecycle` `transitions` `Hidden`/`Draining` edge (never a private `visibilitychange` ingress).

[TRACE_CORRELATED_REPLAY]: REALIZED — `session-replay/session-recorder.md` owns the flag-gated (`RemoteConfig` `session-replay`) sampled (`RuntimeConfig.replaySampleRate`) `MutationObserver`/interaction recorder over `scopedEventStream`, the redaction-at-capture fold, the bounded `Ref<Chunk<RecordedEvent>>` ring, and the `windowId` `SubscriptionRef` `crash.report`/`web.vital.breach` spans annotate as the one `session.replay`-correlated attribute; no vendor replay SDK, no third telemetry path.

[STREAMING_FLAG_DELIVERY]: REALIZED — `feature-flags/flag-stream.md` lifts the native `EventSource` SSE channel through `scopedEventStream` into a `FlagDelta` `Schema.decodeUnknown` fold patching the one `RemoteConfig` `FlagSet` `SubscriptionRef` in place; the `remote-config.md` fixed-interval poll demoted to the reconnect/backfill fallback under one `Schedule`, the two feeding one fold. RE-OPENED as its own SSE-parser-hardening task leg: the SSE ingress hand-rolls the native `EventSource` parser where the admitted `@effect/experimental` `Sse.makeParser`/`Sse.Retry.lastEventId` (the `services` `agent/mcp-transport` confirmed consumer of the same `Retry.lastEventId` reattach cursor) owns the SSE-frame decode and the last-event-id reattach — the parser is a transport-decode mechanic, not a second flag source.

[OPENFEATURE_PROVIDER_SHAPE]: DROPPED — a thin `resolveBooleanEvaluation`-style projection of the existing total `Match` dispatch is a task-sized shaping anchored on one named external standard, below the higher-order-folder-idea bar; the OpenFeature reason/variant alignment, if it earns admission, is one growth row on `remote-config`'s existing `evaluate`, not a folder idea.
