# [DOSSIER_PROCESSING] — RASM-CS-GEOMETRY robust-core Processing lane

Read-only survey, lane = processing. Scope re-verified ON DISK: the six ROBUST-CORE Processing pages (`repair`, `receipts`, `decimate`, `flatten`, `solver`, `fit`) fully; the seven landed Processing pages (`intent`, `sample`, `extract`, `flow`, `register`, `geodesics`, `segment`) as standing-law seam context; member claims verified against the landed fences (`intent.md`, `mesh.md`, `fields.md`, `reconstruct.md`, `cloud.md`, `register.md`) per `[LANDED_KERNEL_LAW]` (a). Anchors are `libs/csharp/Rasm/.planning/`-relative. Stance: hostile; every register row is an initial pointer re-proved on disk.

Headline: every assigned register row HOLDS on disk (zero REFUTED, zero anchor drift beyond ±2 lines). Beyond the register the lane carries FOUR additional confirmed defects the brief did not enumerate, three of them severe: (1) `decimate`'s exact-`Orient3D` collapse-flip guard is a **vacuous no-op** — the page's headline robustness claim never fires; (2) `repair`+`receipts` compose the landed `TopologyReceipt.Project` triple which is **partial (fails when `Genus=None`)** — the heal rail rails out on exactly the non-manifold / boundary / odd-numerator meshes it exists to repair; (3) `decimate` composes a **phantom member** `ScalarField.IsoSurfaceDetailed` (real owner is the static `IsoSurface.Detailed` in `reconstruct.md`); (4) `decimate.md:459` carries a second `QueryResult` hard-cast of the same E10 breach class the register only anchored in `repair`.

---

## [00] REGISTER VERDICTS (Processing-anchored E-rows)

| Row | Anchor(s) | Verdict | Evidence on disk |
|---|---|---|---|
| E4 receipts Converged inversion | `receipts.md:65,99` | HOLD | `:65` mints `new BooleanReceipt(bo.Op, AssetGated: false, ...)`; `:99` `booleanReceipt: static b => b.AssetGated`. Successful managed boolean (`AssetGated:false`) ⇒ `Converged` returns false ⇒ every heal session with a successful boolean reports non-converged. |
| E4 decimate boundary freeze | `decimate.md:310` | HOLD | `LinkCondition` `:310 return shared == 2;`. Boundary edges share exactly 1 opposite vertex ⇒ frozen ⇒ `TargetFraction` unreachable on open meshes ⇒ spurious `DecimationFault`. |
| E4 decimate O(F²) scans | `decimate.md:293,316-360` unfed by `:87` | HOLD | `FanFaces` `:316-320`, `CollapsedFaces` `:340-345`, `NoAdmissibleCollapse` `:349-358` each `foreach ... in edit.Faces`. `:87 Adjacency` is vertex→vertex only; no vertex→incident-face index exists ⇒ every validity check / collapse is O(F) ⇒ O(F²) drain. |
| E4 flatten inverted Dirichlet | `flatten.md:359,364` (`:369` scatter) | HOLD | `:359` adds `Policy.MassShift` (1e-9) to EVERY diagonal; `:364` `rhs[loop[k]] = boundaryValues[k] / Policy.MassShift` = value·1e9. No large penalty on pinned rows ⇒ interior solve corrupted; `Scatter` `:368-372` overwrites only the boundary afterward. |
| E4 flatten dense LSCM vs sparse prose | `flatten.md:377,173` vs `:3` | HOLD | `:3` "smallest-singular-vector of a **sparse** least-squares operator"; `:377 new double[(2*n)*(2*n)]` dense; `:173 conformal.DecomposeSvd()` full dense SVD ⇒ O(n³), non-scaling. |
| E7 repair crossing copies | `repair.md:346-372` | HOLD | `TriangleCrossPoint` `:346`, `EdgesCrossTriangle` `:349`, `PlaneCrossPoint` `:361`, `InTriangle` `:367` — one of the three inline copies `intersect.md:388` declares to collapse; still live. `PlaneCrossPoint` rounds the crossing to `double`. |
| E7/E9 fit LM claim vs re-impl | `fit.md:740` vs `:575-600` vs `solver.md:303-348` | HOLD | `:740` "reusing the `Processing/solver#CONSTRAINT_SOLVER` λ-ladder"; `:575-601` is a full RE-IMPLEMENTATION (`Iterate`/`Step`/`LinearizeNormal`/`PackedIndex`/`ApplyDelta`) not a composition of `ConstraintSolver.Solve`. |
| E9 LM ×2 | `solver.md:303-348` vs `fit.md:575-601` | HOLD | Two structurally-identical LM loops. `solver` is the policy-laddered owner; `fit` hardcodes `1e-3` seed (`:575`), `×10.0` climb (`:598,600`), `1e12` ceiling (`:579`), and `RefineState` (`:701`) carries **no λ** — λ resets to 1e-3 every outer `Iterate` (worse than "no carry-down"). |
| E9 decimate composes the solve rail | `decimate.md:18` | HOLD | `:18` + `OptimalPosition` `:363-369` compose `SymmetricMatrix.Of(...).DecomposeCholesky().SolveDetailed(...)` — the compliant exemplar V7 wants `solver`/`fit`/`flatten` to match. |
| E9 receipts BooleanReceipt name | `receipts.md:44` | HOLD | `:44 BooleanReceipt(BooleanOp Op, bool AssetGated, ManifoldStatus Before, ManifoldStatus After, Set<int> SelectedFaces)` — name collides with `arrangement.md:115` (different shape/namespace). |
| E9 flatten dense-LA framing | `flatten.md:18` | HOLD (framing) | `:18` states a "wrapper discipline" naming `MathNet.Numerics`/`CSparse` directly, parallel to `solver`/`decimate`/`register`'s "the ONE `matrix.md` access path" phrasing — two vocabularies for one law. NOTE: flatten's CODE is compliant (routes `CholeskySparse.Of`/`SparseMatrix`/`Matrix.DecomposeSvd`); no raw provider call exists. Defect is consolidation-level, not a code bug. |
| E10 repair hard cast | `repair.md:243` | HOLD | `:243 foreach (int g in ((QueryResult.Hits)hits).Ids...)` — unguarded downcast of a union result ⇒ `InvalidCastException` escape on the rail. |
| E11 fit FitStore unwired | `fit.md:246-253` | HOLD | `FitStore` (`:246-255`, `Empty` factory `:253`) is never instantiated; the live consensus carrier is `Candidate` (`:342,351,697`). Dead SoA + prose/density-bar (`:730,735`) misdescribe it as "the shared consensus memory". |
| E11 flatten dead ChartStore carriers | `flatten.md:81` (`:72,73`) | HOLD | `Kill` `:81`, `Dead` field `:72`, `FreeList` `:73` — `Kill` is never called; islands are formed by BFS labelling (`Islands` `:262-279`), never by vertex kill. Dead. |
| E11 flatten wrong-operator FactorNonZeros | `flatten.md:330` | HOLD | `:330 public int FactorNonZeros => Calculus.D1.NonZeros;` — reports the `D1` (edge→face curl) structural nnz, not the Cholesky FACTOR fill the receipt documents (`:17`). Doubly wrong: `D1` is not even in the stiffness path (`L = D0ᵀ·diag(Star1)·D0`). |

No DRIFT, no REFUTED. Minor anchor note: E9/E7 `solver.md:303-346` — the `Step` fold body extends to `:348` (accept/reject λ ladder at `:344-347`); the register's `303-346` covers the engine core, treat as exact.

---

## [01] PER-PAGE DOSSIERS

### repair.md — `Rasm.Geometry.Healing` — grade now 6 / target 9.5

Verdict: REBUILD (register-confirmed defects + one severe unflagged cross-page defect + V6 donor excision + V5 boolean re-home + V4 crossing collapse). The predicate-gated author-kernel repair algebra is structurally sound (`HealOp` `[Union]`, one `Repair` fold, `Standard` order at `:388-395` matching its own prose); the rot is in the kernels and the topology seam.

Defects (anchored):
- **[SEVERE, unflagged] Topology projection partiality kills the heal rail on its own input class.** `Heal.Repair` (`:379-386`) binds `from before in Topology(state.Edit, ...)` (`:382`) and `from after in Topology(next, ...)` (`:384`); `Topology` (`:397-401`) ends in `intent.Project<(int Euler, int Genus, int BoundaryComponents)>(tolerance)`. The landed `TopologyReceipt.Project` (`mesh.md:202-205`) exposes ONLY that triple and returns `Fin.Fail(key.InvalidResult())` when `self.Genus` is `None`. `Genus` is `None` whenever `!(manifold && oriented && numerator≥0 && numerator%2==0)` (`mesh.md:548,555`) — i.e. for **non-manifold, non-oriented, boundaried (odd numerator, e.g. a disk), or odd-Euler** meshes. Consequence: `Heal.Repair` fails at the FIRST before-topology projection for exactly the defective-mesh classes `ManifoldRepair`/`GapClose`/`DegenerateCollapse` exist to fix; the healer can only produce receipts for already-clean closed orientable manifolds (which need no heal). The receipts prose (`receipts.md:16`) even codifies the hard-fail ("binds the projection on the `Fin` rail … never a swallowed default"). Fix: the heal rail needs a TOTAL before/after status that tolerates `Genus=None` — either a landed `TopologyReceipt` `ProjectionRow` extension exposing the raw Euler/Boundary fields without the genus gate (a `mesh.md` Growth item, `[LANDED_KERNEL_LAW]` (b)/(c)), or `ManifoldStatus` composes the un-gated fields directly. This is the load-bearing repair the page needs before any of E4/E7/E10 matter.
- **[E10] Unguarded union downcast** `:243 ((QueryResult.Hits)hits)` — a range query returning any non-`Hits` arm throws; route the `QueryResult` union on the `Fin`/match rail.
- **[E7] Crossing-kernel copies** `:346-372` (`TriangleCrossPoint`/`EdgesCrossTriangle`/`PlaneCrossPoint`/`InTriangle`) — the fourth live copy of the exact-crossing lattice `intersect.md:388` owns; `PlaneCrossPoint` `:361-365` rounds the crossing to `double`. Re-route through `Intersection.Apply`/`Crossing` (V4).
- **[axis-1] `SelfIntersectResolve` does not resolve self-intersection** (`:234-262`). It treats a triangle–triangle intersection (a SEGMENT) as a single point (`TriangleCrossPoint` returns one `Option<Point3d>`), fans only face `g` around it (`patched[g]`, `spawned` `:248-250`), leaves face `f` untouched, and `patched.ContainsKey(g)` (`:243`) drops every crossing after the first on a given face. The penetration survives; only a vertex is added. V9's "real CDT re-mesh through the substrate" is the correct rebuild; the current body is naive beyond "not CDT".
- **[axis-2] 2N topology recompute** `:382-385` — each op computes `before=Topology(state.Edit)` and `after=Topology(next)`; the fold never carries `after[n-1]` forward as `before[n]`, and `Topology` rebuilds a full native `Mesh` via `edit.ToSpace` each call. Thread the status forward (`before[n]=after[n-1]`); halves the mesh rebuilds.
- **[axis-1, secondary] `GapClose` loses boundary orientation** (`:186-207`). `Boundaries` (`:309-313`) returns boundary edges as index-sorted `(min,max)` keys (direction lost via `EdgeIncidence` `:320`), so the anti-parallel match `p0~q1 && p1~q0` (`:197`) pairs on arbitrary-oriented endpoints and the bridge triangles (`:198-199`) are inconsistently wound. `OrientNormals` runs after in `Standard`, but the pairing predicate itself is unreliable.
- **[V6] `MeshEdit` donor block** `:76-102` — the de-facto shared mutable working set consumed cross-page (decimate via `using Rasm.Geometry.Healing` at `decimate.md:33`; receipts unqualified `MeshEdit result` at `receipts.md:56`). Extract to the Meshing substrate page beside `mesh.md`. Note it is currently an IMMUTABLE record (persistent `Arr<T>`, `with`-copy per op) — every kernel rebuilds full vertex/face arrays (O(V+F) per op), which is the arena-mutability motion V6/`[SEAM_AND_RAIL_LAW]` targets.
- **[V5] `BooleanArrangement`** `:288-292` composes `Arrangement.Apply(MeshBoolean, ...)` — the `HealOp.Boolean` case + `BooleanOp` smart-enum (`:57-62`) re-home to `arrangement.md` (kills the lower-stratum-consumes-upper shape and the repair⇄arrangement cycle).
- **[catalog] `api-rhino.md:134` overclaim** — repair composes only `mesh.RebuildNormals()` (`:89`) at the working-set boundary; welding/hole-filling/orientation are author-kernel. The catalog claim that repair composes the Rhino heal surface corrects to `RebuildNormals`-only.

Charter as it should be: the predicate-gated heal fold over the ONE `HealOp` union, minting typed receipts into the `IValidityEvidence` fold (killing the standalone `Converged`), with a TOTAL topology status (Genus-tolerant), `MeshEdit` sourced from the extracted Meshing-substrate arena, crossings through `Intersection.Apply`, `SelfIntersectResolve` a real substrate CDT re-mesh, boolean re-homed to `arrangement`, and `before[n]=after[n-1]` threading. Preserve the `HealOp`/`Standard` shape, the exact `DegenerateCollapse`/`DominantAxis` sliver logic (`:171-183` — genuinely sound), the `WeldDuplicates` union-by-smallest-index idempotence (`:140-168`).

### receipts.md — (NO namespace declared) — grade now 7 / target 9.5

Verdict: REBUILD (namespace mint per V1, Converged fix, BooleanReceipt collision, validity-fold adoption). The typed `RebuildReceipt` `[Union]` (7 cases, one per `HealOp`) and the `ManifoldStatus`/`HealSession`/`RebuildLog` fold are transcription-complete and the `Affected` re-anchor seed (`:68-77`) is well-shaped.

Defects (anchored):
- **[E4] `Converged` inversion** `:91-99`. `booleanReceipt: static b => b.AssetGated` reports converged ONLY when the boolean was ASSET-GATED (blocked); a successful managed boolean mints `AssetGated:false` (`:65`) ⇒ non-converged. Category error compounding it: for every OTHER case `Converged` returns `true` unconditionally or `Improved` (`:96`); testing the tier-3 gate flag as a convergence signal is itself wrong — a boolean's convergence is its topological success, not its gate state. Fix `:99` to `!b.AssetGated` AND reconsider whether the gate flag belongs in the convergence fold at all.
- **[V1] No namespace declaration.** The fence (`:22-107`) has no `namespace`/`using` prelude — one of the four namespace-less pages `[V1]` names. Its types (`ManifoldStatus`, `RebuildReceipt`, `HealSession`, `RebuildLog`) are consumed by `repair` unqualified, implying `Rasm.Geometry.Healing`. Ratify the declaration.
- **[E9/V5] `BooleanReceipt` name collision** `:44` vs `arrangement.md:115` — same name, different shape/namespace. Resolve to one type or two names in the same motion as the V5 boolean re-home.
- **[dual-paradigm] Hand-rolled `Converged`/`Improved` beside the substrate validity fold.** `HealSession.Converged` (`:91`) and `ManifoldStatus.Improved` (`:28`) are per-receipt `IsValid`-style guards — exactly the dual paradigm `[SEAM_AND_RAIL_LAW]` forbids; receipts should register into the `IValidityEvidence` fold (`Domain/validation.md`).
- Downstream of the repair topology-partiality defect: `ManifoldStatus.Of` (`:25`) and the `manifoldReceipt` `Improved` arm (`:96`) never receive valid before/after status for non-manifold inputs (the projection fails upstream). `manifoldRepair`'s `Improved` heuristic is additionally questionable — edge-splitting INCREASES boundary components, so `BoundaryComponents < before` (`:29`) is false for a successful split.

Charter as it should be: the ONE typed `RebuildReceipt` union in a ratified `Rasm.Geometry.Naming`/`Healing` namespace, receipts registering into the validity fold (no standalone `Converged`), `BooleanReceipt` name deconflicted, and the composed status carried from a Genus-tolerant total topology seam. The `[Union]` shape, `Of` mint discrimination, `Affected` seed, and `ToLog` fold are correct — preserve them.

### decimate.md — `Rasm.Geometry.Simplification` — grade now 7 / target 9.5

Verdict: REBUILD (register O(F²)+boundary-freeze + one SEVERE unflagged no-op + phantom member + second E10 cast). Structurally the QEM/`SimplifyOp` union, `ddouble` quadric accumulation (`:121-144`), lazy version-skip queue, and `matrix.md`-routed `OptimalPosition` (`:363-369`, the E9 exemplar) are strong. The correctness core is broken.

Defects (anchored):
- **[SEVERE, unflagged] The exact-`Orient3D` collapse-flip guard is a vacuous no-op.** `CollapseValid` `:297-301`: `before = Orient3D(oa,ob,oc, oa + cross(ob-oa,oc-oa))` and `after = Orient3D(pa,pb,pc, pa + cross(pb-pa,pc-pa))`. With the fourth point = face + its own normal, `Orient3D(a,b,c,a+n) = sign((b-a)·((c-a)×n)) = sign(|n|²) = Positive` for every non-degenerate triangle (Zero only when the triangle is degenerate). So `before` and `after` are ALWAYS `Positive`; the flip test `before != Sign.Zero && after != before` (`:301`) can never fire. The page's entire headline claim ("every collapse admitted ONLY when the exact `Orient3D` sign of each incident face is preserved", `:3,5,16,558`) is inert — the guard rejects only degeneracy (`after == Sign.Zero`), never an orientation flip. A correct guard compares the face NORMAL direction pre/post (e.g. `sign(n_before · n_after)`, or `Orient3D` of the moved apex against the original supporting plane).
- **[E4] Boundary freeze** `LinkCondition:310 return shared == 2;` — boundary edges (1 shared opposite vertex) never collapse ⇒ open meshes stall below budget ⇒ spurious `DecimationFault`. Admit `shared == 1` for boundary edges (V9).
- **[E4] O(F²) drain** — `FanFaces` `:316-320`, `CollapsedFaces` `:340-345`, `NoAdmissibleCollapse` `:349-358` scan `edit.Faces` per candidate; `edit.Faces` is the ORIGINAL immutable array (never compacted; liveness via `store.Valid`). The maintained `Adjacency` (`:87`) is vertex→vertex only. Extend it to a vertex→incident-face index so the fan walk is O(1) (V9, O(F log F) drain).
- **[phantom] `ScalarField.IsoSurfaceDetailed` does not exist.** `Voxelize` `:414-417` calls `field.IsoSurfaceDetailed(bounds, policy.VoxelResolution, maxRootSteps: 8, context: tolerance)` as an INSTANCE method on `ScalarField`. `fields.md:228` explicitly disclaims iso-surfacing ("iso-surface extraction is `Meshing/reconstruct.md`'s — this page samples, never meshes"). The real member is the STATIC `IsoSurface.Detailed(ScalarField field, BoundingBox bounds, int resolution, IsoSurfacePolicy policy, Context context, Op? key)` (`reconstruct.md:609`), returning `Fin<IsoSurfaceResult>` with `IsoSurfaceResult(Mesh Mesh, IsoSurfaceReceipt Receipt)` (`reconstruct.md:438`). decimate's call is triple-wrong: instance-vs-static, name (`IsoSurfaceDetailed` vs `Detailed`), and signature (`maxRootSteps: 8` bare int vs the `IsoSurfacePolicy(Dimension MaxRootSteps, long MaxCells)` param, `reconstruct.md:222`). Prose repeats the phantom at `:3,5,16,18,20,553,560`. The `.Mesh` access (`:417`) is valid once the real `IsoSurface.Detailed` is composed.
- **[E10-class, unflagged] Second unguarded union downcast** `:459 var hit = (QueryResult.Nearest)index.Query(new SpatialQuery.Nearest(sample, 1));` — same breach class as `repair.md:243`; a non-`Nearest` result throws. Route on the `Fin`/match rail.
- **[ddouble narrowing] Cost key readout** `Quadric.Evaluate:137-143` narrows to `double` at the queue key — consistent with `README:86`. Sound.

Charter as it should be: the ONE QEM `SimplifyOp` fold with a REAL orientation-flip guard (normal-direction sign, not `Orient3D(face, face+normal)`), boundary-collapse admitted, a vertex→incident-face incidence index feeding an O(F log F) drain, the `VoxelRemesh` resample composing the real `IsoSurface.Detailed` seam, `QueryResult` on the `Fin` rail, and `MeshEdit` from the extracted arena. Preserve the `ddouble` quadric algebra, the lazy version-skip queue, the `matrix.md`-routed `OptimalPosition`, and the BVH Hausdorff bound (`Hausdorff`/`DistanceToFace` `:452-502` are correct).

### flatten.md — `Rasm.Geometry.Parameterization` — grade now 6 / target 9.5

Verdict: REBUILD (inverted pin, dense LSCM, O(F²) ARAP, dead carriers, wrong FactorNonZeros). The DEC-composition shape over the landed `MeshAdjointSnapshot.Of`/`DiscreteCalculus` public handle is the correct floor (preserve); the solves are broken.

Defects (anchored):
- **[E4] Inverted Dirichlet pin** `:359,364`. `StiffnessTriplets` adds `Policy.MassShift` (1e-9) to EVERY diagonal (`:359`), and `PinnedRhs` sets `rhs[loop[k]] = boundaryValues[k] / Policy.MassShift` = value·1e9 (`:364`). No large penalty on pinned rows: the pinned-row equation `(L_vv + 1e-9)·x_v + Σ L_vw·x_w = value·1e9` drives `x_v ≈ value·1e9` (huge), pulling the interior toward garbage through the coupling; `Scatter` (`:368-372`) overwrites only the boundary afterward, so the INTERIOR harmonic/BFF map is corrupted. Fix: large penalty P on pinned diagonal with `rhs = P·value`, or the eliminate-boundary-rows formulation.
- **[E4] Dense LSCM contradicts sparse prose** `:377,173` vs `:3`. `ConformalOperator` allocates `new double[(2n)*(2n)]` (`:377`) and `FlattenLscm` runs full `conformal.DecomposeSvd()` (`:173`) — O(n³) dense, the exact opposite of the `:3` "sparse least-squares operator" claim. Assemble the conformal energy SPARSE and take the smallest eigenpair via the `matrix.md` sparse owners.
- **[axis-1+axis-2, V9] ARAP `IndexOf` O(F²) + `return 0` on miss** `:482-485`. `AccumulateRotated` (`:473-480`) calls `FacePoints(IndexOf(i,j,k))` to RE-FIND a face index its caller `RotatedGradient` (`:393-401`) already holds as `f`. `IndexOf` scans all faces (O(F)) ⇒ O(F²) global assembly, and returns face 0 on any miss (e.g. a rotated vertex order) ⇒ wrong geometry silently. Pass the in-scope `f`.
- **[E11] Wrong-operator `FactorNonZeros`** `:330 => Calculus.D1.NonZeros;` — reports the `D1` curl-operator structural nnz, not the Cholesky factor fill the receipt documents; `D1` is not even in the stiffness path. Report the actual `CholeskySparse` factor fill via the `SparseMatrix.Solve` rail.
- **[E11] Dead `ChartStore` carriers** `:81,72,73` — `Kill`/`Dead`/`FreeList` never exercised (islands via BFS labelling `:262-279`). Wire or delete.
- **[axis-1, secondary] `OrderLoop` boundary loop is fragile** `:540-548`. `BoundaryOf` (`:527-537`) yields index-sorted `(min,max)` boundary edges; `OrderLoop` builds `next[u]=v` from them (`:543`), so a vertex that is the `min` of two boundary edges has its `next` overwritten ⇒ the harmonic/BFF boundary loop can be partial/broken. Compounds the Dirichlet defect.

Charter as it should be: the ONE `ParamOp` fold over the landed DEC handle with a CORRECT Dirichlet penalty, a sparse LSCM eigenpair through `matrix.md`, an O(F) ARAP global assembly (in-scope `f`), an honest `FactorNonZeros`, dead carriers removed, and a developable seam to the `[V2]` parametric tier. Preserve the `MeshAdjointSnapshot.Of`/`DiscreteCalculus` composition, the per-face Jacobian-SVD distortion scoring (`:236-249`), the exact `Orient2D` flip guard (`:246`), and the seam-cut island walk.

### solver.md — `Rasm.Geometry.Constraints` — grade now 8 / target 9.5

Verdict: KEEP-and-GENERALIZE (V7 one-LM owner; phantom namespace kill). The cleanest robust-core Processing page: analytic residual+Jacobian `Constraint` union, packed-upper `JᵀJ` scatter, `SolveReceipt`-gated Cholesky step, witness-rank DOF refinement, bounded reject chain. It is the LM engine V7 wants `fit`/`decimate`/`flatten` to share.

Defects (anchored):
- **[V1/E3/E5] Phantom `Rasm.Geometry.Topology` namespace** `:13` — the owner prose homes `EntityKind` in "the sibling `Rasm.Geometry.Topology` namespace", which collides with the landed `Rasm.Domain.Topology` smart enum (`Domain/normalization.md`, props global-usings-injected). Re-anchor to the ratified naming namespace (ruled `Rasm.Geometry.Naming`).
- **[V7] Engine is private and `Constraint`-coupled.** `Iterate`/`Step` (`:303-348`) fold over `system.Constraints` via `LinearizeNormal` (`:352`, called at `:310`) and `LinearizeDense` (`:369`, called at `:262,319`). Generalize to a residual+Jacobian functor over the existing `SolvePolicy` λ-ladder (`:77-87`) so `fit`'s `Refine` instantiates it rather than re-implementing. The `SolvePolicy` record already carries `LambdaUp`/`LambdaDown`/`StepFloor`/`InitialLambda`/`MaxIterations` — the policy exists; only the residual source needs abstracting.
- **[dual-paradigm] Bare `Fin` entries + hand-rolled guards** — like every settled fence, solver rides bare `Fin<Solution>`/`Fin<LmResult>` with no `Op? key` threading or `IValidityEvidence` registration (the `[SEAM_AND_RAIL_LAW]` substrate adoption).

Non-defects (preserve): the `LambdaCeiling` singular guard (`:318-322`), the `SolveReceipt` all-finite gate driving the λ-climb (`:329-334`), `WitnessAnalyze`/`ConsistentAtWitness` (`:261-283`), the accept-only ladder (no divergence case), the bounded reject chain (`[LM_CONVERGENCE]` research `:429`). The `ConstraintSolveReceipt` is deliberately name-distinct from the composed `Rasm.Vectors` `SolveReceipt` (`:5`) — correct, not a collision.

Charter as it should be: the ONE generalized damped-Gauss-Newton owner — a residual+Jacobian functor over `SolvePolicy` — in a ratified namespace, with the `ddouble` Σd² objective (`README:86`) landing here, adopting the substrate Op-threading + validity fold. Likely lifts into a `Solving/` folder with `fit` (V1/V7 one-LM family).

### fit.md — `Rasm.Geometry.Fitting` — grade now 8 / target 9.5

Verdict: REBUILD-to-compose (V7 LM instantiation, FitStore, BVH wrong-owner, Line chart, transcription artifact). Strong MLESAC/PROSAC efficient-RANSAC sampler and analytic orthogonal-distance `Gradient` arm; the refine duplicates solver and the spatial lane is wrong.

Defects (anchored):
- **[E7/E9/V7] Re-implements the solver LM instead of composing it.** `:740` claims reuse; `Refine`/`Iterate`/`Step`/`LinearizeNormal`/`PackedIndex`/`ApplyDelta` (`:560-627,686-690`) are a full copy of `solver.md:303-397`. Hardcoded `1e-3` seed (`:575`), `×10.0` climb (`:598,600`), `1e12` ceiling (`:579`); `RefineState` (`:701`) carries no λ, so the accept branch (`:597`) re-enters `Iterate`→`Step(...,1e-3)` and λ RESETS every outer iteration — a stateless ladder worse than solver's carry-down (`solver.md:344`). Instantiate the generalized `ConstraintSolver` engine.
- **[E11] `FitStore` dead** `:246-255`. Never instantiated; `Candidate` (`:697`) is the live consensus carrier. The density bar (`:730`) and prose (`:3,13,17,735`) misdescribe `FitStore` as "the shared consensus memory". Delete or wire.
- **[E16/README:93/V9, unflagged severity] BVH built, never queried; wrong owner.** `Consensus` (`:331-333`) builds `SpatialIndex.Build(Bvh, ...)` and threads `index` into `Draw` (`:336`), but `Draw` (`:337-357`) and `Score` (`:360-371`) never reference it — `Score` is a full-cloud reduce (`for i in cloud.Length`). So the BVH is O(n log n) dead work. `README:93` rules the kd-tree lane feeds fit's MLESAC; the landed `register.md` already composes the `neighbors` substrate (`register.md:562,582`) — fit should ride `neighbors.md` (`Supercluster.KDTree.Net`) for the seeded local draw / range prefilter, not build an unqueried `Spatial/index` BVH. register is the exemplar.
- **[V9] Line chart degenerate for near-vertical lines.** `Pack` (`:650`) parametrizes a line with 4 params `(From.X, From.Y, atan2(dir), acos(dir.Z))` — `From.Z` is NOT a parameter; `Unpack` (`:661`) freezes the anchor at the seed `ln.Axis.From.Z`. A near-vertical line's perpendicular offset lives mostly in Z, which the Z=const anchor cannot express ⇒ biased fit. Also `FitKind.Line.MinimalSamples=3` (`:52`) but `MinimalLine` (`:477-483`) uses only 2 points ⇒ the adaptive budget `Pow(fraction, 3)` is over-conservative and `draw[2]` is ignored. Parametrize the anchor as the foot-of-perpendicular (as `PackPlane` `:665-669` does for the plane) and set `MinimalSamples=2`.
- **[transcription] Broken Packages line** `:18` — a dangling `` ` solve, the one dense-linear boundary the constraint iterate sanctions), `` fragment (orphaned backtick + clause) mid-enumeration. Editing artifact; repair the prose.

Charter as it should be: the ONE `FitOp` fold composing (not copying) the generalized `ConstraintSolver` LM, riding the landed `neighbors.md` kd-tree for the seeded draw / prefilter, `FitStore` deleted, the Line chart de-degenerated (foot-of-perpendicular anchor, `MinimalSamples=2`), the `ddouble` Σd² objective, and `FitReceipt` feeding the `Rasm.Bim` reconstruction. Preserve the MLESAC/PROSAC truncated-cost sampler (`:330-421`), the adaptive budget, the six closed-form minimal solvers, and the analytic per-kind `Distance`/`Gradient` arms (`:106-244`).

---

## [02] LANDED PROCESSING PAGES — standing-law seam context (verified, not re-litigated)

- **`intent.md`** — `VectorIntent.Topology(MeshSpace)` factory REAL at `:214` (`TopologyCase` `:71`); `VectorIntent.Cloud(cloud, VectorCloudMetric, ...)` REAL at `:109`; `VectorIntent.Features(space, policy)` REAL. The receipts/repair/decimate compositions of these are genuine landed seams, not phantoms. The 33-case `VectorIntent` union is the kernel consumer rail; the robust-core pages compose it correctly through `Project<TOut>`.
- **`register.md`** (namespace `Rasm.Vectors`, landed) — the EXEMPLAR for two robust-core laws: it composes the `neighbors` kNN substrate for ICP correspondence (`:562,582`) and routes ALL dense solves through the `matrix` owners ("MathNet.Numerics only through the `matrix` owners", `:18`). fit's unqueried `Spatial/index` BVH and flatten's "wrapper discipline" framing are the deviations register already resolves.
- **`mesh.md`** (landed seam, standing law) — `TopologyReceipt.Project` (`:196-205`) exposes ONLY the `(Euler,Genus,BoundaryComponents)` triple, gated on `Genus.Some`; `Genus` is `None` unless `manifold && oriented && numerator≥0 && numerator%2==0` (`:548,555`); `TopologyDetailed` is `internal` (`:539`). This is the root of the repair/receipts heal-rail partiality — a `TopologyReceipt` `ProjectionRow` extension (Growth) is the correct fix per V9.
- **`fields.md`** (landed seam) — `SignedDistanceFromMeshCase` `:284` REAL; `SampleDetailed` public rail `:370`; iso-surfacing DISCLAIMED to `reconstruct.md` (`:228`) — the source of decimate's phantom `IsoSurfaceDetailed`.
- **`reconstruct.md`** (landed seam) — the true iso owner: static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key) → Fin<IsoSurfaceResult>` (`:609`); `IsoSurfaceResult(Mesh, IsoSurfaceReceipt)` `:438`; `SdfMeshPolicy.GeneralizedWinding()` `:171` (returns `Fin<SdfMeshPolicy>`, decimate's `:414 .Bind(...)` correct).
- **`cloud.md`** (landed seam) — `NeighborKernel.OrientNormals` delegation confirmed (`:176`); the `VectorCloudMetric.OrientedNormals`/`PrincipalCurvature` metrics fit/decimate compose are landed cloud rows (not re-verified member-by-member; the brief treats these compositions as legitimate and `OrientNormals`/`Cluster`/`Project` are confirmed present).
- **`sample`/`extract`/`flow`/`geodesics`/`segment`** — landed enabling primitives (CCVT/blue-noise seeds, isoline extraction, streamline tracing, log/exp maps + vector-heat transport, Knöppel cross-fields + stripes + host QuadRemesh capture). Standing law; the `[V2]`/`[V10]`/`[V11]` new planes compose them one level up. No robust-core Processing page re-derives them.

---

## [03] CROSS-CUTTING FINDINGS

### Duplicate mechanisms
- **LM ×2** — `solver.md:303-348` (policy-laddered owner) vs `fit.md:560-627` (hardcoded re-impl). Includes duplicated `LinearizeNormal`/`PackedIndex`/`ApplyDelta`. V7: generalize solver's engine to a residual+Jacobian functor; fit instantiates. `decimate` already composes the shared solve rail (`OptimalPosition` `:363-369`) — the pattern the other two adopt.
- **Crossing kernel** — `repair.md:346-372` is the fourth live copy `intersect.md:388` owns; collapse through `Intersection.Apply` (V4).
- **`BooleanReceipt` name** — `receipts.md:44` vs `arrangement.md:115` (E9/V5).
- **`MeshEdit` mis-homed shared carrier** — defined in `repair.md:76` (`Rasm.Geometry.Healing`), consumed by `decimate` (`:33 using Rasm.Geometry.Healing`) and `receipts` (`:56` unqualified). V6: extract to the Meshing substrate arena beside `mesh.md`; currently immutable-copy-per-op (arena-mutability motion pending). `flatten` does NOT consume `MeshEdit` (uses its own `MeshDec` file-record) — independent.

### Dead / unwired carriers
- `flatten.md:81` `ChartStore.Dead`/`FreeList`/`Kill` (E11) — never exercised.
- `fit.md:246-255` `FitStore` (E11) — never instantiated (`Candidate` is live).
- `fit.md:331-336` the `Consensus` BVH `index` — built, threaded, never queried (dead O(n log n) + wrong owner vs `neighbors.md`).

### Phantom / mis-shaped member claims (verified against landed fences)
- `decimate.md:416` `field.IsoSurfaceDetailed(...)` — PHANTOM; real is static `IsoSurface.Detailed(field, bounds, resolution, IsoSurfacePolicy, context, key)` (`reconstruct.md:609`). Instance-vs-static, name, and signature all wrong.
- All other cross-page member compositions verified REAL: `VectorIntent.Topology`/`Cloud`/`Features` (`intent.md`), `TopologyReceipt.Project` triple (`mesh.md`), `SignedDistanceFromMesh`/`SampleDetailed` (`fields.md`), `SdfMeshPolicy.GeneralizedWinding` (`reconstruct.md`), `SymmetricMatrix`/`CholeskyResult`/`Matrix.DecomposeSvd`/`CholeskySparse`/`SparseMatrix` (`matrix.md` owners), `NeighborKernel.OrientNormals` (`cloud.md`).

### Rail breaches (E10 class)
- `repair.md:243` `(QueryResult.Hits)` (register) and `decimate.md:459` `(QueryResult.Nearest)` (UNFLAGGED) — both unguarded union downcasts throwing `InvalidCastException` on a non-matching arm. Route `QueryResult` on the `Fin`/match rail. solver/fit/flatten carry no equivalent (fit builds but never queries its index).

### Severe correctness defects beyond the register
- **repair+receipts topology-projection partiality** — the heal rail fails on non-manifold / boundaried / odd-Euler meshes (its own input class) because `TopologyReceipt.Project<triple>` fails when `Genus=None`. The most consequential defect in the lane; gates the entire heal capability.
- **decimate orientation-flip guard no-op** — `Orient3D(face, face+normal)` is identically `Positive`; the exact-sign robustness claim never fires.

### Namespace / fault-cluster census (V1 input)
| Page | Namespace declared | Fault sub-band(s) | Note |
|---|---|---|---|
| repair | `Rasm.Geometry.Healing` | `UnrepairableMesh` (healing) | V6 donor + V5 boolean re-home |
| receipts | NONE | (healing) | V1 must mint; implicitly `Healing` |
| decimate | `Rasm.Geometry.Simplification` | `DecimationFault` 2440, `DegenerateInput` | — |
| flatten | `Rasm.Geometry.Parameterization` | `ParameterizationFault` 2432 | V2: `curve.md`'s borrowed `ParameterizationFault` ends; flatten keeps 2432 |
| solver | `Rasm.Geometry.Constraints` | `OverConstrained`, `SingularSystem` | + PHANTOM `Rasm.Geometry.Topology` at `:13` (dies) |
| fit | `Rasm.Geometry.Fitting` | `FitFault` 2428, `DegenerateInput` | — |

Five robust-core concept namespaces (Healing, Simplification, Parameterization, Constraints, Fitting) + the receipts namespace-mint + the solver phantom kill. V1/V7 lift solver+fit into `Solving/` (the one-LM family) — the `Constraints`/`Fitting` namespaces reconcile there. All feed the ratified TIER-2 partition mapping 1:1 to fault sub-bands.

### Dual-paradigm (E5) — corpus-wide, every Processing page
Every robust-core Processing fence rides bare `Fin` entries with no `Op? key` threading, hand-rolled per-receipt guards (`receipts` `Converged`/`Improved`), and no `IValidityEvidence` registration — the exact dual paradigm the landed substrate (`rails.md`, `Domain/validation.md`) forbids. The rebuild adopts the Op-threading law + validity fold while keeping `GeometryFault` (band 2400) as the robust-core fault family (two-family seam, `ARCHITECTURE.md:112`).
