# [RASM_CS_FABRICATION_DECISION]

Binding folder/page-set blueprint for the restructure of `libs/csharp/Rasm.Fabrication/.planning/` (5 folders → 15; 17 pages → 59). WAVE 1 AND THE WIDENING STRUCTURE ARE LANDED ON DISK — the partition is physical at 15 folders/44 pages (the seven moves + `Forming/`/`Joining/` minted; all 26 widening pages at owner depth; 15 `QUEUED` rows complete the 59-map), the tranche-1 interiors are rebuilt AND extended (the 10-case entry with the `derive` arm filled, the 7-row `ProcessModality` axis under the 4-way `ModalityClass`, `EgressKind` at 13 rows, the 46-arm fault registry with tier 3 landed 2730-2746), and `ARCHITECTURE.md`/`README.md` are re-emitted to the 15-folder map with the K24+ ledger rows and the REALIZED `FabricationProjector` registration; those rows and sections now read as LANDED LAW — verify and extend on a hostile re-read, never re-initialize. The LIVE build is tranches 2-3 (rows 7-33), each row rebuilding over the landed foundation. Structure landed first — the `[01]` partition and the `[02]` roster precede every page interior; the landed kernel corpus (`libs/csharp/Rasm/.planning/`) is the signature-lock source; `docs/stacks/csharp/` governs every fence. Dispositions are complete — `[06]`-`[08]` carry the verdict/evidence/plane maps, `[05]` the roster, `[09]` the build order, `[11]` the campaign boundary; no open item survives to execution. This DECISION stands alone.

The blueprint composes the namespace-true partition, the kernel-seam signature-lock TABLE (K1-K23 + the K24+ widening extensions), the 10-case entry (`Post`+`Document`+`Derive`+`Form`), the `EgressKind` egress-spine collapse onto `owner#atoms`, the F1-F4 flagship dry-run closure, the `CapabilityVerdict`-on-`owner#atoms` plan gate, the clipper SPLIT under one custodian, and the exhaustive 24-row roster. The neutral `CutProgram` AST is `Posting/program`-local and assembled in the `Run(Post)` case from the `owner#atoms` `Motion` result — never inside the Cam fold — preserving the disk-true decoupling the acyclicity proof depends on (`[00]` ruling 5).

## [00]-[STANDING_LAW]

The corpus is the making-domain owner across three registers: general fabrication (subtractive CAM, additive from industrial LPBF to consumer FFF/resin, thermal cutting, forming, joining), AEC fabrication (a designed building component — `Rasm.Element` graph, `Rasm.Bim` semantics, `Rasm.Materials` layered assemblies and joining vocabulary — derives its connection details, flat patterns, programs, and shop documentation), and the higher-order design-to-fabrication derivation above both: the designed artifact plus its demanded spec DERIVES the process route, the manufacturability verdict, the tooling/setup/assembly plan, the programs, and the documents. The folder map realizes that telos. `Process/` hosts four realized namespaces (`Process`/`ProcessModel`/`ProcessPhysics`/root), `Posting/` two (`Posting`/`Projection`), `Toolpath/` three (`Toolpath`/`Kinematics`/`Additive`), `Polygon/` and `Nesting/` each hide a migrant (`Geometry2D` import, `Fixturing` workholding — both already namespace-true on disk). The fence `namespace` declaration is the ground truth of where a concern BELONGS; the folder is the lie the DECISION corrects. Twelve realized namespaces (disk-verified: `Process`·`ProcessModel`·`ProcessPhysics`·root·`Geometry2D`·`Toolpath`·`Kinematics`·`Additive`·`Nesting`·`Fixturing`·`Posting`·`Projection`) became thirteen folders (landed); the widening mints two more — `Forming/` (`Rasm.Fabrication.Forming`) and `Joining/` (`Rasm.Fabrication.Joining`) — for FIFTEEN, with `Verify`·`Spec`·`Tooling` the mandate lanes the roster provisioned. Fabrication is ALSO an egress engine: every flagship terminates in a content-keyed machine-consumable artifact, so the egress concern COLLAPSES onto the entry vocabulary — the `EgressKind` discriminant + ONE `ContentHash.Of` fold on `owner#atoms` — never a fourteenth `Egress/` folder.

FIVE structural rulings:

1. **`ProcessKind` collision-break (HIGH hazard — the V1 floor introduces it).** `owner.md` declares `namespace Rasm.Fabrication.Process`; `family.md` declares `[SmartEnum<string>] public sealed partial class Process` in `namespace Rasm.Fabrication.ProcessModel`. The V1 ratification homes family into `Rasm.Fabrication.Process`, so the type resolves `Rasm.Fabrication.Process.Process` — and every bare `Process` reference from within `Rasm.Fabrication.*` binds the NAMESPACE `Rasm.Fabrication.Process` before the imported type (namespaces in the enclosing hierarchy win name lookup), a live CS0118 (the `owner.md` `Process Process` field, `program.md` bare `Process` refs). **RULING: rename the SmartEnum type `Process` → `ProcessKind`** (family declaration + every `input.Process`/`ProcessKind process`/`FabricationInput.Process`-typed reference across owner/physics/motion/program/magazine). This is the namespace-PRESERVING break — renaming the namespace segment forfeits the folder=namespace 1:1 the partition ratifies. **`Machine` does NOT rename** (no `Machine` namespace exists — verified; the asymmetry is collision-forced). A companion `Machine→MachineKind` rename is gratuitous churn, REJECTED.

2. **`CutterForm` mints on `owner#atoms`, never `Tooling/`.** `FabricationPolicy.Cam` re-frames its bare `ToolRadius` double on `owner.md` to a `CutterForm` reference (V5). Siting `CutterForm` on `Tooling/cuttingdata` cycles: `owner#atoms(Cam.CutterForm) → cuttingdata(CutterForm) → magazine(CutterForm.Of(ToolAssembly)) → owner#atoms(HolderEnvelope→Loop)` (the `magazine.md` `HolderEnvelope(ToolAssembly)→Loop` projection is the back-edge anchor). RULING: the `CutterForm` value TYPE mints on `owner#atoms` (a pure-scalar leaf — flat/ball/bull/taper/drill/chamfer/thread-mill rows with diameter/corner-radius/taper-angle/flute-length), exactly as `CutStrategy` and `ResidualStock` ride the entry vocabulary; `Tooling/cuttingdata` owns the PROJECTION `CutterForm.Of(ToolAssembly)` + the Kienzle machinability keyed by it. The four consumers (surface/removal/guard/cuttingdata) read the `owner#atoms` type; the cycle never forms.

3. **The two reciprocal `[V12]` mis-bindings stay refuted — and both underlying seams are LANDED.** `.archive/RASM-CS-PERSISTENCE-BRIEF.md` `[V12]` is `[V12_GOVERNANCE_RECONCILE]`, NOT an egress index — the durable-row mechanism is LANDED at `Rasm.Persistence/Query/cache.md#[02]-[ARTIFACT_BLOB_INDEX]` (`ArtifactKind [SmartEnum<string>]` + `ArtifactIndexRow.Admit(kind, key, bytes, classification, at, sourceKey)` content-keyed through the kernel one-hasher), so the residual is ENROLLMENT: the eight Fabrication artifact families (`cutprogram`/`placement`/`remnant`/`cli`/`threemf`/`nc1`/`stock-snapshot`/`traveler`) land as one-row-each Growth-law rows on that owner, each riding its egress page's landing (`[04]`). `.archive/RASM-CS-COMPUTE-BRIEF.md` `[V12]` is `[V12_DISCIPLINE_COVERAGE]`, NOT a `WasteAreaMm2` rollup — and the Compute counterpart is LANDED (`Rasm.Compute/ARCHITECTURE.md:90`; `Analysis/aggregator.md` `ElementQuantity.WasteAreaM2`; `Analysis/lifecycle.md` `NestWasteM2`, frozen decode key `NestWasteArea` SI m²), so the `Nesting/stock → Compute` seam is TWO-SIDED with the quantity-bag lowering riding the deferred `FabricationProjector`. RULING: every machine-consumable egress artifact mints its content key through the CONFIRMED `ContentHash.Of` seed-zero entry (landed kernel law); the Fabrication egress discriminant is the LOCAL `EgressKind` axis, FEDERATED to the Persistence `ArtifactKind` rows at the content-key boundary — never a strata-crossing type reference and never a second same-named mint. Signature-lock rides the landed kernel page `#Symbol` anchors (the `[04]` K-table), never line-number pins. The `[RESEARCH]`-tail census is FIVE (`kinematics` · `skeleton` · `slicing` · `stock` · `program`).

4. **The entry is 10-case; the egress spine collapses onto `owner#atoms`.** `FabricationPolicy`/`FabricationResult` grow the ruled `{Additive, Verify, Inspect}` PLUS `{Post, Document}` atop the disk `{Cam, HiddenLine, Nest}` — and the widening adds `{Derive(AdmittedComponent, DerivePolicy) -> FabricationPlan, Form(FormPolicy) -> FormedResult}` — TEN cases. Welding EGRESSES through the existing `Cam` case under the `Deposition` modality (torch `Motion` via the Robots cell), never an eleventh case; `Derive` is the forward mirror of `Document`'s widest fan-in and composes DOWNWARD only, so no cycle forms. Re-posting one `Motion` to five dialects is `Run(Post{Motion, PostDialect})` ×5 (F1); traveler-compose is `Run(Document{results})` (F4) — both re-invocable terminal transforms over a prior result, so each is a `Run` policy case carrying the prior result on `owner#atoms`, NEVER a second public `Post()`/`Traveler.Assemble()` fold. The `EgressKind` discriminant is a row family co-located with the egress `FabricationResult` cases on `owner#atoms`, keyed through ONE `ContentHash.Of` fold — Fabrication-local by name and by strata (the Persistence `ArtifactKind` taxonomy is a different owner the egress rows federate to at the content-key boundary). `CapabilityVerdict` mints on `owner#atoms` as a minimal input-carried verdict leaf (pass/fail + Cpk + demanded grade); `Spec/capability.Gate` PRODUCES it in the truth tranche of the build order, the plan READS it input-carried, closing spec→plan→verify→capability with no intra-run cycle (the full `CapabilityReport` stays terminal on `capability`).

5. **The result-payload discipline (restores the disk-true motion↔program decoupling).** Disk: `Cam.Solve` returns `Fin<FabricationResult>` carrying a `Motion` (`motion.md`); `Posting.Post(FabricationResult, …)` returns `Fin<CutProgram>` (`program.md`) — the `Motion`/`Placement` streams dead-end into the `CutProgram` AST program OWNS. RULING: **every `FabricationResult` egress case declared on `owner#atoms` carries ONLY owner#atoms-safe payloads** (primitives, kernel vocabulary, and the owner#atoms leaves `Move`/`Edge3`/`PartTransform`/`Remnant`/`EgressKind`/`ContentKey`/`CutterForm`/`ResidualStock`); the rich plane-internal types (`CutProgram` AST, PicoGK `Voxels`, GD&T frames) stay plane-local and NEVER ride a result case. The neutral `CutProgram` AST is `Posting/program`-local, assembled in the `Run(Post)` case body from the `owner#atoms` `Motion` result — NOT inside the Cam fold (the Cam fold executes Guard/Workholding/Magazine conditioning and emits the conditioned `Motion`; posting-side conditioning `Kerf`/`Feedrate`/`Lookahead` + `dialect.Emit` lowering are program's; the Placement-vs-Motion asymmetry binds which conditioning runs on which egress case — the 2D-cut `Placement` runs `Kerf`/`Lead`/`Pierce`/`Tab`/`Sequence`, the already-conditioned `Motion` NEVER a second kerf pass, V3). `motion` and `program` decouple through the `owner#atoms` `Motion` type — no `motion→program` compose edge exists — and the `PostedProgram` result case carries the lowered blocks + `EgressKind` + `ContentKey`, never the AST.

OpenCAMLib probe = **FEASIBLE** — sited on `Toolpath/surface` via a thin `extern "C"` C-shim + source-generated `[LibraryImport]` P/Invoke over the shipped C++-ABI SHARED `libocl` archives; author-kernel SDF drop-cutter (K8) retired to a recorded admission-abandonment fallback (`[00]` probe ruling below, `[05]` row 1). Both ruled kernel hinges carried verbatim and signature-locked, never re-opened: **`[V10]a` kerf split** (K1 — kernel owns the exact `JoinType`×`EndType` corner-row offset assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours on `Geometry2D/arcs`; landed at `Meshing/offset.md#Offsetting.Apply`) and **`[V10]d` medial radius carriage** (K1/K2 — kernel medial carries the per-point clearance RADIUS + arbitrary-probe `Clearance(Point3d probe)` as ONE shared result family; `Toolpath/Skeleton.cs` wavefront dies unconditionally, keeps only the trochoidal WALK; landed at `Meshing/offset.md#Medial` + `Meshing/skeleton.md#Skeletonize.Apply`).

**PROBE RULING (OpenCAMLib surface-engine siting).** ADMIT-WITH-SHIM. All three target RIDs ship as SHARED libraries in release `2023.01.11` (`macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64`; `cxxlib.cmake` `add_library(ocl SHARED …)`). The exported ABI is C++-mangled with by-reference `STLSurf&`/`CLPoint&`/`std::vector<CLPoint>` arguments — a direct P/Invoke is impossible and a C-shim is MANDATORY (the ruled path, not a defect). The wrap surface is small and uniform: one `ocl::Operation` base (`setSTL`/`setCutter`/`setSampling`/`setThreads`/`run`/`getCLPoints`) unifies `BatchDropCutter`/`PathDropCutter`/`Waterline`/`AdaptiveWaterline`; the `MillingCutter` hierarchy (`Cyl`/`Ball`/`Bull`/`Cone`/`Composite`) maps 1:1 onto the `CutterForm` axis (flat/ball/bull/taper/compound). ~18-24 `extern "C"` functions, all blittable (opaque `void*` handles + flat `double[]` triangles/points); the Boost.Python binding (`src/pythonlib/ocl_*.cpp`) is the exact member-by-member blueprint. LGPL-2.1; dynamic-link P/Invoke through a separate replaceable `libocl.dylib/.so/.dll` satisfies §6 and is ratified campaign policy — keep `libocl` dynamically linked, never statically folded into the shim. Dependency closure is tiny: header-only Boost (`Boost::boost`) + OpenMP; zero submodules; no CGAL/Eigen/TBB. Two admission routes doubly cover the RID matrix: consume the shipped SHARED archives OR build `libocl`+shim from source per RID (`BUILD_CXX_LIB=ON`), Forge-provisioned like the kernel Z3 precedent. No C ABI/C# binding/NuGet exists (feed-verified). The fallback (author-kernel drop-cutter over the kernel `[V8]` SDF) is strictly heavier on BOTH axes — re-derives exact per-cutter-form contact (flat/bull/cone are the cutter-solid lower envelope, NOT a mesh-SDF iso-offset), is resolution-bound where OCL is analytically exact, degrades waterline to marching-squares, and is calendar-HOSTAGE to the UNLANDED kernel `[V8]` SDF lane.

## [01]-[RATIFIED_PARTITION] — 15 folders = 15 namespaces (bijective), fault clusters

Folder deltas (all within `[STRUCTURAL_AUTHORITY]`, all namespace-motivated on disk): `Polygon/` DIES (clipper SPLITS to `Geometry2D/{algebra,arcs}`, import MOVES to `Ingress/profile`). `Process/` KEEPS (loses magazine → Tooling; the collision-break renames its axis type). `Toolpath/` KEEPS (loses kinematics → Kinematics, slicing → Additive; gains surface + partition). `Nesting/` KEEPS (loses workholding → Fixturing). `Posting/` KEEPS (loses projection → Documentation; gains dialect). MINTED: `Tooling/` · `Geometry2D/` · `Ingress/` · `Kinematics/` · `Additive/` · `Fixturing/` · `Verify/` · `Spec/` · `Documentation/` (landed) · `Forming/` · `Joining/` (the widening). Every folder ≥2 pages; the mandate lanes are the growth that makes the thin folders legal. The `ARCHITECTURE.md` `[01]` codemap + `[02]` seam ledger + the README router rewrite land with the partition, before any page interior.

Namespace partition — 15 TIER-1 folders ↔ 15 TIER-2 namespaces, BIJECTIVE. The **six folder-overrides-namespace ratifications** (fence `namespace` rewrites in the same MOVE): family/faults/physics → `Process` (from `ProcessModel`/root/`ProcessPhysics`), magazine → `Tooling` (from `ProcessModel`), import → `Ingress` (from `Geometry2D`), projection → `Documentation` (from `Projection`). The **four namespace-KEEPING moves** (the realized namespace already names the target folder, the FOLDER catches up): clipper `Geometry2D` (SPLIT, folder Polygon→Geometry2D), kinematics `Kinematics` (folder Toolpath→Kinematics), slicing `Additive` (folder Toolpath→Additive), workholding `Fixturing` (folder Nesting→Fixturing).

| # | Folder | Namespace | Pages | Fault cluster | Charter |
|---|---|---|---|---|---|
| 1 | `Process/` | `Rasm.Fabrication.Process` | owner, family, physics, faults | 2700 band owner (`OpenLoop` 2704 cross-cutting recorded exception) | the entry, the axes, the physics, the rail |
| 2 | `Tooling/` | `Rasm.Fabrication.Tooling` | magazine, cuttingdata | 2712 `MachinabilityUnknown`, 2724 `NoToolForOp` | ISO-13399 tool intelligence + machinability |
| 3 | `Geometry2D/` | `Rasm.Fabrication.Geometry2D` | algebra, arcs | 2703 `KerfCollision` (re-attributed from Posting) | the 2D substrate (line-space + arc-space) |
| 4 | `Ingress/` | `Rasm.Fabrication.Ingress` | profile, solid, steel | 2711 `IngressTranslation` | everything entering as geometry |
| 5 | `Toolpath/` | `Rasm.Fabrication.Toolpath` | motion, surface, partition, guard, skeleton | 2705 `InadmissiblePair`, 2706 `Gouge`, 2707 `Collision`, 2713 `SampleStalled`, 2723 `PartitionDegenerate` | subtractive CAM |
| 6 | `Kinematics/` | `Rasm.Fabrication.Kinematics` | cell, machine | 2702 `Unreachable`, 2714 `AxisSingularity` | motion topology |
| 7 | `Additive/` | `Rasm.Fabrication.Additive` | slicing, implicit, production | 2708 `NonManifoldSlice`, 2715 `VoxelFault`, 2716 `OrientationInfeasible`, 2725 `Unsupported3mfExtension` | production 3DP |
| 8 | `Nesting/` | `Rasm.Fabrication.Nesting` | nfp, stock | 2701 `NoFit`, 2709 `StockOverflow`, 2710 `Nest` | layout + yield |
| 9 | `Fixturing/` | `Rasm.Fabrication.Fixturing` | workholding, setups | 2717 `SetupInfeasible`, 2726 `DatumLineageBroken`, 2727 `ClampOnMachinedFace` | keep-out + setup planning |
| 10 | `Posting/` | `Rasm.Fabrication.Posting` | program, dialect | 2718 `DialectUnsupported`, 2719 `ProgramParse`, 2728 `BlockCapExceeded` | machine-code emission |
| 11 | `Verify/` | `Rasm.Fabrication.Verify` | removal, probing | 2720 `ProbeOvertravel` (+ `Gouge` 2706 shared verify-time) | program-level truth |
| 12 | `Spec/` | `Rasm.Fabrication.Spec` | tolerance, capability | 2721 `ToleranceUnsatisfiable`, 2722 `CapabilityShortfall`, 2729 `StackupExceeded` | production specs |
| 13 | `Documentation/` | `Rasm.Fabrication.Documentation` | projection, traveler | reserved (projection routes kernel `ProjectionFault`; traveler assembles) | shop documentation |
| 14 | `Forming/` | `Rasm.Fabrication.Forming` | sheet, brake, tube | tier-3 arms (landed, `faults.md` authority) | deformation fabrication — unfold, bend, tube |
| 15 | `Joining/` | `Rasm.Fabrication.Joining` | weld, sequence, procedure | tier-3 arms (landed, `faults.md` authority) | weld fabrication + assembly-weld sequencing + WPS compliance |

RULED, never re-litigated: (a) the `Process` axis TYPE renames `Process`→`ProcessKind` (`[00]` ruling 1); `Machine` unchanged. (b) `Geometry2D` is the ONE namespace carrying two pages — the split is CONCERN-driven (Clipper2 int64 line lattice vs CavalierContours bulge arc lattice, `[V10]a` stratum), not namespace-driven, so folder=namespace 1:1 holds. (c) `Ingress` carries three pages under ONE polymorphic `Ingress.Admit(IngressSource)` fold — profile/solid/steel own the `Profile`/`Solid`/`Steel` source arms. (d) `Documentation/` has an EMPTY reserved fault block (projection routes the kernel `GeometryFault.ProjectionFault(EdgeKind, int)` cluster 2436-2439 — `Rasm.Numerics`, raised by `Rasm.Drawing` `View.Apply`; traveler is content-keyed assembly with no fault producer) — mechanically checkable. (e) The `owner#atoms`/`owner#run` two-node split is the ONE ratified acyclicity exception: `Process/owner` is both the upstream atom vocabulary and the terminal `Fabrication.Run` fold; the ledger evaluates it as two nodes and the page never splits physically (its ledger passes the two-node discipline — see `[09]`).

Fault-registry design: the 2700 century divides into 13 folder clusters. The landed **2701-2710 are the FROZEN first-instance layer** (disk-verified against the landed `faults.md` arm table, one arm per `+n` code) — wire codes retained per the `.archive` "build ON, never re-band" law + the landed artifact-index persisted-decode constraint — and are NON-contiguous per folder (Nesting spans 2701/2709/2710) as the recorded consequence of the freeze. The NEW growth arms allocate folder-grouped from 2711 across two tiers (2711-2722 · 2723-2729), so folder = fault-cluster is mechanically checkable within each growth tier; the ten legacy arms are a recorded frozen exception mirroring the kernel's cross-cutting `DegenerateInput` 2400. Full registry in `[03]`.

## [02]-[PAGE_SET] — 59 rows: 33 (tranches 1-3) + 26 widening rows (tranche W; paths `libs/csharp/Rasm.Fabrication/.planning/`-relative)

Row schema: path | action | charter (folder · namespace · fault cluster) | entry (owning fold signature + the `FabricationPolicy`/`FabricationResult` case ONLY where the page grows the entry) | seams (out = composes; in = consumed by; `owner` evaluates as TWO nodes — `owner#atoms` upstream-only, `owner#run` terminal). The roster lists in build order (`[09]`): foundations before consumers, three dependency tranches. Per-page NEW/REBUILD skeletons follow the docs/stacks/csharp shape law; `[00]` ruling 5 governs every result-case payload.

Seven source pages leave disk, each deleted by exactly ONE destination that carries its content: `Process/magazine.md` → `Tooling/magazine.md` · `Polygon/clipper.md` → `Geometry2D/algebra.md` (whole-file custody — `Geometry2D/arcs.md` builds standalone from its own charter inventory of the clipper arc-space block, never a second custody over the same source) · `Polygon/import.md` → `Ingress/profile.md` · `Toolpath/kinematics.md` → `Kinematics/cell.md` · `Toolpath/slicing.md` → `Additive/slicing.md` · `Nesting/workholding.md` → `Fixturing/workholding.md` · `Posting/projection.md` → `Documentation/projection.md`; `Polygon/` deletes entirely. ONE donor-block excision moves content WITHOUT deleting its source: the `CuttingData` table leaves `physics.md` for `Tooling/cuttingdata.md` — `physics` keeps the identity map, `cuttingdata` deepens the table to the Kienzle `kc` model. `Process/owner.md` builds ONCE (tranche 1: atoms + policy growth); each plane fills its own `owner#run` dispatch arm as that plane lands, so no arm ever references an unbuilt plane and `owner.md` never rebuilds a second time. Every other page is single-touch.

### Tranche 1 — STRUCTURE + AXES + RAIL (Process rebuilds + Tooling)

| # | Path | Action | Charter | Entry (+case) | Seams |
|---|---|---|---|---|---|
| 1 | `Process/owner.md` | REBUILD | Process · `Rasm.Fabrication.Process` · 2700 owner. The settled `Run` entry + generated total `Switch` PRESERVED VERBATIM (cold-pass core); the rebuild is atom-vocabulary + policy/result growth to **8 cases**: `Loop` widens to `(Arr<Point3d>, bool, Arr<double> Bulges)` + 3-arg ctor + `BulgeAt` (the `[V10]a` arc rail's one shape, 0/tan(θ/4)); `ProjectionDir` mints HERE (SEAM inversion fix — the disk `owner.md` imports it downstream from `Projection`); `CutterForm` value type mints HERE (`[00]` ruling 2); `ResidualStock` + per-setup `StockSnapshot` mint HERE (V7, input-carried); `CapabilityVerdict` mints HERE (V8, input-carried); `EgressKind` discriminant mints HERE (egress row family, keyed through the ONE `ContentHash.Of` fold). Every egress result case carries ONLY owner#atoms-safe payloads (`[00]` ruling 5) | `Fin<FabricationResult> Fabrication.Run(FabricationPolicy, FabricationInput)` — **8→10-case (widening landed)** `FabricationPolicy` {`Cam`, `HiddenLine`, `Nest`, +`Additive(AdditivePolicy)`, +`Verify(VerifyPolicy)`, +`Inspect(InspectPolicy)`, +`Post(FabricationResult.Motion, PostDialect)`, +`Document(Seq<FabricationResult>)`}, **8→10-case** `FabricationResult` {`Motion`, `HiddenLineResult`, `Placement`, +`AdditiveResult`, +`VerificationResult`, +`InspectionResult`, +`PostedProgram`, +`TravelerDocument`, +`FabricationPlan`, +`FormedResult` (widening, landed)}; egress cases carry the `EgressKind` discriminant + `ContentKey` (lowered blocks/keys only, never plane AST) | **owner#atoms** out: predicates `Orient2D`, Vectors `MeshSpace`/`Point3d`, `ContentHash.Of` (content-key seed), family `CutStrategy`/`ProcessKind` (leaf axes) · in: EVERY plane reads the atoms. **owner#run** out: projection.Hlr, motion.Cam, nfp.Nest, additive, removal, probing, program/dialect Post-lowering, traveler Document-compose (terminal 8-arm dispatch — each arm fills as its plane lands) · in: none (terminal) |
| 2 | `Process/family.md` | REBUILD | Process · `Rasm.Fabrication.Process` · 2700. Axis widening + the collision rename: `ProcessKind` (ex-`Process`, `[00]` ruling 1) · `Machine` (UNRENAMED) · `ProcessModality` (ex-`RemovalModality` — the landed 7-row axis under the 4-way `ModalityClass`) · `KinematicClass` deepens to rotary-topology rows (table-table trunnion \| head-head \| head-table \| nutating — `mill-5axis` stops binding `CartesianGantry`, V5/V8) · `HoldingClass` · `PostDialect` grows GRAMMAR-family capability columns (family · canned-cycle grammar · macro/subprogram convention · WCS roster · cutter-comp admission · arc mode · block cap · decimal/modal policy · per-`GCommand` code-override — V4; the flat 3-column render dies) · `CutStrategy` gains a dimensionality column (2.5D \| 3D-surface \| multi-axis, V5). Data-vs-lowering: widened axes stay HERE, emission folds are `Posting/dialect`'s | NONE (pure `[SmartEnum]` axes; `Admits` row-relations — a leaf, composes only kernel) | out: `Rasm.Numerics` `GeometryFault`, predicates · in: owner#atoms (CutStrategy/ProcessKind), physics, motion, program, dialect, magazine, workholding, machine, cell |
| 3 | `Process/physics.md` | REBUILD | Process · `Rasm.Fabrication.Process` · 2700. Identity map (V9): ONE `Material` identity carrying `Map<ProcessModality, ModalityPhysics>` (the disk `stainless`/`stainless-abrasive` + `mild-steel`/`mild-steel-thermal` fragmented rows collapse); `Budget` modality-dispatches into the map; `RemovalBudget.Erosion` lands (EDM discharge/pulse/wire-feed — the `erosion→ThermalBudget` conflation dies) beside `Resin` (exposure/cure) + `Powder` (laser/hatch/scan) + DED-distinct-from-FFF; the dead `Overrides` carrier DELETES; the `90.0` fallback demotes behind the data table. The `CuttingData` table DONOR-BLOCK excises to `Tooling/cuttingdata` (the source page survives the excision) | `RemovalBudget RemovalParameter.Budget(ProcessKind, Material, ToolAssembly, Operation)` | out: family (`ProcessModality`), UnitsNet, `Rasm.Materials/Properties` (raw doubles, AEC-peer boundary — anchor reconciled to `Properties/properties#MATERIAL_PROPERTY_CATALOGUE`) · in: motion, cuttingdata, surface (chip-load budget) |
| 4 | `Process/faults.md` | REBUILD | Process · `Rasm.Fabrication.Process` · 2700 band owner. `FabricationFault` `[Union]` on `FaultBand.Fabrication = 2700`: every legacy arm retypes its bare `string Detail` (disk `faults.md`) to a typed payload (`Gouge`→point+CutterForm, `Collision`→zone, `InadmissiblePair`→typed `(ProcessModality, CutStrategy)`, `NoFit`→part+tried rotations, `Unreachable`→joint diagnostics); the frozen 2701-2710 wire codes PRESERVED; nineteen new arms land 2711-2729 folder-grouped across two growth tiers (`[03]`); the `band-2500` root-anchor + the `ARCHITECTURE.md`/`kinematics`/`.api` 25xx references purge to 2700 | NONE (`FabricationFault.<Case>(...).ToError()` vocabulary; composes kernel band-2400 `GeometryFault` for `DegenerateInput`) | out: `Rasm.Element` `FaultBand` registry, `Rasm.Numerics` `GeometryFault` · in: every fault-routing owner |
| 5 | `Tooling/magazine.md` | MOVE (ProcessModel→Tooling) + wire — carries `Process/magazine.md`'s content; the source leaves disk with this move | Tooling · `Rasm.Fabrication.Tooling` · 2712,2724. MTConnect `CuttingToolAsset` `ToolAssembly`/`Schedule` life-split PRESERVED; `HolderEnvelope` projection kept; `Runout` reads the real `ShankDiameterMeasurement` value (the disk literal `0.01`/`0.005` stub dies, E12); `GenerateHash` reconciles onto `ContentHash.Of` (no second mint); WIRED — the tool-change `Schedule` feeds `Posting/program` (real `M6`/`G43`/`Tnn` blocks at interval boundaries, the E2 fiction realized) | `ToolMagazine.Schedule(Seq<ToolAssembly>, …)` life-split; `HolderEnvelope.Of(ToolAssembly)` | out: MTConnect `CuttingToolAsset`, `ContentHash.Of`, UnitsNet, physics (Tool), owner#atoms (Loop), Riok.Mapperly · in: cuttingdata (ToolAssembly), guard (HolderEnvelope), program (Schedule) · cross-pkg: ← `Rasm.AppHost` decoded tool-life telemetry (the APPHOST-BRIEF MTConnect.NET row, first consumer) |
| 6 | `Tooling/cuttingdata.md` | NEW | Tooling · `Rasm.Fabrication.Tooling` · 2712. The machinability model (V9): Kienzle unit-cutting-force `kc` + chip-thickness exponent `mc` + per-operation surface-speed + feed-per-tooth columns keyed by the `physics` `Material` identity. Seed-data policy: the page AUTHORS the column schema, the ISO 513 material-class row keys (P/M/K/N/S/H), and one physically-sourced representative seed row per class; per-material/per-operation production rows enter ONLY through the CSV/QIF data-INGRESS arm — never hand-transcribed vendor cards (the FreeCAD card set is copyright-excluded; its `kc` SCHEMA is the reference); a lookup missing both a `kc` row and an admissible formula fallback returns `MachinabilityUnknown` 2712, never a silent default; the `CutterForm.Of(ToolAssembly)` projection from the ISO-13399 measurement set (the type mints on owner#atoms, `[00]` ruling 2); `program`'s `0.0012` deflection literal reads the `kc` column | `CuttingData CuttingData.Of(Material, CutterForm, Operation)`; `CutterForm CutterForm.Of(ToolAssembly)` | out: magazine (ToolAssembly), physics (Material), owner#atoms (CutterForm), MathNet, UnitsNet · in: program (kc deflection), surface (effective-diameter speed) |

### Tranche 2 — SUBSTRATE + INGRESS (Geometry2D split, Ingress, skeleton staged early)

| # | Path | Action | Charter | Entry (+case) | Seams |
|---|---|---|---|---|---|
| 7 | `Geometry2D/algebra.md` | SPLIT from clipper — the ONE clipper custodian (whole-file custody: the line-space half lands HERE, the arc-space block rides row 8's standalone inventory; this page carries the single clipper delete) | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The Clipper2 line-space owner (offset · `DeltaCallback64` variable offset · Boolean clip · open-subject/open-path clip — trimming open toolpath centerlines + marking/engrave polylines against region boundaries, a distinct Clipper2 capability from closed-region Boolean, the ruled line-space roster · Minkowski sum/diff · `ReuseableDataContainer64` scanbeam reuse) — the settled craft, namespace already true on disk. ADDS `NestingOrder` — the inner-before-outer containment-depth sort (namespace-true re-home of the mis-attributed `Posting.Sequence`: contour ordering by nesting depth is a Geometry2D concern via `Loop.Covers`, not machine-code; posting/motion/workholding all compose it, killing the workholding→posting cross-tranche edge). Growth lane held: the inner-fit dual primitive (the Minkowski-difference container-feasibility fold `nfp` consumes) lands as one `PolygonAlgebra` row when nfp demands it | `PolygonAlgebra` — `Offset`/`Clip`/`ClipOpen`/`Minkowski.Sum`/`Minkowski.Diff`/`OffsetVariable`/`NestingOrder` (line-space) | out: Clipper2 `Clipper2Lib`, predicates `Orient2D`, owner#atoms (Loop) · in: nfp, guard, workholding, motion, arcs (kerf base), slicing, partition (open-path engrave/marking trim), program (NestingOrder) |
| 8 | `Geometry2D/arcs.md` | SPLIT from clipper — standalone build with NO second custody (`Polygon/clipper.md` leaves disk through row 7 alone; a second custody over one source is the double-delete race). Content source: the clipper arc-space block (`ArcAlgebra`/`BoolKind`/CavalierContours), fully inventoried in this charter so the build stands alone against the source delete | Geometry2D · `Rasm.Fabrication.Geometry2D` · 2703. The CavalierContours arc-space owner composing the RULED `[V10]a` split: the kernel owns the exact corner-row offset assembly + medial, `ArcAlgebra` owns pure kerf compensation IN ARC SPACE over the kernel-emitted region offsets — the landed `BoolKind` `[SmartEnum<string>]` set-op axis (or/and/not/xor→CavalierContours `BooleanOp`) + `ArcOffset`/`ArcBoolean`/`ShapeOffset`/`ArcLength`/`SampleAtLength`, island-preserving `Shape<T>.FromPlines(loops).ParallelOffset`, `StaticAABB2DIndex<T>`, the bulged-`Loop`↔`Polyline<double>` boundary seam (all carried from clipper); the kerf/lead/adaptive rails wire through HERE (arc-native, no biarc refit); the `result.Positive` PHANTOM corrects to `result.PosPlines[].Pline` (a disk phantom; `PosPlines` is the real member per the `api-cavaliercontours.md` authority); reads the widened `Loop.Bulges`/`BulgeAt`/3-arg ctor (landed on owner#atoms in tranche 1) | `ArcAlgebra` — `KerfArc`/`LeadArc`/`AdaptiveArc`/`ArcBoolean(BoolKind)` over kernel `Offsetting.Apply` region offsets | out: CavalierContours (arc-space), kernel `Offsetting.Apply` (region offsets, K1), algebra (kerf base), owner#atoms (Loop.Bulges), predicates `Orient2D` · in: program (arc-native kerf/lead), motion (adaptive), skeleton (trochoidal chord) |
| 9 | `Ingress/profile.md` | MOVE (Polygon/import, ns Geometry2D → Ingress) — carries `Polygon/import.md`'s content; the source leaves disk | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DXF/DWG profile-ingress arm of the ONE polymorphic `Ingress.Admit` fold; ACadSharp `Failsafe`/bulge+NURBS samplers/`Insert.Explode` PRESERVED; the dead `warnings` accumulator wires the strict-mode `NotificationType.Error` escalation (ruled — the claim-deletion form loses ingress-degradation visibility); the netDxf rejection frame re-writes (netDxf removed repo-wide) | `Fin<AdmittedGeometry> Ingress.Admit(IngressSource)` over `IngressSource` `[Union]` {`Profile` · `Solid` · `Steel`} — profile owns the fold + `Profile` arm | out: ACadSharp, predicates, owner#atoms (Loop) · in: nfp (part library), program (profile program), motion · cross-pkg: ← `Rasm.Bim/Exchange` (shared ACadSharp read pin, the `ARCHITECTURE.md` netDxf parenthetical → the AppUi ACadSharp two-format WRITE leg) |
| 10 | `Ingress/solid.md` | NEW | Ingress · `Rasm.Fabrication.Ingress` · 2711. The OcctNet B-rep solid-ingress arm: `ImportStep`(AP203/214/242)/`ImportIges`/`ImportStl` → `OcctShape` → `Triangulate` → `OcctMesh`; the tessellated `OcctMesh` crosses to kernel vocabulary at the seam (`MeshSpace` admission; dirty STL routed through the kernel predicate-gated `HealOp`), so downstream planes consume admitted kernel geometry, never raw soup; the `libTKHLR`/`libTKXCAF` managed-unbound SCOPE_LIMIT is a standing demand row (assembly + AP242 PMI surfacing = wrapper pressure, the `Spec/` plane's secondary source) — a standing demand invariant | `Ingress.Admit` `Solid` arm (STEP/IGES/STL → kernel `MeshSpace`) | out: OcctNet.Wrapper, kernel `MeshSpace` admission (`mesh.md`), kernel `HealOp` (`repair.md`, dirty STL), Riok.Mapperly · in: slicing, surface, projection (the B-rep the format shops receive) |
| 11 | `Ingress/steel.md` | NEW | Ingress · `Rasm.Fabrication.Ingress` · 2711. The DSTV NC1 steel-exchange READ arm: the record tree (`ST` header + `BO`/`SI`/`SC`/`AK`/`IK`/`KA` blocks) → `Loop` via a Riok.Mapperly generated projection (hand-written seam copyists die); the `KA` bend blocks seed the sheet-metal growth lane — K-factor bend-allowance is the Fabrication overlay over the kernel `develop.md` isometric unroll, never a second unroll engine; unfold waits for its consumer; the emission MODEL is shared with the `Posting` NC1 target (mirroring the read) | `Ingress.Admit` `Steel` arm (NC1 → Loop) | out: DSTV.Net, Riok.Mapperly (DSTV-record → Loop), owner#atoms (Loop) · in: nfp, dialect (NC1 emit model shared — the counterpart of row 27's `steel` out-edge) |
| 12 | `Toolpath/skeleton.md` | REBUILD (staged early: motion's engagement bound reads its clearance field) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705-2707,2713. Kernel-medial CONSUMER (`[V2]a`/`[V10]d`, RULED): `Toolpath/Skeleton.cs` wavefront (`Wavefront`/`SkeletonEvent`/`Propagate`/`OffsetAt`) DIES UNCONDITIONALLY — the page's CURRENT fences still author the `StraightSkeleton`/`Wavefront`/`SkeletonEvent` medial kernel, so the rebuild REPLACES the fence bodies with kernel clearance-family delegation (`Meshing/offset.md#Medial`/`#Clearance` + `Meshing/skeleton.md#Skeletonize.Apply`), never an edit around them; the page keeps ONLY the trochoidal constant-engagement WALK strategy over the kernel clearance field; the `[03]-[RESEARCH]` tail + false "no managed library exists" seal purge | `Skeleton.Walk(CurveSkeleton, EngagementPolicy)` (trochoidal WALK only) | out: kernel `Skeletonize.Apply` → `Fin<CurveSkeleton>` + `Offsetting.Apply` clearance-radius/`Clearance(Point3d probe)` (the ONE shared clearance family, 2D+3D, K1/K2), arcs (trochoidal loop chords — arcs authors earlier in the build order) · in: motion (Adaptive engagement bound), guard (ClearanceAt reach) |

### Tranche 3 — CAM + ADDITIVE + NESTING + FIXTURING

| # | Path | Action | Charter | Entry (+case) | Seams |
|---|---|---|---|---|---|
| 13 | `Toolpath/guard.md` | KEEP + wire | Toolpath · `Rasm.Fabrication.Toolpath` · 2706-2707. Shape-correct interior PRESERVED (swept Minkowski envelope, distinct part-keep/stock-keep-out verdicts, the per-move DESIGN-time floor); WIRED — `Check` consults per committed feed move inside the Cam fold (`Gouge`/`Collision`→typed fault, `Clearance`→substitute lift, `Clear`→commit); reads the CURRENT `StockSnapshot` (per-setup), never the raw blank; the 3 out-seams DECLARED | `Verdict Guard.Check(Move, Part, Stock, Fixture)` | out: skeleton (`ClearanceAt` reach), magazine (`HolderEnvelope`), algebra (Minkowski), workholding (ExclusionZone), kernel `Spatial/index` (BVH broad-phase), owner#atoms (CutterForm swept radius, StockSnapshot) · in: motion (per-move), setups (per-op collision) |
| 14 | `Fixturing/workholding.md` | MOVE (Nesting → Fixturing) + safety rebuild — carries `Nesting/workholding.md`'s content; the source leaves disk | Fixturing · `Rasm.Fabrication.Fixturing` · 2717 (namespace already `Fixturing` on disk). Safety hardening (V3/E9): `Clears`/`Condition` replace 3-point endpoint+midpoint sampling with segment-vs-polygon intersection (the thin diagonal clamp crossing detected); `ForHolding` first-match becomes a TOTAL mapping (`Vise` reachable behind `Clamp`); per-kind footprint SHAPES land as real geometry columns (two-jaw vise, revolved chuck jaw, full-bed vacuum), not a `MarginScale` scalar; `Condition` WIRED into the Cam conditioning fold and composing `NestingOrder` (the re-homed ordering, backward to Geometry2D) | `Fin<Seq<Move>> Workholding.Condition(Seq<Move>, Fixture)`; `ExclusionZone Workholding.Zone(…)` | out: algebra (Offset/Clip, NestingOrder), family (HoldingClass), owner#atoms (Loop) · in: guard (ExclusionZone), motion (Condition), setups (per-setup fixture) |
| 15 | `Fixturing/setups.md` | NEW (the multi-fixture-scheduler mandate realizes) | Fixturing · `Rasm.Fabrication.Fixturing` · 2717,2726,2727. Multi-fixture setup scheduler (V3/V7): `Setup` `[ComplexValueObject]` (Fixture, WCS datum, ReachableOps); a QuikGraph operation-precedence/datum-lineage graph partitions operations across reorientation setups (the NP-hard SEQUENCING half homes here, magazine eviction greedy stays); OWNS the setup→WCS assignment rows (setup k → the dialect's G54-G59.x/G54.1-Pn roster — posting RENDERS what setups assigns, never a posting-side default); admits op-N workholding against the op-N-1 machined `StockSnapshot` | `Fin<SetupSchedule> Setup.Schedule(Seq<Operation>, SchedulePolicy)` | out: QuikGraph (precedence graph), workholding (fixture reach), guard (per-op collision), family (`Machine.AxisCount` reach), owner#atoms (StockSnapshot, input-carried) · in: program (WCS rows), probing (per-setup datum), traveler (setup plan) |
| 16 | `Kinematics/cell.md` | MOVE (Toolpath/kinematics → Kinematics) — carries `Toolpath/kinematics.md`'s content; the source leaves disk | Kinematics · `Rasm.Fabrication.Kinematics` · 2702 (namespace already `Kinematics` on disk). The Robots serial-chain cell HELD (per-manufacturer RAPID/KRL/URScript/VAL3/DRL posts, `Program` look-ahead, the `extern alias R3` boundary exemplar); band-2500→2700; `[03]-[RESEARCH]` tail purge; its jerk/accel planner NAMES the shared motion-dynamics law homed on `machine` | `Fin<FabricationResult.Motion> RobotProgram.Solve(RobotCell, Seq<Move>)` | out: Robots, Rhino3dm (`extern alias R3`), Riok.Mapperly (R3 seam), machine (motion-dynamics law) · in: motion (articulated-arm dispatch) |
| 17 | `Kinematics/machine.md` | NEW | Kinematics · `Rasm.Fabrication.Kinematics` · 2714. The 5-axis machine-tool topology owner (V5, closing the 5-axis gap beside the Robots cell): rotary-axis inverse + TCP/RTCP admission over the `KinematicClass` rotary-topology rows; multi-axis orientation interpolation COMPOSES the kernel one-slerp owner (`MotionInterpolation`), never a second slerp site; HOMES the ONE jerk/accel motion-dynamics law posting `Lookahead` reads as a typed policy shape (the Robots planner + posting planner read it, mapped at the R3 edge so they cannot drift) | `Fin<FabricationResult.Motion> MachineTool.Solve(MachineKinematics, Seq<Move>)` | out: kernel `MotionInterpolation` (slerp), family (KinematicClass rotary rows) · in: motion (multi-axis dispatch), cell/program (motion-dynamics law) |
| 18 | `Nesting/nfp.md` | KEEP + identity fix | Nesting · `Rasm.Fabrication.Nesting` · 2701,2709. The NFP-feasibility true-shape nesting engine PRESERVED (minus E9); `Stock.Of()` hashes discriminant + ALL dimensions through `ContentHash.Of` (the area-only collision poisoning `Remnant.Parent` dies); the 3 raw `XxHash128` mints route the ONE `ContentHash.Of` federation entry; `Ty*1e6+Tx` becomes a `(Ty,Tx)` tuple comparison; the NFP origin-vs-`Anchor` reference-frame incoherence PROVEN or fixed on the golden fixture; the rect-fastpath rides `RectangleBinPack.CSharp` `MaxRectsBinPack` heuristic-sweep (the RectpackSharp re-home landed) | `Fin<FabricationResult.Placement> Nest.Solve(NestPolicy, FabricationInput)`; `Nest.Honor(NestPlan)` | out: algebra (Minkowski), `ContentHash.Of` (identity), RectangleBinPack (fastpath), owner#atoms (Placement/Remnant, reads input.Plan) · in: owner#run · cross-pkg: ← kernel `Processing/flatten.md` `ChartAtlas` (unrolled UV islands + `DistortionReceipt` as true-shape part input — an inbound part-feed seam; the NFP + RectangleBinPack nesting engine stays in-folder); ← kernel `Parametric/develop.md` `DevelopmentReceipt` isometric strips (the SECOND true-shape part feed — the kernel end names nfp the consumer, `Rasm/ARCHITECTURE.md:114`); → `Rasm.Persistence` (Placement/Remnant content keys via `ContentHash.Of` — `placement`/`remnant` `ArtifactKind` enrollment rows on the LANDED `Query/cache#ARTIFACT_BLOB_INDEX` owner); ← `Rasm.Compute` DRL nest-policy score (injected delegate, strata-correct) |
| 19 | `Nesting/stock.md` | KEEP | Nesting · `Rasm.Fabrication.Nesting` · 2710. The rectangular cutting-stock YIELD engine HELD (the scalar exemplar); the `Nesting/stock ← Rasm.Element/MaterialId` ledger row ADDED (wired-undeclared, E11); the `RectangleBinPack.CSharp` central pin rides the Fabrication label (re-group landed); the `[05]-[RESEARCH]` tail purges | `Fin<NestPlan> StockNest.Pack(NestStrategy, Seq<CutPart>)` | out: RectangleBinPack, algebra, `Rasm.Element/MaterialId` (NEW ledger row), owner#atoms · in: nfp (NestPlan via input.Plan) · cross-pkg: → `Rasm.Compute` `NestYield.WasteAreaMm2` (TWO-SIDED, landed — Compute decodes `ElementQuantity.WasteAreaM2`/`NestWasteM2` under the frozen `NestWasteArea` key, `Rasm.Compute/ARCHITECTURE.md:90`; the quantity-bag lowering rides the deferred `FabricationProjector`, `[00]` ruling 3) |
| 20 | `Spec/tolerance.md` | NEW (staged early: surface's Ra→scallop reads its rows) | Spec · `Rasm.Fabrication.Spec` · 2721. The typed GD&T vocabulary author-kernel (V8 — NO admissible OSS library exists): ISO 286 fits (grade/deviation tables as GENERATED rows, never enumerated), ISO 1101/ASME Y14.5 feature-control frames + datum systems, ISO 1302 surface texture (Ra/Rz + lay + process marks), MMC/LMC modifiers — QIF-aligned (ISO 23952); the spec DRIVES process: the Ra/Rz→cutter-corner-radius→constant-scallop stepover/stepdown derivation row (`surface` consumes), the ISO 286 IT-grade→finishing-allowance row (`[V5]` pass planning conditions) | `Tolerance.Frame(FeatureControl)`; `double Tolerance.ScallopStep(RaTarget, CutterForm)`; `double Tolerance.Allowance(ItGrade)` | out: UnitsNet (extended Force/Power/Temp/Angle/Torque), owner#atoms (CutterForm) · in: surface (Ra→scallop), motion (IT→allowance), capability (grade), traveler (spec rows) |
| 21 | `Toolpath/motion.md` | REBUILD | Toolpath · `Rasm.Fabrication.Toolpath` · 2705. Real `(ProcessModality, CutStrategy)` generators (V5 — every stub arm becomes a REAL operation): `Turn` a true ZX radial sweep over a revolved stock envelope (the `BarStock` phantom lands as a real shape whose floor composes the kernel `Analysis/measure.md` `Bounds` enclosing-cylinder fit), `helical` a distinct thread/ramp (the `Turn` alias dies), `Peck` real canned-cycle geometry with dwell/retract, `SliceWalk` a real perimeter/infill/travel orderer; rest-machining reads the input-carried `ResidualStock` (never a motion→removal page edge); `Guard.Check` + `Workholding.Condition` WIRED per feed move; adaptive engagement reads the medial clearance field AND the physics chip-load budget on ONE policy row; the Cam fold produces the conditioned `Motion` result (`[00]` ruling 5 — the `CutProgram` AST is `program`'s, assembled from this `Motion`); the `Option<ArcCenter>` chord feeds the arc-native posting rail (not biarc refit) | `Fin<FabricationResult> Cam.Solve(FabricationPolicy.Cam, FabricationInput)`; `Cam.Generate((ProcessModality, CutStrategy))` (assembles the conditioned `Motion` inside this fold — Guard/Workholding/Magazine execute here) | out: guard (per-move Check), workholding (Condition), cell/machine (dispatch), skeleton (Adaptive clearance), family (Admits), physics (budget), arcs (adaptive arc), algebra, surface (surface arms), partition (partition rows) · in: owner#run |
| 22 | `Toolpath/surface.md` | NEW (OpenCAMLib FEASIBLE) | Toolpath · `Rasm.Fabrication.Toolpath` · 2713. The 3D-surface finishing family (V5): OpenCAMLib owns cutter POSITIONING (drop/push/waterline/adaptive-waterline height sampling — `BatchDropCutter`/`PathDropCutter`/`AdaptivePathDropCutter` `getCLPoints`, `BatchPushCutter` fibers, `Waterline`/`AdaptiveWaterline` `getLoops`, one `setSTL`·`setCutter`·`setSampling`·`run` lifecycle) via the thin `extern "C"` C-shim + source-generated `[LibraryImport]` P/Invoke over the shipped SHARED `libocl` (ALC-firebreak/sidecar posture, content-keyed at the wire; author-kernel SDF drop-cutter K8 is the recorded admission-abandonment fallback); path LAYOUT composes the kernel on-mesh machinery — geodesic-parallel/constant-stepover read the kernel heat distance (`geodesics.md` via `fields.md` `Geodesic`) as `extract.md` `ContourPolicy.MeshScalar` isolines, flowline/morph trace `flow.md` streamlines, `segment.md` cross-fields seed flank/swarf orientation (a Fabrication-side on-mesh re-implementation is the `[V2]` defect); waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family rows; the `CutterForm` axis drives OCL cutter selection at the P/Invoke edge; growth lane: 5-axis simultaneous swarf refinement atop the 3+2/swarf rows | `Fin<Seq<Move>> SurfacePath.Sample(SurfaceStrategy, MeshSpace, CutterForm)` | out: OpenCAMLib (P/Invoke), kernel `geodesics.md`/`extract.md`/`flow.md`/`segment.md` (path layout, K10-K13), kernel `fields.md` SDF (fallback, K8), tolerance (Ra→scallop), owner#atoms (CutterForm), physics (chip-load), System.Numerics.Tensors (SIMD grids) · in: motion (surface arms) |
| 23 | `Toolpath/partition.md` | NEW (SharpVoronoiLib mandate) | Toolpath · `Rasm.Fabrication.Toolpath` · 2705,2723. The Voronoi region-decomposition CAM lane (V5, the `[V1]` realization the kernel supersession left to Fabrication): `VoronoiPlane` Fortune sites + Lloyd relaxation (`Relax(iterations, strength, reTessellate)`, `VoronoiSite.Centroid` the seed) → pocket-region seeds, Lloyd-relaxed stipple/engrave/even-spacing/pen-plot strategy rows; point-site only (polygon medial stays the kernel's); the `extern alias Voronoi` posture carried | `Fin<Seq<Loop>> Partition.Seed(PartitionStrategy, Loop)` | out: SharpVoronoiLib (`extern alias Voronoi`), algebra · in: motion (partition strategy rows) |
| 24 | `Additive/slicing.md` | MOVE (Toolpath → Additive) + kernel-consumer rebuild — carries `Toolpath/slicing.md`'s content; the source leaves disk | Additive · `Rasm.Fabrication.Additive` · 2708 (namespace already `Additive` on disk). Kernel slice-stack CONSUMER (`[V2]b`, the `ARCHITECTURE.md` "when realized" promise FIRED): the `Section`/O(n²) `Chain` author-kernel DIES — the source page's CURRENT fences still author `Slicing.Section`, so the rebuild REPLACES the fence bodies with kernel `SliceStack` delegation (`Meshing/slice.md#Slicing.Apply`), never an edit around them; keeps the FFF/DED `InfillPattern` + shell composition over the kernel-emitted oriented contours (enriched from gsSlicer prior art — open-sheet, overlapping shells); gyroid/TPMS route to the PicoGK voxel lane (the `InfillPattern`-row false collapse corrects); variable/adaptive layer height as kernel slice-stack POLICY rows (Fabrication's named demand, never a Fabrication-side layer engine); the false author-forever seal + `[03]-[RESEARCH]` tail purge | `Fin<AdditiveResult> Slice.Layers(SliceStack, InfillPolicy)` | out: kernel `Slicing.Apply` → `Fin<SliceStack>` (4 layer policies + 5-channel SoA forest, K3), algebra, implicit (gyroid/TPMS route) · in: owner#run (Additive case), production |
| 25 | `Additive/implicit.md` | NEW (PicoGK, THREE lanes) | Additive · `Rasm.Fabrication.Additive` · 2715. PicoGK to depth (V6/V7): `IImplicit → Voxels` TPMS/gyroid/cellular conformal infill (the quartet cannot express TPMS); `Lattice` beam supports + overhang-conditioned scaffolds; SLA/DLP/MSLA grayscale + `.cli` vector layer stacks (`oVectorize → CliIo`, `Vdb2Cli`); voxel offset/shell lightweighting; DECLARES ONCE the net9+native ALC-firebreak/sidecar posture + the content-keyed mesh↔`Voxels` wire (removal composes this declared seam, never a second posture); the `Voxels` stay plane-local (never on a result case, `[00]` ruling 5) | `Fin<Voxels> Implicit.Voxelize(ImplicitOp)`; `CliStack Implicit.Cli(Voxels)` | out: PicoGK (`IImplicit`/`Voxels`/`Lattice`/`CliIo`), kernel mesh↔Voxels wire, `ContentHash.Of` (`.cli`/grayscale keys) · in: slicing (gyroid/TPMS), production (lattice/support), removal (declared voxel seam) |
| 26 | `Additive/production.md` | NEW (lib3mf) | Additive · `Rasm.Fabrication.Additive` · 2716,2725. Build-orientation + 3MF egress (V6): author-kernel orientation optimization on the overhang/bottom-area/contour objective (the overhang/bottom terms read the kernel axis-ranked face decomposition + draft capture, `Analysis/select.md` `Faces`/`Curves.Draft` per candidate orientation, never a hand-rolled normal classifier); machine profiles (build volume, nozzle/vat/laser, kinematics class as rows); lib3mf 3MF core/production/beam-lattice writer via the vendored official C# binding + RID-keyed native asset (`Wrapper.CreateModel` → `AddMeshObject`/`AddBuildItem`, `CMeshObject.BeamLattice()` carrying the PicoGK `Lattice` map, `QueryWriter("3mf")` egress; STL-implied hand-off dies) | `Fin<AdditiveResult> Production.Plan(BuildPolicy, MeshSpace)` | out: lib3mf (3MF), implicit (lattice/support), slicing (planar layer stacks — the counterpart of row 24's `production` in-edge), kernel `Analysis/select.md` (orientation faces, K20), `ContentHash.Of` (3MF key) · in: owner#run (Additive case) |

### Tranche 3 (continued) — EGRESS + TRUTH + DOCS

| # | Path | Action | Charter | Entry (+case) | Seams |
|---|---|---|---|---|---|
| 27 | `Posting/dialect.md` | NEW | Posting · `Rasm.Fabrication.Posting` · 2718. The per-dialect emission generator (V4, data-vs-lowering split — the axis is family's, the FOLDS are here; the byte-lowering half of the `Run(Post)` case): `Emit` a generated total `Switch` over the `PostDialect` GRAMMAR family (`WordAddress` RS-274 \| `Conversational` Klartext/Mazatrol/OSP \| `Additive` Marlin/RepRap + resin/powder layer-stack); canned-cycle expansion (G81-G89/G76 single blocks where admitted, expanded moves where not); macro/subprogram lowering (Fanuc macro-B `#`/WHILE/GOTO, M98/M99, Siemens R-params, Heidenhain Q); code-override resolution; the NC1 steel emit TARGET (the DSTV record tree as emission model, mirroring `Ingress/steel`); rows widen as seed DATA (Siemens 840D, Heidenhain TNC, Okuma OSP, Fagor, Centroid); growth lane: conversational-dialect breadth beyond the seed rows | `GWord Dialect.Emit(PostDialect, GNode)` (generated total Switch; invoked by the `Run(Post{Motion, PostDialect})` case, never a public fold) | out: family (PostDialect grammar columns), steel (NC1 emit model) · in: program (per-family lowering), owner#run (Post case byte-lowering) |
| 28 | `Verify/removal.md` | NEW (PicoGK NC-verify) | Verify · `Rasm.Fabrication.Verify` · 2706 (verify-time Gouge). Voxel material-removal verification (V7): stock `Voxels` minus the accumulated swept-tool volume via PicoGK `BoolSubtract` (a ball-nose sweep IS the capsule beam); emits typed gouge/uncut/overcut/air-cut RECEIPTS against the ACTUAL removed-stock state (owner#atoms-safe payloads, `[00]` ruling 5); PRODUCES the `ResidualStock` field (`[V5]` rest rows read it, input-carried) + content-keyed per-SETUP intermediate `StockSnapshot`s (never one terminal state — `Fixturing/setups` admits op-N against op-N-1, `guard` reads the current snapshot); composes the declared `Additive/implicit` ALC/voxel seam, never a second posture | `Fin<FabricationResult> Removal.Verify(VerifyPolicy, FabricationInput)` — the `Verify` policy case | out: implicit (declared voxel seam, BoolSubtract), owner#atoms (ResidualStock/StockSnapshot mint, CutterForm swept) · in: owner#run (Verify case) · cross-pkg: → `Rasm.Persistence` (snapshot content key via `ContentHash.Of`; the `stock-snapshot` `ArtifactKind` enrollment row lands with this page) |
| 29 | `Spec/capability.md` | NEW | Spec · `Rasm.Fabrication.Spec` · 2722,2729. Process capability (V8): Cp/Cpk/Pp/Ppk with the streaming-moment half on the kernel `Domain/stats.md` owners (`Stat.Of` Welford + its public quantile surface, `StatContext.Tolerance` provenance — probing evidence arrives PRE-FOLDED, a re-derived Welford is the dual-paradigm defect; `Distribution.Of` is kernel-internal, K18) and the distribution-fit + Monte-Carlo tolerance-stackup half over `MathNet.Numerics` (first Fabrication consumer); SPC control-limit rows; the plan-time Cpk GATE (a process row whose Cpk history cannot hold the demanded grade fails admission BEFORE material is cut) PRODUCES the `CapabilityVerdict` minted on `owner#atoms` (the plan reads it input-carried, closing spec→plan→verify→capability; the full `CapabilityReport` stays terminal here) | `Fin<CapabilityReport> Capability.Assess(Seq<ResidualSample>, Tolerance)`; `CapabilityVerdict Capability.Gate(ProcessKind, ItGrade)` | out: kernel `Domain/stats.md` (Stat/Distribution, K18), MathNet (fit/Monte-Carlo), tolerance (grade), NodaTime (SPC stamps), owner#atoms (CapabilityVerdict mint) · in: owner#run (Inspect projection), traveler (capability rows), owner#atoms plan-admission (input-carried); reads probing measured ingress (input-carried) |
| 30 | `Posting/program.md` | REBUILD | Posting · `Rasm.Fabrication.Posting` · 2703,2719,2728. AST growth + content key (V3/V4): the flat `Seq<GWord>` block stream gains canned-cycle nodes with typed R/Q/P slots, subprogram units with repeat counts, macro parameter slots (per-family lowering is node expansion via `dialect.Emit`, never string synthesis); the cut-conditioning author-kernel MATH PRESERVED (`Kerf`/`Simplify`/`Compensate`/`Feedrate`/`Lookahead`, plus `Pierce`/`Tab` for the `Placement` 2D-cut egress — lead-in/pierce-point placement + ordering and retained micro-tab generation, the profile-cutting laser/plasma/waterjet/nest conditioning the F1 word-address/NC1 and the `Toolpath/partition` engrave lane feed; the Placement-vs-Motion asymmetry forbids a `Kerf` pass over the already-conditioned `Motion`, `[00]` ruling 5 / V3); the arc rail REALIZES — kerf/lead/adaptive read `arcs` arc-native offsets + `Loop.Bulges` (`g3.BiArcFit2` retained ONLY for genuinely line-sourced kernel mesh-section chains until the owned Bolton biarc fold lands); CONSUMES `Magazine.Schedule` (real `Tnn`/`M6`/`G43`), `Workholding` conditioning, `Setups` WCS assignment; the neutral `CutProgram` AST is assembled HERE in the `Run(Post)` case body from the `owner#atoms` `Motion` result (`[00]` ruling 5 — NOT in the Cam fold; `motion`↔`program` decouple through the `Motion` type); `CutProgram` gains its content key via `ContentHash.Of` + its `EgressKind` discriminant; a `Parse` round-trip arm carries the NIST RS274NGC modal-group state machine — the five tracked groups (motion · plane · distance · units · feed), the state the `ModalGroup` payload (`ProgramParse` 2719) discriminates over and the round-trip must preserve, without which round-trip is unimplementable (`Parse ∘ Emit = id` on each dialect's admitted subset); the `[03]-[RESEARCH]` tail purges | `Run(Post{FabricationResult.Motion, PostDialect}) → PostedProgram` (the case body: assemble the neutral `CutProgram` AST from the `Motion`, condition, lower via `dialect.Emit`; `PostedProgram` carries lowered blocks + `EgressKind` + `ContentKey`, never the AST); `Fin<CutProgram> Post.Parse(string, PostDialect)` (ingress arm; the disk `Posting` static class renames `Post` with this rebuild — `#Post.Parse` is the target spelling) | out: dialect (Emit lowering), magazine (Schedule), workholding (conditioning), setups (WCS), arcs (arc-native kerf/lead), algebra (NestingOrder), geometry3Sharp (line-sourced biarc only), owner#atoms (Move, Motion result, EgressKind), `ContentHash.Of` (program key) · in: owner#run (Post case), traveler (program facts) · cross-pkg: → `Rasm.Persistence` (CutProgram durable rows on the LANDED `Query/cache#ARTIFACT_BLOB_INDEX` owner; the `cutprogram` `ArtifactKind` enrollment row lands with this page); → kernel `Drawing/pack.md` `toolpath` `PackKind` (committed motion residency, K7, a ledger row never a Fabrication encoder) |
| 31 | `Verify/probing.md` | NEW (the in-process-metrology mandate realizes) | Verify · `Rasm.Fabrication.Verify` · 2720. In-process metrology (V7): touch-probe cycle vocabulary (G31/G38 rows on the posting AST), work-offset/tool-length metrology, measured-feature receipts; datum best-fit COMPOSES the kernel `Processing/register.md` `AlignKind` ICP dispatcher under one `AlignmentPolicy` (probed points → nominal, never a hand-rolled least-squares); measured-vs-nominal folds through the kernel `Analysis/measure.md` `ConformanceMetric` over `ResidualSample`; QIF (ISO 23952) MeasurementResults feed `Spec/capability`; measured ingress is HONEST — probe values enter through the same AppHost decoded-telemetry seam or typed manual-entry rows (the F2 dry-run composes on fixture-synthetic measurements) | `Fin<FabricationResult> Probe.Inspect(InspectPolicy, FabricationInput)` — the `Inspect` policy case | out: kernel `register.md` (ICP, K16), kernel `measure.md` (ConformanceMetric, K17), program (G38 rows), setups (per-setup datum), NodaTime (receipt stamps) · in: owner#run (Inspect case), capability (measured ingress) · cross-pkg: ← `Rasm.AppHost` decoded probe telemetry (the APPHOST-BRIEF MTConnect.NET row, second consumer) |
| 32 | `Documentation/projection.md` | MOVE (Posting → Documentation) + kernel-consumer rebuild — carries `Posting/projection.md`'s content; the source leaves disk | Documentation · `Rasm.Fabrication.Documentation` · kernel-routed. Kernel `DrawingProjection` CONSUMER (`[V2]c`/`[V3]`, RULED): the BSP interior (centroid-classified facets, per-edge average depth, undeduped mesh-edge soup, `1e6` eye) DIES — the source page's CURRENT fences still author the `Hlr.Solve` BSP visibility solver, so the rebuild REPLACES the fence bodies with kernel `DrawingProjection` delegation (`Drawing/view.md#View.Apply`), never an edit around them; the PHANTOM `Predicate.Orient2D` import drops (0 fence calls, the page's own forbidden defect); KEEPS the `BooleanSolid` watertight arm (kernel `Arrangement.Apply` compose-seam reading `ArrangementResult.Boolean.Solid`); PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at (`UIBRIEF [V7]`(b)) | `Fin<FabricationResult> Hlr.Solve(FabricationPolicy.HiddenLine, FabricationInput)` | out: kernel `Drawing/view.md` `View.Apply` → `Fin<DrawingProjection>` (K6), kernel `Meshing/arrangement.md` `Arrangement.Apply` (watertight via `ArrangementResult.Boolean.Solid` — the arrangement seam row HOLDS, the healing seam row re-points off `Processing/repair`; K5), owner#atoms (Loop) · in: owner#run (HiddenLine case), traveler (views) · cross-pkg: → `Rasm.AppUi/Render` (`HiddenLineResult`, supersession insulated at the receipt) |
| 33 | `Documentation/traveler.md` | NEW | Documentation · `Rasm.Fabrication.Documentation` · reserved. Setup sheets + shop travelers (V8): content-keyed shop documents composing the projection views, magazine tool lists, workholding/setups plans, program facts, and spec rows (the typed document MODEL only — sheet/annotation RENDERING rides the artifacts-plane seam); the widest fan-in node (validates every upstream owner exposes a typed receipt). `Traveler.Assemble` folds into the entry as the `Run(Document{Seq<FabricationResult>})` case (a re-invocable terminal transform over a completed result-set, NOT a second public fold) | `Run(Document{Seq<FabricationResult>}) → TravelerDocument` (the case body: compose views/tool-lists/fixture-plans/program-facts/spec-rows into the content-keyed document model) | out: projection (views), magazine (tool lists), workholding/setups (fixture plans), program (facts), tolerance/capability (spec rows), NodaTime (stamps), owner#atoms (EgressKind), `ContentHash.Of` (doc key) · in: owner#run (Document case) · cross-pkg: → `Rasm.Persistence` (traveler durable rows on the landed artifact index; the `traveler` `ArtifactKind` enrollment row lands with this page) |

### Tranche W — THE WIDENED PLANES (rows 34-59; STRUCTURE LANDED — all 26 pages on disk at owner depth, 88-315 LOC, zero stubs; interiors deepen per row where the recorded residuals name them)

| # | Path | Action | Charter · entry · seams |
|---|---|---|---|
| 34 | `Process/derivation.md` | NEW (CROWN JEWEL) | The design-to-fabrication orchestrator: `Run(Derive{AdmittedComponent, DerivePolicy}) -> FabricationPlan` — manufacturability -> process routing -> fleet match -> setup/assembly -> programs -> documentation; registers `FabricationProjector : IElementProjection` (`Element/Projection/projection.md#IElementProjection`, the one-registration-row hook `Materials/Projection/component.md`); the widest FORWARD fan, `Document`'s mirror. in: element, manufacturability, fleet, setups/assembly, estimation · out: Element registration (`GraphDelta` lowering), owner#run (Derive arm) — builds LAST |
| 35 | `Ingress/element.md` | NEW | The 4th `Ingress.Admit` arm: `AdmittedComponent` from `ElementGraph.Bake` + `RepresentationContentHash` + property bags + Materials `Ply`/`Layered` composition + Bim connection bags (`Semantics/connection` Detail reader, `Connect.Realizing` topology) + `DetailSchema.Realization`. in: Element/Materials/Bim seams · out: derivation, assembly, manufacturability |
| 36 | `Spec/manufacturability.md` | NEW | Cross-modality DfM on ONE surface: draft/min-wall/undercut/tool-access/bend-radius/weld-access verdicts + process routing + stackup feasibility; `Manufacturability.Assess(AdmittedComponent, Seq<ProcessModality>) -> ManufacturabilityVerdict`. GROWTH row: feature recognition gates on THREE absent kernel primitives (signed per-edge concavity, analytic-form classifier, exposed face-adjacency) — recorded kernel counterpart demands, never a page. in: tolerance, capability, physics, fleet · out: derivation |
| 37 | `Geometry2D/curves.md` | NEW | The parametric-curve substrate: kernel exact offsets with self-intersection trim (`Parametric/curve.md` `ParametricOp.Offset -> Offsets(Curves, Receipt, TrimmedCrossings, KeptSegments)`), station frames (`Stations -> StationField`), `Fill`/`Divide`, NURBS refit (`Reconstruct -> Refit`; `nurbs.md` `CurveThrough(FitPolicy)`, `PerpendicularFrames`). in: kernel curve/nurbs (K24) · out: turning, motion 2.5D, program stations |
| 38 | `Toolpath/turning.md` | NEW | `LatheOp [Union]` {FaceRough, TurnRough(shape-cycle schedule), ProfileFinish, Groove(multiplex), Thread(ISO depth degression d·sqrt(n/N), flank infeed), Part}; tool-nose-radius comp 9-quadrant algebra; revolved envelope via K14; spindle {ConstantRpm, Css(clamp)} (clamp toolpath-side, emission dialect's); G71/G72/G73 pass folds — supersedes the `helical: => Turn(...)` alias placeholder in motion. in: curves, physics, cuttingdata · out: motion dispatch, program |
| 39 | `Toolpath/wire.md` | NEW | Wire-EDM: cycle `[Union]` {Contour, TaperContour, FourAxis(UV/XY sync), NoCorePocket, Collar}; per-pass compound offset wireR+sparkGap(n)+overburn(n)+stock(n) with power taper; taper guide-planes + corner modes {Square, Conic, ConstantRadius}; corner slowdown Ls=4R·cot(theta/2) + wire-lag; slug/tab `[Union]` {GlueStop, Tabs, RetraceRelease, NoCore}; thread-point admission; rides `Cam(Erosion, strategy)` — the `Erosion` budget's consumer. in: physics (Erosion), algebra · out: program |
| 40 | `Toolpath/link.md` | NEW | Rapid-travel minimization: MST/DFS tour (QuikGraph `MinimumSpanningTreeKruskal/Prim` + `TreeDepthFirstSearch`), A*/Dijkstra obstacle-routed retracts over guard clearance + kernel `SpatialIndex` — kills the input-order peck emission and the fixed up-over-down lift. in: guard, kernel Spatial, QuikGraph · out: motion (reorder fold), program |
| 41 | `Toolpath/bevel.md` | NEW | `BevelType [Union]` {I,V,A,Y,K,X}; per-edge (feed, tilt, kerfOffset, arcVoltage) coupled rows; waterjet twin-tilt taper-lag; THE THC/Z-law custodian (pierce-height %, arc-voltage loop with corner sample-hold/anti-dive, small-hole disable — ONE owner, never split with program); groove-prep feeds `Joining/weld`. in: arcs, physics, family · out: program (Placement conditioning), weld |
| 42 | `Kinematics/fleet.md` | NEW | Machine-capability registry: `Fleet.Capable(AdmittedComponent, MachineFleet) -> Seq<MachineMatch>` — envelope/axis/spindle/tool-capacity/material/grade join over cell+machine+magazine+capability. in: cell, machine, magazine, capability · out: manufacturability, derivation |
| 43 | `Additive/scanpath.md` | NEW | LPBF hatch/scan: hatch `[Union]` {Meander, Stripe(width/overlap/stagger), Island, Hexagon}; angle recurrence theta(n)=(theta0+n·66.7°) mod 180; sorter = direction-op x ordering-op axes; up/down/in-skin `DownSkin(n) = R(n) \ Union R(n-k)`; `LayerGeometry` 3-case; island sequencing; consumes the `Powder(LaserPower, HatchSpacing, ScanSpeed)` budget (PySLM prior-art shape). in: kernel SliceStack (K3), physics · out: implicit `.cli` egress |
| 44 | `Additive/support.md` | NEW | PLANAR support algebra: overhang layer-diff `layer(i) \ offset(layer(i-1), tan(alpha)·h)`; top-down accumulation + interface carve; TREE influence-area graph with {Slow, FastSafe, Fast, Collision} avoidance cache; bridge anchor-coverage state machine {Outside, Hanging, Anchored, Supported} — implicit keeps the voxel/lattice lane. in: kernel SliceStack, algebra · out: slicing, production |
| 45 | `Nesting/remnant.md` | NEW | Offcut lifecycle: `Remnant.Reconcile(Seq<Remnant>, RemnantInventory) -> RemnantPlan` — cross-job reuse, stock reconciliation, `Remnant.Parent` lineage (the owner#atoms type gains its owner); reuse reduces the landed Compute WasteArea rollup. in: nfp, stock · out: Compute rollup, nfp (remnant as stock) |
| 46 | `Nesting/linking.md` | NEW | Common-line/chain/bridge cutting: `CommonLine` (collinear opposite-wound pairs within kerf tolerance, cut once), `ChainCut`, `Bridge/AutoBridge`, `SkeletonCutUp` — collision-checked graph edits over the `Placement`; pierce-count/rapid-length objective; one pierce/lead per chain. in: nfp Placement, algebra · out: program |
| 47 | `Fixturing/assembly.md` | NEW | Join-PRECEDENCE planning: `Assembly.Sequence(AdmittedComponent, AssemblyPolicy) -> AssemblyPlan` — QuikGraph precedence + datum lineage + per-joint access/clamp keep-out; consumes the Materials joining VOCABULARY (`Component/{joint,fastener,connector}` — AWS D1.1 `GrooveGeometry`/`GroovePrep`/`ConnectorPlate`/`PlateStock`), never re-mints. Folder charter widens: keep-out + setup + assembly planning. in: setups, workholding, element, Materials seams · out: sequence, traveler, derivation |
| 48 | `Posting/optimization.md` | NEW | `Optimize.Feeds(CutProgram, OptimizePolicy) -> Fin<CutProgram>` — MRR-adaptive feedrate, HSM corner smoothing, block-cap compaction; physics chip-load + motion engagement inputs. in: program AST, physics, family (block cap) · out: program, estimation |
| 49 | `Verify/estimation.md` | NEW | `Estimate.Of(FabricationResult) -> CostReceipt` — cycle-time from simulate, material from nest WasteArea + stock, tooling from wear, machine rates; extends the LANDED Compute rollup with cost. Receipts only, no fault arms. in: simulate, nfp/stock, wear · out: Compute, traveler, derivation |
| 50 | `Verify/audit.md` | NEW | Additive layer-stack pre-flight: per-layer connected components over `Span2D`; `LayerDefect [Union]` {Island, ResinTrap, SuctionCup, OverhangArea, TouchingBound}; cross-layer adjacency graph; typed receipts on the `Verify` case. in: implicit/slicing stacks, HighPerformance · out: owner#run (Verify) |
| 51 | `Verify/simulate.md` | NEW | Program-level controller simulation: the modal-state execution walk over the parsed `CutProgram` (the row-30 five groups widened to the full RS274 group census AS execution state); per-block time integration under the ONE `Kinematics/machine` motion-dynamics law (never a second planner); axis-envelope/overtravel verdicts; dwell/tool-change/pierce accounting; cycle-time receipt. in: program Parse, machine, family · out: estimation, traveler quoting |
| 52 | `Tooling/wear.md` | NEW | `Wear.Predict(ToolAssembly, CuttingData, Material) -> ToolLife` — Taylor VB flank wear + condition-based RUL from DECODED MTConnect telemetry + cross-modality consumable state (inserts/nozzles/optics/dies); MathNet fit substrate (the `Fit.Line` catalog row lands with this page; `MathNet.Optimization` is NOT in the pinned package — never named). in: cuttingdata, magazine, AppHost telemetry · out: magazine Schedule, estimation |
| 53 | `Documentation/report.md` | NEW | The as-built quality record MODEL: FAI/mill-cert/weld-inspection composing measured features + Cp/Cpk + conformance, content-keyed via the `Run(Document)` family; rendering rides the artifacts seam — MODEL only. Receipts only. in: probing, capability, tolerance · out: Persistence enrollment |
| 54 | `Forming/sheet.md` | NEW | THE unfold owner: bend projection `(A,R,T,K) -> {BA, BD, OSSB, flatDelta}` with `BA=(pi/180)·A·(R+K·T)`, inverse-K coupon back-solve, Y-factor, DIN 6935; `KFactorTable` interpolation rows; `FlatPattern.Unfold` = tangent-face adjacency -> MST -> per-bend neutral-fiber substitution as the OVERLAY over kernel `Parametric/develop` (`StripField`, `DevelopOp.Decompose` — never a second unroll); relief/corner/hem `[Union]`s; three feeds: DSTV `KA`, Materials `ConnectorPlate`/`PlateStock`, nfp flat-pattern. NEVER a DXF writer (AppUi's). in: kernel develop/flatten, steel, Materials · out: nfp, brake, program |
| 55 | `Forming/brake.md` | NEW | `BendSequence.Plan` best-first over the bend-feasibility matrix (back-gauge reach, collision, gauge occlusion; tool-change/flip soft cost — the setups QuikGraph posture); die selection V=f·T, Ri≈0.16V, min flange b>=c(A)·V; tonnage F=(C·Rm·S²·L)/(V·1000); `BendMethod [SmartEnum]` {Air, Bottoming, Coining}; springback overbend; back-gauge positioning. in: sheet, physics FormBudget · out: program (bend program), traveler |
| 56 | `Forming/tube.md` | NEW (research-gated) | XYZ -> YBC/LRA centerline fold, per-bend springback+elongation carry, CLR/mandrel/wiper admission, coping/fishmouth saddle development (kernel intersect+develop into the profile-cut egress). Vocabulary/entry/seams land now; the deep interior states its ONE bounded research lane on the row. GROWTH lane recorded: `composite.md` (draping + AFP courses over K10-K13) admits last on a named consumer. in: sheet, kernel intersect/develop · out: program |
| 57 | `Joining/weld.md` | NEW | Joint x prep `[Union]`s over the Materials-owned `GrooveGeometry`/`GroovePrep`; multi-pass bead-stack fill fold; weave rows; heat input `HI=eta·60·V·I/(1000·v)` consuming the landed `Deposition(PowerW, WireFeedRate, Standoff, InterpassTemp)` budget — its FIRST consumer; torch frames via K19; egress = torch `Motion` through `Cam(Deposition)` + the Robots cell arc rows. in: Materials joint seams, physics Deposition, bevel groove-prep · out: cell posting, sequence |
| 58 | `Joining/sequence.md` | NEW | Distortion ordering {Backstep, SkipWeld, Balanced, Block}; tack plan; interpass-temperature scheduling (`Deposition.InterpassTemp`'s consumer); consumes assembly's `AssemblyPlan`. in: weld, assembly · out: cell posting, traveler |
| 59 | `Joining/procedure.md` | NEW | WPS/PQR essential-variable rows + the heat-input compliance gate feeding `Spec/capability`; QIF-aligned. in: weld, sequence · out: capability, report |

Widening build order (leaf-before-consumer): curves -> {turning, wire, bevel, link} · sheet -> brake -> tube · weld -> sequence -> procedure · scanpath/support · remnant/linking · wear · fleet -> manufacturability · element -> assembly · simulate -> estimation · report -> **derivation LAST** (composes everything). Strengthen-rows ride their owning existing rows, never new pages: seam placement + comb routing + monotonic ordering -> motion's SliceWalk arm (row 21); cooling min-layer-time redistribution -> program as a `Lookahead`-sibling pass (row 30); Arachne variable-width beading -> slicing shells reading the K1 medial clearance RADIUS (row 24); adaptive infill DENSITY -> one field-policy row on slicing.

## [03]-[FAULT_REGISTRY] — one 2700-century block per folder (frozen 2701-2710 + folder-grouped growth 2711-2729)

`FabricationFault` stays ONE `[Union]` on `FaultBand.Fabrication = 2700`. Layer 1 (frozen 2701-2710) retains wire codes per the `.archive` build-ON law + the landed artifact-index persisted-decode constraint; every legacy arm RETYPES its `string Detail` to a typed payload. Layer 2 (2711-2729) allocates folder-grouped across two growth tiers (first tier 2711-2722, second tier 2723-2729 — the distinct-mechanism arms split off broader arms), sized by the growth law. Folder = cluster is mechanically checkable within each tier (a code maps to exactly one folder; each folder's arms are a contiguous run per tier); the non-contiguous legacy codes are the recorded frozen exception. Disk-confirmed against the landed `faults.md` arm table: NoFit→Nesting (2701), Unreachable→Kinematics (2702), KerfCollision→Geometry2D (2703), OpenLoop→Process cross-cutting (2704), InadmissiblePair→Toolpath (2705) — any "2701-2704 as Process arms" re-attribution is REFUTED on disk.

| Folder | Block | Arms (code · typed payload) |
|---|---|---|
| Process | 2700 owner + cross-cut | `OpenLoop` 2704 `(FabConcern concern, int primitive)` — the ONE cross-cutting fabrication-contract arm (disk producers motion·nfp·skeleton·program), recorded exception |
| Nesting | 2701, 2709, 2710 (legacy) | `NoFit` 2701 `(int part, Seq<double> triedRotations)` · `StockOverflow` 2709 `(int unplaced, int sheets)` · `Nest` 2710 `(NestFault kind, int part)` |
| Kinematics | 2702, 2714 | `Unreachable` 2702 `(JointDiagnostic diag, int target)` · `AxisSingularity` 2714 `(MachineAxis axis, double angle)` — RTCP/rotary singularity distinct from reach (`MachineAxis` the landed axis carrier) |
| Geometry2D | 2703 | `KerfCollision` 2703 `(Point3d at, double kerf)` — re-attributed to the kerf-compensation owner (`arcs`), off Posting |
| Toolpath | 2705-2707, 2713, 2723 | `InadmissiblePair` 2705 `((ProcessModality, CutStrategy) pair)` · `Gouge` 2706 `(Point3d point, CutterForm tool)` · `Collision` 2707 `(ExclusionZone zone)` · `SampleStalled` 2713 `(SurfaceStrategy strategy, int iteration)` NEW — OpenCAMLib drop-cutter non-convergence · `PartitionDegenerate` 2723 `(PartitionStrategy strategy, int sites)` NEW — the `Toolpath/partition` (row 23) SharpVoronoiLib Fortune/Lloyd tessellation degenerates/empties (collinear sites, coincident points); distinct from `SampleStalled`'s OCL surface-sampling stall |
| Additive | 2708, 2715, 2716, 2725 | `NonManifoldSlice` 2708 `(int layer, int openChains)` · `VoxelFault` 2715 `(ImplicitOp op, VoxelBudget budget)` NEW — `budget` = bbox + resolution cap, a typed carrier (the retype-to-typed payload law binds Layer-2 growth arms, not only the frozen legacy arms) · `OrientationInfeasible` 2716 `(int overhangs, double bestScore)` NEW · `Unsupported3mfExtension` 2725 `(ThreeMfExtension extension, EgressKind target)` NEW — the `Additive/production` (row 26) lib3mf writer cannot express a required extension (beam-lattice/production) for the target profile; a native egress fault distinct from the `ImplicitOp`-scoped `VoxelFault` and the orientation arm (a raw lib3mf write error lifts to this arm at the boundary) |
| Ingress | 2711 | `IngressTranslation` 2711 `(SourceKind kind, SourceLocus locus)` NEW — the ONE polymorphic ingress fault (profile/solid/steel discriminated by `kind`); `locus` is a discriminated union over the three source sites (DXF entity handle · OCCT shape id · DSTV block+line), so the fold stays single-arm with a typed carrier, never the weak-string escape hatch the retype-to-typed payload law forbids on Layer-2 arms too |
| Tooling | 2712, 2724 | `MachinabilityUnknown` 2712 `(Material material, Operation op)` NEW — no cutting-data row and no admitted formula fallback · `NoToolForOp` 2724 `(Operation op, CutterForm required)` NEW — the `Tooling/magazine` (row 5) `Schedule` fold exhausts the assembly set with no form/diameter match for the operation; a SCHEDULING failure orthogonal to `MachinabilityUnknown`'s missing-`kc`-DATA failure |
| Fixturing | 2717, 2726, 2727 | `SetupInfeasible` 2717 `(int operation, int triedSetups)` NEW — no feasible setup assignment / operation unreachable in every fixture · `DatumLineageBroken` 2726 `(SetupChain chain)` NEW — the `Fixturing/setups` (row 15) QuikGraph datum-lineage graph is cyclic or references an un-machined/un-datumed feature; the graph's acyclicity/reachability check is its producer, a PRECEDENCE failure distinct from `SetupInfeasible`'s kinematic-reach infeasibility · `ClampOnMachinedFace` 2727 `(int operation, Point3d at)` NEW — the op-N workholding admits against the op-N-1 machined `StockSnapshot` (rows 15,28) and the clamp lands on material already cut away; the typed verdict of the snapshot keep-out (V3/V7 — the plan-time defect the snapshot catches), a SAFETY/scrap fault distinct from feasibility and from the per-move `Collision` 2707 |
| Posting | 2718, 2719, 2728 | `DialectUnsupported` 2718 `(PostDialect dialect, GNodeKind node)` NEW — a required cycle/macro the grammar cannot express or degrade · `ProgramParse` 2719 `(int line, ModalGroup group)` NEW — G-code ingress modal-group violation · `BlockCapExceeded` 2728 `(PostDialect dialect, int blocks, int cap)` NEW — the lowered program overruns the dialect's `block cap` capability column (family row 2); SPLIT off `DialectUnsupported` because a program-total-SIZE overflow carries no offending `GNodeKind` — forcing one is the weak-payload defect the typed-payload law forbids (F1 routes block-cap HERE) |
| Verify | 2720 (+2706 verify-time) | `ProbeOvertravel` 2720 `(Point3d at, double limit)` NEW — the `Verify/probing` (row 31) in-process-metrology fault; a detected verify-time gouge routes the shared `Gouge` 2706 |
| Spec | 2721, 2722, 2729 | `ToleranceUnsatisfiable` 2721 `(FeatureControl frame, double achievable)` NEW · `CapabilityShortfall` 2722 `(ProcessKind process, double cpk, double demanded)` NEW — the plan-time Cpk gate (the `CapabilityVerdict` fail arm) · `StackupExceeded` 2729 `(ToleranceChain chain, double accumulated, double bound)` NEW — the `Spec/capability` (row 29) Monte-Carlo tolerance-stackup accumulates past the assembly bound though each feature is achievable and each process capable; an ASSEMBLY-level accumulation failure orthogonal to the per-feature `ToleranceUnsatisfiable` and the per-process `CapabilityShortfall` |
| Documentation | reserved (empty) | projection routes the kernel `GeometryFault.ProjectionFault(EdgeKind, int)` cluster 2436-2439 (`Rasm.Numerics`, raised by `Rasm.Drawing` `View.Apply`); traveler is content-keyed assembly with no fault producer |

Tier 3 (the widening) is LANDED at 2730-2746 — 17 folder-grouped arms (46 total on the band; the landed `faults.md` arm table is the authority), each a typed payload per the registry law (`MachineAxis` supersedes `RotaryAxis` as the axis carrier; `SourceLocus` widens to 4 loci); estimation/report/audit-clean paths emit receipts, no arms; simulate owns the envelope/overtravel arms; next free offset `+47` (2747). Growth law: a new concern is one arm on its folder's block, never a new band; `Code` derives `FaultBand.Fabrication + n`; message strings for 2701-2710 UNCHANGED; 2730-2799 headroom covers every widening candidate.

## [04]-[SEAM_LEDGER] — both directions, one anchor per seam, counterpart obligations, stale re-points

**Intra-package (arrow = composes).** The compose graph is a DAG; every edge lands equal-or-earlier in the `[09]` build order (a tranche never depends on a later tranche — the `owner#run` dispatch arms realize in their plane's own tranche). Listed order within a tranche IS dependency order, so consumers author against landed foundations. The WIRED-PIPELINE law realized (E2 killed): `owner#run → Cam`; `Cam → {guard.Check per feed move, workholding.Condition, magazine.Schedule consult}` producing the conditioned `Motion`; `Run(Post{Motion, PostDialect}) → {program AST-assembly + condition + dialect.Emit, magazine.Schedule, workholding conditioning, setups WCS}` (egress tranche — program reads the `owner#atoms` `Motion` — `[00]` ruling 5, NO `motion→program` edge); `Run(Document{results}) → traveler → {projection, program, magazine, setups, tolerance, capability}` (egress tranche, backward); `workholding.Condition → algebra.NestingOrder` (the re-home that BREAKS the workholding→posting cycle the CAM and egress tranches would otherwise form); `guard → {skeleton.ClearanceAt, magazine.HolderEnvelope, workholding.ExclusionZone}`; `motion.Adaptive → skeleton`; `setups → {workholding, guard}`; `removal → implicit` (the declared voxel seam — implicit ordered before removal); `probing → program` (program ordered before probing — probing lands the G38 rows on the posting AST); `capability.Gate → owner#atoms(CapabilityVerdict)` (truth-tranche mint, read input-carried at plan).

Owner two-node discipline HOLDS: `owner#atoms` (Loop+Bulges, Edge3, ArcCenter, Move, PartTransform, ProjectionDir, CutterForm, ResidualStock, StockSnapshot, CapabilityVerdict, EgressKind, FabricationInput/Policy/Result, ContentKey seed) is upstream-only — every plane reads it, it reads only family's leaf axes + kernel; `owner#run` is terminal (8-arm dispatch: Cam·HiddenLine·Nest·Additive·Verify·Inspect·Post·Document) — composes plane kernels, nothing composes it; the flow is `owner#run → planes → owner#atoms → family(leaf)`, linear and acyclic, so the physical page never splits. **FIVE cycle-breaks:** `ProjectionDir` on owner#atoms breaks the former `owner↔projection` (the disk `owner.md` imports it downstream); `ResidualStock` input-carry breaks `motion→removal→program→motion`; `CutterForm` on owner#atoms breaks `owner→cuttingdata→magazine→owner` (the `magazine.md` `HolderEnvelope→Loop` anchor, `[00]` ruling 2); `NestingOrder` re-home to `Geometry2D/algebra` breaks `workholding→posting` (a CAM⇄egress-tranche cycle otherwise — the re-home stands under the merge); `CapabilityVerdict` input-carry breaks `capability→plan` (produced in the truth tranche, read as next-run input, identical to `ResidualStock`). The `[00]` ruling-5 result-payload discipline is the SIXTH latent-cycle guard: no `FabricationResult` egress case carries a plane-internal type, so `owner#atoms` never gains a forward edge to any plane (the disk-true `motion`-produces-`Motion` / `program`-reads-`Motion` decoupling is preserved). Full acyclicity walk in `[09]`.

**Signature-lock — kernel seam member spellings, K1-K23 TABLE (locked against the LANDED kernel pages under `libs/csharp/Rasm/.planning/` — page `#Symbol` anchors, never line pins).** Every page signature-locks these spellings and NEVER re-derives the interior; every entry threads the kernel `Op` value key with `Eff<Env>` carriage (K21), registers on the `IValidityEvidence`+`ValidityClaim` fold reaching `OpAcceptance` with zero oracle edits (K22), and rides `AtomProjection`/`ProjectionRow` output (K23). Each spelling re-verifies against the landed kernel fences before its first composition — the kernel is planning-scoped, so the fences are member truth; `assay api` covers restored external assemblies only.

| # | Kernel member (locked signature) | Namespace · fault | Consuming page(s) | Ruled disposition carried |
|---|---|---|---|---|
| K1 | `Fin<OffsetResult> Offsetting.Apply(OffsetOp, Op? key = null)` — cases `Skeleton · Offset(JoinType×EndType corner rows) · Medial · Minkowski · Weighted · Clearance`; per-point clearance **RADIUS** first-class result field; the arbitrary-probe query is the `OffsetOp.Clearance(Polyline, Point3d, OffsetPolicy)` CASE → `OffsetResult.Probe` (the `Clearance(Point3d)` METHOD is `CurveSkeleton`'s on `Meshing/skeleton.md`) | `Rasm.Meshing` (`Meshing/offset.md`) · 2416-2419 | `Geometry2D/arcs`, `Toolpath/skeleton`, `Toolpath/motion` | **[V10]a kerf split** — kernel owns exact corner-row assembly + medial; Fabrication owns pure kerf compensation in arc space over CavalierContours ON TOP (`Meshing/offset.md#Offsetting.Apply`) |
| K2 | `Fin<CurveSkeleton> Skeletonize.Apply(SkeletonOp, Op? key = null)` — COMPOSES K1's clearance family (same RADIUS + `Clearance(Point3d probe)`, ONE result family across 2D/3D) | `Rasm.Meshing` (`Meshing/skeleton.md`) · `SkeletonStalled 2417`/`CollapseStalled 2418` | `Toolpath/skeleton` (3D MCF curve-skeleton) | **[V10]d + [V2]a** — `Skeleton.cs` DIES UNCONDITIONALLY (`Meshing/skeleton.md#Skeletonize.Apply` — mints ZERO new clearance types, composes `offset.md`'s family verbatim); page keeps ONLY the trochoidal WALK over the kernel clearance field |
| K3 | `Fin<SliceStack> Slicing.Apply(SliceOp, Op? key = null)` — ALL FIVE `LayerPlan` height-law rows OPEN (`Uniform` · `Adaptive` · `BySlope` · `SupportInterface` · `AtElevations`); oriented contours; typed OPEN-chain rows; 5-channel SoA forest wire | `Rasm.Meshing` (`Meshing/slice.md`) · `SectionFault 2425` | `Additive/slicing` | **[V2]b** — `slicing.Section` + O(n²) `Chain` DIE; the `ARCHITECTURE.md` "when realized" promise REALIZED (`Meshing/slice.md#Slicing.Apply` — the slice-stack OWNER; intersect composes UNDER it); layer rows are kernel policy seeds, never a Fabrication layer engine |
| K4 | `Fin<IntersectResult> Intersection.Apply(IntersectOp, Op? key = null)` — `IntersectOp.PlaneMesh` Section fold; oriented `Chain` | `Rasm.Meshing` (`Meshing/intersect.md`) · 2424 | `Additive/slicing` (section primitive under K3) | consumed under K3 |
| K5 | `Fin<ArrangementResult> Arrangement.Apply(ArrangementOp, Op? key = null)` — `ArrangementOp` `[Union]` cases `MeshBoolean · PlanarOverlay · CellComplex` over the `BooleanOp` `[SmartEnum<int>]` (`Union · Difference · Intersection`); the watertight solid is the `ArrangementResult.Boolean(MeshSpace Solid, BooleanReceipt)` result field (frozen via `MeshEdit.ToSpace` — no `ToMesh` member exists on the owner); Manifold tier behind `ArrangementPolicy.ScaleCeiling` | `Rasm.Meshing` (`Meshing/arrangement.md`) · 2420+2423 | `Documentation/projection` (watertight `BooleanSolid` arm) | **[V2]e** — `BooleanOp` is RE-HOMED on `Meshing/arrangement.md#Arrangement.Apply` (off `Processing/repair`; the managed exact arrangement is the correctness rail, `manifoldc` only past `ScaleCeiling` with `NativeAssetMissing` 2423); `projection`'s Healing import re-points to `Rasm.Processing` (`Processing/repair.md#HealOp`) |
| K6 | `Fin<DrawingProjection> View.Apply(ViewOp, Op? key = null)` — `HiddenLine · Silhouette · Section · Outline`; exact analytic Appel QI (zero sampling) | `Rasm.Drawing` (`Drawing/view.md`) · `ProjectionFault(EdgeKind, int)` 2436-2439 (`Rasm.Numerics`) | `Documentation/projection` | **[V2]c / [V3]** — the BSP interior DIES; "beside the in-folder BSP solver" clause dies (`Drawing/view.md#View.Apply` — the dead BSP form is named on the page); page PRESERVES the `HiddenLineResult` receipt shape AppUi is insulated at |
| K7 | `Fin<EncodedGeometry> Encode.Apply(PackOp, Op? key = null)` — `PackKind` carries the **`toolpath`** row + active-channel set | `Rasm.Drawing` (`Drawing/pack.md`) · `EncodingFault` 2444-2447 | `Posting/program` (committed motion residency) | **[V13]** — the channel-set alignment lands as ONE ledger row (Compute decodes residency, AppHost locks `EncodingKind.Toolpath`); NEVER a Fabrication-side encoder |
| K8 | `SignedDistanceFromMeshCase` (landed lane: `Spatial/fields.md` → `Meshing/reconstruct.md` GWN → `Spatial/index.md` BVH winding) — the ONE distance-field lane | `Rasm.Spatial` (`Spatial/fields.md`) | `Toolpath/surface` (OCL fallback) | **[V8]** — the kernel mints no second SDF owner (`Spatial/fields.md#SignedDistanceFromMeshCase` delegates `MeshSdf`); PicoGK stays Fabrication's VOXEL lane, never a second kernel SDF owner |
| K9 | `ContentHash.Of` — seed-zero federation entry (`XxHash128.HashToUInt128` reached THROUGH it) | `Rasm` (`Domain/identity`) | ALL egress: `program`, `nfp`, `implicit`, `production`, `removal`, `traveler`, `magazine`, `stock` | **[SEAM_AND_ENTRY_LAW]** — the ONE mint site; every raw `XxHash128`/`GenerateHash` is the second-hasher defect; content keys keyed by the `EgressKind` discriminant, the durable rows the Persistence artifact-index enrollment carries (`[00]` ruling 3) |
| K10 | `Processing/geodesics.md` heat distance (via `Spatial/fields.md` `Geodesic` case) | `Rasm.Processing` | `Toolpath/surface` | geodesic-parallel / constant-stepover path LAYOUT |
| K11 | `Processing/extract.md` `ContourPolicy.MeshScalar` isolines | `Rasm.Processing` | `Toolpath/surface` | pass extraction |
| K12 | `Processing/flow.md` streamline over `VectorField` | `Rasm.Processing` | `Toolpath/surface` | flowline / morph rows |
| K13 | `Processing/segment.md` cross-field / stripe | `Rasm.Processing` | `Toolpath/surface` | flank / swarf direction alignment |
| K14 | `Analysis/measure.md` `Bounds` enclosing-cylinder + `Meshing/delaunay.md` `LowerHull` + `Spatial/cloud.md` rail | `Rasm.Analysis` / `Rasm.Meshing` / `Rasm.Spatial` | `Toolpath/motion` | `Turn` revolved stock envelope + clearing-stock hull (`[V11]` two-tier) |
| K15 | `Meshing/mesh.md` `MeshSpace` admission + `Processing/repair.md` `HealOp` (predicate-gated) | `Rasm.Meshing` / `Rasm.Processing` | `Ingress/solid` | `OcctMesh` crosses to kernel vocabulary; dirty STL healed at the seam |
| K16 | `Processing/register.md` `AlignKind` ICP dispatcher (one `AlignmentPolicy`) | `Rasm.Processing` | `Verify/probing` | probe-to-nominal datum best-fit |
| K17 | `Analysis/measure.md` `ConformanceMetric` over `ResidualSample` | `Rasm.Analysis` | `Verify/probing`, `Spec/capability` | measured-vs-nominal verdicts, pre-folded evidence |
| K18 | `Domain/stats.md` `Stat.Of` (Welford, public — the composable entry) / `StatContext.Tolerance`; `Distribution.Of` is kernel-INTERNAL and never crosses the assembly boundary — capability composes the public `Stat.Of` quantile surface | `Rasm` | `Spec/capability` | streaming-moment half of Cp/Cpk/SPC (MathNet owns fit/Monte-Carlo half) |
| K19 | `Parametric/projections.md` one-slerp law via the PUBLIC front `Processing/intent.md` `VectorIntent.PoseCase` (`MotionInterpolation.Interpolate` is kernel-internal and never crosses) | `Rasm.Parametric`/`Rasm.Processing` | `Kinematics/machine`, `Joining/weld` (torch frames) | multi-axis orientation interpolation — never a second slerp site |
| K20 | `Analysis/select.md` `Faces` / `Curves.Draft` axis-ranked face decomposition | `Rasm.Analysis` | `Additive/production` | build-orientation overhang/bottom-area objective — never a hand-rolled normal classifier |
| K21 | `Domain/rails.md` — `Op` value-key threading + `Eff<Env>` carriage; receipts register into `IValidityEvidence`+`ValidityClaim` reaching `OpAcceptance` with ZERO oracle edits | `Rasm` | every entry | substrate rail |
| K22 | `Domain/validation.md` — input guards compose the admission vocabulary + the one `OpAcceptance` oracle | `Rasm` | every entry | substrate rail |
| K23 | `Numerics/atoms.md` — typed output projection rides `AtomProjection`/`ProjectionRow`, never a `typeof(TOut)` switch | `Rasm` | every egress projection | substrate rail |

**K24+ — the widening kernel-seam extensions (same lock law: page `#Symbol` anchors, verified against the landed kernel fences; NEVER a kernel `internal` member):** K24 curve CAM (`Parametric/curve.md` `ParametricOp.Offset` exact self-intersection-trimmed offsets, `Stations -> StationField` RMF tool frames, `Fill`/`Divide`, `Reconstruct -> Refit` + `nurbs.md` `CurveThrough(FitPolicy)`/`PerpendicularFrames`) -> `Geometry2D/curves`, turning, program. K25 surface CL-offset/curvature/isolines (`Parametric/surface.md` `NormalOffset`, `CurvatureSample -> CurvatureField`, `Isolines`) -> surface. K26 robust primitive fitting (`Solving/fit.md` `Fit.Apply(FitOp, Context, Op?) -> Fin<FitReceipt>`, `FitKind` plane/sphere/cylinder/cone/torus/line — probing substitute datums DISTINCT from K16 ICP; tolerance form deviation) -> probing, tolerance. K27 mass properties (`Analysis/measure.md` `Measure.Volume/Centroid/Inertia/PrincipalAxes`) -> estimation, workholding CoM. K28 OBB/sphere envelopes (`measure.md` `Bounds.Principal`, `EnclosingSphere/Circle`) -> stock sizing, fixturing. K29 narrow-phase clearance (`Spatial/support.md` `SupportSpace.Of` + `Closest`/`SignedDistance`/`ContainmentDistance`; `index.md` GWN winding) -> guard, removal. K30 ray interrogation (`Analysis/relations.md` trim-aware `RayQuery`; `Spatial/index.md` `SpatialQuery.Ray`) -> tool-axis access, probe visibility, clamp line-of-sight. K31 RMF chain — the PUBLIC front `Numerics/atoms.md` `VectorFrame.Chain` (never the internal BishopChain) -> machine 5-axis framing. K32 orientation cones (`atoms.md` `VectorCone.Of/SolidAngle/Enclose/PartitionBy`) -> 3+2/5-axis feasibility, manufacturability draft. K33 face decomposition + frames (`Analysis/select.md` `Faces.All/At`; `Domain/evaluation.md` `FrameAt`) -> setups planes, manufacturability. K34 native arc/line classification (`select.md` `Curves.Segments/Form`; `Domain/normalization.md` public `CurveForm`) -> program G2/G3 emission. K35 `Orient3D` — the predicates page declares Orient2D AND Orient3D the Fabrication Posting wire; the lock asymmetry resolves by locking BOTH. K36 mesh printability census (`Analysis/inspect.md` `Meshes.Validity/Defects/Quality/NakedEdges` — INSPECTION beside K15's repair) -> slicing gate, audit. K37 inspection sampling (`Processing/sample.md` 12-case `SampleKind` incl. `AdaptiveCase`) -> probing CMM grids. K38 Hausdorff LOD + isotropic remesh (`Processing/decimate.md`, `remesh.md`) -> slice/OCL mesh conditioning, solid ingress cleanup.

**Cross-package (both directions; stale targets re-point — disk-verified against `ARCHITECTURE.md`):**

| Seam (Fabrication side) | Direction | Counterpart / re-point |
|---|---|---|
| `Documentation/projection ← Rasm/Drawing/view` | consumes | kernel `View.Apply → Fin<DrawingProjection>` (the "beside the in-folder BSP solver" clause DIES; row 32) |
| `Documentation/projection ← Rasm/Meshing/arrangement` | consumes | kernel `Arrangement.Apply` watertight — the arm reads `ArrangementResult.Boolean.Solid` (the healing seam row re-points off `Rasm/Processing/repair` per the kernel `BooleanOp` re-home; the arrangement seam row already targets the owner and HOLDS; K5) |
| `Documentation/projection → Rasm.AppUi/Render` | produces | `HiddenLineResult` receipt (supersession insulated AT the receipt, `UIBRIEF [V7]`(b)) |
| `Additive/slicing ← Rasm/Meshing/slice` | consumes | kernel `Slicing.Apply → Fin<SliceStack>` (`Meshing/slice.md` the slice-stack OWNER — intersect's `PlaneMesh` composes under it; "when realized" REALIZED; row 24) |
| `Nesting/nfp ← Rasm/Processing/flatten` | consumes | kernel `ChartAtlas` unrolled UV islands + `DistortionReceipt` true-shape part input (an inbound part-feed seam; the nesting engine stays in-folder) |
| `Toolpath/skeleton ← Rasm/Meshing/offset+skeleton` | consumes | kernel `Offsetting.Apply`/`Skeletonize.Apply` clearance-radius (`Skeleton.cs` dies unconditionally; rows 8,12) |
| `Toolpath/surface ← Rasm/Processing/{geodesics,extract,flow,segment}` + `Rasm/Spatial/fields` | consumes | kernel on-mesh path-layout + SDF fallback (NEW ledger rows, K10-K14/K8) |
| `Process/physics ← Rasm.Materials/Properties` | raw doubles | AEC-peer boundary (the `physics.md` anchor reconciled to `Properties/properties#MATERIAL_PROPERTY_CATALOGUE`) |
| `Nesting/stock → Rasm.Compute` | produces (two-sided, landed) | `NestYield.WasteAreaMm2` — Compute decodes `ElementQuantity.WasteAreaM2` + `NestWasteM2` under the frozen `NestWasteArea` key (`Rasm.Compute/ARCHITECTURE.md:90`, `Analysis/aggregator.md`, `Analysis/lifecycle.md`); the quantity-bag lowering rides the deferred `FabricationProjector` (`[00]` ruling 3) |
| `Nesting/stock ← Rasm.Element/MaterialId` | consumes | NEW ledger row (wired-undeclared on disk, E11) |
| `Ingress/profile ← Rasm.Bim/Exchange` | shared read pin | ACadSharp read codec (the netDxf parenthetical → AppUi ACadSharp two-format WRITE leg); the Bim-side anchors re-point `Polygon`→`Ingress/profile` with the partition (recorded counterpart: `Rasm.Bim/ARCHITECTURE.md:105`, `Exchange/format.md:21`, `Exchange/import.md:1329`, `api-acadsharp.md:120`) |
| `Posting/program → Rasm.Persistence` | durable-row enrollment | `CutProgram` durable rows on the LANDED `Query/cache#ARTIFACT_BLOB_INDEX` owner (`ArtifactIndexRow.Admit` over the kernel one-hasher; the `cutprogram` `ArtifactKind` row lands as the Growth-law enrollment with this page) |
| `Nesting/nfp → Rasm.Persistence` | durable-row enrollment | Placement/Remnant rows on the landed artifact index (`placement`/`remnant` `ArtifactKind` enrollment rows land with the page) |
| `Nesting/nfp ← Rasm.Compute` | gated consume (Compute-side) | DRL nest-policy score as an injected `Func<NoFitPolygon, PartTransform, double>` delegate column (row 18) — the standing strata-correct shape; the gate closes only when Compute lands its side |
| `Fabrication → Rasm.Element/Projection` | LIVE registration (the widening) | `FabricationProjector : IElementProjection` — `Process/derivation` (row 34) implements `Project(ProjectionContext) -> Fin<GraphDelta>`; the one-registration-row hook is named on both sibling ends (`Element/Projection/projection.md`, `Materials/Projection/component.md`) |
| `{Fixturing/assembly, Joining/weld} ← Rasm.Materials/Component` | consumes (vocabulary) | the joining DESIGN vocabulary — `joint.md` `GrooveGeometry`/`GroovePrep` (AWS D1.1), `fastener.md` `FastenerAssembly.Of`, `connector.md` `ConnectorPlate`/`PlateStock` — Materials-owned, consumed at the seam, never re-minted |
| `Ingress/element ← Rasm.Bim/Semantics` | consumes | the connection-detail reader (`Semantics/connection.md` `ConnectionProjection.Detail` bags + the `Connect.Realizing` joint topology) — the named downstream Fabrication consumer |
| `Posting/program → Rasm/Drawing/pack` | residency ledger row | kernel `toolpath` `PackKind` (K7; channel-set alignment, NEVER a Fabrication encoder) |
| `{Tooling/magazine, Verify/probing} ← Rasm.AppHost` | decoded telemetry | MTConnect `-Common` model slice — the LANDED AppHost owners (`.api/api-mtconnect.md` consumer rows + `Wire/livewire.md` `MtconnectLane`→`ExternalValue` decode); magazine tool-life FIRST, probing measured-feature SECOND |
| `{Verify/removal, Documentation/traveler, Additive/production} → Rasm.Persistence` | durable-row enrollment | `.cli`/3MF/NC1/snapshot/traveler egress content keys via `ContentHash.Of` — the `cli`/`threemf`/`nc1`/`stock-snapshot`/`traveler` `ArtifactKind` enrollment rows land on the landed `Query/cache#ARTIFACT_BLOB_INDEX` owner with their egress pages |

## [05]-[ROSTER_DELTA] — 24-package disposition (INTEGRATION-FIRST: the four dead admissions are MANDATES realized as pages)

Central ownership `Directory.Packages.props` (hand-edited, label-grouped) + `Rasm.Fabrication.csproj`; folder-tier catalogs `libs/csharp/Rasm.Fabrication/.api/`, shared substrate `libs/csharp/.api/`. License gate: OSS + free-for-OSS-commercial admissible; LGPL native engines admissible as dynamic-link P/Invoke under the golden-fixture gate; AGPL REFERENCE-only. Manifest anchors are label-anchored and disk-true; VERIFY-REMOVE and re-group motions name-locate their rows, never line-locate. The roster's manifest and catalog motions are LANDED: `OpenCAMLib` + `lib3mf` hold Fabrication-label central pins (vendored, no NuGet — the pin records the vendored release, the RID-keyed native assets ride the folder), `RectpackSharp` is dropped with its catalog, `RectangleBinPack.CSharp` rides the Fabrication label, `api-opencamlib.md`/`api-lib3mf.md` are authored, `api-hashing.md` is the thin overlay, the `UnitsNet` overlay extension is in place, and the csproj `Description` names the production-fabrication telos.

| # | Package | Disposition | Home page(s) | `.api` obligation + proof |
|---|---|---|---|---|
| 1 | `OpenCAMLib` | **ADMITTED** (LGPL-2.1; probe FEASIBLE; central pin LANDED — vendored, RID SHARED assets + the `USE_OPENMP=OFF` build-policy row recorded) | `Toolpath/surface` | `api-opencamlib.md` (authored): `Operation` lifecycle (`setSTL`/`setCutter`/`setSampling`/`run`/`getCLPoints`), the `Cyl`/`Ball`/`Bull`/`Cone`/`Composite`Cutter → `CutterForm` ctor map, ~18-24-fn `extern "C"` shim ABI (Boost.Python `src/pythonlib/ocl_*.cpp` the blueprint), RID assets (`macos-cxx-arm64`/`windows-cxx-x64`/`linux-cxx-x86_64` SHARED `libocl`, all three present), the per-RID OpenMP runtime carriage (osx-arm64 bundles `libomp.dylib` — upstream already rpaths it; linux resolves system `libgomp`; win the MSVC `vcomp` redist; `USE_OPENMP=OFF` the build-policy fallback), LGPL-2.1 dynamic-link note, golden-fixture gate. NO NuGet (feed-verified) |
| 2 | `lib3mf` | **ADMITTED** (BSD-2, official C# binding v2.5.x; central pin LANDED — vendored binding) | `Additive/production` | `api-lib3mf.md` (authored): 3MF core/production/beam-lattice writer; vendored official binding + RID-keyed native asset (NO NuGet — feed-verified; `IxMilia.ThreeMf` dominated); golden-fixture gate; PicoGK `Lattice` → beam-lattice map |
| 3 | `PicoGK` | **KEEP-REALIZE** (integration mandate) | `Additive/implicit` (3 lanes), `Verify/removal` | `api-picogk.md`: DROP the `geometry3Sharp DMesh3` permanent-owner coupling → owned biarc fold + kernel mesh vocab; re-point `Persistence/Schema` → artifact index |
| 4 | `OcctNet.Wrapper` | **KEEP-REALIZE** (integration mandate) | `Ingress/solid` | `api-occtnet-wrapper.md`: re-point `Rasm.Persistence/Schema` → `ContentHash.Of` egress; the `libTKHLR`/`libTKXCAF` SCOPE_LIMIT demand row is a standing invariant |
| 5 | `DSTV.Net` | **KEEP-REALIZE** (integration mandate) | `Ingress/steel` (read), `Posting/dialect` (NC1 emit model) | `api-dstv-net.md`: purge band-2500→2700; purge netDxf |
| 6 | `SharpVoronoiLib` | **KEEP-REALIZE** (integration mandate) | `Toolpath/partition` (`extern alias Voronoi`) | `api-sharpvoronoilib.md`: slim dual-ownership prose → Fabrication-sole (kernel `Rasm.csproj` has no reference) |
| 7 | `CavalierContours` | **RE-GROUPED** (landed on the Fabrication label; `[V10]a` stratum) | `Geometry2D/arcs` | `api-cavaliercontours.md`: `PosPlines` authority HOLDS (drives the `result.Positive`→`PosPlines[].Pline` fix); sole-consumed by Fabrication |
| 8 | `Clipper2` | **RE-GROUPED** (landed on the Fabrication label) | `Geometry2D/algebra` | `api-clipper2.md`: clean |
| 9 | `RectangleBinPack.CSharp` | **RE-GROUPED** (landed — the vacated Materials label → Fabrication) | `Nesting/stock` (5 packers), `Nesting/nfp` (MaxRects fast-path) | `api-rectanglebinpack-csharp.md`: the subset supersession is the removal record (per-packer heuristic enums over one stateful `MaxRectsBinPack.Insert` fold); namespace `RectangleBinPacking` diverges from the package id. Feed 1.0.4 current |
| 10 | `RectpackSharp` | **REMOVED** (redundancy; central pin dropped, catalog deleted, README/catalog residue purged) | (nfp fast-path rides RBP) | Proof: RectpackSharp's one-shot `PackingHints.FindBest` sweep is a strict subset of the per-heuristic `MaxRectsBinPack.Insert` fold; the nfp re-home is row 18's landed interim + rebuild |
| 11 | `netDxf` | **REMOVED** (verified — zero surviving csproj references, no central pin) | (none — AppUi's `[V7]` write leg) | netDxf-purge ripples: README, `ARCHITECTURE.md`, `import.md`, `api-dstv-net.md`, `api-acadsharp.md` re-frame to the AppUi ACadSharp two-format leg |
| 12 | `netDxf.netstandard` | **DEPARTED** (the Compute FEALiTE retirement landed — zero `FEALiTE*`/`netDxf.netstandard` rows survive in `Directory.Packages.props`) | (none) | no Fabrication action; the transitive floor left with its parent |
| 13 | `geometry3Sharp` | **REPLACE-candidate** (single-owner biarc architecture — one owned Bolton fold supersedes the external fit; the feed pin stays 1.0.324) | `Posting/program` (owned Bolton biarc fold, line-sourced chains only) | `api-geometry3sharp.md`: the "dropped CavalierContours" self-contradiction dies; the `api-picogk.md` g3-as-sole-owner coupling re-frames. Leaves when the owned fold lands + Bim drops its mesh-importer (cross-package residual on `Rasm.Bim.csproj`, RECORDED not executed) |
| 14 | `Robots` | **KEEP-REALIZE** (cell) | `Kinematics/cell` | `api-robots.md`: band-2500→2700; `[03]-[RESEARCH]` tail folds into `Packages`/`.api` |
| 15 | `MTConnect.NET-Common` | **KEEP** (model-only) | `Tooling/magazine`, `Verify/probing` | `api-mtconnect-net-common.md`: `GenerateHash → ContentHash.Of` reconcile; re-point `Persistence/Schema` → artifact index; transport half is AppHost livewire's |
| 16 | `ACadSharp` | **KEEP** (read-only) | `Ingress/profile` | `api-acadsharp.md`: the stale write-leg claim → AppUi ACadSharp two-format leg |
| 17 | `MathNet.Numerics` | **HELD** (`6.0.0-beta2`; first Fab consumer) | `Spec/capability` | The `6.0.0-beta2` pin HOLDS — a `5.0.0` downgrade is REFUSED (Compute `Integrate.OnCuboid` is 6.0-only; the central-manifest comment carries the ruling) |
| 18 | `Riok.Mapperly` | **SHARED-MINE** (admitted substrate) | `Kinematics/cell`, `Tooling/magazine`, `Verify/probing`, `Ingress/steel` | DSTV-record→Loop + MTConnect rows + R3 seam (zero-reflection partials); `api-mapperly.md` shared, VERIFIED |
| 19 | `System.Numerics.Tensors` | **SHARED-MINE** | `Toolpath/surface`+`motion`, `Nesting/nfp`, `Spec/capability` | SIMD-lower the hot sampling folds (engagement/scallop/NFP-sweep/capability-batch); `api-tensors.md` shared |
| 20 | `CommunityToolkit.HighPerformance` | **SHARED-MINE** | `Additive/implicit`, `Verify/removal`, `Toolpath/surface` | `Span2D`/`Memory2D` 2D grids (grayscale/uncut/engagement rasters); `api-highperformance.md` shared |
| 21 | `NodaTime` | **SHARED-MINE** | `Documentation/traveler`, `Verify/probing`, `Tooling/magazine`, `Spec/capability` | `Instant` traveler/probing/tool-life/SPC stamps; `api-nodatime.md` shared |
| 22 | `QuikGraph` | **SHARED-MINE** | `Fixturing/setups` | precedence/datum-lineage graph (magazine eviction greedy stays); shared tier |
| 23 | `System.IO.Hashing` | **KEEP** (via `ContentHash.Of` ONLY) | all egress | `api-hashing.md` SLIMMED (landed) to the `api-unitsnet.md` thin-overlay pattern; the false "no shared tier" law is dead — the overlay defers to shared `libs/csharp/.api/api-hashing.md` and names `ContentHash.Of` the one mint |
| 24 | `UnitsNet` | **OVERLAY-EXTENDED** (landed) | cut-parameter boundary + `Spec/tolerance` | `api-unitsnet.md` overlay carries Speed/Length/RotationalSpeed/Pressure + Force/Power/Temperature/Angle/Torque (thin-overlay pattern) |

`.api` state: `api-opencamlib.md` + `api-lib3mf.md` authored, `api-rectpacksharp.md` deleted, `api-hashing.md` slimmed, `api-unitsnet.md` extended; the re-point/slim obligations (sharpvoronoilib, picogk, occtnet, mtconnect, dstv-net, robots, acadsharp, geometry3sharp) ride their owning pages. Substrate `api-languageext.md` stands, never re-authored.

REFERENCE records (never vendored; silence ≠ oversight): `gsGCode` (parse-arm prior art), `gsSlicer` (FFF enrichment), MillSimSharp (voxel-verify corroboration), CAMotics/PrusaSlicer/CuraEngine/libslic3r (concern-validity proofs), FreeCAD `Adaptive2d`+machinability (`kc` schema), FreeCAD_SheetMetal (K-factor unfold), Tweaker-3 (orientation objective), UVtools (resin machine-format breadth), PySLM (Powder hatch/scan), QIF/ISO 23952 (Spec data-model authority), NIST RS274NGC v3 (dialect grammar authority), E3Studio (dialect preset reference). REJECTED: Fanuc FOCAS (distributor-gated), STEP-NC/ModuleWorks/EncyCAM (pay), Aspose.3D/ODA/CadEx (proprietary), PathCAM (GPL dominated), GcodeParserSharp (no modal-group state), Occt.NET/Macad.Occt/Ab4d.OpenCascade/Xbim.Geometry.Occt (OCCT-wrapper supersession sweep re-ran, `OcctNet.Wrapper` HOLDS with its SCOPE_LIMIT demand row). Stratum-split: Manifold is the KERNEL's `[V5]` tier (Fabrication consumes booleans at the arrangement seam, never a pin); MTConnect full-stack/OPC-UA/umati + BACnet are AppHost transport.

## [06]-[VERDICT_DISPOSITIONS] V1-V10

| V | Disposition |
|---|---|
| V1 | RATIFIED at `[01]` and extended by the widening: 15 folders ↔ 15 TIER-1 namespaces BIJECTIVE (`Forming/`/`Joining/` minted), every folder ≥3 pages on the 59-map, folder=namespace=fault-cluster 1:1. Six folder-overrides-namespace ratifications execute in-MOVE (family/faults/physics→`Process`, magazine→`Tooling`, import→`Ingress`, projection→`Documentation`); four namespace-keeping moves (clipper split→`Geometry2D`, kinematics→`Kinematics`, slicing→`Additive`, workholding→`Fixturing` — all namespace-true on disk). `Polygon/` dies. The `Process`→`ProcessKind` collision-break (`[00]` ruling 1) is the ONLY resolution preserving literal 1:1 under the ruled "faults→Process" homing; `Machine` UNRENAMED. the `ARCHITECTURE.md` `[01]/[02]/[03]` + README router + faults taxonomy rewrite land with the partition; the two-node `owner#atoms`/`owner#run` split is the one ratified acyclicity exception. |
| V2 | The five author-kernels re-frame to CONSUMER under the RULED dispositions (K1-K6/K8 signature-locked against the landed kernel pages, `[04]` K-table): (a) skeleton keeps ONLY trochoidal WALK, `Skeleton.cs` dies unconditionally (K2); (b) slicing re-routes to `Slicing.Apply`, `Section`/O(n²)`Chain` die (K3); (c) projection BSP dies for `View.Apply` (K6), keeps the watertight `Arrangement` arm (K5) + PRESERVES `HiddenLineResult`; (d) arcs composes the `[V10]a` split (K1, CavalierContours kerf lane Fabrication-side, carried from clipper); (e) booleans/SDF kernel-consumed (K5/K8). The FIVE `[RESEARCH]` tails purge (kinematics · skeleton · slicing · stock · program); `rg '\[RESEARCH\]'` → zero. |
| V3 | The pipeline islands wire into ONE fold (E2 killed): `Cam` consults `guard.Check` per feed move + `workholding.Condition`, producing the conditioned `Motion`; `Run(Post)` assembles the neutral `CutProgram` AST from the `Motion` (`[00]` ruling 5) + consumes `magazine.Schedule` (real Tnn/M6/G43) + `dialect.Emit`; the arc rail REALIZES (kerf/lead/adaptive read `arcs` arc-native + `Loop.Bulges`, g3 line-sourced only); `owner#atoms` lands the widened `Loop`+`Bulges`+3-arg-ctor+`BulgeAt`; the `result.Positive`→`PosPlines` phantom fix (row 8); Fixturing hardens (segment-vs-polygon keep-out, total `ForHolding`, per-kind footprints, `Condition` wired); the Placement-vs-Motion conditioning asymmetry states on the arm as a LOAD-BEARING invariant, not a bare label: `Placement` (the 2D-cut result) runs the full `Kerf`/`Lead`/`Pierce`/`Tab`/`Sequence` conditioning; `Motion` (the 3D-mill result) is already CAM-conditioned (lead arcs land IN the Cam fold via the arc rail), so a `Kerf` pass over `Motion` DOUBLE-COMPENSATES — the forbidden form, because the toolpath already embeds kerf; the split routes which conditioning runs on which egress case and an implementation that kerf-passes `Motion` ships a silent double-compensation defect; the work-offset chain wires end-to-end (`setups` owns setup→WCS, posting renders, `probing` reconciles). |
| V4 | Posting becomes a real per-dialect generator (rows 2, 27, 30): `PostDialect` gains grammar-family capability columns; `dialect.Emit` a generated total Switch (the flat 3-column render dies); the AST grows canned-cycle/subprogram/macro nodes with typed R/Q/P slots (per-family lowering = node expansion); rows widen as DATA (840D/TNC/OSP/Fagor/Centroid); NC1 emit target on the `Run(Post)` case; G-code INGRESS = the `Parse` arm with the NIST modal-group state machine (`Parse ∘ Emit = id`); data-vs-lowering split (axis family's, folds dialect's); the two look-ahead planners read ONE motion-dynamics law homed on `machine`. |
| V5 | `CutStrategy` widens to production CAM on the `(ProcessModality, CutStrategy)` cross-product + dimensionality column (rows 20-23): waterline/scallop/pencil/rest/3+2/swarf/thread-mill/drill-family; every stub becomes a REAL generator (Turn ZX sweep over the `Bounds` cylinder K14, helical distinct, Peck canned, SliceWalk orderer); OpenCAMLib owns cutter POSITIONING (probe FEASIBLE, ADMIT-WITH-SHIM, row 22; author-kernel SDF K8 the recorded fallback), path LAYOUT kernel-composed (K10-K13); `Toolpath/partition` realizes the Voronoi mandate (row 23); engagement reads the medial clearance AND the physics chip-load budget; `Kinematics/machine` closes the 5-axis gap (rotary topology + TCP/RTCP, kernel one-slerp K19); the `CutterForm` axis lands (four consumers) — the type on `owner#atoms` (`[00]` ruling 2), the projection + machinability on `Tooling/cuttingdata`. |
| V6 | Additive becomes a folder (rows 24-26): slicing keeps FFF/DED planar over the kernel slice-stack (K3), layer-height demand UPSTREAM as kernel policy rows; implicit realizes PicoGK to depth (TPMS/gyroid/lattice/support/resin-powder `.cli`/grayscale, ALC-firebreak); production owns orientation (kernel `Analysis/select` K20) + machine profiles + lib3mf 3MF; physics mints Resin/Powder/DED budgets. gyroid corrects from the `InfillPattern`-row false collapse to the voxel lane. |
| V7 | NEW Verify plane (rows 28, 31): removal owns voxel material-removal (PicoGK `BoolSubtract`, gouge/uncut/overcut/air-cut receipts) PRODUCING `ResidualStock` + per-setup content-keyed `StockSnapshot` (both mint on `owner#atoms` in tranche 1 so Verify produces while rest/setups READ input-carried — run N feeds run N+1); guard stays the per-move DESIGN-time floor (two owners, two moments, both wired); probing realizes the in-process-metrology mandate (G31/G38 rows, kernel `register.md` ICP K16, `ConformanceMetric` K17 verdicts, QIF→capability, AppHost decoded/manual ingress); the PicoGK net9/native boundary declares ONCE on implicit; `FabricationPolicy` grows `Verify`/`Inspect` — and the `Run(Post)`/`Run(Document)` egress cases that make re-post and traveler-compose one-entry. |
| V8 | NEW Spec plane (rows 20, 29): tolerance owns the typed GD&T vocabulary (ISO 286 generated fit rows, ISO 1101/ASME Y14.5 FCF+datums, ISO 1302 finish, MMC/LMC, QIF-aligned); capability owns Cp/Cpk/Pp/Ppk (streaming half on kernel `Stat`/`Distribution` K18, fit+Monte-Carlo on MathNet) + SPC; the spec DRIVES process (Ra→scallop stepover `surface` consumes, IT-grade→allowance conditions pass planning, the plan-time Cpk GATE PRODUCES the `CapabilityVerdict` minted on `owner#atoms` — the plan reads it input-carried, spec→plan→verify→capability one rail); traveler owns setup-sheet/traveler document MODELS via the `Run(Document)` case; UnitsNet overlay extends. |
| V9 | physics identity (row 3): ONE `Material` per physical material with `Map<ProcessModality, ModalityPhysics>` (disk `stainless`/`stainless-abrasive`/`mild-steel-thermal` collapse); `RemovalBudget.Erosion` lands (the `erosion→ThermalBudget` conflation dies) beside Resin/Powder; `CuttingData` moves to `Tooling/cuttingdata` and deepens to the Kienzle `kc` machinability model behind a data-ingress arm (FreeCAD-cards-removed gap honest); the `0.0012` force literal reads `kc`; the dead `Overrides` deletes onto the ingress arm; `90.0` demotes behind the table. |
| V10 | The stale-law closures are largely LANDED — hold them, a regression is a defect: the 25xx purge is DONE (zero 25xx tokens in the folder, `.api` tier, `ARCHITECTURE.md`, README; `faults.md` anchors 2700 with the frozen 2701-2710 arms byte-exact) except `kinematics.md:3` which rides row 16's rebuild; the netDxf purge is DONE everywhere but `Polygon/import.md:20` (rides row 9); the `Rasm.Fabrication.csproj` `Description` rewrite is LANDED; the `api-geometry3sharp.md` self-contradiction is DEAD, `api-hashing.md` SLIMMED, `api-sharpvoronoilib.md` Fabrication-sole — all landed. STILL LIVE: the `ARCHITECTURE.md` scheduler overclaim → `Fixturing/setups` (the ARCH:30/:66 anchor vs the README's already-correct card is a live split-brain the partition rewrite resolves); nfp's two logic defects (`Stock.Of` full-dimension hash, `Ty*1e6+Tx`→tuple, NFP frame proven) land with row 18. |

## [07]-[EVIDENCE_DISPOSITIONS] E1-E14

| E | Disposition |
|---|---|
| E1 | The four zero-consumer admissions realize as pages (INTEGRATION-FIRST, `[05]` rows 3-6): PicoGK→implicit+removal, OcctNet→solid, DSTV→steel+NC1, SharpVoronoiLib→partition. README `[IMPLICIT_VOXEL]`/`[SOLID_INGRESS]`/`[STEEL_FABRICATION_EXCHANGE]`/`[VORONOI_TESSELLATION]` charters become fence-true. |
| E2 | The three pipeline islands wire (V3): `Cam→guard.Check` (row 21→13), `Run(Post)→magazine.Schedule` (row 30→5), `workholding.Condition` wired + composing `NestingOrder` (row 14→7). The `Cam→{Guard,Workholding}` + `Run(Post)→dialect.Emit` fold EXISTS on disk. |
| E3 | The arc rail realizes (V3): `owner#atoms` lands `Loop`+`Bulges`+`BulgeAt`+3-arg-ctor (row 1); `arcs` reads them + fixes `result.Positive`→`PosPlines[].Pline` (row 8, carrying clipper's `ArcAlgebra`); `program` reads arc-native offsets (g3 line-sourced only, row 30); the live phantom is the `.Positive` accessor (real member `PosPlines` per the `api-cavaliercontours.md` authority). |
| E4 | Post thinness dies (V4, rows 2/27/30): PostDialect grammar columns, generated `Emit` Switch, structured cycle/subprogram/macro AST nodes, per-dialect code override; the dialect-invariant `Code` table (thread-cycle→"G92") dies; 9-of-21 stub GCommand rows become real emitted blocks or DATA rows. Byte-diversity across the F1 five targets is the kill-test. |
| E5 | Stub generators become REAL (V5, row 21): radial-sweep/helical de-alias from Turn, Turn a true ZX sweep over the `Bounds` cylinder (BarStock real), Peck canned-cycle geometry, SliceWalk a perimeter/infill/travel orderer; the 3D-surface family lands on `surface` (row 22). |
| E6 | physics identity map (V9, row 3): per-modality `Map`, Erosion/Resin/Powder budgets, dead `Overrides` deleted, `90.0` demoted; `0.0012` force → the `kc` column on `cuttingdata` (row 6). |
| E7 | HLR not-CAD-grade dies (V2c, row 32): the centroid BSP + per-edge average depth + undeduped mesh-edge soup + `1e6` eye + the PHANTOM `Orient2D` import all die for the kernel `View.Apply` (K6); the watertight arm (K5) + `HiddenLineResult` receipt preserved (AppUi insulated). |
| E8 | Coverage silences close: waterline/scallop/pencil/rest/swarf (row 22), NC verification (row 28), production specs (rows 20/29/33), additive supports/orientation/profiles/3MF (rows 25/26), G-code READ (row 30 Parse), 5-axis rotary topology (row 17), typed CutterForm (owner#atoms + row 6), spec→plan derivation (row 20). |
| E9 | Identity/logic defects land: `Stock.Of` full-dimension hash via `ContentHash.Of` (row 18, area-only on disk), `Ty*1e6+Tx`→tuple (row 18), NFP frame proven/fixed (row 18), workholding segment-vs-polygon + total `ForHolding` + per-kind footprints (row 14). The 3 raw `XxHash128` mints route the one `ContentHash.Of` entry (K9). The `EgressKind`-on-owner#atoms spine + the `[00]` ruling-5 result-payload discipline prevent an egress folder re-minting the second-hasher defect. |
| E10 | Stale governance is largely CLOSED on disk (V10 census); the residuals close with the pages that touch each fact: `kinematics.md` band prose (row 16), `import.md` netDxf frame (row 9), the `ARCHITECTURE.md:54-55` `Persistence/Schema` seam rows → the landed `Query/cache#ARTIFACT_BLOB_INDEX` owner, the scheduler re-scope (partition rewrite). |
| E11 | Ledger gaps land (`[04]`): `stock←Rasm.Element/MaterialId` NEW row; `nfp←Rasm/Parametric/develop` NEW row (the kernel end names nfp the consumer); the intra-package web declared (magazine→guard/workholding, guard→skeleton/workholding, workholding→algebra.NestingOrder, motion→skeleton/machine/cell); the `ARCHITECTURE.md:57` Compute rollup row HOLDS live-worded (the landed counterpart confirms it — never downgrade); the `physics.md`↔`ARCHITECTURE.md` materials anchor reconciled. |
| E12 | Redundancy/overlay: RectpackSharp REMOVE (row 10, ⊂ proof); `api-hashing` "no shared tier" slims (row 23); `magazine.Runout` reads the real measurement (row 5, the literal stub dies); the README MaxRect/BinPack rejection row rewrites (RBP subsumes RectpackSharp). |
| E13 | Page-craft: the FIVE `[RESEARCH]` tails purge (V2); the false "no managed library exists" seals (skeleton, slicing) die (the kernel owners landed). |
| E14 | Upstream bindings signature-lock (`[04]` K1-K23 TABLE): against the landed kernel page `#Symbol` anchors, never line-number pins; the two `[V12]` reciprocals DEMOTED (`[00]` ruling 3); the AppUi insulation binding is `UIBRIEF [V7]`; the kernel members K1-K9 + suites K10-K20 + rails K21-K23 locked; every spelling re-verifies against the landed kernel fences, `assay api` covering restored external assemblies. |

## [08]-[DELTA_DISPOSITIONS] — capability escalation, all planes

| Plane (now→target) | Homed at (rows) |
|---|---|
| Process axes owner/family/faults (8→9.5) | Rows 1,2,4 — the 8-case entry (Additive/Verify/Inspect/Post/Document), dialect grammar columns, CutStrategy dimensionality, KinematicClass rotary topology, typed fault payloads + 19 new arms, `Loop.Bulges` ratified, `ProcessKind` collision-break, EgressKind + CapabilityVerdict on owner#atoms, substrate rails (K21-K23). |
| Physics + machining data (6→9) | Rows 3,6 — per-modality identity map, Erosion/Resin/Powder, Kienzle `kc` on `cuttingdata`, data-ingress arm, dead `Overrides` deleted. |
| Tooling (8→9.5) | Rows 5,6 — magazine wired into Post, cuttingdata page, typed `CutterForm` (four consumers, type on owner#atoms), tool-life AppHost seam, `Runout` policy-typed. |
| Geometry2D substrate (8→9.5) | Rows 7,8 — two-owner split (both from clipper — line-space→algebra, arc-space→arcs), `[V10]a` siting (K1), `PosPlines` phantom fix, arc rail consumed downstream, `NestingOrder` re-home. |
| Ingress (5→9.5) | Rows 9,10,11 — profile move, OcctNet solid (kernel `MeshSpace`/`HealOp` seam K15), DSTV steel, ONE polymorphic `Ingress.Admit(IngressSource)` emitting kernel-admissible geometry. |
| Toolpath subtractive CAM (5→9.5) | Rows 12,13,21,22,23 — real generator arms, waterline/scallop/pencil/rest/3+2/swarf, OpenCAMLib surface (path layout kernel-composed K10-K13), Voronoi partition, engagement+finish laws, guard wired, skeleton→kernel-medial (K2). |
| Kinematics (7→9.5) | Rows 16,17 — robot cell held, 5-axis machine topology + TCP/RTCP, kernel one-slerp (K19), one motion-dynamics law with posting. |
| Additive (4→9.5) | Rows 24,25,26 — kernel slice-stack consumption (K3), PicoGK implicit/lattice/support/resin-powder, orientation owner (K20), machine profiles, lib3mf 3MF, budget cases. |
| Nesting (8.5→9.5) | Rows 18,19 — `Stock.Of` identity fix, NFP frame proof, Persistence/Compute seam re-anchors, MaterialId ledger row. |
| Fixturing (6→9.5) | Rows 14,15 — segment-exact keep-out, per-kind footprints, `Condition` wired, multi-fixture setups owning WCS, op-N vs op-N-1 snapshot. |
| Posting (6→9.5) | Rows 27,30 — grammar families + canned/macro/subprogram + WCS + cutter-comp + arc-mode, per-dialect overrides, structured AST nodes, NC1 emit, modal-state `Parse`, `CutProgram` content key + EgressKind, the `Run(Post)` one-entry lowering (AST assembled from the `Motion`, `[00]` ruling 5). |
| Verify (—→9.5) | Rows 28,31 — voxel material-removal receipts, residual-stock field, per-setup snapshots, probing cycles + measured features on AppHost ingress, kernel ICP best-fit (K16), `ConformanceMetric` verdicts (K17). |
| Spec (—→9.5) | Rows 20,29 — ISO 286/1101/1302 + ASME Y14.5 QIF-aligned vocabulary, Cp/Cpk/SPC over kernel `Stat`/`Distribution` (K18) + MathNet, process-driving derivation rows + plan-time Cpk gate producing the `CapabilityVerdict`. |
| Documentation (6→9.5) | Rows 32,33 — projection as kernel-`DrawingProjection` consumer (K6, preserved `HiddenLineResult`), setup-sheet/traveler owner composing views/tools/fixtures/specs via the `Run(Document)` case (widest fan-in). |
| Derivation (—→10) | Rows 34-36, 42, 35 — the design-to-fabrication plane: `Derive` case + `FabricationProjector` registration, the element admit, cross-modality DfM, the fleet match; the F5 chain is the acceptance bar. |
| Forming (—→9.5) | Rows 54-56 — K-factor unfold overlay over kernel develop, brake sequencing/tonnage, tube (research-gated); `ProcessModality.Formed` + `FormBudget` land with it. |
| Joining (—→9.5) | Rows 57-59 — weld prep/bead-stack/heat-input over the Materials vocabulary + the landed `Deposition` budget, distortion sequencing, WPS/PQR compliance; egress through `Cam(Deposition)`. |
| Toolpath widening (→9.5+) | Rows 37-41 — curves substrate (K24), turning (the `helical` alias placeholder dies), wire-EDM (the `Erosion` budget's consumer), travel linking, bevel/THC custodian. |
| Verify widening (→9.5+) | Rows 49-51 — modal-walk simulate under the ONE motion-dynamics law, cost estimation extending the landed Compute rollup, additive layer-stack audit. |

## [09]-[BUILD_ORDER] — dependency-ordered, acyclic

**Acyclicity proof over the `[04]` ledger.** The compose graph is a DAG. The certificate is TWO conditions, both proven: (1) **no cycle** — the five page-cycle breaks (`ProjectionDir`/`ResidualStock`/`CutterForm`/`NestingOrder`/`CapabilityVerdict` on `owner#atoms` input-carry or re-home) plus the `[00]` ruling-5 result-payload discipline (no plane-internal type on a `FabricationResult` case) eliminate every back-edge; the two-node owner split gives the linear flow `owner#run → planes → owner#atoms → family(leaf)`; a mechanical walk of the 33 nodes finds no directed cycle. (2) **every cross-tranche edge lands equal-or-earlier** — a tranche never composes a later tranche's page: the `owner#run` dispatch arms realize incrementally (each in its plane's own tranche, `[02]`), so no tranche-1 arm references an unbuilt plane; `motion` does NOT compose `program` — both tranche 3, and both compose the `owner#atoms` `Motion` type (`[00]` ruling 5), so no edge forms either way; every cross-tranche edge (skeleton T2 ← guard/motion T3; arcs T2 ← program T3; algebra T2 ← program/workholding T3) points BACKWARD to an earlier tranche, and the egress/truth/docs crossings (implicit ← removal; probing → program; dialect → program) are INTRA-tranche with each leaf ordered earlier. Zero upward strata edges (Materials/Compute raw-double/delegate/seam-row only; Persistence/AppHost/AppUi seam-ledger counterparts only). Tranche order 1→3 is a valid topological sort.

Intra-tranche dependency order (strict leaf-before-consumer — the listed order IS the build order): **Tranche 1 (LANDED)** family·physics·faults (leaves) → owner#atoms → magazine → cuttingdata → owner#run arms. **Tranche 2** algebra → arcs → {profile·solid·steel} → skeleton. **Tranche 3** {machine·workholding·nfp·stock·tolerance·implicit·partition} (leaves) → {cell·guard·setups} → surface → motion → slicing → production → dialect → {removal·capability} → program → probing → projection → traveler (the widest fan-in) — `surface` precedes `motion` (motion composes the surface arms), `slicing` precedes `production` (production composes the layer stacks), `program` precedes `probing` (probing lands G38 rows on the posting AST).

### [PROOF_TRACES] — composed closure proofs; each trace composes solely from rebuilt page anchors

| proof | composes |
|---|---|
| `entry-eight-case` | `Process/owner.md#Fabrication.Run` · `Process/family.md#ProcessKind` · `Process/faults.md#FabricationFault` |
| `substrate-ingress` | `Geometry2D/algebra.md#PolygonAlgebra` · `Geometry2D/arcs.md#ArcAlgebra` · `Ingress/profile.md#Ingress.Admit` · `Toolpath/skeleton.md#Skeleton.Walk` |
| `F1-posting` | `Toolpath/motion.md#Cam.Solve` · `Posting/program.md#Post.Parse` · `Posting/dialect.md#Dialect.Emit` · `Process/owner.md#EgressKind` |
| `F2-verified-job` | `Spec/tolerance.md#Tolerance.Frame` · `Fixturing/setups.md#Setup.Schedule` · `Verify/removal.md#Removal.Verify` · `Verify/probing.md#Probe.Inspect` · `Spec/capability.md#Capability.Gate` |
| `F3-additive-pair` | `Ingress/solid.md#Ingress.Admit` · `Additive/implicit.md#Implicit.Voxelize` · `Additive/slicing.md#Slice.Layers` · `Additive/production.md#Production.Plan` |
| `F4-traveler` | `Documentation/projection.md#Hlr.Solve` · `Documentation/traveler.md#TravelerDocument` · `Process/owner.md#EgressKind` |

## [10]-[FLAGSHIP_DRY_RUN_CLOSURE] — F1-F4 backward traces

Each flagship is a chain of `Fabrication.Run` invocations over the 8-case policy; the terminal invocation projects a content-keyed artifact. Every page is pulled into existence by ≥1 flagship terminal (or the SharpVoronoiLib integration mandate). A trace reaching a missing owner reopens the owning verdict — all four close on rebuilt fences.

**F1 — five-target posting suite** (word-address ×3 grammar-true + conversational + additive + NC1 steel). Artifacts: `fanuc.nc`/`haas.nc`/`grbl.nc` (word-address, grammar-true: canned cycles G81-89/G76 single blocks, macro-B `#`-vars/WHILE, M98/M99 subprograms, G54-59 WCS, G41/42 comp, IJK/R arc mode — each DISTINCT), `klartext.h` (Heidenhain conversational), `marlin.gcode` (FFF additive), `part.nc1` (DSTV steel) — six byte-diverse files, each content-keyed by `EgressKind`; `Parse ∘ Emit = id` on the word-address subset. Backward: `Run(Cam)` → `Motion` carrying the conditioned move-stream ⇒ `Toolpath/motion` (row 21). Then `Run(Post{Motion, dialectᵢ})` ×5 ⇒ `Posting/program` assembles the neutral `CutProgram` AST from the `Motion` and lowers via `Posting/dialect` (generated grammar switch over `PostDialect`, rows 30,27) + `Process/family` (the `PostDialect` axis = seed DATA, row 2). NC1 = a sibling emit on the `Post` fold over the DSTV record tree ⇒ `Ingress/steel` (row 11). `Parse` arm (NIST modal-group state machine) ⇒ `Posting/program`. Content key ⇒ `owner#atoms` `EgressKind` + `ContentHash.Of` (row 1). Fault arms: inexpressible-cycle/macro-unsupported ⇒ `Process/faults` `DialectUnsupported` 2718; block-cap ⇒ `BlockCapExceeded` 2728 (a program-SIZE overrun carries no offending `GNodeKind`, so it splits off `DialectUnsupported`, row 4). **Closes** on rows 1,2,4,11,21,27,30 — `Run(Cam)→Run(Post)×5`, no second public `Post` fold, no `motion→program` compose edge (`[00]` ruling 5).

**F2 — verified two-setup multi-tool machining job** (spec → plan → guard/workholding/magazine wired → post → voxel verify → capability). Artifacts: one two-setup multi-tool program (per-setup G54-59 WCS blocks), voxel gouge/uncut/overcut/air-cut receipts, the op-2 clamp admitted against the op-1 stock snapshot, probe-reconciled work offsets, rest-machining consuming the residual field, a Cp/Cpk row gating the next plan, one setup sheet + one traveler. Backward: `Spec/tolerance` (row 20) + `Spec/capability` (plan-time Cpk gate producing `CapabilityVerdict`, row 29) → `Nesting/stock`+kernel `[V11]` hull (row 19) + `Fixturing/setups` (op→setup partition, setup-k→WCS, row 15) + `Fixturing/workholding` (per-kind footprint keep-out, `Condition` wired, row 14) + `Tooling/magazine` (schedule wired into the Cam fold, row 5) + `Tooling/cuttingdata` (Kienzle `kc`, row 6) + `Toolpath/{motion, surface (OCL), guard, skeleton}` (rows 21,22,13,12) + `Kinematics/{cell, machine}` (rows 16,17) → `Run(Verify{Motion, stock})` ⇒ `Verify/removal` (PicoGK `BoolSubtract`, receipts, residual field, per-setup snapshot, row 28) + `Run(Inspect{cycles})` ⇒ `Verify/probing` (ICP datum best-fit, ConformanceMetric, row 31) → `Spec/capability` (Cp/Cpk) → `Run(Post)` (per-setup WCS from `setups`) → `Run(Document)` ⇒ `Documentation/{projection, traveler}` (rows 32,33). Owner grows `Verify`/`Inspect`/`Post`/`Document` cases + `ResidualStock`/`StockSnapshot`/`CapabilityVerdict` on `owner#atoms` (row 1). **Closes** the run-N→run-N+1 truth loop on rows 1,5,6,12-22,28,29,31-33; the CapabilityVerdict input-carry makes the plan-time gate composable.

**F3 — resin/powder + FFF additive pair** (implicit lane + slice stack + 3MF). Artifacts: a STEP solid in; an oriented part scored; lattice supports + TPMS infill; an FFF `.gcode` AND a resin `.cli` layer stack AND a 3MF hand-off — three content-keyed egress. Backward: `Ingress/solid` (OcctNet `ImportStep`→`OcctShape`→`OcctMesh` → kernel `MeshSpace` K15, dirty STL through kernel `HealOp`, row 10) → `Additive/production` (build-orientation over kernel `Analysis/select` K20; machine profiles; 3MF via lib3mf, row 26) + `Additive/implicit` (PicoGK `IImplicit→Voxels` TPMS/gyroid, `Lattice` supports, SLA/DLP/MSLA grayscale + `.cli` via `oVectorize→CliIo`; the `Voxels` stay plane-local; ALC-firebreak, row 25) + `Additive/slicing` (FFF/DED planar consuming kernel `Slicing.Apply` K3, row 24) → `Run(Post{additive dialect})` ⇒ `Posting/{program, dialect}` (Marlin). `Process/physics` mints `RemovalBudget.{Resin, Powder, DED}` (row 3). Content keys ⇒ `owner#atoms` `EgressKind` + `ContentHash.Of`. **Closes** on rows 1,3,10,24,25,26,27,30.

**F4 — issued setup-sheet/traveler package** (the document half of the verified-job chain). Artifact: one content-keyed setup sheet + one shop traveler (typed document MODEL; rendering rides the artifacts-plane seam). Backward: `Run(Document{Seq<FabricationResult>})` ⇒ `Documentation/traveler` (row 33) composing `Documentation/projection` views (row 32) + `Tooling/magazine` tool lists (row 5) + `Fixturing/{workholding, setups}` plans/WCS (rows 14,15) + `Posting/program` facts (row 30) + `Spec/{tolerance, capability}` rows (rows 20,29) into ONE content-keyed model keyed by `EgressKind` (row 1). The widest fan-in node validates every upstream owner exposes a typed receipt — receipt discipline is validated corpus-wide by the terminal composer, not asserted per-page. **Closes** on rows 1,5,14,15,20,29,30,32,33 — `Run(Document)` is one entry case, NOT a second public `Traveler.Assemble` fold.

**F5 — derived steel moment connection** (the design-to-fabrication crown-jewel chain). Artifacts: from ONE designed beam-column moment connection (`Rasm.Element` graph node + `Rasm.Materials` connector/fastener/weld vocabulary + `Rasm.Bim` connection bags): the manufacturability verdict, the process route, the connection-plate cut programs (profile + bevel groove prep), the weld plan + distortion sequence + WPS compliance row, the bolt schedule, the assembly order, the flat patterns nested with remnant reuse, the cost/cycle receipt, and one traveler + one as-built report — every artifact content-keyed. Backward: `Run(Derive{AdmittedComponent, DerivePolicy})` ⇒ `Process/derivation` (row 34) → `Ingress/element` (row 35, the admit) → `Spec/manufacturability` (row 36, the verdict) + `Kinematics/fleet` (row 42, the match) → `Fixturing/assembly` (row 47, the join precedence) → `Joining/{weld, sequence, procedure}` (rows 57-59) + `Toolpath/bevel` (row 41, groove prep) → `Forming/sheet` (row 54, plates flat-pattern) → `Nesting/{nfp, linking, remnant}` → `Run(Post)` ×dialects → `Verify/{simulate, estimation}` (rows 51, 49) → `Run(Document)` ⇒ traveler (row 33) + report (row 53). **Closes** the all-making telos exactly as F4 closes the traveler — the widest end-to-end trace, and the acceptance proof that the derivation plane orchestrates without a single hand-wired seam.

## [11]-[OUT_OF_SCOPE]

- Kernel interiors: corner-strategy offset (`Meshing/offset.md`), slice stack (`Meshing/slice.md`), medial/clearance (`Meshing/offset.md`/`Meshing/skeleton.md`), `DrawingProjection` (`Drawing/view.md`), exact booleans + Manifold tier (`Meshing/arrangement.md`), the distance-field lane (`Spatial/fields.md`), hull (`Meshing/delaunay.md#LowerHull`) — consumed at seams; likewise the landed kernel algorithm and substrate suites Fabrication composes, never re-implements: on-mesh geodesics/isolines/streamlines/direction-fields (`geodesics.md`/`extract.md`/`flow.md`/`segment.md`), ICP registration (`register.md`), metrology + conformance + statistics (`measure.md`/`stats.md`), identity/validity/admission/projection rails (`identity.md`/`rails.md`/`validation.md`/`atoms.md`), and the `MeshSpace`/heal admission (`mesh.md`/`repair.md`). The waterfall demand rows landed there are verified in place, never re-planned here.
- `Rasm.Compute` interiors: the `WasteAreaMm2` rollup decode (`Analysis/aggregator.md`/`lifecycle.md` — landed, Compute's own) — Fabrication supplies the wire value, the quantity-bag lowering rides the deferred projector; the mesh/GLB wire layout law (decode, never re-mint); DRL nest-policy learning (the injected delegate stays the boundary).
- `Rasm.Persistence` interiors: blobstore content rows and the `ArtifactKind` artifact-index mechanics (`Query/cache#ARTIFACT_BLOB_INDEX`, landed) — Fabrication mints content keys through the kernel `ContentHash.Of` entry, decodes 2701-2710, and enrolls its eight artifact families as one-row-each Growth-law ripples on the landed owner, its own `EgressKind` axis staying local.
- `Rasm.AppHost` transport: machine connectivity (MTConnect agent/SHDR/MQTT, OPC-UA/umati, BACnet-class industrial transports) — livewire's; Fabrication consumes decoded tool-life telemetry only.
- `Rasm.AppUi`: the CAD WRITE leg (ACadSharp `DxfWriter`+`DwgWriter`), `Viewport2D` rendering — Fabrication supplies the insulated `HiddenLineResult` receipt and never grows a write arm.
- `Rasm.Bim`/`Rasm.Element`/`Rasm.Materials` interiors: IFC semantics, the `ElementGraph`, material property tables — peer boundaries stay raw-double/delegate/seam-row shaped; the `FabricationProjector : IElementProjection` registration is LIVE at `Process/derivation` (row 34) — the joining/property VOCABULARIES stay sibling-owned, consumed at seams.
- GD&T frame DRAWING and sheet/annotation rendering — the artifacts/drawing plane's (Fabrication owns the `Spec` vocabulary it draws from).
- Sheet-metal forming is LIVE at `Forming/` (rows 54-56) — the kernel unroll interior stays kernel-owned (`Parametric/develop`), Fabrication owns only the K-factor overlay; `Forming/composite` stays the recorded growth lane.
- The federation-wide fault-band renumber beyond the Fabrication registry — escalation path only if the new +11.. arms cannot fit the 2700 century.
