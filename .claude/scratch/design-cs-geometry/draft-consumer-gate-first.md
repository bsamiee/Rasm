# [DRAFT] CONSUMER-GATE-FIRST — complete structural blueprint

LENS: work BACKWARD from the two consumer gates — the Generation SpineRef placement chain (`RASM-GENERATION-SPEC.md:164,179,226`: content-key resolution → arc-length stations + RMF frames over `[T0,T1]` → isolines/geodesics/offsets on UV-pulled-back surfaces → subdivision → panelization → pattern-mapped instance streams) and a full Fabrication toolpath program (slice → corner-strategy offset → medial clearance → post: booleans, developables, hull, HLR, residency wire) — to the exact page-set, namespace partition, and seam ledger that serve both. Every structural call below is derived from a gate demand; the robust floor exists to make the gate outputs exact, and the gate tier exists to make the floor consumable.

THESIS: the corpus fails both gates today for ONE structural reason — the exact floor is award-grade but no owner one level up composes it into consumer-shaped results (E2), and where consumer-shaped results are claimed they are fake (E1 dead HLR, E7 discarded medial, the intersect `Chain` that does not chain). The blueprint therefore (a) mints the ten missing consumer-facing owners (six Parametric-tier pages including the vendored NURBS engine the GShark probe forces, plus slice/skeleton/remesh/edit), (b) rebuilds the fourteen defective settled pages against their gate demands, (c) ratifies a BIJECTIVE namespace↔fault-sub-band partition (13 routing namespaces ↔ 13 sub-bands + 2 stated exemptions) inside the landed folder root plus the `Solving/` lift, and (d) rules both probe hinges NOW: GShark = vendor-and-own the whole composed algorithm set (pin dies), implicit-point = the `[V4]` exact path LANDS with the probe's five companion gaps in the same legs (no honesty re-scope).

---

## [01]-[GATE_HOMING] — every gate item named to its owning page and seam

### [GENERATION_KERNEL_GATE] (`RASM-GENERATION-SPEC.md:179,226`; SpineRef `:164`; PathRow `:138`; Placement `:173-176`)

| Gate item | Owning page | Seam |
|---|---|---|
| SpineRef content-key resolution (`:164`) | `Spatial/reconciliation.md` `OfParametric` canonical-bytes op row (degree·knots·weights·control net, NORMALIZED-knot form per probe G10) composing landed `Domain/identity.md` `ContentHash.Of`; wire INGRESS (key → live surface/curve) through `Parametric/nurbs.md` arbitrary-knot constructors (probe G1 closed by vendoring) | `Spatial/Reconciliation.cs ← Parametric` component row into Element `ToCanonicalBytes` (`Rasm.Persistence/Element/codec.md:151-152`) — a COMPONENT, never a sibling SpineRef key |
| Arc-length stations + RMF frames on arbitrary curves over `[T0,T1]` | `Parametric/curve.md` `Stations`/`Frames` first-class `ParametricOp` cases: `Divide(maxSegmentLength, equalSegmentLengths)`/`ParameterAtLength`/`PointAtLength` sweeping batch `PerpendicularFrames` (Wang-2008 RMF) over `SubCurve[T0,T1]`, clamp/normalize-at-ingress (probe G6/G10), RMF closure-redistribution policy row (probe #373 fix, vendored) | `Parametric/Curve.cs → Rasm.Generation` `(station, frame)` PathRow wire (`RASM-GENERATION-SPEC.md:138`) — NEW ledger row |
| Surface isoline families | `Parametric/surface.md` `Isolines` op: `KnotVector.Refine` + `IsoCurve` fold (both public, probe-verified) | surface→Generation fan row |
| Surface geodesics | `Parametric/surface.md` `Geodesic` op: UV-provenanced tessellation (per-vertex `(u,v)` beside `MeshSpace`) → landed `fields.md` `Geodesic` + `geodesics.md` heat/MMP → pullback to parametric domain by construction; `ClosestParameter` foreign-point fallback only | surface→fields/geodesics landed-seam rows (compose, never re-derive) |
| Surface offsets (normal) | `Parametric/surface.md` `NormalOffset` op: Greville-abscissae normal offset + vendored Piegl-Tiller surface-interpolation refit (probe G5 vendored branch; offset tessellation is the emission wire) | surface→Generation fan row |
| Curve offset | `Parametric/curve.md` `Offset` PROMOTED first-class case: vendored offset seed + kernel deviation-refinement + self-intersection trim through the `[V4]` crossing lattice (probe G8) | curve→intersect seam |
| Region tessellation + subdivision | `Parametric/subdivide.md` NEW: Catmull-Clark/Loop as stencil ROWS over ONE refinement fold; limit-surface evaluation; OpenSubdiv NOT admitted (no managed binding; burden unproven) | subdivide→edit arena seam |
| Developable | `Parametric/develop.md` NEW: guaranteed-isometric strip unroll + isometry witness receipt; distinct from flatten conformal AND segment host capture (boundary stated) | develop→flatten (wave-3) + develop→Fabrication sheet consumers |
| Panelization | `Parametric/panelize.md` NEW: cross-field-guided quad/diagrid/tri as ONE `int symmetry` argument; composes `Meshing/remesh.md` + landed `segment.md` Knöppel cross-fields/stripes + `sample.md` CCVT seeds | panelize→remesh/segment/sample seams |
| Pattern-to-surface mapping | `Parametric/patternmap.md` NEW: symmetry-group tiling rows over ONE log-map instancing fold; per-instance frames parallel-transported by landed vector-heat (`geodesics.md`) — position without orientation is half an instance | patternmap→geodesics landed seam; →Generation PATTERN/TILING plane |
| UV pullback (tier-wide obligation) | `Parametric/surface.md` owns the UV-provenanced sampler; every field-plane composition crosses the parametric↔discrete boundary through it | stated on surface.md + every tier consumer |

### [FABRICATION_TOOLPATH_GATE] (`Rasm.Fabrication/ARCHITECTURE.md:22,23,33,44,46-48`; dry-runs `brief:226` b+c+d)

| Gate item | Owning page | Seam |
|---|---|---|
| Corner-strategy offsets | `Meshing/offset.md` `JoinType`(Miter/Round/Bevel/Square)×`EndType` ROWS over ONE exact-wavefront assembly; CavalierContours kerf/arc stays Fabrication-lane (`README:99`) | offset→Fabrication/Toolpath wire |
| Slice stack | `Meshing/slice.md` NEW: fold of `IntersectOp.PlaneMesh` over parallel-plane family; uniform/adaptive layer-height policy ROWS (OPEN seed family — variable-by-slope + support-interface recorded for `RASM-CS-FABRICATION-BRIEF [V6]`); ORIENTED contours (outer CCW/holes CW), typed OPEN-chain rows, containment-nesting FOREST emitted as kernel-owned SoA wire (QuikGraph law) | `Meshing/Slice.cs → Rasm.Fabrication/Toolpath/slicing` (FAB:48 re-route lands); `→ Rasm.Compute` circulation runner (COMPUTE `[V12]`a) — TWO named consumers |
| Chained section curves (the slice/HLR substrate) | `Meshing/intersect.md` `Chain` REBUILT: merge keys on DEFINING ENTITIES (probe carrier plumbing), cross-face linking, oriented loops + typed open chains — the meshing-lane top finding repaired | intersect→slice, intersect→view Section, intersect→Fabrication/Posting (kept row ARCH:87) |
| Medial + clearance radius | `Meshing/offset.md` `Medial` REAL against `delaunay.md` Voronoi-dual accessor (probe V10c: DECOUPLED — all-explicit ring CDT, land-able now); result carries PER-POINT CLEARANCE RADIUS + ARBITRARY-PROBE clearance query as first-class fields (mandate — a radius-less medial is a `[V10]` defect) | offset→delaunay `Dual` seam; offset/skeleton→Fabrication clearance wire (FAB:22 dies for it) |
| 3D medial/curve-skeleton | `Meshing/skeleton.md` NEW: MCF skeletonization composing LANDED `geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow`; same radius+probe payload | skeleton→geodesics/fields landed seams; →Fabrication clearance wire |
| Manufacturing-tolerance booleans | `Meshing/arrangement.md`: `BooleanOp` homed here (`[V5]`), managed exact arrangement = correctness owner, Manifold (`manifoldc` P/Invoke) = tier-3 SCALE companion behind finite `ScaleCeiling` + golden-boolean fixture gate | arrangement→Fabrication (FAB:44 re-anchor) |
| Developable/unroll | `Parametric/develop.md` (isometry witness) | develop→Fabrication sheet/plywood/fabric |
| Stock-envelope hull | landed `cloud.md` rail (standing law) + `delaunay.md` `Hull` exact lifted-paraboloid lower-hull PROJECTION (V11 ruling: LANDS — predicate-gated construction demanded at robust grade); tier boundary stated on BOTH pages, one anchor each | delaunay↔cloud tier-boundary anchors |
| Distance-field read | the `[V8]` ONE lane: `fields.md` `SignedDistanceFromMesh` → `reconstruct.md` GWN → `index.md` BVH-accelerated winding (index OWNS the accelerated query; reconstruct/fields COMPOSE through the declared seam) | index→reconstruct/fields GWN seam rows (landed-page Growth extensions) |
| Plan/section HLR, zero sampling misses | `Drawing/view.md` REBUILT: exact analytic Appel quantitative-invisibility through the crossing lattice (ruling below); connectivity-chained polylines | view→Fabrication/Posting (ARCH:90, FAB:33 dies), view→AppUi (ARCH:91, FAB:47 thin projection) |
| Toolpath/field residency | `Drawing/pack.md`: `PackKind` gains `field`·`toolpath` DATA rows; dtype-strided byte store made real | pack→AppHost (ARCH:92), pack→Compute residency (ARCH:93) |
| Reality-capture chain (dry-run d) | `Solving/fit.md` (MLESAC via landed `neighbors.md` kd-tree, `README:93`) → `Processing/repair.md`+`receipts.md` (Genus-tolerant TOTAL topology via ONE landed `mesh.md` `ProjectionRow` Growth extension) → `Processing/decimate.md` (boundary collapse, real flip guard) → `Parametric/flatten.md` (corrected Dirichlet) → `Spatial/naming.md` (Self-keyed, harness-proven fixtures) | rows below |

---

## [02]-[RATIFIED_PARTITION] — folders · namespaces · fault clusters (V1 + V12 discharged)

Folder set: the landed eight-folder root + ONE mint — `Solving/` (`solver.md`, `fit.md` — the `[V7]` one-LM family lifted from the Processing grab-bag; ≥2 pages ✓, real growth axis: constrained solving + model fitting). `Drawing/` keeps its ruled charter (downstream emission = projection + residency encoding; passes the growth-axis test). `Parametric/` becomes the surface-development tier (8 robust pages beside landed `projections.md`/`locate.md`) — `flatten.md` MOVES in (rationale: the 1:1 law + never-spans-two-folders makes the `[V12]` ruled default — subdivision/develop/panelize sharing the parameterization stride — otherwise ILLEGAL; flatten IS the UV/chart-atlas substrate the tier composes, and the move makes the Generation gate Parametric-folder-complete). `Processing/` keeps repair/receipts/decimate + 7 landed pages.

The BIJECTION (13 fault-routing concept namespaces ↔ 13 sub-bands; two stated exemptions: `Rasm.Geometry` root owns the BAND itself, `Rasm.Geometry.Numerics` is total-pure and routes no faults — both recorded in `ARCHITECTURE.md:[03]` replacing the placeholder row, and in `faults.md`'s cluster taxonomy in the same motion):

| Namespace | Pages | Folder | Sub-band (code plan) |
|---|---|---|---|
| `Rasm.Geometry` (root) | faults.md | Numerics/ | BAND OWNER 2400-2449 (exemption row) |
| `Rasm.Geometry.Numerics` | predicates.md | Numerics/ | NONE — total pure, no rail (exemption row) |
| `Rasm.Geometry.Spatial` | index.md | Spatial/ | `spatial` 2400-2403: DegenerateInput 2400 · IndexMismatch 2401 · KindMismatch 2402 (NEW, the V8 typed kind-mismatch) · 2403 free |
| `Rasm.Geometry.Naming` | naming.md, reconciliation.md | Spatial/ | `naming` 2404-2407 (RENAMED from `topology` — the phantom kill rides it): NameCollision 2404 · HashMismatch 2405 · 2406-2407 free |
| `Rasm.Geometry.Healing` | repair.md, receipts.md | Processing/ | `healing` 2408-2411: UnrepairableMesh 2408 (re-typed) · 2409 VACATED (NativeAssetMissing re-codes to 2421 with the V5 re-home) · 2410-2411 free |
| `Rasm.Geometry.Constraints` | solver.md | Solving/ | `constraints` 2412-2415: OverConstrained 2412 · SingularSystem 2413 · 2414-2415 free |
| `Rasm.Geometry.Offsetting` | offset.md, skeleton.md | Meshing/ | `offsetting` 2416-2419: DegenerateOffset 2416 (re-typed) · SkeletonStalled 2417 · ClearanceUnresolved 2418 (NEW, skeleton probe-query failure) · 2419 free |
| `Rasm.Geometry.Arrangement` | arrangement.md | Meshing/ | `arrangement` 2420-2423: DegenerateArrangement 2420 (re-typed) · NativeAssetMissing 2421 (RE-CODED from 2409, tier-3 scale gate) · 2422-2423 free |
| `Rasm.Geometry.Intersection` | intersect.md, slice.md | Meshing/ | `intersection` 2424-2427: IntersectionFault 2424 (re-typed) · SliceFault 2425 (NEW, degenerate plane family/empty stack) · 2426-2427 free |
| `Rasm.Geometry.Fitting` | fit.md | Solving/ | `fitting` 2428-2431: FitFault 2428 · 2429-2431 free |
| `Rasm.Geometry.Parametric` | nurbs.md, curve.md, surface.md, flatten.md, subdivide.md, develop.md, panelize.md, patternmap.md | Parametric/ | `parametric` 2432-2435 (RENAMED from `parameterization`, scope-widened per V12): ParameterizationFault 2432 (re-typed; flatten + curve/surface/patternmap route it) · SubdivisionFault 2433 (NEW) · DevelopabilityFault 2434 (NEW) · PanelizationFault 2435 (NEW; panelize + patternmap route) |
| `Rasm.Geometry.Projection` | view.md | Drawing/ | `projection` 2436-2439: ProjectionFault 2436 (re-typed) · 2437-2439 free |
| `Rasm.Geometry.Simplification` | decimate.md | Processing/ | `simplification` 2440-2443: DecimationFault 2440 · 2441-2443 free |
| `Rasm.Geometry.Encoding` | pack.md | Drawing/ | `encoding` 2444-2447: EncodingFault 2444 (re-typed) · 2445-2447 free |
| `Rasm.Geometry.Meshing` | edit.md, delaunay.md, remesh.md | Meshing/ | `meshing` 2448-2449 (the sanctioned Growth claim, `faults.md:19` — the last stride, spent DELIBERATELY on the substrate trio): TessellationFault 2448 (NEW — typed constraint-recovery/cavity failure; discharges "Tessellation owns no cluster") · RemeshFault 2449 (NEW — target-vs-achieved edge length, stall witness). Century FULL; further growth = federation re-plan per `[06]` escalation, never a squeeze |

Dispositions this table discharges: the phantom `Rasm.Geometry.Topology` dies at its exactly-three sites (`pack.md:33` import → `using Rasm.Geometry.Naming`; `pack.md:18` packages row re-credits `CanonicalTopology.OfMesh`/`NamingHashOps.Encode` to Naming; `solver.md:13` owner prose re-homes `EntityKind` to Naming); the four namespace-less pages gain declarations (predicates→Numerics, receipts→Healing, naming+reconciliation→Naming); the `Tessellation`-owns-no-cluster and `Parametric`-borrows census defects both close; `delaunay.md`'s declared `Rasm.Geometry.Tessellation` consolidates into `Rasm.Geometry.Meshing` (the type name `Tessellation` survives — consumers call `Tessellation.Apply` under the Meshing namespace); the `topology` cluster 2404-2407 renames to `naming`; `faults.md:3,14` cluster prose updates to 13 siblings; `ARCHITECTURE.md:[03]` row [04] replaces the placeholder with this enumeration; every folder delta runs the `[LANDED_KERNEL_LAW]` (b) inbound-reference sweep (named ripples: `decimate.md:33`/`view.md:30`/intersect/arrangement `using Rasm.Geometry.Healing` → `Rasm.Geometry.Meshing` for the arena; `mesh.md:647` "settled Processing/repair owns MeshEdit" prose → edit.md; ARCH `Processing/Flatten.cs` seam rows → `Parametric/Flatten.cs`; `README.md` router paragraphs for Solving/ and the Parametric tier).

Fault re-typing (E15, the ten stringly cases — payloads the faults.md rebuild fences): `DegenerateInput(EntityKind Kind, int Index, double Witness)` · `IndexMismatch(EntityKind Kind, int Expected, int Actual)` · `KindMismatch(SpatialKind Held, string Requested)` · `UnrepairableMesh(HealKind Kernel, int Iterations, int Residual)` · `NativeAssetMissing(string Rid, long RequiredCells)` · `DegenerateOffset(int VertexCount, double SignedArea)` · `DegenerateArrangement(int CellCount, int NonManifoldEdges)` · `IntersectionFault(EntityKind A, EntityKind B, int Witness)` · `ParameterizationFault(int ChartId, int Iterations, double Residual)` · `ProjectionFault(int Faces, int UnresolvedCrossings)` · `EncodingFault(int Channel, UInt128 Expected, UInt128 Actual)` — the five rich cases (`NameCollision`/`HashMismatch`/`OverConstrained`/`SingularSystem`/`SkeletonStalled`/`FitFault`/`DecimationFault`) stay the template.

---

## [03]-[PAGE_SET] — 28 rows; schema: path | action → engine lowering | charter (TIER-1 folder · TIER-2 namespace · fault cluster) | entry | seams in/out | wave

### Numerics/ (2 robust + 5 landed)

| # | Row |
|---|---|
| 01 | `Numerics/predicates.md` — KEEP → improve. Charter: Numerics/ · `Rasm.Geometry.Numerics` (minted) · no cluster (total pure). The four-tier ladder + four-way oracle + `Expansion` owner PRESERVED VERBATIM; in-place surgical motions: (i) implicit-construction RE-FOUNDING per the probe — `Lpi` re-founds as SSI-2D (four points) beside NEW LPI-3D (five points, three numerators + exact λ), `Tpi` re-founds over NINE input points with X·Y·Z numerators, EFT leaf differences, the rounded `Denominator` field DIES, negative-λ verdict flip on the orient pair (mirroring `RationalOracle.InCircum`); (ii) family WIDENING as case rows on the ONE `Predicate` owner — `Orient2D` implicit combos {IEE,IIE,III} × xy/yz/zx axis projections, `LessThanOn{X,Y,Z}` {IE,II} (the honest `OrderKey`), an INTERVAL-FILTER tier for every implicit member (kills the false "no filter possible" rationale at `:15,572`); (iii) `[V14]` hardening — strict-IEEE/RID invariant on `NumericsPolicy`, FMA-free Dekker-split `TwoProduct` row gated on RID FMA capability (`Splitter :385` becomes load-bearing), PeterO `ERounding.Floor`/`Ceiling` directed-rounding interval filter as a named ladder tier (`README:86` LANDS), honest precursor rationale (`[V14]` rejection-text correction); (iv) CDTet implicit family ({IEEE..IIII} orient3d, {IEEEE..IIIII} inSphere) recorded as GROWTH gated on the Compute CDTet consumer. Entry: NONE (total-pure static `Predicate` family; `Orient3D`/`InSphere` already public — the Compute export satisfied by shape). Seams — out: ← `Fabrication/Posting` (ARCH:89 kept); ← `Rasm.Compute` CDTet floor (NEW ledger row); in: composed by every robust owner. Wave 1. |
| 02 | `Numerics/faults.md` — REBUILD → rebuild. Charter: Numerics/ · `Rasm.Geometry` (root) · band owner. The `[02]` band plan lands verbatim (13 sub-bands, renames, re-codes, new cases, re-typed payloads); the TWO-FAMILY SEAM statement lands (`GeometryFault` band-2400 robust core ⟂ `Rasm.Domain` `Expected`/`Fault` substrate — mirroring `rails.md`'s side; discharges ARCH:112/E16); cluster taxonomy = the ratified partition 1:1. Entry: NONE (vocabulary: `GeometryFault` union + `ToError()`). Seams — in: routed by every robust page; out: `Error` rail lowering only. Wave 1. |

### Spatial/ (3 robust + 5 landed)

| # | Row |
|---|---|
| 03 | `Spatial/index.md` — REBUILD → rebuild. Charter: Spatial/ · `Rasm.Geometry.Spatial` · `spatial` 2400-2403. ONE excision motion: `SpatialIndex.PointCloud` DIES (case `:118`, kind `:44`, `BuildPointCloud :151-155`, `KdNearest`/`KdRange :372-381`, guards `:130,354-359,542,558,573`) — point kNN/radius/MLESAC/ICP route the landed `neighbors.md` kd-tree (`README:93`); with it die the three throws (`:130,574,579` — `Store`/`ToAcceleration` become total over `{Bvh, LinearOctree}`) and the silent empty (`:358`; genuine kind-mismatch routes `Fin` fail with typed `KindMismatch` 2402); bounds narrowing rounds OUTWARD (min toward −∞, max toward +∞ — `:79` and the wire casts `:590-591`); `ToAcceleration` returns `Fin<(float[] Bounds, long[] Nodes)>` — the kernel-owned `NodeLinkProjection` wire the ledger declares (ARCH:86; `using Rasm.Compute.Solver` DIES); GWN single-owner: index KEEPS the BVH-accelerated `Winding` — `reconstruct.md` GWN method + `fields.md` `SignedDistanceFromMesh` COMPOSE it through the declared seam (the ONE distance-field lane); counts resync (2 union cases · 3 kinds); build kernels/query bodies/refit/`CLASH_GOLDEN` preserved verbatim (dossier-verified sound). Entry: `SpatialIndex.Apply(SpatialOp: Build \| Refit \| Range \| Ray \| Overlap \| Winding \| ToAcceleration)` → `Fin<QueryResult>`/`Fin<wire>`, `Op? key = null` threaded. Seams — out: ⇄ `Rasm.Compute` node-array wire (ARCH:86, fence now CONFORMS; COMPUTE:121 decodes, COMPUTE:120 spurious row dies); → `Fabrication/Toolpath/guard` + ← `Fabrication/Posting` (ARCH:84-85); → reconstruct/fields GWN seam (NEW rows, landed-page Growth); in: ← arrangement/intersect/view/decimate broad-phase. Wave 1. |
| 04 | `Spatial/naming.md` — REBUILD → rebuild. Charter: Spatial/ · `Rasm.Geometry.Naming` (minted) · `naming` 2404-2407. `RebuiltEntity` gains explicit `int Self`; `With`/`Track` key `VertexNames` by `Self` (the E4 star-minimum mis-key dies at `:113`); `Subsumes` subset direction FIXED for splits (child ⊂ parent) with the migration predicate re-founded on shared-name overlap fraction (`:62-66`); Survive×Migrate injectivity residual (`:161`) resolved — a claimed survivor still seeds sibling `Parent` provenance (lineage kept, injectivity by fresh `born.Value` construction); `topology`→`naming` cluster rename rides this page's motion; Op-threading + validity-fold adoption. Entry: `Naming.Apply(NamingOp: Track \| Resolve \| Signature)` → `Fin<NamingTable>`. Seams — in: ← reconciliation (entities), ← pack (`Encode` consumers); out: → Persistence diff plane via reconciliation. Wave 1. |
| 05 | `Spatial/reconciliation.md` — REBUILD → rebuild. Charter: Spatial/ · `Rasm.Geometry.Naming` · `naming`. `BuildEntities` emits `Self=v` (`:62`; `star` stays histogram-only); namespace + `using Rasm.Domain` declared (the unqualified `ContentHash.Of` at `:101,109` compiles); the Persistence seam unifies to ONE anchor (`Rasm.Persistence/ARCHITECTURE.md:55` spelling; `:3`/`:129`/ARCH:79 re-spell to match); `CANONICAL_BYTE_IDENTITY` digest (`:147`) flagged harness-confirmed-once, re-proven against actual `XxHash128` before any page cites it REAL (`[SEAM_AND_RAIL_LAW]`); THE GATE MOTION — `OfParametric` op row: canonical-bytes projection for parametric forms (degree·knots·weights·control net, canonicalized on the NORMALIZED knot form per probe G10) on the ONE `Encode` family composing landed `identity.md` `ContentHash.Of` — the SpineRef resolution component (`codec.md:151-152`), never a sibling key. Entry: `Reconcile.Apply(ReconcileOp: OfMesh \| OfParametric \| Encode \| Reconcile)` → `Fin<...>`. Seams — out: → `Rasm.Persistence/Query` (ARCH:79, one anchor); ⇄ python/ts identity mirrors (ARCH:80-81); ← Parametric tier (canonical bytes, NEW row); in: ← naming, ← pack. Wave 1. |

### Meshing/ (8 robust + 3 landed)

| # | Row |
|---|---|
| 06 | `Meshing/edit.md` — NEW → new (donor: `repair.md:76-102` `MeshEdit` block + `Kernels.WeldDuplicates`+`RepairPolicy.WeldTolerance` weld primitive + the soup adapter ×3 — `intersect.md:326`, `view.md:386`, `arrangement.md:352-365`; wave-1 SURGICAL EXCISION ripple on repair.md, whose full rebuild stays wave 3 — absorb-without-delete, stated engine note). Charter: Meshing/ · `Rasm.Geometry.Meshing` (minted) · `meshing` 2448-2449 (routes spatial admission cases; mints none itself). The robust-core MUTABLE-ARENA tier beside landed `mesh.md`: two-carrier seam stated on both pages (`MeshSpace` immutable admission snapshot ⟂ `MeshEdit` predicate-gated build arena — no third carrier); `MeshEdit` becomes a REAL SoA arena (the immutable-copy-per-op `Arr<T>` O(V+F)-per-op shape dies); ONE `Soup(MeshSpace)`/`Soup(Triangle[])` adapter (three copies collapse); dedup weld (`WeldDuplicates` + tolerance knob) sited HERE — the arrangement→repair wave-2→wave-3 edge the meshing dossier flagged DIES; the `[SEAM_AND_RAIL_LAW]` store-mutability + arena-concurrency contract lands corpus-wide from this page (arenas single-writer, publish by freeze, only frozen surfaces feed parallel folds, `LaplacianCache` the exemplar); `CommunityToolkit.HighPerformance` ADMITTED on these arena shapes (`Span2D`/`MemoryOwner<T>` arena backing; `ParallelHelper` on frozen/partition-disjoint spans ONLY). Entry: NONE (substrate: `MeshEdit` carriers + `OfMesh`/`ToSpace`/`WeldDuplicates`/`Soup` kernels). Seams — in: ← intersect/arrangement/view/decimate/receipts/repair/remesh/slice/skeleton; out: → landed `mesh.md` (two-carrier anchor, both pages). Wave 1. |
| 07 | `Meshing/delaunay.md` — REBUILD → rebuild. Charter: Meshing/ · `Rasm.Geometry.Meshing` (consolidated from declared `Tessellation` per the `[02]` bijection; TYPE `Tessellation` survives) · `meshing` (TessellationFault 2448). The seven `[STORE_OPS]` stub bodies TRANSCRIBED (`Seed`/`Retriangulate`/`RecoverOne`/`StripSuper`/`LastLive`/`TouchesSuper`/`AddSimplexFaces` — the E15 hardest-transcription target); the `[V4]` HYBRID VERTEX TABLE lands (probe gap 1): vertex payload widens `Point3d[]` → closed two-kind row store `Explicit(Point3d) \| Crossing(defining-entity ids)` (SSI-2D:4 · LPI-3D:5 · TPI-3D:9) under the DEPTH-1 law (implicit rows reference INPUT vertices only; recovery-time constraint×constraint crossings re-express as TPI-3D over original triangles; sub-segment splits re-anchor on the `Constraint` carrier); `TessellationPolicy` gains the CONSTRAINED-ONLY mode for implicit-bearing builds (probe gap 4 MINIMAL ruling — validity not Delaunayhood, the reference pipeline's exact shape; the `InCircle` {IEEE..IIII} maximal branch recorded as growth); `Dual` accessor LANDS (probe V10c: decoupled — exact DT adjacency + circumcenters materialized at the emission seam; `README:99` LANDS; `offset.md:325-326` repairs against it); `Hull` exact lifted-paraboloid lower-hull projection LANDS (V11 ruling) with the cloud-rail tier boundary stated both pages; `SimplexStore` an explicit mutable arena (`:5` immutable prose dies); honest rejection rationale (`:3` → Triangle.NET is MIT, TetGen AGPL stays license-rejected, the load-bearing bar is float-epsilon + no exact CDT/CDTet); fault home = TessellationFault 2448. Entry: `Tessellation.Apply(TessellateOp: Build \| Insert \| Recover \| Dual \| Hull \| ToMesh)` → `Fin<...>`. Seams — in: ← arrangement (`Build`), ← offset (`Dual`), ← repair (CDT re-mesh), ← intersect; out: → predicates (implicit family), → edit (arena), ↔ cloud.md hull tier anchor. Wave 2. |
| 08 | `Meshing/intersect.md` — REBUILD → rebuild. Charter: Meshing/ · `Rasm.Geometry.Intersection` · `intersection` 2424-2427. `Chain` RE-FOUNDED (the lane's top finding): merge keys on DEFINING ENTITIES (probe gap 5 — the edge-id/entity-tuple key `Section` never recorded), cross-face linking, true connectivity walk — closed loops CLOSE (oriented outer-CCW/holes-CW), open curves run end-to-end as typed OPEN-chain rows (`IntersectResult` widens: `Loops` + `OpenChains`, never silent closure); the dead `Next` column wired or deleted with the choice; `Crossing` carries the defining-entity ids (the implicit payload) instead of `(Point3d, bool)` — `.Round()` becomes the ONE emission-seam materialization; the E7 consumer collapse LANDS BY CONSUMERS (arrangement/offset/repair route `Intersection.Apply`/`Crossing` — no fourth kernel); `SegmentSegment`/`LineLine` typed 2D restriction on the op case (the silent Z=0 at `:293` dies); the `Lpi.Denominator`-vs-`Lambda` dual-field reading (`:293`) repairs against the re-founded predicates surface; unguarded `QueryResult` casts (`:222,233,241`) route the `Fin` rail; BOTH deleted-source cites (`:5`,`:388`) re-anchor to landed `Analysis/relations.md`; the `contract` label earned back only when the chain is real. Entry: `Intersection.Apply(IntersectOp: CrossTriangles \| PlaneMesh \| SegmentSegment2D \| LineLine2D \| RayMesh \| MeshMesh)` → `Fin<IntersectResult>`. Seams — in: ← arrangement/offset/repair (V4), ← slice (V10b), ← view (Section, V3), ← curve (offset trim); out: → predicates (LPI-3D, LessThan ordering), → index (broad-phase), → edit (soup), → `Fabrication/Posting` (ARCH:87 kept). Wave 2. |
| 09 | `Meshing/arrangement.md` — REBUILD → rebuild (absorbs `HealOp.Boolean` + `BooleanOp` smart-enum from `repair.md:57-62,288-292` — donor motion, repair not deleted). Charter: Meshing/ · `Rasm.Geometry.Arrangement` · `arrangement` 2420-2423 (NativeAssetMissing 2421 re-coded here). `BooleanOp` HOMED (ARCH:42 conforms; the repair⇄arrangement cycle dies — weld already wave-1 in edit.md, so NO wave-2→wave-3 edge survives); `BooleanReceipt` collision resolved: arrangement's `(Classified, Kept, Welded, AssetGated)` is THE type; receipts' healing-session record renames `HealBooleanReceipt` (composes arrangement's); the `[V4]` implicit `Constraint` case WIRED (probe ruling): operand crossings carry `Lpi`/LPI-3D constructions into `Tessellation.Build` — interning keys on defining entities (bit-equality interning at `:284-285` dies), `PlaneCrossPoint` rounding survives ONLY at the emission seam; the exactness contract stated per owner (signs exact · coordinates rounded at emission — `:467` becomes TRUE); Manifold NAMED as the tier-3 scale companion behind finite `ScaleCeiling` (`:61` dead `int.MaxValue` dies) + golden-boolean fixture gate + `NativeAssetMissing` 2421 — the managed exact arrangement stays the ONE correctness rail; `ArrangementStore` honest arena (`:488` prose dies); soup + weld through edit.md; triple `DuplicateNative` materialization (`:155,353,316`) collapses onto the one adapter; `SegmentOf` middle-crossing drop (`:234`) repaired in the chain re-found. Entry: `Arrangement.Apply(ArrangementOp: MeshBoolean(BooleanOp) \| PlanarOverlay \| CellComplex)` → `Fin<ArrangementResult>`. Seams — in: ← repair (booleans — never the reverse), ← view/curve (`PlanarOverlay` fill, `README:99` LANDS); out: → delaunay (`Build`), → intersect (`Crossing`), → index (`Winding`/`Overlap`), → edit (arena+weld), → `Fabrication/Posting/projection` (ARCH:88 kept; FAB:44 re-anchor ripple). Wave 2. |
| 10 | `Meshing/offset.md` — REBUILD → rebuild. Charter: Meshing/ · `Rasm.Geometry.Offsetting` · `offsetting` 2416-2419. `JoinType`×`EndType` corner-strategy ROWS over the ONE exact-wavefront assembly (`[V10]`a; CavalierContours kerf stays Fabrication-lane); `Medial` REAL: reconciles against delaunay's `Dual` accessor (the discarded build at `:325-326` dies; parabolic reflex arcs land), result carries PER-POINT CLEARANCE RADIUS + arbitrary-probe clearance query (the FAB:22 mandate); `MinkowskiConvolution` COMPLETED (convex-vertex arc fans + BOTH convolution directions) or its prose re-scoped — ruled: completed (Fabrication NFP feeds on it); `WavefrontStore` sizing fixed (the 2n under-allocation at `:195` — growth policy on `Spawn`); store-mutability prose dies (`:5` vs `:13`); `SegmentsCross` (`:440-444`) routes `Intersection.Apply`; `PlanarOverlayResolve` feeds arrangement a VALID soup (the `CreateFromClosedPolyline` self-defeat at `:405-411` dies — ring soup built from the wavefront's own segments). Entry: `Offsetting.Apply(OffsetOp: Skeleton \| Offset(JoinType, EndType) \| Weighted \| Medial \| Minkowski)` → `Fin<OffsetResult>` (medial rows carry `Radius`/`ClearanceAt`). Seams — in: ← Fabrication (FAB:22 dies for THIS surface), ← skeleton (2D↔3D boundary anchor); out: → delaunay (`Dual`), → intersect (crossings), → edit (arena). Wave 2. |
| 11 | `Meshing/slice.md` — NEW → new. Charter: Meshing/ · `Rasm.Geometry.Intersection` (shares intersect's namespace + sub-band; SliceFault 2425) · `intersection`. The `[V10]`b slice-stack owner: ONE section fold of `IntersectOp.PlaneMesh` over a parallel-plane family; layer-height POLICY ROWS (uniform · adaptive — OPEN seed family: variable-by-slope + support-interface recorded for `RASM-CS-FABRICATION-BRIEF [V6]`); per-layer ORIENTED contours (outer CCW / holes CW — exact signs over implicit crossings per probe V10b: LPI-3D + projected `Orient2D` + `LessThan` ordering, NO in-circum, NO substrate) + typed OPEN-chain rows for non-watertight sections; containment-NESTING forest per layer emitted as a kernel-owned SoA wire (QuikGraph in-kernel combinatorics only, per the landed lane law); coordinates round at `Polyline` emission. Entry: `Slicing.Apply(SliceOp: Stack(LayerPolicy) \| Single(Plane))` → `Fin<SliceStack>`. Seams — out: → intersect (`PlaneMesh` fold), → predicates (orientation/nesting signs), → `Rasm.Fabrication/Toolpath/slicing` (FAB:48 re-route LANDS — NEW ledger row), → `Rasm.Compute` circulation runner (COMPUTE `[V12]`a — NEW ledger row); in: ← Fabrication + Compute (the two named consumers). Wave 4 (first in leg order). |
| 12 | `Meshing/skeleton.md` — NEW → new. Charter: Meshing/ · `Rasm.Geometry.Offsetting` (shares offset's namespace + sub-band; ClearanceUnresolved 2418) · `offsetting`. The `[V10]`d 3D medial/curve-skeleton: author-kernel MCF skeletonization COMPOSING the landed machinery (`geodesics.md` implicit MCF + `fields.md` `MeanCurvatureFlow` case — never re-derived); per-point CLEARANCE RADIUS + arbitrary-probe clearance query as first-class result fields (the mandate — a radius-less medial is a `[V10]` defect); skeleton topology emits as kernel-owned SoA wire. Entry: `Skeleton.Apply(SkeletonOp: Mcf \| ClearanceAt(Point3d))` → `Fin<CurveSkeleton>`. Seams — out: → geodesics/fields (landed seams), → edit (arena), → `Rasm.Fabrication/Toolpath` clearance wire (with offset.md — FAB:22 dies unconditionally); in: ← Fabrication. Wave 4. |
| 13 | `Meshing/remesh.md` — NEW → new. Charter: Meshing/ · `Rasm.Geometry.Meshing` · `meshing` (RemeshFault 2449). The `[V11]` remesh substrate: Botsch-Kobbelt isotropic (split/collapse/flip/tangential-relax, genus-preserving — NOT deferrable: panelize, Fabrication mesh-prep, and Compute's volumetric pre-step all gate on it) + cross-field-GUIDED quad remesh as POLICY ROWS on the same refinement fold (deferral contingency REMOVED — panelize's grid family lands on guided rows composing landed `segment.md` Knöppel fields); the AUTHOR-KERNEL robust tier beside segment.md's landed HOST-capture tier (QuadRemesh/Reduce), coexisting per the capture law, one anchor each; `decimate.md`'s `VoxelRemesh` stays a distinct row (boundary stated). Entry: `Remesh.Apply(RemeshOp: Isotropic(policy) \| Quad(guidance))` → `Fin<RemeshResult>`. Seams — out: → edit (arena), → segment.md (guidance capture anchor), → mesh.md (`MeshSpace` re-admission); in: ← panelize, ← Fabrication mesh-prep, ← Compute discretization (recorded growth, wire-decoded). Wave 4. |

<!--CONTINUE-->
