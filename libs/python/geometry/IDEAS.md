# [PY_GEOMETRY_IDEAS]

The forward pool of higher-order concepts for `geometry`, grounded in the host-free companion role. Each idea is a card — slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on — and spawns one or more tasks in `TASKLOG.md`. `[1]-[OPEN]` holds live concepts; `[2]-[CLOSED]` records dispositions so an idea is never re-litigated.

## [1]-[OPEN]

[COMPANION_CPU_OFFLOAD]: the heavy geometry kernels run on the runtime subinterpreter offload lane, not the companion event loop. (Trickle-down from `python:GEOMETRY_KERNEL_OFFLOAD_LANE`.) [BLOCKED] sequence-after the runtime `concurrency/lanes#LANES` `LanePolicy` CPU-offload variant.
- The registration ICP loops, the OCCT/IFC tessellation iteration, the `pdal` filter graph, the open3d reconstruction, and the `trimesh`/`manifold3d` boolean are all CPU-bound kernels that today block the daemon's `anyio` event loop or serialize through `to_process` pickle. The branch `runtime/concurrency/lanes` gains the `anyio.to_interpreter.run_sync` CPU-offload `LanePolicy` variant; every geometry owner that runs a heavy kernel hands it to that lane (the lane never imports the kernel) under one `CapacityLimiter` and one `DrainReceipt`, falling back to `to_process` where subinterpreters are unavailable.
- Unlocks true-parallel CPU geometry work without the companion stalling: a tessellation request, a registration fit, and a reconstruction run concurrently on subinterpreter-capable runtimes, and the daemon stays responsive during the 200-400 ms OCCT cold start and the multi-second registration loops.
- Draws on the cross-package `GEOMETRY_KERNEL_OFFLOAD_LANE` idea naming the geometry registration/tessellation loops as the exact heavy CPU kernels the lane must absorb; the geometry side is the consumer that hands its kernels to the lane, the lane owner staying branch-side — never a second concurrency surface minted in geometry.
- Close-condition: the runtime `concurrency/lanes#LANES` `LanePolicy` `anyio.to_interpreter.run_sync` CPU-offload variant lands on the branch runtime owner; the five geometry owner fences (daemon/registration/ingestion/reconstruction/repair) already carry the gated one-call hand-off seam (`daemon.md#DAEMON`, `scan-registration/registration.md#REGISTRATION`, `mesh-utility/repair.md#MESH`, plus the `ingestion.md`/`reconstruction.md` consumers), so the geometry-side realization is a uniform one-call growth across them once the lane exists.

## [2]-[CLOSED]

[SCAN_INGESTION_PIPELINE]: realized — `scan-registration/ingestion.md#INGESTION` owns the `IngestStage`-discriminated raw-scan preprocessing pipeline. `ScanIngestion` composes `pdal`'s `Reader | Filter | Writer` stage graph keyed by `IngestStage` (`filters.smrf`/`filters.pmf` ground classify, `filters.outlier` removal, `filters.decimation`/`filters.voxeldownsize`, `filters.range` crop) over `Pipeline.execute`/`arrays`, plus the `ScanSource`-discriminated `pye57` E57 structured-scan-plus-pose path, folding the cleaned structured arrays into one registration-ready `o3d.t.geometry.PointCloud` `registration.py` consumes. The inbound boundary is the data-branch `data/mesh/exchange#POINTCLOUD` `pyarrow.Table` (never a pdal `Pipeline` object across the seam, never re-owning `laspy` LAS decode); `pdal`/`pye57` ride the `<'3.15'` companion band (function-local import), and the `open3d` egress shares the manifest-admission delta with `reconstruction.md`.

[SCAN_TO_MESH_RECONSTRUCTION]: realized — `scan-registration/reconstruction.md#RECONSTRUCTION` is the producer of the `reconstructed-mesh` graduation subject the compute union already declared. `ScanReconstruction` runs the shared `estimate_normals` + `orient_normals_consistent_tangent_plane` pre-step, then a `match` over `ReconstructionMethod` selecting the STATIC open3d constructor (`create_from_point_cloud_poisson` watertight default, `ball_pivoting` detail, `alpha_shape` concave hull — the algorithm is a constructor choice, never a runtime flag), with `cluster_dbscan` optional multi-object split, GLB export, and a route to `mesh-utility/repair.md#MESH` `MeshOp.Repair` for watertight conditioning (never re-implementing repair). The reconstructed-mesh is disambiguated from the compute `spatial/query.md#SPATIAL` "reconstructed boundary, never crossing as a geometry-branch mesh" same-axis subject; `open3d` admits the shared manifest row; `create_from_point_cloud_alpha_shape` is a folder-`.api` member row (`open3d.md` row [15]), the `reconstruction#3` note narrowed to the optional `tetra_mesh`/`pt_map` reuse overload (a performance choice, not an existence gate).

[SCAN_TO_BIM_DEVIATION]: realized — `scan-registration/deviation.md#DEVIATION` owns the signed scan-vs-model deviation plus RANSAC primitive extraction, both open residuals resolved. The `DEVIATE` arm folds `trimesh.proximity.signed_distance` (positive inside the watertight design solid, negative outside) into under-build (`signed.max`)/over-build (`signed.min`) extrema and the absolute `max`/`mean`/`std` band against tolerance for the construction-verification verdict, composing `proximity.closest_point` triangle ids only for the colored-overlay egress (`deviation#3-SIGNED_BAND_REDUCTION`); the `SEGMENT` arm iterates `segment_plane`/`select_by_index` oversegmentation. The `scan-deviation` `GeometrySubject` literal admits as ONE new row on the compute `graduation/receipt.md#GRADUATION` owner (cross-folder seam the synthesis tier reconciles, never authored into the compute interior), distinct from the present `reconstructed-mesh` and the compute spatial `reconstructed-boundary` subjects (`deviation#4-GRADUATION_SUBJECT`).

[STEP_AP242_COMPANION_HOP]: page authored (`step-bridge/cad.md`). The `step-bridge` sub-domain owns the ISO 10303 AP242/AP203/AP214 + IGES CAD-STEP tessellation hop over `cadquery-ocp` — `STEPCAFControl_Reader`/`IGESCAFControl_Reader` into an XCAF `TDocStd_Document`, `BRepMesh_IncrementalMesh` triangulation, and the native `RWGltf_CafWriter` GLB write — returning raw GLB the daemon keys, so one daemon serves IFC and CAD-STEP through one content-addressed hop. The retired `pythonocc-core` conda-only blocker is resolved by `cadquery-ocp` (sole PyPI OCP path, cp310-cp314 wheels, `<'3.15'` companion band); the C# `StepIso10303` codec gap closes end-to-end at the wire.

[TENSOR_MULTIWAY_REGISTRATION]: realized in `scan-registration/registration.md`. The one `RegistrationMode`-discriminated owner carries `GLOBAL` (the `kiss_matcher` initialization-free Faster-PFH/ROBIN/GNC bootstrap, no initial pose), `MULTISCALE` (tensor `multi_scale_icp` with robust kernels), `COLORED_ICP`, `VGICP` (`small_gicp` speed path), and `MULTIWAY` (pose-graph `global_optimization` folding `kiss_matcher` pairwise edges). The `kiss_matcher` global path supersedes the open3d RANSAC+FPFH bootstrap; a fitness/RMSE receipt graduates through the geometry case.

[ROBUST_MESH_ALGEBRA]: page authored (`mesh-utility/repair.md`). The `mesh-utility` sub-domain owns robust mesh repair and boolean as one `MeshOp` tagged union — watertight/winding/normal/hole-fill repair over `trimesh.repair`, exact union/difference/intersection over the `trimesh.boolean` `manifold3d` backend, and mesh-file decode/encode across `trimesh`/`rhino3dm`/`meshio` keyed by `MeshFormat` — the guaranteed-watertight, boolean-capable substrate the tessellation, scan-reconstruction, and step hops compose, and the clash-volume engine `ifc-analysis` composes rather than re-implements.

[IFC_LIFECYCLE_DEPTH]: realized in `ifc-analysis/costing.md#LIFECYCLE`. The `LifecyclePhase`-discriminated `IfcLifecycle` owner folds the IfcOpenShell ecosystem siblings into one `@staticmethod` boundary capsule: `QUANTITY` (the `ifc5d.qto.quantify`/`edit_qtos` rule-driven take-off keyed by the `qto.rules` `RULE_SET` table writing `IfcElementQuantity` base quantities, superseding `analysis.py`'s deleted single-`NetFloorArea` fold), `COST` (the `ifcopenshell.api.cost.calculate_cost_item_resource_value` per-item resource rollup plus the `CostReport`-selected `ifc5d.ifc5Dspreadsheet.Ifc5DCsvWriter`/`Ifc5DOdsWriter` structured export), `SCHEDULE` (the `ifc4d` `ScheduleFormat`-selected `<Format>2Ifc` named-parser conversion populating `IfcWorkSchedule`/`IfcTask`/`IfcRelSequence`), `PATCH` (the `ifcpatch.execute`/`write` named-recipe transformation over the `recipes` namespace), and `DIFF` (the `ifcdiff.IfcDiff(old, new).diff()` revision comparison over `deepdiff`). Each phase folds a `LifecycleReceipt` graduating through the geometry case; the `0.8.5` siblings ride the `ifcopenshell` companion lane (`<'3.15'`, function-local boundary-scope import) and admit as one manifest batch alongside `ifcopenshell`. Residuals: the `qto.rules` `RULE_SET` literal set (`costing#3-QTO_RULE_SET`), the `IfcCostItem.CostValues` `AppliedValue` measure shape + `Ifc5DCsvWriter`/`Ifc5DOdsWriter` write contract (`costing#3-COST_ROLLUP_EXPORT`), the `<Format>2Ifc` `.file`/`.xml`/`.work_plan` slot-population semantics (`costing#3-SCHEDULE_SLOT_POPULATION`), and the `ifcpatch.write` output-type dispatch (`costing#3-PATCH_OUTPUT_DISPATCH`).

[IDS_BCF_MODEL_CHECK]: realized in `ifc-analysis/analysis.md`. The `AnalysisKind`-discriminated owner carries IDS model-checking through `ifctester` (`ids.open`/`Ids.validate`/`Specification.status`, BCF export via `ifctester.reporter.Bcf`), clash detection through `ifcclash` (`Clasher(ClashSettings())` over `ClashSet`/`ClashSource` TypedDicts, `ClashResult.a_global_id`/`b_global_id`), and BCF issue round-trip through `bcf-client` (`bcf.v3.bcfxml.BcfXml.create_new`/`add_topic`/`get_topics`). The buildingSMART triad is three separate PyPI distributions catalogued in the folder `.api`; `bcf-client` ships a pure-Python wheel, `ifctester`/`ifcclash` ride the companion lane. The `QUANTITY` quantity arm is superseded by the `IFC_LIFECYCLE_DEPTH` `ifc5d` rule-driven take-off (moved to `costing.py`).

[VALIDATED_SELECTOR_GRAMMAR]: realized — `ifc-analysis/selector.md#2-SELECTOR` owns the `lark` EBNF selection grammar validating the element-selection query before `util.selector.filter_elements`. `IfcSelector.parse` compiles one `SelectorGrammar` EBNF over the five IFC selection axes (entity class, attribute predicate, pset/property predicate, classification reference, spatial containment), folds the parse `Tree` through one `SelectorTransformer` (`@v_args(inline=True)`, one method per rule) into a frozen `SelectorQuery`, and lifts `UnexpectedInput` into the runtime fault rail once at admission, so a malformed selector is a typed fault at the boundary, never a silent empty match; the validated query re-serializes to the `filter_elements` string `ifcopenshell` consumes, no second selection engine. `lark` is manifest-present (pure-Python cp315-clean, no companion gate); the analysis/costing selector arms consume the validated query rather than a raw string.
