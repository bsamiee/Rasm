# [PY_DATA_TASKLOG]

Open and closed work for `data`, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` open; `[COMPLETE]` or `[DROPPED]` closed — and carries the capability or file to build, the external packages to integrate, the integration points and boundaries, and the key considerations. `[1]-[OPEN]` holds live work; `[2]-[CLOSED]` records finished or dropped tasks.

## [1]-[OPEN]

[QUEUED] Author the CF field-dataset page and bind the unconsumed `netcdf4` — `CF_FIELD_DATASET_OWNER`.
- Author `field-dataset/dataset.md` owning one `FieldDataset` over `xarray` reading/writing CF-metadata field cubes (`netcdf4`/HDF5/Zarr engines), exposing CF-aware coordinate selection, label-indexed slicing, grouped and resampled reductions, and unit/coordinate-reference metadata, folding one content-keyed field receipt and materializing to the `pyarrow`/Zarr egress the `columnar`/`tensor` owners already speak.
- Integrate `xarray` (`open_dataset`/`open_mfdataset`/`open_zarr`/`sel`/`isel`/`groupby`/`groupby_bins`/`resample`/`interp`/`decode_cf`/`to_netcdf`/`to_zarr` — all present in `data/.api/xarray.md`), `netcdf4` (the CF reader engine — binds the present `data/.api/netcdf4.md` catalogue to its first consumer), `h5py` (HDF5 engine), composing `tensor/store.md#TENSOR` Zarr egress and the runtime `ContentIdentity`/`TransportResource`.
- Lands in the new `data/field-dataset/` sub-domain; the labelled CF-field concern (coordinates/labels/CF-metadata) is distinct from the dense chunk-grid `tensor/store` owner (chunk bytes), so it is a new owner, never a second labelled-array store inside `tensor`; this closes the one admitted-but-unconsumed package (`netcdf4`).
- `xarray`/`netcdf4` ride the `python_version<'3.15'` native band (or the Forge scientific toolchain source-build), so the gated arms dispatch to the runtime `anyio.to_process.run_sync` subprocess seam exactly as the gated data rows do; `xarray` is on `banned-module-level-imports`, so the CF-selection/groupby/resample calls bind function-local under `# noqa: PLC0415`. The present `data/.api/xarray.md` catalogue already carries the full CF IO/selection/reduction surface, so this owner needs a re-owner pass on the present catalogue (study-input framing to CF field-owner framing), not a new catalogue authored.

[QUEUED] Promote the `VectorGeoClaim` operation surface into one `VectorOp` axis — `VECTOR_SPATIAL_ALGEBRA`.
- Extend `geospatial/claim.md#GEO` with one `VectorOp` tagged-union operation axis on `VectorGeoClaim` owning the catalogued `geopandas` vector algebra — `Join(predicate, how)` over `sjoin`/`sjoin_nearest`, `Overlay(how)` over `overlay` set algebra, `Dissolve(by, aggfunc)` over `dissolve`, `Clip(mask)` over `clip`, `Construct(kind, param)` over `buffer`/`simplify`/`convex_hull`/`concave_hull`/`voronoi_polygons`, and `Predicate(name, other)` over the vectorized predicate family — matched by `match`/`case` closed by `assert_never`, folding one `columnar` `QueryReceipt` keyed by `ContentIdentity`; the SQL-engine `[3]-[SPATIAL]` `SpatialQuery` row stays the columnar-join path, `VectorOp` the in-frame GeoDataFrame path on the same owner.
- Integrate `geopandas` (`sjoin`/`sjoin_nearest`/`overlay`/`dissolve`/`clip`/`buffer`/`simplify`/`convex_hull`/`concave_hull`/`voronoi_polygons`/`union_all`/the predicate accessors/`sindex` — all present in `data/.api/geopandas.md`), `shapely` (the constructive backing), `pyproj` (the CRS engine the operands share through `reproject`), runtime (`RuntimeRail`/`ContentIdentity`/`QueryReceipt`).
- Lands in `geospatial/claim.md` on the existing `VectorGeoClaim` owner, never a new sub-domain or a parallel spatial owner; the operation rides the `GeoDataFrame.sindex` spatial-index acceleration, never a Python STRtree/`isinstance` loop.
- A new vector operation is one `VectorOp` case dispatching to its `geopandas` surface; the `sjoin`/`overlay`/`dissolve` knobs (`how`/`predicate`/`aggfunc`/`max_distance`) are case payload fields, never a `spatial_join`/`do_overlay`/`run_dissolve` method family; `geopandas` rides the Forge scientific source-build band, so the vector body binds function-local under `# noqa: PLC0415` exactly as the present `_snap`/`_warp` arms do.

[QUEUED] Promote the `RasterGeoClaim` operation surface into one `RasterOp` axis — `RASTER_COVERAGE_ALGEBRA`.
- Extend `geospatial/claim.md#GEO` with one `RasterOp` tagged-union coverage-operation axis on `RasterGeoClaim` owning the catalogued `rasterio` algebra — `Window(bounds, boundless)` over `windows.from_bounds`+`DatasetReader.read(window=, boundless=)` streamed over `block_windows`, `Mosaic(sources, method)` over `merge.merge`, `Mask(shapes, crop)` over `mask.mask`, `Vectorize(connectivity)` over `features.shapes`, `Rasterize(shapes, merge_alg)` over `features.rasterize`, and `Reproject(target_crs, resampling)` over `warp.calculate_default_transform`+`warp.reproject` — matched by `match`/`case` closed by `assert_never`, folding one content-keyed coverage receipt over the rasterio NumPy-array egress.
- Integrate `rasterio` (`open`/`DatasetReader.read`/`block_windows`/`windows.from_bounds`/`merge.merge`/`mask.mask`/`features.shapes`/`features.rasterize`/`warp.reproject`/`warp.calculate_default_transform`/`enums.Resampling`/`enums.MergeAlg`/`WarpedVRT` — all present in `data/.api/rasterio.md`), `numpy` (the array egress), `pyproj`-backed CRS through the rasterio transform, runtime (`RuntimeRail`/`ContentIdentity`).
- Lands in `geospatial/claim.md` on the existing `RasterGeoClaim` owner, the raster counterpart of the `VectorOp` axis, never a new sub-domain; the windowed/boundless read is the large-coverage streaming path, never a full-raster materialization.
- A new raster operation is one `RasterOp` case dispatching to its `rasterio` submodule; the merge `method`/mask `crop`/rasterize `merge_alg`/reproject `resampling` knobs are case payload fields, never a `do_merge`/`clip_raster`/`rasterize_features` method family; `rasterio` rides the Forge GDAL source-build band, so the coverage body binds function-local under `# noqa: PLC0415`.

[QUEUED] Author the STAC cloud-native catalog sub-domain — `STAC_CLOUD_NATIVE_CATALOG`.
- Author `stac-catalog/catalog.md` owning one `StacCatalog` over `pystac-client` STAC API search — `StacQuery` discriminating bbox/datetime/collection/CQL2-filter item search — folding the discovered `pystac.Item` asset hrefs into the `cloud-egress/store.md#EGRESS` `ObjectEgress.GetRange` archival-byte path and the `tensor/store.md#TENSOR` `virtualizarr` virtual-reference cube, and encoding the item catalog itself as a `stac-geoparquet` columnar Arrow table the `columnar/dataset.md#SCAN` scan and `query/relational.md#QUERY` engine consume; one `StacReceipt` keyed by `ContentIdentity` over the matched item-id set.
- Integrate `pystac-client` (`Client.open`/`Client.search`/`ItemSearch.{items,item_collection,matched}`), `pystac` (`Item`/`Asset`/`Collection`/`ItemCollection`), `stac-geoparquet` (`to_arrow`/`from_arrow` item-table encoding), composing `cloud-egress` `ObjectEgress.GetRange`, `tensor` `TensorStore.virtual`, and `columnar`/`query` for the catalog table.
- Lands in the new `data/stac-catalog/` sub-domain; the discovery owner folds INTO the egress and virtual-cube owners, never a second object-store transport, a second virtual-cube builder, or a durable catalog store; the asset byte windows read through the existing `GetRange` path.
- Requires admitting `pystac-client`, `pystac`, and `stac-geoparquet` (return the manifest-row delta to the orchestrator); `pystac` (python-dateutil) and `pystac-client` (requests) are pure-Python cp315-clean and import module-top, while `stac-geoparquet` co-resides with the geospatial source-build stack through its `pyarrow`/`geopandas` egress (geopandas is source-build band but not banned-module-level, so its `to_arrow`/`from_arrow` stay module-top, never the `pandas`/`polars`/`xarray` banned-import discipline); author the folder `.api/pystac-client.md`, `.api/pystac.md`, and `.api/stac-geoparquet.md` catalogues by reflection on admission before the page transcribes any member as settled fence code.

[QUEUED] Extend the `ScanPlan` axis with the remote-glob/window scan — `COLUMNAR_REMOTE_GLOB`.
- Extend `columnar/dataset.md#SCAN` `ScanPlan` with one `RemoteGlob(glob, predicate, partition_keys)` case over `duckdb` `read_parquet(file_glob)` with `httpfs`/`s3` filesystem registration and Hive-partition predicate pushdown, plus one `Window(partitions, order, functions)` projection composing the `DuckDBPyRelation` analytical window functions (`row_number`/`rank`/`dense_rank`/`lag`/`lead`/`first_value`/`last_value`/`percent_rank`/`cume_dist`), both terminating in the uniform `pyarrow.Table` the shared `QueryReceipt` folds.
- Integrate `duckdb` (`read_parquet(file_glob)`/`register_filesystem`/`install_extension("httpfs")`/`load_extension`/the `DuckDBPyRelation` window-function family — all present in `data/.api/duckdb.md`), composing the `cloud-egress/store.md#EGRESS` `TransportResource` credentials for the remote filesystem, runtime (`RuntimeRail`/`ContentIdentity`).
- Lands in `columnar/dataset.md#SCAN` on the existing `ScanPlan` owner, never a second SQL engine or a second transport owner; the remote-glob credentials route through the `cloud-egress` transport seam, the `httpfs` extension the `geospatial` `SpatialEngine` already loads on a sibling path.
- A new remote source is one `ScanPlan` case; the window-function family is one `Window` case payload, never a `scan_remote`/`scan_glob`/`window_rank` method family; the DuckDB connection stays request-scoped, never a module global.

[QUEUED] Author the `laspy` folder `.api` catalogue (host-environmental, not an upstream blocker) — `laspy` 2.7.0 is pure-Python cp315-clean and lock-resolved; the LAS/LAZ surface (`read`/`LasData.{x,y,z,header,write}`/`PointFormat.{id,dimension_names}`/`header.parse_crs` for `mesh/exchange.md#POINTCLOUD` `[LASPY_SURFACE]`) catalogues by reflection once the authoring host carries `laspy` + `assay api`, blocked on host provisioning alone.

[BLOCKED] Author the `pdal` folder `.api` catalogue (genuine upstream blocker) — `pdal` rides the `<'3.15'` gated band, sdist-only with no cp315 wheel; its COPC octree-subset surface (`Reader.copc(path, bounds=)`/`Pipeline.{execute,arrays}` for `mesh/exchange.md#POINTCLOUD` `[PDAL_COPC]`) stays RESEARCH until the `<3.15` companion host installs the dist and reflects the spelling.

[BLOCKED] Confirm the scalar `LanceDataset.version` accessor (genuine catalogue gap) — the `pylance` `.api` lists only `versions()`; the scalar `.version` integer property the `_lance_receipt` reads (`lakehouse/table.md` `[LANCE_VERSION]`) stays catalogue-pending until reflection against the live `pylance` distribution captures the `.version` accessor.

## [2]-[CLOSED]

[COMPLETE] Iceberg snapshot-id read rebind — the `_iceberg_receipt` (`lakehouse/table.md`) now reads the current snapshot id from the catalogued `Table.inspect.snapshots()` Arrow metadata table (last `snapshot_id` row), the inspection surface the `pyiceberg` `.api` reject-law mandates over raw metadata-attribute access; the prior `Table.metadata.current_snapshot_id` binding violated that law and is the deleted form, so the blocker resolves with zero catalogue gap.

[COMPLETE] Incremental CDC-materialization owner — `columnar/dataset.md#MATERIALIZE` carries `DerivedSnapshot`/`PartitionBundle`/`snapshot_key` composing the `lakehouse` change feed and the `query` engine, re-keying only touched partitions through the Merkle `ContentIdentity.of` fold.

[COMPLETE] GeoArrow egress + DuckDB-spatial engine — `geospatial/claim.md` carries `EgressFormat.GEOARROW` and the `#SPATIAL` `SpatialQuery`/`SpatialEngine` over the DuckDB `spatial`/`h3` extensions; extension SQL function spellings RESEARCH-flagged (`[DUCKDB_SPATIAL_H3]`).

[COMPLETE] Ibis IR + remote transport `QuerySpec` cases — `query/relational.md#QUERY` carries `QuerySpec.Ir` (ibis `connect`/`to_pyarrow`) and `QuerySpec.Remote` (ADBC module-top, ConnectorX `<3.15` function-local), both to uniform `pyarrow.Table`; catalogues confirmed.

[COMPLETE] `TableFormat` axis — `lakehouse/table.md#LAKEHOUSE` carries the Delta/Iceberg/Lance provider axis crossed with `LakeOp`, dispatching `_apply` to the provider, `pyiceberg` function-local gated, Delta-only ops rejecting as typed faults; the iceberg snapshot id now reads the catalogued `Table.inspect.snapshots()` surface (blocker resolved), the `LanceDataset.version` scalar accessor still RESEARCH-flagged (catalogue task above).

[COMPLETE] LAS/LAZ/COPC point-cloud row — `mesh/exchange.md#POINTCLOUD` carries `PointCloud`/`PointBounds` over module-top `laspy` and the `<3.15`-gated function-local `pdal` COPC octree-subset, with the numpy/Arrow columnar bridge; member surfaces RESEARCH-pending (catalogue task above).

[COMPLETE] VirtualiZarr virtual-reference row — `tensor/store.md#TENSOR` carries `TensorStore.virtual`/`_virtual_cube` over module-top `virtualizarr` `open_virtual_dataset`/`HDFParser`, function-local `xarray` combine, and `<3.15`-gated `icechunk` `to_icechunk`/`commit`; catalogues confirmed, the `combine_attrs`/`virtualize.nbytes` spellings RESEARCH-flagged.

[COMPLETE] `cloud-egress/store.py` `ObjectEgress` owner — `cloud-egress/store.md#EGRESS` carries `ObjectEgress`/`StoreOp`/`EgressReceipt` over `obstore` `from_url`/`put`/`get`/`get_range`/`list`/`delete`/`copy`, composing the runtime transport, keyed by `ContentIdentity`; `PutResult`/`ObjectMeta` shapes RESEARCH-flagged.

[COMPLETE] cp315-clean folder `.api` catalogues — `narwhals`/`zarr`/`cubed`/`awkward`/`nanoarrow`/`arro3-core` and the gated/abi3 `virtualizarr`/`icechunk`/`xarray`/`pyiceberg`/`pylance`/`obstore`/`ibis-framework`/`adbc-driver-manager`/`connectorx` are decompile-verified in `.api/`, retiring the catalogue-pending RESEARCH on `interop`/`query`/`contracts`/`tensor`/`lakehouse`. The `laspy`/`pdal`/`lance`-scalar residue carries to the open tasks above; the `iceberg`-snapshot residue resolved by rebinding the receipt to the catalogued `Table.inspect.snapshots()` surface.

[DROPPED] `sparse` N-D tensor row — the admitted `sparse` (PyData COO/GCXS/DOK) is owned by `compute/arrays/payload.md` as an Array-API backend with a `compute/.api/sparse.md` catalogue, resolved through `array_namespace`; a second `sparse` row inside `data/tensor` re-mints the compute array concern, so it is not a `data` unexploited admission and never a `TensorBackend` case.
