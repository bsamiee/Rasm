language js(typescript, jsx)

or {
    `export * from $source`,
    `export * as $ns from $source`
} where {
    register_diagnostic(span=$source, message="Star re-export is a barrel. Name each deep owner the entrypoint exposes and leave the interior unresolvable.", severity="error")
}
