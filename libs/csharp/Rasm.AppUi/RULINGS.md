# [RASM_APPUI_RULINGS]

`Rasm.AppUi` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- A satisfied restore graph is NOT a binding proof, and a package whose types moved out from under it is DROPPED rather than pinned around — `ReactiveUI.Validation` declares floors the bumped reactive major satisfies upward with zero `NU1605`, then type-loads against nothing because `IReactiveObject` moved to a Core assembly with no type forward, and its undeclared transitives vanish from the graph with the major that supplied them; the two escapes are both refused, since pinning the whole reactive rail back surrenders the bump across every consumer for one aggregator and adding the vanished transitives leaves the identical fault because the break is type IDENTITY. The admitted form rebuilds the lost capability as owned rows on the surviving rail — screen validation is slot rows over `Validation<Error,T>` folded into the inbox `INotifyDataErrorInfo` bridge (`Shell/screens#VALIDATION_UX`) — and the drop carries its whole touch-point set in one pass: `Directory.Packages.props` row, csproj `PackageReference`, README registry row, `.api/` catalog file, and every fence, doctrine law, and sibling-catalog sentence that named a member of it. The recurring move reads a silent restore as a compatibility verdict and discovers the break at first mount; binding every package the corpus carries across a dependency's major.
- A cross-major pair the loader ALREADY satisfies is settled by a re-runnable guard row, never by a version pin — a framework's declared dependency is a floor, so a higher major under a matching public key binds and a pin would freeze a whole stack against a risk that never materialized; the admitted form records the witnessed verdict beside a guard entry that exercises the SAME crossing production takes (the render-boundary lease, not a restore read) and re-runs on every bump of either side, because a satisfied restore graph proves nothing and a verdict is a fact about the versions it was taken under. The recurring move reads a green build as a compatibility verdict, or pins defensively and pays the freeze forever; instantiated at `Diagnostics/proof#GUARD_REGISTRY`, binding every declared-floor pair the corpus carries.
- A transitive assembly a fence composes by member is admitted as a direct package, never consumed off the floor — `Mapsui.Nts` (basemap `Apply` binds `EditManager`/`EditMode` by name) and managed `HarfBuzzSharp` (typography tag-mint constructs `Feature`/`Tag`) each carry the full touch-point set: central `Directory.Packages.props` row, csproj `PackageReference`, README registry row, folder `.api/` catalog; a member-composing fence over a transitive-only assembly leaves the version pin, the catalog rail, and the registry claim all unowned. `--key` refusal on the assay rail is a top-level-source fact, never a decompile blocker — the managed DLL in the NuGet cache is the standing rail.
- A foreign package's named DEFAULT is bound from the container it is registered in, never from the static property spelling its name — a table-entry factory property mints a fresh UNREGISTERED instance on every read, so binding it once per row seats N identically-named entries, the writer rejects the duplicate table rows, and every type check, every read, and the property's own name all report the singleton it is not. The admitted form takes the container's own registered accessor, because a document that seeded its defaults already holds exactly one, and the same read is what makes a once-per-set registration provable rather than asserted; the recurring move reads a `Type.Name`-shaped static as a singleton because nothing at the call site distinguishes a factory from a lookup. Instantiated at `Render/drafting#DRAFT_EMIT` over the ACadSharp continuous line type, binding every foreign registry the corpus writes entries into.
- Two packages publishing one full type name is resolved by ALIASING THE ONE THE PRODUCT DOES NOT NAME, as manifest metadata on its `PackageReference` — a renamed package id keeps shipping its old assembly through whichever dependency still declares the old id, so both assemblies land and every plain mention is `CS0433`. Aliasing the pinned package instead lifts its whole type set out of global scope, so every existing mention silently rebinds the stale type and only the members the stale version lacks ever fault, which reads as a missing-member bug rather than a wrong-assembly bind; `ExcludeAssets` is unusable wherever the depending package inherits the type it would remove; and a source `extern alias` cannot reach generated code, so a name generator emitting the type unqualified reintroduces the ambiguity no source directive can repair. The verdict is witnessed in both directions before it lands, because the failing alias direction still compiles. Instantiated at `Editing/graph#GRAPH_MODEL` over the two pan-zoom assemblies, binding every duplicate-type closure the corpus carries.

## [02]-[SHAPE]

- An ACTION-shaped port carries a VALUE across its boundary on a completion gate, never on a value-returning lambda — a marshal typed `Func<Action, IO<Unit>>` answers `Unit`, so a lambda handed to it returning the computed value converts as an expression-statement body and the value is discarded at the one hop that existed to deliver it, leaving a rail whose declared payload type nothing ever fills. The admitted form computes ahead of the post, fills a gate inside it, and awaits that gate on the same rail, so the value re-enters only after the target thread has run the body; the recurring move reads the port as `Func<Func<A>, IO<A>>` because that is the shape the caller wants rather than the shape the boundary published. Instantiated at `Diagnostics/devloop#LOOP_SURFACES` and `Render/animation#SCRUB` over `SurfaceScheduler.Marshal`, binding every effect-shaped port a value must cross.
- Durable counterparts to an evidence fold are SOURCES, never second folds — `EvidenceSource` hands back the same `ReceiptEnvelope` values live or resident, so the correlation join and the billing accrual each stay one implementation; the recurring move writes a store-side query returning pre-aggregated rows, re-deriving the accrual at a second site, and re-litigation opens only if the durable plane stops carrying whole envelopes.
- Every device-resident cache carries a BYTE ceiling and a least-recently-touched release, per plane class and per backend — geometry pages at `Render/meshlets` `ResidencyBudget`, texture planes at `Render/shading` `ShaderAssetCache`; a handle count is not a budget, because one 16k plane outweighs a thousand masks. Each resolution stamps a generation every plane it touches carries, and eviction never releases a cell at or above the live generation, so a budgeted cache cannot free a handle the current draw holds; the recurring move grows a `ConcurrentDictionary` of native handles until the device refuses, and re-litigation opens only if a single owner takes both plane classes.
- A cache whose cells back NO device handle keeps the byte ceiling and the generation stamp but inverts the live-generation protection — an asset product re-materializes from its catalogue, so the retention that matters is a CONSUMER still holding the value, which a generation floor cannot see; the admitted form is two-phase retirement, where pressure and staleness unlink a cell immediately and its payload disposes one cycle later, after the theme swap's re-materialization roster has re-resolved every mounted surface. The recurring move copies the device clause whole and either disposes an image a mounted control still draws or protects the entire live table from a budget it then never enforces; instantiated at `Theme/assets#ASSET_CACHE` against the device-plane clause above, binding every non-device budgeted cache the corpus grows.
- Icon geometry is a STEP on the resolved metric scale, never a pixel column — a request carries a step and the resolve reads the icon family off the same generation that sized the surface around it, so a density election, a text-scale flip, and a high-contrast projection move glyph and chrome together; a third-party family whose own size axis is fixed ELECTS the largest member at or below the resolved metric and gates that election against the package's own availability predicate, so a member with no glyph at the elected size refuses by name instead of rendering a notdef box. The recurring move authors a size roster beside the metric scale and the two drift on the first density change; instantiated at `Theme/assets#ICON_AXIS`, binding every glyph vocabulary carrying a foreign size axis.
- A foreign presence property whose GETTER builds the thing it reports is not a probe, and the capability a surface may reach is its own declared ROW rather than that property — the retained SVG scene is the archetype, where both `HasRetainedSceneGraph` and `RetainedSceneGraph` run the same build, so a probe-then-reach shape pays the build to ask whether the build happened and silently grants a picture-only row the scene its row declined. The admitted form reads the declared capability, refuses by name where the row carries none, and where the build genuinely must run it takes the package's own single build-and-carry-out member rather than a presence read beside it; the recurring move reads a `Has*` property as a cheap question because its name is a question. Instantiated at `Theme/assets#SVG_PIPELINE` over `SvgLease`, binding every lazily-built foreign capability the corpus leases.
- A capability ELECTED off a foreign face is APPLIED to the object the renderer reads it from, never recorded beside it — a colour face's palette index lives on the typeface Skia rasterizes through, so an index the capsule merely holds renders every layered glyph in the shipped default while the capsule reports a posture it never wore, and the defect is invisible because both the election and the render succeed. Where the election can only follow a read the applied object does not carry (the palette table is a shaper-face read), the clone that applies it follows the election and the superseded handle is held for release, because the applied form is a SECOND owned native handle; the recurring move records the election as evidence and never asks which object the renderer consults. Instantiated at `Theme/typography#FONT_ADMISSION` over `FaceInstance.Palette`, binding every foreign-face election the corpus makes.
- A two-phase retirement queue advances on EVERY edge that fills it, or the edges that only append strand their own retirees behind an edge that may never arrive — an asset cache retired by a display move and drained only by a theme swap accumulates native handles forever on a host nobody re-themes, while its receipt reports a release count of zero and reads as healthy. One rotation therefore owns unlink-then-release and each edge differs only in its staleness predicate, whatever generation it additionally raises, and the LANE its cause names — a COHORT retirement whose whole matching set dies at once needs a grace spanning the consumer sweep that re-resolves it, while a PRESSURE retirement of the coldest cell needs only the next eviction, so folding both onto one lane either leaks the pressure fill on an estate nobody re-themes or releases the cohort mid-sweep under a surface that has not re-resolved, and those two failures are opposite and neither shows. The first recurring move writes the second edge as an append because the first edge's drain is right there; the second folds every edge onto one lane because the rotation already exists. Instantiated at `Theme/assets#ASSET_CACHE`, binding every multi-edge retirement the corpus grows.
- Raster shading READS a resolved `EnvironmentLight`; it never integrates one. Prefilter products — the SH irradiance run, GGX roughness ladder, split-sum LUT, and stored equirect — cross as data and blobs, and the roughness-to-level correspondence crosses as the ladder itself rather than its formula, so the level a raster shade picks and the level the prefilter wrote agree by construction; the recurring move re-derives an SH reconstruction or a level formula on the Render side to save a uniform, and re-litigation opens only if a backend cannot bind the ladder as a level set.
- A value arriving both from a local computation and from a store wire has ONE construction site — the local leg builds the wire shape and decodes through the same gate the store's answers take, so a locally built result cannot carry an anchor the decode would refuse and the two legs cannot drift; instantiated at `Document/search#SOURCE_COVERAGE`, binding every consumed-wire seam.
- A union whose roster a foreign backend closes carries no open or arbitrary arm — the backend's silent-drop path makes such an arm a no-op wearing a capability's name; instantiated at `Document/export#PDF_POLICY` (the three-key Skia annotation roster), governing every host-key vocabulary.
- A draft-edited immutable record is TWO type parameters, never one — the mutable notifying draft the property grid writes in place and the record its commit rebuilds; conflating them on one parameter makes "persist the rebuilt value, never the draft" unenforceable at the seam, and the grid packages carry no immutable-record facility, so the draft is mandatory, not a style choice.
- A host control's presented editor receives its write channel BOUND, never the raw descriptor — the framework's own factory write path carries the command mint, the veto edge, and the undo enqueue, and a direct descriptor write bypasses all three at once while type-checking clean.
- Receipt sealing is double-count-proof by construction — a running-total scan feeding a per-delta receipt seals the per-delta cell, never the cumulative one, and two receipt producers on one user action seal DISTINCT outcome cases wherever the evidence fan routes on outcome alone; a cumulative seal re-counts every earlier delta per emission and identical outcomes on one action double-increment the fan's write.
- Presence is a HINT and the durable register is AUTHORITY — an authorization or admission gate reads the durable membership register alone and never a TTL-expiring, forgeable awareness channel; the ephemeral channel serves display (who is here, who follows) and its expiry or forgery therefore costs a stale cursor, never a granted capability. Instantiated at `Collab/session.md`; binds every ephemeral/durable pair the corpus grows.
- A foreign NARROWING that lifts its own handle leaves the wrapper that produced it standing, so a resolve binds BOTH lifetimes or strands one — the FFI result union clones a fresh reference-counted pointer per `As*` narrow, which is exactly what lets the narrowed child outlive its probe and exactly what makes `Get(key)?.As*()` a leak that type-checks, disposes cleanly on the half it holds, and shows nothing until the process reaches its handle ceiling. The admitted form is one scoped owner per descent shape — a leaf read and a nested-level read, each freeing probe and product — so no consumer re-spells the narrow and a passive record merely holding a live foreign handle is unrepresentable; the recurring move copies the leaf read's shape and then reaches past it for a child container, because the probe is invisible in the expression that discards it. Instantiated at `Collab/sync#DOCUMENT_OWNER` over `CollabRegister.Read`/`Level`, binding every foreign result-union narrow the corpus crosses.
- A veto edge whose args type DERIVES from its receipt args type narrows to the derived type — a base-typed probe accepts both edges and silently never reaches the cancel slot; instantiated at `Editing/inspector` over the grid's cancelable command pair.
- A foreign registry's admission predicate is read against the LIFECYCLE it gates before it is narrowed — the property-grid factory's `Accept` filters CLONING into a per-grid set while cell building walks the priority order and takes the first non-null create, so narrowing `Accept` to the inspected type refuses the grid access token, keeps the factory out of every grid's set, and points every symptom at the create member; instantiated at `Editing/inspector#EDITOR_FACTORIES`.
- A merged multi-target accessor that answers a SENTINEL on disagreement is not a mixed-state channel — the property grid's merged descriptor returns null for divergent targets and for a uniformly null value alike, so mixed state reads the per-target array and folds distinctness while the sentinel read renders every disagreeing cell as an empty editor a user then commits across the whole selection; instantiated at `Editing/inspector#INSPECTOR_SURFACE`, binding every merged-selection projection.
- Chrome for an embedded third-party render stack resolves from the product token resolve and PROJECTS into that stack's vocabulary; reading the stack's own palette back is the deleted direction — the grammar-theme corpus authors its chrome block partially and spells one pixel under two names across themes, so a read-back pins the product surface to whichever theme a user picked, while a projected block is total by construction. A foreign stack taking a VALUE rather than an observable puts that projection on the re-materialization roster; instantiated at `Editing/inspector#CODE_EDITING`.
- A control publishing a typed gesture VALUE binds a surface-owned LIFTING arrow, never a deck row's own materialized command — `ReactiveCommandBase<TParam,TResult>.ICommandExecute` throws `InvalidOperationException` for any parameter outside `TParam`, so a strip drag handing an `Avalonia.Point` to a `ReactiveCommand<CommandPayload, CommandReceipt>` faults on every gesture while the availability read passes, which is why the defect surfaces as a runtime throw rather than a dead control; the arrow lowers the value onto an existing payload case and runs the row, so the verb stays a deck row and the payload union stays closed; instantiated at `Editing/history#EDIT_HISTORY` over the overview strip's jump seam, binding every control that publishes geometry, an index, a node, or any non-payload value; the `MaterializeContext.Gesture` column at `Shell/controls#MATERIALIZE_FOLD` is the fold's own seat for that arrow, which both the overview strip and the tree disclosure resolve through.
- A revert LANE is an INSTANCE of the one revert algebra — its own recorder, its own typed-op roster, its own window read, its own cursor — so a parameter undo can never pop a document edit off the shared client window, and a session-scoped lane binds a window answering empty by construction so a turn past its client window seals the direction's absent fault instead of reaching the durable ledger; instantiated at `Editing/history#REVERT_SCOPE` with the parameter lane at `Editing/forms#BATCH_EDIT`, binding every scoped history the corpus grows.
- Two undo owners over one surface partition by PLANE and neither wraps the other — the editor's rope-edit stack owns in-pane text history and the cancelable-command recorder owns typed-op history, so a pane whose text is a durable document routes its COMMITTED text through the edit gate rather than per keystroke and no change sits in both stacks; instantiated at `Editing/inspector#CODE_EDITING` against `Editing/history`, binding every surface carrying two history owners.
- A platform-capability gate reads the capability PROPERTIES the operation needs AND the operation's activation preconditions, never a provider type test — the Avalonia no-op storage provider is `internal`, so a type test is unspellable outside its assembly AND the weaker predicate, and the live embedded provider answers every capability true while a picker launched on a windowless root returns a task that never activates, so the property read alone admits the worst failure shape; instantiated at `Shell/dialogs` (the seam's own window fact beside the capability columns), binding every capability probe over a foreign floor.
- A meter delegate's arity mirrors its instruments' declared `Dimensions`, and a runtime meter column carries `(string instrument, Option<(string Slot, string Value)>)` — wider invites an untagged write against a declared breakdown, narrower makes a declared breakdown unwritable, and a bare tag value can neither check against the declaration nor express a zero-dimension row; instantiated at `Shell/navigation`.
- Capability admission folds every TOTAL gating plane through one predicate — a health-derived `DegradationLevel` is one plane and never the whole fact (its `Full` row retains every capability), so a structural plane — mount shape, isolation, tenancy — contributes its own roster-total set beside it; instantiated at `Shell/hosts` mount admission.
- Pose interpolation partitions by TYPE DOMAIN — `Render/animation` `TrackInterp` owns the AppUi stratum over `System.Numerics.Quaternion`/`ViewCamera`/`ElementPose`, `[NOT]` the kernel `MotionInterpolation` one-slerp law, which stays scoped to the kernel interior over `Plane`/`Rhino.Geometry.Quaternion`; the pressure earning the partition is per-sample `ViewCamera`-to-`Plane` marshaling plus the host-free-shell constraint.
- `Render/capture` owns the `NativeAssetFact` TYPE and `Shell/hosts` `NativeAssets.Identity` inside the mount transaction is its SOLE producer — a second same-shaped receipt record and a capture-side re-probe port are both the deleted twin.
- Popup, flyout, and tooltip motion has ONE owner — the package motion vocabulary through `MotionEasing`; the shipped Semi popup-animation style stays unmounted, because its inline duration literals would be a second untokened timing source.
- Semi slot re-emission writes the SEMANTIC brush layer only — the raw hue scale binds into the semantic brushes through `{StaticResource}`, so a runtime scale override re-tints nothing and a scale-targeted write is the deleted form.
- A shipped-theme token whose key names its own VALUE (a numeric step scale) is never re-seeded from a resolved metric — only role-named keys re-seed; re-seeding a value-named key makes the token lie about itself while passing every structural check.
- A resolved token reaches a consumer through RESOURCE RESOLUTION alone — the emission writes every key into `ResourceDictionary.ThemeDictionaries` partitioned by `ThemeVariant`, XAML binds `{DynamicResource}`, and code binds `GetResourceObservable`; a `SetValue` write of a resolved paint seats a LocalValue no dictionary edit re-resolves, so the atomic-swap promise silently covers nothing already materialized. What genuinely cannot observe a dictionary — a sealed Skia op list, the `FluentTheme.Palettes` value, an `IColorPalette` swatch source, a tinted raster, an export colour snapshot — rides the named `Rematerialize` roster the swap rebuilds, and a surface holding a resolved value off that roster is a defect; instantiated at `Theme/tokens#CONTROL_THEMES`, binding every retained-visual token consumer.
- A correspondence against a compiled third-party resource vocabulary proves against a GENERATED roster of that vocabulary's keys, never against the page's own claim list — a claimed key the shipped theme never defines writes a dead dictionary entry that re-tints nothing, and a shipped key neither claimed nor excluded is a silent remainder no self-referential check can surface; the conformance is three-way (mints, defined, claimed-or-excluded) and every carve-out is a verdict row carrying its reason. Compiled AXAML keeps each `x:Key` inside a XamlClosure body, so the roster derives from a LIVE graph walk under a headless application and never from metadata, which splits conformance across the boot path and the proof lane; instantiated at `Theme/tokens#TOKEN_CATALOG`, binding every compiled-resource correspondence the corpus grows.
- A per-variant colour column on a role row is the deleted shape — light and high-contrast are PROJECTIONS of one generation through `VariantProjection`, because three hand-authored columns drift the moment one seed moves and a structural check cannot tell a deliberate divergence from an unmaintained one; the projection carries tone direction, near-neutral chroma scale, contrast-floor lift, shadow admission, and stroke gain as five row values.
- Alternative-input admission takes a cross-platform SDK on the estate's own target framework or nothing — an input modality whose only SDKs are per-platform (eye-tracking, exotic HID beyond the admitted SDL/MIDI/SpaceMouse set) stays excluded, because each admission mints a per-platform capsule with no shared decode, and the exclusion is stated at `Shell/input` as its own reason rather than a dangling roster token.
- A device case addressing two legs by one ordinal addresses the space BOTH legs enumerate — SDL's independent joystick and haptic index spaces are the archetype, and the defect is silent because a one-device machine pairs correctly; instantiated at `Shell/input`, binding every multi-leg device vocabulary.
- A visual that SAMPLES pixels outside its own bounds drives its own invalidation or clamps its sample region — there is no third resolution. The compositor re-runs a draw operation only when the invalidated region intersects that operation's own visual bounds and never widens a dirty region to cover a visual that merely reads it, so a whole-surface tint, a global wash, and a blur bleeding past its edge each keep compositing a sample taken before the change; the two admitted forms are a clamp to own bounds and a subscription to the sampled region's change source issuing `InvalidateVisual` per change, and the recurring move is trusting the dirty rect to widen. Instantiated at `Vfx/material#SAMPLE_CONTRACT`, binding every backdrop-reading surface the corpus grows.
- Runtime-shader compilation partitions by TYPE DOMAIN — `Render/shading#SHADER_ASSET` owns the per-`GpuBackend` appearance cache whose cells carry retained native programs, resident planes, and a VRAM ceiling, `[NOT]` the 2D chrome roster at `Vfx/shader#EFFECT_PROGRAM`, whose programs have no backend variant, no resident plane, and a CPU-side program-and-picture budget. The pressure earning the partition is a chrome program forced through the appearance cache carrying a wgpu pipeline-state arm it can never take; a compiled program is SOURCE the estate ships either way, so neither roster admits caller-supplied shader text.
- A layer opens on filtered ground or on an unfiltered copy, never on a flag pair — `SKCanvasSaveLayerRec` treats `Backdrop` and `InitializeWithPrevious` as mutually defeating, so a rec carrying both filters the ground and then overwrites it with the raw copy while a rec carrying neither opens on transparent black and erases everything beneath; the choice is therefore a closed union at the one `SaveLayer` site, and the enum arithmetic that admits the silent-discard pair is the deleted form. Instantiated at `Render/capture#DRAW_CAPSULE` `LayerGround`, consumed by `Vfx/material#LAYER_ALGEBRA`.
- A per-frame effect term redraws on the RENDER thread and a content-interleaved treatment redraws in tree — the choice is cadence, not preference. Composition animation reaches transform, opacity, and colour alone and the compositor publishes no effect, backdrop, mask, or shape brush at all, so a blur radius, a glow intensity, a shader phase, and a crossfade weight animate only by redrawing; a per-frame `InvalidateVisual` re-enters layout arbitration for the whole tree on the UI thread where `CompositionCustomVisualHandler.Invalidate` stays on the render thread, and the recurring move drives every effect from the in-tree host because that is where the content already is. Instantiated at `Vfx/compose#CUSTOM_VISUAL_TICK`.
- Numeric entry binds the TYPED spinner matching the bound CLR type, and its bounds cross in the widest form that type's family admits — integral, unsigned, binary-float, and decimal are four arms of one range union narrowed by checked generic-math conversion, so a `double` field keeps `NaN` and full range while a `ulong` field keeps its top decade and an out-of-range bound refuses at materialize; one `decimal` bound column across every numeric field silently lost all three at once, and the untyped spinner it fed clamped the loss out of sight. Instantiated at `Shell/controls#CONTROL_INTENT`, binding every numeric-entry surface.
- An in-control progress affordance lives ON the leading-visual slot it replaces, never beside it — the pending key is a field of the icon slot, so a spinner that widens its own control is unrepresentable and a pending column with no visual to take over cannot be authored; the state stays enabled-disabled honest because pending drives the loading slot alone. Instantiated at `Shell/controls#CONTROL_INTENT`, binding every busy affordance inside a control.
- A composition-bound delegate column on a materialize or projection context is earned ONLY by third-party or host construction — a third-party owner the fold must not duplicate, or a capability the running host supplies; everything the fold can construct itself constructs in the arm, and every admitted package candidate is seated at a named case or refused with its reason, so absence is closed rather than silent. The recurring move grows a context column per awkward arm until the context IS the fold; instantiated at `Shell/controls#MATERIALIZE_FOLD`, binding every context-carrying fold.
- A bound verb ATTACHES and its gesture column narrows WHICH gesture raises it, never whether one does — read as a gate, an authored command beside an unauthored trigger materializes a control that resolves its verb, passes every availability read, and can never be invoked, the one control failure a running screen cannot report on its own. The default is the gesture every case admits and the narrowings are what a value-carrying case authors, so a surface states a trigger only where the choice is real; the recurring move authors the command and leaves the optional column empty because it reads as optional. Instantiated at `Shell/controls#MATERIALIZE_FOLD`, binding every declarative binding carrying an optional gesture column.
- A capability a foreign face or asset only MIGHT implement is proved by exercising it, never by naming its tag — the managed HarfBuzz surface publishes no layout-feature enumeration, so a discretionary OpenType tag admits per face by shaping its probe with the feature on and off and comparing glyph ids, and the role's request intersects that proved set; the recurring move ships a tag roster the face silently ignores while the row claims it as capability, and re-litigation opens only if the binding grows a feature-table read.
- Text no admitted face covers is a REFUSAL, never output — the itemizer demands each codepoint of the elected face and falls to the coverage matcher before it segments, so a notdef box cannot reach a surface and a missing script is a typed fault at the shaping edge; the same law makes a shaped text the ONLY measurement authority, because an unshaped width estimate cannot know which face answered.
- Every host-variable font knob is pinned by a DECLARED render posture per surface class — edging, hinting, subpixel positioning, metric linearity, and baseline snap ride one row so a golden capture reproduces on any machine, a paged export lays out device-linear, and a run hosted on a translucent layer drops subpixel coverage it has no ground for; a per-call-site knob write is the deleted form, and instantiated at `Theme/typography#SHAPING_RAIL` it binds every raster-posture family the corpus grows.
- An animated axis declares its KIND and a CLAMPING channel refuses an overshooting token at the bind — colour, opacity, brush, shadow, and corner saturate at their domain edges, so an under-damped spring renders there as a flat hold with a visible snap out of it, while position, size, and transform carry overshoot as weight; the recurring move authors one spring token and seats it across every axis of a surface at once, and instantiated at `Theme/motion#MOTION_BINDING` it binds every axis family the corpus grows.
- Feedback form derives from the EXPECTED duration of the operation, never from the surface presenting it — one latency ladder selects no feedback, in-place motion, a shimmering skeleton, a choreographed determinate surface, or a handoff to the notification plane, and each rung names the choreography plan its surface composes rather than a second placement vocabulary; instantiated at `Theme/motion#MOTION_BINDING`, binding every long-running-operation surface.
- Floating chrome animates on transform and opacity ALONE — a rail, HUD chip, toast, palette, or overlay that animates extent or inset re-enters layout for the whole content plane beneath it on every frame of its own entrance, so extent motion is confined to in-flow disclosure where the reflow IS the effect; instantiated at `Theme/motion#MOTION_BINDING` against every chrome and overlay surface.
- A package publishing a LAYERED capability ladder receives the per-row answer at its WEAKEST rung, never its strongest — the dock resolver ranks dockable base value under root policy under dock policy under per-dockable override, so a row writing its six answers into the override wins over every policy above it and turns each one into dead configuration that still resolves, still reports itself as the deciding source, and still type-checks; the override rung is reserved for a genuine per-instance exception, and the recurring move is reaching for the strongest rung because it is the one whose name matches the row. Instantiated at `Shell/navigation#DOCK_LAYOUTS`, binding every layered-policy surface the corpus composes.
- A framework that DESERIALIZES its own object graph rehydrates live references through the locator seam it publishes, never through a second pass above it — a dock graph restores ids alone and resolves each context through the factory's `ContextLocator`/`DockableLocator`, so an unbound locator yields a structurally perfect layout of context-less shells that passes every shape check and renders empty; the stale-id arm seals the SAME typed fault the live ingress produces, so one identifier vocabulary serves both paths. Instantiated at `Shell/navigation#DOCK_LAYOUTS`, binding every restore over a foreign serializer.
- A substrate that DEGRADES silently is measured, never inferred from a channel it never writes — Cassowary relaxing a soft constraint raises nothing and refuses nothing, so reading relaxation off a fault cell reports zero on every genuinely relaxed pass and reports relaxed on a pass that only failed; the reading is a post-solve scan of each live row's own violation predicate, relaxation and refusal stay separate receipt columns and separate instruments, and a magnitude fact records its COUNT rather than one tick per pass, because one surface relaxing twelve rows and twelve surfaces relaxing one each are different systems. Instantiated at `Shell/solver#SOLVER_PANEL`, binding every soft-constraint substrate.
- A per-surface admission matrix is a COLUMN ON THE SLOT, never a predicate repeated per row — the matrix states a fact about surfaces, so N rows each re-deriving it are N chances to disagree with it and no structural check can hold them in agreement; a row-local predicate NARROWS within an admitted slot and can never widen past it, which keeps the matrix authoritative while leaving per-row conditions expressible. Instantiated at `Shell/navigation#SHELL_CHROME`, binding every visibility, capability, or availability matrix the corpus grows.
- Multiple persisted carriers restore in the DEPENDENCY ORDER of the keys they partition on, stated as law rather than left to composition order — layout restores before window geometry before per-screen state before per-viewport state, because each later carrier keys on a surface the earlier one mints, and a carrier restoring early writes into a partition its predecessor is about to name differently; the collision rule is last-writer-by-rank INSIDE one carrier and never across carriers, and an absent partition is a first-run fact that restores nothing and seals nothing. Instantiated at `Shell/navigation#DOCK_LAYOUTS`, binding every multi-carrier restore.
- A display unit is an EXPLICIT token per readout per system, never a unit-system projection — the quantity package resolves a system to a unit through full seven-axis base-unit equality and most unit rows leave that metadata undeclared, so a system-driven projection throws for the majority of the registry while succeeding for a handful, and the failure is per family rather than uniform. A readout row therefore names both its metric and its imperial unit outright and a system flip re-renders the estate by re-reading one policy value; the recurring move declares one unit system and lets each call site convert, which passes for the families that happen to carry the metadata and throws for the rest at the first unusual readout. Instantiated at `Theme/locale#MEASUREMENT_FORMAT`, binding every dimensioned readout the corpus grows.
- Right-to-left is a PROJECTION over a closed subject table, never a per-surface audit — a surface names its subject once and asks the row for a flow, so the flipping set (layout flow, chrome zones, dock sides, directional glyphs, breadcrumb and pagination order, drawer and peek anchors) and the never-flipping set (numeric axes, geometry viewports, code surfaces, timelines, media transport) are both stated in one place and a never-flipping subject PINS left-to-right at its own root rather than defending itself against an ambient cascade. Order reversal and anchor swap derive from that one predicate, so a never-flipping subject passing through either returns its input by construction; the recurring move audits each new surface for mirroring and the set drifts on the first surface nobody audits. Instantiated at `Theme/locale#MIRRORING_LAW`, binding every directional surface.
- Coverage against a FALLBACK-BEARING lookup reads the non-falling-back accessor or it proves nothing — a resource, dictionary, or config chain that silently answers a parent's value makes every key look present, so conformance enumerates the layer's OWN set and diffs it against the layer the product authors, and a layer that carries no set by construction is EXEMPT with its reason rather than scored as wholly missing. The recurring move probes the ordinary read and reports total coverage on an untranslated product; instantiated at `Theme/locale#MESSAGE_REGISTRY`, binding every layered-lookup conformance.
- A declared pipeline row names its INPUT and OUTPUT shape, and the chain is folded at declaration — so an unsatisfiable reshape is a spec defect surfaced when the board is admitted, never an empty surface at draw time, and the terminal shape is what a consumer's own arity requirement is checked against. The shape carries the magnitude COUNT beside its keyed-or-ordered posture, because those are the two facts a downstream binding cannot recover from the data itself: a five-magnitude summary feeding a two-magnitude mark renders every point as a gap and reports nothing. The recurring move validates per element on the hot path, which pays the check millions of times and still cannot refuse the declaration that caused it; instantiated at `Charts/dashboards#STREAM_BINDING` over the transform rows and read at `#CHART_GRAMMAR` by the layer encoding.
- Alert severity is a RANKED row family over the status paint ladder, and a surface reading two live crossings takes the highest rank rather than the latest — a per-surface severity enum forks the ordering from the ink, so a badge, a band, and a table cell disagree about what amber means while each passes its own check; instantiated at `Charts/dashboards#CHART_PAINTS`, binding every alerting surface the corpus grows.
- A rule watching a live stream carries a STALENESS comparator or the stall is invisible by construction — a feed that stops emitting breaches no value predicate, so every level rule reads as satisfied while the surface shows a frozen number under a caption claiming currency. The comparator therefore reads a SAMPLE carrying both the value and its age, the age advances on the rule's own probe cadence rather than on the feed, and a stream that never delivered is an empty state rather than a staleness alert, because an alert about a series that never existed names a breach of nothing. Pending-for and quiet are two columns, never one: collapsing them makes a slowly-rising breach and a fast-flapping one indistinguishable; instantiated at `Charts/dashboards#DASHBOARD_TILES`.
- Point-in-area containment binds the admitted geometry engine's indexed locator, never a hand-rolled ray cast — the locator amortizes one interval index across every query point and answers a three-valued location, so interior-versus-boundary is a stated choice rather than a floating-point tie-break, and every degenerate case a hand-rolled even-odd cast gets wrong (a vertex on the ray, a horizontal edge at the query ordinate, a self-touching ring) is the engine's own robust crossing count. The prepared-geometry predicate is the one-geometry-many-GEOMETRIES sibling; the locator is the one-geometry-many-POINTS one; instantiated at `Charts/dashboards#DASHBOARD_TILES` over the lasso brush.
- Where a field visualization samples is a `FieldSites` row, never a count — `Sampled` and `Declared` are two rows of one axis shared by `SimVisual.Streamline` and `SimVisual.Glyph`, so a topology-guided seeding declares its sites where the pass key, the receipt, and a viewpoint diff read them instead of hiding them in a delegate closure. A flow-topology visualization is therefore NOT a case: a Morse atlas renders as separatrix arcs through `Streamline` and classified fixed points through `Glyph`, and the atlas itself is projected upstream and crosses as `Declared` coordinates, so the never-re-computes law covers field ANALYSIS as well as field GEOMETRY.
- A registry whose absent-key answer is INDISTINGUISHABLE from a real result is reached only behind a mount-bound presence fact, never behind a probe — an identifier-addressed host surface that throws on zero, unmatched, and ambiguous lookups and an internal registry that answers an unregistered id with the same value a user cancel produces are the same defect wearing two faces, so the caller proves presence from the fact its own mount already holds and seals a typed refusal, while the derivation that mints the key forecloses the ambiguous and duplicate cases by construction. The recurring move probes the registry and treats a falsy answer as absence, which silently converts an unmounted surface into a cancelled user. Instantiated at `Shell/dialogs#SESSION_ALGEBRA`, binding every identifier-addressed or self-registering host the corpus composes.
- Two stacks are ONE law when the split is stated on the RESULT HANDLE — single-occupancy governs a stack whose answer is one root's awaited close parameter, because two open decisions leave that answer ambiguous, and it does NOT govern a stack of layers each carrying its own handle, where co-residency is the capability the stack exists to provide. A single vocabulary projects each modality onto its owning stack totally, so a new modality states its stack at compile time; the recurring move applies the deciding stack's occupancy rule to the co-resident one and refuses exactly the overlap the second stack was added for. Instantiated at `Shell/dialogs#SESSION_ALGEBRA`, binding every multi-stack presentation surface.
- Modality on a layered host is the DISPATCH SHAPE, never a member name and never a row column beside it — the in-canvas suite adds a plain layer from its void fire shapes and a masked one from EVERY awaited overload, so a co-resident modality reaching for an awaited result scrims the surfaces it exists to sit over while its own row still reads non-modal, and the member whose NAME says modal is an obsolete forwarder onto the awaited one that buys a deprecation and no capability. A modality column is therefore the projection of which family the arm took, and a surface whose answer is a sealed receipt rather than a close parameter owes the awaited shape nothing; the recurring move reads the awaited overload as the general one because it is the one that returns a result. Instantiated at `Shell/dialogs#SESSION_ALGEBRA` over `OverlayShape`, binding every layered presentation host the corpus composes.
- A presentation timer the product must pause, restart, or re-tint is OWNED by the product, never by the package — a manager that copies severity and expiration onto a card at show time and then awaits a bare delay cannot express a hover pause, a settle-in-place morph, or a re-timed note, so the product presents under the package's own never-auto-close posture and drives one clock off the injected time provider. The recurring move keeps the package timer and adds a second product one beside it, which races on every hover; instantiated at `Shell/dialogs#NOTIFICATIONS`, binding every queued-presentation surface.
- A package fold constrained to a CLOSED family type is unreachable from an erased face, and the admitted answer is the projection that fold would have performed — never a reflective narrowing to reach it. The quantity package's clamp, min, and max constrain their parameter to `IComparable, IQuantity`, which the boxed face satisfies for neither, so an erased schema field bounds its value by projecting through the elected unit, clamping the scalar, and rebuilding through the boxed factory; the recurring move reflects a closed generic into existence to call the named member and buys a reflection site for an answer three total operations already give. Instantiated at `Editing/forms#FORM_SCHEMA`, binding every erased-family boundary over a generically-constrained package fold.
- One policy row governing SIBLING types from several packages constrains at the framework base they share, splits its writes into a universal leg and a family-gated leg, and never at the richest sibling — the richest type is the one whose members the row's columns were drafted against, so constraining there reads as the natural bound while silently excluding every sibling the row exists to govern, and the exclusion is invisible in prose because the columns still read as a complete policy over every row. The universal leg is what the base itself publishes and the family leg rides a type pattern rather than a family discriminant column, because the type already answers which members exist and a column beside it answers the same question a second way that drifts on the first row moved; a row reaching a family with no member for a column states what the universal leg produces there instead of asserting writes it cannot make. The recurring move drafts the columns against the one sibling in hand and discovers at the mount site that the sibling packages derive the framework base directly; instantiated at `Shell/hosts#HOST_AXIS` over `WindowChrome` across the Ursa shell window, the Ursa splash, and the Dock float host, binding every policy row the corpus applies across package-sibling types.
- A package owning a geometry across its OWN item tree receives that geometry once at the host and never per row — the form mechanism republishes its label width, position, and alignment onto every descendant row through its own ancestor-walking subscription, so a per-row write is both redundant and a second source the next host edit contradicts; the same walk is what lets an intervening panel keep a row's geometry live, so nesting for furniture costs no geometry. The recurring move re-spells the package's axis as a local vocabulary and then writes it per row; instantiated at `Editing/forms#FORM_CHROME`, binding every layout mechanism composed by item.
- A verb whose subject is a ROW of a runtime-compiled surface is never a registry row — the command deck freezes at boot and a schema compiled at run time has no rows in it, so a per-row reset, per-row revert, or per-row inspect binds the surface's own arrow through the one command bridge and the deck keeps the surface-scoped verbs alone. The recurring move mints one deck row per field and discovers at the first dynamic schema that the deck it needed was already frozen; instantiated at `Editing/forms#FORM_CHROME`, binding every per-row verb over a frozen registry.
- A theme row selecting a state its control never DECLARES styles nothing — a shipped control publishes its pseudo-class roster in metadata and sets those alone, so a product state absent from that roster reaches appearance as a style class the materializing fold stamps, and a selector naming an undeclared pseudo-class resolves cleanly while painting nothing. The recurring move reads a property name off the control and assumes a matching state; instantiated at `Editing/forms#FORM_CHROME` against the form row's declared horizontal and no-label states, binding every product state authored over a shipped control.
- Two facts on one instrument dimension is a counted lie — an admission verdict and a terminal cause answer different questions in different vocabularies, so each names its OWN dimension slot and the same event is counted once on each series rather than twice under values no board can separate. The recurring move folds every string-shaped outcome onto one slot because they all read as outcomes; instantiated at `Shell/dialogs#NOTIFICATIONS` with the cause slot minted at `Diagnostics/evidence`, binding every producer writing more than one verdict vocabulary.
- A stream of SNAPSHOTS lowers into a change-set through a DIFFING operator, never an upserting one — a superseding answer is a set replacement, so an additive lowering leaves every row of every earlier answer standing while the surface reads as if it had narrowed. The two operators are one keystroke apart and their names promise the same thing, so the choice is stated at the seam with its removal semantics rather than left to whichever the caller reached for; the recurring move reaches for the operator whose name says "to change set" and discovers the leak only when a second, narrower query is run. Instantiated at `Shell/commands#PALETTE_FEDERATION`, binding every snapshot-shaped source the corpus lowers.
- A per-user override of AUTHORED data is an overlay folded ahead of the freeze, never an edit of the authored table — the authored value stays data a reset restores, a whole named set swaps by naming it, and every derived view reads one frozen result instead of reconciling an authored table against a mutation log. Absence and deliberate removal are DIFFERENT values in the overlay, because a missing entry that meant "no opinion" would resurrect a binding the user removed the moment the default gained one; the recurring move edits the authored row in place and then has nothing to reset to. Instantiated at `Shell/commands#INTENT_TABLE`, binding every user-customizable authored vocabulary.
- Progress is a COLUMN of the answer, never a second stream beside it — a producer that emits rows on one stream and status on another lets a settled status arrive beside a stale row set, which makes an honest empty and a still-loading state indistinguishable exactly when a surface must tell them apart. One emission carrying both facts also costs one subscription, so a leg cannot be opened twice to be asked two questions; the recurring move adds the status stream later, when the empty state is first noticed to be lying. Instantiated at `Shell/commands#PALETTE_FEDERATION`, binding every federated or streaming answer the corpus renders.
- A scoped SOURCE and a scoped ACTUATOR are the same shape — an armed output carries its receipt, its sink, and its own teardown exactly as an opened input carries its receipt, device, stream, and teardown — and that teardown QUIETS the device before releasing the handle, because physical state a process set outlives the process: a motor keeps spinning at the last speed written, a rumble effect keeps playing, a control surface keeps every fader driven and pad lit, and no release path in any of these SDKs clears them. The recurring move returns the sink bare and leaves the release site to the caller, which reads as symmetric until the first unplugged-hands test; instantiated at `Shell/input#DEVICE_DRIVERS` over the gamepad, haptic, and MIDI arms, binding every actuator a capsule arms.
- A value object whose member is a reference-equating collection (`FrozenSet`, `FrozenDictionary`, an array) needs an EXPLICIT distinct key wherever its stream deduplicates — the generated structural equality falls through to `EqualityComparer<T>.Default`, which is reference identity for these carriers, so every emission reads as changed and a gate that looked deduplicated re-evaluates on every upstream tick. The recurring move trusts the value object's own equality because every OTHER member of it equates structurally; instantiated at `Editing/forms#SELECTION_MODEL` over the selection snapshot's kind set.
- Contested-resource ownership has ONE oracle, read by the admission that refuses and by the editor that assigns — a freeze-time conflict check and an assignment-time conflict check that compute the same fact twice will disagree, and the disagreement surfaces as an assignment the editor accepted and the next freeze rejected. The oracle answers with the CLAIMANT LIST rather than a verdict, because a surface that must show the conflict needs the owner's name and a boolean forces it to re-derive one; the recurring move writes the editor's check against the winning binding table, which hides every loser the table already dropped. Instantiated at `Shell/commands#BINDING_EDITOR`, binding every uniqueness constraint a user can edit into.
- A key a SURFACE raises is a row on the frozen registry or the surface does not materialize at all — a tree's expansion command, a strip's jump command, and a result kind's reveal verb each resolve against the boot-frozen deck inside the materialize fold and abort the whole fold on a miss, so an owner roster left unlifted is a dead SCREEN rather than a dead button, and a reveal key composed beside an owner that already declares the act is one act under two keys where only the unrostered one is ever raised. Every raised key therefore names its owner's own constant and lands its registry row in the same pass, and the registry's family table is the census that proves it; the recurring move authors the key at the raising surface and trusts composition to have found it. Instantiated at `Shell/commands#INTENT_TABLE`, binding every verb the corpus raises.
- Provenance-distinguished rows of ONE concept live in one registry under one key space, discriminated by a provenance column — standard views, user bookmarks, captured cameras, and traversal history are one `NamedView` family, so back/forward reads the rows a bookmark list renders, a standard view is bookmarkable, and a shared link addresses any of them identically. Two registries sharing a verb set force every consumer to ask which one holds a key, and the answer changes the moment a user bookmarks the entry they just stepped back to; a bounded traversal ring therefore stores KEYS rather than values, so a renamed row keeps its history position and a deleted row's entries retire with it instead of dangling. Instantiated at `Render/pipeline#VIEW_REGISTRY`, binding every recall-and-history surface the corpus grows.
- A view TOGGLE between two projections preserves the framed extent, so toggling twice is the identity — a perspective lens at distance `d` with vertical field `f` frames `2·d·tan(f/2)`, which becomes the orthographic view height, and the inverse re-derives the distance at the retained field. The recurring move swaps the case and keeps whichever scalar the target case happens to carry, which reads to a user as an unexplained zoom on every switch; instantiated at `Render/pipeline#VIEW_REGISTRY`, binding every projection or unit toggle over a framed quantity.
- A manipulator COMMITS through the settled model it manipulates and renders through the frame's existing overlay pass — the section handles rewrite the one `SectionBox` a viewpoint captures and a BCF exchange projects, so the interactive state, the saved view, and the exchange are one value; a manipulator holding its own copy is what lets a saved view restore a state the user already dragged away. A constrained drag is constrained BY CONSTRUCTION (each plane row owns the single ordinate it writes) rather than by a modifier key, and a motion that would invert the model refuses rather than clamping, because a clamped inversion leaves a user holding a handle they cannot see. Instantiated at `Render/pipeline#SECTION_MANIPULATOR`, binding every direct-manipulation surface over a settled model.
- A displayed LABEL over dimensioned values is an expression evaluated under quantity-family rules, never a formatted scalar — addition and subtraction demand one family, multiplication and division admit exactly one scalar operand, and a mixed-family term refuses by name. A scalar evaluator sums two magnitudes whose units differ and prints a number that is neither, with no fault anywhere; instantiated at `Render/pipeline#MEASURE_MODE`, binding every user-authored expression over measured values.
- A DISPLAY POSTURE is a fold onto the settled visibility-override channel and never a viewer-local display mode — colour-by-property, participation-role recolouring, precision wireframe, version ghosting, and hover highlight all answer the same override rows, so postures compose through one merge, a posture's product is captured by a saved view, and a shared link reproduces what the author saw. A colour session's palette class and legend arm are one fact its property domain DERIVES from the values, because a categorical property under a sequential ramp reads as a magnitude the data never carried and a numeric property under swatches loses the ordering that is its whole content; instantiated at `Render/pipeline#VIEWPOINT_CODEC`, binding every scene-wide presentation channel.
- A TRANSIENT emphasis and a USER-ISSUED posture are two rows of one fold, never one row serving both — a hover highlight emits a row for EVERY element so the previous hover's ghosts clear without waiting for a reset, and it ghosts more lightly than the posture a user lives inside; folding hover onto the persistent row makes a scan across a table read as a strobing scene, because each row's fold leaves the previous row's ghosts standing. Instantiated at `Render/pipeline#VIEWPOINT_CODEC` over `VisibilityAction`, binding every brushing channel the corpus grows.
- An EDITOR over a sorted, non-empty carrier re-enters that carrier's own admission gate on every structural edit rather than rewriting it in place — a moved, added, deleted, or re-eased keyframe run re-sorts and re-proves through the same constructor an authored run passes, because a binary-search bracket over an unsorted run answers a plausible wrong value at every sample instead of failing where the defect is. Instantiated at `Render/animation#TIMELINE_EDITOR`, binding every editor over an invariant-bearing collection.
- Two mute-class flags over one roster resolve through ONE audibility answer — a non-empty solo set narrows to the soloed rows and mute applies only where nothing is soloed — and the narrowing is applied by the EDITOR as a composition the sampler never learns about. Consulting the flags independently makes a muted-and-soloed row's behaviour depend on which check runs first, a question no user can answer and no test can pin; instantiated at `Render/animation#TIMELINE_EDITOR`, binding every override-pair vocabulary.
- One TRANSPORT grammar serves every playback surface — play, pause, stop, step, jump-to-bound, loop, and speed as one verb roster with one state fold and one command key, so a scrub, a timeline, an offline render, and a clip all read that roster and a verb added to it reaches every surface with zero edit — and a subordinate medium's time is a keyframed function of the master clock rather than a clock of its own. A second vocabulary beside it, down to a surface-local next/previous pair, forces every surface hosting both to translate, and the translation is where a paused clip under a playing timeline comes from; speed scales the tick CADENCE and never the frame step, because a half-speed playback must render every frame slowly rather than every other frame quickly, and where a surface's frames carry no wall-clock rate the playhead is minted at one ordinal per unit with its position never read, since binding recorded instants to a rate makes a step skip ordinals exactly where an operator is stepping. Instantiated at `Render/animation#TIMELINE_EDITOR` and consumed at `Diagnostics/devloop#LOOP_SURFACES`, binding every playback surface the corpus grows.
- Authored WEIGHTS in physical output units are never scaled by a display transform — a stroke weight authored in paper millimetres prints at that width from every canvas, and screen legibility rides a separate per-canvas display scale applied to the preview paint set alone. Folding the zoom into the authored weight bakes whichever zoom the author last used into every emitted format, replacing a drawing standard with a UI state; instantiated at `Render/drafting#SHEET_COMPOSER`, binding every physical-unit authoring surface.
- A PREVIEW of an emitted artifact consumes the emitter's own projection, never a second render path — a plot preview folds the identical entity run the print arm pages, under a paint set differing by the display scale alone, so what the reviewer approves and what the plotter produces cannot diverge. A preview-local render path is the only way a preview can lie, and it lies exactly where the reviewer stops checking; instantiated at `Render/drafting#SHEET_COMPOSER`, binding every preview-of-output surface.
- A modality-specific chrome plane RENDERS the settled control vocabulary and mints no controls of its own — an immersive panel is a `ControlIntent` tree on a quad through the one control factory and the one paint catalog, and its ray pick answers PANEL PIXELS so every desktop hover, hit-test, and press path is reachable unchanged. A modality-local widget family forks every verb, every label, and every availability rule at the modality boundary; comfort, meanwhile, is policy ROWS a user sets, because comfort tolerance varies by person more than any other setting and a fixed value makes the modality unusable for the users it does not suit. Instantiated at `Render/immersive#XR_REVIEW_CHROME`, binding every alternate-modality chrome plane.
- A pigment is addressed by the key its GENERATION minted and never by a composed string — a paint spec, an effect row, and a token edge all take the token owner's own key type, so a spec naming a rung the ladder never emitted refuses where the key is composed rather than resolving to a catalog miss at draw time; the draw-site ROLE stays its own string vocabulary, because a role addresses a paint and a pigment addresses a colour and one string type over both lets each be spelled with the other's name. Instantiated at `Render/capture#DRAW_CAPSULE`, binding every generated-key consumer.
- A domain-scoped artifact ELECTS its own measurement posture over the reading user's, and only the posture — a sheet declares metric or imperial and every label on it renders in that system while the locale keeps its number formats, fraction denominator, and grammar rows. A locale-elected unit on an artifact that declared its own prints the same drawing differently for two users, under a title block that states one of them; instantiated at `Render/drafting#DIMENSIONING`, binding every artifact carrying its own unit declaration.
- A surface presenting a settled instrument SEALS nothing — every fact it renders is already on the envelope stream, so a presentation-layer seal re-bills the duration the usage fold already accrued and re-counts the fan write the producing arm already made; the surface owns projection, zoom, grouping, hit-testing, and filtering, and its instrument owns measurement. The recurring move gives a new face its own receipt because the face is new; instantiated at `Diagnostics/devloop#LOOP_SURFACES` over the flame, scrub, block, and chip surfaces, binding every projection over sealed evidence.
- One nesting arithmetic serves every reading of one value tree, answered in UNIT FRACTIONS with the source index riding each span — a radial ring, a rectangular flame, and a pointer hit-test each scale those fractions into their own coordinate, so the rectangle a pointer resolves is the rectangle the plane drew and a repair to the nesting lands once. Two folds over one tree agree until the first repair and then disagree silently, because nothing compares them; a second payload case for the second reading is the same defect wearing a type name, since the readings differ in FOLD and not in data. Instantiated at `Charts/custom#SKIA_KINDS` (`WedgeSpans` under the sunburst, the flame, and the devloop hover), binding every plane carrying two readings of one nesting.
- A golden capture pins every HOST-VARIABLE raster input as a cell column, and text posture is one of them — subpixel coverage carries the panel's stripe order and hinted outlines carry the platform rasterizer, so a golden admitting the live posture diffs on the machine rather than on the change. Posture is a COLUMN rather than a constant because a screen golden and a paged-export golden must each pin their own reading, and one file two lanes overwrite in turn proves neither; the cell key spells every pinned axis so a break names the axis it moved on. Instantiated at `Diagnostics/proof#CAPTURE_LANES` over `CaptureRow.Posture`, binding every committed pixel golden.
- A surface that DISPLAYS a solved result computes none of it — every value it renders is a projection of a sealed receipt, and the only arithmetic it may perform is a display read whose output is provably inside the range the receipt already carries: a barycentric weight over three sealed samples and a mean over faces the payload already connects both qualify, while a re-bin, a re-sample, a unit conversion, and a fill-in for a coordinate nobody ran do not. The elected fidelity is stamped on the result at construction, so a reduced-model answer can never be read as a detailed one on a board, in a report, or in a comparison cell; a run that sealed nothing yet is ABSENT rather than zero, and an estimate with no prior run answers absent rather than an extrapolation, because a fabricated number is what teaches an operator to stop reading the readout at all. The recurring move fills a gap in the display layer because the data is right there; instantiated at `Analysis/layers#RESULT_LAYER` and `Analysis/context#BUDGET_METER`, binding every surface over a solver it does not own.
- An environmental coordinate has ONE owner and every consumer reads it — a layer, a diagram, a sun-position read, a shadow study, and a bound chart series each holding its own date is a screenshot showing July in one pane and January in the next, and the disagreement is invisible until someone exports it. The context therefore publishes PROJECTIONS rather than being copied: a linear scrub track for the playhead, an absolute range for chart binding, and a bounded variable for comparison, so a consumer binds an arrow instead of holding a value. Scrub easing is LINEAR by construction, since a curved playhead accelerates the sun through midday and every derived shadow and irradiance inherits the distortion; a projected climate horizon shifts the YEAR a record is read in and never the solar geometry, which orbital mechanics fix regardless of an emissions pathway. Instantiated at `Analysis/context#TEMPORAL_AXIS`, binding every shared coordinate the corpus grows.
- A bounded partition of ROWS folds its residual into one member; a bounded lattice of SCENES truncates and states the truncation. The facet residual works because unioning N partitions' rows renders a real chart, and the same move over compare cells renders a scene that is the union of twelve options — which nothing can draw and no reader can interpret. The cap therefore publishes the held-back count for the header rather than a thirteenth cell, and the difference is the shape of the thing being capped rather than a preference. The recurring move copies a settled overflow whole because both surfaces are grids; instantiated at `Analysis/compare#COMPARE_CELL` against `Charts/dashboards#CHART_GRAMMAR`, binding every capped surface the corpus grows.
- A comparison shares its VALUE SCALE or it is a gallery — cells painted against their own measured extents render a twenty-percent shortfall identically to the reading beside it, which is the exact fact a side-by-side exists to show. The union is over every bound cell's own span and it re-seats on the FIRST cell's arm, so a threshold list or a code dictionary the cells were graded against survives the widening and only the bounds move; incompatible arms REFUSE rather than unify, because a continuous field and a coded classification carry no common ramp and a gradient across class codes states a magnitude nothing measured. Instantiated at `Analysis/compare#SHARED_CHANNELS`, binding every multi-cell surface over one measured quantity.
- A string a materialize resolves against a registry is a DECLARED constant on the surface that owns it, and two registries take two constants — a named-producer intent carries a source key the frame lookup resolves, an intent key the solver stamps as child identity, and a verb key the boot-frozen deck must already hold, so a spelling reconstructed at the construction site is a control the deck aborts on and one literal serving both key roles lets a second producer bind a control by accident. The recurring move derives the neighbouring key inline because the surface's own root is right there, and the two registries look like one because a scroll strip has only one visible affordance; instantiated at `Editing/inspector#CODE_EDITING` and `Editing/history#TIMELINE_SURFACE` over the overview strips, binding every named-producer intent the corpus grows.
- A declared instrument carries a WRITE SITE on the page that declared it, and a plane's whole instrument set writes through ONE observation fold — a `TelemetryRow` naming three rows beside a fold that writes two ships a series a board plots, an operator reads, and nothing ever fills, which is indistinguishable from a plane nobody used and is exactly how a capacity signal comes to report zero forever. The fold takes every fact as a column and writes only the columns it carries, so an absent study writes no adoption and an unanswered probe writes no reading rather than a fabricated zero; the recurring move adds a row to the declaration because a board asked for the tile and leaves the producer for the surface that raises the act. Instantiated at `Analysis/layers#LAYER_STACK` and `Analysis/compare#COMPARE_CELL` over their `Observe` folds, binding every page contributing instruments.
- A closed vocabulary of CHANNELS carries each channel's fold on its own row, and a row with no per-tick fold earns its place as an ADMISSION at another edge rather than standing inert — a link set resolved by one predicate per channel is one branch per row re-read at every site that composes two of them, and a row nothing ever reads is a declared capability with no consumer at all, which reads as configured while doing nothing. The declared set therefore folds through one traverse over the roster and the odd row states which edge it gates, so a bake, an export, or a mount refuses where its channel is undeclared instead of producing the unrelated artifacts the channel existed to prevent. Instantiated at `Analysis/compare#SHARED_CHANNELS` over `CompareLink`, binding every declared-channel vocabulary the corpus grows.
- A shared coordinate whose POLICY column shifts what a consumer reads publishes that shift as a SECOND projection off one derivation, never as a mutation of the primary one — a climate horizon that moved the window every consumer bounds would move the sun with an emissions pathway, while a horizon column no projection consumes is a policy a study can never be read at, and the two failures are the same defect from opposite sides. One civil derivation therefore feeds both projections and each consumer names the one it is honestly read at, so a physical series and a policy-shifted series cannot be captioned with each other's coordinate. Instantiated at `Analysis/context#TEMPORAL_AXIS` over `Window` beside `Record`, binding every coordinate carrying a policy horizon.
- A SEQUENCE crossing an owner boundary stays a sequence — joining it into a delimited string collapses the empty sequence and the one-empty-element sequence onto one value, and those two are exactly the pair a downstream fold has to tell apart: a deleted region against a blank line, a dropped column against an empty cell, no tags against one unnamed tag. The collapse is silent at the join and surfaces far away as a merge that takes the wrong side, a blank line nobody typed, or a consumer re-splitting the string and guessing the empty case back from its own neighbouring state. A run therefore travels as `Seq<string>` and its length is `Count`, with the join deferred to the ONE edge that genuinely renders text; the recurring move joins at the last step of a fold that carried the sequence at every earlier one, because the field reads as text at the seam. Instantiated at `Editing/inspector#CONFLICT_RESOLUTION` over `ThreeWayHunk`, binding every run, roster, or cell set the corpus hands across an owner.
- A sealed RECEIPT is not a landing — a TOTAL execution rail seals a receipt for a rejected, cancelled, and faulted run exactly as it does for a completed one, so a fold reading only the receipt's arrival treats every refusal as success: it clears the pending marks, records a revert step for a write nothing applied, hands a queue a submission for a run that never started, and counts all of it under the applied instrument while the rejected one stays at zero. The OUTCOME is the fact, and it admits once per page onto the same typed rail every other refusal takes rather than at each fold; the recurring move trusts the carrier precisely because the effect rail did not fail, which is the guarantee a total rail was built to give. Instantiated at `Editing/forms#BATCH_EDIT` over `BatchEdit.Landed`, binding every consumer of a totalized execution rail.
- A ROW address and a DOMAIN ordinal are TWO spaces the moment disclosure, grouping, or a band puts anything in the row stream the domain does not count, so a seek that answers a row index converts through the addressed row's own key before any domain fold consumes it. The two spaces agree exactly while nothing is expanded, which is the state every first reading is in, so the defect ships and then appears only under disclosure and only as a distance that is quietly short; the recurring move hands the ledger's index straight to the domain because both are `int` and both are called an ordinal. Instantiated at `Editing/history#TIMELINE_SURFACE` over `OrdinalAt`, binding every surface whose virtualized rows and whose domain steps are not one-to-one.
- A READ-ONLY seat over a shared editing chrome binds that chrome's mutating callback to a NAVIGATION arrow rather than accepting one from its caller — the chrome is the editing owner's and its gutter, band, and margin surfaces take a resolution callback because a merge resolves through them, so a seat whose whole law is "renders and never computes" either supplies its own arrow or hands every caller a mutation channel into a surface that declares itself read-only. The arrow that survives is the one action the read-only reading admits, which is seating the cursor at the addressed row, so the chrome keeps its affordance instead of rendering three dead verbs; the recurring move forwards the parameter because the mount signature asks for it, and the read-only claim then lives only in prose while the seam contradicts it. Instantiated at `Document/media#DIFF_SEAT` over `HunkBands.Attach`'s take arrow, binding every read-only projection reusing a mutating owner's chrome.
- A tiled payload rides the carrier its PAYLOAD ARITY admits, never the one its transport shares — a raster tile layer answers ONE feature per tile into a raster-styled carrier, so a vector tile decoding to a feature SET collapses into a single geometry and loses every attribute the symbology selector inks by; the admitted form mounts the pyramid as an ordinary provider on a vector layer, where the schema's own covering-tile arithmetic supplies the LOD and every decoded row crosses the SAME ingress gate a resident row crosses, so a tiled set and a resident set stay one draw path under one CRS law. The recurring move reads "tiles" as the shared noun and mounts both on the tile rail, where the collapse stays invisible until a style keys on an attribute the carrier never kept; instantiated at `Charts/basemap#NTS_OVERLAY` over the Bim MVT pyramid, binding every tiled source the corpus mounts.
- A durable payload names a vocabulary ROW, never that row's key spelled as text — a generated keyed owner stamps its own converter, so the wire still carries the key scalar while a row that does not exist refuses at construction rather than resolving to nothing at re-render; a text key additionally forks the producer, because the surface that OWNS the vocabulary already answers the typed row and a transcription between the two drifts on the first rename. The recurring move spells the key because the payload is bound for JSON and a string looks like the wire form; instantiated at `Charts/basemap#REDLINE` over `RedlineStyle`'s paint and fill against the tool family's own `PaintRole`, binding every persisted policy the corpus writes.
- A fault case with NO fence raising it is deleted from its union, and a condition a sibling band already owns never mirrors into a second one — an unraised case reads as declared capability, so a reviewer counts the union as covering a failure nothing detects, and a mirrored case gives one condition two codes whose receipts never aggregate. The band's own width is what leaves room for a real case to land later, so speculative rows buy nothing; where the condition is real but the raise is missing, the repair BINDS the case rather than cutting it, because the cut would delete the only statement that the failure exists. The recurring move mints the roster from the section's prose and never returns to ask which arms raise which; instantiated at `Vfx/material#LAYER_ALGEBRA` and `Vfx/compose#VISUAL_ACQUISITION` over the tile and interop cases neither plane owns, binding every typed fault union in the package.
- A PER-DRAW native is owned by the capsule that built it, and a scoped paint owns none of what it binds — `SKPaint.Dispose` releases neither the shader, the image filter, the colour filter, nor the path effect in its four slots, so a filter stack rebuilt per frame under a `using` paint leaks one handle per row per frame while reading as scoped. The release therefore rides the capsule on BOTH exits, and the build FOLDS rather than traverses, because a short-circuiting traverse drops every native minted before the offending row with no other owner holding them; the same law makes a refused admission release the pair it measured rather than the fault alone. The recurring move reads the `using` on the paint as covering the natives the paint points at; instantiated at `Vfx/material#MATERIAL_EXECUTION` over the filter stack, the grain source, and the wash blend, binding every plane minting natives per frame.
- A presence probe over a foreign block that answers its DECLARED schema proves nothing about that block's CONTENT — `SKRuntimeEffectUniforms.Contains` tests the compiled program's own name roster, so a coverage gate written against it reads every declared name as present on a block nothing has bound and the gate that names the page's central law can never fire. Coverage is therefore the fold's OWN record of what it wrote, tested against the declaration, which is also what retires a defensive reset: total coverage forecloses the staleness the reset existed to clear, and the reset's own cost was a native buffer abandoned per frame. The recurring move reads a `Contains` as a state question because the block is the thing being written; instantiated at `Vfx/shader#UNIFORM_FRAME`, binding every gate over a foreign block that publishes its schema.
- A value that varies by MOUNT is a mount parameter, never a column on the vocabulary the mount names — the ground a material composites on depends on whether pixels exist beneath the surface, so an embedded host and a translucent window take different arms under one tier, and a per-tier arm can express neither while costing an edit on every tier the generation later grows. What the plane genuinely owns is the OVERRIDE its own resolve forces, which is one rule rather than a roster; the recurring move reads the estate's current mounts as a per-row constant because each tier happens to have one. Instantiated at `Vfx/material#MATERIAL_EXECUTION` over `LayerGround`, binding every host-varying posture the corpus resolves.
- A union that outgrew its registry band WIDENS the band into its folder decade's tail and restrides the successor rows behind it — a registry-derived code is a construction guard, so a case past the bound throws where the union's own static case initializes and takes the whole type down at boot rather than surfacing on a rail. The stride is a default rather than a law: a decade holds the unions a folder happens to carry, so the tail exists precisely to absorb the one that grows, and the successors move because their keys carry no external contract while the neighbourhood's outer bound is the reciprocal pin a sibling registry mirrors. A folder whose hundred is FULLY assigned seats its next union on the neighbourhood's next unassigned decade rather than colliding or spilling past that outer bound, because the disjointness fold and the reverse index both resolve on the span alone and the prefix answers a reader's eye rather than an address. The recurring move reads the append-only rule as covering the SPAN column too and leaves an over-full union unrepairable, or appends past the bound because the next case number looks free; instantiated at `Diagnostics/evidence#FAULT_TABLES` over the chart and proof unions and the exhausted Shell hundred, binding every band the registry strides.
- Two INDEPENDENTLY authored rosters merged into one keyed sink refuse a shared key BY NAME on the rail, because the collision is invisible to either roster's own conformance and the sink's admission throws — a resource dictionary, a registry, and a frozen index all reject a duplicate rather than reporting it, so the throw escapes whatever rail the merge sits inside and lands as a boot failure with neither roster named. Each roster is total against its own vocabulary and neither can see the other's claims, which is why the proof belongs to the MERGE and not to a rule either author could have followed; the recurring move folds the merge with the sink's own admission because a single-roster fold never collided. Instantiated at `Theme/tokens#CONTROL_THEMES` over the emission partition folding the Semi correspondence beside the node-editor skin, binding every multi-roster merge the corpus writes into a refusing sink.
- An untyped property write names a slot the TARGET TYPE registers, so a per-type slot table is exhaustive over its own parked roster and never total by a content fallback — `AvaloniaObject.SetValue`/`ClearValue(AvaloniaProperty)` throws on an unregistered slot, and the archetype is `TextBlock : Control`, which carries no content property at all, so a label bound and cleared through `ContentControl.ContentProperty` faults on its FIRST recycle rather than on its first bind, past the `Fin` rail the fold exists to refuse on. The admitted form gives every parked type outside the content hierarchy its own row and leaves the content default to real content hosts (`Skeleton` does derive `ContentControl`, so its default is honest); the recurring move writes the fallback arm from the most common base and never asks which types reach it, because both the bind and the clear read as ordinary property writes at the call site. Instantiated at `Shell/controls#MATERIALIZE_FOLD` over `ControlFactory.ContentPropertyOf`, binding every untyped slot table the corpus writes through.

- A decoration measured in LINE SPANS and the text it decorates inhabit ONE document, so a surface rendering a derived subset of a source carries that subset's own coordinates or every span it was handed addresses a space its document does not have — and each consequence is silent: a segment past the last line drops at the collection's own guard, the strip lane it published goes empty, a collapse region folds nothing, and a scroll clamps to the end. The geometry therefore comes from the value being rendered rather than from a caller, because a supplied span arrow is a second authority over where a run sits and nothing compares it against the text; where the value carries several documents, every read is addressed by the same ordinal the pane roster is — text, span, and region set alike — since two cuts diverge in line numbering exactly where the change the surface exists to show sits, and one span function over both measures the second against the first. The recurring move builds the pane from the changed runs alone because those are the rows in hand, and the result renders as a changes-only list with no marks and no context, which reads as a design rather than as a defect. Instantiated at `Document/media#DIFF_SEAT` over `DiffSurface`'s pane-addressed reads, binding every surface decorating text it did not itself assemble.
- A projection that OPENS a host resource is an owner, whatever its signature suggests — a markdown render that mounts a code pane per fence holds one grammar installation, one search overlay, one folding manager, one resource-bound ink set, and one input handler per fence, and a document re-renders on every theme swap, every locale flip, and every keystroke of its own source. The produced value therefore CARRIES what the pass mounted and the surface disposes the previous one before seating the next, and a consumer wanting only the pure half of that pass takes a projection that mounts nothing rather than a render it then cannot release: an outline read off a full materialization pays a control tree and a live session for a heading list, on the one plane that recomputes its outline whenever a cell's source changes. The recurring move reads a `Seq`-returning static as pure because nothing in its return type says otherwise; instantiated at `Document/media#MARKDOWN_BLOCKS` over `MarkdownRendered` and its anchor-only sibling, binding every projection that mounts.
- A multi-seat mount carries CUSTODY across its roster and a re-seat mounts before it releases — a traverse that short-circuits on the second seat strands everything the first already took, and a dispose-then-mount re-seat makes every refusal destructive exactly where the refusal check was added to prevent one. The fold therefore threads the seats already taken and releases them on the refusal that ends it, while a geometry change answers a NEW value and disposes the standing one only once the new one exists, so a host that cannot supply the new geometry leaves the operator looking at the surface they had. The recurring move reaches for the traverse because the per-seat mint already returns the rail; instantiated at `Document/media#DIFF_SEAT` over the pane fold and the layout toggle and at `Editing/graph#COEDIT_BRIDGE` over the two-container co-edit bind, binding every surface mounting more than one host resource per act.
- A read the interior performs BY CONSTRUCTION is proved at the MINT, because its failure mode lands outside every rail the owner declares — an ordinal into a carrier whose positional read throws faults out of the constructing expression itself, where no rail exists to name it, and a magnitude narrowed into an integral domain it overflows WRAPS, which a ceiling comparison then reads as a request comfortably inside the bound and admits exactly what the gate existed to refuse. The mint therefore proves every companion roster's ordinals against the roster they index and folds a gated magnitude in the real domain, pinning at the integral bound so an impossible request refuses by name, and the derived structure depending on either runs only behind that proof. The recurring move admits the fields a caller supplies and treats an index and a product as arithmetic rather than as admission, so both defects survive every type check and every review of the rail; instantiated at `Analysis/layers#RESULT_LAYER` over the payload's face ordinals and `Analysis/context#BUDGET_METER` over the lattice cell count, binding every owner whose interior indexes or multiplies what its caller handed it.
- A plane projecting into a settled render, effect, or pass owner answers that owner's EXECUTABLE form and never the declaration one hop before it — the entry takes an argument only the producing plane holds, so a projection answering the bare case hands its consumer a value nothing can enter, and the gap is invisible because both halves type-check while the boundary prose still reads as composed. Where the entry's argument is a column the producer's payload does not carry, the PAYLOAD widens to carry it rather than the projection shipping the case, because that argument is what the owner's family is parameterized over and minting one at the seam is a shim wearing a composition's name. The recurring move stops at the case because the case is what the boundary names as the owner; instantiated at `Analysis/layers#LAYER_STACK` over `LayerStack.Passes` against `Render/pipeline#SIM_VISUAL`, binding every plane projecting into a settled executable family.
- A standing fact has THREE declarations — a keyed pulled family, a scalar pulled row, and a pushed gauge — and the write site's shape elects which: a scalar depth declared as the keyed family owes a tag no single value fills, a keyed family written with no key answers the family's unpartitioned entry rather than the cell a board plots, and a fact carrying more than one dimension can only be the pushed gauge, because a pulled family partitions on exactly one tag and its cells are reached through the level entry alone, so declaring it there leaves every dimension past that tag permanently unwritable AND routes each push onto the pushed-row refusal. The same law one axis over governs the measurement TYPE: a row binds its handle at the type its declared form names, so a write at a narrower type lands on the type-mismatch verdict while the call site reports a rail that carried, and the series stays permanently empty under a producer correct at every reading. Declaration and write site are chosen together per ROW and never per instrument family; the recurring move reaches for the keyed factory because the level reads as plural and hands the carrier's own count width because that is the value in hand. Instantiated at `Analysis/layers#LAYER_STACK` and `Analysis/compare#COMPARE_CELL` over the mounted-depth rows and at `Editing/livedata#AGGREGATION_SPINE` over the feed-age and pending-overlay gauges, binding every instrument the corpus declares.
- An owner holding a LIVE mutable cell is a sealed class and its transitions are pure functions that ANSWER what they retired — a record forks exactly the state it exists to own, since a copy shares the cell by reference while duplicating every counter beside it, so two owners drive one cell under two clocks and the resource each installed belongs to neither, while value equality over a live native handle, a device binding, or a lease delegate answers nothing a caller can use. The transition body releases nothing, because the CAS cell re-runs it on every losing attempt and a handle freed there is a live cell pointing at a dead native; it records what it unlinked on the value it installs, the caller drains that record once after the exchange, and every transition on the cell rewrites the record, so nothing survives its own act and nothing is released twice. The recurring move reaches for a record because the owner reads as a value at its construction site and moves the release into the swap because that is where the superseded handle is in scope; instantiated at `Render/pipeline#RENDER_GRAPH` over the resolve cell, `Render/reality#CAPTURE_PASS` over the retained decode set, `Render/pathtrace#PATH_TRACE` over the accumulation film, `Render/shading#SHADER_ASSET` over the plane residency, and `Editing/graph#CANVAS_VERBS` over the camera's bookmark roster and the find cursor, binding every owner of shared mutable state the corpus grows.
- A COUNT instrument is fed what happened since the previous receipt and a LEVEL what stands right now, so the cell behind a count DRAINS at the seal that reads it — a lifetime running total handed to a counter re-adds the whole history at every write, the series sums the triangular number of its own readings, and a cache that admitted one plane reads as runaway admission under a producer correct at every site. The drain also retires the standing accessor: the lifetime total is what the counter's own series sums to, so an owner publishing both the cumulative property and the counter carries two answers to one question and a reader cannot tell which the dashboard plotted. The recurring move reads a monotone counter cell as the natural argument for a counter instrument, and the defect survives every review because both halves of the sentence say count; instantiated at `Render/shading#SHADER_ASSET` over the mint/reuse/refuse admission triple and its eviction count, binding every measured cell the corpus contributes.

- A bound surface publishes its PRODUCT beside its lifecycle, and a bind that answers only lifecycle is a subscription whose payload nothing reads — the shape passes every review because it compiles, subscribes, and reports `Ready` on every delivery while the surface it feeds stays empty forever. One product union keyed by the surface that made it is what forecloses it: a callback per product class means the cases whose product has no callback quietly drop theirs, and the drop is invisible precisely because the lifecycle sink is still firing. Instantiated at `Charts/dashboards#DASHBOARD_TILES` over `TileRender`, binding every mount that subscribes on a surface's behalf.
- A total re-reads the BASIS its rows already carry and never recovers it by inverting a derived column — a ratio divided back out of its own numerator is a lossy inverse that answers NaN wherever the numerator is legitimately zero, and that NaN propagates into every total above it while each contributing row still prints correctly. The basis is therefore a column on the reading beside the ratio, since the fold that produced the ratio held both. Instantiated at `Charts/telemetry#METRIC_PANEL`, binding every aggregate whose rows carry a measured value against a declared target.
- A SCHEDULED retirement is custody per SUBJECT, and one serial slot serving a whole roster is the deleted form — arming that slot for a second subject disposes the first one's pending release, so that row stands in its terminal posture forever, outranking the truth beside it under chrome nothing will ever clear, and the leak is invisible because each retirement worked perfectly while it was the only one in flight. Each subject therefore arms and re-arms its own slot and the terminal act retires the slot with the subject, so a long session accumulates neither stranded rows nor slots for subjects nothing holds; the recurring move reads a serial slot as "the one pending timer" because at the moment it is written there is only one. Instantiated at `Editing/livedata#OVERLAY_SPINE` over the refusal linger, binding every deferred retirement the corpus schedules.
- A latch guarding a WHOLE-STATE reconcile HANDS its refused arrival to the holder rather than dropping it — the reconcile reads the entire container, so an arrival landing after the holder took its read describes a state that pass could not have seen, and returning early leaves the surface on a superseded state until an unrelated edit happens to fire the same source, which no receipt records and no later merge repairs. The holder therefore clears the arrival mark before each pass and re-checks it after releasing, closing the window between the last read and the release and collapsing any burst into exactly one extra pass, at no cost wherever delivery is already serial; the recurring move reads the early return as de-duplication because the pass it would repeat carries the same name. Instantiated at `Editing/graph#COEDIT_BRIDGE` over the remote-apply latch, binding every re-entrancy latch over a whole-state reconcile.
- A gesture whose COMMIT IS DEFERRED carries the frame its samples were taken under, because view coordinates mean nothing without the camera that produced them — a stroke, a lasso, a rubber band, and a drag all end before their commit runs, so unprojecting through the MOUNTED camera lands every result displaced by exactly whatever pan, zoom, or rotation happened in between, and the displacement is invisible because the capture succeeds, the commit succeeds, and nothing compares the two frames. A camera is a small value, so the frame rides the ingress case and the stale-frame commit becomes unrepresentable rather than guarded; the recurring move reads "the live viewport" as the current truth, which it is — just not the one the samples were drawn against. Instantiated at `Charts/basemap#REDLINE` over `RedlineOrigin.Stroke`, binding every deferred commit over captured view coordinates.
- An inverse projection's answer is admitted on its own DOMAIN, never on finiteness alone, and the two failure classes take opposite verdicts — a cylindrical axis WRAPS, because a pan past the seam still names the place it points at, while an axis the projection is asymptotic at REFUSES, because a sample from that band never had ground. Reading both as one rule loses either way: a shared refusal rejects the legitimate seam crossing, a shared wrap admits a coordinate no consumer accepts, and a bare finiteness gate admits both, because the arithmetic stays finite well past the domain. The wrap therefore rides the inverse row itself so every crossing carries it, and the domain gate rides the shape admission the durable payload passes; the recurring move checks `IsFinite` and stops, since that is the only thing the numbers themselves complain about. Instantiated at `Charts/basemap#REDLINE` over `MercatorFilter.Inverse`, binding every projection crossing whose result reaches durable truth.
- A durable REMOVAL proves its target existed, or it seals a delta empty on both halves — the ledger keeps that no-op, the evidence fan counts it as an applied edit, and its undo restores nothing, so a removal over an absent target is indistinguishable in every receipt from one that removed something. The proof is the prior payload the same pass already snapshots, so the gate costs one read and lands at the ONE site both the explicit removal verb and every routed erase cross; a first authoring reads the identical empty prior and admits, because the removal is what the emptiness contradicts rather than the emptiness itself. The recurring move gates the removal on identity alone, which is the half a caller can always supply. Instantiated at `Charts/basemap#REDLINE` over `RedlineSurface.Annotation`, binding every durable delete the corpus records.
- A vocabulary a boot-frozen registry reads declares on a NON-GENERIC owner — a key roster held on a generic projection type is unspellable from the table whose own law requires it, because the freeze runs before any receipt type exists and the table cannot name a type argument it never learns; the projection's instance column re-seeds from that same roster so the pane and the deck cannot drift, and the generic type keeps exactly the members that genuinely bind its parameter. The recurring move parks the constants on the projection because the pane is where they read as used; instantiated at `Editing/inspector#CONFLICT_RESOLUTION` `ConflictIntent` against `Shell/commands#INTENT_TABLE` `DeckRows.Conflict`, binding every roster a frozen registry reads.
- An angular extent derived from a CORNER SET is the minimal arc those bearings share, never their plain extremes — a cell straddling the seam has its lowest and highest bearing on opposite sides of it, so the extreme read answers the COMPLEMENT and one narrow cell claims the rest of the turn while every cell away from the seam renders correctly, which is what makes the defect read as a data problem. The arc is the turn less the WIDEST gap between consecutive sorted bearings, and that one fold answers three facts: no gap at all is a set standing on one bearing whose extent is genuinely zero, a remainder past a half turn is a set enclosing the pole where the angle is undefined and the extent is the whole turn, and everything else is the arc itself, crossing the seam by carrying an upper bound past a full turn for the periodic projection to absorb. Instantiated at `Charts/climate#CLIMATE_FEED` over `ClimateFeed.Arc`, binding every angular extent the corpus derives from located samples.

## [03]-[COLLAPSE]

- A shared mutable board state carries ONE delta-discriminated push and ONE projection per reader class, never a mutator per field — a per-field mutator set re-states the admission gate and the provenance stamp in every arm, so the fifth field is the fourth chance to forget one and a sixth brushed concern costs a method, an admission call, and a stamp. The delta arm owns its own state fold, so a column is written in exactly one place, and restore and reset are DELTAS rather than side doors that would bypass the gate the push holds. Projector sets travel as one lens VALUE per consumer rather than as optional delegate parameters threaded through each call, because optional projectors let one call brush on an axis the next call silently drops; the recurring move adds the convenience overload whose whole body is the projection plus one operator, which doubles the surface and lets a caller pick the arm that skipped the lens. Instantiated at `Charts/dashboards#DASHBOARD_TILES` over the cross-filter push, binding every multi-field shared state the corpus grows.
- An operator vocabulary that varies only in OPERAND COUNT is one row, not two — the predicate a cardinality-morphing sense evaluates folds `Exists` over the operand set and reads identically at one operand and at twenty, so the row stays fixed while its LABEL and its admitted count band morph; minting a plural row beside the singular one lets a picker that gained a second value keep the one-operand row and then admit a term no predicate matches, and the two bodies drift on the first repair to either. Instantiated at `Editing/livedata#FILTER_ALGEBRA` over `FilterOperator`, binding every vocabulary whose rows differ only in arity.
- Two ingresses differing only in where the GEOMETRY COMES FROM collapse into one commit verb over an origin union — identity admission, frame crossing, serialize, and recorder hop are identical past the projection, so a second verb beside the first is that whole durable leg copied and maintained apart, and the copy is exactly what lets one ingress gain an undo the other never gets. The origin arm owns its own projection and its own policy resolution, so a third ingress is one case rather than a fourth verb; the recurring move mints the second verb because the two gestures feel different to the operator, while everything durable about them is one body. Instantiated at `Charts/basemap#REDLINE` over `RedlineOrigin`, binding every multi-ingress commit the corpus grows.
- Filtering and highlighting are ONE channel at two intensities, never two — both answer "which rows does the viewer mean", one by removal and one by dimming, so they read one state and one lens and differ only in the projection a consumer subscribes. Split into two subjects they disagree the moment either is pushed while the other is live, and every new surface that must respond to both grows a second implementation; the source tile is excluded from its own FILTER, since a brush that emptied its own tile is a loop, and is NOT excluded from its own HIGHLIGHT, since the surface a pointer is over must stay lit. Instantiated at `Charts/dashboards#DASHBOARD_TILES` and published at `Charts/telemetry#METRIC_PANEL`, binding every linked-brushing surface.
- An absolute move over a stepwise algebra is N single steps under a posture row, and its receipt is TOTAL — a bespoke multi-step path re-derives the admission, the application, and the cursor advance the single step already owns, while a `Fin` receipt discards the prefix a halted walk already applied and leaves the surface addressing a state the document has left; the batching decision is a row carrying the fold it wraps the walk in, so a single step re-solves per step and a scrub re-solves once, and the halt rides the receipt as a VALUE so the resume half runs unconditionally. Instantiated at `Editing/history#REVERT_SCOPE` and `#EDIT_HISTORY`, binding every scrubbable surface over a stepwise owner.
- A gesture grammar every plane shares lives on the MODEL as rows carrying their own state custody, never as per-plane handling — a table, a tree, a canvas, a viewport, and a board each interpreting click, modifier-click, shift-click, and sweep is five chances to disagree about what shift means, and the disagreement is invisible until a user moves between two of them. The range ANCHOR is the custody that proves it: kept per plane it makes shift-click select a different span in a tree than in the table beside it, so it is a column on the model and the range row alone preserves it while every other row re-seats it. A plane contributes its own ORDER and its own hit set and nothing else; the recurring move adds the second plane's handler by copying the first's. Instantiated at `Editing/forms#SELECTION_MODEL`, binding every multi-plane interaction vocabulary the corpus grows.
- Two readings of one payload are two rows only where the FOLDS genuinely differ, and one row wherever they do not — the test is the body, never the diagram's name. A gantt and a timeline earn two rows because one draws rulers, baselines, and dependency elbows while the other merges equal consecutive states; a sunburst and a flame earn two because one scales a nesting into a turn and the other into a raster width. A psychrometric chart and an adaptive-comfort chart earn ONE, because they differ in frame skew, zone polygons, and curve set — all payload data — and in nothing a fold does, so a second row would be a second copy of one body maintained apart and drifting on the first repair. The recurring move mints a row per named diagram because the domain names them separately; instantiated at `Charts/climate#COMFORT_GRAMMAR` against `Charts/custom#PLAN_GRAMMAR`, binding every catalog whose rows share a payload.
- A presentation switch that a row could carry is a ROW, never a boolean — a `bool Labelled` beside a fixed formatter can say only whether text appears, so every caption a surface needs becomes a lambda each bind edge re-invents; the label SOURCE and the chrome that text takes ride one row family, and a new caption class is one row. Instantiated at `Charts/dashboards#CHART_GRAMMAR` over the data-label rows, binding every presentation axis that a vocabulary can carry.
- A retained framework instance a swap re-writes has ONE styling body serving both its mint and its swap, and the slot set is the vocabulary row's, never the caller's — a mint that constructs and a swap that re-writes are two rosters of one slot set, so the slot added at one site keeps the previous generation's value forever at the other and the defect surfaces only on the second variant flip, on the one path nothing renders twice. The row therefore carries the WHOLE style as its delegate column and clears every slot it does not set, because an effect held from a prior resolve decorates a row that never asked for one; a column the row could carry and a body the caller re-spells are the two deleted forms. Instantiated at `Charts/dashboards#CHART_PAINTS` over `ChromeInk`, binding every long-lived draw task, brush, or handle the token swap re-styles in place.
- A record whose columns no construction site can populate is a promise, not a shape — the factory is the whole reachable surface, so a column absent from it is dead however richly the card describes it, and a derivation member nothing calls is the tell. Every column therefore DERIVES at the one fold from the stream the owner already subscribes, so a headline, its delta, its trend samples, and its declared quantiles are one population read four ways rather than four subscriptions that agree only while nothing drops; a caption spellable from two columns already present is not a third column, it is a phrase at the render site that holds the locale. Instantiated at `Charts/dashboards#DASHBOARD_TILES` over `StatAnatomy`, binding every reading record a surface renders.
- A pair of entrypoints differing only in PAYLOAD CARRIER is one entrypoint at the widest shape its arms admit, because the language's own implicit widening binds every narrower caller with no adapter and no call-site edit — an array-shaped sibling forwarding into a memory-shaped body is a hop rather than a modality, and while it stands a streamed producer can reach the copying arm on the exact path the wider shape exists to spare. Arms whose seams are composition-bound delegate columns typed on the narrower carrier materialize INSIDE their own arm, so the copy lands on the paths that cannot avoid it rather than on all of them and the split is visible as one line per arm instead of one method per carrier; the recurring move adds the wider overload beside the original when the first large payload appears and leaves both standing, so half the corpus keeps paying a copy nobody removed. Instantiated at `Document/export#EXPORT_DESTINATIONS` over `ExportDelivery.Deliver`, binding every payload seam the corpus grows.

- A vocabulary the estate declares carries EVERY reading of itself as row columns, because a bare-constant roster read by N switches is that vocabulary enumerated N+1 times and only the declaration can break — each switch grows a default arm to stay total, and the default arms are what make a new member compile, scope, attribute, and decode while contributing nothing: an export capability named on a format's roster rendered no control and reported itself honoured, a coverage source projected zero candidates and read as a corpus that simply held none, and an insertion menu took its kind list from a caller so a new cell case was offered nowhere. The row therefore owns its field contribution beside its verdict, its projection beside its key, its locale label beside its wire token — and a member the row's own constructor demands is a member no growth can omit, which is the whole difference between a roster and a family. Where a column must name the row it fires for, the KEY arrives as an argument the row supplies from itself rather than a literal each body repeats, so a note and the row that produced it are one value. The recurring move mints the constants first because they are the cheap half and reaches for a switch at each reading; instantiated at `Document/export#EXPORT_FORM` over `ExportCapability`, `Document/search#SOURCE_COVERAGE` over `SearchSource`, and `Document/notebook#CELL_MODEL` over `CellKind`, binding every vocabulary the corpus reads more than once.

- A cold projection several surfaces read is SHARED once and replayed at one, never subscribed N times — a gauge, its threshold rule, and its staleness rule reading one declared stream through three cold subscriptions run the source three times and each hold a private materialization, so one declaration reads as three series and the divergence the shared declaration exists to foreclose arrives anyway. Replay depth is one because the readers attach across a composition pass rather than simultaneously, and a reader attaching second would otherwise hold nothing until the next emission. Instantiated at `Charts/telemetry#SLO_TILES` over `BurnFeed.Of`, binding every projection a surface and its rules both read.

- A driver that PACES a shared state holds none of it — a generator seeded with a value forks the state at subscription, so every verb, drag, and policy change afterwards lands on the owner's record while the stream advances a private lineage, and the one-state law reads satisfied because both halves are the same TYPE. The tell is a cadence the stream derives from its own copy: nothing the owner does can move it, so a speed set after subscription is pinned forever at the seed's. The driver therefore reads the live state per tick and answers the state one step on for the owner to seat, which keeps the owner a pure fold and leaves the driver with nothing to diverge; the read rides the generator's own state slot so every arm stays closure-free. The recurring move seeds because the generator's signature asks for a state and the owner has one to hand it. Instantiated at `Render/animation#SCRUB` over the kinematic playback driver, binding every paced stream over state another surface edits.

- A cell, column, or parameter whose PRESENT arm no member on the owning surface can produce is a declared capability the runtime never receives — an optional runtime value wired into a per-frame arrow with no mint never leaves its absent arm, so the levers it carries are enumerated, documented, and dead, and every structural check passes because absence is a legal state. The same defect one hop out is a member taking an argument the surface publishes no producer for: the chain reads composable and is not, and the missing producer is invisible precisely because the parameter looks like caller context. Every optional runtime slot therefore names its mint beside its readers, and a fact only one kernel computes is PUBLISHED where its readers sit rather than left in that kernel's local. The recurring move writes the consumer first because the consumer is where the design pressure was; instantiated at `Render/immersive#XR_INPUT_PASSTHROUGH` over the comfort cell's bind and the frame's predicted display time, binding every post-construction runtime slot the corpus declares.

## [04]-[STRUCTURE]

- A declaration a host package cannot fully RENDER states which arm draws what, and the surplus rides the surface the corpus already owns — the shipped legends build their own content from the chart's series and admit no caller-supplied entry set, so a swatch list and a two-label gradient are everything they can draw and a statistics column, a stepped band set, an ordinal dictionary, and every corner placement are undrawable through them. The arm verdict is therefore a VALUE a composition root dispatches on rather than a rule each mount site re-derives, and a richer declaration silently degrading to the poorer arm is what the verdict forecloses; consequences the host derives — orientation from placement, domain from the series' own measured bounds — are never restated as declaration columns, because a column the renderer overrides is a lie on two of five positions. Instantiated at `Charts/dashboards#LEGEND_ALGEBRA`, binding every declaration that outruns its host surface.
- A mark anchored to DATA rides a data-anchored owner or it drifts — a host's free-label visual positioned in pixels carries no axis index, so a caption bound to a datum is re-placed by hand on every pan, zoom, and re-range, and the plane that looks like the annotation surface is the one that cannot be it. Marks therefore project onto the owners that already carry coordinates and axis indices: a shaded span and an instant line are the settled band at three coordinate populations, and a labelled point is a one-datum series, so the annotation plane mints no geometry and inherits null-gap semantics, hit-testing, and axis binding for free. Each mark names the LAYER it annotates rather than restating an axis pair, so it moves when its series moves; instantiated at `Charts/dashboards#AXES_AND_MARKS`.
- An absent property REFUSES a similarity query; it never matches. A facet the seed does not carry has no value to compare, and the two available readings — wildcard or skip — both widen the result to the whole population the first time an element lacks a layer, a material, or a phase, which is a selection a user then acts on believing it narrow. The admitted form composes one signature per seed over the chosen facets and refuses per missing facet so a mixed pick reports every gap at once; the recurring move treats absence as a null that compares equal to other nulls, which quietly groups every unclassified element into one match set. Instantiated at `Editing/forms#SELECTION_MODEL`, binding every property-driven match the corpus grows.
- A bound and the value it checks stand in ONE canonical unit and elect their display unit at render — a check that converted at comparison time would compare through whichever unit the caller last held, and a row carrying a transcribed abbreviation prints the authoring machine's units to every viewer. A verdict therefore carries BOTH a margin in that canonical unit and a unitless ratio, because a scorecard ranking incommensurable metrics can only sort on the ratio and a designer can only act on the margin; the failing driver names the broken edge, and a passing row names none, since naming a driver on a passing check invents a cause. Instantiated at `Charts/dashboards#THRESHOLDS_AND_COMPLIANCE`, binding every dimensioned constraint the corpus evaluates.
- A hand-drawn plane admits a kind only where NO shipped series or chrome of the host package expresses it, and the test is semantics against chrome: what the package draws with its decoration removed stays the package's (a sparkline is an axis-less, legend-less, frame-less chart, so it renders through the host's own offscreen chart), and what the package's own surface structurally cannot draw comes to the plane (a legend carrying statistics columns, stepped bands, an ordinal dictionary, or a corner dock exceeds both shipped legends' fixed content). An angular reading whose renderer the package ships is the same test at its sharpest — hand-rolled per-axis trigonometry into an accumulated path re-implements a shipped polar series and loses its axes, hit testing, tooltip, and animation in one move — so the polar plane is the package's and the custom plane renders only its complement. Every refusal is STATED on the admitting catalog's own boundary, because an unstated absence is re-proposed every pass; instantiated at `Charts/custom#SKIA_KINDS` against `Charts/dashboards#SERIES_TABLE` and `#LEGEND_ALGEBRA`, binding every plane that draws beside a charting host.
- A record-once pack is once for EVERY consumer, so each derived product is sealed BY that pass and carried on the record rather than re-derived from the inputs — a raster replay, a proof twin, and a vector export off one layout fold cannot disagree, while a second fold produces a divergence indistinguishable from a rasterizer difference, which is the exact attribution a draw hash exists to prevent. Every retained half of the sealed record counts against the ONE retention ceiling, because a cache pins what the record holds and not only its native handle; instantiated at `Charts/custom#SKIA_KINDS` over the picture-and-vector seal, binding every recorded-then-replayed product.
- A per-element mark carries its own GEOMETRY row — paint style, the generated stroke step its width resolves from, dash intervals in width multiples, cap, and join — and the record bands on ink AND that row together, so one emission mixes a filled area with a stroked outline and an open contour renders at all; a paint pinned to fill draws a zero-area path as nothing, which is a silent empty layer rather than a fault. Dash intervals scale by the resolved width and are cited from the owning row by any surface that must match it, never transcribed; instantiated at `Charts/custom#SKIA_KINDS`, cited by the chart rail's ghost paint, binding every plane drawing mixed mark geometry.
- A label field on a dense plane is PLACED, never merely drawn — marks carry an anchor, a placement row, and the element's own significance as priority, and the fold admits each candidate box against the boxes already placed, so a field too dense to caption whole drops its least significant captions rather than whichever the emission order reached last. The box comes from the shaper, never from a character-count estimate, which mis-sizes every non-Latin script and every proportional face; the drops are counted onto the sealed record so an over-dense field reads as data rather than as a diagram that mysteriously lost its captions. Instantiated at `Charts/custom#SKIA_KINDS`, binding every plane emitting one caption per element.
- A tiled basemap's appearance is its SOURCE and never a post-effect — the raster draw path exposes no tint, blend, or colour-matrix hook, so a dark map is a second source row selected by the resolved variant and an inverted or hue-rotated basemap names a member the renderer never reads. Attribution is an ADMISSION rather than chrome taste: a tile row whose source declares no credit refuses at build, because rendering another party's imagery without its credit breaches the terms the map draws under, and the credit rides the source row rather than the caller so it cannot drift from what it credits. Fetch state — loading, ready, refused, offline — is read off the layer's own published signals and rendered as fact, since a blank tile grid leaves a viewer interpreting; instantiated at `Charts/basemap#MAP_SURFACE`, binding every third-party tile plane.
- Every entry on one surface answers the SAME rail — a sibling returning a bare effect where its neighbours return a typed result swallows out-of-range arguments into a silent no-op and forces every caller to discriminate on the shape of the rail before it can compose two calls; the argument admission belongs at that entry, per case, so a degenerate camera move refuses by name exactly as a degenerate layer set does; instantiated at `Charts/basemap#MAP_SURFACE` over the navigation verbs, binding every surface whose entries share a caller.
- A durable edit RECORDS on the same pass that commits it, and the recorded delta is the payload the target carried beside the payload it now carries — snapshotting after the seal captures the edit itself and yields a no-op whose undo reverses nothing, so the prior payload is read before the write and rides the surface beside its identity. The recorded op's apply fold is the SAME write the committed truth re-renders through, so revert and re-render cannot drift, and an edit that committed without recording is durable and unreachable by undo, which is the one shape a review surface must never have; instantiated at `Charts/basemap#REDLINE` against `Editing/history#EDIT_HISTORY`, binding every surface committing durable intent.
- Restored control state is written in the SHAPE the control's own gesture produces, and read back through the identity that gesture leaves behind — a restore that constructs a descriptor the gesture would never build appends a second entry for one column on the next click, and a capture that matches only on the descriptor's declared path silently drops every column ordered by a domain comparer, because a comparer-built descriptor carries no path at all. The comparer is therefore the identity the capture matches on, and declaring a comparer on the control AND a path-bearing descriptor for the same column is the shape that doubles the entry; instantiated at `Editing/tables#VIEW_STATE` over the grid's multi-sort gesture, binding every surface persisting state the user mutates through the control.
- A presentation state the control writes from its OWN gate is produced by satisfying that gate, never by a parallel write — the setter is internal precisely because the control owns when the state is true, so the reachable producer is the value the gate reads, written at the hook that fires before it. Where a case cannot reach the gate at all, the surface states that ceiling and routes that case to the next enclosing gate rather than shipping an unreachable claim; the recurring move declares the presentation state on the strength of the pseudo-class existing and never asks which writer sets it. Instantiated at `Editing/tables#GRID_COMMIT` over cell validity, binding every borrowed presentation state a control owns.
- An admitted cell value crosses ONE fold whatever gesture delivered it — a typed edit, a pasted block, and a filled-down span each hand the same text to the column's own admission, so a column that refuses a value refuses it identically at three seats and the refusal names the column rather than the gesture. A per-gesture admission is the shape where paste writes what the editor would have refused; instantiated at `Editing/tables#GRID_SUBSTRATE` over `TableCellEdit`, binding every surface accepting one value through more than one gesture.
- A re-entrancy latch lives on the object the RE-ENTRY is scoped to, which is whatever the bind mints one of — a latch held by the composition record while its bind mints one leg per surface makes two surfaces over one document cross-suppress, so one surface applying a remote diff silently drops the other's unrelated local commit and that edit is never sent at all: a loss the convergence protocol cannot detect, no receipt records, and no later merge repairs. The composition record therefore carries the adapters and the per-bind object carries the latch, the conflict cell, and the subscription lifetime; instantiated at `Editing/graph#COEDIT_BRIDGE`, binding every suppression latch beside a per-surface bind.
- A readout the hosting control already SHIPS stays the control's, and the chrome family that could mirror it does not — an in-control indicator reads the live transform inside the viewport with no subscription, no second formatter, and no placement tracking a surface it sits outside, where a mirrored chip needs all three and drifts by one frame the first time either side re-renders on its own schedule. The chrome family keeps what the control does not draw; instantiated at `Editing/graph#NAVIGATION_SURFACE` over the viewport zoom indicator against the chip vocabulary, binding every readout a host control already renders.
- FILTER state and VIEW state are two values on one binding, never one record — filtering is the high-frequency act and grouping, ordering, and visibility are the rare one, so a fused record makes every keystroke re-derive the column axis and makes "same view, different filter" unspellable; a saved view is the PAIRING, which is precisely why the live values stay apart. Where a control owns mechanism cells of its own — a display index, a resolved pixel width — those stay with the control and the domain axes stay shared, because a second copy of visibility beside the shared one disagrees the first time one surface hides a column the other still shows. Instantiated at `Editing/livedata#VIEW_STATE` against `Editing/tables#VIEW_STATE`, binding every surface persisting what a user calls "the view".
- One ENCODING serves a shareable link and a durable checkpoint — the query fragment a user sends a colleague and the string a session snapshot restores are the same bytes over the same codec, so a decode repair lands once and a store never holds a tree it would have to version. The codec carries no type tag because the consuming SCHEMA already declares each property's domain, so a value that does not parse under its declared kind refuses the whole link rather than decoding into a term nothing can evaluate; structural delimiters are safe only because every payload crosses an escape that provably escapes them, and DEPTH is admitted at the decode boundary so the recursive fold behind it walks a tree whose bound was already proved. Instantiated at `Editing/livedata#FILTER_ALGEBRA` over `FilterLink` against `Shell/screens#SCREEN_STATE`, binding every shareable-and-persistable state grammar.
- An optimistic mutation is a MERGE leg that outranks the authoritative one, never a write-back — the merge tracker republishes the best surviving value across every leg whenever one is removed, so acknowledgment and rollback are the same drop and no owner has to remember the value it overwrote; suppression of a late authoritative echo is then structural rather than a filter, because the pending row simply wins, and no transport ordering guarantee is required for the view to hold still. A REFUSAL keeps a rank above the settled truth so the rejected value stays visible under its refusal chrome before falling back, since a rollback that removed the row on arrival is indistinguishable from a network hiccup; the overlay's posture rides the merged element type, because the posture IS the presentation and erasing it back to the bare row deletes the only reason the overlay exists. Instantiated at `Editing/livedata#OVERLAY_SPINE`, binding every provisional-then-confirmed projection.
- BACKPRESSURE on a delta stream COALESCES and never drops — a rate limiter over a change-set discards whole deltas and leaves the bound collection describing a state no producer published, a corruption no later delta repairs, so the pacing operator buffers the window and flattens it into one change-set; the pacing is declared as a POLICY row on the source rather than applied per subscriber, so one high-rate feed is paced once for every downstream chain instead of once per consumer that remembered to ask, and a suspended surface holding its subscription is one more row on that policy rather than a second lifetime. Instantiated at `Editing/livedata#DATA_SOURCES` over `FeedPace`, binding every rate-bounded delta stream.
- A bounded numeric axis over generated candidates is the FILTER algebra's operators over a projected property, never a min/max vocabulary of its own — projecting each measured column as a numeric property makes at-least, at-most, and between the operators that already exist, makes ordering by that column the view axis that already exists, and makes the resulting narrowing shareable through the one codec; a bound record minted beside the algebra is a second grammar over the same question that no chip, no link, and no saved view can express. Instantiated at `Editing/livedata#OPTION_SETS` over the KPI schema, binding every candidate-narrowing surface.
- A vocabulary that OWNS a co-edit register carries BOTH legs of its correspondence — the write arm and the declared-inverse read projections — and the decode dispatch HOPS onto it rather than inlining either; a read projection seated away from the write arm that fed it is exactly how a column comes to exist at one end only, which no type checks and no merge repairs, because the register accepts the write and the rehydration silently answers a narrower row. The hop also keeps the decode dispatch one site while every column stays declared, written, and read where its vocabulary lives. Instantiated at `Collab/sync#DURABLE_INTENT` over `GraphRegister`, `MemberRegister`, and `IssueRegister`, binding every register a co-edited plane grows.
- A co-edited SEQUENCE is ordinal-keyed mergeable cells and a co-edited SET is a keyed mergeable map — never one replaced leaf, because a list or set written as a single value makes the later of two concurrent edits erase the earlier with no conflict the merge can surface, and the loss is invisible to every receipt; ordinal levels read back in ORDINAL order rather than the register's lexical key order, since a lexical walk seats slot 10 ahead of slot 2. A package enum crosses such a register as its ORDINAL under a declared-value guard, because a package enum carries no key member and a name round-trip degrades to a silently absent column. Instantiated at `Collab/sync#DURABLE_INTENT` over node pins, wire waypoints, and issue labels, binding every collection a co-edit register holds.
- Optimistic acknowledgment has ONE producer and it is the merge authority, which alone holds both evidence shapes — an `EventTriggerKind.Import` diff carries converged VALUES and settles the value arm, while the merge receipt carries the VERDICT and joins the outstanding ticket onto the acknowledged or refused arm; a `Local` diff is the session's own echo whose ticket the receipt settles and a `Checkout` diff is a historical read state that owes the live state nothing, so folding either would clear a pending row no peer can observe. A consumer settling on a timer or an assumed success is the deleted form, because it reports convergence the authority never reached. Instantiated at `Collab/sync#LIVE_WIRE` over `CollabEcho`, binding every optimistically-overlaid co-edited plane.
- A shared TRANSPORT vocabulary has one owner and every other plane CONSUMES it — a media clip and a 4D sequence differ in what a verb does, never in which verbs exist, so a second nine-row roster beside the settled one is the exact seam a paused clip under a playing timeline comes out of; the payload-bearing commands one plane alone can express live at that plane as a union whose FIRST arm carries the shared row verbatim, so the shared intent keys stay spelled at their owner and the local ones cannot collide with them. Which clock owns the playhead is then enforced at the RAISE rather than by convention: under a following posture the shared arms route to the owning clock and the position-writing arms refuse, because a transport that could start a clip independently of its owning sequence is the desynchronization the one-grammar law exists to foreclose. Instantiated at `Document/media#PLAYBACK_TRANSPORT` over `MediaCommand` against `Render/animation#TIMELINE_EDITOR` `TransportVerb`, binding every plane that plays.
- A property FEED subscribes the typed wrapper's own change event, never the raw client event beside a name string — the wrapper registers its observation under its own property name and format on first subscription and unregisters on last, so the feed leaks no registration, spells no property literal, keeps no request id, and delivers a genuinely optional payload; re-reading every tracked property synchronously on each raw event is a POLL wearing an event's name, and wrapping a nullable read in an absence type only restores the honesty the raw read had already thrown away. Instantiated at `Document/media#PLAYBACK_TRANSPORT` over `PlaybackTransport.Observe`, binding every native-client property projection.
- An intake that returns a CONTROL seals its readiness receipt on the RESOLVE, never on the assignment — a control whose source is a URL has attempted nothing at construction, so a receipt stamped there reports a decode that has not happened and a 404 seals as ready; the resolve runs through the shared loader so the control that follows hits the very cache entry the receipt proved. A lease whose release is a no-op under an entry promising release is the same lie one layer down: every arm drops what it actually holds, and an arm that holds nothing visual carries an OPTIONAL control rather than a zero-area stand-in, because a caller reading a non-null control would be wrong exactly on the row that has none. Instantiated at `Document/media#MEDIA_SURFACE` over `MediaSurfaces.Materialize`, binding every asynchronous mount.
- A CAPTURE-side concern and a POLICY-side concern split at the language election: what a caption says it is transcribed INTO belongs to the locale owner, while the tap, the segmentation, the engine, and the timed band belong to the media owner — and where the decoding player exposes no sample tap, the route between them is a SIDECAR the player itself then times, never a page-side cue clock beside the decoder that already parsed the file. A live microphone caption is a different capture owner entirely and never enters the media plane. Instantiated at `Document/media#CAPTION_TRACK` against `Theme/locale#SPEECH_POLICY` `CaptionPolicy`, binding every transcription surface.
- A composed surface REFERENCES its sources by key and resolves them live, never by capturing their values — a deliverable that embedded a resolved view, series, or sheet is a snapshot wearing the composition's name and goes stale on the first source edit with nothing to signal it; a reference that no longer resolves renders its own absence in place rather than vanishing, because a panel that disappeared reads as a complete deliverable that was always that size. Presentation state a composition owns — a crop, a scale, a placement — stays the composition's and never writes back into the source, so two compositions can frame one source differently and neither disturbs the other. Instantiated at `Document/board#BOARD_MODEL` over `BoardItem`, binding every composed deliverable.
- A DERIVED count and a REALIZED count are two facts and a badge reads the derived one — a count taken off the virtualized window shrinks as the user scrolls, which reads as results disappearing, so the answer's size comes from the cache while the list renders the viewport; the same split governs collapse, where the hidden EXTENT is measured and shown rather than reduced to a boolean, because a reader deciding whether to expand needs to know how much is there. Instantiated at `Document/search#RESULTS_PANEL` over `SourceGroup` and at `Document/notebook#CELL_CHROME` over `OutputCeiling`, binding every surface presenting a bounded slice of an unbounded answer.
- Per-format CONFIGURATION is a schema row on the format's own capability roster, never a per-format dialog — the roster that builds the options is the roster the preflight reads, so a surface cannot offer an option its arm never consults nor promise a capability the row does not claim; the readout then NAMES capability in three readings, because "honoured", "degraded and here is why", and "this target cannot" are three things a user acts on differently and a boolean collapses the middle one into whichever pole it resembles. Instantiated at `Document/export#EXPORT_FORM` over `ExportTarget`, binding every configured act.

- A reshape declared at the ELEMENT grain evaluates at that grain — a container that opens one pipeline and runs its own rows leaves every element's declared chain unevaluated, and the failure is silent in exactly the way that hurts most: the element's encoding reads magnitudes the unreshaped feed never produced, renders the host's own empty coordinate at every point, and reports success on every delivery. One stream per element combined into one frame is the admitted form, because the elements are read together and a merged partial frame swaps one element's collection against another's previous revision. A container-level source beside element-level ones is the same defect wearing a redundancy: two declarations of one fact that no gate compares. Instantiated at `Charts/dashboards#DASHBOARD_TILES` over the per-layer chart bind, binding every container whose parts declare their own pipelines.
- A budget DERIVED from a cadence is spelled as that derivation, never transcribed as the duration it currently equals — the transcription reads correct at authoring and drifts on the first retune, and both directions of the drift are silent: a budget too tight alerts on ordinary jitter, one too loose lets a dead lane read as live. Instantiated at `Charts/telemetry#BOARD_ROWS` over the staleness budget against the board refresh, binding every policy value another policy value determines.

## [05]-[PROCESS]

- (none)
