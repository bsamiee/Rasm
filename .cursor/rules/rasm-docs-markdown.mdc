---
description: Rasm Markdown and Cursor rules: style hygiene, rule pack scope, and private-data boundaries
globs: ".cursor/**/*.md,.cursor/rules/*.mdc,AGENTS.md,CLAUDE.md,**/AGENTS.md"
alwaysApply: false
---

# Rasm Documentation

Read `docgen` and `style-standards` before editing Markdown or `.mdc` files.

## Routing

| Need | Source |
| --- | --- |
| README, ADR, changelog, architecture | `rasm-docs-architecture.mdc` + `docgen` |
| Headers, lists, voice, separators | `style-standards` |
| BCL, product libraries, testing APIs, usage ladder | `rasm-docs-reference.mdc` |
| Cursor project rules | `.cursor/rules/README.md` + current Cursor Rules docs |

## Rules

- Remove stale or invented commands, paths, APIs, platforms, and package claims.
- Keep Cursor rules concise: one concern per `.mdc`, documented activation frontmatter, and no full skill duplication.
- Do not duplicate full `CLAUDE.md`, `AGENTS.md`, skills, or docs. Link and distill.
- Do not expose private transcript paths or secrets in repo docs.
- Use ASCII unless existing file context justifies otherwise.

## Cursor Artifacts

Project rules belong in `.cursor/rules/*.mdc`. User rules are plain text in Cursor Settings. Rasm Cursor skills are intentionally absent because `.claude/skills/` owns procedures.
