# [RASM_BIM_IDEAS]

The forward pool of higher-order concepts for the host-neutral BIM-and-exchange engine. `[1]-[OPEN]` holds active ideas as cards; `[2]-[CLOSED]` records a finished or dropped idea with a one-line disposition.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[CONNECTION]-[QUEUED]: Introduce a host-neutral structural-connection sub-domain owning the physical joint detail the analysis MemberConnection edge names only abstractly: bolt patterns, weld schedules, bearing surfaces, and clearance envelopes folded from IfcRelConnectsWithRealizingElements and the realizing IfcMechanicalFastener/IfcFastener/IfcReinforcingElement family into one ConnectionDetail record the fabrication and structural consumers both read.
- Capability: A Semantics/connection sub-domain: one ConnectionDetail record carrying a closed ConnectionRealization [Union] (Bolted/Welded/Bearing/Cast) over the IfcRelConnectsWithRealizingElements realizing-element set, a BoltPattern [ValueObject] (grade, diameter, gauge grid, edge distance), a WeldSchedule [ValueObject] (process, throat, leg, intermittent pitch), a Clearance envelope, and the ConnectionProjection fold from the GeometryGym realizing-element surface, joined to the structural graph by the MemberConnection GlobalId pair and to the physical BimElement by GlobalId, with a (GeometryKey, DetailKey) content-key identity the fabrication consumer reads.
- Shape: A new Semantics/connection.md design page (one semantic word file Connection.cs) authoring ConnectionDetail, ConnectionRealization [Union], BoltPattern/WeldSchedule/BearingSurface [ValueObject] rows, ConnectionKind [SmartEnum<string>], and ConnectionProjection.Project/ProjectAll folding IfcRelConnectsWithRealizingElements.RealizingElements onto the typed detail; the realizing-element geometry binds the kernel Rasm geometry by reference like every other Bim owner, never re-tessellated.
- Unlocks: Steel/precast/timber connection detailing first-class, the Rasm.Materials/Connection ConnectionItem IFC wire seam owner on the Bim side, connection-capacity binding to the analytical member.
- Anchors: Model/structural#ANALYSIS_MODEL MemberConnection (MemberGlobalId, ConnectionGlobalId) edge; ARCHITECTURE seam Model <- csharp:Rasm.Materials/Connection ConnectionItem IFC wire IfcReinforcingBar/IfcMechanicalFastener; GeometryGymIFC_Core IfcRelConnectsWithRealizingElements/IfcMechanicalFastener/IfcFastener/IfcReinforcingElement; .api/api-geometrygym-ifc has IfcRelConnectsElements row 11 + IfcRelConnectsPorts RealizingElement row 10 but NO IfcRelConnectsWithRealizingElements / fastener / reinforcing rows.
- Tension: The connection detail stays host-neutral scalar/handle data and never re-tessellates the fastener; the api-geometrygym-ifc catalogue carries NO IfcRelConnectsWithRealizingElements/IfcMechanicalFastener/IfcFastener/IfcReinforcingElement rows today, so the projection fold is candidateVerify: the member spellings ground against a fresh GeometryGym decompile and the .api catalogue gains the realizing-element + fastener + reinforcing rows before the page authors the fold.

[COORDINATION]-[QUEUED]: Introduce a Review/coordination automation sub-domain owning an if-X-then-Y rule engine over the ElementPredicate algebra, a clash-detection-and-resolution-proposal fold over the systems and structural geometry, an A/B model-impact report over two ModelDiff snapshots, and a BCF sign-off state machine: the model-checking automation layer the IDS/BCF/Diff owners give the vocabulary for but never compose into a workflow.
- Capability: A Review/coordination sub-domain: a CoordinationRule [Union] of applicability-predicate then-requirement-predicate rows reusing the Model/query#ELEMENT_SET ElementPredicate algebra verbatim (the All/Any composite arms confirmed present), a ClashProposal fold detecting hard/clearance/soft clashes and proposing a ranked Resolution, an ImpactReport fold over two Review/diff change-sets into the downstream-affected element/schedule/cost set, and a SignOff [SmartEnum] state machine over the BcfStatus lifecycle, all lowering onto the one BimFault band.
- Shape: A new Review/coordination.md page (one semantic word file Coordination.cs) authoring CoordinationRule [Union] (Require/Prohibit/Recommend each carrying applicability and requirement ElementPredicate), ClashProposal (clash pair, ClashKind, ranked Resolution), an ImpactReport fold over Review/diff#MODEL_DIFF, and a SignOff [SmartEnum] over the Review/issues#BCF_ARCHIVE BcfStatus transitions; the clash proximity test routes to the kernel geometry owner, never re-tessellating here.
- Unlocks: Parameterized rule-checking library, clash report with proposed fixes anchored to BCF, change-impact analysis, governed sign-off workflow; MEP_INTERFERENCE_CHECK feeds ClashProposal.
- Anchors: Model/query#ELEMENT_SET ElementPredicate (All/Any composite arms present, L28-29); Review/validation#IDS_FACETS predicate fold; Review/issues#BCF_ARCHIVE BcfStatus; Review/diff#MODEL_DIFF ElementChange; Model/systems#SYSTEM_TRACE; Model/structural#ANALYSIS_MODEL.
- Tension: A coordination rule carries a then-action proposal richer than an IDS pass/fail facet yet reuses the ElementPredicate algebra (never a second selection surface); the clash fold's geometric proximity binds the kernel Rasm geometry by reference and the intersection test routes to the geometry owner; the MEP_INTERFERENCE_CHECK Interference row is the ClashProposal substrate, so coordination consumes the systems-page clash evidence rather than re-deriving proximity. The Review/issues#BCF_ARCHIVE BcfTopic/BcfComment/BcfViewpoint family and the .bcfzip codec are the confirmed Bim-owned BCF surface the SignOff state machine reads — Rasm.AppUi/Editing/issues consumes the Bim BcfTopic contract at the package edge and never re-mints a BCF schema, so the relocation of issue-tracking toward Bim leaves SignOff on a settled owner and ClashProposal anchors its proposed fix to that BcfTopic. DOUBLE-OWNERSHIP TO RESOLVE (not resolvable from Bim-local docs): Rasm.Persistence/Sync/annotation also declares it owns a BcfTopic/BcfComment/BcfViewpoint backbone reading and writing BCF 2.1/3.0 archives plus the BCF-API REST surface — two packages claim the BCF codec/REST owner; the Bim-side resolution is that Bim owns the BCF record family and .bcfzip/BcfApi codec while Persistence rides the durable op-log/CDE-sync ripple by the GlobalId content-key, but the conflict is recorded for cross-package reconciliation rather than absorbed here.

[VERSIONED]-[QUEUED]: Introduce a Versioned sub-domain folding the content-keyed ElementFingerprint and the ModelDiff change-set into a content-addressed commit-DAG: a BimCommit referencing parent commit(s) by content-key, a BimBranch ref, and a three-way Merge fold reconciling two divergent revisions through the existing ElementChange union against a common ancestor, so model history is a first-class Bim concept meeting the Persistence Version owner at the content-key wire rather than living only as a pairwise Diff.
- Capability: A higher-order versioning algebra over the settled diff: the per-element content-key fingerprint is already the commit-object identity, so a commit is the fingerprint set, a diff is two commits, and a merge folds two ElementChange streams against a common ancestor producing a merged graph plus a MergeConflict [Union] (both-modified / modified-and-removed / added-twice-divergent) the coordination sign-off resolves.
- Shape: A new Versioned sub-domain (one node: Version.cs) carrying BimCommit (UInt128 CommitKey, Seq<UInt128> ParentKeys, Map<string,ElementFingerprint> Fingerprints, Instant At), BimBranch ref, and Version.Merge folding two revisions through ElementChange against a common ancestor; the commit-DAG meets the Persistence Version owner at the content-key wire and reuses Diff/Between verbatim.
- Unlocks: Federation version control (branch/merge/resolve), model history lineage, three-way merge governed by coordination sign-off, the Persistence Version owner storing the DAG by the same content-key the wire and diff carry, and the OpLog wire face the Persistence CRDT sync converges over.
- Anchors: Review/diff#MODEL_DIFF ModelDiff/ElementFingerprint/ElementChange; Exchange/wire#WIRE_PROJECTION OpLog face (ONE_MODEL_THREE_FACES Snapshot/OpLog/Grpc confirmed); Model/elements#ELEMENT_MODEL BimModel; csharp:Rasm.Persistence Version commit-DAG; csharp:Rasm.Compute CONTENT_ADDRESSING.
- Tension: The commit-DAG storage is the Persistence Version owner's concern and Bim depends strictly upward: the Bim Versioned owner produces the content-addressed commit objects and the merge algebra, the durable DAG riding the Persistence ripple; the version lineage reuses the diff content-key, never a second identity scheme; conflict resolution is the coordination sign-off's concern, not an auto-resolve.

[PROPERTY_TEMPLATE_RESOLUTION]-[QUEUED]: Make Semantics/properties a true bSDD-resolved template owner: the PropertyKey vocabulary resolves its standard-Pset and material-mechanical-grade rows (concrete strength class, steel yield grade, rebar diameter, masonry bond) from the live BsddClass ClassProperty set rather than the hardcoded Pset_* table the page forbids, and QuantitySet.Derive derives real base quantities from the bound kernel geometry, so the property layer stops being a flat passthrough and becomes the dictionary-driven takeoff-and-spec substrate cost/IDS/structural consumers join.
- Capability: One dictionary-driven property/quantity owner: PropertyKey template rows (standard Pset plus Pset_ConcreteElementGeneral/Pset_MaterialSteel/Pset_ReinforcingBarBendingsCommon/Pset_MaterialMasonry) resolve their characteristic-strength/yield/modulus from the BsddResolution ClassProperty mapping, and QuantitySet.Derive computes NetVolume/GrossArea/Length per IfcClass from the GeometryHandle the element binds by reference, with unit coercion riding the UnitsNet SI-base resolver, never a frozen grade table or string-match unit switch.
- Shape: An expansion of properties#PROPERTY_SETS adding the material-mechanical PropertyKey rows bound to the Semantics/classification#BSDD_RESOLUTION class-to-property mapping, replacing the QuantitySet.Derive no-op body with a per-IfcClass base-quantity fold reading GeometryHandle, and collapsing the KindOf string-switch into a UnitsNet quantity/unit coercion; PropertySet/QuantitySet records and the QTO_TYPEDRIVENOVERRIDE inheritance stay settled.
- Unlocks: A 5D Cost rate joins a real derived QuantitySet, the IDS Property facet matches a typed SI-unit measure, bSDD-resolved material grades the structural/cost consumers join and IDS validates, and a new standard Pset or grade becomes one dictionary-URI row not a hardcoded template.
- Anchors: Semantics/properties#PROPERTY_SETS QuantitySet.Derive/PropertyKey/KindOf (Derive confirmed no-op L73-78); Semantics/classification#BSDD_RESOLUTION BsddClass.Properties; Model/elements#GEOMETRY_HANDLE GeometryHandle; Planning/cost#ESTIMATE CostItem.ValueOf->QuantityOf QuantitySet join (L138-152); Review/validation#IDS_FACETS Property facet; UnitsNet 5.75.0 already pinned (Directory.Packages.props L158).
- Tension: Base-quantity derivation reads the kernel Rasm geometry by reference and a re-tessellation here is the named seam violation; UnitsNet is ALREADY pinned and consumed by AppUi/Compute/Materials with .api-unitsnet.md catalogues in three folders — the Bim admission is csproj ref + a Bim-folder .api/api-unitsnet.md + README registry, NOT a new Directory.Packages.props pin; material grades resolve through BsddResolution, never a parallel concrete/steel grade lookup.

[SCHEDULE_NETWORK_DEPTH]-[QUEUED]: Deepen Planning/schedule from a static activity-window store into a true CPM schedulable network: a forward/backward-pass CriticalPath fold over the SequenceRel edges, a WBS rollup over the IfcTask Nests tree, an IfcWorkCalendar working-time fold, and a glTF construction-sequence animation track over the dormant SharpGLTF.Runtime keyframe surface, so durations resolve against a real construction calendar and the 4D schedule emits a self-contained animated artifact rather than a per-instant re-render.
- Capability: Full critical-path-method depth plus 4D emit in one fold family: forward pass deriving early-start/early-finish from predecessor SequenceRel lags, backward pass deriving late-start/late-finish, total-float and free-float, the zero-float critical-path set, a WbsNode hierarchy folding the IfcTask Nests summary-task tree, a WorkCalendar mapping elapsed Interval onto working hours over IfcWorkCalendar WorkingTimes/ExceptionTimes, and a ConstructionAnimation baking ConstructionState.At snapshots into per-element glTF visibility/transform keyframe tracks through the admitted-but-unexercised SharpGLTF.Runtime CreateAnimation surface, all immutable folds over the SequenceRel edge set the schedule already owns.
- Shape: An expansion of schedule#SCHEDULE adding a CriticalPath value record per task (EarlyStart/EarlyFinish/LateStart/LateFinish as Instant, TotalFloat/FreeFloat as Duration, bool IsCritical), a WbsNode record folded from IfcTask.Nests, a WorkCalendar record folded from IfcWorkCalendar.ExceptionTimes IfcRecurrencePattern, and an export#EXPORT_RAIL AnimateSchedule arm over SharpGLTF ModelRoot.CreateAnimation keyframes keyed by Interval-to-seconds via NodaTime Duration; the SequenceRel union and ConstructionTask record stay settled.
- Unlocks: Critical-chain-aware schedule report and 4D playback, WBS summary-task Gantt rollup, working-time-accurate finishes with holiday/weather exception extension, the float window resource leveling needs, and a Cesium/three.js timeline scrub off one animated GLB.
- Anchors: Planning/schedule#SCHEDULE ScheduleNetwork/ConstructionTask/SequenceRel; schedule.md Growth note names IfcWorkCalendar as the intended growth path (L17) and the NodaTime under-utilization note (L18,L248); GeometryGym IfcWorkCalendar/IfcWorkTime/IfcRecurrencePattern/IfcTask.Nests; NodaTime Duration/Period/ZonedDateTime/LocalTime; SharpGLTF.Runtime Animation rows 14/15, ModelRoot.CreateAnimation entrypoint 07 (admitted, unexercised).
- Tension: The forward/backward pass needs a topological order over the SequenceRel DAG; a cyclic edge lowers BimFault.ModelRejected rather than looping, and the StartToStart/FinishToFinish arms shift the pass differently than FinishToStart so the float algebra discriminates the union arm; the IfcRecurrencePattern grammar narrows to the construction work-week without minting a parallel recurrence engine and composes NodaTime date arithmetic; the WBS rollup shares the IfcTask Nests traversal the cost CostItem.ParentGlobalId reads; the 4D animation exercises the existing SharpGLTF.Runtime pin (no new package), the keyframe author and ConstructionState.At sharing one time axis. PARALLEL-CPM BOUNDARY (surfaced by the Sync/schedule relocation): Rasm.Persistence/Sync/schedule owns its own CpmPass forward/backward float pass over a TaskRelation DAG sourced from Primavera P6 XER and MS-Project XML imports, distinct from this idea's CriticalPath fold over the IFC-sourced SequenceRel edges of the host-neutral ScheduleNetwork — the two CPM passes are not a duplication because they read disjoint source lanes (IFC IfcWorkPlan vs external scheduling-tool files), but the Bim CriticalPath fold scopes strictly to the IFC-projected network and the float algebra is the host-neutral domain fold while Persistence owns the P6/MSProject store and its durable residence; a Bim CriticalPath reading a P6/MSProject TaskRelation, or a Persistence CpmPass re-deriving the IFC SequenceRel network, is the cross-package drift defect the relocation makes explicit.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
