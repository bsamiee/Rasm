# [COMPUTE_TASKLOG]

The open and closed work for measured execution, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup and the residual live-host probes whose owner shape is complete.

## [1]-[OPEN]

[T-SYMBOLIC-OWNER] [QUEUED]: Author the `symbolic/expression.md` design page — the closed `SymbolicExpr` CAS owner that internalizes the admitted `[SYMBOLIC]` package row.
- Capability: a new `.planning/symbolic/expression.md` page with the `SymbolicExpr` value wrapping the F# `Expression`, one polymorphic `Build` entry discriminating an infix-string parse from a structured construction, the `Differentiate`/`Simplify`/`Compile`/`Substitute`/`Project` operation family folding `MathNet.Symbolics` `Calculus`/`Rational`/`Algebraic`/`Trigonometric`/`Polynomial`/`Structure`/`Compile`/`Infix`/`LaTeX`, a `SymbolicFault` rail (parse/undefined/non-differentiable) on the `ComputeFault` 2200 band, and the canonical-normal-form `XxHash128` cache key that reuses a compiled delegate.
- Packages: MathNet.Symbolics, MathNet.Numerics.FSharp, FParsec (all admitted in the central manifest under `[SYMBOLIC]`, catalogued at `.api/api-mathnet-symbolics.md`).
- Integration: register the `symbolic/` charter on `ARCHITECTURE.md` and the router on `README.md`; the compiled delegate keys through `interchange/codecs#CONTENT_ADDRESSING` `XxHash128` and warms from the model-lane cache the surrogate uses; the analytic Jacobian seam feeds `solver/optimizer#OPTIMIZER_LANE` and the `numeric/algebra#OWNED_BUILDS` integrator, the dimensional-consistency seam feeds `units/quantities`; the branch wire to the Persistence cost-catalog/QTO consumers and the cross-libs `symbolic` graduation axis is `csharp:IDEAS#SYMBOLIC_PARAMETRIC_ALGEBRA`, aligned at the `SymbolicExpr` content key, never coupled into a sibling interior.
- Considerations: a compile fence transcribes only after the `.api/api-mathnet-symbolics.md` `[4]-[RESEARCH]` leaf closes the precise `Compile.compileExpression{,1,2,3}`/`compileComplexExpression` arity and `Func<>` return shape and the `SymbolicExpression` operator/conversion surface; the F# `Expression` DU is netstandard2.0 ALC-safe; `FParsec` is consumed transitively through `Infix.parse`, never called directly; an in-proc symbolic-regression fit is the rejected form (offline-science companion owns formula discovery).

[T-SYMBOLIC-DIMENSIONAL] [QUEUED]: Author the `symbolic/dimensional.md` design page — the symbolic unit-dimension proof feeding the units boundary, spawned by `SYMBOLIC_CAS_SUBDOMAIN`.
- Capability: a new `.planning/symbolic/dimensional.md` page lowering a parsed `SymbolicExpr` onto a `DimensionMonomial` (the seven SI base-dimension exponent vector) by folding `MathNet.Symbolics` `Structure.map`/`Polynomial.coefficients` over the expression tree, a `DimensionProof` entry returning `Validation<SymbolicFault,DimensionMonomial>` accumulating every dimension mismatch in a compound expression, and the projection that hands a proven monomial to the `units/quantities` compound-consistency boundary as the pre-numeric admission gate.
- Packages: MathNet.Symbolics, UnitsNet (the `units/quantities` `QuantityFamily` base-dimension rows the monomial maps onto), Thinktecture.Runtime.Extensions, LanguageExt.Core.
- Integration: composes `symbolic/expression#SYMBOLIC_EXPR` as the input value and the `units/quantities` SI base-dimension vocabulary as the target; a dimension mismatch is one `SymbolicFault` arm on the existing `ComputeFault` 2200 band, never a new fault family; the proof runs before numeric admission so a dimension-inconsistent formula never reaches the optimizer or the cost catalog.
- Considerations: the dimension fold is over the F# `Expression` DU case payloads (`Sum`/`Product`/`Power`), so it grounds on the same DU-payload confirmation `T-SYMBOLIC-OWNER` closes; an `Identifier` carries its declared dimension from the parse context, never an inferred default; this page adds capability the units boundary lacks (symbolic dimension over a frozen `QuantityFamily` row pair) and must not re-mint the UnitsNet quantity vocabulary.

[T-SYMBOLIC-LOWERING] [QUEUED]: Author the `symbolic/lowering.md` design page — the content-keyed compiled-delegate cache and the differentiable-program bridge to the optimizer adjoint, spawned by `SYMBOLIC_CAS_SUBDOMAIN`.
- Capability: a new `.planning/symbolic/lowering.md` page owning the `CompiledExpr` carrier keyed by the canonical-normal-form `XxHash128` over the simplified `SymbolicExpr`, a `LoweringCache` read-through over the `Microsoft.Extensions.Caching.Hybrid` model-lane cache the surrogate uses, and the `GradientSource.Symbolic` row that emits a `Compile.compileExpression`-lowered analytic Jacobian delegate the `solver/optimizer#OPTIMIZER_LANE` `Adjoint` fold consumes beside the existing `GeometryTape` DDG tape and the finite-difference fallback.
- Packages: MathNet.Symbolics, System.IO.Hashing, Microsoft.Extensions.Caching.Hybrid, LanguageExt.Core.
- Integration: extends the `optimizer#OPTIMIZER_LANE` gradient surface with a third `GradientSource` arm (the symbolic Jacobian) under the same `(Seq<double>, double)` evaluate contract, never a parallel adjoint path; the cache key composes `interchange/codecs#CONTENT_ADDRESSING` so an identical formula reuses its delegate; the symbolic Jacobian feeds the `numeric/algebra#OWNED_BUILDS` integrator seed.
- Considerations: the optimizer `DescendAdjoint`/`Adjoint` fold must admit the symbolic gradient source without breaking the empty-tape degenerate-descent invariant — a formula with no free design symbol lowers an empty Jacobian and falls to finite-difference, mirroring the absent-mesh case; the compiled delegate is netstandard2.0 ALC-safe; the offline symbolic-regression fit is rejected (Python owns formula discovery), Compute owns only the compile-and-cache.

[T-UNCERTAINTY-LANE] [QUEUED]: Author the `solver/uncertainty.md` design page — the forward-UQ/reliability owner and the C# producer of the `uncertainty-law` graduation axis, spawned by `UNCERTAINTY_PROPAGATION_LANE`.
- Capability: a new `.planning/solver/uncertainty.md` page with one `UncertaintyMethod` `[SmartEnum<string>]` axis (monte-carlo · latin-hypercube-mc · polynomial-chaos · first-order-reliability · subset-simulation), one `RandomVariable` `[Union]` distribution case set (normal · lognormal · uniform · weibull · beta · empirical) over `MathNet.Numerics.Distributions`, one `UncertaintyResult` carrier (mean/variance/quantiles, Sobol total-effect decomposition, failure-probability, reliability index β), and one `Propagate` fold driving the `optimizer#OPTIMIZER_LANE` `evaluate` oracle over the sampled inputs.
- Packages: MathNet.Numerics (Distributions + Statistics, admitted under `[TENSORS_NUMERIC]`, catalogued at `.api/api-mathnet-providers.md`), System.Numerics.Tensors, Thinktecture.Runtime.Extensions, LanguageExt.Core, NodaTime, Rasm.Persistence (project).
- Integration: register the `solver/uncertainty` page on the `solver/index.md` page index and the `ARCHITECTURE.md` solver charter; the PCE fit rides `numeric#DENSE_ALGEBRA` least-squares/QR, the MC sampling reuses the `numeric#OWNED_BUILDS` `LowDiscrepancy` sampler the sweep `Sobol` axis owns, the `evaluate` oracle is the same `solve-contract#SOLVE_CONTRACT` `Solve`-or-`Surrogate.Predict` contract the sweep and optimizer share; the `UncertaintyResult` crosses to Persistence content-keyed and the offline distribution/PCE fit crosses as the cross-libs `ONE_GRADUATION_EVIDENCE` `uncertainty-law` axis; realizes `csharp:IDEAS#AEC_SIMULATION_BRIDGE`'s distribution-valued envelope.
- Considerations: a new `Uncertainty`/`Reliability` `ComputeReceipt` case carries the method key, sample count, response moments, the Sobol indices, and the failure probability + β — one case row plus one `[JsonDerivedType]` row on `receipts/union.md`, never a standalone receipt type; the in-process distribution-learning loop is rejected (the learned input distribution is the offline-science companion's); the `SensitivityTornado` Sobol fold the sweep owns is composed, never re-minted.

[T-STREAMING-RESIDENCY] [QUEUED]: Author the `interchange/residency.md` design page — the content-keyed residency-PAYLOAD codec the AppUi `ResidencyManifest` references, spawned by `STREAMING_RESIDENCY_PAYLOAD`.
- Capability: a new `.planning/interchange/residency.md` page with one `ResidencyKind` `[SmartEnum<string>]` payload axis (meshlet-cluster · quantized-vertex · point-splat · gaussian-splat), one `ResidencyPayload` content-keyed buffer carrier (the encoded bytes plus the cluster-cone bounds, NOT a manifest), and one `Encode` fold lowering an `ImportedGeometry` octree leaf onto `Meshopt.BuildMeshlets`/`ComputeMeshletBounds` cluster-cone bounds, `Meshopt.EncodeVertexBuffer`/`EncodeIndexBuffer` + `QuantizeHalf`/`QuantizeFloat` quantized buffers, and `Meshopt.SpatialSortRemap` locality ordering, plus a `point-splat` arm decoding the companion SPZ/SOG splat bytes that supersedes the `codecs#FIELD_RESULT_CODEC` `point-catalogue-pending` fault.
- Packages: Alimer.Bindings.MeshOptimizer (`BuildMeshlets`/`ComputeMeshletBounds`/`ComputeClusterBounds`/`SpatialSortRemap`/`EncodeVertexBuffer`/`EncodeIndexBuffer`/`QuantizeHalf`/`QuantizeFloat`, all catalogued at `.api/api-alimer-meshoptimizer.md`), System.IO.Hashing, CommunityToolkit.HighPerformance, System.Numerics.Tensors, LanguageExt.Core, Rasm.Persistence (project).
- Integration: register the `interchange/residency` page on the `interchange/codecs` sub-domain and the `ARCHITECTURE.md` interchange charter; the payload content-key composes `interchange/codecs#CONTENT_ADDRESSING` so the bytes byte-match across the wire; the partition reads the `TILE_PARTITION` `ImportedGeometry` octree leaf the tile owner already builds, never a second partition; this page owns ONLY the content-keyed payload bytes — the `WEB_GEOMETRY_RESIDENCY_WIRE` manifest mint+decode leg is `[COMPLETE]` and `csharp:Rasm.AppUi/viewport/viewport-pipeline#TS_PROJECTION` `ResidencyManifest.Mint` is the SINGLE manifest owner that references these payload bytes by content key, so a Compute-side `ResidencyManifest` is the named second-mint drift defect; the splat decode is the exact residual the AppUi `ResidencyManifest` boundary names `[UPSTREAM-BLOCKED]`, realizing the `csharp:IDEAS#REALITY_CAPTURE_TO_BIM` splat seam at the payload boundary.
- Considerations: the meshlet build, cluster bounds, and encode members are confirmed (catalogued), so the encode fence transcribes directly; the gaussian/point-splat payload rows ground on the Python `open3d`/splat companion's emitted SPZ/SOG shape at the wire and stay payload-format rows, never an in-proc splat fit; the splat decode bytes feed the AppUi `SplatSource` residency keying by content key; a new `StreamSegment` receipt slot carries the meshlet/cluster/splat count, never a new receipt case; the manifest assembly, the `ResidencyKind`/`ResidencyManifest` wire shape, and the TS projection stay AppUi-owned and are NOT re-minted here.

[T-EMBEDDING-LANE] [QUEUED]: Author the `models/embedding.md` design page — the embedding/vector-compute owner deepening the `Embed` run, spawned by `EMBEDDING_VECTOR_LANE`.
- Capability: a new `.planning/models/embedding.md` page with one `VectorEncoding` `[SmartEnum<string>]` axis (float32 · int8-scalar-quantized · binary-hamming · product-quantized), one `VectorScore` `[SmartEnum<string>]` metric axis (cosine · dot · euclidean · hamming), one `EmbeddingVector` carrier, and one `Score` fold lowering each metric onto `TensorPrimitives.CosineSimilarity`/`Dot`/`Distance` and the int8 quantize onto `TensorDtype` `ConvertSaturating` + `QuantizationPolicy`.
- Packages: Microsoft.ML.OnnxRuntime (the `inference#INFERENCE_MODES` `Embed` run + `OrtIoBinding` device path), System.Numerics.Tensors (`CosineSimilarity`/`Dot`/`Distance`, catalogued at `.api/api-tensors.md`), CommunityToolkit.HighPerformance, LanguageExt.Core, Rasm.Persistence (project).
- Integration: register the `models/embedding` page on the `models/` sub-domain and the `ARCHITECTURE.md` models charter; the embed run extends `inference#INFERENCE_MODES` `Embed`/`BoundLoop` (the device-resident path), never a new model lane; the candidate set crosses to the `Rasm.Persistence` vector lane by content-key reference (Persistence owns the index, Compute owns embed-and-score); the BIM point-cloud/symbol classifier reuses the same embed surface.
- Considerations: the `TensorPrimitives.CosineSimilarity` member is confirmed in the tensors catalogue; the binary-Hamming pre-filter prunes before the exact float rerank so a quantized score is a coarse gate, never the terminal verdict; the offline index build stays the Persistence vector-lane owner, Compute mints no second vector store; a `ModelRun`/`Generate` receipt slot carries the embed-vector count, never a new receipt case.

[T-COMPUTE-REGISTRY-AUDIT] [QUEUED]: Reconcile the README package registry against the central manifest and prune the one catalogued-but-non-consumed package.
- Capability: the `Grpc.AspNetCore.HealthChecks` and `Grpc.AspNetCore.Web` rows are registered under `[REMOTE_WIRE]` (done); `SharpGLTF.Runtime` is de-registered from `[INTERCHANGE_STAGING]` because no Compute design page consumes it — the codecs `TESSELLATION_BRIDGE` re-imports the companion GLB through the Bim glTF import rail (a Bim concern, confirmed by the codecs `Packages` line carrying only `SharpGLTF.Core`/`SharpGLTF.Toolkit` and the csproj referencing `.Core`/`.Ext.3DTiles`/`.Toolkit` only), so `SceneTemplate.CreateInstance` runtime decode is the Bim import owner's, not Compute's; the `.api/api-sharpgltf.md` `SharpGLTF.Runtime` catalogue entry is retained as reference but names no Compute consumer.
- Packages: Grpc.AspNetCore.HealthChecks, Grpc.AspNetCore.Web (registered, manifest-pinned); SharpGLTF.Runtime (de-registered — non-consumed by Compute).
- Integration: README registry rows only; the `Grpc.AspNetCore.HealthChecks` row aligns to the `remote/channels` wire-health leg `csharp:IDEAS#ONE_HEALTH_DEGRADATION_WIRE` consumes; the re-imported GLB `SceneTemplate` decode aligns to the Bim glTF import rail the `interchange/codecs#TESSELLATION_BRIDGE` re-enters, never a Compute decode owner.
- Considerations: a README row exploited by no design page is the named coverage gap — the Compute registry now lists only packages a Compute page consumes; the GLB re-import decode crosses the named Bim boundary, so a Compute-side `SharpGLTF.Runtime` decode is the deleted form; no version pin enters the README (centralized in the manifest).

[T-NATIVE-BLAS] [BLOCKED]: Activate the native-OpenBLAS/MKL dense and sparse execution row.
- The `numeric#DENSE_ALGEBRA`/#SPARSE_SOLVE `LinearProvider` RID axis is shape-complete and the managed terminal is proved end-to-end on osx-arm64; the native row activates on a win-x64 / linux-x64 host carrying the `MathNet.Numerics.MKL.Win-x64`/`.Linux-x64` or OpenBLAS native asset where `LinearProvider.Select` binds the native `ILinearAlgebraProvider` and the `Control.TryUseNativeOpenBLAS`/`TryUseNativeMKL` probe returns true.
- Blocked on a host RID that resolves the native asset; the managed fallback is the correct cold start until then.

[T-GPU-EP] [BLOCKED]: Register and run the CUDA/DirectML GPU execution-provider rows.
- The `models#EP_AXIS` `AppendExecutionProvider_CUDA(0)`/`_DML(0)` member shapes are compiled; execution registers and runs on device on an NVIDIA-Linux/Windows RID + driver (CUDA) or Windows RID (DirectML), gated on the `Microsoft.ML.OnnxRuntime.Gpu`/`.DirectML` Windows-profile admission verdict.
- Blocked on a GPU host RID and the manifest admission verdict.

[T-LIVE-HOST-ONNX] [BLOCKED]: Prove the in-host ONNX/GenAI native load under the live Rhino plugin ALC.
- The `models#SESSION_CAPSULE`/#INFERENCE_MODES/#GENERATIVE_RUN owners are transcription-complete; the residual gates are the `libortextensions.dylib` versioned-RID resolution, the `RunOptions.Terminate` latch cadence, the ORT native dylib load inside the RhinoWIP plugin ALC, and a live multi-token generation against a genai-format model asset — each a live-host probe this osx-arm64 host structurally supplies only under the bridge scenario.
- Blocked on the live Rhino/GH2 host process and the genai-format model artifact.

[T-LIVE-HOST-UDS] [BLOCKED]: Prove the Kestrel UDS server leg and GH2 solve-path ceiling under the live host.
- The `remote#TRANSPORT_AXIS`/#CALL_POLICY UDS transport and the `scheduling#SOLVE_GUARD` async-result guard are shape-complete; the residual gates are the Kestrel `ListenUnixSocket` server leg inside the Rhino plugin ALC, the `Grpc.Core.Api` transitive route proof, the live CoreML option-value domains, and the GH2 async readback ceiling under the live GH2 host.
- Blocked on the live Rhino-plugin app root + UDS attach scenario and the live GH2 host.

## [2]-[CLOSED]

[T-DDG-ADJOINT-ROWS] [COMPLETE]: Landed the six DDG geometry rows (`Gradient`/`Divergence`/`Curl`/`CotangentLaplacian`/`HeatFlow`/`Spectral`) on the closed `vocabulary#OPERATION_TABLE` `TensorOpFamily` axis under `TensorOpKind.Geometry`, the `dispatch#EQUIVALENCE_INTEROP` `GeometryAdjoint.Rows`→`OperatorRow` bindings and `Backward.Operator(GeometryTape, seed)` reverse-mode apply, and the `optimizer#OPTIMIZER_LANE` `DesignProblem.OperatorRows` registry (`DesignVariable.Continuous`→`Gradient`, `Density`→`CotangentLaplacian`) so `Lower` emits a real `Seq<GeometryTape>` tape — over the now-published `Rasm/Vectors/_ARCHITECTURE.md` DDG adjoint surface (the closed `OperatorRow` `[SmartEnum<string>]`, the public `MeshAdjointSnapshot` handle, the four `DiscreteCalculusKernel` compositions), a `SETTLED CONTRACT, SOURCE-MEMBER TRANSCRIPTION` over the verified-live `Spectral.cs` `DiscreteCalculus`/`SparseMatrix.Multiply`/`LaplacianCache` assembly with no fabricated `TensorOpFamily` member and no re-assembled DEC operator. The residual leaf is the consumer design-mesh wiring at the shape/topology-optimization call site (`solver#GRADIENT_ADJOINT`) supplying the `MeshAdjointSnapshot` through `MeshAdjointSnapshot.Of`; an absent `DesignMesh` lowers an empty tape and descends degenerate by construction.

[T-NEURAL-SURROGATE] [COMPLETE]: Realized the `SurrogateKind.NeuralField` row, the `NeuralFieldModel` content-keyed coordinate-MLP/Fourier-feature field, `Surrogate.OfField`, and the `Predict` arm threading the leased `inference#INFERENCE_MODES` `RunOps.Infer` run behind the identical `(Seq<double>, double)` surrogate contract on `solver/optimizer.md` #OPTIMIZER_LANE — the network fit is the Python offline companion, an in-proc ORT-Training fit is the rejected form, the surrogate-hit count rides the existing `Optimization` receipt slot.

[T-RESIDUAL-CODEC] [COMPLETE]: Realized the `FieldCodecPolicy.ResidualPredict` column, the `ResidualPredictor` model-lane chunk predictor, and the `FieldCodec.ResidualEncode` neighbour-stencil fold on `interchange/codecs.md` #FIELD_RESULT_CODEC — the grid-coordinate chunk index is preserved (content-defined byte chunking is rejected), the lossless deflate stays the terminal fall-through, the achieved residual rides the `Cache` receipt, and the offline-trained predictor warms from the same model-lane cache the surrogate uses.

[T-TILES-METADATA] [COMPLETE]: Realized the `MetadataProperty`/`PropertyTable`/`TileMetadata`/`FeatureBand` `EXT_structural_metadata` semantic layer, the `TileNode.Metadata` slot, and the `metadata`-threaded `Build`/`ExportTiles` on `interchange/codecs.md` #TILE_PARTITION joining the `Rasm.Bim` IFC classification and the `solver#DISCRETIZATION_MESH` field values at the shared content-key, with `meshoptimizer` LOD on the leaf geometry — the SharpGLTF raw glTF-extension binary write (property-table buffer views, `_FEATURE_ID_0`) is the `[EXT_STRUCTURAL_METADATA]` RESEARCH leaf and the leaf-content body stays gated on the Bim tile-emit codec.

[T-COMPILED-EP-CONTEXT] [COMPLETE]: Realized the `ModelSessions.Compile` member driving `OrtModelCompilationOptions` (`SetInputModelFromBuffer`/`SetOutputModelPath`/`SetEpContextEmbedMode`/`SetEpContextBinaryInformation`/`SetGraphOptimizationLevel`/`SetFlags`/`CompileModel`) and the `FleetContextKey` `(checksum, OrtEpDevice fingerprint)` device-keyed `ArtifactIndexRow.EpContext` publish on `models/sessions.md` #SESSION_CAPSULE — a matching-hardware peer warms from the one blob-lane owner over `remote#ARTIFACT_FRAMES`, a mismatched fingerprint addresses a distinct row and falls back to a fresh compile, the live compile/compat-verdict probe rides the `T-LIVE-HOST-ONNX` host-blocked card.

[T-REACTIVE-GRAPH] [COMPLETE]: Realized the `JobNode.InputBytes`/`NodeKey` content seed, `JobGraph.Keys`/`Topological`/`Reconcile` digest-diff against the prior key map, and the clean-node replay from `models#RESULT_CACHE` on `scheduling/runtime.md` #JOB_GRAPH — the dirty mark is the one `interchange#CONTENT_ADDRESSING` `XxHash128` content-key inequality over the existing edges, the clean-node short-circuit is the existing cache hit, no per-node version stamp or second memoization owner appears, a cycle faults on the existing `ComputeFault` rail.

[T-HTTP3-CHANNEL-POLICY] [COMPLETE]: Lifted `GrpcChannelPolicy.Canonical` into the `remote/channels#TRANSPORT_AXIS` owner (send/receive caps, reconnect-backoff, pooled-idle, keepalive, multi-HTTP2, the `HttpVersion`/`HttpVersionPolicy` pair, the fifth `RemoteTransport.Http3` QUIC-gated row, `Http3` forward-only on macOS behind the `[HTTP3_QUIC]` RESEARCH leaf) threaded into `WireChannels.Canonical`, and closed `#COMPOSED_CREDENTIAL` with `CompressionProviders.Winning`; `DisableResolverServiceConfig=true` keeps the AppHost Polly pipeline the one hop-retry owner.

[T-DOC-MIGRATE] [COMPLETE]: Re-homed the 12 design pages into the single `.planning/<sub-domain>/<page>.md` tree, de-doubled each page stem off its folder, rebuilt `ARCHITECTURE.md` as a codemap, `README.md` as router-plus-package-registry, and authored `IDEAS.md`/`TASKLOG.md`. The per-folder `.api/` stays at the package root.
