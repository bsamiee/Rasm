# [APPHOST_TASKLOG]

The open and closed work for the runtime spine, distilled from `IDEAS.md`. Each open task carries a status marker and the capability-to-build, packages, integration points/boundaries, and key considerations; one idea spawns one or more tasks across one or more files. Closed cards record already-settled cleanup.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[DEEPEN_MODEL_ROUTING_GUARDRAILS]-[QUEUED]: deepen the existing `Agent/reasoning.md#MODEL_GOVERNANCE` `ChatClientBuilder` fold with a model-selection axis and content-filter/guardrail middleware rows, so a model draw routes to a target model by targeting/experiment verdict and passes a content-policy gate — extending the one `ModelGovernance.Compose` pipeline, not a second model cache.
- Capability: model routing (a `ChatClientBuilder.Use` arm selecting by feature verdict: cost-tier, capability, A/B variant, sticky bucket) plus guardrail middleware (pre-call prompt classification, post-call content-filter/PII redaction over the existing `RedactionRegistration`) — covering the whole model-governance category (route, meter, cache, trace, filter) on one decorator chain.
- Shape: new `ChatClientBuilder.Use` arms folded into `ModelGovernance.Compose` — one `UseModelSelection` arm reading the `OpenFeature` `FlagVerdict`, one `UseContentFilter` arm over `RedactionRegistration` — and one `ModelRoute` row family discriminating target client, owned by `Agent/reasoning.md`, never a parallel routing client beside `GovernedClient`.
- Unlocks: content-policy-gated, experiment-routed model draws the in-process reasoning loop and the MCP server-sampling leg both inherit from the one `Compose` fold; closes the guardrail gap without a second governance pipeline.
- Anchors: `ModelGovernance.Compose` returning `GovernedClient` via `inner.AsBuilder().UseOpenTelemetry().UseDistributedCache().UseFunctionInvocation().Build` (`Agent/reasoning.md` MODEL_GOVERNANCE), `ModelGovernance.Charge -> GrantBroker.Admit` `CostUnit.ModelTokens`, `CacheLane.ModelResult` HybridCache via `UseDistributedCache` (`Runtime/resources.md` CACHE_PORT), `RedactionRegistration` (`Wire/companion.md` `ControlInbound.DispatchTool` redaction seam), `DataClassification`; deepens the admitted `Microsoft.Extensions.AI` 10.7.0 middleware, with the second touch-point on `Runtime/features.md` for the `FlagVerdict` the selection arm reads.
- Tension: the new arms must compose into the same `ChatClientBuilder` fold both front doors share — a second `IChatClient` pipeline, a model-routing client beside `GovernedClient`, or a second response cache is the deleted form (an unmetered/opaque model call is forbidden; every draw still rides `Charge -> GrantBroker.Admit`). The `UseModelSelection` arm reads the `FlagVerdict` the admitted `OpenFeature` provider on `Runtime/features.md` projects, composing against that verdict at the seam.

[DEEPEN_FLEET_ROLL_STRATEGY_AXIS]-[QUEUED]: deepen the existing `Sandbox/provisioning.md#ROLLOVER_DRAIN` `FleetRoll` conductor with a rollout-strategy axis (canary, blue-green, linear-wave) as a `[SmartEnum]` row on the fleet-roll, so a fleet-wide update wave selects a progressive strategy off the targeting/health verdict instead of one fixed health-gated wave.
- Capability: rollout strategies — canary (single-node probe then expand on health-hold), blue-green (parallel cohort swap on health-pass), linear-wave (N% increments with bake windows) — covering the whole progressive-delivery-of-binaries category, each strategy one row driving the same drain-before-swap handshake.
- Shape: one `RollStrategy` `[SmartEnum<string>]` row family with a `Next(cohort, health)` arm per strategy, owned by `Sandbox/provisioning.md`; `FleetRoll` discriminates the wave shape on the row over `PeerRoster`, the strategy a column on the existing `ScheduleEntry.Spread` fleet-rollup row rather than a parallel roll conductor.
- Unlocks: canary/blue-green waves the membership view and health rail gate; ties progressive binary rollout to the same targeting plane as feature rollout without a second rolling-update owner.
- Anchors: the `FleetRoll` conductor over `PeerRoster` + ROLLOVER_DRAIN drain-before-swap (`Sandbox/provisioning.md`), `UpdatePhase.RollingOver/RolledBack` + `UpdateRail.Rollover` drain-gated restart (`Sandbox/provisioning.md` UPDATE_RAIL), `FleetRollReceipt` via `ReceiptEnvelopeWire` TS_PROJECTION (`Sandbox/provisioning.md`), `ScheduleEntry.Spread` + `XxHash3.HashToUInt64` seed (`Runtime/time.md`), `HealthSnapshot`/`DegradationLevel` health-gate (`Observability/health.md`), `FencingToken` conductor election (`Runtime/time.md` FENCING_TOKEN); the second touch-point is `Runtime/features.md` for the shared targeting plane.
- Tension: strategy is a row on the existing `FleetRoll`, not a parallel conductor — a second roll state machine or a strategy-specific scheduler beside `ScheduleEntry.Spread` is the rejected form; canary health-hold reuses the existing `HealthSnapshot` gate, never a new probe owner.

[DEEPEN_DISTRIBUTED_QUOTA_STORE_SEAM]-[QUEUED]: extend the `GrantBroker` cost-metering to a cross-process quota over a shared Persistence store, so a tenant's cost ceiling is enforced fleet-wide — every node debits one durable budget under a fenced write — instead of per-process, closing the multi-node metering gap the identity plane exposes.
- Capability: distributed quota — a durable per-tenant `Budget` cell, atomic `FencingToken`-fenced debit across nodes, quota-window reset, and over-ceiling to degradation projection — covering the whole cross-process cost-governance category for tokens, ops, and storage as one shared ledger.
- Shape: the existing `GrantBroker.Admit` cost-ceiling debit gains a store-backed seam reading/writing the shared budget cell under a `FencingToken.Admits`-fenced write, owned by `Agent/identity.md`; one `DistributedBudget` seam row, never a parallel quota owner — the per-process broker stays the one entry, the store the backing.
- Unlocks: fleet-wide cost ceilings the model-governance `Charge`, the plugin `GrantHandle` charge, and the operator call all debit against one durable budget; required for any multi-node identity plane where per-process budgets would let a tenant exceed its ceiling N-fold.
- Anchors: `GrantBroker.Admit` + `CostModel` + `CostVector(HashMap<CostUnit,long>)` + `CostUnit.ModelTokens` (`Agent/capability.md` GRANT_BROKER; `Agent/reasoning.md` `ModelGovernance.Charge`), the object-set × op-class × cost-ceiling × time-window algebra (`Agent/capability.md`), `FencingToken.Admits` Kleppmann reject-lower + `LeaseElection.Acquire` (`Runtime/time.md` FENCING_TOKEN), `TenantContext.TenantId` (per-tenant budget key + RLS, `Runtime/ports.md`), the Persistence/server-tier fence (`Runtime/time.md`); the seam ripples to `Rasm.Persistence` (distributed-quota store — durable per-tenant `Budget` cell + fenced debit ledger — under `TenantId` RLS).
- Tension: the shared store is a Persistence ripple, not an AppHost owner — the debit fences through the existing `FencingToken` and the broker stays the one metering entry; a second cost meter or a quota service beside `GrantBroker` is the rejected form. The broker debits through the `FencingToken`-fenced seam interface; the durable per-tenant `Budget` store is the `Rasm.Persistence` ripple (the branch `ONE_FENCED_LEASE_STORE` CAS store), consumed at the seam and landing in parallel.
- Ripple: `csharp:Rasm.Persistence` `[ONE_FENCED_LEASE_STORE]` (fenced per-tenant `Budget` debit ledger).

[RESTRUCTURE_AGENT_COMMAND_DISPATCH_PAGE]-[QUEUED]: add the `Agent/runtime.md` command-dispatch page the spine references but nowhere owns — consolidating the `CommandAlgebra` transaction, the brokered `CommandAIFunction` dispatch shared by MCP/reasoning/plugin front doors, and the `CommandReceipt` chaining into one declared owner page, so the durable-orchestration spine has a named dispatch surface to drive steps through.
- Capability: one declared command-dispatch owner — commit-or-rollback `CommandAlgebra.Run` over `ComputeIntent`, the brokered `CommandAIFunction` adoption seam, `CommandReceipt` projection, and the three-front-door invariant (MCP tool / reasoning loop / plugin `GrantHandle` route one dispatch) — covering the whole command-execution category the orchestration steps invoke.
- Shape: a new `Agent/runtime.md` page owning the `CommandAlgebra` transaction + dispatch fold currently scattered across `Agent/capability.md`/`Agent/mcp.md`/`Agent/reasoning.md`; one `Run(ComputeIntent) -> CommandReceipt` entry the durable workflow steps call, no parallel dispatcher.
- Unlocks: a named command-dispatch surface the `DURABLE_ORCHESTRATION_SPINE` steps invoke and the `EVENT_BUS` subscriptions trigger; resolves the dangling spine reference so orchestration has a concrete step-execution owner.
- Anchors: `CommandAlgebra` commit-or-rollback over `ComputeIntent` (`Agent/capability.md` COMMAND_ALGEBRA), `CommandReceipt` chained by `EventLog.Append` (`Agent/reasoning.md` REPLAYABLE_TRANSCRIPT), `CommandAIFunction` adoption via `ToolProjection.Adopt` — the one tool-adoption seam, two front doors (`Agent/reasoning.md` REASONING_LOOP; `Agent/mcp.md` METHOD_AXIS), `GrantHandle.Invoke` routing through the command algebra exactly as an agent call (`Sandbox/isolation.md` GRANT_HANDLE), `ControlInbound.DispatchTool` (`Wire/companion.md`), the `EventLog` hash-chain (`Runtime/determinism.md` EVENT_LOG); the second touch-point is `Runtime/orchestration.md`, whose durable steps call the named `Run` entry.
- Tension: this is a restructure that names an existing-but-undeclared owner, not a new capability — it must not fork a second dispatcher beside `CommandAlgebra`; the page consolidates references, the one `Run` entry stays the only command transaction, and `EventLog` stays the one chained event-log owner on the durable OpLog changefeed.

[COLLAPSE_FAULT_SOURCE_SUPPORT_TRIGGER]-[QUEUED]: collapse the `FaultSpine` fault-transition into `SupportTrigger` as a `SupportTrigger.FaultTransition` case carrying the `FaultRecord`, so a fault commit and its support-capture trigger are one fact stream rather than an untyped capture-delegate arming a separate trigger — densifying the fault-to-capture path the durable spine relies on for crash-durable recovery.
- Capability: unified fault-to-capture — the four `FaultSource` cases project through `FaultRecord.From` into one `SupportTrigger.FaultTransition`, so every fault arms support capture through one trigger case beside `ExternalCommand`/`WatchdogTimeout`, covering the whole fault-evidence category (unhandled, unobserved-task, signalled, host-crash-marker) on one trigger vocabulary.
- Shape: fold `FaultSpine.ArmTraps`'s capture-delegate-then-`PhaseTrigger.FaultCommitted` into a single `SupportTrigger.FaultTransition(FaultRecord)` emission, owned by `Runtime/lifecycle.md`; the `FaultSource` `[Union]` stays the trap payload and `SupportTrigger` absorbs the transition case, collapsing two parallel construction sites into one fact with kind metadata.
- Unlocks: one fault-transition fact the durable orchestration's crash-recovery reads to resume in-flight workflow steps after a crash; removes the `FaultSource`/`SupportTrigger` seam duplication so `HostCrashMarker` boot-probe evidence and live faults share one capture trigger.
- Anchors: `FaultSource` `[Union]` `Unhandled`/`UnobservedTask`/`Signalled`/`HostCrashMarker` (`Runtime/lifecycle.md` FAULT_SPINE), `FaultRecord.From` total flatten with `JsonDerivedType` kind literals, `FaultSpine.ArmTraps` (capture delegate then `PhaseTrigger.FaultCommitted`), `SupportTrigger.ExternalCommand/WatchdogTimeout` (`Wire/companion.md` `ControlInbound`; `Runtime/time.md` SCHEDULE_PORT Heartbeat), `SupportCapture.Capture` (`Wire/companion.md` `ControlInbound`); the second touch-point is `Runtime/orchestration.md`, whose crash-recovery reads the collapsed transition fact.
- Tension: the collapse must preserve every `FaultSource` case and the `HostCrashMarker` boot-probe path — the `SupportTrigger.FaultTransition` carries the `FaultRecord` so no fault evidence is deleted; a generic `IReceipt` or a flattened trigger that loses the kind-discriminated `FaultRecord` fields is the rejected form (keep the typed receipt).

[COLLAPSE_HEALTH_DEGRADATION_CELL]-[QUEUED]: collapse the `HealthSnapshot` read and the `DegradationState.Derive` derivation into one atomic `DegradationCell` read so a load-shed/governor decision reads a single coherent (snapshot, level) cell rather than two independently-mutating surfaces, giving the work-lane governor a race-free pressure signal.
- Capability: one atomic degradation cell — `HealthSnapshot.Snapshot` and `DegradationState.Derive` fold into one atom swap producing a `DegradationCell` snapshot the governor reads coherently — covering the whole pressure-read category (health probe, utilization grade, derived level, forced override, cascade floor) as one consistent read.
- Shape: `DegradationCell` already owns the atom + `IHealthCheckPublisher` seam; fold the `HealthSnapshot` read into the same atom so `Derive` and the snapshot are one swap, owned by `Observability/health.md` — the governor's load-shed/bulkhead-resize reads one `DegradationCell` value, collapsing the two-surface read into one cell with snapshot+level slots.
- Unlocks: a race-free pressure cell the `LaneGuard` governor reads for adaptive-concurrency and load-shed decisions; without the collapse the governor could read a stale snapshot against a fresh level (or vice versa) under concurrent probes.
- Anchors: the `DegradationCell` atom cell + `IHealthCheckPublisher` publisher seam, `Derive(state, snapshot)` escalation-immediate/recovery-hysteresis + `Force(forced)` + `Cascade(parent)` floor (`Observability/health.md` DEGRADATION_RAIL), `DegradationState(Derived, Forced, Cascade, streak, dwell)`, `HealthSnapshot.Snapshot(Instant, CorrelationId)` (`Observability/health.md` HEALTH_FOLD), `UtilizationCell` MeterListener grade-on-read, `DegradationPolicy.Rule` rows; the second touch-point is `Runtime/laneguard.md`, whose governor reads the collapsed cell.
- Tension: the collapse must keep the escalation-immediate/recovery-hysteresis `Derive` semantics and the cross-process cascade seam-split (READ stays here, WRITE at `Wire/companion.md#DEGRADATION_CASCADE`) — folding the snapshot into the cell must not merge the parent-cascade owner; a single cell that loses the cascade-floor/hysteresis fields is the rejected form.

[SEAM_OUTBOX_AND_WORKFLOW_PERSISTENCE_TABLE]-[QUEUED]: reconcile the durable-orchestration step-state table and the transactional-outbox table as a coordinated Persistence ripple, so the workflow step-state commit and the outbox enqueue ride one transaction boundary — giving exactly-once-effective delivery and crash-durable step resumption a shared durable seam.
- Capability: one coordinated durable seam — workflow step-state rows (step id, status, attempt, payload, fence) + outbox rows (topic, payload, dispatch status, dedup key) committed under one tenant-scoped transaction, with dead-letter and at-least-once dispatch sweep — covering the whole durable-message+durable-step category the bus and orchestration share.
- Shape: two Persistence tables under one transaction owner the AppHost names but Persistence implements; step-state and outbox share the `FencingToken` fence and `TenantId` RLS predicate, with the AppHost orchestration/bus owners on `Runtime/orchestration.md` and `Wire/outbox.md` writing through this one seam rather than two uncoordinated stores.
- Unlocks: crash-durable step resumption (orchestration) and exactly-once-effective delivery (bus) on one transactional boundary; both the durable-orchestration spine and the event-bus fabric depend on this shared durable table landing.
- Anchors: the `EventLog` hash-chain on the durable OpLog changefeed (`Runtime/determinism.md` EVENT_LOG, the one non-chained-event-store rejection), `SchedulePort` + `ScheduleEntry.Spread` dispatch-sweep cadence (`Runtime/time.md` SCHEDULE_PORT, the one scheduler), the `DeliveryFanout` dedupe cell (`Wire/outbound.md` DELIVERY_FANOUT, the dedup-key precedent), `FencingToken.Admits` fence (`Runtime/time.md`), `TenantId` RLS `current_setting('rasm.tenant')::uuid` (`Runtime/ports.md`), `CommandReceipt` durable receipt; the seam ripples to `Rasm.Persistence` (orchestration step-state table + transactional-outbox table under one tenant-scoped transaction + dead-letter, dispatch-sweep cursor + dedup-key index).
- Tension: step-state and outbox are Persistence tables, not AppHost owners — the AppHost names the seam and writes through it; the dispatch sweep rides the one `SchedulePort`, dedup reuses the `DeliveryFanout` idempotency-key precedent, and `EventLog` stays the one chained log on the OpLog. A second scheduler for the outbox sweep or a parallel event store is the rejected form. The AppHost orchestration and bus owners write through the one transaction-boundary seam interface; the transactional-outbox table is the branch `ONE_OUTBOX_EGRESS_SPINE` Persistence ripple and the workflow step-state CAS row is the branch `ONE_FENCED_LEASE_STORE` Persistence ripple, the two committing under one tenant-scoped transaction and landing in parallel as the durable backing.
- Ripple: `csharp:Rasm.Persistence` `[ONE_OUTBOX_EGRESS_SPINE]` (transactional outbox) + `[ONE_FENCED_LEASE_STORE]` (CAS workflow step-state).

[DEEPEN_EVENTBUS_DATAFLOW_TOPOLOGY]-[QUEUED]: deepen the existing `DRAIN_QUEUES` Dataflow surface with pub/sub topic fan-out, subscription join, and coalesce blocks (`BroadcastBlock`/`BufferBlock`/`ActionBlock`/`JoinBlock`/`BatchedJoinBlock`), turning the `DeliveryFanout` into a real in-process bus — topics, subscriptions, bounded buffers, and back-pressure over the admitted `Tasks.Dataflow` primitives.
- Capability: full in-process bus topology — `BroadcastBlock` topic fan-out, `BufferBlock` bounded queues with back-pressure, `ActionBlock` subscriber consumers, `JoinBlock`/`BatchedJoinBlock` subscription correlation/coalesce, linking with greedy/non-greedy + `DataflowLinkOptions` `PropagateCompletion` — covering the whole pub/sub-topology category for the bus.
- Shape: topic/subscription rows built as `DrainSurface` builders over the same `DrainKind` union the `DRAIN_QUEUES` family owns (Network case, `Row.Kind` names the topology), owned by `Wire/topics.md`; one `Topic<T>` with `BroadcastBlock` fan-out feeding bounded `BufferBlock`s per subscription and `ActionBlock` consumers draining under the `DrainBand` conductor, never a hand-rolled fan-out loop or dual-queue zip.
- Unlocks: topic/subscription delivery the outbox dispatch sweep feeds and `DeliveryFanout` extends; turns the single-message fan-out into a multi-topic bus with bounded back-pressure, the in-process leg of exactly-once-effective delivery.
- Anchors: `DrainSurface` fan-out/join/coalesce builders + `DrainKind` `[SmartEnum]` (Pipe/Network/FanOut/CorrelatedJoin) over the `DrainQueue<T>` union (`Runtime/resources.md` DRAIN_QUEUES: `BroadcastBlock` fan, `JoinBlock` correlate, `BatchedJoinBlock` coalesce, Greedy/MaxGroups/PropagateCompletion policy columns), `DeliveryFanout.Fan` + dedupe cell + `DeliveryReceipt` (`Wire/outbound.md` DELIVERY_FANOUT), `DrainBand` conductor + `CancelScope` completion awaits (`Runtime/resources.md`), the `System.Threading.Tasks.Dataflow` transitive floor (admitted 10.0.9, `.api/api-dataflow.md`); the second touch-point is `Wire/outbox.md`, whose dispatch sweep feeds the topics over the branch `ONE_OUTBOX_EGRESS_SPINE` op-log shared with `SEAM_OUTBOX_AND_WORKFLOW_PERSISTENCE_TABLE`.
- Tension: topology blocks are `DrainSurface` builders over the one `DrainKind` union (Dataflow is a transitive framework floor, never a direct project asset) — a per-topic background loop, a parallel correlation buffer, or a second queue owner is the deleted form; bounded `BufferBlock`s must carry back-pressure (`BoundedCapacity`), never unbounded accumulation.
- Ripple: `csharp:Rasm.Persistence` `[ONE_OUTBOX_EGRESS_SPINE]` (op-log feeds the topic dispatch sweep).

[DEEPEN_LANEGUARD_RESILIENCE_GOVERNOR]-[QUEUED]: build the new `LaneGuard` owner applying `Polly.Core` resilience (bulkhead, adaptive-concurrency, load-shed, hedge) plus first-class Simmy chaos to the in-process work-lane/command edge, since the transport hops already carry the keyed Polly registry but the `WorkLane`/command edge has none — closing the in-process resilience gap.
- Capability: an in-process resilience governor — bounded-concurrency bulkhead per lane, ResourceMonitoring-fed adaptive concurrency, load-shed on degradation pressure, hedge for idempotent commands, and `Polly.Simmy` chaos (latency/fault/outcome injection) — covering the whole work-lane resilience category the command/solve edge lacks.
- Shape: one `LaneGuard` owner composing a keyed Polly `ResiliencePipeline` per `WorkLane` (mirroring `KeyedLane.Register`'s per-hop `AddResiliencePipeline` registry but for in-process lanes), owned by `Runtime/laneguard.md`; bulkhead+rate-limiter+circuit-breaker+chaos strategies as pipeline rows driven by the collapsed `DegradationCell` pressure read, never a per-lane retry loop.
- Unlocks: adaptive-concurrency and load-shed for the command/solve edge gated on the atomic `DegradationCell`; first-class chaos for the work lanes; gives the spine a resilience governor symmetric to the transport `KeyedLane`.
- Anchors: the `WorkLane` Compute solve-path name (`Runtime/resources.md` DRAIN_QUEUES boundary note distinguishing `WorkLane` from `DrainQueue`), the `KeyedLane.Register/Limiter` registry pattern (`Wire/outbound.md` KEYED_PIPELINES: `AddResiliencePipeline`, `RateLimiterStrategyOptions`, `ConcurrencyLimiterOptions`, `CircuitBreakerManualControl/CircuitBreakerStateProvider`, `AddChaosLatency/AddChaosFault/AddChaosOutcome` on the test-host profile), `UtilizationCell` MeterListener + `ResourceQuota` grade (`Observability/health.md` HEALTH_FOLD), the `DegradationCell` pressure read (`Observability/health.md` DEGRADATION_RAIL), `DeadlineClass` allotted projection (`Runtime/time.md`); reuses the admitted `Polly.Core`/`Polly.Extensions`/`Polly.RateLimiting` 8.7.0 and `Microsoft.Extensions.Diagnostics.ResourceMonitoring` 10.7.0 (all present), no `AddSingleton` spelling.
- Tension: `LaneGuard` is a new spine owner for the in-process edge, distinct from the transport `KeyedLane` — it must not become a second retry on the transport seam; in-process lanes get exactly one retry owner (`LaneGuard`) just as each hop has exactly one. A retry both on the lane and re-applied at the hop on one seam is the rejected form (the one-retry-owner-per-seam discipline). The per-`WorkLane` shed verdict `LaneGuard` mints from the `DegradationCell` is the one verdict `Rasm.Compute/Runtime/admission` consumes on its `SubstrateSelection` fold, never a Compute-side re-derivation.
- Ripple: `csharp:Rasm.Compute` `[ONE_DEGRADATION_SHED_VERDICT]` (shared `WorkLane` shed verdict on Compute admission).

[STRUCTURE_SANDBOX_GRANT_MEDIATION]-[QUEUED]: unify the host-facing `GrantBroker` and the plugin-facing `GrantHandle` into one mediation surface so host calls, agent calls, and plugin calls route through a single grant-and-charge fold — giving the identity/authz plane one admission owner the new `RuntimePolicy` verdict gates.
- Capability: one grant-mediation owner — the `GrantBroker`'s object-set × op-class × cost-ceiling × time-window admission and the `GrantHandle`'s per-plugin scoped dispatch fold into one `BrokeredCall` mediation discriminating caller shape (operator/agent/plugin) — covering the whole capability-admission category on one charge-and-dispatch seam.
- Shape: `GrantHandle.Invoke` already dispatches through the command algebra exactly as an agent call; lift the shared admission into one mediation surface where caller modality is a discriminant, not a parallel broker, owned across `Agent/identity.md` and `Agent/policy.md` — the per-call charge debits one `Budget`, the scope check is one `Scope.Covers` fold, the policy verdict one gate.
- Unlocks: a single admission point the `RuntimePolicy` ABAC verdict gates and the distributed-quota debit hooks; collapses the host/plugin mediation duplication so identity, policy, and cost meet on one fold.
- Anchors: `GrantBroker` consent/elevation/dry-run + `CostModel` (`Agent/capability.md` GRANT_BROKER), `GrantHandle.Permits(descriptor, now) -> Scope.Covers` + `GrantHandleSurface.Invoke/Bind` dispatch closure (`Sandbox/isolation.md` GRANT_HANDLE), `BrokeredCall` per-call mediation record (`PluginId`/`Descriptor`/`Permitted`/`CostVector`/`At`), `CommandReceipt` (one evidence stream), `Consent.Elevated` transient scope, `CapabilityDescriptor.Permission` (`PermissionShape`); the seam ripples to `Rasm.Persistence` (shared with the distributed-quota store `Budget` debit).
- Tension: unify the mediation without a second broker — the `GrantHandle` holds no host references by construction (the no-ambient-authority law: the host never passes a service provider, config root, or clock into a plugin, only the grant handle), so the unified surface must preserve that the plugin path carries only scope+dispatch-closure; merging in a way that hands a plugin a service provider is the deleted form. Caller modality is a discriminant on one surface, not parallel objects. The `RuntimePolicy` verdict resolves against the branch `ONE_IDENTITY_STORE` principal/role rows and the per-call charge debits the branch `ONE_FENCED_LEASE_STORE` `Budget`, both consumed at the seam.
- Ripple: `csharp:Rasm.Persistence` `[ONE_IDENTITY_STORE]` (principal/role admission rows) + `[ONE_FENCED_LEASE_STORE]` (per-call `Budget` debit).

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[ADMIT_OPENIDDICT_CLIENT]-[COMPLETE]: `OpenIddict.Client` 7.5.0 pinned + referenced; README `[IDENTITY_AUTHZ]` group and `.api/api-openiddict-client.md` authored.
[ADMIT_ASPNETCORE_AUTHORIZATION]-[COMPLETE]: `Microsoft.AspNetCore.Authorization` 10.0.9 pinned + referenced (host-neutral ABAC evaluation core, no HTTP-pipeline coupling); README `[IDENTITY_AUTHZ]` group and `.api/api-authorization.md` authored; the ABAC evaluation over the principal rides `RUNTIME_IDENTITY_AUTHZ_PLANE`.
[ADMIT_SERVICEDISCOVERY]-[COMPLETE]: `Microsoft.Extensions.ServiceDiscovery` 10.7.0 pinned + referenced; README `[OUTBOUND]` group and `.api/api-service-discovery.md` authored.
[ADMIT_OPENFEATURE]-[COMPLETE]: `OpenFeature` 2.13.0 pinned + referenced (in-memory provider ships in the core SDK); README `[FEATURE_FLAGS]` group and `.api/api-openfeature.md` authored.
