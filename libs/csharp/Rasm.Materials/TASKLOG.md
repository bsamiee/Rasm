# [MATERIALS_TASKLOG]

The open and closed work for the host-neutral materials owner, distilled from `IDEAS.md`. Each task card leads with a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open, `[COMPLETE]`/`[DROPPED]` closed — and names the exact sub-domain and file it lands in. One idea spawns one or more tasks; a task is scoped guidance, not a full spec.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[JOINT_PAGE_AUTHOR]-[QUEUED]: Author the Connection/joint.md sibling page realizing the fourth ConnectionFamily case `joint` as the continuous-connection vocabulary — the JointKind weld/adhesive/stud discriminant, the JointSection ConnectionSection arm, and the BuildJointRows AWS D1.1 / structural-adhesive row table the one ConnectionCatalogue.Build folds.
- Capability: Realizes the JOINT_CONNECTION_FAMILY idea: the `JointKind` [SmartEnum] (weld/adhesive/stud), the `WeldType` (fillet/groove/plug/butt) / `AdhesiveClass` / `StudClass` axes carrying leg/throat/groove-angle/root-opening, bond-line/substrate/lap-shear, and diameter/height/spacing columns as PositiveMagnitude receipts, the `ConnectionSection.Joint` arm projecting the family columns and the NominalMm dispatch (throat for weld, bond-line for adhesive, stud-diameter for stud), and the `ConnectionCatalogue.BuildJointRows(context)` seed concatenated into ConnectionCatalogue.Build — one continuous-connection case, never a Weld/AdhesiveBond/ShearStud type.
- Shape: A new page Connection/joint.md mirroring the reinforcement/fastener/hanger sibling structure: the JointKind/WeldType/AdhesiveClass/StudClass vocabulary, the JointSection arm, and the BuildJointRows table, composing connection#CONNECTION_OWNER ConnectionItem/ConnectionId/ConnectionFault exactly as the three existing families do.
- Unlocks: A weld schedule, a bonded joint, and a shear-stud row admit through the same ConnectionItem.Of and key the same way a bolt does, so the connection owner spans the continuous-connection discipline over one polymorphic surface.
- Anchors: connection#CONNECTION_OWNER ConnectionItem.Of/ConnectionSection Union/ConnectionCatalogue.Build (connection.md lines 58-101); reinforcement.md/fastener.md/hanger.md the family-vocabulary-on-its-own-page pattern; AWS D1.1 weld/electrode tables, structural-adhesive lap-shear allowables, AWS D1.1 stud-welded shear-connector data; realizes the `JOINT_CONNECTION_FAMILY` idea.

[SUSTAINABILITY_PAGE_AUTHOR]-[QUEUED]: Author the Properties/sustainability.md sibling page realizing the Environmental, Cost, and Classification MaterialProperty cases and the lifecycle aggregation folds so a material carries its embodied-carbon, cost, and BIM-classification beside its mechanical/thermal/acoustic/fire properties.
- Capability: Realizes the SUSTAINABILITY_PROPERTY_DISCIPLINE idea: the `Environmental` case (EN 15978 cradle-to-gate GWP per declared unit over a UnitsNet Mass/MassConcentration quantity, the EPD declaration/validity, the per-module A1-A3/A4/A5/C lifecycle vector as a fixed-length ReadOnlyMemory<double> over a `LifecycleStage` band exactly as Acoustic carries its banded spectrum, recycled-content/end-of-life fractions), the `Cost` case (supply/install/lifecycle over a `Currency` band and measurement basis), the `Classification` case (Uniclass2015/OmniClass/Pset over a `ClassificationSystem` band), and the AssemblyAggregator.AggregateEnvironmental/AggregateCost folds summing per-ply GWP/cost the way the U-value/STC folds aggregate — one discipline by case and column, never a parallel Eco/Cost owner.
- Shape: A new page Properties/sustainability.md realizing the three MaterialProperty cases, the LifecycleStage/Currency/ClassificationSystem closed bands, the EPD/embodied-carbon receipt, and the lifecycle aggregation fold, composing properties#MATERIAL_PROPERTY MaterialProperty/MaterialPropertySet and properties#ASSEMBLY_PROPERTY AssemblyAggregator.
- Unlocks: A whole-building embodied-carbon takeoff and cost rollup compute from the same MaterialAssignment plies the thermal/acoustic envelope already folds, feeding the forward LCA/QTO consumer by MaterialId without a second property surface.
- Anchors: properties#MATERIAL_PROPERTY MaterialProperty Union + the banded-vector Acoustic carrier (properties.md lines 70-119); properties#ASSEMBLY_PROPERTY AssemblyAggregator fold (properties.md lines 220-279); UnitsNet Mass/MassConcentration; EN 15978/ISO 14040 LCA stages, Uniclass2015/OmniClass, IFC Pset_EnvironmentalImpactValues; realizes the `SUSTAINABILITY_PROPERTY_DISCIPLINE` idea.

[STEEL_COMPOSITE_AND_COLDFORMED]-[QUEUED]: Add the composite steel-concrete and cold-formed AISI S100 SteelClass arms so the steel family spans built-up/composite members and light-gauge framing, the SteelClass growth axis the steel page names but does not yet realize.
- Capability: Two new SteelClass cases — `Composite` (a steel-concrete section over the steel-shape reference, the concrete-slab effective width/depth, the shear-stud reference into the JOINT_CONNECTION_FAMILY StudClass vocabulary, and the AISC 360 Chapter I composite-flexural-capacity columns) and `ColdFormed` (an AISI S100 section over the gross/effective-width columns, the corner radius, the design thickness, and the effective-section-modulus the local-buckling reduction yields) — each mapping to its IfcProfileDef subtype, plus their catalogue rows, never a parallel composite/cold-formed owner.
- Shape: A capability addition on steel.md#STEEL_FAMILY: the SteelClass.Composite/SteelClass.ColdFormed cases with their IfcProfileDef subtype mapping, the composite effective-width/stud-reference columns and the AISI S100 effective-section-modulus projection, and the BuildSteelRows row additions, composing the JOINT_CONNECTION_FAMILY shear-stud for the composite interface.
- Unlocks: A composite floor beam and a cold-formed steel stud carry the same Profile shape and place through the same Resolve fold as a rolled W-shape, so the steel family covers the full structural-steel product range by SteelClass case and row data.
- Anchors: steel#STEEL_FAMILY SteelClass axis + the COMPOSITE_AND_BUILTUP named growth note (steel.md line 99); the JOINT_CONNECTION_FAMILY shear-stud StudClass; AISC 360 Chapter I, AISI S100 effective-width method, the IfcProfileDef arbitrary/I-shape mapping; realizes the `STEEL_DESIGN_OBJECT` idea.

[GENERATED_BOND_INTERPRETER_REALIZE]-[QUEUED]: Realize the masonry generated-bond interpreter so a flemish/herringbone/basketweave/pinwheel/diaper bond derives its course placement from a parametric BondGeometry descriptor rather than railing ProfileFault.Bond, closing the masonry page's last open probe.
- Capability: Realizes the GENERATED_BOND_INTERPRETER idea: the `BondGeometry` parametric descriptor (unit-cell repeat, per-course orientation pattern, diagonal/herringbone rotation, flemish alternating stretcher-header sequence), the flemish/herringbone/basketweave/pinwheel/diaper/stack generated BondName rows, and the `GeneratedBond.Interpret` course-deriving fold filling the Course arm so BondName.Course(index, key) computes a generated bond's course (and its per-unit rotation into Placement.PathAngleDegrees) rather than failing — one fold over a bounded bond-geometry vocabulary.
- Shape: A capability addition on masonry.md#PROFILE_FAMILY: the BondGeometry descriptor, the generated BondName rows replacing the stubs that rail today, and the GeneratedBond.Interpret fold that the layout#ASSEMBLY_FOLD StepCourse already consumes — the CourseTemplate shape and the Construction layout fold unchanged.
- Unlocks: Every decorative and structural bond places through the one Construction layout fold as a parametric Bond axis, so a new bond is a BondGeometry row and the masonry page carries the full professional bond vocabulary.
- Anchors: masonry#PROFILE_FAMILY BondName/BondKind/CourseTemplate + the BondKind.Generated rows that rail ProfileFault.Bond (masonry.md lines 3, 13); the GENERATED_BOND_INTERPRETER named probe; layout#ASSEMBLY_FOLD StepCourse + Placement.PathAngleDegrees; the flemish/herringbone/basketweave/pinwheel/diaper coursing geometry; realizes the `GENERATED_BOND_INTERPRETER` idea.

[MORTAR_JOINT_PROFILE]-[QUEUED]: Add a mortar-joint profile vocabulary to the masonry family so a masonry run carries its head/bed joint width AND depth AND tooled profile per ASTM C270 / EN 1996, replacing the single scalar StandardJointThicknessMm with the professional mortar-joint specification the layout fold and weathering/thermal seams read.
- Capability: A `MortarProfile` [SmartEnum] (concave/V/weathered/struck/raked/flush/beaded tooled-joint profiles per ASTM C270) carrying joint-depth and shadow-line columns, and a `MortarJoint` record (head-width, bed-width, profile, the ASTM C270 mortar type M/S/N/O/K or EN 998-2 class compressive-strength receipt) the JointPolicy resolves so the layout fold reads a full joint specification — the masonry Profile gaining a MortarJoint column the JointPolicy.Resolve reads beside the coordinating thickness, one joint owner, never a per-profile joint method.
- Shape: A capability addition on masonry.md#PROFILE_FAMILY: the MortarProfile SmartEnum and the MortarJoint record, plus the layout.md#ASSEMBLY_FOLD JointPolicy widening to resolve head/bed width from the MortarJoint rather than only the ProfileStandard scalar, the StationStep pitch reading the joint width unchanged.
- Unlocks: A masonry run specifies its joint profile and mortar class so the construction model carries the buildable joint detail, the weathering seam reads the raked-joint shadow line, and the thermal/structural seams read the ASTM C270 mortar-strength receipt.
- Anchors: masonry#PROFILE_FAMILY Coring/BondName/Orientation vocabulary (masonry.md lines 11-16); layout#ASSEMBLY_FOLD JointPolicy.Resolve head/bed resolution + the StationStep pitch = unitLength + headJoint (layout.md lines 12-17); ProfileStandard.StandardJointThicknessMm the scalar the MortarJoint replaces; ASTM C270 mortar types and tooled-joint profiles, EN 1996/EN 998-2 mortar classes.

[ARCH_DOME_VAULT_EXPANSION]-[QUEUED]: Expand the construction layout arch stage from the single voussoir fold over RunPath.Arc to the full professional masonry-arch vocabulary — segmental/semicircular/three-centre/ogee arch profiles plus the dome ring-course fold — so the arch/dome placement spans the architectural arch types as a parametric ArchProfile axis the one Resolve fold reads, realizing the page's voussoir-taper/keystone probe.
- Capability: An `ArchProfile` [SmartEnum] (segmental/semicircular/three-centre/ogee/equilateral/lancet) driving the voussoir radial pitch and springing/crown station geometry, a `RunPath.Dome` case (dome radius and ring-course count) the Voussoirs fold extends so a dome resolves as a stack of horizontal ring courses with per-ring voussoir taper, plus the keystone SpecialShape.Voussoir special-cut at the crown — each a column/case on the existing arch stage, never a parallel arch/dome layout owner.
- Shape: A capability addition on layout.md#ASSEMBLY_FOLD: the ArchProfile axis on the Voussoirs/ArchCourses fold, the RunPath.Dome case on assembly.md#ELEMENT_MODEL with its RunPathAlgebra.LengthOf/AngleAt arm, and the ring-course dome fold extending the realized Voussoirs station-normalized projection, the ConstructionFault.Course rail and the host-neutral scalar Placement unchanged.
- Unlocks: A segmental/ogee/three-centre arch and a masonry dome place through the one Construction layout fold as a parametric ArchProfile/RunPath axis, so the construction model spans the curved-masonry vocabulary professional software carries, and the keystone and per-voussoir taper read into the existing Placement columns.
- Anchors: layout#ASSEMBLY_FOLD Voussoirs/ArchCourses fold over RunPath.Arc + SpecialShape.Voussoir (layout.md lines 12, 17, the Voussoirs gated to masonry/solid profiles); assembly#ELEMENT_MODEL RunPath Union + RunPathAlgebra.LengthOf/AngleAt; the segmental/semicircular/three-centre/ogee arch springing-and-radius geometry, the dome ring-course voussoir-taper rules.

[BSDF_PAGE_SPLIT]-[QUEUED]: Split the 734-LOC bsdf.md into the lobe/microfacet shading-kernel half and a new lowering/grounding half so each page owns one cohesive concern under the per-page LOC pressure instead of one page carrying eight sections and three independent author-kernels.
- Capability: Separates the BsdfLobe Union + Microfacet kernel + LayeredBsdf fold (the per-sample shading math the path tracer drives) from the SpectralUpsample RGB->SPD kernel + ToneMap ACES table + ConductorIor measured table + SlabStack OpenPBR lowering (the color-science and OpenPBR-construction concern the wire and library drive), two pages sharing the MaterialFault band and MaterialKeyPolicy declared once on the kernel page and composed by the lowering page.
- Shape: bsdf.md retained as the shading kernel (sections 1-4: ShadingFrame/MaterialFault/MaterialKeyPolicy, Microfacet/FresnelMode, BsdfLobe/MultiScatter, LayeredBsdf) and a new surface.md carrying sections 5-8 (SpectralUpsample/SpectralBand, ToneMap/ToneOperator, ConductorIor/ConductorMetal, SlabStack/Slab/SlabKind/OpenPbrSurface), with the README router and ARCHITECTURE codemap adding the Surface.cs node and the interchange/weathering/graph cross-refs re-pointed to surface#OPENPBR_SLAB.
- Unlocks: Each page sits under the established per-page depth (the kernel page owns frame-local shading, the surface page owns the OpenPBR construction the MaterialWire and weathering trajectory target), and the openpbr-slab section gains room to deepen the layering operators the WEATHERING and INTERCHANGE seams reach into.
- Anchors: bsdf.md sections 1-4 the frame-local lobe kernel vs sections 5-8 the color/conductor/slab lowering (bsdf.md INDEX lines 7-14, verified 734 total lines); MaterialFault and MaterialKeyPolicy declared once at section 1 and composed by 5-8; weathering#OPENPBR_SLAB_AGING and interchange#MATERIAL_WIRE already cite the slab half as a distinct concern.

[WEATHERING_SLAB_DEEPEN]-[QUEUED]: Deepen the weathering.md from the flat MaterialParameters interpolation to the realized SlabStack-column aging the page's own RESEARCH defers, so chalking raises coat_roughness, soiling raises fuzz_weight, and patina swaps the base ConductorMetal as slab-column deltas over the realized openpbr-slab construction rather than a single base-color lerp.
- Capability: Lifts WeatheringTrajectory from a base-color/roughness/metalness/sheen lerp to a per-effect SlabColumnDelta trajectory targeting the bsdf#OPENPBR_SLAB SlabStack columns (coat/fuzz/base-conductor) so an aged row lowers through the SAME slab algebra a fresh row does, plus the procedural-soiling cavity-mask seam reading the AgeParameter per-shade-point from a texture#TEXTURE_UV ambient-occlusion sample so spatially-varying weathering rides the graph fold.
- Shape: An expansion of weathering.md section 2 and the Apply signature: WeatheringTrajectory gains the slab-column delta fields (CoatRoughnessTo/FuzzWeightTo/ConductorSwap), Weathering.Apply folds onto the SlabStack columns before the ToLayered collapse rather than the flat MaterialParameters, and the AgeParameter admits a per-shade-point cavity-mask source.
- Unlocks: A weathered material is the realized slab-column delta the renderer shades through one LayeredBsdf, the procedural-soiling mask drives crevice-accumulation grime without a second aging surface, and the trajectory aligns to the MaterialX aging-operator node-graph at interchange#MATERIALX_DOCUMENT.
- Anchors: weathering.md OPENPBR_SLAB_AGING and PROCEDURAL_SOILING_MASK both deferred to once a consumer drives the slab columns; bsdf#OPENPBR_SLAB SlabStack.Lower the realized slab columns (the same Slab.Coat finish.md lowers to); texture#TEXTURE_UV cavity/AO sample for the mask source; the WeatheringTrajectory record.

[THINKTECTURE_WIRE_CODEC]-[QUEUED]: Wire the centrally-pinned-but-unreferenced Thinktecture.Runtime.Extensions.Json and .MessagePack serializer packages into the Interchange MaterialWire/MtlxDocument projection so the SmartEnum/Union/ValueObject wire shapes serialize through the generated codecs instead of bespoke string projection, carrying the full csproj + README admission chain.
- Capability: Routes the MaterialWire/OpenPbrGroupsWire/WireProvenance/WireColor record shapes and the MtlxDocument node-graph through the Thinktecture generated System.Text.Json and MessagePack formatters so the MaterialId/ConductorMetal/NodeCategory SmartEnum keys, Union discriminants, and ValueObject keys serialize via the generated codec the wire round-trips through, replacing the hand-rolled CultureInfo.InvariantCulture ToString('R') string projection — the C# sole-producer mint reads one generated codec, the TS/Python peers decode the same shape. The MessagePack heavy-transitive branch scopes to the companion/outside-Rhino wire, the in-Rhino ALC staying STJ-only per the Speckle.Sdk firebreak doctrine.
- Shape: A capability addition on interchange.md#MATERIAL_WIRE: the MaterialProjection.Mint and Mtlx.FromGraph/ToOpenPbr projections compose the Thinktecture generated Json/MessagePack formatters for the wire shapes, plus the Rasm.Materials.csproj gaining the Thinktecture.Runtime.Extensions.Json/.MessagePack PackageReference and the README [03]-[SUBSTRATE_PACKAGES] registry gaining the two serializer companions beside the core Thinktecture pin.
- Unlocks: The OpenPBR wire and the .mtlx document serialize through one generated codec so the SmartEnum/Union/ValueObject wire shapes round-trip without a hand-maintained string projection, and the single-mint invariant holds with the codec as the canonical shape the peers decode.
- Anchors: interchange.md MaterialWire/OpenPbrGroupsWire/WireProvenance/WireColor record shape + the MaterialProjection.Mint and Mtlx.FromGraph/ToOpenPbr projections; Thinktecture.Runtime.Extensions.Json/.MessagePack 10.3.0 PINNED (Directory.Packages.props lines 12-13, VERIFIED present); the README [03] currently lists only core Thinktecture; the C#-sole-producer single-mint invariant (ARCHITECTURE line 39).

[MATERIALX_NODE_CATEGORY_MAPPING]-[QUEUED]: Complete the TextureSource-case-to-MaterialX-NodeCategory mapping against the MaterialX 1.39.4 standard-node library so each already-closed TextureSource/NoiseBasis case round-trips to its .mtlx node category, realizing the MATERIALX_NODE_PARITY probe — the union is already closed, the residual is the interchange category mapping, not a union enumeration.
- Capability: Completes the texture#TEXTURE_UV per-case NodeCategory projection the interchange#MATERIALX_DOCUMENT consumes: the closed TextureSource cases (noise/checker/gradient/image/triplanar) and the NoiseBasis (Perlin/Simplex/Worley) map to the MaterialX 1.39.4 image/tiledimage, worleynoise2d/3d, ramplr/ramptb, and the improved-Worley/color-ramp nodes, and the AddressMode (Repeat/Clamp/Mirror) / FilterMode bands map to the .mtlx node parameters — settling whether a value/cubic NoiseBasis fifth row is needed against the standard library or the three-basis set is complete, so a texture source round-trips to .mtlx by its NodeCategory.
- Shape: A capability addition on texture.md#TEXTURE_UV and interchange.md#MATERIALX_DOCUMENT: the TextureSource-case-to-NodeCategory table and the NoiseBasis-to-MaterialX-noise mapping completed against the MaterialX 1.39.4 standard nodes, the [MATERIALX_NODE_PARITY]/[MATERIALX_GRAPH_INTERCHANGE] probe advancing from probe to realized mapping, the FastNoiseLite kernel staying the documented LIBRARY_DEPTH NOT_COVERED vendored carve-out, the TextureUv sampling fold unchanged.
- Unlocks: A Texture graph node round-trips through .mtlx with every TextureSource case mapped to a real MaterialX category, confirming the closed-union count against the live MaterialX 1.39.4 standard library and settling the NoiseBasis completeness question.
- Anchors: texture#TEXTURE_UV the TextureSource closed union (noise/checker/gradient/image/triplanar, VERIFIED closed at 5) + NoiseBasis (Perlin/Simplex/Worley, VERIFIED closed at 3) + AddressMode/FilterMode bands + the MATERIALX_GRAPH_INTERCHANGE target (texture.md line 15); interchange#MATERIALX_DOCUMENT the .mtlx node-graph the NodeCategory maps to; the MaterialX 1.39.4 standard-library node categories; the FastNoiseLite vendored carve-out (LIBRARY_DEPTH NOT_COVERED).

[BRDF_FIT_COMPUTE_SEAM]-[BLOCKED]: Upgrade the acquisition MeasuredBrdf fit from the specular-contrast FitRoughness heuristic to a true overdetermined GGX/Smith least-squares solve over the angular reflectance design matrix — IN-FOLDER, because the strata graph forbids a Rasm.Compute reference; the QR/LM solver is admitted as a direct Materials NuGet pin or the leg defers behind the host-edge.
- Capability: The acquisition#ACQUISITION FitBrdf fit gains an overdetermined least-squares solve fitting the bsdf#MICROFACET_KERNEL GGX roughness + Smith masking + Fresnel-IOR parameter vector against the measured angular reflectance samples, the residual recomputed onto the Provenance.FitResidual receipt and gated through the bsdf#WHITE_FURNACE_HARNESS — replacing the specular-contrast FitRoughness heuristic with a witnessed parameter solve, the microfacet kernel the forward model. The strata graph is acyclic (app-platform consumes AEC-domain, never the reverse), so Materials CANNOT reference the app-platform Rasm.Compute solver and "MathNet transitive via Rasm.Compute" is impossible (transitivity flows from Compute's references to Compute's consumers, not the reverse); the algorithms#ROUTE_SPINE thin-QR is a DOCTRINE reference this card cites for the fit shape, and the solver is either a direct Materials MathNet.Numerics NuGet admission (the AEC-domain folder's own dependency, never a Compute edge) or the leg stays the realized in-folder FitRoughness heuristic until a host-free-peer fit lands over the wire.
- Shape: A capability addition on acquisition.md#CAPTURE_SOURCE: the MeasuredBrdf FitBrdf arm runs the overdetermined GGX/Smith/IOR solve over the BrdfSample angular matrix, the bsdf#MICROFACET_KERNEL evaluation the forward model, the residual feeding the existing Provenance receipt, the CaptureSource union and the import fold otherwise unchanged; the solver is an in-folder kernel or a direct Materials MathNet NuGet pin, NOT a Rasm.Compute project reference.
- Unlocks: A measured BRDF imports as a fitted OpenPBR parameter row the LayeredBsdf shades directly, the residual becomes real measured evidence distinguishing a fit material from an authored guess, so acquisition grounds a real-world capture in the closed lobe set through a true optimization the renderer reproduces.
- Anchors: acquisition#CAPTURE_SOURCE the MeasuredBrdf FitBrdf + the FitRoughness specular-contrast heuristic it replaces (acquisition.md lines 45-60) + the [BRDF_PARAMETER_FIT] probe naming the algorithms-doc QR route (line 81, 'never hand-rolls a Levenberg-Marquardt loop'); bsdf#MICROFACET_KERNEL the GGX/Smith/Fresnel forward model; csharp:Rasm.Compute/algorithms#ROUTE_SPINE the thin-QR DOCTRINE reference (the fit SHAPE only — not a Rasm.Compute project edge, which the acyclic strata forbids the AEC-domain folder); a direct Materials MathNet.Numerics NuGet admission if the in-folder solver lands, never a transitive Compute reference.
- Tension: The strata graph forbids the AEC-domain Materials a reference to the app-platform Rasm.Compute, so the fit cannot route through the Compute solver and cannot obtain MathNet "transitively via Compute" — the leg is BLOCKED on the choice between a direct Materials MathNet NuGet pin (the AEC folder's own dependency) and a deferred host-free-peer fit over the wire; until that choice lands, the realized FitRoughness heuristic stays.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[STEEL_LRFD_CAPACITY_AND_SHAPES]-[COMPLETE]: steel#STEEL_FAMILY grown to all six SteelClass cases (W/M/S/HP·C/MC·L·HSS-rect·HSS-round·WT) with the CompactnessClass 3-state verdict replacing IsCompact and the DesignCapacity LRFD projection (φMn/φPn/φVn over AISC 360 F/E/G) derived from the stored Zx/Sx/Ix/Area columns; 15-row SteelRows table carrying the SteelClass discriminant, admitted through SteelShapeOf.
[WEATHERING_COLOURMAP_RAMP]-[COMPLETE]: weathering#WEATHERING trajectory Terminal is now a Wacton.Unicolour.Datasets Colourmap ramp (Flare/Rocket/Mako/Vlag) the Age fold samples by the eased fraction via Terminal.Map(f), the four frozen Unicolour endpoints deleted; verified against api-unicolour-datasets.md Colourmaps registry + Map(double).
[MATERIALS_THERMAL_SCALAR_PEER_CONTRACT]-[COMPLETE]: properties#MATERIAL_PROPERTY states the THERMAL_SCALAR_PEER_EXPORT contract — Thermal.ConductivityWMK/SpecificHeatJKgK + Mechanical.DensityKgM3 as raw SI doubles, decoupled from the UnitsNet proxy, the stable Fabrication peer-read surface; Ripple: Rasm.Fabrication [TOOL_CUTTING_DATA_TABLE].
