# [PY_COMPUTE_IDEAS]

The forward pool of higher-order folder concepts grounded in the numeric-science domain and the monorepo purpose. `[1]-[OPEN]` carries the active ideas as cards; each card names the capability, what it unlocks, and the gap or modern technique it draws on. `[2]-[CLOSED]` carries the finished or dropped ideas with a one-line disposition so the same idea is never re-litigated. An idea drives one or more `TASKLOG.md` tasks.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[CERTIFIED_INTERVAL_PIPELINE]-[QUEUED]: a whole numeric program runs under the certified-enclosure floor ladder, not one expression at a time.
- Capability: a multi-step certified-numerics pipeline that threads `numerics/interval.md#ENCLOSURE` through a composed expression graph, propagating the `certified` flag and the Arb/mpmath/numpy floor verdict across every stage rather than re-certifying each node in isolation — the genuinely new capability the page's single-op `evaluate` entry does not yet carry.
- Shape: `IntervalNumerics` gains a left-fold entry over a `Sequence[IntervalOp]` beside the existing single-op `evaluate`, threading one carried `Enclosure` through the chain so the `certified` flag degrades taint-monotonically (a single uncertified numpy-floor stage taints the whole chain) and the width contracts under the bisection budget — so a Horner evaluation followed by a containment proof of a target root is one certified derivation, structurally mirroring the `analysis/symbolic.md#DERIVATION` `reduce`-based pipeline fold (staging ops then a terminal) and the convex dual-certificate posture, never a re-certify-per-node loop.
- Unlocks: certified root isolation, certified range bounding over a design box, and a verified-tolerance handoff where the C# gate admits a numeric result only when every pipeline stage stayed on the Arb or mpmath floor, the validated-numerics analogue of the convex dual certificate and the inference convergence bar.
- Tension: the pipeline is a fold over the existing `IntervalOp` union and the existing floor ladder, never a parallel rigorous-arithmetic surface; the certified flag is taint-monotone, so a single numpy-floor stage downgrades the chain to advisory evidence the C# gate refuses as a proof.

[SURROGATE_GRADIENT_LOOP]-[QUEUED]: the study spine closes an acquisition loop that proposes the next design from the fitted surrogate's gradient.
- Capability: an active-learning sequential-design loop layered on `experiments/study.md#STUDY` — the fitted surrogate (the `numpy.polynomial` floor or the scikit-learn estimator) plus its uncertainty estimate drive a `scipy.optimize`-evaluated acquisition that proposes the next `qmc`-anchored design batch, so the DOE spine learns where to sample rather than drawing one fixed grid.
- Shape: a `StudyMethod.Acquire(surrogate, acquisition, budget)` case rowing an `AcquisitionPolicy` (expected-improvement / lower-confidence-bound / max-variance) over the surrogate posterior, evaluated through `optimization/program.md#PROGRAM`'s bounded `scipy.optimize` route, refilling the next batch through the same `scipy.stats.qmc` engine the sampler cases use and folding the round's regret and acquired-point gain into the existing `StudyReceipt`.
- Unlocks: sample-efficient design exploration, surrogate-guided experiment campaigns, and warm-start seeding for the inverse-design loop — the offline-science layer above one-shot DOE.
- Anchors: `experiments/study.md#STUDY` (`StudyMethod`, the surrogate cases, `qmc` engines, `StudyReceipt`), `scipy` (`stats.qmc`, `optimize`), `scikit-learn` (the estimator protocol and its variance estimate), `experiments/history.md#RUN_HISTORY` (per-round resume), `solvers/sensitivity.md#SENSITIVITY` (the surrogate gradient where the estimator is differentiable).
- Tension: this is the sampled, surrogate-driven, derivative-free acquisition loop the `experiments/study.md#STUDY` charter explicitly defers as out-of-scope for one-shot DOE; it is a new `StudyMethod` case on the study spine, distinct from the gradient-driven PDE-constrained inverse design `optimization/design.md#DESIGN` owns over the implicit-adjoint solver gradient. Neither subsumes the other — the surrogate loop proposes designs in input space, the design loop descends the true objective's adjoint gradient.

[PROBABILISTIC_GRADUATION]-[QUEUED]: a posterior graduates as a probabilistic receipt carrying its credible bounds and predictive reliability, not a point estimate.
- Capability: the inference→graduation seam emits a probabilistic graduation receipt — the posterior's HDI credible interval, the rhat/ess convergence evidence, and the LOO predictive-reliability `pareto_k` ride the `uncertainty_law` handoff axis as a distribution-bearing claim the C# quantity owner reconstructs, rather than a scalar mean the receipt collapses to.
- Shape: `experiments/inference.md#BAYESIAN`'s `InferenceReceipt.graduates` projects the full `arviz.hdi` interval band, the per-variable mean/sd, and the predictive `elpd_loo`/`pareto_k_max` into a `HandoffAxis(uncertainty_law=...)` payload, so a graduated posterior crosses the wire as a credible-bounded uncertain quantity the C# `UncertainQuantity` consumer rehydrates, the probabilistic analogue of the certified-enclosure and dual-certificate handoffs.
- Unlocks: end-to-end uncertainty propagation across the wire — a downstream managed owner reads the credible band and the predictive-reliability flag rather than re-running inference, and the gate refuses a posterior whose `pareto_k_max` exceeds the bar as an unreliable importance-sampling claim.
- Anchors: `experiments/inference.md#BAYESIAN` (`InferenceReceipt`, `ConvergenceBar`, the arviz `summary`/`hdi`/`loo` diagnostics), `graduation/handoff.md#GRADUATION` (the `uncertainty_law` axis, `GraduationReceipt.graduates`), `numerics/quantity.md#QUANTITY` (the unit-bearing uncertain-quantity shape the band aligns to), runtime `ContentIdentity`.
- Tension: the credible band rides the existing `uncertainty_law` axis — no new handoff family; the receipt stays the typed `InferenceReceipt` carrying the distribution evidence, never a generic reported-value bag that erases the HDI/pareto_k fields. Production uncertainty propagation remains the C# quantity owner; compute emits the offline posterior evidence the wire carries.

[SYMBOLIC_CODEGEN_BRIDGE]-[QUEUED]: a derived symbolic expression compiles to a callable machine kernel, closing the analysis→native loop.
- Capability: the symbolic derivation's C-source terminal feeds a compiled-kernel bridge — the `analysis/symbolic.md#OP` `CCodegen` C99 source compiles through `numba.cfunc` to a callable native function pointer, so a closed-form expression derived symbolically becomes an executable kernel a solver evaluates at array speed rather than a source artifact only the C# gate consumes.
- Shape: a `numerics/jit.md`-owned codegen bridge case beside the existing `Njit`/`Vectorize`/`JaxJit`/`none` cases — a `Cfunc(signature)` `JitBackend` row taking the `SymbolicReceipt` C-source (or the lowered `cse` expression tree), compiling it through `numba.cfunc(sig)` to a native callable, and folding the compile evidence onto the existing `Jitted` carrier (kernel arity, the resolved signature, the address verdict) — the runtime-native sibling of the `Lambdify` callable lowering, distinct from it because `numba.cfunc` yields a function pointer a scipy low-level-callable quadrature or ODE right-hand side consumes, where `lambdify` yields a Python closure. The bridge folds onto the one `Jitted` evidence carrier, never a second compiled-kernel union beside `JitBackend`.
- Unlocks: symbolically-derived integrands and ODE right-hand sides compiled to native callbacks for inner-loop evaluation, closed-form Jacobians compiled native, and an analysis→solver fast path that never round-trips through the Python interpreter per evaluation.
- Anchors: `analysis/symbolic.md#OP` (`CCodegen`, `Lambdify`, the `cse` lowering, `SymbolicReceipt`), `numerics/jit.md#JIT` (`JitBackend`, the `Jitted` evidence carrier the `Cfunc` case folds onto), `numba` (`cfunc` — the C-callable function-pointer entry confirmed in `compute/.api/numba.md`), `solvers/quadrature.md` (the integrand the compiled kernel feeds), runtime (`RuntimeRail`, `boundary`).

[SPARSE_EIGENVALUE_ROUTE]-[QUEUED]: the large-sparse spectral problem gets a shift-invert and preconditioned route distinct from the in-pass eigen body.
- Capability: a dedicated large-sparse eigensolver route on `solvers/linear.md#LINEAR` — interior-eigenvalue shift-invert through the catalogued `scipy.sparse.linalg.eigsh(A, k, which, sigma)` `sigma`-shift entry, a matrix-free preconditioned generalized-symmetric solver, and a non-convergence partial-result recovery — folding the spectral residual and the Arnoldi/Lanczos restart evidence into the `SolverReceipt`, so a structural-modal or graph-Laplacian spectrum at scale is a typed spectral route rather than the dense `eigh` floor.
- Shape: the existing `Eigen` route's `SpectralMode` axis gains an interior-mode discriminant — `eigsh` with a `sigma` shift (the catalogued shift-invert parameter) and a shift-invert `OPinv` operator built from the existing `SparseScheme` factorization, a `lobpcg` matvec route over a `LinearMap` with an optional preconditioner `M` for the largest/smallest few generalized eigenpairs, and the Arnoldi non-convergence folded into `SolveStatus.MAX_STEPS` carrying the partially-converged eigenpairs rather than discarding them — every cell reading the one `LinearMap`/`MatrixStructure` operand the route already owns, the partial-result recovery mirroring the existing `_info_status`/`_ISTOP` typed-verdict fold rather than a raise.
- Unlocks: structural eigenfrequency extraction near a target shift, spectral graph partitioning over the Fiedler vector, buckling-mode and resonance analysis at FEM scale, and PCA-class dominant-subspace recovery without a dense decomposition.
- Anchors: `solvers/linear.md#LINEAR` (`Eigen`, `SpectralMode`, `LinearMap`, `MatrixStructure`, `SparseScheme`, the `_info_status`/`_ISTOP` non-convergence fold, `SolverReceipt`), `scipy` (`sparse.linalg.eigsh(A, k, which, sigma)` and `svds` — both catalogued in `compute/.api/scipy.md`; the `lobpcg` preconditioned solver, the `OPinv` shift-invert operator argument, and the `ArpackNoConvergence` partial-result exception are NOT yet catalogued and stay RESEARCH items on `solvers/linear.md#LINEAR` until a uv-sync reflection pass carries them), `numerics/array.md#PAYLOAD`, runtime.
- Tension: this is the eigensolver *route* — shift-invert, preconditioned LOBPCG, and partial-convergence recovery — distinct from the in-pass sparse-matrix construction the `array`/`solvers/linear` operand layer already owns; it is new `SpectralMode`/`Eigen` cells on the existing owner reading the existing operand, never a parallel eigen owner and never a re-built sparse assembler. The shift-invert `eigsh(sigma=)` entry is catalogued; the `lobpcg`/`OPinv`/`ArpackNoConvergence` spellings the route also needs are catalogue-unverified, so the task lands the `eigsh` shift-invert cell on the confirmed API and gates the LOBPCG/partial-recovery cells behind the catalogue verification it carries as RESEARCH.

[DIFFERENTIABLE_EVERYTHING_RAIL]-[QUEUED]: every solver receipt carries the adjoint witness, so a sensitivity threads through the whole solve stack uniformly.
- Capability: an end-to-end differentiability rail that threads `solvers/sensitivity.md#SENSITIVITY`'s implicit-adjoint witness through every owner that returns a `SolverReceipt` — linear, nonlinear, differential, quadrature/FEM, and the optimization siblings — so a `value_and_grad` over any composed solve reads the implicit-function-theorem adjoint at every stage and the receipt records the differentiability verdict (autodiff-exact vs finite-difference floor vs non-differentiable) as a first-class field on the convergence evidence.
- Shape: `solvers/receipt.md#RECEIPT`'s `SolverReceipt` and the optimization receipts gain a uniform `differentiable`/`adjoint_source` evidence field every route stamps — the Lineax/Optimistix/Diffrax routes recording autodiff-exact adjoint availability, the worker-absent routes recording the finite-difference floor — so the `solvers/sensitivity.md#SENSITIVITY` owner discovers the adjoint path off the receipt field rather than re-deriving it per call, and a composed pipeline of solves reports one differentiability provenance. The field is the new cross-route evidence contract; the adjoint mechanics themselves stay owned once by the sensitivity page's `[IMPLICIT_ADJOINT]`, never re-derived per receipt.
- Unlocks: PDE-constrained sensitivity through chained solves, gradient-checked solver verification (autodiff vs finite-difference agreement as a graduation signal), and a uniform differentiability contract every downstream optimizer and study reads off the receipt.
- Anchors: `solvers/sensitivity.md#SENSITIVITY` (the `Differentiation` AD owner, the implicit-adjoint loop, `DiffReceipt`), `solvers/receipt.md#RECEIPT` (`SolverReceipt`, `SolveStatus`), `solvers/linear.md#LINEAR`/`nonlinear.md`/`differential.md` (the autodifferentiable solves carrying the adjoint), `optimization/design.md#DESIGN`, `equinox`/`optimistix`/`lineax`/`diffrax`.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[MCERP_SOERP_REDTEAM]-[DROPPED]: second-order error-propagation backend decision resolved against `numerics/quantity.md`'s `uncertainties` first-order owner; no parallel mcerp/soerp Monte-Carlo propagation surface admitted.
