# [PLATFORM_TASKLOG]

The folder's open and closed work, distilled from `IDEAS.md`. Each task is a card whose leader carries a status marker — `[QUEUED]`/`[ACTIVE]`/`[BLOCKED]` open; `[COMPLETE]`/`[DROPPED]` closed — plus three to four bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks; each names the exact sub-domain/file it lands in.

## [1]-[OPEN]

## [2]-[CLOSED]

[COMPLETE] Socket-transport modality — `realtime/transport-modality.md` owns the closed `TransportModality` `Data.TaggedEnum` (WebSocket/WebTransport), the `dialModality` total `$match` fold yielding a per-connection `SocketDial` `{ inbound, send }` over the verified `BrowserSocket.layerWebSocketConstructor` (`Socket.makeWebSocket`/`socket.runRaw`/`socket.writer` for the WebSocket case, native `WebTransport` datagrams for the growth case), the one `SocketFault` `Data.TaggedEnum` (`DialFailed`/`Closed`/`WriteFailed`) folded through `dialFaultOf`/`Socket.SocketCloseError.is`, and the exported `socketReconnect` `StreamPolicy`. `realtime/socket-transport.md` owns the `SocketTransport` `Effect.Service` driving `dialModality` (`RuntimeConfig` `socketUrl`, `Effect.forkScoped` connection lifetime), draining `dial.inbound` into a bounded `Queue.sliding`, the `subscribe<A,I>(schema)` `Schema.decodeUnknown` frame fold, the live `dial.send` re-publish, the `ConnectionState` cell, and the session retry on `socketReconnect.reconnect`; it re-exports `SocketFault`. The folder `.api/effect-platform-browser.md` `[BrowserSocket]` carries the `layerWebSocket`-vs-`layerWebSocketConstructor` single-url-vs-dynamic-url modality law; a second ad-hoc `WebSocket` outside `transport-modality.md` is the named parallel-socket defect.

[COMPLETE] OIDC redirect-continuity leg in `identity-session/auth-session.md` — `AuthSession` deepened with the `arctic` mint-persist-redirect / extract-match-validate-decode round-trip: `generateState`/`generateCodeVerifier` mint, the `{ state, codeVerifier:Redacted, returnTo, nonce }` `PendingFlow` record persisted into the `local-persistence` `auth-flow` `StoreDomain`, `createAuthorizationURLWithPKCE` redirect, the callback `state` CSRF match, `validateAuthorizationCode`, `decodeIdToken` into `SessionStatus.Authenticated`, and `revokeToken` on logout. The flat `AuthFault` `reason` string union COLLAPSED into a closed `Data.TaggedEnum` (Denied/Expired/RefreshFailed/ProtocolError) where `OAuth2RequestError.code` folds to `ProtocolError`; the folder `.api/arctic.md` `[LOCAL_ADMISSION]` gained the redirect-continuity round-trip usage law. `codeVerifier` stays `Redacted`; cross-origin Bearer/DPoP and WebAuthn second-factor stay designed-only.

[COMPLETE] Local-store topology in `local-persistence/local-persistence.md` — `LocalPersistence` deepened with the `StoreDomain` `Schema.Literal` axis (`snapshot`/`offline-queue`/`auth-flow`/`viewpoint-cache`) mapped through one frozen `Record<StoreDomain, UseStore>` table to per-domain `idb-keyval` `createStore` named stores, the atomic `entries`-then-`clear` offline-queue drain, the `update` read-modify-write enqueue, and the single-entry `pendingFlow` record the OIDC chain consumes. The false `BrowserKeyValueStore`-backs-IndexedDB premise was resolved — the Schema-encoded `lastGood` snapshot rides a custom `KeyValueStore.make` adapter over the `snapshot` `UseStore` (the one sanctioned `idb-keyval` site), since `BrowserKeyValueStore` exposes only `layerLocalStorage`/`layerSessionStorage`. The folder `.api/idb-keyval.md` `[LOCAL_ADMISSION]` gained the named-store + atomic-drain + adapter law; a key-prefix in one flat store is the named flat-store defect.

[COMPLETE] W3C trace-context continuation in `observability/metric-registry.md#TRACE_PROPAGATION` — `MetricRegistry` gained the `tracePropagation` layer registering the `CompositePropagator([W3CTraceContextPropagator, W3CBaggagePropagator])` extract-and-continue propagator on a self-constructed `@opentelemetry/sdk-trace-web` `WebTracerProvider.register({propagator})` (path (a), in the current package set), since the verified `@effect/opentelemetry` `WebSdk.Configuration` carries NO propagator field and exposes no `.register` handle. The folder `.api/opentelemetry-core.md` CORRECTED the wrong `DefaultTextMapPropagator`/WebSDK-registration claim (lead + `[LOCAL_ADMISSION]` + `[RAIL_LAW]`), `opentelemetry-sdk-trace-web.md` annotated the self-constructed-provider-only `register({propagator})` reach, and `effect-opentelemetry.md` `[7]-[WEBSDK]` already shows the no-propagator-field shape. The `ParentBasedSampler` root row is unchanged; the inject leg stays settled at `interchange/transport/transport#WIRE_TRANSPORT`. The `@opentelemetry/core` manifest-row pin and the optional `@opentelemetry/api` global-path admission return to the orchestrator (see centralManifestNotes / residuals).

[COMPLETE] SSE parser hardening in `feature-flags/flag-stream.md` — the native `EventSource` ingress DROPPED for the raw `text/event-stream` body of an `HttpClient.execute` streaming request (`HttpClientResponse.stream` -> `Stream.decodeText` -> one `Sse.makeParser(onParse)` `Parser` fed each text chunk), since `EventSource` owns its own frame parsing and reconnect and so cannot host the admitted parser. The parser emits `Sse.AnyEvent` into a `Queue` the fold drains: an `Event` folds through the unchanged `FlagDelta` `Schema.decodeUnknown` + in-place `RemoteConfig.flags` `SubscriptionRef.update` patch, a `Retry` records its `lastEventId` into the reattach cursor `Ref` that seeds the `Last-Event-ID` request header on reconnect. Reconnect rides the reused `projection` `StreamPolicy` `interactive` reconnect `Schedule` (`policy.reconnect`). `platform` is now the second `Sse` consumer beside `services` `agent/mcp-transport#MCP_TRANSPORT` — the catalogue promotion `services/.api/effect-experimental.md` -> branch `libs/typescript/.api/` returns to the orchestrator (see centralManifestNotes / residuals).

[COMPLETE] Browser-capability owner — `capabilities/browser-capability.md` owns the new `capabilities/` sub-domain and the `BrowserCapabilityLive` `Effect.Service`: the closed `CapabilityKind` `Schema.Literal` axis, the per-kind `PermissionState` `Data.TaggedEnum` in ONE `Record<CapabilityKind, SubscriptionRef<PermissionState>>` cell table, the `query` read-seed-and-`change`-fork plus the `request` ceremony `Match.exhaustive` dispatch over the verified `Permissions`/`Clipboard`/`Geolocation` capsules and the native `navigator.storage.persist`/`estimate`/`Notification.requestPermission` calls, the `retained`/`storageRetained` projections, and the `PermissionStatus.change` ingress over `scopedEventStream`. The folder `.api/effect-platform-browser.md` gained the `StorageManager.persist`/`estimate` + `PermissionStatus.change` EventTarget shapes and the ungated-native-call admission law. The `capability-rank.md` fifth-health-input wire (`HealthInputs.storageRetained`/`candidateRank` arm/`inputs` `Effect.all`/`Stream.merge` source) is the same-unit edit to that peer page.

[COMPLETE] Page-lifecycle spine in `runtime-composition/app-lifecycle.md` — the closed `Phase` `Data.TaggedEnum` over the merged `visibilitychange`/`pagehide`(`persisted`)/`beforeunload` `Stream.merge` fold in one `SubscriptionRef`, the `transitions` edge projection, the `LifecycleSignal` arm fold; the three private `visibilitychange` ingresses retired.

[COMPLETE] Capability-rank cell in `runtime-composition/capability-rank.md` — the ordered `Rank` `as const satisfies Record` vocabulary, the four-input `Order.max` worst-of candidate fold, the escalate-fast/recover-slow `advance` hysteresis, and the `retains` capability query; PROJECTS capability, holds no behavior.

[COMPLETE] Generic scoped-event-stream bridge in `runtime-composition/scoped-event-stream.md` — one `scopedEventStream` `Stream.asyncScoped` over the `Effect.acquireRelease` listener pair, the single non-`WindowEventMap` ingress the navigate/`Workbox`/`PerformanceObserver`/`MutationObserver`/`PermissionStatus` sources compose.

[COMPLETE] Connectivity owner in `connectivity/connectivity.md` — the one `online` `SubscriptionRef` merged fold, the `redials` rising-edge projection, and the native `SyncManager` `rasm-offline-drain` wake; four concerns read the one cell, no per-component `navigator.onLine` probe.

[COMPLETE] Decode pool relocated to `worker/decode-pool.md` — the transferable `Worker.makePoolSerialized` `DecodeRequest` family, the `DecodeResidency` case, and the worker-interior `hash-wasm` `createXXHash128` single content-key mint; `build-pipeline.md` is build-time-only, the spawner repointed to `../worker/decode.worker.ts`. Residency-wire shape stays RESEARCH-gated on `ONE_CONTENT_IDENTITY` + C# `SPLAT_REALITY_CAPTURE`.

[COMPLETE] Boot entry + host-owner wiring in `runtime-composition/composition-root.md` — `BrowserRuntime.runMain` replaced `runtime.runFork`; `AppLifecycle`/`CapabilityRank`/`Connectivity`/`DecodeWorkerPool`/`SessionRecorder` merged as platform-bound HOST owner bands; the closed five-app-service budget holds. The boot arms `connectivity.registerNativeSync` and the flag-gated session recorder.

[COMPLETE] HTTP-claim repair in `runtime-composition/browser-platform.md` — `FetchHttpClient.layer` serves `RemoteConfig` config fetch and OTLP exporters, never `interchange` (connect-web transport); spawner repointed.

[COMPLETE] OTLP exporters + trace Sampler in `observability/metric-registry.md` — `WebSdk.layer` over the required `resource`, `BatchSpanProcessor`/`OTLPTraceExporter`, `PeriodicExportingMetricReader`/`OTLPMetricExporter`, and the one `tracerConfig.sampler` `ParentBasedSampler`(`TraceIdRatioBasedSampler`) row reading the `RuntimeConfig` collector endpoint. The `session.replay` span added to the closed `spanVocabulary`. RE-OPENED for trace-context propagation — see `[1]-[OPEN]` W3C trace-context continuation.

[COMPLETE] Flag-stream promoted to REAL in `feature-flags/flag-stream.md` — the SSE flag-delta channel folds through `FlagDelta` `Schema.decodeUnknown` patching the one `RemoteConfig` `FlagSet` cell in place; the `remote-config.md` poll demoted to the backfill fallback under one `Schedule`. SUPERSEDED by the SSE-parser-hardening leg (the raw `HttpClient` SSE-body ingress over `Sse.makeParser`, the native `EventSource` dropped) — see the SSE-parser-hardening card.

[COMPLETE] Router re-founded on the Navigation API in `routing/app-router.md` — the scoped `navigate`-event interception over `window.navigation`, `event.intercept` `scroll: "after-transition"`, and the `NavigateEvent` `signal` pending/abort state replaced the `popstate` cell + manual history mutation + scroll-offset map; `AppRouter` Tag + `AppRouterLive` Layer exported; ZERO router package.

[COMPLETE] View-transition fold in `routing/app-router.md#VIEW_TRANSITION` — the `viewTransition` `Effect.acquireRelease` over `document.startViewTransition` (commit in the update callback, `ready`/`updateCallbackDone`/`finished` via `Effect.tryPromise`, `skipTransition()` on scope exit) gated by one `matchMedia` reduced-motion row; wired into the intercept handler; ZERO animation dependency.

[COMPLETE] Soft-nav web-vitals reset + flush in `web-vitals/performance-budget.md` — the CLS/INP accumulators and `(vital, route)` dedupe hoisted to service-level `Ref`s, reset on the `AppRouter` `location.changes` commit, flushed terminal on the `AppLifecycle` `transitions` `Hidden`/`Draining` edge with a final breach check; the breach span carries the `SessionRecorder` `replayWindow` correlation.

[COMPLETE] Session-replay recorder authored at `session-replay/session-recorder.md` — the flag-gated (`RemoteConfig` `session-replay`) sampled (`RuntimeConfig.replaySampleRate`) `MutationObserver`/interaction recorder over `scopedEventStream`, the redaction-at-capture `redactEvent` fold, the bounded `Ref<Chunk<RecordedEvent>>` ring, the `session.replay` flush span, and the `windowId` cell `crash.report`/`web.vital.breach` annotate; `FlagEvaluation` exported, `replaySampleRate` added to `RuntimeConfig`, `session-replay` flag added to `FlagKey`. No vendor SDK, no third telemetry path.

[COMPLETE] Live acceptance signals carried on `offline-cache`, `fault-capture`, and `web-vitals` — the SW install/activate/`skipWaiting` + redial-drain convergence on `offline-cache`, the uncaught-`throw`/rejected-`Promise` round-trip into the one `crash.report` span (now `replayWindow`-correlated, recorder-flushed) on `fault-capture`, and the native `PerformanceObserver` LCP/INP/CLS/TTFB/FCP attribution with the 40ms INP floor and soft-nav reset on `web-vitals`.
