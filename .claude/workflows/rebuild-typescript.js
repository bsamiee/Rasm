export const meta = {
  name: 'rebuild-typescript',
  description: 'Ground-up rebuild of libs/typescript design pages to TRULY ultra-advanced TypeScript (Effect-TS rails, Schema-first boundaries, branded/nominal types, exhaustive discriminated unions, zero any/throw/enum) per docs/stacks/typescript/ + coding-ts, AND justified IN-PLACE capability extension: a per-folder Survey first maps the shared owners (cross-file unification) and the per-page capability gaps (the cases/fields/members a domain-complete owner must carry but the page omits, sourced from the admitted package surface, the real domain concept, or a consumer contract), then each page is rebuilt+critiqued+redteamed to collapse, stack both .api tiers, AND close those gaps by growing the existing owner in place. Structural collapse and capability completeness are orthogonal: a fully-collapsed page can still be capability-sparse. args = optional area scope (e.g. "ui"); empty/"ALL" = all of libs/typescript.',
  phases: [
    { title: 'Discover', detail: 'list every design page under the target (recursive .planning specs)' },
    { title: 'Survey', detail: 'per area: read ALL its pages + ARCHITECTURE/README + both .api tiers + docs/stacks/typescript -> map shared owners (cross-file unification) + per-page JUSTIFIED capability gaps to extend in place; assess capability COVERAGE vs the domain (not byte-count)' },
    { title: 'Rebuild', detail: 'per page (survey-informed): rebuild(max) -> critique(xhigh) -> adversarial redteam(max) + justified-extension, pooled at CAP=14' },
  ],
}

const DISCOVERY_SCHEMA = { type: 'object', additionalProperties: false, required: ['pages'], properties: { pages: { type: 'array', items: { type: 'string' } } } }
const SURVEY_SCHEMA = { type: 'object', additionalProperties: false, required: ['folder', 'pages'], properties: {
  folder: { type: 'string' },
  shared_owners: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['owner', 'home_page', 'spans'], properties: { owner: { type: 'string' }, home_page: { type: 'string' }, spans: { type: 'array', items: { type: 'string' } }, note: { type: 'string' } } } },
  pages: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['page', 'coverage', 'gaps'], properties: { page: { type: 'string' }, coverage: { type: 'string', enum: ['sparse', 'partial', 'complete'] }, size: { type: 'string', enum: ['small', 'medium', 'large'] }, gaps: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['capability', 'source', 'lands_as'], properties: { capability: { type: 'string' }, source: { type: 'string', enum: ['package', 'domain', 'consumer'] }, cite: { type: 'string' }, lands_as: { type: 'string' } } } } } } },
} }
const FIXLOG_SCHEMA = { type: 'object', additionalProperties: false, required: ['file', 'verdict', 'summary'], properties: { file: { type: 'string' }, verdict: { type: 'string', enum: ['rebuilt', 'refined', 'clean'] }, collapsed: { type: 'string' }, extended: { type: 'string' }, residual_high: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['files', 'claim'], properties: { files: { type: 'array', items: { type: 'string' } }, claim: { type: 'string' } } } }, summary: { type: 'string' } } }

// --- [HARNESS] -- bounded worker pool: steady <=cap concurrent, no burst ----------------
const STAGGER_MS = 1500
const pool = async (items, cap, worker) => {
  const out = new Array(items.length)
  let next = 0
  const run = async (slot) => {
    if (slot) await new Promise((res) => setTimeout(res, slot * STAGGER_MS))
    while (next < items.length) { const i = next++; out[i] = await worker(items[i], i) }
  }
  await Promise.all(Array.from({ length: Math.min(cap, items.length) }, (_, slot) => run(slot)))
  return out
}
const CAP = 14

// --- [INPUT] -- args = optional scope under the language root (area name or sub-path; empty/"ALL" = whole root) ---
const input = typeof args === 'string' ? (() => { try { return JSON.parse(args) } catch { return args } })() : args
const ROOT = 'libs/typescript'
const SHARED_API = 'libs/typescript/.api'
const rawScope = (typeof input === 'string') ? input.trim() : (input && typeof input === 'object' && input.target) ? String(input.target).trim() : ''
const SCOPE = (!rawScope || rawScope === 'ALL') ? '' : rawScope
const SWEEP = !SCOPE ? ROOT : (SCOPE === ROOT || SCOPE.indexOf(ROOT + '/') === 0) ? SCOPE : ROOT + '/' + SCOPE
const folderOf = (p) => { const head = p.split('/.planning/')[0].split('/'); return head[head.length - 1] || 'root' }
const folderPathOf = (p) => p.split('/.planning/')[0]
const subOf = (p) => p.split('/.planning/').pop()

const LAW = [
  'Rasm monorepo, libs/typescript planning corpus (markdown specs of intended TypeScript module designs). The current TypeScript code quality is POOR; this is a TRUE modernization to ultra-advanced TS, not a polish pass — discard naive idioms wholesale. CLAUDE.md manifest law governs. DENSITY BAR + STANDARD: docs/stacks/typescript/ (README, language, shapes, surfaces-and-dispatch, rails-and-effects, boundaries, system-apis) and the coding-ts standard. Cite only real members of admitted packages — MINE BOTH catalog tiers: the shared/universal `libs/typescript/.api/*.md` (effect, effect-platform, effect-opentelemetry, effect-atom, react, react-dom, clsx) AND the area-specific `libs/typescript/<area>/.api/*.md`, cross-checked against the published types in node_modules. Maximize the shared/universal Effect/Schema/React rails wherever relevant, never only the area set.',
  'This is a FUNDAMENTAL GROUND-UP REBUILD of a planning-stage DESIGN PAGE. Stop anchoring to naive/surface/junior JavaScript-in-TypeScript. Improve the page objectively: collapse interfaces/types into unions, stack the Effect ecosystem, AND close the concept capability gaps.',
  'WRITE-FULLY MANDATE: every fix you identify you MUST make NOW via Edit/Write directly in the file — the structured fix-log you return is a REPORT of edits ALREADY MADE, never a to-do list, a ledger, or a would/should-fix hedge; leave nothing behind except genuine cross-FILE items (report those in residual_high). If after real investigation the file is already correct, return verdict=clean — never invent edits to look busy.',
].join('\n')
const ULTRA = [
  'ULTRA-ADVANCED REBUILD MANDATE: COLLAPSE >=3 parallel interfaces/types/classes modelling one concept into ONE polymorphic surface (tagged discriminated union + exhaustive match), never parallel names. AOP: cross-cutting concerns (retry, telemetry, validation, caching, receipts, fault rails) as Effect combinators/layers/decorators, not repeated inline. UNIFIED rails + UNIFIED pipelines + feature-arms-as-cases (never loose separate). Parameterize inputs AND outputs with generics at depth; no stringy/weak typing.',
  'STACK CAPABILITY: FIRST inventory the COMPLETE catalog set — BOTH the shared/universal `libs/typescript/.api/*.md` AND the area-specific `libs/typescript/<area>/.api/*.md` — then compose the admitted libraries into single dense rails. There is NO fixed library count: weave EVERY relevant library, ALWAYS layering the shared/universal Effect ecosystem (`Effect`/`Layer`/`Context`/`Schema`/`Stream`) end-to-end ON TOP OF the area-specific packages, NOT naive `Promise`/`try`-`catch` glue. Maximize the shared/universal rails, never only the area set. Use the MOST powerful combinators; reject surface-level single-feature uses.',
  'PRESERVE all intended capability (densify, never delete functionality). Where a page is already strong, refine; where it is flat/naive, rebuild ground-up. Never regress correctness or boundary law.',
].join('\n')
const EXTEND = [
  'CAPABILITY EXTENSION (justified, in-place, never flat spam) — collapsing to tagged unions + Effect-stacking is NECESSARY but NOT SUFFICIENT. A page can be fully collapsed into one polymorphic surface and STILL be capability-thin: modeling a NAIVE, LIMITED slice of its domain concept — a flat id/member set where the concept owns geometry, metrics, attributes, topology, and operations; a 2-case discriminated union where the domain has twenty; an interface with the obvious 3 fields where the concept carries fifteen. Structural completeness and CAPABILITY completeness are ORTHOGONAL. A FULL rebuild ALSO closes the capability gap so the page OWNS ITS DOMAIN CONCEPT COMPLETELY. Capability grows sublinearly: every real missing concern lands as a CASE in the existing tagged discriminated union, a FIELD on the existing `Schema`/`Struct`/branded record, a member on the existing `Effect.Service`, a ROW in the existing `const`-union/table, or a POLICY value on the existing vocabulary — reshaping the owner as if it had always carried it; NEVER a parallel interface/type, a new file, a sibling shape, or flat appended code.',
  'GAP SOURCES (every extension MUST cite exactly one — justified, never speculative): (a) PACKAGE — a member the admitted package surface exposes that the concept ADMITS but the page IGNORES is a missing case in the owner law (BOTH tiers: the shared `libs/typescript/.api/` Effect/Schema/React rails AND the area domain packages, cross-checked against the published node_modules types; stacking that full surface IS new functionality woven into the owner, not naive Promise/try-catch glue). (b) DOMAIN — an attribute, metric, sub-kind, relationship, state, or operation the REAL concept demands but the page omits (a chart owns scale/axis/series/interaction/annotation families and zoom/brush/tooltip/legend operations, not two naive renders; a service owns retry/telemetry/validation/cache layers, not a bare fetch; a projection owns the full transform/diff/patch family the domain needs). (c) CONSUMER — a contract a sibling or downstream owner will require that has no composed spelling here yet (a need with no spelling marks a missing case: the law extends first, the feature lands second).',
  'COVERAGE OVER SIZE: byte-count is a WEAK proxy — capability COVERAGE against the full domain + both-tier package surface is the real measure. A SMALL page modeling a rich concept is almost always under-built (give it the DEEPEST sweep), AND a LARGE, well-collapsed page can still be capability-SPARSE (an owner that renders membership but models none of the concept scale/interaction/annotation/analytics). Assess each owner against its domain independently of size and EXTEND every owner the concept under-realizes IN PLACE — integrated and unified into the one owner at full Effect/Schema depth, every new field/case/member composing the existing rails — never a new flat surface beside it.',
  'JUSTIFIED, NOT RANDOM: if after a real domain + package + consumer sweep the concept is genuinely complete, prove it by adding nothing — never invent capability to look busy or pad with flat fields. Every added case/field/member/row is load-bearing, cites a package member / domain attribute / consumer contract, and composes the existing Effect/Schema rails; preserve ALL existing capability — extension only deepens, never regresses.',
].join('\n')
const PATLAW = [
  'TS PATTERN LAW (ultra-advanced ONLY; do not preserve the naive idioms of the existing code): ZERO `any`, zero implicit `any`, zero unsafe `as`, zero non-null `!`; model with branded/nominal types, exact discriminated unions with EXHAUSTIVE handling (`assertNever` on the default), `readonly`/`as const`, template-literal types, conditional/mapped types, and the `satisfies` operator. NO runtime `enum` — use `const` unions or `Schema`/Effect.',
  'Domain logic runs on typed-error rails — `Effect`/`Either`/`Option`, NEVER `throw` in domain code; boundaries validate through `Schema` (parse, never trust input). `import type`/`export type` are explicit; side-effect/value imports preserve runtime order. Per the docs/stacks/typescript file-organization overlay: `Effect.Service` owners are SERVICES, `Layer`/runtime wiring is COMPOSITION, runtime schemas/classes are MODELS, and catalog/registry rows stay after the owners they reference.',
  'Keep conventions IDENTICAL across every area so the corpus reads as one ultra-advanced codebase. One canonical semantic name per bounded concept; discriminate on input shape rather than proliferating `get`/`getMany`/`getById` names.',
].join('\n')
const BOUNDARIES = 'BOUNDARY LAW: keep every area owner strictly in its lane; internal code uses canonical names and shapes with mapping (and `Schema` validation) only at the edge; do not trample a sibling owner while densifying; respect the dependency direction of the workspace.'
const PROSE = [
  'PROSE QUALITY — apply docs/standards/style-guide.md. The page is a design SPEC: high-signal prose ONLY. Lead each section with the controlling rule/contract; one idea per paragraph; close on the consequence or boundary. Cut noise: no provenance, process narration, freshness disclaimers, report framing, or empty hedges (may/might/probably/generally/where possible). Trim walls of explanation to the load-bearing contract, and prefer a table, a typed signature block, or a tight bullet wherever it carries the design better than a paragraph.',
  'BACKTICK ALL CODE: wrap every symbol, type, field, function, operator, package ID, path, command, flag, and literal value in backticks. Name the exact member/type/rail in backticks instead of paraphrasing behavior. Trimming prose MUST NOT reduce technical density or remove design content.',
].join('\n')
const COMMENTS = 'COMMENT HYGIENE: code fences are agent-facing — comment for the next agent, never as a tutorial. KEEP the canonical section-divider headers (language-comment marker + space + `---` + bracketed `[UPPERCASE_LABEL]` + dash-fill). Beyond dividers, comment ONLY where intent is not already obvious from names, types, and signatures: default to ZERO comments on self-evident code; at most 1 line where a comment genuinely earns its place; 1-2 lines only for a truly subtle invariant, contract, or boundary. NO restating the code, no narration, no task/process/session/history/proof/review comments, no TSDoc bloat. Densify names and types so comments are rarely needed; cut every low-value comment.'

// --- [OPERATIONS] -- prompt builders: survey (area map), author (rebuild), critique, redteam ----
const svyBlock = (svy) => (svy && ((svy.gaps && svy.gaps.length) || (svy.shared_owners && svy.shared_owners.length))) ? '\nSURVEY MAP for this page (capability COVERAGE = `' + (svy.coverage || 'unknown') + '`, size = `' + (svy.size || 'unknown') + '`; compose the shared owners as ONE body, and CLOSE these justified capability gaps by extending the owner IN PLACE — a case/field/member on the EXISTING owner, never a parallel surface or a new file; `sparse` coverage means an under-built concept that earns the deepest domain + package sweep regardless of page size):\n' + JSON.stringify({ shared_owners: svy.shared_owners || [], gaps: svy.gaps || [] }, null, 1) : ''

const surveyPrompt = (folderPath, pages) => [LAW, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '',
  'TASK: CAPABILITY SURVEY of the area `' + folderPath + '` (its design pages: ' + JSON.stringify(pages) + '). Read EVERY page in the area, its `ARCHITECTURE.md` + `README.md`, BOTH .api tiers (the shared `' + SHARED_API + '/*.md` AND the area `' + folderPath + '/.api/*.md`) cross-checked against the published node_modules types, and the docs/stacks/typescript/ pages + coding-ts. Produce an area-level map that makes the per-page rebuild both UNIFIED and JUSTIFIED-EXTENDED, NOT random — this is a READ-ONLY map, edit nothing:',
  '(1) shared_owners — the shared logic/capabilities that span >1 page and their canonical home, so each per-file rebuild COMPOSES the ONE owner instead of re-deriving it: name the owner, its home page, and the pages that must compose it.',
  '(2) pages — for EACH page: a capability COVERAGE rating (`sparse`/`partial`/`complete` = how completely the page realizes its domain concept against the FULL domain + both-tier admitted-package surface, INDEPENDENT of byte-count — a large well-collapsed page can be `sparse` if it models a thin slice of a rich concept, e.g. a chart owner with two naive renders carrying none of the concept scale/axis/series/interaction/annotation families), a `size` note (`small`/`medium`/`large`), and the JUSTIFIED capability gaps — the cases/fields/members the concept owner MUST carry but the page omits, EACH with its `source` (`package` = a member the admitted surface exposes that the concept admits but the page ignores / `domain` = an attribute, metric, sub-kind, relationship, or operation the real concept demands / `consumer` = a contract a sibling or downstream owner will require), a `cite` (the exact package member, domain concern, or consumer page), and `lands_as` (the case/field/member on which EXISTING owner it extends — never a parallel surface). Hunt HARDEST on `sparse`-coverage owners and small files; if a concept is genuinely complete, list no gap for it. Return the structured survey.'].join('\n')

const authorPrompt = (page, svy) => [LAW, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', PROSE, '', COMMENTS, '', 'TASK: ground-up REBUILD of ' + page + ' to the ULTRA-ADVANCED bar AND to domain-complete capability. Read the page, its sibling pages (for cross-page unification), the docs/stacks/typescript standards + coding-ts, and the `.api/` catalogs of BOTH tiers — the shared `libs/typescript/.api/` universal set AND the area `libs/typescript/<area>/.api/` set, not only the catalogs the page already cites — cross-checked against the published types of the libraries it composes (ADD any shared/universal Effect/Schema rail the page under-uses, and STACK them all). Collapse shapes into tagged unions + exhaustive match, STACK Effect capability into unified rails, apply AOP via combinators/layers, parameterize I/O with generics, branded types and Schema boundaries. BEYOND structural collapse + `.api` maximization, CLOSE THE CONCEPT CAPABILITY GAPS so the page OWNS ITS DOMAIN CONCEPT COMPLETELY: extend the EXISTING owner in place with the missing cases/fields/members (the survey gaps below plus any further justified gap you find), each grown as a case on the union / a field on the `Schema`/branded record / a member on the `Effect.Service`, citing a package member / domain attribute / consumer contract — never a parallel interface/type, a new file, or flat spam; a `sparse`-coverage owner gets the deepest domain + package sweep. Write high-signal prose with all code backticked. Fix-in-place (read-then-extend, preserve capability).' + svyBlock(svy) + '\nReport what you collapsed (count before->after) in `collapsed` and what capability you extended (each addition + its cited source) in `extended`. Return the fix-log + residual_high — each a {files: [every repo-relative path the cross-file fix spans], claim} object for any CROSS-FILE item you surface but cannot fix from this one file (NO severity; the reconcile phase fixes all of them).'].join('\n')
const critiquePrompt = (page, svy) => [LAW, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', PROSE, '', COMMENTS, '', 'TASK: CONSTRUCTIVE CRITIQUE + CAPABILITY-COMPLETENESS + FIX IN PLACE of ' + page + '. Is this TRULY ultra-advanced TS, or naive code in disguise? Push further: deeper union collapse + exhaustive match, more Effect-stacking (are BOTH the shared `libs/typescript/.api/` rails AND the area catalogs maximized — Layer/Schema/Stream woven, not a thin area-only subset or Promise glue?), branded types where primitives are overloaded, richer generic parameterization. CAPABILITY-COMPLETENESS (structural collapse and capability completeness are ORTHOGONAL — a fully-collapsed owner can still model a NAIVE, LIMITED slice of its concept): verify EVERY survey gap for this page is genuinely CLOSED by an IN-PLACE extension (a case/field/member on the EXISTING owner, never a parallel surface or flat appended code); any unaddressed gap, any extension bolted on as flat spam, OR any further capability the both-tier admitted-package surface / the real domain concept / a consumer contract admits that the owner still omits is a DEFECT — close it in place now, citing its source; conversely a speculative/padding field with no justification is deleted. ENFORCE the TS pattern law (zero `any`/`throw`/`enum`, Schema boundaries, `import type`) and cross-area consistency per coding-ts + docs/stacks/typescript. ENFORCE prose + comment hygiene. EDIT the page. Report what you extended in `extended`.' + svyBlock(svy) + '\nReturn fix-log + residual_high — each a {files: [every repo-relative path the cross-file fix spans], claim} object for any CROSS-FILE item you cannot fix from this one file (NO severity; the reconcile phase fixes all of them).'].join('\n')
const redteamPrompt = (page, svy) => [LAW, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', PROSE, '', COMMENTS, '', 'TASK: ADVERSARIAL RED-TEAM + FIX IN PLACE of ' + page + '. You are a HOSTILE principal reviewer whose explicit goal is to REJECT this design as naive TypeScript. Assume it is junior, under-typed, or wrong until the page proves otherwise; the burden of proof is on the design. Read the docs/stacks/typescript standards + coding-ts and the published library types, then ATTACK relentlessly: `any`/implicit-any/unsafe `as`/non-null `!`; `throw` in domain logic instead of `Effect`/`Either` typed errors; non-exhaustive union handling (no `assertNever`); runtime `enum` misuse; missing branded/nominal types where primitives are overloaded; loose `import` where `import type` is required; mutable state where `readonly` fits; naive `Promise`/`async` glue where `Effect`/`Layer` belongs; using only a thin area-only subset while ignoring the shared/universal `libs/typescript/.api/` rails (effect/effect-platform/Schema/Layer/Stream) that should layer onto area packages; unvalidated boundary (no `Schema` parse); >=3 parallel interfaces/types NOT collapsed into a tagged union; structural drift from coding-ts or the file-organization overlay; prose bloat and un-backticked code. ALSO — CAPABILITY-COMPLETENESS + JUSTIFIED EXTENSION (counterfactually attack the owner for DOMAIN-COMPLETENESS independently of how collapsed it is): does the both-tier admitted-package surface, the real-world concept, or a consumer contract admit a capability this owner still OMITS (a flat membership/id set where the concept owns geometry/metrics/attributes/topology/operations; a 2-case union where the domain has twenty; the obvious 3 fields where the concept carries fifteen)? Name it with a cite and EXTEND THE OWNER IN PLACE (a case/field/member) — a structurally-perfect but capability-sparse owner is a DEFECT, not a finished page; the survey map below is the floor, not the ceiling; conversely reject any flat-spam/speculative/parallel-surface extension. For EVERY weakness, state the concrete failure (what breaks, under which input/output/edge case, and why) and THEN repair it in place — no soft-pedalling, no could/should. Reject "good enough"; force the objectively densest, most type-safe, MOST CAPABLE form, and run COUNTERFACTUALS. Hold the highest bar of any stage. EDIT to repair every defect you raise. Report what you extended in `extended`.' + svyBlock(svy) + '\nReturn fix-log + residual_high — each item a {files: [every repo-relative path the cross-file fix spans], claim} object for a CROSS-FILE item you cannot fix from one file (NO severity — every finding counts equally and the reconcile phase addresses all; every single-file fix you make yourself).'].join('\n')

// --- [STAGES] ----------------------------------------------------------------------------
const STAGES = [
  { key: 'rebuild', build: authorPrompt, effort: 'max' },
  { key: 'crit', build: critiquePrompt, effort: 'xhigh' },
  { key: 'redteam', build: redteamPrompt, effort: 'max' },
]
const processPage = async (w, tag) => {
  const logs = {}
  for (const st of STAGES) {
    const r = await agent(st.build(w.page, w.svy), { label: st.key + ':' + folderOf(w.page) + ':' + subOf(w.page), phase: tag + folderOf(w.page), schema: FIXLOG_SCHEMA, effort: st.effort, stallMs: 300000 })
    if (r === null) break
    logs[st.key] = r
  }
  return { page: w.page, logs, ok: Object.keys(logs).length === STAGES.length }
}

// --- [COMPOSITION] -----------------------------------------------------------------------
phase('Discover')
const inv = await agent('List every design page under ' + SWEEP + ' — markdown specs at paths matching */.planning/**/*.md. Return each as a repo-relative path (e.g. ' + ROOT + '/<area>/.planning/<sub>/<page>.md). Exclude IDEAS.md/TASKLOG.md/README.md/ARCHITECTURE.md. Use find; do not cd.', { label: 'discover', phase: 'Discover', schema: DISCOVERY_SCHEMA, model: 'sonnet', effort: 'low' })
const pages = ((inv && inv.pages) || []).filter(Boolean)
const total = pages.length
log('Discover under ' + SWEEP + ': ' + total + ' design pages; pooling at CAP=' + CAP)

// --- [SURVEY] -- per area: shared owners (cross-file unification) + per-page justified capability gaps ---
phase('Survey')
const byFolder = new Map()
for (const p of pages) { const fp = folderPathOf(p); (byFolder.get(fp) || byFolder.set(fp, []).get(fp)).push(p) }
const folderPaths = [...byFolder.keys()]
const surveys = (await pool(folderPaths, CAP, (fp) => agent(surveyPrompt(fp, byFolder.get(fp)), { label: 'survey:' + (fp.split('/').pop() || fp), phase: 'Survey', schema: SURVEY_SCHEMA, effort: 'xhigh', stallMs: 300000 }))).filter(Boolean)
const surveyByPage = new Map()
for (const s of surveys) for (const pg of ((s && s.pages) || [])) surveyByPage.set(pg.page, { shared_owners: (s.shared_owners || []), coverage: pg.coverage, size: pg.size, gaps: (pg.gaps || []) })
const sparse = [...surveyByPage.values()].filter((v) => v.coverage === 'sparse').length
const gapTotal = [...surveyByPage.values()].reduce((n, v) => n + (v.gaps ? v.gaps.length : 0), 0)
log('Survey: ' + folderPaths.length + ' areas mapped; ' + sparse + ' sparse-coverage pages; ' + gapTotal + ' justified capability gaps to close in place')

phase('Rebuild')
const pending = pages.map((p) => ({ page: p, svy: surveyByPage.get(p) || null }))
const done = (await pool(pending, CAP, (w) => processPage(w, 'Rebuild-'))).filter(Boolean)

// --- [RECONCILE] -- consume the cross-FILE residuals the per-page pass deferred -----------
const RESIDUAL_FIX_SCHEMA = { type: 'object', additionalProperties: false, required: ['files', 'verdict', 'summary'], properties: { files: { type: 'array', items: { type: 'string' } }, verdict: { type: 'string', enum: ['fixed', 'clean'] }, summary: { type: 'string' } } }
const RECONCILE_VERIFY_SCHEMA = { type: 'object', additionalProperties: false, required: ['overall', 'claims'], properties: { overall: { type: 'boolean' }, claims: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['claim', 'status'], properties: { claim: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'invalid', 'open'] }, evidence: { type: 'string' } } } } } }
const norm = (x, page) => typeof x === 'string' ? { files: [page], claim: x } : { files: x.files && x.files.length ? x.files : [page], claim: x.claim }
const allRes = []
for (const r of done) for (const st of ['rebuild', 'crit', 'redteam']) { const l = r.logs && r.logs[st]; if (l && l.residual_high) for (const x of l.residual_high) allRes.push(norm(x, r.page)) }
const uniq = [...new Map(allRes.map((r) => [r.files.slice().sort().join(',') + '|' + r.claim, r])).values()]
const clusters = (() => {
  const parent = new Map(); const find = (f) => { let p = f; while (parent.get(p) !== p) p = parent.get(p); return p }; const add = (f) => { if (!parent.has(f)) parent.set(f, f) }
  for (const r of uniq) { r.files.forEach(add); for (let i = 1; i < r.files.length; i++) parent.set(find(r.files[i]), find(r.files[0])) }
  const by = new Map()
  for (const r of uniq) { const root = r.files.length ? find(r.files[0]) : '__none__'; (by.get(root) || by.set(root, []).get(root)).push(r) }
  return [...by.values()]
})()
log('Rebuild: ' + done.length + '/' + total + ' pages; reconcile ' + uniq.length + ' residuals (crit+redteam, deduped) -> ' + clusters.length + ' clusters')
let reconciled = []
if (clusters.length) {
  phase('Reconcile')
  reconciled = (await pipeline(
    clusters,
    (cl) => agent([LAW, '', ULTRA, '', EXTEND, '', PATLAW, '', BOUNDARIES, '', 'TASK: RECONCILE these cross-FILE residuals the critique AND red-team passes deferred. There is NO severity — treat EVERY residual as must-address. Read EVERY listed file. For each: if it is a real cross-file defect, FIX it in place (unify the shared type/seam/rail, repair the strata/boundary issue, or extend the shared owner in place to close a capability gap that spans files), preserving all capability and regressing no file; if a residual is FACTUALLY INCORRECT or not a real defect, leave it and say why in the summary — never silently skip a real one to avoid work. Residuals:\n' + JSON.stringify(cl, null, 1)].join('\n'), { label: 'reconcile-fix', phase: 'Reconcile', schema: RESIDUAL_FIX_SCHEMA, effort: 'max', stallMs: 300000 }),
    (fix, cl, i) => fix ? agent([LAW, '', BOUNDARIES, '', 'TASK: ADVERSARIAL VERIFY, one verdict per claim. Read the named files from disk and classify each residual: status "fixed" (real defect, now genuinely resolved), "invalid" (the claim is factually wrong / not a real defect — cite why), or "open" (real defect still NOT resolved). Default to "open" on any doubt for a real-looking defect; mark "invalid" ONLY when you can show the claim is wrong. Claims:\n' + JSON.stringify(cl, null, 1) + '\nFiles the fixer touched: ' + JSON.stringify(fix.files)].join('\n'), { label: 'reconcile-verify:' + i, phase: 'Reconcile', schema: RECONCILE_VERIFY_SCHEMA, effort: 'xhigh', stallMs: 300000 }).then((v) => ({ cluster: cl, fix, verify: v })) : null,
  )).filter(Boolean)
}
const claimsAll = reconciled.flatMap((r) => (r.verify && r.verify.claims) || [])
const hard_residual = claimsAll.filter((c) => c.status === 'open').map((c) => c.claim)
const dropped = claimsAll.filter((c) => c.status === 'invalid').map((c) => c.claim)
log('Reconcile: ' + clusters.length + ' clusters; ' + hard_residual.length + ' open (hard residual), ' + dropped.length + ' dropped as invalid')
return { root: ROOT, scope: SCOPE || 'ALL', complete: done.filter((r) => r.ok).length, incomplete: done.filter((r) => !r.ok).length, total: total, sparse_pages: sparse, capability_gaps: gapTotal, clusters: clusters.length, hard_residual: hard_residual, dropped: dropped }
