# [PERSISTENCE_FEATURES]

Isolation-concept atlas for durable state. Every concept rides one budgeted owner — `StoreProfile`, `StorePlacement`, `StoreLifecycle`, `DataLane`, `StoreOp<T>`, `SnapshotCodec`, `CompressionPolicy`, `HashPolicy`, `SyncTransport`, `RetentionPolicy`, `IdentityPolicy`, `SqlitePragma`, `ExtensionGate`, the cache contribution capsule, and the extension/registry tables — so a new product is a row, case, or policy value, never a surface. Mechanics live in the finalized `.planning/` pages; each concept names its spine then its owning `page#cluster` anchors, and the `[STATE]` column mirrors the charter DENSITY_BAR lens: `FINALIZED` where every owning fence is transcription-complete with no open gate, `SPIKE` where the owner is fence-complete but its proof carries a residual native, bridge, or live-server probe named in the page RESEARCH cluster, `PARTIAL` where a column or arm is shaped but its row set is not closed, `MISSING` where no fence yet carries the concept.

## [1]-[ISOLATION_CONCEPTS]

The products this folder uniquely owns. Each concept is a spine over owned clusters; modalities name the host contexts it serves.

| [INDEX] | [CONCEPT]                                              | [SPINE]                                                                                              | [PAGE#CLUSTERS]                                                                                              | [MODALITIES]              | [STATE]   |
| :-----: | :----------------------------------------------------- | :--------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------- | :------------------------ | :-------- |
|   [1]   | Topology-as-rows store axis                            | one `StoreProfile` row crosses engine · placement · codec; a new deployment is one row, zero code     | store-profiles#PROFILE_AXIS · store-profiles#PLACEMENT_MATRIX                                               | all                       | FINALIZED |
|   [2]   | Receipted open/restore/drain ceremony                  | five-state `StoreLifecycle` folds open-proof order to one `StoreOpenReceipt`; restore is the capstone | store-profiles#STORE_LIFECYCLE · snapshot-codecs#RESTORE_AND_DIFF                                           | plugin, standalone, all   | SPIKE     |
|   [3]   | Cross-process writer lease + epoch fence               | `StoreLeaseRow` `Claim` elects one writer; `Fence` epoch invalidates stale handles; locality guard    | store-profiles#CROSS_PROCESS_LAW · native-sqlite#PRAGMA_TABLE                                               | plugin, standalone        | SPIKE     |
|   [4]   | Seven-lane durable shape axis                          | `DataLane` union folds against profile capability columns; a lane the engine lacks never composes     | data-lanes#LANE_AXIS                                                                                        | all                       | FINALIZED |
|   [5]   | Aggregate document store + per-column JSON index       | `ComplexProperty.ToJson` document lane; `JsonIndex` keys jsonb/json column and GIN opclass             | data-lanes#DOCUMENT_LANE · schema-rail#GENERATED_COLUMNS · query-rail#PROJECTION_SHAPES                     | service, embedded         | SPIKE     |
|   [6]   | Vector + full-text semantic search                     | `VectorQuery`/`FullTextQuery` shapes project per provider; `EmbeddingIdentity` dedupes by content      | data-lanes#SEARCH_LANES · native-sqlite#EXTENSION_GATES · schema-rail#EXTENSION_DDL                         | service (pgvector), embedded (FTS5 + gated vec0) | SPIKE     |
|   [7]   | Geospatial site-context lanes (PostGIS + GeoPackage)   | NTS interior; geography/geometry server columns; R*Tree window then managed refine; GPB blob codec     | data-lanes#GEO_LANES · schema-rail#EXTENSION_DDL · snapshot-codecs#CODEC_AXIS                               | service, standalone, plugin | SPIKE   |
|   [8]   | Analytical projection lane + HLC changefeed→parquet    | DuckDB read-only attach; `TabularExportSpec` reader/writer pair; ordered op-log appender to parquet     | data-lanes#ANALYTICAL_LANE · cache-indexes#BENCHMARK_INDEX                                                  | all                       | SPIKE     |
|   [9]   | One operation algebra + Kleisli composition            | eight-case `StoreOp<T>` in one pooled bracket; `Then` composes ops keeping self-emitted receipts/tags   | query-rail#OPERATION_ALGEBRA                                                                                | all                       | FINALIZED |
|  [10]   | Keyset pagination + typed projection egress            | `KeysetPage<TRow>` probe-row cursor on uuid-v7 order; typed records only — entities never cross         | query-rail#PROJECTION_SHAPES                                                                                | all                       | FINALIZED |
|  [11]   | Bulk movement with self-emitted changefeed + delta     | `BulkRoute` Copy/Merge/Set over linq2db; RETURNING old/new straight into `BulkDelta<TRow>`              | query-rail#BULK_LANE · sync-collaboration#OPLOG_CHANGEFEED                                                  | all                       | SPIKE     |
|  [12]   | Four-hook interceptor spine + store observability      | one `StoreInterceptor` capsule; slow/burst facts; op-log stamp in-txn; Npgsql tracer/meter contribution | query-rail#INTERCEPTOR_SPINE · native-sqlite#MAINTENANCE_OPS                                                | all                       | SPIKE     |
|  [13]   | Schema-drift gate + expand/contract migration law      | `SchemaFault.Gate` folds applied-vs-known + fingerprint; destructive step waits on retention approval   | schema-rail#MIGRATION_LAW · schema-rail#GENERATED_COLUMNS                                                   | all                       | SPIKE     |
|  [14]   | Three-row identity axis (uuid-v7 / content / natural)  | `IdentityPolicy` per-provider default SQL; uuid-v7 orders B-tree; content hash addresses immutables      | schema-rail#IDENTITY_POLICY · native-sqlite#MAINTENANCE_OPS                                                 | all                       | SPIKE     |
|  [15]   | Generated-converter admission + snake-case naming      | `ConverterRail` installs Thinktecture converters once; every value-object/smart-enum/union maps free     | schema-rail#CONVERTER_RAIL                                                                                  | all                       | FINALIZED |
|  [16]   | Embedded engine floor (PRAGMA · compile · maintenance) | four policy tables over Microsoft.Data.Sqlite; receipted verbs; snapshot pin; blob streams               | native-sqlite#PRAGMA_TABLE · native-sqlite#COMPILE_SURFACE · native-sqlite#MAINTENANCE_OPS                  | plugin, standalone        | SPIKE     |
|  [17]   | Loadable-extension + encryption gate                   | `ExtensionGate` rows name artifact route + fallback lane; `DbConfig` hardening; SQLCipher research row    | native-sqlite#EXTENSION_GATES                                                                               | standalone, plugin        | SPIKE     |
|  [18]   | Three-row sealed snapshot codec + atomic seal          | `SnapshotCodec` json-stj/messagepack/file-raw; header law; flush-before-rename; orphan sweep              | snapshot-codecs#CODEC_AXIS · snapshot-codecs#SNAPSHOT_PROTOCOL                                              | all                       | SPIKE     |
|  [19]   | Compression + hash policy axes                         | `CompressionPolicy` (none/lz4) + `HashPolicy` (content/identity/frame/wide/framewide) delegate rows       | snapshot-codecs#COMPRESSION_HASHING                                                                         | all                       | FINALIZED |
|  [20]   | Content-addressed snapshot identity + diff             | sealed `Hash` is the one content address for cache tag, blob lookup, and diff identity                    | snapshot-codecs#SNAPSHOT_PROTOCOL · snapshot-codecs#RESTORE_AND_DIFF · cache-indexes#ARTIFACT_BLOB_INDEX    | all                       | FINALIZED |
|  [21]   | L2 cache contribution behind the AppHost port          | `CacheContribution` capsule is L2 `IBufferDistributedCache` + `IHybridCacheSerializerFactory`            | cache-indexes#L2_CONTRIBUTION · data-lanes#LANE_AXIS                                                        | all                       | FINALIZED |
|  [22]   | Model-result index (warm-start substrate selection)    | `ModelResultKey` deterministic identity; read-through `Result`; one hit/miss/evict fact stream            | cache-indexes#MODEL_RESULT_INDEX                                                                            | companion, all            | FINALIZED |
|  [23]   | Content-addressed artifact-blob catalog                | `ArtifactIndexRow` ep-context / onnx-profile; identity from payload; classification typed at admission     | cache-indexes#ARTIFACT_BLOB_INDEX                                                                           | companion, all            | FINALIZED |
|  [24]   | Recency-gated benchmark route index                    | `BenchmarkRow.Claim` admits fingerprint-matched rows inside a staleness horizon; folds to most-recent      | cache-indexes#BENCHMARK_INDEX                                                                               | all                       | FINALIZED |
|  [25]   | Op-log changefeed (one log = outbox·audit·feed·sync)   | one `OpLogEntry` shape per entity kind; HLC stamp; commits in the entity transaction; `Closure` manifest   | sync-collaboration#OPLOG_CHANGEFEED · query-rail#INTERCEPTOR_SPINE                                          | all                       | FINALIZED |
|  [26]   | LWW merge law + typed conflict receipts                | `SyncMerge.Adjudicate` LWW over (HLC, origin); four `ConflictOutcome` cases; idempotent `Apply` fold        | sync-collaboration#MERGE_LAW                                                                                | hub, standalone           | FINALIZED |
|  [27]   | Three-transport sync axis (replication·http·diff)      | `SyncTransport` union widened by topology + direction fields; PgLogical V4 parallel; RFC 6902 fallback      | sync-collaboration#TRANSPORT_AXIS · store-profiles#PROVISIONING_ROWS                                        | hub, service              | SPIKE     |
|  [28]   | Object-graph sync (speckle-class diff transport)       | `CopyGraph` set-difference over content-key `Closure`; `HasObjects` negotiation; ArtifactSync frames        | sync-collaboration#TRANSPORT_AXIS · sync-collaboration#PRESENCE_AND_BLOB                                    | hub                       | FINALIZED |
|  [29]   | Presence + offline-queue collaboration                 | `PresenceRow` ephemeral op-log row; TTL = 3× heartbeat; DropOldest lane; offline queue drains on reconnect  | sync-collaboration#PRESENCE_AND_BLOB                                                                        | hub, standalone           | FINALIZED |
|  [30]   | Streamed blob transfer (incremental frame hash)        | 64 KiB frames, Crc32 per frame, `XxHash128` whole-artifact via `Append`/`GetCurrentHash`                    | sync-collaboration#PRESENCE_AND_BLOB · store-profiles#PROFILE_AXIS                                          | hub, sidecar              | FINALIZED |
|  [31]   | Classification enforcement at every store egress       | `ClassificationGuard.Admit` rejects unpersistable/unclassified; `ArtifactClassRow` registry                | redaction-retention#CLASSIFICATION_ENFORCEMENT                                                              | all                       | FINALIZED |
|  [32]   | Hold-first receipted retention sweep                   | `RetentionSweep` filters holds before every bound; `SweepReceipt` proves inventory = kept+held+evicted      | redaction-retention#RETENTION_SWEEPS                                                                        | all                       | FINALIZED |
|  [33]   | Support-bundle store evidence + hash-proved export     | `StoreEvidence` seven rows; redactor resolves from classification; `ExportProof` content hash               | redaction-retention#EXPORT_PROOF · redaction-retention#AUDIT_BINDING                                        | all                       | SPIKE     |
|  [34]   | Classification→pgaudit category binding                | `AuditBinding.Bind` folds present classes to the escalating `pgaudit.log` value; provisioning-verified       | redaction-retention#AUDIT_BINDING · store-profiles#PROVISIONING_ROWS                                       | service                   | SPIKE     |
|  [35]   | Operator-provisioning manifest + capability verdict    | `ExtensionRequirement.Verify` reads pg_extension/pg_settings; runtime verifies, never executes               | store-profiles#PROVISIONING_ROWS · schema-rail#EXTENSION_DDL                                                | service                   | SPIKE     |
|  [36]   | Dashboard wire surface (snapshot + sync TS contracts)  | `PersistenceWireContext` STJ source-gen; primitive-mapped wire so the TS extension union is `never`          | snapshot-codecs#TS_PROJECTION · sync-collaboration#TS_PROJECTION                                            | web                       | FINALIZED |

## [2]-[CAPABILITY_EXPANSION_REGISTER]

Future-app-dev gap-hunt survivors: capabilities a downstream app would hand-roll in this domain, each mapped to the exact owner·`page#cluster` row/case/column to add on an existing budgeted owner. Every survivor is grounded in a `.api` catalogue (else flagged `RESEARCH`). `[STATE]` is the gap's current realization.

| [INDEX] | [GAP]                                                      | [OWNER · PAGE#CLUSTER]                              | [ADDITION]                                                                  | [GROUNDING]            | [STATE]   |
| :-----: | :--------------------------------------------------------- | :------------------------------------------------- | :-------------------------------------------------------------------------- | :--------------------- | :-------- |
|   [1]   | Live store attach snapshot coherence under one writer       | `TabularExportSpec` · data-lanes#ANALYTICAL_LANE   | `ATTACH (TYPE sqlite, READ_ONLY)` row resolved from `[LIVE_ATTACH]` probe   | api-duckdb (`GetSchema`/`Duplicate`) | SPIKE |
|   [2]   | In-process managed sequence as a planner relation           | `TabularExportSpec` · data-lanes#ANALYTICAL_LANE   | `RegisterTableFunction`/`RegisterScalarFunction` row with `CardinalityHint` | api-duckdb (FUNCTION_TYPES) | PARTIAL |
|   [3]   | Vector-quantum chunk transfer for derived rollup staging     | `TabularExportSpec` · data-lanes#ANALYTICAL_LANE   | `VectorDataReaderFactory`/`IDuckDBDataWriter` `WriteValue`/`IsValid` row     | api-duckdb (CHUNK_TYPES) | PARTIAL |
|   [4]   | Multi-statement fan-out read as one server round-trip        | `StoreOp<T>` · query-rail#OPERATION_ALGEBRA        | `NpgsqlBatch`/`NpgsqlBatchCommand` execution inside Query/Aggregate arms     | api-npgsql (`NpgsqlBatch`) | PARTIAL |
|   [5]   | Custom provider-type classification without per-type MapEnum  | `StoreProfile` · store-profiles#PROFILE_AXIS       | `EnableUnmappedTypes` column + `INpgsqlTypeMapper` classification seam        | api-npgsql (`EnableUnmappedTypes`, `INpgsqlTypeMapper`) | PARTIAL |
|   [6]   | PostgreSQL composite-type round-trip                         | `SchemaDdl.Composite` · schema-rail#EXTENSION_DDL  | `MapComposite` row + `Composites` set entry (empty until landmark)           | api-npgsql (`MapComposite`); api-npgsql-ef (`PostgresEnum`/`PostgresRange`) | PARTIAL |
|   [7]   | Read-only embedded reader replica without writer contention   | `StoreProfile` · store-profiles#PROFILE_AXIS       | `SqliteOpenMode.ReadOnly` placement value on the embedded row                 | api-sqlite (`SqliteOpenMode`) | PARTIAL |
|   [8]   | Domain-fold aggregate registered into the embedded engine     | `FunctionRegistration` · native-sqlite#MAINTENANCE_OPS | `CreateAggregate` row inside the same `Bind` delegate                       | api-sqlite (`CreateAggregate`) | PARTIAL |
|   [9]   | Case-insensitive / locale collation in embedded queries       | `FunctionRegistration` · native-sqlite#MAINTENANCE_OPS | `CreateCollation` row beside `instant_iso`                                  | api-sqlite (`CreateCollation`) | PARTIAL |
|  [10]   | Paged hot-store backup with progress receipts                 | `SqliteMaintenance` · native-sqlite#MAINTENANCE_OPS | `sqlite3_backup_step` paged session over `Handle` subsuming `BackupDatabase`  | api-sqlite (`Handle`, `BackupDatabase`) | SPIKE |
|  [11]   | Consistent multi-transaction read pin (monotone reader)       | `SqliteMaintenance` · native-sqlite#MAINTENANCE_OPS | `WithSnapshot` over `sqlite3_snapshot_get`/`_cmp`/`_open` with floor          | RESEARCH (`[EXTENSION_LOADING]` snapshot bracket; not in api-sqlite) | SPIKE |
|  [12]   | jsonb blob fast path beside the TEXT-json EF mapping          | `ExtensionGate` · native-sqlite#EXTENSION_GATES    | `jsonb_*` in-process raw-SQL row gated off the seam, EF mapping stays TEXT     | api-sqlite (compile probe `jsonb`) | PARTIAL |
|  [13]   | Per-connection defensive hardening before first statement     | `DbConfig` · native-sqlite#EXTENSION_GATES         | `DbConfig.Hardened` `sqlite3_db_config` row set ordering                       | RESEARCH (`[DB_CONFIG_OPS]`; `Handle` in api-sqlite) | SPIKE |
|  [14]   | Streaming compression for payloads above 1 MiB                | `CompressionPolicy` · snapshot-codecs#COMPRESSION_HASHING | `LZ4Encoder`/`LZ4ChainDecoder` `Topup`/`Drain` chained-block route          | api-lz4 (streaming frame) | PARTIAL |
|  [15]   | Zstandard compression swap keeping LZ4 archives readable       | `CompressionPolicy` · snapshot-codecs#COMPRESSION_HASHING | one delegate row carrying its own `HeaderId`                                 | RESEARCH (`[ZSTD_SWAP]` inbox TFM)  | MISSING |
|  [16]   | AOT generated MessagePack resolver replacing runtime composite | `SnapshotCodec.Binary` · snapshot-codecs#CODEC_AXIS | `GeneratedMessagePackResolverAttribute`/`CompositeResolverAttribute` row      | RESEARCH (`[RESOLVER_PRECEDENCE]`; api-messagepack) | SPIKE |
|  [17]   | Reader-to-writer fusion (no parse-materialize-rebuild)         | `TabularExportSpec` · data-lanes#ANALYTICAL_LANE   | `SepReaderWriterExtensions.CopyTo` + `SepWriter.NewRow(readerRow)` row         | api-sep (`SepReaderWriterExtensions`) | PARTIAL |
|  [18]   | Parallel ordered tabular projection                           | `TabularExportSpec` · data-lanes#ANALYTICAL_LANE   | `ParallelEnumerate` with the thread-safe `SepToString` pool variant            | api-sep (`ParallelEnumerate`) | PARTIAL |
|  [19]   | Typed feature-attribute projection (no loose-table walking)    | `GeoLayer` · data-lanes#GEO_LANES                  | `TryDeserializeJsonObject<T>`/`TryGetJsonObjectPropertyValue<T>` row           | api-nts-io (ATTRIBUTE_TYPES) | PARTIAL |
|  [20]   | Constructive managed geometry refine over container candidates | `GeoLayer` · data-lanes#GEO_LANES                  | `Centroid`/`ConvexHull`/`Intersection` rows beyond Intersects/Contains/Buffer  | RESEARCH (`[MANAGED_REFINE_OPS]`)   | SPIKE |
|  [21]   | NodaTime range + multirange temporal columns                   | `GeoLayer` · data-lanes#GEO_LANES                  | `NpgsqlRange<T>`/`IntervalMultirangeMapping` + `RangeAgg` row                   | api-npgsql (`NpgsqlRange<T>`, `NpgsqlInterval`) | PARTIAL |
|  [22]   | Hierarchy path lane (assembly/part trees)                      | `GeoLayer` · data-lanes#GEO_LANES                  | `ltree` driver-native row with lquery operators                                | api-npgsql-ef (`HasPostgresExtension`); schema-rail Extensions ltree | PARTIAL |
|  [23]   | DB-temporal daylight-transition policy                         | `StoreProfile` · store-profiles#PROFILE_AXIS       | `Resolvers` column — AtStrictly / AtLeniently / ResolveLocal                    | api-npgsql-ef-nodatime (RESEARCH-confirm value domain) | PARTIAL |
|  [24]   | Idle replication-slot reclamation + conflict statistics         | `SyncTransport.PgLogicalReplication` · sync-collaboration#TRANSPORT_AXIS | `IdleSlotTimeout` policy column + subscription-stats read view             | RESEARCH (`[LIVE_REPLICATION]`)     | SPIKE |
|  [25]   | Join-preserving HMAC redaction across artifacts                | `ExportProof` · redaction-retention#EXPORT_PROOF   | `RedactorKind.Hmac` resolving `HmacRedactor` via `GetRedactor`/`GetRedactedLength` | api-redaction (`HmacRedactor`, `GetRedactor`) | SPIKE |
|  [26]   | Native at-rest encryption for personal-class stores             | `EncryptionGate.Sqlcipher` · native-sqlite#EXTENSION_GATES | `PRAGMA key`/`cipher_migrate`/`rekey` ceremony row off Research state         | RESEARCH (`[EXTENSION_LOADING]` SQLCipher) | SPIKE |
|  [27]   | Compiled hot-query delegates for proven hot shapes              | `ProjectionRail` · query-rail#PROJECTION_SHAPES    | `EF.CompileAsyncQuery` static delegate rows beside projection records           | api-npgsql-ef / api-ef-sqlite (translation)  | PARTIAL |
|  [28]   | pg_stat_statements slow-query evidence read view               | `StoreFact` · query-rail#INTERCEPTOR_SPINE         | one `Aggregate` raw-SQL shape gated on the provisioning probe                    | store-profiles Provisioning (pg_stat_statements) | SPIKE |

## [3]-[FOLDER_CONCEPT_SEEDS]

Higher-order isolation concepts this folder uniquely enables — each a fold or combinator over already-budgeted owners, feeding Phase 3 integration.

- `StoreOp<T>→StoreOp<U>` Kleisli combinator: `Then` sequences a producing op with a continuation through the generated total `Switch`, rewrapping into the source case so a receipt- or tag-bearing arm keeps its self-emitted invalidation — one composed op runs in one bracket inside one execution strategy. (query-rail#OPERATION_ALGEBRA)
- `SchemaVerdict × StorePlacement` deployment lattice: boot folds applied/known identifier sets and the fingerprint gate against the placement's migrate/read-ahead authority, so a store newer than the binary is a typed rejection and the apply arm is placement-derived, never a runtime branch. (schema-rail#MIGRATION_LAW × store-profiles#PLACEMENT_MATRIX)
- Content-addressed identity unification: the sealed snapshot `Hash` is one `ContentAddress` value selecting cache tag, blob lookup, and diff identity at once — no parallel key mints, so the artifact-blob index, the L2 cache, and the snapshot diff share one content key. (snapshot-codecs#SNAPSHOT_PROTOCOL → cache-indexes#ARTIFACT_BLOB_INDEX)
- `RetentionPolicy × hold` hold-first sweep transformer: `Sweep`'s `let live = facts.Filter(!Hold)` precedes every `Eligible` evaluation so `LegalHold` dominates any bound by construction, and a new retained class composes with zero consumer change. (redaction-retention#RETENTION_SWEEPS)
- Interceptor-spine effect-transformer stack: the four EF hooks, the linq2db `AddInterceptor` altitude, and the Npgsql tracer/meter roots register as ordered transformers on one stack where registration order is execution order — a soft-delete audit or trace-depth cap is a new transformer row, never code in an operation body. (query-rail#INTERCEPTOR_SPINE)
- One-log four-fold projection: the single `op_log`/`OpLogEntry` table is simultaneously outbox, audit artifact, change feed, and sync feed — four folds over one structure committed inside the entity transaction, so a polled outbox, a CDC trigger, and a second changefeed are all structurally deleted. (sync-collaboration#OPLOG_CHANGEFEED)
- LWW join-semilattice convergence: `Adjudicate` over (HLC, origin) makes any partition of any permutation of the op multiset applied any number of times converge to identical materialized state — one sentence subsuming idempotency, commutativity, reorder tolerance, and crash replay, and the property-test specification verbatim. (sync-collaboration#MERGE_LAW)
- Transport-invariant merge core: `SyncTransport` variance is case rows plus two fields (topology, direction); the LWW law and the op-log shape stay transport-invariant, so PgLogical replication, HTTP delta, and speckle-class diff all fold into one `Apply`, and a new transport touches zero entity or query surface. (sync-collaboration#TRANSPORT_AXIS)
- Incremental whole-artifact identity: `FrameHash` feeds each 64 KiB frame into one `XxHash128` instance via `Append` and reads `GetCurrentHash`, so a multi-gigabyte blob never materializes contiguously and the same content key threads streamed transfer, the blob catalog, and the sync `Closure` manifest. (sync-collaboration#PRESENCE_AND_BLOB)
- Capability-fold dispatch: `DataLane.Profiles` and `ExtensionRequirement.Verify` fold engine/server capability into a typed verdict the rail dispatches on, so a lane the engine lacks is an absent slot that never composes — exclusion at composition, never a runtime not-supported throw. (data-lanes#LANE_AXIS × store-profiles#PROVISIONING_ROWS)

## [4]-[ROUTING]

- Concept mechanics: `.planning/<page>.md` at the named `cluster` anchor.
- Owners, budgets, gaps, admissions: `.planning/README.md` (DENSITY_BAR, GAP_LEDGER, ADMISSIONS_RECORD).
- Implementation-start gates and research probes: `ROADMAP.md` and each page RESEARCH cluster.
- External-API grounding: `.api/<package>.md` per the catalogue README rail map.
- Cross-package topologies and concert concepts: `../.planning/FEATURES.md`.
- TS web-layer consumption of the wire surfaces: `libs/typescript/.planning/`.
