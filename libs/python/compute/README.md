# [PY_COMPUTE]

`compute` owns offline scientific payload admission, solver plans, studies, model assets, and graduation receipts. This package currently contains planning and API placeholders only; future source lands directly in this folder after the planning and API pages are filled.

## [OWNER]

[PLANNING]:
- Path: `.planning/README.md`
- Owns: array admission, solver routes, unit/uncertainty claims, study evidence, model assets, and graduation planning.

[API]:
- Path: `.api/README.md`
- Owns: flat `api-*.md` placeholders for compute dependencies and pending accelerator/model candidates.

[BOUNDARY]:
- Compute prepares evidence and research output for product owners.
- `Rasm.Compute` remains the owner for production substrate selection, tensor and model sessions, progress, receipts, and benchmark claims.
