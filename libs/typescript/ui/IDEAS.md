# [UI_IDEAS]

The forward concept pool for the host-free browser UI library. Open ideas are higher-order folder concepts grounded in the browser-platform purpose and the current effect-atom, React concurrent-features, WebGPU, and design-system capability surfaces; each drives one or more task cards in `TASKLOG.md`. A finished or dropped idea moves to closed with a one-line disposition so it is never re-litigated.

## [1]-[OPEN]

[COMMAND_SURFACE_VOCABULARY]:
- Promote the unowned content layer to its own `content` sub-domain owning the in-app command surface as ONE `CommandAction` `as const satisfies Record` vocabulary whose rows carry the action label, the `lucide-react` icon component, the keybinding, the `politeness`/role provenance, and the `interchange` `CommandGateway` intent it dials — rendered through the `cmdk` `Command` palette with `useFilter`-locale-aware scoring and the `react-stately` `UNSTABLE_useFilteredListState` view, plus the `vaul` `Drawer` directional-sheet and the one `cva`+`twMerge` variant-recipe owner the whole library composes for class-name dispatch.
- Unlocks one command vocabulary the keyboard palette, the action menu, and the deep-link router all read, an AEC-specific action lexicon (place-element, run-analysis, open-viewpoint, export-fabrication) rather than a thin generic "search" box, a single `cva` recipe owner so every component variant is one row not a per-`.tsx` className soup, and the icon set as a tree-shaken vocabulary keyed by action — collapsing `cmdk`/`vaul`/`lucide-react`/`class-variance-authority`/`tailwind-merge`, five admitted packages with zero owner page, into one sub-domain.
- Draws on `cmdk` `Command`/`Command.Dialog`/`useCommandState`, `vaul` `Drawer` snap-point sheets, `lucide-react` `createLucideIcon`/named-icon tree-shaking, `class-variance-authority` `cva`/`VariantProps`, `tailwind-merge` `twMerge`/`twJoin`, the `react-aria` `useFilter` and `react-stately` `UNSTABLE_useFilteredListState` filtering surface, and the `tw-animate-css` `data-[state]` enter/exit layer — the prior `role-behavior` page listed all five in its package line yet owned none of them, the canonical thin-folder/unexploited-package signal.

[PERCEPTUAL_PICKER_FAMILY]:
- Realize the `pickers` role's color and date behaviors as ONE headless `PickerBehavior` family over the `react-stately` `useColorAreaState`/`useColorChannelFieldState`/`useColorPickerState`/`parseColor`/`getColorChannels` color state and the `react-aria-components` `Calendar`/`RangeCalendar`/`DatePicker`/`FileTrigger` widgets, the color channels projected through the `theming` OKLCH `colorjs.io` space so a picked color is a perceptual `oklch` token, never an sRGB hex — the color/date/file behaviors the `_Roles` `pickers` row already names but no page realizes.
- Unlocks an AEC-grade color picker operating in the perceptual OKLCH space the design-token layer already owns (a material-color pick is contrast-checked and token-emitted in one fold), a calendar/date-range picker for schedule and milestone surfaces, and a `FileTrigger` IFC/GLB ingest affordance — three named-but-empty `pickers` behaviors collapsed onto one headless family.
- Draws on the `react-stately` color state hooks and `parseColor`/`getColorChannels`, the `react-aria-components` `Calendar`/`DatePicker`/`FileTrigger`, and the `theming/theme-tokens` `colorjs.io` OKLCH conversion bridge; the `role-behavior` page named the `color`/`date`/`file-upload`/`file-preview` behaviors in the `pickers` row yet left the entire react-aria color/date state family — a fully catalogued surface — unexploited.

[CARTOGRAPHIC_AGGREGATION_ARM]:
- Extend the `cartography` `GeoSeriesLayer` `source` axis with a cell-index aggregation arm — `H3HexagonLayer`/`H3ClusterLayer`/`S2Layer`/`QuadkeyLayer`/`GeohashLayer`/`A5Layer` for spatial-bin density, `Tile3DLayer` for OGC-3D-Tiles/`i3s` city-scale context streaming, and `TripsLayer` for time-windowed movement — so site/regional density and 3D-context streaming are rows on the existing `$match`, not a parallel surface, fed by the same `interchange` GeoArrow projection.
- Unlocks AEC site-and-regional analytics the flat `GeoJsonLayer`/`MVTLayer` arms cannot express: hexagonal-bin density of sensor or asset points, S2/quadkey administrative aggregation, streamed 3D city context under the model, and animated logistics/movement — the `@deck.gl/geo-layers` cell-index and tile-3d catalogue rows named as growth yet wired to no arm.
- Draws on the `@deck.gl/geo-layers` `H3HexagonLayer`/`S2Layer`/`Tile3DLayer`/`TripsLayer` catalogue (the cell-index layers extending `_GeoCellLayer`, `Tile3DLayer` over the `Tileset2D` indexing model) and the existing `geoarrow` Arrow `RecordBatch` binding; the `geo-series-layer` page enumerated these as one-line growth candidates without an actual `source`/`featureKind` arm realizing them.

[ROUTE_TRANSITION_REDUCED_MOTION]:
- Pair the `motion` `SurfaceTransition` `swap` case with the `platform` Navigation API location commit and a reduced-motion gate — the `<ViewTransition>` enter/exit class is suppressed under `prefers-reduced-motion: reduce` through the `tw-animate-css` reduced-motion-aware layer, and the transition fold is one scoped resource the `platform` navigation pipeline wraps around the guard-admitted commit, the `ui` side owning only the `<ViewTransition>`/`<Activity>` wrapper and the motion gate, never the router.
- Unlocks correct, accessible native route transitions: a motion-suppressed user sees an instant swap with no animation while the GPU-composited transition rides for everyone else, the transition scoped to exactly the navigation commit rather than a parallel route tracker, and the backgrounded viewport GL context preserved across the swap.
- Realizes the branch `NATIVE_TRANSITION_PAIR` on the `ui` side; draws on the View Transitions API reduced-motion contract, the `tw-animate-css` `prefers-reduced-motion` collapse, and the React `<ViewTransition>`/`<Activity>` exotics — the `view-transitions` page owned the `swap`/`preserve` fold but carried no motion gate and no explicit navigation-commit coupling.

[CLOCK_UNCERTAINTY_BAND_RENDER]:
- Render the `projection` `SkewBand` as a load-bearing confidence interval on the `observation` evidence timeline — two HLC rows closer in midpoint than the sum of their `radiusMs` draw as a "causally-ambiguous within ±N ms" band rather than a forced total order, the `EvidenceTimelineRoute` reading the promoted ordering input from the `projection` skew-aware fold over the `@tanstack/react-virtual` row set, never a render-only leaf re-deriving order.
- Unlocks honest distributed-systems-correct event ordering surfaced as product UI the desktop AppUi structurally cannot produce: the timeline shows uncertainty bands and concurrent-uncertain pairs as a first-class visual, the confidence interval load-bearing in both the rendered timeline and the conflict adjudication the same band feeds.
- Realizes the branch `HONEST_CLOCK_UNCERTAINTY` on the `ui` side; draws on the existing `SkewBand` `{ midpointMs, radiusMs }` shape the `observation-routes` page already reads for HLC order, promoting it from a render-only `SkewBand` annotation into the band-render and concurrent-uncertain marking — the page ordered by HLC but rendered no uncertainty band.

## [2]-[CLOSED]

[ATOM_NATIVE_BINDING_COLLAPSE]: REALIZED — `binding/atom-binding.md` (T-BINDING-COLLAPSE) authored the `AtomBinding` spine over the native `Atom.searchParam`/`kvs`/`family`/`pull`/`runtime` constructors, the `UndoStack` bounded fold, and the `Result.builder` `onWaiting`/`onSuccess`/`onError`/`render` chain; the offline cell binds `platform` `LocalPersistence` through `Atom.kvs`. No hand-rolled query-string or storage fold survives.

[OKLCH_TOKEN_LAYER]: REALIZED — `theming/theme-tokens.md` (T-THEME-TOKENS) authored as its own sub-domain: `oklchScale` over the `colorjs.io` `Color.steps` perceptual interpolant, `contrastWith` over `Color.contrastWCAG21`, and the single `CssVarSync` `Stream` fold to the `:root` custom properties, decoupled from the role vocabulary.

[FORM_ENGINE_SCHEMA_BINDING]: REALIZED — `component-system/form-binding.md` (T-FORM-BINDING) authored the headless `FormBinding<A>` folding `Schema.decodeUnknownEither` over the raw `FormData` into the typed value or a `ParseError`, projected per-path through `ArrayFormatter.formatErrorSync` onto the `react-aria-components` `Form` `validationBehavior: "aria"` `FieldError` map, a valid decode crossing the `interchange` `CommandGateway`. One schema owns the validity, no parallel validator.

[SHARED_GESTURE_ALGEBRA]: REALIZED — `motion/gesture-algebra.md` (T-GESTURE-ALGEBRA) authored the one `CameraGesture` `Data.TaggedEnum`, the total `applyGesture` `Match.tagsExhaustive` fold, and the `useGestureFold` `@use-gesture/react` `useGesture` recognizer over the verified `FullGestureState`; the viewport camera composes the shared fold, no orphaned per-surface recognizer.

[NATIVE_VIEW_TRANSITIONS]: REALIZED — `motion/view-transitions.md` (T-VIEW-TRANSITIONS) authored the one `SurfaceTransition` `Data.TaggedEnum` over `swap`/`preserve` under the total `mountSurface` `$match`, the verified `useEffectEvent` callback riding it. Residual `[VIEW_TRANSITION_ACTIVITY]`: the React 19.2 `<Activity>`/`<ViewTransition>` exotics stay RESEARCH (absent from branch `react.md`/`react-dom.md`); the owner fold is settled regardless of the import path.

[ACTIVITY_PRESERVED_SURFACES]: REALIZED — the React `<Activity>` mounted-but-hidden surface is the `preserve` case of the `motion/view-transitions.md` `SurfaceTransition` union (T-VIEW-TRANSITIONS) and the `glb-viewport.md` backgrounded-viewport entry (T-VIEWPORT-BACKEND), preserving the `acquireRelease` GL context and atom subscriptions across visibility toggles; folded into the transition owner, never a parallel surface. Shares the `[VIEW_TRANSITION_ACTIVITY]` RESEARCH residual on the `<Activity>` exotic import.

[WEBGPU_BACKEND_COLLAPSE]: REALIZED — `viewport/glb-viewport.md` (T-VIEWPORT-BACKEND) authored the two-row `RendererBackend` axis: the `three` row over the verified `three/webgpu` `WebGPURenderer` `init()` auto-detect/WebGL-fallback and the `model-viewer` zero-GL-handle embed row, the `Effect.acquireRelease` GL context, the gesture-fold camera. The Babylon and raw-WebGPU rows and the manual `navigator.gpu` degrade are deleted; the cluster-LOD `three/tsl` kernel stays RESEARCH.

[LIVE_BINDING_STUDIO_DASHBOARD]: REALIZED — `observation/live-binding-dashboard.md` decodes the settled `Rasm.AppHost/live-wire#TS_PROJECTION` `BindingStatusWire`/`WriteReceiptWire`/`CoercedValueWire` rows (the `WriteBackWire` `kind`-discriminated disposition, the smart-enum binding-status keys) over `@tanstack/react-table`+`react-virtual`, a read-only leaf over a `projection` live cell, the write receipt riding the existing `ReceiptEnvelopeWire`. No second binding-status enum, no transport dial.

[COLLAB_PRESENCE_OVERLAY]: REALIZED — `overlay/presence-overlay.md` renders the collaborator cohort over the two settled wire arms (the `convergence/presence#PRESENCE` `PresenceRowWire` TTL claim and the `causality-graph/version-vector#CRDT_SEMILATTICE` `EphemeralMap` `beat`/`leave` state), each peer one `Atom.family` cell, composing `overlay/floating-anchor`; the honest `[BEAT_PAYLOAD]` residual on the producer `Awareness` beat byte-encoding pin is kept, the decode itself settled.
