# DOSSIER — corpus-b (Rasm.Persistence/.planning second half: Store/ + Version/)

Lane: corpus-b. Deep-read set (9 pages, 3568 LOC): `Store/blobstore.md`, `Store/provisioning.md`, `Version/{ledger,commits,merge,provenance,recovery,retention,timetravel}.md`. First half (Element/, Ingest/, Query/) skimmed only at seam crossings.

Headline: the corpus-b half is **world-class and genuinely current**, not illusory. Two load-bearing external-API claims I attacked survived live verification: AWS S3 `XXHASH128` full-object server-side checksum (blobstore's integrity spine) is REAL and shipped 2026-04 (`ChecksumXXHASH128` member; ten S3 checksum algorithms now); Marten `IProjectionDaemon.WaitForNonStaleData(timeout)` (recovery's caught-up gate) is a REAL daemon method distinct from the store-extension `WaitForNonStaleProjectionDataAsync`. Fault-band federation, rail discipline, ADT collapse, AOP (FailureRank.Absorb, MergeStance dispatch, derived Family/Evidence), and deep specific external-member mining are exemplary. Verdicts sit 8.5–9.5. The real findings are localized logic/dead-carrier bugs plus the four SCOPE-MANDATE latent gaps, all evidence-anchored below.

VERIFICATION NOTE (for build legs): the S3 REST API supports `XXHASH128` since 2026-04; the *managed* `AWSSDK.S3` enum member (`ChecksumAlgorithm.XXHASH128`, `CompleteMultipartUploadRequest.ChecksumXXHASH128`, `ChecksumType.FULL_OBJECT`) must be `assay api`-confirmed against the pinned assembly — verified-local wins. Design is sound; only the SDK-member existence is the open confirm.

---

## PER-PAGE VERDICTS

### Store/blobstore.md — 9.0
Charter (correct as-is): content-keyed geometry/coverage object store; four-provider `ObjectClient` `[Union]` (S3/Azure/GCS/Minio); write-once `412`-noop seal as the concurrency primitive; per-row `ObjectChecksum`/`ObjectEncryption`/`ObjectLock` write stances applied on the wire; content-defined multipart with resume; write-blob-first + `InFlightFence`; routes ALL reclaim through the ONE `Version/retention` `RetentionSweep` (never a second sweeper). The nine-delegate `ObjectLeg` (the only per-provider code) is the right boundary shape.

Defects:
- **[dead typed carrier] `MultipartTransfer.Upload` + `BlobTransferReceipt` are orphaned.** `Upload` (blobstore.md:362) is the only path that emits a `BlobTransferReceipt` (:323), but NO wired caller invokes it — `store.Put` (:256), `Placement.Put` (:277), and `BlobGc.WriteBlobFirst` (:548) all call `provider.Put` directly and the live fact stream rides `BlobTransferFact` through the `sink`. The advertised `store.blob.*` `BlobTransferReceipt` is never produced by a composed path. Wire `Upload` as the real receipt entry or delete it and fold `BlobTransferReceipt` away.
- **[prose-vs-fence asymmetry] `ObjectChecksum` is a non-load-bearing column on 3 of 4 rows.** The Encryption/Lock prose claims "a per-provider leg silently dropping the column is the deleted form" (:16,80), but `ObjectChecksum` IS silently dropped on Azure/GCS/Minio: `Wire` returns `Some` only for `XxHash128` (:45-49) and is consumed only in `S3Leg.Seal` (:467). `AzureBlob`'s `ObjectChecksum.Crc64` (:238) is never applied — Azure's leg never reads `store.Integrity`. Honest in prose (:16 admits SDK-native fallback) but the "column honored on every wire" invariant holds for Encryption/Lock and NOT for Checksum. Either state the asymmetry in the `ObjectChecksum` doc or drop the decorative value.
- **[dead field, low] `BlobResidence.Correlation`** (`CorrelationId`, :160) is set to `CorrelationId.None` in `From` (:161) and never populated on any path.
Split/merge/move: none. Blobstore is a coherent single-owner page.

### Store/provisioning.md — 9.5
Charter (correct as-is): verification-first PostgreSQL 18 server tier (NEVER `ALTER SYSTEM`, never spawns PG) as ONE six-command `NpgsqlBatch` fold emitting typed `ProvisionVerdict` + `RepairArtifact`; the AUTHORITATIVE `ServerExtension` roster supersetting README with base-type rows; `ExtensionAdmission` install-gate `[Union]` (Preload/BaseType/AccessMethod/Standalone); `FailureRank.Absorb` delegate AS the absence policy (zero rank arms in the fold — exemplary AOP); embedded SQLite floor with the raw `Handle` bridge capsule (`EngineOps`: checkpoint/snapshot/backup/blob/integrity) and the closed two-engine sweep. Among the strongest pages in the package.

Defects:
- **[loose error codes beside a banded union]** `ServerFault` is a proper `Expected`-banded `[Union]` (835x, :210), but the `Fold`/`FailureRank`/`Admit` readiness and absence receipts use raw `Error.New(8371..8375, 8379, 8380)` (:72-74, :293-294, :321,326) — un-banded ad-hoc integers alongside the typed band. These 837x/838x codes are partitioned by prose only (see cross-cutting X1). Fold them into a typed carrier or register the sub-band.
- **[roster-vs-README consistency]** the fence carries 20 `ServerExtension` rows including `H3Postgis` (:133) which README folds parenthetically into the `h3-pg` line (README:75); superset relation holds (fence ⊇ README, as required) but the campaign should confirm the "two MUST agree" clause (README:60) treats the bridge row explicitly.
Split/merge/move: none. `#SERVER_EXTENSIONS`/`#EMBEDDED_FLOOR`/`#ENGINE_OPERATIONS` are three coherent sections of one provisioning owner.

### Version/ledger.md — 9.0
Charter (correct as-is): the ONE `OpLogEntry` changefeed PROJECTED from Marten events (bespoke op-log store RETIRED, H11); `(Family, Codec)` payload discrimination; `TraceSlot` distinct from AppHost `TraceContext`; `MergeStance`-row dispatch; the `FirstWriter` earliest-wins inversion; `Conserves` exact-partition invariant; the causal-fork `SyncFault.Forked` halt; `SubtreeCheckout` blob-manifest-vs-entry distinction; presence + lossy awareness lanes.

Defects:
- **[dead vocabulary] `SyncOpKind.Truncate`** (:31, `WholeRelation:true`) has NO minter and NO consumer reading `.WholeRelation` across all 9 pages — `Project` emits only `Upsert`/`Delete` (:152). `SyncOpKind.Presence` (:32) is weakly-wired (the presence lane mints a `PresenceRow`, not an `OpLogEntry` with this kind). Truncate is a dead case; delete or wire it.
- **[derivable-not-stored field] `OpLogEntry.Codec`** is always `SnapshotCodec.JsonStj` on every `Project`ed entry (:149,153); the `(Family, Codec)` "pair discriminates" claim (:15,100) is really `Family → Codec` (geometry/commit→JsonStj, crdt→MessagePack via `CrdtOpWire`). If no lane is bi-codec, `Codec` is a projection of `Family`, not an independent field. Confirm a lane needs both codecs or collapse `Codec` to a `Family`-derived accessor.
- **[boundary-kernel perf, low] `ChangefeedSubscription.ProcessEventsAsync`** (:119) runs one `sink(...).RunAsync(EnvIO)` per event sequentially in a `foreach`; a `TraverseM` fold of the projected batch then one `RunAsync` is denser for large `EventRange`s.
Split/merge/move: none. 4-section owner (changefeed/merge-law/transports/presence) is coherent; presence could theoretically move but shares the `ColumnFamily`/`OpLogEntry` spine — keep.

### Version/commits.md — 9.5
Charter (correct as-is): content-addressed commit-DAG (`XxHash128` over canonical `(SortedDistinctParents, SortedOpKeys, Hlc)`); maximal-antichain `MergeBase`; the six-type convergent CRDT algebra with total diagonal `(state,op)`/`(left,right)` switches; the flagship `CrdtOpWire` `[MessagePack.Union]` amendment; the `ContentParityCorpus` cross-runtime golden spine with the one-directional `Contribute` seam for the foreign `elementset` leg. Exemplary.

Defects:
- **[scaling] `CommitGraph.MergeBase` re-ranks from every common candidate.** `MergeBase` (:108-113) computes `Rank(left)`, `Rank(right)`, then inside `dominated` calls `Rank(resolve, candidate)` for EVERY element of `common` (:112) — O(|common|·rank-cost), potentially O(V²) on a criss-cross-heavy history. `Rank` (:132) is the sanctioned mutable-work-list BFS, but re-invoking it per candidate is the cost defect. A single reverse-reachability generation-mark pass, then dominance-filter, is near-linear. The page claims "the true merge-base set" without bounding cost.
- **[unconsumed-in-page, low] `VectorOrder`/`CommitGraph.Order`** (:38,100) — `Order`'s result and `.Ordered` are never read in-page; a consumer surface (merge/sync) presumably reads it, but no in-page fence composes it.
Non-defect (deliberate): `ParityVector.Digest` `Option<UInt128>` `None` on `Cell`/`CommitPreimage` (:386,392) is the declared design-pin gap (`Pin()` recomputes from `Canonical` at compare; digests stamp on host-validation) — correct deferral, not rot.

### Version/merge.md — 8.5
Charter (correct as-is): re-ingest `Reconcile` GlobalId alignment BEFORE diff (the H6 identity-survival mechanism); two-axis diff (Object forest via `Relationship.Compose` + non-topologized content nodes); Merkle-pruned `SubtreeHash`; one-pass conflict accumulation (`MergeOutcome` carries merged AND conflicts); member-granular RFC 6902 patch via graph `Inequalities`; derived `Family`/`Evidence` collapse over the seven conflict cases. Strong — but hosts the campaign's biggest latent gap (VC1).

Defects:
- **[SCOPE-MANDATE census-4 gap] `Reconcile` has NO alignment path for derived Type nodes.** `ExternalKey` (:283) returns `Some` ONLY for `Node.Object` with an `ExternalId` (the 1:1 IFC GlobalId). A `NodeRole.Type` node is a `Node.Object { Kind: ObjectKind.Type }` (:48,56) that enters the FOREST (`Forest` filters `Node.Object`, :156), yet a type definition carries no occurrence GlobalId, so `ExternalKey` → `None` → `Reconcile` does not align it. When the next-campaign Materials Type-path classification amendment RE-KEYS derived Type NodeIds, the NodeId-keyed `Diff`/`ThreeWay` reads every Type node as delete-all + insert-all — the exact defect `Reconcile` exists to prevent, solved only for ExternalId-bearing rooted nodes. This IS the store counterpart the mandate names. See VC1.
- **[verify-or-die] member-localization rests on an unverified Generator.Equals behavior.** `OwningNode`/`CellOf` (:246-249, :283-285) assume `ElementGraph.EqualityComparer.Default.Inequalities` emits `MemberPath`s whose `Nodes` dictionary-key segment `.Value is NodeId` (a boxed strongly-typed key, not a string). If Generator.Equals `[UnorderedEquality]` emits string keys, every content patch silently falls back to whole-node replace or empty. Must `assay api`-verify `MemberPathSegment.Value` typing. See VC9.
- **[STJ options gap] `Append` insert arm** (:390) does `s.Doc.Add($"/{key}", (object)Node)` — serializing a seam `Node` `[Union]` through the untyped `JsonPatchDocument` (:229) requires the Thinktecture/union converters, but no options threading is shown (contrast provenance.md:266 which threads `ElementJson.Options`). Confirm the JsonPatch serializer composes the union-aware converter or the Insert egress serializes wrong.
Split/merge/move: none — the two-axis diff + reconcile + patch is one coherent owner; the `Reconcile` aligner correctly is NOT a fourth diff.

### Version/retention.md — 9.5
Charter (correct as-is): six closed `RetentionClass` rows each carrying five decisions; the seam-local `RetentionCeiling` rank table (`DataClassification` carries no ordinal — the rank is a Persistence policy, correctly); the fail-closed `Unstamped` gate (absence of a seam rank ≠ clearance); the pure state-threaded newest-first `Sweep` fold; the full-history reachability `Mark` (never head); the ONE receipted executor (`evict` + `demote`); the `Cool` cold-tiering verdict; the `Conserves` partition. Among the two strongest pages.

Defects:
- **[dead column, low] `StorageLane.Durable`** (:36) is never read across the 9 pages; the sweep/executor route by the injected `evict`/`demote` delegates the caller wires per lane, not by `cls.Lane`/`Lane.Durable` — the "route by lane" (:19) is a classification-time fact consumed at the caller, so the bool is unread.
Non-defects (verified sound under hostile trace): the newest-first walk correctly reconciles count-and-size eviction in one pass (`state.Bytes` accumulates kept survivors, oldest-beyond-budget evict; :246-276); `HeldOverBudget` counts held bytes without evicting; `Conserves` closes over kept+held+cooled+evicted. `RetentionCeiling.Rank` hardcoding the classification ORDER (:103) is the RIGHT call — a genuine policy that must live somewhere, parameterized as one-row-extensible.
Split/merge/move: none.

### Version/recovery.md — 9.0
Charter (correct as-is): `RecoveryRoute` substrate axis (pg-pitr/object-replica/snapshot-archive); the REAL `RecoveryPoint` `(Timeline, Lsn)` coordinate (never wall-clock); the verified reverse-ladder restore choreography with generated `RestoreStep.Switch`; the head-caught-up proof via `daemon.WaitForNonStaleData` (VERIFIED real); the re-attest content-identity commit point reusing provenance's independent-digest `AttestedLedger.Verify` (`Unauthored` reachable).

Defects:
- **[logic bug, unit conflation] `ObjectReplica` RPO is a count spelled as minutes.** `ObjectReplica` (:181) returns `Duration.FromMinutes(absent.Count)` — the COUNT of missing content keys AS MINUTES. A count is not a time lag. The Auto/Boundary prose says the RPO is "the freshest-blob-minus-newest-event lag" (:15,177) — which is NOT what the fence computes. Prose-vs-fence divergence + dimensional error on a load-bearing DR gauge that feeds `objective.MeetsRpo` (:151). See VC6.
- **[magic-constant heuristic] `PgPitr` WAL-lag→Duration** (:172) divides `lagBytes` by `16d*1024*1024` "at the nominal 16-MiB WAL-segment rate" — reusing the WAL SEGMENT SIZE literal as a per-second THROUGHPUT rate (dimensionally: bytes/bytes = seconds only if 1 segment/sec). Declared as an approximation (:161) but the constant conflates size with rate; parameterize the throughput assumption as a policy value or read the archive cadence.
Non-defect: `RecoveryPoint.AsCut` (:65) cleanly one-hop-bridges to timetravel's `TimeCut.AtVersion`/`Of` (verified against timetravel.md:65-67). `WaitForNonStaleData` (:314) is correct (daemon-instance method; ARCHITECTURE.md prohibition names the store-extension `WaitForNonStaleProjectionDataAsync` — both real, two altitudes; recovery holds the daemon so the daemon method is right).
Split/merge/move: none.

### Version/provenance.md — 9.5
Charter (correct as-is): W3C-PROV-O exact vocabulary (11 influence terms with endpoint law + derivation-subclass generalization); exact typing (`WasAssociatedWith` activity→agent vs `WasAttributedTo` entity→agent); revision off each parent commit's `OpKeys` entities (NEVER `Closure.Head`, a blob set); the attested KMS-signed hash-chained ledger composing `Authority.Verify` with an INDEPENDENT `digestOf` recomputation (the mechanism that makes `Unauthored` reachable — illusory-verify avoidance); the Merkle inclusion/consistency transparency proofs; the bounded frontier walk. The strongest page in the package.

Defects:
- **[hardcoded agent class, low] `ProvJson` bundle authority** (:257) stamps `AgentClass.Person.ClassIri` and `rasm:signed=false` unconditionally; an Organization/SoftwareAgent authority mis-types. Read the authority's class off the `Principal` instead.
- **[unmarked mutation-boundary, low] `ProvJson`** (:243-284) mutates `Dictionary` accumulators (`entities[...]`, `agents[...]`) imperatively in a domain projection; it is the JSON-building boundary kernel but is not marked as the platform-forced statement-seam exemption the other pages annotate.
Model to propagate: the independent-`digestOf` verify (`AttestedLedger.Verify`, :364) is the correctness template recovery's `ReAttest` correctly reuses — cite it as the federation-wide verify shape.
Split/merge/move: none.

### Version/timetravel.md — 9.0
Charter (correct as-is): the ONE materializer (`GraphDelta.ReplayOnto`, identical to the inline projection — no second prefix fold); `AggregateStreamAsync(version|timestamp)` AS-OF over the periodic Marten snapshot floor; the two-axis `RangeDiff` (node `Inequalities` + edge content-set-diff — the topology-drop avoidance is a real insight); per-node `Blame` with superseded lineage; `Scrub` reel; monotone `Bisect`; branch-from-past; the content-addressed checkpoint chain (reproducibility, explicitly defers authenticity to provenance).

Defects:
- **[SCOPE-MANDATE census-5 pressure] Scrub/Bisect are the heaviest whole-graph `ContentAddress.OfGraph` consumers.** `Scrub` (:234-236) calls `ContentAddress.OfGraph` TWICE per frame (`Before` on `acc.Graph`, `After` on `next`) — 2N order-independent whole-graph `XxHash128` passes for an N-event window; `Bisect` (:253,258-259) calls it per probe over full `ReconstructAt` reconstructions. This IS the "streaming/incremental `ContentAddress.OfGraph` kernel-owner boundary pressure" the mandate names. The frame ALREADY carries the delta, so a delta-foldable `OfGraph` collapses Scrub from O(N·|graph|) to O(N·|delta|). See VC2.
- **[seam-overload consistency] node content address is computed two ways.** timetravel `Deltas` (:187-188) uses `ContentAddress.Of(n, a.Header.Tolerance)` (a `Of(Node, Tolerance)` overload); merge.md GeometryDigest/PropertyHash uses `ContentAddress.Of(o.ToCanonicalBytes(tolerance).Span)` (merge.md:160, the span overload). Same content address, two spellings. Verify `Of(Node, Tolerance)` exists on the seam (assay api) or unify on the span form. The `#CONTENT_ADDRESS` seam (Element/codec, corpus-a) owns the overload set — flag for that lane.
- **[package-vs-fence drift, low] `AggregateStreamToLastKnownAsync`** is listed in Packages (:16) but composed by no fence.
Non-defect: Scrub `Before`/`After` seeding is correct (`seeded = ReconstructAt(head.Version-1)`, :230); the two-axis diff's edge content-set-diff correctly recovers topology the node `Inequalities` drops.
Split/merge/move: none.

---

## CROSS-CUTTING

### Duplication
- **Rolling `XxHash128`-over-`UInt128` idiom recurs ~6× across 3 pages** (timetravel `ChainHash` :154; provenance `Append` :348, `Pair` :417, `Bundle` :229; commits `Preimage` :94, `MerkleRange.Of` :116). Each is a 3–5 line domain-specific preimage with DISTINCT byte layout, and the two rolling CHAINS (provenance authenticity vs timetravel reproducibility) are DELIBERATELY separate concerns the design distinguishes (provenance.md:310). NOT a collapse target — intentional parallel mechanism for distinct concerns. commits already centralizes its writers in `ContentParityCorpus`. Observed, justified.
- **`ContentAddress.Of*` overload set** (`Of(UInt128)` blobstore, `Of(ReadOnlySpan<byte>)` merge, `Of(Node, Tolerance)` timetravel, `OfGraph(ElementGraph)`) is polymorphic-by-input-shape (sanctioned), EXCEPT `OfGraph` is a parallel NAME rather than `Of(ElementGraph)`. Seam-owned (Element/codec, corpus-a); flag the `OfGraph`-vs-`Of` naming split for that lane. Low.

### Concern mixing / folder architecture
- **`Store/` pairs two heterogeneous storage concerns** (remote blob bytes vs relational-engine provisioning) sharing no types/seams — acceptable (both are durable-home owners, and a third backend from README STORE_BACKENDS/EMBEDDED_KV lands as a Store page, so it IS a growth axis), but the folder cohesion is weaker than Version/. Low.
- **`Version/` is a coherent 7-page domain** (all project from the Marten changefeed) at the upper bound of a flat folder. If it grows, the natural sub-partition is core (commits/ledger) / read (timetravel/merge) / governance (provenance/retention/recovery) — but splitting now would fragment the shared `OpLogEntry`/`Hlc`/`ContentAddress` spine. Keep flat; note the axis.
- **[package-level] TASKLOG/IDEAS anchor a PRE-RESTRUCTURE page set the 18-page tree no longer contains.** Closed cards cite `Sync/egress.md`, `Sync/annotation.md`, `Sync/schedule.md`, `Query/{federation,transaction,pipeline}.md`, `Store/quality.md`, `Version/snapshots.md`, `Schema/ddl.md` — none exist in the tree. Corpus-b-relevant: `Version/snapshots#SNAPSHOT_PROTOCOL` (TASKLOG ICECHUNK_ASOF) is ABSORBED into `Element/codec#SNAPSHOT_SPINE` (confirmed — recovery/timetravel consume `Element/codec#SNAPSHOT_SPINE` `Snapshots.Verify`, not `Version/snapshots`); the icechunk `AsOfKey` concern needs an explicit home. The CDC streaming-egress concern (and its ~10 admitted packages, below) has NO Version/ or Store/ owner in the tree. Flag for align-cards/hygiene — this is package-governance drift, not a corpus-b page defect.

### Hardcoding-vs-generator
- Corpus-b is CLEAN on the generator axis: `ServerExtension`/`ObjectStore`/`RetentionClass`/`RecoveryRoute`/`RestoreStep`/`ColumnFamily`/`ProvRelation`/`CrdtField`/`CrdtOpWire` are all `[SmartEnum]`/`[Union]` + FrozenDictionary data-table dispatch where rosters are seed DATA (a new provider/extension/class/route/relation/CRDT-type is one row). `FailureRank.Absorb` and `MergeStance` dispatch are the AOP-as-data model done right. The only "hardcoded" tables (`RetentionCeiling.Rank` order :103; `RetentionCeiling.Colder` ladder :121) are correct policy tables, one-row-extensible.

### Dead carriers / unwired seams
- `MultipartTransfer.Upload` + `BlobTransferReceipt` (blobstore, orphaned receipt path).
- `SyncOpKind.Truncate` (ledger, dead case); `SyncOpKind.Presence` (weakly wired).
- `OpLogEntry.Codec` (ledger, derivable-from-Family candidate).
- `BlobResidence.Correlation`, `StorageLane.Durable` (unread fields).
- `AggregateStreamToLastKnownAsync` (timetravel, listed-not-composed).

### Fault-band federation (X1 — a genuine structural finding)
Every corpus-b fault union derives from the KERNEL `Rasm.Domain.Expected` via `[SkipUnionOps]` + parameterless `:base()` + per-case `Code`/`Message`/`Category` (NOT `LanguageExt.Common.Expected`) — exemplary consistency (the `using Expected = Rasm.Domain.Expected;` alias in every prelude). Bands are DISJOINT: RemoteStoreFault 540x, EmbeddedFault 771x, SyncFault 825x (+ commits 8261/8263/8264), RetentionFault 828x, RecoveryFault 829x, ServerFault 835x (+ provisioning loose 8371–8375/8379/8380). BUT: the Component-Paradigm campaign just type-enforced AEC-domain band disjointness via the `FaultBand` `[SmartEnum<int>]` registry (2300–2700, "a duplicate integer fails the generated key lookup at type initialization"), while these 6+ app-platform bands remain partitioned by PROSE convention across the pages — a future page picking `8257`/`8282` collides silently. The corpus-b bands sit in 5xxx/7xxx/8xxx, clear of the 2300–2700 registry, so no collision today, but they lack the type-enforced disjointness the AEC leg gained. See VC5.

### Unmined / zero-consumer capability (catalog anchors)
- `PollinationSDK` (`.api/api-pollination-sdk.md`, README `[VERSIONING_SYNC]`) — no corpus-b consumer; likely Compute/energy or unbuilt. Verify owner or roster fiction.
- Streaming-egress set — `Confluent.Kafka`, `Confluent.SchemaRegistry(.Serdes.{Avro,Protobuf,Json})`, `NATS.Net`, `RabbitMQ.Client`, `DotPulsar`, `CloudNative.CloudEvents(.Kafka/.SystemTextJson)`, `Chr.Avro(.Binary/.Confluent)` (README `[STREAMING_EGRESS]`/`[MESSAGING_PROTOCOLS]`/`[INTERCHANGE_CODECS]`) — back the `Sync/egress` CDC concern that has NO page in the tree. Either an absorbed owner (name it) or ~10 zero-consumer packages (the geometry-brief-style roster-fiction defect).
- STORE_BACKENDS (`ClickHouse.Driver`/`ScyllaDBCSharpDriver`/`Qdrant.Client`/`DeltaLake.Net`) and EMBEDDED_KV (`rocksdb`/`LightningDB`) — no corpus-b consumer (correctly — Query lanes / corpus-a own the columnar/vector/KV concerns; provisioning correctly CLOSES its embedded RELATIONAL sweep to SQLite, :377). Flag for corpus-a confirmation, not a corpus-b defect.
- Correctly-scoped-out of corpus-b: the KMS SDKs (`AWSSDK.KeyManagementService` etc.) are `Element/identity#AUTHORITY`'s (corpus-a); blobstore correctly stamps only the SSE key-id STRING (blobstore.md:528). No under-mining.

### Cross-lane seam observations (for corpus-a author)
- The geometry-brief `[V2]` PARAMETRIC CONTENT IDENTITY seam ("kernel emits the digest, Persistence mints the key") lands on `Element/codec#CONTENT_ADDRESS` (corpus-a). blobstore's four-key representation map `Body/Box/Axis/FootPrint` (blobstore.md:3) is the downstream blob consumer that gains a parametric-representation key slot IF the seam adds a parametric `RepresentationIdentifier`. NOT a waterfall edit into RASM-CS-GEOMETRY-BRIEF (the brief already assigns Persistence as key-minter; the owner is codec, corpus-a).
- NO waterfall edit into any upstream brief is warranted from corpus-b: the two kernel-boundary pressures it surfaces (parametric identity, streaming `OfGraph`) both land on `Element/codec` (corpus-a's seam-counterpart), and the geometry brief already assigns Persistence as the consumer/minter.

---

## VERDICT CANDIDATES (campaign-defining, evidence-anchored)

**VC1 — merge.md `Reconcile` has no cross-ingest alignment for derived Type nodes (SCOPE-MANDATE census-4 store counterpart).** `ExternalKey` aligns ONLY `Node.Object` with an `ExternalId` (merge.md:283); a `NodeRole.Type` node is an ExternalId-less Object that enters the forest (:48,56,156), so the next-campaign Materials Type-path re-key forks every Type node as delete-all+insert-all — the exact defect Reconcile prevents, solved only for rooted GlobalId-bearing nodes. RULING: either Type nodes gain a stable cross-ingest correlation key `ExternalKey` reads (a Type-path/classification-derived stable id mirroring the ExternalId path), OR the Type-seed excludes Classifications from the Type NodeId preimage (like Representations are excluded) so Type NodeIds don't re-key. The mandate's "exclude Classifications from the seed like Representations, or re-key with migration" — merge.md is the consumer that breaks without option (a).

**VC2 — timetravel Scrub/Bisect are the streaming-`ContentAddress.OfGraph` consumer (SCOPE-MANDATE census-5).** Scrub hashes the WHOLE graph twice per frame (`Before`+`After`, timetravel.md:236); Bisect hashes per probe (:253,259). Record the consumer contract: a delta-foldable `ContentAddress.OfGraph` (the frame already carries the delta) collapses Scrub from O(N·|graph|) to O(N·|delta|). NEVER re-home the hasher (kernel/Element-codec owns it); the contract is a delta-composable OfGraph variant the Scrub reel folds.

**VC3 — SCOPE-MANDATE census-1 & census-2 are CLEAN for corpus-b, with precise evidence.** (1) The count-prefix amendment forks merge.md `PropertyHash` and timetravel node/edge `ContentAddress` TRANSPARENTLY (fences compose the seam `Node.ToCanonicalBytes` writer, merge.md:160/173, timetravel.md:187/203); NO corpus-b page pins a golden node-canonical vector (commits `ContentParityCorpus` pins only hlc/commit/crdt/elementset — none node-canonical, commits.md:365-368); the op-log `OpLogEntry.ContentKey` is JSON-codec-derived (`XxHash128` over `JsonStj.Serialize`, ledger.md:149-153), NOT canonical-bytes-derived, so it does not even fork. Corpus-b absorbs the amendment with ZERO edit — "the amendment IS the migration." (2) The Fabrication 25xx→27xx re-band: NO corpus-b fault band is in 25xx (bands 540x/771x/825x/828x/829x/835x); NO corpus-b persisted row/receipt/op-log/blob-catalog carries a Fabrication fault integer (receipts carry Persistence's own typed bands). The `[LEG-FABRICATION]` persisted-boundary check passes trivially for corpus-b.

**VC4 — the corpus-b Version engine IS the durable consumer the queued PERSISTENCE_GRAPH_REBUILD realizes (census-3).** ledger/commits/merge/timetravel consume the ElementGraph + GraphDelta event body. The member contract the rebuild must preserve or the consumer forks: `GraphDelta.{AddedNodes,RevisedNodes(.Before/.After),RemovedNodes,AddedEdges,RemovedEdges}` (timetravel `Touched`/`Scrub` :297-301, ledger `Closure` :164, merge `Reconcile`), `EventLifecycle.Retired` (ledger :152, timetravel `ScrubFrame`), `Node.Object.Representations.ByIdentifier` (ledger `Closure` :167, merge `GeometryDigest` :305), `GraphDelta.ReplayOnto` (timetravel — the ONE materializer :235), `ElementGraph.EqualityComparer.Default.Inequalities` + `MemberPath` NodeId-key segment (merge patch-localization :237, timetravel `Deltas` :179). Record as the consumer contract the graph rebuild honors.

**VC5 — extend the federation `FaultBand` registry to the Persistence app-platform bands.** The Component-Paradigm campaign type-enforced AEC-domain disjointness (2300–2700 `[SmartEnum<int>]` registry, "duplicate integer fails at type init"), but the 6+ corpus-b bands (540x/771x/825x/828x/829x/835x) + loose 82xx/83xx receipt codes remain convention-disjoint via prose (blobstore.md:166-177, provisioning.md:202-235/72-74, ledger.md:230-277, retention.md:56-92, recovery.md:70-117). A future page picking 8257/8282 collides silently. RULING: mint a Persistence-local band registry (or extend `FaultBand` with app-platform rows) so these bands get the same type-enforced disjointness; the per-page raw `Code => 82xx` literals + the un-banded `Error.New(837x/838x)` receipts are the pre-campaign shape the AEC leg just retired.

**VC6 — recovery.md `ObjectReplica` RPO is a unit-conflation logic bug.** `Duration.FromMinutes(absent.Count)` (recovery.md:181) spells a COUNT of missing content keys as MINUTES, contradicting the page's own "freshest-blob-minus-newest-event lag" prose (:15,177) and feeding a false value into `objective.MeetsRpo` (:151). The RPO must be a time lag; compute freshest-replicated-blob age vs newest-event instant. (Secondary: `PgPitr`'s 16-MiB-segment-size-as-throughput-rate magic constant, :172, is a declared but dimensionally-loose approximation to parameterize.)

**VC7 — ledger.md dead vocabulary + derivable field.** `SyncOpKind.Truncate` (`WholeRelation`) has no minter and no `.WholeRelation` reader across all 9 pages (ledger.md:31,152) — a dead case; `OpLogEntry.Codec` is always `JsonStj` on `Project` and appears derivable from `Family` (crdt→MessagePack, else→JsonStj; ledger.md:109,149-153,146). Delete/wire the case; collapse `Codec` to a `Family`-derived accessor unless a lane is bi-codec.

**VC8 — blobstore.md orphaned receipt path.** `MultipartTransfer.Upload` + `BlobTransferReceipt` (blobstore.md:362,323) are never called by any wired path (`store.Put`/`Placement`/`WriteBlobFirst` call `provider.Put` directly, :256/277/548); the advertised `store.blob.*` `BlobTransferReceipt` is never emitted (the live stream uses `BlobTransferFact` via `sink`). Wire `Upload` as the real receipt entry or delete it and fold `BlobTransferReceipt`. (Secondary: `ObjectChecksum` is a non-load-bearing column on Azure/GCS/Minio, contradicting the Encryption/Lock "honored on every wire" invariant, :16,45-49,467.)

**VC9 — merge.md member-granular patch rests on an unverified Generator.Equals behavior (verify-or-die).** `OwningNode`/`CellOf` (merge.md:246-249,283-285) and timetravel `CellOf` (timetravel.md:283-285) assume `Inequalities` emits `MemberPath`s whose `Nodes` dictionary-key segment `.Value is NodeId` (boxed strongly-typed key). If Generator.Equals `[UnorderedEquality]` emits string keys, every content-node patch silently falls back to whole-node replace/empty — the entire member-granular RFC 6902 egress degrades. `assay api`-verify `MemberPathSegment.Value` typing against the restored `Generator.Equals`/seam assembly before the leg composes it.
