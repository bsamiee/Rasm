# [MATERIALS_IDEAS]

Forward pool of higher-order concepts for the host-neutral materials owner, each grounded in the folder's domain and current platform capability. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
Capability, Shape, Unlocks, and Anchors are required on every open card; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Ideas state higher-order concepts, never landing-grain tasks.
-->

(none)

## [02]-[CLOSED]

<!-- source-only: closed idea card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[MATERIAL_SYMMETRY_AUTHORITY]-[COMPLETE]: landed as the `ComponentFamily` symmetry column binding three named `ComponentSymmetry` producers (Grain · Stack · Isotropic) with `Component.Symmetry` the ONE derivation member projecting construction rows onto the kernel `MaterialSymmetry` — timber reads Half with the blank-turn mirror on every admitted arm, the new `PlyRole.Oriented` column (Longitudinal/Transverse/VeneerPly/StrandLayer true) drives the stack fold with palindrome-gated Turned/Refused mirrors, Corrugated reads Half/Reflective off the flute axis, and the fifteen isotropic families read Free; masonry stays isotropic by declared boundary (`BondGeometry` is ASSEMBLY symmetry, never the unit substance's), the derivation under-grants and never over-grants, and `MirrorGrant.Matched` book-match and the `SymmetryFold.Fixed` nap vector are declared Growth triggers awaiting a two-sourced construction fact; consumers (`PatternPlan.Law`, `PanelPolicy.Law`, the `Rasm.Fabrication` `[DIRECTIONAL_STOCK_NESTING]` move gate) read the projection, never re-derive.
[CONSTITUENT_SET_PRODUCER]-[COMPLETE]: resolved as a DERIVATION LANE, never an external recipe registry (the card's own refutation held — registries publish impacts, not recipes): `ConstituentRecipe` (`Mix` · `Layup`) and the one `Constituents.Of` producer landed beside `CompositionAuthor`, the `Mix` arm folding the new `Properties/properties#MIX_PROPORTION` catalogue — EN 206:2013 Annex F Table F.1 whole (18 exposure classes) and the ACI 211.1-91 SI proportioning tables (water/air, w/c-strength anchors, coarse-fraction lattice), 326 cells acquired two-source with zero absent — through the absolute-volume fold under the EN exposure floor (by-test columns caller-required, printed dashes typed-absent, out-of-lattice reads refusing), the `Layup` arm deriving thickness × catalogued density with a density-less ply railing by name; `Capture` gained the `recipeOf` resolver (the `setOf` mirror) selecting `CompositionAuthor.ConstituentSet` — the fourth seam case carrying traffic — with the author tuple widened to the seam's own `PartName` column; the `IfcMaterialConstituentSet` egress chain verified both directions at `Rasm.Bim` `Semantics/composition.md` and the Compute rule-of-mixtures reads the normalized fractions; the recipe-declaration ruling landed at RULINGS `[02]`.
[PATTERNPLAN_NEUTRAL_CURRENCY]-[COMPLETE]: the kernel counterpart landed — `Rasm/Parametric` patternmap's 2D pattern currency is neutral `(U, V)` doubles end to end (plan basis/anchors/root, seat shifts, `PatternLattice.Admits`, `InstanceStream.Planar.Site`, `Mapped.Uv`) with `Point3d`/`Vector3d` confined to the `Mapped` world lane and the `Map` arm's `UvTessellation` charter machinery; the kernel `hatch.md` consumer swept, the masonry `[KERNEL_EDGE]` block deleted to plain composition, and the RULINGS carve RE-CUT rather than retired — the card's one-crossing premise undercounted: the environment sun-direction lift over the press subject's `Vector3d` closure remains the folder's landed instance, with the masonry retirement recorded as the proven path. The Component tree is host-free.
[SSMA_SEFF_CHECK_VALUES]-[COMPLETE]: 102 check cells landed two-source as `steel.md` `PublishedSeff` (Se/Sx at Fy = 50 ksi over the 54/68/97-mil band the SSMA guide itself prints at Fy50; AISI S100-07/-12 print lineage, four independent corroborating publishers, intra-lineage agreement exact to the printed digit) folded into `ColdFormedSections.Rows` construction; the census honest — 21 designations no publisher lists, 5 single-sourced, the 33/43-mil Fy50 prints manufacturer-only, and the 2.5"-flange 33-mil rows past the AISI B4.1 w/t 60 bound all stay typed-absent; the S100-16/S2-20 print lineage differs up to ~4% on Se, so a drift breach names print-basis divergence before it names the algorithm.
[EPD_DATA_INGESTION]-[COMPLETE]: arm (a) landed UNIVERSALLY — the `declaration-record` DOMAIN contract at `tests/contracts/MANIFEST.md` `[02.26]` (canonical JSON, the frozen 13-indicator × 15-module rosters, KEY PRESENCE the coverage census), `python:data` `impact/declaration.md` the one producer (Ökobaudat/ILCD whole through `epdx`, EC3 on the same axis; the python `[ASSESSMENT_RECORD_WIRE]`/`[EPD_RECORD_WIRE]` cards closed at their tier), and the C# leg `DeclarationWire.Decode` banding declaration modules onto the six-band `LifecycleStage` axis with the full `Matrix` constructed only on full coverage, reaching `EpdRow`/`AssessmentSet.Of` unchanged; `EpdRow`'s required resource fractions widened to `Option` (the seam `OfEnvironmental`'s own pair — the forged-zero repair); NO TS leg — zero TypeScript consumers verified, a decorative mirror refused; both ARCHITECTURE registries carry the `DeclarationRecord` edge.
[EN_TIMBER_GRADE_TABLES]-[COMPLETE]: the class space landed whole under the corrected premises — EN 338:2016 all 12 C + 14 D classes with the derivation equations seated at their real owner (EN 384:2016 Table 2, softwood/hardwood axis, 44 hand-typed cells deleted as derivations), EN 14080:2013 all 14 GL h+c classes with fr,g,k/Gr,g,mean, CLT rolling shear re-seated to EN 16351:2021, the eight disputed hardwood E90 cells honestly out; two-source verified cell by cell.
[PANEL_LATERAL_CAPACITY]-[COMPLETE]: the atomic unit landed whole — three frozen SDPWS-2021 seed tables (140 two-source cells, single nominal column exactly as the tension law legislated, ℓₘ bearing-length field truth, AWC-primary-only cells typed-absent) beside `GoverningAction.InPlaneShear`, the railed §4.1.4 `LateralHazard` factor derivation, `DesignBasis.Sdpws`, and the `LateralPanel` case/receipt/`Check` arm with reduction applied exactly once at the family producer; sheathing selection is lateral design end to end.
[MICROFACET_GENERIC_MATH]-[COMPLETE]: landed whole across all four pages — `LocalVector<T>`/`Scalar<T>` anchor, `Microfacet<T>` generic over the six-interface constraint set declared once on the type, every lobe at `LocalVector<double>`, and `acquisition` on `BrdfResidual : IDualResidual` over `Lm.Minimize(new DualModel(residual), SolvePolicy.Canonical, key)` with the differentiable box reparameterization; the hand Gauss-Newton loop, its damping, the QR/SVD fallback, and the central-difference Jacobian all deleted with the EXPRESSION_SPINE exemption; zero bare spellings remain (census-proved). The kernel `Dual<T>` generic-math conformance rode the same pass as the one exposed substrate gap.
[COAT_ANISOTROPY]-[COMPLETE]: the coat grain landed wider than carded — Clearcoat AND ThinFilm both take the (AlphaX, AlphaY, Rotation) triple off the shared Aspect remap (an isotropic film under a brushed lacquer lost the grain at every nonzero film weight), `Slab.Coat`/`OpenPbrSurface`/`MaterialParameters` carry CoatAnisotropy + CoatRotation, and `geometry_coat_tangent`'s shading consumer is the scalar rotation per the anisotropy ruling.
[SKY_AS_PRESS_SUBJECT]-[COMPLETE]: both halves landed — `PressSubject.Sky` carries the radiance closure with `PressProgram.Dome` and the CubeFaces band arm at press, and `SkyRender.Render`/`SkySweep` DELETED at environment with `SkyRender.Radiance` returning the closure the subject calls; one bake engine owns partitioning, cancellation, receipt, and the accelerator lane, the host lift confined to one [KERNEL_EDGE] site.
[IBL_PREVIEW_PRODUCT]-[COMPLETE]: `IblProduct` union (Minted | Preview) mirrors the press structural split — `EnvironmentBlobs` reachable only from Minted so the GPU product is structurally un-keyable; the accelerator arm ARMED, dispatching `irradianceSh` + per-mip `prefilterSpecular` through `PressDevice.Dispatch` with the uniform blocks matching the kernels' Params word for word; Preview omits BrdfLut/Cdf honestly.
[WEATHERING_COLUMN_LENSES]-[COMPLETE]: the hand-widened `SlabColumnDelta` roster died — `SurfaceDelta` rides a type-init-derived `SurfaceColumn` roster off the surface vector via `nameof`, Slab/MaterialParameters appliers explicit with the boundary stated, so a new surface column (the coat-anisotropy widening proved it) is an aging target with zero weathering edits.
[CURVATURE_DRIVEN_WEAR]-[COMPLETE]: both halves landed — `SurfaceExposure(Occlusion, Curvature)` with `CavityResponse.Scale(age, exposure)` and `Convex`/`Concave` rows at weathering, the second `CurvatureField` source and the `Curvature` ladder axis at press with the cell product priced (16×8×4 = 512, `Curvature: 1` the declared opt-out, per-texel `Apply` the growth leg); the signed curvature channel has its aging consumer.
[TEXTILE_LEARNED_SCORER]-[COMPLETE]: the capability landed whole with the one candidate honestly refused — tile records the optional learned reading as a frontier-supplied closure (deterministic half authoritative; a thrown scorer measures nothing and faults no grade), neural carries `ScoreField`/`StageProduct.Measure` as the scalar-modality machinery, and TexTile enters the registry at `LicenseClass.Blocked` (weights licence-silent, probed live) with its captured fixed-shape contract, granting nothing until a card speaks.
[FABRICATION_SCHEDULE_WIRE]-[COMPLETE]: end to end — the `ScheduleKind` rosters re-cut into TYPE rows (Materials type bags publish: bar identity + bend block, weld throat + part thickness + prep, stud identity + pitch) and OCCURRENCE rows (Fabrication detailing authors: cut length, carried-member dims, edge distance) over the merged `OccurrenceWins` bag, every name corresponding cell for cell, `Rasm.Element` declaring the eight shop-deliverable statics; a realized component reaches a shop document with zero silent drops.
[CONNECTOR_CATALOGUE_BREADTH]-[COMPLETE]: the ICC-ES tables proved publicly obtainable — 25 sourced rows across hurricane ties, post caps and bases, purlin ties, top-flange and concealed-flange hangers, and drag struts landed with per-row evaluation-report identity and the F1/F2 lateral axis, every fabricated allowable replaced by a sourced value or typed absence.
[UDIM_STREAMING]-[COMPLETE]: per-tile residency landed with the key whole — `Raster/plane#PLANE_RESIDENCY` (`ResidencyPolicy` rank-projection rows, `ResidentTile`, texel-budget eviction over a caller-supplied mint thunk) and `Raster/set` `UdimResidency` with the `Declared`/`Tiles` split, so a bind resolves one tile without decoding the grid and the set key stays over the full declared grid; the tension resolved as designed — residency is read-time policy, never key input.
[ATLAS_PACKER]-[COMPLETE]: `Raster/set` `TextureAtlas`/`AtlasPlacement` land the packing fold over N sets — shared per-channel planes referenced by content address with per-set UV transform rows, each set keeping its own key and appearance identity, `Dilate` closing the inter-chart gutter.
[MESH_SPACE_BAKE]-[COMPLETE]: `Raster/press` `PressSubject.MeshSpace` carries the flattened chart set as DATA (`ChartTexel` rows off the kernel `ChartAtlas` product — no host mesh crosses) with `PressProgram.Surface`, so occlusion, thickness, and curvature are measured `ChannelOrigin.Geometric` producers; the GPU lane refuses per the CPU-mint veto and the height-field folds stay the declared fallback origin.
[MAGICK_BREADTH]-[COMPLETE]: the arm was met by runtime probe (native delegates measured on osx-arm64; AVIF and JXL round-trip, HEIC read-only, no KTX coder) — `Raster/codec` `RasterEngine.Breadth` landed ingest-only with `avif`/`heif`/`jxl`/`dpx`/`cineon` rows, encode structurally refused (the case carries no encoder), and the folder RULINGS row re-cut from DECLINED to ADMITTED-ingest-only.
[TEXT_TO_MATERIAL_SEAM]-[COMPLETE]: the estate-side landing is whole — `Raster/set` `IngestProvenance` carries the declared licence/provenance token a service response fills, and `SetIngest.Classify` admits a service product with no new `IngestSource` case (Stems/Declared already cover it); a service contract remains an operator-side procurement fact, not estate capability.
[ASSESSMENT_WIRE_INGESTION]-[COMPLETE]: `Properties/assessment.md` landed whole and defect-drained — `AssessmentModality`/`ConditionGrade`/`AssessedProperty` with per-row `PropertyLanding` lenses, the `AssessmentRecord` union on one hoisted `AssessedIdentity`, `AssessmentAdmission.Admit` onto the shared `Published<T>`, `AssessmentResolution.Resolve` over the memoized catalogues, and `EpdRow` with full-matrix admission; the peer transport is structurally refuted (the `python:data` impact wire carries no material identity, dates, or coverage census), so the seam edge exited the maps and the residue rides `[EPD_DATA_INGESTION]` arms.
[OPENPBR_GROUPS_MAP_COLUMNS]-[DROPPED]: plane storage remains on `TextureSetWire` and sampling policy on the per-bind `UvFrame`; adding either to scalar `OpenPbrGroupsWire` duplicates owner truth and loses pack, level, layer, UDIM, transfer, and coordinate-set semantics.
[DESIGN_BASIS_AXIS]-[COMPLETE]: the jurisdiction axis landed as `capacity#SECTION_CAPACITY` `DesignBasis` — one policy row carrying the authority body, `SafetyFormat`, γM0/γM1, the `NationalAnnex`-threaded `IStandard` citation, and the combined-action kernel every `Check` arm folds through — with the case family re-cut off code names onto family+modality so a second code is a ROW; `steel#STEEL_FAMILY` prices the EN 1993-1-1 χ/χLT arm over per-class Table 6.1/6.4 imperfection factors and `masonry#MASONRY_FAMILY` the EN 1996 Table 3.4/3.5 `FlexuralStrengthEn` sibling, so the EN-seeded catalogue half receives an EC3/EN 1996 verdict through the one rail. Tension resolved as designed: the `Rasm.Compute/Analysis/structural#DESIGN_CHECK` co-sign landed the same pass — the key set is shared, the `en1994`/`en1996` rows flow through the standing capacity table, and no parallel verdict family exists.
[IFC_PRODUCT_LIBRARY_ADMISSION]-[COMPLETE]: the reverse type path landed at `component#CATALOGUE` `AdmitImported` — reconciled `TypeCandidate` rows (the co-signed `Rasm.Bim` `ExportTypeCandidates` mint, spelled locally by contract alignment exactly like `IIfcTypeReconciler`) lower onto family-dispatched railed `Component.Of` construction through a genuine reverse `IfcBinding` read with ambiguous claims electing nothing, imported rows provenance-marked `ComponentOrigin.Imported` beside the seeds in one `ComponentId` space; the tension resolved as designed — the reverse mint is Bim-side, the provenance column co-signed here.
[TYPE_QUANTITY_RECEIPT]-[COMPLETE]: the seam co-sign was never a foreign ask — `Rasm.Element` gained three owner-declared `DetailSchema` takeoff statics under a `Takeoff` bag at `InheritanceMode.TypeDrivenOverride`, driving every occurrence off one Type-bound bag with no `Bake` edit, and a `Density` accessor total over both stiffness carriers; `TypeTakeoff` mints the set at `Projection/component#COMPONENT_PROJECTOR`, and `LinearDensity`/`VolumePerLength` being real UnitsNet registry quantities is what clears the dimension check a consumer mint answers `None` on.
[CATALOGUE_ANALYTICS_EGRESS]-[COMPLETE]: `Projection/analytics.md` rebuilt onto the custodian seam — the folder-local `ColumnType`/`ColumnRow`/`AnalyticsSchema` twin died, `ColumnToken`/`DatasetColumn`/`DatasetWire` declare the producer half of `[WIRE]: AnalyticsSchema` with `Admission` the crossing, and each of `MaterialsDatasets`' five rows declares `observed` as its spine beside the `gwp` and `elapsed_s` measures, so Series, Fleet, and Lake provision one declaration; the folds thread `ProjectionContext` and `PropertyColumn` carries its unit column with four dimensioned UnitsNet selectors.
[KERNEL_BENCH_PROFILE_CORPUS]-[COMPLETE]: landed as `Projection/benchmarks.md` `[03]-[GATE_COMPOSITION]` — `MaterialsBench.Fresh` projects each content-bound workload through `BenchmarkReceipt.Of` and `MaterialsBench.Gate` traverses the corpus through `BenchmarkGate.Gate`, with harness and claim residence injected; the `BenchWorkload` row joined `Rasm.AppHost/Observability/benchmarks#CLAIM_FIELD_MAP`.
[CMU_SUBTYPE_CARRIER]-[COMPLETE]: Ruled a realization-bag row — `CmuSeed.Rows` seeds the `DetailSchema.ProfileSubtype` token off `CmuPhysics.IfcSubtypeOf` (the family widened to `DetailLane.Realization`), the `Rasm.Bim` egress profile lane resolves the subtype from the carried row, never a `CmuRow` seed column and never a cross-package call.
[MATERIALS_SIGNAL_TAP]-[COMPLETE]: landed as kernel composition on `Projection/observability.md` — `MaterialsFact` family, the seven-point `MaterialsHooks` rail over the kernel capsule, the `MaterialsInstruments` `InstrumentSpec` roster with the contributor port, `MaterialsLog` and `MaterialsLatency`, and the `MaterialsDescriptors` pack over the kernel SLO algebra; the descriptor iac decode row stays open on `TASKLOG.md` `[SIGNAL_DESCRIPTOR_ROWS]`.
