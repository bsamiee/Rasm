# postgres.md — final region rows

## page region (card altitude)

- domain/postgres.md: PostgreSQL store law — the one data-source-builder profile fold with connector-binding budget, round-trip levers, and driver tracing-shape rows; engine-gated SQL feature law (v7 identity, generated-column forcing, index-as-model-data, temporal constraints, jsonb index vocabulary, mutation grammar) under two-door construct routing with the root-only composition hinge; total SQLSTATE-class fault disposition; extension-lane grammar (three-declaration lanes, one-vocabulary-per-column-family, vector/geo/text ordering law) composing one verdict-gated typed query rail; binary COPY commit-edge inversion with conservation-receipted MERGE reconciliation; the NOTIFY-wake-over-slot-decode change contract ending at the op-log boundary; deploy-artifact, suite-scheduler-maintenance, and verification-only provisioning law.

## snippet regions (fence altitude)

- PG-01 STORE_PROFILE_FOLD: one `NpgsqlDataSourceBuilder` fold owning mapping, JSON, credential-rotation, session-initializer, and tracing-shape rows into one process-lifetime `NpgsqlDataSource`, with `AddNpgsql`/`AddNpgsqlInstrumentation` as the two composition-root telemetry rows — telemetry depth (`ConfigureTracing` filters, span-name providers, enrichment, first-response and physical-open events) declared as store-profile data. Distinct from RT-02 (descriptor fold) and DG-03 (signal-root admission — governance is diagnostics-owned; this region owns only the driver's shape rows).
- PG-02 SQLSTATE_DISPOSITION: total SQLSTATE-class disposition — two-tier `NpgsqlException`/`PostgresException` capture at one connection-less source-level seam (the statement callback receives the `NpgsqlDataSource`; binding lanes own their connections), `PostgresErrorCodes` constant patterns plus the `['2','2',..]` class pattern minting a closed `Expected`-derived store-fault family carrying constraint/column evidence, contention isolated as the only strategy-owned retry class. Distinct from RS-02 (wire outcome fold), A-10 (solver terminal partition), VL-02 (severity-partition bridge), R-01 (capture grammar is supporting material).
- PG-03 CAPABILITY_VERDICT: verification-only store admission — one connection-less `NpgsqlBatch` catalog read folded by failure-rank rows whose absence policy is each rank's own `Absorb` delegate column (required refuses, degradable folds the lane out with a receipt, observational records evidence — zero rank `Switch` arms at the fold) into one typed capability verdict the process dispatches on; extension generation floors as row data; the process never alters the environment. Distinct from RT-05 (health rank fold) and VL-04 (metadata bijection sweeps).
- PG-04 LANE_COMPOSED_QUERY: the one typed query rail with a root from either door — vector distance, geodesic predicate, generated-tsvector match, and range containment as declared `IQueryable` transform rows gated by the typed capability verdict and folded over one root, the LINQ base or the mapped profile's `FromSql` `DISTINCT ON` typed-SQL hinge re-entering the same fold (`UseNodaTime`+`UseNetTopologySuite`+`UseVector`+`SetPostgresVersion` dual admission, index DDL as model data). Distinct from PG-03 (verdict production, not consumption) and PS-06 (single-engine extension-lane composition over one rail, not the store-op bracket family).
- PG-05 COPY_RECONCILE: binary COPY commit-edge inversion — `Complete()` commits, disposal discards — with `NpgsqlDbType`/data-type-name discriminated writes of mapped enum and composite rows into staging, then one `MERGE ... RETURNING merge_action()` reconcile on the same bound connector folding per-row verdicts into one self-proving conservation receipt (staged = inserted + updated, breach failing typed). Distinct from R-05 (drive-polarity bracket — the inversion is the spotlight, not bracket grammar), CC-06 (lane drain), and durability's MergeReceipt (op-log merge outcomes, not COPY/MERGE row verdicts).
- PG-06 REPLICATION_DECODE: pgoutput decode fold — closed message-family switch into change facts with driver-resolved `Relation` references surfacing as in-stream schema markers, replica-identity before-image evidence carried on the upsert/retract facts, truncate decoding to a relation-set purge fact, xid-keyed streamed-transaction staging released on stream-commit and dropped on abort, three-state cell decode (value | absent | unchanged-toast) with in-order tuple drains, emitted-message markers, apply-then-acknowledge `SetReplicationStatus` watermark. The op-log shape and LWW adjudication are durability-owned; this region owns exactly the decode. Distinct from B-08 (wire converter) and DG-05 (envelope seam).
