# [UI_TASKLOG]

Open and closed work for the host-free browser UI library, distilled from the ideas in `IDEAS.md`. Each open card carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — plus the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. A closed card carries `[COMPLETE]` or `[DROPPED]` with a one-line disposition. Cross-branch wire preconditions are tracked as blocked tasks against the C# fence that unblocks them.

## [1]-[OPEN]

## [2]-[CLOSED]

[T-COMMAND-SURFACE] [COMPLETE] — `content/command-surface.md#COMMAND_SURFACE` owns the `content/` sub-domain birth: the `CommandAction` `as const satisfies Record<string, CommandSpec>` AEC vocabulary (place-element/run-analysis/open-viewpoint/export-fabrication/toggle-overlay rows each carrying `label`/`LucideIcon`/`keybinding`/`InteractionRole`/`IntentRegistry`-key), `CommandPalette` over `cmdk` `Command.Dialog` folded through the shared `useFilter`+`UNSTABLE_useFilteredListState` primitive, `ActionDrawer` over the `vaul` `Drawer.Root` snap-point sheet, the one `cn = twMerge(cx(...))` recipe paired with the `@radix-ui/react-slot` `Slot`/`Slottable` `asChild` primitive, and `ActionIcon` over the tree-shaken `lucide-react` named exports + `LucideProvider`. Six admitted packages with zero prior owner collapsed into one charter-mandated page; dials only through `interchange` `CommandGateway` (`IntentRegistry.resolve(key) => {verb,payload}` over the closed `ControlVerb`), gated by `projection` `AvailabilityStore.isEnabled(intentKey)` (`ONE_EGRESS_VERB_FACE`). Single-mint held — no second command/intent enum, the leaf binds the C# `commands-availability` `TS_PROJECTION`.

[T-PICKER-FAMILY] [COMPLETE] — `component-system/picker-behavior.md#PICKER_BEHAVIOR` owns the one headless `PickerBehavior` `Data.TaggedEnum` over the four `_Roles.pickers` behaviors (color/date/file-preview/file-upload) under a total generated `$match` `applyPicker`, the `color` arm projecting the picked channel through the `react-stately` `parseColor`/`getColorChannels` surface into the `theme-tokens#THEME_TOKENS` OKLCH `colorjs.io` space so a pick is an `oklch` token with `contrastWith` run on it (the live `useColorPickerState` cell stays the consumer's stateful surface, by reference), the `date` arm selecting the `react-aria-components` `RangeCalendar`/`Calendar` widget by `mode`, and the `filePreview`/`fileUpload` arms selecting `FileTrigger` kept distinct. Composes the `role-behavior#ROLE_BEHAVIOR` `pickers` `RoleBehavior` contract by reference, reads/writes through `atom-binding#ATOM_BINDING`. Only the headless behavior owned; the file-preview+file-upload split preserved.

[T-CARTOGRAPHY-AGGREGATION] [COMPLETE] — `cartography/geo-series-layer.md#GEO_SERIES_LAYER` extends the overlay-arm `source` literal union with a `cell-index` arm and the `FeatureKind` union with the aggregation kinds (h3/h3-cluster/s2/quadkey/geohash/a5/tile-3d/trips), the `overlayLayers` total `Match.value(source)` gaining a `cell-index` `Match.when` arm with its own nested `Match.value(featureKind)`. The Arrow-native density families (`h3`/`s2`/`geohash`/`a5`) plus `trips` bind the `@geoarrow/deck.gl-geoarrow` `GeoArrowH3HexagonLayer`/`GeoArrowS2Layer`/`GeoArrowGeohashLayer`/`GeoArrowA5Layer`/`GeoArrowTripsLayer` index-column layers reading the same `interchange` GeoArrow `RecordBatch` projection directly (no second decode); the two cell kinds with no Arrow-native variant (`h3-cluster` set-merge via `getHexagons`, `quadkey` via `getQuadkey`) bind the `@deck.gl/geo-layers` `H3ClusterLayer`/`QuadkeyLayer` `_GeoCellLayer` classes over the bounded JS cell-id projection rather than re-materializing the batch; `tile-3d` streams `Tile3DLayer` over the tileset URL under the existing `MapboxOverlay` interleave — no parallel surface, no second decode.

[T-ROUTE-MOTION-GATE] [COMPLETE] — `motion/view-transitions.md#VIEW_TRANSITIONS` adds the reduced-motion gate as one `swap`-payload field (`reduceMotion`), the `mountSurface` swap arm reading `prefersReducedMotion()` over `matchMedia("(prefers-reduced-motion: reduce)")` and suppressing the `<ViewTransition>` capture so the `theme-tokens#THEME_TOKENS` `tw-animate-css` `prefers-reduced-motion` layer owns the declarative collapse, plus the explicit platform Navigation-API coupling note (ui owns only the `<ViewTransition>`/`<Activity>` wrapper + gate, declares the navigation-commit requirement, never imports `platform`); realizes branch `NATIVE_TRANSITION_PAIR`. The `<ViewTransition>`/`<Activity>` exotic import path stays a soft RESEARCH residual on the page (`[VIEW_TRANSITION_ACTIVITY]`); the `mountSurface` fold is settled regardless.

[T-SKEW-BAND-RENDER] [COMPLETE] — `observation/observation-routes.md#OBSERVATION_ROUTES` promotes the `EvidenceTimelineRoute` `SkewBand` from a render-only annotation to the load-bearing band render: a total `Match` over the `projection/causality-graph/version-vector#VERSION_VECTOR` `CausalVerdict` each `causalStore` `CausalCell` tags, where the `ConcurrentUncertain` arm draws the "within +/-`radiusMs` ms" band + concurrent-uncertain pair marking over the `@tanstack/react-virtual` rows, reading the verdict from `causalStore` and the radius from `skew-band#SKEW_BAND` `SkewInterval.radiusMs` by reference through `atom-binding#ATOM_BINDING`, re-deriving no HLC order (the `bandsOverlap` widening is `version-vector` `skewVerdict`'s, not re-derived here); realizes branch `HONEST_CLOCK_UNCERTAINTY`. The recon blocked-tag was verified stale against the COMPLETE projection clock-uncertainty/causality-graph corpus and re-dispositioned to execute.

[T-TW-ANIMATE-REGISTER] [COMPLETE] — `theming/theme-tokens.md#THEME_TOKENS` registers the `tw-animate-css` enter/exit utility layer as the second styling substrate: the `@import "tw-animate-css"` directive at the stylesheet root after `@import "tailwindcss"` and beside the `@theme` OKLCH layer, the `data-[state=open]:animate-in`/`data-[state=closed]:animate-out` Radix-data-state enter/exit selectors every `@radix-ui/*`-derived overlay (`cmdk` `Command.Dialog`, `vaul` `Drawer`) emits, the `duration-*`/`ease-*` timing resolving against the live `@theme` tokens, and the `@media (prefers-reduced-motion: reduce)` keyframe collapse the motion gate rides — three rows on the one styling substrate, never a sibling stylesheet. The OKLCH `oklchScale`/`contrastWith`/`CssVarSync` floor is untouched. A JS animation controller or hand-authored `@keyframes` beside the declarative `data-[state]` layer is the named defect, extending the existing direct-style-write defect to animation.

[T-FILTER-COLLAPSE] [COMPLETE] — `content/command-surface.md#COMMAND_SURFACE` authors the one `useFilter` (locale-aware `startsWith`/`endsWith`/`contains`) + `UNSTABLE_useFilteredListState` scoring primitive and binds the `cmdk` `Command` `filter` prop to the shared `score` predicate under the default `shouldFilter` so the palette scores through the shared `useFilter` `contains` and never the built-in `command-score` `defaultFilter` (a `shouldFilter={false}` beside a `filter` prop is contradictory and is the deleted form, since it disables the predicate entirely); `component-system/role-behavior.md#ROLE_BEHAVIOR` collapses the `collections` (grid-list/table/tree) and `actions` filterable surfaces onto that one primitive by reference (named on the Cases + Boundary), so the command palette, a filtered list, and a filtered table all read one filtering algebra. A hand-rolled `contains`/`startsWith` predicate or a second filtering algebra is the named defect.

[T-MESH-DECODE-SEAM] [COMPLETE] — `viewport/glb-viewport.md` `decodeMeshView` authored as a total `Match` over the settled `GeometryPayload.kind` proto oneof → `GeometryView` `Data.TaggedEnum` (`mesh`/`pointCloud`/`voxel` arms with real `MeshTensor`/`PointCloudTensor`/`VoxelTensor` byte projections), each folded onto a real `three` draw (indexed `Mesh`/`Points`/instanced), consuming the C#-minted `csharp:Rasm.Compute/remote/channels` `MeshTensor`/`GeometryPayload(mesh)` GENERATED proto descriptor BY REFERENCE via protoc-gen-es — re-minting no branch-side `MeshTensorWire`. The prior "C# must promote MeshTensor into a TS_PROJECTION fence" framing was the consumer's misframing: mesh correctly crosses as the generated binary proto descriptor, so the C# producer obligation is met. Single-mint held; XxHash128 verify rides the rail seam. Honest residual: the `three/tsl` cluster-LOD compute kernel stays RESEARCH (a render-side optimization, not the decode).

[T-PRESENCE-OVERLAY] [COMPLETE] — `overlay/presence-overlay.md` authored (from COLLAB_PRESENCE_OVERLAY): `PresenceOverlay` renders the live collaborator cohort over two settled wire arms — the `projection/convergence/presence#PRESENCE` `PresenceRowWire` TTL claim and the `causality-graph/version-vector#CRDT_SEMILATTICE` `EphemeralMap` `beat`/`leave` state — each peer one `Atom.family` cell keyed by `actor`, composing `overlay/floating-anchor#FLOATING_ANCHOR`, subscribing through `binding/atom-binding#ATOM_BINDING` to the `projection` folds; reads only the settled C# `version-control#CRDT_WIRE`/`collaboration#TS_PROJECTION` projections, re-minting no presence op vocabulary. Honest residual `[BEAT_PAYLOAD]`: the C# `Awareness` beat `state: ReadOnlyMemory<byte>` encoding (JSON vs MessagePack) is producer-unpinned — `BeatStateWire` decodes the `state` slot through `Schema.parseJson` until the producer serializer pins it; the slot is producer-owned, the render-side interpretation not a second wire shape.

[T-BCF-ANCHOR-SURFACE] [COMPLETE] — `overlay/bcf-anchor.md` authored: the viewpoint/issue-anchored observation overlay decodes the settled `interchange/codecs/decode-rail#BCF_LIVE_WIRE_DECODE` `BcfTopicWire`/`BcfViewpointWire` rows (the `BcfStatusKey` lower-kebab vocabulary, `anchorViewpoint` projecting the `cameraPosition/cameraDirection/cameraUpVector` triplets onto a `CameraState` and the `selectedGlobalIds` onto element GlobalIds), composing the existing `viewport`/`overlay/floating-anchor` leaves; the gate was the interchange decode-rail BCF rows + the `wire-inventory` `Rasm.Bim/coordination` row, both now landed, the producer `csharp:Rasm.Bim/coordination/issue-exchange#TS_PROJECTION` confirmed `[COMPLETE]`. Single-mint held — the leaf decodes the `Rasm.Bim` topic vocabulary and re-mints no parallel issue shape.

[T-LIVE-BINDING-DASHBOARD] [COMPLETE] — `observation/live-binding-dashboard.md` authored (from LIVE_BINDING_STUDIO_DASHBOARD): the live-wire binding studio over `@tanstack/react-table`+`@tanstack/react-virtual` rendering the per-binding connect/subscribe/stale/fault health table off the decoded `interchange/codecs/decode-rail#BCF_LIVE_WIRE_DECODE` `BindingStatusWire`, the source-versus-canonical coercion off `CoercedValueWire`, and the `dispositionLabel` discriminating the `WriteReceiptWire.disposition` `WriteBackWire` `kind` (acknowledged/rejected/rolled-back/coalesced) verbatim, each binding-status key the decoded smart-enum string; subscribes through `binding/atom-binding#ATOM_BINDING` to a `projection` live cell, the write receipt riding the existing `ReceiptEnvelopeWire`, reading and never emitting. The gate was the producer `csharp:Rasm.AppHost/live-wire#TS_PROJECTION` fence (now landed C#-side with the `[Flags]`→`DirectionKey` token + `WriteBack` disposition projection) plus the interchange decode-rail live-wire rows + the `wire-inventory` `Rasm.AppHost/live-wire` row, all now landed. Single-mint held — no branch-side binding-status enum, no second `WriteBackWire` re-mint.

[T-API-CATALOGUE] [COMPLETE] — every folder `.api/` catalogue row the new admissions named is admitted; the 32 catalogues under `ui/.api/` carry the verified members (`react-aria-components` `UNSTABLE_Toast`/`UNSTABLE_ToastRegion`, `react-stately` `ToastQueue`/`useToastQueue`, `@react-aria/live-announcer` `announce`, `three/webgpu` `WebGPURenderer`/`init`, `@google/model-viewer` element surface, `@floating-ui/react` hooks/middleware, `@use-gesture/react` `useGesture`/`FullGestureState`, `colorjs.io` `Color.steps`/`contrastWCAG21`, the `@deck.gl/geo-layers` `TileLayer`/`MVTLayer` and `@geoarrow/deck.gl-geoarrow` `RecordBatch` layers). Every RESEARCH fence that names a verified member is promoted to settled fence code.

[T-BINDING-COLLAPSE] [COMPLETE] — `binding/atom-binding.md` authored to its full surface: `Atom.searchParam`/`kvs`/`family`/`pull`/`runtime` over the verified `@effect-atom/atom` constructor signatures, the `UndoStack` bounded history fold, and the `Result.builder` `onWaiting`/`onSuccess`/`onError`/`render` status chain. The offline cell binds the `platform` `LocalPersistence` `KeyValueStore` through the `Atom.kvs` runtime/key/schema/defaultValue quad.

[T-ROLE-VOCABULARY] [COMPLETE] — `component-system/role-behavior.md` (the eight-role `_Roles` `as const satisfies Record` owner-block with `politeness`/`behaviors`/`focusable` columns, the `InteractionRole` `keyof typeof` literal, the `announceFor` indexed projection, the `RoleBehavior` contract) and `component-system/accessibility-broadcast.md` (the `announceRole` politeness-gated `@react-aria/live-announcer` path filtering `"off"`, the `react-stately` `ToastQueue` external store, the `UNSTABLE_ToastRegion` landmark) authored with verified spellings. The `ToastQueue<A>` constructor options and the `add`/`close` method rows are now enumerated on the `react-stately` catalogue; the class, `useToastQueue`, `UNSTABLE_ToastRegion`/`UNSTABLE_Toast`, and `announce` are verified.

[T-THEME-TOKENS] [COMPLETE] — `theming/theme-tokens.md` authored: `oklchScale` over the `colorjs.io` `Color.steps` OKLCH interpolant, `contrastWith` over `Color.contrastWCAG21`, and the single `CssVarSync` `Stream` fold to the `:root` custom properties.

[T-OBSERVATION-ROUTES] [COMPLETE] — `observation/observation-routes.md` authored: `EvidenceTimelineRoute` (HLC order + `SkewBand`, `@tanstack/react-virtual`), `BenchmarkRoute` (fingerprint-gated by comparing the decoded `HostFingerprintWire.fingerprintId` the C# fingerprint owner stamps onto the wire — never a browser-side stamp-line reconstruction), and `CollectorPanel` as read-only leaf subscribers.

[T-CARTOGRAPHY] [COMPLETE] — `cartography/geo-series-layer.md` deepened: the one `GeoSeriesLayer` `Data.TaggedEnum` family dispatched by `$match`, the `source` arm routing `geojson`/`tile`/`geoarrow` through the verified `@deck.gl/geo-layers` `MVTLayer`, the `@geoarrow/deck.gl-geoarrow` `GeoArrowScatterplotLayer`/`GeoArrowPathLayer`/`GeoArrowPolygonLayer` with one shared `initEarcutPool`, and the `@deck.gl/mapbox` `MapboxOverlay` `interleaved` camera-sync path.

[T-GESTURE-ALGEBRA] [COMPLETE] — `motion/gesture-algebra.md` authored: the `CameraGesture` `Data.TaggedEnum`, the total `applyGesture` `Match.tagsExhaustive` fold, and the `useGestureFold` `@use-gesture/react` `useGesture` recognizer over the verified `FullGestureState` drag/pinch/wheel state.

[T-OVERLAY] [COMPLETE] — `overlay/floating-anchor.md` authored: `useFloatingAnchor` over the verified `@floating-ui/react` `useFloating`/`autoUpdate`/`offset`/`flip`/`shift`/`arrow` positioner and the `useInteractions` merge of `useClick`/`useFocus`/`useRole`/`useDismiss`, the `ROLE_OF` overlay-kind-to-ARIA-role table, and the CSS Anchor Positioning bridge.

[T-VIEWPORT-BACKEND] [COMPLETE] — `viewport/glb-viewport.md` `RendererBackend` two-row axis authored: the `three` row over the verified `three/webgpu` `WebGPURenderer` `init()` auto-detect/WebGL-fallback and the `model-viewer` zero-GL-handle embed row, the `Effect.acquireRelease` GL context, and the `ViewportCamera` `RoleBehavior` composing the gesture fold. Residual: the `three/tsl` cluster-LOD compute kernel stays RESEARCH pending the mesh-wire promotion (tracked on `T-MESH-DECODE-SEAM`).

[T-VIEW-TRANSITIONS] [COMPLETE] — `motion/view-transitions.md` authored: the one `SurfaceTransition` `Data.TaggedEnum` over `swap`/`preserve` under the total `mountSurface` `$match`, with the verified `useEffectEvent` transition callback. Residual: the React 19.2 `<Activity>`/`<ViewTransition>` component exotics are absent from the branch `react.md`/`react-dom.md` catalogues and stay RESEARCH (admitted-on-precondition); the `SurfaceTransition` owner fold is settled regardless of the final component import path.

[T-FORM-BINDING] [COMPLETE] — `component-system/form-binding.md` authored (from FORM_ENGINE_SCHEMA_BINDING): the headless `FormBinding<A>` folding `Schema.decodeUnknownEither` over the raw `FormData` into either the typed value or a `ParseResult.ParseError`, projected onto the `react-aria-components` `Form` `validationBehavior: "aria"` `FieldError` map, with a valid decode crossing the `interchange` `CommandGateway`. The `ParseResult.ArrayFormatter`/`TreeFormatter` per-path projection driving `errorMap` is now catalogued on the branch `effect.md` and the `errorMap` fold over `ArrayFormatter.formatErrorSync` is settled alongside the `decodeUnknownEither`/`Either.match` fold.
