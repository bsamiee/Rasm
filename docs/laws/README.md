# [LAWS_CORPUS]

Maintenance law outliving every language branch and campaign homes in this corpus.

Heavy workflow runs land their generalizable findings here through one terminal doctrine stage.

## [01]-[PAGES]

| [INDEX] | [PAGE]        | [OWNS]                                                           |
| :-----: | :------------ | :--------------------------------------------------------------- |
|  [01]   | `topology.md` | Coupling map: which surfaces obligate which counterparts on edit |
|  [02]   | `patterns.md` | Pattern law binding two or more language branches                |
|  [03]   | `scars.md`    | Regression-proven law with no other owner and its trigger        |

## [02]-[ADMISSION]

Each fact kind below routes to its owner; a fact earns a row in this corpus only after the ladder proves no owner holds it.

| [INDEX] | [FACT_KIND]                  | [OWNER]                                            |
| :-----: | :--------------------------- | :------------------------------------------------- |
|  [01]   | Language implementation law  | `docs/stacks/<language>/`                          |
|  [02]   | Documentation law            | `docs/standards/`                                  |
|  [03]   | Design-phase campaign method | `libs/.planning/campaign-method.md`                |
|  [04]   | Folder-scoped operation      | the folder's `README.md`                           |
|  [05]   | Agent constitution           | `CLAUDE.md` / `AGENTS.md`                          |
|  [06]   | Reviewer behavior            | `.coderabbit.yaml` + `.greptile/` + `.macroscope/` |
|  [07]   | Cross-surface edit coupling  | `topology.md`                                      |
|  [08]   | Cross-branch pattern law     | `patterns.md`                                      |
|  [09]   | Paid-for regression trap     | `scars.md`                                         |
|  [10]   | Settled recurring decision   | the narrowest owning `RULINGS.md` tier             |
|  [11]   | Test-estate law              | `tests/README.md` + the owning `tests/` tree tier  |

[EXTENSION_LADDER]:
- Law: Capability hardens a clause, then extends a page inside its owner, then mints a new page, each rung proving the prior cannot carry the fact.
- Law: New pages land their `[01]-[PAGES]` row in the same change.

[SECOND_COPY]:
- Law: Facts owned elsewhere gain no copy here; the owner keeps each, and a topology row names it as a counterpart.

[THRESHOLD]:
- Law: Admission demands recurrence across independent units, or a structural singleton whose blast radius spans folders or branches.
- Boundary: Unit-local lessons stay in the run report, and adding nothing is a first-class verdict.

[GATE_ROUTING]:
- Law: Rules a formatter, analyzer, or gate enforces route to that gate, never to a doctrine page.
- Law: Constitution facts land at their one acting reader.
- Law: Reviewer rules mirror across every reviewer config.

[RE_PROVING]:
- Law: Heavy workflow runs re-prove every row against the live tree; no row archives.

## [03]-[PAGE_SHAPE]

- Law: Each law family's own contrast picks its page shape — a table per shared question, `[TOKEN]:` cards per contrast, sections for universal law.
