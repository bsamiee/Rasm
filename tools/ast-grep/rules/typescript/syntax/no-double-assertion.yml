# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: no-double-assertion
language: tsx
severity: error
utils:
    wide-assertion:
        any:
            - kind: as_expression
              has: {kind: predefined_type, regex: "^(unknown|never|object)$"}
            - kind: as_expression
              has: {kind: object_type, not: {has: {pattern: $_, not: {kind: comment}}}}
            - kind: parenthesized_expression
              has: {matches: wide-assertion}
rule:
    pattern: $VALUE as $TYPE
constraints:
    VALUE: {matches: wide-assertion}
message: 'Double assertion reaches `$TYPE` with no validation'
note: '`Schema.decodeUnknownSync(schema)(value)` validates into the type'
labels:
    TYPE: {style: primary, message: Type the assertion claims}
