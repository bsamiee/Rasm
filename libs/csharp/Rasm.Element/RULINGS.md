# [RASM_ELEMENT_RULINGS]

`Rasm.Element` rulings settle package-scoped decisions.

## [01]-[PACKAGES]

- (none)

## [02]-[SHAPE]

- Seam vocabulary stays provider-blind — no GeometryGym, host, or app-platform type enters any `Rasm.Element` surface; every ingest enrichment lands as neutral graph content a peer decodes without the producer's assembly, and a new ingest feature proposing a provider payload column for convenience is the recurring move this row rejects — one provider type on the seam couples every projector and consumer to that provider's assembly.
- `AppearanceSummary` is FROZEN at its seven-value preimage and its eight-parameter `Of(…, Op key) -> Fin<AppearanceSummary>` arity — a richer appearance fact (a baked texture-set key, an environment binding, a UV transform, a measured refractive index) hangs BEHIND the `AppearanceKey` on its producer's own wire, and the LANDED escape hatch for a field-valued one is the seam `DetailSchema.Appearance` bag riding the appearance node's own `Associate` edge beside the summary — the `TextureSet` baked-set address and the `DoubleSided` shell flag the Materials projector writes and the Bim graph reader resolves — so an appearance fact two non-referencing packages key on is one bag row and the frozen seven-value preimage never widens; the column is refused because an eighth column re-keys every stored `Node.Appearance` and forks the `Rasm.Bim` surface-style dedup key in the same edit while both peers mint through this one factory; the recurring move reads the record as an open PBR bag and adds the column at whichever peer needed it, which is invisible until two runtimes disagree about one surface's id, and re-litigation opens only under a coordinated re-key of every persisted appearance node.
- Float-bearing members on this branch never take `[PrecisionEquality]` — the generator omits precision members from `GetHashCode` entirely, so a payload distinguished only by tolerance-compared scalars collapses to one hash bucket. Reopens only on a float-bearing member whose tolerance no canonical projection already owns.
- Every double the graph carries is tolerance-owned elsewhere: a `MeasureValue` quantized to `Header.Tolerance` by the canonical projection, where a second tolerance forks the cross-runtime parity corpus, or an `AppearanceSummary` channel preimage to a frozen `AppearanceKey`, where tolerance-equality rules two nodes equal whose content keys differ.
- String members the `CanonicalWriter` writes verbatim declare `[StringEquality(StringComparison.Ordinal)]` rather than inheriting the default, so equality and content identity cannot fork on a comparer edit.
- Imported material `Pset` rows land in a neutral `Properties/property#PROPERTY_BAG` node keyed by the foreign set name under `PropertySource.Import`, never as Option-widened columns on the FULL-VECTOR typed `MaterialPropertySet` cases — a partial foreign set folded onto a case fabricates every undeclared column, and the recurring move reads that family as an open bag because a foreign set name matches a case. Reopens where a published template proves one foreign `Pset` TOTAL against one case.
- `ValueBag` rows carry their `MeasureValue` `QuantityType` identity wherever EVERY producer of the row names that identity truthfully — a takeoff derived off a registry quantity, a round-tripped row whose foreign measure-type name the importing projector's own roster resolves — and take the dimension-anonymous `OfSi(Dimension, double)` mint ONLY where a name is unspellable: a value derived from no named quantity (a normalized fraction, a code factor, a bare-real source attribute), or a row two non-referencing peers mint where one holds no name and its silence forks the content key the pair exists to share. This RETIRES the absolute form — a round-tripped bag row stays dimension-anonymous for content-key stability — which bought key stability by discarding an identity the file itself declared and stranded every re-export on a flattened `IfcReal`, while key stability binds only inside a DECLARED pin window and no pin is committed anywhere in the estate, so the move re-keys the reached bags exactly ONCE and every corpus snapshot key derives at its own landing. Re-litigation opens only under a declared pin window, which turns a typed mint into a migration rather than an edit.
- Seam `Vector3` owns analytical and domain math; BCL `System.Numerics` `Matrix4x4`/`Vector3` are interchange-carrier LAYOUT types confined to `MeshInstance` and `ImportedGeometry.Bake` — a BCL vector in a domain fold and a seam vector in a byte-layout carrier are the two crossing directions this carve forbids.
- `Object.Placement` is EXCLUDED from the canonical-bytes preimage under the `OwnerHistory` exclusion law — folding placement into content identity re-keys every node on a pure rigid move and destroys the `Rasm.Bim` diff's `Moved` discriminant, which keys placement in its own bucket.
- Foreign provider sentinels never become seam column types — GeometryGym's `int.MinValue` unset priority, a `NaN` extent, a defaulted enum retire to `Option<T>` at the projector read that first sees them, and a three-state foreign logical takes the one `Option<bool>` encoding `PropertyValue.Logical` ratifies, never a second per-domain vocabulary; the recurring move mirrors the provider's field type onto the seam row for fidelity, carrying the sentinel into the hash and the wire.
- `Federate` takes ONE caller-supplied coordination `Header` and refuses divergence rather than reconciling it [FEDERATION_HEADER_RULING] — a source whose `Header.Tolerance` differs bitwise from the coordination value or whose `Header.Reference` differs structurally refuses with a typed fault naming the axis, because tolerance-quantized measure bytes and non-rooted `NodeId`s mint at the SOURCE tolerance and no union re-quantizes them without re-keying the source's own nodes; frame alignment is the upstream reprojection leg's job (`Rasm.Bim` geodetic preflight), never the union's. Source headers demote to provenance rows on the `FederationReceipt` — per-source snapshot address, header columns, node and edge counts, beside the union totals and the merged tally — so the union carries one semantic identity and zero header rosters; a collided tally is unrepresentable there because a collision refuses the union outright and no receipt is minted; a per-source `Header` roster on `ElementGraph` is the rejected form that widens the one graph spine every consumer reads for a federation-only concern. Id union law: a rooted occurrence id colliding across sources is a fault, never a rename; content-derived and type-derived ids colliding with EQUAL payloads (generated comparer) merge as the dedup the id regime exists for, and colliding with UNEQUAL payloads fault naming the id.
- Redacted crossings PRESERVE source content keys and spell redaction as wire-level presence clearing [REDACTION_IDENTITY_RULING] — the egress policy maps a sensitivity classification onto protobuf field paths and CLEARS them on the encoded message, so absence is proto3-presence-honest and no redactor token fabricates a value; re-deriving keys over redacted bytes is the rejected arm because it mints one identity space per policy and strands every partner reference off the source model. The crossing carries its redaction manifest — policy identity, cleared paths, and the roster of node ids whose cleared columns fold into canonical bytes — as an additive wire field, and the verifying decode admits exactly the manifest-named nodes as declared-unstable while a drifted node OUTSIDE that roster still faults `AddressUnstable`; identity-inert columns (`OwnerHistory`, `StepHeader` authors and organizations, `Provenance` identity slots) clear with no manifest row because no key moves.

## [03]-[COLLAPSE]

- Every seam admission slot returns concrete `Validation<Error, _>` — a `K<Validation<Error>, _>` slot return is unrepresentable, not merely verbose: C# excludes interface targets from user-defined conversions, so the bare-value/bare-fault ternary lift the admission grammar rides compiles only on the concrete carrier (compile-probed, CS0029 on both arms of the interface form).

## [04]-[STRUCTURE]

- Wire-unfreeze events do NOT re-enter — the `rasm.element.v1` wire evolves additively under the `Graph/wire.md` contract-evolution law, so an appended field or new `oneof` arm lands with its seam feature and no campaign gate queues it; "frozen this campaign, lands at the unfreeze" narration re-imports a process construct the wire owner's law already forecloses.

## [05]-[PROCESS]

- (none)
