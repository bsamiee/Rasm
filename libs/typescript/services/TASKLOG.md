# [SERVICES_TASKLOG]

Open and closed work for the node tier, distilled from `IDEAS.md`. Each open card's leader carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — and three to four scoped bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries/wires, and the key considerations. One idea spawns one or more tasks; closed work moves to `[2]-[CLOSED]`.

## [1]-[OPEN]

(none)

## [2]-[CLOSED]

[COMPLETE] OBJECT_STORE_TIER — `object-store/object-store#OBJECT_STORE` owns the one `ObjectStore` `Effect.Service` over the `@effect-aws/client-s3` `S3Service` layer (getObject/putObject incl. `{presigned:true}` + the full multipart `createMultipartUpload`/`uploadPart`/`completeMultipartUpload`/`abortMultipartUpload` + `listObjectsV2Stream`), the `ObjectKey` content-addressed `[ValueObject]` reusing the `interchange` `XxHash128`/`ONE_CONTENT_IDENTITY` seed, the `PresignGrant` `Data.TaggedEnum` (get/put/multipart, `persistence/tenancy#TENANCY`-GUC-scoped, `Match.tagsExhaustive`), the `AssetCodec` one-`Schema.Literal` fan-out (csv/xlsx/pdf/archive + sharp transform) migrated OUT of `persistence/work-and-signals#WORK_AND_SIGNALS`, and the `LifecyclePolicy` rows over `listObjectsV2Stream`. Raw `@aws-sdk/client-s3` demoted to interop-only via `S3ClientInstance`; `@effect/platform` Multipart is an orchestrator-owned branch-`.api` dependency the page references but does not catalogue (the S3Service multipart wrapper may suffice). `work-and-signals` re-pointed to cite this owner for the codec concern.

[COMPLETE] REACTIVE_QUERY_INVALIDATION — `persistence/reactive-query#REACTIVE_QUERY` owns the read-side `ReactiveQuery` `Effect.Service` over `SqlClient.reactive([keys], query)`/`reactiveMailbox` bound to the `@effect/experimental` `Reactivity` substrate (the `Reactivity.Reactivity` requirement discharged by the `@effect/sql-pg` `PgClient.make` layer's bundled default — no separate wiring), the entity-keyed `InvalidationKey` `as const satisfies Record` vocabulary, the `Reactivity.mutation` key-publish edge the `interchange` `CommandGateway`/`eventing` relay wrap, and the dependent-read re-run `Stream` (the `messaging/internal-rpc#INTERNAL_RPC` query procedures + the `observability/slo-budget#SLO_BUDGET` signal reads subscribe). Tenant-scoped via the `persistence/tenancy#TENANCY` GUC fold into the key. Re-affirms the `EXPERIMENTAL_RETIRE_OR_KEEP` keep. No new admission, no .NET wire.

[COMPLETE] NATIVE_DELAYED_DELIVERY — `messaging/entity#DELAYED_DELIVERY` ROW owns the `DeliverAt` per-key future-message rail: a `DeliverAt`-implementing `DeferredAction` payload (`[DeliverAt.symbol]()` → delivery `DateTime`, `DeliverAt.isDeliverAt`/`toMillis`, the `SqlMessageStorage` `deliverAt: number | null` storage rail) scheduling its own future message through the shard manager beneath the cluster-wide `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` cron, composing rather than `Effect.timeoutTo`/a parallel timer table. Wires the per-key retry-after, the `durable-execution/saga#SAGA` deferred step, the `durable-execution/ai-activity#AI_ACTIVITY` cooldown, and the scheduled tenant action. `effect-cluster.md` already carries `DeliverAt` — no new admission, no .NET wire.

[COMPLETE] ENTITY_RESOURCE_LIFETIME — `messaging/entity#ENTITY_RESOURCE` ROW owns the `EntityResource` restart-surviving per-key resource: `EntityResource.make({ acquire, idleTimeToLive })`, the `makeK8sPod(v1.Pod)` managed-pod arm over `K8sHttpClient` (`v1.Pod` spec from `provisioning/contract#PROVISIONING`), the `CloseScope` release — beside the state-durability the actor persists. `agent/session-actors#SESSION_ACTORS` deepened to COMPOSE it for the per-session persisted `Chat` handle + host tool-transport session bound to the session key (acquired on `Start`, shared across `Turn`, released on `End`/idle-TTL), never re-acquired per `Turn`. `effect-cluster.md` already carries `EntityResource`/`CloseScope`/`makeK8sPod`/`K8sHttpClient` — no new admission, no .NET wire beyond the agent's own tool transport.

[COMPLETE] IDEMPOTENCY_LEDGER — `persistence/idempotency-ledger#IDEMPOTENCY_LEDGER` owns the keyed `IdempotencyLedger` `Model.Class` over the one `PgClient` + the `ClaimOrReplay` claim-state fold (`fresh`/`claimed`/`replayed` `Data.TaggedEnum`, `Match.tagsExhaustive` over the atomic `ON CONFLICT DO UPDATE … RETURNING (xmax = 0)` claim and the single-writer `settle`), serving `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX`/`messaging/entity#ENTITY`/`agent/session-actors#SESSION_ACTORS` as the shared cross-surface exactly-once owner. Boundary recorded: admits ONLY the cross-surface dedup the per-surface primitives do not span — `@effect/cluster` `MessageStorage` `SaveResult.Duplicate` (`Success | Duplicate<R>` carrying `originalId`+`lastReceivedReply`) keeps mailbox-replay dedup, `@effect/workflow` `idempotencyKey` keeps workflow-activity dedup. Recon confirmed the gap is NON-redundant, so authored rather than down-scoped/dropped. No .NET wire.

[COMPLETE] QUOTA_GOVERNOR — `messaging/quota-governor#QUOTA_GOVERNOR` owns the per-tenant aggregate-backpressure `QuotaGovernor` `Entity` placement (via `getShardGroup`, sequenced after the `messaging/entity` ROWs settled the shared page) over the `@effect/experimental` `RateLimiter` (`fixedWindow`/`tokenBucket` over `RateLimiterStore`, the `RateLimiterError` `RateLimitExceeded`/`RateLimitStoreError` fold — the THIRD first-class `@effect/experimental` consumer), the `TenantQuota` cap vocabulary, and the backpressure `Stream` regulation, ABOVE the per-key `@effect/workflow` `DurableRateLimiter` (cited as the boundary). Caps `agent/agent-runtime#AGENT_RUNTIME` turns + `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX` external sinks. Recon confirmed the per-tenant aggregate fairness is NON-redundant, so authored rather than dropped. No new admission, no .NET wire.

[COMPLETE] EMBEDDING_LIFECYCLE — `hybrid-search/embedding-lifecycle#EMBEDDING_LIFECYCLE` owns the `EmbeddingProfile` migration-row `Model.Class`, the `BatchEmbed` one-entrypoint embed over `EmbeddingModel.makeDataLoader` (`{ window, maxBatchSize }`) + `EmbeddingModel.make` per-call cache + `embed`/`embedMany` by input shape, and the durable re-embed `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` job. Split recorded: admits ONLY the profile-MIGRATION + batch-embed ORCHESTRATION; `hybrid-search/fused-rank#FUSED_RANK` keeps the staleness column, the single re-embed `WorkQueue` row, and the `EmbeddingProfile` shape — the lifecycle FEEDS the vector arm. Profiles only the `pgvector` column, never the gated BM25 index (so UNBLOCKED) — authored rather than down-scoped. `effect-ai.md` already carries `makeDataLoader`/`make` — no new admission, no .NET wire.

[COMPLETE] Author the `secrets/` runtime secret-governance sub-domain (from RUNTIME_SECRET_GOVERNANCE) — `secrets/secret-store#SECRET_STORE` owns the one `SecretStore` `Effect.Service` over the `ConfigProvider` boundary, the `SecretRef` `Data.TaggedEnum` provider axis (`EscEnv`/`Doppler`/`Static` settled, `AwsSecretsManager` authored-but-gated) folded by `Match.tagsExhaustive`, the `LeasedSecret` `SubscriptionRef`-backed TTL cache with the `changes` rotation edge (no restart), the `LeasePolicy` per-provider TTL/rotation vocabulary, the `SecretFault` policy table, and the tenancy-GUC `AuditReceipt`. Mined `effect` (`ConfigProvider`/`Config.redacted`/`Redacted`/`SubscriptionRef.changes`), `@pulumi/esc-sdk` (`openAndReadEnvironment`/`readOpenEnvironment` runtime read), `@dopplerhq/node-sdk` (`secrets.get`/`download` Format `'env'` + `DynamicSecretsService.issueLease`/`revokeLease`). The TTL-expiry sweep is a `runtime-backplane` singleton; deploy-time and runtime resolution meet at the one `ConfigProvider` boundary. The `AwsSecretsManager` arm stays gated on the orchestrator-owned `@aws-sdk/client-secrets-manager` workspace-catalog admission (absent today) — authored as a family case, never a settled provider row. The `.api/` runtime-read annotation deepening on `pulumi-esc-sdk.md`/`dopplerhq-node-sdk.md` is owed to the folder deepening agent. Crosses no .NET wire.

[COMPLETE] Author the `observability/` SLO and error-budget sub-domain (from BACKEND_RUNTIME_SLO) — `observability/slo-budget` owns the closed `Objective` `as const satisfies Record` delegate-row vocabulary (`latency`/`availability`/`saturation`/`freshness`, each row carrying its target + fast/slow window pair + page/ticket burn multipliers), the multi-window multi-burn-rate fast-AND-slow `BurnRate` fold over the `@effect/sql-pg` `SignalSeries` (one `windowAggregates` round-trip per objective, fires only when both windows confirm), the `AlertRoute` `Data.TaggedEnum` enqueued through the `eventing/transactional-outbox#TRANSACTIONAL_OUTBOX` `DeliverySink` in the evaluation txn, the `ClusterMetricsReader` over the five `@effect/cluster` `ClusterMetrics` gauges as the `saturation` signal, the `runtime-backplane/backplane#RUNNER_AND_SCHEDULING` shard-pinned cron evaluation, and the decode-only `HealthWire`/`TraceContextWire` carriers off the C#-minted `ONE_HEALTH_DEGRADATION_WIRE`/`ONE_DISTRIBUTED_TRACE` frames. PACKAGE CORRECTION: the reader is `@opentelemetry/sdk-metrics` `MeterProvider`/`MetricProducer` + `@opentelemetry/sdk-trace-node` `NodeTracerProvider`, NOT `@effect/opentelemetry` (which stays the `[3]-[CROSS_CUTTING]` exporter edge with no reader `.api`); the `MetricProducer`-to-`ClusterMetrics` bridge is DESIGN-PIN'd against `MeterProviderOptions.metricProducers` pending a branch `.api/effect-opentelemetry.md`.

[COMPLETE] Add the MCP agent-transport consumer at `agent/mcp-transport` (from MCP_AGENT_TRANSPORT) — authored the node-tier decode of the settled `csharp:Rasm.AppHost/agent/mcp-projection#TS_PROJECTION` single mint: `catalog` lifts each `McpToolWire` into one `@effect/ai` `Tool.make` with host-validated `payload` passthrough + effect-hint annotations, `progress`/`resume` fold the SSE stream through `@effect/experimental` `Sse`, `costPreview`/`result` decode the cost and `ReceiptEnvelopeWire<ToolResultWire>`. Six decode-only `*Wire` carriers, no re-mint; the lifted `Toolkit` enters `agent/agent-runtime#AGENT_RUNTIME`.

[COMPLETE] Resolve the retire-or-keep gate on `@effect/experimental` (from EXPERIMENTAL_RETIRE_OR_KEEP) — RE-OPENED, re-resolved keep-with-three-direct-consumers: the `Sse` decode (`agent/mcp-transport`), the `Reactivity` key-scoped invalidation (`persistence/reactive-query`), and the rate-limiting module (the quota governor); `EventJournal`/`PersistedQueue`/`Persistence` stay transitive. No move to transitive-only.

[COMPLETE] Author the `authn/verifier` server-side credential-verification page (from AUTHN_VERIFIER) — `Authn`, one `Effect.Service` over the closed `AuthCommand` `Data.TaggedEnum` (TOTP via `otplib`, WebAuthn via the four `@simplewebauthn/server` primitives, OAuth, API-key) folded by one `$match`, verifying every credential the `platform` browser ceremony forwards over `interchange` `CommandGateway`; rides the one `PgClient` inside the `persistence/tenancy#TENANCY` GUC, crosses no .NET wire.

[COMPLETE] Reconcile the `otplib` cross-cutting README line (from OTPLIB_CROSS_CUTTING_RECONCILE) — `otplib` is genuinely branch-cross-cutting (the `platform` browser owns the TOTP ceremony, the `services` `authn/verifier#VERIFIER` owns server-side verification), so it stays in both `[CROSS_CUTTING]` sections with the branch `otplib.md` catalogue; the README line now names the `authn/verifier#VERIFIER` owner. No move to folder-local `[PACKAGES]`.

[COMPLETE] Author the `eventing/transactional-outbox` page (from TRANSACTIONAL_OUTBOX) — the `Outbox` `Model.Class` written in the same `SqlClient.withTransaction` as the domain mutation, the `OutboxRelay` draining via `FOR UPDATE SKIP LOCKED` + `LISTEN`/`NOTIFY`, the `DeliverySink` `Data.TaggedEnum`; shares the one `PgClient`, registers as a `runtime-backplane` singleton, gives `work-and-signals` `EventJournal`/`Notifications` their atomic-publish contract.

[COMPLETE] Author the addressable-`Entity` actor page at `messaging/entity` (from ENTITY_ACTOR_MODEL) — `SessionEntity`, an `Entity.fromRpcGroup`/`toLayer` actor with `ClusterSchema.Persisted` rpcs, `getShardId`/`getShardGroup` single-writer placement, and the `EntityProxy` projection; rides the workflow shard manager, persists per-key state through the one `PgClient`, and the collaborative `Apply` decodes the C# CRDT op-log wire off `interchange` without re-mint.

[COMPLETE] Compose the `agent/` subpath closure: agent-runtime + session-actors (from DURABLE_AGENT) — no stock `DurableAgent` exists, so `agent/agent-runtime` COMPOSES it from `Activity` + `@effect/ai` `Toolkit`/`Tool`/`Chat.layerPersisted` on `ClusterEngine` over the one `ai-activity#AI_ACTIVITY` provider axis, and `agent/session-actors` pins one conversation to a single-writer `Entity`; both ride the one `PgClient` and shard manager.

[COMPLETE] Upgrade `hybrid-search/fused-rank` fusion to RRF-over-BM25 (from RRF_BM25_FUSION) — replaced the weighted-sum lexical arm with BM25 and fused four `RANK()` CTEs by Reciprocal Rank Fusion (scale-invariant, per-signal normalization dropped), the `RerankModel` cross-encoder terminal kept; the BM25 extension (`pg_search`/Tiger) is a RESEARCH item gated on the provisioned Postgres image, the C# `federation#FUSION_RANK` stays distinct.

[COMPLETE] Extend `services/.api/pulumi-pulumi.md` with the automation event-stream catalogue (P1) — section [4]-[AUTOMATION_EVENT_STREAM] transcribed from the installed `@pulumi/pulumi@3.234.0` `automation/events.d.ts`/`stack.d.ts` (`EngineEvent`, the 15-member `OpType`, the 6-member `DiffKind`, the `previewRefresh`/`onEvent`/`changeSummary` driver), clearing the `provisioning/drift#PROVISIONING` RESEARCH flag.

[COMPLETE] Wire the continuous drift sweep at `provisioning/drift` (P2, from CONTINUOUS_DRIFT_SWEEP) — `DriftSweep` ROW: a `runtime-backplane` `ScheduledWork.cron` shard-pinned sweep resolving the ESC policy-pack environment via `@pulumi/esc-sdk`, re-running `computeStackDrift`, and emitting the primitive `StackDriftSummary` to the `provisioning/contract#PROVISIONING` `ObservabilityStack.emit` collector; deploy-time-only, no `@pulumi/*` type crosses to the hot path.

[DROPPED] Declare the implementation-time subpath `exports` — folded into `provisioning/contract#PROVISIONING`: the page Entry line already owns the `./provisioning` subpath `exports` as a transcription-time mechanic, so a pure build-fence step is no standalone idea-driven card; the `./agent` subpath is owned the same way by `agent/agent-runtime#AGENT_RUNTIME`.
