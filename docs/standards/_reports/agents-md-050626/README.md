# [AGENTS_MD_050626]

Scope: source material for the June 5, 2026 `AGENTS.md` standards and overlay rebuild. Use this README to choose the right report lane; active standards and overlays outrank every report here.

## [1][SESSION_CARD]

Target: `docs/standards/agents-md.md`, root `AGENTS.md`, `docs/standards/AGENTS.md`, and affected subtree overlays.
Status: PROMOTED, with retained source evidence.
Source set: existing `AGENTS.md` overlays, standards files, repo-local source context, focused language/library context, and frontier instruction guidance captured in reports.
Read order: start with `track-promotion` for final decisions, use `track-owner-routing` to inspect synthesis, then read `track-source-scans` only for original evidence.
Boundary: this session records why rules moved; it does not define current policy after the active owner files changed.

## [2][TRACK_CARDS]

`track-promotion`
    Direction: final critique and promotion maps after earlier scans and synthesis.
    Facets: adversarial gaps, `agents-md.md` wording, C# overlays, root overlay, and tests plus bridge overlays.
    Use when: checking what was intended to land or deciding whether a promoted rule is stale.
    Extend by: correcting a promoted route with current active-file proof or adding a new finalization facet that the existing five reports did not cover.
    Route: active owner file named by the report; do not preserve promotion prose in this archive.

`track-owner-routing`
    Direction: second-pass synthesis that collapses broad findings into owner routes and overlay deltas.
    Facets: standards shape, C# overlay correction, cross-language internalization taxonomy, root layering, and tests plus bridge refinement.
    Use when: a future overlay edit needs the rationale behind a parent/leaf split or route-away rule.
    Extend by: folding a new owner route into the nearest existing synthesis report when the merge key matches; create a new report only for a new owner family or contradiction.
    Route: `docs/standards/agents-md.md` for meta-rules and the affected local overlay for implementation guidance.

`track-source-scans`
    Direction: first-pass evidence gathering across repo folders, instruction practice, language polymorphism, root layering, and tests plus bridge proof boundaries.
    Facets: Assay, C# family, C# platform leaves, standards corpus, provider instruction practice, C#, TypeScript, Python, Bash, SQL, root layering, root integration, tests, and bridge.
    Use when: later synthesis needs the original proof trail or when a new report must avoid repeating a first-pass source scan.
    Extend by: adding only a new source class, a missed folder family, a stronger language/tooling rail, or a correction to a cited source claim.
    Route: synthesize into `track-owner-routing` before promoting to active standards.

## [3][NEXT_PASS_RULE]

Before adding a report, name the track card the new work extends and the merge key it changes. If the new work only validates a prior finding, update the relevant report or promotion state instead of adding a file. If it changes the direction of a lane, add the correction beside the earlier claim and update this README's track card so the next agent sees the new path first.

## [4][PROMOTION_STATE]

Durable rules from this session were promoted into active standards and overlays during the June 2026 rebuild. Keep the reports that preserve unique evidence, proof gaps, or candidate wording; drop transcript-only or validation-only material when it no longer explains a current route.
