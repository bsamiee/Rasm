language js(typescript, jsx)

`const $alias = $target` where {
    $alias <: r"^[A-Za-z_$][A-Za-z0-9_$]*$",
    $target <: r"^[A-Za-z_$][A-Za-z0-9_$]*$",
    $target <: not or {
        `undefined`,
        `null`,
        `true`,
        `false`,
        `NaN`,
        `Infinity`,
        `globalThis`
    },
    $alias <: not within arrow_function() ,
    $alias <: not within `class $klass { $members }` ,
    register_diagnostic(span=$alias, message="Declaration aliases an identifier, export the owner under its canonical name and reference it directly", severity="error")
}
