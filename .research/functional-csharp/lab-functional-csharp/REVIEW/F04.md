# F04 review - 04-language-features.md

Checked: every named member against the assembly and `F04/*.cs`; all six `csharp` blocks against the lab
(all six match verbatim, modulo the `namespace` line); the banned-token, cross-reference, `var`, and hedge
sweeps (all empty); `21-languageext-result-types.md` rows 9-12 (no contradiction: `readonly struct`,
`abstract class`, `abstract record class`, `abstract record class`).

No fabrication found. `public readonly struct Option<A>` (`sigs/Option_1.txt:22`), `public abstract class
Fin<A>` (`sigs/Fin_1.txt:15`), `public abstract record Either<L, R>` (`sigs/Either_2.txt:29`), `public
abstract record Validation<F, A>` (`sigs/Validation_2.txt:34`), `Match` on each of the four
(`Either_2.txt:75`, `Validation_2.txt:75`, lab `Alternatives.cs`), `Prelude.Optional<A>(A?) : Option<A>`
(`sigs/Prelude.txt:7333`), `Seq<A>.Map<B>(Func<A, B>) : Seq<B>` (`sigs/Seq_1.txt:765`).

1. Location: heading at line 113, and heading at line 195.
   Claim: "## Modeling alternatives" and "## Nullable reference types".
   Evidence: the book headings are "## Modeling Alternatives Explicitly" (chpater-3.md:85) and "## Nullable
   Reference Types as a Boundary Guard" (chpater-3.md:198). Every other heading in the file keeps the book
   spelling. The BRIEF keeps the heading order and the section structure; nothing in the two headings became
   false. Both renames are unforced.
   Correction: restore `## Modeling Alternatives Explicitly` and `## Nullable Reference Types as a Boundary
   Guard`.
   (The removal of the `Customer` flag class and the two variant classes under the first heading is not a
   defect: the `StandardBankAccount` hierarchy in the pattern-matching section already shows the variant
   model, and one owner per concept applies.)

2. Location: line 195-197, the whole "Nullable reference types" section.
   Claim: "Nullable reference types are compiler analysis, not a new runtime type, so a nullable reference
   enters the domain through `Optional(x)` as an `Option<A>`."
   Evidence: the book section carried three further claims, none of which names a hand-rolled type, claims a
   missing C# capability, or describes a removed code block: the compiler warns on an uninitialized
   non-nullable property and on a `null` assignment to a non-nullable reference (chpater-3.md:208); `?`
   belongs only where null is a deliberate part of the representation (chpater-3.md:210); an external source
   that carries nullable values is isolated in the parsing code and converted before the value travels
   further (chpater-3.md:221). All three still hold under LanguageExt, and the third is the boundary rule the
   BRIEF asks each file to state once. The section as it stands is one sentence and states no boundary.
   Correction: replace line 197 with:
   `Nullable reference types are compiler analysis, not a new runtime type. The compiler warns when a`
   `non-nullable property stays uninitialized and when a caller assigns null to a non-nullable reference.`
   `Write `?` only where null is a deliberate part of an external representation. That representation stops`
   `at the boundary that reads it: a nullable reference enters the domain through `Optional(x)` as an`
   `Option<A>`, and the domain receives no null.`

3. Location: line 156.
   Claim: "`Seq<A>` is immutable, so the nested value needs no defensive copy, and a mutable child object
   still does."
   Evidence: "still does" elides "needs a defensive copy" across an intervening negation, so the sentence
   reads two ways. The BRIEF's prose rules require one unambiguous statement per sentence.
   Correction: `` `Seq<A>` is immutable, so the nested value needs no defensive copy. A mutable child object
   still needs one. ``

4. Location: lines 158-170, "Init-only properties".
   Claim: the `readonly struct Movie` block declares `public string Title { get; init; }` with no `required`,
   while the record block at line 177 declares `public required string Title { get; init; }`.
   Evidence: lab `Samples.cs:59` constructs `Init.Movie` through an object initializer; the same initializer
   with `Title` omitted compiles and leaves a null `string`, because CS8618 does not fire on a struct
   (NOTES/F04.md records the struct compiling without `required` and the record needing it). The file shows
   the two spellings without saying why they differ, so a reader copies the struct form and admits a missing
   value.
   Correction: append to line 170: `A struct with `init` properties admits an initializer that omits a
   property, so mark a property the value cannot do without as `required`.`

5. Location: lines 16-29, the tuple block.
   Claim: `Catalogue.GetFilm(id)` and `Catalogue.GetCastList(id)`.
   Evidence: `Catalogue` is defined in lab `Tuples.cs:5-17` over two `HashMap` values and is deliberately kept
   out of the doc (NOTES/F04.md, "Host-only and uncompiled blocks"). The doc names the type in code and never
   in prose, so the block reads as a reference to a type the chapter never introduces. The book had the same
   gap with bare `GetFilm`, so this is inherited, not new; one clause closes it without adding a block.
   Correction: extend line 14 to end `... between `Map` operations on a `Seq<A>`, where `Catalogue` is the
   lookup source the pipeline reads:`

Blockers: none
