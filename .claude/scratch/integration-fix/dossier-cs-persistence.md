# [DOSSIER_CS_PERSISTENCE_INTEGRATION]

Blueprint overturning `RASM-CS-PERSISTENCE-DECISION.md` `[05]-[ROSTER_DELTA]` PRUNE table. Law: every admitted package integrates lib-grade at its proper home; zero-consumer, charter-misplacement, and sibling-redundancy findings are integration gaps, never prune proofs. Every row below is disk- or feed-anchored. The DECISION's page-set, band registry, seam ledger, leg partition, and V1-V13 rulings stand except where a row below names the exact edit.

## [01]-[PER_PACKAGE_INTEGRATION]

### `PollinationSDK` — the cloud-domain store integration (deepest treatment)

**Pruned as**: "charter misplacement — a domain cloud SDK with no store-class concern" (DECISION `[05]` PRUNE row 1). **Refuted**: the SDK's own catalog (`libs/csharp/Rasm.Persistence/.api/api-pollination-sdk.md:121-128`) records THREE store-class concerns — a presigned-S3 artifact plane (`ArtifactsApi.CreateArtifactAsync → S3UploadRequest`, `DownloadArtifactAsync`, `ListArtifactsAsync → FileMetaList`), a content-keyed result-landing seam ("a downloaded `RunAsset` lands as a content-keyed (`XxHash128`) blob in the Persistence store"), and a lineage seam ("a completed cloud run becomes a content-addressed, lineage-tracked artifact set"). The charter misplacement is real for HALF the SDK — the job-submission/orchestration half (`JobsApi.CreateJobAsync`, `RunsApi.GetRunAsync`/`GetRunOutputAsync`, `Wrapper.JobInfo`/`JobRunner`/`ScheduledJobInfo.WatchJobStatusAsync`) is compute-shaped and ROUTES to `Rasm.Compute` (`[04]` row 1) — but routing half a package is not pruning it; the durable half is store-shaped and lands here.

**Persistence integration — three axis rows, each on an existing owner:**

1. **`Store/blobstore` — the presigned-grant acquisition row.** The `ObjectStore` `[SmartEnum<string>]` axis (blobstore.md:237-240, four rows s3/azure-blob/gcs/minio, each carrying `PartSize`/`ChunkPolicy`/`conditionalWrite`/`Integrity`/`Tier`/`Encryption`) gains ONE row — `ObjectStore.Presigned` — whose `ObjectClient` case carries a `GrantMinter` delegate `Func<GrantRequest, IO<ObjectGrant>>` instead of endpoint+credential: the leg fills the nine `ObjectLeg` delegates (blobstore.md:311-312 `ObjectIo.For` resolves per-provider legs through `ObjectClient.Map`) by minting a grant per operation and executing it over the SAME transfer engine. `ObjectGrant` is a two-case `[Union]` matching the verified wire shapes: `FormPost(Uri Url, HashMap<string,string> Fields)` — the upstream `S3UploadRequest { string Url, Dictionary<string,string> Fields }` executed as one `multipart/form-data` POST carrying every field plus the payload (repo-verified `Helper.UploadArtifactAsync`, `Helper.cs:177-206`) — and `SignedUrl(Uri)` for GET (upstream `DownloadArtifact`/`GetRunOutput` return signed URLs). This is GENERIC parameterization: any presigned-grant cloud domain (Pollination projects, a partner CDE, any S3 POST-policy or signed-URL service) is one `GrantMinter` value — deployment DATA, zero central edits. Pollination is the SEED minter: `ArtifactsApi.CreateArtifact(owner, name, KeyRequest{Key}) → S3UploadRequest` mints write grants, `DownloadArtifact(owner, name, path)`/`JobsApi.DownloadJobArtifact` mint reads, `ListArtifacts(owner, name, path, page, perPage) → FileMetaList` fills `Head`/`Stat` (path-match on `FileMeta { Key, FileType, FileName, LastModified, Size }`). Honesty rows, all feed-verified as upstream ABSENCES: `FileMeta` carries NO checksum/etag → `ObjectChecksum.None` (the GCS/Minio SDK-native-stance precedent, blobstore.md:32-43); no multipart/resumable upload exists → the `ChunkPolicy` row pins single-shot and `conditionalWrite: false`. Fault: `RemoteStoreFault.GrantExpired(ContentAddress, Instant)` — ONE new case in the existing 5400 band, no new decade.
2. **`Query/cache` — the cloud-run reuse rows.** `cache.md:20` is the load-bearing growth law ("a new artifact family is one `ArtifactKind` row carrying its retention class and lane"). Cloud-run results are compute-result reuse EXACTLY as the index charters (`ModelResultIndex` recency horizon; `ArtifactKind` rows `ep-context`/`onnx-profile` already model compute contexts, cache.md:31-32): add `ArtifactKind.CloudRun = new("cloud-run", RetentionClass.Cache, CacheLane.ModelResult)` — the index row content-keys the run receipt over the LENGTH-FRAMED preimage (recipe digest · input-asset content keys · project slug), where the recipe digest is the verified upstream member `PackageVersion.Digest` (`RecipesApi.GetRecipeByTag(owner, name, tag) → RecipePackage`; `Model/PackageVersion.cs` carries `Name`/`Tag`/`Digest`/`Manifest`), so a re-submitted identical recipe+inputs resolves to the prior run's landed assets without a cloud round-trip. The run's output assets land as `ArtifactKind.Interchange` blob rows through the ONE `Admit` path (cache.md:16-17, seam `ContentAddress.Of(bytes)`); the `Store/blobstore` presigned row carries the bytes. This row SUPERSEDES the SDK's own reuse mechanics, which are verifiably weak: `Helper.CheckCached` is path-existence-only with zero hash verification, and `Wrapper.LocalDatabase` is a bare SQLite job ledger — the content-keyed index adds the integrity the upstream cache lacks, the exact store-class value the prune denied existed. No new type — rows on the one taxonomy.
3. **`Version/provenance` — the cloud-run attribution rows.** A completed run is a W3C-PROV `Activity` (the page's exact typing charter): Agent = the service principal behind `Configuration.AccessToken` (`TokenRepo`), Used = input-asset content keys, Generated = output-asset content keys, the recipe reference (`owner/name:tag` + source digest) as the plan. This is data on `ProvNode.Of`/`AttestedLedger.Append` — the attested verify makes an externally-computed result tamper-evident locally, the federation-wide verify template the page already claims. No new band (provenance stays no-band).

**Boundary law carried**: token lifecycle stays the composition root's (`api-pollination-sdk.md:119` — `Configuration`/`TokenRepo` handed over, never a Persistence fence member; dual auth verified: `x-pollination-token` API-key header + JWT `Authorization: Bearer` via `Configuration.AccessToken`); the vendored `LBT.RestSharp`/`LBT.Newtonsoft.Json` fork closure never touches the STJ rails (`:118`); the SDK never loads in-Rhino (`:19`). **Leg placement**: blobstore row + provenance rows + cache rows land with their owners (legs 4, 2, 3); the `.api` re-anchor (`api-pollination-sdk.md` → `Store/blobstore#OBJECT_STORE` + `Query/cache#ARTIFACT_BLOB_INDEX` + `Version/provenance` + the `Rasm.Compute` counterpart pointer) lands leg 1 with the roster motion, folding the verified `S3UploadRequest`/`FileMeta`/`PackageVersion.Digest`/`Helper` rows in. **Feed** (registration API + repo, verified): MIT, netstandard2.0, 1.10.0 published 2026-02-04, 160 versions, ~108k downloads, `pollination/csharp-sdk` active (last push 2026-02-04) — a live, healthy admission, not an abandonment candidate.

### `ScyllaDBCSharpDriver` — the wide-column index-residency row

**Pruned as**: "no wide-column axis owner; the wide-column concern is redundant against the columnar-OLAP ∪ LSM-KV union" (DECISION `[05]` PRUNE row 5, with forward re-admission recorded). **Refuted by the DECISION's own axis map**: the redundancy proof compares engines, not WORKLOADS. The store carries a named workload neither ClickHouse (analytical MPP, no point-write LWT) nor rocksdb/LMDB (embedded, single-node) can own: the DISTRIBUTED content-index at corpus scale — partition-keyed point lookups with conditional write-once claims and tunable consistency. Two owners already spell that workload on disk: `Query/cache#ARTIFACT_BLOB_INDEX` (`ArtifactIndexRow` content-keyed admission, cache.md:14-21 — billions of content keys at federation scale) and `Store/blobstore` `BlobResidence` (blobstore.md:160-161 — the residence catalog beside the object plane). The driver's LWT is the exact semantics match: `Mapper.InsertIfNotExistsAsync<T> → AppliedInfo<T>` (api-scylladb.md:76-82) IS the write-once claim the blobstore seal spells as 412-noop and the benchmark-claim gate spells as first-writer-wins — a distributed `Admit` whose duplicate registration is `Applied=false`, never an error.

**Integration shape**: the `[V13]` axis map gains the **wide-column content-index axis** — owning page `Query/cache` (the index owner; blobstore residence rows ride the same row family): provider rows `marten-pg` (default, the existing in-PG index) · `scylla-widecolumn` (scale-out). The scylla row: partition key = `(TenantId, ArtifactKind)`, clustering = recency/`ContentAddress`, `InsertIfNotExistsAsync` for claim-gated admission, `FetchPageAsync<T>`+`PagingState` for the sweep scans, `ConsistencyLevel`/retry as named `ExecutionProfiles` rows (api-scylladb.md:223 — policy rows, never per-call branching), `TokenAwarePolicy(DefaultLoadBalancingPolicy)` shard routing. Prepared-statements-only law carried (`:216`). Fault conversion at ONE boundary: `DriverException` family → new `WideColumnFault : Expected` `[Union]` (`Unavailable`/`WriteTimeout`/`LwtRefused`/`HostDown`) — band **8450** (headroom above `CoordinationFault` 8430 and the new 8440; DECISION `[03]` records 8440+ growth space). `CqlVector<T>` recorded embedding-next-to-row only — Qdrant/pgvector keep the ANN axis (`:226`). The SoR-spine seal is untouched: the index row is a PROJECTION residence, never a second event store, materializer, identity, or changefeed. **Selection**: deployment DATA on the axis — a single-node deployment never composes the row; a federation-scale deployment flips one policy value. **Leg placement**: leg 3 (cache's leg) for the axis row; the `[V13]` map re-emit rides leg 4 (provisioning). `.api` re-anchor → `Query/cache#ARTIFACT_BLOB_INDEX`.

### The NTS tier (4 packages) — the spatial store plane

**Pruned as**: "no persisted NTS geometry column" (DECISION `[05]` PRUNE row 2). **Refuted on disk twice over**: (a) the BRIEF's own reconciliation ruled two of the four KEEP on LIVE consumers the DECISION prune contradicts without new evidence — `codec.md:53` composes `GeoJsonProjection.Default.Factory` into the package STJ options and `codec.md:134-136` is the full `GeoJsonProjection` record over `GeoJsonConverterFactory` (verified on disk this pass); `identity.md:35` imports `NetTopologySuite.Geometries` and `identity.md:135-165` mints H3 cells through NTS `Point`/`Envelope` (`H3.H3Index.FromPoint(new Point(...) { SRID = 4326 })`). (b) The provisioning roster ships `postgis`, `postgis_raster`, `postgis_sfcgal`, `pgrouting`, `h3-pg` as provisioned `ServerExtension` rows (README.md:64-77) — a store that provisions PostGIS and routes `pgrouting` but cannot bind ONE geometry value on the wire is structurally dishonest. The gap is the unfinished spatial COLUMN lane, not a dead tier. Per-package:

1. **`Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite`** → `Element/identity` (leg 1). The `[V6]` EF commit extends one clause: `UseNetTopologySuite()` on the provider options beside `UseValueObjectValueConverter()`+`UseSnakeCaseNamingConvention()`; `element_identity` gains the `Envelope`-derived `geometry(Polygon, 4326)` bounds column + GiST index row in the V6 migration owner (the DDL rail `ServerExtension.CreateSql` already commits through — `postgis` is a provisioned row). The spatial predicate then rides `EF.Functions` PostGIS translators (api-nts-ef.md:58,67-69 — `ST_Union`/`ST_Extent` aggregates, `.Distance`/`.Intersects` methods) so a bounds query is server-side SQL, never a client scan. The H3 cell (`node_cell`) and the PostGIS bounds are complementary rows on the one spatial-key axis: H3 for cell-bucket equality joins, the geometry column for exact predicates — `identity.md:179` already pins in-process/in-PG cell parity.
2. **`Npgsql.NetTopologySuite`** → `Store/provisioning` (leg 4). The ADO wire-codec admission is a DATA-SOURCE row, not an EF concern: `NpgsqlDataSourceBuilder.UseNetTopologySuite(...)` (api-npgsql-nts.md:31-35) registers on the `StoreProfile` data-source build so RAW Npgsql lanes read/write geometry — the `cypher.md` pgrouting results, the provisioning verification probes over PostGIS, and any `QueueSqlCommand` spatial write. `geographyAsDefault`/precision/ordinates are profile policy values (`:39-42`), parameterized never call-site literals. The DECISION's "the kept EF bridge transits it" was half-true — transiting the ASSEMBLY does not admit the ADO codec; the `UseNetTopologySuite` data-source call is this package's namespace and the EF plugin does not place it on raw connections.
3. **`NetTopologySuite.IO.GeoJSON4STJ`** → `Element/codec` (leg 1) — ALREADY LIVE (`codec.md:53,134-136`); the prune would have deleted a composed consumer. Integration deepens: the `GeoJsonProjection` row also serves the NEW geospatial ingress (below) — feature `properties` reify through `IPartiallyDeserializedAttributesTable.TryDeserializeJsonObject<T>` under the SAME converter graph (api-nts-io.md:67).
4. **`NetTopologySuite.IO.GeoPackage`** → **NEW `Ingest/geospatial.md`** (leg 3). The `[A.4]` Ingest growth law is verbatim ("the next foreign-file codec into the record rail lands as a page HERE") and the BRIEF's `[V7]` already ruled this package "re-disposes to a `[V11]` ingest format row on the `Ingest/` codec axis". The page: ONE `GeoSource.Run(GeoSpec)` (`[Union]`) owner over three format rows — `GeoPackage` (`GeoPackageGeoReader`/`GeoPackageGeoWriter` GPB header+WKB body over the ALREADY-ADMITTED `Microsoft.Data.Sqlite`, api-nts-io.md:74-78), `GeoJson` (feature collections through the codec's `GeoJsonProjection` factory), `Wkb`/`Wkt` (core-NTS IO, transitive) — projecting features into the record rail exactly as tabular law states (row shape only; the app composition root owns the geo→element map, mirroring ARCH:61). H3 derivation at ingest composes `identity#SPATIAL_CELL` (`pocketken.H3`, cell-parity law). Faults: `GeoIngestFault` `[Union]` = `CodecReject`/`CrsUnsupported`/`GeometryInvalid` — band **8440**. Ingest grows 2→3 pages; the folder law strengthens.

### `Microsoft.EntityFrameworkCore.Sqlite` — the embedded relational provider row

**Pruned as**: "the embedded floor is raw ADO by design (`provisioning.md:369-434`)" (DECISION `[05]` PRUNE row 3). **Refuted by the DECISION's own V6 ruling**: V6 commits the identity tier to generated EF mapping (`UseValueObjectValueConverter()` + `UseSnakeCaseNamingConvention()`, the hand-`NodeId`-converter dies) AND scopes the embedded charter to "relational identity floor + `EngineOps` checkpoint/snapshot/backup + read-only/offline tier". Those two rulings together make the prune self-contradictory: the embedded RELATIONAL IDENTITY FLOOR must map the SAME `ElementIdentity` model — with the prune, the only path is a hand-rolled raw-ADO SQLite mapping beside the generated PG mapping, which is the exact form `ARCHITECTURE.md:113` deletes ("NEVER hand-written converters ... beside the generated rails") and the exact hand-mapping die-off V6 celebrates. The `provisioning.md:369-434` anchor covers `EmbeddedRitual`/`EmbeddedStore.Open` (PRAGMA lanes) and `EngineOps`/`HandleBridge` (`provisioning.md:476-615`, `sqlite3_snapshot_*`/`backup` native crossings) — raw ADO stays THE owner there; it never spoke for the identity floor.

**Integration shape**: ONE identity `DbContext`, TWO provider rows — `StoreProfile.Postgres` binds `UseNpgsql` (+NTS, +NodaTime, +Thinktecture.EF), `StoreProfile.Embedded` binds `UseSqlite` (api-ef-sqlite.md:33-36 admission family) over the connection the `EmbeddedStore.Open` ritual already dialed — provider variance as row data on the closed `StoreProfile` axis (`provisioning.md:39` `StoreProfile.Embedded` exists), the `[V13]` law verbatim. Provider-divergent mappings (no PostGIS on SQLite: the bounds column degrades to the H3 `bigint` cell + WKB blob row; no pgvector: the vector column is absent on the embedded floor) are model-building rows keyed by `Database.IsSqlite()` (`:36`), never a second context. `Microsoft.EntityFrameworkCore.Design` (already COMMITTED per V6) then generates BOTH providers' migrations through the one migration owner. Raw ADO keeps `EmbeddedRitual`/`EngineOps`/`HandleBridge` untouched. **Owning pages**: `Element/identity` (the provider row, leg 1) + `Store/provisioning` (the `StoreProfile.Embedded` admission clause, leg 4). Fault: rides `IdentityFault` 8340/`SchemaVerdict` — no new band.

### `StackExchange.Redis` — the L2 backplane + stream-sink rows

**Pruned as**: "the L2 swap row keeps `Microsoft.Extensions.Caching.StackExchangeRedis`, which transits the driver" (DECISION `[05]` PRUNE row 4). **Refuted by capability arithmetic**: the caching bridge exposes ONLY the `IDistributedCache`/`IBufferDistributedCache` get/set surface (api-redis.md:69-70). The direct driver owns store-class capability the bridge structurally cannot reach and the corpus already needs:

1. **`Query/cache` — the L2 invalidation backplane row** (leg 3). `CacheL2Store` is Marten-backed today with a Redis SWAP row (cache.md:179-186, 191). The swap row without invalidation is a stale-read generator: HybridCache L1 entries on N processes outlive an L2 eviction. The direct driver's `ISubscriber`/`ChannelMessageQueue` (api-redis.md:39,41 — async-enumerable pub/sub) is the backplane: ONE invalidation channel keyed `(storeKey, tenant)` carrying evicted-key beats; each process's HybridCache tag-invalidates on receipt. Lossy-by-design (a missed beat is a TTL-bounded stale, not corruption) — the PRESENCE-lane precedent. This is a composition ROW on the existing L2 owner, `CacheLane.Store`-gated, active only when the Redis swap row is the deployed L2 — deployment DATA.
2. **`Version/egress` — the `EgressSink.RedisStream` delivery row** (leg 4). One sink row on the `[V3]` `EgressSink` `[Union]` per the generator law ("the next sink is one data row"): `XADD` via `IDatabase` stream ops (`StreamEntry`/`StreamPosition`/`NameValueEntry`, api-redis.md:56-60), the CloudEvent `id`=`OpLogEntry.ContentKey` carried as the dedup field, consumer-group ack (`StreamReadGroup`) advancing the per-sink cursor, `StreamTrimMode.Acknowledged` (`:59`) as the retention stance. Delivery honesty stated per the V3 owner law: at-least-once with receiver-side id-dedup — the same stance as the webhook row. Faults ride `EgressFault` 8270 (existing union; sinks are rows).

`Microsoft.Extensions.Caching.StackExchangeRedis` stays the L2 swap row exactly as ruled; the direct reference now has two named fences. `ConnectionMultiplexer` is a long-lived singleton the composition root supplies — connection input, not a fence member. `.api` re-anchor → `Query/cache#L2_CONTRIBUTION` + `Version/egress`.

### `linq2db.EntityFrameworkCore` — CONDITIONAL resolves to COMMIT

**Conditioned as**: "composes a real BulkCopy fence in `Ingest/tabular` → KEEP, else LEAVES" (DECISION `[05]`). **The condition is already met on disk**: `tabular.md:20` charters the rail verbatim — "the bulk-copy rail (`api-linq2db-ef`) consumes the typed `IEnumerable<T>`/`IAsyncEnumerable<T>` a `Read<T>` yields (`BulkCopy`/`BulkCopyAsync` are `IEnumerable<T>`-sourced), while the `MiniExcelDataReader : IDataReader` from `Reader` feeds the `Query/columnar` Arrow/DuckDB materializer — the reader is the columnar source, the typed enumerable the bulk source, never conflated." The BRIEF `[04]:201` graded it KEEP-AND-COMMIT on this anchor; the DECISION's demotion cites no new evidence. **Integration shape**: the composed fence lands in `Ingest/tabular`'s rebuild (leg 3): `LinqToDBForEFTools.BulkCopyAsync<T>(context, BulkCopyOptions, IAsyncEnumerable<T>, ct) → Task<BulkCopyRowsCopied>` (api-linq2db-ef.md:74-78, typed receipt) over the V6 identity `DbContext` — `ProviderSpecific` lowers to PG binary COPY, the mapping schema derives from the EF model (no second mapping, `:47` `EFCoreMetadataReader`). Redundancy boundary stated as law: DuckDB `COPY` owns columnar FILES, Npgsql binary import owns raw-wire streams, linq2db owns the EF-MODEL-MAPPED bulk lane — three non-overlapping rows on the one bulk-ingress rail. Fault: `TabularFault.BulkRefused` in-band 8390.

### `MPXJ.Net` — ruled KEEP; carried with one deepening

The DECISION's V11 ruling stands (Ingest/schedule mints, BIM:102 the consumer). Two riders: (a) the WRITE half is chartered, not dropped — `UniversalProjectWriter(FileFormat).Write` over the seven writable members (api-mpxj.md:68-70,83: `XER`/`PMXML` P6 round-trip, `MSPDI`/`MPX`, `JSON`) is the schedule EGRESS row on the same `ScheduleSource` owner, so the durable `TaskRelation` DAG round-trips OUT of the store, not merely in; `Relation.Builder`/`Duration.GetInstance` (`:71-72`) are the synthesis members. (b) version drift: the manifest pins `16.5.0` (Directory.Packages.props), the catalog says `16.4.1`, the DECISION cites `16.4.1` — the leg-1 catalog motion re-verifies at the pinned 16.5.0. Leg 3 as ruled.

### `FlowtideDotNet.Substrait` set — ruled KEEP; carried unchanged

V1's REINTRODUCE ruling is integration-first already and stands whole: `FlowtideDotNet.Substrait@0.15.0` (shipped-public `Plan.Parser.ParseFrom` + `SubstraitDeserializer.Deserialize`), `Apache.Arrow.Adbc`+`.Drivers.{Apache,BigQuery}` (SourceKind rows), `Apache.Arrow.Flight` (bulk-result hop), the DuckDB `substrait` extension row, `Grpc.Tools` DROP, `Google.Protobuf` sole runtime dep. This dossier adds ONE stacking clause: the `SourceKind.SignedArtifact` binding row (provenance-gated) also resolves cloud-run artifacts landed via the Pollination rows above — a federated read over an attested cloud result is the composed case of `[01]` rows 2-3, zero new surface. Leg 3 as ruled.

## [02]-[STORE_UNIVERSALITY]

The `[V13]` STORE_AXIS_MAP extends from 7 axes to 11; every store-class concern a future app can name resolves to an axis whose provider rows are deployment/policy DATA. Additions in **bold**; unchanged rows abbreviated.

| Axis | Owning page | Provider/format rows (seed DATA) | Selection | Ceiling / charter proof |
|---|---|---|---|---|
| Relational SoR spine | `Store/provisioning`+`Element/graph` | postgres-18 (SINGULAR) | SEALED | unchanged — one event store/materializer/identity/changefeed |
| Object store | `Store/blobstore` | s3 · azure-blob · gcs · minio · **presigned-grant (`GrantMinter` delegate; Pollination the seed minter)** | `ObjectStore` `[SmartEnum]` | the presigned row reaches domain-cloud object planes no credentialed row can (the credential never exists client-side) |
| Egress sink | `Version/egress` | webhook · nats · kafka · rabbitmq · pulsar · wire-native · **redis-stream** | `EgressSink` `[Union]` | consumer-group ack + `Acknowledged` trim — the zero-broker-install stream row |
| Read-lane/analytic | `Query/columnar` | duckdb · pg_duckdb · clickhouse | `ColumnarEngine` axis | unchanged |
| Lakehouse interchange | `Query/columnar` | ducklake(fwd) · delta | format row | unchanged |
| Vector search | `Query/retrieval` | pgvector · pgvectorscale · pq-adc · qdrant | `VectorBackend` axis | unchanged; `CqlVector` recorded embedding-next-to-row, never a fifth row |
| Embedded/KV floor | `Store/provisioning` | sqlite(raw-ADO EngineOps) · rocksdb-lsm · lmdb | `EngineOps`-tier row | unchanged |
| **Embedded relational** | `Element/identity`+`Store/provisioning` | **npgsql-ef · sqlite-ef** | **`StoreProfile` axis on ONE DbContext** | one generated mapping, two providers; a hand ADO mapping beside the generated rail is the deleted form (ARCH:113) |
| **Wide-column content-index** | `Query/cache` | **marten-pg (default) · scylla-widecolumn** | index-residency row | LWT `AppliedInfo` claim-gate + shard-routed point reads at federation scale — the workload neither columnar-MPP nor embedded-LSM owns |
| **Cache backplane** | `Query/cache` | **none (single-node default) · redis-pubsub** | `CacheLane.Store`-gated row | cross-process L1 invalidation the `IDistributedCache` contract cannot express |
| **Spatial store plane** | `Element/identity` · `Store/provisioning` · `Element/codec` · `Ingest/geospatial` | **postgis-column(EF-NTS) · ado-codec(Npgsql.NTS) · geojson-stj · geopackage · wkb/wkt · h3-cell(pocketken)** | profile policy rows (`geographyAsDefault`, SRID, precision) | the provisioned postgis/pgrouting/h3-pg extension tier gains its wire, column, codec, and file-ingress counterparts — capability closed end-to-end |

Universality law restated as the acceptance bar: a future app selects providers by POLICY VALUES on these axes (profile rows, grant minters, sink rows, index-residency rows) — never a central-manifest edit, never a new entry point, never a parallel rail. Every row carries its provisioning/health/recovery posture through the verification-first fold (scylla and redis rows gain AppHost probe rows — `[04]`).

## [03]-[NEW_LEG_PROPOSAL] — exact DECISION edits

### `[02]` page-set edits (integration clauses; page count 24 → 25)

| Page | Edit |
|---|---|
| `Element/identity` (leg 1) | charter gains: `UseNetTopologySuite()` in the V6 provider options; `geometry(Polygon,4326)` bounds column + GiST row in the migration owner; the `StoreProfile.Embedded` `UseSqlite` provider row on the ONE DbContext (provider-divergent mappings as `IsSqlite()`-keyed model rows); the linq2db bulk-lane lift target |
| `Element/codec` (leg 1) | `GeoJsonProjection` row KEEPS and additionally serves `Ingest/geospatial` feature reification (same converter graph) |
| `Version/provenance` (leg 2) | cloud-run attribution rows (Activity=run, Agent=token principal, Used/Generated=content keys, recipe digest) on `ProvNode.Of`/`AttestedLedger.Append` |
| `Query/cache` (leg 3) | `ArtifactKind.CloudRun` row; the wide-column index-residency axis (`marten-pg`/`scylla-widecolumn` rows, `WideColumnFault` 8450); the redis invalidation-backplane row beside `L2_CONTRIBUTION` |
| `Ingest/tabular` (leg 3) | the linq2db `BulkCopyAsync` fence lands (CONDITIONAL → COMMIT); `TabularFault.BulkRefused` in-band |
| **`Ingest/geospatial.md` (leg 3, NEW · `new`)** | `GeoSource.Run(GeoSpec)` `[Union]` over GeoPackage/GeoJSON/WKB-WKT format rows → record rail; H3 derivation composes `identity#SPATIAL_CELL`; `GeoIngestFault` 8440; seams: `→ Rasm.Element` (row shape only, mirrors ARCH:61); `← Rasm.Bim/Semantics/geospatial` (counterpart, joins BIM:81) |
| `Ingest/schedule` (leg 3) | + the `UniversalProjectWriter` egress row (seven `FileFormat` members) on the same owner |
| `Version/egress` (leg 4) | + `EgressSink.RedisStream` row (XADD, consumer-group ack advance, `Acknowledged` trim) |
| `Store/blobstore` (leg 4) | + `ObjectStore.Presigned` row with the `GrantMinter` delegate case on `ObjectClient`; Pollination seed minter; `RemoteStoreFault.GrantExpired` in-band 5400 |
| `Store/provisioning` (leg 4) | + `NpgsqlDataSourceBuilder.UseNetTopologySuite` data-source admission row (profile policy values); + the `StoreProfile.Embedded` EF admission clause; the `[V13]` axis-map re-emit carries the four new axes |

### `[03]` band registry edits

Two new decades in the recorded 8440+ headroom: **8440 `GeoIngestFault`** (`CodecReject`/`CrsUnsupported`/`GeometryInvalid`, owner `Ingest/geospatial`) · **8450 `WideColumnFault`** (`Unavailable`/`WriteTimeout`/`LwtRefused`/`HostDown`, owner `Query/cache`). In-band case adds: `RemoteStoreFault.GrantExpired` (5400), `TabularFault.BulkRefused` (8390). Growth headroom moves to 8460+.

### `[05]` ROSTER_DELTA rewrite — PRUNE table → INTEGRATE table

| Package | Disposition | Home (axis · page · leg) |
|---|---|---|
| `PollinationSDK` | INTEGRATE (durable half) + ROUTE (job half → `Rasm.Compute`) | presigned-grant object row (blobstore, 4) · `ArtifactKind.CloudRun` (cache, 3) · PROV attribution (provenance, 2) |
| `Npgsql.EntityFrameworkCore.PostgreSQL.NetTopologySuite` | INTEGRATE | V6 EF spatial column + GiST (identity, 1) |
| `Npgsql.NetTopologySuite` | INTEGRATE | data-source codec admission row (provisioning, 4) |
| `NetTopologySuite.IO.GeoJSON4STJ` | KEEP (live) + INTEGRATE | codec `GeoJsonProjection` (1) + geospatial ingress reification (3) |
| `NetTopologySuite.IO.GeoPackage` | INTEGRATE | `Ingest/geospatial.md` format row (3) |
| `Microsoft.EntityFrameworkCore.Sqlite` | INTEGRATE | embedded relational provider row on the ONE DbContext (identity 1 · provisioning 4) |
| `StackExchange.Redis` | INTEGRATE | L2 invalidation backplane (cache, 3) + `EgressSink.RedisStream` (egress, 4) |
| `ScyllaDBCSharpDriver` | INTEGRATE | wide-column content-index row + `WideColumnFault` 8450 (cache, 3) |
| `linq2db.EntityFrameworkCore` | COMMIT (conditional resolved by `tabular.md:20`) | `BulkCopyAsync` fence (tabular, 3) |
| `MPXJ.Net` | KEEP as ruled + write-egress rider | schedule (3); re-verify at pinned 16.5.0 |
| FlowtideDotNet.Substrait set | KEEP as ruled + SignedArtifact/cloud-run stacking clause | federation (3) |

The leg-1 manifest motion becomes integrate-only: ZERO packages leave; every previously-pruned catalog RE-ANCHORS to its live owner above instead of deleting (`api-pollination-sdk`→blobstore/cache/provenance+Compute pointer, `api-scylladb`→cache, `api-npgsql-nts`→provisioning, `api-nts-ef`→identity, `api-nts-io`→codec+geospatial, `api-ef-sqlite`→identity+provisioning, `api-redis`→cache+egress, `api-linq2db-ef`→tabular+identity). The `[05]` reversibility clause inverts: integration rows are additive and individually revertible by the same one-row motion. AppHost probe-row riders (`[04]`) travel with the leg that lands each backend row.

### Leg partition — extended in place, no fifth leg

The four legs stand; the integration clauses land inside each page's ONE rebuild pass (a fifth leg would re-open landed pages — rejected). Target-list delta: leg 3 gains `libs/csharp/Rasm.Persistence/.planning/Ingest/geospatial.md` (NEW), ordered after `tabular.md`. Riders per leg: leg 1 carries the rewritten roster motion + all eight catalog re-anchors + the 8440/8450 registry rows (registry lands COMPLETE in leg 1 per the existing gate); leg 2 the provenance rows; leg 3 the cache/tabular/geospatial/schedule rows; leg 4 the blobstore/provisioning/egress rows + the `[V13]` 11-axis re-emit. Acyclicity is preserved: every new edge is intra-leg forward (`geospatial → identity#SPATIAL_CELL` is leg-3→leg-1, downward-fine; `cache scylla row` touches no seam; `blobstore presigned` adds no cross-page edge) — zero new upward or cross-leg-backward edges. The campaign-acceptance dry-runs gain one composed case: (e) SPATIAL+CLOUD — a GeoPackage ingests to the record rail with an H3 cell + bounds column landing server-side; a cloud-run artifact set lands presigned-grant → content-keyed blob → PROV-attested → reuse-indexed, and a re-submitted identical recipe resolves from the index without a cloud call.

## [04]-[COUNTERPART_OBLIGATIONS] — touchpoints owned by other campaigns

| # | Obligation | Owning campaign / doc |
|---|---|---|
| 1 | `Rasm.Compute` gains the CLOUD ASSESSMENT provider row: `PollinationSDK` job orchestration (verified members: `JobsApi.CreateJob(owner, name, Job) → CreatedContent{Guid Id}`, `JobStatusEnum`/`RunStatusEnum` terminal states, `RunsApi.GetRun`/`GetAllRunSteps`/`GetRunStepLogs`, `JobRunner.RunOnCloudAsync(Project, …) → CloudJob`, `ScheduledJobInfo.SyncCloudJob()`/`IsCloudJobDone(out string)`, `RunInfo.GetOutputAssets(platform) → List<RunOutputAsset>`) as a row on the `Analysis`/`Assessment` spine — `Rasm.Compute/ARCHITECTURE.md:53` (Assessment fact-stream) and `:181` (the parameterized `EnergyToolchain` discovery this row extends to the cloud leg); the durable landing composes the Persistence rows of `[01]` (blob via presigned grant, index via `ArtifactKind.CloudRun`, lineage via PROV) — `Rasm.Compute/ARCHITECTURE.md:86` already routes AssessmentPayload blobs to Persistence retention; the SDK's `Wrapper.LocalDatabase` SQLite job ledger and path-existence `CheckCached` are NOT composed (the Persistence index owns reuse) | Compute campaign (`RASM-CS-COMPUTE` brief when opened); seam rows added to `Rasm.Compute/ARCHITECTURE.md` |
| 2 | `Rasm.AppHost` health-probe roster: APPHOST:85 was ruled Npgsql-only; the scylla and redis rows re-enter as DEPLOYMENT-CONDITIONAL probe rows (probe present only when the axis row is composed) — the ruled default stays Npgsql-only for the default deployment | AppHost campaign (`RASM-CS-APPHOST-BRIEF.md` `[V6]` probe-row law) |
| 3 | `Rasm.Bim` geospatial semantics ⇄ `Ingest/geospatial`: BIM:81 (GDAL/OGR GeoParquet → `Query/columnar`, already homed) gains the sibling row `Rasm.Bim/Semantics/geospatial ⇄ Persistence/Ingest/geospatial` (feature ingress); the Bim campaign re-points the row interior | Bim campaign (`Rasm.Bim/ARCHITECTURE.md:81`) |
| 4 | `Rasm.Bim` schedule egress: BIM:102's counterpart widens read→round-trip (the `UniversalProjectWriter` XER/PMXML egress row) | Bim campaign (BIM:102 row interior) |
| 5 | `Rasm.Element` spatial-bounds producer contract: the `Envelope` the identity bounds column persists derives from the node's representation bounds — Persistence records itself the demanding consumer of a seam-stable bounds projection (mirrors the V8/V9 consumer-contract pattern) | Element campaign (`libs/csharp/Rasm.Element` planning) |
| 6 | `python:data` federation producer + `python:artifacts` SignedArtifact producer — unchanged named blockers on `Query/federation`; the cloud-run stacking clause (`[01]` Flowtide row) adds no new gate | Python campaigns (as recorded in DECISION V1) |
| 7 | Kernel/AppHost frame re-home contingency — unchanged (DECISION `[10]`.5) | Kernel/AppHost campaigns |
