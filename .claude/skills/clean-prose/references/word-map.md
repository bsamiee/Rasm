# [WORD_MAP]

Deletion rows remove the word, replacement rows substitute listed word, noun and phrasal rows substitute listed verb.

## [01]-[DELETIONS]

| [INDEX] | [CATEGORY]       | [WORDS]                                                                                                               |
| :-----: | :--------------- | :-------------------------------------------------------------------------------------------------------------------- |
|  [01]   | Significance     | `crucial`, `crucially`, `vital`, `pivotal`, `paramount`, `key` (adjective), `critical` (outside a real severity)      |
|  [02]   | Intensity        | `dramatically`, `significantly`, `greatly`, `highly`, `extremely`, `incredibly`, `notably`, `importantly`, `arguably` |
|  [03]   | Praise           | `groundbreaking`, `cutting-edge`, `state-of-the-art`, `innovative`                                                    |
|  [04]   | Hype             | `unprecedented`, `transformative`, `revolutionary`                                                                    |
|  [05]   | Decoration       | `tapestry`, `testament`, `synergy`, `interplay`, `intricate`                                                          |
|  [06]   | Embellishment    | `vibrant`, `nuanced`, `multifaceted`, `holistic`, `meticulous`                                                        |
|  [07]   | Ease             | `effortlessly`, `blazingly`, `performant`, `intuitively`, `conveniently`                                              |
|  [08]   | Time markers     | `now`, `currently`, `at this point`, `at this time`, `going forward`                                                  |
|  [09]   | Additive markers | `additionally`, `in addition`, `as well`, `too`, `likewise`, `also`                                                   |
|  [10]   | Sequence markers | `subsequently`, `lastly`, `finally` (outside a numbered step)                                                         |
|  [11]   | Chat phrases     | `please`, `certainly`, `absolutely`, `great`, `oops`, `let us`, `feel free`, `I hope this helps`, `that being said`   |
|  [12]   | Scope padding    | `in terms of`, `with regard to`, `as far as`, `when it comes to`, `from the perspective of`, `in the context of`      |
|  [13]   | Rhetoric         | `not just X but Y`, decorative triplets, `studies show`, `ranging from X to Y` without real limits                    |
|  [14]   | Purpose padding  | `is designed to`, `aims to`, `is intended to` (state what it does)                                                    |
|  [15]   | Stance adverbs   | `genuinely`, `honestly`, `frankly`, `truly`, `admittedly`, `straightforward` (as a hedge)                             |
|  [16]   | Quality praise   | `compact`, `minimal`, `minimalist`, `rich`, `efficient`, `unified`                                                    |
|  [17]   | Quality words    | `friendly`, `precise`, `true`, `normal` (of a thing)                                                                  |
|  [18]   | Connectives      | `so` (as a result), `therefore`, `thus`, `hence`, `which is why`, `consequently`, `furthermore`, `moreover`           |
|  [19]   | Fillers          | `simply`, `just`, `actually`, `essentially`, `basically`, `really`, `very`, `quite`, `rather`, `somewhat`, `easily`   |
|  [20]   | Marketing        | `robust`, `powerful`, `comprehensive`, `seamless`, `elegant`, `clean` (as praise), `modern`, `lightweight`, `best`    |
|  [21]   | Meta phrases     | `note that`, `it is worth noting`, `it is important to`, `in other words`, `as mentioned`, `in summary`               |
|  [22]   | Empty hedges     | `possibly`, `typically`, `generally`, `usually`, `often`, `in some cases`, `in most cases`, `where appropriate`       |
|  [23]   | Enumeration      | `one`, `two`, `three`, `several`, `a number of`, `various`, `multiple` (as a visible count)                           |
|  [24]   | Ordering         | `first`, `second` (as a sentence order)                                                                               |
|  [25]   | Version markers  | `since version`, `as of`, `in version`, `upgrade from`                                                                |
|  [26]   | Version words    | `legacy`, `new and current` (as a version), `migrate` (to a version)                                                  |
|  [27]   | Contrast fillers | `still`, `already`, `rather than` (when the contrasted form is absent from the sentence)                              |

## [02]-[REPLACEMENTS]

| [INDEX] | [WORD]                                            | [USE]                                  |
| :-----: | :------------------------------------------------ | :------------------------------------- |
|  [01]   | `leverage`, `harness` (verb), `employ`, `utilize` | `use`                                  |
|  [02]   | `facilitate`, `foster`, `empower`, `bolster`      | `help`, `support`, `let`               |
|  [03]   | `enhance`, `elevate`, `optimize` (vague)          | `improve`, `increase`, Name the change |
|  [04]   | `streamline`                                      | `simplify`, `shorten`                  |
|  [05]   | `showcase`, `underscore` (verb)                   | `show`                                 |
|  [06]   | `emphasize`                                       | `state`                                |
|  [07]   | `plethora`, `myriad`, `a wealth of`               | `many`                                 |
|  [08]   | `boasts`, `features` (verb)                       | `has`                                  |
|  [09]   | `tackle`, `address` (problem)                     | `handle`, `fix` (when it fixes)        |
|  [10]   | `ascertain`, `determine` (find out)               | `find`, `decide`, `compute`            |
|  [11]   | `commence`, `initiate`, `embark`                  | `start`                                |
|  [12]   | `cease`                                           | `stop`                                 |
|  [13]   | `endeavor`                                        | `try`                                  |
|  [14]   | `delve into`, `dive into`                         | `read`, `examine`                      |
|  [15]   | `functionality`                                   | `feature`, `function`                  |
|  [16]   | `in order to`                                     | `to`                                   |
|  [17]   | `prior to`                                        | `before`                               |
|  [18]   | `due to`, `due to the fact that`, `because`       | `from`, or the condition               |
|  [19]   | `except for`                                      | `except`                               |
|  [20]   | `in the event that`                               | `if`                                   |
|  [21]   | `however`, `nevertheless`, `that said`            | `but`, or delete                       |
|  [22]   | `in spite of`, `despite the fact that`            | `although`                             |
|  [23]   | `need to`, `have to`                              | `must`, or the imperative              |
|  [24]   | `enables you to`, `allows you to`                 | `can`                                  |
|  [25]   | `as needed`, `as necessary`, `as appropriate`     | Name the condition                     |
|  [26]   | `out of the box`                                  | `by default`                           |
|  [27]   | `under the hood`                                  | `internally`                           |
|  [28]   | `gracefully handles`                              | Name the behavior                      |

## [03]-[NOMINALIZATION]

| [INDEX] | [NOUN_FORM]                            | [VERB]           |
| :-----: | :------------------------------------- | :--------------- |
|  [01]   | `perform an analysis of`               | `analyze`        |
|  [02]   | `make a decision`                      | `decide`         |
|  [03]   | `provide support for`                  | `support`        |
|  [04]   | `conduct a review of`                  | `review`         |
|  [05]   | `carry out validation of`              | `validate`       |
|  [06]   | `take into consideration`              | `consider`       |
|  [07]   | `give an indication of`                | `indicate`       |
|  [08]   | `is responsible for building`          | `builds`         |
|  [09]   | `serves as`, `acts as`, `functions as` | `is`, `replaces` |
|  [10]   | `allows for`, `provides the ability`   | `allows`, `can`  |
|  [11]   | `is able to`, `has the ability to`     | `can`            |
|  [12]   | `is used to compute`                   | `computes`       |
|  [13]   | `in charge of building`                | `builds`         |

## [04]-[PHRASAL_VERBS]

| [INDEX] | [PHRASAL]                           | [VERB]                 |
| :-----: | :---------------------------------- | :--------------------- |
|  [01]   | `set up` (outside test fixtures)    | `install`, `configure` |
|  [02]   | `spin up`, `kick off`               | `start`                |
|  [03]   | `tear down` (outside test fixtures) | `delete`, `remove`     |
|  [04]   | `reach out`                         | `contact`              |
|  [05]   | `figure out`                        | `find`, `decide`       |
|  [06]   | `hook up`, `wire up`                | `connect`, `register`  |
|  [07]   | `pull in`                           | `import`, `add`        |
|  [08]   | `put together`                      | `assemble`, `build`    |
|  [09]   | `go down` (value)                   | `decrease`             |
|  [10]   | `come up` (service)                 | `start`                |
|  [11]   | `bail out`                          | `exit`                 |
|  [12]   | `end up`                            | `become`, `result in`  |

Established terms that keep their form:
- `roll back`
- `fall back`
- `back off`
- `opt in`, `opt out`
- `log in`, `sign in`
- `look up`
- `check in`, `check out`
- `shut down`
- `time out`
- `scale out`, `fan out`
- `back up`
- `warm up`
