# [RASM_PERSISTENCE_API_PG_DUCKDB]

`pg_duckdb` supplies the in-PostgreSQL DuckDB analytical bridge — columnar SQL execution inside the server, provisioned per the `Store/provisioning#SERVER_EXTENSIONS` row and distinct from the in-process `DuckDB.NET` lane at `api-duckdb`.
