# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: python-docstring-or-comment
language: python
rule:
    any:
        - kind: comment
        - kind: expression_statement
          has: {kind: string, regex: "^(?i:r|u)?[\"']"}
