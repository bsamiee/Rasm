# [RASM_CS_APPUI_BRIEF]

[UPSTREAM_LAW]: The AppHost, Persistence, and Compute contracts and the landed kernel corpus are settled upstream law AppUi composes — foundations before consumers. This ruling binds NAMED contracts, never sibling line numbers.

The standalone ruling for the restructure-and-harden of `libs/csharp/Rasm.AppUi/.planning/` — the APP-PLATFORM Avalonia product UI engine (on disk: 37 pages, 5 folders — Shell 10 · Render 9 · Charts 2 · Editing 10 · Theme 6; ruled end-state: 8 folders, 43 pages per `[STRUCTURAL_AUTHORITY]`). Upstream law honored throughout: the landed `Rasm.AppHost` corpus (the composition-root exports, seven-port vocabulary, and the `Runtime/lifecycle.md` `FaultBand` registry AppUi composes as settled — never re-mints), the landed `Rasm.Persistence` corpus with the emitted `.archive/RASM-CS-PERSISTENCE-DECISION.md` (the durable spine: the `OpLogEntry`/`SyncOpKind` op-log, the `CrdtOpWire` bit-parity law, the blob lane, the `Version/ledger` changefeed — AppUi projects and decodes, never persists a second CRDT; every Persistence-owned spelling signature-locks against the DECISION), the landed `Rasm.Compute` corpus (the `Runtime/payload.md` `ResidencyPayload` meshopt cluster wire and `ONE_WGPU_DEVICE` — AppUi consumes clusters, never re-partitions), the landed kernel corpus (`libs/csharp/Rasm/.planning/` — one root, nine folders; standing law composed as LANDED FENCES through the csproj kernel project reference: the `Domain/identity.md` `ContentHash.Of` federation hasher every AppUi content-identity mint composes under the kernel ledger's one-hasher law, and the kernel `DrawingProjection` engine Fabrication consumes — AppUi stays on the Fabrication receipt; kernel members verify against the landed fences, never a restored assembly), and the archived federation `FaultBand` shape (`.archive/RASM-COMPONENT-PARADIGM-DECISION.md` — the `[SmartEnum<int>]` band form; AppUi occupies its own neighborhood, never the AEC 23xx-27xx or APP-PLATFORM 4xxx registries). Seven upstream consumer-pressure contracts are settled law, verified in place and never re-applied: the AppHost fault-registry AppUi 6xxx pinned-mirror row; the AppHost `RecomputeGraph` granularity-neutral clause (per-cell composition caller-keyed, AppUi `[V11]` named); the AppHost secrets-lease signing-consumer clause (the `Document/export.md` `IDigitalSigner` material ingress, AppUi named); the AppHost topics/bus live-delta broadcast row (session-ephemeral Loro wire as opaque payload rows, AppUi `[V2]`b named, both sides declared); the Persistence ledger op-log + blob-lane consumer-pressure rows (AppUi edit-intent projection + replay-window read; the snapshot accelerator content-keyed and derivable); the Compute `payload.md` `meshlet-cluster` cluster-LOD clause (the error columns + monotonic-error cut guarantee the AppUi LOD cut requires); the Compute solar-position package-boundary export (the `LightSource.Sun` azimuth/altitude seam, AppUi `[V6]` named). `docs/stacks/csharp/` governs.

## [00]-[SHARED_LAW]

### [DEFECT_LAW]

The fence interiors are settled craft — 18-case `[Union]` control families, seed-DATA rosters everywhere, `Fin`/`Validation`/`IO` rails, single-owner spines consistently consumed (`AppUiTelemetry.Contribute`, `ReceiptSinkPort`, `SurfaceSeam`, `VirtualWindow`, `CommandIntent`, `Viewpoint`, `TokenRow`), zero host-plumbing re-mints. The PERIMETER carries six standing defects; each is a required correction:

1. **The fault-code space is collided, foreign-squatting, and dual-radix — the `[V1]` re-partition is total.** Three confirmed internal integer collisions: `TourFault` 4300-4302 (`Editing/tour.md:30-32`) ≡ `VirtualFault` 4300-4303 (`Shell/virtualization.md:48-51`); `FormFault` 4900-4903 (`Editing/forms.md:29-32`) ≡ `CaptureFault` 4900-4904 (`Render/reality.md:30-34`); `CollabFault` 0x4B00-0x4B05 (`Editing/collab.md:43-48`) ≡ `ContentFault` 0x4B00-0x4B03 (`Editing/media.md:28-31`). Five AppHost-registry squats: `SurfaceFault` 4100 (`Shell/hosts.md:43`) + `AssetFault` 4120 (`Theme/assets.md:29-32`) + `DialogFault` 4130 (`Shell/dialogs.md:39`) + `InputDriverFault` 4150 (`Shell/input.md:289-292`) all inside AppHost `ConfigError` 4100-4199; `ViewportFault` 4500-4504 (`Render/pipeline.md:32-36`) ≡ AppHost `HopFault` 4500; `DraftFault` 4600-4603 (`Render/drafting.md:271-274`) ≡ AppHost `CommandFault` 4600; `EditFault` claims the WHOLE 4700-4799 decade (`Editing/inspector.md:138,152-174`) — swallowing TEN AppHost bands (`FeatureFault`/`SolverFault`/`LiveWire` 4720/`BusFault`/`OutboxFault`/`LaneFault`/`ReplayFault`/`OrchestrationFault`/`SecretFault`/`PemFault`); `NotebookFault` 4800-4803 (`Editing/notebook.md:96-99`) ≡ AppHost `FederationFault` 4800; `IssueFault` 5000-5003 (`Editing/issues.md:29-32`) inside Persistence 5xxx. Plus a decimal/hex FORK (`HistoryFault` 0x4A00, `CollabFault` 0x4B00, `ShaderFault` 0x4C00, `ImmersiveFault` 0x4D00 — 0x4A00 = 18944, not decimal-adjacent to 4900). Every code is a `base(detail, NNNN)` literal — the exact form the federation GENERATOR_LAW bans — and NO registry exists. Every AppUi fault also crosses the shared `ReceiptEnvelope`/`EvidenceTimeline`, so disjointness is load-bearing telemetry identity, not hygiene.
2. **The durable collaboration spine is the exact form the Persistence law forecloses — the `[V2]` recharter replaces it.** `Editing/collab.md:105` designs `SyncRail` durability as "the local delta projects onto the Persistence `OpLogEntry` ... and Persistence replays the opaque delta blob without re-modeling the CRDT"; `:117-120` content-addresses the raw Loro `Export(Snapshot)` bytes as THE durable document state. That mints a SECOND durable CRDT representation — the "Rust-binding CRDT whose wire is its own" the Persistence roster law records as rejected and re-seals ("LoroCs is rejected for this package (wire bit-parity law)"). `Editing/history.md` proves the correct discipline on the SAME seam (typed `RevertibleOp` deltas -> `SyncOpKind`); collab, notebook (`notebook.md:178`), and issues (`issues.md:123`) all ride the rejected form.
3. **The merge authority's corpus contradiction stands — `[V3]`/`[V4]` reconcile it.** The registration half is DISCHARGED on disk: `collab.md` sits in the codemap (`ARCH:39`), the seam ledger (`ARCH:71-72`), and the README router (`README:31`, 37 rows) — only the `[V3]` folder move remains. The contradiction is live: `Editing/issues.md:118-146` ships a full bespoke HLC last-writer-wins CRDT (`CommentOp` `[Union]` + `CommentThread` `Apply`/`Merge`/`Log`) that `collab.md:20` explicitly DROPS root-up ("the `CommentThread`/`CommentOp` register are DROPPED root-up"; the map-container charter replaces it) — `issues.md:123` still cites the dead `notebook.md#CRDT_COEDIT` law. The collab refactor reconciled notebook and never issues.
4. **Settled Render fences are decorative — `[V5]`/`[V6]` make them honest.** `Render/meshlets.md:71-81` `Partition` assigns EVERY meshlet the whole-mesh `Bounds`, a never-cull `NormalCone(0,0,1,-1)`, and `ScreenSpaceError = mesh.Bounds.Radius` — cull is non-functional while `:14` claims per-meshlet bounds/cones and an edge-collapse LOD tree, and while `pipeline.md:463-470,542-556` consumes Compute's meshopt-built clusters whose wire carries REAL cone apex/axis/cutoff. `Render/pathtrace.md:33-36` `Bvh.Split` never recurses (the root claims children never emitted) and `:64-67` `Accumulate` traces nothing under a "correctness oracle" claim (`:3`). `Shell/virtualization.md:127-131` `Reaccumulate` is an O(n) prefix rebuild under an O(log n) Fenwick claim (`:78`). `Shell/controls.md:181-184` `Rebind` type-checks and resets nothing (the recycling stale-state core). `Render/reality.md:46-49` re-hashes splat components through a local `XxHash128` against `pipeline.md:404,496`'s single-mint law, and its `SplatSort` enum (`:54-58`) is never read.
5. **Three admitted packages have zero page consumers while their concerns are hand-rolled or unwired — each is an integration MANDATE (`[V8]`), never a REMOVE.** `PDFsharp`+`PDFsharp-MigraDoc`: zero consumers while `Render/capture.md:274-340` hand-rolls the `FlowBlock`/`FlowFold` pagination engine MigraDoc's flow DOM owns and `:478` emits PDF via `SKDocument` with none of PDFsharp's Security/Signatures/AcroForms/UA surface — `api-pdfsharp.md:155,161` names consumers that do not consume. `Mapsui.Avalonia12`: zero consumers while `ARCH:69` declares the Bim NTS basemap seam no page realizes. `NodeEditorAvalonia`: zero consumers while README promises the parametric/dependency-graph canvas no page owns.
6. **The seam ledger is stale in both directions — `[V9]` makes it machine-checkably true.** Phantom source nodes: `Render/glb` (`ARCH:65`) and `Render/query` (`ARCH:68`) name pages that do not exist. Two rows target `Rasm.Persistence/Sync` (`ARCH:72` collab, `ARCH:82` history, plus the `:103` prose) — a folder the Persistence campaign deleted (owner: `Version/ledger`); the dead `NotebookOp` row is already gone. Declared-unwired: `ARCH:74` kernel `DrawingProjection` direct row (the kernel `Drawing/view.md` now states receipt-only AppUi routing — the row retires), `ARCH:75` `ChartAtlas`, `ARCH:69` basemap. Wired-undeclared: collab -> AppHost transport (the ledger/blob rows are declared at `ARCH:71-72`; the transport row is not) and the entire AppHost port spine (`TelemetryContributorPort`/`ReceiptSinkPort`/`UiSchedulerPort`/`ClockPolicy`/`DataClassification` — composed on ~30 pages, ledgered nowhere beyond the one notebook row).

Preserve everything a verdict does not name; harden ONLY per the verdict that names it: `Shell/hosts.md` (the host-neutral mount exemplar; mandate item 3's composition law disposed), `commands.md` (the one command rail), `screens.md`/`controls.md`/`solver.md`/`virtualization.md`/`input.md`/`navigation.md`/`dialogs.md`/`accessibility.md`, `Render/pipeline.md` (pass-DAG + backend factory + resolve ladder + BCF `Viewpoint` codec + the ONE `ResidencyManifest.Mint`), `capture.md` (float-end-to-end color management), `drafting.md` (painter sort dropped root-up, `HiddenLineSeam` composing Fabrication's `#PROJECTION_HIDDEN_LINE`, zero painter-sort remnant corpus-wide: mandate item 2 DISPOSED), `evidence.md` content (mis-filed, not weak), `Editing/inspector.md` (minus the diff), `tables.md`/`livedata.md`/`forms.md`/`history.md` (history is the correct-durable-discipline exemplar), `notebook.md`, `Charts/dashboards.md`/`custom.md` (the `DimensionIndex`/`CrossFilter` bitmap engine and 14-case layout-fold algebra are award-grade), `Theme/tokens.md`/`typography.md` (the Unicolour OKLab delegate law and one-shaping-rail are exemplary), `motion.md`/`assets.md`/`locale.md`, `animation.md` content (mis-placed), and a 54-catalog `.api` tier that is dense, TFM-honest, and verified-real, with defects that are RELATIONAL, not intrinsic. Rosters are seed DATA corpus-wide — zero enumerated-instance defects stand on the generator axis. The rot is the PERIMETER — bands, the durable seam, folder placement, ledger truth, three unconsumed admissions — and no per-page cold pass fixes the perimeter. Perimeter law first.

### [TELOS]

`Rasm.AppUi` is the federation's product surface: the host-neutral Avalonia shell (mount axis, one command rail, one control vocabulary, constraint layout, one virtualization fabric, full a11y), the GPU viewport engine (pass-DAG over one wgpu device, cluster-LOD residency, path-traced appearance from Materials BSDF, reality capture, OpenXR), the 2D documentation surface (sheets, GD&T, hidden-line consumption, DWG/DXF/PDF emit), the chart/dashboard plane (LiveCharts rail + bespoke Skia algebra + cross-filter engine + geo basemap), the typed-edit plane (inspector, tables, forms, revert algebra, reactive data spine, node-graph canvas), the document plane (reproducible notebook, media, paginated Office/PDF export), the live-collaboration plane (Loro live merge + presence + time travel over a Persistence-owned durable spine), the diagnostics plane (one evidence union, correlation, headless proof, dev loop, quality governor), and the theme vocabulary every literal traces to — one body. Every owner grades against the 5x consumer bar: every screen windows through the one `VirtualWindow`, every fault crosses the shared envelope under one registry, every collaborative surface converges through one merge authority whose durable truth replays in ANY runtime, every visual export is color-managed and print-honest, every viewport tile streams from Compute's content-keyed payloads. World-class additionally means the fences are HONEST: a cull that never culls, a BVH that never recurses, a Fenwick claim over a linear scan, and a durable claim over a process-local blob are each fiction — the fence is the product, and a fence that lies about being settled is a defect, not a deferral. Zero current consumers never lowers the bar.

### [STRUCTURAL_AUTHORITY]

The post-campaign map is SEALED — 8 folders, 43 pages, zero one-file folders, every folder a growth axis:

- `Shell/` 10 — unchanged (navigation, screens, hosts, commands, controls, solver, virtualization, dialogs, input, accessibility).
- `Render/` 9 — the pure GPU-viewport + temporal tier: pipeline, meshlets, pathtrace, shading, immersive, reality, capture (post-`[V3]` extraction: raster capsule + codec/color only), drafting, animation (IN from Theme — the timeline/playhead/walkthrough runtime engine; Theme keeps only vocabulary).
- `Charts/` 3 — dashboards, custom, + NEW `basemap.md` (`[V8]`).
- `Editing/` 6 — the typed-edit tier: inspector, tables, forms, history, livedata, + NEW `graph.md` (`[V8]`).
- `Document/` 3 (NEW folder) — notebook (in from Editing), media (in from Editing), + NEW `export.md` (`[V3]`/`[V8]` — the Office/PDF flow-export owner extracted from capture).
- `Collab/` 3 (NEW folder) — `sync.md` (in from `Editing/collab.md`, re-chartered per `[V2]`), issues (in from Editing), tour (in from Editing).
- `Diagnostics/` 4 (NEW folder) — `evidence.md` (receipt union + correlation join), `proof.md` (capture lanes + headless derivation + `ProofLaw`), `devloop.md`, `governor.md` — the `Render/evidence.md` 456-LOC six-concern split (`[V3]`).
- `Theme/` 5 — tokens, typography, motion, assets, locale.

A move is a rebuild opportunity, never a bare `git mv`: the moving page re-fences against its new folder charter. The two cross-folder absorbs reduce to structural end-states, both absorber-owns-first: `Document/export.md` OWNS capture's document-export fold — the donor sections transfer verbatim into the absorber's skeleton at the structure motion, condemned-but-intact THERE, so `capture.md` rebuilds genuinely post-extraction and the absorber rebuilds the transferred content ground-up; `Render/animation.md` OWNS the walkthrough fold (`animation.md:194-238` `WalkthroughSpec`/`Walkthrough.Render`) — nothing transfers, the absorber hardens its own fold and `Collab/tour.md` deletes its clones and re-fences as projection. Every structural change updates `ARCHITECTURE.md` `[01]`-codemap/`[02]`-seams and the `README.md` router and package groups in the same motion. No page survives on incumbency, and no page falls to it either: a sound fence is rebuilt only where a verdict names its defect.

### [PLACEMENT_LAW]

The strata law is absolute (`libs/.planning/architecture.md`): AppUi is APP-PLATFORM, depending UP on `Rasm` (kernel) only — the strata law fixes AppUi's reference set as kernel+AppHost+Compute+Persistence, and the `Rasm.Element` seam, though legal app-platform substrate, has ZERO AppUi page consumers on disk: an `ElementGraph` crossing admits it wire-shaped when a page names one, never speculatively — PORT-peering with `Rasm.AppHost`/`Rasm.Persistence`/`Rasm.Compute`, consuming AEC-domain contracts (`Rasm.Bim` BCF/geospatial/planning, `Rasm.Materials` appearance, `Rasm.Fabrication` posting receipts) as boundary wire only, and it is NOT the app composition root — host binding (Rhino/GH2), exporter pins, and process attachment belong to the APP stratum. Consequences sealed here: (a) AppHost exports are settled vocabulary — `TelemetryContributorPort`, `ReceiptSinkPort`, `UiSchedulerPort`, `ClockPolicy`, `CorrelationId`, `TenantContext`, `DeterminismContext`/`RecomputeGraph`, `DataClassification`, `FaultBand` — composed, never re-minted, with every crossing a declared `[V9]` ledger row; (b) Persistence results are decode-only (the AppHost decode-only law binds AppUi symmetrically): the op-log rows, replay windows, blob receipts, and conflict receipts are Persistence-owned types — no AppUi interface or type crosses down; (c) the in-Rhino ALC firebreak stays law — `loro.dylib` and every companion-only native never enters a plugin ALC (`collab.md:20`); (d) `ONE_WGPU_DEVICE` is shared with Compute at the declared seam (`ARCH:81`), never a second device; (e) the kernel is the one below-port sibling composed as DIRECT compile surface (the csproj kernel project reference), bounded by the RhinoCommon compile firebreak — AppUi is deliberately NOT RhinoCommon-aware (`Directory.Build.props` scopes host-reference inheritance via `IsRhinoCommonAwareProject` to the kernel and the Rhino/GH roots), so AppUi composes exactly the kernel members whose signatures are Rhino-type-free (`Rasm.Domain` `ContentHash.Of(ReadOnlySpan<byte>) -> UInt128` is the canonical composable entry) while a Rhino-typed kernel member (`Spatial.Apply(SpatialOp.Build(...))` over `BoundingBox[]`, `VectorIntent.Pose` over `Plane`) is boundary-unreachable and its concern lands as an AppUi-local measured kernel or arrives as wire, never by widening AppUi's host-reference surface.

### [GENERATOR_LAW]

The realized corpus is clean on this axis — `ControlIntent`(18), `ChartSeriesSpec`(15), `CustomVisual`(14), `EditorFactory`(11), `TypographyRole`(10), `SheetSize`, `GpuBackend`, `TokenRow`, `LocaleRow` are all `[Union]`/`[SmartEnum]` + FrozenDictionary seed DATA — so the law binds the NEW planes: fault bands are rows in ONE type-enforced registry where a duplicate integer fails at type initialization (`[V1]`), never per-page literals; collaborative containers are `CollabContainer` rows, never new CRDTs; residency tiles are Compute payload rows, never local re-clustering (`[V5]`); export destinations are rows on one export owner (`[V8]`); basemap layers are rows over one `MapControl` surface; gamut/transfer is ONE color-policy row family (`[V10]`); track interpolation is a per-`Track`-case policy row — the Camera row the ONE pose-interpolation site AppUi-wide, the Color row the tokens OKLab mix (`[V6]`). The test: the next fault, container, tile kind, export target, chart series, device driver, or interpolation law is one data row, never a new arm, a loose integer, or a second engine.

### [SEAM_AND_RAIL_LAW]

- ONE entry per owner discriminating on a closed `[Union]`/`[SmartEnum]` — the corpus norm (`CommandDeck.Freeze`, `ControlFactory`, `SurfaceHost` mount, `VirtualWindow`, `Assess`-shaped folds), sealed for every new owner.
- Every fault is a typed `[Union] : Expected` case whose `Code` derives through the `[V1]` registry row; bare `Error.New` on a rail is the deleted form — the live breaches are `navigation.md:46,81`, `locale.md:172-173`, `dashboards.md:296,304`, `custom.md:168-231`, `animation.md:62`.
- Durable collaboration is decode/replay at the boundary: the edit-intent op stream is Persistence-owned rows (`[V2]`); a Loro-native byte persisted as system-of-record is the deleted form; the snapshot blob survives only as a derivable content-keyed accelerator.
- Content identity is single-mint and ONE-HASHER: residency and splat tiles key by the Compute payload's own `ContentKey` — decode-only, a local `XxHash128` over raw component floats (`reality.md:46-49`) is the deleted form (`pipeline.md:404,496` is the law) — and every AppUi-SIDE content-identity mint composes the kernel `Rasm.Domain` `ContentHash.Of` seed-zero entry (`Rasm/.planning/Domain/identity.md`; the kernel ledger's federation one-hasher law): the capture runtime `ContentHash` delegate binding (`capture.md:84,162`) and with it the walkthrough per-frame proof, the command payload digest (`commands.md:197`), the notebook replay-input hashes (`notebook.md:243`), and the `[V2]` snapshot-accelerator key — a page-local `XxHash128.Hash` call site is the deleted form, hex/wire encodings stay boundary projections at the consuming seam, and the TS `RenderReceiptWire` frame-hash proof (`ARCH:60`) thereby reproduces the one federation seed exactly as the kernel ledger's typescript/python seed rows demand.
- Consumer contracts are RECORDED where the owner is upstream, never re-homed — each a settled contract naming AppUi: meshlet clusters and their LOD/error columns are Compute `payload.md` rows; the edit-intent op-log ingest and replay-window read are Persistence `Version/ledger` rows; the AppUi 6xxx band neighborhood is an AppHost registry mirror row; the signing-credential lease is an AppHost `Runtime/secrets.md` export row; `HiddenLineResult` stays the Fabrication Posting receipt, insulated from the kernel engine supersession beneath it.
- Cross-package member claims verify or die: `uv run python -m tools.assay api` over restored assemblies is truth for every composed external-package member (LoroCs `SubscribeLocalUpdate`/`ExportMode`/`EphemeralStore`/`EventTriggerKind`, LiveCharts `Xaml*Series`, MigraDoc `PdfDocumentRenderer`/`FormattedDocument`, PDFsharp `DigitalSignatureHandler`/`IDigitalSigner`, Mapsui `MapControl`/`GeometryFeature`/`SphericalMercator`, NodeEditor `IDrawingNode`/`DrawingNodeEditor`, ACadSharp `DwgWriter`/`DxfWriter` entity set, ProDiagnostics `DevToolsOptions`/`PropertyValueEditorService`); KERNEL members verify against the LANDED KERNEL FENCES — the planning-scoped kernel restores no assembly, so the landed `libs/csharp/Rasm/.planning/` fences are the kernel verification spine (`ContentHash.Of` at `Domain/identity.md`; the `Spatial/index.md` `Spatial.Apply`/`SpatialOp.Build` and `Processing/intent.md` `VectorIntent.Pose` surfaces verify as Rhino-typed and therefore firebreak-unreachable per `[PLACEMENT_LAW]`(e)); a member you cannot verify is a phantom.
- Prose-fence parity is law: a Boundary paragraph describing behavior the fence does not encode (the 40-60-line run-ons at `dashboards.md:20`, `custom.md:19`, `collab.md:20`, `capture.md:165`, `evidence.md:278`) either lands in the fence or converts to an explicit research gate.

### [ROSTER_RECONCILIATION]

Central ownership is `Directory.Packages.props` (hand-edited, the label-grouped `[App UI]` block) + `Rasm.AppUi.csproj` (label-grouped); per-package catalogs under `libs/csharp/Rasm.AppUi/.api/`, shared substrate under `libs/csharp/.api/`. The roster is COMPLETE; the settled end-state:

- `ACadSharp` is write-scoped consumption of the shared central pin (the props `[BIM]` group — Bim is the READ authority): the csproj direct reference owns BOTH write rows — DXF via `DxfWriter`, DWG via `DwgWriter` — over ONE `CadDocument` typed entity fold.
- `netDxf` is REMOVED — settled on feed-verified upstream abandonment (haplokuon/netDxf archived, a `net6.0` asset under the net10 consumer): the props row, the csproj row, and the `api-drafting-export.md` netDxf surface are gone; the catalog is write-scoped to ONE writer family (`DwgWriter`/`DxfWriter`/`SvgWriter` + typed entities over one `CadDocument`) with its READ rows the Bim catalog's recorded adoption. The DXF round-trip fidelity probe (dimensions/hatches/leaders/blocks against the drafting entity set) is a standing `[V7]` obligation — a failed entity class re-pins netDxf as a recorded exception, never silently — so capability never deletes.
- `ProDiagnostics` is ADMITTED — MIT, feed-verified current on the Avalonia-12 family, wieslawsoltes (the roster's proven NodeEditor/Dock/PanAndZoom/Svg.Skia author), shipping the ORIGINAL `Avalonia.Diagnostics` assembly and namespace: the dev-loop visual-tree/property/event/layout inspector `Diagnostics/devloop.md` charters, Debug-gated `PrivateAssets="all"` in the csproj `Dev Loop` group beside HotAvalonia; both first-party alternates fail the gate (`Avalonia.Diagnostics` feed-dead at 11.3.x with no Avalonia-12 asset; the Accelerate DevTools pay-tiered, license-gate REJECTED). `api-prodiagnostics.md` is authored at full member depth.
- `PDFsharp`+`PDFsharp-MigraDoc` INTEGRATE into `Document/export.md` (`[V8]` — the bespoke `FlowBlock`/`FlowFold` pagination dies for the MigraDoc flow DOM; PDF gains Security/AES-256/Signatures/AcroForms/tagged-UA; `SKDocument.CreatePdf` narrows to the pure-visual vector-print arm); `Mapsui.Avalonia12` INTEGRATES into `Charts/basemap.md` (`[V8]` — wires the declared `ARCH:69` basemap seam); `NodeEditorAvalonia` INTEGRATES into `Editing/graph.md` (`[V8]`). Absence of consumers is never a REMOVE reason — each admission binds its named page.
- Catalog hygiene per `[V12]` is landed law; the standing obligations ride the pages that mint (`[V12]`).
- Three ruled admissions close the capability gaps, each integration-first with its composing page named: `FFmpeg.AutoGen` (MIT, FFmpeg 8.x) is the in-process video mux/encode owner — encode rows land on `capture.md` (the codec/color owner) and animation's `[04]-[WALKTHROUGH]` flythrough composes them past its frame-sequence terminal; `lcmsNET` (MIT, the LittleCMS engine — proofing transforms, K-preservation intents, device-link; the native lcms2 ships with the app) is the ICC/device-CMYK print-fidelity owner `Document/export.md`'s print arm composes; `Whisper.net` (MIT, whisper.cpp — streaming `ProcessAsync`, Silero VAD, the built-in translate-to-English task, offline) realizes `LiveCaption` on `locale.md` — transcription plus translate-to-English on one owner, with broad-target MT staying a growth row on a named consumer. Props/csproj rows and member-true `.api` catalogs land with the admissions.
- Recorded REJECT, never re-proposed: a `meshoptimizer` AppUi admission (Compute owns clustering — `[V5]` consumes its payload rows); a second color-science kernel beside Unicolour (`[V10]` — `lcmsNET` owns device-CMYK/ICC transforms at the print boundary, Unicolour stays the suite color-model kernel, disjoint charters); LoroCs REMOVAL (integration-first: four live consumers; `[V2]` re-charters its durable half only); a `TreeDataGrid` second grid engine (the `tables.md:120` seal upheld — hierarchy renders as `TreeRow` rows on the ONE virtualized `DataGrid` rail).
- License gate: OSS and free-for-OSS-commercial admissible; pay-tiered/seat-licensed/proprietary-gated REJECTED.

## [01]-[CAMPAIGN_VERDICTS]

Every verdict below lands. Recommended shapes follow each; exceed them, never dilute them.

### [V1_FAULT_BAND_REGISTRY]

Mint the AppUi band registry mirroring the federation `FaultBand` (the archived `[SmartEnum<int>]` band form, the AppHost registry precedent) — a `[SmartEnum<int>]` whose generated key lookup makes a duplicate band fail at type initialization; every fault union's `Code` derives through the registry row, never a `base(detail, NNNN)` literal. Ruled siting: a `[FAULT_TABLES]` owner block on `Diagnostics/evidence.md` — the receipt-union spine every page already folds through (`AppUiTelemetry.Contribute`). The re-partition is TOTAL — no AppUi union keeps its integer, because the whole occupied space is foreign (AppHost 41xx-48xx, Persistence 5xxx) or internally collided, and the hex block dies with it. Ruled neighborhood: **6000-6999**, folder-strided, single-radix decimal, one decade per union: Shell 60xx (`SurfaceFault` 6000, `ControlFault` 6010, `LayoutFault` 6020, `VirtualFault` 6030, `DialogFault` 6040, `InputDriverFault` 6050, NEW `NavFault` 6060 — the navigation bare-`Error.New` rail lands typed); Render 61xx (`ViewportFault` 6100 — pipeline+pathtrace shared, `ShaderFault` 6110, `ImmersiveFault` 6120, `CaptureFault` 6130 (reality.md's union — the reality-capture rail; `capture.md` owns no fault union), `DraftFault` 6140, NEW `AnimationFault` 6150 — `animation.md:62` lands typed); Charts 62xx (NEW `ChartFault` 6200 — the dashboards/custom bare rails land typed); Editing 63xx (`EditFault` 6300 — ONE decade, the 4700-4799 whole-decade claim dies, `Aggregate` carries child codes in the payload rather than a reserved 6399; `FormFault` 6310, `HistoryFault` 6320, NEW `CanvasFault` 6330 for `graph.md`); Document 64xx (`NotebookFault` 6400, `ContentFault` 6410, NEW `ExportFault` 6420); Collab 65xx (`CollabFault` 6500, `IssueFault` 6510, `TourFault` 6520); Theme 66xx (`AssetFault` 6600, NEW `LocaleFault` 6610); Diagnostics 67xx (NEW `ProofFault` 6700 as the proof/devloop/governor rails demand typed cases). The registry carries PINNED MIRROR rows for every foreign neighborhood (AppHost 1xxx/4100-4810, Compute 2200-2220 + Remote 4520-4532, Persistence 5xxx/771x/82xx-83xx, the AEC 23xx-27xx registry) exactly as the AppHost registry pins its foreigners — and the AppHost registry carries the reciprocal AppUi 6xxx pin, a settled contract. Every page's band prose becomes a registry reference; each union's re-derivation lands with its owning page's rebuild; the registry landing makes the three internal collisions and eleven foreign squats unrepresentable.

### [V2_COLLAB_DURABLE_RECHARTER]

Mandate item 1 disposed honestly. LoroCs KEEPS as the LIVE-session merge authority — `CollabDoc` over one `LoroDoc`, the 6-kind `CollabContainer` axis, `Presence`/`EphemeralStore`, `TimeTravel`/`UndoManager`, live peer broadcast via `SubscribeLocalUpdate` delta -> `Import` — four live consumers make removal a capability deletion, and the live half violates no law. The DURABLE seam re-charters root-up in `Collab/sync.md`: (a) the durable stream is the TYPED EDIT-INTENT op stream — AppUi's own domain ops (cell insert/edit/move, comment add/edit/resolve, table row commit, annotation ops), the `history.md` `RevertibleOp` -> `SyncOpKind` discipline generalized — projected onto Persistence-owned `OpLogEntry`/`SyncOpKind` rows through the `Version/ledger` changefeed (the ledger's consumer-pressure row names AppUi; Persistence owns the row types, AppUi projects and decodes) — and the intent vocabulary has ONE owner: `Collab/sync.md` mints the single edit-intent `[Union]` whose rows the domain planes contribute per `[GENERATOR_LAW]`, `history.md`'s `RevertibleOp` staying the local revert algebra that projects onto the same family (`ARCH:80`'s target re-points onto this rail), never parallel per-page op unions; (b) cold-load = the per-document replay window decoded from the ledger and replayed into a FRESH `LoroDoc` in log order — deterministic hydration, no Loro byte read from durable truth — under the SESSION-EPOCH law that makes it honest: a rehydrated `LoroDoc`'s version vector is unrelated to any live session's, so a live peer's `Export(Updates(vv))` delta cannot import over it (`ImportUpdatesThatDependsOnOutdatedVersion`/`DecodeVersionVectorException`, `api-loro.md:158-159`, are the verified failure surface); replay-window rehydration is therefore the cold-START path that SEEDS a session epoch, and a peer joining an ACTIVE session syncs Loro-native session state from a live peer over the AppHost transport (in-session wire, ephemeral, never persisted — the bit-parity law binds durable truth only), never by replaying the log beside a live epoch; (c) the Loro `Export(Snapshot)` blob survives ONLY as a content-keyed cold-start ACCELERATOR in the Persistence blob lane — its content key composing the kernel `ContentHash.Of` one-hasher entry the blob lane's own content addressing already rides (the page-local `XxHash128.Hash` at `collab.md:119` dies with the durable claim) — derivable, deletable, verified reconstructible from the op-log alone, never system-of-record; `collab.md:105`'s opaque-replay clause and `CollabSnapshot`-as-durable (`:117-120`) are the deleted form, and `api-loro.md` already states the re-pointed charter; (d) `notebook.md:171-210` and `issues.md` re-anchor their durable prose onto this charter. The one hard container is DECIDED: character-granular text co-editing loses convergence information at intent granularity under concurrent intra-cell edits, so the text container's durable rows are character-run ops riding the existing `CrdtField.RgaSequence` wire — the Persistence `Version/commits.md` stable-position sequence case, Persistence-owned wire, stable position identifiers, never Loro binary, zero new Persistence row. Named gate: the text arm fences only after a two-replica concurrent-intra-cell-edit convergence probe passes on that wire.

### [V3_FOLDER_REPARTITION]

The `[STRUCTURAL_AUTHORITY]` map executes as a verdict: (a) `Diagnostics/` mints from the `Render/evidence.md` six-concern god-page (sections `[02]`-`[07]`, 456 LOC — receipt union, correlation join, capture lanes, headless derivation, dev loop, perf governor; none a viewport projection) as four real owners — `evidence.md` (union + `EvidenceJoin`/`SkewBand` + the `[V1]` `[FAULT_TABLES]` block), `proof.md` (capture lanes + `ProofEngine`/`ProofLaw` headless matrix), `devloop.md` (hot-reload gating + HUD + flamegraph + solve-scrub + replay-verify + REPL + the ProDiagnostics inspector), `governor.md` (`PerfBudget` quality governor + `GpuTimeline`); the donor page's `[08]`-`[TS_PROJECTION]` `EvidenceFeed` rides the new `evidence.md` beside the union it projects (the `ARCH:61` source re-anchors) and its `[09]` research items redistribute to their owning new pages; (b) `Collab/` mints from Editing's coordination cluster — `sync.md` (ex-collab, `[V2]`), `issues.md`, `tour.md` — the codemap/router registration is already landed, so the motion re-anchors the registered rows to their Collab/ paths; tour's Collab placement is law-grounded, not assumed: `TourSource.TopicTour` folds the issues-owned BCF topic set into stops (`tour.md:3,10`) and the named growth arm is presenter-follow live review riding sync's `Presence`/`EphemeralStore` — the page composes zero collab machinery today, so the moving rebuild states the arm as its charter; (c) `Document/` mints — `notebook.md`, `media.md`, and NEW `export.md` extracting capture's `[06]`-`[DOCUMENT_EXPORT]` + `OfficeExport` OOXML part-graph writers (`capture.md:274-340,367-462,478`) and realizing the `[V8]` PDFsharp/MigraDoc mandate; `capture.md` remains the pure raster capsule + `VisualCodec` color owner; (d) `Theme/animation.md` MOVES to `Render/animation.md` — the timeline/playhead/walkthrough runtime engine is temporal viewport machinery (pipeline `SimVisual` playback, reality `CaptureClip`, capture clip render), not vocabulary; Theme stays the pure vocabulary tier; (e) `Editing/tour.md`'s duplicated sampler collapses onto animation: the page structurally IS one camera `Track` played through a duplicated engine — `:106` re-mints `Bracket`, `:112` `Walk`, and the `WalkthroughTour` class (`:228`) re-mints `Render`, with the `lerpCam` tail at `:73,:93` — the tour becomes a projection (stops -> camera `Track` keyframes; `WalkthroughTour` -> `animation.Walkthrough`), deleting both clones. Charts stays (the 2-page thinness resolves by `basemap.md`, not by fold).

### [V4_ISSUES_CRDT_COLLAPSE]

`Editing/issues.md` (-> `Collab/issues.md`) rebuilds its `[03]-[COMMENT_THREAD]`: the bespoke `CommentOp` `[Union]` + `CommentThread` `Apply`/`Merge`/`Log` + `HlcStamp` register (`issues.md:118-146`) DIES — `collab.md:20` dropped it root-up and `collab.md:17` already charters the replacement ("an issue comment thread is a `map` container keyed by comment GUID"). The comment thread becomes a `CollabDoc` `map` container attach; `BcfComment` projects from the map at the Bim boundary (the lossless `ToTopic` round-trip and carried-source-row preservation at `issues.md:54-95` survive untouched — they are the page's excellence); the stale `notebook.md#CRDT_COEDIT` citation (`issues.md:123`) re-points to `Collab/sync.md`; the durable half rides `[V2]`'s edit-intent stream. The board stays a pure PROJECTION over the Bim-owned BCF contract — `IssueBoard`, `ViewpointCodec.FromBcf`, and the `IssueStatus` lifecycle are correct as-is.

### [V5_RESIDENCY_CONSUMPTION]

The meshlet/splat plane consumes Compute, never re-builds. (a) `Render/meshlets.md` drops `MeshletCluster.Build`/`Partition` (`meshlets.md:57-81` — degenerate whole-mesh bounds, never-cull cones, no LOD) and consumes `ResidencyPayload.Clusters` — the meshopt-built, cone-carrying `ResidencyMeshlet` descriptors the Compute `payload.md` `meshlet-cluster` row mints and `pipeline.md:542-556` already projects to the TS wire; the page KEEPS its genuinely strong half — `ResidencyBudget` LRU/velocity-prefetch (`:107-125`), `BindlessTable`, `InstanceBuffer`, `ClusterCull` — now culling on REAL per-cluster wire cones. The cluster-LOD SELECTION ALGEBRA is AppUi's own and lands NOW: the per-cluster error bound (the encoded error columns of the Compute `meshlet-cluster` row — a settled contract naming AppUi the demanding consumer) projects to screen space under the camera row, a `LodPolicy` threshold picks the cut, a hysteresis band on the same policy row kills LOD flicker across dolly moves; cut well-formedness (crack-free, no double-draw) rides the LANDED Compute encode guarantee — the error columns are monotonic BY CONSTRUCTION (`ParentError >= Error`) on the `payload.md` row that names AppUi the demanding consumer. The cull ladder is RAISED past cone parity to the page's own infinite-viewport charter (`meshlets.md:3`): frustum -> wire-cone backface -> LOD cut -> prior-frame depth-pyramid (HZB) two-phase occlusion, each a pass-DAG row over the shared device — the pyramid build is one compute pass (`ComputePassEncoder`/`BeginComputePass`/`DispatchWorkgroups`, `api-silk-webgpu.md:52,118-120`; `QueryType.Occlusion`, `:80,142`, the fallback probe row) — frustum+cone alone is the parity form, not the bar. (b) `Render/reality.md` reconciles its divergent carriers: `SplatPayload`/`PointPayload` (`reality.md:52,112`) collapse onto the one Compute `ResidencyPayload` boundary record `pipeline.md` already consumes — one AppUi-side projection of one Compute concept; `SplatEllipsoid.ContentKey` (`:46-49`) DIES — tiles key by the payload's own `ContentKey` per the single-mint law, doubly foreclosed by the kernel one-hasher law (no AppUi-side content-key fold exists beside `ContentHash.Of`); `SplatSort` (`:54-58`) is consumed by a real radix sort or the enum dies with the `OrderByDescending` stopgap stated honestly; the `MemoryMarshal.Cast` layout assumption stays gated on `CAPTURE_PAYLOAD`. The splat scan-buffer WIRE is minted upstream (`Rasm.Compute/Runtime/wire.md:114` `GaussianSplatScan`, the `[SPLAT_SCAN_WIRE]` row) — `reality.md` consumes already-decoded scans over that wire; only the in-process SPZ/SOG decoder stays companion-routed per the Compute ruling.

### [V6_FENCE_REALISM_SET]

The confirmed decorative-fence set lands as repairs with the owning page's rebuild, never patches beside it — each either implements to the prose or downgrades the prose to an explicit gate: `pathtrace.md:33-36` `Bvh.Split` becomes a real recursive SAH build (children emitted, leaf criterion), `:38-40` `Enclose` a real bounding sphere, the `:30-31` leaf-only `Refit` a real bottom-up re-bound whose refit-vs-rebuild policy adopts the kernel `[DEGRADATION_REFIT]` shape (topology-stable in-place re-bounding + a deterministic SahCost rebuild trigger, `Rasm/.planning/Spatial/index.md` — the law the page's `:17` refit prose claims), the oracle BVH staying a PAGE-LOCAL measured kernel over wire-decoded meshlet bounds and staying PRIVATE — the kernel spatial engine is the federation broad-phase owner behind the `[PLACEMENT_LAW]`(e) firebreak and its cross-package acceleration crossing stays wire-shaped (`SpatialAnswer.Wire` `NodeLinkProjection` — the retired `ToAcceleration` member's successor on kernel disk), so an AppUi acceleration wire or a second exported BVH is unrepresentable; `:64-67` `Accumulate` a real integrate-or-gate — and the integrate arm has nothing to integrate until the plane owns an emitter: the `Reservoir` prose samples "light samples" (`pathtrace.md:14`) while the WHOLE Render plane carries zero light vocabulary (no environment/sun/emissive row on any page), so the "correctness oracle" renders black by construction; the repair mints ONE closed `LightSource` row family (Environment | Sun | Emissive — seed DATA per `[GENERATOR_LAW]`) shared by the raster shading path and the oracle — one light rig, two integrators, comparability by construction — with the site-anchored Sun row deriving from the Bim `GeoReference` seam + the NodaTime instant under `ClockPolicy`, its azimuth/altitude COMPOSING the LANDED Compute solar-position export (`SolarPosition.At(SolarSite, Instant) -> SunPosition` — the declared `[APPUI_SUN_EXPORT]` package-boundary row on `Analysis/daylight.md` naming the AppUi viewport sun-light; the crossing lands as a declared `[V9]` ledger row), never a second geodesy or solar-position kernel; `virtualization.md:127-131` `Reaccumulate` becomes the Fenwick/prefix tree the `:78` prose claims (O(log n) update/query) — ruled: Fenwick, the million-row tree is the page's charter; `controls.md:181-184` `Rebind` actually resets bindings to the new row's `ValuePath` and re-applies the token role (the recycling core), and `Apply` (`:136-141`) covers the token brush/metric set its prose claims; `animation.md:107,121` `PingPong` gains a direction field and actually reverses, `Scrub.To`'s unused `scheduler` param (`:176`) marshals or dies, and the caller-threaded interpolation-delegate tail (`lerpD`/`lerpCam`/`lerpColor` re-spelled at every call site — `SampleAt` `:141-145`, `Scrub.To` `:176-185`, `Walkthrough.Render` `:210-217`, `tour.md:73`) collapses into track-owned interpolation policy rows per `[GENERATOR_LAW]`: the Camera row is a POSE interpolation (`System.Numerics.Quaternion.Slerp` over the camera orientation with the eased positional arc — the stratum peer of the kernel `MotionInterpolation` one-slerp law, `Rasm/.planning/Parametric/projections.md`, under the same one-site discipline: animation's Camera row is the ONE pose-interpolation site AppUi-wide and `Collab/tour.md`'s transition interpolation rides it at the `[V3]`e collapse), the Color row is the tokens OKLab mix the page's own `:18` prose already legislates against its delegate-taking fence (prose-fence parity), and a component-wise eye/target/up lerp or a second pose-interpolation site is the deleted form; `tables.md:234-237` `Delimited` gains RFC-4180 quoting/escaping; `inspector.md:360-372` `ThreeWay.Diff` becomes a real LCS/diff3 alignment (anchor-based hunk detection) — the positional zip contradicts the page's own `:299` law; `pipeline.md` `Empty` fault path stops sinking ordinal `0L` (`:194` — thread the interlocked ordinal) and the `:16` prose signature resyncs with the `:155` fence; `drafting.md:310` `ProjectionBasis.Top` un-pins — `SheetRegion` (`:77`) carries its own basis/view spec per region and `meshOf(region.Key)` stops conflating region key with mesh lookup; `solver.md:182` `Suggest` registers edit variables in `Load` (the implicit contract enforced) and `SolvedRect` (`:190`) keys by a required child identity, never a nullable `Name`; `dialogs.md:68` `ToastPipe` returns `IO<Unit>`; `media.md:100-101` `Mount` wires seam/`Source`/`AutoPlay`/`Loop` over the verified `MpvView.Renderer`/`MpvContext` binding — ruled: wire (`api-libmpv` members verified); `MPV_SURFACE_MOUNT` narrows to the libmpv-native provisioning fact; `navigation.md:39` parses the `rasm:` scheme robustly; `typography.md:172-178` FrontMatter/Footnotes populate or the fields carry their `FRONT_MATTER_AST` gate inline; `shading.md:106-109` `BindShade` stays spike-gated with the stub named. The five run-on Boundary paragraphs refactor to declarative clauses under the `[SEAM_AND_RAIL_LAW]` prose-fence parity law.

### [V7_DRAFTING_WRITE_LEG]

Mandate item 2 is DISPOSED as satisfied on disk — painter sort dropped root-up (`drafting.md:96,418`), `HiddenLineSeam` composes Fabrication `#PROJECTION_HIDDEN_LINE`, the seam declared BOTH sides (`ARCH:75-76` ⇄ `Rasm.Fabrication/ARCHITECTURE.md:47`), zero painter-sort remnant corpus-wide — and this verdict seals its perimeter. (a) The WRITE leg is TWO rows on one `DraftEmit` axis, BOTH binding write-scoped ACadSharp over ONE `CadDocument` entity fold: DXF via `DxfWriter` (binary/text), DWG via `DwgWriter` (`drafting.md:342-360`). The `netDxf` removal is LANDED per `[ROSTER_RECONCILIATION]` (props row, csproj row, catalog surface — one document model with two writer rows is the `[GENERATOR_LAW]` form the two-model split violated); the page's netDxf emit fence (`drafting.md:363-371`) re-fences on `DxfWriter` in this rebuild; the DXF round-trip fidelity probe (dimensions/hatches/leaders/blocks) is a standing obligation on the write-leg rebuild — a failed entity class re-pins netDxf as a recorded exception, never silently. Bim keeps the READ authority (the Fabrication-side wording is already refined — `RASM-CS-FABRICATION-DECISION.md` records netDxf removed and names the AppUi ACadSharp two-format WRITE leg at its Ingress/profile row); the hardcoded `AutoCad2018` version literal becomes a policy row. (b) Geometry migration insulation: the kernel visibility engine is LANDED (`Drawing/view.md` `DrawingProjection`) and Fabrication's BSP dies for it — AppUi's producer STAYS the Fabrication `HiddenLineResult` receipt, and the kernel page now states that routing itself (`view.md` names Fabrication the carrier consumer and AppUi the receipt consumer — the RhinoCommon-typed `Point3d`/`Polyline` surface reaches the host-free sheet layer only through the receipt), so the supersession is invisible at this seam (the HLR-row label still reads "BSP visibility solver" — the wording re-points to the kernel `DrawingProjection` route in the same ledger motion); the stale declared-unwired `ARCH:74` kernel `DrawingProjection` direct row RETIRES, and `ARCH:75` `ChartAtlas` retires AppUi-SIDE only (ruled: retire — no AppUi page demands a UV atlas today; the capability stands on the `[V9]` growth register; the kernel `Processing/flatten` ChartAtlas owner does NOT retire — its live consumer is Fabrication `Nesting/nfp`); both retirements stay TWO-SIDED: the kernel LEDGER still declares the view→AppUi and flatten→AppUi mirror rows (`Rasm/ARCHITECTURE.md:109,:113`), so the ledger re-scope is the RECORDED kernel-side counterpart edit — the flatten row re-scopes toward the Fabrication route with the growth recording carried — never left one-sided. (c) The silhouette edge-style consumption (`EdgeStyle.Silhouette`) stays forward-ready with its producer chain NAMED: the landed kernel view engine carries connectivity-chained visible/hidden polylines with the `EdgeKind` classes (`DrawingProjection.ToPolylines`, successor-linked per set), Fabrication's `HiddenLineResult` receipt carries the silhouette class through the settled route (`FAB:46-47`), and the AppUi row consumes the receipt class — no new seam.

### [V8_INTEGRATION_MANDATES]

The three zero-consumer admissions realize as named pages (integration-first law): (a) `Document/export.md` — PDFsharp+MigraDoc become the paginated-document owner: the MigraDoc flow DOM (`Document`/`Section`/`PdfDocumentRenderer`/`FormattedDocument`) replaces the hand-rolled `FlowBlock`/`FlowFold` engine (`capture.md:274-340`); PDFsharp `Security` (AES-256), `Signatures`, `AcroForms`, and `UAManager.ForDocument` PDF-UA tagged output (namespace `PdfSharp.UniversalAccessibility`) land as export policy rows — the signature arm's key ingress is honest: `DigitalSignatureHandler.ForDocument(PdfDocument, IDigitalSigner, DigitalSignatureOptions)` (`api-pdfsharp.md:137`) takes a signer COMPOSED at the boundary whose credential material rides the AppHost `Runtime/secrets.md` lease lifecycle (acquire/renew/zeroize — the settled AppHost export naming AppUi), never AppUi-held key bytes, with the crossing a declared `[V9]` ledger row; `OfficeExport` XLSX/DOCX/PPTX (`capture.md:367-462`) moves in as the OOXML arm; `SKDocument.CreatePdf/CreateXps` (`capture.md:478`) narrows to the pure-visual vector-print arm capture keeps; drafting's sheet-PDF and the diagnostics report-PDF compose THIS owner. (b) `Charts/basemap.md` — Mapsui `MapControl`/`Map`/`GeometryFeature` realize the declared `ARCH:69` seam: Bim-owned NetTopologySuite geometries draw as tiled map overlays beside the Wgpu viewport, with the CRS chain honest: the features arrive carrying the Bim-owned `GeoReference` (the `GeoReferenceProjector` IfcMapConversion/IfcProjectedCRS lowering) and Bim's `GeoFeature.Reproject` owns geodesy (`Rasm.Bim/ARCHITECTURE.md:25,80,82` — the seam already declared both sides), so AppUi reprojects only WGS-84 input through `SphericalMercator.FromLonLat`/`ProjectionDefaults.Projection` (`api-mapsui.md:78-79`) and a local geodesy kernel is the forbidden form; LiveCharts `GeoMap`/`DrawnMap` (dashboards) stays the CHART-projection row, Mapsui the TILED-basemap owner — disjoint charters stated on both pages; the dashboards geo research items (`dashboards.md:473-474`) resolve against the split. (c) `Editing/graph.md` — NodeEditorAvalonia `IDrawingNode`/`DrawingNodeEditor` realize the promised parametric/dependency-graph canvas on ReactiveUI: node/pin/connector editing over a typed graph model, `LoroTree` as the co-edit data seam (`api-loro.md`) under ONE bidirectional projection with echo suppression — subscriber diffs discriminate `EventTriggerKind.Local/Import/Checkout` so a local UI mutation commits as tree ops without re-applying its own echo and a remote `Import` diff applies to the ReactiveUI graph model without re-emitting (the feedback loop is the named deleted form) — notebook `DependencyGraph` visualization as a read projection; `api-nodeeditor.md:149`'s pan/zoom disambiguation stands RULED: the canvas rides the package's own `NodeZoomBorder` (a distinct asset, NOT the admitted `PanAndZoom`, which keeps its five page consumers) — no dup exists. (d) The three ruled gap admissions realize on their composing pages in the same integration-first form: `capture.md` gains the `FFmpeg.AutoGen` mux/encode rows (frame stream -> H.264/MP4 under the codec/color owner; the walkthrough flythrough composes them — animation keeps its frame-sequence terminal and the encode is capture's row), `Document/export.md`'s print arm gains the `lcmsNET` device-CMYK transform rows (profile-to-profile with proofing and K-preservation intents as policy rows beside the PDFsharp arm), and `locale.md` realizes `LiveCaption` over `Whisper.net` (streaming transcription + VAD segmentation + the translate-to-English task as one owner; `LiveCaption.Translated` binds the English target, broad-target MT a growth row). Each page lands its `.api` re-anchor in the same motion.

### [V9_SEAM_LEDGER_AND_GOVERNANCE]

The perimeter becomes machine-checkably true in one motion. Ledger, stale rows: `ARCH:65` `Render/glb` phantom node DIES (the residency wire lives at `pipeline#TS_PROJECTION` — the row re-anchors there); `ARCH:68` `Render/query` ← Bim `ElementSet` retires (ruled: retire — no query page is chartered; the Bim query algebra stands on the growth register, re-opening when a consumer names it); `ARCH:72,:82` and the `:103` prose re-point off deleted `Rasm.Persistence/Sync` onto `Version/ledger` (the op-log/changefeed owner; the collab row re-writes to the `[V2]` edit-intent stream); `ARCH:74,:75` per `[V7]`; `ARCH:70` Bim/Planning `ScheduleNetwork` RETIRES (ruled: zero consuming fence corpus-wide — the same class as the query/atlas rows; it stands on the growth register). Ledger, wired-undeclared rows land: the declared collab rows (`ARCH:71-72`) re-target per `[V2]` (op-log projection + replay-window read on `Version/ledger`; snapshot accelerator on the blob lane); `Collab/sync` -> AppHost transport (the live-delta broadcast topic riding the AppHost bus/topics law — both sides named, the AppHost `topics.md` `[COLLAB_DELTA_FEED]` row landed); the AppHost PORT spine row family (`TelemetryContributorPort`/`ReceiptSinkPort`/`UiSchedulerPort`/`ClockPolicy`/`DataClassification` — one declared row family naming every composing folder, mirroring the AppHost wire-vocabulary tier's declared row form); `Charts/basemap` ← Bim geospatial (realized per `[V8]`, the `GeoReference` CRS ingress named); `Render/meshlets` ← Compute `payload.md` clusters (`[V5]`); `Document/export` -> AppHost `Runtime/secrets.md` (the signing-credential lease ingress, `[V8]`a); the content-identity mint row family -> kernel `Domain/identity` (`ContentHash.Of` — the capture/commands/notebook-replay/snapshot mints, ONE declared row family naming every minting folder, mirroring the AppHost port-spine row form; the kernel ledger's CONTENT_KEY fan gains the reciprocal AppUi row as a recorded counterpart). Named host gate, standing: the live-Rhino embed probe — `SurfaceHost.RhinoPanel`/`EmbedCapsule`/`RenderGraph.Lease` GPU-lease validation against an integrated RhinoWIP host surface that does not yet exist — stays host-gated and open. Growth register, the standing deferred-capability record (each row re-opens only when a consumer names it, never as a speculative seam): `ChartAtlas` texture channel, Bim `ElementSet` query surface, `ScheduleNetwork` 4D/CPM dashboards projection, broad-target machine translation past the Whisper.net English arm, kernel `Analysis/query.md` `Analyze` measured-query receipt projection — measure/inspect receipts as inspector and dashboard rows when a host-side producer ships them over the receipt spine (the kernel receipt-none ruling at `query.md:217` makes the host-side producer the gate). README router and the `ARCHITECTURE.md` codemap re-emit the final 8-folder/43-page set; every seam row whose SOURCE page moves re-anchors to its post-`[V3]` path in the same motion (the evidence source row -> Diagnostics, the notebook rows -> Document, the issues rows -> Collab).

### [V10_COLOR_VOCAB_SINGLETONS]

Two vocabulary splits collapse to one owner each. (a) Gamut: `custom.md:452` `ColorSpaceAxis` (4 rows, claimed "single suite-wide gamut vocabulary") vs `capture.md:190-196` `ColorPolicy` (6 rows: +WideGamut/+HdrPq) — ruled: `ColorPolicy` on the capture codec is the ONE gamut/transfer row family; `ColorSpaceAxis` dies or becomes a keyed projection of `ColorPolicy` rows (never a parallel enum with divergent membership). (b) Color science: `accessibility.md:142-143` computes WCAG relative luminance via Avalonia `ColorHelper` while `tokens.md:20` seals Unicolour as the one suite colour owner — ruled: the `ContrastGate` luminance routes through the Unicolour kernel (it owns the sRGB->luminance transform beside OKLab mix and colormap sampling), one color kernel suite-wide; the ColorHelper call is the deleted form.

### [V11_RECOMPUTE_CONSUMPTION]

`Document/notebook.md` resolves its one-owner contradiction: `DependencyGraph` (`notebook.md:115-168`) implements a full local topo/dirty/recompute engine while `:210` claims alignment to the AppHost `RecomputeGraph` and names "a second incremental-recompute owner" the deleted form. Ruled: COMPOSE, unconditional — the cell-dependency identity and dirty-propagation algebra ride the AppHost `RecomputeGraph` vocabulary through the declared port (decode-only), the notebook keeps only the document-local projection (cell-id <-> node-id map, UI dirty overlay), and the local engine DIES with no survival branch: `RecomputeGraph` is caller-keyed and granularity-neutral (`determinism.md:277-320`, its `:283` clause naming the AppUi notebook's per-cell composition), the consumer supplies its own descriptor and inputs, so per-cell recompute demands nothing the vocabulary lacks. The `CapabilityPin`/`DeterminismContext`/`EnvFingerprint` composition (`:206-210`) is already correct and stays. The replay-bundle input hashes (`notebook.md:243`) re-route through the kernel `ContentHash.Of` entry per the `[SEAM_AND_RAIL_LAW]` one-hasher clause.

### [V12_API_TIER_HYGIENE]

Landed catalog state, standing as law: the target-tier `api-unicolour.md` and `api-thinktecture-json.md` copies are DELETED (shared tier canonical per the two-tier law); `api-nodatime.md`/`api-unitsnet.md` are DELETED and `api-hashing.md` is the AppUi delta overlay over the shared canonical; `api-drafting-export.md` is write-scoped per `[V7]` (netDxf surface deleted; READ rows are the Bim catalog's recorded adoption); `api-loro.md`'s durable stacking states the `[V2]` charter; `api-avalonia-color.md` carries the `[V10]` caveat (`ColorHelper.GetRelativeLuminance` = deleted form, `ToDisplayName` stays); `api-mapsui.md`'s phantom cross-reference is repaired; `api-prodiagnostics.md` is authored at full member depth. The manifest is settled: zero version motion beyond the ruled admissions — the `netDxf` row removed (props + csproj + catalog), the `ProDiagnostics` row admitted (props `[App UI]` group + csproj `Dev Loop` group), and the `FFmpeg.AutoGen`/`lcmsNET`/`Whisper.net` rows admitted with their member-true catalogs per `[ROSTER_RECONCILIATION]`. Standing obligations riding the pages that mint: `api-pdfsharp.md`/`api-mapsui.md`/`api-nodeeditor.md` re-anchor to their `[V8]` owning pages; `api-avalonia-fluent.md` confirms its retained-floor-beneath-Semi charter live (one composition-level wiring) or flags dead; the substrate `api-languageext.md` VERIFIES under the track-shared authoring law (one substrate file — verified, never re-authored); every kept catalog's page anchor re-verifies against the 43-page set.

## [02]-[EVIDENCE_REGISTER]

The corpus's current-state defect record — the load-bearing anchors every correction lands against. Anchors are `libs/csharp/Rasm.AppUi/.planning/`-relative; `ARCH:` anchors the package `ARCHITECTURE.md` at its CURRENT re-emitted state (glb `:65`, query `:68`, basemap `:69`, ScheduleNetwork `:70`, collab-blob `:71`, collab-ledger `:72`, kernel-view `:74`, ChartAtlas `:75`, HLR `:77-78`, history-ledger `:82`, device `:83`; collab registered at codemap `:39` + router `README:31`, 37 rows each); `API:` the target catalog tier; `FAB:` `Rasm.Fabrication/ARCHITECTURE.md`. Two AppUi pages carry post-verify sibling-resolver edits as settled disk law: `Shell/commands.md` (the palette wire is `CommandGateWire`; `CommandAvailabilityWire` is AppHost health's carrier, never shadowed) and `Editing/notebook.md` (the composed AppHost determinism vocabulary spells `ChainHash`). Upstream law binds by NAMED contract, never by sibling line number.

| # | Class | Evidence |
|---|---|---|
| E1 | Band collisions + squats | Internal: `tour.md:30-32`(4300)≡`virtualization.md:48-51`(4300); `forms.md:29-32`(4900)≡`reality.md:30-34`(4900); `collab.md:43-48`(0x4B00)≡`media.md:28-31`(0x4B00). AppHost squats: `hosts.md:43`(4100)+`assets.md:29-32`(4120)+`dialogs.md:39`(4130)+`input.md:289-292`(4150) ⊂ `ConfigError` 4100-4199; `pipeline.md:32-36`(4500)≡`HopFault`; `drafting.md:271-274`(4600)≡`CommandFault`; `inspector.md:138,152-174`(4700-4799) ⊃ ten AppHost bands; `notebook.md:96-99`(4800)≡`FederationFault`. Persistence squat: `issues.md:29-32`(5000). Hex fork: `history.md:37-40`(0x4A00), `shading.md:28-31`(0x4C00), `immersive.md:29-33`(0x4D00). Also `controls.md:29-33`(4200), `solver.md:58-65`(4400) — foreign-space residents. Zero registry; every code a `base(detail, NNNN)` literal — the banned form the AppHost registry law names |
| E2 | Durable CRDT inversion | `collab.md:101,105` opaque-delta replay + `:117-120` `CollabSnapshot` content-addressed Loro blob as durable state; `notebook.md:178` co-edit over "the Persistence op-log changefeed" inheriting the form; `issues.md:123` `CommentOp` "projects onto the Persistence `OpLogEntry`"; the Persistence roster law records LoroCs rejected for the durable wire (bit-parity) and re-seals it; `history.md` the correct-discipline exemplar (typed deltas -> `SyncOpKind`) |
| E3 | Registration discharged + live contradiction | `collab.md` registered on disk (codemap `ARCH:39`, seams `ARCH:71-72`, router `README:31` — 37 nodes/rows); only the `[V3]` folder move remains; LIVE: `issues.md:118-146` `CommentOp`/`CommentThread`/`HlcStamp` vs `collab.md:20` ("DROPPED root-up") + the map-container charter; `issues.md:123` cites dead `notebook.md#CRDT_COEDIT` law |
| E4 | Decorative fences | `meshlets.md:71-81` degenerate Partition (whole-mesh `Bounds`, `NormalCone(0,0,1,-1)`, radius-as-error) vs `:14` prose + `pipeline.md:463-470` cone-carrying `MeshletWire` + `:542-556` Compute-built consumption; `pathtrace.md:33-36` non-recursive `Bvh.Split`, `:38-40` center-sum `Enclose`, `:64-67` traceless `Accumulate` vs `:3` oracle claim; `pathtrace.md:14` `Reservoir` "light samples" prose with ZERO light/emitter vocabulary on any Render page (no environment/sun/emissive row — the oracle has no light to transport); `virtualization.md:127-131` O(n) vs `:78` Fenwick claim; `controls.md:181-184` no-op `Rebind` vs `:158`; `reality.md:46-49` local `XxHash128` vs `pipeline.md:404,496` single-mint; `reality.md:54-58` dead `SplatSort` (`:72-78` `OrderByDescending`); `animation.md:121` PingPong≡Once, `:176` unused scheduler; `inspector.md:360-372` positional zip vs `:299`; `tables.md:234-237` unquoted `string.Join`; `media.md:100-101` Mount stub; `drafting.md:310` pinned `ProjectionBasis.Top`; `solver.md:182,190` Suggest/SolvedRect contracts; `pipeline.md:194` `Empty` ordinal 0L, `:16` vs `:155` prose drift; `typography.md:172-178` always-empty FrontMatter/Footnotes |
| E5 | Zero-consumer admissions | PDFsharp: `API:api-pdfsharp.md:155,161` names non-consuming consumers, `capture.md:274-340` bespoke `FlowBlock`/`FlowFold`, `:367-462` OfficeExport, `:478` `SKDocument.CreatePdf`; Mapsui: `ARCH:69` declared-unwired; NodeEditor: README promise, no page, `API:api-nodeeditor.md:149` PanAndZoom note — pins live in the props `[App UI]` block and csproj/README package groups |
| E6 | Seam-ledger rot | Phantoms: `ARCH:65` `Render/glb`, `ARCH:68` `Render/query`; dead targets: `ARCH:72,:82` + prose `:103` `Persistence/Sync` (deleted by the Persistence campaign; owner `Version/ledger`); the `NotebookOp` row already gone; declared-unwired: `ARCH:74` kernel `DrawingProjection` direct row (the kernel `view.md` now states receipt-only AppUi routing; `drafting.md:308-310` composes the FAB seam; the kernel ledger's view→AppUi mirror row at `Rasm/ARCHITECTURE.md:109`), `ARCH:75` `ChartAtlas` (flatten→AppUi mirror at `:113`), `ARCH:69` basemap; wired-undeclared: collab->transport, the AppHost port spine (~30 composing pages, one notebook row), meshlets<-Compute payload; two-sided-correct: `ARCH:77-78` ⇄ `FAB:46-47` HLR receipt |
| E7 | Write-leg scope | `drafting.md:342-360` ACadSharp `CadDocument`/`DwgWriter` + `AutoCad2018` literal (`:363`) beside a live netDxf emit fence (`:363-371`) and a netDxf Packages row (`:260`) — the `[V7]` rebuild re-fences both on `DxfWriter`; the Fabrication counterpart is CLOSED on disk — RASM-CS-FABRICATION-DECISION.md's `Ingress/profile` seam row names the AppUi ACadSharp two-format WRITE leg and its roster records netDxf REMOVED (the once-imprecise "netDxf owns the WRITE leg" wording is gone); manifest state LANDED: the netDxf props/csproj rows removed, `API:api-drafting-export.md` write-scoped (netDxf rejected inline, READ rows the Bim adoption); ACadSharp stays the shared `[BIM]`-group pin with the csproj direct reference; feed truth: haplokuon/netDxf ARCHIVED 2023-10 (latest `2023.11.10`), DomCR/ACadSharp active |
| E8 | Vocabulary splits | `custom.md:452` `ColorSpaceAxis` (4 rows) vs `capture.md:190-196` `ColorPolicy` (6 rows), claimed projection at `capture.md:165`; `accessibility.md:142-143` `ColorHelper.GetRelativeLuminance` vs `tokens.md:20` Unicolour one-owner law; `notebook.md:115-168` local recompute engine vs `:210` one-owner claim (AppHost `RecomputeGraph`) |
| E9 | Bare `Error.New` rails | `navigation.md:46,81`; `locale.md:172-173`; `dashboards.md:296,304`; `custom.md:168,184,203,220,231,236,251,259,271,282,296,301,313,364`; `animation.md:62` — federation SEAM_AND_RAIL_LAW breaches pairing with E1; the `ChartFault` typing covers the full custom.md census |
| E10 | Governance state | the corpus presents a settled surface while the E1-E9 perimeter defects stand — the sealed-corpus illusion; the live-Rhino embed probe (host-gated GPU-lease validation, no integrated RhinoWIP host surface exists) stands open per the `[V9]` host gate; run-on Boundary paragraphs `dashboards.md:20`/`custom.md:19`/`collab.md:20`/`capture.md:165`/`evidence.md:278`; `locale.md:212-226` `LiveCaption` realizes on the admitted `Whisper.net` owner per `[V8]`d |
| E11 | Catalog tier | dedupe LANDED: target-tier unicolour/thinktecture-json/nodatime/unitsnet copies deleted, hashing trimmed to the delta overlay, `API:api-loro.md` re-pointed to the `[V2]` charter, `API:api-drafting-export.md` write-scoped, `API:api-prodiagnostics.md` authored; 54 catalogs, craft dense/TFM-honest/verified-real; remaining defects RELATIONAL — page anchors re-verify against the 43-page set as it lands |
| E12 | Upstream-clean confirmations | AppHost ports composed as settled vocabulary corpus-wide (hosts/commands/navigation/notebook/evidence — no host-plumbing re-mint; mandate item 3 sound except E1 bands + E8 recompute); `drafting.md:96,418` painter sort dropped (mandate item 2 disposed; zero painter-sort tokens corpus-wide); `pipeline.md:496` single-mint law + the settled Compute `ResidencyManifest.Mint` peer law; `reality.md` in-process SPZ/SOG decode honestly companion-routed while the minted `GaussianSplatScan` scan wire (`Compute/Runtime/wire.md:114`) is the consumed contract; Compute `payload.md` `meshlet-cluster` row with per-cluster `ResidencyMeshlet` descriptors and BY-CONSTRUCTION monotonic error columns — the `[V5]` producer verified real |
| E13 | Kernel composition surface | the csproj kernel project reference is the material surface; firebreak: `Directory.Build.props` `IsRhinoCommonAwareProject` scopes host-reference inheritance to `libs/csharp/Rasm/` + the Rhino/GH roots — AppUi is NOT RhinoCommon-aware, so Rhino-typed kernel ingress (`Spatial/index.md` `Spatial.Apply(SpatialOp.Build(SpatialKind, BoundingBox[], BuildPolicy))` at `:119`, `Processing/intent.md` `Pose(Plane, Plane, ...)`) is boundary-unreachable while `Domain/identity.md` `ContentHash.Of(ReadOnlySpan<byte>) -> UInt128` (`:31`) composes clean; the kernel `Domain/identity.md:14` one-hasher prose law (naming Element/Persistence/python-runtime/ts-kernel — one algorithm, one seed) plus the `[02]-[CONTENT_KEY]` fan; AppUi raw-hash census the law re-routes: `capture.md:84,162` runtime `ContentHash` delegate, `commands.md:197`, `notebook.md:243`, `collab.md:117-119`, `reality.md:46-49` (the `[V5]` kill); the kernel ledger's view→AppUi and flatten→AppUi mirror seam rows; caller-threaded lerp tails `animation.md:141-145,176-185,210-217` + `tour.md:73`; the kernel `Spatial/index.md` `[DEGRADATION_REFIT]` law vs the `pathtrace.md:30-31` leaf-only refit |

Sound surfaces (preserve; touch only where a verdict names them): `Shell/hosts.md`, `commands.md`, `screens.md`, `Theme/tokens.md`/`typography.md`, `Editing/inspector.md` (minus E4 diff), `livedata.md`/`tables.md`/`forms.md`/`history.md` (minus named repairs), `Charts/dashboards.md`/`custom.md` (minus E8/E9), `Render/pipeline.md` (minus E4 edges), `capture.md` (minus the `[V3]` extraction + E8), `drafting.md` (minus the `[V7]` perimeter), `evidence.md` content (structure moves per `[V3]`), `animation.md` content (placement moves), and the corpus-wide receipt/telemetry spine (`AppUiTelemetry.Contribute` + `ReceiptSinkPort` envelope discipline) — the strongest single-spine discipline in the C# tree.

## [03]-[CAPABILITY_ESCALATION]

The per-plane required end-states — each row is the acceptance bar its owning verdicts carry:

| Plane | Required end-state |
|---|---|
| Fault space (registry) | type-enforced 6xxx `FaultBand` registry; 3 collisions + 11 foreign squats unrepresentable; hex fork dead; zero bare `Error.New`; AppHost mirror row both directions |
| Collab durable spine | edit-intent ops on Persistence-owned rows; replay-window cold-load into fresh `LoroDoc`; session-epoch join law (active-session join = in-session Loro wire, cold start = replay seed); snapshot demoted to derivable accelerator; issues on the map container; live half untouched |
| Render residency (meshlets/reality) | Compute cluster consumption (local Partition dead); real wire-cone cull; hysteresis LOD cut off monotonic error columns; HZB two-phase occlusion pass; one payload carrier; payload-key identity (re-hash dead) |
| Render integrators + temporal (pathtrace/shading/animation) | recursive SAH BVH + real enclose + kernel-shaped degradation refit, page-local and private (the kernel `SpatialAnswer.Wire` node-link crossing stays the one acceleration currency) + honest accumulate; ONE `LightSource` row family shared by both integrators (oracle comparability by construction); shading spike-gates named; one BSDF two evaluators preserved; the lerp-delegate tail dead — track-owned interpolation policy rows, the Camera row the one pose-slerp site AppUi-wide |
| Drafting + CAD emit | per-region projection basis; two-row write leg on ONE `CadDocument` fold (ACadSharp `DxfWriter`+`DwgWriter`; netDxf removed, fidelity-probed); version-policy row; Geometry supersession insulated at the Fabrication receipt |
| Document plane (notebook/media/export) | MigraDoc flow DOM + PDF security/signatures/AcroForms/UA; signing-key ingress via the AppHost secrets lease; lcmsNET device-CMYK print lane; OOXML arm re-homed; `RecomputeGraph` composed; media mount wired; bespoke pagination dead |
| Charts + geo | typed `ChartFault`; Mapsui basemap page wiring the Bim NTS seam with the `GeoReference` CRS ingress named; GeoMap/basemap charters disjoint; cross-filter engine preserved |
| Editing typed-edit | real diff3; CSV quoting; NodeEditor graph canvas page with echo-suppressed LoroTree co-edit |
| Shell spine | Fenwick ledger real; Rebind recycling real; solver contracts enforced; typed `NavFault`; Unicolour luminance |
| Theme vocabulary | animation re-homed (pure vocabulary tier); typed `LocaleFault`; LiveCaption realized on Whisper.net; front-matter fields live or gated |
| Diagnostics | four real owners (evidence/proof/devloop/governor); `[FAULT_TABLES]` sited; receipt union + correlation preserved verbatim; devloop composes the ProDiagnostics inspector |
| Governance perimeter | ledger complete both directions; 43-page codemap/router; collab registered; catalogs anchored to live pages |

## [04]-[PACKAGE_PRESSURE]

Mine-to-depth obligations over the ADMITTED roster (both `.api` tiers stack; folder tier alone is a defect):

- SHARED-TIER LAW: every fold demands the `libs/csharp/.api` weave — Thinktecture `[Union]`/`[SmartEnum<TKey>]`/`[ValueObject]` + `.Json` as the ADT backbone, LanguageExt `Fin`/`Validation`/`IO`/`Atom` rails at every entry, content identity through the kernel `Rasm.Domain` `ContentHash.Of` seed-zero entry (the federation one-hasher — an AppUi-local `System.IO.Hashing` `XxHash128` call site is the deleted form; Compute-minted payload/splat keys stay decode-only per `[V5]`), NodaTime under `ClockPolicy`, UnitsNet on measure-bearing rows, Unicolour as the ONE color kernel (`[V10]`), Verify+Headless as the proof substrate — a page composing folder packages without this weave is the same defect class as a thin one-shot call into a deep package. The weave extends past the base seven where a page's concern names it: `QuikGraph` (shared `api-quikgraph.md`) owns `graph.md`'s connection-admission cycle gate and graph algebra (never a hand-rolled adjacency, never a second recompute owner beside the AppHost `RecomputeGraph`); `CommunityToolkit.HighPerformance` (shared `api-highperformance.md`) owns the Charts `DimensionIndex`/`CrossFilter` bitmap engine's and residency/virtualization hot paths' pooled buffers (`Span2D`/`MemoryOwner`); `Riok.Mapperly` (shared `api-mapperly.md`) owns the boundary wire projections (BCF `ToTopic`, TS wires, payload decode) per the mapping-aspect law; the typography `FRONT_MATTER_AST` gate's YAML decode binds the existing `YamlDotNet` transitive-floor pin promoted to a direct reference only when the arm goes live — no new pin.
- `Rasm` (kernel, the csproj project reference) — the below-port compile surface, composed as landed fences under the `[PLACEMENT_LAW]`(e) firebreak: `Domain/identity.md` `ContentHash.Of` is THE composed entry (every AppUi content-identity mint — capture runtime delegate, walkthrough frame proof, command payload digest, notebook replay inputs, the `[V2]` snapshot key — with hex/wire encodings staying boundary projections at the consuming seam); `Drawing/view.md` `DrawingProjection` reaches AppUi ONLY through the Fabrication `HiddenLineResult` receipt (`[V7]`); the `Analysis/query.md` `Analyze` receipt projection stays a `[V9]` growth-register row, never a speculative seam; Rhino-typed kernel members are firebreak-unreachable and never named in AppUi fences.
- `LoroCs` — the tier's best-mined catalog stays the reference depth (container forest, `SubscribeLocalUpdate`, `ExportMode.Snapshot/Updates/ShallowSnapshot`, `EphemeralStore`/`Awareness`, `UndoManager`, `ForkAt`/`RevertTo`); the `[V2]` recharter binds ONLY the durable stacking; `LoroTree` gains its `[V8]` graph-canvas consumer.
- `PDFsharp`+`PDFsharp-MigraDoc` — the `[V8]` export owner mines: MigraDoc `Document`/`Section` flow DOM + `PdfDocumentRenderer`/`FormattedDocument` auto-pagination (kills `FlowBlock`/`FlowFold`), PDFsharp `PdfDocument` security (AES-256), `DigitalSignatureHandler.ForDocument`/`IDigitalSigner` signatures (credential material via the AppHost secrets lease, never AppUi-held bytes), `AcroForm` fields, `UAManager.ForDocument` PDF-UA tagged output; `XGraphics` interop with the capture capsule for placed visuals.
- `Mapsui.Avalonia12` — `MapControl`/`Map`/`Navigator`, `GeometryFeature` over Bim NTS, tiling/rendering-Skia layers as basemap rows, `SphericalMercator`/`ProjectionDefaults.Projection` as the ONLY AppUi-side reprojection (geodesy stays Bim's `GeoFeature.Reproject`); the transitive Mapsui/Tiling/Nts/Rendering.Skia set stays transitive.
- `NodeEditorAvalonia`(+`.Model`, the transitive-floor pin) — `IDrawingNode`/`DrawingNodeEditor`/pin-connector model on ReactiveUI; the `LoroTree` co-edit bridge discriminates `EventTriggerKind` for echo suppression; export surface for canvas snapshots through the capture capsule; the PanAndZoom transitive-vs-admitted split is disjoint per `[V8]`c (`NodeZoomBorder` is the package's own asset).
- `ProDiagnostics` — the dev-loop inspector under the ORIGINAL `Avalonia.Diagnostics` assembly/namespace, so `AttachDevTools(DevToolsOptions)` binds unchanged: `DevToolsOptions` (default `F12` gesture, `LaunchView : DevToolsViewKind`, `HotKeys : HotKeyConfiguration`, `ScreenshotHandler : IScreenshotHandler`, `PropertyEditHandler : IDevToolsPropertyEditHandler`) is the attach-config row `Diagnostics/devloop.md` composes; `PropertyValueEditorService` owns live property commits, `VisualTreeDebug` the layout/renderer overlays, `VisualExtensions.RenderTo(Control, Stream, double)` the control-snapshot lane; Debug-gated `PrivateAssets="all"`, absent from the Release surface; the `ProDataGrid`/`ProCharts` siblings are NOT admitted.
- `SkiaSharp`(+harfbuzz/svg/skia-native) — `SKDocument` narrows to the pure-visual vector-print arm; the color-management mine (`SKColorSpace` ICC, float surfaces, `ToneMap` LUTs) stays capture's reference depth; `svg-skia` retained-scene pipeline stays assets'.
- `Silk.NET.WebGPU`(+`Extensions.WGPU`+wgpu native) — pipeline/shading/meshlets share `ONE_WGPU_DEVICE`; the `[V5]` occlusion raise mines `ComputePassEncoder`/`DispatchWorkgroups` for the depth-pyramid build and `QueryType.Occlusion` as the probe fallback; the timestamp-query mine (`GpuTimeline`) moves with `Diagnostics/governor.md`; `Silk.NET.OpenXR`(+EXT/FB/KHR ext) stays immersive's certified stack with the macOS loader-absent degrade honest.
- `LiveCharts`(XamlSeries), `DynamicData` (`Virtualise`/`TransformToTree`/one-`ObserveOn` capsule), `Kiwi` (`Try*` family + edit variables per `[V6]`), `PropertyGrid`/PropertyModels (ranked `EditorFactory` metadata lookup), `AvaloniaEdit`+`TextMateSharp` (CodePane capsule), `LibMpv` (mount wiring per `[V6]`), `AsyncImageLoader`, `Markdig` (front-matter/footnote arms live or gated), `HidSharp`/`DryWetMidi`/`Silk.NET.Input`/`Silk.NET.SDL` (the 4-SDK `DeviceDriver` capsules), `Dock.Avalonia`(+`Dock.Model.ReactiveUI`+serializer — `BoardState` + layout blobs over one serializer), `Semi`/`Ursa` over the retained Fluent floor, `Behaviors` (one `BehaviorRail.Intent` bridge), `PanAndZoom`, `HotAvalonia` (devloop), `MessageFormat` (ICU CLDR plural law) — mined-confirmed, zero action beyond registry-band touches and the named `[V6]` repairs.
- Avalonia platform tier — mined-confirmed, previously uncited (stub anchors now law): `api-avalonia.md` (the composition/`ICustomDrawOperation`/`GetObservable` core capture/pipeline/controls compose), `api-avalonia-desktop.md`/`api-avalonia-skia.md`/`api-avalonia-fonts.md` (host lifetime + Skia platform + the Inter floor under tokens/typography), `api-avalonia-grid.md` (the `DataGridCollectionView` projection engine — `tables.md`'s ONE tabular boundary; the `tables.md:120` TreeDataGrid rejection stays sealed), `api-avalonia-color.md` (inspector `ColorView` editor row; its `ColorHelper.GetRelativeLuminance` row is the `[V10]` deleted form — the catalog carries the Unicolour-routing caveat, `ToDisplayName` stays), `api-dialoghost.md` (the `DialogIntent` -> static `Show`/`DialogSession` stack `dialogs.md` composes; the `[V6]` `ToastPipe` repair rides it), `api-fluenticons.md` (the 2789-member `Symbol` icon identity — `assets.md`'s canonical vocabulary).
- `api-avalonia-gpu-interop.md` — UNMINED depth, the one catalog whose consumer fence lags its own law: `:3` rules `ICompositionGpuInterop.ImportImage`/`ImportSemaphore` + `CompositionDrawingSurface.UpdateWith{KeyedMutex,Semaphores,TimelineSemaphores}Async` as the composited-presentation family superseding the bare `ISkiaSharpApiLease` path when an external backend owns the render, while `pipeline.md:594` stops at `DeviceLuid`/`DeviceUuid` adapter matching and keeps a `SurfaceConfigure`/`SurfaceGetCurrentTexture` swapchain; the pipeline rebuild discriminates the Wgpu presentation arm — the in-tree composited viewport imports the rendered texture through the interop family (a second swapchain in composited mode is the deleted form), the surface swapchain surviving only as the exclusive-fullscreen/headless arm.
- Reactive stack quartet — mined-confirmed, previously uncited: `api-reactive.md` (scheduler/disposable algebra — the one-`ObserveOn` law's substrate), `api-reactiveui.md`/`api-reactiveui-avalonia.md` (`WhenActivated` lifecycle + `ReactiveCommand` under `commands.md`'s one rail), `api-reactiveui-validation.md` (`ValidationContext`/`ValidationRule` lifted into the suite `Validation<Error,T>` applicative — `forms.md`'s stated law); `api-avalonia-xaml-loader.md` re-anchors to `Diagnostics/devloop.md` (zero direct page composition — it is HotAvalonia's runtime-inflation substrate, `PrivateAssets` dev-loop scoped, the `evidence.md:368-375` knob rows moving with the `[V3]` split).
- `ACadSharp` (write-scoped per `[V7]`) — `DwgWriter`+`DxfWriter` over ONE `CadDocument` typed entity fold; version/unit derivation as policy rows; READ members re-home to the Bim catalog; `netDxf` REMOVED (settled per `[ROSTER_RECONCILIATION]`; the DXF fidelity probe is the standing `[V7]` obligation).
- `FFmpeg.AutoGen` — the in-process mux/encode owner on `capture.md`'s codec rows: format/codec contexts, `Yuv420p` frame conversion, H.264 encode, MP4 mux — the walkthrough flythrough composes the rows past its frame-sequence terminal; native FFmpeg ships as LGPL-configured dynamic-linked libraries (the catalog states the boundary fact).
- `lcmsNET` — the device-CMYK/ICC print-fidelity owner on `Document/export.md`'s print arm: `Profile`/`Transform` profile-to-profile conversion, `CreateProofingTransform` soft-proof, black-point compensation and K-preservation intents as policy rows; the native lcms2 ships with the app; Unicolour stays the suite color-model kernel — disjoint charters.
- `Whisper.net` — the `LiveCaption` owner on `locale.md`: `WhisperFactory` model load, streaming `ProcessAsync` timestamped segments, Silero VAD live segmentation, the built-in translate-to-English task binding `LiveCaption.Translated`; offline, model weights arrive via `WhisperGgmlDownloader`; broad-target MT stays a growth row.
- `DocumentFormat.OpenXml` — the OOXML part-graph writers move to `Document/export.md` intact.
- `Avalonia.Headless`+`Verify` — the `Diagnostics/proof.md` matrix substrate (CsCheck property lanes + Verify snapshot goldens); the headless capture lanes are the rewire's parity oracle for every `[V6]` fence repair.
- ROSTER end-state: ADMITTED — `ProDiagnostics` (devloop inspection; MIT; Debug-gated `PrivateAssets` binding), `FFmpeg.AutoGen` (capture encode rows), `lcmsNET` (export print arm), `Whisper.net` (locale LiveCaption); INTEGRATE — `PDFsharp`+`PDFsharp-MigraDoc`, `Mapsui.Avalonia12`, `NodeEditorAvalonia` (`[V8]`); REMOVED — `netDxf` (feed-verified archived upstream; DXF lands on ACadSharp `DxfWriter`, fidelity-probed at `[V7]`); REJECT recorded — `meshoptimizer` (Compute owns clustering), a second color-model kernel beside Unicolour, LoroCs removal, `TreeDataGrid` (`tables.md:120` seal upheld), `Avalonia.Diagnostics` (feed-dead at 11.3.x) and the pay-tiered Accelerate DevTools.

## [05]-[BUILD_ORDER_AND_ACCEPTANCE]

Foundations before consumers; the listed order IS dependency order. `ARCHITECTURE.md`/`README.md` close with the motion that changes their facts; `.api` re-anchors land with the motion that moves or first composes a package, and the page that first composes an integrated package deepens its catalog.

1. **PERIMETER + STRUCTURE FLOOR** — the `[V3]` structure motion FIRST (Diagnostics/Document/Collab folder mints with skeleton pages, the animation and Editing moves, the transfer of capture's `[06]` export sections into the `export.md` skeleton — condemned-but-intact there per `[STRUCTURAL_AUTHORITY]`); then the `[V1]` registry landing COMPLETE on the freshly minted `Diagnostics/evidence.md` (every 6xxx row assigned including the NEW union bands; a duplicate integer unrepresentable from the first landing; each union's re-derivation lands with its owning page's rebuild); the `[V9]` ledger corrections + codemap/router re-emit to the 43-page map; the seven upstream consumer-pressure contracts verified in place, never re-applied.
2. **VOCABULARY + SHELL SPINE** — `Theme/` (tokens/motion cold with bands; typography front-matter arms per `[V6]`; assets band; locale typed `LocaleFault` + LiveCaption gate); `Shell/` (controls `Rebind`/`Apply` real, virtualization Fenwick, solver contracts, navigation `NavFault` + scheme parse, dialogs `IO<Unit>`, input band, accessibility Unicolour luminance per `[V10]`, hosts/commands/screens cold with bands, the `commands.md:197` payload digest re-routed through the kernel `ContentHash.Of` per the one-hasher law).
3. **RENDER PLANE** — `pipeline.md` (ordinal thread, prose resync, band, the gpu-interop presentation-arm discrimination); `meshlets.md` (`[V5]` consumption — Compute clusters, wire cones, hysteresis LOD cut, HZB occlusion pass, the monotonic-error gate stated inline); `pathtrace.md` (`[V6]` real BVH/enclose/degradation-refit — page-local, private, kernel-wire-respecting — + honest accumulate + the `LightSource` rig shared with shading); `shading.md`/`immersive.md` (bands, honest gates); `reality.md` (`[V5]` carriers + payload-key identity + SplatSort disposition); `animation.md` (post-move rebuild: PingPong direction, scrub marshal, typed `AnimationFault`, the lerp-delegate tail collapsed to track-owned interpolation policy rows — the Camera row the one pose-slerp site, the Color row the tokens OKLab mix — the `Walkthrough` fold hardened as the surface tour later collapses onto); `capture.md` (post-extraction raster/codec owner + the `[V10]` gamut singleton + the runtime `ContentHash` delegate bound to the kernel `ContentHash.Of` entry per the one-hasher law); `drafting.md` (`[V7]` — region basis, the one-`CadDocument` two-row ACadSharp write leg with the DXF fidelity probe, version policy row, retired Geometry rows; the sheet-PDF egress signature-locks on the export destination vocabulary the structure motion moves, its MigraDoc deepening verifying at the output dry-run).
4. **CONSUMER PLANES + GOVERNANCE** — `Collab/` (`sync.md` `[V2]` recharter with the text-container convergence probe; `issues.md` `[V4]` map-container collapse; `tour.md` `[V3]`e collapse onto animation — its offline arm rides animation's `Walkthrough` fold and the moved `VisualDestination`, zero forward dependency on export's later deepening); `Document/` (`notebook.md` `[V11]` + `[V2]` re-anchor; `media.md` mount wiring; `export.md` NEW per `[V8]`a absorbing capture's flow/Office arms); `Editing/` (inspector diff3, tables quoting, forms/history/livedata cold with bands, `graph.md` NEW per `[V8]`c); `Charts/` (dashboards typed `ChartFault` + geo-split re-anchor, custom `ColorSpaceAxis` disposition, `basemap.md` NEW per `[V8]`b); `Diagnostics/` (evidence/proof/devloop/governor full rebuilds off the split skeletons — the `[FAULT_TABLES]` block landed at the registry motion is SEALED, preserved verbatim with rows append-only); the final `[V9]` governance truth pass; the acceptance proofs.

Cross-package counterpart state, AppUi named the demanding consumer throughout, sibling interiors never edited here: VERIFIED IN PLACE — the Compute cluster-LOD columns with the by-construction monotonic-error guarantee (`[V5]`), the Compute solar-position export (`[V6]`), the Persistence op-log ingest + replay-window read (`[V2]`), the AppHost 6xxx mirror row (`[V1]`, `lifecycle.md:329` both directions), the AppHost secrets-lease signing ingress (`[V8]`), the Fabrication write-leg recording (`[V7]` — the DECISION's Ingress/profile row), the kernel `view.md` receipt-only routing (`[V7]`b page-side). STILL RECORDED, pending sibling-side: the kernel LEDGER's view→AppUi and flatten→AppUi mirror-row re-scope plus the CONTENT_KEY fan's reciprocal AppUi row (`[V7]`/`[V9]`); the Bim catalog READ-row adoption (`[V12]`). Every `[V2]` signature-lock against a Persistence-owned spelling resolves against the emitted `.archive/RASM-CS-PERSISTENCE-DECISION.md`; every `[V5]` lock resolves against the landed Compute `payload.md` `meshlet-cluster` row.

Campaign acceptance closes on composed contracts, not the ledger: (a) a collaboration dry-run — two live replicas converge through one `LoroDoc`; a committed edit lands as a typed Persistence-owned op row; a FRESH process rehydrates the document from the replay window ALONE (zero Loro bytes read from durable truth) and matches the live frontier at content parity; a NEW peer joins the rehydrated epoch via live-peer state sync and a live delta round-trips both directions (zero Loro bytes crossing durable truth on either path); the snapshot blob deletes and cold-load still succeeds; an issue comment lands as a map-container op with `CommentOp` unrepresentable; (b) a fault-space audit — `rg 'base\(detail, [0-9]'` and `rg 'base\(detail, 0x'` return zero under `.planning/`, every `Code` resolves through exactly one 6xxx registry row, the three collisions unrepresentable, the AppHost mirror row landed both directions; (c) a residency dry-run — a viewport tile renders from a Compute `meshlet-cluster` payload with ZERO AppUi-side clustering, cull decisions read per-cluster wire cones, a cluster fully occluded by the prior-frame depth pyramid draws nothing, the LOD cut holds stable under the hysteresis band across a dolly move, a splat tile keys by the payload's own `ContentKey`; (d) an output dry-run — a paginated flow report with running headers/footers renders through the MigraDoc owner, a signed tagged-PDF (its `IDigitalSigner` composing AppHost-leased credential material) and an XLSX cross the same export owner, a print-arm page crosses the lcmsNET device-CMYK transform, drafting emits DWG+DXF through one `CadDocument` fold (the DXF fidelity probe green) + sheet-PDF with per-region projection bases, a basemap tile draws a Bim NTS overlay, a node-graph canvas round-trips over `LoroTree`, a flythrough clip muxes through the capture FFmpeg encode rows, a caption stream transcribes and translates through the Whisper.net owner; (e) the perimeter audit — every ledger row composed in a fence and every fence crossing ledgered, every catalog anchoring a live page, the router and codemap carrying exactly 43 pages in 8 folders, and the one-hasher audit green — every AppUi content-identity mint resolves through the kernel `ContentHash.Of` entry and `rg 'XxHash128\.Hash'` returns zero under `.planning/` outside decode-only payload-key reads. A dry-run reaching for a missing owner fails acceptance and reopens the owning verdict (`[V2]`/`[V5]`/`[V8]`).

## [06]-[OUT_OF_SCOPE]

- `Rasm.Persistence` interiors: the `OpLogEntry`/`SyncOpKind` row shapes, the ledger changefeed, the replay-window read, the blob lane, and the `CrdtOpWire` vocabulary are the Persistence campaign's work — AppUi projects and decodes; the ledger's consumer-pressure row naming AppUi is settled; no upstream edit is open.
- `Rasm.AppHost` interiors: the registry mechanics, transport/topics, `RecomputeGraph`, and the port implementations are the AppHost campaign's work — AppUi composes settled exports; the 6xxx mirror row and the secrets-lease signing-consumer clause are settled contracts.
- `Rasm.Compute` interiors: the `meshlet-cluster` encode arm, cluster-LOD columns, splat payload decode, and `ONE_WGPU_DEVICE` custody are the Compute campaign's work — the cluster-LOD clause (with the by-construction monotonic-error guarantee) is a settled contract; the in-process SPZ/SOG decode stays companion-routed per the Compute ruling, the minted `GaussianSplatScan` scan wire the consumed contract.
- `Rasm` kernel and `Rasm.Fabrication` interiors: the landed kernel corpus is standing law consumed as landed fences — the `DrawingProjection` engine (`Drawing/view.md`, now stating receipt-only AppUi routing itself), the spatial engine (`Spatial/index.md`), and Fabrication's consumption of the kernel engine are those campaigns' work; AppUi stays on the Fabrication `HiddenLineResult` receipt, composes `ContentHash.Of` as a landed public surface without editing kernel interiors, and records consumer pressure only (the kernel ledger's view→AppUi/flatten→AppUi mirror-row re-scope and the CONTENT_KEY-fan AppUi row are the recorded kernel-side counterpart obligations); the Fabrication-side write-leg recording is closed at the DECISION's Ingress/profile row.
- `Rasm.Bim` interiors: BCF topic semantics, the ACadSharp READ surface, NTS geospatial ownership, and the `ScheduleNetwork` domain — consumed at declared seams only; the READ-row catalog adoption is that campaign's recorded obligation.
- `Rasm.Materials` interiors: `LayeredBsdf`/`SlabStack`/`SurfaceShade` are consumed at the PATH_TRACE seam — no Render-side lobe math, the standing law.
- App-root composition: Rhino/GH2 host binding, exporter pins, process attachment — the APP stratum's; AppUi never grows a composition root, and the in-Rhino ALC firebreak (`loro.dylib` companion-only) stays law.
- The live-Rhino embed probe (`SurfaceHost.RhinoPanel`/`EmbedCapsule`/`RenderGraph.Lease` GPU-lease validation) — host-gated per the `[V9]` standing host gate, untouched.
- The typescript viewer/state/wire counterparts of the declared TS projections — consumed at the boundary wires (`GeometryResidencyWire`, `CommandPayloadWire`, `LayoutConstraintWire`, `EvidenceFeed`), never re-planned here.
- The federation-wide fault-band renumber beyond the AppUi 6xxx registry — escalation path only if the `[V1]` neighborhood cannot fit, which its registry headroom forecloses.
