id: no-predefined-target-name
snapshots:
  ? |
    <Target BeforeTargets="Build" Name="BeforeBuild"><Message Text="building" /></Target>
  : fixed: |
      <Target BeforeTargets="Build" Name="BeforeBuildStep"><Message Text="building" /></Target>
    labels:
    - source: <Target BeforeTargets="Build" Name="BeforeBuild"><Message Text="building" /></Target>
      style: primary
      start: 0
      end: 85
  ? |
    <Target Name="AfterBuild" AfterTargets="Build"><Message Text="built" /></Target>
  : fixed: |
      <Target Name="AfterBuildStep" AfterTargets="Build"><Message Text="built" /></Target>
    labels:
    - source: <Target Name="AfterBuild" AfterTargets="Build"><Message Text="built" /></Target>
      style: primary
      start: 0
      end: 80
  ? |
    <Target Name="AfterBuild" DependsOnTargets="Stamp"><Message Text="built" /></Target>
  : fixed: |
      <Target Name="AfterBuildStep" AfterTargets="Build" DependsOnTargets="Stamp"><Message Text="built" /></Target>
    labels:
    - source: <Target Name="AfterBuild" DependsOnTargets="Stamp"><Message Text="built" /></Target>
      style: primary
      start: 0
      end: 84
  ? |
    <Target Name="AfterBuild"><Copy SourceFiles="settings.ini" DestinationFolder="$(OutDir)" /></Target>
  : fixed: |
      <Target Name="AfterBuildStep" AfterTargets="Build"><Copy SourceFiles="settings.ini" DestinationFolder="$(OutDir)" /></Target>
    labels:
    - source: <Target Name="AfterBuild"><Copy SourceFiles="settings.ini" DestinationFolder="$(OutDir)" /></Target>
      style: primary
      start: 0
      end: 100
  ? |
    <Target Name="AfterPublish" Condition="'$(Count)' > '0'"><Message Text="published" /></Target>
  : fixed: |
      <Target Name="AfterPublishStep" AfterTargets="Publish" Condition="'$(Count)' > '0'"><Message Text="published" /></Target>
    labels:
    - source: <Target Name="AfterPublish" Condition="'$(Count)' > '0'"><Message Text="published" /></Target>
      style: primary
      start: 0
      end: 94
  ? |
    <Target Name="AfterResGen"><Message Text="resources" /></Target>
  : fixed: |
      <Target Name="AfterResGenStep" AfterTargets="ResGen"><Message Text="resources" /></Target>
    labels:
    - source: <Target Name="AfterResGen"><Message Text="resources" /></Target>
      style: primary
      start: 0
      end: 64
  ? |
    <Target Name="BeforeCompile"><Message Text="compiling" /></Target>
  : fixed: |
      <Target Name="BeforeCompileStep" BeforeTargets="Compile"><Message Text="compiling" /></Target>
    labels:
    - source: <Target Name="BeforeCompile"><Message Text="compiling" /></Target>
      style: primary
      start: 0
      end: 66
  ? |
    <Target Name="BeforeResolveReferences"><Message Text="resolving" /></Target>
  : fixed: |
      <Target Name="BeforeResolveReferencesStep" BeforeTargets="ResolveReferences"><Message Text="resolving" /></Target>
    labels:
    - source: <Target Name="BeforeResolveReferences"><Message Text="resolving" /></Target>
      style: primary
      start: 0
      end: 76
  ? |
    <Target Name="afterbuild"><Message Text="built" /></Target>
  : fixed: |
      <Target Name="afterbuildStep" AfterTargets="build"><Message Text="built" /></Target>
    labels:
    - source: <Target Name="afterbuild"><Message Text="built" /></Target>
      style: primary
      start: 0
      end: 59
  ? |
    <Target Name='AfterBuild'><Message Text="built" /></Target>
  : fixed: |
      <Target Name='AfterBuildStep' AfterTargets="Build"><Message Text="built" /></Target>
    labels:
    - source: <Target Name='AfterBuild'><Message Text="built" /></Target>
      style: primary
      start: 0
      end: 59
