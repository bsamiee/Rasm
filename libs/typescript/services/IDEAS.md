# [SERVICES_IDEAS]

The forward pool of higher-order concepts for the node tier and its hosting IaC. Each open card is a bracketed slug leader plus the capability, what it unlocks, and the gap or modern technique it draws on; an idea drives one or more `TASKLOG.md` tasks. Closed ideas move to `[2]-[CLOSED]` with a one-line disposition so the same idea is never re-litigated.

## [1]-[OPEN]

(none)

## [2]-[CLOSED]

[OBJECT_STORE_TIER]: realized — `object-store/object-store#OBJECT_STORE` owns the one `ObjectStore` `Effect.Service` over the `@effect-aws/client-s3` `S3Service` layer (getObject/putObject incl. `{presigned:true}` + the full multipart surface + `listObjectsV2Stream`), the `ObjectKey` content-addressed `[ValueObject]` reusing the `interchange` `XxHash128`/`ONE_CONTENT_IDENTITY` seed, the `PresignGrant` `Data.TaggedEnum` (get/put/multipart, GUC-scoped, `Match.tagsExhaustive`), the `AssetCodec` one-`Schema.Literal` fan-out (csv/xlsx/pdf/archive + sharp transform) migrated OUT of `persistence/work-and-signals#WORK_AND_SIGNALS`, and the `LifecyclePolicy` rows over `listObjectsV2Stream`. Raw `@aws-sdk/client-s3` demoted to interop-only via `S3ClientInstance`; `@effect/platform` Multipart flagged as an orchestrator-owned branch-`.api` dependency the page references but does not catalogue.

[REACTIVE_QUERY_INVALIDATION]: realized — `persistence/reactive-query#REACTIVE_QUERY` owns the read-side `ReactiveQuery` `Effect.Service` over `SqlClient.reactive([keys], query)`/`reactiveMailbox` bound to the `@effect/experimental` `Reactivity` substrate (the `Reactivity` requirement discharged by the `@effect/sql-pg` `PgClient.make` layer's bundled default), the entity-keyed `InvalidationKey` vocabulary, the `Reactivity.mutation` key-publish edge the `interchange` `CommandGateway`/`eventing` relay wrap, and the dependent-read re-run `Stream`. Re-affirms the `EXPERIMENTAL_RETIRE_OR_KEEP` keep — `Reactivity` the second first-class `@effect/experimental` consumer beside the `Sse` decode.

[NATIVE_DELAYED_DELIVERY]: realized — `messaging/entity#DELAYED_DELIVERY` owns the `DeliverAt` per-key future-message rail (`[DeliverAt.symbol]()` → delivery `DateTime`, `DeliverAt.isDeliverAt`/`toMillis`, the `SqlMessageStorage` `deliverAt` storage rail), a `DeliverAt`-implementing payload scheduling its own future message through the shard manager beneath the cluster-wide cron, composing rather than `Effect.timeoutTo`/a parallel timer table; wires the per-key retry-after, deferred saga step, agent-turn cooldown, and scheduled tenant action.

[ENTITY_RESOURCE_LIFETIME]: realized — `messaging/entity#ENTITY_RESOURCE` owns the `EntityResource` restart-surviving per-key resource (`EntityResource.make({ acquire, idleTimeToLive })`, the `makeK8sPod(v1.Pod)` managed-pod arm over `K8sHttpClient`, the `CloseScope` release) beside the actor's state-durability; `agent/session-actors#SESSION_ACTORS` deepened to COMPOSE it for the per-session `Chat` handle + host tool-transport session bound to the session key (acquired on `Start`, shared across `Turn`, released on `End`/idle-TTL), never re-acquired per `Turn`.

[IDEMPOTENCY_LEDGER]: realized — `persistence/idempotency-ledger#IDEMPOTENCY_LEDGER` owns the keyed `IdempotencyLedger` `Model.Class` + the `ClaimOrReplay` claim-state fold (fresh/claimed/replayed, `Match.tagsExhaustive` over the atomic `ON CONFLICT DO UPDATE … RETURNING (xmax = 0)` claim), serving outbox/entity/session-actors as the shared cross-surface exactly-once owner. Boundary recorded: admits ONLY the cross-surface dedup the per-surface primitives do not span — `@effect/cluster` `MessageStorage` `SaveResult.Duplicate` (`Success | Duplicate<R>`) keeps mailbox-replay dedup and `@effect/workflow` `idempotencyKey` keeps workflow-activity dedup; the gap is non-redundant, so authored rather than dropped.

[QUOTA_GOVERNOR]: realized — `messaging/quota-governor#QUOTA_GOVERNOR` owns the per-tenant aggregate-backpressure `QuotaGovernor` `Entity` placement (via `getShardGroup`) over the `@effect/experimental` `RateLimiter` (`fixedWindow`/`tokenBucket` over `RateLimiterStore`, the third first-class `@effect/experimental` consumer), the `TenantQuota` cap vocabulary, and the backpressure `Stream` regulation, ABOVE the per-key `@effect/workflow` `DurableRateLimiter` (cited as the boundary). Sequenced after the `messaging/entity` ROWs settled the shared page; non-redundant aggregate fairness, so authored rather than dropped.

[EMBEDDING_LIFECYCLE]: realized — `hybrid-search/embedding-lifecycle#EMBEDDING_LIFECYCLE` owns the `EmbeddingProfile` migration-row table, the `BatchEmbed` one-entrypoint embed over `EmbeddingModel.makeDataLoader` (`{ window, maxBatchSize }`) + `EmbeddingModel.make` per-call cache, and the durable re-embed `runtime-backplane` job. Split recorded: admits ONLY the profile-MIGRATION + batch-embed ORCHESTRATION; `fused-rank#FUSED_RANK` keeps the staleness column, the single re-embed `WorkQueue` row, and the `EmbeddingProfile` shape — the lifecycle FEEDS the vector arm. Profiles only the `pgvector` column, never the gated BM25 index (UNBLOCKED), so authored rather than down-scoped to drop.

[RUNTIME_SECRET_GOVERNANCE]: realized — `secrets/secret-store#SECRET_STORE` owns the one `SecretStore` `Effect.Service` over the `ConfigProvider` boundary resolving every runtime key, the `SecretRef` `Data.TaggedEnum` over the provider axis (`EscEnv` via `@pulumi/esc-sdk` `openAndReadEnvironment`/`readOpenEnvironment`, `Doppler` via `sdk.secrets.get`/`download` + `DynamicSecretsService.issueLease`/`revokeLease`, `Static` via `Config.redacted`) folded by `Match.tagsExhaustive` with the `AwsSecretsManager` arm authored-but-gated on `@aws-sdk/client-secrets-manager` admission, the `LeasedSecret` `SubscriptionRef`-backed TTL cache whose `changes` `Stream` is the rotation-invalidation edge (no restart), the `LeasePolicy` per-provider TTL/rotation vocabulary, the `SecretFault` policy table, and the `AuditReceipt` `Model.Class` every resolution emits under the `persistence/tenancy#TENANCY` GUC. Deploy-time and runtime resolution meet at the one `ConfigProvider` boundary the `Static` arm reads; the TTL-expiry sweep is a `runtime-backplane` singleton that re-resolves rotating rows and `revokeLease`s expired Doppler dynamic leases. Crosses no .NET wire.

[BACKEND_RUNTIME_SLO]: realized — `observability/slo-budget#SLO_BUDGET` owns the closed `Objective` `as const satisfies Record` delegate-row vocabulary (`latency`/`availability`/`saturation`/`freshness`), the multi-window multi-burn-rate fast-AND-slow `BurnRate` fold over the `@effect/sql-pg` `SignalSeries`, the `AlertRoute` `Data.TaggedEnum` enqueued through the `eventing/transactional-outbox` `DeliverySink`, the `ClusterMetricsReader` over the five `@effect/cluster` `ClusterMetrics` gauges as the `saturation` signal, the shard-pinned cron evaluation, and the decode-only `HealthWire`/`TraceContextWire` carriers off the C#-minted `ONE_HEALTH_DEGRADATION_WIRE`/`ONE_DISTRIBUTED_TRACE` frames. PACKAGE CORRECTION: the reader is `@opentelemetry/sdk-metrics` `MeterProvider`/`MetricProducer` + `@opentelemetry/sdk-trace-node` `NodeTracerProvider`, NOT `@effect/opentelemetry` — `@effect/opentelemetry` stays the `[3]-[CROSS_CUTTING]` exporter edge with no reader catalogue; the `MetricProducer`-to-`ClusterMetrics` bridge is DESIGN-PIN'd against `MeterProviderOptions.metricProducers` pending a branch `.api/effect-opentelemetry.md`.

[MCP_AGENT_TRANSPORT] — Authored `agent/mcp-transport`: the node-tier consumer that decodes the C# `Rasm.AppHost/agent/mcp-projection#TS_PROJECTION` single mint — `catalog` lifts each `McpToolWire` descriptor into one `@effect/ai` `Tool.make` with a host-validated `payload` passthrough and `tool.annotate(Tool.Readonly|Destructive|Idempotent, …)` effect hints, collected through `Toolkit.make`; `progress`/`resume` fold the `ProgressNotificationWire` SSE stream through `@effect/experimental` `Sse.makeParser` and reattach by the `Sse.Retry.lastEventId` cursor; `costPreview` decodes `CostPreviewWire` and gates on `covered`; `result` reconstructs through the host `ReceiptEnvelopeWire<ToolResultWire>`. Five decode-only `Schema` carriers, never a second mint; a reconstructed frame union from the raw SDK notification is the named defect. The lifted `Toolkit` enters the `agent/agent-runtime#AGENT_RUNTIME` `DurableAgent`, distinct from the agent's own `AgentToolkit`.

[EXPERIMENTAL_RETIRE_OR_KEEP] — RE-OPENED then re-resolved keep-with-three-consumers: the prior disposition kept `@effect/experimental` first-class "solely on the `Sse` decode" with `Reactivity`/`Machine`/`PersistedCache` dismissed as transitive — wrong, because `Reactivity` is the named substrate the new `REACTIVE_QUERY_INVALIDATION` idea exploits directly and the `@effect/experimental` rate-limiting module backs `QUOTA_GOVERNOR`. The package now carries THREE first-class direct consumers: the `agent/mcp-transport` `Sse` decode, the reactive-query `Reactivity` invalidation, and the quota rate-limiting module. The `EventJournal`/`PersistedQueue`/`Persistence` substrate stays transitive against the durable tier.

[AUTHN_VERIFIER] — Authored `authn/verifier`: the one `Authn` `Effect.Service` over the closed `AuthCommand` request `Data.TaggedEnum` (TOTP/WebAuthn-registration/WebAuthn-authentication/OAuth/API-key) folded by one `$match`, the server-side credential verifier the `platform/identity-session/auth-session` browser ceremony forwards to over the `interchange` `CommandGateway`. TOTP rides `otplib`, WebAuthn rides the four `@simplewebauthn/server` primitives, the replay floor advances the `timeStep`/`counter`, the `CredentialKind` vocabulary owns the per-credential policy, and the one `AuthFault` projects through a policy table. Closed the cross-folder seam the `platform` boundary named (a browser-side verifier is the named defect) and gave the registered `otplib`/`@simplewebauthn/server` packages their consuming page.

[OTPLIB_CROSS_CUTTING_RECONCILE] — Resolved: `otplib` is named in both `platform` and `services` `[CROSS_CUTTING]` (browser ceremony vs server `authn/verifier` verification), so it is genuinely branch-cross-cutting and stays in `[CROSS_CUTTING]` against its branch `.api` catalogue; the README line now names the `authn/verifier#VERIFIER` owner.

[TRANSACTIONAL_OUTBOX] — Authored `eventing/transactional-outbox`: the same-txn `Outbox` `Model.Class`, the `FOR UPDATE SKIP LOCKED` relay with `LISTEN`/`NOTIFY` wake, the `DeliverySink` tagged axis; logical-replication recorded as a RESEARCH-gated push variant, confirmed correctly gated — the `effect-sql-pg.md` catalogue verifies `listen`/`notify` are the ONLY push surfaces, so no logical-replication slot/decode surface exists in `@effect/sql-pg` to author against.

[ENTITY_ACTOR_MODEL] — Authored `messaging/entity`: the `Entity.fromRpcGroup` actor with `ClusterSchema.Persisted` rpcs, `getShardId`/`getShardGroup` single-writer-per-key placement, the `EntityProxy` projection, and the collaborative-session `Apply` decoding the C# CRDT op-log wire off `interchange`.

[DURABLE_AGENT] — Authored the `agent/` subpath closure (`agent-runtime` + `session-actors`). Correction: no stock `DurableAgent` primitive exists in `@effect/ai`/`@effect/workflow`, so the page COMPOSES the agent from `Activity` + `Toolkit`/`Chat.layerPersisted` on `ClusterEngine`, journaling `AgentJournal`, over the one `AiProvider` axis with runtime provider selection.

[RRF_BM25_FUSION] — Upgraded `hybrid-search/fused-rank` to Reciprocal Rank Fusion over BM25 (four per-signal `RANK()` CTEs folded by `SUM(weight / (rrfK + rank))`), dropping the per-signal normalization; the BM25 extension (`pg_search`/Tiger) recorded as a RESEARCH item gated on the provisioned Postgres image.

[CONTINUOUS_DRIFT_SWEEP] — Wired the `DriftSweep` ROW on `provisioning/drift`: a `ScheduledWork.cron` shard-pinned sweep resolving the ESC policy-pack environment, re-running the fold, and emitting only the primitive `StackDriftSummary` to `ObservabilityStack.emit`, deploy-time-only inside the `./provisioning` subpath. Paired with the P1 `pulumi-pulumi.md` automation event-stream catalogue extraction that cleared the drift RESEARCH flag.
