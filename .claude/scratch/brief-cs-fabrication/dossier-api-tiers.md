# [DOSSIER] Rasm.Fabrication — lane: api-tiers

Scope read FULLY: both catalog tiers (`libs/csharp/.api/*.md` 31 files scanned, hashing pair read in full; `libs/csharp/Rasm.Fabrication/.api/*.md` all 14 read in full), `Directory.Packages.props`, `Rasm.Fabrication.csproj`, README, ARCHITECTURE, plus pages `Process/family.md`, `Posting/program.md`, `Toolpath/slicing.md` (full) and grep-verified consumption across all 17 planning pages. Stance: hostile. Core finding: the `.api` tier is world-class and FAR ahead of the page tier — 4 of 14 admitted packages carry rich 120-161 line catalogs with ZERO page consumers, and the single richest arc-substrate owner is built but unwired.

## [00] ROSTER RECONCILIATION (manifest ↔ csproj ↔ .api ↔ page-consumer)

csproj `Rasm.Fabrication.csproj` references 14 packages; the `Fabrication` ItemGroup in `Directory.Packages.props:81-88` pins only 6 (DSTV.Net, MTConnect.NET-Common, OcctNet.Wrapper, PicoGK, RectpackSharp, Robots) — the other 8 draw from cross-group pins (ACadSharp→BIM 122, CavalierContours/Clipper2/geometry3Sharp/SharpVoronoiLib→Kernel Geometry 70-76, System.IO.Hashing→Foundational 31, UnitsNet→Shared Primitives 43, RectangleBinPack.CSharp→Materials 167). Central management is global, so this is legal.

| package | .api LOC | page consumer (exact-symbol grep) | MINED? |
| :------ | :------- | :-------------------------------- | :----- |
| Clipper2 | 253 | clipper.md (PolygonAlgebra), nfp/projection substrate | YES (gold) |
| CavalierContours | 164 | clipper.md ONLY (ArcAlgebra owner, lines 169-261) | **BUILT-BUT-UNWIRED** (0 downstream) |
| ACadSharp | 120 | import.md | YES |
| geometry3Sharp | 106 | clipper.md, program.md (BiArcFit2) | YES |
| Robots | 105 | kinematics.md, motion.md, program.md, owner/family/faults | YES |
| RectangleBinPack.CSharp | 138 | stock.md (StockNest.Pack) | YES |
| RectpackSharp | 91 | nfp.md (rect-fastpath) | YES — but redundant w/ RBP (see V2) |
| System.IO.Hashing | 94 | nfp.md, magazine.md | YES (overlay defective, V6) |
| UnitsNet | 84 | physics.md, magazine.md, slicing.md | YES (overlay exemplary) |
| MTConnect.NET-Common | 126 | magazine.md | YES |
| **PicoGK** | 161 | **NONE (0)** | **UNMINED** |
| **OcctNet.Wrapper** | 143 | **NONE (0)** | **UNMINED** |
| **DSTV.Net** | 143 | **NONE (0)** | **UNMINED** |
| **SharpVoronoiLib** | 121 | **NONE (0)** | **UNMINED** |

Grep proof (word-boundary, pages only): PicoGK/Voxels/IImplicit/Lattice/CliIo/oVectorize = 0; OcctNet/OcctShape/ImportStep/STEP/IGES/B-rep = 0; DSTV/DstvReader/nc1 = 0; SharpVoronoiLib/VoronoiPlane/VoronoiSite = 0. (Earlier `import.md` Voronoi hit was a false match on the generic word "Tessellate"/"Relax".)

## [01] PER-CATALOG VERDICTS (1-10)

- **api-clipper2.md — 9/10.** Gold standard. Explicit STACKING law (245-246): `ReuseableDataContainer64` scanbeam-reuse composes with the `NoFitPolygon.PairKey` memo on disjoint axes; buggy `Triangulate` surface routed OUT (247). Cross-refs owning pages. Charter correct as-is.
- **api-cavaliercontours.md — 9/10 (catalog) / defect is downstream.** Rich INTEGRATION_STACK, retirement law for the Clipper2-offset-then-`g3.BiArcFit2` rail (153-154). Catalog is right; the DEFECT is that its claimed consumers (motion/skeleton/program) never call it (V1). Charter: keep; enforce the retirement in the consuming pages.
- **api-picogk.md — 9/10 (catalog) / 2/10 (realization).** Full ALC-firebreak topology, mutate-vs-copy discipline, resin/powder CLI + lattice + implicit-infill STACKING (151-155). ZERO page consumers (V3). Charter: this is the AM lane owner; needs an owning page-set.
- **api-occtnet-wrapper.md — 8/10 (catalog) / 1/10 (realization).** Records the load-bearing SCOPE_LIMIT (120-124): OCCT ships `libTKHLR` (hidden-line) + `libTKXCAF` (assembly/PMI) native toolkits but they are managed-UNBOUND; v0.1.1, single-shape import only, no fillet/chamfer/loft. ZERO page consumers (V4). Charter: the 3D-solid/B-rep ingress owner — needs an ingress page + a demand row on the thin wrapper.
- **api-dstv-net.md — 7/10.** Rich NC1 record tree. DEFECTS: line 3/126 "band-2500 FabricationFault" (STALE→2700, V5); line 132/143 "DXF write is the `Rasm.AppUi`/`netDxf` leg" / "use `ACadSharp`/`netDxf`" (STALE, netDxf removed, V5). ZERO page consumers (V4). Charter: steel NC1 read owner + emit-mirror; needs an ingress/emit row.
- **api-robots.md — 8/10.** Correctly supersedes hand-rolled DH/IK; per-manufacturer robot post family (RAPID/KRL/URScript/VAL3/DRL). DEFECT: line 5/94 "band-2500 FabricationFault" (STALE→2700, V5). Note: its `Program` runs a look-ahead jerk/accel motion planner that overlaps Posting/program `Lookahead` (concern-adjacency, V8).
- **api-mtconnect-net-common.md — 8/10.** ISO-13399 tool-asset model, richly mined into magazine.md. Records DOMAIN GAP (107): no feeds/speeds DATASET — the model is a container, no machining data (G7).
- **api-geometry3sharp.md — 6/10.** BiArcFit2 rail is well-mined. DEFECT: line 106 reject clause "the dropped CavalierContours arc-offset engine … one-Clipper2 law … single Clipper2 chord owner" — STALE, self-contradicting; CavalierContours is fully admitted (own 164-line catalog + csproj + clipper.md ArcAlgebra). Isolated to this one line but a hard cross-catalog contradiction (V1).
- **api-acadsharp.md — 9/10.** Exemplary READ catalog (Failsafe rail, bulge/NURBS samplers, block explode). DEFECT: line 120 reject "Rhino owns the host-bound native write" — STALE; per census the DXF+DWG WRITE leg is ACadSharp, AppUi-owned (ties ARCHITECTURE.md:50 refine). Fabrication-read-only stance itself is correct.
- **api-sharpvoronoilib.md — 9/10 (catalog) / 1/10 (realization).** Rich; correctly firewalls straight-skeleton to the hand-rolled `Toolpath/skeleton`. ZERO page consumers (V4). Charter: the CAM Voronoi tessellation primitive (spiral-pocket seeds, region partition) — needs a motion/skeleton consumer row.
- **api-rectanglebinpack-csharp.md — 8/10.** 4 packers + SingleBinPack mass-cut + `FreeRectangles` remnant geometry; well-mined into stock.md. The line-3 "DISJOINT from RectpackSharp, neither re-packs the other" is the contested split (V2).
- **api-rectpacksharp.md — 6/10.** Correct as a catalog, but the package is redundant: one-shot `FindBest` MaxRects/Guillotine, a strict subset of RBP's `MaxRectsBinPack` + a trivial multi-heuristic fold; "uint vs int" and "one-shot vs incremental" are impl-detail, not disjoint concern (V2).
- **api-hashing.md — 4/10 (DEFECTIVE).** Full re-doc of the 7-type surface AND asserts a FALSE law: line 3/88 "the same package is copied … under the no-shared-C#-tier law (C# has no shared tier)" — refuted by the existing `libs/csharp/.api/api-hashing.md` and by README.md:70 ("shared API evidence lives in `libs/csharp/.api/`"). Should be a THIN nesting-identity overlay deferring surface docs to the shared tier (V6).
- **api-unitsnet.md — 9/10 (EXEMPLAR overlay).** Thin, domain-scoped: only Speed/Length/RotationalSpeed/Pressure for the cut-parameter boundary, references owning-page types (`RemovalParameter.Admit`, `Cam`, `StraightSkeleton`). This is the pattern the hashing overlay should copy. (Minor scope note: production specs will need Force/Power/Temperature/Angle — G7.)

## [02] CROSS-CUTTING

**Unmined capability (COVERAGE naivety, catalog-anchored).** Four fully-catalogued packages, zero page realization: PicoGK (implicit/SDF/voxel AM — resin/powder CLI `oVectorize`→`CliIo`, `Lattice` supports, `IImplicit`→`Voxels` TPMS/gyroid infill), OcctNet.Wrapper (STEP/IGES/STL→`OcctShape`→`OcctMesh` B-rep ingress), DSTV.Net (NC1 steel read + emit-mirror), SharpVoronoiLib (`VoronoiPlane` region partition). Each is an INTEGRATION MANDATE per brief law, not a removal candidate.

**Unwired seam (declared-but-unwired).** `ArcAlgebra`/CavalierContours is a complete arc-native offset/Boolean owner in `clipper.md:169-261`, but its declared consumers never call it: `program.md` Kerf (`:174-180`) and LeadArc (`:215-223`) still use `PolygonAlgebra.Offset` (Clipper2) + `g3.BiArcFit2`; `motion.md`/`skeleton.md` name it 0×. The catalog (154) and `program.md:20` both state the arc-native substrate "retires the post-hoc Clipper2-offset-then-refit" and is "never re-rolled here" — yet the rail it retires is exactly what the pages still run. Retirement unrealized.

**Duplication / parallel rails.** (a) TWO rectangle packers: RectpackSharp (`Directory.Packages.props:86`) + RectangleBinPack.CSharp (`:167`), both MaxRects/Guillotine AABB engines (V2). (b) Two dead-carrier GCommand rows share codes: `program.md:46,54` `Dwell`/`Pierce` both "G4"; `:43,58` `Feed`/`Extrude` both "G1" — pierce is a dwell-with-flag, collapsible. (c) Look-ahead feedrate planning exists twice: Robots `Program` (`api-robots.md:72`) and Posting `Lookahead` (`program.md:290`) — different domains (robot cell vs CNC) but the same jerk/accel concern; watch for a shared motion-dynamics owner (V8).

**Hardcoding-vs-generator.** (a) `program.md:183` `radialForce = 0.0012 * policy.RemovalRate` — magic specific-cutting-force coefficient; production wants a material/tool-derived Kc from `Process/physics`, not a literal 0.0012. (b) family.md rosters are correctly DATA (`[SmartEnum<string>]` rows), but the PostDialect BEHAVIOR model is 3 columns (`Rs274`/`Comment`/`LineNumbers`, `family.md:80-96`) — insufficient to generate controller-family variation; `CutProgram.Emit()` (`program.md:122-136`) varies output only by `LineNumbers`+FeedMode, so the "one Emit arm per dialect" growth claim (`:19`) is structurally unrealized (no per-dialect Switch, no canned cycles, no macros) (V7).

**Concern mixing / folder architecture.** Current 5-folder map (`ARCHITECTURE.md:9-34`) is by-mechanism grab-bag, not by growth axis: `Process/` mixes owner+vocab+physics+magazine(tooling)+faults; `Polygon/` mixes Clipper2 algebra + CAD ingress; `Posting/` mixes HLR projection (drafting) + G-code emit (NC); `Nesting/` mixes nfp/stock + workholding (fixturing). No loose one-file folders (each 2-6 pages), but the scope's new concerns have NO home: resin/powder AM (PicoGK), STEP/IGES ingress (OcctNet), steel NC1 (DSTV), post-dialect breadth + canned cycles/macros, feeds/speeds machining data, NC verification, and production specs (tolerances/GD&T/surface-finish/setup-sheets/travelers) (V9).

**Stale-reference census (FAULT_REBAND_2700 + netDxf ripples never propagated).** `faults.md` is CORRECT (FaultBand.Fabrication=2700, offsets +1..+10, `:52-61`). Ripple NOT propagated to: `ARCHITECTURE.md:16` ("band-2500 composing kernel band-2400"), `api-dstv-net.md:3,126` ("band-2500"), `api-robots.md:5,94` ("band-2500 … composing the kernel band-2400"). netDxf STALE (removed repo-wide; WRITE leg = ACadSharp two-format, AppUi): `README.md:33,64`, `ARCHITECTURE.md:51`, `api-dstv-net.md:132,143`, `api-acadsharp.md:120`. `README.md:65` rejection ("MaxRect/BinPack.NET superseded by RectpackSharp") ignores that the richer RectangleBinPack.CSharp is also admitted (V2). Census point (4) target: durable receipts decode 2701-2710, never 25xx.

**Seam re-point pressure (from brief census, present in ARCHITECTURE).** `ARCHITECTURE.md:52-53` Posting/program→`Rasm.Persistence/Schema` and Nesting/nfp→`Rasm.Persistence/Schema` must re-point to post-Persistence-campaign owners (blobstore content rows + cache artifact index). `:50` Nesting/stock→`Rasm.Compute` NestYield.WasteAreaMm2 rollup is the recorded Compute counterpart (present, correct).

## [03] DOMAIN GAPS (named, NOT researched — roster candidates)

1. **G-code post-processor / canned-cycle / macro framework.** No admitted package; `program.md` hand-rolls AST+Emit with a 3-column dialect model. Scope demands Fanuc macro-B, Siemens 840D cycles, Heidenhain TNC Q-params, Okuma OSP, Mazak, LinuxCNC, GRBL, Marlin/RepRap + canned cycles (G81/G83/G73/G76) + subroutines (M98/M99). No mature OSS C# post library exists → author-kernel stays, but the AST must grow canned-cycle/macro/subroutine/variable nodes and the Emit must become a per-dialect Switch.
2. **Feeds & speeds / machining-data source.** Recorded gap (`api-mtconnect:107`, README:58). No free-full feeds/speeds dataset on NuGet; Machinery's-Handbook data proprietary. Stays hand-rolled table; gap real for chip-load/SFM/chip-thinning.
3. **NC verification / material-removal simulation.** No admitted package. STRONG integration opportunity: **PicoGK voxel Boolean** (`stock Voxels − swept-tool Voxels`) is exactly the subtractive material-removal-sim engine — turns an unmined AM package into an NC-verify owner beside `Toolpath/guard`.
4. **STEP assembly/PMI/XCAF.** OcctNet.Wrapper ships `libTKXCAF` unbound, single-shape only, v0.1.1 — production assembly-level fabrication + PMI-driven tolerances have no reader (waterfall demand row onto the thin wrapper, or a richer OCCT binding).
5. **Production specs (tolerances / GD&T / surface finish / SPC / setup sheets / shop travelers).** Entire domain absent — no page, no package. Scope-mandated. Quantities covered by UnitsNet; the models/owners are missing.
6. **True-shape nesting engine.** Hand-rolled (Clipper2 Minkowski NFP + GeneticSharp GA [admitted, Compute] + rect fast-path). Reference OSS (SVGnest/Deepnest) is JS/GPL; no C# categorical-best. Current composition defensible.
7. **Straight-skeleton / medial-axis library.** Hand-rolled `Toolpath/skeleton`. CGAL is GPL/C++ (license-gated out); no clean OSS C#. Author-kernel defensible; SharpVoronoiLib (unmined) can feed region seeds.

## [04] VERDICT CANDIDATES (strongest, campaign-defining)

**V1 — ArcAlgebra/CavalierContours is built-but-unwired; realize the declared arc-rail retirement.** clipper.md ships a complete `ArcAlgebra` owner (`:169-261`) but motion/skeleton/program never call it — they still run Clipper2-offset + `g3.BiArcFit2` (`program.md:174-180,215-223`), the exact rail the catalog (`api-cavaliercontours:154`) and `program.md:20` claim it retires. Wire the kerf/lead-arc + morphed-spiral adaptive-clearing rails through `ArcAlgebra.ArcOffset`/`ShapeOffset`; keep `g3.BiArcFit2` only for genuinely line-sourced (OcctMesh-section) paths. Evidence: 0 CavalierContours calls outside clipper.md; program.md:20 boundary-note vs program.md:174 actual Clipper2 call. Fix `api-geometry3sharp.md:106` stale "dropped CavalierContours / one-Clipper2 law" as the paired ripple.

**V2 — Collapse the two rectangle packers onto RectangleBinPack.CSharp; flag RectpackSharp REMOVE (SEAL-CHALLENGE the "disjoint" split).** Both are MaxRects/Guillotine AABB engines. RectangleBinPack.CSharp is strictly richer (MaxRects/Skyline/Guillotine/Shelf + SingleBinPack mass-cut + `FreeRectangles` remnant geometry + panel-saw constraint, `api-rectanglebinpack:27-32`); RectpackSharp adds only a `FindBest` multi-heuristic sweep (a trivial fold over `MaxRectsBinPack.Insert` under each heuristic) + a uint domain. The corpus's "one-shot fast-path vs yield engine, neither re-packs the other" (`api-rbp:3`, README:40) is a thin impl-detail rationalization; README:65 even rejects MaxRect/BinPack.NET "superseded by RectpackSharp" while ignoring that RBP subsumes RectpackSharp. Re-home the nfp `rect-fastpath` arm to a `MaxRectsBinPack` heuristic-sweep fold. Proven-redundancy REMOVE (concern lands on a stronger admitted owner, named).

**V3 — PicoGK is an integration MANDATE: stand up the implicit/voxel AM lane + correct slicing.md's naive gyroid.** 161-line catalog, 0 page consumers. slicing.md (`:3,20`) claims "the ONE additive-slicing owner" but is planar plane-vs-triangle FFF only, and lists gyroid/TPMS as a future 2D-hatch `InfillPattern` growth row (`:19`) — precisely the concern `api-picogk:152` says "the rectilinear/honeycomb quartet cannot express" and PicoGK's `IImplicit`→`Voxels` owns in true 3D. Realize an AM page-set: resin/powder vector CLI (`oVectorize`→`CliIo`/`Vdb2Cli`), conformal lattice supports (`Lattice.AddBeam`), implicit/TPMS/gyroid infill; correct slicing.md's gyroid row to route through PicoGK; add the NC-verify material-removal-sim use (`stock − swept-tool` voxel Boolean) beside Toolpath/guard.

**V4 — Realize the three other zero-consumer catalogs (OcctNet, DSTV.Net, SharpVoronoiLib) as named rows on owning pages.** OcctNet: a STEP/IGES/STL B-rep ingress page (or an `import.md` arm) → `OcctShape.ImportStep`→`Triangulate`→`OcctMesh` feeding `Toolpath/slicing` section + `Posting/projection` HLR (the mesh seam the catalog names at `api-occtnet:133`). DSTV.Net: an NC1 steel ingress row in import + an emit-mirror in the posting owner (the catalog says emission is the posting owner's `PostDialect` writer, `api-dstv:135`). SharpVoronoiLib: a CAM tessellation row in motion/skeleton (spiral-pocket seed centroids, region partition). Absence-of-consumers is never a REMOVE reason; each is an integration row.

**V5 — Propagate FAULT_REBAND_2700 to the .api tier + ARCHITECTURE; excise all netDxf references.** faults.md landed 2700 but the ripple stalled: fix band-2500 in `ARCHITECTURE.md:16`, `api-dstv-net.md:3,126`, `api-robots.md:5,94` → FaultBand.Fabrication (2700, offsets 2701-2710). Excise netDxf in `README.md:33,64`, `ARCHITECTURE.md:51`, `api-dstv-net.md:132,143`, `api-acadsharp.md:120` → the WRITE leg is the ACadSharp two-format (DXF+DWG) AppUi leg (ties ARCHITECTURE.md:50 refine). Durable-receipt decoders and any cross-process reader filtering 25xx re-point to 2701-2710.

**V6 — Slim the hashing overlay to the unitsnet pattern; kill the false "no shared tier" law.** `api-hashing.md:3,88` asserts "C# has no shared tier" and full-re-docs the 7-type surface — refuted by the extant `libs/csharp/.api/api-hashing.md` and README.md:70. Make it a THIN nesting-identity overlay (Remnant/Stock XxHash128 content-address, `NoFitPolygon.PairKey` XxHash3 memo, the seed-partition law) deferring the type/entrypoint surface to the shared substrate — exactly how `api-unitsnet.md` (Fabrication) scopes to the 4 cut-parameter quantities. Removes a load-bearing false statement duplicated into Rasm.Compute's copy.

**V7 — Deepen PostDialect from a 3-column enum into a canned-cycle/macro/modal generator (scope: post-processor breadth).** family.md PostDialect (`:80-96`) carries `Rs274`/`Comment`/`LineNumbers` only; `CutProgram.Emit()` (`program.md:122-136`) varies by `LineNumbers`+FeedMode — cannot express canned cycles (G81/G83/G76), macro/subroutine syntax (Fanuc macro-B, Heidenhain Q-params, Siemens R-params, M98/M99), arc-format (IJK vs R), work-offsets (G54-G59), or plane selection, and lacks Siemens/Heidenhain/Okuma rows entirely. Grow GCommand into canned-cycle + macro/subroutine + variable nodes and make Emit a real per-dialect Switch. Roster is seed DATA; the APPROACH gap is the thin behavior model + flat renderer.

**V8 — Widen CutStrategy for the multi-axis + rest-machining scope; unify the two look-ahead planners.** family.md CutStrategy's 8 rows (`:34-43`) are all 2.5D/3-axis (boundary-pass/pocket-clear/peck/adaptive/radial-sweep/plunge-dwell/helical/layer-walk) despite Machine rows for mill-5axis/waterjet-5axis/robot-6/7axis — no swarf/flowline/multiaxis-contour/3+2-positional, no rest-machining/rest-roughing (both scope-named), and additive is a single `layer-walk`. Add the multi-axis + rest rows on the settled `(RemovalModality, CutStrategy)` cross-product. Separately, reconcile the jerk/accel look-ahead concern owned twice: Robots `Program` planner (`api-robots:72`) vs Posting `Lookahead` (`program.md:290`) — keep per-machine-class but name the shared motion-dynamics law so they don't drift.

**V9 — Re-partition the 5-folder map into a growth structure for the production scope (folder-architecture verdict).** Current by-mechanism grab-bag (Process=owner+vocab+physics+magazine+faults; Polygon=algebra+ingress; Posting=projection+emit; Nesting=nfp/stock+workholding) has no home for the scope's new concerns. Partition by fabrication-pipeline domain: Ingress (CAD/import + STEP-IGES/OcctNet + DSTV steel), Geometry (Clipper2 line + CavalierContours arc + Voronoi + skeleton), Process (owner+family+physics/machining-data+faults), Tooling (magazine+tool-data+feeds/speeds+machine-profiles), Toolpath/CAM (motion+guard+kinematics+rest+multiaxis), Additive (FFF slicing + PicoGK resin/powder/lattice/implicit), Nesting (nfp+stock), Setup (workholding+fixturing+setup-sheets+travelers), Posting (G-code AST+post-dialects+DSTV-emit), Verification (NC-verify material-removal-sim+probing+projection). Every scope concern gets a growth axis.

**V10 — Production-specs domain is entirely absent (COVERAGE gap).** Scope demands tolerances, surface finish, process capability (SPC), setup sheets, shop travelers — no page and no owner exists. Quantities are covered by UnitsNet (extend the overlay to Force/Power/Temperature/Angle); the tolerance/GD&T/surface-finish/SPC/setup-sheet/traveler MODELS must be authored as a new owner (folds into V9's Setup + a new Specs owner). This is the single largest missing sub-domain relative to "PRODUCTION-GRADE."
