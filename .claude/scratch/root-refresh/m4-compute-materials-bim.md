# [M4] COMPUTE BRIEF vs REALITY — MATERIALS/BIM INTEGRATION EVIDENCE

Census scope: `RASM-CS-COMPUTE-BRIEF.md` (229 lines) against `libs/csharp/Rasm.Compute/.planning/**` disk state; `Rasm.Materials`/`Rasm.Bim` corpora as integration evidence; engine executability of `[EXECUTION]`. Mid-edit flag: a redteam concurrently edited `Solver/contract.md`, `Solver/optimizer.md`, `Tensor/dispatch.md`, `Tensor/vocabulary.md` during this census — those files read internally consistent (no false drift detected); their anchors below carry a `[MID-EDIT]` tag where cited.

## [01]-[CITATION_CENSUS] — brief anchors vs disk

Counts: 21 CONFIRMED exact · 24 DRIFTED (content present, line moved) · 3 ABSENT/STALE premises · 3 stale LOC figures · 1 phantom member name propagated through 4 files.

### Confirmed exact (no motion needed)

| Anchor | Fact |
|---|---|
| `assessment.md:18,107,114` | seam-vocabulary law; ResultBlob sink pending; `Option<UInt128> ResultBlob` |
| `channels.md:9,92` + 7 index owners + `WireFault` 4520–4532 (decl 218–230) | seventeen-code prose / fifteen-row rail; CHANGEFEED cite; sub-band real |
| `receipts.md:416` | `AssessmentWire` has NO failure column — exact |
| `admission.md:173` | 2200-band custody map; 2217/2218/2219 analysis lane — exact |
| `contract.md:65-66,297-304,301,104,733,35,520-538` `[MID-EDIT]` | nu=1/3 collapse; no-Poisson + culture-string key at `:304`; `DenseEvd`; LOBPCG self-flag; daylight-radiosity row; lumped-mass field REAL (comment: "the prior all-ones fake is deleted") |
| `discretization.md:585,617,913-921,962,373,3,18` | float predicates + local `record MeshSpace` all as cited |
| `expression.md:5,22,101` / `units.md:81-96,129,209-211` / `estimator.md:574` | FParsec transitive law; bare-Guid E14; autodiff-tape-absent fall |
| `providers.md:12,173` / `capacity.md:144,253-254,262,380` (Materials) | Cpu+CoreMl only; EC2 shear defer byte-for-byte, Compute named 3x (`:13,254,503`) |
| md5 census | 9 identical cross-tier pairs + 8 divergent pairs — brief counts EXACT |
| Kernel truths | namespaces folder-true (`Rasm.Domain`/`Rasm.Numerics`/`Rasm.Meshing`); `ContentHash.Of` (`identity.md:30`), `Predicate.Orient2D/3D/InCircle/InSphere` (`predicates.md:17-18,162`), `MeshSpace [BoundaryAdapter]` (`mesh.md:13`) all landed |

### Drifted (E2 known-drift censused + the rest)

| Brief anchor | Real | Note |
|---|---|---|
| `structural.md:285,389` | `:302,:406` | `<bfe-solve-break>` / `<fealite-singular>` +17 |
| `structural.md:461,466,621-623,636,197-201` | `:534,:619,:638-644,:648-654/751,:213-218` | aisi row / effective-width defer / AISI cells / ConcreteVc / FrameBackend |
| `energy.md:279,299,318` | arms at `:336,:362,:388` + THREE uncounted cloud arms `:613,:630,:638` | brief counts 3 stringly arms; disk has 6 |
| `energy.md:30,354` | `:31,:424` | WeatherRef; eplusout.sql growth prose |
| `assessment.md:245-247,248-250,278-283` | `:254-258,:260-261,:287-292` | behaviors CONFIRMED (CacheFirst noop, no Failed write, GeometrySource port); lines +9. CacheFirst is `nodeId`-gated, not literally "blanket ANY hit" — verdict substance holds |
| Compute `ARCHITECTURE.md:99,100,111,115,116,119,121,123,124,126` | uniform +2/+3: `:101,:102,:113,:117,:118,:121,:122,:126,:127,:128-129` | `:108-109` additionally mispointed (real Query/cache rows at `:110-111`) |
| Kernel `Rasm/ARCHITECTURE.md:78,82-83,86,89,93` | `:89,:93-94,(85=seams start),:105-106,:93` | +11..+16 — brief predates a kernel codemap/seams expansion |
| `contract.md:1024` `[MID-EDIT]` | `:1038` | 2-parameter `ElasticPrimal` |
| `estimator.md:582,604,621` | `:582` IS ARMA; `:604` is Holt; `:621` a separate recurrence | brief misattributes all three to "ARMA-MLE" |
| LOC: channels 778, codecs 832, extension 66 | 782 / 846 / 79 | narration stale |

### Absent / dead premises

1. `ARCH:120` reverse clause "Processing/solver consumes the Compute LM/constraint Solver rail" — ABSENT from disk. The Geometry campaign's `[V14]` kill landed first; `[V14]`'s "verify dead-or-kill" is ALREADY DISCHARGED. The brief row should flip to verified-dead.
2. `MechanicalOf` is a PHANTOM MEMBER: zero hits in Element and Materials trees; the real seam read is `graph.PropertiesOf(member).Mechanical` (`Rasm.Element/.planning/Graph/element.md:510`; the `Mechanical` case with Poisson exists — `Element Composition/material.md:255-256` `OfMechanical(..., poissons, ...)`). `MechanicalOf` appears ONLY Compute-side: the brief (`[V6]`, `ARCH:123` row, `[03]` table), Compute `ARCHITECTURE.md:126`, `contract.md`, `structural.md` — the brief's own `[SEAM_AND_RAIL_LAW]` "a member no instrument verifies is a phantom" convicts its own flagship repair spelling. `[V6]`(a) substance stands; the member name must retarget.
3. `api-languageext.md` "absent both tiers / AUTHOR" — STALE: it now EXISTS at the substrate tier (an earlier track campaign landed it per the brief's own track-shared law). `[ROSTER_RECONCILIATION]` + `[V15]` rows flip from AUTHOR to VERIFY.

### Roster reality

- MathNet.Numerics central pin is `6.0.0-beta2` HELD — the Geometry "5.0.0 downgrade ruling" the brief says leg-2 verifies against has NOT executed. Leg-2's "verify against the downgraded surface" is currently unexecutable as written; the row needs a gate ("when the downgrade lands") or the ruling re-confirmed.
- csproj direct rows (FParsec, BFE pair, FEALiTE pair, DirectML/Gpu, 3DTiles) and PROPS transitive floors confirmed present; md5 dedupe counts exact (above).

### What the finalized kernel changes per verdict — compose, never re-derive

| Verdict | Kernel fact now on disk | Compute page that composes |
|---|---|---|
| `[V7]` | Kernel ledger `:106` ALREADY declares `Numerics/Predicates.cs <- Compute/Solver/discretization # [SHAPE]: CDTet exact gates — the public Predicate.Orient3D/InSphere verdicts` — the waterfalled `[V14]` export is DECLARED both ends | `discretization.md` routes sign decisions through `Predicate`; the `RobustGeometry.NET` contingency is effectively dead — demote to a one-line fallback note |
| `[V6]`b | `ContentHash.Of` landed (`identity.md:30`; ledger `:89` names Compute `ModelIdentity.Checksum`) | `contract.md` ContentKey composes it over canonical bytes |
| `[V9]`/`[V10]` | Ledger `:93-94`: `DiscreteCalculus` DEC bundle SHAPE + `MeshAdjointSnapshot` handle toward Compute; spectral/matrix machinery stays kernel-interior (no matrix-plane seam) | `Tensor/dispatch` `Sensitivity.Operator` over the snapshot; ARPACK row composes Compute's OWN CSparse K/M — never kernel LOBPCG |
| `[V12]` seismic | `contract.md:520-538` lumped-mass field is REAL (geometry-based, quadrature-exact — the all-ones fake deleted) | the response-spectrum route's modal-mass link exists; only the sparse-modal (`[V9]`) leg is missing |
| `[V12]` circulation | kernel slice-stack (`Geometry [V10]`b) still upstream — floor-plate upgrade stays wire-gated as written | `Analysis/circulation.md` (new) |
| `[V13]`/residency | kernel `Drawing/pack.md:19` PackKind growth law standing; Geometry `[V13]` lands field-grid/toolpath rows against `ARCH:128-129` (re-anchored) | `Tensor/residency` — rows ride as brief already records |
| `MeshSpace` | frozen owner `Meshing/mesh.md:13` confirmed | `discretization.md:373` rename obligation stands verbatim |

## [02]-[ENGINE_EXECUTABILITY] — should [EXECUTION] declare waves?

YES — declare the leg partition as explicit waves; the engine already runs one-run multi-leg and the brief's prose under-uses it.

Engine facts (`.claude/workflows/rebuild.js`, 583 LOC): the PLAN_SCHEMA requires a per-page `wave` integer (`:63-65`, "ARRAY ORDER IS DEPENDENCY ORDER"); riders carry a required `wave` (`:50-57`); the plan prompt instructs "Assign each page its `wave` (brief wave/leg partition...)" (`:399-400`); the run loop `for (const w of WAVES)` (`:558`) executes EVERY wave in one invocation behind "the ONE inter-wave barrier — real: cross-wave content moves", with per-wave serialized tails, "a failed page halts before dependent waves, landed waves intact", and one terminal acceptance agent. The `leg` selector (`:34-35,238-245`) is a SUBSET bound for resume, not the primary mode; `upstreamMissing` (`:541-544`) halts an out-of-order leg at plan.

The brief's `[EXECUTION]` (`:9,13`) reads as one step whose "plan step... loops once per `[05]-[BUILD_LEGS]` leg" — compatible with the wave loop but it never assigns wave indices, so the sonnet plan agent must reverse-engineer the partition from ~1,000 words of `[05]` prose. Concrete proposal:

1. `[05]` gains a machine-readable partition — each leg header carries its wave index and page list: wave 0 = governance/roster (ledger re-points, `[V15]` dedupe, unconditional manifest motion — riders `manifest-drop`/`manifest-add` with `wave: 0`); wave 1 = numeric spine (factor, blas, dispatch, contract, discretization, optimizer, uncertainty, estimator, signal, Symbolic quartet); wave 2 = assessment spine (assessment, structural, energy, physics, lifecycle, receipts, wire/transport split, codecs split); wave 3 = coverage + closeout (circulation, daylight, satisfy, Model cold pass, truth pass).
2. Dependency evidence the barrier serves: wave-2 `structural.md` re-routes `FrameBackend` onto wave-1 beam rows and lands the seismic route over wave-1 sparse modal ("ONE touch of the page" is only possible if wave 1 landed); wave-3 circulation composes wave-2's rebuilt `Assess` spine and `GeometrySource` port; the `[V8]` parity gate is intra-wave-1 (fixtures freeze and the manifest removal rides a `guardPage` rider — the rider/`guardPage` fields exist for exactly this).
3. `[EXECUTION]` keeps ONE row — `{targets: "libs/csharp/Rasm.Compute", brief: "RASM-CS-COMPUTE-BRIEF.md"}` — producing "all four legs, one run" as PRODUCES; `leg: [N]` documented as the resume/partial form only. The "re-runs across sessions until a cold pass lands nothing" law survives as the iterative close, not the primary sequencing mechanism.

## [03]-[MATERIALS_BIM_INTEGRATION_EVIDENCE]

Both corpora are post-kernel/post-DECISION clean: zero live pre-kernel shapes, zero forbidden downward edges (Materials `Appearance/surface.md:190,384` carries explicit anti-edge guards). Findings split root-row vs package-level:

### Compose correctly (confirmation rows)

- EC2 shear defer (`Materials Component/capacity.md:144,253-254,257-262,380,503`) — `V_Rd,max` ceiling + `Asw` datum section-side, member `V_Rd,s` deferred to Compute BY NAME. ROOT: `[V5]` counterpart row, anchors as-is.
- Twenty-column `ComputedSection` -> seam `SectionProperties` (`Materials Component/component.md:804-829`; `Projection/component.md:249,454`) read via `graph.SectionOf` (`component.md:797,927`). ROOT: `[V4]` counterpart row.
- `AggregateEnvironmental`/`AggregateCost` RELOCATED to Compute (`Materials Properties/sustainability.md:3,352`; basis-native EC3 tagging `:14,17,351`; scaling law `:138`). ROOT: Compute-brief lifecycle row — Compute `Analysis/aggregator` owns the fold, Materials supplies basis-tagged plies.
- Energy exchange seam content-keyed both ends (`Bim Energy/exchange.md:3,21,63,76`; `derive.md:15,20,73`; `projector.md:487-489,533-539,564`) — dual-keyed `EnergyArtifact`, `GeometrySource` port, "a Compute project reference in either direction is the named strata defect". ROOT: confirmation row on the energy runner.
- `IdsVerdict` Bim-owned (`Bim Review/validation.md:613,590`), Compute composes UP via `TWO_HOP_TESSELLATION IdsAuditRequest` (`:699`). ROOT: settled-seam row (persistence/root DECISION tier) — correct DAG direction proven.
- Footprint content-key WRITE-BLOB-FIRST -> `RepresentationContentHash`/`XxHash128` (`Bim Exchange/exchange.md:21`, `reconstruct.md:200`, `Projection/semantic.md:215`). ROOT: persistence DECISION row — the artifact-index shape both AEC packages compose.

### Premise corrections the refreshed brief must carry (root rows)

1. `[V4]` correction: the cold-formed effective section is AISI-S100 published-ratio data Materials ALREADY owns (`ColdFormedRow(..., EffectiveWebMm, SeffRatio)` `steel.md:437`; capacity overload `steel.md:321-333,73,319`) — zero `1993-1-5` hits in the tree. The `[V4]` counterpart obligation ("bake ColdFormedDetail effective properties") is largely DISCHARGED; the residual is only the seam-bake onto `ComputedSection` columns, not an iterative EN bake.
2. `[V6]` correction: retarget the elasticity read to the real seam member — `graph.PropertiesOf(id).Mechanical` (Element-owned; Materials-side spelling `graph.Material(id).Map(m => m.Properties)` `properties.md:583`; Poisson guarded [0,0.5] `:230`, `OfMechanical` seam ctor `:588`). The phantom `MechanicalOf` also infects Compute `ARCHITECTURE.md:126`, `contract.md`, `structural.md` — a 4-file rename rider.
3. `[V12]` adjacency gap: Bim exposes containment (`SpatialStructure`, six axes `spatial.md:38-44`) and grouping (`BimZone` `zones.md:142-192`) but NO space-to-space lateral adjacency (`Adjacent` at `spatial.md:164` is containment-child; voids route through seam `Relationship.Void`, `spatial.md:16`). The circulation runner must DERIVE adjacency from `Compose.Contain` co-membership + `Relationship.Void`/opening edges — state the derive obligation in `[V12]`, or the runner's ingress claim is hollow.
4. `[V12]` homonym note: `Bim Projection/egress.md` is IFC file-serialization egress (`SemanticProjector.Emit`, `:54`) — zero overlap with means-of-egress circulation; one disambiguation line prevents agent mis-routing.

### Package-level only (no root row)

Materials: AISI-vs-Eurocode internals, `MechanicalSource` delegation, timber `OrthotropicLaw` (`timber.md:415,145`), glazing EN-673. Bim: `SpatialStructure`/`BimZone` axis design, `DisciplinePriority` (`coordination.md:97`), IFC codec mapping, `EnergyProjector` arm internals, BCF transport riding Compute `TRANSPORT_AXIS`.

## [04]-[CHAFF_CENSUS] — the brief itself

1. `:9` `[EXECUTION]` paragraph duplicates the `[05]:210-217` sequencing law nearly clause-for-clause (leg self-containment, in-pass ripple repair, serialized tail) — one owner; `[05]` keeps it, `:9` shrinks to the launch row + iterate-until-cold sentence.
2. `:21-26` six verdict lead sentences fully bolded (`**The assessment rail is blind...**` etc.) — whole-sentence bolding, style violation; the numbered structure already carries the emphasis.
3. `:26` "(the figure is exact)" and `:28` "(mandate item 6 RECORDED...)", "(mandate item 4 CLEAN — grep `ComponentSection` is empty...)" — process/verification narration referencing an external mandate document the brief never reproduces; dangling references, state the facts flat.
4. `:58` `[ROSTER_RECONCILIATION]` is ONE ~1,900-word paragraph carrying ~14 independent motions — unauditable as prose; the motions are rows (motion | package | gate | leg) begging for the table form every other section uses.
5. Stale-anchor debt as chaff: ~24 drifted line anchors (kernel +11..+16, ARCH +2, structural +17, energy arms fully moved) — the E2 rows self-declare "known-drifted" yet E1–E15 all carry line-precision citations that decay; symbol anchors (the engine's own rider law: "SYMBOL anchors, never line numbers", `rebuild.js:244`) would not.
6. Stale LOC narration (`channels` 778/782, `codecs` 832/846, `extension.md` 66/79) — three figures wrong within weeks; drop LOC from prose or mark at-census.
7. `:120` `[V11]` and `:132` `[V14]` re-assert "verified in place" waterfall states that have since MOVED (`ARCH:120` clause now absent — already discharged; `api-languageext` now authored) — the "both landed and verified" closures at `:132` were true at authoring and now read as false precision; refresh flips them to present-tense disk truth.
8. `:160` sound-surfaces paragraph embeds two full consumer-pressure sub-briefs (AppUi meshlet-cluster, AppHost residency rows) as 150+-word parentheticals inside a preserve-list — they are rows for `[04]-[PACKAGE_PRESSURE]`, not preservation notes.

## [05]-[COUNTS]

- 51 brief citations censused: 21 exact, 24 drifted, 3 absent/stale premises, 3 stale LOC.
- 1 phantom member (`MechanicalOf`) propagated through 4 files including the brief's own repair verdict.
- 2 waterfall obligations already satisfied upstream and re-verifiable as landed (kernel predicate row `Rasm/ARCHITECTURE.md:106`; Persistence `ArtifactKind` axis in `cache.md:14-31`); 1 waterfall premise unexecuted (MathNet 5.0.0 downgrade — pin still 6.0.0-beta2).
- 3 energy stringly arms uncounted by the brief (cloud route `:613,:630,:638`) — `[V2]`'s classification row-set must widen.
- 9 md5-identical + 8 divergent catalog pairs — brief counts CONFIRMED exact.
- 4 files mid-edit during census (contract/optimizer/dispatch/vocabulary) — anchors flagged, none contradicted.
- Engine: one-run multi-leg is native (`rebuild.js:558` wave loop; `:63-65` wave-typed plan schema); proposal = 4 legs as waves 0-3 declared in `[05]`, `leg` demoted to resume-only.
