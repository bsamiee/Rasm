id: no-identity-field-validator
valid:
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value", mode="plain")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value", mode="before", json_schema_input_type=str | int)
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      class Model(Parent):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def normalize(cls, value):
              return value.strip()
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def record(cls, value):
              log(value)
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          @instrument
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value", mode="wrap")
          @classmethod
          def unchanged(cls, value, handler):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def collect(cls, *values):
              return values
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          async def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def normalize(cls, value):
              return value.strip()
          @field_validator("value")
          @classmethod
          def normalize(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, *, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value", check_fields=False)
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator(*names)
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      import other
      class Model(other.BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      import pydantic
      class Model(pydantic.TypeAdapter):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      from other import BaseModel
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator, TypeAdapter
      class Model(TypeAdapter):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      import other
      class Model(other.BaseSettings):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      import pydantic_settings
      class Model(pydantic_settings.SettingsConfigDict):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      from other import BaseSettings
      class Model(BaseSettings):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      from pydantic_settings import SettingsConfigDict
      class Model(SettingsConfigDict):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel
      import other
      class Model(BaseModel):
          value: int
          @other.field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel
      import pydantic
      class Model(BaseModel):
          value: int
          @pydantic.field_serializer("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel
      from other import field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_serializer
      class Model(BaseModel):
          value: int
          @field_serializer("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value, info, extra):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value, info, extra):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(cls, value, info):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(info, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(*rest, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value, *info):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value, *info):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(*args, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel, Mixin):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator(f"{field}")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      after = str
      class Model(BaseModel):
          value: int
          @field_validator("value", mode="before", json_schema_input_type="after")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          def unchanged(cls, value):
              return value + 1
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      def make(BaseModel):
          class Model(BaseModel):
              value: int
              @field_validator("value")
              @classmethod
              def unchanged(cls, value):
                  return value
    - |
      from pydantic import BaseModel, field_validator
      def make(field_validator):
          class Model(BaseModel):
              value: int
              @field_validator("value")
              @classmethod
              def unchanged(cls, value):
                  return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
          # The field is inherited
invalid:
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value: int) -> int:
              return value
    - |
      import pydantic as pd
      class Model(pd.BaseModel):
          value: int
          @pd.field_validator("value", mode="before")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import field_validator
      from pydantic_settings import BaseSettings
      class Settings(BaseSettings):
          value: int
          @field_validator("value", mode="after")
          @classmethod
          def unchanged(cls, value):
              """Return the value."""
              return value
    - |
      from pydantic import field_validator
      import pydantic_settings
      class Model(pydantic_settings.BaseSettings):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              # Preserve the value
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value",  # Target field
              mode="after")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(klass, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
              # No transformation
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value, /):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value, /):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value: int = 0):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          other: int
          @field_validator("value", "other")
          @classmethod
          def unchanged(cls, value):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          @field_validator("value")
          @classmethod
          def unchanged(cls, value):
              return value
          value: int
    - |
      from pydantic import BaseModel, field_validator
      class Outer:
          class Model(BaseModel):
              value: int
              @field_validator("value")
              @classmethod
              def unchanged(cls, value):
                  return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value, info):
              return value
    - |
      from pydantic import BaseModel, field_validator, ValidationInfo
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value: int, info: ValidationInfo | None = None) -> int:
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value, info):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @classmethod
          def unchanged(cls, value, info, /):
              return value
    - |
      from pydantic import BaseModel, field_validator
      class Model(BaseModel):
          value: int
          @field_validator("value")
          @staticmethod
          def unchanged(value, info, /):
              return value
