# [COMPUTE_IDEAS]

The forward pool of higher-order concepts for measured execution, each grounded in the folder's domain and current platform capability — some deepen a thin owner into a fuller axis, others bind a concrete technique to a settled abstract surface. Open ideas drive the tasks in `TASKLOG.md`; a finished or dropped idea moves to `[2]-[CLOSED]` with a one-line disposition so it is never re-litigated.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
-->

[DEVICE_COMPUTE_SUBSTRATE]-[QUEUED]: promote the CPU-only Tensor lane to a device-resident GPGPU substrate that activates the dormant `OrtResidency.DeviceResident` gate under the same `ComputeReceipt` rail.
- Capability: The numeric lane is CPU-terminal today — `KernelLowering.Lower` lands on `Matrix<double>.Multiply` over a `LinearProvider` (managed/native-openblas) and `TensorOps.Map` binds `TensorPrimitives` span kernels, while `OrtResidency.DeviceResident` exists as a residency row with no compute substrate consuming it and `Substrate` carries only `CpuTensor`/`RemoteGrpc`. A world-class GPGPU platform owns device-resident kernels — kernel JIT, device buffers, stream synchronization — not just a residency descriptor. The shared `ONE_WGPU_DEVICE` branch (WebGPU via `Silk.NET.WebGPU`, Metal-backed on Mac) is the one device handle both AppUi Render and Compute GPGPU bind, so dense GEMM, im2col-GEMM, SpMV, and elementwise maps run on accelerator memory without a second GPGPU paradigm.
- Shape: One `Substrate` extension row `DeviceWgpu` joining `CpuTensor`/`RemoteGrpc`; one `DeviceKernel` registry — a `FrozenDictionary<TensorOpFamily, KernelDispatch>` mirroring the existing `KernelLowering`/`TensorOps` row tables — that compiles one WebGPU compute pipeline per op-family and dispatches over device-resident buffers; the `OrtResidency.DeviceResident` gate (`CreateAllocatedTensorValue`/`CreateTensorValueWithData` over a foreign device pointer) becomes the ingress/egress bridge so a WGPU buffer and an ORT device value share residency without a host round-trip; `KernelLowering.Lower` and `TensorOps.Map` gain a device arm selected by the active `Substrate` row, never a parallel device-tensor type. These land as `Tensor/device/substrate.md` and `Tensor/device/kernels.md`.
- Unlocks: GPU-accelerated GEMM/im2col-GEMM/SpMV/elementwise for FEM assembly, optimizer population evaluation, and inference pre/post-processing; a real device-residency seam to AppUi render sharing one GPU buffer instead of a host copy; the residency lattice's dormant `DeviceResident` row gains its first compute consumer; foundation for fused device kernels and device-resident sparse algebra.
- Anchors: `OrtResidency.DeviceResident` (`Tensor/residency#ORT_BRIDGE`), `CreateAllocatedTensorValue`/`CreateTensorValueWithData`/`OrtMemoryInfo` device descriptor, `Substrate.CpuTensor`/`Substrate.RemoteGrpc`, `KernelLowering.Lower(TensorOpFamily, Matrix<double>, ShardPlan)`, `TensorOps.Map`/`PartitionRoute`, the `TensorOpFamily` 113-row table, `BoundFlow.BindOutputToDevice`, and `ComputeReceipt.Factorization`/`AllocationClass`. `Silk.NET.WebGPU` is admitted on the shared `ONE_WGPU_DEVICE` branch (registered in `Directory.Packages.props`, README `[TENSOR_NUMERIC]`, `.api/api-silk-webgpu.md` via `uv run python -m tools.assay api`); ML inference stays on ONNX Runtime Mac EPs so no second GPGPU paradigm enters. The `DeviceWgpu` row folds into the existing `Runtime/admission` `SubstrateSelection` receipt rather than minting a new selection owner; the device buffer and the `ResidencyManifest.Mint` web-geometry residency negotiate one shared GPU allocation across the AppUi Render seam.
- Tension: Device determinism collides with the `LinearProvider.DeterminismTag` bit-divergence law — a device GEMM is a new determinism tag, and the `SolveDedupKey` must fold the device identity exactly as it folds the managed/native provider, or a cross-substrate cache hit returns bit-divergent numbers.

[SPARSE_TENSOR_ALGEBRA]-[QUEUED]: lift the sparse lane from SOLVE-only to a full sparse-tensor algebra carrying the same op-family breadth the dense `TensorOpFamily` table owns.
- Capability: `Factor.cs` owns sparse ingestion (`SparseFormat` csr/csc/coo/dok into one CSR storage via the `Ingestors` fold) and direct/iterative solve (`FactoredOp`, `IterativeMethod`, CSparse factorizations), but there is no sparse arithmetic — no SpMV outside the Newton residual mat-vec, no SpMM, no sparse transpose/Kronecker/contract/einsum. A world-class sparse-linear-algebra platform owns the full BLAS-sparse plus tensor-contraction category, not just factorization. `MathNet.Numerics` `SparseMatrix` and `CSparse` already supply the storage and primitive multiply/transpose; the gap is one polymorphic op-family fold over them, mirroring how the dense lane folds `TensorOpFamily`.
- Shape: One `SparseTensorOpFamily` `[SmartEnum<string>]` op table (spmv · spmm · sp-add · sp-scale · sp-transpose · kronecker · contract · einsum) folded by one `SparseTensorOps` static surface over `SparseCompressedRowMatrixStorage<double>` and CSparse `CompressedColumnStorage<double>`, exactly parallel to the dense `TensorOps`/`TensorOpFamily` pattern; einsum is one index-string planner lowering to a sequence of contraction rows; each result leaves as the typed sparse `ComputeReceipt` carrying nnz, format, and the recomputed witness — never a second sparse storage owner, with csc/coo/dok staying ingestion conversions into CSR. These land as `Tensor/sparse/ops.md` and `Tensor/sparse/einsum.md`.
- Unlocks: FEM stiffness operator arithmetic (assembly `+=` element blocks, `K·u` products) without densifying; graph-Laplacian and DEC-operator composition staying sparse; sparse Jacobian assembly for autodiff coloring; Kronecker structure for tensor-product element spaces; a device arm via the `DEVICE_COMPUTE_SUBSTRATE` SpMV kernel.
- Anchors: `SparseFormat` csr/csc/coo/dok, `SparseOps.Ingest`/`Ingestors` `FrozenDictionary` fold, `SparseCompressedRowMatrixStorage<double>`, CSparse `CompressedColumnStorage<double>.OfIndexed`/`CoordinateStorage`, `FactoredOp`, `SparseMatrix.OfStorage` mat-vec (the `Solver/contract` Newton residual), `TensorOpFamily`/`TensorOps.Map` (dense parallel), and `ComputeReceipt.Factorization`; every op binds a `MathNet.Numerics` `SparseMatrix`/`CSparse` primitive (multiply, transpose, Kronecker) rather than re-minting a CSR loop. The new ops are a third sibling fold under the same `Factor.cs` owner that already separates ingestion from solve. The `Solver/contract` `SolveLane` `Bᵀ·D·B` assembly and `A·x` residual consume `SparseTensorOps.Spmv`/`Spmm` instead of per-call `SparseMatrix.OfStorage` re-materialization; the `AUTODIFF_DUAL_MODE_ENGINE` colored Jacobian assembles as sparse contractions over `SparseTensorOpFamily` rows.
- Tension: `MathNet.Numerics` `SparseMatrix` operators allocate new storage per op against the in-place fold discipline the dense lane uses (`SpanOwner` scratch), so the sparse fold must fix an nnz-growth allocation policy and stamp `AllocationClass`; einsum index-planning must lower to typed contraction rows, not become a string-eval path or a 114th literal `SmartEnum` row.

[AUTODIFF_DUAL_MODE_ENGINE]-[QUEUED]: promote the reverse-only adjoint to a full dual-mode AD engine and route its consistent tangent into the FEM constitutive update.
- Capability: `Tensor/dispatch#EQUIVALENCE_INTEROP` owns `DifferentiableOp` (per-`TensorOpFamily` VJP, a `Diagonal` flag, an optional JVP), `SensitivityLaw.Adjoint`/`Chain` (reverse-mode tape), `Backward` (MatMul/SoftMax/geometry-operator adjoints), and `AdjointMode` forward/reverse rows — but forward-mode faults `<no-forward-jvp>` on every non-diagonal op (MatMul, SoftMax, the six DDG rows), there is no HVP, and no sparse-Jacobian coloring. A world-class AD engine owns forward AND reverse AND second-order (HVP/GGN) AND sparse-Jacobian exploitation. The FEM side (`Solver/contract`) carries only `MaterialForm { Elasticity, Isotropic, MaxwellEddy }` — no inelastic constitutive law, and `Contact` lowers to a fixed penalty — so the consistent-tangent category is open and AD is its natural supplier.
- Shape: Extend the existing `DifferentiableOp.Rows` table with real forward-mode JVP columns for the non-diagonal ops (MatMul operand-tangent, SoftMax Jacobian-vector, geometry incidence), add an `Hvp` surface composing forward-over-reverse on `SensitivityLaw`, and add a sparse-Jacobian coloring fold (graph-coloring over the sparse structure to batch directional derivatives); then add a `ConstitutiveModel` `[SmartEnum]`/`[Union]` axis on the Solver constitutive sub-domain whose stress-update returns its consistent tangent through the AD `Hvp`, plus a frictional-contact enforcement row whose contact stiffness is the AD-differentiated gap function — all on the existing `DifferentiableOp`/`Backward`/`SensitivityLaw` surface, never a parallel autodiff type. These land as `Tensor/autodiff/forward.md`, `Tensor/autodiff/hessian.md`, `Solver/constitutive/stress.md`, and `Solver/constitutive/contact.md`.
- Unlocks: Hessian-based optimization (Newton/GGN) in the optimizer; nonlinear FEM with true Newton tangents instead of fixed-step Picard; plasticity/hyperelastic/viscoelastic/damage material laws; frictional contact with consistent linearization; sparse-Jacobian-driven implicit time integration; a material-source seam to Rasm.Materials.
- Anchors: `DifferentiableOp.Rows`/`Diagonal`/`Jvp`, `SensitivityLaw.Adjoint`/`Chain(Seq<GeometryTape>)`, `Backward.MatMul`/`Backward.SoftMax`/`Backward.Operator`, `AdjointMode.Forward`/`Reverse`, `GeometryTape`/`MeshAdjointSnapshot`, `PhysicsKind.Material`/`MaterialForm`, `BoundaryCondition.Contact(Gap, Penalty)`, the `Solver/contract` Newton-Raphson tangent re-assembly loop, and the `ComputeFault` 2200 band (symbolic faults extend at the next-free code, no string eval in the tangent). The engine completes by filling the optional `Jvp` column and `AdjointMode.Forward` row and composing `Hvp` from forward+reverse primitives over `System.Numerics.Tensors`/`MathNet.Numerics`, not by adding an AD owner. The `ConstitutiveModel` stress-update reads the elastic `D`-matrix and inelastic parameters (yield, hardening, damage) once from the Rasm.Materials physical-properties source; the analysis element crosses the existing `AnalysisModel (GeometryKey, PropertyKey)` content-key seam to Rasm.Bim/Model; the colored Jacobian assembles through `SPARSE_TENSOR_ALGEBRA` `SparseTensorOpFamily` contraction rows.
- Tension: Forward-over-reverse HVP memory cost against the `ReadOnlyMemory<float>` tape the reverse mode uses (HVP threads the forward tangent through the recorded tape); the constitutive sub-domain is a new Solver owner — per-Gauss-point material state evolution distinct from the `PhysicsKind` physics-assembly axis — not a fourth `MaterialForm` literal; coloring graph-construction cost against dense finite-difference for small Jacobians, so the coloring fold falls through to direct AD below a sparsity threshold.

[STATISTICS_AND_LEARNING_LANE]-[QUEUED]: open a Stats folder owning an `Estimator(Fit/Predict)` statistical-learning axis and a `Transform` signal axis over the dense factorization surface.
- Capability: The package has descriptive statistics only, reached into `MathNet.Numerics` `Statistics`/`Distributions` ad hoc by the UQ lane; there is no estimator/learning lane and no signal/spectral lane — no regression/GLM, PCA, clustering, classification, hypothesis testing, time-series, FFT/STFT, or filter design. A world-class compute platform owns the classical-statistics plus statistical-learning plus DSP category. The dense linear algebra (`DenseOps` SVD/QR/Cholesky/Evd, the `Factorization` union), `MathNet.Numerics` `Statistics`/`Distributions`, and `MathNet.Numerics` `IntegralTransforms.Fourier` already exist as substrate; the gap is two polymorphic owners over them, mirroring the optimizer's `OptimizerKind`/`Surrogate` pattern.
- Shape: One `Estimator` axis — an `EstimatorKind` `[SmartEnum]` (ols/ridge/lasso/glm-logistic/glm-poisson · pca/kernel-pca/nmf/umap-init · kmeans/gmm/dbscan/hierarchical · knn/svm/naive-bayes · t-test/anova/ks/chi2 · arima/exponential-smoothing/state-space) folded by one `Estimator.Fit`→`Model`→`Predict` surface over `DenseOps` factorizations and `Statistics`/`Distributions`; one `Transform` axis — a `TransformKind` `[SmartEnum]` (fft/ifft/rfft · stft/spectrogram · welch-psd · fir-design/iir-butterworth/chebyshev · convolve/correlate/hilbert) folded by one `Transform.Apply` over `IntegralTransforms` and `Complex` kernels — two dense owners under one Stats folder, not a class-per-algorithm family. These land as `Stats/estimator.md` and `Stats/signal.md`.
- Unlocks: Surrogate-model training-data analysis, sensitivity/regression post-processing for the sweep, anomaly detection on sensor streams, modal-analysis spectral post-processing (FFT of transient response), filter design for signal conditioning, and time-series forecasting for digital-twin baselines; feeds the optimizer `Surrogate` GP/RBF rows real fitted models.
- Anchors: `DenseOps.Decompose`/`Factorization` (Lu/Qr/Cholesky/Svd/Evd), `LinearProvider`/`FactorRoute.RankRevealing`/`Orthonormal`, `MathNet.Numerics` `Statistics.Mean`/`Variance`/`Quantile`/`StdDev`, `MathNet.Numerics` `Distributions` (`Normal`/`Beta`), `MathNet.Numerics` `IntegralTransforms.Fourier`, `System.Numerics.Complex`, `TensorOps.ComplexZip`/`ComplexMap`/`ComplexAbs`, the optimizer `Surrogate.GpModel`/`RbfModel` consumer, and `ComputeReceipt`. Every estimator factors through `DenseOps`/`Factorization` (OLS via QR, PCA via SVD, GP via Cholesky) and every transform binds `IntegralTransforms.Fourier`/window functions, never a hand-rolled FFT or normal-equations solve; `MathNet.Numerics` ships these in the already-admitted assembly, so admission is a Stats folder plus README registry entry plus C# production form standards, no new package. The `Estimator` GP/RBF/linear-trend fits feed the `Solver/optimizer` `Surrogate` rows so the optimizer and the stats lane share one regression fold; the FFT/spectrogram post-processes the transient `SolveResult` field of an `fea-transient` march on the `Solver/contract` modal seam; offline learned models cross the `ONE_GRADUATION_EVIDENCE` seam from Python by content key, with C# owning inference plus classical fit and Python owning deep training.
- Tension: The Stats folder is a new top-level footprint (folder, README registry entries, ARCHITECTURE.md domain-map nodes) against the rest being page-level deepens; the `Estimator` axis is broad enough that the fit-result `Model` carrier must be one `EstimatorModel` `[Union]` keyed by `EstimatorKind`, mirroring `Factorization`, without proliferating one result type per algorithm.

[UQ_METHOD_BREADTH]-[QUEUED]: fill the uncertainty lane to full UQ-method breadth — forward UQ, global sensitivity, polynomial chaos, and reliability — as one polymorphic `UncertaintyMethod` axis over the shared sampling, quadrature, and DOE surface.
- Capability: `Solver/uncertainty` already declares a `UncertaintyMethod` SmartEnum (monte-carlo/latin-hypercube-mc/polynomial-chaos/first-order-reliability/subset-simulation) and a `RandomVariable` union, but the breadth is thin: PCE is a single `Matrix<double>.QR` least-squares row with no orthogonal-polynomial basis selection, FORM has no SORM second-order curvature companion, global sensitivity reaches into the sweep's `SensitivityTornado` without owning Sobol-index estimation (first plus total via Saltelli), and quasi-MC randomization is unspecified. A world-class UQ platform owns the method matrix (sampling × sensitivity × spectral × reliability) as one axis; the substrate (`LowDiscrepancy.Sobol`, `Statistics`, `Distributions`, `Matrix<double>.QR`, `SensitivityTornado`) exists and the gap is method depth on the existing fold.
- Shape: Deepen the `UncertaintyMethod` axis with quasi-MC randomization rows (digital-shift/scramble already in `LowDiscrepancy`), a Morris elementary-effects row and a Sobol-index row (first-order plus total-effect via the Saltelli A/B/AB matrix design) composing the sweep `SensitivityMethod`, a PCE row with an orthogonal-polynomial-basis selector (Hermite/Legendre/Laguerre keyed by `RandomVariable` case) fitting through `Matrix<double>.QR` least-squares and reading moments plus Sobol in closed form from the spectral coefficients, and a SORM row adding the second-order curvature correction over the FORM most-probable-point — all rows on the one `Uncertainty.Propagate` fold over the shared `evaluate` oracle, never a parallel UQ engine. These land as `Solver/uncertainty.md` and `Solver/sweep.md`.
- Unlocks: Reliability-based design optimization (FORM/SORM β feeding the optimizer constraint), variance-based factor screening (Morris) before a full Sobol study, surrogate-accelerated PCE moments, quasi-MC convergence for high-dimensional integrals, and auditable `pf`/`β`/Sobol receipts for a reliability verdict.
- Anchors: `UncertaintyMethod` (monte-carlo/latin-hypercube-mc/polynomial-chaos/first-order-reliability/subset-simulation), the `RandomVariable` union (Normal/LogNormal/Uniform/Weibull/Beta/Empirical), `Uncertainty.Propagate(evaluate)`, `UncertaintyResult` (moments/quantiles/Sobol/pf/β), `LowDiscrepancy.Sobol`/`Scramble.DigitalShift` (`Tensor/quadrature#OWNED_BUILDS`), `SensitivityTornado.Of`/`SensitivityMethod` morris-elementary/sobol-variance (`Solver/sweep`), `Matrix<double>.QR` least-squares (`Tensor/blas`), `Normal.CDF`/`InvCDF`, and `ComputeReceipt.Uncertainty`. Every draw rides the `LowDiscrepancy` generator (never per-call `System.Random`), the Sobol-index and Morris rows compose the sweep's `SensitivityMethod` so the sweep tornado and the UQ total-effect stay one variance-decomposition fold, and PCE fits through the existing `Matrix<double>.QR` route with no hand-rolled normal equations; `MathNet.Numerics` and `CSparse` supply the QR and statistics substrate already admitted. The `β`/`pf` feed a reliability constraint on the optimizer `ConstraintHandling` axis for RBDO; offline learned-input-distribution and PCE-surrogate fits stay the Python companion's `ONE_GRADUATION_EVIDENCE` `uncertainty-law` artifact decoded by content key, with C# owning forward propagation and reliability scoring.
- Tension: Saltelli Sobol estimation needs a structured A/B/AB sample matrix the current one-point-per-realization MC draw does not produce, so the sample-design column on `UncertaintyMethod` must carry the matrix-design kind; PCE orthogonal-basis selection couples the polynomial family to the `RandomVariable` case (Hermite↔Normal, Legendre↔Uniform, Laguerre↔Weibull-ish) as row data on `RandomVariable`, not a call-site switch; SORM curvature requires the limit-state Hessian at the MPP, sourced from the `AUTODIFF_DUAL_MODE_ENGINE` HVP.

[OPTIMIZATION_DEPTH]-[QUEUED]: extend the 7-algorithm continuous/evolutionary optimizer to the full optimization category — mixed-integer programming, constraint programming, and global/multi-start search — as new `OptimizerKind` rows over Google OR-Tools.
- Capability: `Solver/optimizer` owns a strong continuous/evolutionary axis (nsga2 · bayesian-gp · gradient-adjoint · topology-simp · simulated-annealing · cma-es · pso) with `DesignVariable.Integer`/`Categorical` cases already declared — but there is no exact integer/combinatorial solver: the `Integer`/`Categorical` variables are only sampled or rounded by the metaheuristics, never solved by branch-and-bound or constraint propagation, and there is no global-optimality guarantee or multi-start basin search. A world-class optimization platform owns the discrete/exact category (MILP, CP-SAT, scheduling/assignment) alongside the continuous one. Google OR-Tools CP-SAT is the canonical managed-friendly MILP/CP engine; it slots as `OptimizerKind` rows because the `DesignVariable` typed-variable axis and the `ConstraintHandling` axis already model the problem.
- Shape: New `OptimizerKind` rows (cp-sat · milp · multi-start-global · robust-minimax) on the existing `Optimizer` static fold, each binding its proposal/solve kernel — cp-sat/milp lower the typed `DesignVariable.Integer`/`Categorical`/`Continuous`/`Linked` and `ConstraintHandling` constraints to OR-Tools `CpModel`/`CpSolver` (CP-SAT) or the linear-solver MILP backend, multi-start-global wraps any local row with a `LowDiscrepancy`-seeded basin restart, and robust-minimax adds the worst-case-over-uncertainty objective transform reading the `UQ_METHOD_BREADTH` scenario set — all under the one `Optimize` entry and `ParetoFront`/`OptimizationResult` carriers, never a parallel MIP solver owner. These land as `Solver/optimizer.md` and `Solver/integer.md`.
- Unlocks: Discrete design (member sizing from a catalog, topology with integer connectivity, facility/space assignment, scheduling), exact global optima for combinatorial subproblems, robust optimization under the UQ uncertainty set, and multi-start escape from local minima for the continuous rows; every optimization modality stays on one queryable Pareto artifact.
- Anchors: `OptimizerKind` (nsga2/bayesian-gp/gradient-adjoint/topology-simp/simulated-annealing/cma-es/pso, with populationBased/gradientBased columns), `Optimizer.Optimize(DesignProblem, OptimizerPolicy, evaluate)`, `DesignVariable.Integer`/`Categorical`/`Continuous`/`Linked`, `ConstraintHandling` (death-penalty/static-penalty/feasibility-rules/augmented-lagrangian), the `Optimizer.Steps` `FrozenDictionary` fold, `ParetoFront`/`OptimizationResult`/`OptimizerPolicy`, and `ComputeReceipt.Optimization`. `Google.OrTools` admits as a Compute `[SOLVER]` package (registered in `Directory.Packages.props`, README registry, `.api/api-ortools.md` via `uv run python -m tools.assay api`, with its RID native assets registered like the MathNet OpenBLAS provider precedent); CP-SAT/MILP/global are rows on the existing fold, not a second optimizer owner, and the `CpModel` is built through the typed model-builder API rather than a string-parsed model. The new rows lower the existing `DesignProblem` to a `CpModel` through the `Optimizer.Steps` fold so the already-declared `DesignVariable.Integer`/`Categorical` cases finally get a solver instead of metaheuristic rounding; robust-minimax reads the `UQ_METHOD_BREADTH` `RandomVariable` scenario set so robust optimization and forward UQ share inputs; the `ParetoFront`, now spanning discrete plus continuous fronts, stays the one content-keyed artifact a dashboard queries by objective region, and large MILP relaxations cross the Python graduation seam while C# owns the in-host solve and Pareto harvest.
- Tension: OR-Tools CP-SAT models integer/boolean variables natively but maps continuous variables only through its linear-solver (Glop/SCIP) backend, so a mixed continuous+integer `DesignProblem` must route the convex-relaxation continuous part separately or discretize — each row must declare which variable kinds it solves exactly versus relaxes; robust-minimax couples to the UQ uncertainty set, creating an ordering dependency where the `UQ_METHOD_BREADTH` scenario design must exist before robust-minimax reads it.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

(none)
