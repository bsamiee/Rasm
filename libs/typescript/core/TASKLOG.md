# [TS_CORE_TASKLOG]

Open and closed work distilled from `IDEAS.md`. `[1]-[OPEN]` carries task cards whose leader holds a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — and three to four scoped bullets: the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. `[2]-[CLOSED]` carries `[COMPLETE]` and `[DROPPED]` items. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open task card template:
[ID]-[STATUS]: <ambitious concise thesis — the capability outcome, never the landing motion>.
- Capability: <the higher-order invariant, owner capability, or concept established — concept grain only, never a page path, row list, or member spelling>.
- Shape: <where the work lands and at what grain — repo-relative page with section/row, or a new-page path; the concrete surface, so Capability never names it>.
- Unlocks: <the downstream capability at the consumer grain — a task narrows its parent idea's Unlocks to THIS slice as `IDEAS.md [SLUG] — consequence`; a set-completion card states the completeness bar that is its acceptance contract>.
- Anchors: <owners, seams, packages, catalogs, doctrines, and techniques making the work plausible — anchors, never procedures>.
- Arms: <BLOCKED or gated cards only; the exact observable flipping it actionable — catalog row landing, member query evidence, package admitted>.
- Route: <present only on a probe, research, or member-pin card; the ordered verification path run before any fence lands>.
- Tension: <only when an unresolved constraint, boundary, or bet shapes the work — the genuine bet, never the arming condition Arms carries>.
- Ripple: <counterpart — cross-folder `pkg` `[SLUG]`, same-folder prerequisite `[SLUG]`; load-bearing build order prefixes follows/precedes/mirrors>.
- Atomic: <present only on a minor-scope task; names the small unit so a later session sizes its turn>.
Capability, Shape, Unlocks, and Anchors are required on every open card, Atomic included; statuses closed — `ACTIVE|QUEUED|BLOCKED` open, `COMPLETE|DROPPED` closed; IDs are SEMANTIC UPPERCASE_SNAKE slugs carrying meaning — never numeric (`[0007]`-class NNNN IDs are a defect), for cards AND research tokens alike; a hyphenated slug anywhere is a defect; repo-relative paths only. Design pages carry the terminal `[RESEARCH]` section always — `(none)` marks empty, absence is an error. Tasks state landing-grain work decomposing an idea.
-->

[VOCABULARY_TABLE_OWNER]-[QUEUED]: One generative vocabulary-table owner for the value floor.
- Capability: a declaration-time generator deriving the kinds tuple, literal schema, guard pair, positional `Order`, and assembled `Shape` from one row-table declaration.
- Shape: lands in `libs/typescript/core/.planning/value/schema.md` as the owner the six re-spelled assembly grammars become declarations of; drives from IDEAS `[VALUE_VOCABULARY_TABLE]`.
- Unlocks: IDEAS.md [VALUE_VOCABULARY_TABLE] — the next value-floor vocabulary is one row-table declaration, the position-to-`Order` projection carrying one spelling branch-wide.
- Anchors: `value/fault#CLASS_VOCABULARY`; the derivation vocabulary-table owner form.
- Tension: the fault-module collapse ruling keeps the three row families distinct, and the stated-annotation export gate constrains a generic assembled-owner annotation.

[BIM_CENSUS_RECONCILE]-[QUEUED]: Three codec census rows name wires no Bim surface mints — the census re-anchors to the settled spellings.
- Capability: every `[WIRE]`-kinded Bim row resolves to a spelling its C# owner declares, so the census, the landing table, the byte-schema suite, and the seam labels all read one vocabulary and a decode-once consumer cannot bind a family nobody emits.
- Shape: the `BimWire`, `DiffWire`, and `IdsAuditWire` rows in `libs/typescript/core/.planning/interchange/codec.md` `_census`, their `_landingRows` and `_schemas` twins, the `_names`/`_suite` entries in `libs/typescript/core/.planning/interchange/format.md`, and the seam label in `libs/typescript/core/ARCHITECTURE.md`.
- Unlocks: the codec census reads as the closed set of wires a producer mints, so a landing guard proves membership instead of admitting three fabrications.
- Anchors: `csharp:Rasm.Bim/Exchange/wire.md` declaring `IfcWire` and naming `BimWire`/`BimWireContext` the deleted form; `csharp:Rasm.Bim/Review/diff.md` declaring `ModelDiff` with `Encode`/`Decode` and naming a parallel `DiffWire` the deleted form twice; `csharp:Rasm.Bim/Review/validation.md` crossing IDS as `IdsVerdict` under `[TRANSPORT]`/`[BOUNDARY]`, never `[WIRE]`; `libs/typescript/core/ARCHITECTURE.md` already carrying `[WIRE]: IfcWire`.
- Tension: the BCF pair and the IFC wire agree across all three branches, so only the diff family and the IDS kind need deciding — the diff spelling is settled at its owner and the IDS row needs its kind ruled before a landing class exists, and the retired `BimModel`/`BimElement` pair leaves the `BimWire` row's landing class with no referent.
- Ripple: `csharp:Rasm.Bim` `ARCHITECTURE.md` seam row spelling `DiffWire`; `libs/typescript/ui/ARCHITECTURE.md` carrying the same stale token.

[COMMAND_GATE_LANDING]-[QUEUED]: `CommandGateWire` closes its guard pair — census, format suite, and landing agree.
- Capability: the codec-homed command-gate family gains its landing line and byte-schema row, or leaves the census and format suite whole — either way the landing guard pair proves the table closed at the declaration.
- Shape: one `_landingRows` line and one `_schemas` row in `libs/typescript/core/.planning/interchange/codec.md`; the `_census` row and the `interchange/format.md` `_names`/`_suite` entries stay or leave with it.
- Unlocks: codec.md's own guard law holds — a codec-homed census row without a landing line is the compile error the page declares, live for this family.
- Anchors: `codec.md` `_census` `CommandGateWire` row (`home: "codec"`, source `Rasm.AppUi/Shell`); `format.md` `_suite` `pb.CommandGateWireSchema`; the `_Landed`/`_LandingKeys` guard pair.
- Atomic: one landing line and one schemas row, or two entry deletions.

[EVIDENCE_TIMELINE_KIND]-[QUEUED]: `EvidenceTimelineWire` reconciles to one kind — census family or process-local composition, spelled identically at every seam.
- Capability: the AppUi evidence-timeline crossing carries one truth: a census-plus-landing pair making it a decoded wire family, or every seam registry re-kinds it to the `ReceiptEnvelopeWire` reuse the feed page states — no `[WIRE]`-labeled family sits outside the closed census.
- Shape: codec census and landing rows, or one seam relabeling on the AppUi edge and branch external-contract registry.
- Unlocks: decode-once reads consistently — every `[WIRE]`-kinded seam edge resolves to a census row.
- Anchors: `state/feed.md` `Feed.Timeline` (`ReceiptEnvelope` wire-twin reuse); `codec.md` `_families` (no row); the core and branch seam registries naming the family as `[WIRE]`.
- Atomic: one kind reconciliation across three surfaces.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition — a DROPPED row carries the rejection reason at ruling grain>; keep closed cards collapsed unless a second retained fact changes future routing.
-->

[CARRIER_OWNED_SPELLINGS]-[COMPLETE]: `interchange/carrier.md` reads its owned names — `_TENANT` composes `Convention.rasm.tenant` off the observe owner, and `_BIN` binds to the codec census through a type-only `Codec.Family` guard, so a rename at either owner fails at this declaration instead of orphaning a twin.
[SECURITY_PACK_COMPLETION]-[COMPLETE]: `observe/board.md` `_PACKS.security` gained the deny, replay, rotation, cold-JWKS, quarantine, and shredded-open trends beside the JWKS-resolve and key-derivation quantiles, so every `Convention` security instrument carries a standing consumer; the pack payload took `quantiles`, and the tenant variable proved already present — `DashboardModel.of` prepends it to every page.
[TRACE_RENDER_TARGET]-[COMPLETE]: `Query.Target` stays closed over the metric-series backends and the ruling landed at `RULINGS.md` `[02]-[SHAPE]` — every leaf keys a metric name against a series name, so a span-selection dialect earns no arm and trace evidence compiles as a residence panel over the wide-event relations a columnar plane already relates.
[RESIDENCE_KIND_SCALAR]-[COMPLETE]: `Query.Residence.value` widened to the same kind-keyed record `table` carries and `_leaf` reads both off one kind — a bucket relation carries no per-point value column its sum-relation sibling carries, so the residence fold now names the column its own relation declares.
[MITATA_STATS_DERIVATION]-[COMPLETE]: `mitata` declares its stats interface unexported, so `Bench.Metric` derives the modality union as `Awaited<ReturnType<typeof measure>>`; the phantom type import left with it.
[ALTERNATE_REGEX_ESCAPE]-[COMPLETE]: `_alternate` composes `effect` `RegExp.escape` instead of a hand-rolled character class — the substrate escapes a strict superset of the RE2 metacharacter set, which RE2 accepts escaped, so the anchored-match correctness condition holds unchanged.
[LATENCY_TEMPORAL_ADMISSION]-[COMPLETE]: `slo.md`'s `Latency` case admits on the measurement domain beside the instrument role — `_LatencyMetric` intersects `Convention.durations` with the histogram roster, so a size or ratio distribution refuses at decode instead of reaching a breach fold whose ceiling only a temporal multiplier scales; the tension resolved to a second declared schema beside `_metric(kind)` rather than a widened generator, since the two axes close against different anchors.
[METRIC_PLANE_ROSTER]-[COMPLETE]: `dimensions` became an instrument-row column and `Convention.dimensions` its fold, so a governor allow-lists exactly the keys some row mints; `Convention.wire.occurrence` joined it as the word axis both `@effect/opentelemetry` metric bridges append from a hardcoded spelling, closing a roster gap that stripped every frequency series to one undifferentiated sum, and `rasm.scene.reason` retired as the phantom its own frequency kind forecloses.
[DURATION_SCALE_COLLAPSE]-[COMPLETE]: the per-name multiplier table collapsed into `_scale` read through each row's own unit code, leaving `Convention.durations` as the ordered temporal roster its two-way guard closes; `Convention.duration` keeps its consumers, a temporal row costs one tuple entry rather than a table row carrying its own re-proof, and the roster publication closed `[LATENCY_TEMPORAL_ADMISSION]`'s stated blocker.
[INSTRUMENT_MATERIALIZATION]-[COMPLETE]: `Convention.mount(metric, words?)` became the branch's one materialization — `bounds` generates each ladder from three scalars, the kind column selects the constructor and its monotonicity, `bigint` selects the value width, a temporal code maps the `Duration` input through `_scale`, and the UCUM code lands as the `wire.unit` tag at every mint; the per-site picks across core, data, security, runtime, and ui are gone, and `Metric.counter`/`gauge`/`frequency`/`histogram` appear in no planning fence but the owner's arms.
[SELECTOR_SUFFIX_RULE]-[COMPLETE]: suffixing became a render-target property — `convention.md` gained the `_translation` strategy roster, the `_promUnit` word per UCUM code, the `_tail` type tail per instrument kind, and the `Convention.translated` projection over a `Convention.named` index, while `board.md`'s PromQL fold renders every selector through it; the Tier-0 pin stays unnarrowed and a store row naming another strategy renders its own series off the same query value.
[SCOPE_VERSION_COORDINATE]-[COMPLETE]: `Hooks.meter` and `_governed` both spend `Convention.scope(module, version)` whole at `runtime/otel/emit.md`, so the versioned single-semconv coordinate holds at both raw-OTel seams; the version tension resolves to the COMPOSED build's, matching the `service.version` the one `Resource` stamps, never a per-library constant.
[VITAL_ATTRIBUTE_VOCABULARY]-[COMPLETE]: `observe/convention.md` gained the vital accounting, phase, and subject rows with the phase family's numeric `ValueOf` clause; `runtime/otel/vital.md` keys both causal records on them and `_drained` stamps the fact whole, retiring three blocked research rows.
[VITAL_LEVEL_UNIT_SPLIT]-[COMPLETE]: the level family split per UCUM code (`vitalDuration`/`vitalScore`/`vitalSize`) on the `bench*` precedent, `_LEVELS` keys the instruments by code, `Vital.level(kind)` is the one series projection, and `observe/board.md`'s vital gauge reads the producer's own metric row.
[UNIT_CARRIER_ROW]-[COMPLETE]: the synthetic UCUM tag key homes at `Convention.wire.unit`, so `otel/meter` and `otel/vital` stop spelling it locally; the branch ruling at `libs/typescript/.planning/RULINGS.md` `[02]-[SHAPE]` binds every mint site to it.
[PROFILE_SPAN_JOIN]-[COMPLETE]: `Convention._profile` gained the `span_id` and `trace_id` store labels and `Profile.banded` writes them from one span read beside the `pyroscope.profile.id` attribute, closing the join both directions; `@pyroscope/nodejs` ships no span processor, so the branch writes both label halves itself.
[CONVENTION_ROW_CULL]-[COMPLETE]: `observe/convention.md` ran its consumer-earned law live — `container.*`/`k8s.*` moved to `_attr` and the stable import, the RUM and flag planes kept their rows under the `runtime/otel/vital` and `runtime/proc/flag` stamps, `container.id` left under the SDK-detector carve, and the placement plane earned its rows outright when `iac/operate/observe` spelled convention rows at its `k8s_attributes` roster, association sources, OTTL migration, and probe filter — one key set, seam edge landed at both `ARCHITECTURE.md` registries.
[WIRE_NAMESPACE_PIN]-[COMPLETE]: `Convention.wire` became the estate constant triple — `namespace` joined `schemaUrl` and `translation`, `Convention.identity` projects `service.namespace` unconditionally with the identity's own `Option` as the fleet override, and `Convention.Identity` moved the key into its required half so a telemetry node holding no `AppIdentity` reads the pin instead of forking it.
[CONVENTION_WIRE_ROWS]-[COMPLETE]: `observe/convention.md` gained the estate observability conformance as rows — `Convention.scope`/`Convention.wire`, the work-plane/lane/batch/security metric+instrument rows with descriptions, the dotted-name guard, and the log-signal ruling; `observe/board.md` aligned panel and model fields to the Foundation-SDK builder members.
[BOARD_PACK_CONSUMERS]-[COMPLETE]: `observe/board.md` gained the `work` and `security` packs with the crash fingerprint table, so every declared instrument has a board consumer; `value/fault.md` gained the stable `code.*` frame quartet on `FaultCapture.Forensic` with the `Evidence.frame` admission; the semconv catalog's consumer census reconciled to the `Convention` hub and the `otel/emit`/`otel/vital`/`otel/crash` owners.
[DIALECT_EXPORT_PAIR]-[COMPLETE]: `observe/board.md` gained the `_DIALECT` export-label pair with the invoke fault-reason and vital grade panels — the last instrument rows without board consumers; `ARCHITECTURE.md` seam labels reconciled to the page-owned wire spellings (`EvidenceTimelineWire`, `GeoFeatureWire`) and `Alert.Spec` re-sourced to its `slo` owner; `README.md` registered `@effect/experimental` and the interchange charter gained the capability dial.
[OBJECT_CONVENTION_ROWS]-[COMPLETE]: object rows landed in `observe/convention.md` (`objectWritten`/`objectSize`/`objectReclaimed`/`streamSize`, the `objectOutcome` axis) with `observe/board.md`'s `object` pack as their standing consumer.
[TAP_PAGE]-[COMPLETE]: `observe/tap.md` landed with the point brand, modality table, subscription contract, and breach isolation; `README.md` routes OBSERVE to the hook rail and `ARCHITECTURE.md` carries the `tap.ts` codemap row and the `Tap.Registry` runtime seam.
[TAP_POINT_ANCHORS]-[COMPLETE]: three standing tap points ride `tap.md`'s `_points` rows with publisher anchors at `state/machine#ACTOR`, `interchange/invoke#COMMAND_GATEWAY`, and `interchange/codec#FAULT_RAIL`.
[CARRIER_PAGE]-[COMPLETE]: `interchange/carrier.md` landed — typed triple, total folds, `rasm.tenant` promotion, and the closed connect/nats/kafka/mqtt/cloudevents dialect table — with the codemap row and the `Carrier.Context` runtime seam in `ARCHITECTURE.md`.
[BINARY_HEADER_LANE]-[COMPLETE]: `Carrier.bin` rows land the `-bin` lane at `interchange/carrier#DIALECT_TABLE` over `encodeBinaryHeader`/`decodeBinaryHeader` (`rasm-tenant-bin`/`rasm-stamp-bin`), consumed by `interchange/invoke#DIAL_AXIS`'s interceptor law.
[PROFILE_CONVENTION_ROWS]-[COMPLETE]: `Convention.profile`/`Convention.profiled` landed in `observe/convention.md`; `observe/board.md` gained the `Flamegraph` row with the per-tag datasource arm and the `profile` pack.
[SPAN_LIFETIME_ANCHORS]-[COMPLETE]: `Effect.makeSpanScoped` lifetime anchors landed — `machine/<name>` on the actor boot/restore path (`state/machine#ACTOR` `risen`) and `gateway/duplex` on the duplex acquisition (`interchange/invoke#COMMAND_GATEWAY`).
[CLAIM_BAND_LANDING]-[COMPLETE]: `Claim.Band` landed on the `interchange/codec#LANDING_WIRE` claim landing as the sample count beside its measured-rung map under `tests/contracts/` `BENCHMARK_CLAIM`; `observe/board#BENCH` carries `Bench.graded` with the host-print and unmeasured-rung refusals and the `bench` pack trends the bridged series.
