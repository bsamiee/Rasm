# [APPUI_TASKLOG]

Open and closed work for the product UI engine, distilled from the ideas in `IDEAS.md`. Each open card carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — plus the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. Live-host probes and tool-gated arms are tracked as blocked tasks against the substrate that unblocks them.

## [1]-[OPEN]

[T-EMBED-PROBE] [HOST-PROBE-DEFERRED] — live host-shared embedding and GPU lease
- Resolve the `hosts/surface-hosts.md` and `viewport/viewport-pipeline.md` RESEARCH rows under a running integrated host: Avalonia-in-Rhino NSView embedding (pump coexistence, resize sync, render-backend contention), the seam `OnUiThread` access-assertion under the Rhino-owned AppKit run-loop, and the host-shared `GRContext` lease through `ISkiaSharpApiLease.TryLeasePlatformGraphicsApi` for meshlet/path-trace emit.
- Integrate the SkiaSharp GPU backend-context (`GRMtlBackendContext`/`GRVkBackendContext`) and `SKRuntimeEffect` over the leased context bound through one `SurfaceSeam` delegate column.
- Wires bind only at the app root that composes a live host; AppUi references the abstract seam columns, never a host API.
- HOST-PROBE-DEFERRED on the live RhinoWIP host surface and the host-owned GPU pipeline (external: an integrated running host does not exist); the CPU/2D-Skia fallback ships today, de-risked standalone against a windowed `GRContext`, and the `GpuBackend` `RenderTargetFactory` column the `T-BACKEND-PORT` realization landed gives the lease a backend-bound target seam to confirm in-host.

[T-INPUT-FABRIC-SDKS] [UPSTREAM-BLOCKED] — alternative-input device SDK spellings
- Re-ground the `input/input-interaction.md` `InputDevice`/`DeviceOutput` per-device member spellings — SpaceMouse HID, gamepad API, gaze SDK, switch-access scan, speech recognition, MIDI, and CNC/robot/haptic transport — and the `localization/#RTL_MIRRORING` `LiveCaption.Translated` recognizer/translator spellings.
- Integrate each device SDK and the speech-and-translation package as `SurfaceSeam` delegate columns bound at composition.
- Wires fold every device sample onto the one `CommandIntent` table and symmetric output over normalized `DeviceAxis`; SDKs cross only as composition delegates.
- UPSTREAM-BLOCKED on the host-bound device/recognizer SDKs (external: SpaceMouse HID, gamepad, gaze, switch-access, speech, MIDI, CNC/robot/haptic transports are not-yet-admitted bindings crossing only as `SurfaceSeam` composition delegates); the fabric vocabulary and the one `CommandIntent`/`DeviceAxis` fold are fence-complete now.

## [2]-[CLOSED]

[COMPLETE] T-SHADING-PAGE — `shading/shader-pipeline.md` owns the `XxHash128` content-keyed `ShaderModule` `[ValueObject]`, the `PipelineState` `[Union]` (RenderPipeline|ComputePipeline|RuntimeEffect) cache over the `PipelineCacheKey` `[ComplexValueObject]` `(ShaderModule, GpuBackend.Backend, VertexLayout)` key, the `ShaderCompileReceipt` `evidence/diagnostics-evidence.md#RECEIPT_UNION` case sealing the `Silk.NET.WebGPU` `DeviceCreateShaderModule`/pipeline-create and SkiaSharp `SKRuntimeEffect` compile diagnostic, and the HotAvalonia `ShaderWatch` re-key Fold; `wgsl`/`sksl` resolves at the existing `GpuFamily` axis the cache keys on, and the live GPU device compile is the `#RESEARCH` SPIKE row exactly as `viewport-pipeline.md#RESEARCH` `[VIEWPORT_GPU]`.

[COMPLETE] T-GPU-PROFILER — `evidence/diagnostics-evidence.md#PERF_BUDGET` owns the `GpuTimingPass` writing `QueryType.Timestamp` queries at each pass boundary through `CommandEncoderWriteTimestamp`/`CommandEncoderResolveQuerySet` and retiring through the non-blocking `Wgpu.DevicePoll` (never a blocking fence), the `PipelineStat` `QueryType.PipelineStatistics` frozen-column Fold, and the `GpuTimeline` measured-vs-projected projection keyed by frame ordinal that `Migrate`s the `viewport-pipeline.md#RENDER_GRAPH` `FrameReceipt` GPU figure from encoder-projected accumulated cost to resolved nanoseconds (deepened, never forked) feeding the `QualityVerdict`; the live timestamp resolve is the `#RESEARCH` host-shared-GPU SPIKE row.

[COMPLETE] T-RESOLVE-LADDER — `viewport-pipeline.md#RENDER_GRAPH` owns the `ResolvePass` `[SmartEnum]` (Msaa|Taa|Fsr|Smaa) `Composite` policy column carrying its samples/render-scale/reproject behavior as the smart-enum row, the `ResolvePolicy` `QualityTier`-rank-bound frozen dispatch table, and the `ResolveState` Halton-jitter-plus-motion-vector-reprojection state-threaded Fold with the motion-vector buffer one `GEOMETRY_VIRTUAL` `BindlessTable` slot (never a parallel motion-vector owner); the `Fsr` sub-resolution render reads the `RESIDENCY_BUDGET` VRAM bound, the `Msaa`/`Smaa`/single-sample resolve runs on the CPU raster today, and the live `Taa`/`Fsr` GPU compute resolve is the `#RESEARCH` `[VIEWPORT_GPU]` SPIKE row.

[COMPLETE] T-GPU-MANIFEST-COVERAGE — VERIFY-ONLY confirmed: `Directory.Packages.props:297-305` carries all seven Silk.NET GPU/XR rows incl `Silk.NET.OpenXR.Extensions.FB` (305) at the `2.23.0` line, `README.md#PACKAGES` `[GPU_BACKEND]:84-91` lists all seven, and `.api/api-silk-openxr-fb.md` is authored through ENTRYPOINTS+IMPLEMENTATION_LAW (the FB `Create/Start/Pause/SetStyle/Destroy` entrypoints + `PassthroughCreateInfoFB`/`PassthroughLayerCreateInfoFB`/`CompositionLayerPassthroughFB` carriers); the manifest is complete and the README registry is confirmed — no manifest delta and no `.api` patch needed this pass (STALE-SOURCE FLAG cleared: the FB env-blend admission the older idea-pool narration claimed missing is present).

[COMPLETE] T-EXPORT-WRITERS — `visuals/visuals-offscreen.md#DOCUMENT_EXPORT` `OfficeExport.Write` transcribes the XLSX (`SpreadsheetDocument.Create`/`WorkbookPart`/`WorksheetPart`/`SheetData`/`Row`/`Cell`/`Sheets`/`Sheet`) and DOCX (`WordprocessingDocument.Create`/`MainDocumentPart`/`Body`/`Paragraph`/`Run`/`Text`) part-graph bodies with `FontTablePart` embedded fonts, and `drafting/drafting-sheets.md#DRAFT_EMIT` `DraftEmit` transcribes the DWG (`CadDocument`/`Line`/`Layer`/`MText`/`DwgWriter.Write`) and DXF (`netDxf.DxfDocument`/`Line`/`Layer`/`MText`/`Save`) typed-entity-writer bodies over the shared `SheetEntities` projection, both delivering through the one `VisualDestination` and reusing the `FlowFold` pagination; the `.api/api-drafting-export.md` catalogue gains the OOXML part/content-element family + part-add entrypoints + the ACadSharp/netDxf construction-value spellings, the PPTX slide part-graph and cell/run-style member spellings staying the `OFFICE_OPENXML` residual and the per-entity `Dimension`-style/`MText`-format spellings the `DRAFT_ENTITY` residual; PDF/SVG ship on the `SKDocument`/`SKSvgCanvas` rail.

[COMPLETE] T-HEADLESS-PROOF — `evidence/diagnostics-evidence.md#HEADLESS_DERIVATION` owns the `RenderHashLane` `key@scale×gamut` cell and the `ProofLaw` law-matrix fences: `FrameHashEquality` sealing one cell through `Captures.Shot` then `Verifier.Verify`, `DeterministicCapture` the CsCheck property that two captures of one lane hash identically, `ReplayDeterminism` replaying the journal twice under `FakeTimeProvider.SetUtcNow(UnixEpoch)` and `Verifier.Verify`-equating the payload-digest seqs, and `ProofMatrix` the one entrypoint owning singular-cell and full-matrix run by input shape over `ProofEngine.Derive`/`Dispatch`; the `SurfaceHost.Headless` row rides `ForceRenderTimerTick`, `UseHeadlessDrawing=false` selects Skia on every hash lane, and the `RenderHashGrid` golden bytes are the C#-host leg of the content-addressed `XxHash128`-keyed ONE_WIRE_FIXTURE_CORPUS.

[COMPLETE] T-IMMERSIVE-PAGE — `immersive/xr-review.md` authored: `XrSession` the boundary capsule owning the instance/system/session lifecycle over the shared `Wgpu` `Device`/`Queue` graphics binding (one GPU lifetime, the host-loader probe folding to `XrFault.LoaderAbsent` and the flat viewport), `StereoSwapchain` the per-eye image array as a per-eye `RenderTargetFactory` projection of the one `viewport-pipeline.md#RENDER_GRAPH` never a second viewport engine, `XrFrameLoop` the `WaitFrame`/`BeginFrame`/`LocateViews`/`EndFrame` fold on the runtime-predicted display-time clock sealing the stereo `XrFrameReceipt` through the one `diagnostics-evidence.md#RECEIPT_UNION` `Render` case, `XrViewpoint` the `[ComplexValueObject]` round-tripping the portable `viewport-pipeline.md#VIEWPOINT_CODEC` `Viewpoint` plus the LiDAR `reality-capture.md#MEASURE_OVERLAY` markup and section, `EnvironmentBlend` the closed `[SmartEnum]` = `Opaque | Additive | AlphaBlend` whose FB-passthrough arm folds to `Opaque` when the runtime lacks `XR_FB_passthrough` (`Silk.NET.OpenXR.Extensions.FB` admitted `Directory.Packages.props:305`, `.api/api-silk-openxr-fb.md` surface authored), and `XrActionSet` projecting the OpenXR controller model onto the one `commands-availability.md` `CommandIntent` table never a second input fabric; the live host-loader frame and per-vendor graphics binding stay RESEARCH-gated runtime-action surfaces folding to the flat viewport ([XR_HOST_LOADER]/[XR_VENDOR_BINDING] gated on the host OpenXR loader probe post-approval).

[COMPLETE] T-PRESENTATION-PAGE — `presentation/review-tour.md` authored: `ReviewTour` the ordered non-empty `Seq<TourStop>` value object and `TourStop` the structural-identity stop binding a saved `Viewpoint`, dwell `Duration`, transition `MotionToken`, and `NarrationTrack`; `TourPlayhead` the state-threaded camera-interpolation fold over the animation `Playhead`/`ScrubState` re-entrant-no-drift clock with the transition easing tracing to the `motion/` catalog and reducing once through `ReducedMotion.Select`; `NarrationTrack` projecting captions onto the `typography/` `TypographyRole` vocabulary through the HarfBuzz `DrawShapedText` rail; `TourSource` the one closed `[Union]` = `SavedSequence | TopicTour` whose topic arm folds a `Rasm.Bim` `BcfTopic` set into stops via `ViewpointCodec.FromBcf` at the package edge; and `WalkthroughTour` rendering the tour offline through `VisualCodec.Encode`/`VisualDestination` over the timeline `Walkthrough` fold — no package admission, no tour-local stopwatch, no second camera or BCF model (`review-tour.md#TOUR_MODEL`, `#TOUR_PLAYHEAD`, `#NARRATION`, `#TOUR_SOURCE`, `#WALKTHROUGH_RENDER`).

[T-BACKEND-PORT] [COMPLETE] — `GpuBackend` SmartEnum gains the `GpuFamily` column and the `Wgpu`/`WebGpu` rows beside the `Metal`/`Vulkan`/`OpenGl`/`Software` Ganesh rows, the `RenderTargetFactory` owner binds a backend-provided target factory the `Lease` and every `RenderPass`/`CapturePass`/`CustomVisual` emit body consumes, and `visuals-offscreen.md#DRAW_CAPSULE` mirrors the factory; the `Wgpu` row binds the stable .NET Foundation `Silk.NET.WebGPU` wgpu/Dawn surface and the `Software` Skia Ganesh raster row is the shippable floor, Graphite re-admitting as one `SkiaGraphite` row the moment SkiaSharp ships its Recorder/Context surface; the archived VelloSharp/VelloSharp.Avalonia.Vello rows retired root-up with no shim, no Vello identity surviving in the manifest, README, or `.api/` (`viewport-pipeline.md#RENDER_GRAPH`).

[T-REALITY-CAPTURE] [COMPLETE] — `realitycapture/reality-capture.md` authored: `SplatSource`/`PointCloudSource` decoded off the Compute splat/point payloads, the `CapturePass` `Splat`/`Point` cases over the backend target factory, the LiDAR-anchored `MeasureOverlay` bound to the `Viewpoint`, and the `CaptureClip` on the animation playhead; the LAZ/SOG scan decode stays the Python companion's external producer crossing as a Compute payload ([CAPTURE_DECODE] upstream).

[T-WEB-VIEWPORT-WIRE] [COMPLETE] — `viewport-pipeline.md#TS_PROJECTION` deepened into the `ResidencyManifest` single-mint producer of the `WEB_GEOMETRY_RESIDENCY_WIRE` (geometry-residency/meshlet/splat tile wire shapes) the TS web leg consumes, each tile content-keyed by `XxHash128` off the same Compute `GeometryPayload` residency; single-mint invariant graded at the cross-libs master.

[T-COORDINATION-BOARD] [COMPLETE] — `coordination/issue-board.md` authored: `Issue` composing the AppUi `Viewpoint` and the `Rasm.Bim` `BcfTopic`/`BcfComment`/`BcfViewpoint` at the boundary, the `CommentThread` CRDT over the notebook replicated-document law, the `IssueTile` dashboard projection, and `IssueBoard` owning the issue-to-viewpoint binding and the BCF round-trip; BCF schema stays `Rasm.Bim`-owned ([BCF_TOPIC_SEAM] composed at the package edge).

[T-PERF-GOVERNOR] [COMPLETE] — `evidence/diagnostics-evidence.md#PERF_BUDGET` cluster adds the `QualityTier` smart enum, the `PerfSample` telemetry fold, and the `PerfBudget` governor folding the `FrameReceipt`/`ResidencyBudget`/HUD telemetry into one `QualityVerdict` degrading render passes, the residency watermark, and the motion tokens together under a hysteresis band so render-hash lanes stay attributable.

[T-NOTEBOOK-DETERMINISM] [COMPLETE] — the `CapabilityPin` composes the AppHost `DeterminismContext`/`EnvFingerprint` and `DeterminismKernel.Reproduces`, the `ReplayManifest`/`NotebookReplay.Verify` composes the `ContentHash` chain and `ReplayVerify` per-step proof through `ProofEngine.Replay`, and the `RecomputePlan` cell DAG aligns to the AppHost `RecomputeNode` content-address node identity ([NOTEBOOK_DETERMINISM] composed at the package edge).
