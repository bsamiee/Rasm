# F03 - Signatures and types

Lab: `Lab.F03` (`Age.cs`, `Timing.cs`, `Absence.cs`, `Totality.cs`, `ExternalMovie.cs`, `Samples.cs`). Build: zero diagnostics. Run: exit 0.

## Members verified

- `[ValueObject<int>]` on `readonly partial struct Age` with `[ValidationError<InvalidAge>]`: the generated `Validate(int, IFormatProvider?, out Age)` returns `InvalidAge?`, `Create` throws on an invalid value, `Age < Age` and the implicit conversion to `int` are generated, and `From` maps `Validate` to `Fin<Age>`.
- `Expected`: `sealed record InvalidAge() : Expected("age out of range", 1001), IValidationError<InvalidAge>` lifts implicitly to `Fin<Age>` and is the error the hook assigns; `Error.IsType<InvalidAge>()` and `Error.HasCode(1001)` read it back inside `Fin.Match(Succ:, Fail:)`.
- `Fin<Age>`: `IsSucc`, `Match(Succ:, Fail:)`, `ToOption()`.
- `Unit`, `unit`: `Unit` equality with `unit`.
- `Prelude.fun(Action) : Func<Unit>`: adapts an `Action` for a `Func<T>` overload (`Time<Unit>(operation, fun(action))`).
- `Option<A>`: `Match(Some:, None:)` (named arguments, `Some` first in the assembly signature), `IsSome`, `IsNone`, `==` with `Some(x)`, `Bind`.
- `Prelude.Some(A)`, `Prelude.None` (`Fail<Unit>` with an implicit conversion to `Option<A>`), `Prelude.Optional(A?)`, implicit `Option<A>(A?)`.
- `Prelude.parseInt(string?) : Option<int>`.
- `HashMap<K, V>.Find(K) : Option<V>`; `Prelude.HashMap((K, V) head, params (K, V)[] tail)` and `Prelude.HashMap<K, V>()`.

## Corrections and confirmations

- `Some(null)` is a `Some` that holds `null` (`Option<string?> someNull = Some(missing); someNull.IsSome` is true). The doc states `Some(x)` wraps the value as given and names `Optional` and the implicit conversion as the null boundary. Matches the CORRECTION in `LAB-NOTES.md`.
- `Bind(ParseAge)` resolves with the lab method group. A probe with `.Bind(parseInt)` also compiles: the generic `parseInt<M>` overload does not make the method group conversion ambiguous. The doc keeps `ParseAge` so the block reads in its own terms.
- `System.Diagnostics` is not in the global usings and the brief forbids a file-level `using`, so the `Time` sample spells `System.Diagnostics.Stopwatch` in full.
- `Risk` needs explicit enum values (RCS1161). String equality in the sample checks uses `string.Equals(a, b, StringComparison.Ordinal)` (MA0006).
- `ExternalMovie` needs one instantiation or CA1812 fires; the sample constructs it and converts each nullable property with `Optional`.

## Host-only and uncompiled blocks

- The arrow-signature block under "Read behavior from the signature's shape" is a `text` fence: it is notation, not C#.
- `Age -> Risk` and `Option<A> = None | Some(A)` are `text` fences.
- The `<Nullable>enable</Nullable>` block is `xml` and describes the project file.

## What a project built on this file still needs

- `Age` has a generated private constructor, so `From` admits input and `Create` constructs a value the code already knows to be valid; `Create(200)` throws `ValidationException` with the `InvalidAge` message, and `TTRESG047` rejects `default(Age)`.
- The generator implements `IComparable<Age>`, `IEquatable<Age>`, and the comparison operators, so `Map`, `Set`, and `HashMap` keyed on `Age` need no hand-written members.
- `CalculateRiskProfile` and the `Timing` sample are pure demonstrations; the timing side effect belongs in an `IO<A>` at a host boundary.

## Integration changes

- `Age` is a Thinktecture value object: `DomainType<Age, int>`, the private constructor, `FromUnsafe`, `To()`, `IsValid`, and the four operators are generated or retired, and the rule moved into `ValidateFactoryArguments`.
- `InvalidAge` implements `IValidationError<InvalidAge>` so the hook raises the typed `Expected` and `From` maps `Validate` to `Fin<Age>` without a second error type.
- No operator settings on `[ValueObject<int>]`: `age < 60` resolves through the implicit key conversion (probe), so `DefaultWithKeyTypeOverloads` is not needed.
- Prose: the `DomainType` sentence, the `FromUnsafe` sentence, and two design rules name the generator, the hook, `Validate`, and `Create`.
- `Samples.cs`: `Age.FromUnsafe(x)` is `Age.Create(x)` and `adult.To() == 70` is `adult == 70`.