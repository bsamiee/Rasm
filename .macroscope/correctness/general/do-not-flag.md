# [DO_NOT_FLAG]

Deliberate house shapes that violate generic best practice — flagging them is a false positive:
- Source descriptors retained beside a derived projection are not ignored, so an omission finding proves absence from both owners.
- A modulo accessor is not an out-of-range access, and an option-shaped lookup is not a throwing access.
- An optional egress that maps an empty aggregate to `None` exposes absence rather than concrete closure evidence.
- Author-declared census fields are not native read-back counters, so a host-completion finding cites the actual read surface.
- Private planning or search evidence is not output-affecting identity unless the frozen egress carries it.
- An in-process callback owes owner-derived coverage rather than host-completion evidence when its charter exposes no host boundary.
- Branches already inhabiting one typed rail need no carrier repair.
- A duplicate-validation finding cites a second declaration or admission call rather than an imagined sibling site.
- A consistency-check demand between a declared shape and a value the owner computes from that same shape is void — a computed projection cannot desynchronize from its inputs. The carve ends at a provenance boundary: a count, length, or shape arriving from foreign bytes, a wire header, or a peer process is independently supplied, so a bound-before-allocate demand against it is live exactly as against an independently stored field.
- Completeness and restoration demands against a registry or research row deliberately BLOCKED on an upstream producer are void — the blocked row is the correct fail-closed form, and the demand routes to the producer owner the row names. Inversion covers EVERY close route, not producer-landing alone: a row retires when its producer lands OR when its question settles — a ruling row landed at the owning tier, a refusal-at-admission the ruling designates as the resolution — so a restore-the-row demand against any settled close, and a restore-or-fix compound whose fix half re-reads the ruled refusal as a pending defect, are void at the same bar; a resolved row deletes whole with no tombstone and `(none)` is the correct roster, never evidence of a missing producer.
- Refusal-at-admission designated the verdict by page or ruling law IS the landed resolution: a fix-the-gap, add-a-fallback, require-at-construction, or restore-the-capability demand against it re-litigates the ruling — absence riding a gate verdict is design, never missing data or a pending fix.
- Operation identity derives apart from payload identity, so a stable-content idempotency-key demand — a settlement, causal-log entry, or dedup keyed on a payload digest, however phrased: stable operation identity, stamp-free content key, caller-threaded coordinate — is void: dedup homes at the causal-log owner on the operation-identity composite, the record stamp is writer-owned at that authority — a caller-threaded or stamp-free key lets two producers mint one key and dedup genuine evidence away as a redelivery — a retried operation that re-performs its work is a distinct operation pricing that work, and a railed Error is never retried: cross-run re-execution is content-keyed deterministic recompute, and the crash-window re-record is the declared at-least-once posture of an append-only evidence plane, never a defect the demanded key must guard.
- The durable record seats at the nearest async fold off settled evidence, so a relocate-the-record-into-the-computation demand — settling journal custody inside the weave or kernel a rail already harvests — is void where the page returns its result only from the record's landed continuation: the relocation converts a settlement fault into loss of completed, paid-for evidence.
- Contract-version and CloudEvents `dataschema` findings resolve from `libs/.planning/RULINGS.md`; a demand conflating their authorities is void. A bump demand naming machinery the file does not carry is stale, as is replay compatibility without a replay ring or decode path.
- Exact equality is the discriminant for values decoded directly from literal attributes with no arithmetic accumulation, so a tolerance-comparison demand there is void — a bare per-module epsilon is the rejected literal, and an epsilon collapse over a near-equal set silently discards a real discriminant.
- Admission modality derives from what the emission IS, so a demanded fix degrading a ruled modality — bypassing keyed reuse for a content-determined emission instead of completing the keyed preimage — is the weaker-form demand: the lawful fix completes the preimage the identity omits.
- Library tiers stay app-neutral and name no lifecycle owner, so a move-to-application-startup or composition-root demand against one is void where the ruled form arms the process-global setting on the single lazy door's first line — the demanded relocation assumes the consumer the estate forbids.
- Installed-surface member spellings that read as typos are the shipped spelling once probed on the distribution — a misspelling flag or corrected-spelling demand breaks the binding, void at every severity on every language.
- Bounds answer the scope their owner declares and no second one: a directory-, queue-, or run-wide cap is not a per-item ceiling, and a ceiling stamped as evidence on a result is not a second input to the grade that result reports — reuse across that boundary proves the second scope at the bound's own owner first.
- Move-the-check-earlier demands prove the invariant expressible at the demanded seam: a cross-roster or two-column invariant no per-row admission can state stays at the roster owner, and a locally re-derived schema or validator binds every peer to the deriving type system where the transported one binds none.
- Co-requirement demands across optional fields prove no admitted row carries one without the other; where the owning roster's own rows split them, the true invariant is one-way and the co-requirement demand fabricates law.
- Ack-until-landed and at-least-once demands against a lane whose declared overflow policy sheds convert deliberate shedding into unbounded redelivery — the wedged-retry defect — so the delivery verdict settles at the lane's declared policy, never at a threaded acknowledgement the one-enumerable-shape law forks; a land-coupling demand against a plane exposing no per-fact landing acknowledgment demands a phantom signal — intake admission is the strongest producer-observable guarantee, and the repair corrects a LANDED overclaim in prose, never restructures against the plane.
- Current-state claims trace composed calls, derived projections, internal provider operations, and result or canonical-egress consumption before flagging; a declaration is not a second call site, a value proven on that trace is not unused, and a path already entering the named admission owner is not missing the check.
- Demanded mechanisms prove semantic compatibility: quantities share dimensions, the target owner carries the needed policy rather than adjacent capability alone, leveraged algorithms carry acceptance and fallback law, and fixed ceilings or invariants exist in owner or provider law rather than being invented.
- Planning registries relate only as their charters declare; concept-to-task derivation is not identity or status mirroring, and design-corpus fences owe transcription-complete code but no executable test.
- Provider and algebra claims verify semantics: count-taking ranges yield the requested count, projecting a vector onto a basis perpendicular to its parallel or antiparallel direction yields zero, and an internal provider operation satisfies the claimed setup step.
- An admitted lower bound refutes a counterexample the complete owner roster cannot cross.
- Algebraically equivalent factorings are settled current shapes rather than arithmetic defects.
- A declaration migration demand moves every direct constructor and consumer in the same change, so a declaration-only factory repair is incomplete.
- A local codec never duplicates a canonical-byte owner, whose case family extends before the consumer routes.
- Recursive execution expansion deletes only after the published trace retains every executable case, structural locus, and repetition identity.
- A predecessor dependency deletes only after the owning atom carries the intrinsic start, sweep, or equivalent evidence the consumer needs.
- Equal row shape, labels, formulas, codecs, current mappings, or a shared type NAME across independent packages are not duplicate law without one existing winner preserving semantics, policy, precedence, lifetime, evidence, and consumers; a declared package or stratum boundary admitting the peers only through a ruled seam refutes the merge outright, required ABI spellings stay on both sides, and a replacement spanning untouched ownership or lifetime concerns exceeds the changed defect. Every collapse demand names what the merge loses: where the plural form carries a guarantee — a per-folder closed roster's compile-time break, a dependency boundary keeping host SDKs above their stratum, a deliberate isolation co-location — the demand withdraws, the plurality lawful with its discriminant stated at a durable owner.
- A package traversal lacking the owner's deterministic connected walk is not approved replacement leverage.
- Both sides of an external ABI retain the shared operation spelling required by that boundary.
- Payloads with different motion or effect semantics remain distinct even when their carriers look alike.
- Gate failures confined to pre-existing untouched lines are not findings against the reviewed diff and authorize no collateral edits.
- Aggressive API breaks with every call site updated in the same change: the sanctioned rename path — and a demanded migration, invalidation, or compatibility surface for a changed key, schema, or identity encoding is void on a corpus with no legacy consumer, the demand inventing the forbidden surface.
- Dense single-expression bodies and heavy polymorphic dispatch: the bar, not obfuscation.
- Absent defensive guards inside domain logic: admission-once boundaries, not missing error handling — a band the producer's own construction proves, a norm-minted magnitude or an absolute deviation, admits no out-of-range instance, so the interior re-check and the boundary negative-rejection demand are both the defect.
- Sparse 1-2 line agent-facing comments: comment-law compliance, not missing documentation.
- A large file owning one full concern: sanctioned; file-size budgets do not exist here.
- Bracketed uppercase section dividers (`# --- [LABEL] ---`): source structure, not commented-out code.
- `TODO`/`FIXME` as source comment markers: sanctioned; the ban applies only to durable markdown.
- Rhino-rich host capture coexisting with host-neutral semantics: architecture, never duplication.
- Explicit match/dispatch arms over merged ternaries, and named intermediates on result rails: deliberate.
- A transitive central-manifest row carrying its `Rides <puller>` comment: proven indirect custody, never an orphan or unused-dependency finding.
- A missing-registration claim emitted without sweeping every README section cluster: void — a package registers once at its tier-correct section, and the single-cluster sweep is the false-missing engine that manufactures double rows.
- Re-litigating a `RULINGS.md` row — re-admitting its rejected package, folding its ruled-plural surface, re-homing its ruled owner, re-adding its retired structure: a false positive at every severity, the row itself the refutation.
- A single-member bump, routine bump-to-newest, or unify-the-versions demand against a version family ruled to move as a matched set — twin release tracks, one upstream wave, a channel a composed member requires: the demand is the defect, the ruling riding the RULINGS registry or pin comment.
- A cross-folder dedup demand against a package dual-registered under a ruled role split the strata force apart: false — each folder owns a distinct role, and removing either registration breaks the split.
- Bundle grants and loads judge by contract and consumption: a mandatory whole-set reference load whose every invocation consumes the set is the ruled flat-bundle design, the documented project-directory substitution is the sanctioned allowed-tools spelling (the skill-directory one binds skill content and bash injection alone), and a tool a bundle's mandatory reference reads or its ruled rails consume is load-bearing — task-matched partial-loading, portability-swap, and minimization demands against any of the three re-litigate the bundle law.
- A machinery demand the file's own standing mechanism already discharges is void — a streaming drain on a timeout-bounded one-shot, retry rails on a cheap probe, a per-scope lifecycle cell where one held gate already makes every ownership transition atomic, a propagation demand where every raise already crosses the boundary fence onto the typed fault with the single trapped exception class the one deliberate degrade — so the demand proves that mechanism absent or insufficient before naming a new cell; a success-masquerade claim against a ruled typed degrade whose result discloses the answering provider is void at the same bar, since disclosed degrade is not success; and a template header stating its fill-and-replace contract in the bundle's sibling-consistent schema is schema, never narration to delete.
