# [APPUI_TASKLOG]

Open and closed work for the product UI engine, distilled from the ideas in `IDEAS.md`. Each open card carries a status marker — `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` — plus the capability or file to build, the external packages to integrate, the integration points and boundaries or wires, and the key considerations. Live-host probes and tool-gated arms are tracked as blocked tasks against the substrate that unblocks them.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

[EMBED-PROBE]-[BLOCKED]: live Rhino embedding proves the shared AppUi GPU seam.
- Capability: live-host validation for `SurfaceHost.RhinoPanel`, `EmbedCapsule`, `SurfaceScheduler.Affinity`, and `RenderGraph.Lease` against the Rhino-owned AppKit run-loop and GPU pipeline.
- Shape: one `SurfaceSeam`-bound GPU lease carries `ISkiaSharpApiLease.TryLeasePlatformGraphicsApi`, `GRMtlBackendContext`/`GRVkBackendContext`, and `SKRuntimeEffect` into the `GpuBackend` `RenderTargetFactory` rows without per-host render arms.
- Unlocks: meshlet, path-trace, live `Taa`/`Fsr`, and embedded-panel frame-receipt proof while preserving the CPU/2D-Skia fallback already de-risked against a windowed `GRContext`.
- Anchors: `.planning/Shell/hosts.md#07research`, `.planning/Render/viewport.md#09research`, `.api/api-avalonia-skia.md`, `.api/api-skiasharp.md`, `.api/api-avalonia-gpu-interop.md`, `SurfaceSeam.OnUiThread`, `GpuBackend`, and `RenderTargetFactory`.
- Tension: no integrated running RhinoWIP host surface exists yet, so pump coexistence, resize sync, render-backend contention, and host-owned shared-context spelling remain blocked.

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[CONTROLS_INTENT_UNION_PAGE]-[COMPLETE]: `Shell/controls.md` `[02]-[CONTROL_INTENT]` names the closed eighteen-case `ControlIntent` `[Union]` (button/text/number/date/path/select/slider/toggle/radio/grid/tree/menu/toolbar/tab/accordion/panel/dock/splitter) with each kind's typed shape plus the `IntentBinding` command/token/automation column and the `ControlFault` 4200-band family, and `[05]-[TS_PROJECTION]` the `ControlIntentWire`; Ripple: csharp:Rasm.AppUi/Controls [ONE_UI_INTENT_WIRE].
[LAYOUT_CONSTRAINT_ALGEBRA_PAGE]-[COMPLETE]: `Shell/solver.md` `[02]-[CONSTRAINT_ALGEBRA]` names the `LayoutVar`/`LayoutTerm`/`LayoutExpr`/`LayoutRelation`/`LayoutStrength`/`LayoutConstraint` vocabulary over `Kiwi` operator algebra and `[03]-[LAYOUT_PRESETS]` the flex/grid/auto-layout `LayoutPreset` rows expanding to `ConstraintProgram`, with `[05]-[TS_PROJECTION]` the ordered `LayoutProgramWire` (introduction order + edit set + suggestion sequence); Ripple: csharp:Rasm.AppUi/Layout [ONE_UI_INTENT_WIRE].
[APPUI_VIEWPORT_SILHOUETTE_CONSUMPTION]-[COMPLETE]: `Render/drafting.md` `[02]-[PROJECTION]` `Viewport2D.Project` tags the Fabrication `Silhouette` `Edge3` set as the first-class emphasized `EdgeStyle.Silhouette` (0.7-weight outline, not folded into `Visible`) over the `HiddenLineSeam` delegate, the consumption contract forward-ready so the Fabrication `CSG_WATERTIGHT_SILHOUETTE` deepening flows through the same seam with zero AppUi change; Ripple: csharp:Rasm.Fabrication/Posting/projection [CSG_SILHOUETTE].
[RENDER_LAYEREDBSDF_PATHTRACE]-[COMPLETE]: `Render/pathtrace.md` `[BSDF_SHADING]` realizes the `BsdfShading` integrator shading FROM the Materials `LayeredBsdf.Sample`/`SlabStack.ToLayered`/`MaterialGraph.Evaluate` `SurfaceShade` via `ShadingFrame.ToWorld` and the MIS-balanced lobe sample, with the position-free multi-scatter and spectral-conductor high-fidelity routes as Materials-owned `LayeredBsdf` policies (no Render-side lobe math); CPU reference path tracer ships, GPU dispatch SPIKE-gated; Ripple: csharp:Rasm.Materials [SPECTRAL_REFLECTANCE_GROUNDING]/[BSDF_PAGE_SPLIT].
[ARCHITECTURE_PROHIBITIONS_BLOCK]-[COMPLETE]: `ARCHITECTURE.md` `[05]-[PROHIBITIONS]` authored — the closed NEVER list naming the concrete owner foreclosing each pattern (runtime XAML, per-host `GpuBackend`/`GRContext`, per-surface `AsyncImageLoader`/sink, `SKSurface` outside `Offscreen`, ReactiveUI code-behind binding, second command/hotkey registry, parallel control/layout framework, per-surface virtualizer, generic `IReceipt`, second revert vocabulary, second BCF owner).
[CONTROLS_MATERIALIZE_FOLD_PAGE]-[COMPLETE]: `Shell/controls.md` `[03]-[MATERIALIZE_FOLD]`/`[04]-[CONTROL_RECYCLING]` realize the one `ControlFactory.Materialize` fold (total generated `Switch` over the eighteen-case `ControlIntent`), the single `BehaviorRail.Intent` `ICommand` bridge, `Token`-resolved visuals, total `AutomationProperties.SetName` derivation, and `MaterializePool`/`RecycleScope` recycling over the `VirtualWindow`.
[SCREENS_CONTROL_STREAM_DEEPEN]-[COMPLETE]: `Shell/screens.md` `[06]-[CONTROL_STREAM]` adds the `ScreenCatalogRow.Body` generative control-intent body column and the `ScreenWire.Wire`/`Compose` stream that materializes through `ControlFactory` with `RecycleScope` recycling and `Calm` pacing, deleting the per-screen XAML literal body.
[LAYOUT_SOLVER_PANEL_PAGE]-[COMPLETE]: `Shell/solver.md` `[04]-[SOLVER_PANEL]` realizes the one `LayoutSolver` custom Avalonia `Panel` folding the `Kiwi` `Solver` solve into `MeasureOverride`/`ArrangeOverride` via the guarded `TryAddConstraint`/`TryAddEditVariable`/`TrySuggestValue` family, reading solved positions through `VariableEnv.ValueOf` after `Solve` flushes the row constants.
[VIRTUALIZATION_WINDOW_OWNER_PAGE]-[COMPLETE]: `Shell/virtualization.md` realizes the `VirtualWindow<TItem,TKey>` owner over `DynamicData.Virtualise`/`Page`, the `ExtentLedger` O(log n) measured-extent prefix tree, `StickyProjection` overlay, and the `HierarchyFlatten` `TransformToTree` bridge — the one fabric every windowed surface consumes.
[VIRTUALIZATION_TREE_FLATTEN_DEEPEN]-[COMPLETE]: `Editing/tables.md` `[03]-[TREE_FLATTEN]` routes the `TreeFlattened` arm through `HierarchyFlatten.Flatten` and the `Virtualized` arm through `VirtualWindow.Realize`, moving the `TransformToTree`-plus-recursion to the shared owner with the column-metadata family and sibling-order comparer staying tables-owned (zero capability lost).
[INPUT_FABRIC_DEVICE_FOLD]-[COMPLETE]: `Shell/input.md` `[05]-[INPUT_FABRIC]`/`[06]-[DEVICE_DRIVERS]`/`[07]` realize the `InputDevice`/`DeviceOutput` `[Union]` + `InputFabric` device-to-intent and intent-to-device fold over the one `CommandIntent` table, with the four SDK boundary capsules (`HidSharp`/`Silk.NET.Input`/`Silk.NET.SDL`/`Melanchall.DryWetMidi`) normalizing raw reports to `DeviceAxis` at the edge.
[FORMS_SCHEMA_WIZARD_PAGE]-[COMPLETE]: `Editing/forms.md` `[02]-[FORM_SCHEMA]`/`[03]-[WIZARD_FLOW]` realize the `FormSchema`/`FormField`/`WizardFold` materialized through `ControlFactory`, validated through the applicative `Validation<Error,T>` lift, with conditional visibility over the admitted PropertyModels `[PropertyVisibilityCondition]`/`[DependsOnProperty]` annotations (no form framework minted).
[SELECTION_BATCH_EDIT_PAGE]-[COMPLETE]: `Editing/forms.md` `[04]-[SELECTION_MODEL]`/`[05]-[BATCH_EDIT]` realize the `Selection<TItem>` over the admitted `ICheckedList` (`SetChecked`/`SetRangeChecked`/`Items`) and the `BatchEdit` fold to one combined `CommandReceipt` via `CommandExecution.Combine`, each child sealing its own receipt under one `CorrelationId`.
[EDIT_HISTORY_REVERTIBLE_PAGE]-[COMPLETE]: `Editing/history.md` realizes the `RevertibleOp` forward/inverse algebra and the `RevertScope` two-arm fold over the admitted `CancelableCommandRecorder`/`GenericCancelableCommand` (`MaxCommand=20`) client window and the durable `Rasm.Persistence/Sync` `OpLogEntry` inverse stream keyed by `ContentIdentity`, sealing `EditReceipt` with `EditOutcome.Reverted`; Ripple: csharp:Rasm.AppHost [ONE_REVERT_VOCABULARY].
[RICHTEXT_MARKDIG_RENDERER_PAGE]-[COMPLETE]: `Editing/media.md` `[02]-[MARKDOWN_INLINES]` realizes the `MarkdownInlineRenderer` retained materialization over the `Theme/typography` `MarkdownRow`/`InlineRun` rows (total generated `.Switch` over the closed family) into `Avalonia.Controls.Documents` inlines styled through `TextStyleRow.Resolve`, code-fence handed to the `CodePane`, no re-parse.
[MEDIA_SURFACE_CODEC_PAGE]-[COMPLETE]: `Editing/media.md` `[03]-[MEDIA_SURFACE]`/`[04]-[PLAYBACK_TRANSPORT]` realize the `MediaSurface` `[Union]` codec rows (image via `AsyncImageLoader` single cache, video/audio via `HanumanInstitute.LibMpv.Avalonia` OpenGL path) mounting on the one `SurfaceSeam`, and the `PlaybackTransport` rail folding verbs onto typed `MpvContext` members.
[RENDER_SHADING_ASSET_PAGE]-[COMPLETE]: `Render/shading.md` realizes the `ShaderAssetCache` keyed per `GpuBackend` (`SKRuntimeEffect.CreateShader` Ganesh, wgpu `RenderPipeline` Wgpu) and the `ShaderShade` pass consuming the Materials `LayeredBsdf -> SurfaceShade` as `ShadeUniforms` on the shared `Wgpu` device; CPU reference shade ships, GPU compile SPIKE-gated.
[RENDER_IMMERSIVE_OPENXR_PAGE]-[COMPLETE]: `Render/immersive.md` realizes `ImmersiveSession`/`XrFrame`/`XrInput`/`Passthrough` over `Silk.NET.OpenXR`+`Extensions.FB` sharing the one `Wgpu` device through the graphics binding, the predicted-display-time stereo frame loop submitting one `CompositionLayerProjection`, the action-set controller folding to `CommandIntent`, and the flat-fold fallback when the loader is absent.
[VIEWPORT_GODPAGE_SPLIT]-[COMPLETE]: the 750-line `Render/viewport` god-page split into `Render/{pipeline,meshlets,pathtrace}` (each fence-complete, SPIKE-gated, capability conserved), `viewport.md` reduced to a redirect stub, and the `ARCHITECTURE.md` codemap updated to the three owners with no `viewport` node.
[ISSUES_RELOCATE_TO_BIM]-[COMPLETE]: `Editing/issues.md` retains only the board PROJECTION — `Issue`/`CommentThread` CRDT/`IssueTile`/`IssueBoard` composing the `Rasm.Bim/Review/issues` `BcfTopic`/`BcfComment`/`BcfViewpoint` contract consumed at the package edge via `ViewpointCodec.FromBcf`, with the BCF semantics owned by `Rasm.Bim` (no second BCF model or `.bcfzip` writer in AppUi); the `Editing/issues <- csharp:Rasm.Bim/coordination [PORT]` consumption seam is in `ARCHITECTURE.md [02]-[SEAMS]`.
[KIWI_ADMISSION]-[COMPLETE]: admitted `Kiwi` 3.0.0 (Apache-2.0, pure-managed incremental linear-arithmetic constraint solver) as the `Shell/Layout` constraint-layout solver; pinned + referenced; README `[LAYOUT]` group and `.api/api-kiwi.md` authored.
[LIBMPV_ADMISSION]-[COMPLETE]: admitted `HanumanInstitute.LibMpv` + `HanumanInstitute.LibMpv.Avalonia` 0.10.1 as the Avalonia-12 `MediaSurface` (OpenGL render path); pinned + referenced; README `[MEDIA]` group and `.api/api-libmpv.md` authored; the libmpv native is provisioned at the app-host distribution layer.
[INPUT-FABRIC-SDKS]-[COMPLETE]: admitted the cross-platform net10 input SDKs — `HidSharp` (SpaceMouse/HID), `Silk.NET.Input` (gamepad), `Silk.NET.SDL` (haptic/rumble), and `Melanchall.DryWetMidi` (MIDI); pinned + referenced; README `[INPUT_FABRIC]` group and `.api/api-hidsharp.md`, `api-silk-input.md`, `api-silk-sdl.md`, `api-drywetmidi.md` authored. Eye-gaze, switch-access, speech-to-text, translation, and robotics have no viable cross-platform net10 NuGet and stay out of scope.
