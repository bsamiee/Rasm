# [RASM_BIM_TASKLOG]

Open and closed work distilled from `IDEAS.md`. `[01]-[OPEN]` carries task cards with `[QUEUED]`, `[ACTIVE]`, or `[BLOCKED]` leaders; `[02]-[CLOSED]` carries `[COMPLETE]` or `[DROPPED]` cards. One idea spawns one or more tasks; each task names the exact sub-domain or file it lands in.

OPEN contains `ACTIVE` work and `QUEUED` next-up work in logical sequence; `BLOCKED` keeps open but non-actionable work; `CLOSED` separates finished `COMPLETE` items from unimplemented `DROPPED` items. `Ripple` names the origin or counterpart card a cross-folder entry pairs with. `Atomic` flags a minor-scope task so a later session sizes its turn correctly and does not overscope a batch of small items.

## [01]-[OPEN]

<!-- source-only: open idea card template:
[ID]-[STATUS]: <ambitious concise thesis>.
- Capability: <higher-order concept, invariant, or owner capability>.
- Shape: <what the idea becomes as a system, product, owner, or feature set(s)>.
- Unlocks: <new branch, package, workflow, proof, user, or agent capability made possible>.
- Anchors: <owners, seams, packages, doctrines, or techniques that make the idea plausible>.
- Tension: <only when an unresolved constraint, boundary, bet, or dependency shapes the idea>.
- Ripple: <origin/counterpart card this entry pairs with across folders, as `pkg` `[SLUG]`; present only on a cross-folder ripple counterpart card>.
- Atomic: <present only on a minor-scope task; one short phrase naming the small unit so a later session does not overscope its turn>.
-->

## [02]-[CLOSED]

<!-- source-only: closed task card template:
[ID]-[COMPLETE|DROPPED]: <one-line disposition>; keep closed tasks collapsed unless a second retained fact changes future routing.
-->

[QUANTITY_DERIVE_FOLD]-[COMPLETE]: `properties#PROPERTY_SETS` replaces the no-op `QuantitySet.Derive` with a per-IfcClass base-quantity fold over `GeometryHandle` through the `BaseQuantityTable`, threads `PropertyKey.Resolve` over the live `BsddClass.Properties` dictionary rows (dictionary-wins over the static anchors), and collapses `KindOf` onto the `UnitsNet` `UnitParser`/`Quantity`/`ToUnit(UnitSystem.SI)`/`UnitMath.Sum` SI-base resolver.
[CRITICAL_PATH_NETWORK]-[COMPLETE]: `schedule#CRITICAL_PATH` `ScheduleCpm.Schedule` runs Kahn topological order then forward/backward `CriticalPath` folds over the `SequenceRel` DAG (the `Switch` arm shifting `FinishToStart`/`StartToStart`/`FinishToFinish`), deriving early/late start-finish + total/free float + `IsCritical`, a cycle lowering `BimFault.ModelRejected`.
[CALENDAR_WORKING_TIME]-[COMPLETE]: `schedule#CRITICAL_PATH` `WorkCalendar.Of`/`Advance`/`Recede` folds `IfcWorkCalendar` `WorkingTimes`/`ExceptionTimes` (`IfcWorkTime`/`IfcRecurrencePattern` `WeekdayComponent`) onto a NodaTime working-time function over `LocalDate`/`LocalTime`/`IsoDayOfWeek`, a duration-driven task finishing past the non-working days.
[RUNTIME_4D_SCHEDULE_ANIMATION]-[COMPLETE]: `export#SCHEDULE_ANIMATION` `BimExport.AnimateSchedule` bakes the `ScheduleNetwork` task intervals into per-element visibility/scale keyframe tracks via the decompile-verified `ModelRoot.CreateAnimation` + `Animation.CreateVisibilityChannel`/`CreateScaleChannel` surface over `KHR_node_visibility`, sharing the `ConstructionState.At` time axis; Ripple realizes `SCHEDULE_NETWORK_DEPTH`.
[EARNED_VALUE_CHANGE_ORDER]-[COMPLETE]: `cost#EARNED_VALUE` adds the `ChangeOrder.Apply` priced-revision fold over a baseline, a `Contingency.Drawdown` reserve column, and the `CostPerformance.EarnedValue` fold (BCWS/BCWP/ACWP, CPI/SPI, EAC/VAC) joining the `ConstructionTask` actual/scheduled progress onto a typed `EarnedValueReport`.
[ASSEMBLY_REL_EXTRACTION]-[COMPLETE]: `import#IMPORT_RAIL` `Decomposition` extracts all five `AssemblyRel` arms (`IfcRelAggregates`/`IfcRelNests`/`IfcRelContainedInSpatialStructure`/`IfcRelVoidsElement`/`IfcRelConnectsElements`, member spellings verified) and `structure#ASSEMBLY_TREE` `BimAssembly.Traverse(globalId, TraversalDirection)` is the one direction-discriminated breadth-first fold over the complete graph.
[MEP_INTERFERENCE_CHECK]-[COMPLETE]: `systems#INTERFERENCE` `InterferenceCheck.Interferences` pairs distribution-member + structural geometry through the injected `GeometryProximity` kernel seam (host-neutral `GeometryHandle` AABB prune + signed-gap test), emitting ranked `Interference` rows (hard/clearance, cross-discipline weighted) feeding `coordination#COORDINATION` `ClashProposal`; Ripple realizes `COORDINATION`.
[AUDIT_TRAIL]-[COMPLETE]: `diff#AUDIT` `AuditTrail.Fold` chains the per-version `ModelDiff` `ElementChange` sets into a content-addressed `AuditEntry` Merkle chain (each `EntryKey = XxHash128(prior:content)`), with `For(globalId)` lifecycle query and `Verify()` tamper witness; the commit-DAG half of `VERSIONED` stays open.
[INCREMENTAL_DELTA_REIMPORT]-[COMPLETE]: `import#IMPORT_RAIL` `BimIo.Reimport` re-projects only the changed elements against a prior `BimModel` through the `Review/diff#MODEL_DIFF` `ElementFingerprint` content-key, reusing unchanged rows by reference (no re-tessellation) and emitting the `ReimportResult` patched model + `ModelDiff` in one fold.
[ROUNDTRIP_VERIFICATION_MATRIX]-[COMPLETE]: `export#ROUNDTRIP` `RoundTrip.Verify`/`Matrix` folds an export-reimport cycle over the IFC STEP/XML/JSON triad through the settled `ExportIfc`/`ImportIfc` owners into a per-format `RoundTripReport` keyed on the `ElementFingerprint` content-key, never a second comparison surface.
[ACADSHARP_MANAGED_DWG]-[COMPLETE]: `format#FORMAT_AXIS` `Dwg` row routes `.dwg`/`.dxf` to the managed `InterchangeCodec.AcadSharp` (companion=false) and `import#IMPORT_RAIL` `BimIo.AcadGeometry` folds the decompile-verified ACadSharp `CadDocument` mesh-bearing entities onto `ImportedGeometry`; ACadSharp admitted to `Rasm.Bim.csproj` + `.api/api-acadsharp.md`; the central pin pre-exists; Ripple: `Rasm.Fabrication`/`Rasm.AppUi` (shared owner).
[PANEL_COVERING_EGRESS]-[COMPLETE]: verified — the `Model/elements#IFC_CLASS` `IfcClass` roster already admits every panel-family predefined token the `Rasm.Materials` `Projection/component#COMPONENT_PROJECTOR` panel arm stamps, so the `Projection/egress#IFC_EGRESS` `Emit` gate re-authors a `ComponentClass.Panel` Type with no `IfcClass` row growth: the `Covering` row valid set carries `CLADDING`/`CEILING`/`INSULATION` (gypsum·cement·rigid-board covering), the `Plate` row valid set `SHEET` (wood structural sheet + steel roof/form deck), the `Slab` row valid set `FLOOR` (composite floor deck), all `IfcSchema.Ifc2x3`-spanned, so `IfcClass.AdmitPredefined` admits each token against the frozen valid set AND the schema span without a `BimFault.UnmappedClass` ([C6][H8]). The panel is an `IfcBuiltElement`, so its neutral `DetailSchema.Realization` bag round-trips through the GENERAL `Projection/egress#IFC_EGRESS` `ReauthorProperties`/`Projection/semantic#SEMANTIC_PROJECTOR` `Bags` fold, never the `connection#CONNECTION_DETAIL` realizing-element reader; no `elements.md`/`egress.md` edit needed. Ripple: Rasm.Materials [PANEL_PAGE_AUTHOR].
[BIM_CONNECTION_IFC_WIRE]-[COMPLETE]: `connection#CONNECTION_DETAIL` `ConnectionProjection.Detail`/`Bag`/`All` (composed by `semantic#SEMANTIC_PROJECTOR` `Project`) lowers the whole GeometryGym realizing surface (`IfcMechanicalFastener`/`IfcFastener`/`IfcReinforcingBar`/`IfcReinforcingMesh`/`IfcTendon`/`IfcTendonAnchor`/`IfcBearing`, fastener/tendon nominal diameter recovered through the associated `IfcMaterialProfileSetUsage` `IfcCircleProfileDef.Radius` since `mNominalDiameter` is GeometryGym-internal, decompile-verified) onto the content-keyed `Rasm_ConnectionRealization` seam `PropertySet` bag bound by an `Assign.PropertyDefinition` edge — the joint topology riding the `Connect(ConnectKind.Realizing)` edge `semantic#RELATION_ALGEBRA` `EdgeProjection` authors, the egress the `semantic#IFC_EGRESS` `Emit` generic `ReauthorProperties`/`ReauthorRelationships`. The `ConnectionWire.Author`/`ConnectionItemWire` second wire is RETIRED (the deleted form `connection#CONNECTION_DETAIL` EGRESS_AND_WIRE_RETIREMENT names), not pending: the Materials `ConnectionItem` axis reaches Bim through its OWN `Connection/connection#CONNECTION_PROJECTOR` `ConnectionProjector : IElementProjection` (the `Projection/material` `MaterialProjector` sibling, §4D/§6 anticipated and now REALIZED) authoring the IDENTICAL bag onto the seam. The material/composition/property/classification egress reads the projected seam `Material` subgraph through `composition#MATERIAL_COMPOSITION` `MaterialProjection.AuthorComposition`/`AuthorUsage` ([C7] `IfcMaterialLayerSetUsage`/`IfcMaterialProfileSetUsage` + `IfcMaterialProperties` `Pset_Material*`) + `classification#CLASSIFICATION_AXIS` `ClassificationSystem.Author` (`IfcClassificationReference`), composed by `semantic#IFC_EGRESS` `Emit` `ReauthorMaterials`/`ReauthorClassifications` (egress ctors decompile-verified, GeometryGym 25.7.30); the `AuthorMaterial`/`AuthorAppearance` consumers + the `MaterialAssignmentWire`/`MaterialPropertyWire`/`ConnectionItemWire`/`ConnectionWire` carriers are all DROPPED. Ripple: `Rasm.Materials` [JOINT_CONNECTION_FAMILY] (material + connection wires retired, `ConnectionProjector` realized).
[BIM_COORDINATION_ANNOTATION]-[COMPLETE]: `issues#BCF_ARCHIVE` owns the `BcfTopic`/`BcfComment`/`BcfViewpoint` (`Rasm.Bim.Coordination` namespace) record family, the `.bcfzip` `BcfArchive.Read`/`Write` fold over `ZipArchive`/`XDocument`, and the `BcfApi` REST projection riding Compute transport; Persistence keeps the durable op-log/CDE-sync joined by `GlobalId`; Ripple: `Rasm.Persistence` [ANNOTATION_RELOCATE_TO_BIM].
[BIM_COORDINATION_ISSUES]-[COMPLETE]: `coordination#SIGN_OFF` grounds the issue-board domain — the `SignOff` `[SmartEnum]` lifecycle state machine (legal forward set per-case data, polymorphic `Advance`) over `BcfStatus` and `IssueBoard.Of` partition over the `BcfTopic` contract — AppUi keeping only the board projection; Ripple: `Rasm.AppUi` [ISSUES_RELOCATE_TO_BIM].
[BIM_SCHEDULE]-[COMPLETE]: `schedule#SCHEDULE` grounds the host-neutral 4D/CPM domain — `ScheduleProjection.Project`/`ProjectAll` over `IfcWorkPlan`/`IfcWorkSchedule`/`IfcTask`, the `CriticalPath`/`WorkCalendar` folds over the IFC `SequenceRel` network, and `ConstructionState.At(Instant)` snapshot — federating to the Persistence durable P6/MS-Project store by `GlobalId`; Ripple: `Rasm.Persistence` [SCHEDULE_RELOCATE_TO_BIM].
[T-BSDD-TRANSPORT]-[COMPLETE]: `classification#BSDD_RESOLUTION` realizes `BsddResolution.Resolve` over the injected `BsddPort.Fetch` with `BsddClass.Of` projection and the `LocalShape` unreachable-degrade; live fetch binds at the composition edge; Ripple: `Rasm.Compute` [COMPUTE_BSDD_PORT_TRANSPORT].
[T-IDS-IFCTESTER-COMPANION]-[COMPLETE]: `validation#IDS_FACETS` realizes the in-process `IdsSpecification.Audit` self-audit plus `IdsAudit.Reconcile`→`IdsParity` on the (GlobalId, `FacetKey`) join over the returned `IdsVerdict` oracle rows; Ripple: `Rasm.Compute` [COMPUTE_IFCTESTER_ORACLE_RPC].
[T-IMPORT-CODECS]-[COMPLETE]: `import#IMPORT_RAIL` imports every managed mesh-text (`geometry3Sharp` OBJ/STL/OFF + in-process `PlyReader`/`ThreeMfReader`) and the in-process `StepReader` Part-21 semantic graph, faulting `import-needs-companion` for `iges-ansi`/Revit/Navisworks by the `TessellationRequiresCompanion` row column; DWG deferred to `[ACADSHARP_MANAGED_DWG]`.
[TESSELLATION_OUTCOME_RECEIPT]-[COMPLETE]: `tessellation#TESSELLATION_BRIDGE` returns the typed `TessellationOutcome` (re-imported `ImportedGeometry`, `ArtifactKey`, dual `ContentKey`/`SourceKey`, `Deflection`, `TessellationOrigin`, `At`) on `Fin`; `Resolve` reads the content-addressed `TessellationStore` by `ArtifactKey` cache-before-cross-and-store-before-return over the injected `TessellationStore`/`TessellationCompanion` ports, `Plan` carries the typed `TessellationSettings`/`TessellationScope` the companion maps, a non-companion source format is the `BimFault.CapabilityMiss` gate, a companion/store-unreachable miss the `BimFault.CodecReject` degrade, and a degenerate re-import the kernel `GeometryFault.DegenerateInput`.
