---
title: Rasm API Stacking Census
input: full_diff
effort: high
conclusion: neutral
include:
  - "libs/**"
---

# [API_STACKING]

Bidirectional capability closure is this pass's one question: whether every touched fence stacks admitted packages and host SDKs to their deepest contract-complete form and consumes every declared capability. Run the census against the folder `.api/`, language `.api/`, and central manifest before any finding. Approved leverage proves the exact invocation, return shape, complete provider roster, owner input contract, and preserved semantics; a partial catalogue fragment or an operator lacking the owner's deterministic walk, geometry, precedence, or custody contract grounds no demand.

Hunt these classes, each finding naming file, anchor, the shallow or hand-rolled form, and the exact catalogued member demanded:
- Handroll: a fence body re-deriving an admitted operator's exact contract — proven leverage includes `Try.lift(...).Run()` for provider exception custody, `TensorPrimitives.SumOfSquares` for reductions, QuikGraph connected-component operators for admitted graphs, and `ArrayPoolBufferWriter<byte>` for framed canonical emission; the demand names the member, canonical owner, and smallest replacing range.
- Under-stacking: a shallow composition leaving a deeper verified member idle — proven leverage includes NodaTime mapping resolvers for gap/overlap policy and `FindMinimum(..., scales:, isFixed:)` for heterogeneous inverse coordinates; the extension lands as a row, case, or member on the existing owner, never a parallel surface.
- Unconsumed capability: a rail-verified host or package member that strengthens a touched owner and sits unconsumed — the extension lands as a row, case, or member on that owner, never a parallel surface.
- Decorative declaration: a capability noun with no anchoring fence member, a receipt nothing mints, or a policy row nothing consumes — demand the consuming member or the deletion; a policy column consumed only by a cache key, receipt, or log annotates rather than governs, and an arm that cannot honor a policy field refuses the combination loudly before provider work.
- Charter-gap ideation: a catalogued capability that unlocks what the folder charter implies yet no touched page carries — the gap names the member and existing owner, while an unresolved invocation, incomplete vocabulary, missing precedence law, or unproven consumer contract returns no finding.

Settled corpus law outranks intuition: a composition the owning page or `.api/` catalogue rules is never a finding, and a manifest-admitted package under a temporary distribution gap stays design-real. A pass that finds nothing after a genuine census states that verdict plainly.
