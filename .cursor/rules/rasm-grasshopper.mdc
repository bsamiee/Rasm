---
description: Rasm Grasshopper: GH2 components, typed ports, Shape conversion, GhUi, and host lifecycle rails
globs: "libs/csharp/Rasm.Grasshopper/**/*.cs,apps/grasshopper/**/*.cs"
alwaysApply: false
---

# Rasm Grasshopper

Read `libs/csharp/Rasm.Grasshopper/AGENTS.md` and verify GH2 APIs through local RhinoWIP XML/decompile when uncertain.

## Component Rails

- Keep component definitions type-owned through `IComponentDefinition<TSelf>.Definition`.
- Use `ComponentSpec.Of`, `Port`, `Port<TVal>`, `PortPolicy`, `Output`, and `Shape` as the public component/data rails.
- Preserve GH2 tree, path, conversion, disposal, unsupported/empty, diagnostic, and ownership-transfer evidence.
- Keep apps such as `Radyab` thin; library boundaries set capability.

## UI Rails

- Keep `GhUi` as the request constructor and `GrasshopperUi.Use` as the executor.
- Extend existing algebras: `CanvasOp`, `DocumentOp`, `EditorOp`, `InputRequest<T>`, `UiEvent`, `LayoutOp`, `PaintRequest<T>`, and `WireOp`.
- Do not add wrapper services, WinForms, WPF, polling replacements, or parallel UI frameworks. Extend current component, `Shape`, `GhUi`, and `GrasshopperUi` owners instead of reviving removed wrapper-only surfaces.

## Validation

Use `uv run python -m tools.quality static fix [paths...]` for cleanup. Add `uv run python -m tools.quality static build [paths...]`, managed GH tests, or `uv run python -m tools.quality bridge verify <scenario-or-glob>` when compile, managed behavior, or runtime behavior crosses those rails.
