# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json
id: require-write-lines-when-different
language: xml
severity: error
rule:
    matches: msbuild-tag
    pattern: _TAG
    all:
        - has: {kind: Name, regex: '^WriteLinesToFile$'}
        - has: {matches: {msbuild-attribute: {name: {regex: '^Overwrite$'}, value: {regex: '(?i)^["'']\s*(true|on|yes|!false|!off|!no)\s*["'']$'}}}}
        - not: {has: {kind: Attribute, has: {kind: Name, regex: '^WriteOnlyWhenDifferent$'}}}
transform:
    RESULT: replace($TAG, replace='(/?>)$', by=' WriteOnlyWhenDifferent="true"$1')
fix: $RESULT
message: 'The replacement write updates the timestamp when the content is unchanged, the form of WriteOnlyWhenDifferent="true" on the write'
note: 'An append and a deliberate timestamp update keep Overwrite without the flag'
